# HANDOFF #126 — #275 → #276 — THE PRINCIPLES AND THE SECOND SEAT

> ⚠ **This file is NEWER than `_CHAIN.md` and OUTRANKS it** (and `_HANDOFF-125`). Written by the #275 conductor (Fable) at FILL ≈168,000 real — under the 180,000 working line — BEFORE the delegated wrap. Every claim is a receipt.

## ⛔ READ FIRST, IN THIS ORDER
1. This file. 2. `_CHAIN.md`. 3. `python3 knowledge/_parked.py --check` (3 of 18 due: P-274-1/2/3). ★ `pip install tiktoken --break-system-packages` before `_checkin.py` (its `--plant --session` flag does NOT exist — the memory hook is stale). ★ `.git/index.lock` was stale at the opener: `allow_cowork_file_delete` then `rm -f`. ★ Stale-mount check: `git log --oneline -3` vs `ls -la --time-style=full-iso _CHAIN.md`.

## ⛔⛔ DAVE'S — VERBATIM. Source: `notes/_lanes/275/DAVE-RULINGS-2026-09-15.md`
- *"gm"* → *"go"* on the principles lane (the conductor's recommendation; STEP 3 of s269-D1).
- *"can you surface the page please"* → the review page presented as a file (Artifact publish is refused in Cowork; the file is the surface).
- His page export 16:27Z: RP-1..6 all **a**, no notes → read back → *"go"* → **`s275-D1..D6`** `ffcb029` (578 → 584). `s275-D2` is the ratifying id.
- In the PARALLEL #274 chat (its commit `5dd19ee`, 17:25 BST): *"so go with the recommendations you think?"* — corroboration, recorded, NOT the source of the rulings.

## ⛔ TWO SESSIONS ON ONE REPO — NEW HAZARD, RULING-SHAPED, NOT RULED
The #274 session was still open while #275 ran. It committed `5dd19ee` into `notes/_lanes/275/`, overwriting lane RP's page-builder input `principles-kg-decisions-2026-09-15.json` with a chat-derived pseudo-export (−71 lines); then #275's `Write` of `DAVE-RULINGS` overwrote #274's one line. Both healed by addition in `49e2a44` (builder input restored from `2cbfbad`; the #274 record kept as `DAVE-EXPORT-from-274-chat-2026-09-15.json`; the line appended back). **Ask Dave for one word at #276: is one seat at a time the rule, or does the second seat write only under `notes/_lanes/<its own N>/`?** Do not rule it.

## ⛔ NOT RULED — DO NOT RULE
`--land --ratified` unlocks on ANY existing ruling id (RK and RP alike; a `governs` check would close it) · the explorer's `renderPanel()` shows a dug principle's relations but not its `statement` (one `if(n.statement)` span, RL2's finding) · `gen_kg_rules.py`'s "NO CONSUMER YET" line is STALE (explorer reads `_rule_nodes.json`) · RP-4's four name-only WCAG joins (authored later, s275-D4) · s275-D5's lane: WHICH components first is Dave's · `_kg_explorer.template.html` had to be in RL2's commit (the chip lives there; palette plum `#8A1A5C`/`#E68ABF`, not a third red) · 6 `data-grid` reds (#261 resolver FAIL(6)) · P-272-2..4 · showroom stale · push (≈27 local; push is his word).

## WHAT #275 DID — RECEIPTS (all LOCAL)
1. `ef1213b` fence: a landed `_rule_nodes.json` names `s274-D8` in `$description` (generator fixed in `gen_kg_rules.py:land()`, regen byte-identical but that span).
2. `77b043f` lane RP (Opus 285K): `knowledge/gen_kg_principles.py` (selftest 15 bites, `_mutate.py` 18/18 mutants — three bites were DEAD until the mutation harness found them) + dry-run 175 nodes / 111 edges / 27 declared nulls + review page RP-1..6 + screenshot.
3. `2cbfbad` lane RV (Opus 144K, read-only): 12/12 claims driven GREEN; two figures corrected by addition (my brief's "31 families" → measured 32; "60 live type strings" → 62).
4. `ffcb029` s275-D1..D6 inscribed BY TEXTUAL SPAN — the first attempt re-dumped `_rulings.json` (3,672-line diff, #179 class) and was reverted; the first commit also went out with an UNCHANGED `_rulings.json` (the splice had asserted on a missing trailing newline) and was amended within the minute — a green `_validate_kg` did not catch either.
5. `eb47a58` s275-D1..D6 ENACTED (lane RL2, Opus 183K): `knowledge/_ux_principle_nodes.json` 175 nodes / 111 edges / 15 null edges (the "27" = 15 + 12 edgeless polarities); s275-D1 checked field-by-field, 1,595 comparisons, 0 mismatches; explorer v1.11 → v1.12, chip "UX principles" (`uxprinciples`, OFF by default) + type chips `ux` / `polarity`; base graph pixel-stable (0 of 951 base nodes moved); gates `_validate_kg` OK · selftest PASS · `_validate_compose` PASS · resolver FAIL(6) inherited; chromium 0 console errors, 5 screenshots reviewed by the conductor (`notes/_lanes/275/principles-land/`).
6. `49e2a44` the two-seat heal (above).
**s269-D1 order: (1) roles DONE #273 · (2) rules DONE #274 · (3) principles + polarities DONE #275 · (4) icons then logos = NEXT · (5) edge types setIn / behaviourFrom / capturedFrom / acceptsCapability.**

## #276 — FIRST MOVES
0. Opener: has he pushed? The two-seat word (above). Read both title lines back.
1. **STEP 4 OF s269-D1: icons (666) then logos** — same RK/RL shape: one Opus lane → generator with dry-run + selftest + mutants + `--land --ratified`, review page ≤ 6 decisions, NOTHING landed until his export; verifier lane in the same wave; land lane on his word. Read `notes/_lanes/269/kg-gaps/A-inventory.json` for the icons family's measured shape first.
2. **s275-D5 authored lane** (metas cite the six laws + the rules they obey, one `meta.schema.json` diff) — waits on Dave naming which components go first.
3. **s273-D2 authoring pass two** (49 silent providers) — 3–4 lanes/session when the graph work pauses.
**Cautions:** `model: opus` every lane · verifier same wave · screenshot + text diff before presenting · textual span only (never re-dump a JSON) · never `git stash` · never `gen_kg_edges.py` · `_checkin.py` every ~10 turns · a commit's `--stat` is the receipt, not its message.

## STRUCTURAL REDS AT THE SEAM (wrap heals or declares)
`/sessions` disk 96.1% FULL at the opener (no session boots at 100%) · boot 82,485 = ELEVENTH breach, his · dirty instrument files (`_REHEARSAL-LOG.jsonl`, `_GRADE-DECISIONS.jsonl`) · resolver FAIL(6) = #261 · showroom stale · Memento index stale (today's notes) → 2g.
**Gauge:** boot 82,485 real · FILL 158,314 at the last check-in (before RL2's report) — expect ≈170,000 at the brief cut · subs: RP 285K + RV 144K + RL2 183K = 612K delegated (n=3).
