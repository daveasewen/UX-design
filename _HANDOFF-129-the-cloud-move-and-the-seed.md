# HANDOFF #129 — #278 → #279 — THE CLOUD MOVE AND THE SEED

> ⚠ **This file is NEWER than `_CHAIN.md` and OUTRANKS it** (and `_HANDOFF-128`, which it does NOT replace — #128's §"#278 — FIRST MOVES" is now #279's first moves, unchanged). Written by the #278 conductor (Fable 5.1) at FILL 138,006 real after a short housekeeping session. #278 did NO graph work by design: fill was spent on measuring the environment after the memory move, and the stop line (200K − 42K wrap − a lane step ≈ 150K) left no room for a lane.

## ⛔ READ FIRST, IN THIS ORDER
1. This file. 2. `_HANDOFF-128-the-constitution-and-the-thin-slice.md` — its "#278 — FIRST MOVES" and "STRUCTURAL REDS" sections are live and unchanged. 3. `_CHAIN.md`. ★ `pip install tiktoken --break-system-packages` before `_checkin.py`.

## ⛔⛔ DAVE'S — VERBATIM. Source: `notes/_lanes/278/DAVE-RULINGS-2026-09-16.md`
- The `s277-D13` clause is RULED by his word *"Ill go with you"*: **the thin slice is a SEED** — composed once at step 1 by `_compose_slice.py`, the session works from its own state; **ASK is the on-demand door** that reads the Constitution live for anything the seed lacks. Inscribe as `s278-D1` at this wrap.
- *"as long as it's safe we have fallen foul of silent and creeping failures in the past"* — his posture for the memory move.
- *"lets just move on to the other graph work"* — #279 opens on #128's first moves.

## WHAT MOVED — THE COWORK MEMORY DIRECTORY IS NOW THE CLAUDE.AI PROJECT MEMORY (import 2026-09-16 03:14–17Z, `sources: [cowork-import]`)
- Measured, not assumed: sandbox (aarch64 Ubuntu 22.04), FUSE mount, git, push, CI, delete-guard grant, chromium recipe, tiktoken-first — **all unchanged**.
- **Ruled by Dave (going with the recommendation): the repo is the record, cloud memory is the accelerator** — which is what `_RUNBOOK-capture-ritual.md` step 3 (lines ~591–622) already says; the rule has a new place to point at, nothing else. ⚠ There is NO `MEMORY.md` in the repo and never was — the old one was the Cowork local index.
- **The boot index did not come across.** The cloud store arrived as ~200 flat files + an empty `index.md`. The conductor started `index.md` with the #277 ★★★ line, newest-first, same shape as the old `MEMORY.md`. The pre-import index is preserved verbatim in `hook-overflow-2026-09-02-240` and `MEMORY-ARCHIVE`.
- **Only the conductor's seat can write cloud memory.** Delegated wrap subs cannot (the #275/#276 seat limit, now structural). Pattern, proven at #278: the wrap sub writes `notes/_lanes/<n>/WRAP-MEMORY-HOOK.md`; the conductor places it at the NEXT opener (cold seat, ≈4K tokens estimate). #277's hook was placed this way (`d7b8d72`). **Runbook line owed at step 3 — this wrap adds it by addition.**
- Store is account-wide on READ (other Projects' subtrees visible), Apollo-only on WRITE. Keep Apollo files Apollo-only.
- Cloud index file cap 49,152 B; compaction is still owed periodically, dream pass is the seat; not urgent (one line in it).
- Future risk is ONE-WAY: an Anthropic re-import could overwrite cloud-only lines; harmless while the repo holds everything durable.

## MEASURED AT #278
- **Boot 72,110 real** (n=1) vs the 70,794 `s129-D1` floor — +1,316, inside noise plus a hair. NOT a re-base; the floor's label should read "cloud listing replaces MEMORY.md" (≈8K either way). `BOOT_FIRSTTURN_TK` untouched.
- **FILL 138,006 real / 33 turns** at the check-in — spent on probes (GM header dump, push-method file, runbook excerpts). Declared, under amber.
- ⛔ **DISK `/sessions` 97 % — 324 MB free.** This session owns 8.9 MB; the rest is dead-session scratch, undeletable from here. `_gate_scratch_hygiene.py` ran clean. At 100 % no session boots (#227/#228 class). Nothing in the repo fixes it; the symptom is a session that will not start.
- 27 commits of #277 were UNPUSHED at the opener; pushed `3d320f3`, then `d7b8d72`. CI run 35092901923: `release` green; `gates` carrying the two standing reds (survey + knowledge build); `render` was still running at read-back.

## COMMITS THIS SESSION (all `SESSION_N=278 … --reconciled`, `SHOWROOM_ACK` for the standing 108-page stale showroom)
- `3d320f3` — the two `.t3-rendered` receipts #277's repair left untracked.
- `d7b8d72` — #277 memory hook PLACED, receipt appended to `notes/_lanes/277/WRAP-MEMORY-HOOK.md`.

## #279 — FIRST MOVES (= #128's, restated by pointer, not copied)
1. Land the icons: `gen_kg_icons.py --land --ratified s277-D4` after RIV's A3/A4 pre-land fixes. 2. The Constitution wave in order: reader + ASK → explorer relabel → scope → verbs → tokens. **The seed ruling above is now an input to the reader lane.** Fable on judgement lanes; Fable check + Fable verify against the live file is the shape. Lanes only — the conductor's window is for judgement.

## STRUCTURAL REDS AT THE SEAM — unchanged from #128, plus one
- #128's list stands (showroom 108 stale · resolver FAIL(6) · lane-ownership selftest 2/3 · `.git/_orphan-locks/` 72 entries, Dave's · A2-AUDIT "316" correction owed · compliance/README 31 vs 55).
- NEW: `/sessions` disk 97 % (above).
