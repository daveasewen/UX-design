# HANDOFF #130 — #279 → #280 — THE WAVE LANDS, AND THE GRAPH IS NOT THREE LAYERS

provenance: 279 · 2026-09-16
status: observed

## ⛔ READ FIRST, IN THIS ORDER

1. **THIS FILE.** It is newer than `_CHAIN.md` and **OUTRANKS** it. It does **NOT** replace `_HANDOFF-129` — #129's cloud-store findings and its § STRUCTURAL REDS stand — but #129's **"#279 — FIRST MOVES" list is SPENT: every move on it ran.**
2. `_CHAIN.md` (the read chain: GM header → ★ LATEST banner → `_LIVE-STATE.md`'s ⏱ LATEST delta).
3. `notes/_lanes/279/DAVE-RULINGS-2026-09-16.md` — **his words verbatim**, eight items, committed by this wrap.
4. Everything beyond that is **RETRIEVAL**, never a reading list: `python3 knowledge/_memento_search.py "<q>"` → `--fetch <id>`.

## ⛔⛔ DAVE'S — VERBATIM. Source: `notes/_lanes/279/DAVE-RULINGS-2026-09-16.md`

- On the disk at the opener: *"can we fix this now?"* — answered **no, not from inside**: the space is dead-session scratch owned by `nobody` and there is no root.
- On quota, mid-morning: *"BTW, we have lots of tokens to use over the next day and a half … use fable judgment liberally and subs, this work is critical and we need to squeeze as much out of my subscription"*.
- On the two icons-verify questions: *"1. I need to do the manual review of the 31"* · *"2. you'll have to give me the plain prose of this"*.
- On the pack question, after the plain prose: ***"i agree 'a' it is"*** ⇒ **`s279-D1`**, inscribed at this wrap.
- ⛔ On the explorer 1.16 screenshot: ***"BTW do we still have work to do on the graph... this does not look like 3 layers, just relabelling and grouping them isn't what I expected tbh."*** ⇒ **RULING-SHAPED, OPEN, NOT INSCRIBED.**
- At the close: *"keep an eye on the context window"*.

## THE ONE RULING — `s279-D1` (`knowledge/_rulings.json` 604 → 605, span `18  0`, ZERO deletions)

**The designer pack ships the reader and the Constitution, so ASK reads live in-pack.** designer-skills-v2 now ships `knowledge/_compose_slice.py`, `knowledge/_rulings.json`, the asset node files and `knowledge/guidelines/_scope.json`, so ASK — the on-demand door of `s278-D1` — resolves **inside the pack against the Constitution itself** rather than against a frozen excerpt. It **reverses** the deliberate exclusion in `knowledge/_gen_pack_manifest.py` by his word, is a release under **`P-269-1`**, and changes **neither** storage, **nor** the `s278-D1` seed contract, **nor** the twelve verbs of `s277-D11`. ⚠ **The Spider `v1.0.14` cut is NOT ruled here** — lane PK armed it version-gated (`READER_SHIPS_FROM = "v1.0.14"` / `READER_RULING = "s279-D1"`) because v1.0.13's manifest is RATIFIED (`s268-D3`), its zip frozen, and two BLOCKING arms pin the generator byte-for-byte; cutting Spider is Dave's word under `s219-D4(2)`.

## WHAT LANDED — ELEVEN OF THE THIRTEEN `s277` RULINGS, AND THE TWO THAT DID NOT

| ruling | commit(s) | what is now in the tree |
|---|---|---|
| `s277-D4..D7` icons | `f641242` + `84658db` | 688 nodes / 1,299 edges / 32 declared nulls; selftest 18/18; 26/26 mutants caught; the 32 nulls landed into the node files after the IV verify |
| `s277-D8` three views | `2c6b640` + `9b2e1b0` | explorer **v1.16** — four labelled boxes (SYSTEM · DESIGN GOVERNANCE · EXPLANATION · THE CONSTITUTION), `assets` shipping chip **OFF**, chip-off identity proven node-for-node against 1.14 |
| `s277-D9` scope | `9e7a158` + `e3facb4` | `knowledge/guidelines/_scope.json` **34 rows**, a 19-name facet set, rules reaching a component **107 → 408 of 470**, BLOCKING **9 → 52 of 59**, 27 per-rule overrides on 5 rows |
| `s277-D10` + `s277-D13`/`s278-D1` reader | `d6bd57b` | `_compose_slice.py` v1.0 wired as SKILL **step 1**; seed **31,372 cl100k** vs a pre-wiring step 1 of **1,005,758** = **32.06×**; ASK's twelve questions all ≤1K |
| `s277-D11` verbs | `eb2ff7c` | `_kg_verbs.json` — **12 verbs over 36 of 58 edge types**, **2,351 of 3,136 edges walked = 75%** |
| `s279-D1` pack | `df33a24` + `3710d6b` | designer-skills-v2 **v2.1**, 849 → **1,102 files**, read closure **218 paths with 0 outside the pack**, ledger `e1d8019b97cc` → `ae920995c32e` |
| ⛔ **`s277-D12` tokens at group+tier** | **none** | ⛔ **NEVER STARTED** — no lane was cut and no line exists. #280's third move. |
| `s277-D6` the 31 `-active` edges | `f641242` | **AS LANDED** (reading (a)) — his manual review came back on the sheet and is **UNREAD BY THE RECORD**, below. |

Also: `25e9e32` — the 15-base active-review sheet (lane AR, the `P-277-3` instrument). Twelve filed sub-reports at `notes/_subreports/2026-09-16-279-*` (AR · EV · EX · EX2 · IL · IV · PK · RD · RV · SC · SC2 · VB), each cited by path here per `s218-D7`.

## ⛔ #280 — FIRST MOVES, IN ORDER

1. **THE LAYOUT LANE — his question, and it is first.** *"this does not look like 3 layers."* `s277-D8` as inscribed says *storage untouched; chips and family labels change*, and EX/EX2 built exactly that and EV verified exactly that — **the letter was enacted and the letter was not what he wanted.** He wants **three visibly separated layers on the stage**. ⛔ **Put ONE CONCRETE OPTION ON A PAGE for his eye; do not describe one, and do not rule it.** Priced by lane EX2: the OFF-column band is **5,700 world units** between the base cluster (x ≤ 449) and the assets column (x ≥ 6,172); runtime column packing is **~25 lines** but it moves ghost positions, so chip-off canvas stops being pixel-identical to 1.15 — **its own lane, with pixel proof.**
2. **INSCRIBE HIS 15-BASE EXPORT.** `notes/_lanes/279/active-review/DAVE-EXPORT-active-2026-09-16.json`, in at 15:55Z, committed by this wrap, **and the record has not read it**. **15/15 choose a twin** (7 bare `-active`, 8 `-active-2`); **12/15 flag or note a MISLABELED drawing** — his second defect, the exporter's, as `s277-D6` recorded. ⛔ **Twins → `activeVariantOf`/`defaultActive`; mislabels → the exporter-defect list. WHERE FLAG AND NOTE DISAGREE THE NOTE IS HIS SENTENCE — ASK, DO NOT GUESS**: they disagree on `electricity`, and four rows are **note-only** (`employee-banking-solution`, `financial-health-check`, `reward`, `jade-lifestyle` — *"Im not certain"*).
3. **`s277-D12` — TOKENS AT GROUP+TIER.** Never started. Rests on `s269-D3` (*"TOKENS ENTER THE GRAPH AT TIER GRAIN — semantic versus primitive — and NEVER as 932 leaf nodes"*); the reader's tokens leg reads **25 groups** live and its tier map holds **43**, where the audit's ≈128 was `_blast-radius.json`'s LEAF count (lane SC measured it).
4. **THE EXPLORER READS THE VERBS + THE SCOPE CHIP** — ⚠ **but the scope chip is a RULING BEFORE IT IS ~25 LINES OF CODE**: `s277-D8` fixed DESIGN GOVERNANCE at **three** provenances and "inference by declared scope" would be a fourth.

## ⬛ OPEN, RULING-SHAPED, NOT INSCRIBED — every one a QUESTION PUT, not a state of the world (`s271-D4`)

- **(a) THE LAYOUT** — above. #280's first question.
- **(b) THE `designrulings` SUB-CHIP** (lane EV §9, row `W-279ev`). Its label reads **341** = 295 `governs`→component/snippet + 28 base `governedBy` + 8 asset `ruledBy` + **10 asset `governedBy` nulls that can never draw**; at page defaults a chip labelled 341 governs **28** drawn edges. **(a)** accept EX's reading — third provenance = *the rulings a design cites*, an edge chip ON by default — and relabel the count *citations*, not *rulings*; **(b)** declare the third provenance a set of RULING nodes, remove the sub-chip, ship DESIGN GOVERNANCE with two provenances and a visible *third: pending scope ruling* slot.
- **(c) A THIRTEENTH VERB + `verbVia` KEEP-OR-DROP** (lane VB §9). `usesIcon` **371** / `usesLogo` **19** have a consumer (the slice's `assets` field, ASK Q12) and no verb. `verbVia` costs **~+1,500 tokens** across 122 rows for provenance.
- **(d) THE SPIDER `v1.0.14` CUT** (lane PK) — armed, version-gated, un-fired. His word under `s219-D4(2)`; `P-269-1`'s tripwire.
- **(e) THE `va25-013` SPLIT** (lane SC2). It stays at **111 components by its own words** — *"icons/avatars 1:1 square"* — so an override stripping `icons` would contradict the quoted rule. ⇒ **a RULE SPLIT in `knowledge/guidelines/visual-assets.md`, never a facet override.** Size: one guideline edit + a regen of `_rules-index.json`.
- **(f) CARRIED FROM #278, UNANSWERED:** what `s214-D6` means when `_CHAIN.md` comes in **below** its band · whether the `s129-D1` boot floor's **label** should be corrected · the **`#243` form**, now standing for a **fifth** wrap · the **two-probe-shape §A question** (#272), eleventh session.

## MEASURED AT #279

- **FILL 213,222 real / 43 turns (41 continuous)** — `_checkin.py` first-hand at the wrap seat against the conductor's own transcript (`…/session/58a9bcbb-bb12-4711-a1c7-5964e6dc56ab.jsonl`, first user record = his opener). Brief-cut **207,989 / 41** DECLARED ⇒ **delta 5,233**. ⛔ **200,000 WORKING LINE BREACHED — the second breach in three sessions — DECLARED, not excused.**
- **BOOT 72,447 real, n=1** vs the `s129-D1` floor **70,794** — **+1,653, NOT a re-base**; `BOOT_FIRSTTURN_TK` untouched.
- ⛔ **`/sessions` 98.2% / 169,500 KB free** (`_checkin.py`); `df -h` reads **99% / 166 MB** — two readings of a moving number, both published, neither averaged. **At 100% no session boots, and nothing in the repo fixes it.**
- **NO `subs` line** — the only figure anywhere is an ESTIMATE (*"≈2.4M"*), and `s214-D5` never defaults an absence into a number.
- `knowledge/_rulings.json` **605** · `notes/_RULINGS.html` re-rendered, **605 rulings · 155 sessions**, `--check` **FRESH** · `_CARRIES.md` § `residual → #280` **507 probeable items** (⚠ the seven new ones become countable next wrap — `_AGE_RE` does not match `[NEW — 0]`, and its own docstring says it does).

## COMMITS THIS SESSION

All `SESSION_N=279 SHOWROOM_ACK=1 bash knowledge/_git_commit.sh --reconciled <fresh msgfile> <paths…>`, bare subjects, the `#243` DECLARED not-a-wrap path. Opener `c9af467` (the #278 memory hook placed) · lanes `f641242` `d6bd57b` `84658db` `9e7a158` `3263dd7` `a39d4cd` `2c6b640` `e7ff7b9` `25e9e32` `e3facb4` `9b2e1b0` `eb2ff7c` `b4fd7a8` `df33a24` `3710d6b` `767427a` · then this wrap. ⛔ **Seventeen commits stood UNPUSHED when this ritual opened** (`origin/master` = `7c72cc1`); the push verdict is in the wrap's returned report, read back from the script's own literal `✅ pushed and VERIFIED` line.

## STRUCTURAL REDS AT THE SEAM

- **showroom 108 pages stale** — standing since #273, **HIS call**, `SHOWROOM_ACK` on every commit.
- `_validate_roles_resolve.py` **FAIL(6)** — all six `data-grid` `with`-slugs · `_validate_lane_ownership.py --selftest` **2/3**.
- **The wrap gate's six fails are INHERITED**, every one another session's append-only testimony in `notes/_GAUGE-LOG.md`: the boot-drift ceiling breach and the five boot double-counts of #243, #264, #272, #273, #274. ⚠ A wrap is forbidden to repair them and must therefore declare itself not-a-wrap to commit — question (f) above.
- ✅ **REPAIRED HERE:** `knowledge/_state.json`'s `W-278wr` row was missing its required `links` field (four #279 lanes reported it, none could fix it from a lane seat); added **by textual span**, insertions only.
- ⛔ **MEMORY IS A STRUCTURAL SEAT LIMIT, NOT A SKIP** — only the conductor's seat writes the cloud Project store. The hook is at `notes/_lanes/279/WRAP-MEMORY-HOOK.md`; **place it at the #280 opener** and append the `✅ PLACED` receipt, the way `c9af467` placed #278's.
- **Sandbox, unchanged:** the commit script runs with `< /dev/null` · locks → `mv` to `.git/_orphan-locks/`, **never `rm`** · never `git stash`, never `gen_kg_edges.py`, never `_build_all.py` · `/sessions` at 99% — **download nothing, keep scratch tiny, remove it before the window dies** (`_gate_scratch_hygiene.py --clean` removed **86** entries at this wrap).
