# HANDOFF #131 — #280 → #281 — THE LAYOUT MATRIX, AND THE EXPLORER BECOMES THE FRONT DOOR

provenance: 280 · 2026-09-17
status: observed

## ⛔ READ FIRST, IN THIS ORDER

1. **THIS FILE.** It is newer than `_CHAIN.md` and **OUTRANKS** it. It does **NOT** replace
   `_HANDOFF-130` — #130's four ruling-shaped questions (`designrulings`, a thirteenth verb +
   `verbVia`, the Spider `v1.0.14` cut, the `va25-013` rule split) are **untouched by #280 and
   still stand** — but #130's **"#280 FIRST MOVES" list is SPENT: the layout question was put and
   answered, and his 15-base export is read and fourteen-fifteenths inscribed.**
2. `_CHAIN.md` (the read chain: GM header → ★ LATEST banner → `_LIVE-STATE.md`'s ⏱ LATEST delta).
3. `notes/_lanes/280/DAVE-RULINGS-2026-09-17.md` — **his words verbatim**, committed by this wrap.
4. Everything beyond that is **RETRIEVAL**, never a reading list:
   `python3 knowledge/_memento_search.py "<q>"` → `--fetch <id>`.

## ⛔⛔ DAVE'S — VERBATIM. Source: `notes/_lanes/280/DAVE-RULINGS-2026-09-17.md`

- At the opener, on pace and routing: *"Fable is up to pace, lets rince the opus subs and you as
  fable are the boss"* (panel: 68% this week overall, 86% Fable, both resetting Thursday 23:00).
- On lane LY's strata build: *"I actually like the strata, maybe we just have multiple views, could
  the strata possibly be 3d too?"*
- His sketches export, 20:30Z: *"Okay I want all of these plus the original view, in 2d and 3d for
  all of them. just keep force as the default"* ⇒ **`s280-D1`**.
- On explorer 1.19's INSPECT: *"I want to see any artifact that exists here, including a render of
  the actual component snippet, this is such a good mental model that it becomes a great explorer
  for the entire system"* · *"no i want to inspect the edge or indeed the actual file, a component
  for example"* ⇒ **`s280-D2`**.
- On the legend fix: *"good"*. On the orphan census: *"cool"*. Mid-session, on the gauge:
  *"how hot are we"*.
- On `jade-lifestyle`, in his own export: `choice: "open"`, `twin: null`, *"jade-lifestyle-active-2 -
  think is the most likely the correct icon"* ⇒ **OPEN, and nothing was written for it.**

## THE TWO RULINGS — `knowledge/_rulings.json` 605 → 607, both by textual span, zero deletions

**`s280-D1`** (`53a91bd`, lane LM, span `19  0`) — **THE LAYOUT MATRIX.** Three layouts × two
dimensions: **force · strata · shells × 2D · 3D, six cells, force the default.** His export chose
all four sketches plus the original, and the four map onto the matrix **without remainder** —
FLOORS *is* strata-3D, ORBITS *is* shells-2D. **Storage untouched**: no node id, no edge type, no
file under `knowledge/` moved; what changed is where the dots are put, plus a tool row carrying both
axes and `?layout=` / `?dim=` so any cell can be photographed without the page being clicked.

**`s280-D2`** (`9e04796`, lane EX4, span `18  0`) — **THE EXPLORER IS THE FRONT DOOR.** INSPECT
opens **the artefact**, by kind: a component opens with **RENDER** showing (the real snippet running,
with the showroom's stylesheets, following the page's light/dark), a rule opens its guideline
markdown with **its own row highlighted and scrolled to**, a ruling opens its record, an icon or
logo is **drawn at 48 and 16 px** on a light ground and a dark one, a session shows what was ruled
in it, and anything with no renderer yet opens as the raw file **named by its type**. Relations are
doors too (`↔`), with a **trail** back. ⚠ **It is SERVED, not double-clicked** —
`knowledge/_serve_explorer.py` — because a `file://` page may not read the file beside it, and the
page says so out loud rather than failing quietly.

## WHAT LANDED — 17 lane commits, 8 filed reports, explorer 1.16 → 1.20

| lane | commits | what |
|---|---|---|
| LY | `b212ca2` `aa75fed` | 1.17 strata — one option on a page, default **pixel-identical by canvas md5** |
| IN | `c4d0d22` | 9 twins inscribed, 6 rows asked back on a page; `knowledge/_ICON-GAPS.md` born |
| LS | `a7ecd8f` `53deb46` | four sketches (strata · shells · floors · orbits) from the real node data; **his export answered them** |
| IN2 | `850acbf` | 5 more twins ⇒ **14 of 15 settled**; `gen_kg_icons.py` rebuilds the 14 from his exports and **refuses on disagreement** (22/22, mutants 32/32) |
| LM | `53a91bd` `b5df9c9` `eddf87a` `c3805ec` `94204dc` | 1.18 — the six cells; contact sheet `notes/_lanes/280/layout-matrix/MATRIX-2026-09-16.html` |
| EX3 | `2a406f1` `5e3ba3b` | 1.19 — legend sticky with a side/bottom toggle; INSPECT modal |
| OC | `82359a5` | the orphan census — **157 of 4,618**, four causes, ten sets, `notes/_lanes/280/orphan-census/ORPHANS-2026-09-17.html` |
| EX4 | `9e04796` `e518cd6` `3b92a63` | 1.20 — artefact renderers, edge inspect with a trail, `knowledge/_serve_explorer.py` |

Every lane filed at `notes/_subreports/2026-09-1{6,7}-280-*.md`.

## ⛔ #281 — FIRST MOVES, IN ORDER

1. **THE CENSUS EXPORT.** Ten radios on `notes/_lanes/280/orphan-census/ORPHANS-2026-09-17.html`,
   **his, and not received.** The census becomes the orphan **plan** on export and not before.
   ⚠ Its biggest set is **a switch left off, not a gap in the vocabulary** — 100 of 145 UX
   principles are dark, 99 with no edge of any kind in storage, and the principle generator can
   already emit the two kinds that would light every one (behind `s275-D2` flags). ⛔ **Turning the
   first on puts a FAMILY NODE on the stage — a new kind of thing, and therefore HIS word.**
   The second-largest set is not orphaned at all: 94 base `rule:` dots that light when the HSBC
   `rule:` chip goes on.
2. **THE EYE-CHECK OF THE SIX CELLS.** `notes/_lanes/280/layout-matrix/MATRIX-2026-09-16.html`.
   The conductor **expects** Floors (strata-3D) or Orbits (shells-2D) to go — ⚠ **an expectation, not
   a finding, and no lane may thin a ruled matrix on it.**
3. **`s277-D12` — TOKENS AT GROUP+TIER — STILL NEVER STARTED.** Bumped a second session, this time
   by the layout work. No lane, no line in the tree.

## ⬛ OPEN, RULING-SHAPED, NOT INSCRIBED — every one a QUESTION PUT (`s271-D4`)

- **`jade-lifestyle`** — open by his own word; the lean sits on the null and nothing was written.
- **Declared out by their own lanes, not ruled out:** the shells-3D cutaway (and equal plate/ring
  sizes per layer) · **snippet scripts are stripped in the INSPECT sandbox**, so a script-driven
  component renders as its markup and the page does not say which ones those are.
- **The wrap-sub memory seat limit** — the runbook calls it STRUCTURAL; #279's and #280's wrap subs
  both wrote the store from their own seats. **The line is not edited: that is Dave's.**
- **Carried unchanged from `_HANDOFF-130`:** the `designrulings` sub-chip · a thirteenth verb +
  `verbVia` keep-or-drop · the Spider `v1.0.14` cut (armed, version-gated, un-fired) · the
  `va25-013` rule split.
- **Minted into `_CARRIES.md` at this wrap** because they had been living only in wrap reports: the
  `s129-D1` boot-floor **label** · the **two-probe-shape §A** question (twelfth session) · what
  **`s214-D6`** means when `_CHAIN.md` comes in **below** its ~10–12K band (it read **8,945** here, after the declare-last stamp).

## § STRUCTURAL REDS — inherited, measured at this wrap, and NOT repairable from a wrap seat

- ⛔ **SIX blocking gate fails at the close, SEVEN at the open**, every one another session's append-only testimony in
  `notes/_GAUGE-LOG.md`: the boot-drift **ceiling breach**, the boot-drift **step change** (with a
  #242 declaration whose figures disagree with the gate's own computation), and **five boot
  double-counts** (#243 ×5, #264, #272, #273, #274). ⚠ **The #280 wrap brief declares SIX; this seat
  measured SEVEN AT THE OPEN — both readings are published and neither is smoothed.** ⚠ **AND THE COUNT MOVED DURING THE RITUAL ITSELF: SEVEN AT THE OPEN, SIX AT THE CLOSE.** The boot-drift STEP CHANGE cleared when the wrap's own **2f roll** appended #279's stratum to `notes/_GAUGE-LOG.md` and the derived band re-computed over a new window. The brief's DECLARED SIX is the CLOSING state; the open reading of seven is right for the open. Both published, neither retro-fitted. The commit therefore took
  the `DECLARED not-a-wrap` path for the **sixth** wrap running, which is itself ruling-shaped.
- ⛔ `_validate_roles_resolve.py` **FAIL(6)** — all six `data-grid` `with`-slugs.
- ⛔ `_validate_lane_ownership.py --selftest` **2/3**.
- ⛔ `showroom/` holds **138** top-level `.html` against an inscribed *"108 stale"* — **the figure is
  not rewritten**; both readings stand, and it is HIS call.
- ⛔ `/sessions` **99% full, 158,032 KB free**. Nothing in the repo fixes it.

## ⚙ GAUGE AT THE CLOSE

**FILL 191,294 real / 28 turns**, first-hand at the wrap seat against the conductor's own transcript
(first user record = his opener, so the subtraction is legal), against **185,127 / 25 DECLARED** at
the brief cut ⇒ **delta 6,167**. ✅ **The 200,000 working line was NOT breached** — first in three
wraps. ⛔ The ruled **180,000 stop line passed by 11,294**, inside the ≤220,000 tolerance, DECLARED.
**BOOT 75,740 real, n=1 = +4,946 on the `s129-D1` floor — NOT a re-base.** **subs 1,833,788
(n=8)** — QUOTA, never FILL, and written only because the harness **measured** each lane.

**Next chat title:** `Apollo - #281: the census export and the eye-check`
