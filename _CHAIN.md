<!-- GENERATED by knowledge/_gen_chain.py — DO NOT HAND-EDIT.
     Edit GOOD-MORNING.md / _LIVE-STATE.md and regenerate; `--check` is a build step. -->

# The read chain — everything a cold session must read, and nothing else

> **This file is GENERATED and it is the whole contract.** It carries `GOOD-MORNING.md`'s header
> and ★ LATEST banner, plus `_LIVE-STATE.md`'s ⏱ LATEST delta — the three things GM-D7-am names
> (CUT #33 on Dave's ruling). **If you have read this file, you have read the chain.**
>
> ⚠ **Do NOT now open `GOOD-MORNING.md` to "check".** It is 24,860 tape and 81% of it is
> retrieval surface you are not meant to pay for at boot. That reflex is the thing this file
> exists to stop — five sessions called the chain CUT and every one of them paid full price.
>
> ★ **Everything else is RETRIEVAL, never a reading list:**
> `python3 knowledge/_memento_search.py "<q>"` → `--fetch <id>`. §A orientation is `gm:A`
> (a router with 11 children); the queue is `gm:C1`…; the archives, briefs, runbooks and ledgers
> are all indexed. **Ask for what you need; do not read a file to find out whether you need it.**

---

<!-- ⛔ STOP. If you are a cold session, you are in the wrong file and you pay for ALL of it.
     Read `_CHAIN.md` instead — it is GENERATED from this file's header + ★ LATEST banner and
     `_LIVE-STATE.md`'s ⏱ LATEST delta, which is the ENTIRE contract (GM-D7-am, CUT #33).
     This file stays whole for RETRIEVAL: `_memento_search.py "<q>"` → `--fetch <id>`.
     ⚠ Reading on from here is not thoroughness — it is the exact overspend #41 measured. -->

# Good morning, Dave ☕

> **size:** GM **25.1K tape** · §A **4.2K tape (EXEMPT)** · corpus **44.5K tape** · **measured #58** *(⚠ GM rose **23.2K tape → 25.1K tape** even though 2c rolled TWO banners OUT — #58 added its own banner, its stratum and two header corrections. **A roll is not a cut, and an addition is not drift when it is priced before the writing.**)* — ⛔ **the chain figure lives in `_CHAIN.md`'s footer and NEVER here** (RETIRED #45, gate-enforced by `CHAIN_STAMP_RE`, which bans PRESENCE not drift; ⚠ open 23 — it catches only the `K` form, ⚠ open 24 — and not a self-reference at all). ★ **`tape` = tiktoken cl100k · `bill` = what the window charges — THE TAPE IS NOT THE BILL.** ★★ #53 MEASURED **tape ×1.559 = real tokens**, so `bill` was always the tokenizer and never a billing overhead — ⚠ but the spread is **1.486–1.664**, so **one ratio cannot re-denominate a corpus**, and #56 REPLACED the band rather than converting it. ⚠ **THE FLOOR IS NOT WILLPOWER: nothing shrinks a region below `N` × one unit ⇒ the only lever is WRITING LESS, priced BEFORE the writing** — the four measurements live in `_RUNBOOK-context-gauge.md` § ★★ THE FLOOR IS NOT WILLPOWER. ★ **HOME BY ADDITION, THEN CUT — never the reverse, never in one motion** (#50's probe + quoted homes: `notes/_MEMENTO-DECISIONS.md` § ★ #50).
> **STATE: ★★ THE READ CHAIN IS CUT (#33) and it HELD — sixth session running.** You are reading the contract; it is four paragraphs below the banners. §A and §C are STILL IN THIS FILE, reached by `_memento_search.py`, not by reading top-to-bottom. ⚠ **BUILD CLAIM CORRECTED AT SOURCE #47, ON #46's FINDING — "75/75 exit 0" IS NO LONGER A CLAIM ANY SESSION CAN MAKE.** *(#46 found it; its commit-state block is not a surface anything reads, so #47 re-derived it from zero — open 18.)* The build has **75 steps**, steps 1–73 consume ~40s, and the sandbox call cap kills it at `EXIT=124` (reproduced at 40s AND 43s); `_build_all.py` has **no subset flag**, so its composite exit code is unobtainable in one call. **The honest form, and the only one to write from here: "all 75 accounted for, none gate-failing"** — reaching step 73 PROVES none of 1–73 gate-failed (`_build_all.py:290` `sys.exit`s on any gating failure), and 74/75 are exit 0 standalone. ⇒ **verify in SEGMENTS and SAY it is your composition, not the build's verdict.** ★★ **#38 finished #37's wrap, then found WHY every wrap grinds: the compactable cap is aimed at cold-start cost, and #33 cut the chain out from under it — ~9,000 of the 11,955 tape it governs is never paid at boot.** Full finding + the lane divvy: **`notes/_briefs/2026-07-29-cap-repoint-and-lane-divvy-brief.md`.** ✅ **#37 DOSSIER WRITTEN #44** → `_DECISION-HISTORY/2026-07-29-the-37-dossier.md`.
> ⚠ **`set_content()` IS BANNED IN RENDER-PROOFS** — drops `type.css` silently (14px→16px) and `document.fonts.check` passes anyway; use `goto("file://…")` (#29, measured) · **ds-020 is a gate-enforced obligation** (the DV-D02-A scatter waiver names it as clears-when) · **ritual step 2g: rebuild `_memento-index.json` LAST** — `index_freshness_check` is BLOCKING and compares CONTENT, never mtime (runbook has the body).
> **★ PRICE IN REAL TOKENS — THE UNIT IS `_gauge_tokens.py`, NOT A PERCENTAGE (#56, Dave's).** **amber 160,000 · working 200,000 (DAVE'S — the line jobs are priced against) · hard 256,000 (SOURCED).** Unit = the FULL price (boot + job + wrap); ⚠ **a DECLARED gap passes, a SILENT one fails** — that asymmetry is the mechanism. ⛔ **CORRECTED AT SOURCE #58 — this line taught the RETIRED `(45, 60, 63)` percentage band as "live in code, price against it", six sessions after #56 replaced it (*"the band was REPLACED, not CONVERTED"*), and the header is IN THE READ CHAIN, so every cold session was being handed the wrong unit first.** ★ **MEASURED, not assumed:** `_capture_gate.py:135–137` still defines `BAND_FLOOR/HARD_STOP/MARKED_MAX = 45/60/63`, `check_preflight` (lines 899–994) still implements the % path, and the selftest at **:2543** still pins the triple — but the live wrap emits **no band line**, because it matches on the stamp form and #57's stamp is in tokens. ⇒ **the % path is DORMANT, not wrong, and it was the PROSE that was stale.** ⬛ **FORKED TO DAVE, not decided here: retire the % path in code, or keep it as a pinned known-gap?** Deleting a pinned ruling of his is not an agent's move. ⚠ **THE FLOOR IS PART-UNKNOWN = `ds-025`** — disk half measured (**17,810 tape / ~14.0 pts**, #37), harness half unreachable ⇒ **say which half of your fill is measured.** ⚠ `pip install tiktoken --break-system-packages` — **the gate SILENTLY ESTIMATES without it, UNDER-reporting by 414 tape.** ★ **READ THE CHAIN ONLY.** ★ **Repo-state claims are verified against `git log` / a real run, never a banner** (#29→#38: ten consecutive).
> ⚠ **Render sandbox is FRESH every session** (~4 calls — price it) · **retrieval FIRST:** `_memento_search.py "<q>"` → `--fetch <id>` (⚠ **it served a two-session-old record until #32 — if an answer looks stale, check the index**) · **wrap moves RUN THROUGH `_gm_move.py`** (⚠ **banner headings are BLOCKQUOTED — anchor `> ## ★ PRIOR …`**) · **PACE PANEL: ask at the opener, no gate can see it** — Dave has declined a fresh one twice and put Fable on the table for judgment work; ⛔ **THE "BEHIND PACE ⇒ MORE WINDOWS" CLAUSE IS CONTESTED BY DAVE AND MUST NOT BE ACTED ON UNTIL HE CONFIRMS.** It stood from #48; at #57 he said the opposite — *"we are behind, but I'm less comfortable with multiple windows, too much to decide and consume"* ⇒ **ONE window with more delegation.** ★ **The clause optimised the wrong quantity: it treated TOKENS as the constraint when DAVE'S DECISION LOAD is what binds.** ⚠ **Reflected back #57, and #58's opener did NOT settle it** (asked as the first question; he answered pace with *"no constraint, lets just fix the problems"*). Standing home + status: §C·4.
> *(**RENAME is delivered IN CHAT at wrap, not here — RULED #28, ENACTED in runbook step 4b by #30.**)*
> **TITLE THE NEXT CHAT →** `Apollo - #59 FINISH THE MEMENTO GATE SUITE, THEN THE TEST PLAN (read _CHAIN.md ONLY). * ⛔ OPEN THIS WINDOW BY PRICING IT. Dave #58b: DO NOT REACH 200,000 * 200,000-256,000 is DANGEROUS * past 256,000 everything goes badly. BOTH NUMBERS ARE SOURCED FROM RESEARCH, NOT his preference - provenance corrected in _gauge_tokens.py. #58 ran to ~339,000 tape CUMULATIVE and had to hand off unable to PROVE its fill. * ★★★ FILL WAS NEVER UNOBSERVABLE, AND ds-025 ITEM 1 IS FALSIFIED. It is in `message.usage` on EVERY assistant record of the live transcript: input_tokens + cache_creation_input_tokens + cache_read_input_tokens = Anthropic's OWN count, real tokenizer, HARNESS INCLUDED. Twenty-odd sessions declared this unobservable and it was in the log the whole time. MEASURED AT #58b's CLOSE, CONDUCTOR-REPLAYED INDEPENDENTLY OF THE WORKER: 292,400 real - PAST 200,000 AND PAST THE 256,000 HARD CEILING - with ZERO compaction records in 213 assistant turns, so nothing was dropped and that is genuine resident fill. #58b STOPPED THERE. ⛔ BOOT IS ~3x WHAT EVERY PRE-FLIGHT STAMP CLAIMED: measured 61,582 on turn one, against the inherited ~20,000 ±8,000 estimate. EVERY pre-flight in the record understated its own floor. RE-PRICE FROM THE MEASUREMENT, do not carry the old number forward. ⇒ #59's FIRST ACT, BEFORE ANY JOB: read your own fill from the transcript and state it. The gauge can now be built on a MEASUREMENT instead of an estimate - that is Dave's budget model, unblocked. Evidence + method + caveats: `notes/_briefs/2026-07-31-compaction-and-fill-research.md`. HIS GOAL, PLAYED BACK AND AGREED: conductor budget = 200K minus boot/preload * worker budget = 200K minus brief * AND the conductor pays for READING the return and deciding on it - that third term has never been measured and is the CHEAPEST to fix, because brief-out and report-in are text you can count exactly. Do that FIRST: instrument brief-in/report-out per delegation, then the model has real numbers instead of estimates. ⚠ The ±8,000 harness hole (ds-025 item 1) stays - state the boot as a BAND, not a point. * ⬛ DELEGATION, CONFIRMED AND CORRECTED BY DAVE #58b: subs by default STANDS * behind pace = conductor-worker inside ONE window * workers MAY spawn subagents where the conductor judges it safe * AND multi-window stays AVAILABLE for catching up - do not read it out. UNSCOPED, and he wants RESEARCH not a guess: one god conductor over sub-conductors over workers, vs flat fan-out? EXPLORE LATER, not in the tranche. Safeguard that stays: REPLAY a worker's load-bearing claims before they enter the record. * THE TRANCHE = THE MEMENTO GATE SUITE (his words): chain staleness * strata * unkeyed testimony * pre-flight * index freshness * the mover * section caps. FINISH IT BEFORE APOLLO (his call). Then the TEST PLAN, per gate: what it CLAIMS * what proves it BITES * what mutation makes it RED * what it structurally CANNOT SEE. * THE OWED THREE, now four-times deferred: the 1b DOSSIER for #55 and #57 (#58's IS written) * the archive CONTENT-probe (#58 marked the boundary: `⛔ UNBATCHED REGION`, 190 lines, answer by CONTENT, do NOT re-key by guess) * ds-021 three-homes (RE-SCOPE, smaller than it sounds). * ⬛ STILL DAVE'S, UNDECIDED: the percentage band in code. He gave the GOAL not the decision - one unit, reliably estimated - which implies the % path goes or refuses loudly, but the call is his. MEASURED: dormant, not wrong. * ⬛ AND A THIRD UNREACHABLE CAP, HIS: the banner-region block demands a 2c roll that the index vocabulary FORBIDS (rolling the last PRIOR makes _build_memento_index refuse the corpus). Proven by doing it and reversing it. #58 shaved its OWN additions instead - correct, but not a fix. * RUN `_git_commit.sh`. 2f: DO NOT RE-ROLL #40/#41/#42.` *(superseded #58b →* `Apollo - #59 APOLLO, AT LAST - THE RITUAL IS CLEAN (read _CHAIN.md ONLY)…` *— retitled because Dave ruled the tranche finished FIRST)*
> *(Titles are LABELS — role comes from Dave's opener line. Bands: the ONLY copy is
> `_RUNBOOK-context-gauge.md` § ★ THE FLOOR IS MEASURED — grep it, never recall it.)*
> ## ★ LATEST — 2026-07-31 (Fri **#59**, OPUS conductor + SONNET worker, Dave live throughout — ★★ **THE WRAP COST IS MEASURED FOR THE FIRST TIME IN THE LIVE UNIT AND THE ~25,000 FOLKLORE IS DEAD** · ⬛ **DAVE RULED: RECORD THE FORMULA, NOT THE NUMBER** · ✅ **THE FILL READ IS CLOSED — max-per-id, synthetic skipped**)
> **pre-flight #59 (RETROSPECTIVE — declared a LAPSE; no band was written before the work. Dave's steer at the opener: *"stabilize Memento until ~4pm, then Apollo"*):** boot **61,812 MEASURED** (`message.usage` turn one, harness INCLUDED — reproduces #58b's 61,582 to within 230; the ±8,000 harness hole of `ds-025` item 1 is CLOSED, not estimated) + job **56,343 MEASURED** + wrap **51,592 MEASURED and still climbing** = **169,747 of 200,000 — 🟡 AMBER**. ⚠ **I first stamped this RED and the gate caught it: 169,747 is AMBER against the thresholds in `_gauge_tokens.py`.** ★ **I recalled the band instead of quoting it — the exact thing the gate's own failure text forbids, committed inside the stamp that reports it.** ★★ Every term is MEASURED, which no prior pre-flight in the record could say. ★★ **Every term here is MEASURED, which no prior pre-flight in the record could say — and the reading is RED, which no estimate had predicted.** ★ A pre-flight is normally a PLANNING ESTIMATE; this one is a post-hoc reading, and that is exactly why it is labelled RETROSPECTIVE.
> **⚠ SHORT BY DESIGN — rulings and every figure → `notes/_MEMENTO-DECISIONS.md` § ★ #59 (OWED, see §C) and the #59 stratum below.**
> - **★★ THE WRAP COST IS MEASURED — 42,434 real tokens AND STILL CLIMBING at the stamp, against an inherited estimate of ~25,000.** Wrap-start fill **118,155** → **160,589** at mid-wrap. ⛔ **The corpus contains ZERO prior wrap measurements in the live unit:** every wrap term in `notes/_GAUGE-LOG.md` is a percentage (`wrap 5%`/`6%`/`8%`) in the band #56 RETIRED, and by Dave's own rule those cannot be converted. **~25,000 was never a stale measurement — it was folklore, and it was low by ≥1.7×.** ⇒ every derived stop line built on it is wrong in the dangerous direction.
> - **⬛ DAVE RULED — RECORD THE FORMULA, NOT THE NUMBER.** `stop = wall − wrap − step`, **each term tagged MEASURED or ESTIMATED**, and **160,000 relabelled PICKED** (it was 80% of working wearing a `DERIVED` label — the exact defect *"derive a cap, never pick it"* exists to catch: **ruled #53 · gate-asserted #56 · second remedy #58**, three dates that had collapsed into one citation). ★ **The formula self-corrects — when `wrap` becomes measured the line moves without a new ruling.** ⬛ **AND: reserve the HIGH end of BOTH terms, or neither** — Dave caught me reserving the max for wrap and the median for step, *"a stop line reserved at the median step is under water half the time by construction."*
> - **★★ A NEW CLASS — A RESERVE STACKED ON A RESERVE.** My *"stop at ~150,400"* was PICKED amber − one step, but if amber is itself `wall − wrap − step` the step was already inside it. **~10K a window, invisible because each layer is individually defensible.** Dave: *"nobody adds a bad reserve; they add a second reasonable one to a number whose contents they didn't open."* ⇒ **an argument for recording the formula rather than the number** — the double-charge only surfaced because the formula got written out.
> - **✅ THE FILL READ IS CLOSED, VERIFIED NOT INFERRED.** Grouped by `message.id` across the whole transcript: **35 records / 20 turns, `usage` byte-identical within every group** ⇒ "read the last record" is not reading a partial. ★ **But records ≠ turns (~1.75×)** — one record per content block — so **#58b's "213 assistant turns" is inflated**, and any per-turn model must dedupe. ⛔ **AND the log carries a `model: "<synthetic>"` record with all-zero `usage`** (the client-side *"No response requested."* written at an interrupt): **if it lands last, a naive read returns 0 and reports a comfortable green.** ⇒ the correct read is **max-per-`message.id` with an explicit synthetic/zero skip**, and it is what every figure in this banner used.
> - **★★ `/tmp` IS WORLD-READABLE AND NOT WRITABLE IN THIS SANDBOX — A SILENT STALE-ARTEFACT TRAP, HIT LIVE AT THIS SESSION'S OPENER.** `cmd > /tmp/f` fails *Permission denied*, the command never runs, and the following `tail /tmp/f` **serves a PREVIOUS session's output**. I read `✗ _CHAIN.md is STALE` off a file dated **2026-07-30 21:34 owned by `nobody`** and was one sentence from recording it as this window's verdict. Re-run to the outputs dir: **`✅ _CHAIN.md is FRESH`, exit 0.** ⚠ **No `/tmp` writes exist in the toolchain `.py`/`.sh` — the exposure is in the RUNBOOKS** (`_RUNBOOK-context-gauge.md`, `_RUNBOOK-git-commit.md`, `_RUNBOOK-render-verify.md`), i.e. aimed squarely at a session following instructions. **UNFIXED — §C.**
> - **⚠ WHAT I GOT WRONG, all three caught by Dave in-window.** (i) quoted **122K as "room"** when it was the distance to the line we are told not to reach — *"how a session talks itself into the danger zone one job at a time"*; (ii) **priced the turn that just happened, not the next one** — `usage` is the prompt for the call that already ran, so the stop line is systematically **late by exactly one step**, and steps here measured **181 → 17,681**; (iii) called *"zero compaction records in 10 assistant records"* evidence when at 77K fill **the probe had no failing branch** — the honest form is *"not yet applicable at this fill."*
> - **⚠ THE RETROSPECTIVE FIVE POINTS ARE UNREACHABLE FROM A SANDBOX, AND THAT IS THE FINDING.** `/sessions/` holds ~190 sibling session dirs; **exactly one `.jsonl` is readable — the running session's own.** #58's transcript exists and no session can read it. ⇒ **wrap cost is measurable PROSPECTIVELY ONLY** (stamp at wrap-start, re-read at commit) — which is what this banner did, n=1.
> - **⛔ NO MEMENTO GATE-SUITE WORK AND NO APOLLO — DECLARED, NOT SLIPPED.** The window went to the budget model at Dave's direction. Gates were PROBED not built: `_gen_chain.py --check` **exit 0 / FRESH**, `_capture_gate.py` **40 in scope · 0 fail · 0 warn**. ★ **Both green, and that proves nothing about whether they BITE** — the test plan is still owed.
> - **⬛ DAVE'S FOUNDING PRINCIPLE, HELD NOT FILED (his call):** *"we research, we analyse, we probe, we test."* Placement deferred to after the tidy-up. **Do not inscribe it as canon without his word.**
>

---

## ⏱ LATEST DELTA — 2026-07-31 (Fri **#59**, OPUS conductor + SONNET worker, Dave live throughout — ★★ **THE WRAP COST IS MEASURED FOR THE FIRST TIME** · ⬛ **RECORD THE FORMULA, NOT THE NUMBER** · ✅ **THE FILL READ IS CLOSED**)

- **★★ WRAP COST MEASURED: 118,155 → 160,589 = 42,434 real tokens and still climbing at the stamp.** Inherited estimate ~25,000, low by ≥1.7×. **Zero prior wrap measurements exist in the live unit** — `notes/_GAUGE-LOG.md` carries only retired-band percentages, unconvertible by Dave's own rule.
- **⬛ RULED (Dave):** `stop = wall − wrap − step`, every term tagged MEASURED/ESTIMATED; **160,000 is PICKED, not DERIVED**; **reserve the HIGH end of both terms or neither**. Citation split corrected: **#53 ruling · #56 enactment · #58 second remedy**.
- **★★ NEW CLASS — reserve stacked on a reserve** (~10K/window), invisible because each layer is individually defensible. Surfaced only by writing the formula out.
- **✅ FILL READ CLOSED:** `usage` byte-identical within every `message.id`; records ≠ turns (~1.75×); a `<synthetic>` zero-usage record can land last and read as 0 ⇒ **max-per-id + synthetic skip**.
- **★★ `/tmp` STALE-ARTEFACT TRAP, HIT LIVE:** unwritable + world-readable ⇒ a failed redirect serves a prior session's file. Nearly recorded a stale `STALE` chain verdict as this window's. Toolchain clean; **three RUNBOOKS still instruct `/tmp` — UNFIXED**.
- **⚠ Three pricing errors, all caught by Dave:** wall-distance quoted as "room" · the gauge priced the last turn not the next (late by one step; steps measured 181–17,681) · a compaction probe that could not have failed.
- **⛔ No gate-suite build, no Apollo — declared.** Gates probed green (`--check` exit 0 · capture gate 0 fail/0 warn); **green is not bite.**


---

*(Chain ends. **5,763 tape — the unit is THE WHOLE FILE**, this generated wrapper included:
the number in this sentence is the size of the file containing it, held exact by a fixed point.
`GOOD-MORNING.md` is 24,860 tape and stays whole for retrieval; you have paid for
23% of it, and that is the intended figure, not a shortfall.)*
