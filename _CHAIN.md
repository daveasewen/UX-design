<!-- GENERATED by knowledge/_gen_chain.py — DO NOT HAND-EDIT.
     Edit GOOD-MORNING.md / _LIVE-STATE.md and regenerate; `--check` is a build step. -->

# The read chain — everything a cold session must read, and nothing else

> **This file is GENERATED and it is the whole contract.** It carries `GOOD-MORNING.md`'s header
> and ★ LATEST banner, plus `_LIVE-STATE.md`'s ⏱ LATEST delta — the three things GM-D7-am names
> (CUT #33 on Dave's ruling). **If you have read this file, you have read the chain.**
>
> ⚠ **Do NOT now open `GOOD-MORNING.md` to "check".** It is 49,043 real and 86% of it is
> retrieval surface you are not meant to pay for at boot. That reflex is the thing this file
> exists to stop — five sessions called the chain CUT and every one of them paid full price.
>
> ★ **Everything else is RETRIEVAL, never a reading list:**
> `python3 knowledge/_memento_search.py "<q>"` → `--fetch <id>`. §A orientation is `gm:A`
> (a router with 11 children); the queue is `gm:C1`…; the archives, briefs, runbooks and ledgers
> are all indexed. **Ask for what you need; do not read a file to find out whether you need it.**

---

> **YOU ARE #138. TITLE THIS CHAT →** `Apollo - #138: fontconfig out of the tree — the permanent fix`
> *(read it back in your opener — the chat half of the ritual is ungateable, which is why this line is FIRST)*

---

<!-- ⛔ STOP. If you are a cold session, you are in the wrong file and you pay for ALL of it.
     Read `_CHAIN.md` instead — it is GENERATED from this file's header + ★ LATEST banner and
     `_LIVE-STATE.md`'s ⏱ LATEST delta, which is the ENTIRE contract (GM-D7-am, CUT #33).
     This file stays whole for RETRIEVAL: `_memento_search.py "<q>"` → `--fetch <id>`.
     ⚠ Reading on from here is not thoroughness — it is the exact overspend #41 measured. -->

# Good morning, Dave ☕

> **size:** GM **49.0K tk real** (49,043 exact) · §A **7.0K tk real (EXEMPT)** (6,957 exact) · LS **64.2K tk real** (64,177 exact) · measured 2026-08-09 #137 (`_gauge_tokens.count()` → `real`), **on the artefact, AFTER the regen, iterated to a FIXED POINT** [[chain-is-a-swap-not-an-add]]. ⚠ **THE READ-CHAIN FIGURE IS DELIBERATELY ABSENT — its ONE home is `_CHAIN.md`'s own generated footer** (retired #45). **Point at it; never restate it.** ✅ **THE UNIT IS REAL CLAUDE TOKENS (#82-D1, Dave's, ruled #54) — MEASURED, NEVER CONVERTED; ratios REPORTED, NEVER APPLIED.** *(This line's correction-provenance — #90's self-reproduction, the `CHAIN_STAMP_RE` scope hole closed #94, the #90 2f-roll discharge — HOMED BY ADDITION at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET, then cut here [[home-by-addition-then-cut]].)*
> **STATE: ★★ THE READ CHAIN IS CUT (#33) and it HELD — EIGHTH session running.** You are reading the contract. §A and §C are STILL IN THIS FILE, reached by `_memento_search.py`, not by reading top-to-bottom. ⛔ **BUILD VERDICT: 75 of 102 steps green (#62, `18c7789`) — 27 steps have NEVER been in a green verdict.** Both counts GENERATED from `_build_all.py`'s AST at each end; the shortfall is computed (`s125-D1`). A single-process `_build_all.py` run is SANDBOX-IMPOSSIBLE (~49s vs the ~45s call kill); CI delivers the single-process verdict on push. ⚠ **DO NOT RUN `_build_all.py` TO CHECK — ANY PARTIAL run strands the tree in the documented mid-build intermediate** (step 1 rewrites wholesale; healed only by a COMPLETE pass — docstring lines 5–21). ★ **Only the PREMISE of a derived rule is worth re-verifying** (#47's lesson, turned on #47). *(The #47/#61/#62 build-claim archaeology + the #38 cap-repoint finding are HOMED at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET and `notes/_briefs/2026-07-29-cap-repoint-and-lane-divvy-brief.md`.)*
> ⚠ **`set_content()` IS BANNED IN RENDER-PROOFS** — drops `type.css` silently (14px→16px) and `document.fonts.check` passes anyway; use `goto("file://…")` (#29, measured) · **ds-020 is a gate-enforced obligation** (the DV-D02-A scatter waiver names it as clears-when) · **ritual step 2g: rebuild `_memento-index.json` LAST** — `index_freshness_check` is BLOCKING and compares CONTENT, never mtime (runbook has the body).
> **★ PRICE IN REAL TOKENS — THE UNIT IS `_gauge_tokens.py`, NOT A PERCENTAGE (#56, Dave's).** **amber 160,000 (PICKED) · working 200,000 (SOURCED) · hard 256,000 (SOURCED)** — provenance per `_gauge_tokens.py:63–83`, the authority [[read-chain-is-where-staleness-is-free]]. Unit = the FULL price (boot + job + wrap); ⚠ **a DECLARED gap passes, a SILENT one fails** — that asymmetry is the mechanism. The retired 45/60/63 % band is DORMANT in code, pinned — retire-or-pin = `G8`, Dave's. ⚠ **THE FLOOR IS MEASURED = `ds-025` RE-SCOPED #109 — first turn 65,400±1,400 real (n=5) + `_CHAIN.md` 10,499 additive = 75,899 floor** (was published 30,499 — under by 45,400; the OLD `disk 11,032 + harness unreachable` framing on this line was FALSE, retired #109). `MEMORY.md` 8,470 of the first-turn figure is split and measured; **56,308 remains unattributed — that is what `ds-025` item 1 now means.** ⛔★★★ **FIGURE SUPERSEDED #129 (`s129-D1`, Dave's): FLOOR RE-BASED 81,335 → 70,794** (`BOOT_FIRSTTURN_TK` 65,400 → 54,859 ±1,178, n=7). **By ADDITION: the `ds-025` question above is UNCHANGED; only the constant moved.** Authority = `_gauge_tokens.py`, never this line. ⚠ **`pip install tiktoken --break-system-packages` FIRST in a cold sandbox** — the gauge REFUSES, loud and named (`MeasurementRefused`); `_build_all.py` P5 = ABORT without it (#80). ★ **READ THE CHAIN ONLY.** ★ **Repo-state claims are verified against `git log` / a real run, never a banner.** *(The #58/#59/#80/#83 correction-provenance is HOMED at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET; the #109 boot-floor correction at `_DECISION-HISTORY/2026-08-06-109-boot-floor-measured.md` and § ★ #109.)*
> ⚠ **Render sandbox is FRESH every session** (~4 calls — price it) · **retrieval FIRST:** `_memento_search.py "<q>"` → `--fetch <id>` (⚠ **it served a two-session-old record until #32 — if an answer looks stale, check the index**) · **wrap moves RUN THROUGH `_gm_move.py`** (⚠ **banner headings are BLOCKQUOTED — anchor `> ## ★ PRIOR …`**) · **PACE PANEL: ask at the opener, no gate can see it** — Dave has declined a fresh one twice and put Fable on the table for judgment work; ✅ **THE "BEHIND PACE ⇒ MORE WINDOWS" CLAUSE IS RETIRED — RULED #60: multi-window is settled, two use cases (quota catch-up AND production crank), Claude offers/Dave vetoes, cap 3 revisable. `notes/_MEMENTO-DECISIONS.md` § ★ #60-D1.** It stood from #48; at #57 he said the opposite — *"we are behind, but I'm less comfortable with multiple windows, too much to decide and consume"* ⇒ **ONE window with more delegation.** ★ **The clause optimised the wrong quantity: it treated TOKENS as the constraint when DAVE'S DECISION LOAD is what binds.** ⚠ **Reflected back #57, and #58's opener did NOT settle it** (asked as the first question; he answered pace with *"no constraint, lets just fix the problems"*). Standing home + status: §C·4.
> *(**RENAME is delivered IN CHAT at wrap, not here — RULED #28, ENACTED in runbook step 4b by #30.**)*
> **TITLE THE NEXT CHAT →** `Apollo - #138: fontconfig out of the tree — the permanent fix`
> ✅ *(the #86 hand-patch that stood here is REMOVED by the #87 wrap, per its own text — #85/#86 are recorded as HOLE lines + ledger stubs, `notes/_GAUGE-LOG.md` / § ★ #85–#86.)*
> *(Titles are LABELS — role comes from Dave's opener line. Bands: the ONLY copy is
> `_RUNBOOK-context-gauge.md` § ★ THE FLOOR IS MEASURED — grep it, never recall it.)*
> ## ★ LATEST — 2026-08-09 (Sun **#137**, OPUS conductor, Dave live, ONE lane — dream-pass-6 triage, no build subs, this wrap sub only — ✅ **`s137-D1` RULED AND ENACTED: the push gate stops dirtying its own tree** · ★ **A FALSE GREEN CAUGHT BEFORE IT WAS REPORTED** · ⛔ **FONTCONFIG WRITES INTO THE REPO IT SCANS — CLEANED, CAUSE UNFIXED** · ⚠ **`_capture_gate.py --selftest` FOUND RED SINCE #135 — 1,739 → 7** · ✅ **QUOTA POLLED** · ✅ **WRAP OPENED BELOW THE LINE**)
>
> - ✅ **`s137-D1`** — Dave, over two named alternatives: the `--push` clean-tree assertion excludes **exactly one named file**, `notes/_REHEARSAL-LOG.jsonl` — the one tracked file the verification instruments themselves write (`_capture_gate.py --wrap`, `_checkin.py`), so verifying a commit after making it dirtied the tree and refused the very push `s133-D2` exists to allow. Named IN FULL so the exclusion cannot silently widen. **ENACTED** `knowledge/_git_commit.sh:43-44`; the refusal now also **PRINTS the dirty paths**. **DRIVEN three ways on the artefact's own bytes:** (A) real dirt → rc=1, lists everything, omits the log · (B) log-only → rc=0 · (C) mutation, log + one other → rc=1, **exclusion proven NARROW**. `bash -n` clean. ⚠ **NOT DRIVEN: the end-to-end `--push` — driving it means pushing. UNPROVEN BY CHOICE, declared.** Authority: `knowledge/_rulings.json` § `s137-D1` (98 records).
> - ⬛ **THE LANE'S SOURCE — dream pass 6 fired on its scheduled Sun 07:10 slot and committed `0219075`** (273 lines, `notes/_dream/2026-08-09-proposals.md`, five floated proposals). **Found by a boot-time `git log`, NOT by any gate:** #136's wrap banner was generated at 15:40 and cannot name it, so the largest piece of new work waiting for #137 was **absent from #137's own residual list**. All five premises RE-VERIFIED first-hand (the dreamer has no shell); **all five hold**. ⛔ **P1, P3, P4, P5 remain DAVE'S and UNRULED** — only P2 became `s137-D1`.
> - ★ **A FALSE GREEN, CAUGHT BEFORE IT WAS REPORTED — the method finding.** The first test of `s137-D1` inserted a 6-line comment block, which shifted the line numbers; `sed -n '43,44p'` had been written `'40,41p'`, so it grabbed two COMMENT lines — and **`eval` of comments exits 0**. It printed "GUARD PASSED" having tested nothing. Caught, owned to Dave, redone against the real line numbers. [[green-tests-cannot-see-scope]]. ⚠ Twice this session `$?` after a pipe gave the **PIPE's** code — the #130 defect, recurring.
> - ⚠ **FOUND BY THIS WRAP, NOT BY THE SESSION: `_capture_gate.py --selftest` has been RED since #135** — `_rulings.json` stores `evidence` (and `governs`, from #136) as STRINGS on six records where `_governs.py:294` iterates them as pointer LISTS, so it walked ratified prose character by character: **1,739 failures**. ★ **Nothing runs that selftest at wrap** — only `_build_all.py`, which is sandbox-impossible, so three wraps committed over it ([[instrument-without-a-consumer]]). Attributed with a control: RED on HEAD too ⇒ **#135 introduced it**. **Repaired BY ADDITION — nothing trimmed: 1,739 → 7**, and `s137-D1` now surfaces on the file it governs (driven both directions: before `d0802-P5` only; after, both). ⬛ **The 7 residual are prose-in-`evidence` on five inherited ratified records — NOT a wrap sub's to trim.**
> - ⛔ **FONTCONFIG WRITES INTO THE REPO IT SCANS.** Three untracked files stamped 2026-08-08 22:57 (#136's render run) in `knowledge/assets/fonts/_desktop/TTF/` — `.uuid`, `.uuid.LCK`, `.uuid.TMP-NpSPVs` — **moved to `_to_delete/_fontconfig_strays/`; the tree now has ZERO untracked paths**, 10 tracked `.ttf` untouched. **CAUSE UNFIXED:** #136's ENOSPC fix removed the font copy and pointed `FONTCONFIG_FILE`'s `<dir>` at the repo TTF dir (`_RUNBOOK-render-verify.md:42,46`). ★ Same class as `s137-D1` — an instrument writing into the tree it measures; **two instances in one session makes it a class**. Permanent `/var/tmp` symlink-farm fix **DESIGNED AND PRICED, NOT ENACTED, NOT PROVEN** (needs a real render run; 18,402 of room did not fit one). Dave: *"no patches or hacks, solve it permanently please."* ⛔ **Do NOT gitignore `.uuid*`.**
> - ⚠ **DREAM PASS 6 FALSIFIED ITS OWN CHECKED-CLEAR ITEM.** (cc1) claims a whole-tree grep for `github_pat_`/`ghp_…` returns 0 files; re-run at #137 it returns **TWO** — the proposals file itself (lines 156, 205, 249) and `_memento-index.json`, which indexed it. ✅ **No leak** — every hit is the string in backticks in the dreamer's own prose *about* the grep; **cc1's VERDICT is re-verified and STANDS**, only its count is stale. USE-vs-MENTION with no scope.
> - ★ **The serializer guard fired and earned its keep (held ×6):** the first `_rulings.json` write used `ensure_ascii=False` and the round-trip check **REFUSED before writing**. Recipe, measured: `json.dumps(d, indent=1)` · `ensure_ascii` at DEFAULT · **no trailing newline** · assert byte-identical on the UNCHANGED file first (passed at 136,663 B).
> - ⚙ boot **54,636** real — in the 54,859±1,178 band, another datapoint, **never corrected into the constant** · FILL at wrap-open **132,527** vs stop line **150,929** — ✅ BELOW, **18,402 of room**; three check-ins ran (opener · lane seam · wrap decision) · delegation: this wrap sub only, no build subs · **QUOTA POLLED, first since #135, all three unprompted: session 5% (resets 3h15m) · weekly all-models 30% · weekly Fable 39% (both weeklies reset Thu 11:00 PM)** · evidence: `knowledge/_rulings.json` § `s137-D1` · `knowledge/_git_commit.sh:43-44` · `notes/_dream/2026-08-09-proposals.md` (`0219075`) · dossier `_DECISION-HISTORY/2026-08-09-137-dream-pass-6-triage.md`.
> **residual → #138:** ⬛ **① FONTCONFIG OUT OF THE TREE — the permanent fix: designed + priced, NOT enacted, NOT proven; Dave's "no patches" standing instruction; needs a real render run [0]** · ⬛ **② the 7 residual `_governs` pointers + NO write-time parser for `_rulings.json` — trimming ratified text, so Dave's [0]** · ⬛ **③ dream pass 6 P1 · P3 · P4 · P5 — all still Dave's, unruled [0]** · ⬛ **④ Dave's wrap-step candidate: `git log` since boot, foreign commits named in the banner — FLOATED, NOT RULED [0]** · ⬛ **⑤ the 16 tier-map rows — Dave's eye, controller ready, picks persist [1]** · ⬛ **⑥ `s136-D1` enact lanes: slots key rollout · props.binds audit · DTCG re-encode · fork-ban gate [1]** · ⬛ **⑦ lane A `s135-D4` enactment sub — still HELD [1]** · ⬛ **⑧ clause-A copy refinement readback [1]** · ⬛ **⑨ #67-D2 reconciliation readback — Dave said "cool", not ruled [1]** · ⬛ **⑩ mono no-border extension readback [3]** · ⬛ **⑪ carried set from #135: `s135-D1` enactment · 15 token-split exceptions · s130 remainder · mark-vs-fill 3.0 · SC info-tint · contrast-selftest arms · #130 carried roll (23rd) · render-proof eye pass s134-D4 [2]**.
> **residual (GENERATED #137):** 2c OK (banners 2/2) · 2d OK (deltas 3/3) · 2f OK (strata 1, log #136) — _roll_state.py · 2026-08-09
> **⬛ OPEN WORKLIST — PRESENCE INDEX (28 items, GENERATED). Every open item is named; NO bodies are here — `--fetch gm:DOFIRST`.**
> `0b` ENCODE BEFORE THE WAVE, part-consumed 28 · `0c` NEXT BUILD CANDIDATES · `0d` THE 67 ENACT WAVE, landed · `1` ds-018 C2 follow-through · `2` dv-legend/dv-behaviour CEILING · `3` ds-012(b) gutter-relative plot area · `4` DV-D16 floating growth · `5` Instrument-fit remainder · `6` ds-016, PART-RULED #114 · `7` ds-017, UNRULED · `8` STILL OWED, none superseded · `9` DELEGATION TOPOLOGY · `10` ✅ PER-GATE TEST PLAN · `11` 2c-ROLL / INDEX-VOCAB DEADLOCK · `12` #57 1b DOSSIER · `13` tmp RUNBOOK EXPOSURE, unfixed · `14` ⬛ DAVE'S FOUNDING PRINCIPLE · `15` LEDGER § ★ #59 · `16` UNHOMED PAIR (#78 2c EXIT CHECK) · `17` CHAIN-DIET CUT · `19` type.css 180 dark-mode specificity · `20` ds-029 FIRST IDIOM · `21` PER-THEME COLLISION SWEEP · `22` 89-D2 ruled not enacted, absent from state · `23` ds-025 re-scoped 109, not closed · `24` 110 boot-rent plan · `25` ds-032 enactment, ruled 106 · `26` ✅ boot-drift gate rulings
> **QUEUE — `gm:C1` strands · `gm:C2` ruling batch (Dave's) · `gm:C4` enact-queue.**

---

## ⏱ LATEST DELTA — 2026-08-09 (Sun **#137**, OPUS conductor, Dave live, ONE lane — dream-pass-6 triage, no build subs, this wrap sub only — ✅ **`s137-D1` RULED *AND* ENACTED, DAVE'S: the `--push` clean-tree assertion excludes exactly ONE named file** · ★ **A FALSE GREEN CAUGHT BEFORE IT WAS REPORTED** · ⛔ **FONTCONFIG WRITES INTO THE REPO IT SCANS — CLEANED, CAUSE UNFIXED** · ⚠ **`_capture_gate.py --selftest` FOUND RED SINCE #135 — 1,739 → 7** · ✅ **QUOTA POLLED, ALL THREE NUMBERS** · ✅ **WRAP OPENED BELOW THE LINE**)

**Landed (evidence per claim):**

- **① ✅ `s137-D1` — RULED BY DAVE AND ENACTED, same window.** The `--push` clean-tree assertion now excludes exactly one named file, `notes/_REHEARSAL-LOG.jsonl` — **the one tracked file the verification instruments themselves write** (`_capture_gate.py --wrap`, `_checkin.py`), so verifying a commit after making it dirtied the tree and refused the very push `s133-D2` exists to allow. Dave picked it over two named alternatives (move the append after the staging seam; stop tracking the log). The path is named IN FULL so the exclusion cannot silently widen. Enacted `knowledge/_git_commit.sh:43-44` (`PUSH_DIRT` + `git status --short -- . ':(exclude)notes/_REHEARSAL-LOG.jsonl'`); **the refusal now also PRINTS the dirty paths, which it did not before.** **DRIVEN, not asserted — three ways on the artefact's own bytes:** (A) real tree, real dirt → rc=1, lists everything, omits the log · (B) scratch tree, only the log dirty → rc=0 · (C) mutation, log + one other file → rc=1, **exclusion proven NARROW**. `bash -n` clean. ⚠ **NOT DRIVEN: the end-to-end `--push`, because driving it means pushing — UNPROVEN BY CHOICE, declared.** Authority: `knowledge/_rulings.json` § `s137-D1` (98 records).
- **② ⬛ THE LANE'S SOURCE: dream pass 6 fired on schedule and committed `0219075`** — 273 lines, `notes/_dream/2026-08-09-proposals.md`, five floated proposals P1–P5. **Found by a boot-time `git log`, NOT by any gate.** #136's wrap banner was generated at 15:40 and cannot name a commit made at 07:10 the same day, so **the largest piece of new work waiting for #137 was absent from #137's own residual list**. All five premises were RE-VERIFIED first-hand against the repo (the dreamer runs cold, with no shell); **all five hold**. ⛔ **P1, P3, P4 and P5 remain DAVE'S and UNRULED** — only P2 became `s137-D1`. Do not enact P3's runbook amendment, do not touch P4's credential line, do not remove `--all-dirty` (P5), do not bulk-rewrite the 21 frozen statuses (P1).
- **③ ★ A FALSE GREEN, CAUGHT BEFORE IT WAS REPORTED — and it is the method finding of the session.** The FIRST test of `s137-D1` was invalid: inserting a 6-line comment block shifted the line numbers, `sed -n '43,44p'` had been written `'40,41p'`, so it grabbed two COMMENT lines — and `eval` of comments exits 0. **It printed "GUARD PASSED" having tested nothing.** Caught in-window, owned to Dave, redone against the real line numbers. [[green-tests-cannot-see-scope]] — *a green that can't fail is an assertion*. ⚠ Twice this session `$?` was read after a pipe and returned the **PIPE's** exit code — the #130 defect, recurring.
- **④ ★ THE SERIALIZER GUARD FIRED AND EARNED ITS KEEP — held ×6.** The first `_rulings.json` write attempt used `ensure_ascii=False`; the round-trip check **REFUSED before writing**, because the file stores em-dashes escaped and every such line would have been silently rewritten. Measured recipe, now three knobs: `json.dumps(d, indent=1)`, `ensure_ascii` LEFT AT DEFAULT, **no trailing newline**.
- **⑤ ⛔ FONTCONFIG STRAYS CLEANED, CAUSE UNFIXED.** Three untracked files stamped 2026-08-08 22:57 (#136's render run) in `knowledge/assets/fonts/_desktop/TTF/` — `.uuid`, `.uuid.LCK`, `.uuid.TMP-NpSPVs` — moved to `_to_delete/_fontconfig_strays/`; **tree now has zero untracked paths**, the 10 tracked `.ttf` files untouched. Root cause and the designed permanent fix are in § OPEN, born #137. ⛔ **NOT gitignored — Dave explicitly refused a patch.**
- **⑥ ⚠ NEW, MEASURED BY THIS WRAP AND NOT INHERITED: `_capture_gate.py --selftest` has been RED since #135.** `_rulings.json` stores `evidence` (and, from #136, `governs`) as STRINGS on six records where `_governs.py` iterates them as pointer LISTS — so it walked them character by character and emitted **1,739 failures** of the form *"points at `p` which does not exist"*. ★ **Nothing runs that selftest at wrap:** `_capture_gate.py --wrap` does not call it; the only consumer is `_capture_gate.py --selftest` inside `_build_all.py`, which is sandbox-impossible — [[instrument-without-a-consumer]], and three wraps committed over it. **Attributed with a control:** the selftest is RED on HEAD's `_rulings.json` too, so #135 introduced the class and #136/#137 widened it. **Repaired BY ADDITION this wrap — nothing trimmed, no ratified byte changed:** every string field wrapped in a list, and `s137-D1`'s real path PREPENDED to `governs`. **1,739 → 7** honest failures, and `s137-D1` now surfaces on `knowledge/_git_commit.sh` — **driven both directions** (before: only `d0802-P5`; after: `d0802-P5` + `s137-D1`). ⬛ **The residual 7 are NOT a wrap sub's to clear:** every one is prose in an `evidence` field where a resolvable pointer is required, and clearing them means TRIMMING ratified ruling text on five inherited records. Homed in § OPEN, born #137.
- **⑦ ✅ QUOTA POLLED — first since #135, and Dave gave all three panel numbers unprompted:** session **5%** (resets in 3h15m) · weekly all-models **30%** · weekly Fable **39%** — both weeklies reset **Thu 11:00 PM**. *(A bare percentage is ambiguous; all three are recorded on purpose.)*
- **⚙ GAUGE.** boot **54,636** real — inside the 54,859 ±1,178 band, **another datapoint, NOT corrected into the constant**. FILL at wrap-open **132,527** vs stop line **150,929** — ✅ BELOW, **18,402 of room**, which is why the fontconfig render lane was recorded rather than rushed. Three check-ins ran: opener · lane seam · wrap decision. Delegation: this wrap sub only, no build subs.


---

## ⬛ OPEN WORK — GENERATED from `_state.json`. **The row count IS the count.**

> **36 items · 36 live · 22 Dave's · 14 mine · 17 carry a stated close condition · **19 UNCONDITIONED**.**
> *Every figure in this block is computed from the store at generation time. No number here was typed by anyone — that is the point of it (#86 measured a typed inventory of "118 markers" against a real ~40; #85's "95 slots / 84 distinct" is not reproducible by any probe in this repo).*
> ⚠ **DECLARED DEBT — 19 inherited items have NO close condition and therefore cannot close.** They are exempt as a FROZEN set (`_state.LEGACY_IDS`, size 19) which may only shrink; a NEW item is refused without one. **Each needs Dave's word — an agent inventing a close condition for his open work is the same overreach as inventing his ruling.**
>
> **DAVE'S (22) — 17 with a ratified `closes_when`, 5 with NONE:**
> - `G1` **Worklist-index cap DOFIRST_INDEX_TK_MAX = 700 (_capture_gate.py:1403,…** — *closes when:* Dave ratifies 700 or names his own number
> - `G10` **The "70%/95%" stray band (GM:36** — *closes when:* Dave rules provenance or strikes it
> - `G11` **DS-018 recessive value** — *closes when:* Dave picks the recessive value
> - `G12` **Charter §4b tone-of-voice temperature map, PROVISIONAL 2026-07-02…** — *closes when:* tov-016 review lands, or Dave ratifies the mapping as-is
> - `G13b` **menu-search combined glyph, PROVISIONAL 2026-07-16 (_ICON-GAPS.md; the…** — *closes when:* Dave approves the glyph
> - `G14` **Icon-button dark bindings** — *closes when:* Dave rules the SC-dark bindings
> - `G15` **DV-D13 donut centre figure + st.visible[id]=true release wiring…** — *closes when:* Dave's sign-off eye
> - `G16` **The _proforma/_DATAVIZ-DECISIONS.md:567 enactment call (agent's, not…** — *closes when:* Dave ratifies or reverses
> - `G17` **RAG status manifestation…** — *closes when:* Dave's canon pick — A / A+B / A+B+C
> - `G2` **TAPE_TO_BILL = 1.57 at n=2 (_capture_gate.py:371; RATIO_FIRM_N = 4 per…** — *closes when:* 4 measured pairs logged, then Dave rules firm-or-retire
> - `G3` **retired_unit_prose_audit tier = WARN (_capture_gate.py:2078, 2098,…** — *closes when:* Dave rules warn vs block
> - `G4` **GM §C measured 191 > 150 warn cap (_capture_gate.py:2864,…** — *closes when:* Dave picks OFFLOAD / TRIM / KEEP
> - `G5` **Four advisory size caps as a set (_capture_gate.py:4843–4858** — *closes when:* Re-measured in real (G9 first), then Dave ratifies the set in one pass
> - `G6` **DEFER_STREAK = 6 (_gm_usage.py:353) + USAGE_HISTORY_BLOCKING = False** — *closes when:* Dave rules the streak number + per-candidate OFFLOAD/TRIM/KEEP calls
> - `G7` **Which end of an archive is "newest" (strata oldest-first · archives…** — *closes when:* Dave names the convention — one sentence
> - `G8` **Dormant % band BAND_FLOOR/HARD_STOP/MARKED_MAX = 45/60/63…** — *closes when:* Dave says retire or pin
> - `G9` **ds-023 re-measurement programme** — *closes when:* Dave rules the programme
> - `W-0c` **NEXT BUILD CANDIDATES** — *closes when:* ⛔ **none stated — cannot close until you name one**
> - `W-0d` **✅ THE #67 ENACT WAVE** — *closes when:* ⛔ **none stated — cannot close until you name one**
> - `W-14` **⬛ DAVE'S FOUNDING PRINCIPLE** — *closes when:* ⛔ **none stated — cannot close until you name one**
> - `W-15` **LEDGER § ★ #59** — *closes when:* ⛔ **none stated — cannot close until you name one**
> - `W-16` **UNHOMED PAIR, copied up at the #78 2c EXIT CHECK** — *closes when:* ⛔ **none stated — cannot close until you name one**
>
> **MINE (14), ids only — bodies are in the store, not here:** `W-0b` · `W-01` · `W-02` · `W-03` · `W-04` · `W-05` · `W-06` · `W-07` · `W-08` · `W-09` · `W-10` · `W-11` · `W-12` · `W-13`
>
> ⚠ store gate: **PASS**. Bodies, conditions and provenance: `python3 knowledge/_state.py`.


---

*(Chain ends. **12,330 real — the unit is THE WHOLE FILE**, this generated wrapper included:
the number in this sentence is the size of the file containing it, held exact by a fixed point.
`GOOD-MORNING.md` is 49,043 real and stays whole for retrieval; you have paid for
25% of it, and that is the intended figure, not a shortfall.)*
