<!-- GENERATED by knowledge/_gen_chain.py — DO NOT HAND-EDIT.
     Edit GOOD-MORNING.md / _LIVE-STATE.md and regenerate; `--check` is a build step. -->

# The read chain — everything a cold session must read, and nothing else

> **This file is GENERATED and it is the whole contract.** It carries `GOOD-MORNING.md`'s header
> and ★ LATEST banner, plus `_LIVE-STATE.md`'s ⏱ LATEST delta — the three things GM-D7-am names
> (CUT #33 on Dave's ruling). **If you have read this file, you have read the chain.**
>
> ⚠ **Do NOT now open `GOOD-MORNING.md` to "check".** It is 72,466 real and 79% of it is
> retrieval surface you are not meant to pay for at boot. That reflex is the thing this file
> exists to stop — five sessions called the chain CUT and every one of them paid full price.
>
> ★ **Everything else is RETRIEVAL, never a reading list:**
> `python3 knowledge/_memento_search.py "<q>"` → `--fetch <id>`. §A orientation is `gm:A`
> (a router with 11 children); the queue is `gm:C1`…; the archives, briefs, runbooks and ledgers
> are all indexed. **Ask for what you need; do not read a file to find out whether you need it.**

---

> **YOU ARE #216. TITLE THIS CHAT →** `Apollo - #216: the proposed-for-dave sets from today are all awaiting dave's own eye`
> *(read it back in your opener — the chat half of the ritual is ungateable, which is why this line is FIRST)*

---

<!-- ⛔ STOP. If you are a cold session, you are in the wrong file and you pay for ALL of it.
     Read `_CHAIN.md` instead — it is GENERATED from this file's header + ★ LATEST banner and
     `_LIVE-STATE.md`'s ⏱ LATEST delta, which is the ENTIRE contract (GM-D7-am, CUT #33).
     This file stays whole for RETRIEVAL: `_memento_search.py "<q>"` → `--fetch <id>`.
     ⚠ Reading on from here is not thoroughness — it is the exact overspend #41 measured. -->

# Good morning, Dave ☕

> **size:** GM **72.5K tk real** (72506 exact) · §A **7.0K tk real (EXEMPT)** (6957 exact) · LS **70.6K tk real** (70625 exact) · measured 2026-08-22 #215 (`_gm_usage.py --sizes` → `real`, DECLARE-LAST — after 2c/2d/2f, before the 2g index rebuild), **on the artefact, AFTER the rolls, iterated to a FIXED POINT** [[chain-is-a-swap-not-an-add]]. ⚠ **THE READ-CHAIN FIGURE IS DELIBERATELY ABSENT — its ONE home is `_CHAIN.md`'s own generated footer** (retired #45). **Point at it; never restate it.** ✅ **THE UNIT IS REAL CLAUDE TOKENS (#82-D1, Dave's, ruled #54) — MEASURED, NEVER CONVERTED; ratios REPORTED, NEVER APPLIED.** *(This line's correction-provenance is HOMED at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET, then cut here [[home-by-addition-then-cut]].)*
> **STATE: ★★ THE READ CHAIN IS CUT (#33) and it HELD.** *(the session-count ordinal is DELETED per s178-D1 enacting dream-P1 — stale twice ⇒ generate, never re-stamp)* You are reading the contract. §A and §C are STILL IN THIS FILE, reached by `_memento_search.py`, not by reading top-to-bottom. ⛔ **BUILD VERDICT: 75 of 128 steps green (#62, `18c7789`) — 53 steps have NEVER been in a green verdict.** Both counts GENERATED from `_build_all.py`'s AST at each end; the shortfall is computed (`s125-D1`). A single-process `_build_all.py` run is SANDBOX-IMPOSSIBLE (~49s vs the ~45s call kill); CI delivers the single-process verdict on push. ⚠ **DO NOT RUN `_build_all.py` TO CHECK — ANY PARTIAL run strands the tree in the documented mid-build intermediate** (step 1 rewrites wholesale; healed only by a COMPLETE pass — docstring lines 5–21). ★ **Only the PREMISE of a derived rule is worth re-verifying** (#47's lesson, turned on #47). *(The #47/#61/#62 build-claim archaeology + the #38 cap-repoint finding are HOMED at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET and `notes/_briefs/2026-07-29-cap-repoint-and-lane-divvy-brief.md`.)*
> ⚠ **`set_content()` IS BANNED IN RENDER-PROOFS** — drops `type.css` silently (14px→16px) and `document.fonts.check` passes anyway; use `goto("file://…")` (#29, measured) · **ds-020 is a gate-enforced obligation** (the DV-D02-A scatter waiver names it as clears-when) · **ritual step 2g: rebuild `_memento-index.json` LAST** — `index_freshness_check` is BLOCKING and compares CONTENT, never mtime (runbook has the body).
> **★ PRICE IN REAL TOKENS — THE UNIT IS `_gauge_tokens.py`, NOT A PERCENTAGE (#56, Dave's).** **amber 160,000 (PICKED) · working 200,000 (SOURCED) · hard 256,000 (SOURCED)** — provenance per `_gauge_tokens.py:63–83`, the authority [[read-chain-is-where-staleness-is-free]]. Unit = the FULL price (boot + job + wrap); ⚠ **a DECLARED gap passes, a SILENT one fails** — that asymmetry is the mechanism. The retired 45/60/63 % band is DORMANT in code, pinned — retire-or-pin = `G8`, Dave's. ⚠ **THE FLOOR IS MEASURED = `ds-025` RE-SCOPED #109 — first turn 65,400±1,400 real (n=5) + `_CHAIN.md` 10,499 additive = 75,899 floor** (was published 30,499 — under by 45,400; the OLD `disk 11,032 + harness unreachable` framing on this line was FALSE, retired #109). `MEMORY.md` 8,470 of the first-turn figure is split and measured; **56,308 remains unattributed — that is what `ds-025` item 1 now means.** ⛔★★★ **FIGURE SUPERSEDED #129 (`s129-D1`, Dave's): FLOOR RE-BASED 81,335 → 70,794** (`BOOT_FIRSTTURN_TK` 65,400 → 54,859 ±1,178, n=7). **By ADDITION: the `ds-025` question above is UNCHANGED; only the constant moved.** Authority = `_gauge_tokens.py`, never this line. ⚠ **`pip install tiktoken --break-system-packages` FIRST in a cold sandbox** — the gauge REFUSES, loud and named (`MeasurementRefused`); `_build_all.py` P5 = ABORT without it (#80). ★ **READ THE CHAIN ONLY.** ★ **Repo-state claims are verified against `git log` / a real run, never a banner.** *(The #58/#59/#80/#83 correction-provenance is HOMED at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET; the #109 boot-floor correction at `_DECISION-HISTORY/2026-08-06-109-boot-floor-measured.md` and § ★ #109.)*
> ⚠ **Render sandbox is FRESH every session** (~4 calls — price it) · **retrieval FIRST:** `_memento_search.py "<q>"` → `--fetch <id>` (⚠ **it served a two-session-old record until #32 — if an answer looks stale, check the index**) · **wrap moves RUN THROUGH `_gm_move.py`** (⚠ **banner headings are BLOCKQUOTED — anchor `> ## ★ PRIOR …`**) · **PACE PANEL: ask at the opener, no gate can see it** — Dave has declined a fresh one twice and put Fable on the table for judgment work; ✅ **THE "BEHIND PACE ⇒ MORE WINDOWS" CLAUSE IS RETIRED — RULED #60: multi-window is settled, two use cases (quota catch-up AND production crank), Claude offers/Dave vetoes, cap 3 revisable. `notes/_MEMENTO-DECISIONS.md` § ★ #60-D1.** It stood from #48; at #57 he said the opposite — *"we are behind, but I'm less comfortable with multiple windows, too much to decide and consume"* ⇒ **ONE window with more delegation.** ★ **The clause optimised the wrong quantity: it treated TOKENS as the constraint when DAVE'S DECISION LOAD is what binds.** ⚠ **Reflected back #57, and #58's opener did NOT settle it** (asked as the first question; he answered pace with *"no constraint, lets just fix the problems"*). Standing home + status: §C·4.
> *(**RENAME is delivered IN CHAT at wrap, not here — RULED #28, ENACTED in runbook step 4b by #30.**)*
> **TITLE THE NEXT CHAT →** `Apollo - #216: the proposed-for-dave sets from today are all awaiting dave's own eye`
> ✅ *(the #86 hand-patch that stood here is REMOVED by the #87 wrap, per its own text — #85/#86 are recorded as HOLE lines + ledger stubs, `notes/_GAUGE-LOG.md` / § ★ #85–#86.)*
> *(Titles are LABELS — role comes from Dave's opener line. Bands: the ONLY copy is
> `_RUNBOOK-context-gauge.md` § ★ THE FLOOR IS MEASURED — grep it, never recall it.)*
> ## ★ LATEST — 2026-08-22 (Sat from `date` **#215**, conductor + 4 work subs (MEASURED, n=4, all Opus) (plus this OPUS wrap sub), **DELEGATED wrap**, ★★★ **FIVE RULINGS INSCRIBED: `knowledge/_rulings.json` § `s215-D1`…`s215-D5`** — ★★ **THE DAY THE DESK GOT CLEARED AND CI WENT GREEN AFTER TWENTY REDS**)
>
> - ★ **⓪ THE SHAPE — DAVE CLEARED HIS OWN DESK IN ONE MORNING AND THEN THE RED STREAK GOT PEELED AT CAUSE.** Five rulings, **NINE commits** (⚠ the brief said eight — **read back from `git log`, nine carry `after #215`**; `6a9be36` is the one the brief omitted), all pushed by the conductor, and **CI #413 GREEN on `e645df2` — the first green in twenty runs**, read back by him in chat. His standing word today, now a memory hook: *"always real fixes never patches, they just get lost."*
> - ★★★ **① `s215-D1` — THE `W-` ID SPACE IS WIDENED AND THE STOPGAPS ARE GRANDFATHERED.** `ID_RE` goes `W-[0-9]{1,2}[a-z]?` → `W-[0-9]{1,3}[a-z]{0,2}`; the eight `W-99za`…`W-99zh` stand **VERBATIM** because ids are addresses (WRITE-ONCE, ADR-0017) and renaming them would rot every citation. Next fresh mint **W-100**; used this session **W-100…W-106**. `G` untouched. ⛔ **DRIVEN, not believed: `python3 knowledge/_state.py` now PASSES** — 161 items, 0 refused ids, 0 unresolvable homes, against #214's 8 refused + 2 unresolvable. Commit `9aba453` (+ two ambiguous homes repointed).
> - ★★ **② `s215-D2` AND `s215-D3` — THE ITINERARY STOPS BEING A DESK ITEM, AND `P8` IS FINALLY RULED.** `s215-D2` adopts ITINERARY-STATUS v3 wholesale: the 84 understating rows take their derived store statuses, **row 124 is an honest GAP** (joining row 86 as the only two true gaps), and the **27 off-ratchet Layer-2 snippets are queued ADVISORY, not a blocker**. `s215-D3` rules `P8` at **reading 2 — model-conditional verification**: canon rule 5's adversarial verifier STAYS for Fable-run and high-stakes work, routine re-check-your-own-work scaffolding COMES OFF Opus 5 sessions. **`MODEL-ROUTING.md` amended in the same commit** — `997e40c`, which also carried the canon regen that cured the `[45]` layer of the red streak. `P8` had been carried unruled since #212.
> - ★★★ **③ `s215-D4` + `s215-D5` — THE LIBRARY BECOMES THE INDEX, AND FINDABILITY SHIPS WHOLE.** The ruled ladder: **Foundations · Tokens**, then **Primitives (if needed) / Element / Pattern / Block / Shell / Template**; facet chips replaced by **two tabs, Usage and Type** (Dave: *"Instead of filters can I just have two tabs usage and type"*). All five findability decisions **yes in one word** — v2 **REPLACES** `showroom/index.html` (two indexes would drift) · a **status** facet · **thumbnail-first** browsing · **related-components** cross-links seeded from the 68-alias table · a **machine-readable flat JSON index**. Built: `showroom/index.html` + `showroom/index.json` (135 rows) + `showroom/_thumbs/*.png` (135 screenshots, 1.4 MB), new `knowledge/_render/gen_thumbs.py` and `drive_library_215.py`, `gen_showroom.py`'s index assembly **removed** with `PROTECTED={"index.html"}` so the orphan prune cannot eat the library, selftest **9 → 23 bites**, old address kept alive as a generated redirect stub. Commit `46eb387`; rows `W-102`/`W-103`; receipt `notes/_receipts/2026-08-22-215-library-v2-ia.md`.
> - ★★★ **④ THE `canon.css` 48 IS FIXED AT CAUSE — `48 → 0` — AND THE ROUTE THERE IS THE FINDING.** The absorb prefixer now emits `:where(.cn-<scope>)`, so the scaffolding stops lending specificity; nothing authored is ever wrapped and containment is byte-for-byte unchanged. ⛔ **TWO INFLATORS WERE MASKING EACH OTHER, and the measurement says so**: `:where()` on the prefixer ALONE moved the count **48 → 49 (UP)**, because every dead override slid off the per-component trim onto the hand-authored `.canon :is(…)` default; only `:where(.canon)` on that global rule too gave **0**. The shrink-only ratchet is lowered to the measured **0**. Beside it **G2 is BUILT** — the `--computed` render leg (`getComputedStyle` in headless Chromium), which is the instrument that proves the FEATURE rather than the clause [[mutation-tests-the-clause-not-the-feature]]. Commit `0272996`; rows `W-100`/`W-101`; receipt `notes/_receipts/2026-08-22-215-prefixer-where-fix.md`.
> - ★★ **⑤ THE LIBRARY CHROME IS RESTYLED TO THE SWISS / INTERNATIONAL STYLE, WITH BOTH PROJECT SUBSTITUTIONS DECLARED.** Skill contract `.claude/skills/swiss-design-system/SKILL.md`, read first and followed — with two standing-law overrides written on the face of the receipt: accent `#DB0011` → **`#DA1A00`** (`s151-D1`, the two-red law, red-on-white) and `--black: #000000` → **`#1A1A1A`** (the ink rule, blackest-not-pure-black), **selftest bite 25 proving neither skill value appears in the page**. Project face kept, skill fallback chain appended, `document.fonts.check('16px "Univers Next for HSBC"')` driven **true**. Same commit fixed two gate classes at cause — **directory-address doc rows** (`home` ending in `/` rows every file beneath it, first use `W-106` for the six nav PNGs) and the stale memento index. Commit `1b08f68`; row `W-105`; receipt `notes/_receipts/2026-08-22-215-library-swiss-restyle.md`.
> - ✅ **⑥ THE TWENTY-RUN RED STREAK IS OVER, AND IT WAS PEELED LAYER BY LAYER RATHER THAN SILENCED.** The onion, in the order it came off: **`[45]` canon determinism** → **help gates ×3** (`2a61618`, `4108f16` — the #158 write-by-default class, twice more) → **stale memento index** → **radius / wiring / forks** (`6a9be36`: `W-90` radius RULED bound, the wiring exemption NAMED, two forks DECLARED) → **`ds-018` dangling var** (`e645df2`: the `W-90` bind done properly through the **surface slot + manifest**, Kpi-tile precedent, so `ds-018` resolves clean). **Every layer fixed at cause with its receipt in the commit message**, per Dave's own words today. ⛔ **CI #413 GREEN is the CONDUCTOR'S read-back, quoted from his brief — this seat cannot reach CI and does not claim to have seen it.**
> - ★★ **⑦ A RECEIPT'S OWN CLAIM WAS CAUGHT FALSE BY DRIVING, BEFORE IT CALCIFIED.** The prefixer receipt's open-to-Dave item 1 asserted the `:is()` split was a **MATCHING bug** — that the leading-trim rule had only ever matched `button`. **DISPROVEN BY DRIVING and WITHDRAWN**: a computed-style probe in headless Chromium against the real canon, a `.cn-button` wrapper holding `button` + `a` + `span`, in **BOTH eras** (today's `:where()` file and the pre-#215 form at `161db61`), returns `text-box-trim: trim-both` on **all three elements in both files**. The reasoning error is named in the correction: `:is(button, .cn-x a)` under an outer `.cn-x` scope does not demand a SECOND `.cn-x` ancestor — the same ancestor satisfies both. **Redundant, not broken.** What remains is cosmetic argument bloat. ★ **Appended BY ADDITION to the receipt** — the original claim stands as written history, the correction sits beneath it with its method restated in full so the probe re-derives [[home-by-addition-then-cut]].
> - ⚙ **⑧ GAUGE — THREE MOMENTS, STATED SEPARATELY AND NEVER ROUNDED INTO ONE ANOTHER** [[measure-dont-convert-units]]. **boot 60,248 real** — ⛔ **OUT of the `s208-D1` band (56,749 ± 1,154 → 55,595–57,903) by 2,345, DECLARED at the opener and NOT corrected into the constant**; band read from `knowledge/_gauge_tokens.py:178-179`, never from a hook [[tape-unit-is-not-real-tokens]] · **brief-cut FILL 271,145 real**, MEASURED first-hand from the conductor's transcript at the turn that wrote the brief (⚠ **the brief declared no brief-cut figure**, so this seat measured it rather than leaving the `s215-D5`-successor field unobservable) · this wrap sub's own first-hand cut, **FILL 274,588 real over 216 turns** (`_checkin.py`, `message.usage`, last turn — a FLOOR, late by one step). ⛔ **FILL crossed the ADVISORY stop line 150,929 and the 200,000 working line, both DECLARED live in chat at the 251,090 check-in.** ✅ **The `s214-D1` band conditions HELD and were verified, not asserted:** the recall probe was quizzed **BLIND** and `knowledge/_probe/session-215.json` re-read first-hand at this wrap — `"verdict": "GREEN"`, `"missed": []`, last check `11:21:31Z`; post-crossing work was mechanical/receipt-backed and every ruling predates the crossing. **subs 805,306 tokens (n=4)** — QUOTA, never window FILL [[budget-vs-quota-vocabulary]]. ⛔ **NO CONSTANT, THRESHOLD, STOP LINE, WALL OR BAND WAS TOUCHED.**
> - ★★ **⑨ THE `s214-D4` STAGING CONDITION IS NOW MET — AND MEETING IT IS ALL THIS WRAP DOES ABOUT IT.** This wrap writes the **SECOND** `s214-D5` hand-over row (`n=2` of the required `n≥2`), so the re-derived **~190,000** advisory has its data. ⛔ **IT IS NOT IN FORCE AND THIS SEAT DID NOT PUT IT THERE — `150,929` REMAINS THE ADVISORY IN FORCE.** Switching it is an **OPENER ACT** and it is the **CONDUCTOR'S**, at #216; a wrap sub recording a condition is not a wrap sub arming a line [[feedback-dont-launder-a-premise-into-a-ruling]].
> - ⛔ **⑩ WHAT THIS WRAP DID NOT DO, NAMED RATHER THAN SMOOTHED.** **No push and no CI read-back** — both the conductor's (`s207-D1` / `s203-D1`) · **no memory step 3** — the conductor discharged it in-session and the store is **NON-REPO**, invisible to this seat, so it is recorded as **HIS DECLARED ACT and not verified** · **nothing on the DO-NOT-RULE fence**: `knowledge/_rulings.json` was **READ-ONLY** at this wrap (its only writer is `_inscribe_ruling.py`, and no ruling happens at a wrap), no constant/threshold/band/advisory moved, no Dave-owned row closed · **no §C·1 / DO-FIRST 2e roll** — §C and DO-FIRST measured inside their warn caps, so nothing was even in the warn condition; the deferred thing is the CONTENT trim, which would re-scope Dave's standing worklist and is not a wrap sub's.
> - ⚠ **⑪ CONSEQUENCES AND PITFALLS — MANDATORY (Dave #165).** **(a)** ★ **A FIX THAT MOVES A COUNT THE WRONG WAY IS THE MEASUREMENT DOING ITS JOB** (④): `48 → 49` was the honest reading of a half-fix, and stopping there would have shipped a "repair" that made the artefact worse while every gate stayed green. **Two dominators can hide each other, and only re-measuring after each one separates them.** **(b)** ⛔ **A RECEIPT IS NOT EVIDENCE UNTIL IT IS DRIVEN** (⑦): the matching-bug claim was written in good faith by a sub, was wrong, and would have become a fact the moment it was carried onto a banner [[mutation-tests-the-clause-not-the-feature]]. **(c)** ⚠ **THE HELP-GATE CLASS BIT THREE MORE TIMES IN ONE SESSION** (⑥) — `#158`'s write-by-default shape is still being paid per-script; a gate that catches it at birth is the priced candidate and nothing prevents the fourth. **(d)** ⚠ **A WRAP BRIEF'S COMMIT LIST IS A CLAIM LIKE ANY OTHER** (⓪): it said eight, `git log` says nine [[premise-ages-faster-than-rule]]. **(e)** ⚠ **A FOREIGN SESSION'S ARTEFACT IS NOW LOAD-BEARING FOR RENDERING** — the chromium libs at `/var/tmp/chromelibs-s213e2` belong to `#213`; the re-extract recipe is homed in `_RUNBOOK-render-verify.md`'s fourth stratum, but the directory itself is **not the repo's and can vanish**.
>
> **residual → #216:** ⬛ **① THE PROPOSED-FOR-DAVE SETS FROM TODAY ARE ALL AWAITING DAVE'S OWN EYE [NEW — 0, DAVE'S]** — 16 usage-group assignments + the **Charts → Data-display fold** (`notes/_receipts/2026-08-22-215-library-v2-ia.md` §4) · the **status derivation rule** (§6) · **grey-4 decorative numerals at 2.01:1** · **related-cluster membership** · the **empty Foundations/Tokens tiers** · the **12 PFD rows over 41 components** in `notes/_briefs/2026-08-22-215-taxonomy-research-v2.md` §2. Rows `W-102`/`W-104`/`W-105`. ⚠ **② G2's SCOPE IS LIGHT MODE AND TWO WIDTHS ONLY [NEW — 0]** — dark, the other three themes and the script states are **UNDRIVEN**; the leg proves the class it was built for and nothing wider. Row `W-101`. ⚠ **③ FIVE THINGS ARE PRICED AND NOT BUILT [NEW — 0]** — the thumbnail staleness leg · the Swiss-structure bite · the contrast selftest leg · the regen-serial class fix (**third conductor instance**) · the `mask_comments` dedup gate. The last two are also older carries below, at their own ages. ▫ **④ THE PREFIXER `:is()` COSMETIC BLOAT IS A LOW ROW [NEW — 0]** — the "matching bug" was **DISPROVEN BY DRIVING** and the correction is appended to `notes/_receipts/2026-08-22-215-prefixer-where-fix.md` with its computed-style table over both eras; what is left is argument bloat plus a theoretical `html`/`:root`-leading edge that no current argument has. ⚠ **⑤ A FOREIGN-SESSION ARTEFACT IS A RENDERING DEPENDENCY [NEW — 0]** — chromium libs at `/var/tmp/chromelibs-s213e2` (#213's); the re-extract recipe is now the fourth stratum of `knowledge/_RUNBOOK-render-verify.md`, which also records that the **#125 TLS reading is ENVIRONMENT-DEPENDENT** and both #125 readings were true of their own sessions. ⚠ **⑥ A CI READ-BACK IS OWED AT #216 FOR THIS WRAP'S OWN PUSH [NEW — 0]** — the conductor pushes after reading this report, and the read-back is his (`s203-D1`). ⬛ **⑦ THE `s214-D4` ADVISORY SWITCH IS THE CONDUCTOR'S OPENER ACT AT #216 [NEW — 0]** — the `n≥2` condition is **MET at this wrap** and the ~190,000 line is ready; putting it in force is an opener announcement, not a wrap's, and `150,929` stands until he makes it. ⛔ **SIX #215 CARRIES ARE STRUCK, EACH WITH ITS RECEIPT NAMED (`s183-D1` P2, strengthened by `s188-D2`) — struck, never silently dropped:** **② THE `canon.css` 48-SELECTOR SPECIFICITY CLASS IS HIS** — ~~carried~~ **FIXED AT CAUSE AT #215**: the absorb prefixer made specificity-neutral with `:where()`, **48 → 0**, ratchet lowered to the measured zero. Receipt: `notes/_receipts/2026-08-22-215-prefixer-where-fix.md`; commit `0272996`; row `W-100`. **③ TWO LIBRARY-v2 CALLS ARE HIS** — ~~carried~~ **BOTH RULED AT #215**: v2 REPLACES `showroom/index.html` (`s215-D5`) and the level word-set is the ruled ladder (`s215-D4`). Receipt: `knowledge/_rulings.json` § `s215-D4`/`s215-D5`, read from the store; commit `46eb387`; row `W-102`. **④ ROW-124 SCOPE AND THE 27 OFF-RATCHET SNIPPETS ARE HIS** — ~~carried~~ **RULED AT #215 (`s215-D2`)**: row 124 is an honest GAP, the 27 are queued ADVISORY. Receipt: `knowledge/_rulings.json` § `s215-D2`; commit `997e40c`. **⑥ G2 — THE `--computed` DESCENDER LEG — IS PRICED, NOT BUILT** — ~~carried~~ **BUILT AT #215**, with its scope declared as new carry ② above. Receipt: `notes/_receipts/2026-08-22-215-prefixer-where-fix.md` §4; commit `0272996`; row `W-101`. **⑩ THE `s214-D4` STAGING NEEDS ONE MORE DELEGATED WRAP** — ~~carried~~ **CONDITION MET AT #215**: this wrap writes the `n=2` hand-over row. Receipt: the `wrap-handover` line in this session's stratum below. ⛔ **Met is not armed** — the switch is new carry ⑦ above. **② THE `W-` TWO-DIGIT ID SPACE IS EXHAUSTED AT `W-99` AND THE SCHEME IS RUNNING ON A STOPGAP [was 2, DAVE'S]** — ~~carried~~ **RULED AT #215 (`s215-D1`)**: three digits, stopgaps grandfathered verbatim, next mint W-100. Receipt: `knowledge/_rulings.json` § `s215-D1`; commit `9aba453`; `python3 knowledge/_state.py` PASSES. **① `P8` IS THE ROUTING AMENDMENT LEFT UNRULED, AND IT IS DAVE'S [was 2, DAVE'S]** — ~~carried~~ **RULED AT #215 (`s215-D3`)** at reading 2, model-conditional; `MODEL-ROUTING.md` amended in the same commit. Receipt: `knowledge/_rulings.json` § `s215-D3`; commit `997e40c`. **PRIOR CARRIES, AGES +1, WORDING UNCHANGED (`s183-D1` P2 / `s188-D2`) — re-typed so the gate can MEASURE them:** ⬛ **① THE REVIEW-213 TRANCHE PLAN IS DAVE'S TO ADOPT, AMEND OR DISCARD [1, DAVE'S]** — `notes/_briefs/2026-08-21-214-review-213-tranche-map-v1.md`, row `W-99zc`: 6 sittings of ~20–35 questions each over the 172, statement family confirmed at 5 members (incl. Template-list-index), lock-ups measured **NOT** a family (14.8%). ⬛ **⑤ `ds-005` HAS NO `_rulings.json` ROW, SO A RETRIEVAL-BY-DEFAULT QUERY ANSWERS "NEVER DECIDED" [1, DAVE'S]** — it lives only in `_DS-IMPROVEMENTS.md` prose; retro-inscription is a promotion and promotions are his [[forgotten-document-class]]. ⚠ **⑦ 46 OF 135 SNIPPETS SHIP NO BEHAVIOUR JS [1]** — sidebar-nav 0, command-palette 0, all lock-ups; Replay is disabled with the reason stated on the page, and side-nav `aria-current` is authored static (the rail toggle works, destination selection does not). Row `W-99zg`. ⬛ **⑧ THE SELF-CONTAINED-SNIPPET vs SHARED-CORE ARCHITECTURE QUESTION WAS SURFACED TO DAVE IN CHAT AND HAD NO REPO HOME [1, DAVE'S]** — ⛔ **DECLARED FROM THE BRIEF, NOT VERIFIED BY THIS SEAT**: `grep -rn -i "self-contained"` across the #214 receipts and `knowledge/_state.json` returned **0 hits** [[unmatched-grep-is-not-an-absence]]. **Given a standing home in §C·4 at this wrap** so it stops being a chat-only citation. ⚠ **⑨ THE `_checkin.py` PROBE-STATUS WIRING (OPTION B) IS PRICED AND HIS [1, DAVE'S]** — row `W-99z`; Option A is in force meanwhile. ⬛ **① REVIEW-213 IS DAVE'S TO RULE IN TRANCHES, AND 172 QUESTIONS ARE WAITING [2, DAVE'S]** — `reviews/REVIEW-213-wave-components-four-theme-v1.html`, row `W-99i`; his words are the plan (*"in tranches over the next few days"*), and **28 of the 43 component metas carry no `$decisionsForDave`, so those questions live only in receipt prose** — that gap is named in `W-99i`'s body and is not repaired here. ⚠ **③ CI #401 AND #402 READ-BACKS ARE OWED AT THE #214 OPENER [2]** — the conductor's (`s203-D1`); **#400 was read back FAILURE with the declared pair, and the render job's composition is UNPROVEN and declared.** ⬛ **③ THE STOP-LINE / WALL / BOOT-BAND RE-BASE IS HIS, AND THE NUMBERS HE ASKED FOR ARE NOW MINED [3, DAVE'S]** — banner ⑨ carries the nine measured brief-cut FILL figures and the honest finding that **no conductor-side delegated-wrap cost field exists in `notes/_GAUGE-LOG.md`**; his `s208-D1` rider binds the proposal (a boot-REDUCTION option priced beside any re-base, and **today's boot ran 63,258 — 5,355 OVER the band**). ⬛ **④ `W-99d` — THE TWO G9 RIDERS `s212-D11` DELIBERATELY DID NOT RULE [3, DAVE'S]** — the `bill_of()` **1.57×** overstatement repair, and the re-PICK of `SIZE_BUDGET` compactable **8000** + `TITLE_CAP_TAPE` **120** (picked-never-measured, so **no restamp is possible** and only a fresh pick will do). ⬛ **⑦ THREE ROWS OPENED THIS SESSION ARE HIS TO OPEN OR PARK [3, DAVE'S]** — `W-96` unregistered-source gate (build-or-park) · `W-98` dv-lockup build wave (blocked on his three placeholder titles) · `W-99c` the G15+G16 donut sitting he said he wants to get into. ⚠ **⑧ THE BY-REFERENCE CARRY TAIL NOW COSTS A REDIRECT AT EVERY WRAP, AND THE COST COMPOUNDS [3]** — #211's pointer said the deep tail was *"STILL LIVE IN THIS FILE"*; this wrap rolled that banner, so the tail now lives at `_GM-ARCHIVE.md` § **Batch 2026-08-21 #212**. Redirected below, not left wrong. **Reversing to the measured (re-typed) form is available and is Dave's preference to state.** ⚠ **⑨ THE GOVERNING REGISTER HAS NO JOIN GATE, AND THREE ROWS PROVED IT [3]** — `G3`/`G7`/`G8` read **OPEN** in `knowledge/_GOVERNING-RECORDS.md` while `knowledge/_state.json` had them closed since **#161/#163**, and `G18` has a store row and no register row at all. Repaired at this wrap by MARKING (never deleting), with their own pre-existing pointers. ⛔ **Nothing prevents the next recurrence** — a register-vs-store join check is the priced candidate and building it is not a wrap's. ⚠ **⑩ THIS WRAP'S PUSH ADDS A SECOND OWED CI VERDICT [3]** — the read-back is the conductor's (`s203-D1`) and the queue is now #397 (`da35003`, from #211) plus this wrap's own run. **PRIOR CARRIES, AGES +1, WORDING UNCHANGED (`s183-D1` P2 / `s188-D2`) — the five that survive, re-typed so the gate can MEASURE them:** ⬛ **① DAVE'S #211 SITTING IS SEVEN RECEIPTS WIDE AND NOT ONE ITEM IN IT WAS RULED [4, DAVE'S]** — the **12 ABSENT tints** (`W-87`) · the **`ds-005` class choice** (`W-91`) · the **`P-6` ink-verdict gate extension** (`W-88`) · **`P-7`/`P-8`/`P-6` promote-or-park** (`W-85`) · **REVIEW-204's 7th `.l-split` instance** and ~~the **4 canon `border-radius:0` literals** (`W-90`)~~ **— PARTIAL RETRACTION, WITH ITS RECEIPT: the `W-90` radius bind WAS ruled by Dave in chat at #215 and ENACTED PROPERLY (border-radius/surface slot + manifest, Kpi-tile precedent), commits `6a9be36` + `e645df2`; the rest of this sitting is untouched** · **`error-solid.svg` / `FIGMA_TOKEN`** (`W-89`) · R6's **three priced residuals** (shared `mask_comments` helper · `declared_value` regex→CSS-parse · the now-closed Image-block exposure) (`W-92`). Every other one is PRICED IN ITS RECEIPT and none is ruled. ⬛ **② THE PHOTOGRAPHY PIPELINE RULING IS HIS AND IT BLOCKS THE MANIFEST/KG/TAGGING WORK [4, DAVE'S]** — `W-93`, NON-REPO vs LFS vs committed derivatives. ⚠ **③ #397 (`da35003`) IS UNREAD AND ITS VERDICT IS OWED [4]** — the conductor's, `s203-D1`. ⚠ **⑤ THE REGEN-SERIAL CLASS FIX REMAINS UNBUILT AFTER A SECOND CONDUCTOR INSTANCE [4]**. ⚠ **⑥ `mask_comments` IS NOW DUPLICATED ACROSS TWO GENERATORS WITH NO COMPARING GATE [4]** (`W-92`).
> ⛔ **THE CARRIES STRUCK AT #212, #213 AND #214 ARE NOT RE-TYPED HERE, AND THAT IS NOT A DROP:** the `[115]` healer and the §C-vocabulary finding (struck #212), the §C offload and the two-rulings-as-queued-work pair (struck #213), and the itinerary `LAYER2_NOTE` premise, the memory-step-3 carry and the 2c-carry-gate blindness (struck #214) were each struck **with their receipts** on banners now at `_GM-ARCHIVE.md` § **Batch 2026-08-21 #213/#214** and § **Batch 2026-08-22 #215**. A struck carry stays struck.
> **THE DEEP PRIOR TAIL IS CARRIED BY REFERENCE AND ITS ADDRESS IS UNCHANGED BY THIS WRAP** — `_GM-ARCHIVE.md` § **Batch 2026-08-21 #212**, verbatim, at its #210 ages. ★ **2c/2d EXIT CHECKS RAN AS PROSE AND THEIR PROBE IS NAMED SO THE CLAIM IS FALSIFIABLE** [[unmatched-grep-is-not-an-absence]]: the rolling **#213 banner** (20 lines, 5 lines carrying ⚠/⬛/AWAITING markers) cites **30 distinct `W-`/`G-` rows, 0 of them missing from `knowledge/_state.json`**; the rolling **#212 delta** (16 lines, 4 marker lines) cites **30 rows, 0 missing**. ⇒ every rowed carry is homed in the STANDING, GENERATED open-work block of `_CHAIN.md`, computed from the store at generation time. ⛔ Nothing was dropped, shaved or re-worded to make this banner cheaper — **`s214-D6` commands SHORTER, never decide-what-to-drop**, and the compression here is girth, never content.
> ⚠ **DECLARED, BESIDE THE CARRIES AND NEVER INSIDE THEM.** `carry_wording_check` (`knowledge/_capture_gate.py`, BLOCKING) was RUN and its output is quoted in this session's stratum. ⛔ **AND ONE BESIDE-NOTE THE CARRIES THEMSELVES MAY NOT ABSORB:** carry **③ [2]** says CI #401/#402 read-backs are owed at the **#214** opener. This session read back **#413 GREEN** and peeled the reds behind it, but **whether #401 and #402 specifically were ever read is NOT observable from this seat** — no CI reach, and the brief does not say. It is therefore re-typed **WORDING UNCHANGED** rather than struck on an assumption [[unrun-search-indistinguishable-from-absent-record]].
>
> **residual (GENERATED #215):** 2c OK (banners 2/2) · 2d OK (deltas 3/3) · 2f OK (strata 1, log #214) — _roll_state.py · 2026-08-22
>
> **⬛ OPEN WORKLIST — PRESENCE INDEX (28 items, GENERATED). Every open item is named; NO bodies are here — `--fetch gm:DOFIRST`.**
> `0b` ENCODE BEFORE THE WAVE, part-consumed 28 · `0c` NEXT BUILD CANDIDATES · `0d` THE 67 ENACT WAVE, landed · `1` ds-018 C2 follow-through · `2` dv-legend/dv-behaviour CEILING · `3` ds-012(b) gutter-relative plot area · `4` DV-D16 floating growth · `5` Instrument-fit remainder · `6` ds-016, PART-RULED #114 · `7` ds-017, UNRULED · `8` STILL OWED, none superseded · `9` DELEGATION TOPOLOGY · `10` ✅ PER-GATE TEST PLAN · `11` 2c-ROLL / INDEX-VOCAB DEADLOCK · `12` #57 1b DOSSIER · `13` tmp RUNBOOK EXPOSURE, unfixed · `14` ⬛ DAVE'S FOUNDING PRINCIPLE · `15` LEDGER § ★ #59 · `16` UNHOMED PAIR (#78 2c EXIT CHECK) · `17` CHAIN-DIET CUT · `19` type.css 180 dark-mode specificity · `20` ds-029 FIRST IDIOM · `21` PER-THEME COLLISION SWEEP · `22` 89-D2 ruled not enacted, absent from state · `23` ds-025 re-scoped 109, not closed · `24` 110 boot-rent plan · `25` ds-032 enactment, ruled 106 · `26` ✅ boot-drift gate rulings
> **QUEUE — `gm:C1` strands · `gm:C2` ruling batch (Dave's) · `gm:C4` enact-queue.**

---

## ⏱ LATEST DELTA — 2026-08-22 (Sat from `date`) (**#215**, conductor + 4 work subs (MEASURED, n=4, all Opus) (plus this OPUS wrap sub), DELEGATED wrap, ★★★ **FIVE RULINGS INSCRIBED `s215-D1`…`s215-D5`** — the day the desk got cleared and CI went green after twenty reds)

*(WHY/HOW: `_DECISION-HISTORY/2026-08-22-215-the-red-streak-onion.md`. Receipts: `notes/_receipts/2026-08-22-215-{prefixer-where-fix,library-v2-ia,library-swiss-restyle}.md`; brief `notes/_briefs/2026-08-22-215-taxonomy-research-v2.md`. Rulings READ from `knowledge/_rulings.json`, never retyped from the brief.)*

- ★★★ **FIVE RULINGS, EACH ENACTED IN THE COMMIT THAT CARRIED IT.** `s215-D1` widens `ID_RE` to three digits and **grandfathers the eight `W-99za`…`W-99zh` stopgaps VERBATIM** (ids are addresses, ADR-0017); next mint **W-100**, used today **W-100…W-106**; `python3 knowledge/_state.py` now **PASSES** where #214 left it failing on 8 refused ids + 2 unresolvable homes. `s215-D2` adopts **ITINERARY-STATUS v3** wholesale — row 124 an honest **GAP**, the 27 off-ratchet Layer-2 snippets **queued ADVISORY** — and the itinerary stops being a desk item. `s215-D3` rules **`P8` at reading 2, model-conditional**: the adversarial verifier stays for Fable-run and high-stakes work, routine self-verification scaffolding comes off Opus 5 sessions; `MODEL-ROUTING.md` amended in the same commit. `s215-D4` rules the ladder **Foundations · Tokens**, then **Primitives (if needed) / Element / Pattern / Block / Shell / Template**, with facet chips replaced by two tabs (**Usage** and **Type**). `s215-D5` takes **all five findability decisions yes**.
- ★★★ **THE LIBRARY IS NOW THE INDEX, NOT A REVIEW PAGE.** `showroom/index.html` IS Library v2 (`s215-D5`(1) — two indexes would drift); beside it `showroom/index.json` (135 rows, machine-readable) and `showroom/_thumbs/*.png` (135 screenshots, 1.4 MB). `gen_showroom.py`'s index assembly is **REMOVED** and `PROTECTED={"index.html"}` stops the orphan prune deleting the library it no longer generates; selftest **9 → 23 bites**; the old `reviews/LIBRARY-2026-08-21-v2.html` address is kept alive as a generated redirect stub. New machinery: `knowledge/_render/gen_thumbs.py` (chunked, resumable) and `drive_library_215.py`. Rows `W-102`/`W-103`.
- ★★★ **THE `canon.css` 48 IS ZERO, AND THE ROUTE THERE IS THE FINDING.** The absorb prefixer emits `:where(.cn-<scope>)`, so scaffolding stops lending specificity while matching identically. ⛔ **Two inflators were masking each other**: `:where()` on the prefixer ALONE took the count **48 → 49 (UP)**, because every dead override slid onto the hand-authored `.canon :is(…)` default; only `:where(.canon)` on that rule too gave **0**. Ratchet lowered to the measured **0**. Beside it **G2 is BUILT** — the `--computed` render leg (`getComputedStyle`, headless Chromium) — ⚠ **scope declared narrow: light mode, two widths; dark, the other three themes and the script states are UNDRIVEN**. Rows `W-100`/`W-101`.
- ★★ **THE CHROME IS SWISS, WITH BOTH PROJECT SUBSTITUTIONS DECLARED RATHER THAN QUIETLY APPLIED.** Skill contract `.claude/skills/swiss-design-system/SKILL.md`; accent `#DB0011` → **`#DA1A00`** (`s151-D1`, the two-red law) and `--black: #000000` → **`#1A1A1A`** (the ink rule), **selftest bite 25 proving neither skill value appears in the page**; project face kept with the skill's fallback chain appended, `document.fonts.check` driven true. Same commit fixed **two gate classes at cause** — directory-address doc rows (a `home` ending in `/` rows every file beneath it; first use `W-106`) and the stale memento index. Row `W-105`.
- ✅ **THE TWENTY-RUN RED STREAK IS OVER AND IT WAS AN ONION, NOT A DEFECT.** In the order the layers came off: `[45]` canon determinism → help gates ×3 (the #158 write-by-default class, three more instances) → stale memento index → radius/wiring/forks (`6a9be36`) → `ds-018` dangling var (`e645df2`, the `W-90` bind done properly via the **surface slot + manifest**, Kpi-tile precedent). **CI #413 GREEN on `e645df2`** — ⚠ **the conductor's read-back, quoted from his brief; this seat cannot reach CI.**
- ★★ **A RECEIPT'S OWN CLAIM WAS CAUGHT FALSE BY DRIVING.** The prefixer receipt's open item 1 — *the `:is()` split is a MATCHING bug* — is **DISPROVEN AND WITHDRAWN**: computed-style probe against the real canon, `.cn-button` holding `button` + `a` + `span`, **both eras** (today's file and `161db61`), `text-box-trim: trim-both` on **all three in both**. `:is(button, .cn-x a)` under an outer `.cn-x` does not demand a second `.cn-x` ancestor. **Redundant, not broken**; what is left is cosmetic. Correction appended **BY ADDITION** with its method restated in full.
- ⚙ **GAUGE — THREE MOMENTS, NONE ROUNDED INTO ANOTHER** [[measure-dont-convert-units]]. **boot 60,248 real — ⛔ OUT of the `s208-D1` band (55,595–57,903) by 2,345, DECLARED at the opener and NOT corrected into the constant** (band read from `knowledge/_gauge_tokens.py:178-179`) · **brief-cut FILL 271,145 real**, MEASURED first-hand from the conductor's transcript (⚠ the brief declared no figure) · this wrap sub's own first-hand cut **274,588 real over 216 turns**. ⛔ FILL crossed the **150,929** advisory and the **200,000** working line, **both declared live in chat at 251,090**. ✅ `s214-D1`'s band conditions **HELD and were verified**: the recall probe quizzed BLIND, `knowledge/_probe/session-215.json` re-read first-hand — `"verdict": "GREEN"`, `"missed": []`. **subs 805,306 tokens (n=4)**, QUOTA never window FILL. ⛔ No constant, threshold, stop line, wall or band was touched.
- ⛔ **NOT RUN / NOT CLAIMED BY THIS WRAP:** no push and no CI read-back (both the conductor's, `s207-D1` / `s203-D1`) · no memory step 3 — **the conductor discharged it in-session and the store is NON-REPO, so it is recorded as HIS declared act and not verified** · nothing on the DO-NOT-RULE fence: `knowledge/_rulings.json` READ-ONLY, no constant or advisory moved, no Dave-owned row closed · no §C·1 / DO-FIRST 2e roll (both measured inside their warn caps).
- ⬛ **OPEN AND DAVE'S, CARRIED FORWARD:** every PROPOSED-FOR-DAVE set from today (16 usage-group assignments · the Charts→Data-display fold · the status derivation rule · grey-4 decorative numerals at 2.01:1 · related-cluster membership · the empty Foundations/Tokens tiers · 12 PFD rows over 41 components) · the REVIEW-213 tranche sittings (172 questions, `W-99i`) · `ds-005`'s missing `_rulings.json` row · the stop-line / wall / boot-band re-base · ⛔ **the `s214-D4` advisory switch — its `n≥2` condition is MET at this wrap, but arming it is the CONDUCTOR'S OPENER ACT at #216 and `150,929` stands until he makes it.**


---

## ⬛ OPEN WORK — GENERATED from `_state.json`. **The row count IS the count.**

> **162 items · 94 live · 42 Dave's · 52 mine · 148 carry a stated close condition · **14 UNCONDITIONED**.**
> *Every figure in this block is computed from the store at generation time. No number here was typed by anyone — that is the point of it (#86 measured a typed inventory of "118 markers" against a real ~40; #85's "95 slots / 84 distinct" is not reproducible by any probe in this repo).*
> ⚠ **DECLARED DEBT — 14 inherited items have NO close condition and therefore cannot close.** They are exempt as a FROZEN set (`_state.LEGACY_IDS`, size 19) which may only shrink; a NEW item is refused without one. **Each needs Dave's word — an agent inventing a close condition for his open work is the same overreach as inventing his ruling.**
>
> **DAVE'S (42) — 42 with a ratified `closes_when`, 0 with NONE:**
> - `G15` **DV-D13 donut centre figure + st.visible[id]=true release wiring…** — *closes when:* Dave's sign-off eye
> - `G16` **The _proforma/_DATAVIZ-DECISIONS.md:567 enactment call (agent's, not…** — *closes when:* Dave ratifies or reverses
> - `W-104` **#215 taxonomy research v2 - Carbon deep-dive, 135-component ladder map, lifecycle vocabulary, name distribution, nav screenshots** — *closes when:* Dave has ruled on the 12 PROPOSED-FOR-DAVE placements in section 2.3 and on Map A vs Map B (whether a Primitives tier is used), and gen_library_214.py LEVELS + level_of() have been changed to match, with the section 2.2 table regenerated from the changed derivation
> - `W-38` **Size-ramp + per-theme size modes proposal (FLOATED, Dave's instinct, #200)** — *closes when:* Dave rules the pair semantics and the per-theme mode mapping, or drops the shape
> - `W-41` **#203 delegated-wrap brief (DO-NOT-RULE list + session facts for the Opus wrap sub)** — *closes when:* the #203 wrap commit is pushed by the conductor under s203-D1 AND the CI read-back is relayed to Dave in chat - both limbs, per the ruling's own words
> - `W-51` **#207 addendum: Dave's critique of the #206 record (promotion vocabulary open; candidature homed)** — *closes when:* Dave rules the twice-caught vocabulary question (does a mined historical occasion count as a 'catch' toward s204-D1's bar?) AND rules promote/wait/park on the three candidates; the candidature record in knowledge/_DS-IMPROVEMENTS.md then carries the outcome
> - `W-53` **#207 W-46 three scoped proposals (items 3/4/5) — returning to Dave** — *closes when:* Dave rules build/park per item on the three scoped proposals; W-46 closes on the same event
> - `W-63` **wave-3 NINE components BUILT PROPOSED-NOT-RULED - Daves eye owed (incl. two existence questions)** — *closes when:* Dave rules per component: promote / rework / delete; existence questions answered first
> - `W-66` **two #210 spacing fixes PROPOSED - Time-picker label descender clearance (4px->8px) and Data-grid state switcher margin (0->28px)** — *closes when:* Dave's eye accepts the two fixes on reviews/REVIEW-210-spacing-fixes-v1.html - or moves either value; and he rules whether the two CLASSES (the Input-fields family's identical 4px lblrow, and Table/Segmented-control's identical 0px flush switcher) are swept or left
> - `W-70` **The four progressbar-improvising snippets: do they CONSUME Meter? (named as future work by the #210 impacts memo, deliberately unscoped)** — *closes when:* Dave has ruled whether Progress-tracker, File-upload, Stepper and Runway-bar - the FOUR non-meter snippets that carry role=progressbar in MARKUP and each hand-roll their own track and fill - should CONSUME the Meter molecule rather than re-drawing it, or stay as they are; and if he rules consume, the lane that does it is scoped separately. This row closes on that ruling alone, not on any build.
> - `W-71` **wave-4 lane C — carousel + image-block BUILT PROPOSED-NOT-RULED, Daves eye owed** — *closes when:* Dave rules per component: promote / rework / delete; the autoplay-variant question, the arrow-glyph byte-match deviation, and the type-composite debt-growth tension are answered first
> - `W-72` **wave-4 lane A - calendar + tree BUILT PROPOSED-NOT-RULED - Daves eye owed (19 open questions, 3 defects in GATED components)** — *closes when:* Dave has ruled on the lane-A questions (chiefly Q1 does Date-picker consume Calendar, Q9 the tree selection mark, Q17 the descender-clip gate class) and the conductor has actioned or parked the three findings in already-gated files (Date-picker's today+selected ring, Sidebar-nav's clipped labels, the _a11y_target.py role vocabulary)
> - `W-73` **wave-4 lane B - cascader + splitter + qr-code BUILT PROPOSED-NOT-RULED - Dave's eye owed (19 open questions)** — *closes when:* Dave has ruled the 19 questions in section 4 of the receipt - in particular Q-CS-1 (do Tree and Cascader both exist), Q-SP-1 (is aria-valuenow a percentage or pixels) and Q-QR-1 (what a QR code does in dark mode, where the theme-following variant was MEASURED not to decode) - and the conductor has actioned or parked the section 6 handoff, of which item 5 (the repo-wide ds-005 specificity class, six defeated overrides in five gated snippets) is the one that affects files outside this lane.
> - `W-74` **wave-4 heavy 7 BUILT PROPOSED-NOT-RULED (calendar tree cascader splitter qr-code carousel image-block) - Daves eye + ~47 named questions owed** — *closes when:* Dave rules per component promote/rework/delete across the seven, having seen the three lane receipts 2026-08-20-210-wave4-lane{A,B,C}-*.md and their decisionsForDave lists (19+19+9); the cross-lane ds-005 trim-specificity decision and the dark-mode QR polarity call are answered with or before this row
> - `W-85` **Probe registry P-7 (container self-query) + P-8 (dangling var, TEXT tier) — registered ADVISORY #210, live findings STANDING, nothing repaired** — *closes when:* Dave has observed each probe live and PROMOTED or PARKED it individually (s204-D1 twice-caught bar, derivation governance — promotion is his, never the tool's). ⚠ NOT closed by the probes existing or running: P-7 currently reports 6 findings + 3 WARN and P-8 reports 58 findings in 9 of 135 snippets, and this lane REPAIRED NOTHING. P-7's ledger carries 3 mined #210 catches + its own live drive but ONE distinct session, so it is NOT twice-caught; P-8 has two (184, 210), with the 184 occasion SHARED with P-3 and declared.
> - `W-87` **wave-1 lane R1 — gen_token_ramp comment-injection REPAIRED, P-8 58→12; 12 ABSENT tints + C2 gate blindness priced** — *closes when:* Dave rules the 12 ABSENT tint values (or parks them) and picks up/parks the priced C2-gate and declared_value gate extensions
> - `W-88` **wave-1 lane R3 — Date-picker ring 1.21→17.40, Drawer focus repaired, input descender clip repaired; 3 more clipping snippets + P-6 wrong-organ finding priced** — *closes when:* Dave looks at the three repairs, rules the P-6 ink-verdict gate extension in or out, and disposes of the three remaining clipping snippets
> - `W-89` **wave-1 lane R4 — P-3/P-6 crash→refuse rc=77 REPAIRED; error-solid.svg NOT repairable locally (FIGMA_TOKEN + upstream zero-fill node)** — *closes when:* Dave (or a session holding FIGMA_TOKEN) fixes the Figma error-solid node and re-exports, or Dave rules the icon dropped
> - `W-90` **wave-2 lane R2 — .l-split container self-query REPAIRED at cause, P-7 6+3WARN→0; showroom payloads + review page still carry old defect** — *closes when:* Dave rules P-7 promote/park (W-85), the REVIEW-204 7th instance is repaired at his sitting, and the 4 canon radius literals are ruled snap-or-waive
> - `W-91` **wave-3 lane R5 — the last three input descender clips REPAIRED (Multi-select/Tags-input/Combobox 3.25px→0)** — *closes when:* Dave's eye passes the three repairs (or the ds-005 class ruling supersedes the per-instance shape)
> - `W-92` **wave-3 lane R6 — declared_value/check_contracts/manifest_vars comment-blindness FIXED (R1's class); sync no-op proven; Image-block latent injection exposure priced** — *closes when:* Dave picks up or parks the three priced residuals (Image-block exposure, shared mask helper, CSS-parse upgrade)
> - `W-93` **PHOTOGRAPHY ASSETS ARRIVED (Dave #211): 251 Getty/EyeEm JPEGs, 2.5GB — mapping + KG + tagging owed; originals fenced NON-REPO via .gitignore** — *closes when:* manifest + KG mapping exist in-repo, a tagging approach is ruled, and Dave rules the originals' pipeline
> - `W-94` **wave-4 lane R7 — FIGURE_RE raw-span exposure CLOSED (it ate a real figure, not just latent); locate-live/slice-raw across 3 selectors** — *closes when:* Dave's eye passes it alongside the wave receipts sitting
> - `W-95` **#212 boot + desk triage strike-list PROPOSED (reviews/REVIEW-212-boot-and-desk-triage-v1.html) — connector/skill cuts + park-or-batch options on the 49 Dave rows, Dave rules by control** — *closes when:* Dave returns the page's ruling text (or strikes the page) and each chosen action is enacted or rowed separately
> - `W-96` **Unregistered-source gate (from W-16a): gate the PRESENCE of a canon/.js registration so an unregistered source cannot stay invisible** — *closes when:* the gate is built and driven on real data, or Dave parks it
> - `W-98` **dv-lockup build wave (from W-0d): scoped brief exists, awaits Dave's 3 placeholder titles + his word to open the wave** — *closes when:* Dave supplies the three titles and rules the wave open; closes when the wave lands or he drops it
> - `W-99c` **G15+G16 dedicated sitting: donut release-mode pair, Dave wants to get into it** — *closes when:* the sitting happens and Dave rules G15 sign-off and G16 ratify-or-reverse
> - `W-99d` **G9 riders: bill_of() 1.57x overstatement repair + re-PICK of SIZE_BUDGET compactable 8000 and TITLE_CAP_TAPE 120 (picked-never-measured)** — *closes when:* Dave authorises the bill_of repair and re-picks (or blesses) the two picked constants
> - `W-99e` **Routing currency check #212 — the dated desk-research brief behind s212-D10/s212-D12; P8 is the one amendment still unruled** — *closes when:* Dave rules P8 (one of the brief's three readings, or his own)
> - `W-99k` **#165 2c EXIT-CHECK carries (2): cross-instrument claim check (s163-D1 successor) + the s142-D1 value-level aesthetic leg** — *closes when:* Both `until:` tags quoted from the block's own text: ① Dave rules the successor instrument in or out; ② Dave views the remaining 113 s142-D1 rows or rules the viewing unnecessary.
> - `W-99l` **#164 post-wrap addendum: the designer-community deck lane (v3 awaits Dave's eye) + four FLOATED Dave figures** — *closes when:* The block's own `until:` tags: Dave sits with deck v3 or the 2026-08-13 call passes; AND Dave rules the four floated figures or they are explicitly dropped.
> - `W-99o` **#115's standing set: the mark-observation window (unruled) + the memento known-answer FAIL (pre-existing, unowned)** — *closes when:* The block's own `until:` tags: Dave rules demote on the `_graph_edges.py --tally` evidence; and the known-answer expectation is re-pinned off the REBUILT index or ruled.
> - `W-99q` **Dave's calls, parked live (8): C3 gate candidate · the 1.31:1 resting border · B2's accepted cost · the B-D4 re-dial caveat · ds-014(d) donut cluster · DV-D13 centre-figure · the stacked pair · the 07-23 calm-banner verdict** — *closes when:* The block's own frame and words: each call is Dave's to rule live or judge by eye ('rule it live', 'judge by eye, close it'); the row closes when every call in the set is ruled or explicitly dropped.
> - `W-99r` **Dave's calls copied up #28 (3): consumes-manifest TENTATIVE-to-firm · consult-receipts probe promotion · LS-trim-vs-defer (P4b)** — *closes when:* The block's own words: each is HIS word - the ADR-0015-A2 posture firmed either way, the consult-receipts probe promoted to blocking or left advisory, and the P4b payer question answered.
> - `W-99x` **#214 context-territory strategy brief: delegated-wrap gain measured-adjacent, advisory-line re-derivation option, two instruments, boot-reduction pair (s208-D1 rider satisfied)** — *closes when:* Dave rules on the brief's four-question decision surface (advisory re-derivation / lines hold / instruments / boot-reduction pair), each answerable independently
> - `W-99y` **#214 conditional-band proposal (200-256K): re-source check DONE, band conditioned on work-type + declaration + live recall probe; amends Dave's #58b shape** — *closes when:* Dave rules on the three-question decision surface (adopt band / 256K stays wall / build recall probe), each answerable independently
> - `W-99zb` **#214 itinerary generator Layer-2 fix: shortcut deleted, mechanical row->snippet ladder, premise note GENERATED, selftest derived-fixture repair; v3 snapshot: orphans 28->1, GATED 94->121** — *closes when:* Dave's eye on two calls the fix surfaced: whether row 124 (per-component variant matrices, honest GAP) joins the true-gap list, and the 27 Layer-2 snippets now visible as off the radius-ratchet (advisory 26->53)
> - `W-99zc` **#214 review-213 TRANCHE MAP: 6 sittings ~20-35 questions each; statement family confirmed (5 members incl. Template-list-index); lock-ups NOT a family (14.8%)** — *closes when:* Dave adopts, amends, or discards the tranche plan at his first sitting
> - `W-99zd` **#214 taxonomy research: consensus is Foundations/Tokens/Components/Patterns, NOT atomic-as-navigation; 'lock-up' is a coinage (industry: block); aliases+facets+search beat taxonomy for findability** — *closes when:* Dave picks the library's level vocabulary (3 candidates laid out, trade-offs even) - can ride a tranche sitting
> - `W-99ze` **#214 descender audit: ds-005 fix NOT regressed, components fully TOKENISED; failure = specificity (18 dead overrides, 9 snippets, 6 review components); gate reads declarations not computed edges** — *closes when:* Dave rules on G1 (specificity leg for _validate_descender_clip.py, ~90min/~18K, recommended) + the 18-selector repair (~60min/~12K, needs his nod because Sidebar-nav is ds-005-GATED against 'fixing')
> - `W-99zf` **#214 descender gate leg BUILT + 18 selectors REPAIRED (Dave's nod, incl. ds-005-gated Sidebar-nav); full gate GREEN 151 files; NEW: canon.css has 48 of its own via non-specificity-preserving prefixer** — *closes when:* Dave rules the canon.css 48-selector class (the .cn- absorb prefixer inflates trim (0,1,2)->(0,3,2) vs override +1 class - a snippet-winning repair LOSES in canon; held as shrink-only ratchet 48, canon.css:9316 already says cross-file remedy is his)
> - `W-99zg` **#214 LIBRARY v2 BUILT: true header (theme/dark/width), overlay off component pages (#chrome=0 opt-out), search+68 aliases+cmd-K, level facet chips (config swap point, word-set UNPICKED), 135 components, driven headless 24/25** — *closes when:* Dave's eye on the page + two calls: does v2 REPLACE showroom/index.html, and which level word-set fills the LEVELS config (three candidates commented beside it)
>
> **MINE (52), ids only — bodies are in the store, not here:** `W-0b` · `W-01` · `W-02` · `W-03` · `W-04` · `W-05` · `W-06` · `W-07` · `W-08` · `W-09` · `W-10` · `W-11` · `W-12` · `W-13` · `W-17` · `W-35` · `W-42` · `W-43` · `W-44` · `W-45` · `W-46` · `W-47` · `W-48` · `W-49` · `W-50` · `W-52` · `W-54` · `W-55` · `W-56` · `W-57` · `W-59` · `W-62` · `W-86` · `W-97` · `W-99` · `W-99a` · `W-99f` · `W-99h` · `W-99i` · `W-99j` · `W-99m` · `W-99n` · `W-99p` · `W-99s` · `W-99t` · `W-99u` · `W-99w` · `W-99z` · `W-107` · `G4` · `W-99za` · `W-99zh`
>
> ⚠ store gate: **PASS**. Bodies, conditions and provenance: `python3 knowledge/_state.py`.


---

*(Chain ends. **25,607 real — the unit is THE WHOLE FILE**, this generated wrapper included:
the number in this sentence is the size of the file containing it, held exact by a fixed point.
`GOOD-MORNING.md` is 72,466 real and stays whole for retrieval; you have paid for
35% of it, and that is the intended figure, not a shortfall.)*
