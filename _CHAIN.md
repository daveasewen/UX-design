<!-- GENERATED by knowledge/_gen_chain.py — DO NOT HAND-EDIT.
     Edit GOOD-MORNING.md / _LIVE-STATE.md and regenerate; `--check` is a build step. -->

# The read chain — everything a cold session must read, and nothing else

> **This file is GENERATED and it is the whole contract.** It carries `GOOD-MORNING.md`'s header
> and ★ LATEST banner, plus `_LIVE-STATE.md`'s ⏱ LATEST delta — the three things GM-D7-am names
> (CUT #33 on Dave's ruling). **If you have read this file, you have read the chain.**
>
> ⚠ **Do NOT now open `GOOD-MORNING.md` to "check".** It is 53,085 real and 84% of it is
> retrieval surface you are not meant to pay for at boot. That reflex is the thing this file
> exists to stop — five sessions called the chain CUT and every one of them paid full price.
>
> ★ **Everything else is RETRIEVAL, never a reading list:**
> `python3 knowledge/_memento_search.py "<q>"` → `--fetch <id>`. §A orientation is `gm:A`
> (a router with 11 children); the queue is `gm:C1`…; the archives, briefs, runbooks and ledgers
> are all indexed. **Ask for what you need; do not read a file to find out whether you need it.**

---

> **YOU ARE #146. TITLE THIS CHAT →** `Apollo - #146: build the binds-resolve gate [0]`
> *(read it back in your opener — the chat half of the ritual is ungateable, which is why this line is FIRST)*

---

<!-- ⛔ STOP. If you are a cold session, you are in the wrong file and you pay for ALL of it.
     Read `_CHAIN.md` instead — it is GENERATED from this file's header + ★ LATEST banner and
     `_LIVE-STATE.md`'s ⏱ LATEST delta, which is the ENTIRE contract (GM-D7-am, CUT #33).
     This file stays whole for RETRIEVAL: `_memento_search.py "<q>"` → `--fetch <id>`.
     ⚠ Reading on from here is not thoroughness — it is the exact overspend #41 measured. -->

# Good morning, Dave ☕

> **size:** GM **53.1K tk real** (53120 exact) · §A **7.0K tk real (EXEMPT)** (6,957 exact) · LS **69.9K tk real** (69870 exact) · measured 2026-08-10 #145 (`_gm_usage.py --sizes` → `real`, DECLARE-LAST — after 2c/2d/2f, before the 2g index rebuild), **on the artefact, AFTER the rolls, iterated to a FIXED POINT** [[chain-is-a-swap-not-an-add]]. ⚠ **THE READ-CHAIN FIGURE IS DELIBERATELY ABSENT — its ONE home is `_CHAIN.md`'s own generated footer** (retired #45). **Point at it; never restate it.** ✅ **THE UNIT IS REAL CLAUDE TOKENS (#82-D1, Dave's, ruled #54) — MEASURED, NEVER CONVERTED; ratios REPORTED, NEVER APPLIED.** *(This line's correction-provenance — #90's self-reproduction, the `CHAIN_STAMP_RE` scope hole closed #94, the #90 2f-roll discharge — HOMED BY ADDITION at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET, then cut here [[home-by-addition-then-cut]].)*
> **STATE: ★★ THE READ CHAIN IS CUT (#33) and it HELD — EIGHTH session running.** You are reading the contract. §A and §C are STILL IN THIS FILE, reached by `_memento_search.py`, not by reading top-to-bottom. ⛔ **BUILD VERDICT: 75 of 106 steps green (#62, `18c7789`) — 31 steps have NEVER been in a green verdict.** Both counts GENERATED from `_build_all.py`'s AST at each end; the shortfall is computed (`s125-D1`). A single-process `_build_all.py` run is SANDBOX-IMPOSSIBLE (~49s vs the ~45s call kill); CI delivers the single-process verdict on push. ⚠ **DO NOT RUN `_build_all.py` TO CHECK — ANY PARTIAL run strands the tree in the documented mid-build intermediate** (step 1 rewrites wholesale; healed only by a COMPLETE pass — docstring lines 5–21). ★ **Only the PREMISE of a derived rule is worth re-verifying** (#47's lesson, turned on #47). *(The #47/#61/#62 build-claim archaeology + the #38 cap-repoint finding are HOMED at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET and `notes/_briefs/2026-07-29-cap-repoint-and-lane-divvy-brief.md`.)*
> ⚠ **`set_content()` IS BANNED IN RENDER-PROOFS** — drops `type.css` silently (14px→16px) and `document.fonts.check` passes anyway; use `goto("file://…")` (#29, measured) · **ds-020 is a gate-enforced obligation** (the DV-D02-A scatter waiver names it as clears-when) · **ritual step 2g: rebuild `_memento-index.json` LAST** — `index_freshness_check` is BLOCKING and compares CONTENT, never mtime (runbook has the body).
> **★ PRICE IN REAL TOKENS — THE UNIT IS `_gauge_tokens.py`, NOT A PERCENTAGE (#56, Dave's).** **amber 160,000 (PICKED) · working 200,000 (SOURCED) · hard 256,000 (SOURCED)** — provenance per `_gauge_tokens.py:63–83`, the authority [[read-chain-is-where-staleness-is-free]]. Unit = the FULL price (boot + job + wrap); ⚠ **a DECLARED gap passes, a SILENT one fails** — that asymmetry is the mechanism. The retired 45/60/63 % band is DORMANT in code, pinned — retire-or-pin = `G8`, Dave's. ⚠ **THE FLOOR IS MEASURED = `ds-025` RE-SCOPED #109 — first turn 65,400±1,400 real (n=5) + `_CHAIN.md` 10,499 additive = 75,899 floor** (was published 30,499 — under by 45,400; the OLD `disk 11,032 + harness unreachable` framing on this line was FALSE, retired #109). `MEMORY.md` 8,470 of the first-turn figure is split and measured; **56,308 remains unattributed — that is what `ds-025` item 1 now means.** ⛔★★★ **FIGURE SUPERSEDED #129 (`s129-D1`, Dave's): FLOOR RE-BASED 81,335 → 70,794** (`BOOT_FIRSTTURN_TK` 65,400 → 54,859 ±1,178, n=7). **By ADDITION: the `ds-025` question above is UNCHANGED; only the constant moved.** Authority = `_gauge_tokens.py`, never this line. ⚠ **`pip install tiktoken --break-system-packages` FIRST in a cold sandbox** — the gauge REFUSES, loud and named (`MeasurementRefused`); `_build_all.py` P5 = ABORT without it (#80). ★ **READ THE CHAIN ONLY.** ★ **Repo-state claims are verified against `git log` / a real run, never a banner.** *(The #58/#59/#80/#83 correction-provenance is HOMED at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET; the #109 boot-floor correction at `_DECISION-HISTORY/2026-08-06-109-boot-floor-measured.md` and § ★ #109.)*
> ⚠ **Render sandbox is FRESH every session** (~4 calls — price it) · **retrieval FIRST:** `_memento_search.py "<q>"` → `--fetch <id>` (⚠ **it served a two-session-old record until #32 — if an answer looks stale, check the index**) · **wrap moves RUN THROUGH `_gm_move.py`** (⚠ **banner headings are BLOCKQUOTED — anchor `> ## ★ PRIOR …`**) · **PACE PANEL: ask at the opener, no gate can see it** — Dave has declined a fresh one twice and put Fable on the table for judgment work; ✅ **THE "BEHIND PACE ⇒ MORE WINDOWS" CLAUSE IS RETIRED — RULED #60: multi-window is settled, two use cases (quota catch-up AND production crank), Claude offers/Dave vetoes, cap 3 revisable. `notes/_MEMENTO-DECISIONS.md` § ★ #60-D1.** It stood from #48; at #57 he said the opposite — *"we are behind, but I'm less comfortable with multiple windows, too much to decide and consume"* ⇒ **ONE window with more delegation.** ★ **The clause optimised the wrong quantity: it treated TOKENS as the constraint when DAVE'S DECISION LOAD is what binds.** ⚠ **Reflected back #57, and #58's opener did NOT settle it** (asked as the first question; he answered pace with *"no constraint, lets just fix the problems"*). Standing home + status: §C·4.
> *(**RENAME is delivered IN CHAT at wrap, not here — RULED #28, ENACTED in runbook step 4b by #30.**)*
> **TITLE THE NEXT CHAT →** `Apollo - #146: build the binds-resolve gate [0]`
> ✅ *(the #86 hand-patch that stood here is REMOVED by the #87 wrap, per its own text — #85/#86 are recorded as HOLE lines + ledger stubs, `notes/_GAUGE-LOG.md` / § ★ #85–#86.)*
> *(Titles are LABELS — role comes from Dave's opener line. Bands: the ONLY copy is
> `_RUNBOOK-context-gauge.md` § ★ THE FLOOR IS MEASURED — grep it, never recall it.)*
> ## ★ LATEST — 2026-08-10 (Mon **#145**, OPUS conductor, Dave live, ONE ARC + one dissolved lane — mint the rung, residual ① of #144 — ✅ **`s145-D1` RULED AND ENACTED IN-WINDOW: THE RUNG IS `rag/<severity>-ink`, DAVE'S** · ★ **FIRST MODE-FORKED HUE RUNG IN THE RAG FAMILY, FORCED NOT CHOSEN** · ⬛★★ **LANE ② DISSOLVED — ITS PREMISE WAS STALE, AND THE GAP UNDERNEATH IS WORSE: NO GATE RESOLVES A `binds` ADDRESS AGAINST THE COLOUR SPINE** · ⚠ **THIS WRAP FOUND A LIVE REGRESSION: `canon.css`'S FONT-FAMILY FALLBACK DROPPED BY THIS SESSION'S OWN REGEN** · ⚠ **QUOTA NEVER POLLED, SECOND SESSION RUNNING** · ✅ **READ-CHAIN HELD, TENTH SESSION**)
> 
> - ✅ **① `s145-D1` RULED AND ENACTED IN THE SAME WINDOW, DAVE'S.** The plus/minus rung `s144-D1` left unnamed is **`rag/<severity>-ink`** — minted **`rag/error-ink`** (light `#DA1A00` / dark `#F6604C`) and **`rag/success-ink`** (light `#137F3C` / dark `#66CC8D`) in `knowledge/tokens/semantic-colour.json`; `s144-D1`'s four VALUES unchanged. Dave picked `-ink` from a measured three-option set, costs stated per option. **Why not `-text`:** `rag/text/*` already exists and is GROUND-keyed (`on-light`/`on-dark`/`on-information`, minted `s131-D1`); this rung is MODE-keyed with hue as the meaning — same word, two different axes, one level apart in the same family. **Why it stays under `rag/`:** `s144-D1` locks the hue to the `s122-D2` fills, so the derivation stays visible in the address. **On "ink":** measured before the options were put — `ink` appeared in `tokens/` only as the CSS custom property `--ink` bound to `text/default` (`tokens/_manifests/sutherland-fixtures.json`), never as a path segment in the semantic spine (cf. `s134-D4` "ink on the SHAPE") — new to the spine, not new to the system's vocabulary.
> - ★ **② FIRST MODE-FORKED HUE RUNG IN THE RAG FAMILY, AND THE FORK IS FORCED.** All 12 prior rag hue rungs (`-background`, `-glyph`, the bare roles) carry ONE hex for both modes; only `-tint` forked before this. The two ink rungs fork because `s144-D1`'s own arithmetic makes a mode-invariant AA-text value impossible (no hex clears AA on both `#FFFFFF` and `#1A1A1A`) — the asymmetry in the file is a receipt of that finding, not an inconsistency to tidy. Only error and success get a rung; warning and information are OUT by Dave's word at #144 ("the only coloured text is for plus and minus").
> - ✅ **③ ENACTMENT RECEIPTS, READ OFF THE GENERATED FILES, NOT QUOTED.** `canon.css` lines **304/309** (light) and **659/664** (dark) carry the two new vars — verified by grep on the file after regen. `semantic-colour.json` written by TEXTUAL insertion, rest of file asserted parse-equal, exactly 2 new keys (rag leaves 19→21). Regen RE-DRIVEN by this wrap: `gen_canon_tokens.py --check` → **543 root vars (+2), 189 dark overrides (+2)**, byte-identical on a second run · `gen_theme_cascade.py --check` → **201 override paths, 206 projections, in sync, no change** · `gen_canon_components.py --check` → **75 components, in sync**. `knowledge/_rulings.json` now **105 rulings**, `s145-D1` last; **103 of 104 priors byte-identical**, `s144-D1` amended BY ADDITION only (its "RUNG NAME UNRULED" open line now reads CLOSED, original text kept beneath; its `status` gained an "AMENDED BY ADDITION AT #145" clause). Gates RE-DRIVEN, all rc=0: `_validate_dtcg.py` PASS 0 failures/61 deferrals · `_validate_binds_ratchet.py` PASS 33/75 · `_validate_standing_instructions.py` PASS 28 docs · `_validate_a11y.py` 0 failures/179 warnings/446 controls+201 marks.
> - ⚠ **④ HONEST GAP ON ①: NOTHING CONSUMES THE INK RUNGS YET, SO NO GATE CAN SEE WHETHER THEY PASS AA.** `amount-display.sign`'s enum is `["none","negative"]` — no `positive` value (#143 finding ⑤, never closed) — so `rag/success-ink` has no binding site. Their AA receipts come from Dave's #144 tuner, not from any re-checker: **PROVEN-by-tuner, UNPROVEN-by-gate.** Recorded in `s145-D1`'s own `open` array.
> - ⬛★★ **⑤ LANE ② DISSOLVED — ITS PREMISE WAS STALE, AND THE GAP UNDERNEATH IT IS WORSE.** Parked at #143 AND #144 as "colour-spine DTCG migration, 7 address-intent rows." The conductor probed the premise BEFORE briefing a sub for it: all 7 addresses already RESOLVE in `semantic-colour.json` to `$value`/`$type` leaves (`rag.error` `rag.warning` `rag.success` `rag.information`, all four with `$alias`; `background.default` / `surface.raised` / `surface.subtle` / `surface.action`). `_validate_dtcg.py` reports **0 FAILURES** on the spine — its 8 `semantic-colour.json` deferrals are ALL `DEF-COLOR-MISTYPE` on blur/image OPACITY tokens, **NONE on rag or surface**. The migration those 7 rows waited on already landed at **#141 (`s141-D1`, axis A)** — the lane as written was a job already done, parked twice. ⛔ **THE REAL GAP: NO GATE RESOLVES A META `binds` ADDRESS AGAINST THE COLOUR SPINE.** Four gates mention `binds` (`_validate_binds_ratchet.py`, `_validate_partials.py`, `_validate_radius.py`, `_validate_type_blast_radius.py`) and **none of them opens `semantic-colour.json`** — the 7 addresses resolve by hand-probe, not by instrument. Rename a rung and 7 metas point at nothing while every gate stays green — the `no-gate-parses-the-artefact` class recurring, `instrument-without-a-consumer`'s sibling. **Correction applied BY ADDITION** to all 7 `$status` strings (original kept, rest of each file asserted parse-equal): `alert.meta.json::status` · `amount-display.meta.json::sign` · `banner.meta.json::status` · `toast.meta.json::status` · `avatar.meta.json::surface` · `button.meta.json::surface` · `divider.meta.json::surface`.
> - ⚠ **⑥ THIS WRAP'S OWN FINDING, MADE WHILE VERIFYING ③'S RECEIPTS: `canon.css`'S FONT-FAMILY FALLBACK WAS SILENTLY DROPPED BY THIS SESSION'S REGEN.** `--typography-font-family-default` now reads the bare `Univers Next for HSBC` in both theme blocks, where HEAD carried the full web-safe stack `"Univers Next for HSBC", "Helvetica Neue", Arial, Helvetica, sans-serif`. Cause, read in the generator: `knowledge/canon/gen_canon_tokens.py`'s `FONT_STACK` substitution fires only `if ttype == "string"`, but `typography.json`'s font-family token carries the DTCG type **`"fontFamily"`** — the condition has never matched this token, and the `$webStack` field built for exactly this (`knowledge/guidelines/typography-standards-2026.md`) is never read by the generator at all. `canon.css` had not been regenerated since **#132** (`git log`), so the mismatch sat latent until this session's regen exercised the path for the first time. Confirmed reproducible: re-running `gen_canon_tokens.py --check` a second time reproduced the identical bare value. Not this session's intent, and **not reverted by this wrap** — fixing a generator's substitution logic is a design decision outside a wrap sub's remit. Declared, not smoothed; named in the residual.
> - ⚙ **⑦ GAUGE.** boot **55,068** real — inside the 54,859±1,178 band, a datapoint, never corrected into the constant. Check-ins ran INSIDE the lane, not at its ends: **64,809 → 82,124 → 107,460 → 121,942** (FILL). Stop line 150,929; wrap opened at 121,942 — UNDER. Binding budget FILL throughout. ⚠ Conductor's own avoidable spend, same class as #144's: a grep for `address-intent` caught `knowledge/_memento-index.json` and dumped a full prior-session banner, ~17K in one call — the remedy both times is excluding the index from greps.
> - ⛔ **⑧ QUOTA NEVER POLLED — SECOND SESSION RUNNING.** Asked once at the opener, offered again as an explicit option mid-session; Dave answered the routing question instead of the poll. Routing rested on #143's figures (session 19% · weekly 41% · Fable 53%). Dave DID approve sub spend for lane ②, but the lane dissolved before a sub was spawned — this wrap sub is the session's only delegation.
> - ✅⚠ **⑨ READ-CHAIN HELD, TENTH SESSION** (`_CHAIN.md` only, `GOOD-MORNING.md` never opened at boot). **ENOSPC n=11** — `PYTHONPATH=/var/tmp/py-s142` worked again, zero install. Three runbooks still instruct install-first, unfixed, third session running.
> 
> **residual → #146:** *(DRAFT — the #146 conductor reviews it; this wrap ruled nothing, closed nothing.)* ⬛ **① BUILD THE BINDS-RESOLVE GATE [0]** — parse every meta's `binds` address and resolve it against `knowledge/tokens/semantic-colour.json`; fail loud and named on a miss. This is what lane ② actually was. Must be mutation-tested (rename a rung, watch it go red) and verified to RUN in the conductor's mode, not just exist. · ⬛ **② THE `success-ink` BINDING SITE [0]** — Dave's: add a `positive` value to `amount-display.sign`'s enum, or leave the rung ahead of its consumer. · ⬛ **③ NO DARK SELECTED-ROW TOKEN [0]** — `#272727` unclaimed approximation, carried unchanged. · ⬛ **④ UNVERIFIED: `#1A1A1A` ON DARK RAG TINTS [0]** — carried unchanged; check the SUBJECT before treating it as a finding. · ⬛ **⑤ `tooltip.tip` [0]** + intent/capability vocabularies **[2]**. · ⬛ **⑥ MSGFILE GATE [3]** — class ×4, unbuilt. · ⬛ **⑦ RENDER-PROOF DEBT ×2 [2]**. · ⬛ **⑧ #139 QUEUE [3]** — ds-012(b) `0.42`, narrow columns, 42 fork verdicts, 15 token-split exceptions. · ⬛ **⑨ MEMORY-INDEX REMAINDER [2]** — plus the "exclude `_memento-index.json` from greps" lesson, now measured twice (~27K at #144, ~17K at #145). · ⬛ **⑩ STEP-0 CONFIRM-RETIRE [1]**. · ⬛ **⑪ ENOSPC RUNBOOK CORRECTION [2]** — proven three times, still unmade. · ⬛ **⑫ META REFLOW COSMETIC [3]**. · ⬛ **⑬ THE 8 `DEF-COLOR-MISTYPE` OPACITY DEFERRALS [0]** — NEW, found this session: blur/image opacity tokens typed `color` holding numbers. Not urgent, now named. · ⬛ **⑭ CANON.CSS FONT-FAMILY FALLBACK REGRESSION [0]** — NEW, found by the wrap sub verifying receipts: `--typography-font-family-default` lost its web-safe stack in this session's regen; `gen_canon_tokens.py`'s `ttype=="string"` check never matches the token's actual `$type:"fontFamily"`, and `$webStack` is never read. Fix is a generator-code decision, Dave's/the conductor's.
> **residual (GENERATED #145):** 2c OK (banners 2/2) · 2d OK (deltas 3/3) · 2f OK (strata 1, log #144) — _roll_state.py · 2026-08-10


> **⬛ OPEN WORKLIST — PRESENCE INDEX (28 items, GENERATED). Every open item is named; NO bodies are here — `--fetch gm:DOFIRST`.**
> `0b` ENCODE BEFORE THE WAVE, part-consumed 28 · `0c` NEXT BUILD CANDIDATES · `0d` THE 67 ENACT WAVE, landed · `1` ds-018 C2 follow-through · `2` dv-legend/dv-behaviour CEILING · `3` ds-012(b) gutter-relative plot area · `4` DV-D16 floating growth · `5` Instrument-fit remainder · `6` ds-016, PART-RULED #114 · `7` ds-017, UNRULED · `8` STILL OWED, none superseded · `9` DELEGATION TOPOLOGY · `10` ✅ PER-GATE TEST PLAN · `11` 2c-ROLL / INDEX-VOCAB DEADLOCK · `12` #57 1b DOSSIER · `13` tmp RUNBOOK EXPOSURE, unfixed · `14` ⬛ DAVE'S FOUNDING PRINCIPLE · `15` LEDGER § ★ #59 · `16` UNHOMED PAIR (#78 2c EXIT CHECK) · `17` CHAIN-DIET CUT · `19` type.css 180 dark-mode specificity · `20` ds-029 FIRST IDIOM · `21` PER-THEME COLLISION SWEEP · `22` 89-D2 ruled not enacted, absent from state · `23` ds-025 re-scoped 109, not closed · `24` 110 boot-rent plan · `25` ds-032 enactment, ruled 106 · `26` ✅ boot-drift gate rulings
> **QUEUE — `gm:C1` strands · `gm:C2` ruling batch (Dave's) · `gm:C4` enact-queue.**

---

## ⏱ LATEST DELTA — 2026-08-10 (Mon **#145**, OPUS conductor, Dave live, ONE ARC + one dissolved lane — mint the rung, residual ① of #144 — ✅ **`s145-D1` RULED AND ENACTED IN-WINDOW: THE RUNG IS `rag/<severity>-ink`** · ★ **FIRST MODE-FORKED HUE RUNG IN THE RAG FAMILY, FORCED NOT CHOSEN** · ⬛★★ **LANE ② DISSOLVED — NO GATE RESOLVES A `binds` ADDRESS AGAINST THE COLOUR SPINE** · ⚠ **FONT-FAMILY FALLBACK REGRESSION FOUND BY THIS WRAP** · ⚠ **QUOTA NEVER POLLED ×2** · ✅ **READ-CHAIN HELD, TENTH SESSION**)

**Landed (evidence per claim):**

- ✅ **`s145-D1` RULED AND ENACTED IN THE SAME WINDOW, DAVE'S.** The plus/minus rung `s144-D1` left unnamed is **`rag/<severity>-ink`** — minted `rag/error-ink` (light `#DA1A00` / dark `#F6604C`) and `rag/success-ink` (light `#137F3C` / dark `#66CC8D`) in `knowledge/tokens/semantic-colour.json`; `s144-D1`'s four values unchanged. Dave picked `-ink` from a measured three-option set. `-text` refused because `rag/text/*` is GROUND-keyed (`s131-D1`) while this rung is MODE-keyed with hue as the meaning. Stays under `rag/` because `s144-D1` locks the hue to the `s122-D2` fills. `ink` was already the system's word on the consumption side (`--ink` → `text/default`, sutherland-fixtures.json) but new as a spine path segment.
- ★ **FIRST MODE-FORKED HUE RUNG IN THE RAG FAMILY, FORCED NOT CHOSEN.** All 12 prior rag hue rungs carry one hex both modes; only `-tint` forked before. The two ink rungs fork because `s144-D1`'s arithmetic makes a mode-invariant AA-text value impossible — the file's asymmetry is a receipt of that finding. Only error/success get a rung; warning/information OUT by Dave's word at #144.
- ✅ **ENACTMENT RECEIPTS READ OFF THE GENERATED FILES.** `canon.css` lines 304/309 (light), 659/664 (dark). `semantic-colour.json` by textual insertion, rest parse-equal, 2 new keys (rag leaves 19→21). Regen re-driven: `gen_canon_tokens.py --check` → 543 root vars (+2), 189 dark overrides (+2), byte-identical on re-run · `gen_theme_cascade.py --check` → 201 paths/206 projections, no change · `gen_canon_components.py --check` → 75 components, in sync. `knowledge/_rulings.json` now 105 rulings, `s145-D1` last, 103/104 priors byte-identical, `s144-D1` amended BY ADDITION only. Gates rc=0: `_validate_dtcg.py` PASS 0/61 · `_validate_binds_ratchet.py` PASS 33/75 · `_validate_standing_instructions.py` PASS 28 · `_validate_a11y.py` 0 failures/179 warnings/446 controls+201 marks.
- ⚠ **HONEST GAP: NOTHING CONSUMES THE INK RUNGS YET.** `amount-display.sign`'s enum is `["none","negative"]` — no `positive` value (#143 finding ⑤, never closed) — so `rag/success-ink` has no binding site. AA receipts come from Dave's #144 tuner, not a gate: PROVEN-by-tuner, UNPROVEN-by-gate. Recorded in `s145-D1`'s own `open` array.
- ⬛★★ **LANE ② DISSOLVED — ITS PREMISE WAS STALE, AND THE GAP UNDERNEATH IS WORSE.** Parked #143 and #144 as "colour-spine DTCG migration, 7 rows." Probed before briefing a sub: all 7 addresses already resolve in `semantic-colour.json` to `$value`/`$type` leaves; `_validate_dtcg.py` reports 0 failures on the spine, its 8 deferrals are all `DEF-COLOR-MISTYPE` on blur/image opacity tokens, none on rag/surface. The migration landed at #141 (`s141-D1`, axis A) already — parked twice for a job already done. ⛔ **THE REAL GAP: no gate resolves a meta `binds` address against the colour spine.** Four gates mention `binds`; none opens `semantic-colour.json`. Rename a rung and 7 metas point at nothing while every gate stays green — `no-gate-parses-the-artefact` recurring. Correction applied BY ADDITION to all 7 `$status` strings: `alert.meta.json::status` · `amount-display.meta.json::sign` · `banner.meta.json::status` · `toast.meta.json::status` · `avatar.meta.json::surface` · `button.meta.json::surface` · `divider.meta.json::surface`.
- ⚠ **THIS WRAP'S OWN FINDING: `canon.css`'S FONT-FAMILY FALLBACK WAS SILENTLY DROPPED BY THIS SESSION'S REGEN.** `--typography-font-family-default` now reads bare `Univers Next for HSBC`, both themes, where HEAD carried the full stack `"Univers Next for HSBC", "Helvetica Neue", Arial, Helvetica, sans-serif`. Cause: `gen_canon_tokens.py`'s `FONT_STACK` substitution fires only `if ttype == "string"`, but the token's DTCG type is `"fontFamily"` — never matches; `$webStack` is never read by the generator. `canon.css` had not regenerated since #132, so this sat latent until this session exercised the path. Reproduced on a second `--check` run. Not reverted by this wrap — a generator-code decision outside the remit; named in the residual.
- ⚙ **GAUGE.** boot **55,068** real — inside the 54,859±1,178 band, never corrected into the constant. Check-ins ran INSIDE the lane: 64,809 → 82,124 → 107,460 → 121,942 (FILL). Stop line 150,929; wrap opened at 121,942 — UNDER. Binding budget FILL throughout. One avoidable ~17K spend: a grep for `address-intent` caught `_memento-index.json` and dumped a prior-session banner — same class as #144's ~27K.
- ⛔ **QUOTA NEVER POLLED — SECOND SESSION RUNNING.** Asked at the opener, offered again mid-session; Dave answered the routing question instead. Routing rested on #143's figures (session 19% · weekly 41% · Fable 53%). Sub spend was approved for lane ②, but the lane dissolved before a sub was spawned — this wrap sub is the session's only delegation.
- ✅⚠ **READ-CHAIN HELD, TENTH SESSION** (`_CHAIN.md` only). **ENOSPC n=11** confirms `PYTHONPATH=/var/tmp/py-s142` a third time, zero install; three runbooks still instruct install-first.
- ⛔ **THIS WRAP RULED NOTHING BEYOND WHAT `s145-D1` ITSELF RECORDS.** The `success-ink` binding site, the dark selected-row token, the `#1A1A1A`-on-dark-rag-tints question, the binds-resolve gate's design, `tooltip.tip`, the vocabularies, the 42 fork verdicts, the 15 token-split exceptions, the font-family generator fix — all Dave's/the record's, all untouched.

Dossier: `_DECISION-HISTORY/2026-08-10-145-the-rung-is-named-and-the-lane-it-unblocked-was-already-done.md`.


---

## ⬛ OPEN WORK — GENERATED from `_state.json`. **The row count IS the count.**

> **36 items · 36 live · 22 Dave's · 14 mine · 17 carry a stated close condition · **19 UNCONDITIONED**.**
> *Every figure in this block is computed from the store at generation time. No number here was typed by anyone — that is the point of it (#86 measured a typed inventory of "118 markers" against a real ~40; #85's "95 slots / 84 distinct" is not reproducible by any probe in this repo).*
> ⚠ **DECLARED DEBT — 19 inherited items have NO close condition and therefore cannot close.** They are exempt as a FROZEN set (`_state.LEGACY_IDS`, size 19) which may only shrink; a NEW item is refused without one. **Each needs Dave's word — an agent inventing a close condition for his open work is the same overreach as inventing his ruling.**
>
> **DAVE'S (22) — 17 with a ratified `closes_when`, 5 with NONE:**
> - `G1` **Worklist-index cap DOFIRST_INDEX_TK_MAX = 700 (_capture_gate.py:1403,…** — *closes when:* Dave ratifies 700 or names his own number
> - `G10` **The "70%/95%" stray band (GM:36** — *closes when:* Dave rules provenance or strikes it
> - `G11` **DS-018 recessive value** — *closes when:* Dave picks the recessive value
> - `G12` **Charter §4b tone-of-voice temperature map, PROVISIONAL 2026-07-02…** — *closes when:* tov-016 review lands, or Dave ratifies the mapping as-is
> - `G13b` **menu-search combined glyph, PROVISIONAL 2026-07-16 (_ICON-GAPS.md; the…** — *closes when:* Dave approves the glyph
> - `G14` **Icon-button dark bindings** — *closes when:* Dave rules the SC-dark bindings
> - `G15` **DV-D13 donut centre figure + st.visible[id]=true release wiring…** — *closes when:* Dave's sign-off eye
> - `G16` **The _proforma/_DATAVIZ-DECISIONS.md:567 enactment call (agent's, not…** — *closes when:* Dave ratifies or reverses
> - `G17` **RAG status manifestation…** — *closes when:* Dave's canon pick — A / A+B / A+B+C
> - `G2` **TAPE_TO_BILL = 1.57 at n=2 (_capture_gate.py:371; RATIO_FIRM_N = 4 per…** — *closes when:* 4 measured pairs logged, then Dave rules firm-or-retire
> - `G3` **retired_unit_prose_audit tier = WARN (_capture_gate.py:2078, 2098,…** — *closes when:* Dave rules warn vs block
> - `G4` **GM §C measured 191 > 150 warn cap (_capture_gate.py:2864,…** — *closes when:* Dave picks OFFLOAD / TRIM / KEEP
> - `G5` **Four advisory size caps as a set (_capture_gate.py:4843–4858** — *closes when:* Re-measured in real (G9 first), then Dave ratifies the set in one pass
> - `G6` **DEFER_STREAK = 6 (_gm_usage.py:353) + USAGE_HISTORY_BLOCKING = False** — *closes when:* Dave rules the streak number + per-candidate OFFLOAD/TRIM/KEEP calls
> - `G7` **Which end of an archive is "newest" (strata oldest-first · archives…** — *closes when:* Dave names the convention — one sentence
> - `G8` **Dormant % band BAND_FLOOR/HARD_STOP/MARKED_MAX = 45/60/63…** — *closes when:* Dave says retire or pin
> - `G9` **ds-023 re-measurement programme** — *closes when:* Dave rules the programme
> - `W-0c` **NEXT BUILD CANDIDATES** — *closes when:* ⛔ **none stated — cannot close until you name one**
> - `W-0d` **✅ THE #67 ENACT WAVE** — *closes when:* ⛔ **none stated — cannot close until you name one**
> - `W-14` **⬛ DAVE'S FOUNDING PRINCIPLE** — *closes when:* ⛔ **none stated — cannot close until you name one**
> - `W-15` **LEDGER § ★ #59** — *closes when:* ⛔ **none stated — cannot close until you name one**
> - `W-16` **UNHOMED PAIR, copied up at the #78 2c EXIT CHECK** — *closes when:* ⛔ **none stated — cannot close until you name one**
>
> **MINE (14), ids only — bodies are in the store, not here:** `W-0b` · `W-01` · `W-02` · `W-03` · `W-04` · `W-05` · `W-06` · `W-07` · `W-08` · `W-09` · `W-10` · `W-11` · `W-12` · `W-13`
>
> ⚠ store gate: **PASS**. Bodies, conditions and provenance: `python3 knowledge/_state.py`.


---

*(Chain ends. **13,936 real — the unit is THE WHOLE FILE**, this generated wrapper included:
the number in this sentence is the size of the file containing it, held exact by a fixed point.
`GOOD-MORNING.md` is 53,085 real and stays whole for retrieval; you have paid for
26% of it, and that is the intended figure, not a shortfall.)*
