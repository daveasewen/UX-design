<!-- GENERATED by knowledge/_gen_chain.py — DO NOT HAND-EDIT.
     Edit GOOD-MORNING.md / _LIVE-STATE.md and regenerate; `--check` is a build step. -->

# The read chain — everything a cold session must read, and nothing else

> **This file is GENERATED and it is the whole contract.** It carries `GOOD-MORNING.md`'s header
> and ★ LATEST banner, plus `_LIVE-STATE.md`'s ⏱ LATEST delta — the three things GM-D7-am names
> (CUT #33 on Dave's ruling). **If you have read this file, you have read the chain.**
>
> ⚠ **Do NOT now open `GOOD-MORNING.md` to "check".** It is 34,811 tape (cl100k ESTIMATE); this file already
> carries the part of it a cold session needs, and the rest is retrieval surface you are not
> meant to pay for at boot. That reflex is the thing this file
> exists to stop — five sessions called the chain CUT and every one of them paid full price.
>
> ★ **Everything else is RETRIEVAL, never a reading list:**
> `python3 knowledge/_memento_search.py "<q>"` → `--fetch <id>`. §A orientation is `gm:A`; the queue is `gm:C1`…; the archives, briefs, runbooks and ledgers are all indexed.
> **Ask for what you need; do not read a file to find out whether you need it.**

---

> **YOU ARE #266. TITLE THIS CHAT →** `Apollo - #266: the four ungraded -active glyphs, the proposed lead`
> *(read it back in your opener — the chat half of the ritual is ungateable, which is why this line is FIRST)*

---

<!-- ⛔ STOP. If you are a cold session, you are in the wrong file and you pay for ALL of it.
     Read `_CHAIN.md` instead — it is GENERATED from this file's header + ★ LATEST banner and
     `_LIVE-STATE.md`'s ⏱ LATEST delta, which is the ENTIRE contract (GM-D7-am, CUT #33).
     This file stays whole for RETRIEVAL: `_memento_search.py "<q>"` → `--fetch <id>`.
     ⚠ Reading on from here is not thoroughness — it is the exact overspend #41 measured. -->

# Good morning, Dave ☕

> ⚠ **WRAP DATE SPLIT — SESSION 2026-09-02, COMMIT 2026-09-03.** #241's ritual was authored 09-02 and steps 1→4c landed that night; the commit did not, and step 5 was finished the next morning. ⛔ **No key, filename or stamp was re-dated to match the commit** — that would be the T-D12 false inscription in reverse; this line carries the commit date so the gate's `is not today` check grades a true statement. Detail + the ruling-shaped question in `_LIVE-STATE.md`'s header block.
> ⚠ **WRAP DATE SPLIT, SECOND OCCURRENCE — SESSION 2026-09-05, RITUAL + COMMIT 2026-09-06.** #248's conductor cut the wrap brief at 183,983 real FILL on the evening of 09-05; the delegated wrap sub ran every step the next morning. ⛔ **No key, filename or stamp was re-dated to match the commit** — the #241 shape, by ADDITION (the #241 line above stands; its ruling-shaped question, what a midnight-spanning wrap should stamp, is still Dave's at age 7). This line carries the commit date so the gate's `is not today` check grades a true statement. Detail in `_LIVE-STATE.md`'s header block.
> ⚠ **WRAP DATE SPLIT, THIRD OCCURRENCE — SESSION OPENED 2026-09-08 EVENING, RITUAL + COMMIT 2026-09-09.** #261's R and D3 lanes committed on the evening of 09-08 (22:11–22:44); the design wave, the revision seats and this ritual all ran on 09-09. ⛔ **No key, filename or stamp was re-dated to match the commit** — the eleven filed reports keep their `2026-09-08-261-*` stems, and each of the nine `s261-D*` rulings carries in its own `evidence` the sentence saying the hour was not recorded at the wrap seat, so the date it carries is the COMMIT date and not a claim about the hour. The #241 shape by ADDITION: both lines above stand, and #241's ruling-shaped question — what a midnight-spanning wrap should stamp — is still Dave's, now at age 20. Detail in `_LIVE-STATE.md`'s ⏱ LATEST DELTA.

> **size:** GM **34.3K tape** (34267 exact — whole-file `tiktoken cl100k_base`, the figure this stamp is graded against at 10% tolerance) · §A **7.0K tape** (6,957 real by `_gm_usage.py`; the gate's own §A→§C slice measures **4,375 tape / 16,489 B**, EXEMPT and uncapped by ruling — ✅ **BYTE-IDENTICAL to #237…#264's for a TWENTY-EIGHTH consecutive wrap, PROVEN at this seat by digesting the section against `git show HEAD:GOOD-MORNING.md` with the gate's own `section_a_digest` — both read `4311cce4f78114cb…`**, so the baseline stays armed rather than assumed clean; ⚠ the byte figure reads **16,489** here against #264's **16,490** — a slice-boundary artefact, and the DIGEST is the authority that says nothing moved; ⛔ nothing here is a trim order — §A is standing and what to do about it is Dave's) · LS **61.4K tape** (61397 real) · corpus GM+LS **95.7K tape** (95664 real, the RETRIEVAL surface, not the chain) · measured 2026-09-09 at the #265 wrap. ⚠ **THE DELTA IS THE FINDING, AND BOTH FILES ROSE:** against #264's stamp (**33,343 / 58,552 tape**) GM is **924 tape UP** and LS is **2,845 tape UP**. ★ **The reason is measurable and it is not neglect — this session had more to record than #264 did, and the rolls did not cover it:** 2c rolled the **#263** banner (12 ln → `_GM-ARCHIVE.md` § Batch 2026-09-09 #265), 2f rolled the `#### 2026-09-09 #264` stratum (20 ln → `notes/_GAUGE-LOG.md`, 2 ln → `_GM-ARCHIVE.md`), and 2d rolled the **#262** ⏱ delta (18 ln) **plus its 5,474-character `Previous:` chain segment** VERBATIM to `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-09 #265 — and back in came a **1,200-tape** ★ LATEST banner, a **20-line** ⏱ LATEST delta covering four lanes and two drivers, a longer `Last refreshed` summary and a **21-line** #265 stratum. ✅ **`s241-D2`'s S1 CAP MET A TWENTY-FIFTH TIME — the ★ LATEST banner is 1,200 tape across 9 substantive lines against the cap of 1,200 / 10** (the gate's OWN slice, ★ LATEST heading → ★ PRIOR start), ⚠ **and the cap is BLOCKING, not a guide** — ⛔ **it was met by writing SHORTER, never QUIETER: nine drafts were measured, two bullets were MERGED rather than dropped, and not one item, carry, declared skip, receipt name, path or figure was lost** (gauge and not-done detail lives in the ⏱ delta, which `s241-D2` names as its home). The `/goal` grades, the two rulings' verbatim words, the four lanes' findings, the KG fence and the gauge live in the ⏱ LATEST DELTA. ⛔ **THE CHAIN FIGURE IS NOT COPIED HERE (retired #45); its ONE home is the generated footer of `_CHAIN.md`**, quoted in the `#### 2026-09-09 #265` stratum and RE-READ AFTER this stamp, because the stamp sits inside the chain slice. ⚠ **TWO INSTRUMENTS, TWO UNITS, NEITHER CONVERTED** [[measure-dont-convert-units]]: `_gm_usage.py --sizes --session 265` reports GM **46,573 real** while the whole-file cl100k measurement reads **34,267 tape**; the stamp leads with the figure the gate grades. **DECLARE-LAST — the declared skips, EACH WITH ITS SIZE, live in the `#### 2026-09-09 #265` stratum's ⚠ DECLARED SKIPS line and are NOT restated here** (2e = **0 dated strata**, DO-FIRST **10,140 real / 78 raw lines** · 2c/2d/2f **ALL RAN CLEAN** · memory **0 bytes at this seat, a SEAT LIMIT and the FIFTH consecutive — the conductor wrote the hook before delegating** · `_CARRIES.md` never read whole, **11.4 MB (11,996,718 B, MEASURED at this seat after the section landed)** · **BOOT MEASURED 70,260 real — a BREACH of the 70,000 shrink-only literal, and the literal does not move** · full `test_gates.py` NOT RUN at this seat · `--selftest` NOT re-driven whole · **no Artifact publish** — refused in Cowork since #263). ✅ **TWO RULINGS `s265-D1` and `s265-D2` INSCRIBED AT ENTRIES 440–441 (439 → 441) — the `/goal` bite-test stands alone, and its third arm is graded by a reader.** ⛔ **AND TWO CARRIES ARE STRUCK WITH THEIR RECEIPTS, ONE OF THEM IN HALF** — the #265 lead's `/goal` clause was built, driven and ruled while its four-ungraded-glyphs half is untouched and re-carried in front, and the fail-open venue that said nothing tests whether the pack holds a goal is closed for the HOLD clause. ✅ **THE UNIT IS REAL CLAUDE TOKENS (#82-D1, Dave's, ruled #54) — MEASURED, NEVER CONVERTED; ratios REPORTED, NEVER APPLIED.**
> **STATE: ★★ THE READ CHAIN IS CUT (#33) and it HELD.** *(the session-count ordinal is DELETED per s178-D1 enacting dream-P1 — stale twice ⇒ generate, never re-stamp)* You are reading the contract. §A and §C are STILL IN THIS FILE, reached by `_memento_search.py`, not by reading top-to-bottom. ⛔ **BUILD VERDICT: 75 of 144 steps green (#62, `18c7789`) — 69 steps have NEVER been in a green verdict.** Both counts GENERATED from `_build_all.py`'s AST at each end; the shortfall is computed (`s125-D1`). A single-process `_build_all.py` run is SANDBOX-IMPOSSIBLE (~49s vs the ~45s call kill); CI delivers the single-process verdict on push. ⚠ **DO NOT RUN `_build_all.py` TO CHECK — ANY PARTIAL run strands the tree in the documented mid-build intermediate** (step 1 rewrites wholesale; healed only by a COMPLETE pass — docstring lines 5–21). ★ **Only the PREMISE of a derived rule is worth re-verifying** (#47's lesson, turned on #47). *(The #47/#61/#62 build-claim archaeology + the #38 cap-repoint finding are HOMED at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET and `notes/_briefs/2026-07-29-cap-repoint-and-lane-divvy-brief.md`.)*
> ⚠ **`set_content()` IS BANNED IN RENDER-PROOFS** — drops `type.css` silently (14px→16px) and `document.fonts.check` passes anyway; use `goto("file://…")` (#29, measured) · **ds-020 is a gate-enforced obligation** (the DV-D02-A scatter waiver names it as clears-when) · **ritual step 2g: rebuild `_memento-index.json` LAST** — `index_freshness_check` is BLOCKING and compares CONTENT, never mtime (runbook has the body).
> **★ PRICE IN REAL TOKENS — THE UNIT IS `_gauge_tokens.py`, NOT A PERCENTAGE (#56, Dave's).** **amber 160,000 (PICKED) · working 200,000 (SOURCED) · hard 256,000 (SOURCED)** — provenance per `_gauge_tokens.py:63–83`, the authority [[read-chain-is-where-staleness-is-free]]. Unit = the FULL price (boot + job + wrap); ⚠ **a DECLARED gap passes, a SILENT one fails** — that asymmetry is the mechanism. The retired 45/60/63 % band is DORMANT in code, pinned — retire-or-pin = `G8`, Dave's. ⚠ **THE FLOOR IS MEASURED = `ds-025` RE-SCOPED #109 — first turn 65,400±1,400 real (n=5) + `_CHAIN.md` 10,499 additive = 75,899 floor** (was published 30,499 — under by 45,400; the OLD `disk 11,032 + harness unreachable` framing on this line was FALSE, retired #109). `MEMORY.md` 8,470 of the first-turn figure is split and measured; **56,308 remains unattributed — that is what `ds-025` item 1 now means.** ⛔★★★ **FIGURE SUPERSEDED #129 (`s129-D1`, Dave's): FLOOR RE-BASED 81,335 → 70,794** (`BOOT_FIRSTTURN_TK` 65,400 → 54,859 ±1,178, n=7). **By ADDITION: the `ds-025` question above is UNCHANGED; only the constant moved.** Authority = `_gauge_tokens.py`, never this line. ⚠ **`pip install tiktoken --break-system-packages` FIRST in a cold sandbox** — the gauge REFUSES, loud and named (`MeasurementRefused`); `_build_all.py` P5 = ABORT without it (#80). ★ **READ THE CHAIN ONLY.** ★ **Repo-state claims are verified against `git log` / a real run, never a banner.** *(The #58/#59/#80/#83 correction-provenance is HOMED at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET; the #109 boot-floor correction at `_DECISION-HISTORY/2026-08-06-109-boot-floor-measured.md` and § ★ #109.)*
> ⚠ **Render sandbox is FRESH every session** (~4 calls — price it) · **retrieval FIRST:** `_memento_search.py "<q>"` → `--fetch <id>` (⚠ **it served a two-session-old record until #32 — if an answer looks stale, check the index**) · **wrap moves RUN THROUGH `_gm_move.py`** (⚠ **banner headings are BLOCKQUOTED — anchor `> ## ★ PRIOR …`**) · **PACE PANEL: ask at the opener, no gate can see it** — Dave has declined a fresh one twice and put Fable on the table for judgment work; ✅ **THE "BEHIND PACE ⇒ MORE WINDOWS" CLAUSE IS RETIRED — RULED #60: multi-window is settled, two use cases (quota catch-up AND production crank), Claude offers/Dave vetoes, cap 3 revisable. `notes/_MEMENTO-DECISIONS.md` § ★ #60-D1.** It stood from #48; at #57 he said the opposite — *"we are behind, but I'm less comfortable with multiple windows, too much to decide and consume"* ⇒ **ONE window with more delegation.** ★ **The clause optimised the wrong quantity: it treated TOKENS as the constraint when DAVE'S DECISION LOAD is what binds.** ⚠ **Reflected back #57, and #58's opener did NOT settle it** (asked as the first question; he answered pace with *"no constraint, lets just fix the problems"*). Standing home + status: §C·4.
> *(**RENAME is delivered IN CHAT at wrap, not here — RULED #28, ENACTED in runbook step 4b by #30.**)*
> **TITLE THE NEXT CHAT →** `Apollo - #266: the four ungraded -active glyphs, the proposed lead`
> ✅ *(the #86 hand-patch that stood here is REMOVED by the #87 wrap, per its own text — #85/#86 are recorded as HOLE lines + ledger stubs, `notes/_GAUGE-LOG.md` / § ★ #85–#86.)*
> *(Titles are LABELS — role comes from Dave's opener line. Bands: the ONLY copy is
> `_RUNBOOK-context-gauge.md` § ★ THE FLOOR IS MEASURED — grep it, never recall it.)*
> ## ★ LATEST — 2026-09-09 (Wed from `date` **#265**, **Fable** + **4 OPUS LANES** + 2 drivers + this sub, DELEGATED — **`/goal` BITES ON HOLD ALONE; FOUR REDS WORKED**)
>
> - **⓪ SHAPE.** ⏱ delta #265 · WHY `_DECISION-HISTORY/2026-09-09-265-goal-bite.md` · reports `notes/_subreports/2026-09-09-265-{G,B,C,D,E,W}-*.md` · **6** commits `9a24af9..087e058` + this wrap's, **LOCAL, UNPUSHED** (`s133-D2`). ✅ **NO DATE SPLIT.** ⛔ **Dave went to bed; this seat RULED NOTHING.**
> - ★ **① THE `/goal` BITE-TEST NARROWS `s262-D5`; TWO RULINGS FOLLOWED — HIS, 440–441 (439→441), WORDS IN THE DELTA.** `_bite_goal.py` cuts option 3's span from `COPILOT-BOOT.md` (ASSERTED); 2 blind lanes: **INTACT GREEN 2/2 · MUTATED RED 1/2** ⇒ bites on **G2, HOLD AGAINST DRIFT** alone; **G1 passed BOTH arms — rule 7a's**; **G3 un-regexable**. `D1` ***"1. okay make it stand alone"*** — STANDALONE, **not a CASE** (`claude -p` → `api_error`) · `D2` ***"2. okay yes"*** — G3 **RUBRIC-ONLY**. `9a24af9`·`16922b5`.
> - ✅ **② `s263-D10` HAS ITS BITE ARM, AND IT GRADES THE CLAUSE.** `selftest_rulings_freshness()` + `case_capture_gate_rulings_arm()`, 4 bites, **102 ins/0 del**; 3 FAIL-OPEN edits each turned it **RED**, control GREEN. ⛔ Not a `CASES` row — no exit code, REPO-scoped. `67b1879`.
> - ✅ **③ FOUR REDS WORKED, REPAIRS FROM THE REPO'S OWN HISTORY.** `_validate_icons.py` **GREEN** (Legend's 5 = **chart canvases, not icons**); `footer.meta.json` valid (`edges.composes` = **invented type, no consumer**; provenance RESTORED `6e6271f`); `_build_integrity` **16→14**, schema **133/137**, the 14 = **schema decisions**; `_validate_kg.py` **13→5**, 7 nodes **byte-for-byte from a clean regen's output**. ⛔★ **FENCE, MEASURED (441-ln diff): DO NOT RUN `gen_kg_edges.py` — a regen deletes authored edges; `DECLARED_EDGE_TYPES` holds ONE name.** `a88673e`·`087e058`.
> - ★ **④ `_PROPOSED-263.html` DRIVEN IN A BROWSER, A FIRST:** eleven rulings were ruled off a page never rendered. Round-trip survives reload with its note · `NOT RULED` exact 11/8/0 · 33 `button.rb` **44.0px** · **console silent**. ⛔ **2 defects, page UNEDITED:** bar targets **34.2px**; `[data-theme=dark]` a **DEAD ARM** breaking TWO-RED LAW. `d3a2297`.
> - ⚙ **⑤ GAUGE + NOT DONE (⏱ delta, `s241-D2`):** FILL **147,425/148,308 first-hand**, **32,575 UNDER** 180,000 ⇒ **delegated because he is ASLEEP.** ⛔ **BOOT 70,260 — 260 OVER the shrink-only 70,000, a BREACH** after 8 clean; arm refuses on #255's 70,127. ✅ `subs 560,101 (n=6)`; ⛔ **this seat's spend unreadable — NO number.** ⛔ Open: MEMORY (SEAT LIMIT **5th**) · **13 RSQs unruled** · the `.git` lock was a **PERMISSION**, not stale.
> **residual → #266:** ⬛ **① THE FOUR UNGRADED `-active` GLYPHS, THE PROPOSED LEAD** [NEW — 0, DAVE'S] — ⛔ **POINTER, NOT LIST (`s225-D2`).** **536 aged · 7 new · 2 STRUCK** in `_CARRIES.md` § `residual → #266` (`carries:residual-266`). ⚠ **PROBE → 396:** `PYTHONPATH=knowledge python3 -c "import _capture_gate as c;print(len(c._carry_items([l for l in open('_CARRIES.md') if '#266:**' in l][0])))"`
> **residual (GENERATED #265):** 2c OK (banners 2/2) · 2d OK (deltas 3/3) · 2f OK (strata 1, log #264) — _roll_state.py · 2026-09-09

> **⬛ OPEN WORKLIST — PRESENCE INDEX (28 items, GENERATED). Every open item is named; NO bodies are here — `--fetch gm:DOFIRST`.**
> `0b` ENCODE BEFORE THE WAVE, part-consumed 28 · `0c` NEXT BUILD CANDIDATES · `0d` THE 67 ENACT WAVE, landed · `1` ds-018 C2 follow-through · `2` dv-legend/dv-behaviour CEILING · `3` ds-012(b) gutter-relative plot area · `4` DV-D16 floating growth · `5` Instrument-fit remainder · `6` ds-016, PART-RULED #114 · `7` ds-017, UNRULED · `8` STILL OWED, none superseded · `9` DELEGATION TOPOLOGY · `10` ✅ PER-GATE TEST PLAN · `11` 2c-ROLL / INDEX-VOCAB DEADLOCK · `12` #57 1b DOSSIER · `13` tmp RUNBOOK EXPOSURE, unfixed · `14` ⬛ DAVE'S FOUNDING PRINCIPLE · `15` LEDGER § ★ #59 · `16` UNHOMED PAIR (#78 2c EXIT CHECK) · `17` CHAIN-DIET CUT · `19` type.css 180 dark-mode specificity · `20` ds-029 FIRST IDIOM · `21` PER-THEME COLLISION SWEEP · `22` 89-D2 ruled not enacted, absent from state · `23` ds-025 re-scoped 109, not closed · `24` 110 boot-rent plan · `25` ds-032 enactment, ruled 106 · `26` ✅ boot-drift gate rulings
> **QUEUE — `gm:C1` strands · `gm:C2` ruling batch (Dave's) · `gm:C4` enact-queue.**

---

## ⏱ LATEST DELTA — 2026-09-09 (Wed from `date`) (**#265**, one evening, conductor **Fable** + **4 OPUS 5 LANES** (B, C, D, E) + **2 blind OPUS drivers** + this OPUS 5 wrap sub — ★★★ **THE `/goal` CLAUSE BITES ON HOLD AND ON NOTHING ELSE, AND FOUR LANES PAID DOWN INHERITED RED**)

*(WHY/HOW: `_DECISION-HISTORY/2026-09-09-265-goal-bite.md` — why a mutation test that goes RED is a NARROWING rather than a pass, how three of four inherited reds were repairable from the repo's own history without a single authored word, and why the KG generator is now fenced by a measurement rather than by caution. Filed reports: `notes/_subreports/2026-09-09-265-G-goal-bite-test.md` · `notes/_subreports/2026-09-09-265-B-d10-bite-arm.md` · `notes/_subreports/2026-09-09-265-C-three-reds.md` · `notes/_subreports/2026-09-09-265-D-drive-proposed-263.md` · `notes/_subreports/2026-09-09-265-E-kg-red.md` · `notes/_subreports/2026-09-09-265-W-wrap.md`.)*

- ✅ **NO DATE SPLIT — THE SESSION, ITS RITUAL AND ITS COMMITS ARE ALL 2026-09-09**, confirmed from `date` at this seat. The three standing WRAP DATE SPLIT notices belong to #241, #248 and #261; nothing was re-dated here because nothing needed to be. #241's ruling-shaped question — what a midnight-spanning wrap should stamp — is **still Dave's, now at age 23**.
- ⛔ **DAVE RULED TWICE AND THEN WENT TO BED, AND THAT IS WHY THIS WRAP IS DELEGATED.** His words: *"lets crack on we have lots of tokens to use, we have spare budget and Im going to bed soon"*. The conductor's FILL at the brief cut was **147,425 real — 32,575 UNDER the `s260-D2` delegated stop line 180,000** ⇒ **the line was not crossed; the seat is held open for his read-back.** ⛔ **This seat RULED NOTHING, graded no glyph, and left thirteen lane ruling-shaped questions open in his name.**
- ★★★ **THE `/goal` BITE-TEST IS A NARROWING, NOT A PASS — AND THE NARROWING IS THE FINDING.** `knowledge/_bite_goal.py` (`--emit DIR [--mutate]` / `--grade transcript.json`) cuts option 3's five-line `/goal` span out of `apollo-spider/cold-start/COPILOT-BOOT.md` **with an assertion that it is gone**, and two Opus lanes — blind to each other and to the repo — each played the assistant from ONE boot through a 4-turn scenario. **INTACT GREEN 2/2 · MUTATED RED 1/2.** ⇒ **the clause is load-bearing on exactly one arm: G2, holding the goal against a drift.** Intact T2: *"One check against the goal, then I'll do as you say … It does not answer 'will I make rent this month' at a glance"* → two priced options. Mutated T2: *"The shape I'd build: the answer on top, the list under it … Say go and I'll build it."* — the drift absorbed, never named as one. ⛔ **G1 (confirm before build) passed in BOTH arms** — both T1s end *"Dashboard bento — is that right?"*, which is **rule 7a's**, not `s262-D5`'s; it stays counted because the ruling says so, but **it does not discriminate**. ⛔ **G3 (report against the goal) is UN-REGEXABLE and the transcripts show why**: intact T4 opens *"Against the goal — … The answer reads at a glance — not yet"* (met / not-met), mutated T4 opens *"Where we are: 1. Goal stated 2. Page built …"* (a done-list that happens to name the goal). A reader tells them apart in one glance; `GOAL_TERMS` cannot. ★ **[[no-gate-parses-the-artefact]] holds even for a transcript — halfway.**
- ★★★ **TWO RULINGS, DAVE'S, INSCRIBED — `knowledge/_rulings.json` entries 440–441, the store 439 → 441, both parsing with `by` Dave · `date` 2026-09-09 · `status` ruled.** **`s265-D1`** — ***"1. okay make it stand alone"***: the bite-test is a **STANDALONE instrument**, run by a lane with a live driver at every change to `COPILOT-BOOT.md` option 3, transcripts and grades filed as subreport assets; **it does not join `test_gates.py`'s CASES** while the sandbox has no scriptable driver (`claude -p` → `terminal_reason: api_error`, one probe, not diagnosed), because a case that cannot run in one environment is refused as a gate. **`s265-D2`** — ***"2. okay yes"***: `s262-D5`'s third clause is graded **RUBRIC-ONLY**, a reader grades T4, and the G3 regex arm is **ADVISORY and never counted**, with the reason written into the source beside it. `16922b5`.
- ✅ **`s263-D10` HAS THE REGISTERED BITE ARM IT WAS BORN WITHOUT, AND THE ARM GRADES THE CLAUSE.** Lane B added `selftest_rulings_freshness()` to `knowledge/_capture_gate.py` (wired into `_selftest_body()` beside its sibling `selftest_index_freshness`) and `case_capture_gate_rulings_arm()` to `knowledge/_tests/test_gates.py`, called from `main()` — **75 + 27 insertions, 0 deletions, nothing existing edited**. Four bites: the out-loud SKIP · the never-generated page · **a GREEN control that must also say `FRESH`** · the defect itself. **Three FAIL-OPEN mutants were applied to copies and every one turned the arm RED** — `if rc != 0:` → `if False:`, a silent `fails.append(…STALE…)` → `notes.append(…)` blocking→advisory demotion, and an early `return fails, notes` (which killed **all four** bites) — while an unmutated control loaded through the identical importlib path in the same process returned `[]`. ⛔ **A `CASES` row CANNOT hold this check and the reason is structural, now written into the code so no one retries it**: it is a *function* inside `wrap_checks()` with no exit code (`python3 knowledge/_capture_gate.py` bare is `REFUSED (write-gate)`), and it is REPO-scoped — it reads `knowledge/_rulings.json` **and** `notes/_RULINGS.html`, while `fresh_copy` copies `knowledge/` alone. ✅ **No copytree anywhere: peak scratch under 3 MB, and the mount reads 33% used / 309 G free — the 89% in the brief was the Mac disk, not this one.** `67b1879`.
- ✅ **TWO INHERITED REDS CLOSED, AND BOTH REPAIRS CAME OUT OF THE REPO'S OWN HISTORY RATHER THAN OUT OF A LANE'S JUDGEMENT.** `python3 knowledge/_validate_icons.py` is **GREEN (5 UNKNOWN → 0)**: Legend's five `<svg class="dv-svg">` elements are **bar-chart canvases, not icons** — `<rect class="dv-series">` + `<line class="dv-baseline">`, two specimens and three small multiples — and they gained the `data-bespoke="chart canvas — dataviz geometry, not an icon…"` reason string **copied byte-for-byte** from the fourteen chart snippets. `knowledge/components/footer.meta.json` is **SCHEMA-VALID for the first time since #261**: `edges.composes` was **an invented edge type with no consumer anywhere in `knowledge/*.py`** — silently invisible to the KG rather than merely non-validating — and became `edges.consumes` (the typed key fifteen sibling metas use) plus the `$consumes` array `gen_kg_edges.py:322` name-matches, without which the next regeneration would drop the edge again; `provenance.source: "redesign"` → `"gap-report"` was **RESTORED from `git show 6e6271f`**, not chosen. `_build_integrity.py`: **16 → 14** errors, schema **132/137 → 133/137**. `a88673e`.
- ⛔ **THE FOURTEEN INTEGRITY ERRORS THAT STAND ARE ONE CAUSE — #261-ERA METAS OUTRAN `meta.schema.json` — AND EVERY REPAIR IS A SCHEMA DECISION, SO NONE WAS TAKEN.** Four shapes, censused over the corpus: `stateModel` authored as a rich object in **4** metas where the schema says `enum: simple|full` (111 absent · 23 string · 4 dict) · `with[]` as bare strings (Data grid ×6) and a `when` clause (Legend) with no seat · four edge types with no schema seat (`drivesConsumer`, `composedOf`, `delegatesTo`, `$contract`) · `relationships.composes` + `$composes-verbatim` in a closed object. ⚠ **`edges` is the ONE object in the schema with `additionalProperties:false` and NO `^\$` patternProperties**, which is why a `$`-sigil key reds there and nowhere else — recorded because it makes the "demote to a sigil" repair unavailable inside `edges` specifically. **No ruling governs any of it**: 441 rulings grepped for `footer|provenance|redesign|meta\.schema` — `s263-D7`/`s263-D12` govern footer's dormant variant only.
- ✅ **THE FOURTH RED — `_validate_kg.py` 13 → 5 — AND THE FIVE THAT STAND ARE A FENCE, NOT A BACKLOG.** Lane C found it standing beside the three; lane E classified all 13 by cause and repaired 8 **without wording anything**: seven `pattern:`/`context:` nodes registered with `id`/`label`/`sources` **copied entry-for-entry from a clean scratch regeneration's own output** (`_nodes-pattern.json` 375 → 381, `_nodes-context.json` 218 → 220, serialisation byte-verified against the generator's writer), and Legend's hand-slugged `context:small-multiples` **repointed** to `context:dashboard-rows-of-small-multiples` — the id the generator derives from that meta's own `relationships.livesInside` — because **nothing in the corpus produces the short id, so registering it would have meant inventing a node**. ⛔ **The freshness red is a STALE JOIN KEY, not a stale corpus**: `gen_kg_edges.py:load_resolutions` keys every PROMOTE on an **exact `$note` string**, and #258 rewrote the prose that note is derived from; the #135 verdict itself is unchanged and still correct. `087e058`.
- ⛔★★★ **`gen_kg_edges.py` IS FENCED BY A MEASUREMENT: A CLEAN REGENERATION TODAY REWRITES EIGHT METAS AND DELETES AUTHORED CONTENT.** The diff is **441 lines** and is filed (`probe-regeneration-would-rewrite-8-metas.diff`): it deletes Filter-toolbar-bar's `composedOf` / `delegatesTo` / `drivesConsumer` / `$contract` blocks in full — the `s258-D1` event+attribute contract, ~40 lines — Legend's `containedBy` and `governedBy` edges (`ruling:s151-D1`, `s248-D3`, `s258-D1`), and the tail of Footer's `context:template-dashboard` `$note`; the rest is field-ORDER churn from `splice_edges` putting `edges` last. **`s245-D7` is the ruled mechanism that protects authored edges from exactly this, and its carry list is `DECLARED_EDGE_TYPES = ("groupsWith",)` — ONE entry, so nothing else survives.** ⇒ **do not run the generator before lane E's RSQ 2 is ruled.** The registries were advanced by **ADDITION ONLY** for the same reason: a regeneration would REMOVE `context:page-above-a-table-or-list-items-region`, which `filter-toolbar-bar.meta.json:151` live-references.
- ★★★ **`notes/_PROPOSED-263.html` WAS DRIVEN IN A REAL BROWSER FOR THE FIRST TIME — THE PAGE ELEVEN RULINGS WERE MADE OFF HAD NEVER BEEN RENDERED.** Chromium headless in the sandbox off `_RUNBOOK-render-verify.md`; ⚠ **every path the runbook names as reusable was GONE (VM rebuild)** — `/var/tmp/pylibs`, `/var/tmp/pw-browsers-220`, `~/.cache/ms-playwright`, `<MOUNT>/outputs/syslibs` — so the class fix was re-run from scratch and it worked, exit 0, `ldd | grep -c "not found"` → **0** after one rootless `libxdamage1` deb. **Five contexts, and the interaction core is OBSERVED:** `localStorage` round-trip survives `reload()` with ruling AND note intact (`{"P-01":{"v":"ACCEPT","note":"ink seat, confirmed"},…}`) · click-again-to-unset decrements 3→2→3 · `#tallyN` and `#sRuled` stay in sync through 0 → 3 → 11 → clear · the export block's `NOT RULED` line is exact at **11 / 8 / none** · all **33** `button.rb` measure **44.0 × 243.3** at 760 and **44.0 × 115.0** at 375, one distinct size, no outlier · `scrollWidth == clientWidth` at both widths · **the console is completely silent — zero logs, zero warnings, zero page errors, across all five contexts.** `d3a2297`.
- ⛔ **AND TWO REAL DEFECTS FELL OUT OF THAT DRIVE, BOTH IN THE THEME LAYER, AND THE PAGE WAS NOT EDITED.** **(a)** The sticky control bar does not hold the floor its own ruling rows hold: `#exportBtn`/`#clearBtn` measure **34.2px**, `label.tog` **35.0**, the `#decide` checkbox **13×13** — at BOTH 760 and 375, because the `@media (max-width:760px)` block at lines 314–320 grows `button.rb` and never touches `.bar button,.bar label.tog`. **(b)** The `[data-theme="dark"]` block at line 196 is a **DEAD ARM**: there is no toggle, theming is `prefers-color-scheme` only and on that path it is **CORRECT** (`#DA1A00` light / `#F6604C` dark — TWO-RED LAW `s151-D1` held at 1280 and 375); but the block redefines **no custom properties**, so forced on `<html>` it is invisible (the two PNGs are **byte-identical, 81,705 B both — that identity IS the finding**) and forced on `<body>` it yields a dark ground with **`accent` still `#DA1A00`** — a TWO-RED-LAW violation the working path does not have — with the bar, the quote panel and the output panel still light-painted. ⚠ **The file also declares two different darks: `#1A1A1A` at line 196 against `#0C0C0C` at line 224.** All three are D's RSQ 1–3.
- ⚙ **GAUGE — TWO OBJECTS, REPORTED SEPARATELY, NEVER SUMMED OR CONVERTED** [[measure-dont-convert-units]]. **FILL** is resident context (`message.usage`); **THROUGHPUT** is the cumulative log. ✅ **NO HOLE — `tiktoken` was installed at the conductor's opener.** The conductor **DECLARED 147,425 real** at the brief cut — **32,575 UNDER** the `s260-D2` delegated line of 180,000 ⇒ **the delegation is because Dave is asleep, not because a line was crossed.** First-hand at this seat, `knowledge/_checkin.py` reads his transcript at **148,308 real over 47 turns**; `s214-D5`: **brief-cut 147,425 · sub-cut 148,308 · delta 883 · replay unobservable (conductor-side, post-wrap)** — the second consecutive wrap at which the hand-over is fully measured. ⛔ **BOOT 70,260 real, MEASURED — 260 OVER the 70,000 shrink-only literal, RECORDED AS A BREACH**, ending eight consecutive under-ceiling readings (#264 read 69,955). ⛔ **The literal is SHRINK-ONLY (`s240-D2`/`s241-D1`): the remedy is to cut the boot, never to raise the number, raising it is Dave's word alone, and it is not a price a wrap may pay to unblock itself** [[gate-must-quote-what-it-forbids]]. The gate's standing refusal is still #255's **70,127**; the commit is made in the **#243 form** with the refusal RECORDED, the **nineteenth** wrap running.
- ✅ **A `subs` LINE IS WRITTEN, AND EVERY FIGURE IN IT IS A MEASUREMENT** — Agent `usage`, REAL Claude tokens, **QUOTA not FILL**, never added to the block's fill numbers and never graded against the stop line: the two `/goal` drivers **48,453** + **49,700**, lane **B 117,210**, lane **C 148,627**, lane **D 80,597**, lane **E 115,314** — `subs 560,101 tokens (n=6)`. ⛔ **THIS WRAP SEAT'S OWN SPEND IS NOT IN THAT FIGURE AND NO NUMBER IS WRITTEN FOR IT** — a sub cannot read its own `message.usage` from inside itself, and an UNKNOWN is never turned into a number [[feedback-measuring-tool-must-not-guess]].
- ⛔ **THE "STALE `.git/index.lock`" WAS NEVER STALE, AND THE DIAGNOSIS THAT STOOD FOR TWO SESSIONS WAS WRONG ABOUT THE CAUSE.** The Cowork sandbox was refusing **all unlinks** until `allow_cowork_file_delete` was granted, so git's own lockfiles and its `tmp_obj_*` objects survived every commit — not because a process died holding one, but because nothing on this mount could remove any file. Granted mid-session; cleared. ★ **The carried item that says a stale lock "can only be MOVED, and Dave deletes it Mac-side" now has a second, truer half: the permission is grantable from inside.**
- ⚠ **DECLARED SKIPS AT THIS WRAP, EACH WITH ITS SIZE** (declare-last, and the size is the point): **2e = 0 dated strata rolled** — `## ⬛ DO THIS FIRST` measures **10,140 real / 78 raw lines**, inside the 120-warn / 180-block band, and nothing in it aged past LATEST+1 · **2c RAN** (the #263 banner → `_GM-ARCHIVE.md` § Batch 2026-09-09 #265) · **2d RAN** (the #262 ⏱ delta + its `Previous:` chain segment, VERBATIM → `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-09 #265) · **2f RAN** (the `#### 2026-09-09 #264` stratum → `notes/_GAUGE-LOG.md` + `_GM-ARCHIVE.md`) · **MEMORY = 0 bytes at this seat (SEAT LIMIT, FIFTH CONSECUTIVE; the conductor wrote the hook before delegating)** · **`_CARRIES.md` never read whole — 11.4 MB (11,996,718 B, MEASURED at this seat after the section landed), worked line-wise through `_capture_gate._carry_items()`** · **BOOT MEASURED 70,260 real — no hole, and a BREACH** · full `knowledge/_tests/test_gates.py` **NOT RUN** at this seat (lane B drove its own new case in-process and took no copytree) · `_capture_gate.py --selftest` **NOT re-driven whole** (it exceeds the sandbox call wall — the fifth consecutive wrap to declare it) · no `_build_all.py`, no `_validate_screen.py`, no retrieval query, no recall plant, **no Artifact publish** — refused in Cowork since #263.
- ⬛ **STILL HIS, CARRIED FORWARD — RULING-SHAPED AND NOT RULED HERE:** the **four ungraded `-active` glyphs** (balance-transfer, tax, workspace, digital-statements) on `notes/_REVIEW-264-active-glyphs-2.html`, the conductor's PROPOSED lead for #266 · **`global-money`**, which is one connected ink piece and needs DRAWING, not deriving · **THIRTEEN lane ruling-shaped questions — B:2 · C:5 · D:3 · E:3** — every one filed with its options priced and a recommendation, and **not one ruled**, because Dave was asleep · the **two `_PROPOSED-263.html` defects**, one CSS line each and both ruling-shaped · **`DECLARED_EDGE_TYPES`**, which fences the KG generator until he answers · whether a **gate report should be refused at commit when its own tree ≠ HEAD** (#263, age 2) · **#241's midnight-wrap question, age 23** · and **ritual step 3 at a delegated seat**, now with five datapoints. ⛔ **This seat ruled nothing.** **`_CARRIES.md` § `## residual → #266` is the carry set (`s225-D2`, probe 396); the banner carries the pointer and the probeable count.**


---

## ⬛ OPEN WORK — GENERATED from `_state.json`. **The row count IS the count.**

> **625 items · 526 live · 246 Dave's · 280 mine · 611 carry a stated close condition · **14 UNCONDITIONED**.**
> *Every figure in this block is computed from the store at generation time. No number here was typed by anyone — that is the point of it (#86 measured a typed inventory of "118 markers" against a real ~40; #85's "95 slots / 84 distinct" is not reproducible by any probe in this repo).*
> ⚠ **DECLARED DEBT — 14 inherited items have NO close condition and therefore cannot close.** They are exempt as a FROZEN set (`_state.LEGACY_IDS`, size 19) which may only shrink; a NEW item is refused without one. **Each needs Dave's word — an agent inventing a close condition for his open work is the same overreach as inventing his ruling.**
>
> **DAVE'S (246) — 246 with a ratified `closes_when`, 0 with NONE · MINE (280). IDS, BODIES AND CLOSE CONDITIONS ARE IN THE STORE, NEVER HERE (s227-D2 shape, ids cut `s241-D2`): the counts above are the inventory; the rows are one command away, named on the next line.**
>
> ⚠ store gate: **PASS**. Bodies, conditions and provenance: `python3 knowledge/_state.py`.


---

*(Chain ends. **10,999 tape (cl100k ESTIMATE) — the unit is THE WHOLE FILE**, this generated wrapper included:
the number in this sentence is the size of the file containing it, held exact by a fixed point.
`GOOD-MORNING.md` is 34,811 tape (cl100k ESTIMATE) and stays whole for retrieval; you have paid for
32% of it. Under 40% is this generator's own floor — above it, the wrapper is
carrying more than the slice, and `--selftest` says so by name.)*
