<!-- GENERATED by knowledge/_gen_chain.py — DO NOT HAND-EDIT.
     Edit GOOD-MORNING.md / _LIVE-STATE.md and regenerate; `--check` is a build step. -->

# The read chain — everything a cold session must read, and nothing else

> **This file is GENERATED and it is the whole contract.** It carries `GOOD-MORNING.md`'s header
> and ★ LATEST banner, plus `_LIVE-STATE.md`'s ⏱ LATEST delta — the three things GM-D7-am names
> (CUT #33 on Dave's ruling). **If you have read this file, you have read the chain.**
>
> ⚠ **Do NOT now open `GOOD-MORNING.md` to "check".** It is 34,132 tape (cl100k ESTIMATE); this file already
> carries the part of it a cold session needs, and the rest is retrieval surface you are not
> meant to pay for at boot. That reflex is the thing this file
> exists to stop — five sessions called the chain CUT and every one of them paid full price.
>
> ★ **Everything else is RETRIEVAL, never a reading list:**
> `python3 knowledge/_memento_search.py "<q>"` → `--fetch <id>`. §A orientation is `gm:A`; the queue is `gm:C1`…; the archives, briefs, runbooks and ledgers are all indexed.
> **Ask for what you need; do not read a file to find out whether you need it.**

---

> **YOU ARE #264. TITLE THIS CHAT →** `Apollo - #264: the stat card arrow seat, /goal and the sidebar chart glyph`
> *(read it back in your opener — the chat half of the ritual is ungateable, which is why this line is FIRST)*

---

<!-- ⛔ STOP. If you are a cold session, you are in the wrong file and you pay for ALL of it.
     Read `_CHAIN.md` instead — it is GENERATED from this file's header + ★ LATEST banner and
     `_LIVE-STATE.md`'s ⏱ LATEST delta, which is the ENTIRE contract (GM-D7-am, CUT #33).
     This file stays whole for RETRIEVAL: `_memento_search.py "<q>"` → `--fetch <id>`.
     ⚠ Reading on from here is not thoroughness — it is the exact overspend #41 measured. -->

# Good morning, Dave ☕

> ⚠ **WRAP DATE SPLIT — SESSION 2026-09-02, COMMIT 2026-09-03.** #241's ritual was authored 09-02 and steps 1→4c landed that night; the commit did not, and step 5 was finished the next morning. ⛔ **No key, filename or stamp was re-dated to match the commit** — that would be the T-D12 false inscription in reverse; this line carries the commit date so the gate's `is not today` check grades a true statement. Detail + the ruling-shaped question in `_LIVE-STATE.md`'s header block.
> ⚠ **WRAP DATE SPLIT, SECOND OCCURRENCE — SESSION 2026-09-05, RITUAL + COMMIT 2026-09-06.** #248's conductor cut the wrap brief at 183,983 real FILL on the evening of 09-05; the delegated wrap sub ran every step the next morning. ⛔ **No key, filename or stamp was re-dated to match the commit** — the #241 shape, by ADDITION (the #241 line above stands; its ruling-shaped question, what a midnight-spanning wrap should stamp, is still Dave's at age 7). This line carries the commit date so the gate's `is not today` check grades a true statement. Detail in `_LIVE-STATE.md`'s header block.
> ⚠ **WRAP DATE SPLIT, THIRD OCCURRENCE — SESSION OPENED 2026-09-08 EVENING, RITUAL + COMMIT 2026-09-09.** #261's R and D3 lanes committed on the evening of 09-08 (22:11–22:44); the design wave, the revision seats and this ritual all ran on 09-09. ⛔ **No key, filename or stamp was re-dated to match the commit** — the eleven filed reports keep their `2026-09-08-261-*` stems, and each of the nine `s261-D*` rulings carries in its own `evidence` the sentence saying the hour was not recorded at the wrap seat, so the date it carries is the COMMIT date and not a claim about the hour. The #241 shape by ADDITION: both lines above stand, and #241's ruling-shaped question — what a midnight-spanning wrap should stamp — is still Dave's, now at age 20. Detail in `_LIVE-STATE.md`'s ⏱ LATEST DELTA.

> **size:** GM **33.9K tape** (33902 exact — whole-file `tiktoken cl100k_base`, the figure this stamp is graded against at 10% tolerance) · §A **7.0K tape** (6,957 real by `_gm_usage.py`; the gate's own §A→§C slice measures **4,375 tape / 16,490 B**, EXEMPT and uncapped by ruling — ✅ **BYTE-IDENTICAL to #237…#262's for a TWENTY-SIXTH consecutive wrap, PROVEN at this seat by digesting the section against `git show HEAD:GOOD-MORNING.md` with the gate's own `section_a_digest` — both read `4311cce4f78114cb…`**, so the baseline stays armed rather than assumed clean; ⛔ nothing here is a trim order — §A is standing and what to do about it is Dave's) · LS **58.6K tape** (58616 real) · corpus GM+LS **92.5K tape** (92518 real, the RETRIEVAL surface, not the chain) · measured 2026-09-09 at the #263 wrap. ⚠ **THE DELTA IS THE FINDING, AND THE TWO FILES MOVED IN OPPOSITE DIRECTIONS AGAIN:** against #262's stamp (**34,488 / 58,191 tape**) GM is **586 tape DOWN** and LS is **425 tape UP**. ★ **GM fell by less than it rolled, and that is the honest reading:** 2c rolled the **#261** banner (11 ln → `_GM-ARCHIVE.md` under batch key `2026-09-09 #263`) and 2f rolled the `#### 2026-09-09 #262` stratum (14 ln → `notes/_GAUGE-LOG.md`, 2 ln → `_GM-ARCHIVE.md`) — and a **1,198-tape** ★ LATEST banner plus a **20-line** #263 stratum came back in. ⚠ **LS ROSE AND IS NOT DRESSED AS A HOLD:** 2d rolled the **#260** ⏱ delta (15 ln) and its **2,708-character** `Previous:` chain segment VERBATIM out to `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-09 #263, and a **24-line** ⏱ LATEST delta plus a longer `Last refreshed` summary came back in — thirteen rulings, the re-drive receipts and six new ruling-shaped items are the rise. ✅ **`s241-D2`'s S1 CAP MET A TWENTY-THIRD TIME — the ★ LATEST banner is 1,198 tape across 10 substantive lines against the cap of 1,200 / 10** (the gate's OWN slice, ★ LATEST heading → ★ PRIOR start), ⚠ **and the cap is BLOCKING, not a guide** — and ⛔ **it was met by writing SHORTER, never QUIETER: not one item, carry, declared skip, receipt name, path or figure was dropped.** The full ruling words, the per-lane commits and the gauge live in the ⏱ LATEST DELTA, which the cap's own text names as their home. ⛔ **THE CHAIN FIGURE IS NOT COPIED HERE (retired #45); its ONE home is the generated footer of `_CHAIN.md`**, quoted in the `#### 2026-09-09 #263` stratum and RE-READ AFTER this stamp, because the stamp sits inside the chain slice. ⚠ **TWO INSTRUMENTS, TWO UNITS, NEITHER CONVERTED** [[measure-dont-convert-units]]: `_gm_usage.py --sizes --session 263` reports GM **46,506 real** while the whole-file cl100k measurement reads **33,902 tape**; the stamp leads with the figure the gate grades. **DECLARE-LAST — the declared skips, EACH WITH ITS SIZE, live in the `#### 2026-09-09 #263` stratum's ⚠ DECLARED SKIPS line and are NOT restated here** (2e = **0 dated strata**, DO-FIRST **109 raw lines / 10,140 real** · 2c/2d/2f **ALL RAN CLEAN** · memory **0 bytes at this seat, a SEAT LIMIT and the THIRD consecutive one** · `_CARRIES.md` never read whole, **10.6 MB** · **BOOT NOT MEASURED** — no `tiktoken` at the opener · `notes/_PROPOSED-263.html` **never driven, never published** · no build, no `_validate_screen.py`, `test_gates.py` NOT RUNNABLE, no retrieval query, no recall plant). ✅ **THIRTEEN RULINGS `s263-D1` … `s263-D13` INSCRIBED AT ENTRIES 424–436 (423 → 436) — the eleven PROPOSED defaults, the variant-status schema, and the tenth release gate.** ⛔ **AND ONE CARRY IS STRUCK WITH ITS RECEIPT WHILE THE HALF THAT SURVIVES IT IS RE-CARRIED RATHER THAN VANISHING:** the eleven PROPOSED are ruled, and the page they were ruled off was never driven in a browser. ✅ **THE UNIT IS REAL CLAUDE TOKENS (#82-D1, Dave's, ruled #54) — MEASURED, NEVER CONVERTED; ratios REPORTED, NEVER APPLIED.**
> **STATE: ★★ THE READ CHAIN IS CUT (#33) and it HELD.** *(the session-count ordinal is DELETED per s178-D1 enacting dream-P1 — stale twice ⇒ generate, never re-stamp)* You are reading the contract. §A and §C are STILL IN THIS FILE, reached by `_memento_search.py`, not by reading top-to-bottom. ⛔ **BUILD VERDICT: 75 of 144 steps green (#62, `18c7789`) — 69 steps have NEVER been in a green verdict.** Both counts GENERATED from `_build_all.py`'s AST at each end; the shortfall is computed (`s125-D1`). A single-process `_build_all.py` run is SANDBOX-IMPOSSIBLE (~49s vs the ~45s call kill); CI delivers the single-process verdict on push. ⚠ **DO NOT RUN `_build_all.py` TO CHECK — ANY PARTIAL run strands the tree in the documented mid-build intermediate** (step 1 rewrites wholesale; healed only by a COMPLETE pass — docstring lines 5–21). ★ **Only the PREMISE of a derived rule is worth re-verifying** (#47's lesson, turned on #47). *(The #47/#61/#62 build-claim archaeology + the #38 cap-repoint finding are HOMED at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET and `notes/_briefs/2026-07-29-cap-repoint-and-lane-divvy-brief.md`.)*
> ⚠ **`set_content()` IS BANNED IN RENDER-PROOFS** — drops `type.css` silently (14px→16px) and `document.fonts.check` passes anyway; use `goto("file://…")` (#29, measured) · **ds-020 is a gate-enforced obligation** (the DV-D02-A scatter waiver names it as clears-when) · **ritual step 2g: rebuild `_memento-index.json` LAST** — `index_freshness_check` is BLOCKING and compares CONTENT, never mtime (runbook has the body).
> **★ PRICE IN REAL TOKENS — THE UNIT IS `_gauge_tokens.py`, NOT A PERCENTAGE (#56, Dave's).** **amber 160,000 (PICKED) · working 200,000 (SOURCED) · hard 256,000 (SOURCED)** — provenance per `_gauge_tokens.py:63–83`, the authority [[read-chain-is-where-staleness-is-free]]. Unit = the FULL price (boot + job + wrap); ⚠ **a DECLARED gap passes, a SILENT one fails** — that asymmetry is the mechanism. The retired 45/60/63 % band is DORMANT in code, pinned — retire-or-pin = `G8`, Dave's. ⚠ **THE FLOOR IS MEASURED = `ds-025` RE-SCOPED #109 — first turn 65,400±1,400 real (n=5) + `_CHAIN.md` 10,499 additive = 75,899 floor** (was published 30,499 — under by 45,400; the OLD `disk 11,032 + harness unreachable` framing on this line was FALSE, retired #109). `MEMORY.md` 8,470 of the first-turn figure is split and measured; **56,308 remains unattributed — that is what `ds-025` item 1 now means.** ⛔★★★ **FIGURE SUPERSEDED #129 (`s129-D1`, Dave's): FLOOR RE-BASED 81,335 → 70,794** (`BOOT_FIRSTTURN_TK` 65,400 → 54,859 ±1,178, n=7). **By ADDITION: the `ds-025` question above is UNCHANGED; only the constant moved.** Authority = `_gauge_tokens.py`, never this line. ⚠ **`pip install tiktoken --break-system-packages` FIRST in a cold sandbox** — the gauge REFUSES, loud and named (`MeasurementRefused`); `_build_all.py` P5 = ABORT without it (#80). ★ **READ THE CHAIN ONLY.** ★ **Repo-state claims are verified against `git log` / a real run, never a banner.** *(The #58/#59/#80/#83 correction-provenance is HOMED at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET; the #109 boot-floor correction at `_DECISION-HISTORY/2026-08-06-109-boot-floor-measured.md` and § ★ #109.)*
> ⚠ **Render sandbox is FRESH every session** (~4 calls — price it) · **retrieval FIRST:** `_memento_search.py "<q>"` → `--fetch <id>` (⚠ **it served a two-session-old record until #32 — if an answer looks stale, check the index**) · **wrap moves RUN THROUGH `_gm_move.py`** (⚠ **banner headings are BLOCKQUOTED — anchor `> ## ★ PRIOR …`**) · **PACE PANEL: ask at the opener, no gate can see it** — Dave has declined a fresh one twice and put Fable on the table for judgment work; ✅ **THE "BEHIND PACE ⇒ MORE WINDOWS" CLAUSE IS RETIRED — RULED #60: multi-window is settled, two use cases (quota catch-up AND production crank), Claude offers/Dave vetoes, cap 3 revisable. `notes/_MEMENTO-DECISIONS.md` § ★ #60-D1.** It stood from #48; at #57 he said the opposite — *"we are behind, but I'm less comfortable with multiple windows, too much to decide and consume"* ⇒ **ONE window with more delegation.** ★ **The clause optimised the wrong quantity: it treated TOKENS as the constraint when DAVE'S DECISION LOAD is what binds.** ⚠ **Reflected back #57, and #58's opener did NOT settle it** (asked as the first question; he answered pace with *"no constraint, lets just fix the problems"*). Standing home + status: §C·4.
> *(**RENAME is delivered IN CHAT at wrap, not here — RULED #28, ENACTED in runbook step 4b by #30.**)*
> **TITLE THE NEXT CHAT →** `Apollo - #264: the stat card arrow seat, /goal and the sidebar chart glyph`
> ✅ *(the #86 hand-patch that stood here is REMOVED by the #87 wrap, per its own text — #85/#86 are recorded as HOLE lines + ledger stubs, `notes/_GAUGE-LOG.md` / § ★ #85–#86.)*
> *(Titles are LABELS — role comes from Dave's opener line. Bands: the ONLY copy is
> `_RUNBOOK-context-gauge.md` § ★ THE FLOOR IS MEASURED — grep it, never recall it.)*
> ## ★ LATEST — 2026-09-09 (Wed from `date` **#263**, **Fable** + **3 OPUS 5 SUBS** + this OPUS 5 wrap sub, DELEGATED — ★★★ **THE ELEVEN PROPOSED DEFAULTS RULED, AND A TENTH RELEASE GATE**)
>
> - ★ **⓪ SHAPE.** ⏱ delta #263 · WHY `_DECISION-HISTORY/2026-09-09-263-eleven-defaults-and-the-tenth-gate.md` · **1** report `notes/_subreports/2026-09-09-263-W-wrap.md`, lane fragments `notes/_lanes/263-{P,E,A,C}-*.md` (4) ·**6** commits `cc875e9..5050b33`, PUSHED mid-session (`git ls-remote`, ff), + this wrap's. ✅ **NO DATE SPLIT — session, ritual and commit all 2026-09-09.**
> - ★★★ **① THIRTEEN RULINGS, HIS — 424–436 (423→436), FULL WORDS IN THE ⏱ DELTA:** `D1`–`D11` rule the eleven PROPOSED defaults off `notes/_PROPOSED-263.html`'s export block — **8 straight ACCEPTs**; `D7` doormat = a **"dormant variant"** of Footer (discharging `s261-D3`'s *"for now"*), `D8` chart legend **stays 44px**, `D9` `data-dv-controls` **contract-only** (*"an interesting idea… not required at this stage"*) · `D12` *"whatever is the best standard solution"* · `D13` *"yes"*.
> - ★★★ **② THE THREE `LATER`s WERE RULED ON HIS OWN SENTENCES, NOT FILED AS DEFERRALS** — each note WAS the decision; `D8` needed one question answered first. ★ **A `LATER` that arrives with a sentence is read before it is filed.**
> - ★★★ **③ A GATE `s263-D11` HAD JUST MADE LOAD-BEARING WAS GRADING NOTHING.** `type.css` moved at `4cc4204` (#261 F2) after the receipts were driven at `0420197`, so `_validate_dataviz.py` read **STALE on all 27 pages**. Chromium 151 installed without root; 27 × 8 re-driven at **`521a751`** — **0 differing measured fields** ⇒ **v1.0.9's charts are retroactively clean**.
> - ⛔ **④ AND `_DATAVIZ-GATE.md` AT `81ce08f` READ *PASS / 0 STALE* FROM A TREE THAT DID NOT MATCH HEAD** — the #253 pattern in a new venue, and the dataviz gate was **not** one of the nine release gates. `s263-D13` makes `--receipts-fresh` the **TENTH**, **write-nothing**; mutant RED + control GREEN; frozen/audit/ci still PASS (`5050b33`).
> - ✅ **⑤ `s263-D12` CAME OUT OF AN ENACTMENT, NOT A PLAN** — the schema refused a bare `status` on a variant, lane E took the legal `$status` route **and declared the deviation**; asked, Dave chose the standard: `variants[].status`, enum `active|dormant|deprecated` (`27f0f46`).
> - ⚙ **⑥ GAUGE + NOT DONE — home = the ⏱ delta** (`s241-D2`): conductor FILL **NOT MEASURED at the brief cut** — declared **ESTIMATE ~65–75K** and marked as one; throughput **167,102** (conductor) / **171,698** (this seat), cl100k-estimate. **subs 306,253 (n=3)**, QUOTA not FILL. Boot **not measured** (tiktoken absent at the opener). ⛔ Open: MEMORY (SEAT LIMIT, third running) · 3 pre-existing reds · no bite-test for the D10 arm.
> **residual → #264:** ⬛ **① THE STAT CARD ARROW SEAT, `/goal` AND THE SIDEBAR CHART GLYPH [NEW — 0, DAVE'S]** — ⛔ **POINTER, NOT THE LIST (`s225-D2`).** ****524 aged · 6 new · 1 STRUCK**** at **`_CARRIES.md` § `residual → #264`** (`carries:residual-264`). ⚠ **PROBE:** `PYTHONPATH=knowledge python3 -c "import _capture_gate as c;print(len(c._carry_items([l for l in open('_CARRIES.md') if '#264:**' in l][0])))"` → **387**.
> **residual (GENERATED #263):** 2c OK (banners 2/2) · 2d OK (deltas 3/3) · 2f OK (strata 1, log #262) — _roll_state.py · 2026-09-09
> 
> **⬛ OPEN WORKLIST — PRESENCE INDEX (28 items, GENERATED). Every open item is named; NO bodies are here — `--fetch gm:DOFIRST`.**
> `0b` ENCODE BEFORE THE WAVE, part-consumed 28 · `0c` NEXT BUILD CANDIDATES · `0d` THE 67 ENACT WAVE, landed · `1` ds-018 C2 follow-through · `2` dv-legend/dv-behaviour CEILING · `3` ds-012(b) gutter-relative plot area · `4` DV-D16 floating growth · `5` Instrument-fit remainder · `6` ds-016, PART-RULED #114 · `7` ds-017, UNRULED · `8` STILL OWED, none superseded · `9` DELEGATION TOPOLOGY · `10` ✅ PER-GATE TEST PLAN · `11` 2c-ROLL / INDEX-VOCAB DEADLOCK · `12` #57 1b DOSSIER · `13` tmp RUNBOOK EXPOSURE, unfixed · `14` ⬛ DAVE'S FOUNDING PRINCIPLE · `15` LEDGER § ★ #59 · `16` UNHOMED PAIR (#78 2c EXIT CHECK) · `17` CHAIN-DIET CUT · `19` type.css 180 dark-mode specificity · `20` ds-029 FIRST IDIOM · `21` PER-THEME COLLISION SWEEP · `22` 89-D2 ruled not enacted, absent from state · `23` ds-025 re-scoped 109, not closed · `24` 110 boot-rent plan · `25` ds-032 enactment, ruled 106 · `26` ✅ boot-drift gate rulings
> **QUEUE — `gm:C1` strands · `gm:C2` ruling batch (Dave's) · `gm:C4` enact-queue.**

---

## ⏱ LATEST DELTA — 2026-09-09 (Wed from `date`) (**#263**, one day, conductor **Fable** + **3 OPUS 5 SUBS** (P, E, A) + this OPUS 5 wrap sub, with lane C run at the conductor's own seat — ★★★ **THE ELEVEN PROPOSED DEFAULTS WERE RULED IN ONE PASS, AND A GATE THAT WAS GRADING NOTHING BECAME THE TENTH RELEASE GATE**)

*(WHY/HOW: `_DECISION-HISTORY/2026-09-09-263-eleven-defaults-and-the-tenth-gate.md` — why a defaulted recommendation has no natural moment to come back, and how one of the eleven rulings exposed a gate that was blocking on stale evidence. Filed report: `notes/_subreports/2026-09-09-263-W-wrap.md`. Lane fragments: `notes/_lanes/263-P-proposed-review.md` · `notes/_lanes/263-E-enact-d7-d10.md` · `notes/_lanes/263-A-enact-d11.md` · `notes/_lanes/263-C-redrive-chart-engine.md`.)*

- ★★★ **THIRTEEN RULINGS, DAVE'S, INSCRIBED — `knowledge/_rulings.json` entries 424–436, the store 423 → 436, each parsing with `by` Dave · `date` 2026-09-09 · `status` ruled.** `s263-D1` … `s263-D11` rule the **eleven PROPOSED defaults** the #261 conductor had defaulted and #262 had left standing: **P-01** the delta glyph ink seat *(governs `Kpi-tile.reference.html` AND `Stat-card.reference.html`)* · **P-02** group-row by type · **P-03** disabled-column alpha · **P-04** adopting `data-apollo-filter-*` · **P-05** the 1400px collapse · **P-06** keeping the Tab-bar name · **P-07** the doormat · **P-08** the 44px legend seat · **P-09** `data-dv-controls` · **P-10** `--check` as a wrap gate · **P-11** retiring the source-text aria arm. **EIGHT ARE STRAIGHT ACCEPTS.** ★ **THE THREE THAT CAME BACK `LATER` WERE RULED ON HIS OWN SENTENCES:** `s263-D7` — *"[p]ark — keep the doormat as a dormant variant of Footer so it stays one component"*, then, asked what to call the state, **"dormant variant"** ⇒ the doormat is neither retired nor live, and `s261-D3`'s *"App footer **for now**"* hedge is discharged · `s263-D8` — *"Is this teh legend for charts? if so leave it at 44 it works fine"* → **"accept"** once it was confirmed as the chart legend, so **the 44px seat stands** · `s263-D9` — *"accept contract-only, it's an interesting idea to wire one legend to multiple charts but not required at this stage"*, an acceptance with its scope cut to the contract and the wider idea parked **in his own words**. Then two more, both arising from enactment rather than from a plan: `s263-D12` — *"whatever is the best standard solution"* — and `s263-D13` — **"yes"**.

- ★★★ **THE DAY: A DEFAULTED LIST GOT A SURFACE, AND THE SURFACE GOT ELEVEN ANSWERS IN ONE PASS.** Lane P built `notes/_PROPOSED-263.html` (50,906 B; generator kept as `notes/_lanes/263-P-build.py`, so the page is regenerable and not hand-edited): eleven cards, each with the **verbatim recommendation**, the reason given, **what the code does today**, the alternative, and an Accept / Reject / Later row with a note field — plus an **export block** rendering one line per ruled item **and a `NOT RULED (n):` line**, which is what let the wrap see what was still open. ⛔ **The lane ruled NOTHING — every row shipped empty and no default was pre-selected.** ⚠ **AND THE PAGE WAS NEVER DRIVEN IN A BROWSER AND NEVER PUBLISHED** (hosted publish refused — the approval card could not be answered from the seat), so the surface Dave read was the file; its behaviour is proved by construction, not by observation.

- ⛔ **A GATE THAT `s263-D11` HAD JUST MADE LOAD-BEARING WAS GRADING NOTHING, AND THE PROOF WAS IN A COMMITTED GREEN REPORT.** `s263-D11` deletes `_validate_snippets.py`'s source-text `requiredAria` arm (with its bite-test — a bite-test for a retired arm is a guaranteed RED — and four docs corrected), leaving the **DOM-driven** arm in `_validate_dataviz.py` as the one gate. Lane A then found **every driven receipt in the repo STALE**: `knowledge/canon/type.css` moved at **`4cc4204`** (#261 lane F2, 10:58) *after* `knowledge/_tests/chart-engine/_receipts.json` was driven at 09:54 (`0420197`, hash `295b2867…`), so `_validate_dataviz.py` read **STALE on all 27 pages** and the new one-gate was **blocking-on-stale**. ★ **AND `knowledge/_DATAVIZ-GATE.md`, committed at `81ce08f` (12:56), reads PASS / 0 STALE — a report written from a tree that did not match HEAD**, the #253 two-lanes-wiping pattern in a new venue. Nothing in the repo compares a committed gate report against the commit it rides on. **Ruling-shaped, HIS, and carried.**

- ✅ **THE RE-DRIVE WAS A MEASUREMENT, NOT A REASSURANCE.** Chromium 151 was installed in the sandbox **without root** — playwright plus `libXdamage.so.1` extracted from the arm64 deb into `~/.local/lib` under `LD_LIBRARY_PATH` — and `knowledge/_drive_chart_engine.py` re-drove **27 pages × 8 theme×mode combos** at **`521a751`**. Result: **0 differing measured fields** against the 09:54 receipts — marks, rows, contrast, rogue hex, gradients, curve and aria all identical, same Chromium build. ⇒ **the type.css change moved nothing a chart measures, and v1.0.9's charts are retroactively clean.** `_validate_dataviz.py` → `✅ DataViz gate passed (15 chart surface file(s))`, 0 STALE, 0 blocking; `requiredAria [driven]` reads n/n present in all 8 combos on every page. ⚠ The STALE arm was proved live **by observation** — it fired on the real tree before the re-drive, and lane A had already driven its RED on a mutant receipt.

- ★★★ **THE TENTH RELEASE GATE (`s263-D13`, `5050b33`).** The dataviz gate was **not** among the nine release gates, which is exactly why nothing caught the staleness at the v1.0.9 cut. Asked whether it should join, Dave said **"yes"**. The enactment is deliberately narrow: `apollo-spider/build-designer-pack.sh --release` now runs a **`--receipts-fresh`** check **after `require_clean`**, **write-nothing** — it REFUSES over stale driven receipts rather than re-driving them, because a release that silently re-drives its own evidence is a release grading its own homework. The selftest drives the **mutant RED and the real control GREEN**; the frozen, audit and CI gates still PASS.

- ✅ **`s263-D12` IS A SCHEMA RULING THAT NOBODY PLANNED, AND IT EXISTS BECAUSE A LANE DECLARED ITS DEVIATION INSTEAD OF HIDING IT.** Enacting D7, lane E found `knowledge/components/meta.schema.json` defines `variants[].items` with `"additionalProperties": false` and `patternProperties {"^\\$": {}}`, so a bare `status` key drove the meta validator RED (measured verbatim before anything was changed). The lane took the legal `$status` route **and said so**. Put to Dave, the answer was *"whatever is the best standard solution"* ⇒ **`variants[].status` is now a first-class enum `active | dormant | deprecated`** and the footer's doormat carries `status: "dormant"` (`27f0f46`). ★ **The `$`-prefix convention is for one-off annotations; a state three components will want is not a one-off.**

- ✅ **`s263-D10` IS ENACTED AND RAN FOR THE FIRST TIME AT THIS WRAP.** `rulings_page_freshness_check(repo)` is wired **first inside `_capture_gate.py::wrap_checks()`, BLOCKING**, calling the renderer's own `check()` **in-process** (no subprocess — the sandbox call-boundary lesson), so one implementation of the freshness rule exists; runbook **step 4d** is new, between 4c and 5, and the step list in its header now reads `… 4, 4b, 4c, 4d, 5, 5b`. The RED arm was driven by hand at lane E. ⛔ **There is NO registered bite-test for it in `knowledge/_tests/`** — declared at birth, carried, not discovered later [[instrument-without-a-consumer]].

- ⚙ **GAUGE — TWO OBJECTS, REPORTED SEPARATELY, NEVER SUMMED OR CONVERTED** [[measure-dont-convert-units]]. ⛔ **The conductor's FILL was NOT MEASURED at the wrap cut** — the brief declares an **ESTIMATE ~65–75K real** and marks it as one, because no `message.usage` was read at that seat. **THROUGHPUT at the wrap seam: 167,102 cl100k-estimate declared by the conductor · 171,698 measured first-hand at this seat** (a moving number read minutes apart; both stand). ⛔ **AND `_checkin.py`'s BUDGET line at this seat reads FILL 155,332 real over 74 turns, boot 69,687, which disagrees with the conductor's own estimate by roughly 2× — RECORDED, NOT RECONCILED**, because this seat cannot prove from inside itself which window that transcript belongs to. **subs 306,253 tokens (n=3)** — P **99,889** · E **117,998** · A **88,366** — **QUOTA, never window FILL**, and never graded against the stop line. ⚠ **THE `s260-D2` DELEGATED stop line 180,000 real STANDS AND CANNOT BE GRADED THIS SESSION** — the FILL it grades was not measured, so the line exists and the reading does not; ⛔ **an ESTIMATE is not put in its place** [[feedback-measuring-tool-must-not-guess]]. **Boot: NOT MEASURED** (no `tiktoken` at the opener; installed after the fact) ⇒ the boot-band series takes a **HOLE for #263** rather than an invented number, and the `--wrap` gate form is **REFUSED for the seventeenth wrap running** on #255's 70,127.

- ⚠ **DECLARED SKIPS AT THIS WRAP, EACH WITH ITS SIZE** (declare-last, and the size is the point): **2e = 0 dated strata rolled**, `## ⬛ DO THIS FIRST` at **10,140 real / 109 raw lines** inside the 120-warn band · **2c/2d/2f ALL RAN CLEAN** (#261 banner 11 ln · #260 ⏱ delta 15 ln + its 2,708-character chain segment · the #262 stratum 14 ln → `notes/_GAUGE-LOG.md` + 2 ln → `_GM-ARCHIVE.md`) · **MEMORY = 0 bytes at this seat, a SEAT LIMIT and the THIRD consecutive one** · **`_CARRIES.md` never read whole — 10.6 MB** · **BOOT not measured** · **`notes/_PROPOSED-263.html` never driven, never published** · no `_build_all.py`, no `_validate_screen.py`, `test_gates.py` NOT RUNNABLE (ENOSPC), no recall plant, no retrieval query.

- ⬛ **STILL HIS, CARRIED FORWARD — RULING-SHAPED AND NOT RULED HERE:** the **Stat card arrow seat** (⚠ `s263-D1`'s `governs` names `Stat-card.reference.html` and the conductor reads P-01's *"Stat card moves with it"* as settling it — ⛔ **Dave has not said so and this seat did not ask him**) · **`/goal` untested** — nothing tests that the pack HOLDS a goal across a session · the **sidebar `chart` line glyph**, hand-baked with its filled twin named `insight-active` · **whether a gate report should be refused at commit when its own tree does not match HEAD** · **#241's midnight-wrap question, now at age 21** · and the **three pre-existing reds** on a pristine repo (footer's two schema reds · `_validate_icons.py`'s 5 UNKNOWN Legend icons · `_build_integrity.py`'s 17 errors / 25 warnings), none caused here and none repaired here. **`_CARRIES.md` § `## residual → #264` is the carry set (`s225-D2`): 524 aged · 6 new · 1 STRUCK**, probe **387**; the banner carries the pointer and the count.


---

## ⬛ OPEN WORK — GENERATED from `_state.json`. **The row count IS the count.**

> **618 items · 519 live · 239 Dave's · 280 mine · 604 carry a stated close condition · **14 UNCONDITIONED**.**
> *Every figure in this block is computed from the store at generation time. No number here was typed by anyone — that is the point of it (#86 measured a typed inventory of "118 markers" against a real ~40; #85's "95 slots / 84 distinct" is not reproducible by any probe in this repo).*
> ⚠ **DECLARED DEBT — 14 inherited items have NO close condition and therefore cannot close.** They are exempt as a FROZEN set (`_state.LEGACY_IDS`, size 19) which may only shrink; a NEW item is refused without one. **Each needs Dave's word — an agent inventing a close condition for his open work is the same overreach as inventing his ruling.**
>
> **DAVE'S (239) — 239 with a ratified `closes_when`, 0 with NONE · MINE (280). IDS, BODIES AND CLOSE CONDITIONS ARE IN THE STORE, NEVER HERE (s227-D2 shape, ids cut `s241-D2`): the counts above are the inventory; the rows are one command away, named on the next line.**
>
> ⚠ store gate: **PASS**. Bodies, conditions and provenance: `python3 knowledge/_state.py`.


---

*(Chain ends. **8,923 tape (cl100k ESTIMATE) — the unit is THE WHOLE FILE**, this generated wrapper included:
the number in this sentence is the size of the file containing it, held exact by a fixed point.
`GOOD-MORNING.md` is 34,132 tape (cl100k ESTIMATE) and stays whole for retrieval; you have paid for
26% of it. Under 40% is this generator's own floor — above it, the wrapper is
carrying more than the slice, and `--selftest` says so by name.)*
