<!-- GENERATED by knowledge/_gen_chain.py — DO NOT HAND-EDIT.
     Edit GOOD-MORNING.md / _LIVE-STATE.md and regenerate; `--check` is a build step. -->

# The read chain — everything a cold session must read, and nothing else

> **This file is GENERATED and it is the whole contract.** It carries `GOOD-MORNING.md`'s header
> and ★ LATEST banner, plus `_LIVE-STATE.md`'s ⏱ LATEST delta — the three things GM-D7-am names
> (CUT #33 on Dave's ruling). **If you have read this file, you have read the chain.**
>
> ⚠ **Do NOT now open `GOOD-MORNING.md` to "check".** It is 28,194 tape and 82% of it is
> retrieval surface you are not meant to pay for at boot. That reflex is the thing this file
> exists to stop — five sessions called the chain CUT and every one of them paid full price.
>
> ★ **Everything else is RETRIEVAL, never a reading list:**
> `python3 knowledge/_memento_search.py "<q>"` → `--fetch <id>`. §A orientation is `gm:A`
> (a router with 11 children); the queue is `gm:C1`…; the archives, briefs, runbooks and ledgers
> are all indexed. **Ask for what you need; do not read a file to find out whether you need it.**

---

> **YOU ARE #81. TITLE THIS CHAT →** `Apollo - #81: rule the tokenizer-unit fix (A/B/C/D), then headroom before the legend wave (read _CHAIN.md ONLY)`
> *(read it back in your opener — the chat half of the ritual is ungateable, which is why this line is FIRST)*

---

<!-- ⛔ STOP. If you are a cold session, you are in the wrong file and you pay for ALL of it.
     Read `_CHAIN.md` instead — it is GENERATED from this file's header + ★ LATEST banner and
     `_LIVE-STATE.md`'s ⏱ LATEST delta, which is the ENTIRE contract (GM-D7-am, CUT #33).
     This file stays whole for RETRIEVAL: `_memento_search.py "<q>"` → `--fetch <id>`.
     ⚠ Reading on from here is not thoroughness — it is the exact overspend #41 measured. -->

# Good morning, Dave ☕

> **size:** GM **28.2K tape** · §A **4.4K tape (EXEMPT)** · LS **18.5K tape** · **measured #80 after every roll, edit and correction landed, then ITERATED TO A FIXED POINT with the stamp counted in — ⚠ #79's was measured PRE-STAMP and claimed 26.7K tape against a committed 27,171 tape.** ⛔ **DECLARED, NOT SMOOTHED: GM 27.2K tape → 28.2K tape, and the READ CHAIN GREW TOO — figure in `_CHAIN.md`'s own footer (its ONE home, retired from here #45), delta in the #80 stratum. **The chain is the half a cold session actually pays, and both the correction and the finding land in it.** SECOND CONSECUTIVE GROWTH SESSION ⇒ THE GRUNGE SIGNAL HAS FIRED AND THE CONTRACT RE-OPENS, on #79's own terms.** Attributed by section, not excused: **HDR +144** — the tiktoken clause was FALSE and sitting in the read chain (+89), corrected at source, plus this longer stamp (+55) · **DOFIRST +126 · §C·4 +167** — the 2c EXIT CHECK, and two of #78's items had NO standing home at all · **§C·4 +168** — striking a wrong standing claim at its source · **★ LATEST +309** — the re-discovery finding, which exists to stop a false novelty claim reaching the chain · **PRIOR +45**, not authored here (#79's banner replacing #78's) · **STRATA −119**, my own addition, shaved. **No live record was touched to make this number look better.** *(LS 18.0K tape → 18.5K tape with the #77 delta rolled. The roll-residual line stays GENERATED (`_roll_state.py`, #77-D1/D3); this stamp stays authored + gate-measured.)* · **prior #79** *(as stamped 25.4K tape → 26.7K tape; as MEASURED 25.7K tape → 27.2K tape.)* *(older segments → `notes/_GAUGE-LOG.md`.)*
> **STATE: ★★ THE READ CHAIN IS CUT (#33) and it HELD — sixth session running.** You are reading the contract; it is four paragraphs below the banners. §A and §C are STILL IN THIS FILE, reached by `_memento_search.py`, not by reading top-to-bottom. ⛔ **BUILD CLAIM CORRECTED AT SOURCE AGAIN #61 — AND THIS TIME THE PREMISE DIED, NOT THE WORDING.** #47's honest form (*"all 75 accounted for, none gate-failing"*) was derived from the build REACHING step 73 before the sandbox cap killed it at `EXIT=124`. ★★ **That premise is dead: the build ABORTS AT STEP 10 OF 75 IN 7 SECONDS — MEASURED #61 by running it, not traced.** `affe15d` (#60, 12:29 on 07-31) added `check_budgets`' `TITLE_LINE_RE` and did not teach `_gm_fixture` to satisfy it ⇒ **9 selftest failures, ONE cause**; `_build_all.py:52` routes `--selftest` through the catch-all abort branch, so it died at **step 8**. Parent `ee0db5f`: **0 failures.** #61 fixed the fixture (9→0) and the build advanced to **step 10**, where `_gm_usage.py --selftest` REFUSES an honest `⛔ NOT CAPTURED — UNMEASURED` `section-usage` line written at **#55's** wrap (`9ca96e1`) — two correct behaviours colliding: nobody taught the format that UNMEASURED is a legal value. ⇒ ✅ **VERDICT RESTORED #62 — ALL 75 STEPS ASKED AND GREEN**, the first full verdict since 12:29 on 07-31: [10] fixed (UNMEASURED is a LEGAL testimony value now, scoped to the exact quoted form, 9 bites both directions) · [66] regenerated (the index was STALE against #61's own commits — the write-then-check pairing meant CI could never see it) · the 50 never-asked mutating steps asked via `_build_survey.py --range` chunks on one tree (`ace3ed3` + `18c7789`). ⚠ **Residual, DECLARED: a single-process `_build_all.py` run is SANDBOX-IMPOSSIBLE (~49s measured vs the ~45s call kill) — step-level equivalence is exact (`:214` is the same `subprocess.run` invocation), and CI delivers the single-process verdict on push.** ⚠ **DO NOT RUN `_build_all.py` TO CHECK — an abort leaves the tree PARTIALLY REGENERATED: it gutted 33 `knowledge/compliance/` files at #61, and a reconcile waved them through as "just derived output" (restored `d7cd152`).** *(★★ #62 REATTRIBUTED THE MECHANISM: step [1] completing GREEN strips those 33 files too — the stripped state is the DOCUMENTED mid-build intermediate of a non-atomic build (`_build_all.py` docstring lines 5–21: step 1 rewrites wholesale, verification{} + external refs are rebuilt by LATER steps), healed only by a COMPLETE pass. The abort never caused the gutting; it left the window open. The warning stands — any PARTIAL run strands the tree there.)* ★ **The lesson is #47's own, turned on #47: a carefully derived rule outlives the premise it was derived from, and only the PREMISE is worth re-verifying.** ★★ **#38 finished #37's wrap, then found WHY every wrap grinds: the compactable cap is aimed at cold-start cost, and #33 cut the chain out from under it — ~9,000 of the 11,955 tape it governs is never paid at boot.** Full finding + the lane divvy: **`notes/_briefs/2026-07-29-cap-repoint-and-lane-divvy-brief.md`.** ✅ **#37 DOSSIER WRITTEN #44** → `_DECISION-HISTORY/2026-07-29-the-37-dossier.md`.
> ⚠ **`set_content()` IS BANNED IN RENDER-PROOFS** — drops `type.css` silently (14px→16px) and `document.fonts.check` passes anyway; use `goto("file://…")` (#29, measured) · **ds-020 is a gate-enforced obligation** (the DV-D02-A scatter waiver names it as clears-when) · **ritual step 2g: rebuild `_memento-index.json` LAST** — `index_freshness_check` is BLOCKING and compares CONTENT, never mtime (runbook has the body).
> **★ PRICE IN REAL TOKENS — THE UNIT IS `_gauge_tokens.py`, NOT A PERCENTAGE (#56, Dave's).** **amber 160,000 · working 200,000 (DAVE'S — the line jobs are priced against) · hard 256,000 (SOURCED).** Unit = the FULL price (boot + job + wrap); ⚠ **a DECLARED gap passes, a SILENT one fails** — that asymmetry is the mechanism. ⛔ **CORRECTED AT SOURCE #58 — this line taught the RETIRED `(45, 60, 63)` percentage band as "live in code, price against it", six sessions after #56 replaced it (*"the band was REPLACED, not CONVERTED"*), and the header is IN THE READ CHAIN, so every cold session was being handed the wrong unit first.** ★ **MEASURED, not assumed:** `_capture_gate.py:135–137` still defines `BAND_FLOOR/HARD_STOP/MARKED_MAX = 45/60/63`, `check_preflight` (lines 899–994) still implements the % path, and the selftest at **:2543** still pins the triple — but the live wrap emits **no band line**, because it matches on the stamp form and #57's stamp is in tokens. ⇒ **the % path is DORMANT, not wrong, and it was the PROSE that was stale.** ⬛ **FORKED TO DAVE, not decided here: retire the % path in code, or keep it as a pinned known-gap?** Deleting a pinned ruling of his is not an agent's move. ⚠ **THE FLOOR IS PART-UNKNOWN = `ds-025`** — disk half measured (**17,810 tape / ~14.0 pts**, #37), harness half unreachable ⇒ **say which half of your fill is measured.** ⛔ **CORRECTED AT SOURCE #80 — this clause read *"the gate SILENTLY ESTIMATES without it, UNDER-reporting by 414 tape"*, which #79-D1 made FALSE, and it sat IN THE READ CHAIN.** ⚠ **`pip install tiktoken --break-system-packages` FIRST in a cold sandbox:** the gauge now **REFUSES, loud and named** (`MeasurementRefused`), the paired gate reports ONE named failure, and `_build_all.py`'s P5 step is **ABORT as of #80** ⇒ without it there is no build. ★ **READ THE CHAIN ONLY.** ★ **Repo-state claims are verified against `git log` / a real run, never a banner** (#29→#38: ten consecutive).
> ⚠ **Render sandbox is FRESH every session** (~4 calls — price it) · **retrieval FIRST:** `_memento_search.py "<q>"` → `--fetch <id>` (⚠ **it served a two-session-old record until #32 — if an answer looks stale, check the index**) · **wrap moves RUN THROUGH `_gm_move.py`** (⚠ **banner headings are BLOCKQUOTED — anchor `> ## ★ PRIOR …`**) · **PACE PANEL: ask at the opener, no gate can see it** — Dave has declined a fresh one twice and put Fable on the table for judgment work; ✅ **THE "BEHIND PACE ⇒ MORE WINDOWS" CLAUSE IS RETIRED — RULED #60: multi-window is settled, two use cases (quota catch-up AND production crank), Claude offers/Dave vetoes, cap 3 revisable. `notes/_MEMENTO-DECISIONS.md` § ★ #60-D1.** It stood from #48; at #57 he said the opposite — *"we are behind, but I'm less comfortable with multiple windows, too much to decide and consume"* ⇒ **ONE window with more delegation.** ★ **The clause optimised the wrong quantity: it treated TOKENS as the constraint when DAVE'S DECISION LOAD is what binds.** ⚠ **Reflected back #57, and #58's opener did NOT settle it** (asked as the first question; he answered pace with *"no constraint, lets just fix the problems"*). Standing home + status: §C·4.
> *(**RENAME is delivered IN CHAT at wrap, not here — RULED #28, ENACTED in runbook step 4b by #30.**)*
> **TITLE THE NEXT CHAT →** `Apollo - #81: rule the tokenizer-unit fix (A/B/C/D), then headroom before the legend wave (read _CHAIN.md ONLY)`
> *(Titles are LABELS — role comes from Dave's opener line. Bands: the ONLY copy is
> `_RUNBOOK-context-gauge.md` § ★ THE FLOOR IS MEASURED — grep it, never recall it.)*
> ## ★ LATEST — 2026-08-02 (Sun **#80**, OPUS 5 solo Cowork conductor + 2 peer subs, Dave live, ONE window by his call — ✅ **#79-D1 BUILT: THE GAUGE REFUSES, PAIRED AND MUTATION-TESTED** · ✅ **P5 PROMOTED ADVISORY → ABORT (Dave)** · ⛔ **"TAPE" IS AN OPENAI NUMBER — EVERY SIZE FIGURE IN THIS PROJECT UNDER-REPORTS BY ~36%, MEASURED** · ★ **DAVE RULED THE METHOD: *"I find '5 whys' is better than patching what we have"***)
> - ✅ **#79-D1 ENACTED — AND THE FIX IS THE PAIR, NOT THE RAISE.** `knowledge/_gauge_tokens.py`: new `MeasurementRefused(RuntimeError)`; `count()`'s `len(text)//4` 'crude-estimate' fallback now raises loud + named. ★ **Deliberately NOT `SystemExit`, against this repo's own precedent** (`_context_gauge.py`/`_checkin.py` use it) — those are CLI entry points; `count()` is a LIBRARY function called inside a 39+-check gate, and `SystemExit` is a `BaseException`, so `_arm()`'s `except Exception` would not have caught it and the whole selftest would have died. PAIRED half at `knowledge/_capture_gate.py:2782`, the #53 floor call that was bare at `:2774`: it catches and reports ONE named failure — **refusal is recorded as a FAILURE, because an UNMEASURABLE floor is not a cleared floor.** New `selftest_gauge_refusal_seam()` wired into `selftest()`. **MUTATION-TESTED ×3, each biting with a DISTINCT named failure, revert green:** handler removed ⇒ *"CRASHED instead of reporting"* · handler swallows ⇒ *"NO failure naming it"* · re-parented to `BaseException` ⇒ the structural guard fires. [[a-crash-is-not-a-fail]]
> - ✅ **P5 PROMOTED ADVISORY → ABORT — Dave's word, enacted** in `knowledge/_build_all.py`; two stale comment blocks corrected at source in the same pass. **Gates RE-RUN AT THE WRAP, not quoted from the build:** `_capture_gate.py` build **59 in scope · 0 fail · 0 warn** · `_capture_gate.py --selftest` **EXIT=0** · `_gauge_tokens.py --selftest` **EXIT=0** · `_build_all.py --selftest` **EXIT=0** (85 steps, mutation control red).
> - ⛔⛔ **DAVE'S QUESTION REOPENED THE UNIT → `notes/2026-08-02-the-tokenizer-unit-five-whys.md`. READ IT — it carries a CORRECTION BLOCK the banner summarises, do not reconstruct either.** He asked *"why does tiktoken keep failing? is this this the openai method? are there others doesn't anthropic have its own?"*, then displaced the patch I had started: *"I find '5 whys' is better than patching what we have."* **MEASURED #80:** `_CHAIN.md` **5,761 cl100k / 9,079 REAL (×1.576)** · `GOOD-MORNING.md` **27,171 cl100k / 42,435 REAL (×1.562)** ⇒ **every "tape" figure under-reports by ~36%, against a budget (160/200/256K) denominated in REAL tokens.** ⛔ **AND THE WRAP CAUGHT ITS OWN SESSION OUT — none of that is NEW. #53 measured it (×1.559, five registers, `knowledge/_measure_tokenizer.py`, ledger `notes/_MEMENTO-DECISIONS.md:1716–1785`) and #54 RULED IT (Dave): ONE unit, real tokens, `cl100k` demoted to a labelled estimator, *"never a unit a cap is stated in"* — with *"NOT ENACTED THIS WINDOW, the three homes are untouched."* #80 re-derived a settled ruling from scratch and nearly filed it as a discovery.** [[premise-ages-faster-than-rule]]
> - ★★★ **SO THE REAL FINDING IS A MEMENTO FINDING, AND IT IS SHARPER THAN THE RATIO: A RULED, INSCRIBED MEASUREMENT DID NOT REACH THE SESSION THAT NEEDED IT, 27 SESSIONS LATER.** It is in the ledger, in `_DECISION-HISTORY/2026-07-30-the-gauge-re-denomination.md`, in `notes/_GAUGE-LOG.md:461`, and in a **re-runnable instrument** — and #77's own periphery inventory already lists `_measure_tokenizer.py` as reported **"nowhere", 0 consumers**. **An instrument ships WITH ITS READER; a measurement nothing re-reads decays into a rediscovery.** [[instrument-without-a-consumer]]
> - ★★ **WHAT IS GENUINELY NEW #80 — THE ROOT CAUSE, and it corrects a standing §C·4 claim.** `measurement_degraded()` (`_capture_gate.py:1324–1341`) exists, is wired and is pinned against drift — but `measure_tokens()` can return only `"tiktoken cl100k_base"` or `"bytes/N ESTIMATE"`, so it asks *"is this an estimate?"* and **cl100k answers *no, healthy*. The vocabulary has no word for REAL.** ⇒ §C·4's *"what remains is the three-homes WRITE-UP, smaller than the title implies"* is **WRONG and struck at source: it is a CODE change, because the producer still emits cl100k and the guard cannot see it.** Also new: **17 discard sites** (a first count of 11 came off a TRUNCATED grep — [[unmatched-grep-is-not-an-absence]] inside a note about wrong numbers — and the sharper half is that **8 sites DO keep the method**). [[scope-blindness-gate-vocabulary]]
> - ⬛ **DAVE'S AT #81 — THE ENACTMENT SHAPE ONLY. ⚠ THE UNIT ITSELF IS NOT REOPENED: he ruled it at #54 and an agent does not re-open his ruling.** Four shapes for the `ds-021` three-homes enactment, owed since #54 and standing in §C·4 + `_DS-IMPROVEMENTS.md:1422`: (A) unit-as-TYPE · (B) ONE measuring authority · (C) a CROSS-INSTRUMENT gate — **cheapest, and the only one that catches the NEXT one** · (D) calibrate and keep cl100k, **argued against** (a conversion where a measurement is available; ×1.79 prose vs ×1.56 here). ⚠ Whichever lands, the re-measurement touches the GM size stamps, `ds-025`'s floor, and amber/working's relationship to them.
> - ⚠ **AND A SECOND MEASURE-THE-ARTEFACT SLIP, DECLARED: the #79 `size:` stamp was measured PRE-STAMP** — it claims GM 26.7K where the file as committed at `668857d` measures **27,171 tape** (1.8% out, passes the drift check). This wrap iterates to a TRUE fixed point instead.
> - ✅ **THE OWED #79 DOSSIER, WRITTEN** → `_DECISION-HISTORY/2026-08-02-the-79-dossier.md` (peer sub). ★ **It declared SIX claims it could not reproduce** — including that `_CHAIN.md`'s *"+1,247 ln"* measures **1,228/130** by `git show --numstat`, and that *"an inventory written five sessions ago"* is actually **#77 (two sessions)** / provenance **#65 (fourteen)**. That is what the delegation regime is for.
> - **pricing — ⚠ AN HONEST UNKNOWN, AND IT IS THE FINDING BITING THE SESSION THAT FOUND IT.** The wrap was called by the FILL, not a natural end. `_checkin.py` read **154,330 tape at the close — conversation half only, boot half UNMEASURED (`ds-025` item 1 STANDS)** — and that reading is **in the very unit this session proved wrong.** The REAL figure is materially higher and is **NOT converted here, because converting is the defect just diagnosed.** [[measure-dont-convert-units]]
> - **residual (GENERATED #80):** 2c OK (banners 2/2) · 2d OK (deltas 3/3) · 2f OK (strata 1, log #79) — _roll_state.py · 2026-08-02
> - **⬛ DAVE'S AT THE #81 OPENER:** (a) **rule the `ds-021` ENACTMENT SHAPE — A/B/C/D** (the UNIT is already yours, ruled #54; only the shape is open) · (b) **headroom before any legend change** — 78 B free, RE-MEASURED #80, DO-FIRST 2 · (c) the unregistered-source gate + bar-motion half (DO-FIRST 16) · (d) D3 lockup detail (`_DATAVIZ-DECISIONS.md:798`) · (e) trigger index (§C·2) · (f) radios live-test (`_REVIEW-SIGNOFF.md`) · (g) the six `_decision-graph.json` edge types, unruled since #71.
>
> **⬛ OPEN WORKLIST — PRESENCE INDEX (19 items, GENERATED). Every open item is named; NO bodies are here — `--fetch gm:DOFIRST`.**
> `0b` ★★ ENCODE BEFORE THE WAVE · `0c` NEXT BUILD CANDIDATES · `0d` ✅ THE #67 ENACT WAVE · `1` ds-018 C2 follow-through · `2` dv-legend/dv-behaviour CEILING · `3` ds-012(b) gutter-relative plot area · `4` DV-D16 floating growth · `5` Instrument-fit remainder · `6` ds-016, UNRULED · `7` ds-017, UNRULED · `8` STILL OWED, unchanged, none superseded · `9` DELEGATION TOPOLOGY, UNSCOPED · `10` ✅ PER-GATE TEST PLAN · `11` THE 2c-ROLL / INDEX-VOCABULARY DEADLOCK · `12` THE #57 1b DOSSIER · `13` /tmp RUNBOOK EXPOSURE, UNFIXED · `14` ⬛ DAVE'S FOUNDING PRINCIPLE · `15` LEDGER § ★ #59 · `16` UNHOMED PAIR, copied up at the #78 2c EXIT…
> **QUEUE — `gm:C1` strands · `gm:C2` ruling batch (Dave's) · `gm:C4` enact-queue.**

---

## ⏱ LATEST DELTA — 2026-08-02 (Sun **#80**, OPUS 5 solo Cowork conductor + 2 peer subs, Dave live — ✅ **#79-D1 BUILT: THE GAUGE REFUSES, PAIRED + MUTATION-TESTED ×3** · ✅ **P5 ADVISORY → ABORT (Dave)** · ⛔ **"TAPE" IS AN OPENAI UNIT — EVERY SIZE FIGURE UNDER-REPORTS BY ~36%, MEASURED** · ★ **DAVE: *"I find '5 whys' is better than patching what we have"***)

- ✅ **#79-D1 ENACTED, AND THE DELIVERABLE IS THE PAIR.** `_gauge_tokens.py`: `MeasurementRefused(RuntimeError)`, `count()`'s `len(text)//4` fallback now raises loud + named. **NOT `SystemExit`** despite the `_context_gauge.py`/`_checkin.py` precedent — those are CLI entry points; `count()` is a library function inside a 39+-check gate, and a `BaseException` would slip `_arm()`'s `except Exception` and kill the selftest. Paired handler at `_capture_gate.py:2782` (the #53 floor call, bare at `:2774`) reports ONE named failure; **refusal is a FAILURE — an unmeasurable floor is not a cleared floor.** New `selftest_gauge_refusal_seam()`. Mutations ×3, each a distinct named bite, revert green. **P5 promoted to ABORT in `_build_all.py` (Dave's word).** Gates re-run at the wrap: build 59/0/0 · all three selftests EXIT=0 (85 build steps).
- ⛔⛔ **THE UNIT IS WRONG — AND #53 ALREADY MEASURED IT, #54 ALREADY RULED IT.** → `notes/2026-08-02-the-tokenizer-unit-five-whys.md` (**read its CORRECTION BLOCK first**). #80 measured `_CHAIN.md` 5,761 cl100k / **9,079 REAL** · `GOOD-MORNING.md` 27,171 cl100k / **42,435 REAL** (×1.57) ⇒ every tape-vs-budget comparison is dimensionally invalid. ⛔ **None of that is new:** #53 measured ×1.559 with `knowledge/_measure_tokenizer.py` (ledger `:1716–1785`, dossier `_DECISION-HISTORY/2026-07-30-the-gauge-re-denomination.md`), and **#54 RULED it — ONE unit, real tokens, `cl100k` a labelled estimator, *"never a unit a cap is stated in"* — declaring "NOT ENACTED, the three homes are untouched."** They are still untouched, 26 sessions on. ★★ **The real finding is a Memento one: a RULED, INSCRIBED measurement did not reach the session that needed it** — and #77's periphery inventory had already flagged `_measure_tokenizer.py` as reported "nowhere", 0 consumers. [[instrument-without-a-consumer]]
- ★★ **GENUINELY NEW #80 — THE ROOT CAUSE, and it corrects §C·4 at source.** `measurement_degraded()` (`_capture_gate.py:1324–1341`) is present, wired, pinned — and **blind**: `measure_tokens()` returns only `cl100k` or `bytes/N ESTIMATE`, so it asks *"is this an estimate?"* and cl100k answers *no, healthy*. **The vocabulary has no word for REAL.** ⇒ §C·4's *"what remains is the three-homes WRITE-UP"* is STRUCK: it is a CODE change. Also new: 17 discard sites (first count of 11 came off a truncated grep), 8 of which DO keep the method.
- ⬛ **DAVE'S AT #81 — THE ENACTMENT SHAPE ONLY; ⚠ the UNIT is HIS and is NOT reopened (#54):** (A) unit-as-type · (B) one measuring authority · (C) cross-instrument gate (cheapest, catches the next one) · (D) calibrate and keep cl100k (argued against). **Not shipped because the re-measurement lands on the GM stamps, `ds-025`'s floor and the amber line.**
- ✅ **The owed #79 dossier written** (`_DECISION-HISTORY/2026-08-02-the-79-dossier.md`) and it **declared six claims it could not reproduce**, incl. `_CHAIN.md`'s "+1,247 ln" measuring 1,228/130 by `git show --numstat`.
- ⚠ **Pricing is an HONEST UNKNOWN:** `_checkin.py` read 154,330 **tape** at the close (conversation half only, boot half unmeasured) — in the unit this session proved wrong. **Not converted, because converting is the defect.**
- ⬛ **Dave's at #81:** rule the `ds-021` ENACTMENT SHAPE (A/B/C/D — the unit itself is already his, #54) · headroom before any legend change (78 B free, re-measured) · unregistered-source gate + bar motion · D3 lockup detail · trigger index · radios live-test · the six unruled edge types.


---

*(Chain ends. **6,816 tape — the unit is THE WHOLE FILE**, this generated wrapper included:
the number in this sentence is the size of the file containing it, held exact by a fixed point.
`GOOD-MORNING.md` is 28,194 tape and stays whole for retrieval; you have paid for
24% of it, and that is the intended figure, not a shortfall.)*
