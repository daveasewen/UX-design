<!-- GENERATED by knowledge/_gen_chain.py — DO NOT HAND-EDIT.
     Edit GOOD-MORNING.md / _LIVE-STATE.md and regenerate; `--check` is a build step. -->

# The read chain — everything a cold session must read, and nothing else

> **This file is GENERATED and it is the whole contract.** It carries `GOOD-MORNING.md`'s header
> and ★ LATEST banner, plus `_LIVE-STATE.md`'s ⏱ LATEST delta — the three things GM-D7-am names
> (CUT #33 on Dave's ruling). **If you have read this file, you have read the chain.**
>
> ⚠ **Do NOT now open `GOOD-MORNING.md` to "check".** It is 34,904 tape (cl100k ESTIMATE); this file already
> carries the part of it a cold session needs, and the rest is retrieval surface you are not
> meant to pay for at boot. That reflex is the thing this file
> exists to stop — five sessions called the chain CUT and every one of them paid full price.
>
> ★ **Everything else is RETRIEVAL, never a reading list:**
> `python3 knowledge/_memento_search.py "<q>"` → `--fetch <id>`. §A orientation is `gm:A`; the queue is `gm:C1`…; the archives, briefs, runbooks and ledgers are all indexed.
> **Ask for what you need; do not read a file to find out whether you need it.**

---

> **YOU ARE #279. TITLE THIS CHAT →** `Apollo - #279: land the icons, then the constitution wave — s277-d4..d13`
> *(read it back in your opener — the chat half of the ritual is ungateable, which is why this line is FIRST)*

---

<!-- ⛔ STOP. If you are a cold session, you are in the wrong file and you pay for ALL of it.
     Read `_CHAIN.md` instead — it is GENERATED from this file's header + ★ LATEST banner and
     `_LIVE-STATE.md`'s ⏱ LATEST delta, which is the ENTIRE contract (GM-D7-am, CUT #33).
     This file stays whole for RETRIEVAL: `_memento_search.py "<q>"` → `--fetch <id>`.
     ⚠ Reading on from here is not thoroughness — it is the exact overspend #41 measured. -->

# Good morning, Dave ☕

> ⚠ **WRAP DATE SPLIT — SESSION 2026-09-02, COMMIT 2026-09-03.** #241's ritual was authored 09-02 and steps 1→4c landed that night; the commit did not, and step 5 was finished the next morning. ⛔ **No key, filename or stamp was re-dated to match the commit** — that would be the T-D12 false inscription in reverse; this line carries the commit date so the gate's `is not today` check grades a true statement. Detail + the ruling-shaped question in `_LIVE-STATE.md`'s header block.
> ⚠ **WRAP DATE SPLIT, SECOND OCCURRENCE — SESSION 2026-09-05, RITUAL + COMMIT 2026-09-06.** #248's conductor cut the wrap brief at 183,983 real FILL on the evening of 09-05; the delegated wrap sub ran every step the next morning. ⛔ **No key, filename or stamp was re-dated to match the commit** — the #241 shape, by ADDITION (the #241 line above stands; its ruling-shaped question, what a midnight-spanning wrap should stamp, is still Dave's at age 7). This line carries the commit date so the gate's `is not today` check grades a true statement. Detail in `_LIVE-STATE.md`'s header block.
> ⚠ **WRAP DATE SPLIT, THIRD OCCURRENCE — SESSION OPENED 2026-09-08 EVENING, RITUAL + COMMIT 2026-09-09.** #261's R and D3 lanes committed on the evening of 09-08 (22:11–22:44); the design wave, the revision seats and this ritual all ran on 09-09. ⛔ **No key, filename or stamp was re-dated to match the commit** — the eleven filed reports keep their `2026-09-08-261-*` stems, and each of the nine `s261-D*` rulings carries in its own `evidence` the sentence saying the hour was not recorded at the wrap seat, so the date it carries is the COMMIT date and not a claim about the hour. The #241 shape by ADDITION: both lines above stand, and #241's ruling-shaped question — what a midnight-spanning wrap should stamp — is still Dave's, now at age 20. Detail in `_LIVE-STATE.md`'s ⏱ LATEST DELTA.
> ⚠ **WRAP DATE SPLIT, FOURTH OCCURRENCE ON THIS RUN — SESSION OPENED 2026-09-15 EVENING, WORK + RITUAL + COMMIT 2026-09-16.** #277 opened on the evening of 09-15 and six of its commits carry that date (`f84eb78` · `8c80aa2` · `d84eb72` · `66fdc49` · `81e845a` · `ca294b4`); the three exports, the thirteen rulings 590 → 603, the charts enactment and this ritual are all 09-16, seventeen commits. ⛔ **No key, filename, ruling id or stamp was re-dated to match the ritual** — the #241 shape by ADDITION (the three lines above stand verbatim), and this line carries the ritual's date so the gate's `is not today` check grades a true statement. #241's ruling-shaped question — what a midnight-spanning wrap should stamp — is still Dave's, now at age 36. Detail in `_LIVE-STATE.md`'s ⏱ LATEST DELTA.

> **size:** GM **34.7K tape** (34,733 exact — whole-file `tiktoken cl100k_base`, the figure this stamp is graded against at 10% tolerance) · §A **7.0K tape** (6,957 real by `_gm_usage.py`) — ✅ **THE §A BYTE-IDENTITY PROBE WAS RUN AT THIS SEAT AND §A IS BYTE-IDENTICAL TO ITS STATE AT THE #272…#277 WRAPS** (`sha256 4311cce4…` over 198 lines between the `§A` and `§C` markers, the gate's OWN pinned probe shape `_capture_gate.section_a_digest(lines, spans)`, returning exactly what all six of those stamps recorded — so **§A has not moved in ten sessions**; the gate reports it EXEMPT by ruling: measured and reported, never charged). ⚠ **THE TWO-PROBE-SHAPE QUESTION #272 RAISED IS STILL NOT SETTLED AND IS NOT SETTLED HERE:** #269/#270/#271 all recorded `sha256 b9aef5f5…` over 169 lines, #272…#277 and this stamp all record `4311cce4…` over 198; two probe shapes, not a change in §A, and **which shape the stamp should quote is ruling-shaped and remains Dave's.** · LS **64.9K tape** (64,900 real) · corpus GM+LS **99.6K tape** (99,633 real, the RETRIEVAL surface, not the chain) · ⛔ **the chain's own size is NOT copied here — RETIRED #45; its ONE home is `_CHAIN.md`'s generated footer, exact by construction and `--check`-blocked** · measured 2026-09-16 at the #278 wrap, DECLARE-LAST — after 2c, 2d and 2f, and after the new stratum, so it grades the files this wrap actually leaves behind. ✅ **THE DELTA IS THE FINDING, AND THIS TIME BOTH ROLLS PAID FOR THEIR REPLACEMENT — THE FIRST WRAP IN THREE TO DO SO:** against #277's stamp (**36,278 / 66,808 / 103,086 tape**) GM is **1,545 tape DOWN**, LS **1,908 DOWN**, corpus **3,453 DOWN**. ⛔ **AND THE CAUSE IS THE SESSION, NOT A NEW DISCIPLINE, WHICH IS WHY IT IS NOT READ AS AN IMPROVEMENT:** #278 was HOUSEKEEPING — one ruling, no lanes, no graph work — so the #276 banner (12 lines) and the #275 delta (26 lines) that rolled OUT were both larger than the #278 banner and delta that came IN; a session with thirteen rulings and nineteen lanes costs more tape to record than a session with one and none, and that is arithmetic in the other direction, not sprawl reversed. ✅ **THE NEW ★ LATEST BANNER MEASURES 1,184 tape AGAINST THE `s241-D2` CAP OF 1,200, ON 8 SUBSTANTIVE LINES AGAINST THE SAME RULING'S 10-LINE CLAUSE — 211 tape and 2 lines of headroom, , and `_gm_usage.py --sizes` reads LATEST:989 independently.** ⛔ **THE HEADROOM IS THE SESSION'S, NOT THE IDIOM'S, AND #272…#277's STANDING WARNING IS NOT WITHDRAWN BY IT:** five consecutive wraps recorded that the next one had no room in this idiom, #275 spent five compression passes, #276 eight and #277 eleven to land on 1,199 — this banner is short because the DAY was short, and the next graph-work wrap inherits the same wall. ⛔ **NEITHER FILE SIZE IS A TRIM ORDER; all of these are measurements, and what to do about them is Dave's.**
> **STATE: ★★ THE READ CHAIN IS CUT (#33) and it HELD.** *(the session-count ordinal is DELETED per s178-D1 enacting dream-P1 — stale twice ⇒ generate, never re-stamp)* You are reading the contract. §A and §C are STILL IN THIS FILE, reached by `_memento_search.py`, not by reading top-to-bottom. ⛔ **BUILD VERDICT: 75 of 144 steps green (#62, `18c7789`) — 69 steps have NEVER been in a green verdict.** Both counts GENERATED from `_build_all.py`'s AST at each end; the shortfall is computed (`s125-D1`). A single-process `_build_all.py` run is SANDBOX-IMPOSSIBLE (~49s vs the ~45s call kill); CI delivers the single-process verdict on push. ⚠ **DO NOT RUN `_build_all.py` TO CHECK — ANY PARTIAL run strands the tree in the documented mid-build intermediate** (step 1 rewrites wholesale; healed only by a COMPLETE pass — docstring lines 5–21). ★ **Only the PREMISE of a derived rule is worth re-verifying** (#47's lesson, turned on #47). *(The #47/#61/#62 build-claim archaeology + the #38 cap-repoint finding are HOMED at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET and `notes/_briefs/2026-07-29-cap-repoint-and-lane-divvy-brief.md`.)*
> ⚠ **`set_content()` IS BANNED IN RENDER-PROOFS** — drops `type.css` silently (14px→16px) and `document.fonts.check` passes anyway; use `goto("file://…")` (#29, measured) · **ds-020 is a gate-enforced obligation** (the DV-D02-A scatter waiver names it as clears-when) · **ritual step 2g: rebuild `_memento-index.json` LAST** — `index_freshness_check` is BLOCKING and compares CONTENT, never mtime (runbook has the body).
> **★ PRICE IN REAL TOKENS — THE UNIT IS `_gauge_tokens.py`, NOT A PERCENTAGE (#56, Dave's).** **amber 160,000 (PICKED) · working 200,000 (SOURCED) · hard 256,000 (SOURCED)** — provenance per `_gauge_tokens.py:63–83`, the authority [[read-chain-is-where-staleness-is-free]]. Unit = the FULL price (boot + job + wrap); ⚠ **a DECLARED gap passes, a SILENT one fails** — that asymmetry is the mechanism. The retired 45/60/63 % band is DORMANT in code, pinned — retire-or-pin = `G8`, Dave's. ⚠ **THE FLOOR IS MEASURED = `ds-025` RE-SCOPED #109 — first turn 65,400±1,400 real (n=5) + `_CHAIN.md` 10,499 additive = 75,899 floor** (was published 30,499 — under by 45,400; the OLD `disk 11,032 + harness unreachable` framing on this line was FALSE, retired #109). `MEMORY.md` 8,470 of the first-turn figure is split and measured; **56,308 remains unattributed — that is what `ds-025` item 1 now means.** ⛔★★★ **FIGURE SUPERSEDED #129 (`s129-D1`, Dave's): FLOOR RE-BASED 81,335 → 70,794** (`BOOT_FIRSTTURN_TK` 65,400 → 54,859 ±1,178, n=7). **By ADDITION: the `ds-025` question above is UNCHANGED; only the constant moved.** Authority = `_gauge_tokens.py`, never this line. ⚠ **`pip install tiktoken --break-system-packages` FIRST in a cold sandbox** — the gauge REFUSES, loud and named (`MeasurementRefused`); `_build_all.py` P5 = ABORT without it (#80). ★ **READ THE CHAIN ONLY.** ★ **Repo-state claims are verified against `git log` / a real run, never a banner.** *(The #58/#59/#80/#83 correction-provenance is HOMED at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET; the #109 boot-floor correction at `_DECISION-HISTORY/2026-08-06-109-boot-floor-measured.md` and § ★ #109.)*
> ⚠ **Render sandbox is FRESH every session** (~4 calls — price it) · **retrieval FIRST:** `_memento_search.py "<q>"` → `--fetch <id>` (⚠ **it served a two-session-old record until #32 — if an answer looks stale, check the index**) · **wrap moves RUN THROUGH `_gm_move.py`** (⚠ **banner headings are BLOCKQUOTED — anchor `> ## ★ PRIOR …`**) · **PACE PANEL: ask at the opener, no gate can see it** — Dave has declined a fresh one twice and put Fable on the table for judgment work; ✅ **THE "BEHIND PACE ⇒ MORE WINDOWS" CLAUSE IS RETIRED — RULED #60: multi-window is settled, two use cases (quota catch-up AND production crank), Claude offers/Dave vetoes, cap 3 revisable. `notes/_MEMENTO-DECISIONS.md` § ★ #60-D1.** It stood from #48; at #57 he said the opposite — *"we are behind, but I'm less comfortable with multiple windows, too much to decide and consume"* ⇒ **ONE window with more delegation.** ★ **The clause optimised the wrong quantity: it treated TOKENS as the constraint when DAVE'S DECISION LOAD is what binds.** ⚠ **Reflected back #57, and #58's opener did NOT settle it** (asked as the first question; he answered pace with *"no constraint, lets just fix the problems"*). Standing home + status: §C·4.
> *(**RENAME is delivered IN CHAT at wrap, not here — RULED #28, ENACTED in runbook step 4b by #30.**)*
> **TITLE THE NEXT CHAT →** `Apollo - #279: land the icons, then the constitution wave — s277-d4..d13`
> ✅ *(the #86 hand-patch that stood here is REMOVED by the #87 wrap, per its own text — #85/#86 are recorded as HOLE lines + ledger stubs, `notes/_GAUGE-LOG.md` / § ★ #85–#86.)*
> *(Titles are LABELS — role comes from Dave's opener line. Bands: the ONLY copy is
> `_RUNBOOK-context-gauge.md` § ★ THE FLOOR IS MEASURED — grep it, never recall it.)*
> ## ★ LATEST — 2026-09-16 (Wed **#278**, Fable 5.1, no lanes, DELEGATED wrap — ★★ **THE CLOUD MOVE AND THE SEED**)
>
> - ⓪ SHAPE+WORDS. One day, no split. ⏱ delta #278 · `3d320f3` `d7b8d72` PUSHED at the opener, + this wrap. ⛔ **READ `_HANDOFF-129` FIRST — IT OUTRANKS `_CHAIN.md`**; #128's "#278 FIRST MOVES" is now #279's, unchanged. **HOUSEKEEPING BY DESIGN — NO GRAPH WORK**; the stop line left no room for a lane. *"I've noticed that Anthropic have move the project to the cloud"* · *"as long as it's safe we have fallen foul of silent and creeping failures in the past"*.
> - ★ ① ONE RULING, 603→604, span `15  0`. **`s278-D1`** closes the `s277-D13` clause on his *"Ill go with you, lets just move on to the other graph work"*: the thin slice is a **SEED**, composed once at step 1 by `_compose_slice.py`; **ASK is the on-demand door**, reading the Constitution live for what the seed lacks.
> - ★ ② THE MEMORY DIRECTORY IS NOW THE CLAUDE.AI PROJECT STORE (import 03:14–17Z, `cowork-import`). Sandbox, FUSE mount, git, push, CI, delete-grant, chromium, tiktoken-first — **all unchanged, MEASURED not assumed**. ⛔ **The boot index did NOT come across**: ~200 flat files + an empty `index.md`, restarted from the #277 line; pre-import index preserved verbatim. Account-wide on READ, Apollo-only on WRITE; index cap 49,152 B. ⚠ One-way risk: a re-import could overwrite cloud-only lines.
> - ⛔ ③ ONLY THE CONDUCTOR'S SEAT CAN WRITE CLOUD MEMORY — the #275/#276 seat limit, now STRUCTURAL. The sub writes `notes/_lanes/<n>/WRAP-MEMORY-HOOK.md`; the conductor places it at the NEXT opener (`d7b8d72` placed #277's). **Runbook step 3 takes the line at this wrap, BY ADDITION.** ⛔ There is NO `MEMORY.md` in the repo and never was. **Repo is the record, memory the accelerator — unchanged.**
> - ⚙ ④ GAUGE: boot **72,110 real (n=1)** vs the `s129-D1` floor 70,794 — **+1,316, NOT a re-base**, `BOOT_FIRSTTURN_TK` untouched. FILL **138,006 declared / 33 turns**, **152,476 real / 44 turns** measured here on the conductor's own transcript (boot agrees to the token ⇒ the subtraction is legal), Δ **14,470**. ⛔ `/sessions` **96.6%**, 331,300 KB free — at 100% no session boots. subs: none.
> - ⬛ ⑤ NOT DONE, DECLARED: showroom **108 stale** (standing since #273, HIS call) · `compliance/README` 31 vs 55 · `A2-AUDIT` "316" vs 363 · resolver **FAIL(6)** · lane-ownership selftest **2/3** · `.git/_orphan-locks/` **12 top-level / 82 recursive** measured here, HIS — the handoff's "72" is superseded by the measurement, not by a rewrite.
> **residual → #279:** ⬛ **① LAND THE ICONS, THEN THE CONSTITUTION WAVE — `s277-D4..D13`** [1, DAVE'S] — 13 ruled, 3 enacted. `s225-D2`. **493 items, 13 new**, `carries:residual-278`. PROBE: `PYTHONPATH=knowledge python3 -c "import _capture_gate as c;print(len(c._carry_items([l for l in open('_CARRIES.md') if '#278:**' in l][0])))"` ⛔ **BOTH FIGURES RETRACTED AT #278 AND CORRECTED BY ADDITION WITH THEIR RECEIPT (`s183-D1`/`s188-D2`), NOT RE-TYPED:** the set is **506 items, 1 new** at `_CARRIES.md` § `## residual → #279`, id `carries:residual-279` — same PROBE, `#279:**` for `#278:**`, agrees at 506; ⚠ the +13 is the `_carry_items` mechanism (thirteen `[NEW — 0]` became countable at `[1]`), not new work. **The CARRY is undischarged and unaltered** — `s277-D4`…`s277-D13` are LAW and NOT IN THE TREE; `s278-D1` is an input to the reader lane.
> **residual (GENERATED #278):** 2c OK (banners 2/2) · 2d OK (deltas 3/3) · 2f OK (strata 1, log #277) — _roll_state.py · 2026-09-16

> **⬛ OPEN WORKLIST — PRESENCE INDEX (28 items, GENERATED). Every open item is named; NO bodies are here — `--fetch gm:DOFIRST`.**
> `0b` ENCODE BEFORE THE WAVE, part-consumed 28 · `0c` NEXT BUILD CANDIDATES · `0d` THE 67 ENACT WAVE, landed · `1` ds-018 C2 follow-through · `2` dv-legend/dv-behaviour CEILING · `3` ds-012(b) gutter-relative plot area · `4` DV-D16 floating growth · `5` Instrument-fit remainder · `6` ds-016, PART-RULED #114 · `7` ds-017, UNRULED · `8` STILL OWED, none superseded · `9` DELEGATION TOPOLOGY · `10` ✅ PER-GATE TEST PLAN · `11` 2c-ROLL / INDEX-VOCAB DEADLOCK · `12` #57 1b DOSSIER · `13` tmp RUNBOOK EXPOSURE, unfixed · `14` ⬛ DAVE'S FOUNDING PRINCIPLE · `15` LEDGER § ★ #59 · `16` UNHOMED PAIR (#78 2c EXIT CHECK) · `17` CHAIN-DIET CUT · `19` type.css 180 dark-mode specificity · `20` ds-029 FIRST IDIOM · `21` PER-THEME COLLISION SWEEP · `22` 89-D2 ruled not enacted, absent from state · `23` ds-025 re-scoped 109, not closed · `24` 110 boot-rent plan · `25` ds-032 enactment, ruled 106 · `26` ✅ boot-drift gate rulings
> **QUEUE — `gm:C1` strands · `gm:C2` ruling batch (Dave's) · `gm:C4` enact-queue.**

---

## ⏱ LATEST DELTA — 2026-09-16 (Wed from `date`) (**#278**, ONE DAY, conductor **FABLE 5.1**, **NO LANE SEATS**, with this **OPUS 5** wrap sub, DELEGATED — ★★ **THE CLOUD MOVE AND THE SEED: A HOUSEKEEPING SESSION THAT MEASURED THE NEW ENVIRONMENT AND RULED ONE OPEN CLAUSE**)

*(WHY/HOW: `_DECISION-HISTORY/2026-09-16-278-the-cloud-move-and-the-seed.md`. #278 did NO graph work BY DESIGN — Dave opened on the cloud move, the fill went on measuring the environment rather than assuming it, and the stop line left no room for a lane.)*

- ⛔★★★ **READ `_HANDOFF-129-the-cloud-move-and-the-seed.md` FIRST — IT IS NEWER THAN `_CHAIN.md` AND OUTRANKS IT.** It does **NOT** replace `_HANDOFF-128`: #128's § "#278 — FIRST MOVES" and § "STRUCTURAL REDS" are live and unchanged, and #128's first moves are now **#279's**, by pointer and not by copy.

- ★★ **ONE RULING — `knowledge/_rulings.json` 603 → 604, BY TEXTUAL SPAN: `git diff --numstat` reads `15  0`, ZERO deletions, verified at this seat.** **`s278-D1`** closes the clause `s277-D13` had recorded as an open question, on Dave's word *"Ill go with you, lets just move on to the other graph work"*: **the thin slice is a SEED** — composed ONCE at step 1 by `_compose_slice.py`, after which the session works from its own state — and **ASK is the ON-DEMAND DOOR**, called mid-session whenever the designer's next prompt needs a node the seed excluded, reading the Constitution **live** so that a ruling inscribed after the seed was composed is still answerable. Seed and door under one contract; neither replaces the other. ⇒ **`s278-D1` is now an INPUT to #279's reader lane.**

- ★★★ **THE COWORK MEMORY DIRECTORY IS NOW THE CLAUDE.AI PROJECT MEMORY** (import 2026-09-16 03:14–17Z, `sources: [cowork-import]`). ⚠ **MEASURED, NOT ASSUMED — and that is the whole of what #278's fill bought:** the sandbox (aarch64 Ubuntu 22.04), the FUSE mount, git, push, CI, the delete-guard grant, the chromium recipe and tiktoken-first are **ALL UNCHANGED**. **Dave's ruling, going with the recommendation: the repo is the record, cloud memory is the accelerator** — which is what `_RUNBOOK-capture-ritual.md` step 3 already said; the rule has a new place to point at and nothing else moved. ⛔ **There is NO `MEMORY.md` in the repo and never was** — `git ls-files` reads **0**, verified here; the old one was the Cowork LOCAL index.

- ⚠ **THE BOOT INDEX DID NOT COME ACROSS.** The cloud store arrived as ~200 flat files and an **empty `index.md`**; the conductor restarted it with the #277 ★★★ line, newest-first, in the old `MEMORY.md` shape, and the pre-import index is preserved verbatim in `hook-overflow-2026-09-02-240` and `MEMORY-ARCHIVE`. Store is **account-wide on READ** (other Projects' subtrees are visible) and **Apollo-only on WRITE** — keep Apollo files Apollo-only. Cloud index file cap **49,152 B**; compaction is still owed periodically and the dream pass is its seat, not urgent at one line. ⛔ **The forward risk is ONE-WAY: an Anthropic re-import could overwrite cloud-only lines** — harmless only for as long as the repo holds everything durable, which is the reason the accelerator/record split is a rule and not a preference.

- ⛔★★ **ONLY THE CONDUCTOR'S SEAT CAN WRITE CLOUD MEMORY, AND THE #275/#276 SEAT LIMIT IS NOW STRUCTURAL RATHER THAN CIRCUMSTANTIAL.** A delegated wrap sub cannot reach the store at all. The proven pattern: **the wrap sub writes `notes/_lanes/<n>/WRAP-MEMORY-HOOK.md` (index line + file body) and the conductor places it at the NEXT opener** — #277's hook was placed exactly that way at `d7b8d72`, at a cold seat for ≈4K tokens. ✅ **THE RUNBOOK LINE THAT WAS OWED AT STEP 3 IS TAKEN AT THIS WRAP, BY ADDITION** — no existing step-3 line was rewritten.

- ⚙ **GAUGE, AND THE FIGURE DAVE ASKED FOR BY NAME.** *"what is the boot measurement it way over the 70K now isnt it?"* ⇒ **BOOT 72,110 real, n=1**, against the `s129-D1` floor of **70,794** — **+1,316, inside noise plus a hair, and NOT a re-base**; `BOOT_FIRSTTURN_TK` is untouched and the floor's own label is what should read "cloud listing replaces MEMORY.md" (≈8K either way). ⚠ **n=1 IS STATED AS n=1** — one reading is not a band [[feedback-measuring-tool-must-not-guess]]. **FILL 138,006 real / 33 turns DECLARED by the conductor at his check-in; 152,476 real / 44 turns MEASURED first-hand at this wrap seat** with `knowledge/_checkin.py` against **the conductor's own transcript** — the same window the declaration describes, its first user record being Dave's own opener and **boot agreeing to the token at 72,110**, which is what makes the subtraction legal ⇒ **delta 14,470 real**. ⛔ **`/sessions` DISK 96.6% — 331,300 KB free, measured here** (the handoff carries 97%; both are readings of a moving number, and the later one is this). `_gate_scratch_hygiene.py` reads **clean — no scratch owned by this user**; the VM mount is 48.9%. ⛔ **At 100% no session boots (#227/#228 class), nothing in the repo fixes it, and the symptom is a session that will not start.** **subs: NONE — this session delegated one seat, this wrap, and its cost is not measurable from inside it, so NO `subs` line is written** (`s214-D5`: absence is legal, absence is never defaulted).

- ⚠ **DECLARED SKIPS AND NOT-DONE, EACH WITH ITS SIZE (declare-last, and the size is the point):** **2c, 2d and 2f ALL RAN** — the #276 ★ PRIOR banner (**12 lines**) rolled to `_GM-ARCHIVE.md` § Batch 2026-09-16 #278, the #275 ⏱ PRIOR DELTA (**26 lines**) rolled to `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-16 #278 with #274's `Previous:` chain segment (**6,252 chars**) beside it, and `roll_2f #277` split **30 lines** into `notes/_GAUGE-LOG.md` at true EOF and **2 lines** into `_GM-ARCHIVE.md`; every move went through `knowledge/_gm_move.py` from session-owned, uniquely-named ops files asserted before the mover read them (`notes/_lanes/278/W/ops-278-rolls-1789561323.json`, **1,090 bytes, 5 ops**, and `ops-278-banner-1789561500.json`, **3,696 bytes, 2 ops**), dry-run first and the receipts read back against the ops that were meant to run; the `Previous:` segment moved through `notes/_lanes/278/W/lastrefreshed278.py`, which ASSERTS span reconstruction against the original bytes before it writes. **2e is a NO-OP and the size of the skip is ZERO LINES** — DO-FIRST is unchanged, no notice, tombstone or lifecycle tag was minted and nothing was retro-tagged. **2c(i) ran:** `_CARRIES.md` § `## residual → #279`. ⛔ **MEMORY (step 3) IS UNREACHABLE FROM THIS SEAT — A STRUCTURAL SEAT LIMIT, NOT A SKIP** — the hook body and its one index line are written to `notes/_lanes/278/WRAP-MEMORY-HOOK.md` for the conductor to place at the #279 opener. **NOT DONE and declared:** the showroom's **108 stale pages**, a standing declared gap since #273 carried on every wrap banner and **regeneration is Dave's call, not a wrap seat's** · `knowledge/compliance/README.md` still reads *"31 rules"* against a corpus of **55**, a GENERATED block · `notes/_lanes/277/kg-audit/A2-AUDIT.md`'s *"316"* still stands wrong in a committed file against a measure of **363**, correction by addition still owed and not made here · `_validate_roles_resolve.py` **FAIL(6)**, all six #261's `data-grid` `with`-slugs, measured here and INHERITED · `_validate_lane_ownership.py --selftest` **2/3**, the failing bite a `_CHAIN.md` line-shape parse, INHERITED · `.git/_orphan-locks/` **NOT emptied — the handoff calls it Dave's and it is left as the midden it is**; ⚠ **the handoff's "72 entries" is a figure this seat cannot reproduce: `ls` reads 12 top-level (11 zero-byte lock husks + `tmp_obj/`) and `find` reads 82 recursive** — both measurements are published and the inscribed "72" is NOT rewritten, per `s183-D1`/`s188-D2`.


---

## ⬛ OPEN WORK — GENERATED from `_state.json`. **The row count IS the count.**

> **649 items · 550 live · 270 Dave's · 280 mine · 635 carry a stated close condition · **14 UNCONDITIONED**.**
> *Every figure in this block is computed from the store at generation time. No number here was typed by anyone — that is the point of it (#86 measured a typed inventory of "118 markers" against a real ~40; #85's "95 slots / 84 distinct" is not reproducible by any probe in this repo).*
> ⚠ **DECLARED DEBT — 14 inherited items have NO close condition and therefore cannot close.** They are exempt as a FROZEN set (`_state.LEGACY_IDS`, size 19) which may only shrink; a NEW item is refused without one. **Each needs Dave's word — an agent inventing a close condition for his open work is the same overreach as inventing his ruling.**
>
> **DAVE'S (270) — 270 with a ratified `closes_when`, 0 with NONE · MINE (280). IDS, BODIES AND CLOSE CONDITIONS ARE IN THE STORE, NEVER HERE (s227-D2 shape, ids cut `s241-D2`): the counts above are the inventory; the rows are one command away, named on the next line.**
>
> ⚠ store gate: **FAIL** — 1 failure(s): W-278wr: missing required field(s) links. Bodies, conditions and provenance: `python3 knowledge/_state.py`.


---

*(Chain ends. **8,006 tape (cl100k ESTIMATE) — the unit is THE WHOLE FILE**, this generated wrapper included:
the number in this sentence is the size of the file containing it, held exact by a fixed point.
`GOOD-MORNING.md` is 34,904 tape (cl100k ESTIMATE) and stays whole for retrieval; you have paid for
23% of it. Under 40% is this generator's own floor — above it, the wrapper is
carrying more than the slice, and `--selftest` says so by name.)*
