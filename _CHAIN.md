<!-- GENERATED by knowledge/_gen_chain.py — DO NOT HAND-EDIT.
     Edit GOOD-MORNING.md / _LIVE-STATE.md and regenerate; `--check` is a build step. -->

# The read chain — everything a cold session must read, and nothing else

> **This file is GENERATED and it is the whole contract.** It carries `GOOD-MORNING.md`'s header
> and ★ LATEST banner, plus `_LIVE-STATE.md`'s ⏱ LATEST delta — the three things GM-D7-am names
> (CUT #33 on Dave's ruling). **If you have read this file, you have read the chain.**
>
> ⚠ **Do NOT now open `GOOD-MORNING.md` to "check".** It is 50,699 real and 85% of it is
> retrieval surface you are not meant to pay for at boot. That reflex is the thing this file
> exists to stop — five sessions called the chain CUT and every one of them paid full price.
>
> ★ **Everything else is RETRIEVAL, never a reading list:**
> `python3 knowledge/_memento_search.py "<q>"` → `--fetch <id>`. §A orientation is `gm:A`
> (a router with 11 children); the queue is `gm:C1`…; the archives, briefs, runbooks and ledgers
> are all indexed. **Ask for what you need; do not read a file to find out whether you need it.**

---

> **YOU ARE #140. TITLE THIS CHAT →** `Apollo - #140: dave's ruling queue from #139, five items`
> *(read it back in your opener — the chat half of the ritual is ungateable, which is why this line is FIRST)*

---

<!-- ⛔ STOP. If you are a cold session, you are in the wrong file and you pay for ALL of it.
     Read `_CHAIN.md` instead — it is GENERATED from this file's header + ★ LATEST banner and
     `_LIVE-STATE.md`'s ⏱ LATEST delta, which is the ENTIRE contract (GM-D7-am, CUT #33).
     This file stays whole for RETRIEVAL: `_memento_search.py "<q>"` → `--fetch <id>`.
     ⚠ Reading on from here is not thoroughness — it is the exact overspend #41 measured. -->

# Good morning, Dave ☕

> **size:** GM **50.7K tk real** (50,699 exact) · §A **7.0K tk real (EXEMPT)** (6,957 exact) · LS **69.3K tk real** (69,332 exact) · measured 2026-08-09 #139 (`_gauge_tokens.count()` → `real`), **on the artefact, AFTER the regen, iterated to a FIXED POINT** [[chain-is-a-swap-not-an-add]]. ⚠ **THE READ-CHAIN FIGURE IS DELIBERATELY ABSENT — its ONE home is `_CHAIN.md`'s own generated footer** (retired #45). **Point at it; never restate it.** ✅ **THE UNIT IS REAL CLAUDE TOKENS (#82-D1, Dave's, ruled #54) — MEASURED, NEVER CONVERTED; ratios REPORTED, NEVER APPLIED.** *(This line's correction-provenance — #90's self-reproduction, the `CHAIN_STAMP_RE` scope hole closed #94, the #90 2f-roll discharge — HOMED BY ADDITION at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET, then cut here [[home-by-addition-then-cut]].)*
> **STATE: ★★ THE READ CHAIN IS CUT (#33) and it HELD — EIGHTH session running.** You are reading the contract. §A and §C are STILL IN THIS FILE, reached by `_memento_search.py`, not by reading top-to-bottom. ⛔ **BUILD VERDICT: 75 of 106 steps green (#62, `18c7789`) — 31 steps have NEVER been in a green verdict.** Both counts GENERATED from `_build_all.py`'s AST at each end; the shortfall is computed (`s125-D1`). A single-process `_build_all.py` run is SANDBOX-IMPOSSIBLE (~49s vs the ~45s call kill); CI delivers the single-process verdict on push. ⚠ **DO NOT RUN `_build_all.py` TO CHECK — ANY PARTIAL run strands the tree in the documented mid-build intermediate** (step 1 rewrites wholesale; healed only by a COMPLETE pass — docstring lines 5–21). ★ **Only the PREMISE of a derived rule is worth re-verifying** (#47's lesson, turned on #47). *(The #47/#61/#62 build-claim archaeology + the #38 cap-repoint finding are HOMED at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET and `notes/_briefs/2026-07-29-cap-repoint-and-lane-divvy-brief.md`.)*
> ⚠ **`set_content()` IS BANNED IN RENDER-PROOFS** — drops `type.css` silently (14px→16px) and `document.fonts.check` passes anyway; use `goto("file://…")` (#29, measured) · **ds-020 is a gate-enforced obligation** (the DV-D02-A scatter waiver names it as clears-when) · **ritual step 2g: rebuild `_memento-index.json` LAST** — `index_freshness_check` is BLOCKING and compares CONTENT, never mtime (runbook has the body).
> **★ PRICE IN REAL TOKENS — THE UNIT IS `_gauge_tokens.py`, NOT A PERCENTAGE (#56, Dave's).** **amber 160,000 (PICKED) · working 200,000 (SOURCED) · hard 256,000 (SOURCED)** — provenance per `_gauge_tokens.py:63–83`, the authority [[read-chain-is-where-staleness-is-free]]. Unit = the FULL price (boot + job + wrap); ⚠ **a DECLARED gap passes, a SILENT one fails** — that asymmetry is the mechanism. The retired 45/60/63 % band is DORMANT in code, pinned — retire-or-pin = `G8`, Dave's. ⚠ **THE FLOOR IS MEASURED = `ds-025` RE-SCOPED #109 — first turn 65,400±1,400 real (n=5) + `_CHAIN.md` 10,499 additive = 75,899 floor** (was published 30,499 — under by 45,400; the OLD `disk 11,032 + harness unreachable` framing on this line was FALSE, retired #109). `MEMORY.md` 8,470 of the first-turn figure is split and measured; **56,308 remains unattributed — that is what `ds-025` item 1 now means.** ⛔★★★ **FIGURE SUPERSEDED #129 (`s129-D1`, Dave's): FLOOR RE-BASED 81,335 → 70,794** (`BOOT_FIRSTTURN_TK` 65,400 → 54,859 ±1,178, n=7). **By ADDITION: the `ds-025` question above is UNCHANGED; only the constant moved.** Authority = `_gauge_tokens.py`, never this line. ⚠ **`pip install tiktoken --break-system-packages` FIRST in a cold sandbox** — the gauge REFUSES, loud and named (`MeasurementRefused`); `_build_all.py` P5 = ABORT without it (#80). ★ **READ THE CHAIN ONLY.** ★ **Repo-state claims are verified against `git log` / a real run, never a banner.** *(The #58/#59/#80/#83 correction-provenance is HOMED at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET; the #109 boot-floor correction at `_DECISION-HISTORY/2026-08-06-109-boot-floor-measured.md` and § ★ #109.)*
> ⚠ **Render sandbox is FRESH every session** (~4 calls — price it) · **retrieval FIRST:** `_memento_search.py "<q>"` → `--fetch <id>` (⚠ **it served a two-session-old record until #32 — if an answer looks stale, check the index**) · **wrap moves RUN THROUGH `_gm_move.py`** (⚠ **banner headings are BLOCKQUOTED — anchor `> ## ★ PRIOR …`**) · **PACE PANEL: ask at the opener, no gate can see it** — Dave has declined a fresh one twice and put Fable on the table for judgment work; ✅ **THE "BEHIND PACE ⇒ MORE WINDOWS" CLAUSE IS RETIRED — RULED #60: multi-window is settled, two use cases (quota catch-up AND production crank), Claude offers/Dave vetoes, cap 3 revisable. `notes/_MEMENTO-DECISIONS.md` § ★ #60-D1.** It stood from #48; at #57 he said the opposite — *"we are behind, but I'm less comfortable with multiple windows, too much to decide and consume"* ⇒ **ONE window with more delegation.** ★ **The clause optimised the wrong quantity: it treated TOKENS as the constraint when DAVE'S DECISION LOAD is what binds.** ⚠ **Reflected back #57, and #58's opener did NOT settle it** (asked as the first question; he answered pace with *"no constraint, lets just fix the problems"*). Standing home + status: §C·4.
> *(**RENAME is delivered IN CHAT at wrap, not here — RULED #28, ENACTED in runbook step 4b by #30.**)*
> **TITLE THE NEXT CHAT →** `Apollo - #140: dave's ruling queue from #139, five items`
> ✅ *(the #86 hand-patch that stood here is REMOVED by the #87 wrap, per its own text — #85/#86 are recorded as HOLE lines + ledger stubs, `notes/_GAUGE-LOG.md` / § ★ #85–#86.)*
> *(Titles are LABELS — role comes from Dave's opener line. Bands: the ONLY copy is
> `_RUNBOOK-context-gauge.md` § ★ THE FLOOR IS MEASURED — grep it, never recall it.)*
> ## ★ LATEST — 2026-08-09 (Sun **#139**, FABLE conductor, Dave live, FIVE lanes off #138's residual — all landed — ✅ **ds-012(b) ENACTED AFTER SIX WEEKS PARKED** · ✅ **`s135-D4` ENACTED — 82/82 KG VERDICTS LANDED** · ★ **THE SCHEMA FORBIDS THE TWO KEYS THE MODEL NEEDS** · ✅ **FORK-BAN GATE BUILT — #108's "40 COLLISIONS" IS MEASURABLY STALE** · ✅ **TWO ORPHAN GATES WIRED — ONE ORPHANED SINCE #133** · ⚠ **DOUBLED-PREFIX COMMIT CLASS RECURRED** · ⚠ **ENOSPC n=5** · ⛔ **QUOTA NOT POLLED, ON DAVE'S WORD**)
>
> - ✅ **① ds-012(b) ENACTED — RULED #6, PARKED ~SIX WEEKS, NOW LIVE.** Gutter-relative plot area via a runtime `gutterPL()` in `knowledge/canon/dv-behaviour.js`, opt-in through `data-pl-fit`. **Measured, not asserted: h-bar labels 6/6 CLIPPED → 0/6 at 1180, 480 and 320**, in both the snippet and the showroom. The licensed cut was proven by a **canvas probe with a DejaVu control and a bogus-face control** (the #138 lesson applied, not merely cited). Gate `knowledge/_render/verify_dv_gutter_render.py` — **48/0, and BITE-SENSITIVE: the conductor replayed the bite personally and a neutered copy FAILS.** Commit `94a3a46`. ⚠ **`PL_MAX_FRAC=0.42` is PROVISIONAL-AWAITING-DAVE** — a floor nobody has ruled. ⚠ **DECLARED, OUT OF RULING SCOPE: the JS-off path still clips.**
> - ✅ **② `s135-D4` ENACTED — the verdicts JSON is a GENERATOR INPUT, and 82/82 landed.** MERGE 5 (edges 5→15) · PROMOTE 52 · ATTACH 25 (`governedBy` 0→25). **Four loud refusal arms driven**, check (f) **mutation-tested** (89 red → green), **double-regen zero-diff**. Commit `54b080e`. This closes #135's application blocker (0/82 could land) and #138 residual ⑧'s held lane A. ⛔ **The 15 token-split exceptions are UNTOUCHED and DECLARED, not quietly dropped** — no tokens machinery exists in this generator; they stay **Dave's**.
> - ★ **③ `s136-D1` AUDIT LANE — AND ITS FINDING IS A PRECONDITION, NOT A RESULT.** `knowledge/_audit_props_axes.py`: **280 props across 76 metas · `binds` present 0/76 · `slots` key 0/76** — and ★ **`meta.schema.json` FORBIDS BOTH KEYS** (`additionalProperties: false`). ⇒ **the schema amendment BLOCKS the slots-rollout and DTCG lanes**; they cannot start until Dave rules it. Commit `cc2079a`. **40 rows UNCLASSIFIABLE and honestly so:** 38 are text-content — exactly the FLOATED text-param refinement from #136 — and **2 are behaviour, which the three-axis model has no axis for: a coverage gap REPORTED, NOT PATCHED.**
> - ✅ **④ THE FORK-BAN GATE IS BUILT, AND IT RE-PRICED #108.** `knowledge/_validate_token_forks.py` parses `canon.css` **in the consumer's grammar** — specificity-ladder `var()` resolution, the [[no-gate-parses-the-artefact]] standard — mutation-tested on real bytes plus a two-direction selftest. Ledger `knowledge/_TOKEN-FORK-LEDGER.json`, **42 forks recorded `UNRULED-BASELINE-s139`**; doc `knowledge/_FORK-BAN-GATE.md`. ★ **#108's “40 colliding names” IS MEASURABLY STALE — there are 104, with 20 FORK verdicts.** It reproduces #108's `--pri-hover` (48 contexts / 4 values) and `--sec-hover` (18 / 8) **independently**, which is what makes the new figure trustworthy. ⛔ **The ledger is NOT sanctioned — all 42 verdicts are DAVE'S.**
> - ✅ **⑤ TWO ORPHAN GATES WIRED INTO `_build_all` STEPS — AND ONE HAD BEEN AN ORPHAN SINCE #133.** `_validate_kg.py` (built #133, never wired — **six sessions of a gate that could not fail**) and the new fork gate. `_validate_wiring.py` **32/32, 0 orphans**. Commit `1f786c8`. ★ **Found because the sub asked “does MY gate actually RUN?”** — [[instrument-without-a-consumer]], caught by a question rather than by an instrument.
> - ⚠ **⑥ THE DOUBLED-PREFIX COMMIT-SUBJECT CLASS RECURRED — non-wrap arm this time.** The conductor's msgfile carried the `#139 <date> — ` prefix that `_git_commit.sh` prepends itself. **Caught by the subject-verify step, amended from a FRESH msgfile** (the script mutates the msgfile on refusal — #134's lesson, applied). ⛔ **The class now needs a gate and does not have one:** a msgfile whose line 1 matches `^#\d+ \d{4}-` should be REFUSED. Candidate, **not built, not ruled** — residual ③.
> - ⚠ **⑦ ENOSPC n=5 — THE WORST YET: `/sessions` at 7.5 M free.** Warm `/var/tmp` reused. **Browser launch CRASHED until `TMPDIR=/var/tmp`** (the profile dir was landing on the full mount) — the runbook already says this, so the **premise HELD**; recorded as a confirmation, not a new finding. ★ **NEW POTHOLE, worth the runbook: `git checkout -- <path>` CANNOT restore files on this mount** (“unable to unlink”, the same permission shape as the `index.lock` wart). **Restore via `git show HEAD:<path> > <path>`.** Enacted by ADDITION in `knowledge/_RUNBOOK-git-commit.md` § Gotchas.
> - ⚠ **⑧ SHOWROOM DRIFT — 25 NON-CHART PAGES WERE STALE AGAINST THE THEME CASCADE AT HEAD.** Exposed by lane ①'s regen, **attributed by a CONTROL RUN** rather than assumed ([[attribute-the-diff]]), and committed **separately** so the ds-012(b) diff stayed readable: `b2fb44f`. Nobody had noticed; no gate compares a committed showroom page against the cascade that generates it.
> - ⚙ **GAUGE.** boot **55,985** real — inside the 54,859 ±1,178 band, **another datapoint, NOT corrected into the constant**. Check-ins: opener **57,143** · lane seam **125,038** · wrap-open **~135,000** vs stop line **150,929** ⇒ **~15,900 of room**. Delegation: **3 build subs + this wrap sub** — the binding budget was **FILL** all session, per [[delegation-cost-inversion-110]]. ⛔ **QUOTA NOT POLLED, and that was DAVE'S CALL, verbatim: “dont worry about the pace, i need all these fixes done.”** Recorded UNPOLLED, not assumed comfortable.
> - ⚠ **⚙ ONE CONDUCTOR FACT CORRECTED BY THIS WRAP, NOT INVENTED AWAY.** The brief described #139 as “the first Fable conductor session”. The record disagrees: **#135 and #136 both ran FABLE conductor** (`_GM-ARCHIVE.md` § Batch 2026-08-09 #137/#138, banner headers, verbatim). The banner therefore says **FABLE conductor** and makes **no first-ever claim** — [[premise-ages-faster-than-rule]].
>
> **residual → #140:** ⬛ **① DAVE'S RULING QUEUE FROM #139, FIVE ITEMS: ds-012(b) floor `0.42` · column-chart narrow-width treatment (overlap below ~440, his eye) · the 42 fork verdicts (ledger `UNRULED-BASELINE-s139`) · the 15 token-split exceptions · the `meta.schema.json` binds/slots amendment [0]** · ⬛ **② the remaining `s136-D1` enact lanes — slots-key rollout + DTCG re-encode — BLOCKED on the schema amendment, i.e. on residual ① [1]** · ⬛ **③ doubled-prefix msgfile GATE candidate: refuse a msgfile whose line 1 matches `^#\d+ \d{4}-` — the class has now recurred, and a recurring cross-file fix is the gate-don't-patch trigger [1]** · ⬛ **④ the `/var/tmp` symlink farm is STILL unproven in a COLD sandbox — carried from #138 unchanged; this sandbox was warm again [1]** · ⬛ **⑤ `TMPDIR` fragility in `_validate_kg.py`'s `check_freshness` — the default path is one ENOSPC away from a spurious FAIL [1]** · ⬛ **⑥ the JS-off path of ds-012(b) still clips — declared, out of the ruling's scope; folds into ① if Dave wants it visible [0]** · ⬛ **⑦ carried from #138 UNCHANGED: browser-copy pruning · the 7 `_governs` prose-in-`evidence` pointers · dream-pass-6 P1/P3/P4/P5 · the 16 tier-map rows · readbacks (clause-A copy · #67-D2 · mono no-border) · the #135/#137 carried set · Dave's `git log` wrap-step candidate, FLOATED [2]**.
> **residual (GENERATED #139):** 2c OK (banners 2/2) · 2d OK (deltas 3/3) · 2f OK (strata 1, log #138) — _roll_state.py · 2026-08-09
> **⬛ OPEN WORKLIST — PRESENCE INDEX (28 items, GENERATED). Every open item is named; NO bodies are here — `--fetch gm:DOFIRST`.**
> `0b` ENCODE BEFORE THE WAVE, part-consumed 28 · `0c` NEXT BUILD CANDIDATES · `0d` THE 67 ENACT WAVE, landed · `1` ds-018 C2 follow-through · `2` dv-legend/dv-behaviour CEILING · `3` ds-012(b) gutter-relative plot area · `4` DV-D16 floating growth · `5` Instrument-fit remainder · `6` ds-016, PART-RULED #114 · `7` ds-017, UNRULED · `8` STILL OWED, none superseded · `9` DELEGATION TOPOLOGY · `10` ✅ PER-GATE TEST PLAN · `11` 2c-ROLL / INDEX-VOCAB DEADLOCK · `12` #57 1b DOSSIER · `13` tmp RUNBOOK EXPOSURE, unfixed · `14` ⬛ DAVE'S FOUNDING PRINCIPLE · `15` LEDGER § ★ #59 · `16` UNHOMED PAIR (#78 2c EXIT CHECK) · `17` CHAIN-DIET CUT · `19` type.css 180 dark-mode specificity · `20` ds-029 FIRST IDIOM · `21` PER-THEME COLLISION SWEEP · `22` 89-D2 ruled not enacted, absent from state · `23` ds-025 re-scoped 109, not closed · `24` 110 boot-rent plan · `25` ds-032 enactment, ruled 106 · `26` ✅ boot-drift gate rulings
> **QUEUE — `gm:C1` strands · `gm:C2` ruling batch (Dave's) · `gm:C4` enact-queue.**

---

## ⏱ LATEST DELTA — 2026-08-09 (Sun **#139**, FABLE conductor, Dave live, FIVE lanes off #138's residual — all landed — ✅ **ds-012(b) ENACTED AFTER SIX WEEKS PARKED** · ✅ **`s135-D4` ENACTED — 82/82 KG VERDICTS LANDED** · ★ **THE SCHEMA FORBIDS THE TWO KEYS THE MODEL NEEDS** · ✅ **FORK-BAN GATE BUILT — #108's “40 COLLISIONS” IS MEASURABLY STALE** · ✅ **TWO ORPHAN GATES WIRED** · ⚠ **DOUBLED-PREFIX COMMIT CLASS RECURRED** · ⛔ **QUOTA NOT POLLED, ON DAVE'S WORD**)

**Landed:** **ds-012(b)** (RULED #6, parked ~6 weeks) — gutter-relative plot area, runtime `gutterPL()` in `knowledge/canon/dv-behaviour.js`, `data-pl-fit` opt-in; h-bar labels **6/6 clipped → 0/6** at 1180/480/320 in snippet AND showroom; licensed cut proven by a canvas probe with DejaVu + bogus-face controls; `knowledge/_render/verify_dv_gutter_render.py` **48/0 and bite-sensitive** (a neutered copy fails — replayed by the conductor personally). `94a3a46`. · **`s135-D4`** — the verdicts JSON is a **generator input**: **82/82** landed (MERGE 5→15 edges · PROMOTE 52 · ATTACH 25 `governedBy` 0→25), four loud refusal arms driven, check (f) mutation-tested 89 red → green, double-regen zero-diff. `54b080e`. · **the `s136-D1` props-axes audit** — `knowledge/_audit_props_axes.py`, 280 props / 76 metas, `binds` 0/76, `slots` 0/76. `cc2079a`. · **the fork-ban gate** — `knowledge/_validate_token_forks.py` parses `canon.css` in the consumer's grammar (specificity-ladder `var()` resolution), mutation-tested on real bytes + two-direction selftest; ledger `knowledge/_TOKEN-FORK-LEDGER.json` (42 forks, `UNRULED-BASELINE-s139`), doc `knowledge/_FORK-BAN-GATE.md`. · **two orphan gates WIRED into `_build_all` STEPS** — `_validate_kg.py` (orphan since **#133**) and the fork gate; `_validate_wiring.py` **32/32, 0 orphans**. `1f786c8`. · **showroom drift regen** — 25 non-chart pages + index stale against the theme cascade at HEAD, exposed by lane 1's regen, attributed by a control run, committed separately. `b2fb44f`.

**Found, and it changes what can be scheduled:** ★ **`meta.schema.json` FORBIDS both `binds` and `slots`** (`additionalProperties: false`) ⇒ the schema amendment is a **PRECONDITION** for the slots-rollout and DTCG enact lanes, which therefore cannot start until Dave rules it. ★ **#108's “40 colliding token names” is measurably STALE — 104 colliding names, 20 FORK verdicts**, with `--pri-hover` (48 contexts / 4 values) and `--sec-hover` (18 / 8) reproduced INDEPENDENTLY, which is what makes the new figure trustworthy. ★ **40 audit rows UNCLASSIFIABLE, honestly:** 38 text-content (the FLOATED #136 text-param refinement) and **2 behaviour — the three-axis model has no behaviour axis, a coverage gap REPORTED not patched.**

**Potholes and misses, owned:** ⚠ the **doubled-prefix commit-subject class RECURRED** on the non-wrap arm (the msgfile carried the prefix `_git_commit.sh` prepends itself); caught by subject-verify, amended from a FRESH msgfile. **No gate exists for it — candidate only.** · ⚠ **ENOSPC n=5, `/sessions` 7.5 M free — worst yet**; warm `/var/tmp` reused; the browser crashed until `TMPDIR=/var/tmp`, which the runbook already prescribes (premise HELD). · ★ **NEW: `git checkout -- <path>` cannot restore files on this mount** (“unable to unlink”) — restore via `git show HEAD:<path> > <path>`; enacted by ADDITION in `knowledge/_RUNBOOK-git-commit.md` § Gotchas. · ⚠ **The conductor's brief called this “the first Fable conductor session”; the record says #135 and #136 both ran FABLE.** Corrected here rather than inscribed.

**Still Dave's, nothing promoted:** the `PL_MAX_FRAC=0.42` floor (PROVISIONAL-AWAITING-DAVE) · column-chart narrow-width treatment · the 42 fork verdicts · the 15 token-split exceptions (UNTOUCHED-DECLARED — no tokens machinery in that generator) · the `meta.schema.json` amendment.

⚙ boot **55,985** real — in the 54,859±1,178 band, another datapoint, never corrected into the constant; check-ins opener **57,143** · lane seam **125,038** · wrap-open **~135,000** vs stop line **150,929** — ✅ BELOW, ~15,900 of room. Delegation: **3 build subs + this wrap sub**; binding budget **FILL** all session. ⛔ **Quota NOT polled — Dave declined pace talk: “dont worry about the pace, i need all these fixes done.”** Dossier: `_DECISION-HISTORY/2026-08-09-139-five-lanes-off-138-residual.md`.


---

## ⬛ OPEN WORK — GENERATED from `_state.json`. **The row count IS the count.**

> **36 items · 36 live · 22 Dave's · 14 mine · 17 carry a stated close condition · **19 UNCONDITIONED**.**
> *Every figure in this block is computed from the store at generation time. No number here was typed by anyone — that is the point of it (#86 measured a typed inventory of "118 markers" against a real ~40; #85's "95 slots / 84 distinct" is not reproducible by any probe in this repo).*
> ⚠ **DECLARED DEBT — 19 inherited items have NO close condition and therefore cannot close.** They are exempt as a FROZEN set (`_state.LEGACY_IDS`, size 19) which may only shrink; a NEW item is refused without one. **Each needs Dave's word — an agent inventing a close condition for his open work is the same overreach as inventing his ruling.**
>
> **DAVE'S (22) — 17 with a ratified `closes_when`, 5 with NONE:**
> - `G1` **Worklist-index cap DOFIRST_INDEX_TK_MAX = 700 (_capture_gate.py:1403,…** — *closes when:* Dave ratifies 700 or names his own number
> - `G10` **The "70%/95%" stray band (GM:36** — *closes when:* Dave rules provenance or strikes it
> - `G11` **DS-018 recessive value** — *closes when:* Dave picks the recessive value
> - `G12` **Charter §4b tone-of-voice temperature map, PROVISIONAL 2026-07-02…** — *closes when:* tov-016 review lands, or Dave ratifies the mapping as-is
> - `G13b` **menu-search combined glyph, PROVISIONAL 2026-07-16 (_ICON-GAPS.md; the…** — *closes when:* Dave approves the glyph
> - `G14` **Icon-button dark bindings** — *closes when:* Dave rules the SC-dark bindings
> - `G15` **DV-D13 donut centre figure + st.visible[id]=true release wiring…** — *closes when:* Dave's sign-off eye
> - `G16` **The _proforma/_DATAVIZ-DECISIONS.md:567 enactment call (agent's, not…** — *closes when:* Dave ratifies or reverses
> - `G17` **RAG status manifestation…** — *closes when:* Dave's canon pick — A / A+B / A+B+C
> - `G2` **TAPE_TO_BILL = 1.57 at n=2 (_capture_gate.py:371; RATIO_FIRM_N = 4 per…** — *closes when:* 4 measured pairs logged, then Dave rules firm-or-retire
> - `G3` **retired_unit_prose_audit tier = WARN (_capture_gate.py:2078, 2098,…** — *closes when:* Dave rules warn vs block
> - `G4` **GM §C measured 191 > 150 warn cap (_capture_gate.py:2864,…** — *closes when:* Dave picks OFFLOAD / TRIM / KEEP
> - `G5` **Four advisory size caps as a set (_capture_gate.py:4843–4858** — *closes when:* Re-measured in real (G9 first), then Dave ratifies the set in one pass
> - `G6` **DEFER_STREAK = 6 (_gm_usage.py:353) + USAGE_HISTORY_BLOCKING = False** — *closes when:* Dave rules the streak number + per-candidate OFFLOAD/TRIM/KEEP calls
> - `G7` **Which end of an archive is "newest" (strata oldest-first · archives…** — *closes when:* Dave names the convention — one sentence
> - `G8` **Dormant % band BAND_FLOOR/HARD_STOP/MARKED_MAX = 45/60/63…** — *closes when:* Dave says retire or pin
> - `G9` **ds-023 re-measurement programme** — *closes when:* Dave rules the programme
> - `W-0c` **NEXT BUILD CANDIDATES** — *closes when:* ⛔ **none stated — cannot close until you name one**
> - `W-0d` **✅ THE #67 ENACT WAVE** — *closes when:* ⛔ **none stated — cannot close until you name one**
> - `W-14` **⬛ DAVE'S FOUNDING PRINCIPLE** — *closes when:* ⛔ **none stated — cannot close until you name one**
> - `W-15` **LEDGER § ★ #59** — *closes when:* ⛔ **none stated — cannot close until you name one**
> - `W-16` **UNHOMED PAIR, copied up at the #78 2c EXIT CHECK** — *closes when:* ⛔ **none stated — cannot close until you name one**
>
> **MINE (14), ids only — bodies are in the store, not here:** `W-0b` · `W-01` · `W-02` · `W-03` · `W-04` · `W-05` · `W-06` · `W-07` · `W-08` · `W-09` · `W-10` · `W-11` · `W-12` · `W-13`
>
> ⚠ store gate: **PASS**. Bodies, conditions and provenance: `python3 knowledge/_state.py`.


---

*(Chain ends. **12,110 real — the unit is THE WHOLE FILE**, this generated wrapper included:
the number in this sentence is the size of the file containing it, held exact by a fixed point.
`GOOD-MORNING.md` is 50,699 real and stays whole for retrieval; you have paid for
24% of it, and that is the intended figure, not a shortfall.)*
