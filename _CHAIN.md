<!-- GENERATED by knowledge/_gen_chain.py — DO NOT HAND-EDIT.
     Edit GOOD-MORNING.md / _LIVE-STATE.md and regenerate; `--check` is a build step. -->

# The read chain — everything a cold session must read, and nothing else

> **This file is GENERATED and it is the whole contract.** It carries `GOOD-MORNING.md`'s header
> and ★ LATEST banner, plus `_LIVE-STATE.md`'s ⏱ LATEST delta — the three things GM-D7-am names
> (CUT #33 on Dave's ruling). **If you have read this file, you have read the chain.**
>
> ⚠ **Do NOT now open `GOOD-MORNING.md` to "check".** It is 34,267 tape (cl100k ESTIMATE); this file already
> carries the part of it a cold session needs, and the rest is retrieval surface you are not
> meant to pay for at boot. That reflex is the thing this file
> exists to stop — five sessions called the chain CUT and every one of them paid full price.
>
> ★ **Everything else is RETRIEVAL, never a reading list:**
> `python3 knowledge/_memento_search.py "<q>"` → `--fetch <id>`. §A orientation is `gm:A`; the queue is `gm:C1`…; the archives, briefs, runbooks and ledgers are all indexed.
> **Ask for what you need; do not read a file to find out whether you need it.**

---

> **YOU ARE #268. TITLE THIS CHAT →** `Apollo - #268: rehearse the run-of-show, v1.0.11 canon, the islands`
> *(read it back in your opener — the chat half of the ritual is ungateable, which is why this line is FIRST)*

---

<!-- ⛔ STOP. If you are a cold session, you are in the wrong file and you pay for ALL of it.
     Read `_CHAIN.md` instead — it is GENERATED from this file's header + ★ LATEST banner and
     `_LIVE-STATE.md`'s ⏱ LATEST delta, which is the ENTIRE contract (GM-D7-am, CUT #33).
     This file stays whole for RETRIEVAL: `_memento_search.py "<q>"` → `--fetch <id>`.
     ⚠ Reading on from here is not thoroughness — it is the exact overspend #41 measured. -->

# Good morning, Dave ☕

> ⚠ **WRAP DATE SPLIT — SESSION 2026-09-02, COMMIT 2026-09-03.** #241's ritual was authored 09-02 and steps 1→4c landed that night; the commit did not, and step 5 was finished the next morning. ⛔ **No key, filename or stamp was re-dated to match the commit** — that would be the T-D12 false inscription in reverse; this line carries the commit date so the gate's `is not today` check grades a true statement. Detail + the ruling-shaped question in `_LIVE-STATE.md`'s header block.
> ⚠ **WRAP DATE SPLIT, SECOND OCCURRENCE — SESSION 2026-09-05, RITUAL + COMMIT 2026-09-06.** #248's conductor cut the wrap brief at 183,983 real FILL on the evening of 09-05; the delegated wrap sub ran every step the next morning. ⛔ **No key, filename or stamp was re-dated to match the commit** — the #241 shape, by ADDITION (the #241 line above stands; its ruling-shaped question, what a midnight-spanning wrap should stamp, is still Dave's at age 7). This line carries the commit date so the gate's `is not today` check grades a true statement. Detail in `_LIVE-STATE.md`'s header block.
> ⚠ **WRAP DATE SPLIT, THIRD OCCURRENCE — SESSION OPENED 2026-09-08 EVENING, RITUAL + COMMIT 2026-09-09.** #261's R and D3 lanes committed on the evening of 09-08 (22:11–22:44); the design wave, the revision seats and this ritual all ran on 09-09. ⛔ **No key, filename or stamp was re-dated to match the commit** — the eleven filed reports keep their `2026-09-08-261-*` stems, and each of the nine `s261-D*` rulings carries in its own `evidence` the sentence saying the hour was not recorded at the wrap seat, so the date it carries is the COMMIT date and not a claim about the hour. The #241 shape by ADDITION: both lines above stand, and #241's ruling-shaped question — what a midnight-spanning wrap should stamp — is still Dave's, now at age 20. Detail in `_LIVE-STATE.md`'s ⏱ LATEST DELTA.

> **size:** GM **34.3K tape** (34267 exact — whole-file `tiktoken cl100k_base`, the figure this stamp is graded against at 10% tolerance) · §A **7.0K tape** (6,957 real by `_gm_usage.py`; the gate reports **198 lines, EXEMPT by ruling — measured and reported, never charged**, and at THIS seat the `## What Apollo is` → `## 1. ★ NEXT STRANDS` slice measures **3,764 tape / 14,132 B**, sha256 `b9aef5f5673225f7…` — ✅ **BYTE-IDENTICAL to `git show HEAD:GOOD-MORNING.md`'s same slice for a THIRTIETH consecutive wrap, PROVEN at this seat by comparing the two strings rather than assuming them equal**; ⚠ the slice boundaries here are this seat's own and are NAMED, so this figure is not comparable term-for-term with #237…#265's `4,375 tape / 16,489 B`, which used the gate's §A→§C slice — two slicers, two numbers, neither converted into the other [[measure-dont-convert-units]]; ⛔ nothing here is a trim order — §A is standing and what to do about it is Dave's) · LS **66.8K tape** (66771 real) · corpus GM+LS **101.0K tape** (101038 real, the RETRIEVAL surface, not the chain) · measured 2026-09-10 at the #267 wrap, DECLARE-LAST — after 2c, 2d and 2f, and after the stratum, so it grades the files this wrap actually leaves behind. ⚠ **THE DELTA IS THE FINDING, AND THIS TIME THE TWO FILES MOVED IN OPPOSITE DIRECTIONS:** against #266's stamp (**34,495 / 63,472 tape**) GM is **228 tape DOWN** — the first fall in several wraps, bought by 2c rolling the #265 banner and 2f rolling the #266 stratum out of a file whose new banner is written at **1,190 tape against the `s241-D2` cap of 1,200** — while LS is **3,299 tape UP**, because the ⏱ LATEST DELTA is the sole home for a ten-lane day's gauge, declared skips and not-done detail and 2d only carried out the #264 delta (17 ln) plus its 6,032-char `Previous:` segment. ⛔ **NEITHER NUMBER IS A TRIM ORDER; both are measurements, and what to do about LS's rise is Dave's.**
> **STATE: ★★ THE READ CHAIN IS CUT (#33) and it HELD.** *(the session-count ordinal is DELETED per s178-D1 enacting dream-P1 — stale twice ⇒ generate, never re-stamp)* You are reading the contract. §A and §C are STILL IN THIS FILE, reached by `_memento_search.py`, not by reading top-to-bottom. ⛔ **BUILD VERDICT: 75 of 144 steps green (#62, `18c7789`) — 69 steps have NEVER been in a green verdict.** Both counts GENERATED from `_build_all.py`'s AST at each end; the shortfall is computed (`s125-D1`). A single-process `_build_all.py` run is SANDBOX-IMPOSSIBLE (~49s vs the ~45s call kill); CI delivers the single-process verdict on push. ⚠ **DO NOT RUN `_build_all.py` TO CHECK — ANY PARTIAL run strands the tree in the documented mid-build intermediate** (step 1 rewrites wholesale; healed only by a COMPLETE pass — docstring lines 5–21). ★ **Only the PREMISE of a derived rule is worth re-verifying** (#47's lesson, turned on #47). *(The #47/#61/#62 build-claim archaeology + the #38 cap-repoint finding are HOMED at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET and `notes/_briefs/2026-07-29-cap-repoint-and-lane-divvy-brief.md`.)*
> ⚠ **`set_content()` IS BANNED IN RENDER-PROOFS** — drops `type.css` silently (14px→16px) and `document.fonts.check` passes anyway; use `goto("file://…")` (#29, measured) · **ds-020 is a gate-enforced obligation** (the DV-D02-A scatter waiver names it as clears-when) · **ritual step 2g: rebuild `_memento-index.json` LAST** — `index_freshness_check` is BLOCKING and compares CONTENT, never mtime (runbook has the body).
> **★ PRICE IN REAL TOKENS — THE UNIT IS `_gauge_tokens.py`, NOT A PERCENTAGE (#56, Dave's).** **amber 160,000 (PICKED) · working 200,000 (SOURCED) · hard 256,000 (SOURCED)** — provenance per `_gauge_tokens.py:63–83`, the authority [[read-chain-is-where-staleness-is-free]]. Unit = the FULL price (boot + job + wrap); ⚠ **a DECLARED gap passes, a SILENT one fails** — that asymmetry is the mechanism. The retired 45/60/63 % band is DORMANT in code, pinned — retire-or-pin = `G8`, Dave's. ⚠ **THE FLOOR IS MEASURED = `ds-025` RE-SCOPED #109 — first turn 65,400±1,400 real (n=5) + `_CHAIN.md` 10,499 additive = 75,899 floor** (was published 30,499 — under by 45,400; the OLD `disk 11,032 + harness unreachable` framing on this line was FALSE, retired #109). `MEMORY.md` 8,470 of the first-turn figure is split and measured; **56,308 remains unattributed — that is what `ds-025` item 1 now means.** ⛔★★★ **FIGURE SUPERSEDED #129 (`s129-D1`, Dave's): FLOOR RE-BASED 81,335 → 70,794** (`BOOT_FIRSTTURN_TK` 65,400 → 54,859 ±1,178, n=7). **By ADDITION: the `ds-025` question above is UNCHANGED; only the constant moved.** Authority = `_gauge_tokens.py`, never this line. ⚠ **`pip install tiktoken --break-system-packages` FIRST in a cold sandbox** — the gauge REFUSES, loud and named (`MeasurementRefused`); `_build_all.py` P5 = ABORT without it (#80). ★ **READ THE CHAIN ONLY.** ★ **Repo-state claims are verified against `git log` / a real run, never a banner.** *(The #58/#59/#80/#83 correction-provenance is HOMED at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET; the #109 boot-floor correction at `_DECISION-HISTORY/2026-08-06-109-boot-floor-measured.md` and § ★ #109.)*
> ⚠ **Render sandbox is FRESH every session** (~4 calls — price it) · **retrieval FIRST:** `_memento_search.py "<q>"` → `--fetch <id>` (⚠ **it served a two-session-old record until #32 — if an answer looks stale, check the index**) · **wrap moves RUN THROUGH `_gm_move.py`** (⚠ **banner headings are BLOCKQUOTED — anchor `> ## ★ PRIOR …`**) · **PACE PANEL: ask at the opener, no gate can see it** — Dave has declined a fresh one twice and put Fable on the table for judgment work; ✅ **THE "BEHIND PACE ⇒ MORE WINDOWS" CLAUSE IS RETIRED — RULED #60: multi-window is settled, two use cases (quota catch-up AND production crank), Claude offers/Dave vetoes, cap 3 revisable. `notes/_MEMENTO-DECISIONS.md` § ★ #60-D1.** It stood from #48; at #57 he said the opposite — *"we are behind, but I'm less comfortable with multiple windows, too much to decide and consume"* ⇒ **ONE window with more delegation.** ★ **The clause optimised the wrong quantity: it treated TOKENS as the constraint when DAVE'S DECISION LOAD is what binds.** ⚠ **Reflected back #57, and #58's opener did NOT settle it** (asked as the first question; he answered pace with *"no constraint, lets just fix the problems"*). Standing home + status: §C·4.
> *(**RENAME is delivered IN CHAT at wrap, not here — RULED #28, ENACTED in runbook step 4b by #30.**)*
> **TITLE THE NEXT CHAT →** `Apollo - #268: rehearse the run-of-show, v1.0.11 canon, the islands`
> ✅ *(the #86 hand-patch that stood here is REMOVED by the #87 wrap, per its own text — #85/#86 are recorded as HOLE lines + ledger stubs, `notes/_GAUGE-LOG.md` / § ★ #85–#86.)*
> *(Titles are LABELS — role comes from Dave's opener line. Bands: the ONLY copy is
> `_RUNBOOK-context-gauge.md` § ★ THE FLOOR IS MEASURED — grep it, never recall it.)*
> ## ★ LATEST — 2026-09-10 (Thu **#267**, **Fable** + 10 lanes + this sub, DELEGATED — **v1.0.10 RELEASED; 52/78 EDGES WRONG; RUN-OF-SHOW EXISTS**)
>
> - **⓪ SHAPE + ⛔ PREMISE WRONG — THREE DATES, NOT ONE:** v1.0.9 Fri 09-11 · small demo NEXT WEEK · Rice ~2 WEEKS. ⏱ delta #267 · WHY `_DECISION-HISTORY/2026-09-10-267-*` · 26 commits `fee2572..ceef5b9` PUSHED. ✅ no date split. ★ **4 RULINGS, 441→445.**
> - ⛔★★★ **① COLD RUN 6 (v1.0.9 zip, blind) 2/3/3/2; F01/F02 GONE — in prose, no gate.** `gen_provenance_receipt.py` is in NONE of v1.0.7/8/9 ⇒ `_validate_receipt.py` can only say `FAIL:NO-RECEIPT`. Cause: `local_imports()` walks AST nodes, `:635` calls `__import__()` — a STRING.
> - ✅★★★ **② v1.0.10 RELEASED `d76ffd50…c488`, twice-baked byte-equal, 10 gates green, PUSHED.** Cause fixed `fee2572` (not `HELPER_HOMES`); cut `625be64` lost the dataviz gate REPO-BOUND ⇒ `s267-D1` HOLD. ★ P2 deleted the typed list — `receipt_named_chart_pages(sha)` reads the receipt at the commit — 13 pages, not 6. Re-cut `a9218cc` ⇒ `s267-D2` ⇒ `de8ed57`. ⚠ `--release` needs a clean tree, in no runbook.
> - ★★★ **③ THE KG, TWICE. Sphere restored (v1.9 `4bc5231`):** *"flat plane"* = v1.8's ring, built ON PURPOSE; K's rebuilt sweep, both builds, 4/1,512 vs 0. ⛔ RED: SC dig, default families = 0 neighbours. **Edges 52/78 WRONG** (E, Fable: 26/20/1/31; all 12 `overrides` are NOUN HITS; 5 defects, one family, no direction test). `s267-D3` as judged + `supersedesClause` + edge-from-ACTOR; G: 48 edges, v1.10 DRIVEN, proposals 0. ⛔★ SEAM: `DECLARED_EDGE_TYPES` unaware; #265's fence STANDS.
> - ★★★ **④ canon.css WAS A STALE GENERATED OUTPUT, NOT A DIVERGENCE — ONE REGEN CLOSED ALL 20** (`.nv-item` 0→59). ★ A generated file adrift from its source is an UN-RUN GENERATOR; call it drift and you hand-patch a regression. `s267-D4`: `var(--x, fallback)` RESOLVED. ⛔ not in the zip ⇒ v1.0.11; `gen_theme_cascade` rc 1 before; render gates NOT RUN.
> - ★★ **⑤ RUN-OF-SHOW v1 EXISTS — THE 12 PLOT POINTS NEVER DID** ⇒ a FRESH MAPPING on his 5 beats; 18:00, 6 risks, **4 ⬛ slots his**. F: 102 days = 14.6 wks, every figure sourced. ⛔ DROP the 6-month comparator — no "six months" or baseline exists here; it would be the one INVENTED number. KPI 6/3/2; N unfilled.
> - ⚙ **⑥ GAUGE (⏱ delta):** ⛔★ FILL: he DECLARED ≈185,000; first-hand 201,554/75t — past 180,000 AND the 200,000 wall. **A BREACH**; both stand. ⛔ BOOT 70,717 — 717 OVER, THIRD CONSECUTIVE. ⚠ Brief said boot unmeasured — his seat, not this. ⛔ NO `subs`: only an aggregate ≈1.4M. ✅ MEMORY DONE at his seat.
> **residual → #268:** ⬛ **① REHEARSE THE RUN-OF-SHOW, v1.0.11 CANON, THE ISLANDS** [NEW — 0, DAVE'S] — ⛔ POINTER, `s225-D2`. 552 aged, 8 new in `_CARRIES.md` § `residual → #268` (`carries:residual-268`). ⚠ PROBE → 412: `PYTHONPATH=knowledge python3 -c "import _capture_gate as c;print(len(c._carry_items([l for l in open('_CARRIES.md') if '#268:**' in l][0])))"`
> **residual (GENERATED #267):** 2c OK (banners 2/2) · 2d OK (deltas 3/3) · 2f OK (strata 1, log #266) — _roll_state.py · 2026-09-10

> **⬛ OPEN WORKLIST — PRESENCE INDEX (28 items, GENERATED). Every open item is named; NO bodies are here — `--fetch gm:DOFIRST`.**
> `0b` ENCODE BEFORE THE WAVE, part-consumed 28 · `0c` NEXT BUILD CANDIDATES · `0d` THE 67 ENACT WAVE, landed · `1` ds-018 C2 follow-through · `2` dv-legend/dv-behaviour CEILING · `3` ds-012(b) gutter-relative plot area · `4` DV-D16 floating growth · `5` Instrument-fit remainder · `6` ds-016, PART-RULED #114 · `7` ds-017, UNRULED · `8` STILL OWED, none superseded · `9` DELEGATION TOPOLOGY · `10` ✅ PER-GATE TEST PLAN · `11` 2c-ROLL / INDEX-VOCAB DEADLOCK · `12` #57 1b DOSSIER · `13` tmp RUNBOOK EXPOSURE, unfixed · `14` ⬛ DAVE'S FOUNDING PRINCIPLE · `15` LEDGER § ★ #59 · `16` UNHOMED PAIR (#78 2c EXIT CHECK) · `17` CHAIN-DIET CUT · `19` type.css 180 dark-mode specificity · `20` ds-029 FIRST IDIOM · `21` PER-THEME COLLISION SWEEP · `22` 89-D2 ruled not enacted, absent from state · `23` ds-025 re-scoped 109, not closed · `24` 110 boot-rent plan · `25` ds-032 enactment, ruled 106 · `26` ✅ boot-drift gate rulings
> **QUEUE — `gm:C1` strands · `gm:C2` ruling batch (Dave's) · `gm:C4` enact-queue.**

---

## ⏱ LATEST DELTA — 2026-09-10 (Thu from `date`) (**#267**, one long day, conductor **Fable** + **TEN LANES** — **C6, P, P2, R, K, G, S, N** Opus · **E, F** FABLE (⚠ the wrap brief said *nine* and then named *ten*; the roster is the measurement, the count was a miscount) — plus this OPUS 5 wrap sub, DELEGATED — ★★★ **v1.0.10 RELEASED, 52 OF 78 PROPOSED EDGES WERE WRONG, AND THE FIRST RUN-OF-SHOW EXISTS**)

*(WHY/HOW: `_DECISION-HISTORY/2026-09-10-267-v1010-edges-run-of-show.md` — why a gate that parses the manifest cannot see that the pack fails its own instructions, how a typed candidate list became a cut RULE read off a receipt, why the flat-plane screenshot and the #266 fix were the same object from two sides, and the general form behind canon.css: a divergence between a generated file and its source is an un-run generator, not a drift.)*

- ✅ **NO DATE SPLIT — THE SESSION, ITS RITUAL AND ITS COMMITS ARE ALL 2026-09-10**, confirmed from `date` at this seat. The three standing WRAP DATE SPLIT notices belong to #241, #248 and #261; nothing was re-dated here, because nothing needed to be.

- ⛔★★★ **THE OPENER'S PREMISE WAS WRONG AND DAVE CORRECTED IT — THERE ARE THREE DATES, NOT ONE.** **v1.0.9 releases Friday 2026-09-11** · **the small demo is NEXT WEEK** · **David Rice is ~2 WEEKS OUT**. ⚠ **The #266 record collapses all three into "Friday"**, which is how a release deadline came to be doing duty for a rehearsal deadline and an audience deadline at once. Every piece of the day below is priced against the right one: the release work was for Friday, the run-of-show is for next week, the evidence page is for Rice. **NEVER CONFLATE THEM AGAIN.**

- ★★★ **FOUR RULINGS, DAVE'S, INSCRIBED — `knowledge/_rulings.json` 441 → 445 (entries 442–445), each parsing with `by` Dave · `date` 2026-09-10.** **`s267-D1`** (`185946f`) ***"Hold — re-cut with the chart pages shipped. lets get this sorted"*** — v1.0.10 HELD at `625be64`, re-cut with the chart pages in the ship set. **`s267-D2`** (`318e46d`) ***"okay inscribe"*** — v1.0.10 RATIFIED at `a9218cc`. **`s267-D3`** (`aa891d0`) — the 78 proposed ruling edges RATIFIED **as lane E judged them**, with `supersedesClause` as a declared type and **the edge drawn from the ACTOR**. **`s267-D4`** (`ceef5b9`) — `var(--x, fallback)` is RESOLVED for `_validate_compose.py`.

- ⛔★★★ **THE SIXTH COLD RUN FOUND A PACKAGING HOLE THREE RELEASES DEEP, AND NO GATE COULD HAVE SEEN IT.** Lane **C6** drove the frozen prompt blind against the v1.0.9 **ZIP** (`736d0fb0…c780`), light-led: **scores 2 / 3 / 3 / 2** (C5 was 3/3/3/2 against the tree). ✅ **F01 and F02 NO LONGER REPRODUCE** — `data-theme` sits on `<html>` and toggles both ways under assertion; a `render()`-written `.kpi-cta` fires; ⚠ **both are FIXED IN PROSE and NO GATE CHECKS EITHER.** ⛔ **The finding: `gen_provenance_receipt.py` — the helper step 5 tells a consumer to run FIRST — is in the repo tree and in NONE of the v1.0.7 / v1.0.8 / v1.0.9 zips**, so `_validate_receipt.py` could only ever answer `FAIL:NO-RECEIPT` and `_validate_screen.py` could only carry `receipt: UNPROVEN:NO-RECEIPT`. ★ **MECHANISM, ONE LINE:** `_gen_pack_manifest.py`'s `local_imports()` walked `ast.Import`/`ast.ImportFrom` **nodes**, and `_validate_receipt.py:635` reaches its helper as `__import__("gen_provenance_receipt")` — **a string inside a call**, invisible to a node scan. Report: `notes/_subreports/2026-09-10-267-C6-cold-run-6.md`. **[[no-gate-parses-the-artefact]], sixth venue: every release gate parsed the manifest, the file list and the version literals; not one asked whether the pack can run its own instructions.**

- ✅★★★ **v1.0.10 IS RELEASED, TWICE-BAKED BYTE-EQUAL, TEN GATES GREEN — `d76ffd507319ae53ccc96f4ea4090baa0369e2761985231954b7250eca5dc488`.** **Lane P** fixed the mechanism at cause with `string_call_imports()` (`fee2572`) and pointedly **NOT** by adding a row to `HELPER_HOMES` — that table is a typed seed and goes stale at the next lazy helper — then cut at **`625be64`** (1706 files). ⛔ **The dataviz gate fell OUT of the ship set, REPO-BOUND**, because six typed chart-page lines in `GATE_DATA_CANDIDATES` no longer matched what the gate's own receipts name ⇒ `s267-D1`, HOLD. ★ **Lane P2 did NOT lengthen the list — it DELETED the list**: `receipt_named_chart_pages(sha)` reads `knowledge/_tests/chart-engine/_receipts.json` **at the commit** (`git show <sha>:…`) and returns the rows the gate itself names — **THIRTEEN pages, not six**; reading at the commit and never at the working tree is what makes two bakes of one commit identical by construction. **7 new bites, 233 total, mutation-tested BOTH ways.** Re-cut at **`a9218cc`**: **1714 files / 45,967,883 bytes**, `cmp` rc 0 across two dry-run bakes. **Lane R** then keyed `RATIFY_IDS`, regenerated the manifest at the SAME commit with **exactly one line moved** (`PROPOSED` → `RATIFIED`; manifest sha `4cd9229adb2df432` → `5d37abc0a13ed486`, **file set unmoved**, which is what `s223-D8` cares about), baked (`de8ed57`), and re-seeded the frozen ledger at **10 zips, `1455d4390ec4`** (`6f948c2`). Report: `notes/_subreports/2026-09-10-267-R-release-v1010.md`. ⚠ **AND ONE ORDERING CONSTRAINT THAT IS IN NO RUNBOOK: `--release` REQUIRES A CLEAN TREE**, so `515ef2d` had to commit the session's untracked lane artefacts before the bake could start.

- ✅★★ **THE KG EXPLORER IS A SPHERE AGAIN (v1.9, `4bc5231`) — AND DAVE'S SCREENSHOT AND #266'S FIX WERE THE SAME OBJECT FROM TWO SIDES.** His word: ***"you can spin it but its just on a flat plane, it wast before"***. He was describing **v1.8's camera-plane ring**, which #266 introduced deliberately to stop neighbours hiding behind the focus when they line up with the camera — so the ring solved occlusion by destroying the three-dimensionality that made the picture worth showing. **Lane K restored v1.6's world-space sphere** and pays the occlusion cost differently: a per-frame **screen-space radial nudge capped at 12px** on halo dots whose projected centre lands inside the focus disc, **world positions untouched**, the drawn centre exported as `n._sx/_sy` so the sweep probes what is actually painted. ★ **AND IT REBUILT THE INSTRUMENT BEFORE IT QUOTED THE BASELINE:** #266's sweep script was never committed, so lane K re-implemented it (`notes/_kg-sweeps/267-v19/sweep_267_v19.py`) and ran it against **BOTH** builds — v1.8 rebuilt from `6d3b61e` — for a like-for-like comparison. **The sphere costs 4 wrong-colour centres in 1,512 samples (0.26%) against v1.8's zero.** Reported, not hidden. ⛔ **RED CARRIED, NOT FIXED: an SC dig with the DEFAULT families shows 0 NEIGHBOURS.**

- ★★★ **FIFTY-TWO OF THE SEVENTY-EIGHT PROPOSED RULING EDGES WERE WRONG, AND A FABLE LANE IS WHAT FOUND IT.** Lane **E** read **both** rulings for every one of #266's 78 `proposedType` edges: **ACCEPT 26 · RETYPE 20 · REVERSE 1 · REJECT 31**. ⛔ **All twelve `overrides` proposals are NOUN HITS** — "override sets", "overrides.json". By proposed verb: `supersedes` 10/14 · `extends` 6/8 · `refines` 2/3 · `enacts` 3/12 · `confirms` 2/8 · `corrects` 1/5 · `retires` 1/13 · `narrows` 1/3 · **`overrides` 0/12**. **Five generator defects, all one family — a text scan asked to do a semantic job:** a stem regex with a left `\b` only · a ±80 window spanning neighbouring ids in a list (one sentence proposing fourteen edges) · dict-order-first rather than nearest-verb · **no direction test at all** · and authored supersession FIELDS already in the store (`superseded_by`, `superseded_note_s168_D1`) ignored by the derivation. Lane file `notes/_lanes/2026-09-10-267-E-edge-judgement.md`; page `reviews/EDGE-JUDGEMENT-267-2026-09-10-v1.html`; rows `notes/_lanes/267/E/edge-recs.json`.

- ✅★★ **`s267-D3` IS ENACTED AND THE REPAIRED GENERATOR PROPOSES ZERO.** Lane **G**: `knowledge/_ruling_edges.json` — **48 authored ruling→ruling edges** (47 from lane E's ratified recommendations + 1 derived from the store's own `superseded_by` field) plus **31 ratified plain mentions**; `_gen_ruling_edges_from_recs.py` keeps the derivation re-takeable. `_build_kg_explorer.py` **v1.9 → v1.10** fixes all five defects with a **15-case bite** (`_bite_kg_edge_proposal.py`) driving the REAL `VERB_RX`/`MENTION_RX` and window arithmetic. `supersedesClause` draws in the SAME governance red as `supersedes` but **DOTTED `[1.5,2.5]`** — a **named choice**: dashed `[4,3]` is already the derived signal and the **TWO-RED LAW `s151-D1`** forbids inventing a third red. **Explorer rebuilt at v1.10 and DRIVEN: 48 authored edges drawn, 0 console errors, proposals 0** (`b858080`, `b5c54d2`, `ed1f930`, `f5448cb`). ⛔★ **THE SEAM IS DECLARED, NOT DISCOVERED: `gen_kg_edges.py`'s `DECLARED_EDGE_TYPES` DOES NOT KNOW `supersedesClause`**, and the #265 fence on that generator still stands — **DO NOT RUN IT** before lane E's RSQ 2 is ruled.

- ★★★ **canon.css WAS NOT A DIVERGENCE — IT WAS A STALE GENERATED OUTPUT, AND ONE REGEN CLOSED THE WHOLE TWENTY-SNIPPET LIST.** Lane **N** on C6's finding 1: `knowledge/canon/canon.css` is **GENERATED** by `canon/gen_canon_components.py` **from the snippets** (`_RUNBOOK-compose-from-canon.md` says so, and says "never hand-edit between the AUTO-COMPONENTS markers"); its mtime was **2026-09-08 17:48** while the snippets #261 revised moved **2026-09-09**. Nobody re-ran it. `.nv-item` **0 → 59** occurrences, `.nv-ic` 0 → 19, `.nv-label` 0 → 20 (`e470ae0`). ★ **THE GENERAL FORM, AND IT IS THE ONE TO CARRY: a divergence between a generated file and its source is an UN-RUN GENERATOR, not a drift — and the diagnosis that calls it drift will hand-patch the output and make the next regeneration a regression.** Two reds fell out: `Kpi-tile.reference.html:256`'s dead `min-height:var(--slot)` (`--slot` defined nowhere ⇒ already invalid-at-computed-value-time) removed, and `_validate_compose.py:87–99` now treats `var(--x, fallback)` as resolved when **every** occurrence carries a fallback — Dave's `s267-D4`, generalising `s258-D3` from a name suffix to the grammar (`57ddff1`, `8c219ca`). ⛔ **NONE OF IT IS IN THE v1.0.10 ZIP — it landed after the bake ⇒ v1.0.11 FAST FOLLOWER, carried not closed.** ⚠ **`gen_theme_cascade.py --check` was rc 1 BEFORE this lane (verified by swapping the pre-lane canon.css back in) — the same class, a second generator not re-run; the type-composites gate's rc 1 is PRE-EXISTING and untouched. The RENDER gates were NOT RUN at that lane's seat.**

- ★★ **THE RUN-OF-SHOW EXISTS FOR THE FIRST TIME — AND THE TWELVE PLOT POINTS NEVER DID.** Lane **S** went to write it from #266's *"twelve plot points, accepted in principle"* and found that **the twelve were never inscribed anywhere**: a sweep for `plot point` across the repo returns only the carry and banner lines saying they were NOT written. ⇒ **the twelve on `notes/_RUN-OF-SHOW-david-rice-2026-09-10-v1.html` are a FRESH MAPPING onto Dave's own five beats, in his numbering and his order — declared as such, because a cold reader would otherwise take them for a recovered artefact.** One-screen timing strip with a **stated sum of 18:00** inside his 15–20 band (B1 2:00 · B2 4:00 · B3 5:30 · B4 4:30 · B5 2:00), five beat cards with first-person spoken lines, the beat-3 dead-air script (A *what inspired it*, B *how it developed*, plus an overrun card), fixed click paths for both artefacts, and an **operator card of six MEASURED risks**, the top three drawn from C6. **Four ⬛ slots are left as DAVE'S.** The July deck's stale *"38 reviewed components"* and the two competing graph readings are named on the page **as conflicts rather than averaged** (`0f9ef72`).

- ★★ **THE EVIDENCE FOR HIS ONE SENTENCE IS MEASURED, AND ITS RECOMMENDATION IS TO DROP THE COMPARATOR.** Lane **F** on ***"compressing a 6 month project into N weeks"***: **102 days = 14.57 weeks** (first commit 2026-05-31 → v1.0.10 release 2026-09-10), 73 commit days, 1,280 commits, **234 numbered sessions** (#2…#267, 33 numbers absent), 218 dated dossiers, **444 rulings** all `by: Dave`, 51 KG snapshots — **every figure on the page carries a path or the git command that reproduces it**. ⛔ **RECOMMENDATION: DROP THE SIX-MONTH COMPARATOR** — the repo holds no "six months" and no baseline project, so the comparator would be **the one invented number on the page a CAIO is most likely to probe**. KPI support table **6 supported / 3 partly / 2 not**; the three CAIO attacks are written out. **No N chosen, no hours derived (234 sessions exist; no source states a session length).** `notes/_DEMO-EVIDENCE-n-weeks-2026-09-10-v1.html` (`327a961`).

- ⚙ **GAUGE — TWO OBJECTS, REPORTED SEPARATELY, NEVER SUMMED OR CONVERTED** [[measure-dont-convert-units]]. ⛔★★★ **FILL IS A BREACH AND IS WRITTEN AS ONE.** The conductor DECLARED **≈185,000 real** at the wrap call — **already past the `s260-D2` delegated **stop line 180,000** — and **first-hand at THIS seat `_checkin.py` reads 201,554 real over 75 turns**, which is **also past the 200,000 working wall**. **BOTH FIGURES STAND AND NEITHER IS AVERAGED**; the first-hand one is the one the next session pays. ⛔★★★ **BOOT 70,717 REAL, MEASURED AT THIS SEAT — 717 OVER the 70,000 shrink-only literal and the THIRD CONSECUTIVE BREACH** (#265 70,260 · #266 70,442 · #267 70,717, a rising series). ⚠ **THE BRIEF DECLARED BOOT UNMEASURED because the conductor's opener check-in did not print it — that was true of the conductor's seat and is NOT true of this one; `_checkin.py` prints it first-hand, and a number that can be taken is taken rather than declared absent.** The literal does not move (`s240-D2`/`s241-D1`) and the gate's refusal is recorded in the **#243 form** rather than routed around, for a **twenty-first** wrap running. ⛔ **AND THERE ARE THREE BLOCKING REFUSALS AT THIS WRAP, NOT ONE — TWO OF THEM ARTEFACTS OF THE RITUAL'S OWN 2f ROLL.** `boot double-count #264` is **INHERITED** and was declared in these same words at #266. `boot double-count #243` is **NEW AND WAS CREATED BY THIS WRAP'S ROLL**: moving #266's stratum into `notes/_GAUGE-LOG.md` carried a line reading *"carried in the #243 form"* — boot figures beside it — into the only file that check scans, so it now reads as session #243 stating a first-turn figure twice. ⛔ **THE REPAIR IS NOT AN EDIT: a rolled stratum is verbatim record (`s183-D1`), and quieting a gate by rewriting archived text is the failure mode this project has already paid for three times.** ★ **Ruling-shaped, and it is a mechanism rather than a mistake: the check is scoped to a file that 2f keeps ADDING prose to, so every wrap that names an old session beside a boot figure manufactures a fresh refusal — the SCOPE wants fixing, not the strata.** ⛔ **NO `subs` LINE IS WRITTEN, AND THE REASON IS THAT THE ONLY FIGURE AVAILABLE IS AN AGGREGATE APPROXIMATION.** The brief carries ***"sub tokens ≈1.4M"*** across ten lanes; no per-lane `message.usage` reading was captured, and lane E's own report states its ≈115K as an **estimate, not measured**. **Writing `subs 1,400,000 (n=10)` would inscribe an approximation in the field whose whole contract is that it is measured** [[feedback-measuring-tool-must-not-guess]] — the honest form is the absence, and the ≈1.4M is recorded here as the approximation it is. ⚙ **WRAP-HANDOVER (`s214-D5`): brief-cut ≈185,000 real (conductor, DECLARED as approximate) · sub-cut 201,554 real (first-hand) · delta ≈16,554 real · replay `unobservable (conductor-side, post-wrap)`.**

- ✅ **MEMORY (RITUAL STEP 3) IS DONE AT THE CONDUCTOR'S SEAT AND IS 0 BYTES AT THIS ONE — THE SECOND CONSECUTIVE DELEGATED WRAP WHERE IT IS CLOSED RATHER THAN DECLARED A SEAT LIMIT.** The conductor wrote `v1010-released-edges-ratified-kg-sphere-267.md` plus its `MEMORY.md` index line **before delegating**. ⚠ **Nothing was written to memory from this seat and nothing needed to be** — the hook is authored, and the durable content is inscribed in the repo (this delta, the dossier, the ledger, the lane files) rather than photocopied.

- ⚠ **DECLARED SKIPS AT THIS WRAP, EACH WITH ITS SIZE** (declare-last, and the size is the point): **2e = 0 dated strata rolled** — `## ⬛ DO THIS FIRST` measures **10,140 real by `_gm_usage.py`** (and **7,120 tape / 109 raw lines** by this seat's own slice — two slicers, two numbers, neither converted into the other), inside the 120-warn / 180-block cap, and it carries no stratum older than LATEST + 1 to roll; the section is UNCHANGED by this wrap and the `size:` stamp above is THIS session's, re-measured after 2c/2d/2f. **2c, 2d and 2f ALL RAN** (banners 2/2 · deltas 3/3 · strata 1) so no prior-session stamp is being carried. ⛔ **NOT RUN AT THIS SEAT, EACH NAMED:** `_build_all.py` (fenced by the brief) · `gen_kg_edges.py` (fenced by #265, and now ALSO by the `supersedesClause` seam) · the **render gates** (`_validate_screen.py`, `_validate_fit_physics.py`) — a chromium drive in this sandbox now needs `PLAYWRIGHT_BROWSERS_PATH=/tmp/pw` **and** `NODE_EXTRA_CA_CERTS=…` because `/sessions` is **98% full**, and that recipe was not exercised here · `knowledge/_tests/test_gates.py` (copytrees ≈5 GB, ENOSPC).

- ⬛ **STILL HIS, CARRIED FORWARD — RULING-SHAPED AND NOT RULED HERE:** his **N** in *"a 6 month project into N weeks"*, still unfilled · the **car-plant / gearbox metaphor**, his and nowhere in the repo · **the ask** · **the KPIs to predict** · the **SC-dig auto-switch families** (the 0-neighbour RED) · **v1.0.11**, the canon regen the v1.0.10 zip could not carry · the **`DECLARED_EDGE_TYPES` seam** / lane E's **RSQ 2** · **#241's midnight-wrap question, age 25** · the **thirteen #265 lane RSQs** · the **two orphan registrations** · the **eight islands** · the **four PARKED glyphs and `global-money`** · **run-of-show v1's review** · and **the plot-point mapping**, fresh and his to accept. Gauge, declared skips and not-done: above. **`_CARRIES.md` § `## residual → #268` is the carry set (`s225-D2`); the banner carries the pointer and the probeable count.** **This wrap's own filed report (`s218-D7`): `notes/_subreports/2026-09-10-267-W-wrap.md`.**


---

## ⬛ OPEN WORK — GENERATED from `_state.json`. **The row count IS the count.**

> **634 items · 535 live · 255 Dave's · 280 mine · 620 carry a stated close condition · **14 UNCONDITIONED**.**
> *Every figure in this block is computed from the store at generation time. No number here was typed by anyone — that is the point of it (#86 measured a typed inventory of "118 markers" against a real ~40; #85's "95 slots / 84 distinct" is not reproducible by any probe in this repo).*
> ⚠ **DECLARED DEBT — 14 inherited items have NO close condition and therefore cannot close.** They are exempt as a FROZEN set (`_state.LEGACY_IDS`, size 19) which may only shrink; a NEW item is refused without one. **Each needs Dave's word — an agent inventing a close condition for his open work is the same overreach as inventing his ruling.**
>
> **DAVE'S (255) — 255 with a ratified `closes_when`, 0 with NONE · MINE (280). IDS, BODIES AND CLOSE CONDITIONS ARE IN THE STORE, NEVER HERE (s227-D2 shape, ids cut `s241-D2`): the counts above are the inventory; the rows are one command away, named on the next line.**
>
> ⚠ store gate: **PASS**. Bodies, conditions and provenance: `python3 knowledge/_state.py`.


---

*(Chain ends. **10,700 tape (cl100k ESTIMATE) — the unit is THE WHOLE FILE**, this generated wrapper included:
the number in this sentence is the size of the file containing it, held exact by a fixed point.
`GOOD-MORNING.md` is 34,267 tape (cl100k ESTIMATE) and stays whole for retrieval; you have paid for
31% of it. Under 40% is this generator's own floor — above it, the wrapper is
carrying more than the slice, and `--selftest` says so by name.)*
