<!-- GENERATED by knowledge/_gen_chain.py — DO NOT HAND-EDIT.
     Edit GOOD-MORNING.md / _LIVE-STATE.md and regenerate; `--check` is a build step. -->

# The read chain — everything a cold session must read, and nothing else

> **This file is GENERATED and it is the whole contract.** It carries `GOOD-MORNING.md`'s header
> and ★ LATEST banner, plus `_LIVE-STATE.md`'s ⏱ LATEST delta — the three things GM-D7-am names
> (CUT #33 on Dave's ruling). **If you have read this file, you have read the chain.**
>
> ⚠ **Do NOT now open `GOOD-MORNING.md` to "check".** It is 58,139 real and 80% of it is
> retrieval surface you are not meant to pay for at boot. That reflex is the thing this file
> exists to stop — five sessions called the chain CUT and every one of them paid full price.
>
> ★ **Everything else is RETRIEVAL, never a reading list:**
> `python3 knowledge/_memento_search.py "<q>"` → `--fetch <id>`. §A orientation is `gm:A`
> (a router with 11 children); the queue is `gm:C1`…; the archives, briefs, runbooks and ledgers
> are all indexed. **Ask for what you need; do not read a file to find out whether you need it.**

---

> **YOU ARE #154. TITLE THIS CHAT →** `Apollo - #154: regenerate the state-contrast audit — chunk the sweep past the call cap [0]`
> *(read it back in your opener — the chat half of the ritual is ungateable, which is why this line is FIRST)*

---

<!-- ⛔ STOP. If you are a cold session, you are in the wrong file and you pay for ALL of it.
     Read `_CHAIN.md` instead — it is GENERATED from this file's header + ★ LATEST banner and
     `_LIVE-STATE.md`'s ⏱ LATEST delta, which is the ENTIRE contract (GM-D7-am, CUT #33).
     This file stays whole for RETRIEVAL: `_memento_search.py "<q>"` → `--fetch <id>`.
     ⚠ Reading on from here is not thoroughness — it is the exact overspend #41 measured. -->

# Good morning, Dave ☕

> **size:** GM **54.6K tk real** (54636 exact) · §A **7.0K tk real (EXEMPT)** (6,957 exact) · LS **72.6K tk real** (72641 exact) · measured 2026-08-11 #153 (`_gm_usage.py --sizes` → `real`, DECLARE-LAST — after 2c/2d/2f, before the 2g index rebuild), **on the artefact, AFTER the rolls, iterated to a FIXED POINT** [[chain-is-a-swap-not-an-add]]. ⚠ **THE READ-CHAIN FIGURE IS DELIBERATELY ABSENT — its ONE home is `_CHAIN.md`'s own generated footer** (retired #45). **Point at it; never restate it.** ✅ **THE UNIT IS REAL CLAUDE TOKENS (#82-D1, Dave's, ruled #54) — MEASURED, NEVER CONVERTED; ratios REPORTED, NEVER APPLIED.** *(This line's correction-provenance — #90's self-reproduction, the `CHAIN_STAMP_RE` scope hole closed #94, the #90 2f-roll discharge — HOMED BY ADDITION at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET, then cut here [[home-by-addition-then-cut]].)*
> **STATE: ★★ THE READ CHAIN IS CUT (#33) and it HELD — EIGHTH session running.** You are reading the contract. §A and §C are STILL IN THIS FILE, reached by `_memento_search.py`, not by reading top-to-bottom. ⛔ **BUILD VERDICT: 75 of 110 steps green (#62, `18c7789`) — 35 steps have NEVER been in a green verdict.** Both counts GENERATED from `_build_all.py`'s AST at each end; the shortfall is computed (`s125-D1`). A single-process `_build_all.py` run is SANDBOX-IMPOSSIBLE (~49s vs the ~45s call kill); CI delivers the single-process verdict on push. ⚠ **DO NOT RUN `_build_all.py` TO CHECK — ANY PARTIAL run strands the tree in the documented mid-build intermediate** (step 1 rewrites wholesale; healed only by a COMPLETE pass — docstring lines 5–21). ★ **Only the PREMISE of a derived rule is worth re-verifying** (#47's lesson, turned on #47). *(The #47/#61/#62 build-claim archaeology + the #38 cap-repoint finding are HOMED at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET and `notes/_briefs/2026-07-29-cap-repoint-and-lane-divvy-brief.md`.)*
> ⚠ **`set_content()` IS BANNED IN RENDER-PROOFS** — drops `type.css` silently (14px→16px) and `document.fonts.check` passes anyway; use `goto("file://…")` (#29, measured) · **ds-020 is a gate-enforced obligation** (the DV-D02-A scatter waiver names it as clears-when) · **ritual step 2g: rebuild `_memento-index.json` LAST** — `index_freshness_check` is BLOCKING and compares CONTENT, never mtime (runbook has the body).
> **★ PRICE IN REAL TOKENS — THE UNIT IS `_gauge_tokens.py`, NOT A PERCENTAGE (#56, Dave's).** **amber 160,000 (PICKED) · working 200,000 (SOURCED) · hard 256,000 (SOURCED)** — provenance per `_gauge_tokens.py:63–83`, the authority [[read-chain-is-where-staleness-is-free]]. Unit = the FULL price (boot + job + wrap); ⚠ **a DECLARED gap passes, a SILENT one fails** — that asymmetry is the mechanism. The retired 45/60/63 % band is DORMANT in code, pinned — retire-or-pin = `G8`, Dave's. ⚠ **THE FLOOR IS MEASURED = `ds-025` RE-SCOPED #109 — first turn 65,400±1,400 real (n=5) + `_CHAIN.md` 10,499 additive = 75,899 floor** (was published 30,499 — under by 45,400; the OLD `disk 11,032 + harness unreachable` framing on this line was FALSE, retired #109). `MEMORY.md` 8,470 of the first-turn figure is split and measured; **56,308 remains unattributed — that is what `ds-025` item 1 now means.** ⛔★★★ **FIGURE SUPERSEDED #129 (`s129-D1`, Dave's): FLOOR RE-BASED 81,335 → 70,794** (`BOOT_FIRSTTURN_TK` 65,400 → 54,859 ±1,178, n=7). **By ADDITION: the `ds-025` question above is UNCHANGED; only the constant moved.** Authority = `_gauge_tokens.py`, never this line. ⚠ **`pip install tiktoken --break-system-packages` FIRST in a cold sandbox** — the gauge REFUSES, loud and named (`MeasurementRefused`); `_build_all.py` P5 = ABORT without it (#80). ★ **READ THE CHAIN ONLY.** ★ **Repo-state claims are verified against `git log` / a real run, never a banner.** *(The #58/#59/#80/#83 correction-provenance is HOMED at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET; the #109 boot-floor correction at `_DECISION-HISTORY/2026-08-06-109-boot-floor-measured.md` and § ★ #109.)*
> ⚠ **Render sandbox is FRESH every session** (~4 calls — price it) · **retrieval FIRST:** `_memento_search.py "<q>"` → `--fetch <id>` (⚠ **it served a two-session-old record until #32 — if an answer looks stale, check the index**) · **wrap moves RUN THROUGH `_gm_move.py`** (⚠ **banner headings are BLOCKQUOTED — anchor `> ## ★ PRIOR …`**) · **PACE PANEL: ask at the opener, no gate can see it** — Dave has declined a fresh one twice and put Fable on the table for judgment work; ✅ **THE "BEHIND PACE ⇒ MORE WINDOWS" CLAUSE IS RETIRED — RULED #60: multi-window is settled, two use cases (quota catch-up AND production crank), Claude offers/Dave vetoes, cap 3 revisable. `notes/_MEMENTO-DECISIONS.md` § ★ #60-D1.** It stood from #48; at #57 he said the opposite — *"we are behind, but I'm less comfortable with multiple windows, too much to decide and consume"* ⇒ **ONE window with more delegation.** ★ **The clause optimised the wrong quantity: it treated TOKENS as the constraint when DAVE'S DECISION LOAD is what binds.** ⚠ **Reflected back #57, and #58's opener did NOT settle it** (asked as the first question; he answered pace with *"no constraint, lets just fix the problems"*). Standing home + status: §C·4.
> *(**RENAME is delivered IN CHAT at wrap, not here — RULED #28, ENACTED in runbook step 4b by #30.**)*
> **TITLE THE NEXT CHAT →** `Apollo - #154: regenerate the state-contrast audit — chunk the sweep past the call cap [0]`
> ⚠ **CORRECTED IN PLACE BY AMENDMENT #153-A (post-wrap). NOTHING IS LOST — the superseded line is quoted here verbatim:** *"TITLE THE NEXT CHAT → `Apollo - #154: verify s152-D1 — the fix has a runner, the proof does not [0]`"*. **That title is FALSE:** `s152-D1` was verified after the wrap — `--selftest` rc=0, 57 arms, the mutation control green, and the two Selection-controls star reds MEASURED CLEAR (0 MARK failures, was 2). ★ **The correction is made ON the instruction line, not beneath it, because a cold session reads the GENERATED `_CHAIN.md` header and takes the FIRST title it finds** — an addition that sits below a stale instruction does not stop the stale instruction being obeyed [[read-chain-is-where-staleness-is-free]].
> ✅ *(the #86 hand-patch that stood here is REMOVED by the #87 wrap, per its own text — #85/#86 are recorded as HOLE lines + ledger stubs, `notes/_GAUGE-LOG.md` / § ★ #85–#86.)*
> *(Titles are LABELS — role comes from Dave's opener line. Bands: the ONLY copy is
> `_RUNBOOK-context-gauge.md` § ★ THE FLOOR IS MEASURED — grep it, never recall it.)*
> ## ⚠ AMENDMENT **#153-A** — 2026-08-11, POST-WRAP (OPUS amendment sub, after commit `c5b406f`, Dave live — ⛔ **THE "ENVIRONMENTAL FENCE" IN #153 WAS FALSE** · ✅ **`s152-D1` IS ENACTED *AND VERIFIED*** · ✅ **THE TWO STAR REDS ARE MEASURED CLEAR** · ⚠ **AN ARM BIT AND THE BITE WAS THE ARM'S** · ⛔ **NEW NARROW BLOCKER: THE CALL BOUNDARY** · ⛔ **RULES NOTHING**)
> ⚠ **ADDITION ONLY — the ★ LATEST #153 banner below stands unaltered as the record of what that wrap believed** [[header-wins-over-audit]].
> - ⛔ **① THE FENCE WAS FALSE, AND THE BINDING OBSTACLE WAS A RUNBOOK NOBODY OPENED.** #153 inscribed *"chromium would not launch … `libXdamage.so.1` … no root"* as environmental fact. `knowledge/_RUNBOOK-render-verify.md:181` stages those libs with **`apt-get download`**, and **`:186`** says *"needs no root and no `apt-get update`"*. The farm was **already on disk**: `/var/tmp/chromelibs/root/usr/lib/aarch64-linux-gnu/libXdamage.so.1`, dated **Aug 8** — three days before #153. Runbook **`:36`** names the exact error committed: *"(`PLAYWRIGHT_BROWSERS_PATH` alone) was NOT sufficient this time; the full working env, all four."*
> - ★ **② THIRD INSTANCE OF ONE CLASS — AND DAVE CAUGHT IT WITH THE SAME SENTENCE TWICE.** #123 declared a render gap on *"chromium is TLS-blocked in-sandbox"*; #124 carried it forward **as fact**, and Dave stopped it with *"and there is a runbook for chromium and playwright"* (inscribed at that runbook's **`:131`**; the original ask at **`:5`**). Today, #153: *"I think theres a runbook for chromium and playwright"*. **#123 · #124 · #153.** Each refusal was sincere, named — and about the **wrong obstacle** [[feedback-read-the-runbook]] [[refusal-names-the-first-obstacle]].
> - ✅ **③ `s152-D1` IS ENACTED *AND VERIFIED* — RE-DRIVEN FIRST-HAND, NOT RELAYED.** With the runbook's full env (`PYTHONPATH` · `PLAYWRIGHT_BROWSERS_PATH` · `LD_LIBRARY_PATH` · `TMPDIR` · `PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS`): `--selftest` → **rc=0, "selftest OK — 57 arms", zero bites**. All five `s152-D1` arms green, **including the mutation control `arm_removing_the_skip_brings_the_phantom_back`**, which cuts `if(fg&&bodyPaints){` out of MEASURE and demands the 1.662 return [[mutation-tests-the-clause-not-the-feature]].
> - ✅ **④ THE TWO SELECTION-CONTROLS STAR REDS ARE *MEASURED CLEAR*.** `V.run(['Selection-controls'])` driven live: **MARK text failures 0** (was 2) · other text failures **0**. The audit artefact was deliberately **NOT written** (`run()` measures, `main()` writes) — `knowledge/_STATE-CONTRAST-AUDIT.md` held at **15:38:59 / 2,364 B / 21 lines**, checked before and after every drive ⇒ ⚠ **that artefact is now KNOWN-STALE: it still prints 2 reds that no longer reproduce.** Declared, not silently tolerated.
> - ⚠ **⑤ UNIT CORRECTION, CAUGHT HERE.** The relayed *"28 declared MARK SKIPs"* is the **RAW** record count: **42 raw records → 28 raw `markskip`s → 4 UNIQUE lines** after `render_report`'s de-duplication. **The report prints 4.** Both are true of different things; naming which is which is the point [[measure-dont-convert-units]].
> - ⚠ **⑥ AN ARM BIT ON ITS FIRST REAL RUN AND THE BITE WAS THE ARM'S.** `arm_browser_phantom_shape_fill_is_skipped_and_declared` asserted *"no painted fill at all"*; the branch that fires is the **DECLARED-BUT-UNWORN** one, because an `<svg>` with no fill computes to **`rgb(0, 0, 0)`** — **not `none`**. Now asserts the phantom colour explicitly plus *"no descendant shape paints"*. ★ **The mechanism was right and the expectation was wrong** [[a-crash-is-not-a-fail]].
> - ★ **⑦ AN OBSERVATION ABOUT REACH — RULING NOTHING.** 28 raw (4 unique) skips on **one** snippet says the MARK leg was only ever measuring anything real on **genuine roundels**; its former red count **overstated its reach**. `s152-D1`'s open item (*whether any other of the 75 hits the class*) stays **open and unmeasured**.
> - ⛔ **⑧ NEW, NARROW, MEASURED BLOCKER — THE CALL BOUNDARY, NOT THE BROWSER.** A full sweep exceeds the **~178 s** tool-call cap (killed at 177,998 ms). ⚠ **The snippet count is 75, NOT the 38 relayed** — measured here, and `s152-D1`'s own open item already says 75. Timed first-hand: **16.5 s/snippet ⇒ ~1,237 s (~20.6 min), ~7× the cap.** ⇒ **chunk it in the shape of `_build_all.py --range/--resume`** [[sandbox-call-boundary-kills]].
> - ⚠ **⑨ PRIOR RESIDUAL ① CORRECTED.** It said the arms have *"no runner in the sandbox OR in CI"*. **They DO run in the sandbox.** What remains true: **CI still excludes them** — `.github/workflows/gates.yml:20`–`:22`, re-read first-hand. ⬛ **PROPOSAL FOR #154, NOT A DECISION:** a CI render job is now cheap — `gates.yml` runs on `ubuntu-latest`, which **has root**, so `playwright install --with-deps chromium` works with none of the lib-farm choreography. **Dave's call.**
> - ⛔ **⑩ THIS AMENDMENT RULES NOTHING.** `s153-D1` stays **FLOATED, awaiting Dave** — values **NOT invented**, **NOT in `_rulings.json`**. Untouched: the icon-leg phantom residual (declared, unfixed) · all **22** of Dave's open G-items · every ratified stratum (**ADD, never trim**). ✅ **MEMORY updated by hand.** ⛔ **NOT PUSHED.**
>
> ## ★ LATEST — 2026-08-11 (Tue **#153**, OPUS conductor + this OPUS wrap sub, Dave live, ONE ARC — enact `s152-D1`: fix the gate's MARK leg — ⚠ **SUPERSEDED IN PART BY AMENDMENT #153-A ABOVE** — ⛔ **ENACTED, *NOT VERIFIED* — EVERY BROWSER ARM UNRUN** · ✅ **THE FIX LANDED AND KEYS ON *PAINTED*, NOT ON BLACK** · ✅ **THE SKIP IS DECLARED AND MOVES NO RATIFIED COUNT** · ★ **A REFUSAL NAMES THE FIRST OBSTACLE, NOT THE BINDING ONE** · ⛔ **THE TWO STAR REDS STAY KNOWN-FALSE** · ⛔ **THE ICON LEG SHARES THE PHANTOM** · ⛔ **`s153-D1` FLOATED, NOT CONFIRMED** · ⛔ **QUOTA POLL UNANSWERED**)
> 
> - ✅ **① THE FIX — THE MARK LEG NOW RUNS ONLY ON A FILL SOMETHING ACTUALLY WEARS.** `knowledge/_validate_state_contrast.py`: a new `shapeFillStr` + `bodyPaints` probe in MEASURE's `<svg>` branch (**`:336`–`:355`, located by grep by this sub**) requires a descendant shape — **not** in `defs/clipPath/mask/symbol/marker/pattern`, **not** hidden — whose **computed fill string equals the svg's**; if nothing wears it the comparison is **SKIPPED**. ★ Compares **COMPUTED STRINGS, never `parse()`** — parsing every descendant would **mint refusals this change was not ruled to create**. ★ **Keys on PAINTED, not on black:** a genuine black roundel still fails. *(Full: dossier § Finding 1.)*
> - ✅ **② THE SKIP IS DECLARED, NEVER SILENT — AND IT MOVES NO COUNT DAVE RATIFIED.** New `kind:'markskip'` record + `MARKSKIP_PREFIX` (**`:512`**), **deliberately NOT one of the counted prefixes**; surfaced in `render_report` as a section bit **and** a body line. **`verify_report`'s three counter contracts are UNTOUCHED — verified first-hand:** the whole diff (**+127/−5**) REMOVES only two lines.
> - ✅ **③ THREE FIXTURES, FOUR ARMS, A GUARDED MUTATION CONTROL.** Fixtures **`:832`/`:842`/`:849`**, incl. `mark_real_black_roundel_still_fails` — **the DISCRIMINATING one**. Arms **`:1114`–`:1156`**; `_measure_fixtures` gained `script=`/`only=` (**`:856`**, defaults keep existing calls identical). The control **cuts the guard out of MEASURE** and **DEMANDS the 1.66 reappear**; a guard arm **fails loudly** if the target is missing [[mutation-tests-the-clause-not-the-feature]].
> - ⛔ **④ ENACTED, *NOT VERIFIED* — THE HEADLINE, NOT A FOOTNOTE.** `py_compile` rc 0. Every **browser-driven** arm is **UNRUN**: `--selftest` **RE-DRIVEN FIRST-HAND BY THIS SUB — rc=2**, refusing loud and named (`chromium would not launch … libXdamage.so.1`). No root; disk **97%** (re-measured, worse than relayed). ⚠ **AND CI DOES NOT RUN IT EITHER** — `gates.yml:20`–`:22`, read in place. ✅ The **FIX** does have a runner: `_build_all.py:255`/`:261`. ⇒ ★ **A sharper [[instrument-without-a-consumer]]: THE FIX HAS A CONSUMER; THE PROOF DOES NOT.** Only Dave's local render path closes it.
> - ⛔ **⑤ THE TWO SELECTION-CONTROLS STAR REDS STAY *KNOWN-FALSE AND DECLARED* — NOT GREEN.** They clear when the arms are **driven**, not when the code is **written**.
> - ★ **⑥ TWO FINDINGS, ONE FAMILY — AN HONEST REPORT CAN STILL SAY THE WRONG THING.** **(a) A REFUSAL NAMES THE *FIRST* OBSTACLE, NOT THE *BINDING* ONE.** Same command, twice, both `rc=2`, **two DIFFERENT honest refusals**: without `PYTHONPATH=/var/tmp/pysite` it reports *"the 'playwright' module is not installed"* — **false about the disk** (it IS there). An honest gate would have had a session inscribe a falsehood about its own environment; cause: **bash carries no env across a call boundary** [[sandbox-call-boundary-kills]]. **(b) A FAILED WRITE PLUS A SUCCESSFUL READ LOOKS EXACTLY LIKE A SUCCESSFUL RUN** — the conductor's `/var/tmp/st.out` redirect hit permission-denied and `tail` read a **LEFTOVER FILE FROM AN EARLIER SESSION** (*"selftest OK — 25 arms"*), caught **only** because expected arm names were absent [[ritual-output-is-not-evidence]]. ✅ **Remedy, applied on both of this sub's runs: own the path, print mtime-vs-now.**
> - ⛔ **⑦ DECLARED RESIDUAL, NOT FIXED — THE ICON LEG SHARES THE PHANTOM.** `kind:'icon'` at the same site uses the same phantom `fg`: **WARN-only**, **outside `s152-D1`'s scope** — not touched, **not ruled here**. ★ **18 PURE ARMS PASS IN-SANDBOX TODAY** (measured), so residual ② is cheap and real — but rc=2 overall means nothing reports them as a pass.
> - ⚙ **⑧ GAUGE — RELAYED BY THE CONDUCTOR, NOT RE-MEASURED BY THIS SUB, AND SAID SO.** boot **54,723** real, inside the 54,859±1,178 band — a datapoint, **never corrected into the constant** [[boot-floor-measured-109]]. Check-ins at the **opener, MID-LANE (FILL 111,538) and WRAP-OPEN (FILL 140,364 vs stop 150,929 — inside)**. Delegation: **this OPUS wrap sub only**, binding budget **FILL, not quota**. ★ **SECOND ENOSPC DATAPOINT** (n=2, a datapoint — **not** a runbook rewrite): `pip install --target=/var/tmp/pysite` **SUCCEEDED** where the default prefix died.
> - ⛔ **⑨ QUOTA POLLED AT THE OPENER, NOT ANSWERED — an UNANSWERED POLL is NOT a stale reading.** #150's stands: session **7%** · weekly **52%** · Fable **65%**.
> - ⛔ **⑩ THIS WRAP RULED NOTHING, AND THE OMISSIONS ARE NAMED SO THEY ARE CHECKABLE.** ⛔ **`s153-D1` — green/success-ink scope — is FLOATED, *NOT CONFIRMED*:** Dave picked *"mirror the two-red law"* from an option set and the conductor **reflected it back**; he has not confirmed. The **SHAPE** (background-keyed fork) is what he indicated; ⛔ **the two values are NOT ruled and MUST NOT be invented** [[feedback-dont-launder-a-premise-into-a-ruling]]. In the floated register **`_FUTURE-STATE.md` ONLY** — deliberately **not** in `_rulings.json`. ⚠ **The 2d EXIT CHECK BIT:** two #150 items (*generators write by default, no `--dry-run`* · *the token-drift class*) had **no standing home anywhere** and were copied up **before** the #150 delta rolled. Untouched: the icon leg · the two star reds' status · Banner-8 hover-wash · box border · Chart-bar/Reorder carriers · the ~12 `err-msg` classes · hover-wash symmetry · reds **30 · 45-residual · 82** · the snippet gate's **13** fails · all **22** of Dave's open G-items · every ratified stratum (**ADD, never trim**). ✅ **MEMORY updated by hand (step 3 — no gate sees it).** ⛔ **NOT PUSHED.**
> 
> **residual → #154:** ⚠ **AMENDED #153-A BY ADDITION; the list below stands as written and the corrections ride on top —** *(⛔ the amendment note MUST sit AFTER the `#N:**` colon: `RESIDUAL_RE` at `_gen_titles.py:47` anchors on `residual → #(\d+):\*\*`, so prose inserted between the number and the colon makes the line INVISIBLE to the generator, which then matches the PRIOR session's residual and refuses. Broken and repaired #153-A.)* ✅ **① IS CLOSED — `s152-D1` IS VERIFIED** (rc=0, 57 arms, re-driven first-hand post-wrap; the "no runner" premise was false — see AMENDMENT #153-A). ✅ **② IS MOOT — the browser arms all ran**, so the pure-arm workaround is no longer needed for proof (it stays available as cheap CI-friendly coverage). ⬛ **NEW ① — REGENERATE `_STATE-CONTRAST-AUDIT.md` [0 — NEW TOP]:** the artefact on disk is **KNOWN-STALE** (still prints the 2 cleared star reds) and a full **75**-snippet sweep needs **~1,237 s** against a **~178 s** cap ⇒ **chunk it, `_build_all.py --range/--resume` shape** [[sandbox-call-boundary-kills]]. ⬛ **NEW ② — CI RENDER JOB [0]: A PROPOSAL, NOT A DECISION** — `gates.yml:20`–`:22` still excludes the render gates; `ubuntu-latest` has root, so `playwright install --with-deps chromium` would work. **Dave's call.** · *(original text follows, unaltered)* ⬛ **① VERIFY `s152-D1` [0 — NEW TOP]** — the arms have **no runner in the sandbox OR in CI**; drive them on **Dave's local render path** or find a **browser-free proof**. ★ The fix is enacted; the proof is not [[instrument-without-a-consumer]]. · ⬛ **② PURE ARM NOT ADDED [0]** — `render_report`'s markskip line is **unproven**; a PURE arm before `got = _measure_fixtures()` needs **no browser** and would run **in-sandbox today** (18 pure arms already pass there — measured this wrap). Cheap; skipped for budget. · ⬛ **③ THE ICON LEG SHARES THE PHANTOM [0]** — WARN-only, outside scope, declared NOT fixed. · ⬛ **④ `s153-D1` GREEN/SUCCESS-INK SCOPE [2]** — **AWAITING DAVE'S CONFIRMATION. DO NOT RULE, DO NOT INVENT THE VALUES.** · ⬛ **⑤ BANNER 8 HOVER-WASH GAP [3]** · ⬛ **⑥ BOX BORDER: RUNG OR `#F6604C` [4]** — Dave's. · ⬛ **⑦ CHART-BAR 3 + REORDER 1 MEASURED BY NO GATE [2]** — no readable carrier; declared, not invented. · ⬛ **⑧ ~12 `err-msg` CLASSES vs THE TWO-RED LAW [2]** · ⬛ **⑨ HOVER-WASH SYMMETRY [2]** · ⬛ **⑩ GATE REDS 30 · 45-RESIDUAL · 82 [5]** + **SNIPPET GATE 13 FAILS [2]**. · ✅ *`s152-D1` is ENACTED but **NOT CONSUMED** — it stays ① until VERIFIED; enactment is not proof.* · **PRIOR CARRIES, ROLLED UNCHANGED, AGES INCREMENTED** *(2c EXIT CHECK, off the rolling #151 banner — nothing orphaned)*: ⬛ `_validate_kg` `check_freshness` TEMPFILE **[6]** · ⬛ `success-ink` BINDING SITE **[7]** · ⬛ NO DARK SELECTED-ROW TOKEN **[7]** · ⬛ UNVERIFIED `#1A1A1A` ON DARK RAG TINTS **[7]** · ⬛ `tooltip.tip` **[7]** + intent/capability VOCABULARIES **[9]** · ⬛ MSGFILE GATE **[9 — ×6]** · ⬛ RENDER-PROOF DEBT ×2 **[9]** · ⬛ #139 QUEUE **[10]** · ⬛ MEMENTO-INDEX REMAINDER **[9]** · ⬛ STEP-0 CONFIRM-RETIRE **[8]** · ⬛ ENOSPC RUNBOOK CORRECTION **[9 — now n=2]** · ⬛ META REFLOW COSMETIC **[10]** · ⬛ THE 8 `DEF-COLOR-MISTYPE` OPACITY DEFERRALS **[7]** · ⬛ `_validate_package_delta.py` DOCSTRING PROSE **[9]** · ⬛ **NEW HOMES, NOT NEW WORK:** generators-write-by-default **[0]** + the token-drift class **[0]**, homed in `_FUTURE-STATE.md` by this wrap's 2d EXIT CHECK.
> **residual (GENERATED #153):** 2c OK (banners 2/2) · 2d OK (deltas 3/3) · 2f OK (strata 1, log #152) — _roll_state.py · 2026-08-11

> **⬛ OPEN WORKLIST — PRESENCE INDEX (28 items, GENERATED). Every open item is named; NO bodies are here — `--fetch gm:DOFIRST`.**
> `0b` ENCODE BEFORE THE WAVE, part-consumed 28 · `0c` NEXT BUILD CANDIDATES · `0d` THE 67 ENACT WAVE, landed · `1` ds-018 C2 follow-through · `2` dv-legend/dv-behaviour CEILING · `3` ds-012(b) gutter-relative plot area · `4` DV-D16 floating growth · `5` Instrument-fit remainder · `6` ds-016, PART-RULED #114 · `7` ds-017, UNRULED · `8` STILL OWED, none superseded · `9` DELEGATION TOPOLOGY · `10` ✅ PER-GATE TEST PLAN · `11` 2c-ROLL / INDEX-VOCAB DEADLOCK · `12` #57 1b DOSSIER · `13` tmp RUNBOOK EXPOSURE, unfixed · `14` ⬛ DAVE'S FOUNDING PRINCIPLE · `15` LEDGER § ★ #59 · `16` UNHOMED PAIR (#78 2c EXIT CHECK) · `17` CHAIN-DIET CUT · `19` type.css 180 dark-mode specificity · `20` ds-029 FIRST IDIOM · `21` PER-THEME COLLISION SWEEP · `22` 89-D2 ruled not enacted, absent from state · `23` ds-025 re-scoped 109, not closed · `24` 110 boot-rent plan · `25` ds-032 enactment, ruled 106 · `26` ✅ boot-drift gate rulings
> **QUEUE — `gm:C1` strands · `gm:C2` ruling batch (Dave's) · `gm:C4` enact-queue.**

---

## ⏱ AMENDMENT **#153-A** — 2026-08-11, POST-WRAP (OPUS amendment sub, after commit `c5b406f`, Dave live — ⛔ **THE "ENVIRONMENTAL FENCE" BELOW WAS FALSE, AND THE BINDING OBSTACLE WAS A RUNBOOK NOBODY OPENED** · ✅ **`s152-D1` IS ENACTED *AND VERIFIED* — rc=0, 57 ARMS, RE-DRIVEN FIRST-HAND HERE** · ✅ **THE TWO STAR REDS ARE *MEASURED CLEAR*, NOT MERELY DECLARED KNOWN-FALSE** · ⚠ **AN ARM BIT ON ITS FIRST REAL RUN AND THE BITE WAS THE ARM'S** · ⛔ **THE OLD BLOCKER IS REPLACED BY A NARROW, MEASURED ONE: THE CALL BOUNDARY** · ⛔ **STILL RULES NOTHING**)

⚠ **ADDITION ONLY. Every word of the `#153` stratum below stands unaltered as the record of what that wrap believed and why** [[header-wins-over-audit]]. This section corrects it; it does not replace it.

- ⛔ **THE ENVIRONMENTAL FENCE WAS FALSE — AND THIS IS THE FINDING, MORE THAN THE FIX IS.** The `#153` wrap inscribed *"chromium would not launch … `libXdamage.so.1` … no root"* as an environmental fact. It is not one. `knowledge/_RUNBOOK-render-verify.md` **`:181`** stages the missing libs with **`apt-get download`** — and **`:186`** says in as many words *"(`apt-get download` needs no root and no `apt-get update`.)"*. The lib farm was **ALREADY ON DISK THE WHOLE TIME**: `/var/tmp/chromelibs/root/usr/lib/aarch64-linux-gnu/libXdamage.so.1`, in a directory dated **Aug 8 15:21** — three days before `#153`. ⚠ **Verified first-hand here:** the brief relayed the farm's path as `/var/tmp/chromelibs`; at that level there are only `.deb` files, and the extracted `.so`s live one `root/usr/lib/aarch64-linux-gnu` deeper — **that path IS the `LD_LIBRARY_PATH` the runbook prints at `:44`**, so the correction is a precision, not a reversal. Runbook **`:36`** names the exact error committed: *"(`PLAYWRIGHT_BROWSERS_PATH` alone) was NOT sufficient this time; the full working env, all four."* The `#153` conductor set `PLAYWRIGHT_BROWSERS_PATH`, hit the lib error, tried `apt-get install` (root, refused), and declared a fence — **without opening the runbook that answers it** [[feedback-read-the-runbook]].
- ★ **THIS IS THE THIRD INSTANCE OF ONE CLASS, AND DAVE CAUGHT IT BOTH TIMES WITH ALMOST THE SAME SENTENCE.** `#123` declared a render gap on *"chromium is TLS-blocked in-sandbox"*; `#124` carried that forward **as fact**; Dave stopped it with *"and there is a runbook for chromium and playwright"* — his words are inscribed at `_RUNBOOK-render-verify.md` **`:131`**, and the runbook's own opening at **`:5`** records the same ask (*"there should be a runbook for chromium and playwright"*). Today, at `#153`, he said *"I think theres a runbook for chromium and playwright"*. ⛔ **Three instances — `#123`, `#124`, `#153` — and on all three the agent's own refusal was sincere, named, and about the WRONG obstacle.** The runbook existed each time; the cost each time was that nobody opened it [[refusal-names-the-first-obstacle]].
- ✅ **`s152-D1` IS ENACTED *AND VERIFIED*. RE-DRIVEN FIRST-HAND BY THIS SUB, NOT RELAYED.** With the runbook's full env (`PYTHONPATH=/var/tmp/pysite` · `PLAYWRIGHT_BROWSERS_PATH=/var/tmp/pw-browsers` · `LD_LIBRARY_PATH=/var/tmp/chromelibs/root/usr/lib/aarch64-linux-gnu` · `TMPDIR=/var/tmp` · `PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS=1`), `python3 _validate_state_contrast.py --selftest` → **rc=0, `"selftest OK — 57 arms"`, zero bites**, `grep -c '^  ok '` = **57**. All five `s152-D1` arms green, **including `arm_removing_the_skip_brings_the_phantom_back`** — the mutation control that cuts `if(fg&&bodyPaints){` out of MEASURE and demands the 1.662 return [[mutation-tests-the-clause-not-the-feature]]. Conductor's receipt: `/sessions/<s>/mnt/outputs/s153-selftest-final.txt`; this sub's independent re-drive is the claim of record.
- ✅ **THE TWO SELECTION-CONTROLS STAR REDS ARE CLEARED *WITH A MEASUREMENT*, NOT WITH AN ARGUMENT.** `V.run(['Selection-controls'])` driven live here: **MARK text failures 0** (was **2**) · **other text failures 0** · total TEXT failures **0**. They are no longer *known-false-and-declared*; they are **measured clear**. ⚠ **The audit artefact was deliberately NOT written** — `run()` measures, `main()` writes (`:1206`); `knowledge/_STATE-CONTRAST-AUDIT.md` mtime held at **2026-08-11 15:38:59, 2,364 bytes, 21 lines**, byte-identical **before and after every drive in this session — checked, not assumed**. ⇒ **the artefact on disk is now KNOWN-STALE: it still prints the 2 reds that no longer reproduce.** Declared, not silently tolerated [[enactment-register-adr-0016]].
- ⚠ **UNIT CORRECTION — THE "28 SKIPS" IS A *RAW* COUNT, AND THE REPORT WOULD PRINT *4*.** The brief relayed **28 declared MARK SKIPs**; re-measured here, `Selection-controls` yields **42 raw records → 28 raw `markskip`s → 4 UNIQUE `markskip` lines** after `render_report`'s own de-duplication key (`:632`–`:635`). Both numbers are true of different things; **the audit reports the de-duplicated one**. Naming which is which is the whole point [[measure-dont-convert-units]].
- ⚠ **AN ARM BIT ON ITS FIRST REAL RUN, AND THE BITE WAS THE ARM'S — NOT THE MECHANISM'S.** `arm_browser_phantom_shape_fill_is_skipped_and_declared` asserted the reason string *"no painted fill at all"*. The branch that actually fires is the **DECLARED-BUT-UNWORN** one, because an `<svg>` with no `fill` attribute computes to **`rgb(0, 0, 0)`** — which is **not `none`** — so `shapeFillStr` is truthy. Corroborated first-hand against the conductor's pre-fix receipt `s153-selftest-runbook-env.txt`, which carries the bite verbatim. Remedy in `knowledge/_validate_state_contrast.py`: assert the phantom colour **explicitly** (`fill == "rgb(0, 0, 0)"`) plus `"no descendant shape paints"`, so the arm can no longer pass against the other branch by accident, with the reason inscribed in a comment above it. ★ **A bite that names its own assertion is the arm working** [[a-crash-is-not-a-fail]].
- ★ **AN OBSERVATION ABOUT THE LEG'S REAL REACH — AND IT RULES NOTHING.** 28 raw (4 unique) declared skips on **one** snippet says the MARK leg was only ever measuring anything real on **genuine roundels**; its former red count **overstated its reach**. Recorded as an observation only. ⛔ **No rule, no scope change, no ruling is derived from it here** — `s152-D1`'s own open item (*"whether any other snippet in the 75 hits the same class — unmeasured"*) stays open and unmeasured.
- ⛔ **THE OLD BLOCKER IS RETIRED AND REPLACED BY A NARROW, MEASURED ONE: THE CALL BOUNDARY, NOT THE BROWSER.** A full sweep of `_validate_state_contrast.py` **exceeds the ~178 s tool-call cap** (the conductor's run was killed at **177,998 ms**). ⚠ **THE BRIEF SAID "38 SNIPPETS" AND THAT NUMBER IS WRONG — MEASURED HERE: `knowledge/snippets/*.reference.html` = **75**,** which is also the figure `s152-D1`'s own open item already uses. Timed first-hand on a 3-snippet drive: **49.5 s ⇒ 16.5 s/snippet ⇒ ~1,237 s (~20.6 min) for 75** — roughly **7× the cap**, not marginally over it. ⇒ **the audit cannot be regenerated in one call and needs chunking in the shape of `_build_all.py --range/--resume`** [[sandbox-call-boundary-kills]]. The browser is fine; **the call boundary is the limit** — and unlike the fence it replaces, this one is measured.
- ⚠ **CORRECTING THE PRIOR WRAP'S OWN RESIDUAL ①.** It said the arms have *"no runner in the sandbox OR in CI"*. **They DO run in the sandbox**, via the runbook — proven above. What remains true is that **CI still excludes them**: `.github/workflows/gates.yml` **`:20`–`:22`**, re-read first-hand and quoted — *"The render-dependent checks (`_validate_state_contrast.py`, `_validate_screen.py --render`) are NOT run here — they need a browser engine."* ⇒ **`s152-D1`'s proof now has a runner in the sandbox but still none in CI.**
- ⬛ **PROPOSAL FOR #154 — NOT A DECISION, AND NOT RULED HERE.** A CI render job is now **cheap and live**: `gates.yml` runs on `ubuntu-latest`, which **has root**, where `playwright install --with-deps chromium` works without any of the lib-farm choreography. That would give the browser arms a runner in CI as well as in the sandbox. ⛔ **Put to Dave as a proposal; his call, not this sub's** [[feedback-dont-launder-a-premise-into-a-ruling]].
- ⛔ **THIS AMENDMENT RULES NOTHING.** `s153-D1` (green/success-ink) stays **FLOATED, AWAITING DAVE'S CONFIRMATION** — the SHAPE only, **the two values are NOT ruled and are NOT invented here**, and it stays in `_FUTURE-STATE.md` and **deliberately out of `knowledge/_rulings.json`**. Untouched and carried unchanged: the **icon-leg phantom residual** (declared, unfixed) · all **22** of Dave's open G-items · every ratified stratum (**ADD, never trim**) · the CI job (proposal only). ⛔ **NOT PUSHED** — push only on Dave's explicit word, not given to this sub.

Dossier addendum: `_DECISION-HISTORY/2026-08-11-153-the-phantom-fill-and-the-refusal-that-named-the-wrong-obstacle.md` § **AMENDMENT #153-A**.



---

## ⬛ OPEN WORK — GENERATED from `_state.json`. **The row count IS the count.**

> **36 items · 36 live · 22 Dave's · 14 mine · 17 carry a stated close condition · **19 UNCONDITIONED**.**
> *Every figure in this block is computed from the store at generation time. No number here was typed by anyone — that is the point of it (#86 measured a typed inventory of "118 markers" against a real ~40; #85's "95 slots / 84 distinct" is not reproducible by any probe in this repo).*
> ⚠ **DECLARED DEBT — 19 inherited items have NO close condition and therefore cannot close.** They are exempt as a FROZEN set (`_state.LEGACY_IDS`, size 19) which may only shrink; a NEW item is refused without one. **Each needs Dave's word — an agent inventing a close condition for his open work is the same overreach as inventing his ruling.**
>
> **DAVE'S (22) — 17 with a ratified `closes_when`, 5 with NONE:**
> - `G1` **Worklist-index cap DOFIRST_INDEX_TK_MAX = 700 (_capture_gate.py:1403,…** — *closes when:* Dave ratifies 700 or names his own number
> - `G10` **The "70%/95%" stray band (GM:36** — *closes when:* Dave rules provenance or strikes it
> - `G11` **DS-018 recessive value** — *closes when:* Dave picks the recessive value
> - `G12` **Charter §4b tone-of-voice temperature map, PROVISIONAL 2026-07-02…** — *closes when:* tov-016 review lands, or Dave ratifies the mapping as-is
> - `G13b` **menu-search combined glyph, PROVISIONAL 2026-07-16 (_ICON-GAPS.md; the…** — *closes when:* Dave approves the glyph
> - `G14` **Icon-button dark bindings** — *closes when:* Dave rules the SC-dark bindings
> - `G15` **DV-D13 donut centre figure + st.visible[id]=true release wiring…** — *closes when:* Dave's sign-off eye
> - `G16` **The _proforma/_DATAVIZ-DECISIONS.md:567 enactment call (agent's, not…** — *closes when:* Dave ratifies or reverses
> - `G17` **RAG status manifestation…** — *closes when:* Dave's canon pick — A / A+B / A+B+C
> - `G2` **TAPE_TO_BILL = 1.57 at n=2 (_capture_gate.py:371; RATIO_FIRM_N = 4 per…** — *closes when:* 4 measured pairs logged, then Dave rules firm-or-retire
> - `G3` **retired_unit_prose_audit tier = WARN (_capture_gate.py:2078, 2098,…** — *closes when:* Dave rules warn vs block
> - `G4` **GM §C measured 191 > 150 warn cap (_capture_gate.py:2864,…** — *closes when:* Dave picks OFFLOAD / TRIM / KEEP
> - `G5` **Four advisory size caps as a set (_capture_gate.py:4843–4858** — *closes when:* Re-measured in real (G9 first), then Dave ratifies the set in one pass
> - `G6` **DEFER_STREAK = 6 (_gm_usage.py:353) + USAGE_HISTORY_BLOCKING = False** — *closes when:* Dave rules the streak number + per-candidate OFFLOAD/TRIM/KEEP calls
> - `G7` **Which end of an archive is "newest" (strata oldest-first · archives…** — *closes when:* Dave names the convention — one sentence
> - `G8` **Dormant % band BAND_FLOOR/HARD_STOP/MARKED_MAX = 45/60/63…** — *closes when:* Dave says retire or pin
> - `G9` **ds-023 re-measurement programme** — *closes when:* Dave rules the programme
> - `W-0c` **NEXT BUILD CANDIDATES** — *closes when:* ⛔ **none stated — cannot close until you name one**
> - `W-0d` **✅ THE #67 ENACT WAVE** — *closes when:* ⛔ **none stated — cannot close until you name one**
> - `W-14` **⬛ DAVE'S FOUNDING PRINCIPLE** — *closes when:* ⛔ **none stated — cannot close until you name one**
> - `W-15` **LEDGER § ★ #59** — *closes when:* ⛔ **none stated — cannot close until you name one**
> - `W-16` **UNHOMED PAIR, copied up at the #78 2c EXIT CHECK** — *closes when:* ⛔ **none stated — cannot close until you name one**
>
> **MINE (14), ids only — bodies are in the store, not here:** `W-0b` · `W-01` · `W-02` · `W-03` · `W-04` · `W-05` · `W-06` · `W-07` · `W-08` · `W-09` · `W-10` · `W-11` · `W-12` · `W-13`
>
> ⚠ store gate: **PASS**. Bodies, conditions and provenance: `python3 knowledge/_state.py`.


---

*(Chain ends. **18,556 real — the unit is THE WHOLE FILE**, this generated wrapper included:
the number in this sentence is the size of the file containing it, held exact by a fixed point.
`GOOD-MORNING.md` is 58,139 real and stays whole for retrieval; you have paid for
32% of it, and that is the intended figure, not a shortfall.)*
