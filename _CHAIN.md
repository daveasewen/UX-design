<!-- GENERATED by knowledge/_gen_chain.py — DO NOT HAND-EDIT.
     Edit GOOD-MORNING.md / _LIVE-STATE.md and regenerate; `--check` is a build step. -->

# The read chain — everything a cold session must read, and nothing else

> **This file is GENERATED and it is the whole contract.** It carries `GOOD-MORNING.md`'s header
> and ★ LATEST banner, plus `_LIVE-STATE.md`'s ⏱ LATEST delta — the three things GM-D7-am names
> (CUT #33 on Dave's ruling). **If you have read this file, you have read the chain.**
>
> ⚠ **Do NOT now open `GOOD-MORNING.md` to "check".** It is 35,846 tape (cl100k ESTIMATE); this file already
> carries the part of it a cold session needs, and the rest is retrieval surface you are not
> meant to pay for at boot. That reflex is the thing this file
> exists to stop — five sessions called the chain CUT and every one of them paid full price.
>
> ★ **Everything else is RETRIEVAL, never a reading list:**
> `python3 knowledge/_memento_search.py "<q>"` → `--fetch <id>`. §A orientation is `gm:A`; the queue is `gm:C1`…; the archives, briefs, runbooks and ledgers are all indexed.
> **Ask for what you need; do not read a file to find out whether you need it.**

---

> **YOU ARE #288. TITLE THIS CHAT →** `Apollo - #288: the strand map and the path to friday the 25th`
> *(read it back in your opener — the chat half of the ritual is ungateable, which is why this line is FIRST)*

---

<!-- ⛔ STOP. If you are a cold session, you are in the wrong file and you pay for ALL of it.
     Read `_CHAIN.md` instead — it is GENERATED from this file's header + ★ LATEST banner and
     `_LIVE-STATE.md`'s ⏱ LATEST delta, which is the ENTIRE contract (GM-D7-am, CUT #33).
     This file stays whole for RETRIEVAL: `_memento_search.py "<q>"` → `--fetch <id>`.
     ⚠ Reading on from here is not thoroughness — it is the exact overspend #41 measured. -->

# Good morning, Dave ☕

> ⚠ **WRAP DATE SPLIT — SESSION 2026-09-02, COMMIT 2026-09-03.** #241's ritual was authored 09-02 and steps 1→4c landed that night; the commit did not, and step 5 was finished the next morning. ⛔ **No key, filename or stamp was re-dated to match the commit** — that would be the T-D12 false inscription in reverse; this line carries the commit date so the gate's `is not today` check grades a true statement. Detail + the ruling-shaped question in `_LIVE-STATE.md`'s header block.
> ⚠ **WRAP DATE SPLIT, SECOND OCCURRENCE — SESSION 2026-09-05, RITUAL + COMMIT 2026-09-06.** #248's conductor cut the wrap brief at 183,983 real FILL on the evening of 09-05; the delegated wrap sub ran every step the next morning. ⛔ **No key, filename or stamp was re-dated to match the commit** — the #241 shape, by ADDITION (the #241 line above stands; its ruling-shaped question, what a midnight-spanning wrap should stamp, is still Dave's at age 7). This line carries the commit date so the gate's `is not today` check grades a true statement. Detail in `_LIVE-STATE.md`'s header block.
> ⚠ **WRAP DATE SPLIT, THIRD OCCURRENCE — SESSION OPENED 2026-09-08 EVENING, RITUAL + COMMIT 2026-09-09.** #261's R and D3 lanes committed on the evening of 09-08 (22:11–22:44); the design wave, the revision seats and this ritual all ran on 09-09. ⛔ **No key, filename or stamp was re-dated to match the commit** — the eleven filed reports keep their `2026-09-08-261-*` stems, and each of the nine `s261-D*` rulings carries in its own `evidence` the sentence saying the hour was not recorded at the wrap seat, so the date it carries is the COMMIT date and not a claim about the hour. The #241 shape by ADDITION: both lines above stand, and #241's ruling-shaped question — what a midnight-spanning wrap should stamp — is still Dave's, now at age 20. Detail in `_LIVE-STATE.md`'s ⏱ LATEST DELTA.
> ⚠ **WRAP DATE SPLIT, FOURTH OCCURRENCE ON THIS RUN — SESSION OPENED 2026-09-15 EVENING, WORK + RITUAL + COMMIT 2026-09-16.** #277 opened on the evening of 09-15 and six of its commits carry that date (`f84eb78` · `8c80aa2` · `d84eb72` · `66fdc49` · `81e845a` · `ca294b4`); the three exports, the thirteen rulings 590 → 603, the charts enactment and this ritual are all 09-16, seventeen commits. ⛔ **No key, filename, ruling id or stamp was re-dated to match the ritual** — the #241 shape by ADDITION (the three lines above stand verbatim), and this line carries the ritual's date so the gate's `is not today` check grades a true statement. #241's ruling-shaped question — what a midnight-spanning wrap should stamp — is still Dave's, now at age 36. Detail in `_LIVE-STATE.md`'s ⏱ LATEST DELTA.

> **size:** GM **35.2K tape** (35,238 exact, whole-file `tiktoken cl100k_base` — the figure this stamp is graded against at 10% tolerance) · §A **7.0K tape** (6,957 real by `_gm_usage.py`) — ✅ **THE §A BYTE-IDENTITY PROBE WAS RUN AT THIS SEAT AND §A IS BYTE-IDENTICAL TO ITS STATE AT THE #272…#286 WRAPS** (`sha256 4311cce4…` over the gate's OWN pinned probe shape `_capture_gate.section_a_digest`, returning exactly what all fifteen of those stamps recorded — **§A has not moved in NINETEEN sessions**; the gate reports it EXEMPT by ruling: measured and reported, never charged). ⚠ **THE TWO-PROBE-SHAPE QUESTION #272 RAISED IS STILL NOT SETTLED AND IS NOT SETTLED HERE:** #269/#270/#271 recorded `sha256 b9aef5f5…` over 169 lines and #272…#287 record `4311cce4…` over 198 — two probe shapes, not a change in §A, and **which shape the stamp should quote is ruling-shaped and remains Dave's, now at its NINETEENTH session, standing in `_CARRIES.md` § `residual → #288`.** · LS **70.3K tape** (70,303 real) · corpus GM+LS **105.5K tape** (105,541 real, the RETRIEVAL surface, not the chain) · ⛔ **the chain's own size is NOT copied here — RETIRED #45; its ONE home is `_CHAIN.md`'s generated footer, exact by construction and `--check`-blocked** · measured 2026-09-19 at the #287 wrap, DECLARE-LAST. ★ **AND THIS TIME THE ROLLS DID PAY FOR THEIR REPLACEMENT ON GM, WHICH IS THE FIRST TIME IN THREE WRAPS:** against #286's stamp (**35,503 / 69,049 / 104,552 tape**) GM is **-265 tape** while LS is **+1,254** and the corpus **+989** — ⛔ **so the corpus still grew, and the growth is entirely in `_LIVE-STATE.md`**, which is where `s241-D2` says gauge, declared-skip and 5b detail belongs and is therefore the growth the cap DESIGNS FOR rather than a leak. ⛔ **THE BANNER ITSELF CAME IN UNDER THE CAP ON THE FIRST PASS — 1,145 of the `s241-D2` 1,200 over 10 of 10 substantive lines**, and what paid for the fit is that FOUR carries closed. ⛔ **#272…#286's standing warning that the next wrap has no room in this idiom is NOT withdrawn, and the cap is Dave's.** ⛔ **NEITHER FILE SIZE IS A TRIM ORDER; all of these are measurements, and what to do about them is Dave's.** ⚠ **AND TWO DRIFTS ARE DECLARED RATHER THAN LEFT TO BE DISCOVERED.** (1) **Both this stamp and the `section-sizes` line measure regions that CONTAIN them**, so each was ITERATED TO A FIXED POINT rather than published at a reading taken before it was written — the #240 lesson, mechanised in `notes/_lanes/287/W2/ops287_stamp.py` and `ops287_sizes.py`. (2) **The `s214-D6` chain figure and STEP 5b both land AFTER this stamp** (the stamp is itself inside the chain slice — the #241 rule), so they move GM and `_LIVE-STATE.md` by single-digit-percent tape against a 10% grading tolerance and **the stamp is NOT re-taken for them** — saying so is cheaper and truer than a second reading nobody can reconcile.
> **STATE: ★★ THE READ CHAIN IS CUT (#33) and it HELD.** *(the session-count ordinal is DELETED per s178-D1 enacting dream-P1 — stale twice ⇒ generate, never re-stamp)* You are reading the contract. §A and §C are STILL IN THIS FILE, reached by `_memento_search.py`, not by reading top-to-bottom. ⛔ **BUILD VERDICT: 75 of 144 steps green (#62, `18c7789`) — 69 steps have NEVER been in a green verdict.** Both counts GENERATED from `_build_all.py`'s AST at each end; the shortfall is computed (`s125-D1`). A single-process `_build_all.py` run is SANDBOX-IMPOSSIBLE (~49s vs the ~45s call kill); CI delivers the single-process verdict on push. ⚠ **DO NOT RUN `_build_all.py` TO CHECK — ANY PARTIAL run strands the tree in the documented mid-build intermediate** (step 1 rewrites wholesale; healed only by a COMPLETE pass — docstring lines 5–21). ★ **Only the PREMISE of a derived rule is worth re-verifying** (#47's lesson, turned on #47). *(The #47/#61/#62 build-claim archaeology + the #38 cap-repoint finding are HOMED at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET and `notes/_briefs/2026-07-29-cap-repoint-and-lane-divvy-brief.md`.)*
> ⚠ **`set_content()` IS BANNED IN RENDER-PROOFS** — drops `type.css` silently (14px→16px) and `document.fonts.check` passes anyway; use `goto("file://…")` (#29, measured) · **ds-020 is a gate-enforced obligation** (the DV-D02-A scatter waiver names it as clears-when) · **ritual step 2g: rebuild `_memento-index.json` LAST** — `index_freshness_check` is BLOCKING and compares CONTENT, never mtime (runbook has the body).
> **★ PRICE IN REAL TOKENS — THE UNIT IS `_gauge_tokens.py`, NOT A PERCENTAGE (#56, Dave's).** **amber 160,000 (PICKED) · working 200,000 (SOURCED) · quality-max 256,000 (SOURCED)** — provenance per `_gauge_tokens.py:63–83`, the authority [[read-chain-is-where-staleness-is-free]]. Unit = the FULL price (boot + job + wrap); ⚠ **a DECLARED gap passes, a SILENT one fails** — that asymmetry is the mechanism. The retired 45/60/63 % band is DORMANT in code, pinned — retire-or-pin = `G8`, Dave's. ⚠ **THE FLOOR IS MEASURED = `ds-025` RE-SCOPED #109 — first turn 65,400±1,400 real (n=5) + `_CHAIN.md` 10,499 additive = 75,899 floor** (was published 30,499 — under by 45,400; the OLD `disk 11,032 + harness unreachable` framing on this line was FALSE, retired #109). `MEMORY.md` 8,470 of the first-turn figure is split and measured; **56,308 remains unattributed — that is what `ds-025` item 1 now means.** ⛔★★★ **FIGURE SUPERSEDED #129 (`s129-D1`, Dave's): FLOOR RE-BASED 81,335 → 70,794** (`BOOT_FIRSTTURN_TK` 65,400 → 54,859 ±1,178, n=7). **By ADDITION: the `ds-025` question above is UNCHANGED; only the constant moved.** Authority = `_gauge_tokens.py`, never this line. ⚠ **`pip install tiktoken --break-system-packages` FIRST in a cold sandbox** — the gauge REFUSES, loud and named (`MeasurementRefused`); `_build_all.py` P5 = ABORT without it (#80). ★ **READ THE CHAIN ONLY.** ★ **Repo-state claims are verified against `git log` / a real run, never a banner.** *(The #58/#59/#80/#83 correction-provenance is HOMED at `notes/_MEMENTO-DECISIONS.md` § ★ #105 CHAIN-DIET; the #109 boot-floor correction at `_DECISION-HISTORY/2026-08-06-109-boot-floor-measured.md` and § ★ #109.)*
> ⚠ **Render sandbox is FRESH every session** (~4 calls — price it) · **retrieval FIRST:** `_memento_search.py "<q>"` → `--fetch <id>` (⚠ **it served a two-session-old record until #32 — if an answer looks stale, check the index**) · **wrap moves RUN THROUGH `_gm_move.py`** (⚠ **banner headings are BLOCKQUOTED — anchor `> ## ★ PRIOR …`**) · **PACE PANEL: ask at the opener, no gate can see it** — Dave has declined a fresh one twice and put Fable on the table for judgment work; ✅ **THE "BEHIND PACE ⇒ MORE WINDOWS" CLAUSE IS RETIRED — RULED #60: multi-window is settled, two use cases (quota catch-up AND production crank), Claude offers/Dave vetoes, cap 3 revisable. `notes/_MEMENTO-DECISIONS.md` § ★ #60-D1.** It stood from #48; at #57 he said the opposite — *"we are behind, but I'm less comfortable with multiple windows, too much to decide and consume"* ⇒ **ONE window with more delegation.** ★ **The clause optimised the wrong quantity: it treated TOKENS as the constraint when DAVE'S DECISION LOAD is what binds.** ⚠ **Reflected back #57, and #58's opener did NOT settle it** (asked as the first question; he answered pace with *"no constraint, lets just fix the problems"*). Standing home + status: §C·4.
> *(**RENAME is delivered IN CHAT at wrap, not here — RULED #28, ENACTED in runbook step 4b by #30.**)*
> **TITLE THE NEXT CHAT →** `Apollo - #288: the strand map and the path to friday the 25th`
> ✅ *(the #86 hand-patch that stood here is REMOVED by the #87 wrap, per its own text — #85/#86 are recorded as HOLE lines + ledger stubs, `notes/_GAUGE-LOG.md` / § ★ #85–#86.)*
> *(Titles are LABELS — role comes from Dave's opener line. Bands: the ONLY copy is
> `_RUNBOOK-context-gauge.md` § ★ THE FLOOR IS MEASURED — grep it, never recall it.)*
> ## ★ LATEST — 2026-09-19 (Sat **#287**, Fable 5.1, 5 Opus lanes, DELEGATED wrap — ★★ **THE RULINGS LAND AND THE STRAND MAP IS ORDERED**)
>
> - ⓪ ✅ **NO DATE SPLIT** (09-19). **1 commit `75a490cd`, pushed and VERIFIED at lane C.** ⛔ **`_HANDOFF-138` FIRST — IT OUTRANKS `_CHAIN.md`**; #130–#137 stand, **only the 4 below closed**. Words: `notes/_lanes/287/`.
> - ★★★ ① **TWO RULINGS INSCRIBED, 620 → 622** — `json.load` here, no dupe. His whole reply: ***"1. inscribe · 2. keep `sizes` and rebuild. · 3. do it"***. **`s287-D1`** (the 8 standing lines ratified AS WRITTEN, the 256,000 fix) · **`s287-D2`** (a master is a SIZE FIELD on the existing node). ★ **The JOIN is named on `s287-D1`'s face: #286's reading was the CONDUCTOR'S; *"inscribe"* is his.**
> - ★★ ② **THE EXPLORER READS `sizes`** — builder **v1.27**, three new reads, a MASTERS tab. **8/8 logo nodes, 40 entries byte-identical to `_logo_nodes.json`, PARSED out of the built page, not eyeballed**; 8 nodes · 33 edges either side. ⚠ 2,256 coords moved — **the Constitution's growth, not the field's**.
> - ★★ ③ **THE 256,000 WALL WORDING IS SWEPT on *"do it"* — 58 → 42, and ZERO of the 42 assert a wall.** ⛔ **NO NUMBER MOVED** (`BUDGET_HARD` still `256_000`). Dave's `says` fields UNTOUCHED; 6 records ANNOTATED by appended clause, never re-worded. Generated surfaces REGENERATED.
> - ★★★ ④ **BOOT COLD 74,120 v 73,832, Δ +288 ⇒ THE −7,031 CONNECTOR LEVER IS A MEASURED EFFECT (n=2)**, meeting the acceptance test #286 wrote for itself (prior spread 11,526). ⛔ **12th reading over the 70,000 ceiling — SHRINK-ONLY, NOT a re-base.**
> - ⬛★★★ ⑤ **HIS WRAP CALL, AND #288 OPENS ON THE THIRD:** Mono's 0 **DOUBTED** — a proper review and **BUILD the `s219-D3` arm** (*"the one-shot design to not disappoint"*) · the template's **QUALITY** reviewed · **A STRAND MAP OF APOLLO AND A PATH TO FRIDAY THE 25th**. ⛔ **2 carried unanswered: inline styles (88 raw), template status.**
> - ⚙ ⑥ FILL **181,277 / 24** v **180,375 / 23 DECLARED**, Δ **902** — ⛔ **+1,277 on 180,000**; ✅ 220,000 and 256,000 clear. **subs 573,531 (n=5)**, all `Agent`/opus/depth 1, none in seat; ⚠ **`read_fill` reads 568,179 — two definitions, both published**. ⛔ **6 gate fails, `#243` form, 13th.** CI + ⏱ DELTA.
> **residual → #288:** ⬛ **THE STRAND MAP AND THE PATH TO FRIDAY THE 25TH** [NEW — 0, DAVE'S] — his order at the wrap call. `s225-D2`. **560 items, 12 new, 4 STRUCK**, `_CARRIES.md` § `residual → #288` `carries:residual-288`. PROBE `PYTHONPATH=knowledge python3 -c "import _capture_gate as c;print(len(c._carry_items([l for l in open('_CARRIES.md') if '#288:**' in l][0])))"` = 560. ⚠ **ALL 12 NEW INVISIBLE TO THE PROBE, 10th wrap** (`_AGE_RE` misses `[NEW — 0]`); 551→560 is #286's 9 ageing in. **4 STRIKES: the two sentences, the field key and the explorer, the 58 locations, the DRAFT line** — each receipted (`s183-D1`/`s188-D2`); ⛔ **the connector-lever item is NOT struck** (its headline stays true).
> **residual (GENERATED #287):** 2c OK (banners 2/2) · 2d OK (deltas 3/3) · 2f OK (strata 1, log #286) — _roll_state.py · 2026-09-19

> **⬛ OPEN WORKLIST — PRESENCE INDEX (28 items, GENERATED). Every open item is named; NO bodies are here — `--fetch gm:DOFIRST`.**
> `0b` ENCODE BEFORE THE WAVE, part-consumed 28 · `0c` NEXT BUILD CANDIDATES · `0d` THE 67 ENACT WAVE, landed · `1` ds-018 C2 follow-through · `2` dv-legend/dv-behaviour CEILING · `3` ds-012(b) gutter-relative plot area · `4` DV-D16 floating growth · `5` Instrument-fit remainder · `6` ds-016, PART-RULED #114 · `7` ds-017, UNRULED · `8` STILL OWED, none superseded · `9` DELEGATION TOPOLOGY · `10` ✅ PER-GATE TEST PLAN · `11` 2c-ROLL / INDEX-VOCAB DEADLOCK · `12` #57 1b DOSSIER · `13` tmp RUNBOOK EXPOSURE, unfixed · `14` ⬛ DAVE'S FOUNDING PRINCIPLE · `15` LEDGER § ★ #59 · `16` UNHOMED PAIR (#78 2c EXIT CHECK) · `17` CHAIN-DIET CUT · `19` type.css 180 dark-mode specificity · `20` ds-029 FIRST IDIOM · `21` PER-THEME COLLISION SWEEP · `22` 89-D2 ruled not enacted, absent from state · `23` ds-025 re-scoped 109, not closed · `24` 110 boot-rent plan · `25` ds-032 enactment, ruled 106 · `26` ✅ boot-drift gate rulings
> **QUEUE — `gm:C1` strands · `gm:C2` ruling batch (Dave's) · `gm:C4` enact-queue.**

---

## ⏱ LATEST DELTA — 2026-09-19 (Sat from `date`) (**#287**, ✅ **ONE DAY, NO DATE SPLIT**, conductor **FABLE 5.1**, **FIVE DELEGATED OPUS SEATS — I · K · W · C · X**, with this **OPUS 5** wrap sub, DELEGATED — ★★ **TWO SENTENCES BECOME RULINGS, THE EXPLORER READS `sizes`, THE WALL WORDING IS SWEPT — AND DAVE ORDERS A STRAND MAP AND A PATH TO FRIDAY THE 25TH**)

*(WHY/HOW: `_DECISION-HISTORY/2026-09-19-287-the-rulings-land-and-the-strand-map-is-ordered.md`. **The handoff is `_HANDOFF-138-the-rulings-land-and-the-strand-map-is-ordered.md`, and it OUTRANKS `_CHAIN.md`.** **His words verbatim: `notes/_lanes/287/DAVE-RULINGS-2026-09-19.md` — quote them, never paraphrase them.** Filed reports: `notes/_subreports/2026-09-19-287-{I,K,W,C,X}-*.md` and this wrap's own `notes/_subreports/2026-09-19-287-W2-wrap.md`.)*

- ⛔★★★ **READ `_HANDOFF-138-the-rulings-land-and-the-strand-map-is-ordered.md` FIRST — IT IS NEWER THAN `_CHAIN.md` AND OUTRANKS IT.** ⛔ **It does NOT replace `_HANDOFF-130`…`-137`:** every open item on those eight still stands except the four closed today, each with a receipt. **Strike nothing else without one.**

- ★★★ **FOUR WORDS AND A HALF-SENTENCE SETTLED THREE CARRIES AT THE OPENER, AND THE RULINGS STORE MOVED FOR THE FIRST TIME IN SEVEN SESSIONS.** Dave's whole reply, verbatim: ***"1. inscribe · 2. keep `sizes` and rebuild. · 3. do it"***. ⇒ **`knowledge/_rulings.json` 620 → 622**, verified at THIS seat by `json.load` over the `rulings` list with both ids present and no duplicate: **`s287-D1`** (the eight standing lines RATIFIED AS WRITTEN, `_standing.md` out of DRAFT, the seam re-quoting them, and the 256,000 wording fix in `_gauge_tokens.py` — all HIS) and **`s287-D2`** (a per-size master is a SIZE FIELD on its lockup's existing node in `knowledge/_logo_nodes.json`, never its own node and never a new kind; the node count does not move). ★ **The JOIN IS NAMED ON THE FACE OF `s287-D1` RATHER THAN SMOOTHED:** reading #286's *"okay go on everything"* as ratification-**as-written** was the CONDUCTOR'S reading at #286; the word that makes it HIS is *"inscribe"*, said 2026-09-19. **Same discipline on `s287-D2`** — the key name `sizes` was lane R2's reading of the file's plural vocabulary until *"keep `sizes` and rebuild."* made it his word. Inscribed through the sanctioned writer `knowledge/_inscribe_ruling.py` (`--dry-run` then `--write`, reconstruction proof PASSED on both, all other bytes identical), never a hand-edit. `notes/_RULINGS.html` re-rendered in the same change — **622 rulings / 160 sessions**, `--check` **FRESH**.

- ★★ **THE EXPLORER NOW READS THE FIELD — THE #286 CARRY "NOTHING READS IT" IS DISCHARGED.** Lane K rebuilt `notes/_KG-EXPLORER.html` (builder `VERSION 1.26 → 1.27`) with three new reads in `knowledge/_kg_explorer.template.html`: the aside's `MASTERS 5 · 24 · 28 · 32 · 36 · 40 px` row, INSPECT's RECORD listing each height as a link to its own master SVG with geometry + digest, and a third **MASTERS** tab drawing all five at their true heights on the lockup's own ground. ✅ **Verified by PARSING the built page rather than eyeballing it: all 8 `logo:` nodes carry `sizes` with 5 keys, 40 entries, byte-identical to `knowledge/_logo_nodes.json`; 8 nodes / 33 edges either side; `_logo_nodes.json` was never written by this lane.** Every inline `<script>` re-extracted and `node --check`ed; the three new functions executed for real against the page's own baked data. ⚠ **2,256 baked coordinates moved and 11 nodes / 18 edges appeared, and NONE of it is the field's** — they are the Constitution growing under the build's feet (the four new rulings and their evidence nodes), named in the lane's own report and in the builder's version note rather than claimed as a byte-identical canvas.

- ★★ **THE 256,000 "WALL" WORDING IS SWEPT ON HIS *"do it"* — 58 → 42, AND THE 42 ARE ACCOUNTED FOR ONE BY ONE.** ⛔ **NO NUMBER MOVED:** `BUDGET_HARD` is still `256_000`, `STOP_LINE_TK` still `180_000`; only the word next to each figure changed. Lane W re-ran lane G's own probe before and after (`256,000|256000|256_000|256K` ∧ *hard*|*wall*) across the ten live files. **Genuine wall-assertions remaining: ZERO.** The 42 fall in five named classes — **16** are the correction's own words (*"not a context wall"*, matching by construction), **11** are Dave's inscribed ruling text ANNOTATED not rewritten, **2** name the live constant `BUDGET_HARD` itself, **3** are #286's record of the owed work now carrying `⇒ ✅ SWEPT AT #287`, and **1** is a false positive on the retired `HARD_STOP` percentage constant, deliberately not edited. ⛔ **`says` FIELDS WERE NOT TOUCHED — those are Dave's quotes** (`gauge-band`, `s161-D1`, `s214-D2`, `s214-D4` all stand exactly as inscribed). Six records were annotated by an APPENDED clause applied as a byte-level span swap with the inscriber's own proof discipline (locate by `json.dumps`, exactly one occurrence, no `json.load → dump` reformat, re-parse and prove the only difference is the named field of the named record) — `_inscribe_ruling.py` offers no `ruled` path at all and says so in its own comment block. Generated surfaces were REGENERATED, never hand-edited: `notes/_RULINGS.html`, `dashboard/index.html`, `_CHAIN.md`, `knowledge/_memento-index.json`.

- ⚠ **A GATE FAIL WAS BORN MID-SWEEP AND HEALED BY ITS OWN PRESCRIBED REMEDY, AND THE SEQUENCE IS RECORDED BECAUSE IT IS THE GOOD SHAPE.** Editing the two chain sources took the structural fails 6 → **7** (retrieval index STALE — the #32 defect), and running exactly what the gate named, `_build_memento_index.py` (ritual step 2g, not on the lane's fenced list), took them back to the inherited **6**. ★ **A lane that causes a fail, is told what to run, runs it, and re-measures is the gate working as designed.** ⚠ Two NEW advisory warns are attributable to the same lane and were NOT cleared: the REGEN SERIAL ran out of order because the four generators were run individually rather than through `_build_all.py`, **which the lane was fenced from** — named, not fixed.

- ★★★ **THE SECOND COLD BOOT LANDED AND THE CONNECTOR LEVER IS NOW A MEASURED EFFECT, NOT A DIRECTION.** `notes/_lanes/287/BOOT-COLD-2026-09-19.md`: **74,120 real** on the same setup as #286's **73,832** — **Δ +288**, two readings within 300 tokens of each other on a setup whose prior seven-reading spread was **11,526** wide. ⇒ **#286's OWN STATED TEST — *"two readings within a few hundred tokens of each other would make it measured"* — IS MET, and the −7,031 from blocking the built-in Browser's 17 tools is a MEASURED EFFECT (n=2).** ★ **This is the rare case of a session honouring an acceptance test written by the session that could not pass it.** ⛔ **AND IT CHANGES NOTHING ABOUT THE CEILING: 74,120 is 4,120 over `BOOT_CEILING_TK` 70,000 — the TWELFTH post-diet reading over it — and the literal is SHRINK-ONLY (`s240-D2` / `s241-D1`): cut the boot, never raise the number, and raising it is Dave's word alone.** ⛔ **This is NOT a re-base.** ⚠ **The next lever is still unnamed** — boot is one undecomposed figure (`ds-025` item 1), dark from every mount. Per `s241-D2` the figure is stated ONCE, in the `post-mortem #287:` line of `GOOD-MORNING.md`'s stratum, and the finding file is deliberately NOT in `notes/_GAUGE-LOG.md`.

- ⛔★★★ **THE AFTERNOON BROUGHT A GPT-6 HANDOFF FROM HIS WORK MACHINE, AND DAVE SET THE POSTURE FOR READING IT: *"don't take it on face value, use your judgment, but what it did worked."*** The report — Apollo Spider **v1.0.13**, an HSBC CEO banking prototype, a GPT agent with no vision — is saved into the repo at `notes/_lanes/287/GPT-SPIDER-SPACING-HANDOFF-2026-09-18.md` (ritual step 1's cited-uploads clause: a chat-only attachment is an un-retrievable citation). Lane X tested only its **factual claims about our system**, at file:line, read-only, no gate and no generator run: **5 of 6 VERIFIED, 1 PARTLY** (the 33/8 gate split is arithmetically exact against v1.0.13's 41 RUNNABLE, but the attribution is ~3 screen-caused / 4 pack-baseline / 1 placement-dependent, and the agent ran no `--baseline`, so it could not separate them). ★ **The report's one real analytical miss is the same miss twice: it read `0` and `40/4` as DEFECTS when both are RULED values** (`s217-D2`'s per-theme gutter; the snippet's declared single-theme convention).

- ⛔★★★ **THE CONDUCTOR FRAMED IT AS "TWO RULED SPACING SOURCES DISAGREE" AND DAVE CORRECTED THE FRAMING — HIS SENTENCE, VERBATIM:** *"The problem with the gutters it that they are deliberately different for the themes and the gutters are also different for the inner and outer bentos we essentially have a structural bento and embedded bentos or tile groupings."* ⇒ **There is no contradiction to resolve by picking a winner. The OUTER (structural bento) gutter and the INNER (embedded bento / tile-group) gutter are TWO DIFFERENT QUANTITIES, each deliberately per-theme.** ⚠ **The framing was the conductor's and the correction is Dave's, and BOTH stand in `notes/_lanes/287/DAVE-RULINGS-2026-09-19.md` rather than the wrong one being deleted** — a correction that erases what it corrects leaves no evidence the correction was needed. **The defect that remains is real and narrower:** the template pins ONE theme's pair as literals (`--bento-gutter:40px` at `canon.css:18119`, `4px` at `:18121`) instead of delivering the selected theme's (outer, inner) pair, **because the `s219-D3` GENERATION ARM WAS NEVER BUILT** — `_bento_edit_rails.json` declares itself `$groundwork_only` and has sat un-consumed since #219. ⚠ **Whether `--layout-bento-gutter` (ruled 0 in Mono, `s217-D2`) IS the structural gutter is still to be READ, not assumed.**

- ⬛★★★ **HIS WRAP CALL — FOUR THINGS, VERBATIM, AND #288 OPENS ON THE THIRD.** *"I'm not sure that 0 is right for mono, lets have a proper review and fix this: 'The generation arm that would do that was ruled at #219 and never built.' and any other problem with this, I just need the one-shot design to not disappoint, however it is getting better all the time."* · *"I'm not sure about the quality of the template we need to look at this too."* · *"I want to return to the presentation soon, so I need to understand how the multiple strands of Apollo stand and I need a clear path to presenting something cohesive for friday the 25th"* · *"wrap with all of this in mind"*. ⇒ **Mono's 0 is DOUBTED, not overruled** — a review is ordered, the `s219-D3` arm is to be BUILT, and *"any other problem in that path"* is in scope; the goal he names is **the one-shot design not disappointing**. **The bento template's QUALITY is a SECOND review, not folded into the first.** **A STRAND MAP of Apollo and a clear path to a cohesive presentation on FRIDAY 2026-09-25 — six days — is owed, and it is #288's FIRST move.** ⛔ **TWO DECISIONS WERE PUT AND NOT ANSWERED AND THEY CARRY:** an inline-style rule (lane X's census found **596** `style="` in `knowledge/snippets/`, of which **88 carry a raw px/rem with no `var()`** — that 88 is the actionable number, not the 596 — and **zero rulings on inline styles exist across all 622**), and the template's status (**meta says `PROPOSED / NOT REGISTERED`, `showroom/index.json` says `beta`** — two statuses for one artefact, and the index is the surface the skill tells builders to search).

- ⚙ **GAUGE, MEASURED AT THIS SEAT against the conductor's own transcript** `6ff1b408-192e-4f32-8c4c-889929239137.jsonl` — the session's TOP-LEVEL transcript, this wrap seat being `subagents/agent-addfe852130dd24e1.jsonl` beneath it, which is what makes the subtraction legal [[measure-dont-convert-units]]. **FILL 181,277 real across 24 turns** against the brief's DECLARED **180,375 / 23** — the hand-over cost **902 real**, the smallest on this run. **BOOT 74,120**, agreeing to the token with the conductor's declaration, **and that agreement is what identifies the window.** ✅ **No compaction record and no fill drop**, so the reading is a fill and not an artefact. ⛔ **THE 180,000 QUALITY LINE WAS CROSSED BY 1,277, AND THE BRIEF SAYS WHERE: the stop line was crossed by 375 ON THE WRAP CALL ITSELF** — the ritual fired at the line rather than past it, and the 902 that followed is the hand-over, not the job. ✅ **44,000 inside the ≤220,000 tolerance; 256,000 clear for the fifth session running.**

- ⚙ **subs 573,531 real (n=5)**, QUOTA and never FILL [[budget-vs-quota-vocabulary]] — **five delegated Opus seats, every one an `Agent` at `spawnDepth 1`, model opus, NONE in seat**: X the Spider handoff **142,327** · K the explorer **124,765** · W the wall sweep **122,129** · C the commit and push **112,722** · I the rulings **71,588**. ⛔ **A SIXTH SUB — this wrap seat — IS EXCLUDED BY DECLARATION (`s214-D5`), because a seat cannot read its own final total and an unknown is never turned into a number** [[feedback-measuring-tool-must-not-guess]]. ⚠★★ **AND A DEFINITIONAL DISAGREEMENT IS PUBLISHED RATHER THAN RECONCILED AWAY: the `subs` convention and the FILL rule are TWO DIFFERENT QUANTITIES.** Read with `_checkin.read_fill` — the ONE definition of FILL, input side only (`input_tokens` + `cache_creation` + `cache_read`) — the same five seats sum to **568,179**. The **573,531** above is each seat's last-turn input side **plus that turn's `output_tokens`**, which is the figure the conductor declared and what this log has carried since #284. **The gap is 5,352 and it is entirely the five lanes' final output.** ⇒ **Both readings stand; neither is rewritten. Which one the `subs` line means is ruling-shaped and is Dave's** [[measure-dont-convert-units]]. ★ The FILL figures above for the conductor's own window are the `read_fill` reading throughout, so no line in this delta mixes the two.

- ⛔ **SIX BLOCKING GATE FAILS STOOD AT THIS RITUAL'S OPEN, ALL INHERITED, CARRIED IN THE `#243` DECLARED NOT-A-WRAP FORM — THE THIRTEENTH CONSECUTIVE WRAP ON THAT PATH.** Every one is another session's append-only testimony in `notes/_GAUGE-LOG.md`: the **boot-drift CEILING BREACH** (`BOOT_CEILING_TK` 70,000, seven post-diet readings named in the gate — ⚠ **it does not yet count #286's 73,832 or #287's 74,120; the `post-mortem` line is where those enter**) and **five boot double-counts** (#243 ×5, #264, #272, #273, #274). ⛔ **A WRAP MAY NOT REPAIR AN INHERITED GATE FAIL** — the ceiling's remedy is to CUT THE BOOT, never to raise the literal, and raising it is Dave's word alone [[gate-must-quote-what-it-forbids]]. Also standing and NOT repaired: `carry_wording_check()` is blind (both residual lines are pointers, so it has nothing to pair and goes quiet); `_gate_scratch_hygiene.py` reports the fill of `/` rather than `/sessions`; `_seam.py`'s SCRATCH arm still deletes a lane's `/tmp` files mid-run.

- ⚠ **THREE JUDGMENTS WERE MADE BY LANES THAT COULD HAVE DEFERRED, AND EACH DECLARED ITSELF RATHER THAN BEING DISCOVERED HERE.** **(1)** Lane C wrote **three other lanes' `_state.json` rows** (`W-287i`, `W-287w`, `W-287c`) through `_state.py`'s module API, taking each `closes_when` from that lane's own filed verdict and re-wording nothing, because the alternative — `DOC_ROW_ACK` — *"would have shipped three invisible documents to pass a gate"*. **A close-condition the conductor wants changed is his to correct, not to discover.** **(2)** Lane C regenerated `dashboard/index.html` and MEASURED the reason the diff is 9,590 lines: a regeneration from the **UNCHANGED HEAD `_state.json`** alone moves **11,617** — ⇒ **the committed dashboard was already stale against its own source before #287 touched it**, and it is now FRESH for the first time in several sessions. **(3)** Lane K's 4.3 MB before-copy of the explorer could not be deleted (the sandbox refuses `unlink` under the mount), so lane C used the #284/#286 `mv` precedent: it sits in `_to_delete/287-K-before/`, which is gitignored; the **807 KB `.gz` IS committed** as the evidence that was meant to be kept, and `.gitignore` was not edited.

- ⚠ **DECLARED SKIPS AND NOT-DONE AT THIS WRAP, EACH NAMED WITH ITS SIZE** (declare-last, and the size is the point). **2c, 2d and 2f ALL RAN** — no `size:` stamp is carried forward from #286 and no figure in the header is a prior session's. **2e is a NO-OP at 0 lines:** DO-FIRST minted no supersession notice, no closure tombstone and no lifecycle tag, and ⛔ **nothing was retro-tagged**, which is the rule and not an omission. **2g ran LAST**, after every GM/LS edit was final. ⛔ **`gen_kg_rules.py`, `land_rests_on.py`, `gen_kg_icons.py` and `_build_all.py` were NOT run at this seat** — fenced by the brief, and three of them would undo landed work. ⛔ **The six inherited gate fails were NOT repaired and a wrap may not repair them.** ⛔ **`_seam.py`'s four printed "HARD LINE BREACHED" strings are still the one remaining printed wall wording in live code** — outside the ten-file prose list lane W was given, and owed to whoever owns the seam.

- **CLOUD MEMORY (step 3):** the hook was written FIRST at `notes/_lanes/287/WRAP-MEMORY-HOOK.md` — the index line plus the file body, in the `s271-D4` form — and then placed from this seat. The result is recorded at the hook file's own foot, not assumed. ⚠ **`MEMORY-ARCHIVE.md` stood at 48,990 of its 49,152 B cap at #286 — 162 B of headroom** — so the newest-three cut may again not fit; whatever happened is DECLARED at the hook's foot and ⛔ **nothing was truncated, because a truncated verbatim move is not a move.** ★ **Nothing is at risk either way: the repo is the record and memory is the accelerator.**

- ⚠ **THE CARRY SET IS `_CARRIES.md` § `## residual → #288` (`s225-D2`)** — the banner holds a pointer, the top item's headline and a PROBEABLE count, never the list. **FOUR STRIKES this wrap, each naming the session that proved the claim closed AND where the correction is inscribed (`s183-D1` strike form, `s188-D2` receipt):** the two uninscribed sentences (→ `s287-D1`/`s287-D2`), the masters' field key and the unbuilt explorer (→ *"keep `sizes` and rebuild."* + lane K), the 58 prose wall locations (→ *"do it"* + lane W), and `_seam.py`'s stale DRAFT line with its `:26`-vs-`:44` dispute (→ lane I amended the first clause only, and measured `:44`). ⛔ **THE CONNECTOR-LEVER ITEM IS DELIBERATELY *NOT* STRUCK** — its headline records a reading and a ceiling that both stay true; the n=2 upgrade is said in a NEW item instead, because a strike removes the headline and `s271-D4`'s own warning is that a strike that is wrong is worse than an item that is merely stale.

- ⬛ **POST-WRAP ADDENDUM (5b) — HOMED HERE AND NOT ON THE ★ LATEST BANNER, DECLARED RATHER THAN SLIPPED.** The `s241-D2` cap is BLOCKING at 10 lines / 1,200 tape and this banner is written to it, so an eleventh line would be refused, correctly; the cap's own words settle where the detail goes (*"the ⏱ LATEST DELTA is the sole home for gauge / declared-skip / not-done detail"*), and the delta is IN the read chain. ⛔ **The 5b-versus-cap conflict is ruling-shaped, Dave's, and carried — this is the SEVENTH wrap to meet it and the SIXTH to answer it this way.** **THE COMMIT, THE PUSH AND THE CI READ:** **WRAP COMMIT `04dbeab1e0181d375d888659d77241f1fa2ad91c`** — 44 files, +7,422 / −3,849, through `SESSION_N=287 knowledge/_git_commit.sh --reconciled --quiet` with every path named, **exit 0**, and the tree left clean. **PUSH VERDICT, VERBATIM: `✅ pushed and VERIFIED: remote master == local 04dbeab1e0181d375d888659d77241f1fa2ad91c`**, independently re-verified at this seat by `git ls-remote origin HEAD`. **CI, read over the public API on `head_sha=04dbeab1` and polled to COMPLETION rather than stopped at a colour that was still moving — run `35434571412`: ⛔ RUN-LEVEL `completed / failure`; `gates` `failure` on the SAME TWO INHERITED STEPS — step 5 *Survey the COMMITTED tree* and step 6 *Knowledge build* — `release` `success`, and `render` `success`.** ✅ **THE `render` COLOUR IS RESOLVED FOR BOTH OF TODAY'S COMMITS, AND THAT CLOSES SOMETHING TWO SESSIONS COULD ONLY DECLARE:** lane C could read `render` only as `in_progress` on `75a490cd` and #286's wrap met the same wall; re-read here after completion, **`75a490cd` is `gates` failure / `release` success / `render` SUCCESS with the run-level `failure`** — so the red is the two inherited steps and nothing else, on both. ⛔ **THIS COMMIT NEITHER INTRODUCED A CI FAILURE NOR CLEARED ONE.** ⚠★★ **AND A NEW OBSTACLE WAS MET AT THIS SEAT AND IS RECORDED RATHER THAN SMOOTHED: `git` COULD NOT UNLINK ITS OWN `.git/index.lock` UNDER THE MOUNT** — *"warning: unable to unlink … Operation not permitted"*, the #227/#284 sandbox delete-guard reaching a file git itself owns — **so EVERY git write leaked a stale lock and the commit script's staging loop could never reach its second path.** ⛔ **The repair was the runbook's own and the only one this mount allows — `mv`, never `rm`** (`_to_delete/_stale_locks/`): the 22 named paths were staged one at a time with the lock moved aside between each, leaving 0 unstaged, and the script was then invoked on the one path still dirty so that **its own post-staging asserts ran against the FULL index** — mention-map-staged, doc-rows-for-staged-adds, the T3 subject assert and the one-prefix assert all GREEN, and `git show --name-only` counts **44**. ⚠ **THE DEVIATION IS DECLARED, NOT HIDDEN: the script staged one path and this seat staged the other twenty-one**, which is the #284 *"the commit took six runs"* class with a NEW and worse cause — not a stranded lock from a crashed process, but a lock git leaks on EVERY write because it cannot delete it. ⛔ **Ruling-shaped and not inscribed: whether `_git_commit.sh`'s staging loop should clear locks BETWEEN adds rather than once before them.** ⚠ **One doc-row refusal was met and PAID rather than declared away:** lane X filed its report after lane C's commit, so it had no `_state.json` row and the post-staging gate refused, correctly; the row `W-287x` was written through `_state.py`'s module API with its `closes_when` taken from lane X's own verdict, **not passed with `DOC_ROW_ACK`** — ★ **the gate asked for a row and got a row.** ⚠ **And the id regex refused `W-287w2` for this wrap's own report** (`^(?:W-[0-9]{1,3}[a-z]{0,2}|…)$` allows no digit after the letters) — **the FOURTH instance in three sessions and the first met by a wrap seat**; the row is `W-287ww`, and widening a convention at a wrap is not a wrap's call.


---

## ⬛ OPEN WORK — GENERATED from `_state.json`. **The row count IS the count.**

> **711 items · 604 live · 318 Dave's · 286 mine · 697 carry a stated close condition · **14 UNCONDITIONED**.**
> *Every figure in this block is computed from the store at generation time. No number here was typed by anyone — that is the point of it (#86 measured a typed inventory of "118 markers" against a real ~40; #85's "95 slots / 84 distinct" is not reproducible by any probe in this repo).*
> ⚠ **DECLARED DEBT — 14 inherited items have NO close condition and therefore cannot close.** They are exempt as a FROZEN set (`_state.LEGACY_IDS`, size 19) which may only shrink; a NEW item is refused without one. **Each needs Dave's word — an agent inventing a close condition for his open work is the same overreach as inventing his ruling.**
>
> **DAVE'S (318) — 318 with a ratified `closes_when`, 0 with NONE · MINE (286). IDS, BODIES AND CLOSE CONDITIONS ARE IN THE STORE, NEVER HERE (s227-D2 shape, ids cut `s241-D2`): the counts above are the inventory; the rows are one command away, named on the next line.**
>
> ⚠ store gate: **PASS**. Bodies, conditions and provenance: `python3 knowledge/_state.py`.


---

*(Chain ends. **12,112 tape (cl100k ESTIMATE) — the unit is THE WHOLE FILE**, this generated wrapper included:
the number in this sentence is the size of the file containing it, held exact by a fixed point.
`GOOD-MORNING.md` is 35,846 tape (cl100k ESTIMATE) and stays whole for retrieval; you have paid for
34% of it. Under 40% is this generator's own floor — above it, the wrapper is
carrying more than the slice, and `--selftest` says so by name.)*
