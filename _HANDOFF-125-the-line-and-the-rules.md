# HANDOFF #125 — #274 → #275 — THE LINE AND THE RULES

> ⚠ **This file is NEWER than `_CHAIN.md` and OUTRANKS it** (and `_HANDOFF-124`). Written by the #274 conductor (Fable) at FILL ≈185,000 real — past the 180,000 working line, inside the ~220,000 tolerated band (`s272-D93`) — BEFORE the delegated wrap. Every claim is a receipt.

## ⛔ READ FIRST, IN THIS ORDER
1. This file. 2. `_CHAIN.md`. 3. `python3 knowledge/_parked.py --check`. ★ Stale-mount check: `git log --oneline -3` vs `ls -la --time-style=full-iso _CHAIN.md`. ★ `pip install tiktoken --break-system-packages` before `_checkin.py`. ★ `.git/index.lock` was stale at 13:43 — `allow_cowork_file_delete` then `rm`, never `mv`.

## ⛔⛔ DAVE'S — VERBATIM. Source: `notes/_lanes/274/DAVE-RULINGS-2026-09-15.md` (every sentence, in order)
- His two-level model (containers simple-list / structured-list / card-list; rows; card-list-row = simple-list-row; optional headers) → **`s274-D1..D5`** `04c51c6`. *"I think that actions might be allowable in a simple list in actuality"* → D1 = SURFACE ALONE. *"okay i think you are right on 'headers on card lists' lets get this done"* → D5.
- *"1"* on the record-list default → **`s274-D6`** `3a752d1`: list-items is the default, table takes the comparison test; `roles.json` two `when` strings replaced by span.
- *"note that in edit mode, when we build it, the user can be presented with the alternatives we have defined."* → **P-274-1** (tripwire `_compose_slice.py`) `3b12185`.
- *"… this is just getting teh KG working harder we'll work on the demo/prez after we get this all sorted"* → KG FIRST, demo after. Not a ruling; a direction.
- Rules-into-graph export RK-1 a · RK-2 a · RK-3 null (+note) · RK-4 a (+"A and then C") · RK-5 a (+caveat) · RK-6 a (+"A for now then D") → read back → *"go"* → **`s274-D7..D12`** `f6c5da8`.

## ⛔ NOT RULED — DO NOT RULE
LCE's three proposals: P1 `props.type` enum on list-items spans two levels (split container + row?) · `bordered-per-record` and `$level: "typeset"` are the LANE'S words · RL's opens: chip word `guidelinerules`/"Guideline rules" · 99/634 edges draw only with Governance+Guidelines layers on · `_rule_nodes.json` `$description` still says "PROPOSED … NOT RATIFIED" (generator text, fence it at #275) · the `example` key on 13 `when-fields` entries · the 4 near-dupe pairs · 6 `data-grid` reds (#261) · `input` sub-roles · P-272-2..4 · showroom 108 pages stale (`:is`→`:where`) · push (18+ local).

## WHAT #274 DID — RECEIPTS (7 commits LOCAL before the wrap; push = his word)
1. `04c51c6` s274-D1..D5 inscribed (566 → 571). 2. `58f56ef` ENACTED, lane LCE (Opus 121K): `list-items.meta.json` 3 container variants + `$level` on rows + `when`/`priority 60`; `account-card` `$note`. Gates: roles-resolve FAIL(6) inherited · drift 108/108 · KG OK · compose PASS. 3. `3a752d1` s274-D6 + `roles.json`. 4. `3b12185` P-274-1. 5. `0bfaf91` lane RK (Opus 203K): `gen_kg_rules.py` (selftest 12 bites, 13 mutants caught) + review page RK-1..6. 6. `f6c5da8` s274-D7..D12 (572 → 578). 7. `00f3a87` ENACTED, lane RL (Opus 139K): `_rule_nodes.json` 521 nodes / 634 edges / 19 nulls; explorer v1.11 reads it ("Guideline rules" chip, RULE 470); P-274-2 (19 SC criteria into the compliance corpus) + P-274-3 (meta cites rules) parked. Screenshot reviewed by the conductor.
**s269-D1 order: (1) roles DONE #273 · (2) rules DONE #274 · (3) the 145 principles + 22 polarity edges = NEXT · (4) icons then logos · (5) edge types.**

## #275 — FIRST MOVES
0. Opener: has he pushed? Read both title lines back. Fence `_rule_nodes.json`'s `$description` (one span).
1. **STEP 3 OF s269-D1: the 145 UX principles** (`knowledge/brain/principles.json`, prefix `ux:` per s269-D2, grades A–L; the six A-grade laws cited from metas first per s269-D5). Same shape as RK: one Opus lane → generator with dry-run + selftest + `--land --ratified`, review page ≤ 6 decisions, NOTHING landed until his export. Then a land lane (RL shape) on his word. ≈ two lanes.
2. **s273-D2 authoring pass two** (49 silent providers, 8 roles) — 3–4 lanes/session; `record-list` is now unblocked by s274-D1..D6.
**Cautions:** `model: opus` every lane · verifier same wave · screenshot + text diff before presenting · textual span only · never `git stash` · never `gen_kg_edges.py` · `_checkin.py` every ~10 turns.

## STRUCTURAL REDS AT THE SEAM (wrap heals or declares)
Memento index stale (today's notes) → 2g · boot 81,898 = TENTH breach, his · `/sessions` disk · dirty instrument files (`_REHEARSAL-LOG.jsonl`, `_GRADE-DECISIONS.jsonl`, `_gen_titles_receipt.json`) · resolver FAIL(6) = #261 · showroom stale, declared #273.
**Gauge:** boot 81,898 real · FILL 163,843 at the last check-in (before lanes RK's report and RL) — expect ≈185,000 at the brief cut · subs: LCE 121K + RK 203K + RL 139K = 463K delegated (n=3).
