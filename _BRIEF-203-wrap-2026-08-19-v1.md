# Wrap brief — #203, delegated wrap (OPUS wrap sub)

*Cut by the FABLE conductor at wrap-open, FILL 178,046 real (past the ADVISORY stop line 150,929
by 27,117; the WALL 256,000 binds). Follow `knowledge/_RUNBOOK-capture-ritual.md` end to end with
`--wrap`. Everything below is INPUT to the ritual, verified where marked; re-verify sha/rc claims
yourself — a brief is not evidence [[ritual-output-is-not-evidence]].*

`boot reads: _CHAIN.md (first ~80 lines only, via bash head; GOOD-MORNING.md NOT opened)`

## Session facts

- **#203 · 2026-08-19 (Wed) · FABLE conductor + 12 OPUS work subs (two waves of six) + this OPUS
  wrap sub · DELEGATED wrap.** Title read back at opener: `Apollo - #203: the segmented labels sit
  on the real type scale but nothing binds them` — note the irony for the banner: the session's
  namesake item (type-scale label bindings) was NOT ruled today; it stays open, Dave's.
- **ONE ruling, Dave's: `s203-D1` — PUSH AUTHORITY DELEGATED TO THE CONDUCTOR'S SEAT.** Already
  inscribed BY THE CONDUCTOR via `_inscribe_ruling.py` (202 records, `s203-D1` newest — read it
  back by script yourself). ⛔ You write NOTHING into `knowledge/_rulings.json`.
- ⛔ **PUSH: RULED DELEGATED, and the push is the CONDUCTOR'S ACT, not yours. You COMMIT ONLY.**
  The conductor pushes after your commit (auth layer = GitHub Desktop per the git runbook) and
  owes Dave the CI read-back.
- **QUOTA panel (Dave relayed, verbatim numbers): All models 47% used · Fable 61% used · resets
  Thu 11:00 PM.** Dave's words: a "cranking day"; "rinse the opus subs, run them on max"; "lets
  get All models caught up"; effort up.
- **Sub spend, MEASURED: subs 2,170,761 tokens (n=12)** — wave 3a 964,359 (n=6) · wave 3b
  1,206,402 (n=6). Effort band **L**. ⛔ Your own spend is EXCLUDED and unknowable from inside —
  DECLARE, never invent. Conductor boot 56,488 (in band).

## What landed (receipts are the evidence; verify headline claims by re-running/reading)

1. ★★ **WAVE 3a (six lanes): THE BRIEF'S PREMISE WAS FALSE — 18/18 briefed "P1 gaps" already
   existed, gated, since 2026-07-22.** The itinerary's Status column (2026-07-14 snapshot) was
   five weeks stale; the conductor briefed off it without probing a row — named plainly as the
   conductor's miss, the [[premise-ages-faster-than-rule]] class, fourth surface. The brief's
   mandatory step-0 premise table CONTAINED it: zero duplicates built; all six lanes pivoted
   in-fence to **18 four-theme review spreads** (`reviews/REVIEW-203-*-four-themes-v1.html`) +
   measured findings. Receipts: `notes/_receipts/2026-08-19-203-wave3-lane[A-F]-*.md`.
2. ★★ **WAVE 3b (six lanes, briefed off FIRST-HAND probes): NINE NEW COMPONENTS BUILT AND GATED**
   — Command-palette · Sidebar-nav · Anchor-nav (lane I) · Combobox · Multi-select · Tags-input
   (lane J) · Kpi-tile · Timeline · Avatar-group (lane K). Library 76 → **85**. Receipts:
   `notes/_receipts/2026-08-19-203-wave3b-lane[G-L]-*.md`.
3. ★★ **THE CANON DARK-DROP IS REPAIRED (lane G)** — `gen_canon_components.py` no longer drops
   descendant rules under `[data-theme="dark"]`; mutation-tested 6 arms rc=0; regeneration
   verified; before/after review page `reviews/REVIEW-203-canon-dark-repair-before-after-v1.html`.
   The mono error ink camp reaches dark for the first time in 19 components.
4. ★★ **THE ITINERARY CLASS-FIX EXISTS (lane H)** — `knowledge/gen_itinerary_status.py` derives
   status from the store (refuses to guess, exits 1 on UNRESOLVED); measured **35 stale rows**;
   TRUE-gap list: P1 = only row 86 (asset-only wrap job), plus 8 P2 / 14 P3 / 1 Layer-2
   structural. Frozen 2026-07-14 files untouched. Output `reviews/ITINERARY-STATUS-2026-08-19-v1.*`.
5. ★ **THE HIT-AREA ADVISORY EXISTS AND BITES (lane L)** — `knowledge/_validate_hit_area.py`,
   ADVISORY, NOT wired into `_build_all.py`; acceptance = caught both known real breaches
   (Amount-input 39px · Secure-entry 40×48 corrected from Lane C's 42) and passed controls;
   full sweep **428 control findings across 36 components**; mark tier 332/510 under 24
   (`s116-D1`'s owed measurement). ⚠ Lane L also found **`_a11y_target.py:60-63` ALREADY
   enforces 44** (s114-D5, #116) — contradicting `s201-D2`'s "no enforcement" claim AND the
   standing memory hook; a store correction is DAVE'S, carry it as his.
6. ✅ **CONDUCTOR SERIAL WORK, all verified green at wrap-open:** `--pri-hover` re-sync in 9
   snippets (snippet gate 18 fails → **rc=0**, enacting the minted `s198-D1` values — the drift
   `ds-032` predicted) · Anchor-nav radius literal removed (radius gate → rc=0) ·
   Combobox/Multi-select hand-drawn chevron swapped for the library glyph (icons → **0 UNKNOWN**)
   · MIGRATED_SNIPPETS +9 · CATEGORIES +9 · full regeneration chain · gate suite: snippets 0 ·
   a11y 0 · radius 0 · coverage 0 · icons 0 · all five `--check` rc=0 · type ratchet **1,097**
   held, every new file contributes 0 — ⚠ the carried figure 1,101 was STALE, correct it
   wherever the banner would restate it · `_validate_state_contrast` = honest COULD-NOT-ASK
   (playwright absent; CI render job is its proof of record) · `_build_all.py` deliberately NOT
   run.

## Contradictions and pitfalls to carry HONESTLY (Dave #165 — mandatory section)

- (a) The wave-3a spend (964K) bought review surfaces and findings, not the components it was
  briefed for — say so plainly; the containment (step-0 tables) is the credit, not the brief.
- (b) Lane G: **`--check` has no contract** — 4 of 13 generators write unconditionally on
  `--check`; the base brief told six lanes it was read-only. FALSE for five scripts. Priced
  candidate, not built.
- (c) Lane L vs `s201-D2` vs memory: the 44-token enforcement record is split three ways —
  Dave's store correction, do not resolve.
- (d) Lanes A/J measured the snippet gate opposite (rc=1 then rc=0) — BOTH true at their
  moments; the conductor's re-sync sits between. Timeline it, don't reconcile it away.
- (e) Two-red-law: Stat-card arrows (fill seat, 1.98:1) + Kpi-tile inheriting the same — lanes
  E/K measured 7 of 8 panes disagreeing; **must move together, Dave's.**
- (f) The 9 new components are gated but their four-theme DARK legs ride on lane G's repair —
  CI has seen NONE of this; the push + CI read-back happens after your commit, conductor's.
- (g) `notes/_receipts/_tmp203h/` deleted by the conductor via the delete grant; gitignored
  `_to_delete/s203j-renders/` remains (9 PNGs, harmless).

## Dave's open decisions — DIGEST, never flatten

~50 PROPOSED items across 12 receipts. The banner points at **`reviews/REVIEW-203-INDEX-2026-08-19-v1.html`**
(the 18 wave-3a spreads with flags) + the six 3b receipts; pull forward as headline-worthy ONLY:
the two-red stat-card/kpi seam (e) · the itinerary derived-page-vs-xlsx question · hit-area gate
promotion (advisory→wired, and MARK_TIER warn→fail) · Sidebar-nav vs Navigations promotion ·
the dropped segmented-label type-scale binding (STILL OPEN from #202, untouched today).

## DO-NOT-RULE (binding on you)

Everything in the receipts' decisions-needed lists · the segmented type-scale binding · the
glossary build · store corrections (s201-D2/s114-D5 conflict) · component promotions · colour
swaps · the frozen itinerary files · wiring ANY new instrument into `_build_all.py`.

## Mechanics you must honour

- Reconcile EVERY dirty path by name against the receipts before staging; re-run the reconcile
  immediately before staging; ⛔ never blind `git add -A`.
- T3 commit via `knowledge/_git_commit.sh` with `SESSION_N=203`; **FRESH `printf` msgfile per
  invocation INCLUDING after refusals** (class count 8); read the subject back from `git log`
  and assert the prefix count is exactly 1.
- Ritual steps 1/2 heal the two date zones; step 2g rebuilds `_memento-index.json` LAST (the
  structural fail the rehearsal already names); banner figures PARSED not paraphrased — the stop
  line line must carry the literal ruled phrasing.
- ⛔ NO evidence pointers into rolling files (s177-D1). ⛔ No push. ⛔ No `_build_all.py`.
- Deliver BOTH title lines (this session's + next's) in your report; the conductor relays them
  in chat [[delegated-wrap-rename-relay]].
