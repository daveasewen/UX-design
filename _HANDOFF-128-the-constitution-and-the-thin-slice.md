# HANDOFF #128 — #277 → #278 — THE CONSTITUTION AND THE THIN SLICE

> ⚠ **This file is NEWER than `_CHAIN.md` and OUTRANKS it** (and `_HANDOFF-127`). Written by the #277 conductor (Opus 5 by mistake → **Fable 5.1** from his *"I've switched to fable"*) at FILL ≈290,000 — **PAST the 180K stop line, past the 220K tolerance, past the 256K hard wall — DECLARED**, BEFORE the delegated wrap. Every claim is a receipt.

## ⛔ READ FIRST, IN THIS ORDER
1. This file. 2. `_CHAIN.md`. 3. `python3 knowledge/_parked.py --check`. ★ `pip install tiktoken --break-system-packages` before `_checkin.py`. ★ Two seats' worth of stale `.git` locks live in `.git/_orphan-locks/` (11 zero-byte + `tmp_obj/`, 72 entries) — a midden, never deleted, whether the wrap empties it is Dave's.

## ⛔⛔ DAVE'S — VERBATIM. Source: `notes/_lanes/277/DAVE-RULINGS-2026-09-16.md` (every word, in order)
- *"we have a lot of tokens to use up"* · *"smash the subs and use a lot of fable judgement"* · *"you've been on opus for a while by mistake, I'm not sure I trust its judgement, I've switched to fable, can you check its work please"* · *"Fable can be used liberally when judgement is crucial"* · *"we have 2 days to use 60% of our tokens for the week, we need to rip through this work"* (resets Thu 23:00; both buckets were 41%).
- The KG audit ask, the layers ask (*"accessibility nodes should be design governance … 'system constitution' or 'codex' or 'system management'"*), the augmentation ask (*"orthogonal, lateral or meta"*), *"research as much as you see fit"*, *"consider how this effects our thin slice technique for design knowledge retrieval at build time"* — all verbatim in the file.
- Three exports, three read-backs, three *"go"*: charts (a/a/b) · icons (a/a/**c**/a, two notes) · audit (six × a, *"record: Constitution"*, two notes).

## 13 RULINGS 590 → 603 (`_rulings.json`, every one by `_inscribe_ruling.py`, 0 deletions)
- `s277-D1..D3` (`407f20b`) charts: 29 authored citations land, two replaced · donut shares the pie file · 47 family edges, dv-013 off. **ENACTED `bedf383`** — 87 obeys spliced insert-only, corpus 81 → 168, explorer v1.14.
- `s277-D4..D7` (`08ef58d`) icons: 688 nodes enter · `usesIcon` from the byte-match (371) · **NO default for the 15 multi-active bases — his manual review, and the second defect is the EXPORTER'S** (bare `-active` = Figma enumeration order; `dentist-active` is a price tag) · 12 logos enter, 10 visibly unbound, separate review owed. **NOT ENACTED** — the land is #278's first lane.
- `s277-D8..D13` (`e863c3c`) audit: **one graph, three views by force; the record is THE CONSTITUTION** · scope per guideline file · **thin slice wired as step 1** (`_compose_slice.py`, 415K → 19K on a dashboard) · twelve reading verbs · tokens at group+tier (s269-D3 honoured) · ASK first. **NOT ENACTED.**

## ⛔ SEQUELS — RULING-SHAPED, HIS, NOT RULED
- `s277-D13` clause: *the thin slice is a step-1 seed, not session state; ASK is called mid-session and reads the Constitution live* — my read-back answer, written INTO the ruling as the open question. **One word at #278's opener.**
- `P-277-5` (to park at wrap): npm registry distribution of tokens + components, Angular/React emitters — *"tangential … some time"*.
- `P-277-3` the 15-base manual review (instrument = the sheet on `REVIEW-icons-2026-09-16-v2.html`) · `P-277-4` the logo review · both to park at wrap.
- Not ruled and still his: the `parts ≤ 5` vs `Maximum 6` pie cap (chart-pie.meta L16/L25, a gate carries the 6) · dv-013 on bar (off by his silence) · the precedence ladder + derived scope (A2's proposal, on the page, UNRATIFIED) · `_validate_icons.py`'s twin-arm assumption (known-unsafe on 15) · `P-272-1..4`, `P-273-1`, `P-276-1` · the push (**≈25 local**).

## RECEIPTS — 19 lanes, ≈3.4M sub-tokens, every commit under `notes/_lanes/277/`
CO `8c80aa2` · CJ(F) `f84eb78` · CV `d84eb72` (21/8/8, page failed on sight) · CP `66fdc49` · CH `81e845a` (P-277-1 dv-019 index row carries dv-017's sentence; P-277-2 chart-meta case split, 279 files) · IX `ca294b4` · conductor Fable check `bf9978c` (2/29 `$why` false-mechanism, missed by three Opus seats) · LA `e19fbce` · FV(F) `7102f9d` (6 RED of 87) · LL `bedf383` · RI `5520d43` · RIV(F) `edd33fc` · RIF `702a0ee` (the sheet moved the recommendation) · A1 `13a4cf2` · A2(F) `e958e6d` · A3(F) `3ca6301` (33 sources) · A2V(F) `3a37bcd` (22/9/3) · A2F(F) `be08d90`.
★ **Pattern of the day:** every Opus author lane shipped a page that passed its driver and needed a Fable seat to fail it on sight or on inference. The verifier that reads the *proposal against the report* misses what the one that reads *against the live file* catches. Fable check + Fable verify are now the shape, on his word.

## #278 — FIRST MOVES
0. Opener: pushed? `_checkin.py`. Read the `s277-D13` clause back for one word.
1. **Land icons** (`s277-D4..D7`): `notes/_lanes/277/icons-propose/gen_kg_icons.py --land --ratified s277-D4` — the door's `RATIFIES` is `()`, open it to the four ids; RIV's A3/A4 pre-land conditions (anchor the nav-rail null on the literal "NOT rebound" clause; drop live integers from `meta.schema.diff`) first; the 15 bases carry NO `activeVariantOf`; explorer chip `assets` OFF; grammar +3 kinds by addition. Opus land + Fable verify against the live tree.
2. **The Constitution wave** (`s277-D8..D13`): ONE lane cannot hold it. Order: (i) `_compose_slice.py` wired as step 1 with the contract + ASK door (D10+D13, the reader everything waits on; s274-D11 applies) → (ii) explorer relabel to three views, Constitution named, chips (D8) → (iii) `scope` per guideline file, 34 rows (D9) → (iv) twelve verbs (D11) → (v) tokens at group+tier (D12). Fable on (i) and (iii); measure the 19K-vs-111K claim on the real generate step.
3. Wrap owes: park P-277-3/4/5 with proven tripwires; `A2-AUDIT.md`'s "316" stands wrong in a committed file (363) — corrected by addition only.
**Cautions:** `model: fable` where judgement, `opus` where mechanical · verify against the LIVE file · screenshot in a never-driven context (pages save DOM to localStorage on unload) · `--numstat` re-read from the SHIPPED sha (three lanes quoted amended-away shas) · builders that write on import (`_build_page.py`, `_dry_run.py` class) · never `git stash` / `gen_kg_edges.py` / `_build_all.py` / `_build_kg_explorer.main()` in a read-only lane.

## STRUCTURAL REDS AT THE SEAM
FILL ≈290K (HARD WALL BREACHED — the first time; his *"rip through"* was the reason, DECLARED not excused) · boot 83,636 = 13th breach · `_validate_lane_ownership.py --selftest` 3/3 → 2/3 (a `_CHAIN.md` line-shape bite, not in any lane commit) · resolver FAIL(6) `data-grid` inherited · dirty `_REHEARSAL-LOG.jsonl` / `_GRADE-DECISIONS.jsonl` · `/sessions` disk ≈96% · date split: session opened 09-15, most work 09-16.
