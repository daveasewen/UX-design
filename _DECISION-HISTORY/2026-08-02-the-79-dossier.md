# #79 — Two premises died before they reached a build

provenance: local_8eec8d6c (#80 peer sub; the subject session is #79) · 2026-08-02
status: observed

**Spine entry:** `_LIVE-STATE.md` ⏱ #79 delta · **Ledger:** `notes/_MEMENTO-DECISIONS.md` § ★ #79
(#79-D1…D3) · **Commits:** `cd9975b` (DV-D19 closure) · `01e8baa` (P5/P6/P7 build) · `668857d` (wrap).

⚠ **Every line number below is as at `668857d`, #79's own wrap commit, re-derived with `git show`
rather than copied from prose.** #80 has since edited `_gauge_tokens.py`, `_capture_gate.py` and
`_build_all.py` to enact #79-D1, so the live tree has already moved under two of these citations;
where it still agrees, this file says so. [[measure-dont-convert-units]]

## Why this file exists

Three subs built P5, P6 and P7 on disjoint scopes and every claim came back to be replayed rather
than believed. That is the delegation regime working as designed, and it is not the finding.
**The finding is that two of the conductor's own premises died mid-window — both before they
reached a build, both inherited from documents that read as settled inventory, and both killed by
the same move: probe the SOURCE instead of trusting what a plan says is missing.**

## Premise 1 — P5's stated target was already covered, by import

The #77 plan scoped P5 as a selftest *"pinning the ruled budget triple (160K/200K/256K) and the
cache behaviour — the constants every wrap is graded against are currently unbitten"*
(`notes/2026-08-02-handoff-testing-regime-plan.md:276-277`).

**They were bitten.** `_capture_gate.py:2756 selftest_preflight_tokens()` asserts
`(gauge.BUDGET_AMBER, gauge.BUDGET_WORKING, gauge.BUDGET_HARD) != (160_000, 200_000, 256_000)` at
`:2767` — **by import from `_gauge_tokens.py`**, so there is exactly one authority, and the failure
text at `:2769-2772` already records which number is whose (WORKING is Dave's, HARD is sourced at
93% MRCR v2, AMBER derived at 80% of working). *(Still true in the live tree at the same lines.)*

★ **Building P5 as written would have put one of Dave's rulings in two places that can drift.** The
second pin would have passed its own selftest for exactly as long as the two agreed, and gone
quietly wrong on the day he re-dialled one of them — a failure mode with no red state at all. P5 was
re-aimed at what was genuinely naked: the counting path, the cache-keying, and degraded-measurement
honesty (six arms, `_gauge_tokens.py` 238 → 469 lines at `01e8baa`).
**The rule was sound; only its premise had aged.** [[premise-ages-faster-than-rule]]

## Premise 2 — the 320-vs-4,321-line gap was not drift, it was a declared shim

P7 was scoped as *"drift check or boundary declaration for the two `memento-package/` machinery
copies"* (`…plan.md:280`). The obvious reading of `memento-package/machinery/_capture_gate.py` at
**320 lines** against `knowledge/_capture_gate.py` at **4,321 lines** (both measured at `668857d`)
is catastrophic drift.

**It is not drift, and the file says so itself.** Its docstring, lines 4-10: *"This module is NOT a
copy of that file — it is a small, standalone module that reproduces the behaviour of those four
functions and nothing else."* The four are `cg.chain_parts`, `cg.measure_tokens`,
`cg.measurement_degraded`, `cg.read_chain_tk` — and they are exactly the four `_gen_chain.py`
actually calls (re-derived by AST: those four `cg.*` names, no others, all satisfied by the shim
with no extension). ★ **A wholesale comparison would have gone red at every wrap, forever, and been
a FALSE POSITIVE every time** — the [[unkeyed-gate-vs-roll2f-tension]] shape, where a new gate makes
a correct state unreachable.

So `knowledge/_validate_package_delta.py` (NEW, 530 lines) audits the shim's **declared
provenance** instead, by AST source-segment hashing against the named port commits. The reason is
recorded in the gate's own METHOD PROOF (`:36-47`) and is itself a measurement: the docstring's
cited line ranges *no longer address the right text* after four post-port commits, so a line-range
diff would have silently compared the WRONG code. `ast.get_source_segment` finds each name in both
the historical blob and the current file and hashes only its own span — proven green across heavy
unrelated drift, which a whole-file hash could never have distinguished from a real break.

⇒ **[[premise-ages-faster-than-rule]] twice in one window.** Both premises were plausible, both were
written down, and both were killed by reading the artefact instead of the inventory describing it.

## P7 — the shipped package could not catch a stale chain title

The probe that killed premise 2 found a live violation underneath it. `memento-package`'s
`_gen_chain.py` was declared **verbatim** in `_MACHINERY-MANIFEST.md:14` (`24ff35b`, "22,094 / 391",
`verbatim? yes`) and was **54 lines behind** its `knowledge/` original in BOTH in-package copies —
missing the #73 forward-title block and the stale-title refusal. ⛔ **The package we ship could not
catch a stale chain title, and nothing had ever been positioned to notice.**

Repaired in both copies (`01e8baa`: `54 0` on each), and all three copies now hash identically —
sha256[:16] `e3d6121fe8d776ef` for `knowledge/`, `memento-package/machinery/` and
`claude-plugin/memento/machinery/` (replayed). The manifest's false line claim is corrected to
`514f4bd` / "25,616 / 445" in both copies.

★ **P7 needed no new ruling — Dave had already ruled the boundary at #64**: *"no Apollo file MOVES
into it — copies only, and every copy is delta-audited"* (`memento-package/_PACKAGE-SPEC.md:13-14`,
quoted from the file). The rule existed with nothing built to enforce it for fourteen sessions. The
new gate is the enforcement his ruling always implied, in four arms: verbatim set · shim provenance
· cross-copy identity (both directions, plus a known-files list so a file vanishing from BOTH copies
cannot pass the pairwise check) · unknown files fail LOUD rather than being enumerated away
([[scope-blindness-gate-vocabulary]]). [[instrument-without-a-consumer]]

## The demotion was of kind, never of the finding

P5's arm C was **born RED and correctly so**: at `668857d`, `_gauge_tokens.py:166` was
`return len(text) // 4, "crude-estimate"` — `count()` guessed silently when tiktoken was absent,
which `ds-025` names as the defect itself. The GM header had warned about the silent estimate for
sessions and nothing had ever bitten on it.

It was **not** fixed in-window, and the reason was verified at source rather than inherited from the
sub's report: `assert_budget_clears_floor() → measure_boot() → count()` was called at
`_capture_gate.py:2774` as a bare list comprehension with **no try/except**, so a raise would have
taken out a 39+-check gate mid-sweep and it would have reported nothing at all
([[a-crash-is-not-a-fail]]). The build step therefore ships **ADVISORY** (`_build_all.py` STEPS
`:84-85`, `ROUTE_ROWS` `:332`), the same shape as usage-history and consult-receipts. ⚠ **A demotion
of KIND, never of the finding: the arm still runs, still goes red, still prints its named cause
every build. Promotion to ABORT is Dave's word.**

✅ **RULED at the wrap (#79-D1, Dave): *"Make it refuse."*** Chosen over a louder estimate and over
leaning on the commit script's session-start self-heal. **The fix is PAIRED** — `count()` raises
loud and named, and the gate's caller must catch it and report one named failure.

## DV-D19 closed in both halves, and the probe is inscribed with the closure

Board item (a). The last open pin — swatch-at-rest — was put to Dave on the v2 real-canon specimen
in the doc's own words, neither option pre-selected, and read back as SENSATION before inscribing:
*"on a chart you haven't touched, clicking a swatch just dims that series; nothing latches."* He
ratified in his own words, unprompted by any option label: ***"DV-D19 — the mode latch, shown on
real canon prototype is perfect as it is."*** ⇒ NARROW. **No code change; `canon/dv-legend.js:166`
(`if (st.mode === 'isolate') { st.mode = 'check'; }`) is correct as written.**

★ **The probe that priced the alternative is inscribed WITH the closure, not restated in the
banner**, so a reopening has to re-run it: `active()` (`:51`) returns `st.visible` at rest and
`st.focus` in check, so under the wide reading `visible[]` freezes at the first swatch click; the
Reset guard at `:147` — `count(st, st.visible) === st.ids.length && st.mode === 'rest'` — breaks in
**both** halves; blast radius 14 legend-bearing surfaces. Wide was never a one-line latch change.
Closure by ADDITION in `_proforma/_DATAVIZ-DECISIONS.md:967-990`; `_REVIEW-SIGNOFF.md` v2 row now
FULLY SIGNED OFF.

## P6 — the probe was the deliverable, and the sub corrected the brief

`_GRAPH-REPORT.md` had no reader. Re-derived at the pre-#79 tree (`eff9eb7`): **7 files** mention it
outside `_retired/`, carrying 9 mention lines, and none consumed it. The honest near-miss was named
rather than counted away — `_validate_standing_instructions.py:83-87` genuinely `open()`s it inside
a generic `knowledge/*.md` sweep but reads only the first 40 lines looking for a `STANDING:`
self-declaration, so it can never fail on its content. **A real open, not a consumer.**
[[unmatched-grep-is-not-an-absence]]

Built `--check` (content comparison, never mtime) reusing the established `_gen_chain.py --check`
idiom, plus three red arms on `_build_decision_graph.py` for finding codes that had shipped with
zero coverage since #75 (`unknown-edge-type`, `malformed-target`, `self-loop`), each pinning the
SPECIFIC code so a silently disabled check cannot hide behind a different one firing. ⚠ **The sub
also told the conductor its own brief was wrong** — the "cross-check against `_decision-graph.json`"
was a template mismatch against the real data lineage — and said so instead of silently
reinterpreting. That is the behaviour the delegation regime exists to buy.

⚠ **Declared limit, in the tool's own docstring** (`tokens/_build_blast_radius.py:23-27`): wired
directly after the unconditional-write step, `--check` inside `_build_all.py` can only prove
same-process consistency. It does not by itself catch a `_GRAPH-REPORT.md` that drifted from source
between builds. Honest scope, written down at birth.

## Replayed, not taken on report

`_validate_package_delta.py --selftest` EXIT=0 · the same gate against the real repo EXIT=0 ·
`tokens/_build_blast_radius.py --check` EXIT=0 · `--selftest` EXIT=0 ·
`_build_decision_graph.py --selftest` EXIT=0 · `_build_all.py` STEPS = 85 and ROUTE_ROWS = 85, each
step resolving to exactly one route. *(All re-run for this dossier, not copied from #79's banner.)*

## Declared, not smoothed

- **The banner's "+1,247 ln" for `01e8baa` does not reproduce.** `git show --numstat` measures
  **1,228 insertions / 130 deletions** across 9 files. The discrepancy is unexplained; the git
  figure is the artefact and this line is the correction by addition, not a trim of the record.
- **"an inventory written five sessions ago" does not reproduce either.** The two source documents
  measured: the plan and the periphery inventory are #77 artefacts (added at `4661333`, **two**
  sessions before #79); `_MACHINERY-MANIFEST.md` is `provenance: #65 · 2026-07-31` (**fourteen**).
  Neither is five. The finding is unaffected — both premises were older than the session using them.
- **"19/19 standalone" for the shim was not re-run here.** What was re-derived: `_gen_chain.py`
  references exactly **four** `cg.*` names and the shim satisfies **4/4** with no extension.
- The pricing figure (**159,902 tape** at the P5/P6/P7 close, against Dave's 200,000, boot half
  UNMEASURED) is quoted from the #79 closed band at `GOOD-MORNING.md:491`; it is a throughput
  reading and cannot be re-measured after the fact. ★ **The amber line called the wrap, not a
  natural end** — the gauge working as a throttle.

## Still open at #80

The gauge refusal build (paired, DO-FIRST) · **whether the P5 arm is promoted ADVISORY → ABORT —
explicitly not taken by an agent** · headroom before any legend change (group 78 B free) · the
unregistered-source gate + bar-motion half · D3 lockup detail · trigger index · radios live-test ·
the six `_decision-graph.json` edge types, unruled since #71.
