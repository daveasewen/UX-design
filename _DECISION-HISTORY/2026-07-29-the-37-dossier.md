# The #37 dossier — the session that measured what it could not see, and left its own record thin on purpose

```
provenance: 383e10dd-42d4-4e5e-8ef1-0dd5ddbeb367 · 2026-07-29
status: observed
```

*Written at session #44, 2026-07-29 (Wed evening), Opus solo, Dave live — **six sessions late**.
Reconstructed from **four commit messages and nothing else**, per the guard that has been riding
`GOOD-MORNING.md:400` since #40: `3488332` · `e89c06c` · `9cd2ae7` · `aa8f66b`. ⛔ **No `_LIVE-STATE`
delta, no banner and no ledger entry was consulted in the writing** — those are state lines, and the
arc is not in them. Ledgers this arc touches: `notes/_MEMENTO-DECISIONS.md` § ★ #36 (the band) ·
`knowledge/_DS-IMPROVEMENTS.md` ds-023, ds-025. Spine entry: `_LIVE-STATE.md` ⏱ delta #37.*

---

## Why this document was owed at all

**#37 never wrapped.** At roughly 55% fill it priced its own full wrap at **~73%** — past the 63 Dave
had just ruled at #36 — and took an **Amber spine-flush** instead: capture-ritual **step 1 only**,
`GOOD-MORNING.md` deliberately not written, no rename, no fresh window, session continuing.

That was the correct call under the band it had just enacted. *"Under-pricing it to fit is the
failure the band prevents."* But it has a consequence that took three sessions to surface: **a flush
writes state lines and no narrative.** #37's own delta says so plainly — *"the flush left no dossier,
so the state lines carry the findings alone"* — and that density is exactly what pushed the read
chain over its warn for every session after.

★ **The debt was structural, not sloppy.** #37 chose to leave a hole and named it. The failure was
that nothing then chased the hole for six sessions, and the four commits carrying the arc were one
roll away from leaving the record entirely — which is what the `:400` guard was for.

## What #37 actually did

### 1. The band, enacted — `3488332`

ds-023 re-stated as a **band**, not a ceiling, on Dave's #36 ruling. `PREFLIGHT_CEILING` deleted;
`BAND_FLOOR` / `HARD_STOP` / `MARKED_MAX` pinned to **(45, 60, 63)**. `45–60` passes clean · `60` is
the line and is livable · `61–63` tolerable when **rare and marked** · **`>63` UNRULED, and the gate
says so out loud** rather than inventing a stop Dave never picked.

★★ **That last clause is the whole lesson of the commit, stated by the commit itself:** inventing a
stop is *"exactly how the old `>=45 -> FAIL` got in — ruled by a delegated agent at #31, never by
Dave."* The 57% control fixture has now **flipped twice** — passed at birth, failed from #34, passes
again — and is kept live as the evidence that **only the enforcement ever moved**.

### 2. The auditor that took its clock from the artefact it audits — `3488332`

★★ **THE FINDING.** `gauge_log_continuity` had been **BLOCKING since #34 and could not fire.** It
read the current session number from the **§C stratum key** — written by ritual step **2f**, the very
step whose omission it exists to catch. #36 skipped 2f, the clock froze at #35, the check asked
*"does #34 have a block?"*, found one, and reported **"the 2f split landed"** while #35 and #36 were
both missing.

**It does not self-correct.** The clock stops at the last compliant session and reports GREEN for as
long as the lapse continues. ⇒ **An auditor may not take its clock from the artefact it audits.**

⚠ **#35 and #36 were declared as HOLE markers, not back-filled.** Reconstructing a post-mortem block
from the record *"would forge the dataset the throttle is re-derived from."* The refusal is the
ruling.

### 3. The fallback that had never once run — `3488332`

Promoting the ★ LATEST banner to authoritative revealed that **`BANNER_SESSION_RE` had never
worked**: unanchored, it matched **prose mentioning "★ LATEST"** in the header and returned **#29**.

★ **A fallback path is untested code until something forces it.** #37 logged this as the **third**
substring-vs-structure defect in the same file (#35's usage probe, ds-016's index). Now anchored to
the blockquoted heading, with the decoy line baked into the fixture.

### 4. ds-025 — the numbers no session can observe from inside itself — `e89c06c`

**NEW, OPEN.** Every load-bearing number in the throttle is one a session cannot see:

- **the boot** — first measured at #37: the **disk-resident half alone is 17,810 tape / ~14.0 pts**,
  against an inherited *~17 for the whole thing*; the **harness half is unreachable from any mount**;
- **`bill`** — the log *"has never held a single tape/bill pair"*, so ds-021(c)'s `n>=4` trigger never
  advanced past **n=2**. **A session cannot read its own meter**;
- **`fill`** — the gate's own header admits it cannot see it.

**Three remedies written up, NONE chosen** — and the commit names why: *"ds-023 is the cautionary case
for letting an agent pick."* ★ The session that had just repaired an agent-invented rule declined to
invent another one.

Also recorded in the same commit, and it is the seed of the #33-chain reckoning that ran through #41:
`MEMORY.md` (4,517 tape) was **larger than the entire contract-priced read chain** (4,194), and the
chain *as actually read* was **7,609 tape** — the header being correct about its contents while the
contract text *"sits at line 111, below the worklist, so nobody reaches it without paying 3,415 tape
the contract does not count."*

### 5. The worker hand-back the conductor did not collect — `9cd2ae7`

`worker-context-degradation` **did its job and stopped at the boundary** — its own receipt reads
*"No git — a conductor reconciles and commits."* It handed back and waited. #37 **did not collect at
its first commit**, treating the files as *"not my paths"*. **Dave caught the gap.**

★ The conductor rule is that **the conductor commits and workers hand back** — so those files were
#37's to reconcile, not anyone's to leave. Committed **AS FLOATED**, deliberately unadjudicated:
nothing promoted, nothing ranked, nothing in the commit citing it as authority. **A floated item is
not authority; surfacing it into the record is not promotion.**

### 6. The spent line, and the cost #37 charged the future — `aa8f66b`

**One surgical line in `GOOD-MORNING.md`.** Its "TITLE THE NEXT CHAT" line still briefed #37's job —
enact the band — **as work still to do**. #37 had done it, so *"a cold reader opening on that banner
would have redone landed work — the confident-false-inscription class, live in the file."* Marked
**SPENT**, pointed at the LS delta. GM otherwise untouched, because an Amber flush rules out writing
GM and **marking a hazard is not authoring a handoff.**

⚠ **Two costs, measured not assumed, both declared by #37 against itself:**

- GM compactable **11,683 tape against the 12,000 block — 317 headroom, down from 647.** *"The next
  session should plan its GM spend before starting, not discover this at the gate."*
- **The M10 read chain went to 5,110 tape, over its 4,500 warn** — and #37 named itself as the cause:
  *"My #37 delta is dense on purpose… but it made the chain more expensive for every future cold
  start."*

★★ **That is [[gate-inside-the-growth-loop]] observed six sessions before it was named** — and the
same shape #43's wrap hit head-on when it put the chain over its own warn and had to shave its own
banner back under.

⚠ **Generated indexes were deliberately withheld at the flush** (`e89c06c`) because they referenced
the lane's then-untracked files, and an explicit guard aborted the commit for exactly that reason.
`aa8f66b` re-committed them once the reason was discharged, *"so the tree is consistent rather than
deliberately dirty."* ⛔ **Never `git add -A` on this tree** — #37's words, still true.

## What #37 verified

`_build_all.py` **72/72 exit 0** · capture-gate selftest green · **6 mutations run, all 6 go red**
(BAND_FLOOR, MARKED_MAX, the in-band branch, the below-band branch, the banner anchor, the clock
cross-check) · one LATEST heading · stamp at line 14, inside the 40-line window the wrap check reads.

★ And it left the repo **correctly red**: the new clock check failed on banner #36 vs stratum #35,
*"correctly, and it clears when #37's own 2f runs at wrap."*

## The throughline, and the part that is still open

**#37 is the session that turned the instruments on itself and reported what they could not see.**
Every finding in it has the same shape: **a mechanism was reading its state from something it was
supposed to be checking** — the guard from its own log, the banner regex from prose, the throttle
from numbers no session can observe, the conductor from "not my paths."

⚠ **STILL OPEN, raised by #37 and unruled since:** ds-021(c) wants one **tape/bill pair per wrap**
and Dave's ruling at `n>=4`. **The log has never held a single pair.** #37 named the reason as
structural — *"`bill` is what the window charges and no session can observe that from inside
itself, the same wall that left the boot unmeasured for 36 sessions."* **ds-025 is that wall, and it
is still open.**

★ **A closing correction this dossier is obliged to make about itself.** #37 raised the pair
requirement and could not satisfy it. Six sessions later the ratio is **still the original n=2**, and
every banner since has carried `×1.57 PROVISIONAL` forward as though it were closing. ⇒ **A
requirement that no session can discharge does not decay into a fact; it stays a hole, and the
`PROVISIONAL` tag is the only thing keeping it honest.** ⚠ #41 measured a real **2.11×** against
it — [[assertion-propagation-gap]], and named here rather than resolved.

---

*History: this file. Spine: `_LIVE-STATE.md` ⏱ delta #37. Retires the guard line at
`GOOD-MORNING.md:400` ("until: the dossier is written"), born #40 by the 2c exit check.*
