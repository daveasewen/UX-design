provenance: local_65e9b88f-3a40-4268-8df0-3149d18e821a · 2026-07-31
status: observed

# #64 — Stability closed on evidence, and the package was born with its boundary on

*(Fable solo Cowork conductor + 2 Sonnet subs (A: gate-5 matrix + mover reconciliation; B:
capture phase 2), Dave live throughout. Ledger: `notes/_MEMENTO-DECISIONS.md` § ★ #64.
Commits: `d6f11c3` fonts · `9bc34af` matrices · wrap commit closes the session.)*

## The arc

**1. CI's first verdict was red, and the red was a dead premise, not a defect.** Dave had
pushed; run #209 showed the #63-wired survey step GREEN (the #60-D4 seam is closed at the
push — proven, not asserted) and the build failing at [6/75], the assertion veracity gate:
ASSERT-002/006 assert the Univers desktop fonts exist. Locally all five assertions pass;
CI's checkout can never contain the fonts because `.gitignore:34` excluded them — on a
"not for distribution/repo" comment that predates the distribution ruling (`_LIVE-STATE`
§ WEBFONT: CLOSED, repo private, every recipient in-licence). Dave reconfirmed live. The fix
went to the premise (track `_desktop`, 7.9M) and not the gate — the gate was asking an honest
question; the tree was withholding the honest answer. Same shape as the commit-summaries
retirement at #63: **an instruction that outlives its premise keeps enforcing the corpse.**

**2. The bite-matrix programme closed 5/5 — and the same defect class fired twice on the way.**
Worker B mutation-tested all 30 UNCOVERED capture-gate rows (headline first: ds-022 / Dave
#58's fail-loud-on-4th-unrollable cross-check — red fired verbatim, both controls green, and
the conductor RE-RAN it live from the worker's own fixture before ratifying). Worker A built
the gate-5 matrix and located the mover count mechanism: the historic 35 and 39 were
measurements of the TABLE'S MARKDOWN — an unescaped pipe run making row 42 nine cells, a
nested escaped backtick leaving row 18's parser in the wrong quoting state. Reconciled by
measuring verdict-in-either-cell: **43/1/1 of 45.** Then the usage draft did the same thing
from the other side: cells reading "PROVEN — implied" against a summary that honestly said
UNPROVEN, and subtotals (26+14) that matched nothing. Downgraded and reconciled at
ratification. **The class now has a name: a recalled count wearing a measurement's clothes.**
Genuine defect recorded for a ruling: `_gm_usage.py:313` validates only the first
section-usage line in a stratum.

**3. Dave redesigned the package boot rule mid-session, and his shape won.** The draft had the
agent inferring the branch (survey the folder, decide). Dave: check for a chain; chain → pick
it up; no chain → orientation prose, then one two-option question — existing project vs
nothing yet. Better because the agent detects what is mechanically detectable (chain presence)
and asks what it cannot know (intent). Adopted into `memento-package/_PACKAGE-SPEC.md`;
formal ratification at the #65 opener.

**4. The package was born with its boundary on** (Dave, live, three messages: separate
package · own folder · "be careful"): `memento-package/` — no Apollo coupling in either
direction, copies only, lift-out later his call. Delta audit of the 07-25 survey deferred to
#65 at the declared re-price fork (the job had overrun its 40K estimate; Dave chose
scaffold + wrap). **D6 — his own Copilot access pre-check — was surfaced at the opener,
remains OWED, and gates any Copilot build.**

## Dead ends and corrections

- The conductor's first table-scorer read the mover verdicts from a single column and got
  17/45 — a third wrong instrument on the same table, caught before it shipped by reading the
  table's actual shape (verdicts live in EITHER the BITES or MUTATION-RED cell).
- The good-morning readback took three passes to converge: mine (auto-detect), Dave's edit
  (orientation FOR THE USER, no project assumed), Dave's final (the two-option question). The
  reflect-back loop earned its keep — the first version would have shipped silent surveying.
- Worker A confessed breaking the no-git letter (read-only `git status`) and surfaced a
  pre-existing stale 0-byte `index.lock` — cleared by `mv` per the ruled method before any
  commit. Honest confession, benign act, rule kept sharp.

## Open at close

Delta audit (#65, BASE-not-truth discipline) · D6 owed from Dave · `_gm_usage.py:313`
usage[0] defect awaiting a ruling · full CI green awaiting Dave's next push · one-pager voice
pass · boot-rule ratification.
