provenance: local_4de629f6 (Cowork, Fable #65) · 2026-07-31
status: observed

# #65 — The package goes from spec to v0.1 in one evening, and the #64 defect class bites twice more

*Fable solo Cowork conductor + 2× Sonnet subs · Dave live to mid-evening ("I can watch telly
and eat while you work"). Ledger: `notes/_MEMENTO-DECISIONS.md` § ★ #65. Spine: `_LIVE-STATE.md`
⏱ #65.*

## The arc

**Opener.** Chain read; probes found Dave had pushed (origin level with HEAD), converting #64's
declared UNPROVEN (full CI verdict) to checkable — but the unauthenticated API probe was blind
(named as such, not read as absence). D6 went to a colleague; the boot rule was NOT ratified on
the nod — Dave: "lets check this is the right shape, I have questions." The questions turned out
to be one amendment: plain-language bracket clarifiers on both boot questions. Ratified with them
in. **Why it mattered:** the reflect-back beat caught that a summary of the rule was about to be
ratified instead of its text — the wording was put in front of him verbatim first.

**Delta audit (sub) and the first bite.** A Sonnet sub audited the 07-25 orchestration survey
(BASE, never current truth): 30 claims. Its exec summary said 14/9/2/5 "tallied by eye". Conductor
replay measured the table with a named instrument (`awk`, rule: primary verdict = first token):
12/8/3/7. Total 30 both ways; the split irreproducible under ANY single counting rule — cause
attributed to compound-verdict cells (rows 6, 27) plus a "DEAD (the target half)" (row 28), not
to sloppiness alone. **This is #64's "recalled count wearing a measurement's clothes", caught at
replay this time** — and the correction was written INTO the draft with the counting rule
declared, not argued in chat. Every spot-checked evidence quote held: the table was sound, only
its summary arithmetic was soft.

**The copy, and the second bite — this one mine.** Machinery copied VERBATIM (4 files, sizes
measured, source commits quoted in `memento-package/machinery/_MACHINERY-MANIFEST.md`). My
manifest claimed `_gen_chain.py` needs "exactly four functions" from the capture gate — derived
from `grep -oE 'cg\.[a-z_]+'`. The build sub found `chain_parts` transitively needs
`GM_VOCAB`/`LS_VOCAB` from `_gm_usage.py` — a module the grep could never surface. **A dependency
list from one grep is a list of MATCHES, not of SOURCES** (the unmatched-grep lesson's dual, now
caught in the wild both directions in one session). Manifest corrected at source.

**The shim and the smoke test.** Shim shipped AS `_capture_gate.py` (same module name — the
verbatim import resolves unchanged, so the copies stay byte-identical to their sources; re-diffed
after the smoke test to prove it). Smoke test's green can fail: staleness deliberately induced and
`--check` went red (exit 1). One replay finding attributed, not fixed: `--selftest` from a bare
package folder fails its 3 live-repo bites ("GOOD-MORNING.md is missing") — by design, since the
package ships no project content; 19/19 inside any adopting project. Written into the README as
adopter guidance rather than left to confuse the first tester.

**The zip.** Cut with `git archive HEAD memento-package` — from the committed tree, 13 files.
Dave's instinct was to share a repo zip from GitHub; that would have shipped all of Apollo with
it. The package-only zip is the boundary, applied to distribution.

**Scope cut, ruled at pricing (D4):** `GOOD-MORNING.md`/`_LIVE-STATE.md` stay the PACKAGE'S OWN
convention in v0.1 — those names are Memento's, not Apollo's; configurability stays on the debt
list. This one cut is what made "tonight" honest.

## Dead ends and corrections

- The 07-23 harness-spinoff note: audit found its promised reframe never landed (one commit,
  creation day). Dave ruled: ignore the note, the spec is the record (D2) — cheaper and truer
  than rewriting a thinking-out-loud document to match settled canon.
- My own `git status` calls left 0-byte `index.lock` files twice (sandbox cannot unlink);
  cleared by `mv` per ruling both times, attributed in-chat at the moment they happened.
- First commit attempt refused: fresh sandbox had no tiktoken and the gate would not measure on
  the estimate fallback — the #56 refusal doing its job; fixed by installing the instrument,
  not by accepting the estimate.

## Resolved state

v0.1 committed (`13433cc` machinery + audit · `3bde658` shim + instructions + README + smoke
test) and zipped. Declared UNPROVEN, unchanged: the live Copilot conversation — greeting,
orientation prose, the two-option question as rendered. **The colleague's machine is the first
real test, which fits a package whose ratification note reads "we'll see if it works."**

## Open

Colleague first-boot verdict → #66 · one-pager voice pass (owed since #63) · Claude-plugin
flavour (colleague at home + friend = testers) · CI full green on Dave's next push · generalisation
debt items 2–4 (configurable names · index bootstrap · capture extraction, priced first).
