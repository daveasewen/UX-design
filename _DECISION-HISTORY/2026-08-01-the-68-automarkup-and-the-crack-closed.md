# #68 — the premise died at survey, AUTO-MARKUP was born, and the gauge crack closed honestly

provenance: local_6157f452-8ff0-4ca7-81ed-a64ebf905db7 · 2026-08-01
status: observed

*Ledger: `notes/_MEMENTO-DECISIONS.md` § ★ #68 · banner: `GOOD-MORNING.md` ★ LATEST #68 ·
commits `2a3f6ee` (lockup) · `5f09f2a` (cleanup) + the wrap.*

## The arc

**1 · The sub that stopped was the session's best move.** The dv-lockup build sub, given the
#67 scope brief, surveyed before building and found the brief's central mechanism claim false:
`gen_component_partials.py` had exactly two injection types (CSS, JS) and no markup mechanism —
the thing the whole wave assumed was a drop-in registration did not exist. It stopped with the
tree untouched and forked the choice up. Why this mattered: building anyway would have meant
either hand-copying markup seven times (reproducing the drift the wave existed to kill) or a
build-sub inventing generator machinery without a ruling. The premise-ages-faster-than-rule
lesson, met in the wild one session after it was inscribed — the brief was written #67, stale
by #68, because it described a mechanism nobody had ever verified against the tree.

**2 · The fork was priced and Dave took the machinery.** Option 1 (extend, ~50–70K) vs option 2
(CSS-only, ~35–45K, drift re-admitted). Dave: "1, on instinct" + asked for the comparison in
plain prose before confirming — the reflect-back ran, the ruling landed firm (#68-D1). The
deciding shape: option 2's guarantee rests on discipline, option 1's on machinery, and the
system's own precedent (dv-legend's JS split) already pointed there.

**3 · AUTO-MARKUP was mutation-tested at birth, before anything was built on it.** Two deliberate
breaks: a desynced consumer (exit 1, naming the member) and an undeclared member (exit 1,
quoting the A2 requirement). Only then did adoption start, member-by-member, bar last. A green
that cannot fail is an assertion — the new gate proved it could fail before it was trusted green.

**4 · The cleanup wave's near-miss: a ratified record contradicting the live tree.** The sub
promoted dv-legrow per the ruling, but left the registry `$description` still saying the
universal contract "STAYS EMPTY, for a permanent reason" — a confident false inscription one
read away from confusing a future session. Fixed by ADDITION in-window: dated supersession
line, reversal evidence kept verbatim. The 'permanent' reasoning was correct when written and
was killed by Amendment 2's consumes-manifest — the second premise-death of the session.

**5 · The cleanup sub's false environment claim.** It reported "no git repo detected" — true of
its own sandbox view, false of the tree. Dave asked about this live. The answer that held:
sub reports are testimony; the conductor replayed `git status` (15 paths reconciled by name),
re-ran all five gates in-window, then committed. Work real, claim wrong, nothing believed bare.

**6 · The gauge crack closed the honest way — by disproof, not by fading.** #67-F1's exciting
half (sub-reads-parent = measured mid-window gauge) was killed by a mutation-test with a
falsifiable criterion: quote a commit hash minted in the parent minutes earlier. The probe
found the running parent has no handle at all — absent from the full session inventory. The
useful residue: the boring half (post-hoc calibration on 205 completed sessions) survives and
is available now. An UNPROVEN that stayed exciting for three sessions is now a DISPROVEN that
cost one probe — cheaper than every session it would have haunted.

## Open at close

Three placeholder titles (Dave, at review) · type-composite global red (pre-existing, 1,011,
unowned by #68) · post-hoc calibration as a future lane · horizontal-bar y-label clip observed
at both render widths (outside #68's diff, declared not chased).
