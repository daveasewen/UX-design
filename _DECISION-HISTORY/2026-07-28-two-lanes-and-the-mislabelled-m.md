# Two lanes, and the mislabelled M — how the M-set shed its chart jobs

provenance: local_13f9ebbd-5bdd-4232-87fd-4fc47b4f268e · 2026-07-28
status: ruled — `notes/_MEMENTO-DECISIONS.md` § TWO LANES

Spine entry: `_LIVE-STATE.md` ⏱ LATEST #20 · Ledger: `notes/_MEMENTO-DECISIONS.md` § TWO LANES ·
Chart-side breadcrumb: `knowledge/_proforma/_DATAVIZ-DECISIONS.md` § Open/pending.

## The arc

**1 · The window opened as the M-set build** (ex-M4a → ex-M4b → M5, per the #19 handoff title). Floor
measured at the opener: chain 30,411 tk = 15.2% — the chain ROSE again (29,694 → 30,411) and, for the
first time in three sessions, the header stamp, the ledger and a cold gate measurement all agreed.

**2 · The survey inverted the brief's risk map.** The brief priced sparkline as the risky port (STOP
condition on its inline-scale layout) and scatter as routine. Measurement said the reverse:

- The toggle's JS is not per-file copy — it is an **ADR-0015 injected behaviour** (`canon/dv-behaviour.js`
  carries fit/tip/**table**/seg/csv). Sparkline is a registered member: the JS already sits in it,
  dormant, so its port is markup + CSS only.
- Scatter is **not a member**. The canon route is registration — and the group's universal contract
  (`data-tip="`) forces scatter off its native `<title>` tooltips onto the family popover. Real scope
  the brief never priced. Registration also injects dv-legend's **inert 16,330 B** (the per-member
  opt-in schema question's second live case — Dave's, already logged in the registry's own
  $description). Budgets clear either way: group page 29,334 / 32,768 B — membership doesn't change
  source bytes.
- Two dead-ends worth recording: I first read scatter's header clause "DV-D02 static answer" as
  protecting its `<details>` drawer — the ledger shows DV-D02 is the responsive/fit ruling, tables
  nowhere in it. And sparkline's "flag to conductor if the wave wants toolbar parity" turned out to be
  a question the M-set ruling had already answered — while Batch-2 #2 (2026-07-16) had ruled
  "toolbar for every chart with a data table" all along. The port was re-asserting an existing ruling,
  not overriding an inscribed defence.

**3 · Dave re-cut the frame mid-fork.** Asked to pick a scatter path, he went upstream instead,
verbatim: *"these. are Apollo jobs, I thought M stood for Memento? I want to settle all memento work
before moving to the Apollo charts"* · *"teh coding needs fixing too"* · *"i.e. the M suffix"* ·
*"there are two lanes now"*. The mislabel diagnosis: M4a/M4b were chart-surface jobs swept into the
memento brief because the two-idiom split was *spotted* during #17's memento window — the label
followed the finder, not the surface.

**4 · The ruling** (read back, option-select confirmed): **two lanes.** Lane 1 — Memento, first:
M5 → wrap-ritual section-usage instrumentation → O1′ → O2′. Lane 2 — Apollo charts, blocked until
lane 1 lands: the re-keyed **table-idiom unification (ex-M4a)** + **chart-table-toggle accretion
(ex-M4b)**. M-codes retired on forward-looking surfaces; historical brief/ledger rows verbatim.
Codes deliberately not re-minted — the wave brief that picks the items up keys them.

**5 · "Is there logic in memento for lanes?"** (his question, answered from the repo): one built
mechanism — the capture gate's `--wrap --lane` flag (S-D2), a binary "outside the GM queue" wrap
scope. Everything else is prose + the latent `[born · guards · until]` tags; nothing mechanical
enforces lane order. **First-class lanes are now a registered requirement on O1′** — schema
deliberately not invented mid-window.

**6 · M5 deferred by the throttle.** The survey + fork ate the build budget: Amber (~55% est) before
the mover's first line. Recommended deferral under Dave's own no-new-builds-at-Amber rule (the 07-24
precedent); he took it. Fifth consecutive hand-rolled wrap — `section_a_digest()` called, never
re-derived, §A asserted before and after every write.

## Resolved state / still open

Landed: the two-lane ruling, inscribed three places + this dossier; the survey receipt lane 2
inherits. Open: M5 (next window, brief §11 unchanged) · M11 supervised fire before Sun 08-02 ·
first-class lanes ride O1′'s design window · the per-member opt-in schema question (Dave's) now has
two live cases waiting on it.
