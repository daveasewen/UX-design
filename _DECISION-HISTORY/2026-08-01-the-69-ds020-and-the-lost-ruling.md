# #69 — ds-020 lands, a week-old ruling is recovered, and the review export fails Dave again

provenance: local_0ff0a671-6369-4acb-b15b-03471072ab63 · 2026-08-01
status: observed

*Session #69, Sat 2026-08-01, FABLE solo Cowork conductor, Dave live. Companion to the ★ LATEST
banner (the WHAT); this is the WHY/HOW.*

## Arc 1 — The Memento plugin detour (opener)

Dave asked whether the plugin or the harness was running. Finding: the plugin's boot skill fired on
"good morning" but only pointed at `_CHAIN.md` — the harness's own file; its step-0 (silent
`machinery/` copy) never ran, verified by `ls` + empty `git status` + `--check` byte-match on the
real tokenizer. Dave disabled the plugin: **his reason is want-not-fear** — test done, not wanted in
projects until stable, then OTHER projects. My overwrite-seam alarm (shared `_CHAIN.md` filenames,
two generators) was reflected back and correctly demoted to "Claude's flag, not the basis" — first
draft of the memory laundered my alarm into his motive; he corrected it; the memory now carries his
words. Lesson re-learned: [[feedback-dont-launder-a-premise-into-a-ruling]] applies to memories too.

## Arc 2 — Titles ruled, insight floated

Three #68 placeholder titles ruled in-session (cd2 new copy via `data-lockup-title` + regenerate;
cs1/cs2 ratified as-written). Dave floated a dedicated INSIGHT slot in the lockup → **ds-026**
(FLOATED, his words quoted, machinery observation kept separate from proposal).

## Arc 3 — ds-020, and the waiver that named two conditions under one label

The five-step runbook in the entry was followed exactly: control FIRST (pre/post, both modes,
diffed as computed colours; theme hook found on `<body data-theme>` after a first false-identical
read). The migration itself was mechanical; the finding was at the clears-when: **the DV-D02-A
waiver's "CLEARS WHEN ds-020 is enacted" gates a check that reads `data-fx` fit hooks — work never
inside ds-020**. Scatter: 6 hooks vs bar's 175 (re-measured). Deleting the waiver would have gone
blocking-red on unscoped work. Part-discharged by addition instead. This is
[[premise-ages-faster-than-rule]]'s two-purposes corollary caught BEFORE the deletion, which is the
first time that memory has fired preventively rather than post-mortem.

## Arc 4 — Dave's eye finds four problems; retrieval finds one of them was already ruled

Dave (live, on the real page): responsive missing · key-left/reset-right-wrap-centre · dual gesture
suspect · title-left/popover-right. Verification split them: #1 real gap (queued) · #2 ruled canon,
scatter deviated (`flex-end` vs `space-between`, fixed) · #4 **a LOST RULING** — 2026-07-25 legend
v5 review-edits brief line 12, *"bar keys ranged LEFT / Reset right-inline"*, captured in the brief
and never enacted anywhere (bar included). Recovered and enacted on scatter with an AGENT-PLACEHOLDER
520px wrap-centre breakpoint. **Dave then ruled D3 NEEDS REWORK** — so the recovery was necessary
but my enactment shape isn't what he wants; detail owed at #70's opener.

The dual gesture: **I mis-probed twice** (`is-off`, then hover-only `is-peek/is-faded`) before
reading `render()` and probing the real click contract (`is-ghost` via `paint`). Isolate worked all
along; my "silent no-op" diagnosis was withdrawn in the review doc itself. The real live issue is a
**ruling collision**: Dave's wording today ("additive like a checkbox") vs ★ DV-D17 (swatch ENDS
isolate, 2026-07-27). Surfaced as D4, not auto-changed; Dave picked "discuss live next session."
Lesson: [[unmatched-grep-is-not-an-absence]] extends to DOM probes — name the class the mechanism
actually writes, from the mechanism's own source, before declaring a behaviour dead.

## Arc 5 — The review doc, and the export failing him AGAIN

Review pair built per the two-register runbook; both #66 hard lessons were hit and handled
(specimen copied in-dir; `../canon/type.css` 404 caught in-context; file:// iframe opacity →
postMessage shim; composite font asserted at 20px/HSBC). **And then the #66 failure recurred
anyway: Dave ruled by screenshots — "the radio buttons do nothing."** The overlay existed and was
probed present; what failed is the seam between the doc's own radios and the overlay's export, or
its discoverability at the moment of use. Defect row in `_REVIEW-SIGNOFF.md`; diagnosis owed #70.
The lesson has a shape we know: **a mechanism that exists but is not usable at the moment of need
is absent** — instrument-without-a-consumer, in the review layer.

## Rulings this session (Dave)

- D1 axis/grid colours APPROVED · D2 header layout APPROVED (screenshots, #69)
- D3 legend lockup NEEDS REWORK (detail owed)
- D4 swatch-during-isolate → discuss next session (DV-D17 stands meanwhile)
- D5 queue AGREED: fit hooks + four-theme wiring + **Mono/Console switcher in review controls**
  (asked twice this session — do not lose it a third time)
- Memento plugin disabled for projects (Arc 1) · three lockup titles (Arc 2) · ds-026 floated

## Open at close

D3 rework detail · D4 discussion · review-export defect diagnosis · fit-hook adoption (the D5 line)
· colleague's Copilot verdict · CI glance at next push · radius tuner · render-30 + a11y-8 triage.
