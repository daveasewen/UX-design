# #142 — the binds authoring wave: a false premise, a blocked spine, and a ruling by class

provenance: 142 · 2026-08-10
status: observed

*Spine entry: `_LIVE-STATE.md` ⏱ LATEST DELTA (#142) · `GOOD-MORNING.md` ★ LATEST (#142).
Ledger: `knowledge/_rulings.json` § `s142-D1`. Verbatim record: `reviews/BINDS-AUTHORING-VERDICTS-2026-08-10-s142-v3.json`.
Judgment basis: `notes/_briefs/2026-08-10-142-binds-judgment.md` (PROPOSED, basis-tagged). Survey: `notes/_briefs/2026-08-10-142-binds-survey.md`.
Both-way links per `_DECISION-HISTORY/README.md`.*

---

## Why this session existed

#141 ended with a re-priced residual: axis A (`props[].binds`) is an AUTHORING job, not a migration — only 12 of 109 visual props had a recoverable spine address, and the s141 binds draft left 120 `$doubt` rows OPEN. #142 opened on that residual ① with what looked like a clear plan: put "Dave's pick-lists (exist for 62)" in front of him and author the rest.

## The premise died first

The survey sub measured the inherited claim instead of building on it: the "62 pick-lists" were **not Dave's** — they are the s141 draft's own `spineCandidates` arrays (survey brief lines 8–9; a grep for pick-list files across `knowledge/` and `notes/` matched only index/receipt files). Dave never authored a pick-list. [[premise-ages-faster-than-rule]]: what #141's residual called "Dave's pick-lists exist for 62" was the draft describing itself.

## The blocked half

Second finding: **56 of the doubt rows' plausible targets live in the colour spine — and the colour spine was never DTCG-migrated.** `b4f59e6` (the s141-D1 enactment) touched 5 spine files; colour is not among them (survey brief, per class: "colour spine not in the 5 migrated files"). A row-by-row authoring wave would have manufactured 56 addresses into a spine that has no addresses.

## The turn: judgment by class

Dave's constraint, in-session: the row grid was too much to review. The conductor authored `notes/_briefs/2026-08-10-142-binds-judgment.md` — PROPOSED, basis-tagged (CANON / PROSE / JUDGMENT), classifying the rows into 12 mechanism classes (state enums are selectors, size enums are ramp maps, variant is the variants-axis, and so on), on the frame that Apollo is "lovable on rails": one class mechanism, uniformly enforced and gateable, is more robust than 64 row picks. The colour-spine block is handled inside the classes rather than deferred wholesale: C4 (rag) and surface record ADDRESS-INTENT now, values land when the colour spine migrates; C1 routes per-state maps to the colour lane.

Load arithmetic: 12 ratifications + 7 confirm-sets + 3 row-picks instead of 64 row picks.

## The controller arc, and a laundering catch

v1 (row grid) → v2 (recommendation-first) → v3 (class cards). At v2 the conductor found 10 "recommendations" that were premises wearing verdict clothes and DEMOTED them ([[feedback-dont-launder-a-premise-into-a-ruling]]); the measurable residue: v2 carries 69 "recommend" tokens, v3 carries 3. v3 was render-proven (3 PNGs — light-1180 / dark-1180 / light-480 — in `reviews/`); the 20/20 drive count is the conductor's report with no repo receipt, recorded as such. The s141 and s140 review HTMLs remain UNPROVEN: the render-proof debt stays ×2, not ×3.

## The projection error, corrected by addition

The judgment file projected "57 of 64 + 7 C12". The v3 build sub measured **61 of 64 class-mapped + 3 unmapped surface rows** — C6/C11 members and the 7 C12 sets are draft-roster rows outside the 64. The error is reconciled IN the judgment file ("Reconciliation note (v3 build, measured): my '57 of 64 + 7 C12' projection was WRONG …") — declared, not smoothed; the v3 page is the authority on membership, the judgment file on verdicts. The first judgment pass also missed the 3 `surface` rows and `tooltip.tip`: surface was ruled by row-pick (`background.default` ×3 — C13's class judgment NOT ruled), tip was left OPEN.

## The ruling

`s142-D1` (Dave, via the v3 live controller + verbatim export in chat + read-back): **12 class verdicts + 3 surface row-picks + 7 C12 confirm-sets = 113 of 114 rows; `tooltip.tip` stays OPEN.** Recorded in `knowledge/_rulings.json` by textual insertion, 101 priors asserted parse-equal pre- and post-write. The verdicts JSON's own `$reconcile` key carries the measured mapping; `rows: 114 · rows_ruled: 113 · classes: 12 · controller_rows: 64` are its own keys, re-read at the wrap.

## What is deliberately still open

`tooltip.tip` (the one unruled row) · the intent-map and capability vocabularies (C5 made the intent-map keys NON-BLOCKING for binds — the vocab ruling is still owed) · the enactment (binds into metas — #143) · the colour-spine DTCG migration (#143 candidate; unblocks the C1 per-state maps, the C4/surface values, and the 56 blocked rows) · the step-0 confirm-retire, the 42 fork verdicts, the 15 token-split exceptions — all Dave's.

---

Resolved state: `s142-D1` RULED, NOT ENACTED. The next hop is mechanical enactment plus the colour-spine migration; the judgment method (class-based, basis-tagged, controller-ratified) is reusable for the next authoring wave.
