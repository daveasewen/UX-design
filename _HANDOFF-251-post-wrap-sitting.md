# HANDOFF #251 → #252 — THE POST-WRAP SITTING (2026-09-07)

> ⚠ **This file is NEWER than `_CHAIN.md` and OUTRANKS it.** Read it, then the chain. It will trip
> R3 CHAIN OVERTAKEN at the #252 wrap — expected; fix at the cause (roll + `_gen_chain.py` first),
> never a hatch. Delete this file in the #252 wrap once its content is in the ⏱ delta and `_CARRIES.md`.

**Why it exists:** the #251 wrap ran (`edb0ded` / `f13a330`, pushed) and THEN Dave sat on the two #249 review pages and made FOURTEEN decisions. They are inscribed (`s251-D3` … `s251-D15`, store 373 → 386, plus an evidence amend on `s249-D1`: "fuzzy edges", not "fussy") but they are NOT in the ⏱ delta, `_CARRIES.md`, the GM banner or the memento index. #252's wrap owes all four.

## The decisions (ids are the record; this is the map)
- **DESK, six structures adopted** `s251-D3` · ladder not float, `priority` the only number `s251-D4` · `when` = predicate `s251-D5` · derive reciprocals, `not-with` promoted `s251-D6` · band owns 1100 + wiggle-room caveat `s251-D7` · do it properly, meeting-targeted `s251-D8`.
- **META:** library fact, tags in metas `s251-D9` · 12 canonical (closes #248 collision) `s251-D10` · q3 closed by D4, `answers` extends chart-intents `s251-D11` · first pass = 11 + 14 charts `s251-D12` · rule 6b → two named alternatives, C9 reads them `s251-D14`.
- **Timing:** GOAL = **Monday 2026-09-21**, prep INCLUDED `s251-D13` (supersedes ≈09-19 from #246).
- **Working shape (ruled):** Dave VETOES, Claude AUTHORS — one page per batch, strike what's wrong `s251-D15`. `k` default 1 in the skill = working shape, no id.
- **NOT ruled:** the roles list (`provides`) — a REVIEW of a proposed 8–12 slugs, Claude drafts first. The `when` predicate's field list. Rails for non-factory modes (D7 caveat).

## The plan Dave said yes to (order)
1. Roles list: Claude drafts 8–12 `provides` slugs with the components each covers → one review page → Dave strikes/renames.
2. 25-meta authoring pass (`s251-D12`) + the validator (META's "one script") — Opus lanes, premise probe first (rule 7), one review page per batch.
3. Rule 6b as the worked example (`s251-D14`): reflow / compress with `when`, C9 reads them, the 3 reds retire.
4. Meeting prep, inside the window.
5. Riding along only if cheap: DP-20 26/9 carriers · the unguarded strip→card link (verifier NEW 21) · `--mutate` bites only `dv-behaviour` (NEW 22) · no gate for a ragged row (NEW 24).

## Uncommitted at this handoff
`knowledge/_rulings.json` (13 inscriptions + 1 amend) · this file · `notes/_REHEARSAL-LOG.jsonl` (gate append). Commit + push follows immediately.

**Next title stands:** `Apollo - #252: DP-20 carriers + the unguarded link` — but the OPENER should re-title to the roles review if Dave agrees; the label is his.
