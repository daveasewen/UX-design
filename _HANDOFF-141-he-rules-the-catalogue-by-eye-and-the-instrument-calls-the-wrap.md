# HANDOFF #141 — #290 → #291 — HE RULED THE CATALOGUE BY EYE FOUR TIMES AND TOOK IT ONTO THE INVENTORY PAGE, AND THEN THE SEAM CHECK CALLED THE WRAP

provenance: 290 · 2026-09-20
status: observed

*Written by the delegated OPUS 5 wrap sub at the close of #290 (conductor Fable 5.1). Every figure
here was MEASURED at this seat unless it says otherwise; where a measurement disagrees with the
brief's declaration, both readings are published and neither is rewritten.*

✅ **NO DATE SPLIT.** #290 is one day — Sunday 2026-09-20 — and `date` at this seat read the same.
Nothing was re-dated; the four DATE-SPLIT lines in `GOOD-MORNING.md`'s header belong to prior
sessions and stand verbatim.
⛔★★ **NO RULING WAS INSCRIBED. `knowledge/_rulings.json` STAYS AT 622** — verified here by
`json.load` over the `rulings` list, with **no `s290-` id present**, and the newest three still
reading `s283-D1` · `s287-D1` · `s287-D2`. **He did not say *"inscribe"* today.** Everything
ruling-shaped below is a **QUESTION PUT** (`s271-D4`), never a state of the world.

---

## ⛔ READ FIRST, IN THIS ORDER

1. **This file.** It is newer than `_CHAIN.md` and **OUTRANKS it**.
2. `_CHAIN.md` — the read contract (header → ★ LATEST banner → ⏱ LATEST delta). **9,676 tape.**
3. ⛔ **It does NOT replace `_HANDOFF-130`…`-140`.** Every open item on those eleven still stands
   **except ONE closed here with a receipt** — see § THE ONE STRIKE.
4. `notes/_lanes/290/DAVE-RULINGS-2026-09-20.md` — his nine sentences, verbatim, in order. Quote
   them; never paraphrase.
5. `_CARRIES.md` § `residual → #291` when you need the bodies — **605 probeable items**. Do not
   read it at boot; fetch the section (`_memento_search.py` → `--fetch carries:residual-291`).

---

## ⛔⛔ DAVE'S WORDS — VERBATIM, AND ALL NINE FIT ON ONE PAGE

> Good Morning!

**The four notes on the catalogue drawing, in order:**

> can you fix the catalog diagram first, one page is upside down, and it would be better is it
> wasnt flat maybe a 60 degree angle

> and the bookmark is placed accross a page it should come down the page from teh top

*(with two crops of the render)*

> place the text on the left, the pages seem to turn up in the middle, either this should be on
> the outer edges, or bent into the book at the middle

> and the book make should be in the middle

**Then he took it, and placed it:**

> like it, that should be on the inventory page

> and the parts image on, slide 10

**His last ask — NOT started, and it is your first job:**

> the line slide - 4. could I have two workers in overalls in the place of the robots that are on
> slide 05. so two drawings one with humans and one with robots

**And the wrap, on the seam reading being quoted to him:**

> okay, wrap

---

## WHAT LANDED — ONE COMMIT, `d96f08c7`

**`notes/_lanes/289/illustration/catalogue.html` — v2, with v1 preserved as `catalogue-v1.html`.**
The left page is **MIRRORED across the spine** rather than turned 180° — which is what the
upside-down page actually was, and the distinction matters: rotating a leaf flips its text as well
as its position. The book leans **60°** on a right-triangular wedge stand with its tail edge on the
plate. The page block is **two convex prisms sharing the plane `x = Lg`** so the leaves bend
**INTO** the gutter (`CURL 5.6`, read from the file here), with the head/tail leaf edges following
the dip. Captions **left-aligned on both pages**. The bookmark lies in the gutter astride the
spine, from the head, hanging free off the tail onto the plate. `catalogue-rest.png` and
`catalogue-orbit.png` re-rendered beside it (680×500 @2×, Chromium in sandbox, zero page errors).
⚠ **A dashed hidden outline of the stand was TRIED AND REMOVED — it fought the grid.** Declared,
because a reverted experiment nobody names is indistinguishable from one never tried.

**`notes/_DEMO-SLIDES-apollo-2026-09-20-v11.html` — from v10, which is untouched.** Measured here:
**both files carry TWELVE numbered slides `s1`…`s12` inside THIRTEEN `section.slide` blocks** — the
same shape #289 published. Three changes, and only three: s6's canvas `#bk` → `#ct` (the catalogue
IIFE ported with the gearbox's three deck manners — draw-in-view, orbit spring, print snap to
`#ctPrint`); the books IIFE now draws into an off-screen `#bkHost` (fixed, left −3000px, the
`#amHost` shape) with `start()` fenced, so it **still bakes `#bkPrint` for slide 10's 02 Knowledge
and never animates**; and slide 10's `MAP` an1 reads `ctPrint`, so **01 Parts is the catalogue**,
alt text updated. Print CSS rows updated for `#s6 #ctPrint`.

⛔ **WHAT v11 IS NOT.** The four layout flags lane D2 declared at #289 are **untouched** — s4–s8
are still five white cards in a row, the callipers still fill half their card, the gearbox scale
bar still rides into the s10 Parts cell, the brain frame is still a hair heavier than the
gearbox's — and **the robots slide is not back on dark**. All four are gated on **his
slide-by-slide read, which he has still not given.**

---

## ⛔ ZERO LANES — AND THE DEPARTURE IS DECLARED, NOT SMOOTHED AWAY

**No sub ran this session.** The conductor made three edit passes and five renders himself, at his
own seat, against `s204-D1` as restated at #284. His stated reason, carried verbatim rather than
paraphrased into a justification: **each of Dave's notes was a one-constant geometry change with
him waiting on the render, and the whole exchange cost less than one lane brief.**

⇒ **Whether a *"one-constant edit with Dave waiting"* exception to the delegation rule exists is
ruling-shaped and is HIS.** Until he says so the rule stands as written and #290 is a **recorded
exception, not a precedent**.

⇒ **No `subs` line exists in the #290 gauge-log block, and the absence is declared rather than
defaulted**: absent is legal, there is no zero-line and no n/a line, and an UNKNOWN is never turned
into a number.

---

## ★★★ THE SEAM CHECK FIRED AND WAS OBEYED — THE SECOND TIME ON RECORD

Before the workers lane — a fully specified job — would have been cut, the seam read:

> **FILL 228,094 real / 48 turns · boot 74,165 · ⛔ OUTSIDE TOLERANCE 220,000 — no more lanes,
> wrap · DISK /sessions 9.9%**

The reading was **quoted to Dave in chat** rather than acted on quietly, and his entire answer was
***"okay, wrap"***.

**#283 was the first session to stop on its own instrument. #290 is the second.** Two is evidence
that the instrument is trusted when it is inconvenient, not a rule that it must be obeyed.
⛔ **Whether `s283-D1`'s tolerance arm becomes BLOCKING rather than advisory is ruling-shaped and
remains his.**

---

## ⚙ THE NUMBERS, MEASURED AT THIS SEAT

- **FILL 241,485 real / 53 turns**, peak the same, **boot 74,165**, 0 compaction records, 0 drops
  — `_checkin.read_fill` imported (one definition of the FILL rule, not two) against the
  conductor's transcript `abb53da9-b09a-48e6-820b-6b0bd8854457.jsonl`.
- **DECLARED at the brief cut: 228,094 / 48.** ⇒ **delta 13,391 over five turns — the hand-over's
  own cost, not a disagreement, and both readings stand.**
- ✅ **The declared and measured BOOT agree to the token (74,165)**, which is the `s214-D5`
  precondition that makes that subtraction legal at all.
- ⛔ **21,485 past the 220,000 tolerance · 61,485 past the 180,000 quality line — DECLARED.**
  ✅ **256,000 clear by 14,515.** The overrun is the wrap's price: the session stopped taking work
  at 228,094 exactly as the instrument asked.
- ★ **Boot 74,165 against #288's and #289's identical 74,174 — a FIFTH reading on the
  browser-blocked setup, and the three-session spread is NINE TOKENS.**
- **`_CHAIN.md` 9,676 tape** (slice 8,823 + wrapper 853), ratio 29% against GM's 33,676 —
  inside the generator's `<40%` floor, `--check` FRESH.

---

## ⛔ THE GATES — SEVEN IN, SEVEN OUT

**Seven blocking fails stood at the open, all inherited, carried in the `#243` DECLARED
not-a-wrap form — the SIXTEENTH consecutive wrap.** The boot-drift CEILING BREACH plus six boot
double-counts (#243 ×5, #264, #272, #273, #274, #287). **A wrap may not repair an inherited gate
fail** and this one did not try.

✅★★ **NO EIGHTH WAS BORN BY THE MANDATED 2f ROLL, AND #289'S PREDICTION WAS TESTED RATHER THAN
TRUSTED.** `_parse_boot_samples` run over the exact text the roll would append returned **ONE row
for #289, not three**, and the post-roll check still reads **six**. ⚠ **A second reading fell out
of the same test: the CEILING BREACH list reads SEVEN both before and after the roll, because the
window SLID — #279 fell out as #289 came in.** The delta had been drafted saying the list would
grow to eight; **that draft was corrected through the mover before publication**, and the wrong
draft is named here rather than smoothed away.

---

## THE ONE STRIKE, AND THE ONE THAT WAS DELIBERATELY NOT STRUCK

**STRUCK:** #290's carry ② — *"THE CATALOGUE PAGE VARIANT IS OWED AND IS #290'S FIRST JOB"*. He
took the **open book** for the slot the flat-page variant was owed for. Receipt: commit
`d96f08c7`, v11's slide 6 and slide 10's Parts cell, and his own *"like it, that should be on the
inventory page"*. `s183-D1` strike form, `s188-D2` receipt.

**NOT STRUCK, on purpose:** #290's carry ④ — *"DECK v11 IS OWED AND THE FOUR D2 FLAGS ARE NOT
ACTED ON"*. v11 exists but is only the catalogue swap; the flags are untouched and his read has
not been given. The headline is **half** true, and `s183-D1` strikes a headline and never a
clause. What changed was minted as a **new item** instead. *A strike that is wrong is worse than
an item that is merely stale.*

---

## ⬛ OWED TO #291, IN ORDER

1. **Two workers in overalls on the line** — a second line scene from
   `notes/_lanes/289/illustration/line.html`, same idiom and maths, the two robot arms replaced by
   two human figures in overalls; **slide 4 takes the humans, slide 5 keeps the robots**. One Opus
   lane, rest/orbit renders, a filed report with `RULING-SHAPED QUESTIONS` / `COUNTS:` /
   `REPLAY-THESE:`; then a port to v12. **His last ask.**
2. **His slide-by-slide read of v11** — it gates the four D2 layout flags and the robots slide.
3. **Proposal v3** — the premise as beat zero, both framings side by side.
4. **The week's plan from the strand map** — Monday's overview-dashboard definition and first cold
   one-shot, Thursday's rehearsal, the recorded fallback. **Friday the 25th is the internal, and
   *"still very important"* — five days out.**
5. **`knowledge/_drive_chart_engine.py` re-drive** → clears the release CI red. **THREE sessions
   owed.**
6. **Index sharding by strand + age** — his pick, not started; the memory archive is still at
   **162 B of headroom** and the newest-three cut is suspended a fifth session.

---

## ⚠ THINGS A COLD SEAT SHOULD KNOW BEFORE IT TOUCHES ANYTHING

- **`rm` and `git checkout --` are BLOCKED on this mount.** Move strays with `mv` to
  `_to_delete/<n>-strays/`; a stale `.git/index.lock` is `mv`'d aside, never `rm`'d
  [[git-lock-mv-not-rm]]; restore a file with `git show HEAD:<path> > <path>`.
- **`git status` warned `unable to unlink .git/index.lock: Operation not permitted`** after the
  conductor's commit — the #287 mount defect, still live.
- **`pip install tiktoken --break-system-packages` FIRST** in a cold sandbox, and again after
  `_gate_scratch_hygiene.py --clean --wrap`, which removes `~/.local/lib`.
- **Chromium in the sandbox** per `chromium-in-sandbox-recipe` with `NODE_EXTRA_CA_CERTS` — worked
  first time, under two minutes.
- **Dream pass 13's seven proposals are still unruled**, and P1 (the B3 grader dead since the #278
  store move) printed its stale sidecar again at this session's boot.
- **`notes/_dream/_GRADE-DECISIONS.jsonl` was dirty before #290 started** and is committed with
  this wrap, named rather than swept in silently.

---

*Filed report: `notes/_subreports/2026-09-20-290-W-wrap.md`. Dossier:
`_DECISION-HISTORY/2026-09-20-290-he-rules-the-catalogue-by-eye-and-the-instrument-calls-the-wrap.md`.
Memory hook: `notes/_lanes/290/WRAP-MEMORY-HOOK.md`. His words:
`notes/_lanes/290/DAVE-RULINGS-2026-09-20.md`.*
