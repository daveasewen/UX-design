# HANDOFF → #87 · READ ONLY THIS FILE

⛔ **Do NOT read `_CHAIN.md` at boot — it is TWO SESSIONS STALE (says "YOU ARE #85").**
Neither #85 nor #86 wrapped; both were declared budget stops. This file is the whole contract.
⚠ Known defect of this handoff shape, measured #86: nothing on the boot path routes a cold
session here — the chain wins by default. #86 paid ~13K for that. Fixing the routing is part
of the rebuild job below.

---

## RATIFIED BY DAVE AT #86 — FIRM, his explicit confirm ("Yes, firm — record them")

Source of truth: `reviews/TRIAGE-BANKRUPTCY-2026-08-02-v1.html` (+ .REVIEW pair) — the
export came back through the review machinery, 19 of 19 ruled, zero comment pins.

**Accepted close conditions (16 governing items become standing records):**
G1 index cap 700 → closes when Dave ratifies 700 or names his own ·
G2 TAPE_TO_BILL n=2 → closes at 4 logged pairs, then Dave rules firm-or-retire ·
G3 stale-wording gate tier → closes when Dave rules warn vs block ·
G4 §C 191>150 → closes when Dave picks OFFLOAD/TRIM/KEEP ·
G5 advisory cap set → closes when re-measured in real (after G9) then Dave ratifies the set ·
G6 DEFER_STREAK + USAGE_HISTORY_BLOCKING → closes when Dave rules streak + per-candidate calls ·
G7 newest-end convention → closes when Dave names it (one sentence) ·
G8 dormant % band → closes when Dave says retire or pin ·
G9 ds-023 re-measurement programme → closes when Dave rules the programme ·
G10 "70%/95%" stray band → closes when Dave rules provenance or strikes it ·
G11 DS-018 recessive value → closes when Dave picks it ·
G12 Charter §4b temperature → closes when tov-016 lands or Dave ratifies ·
G14 icon-button dark bindings → closes when Dave rules SC dark ·
G15 DV-D13 centre figure → closes at Dave's sign-off eye ·
G16 DATAVIZ:567 enactment call → closes when Dave ratifies or reverses ·
G17 RAG manifestation → closes at Dave's canon pick (A / A+B / A+B+C).

**PILE 3 — kept OUT of the bankruptcy (his pick):** molecules #66 enacts · radius/corner
tuner ("return SOON") · #84-D1 successor instrument. Ruled-but-unenacted is a protected class.

**INERT — archive all as one block (his pick):** the ~22-item list in the review doc's
INERT section, verbatim. Items return only when named, each gets a `closes_when` on entry.
Flags that survive the archive: donut/chart-bar "awaiting eye" possibly superseded by #79
(verify before archiving those two) · three ledger question-batches archived with a
NOT-CLASSIFIED flag.

**G13 RESOLVED IN CHAT — Dave verbatim, new ruling, supersedes the bow-and-arrow ask:**
> "the crescent is only a mark for apollo, we use it when we need designs to be anonymous
> use the hsbc mark normally"
⇒ crescent = Apollo/anonymity mark (its masthead use is CORRECT, not a gap) · HSBC mark =
normal use. `_ICON-GAPS.md:8` closes on this ruling. **Still open:** `menu-search` combined
glyph (`_ICON-GAPS.md:9`) — joins the governing records, closes when Dave approves the glyph.

**Also #86's measurement, standing:** the #85 handoff's "118 markers" inventory was not
reproducible by any probe; real population ~40 items → 16 governing (+1 unverified) / ~22
inert. The ~15 governing prediction survived; the denominator did not.

---

## THE JOB AT #87, in order

1. **Inscribe** — ledger entry `notes/_MEMENTO-DECISIONS.md § ★ #86` from this file
   (rulings + Dave's G13 words verbatim) + per-pillar propagation (G11/G15/G16/G17 →
   `_DATAVIZ`/pillar ledgers; G13 → `_ICON-GAPS.md`).
2. **Execute the bankruptcy** — archive the INERT block (archive, don't delete; use the
   ritual's move machinery, never hand-rolled) · create the 16+1 governing records with
   their closes_when · pile 3 untouched.
3. **The owed wrap, for BOTH #85 and #86** — the chain currently certifies #85; two
   declared stops stacked. Do not pair a message from one run with a status from another;
   regenerate honestly for the session that runs the wrap.
4. **Then the rebuild store** (#85's ruling: structured state, generated numbers, handoff
   ON the boot path — now THREE measured defects, same root (a stale banner trusted):
   routing loses to the chain · typed counts lie · **`_git_commit.sh` silently rewrites the
   msgfile subject from the un-regenerated chain banner** — #86's first commit certified #84
   (caught, amended to `fd2a44c`; evidence in that commit's body).
5. ✅ **SEQUENCING RULED BY DAVE, #86, closing the chat — verbatim: *"I need memento fixed
   before apollo. so nail Mem and then we carry on."*** ⇒ Memento (bankruptcy execution +
   rebuild store) runs to STABLE before any Apollo product work resumes. This supersedes
   #85's "Apollo at #87". Do not offer Apollo at the opener; offer the Memento worklist.
6. ⬛ **FLOATED by Dave at #86 close, NOT yet ruled — verbatim: *"maybe we need to test mem
   before moving on."*** Proposed shape to put to him at the opener (a proposal, not his
   ruling): the rebuild ships with an ACCEPTANCE DRILL — a cold session boots against the
   new store and we measure (a) it reads the right contract first time (routing), (b) every
   count in that contract reproduces when probed (no typed numbers), (c) the wrap/commit
   certifies the session that actually ran (no stale banner). Pass thresholds are Dave's to
   set; N consecutive passing sessions before Apollo resumes. A rebuild that hasn't survived
   this drill is CLAIMED, not PROVEN.

## BUDGET

#86 closed at **192,720 real measured before this file was written (96% of 200,000),
conversation half only — boot half UNMEASURED, ds-025 stands.** Costs of record: ~13K on
the stale chain (routing defect) · the triage measurement · the review doc + pair.
Price this job at the opener FIRST: `python3 knowledge/_checkin.py --window 200000`.

## CLOSES WHEN

Ledger entry inscribed · inert block archived · 17 governing records standing with
closes_when · wrap run and chain certifying the right session. The store build is a
SEPARATE item and does not gate this one.
