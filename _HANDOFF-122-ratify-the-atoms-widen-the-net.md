# HANDOFF #122 — #271 → #272 — RATIFY THE ATOMS, WIDEN THE NET

> ⚠ **This file is NEWER than `_CHAIN.md` and OUTRANKS it.** Written by the #271 conductor (Fable) at
> FILL ≈195,000 real — past the 180,000 stop line, under the 256,000 hard line, on Dave's word
> *"no panic until we get to 256"* — BEFORE the delegated ritual. Every claim is a receipt.

## ⛔ READ FIRST, IN THIS ORDER
1. This file. 2. `_CHAIN.md`. 3. `notes/_REVIEW-when-harvest-whole-library-2026-09-14-v2.html` (the decision sheet). 4. `python3 knowledge/_parked.py --check`.
★ Stale-mount check: `git log --oneline -3` vs `ls -la --time-style=full-iso _CHAIN.md`.

## ⛔⛔ DAVE'S — VERBATIM. Source: `notes/_lanes/271/DAVE-RULINGS-2026-09-14.md` (every sentence, in order)
- Opener: *"Go / 1. ratify / 2. push / 3. okay"* → **`s270-D2`** (four edge types ratified, LANDED `23f7d16`) · push done · "okay" READ BACK, not treated as a ruling.
- Dream pass 12: *"1. 180 was decided because we use a sub for the wrap the 150 number was chosen before we started this strategy / 2. yep like it / 3. none of these dates are decided yet, the first will be next week or the week after, teh second could be any time after but assume at least a week, three at most, but this a guess for date 2 / 4. Im not sure I understand this"* → **`s271-D1`** (ONE stop line 180,000; 150,929 RETIRED) · **`s271-D2`** (FILL warn arm, ADVISORY; blocking unruled) · **`s271-D3`** (NO demo date decided; small demo w/c 09-21 or 09-28; Rice 1–3 weeks after, "a guess").
- P4 re-explained: *"A. re-check"* → **`s271-D4`** (open-list items = QUESTIONS PUT; final wrap step re-checks vs `_rulings.json`, strikes by addition).
- Mid-turn: *"can you redraft _REVIEW-when-harvest-whole-library-2026-09-14-v1.html so I can record my decision and notes, it's easier. can you add a side nav and filters for the reds and completed etc"* → v2 decision sheet BUILT.
- Wrap call: *"okay fix and then wrap, no panic until we get to 256"* → 10 reds fixed `0c658c3`; **the "256" clause is RULING-SHAPED and NOT inscribed — put to him at #272's opener** (does 180,000 become a working figure with 256,000 the only stop?).

## ⛔ NOT RULED — DO NOT RULE (the wrap inscribes nothing here)
The 69 harvest decisions (his, on the v2 sheet) · the 15 unanimous (bulk-ratify is his click) · s271-D2 / s271-D4 blocking tier · the 3 explorer node hues (`--c-role/intent/shape`, proposed) · the "256" clause · roles.json membership drift (108 vs 24, REPORTED by the landing, not fixed) · door v2 (handoff-121 § C) · Common Toolkit specs into `_INGESTION-QUEUE.md` · all handoff-121 § D remainder (door step-1 switch, `when`s onto metas, split `selection-controls`, multi-select, combobox, every #119 open, boot literal, double-counts).

## WHAT #271 DID — RECEIPTS (8 commits, ALL PUSHED, remote = `0c658c3`)
1. `23f7d16` **s270-D2 landed** — 26 metas, 115 edges (providesRole 24 · answersIntent 28 · hasDataShape 26 · yieldsTo 37); `meta.schema.json` enum + `role:/intent:/shape:` grammar; `_validate_kg.py` resolves against the three stores (no registry files); explorer +3 hue vars per theme; regen 935 nodes / 1292 relations; `_validate_kg.py` OK + selftest OK. Drift reported: 108 memberships vs 24 metas with `provides`.
2. `441a91e` gauge logs committed (the #270 seam red).
3. `a9832ec` **HARVEST** — 3 Opus lanes A/B/C (`notes/_lanes/271/harvest/`), 132 components, 485 verbatim quotes < 15 words, 13 systems; 69 non-unanimous = Dave's decision table · 15 unanimous · 31 no-source (declared guesses) · 11 `component:null` findings · 8 internal clashes (pie/donut ≤5 in roles.json vs 6 in metas; interactive Tag link forbidden by 3 systems; …). Verifier RED (22 over-cap quotes lane A · 1 FALSE Dave attribution "no access today" · 1 comma stem B-24 · 5 receipt mismatches) → repair lane → **GREEN by addition**, `VERIFY.md`. Method: `_RUNBOOK-external-claims.md`; `_recount.py` in the lane folder. Kind of criterion ≈ unanimous, the NUMBER almost never (nav breaks at 5 or 7; toast 4/5/10s; pie 5 or 6).
4. `f4ad826` **s271-D1 + s271-D2 enacted** — `_gauge_tokens.STOP_LINE_TK = 180_000`; `_checkin.py` reads it, `CHAIN_STOP_RE` = declared fallback + disagreement detector; runbook § s214-D4 paragraph by addition; `_capture_gate.fill_working_ceiling_check()` (`FILL_CEILING_BLOCKING=False`), 6 selftest arms, mutation-proven; live: 4 declared breaches, 0 silent.
5. `a16078c` **v2 DECISION SHEET** `notes/_REVIEW-when-harvest-whole-library-2026-09-14-v2.html` — 115 rows wired (69+15+31), decision segmented control + notes, side nav decided/total per role, filters status/most-contested/slice/role/search, localStorage `apollo-harvest-271-v2`, Export/Import JSON, Ratify-all-15, keyboard j/k/1-9/r/l/d/n, driven in chromium (persist across reload, filter counts), 0 dropped strings vs v1, light+dark, 1280 & 400 no overflow. Static v1 "Your word" boxes removed. **Export JSON = the input to #272's rulings.**
6. `cf00872` **s271-D3 enacted** — run-of-show header carries dates AS AT 2026-09-14 ("none decided"); memory hook + stub restated by addition.
7. `b8024de` **s271-D4 enacted** — runbook step 3 + 5b; `_capture_gate.hook_open_items_recheck()` (`HOOK_OPEN_RECHECK_BLOCKING=False`), 9 selftest arms, #267 fixture mutation-proven; first live run flagged the #270 hook's edge-types item (closed by s270-D2) → struck by addition in memory.
8. `0c658c3` **10 `_governs.py` evidence-pointer reds FIXED** — legal `chat #<n> — …` form; `_governs.py --selftest` all bites green. Rulings 469 (`s270-D2`, `s271-D1..D4`).

## #272 — FIRST MOVES
0. ⛔ Opener puts TWO words to Dave: the "256" clause (above) · has he worked the sheet? If yes: **Import his `harvest-decisions-2026-09-14.json`** (or the pasted JSON) → a lane cuts one ruling per decided row (his notes = `says`), by role, review page of the cut before inscribing; the 15 unanimous become the `when` predicates onto the metas (s253-D1 shape) via a generator, never by hand.
1. Then handoff-121's remaining moves: door v2 per § C; Common Toolkit specs → `_INGESTION-QUEUE.md`; roles.json drift page.
**Cautions:** `model: opus` every lane, verifier same wave, `_checkin.py` every ~10 turns, screenshot + text diff before presenting (a green verifier ≠ review — the harvest verifier missed nothing on sight, the repair lane fixed what it found), `.git/index.lock` + `tmp_obj_*` → `allow_cowork_file_delete` then `rm -f`, never `git stash`, never `gen_kg_edges.py` (it was NOT needed — merge_edges carried the new types), `tiktoken` first.

## STRUCTURAL REDS AT THE SEAM (wrap heals or declares)
Memento index stale (today's files) → 2g · boot 80,474 = SEVENTH breach, shrink-only, his · #243/#264 double-counts · MEMORY.md over cap (stub rule) · dream pass 12 CLOSED (all four ruled) — say so in the dream register · `_GRADE-DECISIONS.jsonl` / `_REHEARSAL-LOG.jsonl` may be dirty again (instrument-written).
**Gauge:** boot 80,474 real · FILL ≈195,000 real at handoff (stop 180,000 PASSED, declared on his word; hard 256,000) · subs: harvest A 282,401 · B 194,306 · C 186,415 · verifier 95,718 · page 135,440 · repair 155,287 · v2 sheet 167,576 · D1/D2 160,033 · D4 139,728 = 1,516,904 (n=9).
