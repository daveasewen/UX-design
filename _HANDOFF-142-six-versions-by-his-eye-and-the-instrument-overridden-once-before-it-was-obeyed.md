# HANDOFF #142 — #291 → #292 — HE RULED ONE DRAWING BY EYE SIX TIMES IN A DAY, AND THE INSTRUMENT WAS OVERRIDDEN ONCE BEFORE IT WAS OBEYED

provenance: 291 · 2026-09-21
status: observed

*Written by the delegated OPUS 5 wrap sub at the close of #291 (conductor Fable 5.1). Every figure
here was MEASURED at this seat unless it says otherwise; where a measurement disagrees with the
brief's declaration, both readings are published and neither is rewritten.*

⛔ **DATE SPLIT, THE `#241` SHAPE BY ADDITION.** The session opened Sunday **2026-09-20** in the
morning; all nine lanes and **both** commits landed that day. **The push and this entire ritual are
Monday 2026-09-21** — `date` at this seat read `Mon Sep 21 08:29:29 BST 2026`. **Nothing was
re-dated:** the nine lane reports keep their `2026-09-20-291-*` stems, his rulings file keeps its
day, the deck keeps `-2026-09-20-v12`, and this wrap's own stamps carry today so the gate's
`is not today` check grades a true statement. A **fifth** DATE-SPLIT line stands in
`GOOD-MORNING.md`'s header, added by ADDITION; the four above it were not touched.

⛔★★ **NO RULING WAS INSCRIBED. `knowledge/_rulings.json` STAYS AT 622** — verified here by
`json.load` over the `rulings` list, with **no `s291-` id present**, the newest three still reading
`s283-D1` · `s287-D1` · `s287-D2`. **He did not say *"inscribe"* today.** Everything ruling-shaped
below is a **QUESTION PUT** (`s271-D4`), never a state of the world.

---

## ⛔ READ FIRST, IN THIS ORDER

1. **This file.** It is newer than `_CHAIN.md` and **OUTRANKS it**.
2. `_CHAIN.md` — the read contract (header → ★ LATEST banner → ⏱ LATEST delta). **10,792 tape.**
3. ⛔ **It does NOT replace `_HANDOFF-130`…`-141`.** Every open item on those twelve still stands
   **except ONE closed here with a receipt** — see § THE ONE STRIKE.
4. `notes/_lanes/291/DAVE-RULINGS-2026-09-20.md` — his eleven entries, verbatim, in order, with
   what was done on each. Quote them; never paraphrase.
5. `_CARRIES.md` § `residual → #292` when you need the bodies — **611 probeable items**. Do not
   read it at boot; fetch the section (`_memento_search.py` → `--fetch carries:residual-292`).

---

## ⛔⛔ DAVE'S WORDS — ELEVEN ENTRIES, AND SIX OF THEM ARE ONE DRAWING

> Good Morning!

> go

**The six notes on the workers, in order — each one an Opus lane, each answered with a render
while he waited:**

> the workers look like robots, here are some images, one is a sketch of a man in overalls, one is
> a drawing of overalls the other two are human bodies drawn with a construction method

> okay we need to do some tidying up.
> we don't need the flat facet of the top of the head.
> reposition this body so its at the side of the conveyer
> this arm is coming out of its body
> the ankles could be columns rather than cubes
> lets remove the ball joints

> can we get the shoulders looking more natural, and rounded

> set the shoulders down the body a little
> the first body is clashing with the conveyor, he needs to step back a bit
> I don't think we need the flat facet on top of the shoulders

> the top oval facet can just be removed
> the 'corners on the torso that face us can be removed
> hide the box-hands behind the box

**Then he took it — with one word withheld:**

> okay almost, but lets just get it in the slide

**The deck, with a three-up screenshot attached:**

> lets just have these the three on slide 10

**And then, AFTER the seam check had read wrap:**

> lets make better use of the space here, the images could be bigger

**The wrap:**

> okay push and wrup

⚠ **His four reference images and his crops were READ and NOT FILED** — the #289 stock-image
precedent, his own *"I wouldn't copy this in any way"*. A later reader will find figures with no
source in the repo, and that is deliberate.

---

## WHAT LANDED — TWO COMMITS, `b9ab75b0` AND `b721144a`, BOTH ALREADY PUSHED

**`notes/_lanes/289/illustration/line-workers.html` — v6, with v1…v5 preserved beside it**
(`line-workers-v{1..5}.html` and their rest/orbit PNGs, verified present at this seat), plus
`line-workers-rest.png` / `line-workers-orbit.png`. **Six versions in one day is the record of his
eye at work**, and the constants are in each lane report: v5 `SH_LZ 88→76`, `WK_DA 126→190`; v6
silhouette-only torso prisms, `GRIP_X 72` / `GRIP_Z 194`, orbit wrist sliver 4.0 px. Reports
`notes/_subreports/2026-09-20-291-L-line-workers.md` and `-L2-` … `-L6-`.

**`notes/_DEMO-SLIDES-apollo-2026-09-20-v12.html` — from v11, which is untouched.** Measured here:
**both files carry TWELVE numbered slides `s1`…`s12` inside THIRTEEN `section.slide` blocks** — the
same shape #289 and #290 published. **Slide 4 carries `#lw` + `#lwPrint`** (the workers IIFE with
the three deck manners — draw-in-view, orbit spring, print snap); **slide 5 still carries the
robots `#ln`**; **slide 6 still carries the catalogue `#ct`**; and **the gearbox is displaced to an
off-screen `#gbHost`**. **Slide 10 is three cells** — headline *"Three things she is made of."*,
`MAP` = `an1→gbPrint` `an2→bkPrint` `an3→brPrint`, Tools and Process removed — and the three
drawings were then scaled to fill their cells, the image box going from `135px` to
`clamp(150px,25.5vh,240px)` with `object-fit:contain` kept. Reports `…-291-P-deck-v12.md`,
`…-P2-slide10-three-up.md`, `…-P3-slide10-bigger-images.md`; build scripts and screenshots under
`notes/_lanes/291/P*/`.

⛔ **WHAT v12 IS NOT.** The four D2 layout flags are **still untouched** — s4–s8 five white cards in
a row, the callipers filling half their card, the gearbox scale bar riding into the s10 Parts cell,
the brain frame a hair heavier than the gearbox's — **and the robots slide is still not back on
dark.** All five are gated on **his slide-by-slide read, which he has still not given**, now owed
across three decks.

---

## ⛔ NINE OPUS LANES, NONE IN SEAT — AND THAT IS HALF OF A MATCHED PAIR

**L · L2 · L3 · L4 · L5 · L6 · P · P2 · P3**, all `Agent` at depth 1, all Opus. **Measured here
with `_checkin.read_fill` over each lane's own sub-transcript: `subs 1,413,300 tokens (n=9)`** — L
194,835 · L2 205,181 · L3 213,701 · L4 170,098 · L5 161,330 · L6 164,290 · P 111,851 · P2 101,877 ·
P3 90,137 — against **≈1,420,000 DECLARED** in the brief from the harness usage lines. **Agreement
to within 0.5%, and both stand.** ⚠ **A sum of final-FILL readings is not the same object as a sum
of cumulative tokens billed** [[measure-dont-convert-units]].

★ **Why this matters beyond bookkeeping: #290 made exactly this class of edit IN SEAT** and
published that as a declared departure from `s204-D1`, with the conductor's reason carried
verbatim. **#291 met the same situation and delegated every pass.** ⇒ **Two priced runs of the same
shape now exist.** ⛔ **No wrap invented a rule from n=2** — whether a *"one-constant edit with Dave
waiting"* exception to the delegation rule exists is still ruling-shaped and still HIS.

---

## ★★★ THE INSTRUMENT FIRED, WAS OVERRIDDEN ONCE, AND THEN OBEYED

Before the first commit the seam read:

> **FILL 220,302 real / 42 turns · ⛔ OUTSIDE TOLERANCE 220,000 — wrap**

After the commit it read **245,652 / 66**. **Both were quoted to Dave.**

★ **He answered the first reading with ANOTHER NOTE** — the bigger images — **and the conductor took
it as "one more, not wrap yet" and said so in chat.** Lane P3 ran, and the wrap came on his next
words, *"okay push and wrup"*.

**#283 was the first session to stop on its own instrument. #290 was the second. #291 is the THIRD
time it fired and the FIRST time the human overrode it once before obeying.**

⛔ **That is evidence about the tolerance arm and NOT an argument for making it BLOCKING.** A
blocking arm would have refused a lane Dave asked for. **Whether `s283-D1`'s tolerance arm becomes
BLOCKING rather than advisory remains his.**

---

## ⚙ THE NUMBERS, MEASURED AT THIS SEAT

- **FILL 264,714 real / 76 turns**, peak the same, **boot 74,155**, 0 compaction records, 0 drops
  — `_checkin.read_fill` imported (one definition of the FILL rule, not two) against the
  conductor's transcript `274c73c8-babe-4fc9-a086-2a4da0fb33fb.jsonl`.
- **DECLARED at the brief cut: 245,652 / 66.** ⇒ **delta 19,062 over ten turns — the hand-over's
  own cost, not a disagreement, and both readings stand.**
- ✅ **The declared and measured BOOT agree to the token (74,155)**, which is the `s214-D5`
  precondition that makes that subtraction legal at all.
- ⚠ **AND THE WINDOW HAD TO BE IDENTIFIED RATHER THAN ACCEPTED.** The brief named
  `173634f1-…jsonl`; that file opens on the same *"Good Morning!"*, reads the same boot to the
  token, and stops after **16 turns at FILL 163,756** — it is a **PREFIX**, and the live
  continuation opens two minutes later. **Both readings are published; the subtraction is taken
  against the window carrying all 76 turns.**
- ⛔ **44,714 past the 220,000 tolerance · 64,714 past the 200,000 working ceiling · 84,714 past the
  180,000 quality line · and 8,714 PAST 256,000 — ALL FOUR DECLARED**, and no literal was moved.
  **The cause is nameable: the instrument said stop at 220,302 and one more lane was taken.**
- ★ **Boot 74,155 against #290's 74,165 — a SIXTH reading on the browser-blocked setup, and the
  last four spread NINETEEN TOKENS.**
- **`_CHAIN.md` 10,792 tape** (slice 9,939 + the generator's fixed 853 wrapper), ratio **30%**
  against GM's 35,404 — inside the generator's `<40%` floor, `--check` FRESH, and inside the
  `s214-D6` ~10–12K target.
- **The ★ LATEST banner closed at 1,179 / 10 against the 1,200 / 10 cap** — it was drafted OVER the
  cap and **shortened twice** to fit. ⛔ **The standing warning that the next wrap has no room in
  this idiom is NOT withdrawn.**

---

## ⛔ THE GATES — NINE IN, SEVEN OF THEM INHERITED

**SEVEN blocking fails stood at the open, all inherited, carried in the `#243` DECLARED
not-a-wrap form — the SEVENTEENTH consecutive wrap.** The boot-drift CEILING BREACH plus six boot
double-counts (#243 ×5, #264, #272, #273, #274, #287). **A wrap may not repair an inherited gate
fail** and this one did not try.

⚠ **The gate actually read NINE at the open, and the difference is named rather than smoothed:**
two were **this ritual's own** — `_LIVE-STATE.md` and `GOOD-MORNING.md` not yet carrying
2026-09-21 — and steps 1 and 2 closed them.

✅★★ **NO EIGHTH WAS BORN BY THE MANDATED 2f ROLL, AND THE PREDICTION WAS TESTED RATHER THAN
TRUSTED.** `_parse_boot_samples` run over the exact text the roll would append returned **ONE row
for #290, not three**, and the post-roll check still reads **seven**. ⚠ **And the same sliding-window
reading as #290 fell out again: the CEILING BREACH list reads SEVEN both before and after, because
#280 fell out of it as #290 came in.**

---

## ⛔⛔ THE MOUNT DEFECT WENT TOTAL — AND THE FIX IS AN ORDERING RULE

**Every git write this session stranded an un-unlinkable `.git/index.lock`**, so `_git_commit.sh`'s
per-path `git add` loop failed on its **second** path. **Three refusals were paid before one clean
run:** REUSED-MSGFILE (the T3 prefix on line 1, his own — #289's and #290's trap for a THIRD
session), the doc-row gate (eight unrowed reports), then the lock.

✅ **Fixed by `mcp__cowork__allow_cowork_file_delete` on `.git/index.lock`** — the per-session delete
grant that [[git-lock-mv-not-rm]] has named as the prevention since #287. After it, one clean run,
and `rm` works for the rest of the session.

⛔⛔ **CALL THE GRANT AT THE FIRST COMMIT, BEFORE ANY GIT WRITE.** It was called after the third
refusal, not before the first write — that is the whole lesson. **The grant is per-session and does
NOT survive into the next window.**

---

## THE ONE STRIKE, AND WHAT WAS DELIBERATELY NOT STRUCK

**STRUCK:** #291's carry ② — *"TWO WORKERS IN OVERALLS ON THE LINE — HIS LAST ASK, NOT STARTED, AND
#291'S FIRST JOB"*. Six versions were drawn by his eye and v6 is on deck v12's slide 4 with the
robots kept on slide 5. Receipt: commit `b9ab75b0`, `notes/_lanes/289/illustration/line-workers.html`
(v6), v12's slides 4 and 5, the six lane reports, and his own *"okay almost, but lets just get it in
the slide"*. `s183-D1` strike form, `s188-D2` receipt.

**NOT STRUCK, on purpose:** #291's carry ④ — *"DECK v11 EXISTS AND IS ONLY THE CATALOGUE SWAP — THE
FOUR D2 FLAGS AND HIS SLIDE-BY-SLIDE READ ARE UNTOUCHED"*. v12 exists, but the four flags are still
untouched and the read has still not been given, so the headline is **half** true, and `s183-D1`
strikes a headline and never a clause. **The brief named exactly one strike and no other, and this
wrap struck exactly one.** *A strike that is wrong is worse than an item that is merely stale.*

---

## ⬛ OWED TO #292, IN ORDER

1. **His slide-by-slide read of v12** — it gates the four D2 layout flags **and** the dark robots
   slide, and it is now owed across v10, v11 and v12.
2. **What the *"almost"* is.** He accepted v6 with one word withheld and nobody asked. **Ask; do
   not guess.**
3. **Parts on slide 10: the gearbox or the catalogue.** Two of his own instructions disagree about
   one cell — #290's sentence and #291's screenshot. His.
4. **Proposal v3** — the premise as beat zero, both framings side by side.
5. **The week's plan from the strand map** — ⛔ **Friday the 25th is the internal, FOUR days out at
   this wrap**, and Monday's overview-dashboard definition and first cold one-shot was the plan's
   Monday. **Today is Monday.**
6. **`_drive_chart_engine.py` re-drive** → clears the release CI red. **FOUR sessions owed.**
7. **Index sharding** (his pick, not started); the memory archive still at **162 B of headroom**,
   and the cut suspended a sixth session.
8. **The arm drawing's home, if any** — `amPrint` bakes into nothing now that slide 10 is three
   cells.

---

## ⚠ THINGS A COLD SEAT SHOULD KNOW BEFORE IT TOUCHES ANYTHING

- ⛔ **CALL `mcp__cowork__allow_cowork_file_delete` ON `.git/index.lock` AT THE FIRST COMMIT** — the
  grant is per-session. Without it, every `git add` after the first path fails. Otherwise the
  fallback stands: a stale lock is `mv`'d aside, never `rm`'d [[git-lock-mv-not-rm]]; restore a
  file with `git show HEAD:<path> > <path>`.
- ⛔ **`_git_commit.sh`: a FRESH msgfile with a FRESH name, and NO `after #N …` prefix on line 1 —
  the script adds it.** The REUSED-MSGFILE refusal has now been paid at #289, #290 and #291.
- ⛔ **`_CHAIN.md` goes STALE the moment store rows are added** (its OPEN WORK block is generated
  from the store) — **regenerate with `_gen_chain.py` before every commit that touches
  `_state.json`.**
- **`pip install tiktoken --break-system-packages` FIRST** in a cold sandbox, and **again after**
  `_gate_scratch_hygiene.py --clean --wrap`, which removes `~/.local/lib`. Second consecutive wrap
  to meet it.
- **Chromium in the sandbox** per `chromium-in-sandbox-recipe`: installed once by lane L and reused
  by eight lanes.
- **`knowledge/_graph-mark-observations.jsonl` and `notes/_REHEARSAL-LOG.jsonl` are machine
  appends** — committed with the work and named, never swept in silently.
- **Dream pass 13's seven proposals are still unruled**, and P1's stale B3 sidecar printed again at
  this session's boot.
- **CI on `b721144a`, read job by job and polled to completion:** ⛔ gates FAILURE (steps 5 and 6) ·
  ✅ render SUCCESS · ⛔ release FAILURE (step 12). **Every red is inherited; #289 and #290 read the
  identical three colours.**

---

*Filed report: `notes/_subreports/2026-09-21-291-W-wrap.md`. Dossier:
`_DECISION-HISTORY/2026-09-21-291-six-versions-by-his-eye-and-the-instrument-overridden-once-before-it-was-obeyed.md`.
Memory hook: `notes/_lanes/291/WRAP-MEMORY-HOOK.md`. His words:
`notes/_lanes/291/DAVE-RULINGS-2026-09-20.md`.*
