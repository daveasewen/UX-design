# #247 delegated wrap brief — rulings ALREADY inscribed; run the FULL wrap ritual

**Model: Fable. Conductor: Fable (#247), FILL 180,791 at the brief cut — past the 150,929 advisory (wall 200,000), recall probe band CLOSED (K6 missed: the plant read-back was truncated by the conductor's `tail -8`, 3/3 on facts seen — report it as a measurement artefact AND as the miss it is). This wrap is delegated whole.** Template for the ritual's shape: `notes/_briefs/2026-09-05-246-delegated-wrap-brief.md` § 4 and the runbook it names (`knowledge/_RUNBOOK-capture-ritual.md`). This brief carries only what is different today.

## 1. RULINGS — DONE by the conductor, do NOT re-inscribe. Read back only.
`knowledge/_rulings.json` 355 → **360**: `s247-D1` (29, carefully, test) · `D2` (W1 moved; arm B better) · `D3` (status tiles not hidden in the row — flexibility + design carry) · `D4` (row not grid; count flexible) · `D5` (520: grid ok for density). All `by` Dave, `date` 2026-09-05, verbatim in `says`. Verify the count and that each parses; quote the five ids in the delta.
NOT a ruling (Dave: *"its just my gut reaction, we need this test, it's just me expressing my frustration, the lack of vision is proving to be a rather big issue."*): the blind arm STAYS. Carry it as evidence under the existing **parts-with-addresses** carry.

## 2. WHAT #247 WAS (WHAT here; WHY in `_DECISION-HISTORY/2026-09-05-247-enact-29-carefully-and-test.md`, which you write)
- Opener: chain read (#246), probe planted `knowledge/_probe/session-247.json` (n=4), boot **70,822 real** (state ONCE, in the stratum), `origin/master` = `4dc12b6`, HEAD `04099a8` = ONE ahead, not pushed (Dave's GitHub Desktop).
- Dave's day, in order: *"do we need both?"* → surfaced the principles page → **"okay lets do 29, carefully, and test…"** (`s247-D1`) → **the test plan** `reviews/DP-TEST-PLAN-2026-09-05-v1.html` (W0…W8 schedule · P0 control + P1–P8 probes · three score instruments · DP-17 three-way design · conflict ledger · 4 plan RSQs + Q8–Q16 carried) → **"go w1"**.
- **W1 DENSITY, three lanes + one patch lane, all Fable:** brief `notes/_briefs/2026-09-05-247-W1-density-brief.md`. **D** (edit, 109,328 quota): `knowledge/snippets/Template-dashboard-bento.reference.html` GROUP 1 → six compact `kpi-tile` at `data-c="1"` (square 6/3+3/2+2+2/1), `_validate_snippets.py` 0, `_validate_composition.py` 0, first-chart-y 1440 light **675.7 → 516.5 (−159.2)** rendered; report `notes/_subreports/2026-09-05-247-W1-D-density-edit.md`. **R** (re-run P0, 265,442): `outputs/w1-density/` both arms; sighted **2/2/0/2 (Δ0)**, blind **1/1/0/2** (harness: Tabs.reference.html L80/L120 demo width → one column, 2719px page — NOT the edit); page 1316 → **1157px**; seeds 1 PASS, 2–6 FAIL (later waves); sheet met 8 · missed 18 · n/a 3; conflict ledger 2 rows; `notes/_dp-scores/w1.json`; `reviews/W1-DENSITY-2026-09-05-v1.html`; `notes/_subreports/2026-09-05-247-W1-R-rerun.md`. **J** (blind judge, 45,183): picked W1 unprompted; *"the six tiles are uniformly weighted so nothing leads"* = DP-07/DP-14 vs DP-06 — first real conflict-ledger entry, W2's; `outputs/w1-density/judge/verdict.md` + KEY. **Patch** (60,440): full-page re-captures with the 900px fold line (W1 sighted 1157 · blind 2719 · baseline 1316), embedded; the review page's RSQ list got per-question reveal accordions (DP-04/05 on our own page) and two CSS fixes (grid column collapse; padding shorthand) — a lane shipped a page it never rendered: same class as no-gate-parses-the-artefact.
- Subs total **480,393 quota** (n=4). Conductor FILL at wrap cut 180,791.
- Facts to carry: `kpi-tile.meta.json` has NO `useFor` field — nearest are `purpose`, `antiPatterns`, `edges.usedInContext/commonPattern`; its own antiPattern *"a KPI tile with no series is a Stat card"* is broken by the two status tiles · richness instrument is blind to density (Q1 → `s247-D2`; fold row for W2+ NOT ruled) · `_enactment-register.json` has no kpi-tile / bento row (probe done per #244).

## 3. STILL DAVE'S (carry): plan RSQ 1 wave order · RSQ 2 re-run cadence · RSQ 3 blind judge every wave · RSQ 4 out-of-group fixes · fold row in instrument A · status surface design (`s247-D3`) · Tabs demo width under DEMO CHROME (W1 R RSQ 4) · stale "only 6 and 3" comments L73–74/L837 (lane D RSQ) · Q8–Q16 (W4 waits on Q8–Q13, W5 on Q14–Q15) · DP-17 test = W6 · everything in #246's § 3 at its age.

## 4. NEXT: **W2 — Bento rhythm** (DP-14 15 16 18; spans dial trial `s246-D3`; the judge's "nothing leads" is its opening fact). Next title: `Apollo - #248: W2 rhythm — one tile leads`.

## 5. THE RITUAL, WHOLE — steps 1→5 per the runbook, `--wrap`, subject READ BACK from `git log`, both title lines in chat. No push. Declare every skip with its size. Memory: the CONDUCTOR writes it (`w1-density-landed-247`, NON-REPO) — declare, don't redo. Commit the W1 evidence under `outputs/w1-density/` if `.gitignore` allows; if ignored, say so with its size. ⛔ Do NOT commit `_tools/` (untracked at the opener, provenance unknown — name it as a stray), `knowledge/_screen-gate/dashboard.md`, or `.git/index.lock`. `.git/index.lock` blocking ⇒ STOP and report.
