# #237 — DELEGATED WRAP BRIEF (mechanical wrap; conductor FILL 204,725 real, PAST the 200K wall, inside the `s214-D1` band, probe 4/4 GREEN twice)

*Conductor: Fable, #237. Wrap sub: Opus. Run `knowledge/_RUNBOOK-capture-ritual.md` — READ IT FIRST, it is canon; this brief is the FACTS of the session and the DO-NOT-RULE fence, nothing more. Ritual step 3 (memory) is DONE by the conductor: `designers-brain-p0-rulings-237.md` + one MEMORY.md line — write no memory.*

## THE SESSION IN FACTS (every figure copied off an instrument or a file; probe before restating)

- **Title read back at the opener:** `Apollo - #237: p0 — the twelve rulings, then l2`. Dave picked P0; L2 did NOT run — CARRIED, not dropped.
- **Opener commit `6516187`** (`after #237 …`): the #236 post-wrap `_HANDOFF-237-post-wrap-link.md` was DIGESTED and DELETED per its own instruction (its one item, Dave's dialogdesign.dk link, became gap R1-1); the commit deadlock it declared REPRODUCED at this seat (`SESSION_N=237` still trips R3 CHAIN OVERTAKEN while the handoff file exists) and is minted **`W-355`** (mine, `closes_when` stated, home `knowledge/_git_commit.sh:347`). Probe store `knowledge/_probe/session-237.json` committed there (hashes only).
- **TEN RULINGS INSCRIBED through `_inscribe_ruling.py` — `s237-D1` … `s237-D10`, store 311 → 321, ids READ BACK from the file.** All Dave's, from his opener message (R1 Q1·Q2·Q3·Q5·Q6, R2 Q1·Q2·Q4·Q5·Q6) plus the grade names given mid-session: **REPLICATED · STUDIED · PRACTISED · DEBUNKED · OBLIGATION** (D1 carries the definitions — their ONE home). Probe: `python3 -c "import json;r=json.load(open('knowledge/_rulings.json'))['rulings'];print(len(r),[e['id'] for e in r if e['id'].startswith('s237')])"`.
- **Lane G (Opus, gap discharge):** brief `notes/_briefs/2026-09-02-237-G-gap-discharge-brief.md`; report `notes/_subreports/2026-09-02-237-G-gap-discharge.md`; assets `notes/_subreports/assets/2026-09-02-237-G-gap-discharge/` (8 JSON). Counts off the file: **gaps 7 · DISCHARGED 7 · STAYS-UNPROVEN 0 · fetches 20 (ok 15 / failed 5)**. R2-2 (the video) DROPPED by Dave. R2-3's residual (user/managed/desktop-app hook layers) was CLOSED BY DAVE'S OWN TERMINAL pasted in chat — recorded inside `s237-D8`'s `says`. Sub spend **202,206** (harness). Conductor READ its REPLAY-THESE.
- **Lane T (Opus, tensions schema):** brief `notes/_briefs/2026-09-02-237-T-tensions-schema-brief.md`; report `notes/_subreports/2026-09-02-237-T-tensions-schema.md`; assets `…/assets/2026-09-02-237-T-tensions-schema/` (incl. `open-tensions.json`, `_derive_sort.py`, two PNGs); review **`_REVIEW-tensions-schema-2026-09-02-v1.html`** at repo root (owner Dave; rendered light+dark at 1440/390, a 390 overflow was fixed at the cause — finding 12). Counts off the file: **tensions 30 · settled-by-obligation 6 · resolved-here 3 · open 21 · of open: questions 4 / default-and-declare 17 · pitfalls 8 · ruling-shaped 5 · UNPROVEN 6**. Headline: the four questions are all CONDITIONAL, so the standing grill grows by ZERO; the cost moved to the `Defaults used:` DECLARATION (21 lines into a field built for six). Finding 5: **"tension" is a CONFIRMED `s202`-class vocabulary collision** (43 hits; `s143-D1` downgrades TENSION → CROSS-REFERENCE). Sub spend **184,093**. Conductor READ findings 2 and 5 and the REPLAY-THESE.
- **Store rows to MINT at this wrap (forgotten-document class, next free numeric id after `W-355`):** the G brief · the G report · the T brief · the T report · the review HTML · this wrap brief. Use `_state.add()` with a stated `closes_when` each; the doc-row gate will refuse the commit otherwise.
- **Gauge (moments stated separately, never converted):** boot **76,915 real** (first turn, `message.usage`; ⛔ outside the `s208-D1` band — the ordinal is NOT claimed, no constant moved) · opener FILL **109,659 @ 4 turns** · lane-G seam FILL **163,186 @ 26 turns** — PAST the advisory 150,929 by 12,257, DECLARED IN CHAT · post-lane-T FILL **204,725 @ 38 turns** — PAST THE 200K WALL, DECLARED IN CHAT, judgment CLOSED there · probe PLANTED 12:00:32Z, QUIZZED **4/4 GREEN at the 163K seam and again at 204K** (`knowledge/_probe/session-237.json`) · **subs 386,299 tokens (n=2)** — QUOTA never FILL · effort band **L** · this wrap sub's own spend UNOBSERVABLE, DECLARED. ⚠ THE GAUGE LESSON FOR `notes/_GAUGE-LOG.md`: lane T was launched at ~185K and its return + REPLAY read landed the conductor in the band; a lane launched past the advisory returns INTO the band.
- **Push:** NOT authorised this session unless Dave says so in chat after this brief was written. Commit with `SESSION_N=237 bash knowledge/_git_commit.sh --wrap --reconciled <fresh msgfile>`; do NOT pass `--push`. Read the subject back from `git log -1 --format=%s` and quote it in your stub.

## RESIDUAL → #238 (all Dave's unless marked) — a POINTER LIST; bodies in `_CARRIES.md` § `## residual → #238`

1. **Tensions: edges or nodes** — rule BY EYE from the T review. Lane T's five ruling-shaped questions carry with it (who settles the 21 open · rename "tension" · declaration cap · typed ruling links).
2. **The first question (R2 Q3)** — Dave rewrote the three answers in his own words in chat; NOT inscribed; waits for his "firm". Quote them verbatim in the carry: *"I know what's wrong, or I have an Intuition for a solution, but I'm not sure what to build"* → intent · *"I'm pretty clear on what I what to build"* → grill · *"I won't really know what I want until I see it"* → pattern-first (routes to the grill with the preference noted until the retrieval set exists).
3. **Plan v2** — `_PLAN-designers-brain-2026-09-02-v2.html` (never edit v1): ten rulings marked, seven gaps discharged with receipts pointing at the G asset folder, R2-2 dropped, R2-3 closed by Dave's terminal. One Opus sub.
4. **L2** (and L1's three questions: ratchet date · composed-CSS route · screen-gate index membership) — rolled from #236 and #237.
5. **`W-355`** (mine) — the post-wrap handoff commit deadlock; fix priced, not made.
6. **Licence tiering of 13 untiered families before P2; the "gestalt" collision before any import; the render-runbook seventh-stratum correction (lane T finding 11: the stored `fonts.conf` hardcodes another seat's mount path).**
7. Every prior carry at its own age, bumped.

## DO NOT RULE

Nothing about tensions (edges/nodes, settlement, rename, cap, links) · nothing about the first-question wording · no new names · no plan v2 content · nothing on L2 or `W-355`'s fix · no constant, band, floor, stop line, roster or version moves · no `s237-D*` re-worded · no memory write · no push. If the ritual asks you for a judgment the brief does not give, write `UNPROVEN — conductor's` and carry it.

## PITFALLS (consequences replayed)

1. **A malformed T3 subject** (the #236 class: an em dash inside the leading bold clause). Keep the ★ LATEST banner's leading clause free of ` — ` before its first close; read the subject back and, if it is wrong, repair the BANNER and re-commit through the script — never `--amend`, never raw git.
2. **Counting the carries by hand.** Use `_capture_gate._carry_items` for the count; state the +N drift cause as prior wraps did.
3. **Index last.** Ritual step 2g rebuilds `_memento-index.json` LAST; `index_freshness_check` compares CONTENT.
4. **Forgetting the doc rows** — six new documents this session; the gate refuses an unrowed one.
5. **Declaring skips silently.** Every ritual step you cannot run is named WITH ITS SIZE in the `⚠ DECLARED SKIPS` line.

Return a STUB only: the wrap commit hash + subject read back from `git log`, the store count read back, the carry count from the gate, both title lines (this session's, and `TITLE THE NEXT CHAT →` for #238), and the DECLARED SKIPS line.
