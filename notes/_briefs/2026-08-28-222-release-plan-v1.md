# THE RELEASE PLAN — every loose end to the bake, in priority and dependency order

session `#222` · 2026-08-28 · drafted by an Opus sub, READ-MOSTLY lane
ruling this answers: **`s222-D3`(2)** — Dave: *"need to get this release fixed before we do any
thing else… hand me a plan to get all the loose ends fixed in priority and dependancy order"*
ground: HEAD `36754e2` plus the dirty working tree, every claim below re-driven at HEAD
⛔ **Nothing here is decided. Every decision is listed as yours, in the place it falls.**

⚠ **This plan was overtaken while it was being written.** Between my first reading of the tree and
my last, **route (a) was landed in the working tree by another hand** — `measure_tokens` in
`knowledge/_capture_gate.py` and both packed copies now carry the fallback tier, and the doc
demotion has been pasted in. I did not do it and it is not in a commit. Items 1, 2 and 3 below are
corrected to say what I actually measured at the end. **The delta gate is RED right now** — the
half of the dance that bumps the port constant has not happened yet.

---

## THE ONE PARAGRAPH

The v-next release is **built, uncommitted, and standing on a red gate**. The encoder data ships
inside the pack, a pure-Python exact encoder has been proven against real tiktoken across 69.4
million characters with zero divergent tokens, and — as of an hour ago — the wire that connects
them has been pasted into the working tree along with the doc demotion. What has **not** happened
is the second half of that move: the delta gate's port constant has not been bumped, so
`_validate_package_delta.py` reads **1 failure** as I write this, and nothing has been committed
or re-proven out of the box. That is the top of this list and it is nearly finished. Behind it sit
four more things that must be right before a zip is cut: a browser-gate defect I found this morning that has **silently dropped two gates out
of the ship list and moved your ruled roster of 55 to 56**; a version number that still says
`v1.0.1` in two places while `v1.0.1` is frozen history in `dist/`; a ratification check that is
keyed to your `#219` word forever and will therefore **wave a v-next bake straight through
without asking you**; and the manifest and gate probe, which must be regenerated at the commit
that actually lands. **What you personally must do: four decisions and one test.** The decisions
are the wiring route (item 1), the version number (item 6), your fresh word to ratify (item 7,
which today no machine will ask you for), and your eye on the release page before the bake (item
11). The test is the last item and it is the only real proof of any of this: your fifteen-minute
Copilot first-session on the new pack, on the machine that produced the original failure.

---

## THE SEQUENCE

Each item: **WHAT · WHY HERE · OWNER · PRICE · DONE LOOKS LIKE.**

### BAKE-BLOCKING — the bake cannot honestly happen until these are done

---

**1 · Confirm the wiring route that has already been taken — route (a)**

- **WHAT.** Three routes were priced. **(a)** add the fallback tier to `knowledge/_capture_gate.py`'s
  `measure_tokens`, re-port it verbatim into both packed copies, then bump the gate's
  `PORT_COMMIT_A` constant — this is literally the remedy the delta gate's own failure message
  asks for. **(b)** a new-name wrapper that rebinds `measure_tokens` inside the shim. **(c)** edit
  `_gen_chain.py` as a matched triple. The sub that built the engine **recommends (a)** and
  recommends against (b), because (b) makes the live function a different function from the
  audited one, which is the guarantee the gate exists to give.
- **STATE AT HEAD, MEASURED.** **(a) is already in the working tree.** All three copies of
  `measure_tokens` carry the new tier and the new `encoder_home_module()` accessor. It is
  uncommitted and I did not put it there.
- **WHY HERE.** Everything downstream depends on it, and it has now happened without a word from
  you on the record. That is the only reason it is still item 1: it wants your yes, or your stop,
  before it is committed.
- **OWNER. Dave** — confirm or reverse.
- **PRICE.** Your decision: **one minute.**
- **DONE LOOKS LIKE.** One word: (a) stands, or it comes out.

---

**2 · ⛔ Finish the dance — the delta gate is RED right now**

- **WHAT.** Route (a) is a **two-commit move**: land the source edit, then re-port and bump
  `PORT_COMMIT_A` in `knowledge/_validate_package_delta.py`. Only the first half has happened.
  `PORT_COMMIT_A` still reads `9dcf62d`, and driven at HEAD just now the gate says:
  *"SHIM PROVENANCE chain(a): 'measure_tokens' in knowledge/_capture_gate.py has CHANGED since the
  port commit 9dcf62d — 22 line(s) differ."* — **1 failure.**
- **WHY HERE.** Depends on 1. It blocks 4, 9 and every bake step. A red delta gate is the one gate
  in this suite whose whole subject is the surface that just changed, and it must be honestly
  green — not silenced — before the commit.
- **OWNER.** Claude sub (Opus — it moves a gate constant on a frozen surface).
- **PRICE.** ~8–12K tokens.
- **DONE LOOKS LIKE.** `_validate_package_delta.py` reads `0 failure(s)`, and nothing else in the
  suite moved from its baseline.

---

**3 · The doc demotion — `pip install tiktoken` becomes recommended, not required**

- **WHAT.** Three sentences in `apollo-spider/FIRST-SESSION.md` and
  `apollo-spider/build-designer-pack.sh`.
- **STATE AT HEAD, MEASURED. Already pasted in.** FIRST-SESSION.md now reads *"recommended rather
  than required"* at line 33 and *"Recommended, not required."* at line 37.
- **WHY HERE.** It had to come **after** the wire and never before — land the promise first and the
  pack lies to a designer in the one document they read first. That ordering was respected. What
  it now depends on is item 4: the promise is in the docs, so the proof is owed.
- **OWNER.** Whoever finishes item 2, same lane.
- **PRICE.** included in item 2.
- **DONE LOOKS LIKE.** `_gate_pack_docs.py --stage` still reports **45 findings, exit 0** — the
  same baseline, zero net new.

---

**4 · The final out-of-the-box proof, in the state that actually failed on your machine**

- **WHAT.** Rebuild the stage, make tiktoken unimportable with a venv, kill egress, and drive
  `_gen_chain.py` and `--check` end to end. The exact commands exist in the filed report.
- **WHY HERE.** Depends on 2, and it is now **owed harder than before**: the docs already promise
  the behaviour (item 3), so until this runs the pack makes a claim nothing has measured. Without
  it, "fixed" is a construction argument.
- **OWNER.** Same sub as 2.
- **PRICE.** included in item 2.
- **DONE LOOKS LIKE.** `✅ _CHAIN.md: … purepy cl100k_base (exact, equality-gated)`, exit 0, with
  no tiktoken anywhere and no network — and the same chain byte-identical to one built with real
  tiktoken.

---

**5 · ⛔ THE ONE NOBODY HAS SEEN YET: two browser gates have silently fallen out of the pack**

- **WHAT.** Driven at HEAD this morning: `_validate_state_contrast.py` and
  `_validate_descender_computed.py` both die with a **raw traceback at exit 1** when playwright
  *imports fine but its browser binaries are not installed*. `#221` lane C fixed the case where
  playwright cannot import; it did not cover this third state. The consequence is not cosmetic —
  the release generator's gate probe classifies by what actually happens when it runs, so on the
  working tree those two gates flipped from `NEEDS-DEP` to `REPO-BOUND`, and **REPO-BOUND gates
  do not ship**. Measured against the committed manifest: `_validate_hit_area.py` and
  `_validate_state_contrast.py` have **left the pack**, three files have joined it, and the gates
  group has moved **55 → 56 files**. `s219-D9` is your ruled *"the pack ships 55 gates"*.
- **WHY HERE.** It is the highest-consequence item after the wire and it is independent of it, so
  it can run in parallel — but it must land **before** the manifest is regenerated for the bake,
  because the manifest is what the bake reads. There is a second, wider consequence worth one
  sentence: as it stands, **which gates ship depends on what happens to be installed on the
  machine that runs the probe.** A release should not have that property.
- **OWNER.** Claude sub to fix the two gates; the roster number itself, if it still moves after
  the fix, is **Dave's** under `s219-D9`.
- **PRICE.** ~12–15K tokens for the fix, plus a re-probe. Your decision only if the count still
  moves.
- **DONE LOOKS LIKE.** Both gates emit `COULD-NOT-ASK:` and exit 77 in the browsers-missing state,
  the re-run probe puts them back at `NEEDS-DEP`, and the gates group reads a number you have
  ruled — 55, or a new one you say out loud.

---

**6 · The version bump — and it lives in FOUR places, not two**

- **WHAT.** `dist/` already holds `Apollo-Spider-v1.0.1.zip` and the frozen-release ledger names
  `v1.0.1` as the newest. A bake today would try to overwrite frozen history. The number must move
  in `apollo-spider/build-designer-pack.sh:38`, `knowledge/_release/_gen_pack_manifest.py:107`, the
  ledger row's `version`, and the ledger must be re-seeded in the same commit or the gate's
  laundering arm fires.
- **WHY HERE.** Independent of items 1–5, but bake-blocking. The `#220` report already flagged the
  two code homes as an ADR-0017 one-home violation; the ledger and the re-seed are the other two.
- **OWNER. Dave** names the number (release surface is yours). Conductor moves it.
- **PRICE.** Your decision: **one minute.** Enactment: ~6–8K tokens.
- **DONE LOOKS LIKE.** All four homes say the same new number and
  `_gate_frozen_release.py` passes on three arms.

---

**7 · ⛔ Your ratification is currently a rubber stamp — and this one is a real hole**

- **WHAT.** `build-designer-pack.sh --release` refuses unless the manifest says `RATIFIED`. The
  manifest's status is read from the store — but the generator keys it to **one hard-coded ruling
  id, `RATIFY_ID = "s219-D10"`**, which is your `#219` word for **v1.0.0**. So the manifest reads
  `RATIFIED` at any commit, for any version, forever. Measured: the working-tree manifest says
  `RATIFIED` today. **The discipline that says "release is Dave's word" is, right now, not
  enforced by anything for v-next.**
- **WHY HERE.** Bake-blocking as a matter of honesty rather than mechanics — the machine will let
  the bake through, which is exactly why a human has to catch it. Independent of every other item.
- **OWNER. Dave.** Two things: a decision on whether the ratify check should key to *the release
  being cut* rather than one frozen id, and — separately — your actual fresh word for this bake.
- **PRICE.** Your decision: **two minutes.** The mechanical fix: ~5K tokens.
- **DONE LOOKS LIKE.** `--release` refuses on a v-next manifest until a ruling naming *this* cut is
  in the store, and then your word puts one there.

---

**8 · Rule or park the delta gate's blind arm**

- **WHAT.** Arm 2 of `knowledge/_validate_package_delta.py` is supposed to prove the packed shim's
  ported function bodies have not drifted. Driven both ways: editing **both packed copies**
  identically leaves the gate **green**; editing the source fires it red. The arm points at
  `knowledge/_capture_gate.py` in both halves of its comparison, so it **never opens the shim's
  ported bodies at all**. What it actually enforces is useful — "the source has not drifted since
  the port commit" — but it is not what its own docstring claims. And the fix is not a one-line
  repoint: the shim's `measure_tokens` legitimately differs from the source by one declared
  optional import, so aiming the arm at the shim naively goes red on a correct pack.
- **WHY HERE.** Item 2 edits precisely the surface this arm is supposed to audit. You do not have
  to repair it before the bake — but it should be **explicitly parked with its reason** rather
  than passed over silently, because the next person who reads "0 failure(s)" will believe
  something the gate does not check.
- **OWNER. Dave** — repair, re-scope, or park.
- **PRICE.** Your decision: **one minute.** Repair, if you want it: ~20K tokens and it is design
  work with a ruling in it, not a patch.
- **DONE LOOKS LIKE.** A word: repair / re-scope / park — and if park, it is written down.

---

**9 · One clean commit, pushed, CI read back**

- **WHAT.** Reconcile the working tree path by path — it currently carries the s222-D2/D3 lane
  edits, two untracked subreports, two untracked briefs, and a manifest and gate probe already
  regenerated at `36754e2`. **Discard nothing blindly, but treat those two regenerated release
  files as throwaway**: they must be generated again at the commit that actually lands (item 10).
  Then commit, push, read CI back in chat (`s203-D1`).
- **WHY HERE.** Everything above must be in it, and everything below reads a named commit. The
  release generator refuses to work from a working tree by design.
- **OWNER.** Conductor.
- **PRICE.** ~15K tokens.
- **DONE LOOKS LIKE.** A clean tree, a named sha, and the CI verdict relayed to you in chat. Expect
  the `gates` job still red on your **three standing decisions** (the pre-flight stamp, the
  two-wraps case, the third) — those predate this wave and are not this release's business. Expect
  the **baked-pack audit still red**; it clears at the bake and not before, which is a designed
  red, recorded at `#219` and again at `#220`.

---

**10 · Regenerate the gate probe and the manifest at the landing sha, and read the closure block**

- **WHAT.** `_gen_pack_manifest.py --probe --commit <sha>` then `--manifest --commit <sha>`.
- **WHY HERE.** Hard dependency on 9 — both modes read a named commit through `git archive` and
  cannot see a working tree. **Both** encoder subreports declare this as their one COULD-NOT-RUN:
  the new files have never been through `import_closure`, `companion_closure` or the seed-map
  collision refusal. Until this runs, "the pack will carry these files" is an argument, not a
  measurement.
- **OWNER.** Conductor.
- **PRICE.** ~8K tokens.
- **DONE LOOKS LIKE.** The closure block read and quoted, the gates group at the ruled count from
  item 5, and `_gate_release_audit.py --manifest-check` and `--drift` both PASS.

---

**11 · Dry-run twice, your eye on the page, your word, then the bake**

- **WHAT.** `--dry-run` into a throwaway directory, twice, proving the two zips are byte-identical;
  the release page regenerates from the manifest; you read it; you say the word; `--release` cuts
  into `dist/`; the frozen ledger is re-seeded; commit; push; CI read back.
- **WHY HERE.** The end of the chain. Depends on everything above.
- **OWNER.** Conductor drives; **the word is Dave's** and the page is for your eye.
- **PRICE.** ~25–30K tokens. **Your time: ten to fifteen minutes on the page.**
- **DONE LOOKS LIKE.** A new zip in `dist/`, `--check` matching it against the manifest, the
  baked-pack audit finally **green** in CI, and the ledger row naming the new version.

---

### POST-BAKE — the only real proof

**12 · Your fifteen-minute Copilot first-session, on the NEW pack, on the machine that failed**

- **WHAT.** Unzip the new pack on your own machine, follow `FIRST-SESSION.md` as written, and get
  to Step 4c.
- **WHY HERE.** It cannot happen before the bake. Everything above was driven in a Linux sandbox
  with a venv standing in for "never installed" and a dead proxy standing in for a corporate
  network. That is a faithful model. **It is not your laptop, and it is not VS Code with Copilot.**
  Both encoder subreports declare this as an UNPROVEN in their own words.
- **OWNER. Dave.**
- **PRICE. Fifteen minutes.**
- **DONE LOOKS LIKE.** The chain inscribes, the engine names itself in the output, and the next
  morning Copilot greets you from the chain you wrote — the thing that did not happen last time.

---

### PARKED BEHIND THE RELEASE — by your own sequencing word

`s222-D3`(2): *"the release is fixed before anything else."* These are live, priced, and **not
being worked** until item 11 lands. Listed so nothing is lost, not to reopen them.

- The `#217` bento sitting surface, the mono dark-caption chord left expressly open at `s222-D1`,
  and the standing mutation-arm call.
- **W-198** — whether the pack-import gate is promoted to blocking in this repo's CI. (Its other
  half, whether it ships in the pack, is answered in fact: it is fenced out and does not.)
- **W-206 Q2** — `showroom/_foundations/photography.html` ships with 442 broken images. The `#220`
  proposal was to drop the page. Cosmetic for the bake, real for a designer's first look.
- The `DOOR_COMPANIONS` declaration for the shim→helper runtime dependency — today nothing static
  can see it, so a future cut could drop the helper silently.
- Re-running the wide equality gate on a newer Python, to narrow the declared Unicode-drift
  boundary. Advisory.

**Already settled, do not re-litigate:** the photography licence is **ruled** — `s220-D3`, your
words *"images are in licence, all good"*, scoped to the 30 web derivatives. All four of L4's
first-hour blockers (the tiktoken lie, the dead retrieval signpost, the discarded gate population,
the browser gates' illegal refusal) were **repaired at cause in `#221` lane C** and are sitting in
the repo unbaked — item 11 is what finally puts them in a designer's hands.

---

## WHERE THE ORDER IS FORCED

Only three chains are truly serial. Everything else can move.

1. **1 → 2 → 3 → 4.** Rule the route, land the wire, then change the docs, then prove it. Land the
   docs before the wire and the pack promises something it cannot do.
2. **5, 6, 7, 8 are independent of that chain and of each other** — they can run in parallel with
   it — but **all of them must be finished before 9**, because 9 is the commit the bake reads.
3. **9 → 10 → 11 → 12.** Absolutely serial. The generator reads a named commit, the bake reads the
   manifest, the proof reads the zip.

---

## WHAT GOES WRONG IF THIS IS REORDERED

- **Bake before item 5** and the pack silently ships without two browser gates, the roster
  contradicts your `s219-D9` ruling of 55, and nobody notices until an audit — because every gate
  in the suite is green about it. The probe has no opinion about a gate that is simply absent.
- **Bake before item 6** and the build either refuses on the frozen surface or overwrites
  `Apollo-Spider-v1.0.1.zip`, which is frozen history. If the ledger is re-seeded without the
  version bump in the same commit, the laundering arm calls it a laundered edit — correctly.
- **Bake before item 7** and the machine never asks for your word. You get a released pack that
  nothing in the record shows you approved, which is the exact failure `s219-D4`(2) exists to
  prevent.
- **Docs (3) before the wire (2)** and `FIRST-SESSION.md` tells a designer tiktoken is optional
  while Step 4c still hard-refuses without it. That is `#220`'s F5 defect returning under a new
  name, in the one document a new designer reads first. ⚠ **The tree is in exactly this state
  right now** — the promise is written, the dance is half done, and nothing has re-proven it. It
  is safe only because none of it is committed.
- **Commit before item 2 finishes** and the delta gate goes into the release commit red, on the
  one surface this release changes.
- **Skip item 4** and the release ships on a construction argument. The whole reason this lane
  exists is that a plausible argument about tiktoken's cache was wrong on your machine.
- **Skip item 10** and the manifest describes a commit that is not the one being baked. That is the
  stale-copy-list defect the entire release shape was built to end, and the `--pack` audit will
  find it after the zip exists rather than before.
- **Skip item 12** and "works out of the box" remains a sandbox claim. Every proof above was driven
  on Linux with a venv and a dead proxy. Your machine is the one that produced the bug.
- **Reopen the parked list before the bake** and the release slips again — which is the specific
  thing `s222-D3`(2) rules against, in your own words.

---

## THE FIVE THINGS THAT ARE YOURS

1. **Item 1** — the wiring route. **(a) has already been pasted into the tree** without your word;
   it needs your yes or your stop before it is committed.
2. **Item 6** — the version number for this cut.
3. **Item 7** — your fresh word to ratify, and whether the ratify check should stop being keyed to
   a `#219` id.
4. **Item 8** — the blind delta-gate arm: repair, re-scope, or park.
5. **Item 11** — your eye on the release page, and the word "bake".

Plus **item 5's roster count**, if it still moves after the gates are fixed. And **item 12** is
your fifteen minutes, after.
