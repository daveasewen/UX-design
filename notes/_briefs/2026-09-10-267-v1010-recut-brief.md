# #267 lane P2 brief — RE-CUT v1.0.10 with the chart pages shipped (s267-D1, Dave's word)

**Model: Opus. Conductor: Fable (#267). Ruling `s267-D1` (commit `185946f`): "Hold — re-cut with the chart pages shipped. lets get this sorted". ⛔ RATIFICATION IS STILL A SEPARATE WORD — this lane takes the re-cut to PROPOSED + a refreshed go/no-go page and STOPS. No `--release`, no push, no `RATIFY_IDS`.**

## WHERE LANE P LEFT IT (read `notes/_lanes/2026-09-10-267-P-v1010-cut.md` FIRST — it is the runbook for the cut mechanics)

- `fee2572` fix (the lazy `__import__`) STANDS. `625be64` version story STANDS. `fbd9c14` = v1.0.10 PROPOSED at 625be64, 1706 files. `ea25058` = go/no-go v1 page.
- The RED: `knowledge/_validate_dataviz.py` fell out of the cut as REPO-BOUND. Lane P's finding: `_receipts.json` names **13** chart-engine test pages, `GATE_DATA_CANDIDATES` at `_gen_pack_manifest.py:266-272` ships **6**, so the gate dies in-pack on `knowledge/_tests/chart-engine/boxplot.html` and passes on the full tree.

## THE JOB, IN ORDER

1. **Find the real set.** From `_validate_dataviz.py` and its receipts file, enumerate exactly which pages (and any sidecar files — receipts, partials, data) the gate reaches when run in-pack. Do it by RUNNING the gate against a dry-run stage and reading what it opens (strace-free: instrument by reading its source paths / catching the FileNotFoundError chain), not by guessing. The 13 vs 6 is lane P's count — re-measure it.
2. **Fix the CUT RULE, not a list.** Extend the `GATE_DATA_CANDIDATES` rule (or whichever group claims `knowledge/_tests/chart-engine/`) so the pages the gate names ship by measurement — the same discipline as `fee2572`. If the correct seat is a glob over `knowledge/_tests/chart-engine/*.html` + the receipts file, say so in the docstring and why. Run `_gen_pack_manifest.py --selftest` (226 bites at lane P's end) and `build-designer-pack.sh --selftest`; add a bite that fails if a receipt-named page is absent from the cut.
3. **Commit the fix** (one commit, `during #267 2026-09-10 — …`, names s267-D1 and the mechanism).
4. **Re-cut v1.0.10 PROPOSED at that commit:** `--manifest --commit <sha>` → `--probe` → `--dry-run` TWICE, `cmp` rc. Confirm `_validate_dataviz.py` is BACK in the manifest and graded RUNNABLE/PASS in the probe, `gen_provenance_receipt.py` still in. Manifest delta vs `fbd9c14`'s and vs v1.0.9's, both named (added / removed — removed must be EMPTY vs v1.0.9 or it is a finding).
5. **Prove it in-pack:** from the unzipped dry-run stage, run `_validate_dataviz.py` the way a consumer would — exit code + first 20 lines. Re-run the consumer receipt proof from lane P (copy of `outputs/coldrun-267/arm-A-blind/dashboard.html`; frozen arm untouched) — mint rc, `_validate_receipt.py` rc.
6. **Ten release gates**, same roster as lane P; each PASS/FAIL with rc. Gates 4/5 pre-bake fact stays declared.
7. **Commit the PROPOSED re-cut** (manifest + probe + RELEASE-SPIDER pair — the `fbd9c14` shape).
8. **Go/no-go page v2:** `reviews/GO-NOGO-v1.0.10-2026-09-10-v2.html` (swiss-design-system; v1 stays as record) — s267-D1 quoted at the top · what changed since v1 (file:line) · the dataviz gate back with its in-pack rc · both manifest deltas · fingerprints · consumer proofs · ten gates · the ONE question, unanswered: *Ratify v1.0.10 at `<new sha>`?*
9. `notes/_lanes/2026-09-10-267-P2-v1010-recut.md` — done / NOT done / tokens / paths / the post-ratification command list carried from lane P, re-keyed to the new sha.

## RULES
- ⛔ No `--release`, no push, no ratification, no edits to `_rulings.json`, no `_build_all.py`, no `gen_kg_edges.py`, no `git stash`, never delete `.git/index.lock` (if one blocks, `mv` it to `knowledge/_tmp/_orphan-locks/` and say so).
- ⛔ Every claim carries a probeable token. A red is reported, never routed around. Ship NOTHING beyond what the gate measurably needs — "while we're here" is the v1 copy-list defect.
- Check-in at the seam between step 4 and step 5.
- Return under 400 words: fix sha · re-cut sha · manifest deltas (vs fbd9c14 and vs v1.0.9) · fingerprint + cmp rc · dataviz in-pack rc · receipt proof rcs · ten gates · page path · lane path · tokens · anything RED or NOT DONE.
