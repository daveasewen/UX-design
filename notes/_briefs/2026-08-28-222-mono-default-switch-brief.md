# #222 sub brief — enact `s222-D1`: mono's gallery default becomes square + transparent caption

conductor: Fable, session #222, 2026-08-28. Sub: Opus, one lane.
⚠ AGE BRACKETS (the #221 lesson — a carry's age is invisible inside a brief, so every premise
here is dated): **every premise in this brief was written TODAY, 2026-08-28, at #222, against
HEAD as of this morning.** Replay each named premise at HEAD before acting on it anyway.

## THE RULING (read it from the store, not from here)

`s222-D1` is inscribed in `knowledge/_rulings.json` (264 rulings; read it back yourself).
Substance: **mono's gallery DEFAULT becomes the candidate — square image + transparent caption,
console's colours, identical in light and dark** — closing `s220-D2 (3)`'s expressly-open mono
clause. **The grey caption ground (`#F0F0F0` light per `s219-D2 (1)`) STAYS an edit-pass option;
nothing leaves the grammar.** Dave, verbatim: *"yes the defaults for mono are fine"*, and on the
reflected firm reading, *"yes this is correct"*.

**EXPRESSLY NOT RULED and not yours to touch:** whether mono may reach the DARK caption chord
(`s219-D3`'s open question).

## THE MECHANISM — COPY THE PROVEN SHAPE, DO NOT RE-DERIVE

Read `notes/_subreports/2026-08-27-220-default-switch.md` FIRST. The #220 console switch is the
exact same class of enactment and its shape is ruled good: the receipt
`notes/_receipts/2026-08-25-219-role-defaults-exports.md` is NEVER edited; the change is a new
entry (or entries) in the `SUPERSESSIONS` layer of `knowledge/_render/role_defaults_219.py`
(table at ~line 221 — verify at HEAD).

The dial delta for mono/gallery is **ONE dial: `capBg: grey → transparent`, both modes**.
⚠ Mono needs **NO `rounding` entry** — mono's `--border-radius-container` resolves 0 in both
modes (verified at HEAD by the #221 specimen), so square is the theme's own radius token, not a
dial change. **Verify this premise at HEAD before deciding not to add the entry; if you find it
false, STOP and return — that is a conductor question, not yours to resolve.**

`ruled_by: "s222-D1"` · `supersedes: "s220-D2 (3) EXPRESSLY OPEN clause · the s219-D1 (3) mono
gallery export's grey capBg"` · quote Dave's words exactly as the store carries them.

## WHAT DONE LOOKS LIKE

1. `SUPERSESSIONS` entry landed; `role_defaults_219.py` selftest green with new clauses covering it.
2. Affected pages regenerated by their OWN generators — ⚠ **the regen serial set is ORDERED: run
   the WHOLE serial per the runbook, ramp first, index last** (#210 paid ~6 CI reds for skipping
   this). `showroom/_foundations/photography.html` is the known consumer; find the others from the
   #220 receipt's file list, not from memory.
3. Browser-measured dial table like the #220 receipt: mono light/dark now
   `cap rgba(0,0,0,0)`, imgR 0; **console/legacy/supercharge rows UNCHANGED, asserted**; contrast
   figures read off the live document.
4. **No option removed, asserted both directions**: grey/white/transparent all still reachable in
   mono gallery's chord set (`chord_refusals` machinery per the #221 mono-specimen report).
5. Advisory gates re-driven where they touch regenerated files — incl.
   `knowledge/_render/_gate_fallback_drift_221.py` (`0 drifted` expected). They stay ADVISORY.
6. Mutation arms driven BOTH ways on any new selftest clause (flip fires, restore clears).
7. Filed report at `notes/_subreports/2026-08-28-222-mono-default-switch.md` — **COUNTS in the
   REPO TEMPLATE form** (the one home, per the conductor's #221 answer: the template's field set,
   parseable by `_capture_gate.py::SUBREPORT_COUNTS_RE`), plus `REPLAY-THESE` and
   `RULING-SHAPED QUESTIONS` sections. Chat gets a STUB; the report is the deliverable.

## DO-NOT-RULE FENCE

- `knowledge/_rulings.json` is READ ONLY for you. No inscription, no edit.
- No advisory promoted, no threshold/band/constant moved, no lane touched.
- The dark-chord question stays open — do not answer it, price it, or narrow it.
- No row of Dave's closed, reworded or re-scoped. You may mint rows ONLY for your own new
  documents (forgotten-document class) via `knowledge/_state.py`, the store's own writer.
- `notes/_receipts/2026-08-25-219-role-defaults-exports.md` untouched — it is evidence.
- No option removed from ANY grammar, any theme.
- No push, no commit — the conductor reconciles and commits. No `_build_all.py` (any partial run
  strands the tree). No `git checkout` (sub ban). Version, don't overwrite: new review artefacts
  get `-vN` names; sandbox cannot rm — use mv.

## PITFALLS — CONSEQUENCES REPLAYED (Dave #165, mandatory)

- (a) Sandbox call-boundary kill ~178s: drive steps individually; `pip install tiktoken
  --break-system-packages` FIRST or measurement refusals are artefacts.
- (b) If you regenerate `gen_mono_gallery_221.py`'s page or siblings: fallbacks are RE-DERIVED
  from canon, never typed — the #221 drift gate exists because `#F3F3F3` went stale in seven
  generators. Expect canon's answer `#F0F0F0` for `--surface-subtle`.
- (c) `set_content()` is BANNED in render proofs — `goto("file://…")`; canvas probe not
  `fonts.check()`; chromium libs recipe is stratum 4 of `knowledge/_RUNBOOK-render-verify.md`;
  ENOSPC → `/var/tmp`.
- (d) `_capture_gate.py --selftest` WRITES `knowledge/_CAPTURE-GATE.md`; if it fires, restore via
  `git show HEAD:knowledge/_CAPTURE-GATE.md > knowledge/_CAPTURE-GATE.md`.
- (e) A crash is not a fail: parse helpers fail LOUD and NAMED; declare the residual. Could-not-run
  is graded COULD-NOT-RUN, never green.
- (f) An unmatched grep is not an absence: name every probe; search twice before writing "missing".

## BUDGET

QUOTA binds today (Opus sub spend is quota, not the conductor's FILL). One lane, S/M effort —
report your own turn count; your token spend is UNMEASURED from your seat, say so in the report
header exactly as the #220 receipt does.
