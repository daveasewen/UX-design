# #220 delegated-wrap brief — Opus wrap sub. Follow `knowledge/_RUNBOOK-capture-ritual.md`; this brief carries session facts + the fence, the runbook carries the mechanics.

## Session facts (verify each against the tree/store — never retype from here)
- **#220, Fable conductor, 2026-08-27, one day.** Title: `Apollo - #220: dave's parked bento lane and his unenacted correction`.
- **THREE RULINGS INSCRIBED: `s220-D1` · `s220-D2` · `s220-D3`** — verify present by READING `knowledge/_rulings.json`; quote `says` from the store only.
- Arc: readings page → dark-ladder → rung enact → theme lift (all four themes, by lift not hex) → default switch (console gallery = corners+transparent both modes; MONO EXPRESSLY OPEN) → #219 PARKED CORRECTION discharged by addition (receipt cites s220-D2) → **Apollo - Spider v1.0.1 carrying Memento - Gumdrop v1.0.1 BAKED + PUSHED** (both cold-start breakers at cause · `_gate_pack_imports.py` born ADVISORY · 30 derivatives per s220-D3 · pack-arm re-scoped at cause on the first two-release day, zip 18,992,272 B / 1,646 entries / sha c8934ebd… — re-measure from the artefact) → **six-lane audit wave L1–L6 filed** + `reviews/SITTING-220-2026-08-27-v1.html` (122 calls: 97 open · 25 CANDIDATE-closed, nothing closed).
- Filed reports: `ls notes/_subreports/2026-08-27-220-*.md` is the authority (~16). Cite EVERY one BY PATH with COUNTS copied off the files (`s218-D7`).
- Commits: verify `git log --oneline` since `f008a2a`; pushed through `b6f5ad0`; `707e5aa` + the wave docs are unpushed/uncommitted — reconcile BY PATH (attribution: wave reports+assets = lanes L1–L6; `_graph-mark-observations.jsonl` rows = lanes' `_memento_search` marks, machinery; sitting page = L5; this brief = conductor), then wrap commit (`--wrap`, RED BLOCKS) and push per `knowledge/_RUNBOOK-git-commit.md` (SESSION_N=220, delete-grant already given this session).
- Store rows: mint via `_state.add()` for the audit divvy brief, L1–L6 reports, the sitting page, and this wrap brief (W-213…W-217 exist; the doc-row gate names anything missed). Close NO row.

## Gauge (three moments, never rounded into one another)
- **boot 69,565 real — TENTH consecutive out-of-band (s208-D1 band 55,595–57,903), declared at the opener, LOG ONCE** (it is already the session's one boot sample — do not double-log).
- Conductor brief-cut **FILL 295,443 real over 88 turns** (his last `_checkin.py`); measure your OWN cut first-hand and report both, with the delta.
- **subs MEASURED n=12, total 2,917,421 quota tokens** (240,404 · 274,341 · 215,636 · 282,965 · 313,414 · 334,532 · 227,310 · 217,703 · 196,969 · 162,328 · 270,605 · 181,214) — QUOTA never window FILL. ⛔ **≥4 further subs ran (readings-capsule, charts-sparkline, bake-seam, bake-addendum) whose usage lines the conductor no longer holds — DECLARED UNMEASURED, excluded from count and total, never estimated.**
- Panel, Dave's own readings verbatim: morning **68 all / 63 Fable**; ~3.5h-to-reset **77 / 69**. Quote; invent no final.
- ⛔ **THE STOP LINE (190,000, armed) WAS CROSSED MID-BAKE WITHOUT AN AT-CROSSING DECLARATION — the conductor declared it LATE, when Dave asked "how hot".** Record the miss as the conductor's, named, in the banner ([[feedback-wrap-is-not-optional]] honesty clause). The 200K and 256K lines were also crossed; ~15M platform window; evidence FOR the pending re-base, never a licence. NO CONSTANT, BAND, ADVISORY, STOP LINE OR WALL MOVES.

## DO-NOT-RULE fence
No Dave-owned row closed, reworded or re-scoped (mono default stays OPEN · the 97 sitting calls stay open · CANDIDATE-closed stays candidate) · no threshold/band/advisory/stop-line/wall/lane change · `_rulings.json` READ-ONLY (verify s220-D1/D2/D3 present by reading) · no promotions — every advisory gate stays advisory (`_gate_pack_imports`, minted-consumption, hidden-display, the L1 findings) · no memory writes (step 3 is the conductor's in-window; record DECLARED) · no ADR edits · no MODEL-ROUTING change · no new divvy plans · no repairs from the audit findings (they are next-session lanes).

## Residuals to carry (name, never smooth)
Mono's gallery default — his one word · THEME-LIFT / DEFAULT-SWITCH / RUNG proof pages await his eye · **CI red at the FIRST `gates` step since `aa26947` — two sessions of gate bites unrun in CI (L1, CLAIMED via local bare-clone proxy), read-back queue still blocked at his GitHub sign-in** · `git gc` recovers ~744 MB — DAVE'S MACHINE ONLY (the sandbox tmp_obj kill class, L6 T-1) · L1's 8 false-greens + 26 blocking-without-known-answer + orphan selftests · L2's `gen_dashboard --check` no-FAIL-path + the `Chart-*.meta.json` case-sensitivity class (7 components silently degraded on CI's Linux) + SPACING_STOPS still two homes · L3's 16 drifted fallback cause-sites in 5 sibling generators + the probe-README false premise (64 findings that are gone) + fork-ledger rot 5 rows · L4's four blocking first-hour stumbles (tiktoken "optional" is false · retrieval signpost · runner's phantom zero-population logic · no-playwright FAIL-vs-CNA) + the priced doc-resolver bake gate (~2.5K) + ⛔ the addendum Q1 is NARROWED, not open — do not re-put it as unstartable · L6's T-2 parallel survey (−118s/push) · T-4 timeout re-dial · dead runbook stratum (0/17 debs) · **L6's DECLARED fence-breach: `_capture_gate.py --selftest` ran twice, no tracked file moved — premise re-check upward priced, not run** · type-ratchet quoted 3 ways (1091/1093/1097) — measure once at wrap and state the one number · Copilot bridge UNPROVEN (narrowed by L4's artefact drive) · v1.0.1 does NOT carry today's caption work — next release · the s208-D1 re-base still Dave's, tenth drift + two past-wall windows banked.

## Pitfalls for the banner (Dave #165) — today's own
(a) a refusal names the first obstacle (T3's SESSION_N) · (b) a gate green over an artefact that cannot RUN (the delta gate; the class fix now exists advisory) · (c) a two-release day breaks any gate that audits history against the live manifest (pack arm, fixed at cause) · (d) a silent stop-line crossing is a failed declaration even when the platform window makes it safe (the conductor's own miss) · (e) a licence word homed in a code comment is not homed (s220-D3 is the fix).

## Deliver
Final message = the wrap receipt stub: commit hashes, push verification, subject read back, **BOTH title lines** (this session's + the regenerated NEXT title from `_CHAIN.md`) for the conductor to relay in chat, plus anything the fence forced you to leave.
