# #218 delegated-wrap brief (Opus wrap sub)

**Session facts (conductor-measured, use these — do not re-derive):**
- #218, 2026-08-24 (Mon), FABLE conductor + **2 Opus work subs** — measured:
  `subs 598476 tokens (n=2)` (grids 300,141 · photography 298,335). This line is PARSED — copy
  it exactly; no `job` word in it.
- DELEGATED wrap ⇒ the `s214-D4` ~190,000 advisory was ARMED at the opener and announced.
- boot **66,845 real** — ⛔ OUT of the s208-D1 band (55,595–57,903) by 8,942, the EIGHTH
  consecutive out-of-band boot, DECLARED not corrected. ⚠ Phrase as "boot drift of 8,942" —
  a bare number beside 'boot' breaks `_parse_boot_samples` (the #215 pothole).
- Conductor FILL at brief-cut: **~351,489 real over ~141 turns** (checkin, this seat) — past the
  armed advisory, the 200K working line and the 256K wall; the seat's platform window shows
  ~15M; DECLARED, evidence for Dave's pending re-base, ⛔ NO constant/band/advisory touched.
- The platform reset panel Dave pasted: all-models 38% · Fable 36% (parity healthy); Dave opened
  a four-lane CRANK (#60 production-crank case) — divvy plan is the CONDUCTOR'S, after this wrap.

**What happened today (banner material, in order):**
1. **Lane minted at the opener, Dave's word** ("the bento lane still needs work"): `lane-3-bento`
   ACTIVE in `knowledge/_lanes.json` + GM ROUTING line + lanes view regenerated — discharges the
   register disagreement #216 and #217 both named. lane-2 stays active.
2. **`s218-D1` corner keylines** — Dave's construction, built into the explorer, probe green all
   8 states + narrow bands, `--break-layout` arm red 9 corner assertions by name; SIGNED OFF by
   his eye ("Yay it works. very happy with this."), scope DASHBOARD-ONLY option-selected.
   En route: a stale-tab miscommunication cost the morning; Dave: "lesson learned today, we
   spent hours fixing a miscommunication" — conductor saved the memory rule (step 3 DONE,
   conductor's, this window: mock the readings before building).
3. **`s218-D2` Grids group** — Foundations > Grids: grids-12col + grids-display/gallery/dashboard,
   controls kept, one data path, explorer untouched; grids sub's probes green, `--break-groups`
   arm red 5 by name; its first arm run exposed and fixed two probe holes (split-on-absent-marker;
   mutant-dangle reading 0==0 — a 0px console radius is now a REFUSAL).
4. **`s218-D3` photography** — per-theme settings from Dave's four verbatim exports (receipts at
   `notes/_receipts/2026-08-24-218-photography-theme-settings.md`), ALL 251 derivatives minted
   (42.1MB measured, 5 over the 300KB ceiling flagged `over_target`), page lazy-loaded, mono
   captions #1A1A1A/white (PROPOSED shade, Dave's eye owed), `edge:square` RECORDED NOT ENACTED.
5. **⛔ SPECIMEN CORRECTION, conductor's, after the sub returned:** the sub's filename-slice cap
   had swapped 14/15 photographs on the live decision surfaces; the specimen is now PINNED BY
   NAME to the ratified s217-D1 set in `gen_bento_roles_217.SPECIMEN_FILES` (+ the canon
   generator's private reader repointed), verified identical, selftests + both photo-bearing
   grids pages re-driven green. Full matrix re-verify after the pin NOT re-run — bounded
   (s172-D3), declared: the pin restores the exact files the green morning run loaded.
6. **Class fixes:** `BM_MUTANT_DIR` env honoured by generator+verifier (the shared-/var/tmp
   stale-mutant defect — a mutation arm silently proving yesterday's clause);
   `drive_library_215.py` three typed constants → read from index.json. **Class findings named
   not fixed:** `_capture_gate.py --selftest` WRITES `_CAPTURE-GATE.md` (#158 class, new door —
   if it fires during this wrap, restore from HEAD and carry the finding); INSTRUMENT-STRAY
   still keys on UNTRACKED (staging the 236 new derivatives clears it — #217 precedent).
7. **Three rulings INSCRIBED by the conductor** — s218-D1/D2/D3, reconstruction proofs green.
   ⛔ `knowledge/_rulings.json` is READ-ONLY to you: verify the three ids present by reading.
   Entry inputs live in gitignored `_to_delete/_218-entry-inputs/` (do not commit — #216/#217
   precedent; archiving inputs has been raised to Dave twice, still his).
8. Stale `.git/index.lock` (0 bytes) cleared by the conductor under the delete grant.

**Carries to re-type / residuals:** the #217 residual set ages +1 where unstruck. STRUCK today
with receipts: the lane-register disagreement (mint, item 1); the #217 residual-⑤ INSTRUMENT-
STRAY re-scope stays PRICED NOT BUILT (it will bite again on the next untracked derivative).
NEW Dave-owed: mono caption shade (his eye); 5 over-ceiling derivatives (spec call);
`edge:square` reopen-or-not; stale review artefacts v2/v3/gallery-compare now render the pinned
specimen only if regenerated — NOT regenerated, declared. NEW carry: the crank divvy plan +
two window handoffs are the conductor's next act; CI read-back queue grows by this push.

**Your job (the mechanical wrap, per `knowledge/_RUNBOOK-capture-ritual.md` — READ IT FIRST):**
run the full ritual: reconcile the working tree path by path (`git status --short` accounted,
no blind `git add -A`; `_to_delete/` stays out), gauge-log lines (subs line above; ABSENT is
legal only where a figure truly did not reach you — these did), banner + ⏱ LATEST delta authored
per the roll conventions, 2c/2d/2f exit checks, chain regen (`_gen_chain.py`), **2g memento
index rebuild LAST**, doc-row gate (mint existence rows for today's two briefs + receipt + new
instruments if it refuses — existence and close-condition only, they rule nothing), W rows for
the two builds (same discipline), commit with a FRESH printf msgfile per
`knowledge/_RUNBOOK-git-commit.md` (READ IT), **read the commit subject back after committing**,
push. CI read-back is the conductor's, not yours.

**DO-NOT-RULE:** no Dave-owned row closed or reworded; no threshold/constant/band/advisory/lane
change; no `_rulings.json` write; no memory writes; no ADR edits (ADR-0011 amendment stays
owed/carried); no divvy plan; no `MODEL-ROUTING.md` change. If a gate demands something on this
list, STOP on that item and return the refusal instead.

**Deliver back:** commit hash + subject line read back from `git log`, the next-session title
line (both forms, for the conductor to relay — delegated-wrap rename relay), gauge figures as
logged, every gate's verdict, refusals/residuals named.
