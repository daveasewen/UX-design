# W-46 scope-PM brief — mechanisation items 3/4/5, SCOPE ONLY (#207, 2026-08-19)

*Conductor-minted (Fable seat). Governed by `s204-D1` and the programme brief
`notes/_briefs/2026-08-19-204-mechanisation-programme-v1.md` § SCOPE AND PLAN ONLY.
Store row for this brief: `W-52` (minted at creation, forgotten-document class #185).*

## The job

Produce **three scoped proposals returning to Dave** — one per item below. Output is ONE
document: `notes/_briefs/2026-08-19-207-w46-three-scoped-proposals-v1.md`, plus an incremental
claim table at `notes/_claims/207-w46-claims.jsonl` (copy the schema from
`notes/_claims/206-w45-claims.jsonl`; append rows as each proposal closes, never as a final act).

⛔ **SCOPE-ONLY MEANS SCOPE-ONLY.** No code is built. No generator is written. No gate is wired.
A throwaway measurement command run to PRICE something is fine; a reusable script left in the
tree is a build and is out of scope.

### Item 3 — decision-capture controller (attacks Dave's review burden directly)
One generated page: one card per PROPOSED item — evidence, attached store-search (`s202-D3`),
options as buttons; picks written to a jsonl the CONDUCTOR inscribes from via
`_inscribe_ruling.py`. Machinery captures and transports decisions; it NEVER makes them.
Scope: where picks land (new jsonl vs `_REVIEW-SIGNOFF.md`) · how a pick's firmness is read
back (the readback-sensation rule: a pick from an incomplete set reads as a ruling) ·
live-controller conventions (live contrast + export — Dave's standing preference).
⚠ **MANDATORY input:** read `notes/_briefs/2026-08-19-207-addendum-206-report-critique.md` —
Dave's own critique of an ID-code decision surface delivered TODAY. The controller's cards are
exactly the surface he critiqued: they must render plain words, never bare IDs; they must not
launder an open premise into a settled choice; option sets must declare their completeness.

### Item 4 — mint-time brief generation (kills the stale-premise class at source)
`gen_brief.py` mints PM briefs from the live store: open items, DO-NOT-RULE pulled from rulings
tagged Dave's, fences from the runbook, premise table PRE-FILLED by running probes at mint time
(`s200-D1` pattern). Scope: brief schema · which probes are mintable vs seat-bound · how the
generator's owned regions are declared (the do-not-rule-list-cannot-fence-a-generator class).

### Item 5 — CI pixel leg
The existing CI render job runs the W-45 pixel probes on every push. Scope: shared measurer
with the sandbox (#173 — a gate that cannot pass in one environment is the named killer here;
P-3 is currently rc=0 / rc=1-refused / unproven across three environments) · push-blocking vs
survey-tier per probe · the could-not-ask exit protocol (unruled, carried — do not rule it).

## The rigour bar (every proposal, non-negotiable)
Consumer named at birth · priced consequences section (pitfalls REPLAYED, Dave #165) · no gate
that cannot pass in the environment it runs in · promotion by evidence, never speculation ·
every open question carries its run store-search with pasted output (run-before-cite — you may
not cite a search you did not run) · every mechanical claim carries its probeable token
(`s182-D1`) · decision surfaces in PLAIN WORDS (the #170 class).

## DO-NOT-RULE (all of these are DAVE'S — propose around them, never settle them)
- The twice-caught vocabulary question (does mined history count as a "catch"?) and
  promote/wait on the three probe candidates.
- Repair-or-park on the 46 duplicate-id/IDREF findings (`W-49`).
- The five W-44 schema choices; whether `notes/_claims/` earns a store row.
- PM-topology permanence (`s203-D2`); the could-not-ask exit protocol.
- Anything in `knowledge/_rulings.json` tagged as Dave's; anything marked FLOATED or PROPOSED.
- If a scoping question turns out to be pre-answered by an existing ruling, SAY SO with the
  ruling quoted — do not re-put a settled ruling as an option.

## Fences and environment
- ⛔ No `git checkout`, no `git add -A`, no commits — the conductor commits at the seam.
- ⛔ Do not touch `_LIVE-STATE.md`, `GOOD-MORNING.md`, `_CHAIN.md`, `knowledge/_rulings.json`.
- ⛔ Do not run `_build_all.py` (a partial run strands the tree).
- Mint ONE store row (`W-53`) for the proposals doc in `knowledge/_state.json`, home = the doc,
  closes_when = "Dave rules build/park per item; W-46 closes on the same event". Run
  `python3 knowledge/_state.py` after and paste the tail.
- `pip install tiktoken --break-system-packages` first if any measuring tool refuses.
- Sandbox kills calls ≥~45s; nothing survives a call boundary.
- Regenerate the memento index (`python3 knowledge/_build_memento_index.py`) after the docs land.

## Return to the conductor
File paths written · claim-table row count and any DECLARED refusals · the three decision
surfaces in plain words, ≤3 sentences each · every deferment priced.
