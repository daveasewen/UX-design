# #258 lane C2 brief — THE SECOND COLD RUN: the frozen prompt against the TREE after s258-D1/D2 are enacted

**Model: Opus 5. Conductor: Fable (#258). Read-only against canon/knowledge — MOVES NOTHING, RULES NOTHING.**

## WHY
Commit `e774fb7` enacted `s258-D1` (rule 2a removed — the author writes JS) and `s258-D2` (ambitious by default: data model first, everything works, state persists, zero JS errors, footer mandatory). The first cold run against the v1.0.7 zip scored **3 / 2 / 0 / 2** (`reviews/COLDRUN-258-2026-09-08-v1.html`). This lane answers: what does the SAME prompt produce now?

## THE PACK UNDER TEST — the TREE at `e774fb7`
No zip exists for this state. Read from the repo: `apollo-spider/skills/generate-from-canon/SKILL.md` (name `ADS-generate-from-canon`), `apollo-spider/skills/grill-me/SKILL.md`, `showroom/index.json`, `knowledge/components/*.meta.json`, `knowledge/snippets/*.reference.html`, `knowledge/canon/*.css`, `knowledge/guidelines/`, `knowledge/tokens/`, `knowledge/assets/icons/`. Record `git rev-parse HEAD` in the READ-LOG.

## THE PROMPT (verbatim, FROZEN)
```
/generate-from-canon build me a financial dashboard for corporate international banking. Please make the all the interactive elements work such as filtering and navigation.
```

## ONE ARM — BLIND
Follow `notes/_briefs/2026-09-08-258-cold-run-brief.md` § ONE ARM and § MEASURE exactly, with: output root `outputs/coldrun-258-v2/arm-A-blind/`; review `reviews/COLDRUN-258-2026-09-08-v2.html` (delta table now has THREE columns: #246 · #258 v1 · #258 v2); subreport `notes/_subreports/2026-09-08-258-C2-cold-run-2.md`. Do NOT read `outputs/coldrun-258/` or the v1 review until the arm is frozen. Follow the NEW skill honestly — if it now tells you to write a data model and wire everything, do that as a capable Copilot-class model would, and no more than the skill asks. Add two measurements: (8) JS `pageerror` count on load; (9) a one-line judgement, labelled as such, of what "code creativity" the rewrite invited that a 2a-bound build could not have produced.

## RULES
⛔ No edits under `knowledge/`, `showroom/`, `apollo-spider/`. No `_build_all.py`, no commit/push, no `git checkout`/`stash`. Write NOTHING under `knowledge/_screen-gate/` — if `_validate_screen.py` insists, run it on a copy of the page placed under `/tmp/` and note that the gate wrote its ledger there instead; if it still writes into the repo, name the files so the conductor can delete them. Rule nothing of Dave's; RSQs on the review page. Every claim carries a probeable token. Context use noted at the build→measure seam.

Return ≤25 lines: paths, four scores with earning facts, pageerror count, the three-column delta in one line, top three findings, the creativity judgement, NOT-done, tokens.
