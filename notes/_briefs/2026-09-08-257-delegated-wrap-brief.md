# #257 delegated wrap brief — run the FULL wrap ritual

**Model: Opus 5 (rule 7). Conductor: Fable (#257), FILL 216,975 real per `_checkin.py` at the brief cut — PAST the 150,929 advisory by 66,046 and past the 200K wall (the harness reported far more room; the ruled unit wins and is stated, not argued). Recall probe NOT planted (declare, 0 claims). Boot 68,837 real — the FIRST reading UNDER the 70,000 ceiling after two breaches; the `MEMORY.md` stub cut of #256 measured at last. State ONCE, in the stratum. No pace panel asked.**

## 1. RULINGS — THREE, Dave's, ALREADY INSCRIBED by the conductor (store 395 → 398, parsed). Do NOT re-inscribe. Read back from `knowledge/_rulings.json` and quote `says` verbatim in the stratum.
- `s257-D1` — v1.0.6 RATIFIED at `1f4c35587f30`. Dave: *"ratiify."* (sic). RATIFY_IDS keyed directly (not at the next cut) because `dist/` did not match the manifest.
- `s257-D2` — CUT v1.0.7 with the six pack skills renamed `ADS-<name>` (SKILL.md `name:` + 25 in-skill cross-refs; directories unchanged). Dave: *"Cut 1.0.7 can you prepend ADS- to the skill names so they are easy to spot in a list"*. An AUTHORISATION (s223-D3).
- `s257-D3` — v1.0.7 RATIFIED at `b9c4c802439c`. Dave: *"ratify"*. The ratifying word.

## 2. WHAT #257 WAS (WHAT here; WHY in `_DECISION-HISTORY/2026-09-08-257-two-ratifications-and-the-frozen-proposed-zip.md`, which you write)
- Opener: chain read (#256 → #257), tiktoken installed cold, `_checkin.py` boot 68,837. Led with the one item with weight (ratify v1.0.6); Dave: *"ratiify."*
- **v1.0.6:** `s257-D1` inscribed; RATIFY_IDS['v1.0.6']='s257-D1'; manifest regenerated RATIFIED at `1f4c355` (1,673 files, 43,160,016 B). A 0-byte `.git/index.lock` blocked the commit; `mcp__cowork__allow_cowork_file_delete` granted, `rm` worked (runbook step 0). Commit `367be6d`. `--release` baked `dist/Apollo-Spider-v1.0.6.zip` sha256 `4781994f…` (byte-identical to #256's dry-run). `--pack` flipped RED → PASS. Commit `2b781c9`. ⛔ **FINDING: `_gate_frozen_release.py` then went RED — `RE-RECORDED WITHOUT A VERSION BUMP` (recorded `398c695a`, measured `85f4f4db`).** Root cause: #245 seeded a PROPOSED zip as frozen; a ratified re-bake at the same version is laundering by the ledger's own rule. Gate was RIGHT. Put to Dave with "bump to v1.0.7" recommended.
- **Sutherland correction:** Dave: *"where do you get the idea that this has anything to do with Sutherland? The demo is to David Rice at HSBC"*. Source found: `_DECISION-HISTORY/2026-09-05-246-the-library-dictates.md` L31 wrote "the Sutherland demo" as an INFERENCE (Sutherland = HSBC's React library). Corrected BY ADDITION (the line stands, a ⚠ CORRECTED block above it). Web research (HSBC media release 23 Mar 2026, Banking Dive, Disruption Banking, HSBC USA 2020): Rice = HSBC's first Chief AI Officer from 1 Apr 2026, ex-COO CIB, ~20 yrs, operator; CTO Shamtani owns the model platform. Prep note `notes/_DEMO-PREP-david-rice-hsbc.html` (swiss, ~6 KB). Memory hook written by the CONDUCTOR (`demo-audience-is-david-rice-hsbc-257.md`), MEMORY.md re-trimmed to 1,781/1,802 tape. Commit `992699b`.
- **v1.0.7 cut:** `s257-D2` inscribed. Rename: `name: ADS-…` ×6 + 25 cross-refs (no gate binds name to directory — checked). Version story in the #245 shape: VERSION + MEMENTO_CUT_VERSION → v1.0.7 with the four carried literals (FIRST-SESSION.md, gumdrop `_state.json` built_by, two gumdrop runbook headers). Selftest 216/216. Commit `b9c4c80` = the cut commit. `--probe` 41 RUNNABLE / 9 REPO-BOUND / 4 NEEDS-DEP; `--manifest` at `b9c4c80`: 1,673 files, 43,162,006 B; dry-run zip sha256 `13a593de1b11…` 20 MB, `--check` GREEN, `--manifest-check` PASS; `ADS-grill-me` verified INSIDE the zip. Commit `540c7ec` (manifest, PROPOSED). Dave: *"ratify"*.
- **v1.0.7 release:** `s257-D3` inscribed; RATIFY_IDS['v1.0.7']='s257-D3'; frozen-ledger literal v1.0.6 → v1.0.7; manifest regenerated RATIFIED (`dbf4d0a`); `--release` → `dist/Apollo-Spider-v1.0.7.zip` sha256 `13a593de…` byte-identical to the dry-run (`a4bf8c2`); `--seed` → apollo-spider row v1.0.7, 7 files, `9431a8f6a707` (`82b310c`). Gates: frozen PASS (3 arms) · `--pack` PASS at `b9c4c802439c` · `--drift` clean. **PUSHED `71ab931..82b310c`, VERIFIED.**
- Review page `reviews/RELEASE-SPIDER-2026-08-26-v1.html` regenerated 4× with the overlay re-injected each time. Three #256-dirty jsonl logs (`_graph-mark-observations`, `_REHEARSAL-LOG`, `_dream/_GRADE-DECISIONS`) carried into `367be6d`.
- Subs: **0** this session before this wrap (no lanes). Fable did the release work itself — under the advisory at #256's handover, past it by the end; the check-in was run at the opener and at the end, NOT at the seams between the three cuts (declare: a seam-less run, `checkin-at-the-ends-cannot-catch-the-lane`).

## 3. STILL DAVE'S (carry, by addition to `_CARRIES.md` § residual → #258): **dashboards one-shotable — the cold run of the frozen prompt NOT DONE at #257** (the day's stated job; two ratifications took the window) · **his rulings page** not built · whether to rename the skill DIRECTORIES too (name-only rename shipped) · **P1(b)** · **`POPULATION_DELTA_THRESHOLD = 5`** · dashboards as permanent gate subjects (P4(a) premise expired) · **Q2–Q6** · **R1** · `intent` collapse ≈09-19 · the boot-ceiling literal (first reading UNDER — 68,837 — what to do with the arm is his) · midnight-wrap stamp (age 9) · which scopes may write `repeat(n)` · 2 STALE hooks naming `_measure_tokenizer.py` · CI read-back `s203-D1` for `82b310c` (owed) · the frozen-ledger design question: a PROPOSED zip was seeded as frozen at #245 — should `--seed` refuse a PROPOSED surface, or should the birth clause admit PROPOSED→RATIFIED at one version? (priced, not taken; v1.0.7 bump was the remedy) · demo prep: who is in the room, the ask, HSBC's external-LLM constraint (three questions in the prep note) · `META-TAGS-CALLS-CONTEXT` still never rendered · no render/browser/`_build_all.py`/plant at any seat.

## 4. NEXT: the cold run of the frozen prompt against a dashboard, then his rulings page. Next title: `Apollo - #258: dashboards one-shotable — cold run of the frozen prompt; his rulings page`.

## 5. THE RITUAL, WHOLE — steps 1→5 per `knowledge/_RUNBOOK-capture-ritual.md` (grep; read the runbook, not the hook). Step 2g rebuilds `_memento-index.json` LAST. `--wrap` (the ceiling arm may now PASS on 68,837 — report what it says; if REFUSED, commit in the #243 form and SAY SO). Subject READ BACK from `git log`; both title lines returned to the conductor. **PUSH the wrap commit** via `bash knowledge/_git_commit.sh --push`; if it fails, declare. Declare every skip WITH ITS SIZE. Memory: the CONDUCTOR wrote it (one hook + index trim) — declare 0 bytes at this seat. Do NOT rule anything in §3. Do NOT edit `_DECISION-HISTORY/2026-09-05-246…` further. Do NOT run `_build_all.py`. Do NOT touch `dist/` or the frozen ledger. Git: delete grant is ON this session (`rm .git/index.lock` works); use `SESSION_N=257 bash knowledge/_git_commit.sh --reconciled <msgfile> --all-dirty` (or named paths); msgfile under `/sessions/adoring-exciting-cori/mnt/outputs/`. Sandbox paths: repo at `/sessions/adoring-exciting-cori/mnt/UX-design/`; `pip … --break-system-packages`; tiktoken installed. Batch independent reads; Edit, never Write, on existing files. Check-in inside the lane if you pass 15K.

Return under 350 words: `_rulings.json` count, commit hash + subject read back, push result, `--wrap` result, `_CHAIN.md` size line, declared skips, both title lines.
