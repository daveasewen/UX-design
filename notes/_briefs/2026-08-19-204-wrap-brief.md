# Wrap brief — #204 (delegated wrap, Opus sub), 2026-08-19

*Cut by the FABLE conductor. You are the wrap sub. Read `knowledge/_RUNBOOK-capture-ritual.md`
FIRST and follow it — this brief is session FACTS, the runbook is the PROCEDURE. You COMMIT
ONLY; the push and its CI read-back are the CONDUCTOR'S under `s203-D1`. Re-run every gate rc
and read back every sha first-hand — never copy a figure from this brief without re-measuring
where the runbook requires it.*

## ⛔ DO-NOT-RULE (Dave's, untouchable — carry verbatim into the banner as open):
the `[114]` 0.40 chain-ratio constant (`_gen_chain.py:647-648`; store search run this session:
`_memento_search.py "chain selftest 40% materially smaller threshold"` + rulings grep → NO
settling hit, ds-025 tangential) · the two-red Stat-card/Kpi-tile seam · hit-area gate
promotion (both calls) · the itinerary xlsx retirement · the segmented type-scale binding ·
Kpi-tile PROPOSED status · the 44-record three-way split reconciliation · `W-38` ·
**the PM-topology PERMANENCE verdict (s203-D2 — Dave rules on the trial numbers below)** ·
every PROPOSED item in the #203/#204 receipts. ⛔ Do NOT write `knowledge/_rulings.json`;
do NOT touch the non-repo memory store (memory step 3 was RUN by the conductor this session —
record it as DONE-BY-CONDUCTOR, never claim it as yours).

## Session facts (#204, FABLE conductor · Opus subs n=4 · PM-topology trial per `s203-D2`)

**RULING, ONE, DAVE'S — `s204-D1`** — topology ADOPTED with four amendments (incremental
claim table · fix loop in build-PM mandate · run-before-cite · verifier render lane) +
mechanisation programme ordered: BUILD NEXT `W-44` (schema'd claim/challenge JSONL + join +
evidence linter) and `W-45` (probe registry + twice-caught promotion rule); `W-46` = scope-only
lane for items 3/4/5. Inscribed by the CONDUCTOR via `_inscribe_ruling.py`, read back by
script: 204 records, `s204-D1` newest, by Dave. ONE home:
`notes/_briefs/2026-08-19-204-mechanisation-programme-v1.md`. Permanence ≠ adoption — still his.

**The trial ran, four seats:**
1. BUILD-PM (Opus, 131,688 tk, 58 calls) — built 6 of 7 P2 components (Popconfirm · Footer ·
   Layout-utilities · Document-row · Payment-card-visual · Runway-bar; library 85 → 91) +
   lane-1 CI repair. ⚠ KILLED by connection loss at the claim-table write (min ~43) — the
   trial's one LOSSY HOP, and the direct cause of amendment ①.
2. FINISHER (Opus, 136,326 tk) — reconstructed the claim table from receipts
   (`notes/_receipts/2026-08-19-204-buildpm-claim-table.md`), re-ran all five gates rc=0,
   verified lane 1. ⚠ Its own catch: /var/tmp redirect served a FOREIGN SESSION's stale gate
   output as this run's evidence — two gates read RED off months-old numbers; re-run in mktemp.
3. ADVERSARIAL VERIFIER-PM (Opus, 113,224 tk, 48 calls) — 34 CONFIRMED · 3 CONTRADICTED ·
   12 UNTESTED · 4 NEW findings, all grounded
   (`notes/_receipts/2026-08-19-204-verifier-challenge-table.md`). Caught ITSELF citing an
   unrun search (claimed 2 hits, real 35) — cause of amendment ③. Confirmed row-91
   Transaction refusal correct (false gap — ships in List-items `type:"transaction"`,
   PROMOTED 2026-06-22 Dave). No browser driven — visual conformance UNVERIFIED this session
   (cause of amendment ④).
4. FIX sub (Opus, 100,182 tk) — all three verifier hits repaired + verified: 3 metas schema-
   conformed (92-meta sweep: 91 PASS, sole fail = the pre-existing EXAMPLE template) ·
   duplicate ids in 3 review pages fixed (all six pages DUP=0, unresolved_aria=0; lane-M pages
   regression-checked) · `W-43` store row added (`_gate_doc_rows.py` rc=0, mutation-checked).
   Addendum appended to the claim table.

**CI state:** #203 head's gates job was RED (4 survey fails) — relayed to Dave at the opener
(run 32239404300). This session FIXED `[3]` blast-radius rc=0 + `[110]` mention-map rc=0;
`[13]` capture-gate and `[114]` chain-ratio remain rc=1, DECLARED — `[114]` is the picked 0.40
constant (Dave's, store search quoted above); `[13]` part-re-reports `[114]` + the `_governs`
matcher. Expect the next CI gates job to stay RED on those two — say so on the banner, and the
conductor relays the actual verdict post-push.

**Conductor's other acts:** premise probe caught the banner's "8 P2" vs the JSON's 7 (briefed
off the JSON) · `s204-D1` inscription · programme doc + `W-44`/`W-45`/`W-46` rows
(`_state.json` pure append, check ok=True) · trial-brief addendum (pointer only, ADR-0017) ·
memory step 3 RUN (the false "44 token has NO enforcing gate" clause in
`mint-time-derivation-s200.md` STRUCK with receipt; the three-way-split reconciliation left
Dave's; new hook `pm-topology-adopted-204.md` + index line).

**GAUGE (conductor's, first-hand):** boot 56,589 real (in band) · FILL at wrap-brief cut
~155K real, past the ADVISORY stop line 150,929, under the 200,000 working line, wall 256,000
BINDING · **subs 481,420 tokens (n=4, MEASURED, itemised above)** · effort band **L**.
⛔ Your own spend is EXCLUDED and unknowable from your seat — declare it. **QUOTA relayed by
Dave at the opener: All 54% · Fable 66%, resets Thu 10:59 PM** (#203 close was All 47% ·
Fable 61%). The CLOSE panel is OWED — the conductor asks Dave in chat; do not invent it.

**TRIAL NUMBERS FOR DAVE (the `s203-D2` return — put them on the banner):**
① Fable FILL end-to-end: 56,589 boot → ~155K at wrap-open (#203 baseline: 70K→178K).
② Quota deltas: opener All +7 / Fable +5 vs #203 close; close panel owed from Dave.
③ Sub spend 481,420 (n=4, both PMs included) vs #203's 2,170,761 (n=12).
④ Contradiction yield: 3 CONTRADICTED + 4 NEW, ALL survived the conductor's read, ALL repaired
   pre-review. The verifier was NOT toothless — its teeth are named in its own receipt.
⑤ Lossy-hop tally: 1 (build-PM kill → finisher reconstruction). Environmental note: agent
   resume (SendMessage) unavailable in this harness — the finisher and fix sub were cold starts.

**residual → #205 (carry per runbook 2c discipline):** `W-44`/`W-45` builds + `W-46` scope
lane (Dave's order, `s204-D1`) · ⬛ DAVE'S EYE now on **35 surfaces** (29 from #203 + 6 new
`REVIEW-204-*` spreads; fintech four PROPOSED throughout) · ⬛ the `[114]` constant (store
search quoted above) · ⚠ `[13]`+`[114]` still red in CI · ⬛ all #203 headliners unruled
(two-red seam · segmented binding · hit-area promotion · Sidebar-nav vs Navigations ·
xlsx retirement) · ⚠ document-row/footer placeholder hrefs (pre-existing demo links, declared
by the fix sub, not invented targets) · ⚠ visual conformance of all 6 new components is the
builder's word — no browser driven; the adopted render lane covers this from the next wave ·
⚠ `notes/_REHEARSAL-LOG.jsonl` + `notes/_dream/_GRADE-DECISIONS.jsonl` touched (shape says
`_checkin.py` appends; UNATTRIBUTED, policy remains Dave's, the standing carry).

Commit everything, reconcile path by path (30+ untracked #204 artefacts land in this commit),
read the subject back from `git log` after the commit, deliver both title lines to the
conductor, COMMIT ONLY — no push.
