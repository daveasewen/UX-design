# Brief — #232 re-stage lane (Opus): v1.0.5 WITH the bento snippet + regenerated proving zip

provenance: 232 · 2026-09-01 · conductor Fable · row W-331 (minted AT CREATION)
Dave's word, #232 opener, today: item 1 "okay sounds good" — re-stage v1.0.5 with the bento
snippet and regenerate the proving zip for his cold test. **The demo is TODAY (W-308).**

## The job

Re-stage Apollo-Spider v1.0.5 so the pack carries the bento snippet
(`knowledge/snippets/Template-dashboard-bento.reference.html` + its meta, built #231, F4
closed), and regenerate the proving zip for Dave's cold test. Follow the PROVEN procedure —
do not re-derive: reference runs are `notes/_subreports/2026-08-31-228-cut-v104.md` and
`notes/_subreports/2026-08-31-230-cut-v105.md` (the #230 cut of this same version — read it
FIRST; this lane is its re-stage, not a new cut). Dry-run twins → fingerprint on the go/no-go
page → bake → `cmp` byte-match over the whole zip.

State you inherit (verify, don't trust): v1.0.5 is STAGED PROPOSED — no ratify row; the
proving zip has VANISHED from the tree (if you can see where/why from git or the reports,
record it as a receipt; otherwise declare UNKNOWN, never guess); roster shipped HELD at 58
under `s228-D5`.

## Hard rules (release discipline — machine-enforced, packs are RELEASES)

- **Roster moves 58 → 59 by adding ONLY the bento snippet.** This is licensed by Dave's word
  in chat TODAY (#232, item 1) — the single sanctioned membership change. Any other
  membership move is W-303's lane and forbidden. Stamp roster before/after in the report.
- ⛔ `RATIFY_IDS` moved to `s228-D4` at the #230 cut — verify its state in
  `build-designer-pack.sh` and DO NOT move keys; the key moves at the NEXT cut, never by
  re-baking a ratified zip (the v1.0.3 pattern).
- ⛔ **THE BAKE IS PROPOSED, NOT RATIFIED.** Dave explicitly withheld his ratify word at #231
  and none was inferred. Produce the go/no-go page (`reviews/RELEASE-SPIDER-*` pattern,
  regenerate don't hand-edit), the fingerprint, and STOP. The conductor relays; Dave
  ratifies, separate and after his cold test.
- The baked zip is never edited after `cmp`; the dry-run twin proves the bake.
- Survey/CI reds: the known standing shape (gates job red / release+render green) is
  DECLARED, not yours to fix. A NEW red in the release half stops the lane, loudly. The #228
  cut hit six survey reds and closed five by measurement — read its report § before treating
  any red as new.
- If the bento snippet fails a pack gate: STOP and report. The snippet is ratified work from
  #231 — you package it, you do not edit it.
- Presence probe after staging: `grep -rl '\.c-bento'` over the staged pack knowledge dir
  must find the snippet — quote the matched path in the report, matched ≠ presence otherwise.

## DO-NOT-RULE

No `_rulings.json`, no W-rows, no memory writes, no git operations (the conductor commits),
no push, no canon/snippet/roster edits beyond the single licensed +1, no `_build_all.py`,
no threshold pick, no bento-RSQ answers, no ratification claim of any kind — all of those
are Dave's or the conductor's.

## Pitfalls — replayed (they have each cost a session)

- `pip install tiktoken --break-system-packages` FIRST in the cold sandbox.
- **Nothing survives a tool-call boundary** — scratch files die between calls; write+use in
  ONE call. Call wall ~178s: drive the bake's long steps in stages exactly as the v1.0.4
  report receipts show.
- Everything MOUNT-SIDE, `TMPDIR=/dev/shm` — `/var/tmp` is a trap (VM disk pressure, #227).
- A version string is not evidence — the sha256 discriminator set is (#224 forensics).
  Stamp both.
- Never pipe critical output through `tail`/`head` — a clipped key/hash lies silently.
- Counts in the report are COPIED off files/command output, never estimated; banner
  discipline throughout.

## Report

FILE the full report at `notes/_subreports/2026-09-01-232-restage-v105.md` — COUNTS
(roster before/after · files in zip · bytes · sha256 · gates red before/after · splice/
presence probes), RECEIPTS (including the vanished-zip finding or declared UNKNOWN),
REPLAY-THESE, RULING-SHAPED QUESTIONS, and the `s214-D5` wrap-handover cost line
(brief-cut/sub-cut/delta, unobservables declared). Chat STUB only: version, sha256 head,
byte-match verdict, ratify-key state, roster count, go/no-go page path.
