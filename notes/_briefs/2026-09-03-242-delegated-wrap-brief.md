# #242 — DELEGATED WRAP BRIEF (Opus wrap sub; conductor = Fable, FILL ~140K @ 32 turns, room ~10K — judgment CLOSED)

**Seat:** `/sessions/eager-wizardly-lovelace/mnt/UX-design/` (bash) = `/Users/daviewen/Documents/Claude/Projects/UX-design/` (Read/Write/Edit). Branch `master`, HEAD `7f8801f` (the #241 wrap). **Form to COPY:** `notes/_briefs/2026-09-02-241-delegated-wrap-brief.md` + the canon `knowledge/_RUNBOOK-capture-ritual.md` (steps 1a/1b/2c/2d/2f/2g + the `--wrap` call form are THERE — read the runbook, not this brief, for mechanics). `pip install tiktoken --break-system-packages` first. `TMPDIR=/dev/shm`, mount-side. Call wall ~178 s. ⚠ `.git/index.lock` was a STALE lock from a lane; the conductor moved it out. If it reappears, `mv` it, never `rm`.

## SESSION DATE = COMMIT DATE = 2026-09-03 (no split this time).

## WORKING TREE AT BRIEF TIME — ALL intended, ALL in this wrap commit (reconcile with `git status`, never blind `git add -A`)
`M knowledge/_validate_polarities.py` (lane P + P2) · `M knowledge/brain/schema/polarity.schema.json` (lane P; `SCHEMA_SHA256` moved `42f845…` → `c2c165…`, then moved again by P2's floor — read the live value) · `M knowledge/_capture_gate.py` (P2: `_boot_decompose.py` registered in MEASURERS as estimate-only) · `M knowledge/_graph-mark-observations.jsonl` · `M notes/_REHEARSAL-LOG.jsonl` · `M notes/_dream/_GRADE-DECISIONS.jsonl` · `?? knowledge/_boot_decompose.py` (lane F, `--selftest` 3 arms) · `?? notes/_briefs/2026-09-03-242-lane-P-polarity-receipt-build-brief.md` · `?? …-lane-F-boot-offload-brief.md` · `?? notes/_subreports/2026-09-03-242-lane-P-polarity-receipt.md` (+ P2 addendum appended) · `?? …-lane-F-boot-offload.md` · `?? …-V2-polarity-verifier.md` · `?? notes/_subreports/assets/2026-09-03-242-lane-P-polarity-receipt/` · this brief · your wrap report.

## THE SESSION IN SIX LINES (WHY → 1b dossier `_DECISION-HISTORY/2026-09-03-242-<slug>.md`)
1. **Boot 70,710 real** (first turn, `message.usage`, `_checkin.py`) — **710 OVER the `s241-D1` ceiling 70,000** on its first morning. Stated once, in the 2f stratum.
2. **The conductor's opener recommendation was a STALE PREMISE and was corrected in chat:** "charts wave 3" (GM §C·1 strand b, still `queued` in `_lanes.json`) — waves 3–6 all LANDED (#209/#210/#218; `ITINERARY-STATUS-2026-08-25-v4.json`: 121 GATED · 1 GAP). The assertion-propagation class, fourth recurrence. Record it; do NOT edit `_lanes.json`'s step state (conductor's, carried).
3. **PRODUCT: `W-387` POLARITY RECEIPT BUILT (lane P, Opus) — `s240-D3` enacted.** `$seed` + `retiredBy` accepted, refusals by name, pin moved, six false-red controls each have a green form (literal shapes stay red on #239's Q3 — a node may not name its own oracle; RULING-SHAPED, put to Dave, UNANSWERED). **V2 (Opus, adversarial) SATISFIED the "V2 has run" clause:** 43 attacks · expect-red 29 · caught 29 · ESCAPED 4 (the pre-existing UNRULED 241·243·245·301) · crash 0. V2 found two gaps — a retired node still named in `polarity-status.json` via a hardcoded literal at `:1208`, and the widening unfloored (`sources` back in `required` would silently kill `$seed`) — **both FIXED by lane P2**, selftest **137 arms / 0 failures**. ⚠ **#239's filed `_seam_block.sh` is STALE (pre-fix #238 block)** — V2 confirmed; lane P's re-extracted copy in its assets is the one to use. `W-387` CLOSES.
4. **BOOT DECOMPOSED (lane F, Opus, `knowledge/_boot_decompose.py --real 70710 --transcript …`):** ours **7,964 tape ≈ 11%** (MEMORY.md 3,569 · skills 1,626 · MCP blocks 1,398 · deferred names 800 · agent list 557) · harness remainder **≈62,746 ESTIMATED-BY-SUBTRACTION, units mixed by construction, never a measurement**. ⛔ **Lane F's finding "computer-use came back on" was FALSIFIED by Dave's panel screenshot** (toggle OFF since #241) — the block ships regardless, so it is HARNESS; the +1,618 vs #241 is harness drift. Record the correction in the delta + dossier; lane F's report is dated history, NOT edited.
5. **DAVE'S WORD (verbatim, three lines):** *"1. computer use has been off since the last session, if you need to do something do it. 2. yes 3. yes"* — to the conductor's numbered questions (1 turn computer-use off · 2 prune plugin-roster skills · 3 stub `MEMORY.md` to the ⛔/★★★ tier + one retrieval line). **The stub is DONE at the conductor's seat (step 3, his):** `MEMORY.md` 3,569 → **1,417 tape** (12,656 → 4,636 B, 31 lines); the whole prior index verbatim in `hook-overflow-2026-09-03-242.md`; new memory `boot-decomposed-242.md`. Skills prune = Dave's panel action; the conductor RECOMMENDED sequencing it after #243's first reading (one variable per session) — recommendation, not his word.
6. **NOT DONE:** `s203-D1` CI read-back for `a09a3ea` AND `7f8801f` NOT RUN (this push adds a third) · the 4 UNRULED escapes untouched, `W-374` at ESCAPED 4 · `BOOT_BAND_SIGMA` Dave's · diet S2/S3/S4/S6 Dave's · 119-sweep re-run owed · `_validate_wiring.py` red on #235's orphan · lane F's proposed mechanised `MEMORY.md` cap NOT built (consumer unnamed — do not build) · the surface experiment (Claude Code CLI boot floor) FLOATED, Dave's · `_lanes.json` lane-2 step `§C·1 strands (a)–(d)` still `queued` though (b) landed — conductor's, carried · the P2 floor reuses the word `SCHEMA-LOOSENED` for a tightening — new word is Dave's.

## RULINGS TO INSCRIBE — via `knowledge/_inscribe_ruling.py` ONLY, `says` = Dave's verbatim lines above, `by` Dave, date 2026-09-03, then READ BOTH IDS BACK from the store (probe: count 333 → 335)
- **`s242-D1`** — `MEMORY.md` IS A PROGRESSIVE-DISCLOSURE STUB: the boot index carries the ⛔/★★★ tier plus ONE retrieval line; every other hook lives verbatim in a dated overflow file, looked up BY NAME, never deleted (`MEMORY-ARCHIVE` #49 line). Governs: the auto-memory store (non-repo — DECLARE the home per `s191-D2`), `knowledge/_boot_decompose.py`. Evidence: chat #242 line "3. yes"; lane F report; measured 3,569 → 1,417 tape.
- **`s242-D2`** — THE PLUGIN-ROSTER SKILLS ARE PRUNED FROM THE BOOT (Dave's panel; non-repo, declared): docx · pptx · xlsx · pdf · schedule · setup-cowork · import-memory · explain-usage · cowork-plugin-management ×2 are candidates; `dave-voice` · `swiss-design-system` · `dream-pass` stay. Evidence: chat #242 line "2. yes"; lane F row 2 (1,626 tape, ~900 movable). ⚠ The sequencing (after #243's reading) is the CONDUCTOR'S recommendation — write it in the ruling's body as such, not as ruled.
If `_inscribe_ruling.py` refuses either (e.g. non-repo `governs`), STOP on that one, declare it, and carry it — do not hand-edit the store.

## GAUGE — moments stated separately, never converted
boot **70,710 real** · opener FILL 74,107 @ 3 · post-lanes-P/F **123,303 @ 19** · post-V2/P2 **134,296 @ 28** · brief-cut ≈140K @ 32 (conductor's DECLARED figure; read your own sub-cut via `_checkin.py`) · room to the 150,929 advisory ≈10K at brief-cut — ⚠ this wrap is launched INSIDE the last 15K: keep the stub ≤ 12 lines · probe: NOT planted this session (conductor skipped it — declare: `knowledge/_probe/session-242.json` ABSENT, NO QUIZ RUN) · **subs: P 225,941 · F 117,270 · V2 177,041 · P2 138,652 = 658,904 tokens (n=4)** — QUOTA, never FILL · effort band **L** · Dave's quota panel at the opener: All models 40% · Fable 61%, reset 12 h.

## BOOT-DRIFT AT THIS WRAP (lane F's reading — verify at your seat with the gate's own output)
The window rolls to #235–#241: #241's 69,092 grades **RED as a step change** (−5,707 vs the pre-diet band) → write the `boot-drift DECLARED #242` line in the new form (template: lane B's #241 report § Consequences); the 70,710 reading is GREEN in the band (1.15σ) and is graded against the ceiling only from #243's wrap. **Do NOT move any constant, band, ceiling, floor, advisory, stop line or wall.**

## CARRIES → #243 (`_CARRIES.md` § `## residual → #243`; bump ages; STRIKE keeps text verbatim; NEW at age 0)
STRIKE (receipt `s188-D2`): "THE POLARITY-RECEIPT BUILD — `s240-D3` RULED NOT BUILT, V2 gated" → BUILT lane P, VERIFIED V2, `W-387` CLOSED. NEW: ① Dave's answer to the six-controls reading (literal vs green-form) · ② #243 A/B = MEMORY.md stub ALONE — read `_checkin.py` boot first-hand, compare 70,710 · ③ skills prune after that reading (Dave's panel) · ④ `s203-D1` CI read-back ×3, capped route · ⑤ the stale `_seam_block.sh` in #239's assets — dated history, pointer to lane P's copy · ⑥ `_lanes.json` lane-2 step state stale (strand b landed) · ⑦ `SCHEMA-LOOSENED` reused for a tightening — word is Dave's · ⑧ the surface experiment (CLI boot floor), floated · ⑨ the stale-premise recurrence (charts wave 3) — a queue line must carry its landed receipt or the gate cannot see it. Everything else at its age.

## W-ROWS: close `W-387` (payload: lane P report + P2 addendum + V2 report) via the sanctioned tool; mint rows for lane P, lane F, V2 reports, `_boot_decompose.py`, and this brief (forgotten-document class). Never json.dump by hand.

## TITLE THE NEXT CHAT → `Apollo - #243: the stub's first boot — polarity V2 is live`
Deliver BOTH title lines in the stub (this one and #242's own, `Apollo - #242: first dieted morning — back to product`).

## DO-NOT-RULE
Nothing beyond `s242-D1`/`D2` above, and those only with Dave's verbatim `says`. No re-wording of any prior ruling. No constant/band/ceiling move. No memory write (done at the conductor's seat). No edit to any lane report, asset, brief, plan or review (ADR-0017). No `_build_all.py`. No render. 2c/2d/2f/2g as the runbook says, probes NAMED, 2g LAST (the index is currently STALE — 2g heals it). Banner ≤ 10 ln / 1,200 tape (S1). Stratum states 70,710 ONCE (S5). Declare every skip with its size.

## PUSH — AUTHORISED (`s207-D1`): after the wrap commit, clean tree; `git rev-parse origin/master` == HEAD; only `✅ pushed and VERIFIED` counts. CI read-back is the conductor's, carried.

## STUB back to chat (≤ 12 lines): commit hash + subject READ BACK from `git log -1` · push verdict with both hashes · rulings inscribed + ids read back (or STOPPED) · GM/LS/chain sizes from the gate's stamp · banner lines + tape vs cap · 2c/2d/2f/2g receipts · carry count via `_capture_gate._carry_items` · declared skips with sizes · BOTH title lines · anything STOPPED. Full report at `notes/_subreports/2026-09-03-242-wrap.md`.
