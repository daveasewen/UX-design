# Divvy plan — #194 conductor + window 2 (2026-08-17)

status: floated
provenance: 2026-08-17, #194 conductor, on Dave's word "you also need to conduct for another window"

## Seats

- **#194 (this window) is the SOLE COMMITTER.** Window 2 commits NOTHING — no git write
  commands, no push, no `_rulings.json`, no `_inscribe_ruling.py`, no `_build_all.py`
  (full or partial). Window 2 reports diffs + receipts back; #194 reconciles and commits.
- #194 keeps: the mono/contrast lane (s194-D1 pending Dave), CI read-back, dashboard,
  wrap + capture ritual.

## Window 2 lane (RECOMMENDED, Dave may veto): ds-034/ds-035 evidence homing

The #193 residual ②: ds-034/ds-035 evidence cites gitignored `outputs/` artefacts —
permanently unverifiable from any clone. Governed by s191-D2 HOME-OR-DECLARE: home each
artefact in-repo, or mark the evidence line `(NON-REPO: <where>)`. STALE must be 0 after.

## DO-NOT-RULE (carry verbatim into any sub window 2 spawns)

- Two-red law s151-D1 · mono ink camp s149-D1 · ink halation s175-D1/s176-D1 — untouched.
- Anything under the pending s194-D1 (label+glyph a11y nuance) — conductor's/Dave's.
- Promotion of any floated item — Dave only.
- The working tree carries ~42 regenerated derived views from #194's build run — do not
  clean up, stage, or "fix" them.

## Consequences / pitfalls (Dave #165)

- Two windows editing evidence-adjacent files can collide with #194's contrast-audit lane;
  window 2 stays out of `knowledge/_contrast_utils.py`, `semantic-colour.json`,
  `palettes/`, and the audit outputs.
- A homed artefact that is itself stale inscribes a confident false record — verify each
  artefact's content against its citing claim before homing (Memento risk).

## Report format back to #194

Chosen homes/declares per artefact · files touched (diff, unstaged) · gates driven +
exit codes · pitfalls · token spend if measurable.
