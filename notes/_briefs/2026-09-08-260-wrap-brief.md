# #260 wrap brief — cut by the conductor (Fable) at ≈167,000 real FILL (last measured 163,264 before two small lanes), 2026-09-08

**Session title:** `Apollo - #260: the budget ruling, the eight fast-follower charts; v1.0.8 cut`
**Stop line in force for THIS wrap: 180,000 (s260-D2, delegated wrap).** Conductor is under it.

## Rulings, Dave's, all inscribed via `_inscribe_ruling.py` (store 403 → 407) — READ BACK, never re-inscribe
- `s260-D1` (404) — page budget: engine core `dv-render` priced ONCE PER PAGE; PAGE_BYTES 34,816 UNMOVED; his caveat "might have to be raised at some point in the future" carried as a caveat. Words: *"11 but I feel like the budget might have to be raised at some point in the future"*. Enacted: ADR-0015 Amendment 4, `_validate_behaviour.py` `shared: true`, commit `b8796f8`. Combo 34,209 (607 spare), donut 28,478.
- `s260-D2` (405) — stop line repriced: 180,000 advisory for DELEGATED wraps, 150,929 kept inline; s214-D4's staged band enters force. Words: *"can we reprice the token ceiling"* → *"yes"*.
- `s260-D3` (406) — driven receipts become the gate for engine-drawn charts, "static OR driven, never skipped"; option (b) self-report marker REFUSED. Word: *"a"*. Enacted: lane A `223310b` (`knowledge/_drive_chart_engine.py`, `knowledge/_tests/chart-engine/_receipts.json`, gate route in `_validate_dataviz.py`), verifier V confirmed 5/5 + 2 mutations; STALE-arm reachability fixed `eee82d9` after verifier V2 found it unreachable for 12/13 pages.
- `s260-D4` (407) — v1.0.8 RATIFIED. Words: *"ratified... go for it"*. RELEASED at cut commit `193c567`, release commit `49bc5ef`, zip sha256 `9b90edcbfb5976e6b8036f21dc368f5820ad1c48a65ff4c9d9288d5f52ca7b51`, 9/9 release gates green, frozen row `c115142abcda`. ⛔ NOT PUSHED — `s133-D2` makes the push Dave's word; ask him in the wrap summary.

## Built (commits, in order)
`b8796f8` D1–D3 + A4 · `223310b` lane A receipts gate · `ff46588` showroom 66→0 · `ffb2240` version bump (6 literals) · `7d45a12` manifest · `193c567` R2: receipts+test pages+driver into ship set, `_validate_fit_physics.py` #223 treatment (gates 63→73 files / 45 gate .py) · `1459e55` manifest · `a53ed79` ratify · `e20a8f0` · `49bc5ef` RELEASE · `7ac7910` seed · `e651345` R3 report · `9a702b5` I: eight fast-followers (registry +95/−9 textual, 6 partials, 7 test pages, 8 snippets, 8 metas) · `1b8a3f1` receipts 13/13 · `4a3707a` showroom 6→0 · `1a1f516` lane fragments + `notes/_lanes/260/_CORE-REQUESTS-collated.md` · `b74576d` I report · `eee82d9` STALE arm.

**Fast followers (s259-D1) — ALL EIGHT LANDED on the engine:** scatter 24,930 · histogram 15,266 · pie 24,589 (composes dv-render-donut, no new partial) · boxplot 17,384 · bullet 17,491 · candlestick 17,920 · butterfly-h/-v 25,493 (one partial, both orientations). 104 drives, 0 pageerrors. Worst page stays Chart-combo 34,209. The s259-D1 carry is STRUCK with receipt `9a702b5`. v1.0.9 NOT cut (later; nothing bumped or baked after v1.0.8).

**Reports:** `notes/_subreports/2026-09-08-260-{A-driven-receipts,V-verify-A,R-release-prep,R2-pack-receipts,R3-release,F1-scatter-histogram,F2-pie-boxplot,F3-bullet-candlestick,F4-butterfly,I-integration,V2-verify-I}.md`. Lane brief `notes/_lanes/260/`.

## Gauge — moments separately, never converted
Boot 69,086 real (opener; UNDER 70,000 ceiling, fourth consecutive; #255's 70,127 still stands ⇒ `--wrap` will refuse again — record in the #243 form, do not move the literal). Conductor check-ins: opener 72,529 · after D1–D3 126,080 · after A+V 132,962 · before F-wave 154,252 · after I 163,264. Subs (QUOTA, never FILL): A 157,265 · V 60,167 · R 126,220 · R2 136,107 · R3 118,940 · F1 269,975 · F2 251,547 · F3 268,577 · F4 247,819 · I 175,534 · V2 81,139 · fix 62,820 = **1,956,110 (n=12)**. Sub-cut FILL: measure first-hand at your seat with `_checkin.py`.

## Ruling-shaped, collected, NOT ruled (Dave's) — carry by name
1. PUSH of 6+ release commits (s133-D2).
2. s260-D3's DRIVER scope: only donut.html measures dv-004; butterfly-v's 2.200px baseline join is provable and ungated; siblings dv-016/017/009/line-011 vacuous on an engine canvas (2nd + 3rd venue). `requiredAria`/`requiredDeclarations` pass on a JS string literal — fails OPEN (4th/5th venue of [[no-gate-parses-the-artefact]]).
3. `--full-stage` optional flag silently decides the ship set (lane R RS2).
4. `_git_commit.sh` appends a `_REHEARSAL-LOG.jsonl` row after staging ⇒ sanctioned commit can never leave a clean tree, and `--release` requires one (R3).
5. `_validate_screen.py` clobber, still unrepaired; `test_gates.py` copytrees 5 GB, cannot run in sandbox.
6. Drawn donut gap 2.109 (rounding); 2dp emission patch measured 2.109→2.194 (F2), filed in collated core requests, NOT applied; pie has no inner edge — dv-004 judged at a magic 0.35·ro (ruling-shaped).
7. Core requests collated (furniture opt-out ×3, zero-clamp ×3, numeric x, valueLabel, mirrored-axis label, 2dp radial) — none enacted.
8. F2 moved Chart-boxplot's ruled exemplar data (outliers 8 and 3) because s249-D4 has no null; F4 corrected meta rot since #95; F2 broke the checkout fence on its own two metas (harmless, measured by I).
9. a11y gate silently stopped counting 37 sub-24px marks (I §9); histogram bins cannot honour s116-D1 24px floor and stay contiguous.
10. `_validate_behaviour.py` rewrites tracked `_BEHAVIOUR-GATE.md` on every run (absorbs rather than reds).
11. Boot-ceiling arm on one remaining breach (#255) — unchanged.
12. Dave asked about Cowork "run in the cloud" setting — answered in chat (keep local for this repo), not a ruling.

## Declared skips
`_build_all.py` not run · `_validate_screen.py` not run · `test_gates.py` not runnable · no memory write at the sub seat (conductor writes the hook) · `_CARRIES.md` never read whole.

## Wrap instructions for the sub
Run `knowledge/_RUNBOOK-capture-ritual.md` in full: `_checkin.py --wrap` (expect refusal on #255; commit in the #243 form), GM/LS rolls, ★ LATEST banner ≤ s241-D2 cap written SHORTER not QUIETER, ⏱ delta with the full gauge above, strata in `notes/_GAUGE-LOG.md` — ⛔ a discharge line must NOT use a `#### <date> #<N>` key (it makes the GM stratum unrollable; #259's did — repair by renaming that heading if the runbook allows, else name it), `_CARRIES.md § residual → #261` (strike s259-D1 with receipt; add items 1–11 above), `_DECISION-HISTORY/2026-09-08-260-*.md`, `_state.json` via `_state.py`, `_gen_chain.py` + `--check`, title line for #261, commit via the sanctioned path, NO push. Reply with both title lines and a ≤600-token plain-prose summary for Dave.
