# #239 — COMMON RULES FOR EVERY LANE (read first; each lane brief points here)

**Seat:** repo at `/sessions/keen-serene-johnson/mnt/UX-design/` (bash) = `/Users/daviewen/Documents/Claude/Projects/UX-design/` (Read/Write/Edit). Branch `master`, HEAD `7104fc9` (the #238 wrap, pushed; CI run #478 RED — first blocker `ASSERT-009`, being re-based by the chore lane in parallel). Scratch: `/sessions/keen-serene-johnson/mnt/outputs/<lane>/` (nothing under `/dev/shm` survives a call boundary; the mount does).

**Rules:** everything in `notes/_briefs/2026-09-02-238-COMMON-lane-rules.md` stands unchanged — the NEVER list (no git of any kind; no writes to `_rulings.json`, `_state.json`, `_CARRIES.md`, `GOOD-MORNING.md`, `_LIVE-STATE.md`, `_CHAIN.md`, memory; no `_build_all.py`; no `_gen_chain.py`; no edits to dated history — ADR-0017), the register (quote rulings from the store; FLOATED never "the default is"; UNPROVEN is a word, never a guess — ADR-0016), the sandbox notes, the PITFALLS section (Dave #165, owner per row), the filing form (`s218-D7`) and the ≤12-line stub. Read that file; it is 2.4 KB.

**Today's licence (Dave, #239 opener: "Okay rebase, do both"):** the ASSERT-009 re-base is approved; the fix lane's exit test is V's own harness re-driven.

**Filing:** `notes/_subreports/2026-09-02-239-<X>-<slug>.md` from `_TEMPLATE.md`; assets under `notes/_subreports/assets/2026-09-02-239-<X>-<slug>/`. Token spend line: `UNMEASURED — no message.usage at a sub's seat`.
