# WRAP BRIEF — #215, 2026-08-22 (conductor → Opus wrap sub)

**You are the delegated wrap sub for session #215.** Read `knowledge/_RUNBOOK-capture-ritual.md`
FIRST and run the ritual as written — this brief is the session's content, the runbook is the
procedure. The conductor commits nothing further; YOU run the wrap commit per
`knowledge/_RUNBOOK-git-commit.md` (script, fresh printf msgfile, `SESSION_N=215`, subject read
back from `git log`). Do NOT push — the conductor pushes after reading your report.

## The session in one line
**#215 was the day the desk got cleared and CI went green after twenty red runs:** five rulings
inscribed (`s215-D1`…`s215-D5`), eight commits all pushed, the red-streak onion peeled at cause
layer by layer, Library v2 became the index in Swiss dress, and a receipt's false claim was
caught by driving before it calcified.

## Rulings inscribed this session (store is authority — read `knowledge/_rulings.json`)
- `s215-D1` W- ID space widened: 3 digits + grandfather; next mint W-100 (now used: W-100…W-106).
- `s215-D2` Itinerary v3 adopted; row 124 honest GAP; 27 off-ratchet advisory.
- `s215-D3` P8 ruled reading 2 — rule 5 model-conditional; MODEL-ROUTING.md amended same commit.
- `s215-D4` Level ladder (Foundations · Tokens, then Primitives-if-needed / Element / Pattern /
  Block / Shell / Template) + Usage/Type tabs.
- `s215-D5` Five findability decisions all yes (v2 replaces index · status facet · thumbnails ·
  related links · JSON index).
- Also Dave's in chat, enacted not inscribed: W-90 radius bind (border-radius/surface, Kpi-tile
  precedent) · "always real fixes never patches, they just get lost" (conductor's memory hook
  updated) · push rule already covered by s207-D1 (no new inscription needed, verified).

## The eight commits, all pushed, in order
`9aba453` ID widening + two home repairs · `997e40c` canon regen + s215-D2/D3 enacted ·
`0272996` prefixer :where() 48→0 + G2 built · `2a61618` help gates ×2 · `46eb387` Library v2 IA
+ taxonomy v2 · `1b08f68` Swiss chrome + doc-row directory class fix + memento index ·
`4108f16` help gate on drive script · `e645df2` W-90 bind done properly (surface slot).
**CI: #413 GREEN on `e645df2` — read back in chat, first green in twenty runs.** The onion:
[45] canon determinism → help gates ×3 → stale memento index → radius/wiring/forks →
ds-018 dangling var. Every layer fixed at cause, receipts in the commit messages.

## Sub economics (s168-D2 wrap line + s214-D5 cost field — BOTH MANDATORY)
Work subs this session: **4, all Opus** — prefixer/G2 185,581 · library IA 232,053 · taxonomy
research 231,495 · Swiss restyle 156,177 = **subs 805,306 tokens (n=4)**. You are the 5th
(wrap). Write the s214-D5 delegated-wrap cost field per the runbook template: brief-cut ·
sub-cut · delta · replay, each measured or `unobservable (<reason>)`. **Your row is n=2 of the
s214-D4 staging** — note in the banner that the advisory re-derivation condition is now MET and
the ~190K advisory awaits the conductor's opener announcement at #216 (do NOT put it in force
yourself — announcing is an opener act).

## Gauge (measure fresh at your wrap; these are the session's declarations)
- boot 60,248 real — ⛔ 2,345 OVER the s208-D1 band (55,595–57,903), DECLARED at opener, not
  corrected. Band read from `_gauge_tokens.py:178-179`.
- FILL crossed the 150,929 advisory AND the 200,000 working line, both DECLARED live in chat
  (251,090 at the crossing check-in). Recall probe s214-D3: planted at opener, quizzed blind
  at ~251K — **GREEN 4/4** (`knowledge/_probe/session-215.json`), band conditions held:
  post-crossing work was mechanical/receipt-backed; rulings all predate the crossing.
- Your own `_checkin.py` figure at the wrap is the FILL of record — state it, never mine.

## Residual → #216 (type them per the carry contract; nothing here is ruled)
1. ⬛ REVIEW-213 tranche sittings — Dave's, unchanged, 172 questions (row W-99i).
2. ⬛ PROPOSED-FOR-DAVE sets from today, all awaiting his eye: 16 usage-group assignments +
   Charts→Data-display fold (library receipt §4) · status derivation rule (§6) · grey-4
   decorative numerals 2.01:1 · related-cluster membership · empty Foundations/Tokens tiers ·
   the 12 PFD rows over 41 components in taxonomy v2 §2.
3. ⚠ G2 scope: light mode / 2 widths only — dark + 3 themes + script states UNDRIVEN (W-101).
4. ⚠ Priced-not-built: thumbnail staleness leg · Swiss-structure bite · contrast selftest leg ·
   regen-serial class fix (3rd conductor instance) · `mask_comments` dedup gate.
5. ▫ Prefixer :is() cosmetic bloat — the "matching bug" was DISPROVEN BY DRIVING (correction
   appended to the prefixer receipt, computed-style table, both eras); tidy-up is a low row.
6. ⚠ Foreign-artefact dependency: chromium libs at `/var/tmp/chromelibs-s213e2` — re-extract
   recipe now in `_RUNBOOK-render-verify.md` fourth stratum (#215).
7. ⚠ CI read-back owed at #216 for the wrap commit's own push (conductor pushes after wrap).

## DO-NOT-RULE (the fence, s183-D1 lineage)
No writes to `knowledge/_rulings.json` (READ-ONLY at a wrap; its only writer is
`_inscribe_ruling.py` and no ruling happens at a wrap). No constant, threshold, stop line,
band, or advisory changed — the s214-D4 in-force switch is the CONDUCTOR'S OPENER ACT at #216,
you only record that its condition is met. No Dave-owned row closed. No memory-store writes
(NON-REPO; the conductor discharged the memory step in-session — declare it as his act, do not
verify). No push. Banner discipline (s214-D6): SHORTER, never decide-what-to-drop; aim
`_CHAIN.md` at its ~10–12K era; roll-to-archive preserves priors verbatim. Rebuild
`_memento-index.json` LAST (step 2g, content-compared BLOCKING gate). Titles via
`_gen_titles.py --session 215`, never hand-authored. Read the SUBJECT BACK from `git log`
after the commit and put both title lines in your report — the conductor relays them in chat
(a delegated wrap cannot deliver the rename itself).
