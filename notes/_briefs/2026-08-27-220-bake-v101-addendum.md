# #220 sub brief — v1.0.1 bake ADDENDUM: packed retrieval fix at cause + KeyError triage

**Model: Opus.** The v1.0.1 stage is prepared 6/6 green (`notes/_subreports/2026-08-27-220-bake-v101.md` — READ IT FIRST, especially its RED FLAGS and REPLAY-THESE). Two residuals block an honest bake:

1. **Packed `_memento_search.py` points at `knowledge/_build_all.py`, which the pack does not carry — Gumdrop retrieval cannot start.** Reproduce from the fresh stage first. Fix AT CAUSE: decide whether the right cause-level fix is (a) the reference is vestigial in the packed context → the generator/packaging decision strips or repoints it mechanically, or (b) the dependency is real → the manifest closure carries it. Follow the same pattern as the report's ① `import_closure` fix — the DECISION travels, never a hand-patched file. Prove: from a fresh stage extraction, `_memento_search.py "<any query>"` answers against the packed stores, and `--fetch` works on one id.
2. **Two packed compliance builders raise `KeyError: 'verification'` at import (pre-existing in v1.0.0).** Triage: reproduce, find the cause, and EITHER fix at cause if the fix is contained, OR add them to the manifest's honest exclusion table with the probe's own reason (the s219-D9 pattern — Dave preferred honest exclusion over shipped refusals). Do not leave them silently broken. If exclusion changes the ruled gate count, FENCE it exactly as the prior report's ③-bonus did and name it as ruling-shaped.
3. Re-run after fixes: `_gate_pack_imports.py` on the re-staged pack (expect MISSING-LOCAL 0 and note any remaining import-time execution errors by name) · the release gates the prior report ran · the fresh-stage render proof (bento 44/44, gallery 30/30, 0 broken) · the cold-start drive INCLUDING retrieval this time. Re-measure the stage (bytes/entries/sha) — the prior report's figures supersede only by measurement.

## Fences — identical to the main bake brief (`notes/_briefs/2026-08-27-220-bake-v101.md` § DO-NOT-RULE)
No git, no memory, no `_rulings.json`/`_state.json`, no promotions, no touching frozen v1.0.0 artefacts, no `_build_all.py`, no final `--release` (conductor's, at the landing commit). Regions: the release generators/gate, the stage, your filed report.

## Pitfalls (Dave #165)
- A retrieval "fix" that works in the repo but not the fresh stage is the exact green-in-repo/broken-in-pack class — only the stage proof counts.
- Stripping a vestigial reference that is actually load-bearing in some pack path = silent breakage: grep the packed tree for every consumer before choosing (a).

## Report
FILE at `notes/_subreports/2026-08-27-220-bake-v101-addendum.md`: reproduced-red → fixed-green evidence per item, re-measured stage figures, updated conductor REPLAY-THESE (exact bake commands, superseding the prior report's if changed), RULING-SHAPED QUESTIONS. Chat stub ≤6 lines.
