# #235 — DELEGATED WRAP BRIEF (Opus wrap sub)

*Written 2026-09-02 by the Fable conductor at FILL 171,125 real — PAST the 150,929 advisory by 20,196, inside the s214-D1 conditional band; recall probe quizzed 4/4 GREEN (`knowledge/_probe/session-235.json`). Mechanical wrap only. Canon: `knowledge/_RUNBOOK-capture-ritual.md` — READ IT and run every step; `knowledge/_RUNBOOK-git-commit.md` for the commit + `--push`.*

## SESSION #235 — THE FACTS (record these; verify each against the repo, never from this brief alone)

1. **Title:** `Apollo - #235: the v1.0.6 build starts at l1 — the receipt and the entry gate`. One morning, Fable conductor + ONE Opus build sub (L1) + this Opus wrap sub.
2. **Two rulings inscribed via `_inscribe_ruling.py`** — `s235-D1` (the receipt is keyed on a CONTENT HASH, not filename/slug; rC Q1 closed) · `s235-D2` (the new gate is `knowledge/_validate_receipt.py`; `_validate_screen.py` chains into it first). Store 309 → 311. Read both ids BACK from `knowledge/_rulings.json`; never retype.
3. **L1 BUILT and DRIVEN.** Brief `notes/_briefs/2026-09-02-235-L1-receipt-gate-brief.md` (`W-344`, minted via `_state.add`). Report `notes/_subreports/2026-09-02-235-L1-receipt-gate.md` (COUNTS findings 9 · ruling-shaped 3 · UNPROVEN 4 — copy off the file). New: `knowledge/_validate_receipt.py`, `knowledge/gen_provenance_receipt.py`, `dashboards/international-banking-dashboard.regen-v2-receipt.html` + `.spec.json`, report assets. Modified: `knowledge/_validate_screen.py` (gate_receipt, step 0, `--receipt-strict`), `apollo-spider/skills/check-with-gates/SKILL.md` (one row). **Conductor DROVE all three at his own seat:** regen-v1 → `FAIL:NO-RECEIPT` rc 1 · regen-v2-receipt → `PASS (+1 UNPROVEN)` rc 0 · one-byte mutation (`2,450.00`→`3,450.00`, page offset 65,526) → `FAIL:HASH-MISMATCH Stat-card#markup … offset 184 of the region` rc 1.
4. **Side effect, declared:** running the gate rewrote `knowledge/_SCREEN-GATE.md` + 7 `knowledge/_screen-gate/*.canon.md` and minted 2 new `_screen-gate/international-banking-dashboard.regen-v*.md`. These are GENERATED by the gate; commit them as the gate wrote them. Whether the two dashboards STAY in the index is Dave's (sub question 3).
5. **`dave-voice` skill REVISED on Dave's word (NON-REPO: Claude account skill store)** — "prose over lists" retired; landmarked chunks, numbered lists for decisions, HTML-not-md for long-form. Memory updated at the conductor's seat (`dave-voice-skill-and-boot-levers.md`, `MEMORY.md`).
6. **Memory compaction ran at the conductor's seat:** `MEMORY.md` 20,572 → 19,708 B measured (−864, five hooks trimmed, prior text verbatim in `hook-overflow-2026-09-02.md`). Step 3 (memory) is DONE — this sub writes NO memory.
7. **Gauge — moments separately:** boot **75,294 real** (first turn, OUTSIDE the s208-D1 band 55,595–57,903; ordinal not claimed; no constant moved) · opener FILL 95,910 · lane-seam FILL **171,125 real @ 55 turns**, PAST advisory by 20,196, declared · probe planted 07:35:50Z, quizzed 4/4 GREEN at the seam · **subs 193,963 tokens (n=1)** harness-reported, UNMEASURED at the sub's seat · effort band **L**. Your own spend: UNOBSERVABLE, declare.
8. **Not done:** `knowledge/_tmp/` (now 8 spent inscription inputs) UNTRACKED, un-added, un-ignored — declare, do not tidy · `_tests/test_gates.py` REFUSED in the sub's sandbox (`ENOSPC`) — COULD-NOT-ASK, CI is the verdict · no render · no `_build_all.py` · `W-340`/`W-342` NOT closed (their condition names the CONDUCTOR reading REPLAY-THESE; only the sub did) · `W-344` NOT closed (conductor has not read the report's REPLAY-THESE) · the #234 commit `0f77b1a` was UNPUSHED all session (push refused on a dirty tree).

## STEPS

Run the capture ritual per the runbook (2a–2g, stamp on the artefact to a fixed point, index rebuilt LAST). Commit via `knowledge/_git_commit.sh` with a FRESH `printf` msgfile; **read the subject back** from `git log -1 --format=%s`. Then `bash knowledge/_git_commit.sh --push` (s207-D1, conductor's judgment = YES) and quote the result line verbatim — only `✅ pushed and VERIFIED` is a push; a refusal is quoted, never retried blind. Deliver BOTH title lines (this session's + the generated next-chat title) in your stub.

## residual → #236 (add by ADDITION to `_CARRIES.md`; strike nothing without an s188-D2 receipt)

⬛ **L2 — the behaviour address (s234-D5)** is the next beat · **three ruling-shaped questions from L1, Dave's:** (1) when `NO-RECEIPT` starts BLOCKING — 7/7 screens red today, ships as `--receipt-strict` ratchet · (2) composed-page CSS: the verbatim `<style>` splice carries 191 hex and reds `_validate_compose`, while the route that passes compose is the un-receiptable hand-written one · (3) whether the two dashboards stay in the screen-gate index · **UNPROVEN, priced:** receipt across a PACK CUT reads as invention not staleness (~2K tk pack-aware line) · retrievalSet null (rC Q4) · page never rendered (~3K tk) · the sub-report's REPLAY-THESE unread by the conductor (`W-344`/`W-340`/`W-342` close on it) · every prior carry at its own age.

## DO NOT RULE

No new ruling; no re-wording of any `s2xx-D*`. Do not close `W-340`/`W-342`/`W-344`/`W-338`/`W-343`. Do not decide the ratchet date, the composed-CSS route, the screen-gate index membership, rC Q3/Q4, role-name words, row height. No memory writes. No constant/band/floor/stop-line/roster/version move. Do not tidy `knowledge/_tmp/`. Do not read `GOOD-MORNING.md` top-to-bottom — the ritual tools address it.

## PITFALLS (#165)

- `tiktoken` first or every gate reds as an artefact. `TMPDIR=/dev/shm`; `/var/tmp` is a trap; VM disk `/sessions` at 78.9%.
- The subject-assert has never been seen to fail — READ THE SUBJECT BACK yourself.
- An ahead-count against `origin/main` is EMPTY and lies "synced" — the branch is `master`.
- Banner figures are PARSED — use the literal ruled phrasings (`subs N tokens (n=…)`, `FILL … real`).
- Reconcile every path in `git status` before staging; never a blind `git add -A`. The generated `_screen-gate` files ARE part of this commit.
