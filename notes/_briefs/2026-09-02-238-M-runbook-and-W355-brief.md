# #238 — LANE M: two of MINE — the render-runbook SEVENTH stratum (two causes) and the `W-355` deadlock fix

Read `notes/_briefs/2026-09-02-238-COMMON-lane-rules.md` first.

## PART 1 — `knowledge/_RUNBOOK-render-verify.md`
Ground: the runbook's strata in order (the SIXTH is the newest ruled stratum: everything mount-side, `TMPDIR=/dev/shm`, VM disk full, `/var/tmp` a trap); lane T finding 11 (`fonts.conf` stored with another seat's mount path, `determined-affectionate-euler` — fixed at that seat by generating a per-seat fontconfig in the same bash call); the earlier "fifth-stratum hollow-directory" cause. Deliverable: a SEVENTH stratum ADDED (never a rewrite of prior strata) naming BOTH causes and the per-seat generation as the fix, and — the proof — a render DRIVEN from THIS seat following the new stratum verbatim: one PNG of `_REVIEW-tensions-schema-2026-09-02-v1.html` at 1440 light, one at 390 dark, with the canvas font probe (never `fonts.check()`), one heavy call at a time. Record the seat name from `pwd`.

## PART 2 — `W-355`
Ground: `python3 knowledge/_state.py` row `W-355` (title, body, `closes_when` — quote it), `knowledge/_git_commit.sh` around line 347 (R3 CHAIN OVERTAKEN, `SESSION_N`, `SESSION_ACK`, T3), `knowledge/_RUNBOOK-git-commit.md`, ritual step 5b in `knowledge/_RUNBOOK-capture-ritual.md`. The row names two legal fixes; PICK the one that adds a DECLARED form rather than removing a check, say why, implement it in `_git_commit.sh` BY ADDITION, and prove it on a FIXTURE: copy the repo's `knowledge/_git_commit.sh` + a minimal `git init` under `/dev/shm/w355/` with a fake `_CHAIN.md` and a `_HANDOFF-239-x.md`, drive the script there with and without the new declared form (green / red / and the old R3 still red for a real overtake). ⛔ Never run it against the live repo's `.git`.

## DELIVERABLES / COUNTS
**stratum added 1 · renders 2 (ok n) · W-355 fix arms n (behave n/n) · files changed: list.**

## DO NOT RULE
The runbook's prior strata · any constant, band or floor · `W-355`'s `closes_when` (it closes only when a real post-wrap handoff commits through the script — say so, do not close it).

## PITFALLS
1. Fixing the runbook without driving it — the proof is the PNG from this seat. 2. A fixture that does not reproduce the deadlock first (control arm RED before the fix). 3. Leaving a `git` process or lock behind under `/dev/shm`.

## FILING
`X = M`, slug `runbook-and-W355`. Stub back to chat per COMMON.
