# #251 delegated wrap brief — run the FULL wrap ritual

**Model: Opus 5 (rule 7). Conductor: Fable (#251), FILL 144,803 real at the brief cut — under the 150,929 advisory, delegated by rule 7. Recall probe NOT planted (declare, 0 claims). Boot 69,321 real — state ONCE, in the stratum. Quota panel at opener: all-models 29% · Fable 53%.**

## 1. RULINGS — TWO, Dave's, ALREADY INSCRIBED by the conductor via `_inscribe_ruling.py` (store 371 → 373, parsed). Do NOT re-inscribe. Read `s251-D1` and `s251-D2` back from `knowledge/_rulings.json` and quote `says` verbatim in the stratum.
- `s251-D1` — DP-08 resolved as A+B: the header status STRIP is the orientation layer, the Needs-attention card is the detail; strip items point at their detail (a chip may anchor-link to the card); one shared count and label; Undrawn/Available-overdraft is strip-only. Dave: *"I think its A with the needs attention card"* / *"the thing is I like the strip, its very important, this is a good pattern, but it need the coordinate with the needs attention pattern. So the user sees the strip and it orientates them to pay attention towards the detail"* / *"this is good, teh user might even click on teh chip and it anchor links to the needs attention panel, this makes sense to me"*.
- `s251-D2` — the one shared count wording is the strip's: *"14 payments · 5 need you"* (card's "5 of 14 need you" retired).

## 2. WHAT #251 WAS (WHAT here; WHY in `_DECISION-HISTORY/2026-09-06-251-strip-orients-card-details.md`, which you write)
- Opener: chain read (#250 → #251), `_checkin.py` boot 69,321 real (UNDER the 70,000 ceiling — the second post-diet reading under; arm still RED on #247/#248/#249 — grade, move nothing). #250's push had landed (`0b0e6c6` level with origin at the opener). Dave asked for the three open items in plain prose, then *"DP-08 A+B. -- show me the varients"* — the #249 renders (current/A/B2/C) were presented from `outputs/w2-debt/dp08/`. Conductor named four rubs against "A with the Needs-attention card" (Undrawn is not a DP-10 action · double-reporting · four-tile band · B2 rail order). Dave answered with the pattern (`s251-D1`).
- **Lane PROBE (Opus, 119,399 quota):** `notes/_briefs/2026-09-06-251-dp08-premise-probe-brief.md` → `notes/_subreports/2026-09-06-251-PROBE-dp08.md` (findings 10 · ruling-shaped 4 · UNPROVEN 3). P1 four-tile row TRUE — band line is 4/4/4 (page rule out-specifies canon), mutation caught. P2 B2 rail TRUE at 1440 (y678.6 vs 913.7), two-up at 1100 equal; below 1440 the card is at 1112/1242 so the ANCHOR carries the relationship. P3 byte cap TRUE — prototype 1,753, page 14,801 of 34,816; page cap unreachable by one legal partial. DEFECT: option A's chip pointed at `#p2` (Payments tab) and the card had no id. Double-report: A+B2 22/7 vs 20/6; the two copies DISAGREED on the count (→ `s251-D2`). Playwright bootstrapped without root (116 MB headless-shell + libxdamage1, 4 calls).
- **Lane BUILD (Opus, 172,423 quota):** `notes/_briefs/2026-09-06-251-dp08-build-brief.md` → `notes/_subreports/2026-09-06-251-BUILD-dp08.md` (findings 9 · ruling-shaped 3 · UNPROVEN 4). Canon: `knowledge/snippets/Template-dashboard-bento.reference.html` +255 lines (strip · four tiles · card id `dp08-na` · rail reorder · promoted head row) · NEW `knowledge/canon/dp08-anchor.js` (1,821 code-only) · `template-dashboard-bento.meta.json` +9 (`consumes`) · `component-types.json` · `_BEHAVIOUR-GATE.md` regenerated. Gate exit 0, page 14,869 / 34,816; `gen_component_partials.py --check` OK; three `--mutate` modes as designed. Six renders 0 errors: tiles 341×4 / 256×4 / 186×4, rows 1 [4], maxWdiff 0, void 0, dark identical; NA `{976,516.5,432,295.3}` @1440, two-up 295.3 = 295.3 @1100. Anchor drive: scrollY 0→235, active `dp08-na`, highlight gone by 1400ms, reduced-motion clean. DP-20 **26/9** above fold at 1440 (canon page shorter than the specimen), 20/6 at 1100/820 — reported, NOT trimmed. Lane stopped on two: canon has no `#p1` tabpanel (proved the equivalent); canon never had "Undrawn facilities" — it mapped `Available overdraft` and PASTED the specimen's `$120.0m` / `$2.7m` / `40% drawn`.
- **Lane V verifier (Opus, 129,747 quota, cold):** `notes/_subreports/2026-09-06-251-V-dp08-verifier.md` — 26 rows: 17 CONFIRMED · 3 REFINED · 1 CONTRADICTED (cosmetic: BEFORE `active` was `div.tpl-bento-body`, not BODY) · 5 NEW. NEW 21: remove `id="dp08-na"` → chip dead, BOTH gates still exit 0 — the strip→card correspondence `s251-D1` rules on is UNGUARDED (V-MUT-5's shape). NEW 22: all three `--mutate` modes hard-code `dv-behaviour`, cannot bite `dp08-anchor`. NEW 23: declaring `dv-legend` in `consumes` with no payload: byte gate exit 0, only `gen --check` RED. NEW 24: 3 columns → rows [3,1], voids 920/693/507 — `s249-D5` breach caught by NO gate. NEW 25: demo-chrome resize listener uncharged (pre-existing). Currency: 7 `£` vs 2 `$`, both the lane's; `Available overdraft`→Undrawn mapping LEGITIMATE (`s247-D3` names the tile); `40% drawn` vs canon's `None used` = the INVENTED FACT. 820 legibility PROVEN light+dark (overflow 0, clip 0, PNGs read). All adversarial edits reverted.
- **Conductor's one hand edit:** restored canon's own facts in the template — strip plain item `Available overdraft £25,000.00 · None used · Limit reviewed 4 Aug`, head row `£9,840.55`; `$` count 0 outside comments/meta; both gates re-run green; six renders re-run, strip h24 one line at every width, NA boxes unchanged.
- **Commit `fc1209b` PUSHED** (`0b0e6c6..fc1209b`, master level with origin): the four canon files + `dp08-anchor.js` + `_rulings.json` + two briefs + three reports + build assets. A 0-byte `.git/index.lock` (15:07, left by a lane) blocked the commit; `rm` was "Operation not permitted" from the sandbox; Dave enabled deletion via the Cowork prompt and it was removed — FIRST time a lane-left lock was cleared from the sandbox (previous sessions declared it untouchable). `s203-D1` CI read-back for `fc1209b` NOT done — owed.
- Subs total **421,569 quota** (n=3). 0 Fable lanes.

## 3. STILL DAVE'S (carry, by addition to `_CARRIES.md` § residual → #252): **DP-20 seventh (ninth) carrier — 26/9 at 1440 vs budget three** (probe RSQ 4, build reported not trimmed) · **the strip→card link guard** (verifier NEW 21 — V-MUT-5's shape, second instance) · **`--mutate` modes bite only `dv-behaviour`** (NEW 22) · **no gate catches a ragged row** (NEW 24, `s249-D5` unguarded) · the `consumes` guard (V-MUT-5, now with NEW 23 beside it) · legibility unmeasured (ADR-0015 A1 vs A3) · `_validate_composition.py --selftest` broken since #247 · `s249-D3` R/A signal (W4) · `s249-D6` item 6 visually · META's 7 RSQs · DESK's 7 RSQs · W3…W8 · 12 members unproven with the release · themes · C9 (3 reds) · Q8–Q16 · radius · cold test v1.0.6 · [18] · 4px · parts-with-addresses · dream pass 11's four · midnight-wrap stamp (#241, age 10) · strays `_tools/` (92 KB) + `_screen-gate/dashboard.md` (7,663 B) + NEW untracked `notes/_subreports/assets/2026-09-06-251-V-dp08-verifier/` (inspect; commit if evidence, declare size) · CI read-back `s203-D1` for `fc1209b` · the ceiling arm (RED on #247/#248/#249; #250 69,293 and #251 69,321 both UNDER — grade, move nothing) · MEMORY.md 229 tape over its advisory cap.

## 4. NEXT: the DP-20 carrier question + the unguarded strip→card link, then W3. Next title: `Apollo - #252: DP-20 carriers + the unguarded link`.

## 5. THE RITUAL, WHOLE — steps 1→5 per `knowledge/_RUNBOOK-context-gauge.md` and the wrap runbook it names (grep; read the runbook, not the hook). `_checkin.py` reports the retrieval index STALE — step 2g rebuilds it LAST. `--wrap` (expect REFUSED on the ceiling arm — then commit in the #243 form and SAY SO). Subject READ BACK from `git log`; both title lines returned to the conductor. **PUSH the wrap commit** (Dave said push this session; if it fails, declare). Declare every skip WITH ITS SIZE. Memory: the CONDUCTOR writes it — declare 0 bytes at this seat. Do NOT rule anything in §3. Do NOT edit lane reports (ADR-0017). Do NOT run `_build_all.py`. The two jsonl files dirty (`_REHEARSAL-LOG`, `_dream/_GRADE-DECISIONS`) — inspect `git diff`; commit if today's tool output, else declare. Batch independent reads; Edit, never Write, on existing files. Sandbox paths: repo at `/sessions/bold-focused-hopper/mnt/UX-design/`; `pip … --break-system-packages`; tiktoken already installed.

Return under 350 words: `_rulings.json` count, commit hash + subject read back, push result, `--wrap` result, `_CHAIN.md` size line, declared skips, both title lines.
