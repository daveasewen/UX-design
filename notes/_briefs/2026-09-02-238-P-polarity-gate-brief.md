# #238 — LANE P: THE POLARITY GATE — build the one home, the five refusals, and prove them on the real rows

Read `notes/_briefs/2026-09-02-238-COMMON-lane-rules.md` first. Governing rulings: `s238-D1` (hyper-relational node is the home; N typed parties; typed out-links; pairwise edges DERIVED never authored), `s238-D3` (status DERIVED with a clock; Dave rules only the four ask-whens), `s238-D4` (rename tension → polarity before anything is generated), `s238-D5` (declare only defaults that bent from the conservative side), `s238-D6` (link types resolvedBy / explainedBy / challengedBy / touches; untyped refused), `s238-D7` (the five refusals, in the build and the commit gate, mutation self-test on the real rows). Also `s237-D1` (five grades), `s237-D2` (obligation outranks), `s237-D5` (explainedBy one-directional, advisory), `s237-D9` (HELD derived never typed — same class).

## GROUND FIRST
- `notes/_subreports/2026-09-02-237-T-tensions-schema.md` — findings 2, 3, 4, 5, 7, 8, 9; its `assets/…/open-tensions.json`, `_derive_sort.py`, `tension-sort.json`.
- Source rows (FROZEN, read-only): `notes/_subreports/assets/2026-09-02-236-R1-principles-survey/tensions.json` (30) and `principle-register.json` (145).
- `_PLAN-designers-brain-2026-09-02-v1.html` §P1 — does it NAME a home for the brain? If yes use it; if not, the home is `knowledge/brain/` — DECLARE which in the report.
- `knowledge/_build_all.py` docstring (steps, order, how a step registers; DO NOT RUN IT) and `knowledge/_git_commit.sh` (how gates are wired; DO NOT RUN A COMMIT). `knowledge/_inscribe_ruling.py` is the model for a writer that proves it did not reformat.

## DELIVERABLES
1. **The home.** `knowledge/brain/principles.json` — seeded ONCE from the R1 register byte-for-byte (sha256 receipt in the report; no edits). `knowledge/brain/polarities.json` — the 30 rows MIGRATED: `id`, `parties: [{ref, role, note?}]`, `mediating_variable`, `links: [{type, ref, quote?}]`, `sources`. `side_a/side_b/apollo_touch` cease to exist. `pr-info-scent` → `pr-information-scent` (finding 7) with a receipt line. Parties may reference: a principle, an obligation, a `_rulings.json` id, or a DECLARED stub in `knowledge/brain/stubs.json` (finding 8's phrases) — a stub is a node with an id and the verbatim phrase, nothing else.
2. **The schema.** `knowledge/brain/schema/polarity.schema.json` (JSON Schema, `additionalProperties: false`): no `status`, no free-text judgement field of any name; every link `type` ∈ the four; `parties` minItems 2.
3. **The validator-generator** `knowledge/_validate_polarities.py`: (i) PARSE in the consumer's grammar; (ii) resolve every `ref` against principles + stubs + `_rulings.json` ids; (iii) the FIVE REFUSALS of `s238-D7`, each LOUD, NAMED, rc≠0, nothing written on refusal; (iv) DERIVE `knowledge/brain/_generated/polarity-status.json` (settled-by-obligation / resolved / open, `generated_at`, rule printed beside counts — port `_derive_sort.py`'s rules K and D), `polarity-edges.json` (pairwise view for the principle↔principle cases, header `GENERATED — do not hand-edit`), and `defaults-declaration.txt` (the `Defaults used:` lines per `s238-D5` — a row is declared only if its proposed default bent from the conservative side; if `open-tensions.json` does not carry which side is conservative, print the rule you used and mark those rows UNPROVEN); (v) `--check` = content-freshness of the generated files (compare CONTENT like `index_freshness_check`, never mtime); (vi) a writer that proves non-reformatting by reconstruction, as `_inscribe_ruling.py` does.
4. **`--selftest`**: control + one arm per refusal (dangling ref · untyped link · judgement field · an authored file at the generated path · a typed `status`) + extras (self-party · duplicate party · 1 party · link to a superseded ruling id · unicode id · circular polarity→polarity link) — each arm driven THROUGH THE SAME ENTRY POINT the build and commit gate call, on a copy of the REAL rows under `/dev/shm`, each arm must go RED by its name. Print the arm table.
5. **Wiring**: a step in `_build_all.py` (declare its position against the regen serial — it is a check, not a regen; read [[regen-serial-set-is-ordered]] in `notes/_MEMENTO-DECISIONS.md` § #210 if in doubt) and a gate in `_git_commit.sh` that calls `_validate_polarities.py --check`. Prove the gate by invoking the gate FUNCTION/script directly on the real files (green) and on a mutated copy (red) — not by committing.
6. **Report** with counts: **rows 30 · parties n · stubs n · links n by type · refusals 5 · selftest arms n (red n/n) · generated files 3 · UNPROVEN n** — and a migration table: every former `apollo_touch` value → its typed link with the ≤15-word quote that justifies the type, or UNPROVEN.

## DO NOT RULE
What any polarity's default IS (they stay FLOATED, copied from `open-tensions.json`) · the four ask-whens · a fifth link type (flag UNPROVEN instead) · the grill's wording · any write under `apollo-spider/` · any edit to the R1/T assets · any rename beyond tension→polarity in the NEW home (`s143-D1`'s "tension" $status flags are a different concept — leave every one of them).

## PITFALLS FOR THIS LANE
1. A gate without a consumer — wire it AND drive it. 2. A mutation test that proves the clause, not the feature — arms go through the real entry point on real rows. 3. A reformatting writer — prove by reconstruction. 4. Typing a status by hand anywhere, even in a fixture — derive it. 5. "default" has two senses already (`s219-D3`); name the grill's sense in the field (`factory_default`). 6. Running `_build_all.py` "to check". 7. The sort has a clock — `generated_at` on every generated file. 8. `_carry`-style counting bugs — count with the same function you print with.

## FILING
`X = P`, slug `polarity-gate`. Stub back to chat per COMMON.
