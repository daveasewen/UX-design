# #239 — DELEGATED WRAP BRIEF (Opus wrap sub; conductor = Fable, FILL past the advisory, judgment CLOSED)

**Seat:** `/sessions/keen-serene-johnson/mnt/UX-design/` (bash) = `/Users/daviewen/Documents/Claude/Projects/UX-design/` (Read/Write/Edit). Branch `master`, HEAD `3d722d2` (mid-session lane commit, tree CLEAN). **Form to COPY:** `notes/_briefs/2026-09-02-238-delegated-wrap-brief.md` (the previous delegated wrap — same shape, same fences) and the canon `knowledge/_RUNBOOK-capture-ritual.md` (read its newest stratum first; the ritual steps 1a/1b/2c/2d/2f/2g and `--wrap` call form are there, not here). `pip install tiktoken --break-system-packages` first.

## THE SESSION IN SIX LINES (the WHAT; the WHY goes in the 1b dossier `_DECISION-HISTORY/2026-09-02-239-<slug>.md`)
1. **Opener:** the `s203-D1` CI read-back for the #238 push was DONE: run #478 `FAILURE`; every run #227 → #238 reads "failed" off the Actions list (12 consecutive); the first blocker since `e7cf3db` (after #231) = `ASSERT-009` metas 136 ≠ 137 (137th = `knowledge/components/template-dashboard-bento.meta.json`); the `Survey the COMMITTED tree` step also failed, body UNPROVEN (`_build_survey.py` exceeds the sandbox wall). Dave: **"Okay rebase, do both."**
2. **Chore R (Sonnet)** re-based `ASSERT-009` 136 → 137 BY ADDITION — `knowledge/_assertions.json` predicate:191 / claim:185 / provenance append:202, `knowledge/README.md:13`; gate reads `✅ assertion gate passed — 8 claim(s) still true.`; the three other `asserted_in` homes had ZERO live hits, untouched. Report `notes/_subreports/2026-09-02-239-R-assert-009-rebase.md` (`W-382`), brief `W-380`.
3. **Lane F (Fable)** fixed the polarity gate BY CLASS (V's Q1–Q8 recommended answers taken as DECLARED DEFAULTS, `s238-D3`/`D5`): **48 → 4 ESCAPED** (RULED 16 → 0 · PROMISED 26 → 0 · UNRULED 4: 241 · 243 · 245 · 301) · **CRASH 10 → 0 · MISNAMED 3 → 0** · selftest 125 arms red 113/113 (+72 new) · commit-script selftest 14/14 · live `--check` GREEN · determinism OK · wiring rc 1 = #235's orphan only · findings 16 · ruling-shaped 9 · UNPROVEN 6. `polarities.json` / `stubs.json` / `principles.json` BYTE-UNTOUCHED. Files: `_validate_polarities.py`, `brain/schema/polarity.schema.json`, `_git_commit.sh` (seam block), `_build_all.py` ([141] comment + remedy clause). Report `notes/_subreports/2026-09-02-239-F-polarity-fix.md` (`W-381`), brief `W-379`, COMMON `W-378`. ⚠ **Green controls 1/7 — six FALSE-RED under Q3(b): there is NO LEGAL FORM for a NEW or RETIRED polarity** (F ruling-shaped 4) — Dave's, BEFORE any V2.
4. **Committed `3d722d2`** (`after #239 … during #239 …` subject, read back from `git log`), 18 paths reconciled by name + `_CHAIN.md` regenerated (store 450 → 455 items moved its generated block). Five rows minted `W-378 … W-382`.
5. **Dave's mid-turn paste — THE BOOT BAND:** "derive the band, shrink-only ceiling, shrink first" + "we need to get this fixed soon". NO budget here (FILL 168,576 when it arrived); FILED VERBATIM-IN-SUBSTANCE as `notes/_briefs/2026-09-02-240-boot-band-derive-brief.md` — **RULING-SHAPED, Dave's, the #240 opener's FIRST question.** Part 3 is already `s228-D6`.
6. **Memory (ritual step 3, conductor's, DONE at this seat):** `polarity-fix-and-boot-band-239.md` + one `MEMORY.md` line (the #238 line trimmed). ⚠ `MEMORY.md` is **19.8 KB against a 24.4 KB read limit** — a compaction pass is OWED at the #240 opener (Dave's own "shrink first"); this sub writes NO memory.

## GAUGE — moments stated separately, never converted
boot **75,619 real** (first turn, `message.usage`, `_checkin.py`) — OUTSIDE the `s208-D1` band 55,595–57,903; the log's last six sessions all read 75–77K; ordinal NOT claimed; no constant moved · opener FILL **84,635 @ 4 turns** · post-CI-read-back FILL **140,327 @ 34 turns** (the read-back cost ~55K — a finding, homed in the 1b dossier: cap the Chrome route at run page + one JS grep) · post-lanes FILL **168,576 real @ 42 turns — PAST the 150,929 advisory by 17,647, DECLARED IN CHAT, judgment CLOSED; mechanical-only from there** · probe **PLANTED 17:07:48Z, QUIZZED BLIND 4/4 GREEN at 168,576** (`knowledge/_probe/session-239.json` — READ the file's `verdict`/`missed`, never this line) · **subs 531,802 tokens (n=2: F 431,200 · R 100,602)** — QUOTA, never window FILL · effort band **L** · this wrap sub's own spend UNOBSERVABLE, DECLARED. ⚠ Quota panel at the opener (Dave's words): All models 28% · Fable 45% · resets Thu 23:00.

## CARRIES → #240 (`_CARRIES.md` § `## residual → #240`; bump every age bracket; a STRIKE keeps the struck text verbatim with its `s188-D2` receipt)
- **STRIKE** "THE POLARITY GATE — 48 ESCAPES, THEN THE FIX LANE" → `W-381` + `3d722d2` (44 closed by class). **PARTIAL:** the 4 UNRULED escapes stand as Dave's rulings (241/301 register receipt · 243 same-ref contradiction · 245 links receipt), NOT bugs.
- **NEW — 0:** ⬛ **THE BOOT BAND — Dave rules "derive + shrink-only ceiling + shrink first"**, brief `2026-09-02-240-boot-band-derive-brief.md`; then ONE Opus lane. ("soon" — his word.)
- **NEW — 0:** ⬛ **V2 — a FRESH verifier on the FIXED gate**, briefed against F's `escaped-now-caught.txt` + `green-controls-recut.txt`; `W-374` closes only at ESCAPED 0 by V's harness + conductor read. **Gated by** the legal-form ruling (next item) or V2 re-reports the six false-reds.
- **NEW — 0:** ⬛ **A LEGAL FORM for a NEW / RETIRED polarity** (F ruling-shaped 4) — Dave's.
- **NEW — 0:** ⬛ F ruling-shaped 5–9 (the `R1-NOT-LIVE` name · `.DS_Store` by bytes as built · the FLOATED figures block · quote on `explainedBy`/`challengedBy` · prose-reading false positives) — Dave's, priced in the report.
- **NEW — 0:** ⬛ R's two: proposed `asserted_in` trim for `_MEMENTO-DECISIONS.md`; the COMMON-vs-brief rule tension on `_LIVE-STATE.md` writes (name it, don't resolve it).
- **NEW — 0:** ⬛ `MEMORY.md` compaction pass (19.8 KB / 24.4 KB) — conductor's, #240 opener.
- **NEW — 0:** ⬛ the `s203-D1` CI read-back for THIS wrap's push — the NEXT blocker past step 10/142 is unknown; the `Survey` step's failing check is UNPROVEN.
- Every prior carry at its own age (Q9 sort · Q10 watch note · Q1(c) machine `state` · the four ask-whens · 20 open polarities' conservative side · pl-02/pl-29 · P's Q1/Q2/Q6 · B's five · C's ruling-shaped · M's two · plan v2 §0/§10 · "gestalt" collision · L1's three · v1.0.6 brief · `_validate_wiring.py` red on `_validate_receipt.py`) — bump, never drop.

## TITLE THE NEXT CHAT → `Apollo - #240: rule the boot band, then verify the fixed gate`
(Deliver BOTH title lines in the stub — this chat's and the next — the conductor relays them in chat.)

## DO-NOT-RULE (the fence — Dave's #110 lesson; a wrap sub that rules is the defect)
No ruling inscribed, no `s238-D*` re-worded, no `_inscribe_ruling.py` call. No W-row CLOSED (`W-374` stays open — ESCAPED is 4, not 0). No constant / band / floor / advisory / stop line / wall / roster / version move; NO re-base of anything; the boot-band proposal is CARRIED, not enacted, not evaluated. No memory write. No edit to any lane report, asset, brief, plan or review (ADR-0017). No `_build_all.py`. No render. No `_memento_search.py` retrieval beyond what the runbook's steps require. The 2c/2d/2f exit checks run as PROSE with every probe NAMED; 2g rebuilds `_memento-index.json` LAST. Declare every skip with its size.

## PUSH — AUTHORISED (`s207-D1`, conductor's judgment): `bash knowledge/_git_commit.sh --wrap … --push` per the runbook, AFTER the wrap commit, clean tree; read the remote hash back (`git rev-parse origin/master` == HEAD) — only `✅ pushed and VERIFIED` counts. The CI read-back is the CONDUCTOR'S, after your stub.

## STUB back to chat (≤14 lines): wrap commit hash + subject READ BACK from `git log -1` · push verdict with both hashes · GM/LS/chain sizes from the gate's own stamp · 2c/2d/2f/2g receipts (line counts) · carry count by `_capture_gate._carry_items` · declared skips with sizes · BOTH title lines · anything you STOPPED on.
