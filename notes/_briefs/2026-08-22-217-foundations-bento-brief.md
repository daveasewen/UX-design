# Sub brief — Foundations tier: photography + logos, bento-style (#217, Opus build sub)

**Dave's word (#217, verbatim):** *"so we need a foundations section in the library with images and logos displayed like the photography bento"*.

## Context (verified this session)
- Library v2 is generated by `knowledge/_render/gen_library_214.py`, which owns `showroom/index.html`. Its **Foundations ladder tier exists but is empty and not drawn** (logos-sub survey, this session). Wiring an entry needs a component-store entry + the full ordered regen.
- The visual reference Dave pointed at: the **photography bento** — `knowledge/_fitness-test/bento-gallery-showcase-v2.html` (#216, masonry/grid + zero-JS popover lightbox). Copy its approved patterns; never re-draw [[specimen-starts-from-reference]].
- Assets ready: `knowledge/assets/photography-web/` (12 derivatives) + `knowledge/_PHOTOGRAPHY-MANIFEST.{json,md}` (251 rows, s217-D1) · `knowledge/assets/logos/` (12 SVGs; presentation precedent = `reviews/LOGOS-2026-08-22-v1.html`, including correct grounds per variant and the two defective dark-identifier files, ds-045 — show them as the files are, defect noted on-page).

## Scope
1. **SURVEY FIRST**: read `gen_library_214.py` end to end — how tiers, store entries, and generated pages are wired; follow its conventions exactly.
2. **Two Foundations entries**: Photography and Logos, each with a generated library page in the bento-gallery style (v2 patterns: bento/masonry grid, zero-JS lightbox for photography). Logos on correct grounds (dark marks never floated on white), colour+mono, per the LOGOS review page's proven approach.
3. **Photography page**: the 12 minted derivatives, captions from the manifest (EXIF description + licence source); state on-page that the full set is 251 and derivatives mint on demand.
4. ⛔ **Bento stays un-minted as canon**: Dave parked structural bento components (#216). Page-local styles only, declared in the page head; no new canon components, no canon.css edits.
5. **Regen serial is ORDERED** — you are touching the generator, so run the WHOLE serial per wave, ramp first, index last (#210 paid ~6 CI reds for skipping this). `_build_all.py` full single-process run is sandbox-impossible; run the serial the runbook prescribes, never a partial `_build_all.py`.
6. **Render-proof** per `knowledge/_RUNBOOK-render-verify.md` (read first; `set_content` banned; `goto("file://…")`; chunk near 45s). Four themes × light/dark on both pages; dangling-var probe (any `--*` read must resolve in ALL themes — silent black otherwise).
7. **Rows**: store rows for new docs at creation, `_REVIEW-SIGNOFF.md` AWAITING row(s), DS defects by addition to `_DS-IMPROVEMENTS.md`.

## DO-NOT-RULE (hard fence)
- `knowledge/_rulings.json` READ-ONLY.
- No canon component minting (bento parked), no canon.css / token / theme edits, no ds-044 canon fix (Dave has not nodded), no logo policy, no photo preference calls, no gauge/lane/worklist edits.
- Do NOT commit or push; no git-checkout/reset.

## Pitfalls to carry (mandatory replay, Dave #165)
- (a) The bento showcases are themselves AWAITING Dave's sign-off — you are copying a layout he pointed at, not certifying it; say so in the sign-off row.
- (b) A generator edit that regenerates 100+ pages can smuggle diffs — after the serial, `git diff --stat` and REPORT the blast radius; unexplained churn is a finding, not noise.
- (c) The library index gate compares CONTENT not mtime — rebuild the memento/library indexes in the serial's ruled order, index LAST.
- (d) Version don't overwrite (`-vN`); sandbox cannot rm (mv to `_to_delete/`).
- (e) Fontconfig must point at the /var/tmp symlink farm, never the repo TTF dir (#138 stray class).

## Report back (replayed in-window)
Generator diff summary + blast radius measured · paths built · per-theme render evidence · rows added · residuals declared · what a failed probe would invalidate · token spend.
