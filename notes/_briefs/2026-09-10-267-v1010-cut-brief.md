# #267 lane P brief — CUT v1.0.10: close the packaging hole (the two gate helpers the zip never shipped)

**Model: Opus. Conductor: Fable (#267). Dave's word at #267: "lets do this now" on the recommendation "bump to v1.0.10 tomorrow morning with the two scripts in the manifest". ⛔ RELEASE IS STILL HIS WORD — this lane takes the cut to PROPOSED + go/no-go and STOPS. It does not ratify, does not `--release`, does not push.**

## THE FINDING (lane C6, verified at the conductor's seat)

`gen_provenance_receipt.py` and `gen_component_partials.py` are in the tree (`knowledge/`) and in **none** of the v1.0.7 / v1.0.8 / v1.0.9 zips (`unzip -l … | grep -c` = 0 for all three). `_validate_receipt.py` and `_validate_screen.py` name `gen_provenance_receipt.py --mint` as the remedy, and `_validate_receipt.py:635` loads it with `gen = __import__("gen_provenance_receipt")` — a **string call, not an `ast.Import` node**, so `local_imports()` in `knowledge/_release/_gen_pack_manifest.py:836` never sees it. A consumer of the pack cannot pass either gate. Three releases, no gate parsed it.

## THE JOB, IN ORDER

1. **Read the release machinery first, not the hook.** `apollo-spider/build-designer-pack.sh` header · `knowledge/_release/_gen_pack_manifest.py` (the cut rules, `HELPER_HOMES`, `local_imports`, the probe) · `knowledge/_release/_gate_frozen_release.py` · the #262 commit sequence as the runbook: `git log --format="%h %s" --grep="#262" | grep -i -E "cut|manifest|ratif|releas|frozen"` and `git show` the ones that touched `_release/`. Note how the version is DERIVED, not typed (#228), where `RATIFY_IDS` lives, and what the frozen literal is.
2. **Fix the MECHANISM, not the symptom.** Make `local_imports()` (or the probe's helper-copy step) also see `__import__("<name>")` / `importlib.import_module("<name>")` string calls, so the two helpers are claimed by measurement. Only if the generator's design makes that the wrong seat, add them to `HELPER_HOMES` — and say why in the docstring. Establish, by grep/AST, whether `gen_component_partials.py` is actually reached by any shipped gate or skill (C6 says it is missing; prove whether it is *needed*). Run `python3 knowledge/_release/_gen_pack_manifest.py --selftest` and `bash apollo-spider/build-designer-pack.sh --selftest`.
3. **Commit the fix** (one commit, `during #267 2026-09-10 — …`, message names the mechanism: the lazy `__import__` the AST scan could not see). Nothing else in the commit.
4. **Cut v1.0.10 PROPOSED at that commit:** `--manifest --commit <sha>`, then `--probe`, then `--dry-run` TWICE into two throwaway dirs and `cmp` the zips (byte-identical or it is a finding). Verify by `unzip -l` that both helpers are now in the dry-run zip. Then **prove the consumer can pass the gate**: from the unzipped dry-run stage, run `_validate_receipt.py` / `_validate_screen.py` against the C6 dashboard (`outputs/coldrun-267/arm-A-blind/dashboard.html`, copy it — never touch the frozen arm), minting a receipt with the shipped `gen_provenance_receipt.py`. Exit codes are the claim.
5. **Ten release gates** the way #262 ran them (find the roster in the #262 commits / `knowledge/_release/`). Report each PASS/FAIL with its exit code. ⛔ A red is REPORTED, never routed around.
6. **Commit the PROPOSED cut** (manifest + probe + anything the #262 O2 commit shape touched). Do NOT touch `RATIFY_IDS`, the frozen literal, `_frozen-releases.json`, or `dist/`.
7. **Go/no-go page for Dave:** `reviews/GO-NOGO-v1.0.10-2026-09-10-v1.html`, swiss-design-system idiom, ONE page: the finding · the mechanism fixed (file:line before/after) · manifest delta v1.0.9 → v1.0.10 (files added, nothing removed — if anything else moved, name it) · dry-run fingerprints · consumer gate proof (exit codes) · ten gates table · the ONE question, ruling-shaped, not answered: *Ratify v1.0.10 at `<sha>`?*
8. `notes/_lanes/2026-09-10-267-P-v1010-cut.md` — done / NOT done (declared) / tokens / paths / the exact commands the conductor runs after Dave's word (inscribe → `RATIFY_IDS` → frozen literal → `--release` → `--check` → ledger re-seed), each with its #262 precedent commit.

## RULES
- ⛔ No `--release`. No push. No ratification. No edits to `_rulings.json`. No `_build_all.py`. No `gen_kg_edges.py`.
- ⛔ Serial commits only; if `.git/index.lock` exists and cannot be unlinked, STOP and report — do not `git stash`, do not delete it.
- ⛔ Claims carry a probeable token (path:line, sha, exit code). "Should work" is not a finding.
- Check-in at the seam between step 4 and step 5: write your context use into the lane file.
- Return to the conductor, under 400 words: fix commit sha · proposed-cut commit sha · manifest delta (n files added, list) · dry-run fingerprint + cmp rc · consumer gate exit codes · ten gates summary · go/no-go page path · lane file path · tokens · anything RED or NOT DONE.
