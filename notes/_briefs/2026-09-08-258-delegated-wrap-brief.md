# #258 delegated wrap brief — run the FULL wrap ritual

**Model: Opus 5. Conductor: Fable (#258), FILL 187,384 real per `_checkin.py` at the brief cut — PAST the 150,929 advisory by 36,455 (the conductor's own running estimate was ≈95K: HALF — lanes' returns are the FILL; state it, don't argue it). Boot 68,826 real (under the 70,000 literal a second time; #255/#256 readings still over it — the arm decides). Recall probe NOT planted (declare, 0 claims). No pace panel asked. 16 sub lanes ≈2.6M sub tokens; check-in run at the opener and once at the end — seam-less between lanes, declare it (`checkin-at-the-ends-cannot-catch-the-lane`).**

## 1. RULINGS — FOUR, Dave's, ALREADY INSCRIBED by the conductor (store 398 → 402, parsed). Do NOT re-inscribe. Read back from `knowledge/_rulings.json` and quote `says` verbatim in the stratum.
- `s258-D1` — rule 2a REMOVED; the author writes JS. Dave: *"definitely remove 2a I'd like to see not only the wiring work it might deliver some code creativity."*
- `s258-D2` — ambitious by default: everything works, rich deep mock data, code as innovation; `/goal` skill PARKED. His words in `says`.
- `s258-D3` — demo chrome RETAINED for the showroom, FENCED (`APOLLO-DEMO <what> START/END`) out of builds; `--<x>-max` vars page-owned. Dave: "sweep" after choosing retain-for-library / strip-for-build.
- `s258-D4` — Data-grid composes Selection-controls; gate NOT exempted. Dave: *"1. Okay lets do it"*.

## 2. WHAT #258 WAS (WHAT here; WHY in `_DECISION-HISTORY/2026-09-08-258-<slug>.md`, which you write — suggested slug `five-cold-runs-and-the-sentence-2a-forbade`)
- Opener: chain read (#257 → #258), tiktoken installed cold, `_checkin.py` boot 68,837. Led with the cold run; Dave: "go".
- **Cold run v1** (Opus, blind, from the v1.0.7 ZIP `13a593de…`): **3/2/0/2** vs #246 2/2/0/2. Verifier lane confirmed by driving. Everything working was self-contained in one component; everything dead reached across one. Findings: pack's own gate fails the bento template; 21/136 metas carry `behaviour`, none on data-grid/filter-toolbar-bar/sidebar-nav. `reviews/COLDRUN-258-2026-09-08-v1.html`, `notes/_subreports/2026-09-08-258-C-cold-run.md`.
- **D1 + D2 enacted** in one wave of 3 Opus lanes (`e774fb7`): skill rewritten (rules 13–17: data model first, every control works, state persists, zero JS errors, footer mandatory; procedure 2a "Model the data", 6 "Drive it"); `_validate_receipt.py` relaxed for authored JS (selftest 41 arms); footers into side-nav / multi-column / bento (lane 2's premise correction: 36 of the bento's "146 hex" are session numbers in comments — `check_screen` never strips comments; the rest are declared theme splices; `_validate_screen.py` reds all 8 shells unmodified — it is the composed-screen gate; snippets carry TWO themes, the four Apollo themes live on `data-apollo-theme` which 0/137 snippets link); typed `behaviour` metas for data-grid / filter-toolbar-bar / sidebar-nav (51-meta backlog listed in `…L3-behaviour-metas.md`).
- **Cold run v2** vs the tree (`0b8c0c7`): **3/2/3/3**, 0 pageerrors, 58/58 controls live, URL state survives reload; verifier confirmed with two corrections (grid 213px not ~400; `.dgseg` element absent — the switcher was three unstyled buttons). Full held at 2 by the Data-grid snippet's demo switcher + `--demo-width` dial copied into the page.
- **D3 sweep:** probe lane (`…P-demo-chrome-probe.md`: 61 snippets with chrome, 27 dangerous); lanes A/B/C fenced 53 snippets, moved every dangerous `--demo-width` read to a real default, split five template scripts component/chrome; lane G: skill rule 2/2a/step 3 never copy inside a fence, `FAIL:DEMO-CHROME-COPIED` (selftest 46 arms), marker grammar table in `_RUNBOOK-compose-from-canon.md`. Commits `1c27d6b`, `5c241f7`.
- **Cold run v3** (`5c241f7`): **3/2/3/2** — APOLLO-DEMO in page 0, grid 213→760px, compose hex 262→0; three toolbar filters died on an open-contract the skill never said to read; receipt gate never fired (no mint). Fixes: skill step 2 reads `behaviour`, step 5 mints first, no-eyes VS Code fallback; canon.css re-projected via `gen_canon_components` (`68e5c25`); Data-grid width to the page (`84f0a17`).
- **Cold run v4** (`84f0a17`): **3/2/3/3**, 0 dead controls, receipt PASS after `--mint`. Finding: the fenced `.dg{--dg-max:760px}` had re-projected into canon.css:10538 — `gen_canon_components` now strips APOLLO-DEMO fenced CSS (−335 lines of chrome from canon.css); `--<x>-max` are runtime vars in `_validate_compose.py` (PASS). `c9bf745`.
- **Cold run v5** (`c9bf745`): **3/3/3/2, BOTH GATES PASS for the first time in the series** (grid 1,328px in a 1,376px tile). Interactive slipped on a builder bug (listeners before render); skill: theme attrs on `<html>` never beside a `.cn-*` scope (dark theme died silently on `<body>`), wire by delegation. `c7068a2`.
- **D4 enacted** (`f9a12bf`): Data-grid checkboxes composed from Selection-controls, 44px targets, indeterminate driven, compose gate PASS on a v5 copy, mutation-proved.
- **Rule 18 "Charts from data — the interim recipe"** (`1c9c6ae`): dv-behaviour grammar quoted (`data-fx/-fw/-fx2/-fxs/-ys/-tip`, `resize` re-fit), driven green on a 78-line test page. Lane R finding: canon namespaces every `.dv-*` rule under `:where(.cn-chart-*)`; cold runs v4/v5 emitted geometry with NO `cn-chart-*` wrapper, so their bars never animated in canon terms — trap (a) of rule 18.
- Conductor slips, declared: typed `git stash` by reflex during a compose check (popped next call, nothing lost); FILL estimate half the measured figure. Memory hook written by the CONDUCTOR (`one-shot-series-and-2a-removed-258.md` + one MEMORY.md line) — declare 0 bytes at this seat.
- 10 commits `70ed8c7..1c9c6ae`, NOT pushed.

## 3. STILL DAVE'S (carry, by addition to `_CARRIES.md` § residual → #259)
**Data-driven chart ENGINE — #259's stated job** (bar/line/stacked-area/donut/sparkline/combo; the other five post-demo) · **v1.0.8 cut** later this week, after a sixth cold run as receipt (ratified v1.0.7 zip ≠ tree; same-version re-bake = laundering) · **his rulings page** STILL not built (second session running; store 402) · `/goal` skill PARKED · `behaviour` metas on 51 scripted components · `_validate_screen.py` writes its ledger into `knowledge/_screen-gate/` keyed on basename — clobbered tracked files 4× today, ruling-shaped · `check_screen` counting hex in comments (one-line `strip_css_comments` fix, unordered) · chrome CSS still in canon under `.cn-*` before #258's generator fix — now gone; Links keeps `--demo-width` by name as a RUNTIME var · 10 of 11 chart types unused five runs running · Dave's element list for the design pass (KPI tile, grid header, filter toolbar nominated) · plus everything carried from #257 (DIR rename, P1(b), Q2–Q6, R1, `intent` ≈09-19, boot-ceiling arm, midnight-wrap stamp, 2 STALE hooks, CI read-back, `--seed` refusing PROPOSED).

## 4. NEXT: the chart engine (demo set), then v1.0.8. Next title: `Apollo - #259: the chart engine — six data-driven charts in the library; v1.0.8 cut`.

## 5. THE RITUAL, WHOLE — steps 1→5 per `knowledge/_RUNBOOK-capture-ritual.md` (grep; read the runbook, not the hook). Step 2g rebuilds `_memento-index.json` LAST. `--wrap`: report what the ceiling arm says; if REFUSED, commit in the #243 form and SAY SO. Subject READ BACK from `git log`; both title lines returned. **PUSH** via `bash knowledge/_git_commit.sh --push`; if it fails, declare. Declare every skip WITH ITS SIZE. Do NOT rule anything in §3. Do NOT run `_build_all.py`. Do NOT touch `dist/` or the frozen ledger. ⛔ Never `git checkout` / `git stash`. Delete grant is ON this session. Use `SESSION_N=258 bash knowledge/_git_commit.sh --reconciled <msgfile> --all-dirty` (or the runbook's form).

Return under 350 words: `_rulings.json` count, commit hash + subject read back, push result, `--wrap` result, `_CHAIN.md` size line, declared skips, both title lines.
