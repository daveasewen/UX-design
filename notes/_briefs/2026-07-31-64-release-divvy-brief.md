provenance: local_aa3d71d3-9961-4f44-9fc7-1d56283a5f90 · 2026-07-31
status: observed

# #64 RELEASE — divvy plan (multi-window per #60-D1; Dave accepted the offer at #63's close: "divvy plan sounds good")

*Roles come from Dave's opener lines, never from titles (#60-D8 / the 2026-07-21 lesson). Cap 3
windows, Dave vetoes or re-lanes at the opener. ONE conductor commits; workers write ONLY their
named new files and hand everything else back as final-message reports, replayed before
ratification (#57). Per `_RUNBOOK-parallel-conductor.md`.*

## The gate on the whole session: STABILITY CLOSES BEFORE PACKAGING STARTS

Stability = evidence, not vibes: ① CI's first verdict on the wired survey step (Dave's push
delivers it; conductor reads the Actions result) · ② gate-5 matrix · ③ capture phase 2 ·
④ the three DRAFT ratifications + mover count fix. Packaging work begins only when the
conductor can quote all four.

## Lanes

**Window 1 — CONDUCTOR (Opus default; Fable only if Dave assigns the delta audit judgment work
here — his call, Fable is rationed).**
Owns: ALL commits (via `_git_commit.sh`) · all ratifications of worker drafts (replay first) ·
the mover count reconciliation APPLICATION · the packaging **delta audit** of
`notes/2026-07-25-claude-code-orchestration-survey.md` (Dave's #62 caveat: BASE not truth —
re-verify every claim; boot artefact to port is now `_CHAIN.md` + retrieval) · surfacing **D6
(Dave's own access pre-check) at the opener, before any Copilot build** · the wrap.
**Serial set (conductor-only, never parallel):** GOOD-MORNING.md · _LIVE-STATE.md · the ledgers ·
`knowledge/_memento-index.json` / `_CHAIN.md` · anything in `knowledge/_capture_gate.py` · git.

**Window 2 — WORKER A (Sonnet): gate 5 + mover reconciliation report.**
Deliverable 1: `notes/_bite-matrix-usage-DRAFT.md` — `_gm_usage.py` per the phase-1 brief shape
(CLAIMS/BITES/MUTATION-RED/CANNOT-SEE; mutations on tempfile fixtures ONLY; the live record
untouchable; UNPROVEN/CANNOT-TEST-SAFELY legal; counts MEASURED by grep on its own file).
Deliverable 2: a RECONCILIATION REPORT in its final message naming the mover draft's 4
unverdicted rows and proposed verdicts — **as a report, never edits to
`notes/_bite-matrix-gm-move-DRAFT.md`** (that file is conductor's to change).

**Window 3 — WORKER B (Sonnet): capture-gate phase 2.**
Deliverable: `notes/_bite-matrix-capture-gate-PHASE2-DRAFT.md` — mutations for the 30 UNCOVERED
rows from phase 1, tempfile fixtures only, gate never modified, defects RECORDED not repaired.
**Headline row first: ds-022's #58 fail-loud-on-4th-unrollable cross-check.** Chunk to the ~45s
wall; quote every red verbatim.

## After stability: packaging order (conductor, likely spilling to #65)

Delta audit of the 07-25 survey → ledger D4′ sequence → Copilot-in-VS-Code package FIRST (org-push
delivery, no marketplace) **gated on D6** → Claude plugin (IN scope by Dave's #63 redirect — the
old "later" is retired; `create-cowork-plugin` skill exists for the Cowork side). The one-pager
(voice pass owed) fronts the package docs.
