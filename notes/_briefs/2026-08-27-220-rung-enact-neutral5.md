# #220 sub brief — enact PROPOSED: console-dark capsule caption ground → neutral/5 #313131

**Model: Opus.** Dave's pick off `reviews/CAPTION-DARK-LADDER-2026-08-27-v1.html`: *"I prefer rgb(48,48,48) · #303030 … is there a primitive that's close in the neutral ramp?"* Conductor answer: **`color/neutral/5` = `#313131`** (knowledge/tokens/colour.json), one grey point off, ΔL*≈0.4. Status: **PROPOSED pending Dave's confirm — you enact the binding, you do NOT inscribe the ruling** (`_inscribe_ruling.py` is conductor+Dave only).

## Scope
1. AT CAUSE, in the generator(s) that mint the console capsule chord's caption grounds (`knowledge/_render/gen_bento_matrix_217.py` / `role_defaults_219.py` — find the one true home; the rails manifest is GENERATED, never hand-edited): the console **dark-mode** capsule caption ground resolves to `#313131` bound to/derived from `color/neutral/5`, minted concrete per s200-D1 with the primitive named in the mint comment. Light mode untouched. Other themes untouched (mono's access to the dark chord is EXPRESSLY OPEN — s219-D3(3)/X6 — grant nothing).
2. Regenerate: rails manifest + `showroom/_foundations/bento-rails.html` + any page the generators own that renders the console capsule chord. Run their selftests + the mutation arms (all named-red counts as before or better) + `verify_bento_matrix_217.py` / `verify_foundations_217.py` per their own docs.
3. Proof page for Dave: `reviews/RUNG-ENACT-2026-08-27-v1.html` — BEFORE (today's vanishing caption) beside AFTER (`neutral/5`), console dark primary, same photograph card, measured rgb + contrast + lift printed. Copied-from-artefact method (the banked build sources under `notes/_subreports/assets/` are your pattern).

## Fences / regions
Regions: the two named generators + their regenerated artefacts, the proof page, your filed report. ⛔ NO git (a parallel lane owns git this window — your changes stay uncommitted for the next seam). No `_rulings.json`/`_state.json`/memory. No release paths (`knowledge/_release/*`, `apollo-spider/*`) — a bake is running there in parallel. No promotions, no new tokens (neutral/5 exists — consuming it is the point). Two-red law + mono error ink camp untouched.

## Pitfalls (Dave #165)
- Repointing an alias can strip a theme override silently — after the bind, verify all four themes' capsule captions still resolve their own values (the verifier's per-theme arms).
- The dark ramp is mode-inverted (ladder report finding) — a naive token bind breaks light; this is why the bind is DARK-ONLY at the mint site.
- A regenerated page that isn't re-driven is a claim: drive the verifiers on the regenerated artefacts, not the pre-regen ones.

## Report
FILE at `notes/_subreports/2026-08-27-220-rung-enact.md`: COUNTS, the mint site quoted, per-theme verification table, RULING-SHAPED QUESTIONS (chiefly: does the same bind extend to legacy/supercharge capsule captions in dark — NOT enacted, Dave's), REPLAY-THESE. Chat stub ≤5 lines.
