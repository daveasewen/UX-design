# Wrap brief — session #135 (conductor-authored, for the wrap sub)

You are the wrap lane for session #135. **READ AND FOLLOW `_RUNBOOK-capture-ritual.md` — the runbook body, not this summary of it.** (#133/#134 both saw the sub follow the hook instead of the runbook; do not repeat it. The GM roll: you are the off-mover — the runbook's own steps govern.)

## Session record to inscribe

**#135 — "no-border extension readback + rule the KG review" · FABLE conductor, Dave live, 3 Sonnet subs (lane A KG-apply · lane B tier-map · research) + this wrap sub.**

Four rulings, ALL in `knowledge/_rulings.json` already (s135-D1..D4 — the file is the truth, quote it, do not re-derive):

- **s135-D1** — contextual notification shell: legacy+SC bordered, mono+console tint-only; console radius via `border-radius/surface` binding (12). RULED NOT ENACTED (snippet re-base queued with s130 remainder).
- **s135-D2** — KG review ruled at defaults (merge 5 · promote 52 · attach 25 · split 24); Dave's stylistic condition verified first.
- **s135-D3** — library shape REQUIREMENT: hierarchical atoms→molecules→organs→templates; SHELLS = slot mechanism at any scale ("patterns with injected contents"); variants embedded as contextual mutations; build mode generates from a pattern lexicon. Shell readback CLOSED same session.
- **s135-D4** — generator resolutions input: verdicts JSON becomes gen_kg_edges.py input; fixes the class lane A proved (generator wipes edges, 0/82 edge verdicts could land).

**Artefacts (all in `reviews/`):** NOTIF-BORDER-RULE v1+v2 (ruled off v2) · KG-REVIEW-EXPLAINER v1 · KG-REVIEW-ROWS v1 · KG-REVIEW-VERDICTS v1.json (the ruled set) · TIER-MAP-PROPOSAL v1 (424 nodes: 14 atom / 35 molecule / 31 organ / 0 template / 209 lexicon-fold / 134 mutation-key / 29 shells; 16 low-confidence) · RESEARCH-hierarchy-best-practice v1. Plus `notes/_briefs/2026-08-08-135-laneA-kg-apply.md` (exceptions ledger: 82 edge verdicts freshness-blocked, 9/24 token splits applied, 15 exceptions).

**Lane A finding (the session's find):** gen_kg_edges.py has NO preservation path — proven by 3 reverted test edits, validator green throughout. → s135-D4.

**Research verdict:** flexible graph+tiers is current practice (no rebuild needed); Figma Slots ≈ shells; same-button-recontextualised is settled industry answer; W3C DTCG stable Oct 2025.

## Residuals → #136 (carry forward, in this order)

1. ⬛ **s135-D4 enactment** — generator resolutions input, FIRST build lane [0]
2. ⬛ **Tier-map: Dave's 16 low-confidence rows** — his eye, controller export [0]
3. ⬛ **Three research alignments as separate rulings** — DTCG wire format · Figma-Slots semantics for shells · fork-ban gate (floated only, NOT ruled — do not upgrade) [3]
4. ⬛ s135-D1 enactment — Notifications snippet re-base (legacy-tagged, hardcoded radius 0 + unconditional border) [0]
5. ⬛ 15 token-split exceptions from lane A ledger [0]
6. Carried set from #134: s130-D4/D5/D6+tabs (blocked on mints, Dave's) · mark-vs-fill 3.0 gate (Dave's) · SC info-tint re-hue (unruled) · contrast-selftest browser arms · #130 carried set (21st roll) · render-proof eye pass on s134-D4 committed bytes.

**Next chat title:** `Apollo - #136: enact the resolutions input + Dave's 16 tier rows`

**Quota note for the ⚙ line:** 4 Sonnet subs ≈ 340K sub tokens this session (198K+84K+60K+wrap). Boot 55,056 (12th datapoint, in band). Wrap opened at FILL ~131K vs stop 150,929 — below.

## DO-NOT-RULE list (hard)

- Do NOT rule, close, or upgrade the three research alignments (floated), the tier-map defaults (standing, Dave rules the 16), SC info-tint, mono grey ramp, or ANY item marked Dave's. A delegated wrap ruled Dave's open item once (#110) — never again.
- Do NOT touch `knowledge/_rulings.json` (complete), the verdicts JSON, or any reviews/ file.
- Do NOT run `_build_all.py`. Do NOT push (push only on Dave's explicit word).
- Commit via `_git_commit.sh --reconciled <msgfile>` with the CONDUCTOR-AUTHORED msgfile at `notes/_briefs/2026-08-08-135-commitmsg.txt`. ⚠ If the script refuses, it MUTATES the msgfile — regenerate a FRESH msgfile from the copy in this brief before ANY retry (#134's doubled-prefix class). Verify the committed subject with `git log -1 --format=%s` AFTER — it must match the first line exactly (truncate long reads).
- Regen `_gen_chain.py` FIRST (before the commit — subject-ordering bug #116). Rebuild `_memento-index.json` LAST (2g; freshness check is content-compare and BLOCKING).
- 2f roll: anchors aimed at a rolling stratum BREAK (#131 open) — follow the runbook's current wording exactly.
- Reconcile every path — no blind `git add -A`. `knowledge/_rulings.json` + notes/_briefs/* + reviews/* (7 new files) + any runbook-generated files are the expected set; anything else unexplained → list it in your report, do not commit it.
- Git locks: `mv` to `_to_delete/_stale_locks/`, never rm.

## Commit message (copy VERBATIM to notes/_briefs/2026-08-08-135-commitmsg.txt)

```
#135: four rulings — notif borders/radius, KG defaults, atomic hierarchy, generator resolutions input

s135-D1 contextual notification: legacy+SC keep 1px border, mono+console
tint-only; console radius via border-radius/surface binding (12, provisional).
s135-D2 KG review ruled at defaults (merge 5, promote 52, attach 25, split 24);
stylistic condition verified. s135-D3 library requirement: hierarchical
atoms/molecules/organs/templates, shells = slot mechanism at any scale,
variants embedded as contextual mutations, build mode generates from a
pattern lexicon. s135-D4 generator resolutions input — lane A proved
gen_kg_edges.py wipes edges (0/82 verdicts could land); verdicts JSON
becomes generator input, #136 first build lane.

Artefacts: NOTIF-BORDER-RULE v1-v2, KG-REVIEW-{EXPLAINER,ROWS,VERDICTS},
TIER-MAP-PROPOSAL v1 (424 nodes, 16 low-confidence for Dave),
RESEARCH-hierarchy-best-practice v1. Lane A brief: 9/24 token splits
applied, 15 exceptions, validator green.
```

**Report back ≤12 lines:** ritual steps run/skipped+why, commit hash + verified subject, chain regen check, index freshness result, anything unexplained in the tree.
