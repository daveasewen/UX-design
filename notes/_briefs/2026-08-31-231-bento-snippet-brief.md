# #231 LANE BRIEF — the dashboard-bento snippet (F4)

provenance: #231 conductor (Fable) · 2026-08-31 · Dave approved build-today.
Row: W-326 (minted at creation). Report: `notes/_subreports/2026-08-31-231-bento-snippet.md` (FULL report filed there, chat stub, REPLAY-THESE section — s218-D7).

## THE GOAL

Dave's demo pass condition (s230-D1, in the store — READ IT) expects a dashboard built bento-first from canon. Beat 3 is PARTIAL because `grep -rl '\.c-bento' knowledge/snippets/` returns 0 files (rehearsal F4). Build `Template-dashboard-bento.reference.html` in `knowledge/snippets/` so a cold builder can COPY, not paraphrase.

## Method — COPY the approved artefact, never re-draw

[[specimen-starts-from-reference]]: hand-rolling invents defects. FIRST find the ratified bento canon and its approved decision-surface artefacts (#217/#220 programme: Display/Gallery/Dashboard matrix, s217-D1..D8 + s220-D1..D3 — grep `knowledge/_rulings.json` (298) for `s217-`/`s220-`/bento BEFORE building; the queue vs canon rule: canon wins). Splice the Dashboard-mode structure from the approved artefact. Sub git-checkout ban applies — work from files as they stand.

Bound canon you must honour: keylines HUG MODULES (s217-D8) · MONO default (s222-D1) · masthead logo defaults s230-D2 (masterbrand-light-colour light / masterbrand-dark-colour dark, the `<img data-mode>` pair idiom from `Template-auth.reference.html`) · type composites `.t-cm-*`/`.t-ed-*` mandatory, no raw type literals · two-red law (s151-D1) · ink rule (s175/s176) · component sizes from the ramp (28/36/44/48) — the #230 eye lane found raw `height:40px` literals; do not add another. Follow the existing snippet file conventions exactly (header/meta blocks — read 2–3 existing Template-*.reference.html first).

## After the source change — the WHOLE regen serial

[[regen-serial-set-is-ordered]]: #210 paid ~6 CI reds for partial runs. Find the canon ordered serial (grep runbooks/`notes/` for the regen serial; ramp first, index last) and run it COMPLETE, per wave. ⚠ Do NOT run `_build_all.py` (partial run strands the tree — chain warning). Gates you touch must be re-run green or the red DECLARED with its cause.

## Render for Dave's eye

`reviews/BENTO-SNIPPET-2026-08-31-v1.html` — specimen ALIVE (spliced verbatim, balanced-tag extraction like the #230 eye lane), light+dark, responsive spread, all four themes if theme-scoped (mono·legacy·console·supercharge — test PER THEME). Where canon is SILENT (the dark chord is OPEN; the sitting SITTING-220 awaits Dave), render 2–3 readings side by side and mark them AWAITING DAVE — never decide ([[feedback-mock-the-readings-before-building]]). Render mechanics: `_RUNBOOK-render-verify.md` FIRST, MOUNT-SIDE, `TMPDIR=/dev/shm`, no set_content(). -vN filenames, never overwrite.

## DO-NOT-RULE

- Anything the bento sitting owns (W-119 canon demo, roles/keylines open questions, dark chord) — render readings, don't rule.
- NO `_rulings.json` / `_state.json` writes, NO commits, NO push, NO release machinery, NO roster/version moves, NO pack/dist/manifest touches.
- Do not edit other snippets beyond what the regen serial itself regenerates.
- Grep the store before treating any bento question as open.

## Pitfalls replayed

The #230 eye lane's three findings are live in this family: raw height literals off the ramp · wrong rung's corner on a control · dark hover fill 1.05:1 — do not import them into the new snippet, and if the spliced source carries one, DECLARE it rather than silently fixing ratified record. A dangling dataviz var renders SILENT BLACK and 13 gates are blind to it — if any `--dv-*` var is referenced, prove resolution per theme. Sandbox: call boundary ~178s, drive steps individually; tiktoken first.
