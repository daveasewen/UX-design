# #221 WRAP BRIEF — delegated Opus wrap (written by the Fable conductor)

Read FIRST: `knowledge/_RUNBOOK-capture-ritual.md` (the wrap is its procedure) and `knowledge/_RUNBOOK-git-commit.md`. Final commit: `SESSION_N=221 bash knowledge/_git_commit.sh --wrap --reconciled <FRESH printf msgfile> --all-dirty` — red BLOCKS at --wrap. Read the generated files back; never quote a banner as evidence of a run.

## DO-NOT-RULE (binding)
No row of Dave's closed, reworded or re-scoped · `knowledge/_rulings.json` READ only — if no `s221-D1` exists in the store, mono's gallery default is STILL OPEN and the banner says so · no promotions (every advisory stays advisory) · no threshold, band, advisory, stop line, wall or lane moved · no `_to_delete/` moves beyond the runbook's own lock choreography · memory step 3 is the conductor's, NON-REPO — declare skipped, not smoothed. You MAY mint doc-rows via `_state.add()` for this brief and any doc you create (the #185 remedy; W-226 precedent) — rows record existence + close condition, rule nothing.

## SESSION FACTS (compose the ★ LATEST banner + ⏱ delta from these; verify each against the tree/git, not this prose)
- **Shape:** #221, one day spilling past midnight (2026-08-27→28). FABLE conductor + 4 MEASURED Opus work lanes + 2 MEASURED Fable subs (adversarial verifier + mono specimen) + this Opus wrap sub. **subs 1,507,353 tokens (n=6), conductor-measured from harness usage lines** (A 294,687 · B 271,530 · C 294,021 · D 231,273 · verify 216,079 · specimen 199,763) — QUOTA never window FILL; this wrap sub's own usage unmeasured from its seat, declared.
- **The wave:** the #220 six-lane audit's repairs ENACTED — ONE reconciled commit `2d375c6` (subject verified: `after #221 2026-08-28 — #221: the repair wave — the #220 audit findings enacted, six lanes`). Six filed reports: `notes/_subreports/2026-08-27-221-lane{A,B,C,D}.md` · `-verify.md` · `-mono-specimen.md`. Cite BY PATH, COUNTS copied off the files (re-read them yourself). Five new instruments ALL ADVISORY at birth. 12 store rows minted W-227…W-238.
- **Verify wave:** 82 green · 1 red (Lane D replay-prose overclaim — instrument stricter than its own description) · 9 could-not-run; all four cross-lane seams green. **The drift gate's live catch:** `_gate_fallback_drift_221.py` fired in-session on `gen_mono_gallery_221.py`'s stale `#F3F3F3` fallback; conductor repaired (re-derived `#F0F0F0`), page regenerated, gate green — an instrument WITH a consumer on day one.
- **Mono (Dave's, NOT ruled here):** his conditional in chat: *"there are no rounded corners in mono but I think they share the same neutral ramp so the colours can be the same if this is true."* Both premises VERIFIED TRUE at HEAD (neutral/5 `#313131` byte-identical mono/console; mono `--border-radius-container` resolves 0 both modes, console's 20px is the #199 override). Candidate page cut and presented: `reviews/MONO-GALLERY-DEFAULT-2026-08-27-v1.html`. **His one word still owed unless the store says otherwise at your run.**
- **Conductor's own misses, named (honesty clause, [[feedback-wrap-is-not-optional]]):** (1) the divvy brief carried TWO stale premises — Lane D proved 4 of its 6 items ALREADY BUILT at #218/#211, and the brief's global write-door pitfall was FALSE at HEAD — the conductor briefed from chain carries without replaying them; [[premise-ages-faster-than-rule]], the conductor's own instance, digested at the #222 opener. (2) Gauge: FILL crossed the ARMED 190,000 advisory and the 200,000 working line during the commit train — **DECLARED BEFORE CROSSING in chat this time** (the #220 pitfall (d) remedy enacted); the ~15M platform window made it safe; evidence FOR the pending s208-D1 re-base, never a licence.
- **Conductor-answered (not Dave's):** Lane D's contract-divergence question — the REPO TEMPLATE's COUNTS form is the one home; the #221 divvy's field set was the deviation; next divvy conforms.
- **Gauge moments (state separately, never rounded):** boot **69,692 real** — OUT of the s208-D1 band (55,595–57,903) by **11,789, the ELEVENTH consecutive**, declared at opener, LOG ONCE. Conductor FILL at brief-cut: measure via `knowledge/_checkin.py` yourself and report both his cut and your own first-hand cut. Dave's panel readings verbatim (morning): **80% all / 71% Fable, reset in 2h04** — invent no final; his end-of-day reading is owed in chat.
- **Zero rulings inscribed this session** (unless `s221-D1` exists in the store when you run — read, don't assume).

## RESIDUALS → #222 (verify each; carry with ages per s183-D1 P2/s188-D2)
Mono's one word (if still open) · Dave's eye on the MONO-GALLERY page (write its ⬛ AWAITING row in `knowledge/_REVIEW-SIGNOFF.md`, house style) · the 97 open sitting calls + 25 candidates, unchanged · s208-D1 re-base (eleventh drift + today's declared crossings as evidence, his boot-REDUCTION rider binds) · the fork triage: **29 real-defect candidates** + the **THIRD RED `#a8000b` on `--err`** which the two-red law does not name — Dave's eye (laneB report) · Lane C's F9 handoff: `apollo-spider/gumdrop/_state.py` byte-identical twin of `knowledge/_state.py` with no comparing gate · Lane A's residual CI causes: first-step red has 2 ruling-shaped causes + 1 Lane-D-region cause still standing (laneA report) · CI read-back queue still blocked at Dave's GitHub sign-in; this session's push joins it · v-next release carries: #220 caption work + Lane C pack fixes · the five advisory instruments' promote-or-park, all Dave's.

## MECHANICS
2c/2d/2f rolls via `_gm_move.py` · regenerate `_CHAIN.md` and `_memento-index.json` LAST (index freshness is BLOCKING, content-compared) · the 3 pre-existing capture-gate selftest fails: Lane D measured 2 as environment-bound fixture reads (M10: 12,517 tape vs the ruled 10,000) — they pre-date #220's green wrap; if --wrap blocks on them, declare per Lane D's report finding, never patch the fixture · deliver the RENAME + NEXT-TITLE lines in your receipt for the conductor to relay (chat relay is the conductor's, [[delegated-wrap-rename-relay]]) · NO push from your seat — the conductor pushes after you return.
