# #234 — THE v1.0.6 BRIEF: behaviour contract, composition edge, strict retrieval

*Written 2026-09-02 by the Fable conductor after the three #234 research reports. Spine = six rulings inscribed this session (`s234-D1`…`D6`). This is the BUILD brief for the next window(s); nothing here is built yet.*

## THE GOAL, WORKED BACK

`s234-D2` (Dave): output ranked against solid UI/UX standards, accessibility and code standards, ready to hand to a dev for wiring — bento and behaviour work IN SCOPE. Extends `s230-D1`. Pass = nothing needs redesigning, only connecting.

**What the research settled (reports by path, `s218-D7`):**
- `notes/_subreports/2026-09-02-234-rA-standards-bar.md` — 34-criterion rubric, tiers MACHINE / DRIVE / EYE; the artefact path runs 3 checks while ~30 gates are folder-bound; resilience (JS off, CSS on) is a published criterion at GOV.UK + USWDS and unchecked here; `_validate_behaviour.py` name is TAKEN (ADR-0015).
- `…-rB-composition-rules.md` — nobody groups by content type; groups form around a shared question; rules are parent/child RELATIONS (Polaris gap ladder, Carbon tile-group same-size); C1–C10 checkable conditions; four eye-only; no mature system publishes a bento rule; rails file has `group` ×0; 59 BLOCKING guideline rules in `_rules-index` with no consumer.
- `…-rC-retrieval-contract.md` — 528 props, every enum with legal values; 136/136 snippets carry `<script>`; SKILL.md files say slots/behaviour/receipt ×0; external systems carry behaviour as an ADDRESS beside markup; the contract must be the thing fetched, not a doc trusted to be read.

## THE SPINE — six rulings, all Dave's, #234

| id | rules |
|---|---|
| `s234-D1` | v1.0.5 HELD ("it waits for 06"); demo ran on v1.0.2, went well |
| `s234-D2` | the broad goal above |
| `s234-D3` | a11y target = WCAG 2.2 AA + 2.4.10 / 2.5.5 / 2.4.13; NOT 1.4.6 (two-red law) |
| `s234-D4` | grouping lives ONCE in the KG (`groupsWith` edge), derived everywhere else; kpi/chart/rail RE-CUT as role names |
| `s234-D5` | behaviour address = meta owns the typed declaration, generator injects a snippet block (`#token-manifest` precedent) |
| `s234-D6` | rubric binds to the artefact through `_validate_screen.py <path>`; the page carries a RECEIPT the gate PARSES first; globs not widened |

**Design principle running through D4–D6 (Dave's "brain" question, answered):** rules up the whole chain are VALID when the chain is a GENERATION chain and a DEFECT when it is a COPY chain. One home, derived consumers, one gate. Never the same fact twice.

## LANES (ordered; each is one Opus build sub, filed report, conductor reconciles)

**L1 — the RECEIPT + the entry gate (D6).** Define the provenance receipt block a generated page carries (snippet id · variant/prop values used · script address · retrieval-set version · pack version). `_validate_screen.py <path>` parses it FIRST (no-gate-parses-the-artefact rule) and chains: compose, icon-source, a11y, state-contrast (existing) + receipt-vs-snippet diff (invention = comparison) + the eleven `_validate_advisory.py` checks re-pointed at the path + resilience (JS-off render = CSS intact, engine fallback declared). New gate NAME owed — not `_validate_behaviour.py`. Rubric tiers reported honestly: MACHINE green ≠ DRIVE green ≠ EYE.

**L2 — the BEHAVIOUR ADDRESS (D5).** Schema: `behaviour` → typed object `{script: <address>, partial: <id|null>, events: [...], fallback: <text>}`; migrate the 20 existing prose values by ADDITION (keep prose under `$note`). Generator: `gen_component_partials.py` injects a `#behaviour-manifest` block beside `#token-manifest`. SKILL.md rule 2a (beside rule 2, never re-wording it): *copy the script address with the markup; author no JS.* L1's gate reads the meta address and checks the page loads it.

**L3 — the COMPOSITION EDGE (D4).** Schema: `groupsWith` edge beside `mustNotNeighbour` (`meta.schema.json:216` form, `ref:null` where unresolved, Dave's eye). Populate for the bento template members. `gen_bento_matrix_217.py --rails` emits a grouping dial FROM the edge. SKILL.md rule 7b points at the edge. Rename `tpl-group-kpi/-chart/-rail` → role names (Dave picks the words — proposed: `-lead` / `-evidence` / `-context`; FLOATED, not ruled). Gate: C9 span legality + C1 gap ladder first (pure arithmetic, rB Q4).

**L4 — the ROW-HEIGHT RENDERS (owed since #233, precondition to any row-height build).** Three side by side: fixed unit / floor `minmax(rung,auto)` / mis-rung — and rB's warning: canon already ships fixed rows AND a `minmax(unit,1fr)` floor at different nesting levels (`canon.css:1091` vs `:1124`), so the matrix must say which LEVEL it varies. Dave points; nothing built before.

**L5 — the CUT.** Regenerate v1.0.6 pack (regen serial is ORDERED, whole serial per wave), proving zip for Dave's cold test, W-338 closes on his word.

## STILL DAVE'S (not ruled, carried)
Role-name words (L3) · row-height model (L4) · new gate name (L1) · rA Q2 three-tier vocabulary (AVT1/2/3) · rB Q3 one-member group legality · rB Q5 wiring the 470 guideline rules · rC Q1 receipt identity key (filename / slug / content hash) · rC Q3 `events` field · rC Q4 retrieval-set membership marker · his remaining v1.0.5 observations.

## PITFALLS (consequences replayed)
- A gate that cannot pass in one environment (CI vs local) — L1's resilience check needs a browser; declare exit 77 as REFUSED, never green.
- Mutation-tests-the-clause: drive L1–L3 against a REAL generated page, not the library.
- Regen serial reds if L2/L3 generator changes run partially — whole serial, ramp first, index last.
- Renames (L3) reach canon's projection and every downstream copy — grep before, grep after.
- Schema changes (L2, L3) are Dave's per `template-dashboard-bento.meta.json:154` — ratify at his eye before populate.
