# #220 audit-wave divvy — six parallel Opus lanes, Dave's word "launch all six" (2026-08-27, ~3h to quota reset)

**SHARED RULES — every lane.** Model: Opus. Sub spend is QUOTA, use-it-or-lose-it today: be thorough. FINDINGS FILE, NOTHING RULES: no `knowledge/_rulings.json`, no `knowledge/_state.json`, no memory, no git, no promotions (every advisory gate stays advisory), no repairs outside your named region — a defect found is QUOTED + PRICED in your report, never fixed on the spot. ⛔ Never run `knowledge/_build_all.py` (partial run strands the tree). ⛔ Never regenerate `knowledge/_proforma/DataViz-interactive.html` (W-176: it is AHEAD of its generator). ⛔ Never run `_capture_gate.py --selftest` (writes a repo file). `pip install tiktoken --break-system-packages` before any gauge-touching run. Bash calls are independent and die ~178s — drive long steps as separate calls; regen experiments go to `/tmp`/`/var/tmp`, NEVER in place. Retrieval: `python3 knowledge/_memento_search.py "<q>"` → `--fetch <id>`. Verify every premise against the tree before grading it ([[premise-ages-faster-than-rule]]); name every probe ([[unmatched-grep-is-not-an-absence]]); a crash is COULD-NOT-RUN with the error quoted, never a RED.

**Report — every lane:** FILE at `notes/_subreports/2026-08-27-220-audit-L<N>.md` with a `COUNTS:` line, a findings table (each: evidence quoted · class named · fix PRICED in tokens/scope), `RULING-SHAPED QUESTIONS` (Dave's), `REPLAY-THESE` (conductor). Chat stub ≤6 lines. Your only repo writes: the report (+ L5's one review page; + report assets dirs).

**Today's three live lessons, carried into every lane:** a gate can be green over an artefact that cannot RUN (the v1.0.0 delta gate — byte/path comparison, no execution); one ruled rail lived in TWO homes (`SPACING_STOPS` in `gen_bento_matrix_217.py` AND `role_defaults_219.py` — ADR-0017); eleven stale `#767676` fallbacks resolved nowhere until a new arm looked. Sweep for the CLASS, not the instance.

---

## §L1 — GATE FLEET (correctness). Region: your report only.
Enumerate the fleet: `knowledge/*gate*`, `*validate*`, `verify_*`, `_release/*gate*`, plus every arm in `.github/workflows/gates.yml`. For each: DRIVE it (or name why unsafe/could-not-run); classify what it actually examines — PARSES/EXECUTES the artefact vs byte/path/regex comparison ([[no-gate-parses-the-artefact]] — the false-green class); does it have a known-answer test (can it SEE its own defect class)? a named consumer ([[instrument-without-a-consumer]])? blocking or advisory, and does its runbook/docstring agree with its wiring (the s218-D7 REFUSES-vs-WARNS class)? Deliverable: the fleet table + the top 10 priced correctness fixes. ⚠ Timing/runtime belongs to L6 — record rough runtimes as data, do not analyse them.

## §L2 — GENERATOR FLEET (determinism + homes). Region: your report only.
Enumerate `knowledge/**/gen_*.py` + `_gen_*.py`. For each with a committed artefact: regenerate to `/tmp` and `cmp` — byte-identical, or classify the drift (ahead-artefact, stale-artefact, nondeterminism — timestamps/dict-order/etc.). Two-homes sweep: duplicated ruled literals/constants across generators (the `SPACING_STOPS` class) — grep systematically, quote both homes. Orphans both directions: generator with no artefact, artefact whose named generator is gone. ⛔ W-176 fence absolute; anything whose regen would touch the live tree gets the /tmp copy treatment or COULD-NOT-RUN.

## §L3 — TOKEN & CANON HEALTH. Region: your report only.
Run the existing instruments and RECORD (never repair): `_gate_minted_consumption.py`, `_validate_hidden_display.py`, `_validate_token_forks.py`, `_validate_token_tiers.py`, the dangling-var checks, P-7/P-8 probes if runnable. Then the class sweeps: `var()` references that resolve to nothing per theme × mode (the SILENT BLACK class); hex literals in canon/showroom that bypass tokens (excluding ruled mint comments); stale fallback values (the `#767676` class — a fallback canon never resolves); per-theme override integrity after the #220 mints (does every theme × mode resolve every caption/surface var it names?). COUNTS per class, worst 10 quoted.

## §L4 — DESIGNER FIRST HOUR (the shipped pack, as shipped). Region: your report only; work in /var/tmp.
Extract `apollo-spider/dist/Apollo-Spider-v1.0.1.zip` fresh. Follow the pack's OWN instructions literally, as a designer would — README, PROVENANCE, the Gumdrop cold-start/first-chat script, the gate-runner, the skills' walked exercises — command by command, quoting each instruction then what actually happened. File every stumble: wrong path, missing file, unclear step, a doc naming a repo-only tool, an instruction that assumes VS Code state you can't simulate (that part stays UNPROVEN, declared). ⛔ Do not fix the pack; do not touch `apollo-spider/` in the repo. This is the closest pre-designer proof of the Copilot bridge we can get.

## §L5 — THE CONSOLIDATED SITTING PAGE. Region: `reviews/SITTING-220-2026-08-27-v1.html` + your report + assets.
Read every #219 and #220 filed report's `RULING-SHAPED QUESTIONS` section (`notes/_subreports/2026-08-2[567]-*.md`) plus the standing 33-call sitting index (`reviews/SITTING-219-2026-08-25-v2.html`, W-184). Build ONE review page: every OPEN question, deduped with receipts, grouped by surface (bento/gallery · charts · library/taxonomy · release/pack · machinery/gates · gauge/process), each with its live-page link and what a ruling would mint. ⛔ CLOSE NOTHING: where a #220 ruling (s220-D1/D2, read from the store) answers an old question, mark it CANDIDATE-CLOSED with the receipt for Dave's confirm. Plain-prose labels, IDs small-print ([[feedback-decisions-in-plain-prose]]); light+dark; copied-from-artefact for any specimen. State the total count honestly ON the page.

## §L6 — EFFICIENCY LEDGER. Region: your report only.
Measure (never trim): wall-clock per selftest/verifier/gate (time each; the ordered regen serial end-to-end); CI `gates.yml` job graph (what runs serially that could parallelise, what runs twice); page weights (showroom pages, library index, review pages — top 20 by bytes); the chain economics (`_gen_chain.py` slice vs wrapper — read its own printed figures, do not re-derive); sandbox costs (the ~4-call render re-stage, the tiktoken install). Deliverable: a priced trim list — each trim with measured saving, risk, and WHO rules it (most are Dave's). ⚠ Gate correctness belongs to L1 — you own the stopwatch, not the verdicts.
