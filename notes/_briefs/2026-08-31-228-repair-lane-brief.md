# #228 REPAIR LANE BRIEF — Dave's confirmed set, ready to enact

```
provenance: chat #227 2026-08-31 — Dave's confirm-pass paste (Confirmed 9/9), rulings s227-D7 + s227-D8
status: ruled (the set below is CONFIRMED, not floated — enact without re-asking)
```

Written at the #227 wrap so #228's first act is spawning ONE Opus repair sub with this file as
the brief body. Everything here was confirmed by Dave against real renders on
`reviews/CONFIRM-PASS-2026-08-31-v1.html` (W-298). Nothing here is open except where marked.

## The confirmed enactments

1. **Contract repairs, 9 of 10** — apply the red team's written edits for **C2, C3, C4, C5, C6,
   C7, C10, C12, N1** (before/after in `notes/_subreports/2026-08-30-227-contract-redteam.md`).
   ⛔ **C8 is HELD**: Dave said *"lets just do the check on C8 first"* — bring its exact
   before/after to him in chat, one message, before it lands. Do NOT apply C8 unchecked.
2. **B1 remedy** (s227-D6): the pack manifest gains a **cold-start group** claiming all 7
   `apollo-spider/cold-start/` files + `apollo-spider/skills/grill-me/brief-template.md`;
   grill-me gets its `.github/prompts/` shim + skills-table row (the two pre-existing holes).
3. **B2 remedy** (s227-D5): `gen_projections.py`'s copilot projection becomes the MERGED boot
   file (Memento boot + skills table + design contract, one generated file, both sources).
4. **S1 remedy**: the one-sentence REPORT-TEMPLATE box-2 fix (records whether skills were
   reachable, not just the lane word).
5. **W8**: `gen_canon_tokens.py` gains `--check` (red team's written edit).
6. **L8**: search radius moves from the base rule to the **boxed variant only** — underline
   stays straight. Fix at the snippet emitter, regen canon + cascade + showroom (remember the
   cascade! lane 1's binds were inert in console until it ran — same trap waits here).
7. **s227-D7**: `SEGMENTED_CONCENTRIC_SCALES` extends to **all four scales** — m thumb 8 (pad 2),
   l thumb 8 (track 12 − pad 4). xs/s already concentric. Re-run `--assert-mint`, regen consumers.
8. **L1/L2 FAB**: reveal mode B (hot corner, 72px zone) becomes the default config;
   the FAB button face uses a **library icon** (Dave: "use an icon from the library though" —
   pick the settings/gear icon from `knowledge/assets/icons/`, cite which and show him).
9. **s227-D8 (W5 safe path)**: (a) additive theme-key ALIAS — `data-apollo-theme` accepts
   `legacy` AND `common`, everything new emits `common`, nothing existing breaks; find the
   alias point at the cascade/attribute selectors, enact additively; (b) build the
   **reconcile script** the pack ships (rewrites `legacy`→`common` in a designer's project,
   dry-run by default, one flag to write); (c) the FULL rename is a separate post-Sept-1 lane
   (task #9) — do not start it.

## Then the cut (s227-D6, Dave's release word already given)
Repairs green → manifest regen (expected: skills 5→6, new cold-start group, `/carries/what`
drift clears) → dry-run byte-match → **v1.0.4 cut**. Then Dave's live cold test on the second
machine is the acceptance test before Tuesday. CI read-back owed to Dave in chat (s203-D1).

## Standing context #228 needs
- CI on `28c10c8` was **in_progress** at the #227 wrap — read it back before pushing again.
  Expected: gates step GREEN for the first time since #225 (wrapper C landed); release step
  still RED until this lane's manifest regen (the known one-field drift).
- Renders WORK: `_RUNBOOK-render-verify.md` § SIXTH STRATUM (+ the TMPDIR=/dev/shm line).
- VM disk still 100% — everything mount-side, subs briefed accordingly.
- Dave's desk (not this lane): FAB W9–W12 polish · the 20 DISCUSS sitting agenda ·
  W3 zero-inset readings page (owed for his point) · bento exemplar lane (task #8) ·
  dashboard-diff decision page paste (he has not worked that page yet — do not nag, it keeps).
