# #218 build brief — the phantom surfaces (RULED s218-D4: build the missing surfaces)

Dave's call on the seven controls that promise surfaces absent from the DOM: **build them** —
seven per-component design pieces, PROPOSED to his eye, never silently ruled. The seven, from
the W3 receipt (verify each in the file before building — premise ages faster than rule):

1. **Headers** — "More options" → a real menu (APG menu-button pattern).
2. **Navigations** — Search and Account → their disclosure surfaces (search field reveal;
   account menu). ⚠ This file gained behaviour at #218 W3 — extend, don't rewrite.
3. **Avatar-group** — "+N" → the overflow list surface.
4. **Standing-order-mandate-row** — "Manage" → its action surface (menu or panel; propose the
   cheaper honest reading, show it).
5. **Kpi-tile** — table CTA: its own header forbids inventing the panel — so the PROPOSAL here
   is the panel DESIGN as a specimen beside the tile, clearly marked, with the header's
   prohibition quoted; nothing wired until Dave rules the panel.
6. **Confirmation** — the documented Replay button is missing from markup: add it and its
   behaviour per the component's own documentation.
7. **Timeline** — "Load older activity" → a real load surface (demo dataset appended, honest
   in-file declaration like Video-player's demo clock; the header's aria-current/progressbar
   prohibitions stand).

Rules: each surface in the component's own idiom, self-contained inline script (ARIA-first,
≤16KB, no polling/network — the W3 conventions), four themes × light/dark, token-manifest
updated where wording goes stale (the Payment-card knownFindings class). Every built surface is
labelled **PROPOSED (s218-D4)** in its header — Dave ruled the CLASS (build them); each
DESIGN is his to sign at a sitting. One verify script `verify_phantom_surfaces_218.py`, green +
break arm red by name, driven both ways. Receipt at
`notes/_receipts/2026-08-25-phantom-surfaces.md` with per-surface decisions-for-Dave.

Fence: no rulings, no tokens/canon, no registry/serial, no store/lane/GM/LS/memory edits, no
commit/push, do not touch Command-palette or Chart-bar (enactment sub's region) or any wave-3
lane component. /var/tmp session-suffixed; render env recipe per the photography brief.
