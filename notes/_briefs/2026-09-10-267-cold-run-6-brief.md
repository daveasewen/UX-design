# #267 lane C6 brief — THE SIXTH COLD RUN: the frozen prompt against the v1.0.9 ZIP, blind, LIGHT THEME

**Model: Opus (per #250). Conductor: Fable (#267). Dated period record (ADR-0017). Read-only against canon/knowledge — MOVES NOTHING in the library, RULES NOTHING.**

## WHY

v1.0.9 releases **tomorrow, Fri 2026-09-11**. Lane R (#266, `notes/_lanes/2026-09-10-266-R-v109-rating.md`) rated it SHIP-WITH-CAVEAT: ten gates pass, but ⛔ **the frozen prompt has never been driven against the v1.0.9 pack**. The last cold run is the FIFTH, `notes/_subreports/2026-09-08-258-C5-cold-run-5.md`, against v1.0.7. This lane is the AFTER picture for v1.0.9. Nothing is fixed here.

## THE PACK UNDER TEST — the ZIP, not the tree

Unzip `apollo-spider/dist/Apollo-Spider-v1.0.9.zip` into a scratch dir outside the repo (e.g. `/tmp/ads-1.0.9/`). Record the zip sha256 and the top-level listing in the READ-LOG. **Every read in the arm comes from the unzipped pack.** If a file the skill points at is missing from the zip, that is a FINDING, not something to fetch from the repo.

## THE PROMPT (verbatim, FROZEN — do not improve it)

```
/generate-from-canon build me a financial dashboard for corporate international banking. Please make the all the interactive elements work such as filtering and navigation.
```

## ONE ARM — BLIND (the VS Code condition)

Follow `notes/_briefs/2026-09-08-258-cold-run-brief.md` § ONE ARM exactly, with these overrides:
1. Output root `outputs/coldrun-267/arm-A-blind/` (dashboard.html, READ-LOG.md, brief.md with grill-me defaults marked *skipped/defaulted*, the Gaps list).
2. Reads come from the unzipped v1.0.9 zip only.
3. Forbidden reads unchanged: no `notes/`, chain, GM, LS, reviews, decision history, rulings, any `_*.py`, no `_memento_search.py`. No render, gate, playwright, validator during the build.
4. Do NOT read `outputs/coldrun-258*/` or the C5 subreport until the arm is frozen; then you MAY read them for the comparison section only.

## MEASURE (after the arm is frozen — never touch it again)

Same seven measurements as #258, at 1440 and 820, **light theme leads (the demo theme), dark second**: render → gates (`knowledge/_validate_screen.py`, `_validate_receipt.py`, exit codes + first 20 lines; if a gate insists on writing under `knowledge/`, redirect and declare) → inventory (used / available-but-unused) → chart completeness (snippet `<script>` sha256 vs source in the ZIP) → interactions driven with playwright (every nav item, filter, tab, a grid sort; works / dead / missing; persistence = state survives nav + reload) → data believability → four scores 0–3 (rich / full / persistent / interactive), each with the one fact that earned it.

**Specifically re-test the two live defects from C5 and say whether each reproduces on v1.0.9:** F02 (controls rendered after load are dead — `kpi-cta`) · F01 (`data-theme` on `<body>` beside a `.cn-*` scope class kills dark). These are operator-card items for tomorrow, so the answer must be a playwright assertion, not a reading.

Playwright: `python3 -c "import playwright"` first; if fresh, see memory hook `chromium-in-sandbox-recipe` / `knowledge/_ROBUSTNESS-PORTABILITY.md`. `pip install tiktoken --break-system-packages` if any gauge refuses.

## DELIVERABLES
- `outputs/coldrun-267/` — arm, renders, `drive.json`, logs.
- `reviews/COLDRUN-267-2026-09-10-v1.html` — ONE page, swiss-design-system idiom (`.claude/skills/swiss-design-system`; bake `type.css` into any srcdoc per #261): renders per breakpoint/mode, inventory, chart completeness, interactions, scores, **C5 (v1.0.7) → C6 (v1.0.9) delta table** (same rows, both columns, differences named as facts), **F01/F02 status**, **Findings** (numbered, one fact + the file/line that proves it), **Ruling-shaped questions** for Dave — do NOT answer them. Close with a one-line SHIP / SHIP-WITH-CAVEAT / HOLD recommendation with the fact that earns it — a recommendation, not a ruling.
- `notes/_subreports/2026-09-10-267-C6-cold-run-6.md` — done / NOT done (declared) / tokens / paths.

## RULES
- ⛔ No edits under `knowledge/`, `showroom/`, `apollo-spider/`, snippets/metas/canon. No `_build_all.py`. No `gen_kg_edges.py`. No commit, no push.
- ⛔ Rule nothing of Dave's. Claims carry a probeable token (path, sha, exit code, playwright assertion).
- Mid-lane check-in: at the seam between build and measure, write your context use into the subreport.
- Return to the conductor: review path, subreport path, dashboard path, four scores with the earning fact, the C5→C6 delta in one line, F01/F02 reproduce yes/no, top three findings one line each, the recommendation line, tokens used.
