# #218 crank divvy — four lanes, three windows, ONE conductor commits

Opened by Dave at #218 (quota panel all-models 38% / Fable 36%, resets Thu — the #60
production-crank case, his word: "We need to crank"). Cap 3 windows (ruled, revisable).
⛔ **ONLY THE CONDUCTOR WINDOW (#218/#219 seat) COMMITS OR PUSHES.** Worker windows leave their
tree changes in place + write a handoff receipt at `notes/_receipts/2026-08-2X-crank-<lane>.md`;
the conductor reconciles path by path at each seam. Regions are DISJOINT — a worker needing a
file outside its region STOPS and returns the finding instead of touching it.

## Window C (conductor — this seat)
- **Gates wave** then **build-verdict push**, sequential, one Opus sub each.
- Region: `knowledge/_capture_gate.py`, `knowledge/_state.py`, `knowledge/_gm_move.py` parsers,
  `knowledge/_build_all.py` + failing step targets, `knowledge/_gen_chain.py` selftest.
- Also: all commits, CI read-backs, ruling inscriptions, memory.

## Window 2 — CHARTS (paste-in opener below)
- Lane: `lane-2-apollo-charts` §C·1 strands (a)–(d): chart expansion · wave 3 · templates/shells
  · enact window. Consult `knowledge/_proforma/_DATAVIZ-DECISIONS.md` § Open/pending first.
- Region: `knowledge/_proforma/`, chart snippet sources + their showroom pages, dv-* verify
  scripts. ⛔ NOT: canon.css/tokens (findings → `knowledge/_DS-IMPROVEMENTS.md`), not the
  library generators, not gates, no rulings, NO COMMITS.
- Heatmaps stay PARKED; hover tokens are SETTLED (residuals in the memory file's named homes);
  series-3 4.61:1 is ruled — don't lighten.

## Window 3 — BEHAVIOUR WAVE (paste-in opener below)
- The 46 no-behaviour-JS snippets minus lock-ups (blocked on Dave's 3 titles): sidebar-nav
  destination selection + aria-current, command-palette, and kin — real behaviour, a11y-correct,
  self-contained per snippet (the architecture question W-99o §8 is OPEN: default to
  self-contained, name any shared-core temptation as a finding).
- Region: snippet sources + their showroom pages + per-snippet verify. ⛔ NOT: canon/tokens,
  library generators, gates, no rulings, NO COMMITS.

## Both worker windows
- Boot: read `_CHAIN.md` only; run `knowledge/_checkin.py` at opener and inside any lane >15K.
- Delegate builds to Opus subs by default; rulings-shaped questions return to Dave in the
  conductor window, never decided locally.
- Wrap: NO wrap ritual in worker windows — the receipt file is the handoff; the conductor's
  wrap carries the record.
