# #250 delegated wrap brief — run the FULL wrap ritual

**Model: Opus 5 (rule 7, #250: every lane Opus; Fable = conductor). Conductor: Fable (#250), FILL 145,248 at the brief cut — under the 150,929 advisory, delegated by rule 7 not by overflow. Recall probe NOT planted (declare, 0 claims). Boot 69,293 real — state ONCE, in the stratum.**

## 1. RULING — ONE, Dave's, to INSCRIBE in `knowledge/_rulings.json` as `s250-D1` in his VERBATIM words (transcription, not judgment; the text below is fixed by the conductor — add nothing, rule nothing else).
Context: the #96 byte-gate fork (a re-dial · b waiver · c shave · d park) + the conductor's fifth option (e). Dave: *"do you have a recommendation, i like cheap completeness, would that be c?"* → conductor recommended (e): the gate measures CODE-ONLY bytes and sums the page `consumes`-aware, caps unmoved → Dave: *"e"*. `s250-D1` = **option (e) ruled; `MAX_BYTES` 16,384 / `PAGE_BYTES` 34,816 UNMOVED; the unit is code-only bytes.** `by` Dave · `date` 2026-09-06 · `status` ruled. Read the store's record shape from `s249-D6` first and match it exactly; parse count must go 370 → 371.
NOT a ruling (Dave: *"i might be wrong dont follow me blindly"* then *"go for it and then we can get back to Apollo"*): `MODEL-ROUTING.md` rule 7 (Opus lanes · premise probe at the brief · verifier only at canon · `CITES:` line) is a WORKING SHAPE by addition, no id. Say so in the stratum.

## 2. WHAT #250 WAS (WHAT here; WHY in `_DECISION-HISTORY/2026-09-06-250-adversary-at-the-brief.md`, which you write)
- Opener: chain read (#249), `_checkin.py` boot **69,293 real**, tiktoken installed. Dave: *"fable is racing ahead"* — Fable 51% / all-models 27%, *"lets start rinsing opus 5 subs on max and keep you for sound judgment, i need fable until thursday at 8ish"*. Three Fable 5.1 doc URLs fetched IN-CONDUCTOR (≈35K FILL — a mistake, named): findings = API knobs only; two loop behaviours worth a brief line (one-tool-call-per-turn; whole-file rewrites).
- Dave: *"maybe we use the adversarial PM tactic we used before"* → conductor retrieved `s204-D1` / `s215-D3` / rule 5 → pushed back: our failures were PREMISES (#244, #238, #202), an output verifier passes all three → **adversary at the BRIEF**, verifier only at canon, coherence = ruling ids in briefs + `CITES:`. Dave: *"go for it"*.
- Landed: `MODEL-ROUTING.md` rule 7 (by addition) · `notes/_briefs/_TEMPLATE-premise-probe.md` (NEW) · memory `opus-lanes-premise-probe-250` (conductor's, NON-REPO, written).
- **Lane PROBE (Opus, 99,568 quota):** `notes/_subreports/2026-09-06-250-PROBE-byte-gate.md` (findings 11 · ruling-shaped 1 · UNPROVEN 3). Premise "(c) 3.7 KB shave clears both caps" FALSE — file yes (16,068), page 36,710 > 34,816 by 1,894. No waiver form exists. #96 re-dial never finished (gate printed "32 KB"). `check_group` ignored `consumes` — only Chart-donut loads 40,410. `_rulings.json` has no byte-cap ruling; `#96-D5` lives at `notes/_MEMENTO-DECISIONS.md:3874`.
- **Lane E build (Opus, 115,119 quota):** `notes/_subreports/2026-09-06-250-E-byte-gate-unit.md`. `_validate_behaviour.py` +317/−44: code-only scanner, `consumes`-aware `check_group`, raw+code-only printed, "32 KB" strings → 34 KB, `--mutate code-pad|comment-pad|string-slash` + `--selftest` (9 new bites; two pre-existing pads were COMMENT-padded and would have gone silent — converted to `CODE_PAD()`). ADR-0015 Amendment 3 (+129, by addition; carries the "re-dial in disguise" case and names the Amendment 1 LEGIBILITY conflict as re-pointed, not answered). `_BEHAVIOUR-GATE.md` regenerated. **Gate exit 0: behaviour 13,048 · legend 7,734 · sweep 3,889 code-only; worst member Chart-donut 24,671.** No canon `.js` byte touched. Neighbour gates: snippets 0 · composition 1 (the 3 pre-existing C9 reds, untouched).
- **Lane V verifier (Opus, 132,108 quota, cold seat):** `notes/_subreports/2026-09-06-250-V-byte-gate-verifier.md` + `…-V-byte-gate.challenges.jsonl` (EVIDENCE GATE PASS, 22 rows). 13 claims: 12 CONFIRMED, 1 CONFIRMED+REFINED, **0 CONTRADICTED**, 5 NEW. 15 adversarial JS constructs counted right; six under-count attacks failed (errs larger, never smaller — probed). All 15 members' `consumes` match their injected blocks. Old gate exit 1 / new exit 0, same tree. V-MUT-1 page clause bites on live data; V-MUT-2 cap+1 RED / cap+0 GREEN. **V-MUT-5: page sum TRUSTS `consumes`, never reads the snippet — under-declaring goes green (unguarded invariant, not live).** Refinement: composition selftest anchor found ZERO times, drift `data-c` 3→1 from `deb172a` (#247) — dark two sessions. The verifier's HEAD-gate probe overwrote `_BEHAVIOUR-GATE.md`; regenerated; temp copies deleted.
- Subs total **346,795 quota** (n=3, harness counter). Conductor FILL at brief cut **145,248 real**, 0 Fable lanes.

## 3. STILL DAVE'S (carry, by addition to `_CARRIES.md` § residual → #251): the `consumes` guard (V-MUT-5) · legibility unmeasured (ADR-0015 A1 vs A3) · `_validate_composition.py --selftest` broken since #247 · **DP-08 A+B** (`s249-D1` "maybe") · `s249-D3` R/A signal (W4) · `s249-D6` item 6 visually · META's 7 RSQs · DESK's 7 RSQs · W3…W8 · 12 members unproven with the release · themes · C9 (3 reds) · Q8–Q16 · radius · cold test v1.0.6 · [18] · 4px · parts-with-addresses · dream pass 11's four · midnight-wrap stamp (#241, age 9) · strays `_tools/` (92 KB) + `_screen-gate/dashboard.md` · push (`origin/master` 4dc12b6; HEAD 49ded37 is FIVE ahead → SIX after this commit; CI read-back `s203-D1` owed for all) · the ceiling arm (RED after #247/#248/#249; #250 boot 69,293 is UNDER 70,000 — grade it, move nothing).

## 4. NEXT: **DP-08 A+B** (`s249-D1` "maybe"), on Opus with a premise probe first. Next title: `Apollo - #251: DP-08 A+B — premise first`.

## 5. THE RITUAL, WHOLE — steps 1→5 per `knowledge/_RUNBOOK-context-gauge.md` and the wrap runbook it names (grep for it; read the runbook, not the hook), `--wrap` (expect REFUSED on the ceiling arm — then commit in the #243 form and SAY SO), subject READ BACK from `git log`, both title lines returned to the conductor for chat. **No push.** Declare every skip WITH ITS SIZE. Memory: the CONDUCTOR wrote it — declare 0 bytes at this seat. Do NOT re-inscribe `s249-*`. Do NOT rule anything in §3. Do NOT edit lane reports (ADR-0017). Do NOT run `_build_all.py`. Rebuild `_memento-index.json` LAST (step 2g). Commit includes: lane E's three files, rule 7, the template, this brief, the three reports + jsonl + assets, `_CHAIN.md` regenerated. The three jsonl files dirty before the lane (`_graph-mark-observations`, `_REHEARSAL-LOG`, `_dream/_GRADE-DECISIONS`) — inspect `git diff` on each; commit if they are gate/tool output from today's runs, otherwise leave and declare. Batch independent reads; Edit, never Write, on existing files.

Return under 350 words: `_rulings.json` count, commit hash + subject read back, `--wrap` result, `_CHAIN.md` size line, declared skips, both title lines.
