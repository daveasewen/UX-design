# #220 sub brief — Spider/Gumdrop v1.0.1: fix at cause, sweep, add photography, PREPARE the bake

**Model: Opus. Dave's word for the bake is GIVEN (chat, 2026-08-27). Naming: Apollo - Spider v1.0.1 carrying Memento - Gumdrop v1.0.1 (patch stays on the mission; Snoopy is reserved) — conductor-recommended, Dave has veto until the bake commit lands.**

## Scope (all fixes AT CAUSE — never a plaster; s219-D5's own rule: cut from a commit whose gates are green)

1. **The broken import, at cause.** `memento-package/claude-plugin/memento/machinery/_gen_chain.py` dies: `No module named '_could_not_ask'`. The sibling repo copy runs, so the dependency exists and never travelled — find where `knowledge/_release/_gen_pack_manifest.py` (SEED_PREFIXES et al.) decides what accompanies the machinery, and fix the DECISION so any packed module's imports travel with it. Reproduce the failure from the v1.0.0 zip first; prove the fix by importing from a fresh stage.
2. **The blind gate, at class.** The delta gate was green over a file that cannot import — it compares paths/bytes, never runnability. Build `knowledge/_gate_pack_imports.py` (ADVISORY at birth — promotion is Dave's word, you do not wire it blocking): stage-import every packed `.py` in an isolated dir, refuse loud and named on failure ([[a-crash-is-not-a-fail]]: refusal with the module named, never a silent pass). Run it on the v1.0.0 zip (expect exactly the known red — that is the gate's known-answer test) and on the new stage (expect green). Drive it, do not just selftest it — a gate that does not run cannot fail.
3. **Two machinery nits, at cause.** (a) The N1/seam8 stage-2 recipe cites `knowledge/_make_review.py`, which does not exist — find what it meant and correct the recipe text at its home. (b) The frozen-release ledger warning names the wrong mechanism (replay report finding — read it at `notes/_subreports/2026-08-27-220-replay-discharge.md`, quote it, fix the warning's text/code at cause).
4. **The photography group (Dave's ask: "30 or so… so that the bentos and gallery work").** All from `knowledge/assets/photography-web/` (already JPEG, w1600). MUST include all 15 `gen_bento_roles_217.SPECIMEN_FILES` (the pages reference them BY NAME). Add ~15 more: ≤300KB each, aspect spread (portrait/landscape/square per the manifest's own data), subject variety per manifest descriptions; document the selection criteria and the per-file sizes in the report. Rows enter via the manifest GENERATOR (the manifest is generated; a hand edit is a RED). Pack them at the same relative path the shipped pages resolve (`knowledge/assets/photography-web/…` inside the pack root as the pages expect) — then PROVE it: render one packed bento/gallery page from a fresh stage extraction and count zero broken images. Measure the final zip (bytes + entries) — a brief's prose count is not a measurement.
5. **Cold-start drive.** From a fresh stage: run the Gumdrop cold-start path far enough to prove `_gen_chain.py` imports and runs against the empty stores, and `_memento_search.py` answers. The Copilot bridge stays UNPROVEN — declare, do not claim.
6. **Bake PREPARATION only.** Stage everything, run the release gates (`_gate_release_audit.py --check` and `--selftest`, release CI's local halves, the frozen gate's own checks) to green. ⛔ Do NOT commit, do NOT run the final `--release` bake, do NOT write the ledger row — the conductor commits, then continues you for the bake at the landing commit. v1.0.0's zip and ledger row are FROZEN — content-only movement, never edited.

## DO-NOT-RULE / DO-NOT-TOUCH
No git. No memory. No `_rulings.json` / `_state.json`. No promotions (the new gate stays advisory; both #219 advisory gates untouched). No edits to `apollo-spider/dist/Apollo-Spider-v1.0.0.zip` or its ledger row. No `_build_all.py`. No regeneration of `knowledge/_proforma/DataViz-interactive.html`. Regions you own: `knowledge/_release/*` generator edits, `knowledge/_gate_pack_imports.py`, the stage dir, the recipe/warning text homes named above, the manifest-generator photography addition, and your filed report. Name any generator you run and the regions it owns BEFORE running it.

## Pitfalls / consequences (mandatory, Dave #165)
- A hand-copied module instead of a generator fix guarantees recurrence at v1.0.2 — fix the decision, not the file.
- The known-answer test is the proof the gate can see the defect class; a gate green on both zips proves nothing.
- Packing images at the wrong relative path produces a page that is green in the repo and broken in the pack — only the fresh-stage render proof counts.
- Sandbox: calls die ~178s, drive steps individually; `pip install tiktoken --break-system-packages` first if any gauge script runs; nothing survives a call boundary.

## Report
FILE at `notes/_subreports/2026-08-27-220-bake-v101.md`: COUNTS line, the reproduced red + fixed-green evidence per item, image selection table, measured stage figures, `RULING-SHAPED QUESTIONS` (Dave's), `REPLAY-THESE` (the conductor's bake-at-commit steps, exact commands). Chat stub ≤6 lines.
