# Brief — #230 cut lane (Opus): bake v1.0.5 so the demo pack carries tonight's fixes

provenance: 230 · 2026-08-31 · conductor Fable · row W-319
Dave's word: "What the heck am i testing 1.04 for?" — the demo and rehearsal run on v1.0.5.

## The job

Cut Apollo-Spider v1.0.5 by the PROVEN v1.0.4 procedure — follow the receipts, do not
re-derive: `notes/_subreports/2026-08-31-228-cut-v104.md` is the reference run (dry-run
twins → fingerprint on the go/no-go page → bake → `cmp` byte-match over the whole zip).
The pack must carry tonight's enacted fixes (W-318: dv-behaviour tooltip re-home, SKILL.md
step-3 + legacy→common, screen-gate, toolbar radius, meta radius key) and the regenerated
canon they produced.

## Hard rules (release discipline — machine-enforced, packs are RELEASES)

- ⛔ **`RATIFY_IDS` moves to `s228-D4` at THIS cut** — the v1.0.3 pattern: the key moves at
  the NEXT cut, never by re-baking the ratified zip. Check `build-designer-pack.sh` before
  anything.
- Roster stays as v1.0.4 shipped it (58 held under `s228-D5`; explicit-membership fix is
  W-303 and is NOT this lane).
- The baked zip is never edited after `cmp`; the dry-run twin proves the bake.
- ⛔ **THE BAKE IS PROPOSED, NOT RATIFIED, until Dave's fresh word in chat.** Produce the
  go/no-go page (`reviews/RELEASE-SPIDER-*` pattern, regenerate don't hand-edit), the
  fingerprint, and STOP. The conductor relays; Dave ratifies.
- Survey/CI reds: the known standing shape (gates job red / release+render green) is
  DECLARED, not yours to fix; a NEW red in the release half stops the lane, loudly.

## DO-NOT-RULE

No `_rulings.json`, no W-rows, no memory, no git operations (conductor commits), no roster
edits, no canon/snippet edits (the enact lane is done — you package it), no
`_build_all.py`.

## Report

`notes/_subreports/2026-08-31-230-cut-v105.md` — COUNTS: (files in zip, bytes, sha256,
survey reds before/after), REPLAY-THESE:, RULING-SHAPED QUESTIONS. Chat STUB: version,
sha256 head, byte-match verdict, ratify-key state, and the go/no-go page path.

## Pitfalls — replayed

- The #228 cut hit six survey reds and closed five by measurement — read its report §
  before treating any red as new.
- tiktoken FIRST in a cold sandbox; ~178s call wall — the bake script's long steps may need
  driving in stages exactly as the v1.0.4 report receipts.
- A version string is not evidence — the sha discriminator set is (the #224 forensics
  pattern); stamp both in the report.
