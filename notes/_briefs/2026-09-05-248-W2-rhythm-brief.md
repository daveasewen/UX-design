# #248 · W2 — BENTO RHYTHM: ONE TILE LEADS. Lane D (the edit) → Lane R (the re-run) ‖ Lane J (the blind judge)

Plan: `reviews/DP-TEST-PLAN-2026-09-05-v1.html` § 01 W2 — DP-14 15 16 18 (DP-17 HELD for W6). Ruled shape from W1: a ROW, count flexible (`s247-D4`); status tiles not hidden in the row (`s247-D3`, design NOT drawn — do not draw it here); a grid at 520 acceptable (`s247-D5`); the spans dial is a TRIAL (`s246-D3`). Principles page: `reviews/DASHBOARD-PRINCIPLES-2026-09-05-v1.html`. W1 shape to copy: `notes/_briefs/2026-09-05-247-W1-density-brief.md`.

Register probe (#244 rule, done by the conductor 2026-09-05): `knowledge/_enactment-register.json` carries 0 rows matching bento / kpi / span / derived — nothing in W2 is a typed count.

**Opening fact (W2's reason):** lane J's blind verdict on W1 — *"the six tiles are uniformly weighted so nothing leads; promote Group liquidity (larger number, wider tile)"* (`outputs/w1-density/judge/verdict.md`). DP-07/DP-14 against DP-06: the conflict ledger's first row.

## THE ONE EDIT (lane D)

File: `knowledge/snippets/Template-dashboard-bento.reference.html`. Today the wall below the lead row is GROUP 2 evidence `data-c="3"` (L939) + GROUP 3 context `data-c="3"` (L984) — an even 3+3 split, which DP-15 names as the thing a row below the lead row does not do.

1. **DP-14/15 — the wall:** GROUP 2 evidence → `data-c="4"`, GROUP 3 context → `data-c="2"` (the plan's defaults). Check squareness at every band (6 → 3 → 2 → 1) by reading the band rules L377–385 — 4+2 at six cols; at three cols both collapse (4→3, 2→2 = 3+2 orphan?) — MEASURE it, render it, and if the 3-col band orphans, say so with the px and do NOT add a band rule. Report the option you land and why.
2. **DP-16 — edges on column lines, tops aligned, no orphan cell:** confirm by render at four bands; a Playwright assertion on tile `getBoundingClientRect().left` against the column lines at 1440 is the probeable token.
3. **DP-18 — the stretch arm:** the context rail (GROUP 3) holds two stat-cards stacked; the evidence chart is taller. Does the rail stretch its cards to fill the chart's height (empty ground), or answer with content? Render before/after; if `align-items`/`height:100%` (L750–751) stretches a tile past its content floor, that is a FINDING — report the px of empty ground per tile. Fix ONLY if it is one CSS line inside this snippet and you say which; otherwise carry it as an RSQ.
4. **The spans dial (`s246-D3`, TRIAL):** find where the edit rails live (grep `spans` / `chord` / `--bento-` under `knowledge/` and `apollo-spider/`); report path + line + whether a 4+2 · 2+4 · 3+3 · 6 chord exists. Do NOT build it if absent — report absence with the grep, it is Dave's TRIAL to scope.

**Arm L (render-only, NOT landed):** copy the edited snippet to `outputs/w2-rhythm/arm-L/Template-dashboard-bento.LEAD.html` and in the COPY promote ONE lead-row tile — "Closing balance" (L856, the has-cta tile) — to `data-c="2"` with the non-compact figure, leaving five compact tiles (2+1+1+1+1 = 6 cols). Render at 1440 light only. This arm exists for the judge; whether a tile leads the row, and which, is Dave's (`s247-D4` rules the row, not the weights). `git diff --stat` must still show ONE file.

Update the GROUP 2/3 comments (L938, L983) to say what they now are. Fix the stale "only 6 and 3" comments at L73–74 and L837 ONLY if Dave's carry says to — it does not; leave them and name them as still stale.

## GATES (lane D, after the edit)
- `python3 knowledge/_validate_snippets.py` and `python3 knowledge/_validate_composition.py` — exit code + first 20 lines each.
- Render at 1440 · 1100 · 820 · 520, light + dark → `outputs/w2-rhythm/template/*.png` (runbook: `knowledge/_ROBUSTNESS-PORTABILITY.md`; ⛔ `set_content()` is banned — `goto("file://…")`).
- Measure at 1440 light: first-chart-y (W1 landed 516.5 — must not regress), the wall's tile lefts vs column lines, and empty-ground px per context tile.
- ⛔ `_validate_screen.py` writes into `knowledge/` — restore via `git show HEAD:` if run, and say so. Do NOT run `_build_all.py`.

## RE-RUN (lane R, after D returns) — the #246 lane-B / #247 lane-R protocol
Brief: `notes/_briefs/2026-09-05-246-lane-B-baseline-brief.md` — P0 verbatim (typo included), arm A blind then arm B sighted, same fence, measurement 1–7. Differences ONLY:
- Output root `outputs/w2-rhythm/` (arm-A-blind · arm-B-sighted).
- ⚠ **The blind-arm harness defect from W1** (`Tabs.reference.html` L80/L120 demo width → one column, 2719px page): do NOT edit Tabs; REPORT whether it recurs with the y and page height, so the arm is comparable to W1's 1/1/0/2, not fixed under it.
- Score sheet `notes/_dp-scores/w2.json` — 29 rows + seeds 1..6 + richness per arm. DP-14/15/16/18 rows MUST carry file+line facts. Seed 4 (symmetric wall) must not recur — say pass/fail with the spans read from the generated file.
- Review page `reviews/W2-RHYTHM-2026-09-05-v1.html` (swiss-design-system, render it before shipping — a W1 lane shipped a page it never rendered): W1 sighted beside W2 sighted at 1440 light with a 900px fold line; arm L beside the landed row; first-chart-y W0 837.8 → W1 678.6 → W2; seed 4; richness vs W1 2/2/0/2; the 29-row sheet; CONFLICT LEDGER (carry W1's row, add W2's); RSQs as reveal accordions.
- Subreport `notes/_subreports/2026-09-05-248-W2-R-rerun.md` in `_TEMPLATE.md` form WITH `COUNTS:` and `RULING-SHAPED QUESTIONS`.

## JUDGE (lane J, beside R, not after)
Gets ONLY three unlabelled renders in `outputs/w2-rhythm/judge/` — W1 sighted 1440 light, W2 sighted 1440 light, arm L 1440 light — copied as `x.png` / `y.png` / `z.png` in a randomised order (record the seed + key in `KEY.md`, appended by R after J returns; J never sees it). Instruction verbatim: "Three dashboards. Which one has a tile that leads, and does the rest of the wall read as ordered or as scattered? First thing you would change on each. Four lines." Writes `outputs/w2-rhythm/judge/verdict.md`.

## RULES (all lanes)
- ⛔ Edit NOTHING under `knowledge/` except the ONE snippet (lane D only). No `apollo-spider/`, `showroom/`, meta, canon, Tabs. No commit, no push.
- ⛔ Rule nothing of Dave's: which tile leads, the status surface, the spans dial's scope, DP-17 — all RSQs on the review page.
- Claims carry a probeable token (path · line · sha · exit code · assertion · px).
- Lanes are >15K: check in at the seam (D: after the edit, before gates · R: between arms).
- Return to the conductor in ≤250 words: paths, first-chart-y, the wall spans landed per band, seed-4 pass/fail, richness per arm, top three findings one line each, tokens used.
