# #213 delegated-wrap brief — conductor's cut (Fable → Opus wrap sub)

> Dated period record. The store stays the live home. Nothing the wrap writes is a ruling;
> only `knowledge/_inscribe_ruling.py` writes rulings, only on Dave's word.

## SESSION FACTS (#213, 2026-08-21, Fable conductor)

- **Theme:** Dave's word at open: crank Opus subs to bring All-models quota up to Fable parity. Ten subs total: 9 work (7 Opus, 2 Sonnet) + this wrap. **Work-sub spend 1,430,999 tokens (n=9) — QUOTA, never FILL.**
- **Commits, all pushed+verified:** `f6320b5` (wave 1: ds-018 governance fix + four-theme proof instrument `knowledge/_render/verify_ds018_themes.py` · RAG A+B+C wired, canon `*/` comment-close defect FIXED in `gen_canon_components.py` · 5 fork renames · Template family repaired · gate-red repairs) · `d333a78` (wave 2: `reviews/REVIEW-213-wave-components-four-theme-v1.html` over the 43 wave components, 172 questions/5 rule-once panels · itinerary v2 measured, orphans 1→28, Layer-2 ROW_MAP gap · Actions v6 bump · W-99i/W-99j rows) · `ff37efe` (REVIEW-213 specimen viewport fix, driven proof).
- **Receipts:** `notes/_receipts/2026-08-21-213-*` (7 files) + survey `notes/_briefs/2026-08-21-213-c1-residual-survey-v1.md`.
- **CI (s203-D1):** run **#400** read back = FAILURE; gates job = exactly the DECLARED pair (fork gate exit 1: `--panel` value question + `--l-min` gate-model gap, both Dave's; radius 4 strict, W-90). Render job exit 1 — composition **UNPROVEN, declared** (not mined). Runs **#401** and **#402** read-backs **OWED to the #214 opener**. #400's Node-20 deprecation warnings are answered by the v6 bump already committed.
- **Gauge moments (literal):** boot **61,605 real — OUT of the s208-D1 band (55,595–57,903), DECLARED**, not corrected. Last check-in FILL **226,539 real** — past the **ADVISORY stop line 150,929** and the 200,000 working line, under the **256,000** hard line; DECLARED, seat-relative (#209 calibration-gap finding stands). Quota panel opener verbatim, PERISHABLE: *"All models Resets Thu 10:59 PM 7% used · Fable Resets Thu 10:59 PM 9% used"*; no close paste yet.
- **Dave live today:** fired all three targets (chart residual · mine burn-down · his sitting) · REVIEW-213 will be ruled **in tranches over the next few days** (his words: "I'll tackle this in tranches over the next few days so we can return to it") · ruled BOTH boot tidy-ups ride this wrap (below) · viewport fix requested and landed.

## THE TWO ENACTMENTS DAVE PUT ON THIS WRAP (his word, this evening)

1. **W-99b — the s212-D6 §C OFFLOAD, now enacted:** move GOOD-MORNING §C bodies into conditioned store rows (`_state.add`, every row needs `closes_when` — where a §C body states none, the row goes in under the item's existing stated condition or the offload for that item is SKIPPED AND NAMED, never invented: an agent inventing Dave's close condition is the same overreach as inventing his ruling). §C keeps one-line pointers. Target: §C under the 150-line warn cap. Nothing deleted — moved.
2. **The `Previous:` line trim (the 2d clause):** the ~62,000-character single line chaining titles back to #184 in the *Last refreshed* region — trim per the clause's own words (read the clause in the file first; enact what IT says, not this summary).

Both are BY DAVE'S WORD TODAY — cite this brief. Run the capture/§A/§C gates after; if a gate reds on the result, fix within the enactment's own scope or back the step out and declare.

## ROW UPDATES (mechanical, judged by each row's own closes_when text)

- `W-99g`: receipts consumed, replayed to Dave, committed — close if its condition reads as met.
- `W-99i`: append Dave's tranche plan to the body; stays OPEN (his eye is the condition).
- `W-99b`: closes on enactment 1 landing.
- This wrap brief gets its own store row (the W-99f pattern), doc-row gate is blocking.

## DO-NOT-RULE (absolute)

All 43 component rulings and the 5 rule-once panels · `--panel` value · status-indicator one-atom-vs-three · console/supercharge info-ink 3.78:1 leg · lane G build/park · the re-base sitting (boot band + stop line + SIZE_BUDGET/TITLE_CAP_TAPE; the s208-D1 rider: any re-base proposal must carry a priced boot-REDUCTION option) · P8 · W-95 page rulings · the #211 seven-receipt sitting · B-D4 is DISCHARGED — do not resurrect it as open. If a step would rule any of these, stop and declare.

## PITFALLS (replay, Dave #165)

Regen serial is ORDERED — ramp → canon components → snippet tokens → theme cascade → showroom, whole set per wave, chain regen last. Msgfile: FRESH `printf` file per invocation under outputs/, NO hand-written T3 prefix (the script stamps it), `SESSION_N=213`, `--reconciled` + named paths or `--all-dirty` only after accounting for every dirty path. After commit READ THE SUBJECT BACK from `git log` — verify it certifies THIS session. Banner figures literal ("ADVISORY stop line 150,929", the boot-band constant read from `_gauge_tokens.py`, never a remembered figure). A crash is not a fail. Leave wrap headroom; late corrections digest at the #214 opener.

## RETURN CONTRACT

Run the full capture ritual (`_capture_gate.py --wrap` path per `knowledge/_RUNBOOK-capture-ritual.md` — READ IT first). Commit; **push granted** (s207-D1 conductor judgment, `--push` form). Return to the conductor: BOTH title lines (forward + this session's, for chat relay) · commit hash + subject read-back · gate outcomes · the §C line-count before/after and the byte saving from both enactments · anything skipped, NAMED. Declare in the wrap record: memory step 3 + MEMORY.md compaction = conductor's, DEFERRED to #214 opener; CI #401/#402 read-backs owed at #214.
