# BUILD — DP-08 A+B2 on the bento dashboard template (#251, 2026-09-06)

> Opus 5 build lane, cold. A rule-5 verifier follows in the same wave because this touches canon
> (`knowledge/snippets/Template-dashboard-bento.reference.html` + a behaviour partial + the byte gate).
> CITES: `s251-D1` · `s251-D2` · `s247-D3` · `s247-D4` · `s248-D1` · `s249-D2` · `s249-D5` · `s250-D1` · `s234-D5`

## Read first (grep by id / section, never whole files)
- `knowledge/_rulings.json` — the ids above, verbatim.
- `notes/_subreports/2026-09-06-251-PROBE-dp08.md` — the premise probe: all three premises TRUE; the chip `href="#p2"` defect; RSQs 1–4.
- `outputs/w2-debt/dp08/` — `dp08-A.html`, `dp08-B2.html`, `dp08-AB2.html` (probe's own composition), the anchor prototype and `_render.py` / `_build.py`. These are byte-clean copies of `outputs/w2-rhythm/arm-B-sighted/dashboard.html`, NOT the canon template — port the change to canon, don't copy the files.
- `knowledge/_BEHAVIOUR-GATE.md` and `knowledge/_validate_behaviour.py` docstring (lines 1–70) for how a partial is declared and how `consumes` is summed. ADR-0015 A3 in `docs/decisions/`.

## What to build (all in canon, by ADDITION, Edit never Write)
1. **Header status strip (A).** The header's existing `l-row` status row grows to: nostro chip (kept) · `a.status.warn` **"Awaiting approval · 14 payments · 5 need you"** (`s251-D2` wording, exact) with `href` to the Needs-attention card's id (NOT `#p2`) · 1px divider · plain legal span "Undrawn facilities $120.0m · 40% drawn · reviewed 1 Sep" with NO chip (no RAG meaning). Strip is built from Status-indicator form B as today; if you find you need a new component, STOP and say so in the report — don't mint one.
2. **Headline row → four KPI tiles (A).** Awaiting approval and Undrawn tiles leave the row. Page rule on the lead group sets four columns at every band (probe P1: 4/4/4 is clean at 1440/1100/820, light+dark). Row stays a ROW (`s247-D4`).
3. **Needs-attention card (B2).** Gets a stable id (RSQ 1: the CARD, as the probe prototyped — `dp08-na` or the template's naming convention if one exists). Rail reordered: Needs-attention ABOVE Balances by entity. Promoted head row for Awaiting approval ONLY, wording **"14 payments · 5 need you"** + `$2.7m`. ⛔ Undrawn does NOT enter the card (`s251-D1`: strip-only). Keep `s248-D1`'s two-up rail at 1100.
4. **Anchor behaviour partial.** The probe's prototype (click → scroll to card, focus it, transient highlight, `prefers-reduced-motion` honoured, stays inside `#p1`) becomes a behaviour partial at a named address per `s234-D5` and the partials convention; the template declares it in `consumes`. Measure with the gate: `python3 knowledge/_validate_behaviour.py` exit 0, paste the page sum (probe: 14,801 of 34,816 expected order). Run its `--mutate` modes once and paste.
5. **Meta/docs.** Whatever the partials/snippet convention requires (meta json, `consumes`, a line in the template's header comment naming `s251-D1`/`D2`). Nothing else.

## Prove it
- Render the canon template (not the outputs copy) at 1440 / 1100 / 820, light and dark, via the probe's `_render.py` approach (Playwright is bootstrapped in `~/.cache/ms-playwright` + `libxdamage1` — see probe report step 1; if the sandbox is fresh, redo it, 4 calls). Paste: four tile x/w per width; Needs-attention `{x,y,w,h}` per width; rail two-up heights at 1100; 0 page errors.
- Drive the anchor: click the chip, paste scrollY before/after, `document.activeElement` id, and that `#p1` is still the visible panel. Reduced-motion once.
- Count DP-20 signal carriers above the fold at 1440 (probe: A+B2 measured 22/7). Paste. Do NOT trim to hit a budget — RSQ 4 is Dave's; report the number.
- Grep the finished template for `5 of 14` (must be 0 hits) and `#p2` on the chip (0 hits).
- State the strongest case for the option you rejected wherever you chose (RSQ 1 card-vs-row; head row vs no head row).

## Rules
- Never `_build_all.py`. Never touch `_rulings.json`, `_validate_behaviour.py` constants, `canon.css`, or any cap/band/constant. Byte caps UNMOVED (`s250-D1`).
- Edit, never Write, on existing files. Batch independent reads. Every number pasted from a command run here.
- Report: `notes/_subreports/2026-09-06-251-BUILD-dp08.md` per `notes/_subreports/_TEMPLATE.md` — `COUNTS:` exact shape, `CITES:` beside it, `REPLAY-THESE:`, `UNPROVEN` list. Diff summary (`git diff --stat`) pasted. Do not commit.
- Budget ≈150K. If short, stop with UNPROVEN entries rather than overrun.
