# #218 CLOSING-wrap brief (Opus wrap sub) — the session's SECOND wrap event

⚠ **THE SHAPE, STATED FIRST:** #218 already wrapped once (this morning, commits `61302a3`/
`080204a` — banner, gauge log, rename all done for the first half). The session then CONTINUED:
Dave opened a four-lane CRANK and a decision sitting, and four seam commits landed (`adb5130` ·
`8247c52` · `6f39909` · `941c92d`), all pushed. **This wrap rolls the post-wrap half into the
record.** If any ritual step or gate refuses the two-wrap shape, STOP on that step and return
the refusal with its text — do not force, do not improvise a hand-patch (#86's lesson).

**Session facts (conductor-measured — copy, never re-derive):**
- Conductor FILL at brief-cut: **~503,361 real over ~233 turns** (this seat's checkin) — past
  every inscribed line; platform window ~15M; DECLARED, nothing touched (same shape as the
  morning wrap; the s208-D1 re-base evidence grows again).
- Boot 66,845 was declared AND LOGGED at the morning wrap — ⛔ do NOT log a second boot sample
  for this session (double-count). Gauge additions cover only the post-wrap half.
- **`subs 1926223 tokens (n=9)`** — the post-wrap conductor-seat subs, measured: gates 288,529 ·
  build-verdict 182,769 · enactment 266,390 · wave-α 111,353 · wave-β 175,732 · wave-γ 109,081 ·
  phantom 329,770 · cA 219,367 · cB 243,232. (The morning wrap's own sub 291,770 and the two
  worker WINDOWS' spends are recorded in their own artefacts — W2/W3 receipts carry theirs.)
- Advisory in force all day: the armed ~190,000 (delegated-wrap day).

**Post-wrap half, banner material (chronological):**
1. Dave's quota panel (all 38%→44% · Fable 36%→42%, parity held) opened the **#60
   production-crank**: four lanes ruled by option-select, divvy at
   `notes/_briefs/2026-08-24-218-crank-divvy.md`, two worker WINDOWS (receipts:
   `notes/_receipts/2026-08-24-crank-charts.md` · `2026-08-24-crank-behaviour.md`).
2. Seam 1 `adb5130`: gates wave six-for-six (INSTRUMENT-STRAY re-scoped to s217-D1 surfaces;
   capture-gate argv write-doors closed — `--build` sole writer; boot-delta parsing; register-
   store join ADVISORY; `_htmlmask.py`; WEATHER was a typo, vocabulary unchanged) + build-verdict
   push: **126/128 steps green driven individually — the 75/128 record was a tiktoken-less-
   sandbox artefact**; Calendar selected-hover contrast fixed at cause (0 text failures / 135).
   The real call wall re-measured 177,998ms.
3. Seam 2 `8247c52`: worker lanes reconciled — DV-D16 stacked growth enacted (W2) + six snippets
   gained behaviour, 34 judged static with reasons (W3); BW_MUTANT_DIR class fix.
4. **The decision sitting — twelve rulings by option-select, inscribed `s218-D4`/`D5`/`D6`** +
   seam 3 `6f39909`: all five decision enactments landed (house curves + delayed keys; palette
   hidden-fix + chord CLAIMED; overlay behind; photography wall SQUARED per the exports'
   edge:square, 4 tiles re-spanned; **CI contrast sweep BLOCKING**). **Wave-3 DISCHARGED BY
   MEASUREMENT** — all 17 components pre-existed GATED; the 78-gap premise was the frozen
   itinerary column (5th surface of the premise class, the conductor's own miss, memory updated);
   lane β pivoted and fixed four live defects in gated feedback components; phantom surfaces:
   six built PROPOSED, **Confirmation refused per #98-D1** (the Replay lives in the showroom bar).
5. **`s218-D7` FILED SUB-REPORTS adopted** (Dave's own W2 proposal) + seam 4 `941c92d`: machinery
   built and immediately consumed — ⛔ **CITE BOTH FILED REPORTS BY PATH in the banner** (the new
   `subreport_citation_check` reads the session record): `notes/_subreports/2026-08-25-218-cA-filed-reports.md`
   · `notes/_subreports/2026-08-25-218-cB-quality-forms.md`. Frozen column fenced at the writer
   (`itinerary_status_2026_07_14_FROZEN`, v4 beside untouched v3; conductor accepted cA-Q1(a)).
   Quality-forms lane: **8 live defects fixed at cause** (Date-range was keyboard-unusable; the
   `{once:true}` class live in 3 more files); type ratchet 1097→1093.

**Dave-owed, carried (verify each against the store, then re-type):** the morning list (bento
sitting rows, mono shade now RATIFIED s218-D6 — strike with receipt; sideways portrait tile;
six phantom surfaces; Toast announcement) + cA Q2/Q3/Q4 (citation-gate promotion · build wiring ·
doc-row untracked scope) + cB Q1/Q2/Q3 (link composite · Empty-state tightening · its third) +
wave-3 successor standing question (quality lane 2+ scoping) + the CI Actions read-back queue
(~11 verdicts, conductor-owed, seat-blind — Dave's glance or Chrome).

**Known residual reds, pre-existing, named not fixed:** `_gen_chain.py --selftest` chain-size
bite at 40.5% vs the 40% pin (hovering; if YOUR chain regen tips it, that is the bite working —
declare, don't trim content to pass); ds-039 `gen_canon_components` refusal on Chart-bar.

**Your job:** the full capture ritual per `knowledge/_RUNBOOK-capture-ritual.md` (READ IT) —
reconcile (tree should be CLEAN at start except ritual-log appends; every path accounted),
gauge-log additions for the post-wrap half only, banner + LS delta rolls, 2c/2d/2f, chain regen,
2g index LAST, doc-row gate (mint existence rows incl. this brief), commit via the script with a
FRESH printf msgfile (SESSION_N=218), subject read back, push. **DO-NOT-RULE:** no Dave row
closed (the mono-shade strike is a STRIKE WITH RECEIPT, not a closure beyond s218-D6's own
words), no constants/bands/advisories, no promotions, no `_rulings.json` writes (verify
s218-D1..D7 present by reading), no memory (conductor's, DONE), no lane repoint. **Deliver:**
commit hash + subject read back, next-session title both forms, gauge as logged, gate verdicts,
refusals/residuals named.
