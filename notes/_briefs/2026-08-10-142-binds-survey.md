# Binds survey — session #142 (survey sub, 2026-08-10)

Source of truth: `reviews/BINDS-DRAFT-2026-08-09-s141-v1.json` (ruled s141-D1 ACCEPT-PROPOSED; these 120 $doubt rows remain OPEN and UNRULED). This file PROPOSES candidates only — no decision is made here.

## Measured counts
- doubt rows: **120** (premise: 120)
- visual props: 109; bound: 12; unbound visual: **97** (premise: ~97)
- doubt rows already carrying draft spineCandidates (the '62 pick-lists'): **62**; carrying none: **58**
- NOTE: the 'pick-lists for 62 props' in the brief are the draft's own `spineCandidates` arrays (summary key `doubtRowsCarryingSpineCandidates: 62`); no separate Dave-authored pick-list files were found (grep 'pick-list|picklist|pick list' across knowledge/ and notes/ matched only index/receipt files).
- 23 doubt rows are NOT unbound-visual props: they are the classification-judgment rows (16 'prop name in neither list') + set-confirmation rows (7 'array drafted from co-mentioned paths').

## Doubt-text taxonomy
- ×94 — no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- ×16 — prop name is in neither the declared VISUAL nor NON-VISUAL list — classification is a judgment, not a measurement
- ×7 — array drafted from co-mentioned paths — Dave to confirm the set is the prop's, not the component's
- ×1 — no token path in this meta resolves for this prop — the tokens-map entry is prose, not a path: 'non-interactive (depricate)/surface/neutral-1'
- ×1 — no token path in this meta resolves for this prop — the tokens-map entry is prose, not a path: 'font-5/medium'
- ×1 — no token path in this meta resolves for this prop — the tokens-map entry is prose, not a path: 'color-mix 50% bone/page as the gradient highlight (no minted token)'

## Vocab-gating verdicts (see report)
- intent-map keys: gates ONLY the 6 `variant` doubt rows (accordion, avatar, divider, notifications, quick-actions, video-player) — a later intent-vocabulary ruling could re-home these props to the variants axis, invalidating a binds pick. All other doubt rows: NOT gated.
- capability strings: gates NONE — zero doubt rows touch slot/accepts props (measured: no prop name contains 'slot' or 'accept').

## Rows by component meta

### accordion (5 rows)

**variant** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `button.primary`, `button.secondary`, `button.tertiary`, `button.quaternary`
- current state: unbound

**open** (boolean, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**state** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `text.default`, `text.disabled`, `form.background`, `form.border`, `button`
- current state: unbound

**fontSize** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `typography.font-size`
- current state: unbound

**size** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `icon.xsmall`, `icon.small`, `icon.medium`, `icon.large`, `target.min`
- current state: unbound

### account-card (1 rows)

**status** (enum, VISUAL)
- doubt: array drafted from co-mentioned paths — Dave to confirm the set is the prop's, not the component's
- rationale: multiple resolving token paths matched the prop name in this meta's tokens map; ARRAY drafted, but which subset the prop actually drives is unproven
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: BOUND (has proposedBinds)

### account-selector (1 rows)

**selected** (number, VISUAL)
- doubt: array drafted from co-mentioned paths — Dave to confirm the set is the prop's, not the component's
- rationale: multiple resolving token paths matched the prop name in this meta's tokens map; ARRAY drafted, but which subset the prop actually drives is unproven
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: BOUND (has proposedBinds)

### action-bar (1 rows)

**sticky** (boolean, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates (LOW): `elevation:elevation.decorative.X`, `elevation:elevation.decorative.Y`, `elevation:elevation.decorative.blur`, `elevation:elevation.decorative.spread`
- current state: unbound

### alert (2 rows)

**status** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `rag.success`, `rag.warning`, `rag.error`, `rag.neutral`
- current state: unbound

**dismissible** (boolean, UNCLASSIFIED)
- doubt: prop name is in neither the declared VISUAL nor NON-VISUAL list — classification is a judgment, not a measurement
- rationale: unknown is never defaulted
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

### amount-display (3 rows)

**size** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `icon.xsmall`, `icon.small`, `icon.medium`, `icon.large`, `target.min`
- current state: unbound

**sign** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `rag.success`, `rag.error`
- current state: unbound

**redacted** (boolean, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

### amount-input (1 rows)

**size** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `icon.xsmall`, `icon.small`, `icon.medium`, `icon.large`, `target.min`
- current state: unbound

### avatar (4 rows)

**variant** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `button.primary`, `button.secondary`, `button.tertiary`, `button.quaternary`
- current state: unbound

**size** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `icon.xsmall`, `icon.small`, `icon.medium`, `icon.large`, `target.min`
- current state: unbound

**state** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `text.default`, `text.disabled`, `form.background`, `form.border`, `button`
- current state: unbound

**surface** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the tokens-map entry is prose, not a path: 'non-interactive (depricate)/surface/neutral-1'
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `background.default`, `surface.raised`, `surface.subtle`, `surface.action`
- current state: unbound

### badge (2 rows)

**state** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `text.default`, `text.disabled`, `form.background`, `form.border`, `button`
- current state: unbound

**size** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `icon.xsmall`, `icon.small`, `icon.medium`, `icon.large`, `target.min`
- current state: unbound

### banner (2 rows)

**status** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `rag.success`, `rag.warning`, `rag.error`, `rag.neutral`
- current state: unbound

**dismissible** (boolean, UNCLASSIFIED)
- doubt: prop name is in neither the declared VISUAL nor NON-VISUAL list — classification is a judgment, not a measurement
- rationale: unknown is never defaulted
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

### breadcrumbs (1 rows)

**state** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `text.default`, `text.disabled`, `form.background`, `form.border`, `button`
- current state: unbound

### button (4 rows)

**type** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the tokens-map entry is prose, not a path: 'font-5/medium'
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**state** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `text.default`, `text.disabled`, `form.background`, `form.border`, `button`
- current state: unbound

**size** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `icon.xsmall`, `icon.small`, `icon.medium`, `icon.large`, `target.min`
- current state: unbound

**surface** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `background.default`, `surface.raised`, `surface.subtle`, `surface.action`
- current state: unbound

### cards (2 rows)

**type** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**state** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `text.default`, `text.disabled`, `form.background`, `form.border`, `button`
- current state: unbound

### chart-bar (1 rows)

**orientation** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

### chart-line (1 rows)

**highContrast** (boolean, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `data.series-high-contrast`
- current state: unbound

### chart-sparkline (1 rows)

**scale** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates (LOW — size axis; icon vs gap depends on component): `icon-scale:icon.xsmall.height`, `icon-scale:icon.small.height`, `spacing:gap.fixed.content.xxsmall`, `spacing:gap.fixed.content.xsmall`
- current state: unbound

### chart-stacked-area (2 rows)

**fillAlpha** (string, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `alpha`
- current state: unbound

**highContrast** (boolean, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `data.series-high-contrast`
- current state: unbound

### countdown-timer (2 rows)

**style** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**size** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `icon.xsmall`, `icon.small`, `icon.medium`, `icon.large`, `target.min`
- current state: unbound

### data-grid (1 rows)

**state** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `text.default`, `text.disabled`, `form.background`, `form.border`, `button`
- current state: unbound

### divider (2 rows)

**variant** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `button.primary`, `button.secondary`, `button.tertiary`, `button.quaternary`
- current state: unbound

**surface** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `background.default`, `surface.raised`, `surface.subtle`, `surface.action`
- current state: unbound

### drawer (2 rows)

**side** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**width** (number, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `border-width.small`, `border-width.medium`, `border-width.large`
- current state: unbound

### dropdown (4 rows)

**family** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**open** (boolean, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**state** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `text.default`, `text.disabled`, `form.background`, `form.border`, `button`
- current state: unbound

**itemState** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

### empty-state (1 rows)

**posture** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

### headers (3 rows)

**type** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**state** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `text.default`, `text.disabled`, `form.background`, `form.border`, `button`
- current state: unbound

**image** (boolean, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

### hero (2 rows)

**breakpoint** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `breakpoint`, `scale`
- current state: unbound

**height** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

### icon-button (3 rows)

**type** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**state** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `text.default`, `text.disabled`, `form.background`, `form.border`, `button`
- current state: unbound

**glyph** (string, VISUAL)
- doubt: array drafted from co-mentioned paths — Dave to confirm the set is the prop's, not the component's
- rationale: multiple resolving token paths matched the prop name in this meta's tokens map; ARRAY drafted, but which subset the prop actually drives is unproven
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: BOUND (has proposedBinds)

### input-fields (3 rows)

**type** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**state** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `text.default`, `text.disabled`, `form.background`, `form.border`, `button`
- current state: unbound

**dateCellState** (enum, VISUAL)
- doubt: array drafted from co-mentioned paths — Dave to confirm the set is the prop's, not the component's
- rationale: multiple resolving token paths matched the prop name in this meta's tokens map; ARRAY drafted, but which subset the prop actually drives is unproven
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: BOUND (has proposedBinds)

### links (4 rows)

**type** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**fontSize** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `typography.font-size`
- current state: unbound

**state** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `text.default`, `text.disabled`, `form.background`, `form.border`, `button`
- current state: unbound

**mode** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `background.default`
- current state: unbound

### list-items (3 rows)

**type** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**state** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `text.default`, `text.disabled`, `form.background`, `form.border`, `button`
- current state: unbound

**bulletType** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

### loading-indicator (2 rows)

**size** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `icon.xsmall`, `icon.small`, `icon.medium`, `icon.large`, `target.min`
- current state: unbound

**mode** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `background.default`
- current state: unbound

### modals (2 rows)

**type** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**breakpoint** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `breakpoint`, `scale`
- current state: unbound

### navigations (2 rows)

**type** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**breakpoint** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `breakpoint`, `scale`
- current state: unbound

### notifications (2 rows)

**type** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**variant** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `button.primary`, `button.secondary`, `button.tertiary`, `button.quaternary`
- current state: unbound

### pagination (2 rows)

**type** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**state** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `text.default`, `text.disabled`, `form.background`, `form.border`, `button`
- current state: unbound

### popover (2 rows)

**placement** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**dismissible** (boolean, UNCLASSIFIED)
- doubt: prop name is in neither the declared VISUAL nor NON-VISUAL list — classification is a judgment, not a measurement
- rationale: unknown is never defaulted
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

### quick-actions (2 rows)

**variant** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `button.primary`, `button.secondary`, `button.tertiary`, `button.quaternary`
- current state: unbound

**state** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `text.default`, `text.disabled`, `form.background`, `form.border`, `button`
- current state: unbound

### reorder (1 rows)

**state** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `text.default`, `text.disabled`, `form.background`, `form.border`, `button`
- current state: unbound

### search-field (1 rows)

**state** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `text.default`, `text.disabled`, `form.background`, `form.border`, `button`
- current state: unbound

### secure-entry (1 rows)

**masked** (boolean, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

### segmented-control (3 rows)

**shape** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `border-radius.control`, `border-radius.surface`, `border-radius.indicator`
- current state: unbound

**size** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `icon.xsmall`, `icon.small`, `icon.medium`, `icon.large`, `target.min`
- current state: unbound

**state** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `text.default`, `text.disabled`, `form.background`, `form.border`, `button`
- current state: unbound

### selection-controls (4 rows)

**control** (enum, VISUAL)
- doubt: array drafted from co-mentioned paths — Dave to confirm the set is the prop's, not the component's
- rationale: multiple resolving token paths matched the prop name in this meta's tokens map; ARRAY drafted, but which subset the prop actually drives is unproven
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: BOUND (has proposedBinds)

**state** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `text.default`, `text.disabled`, `form.background`, `form.border`, `button`
- current state: unbound

**checkboxKind** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**chipType** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

### skeleton-loader (3 rows)

**shape** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `border-radius.control`, `border-radius.surface`, `border-radius.indicator`
- current state: unbound

**width** (string, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `border-width.small`, `border-width.medium`, `border-width.large`
- current state: unbound

**shimmer** (boolean, VISUAL)
- doubt: no token path in this meta resolves for this prop — the tokens-map entry is prose, not a path: 'color-mix 50% bone/page as the gradient highlight (no minted token)'
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

### slider (2 rows)

**type** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**handleState** (enum, VISUAL)
- doubt: array drafted from co-mentioned paths — Dave to confirm the set is the prop's, not the component's
- rationale: multiple resolving token paths matched the prop name in this meta's tokens map; ARRAY drafted, but which subset the prop actually drives is unproven
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: BOUND (has proposedBinds)

### stat-card (3 rows)

**size** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `icon.xsmall`, `icon.small`, `icon.medium`, `icon.large`, `target.min`
- current state: unbound

**delta** (string, VISUAL)
- doubt: array drafted from co-mentioned paths — Dave to confirm the set is the prop's, not the component's
- rationale: multiple resolving token paths matched the prop name in this meta's tokens map; ARRAY drafted, but which subset the prop actually drives is unproven
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: BOUND (has proposedBinds)

**direction** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

### status-indicator (1 rows)

**state** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `text.default`, `text.disabled`, `form.background`, `form.border`, `button`
- current state: unbound

### tab-bar (1 rows)

**current** (string, UNCLASSIFIED)
- doubt: prop name is in neither the declared VISUAL nor NON-VISUAL list — classification is a judgment, not a measurement
- rationale: unknown is never defaulted
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

### table (3 rows)

**headerType** (enum, UNCLASSIFIED)
- doubt: prop name is in neither the declared VISUAL nor NON-VISUAL list — classification is a judgment, not a measurement
- rationale: unknown is never defaulted
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**orientation** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**cellAlign** (enum, UNCLASSIFIED)
- doubt: prop name is in neither the declared VISUAL nor NON-VISUAL list — classification is a judgment, not a measurement
- rationale: unknown is never defaulted
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

### tabs (3 rows)

**tabState** (enum, UNCLASSIFIED)
- doubt: prop name is in neither the declared VISUAL nor NON-VISUAL list — classification is a judgment, not a measurement
- rationale: unknown is never defaulted
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**track** (enum, UNCLASSIFIED)
- doubt: prop name is in neither the declared VISUAL nor NON-VISUAL list — classification is a judgment, not a measurement
- rationale: unknown is never defaulted
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**overflow** (boolean, UNCLASSIFIED)
- doubt: prop name is in neither the declared VISUAL nor NON-VISUAL list — classification is a judgment, not a measurement
- rationale: unknown is never defaulted
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

### tags (5 rows)

**type** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**size** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `icon.xsmall`, `icon.small`, `icon.medium`, `icon.large`, `target.min`
- current state: unbound

**background** (boolean, UNCLASSIFIED)
- doubt: prop name is in neither the declared VISUAL nor NON-VISUAL list — classification is a judgment, not a measurement
- rationale: unknown is never defaulted
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**mode** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `background.default`
- current state: unbound

**state** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `text.default`, `text.disabled`, `form.background`, `form.border`, `button`
- current state: unbound

### textarea (2 rows)

**maxlength** (number, UNCLASSIFIED)
- doubt: prop name is in neither the declared VISUAL nor NON-VISUAL list — classification is a judgment, not a measurement
- rationale: unknown is never defaulted
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**counter** (boolean, UNCLASSIFIED)
- doubt: prop name is in neither the declared VISUAL nor NON-VISUAL list — classification is a judgment, not a measurement
- rationale: unknown is never defaulted
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

### time-picker (1 rows)

**step** (number, UNCLASSIFIED)
- doubt: prop name is in neither the declared VISUAL nor NON-VISUAL list — classification is a judgment, not a measurement
- rationale: unknown is never defaulted
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

### toast (2 rows)

**status** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `rag.success`, `rag.warning`, `rag.error`, `rag.neutral`
- current state: unbound

**duration** (number, UNCLASSIFIED)
- doubt: prop name is in neither the declared VISUAL nor NON-VISUAL list — classification is a judgment, not a measurement
- rationale: unknown is never defaulted
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

### tooltip (2 rows)

**tip** (enum, UNCLASSIFIED)
- doubt: prop name is in neither the declared VISUAL nor NON-VISUAL list — classification is a judgment, not a measurement
- rationale: unknown is never defaulted
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**tipAlignment** (enum, UNCLASSIFIED)
- doubt: prop name is in neither the declared VISUAL nor NON-VISUAL list — classification is a judgment, not a measurement
- rationale: unknown is never defaulted
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

### video-player (2 rows)

**variant** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `button.primary`, `button.secondary`, `button.tertiary`, `button.quaternary`
- current state: unbound

**state** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `text.default`, `text.disabled`, `form.background`, `form.border`, `button`
- current state: unbound

### view-options (2 rows)

**type** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- proposed candidates: NONE — no plausible spine axis (likely colour/semantic or behavioural; colour spine not in the 5 migrated files)
- current state: unbound

**state** (enum, VISUAL)
- doubt: no token path in this meta resolves for this prop — the meta declares no tokens entry for this prop at all
- rationale: a token NAME is not an ADDRESS — refusing to invent one
- pick-list (from draft, HIGH-relevance — Dave to pick): `text.default`, `text.disabled`, `form.background`, `form.border`, `button`
- current state: unbound
