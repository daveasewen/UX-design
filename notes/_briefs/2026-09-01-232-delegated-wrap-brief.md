# Brief — #232 delegated wrap (Opus): demo-day session, two lanes + five inscriptions, wrap at the conductor's band seat

provenance: 232 · 2026-09-01 · conductor Fable, FILL 231,518 at brief-cut (in the s214-D1 band,
declared LATE — the #222 miss shape, second instance; name it in the record honestly) · row W-337
(minted AT CREATION). SESSION_N=232. Demo day: Dave's cold test + demo pending at wrap time.

## The job

Run the capture ritual per `knowledge/_RUNBOOK-capture-ritual.md`, final commit with `--wrap`,
`SESSION_N=232`. You inherit a tree whose dirty paths are: the phase-2 re-cut artefacts
(`knowledge/_release/_pack_manifest.json`, `_pack_gate_probe.json`,
`reviews/RELEASE-SPIDER-2026-08-26-v1.html` + `.REVIEW.html`,
`notes/_subreports/2026-09-01-232-repair-and-recut.md`, this brief) and instrument appends
(`notes/_dream/_GRADE-DECISIONS.jsonl`, `notes/_REHEARSAL-LOG.jsonl` excluded per s137-D1,
`knowledge/_graph-mark-observations.jsonl`) — reconcile and commit per runbook. ⛔ NO PUSH — the
conductor pushes after you finish and reads back CI (`s207-D1`/`s203-D1`).

## The day's receipts (cite by path; copy counts off files, never from here if they disagree)

- Opener (cold seat): `s231-D1`/`s231-D2` inscribed (banked #231 orders); store 298→300.
- Re-stage lane `W-331`, report `W-332` = `notes/_subreports/2026-09-01-232-restage-v105.md`:
  v1.0.5 re-staged with the bento snippet, twins byte-identical, STOPPED honestly on the pack's
  cold column (35 pass · 2 FAIL exit 1, both bento) + roster found 57 not 58.
- Detector driven on Dave's two REAL v1.0.2 samples (his uploads, this chat): cold build
  Data-grid PARAPHRASE 0.756 (11 invented structures); warm build Filter-toolbar SPLICED 0.913,
  Page-header PARAPHRASE 0.733, bento vocabulary-without-structure ABSENT 0.158. First real
  drives of the instrument; the 0.913 sample discriminated dials A vs B.
- Dave ruled (adopting the recommendation set, "needs done soon so I'll go with your
  recommendations"): `s232-D1` dial B 0.90 · `s232-D2` roster 57 + column clause ·
  `s232-D3` bento rail fork intentional. Store at 303. His KG-routing idea BANKED verbatim
  `W-333` (`notes/_briefs/2026-09-01-232-kg-routing-home-idea.md`); bento-more-work PARKED
  `W-334`. Roster correction marker added in `_LIVE-STATE.md` by addition (header-wins).
- Repair lane `W-335`, report `W-336` = `notes/_subreports/2026-09-01-232-repair-and-recut.md`:
  phase 1 five edits, each gate rc 1→0, survey 6 reds→5, routing step at the SHIPPING skill path
  (briefed v2 path was frozen surface — conductor confirmed the substitution); committed
  `ff1b2f5` (25 paths, subject read back). Phase 2 re-cut at `ff1b2f5`: cold column MEASURED
  **37 pass · 0 FAIL · exit 0** on the pristine twin stage; new zip `9fb07fa2f0dc0ade…` ·
  20,073,815 B, old `92f5e10e…` SUPERSEDED (never quote it as current); `[118]` closed;
  `[136]` stays red BY DESIGN for the PROPOSED window; durable zip at repo root + outputs copy;
  go/no-go pages carry `9fb07fa2`.

## BANKED FOR #233's COLD SEAT (the rule: a wrap sub inscribes NOTHING)

1. **Proving-zip STANDING HOME** — Dave spotted the drift class himself ("is this rot?"):
   #230 outputs (vanished) → #232 root (sub's one-off). Candidate: repo root, MINTED BY THE
   BAKE SCRIPT + one release-runbook line. His word owed, then enactment.
2. **v1.0.5 ratify word** — NOT given, comes after his cold test on `9fb07fa2`, separate.
3. **W-308 demo report-back** — the five s230-D1 beats; closes ONLY on his word. If it lands
   after this wrap, it is #233 opener material.
4. **Factory answer confirm** — his post-wrap #231 answer covered the programme; the visual
   decision-surface half still needs his explicit confirm (receipt
   `notes/_receipts/2026-08-31-231-postwrap-factory-answer.md`).
5. **PROVENANCE.json `files: 1664` vs 1,669 zip entries + ci-template README "36 gates" vs 37**
   — shipped, pre-existing, advisory (phase-2 RSQ 1). Carried, not silent.
6. **Ritual step 3 (memory) DEFERRED to the #233 opener, DECLARED** — the conductor is in-band
   and memory inscription is judgment-adjacent; a wrap sub cannot write memory. Say so in the
   record; do not invent a claim about it.

## Gauge lines for the record (state moments separately, never converted)

boot **75,747 real** — 22nd consecutive outside the s208-D1 band, post-diet sample n=4 of ~5.
Conductor FILL **231,518 real** at brief-cut — the 190,000 stop line was crossed INSIDE the
phase-2 lane and declared LATE (the #222 miss class, stated against the conductor); band entered,
mechanical-only honoured from the declaration on, probe planted at the opener
(`knowledge/_probe/session-232.json`) and quizzed **GREEN 4/4 blind at 231K**. No wall crossed.
Measured subs n=3: 169,305 · 122,953 · 118,415 = 410,673 tokens — QUOTA, never window FILL.
Your own spend: UNOBSERVABLE from your seat — DECLARE it, never estimate. Write the s214-D5
wrap-handover cost line. Effort band M–L, one day, three build lanes + five inscriptions.

## DO-NOT-RULE

No `_rulings.json` writes, no memory writes, no W-row closes or re-words (mint only what the
ritual itself requires, e.g. a doc row for a file the wrap creates), no push, no roster/version/
constant/band/floor/stop-line/wall moves, no ratification claims, no threshold or RSQ answers,
no CI claims beyond the measured backlog (21 unpushed + your wrap commit — and an ahead-count
against `origin/main` is EMPTY and lies; the branch is `master`).

## Pitfalls — replayed

tiktoken FIRST · nothing survives a call boundary, ~178s wall · TMPDIR mount-side or /dev/shm,
never /var/tmp · never pipe hashes/verdicts through tail/head (bit the conductor TWICE today —
say so if you inherit a clipped claim) · msgfile fresh + unique under outputs, NO session prefix
in the headline (T3 generates it) · banner discipline `s214-D6`: chain toward 10–12K tape ·
regen serial ORDERED, ramp first, index LAST · read the subject back after commit · the roll
order trap (age brackets FIRST, then NEW→1).

## Report

The ritual's own artefacts are the report. Chat STUB back to the conductor: wrap commit hash +
subject as landed, chain size, store/state counts read back, the s214-D5 cost line, and ⛔ BOTH
TITLE LINES (RENAME for #232, NEXT-TITLE for #233) verbatim — the conductor must relay them in
chat (delegated-wrap rename relay rule).
