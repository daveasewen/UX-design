# #219 crank divvy — wave 1 (2026-08-25)

**Conductor:** Fable (this seat). **Workers:** Opus subs, launched from the conductor seat.
**Dave's go:** day-shape option-select at the #219 opener ("sitting + crank in parallel"); lanes offered in chat with a strike option — none struck at launch.
**Quota target:** ~71% by end of day (from 50% all / 48% Fable at opener, parity held).

## Lanes

| lane | scope | model | serial constraints |
|---|---|---|---|
| 3 | Regenerate the stale bento review artefacts with the pinned s217-D1 specimen (BENTO-CANON v2/v3 successor, GALLERY-COMPARE successor) — #218 residual ③ | opus | independent (reviews/ only) |
| 1 | Segmented-control adoption gap: wire the minted per-theme radius tokens + the s201-D1 xs/s/m/l dimension grammar into the canon snippet, all four themes; sweep for other minted-but-unconsumed tokens (report-only) | opus | touches canon regen — lane 4 HOLDS until this returns |
| 2 | CI drift diagnosis (survey fails [40][45][50][107] — real staleness vs #173 stale-in-CI class) + boot-delta parser fix + [115] tape-ratio threshold artefact diagnosis | opus | touches knowledge machinery; no canon regen unless a drift fix requires it — then serial order, ramp first, index last |
| 4 (HELD) | DV-D16 chart-expansion continuation | opus | after lane 1 |

## Shared-file rules
- ONE conductor commits. Subs leave working-tree changes + a filed report; the conductor reconciles every path (no blind `git add -A`).
- Generated artefacts are owned by their generators — edits go into the generator/source, never the generated file. Each sub names which generator owns each region it touches BEFORE editing.
- Regen serial set is ORDERED: whole serial per wave, ramp first, index last.
- Sub `git checkout` of specimens is BANNED.

## DO-NOT-RULE (all lanes)
No `knowledge/_rulings.json` writes · no constants, bands, advisories, thresholds, stop lines · no Dave-owned row closed or reworded · no promotions of advisory gates · anything taste-level lands PROPOSED with a live review surface · no lane repoints · no memory writes.

## Reporting (s218-D7)
Each sub FILES a full report at `notes/_subreports/2026-08-25-219-<lane>.md` with a `COUNTS:` line (findings · ruling-shaped · UNPROVEN); chat gets a STUB. Ruling-shaped questions go in their own section — a sub's prose must not arrive as a decision.

## Pitfalls (Dave #165 — consequences replayed)
- Lane 1: the snippet's legacy sm/md/lg classes are consumed by other pages (the grids pages' chart view switch consumes `.seg.sm`); the type composites `.t-cm-ctl-*` are bound to the old sizes (T-D15, `knowledge/canon/_type-bindings.json`) — an unmapped rename breaks consumers silently. Render-proofs assert LOADED, not WHICH — pin any specimen by name.
- Lane 2: a determinism check red in CI and green locally is an ENVIRONMENT fact, not an artefact verdict (#173/#193) — fixing the artefact for it is a patch, not a fix. The [13] pre-flight fail is Dave's standing streak — NOT a lane target.
- Lane 3: version-don't-overwrite — emit `-vN+1` beside the stale pages, never regenerate in place; verify all 15 specimen filenames against `gen_bento_roles_217.SPECIMEN_FILES` by name.
- All: a partial `_build_all.py` run strands the tree — drive steps individually; `pip install tiktoken --break-system-packages` first in any cold sandbox; sandbox call wall ~178s.

**Store rows:** owed for this brief + each filed report at the seam (forgotten-document class, #185).
