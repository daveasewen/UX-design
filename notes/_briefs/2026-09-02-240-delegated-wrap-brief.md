# #240 — DELEGATED WRAP BRIEF (Opus wrap sub; conductor = Fable, FILL near the advisory, judgment CLOSED)

**Seat:** `/sessions/funny-inspiring-turing/mnt/UX-design/` (bash) = `/Users/daviewen/Documents/Claude/Projects/UX-design/` (Read/Write/Edit). Branch `master`, HEAD `4fb75f6` (the #239 wrap). Working tree at brief time: `M knowledge/_rulings.json` (three rulings inscribed via `_inscribe_ruling.py`, reconstruction proof PASSED ×3) · `M notes/_REHEARSAL-LOG.jsonl` · `M notes/_dream/_GRADE-DECISIONS.jsonl` (both touched by `_checkin.py`, not by hand) · `?? knowledge/_probe/session-240.json` · this brief. **Form to COPY:** `notes/_briefs/2026-09-02-239-delegated-wrap-brief.md` (same shape, same fences) and the canon `knowledge/_RUNBOOK-capture-ritual.md` (read its newest stratum first; ritual steps 1a/1b/2c/2d/2f/2g and the `--wrap` call form are there, not here). `pip install tiktoken --break-system-packages` first. Sandbox: `TMPDIR=/dev/shm`, everything mount-side.

## THE SESSION IN SIX LINES (the WHAT; the WHY goes in the 1b dossier `_DECISION-HISTORY/2026-09-02-240-<slug>.md`)
1. **One morning, Fable conductor, NO lanes launched** — the window could not fit one (FILL 135,020 @ 21 turns after the opener + memory compaction; a lane ≈19K). Everything below is conductor-seat work.
2. **THREE RULINGS INSCRIBED, ALL DAVE'S, ALL IN THE STORE:** `s240-D1` the boot band is DERIVED from the last n=7 first-turn readings (figure shown to Dave first: **75,672 ± 641** over #234–#240) · `s240-D2` ONE typed ceiling, shrink-only, set from the FIRST POST-DIET BOOT (77,000 REJECTED as too high; no number chosen) · `s240-D3` a polarity receipt may point at an R1 row OR a ruling id; retired = `retiredBy` + drops from generated output — the legal form lane F found missing; V2 may run. Read `says` off the store; do not paraphrase them from here.
3. **THE ROSTER DIET (shrink-first, `s228-D6`) — DONE at Dave's seat and mine:** Dave REMOVED the Figma connector (its MCP disconnected mid-session, tools gone) and switched computer use OFF; "Disabled" did not work, removal did. `MEMORY.md` compacted **21,064 → 12,454 bytes** (6,104 → 3,509 cl100k), pre-compaction index verbatim in memory `hook-overflow-2026-09-02-240.md`. ⛔ Memory is NON-REPO — `(NON-REPO: Cowork auto-memory)` marker where it is cited.
4. **THE BOOT-GROWTH READING (unmeasured attribution, named as such):** staircase off `_GAUGE-LOG.md` — 57.7K #211 → 63K #212 → 67K #218 → 70K #220 → 75K #222 → 79K #224 → ~76K since. Memory ≈8.5K of it (measured #233); the rest READ off the conductor's own boot as the connector roster + Cowork's base prompt (Artifact tool, three control stacks) — a claim, not a measurement. The measurement is #241's first turn.
5. **Why 30 rows:** 30 is what the #236 R1 principles survey FOUND (`notes/_subreports/assets/2026-09-02-236-R1-principles-survey/tensions.json`), not a chosen limit — answered to Dave in chat.
6. **NOT DONE:** the boot-band build lane (`W-384`) — RULED, NOT BUILT, launches at #241 on a smaller boot · the polarity receipt build (schema + `_validate_polarities.py`) — RULED, NOT BUILT · the `s203-D1` CI read-back for `4fb75f6` — NOT RUN (declared, ~10K capped route owed) · the 4 UNRULED escapes untouched · `_validate_wiring.py` still red on #235's orphan.

## GAUGE — moments stated separately, never converted
boot **76,138 real** (first turn, `message.usage`, `_checkin.py`) — OUTSIDE the `s208-D1` band; ordinal NOT claimed; no constant moved (the constant is now superseded IN PRINCIPLE by `s240-D1` but NOT YET IN CODE — say exactly that) · opener FILL **100,276 @ 6 turns** · post-compaction FILL **135,020 @ 21 turns** · at brief time: read `_checkin.py` yourself for the current FILL and quote it · probe PLANTED 19:39:56Z session 240, **DECLARED GAP: the conductor piped `--plant` through `tail -8` and lost one of four facts — quiz ceiling 3/4 this seat; one plant per session, so it stands declared** · subs **0 tokens (n=0)** before this wrap sub · effort band **M**.

## CARRIES → #241 (`_CARRIES.md` § `## residual → #241`; bump every age bracket; a STRIKE keeps the struck text verbatim; NEW items at age 0)
NEW: ① the boot-band BUILD lane from `s240-D1`/`s240-D2` (brief `notes/_briefs/2026-09-02-240-boot-band-derive-brief.md` stands, now RULED; ceiling value = first post-diet boot, read at #241's opener) · ② the polarity-receipt BUILD from `s240-D3` (schema `$seed {ruling}` / `retiredBy`; then V2) · ③ the `s203-D1` CI read-back for `4fb75f6` AND for this wrap's push, capped at run page + one JS grep · ④ boot ATTRIBUTION is a claim — #241's first turn measures the diet · ⑤ the `_recall_probe.py --plant` runbook line (`--session N` required; never pipe). STRIKE (partial, with receipt): "THE BOOT BAND — #240's FIRST QUESTION" → RULED `s240-D1`/`s240-D2`, build carried as ①. STRIKE (partial): "no legal form for a NEW/RETIRED polarity" → RULED `s240-D3`, build carried as ②. Everything else carried at its age.

## TITLE THE NEXT CHAT → `Apollo - #241: measure the diet, build the derived band`
Deliver BOTH title lines in the stub (this one and #240's own, `Apollo - #240: rule the boot band, then verify the fixed gate`).

## DO-NOT-RULE (the fence — Dave's #110 lesson; a wrap sub that rules is the defect)
No further ruling inscribed, no `s240-D*` re-worded, no `_inscribe_ruling.py` call. No W-row CLOSED (`W-374`, `W-384` stay open — nothing is BUILT). No constant / band / floor / advisory / stop line / wall / roster / version move in code; `BOOT_FIRSTTURN_TK` UNTOUCHED — `s240-D1` is ruled, not enacted. No memory write (done at the conductor's seat). No edit to any lane report, asset, brief, plan or review (ADR-0017). No `_build_all.py`. No render. No `_memento_search.py` retrieval beyond what the runbook's steps require. Mint W-rows for this brief and for the two build items if the store lacks them (forgotten-document class). 2c/2d/2f exit checks as PROSE with every probe NAMED; 2g rebuilds `_memento-index.json` LAST. Declare every skip with its size.

## PUSH — AUTHORISED (`s207-D1`, conductor's judgment): `bash knowledge/_git_commit.sh --wrap … --push` per the runbook, AFTER the wrap commit, clean tree; read the remote hash back (`git rev-parse origin/master` == HEAD) — only `✅ pushed and VERIFIED` counts. The CI read-back is the CONDUCTOR'S, carried to #241.

## STUB back to chat (≤14 lines): wrap commit hash + subject READ BACK from `git log -1` · push verdict with both hashes · GM/LS/chain sizes from the gate's own stamp · 2c/2d/2f/2g receipts (line counts) · carry count by `_capture_gate._carry_items` · declared skips with sizes · BOTH title lines · anything you STOPPED on. File the full report at `notes/_subreports/2026-09-02-240-wrap.md`.
