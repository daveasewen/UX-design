# BOOT COLD — #286, 2026-09-18

**A READING, NOT A VERDICT.** n=1 against n=1. Everything below is measured by the conductor at
this session's first turn via `python3 knowledge/_checkin.py --window 200000 --no-block`; nothing
here is a re-base and no constant moved.

## THE TWO FIGURES

| session | boot, real | n | how |
|---|---|---|---|
| **#286 (this)** | **73,832** | 1 | `_checkin.py --window 200000 --no-block`, first turn = system + tools + MEMORY.md + CLAUDE.md |
| #285 (previous) | 80,863 | 1 | same instrument, same moment in the session |
| **delta** | **−7,031** | — | one reading minus one reading, NOT a trend |

## THE ONE VARIABLE THAT CHANGED

Dave set the built-in Browser connector's **17 tools (`mcp__Claude_Browser__*`) to BLOCKED** in the
Tool-permissions panel at #285.

Held constant, by design:

- **skills untouched** — dave-voice, dream-pass, swiss-design-system, docs, deep-research,
  import-memory (gtb-brand off)
- **computer use off**
- **connectors still on** — Claude Docs, Claude in Chrome, GitHub

★ One variable and a −7,031 move is the cleanest boot experiment this record carries. It is still
**one reading against one reading**, and a single pair cannot separate the lever from the ordinary
session-to-session spread (the last seven post-diet readings span 72,110–83,636, a spread of
11,526 — wider than this delta). ⛔ So: the direction is consistent with the block having worked;
the magnitude is not established.

## WHAT WOULD MAKE IT A VERDICT

**A second cold boot on the same setup** — Browser blocked, skills and connectors unchanged,
computer use off — read with the same instrument at the first turn. Two readings inside a few
hundred tokens of each other would turn this into a measured effect; a reading back near 80K would
say the delta was spread, not the lever. Nothing short of that second boot earns the word "caused".

## THE CEILING IS STILL BREACHED

`_gauge_tokens.BOOT_CEILING_TK` = **70,000**. 73,832 is **3,832 OVER** it.

- The breach therefore **continues** — this is the **11th post-diet reading over the ceiling**
  (the gate's own list, read live at this session, names seven of them by session: #277 83,636 ·
  #278 72,110 · #279 72,447 · #280 75,740 · #281 77,400 · #282 77,474 · #283 80,871; #284, #285
  80,863 and this one continue the run, and the gate will only see the last two once the log is
  written).
- ⛔ `s240-D2` / `s241-D1` keep the literal **SHRINK-ONLY**: boot may go DOWN past 70,000 and never
  up. A 7K improvement is **not** a licence to raise the number — it is progress toward it.
  Raising it is Dave's word alone.

## THE NEXT CANDIDATE LEVER

**The next-largest connector's tool descriptions.** The remaining connectors are Claude Docs,
Claude in Chrome and GitHub; Anthropic's own help-centre note is that *tools and connectors are
token-intensive*, which is the reasoning that made the Browser block worth trying.

⛔ **SIZES UNMEASURED, AND NOT GUESSED.** `_checkin.py` reports boot as ONE figure — its
decomposition into system prompt / tool schemas / deferred-tool list / MCP instructions is
`ds-025` item 1 and is still dark from every mount; the transcript exposes no per-tool-description
sizes either. So no connector is ranked here, and no expected saving is stated. To make a
candidate measurable, either (a) the boot decomposition becomes readable, or (b) the same
one-variable experiment is repeated per connector — block one, read the cold boot, restore.

## FILING NOTE — THE WRAP LOGS 73,832 ONCE

`_checkin.py`'s own structural arm quotes the rule verbatim: a session states a first-turn figure
**ONCE, in the `post-mortem #N:` line** of `notes/_GAUGE-LOG.md` (`s241-D2`). The arm is live and
fires today on #243 (5 statements), #264, #272, #273 and #274 (2 each).

⇒ **The #286 wrap must write 73,832 exactly once, in the `post-mortem #286:` line.** A second
statement of it anywhere in the log is a double-count that displaces a real session from the
derived band's window — and this file is deliberately NOT in `notes/_GAUGE-LOG.md` for that reason.
