# #286 — CLOUD-MEMORY HOOK (capture-ritual step 3)

provenance: 286 · 2026-09-18
status: observed

**WRITTEN FIRST, BEFORE ANY STORE CALL, AND THE RESULT IS RECORDED AT THE FOOT RATHER THAN
ASSUMED.** The runbook's step-3 line still calls the delegated-sub seat limit *structural*; this
seat reached the store again, and **the runbook line is NOT edited here — amending a ratified step
is Dave's word alone.**

---

## THE FILE BODY → `wrap-286-the-connector-lever-reads-and-the-masters-are-sized.md`

**#286 (2026-09-18) — THE CONNECTOR LEVER READS AND THE MASTERS ARE SIZED.** Conductor Fable 5.1,
ONE DAY, no date split, **NINE Opus lanes (S · R · G · T · V · C · P · R2 · C2), every one an
`Agent` at depth 1, none in seat**, with a delegated Opus 5 wrap sub.

⛔ **NO RULING INSCRIBED — `knowledge/_rulings.json` STAYS AT 620**, verified by `json.load` with no
`s286-` id in the store.

★★★ **THE CONNECTOR LEVER READ, AND IT IS A READING AND NOT A VERDICT.** Boot COLD **73,832 real
(n=1)** against #285's **80,863** — **delta −7,031** — with **ONE variable changed**: Dave set the
built-in Browser connector's 17 `mcp__Claude_Browser__*` tools to BLOCKED. Skills, computer use and
the other three connectors held constant by design. ⛔ **One reading against one reading cannot
separate the lever from the spread** — the last seven post-diet readings span 72,110–83,636, a
spread of 11,526, **wider than the delta** — so the direction is consistent with the block having
worked and **the magnitude is not established**; a SECOND cold boot on the same setup is what would
make it a verdict. ⛔ **73,832 is the ELEVENTH post-diet reading over `BOOT_CEILING_TK` 70,000 and
the literal is SHRINK-ONLY: cut the boot, never raise the number.**

★★★ **THE 40 MASTERS ARE REGISTERED AND `W-285lm` CLOSES.** His two sentences today, verbatim:
***"okay go on everything"*** (his whole reply to an opener naming the standing wording, the
masters' registration and the 256,000 fix, plus the cold-boot question) and ***"okay
size-on-the-existing-node"*** (the SHAPE). A master is a **size field on its lockup's existing
node, never its own node**; lane R2 wrote a `sizes` map onto all **8** nodes, five raw heights each,
merge-on-write. Verified at the wrap seat by `json.load`: **8 nodes · 33 edges · 8 of 8 carrying
`sizes` · 12 `governedBy` intact**. ⚠ **The field's KEY NAME is the lane's reading of the file's own
plural vocabulary, NOT his word.** ⚠ Lane R first reported the masters **not registrable** — the
generator is blind to `masters/` — which is why R2 exists.

★★ **`knowledge/_standing.md` IS OUT OF DRAFT AND `W-285sc` CLOSES**, on a change that is **8
insertions / 3 deletions in ONE hunk, entirely above the `---` rule, with the eight lines
BYTE-IDENTICAL**. ⛔ **Reading *"go on everything"* as ratification of the wording AS WRITTEN is the
CONDUCTOR'S reading, not Dave's sentence** — recorded as such so the act stays reversible by one
word from him. ⚠ `knowledge/_seam.py` still calls the file a DRAFT at every seam and was not edited.

★★ **#285'S MOVES 2 AND 3 ARE BUILT:** `_seam.py` gains an `INSEAT` arm (+214/−6) counting the
conductor's own in-seat tool output since the last seam; `_git_commit.sh` gains `--quiet` (+60/−1).
⛔ **`INSEAT_WARN_TK = 10_000` is PICKED, not ruled, and the file says so.**

⛔ **THE 256,000 WORDING IS FIXED IN THE GAUGE WITH THE CONSTANTS UNMOVED (+25/−3, `BUDGET_HARD`
still `256_000`) AND 58 PROSE LOCATIONS ACROSS 10 LIVE FILES ARE STILL OWED** — ratified runbook and
ruling text, including `_RUNBOOK-context-gauge.md:85`'s *"256,000 stays the UNQUALIFIED wall"*.
**Amending them is his.**

✅ **LANE V, ADVERSARIAL, 24 of 24 PASS** — and it **overwrote `knowledge/_capture_gate.py` with
HEAD mid-run and restored it byte-exact**, and moved two symlink farms to `_to_delete/`.

⚙ **FILL 162,555 real / 22 turns** measured first-hand against **161,544 / 20 declared**, Δ **1,011**
— ✅ **17,445 INSIDE the 180,000 QUALITY line, the first session in five**; 256,000 clear for the
fourth running. **subs 1,113,248 real (n=9)**, QUOTA never FILL; **the nine lane replies cost the
conductor's window 3,399 cl100k in total.** ⛔ **His delegation sentence is still uninscribed after
a second session of obedience.** ⛔ **6 inherited gate fails carried in the `#243` form, the TWELFTH
wrap running — a wrap may not repair them.** ⚠ CI is **completed / failure** on `7ed49d37`,
`09ddf155` and `d2ae9c73`, failing the same two steps — **inherited, and it resolves the colour
#285 could only read as `in_progress`.**

**Repo is the record, memory is the accelerator — UNCHANGED.**

---

## THE INDEX LINE → newest-first at the top of `index.md`

- [★★ #286 WRAPPED · THE CONNECTOR LEVER READS −7,031 AND THE 40 MASTERS ARE SIZED ONTO THE EIGHT
  NODES · NO RULING INSCRIBED (620 UNCHANGED) · 9 OPUS LANES, NONE IN SEAT · THE 180,000 QUALITY
  LINE HELD FOR THE FIRST TIME IN FIVE](wrap-286-the-connector-lever-reads-and-the-masters-are-sized.md)
  — see the file for the body.

---

## ⬛ OPEN, DAVE'S — every one written as **the QUESTION IT IS** (`s271-D4`), never as a state of the world

Each item below was **put to Dave at the wrap call** and none of them is a claim that the world is
a certain way.

1. **Whether *"okay go on everything"* is inscribed as a ruling** — put to Dave at the wrap call.
2. **Whether *"okay size-on-the-existing-node"* is inscribed as a ruling** — put to Dave at the
   wrap call.
3. **The `sizes` KEY NAME** — he named the location, not the spelling; put to Dave at the wrap call.
4. **`INSEAT_WARN_TK = 10,000` and the INSEAT block's placement** — PICKED, not ruled; put to Dave
   at the wrap call.
5. **The 58 prose "wall" locations**, `_RUNBOOK-context-gauge.md:85` among them — amending ratified
   runbook text is his; put to Dave at the wrap call.
6. **`knowledge/_seam.py`'s DRAFT line about `_standing.md`** — stale since `1caaa0b1`; put to Dave
   at the wrap call.
7. **The KG explorer, not rebuilt to show the new `sizes`** — put to Dave at the wrap call.
8. **The store's id regex refusing `W-286r2`-shaped ids** — third instance in two sessions; put to
   Dave at the wrap call.
9. **The delegation rule, obeyed a second session and still uninscribed** — put to Dave at the wrap
   call.
10. **A RESUME contract for the ritual** — put to Dave at the wrap call.
11. **`_seam.py`'s SCRATCH arm** — keep-list, opt-in at a seam, or lanes forbidden `/tmp`; put to
    Dave at the wrap call.
12. **The memory archive at 48,990 of 49,152 B** — see the foot of this file; put to Dave at the
    wrap call.
13. **The boot ceiling**, 73,832, the eleventh breach — cut the boot, never raise the literal; put
    to Dave at the wrap call.
14. **The second cold boot** that would turn the −7,031 into a verdict — put to Dave at the wrap
    call.
15. **Everything on `_HANDOFF-130`…`-136` not closed today** — put to Dave at the wrap call.
16. **The two-probe-shape question on the §A digest**, at its eighteenth session — put to Dave at
    the wrap call.
17. **Whether a verifier lane may mutate the tree it verifies** — raised by lane V's byte-exact
    restore; put to Dave at the wrap call.

---

## ⛔ THE RESULT OF THE STORE WRITE — RECORDED HERE, NOT ASSUMED

**(1) The file.** `wrap-286-the-connector-lever-reads-and-the-masters-are-sized.md` was written to
`/projects/01a0a457-df26-7151-80c8-7d2f74bf7c97/` from THIS wrap sub's own seat. ✅ **The runbook's
"structural" seat limit is measured FALSE again** — and ⛔ **the runbook line is still NOT edited,
because amending a ratified step is Dave's.**

**(2) The index cut, and the ceiling it met.** `index.md` keeps the NEWEST THREE wrap lines and
older ones move **VERBATIM** to `MEMORY-ARCHIVE.md`. ⛔ **THE #283 LINE COULD NOT MOVE, AND IT WAS
NOT TRUNCATED.**

- `MEMORY-ARCHIVE.md` measured **48,990 B** in the store listing at this seat, against a documented
  cap of **49,152 B** ⇒ **162 B of headroom.**
- The #283 index line runs to roughly **4,300 bytes** — **more than twenty-six times the headroom.**
- ⚠ **HOW THIS WAS ESTABLISHED IS STATED PLAINLY RATHER THAN DRESSED UP AS A REFUSAL THAT WAS NEVER
  RECEIVED:** it is **arithmetic on two measured quantities** — the listing's byte count and the
  documented cap — **not a rejected write.** No append was attempted, because the only way to obtain
  the archive's version token is to read the whole 48,990 B into this seat, and a failure whose
  arithmetic is already certain is not worth that price. **If a later seat wants the refusal itself,
  the attempt is one call away.**

⇒ ⛔ **THE NEWEST-THREE RULE IS SUSPENDED FOR ONE SESSION, DELIBERATELY AND BY DECLARATION:
`index.md` now carries FOUR wrap lines — #286, #285, #284, #283 — because the alternative was to
truncate the #283 line, and a truncated verbatim move is not a move.**

⚠ **NOTHING IS AT RISK, AND THAT BELONGS BESIDE THE ALARM:** the repo is the record and memory is
the accelerator. The handoffs, dossiers, ledgers and git hold every one of those sessions in full.

⬛ **CARRIED FOR DAVE, AND IT IS HIS:** what to do about the archive — condense it, split it, or
something else — is his call, and the dream pass is its usual seat. It stands as item ⑨ of
`_CARRIES.md` § `residual → #287`.
