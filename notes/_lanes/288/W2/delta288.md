## ⏱ LATEST DELTA — 2026-09-19 (Sat from `date`) (**#288**, ✅ **ONE DAY, NO DATE SPLIT**, conductor **FABLE 5.1**, **FIVE DELEGATED OPUS SEATS — M · B · A · T · P**, with this **OPUS 5** wrap sub, DELEGATED — ★★ **APOLLO COMPOSES, NOT TRACES — AND HE NAMES THE DEFECT CLASS: ALIGNMENT, SPACING, DIMENSIONS**)

*(WHY/HOW: `_DECISION-HISTORY/2026-09-19-288-apollo-composes-and-the-sloppiness-class-is-named.md`. **The handoff is `_HANDOFF-139-apollo-composes-and-the-sloppiness-class-is-named.md` and it OUTRANKS `_CHAIN.md`.** His words verbatim: `notes/_lanes/288/DAVE-RULINGS-2026-09-19.md`. The five filed reports are cited by path below, per `s218-D7`.)*

- ⛔★★★ **READ `_HANDOFF-139-apollo-composes-and-the-sloppiness-class-is-named.md` FIRST — IT IS NEWER THAN `_CHAIN.md` AND OUTRANKS IT.** ⛔ **It does NOT replace `_HANDOFF-130`…`-138`** — every open item on those nine still stands, and **NOTHING was struck today**. ⚠ **Zero strikes is a JUDGMENT with a reason, not an oversight:** the one thing #288 closed that can be receipted is that the `s219-D3` generation arm is BUILT, and the carry that names it reads *"MONO'S 0 IS DOUBTED AND THE `s219-D3` GENERATION ARM IS TO BE BUILT"*. **A strike removes the whole headline** (`s183-D1`) and the first half stays true — `s217-D2` still rules the value 0 for mono and Dave ruled nothing on it today. ⇒ **The discharge is said in NEW carry item ⑩ instead**, which is the move #287 made for the connector-lever item, for the reason `s271-D4` gives in its own words: *"a strike that is wrong is worse than an item that is merely stale."*

- ⛔★★★ **NO RULING WAS INSCRIBED AND `knowledge/_rulings.json` STAYS AT 622** — verified at this seat by `json.load` over the `rulings` list, with **no `s288-` id present**. **He did not say *"inscribe"* today.** ⇒ **All TEN ruling-shaped things this session produced are carried as QUESTIONS PUT (`s271-D4`), never as states of the world**, and the list is on the handoff and in `_CARRIES.md` § `residual → #289`.

- ★★★ **HE CHALLENGED THE PREMISE OF THE WHOLE ONE-SHOT PATH, AND THE SESSION CHANGED SHAPE IN ONE MESSAGE.** On seeing the showroom template after wave 1 landed, verbatim: *"there is a definitely a problem here, 2 actually, the template is a bit wonky and it's basically copied by the AI, what is the point of building the KG if the agent just traces an existing file, its very safe but 1. its of average quality anyway... its okay but not great 2. what was the point of building the system if it just traces from existing examples???"* and *"the results with VS co-pilot are pretty much identical to this, this isn't Apollo its a dot-to-dot book"*. ⇒ **TWO problems, both his: the template's QUALITY, and a delivery path that COPIES it.** ⛔ **The mechanism is named and is not a ruling: the #230 *"zero invented markup"* pass condition REWARDS tracing** — a builder that splices the template byte-identically scores perfectly against it. ⚠ **Whether that condition is re-cut, scoped or replaced is HIS and was not decided here.**

- ★★★ **AND THEN HE RULED BY EYE THAT IT COMPOSES — THE PROBE ANSWERED THE CHALLENGE THE SAME AFTERNOON.** A fifth Opus lane ran a cold one-shot with the template UNREACHABLE. His verdict, verbatim: *"so better is someways and worse in others, its more interesting and complete page , but its more sloppy ."* · *"I'll create a test brief for these tests."* · *"we need to do something about the sloppiness and, its all about alignment spacing and dimensions, this looks like its working the way I'd have expected, at least it's diverged from the tamplete"*. ⇒ ★★ **THE METHOD IS ENDORSED AND THE EXECUTION IS NOT, and the two halves must not be collapsed into one verdict.** ⛔ **THE DEFECT CLASS IS NAMED BY HIM, not derived by a lane: sloppiness = alignment, spacing, dimensions.** ⬛ **The test brief for these probes is OWED BY DAVE HIMSELF** — *"I'll create a test brief"* — and no lane may write it. ⚠ **What the conductor saw on the render is recorded as the CONDUCTOR'S reading and never as his:** KPI tiles with dead space below the sparklines, a half-empty programmes-table card against a full-height decisions card, two bottom-row cards not sharing a bottom edge, a filter bar narrower than the table it filters, and column gutters differing between the KPI row and the rows below.

- ★★ **THE `s219-D3` GENERATION ARM IS BUILT — SEVENTY SESSIONS AFTER IT WAS RULED.** Lane A (`notes/_subreports/2026-09-19-288-A-s219-d3-arm.md`) wrote a new generator `knowledge/canon/gen_bento_role_vars.py`, **289 lines**, which is the **FIRST CONSUMER** of `knowledge/_render/_bento_edit_rails.json` — the `s219-D3` *one generated file*, self-declared `$groundwork_only` and un-consumed since #219. It **cross-checks the manifest against the owner the file itself names** (`gen_foundations_217.ROLE_DEFAULTS` → `role_defaults_219.DEFAULTS`, parsed from Dave's own receipt `notes/_receipts/2026-08-25-219-role-defaults-exports.md` rather than typed) and **a divergence is a NAMED REFUSAL, not a silent pick**. It mints per-theme `--bento-dashboard-main` / `--bento-dashboard-sub` between new `AUTO-BENTO-ROLE-VARS` markers placed **BEFORE** `AUTO-THEMES START`, because `gen_theme_cascade.py` rebuilds everything from that marker onward and would silently destroy a block placed after it — **asserted by the generator's own selftest bite, not remembered**. The snippet's two pinned literals at `:802` / `:804` become `var()` references whose mono fallbacks are load-bearing (no snippet links `canon.css` — 0 of 137). ✅ **Gates green, quoted verbatim in the lane's report:** `gen_bento_role_vars --check OK — 4 themes x 2 dials in sync with the rails manifest` · `gen_bento_role_vars selftest OK — 6 bites` · `gen_canon_components --check OK — 137 components in sync` · `gen_theme_cascade --check OK — 230 override path(s), 397 component projection(s)` · `binds-resolve gate: 137 snippets … 0 failure(s)`.

- ⛔★★ **AND THE ARM DOES NOT SETTLE MONO'S 0 — TWO OUTER READINGS AND THREE INNER ONES ARE PUBLISHED, AND NO WINNER WAS PICKED.** The OUTER (structural bento) gutter reads **0/24/24/0** from the token `layout/bento/gutter` (`s217-D2`) and **40/24/40/24** from the role defaults in the rails manifest, which is what the arm delivers **because that is the file `s219-D3` names**. The INNER (embedded bento / tile-group) gutter has **three** answers: canon's literal `1px`, the `subSpacing` reading 4/4/4/2, and the template's pinned `4`. ⛔ **Supercharge's `0` is INHERITED from the token table and was never decided by anyone.** ★ **Lane A DECLARED its pick rather than hiding it and picked no winner between the outer readings**, which is exactly what Dave's #287 gutters correction requires — two deliberate quantities, not a contradiction. Lane B confirmed the structure **from source at HEAD**, reading every line through `git show HEAD:<file>` because lane A's edits were uncommitted in the same tree: `canon.css:1082` gives any bento the theme gutter, `:1182` gives a plain-tile dashboard the inner `1px`, and `:1198` restores the theme gutter for a **bento-of-bentos**, with canon's own comment saying so — *"a dashboard whose tiles are themselves bentos IS the outer wall."*

- ★★ **THREE MORE DELIVERABLES LANDED FOR HIS EYE, EACH FILED WITH ITS RECEIPTS.** **(1) The strand map**, `notes/_STRAND-MAP-2026-09-19.html` — **53,042 B**, swiss-design-system idiom, **13 sections**, fully self-contained (zero `<script src=>`, zero `<link>`, probed not assumed), **all 13 anchors resolve**, and **41 distinct cited paths all exist** by `os.path.exists`. ⛔ **It puts WHICH EVENT FRIDAY 2026-09-25 IS on a full dark band of its own and declares it NOT ESTABLISHED** rather than guessing between the small internal demo and David Rice. **(2) The four-theme gutter sheet**, `notes/_lanes/288/B/four-themes.html` — read-only, HEAD-sourced, **16 PNGs and 16 one-to-one seam crops**, with the numbers taken by `getBoundingClientRect()` rather than by eye. **(3) The template quality review**, `notes/_lanes/288/T/template-quality-review.html` — headline: ***the drawn page is sound and the paperwork is not.*** Zero invented markup, zero dangling `var()`, zero raw `font:` declarations, zero hex outside the theme blocks — and **four of the five claims in the meta's own `$status` block measured FALSE**, with a six-question decision pack attached and unanswered.

- ⚠★★ **THE PROBE MEASURED THINGS NO GATE IN THE TREE CAN SEE, AND EACH WAS MEASURED ON A LIVE RENDER.** `notes/_subreports/2026-09-19-288-P-composition-probe.md` — **39 reads, 0 fenced**, `COUNTS: decisions 39 · gaps 10 · findings 6 · UNPROVEN 2`. **(i) Canon's own instance-dial recipe loses silently:** canon instructs *"declare instance dials as `.c-bento.my-wall{…}`"* at specificity **(0,2,0)**, and the `:has()` rule that restores the outer gutter for a bento-of-bentos is **(0,4,0)** — measured, a bare `.c-bento.wall-ops{--bento-gutter:40px}` had **no effect** and the wall rendered at **0px, not 40**. **(ii) A nested bento cannot carry its own column count below 1100px** — the compiled bands answer the WALL, not the window; a one-column inner wall 896px wide rendered as **three** columns. **(iii) Canon CLIPS SILENTLY when DP-18 is broken** — a 694px list stretched a row to 791px inside a wall canon fixes at 320px and `overflow:hidden` swallowed it with **no DOM symptom and `pageErrors: []`**. **(iv) No gate reads the composition of a page that LINKS `canon.css`** — `_validate_composition.py` reads the grammar from the artefact's own `@container` blocks, so the orphan-cell arithmetic `DP-16` and `s249-D5` rest on is uncheckable there, and this is the first such page in the tree. ⛔ **Ten gaps named `G1`…`G10`; none fixed, none ruled — every one is a generator or gate change.**

- ⛔★★ **THE FROZEN DEMO PROMPT IS NOT RECOVERABLE FROM THE RECORD, AND THE ABSENCE IS REPORTED AS AN ABSENCE.** Lane P searched `_memento_search.py` (and `--all`), the #229 cold-start acceptance brief, the #230 demo-day brief, the #258 demo-fence brief, and grepped the repo for the prompt's own distinctive phrases. **Every brief DESCRIBES the test; none carries the prompt's text.** The probe therefore ran on the lane brief's FALLBACK wording, **said out loud on the sheet**, and ⛔ **the two pages compared side by side ANSWER DIFFERENT BRIEFS** — the traced template was built at an earlier session against a business-banking account overview. ⇒ **The comparison is about HOW each page was made, never about which answers a shared ask better**, and that limit is printed on the sheet rather than buried. ★ **A lane that prints the limit of its own comparison is the reason his verdict can be trusted as a verdict on the METHOD.**

- ⚙ **GAUGE, MEASURED AT THIS SEAT by importing `_checkin.read_fill` rather than re-implementing it,** against the conductor's own transcript `f0d5834b-1313-45d3-9961-2f10be3c3817.jsonl` — this wrap seat being `subagents/agent-aa9e27e75caeae075.jsonl` beneath it, which is what makes the subtraction legal [[measure-dont-convert-units]]. **FILL 197,234 real over 33 turns** against **180,585 real / 27 turns DECLARED** at the wrap-brief cut ⇒ **delta 16,649** — the hand-over's own cost, not a disagreement, and the conductor kept working (six further turns) after cutting the brief. ⛔ **17,234 past the ruled 180,000 QUALITY line** — the brief's own reading was 585 past it, so the ritual fired AT the line as `ds-023` asks and the rest is hand-over. ✅ **22,766 INSIDE the ≤220,000 tolerance.** ✅ **256,000 clear for the sixth session running.** ✅ **`compaction_records` 0 and `drops` 0**, so the reading is a fill and not an artefact. ✅ **BOOT 74,174 real, DECLARED AND MEASURED AGREEING TO THE TOKEN** — and that agreement is the `s214-D5` precondition for `delta` being a legal subtraction at all. ⛔ **74,174 is the THIRTEENTH post-diet reading over `BOOT_CEILING_TK` 70,000 — SHRINK-ONLY (`s240-D2` / `s241-D1`), NOT a re-base, and raising the literal is Dave's word alone**; the figure is stated ONCE, in the `post-mortem #288:` line (`s241-D2`). **n=3 on the Browser-blocked setup: #286 73,832 · #287 74,120 · #288 74,174** — a spread of 342 where the prior seven-reading spread was 11,526.

- ⚙ **subs 1,004,095 real (n=5)**, QUOTA and never FILL [[budget-vs-quota-vocabulary]] — **five delegated Opus seats, every one an `Agent` at `spawnDepth 1`, model opus, and NO lane work was done in seat**: M 162,304 · B 177,908 · A 174,021 · T 190,369 · P 299,493, the harness's per-agent figures as the conductor declared them. ⚠★★ **AND THE DEFINITIONAL SPLIT #287 FOUND IS RE-MEASURED HERE AND RECONCILES AGAIN TO THE TOKEN.** Read with `_checkin.read_fill` — input side only, the ONE definition of FILL this repo has — the same five seats sum to **998,602**: M 161,333 · B 176,769 · A 172,457 · T 189,501 · P 298,542. **The gap is 5,493 and it is exactly each lane's final-turn `output_tokens`, matching on every one of the five** (971 · 1,139 · 1,564 · 868 · 951). ⇒ **Both published, neither rewritten, and which one the `subs` line means is still HIS** [[measure-dont-convert-units]] — its SECOND consecutive wrap. ⚠ **A SIXTH SUB — this wrap seat itself — is EXCLUDED BY DECLARATION and no placeholder is written for it**: its own total cannot be final from inside itself, and `s214-D5` forbids turning an unknown into a number [[feedback-measuring-tool-must-not-guess]].

- ⛔ **SIX BLOCKING GATE FAILS STOOD AT THIS RITUAL'S OPEN, ALL INHERITED, CARRIED IN THE `#243` DECLARED NOT-A-WRAP FORM — THE FOURTEENTH CONSECUTIVE WRAP ON THAT PATH.** Every one is another session's append-only testimony in `notes/_GAUGE-LOG.md`: the **boot-drift CEILING BREACH** and **five boot double-counts** (#243 ×5, #264, #272, #273, #274). **A wrap may not repair an inherited gate fail** — the ceiling's remedy is to CUT THE BOOT, never to raise the literal [[gate-must-quote-what-it-forbids]]. ⚠ **Also standing and NOT repaired:** `carry_wording_check()` is **BLIND** (both residual lines are pointers, so it has nothing to pair and goes quiet — the `s188-D2` invariant was held BY HAND here, for the fourth wrap); `_gate_scratch_hygiene.py` reports the fill of `/` rather than `/sessions`; `_seam.py`'s SCRATCH arm still deletes a lane's `/tmp` files mid-run; and `knowledge/_render/seat_env.sh` globs a playwright path 1.63 no longer ships — **lane B symlinked around it and lane T could not render at all**, and the one-line fix is owed and NOT done.

- ⚠ **FOUR JUDGMENTS WERE MADE BY LANES OR BY THE CONDUCTOR THAT COULD HAVE BEEN DEFERRED, AND EACH DECLARED ITSELF RATHER THAN BEING DISCOVERED HERE.** **(a)** Lane A ran **`git stash push`** to A/B a gate against the pre-change tree — **a git write from a build lane, against the standing rule** — and declared it: no stash entry was created (`git stash list` empty before and after), no file was altered, and the tree was verified intact immediately afterwards. **(b)** Lane P's `_validate_screen.py` run wrote a stray `knowledge/_screen-gate/composed-dashboard.md`, which the conductor moved to `_to_delete/288-P-stray/` by **`mv`, never `rm`** (the #284 / #286 / #287 precedent), with `.gitignore` untouched; `_COMPOSE-AUDIT.md` was restored to HEAD by the lane itself. **(c)** Lane B **read every line number through `git show HEAD:<file>`** rather than from a working tree another lane was editing in parallel, and said so first in its report. **(d)** Lane T **declined to report a CSS reading as a rendered one** when the render seat failed. ★ **A judgment named in a report is a judgment the conductor can correct; a judgment discovered at the wrap is one he inherits.**

- ⚠ **FOUR OF THE FIVE FILED LANE REPORTS CARRY NO `RULING-SHAPED QUESTIONS` HEADING, AND `s218-D7` CLAUSE 4 MAKES IT MANDATORY** — measured at this seat by grep over the five, not assumed. **Only lane M carries the heading.** Lane T carries a *Decision pack — six questions* doing the same work under a different name; lanes A, B and P carry the substance under *WHAT IS NOT DONE*, *WHAT I COULD NOT ESTABLISH* and *Gaps*. ⛔ **The CONTENT is present in every case — what is missing is the NAME the gate and the next reader look for**, and `subreport_citation_check` parses exactly that heading. Also measured: **only lane P carries a `COUNTS:` line and only lane T a `REPLAY-THESE:` line.** ⛔ **NOT REPAIRED HERE — a wrap does not edit another seat's filed report**, which is dated history (`ADR-0017` / `s192-D1`). ⚠ **Whether the brief template or the gate's tier is what moves is his**; the tier is one line, `SUBREPORT_CITE_BLOCKING`.

- ⚠ **THE `$awaitingDave` COUNT HAS THREE READINGS AND ALL THREE STAND.** `_HANDOFF-138` says **four**; lanes M and T each read **three** at HEAD (`:190-193`, with two `$tokenGaps` at `:195-196`); lane A's additions take the tree to **five** by ADDITION. ⛔ **Nothing is reconciled and nothing is rewritten** — the handoff's four is a dated reading, three is HEAD's, five is the tree after lane A, and **no fourth number is minted here** because the file itself answers the question for one read [[measure-dont-convert-units]].

- ⚠ **DECLARED SKIPS AND NOT-DONE AT THIS WRAP, EACH NAMED WITH ITS SIZE** (declare-last, and the size is the point). **2c, 2d, 2e and 2f ALL RAN**, so the `size:` stamp carried in `GOOD-MORNING.md`'s header is **THIS session's own measurement and not #287's** — re-measured after every move landed. **2e was a NO-OP at 0 lines:** DO-FIRST minted no notice, no tombstone and no lifecycle tag this session, and nothing was retro-tagged. **`knowledge/_REVIEW-SIGNOFF.md` WAS TOUCHED** — #288 produced **three** artefacts awaiting Dave's eye (the strand map, the four-theme sheet, the template quality review) and `s218-D7`'s sibling clause at ritual step 1 puts their PATHS into the register's running list, not only onto a banner. **NOT DONE and named:** the `seat_env.sh` one-line playwright fix; the showroom re-sync after the arm; a `--check` arm for `dashboard/index.html`; and the four fenced generators (`gen_kg_rules.py`, `land_rests_on.py`, `gen_kg_icons.py`, `_build_all.py`) which were **not run by declaration** and whose two REGEN-SERIAL advisory warns therefore stand uncleared, as at #287.

- **CLOUD MEMORY (step 3):** the hook was written FIRST at `notes/_lanes/288/WRAP-MEMORY-HOOK.md` — the index line plus the file body, in the `s271-D4` form, every open item worded as *"put to Dave at the wrap call"* — and the placement result is recorded **at its foot, after the attempt**, never predicted before it. ⛔ **`MEMORY-ARCHIVE.md` STANDS AT 48,990 OF ITS 49,152 B CAP — 162 B OF HEADROOM — AND WHAT THAT MEANS FOR THIS WRAP IS DECLARED RATHER THAN DISCOVERED:** any line that should roll out of `index.md` runs to thousands of bytes, so **no roll is possible and NOTHING IS TRUNCATED, because a truncated verbatim move is not a move.** The index therefore carries one more wrap line than the cap's design intends and says so in its own closing note. ★ **Nothing is at risk — the repo is the record.** **What to do about the archive is Dave's, and the dream pass is its usual seat** — its THIRD consecutive wrap as a carry.

- ⚠ **THE CARRY SET IS `_CARRIES.md` § `## residual → #289` (`s225-D2`)** — the banner holds a pointer, the top item's headline, the retrieval id `carries:residual-289`, the count and the command that recomputes it. **572 probeable items, SEVENTEEN new, ZERO STRUCK.** ⚠ **All seventeen new items are INVISIBLE to the probe** (`_AGE_RE` misses `[NEW — 0]`) — **the ELEVENTH wrap running to say so** — so **560 → 572 is #288's own twelve new items ageing in**, verified by running the gate's own `_carry_items` before and after rather than asserted.

- ⬛ **POST-WRAP ADDENDUM (5b) — HOMED HERE AND NOT ON THE ★ LATEST BANNER, DECLARED RATHER THAN SLIPPED.** The `s241-D2` cap is BLOCKING at 10 substantive lines / 1,200 tape and this banner closed at **{{BANNER}}**, so an eleventh line would be refused, correctly. The cap's own words settle where the detail goes — *"the ⏱ LATEST DELTA is the sole home for gauge / declared-skip / not-done detail"* — and this delta is IN the read chain. ⛔ **The 5b-versus-cap conflict is ruling-shaped, Dave's, and carried — this is the EIGHTH wrap to meet it and the SEVENTH to answer it this way.** **The sha, the push verdict verbatim and the CI read are in the `COMMIT STATE #288` block and in this wrap's filed report `notes/_subreports/2026-09-19-288-W2-wrap.md`** — ⛔ **a commit cannot name itself.**
