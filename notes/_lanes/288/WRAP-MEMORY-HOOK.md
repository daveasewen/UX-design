# #288 — the cloud-memory hook, WRITTEN FIRST AND PLACED SECOND

provenance: 288 · 2026-09-19 · status: observed

*Ritual step 3, in the `#278` form: the wrap sub writes this file FIRST — the index line plus the
topic-file body, in the `s271-D4` shape — and only then attempts the store. **What actually
happened at the store is recorded at the FOOT of this file, not assumed**, which is the whole
reason the file is written before the write is tried.*

⚠ **Every "Open, Dave's" item below is a QUESTION PUT, never a state of the world** (`s271-D4`,
enacting dream-12 P4(a)). The hook is written before his last answers land, so an item recorded as
a state of the world is false the moment he speaks; recorded as a question, it cannot go stale.

---

## 1. THE INDEX LINE — prepended newest-first, above the #287 line

> - [★★ #288 WRAPPED · APOLLO COMPOSES, NOT TRACES — AND HE NAMES THE DEFECT CLASS · NO RULING INSCRIBED, STORE STAYS 622 · THE `s219-D3` ARM IS BUILT AND STILL DOES NOT SETTLE MONO'S 0 · THE STRAND MAP AND THE PATH TO FRIDAY THE 25TH · TEN QUESTIONS PUT, NONE ANSWERED](wrap-288-apollo-composes-and-the-sloppiness-class-is-named.md)

## 2. THE TOPIC FILE — `wrap-288-apollo-composes-and-the-sloppiness-class-is-named.md`

Body as placed (frontmatter `name` / `description` / `sources`, then the sections below).

### What #288 was

A session built to execute three orders that ended up answering a question nobody had asked. Wave 1
landed four Opus lanes — the strand map, the four-theme gutter sheet, the `s219-D3` generation arm,
the template quality review. Then Dave looked at the showroom template and challenged the premise of
the whole one-shot path. A fifth lane ran a composition probe with the template fenced off, and he
ruled by eye that it composes.

### His words, verbatim, never paraphrased

- **At the opener, PARKED and not for now:** *"the Swiss design skill is very very successful, the
  we managed to hit the aesthetic pretty bang on. I want to borrow some of it for Apollo as some
  sort of taste guarantee or guidence."* and *"Not for now but don't let me forget this."*
- **On the presentation:** *"so we also have to actually prepare and create the presentation, we
  made a good start but I want to change the structure. please include this is the plan. we might
  have to parallelise some of this to get it nailed quickly. One question on this, can parallel
  agents cross communicate rather than having solo lanes that report to the orchestrator?"*
- **On parallelism:** *"cool, but lets think about parallelising the entire task set too"* · *"go"*
- **Challenging the premise, on seeing the template:** *"there is a definitely a problem here, 2
  actually, the template is a bit wonky and it's basically copied by the AI, what is the point of
  building the KG if the agent just traces an existing file, its very safe but 1. its of average
  quality anyway... its okay but not great 2. what was the point of building the system if it just
  traces from existing examples???"* and *"the results with VS co-pilot are pretty much identical to
  this, this isn't Apollo its a dot-to-dot book"*
- **His verdict on the probe sheet:** *"so better is someways and worse in others, its more
  interesting and complete page , but its more sloppy ."* · *"I'll create a test brief for these
  tests."* · *"we need to do something about the sloppiness and, its all about alignment spacing and
  dimensions, this looks like its working the way I'd have expected, at least it's diverged from the
  tamplete"*

### ⛔ NOTHING WAS INSCRIBED

**`knowledge/_rulings.json` stays at 622** — verified at the wrap seat by `json.load`, no `s288-`
id. **He did not say *"inscribe"* today.** Everything ruling-shaped below is therefore a question
put, and none of it is a ruling.

### Open, Dave's — every one a QUESTION PUT at the wrap call, not a state of the world

1. **What the one-shot generates FROM** — put at the wrap call. His *"dot-to-dot book"* sentence
   names the problem; the mechanism is that the #230 *"zero invented markup"* pass condition
   REWARDS tracing. Whether that condition is re-cut, scoped or replaced is his.
2. **The sloppiness class — alignment, spacing, dimensions** — put at the wrap call. He named the
   class; what is done about it is not settled.
3. **The test brief for these probes** — put at the wrap call and **OWED BY DAVE HIMSELF**:
   *"I'll create a test brief for these tests."* No lane may write it.
4. **The outer gutter** — put at the wrap call. Token `layout/bento/gutter` reads 0/24/24/0
   (`s217-D2`); the role defaults read 40/24/40/24 and that is what the arm delivers. **Not ruled,
   no winner picked.** The inner gutter has three answers (canon's literal 1px, `subSpacing`
   4/4/4/2, the template's 4), and supercharge's 0 is inherited and was never decided.
5. **Which event Friday 2026-09-25 is** — put at the wrap call. Small internal demo or David Rice.
   **NOT ESTABLISHED**, and the strand map says so on a band of its own rather than guessing.
6. **The deck's new structure** — put at the wrap call and not given. **Wave 2 is blocked on it.**
   And his question in the same turn is unanswered: can parallel agents cross-communicate?
7. **The Swiss design skill as an Apollo taste guarantee** — **PARKED BY HIS OWN WORD** with an
   explicit instruction not to lose it. Cloud copy:
   `swiss-skill-as-apollo-taste-guarantee-288.md`.
8. **Lane T's six-question decision pack** — put at the wrap call and unanswered: an inline-style
   rule (87 raw-value attributes re-measured, against #287's 88, both published), the template's
   status and whether `showroom/index.json`'s fixed list can express *proposed*, the three
   `$awaitingDave`, and a max-width ruling.
9. **The specificity root cause** — put at the wrap call. `.c-bento.wall-ops` at (0,2,0) loses to
   canon's own `:has()` dashboard rule at (0,4,0), so canon's documented instance-dial recipe
   silently loses. Measured at 0px instead of 40. Not fixed, not ruled.
10. **Everything on `_HANDOFF-130`…`-138` not closed today** — put at the wrap call. **Nothing was
    struck**, and the zero is a judgment: the arm being BUILT discharges half of a carry headline
    whose other half stays true, so it is said in a new carry item instead (`s271-D4`).

### What landed, with receipts

- **The `s219-D3` arm:** `knowledge/canon/gen_bento_role_vars.py`, 289 lines, the first consumer of
  `knowledge/_render/_bento_edit_rails.json` (`$groundwork_only`, un-consumed since #219). Gates
  green, four-theme rendered proof. Report: `notes/_subreports/2026-09-19-288-A-s219-d3-arm.md`.
- **The strand map:** `notes/_STRAND-MAP-2026-09-19.html`, 13 sections, 41 cited paths all existing.
  Report: `notes/_subreports/2026-09-19-288-M-strand-map.md`.
- **The four-theme gutter sheet:** `notes/_lanes/288/B/four-themes.html`, read at HEAD through
  `git show`. Report: `notes/_subreports/2026-09-19-288-B-bento-renders.md`.
- **The template quality review:** `notes/_lanes/288/T/template-quality-review.html` — the drawn
  page is sound, the paperwork is not. Report:
  `notes/_subreports/2026-09-19-288-T-template-quality.md`.
- **The composition probe:** `notes/_lanes/288/P/probe-sheet.html` and `composed-dashboard.html`,
  39 reads and 0 fenced. Report: `notes/_subreports/2026-09-19-288-P-composition-probe.md`.
- **Handoff:** `_HANDOFF-139-apollo-composes-and-the-sloppiness-class-is-named.md`.
- **Dossier:** `_DECISION-HISTORY/2026-09-19-288-apollo-composes-and-the-sloppiness-class-is-named.md`.

### The numbers

FILL 197,234 real / 33 turns against 180,585 / 27 declared at the brief cut (delta 16,649). BOOT
74,174, agreeing to the token — the thirteenth post-diet reading over the 70,000 ceiling, and the
third on the Browser-blocked setup (73,832 · 74,120 · 74,174). subs 1,004,095 (n=5) by the log's
convention and 998,602 by `_checkin.read_fill` — two definitions, both published. Six inherited
blocking gate fails, carried in the `#243` declared not-a-wrap form, the fourteenth consecutive wrap.

### ⛔ The archive, declared rather than discovered

`MEMORY-ARCHIVE.md` stands at **48,990 of its 49,152 B cap — 162 B of headroom** — and every line
that could roll out of `index.md` runs to thousands of bytes. **No roll is possible and NOTHING IS
TRUNCATED, because a truncated verbatim move is not a move.** The index therefore carries one more
wrap line than the cap's design intends. **Nothing is at risk: the repo is the record.** What to do
about the archive is Dave's, and the dream pass is its seat — its THIRD consecutive wrap as a carry.

---

## 3. WHAT ACTUALLY HAPPENED AT THE STORE — recorded AFTER the attempt, never predicted

✅ **PLACED FROM THIS SEAT — the FIFTH consecutive wrap written by the wrap sub rather than deferred
to the conductor at the next opener.** Four writes, all accepted, none refused:

| # | call | path | result |
|---|---|---|---|
| 1 | `memory_str_replace` | `index.md` | the #288 line **PREPENDED newest-first** above the #287 line — 54 B → 5,416 B, file 22,230 → 27,592 B, version `188d350d8d83` → `2939eaff0e08` |
| 2 | `memory_str_replace` | `index.md` | frontmatter `description` amended: SUSPENDED at #286/#287 → **#286, #287 and again at #288, which carries SIX lines** — version `2939eaff0e08` → `54e999c9de0b` |
| 3 | `memory_str_replace` | `index.md` | the closing note given a **#288 suspension paragraph** above #287's, which is kept verbatim and only re-tensed (*"was in its second"*) — version `54e999c9de0b` → `fc2ca99a73fd`, file 28,691 B |
| 4 | `memory_write` | `wrap-288-apollo-composes-and-the-sloppiness-class-is-named.md` | **NEW, 9,751 B**, version `71ba4fb4b959` |

⛔ **THREE TARGETED `str_replace` EDITS, NEVER A WHOLE-FILE REWRITE OF `index.md`** — the same
discipline #287 used, and for the same reason: five existing wrap lines cannot drift if they are
never re-typed.

⛔ **THE INDEX CUT COULD NOT BE MADE AND WAS NOT FAKED — THIRD CONSECUTIVE WRAP.**
`MEMORY-ARCHIVE.md` reads **48,990 B against its 49,152 B cap — 162 B of headroom, unchanged across
three sessions** — and either line that should have rolled (#284, #283) runs to thousands of bytes.
**Nothing was truncated, because a truncated verbatim move is not a move**, so `index.md`
deliberately carries **SIX** wrap lines and says so in both its frontmatter and its closing note.
⚠ **How that was established is stated plainly rather than dressed up as a refusal that was never
received: it is arithmetic over two measured quantities — the store listing's byte count, read at
this seat, and the documented cap — not a rejected write.** No append was attempted, because the
only way to obtain the archive's version token is to read all 48,990 B into this seat, and a failure
whose arithmetic is certain is not worth that price. ★ **Nothing is at risk — the repo is the
record.** **What to do about the archive is Dave's, and the dream pass is its usual seat.**

⚠ **Two #288 memory files were written EARLIER IN THE SESSION BY THE CONDUCTOR, not by this seat,
and are named here so the record is complete rather than implied:**
`areas/presentation-friday-25th-288.md` and `swiss-skill-as-apollo-taste-guarantee-288.md` (the
latter is the cloud copy of the parked Swiss-skill idea, per his *"don't let me forget this"*).
