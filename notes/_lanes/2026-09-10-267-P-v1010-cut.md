# #267 lane P — CUT v1.0.10 (PROPOSED): the packaging hole closed

**Model: Opus. Conductor: Fable (#267). Nothing here is ratified, released, or pushed.**

## DONE

1. **Read the release machinery, not the hook.** `apollo-spider/build-designer-pack.sh` header +
   the `dryrun|release` branch (#228: version DERIVED from the manifest, never typed) ·
   `knowledge/_release/_gen_pack_manifest.py` (`HELPER_HOMES:194`, `local_imports`, the probe,
   `RATIFY_IDS:724`, `ratification_status`) · `_gate_frozen_release.py:134` (the frozen literal,
   still `v1.0.9` — it moves at `--seed`, after the bake) · the #262 commit sequence as the runbook
   (`1e564c0` version story → `0ce944d`/`daf1358` manifest → `33a6608` inscribe → `3cab7c3` ratify
   → `b94adff` release → `c0538c1` re-seed).
2. **Mechanism fixed at cause.** `local_imports()` read only `ast.Import`/`ast.ImportFrom` NODES;
   `_validate_receipt.py:635` reaches its mint helper as `__import__("gen_provenance_receipt")`.
   New `string_call_imports()` (`_gen_pack_manifest.py:870`), folded into the local side only
   (`:913`). NOT `HELPER_HOMES` — that table is a typed seed and would go stale at the next lazy
   helper; the docstring says so. 5 new bites, mutation-tested both ways: **226 bites, 0 fail(s)**.
   `bash apollo-spider/build-designer-pack.sh --selftest` exit 0.
   Commit **`fee25720583cf24e48ee5c47d5fa95a533220839`**.
3. **`gen_component_partials.py` is MISSING BUT NOT NEEDED** — proven, not assumed. Every mention
   in a shipped file is prose (`_validate_behaviour.py:5,30,340`; `_check_legend_migration.py:10`;
   `gen_token_ramp.py:6,105`), no import, no invocation, no shipped skill names it. It stays out.
4. **Version story** `v1.0.9 → v1.0.10`: `VERSION` + `MEMENTO_CUT_VERSION` + the four literals
   `--check` reads off the stage (FIRST-SESSION.md, gumdrop/_state.json, 2 gumdrop runbooks).
   `RATIFY_IDS` untouched ⇒ status derives PROPOSED. Commit **`625be6470f4a5249587de095c32443eadab711f5`** — this is the CUT commit.
5. **Cut** `--manifest --commit 625be64 --full-stage /var/tmp/full10` (differential arm ARMED, the
   same shape #262 used). 1706 files, 45810170 bytes, manifest sha256 `0106040dd9ff9658`.
   Twice-baked into `/var/tmp/dry10a` and `/var/tmp/dry10b`:
   sha256 `47b4191fb00f1e04e560905ef55f055dfecf656291cc075f88402cb7cd65b1a2`, `cmp` **rc 0**.
   `gen_provenance_receipt.py` (20018 bytes) is in the zip.
6. **Consumer gate proof**, from the unzipped dry-run stage on a COPY of
   `outputs/coldrun-267/arm-A-blind/dashboard.html` (the frozen arm untouched):
   v1.0.9 mint **rc 2** (no such file), `_validate_receipt.py` **rc 1** FAIL:NO-RECEIPT naming the
   absent mint. v1.0.10 mint **rc 0**, `_validate_receipt.py` **rc 0 PASS**.
   `_validate_screen.py` **rc 1** — icon-source UNKNOWN on the dashboard's own inline arc path, a
   content red; its receipt arm is green.
7. **Ten release gates: 8 PASS.** Gate 4 (`_gate_release_audit.py --selftest`, rc 1) and gate 5
   (`--pack`, rc 1) are the SAME pre-bake fact — no zip in dist/ carries v1.0.10 yet
   (`_gate_release_audit.py:253`, and the bite at `:266` asserts that rc). Re-run both after the bake.
8. **Cut recorded** at **`fbd9c141b6fb672df5322bbe03732b137275436b`** (manifest + probe +
   RELEASE-SPIDER page pair, the #262 O2 commit shape). `RATIFY_IDS`, the frozen literal,
   `_frozen-releases.json` and `dist/` all untouched.
9. **Go/no-go page**: `reviews/GO-NOGO-v1.0.10-2026-09-10-v1.html`.

## ⚠ RED — REPORTED, NOT ROUTED AROUND

`knowledge/_validate_dataviz.py` **leaves the pack** at this cut (1 removed). v1.0.9 shipped it.

Cause, measured: at `dd129e8` its probe verdict was RUNNABLE only because it was red on STALE
receipts *in the pack AND in the full tree* ("full-tree FAIL too"). #263 (`521a751`, `5050b33`)
re-drove the receipts; that red expired and the next layer surfaced — the shipped
`_tests/chart-engine/_receipts.json` names **13** chart pages, `GATE_DATA_CANDIDATES`
(`_gen_pack_manifest.py:266-272`) ships **six**. Inside the pack the gate now dies on
`_tests/chart-engine/boxplot.html`; the ARMED differential proves it passes on the full tree ⇒
REPO-BOUND ⇒ out of the ship set.

NOT fixed here by choice: the remedy (ship the seven missing pages, or teach the gate to grade only
what it can see) is a decision, not a typo, and this cut's scope is one commit. Same
stale-typed-list class the import fix just removed, one list along.

## NOT DONE (declared)

- No `--release`, no push, no ratification, no `_rulings.json` edit, no `_build_all.py`, no
  `gen_kg_edges.py`. 3 commits local and unpushed.
- The dataviz REPO-BOUND red is unfixed (above).
- Gates 4 and 5 not green — they cannot be, pre-bake.
- `reviews/RELEASE-SPIDER-2026-08-26-v1.html` was regenerated by `--manifest`/`--dry-run` (the
  script does this itself) and the review overlay re-injected with
  `knowledge/_review/_make_review.py`. It now describes v1.0.10.

## ⚠ SANDBOX

`.git/index.lock` was present, 0 bytes, stamped 14:15 — ~2.5h stale, no lane holding it. It was
NOT deleted: moved to `knowledge/_tmp/_orphan-locks/` (the #261/#265 precedent). Git could not
unlink its own new locks until `allow_cowork_file_delete` was granted for the folder; after that,
commits ran normally and serially.

## THE COMMANDS THE CONDUCTOR RUNS AFTER DAVE'S WORD

Each with its #262 precedent commit. Serial, in this order.

1. **Inscribe the ruling** — `knowledge/_rulings.json`, a new sNNN-D row naming v1.0.10 RATIFIED.
   Precedent `33a6608` ("s262-D6 inscribed (422→423)").
2. **Key `RATIFY_IDS`** in `knowledge/_release/_gen_pack_manifest.py` to that ruling id, re-run
   `--manifest --commit 625be64 --full-stage <stage>` so the manifest's status derives RATIFIED,
   and move `_gate_frozen_release.py:134`'s apollo-spider literal `v1.0.9 → v1.0.10`.
   Precedent `3cab7c3` (RATIFY_IDS + manifest + probe + frozen literal + the review pair).
3. **Bake** — `bash apollo-spider/build-designer-pack.sh --release --commit 625be64` (refuses on a
   dirty tree, on stale chart receipts, and without ratification). Precedent `b94adff`.
4. **Verify** — `bash apollo-spider/build-designer-pack.sh --check apollo-spider/dist/Apollo-Spider-v1.0.10.zip --commit 625be64`,
   and re-run gates 4 and 5 (they go green once the zip exists). Precedent: the post-bake 9/9 pass
   recorded in `notes/_subreports/2026-09-09-262-W-release-and-wrap.md:103-115`.
5. **Re-seed the frozen ledger** — `knowledge/_release/_frozen-releases.json`, apollo-spider row to
   10 zips. Precedent `c0538c1` ("frozen ledger re-seeded at v1.0.9 — apollo-spider 9 zips").

## TOKENS

Context use at the step 4/5 seam and at wrap: ESTIMATED, not measured — a lane has no
`message.usage` reader of its own and `_checkin.py` is the conductor's instrument. Best estimate
~120K FILL at the seam, ~135K at wrap, on ~30 tool calls dominated by three ~90s probe/bake runs
whose output was tailed, never read whole. Flagged as an estimate deliberately
[[measuring-tool-must-not-guess]] — if the conductor needs a number, it must come from the gauge.

## PATHS

- `knowledge/_release/_gen_pack_manifest.py` (`:870` `string_call_imports`, `:913` the fold,
  `:113` VERSION, `:266-272` GATE_DATA_CANDIDATES)
- `knowledge/_release/_pack_manifest.json` · `knowledge/_release/_pack_gate_probe.json`
- `reviews/GO-NOGO-v1.0.10-2026-09-10-v1.html`
- `reviews/RELEASE-SPIDER-2026-08-26-v1.html` (+ `.REVIEW.html`)
- `apollo-spider/FIRST-SESSION.md` · `apollo-spider/gumdrop/_state.json` ·
  `apollo-spider/gumdrop/runbooks/_RUNBOOK-{capture-ritual,context-gauge}.md`
- dry-run stages `/var/tmp/dry10a`, `/var/tmp/dry10b`; full stage `/var/tmp/full10` (throwaway)
