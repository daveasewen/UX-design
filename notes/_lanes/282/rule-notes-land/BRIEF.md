# LANE RN — BRIEF — LAND THE RULE NOTES: inscribe s282-D1 on his export, make 11 guideline edits in his words, land 2 edges, regen the rules index, prove the graph. Do NOT touch col26-012.
#282 · 2026-09-17 · conductor (Fable 5.1) · **model: opus** · bash root `/sessions/tender-hopeful-allen/mnt/UX-design/`

## Dave's word, verbatim
Export 19:10:24Z — `notes/_lanes/282/rule-notes/DAVE-EXPORT-2026-09-17.json`: **14 of 15 on the recommendation; `col26-012` overruled to (c) "Discuss first"; one note on `col26-016`: *"the same is true for green for possitive BTW"*.** The option sentences he picked are in `notes/_lanes/282/rule-notes/gen_rule_notes.py` (the `opts` tuples; `rec` = his pick everywhere except col26-012). His original notes, verbatim, and the rule text as stored: `notes/_lanes/281/rests-on-land/RULE-NOTES-2026-09-17.md`.

## Read first (retrieval, not a reading list)
`s281-D6` and `s281-D3` in `knowledge/_rulings.json` (shape; "a rule NOTE is not a rule EDIT" — this lane is the ruling that turns 11 notes INTO edits, on his export) · `knowledge/gen_kg_rules.py` (⛔ it would DELETE the 73 hand-authored `restsOn` lines in `_rule_nodes.json` — named by lane RL at #281, unfixed; see Do 5) · `knowledge/guidelines/_rules-index.json` (regenerated by `gen_kg_rules.py` line ~392 — find out whether the index can be regenerated WITHOUT the edge wipe; if not, preserve-block first) · `knowledge/guidelines/data-visualisation.md` `{#dv-017}` — it ALREADY carries a scoped override to col26-012 and col26-016 (delta indicators may carry red/green semantic colour as data semantics, ruled 2026-07-16/07-19): rows 14 and 4 must CROSS-REFERENCE it, never restate it.

## Do
1. **Inscribe `s282-D1`** by textual span (`N  0` numstat), at the END of `_rulings.json`, re-read immediately before commit. id, date 2026-09-17, by Dave, `says` = export ref + the 14 picks + his col26-016 note verbatim + col26-012 = (c) verbatim. `ruled` = ELEVEN of his rule notes become GUIDELINE EDITS in his own words (list the eleven ruleIds and one clause each); TWO become edges (mot-005 second edge; type26-003 second edge); `type26-002` stands as landed; **`col26-012` is NOT edited — "Discuss first" — and stays an open ASK**; the col26-016 exception is SYMMETRIC (RAG red for downward, RAG green for upward movement, stat-card style) on his note; every edit cites this ruling in its own line. `governs` = the eleven guideline files + `_rule_nodes.json` + `_rules-index.json`. `status` ruled.
2. **Eleven guideline edits**, each as a clause ADDED to the existing rule line (or a sibling line directly beneath it), never a rewrite of the stored sentence, each ending `(s282-D1, Dave 2026-09-17)`:
   - `aca-003` (c): the title stays as is; ADD a heuristic line beneath: the first heading need not repeat the page title; an active navigation state or a salutation may lead — the title, not the H1, is what orientation rests on.
   - `aid-009` (a): SPLIT — the obligation line keeps 44 default / 24 floor and states it is measured on the HIT AREA; ADD a graded-heuristic line: the visible target, the clear space around it (spacing as emphasis) and compact views (a user choice) are context the designer weighs.
   - `col26-009` (a): add the second reason — brand consistency ("if designers were allowed to use the supporting palette we would get dramatically varied designs").
   - `dv-bar-007` (a): scope to positive-scale bar charts; negatives use the vertical below-zero form.
   - `icon-006` (a): floor = legibility; ceiling = the icon/illustration boundary; consistency.
   - `logo26-001` (a): in a native app the journey's logon or splash screen satisfies the rule, in his words ("the user summoned an HSBC app, they know where the destination is").
   - `photo26-002` (a): a REVIEW DATE on the gen-AI clause — `review by 2027-03` — no edit to the ban.
   - `pict-001` (a): the definition — a pictogram supports a concept; an icon signals an action.
   - `pict-010` (a): the reason — consistency and enforcement against misuse, not a perceptual threshold.
   - `col26-016` (a) + his note: exception — RAG red text for downward and RAG green text for upward position movement, stat-card style; CROSS-REFERENCE `{#dv-017}`'s delta clause rather than restating it.
   - `neuro-026` (a): the carve-out BY REFERENCE — unpaired icons only for the universal set named in `icons.md`; pictograms stay no-exception (pict-001). If `icons.md` has no actual LIST of the universal set, say so in the report as UNPROVEN and cite the carve-out line that exists.
3. **Two edges** in `_rule_nodes.json`, hand-authored beside the 73, `type: restsOn`, with `$why`, `grade`, `daveNote` verbatim: `rule:mot-005 → ux:pr-wcag-operable` ($why names SC 2.2.2 Pause, Stop, Hide; the register has no node for the SC itself — declare that gap in the report) and `rule:type26-003 → ux:pr-tog-readability` (his "linked to 2 as you stated").
4. **Regenerate `_rules-index.json`** so the eleven edited rules carry their new text. Prove: a `json.load` diff shows exactly those rule entries changed and no rule id lost or added.
5. **The footgun.** If regenerating the index means running `gen_kg_rules.py`, FIRST add a preserve block so hand-authored `restsOn` edges survive a run (read them from the existing file, write them back). Prove with a count: `restsOn` edges before = 73, after = 75. If you do not need to run it, do not fix it — report it still open.
6. **Verify**: `python3 knowledge/_validate_kg.py` (or the explorer build's own check) green on `_rule_nodes.json`; `python3 knowledge/_validate_lane_ownership.py` unchanged; the rule text visible in the explorer for one edited rule (a `grep` on the built HTML is enough).

## Do NOT
Touch `col26-012`, the colour-standards chart-red line, or any rule not named above. Do not add a principle node. Never `git stash` / `gen_kg_edges.py` / `_build_all.py`. Do not edit `notes/_RULINGS.html` by hand — regenerate it the way #281's wrap did (step 4d, `s263-D10`) if a generator exists; else report stale. `/sessions` is 100% FULL: scratch under `/tmp` only; `pip`/playwright need `PYTHONUSERBASE=/tmp/pyu PLAYWRIGHT_BROWSERS_PATH=/tmp/pwb TMPDIR=/tmp` (Chrome headless shell already at `/tmp/pwb`). Locks → `mv` to `.git/_orphan-locks/` (unlink is refused under the mount).

## Report — `notes/_subreports/2026-09-17-282-RN-rule-notes-land.md`, evidence under `notes/_lanes/282/rule-notes-land/`. Plain prose first: which eleven lines changed and the one clause each; the two edges; what you could not prove. COUNTS · ruling-shaped (anything you had to decide that was his) · UNPROVEN · REPLAY-THESE.
Commit via `SESSION_N=282 SHOWROOM_ACK="inherited standing gap: showroom 138 vs inscribed 108, Dave's since #279" bash knowledge/_git_commit.sh --reconciled <fresh msgfile> <paths…> < /dev/null`, msgfile `/tmp/_msg-282-RN-$(date +%s).txt`, subject `#282 lane RN: rule notes landed — s282-D1 inscribed, 11 guideline edits in his words, 2 edges, index regenerated`. Dirty paths you did not make (`notes/_REHEARSAL-LOG.jsonl`, `notes/_dream/_GRADE-DECISIONS.jsonl`, `knowledge/_graph-mark-observations.jsonl`) are the gates' own appends — name them in the msgfile, do not stage them. Do not push. Return ONLY a stub: verdict, span numstat, edits count, edges before/after, sha, report path, spend.
