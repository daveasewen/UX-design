# LANE LR — BRIEF — THE LOGO REVIEW (P-277-4): measure the 12 lockups, render them so Dave can rule by eye, put the questions on one page for his export. PROPOSE ONLY. Edit nothing under `knowledge/`.
#282 · 2026-09-17 · conductor (Fable 5.1) · **model: opus** · bash root `/sessions/tender-hopeful-allen/mnt/UX-design/`

## Dave's word, verbatim
#277 icons export RI-4 note: *"A for now, but we need a separate review for this too"* — the logos entered the graph (12 nodes, `s277-D4`), 10 bound by no rule, and the review was parked as `P-277-4`. #281: *"we need to do the logo review soon"*. #282: *"go"*. Today's `s282-D1` added the native-app clause to `logo26-001` ("the user summoned an HSBC app, they know where the destination is … the logo will usually appear on a logon or splash screen").

## Read first (retrieval)
`knowledge/assets/logos/*.svg` (12) + `_export-logos.py` · `knowledge/_logo_nodes.json` (12 nodes, 31 edges, 19 distinct `usesLogo` component→logo pairs; `governedBy` declared-null) · `knowledge/guidelines/logos.md` (the 2025 sticker sheet — "no rules, standard lives on create.hsbc") · `knowledge/guidelines/brand-refresh-assets.md` (the 2026 refresh: `logo26-*`, `va25-015` masthead contract, `va25-016` never-rules, `va25-017` variant-on-photography; ⚠ there IS a logo rule set — the #277 page's "0 rules in logos.md" was true of the file, not of the corpus) · `notes/_lanes/277/icons-propose/REVIEW-icons-2026-09-16-v2.html` for the page idiom · `notes/_lanes/282/rule-notes/RULE-NOTES-2026-09-17.html` for the export envelope shape.

## Do
1. **Measure the artwork.** For each of the 12 SVGs: sha256, byte size, viewBox, fill colours present, whether the wordmark/identifier paths exist. ⚠ Two smells the conductor saw in byte sizes and has NOT verified: `hexagon-dark-colour.svg` and `hexagon-light-colour.svg` are both 619 bytes; `masterbrand-identifier-dark-colour.svg` (2,584) equals `masterbrand-dark-colour.svg` (2,584) — if the identifier variants are the masterbrand re-exported without the identifier, or dark = light, that is an EXPORTER DEFECT of the #277 icon class and you name it, file by file. Write `notes/_lanes/282/logo-review/artwork.json`.
2. **Measure the bindings.** From `_logo_nodes.json` and a grep over `knowledge/components/**` and `knowledge/snippets/**` for `assets/logos/`: which components use which lockup, on which ground (light/dark), and the 8 lockups nothing references. From `_rules-index.json`: every rule whose text names a logo, lockup, hexagon, masthead or wordmark (expect ≥ 6 in `brand-refresh-assets.md` + `logos.md` 0). Write `bindings.json`.
3. **The page** — `notes/_lanes/282/logo-review/LOGO-REVIEW-2026-09-17.html`, swiss-design-system idiom (`/sessions/tender-hopeful-allen/mnt/.claude/skills/swiss-design-system/SKILL.md`), single file, SVGs INLINED (no external refs, no browser storage). Header: what this is in three sentences and the counts (12 lockups · N bound · N unbound · N rules that name a logo · N exporter defects). **Section A — the sheet, for his eye:** the 12 lockups rendered on their intended ground (light lockups on white, dark on `#000`), each at 3 sizes (a masthead size ~120px wide, a compact ~64px, and the minimum you can find in `va25-*`/`logo26-*` — if no minimum is ruled, say "no minimum ruled" and render at 32px), colour and mono side by side; under each the sha, and the defect flag if any. **Section B — where they ship:** a component → lockup table from bindings.json. **Section C — the questions**, radios + note per question, recommendation tagged, For/Against on every option, plain prose, ids in a Technical fold:
   - Q1 per defective file (one radio group per defect): re-export from Figma / accept as-is / drop the variant.
   - Q2 which lockup is the masthead default per theme (`va25-015` says Masterbrand in ALL mastheads; the four themes are in `knowledge/tokens` / the theme docs — name them): colour on light / mono on dark / colour on dark — one row per theme.
   - Q3 clear space and minimum size: `logos.md` says the standard is on create.hsbc and there are no tokens. Propose a rule in the `logo26-*` shape with a number ONLY if a source in `guidelines/_sources/brand-refresh-assets/` carries one — quote it; else the option is "author from create.hsbc — needs Dave's authenticated fetch" and the number is his.
   - Q4 the hexagon-only rule (`va25-016`: needs Create Direct approval AND 'HSBC' context nearby): which components, if any, may show the hexagon alone — propose from the bindings (app tiles? splash?).
   - Q5 do `logos.md` (2025 sticker sheet) and the `brand-refresh-assets.md` logo section merge into one guideline file, or does `logos.md` become a pointer? Recommend.
   - Q6 the 8 unreferenced lockups: keep as library (declared null stands) / retire the identifier variants until a real identifier exists / other.
   Export envelope `{exportedAt, page:"LOGO-REVIEW-2026-09-17", follows:"REVIEW-icons-2026-09-16-v2", answers:{<qid>:{question, choice, note, recommended, overruled}}}` with "Take all the recommendations", download, copy.
4. **Verify** at 1280 and 390: 12 lockups × 3 sizes × (colour+mono) rendered, radio count, export round-trip, console errors `[]`. Chrome headless shell is at `/tmp/pwb` — `export PYTHONUSERBASE=/tmp/pyu PLAYWRIGHT_BROWSERS_PATH=/tmp/pwb LD_LIBRARY_PATH=$HOME/.local/lib TMPDIR=/tmp`; `python3 -c "from playwright.sync_api import sync_playwright"` proves it. Screenshots beside the page as `verify-1280.png`, `verify-390.png`.

## Do NOT
Edit anything under `knowledge/` — no SVG, no guideline, no node file, no ruling. Do not re-export from Figma. Do not inscribe. `/sessions` is 100% FULL: scratch under `/tmp` only. Never `git stash` / `gen_kg_edges.py` / `_build_all.py`. Every git call under this mount leaves a 0-byte `index.lock` / `HEAD.lock` / `refs/heads/master.lock` that cannot be unlinked — `mv` them to `.git/_orphan-locks/<name>.<epoch>` before each git call.

## Report — `notes/_subreports/2026-09-17-282-LR-logo-review.md`, evidence under `notes/_lanes/282/logo-review/`. Plain prose first: the defects found (file by file, measured), the bindings, the six questions and your recommendations in one line each. COUNTS · ruling-shaped · UNPROVEN · REPLAY-THESE. `_state.json` row via `_state.add()` — id `W-282lr`, owner dave, close = Dave's export of the page received.
Commit via `SESSION_N=282 SHOWROOM_ACK="inherited standing gap: showroom 138 vs inscribed 108, Dave's since #279" bash knowledge/_git_commit.sh --reconciled <fresh msgfile> <paths…> < /dev/null`, msgfile `/tmp/_msg-282-LR-$(date +%s).txt`, subject `#282 lane LR: the logo review — 12 lockups measured, N exporter defects named, 6 questions for his export`. Name the gates' own dirty appends (`notes/_REHEARSAL-LOG.jsonl`, `notes/_dream/_GRADE-DECISIONS.jsonl`, `knowledge/_graph-mark-observations.jsonl`) in the msgfile, do not stage them. Do not push. Return ONLY a stub: verdict, defects count with file names, bindings counts, radio count, sha, page path, report path, spend.
