# #282 — LANE W — the wrap's own working record

provenance: 282 · 2026-09-18
status: observed

**The filed report (`s218-D7`) is `notes/_subreports/2026-09-18-282-W-wrap.md` and is the authority.**
This file holds the two things that belong beside the working set rather than inside the filed
report: the **2c/2d/2f EXIT CHECK** and the **`s271-D4` re-read**, which is the ritual's final beat.

## EXIT CHECK — run BEFORE the three rolls, recorded rather than asserted

Every ⚠ / ⬛ / AWAITING / OPEN-CALL / DEFERRED-TO-DAVE item inside the three rolling regions was
scanned and matched to a **standing** home before the move. Dated homes do not count.

| rolling region | Dave-owed items found | standing home confirmed |
|---|---|---|
| GM ★ PRIOR banner **#280** (12 lines → `_GM-ARCHIVE.md` § Batch 2026-09-18 #282) | ⬛ ④ the orphan census export · the eye-check · `jade-lifestyle` — and ⬛ ⑥ the `#243` form · resolver `FAIL(6)` · lane-ownership 2/3 · showroom *"108 stale"* | `_CARRIES.md` § `residual → #283` holds every one, by probe: `#243` ×49, `showroom` ×10, `FAIL(6)` ×3, `lane_ownership` ×5, `jade-lifestyle` ×4. The census export travels **STRUCK with its #281 receipt** and the eye-check travels **STRUCK with its #282 receipt** — neither deleted. Resolver, ownership and showroom are ALSO re-declared on this wrap's own ★ LATEST banner and ⏱ DELTA |
| LS ⏱ PRIOR DELTA **#279** (20 lines → `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-18 #282) | ⬛ his **15-base export, "still unread by the record"** · ⬛ **four ruling-shaped questions the lanes generated** — the `designrulings` sub-chip, a 13th verb + `verbVia`, Spider `v1.0.14`, `va25-013` · ⚠ #279's own declared-skips block | the 15-base export is **STRUCK in `_CARRIES.md` with its #280 receipt** (the record read it); the four questions stand in the carry set at age `[3]` — `designrulings` ×1, `v1.0.14` ×2, `verbVia` ×2, `va25-013` ×4. The declared-skips block is a wrap's own bookkeeping, not a Dave-owed item, and this wrap writes its own |
| GM stratum **#281** (`roll_2f`: 28 ln → `notes/_GAUGE-LOG.md` at true EOF, 3 ln → `_GM-ARCHIVE.md`) | ⬛ the declared-skips block · ⬛ `COMMIT STATE #281`'s `#243` not-a-wrap form and its declared hash gap | the `#243` form stands in the carry set (49 occurrences) and is **re-declared on this wrap's own `COMMIT STATE #282` block**, now its **EIGHTH** wrap; the hash gap is re-declared in the same block, verbatim in shape |

⛔ **Nothing was orphaned by these moves, and nothing was dropped for being old.** Seven items that
had no line of their own in `_CARRIES.md` — the logo masters, the third dial, the theory door and the
three postures, the two further generator footguns, `col26-012`, the hard-wall question at its third
breach, and the instrumentation-append policy — were MINTED at 2c(i) rather than left in a dated
home. That is the EXIT CHECK doing the job it exists for.

⚠ **AND ONE THING THE EXIT CHECK CANNOT DO, NAMED HERE BECAUSE #282 IS THE SESSION THAT PROVED IT:**
the check tests **PRESENCE** — is this item homed in a standing section? — and has **no test for whose
item it is**. The six-cell eye-check passed this check three times while being an item Dave had
already closed and never re-opened. **A carry that only the conductor wants is not Dave's open item**,
and nothing in 2c can see that.

## ⬛ `s271-D4` RE-READ — the ritual's FINAL beat, run at 5b and RECORDED rather than assumed

The memory hook's "open, Dave's" items (`notes/_lanes/282/WRAP-MEMORY-HOOK.md`) were re-read against
`knowledge/_rulings.json` **after every ruling of the session was inscribed**. #282 inscribed exactly
six — **`s282-D1`…`s282-D6`** — all before this ritual opened.

**TWO ARE STRUCK, WITH THEIR RULING IDS NAMED, AND FOUR NEAR MISSES ARE NAMED RATHER THAN STRUCK:**

1. ✅ **The 15 rule notes — CLOSED by `s282-D1`** (and, for the `col26-012` ASK half, `s282-D2`).
   Struck by addition in `_CARRIES.md` and `knowledge/_REVIEW-SIGNOFF.md`, never deleted.
   ⚠ **Struck on the ELEVEN that landed, NOT on the fifteen:** `col26-012` came back *"Discuss
   first"* and is carried forward as a NEW item in its own right.
2. ✅ **The eye-check of the six matrix cells — CLOSED at `6bac5e4`** on Dave's own word. ⚠ It is
   struck on the STRENGTH OF HIS SENTENCE, not of a ruling id, and the strike says so: what closed it
   was the discovery that `s280-D1` had already settled it.
3. **The 40 per-size logo masters** — ⚠ **NEAR MISS.** `s282-D3`, `s282-D4`, `s282-D5` and `s282-D6`
   are all *about* logos, and `s282-D4` supplies the eight surviving lockups the masters are drawn
   from. **None of them draws a single SVG.** A same-subject ruling is not a same-question ruling.
4. **The theory door and the three postures** — ⚠ **NEAR MISS, AND THE STRONGEST-LOOKING ONE.** It was
   discussed at length and he said *"this all sounds great, I really like this idea"*. **There is no
   `s282` ruling on it at all**, and a strike here would be the exact inversion of `s271-D4`: an idea
   recorded as a decision because it was liked.
5. **The third dial** — ⚠ **NEAR MISS.** His *"no dial 2 isn't right. I'll take a look"* closes the
   OFFER of dial 2; it does not name dial 3, and no ruling touches either.
6. **`col26-012`** — ⚠ **NEAR MISS, AND A TRAP.** `s282-D2` names `col26-012` and closes an ASK about
   it from #281. The *"Discuss first"* on the rule note is a **different question about the same id**,
   and striking it on the id match would lose it.
7. **The git-lock runbook line** (now with its 4c collision), **`s277-D12`**, **`jade-lifestyle`**, the
   **shells-3D cutaway**, the **sandbox snippet scripts**, **`W-281fo`**, **force by grade**, the
   **ghost colour**, the **three ASKs**, **`_HANDOFF-130`'s four** and the **wrap-sub memory seat
   limit** — untouched by any `s282` ruling; all carried at their ages.

⚠ **Every open item above is written as the QUESTION IT IS, not as a state of the world**, and
⛔ **a same-subject ruling is a strong match, not a proof — a strike that is wrong is worse than an
item that is merely stale** (`s271-D4`'s own warning, and the reason four near misses are named here
instead of four items being struck).
