# LANE W — WRAP BRIEF — #282 "the rule notes and the eye-check" → the rule notes land, the logo review is given, the graph's philosophy is put
#282 · opened 2026-09-17, wrapped 2026-09-18 (DATE SPLIT — the #241 shape by ADDITION; stamp the ritual's date, re-date nothing) · conductor Fable 5.1 · **model: opus** (delegated wrap) · bash root `/sessions/tender-hopeful-allen/mnt/UX-design/`

## Run the ritual: `knowledge/_RUNBOOK-capture-ritual.md`, every step, in order. Lane W at #281 (`notes/_lanes/281/W/`, `notes/_subreports/2026-09-17-281-W-wrap.md`) is the worked example — same shape, same commit path (`SESSION_N=282 SHOWROOM_ACK="inherited standing gap: showroom 138 vs inscribed 108, Dave's since #279" bash knowledge/_git_commit.sh --reconciled <msgfile> <paths…> < /dev/null`), the `#243` DECLARED not-a-wrap path if the six inherited gate fails still stand (they do at the conductor's last check-in; a wrap may not repair them — EIGHTH wrap running, say so). Handoff = `_HANDOFF-133-<slug>.md` at the repo root, newer than `_CHAIN.md` and outranking it; it does NOT replace `_HANDOFF-130`/`-131`/`-132`'s still-open items except where named below.

## ⛔ GIT UNDER THIS MOUNT — read before the first git call
Every git call leaves a 0-byte `.git/index.lock` / `HEAD.lock` / `refs/heads/master.lock` the mount will not unlink; the SECOND git call then fails. Run ALL git through the shim: `export PATH=/tmp/gitshim:$PATH` (a bash script that `mv`s 0-byte locks into `.git/_orphan-locks/<name>.<epoch>` then `exec /usr/bin/git "$@"`; if `/tmp/gitshim/git` is gone, recreate it — the sandbox is rebuilt between sessions). `git checkout -- <path>` cannot replace a file here: use `git show HEAD:<path> > <path>`. Deletions must be pre-staged with `git rm --cached` because the commit script's `git add -- <path>` cannot name a path that no longer exists. Three lanes and the conductor hit all of this today; **the runbook line is owed and is Dave's — carry it, do not write it into the runbook yourself.**

## DECLARED by the conductor (measure what you can; never default an absence)
- **FILL at brief-cut: 326,763 real / 137 turns, peak** (`_checkin.py`, conductor's seat) — ⛔⛔ **the 256,000 HARD LINE BREACHED BY 70,763 — the THIRD breach on the record after #277 and #281**, 146,763 past the ruled 180,000 stop line, 106,763 outside the ≤220,000 tolerance. Dave asked *"whats the context temp like, can we squeeze it in?"* and the conductor answered no and called the wrap; his word: *"ouch, better wrap"*. Say plainly that the conductor did not gauge between the logo-review landing and the horizontal clear-space ruling, and cut two Opus lanes past the wall. Measure the final at your seat and publish both.
- **BOOT 77,474 real, n=1** — measured at the conductor's seat by `_checkin.py` at the opener (+6,680 on the `s129-D1` floor 70,794; NOT a re-base). Confirm from the transcript's first turn if you can reach it.
- **Subs: 3 Opus lanes** — RN 163,406 · LR 190,478 · LL 193,036 = **546,920** sub-tokens by the harness. QUOTA, never FILL. No pace panel was asked for — say so.
- **`/sessions` 99%** (142 MB free at the 09-18 opener; 47 MB the evening before — it was 100% and the sandbox was rebuilt overnight). Playwright + Chrome headless shell live under `/tmp` (`PYTHONUSERBASE=/tmp/pyu PLAYWRIGHT_BROWSERS_PATH=/tmp/pwb LD_LIBRARY_PATH=$HOME/.local/lib TMPDIR=/tmp`).
- **PUSHED at `3f009cc`** before this brief — the script's own verdict line: `✅ pushed and VERIFIED: remote master == local 3f009cc90f2db31865565faa09d6f7600635ae5d` (`8591829..3f009cc`). **CI read back via the public API with curl** (`gh` absent): run `35327588302` — `release` success · `render` in progress at read time · **`gates` FAILING at "Survey the COMMITTED tree" and "Knowledge build"** — the SAME two steps that failed on `854ea12` (run `35256282608`) and `8591829`: INHERITED red, unchanged by today. Quote, do not soften.
- Tree clean at brief-cut except this brief and whatever the ritual writes. The two instrumentation appends were COMMITTED (`3f009cc`) to clear the push gate — policy on them is still Dave's (dream pass 6 P2 floated); say so.

## DAVE'S WORDS — verbatim, all in `notes/_lanes/282/DAVE-RULINGS-2026-09-17.md` (committed). His exports: `notes/_lanes/282/rule-notes/DAVE-EXPORT-2026-09-17.json` (19:10Z, 14 of 15 on the recommendation, `col26-012` → "Discuss first", note on `col26-016` green) · `notes/_lanes/282/logo-review/DAVE-EXPORT-2026-09-18.json` (08:20Z, 9 answered, 3 moot, four notes).
Things he said that are NOT inscribed and must be carried as ideas, his: (1) *"what harm is there to an agent having the principle as guidance … make it the hyper-designer I'm aiming for"* — the 121 principles no rule reaches; the conductor's answer: expose all 145 with grade + misreadings, a theory door (#130's thirteenth verb). (2) The graph's philosophy — *"other modes that aren't so strict … form opinions instead of simply following a rules decision tree"*; the conductor's counter: strictness on the GRADE not the mode, three postures obey / weigh / argue, agent opinion cites an edge and is gradeable; his *"this all sounds great, I really like this idea"*. (3) *"three dials we can use to tune the designers experience"* — layers (foundations + a11y + regulation the floor) is one; the tone-of-voice temperature map was offered as the second and REFUSED (*"no dial 2 isn't right. I'll take a look"*); the third unknown — OPEN, his. (4) *"in the future we will re-integrate this material as an AI readable version of create, we might have something like 'Ask create' assistant bot"* — `W-282ll`.

## RULINGS THIS SESSION — 613 → 619, verify each by span (`git show --numstat`; the three amended-in-place ones show `N N`, by design, and say so)
- `s282-D1` (`56c5690`, lane RN) — a rule NOTE becomes a rule EDIT when he says so: 11 guideline edits in his words across 8 files, 2 second `restsOn` edges (73 → 75), `col26-012` not edited.
- `s282-D2` (`bb016c6`, conductor) — the red-text ban is on the PRIMARY red; the exception a SECONDARY red only; `col26-012` ASK closed (cross-ref already present). Backlog `W-282a` (universal icon list) `W-282b` (SC 2.2.2 on mot-005).
- `s282-D3` (`0acb019`, corrected `c143a1b`, conductor) — logo size by RAW HEIGHT (and width), five steps x-small 24 · small 28 · medium 32 · large 36 · x-large 40; the first cut 32–56 stood for one commit and is named inside the ruling.
- `s282-D4` (`da9f824`, conductor) — the identifier lockup SCRAPPED: 8 variants, `_logo_nodes.json` 12 → 8 nodes, files out of the tree (held in gitignored `_to_delete/logos-identifier-282/`).
- `s282-D5` (`eaf1236`, lane LL) — the logo review landed: ONE logo guideline (`logos.md`; Apollo's decisions override the refresh), masthead default full colour both grounds all four themes (`logo26-008` BLOCKING), vertical clear-space floor 0.25 × height snapped up to 4px (`logo26-009` BLOCKING), hexagon alone on nav-rail head / app tile / favicon + responsive small sizes (`logo26-010` ADVISORY); 8 logo nodes bound (27 → 33 edges; `governedBy` widened from declared-null-only — ruling-shaped, the lane's call).
- `s282-D6` (`03fe8ff`, corrected `6df8b64`, conductor) — horizontal clear space 0.25 × the LOGOMARK's width snapped up to 4px, on the open side by alignment (centre both · right-aligned left only · left-aligned right only); same floors for both lockups 12/16/16/20/20; the lockup-width cut stood for one commit.

## WHAT LANDED — 14 commits, ALL PUSHED. Lane reports: `notes/_subreports/2026-09-17-282-RN-rule-notes-land.md` · `2026-09-17-282-LR-logo-review.md` · `2026-09-18-282-LL-logo-land.md`.
- conductor `256e4cf` the rule-notes page (15 rows) · `9e3bab6` export + words · RN `56c5690` · conductor `bb016c6` (D2 + backlog) · `6bac5e4` **the matrix eye-check CLOSED on his word** (*"sorry I though this was settled"* — it was, by `s280-D1`; the carry was the conductor's expectation, never his ask; `W-280lm` done — the LESSON: a carry only the conductor wants is not Dave's open item) · LR `4c536a3` the logo review page (12 lockups, 3 exporter defects, 6 questions) · `0acb019` `c143a1b` D3 · `da9f824` D4 · LL `eaf1236` D5 · `03fe8ff` `6df8b64` D6 · `3f009cc` appends.
- Explorer 1.25 → 1.26. Rules index 470 → 474 (BLOCKING 59 → 62). `restsOn` 73 → 75.

## OPEN, RULING-SHAPED, NOT INSCRIBED (`s271-D4` shape — questions put)
1. **THE PER-SIZE LOGO MASTERS LANE — #283's FIRST MOVE, fully specified, no conversation needed:** 8 lockups × 5 steps = 40 SVGs drawn at raw pixel height AND width (no viewBox), horizontals and stems snapped to the grid, from the 8 surviving exports; the two-inks light-mono and the hexagon duplicate pair are known and not blocking. This is the "hinting" answer he asked for (*"is it possible to 'hint' svgs so they are as sharp as possible?"*).
2. **The three dials** — his; the third unknown.
3. **The theory door / posture idea** — an idea he liked, not a ruling; put ONE page when he asks, not before.
4. `W-282a` universal icon list · `W-282b` SC 2.2.2 citation · `W-282ll` "Ask create".
5. **The git-lock runbook line** (three lanes + conductor today; the shim is in `/tmp`, not the repo) · **the `gen_kg_rules.py` footgun STILL unfixed** (would now delete 75 `restsOn` lines) · `notes/_lanes/281/rests-on-land/land_rests_on.py` would drop RN's two edges (re-lander is `land_rule_notes.py`) · the ratified `gen_kg_icons.py` rewrites `_icon_nodes.json` with older wording when run (reverted twice today).
6. Carried unchanged from #130–#132: force by grade · ghost colour · FO's two untied artefacts + repo-tool family (`W-281fo`) · jade-lifestyle · shells-3D cutaway · sandbox snippet scripts · #130's four (`designrulings`, 13th verb + `verbVia` — now with a use, the theory door · Spider `v1.0.14` · `va25-013`) · `s277-D12` tokens NEVER STARTED (FOURTH session — say so) · the (f) items (`s214-D6`, boot-floor label, `#243` form now an EIGHTH wrap, two-probe-shape §A, the wrap-sub memory seat limit — TEST the store write from your seat again, n=4) · step 5b vs the `s241-D2` cap · what a session should DO at the hard wall (third breach now — press it).

## PUSH — already done at `3f009cc` before this brief. Push the wrap commit too (`--push`), read back the verdict line verbatim; CI via `curl https://api.github.com/repos/daveasewen/UX-design/actions/runs?per_page=1` if you can, quote what you read, claim nothing you did not.

## MEMORY — write the cloud Project store from your seat as #279–#281 did (`wrap-282-<slug>.md` + the index line newest-first, cap 49,152 B); write `notes/_lanes/282/WRAP-MEMORY-HOOK.md` FIRST regardless; record whether the store write succeeded (n=4 on the "structural seat limit" claim).
## NEXT CHAT TITLE — `Apollo - #283: the logo masters and the third dial`
Return: the handoff path, the wrap commit sha, the push/CI verdict line verbatim, final FILL, whether the memory store write succeeded, and any gate you could not pass.
