# LANE LL — BRIEF — LAND THE LOGO REVIEW: inscribe s282-D5 on his export, merge the two logo guidelines into one under Apollo's word, author the clear-space, masthead-default and hexagon-only rules in his words, regen the rules index, re-bind the 8 logo nodes. ENACT.
#282 · 2026-09-18 · conductor (Fable 5.1) · **model: opus** · bash root `/sessions/tender-hopeful-allen/mnt/UX-design/`

## Dave's word, verbatim
Export 08:20:48Z — `notes/_lanes/282/logo-review/DAVE-EXPORT-2026-09-18.json`; his notes are copied verbatim in `notes/_lanes/282/DAVE-RULINGS-2026-09-17.md` (last bullet). The option sentences are on the page `notes/_lanes/282/logo-review/LOGO-REVIEW-2026-09-17.html`. Already ruled today and NOT yours to redo: `s282-D3` (logo size by raw height, 24/28/32/36/40 = x-small…x-large) · `s282-D4` (identifier lockup scrapped, 8 variants, `_logo_nodes.json` 8 nodes).

## Read first (retrieval)
`knowledge/guidelines/logos.md` (2025 sticker sheet, 27 lines) · `knowledge/guidelines/brand-refresh-assets.md` — its logo section (`logo26-*`, `va25-015`, `va25-016`, `va25-017`; `logo26-001` carries today's native-app clause `(s282-D1, Dave 2026-09-17)`) · `knowledge/guidelines/gen_rules_index.py` (regenerates `_rules-index.json`; touches no edge) · `knowledge/_logo_nodes.json` (8 nodes; `governedBy` declared-null on the 4 hexagons; `usesLogo` from 9 components) · `notes/_lanes/282/logo-review/bindings.json` · the four theme names as the page used them (Apollo Legacy · Mono · Console · Supercharge) · `s282-D1`…`s282-D4` in `_rulings.json` for the shape.

## Do
1. **Inscribe `s282-D5`** by textual span at the END of `_rulings.json` (`N 0`; the file ends `\n ]\n}` — insert `\n ,\n <object>` before that tail; re-read immediately before commit). `says` = the export ref + every choice + his four notes VERBATIM. `ruled`, in these terms:
   - **Masthead default = full colour on both grounds, in all four themes** (q2 a×4). `va25-015` (masterbrand in ALL mastheads) stands; this names the variant.
   - **Digital clear space is a FLOOR, not the print rule:** vertical clear space ≥ 0.25 × logo height, snapped UP to the 4px grid. At the five steps that is 24 → 8 · 28 → 8 · 32 → 8 · 36 → 12 · 40 → 12 px. Anything at or above the floor that sits on the 4px grid is legal. His note says VERTICAL; horizontal clear space is NOT stated and is NOT ruled — say so in the rule line and the report. The print (logo-dimension) clear space stays the print rule.
   - **The hexagon alone** may be used on the nav-rail head, app tile and favicon (each conditional on Create Direct approval and "HSBC" in view — `va25-016`'s conditions stand), AND in responsive layouts at the smaller sizes — tablet-portrait and mobile — where the masterbrand does not fit.
   - **One logo guideline.** `logos.md` and the logo section of `brand-refresh-assets.md` MERGE into one file, and **Apollo's decisions override the refresh where they differ** — his "our decisions here over-ride the refresh". Photography and Creative Hexagons stay in `brand-refresh-assets.md`.
   - **The identifier lockups are retired** (q6 b — enacted already by `s282-D4`); his forward note about re-integrating create.hsbc material as an AI-readable "Ask create" assistant is FILED as a carry, not enacted.
   - **Hexagon pair kept** (q1 a): both files, both nodes, no alias.
2. **The merge.** Build `knowledge/guidelines/logos.md` as the one logo guideline: front-matter that names both sources and `s282-D5`; the eight-variant set (`s282-D4`); the five-step height scale (`s282-D3`, raw height and width, no viewBox); the masthead default per theme; the clear-space floor; the hexagon-only rule; and MOVE the `logo26-*`, `va25-015`, `va25-016`, `va25-017` rule bullets from `brand-refresh-assets.md` into it VERBATIM, keeping every `{#id}`, every destiny tag and every `(s282-D1, Dave …)` clause byte-for-byte — the rule ids must survive the move so `_rules-index.json` and the 27 `restsOn`/`obeys` references to them do not break. Leave a one-line pointer in `brand-refresh-assets.md` where the logo section was. New rules get ids in the `logo26-` series (`logo26-0NN`, next free), destiny BLOCKING for the clear-space floor and the masthead default, ADVISORY for hexagon-only, each ending `(s282-D5, Dave 2026-09-18)`.
3. **Regenerate `_rules-index.json`** with `gen_rules_index.py`. Prove: every pre-existing logo rule id still present with its text unchanged (diff = file field moved + N new ids, 0 lost). Grep the corpus for `brand-refresh-assets.md#logo26` / `#va25-015` style anchors and fix any that break.
4. **Re-bind the logo nodes.** The 4 hexagons' `governedBy` nulls become real: `logo:hexagon-* governedBy rule:logo26-0NN` (the hexagon-only rule); the 4 masterbrand nodes gain `governedBy` to the masthead-default and clear-space rules. Do this the way `_logo_nodes.json`'s `$description` says (the ratified generator `notes/_lanes/277/icons-propose/gen_kg_icons.py --land --ratified s277-D4`) IF its `GOVERNS_LOGO_RX` route picks the new rules up from the guideline; if it does not, hand-author the edges in the same shape as the existing ones and say so as ruling-shaped. ⚠ That generator ALSO rewrites `knowledge/_icon_nodes.json` and the lane-277 copies with older wording — restore those byte-identical from HEAD (`git show HEAD:<path> > <path>`; `git checkout` cannot replace files under this mount).
5. **Verify**: `python3 knowledge/_validate_kg.py` (or the explorer build check) green; the explorer shows the hexagon nodes lit (grep the built HTML); `_validate_lane_ownership.py` unchanged.

## Do NOT
Touch `s282-D3`/`s282-D4` text, any SVG, the review page, or any rule not named. Never `git stash` / `gen_kg_edges.py` / `gen_kg_rules.py` (it would delete the 75 hand-authored `restsOn` lines) / `_build_all.py`. `/sessions` is 99% full: scratch under `/tmp`. **Git under this mount:** every call leaves a 0-byte lock the mount won't unlink — run all git through the shim `export PATH=/tmp/gitshim:$PATH` (it exists; if `/tmp/gitshim/git` is missing, recreate it: a bash script that `mv`s `.git/index.lock`, `.git/HEAD.lock`, `.git/refs/heads/master.lock` when 0-byte into `.git/_orphan-locks/` then `exec /usr/bin/git "$@"`). Deletions must be pre-staged (`git rm --cached`) because the commit script's `git add -- <path>` cannot name a path that no longer exists.

## Report — `notes/_subreports/2026-09-18-282-LL-logo-land.md`, evidence under `notes/_lanes/282/logo-land/`. Plain prose first: the merged guideline's shape, the new rules in one line each with their numbers, what moved, what the generator did or did not pick up. COUNTS · ruling-shaped · UNPROVEN · REPLAY-THESE. `_state.json`: close `W-282lr` (`closed_by` = the export + `s282-D5`); add `W-282ll` for the "Ask create" carry (owner dave, closes when Dave rules whether create.hsbc material is re-integrated as an AI-readable assistant) and for horizontal clear space (not stated).
Commit via `SESSION_N=282 SHOWROOM_ACK="inherited standing gap: showroom 138 vs inscribed 108, Dave's since #279" bash knowledge/_git_commit.sh --reconciled <fresh msgfile> <paths…> < /dev/null`, msgfile `/tmp/_msg-282-LL-$(date +%s).txt`, subject `#282 lane LL: logo review landed — s282-D5 inscribed, one logo guideline, clear-space floor + masthead default + hexagon-only ruled in his words, 8 nodes bound`. Name the gates' own dirty appends in the msgfile, do not stage them. Do not push. Return ONLY a stub: verdict, span numstat, new rule ids, index counts before/after, edges before/after, sha, report path, spend.
