# #276 — DAVE'S WORDS, VERBATIM — 2026-09-15

Source: the #276 chat. Export: `notes/_lanes/276/tie-off/DAVE-EXPORT-2026-09-15.json` (19:08:48Z).

- *"Good Morning!"*
- *"1. fix"* — on the two-seat hazard. Read back as "make it impossible". The RULE (one seat / own-lane-only) is NOT ratified.
- *"2. push"* — done, `2740d3b..3b9d89b`.
- *"3. edit mode can wait until we do edit mode, park it has a tripwire. 19 missing  WCAG criteria, I wasn't aware of this. rule→component authored the other way --- maybe we just tie off these loose ends first."*
- *"QQ, where will you get the missing Ally rules?"* — answered: the W3C Understanding pages, the corpus's own `wcag_url` provenance.
- *"yes, and maybe flag any extra you think are applicable"* — the four starter components + flag extras.
- Export 19:08:48Z: D-1 a · D-2 a *"and cited as best practice"* · D-3 a · D-4 a · D-5 a · D-6 a *"Okay but I don't think this is durable, feels like we're patching just to get it done"*.

Two notes are attached ⇒ two SEQUELS (RK-3/RK-4/RK-6). The read-back is where they become a plan; nothing is inscribed until his word on the read-back.

- On the first read-back (seat lock / worktree per session as the "durable" options): *"'a seat lock (one session holds the repo; a second can't commit at all), or a worktree per session (each seat has its own checkout; merges are explicit).' this has gone from a patch to a hack, not sure I like this unless its not permanent"* — both options DROPPED by the conductor.
- On the second read-back (guard lands advisory with a removal date at the next dream pass if the tripwire never fired; one seat at a time is the norm by his #57 word; no lock, no worktrees; parked item = "did the guard ever fire"): *"go"* ⇒ `s276-D1..D6` may be inscribed and landed.
