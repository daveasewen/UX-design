# LANE EX4 — BRIEF — inscribe `s280-D2` and build explorer 1.20: INSPECT opens the ARTEFACT itself — the file, the rendered component snippet, the edge
#280 · 2026-09-17 · conductor (Fable 5.1) · **model: opus** · bash root `/sessions/intelligent-serene-curie/mnt/UX-design/`

## Dave's words, verbatim (`notes/_lanes/280/PACE-2026-09-16.md`, last block)
*"I want to see any artifact that exists here, including a render of the actual component snippet, this is such a good mental model that it becomes a great explorer for the entire system"* · *"no i want to inspect the edge or indeed the actual file, a component for example, I'm starting to see this as an excellent explorer for the entire system"*.

## Step 0 — INSCRIBE `s280-D2` first, its own commit (`_inscribe_ruling.py`, dry-run then `--write`, insertions only, proof PASSED; re-render `notes/_RULINGS.html`, `--check` FRESH). Text:
**THE EXPLORER IS THE FRONT DOOR TO THE WHOLE SYSTEM: INSPECT on any node or edge opens the artefact itself, not a summary of it — a component opens its meta AND a live render of its snippet, a snippet renders, a guideline shows its markdown, a rule shows its row in the guideline, a ruling shows its record, an icon or logo draws its SVG, a session/evidence node shows the filed report, an edge shows its own record (type, verb, provenance, evidence). The page is therefore SERVED, not double-clicked: the repo ships `knowledge/_serve_explorer.py` (one command, repo root, no dependencies) and the explorer says so when opened from disk. Storage untouched. Refines `s277-D8`'s three views and `s280-D1`'s matrix with a door on every dot and every line.** `by: Dave`, `says:` both quotes verbatim, `governs: knowledge/_kg_explorer.template.html, knowledge/_build_kg_explorer.py, knowledge/_serve_explorer.py`, `evidence:` PACE file + lane EX3's report + this lane's.

## Read first
`notes/_lanes/280/explorer-ux/REPORT.md` (1.19: the modal, the `src` key per node, the `file://` fallback, the served test) · `knowledge/_kg_explorer.template.html` · how snippets are stored and rendered in the showroom (`knowledge/` — find the snippet HTML files the `renderedBy` edge points to, and the showroom page builder that renders them with `canon.css`/`type.css`, so the modal renders a snippet the SAME way the showroom does — same stylesheet links, same theme attribute).

## Build — 1.20, commits in this order
1. **`knowledge/_serve_explorer.py`** — `python3 knowledge/_serve_explorer.py` serves the repo root on a free local port, prints the explorer URL, opens nothing itself. Stdlib only. The explorer's `file://` fallback banner names this command.
2. **Node artefacts by type** in the modal, each rendered by its kind: component → meta record (as 1.19) + a **RENDER** tab showing the `renderedBy` snippet live in a sandboxed `<iframe>` with the showroom's stylesheets and the page's current theme, light/dark following the explorer's toggle; snippet → the same render + its source; guideline → markdown rendered; rule → its guideline with the rule's row scrolled to and highlighted; ruling → the record from `_rulings.json` (ruled / says / evidence / governs as a definition list); icon/logo → the SVG drawn at 48 and 16 px light+dark + source; session/evidence/artefact → the filed markdown rendered; token group → the token rows. Unknown kind → the raw file with its type. All via fetch relative to the served root — embed NOTHING.
3. **Edge inspect** — every relation row in the panel gets the same `i`: the modal shows the edge record — type, the verb's sentence (`_kg_verbs.json`) with source and target named, `verbVia`/provenance where the edge carries it, the null declaration if it is a declared null, and the evidence path if any — with both ends clickable to their own artefacts (breadcrumb in the modal so you can walk without closing).

## Gates
Regenerate `notes/_KG-EXPLORER.html` via `python3 knowledge/_build_kg_explorer.py` only. Default canvas md5 identical to 1.19 · node/edge object identity as EX3 proved · served driver (`_serve_explorer.py` on a scratch port, killed after): inspect a component → RENDER tab shows the iframe with the snippet's root element present and `type.css` applied (`document.fonts.check` is NOT proof — see the `set_content()` ban; assert a computed style from `type.css`) · a guideline, a rule, a ruling, an icon, an edge each open with content present · `file://` never-driven: banner names the serve command, no errors · page errors `[]` · screenshots 1280 light+dark: component RENDER tab, an edge modal, a ruling modal. `/sessions` at 99%: download NOTHING, never copy `knowledge/`, scratch under `/sessions/intelligent-serene-curie/mnt/outputs/ex4/`, removed at the end. Never `git stash` / `gen_kg_edges.py` / `_build_all.py`.

## Report — `notes/_lanes/280/explorer-artefacts/REPORT.md` + `notes/_subreports/2026-09-17-280-EX4-explorer-artefacts.md`; `_state.json` add `W-280ea` (close = Dave has opened a component render and an edge in the served explorer). Plain prose first: three sentences and the one command he runs.
Commits via `SESSION_N=280 SHOWROOM_ACK=1 bash knowledge/_git_commit.sh --reconciled <fresh msgfile> <paths…> < /dev/null`, msgfile `/tmp/_msg-280-EX4-$(date +%s).txt`, bare subjects `#280 lane EX4: s280-D2 inscribed — the explorer is the front door` · `#280 lane EX4: explorer 1.20 — INSPECT opens the artefact, the snippet renders, edges open`. Regenerate `_CHAIN.md` in the same call as the commit if the store row trips the gate. Locks → `mv` to `.git/_orphan-locks/`. Do not push. Return: report path, shas, the three sentences and the command.
