# LANE LM — BRIEF — inscribe `s280-D1` and build the explorer's LAYOUT MATRIX: force · strata · shells, each in 2D and 3D, force the default
#280 · 2026-09-16 · conductor (Fable 5.1) · **model: opus** · bash root `/sessions/intelligent-serene-curie/mnt/UX-design/`

## Dave's word, verbatim (his export `notes/_lanes/280/layout-sketches/DAVE-EXPORT-sketches-2026-09-16.json`, 20:30Z)
choice "other" · keep: strata, shells, floors, orbits · note: *"Okay I want all of these plus the original view, in 2d and 3d for all of them. just keep force as the default"*. Earlier today: *"I actually like the strata, maybe we just have multiple views, could the strata possibly be 3d too?"* and *"The 3D view is a real crowd pleaser"*.

## Step 0 — INSCRIBE the ruling FIRST, as its own commit
`s280-D1` in `knowledge/_rulings.json` via `knowledge/_inscribe_ruling.py` (read its docstring; dry-run, then `--write`; `git diff --numstat` must be insertions-only; reconstruction proof must pass — lane W at #279 did exactly this, see `notes/_lanes/279/W/`). Text, in the record's register:
**THE EXPLORER HAS A LAYOUT MATRIX, FORCE THE DEFAULT: three layouts — FORCE (as built), STRATA (three bands, Constitution as bedrock) and SHELLS (concentric, Constitution the ground) — each in 2D and 3D, on one switch; STRATA-3D is the sketch called FLOORS and SHELLS-2D the sketch called ORBITS, so the four sketches plus the original are the six cells of one matrix. Storage untouched: no node id, edge type or file under knowledge/ changes; only the explorer's placement and switch. Force stays the view the page opens with. Refines `s277-D8` (three views by force) with a placement per view; `s279`'s LY/LS lanes are the evidence.** `by: Dave`, `says:` his note verbatim + the two earlier quotes, `governs: knowledge/_build_kg_explorer.py, knowledge/_kg_explorer.template.html`, `evidence:` the two exports and the three lane reports (`notes/_lanes/280/layout/REPORT.md`, `notes/_lanes/280/layout-sketches/REPORT.md`, this lane's). Re-render `notes/_RULINGS.html` the module's own way if a script does it (`--check` FRESH).

## Then build — v1.18, THREE COMMITS in this order so partial progress survives
Read first: `notes/_lanes/280/layout/REPORT.md` (how strata-2D was added: `y2`, the switch, `fit()`, the sim identity method) and `notes/_lanes/280/layout-sketches/REPORT.md` + `_sketches.py` (Fibonacci shell placement ordered by longitude; percentile-rank flattening; the 3D clumping finding). The existing 3D view's projection/rotation in the template is what all 3D cells use.
1. **STRATA-3D (Floors)** — three translucent plates in an exploded stack, each carrying its view's force x,y flat on it, cross-plate edges between plates, Constitution a fourth plate beside (LS sketch 3). Coordinates at build time (`x3s,y3s,z3s` or similar), existing `x,y,x3,y3,z3,y2` untouched.
2. **SHELLS-2D (Orbits)** — concentric rings, angle from the force cluster position, Constitution an arc outside (LS sketch 4). Build-time `r,θ` → `x,y` pair.
3. **SHELLS-3D** — Fibonacci placement per shell radius by view, plinth disc for the Constitution, outer shells at reduced opacity so the core reads; a cutaway toggle is welcome if cheap, otherwise declare it out (LS sketch 2).
Switch: `LAYOUT · force / strata / shells` × `2D / 3D` (the existing 3D toggle becomes the second axis). Default force-2D, pixel-identical to 1.17 (canvas md5 proof). Every cell: `?layout=<name>&dim=<2d|3d>` URL flag, `fit()` fits the live coordinate set, dig/scrub/legend/search work or the report says which cell they do not. Version 1.18; regenerate `notes/_KG-EXPLORER.html` via `python3 knowledge/_build_kg_explorer.py` only.

## Gates
Sim identity on `x,y` at defaults vs 1.17 (LY's `sim.py`) · default canvas md5 identical · per-cell driver: every node inside its band/plate/shell (tolerance stated) · page errors `[]` in never-driven contexts · screenshots 1280 light+dark for all six cells + 390 light for the 2D cells, under `shots/` · `/sessions` at 99%: download NOTHING, never copy `knowledge/`, scratch under `/sessions/intelligent-serene-curie/mnt/outputs/lm/`, removed at the end. Never `git stash` / `gen_kg_edges.py` / `_build_all.py` / `_build_kg_explorer.main()` from elsewhere.

## Report — `notes/_lanes/280/layout-matrix/REPORT.md` + `notes/_subreports/2026-09-16-280-LM-layout-matrix.md`; `_state.json`: close `W-280ls` (export received, receipt names the file), add `W-280lm` (close = six cells shipped and Dave has seen the contact sheet). Plain prose first: one sentence per cell on what the eye gets. Also build a one-page CONTACT SHEET `notes/_lanes/280/layout-matrix/MATRIX-2026-09-16.html` (swiss-design-system idiom): the six cells' 1280-light screenshots in a 3×2 grid, layout by row, dimension by column, one caption each, no radios — it is for looking, not ruling.
Commits via `SESSION_N=280 SHOWROOM_ACK=1 bash knowledge/_git_commit.sh --reconciled <fresh msgfile> <paths…> < /dev/null`, msgfile `/tmp/_msg-280-LM-$(date +%s).txt`, bare subjects: `#280 lane LM: s280-D1 inscribed — the layout matrix, force default` · `#280 lane LM: explorer 1.18a strata-3D (floors)` · `#280 lane LM: explorer 1.18b shells-2D (orbits)` · `#280 lane LM: explorer 1.18 shells-3D — the six cells complete`. Regenerate `_CHAIN.md` if the store row trips the chain gate. Locks → `mv` to `.git/_orphan-locks/`. Do not push. Return: report path, shas, the six one-sentence captions, and anything you declared out.
