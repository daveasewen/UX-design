# #280 — LANE W — the wrap's own working record

provenance: 280 · 2026-09-17
status: observed

**The filed report (`s218-D7`) is `notes/_subreports/2026-09-17-280-W-wrap.md` and is the authority.**
This file holds the two things that belong beside the working set rather than inside the filed
report: the **2c/2d/2f EXIT CHECK** and the **`s271-D4` re-read**, which is the ritual's final beat.

## EXIT CHECK — run BEFORE the three rolls, recorded rather than asserted

Every ⚠ / ⬛ / AWAITING / OPEN-CALL / DEFERRED-TO-DAVE item inside the three rolling regions was
scanned and matched to a **standing** home before the move. Dated homes do not count.

| rolling region | Dave-owed items found | standing home confirmed |
|---|---|---|
| GM ★ PRIOR banner **#278** (11 lines → `_GM-ARCHIVE.md` § Batch 2026-09-17 #280) | ⬛ showroom *"108 stale"* (HIS) · ⬛ the #278 carry ① (`s277-D4..D13` "LAW and NOT IN THE TREE") | `_CARRIES.md` § `residual → #281` — showroom appears in the set (10 occurrences); the #278 carry ① is already **STRUCK with its receipt** by #279's `carry280.py` and travels struck, never deleted |
| LS ⏱ PRIOR DELTA **#277** (32 lines → `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-17 #280) | ⬛ the #277 DO-NOT-RULE list · ⬛ *"what a session should do at the HARD WALL is not ruled"* | both stand in `_CARRIES.md` § `residual → #281` (the hard-wall item present; the DO-NOT-RULE items are the four `_HANDOFF-130` carries, all present) |
| GM stratum **#279** (`roll_2f`: 24 lines → `notes/_GAUGE-LOG.md` at true EOF, 2 lines → `_GM-ARCHIVE.md`) | ⬛ the `#243` not-a-wrap form · ⬛ the commit-hash declared gap | the `#243` form stands in the carry set (48 occurrences) and is **re-declared on this wrap's own COMMIT STATE #280 block**, now its sixth wrap; the hash gap is re-declared in the same block |

⛔ **Nothing was orphaned by these moves, and nothing was dropped for being old.** Three items that
had been carried only in wrap REPORTS — the `s129-D1` boot-floor label, the two-probe-shape §A
question and what `s214-D6` means below band — were found to have **no line of their own** in
`_CARRIES.md` and were MINTED there at 2c(i) rather than left in a dated home. That is the EXIT
CHECK doing the job it exists for.

## ⬛ `s271-D4` RE-READ — the ritual's FINAL beat, run at 5b and RECORDED rather than assumed

The memory hook's "open, Dave's" items (`notes/_lanes/280/WRAP-MEMORY-HOOK.md`) were re-read against
`knowledge/_rulings.json` **after every ruling of the session was inscribed**. #280 inscribed exactly
two — **`s280-D1`** (the layout matrix) and **`s280-D2`** (the explorer is the front door), both by
the lanes that built them, both before this ritual opened.

**NOTHING IS STRUCK.** Item by item, and the near miss is named rather than left to be noticed:

1. **The orphan census export** — `s280-D1`/`s280-D2` do not touch it; his ten radios are unanswered.
2. **The eye-check of the six matrix cells** — ⚠ **THIS IS THE NEAR MISS AND IT IS DECLARED.**
   `s280-D1` is *about* the six cells and might read as closing the question; **it does not.** The
   ruling says the matrix EXISTS with force as the default; the open item is which cells survive his
   eye, which he has not given. A same-subject ruling is a strong match, **not a proof**
   (`s271-D4`'s own warning), and a wrong strike is worse than a stale item.
3. **`jade-lifestyle`** — open by his own word; no ruling of this session speaks to it.
4. **The shells-3D cutaway and the sandbox's stripped snippet scripts** — declared out by their
   lanes, not ruled out; `s280-D1`/`s280-D2` say nothing about either.
5. **The wrap-sub memory seat limit** — measured false twice; no ruling exists, because amending a
   ratified runbook step is Dave's.
6. **The four items carried from `_HANDOFF-130`** (`designrulings`, a thirteenth verb + `verbVia`,
   the Spider `v1.0.14` cut, the `va25-013` split) — untouched by this session.
7. **`s277-D12`** — still never started; nothing closed it.

⚠ **Every open item above is written as the QUESTION IT IS, not as a state of the world.**

---

## POST-WRAP ADDENDUM (5b) — the shipped line, written after the gate ran

✅ **SHIPPED: `0317bb6`** — `SESSION_N=280 SHOWROOM_ACK=1 bash knowledge/_git_commit.sh --reconciled <msgfile> <21 named paths>`, the `#243` DECLARED not-a-wrap path, **sixth wrap running**. ⚠ **The commit was REFUSED TWICE BEFORE IT LANDED AND BOTH REFUSALS WERE THE SCRIPT WORKING:** the `#208` REUSED-MSGFILE gate caught a msgfile whose line 1 already carried a T3 prefix (a fresh, uniquely-named file fixed it), and the `#208` MENTION-MAP gate found `knowledge/_graph-mention-map.json` stale, regenerated it, and **refused to stage a path this seat had not named** — the P5 rule. Both were repaired by re-running with the path named, never by widening the stage.

✅ **PUSHED AND VERIFIED. The script's own literal verdict line, quoted and not paraphrased:**

```
✅ pushed and VERIFIED: remote master == local 0317bb62edd4a8a6faa466130a37bdb8bbc792cf
```

`5cc3ecd..0317bb6  master -> master` — the eighteen commits that stood unpushed at the ritual's open (the seventeen lane commits plus this wrap's) are now on the remote.

⛔ **CI IS NOT OBSERVABLE FROM THIS SEAT AND IS DECLARED RATHER THAN GUESSED.** `knowledge/_git_commit.sh` emits **no CI term at all** — its only verdict line is the push line quoted above — and `gh` is not installed in this sandbox, so no run of `.github/workflows/gates.yml` can be read back here. ⚠ **What is known, and is another session's testimony rather than this one's measurement:** #279's own record states CI `gates` was **RED on its wrap commit and INHERITED** (the same red on `7c72cc1`). **This wrap makes no claim about CI's colour on `0317bb6`** — an unobservable is written as an unobservable [[feedback-measuring-tool-must-not-guess]], and the conductor may read it back at the #281 opener.
