# LANE LY — BRIEF — explorer 1.17 CANDIDATE: the graph laid out as THREE VISIBLE LAYERS, one option on a page for Dave's eye
#280 · 2026-09-16 · written by the conductor (Fable 5.1) · **model: opus** · bash root `/sessions/intelligent-serene-curie/mnt/UX-design/`

## Why — Dave's own words on explorer 1.16
*"BTW do we still have work to do on the graph... this does not look like 3 layers, just relabelling and grouping them isn't what I expected tbh."*
`s277-D8` (read WHOLE in `knowledge/_rulings.json`, id at ~line 10169) ruled ONE GRAPH, THREE VIEWS — SYSTEM · DESIGN GOVERNANCE · EXPLANATION — with THE CONSTITUTION a fourth thing that is not a view. Lanes EX/EX2 enacted the LETTER: four legend boxes over one force graph. What he expected is a LAYOUT: three visibly separated layers on the stage. This lane puts ONE concrete option in front of his eye. **It does not rule; it renders.**

## Read first (retrieval, not a reading list)
- `notes/_subreports/2026-09-16-279-EX2-explorer-fix.md` — the 1.16 legend/fit mechanics, the chip-off identity method (§3, `sim.py` predicate re-implementation), §7.1 sizing of the column-packing fix, the Chromium recipe (`knowledge/_render/seat_env.sh`, nothing downloaded).
- `knowledge/_kg_explorer.template.html` + `knowledge/_build_kg_explorer.py` (v1.16). Where each node's view (SYSTEM / DESIGN GOVERNANCE / EXPLANATION / CONSTITUTION) is decided is the key you need — find the fam→view map EX built.

## The option to build — "STRATA": three horizontal bands, top to bottom
1. **EXPLANATION** (why) — top band. 2. **DESIGN GOVERNANCE** (what a design must do) — middle band. 3. **SYSTEM** (what exists) — bottom band, the ground. THE CONSTITUTION, when its chip is on, sits as a fourth band BELOW system (the bedrock), dimmed. Each band has a full-width label at its left edge in the band's tint, a hairline rule between bands, and its own tint at ~4% so the three layers read as layers before a single node is read. Edges that cross a band boundary are the point of the picture — draw them; within-band edges lighter.
- Positions: keep the builder's x for every node (so the columns/clusters EX built survive left-to-right); assign y by band, packed within the band by a light force (collide + weak y-centre) run AT BUILD TIME in `_build_kg_explorer.py`, stored as `y2` alongside the existing `x,y` — **the existing `x,y` are NOT touched**, so 1.16's layout is still in the file byte-for-byte and the sim in EX2 §3 must still pass on `x,y`.
- The page gets a layout switch `LAYOUT: force · strata` (default **force** = 1.16 behaviour, so the shipped default is pixel-identical to 1.16 — prove it) and a URL flag `?layout=strata` so a screenshot can be taken never-driven. `fit()` fits whichever coordinate set is live. Dig/focus/scrub/legend all keep working in strata; if one cannot in the time, say so in the report rather than fake it.
- Version line → 1.17; regenerate `notes/_KG-EXPLORER.html` the module's own way (`python3 knowledge/_build_kg_explorer.py`). Never `_build_kg_explorer.main()` from elsewhere, never `gen_kg_edges.py`, never `_build_all.py`.

## The page for Dave — `notes/_lanes/280/layout/OPTION-strata-2026-09-16.html`, single file, swiss-design-system idiom (`/sessions/intelligent-serene-curie/mnt/.claude/skills/swiss-design-system/SKILL.md`)
Side by side, same width: **1.16 force (chips at page defaults)** vs **1.17 strata (same chips)**, light and dark stacked; then strata with assets on; then strata with the Constitution on. Screenshots inline as PNG `<img>` (files under `shots/`), never-driven fresh contexts. Under the pair, three plain-prose sentences max on what moved. One radio for Dave: (a) strata, ship it as the default · (b) strata as a switch, force stays default · (c) neither — a note field. Export button writes `{exportedAt, page, answers:{layout:{choice, note}}}`. NO ruling ids in the body copy; the ruling id once in a small footer. No integers typed that the build can compute.

## Gates
`git diff --numstat knowledge/_build_kg_explorer.py knowledge/_kg_explorer.template.html` reported in the report · EX2's `sim.py` method re-run on `x,y` at page defaults for 1.16 vs 1.17 → identical drawn sets and positions · a driver proves `?layout=strata` sets every node's y to its band and no node lies outside its band · page errors `[]` in every context · `/sessions` is at 99%: download NOTHING, keep scratch under `/sessions/intelligent-serene-curie/mnt/outputs/ly/` tiny and remove it at the end.

## Report — `notes/_lanes/280/layout/REPORT.md` + `notes/_subreports/2026-09-16-280-LY-layout.md` (+ `knowledge/_state.json` row `W-280ly` via `_state.add()`, close condition = Dave's export received on the option page). Plain prose first section for Dave, measurements after.
Commit via `SESSION_N=280 SHOWROOM_ACK=1 bash knowledge/_git_commit.sh --reconciled <fresh msgfile> <paths…> < /dev/null`, msgfile `/tmp/_msg-280-LY-$(date +%s).txt`, bare subject `#280 lane LY: explorer 1.17 strata layout — one option on a page for Dave`. Regenerate `_CHAIN.md` (`python3 knowledge/_gen_chain.py`) if the store row trips the chain gate. Locks → `mv` to `.git/_orphan-locks/`, never `rm`. Never `git stash`. Do not push. Return to the conductor: the report path, the commit sha, and the three plain-prose sentences.
