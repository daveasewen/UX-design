# KG AUDIT — LANES A2 and A3 — the judgement, in two seats
#277 · 2026-09-16 · addendum to `BRIEF.md` · both **model: fable** · both READ-ONLY · both read `A1-MEASURE.md` + `A1-measure.json` (`13a4cf2`) FIRST

Dave widened the audit after A1 launched. His words, verbatim (`notes/_lanes/277/DAVE-RULINGS-2026-09-16.md`):
> *"also think about the organisation of the the different layers in this graph, are the 5 layers currently, maybe this needs reconstituted, should they be layered and conjoined as they are, should it be one or less than 5. in my mind (this may not be correct) all the accessibility nodes should be design governance nodes, and what we have as governance should be something like 'system constitution' or 'codex' or 'system management' I don't know right now, but it seems the categorisation might net be corrcet."*
> *"we talked about n-grams before, I'm not suggesting we use them, but are there other ways to augment the graph to make it more powerful or efficient, is there anything orthogonal, lateral or meta we can employ to augment the graph"*
> *"and please research as much as you see fit, its always useful to check the state of the art"*
> *"Fable can be used liberally when judgement is crucial"*

**The five layers as built** (`_build_kg_explorer.py`): **base** (component · pattern · context · snippet · intent · shape · role — the design-system wiring) + four ADDITIVE families each behind a chip: **governance** (ruling · evidence · artefact · session — 2,189 nodes, what Dave ruled and the receipts) · **guidelines** (sc · axe · principle · standard · policy · guideline — the WCAG/EN 301 549 compliance corpus, 138 nodes) · **guidelinerules** (rule — the 470 HSBC guideline rules) · **uxprinciples** (ux · polarity — 175). Dave's instinct: the compliance layer is *design governance*; what is called governance is the *system's own constitution / codex / management record*; the categorisation may be wrong.

**A1's findings both lanes must carry (do not re-measure):** the conductor's table reproduced to the digit · 105 dangling edges are notes wearing edge types (8 types, all `$note`d) · the explorer's `islands 2 / orphans 0` is a SCOPE defect (computed on the 1,050-node base before the 4 families are appended; true figure 137 components / 36 islands / 99 orphans, all `ux:`) · **12 edge types have NO consumer (3,125 edges, 47% of the graph) + 7 are schema-only incl. `obeys`** · join classes STRUCTURAL 6,277 / PROSE 291 / AUTHORED 153 · adjacency 28/400 cells filled, **12 of 20 kinds are pure sinks** · reachability: ANY-path passes 10/10 on most questions but TYPED coverage is the gap — `obeys` reaches 7.3% of components, `mustNotNeighbour` 12.4%, `rule→ux` 0%, and Q9 (tokens) / Q12 (assets) have **no target kind at all** (assets: lane RI has just proposed 688 nodes / 1,299 edges, `5520d43` — read its REPORT).

---
## LANE A2 — STRUCTURE AND LAYERS (Fable) · `A2-AUDIT.md` + `REVIEW-kg-audit-2026-09-16-v1.html`
Everything in `BRIEF.md` § LANE A2 items 1–9 **plus**:
- **§0 THE LAYERS.** Answer Dave directly: are five the right number; should they be layered-and-conjoined (chips over one graph) or one graph or fewer; is "accessibility = design governance" right, and if so what is the compliance corpus's relation to the 470 rules and the 145 principles (three layers of "what a designer must/should do" today — one governance layer with three provenances?); what should the ruling/evidence/session record be called — evaluate his three words (*system constitution · codex · system management*) and propose one, with the argument, and say what the agent would do differently at design time under each organisation. Name every ruling a re-layering touches (`s269-D1`, `s274-D7..12`, `s275-D1..6`, `s276-D3`, `s277-D1..3`, the family/chip rulings) — a re-categorisation that silently undoes a ruling is not on the table; one that proposes a superseding ruling by id is.
- The decisions page carries the layer question as **D-1** — it is the one with the most weight.
- Plain prose FIRST in every section — Dave will read it; then the technical prose; then the recommendation in one line.

## LANE A3 — AUGMENTATION AND THE STATE OF THE ART (Fable, WebSearch allowed and expected) · `A3-AUGMENT.md`
The question: beyond adding nodes and edges, what is **orthogonal, lateral or meta** that would make this graph a better designer brain — more powerful for the agent's 30-second design decision, or more efficient to maintain? n-grams were discussed (#269, `_RESEARCH-ngram-lookups-2026-09-13-v1.html`, `_ngram.py`, `_quote_gate.py` — read them; Dave is NOT proposing them) — what else?
1. **Survey, with sources fetched this session** (cite URL + what you read; nothing from memory presented as current): GraphRAG and its community summaries · property-graph vs RDF/OWL reasoning (inference of implied edges, transitivity, inverse edges — note A1 found `governs`/`governedBy` as the only true inverse pair, 1484 vs 28) · temporal/bi-temporal validity (Graphiti / Zep — `P-269-6` already parks this) · hypergraphs and n-ary relations (a ruling that binds three things at once) · embeddings as a lateral index over nodes (semantic neighbours the graph does not state) · typed-schema / SHACL-style shape validation versus what `_validate_kg.py` does · provenance models (PROV-O — the ruling→evidence→session trail is already one) · design-system-specific graphs (Figma's variables/tokens graph, W3C DTCG token aliasing — token nodes are `P-269-*`) · agent memory architectures (what "memory" means for an autonomous design agent — episodic vs semantic vs procedural; where this graph is and is not that).
2. **For each candidate augmentation**: what it adds for the hyper-designer in one plain sentence · what it costs (build, maintain, tokens at query time) · whether it is orthogonal (a new dimension), lateral (a second index over the same nodes) or meta (structure about the structure) · which A1 gap it closes, by number · which parked item or ruling it touches · **the s274-D12 test**: does it manufacture plausible-but-unratified edges? If yes it is advisory at most.
3. **Query-time reality.** The agent at design time has a context window, not a database. How does it read this graph today (`_consult*`, `designer-skills-v2`, the pack)? What is the cost in tokens of answering each of the 12 canonical questions today, and what would each augmentation do to that cost? This is the efficiency half of Dave's question and it is measurable.
4. **Recommendation**: a ranked shortlist of ≤ 5 augmentations, each with a one-lane build estimate, plus the ones you would REFUSE and why. Plain prose first.
Nothing enacted; no page (A2's page carries one decision slot for "which augmentation first", fed from your shortlist — write `A3-shortlist.json` `[{name, plain, class, closes, cost, touches}]` so A2's builder can read it; if A2 ships before you, the conductor adds it).

## Cautions (both)
Read-only outside `notes/_lanes/277/kg-audit/` · never `git stash` / `gen_kg_edges.py` / `_build_all.py` / `_build_kg_explorer.main()` · stale lock → `.git/_orphan-locks/` · commit serially — the other Fable lane and lane RI's verifier commit in parallel · one commit each `#277 2026-09-16 — lane A2|A3: …`, `--numstat` re-read from the shipped sha · Dave's words only from the rulings file or `says` fields.
