# #283 — LANE W — the wrap's own working record

provenance: 283 · 2026-09-18
status: observed

**The filed report (`s218-D7`) is `notes/_subreports/2026-09-18-283-W-wrap.md` and is the authority.**
This file holds the two things that belong beside the working set rather than inside the filed
report: the **2c/2d/2f EXIT CHECK** and the **`s271-D4` re-read**, which is the ritual's final beat.

## EXIT CHECK — run BEFORE the three rolls, recorded rather than asserted

Every ⚠ / ⬛ / AWAITING / OPEN-CALL / DEFERRED-TO-DAVE item inside the three rolling regions was
scanned and matched to a **standing** home before the move. Dated homes do not count.

| rolling region | Dave-owed items found | standing home confirmed |
|---|---|---|
| GM ★ PRIOR banner **#281** (12 lines → `_GM-ARCHIVE.md` § Batch 2026-09-18 #283) | ⬛ ④ the 15 rule notes · the eye-check · force by grade + the ghost colour · FO's two untied artefacts (`W-281fo`) · the ASKs `col26-012` `aid-009` `type26-002` and the unlanded `type26-003` · the logo review — and ⬛ ⑥ the six gate fails in the `#243` form · resolver `FAIL(6)` · ownership 2/3 · showroom *"138 vs 108"* · `s277-D12` | `_CARRIES.md` § `residual → #284` holds every one, by probe: `#243` ×49, `showroom` ×10, `FAIL(6)` ×3, `ownership` ×5, `s277-D12` ×3, `col26-012` ×4, `aid-009` ×2, `type26-002` ×1, `type26-003` ×2, `W-281fo` ×1, `ghost` ×3, and the rule-notes and eye-check items travel **STRUCK with their #282 receipts** rather than deleted. Resolver, ownership and showroom are ALSO re-declared on this wrap's own ★ LATEST banner and ⏱ DELTA |
| LS ⏱ PRIOR DELTA **#280** (18 lines → `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-18 #283) | ⬛ the orphan census export · the eye-check of the six cells · `jade-lifestyle` · ⚠ #280's own declared-skips block | the census export travels **STRUCK with its #281 receipt** and the eye-check **STRUCK with its #282 receipt** — neither deleted; `jade-lifestyle` stands in the carry set at age `[3]` (×4 by probe). A wrap's own declared-skips block is its bookkeeping, not a Dave-owed item, and this wrap writes its own |
| GM stratum **#282** (`roll_2f`: 30 ln → `notes/_GAUGE-LOG.md` at true EOF, 3 ln → `_GM-ARCHIVE.md`) | ⬛ the declared-skips block · ⬛ `COMMIT STATE #282`'s `#243` not-a-wrap form, its declared hash gap, and the instrumentation-append policy | the `#243` form stands in the carry set (49 occurrences) and is **re-declared on this wrap's own `COMMIT STATE #283` block**, now its **NINTH** wrap; the hash gap is re-declared in the same block, verbatim in shape; the instrumentation-append policy stands as a carry and is re-declared in `COMMIT STATE #283` |

⛔ **Nothing was orphaned by these moves, and nothing was dropped for being old.** Three items that
had no line of their own in `_CARRIES.md` — the disk, the session that stopped at its own seam check,
and the 4c/step-5 collision — were MINTED at 2c(i) rather than left in a dated home.

⛔ **AND THE EXIT CHECK CAUGHT SOMETHING IT IS NOT DESIGNED TO CATCH, WHICH IS WORTH RECORDING AS THE
LIMIT IT EXPOSES.** #282's filed report asserts that the 4c/step-5 collision *"is now in
`_CARRIES.md`"*. A probe of § `residual → #283` finds **no item mentioning `gitshim` or step 4c at
all**. ★ **The check tests PRESENCE for items it can SEE — it walks the ROLLING REGION and asks
whether each item found there is homed. An item that was never written into either place is invisible
to it**, and the only reason this one surfaced is that this wrap went looking for the carry in order
to age it. [[instrument-without-a-consumer]]

## ⬛ `s271-D4` RE-READ — the ritual's FINAL beat, run at 5b and RECORDED rather than assumed

The memory hook's "open, Dave's" items (`notes/_lanes/283/WRAP-MEMORY-HOOK.md`) were re-read against
`knowledge/_rulings.json` **after every ruling of the session was inscribed**. #283 inscribed exactly
one — **`s283-D1`** — and it was inscribed by the conductor before this ritual opened.

**NOTHING IS STRUCK. ONE ITEM IS PARTIALLY ANSWERED AND SAYS SO WITHOUT BEING STRUCK. TWO NEAR MISSES
ARE NAMED RATHER THAN STRUCK:**

1. **The 40 per-size logo masters** — ⚠ **NEAR MISS, and the strongest-looking one.** `s283-D1` is the
   ruling that STOPPED them. A ruling that prevents a thing is not a ruling that closes it: the item
   is more open at the end of #283 than at its start, and striking it on a same-day ruling about the
   same lane would be the exact inversion of `s271-D4`.
2. **What a session should DO at the hard wall** — ⚠ **NEAR MISS.** `s283-D1` gives the session an
   instrument and the session obeyed it, but the ruling is **ADVISORY by its own text** and says so;
   **it does not answer what a session must do at the wall**, and arming `FILL_CEILING_BLOCKING`
   remains `s271-D2`'s and Dave's. **A data point is not an answer** — it is written into the carry
   set as a NEW item in its own right so that the question's wording never had to change.
3. **The git-lock runbook line** — ⚠ **PARTIALLY ANSWERED, NOT STRUCK.** `85aee08` closes the 4c half
   (the shim is on the hygiene gate's KEEP list, verified here). **The runbook line itself — what a
   seat should do when a gate-failed commit leaves `.git/index.lock` behind under a mount that
   refuses `unlink` — is untouched and still his.** The carry ages at `[1]` with its **wording
   unchanged**, and the closed half is recorded in the NEW carry ③ rather than by editing it, because
   `s183-D1`/`s188-D2` license a wording change ONLY to record a retraction and nothing here was
   retracted.
4. **The third dial · the theory door and the postures · `col26-012` · the four generators · the boot
   ceiling · the instrumentation appends · `s277-D12` · `jade-lifestyle` · `W-282a` `W-282b`
   `W-282ll` · `_HANDOFF-130`'s four** — untouched by `s283-D1`; all carried at their ages.
5. **The disk** — ⚠ **NOT A CARRY BEFORE TODAY IN THIS FORM.** The VM-disk item at `[55]` is about the
   sandbox being full and stands unchanged; **this session's findings are a different claim about a
   different cause** and are minted as NEW carry ① rather than folded into the old one.

⚠ **Every open item above is written as the QUESTION IT IS, not as a state of the world**, and
⛔ **a same-subject ruling is a strong match, not a proof — a strike that is wrong is worse than an
item that is merely stale** (`s271-D4`'s own warning, and the reason two near misses are named here
instead of two items being struck).
