# LANE PH — BRIEF — PRINCIPLES GET A HOME: inscribe s281-D1 (family nodes enter; inFamily + evidencedBy go on), land the edges, light the 100. INSCRIBE + WIRE.
#281 · 2026-09-17 · conductor (Fable 5.1) · **model: opus** · bash root `/sessions/nifty-exciting-cerf/mnt/UX-design/`

## Dave's word, verbatim
Decisions export 11:58Z, `notes/_lanes/281/orphan-plan/DAVE-EXPORT-DECISIONS-2026-09-17.json`, q1 = (a), no note, not overruled. The question and option text he ruled on is in `notes/_lanes/281/orphan-plan/DECISIONS-2026-09-17.html` #q1: **"Yes — families enter as nodes, and both lines go on."** Earlier, on the orphan plan (11:02Z): `ux-cloud` = ruling, `polarity-reach` = ruling. That is the whole authority.

## Read first (retrieval)
`knowledge/gen_kg_principles.py` (docstring + `--family-edges` / `--evidence-edges` / `--land --ratified`) · `s275-D2` in `knowledge/_rulings.json` (the shape of the last principles ruling — copy its fields) · `notes/_lanes/280/orphan-census/facts.json` `ux` block (145 / 100 dark / 32 families / 145 with evidence) · `notes/_subreports/2026-09-17-281-CM-chip-map.md` (explorer 1.21 is the base; `held:true` shape for obeys→ux — DO NOT touch) · `knowledge/_build_kg_explorer.py` VERSION 1.21.

## Do
1. **Inscribe `s281-D1`** in `knowledge/_rulings.json` by textual span (insert only, zero deletions, `git show --numstat` must read `N  0`): id `s281-D1`, date 2026-09-17, by Dave, `says` = the export reference + q1 (a) + the option sentence verbatim, `ruled` = research families enter the graph as `family:` nodes in the UX-principles family (Explanation view); `inFamily` (ux → family) and `evidencedBy` (ux → evidence) are RATIFIED into the closed vocabulary and switched on; storage otherwise untouched; the `s275-D2` hold on the two flags is lifted by this ruling. `governs` = the files you touch. `status` ruled.
2. **Land the edges** with the generator's own door: `--land --ratified s281-D1 --family-edges --evidence-edges` (or whatever the docstring names). Unresolvable evidence targets are declared nulls with a note (RP-2 option a), never dropped. Report the counts: family nodes minted (expect 32), inFamily edges (expect 145), evidencedBy edges + nulls.
3. **Explorer 1.22**: the `family:` node type gets a type chip inside the UX-principles family (the CM report says a type without a chip stays dark even with edges — do not repeat that). Evidence targets: say what node type they land on and whether they need a chip. VERSION note in the house style.
4. **Prove it.** Census re-run (`notes/_lanes/280/orphan-census/_census.py` or the CM lane's `facts-after.json` method): `ux` dark 100 → 0 at every-chip-on; publish the new total (expect 108 → 8). Coordinate sets: the layout WILL move (new nodes) — say so, publish before/after md5 of the six cells' coordinate sets, 1280 light screenshot of force-2D before/after via `knowledge/_render/seat_env.sh`. `_validate_kg.py` green. Page errors `[]`, served.

## Gates
Files: `knowledge/_rulings.json` (span insert only), `knowledge/_ux_principle_nodes.json` (regenerated by the generator), `knowledge/_build_kg_explorer.py`, `notes/_KG-EXPLORER.html`, `notes/_lanes/281/principles-home/`, the filed report, `_state.json` row via `_state.add()` if the doc-row gate asks. **No node id or edge type outside this ruling moves. Do not touch `obeys` or its `held` flag.** `/sessions` at 99%: download NOTHING, scratch under `/sessions/nifty-exciting-cerf/mnt/outputs/ph/`, removed at the end. Never `git stash` / `gen_kg_edges.py` / `_build_all.py`. Locks → `mv` to `.git/_orphan-locks/`, never `rm`.

## Report — `notes/_subreports/2026-09-17-281-PH-principles-home.md` (skeleton `_TEMPLATE.md`), evidence under `notes/_lanes/281/principles-home/`. Plain prose first. COUNTS · ruling-shaped · UNPROVEN · REPLAY-THESE.
Commit via `SESSION_N=281 SHOWROOM_ACK=1 bash knowledge/_git_commit.sh --reconciled <fresh msgfile> <paths…> < /dev/null`, msgfile `/tmp/_msg-281-PH-$(date +%s).txt`, subject `#281 lane PH: principles get a home — s281-D1 inscribed, N family nodes, 100 dark principles → M`. Do not push. Return ONLY a stub: verdict, ruling span numstat, counts, dark before/after, sha, report path, spend.
