# LANE TV — BRIEF — THE THEORY VIEW: inscribe s281-D4 (the 14 held citation lines are drawn in the third view) and s281-D5 (the triad is SYSTEM · GOVERNANCE · THEORY), then enact both in the explorer. INSCRIBE + WIRE.
#281 · 2026-09-17 · conductor (Fable 5.1) · **model: opus** · bash root `/sessions/nifty-exciting-cerf/mnt/UX-design/` · **RUNS AFTER lane FO has committed — rebuild on HEAD.**

## Dave's word, verbatim
Decisions export 11:58Z (`notes/_lanes/281/orphan-plan/DAVE-EXPORT-DECISIONS-2026-09-17.json`): q3 = (a) **"The Explanation view — a 'cited by a design' chip beside the polarities"** with a long note asking for a chat; q5 = (a) **"Rename it 'UX Theory'…"** with note *"What is the triad of verbs, i like neatness so maybe we can think of something else"*. The chat put three triads; he took **option 1: System · Governance · Theory — three one-word nouns, verb line under each (choose · obey · consult)** — *"Okay these all look good to me, thanks for the explanation"*. Full words in `notes/_lanes/281/DAVE-RULINGS-2026-09-17.md`. The chat's terms on q3: the 14 lines are the hand-authored SEED of applicability; whether force follows grade is a LATER ruling once rule→principle lines exist (lane RO) — do not draw force by grade.

## Read first (retrieval)
`notes/_subreports/2026-09-17-281-CM-chip-map.md` (how the 14 `obeys` component→`ux:` edges are held — `held:true`, said in the legend and the ↔ door) · `notes/_subreports/2026-09-17-281-PH-principles-home.md` (1.22, the template file is where chips live) · `s277-D8` in `_rulings.json` (the three views; Design governance FIXED at three provenances — this ruling does not add a fourth; "force is the edge's not the node's") · `knowledge/_build_kg_explorer.py` VIEWS / VIEW_OF_FAM and `knowledge/_kg_explorer.template.html` (labels, the strata band labels, the legend's view boxes).

## Do
1. **Inscribe `s281-D4`** (span insert, `N  0`): the 14 authored `obeys` component→ux edges are DRAWN in the third view under their own chip, label **"cited by a design"**, beside the polarities; they are explanation, not obligation — Design governance stays at three provenances; the designer's own `$why` sentence rides the edge and reads in INSPECT; force by grade is NOT ruled here. `governs` = builder + template.
2. **Inscribe `s281-D5`** (span insert): the three views are labelled **SYSTEM · GOVERNANCE · THEORY**, each with its verb line — *what exists · the agent chooses here* / *what a design must or should do · the agent obeys here* / *why · the agent consults here*; the Constitution keeps its name and is still not a view; **storage untouched — no fam KEY, node id or edge type moves; labels only** (the `s277-D8` clause). `governs` = builder + template.
3. **Enact both. Explorer 1.24** (or whatever HEAD+1 is): the `held` flag on the 14 clears; a "cited by a design" chip in the Theory box, ON by default? — NO: default OFF like the rest of the third view's chips, so the default canvas does not move; labels renamed in the chip bar, the legend's view boxes, the strata band labels, the phone sheet, and the `?layout=strata` band tints' left-edge labels. Grep the template for every "Explanation" / "Design governance" / "DESIGN GOVERNANCE" / "EXPLANATION" and list each occurrence and what you did with it. VERSION note in the house style.
4. **Prove it.** Default canvas md5 unchanged (chip off by default — if the label rename repaints a pixel, say which and publish both md5s). With the new chip on: 14 lines drawn, 4 principle dots gain degree; census `obeys-ux` set 4 → 0 at every-chip-on. Coordinate sets byte-identical (no node added). Six 1280 light shots (force-2D default · new chip on · strata-2D showing the three renamed bands · 390 phone sheet · INSPECT on one of the 14 edges showing the `$why` · legend) via `knowledge/_render/seat_env.sh`; page errors `[]`, served.

## Gates
Files: `knowledge/_rulings.json` (span insert), `knowledge/_build_kg_explorer.py`, `knowledge/_kg_explorer.template.html`, `notes/_KG-EXPLORER.html`, `notes/_lanes/281/theory-view/`, the filed report, `_state.json` row if asked. **No fam key, node id or edge type moves. No fourth provenance in Governance. No force by grade.** `/sessions` 99%: scratch under `/sessions/nifty-exciting-cerf/mnt/outputs/tv/`, removed. Never `git stash` / `gen_kg_edges.py` / `_build_all.py`. Locks → `mv` to `.git/_orphan-locks/`. Lane RO may insert a span in `_rulings.json` in parallel — re-read before commit; mid-commit tree → wait, never force.

## Report — `notes/_subreports/2026-09-17-281-TV-theory-view.md`, evidence under `notes/_lanes/281/theory-view/`. Plain prose first. COUNTS · ruling-shaped · UNPROVEN · REPLAY-THESE.
Commit via `SESSION_N=281 SHOWROOM_ACK=1 bash knowledge/_git_commit.sh --reconciled <fresh msgfile> <paths…> < /dev/null`, msgfile `/tmp/_msg-281-TV-$(date +%s).txt`, subject `#281 lane TV: the Theory view — s281-D4 + s281-D5 inscribed, 14 citation lines drawn, the triad renamed`. Do not push. Return ONLY a stub.
