# LANE RO — BRIEF — RESTS ON: inscribe s281-D3 (a rule → principle line is AUTHORED, starting with the 59 blocking rules) and build the 59-card page for Dave's export. INSCRIBE + PROPOSE. Do NOT land any edge.
#281 · 2026-09-17 · conductor (Fable 5.1) · **model: opus** · bash root `/sessions/nifty-exciting-cerf/mnt/UX-design/`

## Dave's word, verbatim
Decisions export 11:58Z (`notes/_lanes/281/orphan-plan/DAVE-EXPORT-DECISIONS-2026-09-17.json`) q4 = (a): **"Yes — start with the fifty-nine blocking rules; bring a page for my export"**, note *"I think this feels right, but maybe we need to talk it through"*; after the chat: *"Okay these all look good to me, thanks for the explanation"* (`notes/_lanes/281/DAVE-RULINGS-2026-09-17.md`). The chat's terms he agreed to: one rule, one proposed principle, one sentence, the grade; a rule that rests on convention alone says so — a declared null with a note is a legal answer.

## Read first (retrieval)
`knowledge/_rule_nodes.json` (470 `rule:` nodes; `destiny` BLOCKING = 59; each carries `text`, `file`, `ruleId`) · `knowledge/_ux_principle_nodes.json` (145 `ux:` nodes with `statement`, `originator`, `grade` A 6 · B 28 · C 75 · D 9 · L 27, `scope_conditions`, and now `inFamily` after lane PH `b95042c`) · `notes/_lanes/277/kg-audit/A2-AUDIT.md` Q3 row (the `restsOn` proposal) · `knowledge/_kg_verbs.json` (a verb per edge type — `restsOn` needs one; propose it in the report, do not add it) · `s275-D2` in `_rulings.json` for the ruling shape · `notes/_lanes/281/orphan-plan/DECISIONS-2026-09-17.html` for the page idiom (two registers: plain prose first, technical in a fold; options with For/Against; recommended tag; export flags overrules).

## Do
1. **Inscribe `s281-D3`** by textual span (`N  0` numstat): id, date 2026-09-17, by Dave, `says` = export ref + q4 (a) + option sentence + his two sentences verbatim, `ruled` = a new AUTHORED edge type `restsOn` (rule → ux, carrying a `$why` sentence) enters the closed vocabulary; NEVER parsed from prose; first wave = the 59 BLOCKING rules; a rule resting on convention alone is `restsOn → ref:null` with a note; the lines LAND only on Dave's export of the proposal page. `governs` = the files a landing lane would touch (name them). `status` ruled.
2. **Propose 59 links.** For each blocking rule: the principle it most plausibly rests on (prefer the six A-grade laws where honest; any grade is allowed; `null — convention` where nothing fits), ONE sentence of why in plain English, the principle's grade. Ground every proposal in the rule's own `text` and the principle's `statement` — quote the clause of each that makes the link in the card's technical fold. Where two principles compete, offer both as options. Write the proposals to `notes/_lanes/281/rests-on/proposals.json` (`{ruleId, file, ruleText, options:[{uxId, grade, why, recommended}], null:{why} }`).
3. **The page** — `notes/_lanes/281/rests-on/RESTS-ON-2026-09-17.html`, swiss-design-system idiom (`/sessions/nifty-exciting-cerf/mnt/.claude/skills/swiss-design-system/SKILL.md`), single file, no browser storage. Header: what this is in three sentences, counts (59 rules · how many with a proposed principle · how many convention · grade spread). One card per rule, grouped by guideline file: the rule in words, the proposed principle's statement and grade in words, the one sentence, then radios — (a) the recommended principle · (b) the alternative if one · (c) convention — no principle · (d) another principle (text) — and a note. "Take all the recommendations" button; export envelope `{exportedAt, page, answers:{<ruleId>:{choice, uxId, note, recommended, overruled}}}`. Plain prose in the body, no ruling ids or node ids as the reading surface (ids in the fold). Verify at 1280 and 390: card count 59, radios 59×n, export round-trip, page errors `[]` via `knowledge/_render/seat_env.sh`.
4. **Do NOT** add `restsOn` to `_kg_verbs.json`, the builder or the explorer; do not land an edge. The landing is a later lane on his export.

## Gates
Files: `knowledge/_rulings.json` (span insert only), `notes/_lanes/281/rests-on/`, the filed report, `_state.json` row via `_state.add()` (close = Dave's export received). Nothing else. `/sessions` 99%: scratch under `/sessions/nifty-exciting-cerf/mnt/outputs/ro/`, removed. Never `git stash` / `gen_kg_edges.py` / `_build_all.py`. Locks → `mv` to `.git/_orphan-locks/`. Lanes FO and TV may commit to `_rulings.json` in parallel — insert your span at the END, re-read the file immediately before your commit, and if the tree is mid-commit wait and retry, never force.

## Report — `notes/_subreports/2026-09-17-281-RO-rests-on.md`, evidence beside it under `notes/_lanes/281/rests-on/`. Plain prose first: how many of the 59 rest on a principle, how many on convention, which principles carry the most rules, the three links you are least sure of. COUNTS · ruling-shaped · UNPROVEN · REPLAY-THESE.
Commit via `SESSION_N=281 SHOWROOM_ACK=1 bash knowledge/_git_commit.sh --reconciled <fresh msgfile> <paths…> < /dev/null`, msgfile `/tmp/_msg-281-RO-$(date +%s).txt`, subject `#281 lane RO: rests on — s281-D3 inscribed, 59 blocking rules proposed against principles, N convention`. Do not push. Return ONLY a stub: verdict, span numstat, the counts, sha, page path, report path, spend.
