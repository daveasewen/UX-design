# LANE CM — BRIEF — the CHIP MAP: six authored edge types no chip can draw get a family. WIRE, do not rule.
#281 · 2026-09-17 · conductor (Fable 5.1) · **model: opus** · bash root `/sessions/nifty-exciting-cerf/mnt/UX-design/`

## Dave's word, verbatim
His export `notes/_lanes/281/orphan-plan/DAVE-EXPORT-2026-09-17.json`, 11:02Z: **wire** on `homeless-edges`, `shapes`, `intents`, `roles`. That is the whole authority for this lane. `obeys-ux` is **ruling** — the 14 obeys→ux lines are NOT drawn by this lane (see Gates).

## Read first (retrieval, not a reading list)
`notes/_lanes/281/orphan-plan/ORPHAN-PLAN-2026-09-17.html` blocks 03–06 · `notes/_lanes/280/orphan-census/REPORT.md` + `facts.json` (`homeless`: answersIntent 28 · hasDataShape 26 · providesRole 108 · obeys 168 = 154→rule + 14→ux · yieldsTo 45 = 43 + 2 declared nulls · defaultActive 1) · `knowledge/_build_kg_explorer.py` (VERSION 1.20; `link(s,t,ty,fam)`, the chip families, VIEW_OF_FAM, the fam→view map the page carries as VIEWS) · `knowledge/_kg_verbs.json` (a verb per edge type — check the six have one; s277-D11) · `knowledge/_serve_explorer.py` (the page is SERVED).

## Do
1. **Find where the six types fall through.** Say, from the code, why `answersIntent`, `hasDataShape`, `providesRole`, `yieldsTo`, `defaultActive` and `obeys` reach storage but no chip family — one sentence each with the line.
2. **Give them a family.** The five System-view types (`answersIntent`, `hasDataShape`, `providesRole`, `yieldsTo`, `defaultActive`) join the base wiring chip they already sit beside. `obeys` **component→rule (154 lines)** joins the HSBC `rule:` sub-chip — it is the rule family's line, and that chip loads off, so the default canvas does not change for it. `obeys` **component→ux (14 lines)** is held: emit it into the data with the family key the ruling will name left NULL (or a `held: true` flag — your call, say which) and draw NOTHING for it; the page must say in the legend or the inspect modal that 14 lines are held pending a ruling, not silently drop them. `yieldsTo`'s 2 declared nulls are carried, never drawn (the s270-D2 shape).
3. **Explorer 1.21.** Bump VERSION with a one-paragraph note in the house style. The header's edge-type count moves with the new families. INSPECT's edge door (↔) must read the six with their verbs.
4. **Prove it.** (a) The census re-run: `python3 notes/_lanes/280/orphan-census/_census.py` (or its equivalent against the rebuilt page) — the 23 shape, 14 intent and 12 role dots must read degree > 0 at "every chip on"; report the new table beside the old (157 → N dark). (b) **Every baked coordinate set is byte-identical** — `x,y,x3,y3,z3,y2,xf,yf,zf,xo,yo,xs,ys,zs` and `plates/rings/shells/bands` — diff them before and after. If adding edges moves the build-time force layout, DO NOT ship a moved layout: add the edges after layout (or exclude the new types from the layout pass) and say so. (c) Default-canvas md5: it WILL change (the base wiring chip is on by default and gains lines) — publish before and after, and a 1280 light screenshot of each via `knowledge/_render/seat_env.sh`; no other cell of the six-cell matrix is touched. (d) Page errors `[]`, never-driven, served via `_serve_explorer.py`.

## Gates
Nothing outside `knowledge/_build_kg_explorer.py`, `notes/_KG-EXPLORER.html` (rebuilt), `knowledge/_kg_verbs.json` (only if a verb is missing), `notes/_lanes/281/chip-map/` and the filed report changes. **No node id, edge type, file or fam KEY moves. No edge is invented; no obeys→ux line is drawn.** `/sessions` at 99%: download NOTHING, never copy `knowledge/`, scratch under `/sessions/nifty-exciting-cerf/mnt/outputs/cm/`, removed at the end. Never `git stash` / `gen_kg_edges.py` / `_build_all.py`.

## Report — `notes/_subreports/2026-09-17-281-CM-chip-map.md` (skeleton `notes/_subreports/_TEMPLATE.md`), evidence under `notes/_lanes/281/chip-map/`. Plain prose first: what was dark, what lights now, the count table, what is held and why. COUNTS · ruling-shaped · UNPROVEN · REPLAY-THESE lines per the template.
Commit via `SESSION_N=281 SHOWROOM_ACK=1 bash knowledge/_git_commit.sh --reconciled <fresh msgfile> <paths…> < /dev/null`, msgfile `/tmp/_msg-281-CM-$(date +%s).txt`, bare subject `#281 lane CM: the chip map — six edge types get a family, N of 157 dark dots light`. Do not push. Return ONLY a stub: verdict, the before/after dark count, coordinate-diff result, sha, report path, token spend.
