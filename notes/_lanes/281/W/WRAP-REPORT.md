# #281 — LANE W — the wrap's own working record

provenance: 281 · 2026-09-17
status: observed

**The filed report (`s218-D7`) is `notes/_subreports/2026-09-17-281-W-wrap.md` and is the authority.**
This file holds the two things that belong beside the working set rather than inside the filed
report: the **2c/2d/2f EXIT CHECK** and the **`s271-D4` re-read**, which is the ritual's final beat.

## EXIT CHECK — run BEFORE the three rolls, recorded rather than asserted

Every ⚠ / ⬛ / AWAITING / OPEN-CALL / DEFERRED-TO-DAVE item inside the three rolling regions was
scanned and matched to a **standing** home before the move. Dated homes do not count.

| rolling region | Dave-owed items found | standing home confirmed |
|---|---|---|
| GM ★ PRIOR banner **#279** (12 lines → `_GM-ARCHIVE.md` § Batch 2026-09-17 #281) | ⬛ ④ the 15-base export · the `designrulings` sub-chip · a 13th verb + `verbVia` · Spider `v1.0.14` — and ⬛ ⑥ showroom *"108 stale"* · resolver FAIL(6) · lane-ownership 2/3 · `s277-D12` | `_CARRIES.md` § `residual → #282` holds every one: the 15-base export **travels STRUCK with its #280 receipt** (never deleted), `designrulings` (1), `v1.0.14` (2), `verbVia` (2), `s277-D12` (3), showroom (10 occurrences), `FAIL(6)` (3), `lane_ownership` (5). Resolver, ownership and showroom are ALSO re-declared on this wrap's own ★ LATEST banner and ⏱ DELTA |
| LS ⏱ PRIOR DELTA **#278** (18 lines → `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-17 #281) | **none** — the region was scanned line by line and carries no ⚠/⬛/AWAITING/OPEN-CALL item of its own | n/a — recorded as a scanned-and-empty region rather than left unstated, because a silent pass cannot be told from a skipped one |
| GM stratum **#280** (`roll_2f`: 26 lines → `notes/_GAUGE-LOG.md` at true EOF, 3 lines → `_GM-ARCHIVE.md`) | ⬛ the declared-skips block · ⬛ `COMMIT STATE #280`'s `#243` not-a-wrap form and its declared hash gap | the `#243` form stands in the carry set (49 occurrences) and is **re-declared on this wrap's own `COMMIT STATE #281` block**, now its **seventh** wrap; the hash gap is re-declared in the same block, verbatim in shape |

⛔ **Nothing was orphaned by these moves, and nothing was dropped for being old.** Seven items that
had been living only in this session's **filed lane reports** — CM's obligation-on-a-`ux:`-principle
question, force by grade, the ghost layer's colour, FO's two untied artefacts and the repo-tool
family, the three ASKs and `type26-003`, `gen_kg_rules.py`'s missing preserve-block with the two
unread edge types, and the sandbox's `unlink` refusal — were found to have **no line of their own**
in `_CARRIES.md` and were MINTED there at 2c(i) rather than left in a dated home. That is the EXIT
CHECK doing the job it exists for.

## ⬛ `s271-D4` RE-READ — the ritual's FINAL beat, run at 5b and RECORDED rather than assumed

The memory hook's "open, Dave's" items (`notes/_lanes/281/WRAP-MEMORY-HOOK.md`) were re-read against
`knowledge/_rulings.json` **after every ruling of the session was inscribed**. #281 inscribed exactly
six — **`s281-D1`…`s281-D6`** — all by the lanes that built them, all before this ritual opened.

**NOTHING IS STRUCK, AND THREE NEAR MISSES ARE NAMED RATHER THAN LEFT TO BE NOTICED:**

1. **The 15 rule notes** — ⚠ **NEAR MISS.** `s281-D6` is the ruling that *produced* this item
   (*a rule NOTE is not a rule EDIT*), so a same-day, same-subject match is as strong as it gets.
   **It does not close it.** The ruling decided where the notes GO; what is open is whether any of
   them is inscribed, and that is fifteen separate answers he has not given.
2. **Force by grade** — ⚠ **NEAR MISS, AND THE RULING SAYS SO ITSELF.** `s281-D4` is *about* force
   and grade and **explicitly declines to rule it**, naming the `restsOn` export as its
   precondition. The export landed the same day, which makes the question **more** live, not less.
3. **Whether the Explanation VIEW is renamed** (his *"maybe something like 'UX Theory'"*) —
   ⚠ **NEAR MISS.** `s281-D5` renamed **the triad** to SYSTEM · GOVERNANCE · THEORY and touches the
   same word. It says nothing about the view's own label. A same-word match is not a same-subject
   match [[premise-ages-faster-than-rule]].
4. **The eye-check of the six matrix cells** — untouched by any `s281` ruling; carried from #131.
5. **The ghost layer's colour** · **FO's two untied governance artefacts and the repo-tool family** ·
   **the three ASKs and `type26-003`** · **`gen_kg_rules.py`'s preserve-block and the two unread
   edge types** — each was *raised by* a lane under one of these rulings and closed by none of them.
6. **The sandbox's `unlink` refusal and the `.git/index.lock` a gate-failed commit can leave** — an
   observation, not a ruling subject; no `s281` ruling speaks to it.
7. **The LOGO REVIEW** — he asked for it by name again today; nothing this session ruled touches it.
8. **The wrap-sub memory seat limit** — measured false a third time here; no ruling exists, because
   amending a ratified runbook step is Dave's.
9. **`s277-D12`**, **`jade-lifestyle`**, the **shells-3D cutaway**, the **sandbox snippet scripts**
   and **`_HANDOFF-130`'s four** — untouched by this session.

⚠ **Every open item above is written as the QUESTION IT IS, not as a state of the world**, and
⛔ **a same-subject ruling is a strong match, not a proof — a strike that is wrong is worse than an
item that is merely stale** (`s271-D4`'s own warning, and the reason three near misses are named
here instead of three items being struck).
