— msgfile head: the #300 wrap - the boot was measured, and the reading stays
— msgfile line 1 carries no T3 prefix (#208 reuse gate passed)
  ✗ _CHAIN.md is STALE — it does not match GOOD-MORNING.md / _LIVE-STATE.md as they now stand, so a cold session would read a PREVIOUS session's record as if it were current. Run `python3 knowledge/_gen_chain.py` and stage the result.
✗ _gen_chain.py --check REFUSED (exit non-zero) — its message is printed directly above and it is the authority on the cause; this script does not second-guess it. Nothing has been staged. If it named STALENESS, run: python3 knowledge/_gen_chain.py — then re-run this script. If it named a DEGRADED MEASUREMENT, regenerating will NOT help: fix tiktoken first (pip install tiktoken --break-system-packages) and re-run.
