# #300 — THE BOOT IS HUGE · shared brief for lanes A B C D, the adversary X and the synthesis seat P

provenance: 300 · 2026-09-23 · written by the conductor (Opus 5.5, cloud)

## Dave, 15:58 BST, verbatim
> 184K!!!, this is getting insane, need you to really think hard about how we get this down, the boot is huge.
> Can you think hard about this, what can we offload, what can split up, what might we delete, I want you to really thing laterally and orthogonally to come up with a careful plan for this.
> Cloud seems to be the source of the current bloat , do some research, hatch a plan, then lets do some quick wins before Friday.
> I really want you to explore some innovative ideas, are we loading too much upfront can we simply index everything the hand of might need and load on demand. fan some subs and use this space efficiently, use the adversarial technique to challenge the subs, test trace and find all the extarnalities and dependancies

## What is measured already (conductor, #300)
- The conductor is a claude.ai CLOUD session linked to Dave's Mac. Its transcript is in this container: `/root/.claude/projects/-home-claude/d75f51cc-494a-56b1-9272-190e7fc46cbb.jsonl`. Real fill per assistant message = `input_tokens + cache_creation_input_tokens + cache_read_input_tokens`, per distinct `message.id`.
- Boot (message 1) = **126,185 real**. Cowork desktop seats (#296 and before) booted ~72–74K (`BOOT_CEILING_TK` 72,768 was ruled for that seat). Cloud #297–#299: 127,600 · 127,661 · 126,178.
- ★ The transcript holds the EXACT boot payload: an `attachment` of type `prompt_snapshot` carrying `systemPrompt` (103,597 chars as JSON) and `tools` (284,966 chars as JSON — every tool's name, description and input schema). Other attachments: `cowork_memory_context` (the Project memory snapshot — profile, preferences, a 150-line file listing — 14,680 cl100k at boot), `skill_listing` (3,112), `deferred_tools_delta` (2,601), `mcp_instructions_delta`, `agent_listing_delta`, `session_context` (the Project instructions should be in here — search for "On the first turn of every session"), `environment`, `remote_session_change`.
- The opener read `_HANDOFF-150`, `_CHAIN.md`, memory `index.md`, a 20 KB memory archive shard and the #299 hook, then wrote five times to Project memory to place the hook: fill 126,185 → 157,635 (reads) → ~181,800 (placement) → 184,309.
- ★ THE RE-SEND: because the store had changed, the NEXT user turn carried the whole `cowork_memory_context` again (14,685 cl100k): Dave's one-paragraph message moved the fill **187,863 → 211,454 (+23,591)**. #298 saw the same shape (+25,021).
- The conductor was at ~212K of a 256K quality-max when this brief was written. It must stay thin — that is why you exist.

## Standing rules for every seat
1. File your FULL report at `notes/_subreports/2026-09-23-300-<LANE>-<slug>.md`; work files under `notes/_lanes/300/<LANE>/`. Return to chat ONLY a STUB of at most 150 words: verdict · top three numbers · report path · your spend · REPLAY-THESE (commands that reproduce your top numbers).
2. The repo is on Dave's Mac: use `mcp__remote-devices__device_bash`, repo root `$HOME/mnt/Projects--UX-design`. Write files with python or a QUOTED heredoc (`<<'EOF'`) — unquoted heredocs eat backticks.
3. READ-ONLY on the repo apart from your own report and work files. ⛔ NEVER run `git status` (it strands `.git/index.lock` on this mount). No commits, no pushes.
4. ⛔ DO NOT WRITE TO PROJECT MEMORY (no memory write / append / str_replace / delete). Any store change re-sends ~23K into the conductor's next turn. `memory_list` and `memory_read` are fine.
5. Never pull a whole transcript or a large file into your own context — process with python + tiktoken (`pip install tiktoken --break-system-packages`) and print aggregates only.
6. Units: real tokens come only from usage fields; cl100k counts are ESTIMATES ("tape"). When you scale tape to real, state the ratio and how you derived it.
7. Every claim carries a receipt (file:line, command, URL). Label inference as inference. A product fact from the web needs its URL and the date read; if you cannot source it, write UNSOURCED.
8. Your report opens with a short section in plain words for Dave (a designer who reads by eye): the answer first, no ID codes.
9. Spend: hand-sum your own transcript (look under `/root/.claude/projects/-home-claude/d75f51cc-494a-56b1-9272-190e7fc46cbb/` for `subagents/*.jsonl`); if you cannot find it, write "unmeasured, declared".

## The frame — four bands
1. **HARNESS** — what the cloud seat loads before anyone speaks (system prompt, tool schemas, injected reminders).
2. **OPENER** — what our own procedure adds (the Project instructions → reads and memory writes).
3. **RUNNING** — what grows in-session (re-sends, tool outputs, sub stubs, replies).
4. **SEAT** — the orthogonal lever: run the conductor somewhere else.
Think laterally: offload, split, index-and-load-on-demand, delete, change the seat, change the timing. Dave asked for innovation — include ideas you cannot yet prove, labelled as such.

## Lanes (wave 1: A B C D in parallel · wave 2: X · then P)
**A — ATTRIBUTE THE WINDOW.** From the transcript: (1) itemise the boot — the system prompt by top-level section, every tool by name grouped by family (core Claude Code tools · Artifact* · Projects · mcp__widgets__* · mcp__claude_ai__* · mcp__claude-code-remote__* · mcp__memory__* · mcp__remote-devices__* · the rest), and every attachment — cl100k per item and a scaled-real figure, reconciled to 126,185 with the residual stated. (2) Trace the opener and the session message by message: what each step added (reads, writes, the re-send). (3) Rank the top 25 items by cost with a column "brought in by" (feature / connector / setting / our own instruction), inference labelled. (4) If the repo attributes the old 72K Cowork boot (`notes/_lanes/298/DAVE-WORDS-AND-BOOT-FINDING-2026-09-23.md`, `knowledge/_gauge_tokens.py`; memory `areas/boot-three-bands-293.md`), line the two seats up: what the cloud adds.

**B — LEVERS AND SEATS (web research).** For each tool family and injected block (read the tool names from the `prompt_snapshot` yourself; do not wait for A), find what switches it off or shrinks it: a per-chat composer toggle, a claude.ai account setting, a desktop-app setting, a Project setting, a connector, or nothing (fixed harness). Especially: Project memory / "Generate memory from chats" (global or per-project?), Artifacts, Claude in Chrome, computer use, the built-in browser, web search, Research, chat search, the widgets family, scheduled tasks, skills toggles, connectors. For each: the saving (from the tool sizes), what Apollo loses, and what Dave's OTHER work loses (externality — his client work runs on the same account). Then the SEAT band: can the conductor run as Claude Code on the Mac (desktop app or CLI) inside the repo — its boot, what carries over (skills, dave-voice, the render env, git, `_checkin.py` transcripts), what breaks (Project memory, the claude.ai UI he reads in)? Is a local (non-cloud) Cowork mode still offered? Could the repo live in the cloud container (GitHub clone) and the computer link be dropped? Sources: support.claude.com, docs.claude.com, code.claude.com, anthropic.com.

**C — OUR OPENER, ITS DEPENDENCIES, THE LEAN DESIGN.** Trace what the opener does and everything that depends on it: the Project instructions (in the transcript), the wrap runbook steps that write the memory hook / cut the index / open archive shards, every script or gate that reads or checks `_CHAIN.md`, `_HANDOFF-*`, `GOOD-MORNING.md`, memory `index.md` or `WRAP-MEMORY-HOOK.md`, `_checkin.py`'s transcript discovery (it finds nothing for a cloud conductor), `_gauge_tokens.py` constants (`BOOT_FIRSTTURN_TK`, `BOOT_CEILING_TK`), the capture gate's boot-ceiling arm. Then design the lean opener: (i) a BOOT CARD generated at wrap (≤1,500 real tokens: session number, chat title, the owed questions in words, the first beat, retrieval commands) — its generator and its gate; (ii) handoff and chain loaded on demand by section through `knowledge/_memento_search.py`; (iii) the memory hook placed at the WRAP seat (no opener write, no re-send) or the memory ritual retired; (iv) `_checkin.py` / `ensure_env.sh` reduced to one line each. Deliver: a dependency map (what breaks or must change for each move), the minimal change set, a draft of new Project instructions for Dave to paste, and the opener's cost before/after. Name every externality.

**D — THE MEMORY STORE.** Census Project memory with `memory_list` (all pages): every file, bytes, source tag; the listing line's format and per-line cost; how many ACCOUNT-level files exist outside `/projects/…/` (they would fill the 150-line listing if project files were cut — test this premise); which project files have a repo twin (search the repo by filename and by a distinctive sentence — #278 moved the Cowork memory directory into the store: was it also committed?); what the snapshot would cost with the store cut to ~5 files; the export-then-delete method and its cost; whether retiring memory for Apollo entirely beats shrinking it. Read-only.

**X — ADVERSARY (after A–D).** Read the four reports. For each lane's top claims: re-measure independently (your own script, not theirs), re-check sources, grep for dependencies they missed, and hunt externalities — to Dave's other work, the wrap ritual, the gates and CI, Friday's deck. Challenge this brief's own premises too ("cloud is the source"; "store writes trigger the re-send"; "prompt_snapshot equals what is billed"). Verdict per claim: HOLDS / WEAKENED / FALSE, with receipts.

**P — SYNTHESIS (after X).** The plan as an HTML page `notes/_PLAN-300-boot-diet-2026-09-23-v1.html`, built with the swiss-design-system skill: the four bands; every lever ranked by saving × risk × reversibility; owner (Dave's switch / a lane's build / a ruling); externalities beside each; and a short BEFORE FRIDAY set — reversible quick wins that cannot touch the deck. Render at the seat (`export TMPDIR=/dev/shm; bash knowledge/_render/ensure_env.sh; source knowledge/_render/seat_env.sh`, `executable_path=$RENDER_SHELL`, `goto("file://…")`, never `set_content`) and check it by eye.

## ADDENDUM — Dave, mid-run, verbatim (binds X and P)
> 16:08 — we have to balance everything against the context that the agents need, that was the reason for memento in the first place, the principles we followed to create it probably still stand but Ill welcome any challenges.
> 16:56 — can we employ an N-gram anywhere as the look-up, could Jev help in any way or how we access the KG?

Wave-1 stubs (reports on disk): A `notes/_subreports/2026-09-23-300-A-attribute-the-window.md` · B `…-300-B-levers-and-seats.md` · C `…-300-C-lean-opener-and-dependencies.md` · D `…-300-D-memory-store.md`.
Conductor note: lane A measured the conductor's OWN retained reasoning and replies at 22% of its fill — the conductor is at or past 256K; it will not read full reports. X and P carry the weight.

**X, in addition:** test every cut against Dave's 16:08 principle — for each lever, what context does a cold seat LOSE, and would it then make a confident false claim (the Memento failure)? Name the drill that proves a lean opener is still sufficient (cold seat, boot card only, N questions the full chain answers). Also test his 16:56 ideas: the existing n-gram doors (`knowledge/_quote_gate.py`, `_near_dupes.py`, #269), `knowledge/_memento_search.py`, the KG and its explorer, and Jev (`knowledge/_jev.py` if present; memory `areas/jev-integration.md`; the typesafe-ai skill) — could an n-gram or BM25 index be the load-on-demand look-up, could Jev make the "which sections does this session need" judgment, what does each cost and what does each break?
**P, in addition:** run concurrently with X. Prepare from A–D while X works; then poll for X's report (`ls notes/_subreports/2026-09-23-300-X-*`, sleep ≤150 s per call) and fold its verdicts in before you finish — a lever X marks FALSE does not go on the page as a recommendation. The page gets a section on the Memento balance (context the agents need vs boot) and one on look-up mechanisms (n-gram / BM25 / KG / Jev), each with cost and risk. The BEFORE FRIDAY set must be things Dave can do in minutes, reversible, and that cannot touch the deck.
