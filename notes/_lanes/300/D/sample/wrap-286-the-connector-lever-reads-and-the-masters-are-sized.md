---
name: "wrap-286-the-connector-lever-reads-and-the-masters-are-sized"
description: "Apollo #286 wrap (2026-09-18): the Browser-connector block read as boot cold 73,832 v 80,863 (a direction, not a cause), the 40 logo masters registered as a `sizes` map on the 8 existing nodes on Dave's 'okay size-on-the-existing-node', _standing.md out of DRAFT on 'okay go on everything', the seam's INSEAT arm and --quiet built, the 256,000 wording fixed in the gauge with 58 prose locations owed. NO ruling inscribed (620)."
sources: [cowork]
---

**#286 (2026-09-18) — THE CONNECTOR LEVER READS AND THE MASTERS ARE SIZED.** Conductor Fable 5.1, ONE DAY, no date split, **NINE Opus lanes (S · R · G · T · V · C · P · R2 · C2), every one an `Agent` at depth 1, none in seat**, with a delegated Opus 5 wrap sub.

⛔ **NO RULING INSCRIBED — `knowledge/_rulings.json` STAYS AT 620**, verified by `json.load` with no `s286-` id in the store.

★★★ **THE CONNECTOR LEVER READ, AND IT IS A READING AND NOT A VERDICT.** Boot COLD **73,832 real (n=1)** against #285's **80,863** — **delta −7,031** — with **ONE variable changed**: Dave set the built-in Browser connector's 17 `mcp__Claude_Browser__*` tools to BLOCKED. Skills, computer use and the other three connectors held constant by design. ⛔ **One reading against one reading cannot separate the lever from the spread** — the last seven post-diet readings span 72,110–83,636, a spread of 11,526, **wider than the delta** — so the direction is consistent with the block having worked and **the magnitude is not established**; a SECOND cold boot on the same setup is what would make it a verdict. ⛔ **73,832 is the ELEVENTH post-diet reading over `BOOT_CEILING_TK` 70,000 and the literal is SHRINK-ONLY: cut the boot, never raise the number.**

★★★ **THE 40 MASTERS ARE REGISTERED AND `W-285lm` CLOSES.** His two sentences today, verbatim: ***"okay go on everything"*** (his whole reply to an opener naming the standing wording, the masters' registration and the 256,000 fix, plus the cold-boot question) and ***"okay size-on-the-existing-node"*** (the SHAPE). A master is a **size field on its lockup's existing node, never its own node**; lane R2 wrote a `sizes` map onto all **8** nodes, five raw heights each (24/28/32/36/40), merge-on-write. Verified at the wrap seat by `json.load`: **8 nodes · 33 edges · 8 of 8 carrying `sizes` · 12 `governedBy` intact**. ⚠ **The field's KEY NAME is the lane's reading of the file's own plural vocabulary, NOT his word** — he named the location, not the spelling. ⚠ Lane R first reported the masters **not registrable** — the generator is blind to `masters/` — which is why R2 exists at all; both readings stand.

★★ **`knowledge/_standing.md` IS OUT OF DRAFT AND `W-285sc` CLOSES**, on a change that is **8 insertions / 3 deletions in ONE hunk, entirely above the `---` rule, with the eight lines BYTE-IDENTICAL** (936 B of body, compared programmatically). ⛔ **Reading *"go on everything"* as ratification of the wording AS WRITTEN is the CONDUCTOR'S reading, not Dave's sentence** — he did not say "as written", did not quote a line and did not say "ratified"; it is recorded as such so the act stays reversible by one word from him. ⚠ `knowledge/_seam.py` still calls the file a DRAFT at every seam and was not edited.

★★ **#285'S MOVES 2 AND 3 ARE BUILT:** `_seam.py` gains an `INSEAT` arm (+214/−6) counting the conductor's own in-seat tool output since the last seam and warning past `INSEAT_WARN_TK`; `_git_commit.sh` gains `--quiet` (+60/−1), answering the ~30K of commit-script output that cost #284's conductor his window. ⛔ **`INSEAT_WARN_TK = 10_000` is PICKED, not ruled, and the file says so.**

⛔ **THE 256,000 WORDING IS FIXED IN THE GAUGE WITH THE CONSTANTS UNMOVED (+25/−3, `BUDGET_HARD` still `256_000`) AND 58 PROSE LOCATIONS ACROSS 10 LIVE FILES ARE STILL OWED** — ratified runbook and ruling text, including `_RUNBOOK-context-gauge.md:85`'s *"256,000 stays the UNQUALIFIED wall"*. **Amending them is his.**

✅ **LANE V, ADVERSARIAL, 24 of 24 TESTABLE CLAIMS PASS** — and it **overwrote `knowledge/_capture_gate.py` with HEAD mid-run and restored it byte-exact**, and moved two symlink farms to `_to_delete/`. A verifier that mutates the tree it verifies is worth naming even when the restore is exact.

⚙ **FILL 162,555 real / 22 turns** measured first-hand against **161,544 / 20 declared**, Δ **1,011** — ✅ **17,445 INSIDE the 180,000 QUALITY line, the first session in five**; 256,000 clear for the fourth running. **subs 1,113,248 real (n=9)**, QUOTA never FILL; **the nine lane replies cost the conductor's window 3,399 cl100k in total** against that sub FILL, roughly 0.3%. ⛔ **His delegation sentence is still uninscribed after a second consecutive session of obedience.** ⛔ **6 inherited gate fails carried in the `#243` declared not-a-wrap form, the TWELFTH wrap running — a wrap may not repair them.** ⚠ CI reads **completed / failure** on `7ed49d37`, `09ddf155` and `d2ae9c73`, failing the same two steps (`Survey the COMMITTED tree` + `Knowledge build`) — **inherited, and it resolves the colour #285 could only read as `in_progress`.**

⛔ **THE MEMORY ARCHIVE IS AT 48,990 OF ITS 49,152 B CAP AND THE #283 INDEX LINE COULD NOT MOVE — it was NOT truncated, so `index.md` carries FOUR wrap lines this session by declaration.** What to do about the archive is Dave's, and the dream pass is its usual seat.

**Repo is the record, memory is the accelerator — UNCHANGED.**

⬛ **OPEN, DAVE'S — every one a QUESTION PUT at the wrap call (`s271-D4`), never a state of the world:** whether either of his two sentences is inscribed as a ruling · the `sizes` key name · `INSEAT_WARN_TK` 10,000 and its placement · the 58 prose "wall" locations · `_seam.py`'s stale DRAFT line · the KG explorer not rebuilt · the store's id regex refusing `W-286r2`-shaped ids · the delegation rule · a RESUME contract for the ritual · `_seam.py`'s SCRATCH arm · the memory archive's cap · the boot ceiling · the second cold boot that would make −7,031 a verdict · the two-probe-shape question on the §A digest at its eighteenth session · whether a verifier lane may mutate the tree it verifies · everything on `_HANDOFF-130`…`-136` not closed today.

Full record: `_HANDOFF-137-the-connector-lever-reads-and-the-masters-are-sized.md` · `notes/_subreports/2026-09-18-286-W-wrap.md` · `_DECISION-HISTORY/2026-09-18-286-the-connector-lever-reads-and-the-masters-are-sized.md` · his words verbatim at `notes/_lanes/286/DAVE-RULINGS-2026-09-18.md`.


