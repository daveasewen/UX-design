<!-- GENERATED at the wrap from _HANDOFF-150-his-last-changes-are-in-the-deck-and-the-ask-is-what-is-left.md + _CHAIN.md — DO NOT HAND-EDIT. Check: python3 knowledge/_gen_boot_card.py --check -->
# BOOT CARD — you are #300
**Title this chat →** `Apollo - #300: the ask on slide 15, then polish for friday`  
**Repo:** `$HOME/mnt/Projects--UX-design` (the empty `UX-design--UX-design` is not it)  
**Last session #299:** His last changes are in the deck, and the ask is what is left

## First beat
(FALLBACK — the wrap wrote no FIRST BEAT) Open on owed item 1: What goes on the ask (slide 15), and is it now first?

## Owed to Dave — 9 questions, in order (ask in plain words, never ID codes)
1. What goes on the ask (slide 15), and is it now first?
2. Now that the structure is settled, is it time for the 07/08 laptop headlines?
3. Should the drawings on 07 and 08 (and 13's graph) move to −27 too?
4. When does the demo brief come?
5. Which of the 26 review suggestions he never answered, if any, before Friday?
6. The boot ceiling: cut the boot, raise `BOOT_CEILING_TK` (72,768 — his word alone), or keep the DECLARED not-a-wrap path?
7. "The ask" is a chapter on the rail but not in the order of play on 02 — does his #296 word still stand?
8. After Friday: his "careful plan" for where the memory hook is placed and what it costs at the opener (this opener's store writes went through a sub), the …
9. Everything still standing in `_CARRIES.md` § `residual → #300`, each at its true age.

## Before you touch anything
- NEVER RUN `git status`, and know that `python3 knowledge/_capture_gate.py --wrap` strands `.git/index.lock` on this mount — …
- A `--wrap` commit is refused by the wrap gate's boot-ceiling arm until the boot is cut …
- THE CONDUCTOR'S TRANSCRIPT IS IN THE CLOUD.
- THE PLAIN DECK IS GENERATED — edit the source, rebuild with `build_c.py`, never `build_e.py`; the rail …

## Since the wrap (post-wrap addendum)
- THE WRAP COMMIT IS `f8321215` AND IT WENT ON THE DECLARED NOT-A-WRAP PATH, NOT `--wrap`.
- The gate stranded `.git/index.lock` at open AND before the commit — moved as `index.lock.299-W-1` and `index.lock.299-W-3`; …
- THE LATEST BANNER, by the gate's own arm (`_gm_usage.py`): LATEST 663 tape against `s241-D2`'s 1,200 / …

## Fetch on demand — never at boot
- handoff section: `python3 knowledge/_gen_boot_card.py --section <HEADING>` · map (tape): top 455 · READ FIRST IN THIS ORD 324 · HIS WORDS  VERBATIM BS 530 · WHAT LANDED 670 · THE STRIKES  BY ADDITI 224 · OWED TO 300 IN ORDER   611 · THE PLAIN DECK  HOW TO 304 · THINGS A COLD SEAT SHO 699 · POSTWRAP ADDENDUM 5b   1,125
- chain banner: `python3 knowledge/_memento_search.py --fetch gm:LATEST` · whole chain: `cat _CHAIN.md`
- every open item across handoffs: `_CARRIES.md` § residual (fetch the section, not the file)
- how hot (cloud seat, container shell): sum input+cache_creation+cache_read of the newest message in the newest `/root/.claude/projects/*/*.jsonl`
- render: `export TMPDIR=/dev/shm; bash knowledge/_render/ensure_env.sh; source knowledge/_render/seat_env.sh`
