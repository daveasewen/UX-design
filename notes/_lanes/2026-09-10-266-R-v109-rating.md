# #266 lane R — HOW GOOD IS v1.0.9 FOR A FRIDAY DEMO?

**READ-ONLY. Nothing edited, built, committed, pushed, stashed or checked out. `_build_all.py` not
run. This file is the only write. Nothing here is a ruling — every recommendation is Dave's to take
or drop.**

Question, his words: *"I need a solid release for Friday (tomorrow, 2026-09-11), how do you rate
1.0.9?"* Use: the frozen prompt building a dashboard from a brief, live, in front of David Rice,
HSBC's Chief AI Officer (`notes/_DEMO-PREP-david-rice-hsbc.html` exists).

## VERDICT — **SHIP-WITH-CAVEAT**

The artefact is sound and every release gate that exists is green. What is missing is not a gate: it
is a **cold run against the thing being demoed**. No cold run has ever been taken against v1.0.9 or
anything close to it.

## 1 — WHAT v1.0.9 IS

- Zip: `apollo-spider/dist/Apollo-Spider-v1.0.9.zip`, 21,132,155 B, tracked at HEAD, baked at cut
  `dd129e8`, released at commit `b94adff`, sha256
  `736d0fb034b71048e0ef7789206837afa7ef76c33330117fa0be3410c149c780` (byte-identical to lane O2's
  twice-proved dry run).
- Ratified `s262-D6` — *"Ratify v1.0.9"*, 2026-09-09 #262. Scope: `s261-D1` + `s262-D1..D5` — six
  dashboard components, the nav's selected glyph, the three-option salutation opener.
- Frozen ledger row `apollo-spider` = `v1.0.9`, 9 zips, `babd4fe1b727`, re-seeded at `c0538c1`.
- Manifest: 1,698 files, sha256 `0265deb8da0b6a04`, status RATIFIED.
- Ten release gates since `s263-D13` (dataviz receipts-fresh is the TENTH). v1.0.9 shipped 9/9;
  `521a751` re-drove the chart engine against the #261 type.css and made **v1.0.9's charts
  retroactively clean** — 27 pages × 8 combos, 0 measured deltas.

## 2 — GATES RUN TODAY AT HEAD `27888f1`, QUOTED

| gate | rc | output |
|---|---|---|
| `_gate_frozen_release.py` | 0 | `PASS — 3 arm(s) asked, no frozen surface moved.` / `apollo-spider version v1.0.9 9 file(s) babd4fe1b727` |
| `_gate_release_audit.py --manifest-check` | 0 | `PASS — the manifest ... is byte-identical to a fresh generation at dd129e8efa79 (1698 files, sha256 0265deb8da0b6a04)` |
| `_gate_release_audit.py --drift` | 0 | `ADVISORY — the manifest was generated at dd129e8efa79; HEAD is 27888f1e9657, 29 commit(s) later.` … `WHETHER TO RE-CUT IS DAVE'S (s219-D4(2))` |
| `_gate_ci_template.py` | 0 | `PASS — the template parses, ships what it calls, and hides nothing.` |
| `_validate_dataviz.py --receipts-fresh` (gate 10) | 0 | `✅ receipts-fresh: all 27 driven receipt(s) match the tree` |
| `_validate_receipt.py --selftest` | 0 | `SELFTEST: PASS ✅` (0.05s) |
| `_validate_icons.py` | 0 | `0 UNKNOWN, 102 bespoke, across 137 snippet(s); 758 library glyphs.` |
| `_validate_kg.py` | **1** | `5 FAIL(s)` — 4 × `filter-toolbar-bar.meta.json: unknown edge-type key` (`composedOf`, `delegatesTo`, `drivesConsumer`, `$contract`) + `gen_kg_edges.py: regeneration on a scratch copy exited 1 (RESOLUTIONS NOT LANDED …)` |
| `_build_integrity.py` | **1** | 14 schema errors — Data grid `relationships` / `with[]` shape, Filter-toolbar-bar `edges`, KPI tile `stateModel`, Legend `provenance/source: 'hand-authored' is not one of [...]` |

**Do the two reds block a release?** **No — by the gate code, not by opinion.** Neither
`_validate_kg.py` nor `_build_integrity.py` is named anywhere in `apollo-spider/build-designer-pack.sh`
or in `knowledge/_release/*.py` (grep: no hits). The release refusals are `require_commit`,
`require_clean`, `require_receipts_fresh` and `ratified()`. Both reds are the known inherited set
from #265 lanes C and E, both are **schema decisions carried as RSQs for Dave**, and both sit on
`.meta.json` knowledge files, not on anything the dashboard renders. They are not demo-visible.

## 3 — GIT

- `git status --short`: three modified, all logs, no source —
  `knowledge/_graph-mark-observations.jsonl`, `notes/_REHEARSAL-LOG.jsonl`,
  `notes/_dream/_GRADE-DECISIONS.jsonl`.
  ⛔ **This makes the tree DIRTY, and `require_clean` refuses `--release` on a dirty tree.** Any
  re-bake needs these committed first.
- **7 commits local, unpushed** (`@{u}..HEAD`): all of #265 — `9a24af9`, `16922b5`, `67b1879`,
  `a88673e`, `d3a2297`, `087e058`, `27888f1`. The v1.0.9 release commits themselves ARE pushed (the
  push ran at the #263 opener), and #264 is pushed too. The demo does not depend on the push.
- 29 commits since the release bake `dd129e8`; 24 since the release commit `b94adff`.

## 4 — THE ONE THING THAT ACTUALLY MATTERS FOR FRIDAY

**The frozen prompt has never been driven against the v1.0.9 pack.**

- Last cold run is the **FIFTH**, `notes/_subreports/2026-09-08-258-C5-cold-run-5.md`, taken at
  `c9bf745` — **before** v1.0.9 existed. Score **3 / 3 / 3 / 2** (visually rich / full / persistent /
  interactive), `pageerror` 0, console errors 0, **both gates PASS for the first time in the series**.
- Between that cold run and the v1.0.9 bake, **39 files changed under `knowledge/canon` +
  `knowledge/snippets`, +15,432 / −2,009 lines** — the whole #261 six-component revision, the chart
  engine's #261 C1–C5 work, Legend, Navigations, Sidebar-nav, Tab-bar. `generate-from-canon/SKILL.md`
  itself moved **+57 / −3** (rule 18, charts-from-data).
- The **SIXTH cold run against the new zip was declared NOT RUN at #260** and re-carried; it has
  still not been run.
- Mitigating: `git diff dd129e8..HEAD -- apollo-spider/skills knowledge/canon` is **EMPTY**. The
  skills and the canon at HEAD are byte-identical to what is inside the v1.0.9 zip. So a cold run
  driven at HEAD today is an honest proxy for the zip.
- Drift that IS in HEAD but NOT in the zip (13 files, +686/−140): the #264 glyph-twin repairs
  (`Sidebar-nav`, `Tab-bar`, `App-shell-*`, `Command-palette` — `balance-transfer`→`transfer`,
  Insights fill slot), `meta.schema.json`, `footer.meta.json`. ⚠ **The shipped v1.0.9 therefore
  still carries the two mis-twinned sidebar glyphs and the old `balance-transfer` name.** Small, but
  it is on a nav surface a demo may well show.

**Known open defects that would show live** (all from cold run 5, none repaired):

1. **F02 — controls rendered after load are dead.** Two `kpi-cta` buttons never fired
   (`kpi_cta.changed: false`); rules 13 and 14 collide and the pack has no delegation convention.
   This is the one that costs a point and the one a live audience can click.
2. **F01 — `data-theme` on `<body>` beside a `.cn-*` scope class silently kills the dark theme.** No
   gate sees it. A dark-mode toggle in the demo is a live risk unless the theme attribute goes on
   `<html>`.
3. F03 — rule 2 cannot be obeyed for a data-driven chart; the geometry has to be authored.
4. Ten of eleven chart types unused for five runs running.

## 5 — RATING, IN ONE LINE

**SHIP-WITH-CAVEAT.** v1.0.9 is a properly cut, ratified, frozen, gate-green release and it is the
right zip to demo. The caveat is entirely about *rehearsal*, not about the artefact: nobody has
watched this pack build a dashboard, and the two defects most likely to be seen live (dead rendered
buttons, dark-theme attribute placement) are known, unfixed, and avoidable by the operator.

## 6 — RECOMMENDED FRIDAY ACTION LIST (recommendations only — Dave rules)

1. **Run the SIXTH cold run tonight, against HEAD, blind.** Highest value by a distance. Skills +
   canon at HEAD equal the zip, so it proves the demo path. Budget it as one lane.
2. **Do NOT re-bake.** A re-bake means a version bump (`s114-D4` — a same-version re-bake is
   laundering, #257's finding), a fresh manifest, ten gates, and a zip nobody has cold-run either.
   The drift since the bake is 13 files of glyph/schema housekeeping — not worth a v1.0.10 the night
   before. If he wants the glyph twins in the demo, the cheaper path is to demo from the repo at
   HEAD rather than the zip.
3. **Push the 7 local #265 commits** if he wants GitHub to match what he demos — his word,
   `s133-D2`, and needs the three dirty `.jsonl` logs committed or left alone first.
4. **Write the two defects onto the operator card**, not into the code: theme attribute on `<html>`
   never beside a `.cn-*` scope; do not click a rendered-after-load button on stage.
5. **Leave the two reds alone.** `_validate_kg.py` (5) and `_build_integrity.py` (14) are Dave's
   schema RSQs, block no release gate, and touch nothing visible.
6. ⛔ Do not run `gen_kg_edges.py` before lane-E RSQ 2 is ruled.
