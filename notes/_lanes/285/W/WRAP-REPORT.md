# #285 — LANE W — the wrap's own working record

provenance: 285 · 2026-09-18
status: observed

**The filed report (`s218-D7`) is `notes/_subreports/2026-09-18-285-W-wrap.md` and is the
authority.** This file holds the two things that belong beside the working set rather than inside
the filed report: the **2c/2d/2f EXIT CHECK** and the **`s271-D4` re-read**, which is the ritual's
final beat. ⚠ It also records what the DEAD first wrap sub left behind, because "nothing" is a
finding and not an absence of one.

## ⬛ WHAT THE DEAD SUB LEFT — checked before anything was written, not assumed

#285's first wrap sub died on a network error mid-ritual. **Before this seat touched the tree it
verified there was no half-state to inherit**, rather than taking the brief's word for it:

| probe | reading |
|---|---|
| `ls -la notes/_lanes/285/W/` | **empty** — the directory did not exist until this seat made it |
| `git status --porcelain` | 3 modified + 3 untracked, **every one a LANE's** (the rulings file, the C / C2 / P subreports) — nothing wrap-shaped |
| `git log --oneline` | `ff354475`, `b99d092c` — the two lane commits, no wrap commit |
| `_CARRIES.md` newest section | `## residual → #285` — #284's, not a half-written #286 |
| `_GM-ARCHIVE.md` newest batch | `## Batch 2026-09-18 #284` — no #285 batch had been opened |
| the dead sub's transcript tail | `API Error: Can't reach the API server — check your internet or DNS (ENOTFOUND)`, `2026-09-18T14:52:22.650Z`, 11 usage records, peak **90,605** real |

✅ **The tree was exactly as the lanes left it.** ⛔ **And that is luck, not design** — the failure
landed in the only window where it costs nothing. `_gm_move.py` guarantees all-or-nothing **per
transaction**; nothing guarantees it **across the ritual**, so a death after a 2c move and before
its receipt was read back would hand the next sub a half-rolled banner with green receipts nobody
had read — the #166 stale-msgfile shape arriving through a different door. **Whether the ritual
owes a RESUME contract is ruling-shaped and is NOT inscribed here.**

## EXIT CHECK — run BEFORE the three rolls, recorded rather than asserted

Every ⚠ / ⬛ / AWAITING / OPEN-CALL / DEFERRED-TO-DAVE item inside the three rolling regions was
scanned and matched to a **standing** home before the move. Dated homes do not count. The standing
home in every case is `_CARRIES.md` § `residual → #286`, probed by token at this seat.

| rolling region | Dave-owed items found | standing home confirmed |
|---|---|---|
| GM ★ PRIOR banner **#283** (12 lines → `_GM-ARCHIVE.md` § Batch 2026-09-18 #285) | ⬛ the `s283-D1` seam ruling and its ADVISORY-vs-BLOCKING question · the DISK findings · the 4c/step-5 collision and #282's un-minted carry · the six inherited gate fails in the `#243` form · resolver `FAIL(6)` · ownership 2/3 · showroom *"138 v 108"* | § `residual → #286` holds every one, by probe: `#243` ×49, `s283-D1` ×4, `showroom` ×17, `FAIL(6)` ×3, `ownership` ×5, `4c` ×37, `gitshim` ×5, `ceiling` ×55 |
| LS ⏱ PRIOR DELTA **#282** (28 lines → `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-18 #285) | ⬛ the per-size logo masters · the THIRD DIAL · the THEORY DOOR and the three postures · `col26-012` · `W-282a` `W-282b` `W-282ll` · the 256,000 breach and the hard-wall question | all present by probe: `THIRD DIAL` ×2, `THEORY DOOR` ×2, `col26-012` ×4, `W-282a`/`W-282b`/`W-282ll` ×1 each, `256,000` ×14, `HARD WALL` ×2 |
| GM stratum **#284** (`roll_2f`: 30 ln → `notes/_GAUGE-LOG.md` at true EOF, 3 ln → `_GM-ARCHIVE.md`) | ⬛ the declared-skips block · `COMMIT STATE #284`'s `#243` not-a-wrap form, its declared hash gap, and the instrumentation-append policy | the `#243` form stands in the carry set (49 occurrences) and is **re-declared on this wrap's own `COMMIT STATE #285`**, now its **ELEVENTH** wrap; the hash gap is re-declared in the same block, verbatim in shape; `_REHEARSAL-LOG` ×3 carries the instrumentation-append policy and it too is re-declared |

⛔ **Nothing was orphaned by these moves, and nothing was dropped for being old.** Eight items that
had no line of their own in `_CARRIES.md` — the connector act, the re-accepted masters, the
`_standing.md` draft, the showroom closure, the delegation counter-measurement, the dead sub, the
unregistered measurer and the seam's SCRATCH arm — were MINTED at 2c(i) rather than left in a
dated home.

⚠ **The probe's own limit, declared for the EIGHTH wrap running rather than discovered:**
`_capture_gate._AGE_RE` does not match `[NEW — 0]`, so **all eight of this session's new items are
invisible to the count**. The move 539 → 543 is #284's four new items ageing into visibility, not
this session's eight. ★ **Two of the eight were briefly visible BY ACCIDENT** — their prose quoted
another carry's `[N]` bracket, which the probe read as an age — and both were rewritten to spell
the age in words **before** the write, so the published count means what it says. That is the
measuring tool being kept honest about its own subject, not a workaround.

## ⛔ TWO STRIKES — and both are receipted, which is what makes them legal rather than tidy

**(1) The #284 masters acceptance.** #284 minted *"THE 40 MASTERS ARE DRAWN AND ACCEPTED BY EYE;
THE REGISTRATION IS OWED"*. ⛔ **Dave reopened that acceptance himself**, with a 4× crop and the
words *"see image the word mark is distorted, look at the B"*. The claim was false **for the
artefacts it named**; the masters were regenerated and re-accepted on different files. Struck in
place, the retraction naming the session and every inscription (`notes/_lanes/285/DAVE-RULINGS-2026-09-18.md`,
the LM2 and V reports, `b99d092c`, `ff354475`), **body not re-typed** — exactly and only what
`s183-D1` / `s188-D2` license. ⛔ **The REGISTRATION half is not struck and is not closed.**

**(2) The showroom's 108 stale pages.** #274's item ⑦ claimed *"HAS NOW BEEN 108 PAGES STALE FOR
TWO CONSECUTIVE WRAPS"*. Re-synced at `ff354475` on his *"1. go"* — 108 files, one line each,
`:is(` → `:where(`, verified by **decoding all 108 payloads rather than sampling**. Struck with its
receipt, and the `138 v 108` half is **explained** in the same line: 138 = 137 generator-owned +
`index.html`, so 108 ⊂ 137 ⊂ 138.

⚠ **AND ONE THAT LOOKS STRIKEABLE AND IS NOT.** #255's item ⑨ — *"SIXTY OF THE 136 SHOWROOM PAGES
ARE STALE AGAINST CANON, AND THIS WRAP DECLARED THE GAP RATHER THAN REGENERATING THEM"* — ages
untouched beside the struck one. **Its headline records an ACT, and the act happened.** The
staleness half is discharged and is said in NEW item ④ instead. `s271-D4`'s own warning is the
reason: **a strike that is wrong is worse than an item that is merely stale.**

## ⬛ `s271-D4` RE-READ — the ritual's FINAL beat, run at 5b and RECORDED rather than assumed

The memory hook's "open, Dave's" items (`notes/_lanes/285/WRAP-MEMORY-HOOK.md`) were re-read
against `knowledge/_rulings.json` **after every ruling of the session was inscribed**.
⛔ **#285 inscribed NONE. `json.load` over the `rulings` list counts 620, unchanged from #284's
close** — so the re-read has, by construction, nothing it may strike.

**NOTHING IS STRUCK. THAT IS A MEASUREMENT, NOT A SHORTCUT, AND FOUR ITEMS ARE NAMED RATHER THAN
STRUCK BECAUSE THEY MOVED WITHOUT BEING RULED:**

1. **The masters** — ⚠ **the near miss this run keeps having, and it read differently this time.**
   They were regenerated, verified 16/16, and Dave accepted them in a form that cannot be mistaken
   for anything else: *"2. accept"*. **That is still not a ruling and still not the row's close
   condition:** `W-285lm` closes on acceptance **and** registration in `_logo_nodes.json`, and the
   registration is untouched behind the `gen_kg_icons.py` fence. **Striking it on an acceptance
   would close a row that is half open.** The hook's item 2 stands.
2. **The connector question** — ⚠ **MOVED BY AN ACT, NOT A RULING.** #284 carried *which connectors
   Apollo needs* as an open question. He answered it by **blocking the built-in Browser's 17
   tools** in Tool permissions. ★ **The act settles what to measure; it does not settle what to do
   with the reading**, and the hook's item 6 (the skills second run) is exactly the part he has not
   answered. Named, not struck.
3. **`knowledge/_standing.md`** — ⚠ **EIGHT LINES THAT LOOK RULED AND ARE NOT.** They are drafted
   from the record, they print at every seam, and the seam is standing by `s283-D1`. **None of that
   makes the wording his.** The file's own header says DRAFT, row `W-285sc` is open on it, and
   ⛔ **an instrument reciting a constraint is not an inscription of it.**
4. **The delegation rule** — ⚠ **OBEYED, WHICH IS NOT THE SAME AS RULED.** Seven lanes, none in
   seat, measured at this seat from the conductor's own transcript. That is evidence the shape is
   affordable; **his sentence is still ruling-shaped and still uninscribed**, and one session's
   obedience is not a rule.

⚠ **Every open item above is written as the QUESTION IT IS, not as a state of the world**, and
⛔ **an acceptance, an act, an instrument and a session's behaviour are each a strong match, not a
proof — a strike that is wrong is worse than an item that is merely stale** (`s271-D4`'s own
warning, and the reason four near misses are named here instead of four items being struck).

## The seventh gate fail — closed here, and named because the shape will recur

Seven blocking fails stood at this ritual's open; six at its close. The six are the declared
structural set and **a wrap may not repair them**. The seventh was this seat's own:

> `ds-021 (C): knowledge/_seam.py counts tokens (cl100k) and is NOT in MEASURERS — an UNREGISTERED measurer.`

Lane SC gave the seam a counting site (`standing_tokens()`) in **this session's own commit**
`b99d092c` and nothing registered it. Entered in `_capture_gate.MEASURERS` as **`estimate-only`** —
it encodes with tiktoken cl100k only, never calls `_gauge_tokens.count()`, has no API path, and its
figure is never a FILL or budget claim — following the `_compose_slice.py` precedent set at the
#270 wrap, which that entry names as a declaration rather than a ruling. ⛔ **A DECLARATION, NOT A
RULING.** ★ **The general form: a lane that adds a counter adds a gate obligation, and the gate
finds out at the wrap.** The registry exists because the last unregistered measurer went
twenty-seven sessions unnoticed; it caught this one inside a day.

## Two corrections this seat made to its own work before the commit, each named rather than erased

1. **The banner cap.** The first draft measured **1,225** against the `s241-D2` cap of 1,200 and was
   refused by this wrap's own pre-check rather than by the gate. Compressed in five passes to
   **1,196**, with **nothing dropped** — the clause commands shorter, never decide-what-to-drop.
   ★ **One pass exists only because the GENERATED residual line was measured at its REAL expected
   width instead of a short placeholder** (1,240 with it, against 1,225 without): **a figure taken
   before the step that writes it is a prediction, not a measurement** — the #240 lesson, met from
   the other direction, and the reason the placeholder was replaced with the exact line
   `_roll_state.py` would later emit. It did, byte-for-byte.
2. **The `section-usage` line.** The first version testified nine GM sections and five LS ones; the
   gate refused it as **MALFORMED — *"worse than missing — a false inscription"***, naming the six
   LS sections with no testimony. Repaired from `_gm_usage.py --usage-template`, whose vocabulary is
   the only copy of the ids; **the codes were written by hand, by the seat that did the reading.**
