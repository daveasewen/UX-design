# #267 lane P2 — RE-CUT v1.0.10 (PROPOSED): the chart pages ship by measurement

**Model: Opus. Conductor: Fable (#267). Ruling `s267-D1` (`185946f`): "Hold — re-cut with the
chart pages shipped. lets get this sorted". Nothing here is ratified, released, or pushed.**

## DONE

1. **Re-measured the 13-vs-6, did not inherit it.** `_validate_dataviz.py` copied into a copy of
   lane P's dry-run stage (`/var/tmp/p2probe`, from `/var/tmp/dry10a`) and run as a consumer runs
   it. With the six shipped pages: **rc 1**, six blocking dv-004 reds —
   `boxplot`/`bullet`/`butterfly-h`/`candlestick`/`histogram`/`scatter`, each
   "…has changed since it was driven (file is missing)". Copy the seven receipt-named pages in:
   **rc 0**, "DataViz gate passed (14 chart surface file(s))". Nothing else was absent — the other
   `sources` rows (`knowledge/canon/*`, `knowledge/snippets/Chart-*.reference.html`) are already
   owned by engine-canon and already ship. The receipts name **45** source rows across 27 entries;
   **13** of them are under `knowledge/_tests/chart-engine/`.
2. **Fixed the CUT RULE, not the list.** The six typed page lines are GONE from
   `GATE_DATA_CANDIDATES` (`_gen_pack_manifest.py:266` now holds only `_receipts.json` and
   `_drive_chart_engine.py`, with the s267-D1 comment above them). New
   `receipt_named_chart_pages(sha)` (`:690`) reads the receipt **at the commit** (`git show
   <sha>:knowledge/_tests/chart-engine/_receipts.json`) and returns its `sources` rows under
   `knowledge/_tests/chart-engine/` ending `.html`; pure half `chart_pages_from_receipt(raw)`
   (`:713`) is bite-testable without a commit; `gate_data_candidates(sha)` (`:727`) is the ONE
   seat, used by the probe stage (`:1153`) and the manifest (`:1918`).
   Read at the commit, never the working tree, so two bakes of one commit cannot differ.
3. **7 new bites — 233 total, 0 fail(s).** `chartpages/{from-receipt, bad-json-empty,
   no-pages-empty, head-has-many, every-named-page-is-a-candidate, receipt-rides-too,
   no-typed-pages-left}`. **Mutation-tested both ways**: truncating the derived list to 2 →
   `chartpages/every-named-page-is-a-candidate` RED naming 11 absent pages; widening the row
   filter to any `.html` → `chartpages/from-receipt` RED with a snippet leaked in.
   `bash apollo-spider/build-designer-pack.sh --selftest` **rc 0**.
   Fix commit **`a9218cc06e8f24a5db1b7fe5648610e17d1f83f5`**.
4. **Re-cut at `a9218cc`**, differential arm ARMED (`--full-stage /var/tmp/full10b`, a
   `git archive` of the commit). 1714 files, 45967883 bytes, manifest sha256 `4cd9229adb2df432`.
   Probe: **41 RUNNABLE · 4 NEEDS-DEP · 9 REPO-BOUND**; `_validate_dataviz.py` **RUNNABLE**,
   population `graded`, why "ran clean, verdict PASS". `gen_provenance_receipt.py` still in.
   Twice-baked `/var/tmp/dry10c` + `/var/tmp/dry10d`: sha256
   `d76ffd507319ae53ccc96f4ea4090baa0369e2761985231954b7250eca5dc488`, `cmp` **rc 0**.
   Status derives **PROPOSED** (RATIFY_IDS untouched).
5. **Manifest deltas.** vs `fbd9c14` (v1's cut): **+8, −0** — the 7 chart pages and
   `knowledge/_validate_dataviz.py` itself. vs **v1.0.9** (`3cab7c3`): **+16, −0** — those 7,
   plus `knowledge/gen_provenance_receipt.py`, plus 8 derived `-active` icons (#264).
   **REMOVED vs v1.0.9 is EMPTY** — the finding the brief named does not fire.
6. **In-pack proofs**, from the unzipped `/var/tmp/dry10c/Apollo-Spider-v1.0.10`:
   `python3 knowledge/_validate_dataviz.py` **rc 0**, 14 surfaces, 0 blocking.
   Receipt proof on a COPY (`/var/tmp/dash-p2.html`) of
   `outputs/coldrun-267/arm-A-blind/dashboard.html` — the frozen arm untouched, `git status` on
   `outputs/coldrun-267/` clean: `_validate_receipt.py` before minting **rc 1** FAIL:NO-RECEIPT,
   `gen_provenance_receipt.py --mint` **rc 0**, `_validate_receipt.py` after **rc 0 PASS**.
   ⚠ the mint helper needs `--mint`; a bare invocation is **rc 2** "REFUSED: one of
   --compose / --mint / --check / --selftest is required".
7. **Ten release gates: 8 PASS.** 1 `_gate_frozen_release.py` rc 0 · 2 `--selftest` rc 0 (14
   bites) · 3 `_gate_release_audit.py --check` rc 0 (byte-identical at a9218cc, 1714 files) ·
   4 `_gate_release_audit.py --selftest` **rc 1** · 5 `--pack` **rc 1** ·
   6 `_gate_ci_template.py --check` rc 0 · 7 `--selftest` rc 0 (10 bites) ·
   8 `build-designer-pack.sh --selftest` rc 0 · 9 `cold-start/gen_projections.py --check` rc 0 ·
   10 `_gate_pack_docs.py --stage /var/tmp/dry10c/Apollo-Spider-v1.0.10` rc 0 (221 findings,
   ADVISORY). Gates 4 and 5 are the SAME pre-bake fact as lane P's — no zip in dist/ carries
   v1.0.10; gate 4's RED is `[pack/frozen-history-is-skipped-not-failed]`, which drives the
   `--pack` arm. Both go green after the bake, not before. `_gate_pack_docs.py` REFUSES (rc 2)
   without `--stage` — it will not guess a tree.
8. **Re-cut recorded** at **`4a7adb8178b2f9efd397df0d099a901dd318a93f`** (manifest + probe +
   RELEASE-SPIDER page pair, the `fbd9c14` shape). `RATIFY_IDS`, `_gate_frozen_release.py:134`'s
   `v1.0.9` literal, `_frozen-releases.json` and `dist/` all untouched.
9. **Go/no-go page v2**: `reviews/GO-NOGO-v1.0.10-2026-09-10-v2.html` (swiss-design-system, the v1
   stylesheet reused verbatim; v1 stays as the record of the held cut).

## ⚠ RED / NOT DONE (declared)

- No `--release`, no push, no ratification, no `_rulings.json` edit, no `_build_all.py`, no
  `gen_kg_edges.py`. **8 commits local and unpushed** (`git log @{u}..HEAD` = 8): lane P2's
  `a9218cc` fix, `4a7adb8` re-cut, and the page+report commit this file lands in; lane P's `fee2572`, `625be64`,
  `fbd9c14`, `ea25058`; the conductor's `185946f`. The push is Dave's word.
- Gates 4 and 5 not green — they cannot be, pre-bake.
- **The v2 page was NOT rendered in a browser.** `file://` is refused on this surface and no
  chromium run was attempted. It is structurally validated only (HTMLParser: 0 unclosed, 0
  mismatched tags, 18352 bytes) on a stylesheet Dave has already seen at v1. Say so before
  claiming it looks right.
- `butterfly-v.html` ships although the gate does not reach it TODAY (its snippet carries marks,
  so it never enters the dv-004 arm). It is in because the RECEIPT names it, and engine-drawn-ness
  is a property of markup that moves without touching this generator. Named on the page, not
  smuggled — but it is the one file in the +8 that today's run does not open.
- `reviews/RELEASE-SPIDER-2026-08-26-v1.html` was regenerated by `--manifest`/`--dry-run` (the
  script does this itself) and the overlay re-injected with `knowledge/_review/_make_review.py`.
- Untracked/modified files left alone: `notes/_REHEARSAL-LOG.jsonl`,
  `notes/_dream/_GRADE-DECISIONS.jsonl`, lane C6's brief/subreport and `reviews/COLDRUN-267-*`.

## ⚠ SANDBOX

No `.git/index.lock` was present at any point; nothing was moved to `_tmp/_orphan-locks/`.
`--full-stage` will NOT create its directory — `probe_gates` dies with `FileNotFoundError` on the
`cwd`. Populate it first: `mkdir -p <stage> && git archive <sha> | tar -x -C <stage>` (458 MB).
That is undocumented in the script's header and cost one failed run.

## THE COMMANDS THE CONDUCTOR RUNS AFTER DAVE'S WORD

Carried from lane P, **re-keyed to `a9218cc`**. Serial, in this order, each with its #262
precedent commit.

1. **Inscribe the ruling** — `knowledge/_rulings.json`, a new sNNN-D row naming v1.0.10 RATIFIED.
   Precedent `33a6608`.
2. **Key `RATIFY_IDS`** in `_gen_pack_manifest.py` to that ruling id, re-run
   `--manifest --commit a9218cc --full-stage <populated stage>` so the status derives RATIFIED, and
   move `_gate_frozen_release.py:134`'s apollo-spider literal `v1.0.9 → v1.0.10`. Precedent `3cab7c3`.
3. **Bake** — `bash apollo-spider/build-designer-pack.sh --release --commit a9218cc`. Precedent `b94adff`.
4. **Verify** — `--check apollo-spider/dist/Apollo-Spider-v1.0.10.zip --commit a9218cc`, and re-run
   gates 4 and 5. Precedent: `notes/_subreports/2026-09-09-262-W-release-and-wrap.md:103-115`.
5. **Re-seed the frozen ledger** — `knowledge/_release/_frozen-releases.json`, apollo-spider row to
   10 zips. Precedent `c0538c1`.

## TOKENS

**ESTIMATED, not measured** — a lane has no `message.usage` reader and `_checkin.py` is the
conductor's instrument [[measuring-tool-must-not-guess]]. Best estimate **~95K FILL at the step
4/5 seam, ~120K at wrap**, on ~30 tool calls; the manifest/probe run and the two dry-run bakes
(~90s each) were tailed, never read whole, and the 458 MB `git archive` was silent. If the
conductor needs a number it must come from the gauge, not from this line.

## PATHS

- `knowledge/_release/_gen_pack_manifest.py` — `:266` the emptied candidate block, `:690`
  `receipt_named_chart_pages`, `:713` `chart_pages_from_receipt`, `:727` `gate_data_candidates`,
  `:1153` probe call site, `:1918` manifest call site, `:2811` the seven bites
- `knowledge/_release/_pack_manifest.json` · `knowledge/_release/_pack_gate_probe.json`
- `reviews/GO-NOGO-v1.0.10-2026-09-10-v2.html` (v1 kept) ·
  `reviews/RELEASE-SPIDER-2026-08-26-v1.html` (+ `.REVIEW.html`)
- `knowledge/_validate_dataviz.py` `:301` `driven_dv004` root resolution, `:317` the re-hash loop
- stages: `/var/tmp/dry10c`, `/var/tmp/dry10d`, full `/var/tmp/full10b`, probe copy
  `/var/tmp/p2probe` (all throwaway)
