# #275 WRAP MEMORY HOOK — WRITTEN FOR THE CONDUCTOR TO PLACE

> ⛔ **SEAT LIMIT, DECLARED, NOT A SKIP.** Ritual step 3 (memory) is **read-only at this wrap seat by the brief** — the conductor's memory directory is not this sub's to write. **0 bytes were written to memory.** The hook body and its one `MEMORY.md` index line are written here instead, in the `s271-D4` form, for the conductor to place at the #276 opener. Provenance: 275 · 2026-09-15 · status: observed.

---

## THE `MEMORY.md` INDEX LINE (one line, top of the stack)

```
- [★★★ #275 WRAPPED · 6 RULINGS 578 → 584 · 145 UX PRINCIPLES + 30 POLARITIES ENTERED THE GRAPH · A SECOND LIVE SESSION WROTE INTO THIS ONE'S LANE](wrap-275-the-principles-and-the-second-seat.md) — ⛔ READ `_HANDOFF-126-the-principles-and-the-second-seat.md` FIRST, it OUTRANKS `_CHAIN.md`; `s275-D1..D6` came from his 16:27Z export (RP-1..6 all **a**, no notes) read back whole + *"go"* (`ffcb029`), and the only other words he typed all day were *"gm"* and *"can you surface the page please"* · `s269-D1` STEP 3 DONE — `eb47a58` landed `knowledge/_ux_principle_nodes.json` 175 nodes / 111 edges / 15 declared nulls, explorer v1.11 → v1.12, base graph pixel-stable (0 of 951 moved); #276 = STEP 4, icons (666) then logos, SAME RK/RP shape · ⛔ THE METHOD IS THE POINT AND IT PAID TWICE: lane RP's mutation harness found **3 selftest bites that were DEAD**, and lane RV corrected TWO of the conductor's own figures by addition (31 families → 32; 60 type strings → 62) · ⛔ NEW HAZARD, RULING-SHAPED, NOT RULED — the still-open #274 seat committed `5dd19ee` INTO `notes/_lanes/275/` and #275's `Write` overwrote its line back; both healed BY ADDITION at `49e2a44`, and **one word at #276: one seat at a time, or second seat only under its own lane?** · ⛔ both inscribe attempts were wrong (a re-dump of `_rulings.json`, #179 class; then a no-op) and a green `_validate_kg` caught neither · boot 82,485 = ELEVENTH breach; FILL 175,508 UNDER the 180,000 line; 30 local UNPUSHED
```

---

## THE HOOK BODY — `wrap-275-the-principles-and-the-second-seat.md`

```
---
metadata:
  provenance: 275 · 2026-09-15
  status: observed
---

# #275 — the principles and the second seat

**Six rulings, `_rulings.json` 578 → 584, all by textual span.** His export off
`notes/_lanes/275/principles-kg/REVIEW-principles-into-graph-2026-09-15-v1.html`, stamped
2026-09-15T16:27:07.539Z, came back RP-1…RP-6 all **(a)**, no notes, no nulls. The conductor read the
whole set back and his *"go"* inscribed them at `ffcb029`. `s275-D1` twelve fields on a `ux:<id>` node ·
`s275-D2` all six edge types ratified, and it is the id `--land --ratified` takes · `s275-D3` a
`polarity:<id>` is a NODE · `s275-D4` the 20 standards rows land plain, joins authored later ·
`s275-D5` metas cite the six A-grade laws in ONE authored lane (+`P-274-3`) · `s275-D6` store and
explorer reader in the SAME commit.

**`s269-D1` step 3 is DONE.** `77b043f` (lane RP, 285K) proposed read-only — dry-run 175 nodes / 111
edges / 27 declared nulls, selftest 15 bites, mutants 18/18 — and landed nothing. `2cbfbad` (lane RV,
144K, read-only) drove 12/12 claims green. `eb47a58` (lane RL2, 183K) landed
`knowledge/_ux_principle_nodes.json` at 175 nodes / 111 edges / 15 declared nulls (the "27" = 15 + 12
edgeless polarities), explorer v1.11 → v1.12 behind an OFF-by-default "UX principles" chip, base graph
pixel-stable at 0 of 951 nodes moved.

**The method paid twice, and that is the finding.** Lane RP's mutation harness found THREE selftest
bites that were DEAD — they passed against a deliberately broken generator. Lane RV corrected TWO of
the conductor's own figures BY ADDITION (31 families → 32; 60 live type strings → 62). Neither changed
a ruling; both changed what the record says.

**⛔ Open, Dave's — every one of these is a QUESTION PUT at the wrap call, not a state of the world
(`s271-D4`):**
- **Two sessions on one repo.** Put to Dave at the wrap call: is one seat at a time the rule, or may a
  second seat write provided it writes only under `notes/_lanes/<its own N>/`?
- **`--land --ratified` unlocks on ANY existing ruling id**, RK and RP alike. Put to Dave at the wrap
  call: does a `governs` check close it?
- **The explorer's `renderPanel()` omits a dug principle's `statement`** — one `if(n.statement)` span.
  Put to Dave at the wrap call.
- **RP-4's four name-only WCAG joins** — authored later under `s275-D4`; who authors them and against
  what evidence is put to Dave at the wrap call.
- **`s275-D5`'s authored lane** — ruled and scoped; WHICH components go first is put to Dave at the
  wrap call.
- **Boot 82,485, the eleventh consecutive breach**, and the `s241-D2` double-count arm now names five
  sessions (#243 · #264 · #272 · #273 · #274). Put to Dave at the wrap call: re-base, shrink, or accept.
- **The push** — 30 local and unpushed after this wrap. His word, his hand.

**⛔ Never:** re-dump a JSON store (both #275 inscribe attempts were wrong and a green `_validate_kg`
caught neither) · `git stash` (use `git show <sha>:<path>`) · `gen_kg_edges.py` before lane E's RSQ 2 ·
push from this seat.
```

---

## `s271-D4` FINAL BEAT — THE OPEN LIST RE-READ AGAINST `knowledge/_rulings.json`

Run after 5/5b, once every ruling of the session is inscribed. **Result at this seat: the seven open
items above were re-read against `knowledge/_rulings.json` (584 rulings, `s275-D1`…`s275-D6` present)
and NOT ONE of them is closed by this session's own rulings** — `s275-D1`…`s275-D6` settle the node
shape, the edge vocabulary, the polarity node, the standards rows, the authored lane and the
same-commit rule, and none of them touches the two-seat question, the `--land` unlock, the
`renderPanel` span, the WCAG joins, the components order, the boot series or the push. **Nothing is
struck. Each item stands as a question put.**
