# #287 — the cloud-memory hook, WRITTEN FIRST AND PLACED SECOND

provenance: 287 · 2026-09-19 · status: observed

*Ritual step 3, in the `#278` form: the wrap sub writes this file FIRST — the index line plus the
topic-file body, in the `s271-D4` shape — and only then attempts the store. **What actually
happened at the store is recorded at the FOOT of this file, not assumed**, which is the whole
reason the file is written before the write is tried.*

⚠ **Every "Open, Dave's" item below is a QUESTION PUT, never a state of the world** (`s271-D4`,
enacting dream-12 P4(a)). The hook is written before his last answers land, so an item recorded as
a state of the world is false the moment he speaks; recorded as a question, it cannot go stale.

---

## 1. THE INDEX LINE — prepended newest-first, above the #286 line

> - [★★ #287 WRAPPED · TWO SENTENCES BECOME RULINGS AND THE STRAND MAP IS ORDERED · RULINGS 620 → 622 · THE EXPLORER READS `sizes` · THE 256,000 WALL WORDING SWEPT 58 → 42 · THE −7,031 CONNECTOR LEVER IS MEASURED AT n=2](wrap-287-the-rulings-land-and-the-strand-map-is-ordered.md) — ⛔ READ `_HANDOFF-138-the-rulings-land-and-the-strand-map-is-ordered.md` FIRST, it OUTRANKS `_CHAIN.md` and does NOT replace `_HANDOFF-130`…`-137`'s still-open items · **FABLE 5.1 conducting, ONE DAY, NO date split, FIVE Opus lanes (I · K · W · C · X), every one an `Agent` at depth 1, NONE in seat** · ★★★ **THE RULINGS STORE MOVED FOR THE FIRST TIME IN SEVEN SESSIONS, 620 → 622, on fourteen characters.** His whole reply to a three-item opener, verbatim: ***"1. inscribe · 2. keep `sizes` and rebuild. · 3. do it"***. **`s287-D1`** — the eight standing lines of `knowledge/_standing.md` are RATIFIED AS WRITTEN, the file is out of DRAFT by HIS word rather than the conductor's reading of it, the seam re-quotes them, and the 256,000 wording fix in `_gauge_tokens.py` is his. **`s287-D2`** — a per-size master is a **SIZE FIELD on its lockup's existing node** in `knowledge/_logo_nodes.json`, never its own node and never a new kind, and the node count does not move. ★ **THE JOIN IS NAMED ON `s287-D1`'S OWN FACE RATHER THAN SMOOTHED: reading #286's *"okay go on everything"* as ratification-AS-WRITTEN was the CONDUCTOR'S reading; the word that makes it HIS is *"inscribe"*.** Inscribed through the sanctioned writer with its reconstruction proof PASSED, never a hand-edit; `notes/_RULINGS.html` re-rendered FRESH in the same change · ★★ **THE EXPLORER NOW READS THE FIELD — the #286 carry "nothing reads it" is DISCHARGED.** Builder `VERSION 1.26 → 1.27`, three new reads (an aside row, INSPECT's RECORD listing each height as a link to its own master with geometry and digest, and a MASTERS tab drawing all five at their true heights); **8/8 logo nodes carry `sizes` with 5 keys, 40 entries byte-identical to the source, PARSED out of the built page rather than eyeballed**, 8 nodes and 33 edges either side. ⚠ 2,256 baked coordinates moved and 11 nodes appeared — **the Constitution growing under the build's feet, not the field** · ★★ **THE 256,000 "WALL" WORDING IS SWEPT ON *"do it"* — 58 → 42, and ZERO of the 42 assert that 256,000 is a wall.** ⛔ **NO NUMBER MOVED** (`BUDGET_HARD` still `256_000`, `STOP_LINE_TK` still `180_000`) and **Dave's `says` fields were NOT touched** — six ruling records were ANNOTATED by an appended clause applied as a byte-level span swap with the inscriber's own proof discipline, because `_inscribe_ruling.py` has no `ruled` path and says so itself. The 42 survivors are classified one by one: 16 are the correction's own words, 11 are annotated ruling text, 2 name the live constant, 3 are #286's record now marked SWEPT, 1 is a false positive left alone on purpose · ★★★ **THE SECOND COLD BOOT LANDED AND THE CONNECTOR LEVER IS NOW A MEASURED EFFECT: 74,120 real against #286's 73,832 on the same setup, Δ +288** — two readings within 300 tokens where the prior seven-reading spread was **11,526** wide, **meeting the acceptance test #286 wrote for itself**. ⛔ **74,120 is the TWELFTH post-diet reading over the 70,000 ceiling — SHRINK-ONLY, NOT a re-base, and raising the literal is his word alone** · ⛔★★★ **THE AFTERNOON CHANGED WHAT #288 IS FOR.** Dave brought a GPT-6 handoff from Apollo Spider v1.0.13 on his work machine (*"don't take it on face value, use your judgment, but what it did worked"*), saved into the repo and tested claim by claim — **5 of 6 VERIFIED, 1 PARTLY**; its one real analytical miss is the same miss twice, reading `0` and `40/4` as DEFECTS when both are RULED values. The conductor framed the result as *"two ruled spacing sources disagree"* and **DAVE CORRECTED THE FRAMING: *"the gutters are also different for the inner and outer bentos we essentially have a structural bento and embedded bentos or tile groupings"*** ⇒ **two deliberate quantities, not a contradiction**, and the real defect is that **the `s219-D3` generation arm was never built** · ⬛★★★ **HIS WRAP CALL ORDERS THREE THINGS AND #288 OPENS ON THE THIRD: a proper review of Mono's 0 with the `s219-D3` arm BUILT (*"I just need the one-shot design to not disappoint"*), a review of the bento template's QUALITY, and A STRAND MAP OF APOLLO WITH A CLEAR PATH TO A COHESIVE PRESENTATION ON FRIDAY 2026-09-25** · ⚙ FILL **181,277 real / 24 turns** first-hand vs **180,375 / 23 declared** = Δ **902**, the smallest hand-over on this run — ⛔ **1,277 past the 180,000 quality line, and the brief says the stop line was crossed by 375 ON THE WRAP CALL ITSELF**; ✅ 220,000 and 256,000 clear. **subs 573,531 (n=5)** QUOTA never FILL — ⚠ **and `_checkin.read_fill` reads 568,179 for the same five seats: the `subs` convention is input-plus-output and the FILL rule is input-side, two definitions published rather than reconciled** · ⛔ **6 inherited gate fails carried in the `#243` form for the THIRTEENTH wrap running — a wrap may not repair them**; ⚠ a seventh was born mid-session by a lane and HEALED by the remedy the gate itself named · ⚠ **CI on `75a490cd` is `completed / failure` at the RUN level — `gates` failing the SAME TWO inherited steps 5 and 6, `release` success, and `render` resolved to SUCCESS, which settles the colour lane C could only read as `in_progress`** · **repo is the record, memory is the accelerator — UNCHANGED**

## 2. THE TOPIC FILE — `wrap-287-the-rulings-land-and-the-strand-map-is-ordered.md`

Body as placed (frontmatter `name` / `description` / `sources: [cowork]`, then the sections below).

### What #287 was

A discharge session that became a direction-setting one. The morning cleared three carries that had
been waiting for one word each; the afternoon brought a GPT-6 handoff from Dave's work machine and
moved the session's subject to what Apollo is for and what lands by Friday the 25th.

### Ruled (his words, never paraphrased)

- **At the opener, whole:** *"1. inscribe · 2. keep `sizes` and rebuild. · 3. do it"* ⇒ `s287-D1`,
  `s287-D2`, and the 58 wall locations amended (including ratified runbook text and the `ruled`
  text of standing rulings, which #286 said was his word alone — this is that word).
- **Correcting the conductor's framing of the gutters:** *"The problem with the gutters it that they
  are deliberately different for the themes and the gutters are also different for the inner and
  outer bentos we essentially have a structural bento and embedded bentos or tile groupings."*
- **At the wrap call:** *"I'm not sure that 0 is right for mono, lets have a proper review and fix
  this: 'The generation arm that would do that was ruled at #219 and never built.' and any other
  problem with this, I just need the one-shot design to not disappoint, however it is getting better
  all the time."* · *"I'm not sure about the quality of the template we need to look at this too."* ·
  *"I want to return to the presentation soon, so I need to understand how the multiple strands of
  Apollo stand and I need a clear path to presenting something cohesive for friday the 25th"* ·
  *"wrap with all of this in mind"*.
- **On the Spider handoff:** *"don't take it on face value, use your judgment, but what it did
  worked."*

### Open, Dave's — every one a QUESTION PUT at the wrap call, not a state of the world

1. **The strand map and the Friday-25th path** — put at the wrap call; it is #288's first move, and
   the presentation strand's own location is **not established from this seat** and must be found
   rather than assumed.
2. **Mono's 0 and the unbuilt `s219-D3` generation arm** — put at the wrap call. DOUBTED is not
   overruled: `s217-D2` still rules the value, and what is ordered is a review and a build.
3. **The bento template's quality** — put at the wrap call, as a second review and not a clause of
   the first.
4. **An inline-style rule** — put at the wrap call and NOT answered. 596 `style="` in the snippet
   corpus, of which **88 carry a raw px/rem with no `var()`** — that 88 is the actionable number.
   **Zero rulings on inline styles exist across all 622.** The cheapest move named is widening
   `DEF-004`'s population, which is a scope change and his.
5. **The bento template's status** — put at the wrap call and NOT answered. The meta says
   `PROPOSED / NOT REGISTERED`; `showroom/index.json`, the surface the skill tells builders to
   search, says `beta`.
6. **Which definition the `subs` line means** — input-side (`read_fill`, 568,179) or
   input-plus-output (the declared convention, 573,531). Found at this wrap seat, published both
   ways, reconciles to the token.
7. **A freshness check for `dashboard/index.html`** — it was 11,617 lines stale against its own
   source and nothing reported it; there is no `--check` arm on `gen_dashboard.py`.
8. **`_to_delete/` accumulating on a persistent disk** — 4.3 MB added this session and nothing
   empties it.
9. **The `INSEAT_WARN_TK` 10,000 and its block's placement** — untouched, still picked not ruled.
10. **The memory archive at 48,990 of its 49,152 B cap** — see the foot of this file.
11. **Everything still standing on `_HANDOFF-130`…`-137`**, and the 560 items in `_CARRIES.md`
    § `residual → #288`.

### Carried forward, unchanged

The repo is the record and memory is the accelerator. `_HANDOFF-138` outranks `_CHAIN.md` and
replaces nothing. Strike nothing without a receipt.

---

## 3. WHAT ACTUALLY HAPPENED AT THE STORE — recorded after the attempt, never before

**Seat:** this wrap sub reached the Project memory store directly (`mcp__memory__memory_*`), as at
#284, #285 and #286 — so the runbook's *"structural seat limit"* at step 3 measured FALSE for a
fourth consecutive wrap. ⛔ **The runbook line is still NOT edited from here, because amending a
ratified step is Dave's.**

**Result:**

- ✅ Topic file `wrap-287-the-rulings-land-and-the-strand-map-is-ordered.md` — **WRITTEN**, the
  store's own receipt reading *"Wrote 8701 of 49152 bytes"*, version `8ab98481e8f4`.
- ✅ `index.md` — **THREE TARGETED `str_replace` EDITS, never a whole-file rewrite**, so the four
  existing wrap lines could not drift; a 16 KB retype from context is exactly the class of silent
  corruption the verbatim rules exist to prevent. (1) the #287 line PREPENDED newest-first above the
  #286 line (`db8dbff42481` → `4f36b6e162b7`, +5,595 B); (2) the frontmatter `description` amended
  to say FIVE lines and why (`→ c8131b4a8574`); (3) the closing note given the #287 suspension
  paragraph (`→ 188d350d8d83`). **Final size 22,230 of 49,152 B.**
- ⛔ **THE NEWEST-THREE CUT COULD NOT BE MADE AND WAS NOT FAKED, FOR THE SECOND CONSECUTIVE WRAP.**
  `MEMORY-ARCHIVE.md` stands at **48,990 of its 49,152 B cap — 162 B of headroom** — and the #283
  line alone runs to roughly **4,300 bytes**, more than twenty-six times it. **Nothing was
  truncated, because a truncated verbatim move is not a move**, so `index.md` deliberately carries
  **FIVE** wrap lines this session (#287 · #286 · #285 · #284 · #283) and says so in its own
  frontmatter and closing note.
- ⚠ **How that was established is stated plainly rather than dressed up as a refusal that was never
  received: it is arithmetic on two measured quantities — the store listing's byte count and the
  documented cap — not a rejected write.** No append was attempted, because the only way to obtain
  the archive's version token is to read all 48,990 B into this seat, and a failure whose arithmetic
  is already certain is not worth that price. **The attempt is one call away for any later seat that
  wants the refusal itself.**
- ★ **Nothing is at risk.** The repo is the record: the handoffs, dossiers, ledgers, `_CARRIES.md`
  and git hold every one of these sessions in full. **What to do about the archive — condense,
  split, or something else — is Dave's, and the dream pass is its usual seat.**
