# BOOT COLD — #287 — 2026-09-19 — THE SECOND READING ON THE SAME SETUP

provenance: 287 · 2026-09-19 · conductor Fable 5.1 · measured at the first turn by `knowledge/_checkin.py --window 200000 --no-block`

| session | boot (real, first turn) | setup |
|---|---|---|
| #285 | 80,863 | Browser connector ON (17 `mcp__Claude_Browser__*` tools) |
| #286 | 73,832 | Browser BLOCKED · skills unchanged · computer use off · Claude Docs, Claude in Chrome, GitHub on |
| **#287** | **74,120** | **same as #286** — no `mcp__Claude_Browser__*` tool in the session's deferred list |

**Δ #287 − #286 = +288.** Two readings within 300 tokens of each other on a setup whose prior seven-reading spread was 11,526 wide.

⇒ **The −7,031 from blocking the Browser connector is a MEASURED EFFECT (n=2), no longer a direction.** #286's own stated test was "two readings within a few hundred tokens of each other would make it measured" — met.

⛔ **74,120 is 4,120 over `BOOT_CEILING_TK` 70,000 — the twelfth post-diet reading over it.** Shrink-only (`s240-D2` / `s241-D1`). The next lever is unnamed: boot is still one undecomposed figure (`ds-025` item 1).

⚠ This file is NOT in `notes/_GAUGE-LOG.md`; the figure is stated once there, in the `post-mortem #287:` line at wrap (`s241-D2`).
