# WRAP MEMORY HOOK — #278 — for `wrap-278-the-cloud-move-and-the-seed.md` + one index line
(The memory directory is the claude.ai Project store and is unreachable from this seat — the seat limit is now STRUCTURAL, only the CONDUCTOR'S seat can write it. The conductor copies this file's body verbatim to `wrap-278-the-cloud-move-and-the-seed.md` in the Project memory and adds the index line to `index.md`, newest-first, at the #279 opener.)

## `index.md` line (insert as the newest wrap line, above #277's ★★★)
- [★★ #278 WRAPPED · HOUSEKEEPING, NO GRAPH WORK BY DESIGN · 1 RULING 603 → 604 · THE MEMORY DIRECTORY IS NOW THE CLAUDE.AI PROJECT STORE · BOOT 72,110 REAL n=1, NOT A RE-BASE](wrap-278-the-cloud-move-and-the-seed.md) — ⛔ READ `_HANDOFF-129-the-cloud-move-and-the-seed.md` FIRST, it OUTRANKS `_CHAIN.md` and does NOT replace `_HANDOFF-128`, whose "#278 FIRST MOVES" are now #279's, unchanged · **FABLE 5.1, no lanes** · ★ **`s278-D1` closes the `s277-D13` clause on his *"Ill go with you, lets just move on to the other graph work"*: the thin slice is a SEED composed once at step 1 by `_compose_slice.py`; ASK is the ON-DEMAND DOOR reading the Constitution live** · ⛔ **ONLY THE CONDUCTOR'S SEAT CAN WRITE CLOUD MEMORY — structural; the wrap sub writes `notes/_lanes/<n>/WRAP-MEMORY-HOOK.md`, the conductor places it at the NEXT opener (`d7b8d72` did #277's)** · ⛔ **there is NO `MEMORY.md` in the repo and never was** · the boot index did NOT come across (~200 flat files + an empty `index.md`); account-wide on READ, Apollo-only on WRITE; index cap 49,152 B; the re-import risk is ONE-WAY · ⚙ boot **72,110 real n=1** vs the 70,794 floor = +1,316, **NOT a re-base**; FILL 138,006 declared / 152,476 measured; `/sessions` **96.6%** · ⛔ showroom 108 stale (standing, HIS), resolver FAIL(6), lane-ownership 2/3 · **repo is the record, memory is the accelerator — UNCHANGED**

## File body
---
name: wrap-278-the-cloud-move-and-the-seed
description: "#278 (2026-09-16) wrapped — a housekeeping session with no graph work by design: the Cowork memory directory became the claude.ai Project memory and the environment was MEASURED rather than assumed; one ruling, s278-D1, closed the s277-D13 clause (the thin slice is a seed, ASK is the on-demand door); boot measured 72,110 real n=1 and was NOT re-based"
metadata:
  type: project
---

**#278 = THE CLOUD MOVE AND THE SEED.** ⛔ READ `_HANDOFF-129-the-cloud-move-and-the-seed.md` FIRST — it OUTRANKS `_CHAIN.md` and does NOT replace `_HANDOFF-128`, whose § "#278 — FIRST MOVES" and § "STRUCTURAL REDS" are live and become **#279's**, unchanged. Dave's words verbatim in `notes/_lanes/278/DAVE-RULINGS-2026-09-16.md`. Conductor **Fable 5.1**, **no lane seats**, delegated wrap.

**Ruling (1, by `_inscribe_ruling.py`, 0 deletions — `knowledge/_rulings.json` 603 → 604, span `15  0`):** **`s278-D1`** closes the clause `s277-D13` had recorded as an open question, on his *"Ill go with you, lets just move on to the other graph work"* — **the thin slice is a SEED**, composed once at step 1 by `_compose_slice.py`, after which the session works from its own state; **ASK is the ON-DEMAND DOOR**, called mid-session when the designer's next prompt needs a node the seed excluded, and it **reads the Constitution live**, so a ruling inscribed after the seed was composed is still answerable. Seed and door under one contract. ⇒ **an input to #279's reader lane.**

**What moved, and it is almost nothing — MEASURED, not assumed.** The Cowork memory directory **is now the claude.ai Project memory** (import 2026-09-16 03:14–17Z, `sources: [cowork-import]`). Sandbox (aarch64 Ubuntu 22.04), FUSE mount, git, push, CI, delete-guard grant, chromium recipe, tiktoken-first — **all unchanged**. His ruling, going with the recommendation: **the repo is the record, cloud memory is the accelerator** — which `_RUNBOOK-capture-ritual.md` step 3 already said; the rule has a new place to point at and nothing else moved. ⛔ **There is NO `MEMORY.md` in the repo and there never was** (`git ls-files` reads 0) — the old one was the Cowork LOCAL index. ⚠ The **boot index did not come across**: ~200 flat files and an EMPTY `index.md`, restarted from the #277 line, pre-import index preserved verbatim in `hook-overflow-2026-09-02-240` and `MEMORY-ARCHIVE`. Store is **account-wide on READ** (other Projects' subtrees visible) and **Apollo-only on WRITE**; index file cap **49,152 B**, compaction owed periodically with the dream pass as its seat; ⛔ the forward risk is **ONE-WAY** — a re-import could overwrite cloud-only lines, harmless only while the repo holds everything durable.

**The seat limit is now STRUCTURAL, and the runbook took the line.** Only the CONDUCTOR'S seat can write cloud memory; a delegated wrap sub cannot reach the store at all. The pattern, proven at #278: **the sub writes `notes/_lanes/<n>/WRAP-MEMORY-HOOK.md` and the conductor places it at the NEXT opener** (cold seat, ≈4K tokens) — `d7b8d72` placed #277's exactly that way. ✅ **`knowledge/_RUNBOOK-capture-ritual.md` step 3 takes this BY ADDITION at the #278 wrap** (`19  0`, no existing line rewritten).

**Gauge, and it is the figure he asked for by name** (*"what is the boot measurement it way over the 70K now isnt it?"*): **BOOT 72,110 real, n=1** against the `s129-D1` floor of **70,794** — **+1,316, inside noise plus a hair, and NOT a re-base**; `BOOT_FIRSTTURN_TK` untouched, n published as n=1. What is genuinely owed is the floor's **label** ("cloud listing replaces MEMORY.md", ≈8K either way), which is a wording fix and not a move of a constant. **FILL 138,006 real / 33 turns DECLARED at his check-in; 152,476 real / 44 turns MEASURED at the wrap seat** against the conductor's own transcript (first user record = his opener, boot agreeing to the token) ⇒ **delta 14,470**. ⛔ **`/sessions` 96.6%, 331,300 KB free** — at 100% no session boots (#227/#228 class), nothing in the repo fixes it, and the symptom is a session that will not start. **No `subs` line** — one delegated seat, cost not measurable from inside it, and absence is never defaulted.

**His posture, and it is the reason the window went on measuring:** *"okay as long as it's safe we have fallen foul of silent and creeping failures in the past"* · *"okay, i have no choice really, lets do it"* · *"whatever you think is right, but these are the only implications right?"*.

**Declared gaps at the close:** showroom **108 pages stale** — a standing declared gap since #273, carried on every wrap banner, and **regeneration is Dave's call** · `compliance/README.md` 31 vs a corpus of 55 · `A2-AUDIT.md`'s "316" vs 363, correction by addition still owed · `_validate_roles_resolve.py` **FAIL(6)** · `_validate_lane_ownership.py --selftest` **2/3** · `.git/_orphan-locks/` NOT emptied (his) — ⚠ the handoff's "72 entries" did not reproduce at the wrap seat: `ls` reads **12 top-level**, `find` reads **82 recursive**, both published, the inscribed figure not rewritten.

**How to apply:** **#279 = #128's first moves, unchanged** — (1) land the icons: `gen_kg_icons.py --land --ratified s277-D4` after RIV's A3/A4 pre-land fixes; (2) the Constitution wave in order: reader + ASK → explorer relabel → scope → verbs → tokens, with **`s278-D1` now an input to the reader lane**. Fable on judgement lanes; **Fable check + Fable verify against the LIVE FILE is the shape**. Lanes only — the conductor's window is for judgement.

Related: [[wrap-277-the-constitution-and-the-thin-slice]] · [[wrap-276-the-loose-ends-tied-off]].

---
⬛ PLACEMENT OWED: this file has NOT been placed. The conductor copies the body above to `wrap-278-the-cloud-move-and-the-seed.md` in the claude.ai Project memory and adds the index line to `index.md` at the #279 opener, then appends a `✅ PLACED <date>` receipt here — the #277 shape.
