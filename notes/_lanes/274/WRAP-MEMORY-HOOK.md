# #274 WRAP — MEMORY HOOK, FOR THE CONDUCTOR TO PLACE

⛔ **THE MEMORY STORE IS READ-ONLY FROM THE WRAP SEAT BY THE BRIEF.** This file is the hook TEXT and
its one `MEMORY.md` index line, written in the `s271-D4` form (every open item is phrased as a
QUESTION PUT, never as a state of the world) so the conductor can place it verbatim. It is not a
memory write and does not claim to be one.

---

## (1) THE ONE `MEMORY.md` INDEX LINE — place at the top of the ★★★ session run

```
- [★★★ #274 WRAPPED · 12 RULINGS 566 → 578 · THE LIST/CARD LINE IS SURFACE ALONE · 470 GUIDELINE RULES ENTERED THE GRAPH](wrap-274-the-line-and-the-rules.md) — ⛔ READ `_HANDOFF-125-the-line-and-the-rules.md` FIRST, it OUTRANKS `_CHAIN.md`; `s274-D1..D5` came from a MODEL HE VOLUNTEERED (containers over rows), not from the page's options, and his one correction — *"I think that actions might be allowable in a simple list in actuality"* — made the line SURFACE ALONE, actions a row property · `s274-D6` = *"1"*, list-items is the DEFAULT of `record-list`, table takes the comparison test · `s274-D7..D12` on his *"go"* landed 470 `rule:<id>` nodes (521 nodes / 634 edges / 19 declared nulls, explorer v1.11) — `s269-D1` STEP 2 DONE, principles are step 3 · ⛔ a `null` or an `a` WITH A SENTENCE ATTACHED is a SEQUEL, and the READ-BACK is where it becomes a plan (RK-3/RK-4/RK-6) · ⛔ the quote gate returned 0/9 on his own words — its index covers neither `notes/_lanes/` nor `says`, so verify against the lane file · boot 81,898 = TENTH breach; 21 local UNPUSHED
```

## (2) THE HOOK FILE — `wrap-274-the-line-and-the-rules.md`

```markdown
---
metadata:
  provenance: 274 · 2026-09-15
  status: observed
---

# #274 — the line and the rules

**⛔ READ FIRST:** `_HANDOFF-125-the-line-and-the-rules.md` — NEWER than `_CHAIN.md` and it
OUTRANKS it. His words verbatim, in order: `notes/_lanes/274/DAVE-RULINGS-2026-09-15.md`.
Narrative: `_DECISION-HISTORY/2026-09-15-274-the-line-and-the-rules.md`.

## What landed (every claim has a receipt)

- **`s274-D1`…`s274-D5` — THE LIST/CARD LINE IS SURFACE ALONE** (`04c51c6`, 566 → 571). He did
  not pick options off `notes/_PROPOSAL-list-vs-card-2026-09-15-v1.html`; he described a model
  nobody had proposed — **containers (simple-list · structured-list · card-list) over rows**,
  with *"card-list-row - (this is basically the same as simple-list-row i guess, so we may not
  need the differentiation )"* — and the four LC decisions fell out of it. ★ **The conductor's
  read-back was corrected in exactly one clause**, *"I think that actions might be allowable in
  a simple list in actuality"*, which struck the actions test and left **surface alone: a record
  is a CARD when its container draws a bordered surface around each record; actions are a ROW
  property legal in any container.** Headers closed on *"okay i think you are right on 'headers
  on card lists' lets get this done"*.
- **ENACTED ON THE META, BY TEXTUAL SPAN** — `58f56ef` (lane LCE, Opus, 121K):
  `knowledge/components/list-items.meta.json` gains three `$level: container` variants, six rows
  marked `$level: row`, the `when` at `priority 60`, `account-card` a `$note` and no `provides`.
  Gates: resolver **FAIL(6)** (all six inherited `data-grid`), drift **108/108**,
  `_validate_kg.py` OK, compose PASS.
- **`s274-D6` IS THE CHARACTER *"1"*** — `3a752d1`: **`list-items` is the DEFAULT provider of
  `record-list`**; table wins only on the comparison test (a field read DOWN a column). Two
  `roles.json` `when` strings replaced by span; `"<= 3 fields per item"` retired as contradicted
  by his own `s274-D1`/`s274-D4`.
- **`s274-D7`…`s274-D12` — 470 GUIDELINE RULES ENTERED THE GRAPH; `s269-D1` STEP 2 IS DONE.**
  `0bfaf91` (lane RK, Opus, 203K) built `knowledge/gen_kg_rules.py` READ-ONLY (12 selftest bites,
  13 mutants caught) and landed nothing; his export **RK-1 a · RK-2 a · RK-3 null · RK-4 a ·
  RK-5 a · RK-6 a** was read back whole and *"go"* inscribed the six (`f6c5da8`, 572 → 578):
  node kind `rule:<id>` · four edge types `definedIn` / `cites` / `enforcedBy` / `flaggedBy`
  RATIFIED · destiny is an ATTRIBUTE not an edge · the 19 unresolved SC citations DECLARED
  `ref:null` · store and reader in the SAME commit · rule → component out of wave 1.
  `00f3a87` (lane RL, Opus, 139K) landed `knowledge/_rule_nodes.json` — **521 nodes / 634 edges
  / 19 declared nulls**, explorer **v1.11**, driven in chromium, screenshot reviewed.
- **`P-274-1` · `P-274-2` · `P-274-3` parked**, each from a sentence of his: edit mode shows the
  alternatives · the 19 SC criteria into the compliance corpus · a meta cites the rule.

## ⛔ The lesson worth keeping

**A `null` WITH A SENTENCE, AND AN `a` WITH A SENTENCE, ARE THE SAME EVENT — A SEQUEL — AND THE
READ-BACK IS WHERE IT BECOMES A PLAN.** RK-3 came back empty with a durability question; RK-4 and
RK-6 came back accepted with *"Can we have A and then C…"* and *"So A for now I guess and then D,
is that correct?"*. Re-asking any of the three would have got a worse answer. Reading the whole
set back with the sequels written as staged follow-ups got one word: *"go"*.

★ **AND THE PAGE WAS NEARLY RIGHT, BUT THE SENTENCE THAT FIXED IT WAS HIS.** LC-1's recommendation
was *surface plus record-level actions*. A lane allowed to resolve it from its own research would
have shipped the actions clause, and no gate could have seen the error.

⚠ **THE QUOTE GATE RETURNED 0/9 ON HIS OWN WORDS AND THE REFUSAL IS ABOUT THE INDEX.**
`knowledge/_quote_gate.py` searches a Memento index that covers neither `notes/_lanes/` files nor
the `says` field of `_rulings.json`, so his sentences are invisible to it on the day he speaks
them. Verify against the lane file, exact substring. Repointing the corpus is ruling-shaped.

## Open — EACH ONE PUT TO DAVE AT THE WRAP CALL, none of them a state of the world

1. **Lane LCE's three proposals** — the `props.type` enum spanning both levels, `priority 60` vs
   the `roles.json` naming table, the `roles.json` `when` — put to Dave at the wrap call. ⚠ And
   **`bordered-per-record` / `$level: "typeset"` are the LANE'S words**, now sitting on a ruled
   meta.
2. **`knowledge/_rule_nodes.json`'s `$description` still says "PROPOSED … NOT RATIFIED"** —
   generator text carried into a landed, ratified store. One textual span at #275; put to Dave at
   the wrap call as to who makes it.
3. **Lane RL's opens** — the chip word `guidelinerules` vs "Guideline rules", and the **99 of 634
   edges that draw only with the Governance + Guidelines layers on** — put to Dave at the wrap
   call.
4. **The `example` key** on the 13 `when-fields.json` entries the 26 legacy ones lack, and **the
   4 near-dupe pairs** — put to Dave at the wrap call; carried unchanged from #273.
5. **`input` sub-roles · the 6 `data-grid` reds (#261) · `P-272-2`…`P-272-4`** — put to Dave at
   the wrap call; carried.
6. **The showroom, 108 pages stale for a second wrap** (`:is(…)` → `:where(…)`, a specificity
   diff) and **the boot ceiling / the #243 double-count** — put to Dave at the wrap call; a wrap
   seat may land neither.
7. **The push — 21 local commits** — put to Dave at the wrap call.

## Cautions for the next seat

`model: opus` every lane · verifier in the same wave · `_checkin.py` every ~10 turns ·
screenshot + text diff before presenting · **textual span only** for `_rulings.json`, metas and
every JSON store · never `git stash` · **never `gen_kg_edges.py`** · **never `_build_all.py`** ·
`knowledge/_roles_drift.py` is the drift gate — run it before any claim about roles ·
`_parked.py --check` is the sidequest register · the RK/RL shape (read-only generator → review
page → his export → land lane) is the proven route for #275's 145 principles ·
**21 commits local and UNPUSHED; the push is Dave's word.**
```

---

## (3) STALENESS CHECK THE CONDUCTOR SHOULD RUN BEFORE PLACING THIS

`s271-D4` requires the "Open" list above to be **re-read against `knowledge/_rulings.json` after the
ritual's last beat** and anything the session's own rulings closed to be **struck BY ADDITION, naming
the ruling id** (`… — CLOSED by sNNN-DN`). That re-read was performed at this wrap's 5b beat against
the store at **578 rulings**: **none of the seven items is closed by `s274-D1`…`s274-D12`**, and each
is written as a question put rather than as a state of the world. If Dave answers any of them at the
wrap call, strike it by addition before placing the file.
