## ⏱ LATEST DELTA — 2026-09-15 (Tue from `date`) (**#274**, ONE DAY, conductor **Fable** + **3 OPUS LANE SEATS** (list-card-enact · rules-kg · rules-land) with this **OPUS 5** wrap sub, DELEGATED — ★★★ **THE LINE AND THE RULES: TWELVE RULINGS, THE LIST/CARD LINE IS SURFACE ALONE, AND 470 GUIDELINE RULES ENTERED THE GRAPH**)

*(WHY/HOW: the arc is two halves of one instruction — finish what the graph cannot answer, then feed it. The first half had been open since #272 parked it and #273 researched it: `P-272-1`, the list-vs-card line. #273's finding was NEGATIVE — thirteen external systems, none of which draws the line on field count — and it left LC-1…LC-4 on a page with nothing ruled. #274 did not re-ask those four questions. Dave answered them by describing a model nobody had proposed: two levels, containers over rows, with the card-list row admitted to be the same object as the simple-list row. The four page decisions then fell out of his model rather than being chosen from the page's options, and the one correction he made to the conductor's read-back — actions are allowable in a simple list — is what turned LC-1 from "surface plus actions" into **surface alone**. That is the shape worth keeping: the recommendation on the page was very nearly right and the sentence that fixed it was his, not the lane's. The second half is `s269-D1` step 2, the 470 tagged guideline rules. It ran in the RK/RL shape #273 proved: one read-only Opus lane builds the generator with a dry-run, a selftest and a `--land --ratified` gate that REFUSES without a recorded ruling id, publishes a review page of six decisions, and lands NOTHING; Dave exports; the conductor reads the whole set back in one message; his one word inscribes; a second lane lands. Two of his six answers were not plain accepts — RK-3 came back `null` with a question about durability, and RK-4 and RK-6 came back `a` with *"and then C"* and *"A for now I guess and then D"* — and the read-back is where those became sequenced follow-ups rather than ambiguity. ⛔ **The wrap seat inscribed nothing and ruled nothing; every open item below is a question put, never a state of the world.** Sources: `notes/_lanes/274/DAVE-RULINGS-2026-09-15.md` (his words, in order, verbatim) · `_HANDOFF-125-the-line-and-the-rules.md` · `_DECISION-HISTORY/2026-09-15-274-the-line-and-the-rules.md`.)*

- ⛔★★★ **READ `_HANDOFF-125-the-line-and-the-rules.md` FIRST — IT IS NEWER THAN `_CHAIN.md` AND THEREFORE OUTRANKS IT.** The #274 conductor wrote it at **FILL ≈185,000 real — PAST the 180,000 working line and INSIDE the ~220,000 TOLERATED band** (`s272-D93`, armed at #273 as an advisory arm) — BEFORE this ritual ran, so nothing in it depends on a delegated wrap remembering it. It carries his words verbatim, the seven commit receipts, the DO-NOT-RULE list and #275's first moves.

- ★★★ **`s274-D1`…`s274-D5` — THE LIST/CARD LINE IS SURFACE ALONE, AND THE MODEL IS HIS, NOT THE PAGE'S.** He opened by describing two levels rather than answering the page: *"we have intersecting components here, so we have a simple-list-row, structured-list-row card-list-row - (this is basically the same as simple-list-row i guess, so we may not need the differentiation ). then we have simple-list, structured, card-list (the containers)."* The conductor read it back as containers-over-rows and he corrected exactly one clause — *"I think that actions might be allowable in a simple list in actuality"* — which struck the actions test out of LC-1 and left **`s274-D1`: a record is a CARD when its container draws a bordered surface around each record; anything unbordered is a list row at any field count.** **`s274-D2`** = ONE meta whose variants are the three CONTAINERS; **`s274-D3`** = `account-card` stays `$not-a-provider` (a stack of account cards is the card-list container hosting account rows); **`s274-D4`** = no cardinality number is a gate, the comparison test decides; **`s274-D5`** on *"okay i think you are right on 'headers on card lists' lets get this done"* = a list-level heading is optional on all three containers, a per-card title is a row field. `04c51c6` inscribed, **566 → 571**.

- ★★★ **THE ENACTMENT WENT ONTO THE META BY TEXTUAL SPAN, AND THE LANE NAMED ITS OWN WORDS AS ITS OWN.** `58f56ef` (lane **LCE**, Opus, **121,000 real**): `knowledge/components/list-items.meta.json` gains the three `$level: container` variants, the six record rows are marked `$level: row`, the `when` predicate carries D1's surface clause and D4's comparison test at **`priority 60`**, `purpose` is amended BY ADDITION and `account-card` takes a `$note` recording D3 with **no `provides`**. Every span reconstruction-proven. Gates at the lane seat: `_validate_roles_resolve.py` **FAIL(6)**, all six inherited `data-grid`; roles drift **108/108, 0 silent**; `_validate_kg.py` **OK**; `_validate_compose.py` **PASS**. ⚠ **DECLARED AND NOT RULED:** `bordered-per-record` and `$level: "typeset"` are the **LANE'S** vocabulary, and its three proposals — the `props.type` enum that spans both levels, `priority 60` against the `roles.json` naming table, and the `roles.json` `when` for `list-items` — are **Dave's**.

- ★★ **`s274-D6` IS ONE CHARACTER, AND `P-274-1` IS A REQUIREMENT, NOT A RULING.** On who is the default provider of `record-list` he answered *"1"* ⇒ **list-items is the default ("show me the items") and TABLE takes the comparison test — it wins only when a field must be read DOWN a column across records** (`3a752d1`; two `roles.json` `when` strings replaced by span, reconstruction-proven, and `"<= 3 fields per item"` retired as contradicted by `s274-D1`/`s274-D4`). Immediately after it he added *"note that in edit mode, when we build it, the user can be presented with the alternatives we have defined."* — a requirement on the composer's EDIT MODE and **not a ruling on a mechanism**: the resolver's runner-up providers are the alternatives to show. Parked as **`P-274-1`** with its tripwire on `knowledge/_compose_slice.py` (`3b12185`).

- ★★★ **`s274-D7`…`s274-D12` — THE 470 TAGGED GUIDELINE RULES ARE IN THE GRAPH; `s269-D1` STEP 2 IS DONE.** `0bfaf91` (lane **RK**, Opus, **203,000 real**) built `knowledge/gen_kg_rules.py` READ-ONLY — dry-run, `--land --ratified` refusing without a recorded ruling id, **selftest 12 bites with 13 mutants all caught** — measured **521 nodes / 634 edges / 23 declared nulls** dry, and **landed nothing**. His export (`notes/_lanes/274/rules-kg/rules-kg-decisions-2026-09-15.json`) came back **RK-1 a · RK-2 a · RK-3 null · RK-4 a · RK-5 a · RK-6 a**, four notes verbatim; the conductor read the whole set back and his *"go"* inscribed **`s274-D7`…`s274-D12`** at `f6c5da8`, **572 → 578**: node kind **`rule:<id>`** · four edge types **`definedIn` · `cites` · `enforcedBy` · `flaggedBy`** RATIFIED into the closed vocabulary · **destiny is an ATTRIBUTE on the node, not an edge** · the **19 unresolved SC citations are DECLARED `ref:null` with a note, not dropped and not blocking** · store and reader land in the SAME commit so no unread file exists in the repo · **rule → component stays out of wave 1**, the durable route being a component's meta citing the rule. `00f3a87` landed it: **`knowledge/_rule_nodes.json` — 521 nodes / 634 edges / 19 declared nulls, 470 of the nodes `rule:`** — with the explorer at **v1.11** reading it behind a "Guideline rules" chip at RULE 470, driven in chromium with 0 console errors and **screenshot-reviewed by the conductor before it was presented** [[art-director-reviews-lane-output-268]]. **`P-274-2`** (the 19 SC criteria into the compliance corpus, from his *"Can we have A and then C"*) and **`P-274-3`** (metas cite rules, from *"So A for now I guess and then D"*) are parked, not ruled.

- ⬛ **HIS DIRECTION, EXPLICITLY NOT GIVEN AN `sNNN`:** *"this is just getting teh KG working harder we'll work on the demo/prez after we get this all sorted"* — the KG-gaps page is KG WORK, not demo prep, and the demo waits. It is carried as ASKED/DIRECTION the way #270's § C was, because a direction inscribed as a ruling is a false inscription with a good excuse.

- ⬛ **STILL HIS, AND EXPLICITLY NOT RULED AT THIS WRAP — the handoff's DO-NOT-RULE list, carried word for word, every item a QUESTION PUT and not a state of the world (`s271-D4`):** lane LCE's three proposals (the `props.type` enum spanning two levels, `priority 60` vs the naming table, the `roles.json` `when`) · `bordered-per-record` and `$level: "typeset"` as the lane's words · lane RL's opens — the chip word `guidelinerules` / "Guideline rules", and that **99 of 634 edges draw only with the Governance + Guidelines layers on** · the `$description` on `knowledge/_rule_nodes.json` still reading *"PROPOSED … NOT RATIFIED"* (generator text, one span at #275) · the `example` key on the 13 `when-fields` entries against the 26 legacy entries that lack it · the 4 near-dupe pairs · the 6 `data-grid` `with`-slug reds (#261) · `input` sub-roles · `P-272-2`…`P-272-4` · the 108 stale showroom pages · the **push (20 local, and this wrap's commit makes 21)**.

- ⚙ **GAUGE, MEASURED AND NOT NARRATED:** FILL **185,505 real / 56 turns** first-hand at this wrap seat (`knowledge/_checkin.py`, reading **the conductor's own transcript** `596082e0-…jsonl` — the same window his declaration describes, so the subtraction is legal) against **≈185,000 declared** at the brief cut ⇒ **delta 505**. Both readings are **PAST the 180,000 working line and inside the ~220,000 TOLERATED band**, 34,495 to tolerance. ⛔ **BOOT 81,898 — a TENTH CONSECUTIVE breach** of `BOOT_CEILING_TK` 70,000, which `s240-D2`/`s241-D1` make SHRINK-ONLY: the remedy is to cut the boot and raising the literal is **Dave's word alone**, never a price a wrap pays to unblock itself. **subs 463,000 real (n=3)** — LCE 121,000 + RK 203,000 + RL 139,000 — ⚠ **QUOTA, never FILL, never summed into the fill numbers.** `/sessions` **97% full**, 375M free.

- ⛔★★★ **FOUR BLOCKING GATE REFUSALS ARE CARRIED IN THE `#243` FORM AND EVERY ONE OF THEM IS ANOTHER SESSION'S TESTIMONY.** The boot-drift **ceiling breach** (ten post-diet readings now over 70,000) and **three boot double-counts — #243, #264 and #272**. `notes/_GAUGE-LOG.md` is **APPEND-ONLY**, and repairing #243's, #264's or #272's block from this seat would be rewriting a session's own testimony about itself. ★ #269's separation stands and is re-stated rather than re-derived: **#264's and #272's are real restatements inside their own blocks; #243's is a MIS-ATTRIBUTION, and therefore a GATE defect and not a record defect.** What to do about either — repair the gate's attribution, or move `BOOT_CEILING_TK` — is **Dave's alone**.

- ⛔ **THE SHOWROOM IS STILL 108 PAGES STALE AND THE DIFF IS STILL A SPECIFICITY CHANGE.** Declared at the #273 wrap, untouched here: `gen_showroom.py --check` reads 108 of 137 pages stale and the payload diff is `:is(…)` → `:where(…)`, one line per generated page. It predates both sessions, it is a **108-page visual diff**, and no wrap seat may land it unreviewed. Committed again with `SHOWROOM_ACK`.

- ✅ **REDS HEALED BY THIS RITUAL, AND ONE SEAT LIMIT DECLARED:** the stale `notes/_RULINGS.html` at step 4d (re-rendered against the twelve new rulings, `--check` FRESH) · the **stale Memento index at 2g** (today's eight commits' worth of notes) · both *"not today"* date arms at steps 1 and 2. ⛔ **MEMORY (step 3) IS READ-ONLY AT THIS SEAT BY THE BRIEF — A DECLARED SEAT LIMIT, NOT A SKIP.** The hook body and its one `MEMORY.md` index line are written in the `s271-D4` form at `notes/_lanes/274/WRAP-MEMORY-HOOK.md` for the conductor to place; the `s271-D4` re-read against `knowledge/_rulings.json` (578) was run at 5b and **nothing in the open list is closed by `s274-D1`…`s274-D12`**.

- ⚠ **DECLARED SKIPS, EACH WITH ITS SIZE:** **2e** — DO-FIRST is unchanged this session, no notice or tombstone was minted, nothing retro-tagged (0 lines moved). **The dream pass was NOT run** — newest is **pass 12, `notes/_dream/2026-09-13-proposals.md`, 2 days old**; its enact commit exists (`c455bc0`) and the brief says report the age, not run one. **Three instrument files were already dirty when this ritual opened** — `notes/_REHEARSAL-LOG.jsonl` and `notes/_dream/_GRADE-DECISIONS.jsonl` are APPEND-ONLY instrument logs written by `_checkin.py` and the grader at this seat, and `knowledge/_gen_titles_receipt.json` is `_gen_titles.py`'s own receipt; all three are committed with this wrap as the instrument output they are, none was hand-edited.

