# #291 wrap brief — cut by the conductor (Fable 5.1) at Dave's "okay push and wrup", 2026-09-21 08:27 BST

provenance: 291 · session opened Sunday 2026-09-20 (morning), all nine lanes and both commits landed 2026-09-20; the push and this wrap are Monday 2026-09-21. ⛔ DATE SPLIT, the #241 shape by ADDITION: NOTHING is re-dated; the wrap's own stamps carry 2026-09-21 so the gate's `is not today` check grades a true statement; the report stem is `2026-09-21-291-W-wrap.md`.
status: brief (every figure here is DECLARED; the wrap seat re-measures and publishes both readings where they differ)

## The session in one line

Dave's carried ask — two workers in overalls in place of the robots — was drawn and ruled BY HIS EYE six times in a row (L → L6), then put on deck v12 slide 4 with the robots kept on 5; slide 10 went from five cells to three on his screenshot (Parts = gearbox, Knowledge, Proficiency) and the three drawings were scaled to fill their cells. Nine Opus lanes, NONE in seat. Two commits, pushed. Nothing inscribed — he did not say "inscribe".

## Dave's words

ALL in `notes/_lanes/291/DAVE-RULINGS-2026-09-20.md` — eleven entries, verbatim, in order, with what was done on each. Quote, never paraphrase. His reference images and crops were READ and NOT filed (the #289 stock-image precedent: "I wouldn't copy this in any way").

## What was built (all committed)

- `notes/_lanes/289/illustration/line-workers.html` v6 + `line-workers-rest.png` / `-orbit.png`; v1–v5 preserved beside it (`line-workers-v{1..5}.html` and their PNGs). Lane reports L, L2, L3, L4, L5, L6 at `notes/_subreports/2026-09-20-291-L*-*.md`. Six versions in one day is the record of his eye at work; the constants are in each report (v5: SH_LZ 88→76, WK_DA 126→190; v6: silhouette-only torso prisms, GRIP_X 72 / GRIP_Z 194, orbit wrist sliver 4.0 px).
- `notes/_DEMO-SLIDES-apollo-2026-09-20-v12.html` — from v11 (untouched). Lane P: workers IIFE on s4 (`#lw`, print `#lwPrint`, the three deck manners), robots on s5 unchanged; the GEARBOX displaced from s4. Lane P2: slide 10 = three-up, headline "Three things she is made of.", Parts = gearbox baked from off-screen `#gbHost`, Tools and Process removed, `amPrint` (arm) now has NO consumer, `ctPrint` still on s6. Lane P3: image box 340×135 → 340×229.5 CSS px, cell 330 → 424 px, slack taken from top/bottom, no re-bake. 12 slides / 13 sections measured at every step; 0 page errors. Reports `2026-09-20-291-P-deck-v12.md`, `-P2-slide10-three-up.md`, `-P3-slide10-bigger-images.md`; build scripts + screenshots under `notes/_lanes/291/P*/`.
- Store rows W-291la…lf, W-291pa…pc added for the nine filed reports (doc-row gate passed both commits).
- Commits: `b9ab75b0` (workers + v12 + slide 10 three-up), `b721144a` (bigger images). **PUSHED and VERIFIED: remote master == local b721144a** (the script's --push on his word). CI: NOT yet read — read it back at this seat on `head_sha=b721144a`, polled to completion; every red on `gates` steps 5/6 and `release` step 12 was inherited at #290 — say whether it still is.

## Lanes

NINE, all `Agent` at depth 1, all Opus, NONE in seat: L ~195k · L2 ~206k · L3 ~215k · L4 ~171k · L5 ~163k · L6 ~165k · P ~113k · P2 ~102k · P3 ~91k — subs ≈1.42M DECLARED (n=9, from the harness usage lines). Re-measure with `_checkin.read_fill` over the sub-transcripts and publish both. All nine reports carry `RULING-SHAPED QUESTIONS` / `COUNTS:` / `REPLAY-THESE:` — check, and say how many do.

## Gauge, declared

- Boot 74,155 real (ten under #290's 74,165; the sixth reading on the browser-blocked setup, spread across the last four now 19 tokens). State it ONCE, in the `post-mortem #291:` line (`s241-D2`).
- Seam BEFORE the first commit: **FILL 220,302 real / 42 turns · ⛔ OUTSIDE TOLERANCE 220,000 — wrap**. After the commit: **245,652 / 66**. Both quoted to Dave. ★ He answered the reading with ANOTHER NOTE (the bigger images), and the conductor took it as "one more, not wrap yet" and said so in chat; the wrap came on his next word. ⇒ third session where the instrument fired; the FIRST where the human overrode it once before obeying — publish that, it is evidence about the tolerance arm (`s283-D1`, advisory vs blocking is still his).
- Wrap seat: re-measure with `_checkin.read_fill` against the conductor's transcript `173634f1-25bc-4033-85d7-c37943a20c1c.jsonl` and publish both readings.
- INSEAT arm of the seam read 2,229,807 cl100k "own tool output" — that figure is the conductor's transcript from start (no prior seam marker), dominated by lane returns and image reads; declare it, do not treat it as in-seat lane work.

## Declared by the conductor, carry forward verbatim

- ⛔ **The mount defect became total this session**: every git write stranded an un-unlinkable `.git/index.lock`, so the commit script's per-path `git add` loop failed on the second path. Three refusals (REUSED-MSGFILE: my own T3 prefix on line 1 — #289/#290's trap a third time; doc-row gate: 8 unrowed reports; then the lock). **Fixed by runbook step 0: `mcp__cowork__allow_cowork_file_delete` on `.git/index.lock`** — the per-session delete grant [[git-lock-mv-not-rm]] names as the prevention. After it, one clean run. The grant is active for this session; `rm` works. Say so in the handoff: **call the grant at the FIRST commit, before any git write.**
- `knowledge/_graph-mark-observations.jsonl` and `notes/_REHEARSAL-LOG.jsonl` were machine appends, committed with the work and named.
- `_CHAIN.md` went STALE the moment the store rows were added (the OPEN WORK block is generated from the store) — regenerate with `_gen_chain.py` before every commit that touches `_state.json`.
- Chromium in the sandbox per `chromium-in-sandbox-recipe`: installed once by lane L, reused by eight lanes.
- Dream pass 13's seven proposals are STILL unruled; P1's stale B3 sidecar printed again at boot.
- `_HANDOFF-130…141` open items all stand. Strike WITH RECEIPT: #290's owed item 1 (the two workers → v12 s4) — receipt `b9ab75b0` and his "okay almost, but lets just get it in the slide". Strike NOTHING else.

## Ruling-shaped, NOT inscribed

1. Six acceptances by eye and "okay almost" — acceptance, not inscription; v6 is on the deck with his "almost" still attached.
2. Parts = gearbox on slide 10 (read from his screenshot) vs #290's "the parts image on, slide 10" (the catalogue). The catalogue stays on s6. His.
3. Slide 10 is three cells; Tools and Process are gone; the arm drawing has no home in the deck. His.
4. The seam check overridden once by a note, then obeyed — advisory vs blocking is his.
5. Slide 4's copy was written against the gearbox and now sits beside the workers — reads fine to the conductor's eye; his.

## Owed to #292, in order

1. His slide-by-slide read of v12 — gates the four D2 layout flags and the dark robots slide; still not given.
2. Proposal v3 (premise as beat zero, both framings).
3. The week's plan from the strand map — **Friday the 25th is the internal, FOUR days out at this wrap**; Monday's overview-dashboard definition and first cold one-shot was the plan's Monday and TODAY IS MONDAY.
4. `_drive_chart_engine.py` re-drive → release CI red (FOUR sessions old).
5. Index sharding (his pick, not started); the memory archive still at 162 B of headroom.
6. The arm drawing's home, if any.

## Ritual

Run `knowledge/_RUNBOOK-capture-ritual.md` in full, every step in order, as #290's wrap did (`notes/_subreports/2026-09-20-290-W-wrap.md` and `notes/_lanes/290/W/` are the model; `notes/_lanes/290/WRAP-MEMORY-HOOK.md` the hook model). Step 0 is already done (delete grant active). Handoff `_HANDOFF-142-<slug>.md` outranks `_CHAIN.md`; it does NOT replace `_HANDOFF-130…141`. Wrap-memory hook at `notes/_lanes/291/WRAP-MEMORY-HOOK.md`, then write it to Project memory from the wrap seat if you have the memory tools (topic file `wrap-291-<slug>.md` + an `index.md` line by targeted str_replace; MEMORY-ARCHIVE cap still 162 B of headroom — declare, don't truncate); if you do NOT have memory tools, say so at the foot of the hook and the conductor places it. Commit ONLY via `knowledge/_git_commit.sh` (fresh msgfile, NO T3 prefix on line 1, `SESSION_N=291`); then `--push` (his word "push" covers the wrap commit) and read CI back into the report, polled to completion. Next chat title from `python3 knowledge/_gen_titles.py --session 291`. File your report at `notes/_subreports/2026-09-21-291-W-wrap.md` with the `s218-D7` clause-4 headings. Return to chat a STUB only: commit sha, push verdict, CI colours, FILL measured vs declared, the next chat title, the handoff filename, and whether the memory hook was placed.
