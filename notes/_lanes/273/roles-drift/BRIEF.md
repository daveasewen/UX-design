# BRIEF — #273 lane RD: the ROLES DRIFT decision page

Repo: `/sessions/elegant-determined-planck/mnt/UX-design` (bash) = `/Users/daviewen/Documents/Claude/Projects/UX-design` (file tools).
Lane folder: `notes/_lanes/273/roles-drift/`. Output: `notes/_REVIEW-roles-drift-2026-09-15-v1.html`.

## The one source of numbers
`notes/_lanes/273/roles-drift/DRIFT.json` — emitted by `python3 knowledge/_roles_drift.py --json` at commit `4294b60`.
⛔ Every figure on the page is READ from this JSON at build time (bake it into the page as a `<script type="application/json">` block and render from it, OR generate the HTML from it with a Python builder saved as `notes/_lanes/273/roles-drift/_build_page.py`). Never type a number by hand. A number that is not in DRIFT.json does not go on the page.

## What the page is
A DECISION page for Dave (he is the bottleneck: recommendation first, one-word accept). It reports the drift between `knowledge/roles.json` (the RULED membership, s252-D1: 12 roles, 108 memberships) and the metas' `provides` (24 metas — the first authoring pass, s251-D12, which said wider scope is "re-judged once that pass is done, not decided now"). Only a meta's `provides` generates a `providesRole` edge (s270-D2), so the graph carries 24 edges where the ruling names 108.

Read first (5 min): `knowledge/roles.json` ($description, $not-a-provider), `notes/_lanes/270/roles-desk/ROLES-DRIFT.md` (the #270 measurement — numbers there are STALE, e.g. chart-panel was 16 then, 12 now; ours win), `knowledge/components/meta.schema.json` lines 381–383 and 859–865, `knowledge/when-fields.json` $description + $grammar, and two metas: `kpi-tile.meta.json` (a full DESK address) and `table.meta.json` (silent).

## Structure (in this order)
1. **Headline** (the finding in one sentence + the four figures: 108 · 24 · 84 silent · 4 roles at zero). State plainly: the drift is ONE-DIRECTIONAL — 0 contradictions, 0 dangling, 0 metas inventing a role, 0 not-a-provider carrying provides — the canon is thin, not wrong.
2. **Decisions** — a numbered list, recommendation FIRST in each, radio buttons + a notes textarea per decision, an Export button that produces JSON `{page, at, decisions:[{id, choice, note}]}` (copy the `exportObj` / download pattern from `notes/_REVIEW-when-harvest-whole-library-2026-09-14-v2.html` — including its `focusout` + `beforeunload` note flush, which fixed a lost-note defect). Also a "copy to clipboard" button. Persist choices in localStorage under a key naming this page (wrap in try/catch).
   - **RD-1 THE ADDRESS.** Write `provides: <role>` onto the 84 silent metas, copied from roles.json, by TEXTUAL SPAN (never `json.dump` a meta — the #179 reformat class). Graph goes 24 → 108 `providesRole` edges. Options: (a) RECOMMENDED — all 84 now, `input`'s 27 included (an address is legal without a gate; `_validate_roles_resolve.py` check 6 forbids priority-without-provides, not the reverse); (b) the 57 that carry a roles.json `when`, `input` waits; (c) leave at 24, roles.json stays a roadmap. Why (a): the membership is already ruled (s252-D1 "with the membership"), so this ENACTS, it does not rule; a resolver that can name substitutes without ordering them beats one that cannot see 78% of them.
   - **RD-2 THE GATES** (`when` + `priority` + `shape` + `answers` — the DESK address, authored not copied: roles.json's `when` strings are prose like "default, read-mostly" and do NOT parse under the closed field grammar). Scope of authoring pass 2. Options: (a) RECOMMENDED — the 49 silent providers of the 9 roles that are neither `record-list` nor `input`, one lane per role (or per 8–12 metas), verifier in the same wave, `when` in the closed grammar, `priority` unique per role; (b) the 4 zero roles first (feedback 11 + overlay 7 = 18), rest later; (c) wait. `record-list` (8) is HELD behind P-272-1 (list-vs-card: `list-items` and its neighbours cannot be gated until the definition exists) — see RD-4; `input` (27, every roles.json `when` null) is HELD behind the input sub-roles roles.json `$extension` expects (s251-D7 "not now").
   - **RD-3 THE 14 FIELD NAMES.** `_validate_roles_resolve.py` went FAIL(6) → FAIL(20) at `b46ea90` (#272's 15 inscribed `when`s): 9 metas use 14 left-hand field names that are not in `when-fields.json` (list them from DRIFT.json `metas_with_illegal_when_fields`). The 6 older reds are `data-grid` `with` entries without a `slug` (#261, unrelated). This was NOT declared at the #272 seam. Options: (a) RECOMMENDED — extend `when-fields.json` with the 14 names by ruling (s254-D2 closed the list so that a LANE cannot add a name; Dave adding is what it anticipated; the gates came from 13 systems' unanimous vocabulary); (b) rewrite the 9 gates in the existing 25 names; (c) mixed — Dave marks per name. Show the 14 names each with its meta and the clause, so (c) is a click not a re-read.
   - **RD-4 LIST-vs-CARD NOW?** P-272-1 blocks the `record-list` gates (RD-2) and nothing else on this page. Options: (a) RECOMMENDED — spin the P-272-1 research lane today, in parallel with RD-1 (definitions from the 13 harvested systems: simple-list · column-list · list-card; rows B-09, B-10, B-20, N-account-card); (b) wait for the tripwire.
3. **Per-role table** from `per_role`: role · json · agree · silent · j-when · a ZERO mark · the role's `definition`. Below each zero role, list its providers (slug + roles.json `when`).
4. **The 84 silent metas** — one compact table (role · slug · roles.json `when` or "—"), grouped by role, so Dave can scan what RD-1 writes.
5. **The 14 names** — table: meta · field · the clause it appears in (parse the gate half of `meta_when` for the clause containing the field) · roles.json's legal set is 25 names (from `legal_when_fields`).
6. **Receipts** — commit, script, corpus, and the exact command line. Footer: "Nothing on this page is a ruling. Dave rules; the lane enacts by addition."

## Design
Follow `/sessions/elegant-determined-planck/mnt/.claude/skills/swiss-design-system/SKILL.md` (read it). Univers-Next-class typography, a 12-col grid, one red (`#DA1A00` on white — the TWO-RED LAW s151-D1), generous whitespace, no decorative colour. Dark theme via `prefers-color-scheme` (the four-themes rule: test both). Must read at 1280 and at 400 (tables in `overflow-x:auto` wrappers). Self-contained single file, no CDN.

## Gates before you report
1. `python3 knowledge/_roles_drift.py --json | diff - notes/_lanes/273/roles-drift/DRIFT.json` → empty (the JSON you rendered is the live one).
2. A text-diff proof: extract every integer on the rendered page's headline + decisions + per-role table and assert each appears in DRIFT.json (write `notes/_lanes/273/roles-drift/_numbers_check.py`; it prints PASS/FAIL per number). No number may be on the page that the JSON does not contain.
3. Screenshots at 1280 light, 1280 dark, 400 light → `notes/_lanes/273/roles-drift/shot-*.png`. Chromium: `source knowledge/_render/seat_env.sh` then playwright (see memory: chromium needs `/tmp/pw`; `_validate_demo_page.py` shows the driving pattern). Look at them: a label that crops, a table that overflows the body, a radio without a name = not done.
4. Write `notes/_lanes/273/roles-drift/REPORT.md`: what you built, the three gate results, and anything you could not do (name the FIRST obstacle, no workaround narratives).

⛔ Do not edit anything outside `notes/_lanes/273/roles-drift/` and the one output HTML. Do not commit. Do not rule: every option list stays open, the recommendation is marked as a recommendation. Do not touch `.git`, never `git stash`.
