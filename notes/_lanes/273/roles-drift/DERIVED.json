{
 "contradictions": {
  "formula": "len(contradictions)",
  "value": 0
 },
 "dangling": {
  "formula": "len(dangling_slugs)",
  "value": 0
 },
 "feedback_plus_overlay": {
  "formula": "per_role[feedback].meta_silent + per_role[overlay].meta_silent",
  "value": 18
 },
 "feedback_silent": {
  "formula": "per_role[feedback].meta_silent",
  "value": 11
 },
 "illegal_field_entries": {
  "formula": "sum(len(v) for v in metas_with_illegal_when_fields.values())",
  "value": 14
 },
 "illegal_field_metas": {
  "formula": "len(metas_with_illegal_when_fields)",
  "value": 9
 },
 "illegal_field_names_distinct": {
  "formula": "len(set of distinct field names across metas_with_illegal_when_fields)",
  "value": 13
 },
 "input_silent": {
  "formula": "per_role[input].meta_silent",
  "value": 27
 },
 "invented_roles": {
  "formula": "len(meta_provides_not_in_json) + len(meta_provides_unknown_role)",
  "value": 0
 },
 "legal_field_count": {
  "formula": "len(legal_when_fields)",
  "value": 26
 },
 "not_a_provider_with_provides": {
  "formula": "len(not_a_provider_with_provides)",
  "value": 0
 },
 "overlay_silent": {
  "formula": "per_role[overlay].meta_silent",
  "value": 7
 },
 "pass2_roles": {
  "formula": "count of roles not in {record-list, input} with meta_silent > 0",
  "value": 8
 },
 "pass2_silent": {
  "formula": "sum(per_role[r].meta_silent) for roles not in {record-list, input}",
  "value": 49
 },
 "pct_silent": {
  "formula": "round(memberships_meta_silent * 100 / memberships_json)",
  "value": 78
 },
 "record_list_silent": {
  "formula": "per_role[record-list].meta_silent",
  "value": 8
 },
 "silent_rows": {
  "formula": "count of per_role[*].providers[*] with state != 'agrees'",
  "value": 84
 },
 "zero_roles": {
  "formula": "len(roles_zero_in_metas)",
  "value": 4
 }
}
