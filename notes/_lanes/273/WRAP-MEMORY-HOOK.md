# #273 WRAP — MEMORY HOOK, FOR THE CONDUCTOR TO PLACE

⛔ **THE MEMORY STORE IS READ-ONLY FROM THE WRAP SEAT BY THE BRIEF.** This file is the hook TEXT and
its one `MEMORY.md` index line, written in the `s271-D4` form (every open item is phrased as a
QUESTION PUT, never as a state of the world) so the conductor can place it verbatim. It is not a
memory write and does not claim to be one.

---

## (1) THE ONE `MEMORY.md` INDEX LINE — place at the top of the ★★★ session run

```
- [★★★ #273 WRAPPED · 4 RULINGS 562 → 566 · 84 SILENT METAS GOT AN ADDRESS · THE `when` LIST IS A REGISTRY, NOT A WHITELIST](wrap-273-the-address-and-the-registry.md) — ⛔ READ `_HANDOFF-124-the-address-and-the-registry.md` FIRST, it OUTRANKS `_CHAIN.md`; `s272-D93` ENACTED as an ADVISORY arm on his *"1. advisory arm"* (`gauge.TOLERATED_TK = 220_000`, STOP_LINE_TK 180,000 UNTOUCHED) ⇒ #273 is the FIRST session graded by an arm it enacted the same morning (FILL 196,588, TOLERATED) · `s273-D1` wrote `provides` onto 84 metas BY TEXTUAL SPAN, drift 108/108, `providesRole` 24 → 108 · `s273-D4` = REGISTRY not whitelist, a lane MAY add a `when` name with definition + example, NO new instrument · ⛔ the #272 seam reported `_validate_kg.py OK` IN THE RESOLVER'S PLACE and the resolver was never run — FAIL(6) → FAIL(20), healed to 6 · boot 81,419 = NINTH breach; 12 local UNPUSHED
```

## (2) THE HOOK FILE — `wrap-273-the-address-and-the-registry.md`

```markdown
---
metadata:
  provenance: 273 · 2026-09-15
  status: observed
---

# #273 — the address and the registry

**⛔ READ FIRST:** `_HANDOFF-124-the-address-and-the-registry.md` — NEWER than `_CHAIN.md` and it
OUTRANKS it. His words verbatim, in order: `notes/_lanes/273/DAVE-RULINGS-2026-09-15.md`.
Narrative: `_DECISION-HISTORY/2026-09-15-273-the-address-and-the-registry.md`.

## What landed (every claim has a receipt)

- **`s272-D93` ENACTED AS AN ADVISORY ARM** — `4294b60`. His words: *"1. advisory arm"*.
  `gauge.TOLERATED_TK = 220_000`; **`STOP_LINE_TK` stayed 180,000**; walls untouched;
  `_checkin.py` says TOLERATED between them and PAST TOLERANCE above. Driven at four points.
  ★ **#273 then closed at FILL 196,588 — the first session in the project's history graded by an
  arm it enacted that same morning.** Advisory is HIS word, not an inference.
- **`s273-D1`…`s273-D3`** from his roles-drift export (**RD-1 a · RD-2 a · RD-4 a**).
  `0d98a34`: `provides: <role>` onto **84 silent metas BY TEXTUAL SPAN, reconstruction-proven per
  file** (`knowledge/_enact_s273_d1_address.py`); the drift #270 and #271 both reported and both
  declined to fix now reads **108/108 agree, 0 silent, 0 roles at zero**; `providesRole`
  **24 → 108** across 207 edges / 85 metas; `_validate_kg.py` OK.
- **`s273-D4` — THE `when` FIELD LIST IS A REGISTRY, NOT A WHITELIST.** `5f0c97f`: 13 names defined
  in `knowledge/when-fields.json` by textual span, `$description` amended BY ADDITION, resolver
  **FAIL(20) → FAIL(6)**. One home; the resolver still refuses an undefined name; **a lane MAY ADD
  a name with a definition and an example, listed in its report**; and **no new instrument was
  built** — the existing advisory `_near_dupes.py` is pointed at the registry at verify time.
- **`s273-D3` pulled `P-272-1` forward** — `783a8d9`,
  `notes/_PROPOSAL-list-vs-card-2026-09-15-v1.html`: **13 systems, 0 draw the list/card line on
  field count**; live predicates **surface · record-level actions · same-shape set**; GOV.UK's
  summary card is the one list-card precedent. The lane CORRECTED its brief: `cards` DOES provide
  `arrangement`; only `account-card` is `$not-a-provider`.
- **`P-273-1` parked** on *"i cant get access to the common library specs right now, we'll have to
  park it."* — the Common Toolkit specs; tripwire on a `knowledge/guidelines` commit.

## ⛔ The lesson worth keeping

**A GREEN GATE IS ONLY EVIDENCE ABOUT THE GATE THAT RAN.** `_validate_roles_resolve.py` went
**FAIL(6) → FAIL(20)** at `b46ea90` — #272's fifteen inscribed `when` predicates used **13 field
names that existed in no registry**, over 9 metas — and nobody saw it because the #272 seam
reported **`_validate_kg.py OK` in the resolver's place and the resolver was never run**. Not a
bug in any gate: a substitution made while writing a receipt.
[[no-gate-parses-the-artefact]]

★ **A `null` answer with a sentence attached is a REDIRECTION, not a deferral.** RD-3 came back
empty with *"maybe closing the list was a bad idea, I have a feeling this will reoccur, lets
explore"*. Re-asking the original question would have got a worse answer; exploring three fence
shapes and bringing those back got `s273-D4`.

⚠ **A GENERATOR-OWNED FIELD EATS HAND-EDITS.** The `s273-D1` regen rewrote Chart-boxplot's
`yieldsTo` `$note` and dropped a hand-added `span.cols` sentence. Declared, not restored — if it
mattered, its home is the meta.

## Open — EACH ONE PUT TO DAVE AT THE WRAP CALL, none of them a state of the world

1. **LC-1…LC-4** on the list-vs-card page — put to Dave at the wrap call. LC-1 (a) needs ONE new
   field, `actions`. `P-272-1`'s owner is him.
2. **The `example` key** on the 13 new `when-fields.json` entries, which the **26 legacy entries
   lack** — put to Dave at the wrap call: retro-fill, or forward-only?
3. **The 4 near-dupe pairs** — `records`/`rows` · `entry`/`content` · `exits`/`destinations` ·
   `steps`/`parts` — put to Dave at the wrap call. Not merged, not aliased.
4. **The `input` sub-roles** (27 providers, `$extension`) — put to Dave at the wrap call; it blocks
   `s273-D2`'s pass two for that role.
5. **The KG-gaps presentation** (`notes/_PROPOSAL-kg-entity-and-edge-gaps-2026-09-14-v1.html`) — his
   mid-turn ask, *"Id really like to get this nailed then go back to the presentation"*. Its
   zero-counts are stale for roles at minimum. ⛔ **Regenerate from the graph; never hand-correct,
   and never run `gen_kg_edges.py`.**
6. **`P-272-2`…`P-272-4`** and the 6 `data-grid` `with`-slug reds (#261) — carried, his.

## Cautions for the next seat

`model: opus` every lane · verifier in the same wave · `_checkin.py` every ~10 turns ·
screenshot + text diff before presenting · **textual span only** for `_rulings.json`, metas and
every JSON store · never `git stash` · **never `gen_kg_edges.py`** ·
`git --work-tree=… checkout` UPDATES THE INDEX (use `git archive | tar` for a historical copy) ·
`knowledge/_roles_drift.py` is the drift gate now — run it before any claim about roles ·
`_numbers_check.py` in lane RD has a void-tag slicer defect (declared) ·
**12 commits local and UNPUSHED; the push is Dave's word.**
```

---

## (3) STALENESS CHECK THE CONDUCTOR SHOULD RUN BEFORE PLACING THIS

`s271-D4` requires the "Open" list above to be **re-read against `knowledge/_rulings.json` after the
ritual's last beat** and anything the session's own rulings closed to be **struck BY ADDITION, naming
the ruling id** (`… — CLOSED by sNNN-DN`). That re-read was performed at this wrap's 5b beat against
the store at 566 rulings: **none of the six items is closed by `s273-D1`…`s273-D4`**, and each is
written as a question put rather than as a state of the world. If Dave answers any of them at the
wrap call, strike it by addition before placing the file.
