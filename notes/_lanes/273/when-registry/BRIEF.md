# BRIEF — #273 lane WR: enact s273-D4 — the `when` field REGISTRY, and define the 13 names

Repo: `/sessions/elegant-determined-planck/mnt/UX-design` (bash) = `/Users/daviewen/Documents/Claude/Projects/UX-design` (file tools).
Lane folder: `notes/_lanes/273/when-registry/`. Read `s273-D4` in `knowledge/_rulings.json` first (grep the id) — it is the whole instruction; this brief is its mechanics.

## What to do, in order
1. **Define the 13 names** in `knowledge/when-fields.json` `fields`, each in the file's existing entry shape (look at `answers`, `shape`, `span.cols` — `definition` + `kind` + whatever siblings the existing entries carry; match them exactly). Source for each definition: the clause that uses it — `python3 knowledge/_roles_drift.py --json | python3 -c "import json,sys; [print(c) for c in json.load(sys.stdin)['illegal_when_clauses']]"` — and the meta's own prose. One definition sentence, one example clause (the real one). Names: baseline · primaryNavigation · breadcrumbs · search · exits · job.count · entry · full · position · order · cause · steps · records. `entry` appears on two metas: one definition covering both. Where a name is arguably a synonym of an EXISTING name (e.g. `records` vs `rows`, `entry` vs `content`), do NOT merge or rename — define it and list the pair in REPORT.md as a near-dupe candidate for Dave.
2. **Amend the `$description`** of `when-fields.json` by addition (append one paragraph, textual span): the list is a REGISTRY under s273-D4 — a lane may add a name with a definition + example clause, by addition, listed in its report; the resolver still refuses undefined names; Dave sees additions at wrap. Do not delete or rewrite s254-D2's paragraph.
3. **Textual span only.** Never `json.dump` the file. Prove it the `_inscribe_ruling.py` way: removing your inserted spans gives back the original bytes; the result parses; the parsed diff is exactly +13 keys in `fields` + the one `$description` change. Write the proof as `_proof.py` in the lane folder and run it.
4. **Point the existing near-dupe door at the registry**: run `python3 knowledge/_near_dupes.py --help`, and if it can take a JSON of records, run it over the 39 definitions (26 + 13) and put the pairs it flags in REPORT.md. If it cannot without code changes, say so in one line and stop — DO NOT modify `_near_dupes.py` or build anything new (Dave: "probably over engineering").
5. **Gates**: `python3 knowledge/_validate_roles_resolve.py` must go FAIL(20) → FAIL(6) (the 6 remaining are `data-grid` `with` entries without a slug, #261 — not yours, leave them). `python3 knowledge/_validate_kg.py` OK. `python3 knowledge/_roles_drift.py` must report `metas whose gate uses a field OUTSIDE when-fields.json: 0`. Paste all three results verbatim in REPORT.md.
6. REPORT.md: the 13 definitions as written, the near-dupe candidates, the gate lines, first obstacle if any.

⛔ Write only `knowledge/when-fields.json` and the lane folder. No commits. No other file. No new names beyond the 13. No ruling — a definition describes what the clause already means; it does not decide anything.
