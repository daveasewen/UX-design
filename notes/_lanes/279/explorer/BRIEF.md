# LANE EX — BRIEF — the explorer: three views by force, the Constitution named, the `assets` chip
#279 · 2026-09-16 · enacting `s277-D8` (relabel) + the explorer half of `s277-D4` (assets chip OFF) · `s274-D11` applies · written by the conductor (Fable 5.1) · **model: fable**

## Rulings — read WHOLE in `knowledge/_rulings.json`: `s277-D8`, `s277-D4`, `s275-D6`, `s275-D4`, `s276-D3`, `s274-D11`
- **D8**: one graph, three VIEWS — SYSTEM (what exists; the agent chooses) · DESIGN GOVERNANCE (what a design must/should do; the agent obeys — the 55 WCAG criteria, the 470 HSBC rules and the design-scoped rulings shown as ONE obligation with THREE provenance sub-chips) · EXPLANATION (why; graded principles + polarities; the agent consults). **Storage untouched** — no node id, edge type or file under `knowledge/` changes; the explorer's chips and fam labels do. Refines `s275-D6` (a sub-chip is still its own chip). `s275-D4`'s 20 standards joins stay their own lane. **The ruling record is named THE CONSTITUTION** — it is NOT one of the three views; name it on the page as its own thing. The precedence ladder and derived per-ruling scope are A2's proposal, UNRATIFIED — do not draw them.
- **D4**: the 688 asset nodes land behind an `assets` chip OFF by default. Node files `knowledge/_icon_nodes.json` / `_logo_nodes.json` (landed `f641242`, nulls in-file at `84658db`). Lane IL's REPORT.md § "what the chip lane needs" has the shape; family key `assets` is free (checked). Four families sit at `cx` −1.0/1.0/2.2/−2.2 (`_build_kg_explorer.py` ~line 425); assets is a fifth column. Payload +12.3%.

## What you build — `knowledge/_build_kg_explorer.py` → VERSION 1.15, and the generated `notes/_KG-EXPLORER.html`
1. **Assets family**: a fifth additive family behind chip `assets`, OFF by default, reading the two node files with the same reader shape `rule_nodes()` uses. Six edge types drawn; null-target edges NOT drawn but COUNTED and shown as declared nulls the way the guideline-rules family shows its 19. With the chip off the page is 1.14 to the pixel — prove it (DOM/edge-count diff with chip off).
2. **Three views**: relabel the chip bar into the three views. Which existing fams go where is YOUR judgement from D8's words — write the mapping down in the report with the ruling sentence each row rests on. Design governance's ONE obligation with THREE provenance sub-chips (WCAG sc: · HSBC rule: · design-scoped rulings) — each sub-chip still its own chip (`s275-D6`). Assets sits in SYSTEM. Fam labels change; fam KEYS do not (storage untouched — `RULE_FAM='guidelinerules'` stays).
3. **The Constitution**: the ruling record is named THE CONSTITUTION on the page, visibly, outside the three views. Governance-family rulings that are NOT design-scoped (process, gauge, ritual) — where do they sit? Decide, say, and rest it on D8's sentence.
4. **No drawing of edges.obeys** is still the standing declared gap (v1.14 note) — leave it declared unless D8 forces it; if you think it does, say so and do not do it.
5. **Never `_build_kg_explorer.main()` in a read-only probe** — but this lane IS the build lane: run the build the way the module's own docstring says, and screenshot in a NEVER-DRIVEN context (fresh browser context, cleared localStorage; the pages save DOM to localStorage on unload). Chromium recipe: memory `chromium-in-sandbox-recipe` in the Project store / `_memento_search.py "chromium"`. Light + dark, chip off + on, 1280 and 390.

## Gates
`_validate_kg.py` rc 0 · the explorer's own selftest if it has one (find it) · `_validate_lane_ownership.py` unchanged from HEAD (2/3 is inherited) · edge/node counts on the page with every chip on = base + 4 families + 688/1,325 (nulls counted not drawn) · `git status` clean except your paths (`notes/_dream/_GRADE-DECISIONS.jsonl` is pre-dirty, leave it).

## Report — `notes/_lanes/279/explorer/REPORT.md` + copy at `notes/_subreports/2026-09-16-279-EX-explorer.md` (+ `_state.json` row via `_state.add()`, W-279ex, copy the W-279rd shape)
The view mapping table with ruling sentences · the Constitution placement decision · chip-off pixel proof · counts · screenshots listed · every gate line · `git diff --numstat` from the SHIPPED sha. Commit via `SESSION_N=279 SHOWROOM_ACK=1 bash knowledge/_git_commit.sh --reconciled <fresh msgfile> <paths…>`, fresh msgfile `/tmp/_msg-279-EX-$(date +%s).txt`, bare subject `#279 lane EX: explorer 1.15 — three views by force, the Constitution named, assets chip OFF — s277-D8 + D4`. Regenerate `_CHAIN.md` (`python3 knowledge/_gen_chain.py`) if the store row makes the chain gate refuse. Stale locks → `mv` to `.git/_orphan-locks/` (`/tmp/gitshim/git` wrapper may exist). Never `git stash` / `gen_kg_edges.py` / `_build_all.py`. Bash root `/sessions/laughing-elegant-feynman/mnt/UX-design/`; `pip install tiktoken --break-system-packages` first.
