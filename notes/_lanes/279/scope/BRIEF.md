# LANE SC — BRIEF — `scope` per guideline file: 34 rows, inference by declared scope
#279 · 2026-09-16 · enacting `s277-D9` · written by the conductor (Fable 5.1) · **model: fable** — judgement lane: every row is a claim about what a file governs and will be read against the file

## Ruling — read WHOLE: `s277-D9` (and `s274-D7..D12` for the rule-node shape, `s277-D5` for why prose/slug-as-word joins are refused, `s277-D8` for the three views)
26 of the 34 guideline files bind a FACET (copy, colour, type, icons …) not a component, so a `scope` attribute per file — applicability via token groups and facets — is how a rule reaches the components it governs, labelled 'inference by declared scope' with an exception row; `obeys` is kept for the specific, authored binding. Measured at #277: 363 of 470 rules bind no component; 46 of the 59 BLOCKING rules reach a component by no path.

## What you build
1. **The 34 rows** — for every file under `knowledge/guidelines/` that carries rules in `_rules-index.json`, a `scope` block: `{kind: component|facet, facets:[…], tokenGroups:[…], components:[…] (authored only), exceptions:[{rule, why}], $source: "<the sentence in the file this rests on>"}`. Read each guideline file's PROSE for its own statement of what it applies to; the `$source` quotes it. Where a file states no scope, the row is a declared null with a note, not a guess. Token groups are the ≈128 groups `_compose_slice.py` reads (grep them; do not invent names). Facets: propose the closed set from the files themselves, list it once, and say how many files each facet covers.
2. **Where it lives** — `s277-D9` governs `_rules-index.json` and `guidelines/`. Store the rows where the index already carries per-file data (check the shape; by addition, existing keys untouched), OR a sibling `_scope.json` if the index is generated by a script that would drop them — find the generator first (`grep -l _rules-index knowledge/*.py`) and put the rows where the generator preserves them. Say which and why.
3. **The consumer, same commit (`s274-D11`)** — `_compose_slice.py`'s `obeys.routed` (lane RD landed it at `d6bd57b`: vocabulary → BLOCKING rules) gains the declared-scope path: a rule reaches a component when the component's meta binds a token group or facet the file's scope names. Label it `routed-by-scope` and keep `routed` (vocabulary) distinct. Do NOT touch the explorer (lane EX is live on it).
4. **Measure** — re-run the two figures after: how many of 470 rules now reach ≥1 component; how many of the 59 BLOCKING reach one. Publish before/after with the command. Also: how many rules reach a component by scope alone, by obeys alone, by both.
5. **Tests** — bites in `_compose_slice.py --selftest` (add, don't rewrite): every file has a row or a declared null; every `$source` sentence is a real substring of its file; every token group / facet named exists; routed-by-scope never fires on a file with a null scope; a planted exception row suppresses its rule. `_validate_kg.py` rc 0.

## Cautions
Verify against the LIVE file — every `$source` is graded as a substring by the bite AND on sight by a verify seat. Never `git stash` / `gen_kg_edges.py` / `_build_all.py` / `_build_kg_explorer.main()`. Locks → `mv` to `.git/_orphan-locks/`. Leave `notes/_dream/_GRADE-DECISIONS.jsonl` unstaged. Lane EX is writing `_build_kg_explorer.py` + `_KG-EXPLORER.html` concurrently — do not touch those, and if the commit script sees them dirty, stage only YOUR paths (explicit-path staging is the script's mode).

## Report — `notes/_lanes/279/scope/REPORT.md` + copy `notes/_subreports/2026-09-16-279-SC-scope.md` (+ `_state.json` row W-279sc via `_state.add()`, W-279rd shape)
The facet set · the 34 rows summarised (kind, facets, groups, null?) · before/after figures with commands · gate lines · `--numstat` from the SHIPPED sha. Commit via `SESSION_N=279 SHOWROOM_ACK=1 bash knowledge/_git_commit.sh --reconciled <fresh msgfile> <paths…>`, msgfile `/tmp/_msg-279-SC-$(date +%s).txt`, bare subject `#279 lane SC: scope per guideline file — 34 rows, routed-by-scope in the reader — s277-D9`. Regenerate `_CHAIN.md` if the store row trips the chain gate. Bash root `/sessions/laughing-elegant-feynman/mnt/UX-design/`; `pip install tiktoken --break-system-packages` first.
