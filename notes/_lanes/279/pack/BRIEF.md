# LANE PK — BRIEF — the pack ships the reader and the Constitution (Dave's "a", 2026-09-16) — a release under P-269-1
#279 · 2026-09-16 · enacting Dave's word (to inscribe as `s279-D1` at wrap — `notes/_lanes/279/DAVE-RULINGS-2026-09-16.md` item 5) under `s278-D1` + `s277-D10` · written by the conductor (Fable 5.1) · **model: fable**

## His word, verbatim
Asked in plain prose whether (a) the pack ships the reader + rulings so ASK reads the Constitution live inside the pack, or (b) ASK stays repo-side and the pack gets the seed only: *"i agree 'a' it is"*.

## What is true today (verify, don't trust)
- `knowledge/_gen_pack_manifest.py:515` (±) deliberately EXCLUDES `_compose_slice.py`, `_rulings.json` and the node files from the pack `designer-skills-v2/` (RD's report `notes/_lanes/279/reader/REPORT.md`, RV's VERIFY F1). Read the exclusion and the reason it gives.
- `_gate_frozen_release.py --check` is RED (rc 1): `designer-skills-v2 (v2) … 1 CHANGED` — RD's SKILL.md edit at `d6bd57b` changed a frozen release without a version bump (`s114-D4`). The fix RV named: bump v2's version + `--seed` in one commit. Read `knowledge/_release/` and the gate before you touch anything; find the release runbook (`_memento_search.py "frozen release"` / P-269-1) and follow it.
- The reader now depends on: `knowledge/_compose_slice.py`, `knowledge/_rulings.json`, `knowledge/_icon_nodes.json`, `knowledge/_logo_nodes.json`, `knowledge/guidelines/_scope.json`, `knowledge/guidelines/_rules-index.json`, `knowledge/_rule_nodes.json`, `knowledge/_ux_principle_nodes.json`, the metas, `chart-intents.json`, `roles.json`, `_blast-radius.json`, tokens — derive the TRUE import/read closure from the code (`grep -n "open(\|HERE\|json.load" knowledge/_compose_slice.py`), not from this list.

## What you do
1. **Reverse the exclusion by his word**: the pack ships the reader's whole read closure. Edit `_gen_pack_manifest.py` by addition where possible (a new allow-block citing the ruling-to-be and his sentence; the old exclusion line stays, commented with why it is superseded, or is removed if the manifest generator cannot carry both — say which).
2. **Release**: bump `designer-skills-v2` per the release runbook (version, `--seed`, manifest regen, whatever the runbook's steps are — every step, run, with its line in the report). `_gate_frozen_release.py --check` → rc 0 after.
3. **Prove ASK works from inside the pack**: copy the built pack to scratch, `cd` into it, run the 12 ASK questions with NO path back to the repo (`HERE` must resolve inside the pack — if `_compose_slice.py` hardcodes the repo root, fix it so it finds its files relative to itself), count tokens, all ≤1K. Plant a ruling in the PACK's copy of `_rulings.json` (scratch) and show ASK sees it. Run the seed from inside the pack for the dashboard task and compare byte-for-byte to the repo's seed.
4. **Size**: publish the pack's size before/after in bytes and files; if the closure drags in something huge (assets/, 5.2 GB, must NOT come), stop and declare.
5. `apollo-spider/skills/generate-from-canon/SKILL.md` — RD declared it unwired (~30 lines) because the ruling governs `designer-skills-v2/` only. Leave it; note it.
6. Bites: add to `_compose_slice.py --selftest` a bite that asserts every file the reader opens is inside the manifest's allow-set (so the exclusion can never silently return). Do NOT change any other bite; do NOT touch `_build_kg_explorer.py`, `guidelines/_scope.json` rows, or the explorer.

## Report — `notes/_lanes/279/pack/REPORT.md` + `notes/_subreports/2026-09-16-279-PK-pack.md` (+ `_state.json` row `W-279pk` via `_state.add()` — id regex is `W-[0-9]{1,3}[a-z]{0,2}`)
Every release step with its line · the closure list · the in-pack ASK numbers · the seed byte-compare · size before/after · gate lines · `--numstat` from the shipped sha (a report-only follow-up commit for the sha is allowed, the SC/EX pattern). Commit via `SESSION_N=279 SHOWROOM_ACK=1 bash knowledge/_git_commit.sh --reconciled <fresh msgfile> <paths…>`, msgfile `/tmp/_msg-279-PK-$(date +%s).txt`, bare subject `#279 lane PK: the pack ships the reader + the Constitution — designer-skills-v2 release, ASK live in-pack (Dave: "a")`. Regenerate `_CHAIN.md` if the chain gate trips. Locks → `mv` to `.git/_orphan-locks/`. Never `git stash` / `gen_kg_edges.py` / `_build_all.py` / `_build_kg_explorer.main()`. `/sessions` at 99% — download nothing; scratch under `/tmp` and remove it after. Bash root `/sessions/laughing-elegant-feynman/mnt/UX-design/`; `pip install tiktoken --break-system-packages` first.
