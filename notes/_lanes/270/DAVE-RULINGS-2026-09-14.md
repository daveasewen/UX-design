# Dave's words — #270 — 2026-09-14

## The two lanes, GO (opener, verbatim)
> "cool, lets do both in teh oredr you prefer"

Context put to him (conductor's framing, NOT his words): (1) lane 1 = roles + DESK fields into the graph, GO on s269-D1; (2) the compose-time door — "Yes / no / later?"

Conductor's reading (labelled as such): BOTH are GO. Order chosen by the conductor: lane 1 first (the door slices roles; roles must be in the graph first), the door second. Spelling corrected per s269-D7 = still his words.

## Dropdown cut-off — RULED from memory (verbatim)
> "If I remember correctly we use dropdowns for 5 and above"
> "okay lets do it from my memory 5 is the cutoff"

Context (conductor's framing, not his words): asked "is a dropdown our default single-select or our last resort?" after the when-research lane found no number in any ingested source. Checked before asking: guidelines/, roles.json, dropdown + selection-controls metas, Memento index, _rulings.json — "5 and above" is in NONE of them. Source = Dave's recollection of the HSBC Common Toolkit (Figma, not accessible today). Conductor's reading, labelled as such: radio = single-select AND options ≤ 4; dropdown = single-select AND options ≥ 5 (default, not last resort); combobox threshold and segmented-control stay separate. PROVISIONAL until the Figma forms-component tranche is ingested (forms.md:40 → node 45226:149920) — tripwire P-270-1 on guidelines-ingest.

## Compose-time door shape (his words, verbatim, this session)
> "three compositions that are all defensible under the same rules"
> "maybe the logic is cast the net wider for the slice, the slice carries the scoring so if variations asked for there is scope to substitute"
Conductor's reading: the slice carries every provider per role with score + when + chosen flag; rules/tokens fixed; Explore = wide net, all layers bind. Not yet ruled as a build — a design direction for the door's v2.
