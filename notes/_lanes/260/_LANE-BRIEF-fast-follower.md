# #260 fast-follower lane brief (shared by lanes F1–F4)

Repo: /sessions/serene-keen-heisenberg/mnt/UX-design (shell) = /Users/daviewen/Documents/Claude/Projects/UX-design (Read/Write/Edit). Absolute paths; each bash call independent. Deletion enabled. NEVER `git stash`, NEVER `git checkout`/`git add`/`git commit` — an INTEGRATION lane commits after you; four lanes run in parallel in ONE working tree.

RULING: `s259-D1` (Dave): scatter, pie, histogram, boxplot, bullet, candlestick, butterfly-h, butterfly-v are the FAST FOLLOWER to the #259 chart engine. You build the two types named in your prompt.

WHAT #259 BUILT (copy its shape exactly): engine core `knowledge/canon/dv-render.js`; per-type partials `knowledge/canon/dv-render-{bar,line,stacked-area,donut,sparkline,combo}.js`; each type has a snippet `knowledge/snippets/Chart-<type>.reference.html` drawing from an inline DATA object via `dvRender(figure, spec)` with an authored `<table class="dv-table">` spine; a typed `behaviour` meta in `knowledge/components/chart-<type>.meta.json`; and a committed driven test page `knowledge/_tests/chart-engine/<type>.html`. READ FIRST: `knowledge/_tests/chart-engine/donut.html` header comment, `knowledge/canon/dv-render-donut.js` and `dv-render-bar.js`, `notes/_subreports/2026-09-08-259-A-engine-core-bar.md` (core API, `dv-fit`, GAP 2.2 bug), `notes/_subreports/2026-09-08-259-D-donut.md` (radial rules, ds-030 no dv-fit on radial), and `docs/decisions/ADR-0015-behaviour-partials-dataviz.md` A4 (core priced once per page). The existing static component for your type already exists (all 14 were components before #259) — its meta, snippet and dataviz rules (`knowledge/_validate_dataviz.py`, `knowledge/guidelines/` dataviz rules dv-*) are the contract you re-point at the engine, keeping every DV rule it already satisfied.

PARALLEL-SAFETY (⛔ the #253 defect was two lanes wiping each other): you may CREATE `knowledge/canon/dv-render-<type>.js` and `knowledge/_tests/chart-engine/<type>.html`, and EDIT ONLY `knowledge/snippets/Chart-<type>.reference.html` and `knowledge/components/chart-<type>.meta.json` for YOUR two types. You may NOT edit `knowledge/component-types.json`, `knowledge/canon/dv-render.js`, `_receipts.json`, any gate, canon.css, or another type's files. Registry changes ($behaviour entry + your members' `consumes`) go into `knowledge/_tmp/260/<type>.registry.json` as a JSON fragment; core requests (things dv-render.js needs) go into `knowledge/_tmp/260/<type>.core-requests.md` — if the core lacks something you need, write the smallest local workaround in your partial and file the request; do not edit the core.

OBLIGATIONS per type: partial ≤16,384 code-only bytes; member page (own consumes minus shared core) ≤34,816 — measure with `python3 knowledge/_validate_behaviour.py` logic (the registry won't include you yet, so compute: sum code-only bytes of dv-behaviour 13,048 + dv-legend 7,734 [if consumed] + your partial(s), core excluded); no dv-fit on radial (pie); keep every static dv-* rule the type owes (run `python3 knowledge/_validate_dataviz.py` — it validates snippets; note pie takes the STATIC route today because its snippet has static marks — once yours is engine-drawn it takes the RECEIPT route, which the integration lane re-drives); a11y spine (table follows the filter); tooltip; category filter re-renders; resize behaviour per family (cartesian re-fits, radial re-centres). Drive it yourself with Playwright 8/8 themes×modes: `python3 knowledge/_drive_chart_engine.py --help` (Playwright setup: `pip install playwright --break-system-packages; python3 -m playwright install chromium`; if Chromium fails on libXdamage see notes/_subreports/2026-09-08-259-D-donut.md §Driven for the apt-get download / dpkg-deb -x / LD_LIBRARY_PATH workaround). Do NOT write `_receipts.json` — run the driver with an `--out` to a scratch path if it supports it, else copy its measuring code into a scratch script; report the measured numbers. 0 pageerrors. Mutants red (break one thing, see the assertion catch it, restore).

REPORT: `notes/_subreports/2026-09-08-260-F<n>-<typeA>-<typeB>.md` (full), reply ≤300 words: files created/edited, bytes per partial, page-budget figure per member, 8/8 drive result with the measured figures, mutation results, core requests filed, anything ruling-shaped named not ruled. Do NOT touch GOOD-MORNING.md, _LIVE-STATE.md, _CARRIES.md, _rulings.json, memory.
