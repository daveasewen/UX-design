# #284 — LANE W — the wrap's own working record

provenance: 284 · 2026-09-18
status: observed

**The filed report (`s218-D7`) is `notes/_subreports/2026-09-18-284-W-wrap.md` and is the authority.**
This file holds the two things that belong beside the working set rather than inside the filed
report: the **2c/2d/2f EXIT CHECK** and the **`s271-D4` re-read**, which is the ritual's final beat.

## EXIT CHECK — run BEFORE the three rolls, recorded rather than asserted

Every ⚠ / ⬛ / AWAITING / OPEN-CALL / DEFERRED-TO-DAVE item inside the three rolling regions was
scanned and matched to a **standing** home before the move. Dated homes do not count. The standing
home in every case is `_CARRIES.md` § `residual → #285`, probed by token at this seat.

| rolling region | Dave-owed items found | standing home confirmed |
|---|---|---|
| GM ★ PRIOR banner **#282** (12 lines → `_GM-ARCHIVE.md` § Batch 2026-09-18 #284) | ⬛ ④ the 40 per-size logo MASTERS · the THIRD DIAL · the THEORY DOOR + the three postures · `col26-012` · `W-282a` `W-282b` `W-282ll` — and ⬛ ⑥ the six gate fails in the `#243` form · resolver `FAIL(6)` · ownership 2/3 · showroom *"138 v 108"* · `s277-D12` | § `residual → #285` holds every one, by probe: `#243` ×49, `showroom` ×11, `FAIL(6)` ×3, `ownership` ×5, `s277-D12` ×3, `col26-012` ×4, `W-282a` ×1, `W-282b` ×1, `W-282ll` ×1, `DIAL` ×5, `THEORY DOOR` ×2, `W-284lm` ×3. ⚠ **The masters carry travels UNSTRUCK and AGED, not closed** — his acceptance met one of row `W-284lm`'s two conditions, and a half-met close condition is not a strike; the acceptance is minted as NEW item ③ instead |
| LS ⏱ PRIOR DELTA **#281** (27 lines → `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-18 #284) | ⬛ the 15 rule notes · the eye-check · force by grade + the ghost colour · FO's two untied artefacts (`W-281fo`) · the ASKs `col26-012` `aid-009` `type26-002` and the unlanded `type26-003` · the logo review | all present by probe (`aid-009` ×2, `type26-002` ×1, `type26-003` ×2, `W-281fo` ×1, `ghost` ×3, `FORCE BY GRADE` ×1, `logo review` ×1); the rule-notes and eye-check items travel **STRUCK with their #282 receipts** rather than deleted, exactly as they arrived |
| GM stratum **#283** (`roll_2f`: 28 ln → `notes/_GAUGE-LOG.md` at true EOF, 3 ln → `_GM-ARCHIVE.md`) | ⬛ the declared-skips block · ⬛ `COMMIT STATE #283`'s `#243` not-a-wrap form, its declared hash gap, and the instrumentation-append policy | the `#243` form stands in the carry set (49 occurrences) and is **re-declared on this wrap's own `COMMIT STATE #284` block**, now its **TENTH** wrap; the hash gap is re-declared in the same block, verbatim in shape; the instrumentation-append policy stands as a carry and is re-declared in `COMMIT STATE #284` |

⛔ **Nothing was orphaned by these moves, and nothing was dropped for being old.** Four items that had
no line of their own in `_CARRIES.md` — the delegation lapse, the window finding and Dave's
correction, the masters' acceptance-without-registration, and the six-run commit — were MINTED at
2c(i) rather than left in a dated home.

⛔ **AND ONE CARRY WAS STRUCK, WHICH IS RARE AND IS THE POINT OF THE CHECK RUNNING BEFORE THE ROLL.**
#284's item ① claimed the disk was *"NOT FIXABLE FROM ANY SEAT WE CAN REACH"*. **#284 fixed it from
his Mac.** The headline is struck in place, the retraction names the session and where the correction
is inscribed (`notes/_lanes/284/DISK-LEVER-2026-09-18.md`, commit `ade8a403`), and **the body is not
re-typed** — the only wording change `s183-D1`/`s188-D2` license, and the receipt is what makes it
legal rather than tidy.

⚠ **The limit #283 exposed is not closed and is not closed here either:** the EXIT CHECK tests
PRESENCE for items it can SEE, and an item never written into either place is invisible to it
[[instrument-without-a-consumer]]. This wrap's only defence was the same one #283 used — going
looking for each carry in order to age it.

## ⬛ `s271-D4` RE-READ — the ritual's FINAL beat, run at 5b and RECORDED rather than assumed

The memory hook's "open, Dave's" items (`notes/_lanes/284/WRAP-MEMORY-HOOK.md`) were re-read against
`knowledge/_rulings.json` **after every ruling of the session was inscribed**. ⛔ **#284 inscribed
NONE. `json.load` counts 620, unchanged from #283's close** — so the re-read has, by construction,
nothing it may strike.

**NOTHING IS STRUCK. THAT IS A MEASUREMENT, NOT A SHORTCUT, AND THREE ITEMS ARE NAMED RATHER THAN
STRUCK BECAUSE THEY MOVED WITHOUT BEING RULED:**

1. **The 40 per-size logo masters** — ⚠ **THE STRONGEST-LOOKING NEAR MISS THIS RUN HAS HAD.** They
   were drawn, and Dave accepted the sheet in his own words. **That is not a ruling and it is not the
   row's close condition:** `W-284lm` closes on acceptance **and** registration in `_logo_nodes.json`,
   and the registration is untouched. **Striking the item on an acceptance would close a row that is
   half open.** The item ages; the acceptance is minted as its own NEW carry ③.
2. **The disk** — ⚠ **MOVED BY AN ACT, NOT BY A RULING, AND STRUCK ONLY FOR THE HALF THAT WAS
   FALSIFIED.** The lever was found and pulled; nothing was ruled about it. What was struck is a
   FACTUAL CLAIM that a measurement disproved, which is the `s183-D1` case; **what remains open — a
   bundle wipe as a standing chore, the 90% promotion — is untouched and is still his.**
3. **The hard-wall question** — ⚠ **PARTLY RE-SHAPED BY DAVE'S OWN CORRECTION AND NOT STRUCK.** His
   *"we gauged this against the messy middle problem not the 1M context window"* tells us what the
   180,000 line is FOR; **it does not answer what a session must DO at a wall**, and arming
   `FILL_CEILING_BLOCKING` is still `s271-D2`'s and his. The carry's wording is unchanged; the
   correction is minted as NEW carry ② in its own right.

⚠ **Every open item above is written as the QUESTION IT IS, not as a state of the world**, and
⛔ **an acceptance, an act and a correction are each a strong match, not a proof — a strike that is
wrong is worse than an item that is merely stale** (`s271-D4`'s own warning, and the reason three
near misses are named here instead of three items being struck).

## The ceiling-breach correction — recorded here because the filed report states the outcome, not the sequence

Three surfaces (the ⏱ LATEST DELTA, the `Last refreshed` segment and the stratum's `COMMIT STATE
#284`) were written asserting that this wrap's own 2f roll would take the boot-drift CEILING BREACH
from SEVEN readings to EIGHT. **The gate, run after the roll, named SEVEN.** The window slid — #283
came in, #276 went out — and the count did not move. All three surfaces were corrected through
`_gm_move.py` (`ops284_ceilingfix.py`) **before the commit**, and each correction **names the
prediction** rather than erasing it. ★ **A figure written before the ritual step that moves it is a
prediction, not a measurement** — the #280 lesson, met from the other direction.
