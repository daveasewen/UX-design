# #295 — WRAP MEMORY HOOK

provenance: 295 · 2026-09-22
status: observed

*Ritual step 3, written at the DELEGATED wrap seat. Per the #278 addition to
`knowledge/_RUNBOOK-capture-ritual.md` § step 3, the Cowork memory directory is now the claude.ai
Project memory.*

⛔★★ **THIS SEAT CAN REACH THE STORE AND THE #295 BRIEF PERMITTED THE WRITE, SO IT WAS ATTEMPTED —
WHAT ACTUALLY HAPPENED IS RECORDED IN § THE PLACEMENT, AT THE FOOT, AND NOWHERE ELSE.** The memory
tools are present at this seat and the Project subtree is writable, exactly as #293's and #294's wrap
subs each recorded. ⚠ **#278's clause should say *reserved to the conductor*, not *unreachable from a
sub*: at #293, #294 and again here, the sub COULD.** That one-line runbook correction is still owed
and is still ruling-shaped — third session running.

⛔ **EVERY OPEN ITEM BELOW IS WRITTEN AS THE QUESTION IT IS** (`s271-D4`): the hook is authored before
Dave answers the session's last questions, so a bare assertion that a thing is open would be false the
moment he speaks. The list was re-read as the ritual's final beat, after 5/5b, against
`knowledge/_rulings.json`, and what this session's own rulings closed is struck **by addition, naming
the ruling id**. ⚠ **The machine arm `_capture_gate.hook_open_items_recheck` was asked TWICE**, as
`_HANDOFF-145` § POST-WRAP 2 instructs — *"ask the arm twice, or inherit its silence"*.

---

## THE INDEX LINE (for `index.md`, at the TOP, newest first)

```
- [★★★ #295 WRAPPED · THE PUSH LANDED, CI WAS READ JOB BY JOB, AND THE WRAP GATE REACHED ZERO ·
  STORE 634 → 638, s295-D1 … s295-D4, ALL FOUR ENACTED IN CODE · 4 OPUS LANES, NONE IN SEAT ·
  ELEVEN COMMITS PUSHED TO 5e9e2cbe, THREE CI RUNS READ JOB BY JOB · THE `#243` NOT-A-WRAP FORM
  NOT NEEDED AFTER TWENTY WRAPS · HE STOPPED ON THE INSTRUMENT, n STAYS 2 · FRIDAY THE 25th IS
  THREE DAYS OUT AND #296 OPENS ON THE DECK]
  (wrap-295-the-push-landed-and-the-wrap-gate-reached-zero.md)
```

---

## THE FILE BODY (`wrap-295-the-push-landed-and-the-wrap-gate-reached-zero.md`)

**Session:** #295 · opened Monday 2026-09-21 · conductor Fable 5.1 · **four delegated Opus lanes —
I · P · I2 · I3, none in seat** except the conductor's own memory reads and the memory-index cut,
with a delegated Opus 5 wrap sub. ⛔ **DATE SPLIT, the sixth on this run: session and all eleven
commits 2026-09-21, ritual and wrap commit 2026-09-22 — the `s294-D11` shape, nothing re-dated.**

**⛔★★★ FOUR RULINGS WERE INSCRIBED AND ALL FOUR ARE ENACTED IN CODE.** `knowledge/_rulings.json`
**634 → 638**, verified by `json.load` (id and count only, never a printed record): `s295-D1` …
`s295-D4`. His authority was ***"inscribe"*** three times and ***"cool do it inscribe"*** once.

**★★★ THE WRAP GATE READS ZERO STRUCTURAL FAILS — the first time in twenty-one sessions, and the
`#243` DECLARED NOT-A-WRAP FORM WAS NOT NEEDED.**

### What landed

1. **The six boot double-counts were never double-counts.** Not one was a session stating its own boot
   twice: **#243 was charged FIVE times because four LATER strata quote its reading inside a `"#243
   form"` carry paragraph**; #264/#273/#287 by their own `wrap-handover:` lines; #272/#274 by a carry
   paragraph. And the arm's own remedy said *"Delete the restatement"* — ⛔ **the one repair no wrap may
   make, because the gauge log is append-only. Twenty consecutive wraps routed around it.** `s295-D1`
   narrows the reading rule instead, and names the 74 uncharged rows rather than hiding them.
2. **A re-base and a breach are two different objects.** `s295-D3` moved `BOOT_CEILING_TK`
   **70,000 → 72,768** on his *"possibly the new ceiling"* — **and lane I2 measured that it did NOT
   close the breach and published that its own brief was wrong.** The seven readings run 74,120–80,871,
   so a ceiling closing it by value would need **≥ 80,871**. `s295-D4` closed it with a **regime
   boundary** instead — `BOOT_CEILING_FROM_SESSION` **241 → 295** — and the seven are now **reported,
   never charged**.
3. **Eleven commits pushed to `5e9e2cbe`, CI read back job by job on three shas.** `gates` ⛔ at steps
   5+6 every time, `release` ✅, `render` ✅. ⛔ **`--push` refused at THREE seats — at the DIRT gate,
   not the credential gate: #294's *"no credential in the sandbox"* is RETIRED, the credential IS
   present.** All pushes went by plain `git push origin master`, **declared**, with the expiry,
   ff-only, master-only and verify gates **all skipped**.
4. **The `status` writer was refused at one seat and built at another — and it changed nothing.** Lane
   I refused the half-stamp; lane I3 read `s172-D3`(e) by id and found it fences speculative checks,
   not a writer a ratified ruling cannot be discharged without, then built `--set-status` with the
   vocabulary read from the store at call time and **two span swaps in one composition**. Fourteen
   stamped. ⛔ **The advisory arm read `98 of 117` before and `98 of 131` after: all fourteen were
   `ruled`, so the stamps grew the DENOMINATOR and the ~87 remainder does not exist.**
5. **`f81bbdd4` enacted TEN in code, not eleven** — driven over its own diff. `s294-D10` is the Jev
   decision; **`s294-D11` is enacted in the RITUAL SURFACES at `359b646b`, with no `.py` file touched.**
   Neither was stamped and **no sha was invented**.
6. **`_gen_chain.py --check` stopped being red by construction**, and CI's `[120]` is green on a pushed
   tree for the first time. The cause was **two differences, not one** — the two shas are the same byte
   length and a different token count — so normalising the sha out would have left the tape figure
   mismatched. A module global was written first and failed, because `_capture_gate.py` imports
   `_gen_chain` while the CLI runs it as `__main__`: **two module objects, one of them holding the pin.**

### His words, verbatim and in order

> Good Morning!

> 1. i need to see them
> 2. I don't know the answer to this
>
> BTW, we need to get back to the presentation soon

> inscribe

> 1. I can't cut anything else permanently, this is possibly the new ceiling
> 2. cool do it inscribe

> inscribe

> wrap

### The numbers

**FILL 219,167 real / 24 continuous turns of 26 · boot 72,768 · peak 219,167 · 0 compaction records ·
0 drops**, measured first-hand off the conductor's own transcript. ⛔★★ **AND FOR THE FIRST TIME IN SIX
WRAPS HIS DECLARED SEAM READINGS DO NOT REPRODUCE:** 214,824/23 and 218,396/25 are absent from the
per-turn cumulative series, whose nearest distinct readings are 215,019 and 219,030 (+195, +634).
✅ **The boot agrees to the token**, which is what identifies the window. **Both series published,
neither rewritten.**

**`subs 690,805 (n=4)` MEASURED** against **697,742 DECLARED** — **high on all four lanes and low on
none, by 6,937 (0.99%)** — the same direction and nearly the same magnitude as #294's offset across
eight lanes. Both published.

**`wrap-handover: brief-cut 218,396 · sub-cut 219,167 · delta 771 · replay unobservable`** — the
**smallest** hand-over on the `s214-D5` record, because no conductor turn falls between the cut and the
reading.

★★★ **HE STOPPED ON THE INSTRUMENT.** The 214,824 / 23 reading was quoted to him past the 180,000
quality line and his whole reply was ***"wrap"***. ⇒ **#283, #290, #293, #294 and #295 stopped ON the
instrument; #291 and #292 each overrode once then obeyed. `s283-D1`'s override shape STAYS AT n=2 and
nothing is inferred from this session.**

### OPEN, DAVE'S — every one a QUESTION PUT AT THE WRAP CALL, not a state of the world

> **OPEN, DAVE'S:** the presentation, Friday the 25th, three days out and the internal — his v12/v13 notes, the deck's brain three passes behind the drawing file, the workers' finessing he ruled is done "together", and the dashboard review on the demo's cold brief — put to Dave at the wrap call · whether the 98 of 131 enacted rulings with no sha is a debt to be paid or a ruling that binds from #295 forward — put to Dave at the wrap call · whether "enacted" is a word about code or about the repo's surfaces, which decides `s294-D11`'s sha — put to Dave at the wrap call · what word `s294-D12`'s third state gets, enacted in code and discharged in fact by his own hands — put to Dave at the wrap call · whether a conductor's PUSH verdict reaches a bare `git push` or only the sanctioned arm — put to Dave at the wrap call · whether the dirt gate gets an ACK hatch spelled like its six neighbours — put to Dave at the wrap call · whether `_capture_gate.py --selftest`'s CI fail on "no pre-flight stamp" is false or true, undiagnosed at four seats — put to Dave at the wrap call · whether `--wrap` gets wired into a CI step so the zero stops being invisible — put to Dave at the wrap call · whether an exact-value selftest pin and a shrink-only literal keep their friction or share one named home — put to Dave at the wrap call · whether `s295-D1`'s literal `post-mortem #N:` is amended by addition now that five of the six strata it closes have no colon — put to Dave at the wrap call · whether a re-base on n=1 with his own "possibly" gets a confirmation clause or stays prose — put to Dave at the wrap call · whether the other worker's J7 and its idea notes are reconciled at #296 or left where they stand — put to Dave at the wrap call · whether the `s241-D2` banner cap of 1,200 tape still constrains girth or now shapes the record, at a third consecutive wrap closing with zero headroom — put to Dave at the wrap call

*(The line above is the machine-read form `_capture_gate.hook_open_items_recheck` (`s271-D4`,
ADVISORY) parses: `HOOK_OPEN_LABEL_RE` wants the label and a colon on one line, and every segment
carries the ruling's own legal phrase, so each item PASSES as a question rather than being graded as a
claim about the world. ⚠ **The numbered list below is the same thirteen items for a human reader and is
the authority on their bodies; the line above exists so the arm is not silently blind** — the failure
#294 found and recorded at its § POST-WRAP 2.)*

1. **The presentation — Friday 2026-09-25, three days out, and it is the internal.** He raised it
   himself, unprompted, mid-ruling. **#296 opens on the deck.**
2. **The 98 of 131.** Debt, or a `_FROM_SESSION` boundary? Until it is answered `ENACTED_SHA_BLOCKING`
   can never flip.
3. **`s294-D11`'s stamp** — code, or surfaces? Two filed records say eleven where the commit says ten.
4. **`s294-D12`'s third state** — third consecutive session it has been named, and the workaround is in
   the record's own status prose.
5. **Does a conductor's PUSH verdict reach a bare `git push`?** The runbook says *"never terminal
   push"* in the imperative; `s133-D2` superseded it by addition, and the addition is the arm that
   refused.
6. **Does the dirt gate get `PUSH_DIRT_ACK`?** The script diagnoses the exact case that would justify
   it and has no hatch to offer.
7. **`_capture_gate.py --selftest`'s CI fail** — possibly false; the file carries three `pre-flight:`
   stamps the arm does not see. **Whether the suite has a second failure is UNKNOWN.**
8. **`--wrap` is in no CI step.** The zero this session reached is invisible to CI.
9. **Exact-pin vs shrink-only, now at two constants.** Every legal shrink is a two-file edit or a red
   selftest.
10. **`s295-D1`'s `post-mortem #N:` literal** against the strata's `POST-MORTEM #N —` form.
11. **`s295-D3` is n=1 and his word is *"possibly"*.** The next cold boots are the test.
12. **The other worker's J7 and idea notes**, still unreconciled and still dirty, and with
    `_GRADE-DECISIONS.jsonl` they refused the sanctioned push arm at three seats today.
13. **The banner cap at exactly 1,200 of 1,200 for a third consecutive wrap.** Three things were
    compressed out of this wrap's banner and each is named in the #295 stratum. **Reported, not
    appealed: the cap is his.**

### STRUCK BY ADDITION AT THIS WRAP — the `s271-D4` re-read, run against `knowledge/_rulings.json`

- **#294's open item 1, the push and a CI read-back job by job** — **CLOSED BY ACT**: `5e9e2cbe`,
  `git ls-remote` = local HEAD, three CI runs read job by job.
- **#294's open item 2, who stamps `status: enacted`** — **CLOSED by `s295-D2`** and by the
  `--set-status` writer at `ac55c7b2`; fourteen stamped. ⚠ **The clause *"all twelve"* is NOT struck:
  `s294-D10` and `s294-D11` are named and unstamped, with the reason.**
- **#294's open item 7, boot 74,656 against a shrink-only 70,000 with seven readings over it** —
  **CLOSED by `s295-D3` + `s295-D4`.** ⚠ **What is closed is the arithmetic being unowned, not the
  ceiling being settled — his own word is *"possibly"*.**
- **`_HANDOFF-145` § POST-WRAP 4, *"every commit now stales `_CHAIN.md` by construction"*** —
  **CLOSED by lane I3's `check()` re-ask at `c896bcaf`**, proven by CI survey `[120]` GREEN.
- ⛔ **NOT STRUCK, deliberately:** #294's items 3, 4, 5, 6, 8, 9 and 10 (untouched this session) · the
  `--push` end-to-end proof (it failed for a NEW reason; the old reason is retired, the item is not) ·
  dream pass 13's seven and pass 9's six, enacted but still unruled — **`s183-D1` strikes a headline,
  never a clause.** *"A strike that is wrong is worse than an item that is merely stale."*

---

## FOR THE CONDUCTOR, AT #296's OPENER

1. **Check § THE PLACEMENT at the foot of this file FIRST** — it says whether this seat placed the
   index line and body itself, or whether the write is still owed. **Do not assume either.**
2. **`index.md` caps at 49,152 B.** #295's opener left it at **14,268 B** after the #291 line moved to
   `MEMORY-ARCHIVE-2` (which is now at 45,733 of 49,152 — ⛔ **the NEXT cut opens
   `MEMORY-ARCHIVE-3`**). Check the headroom before writing and **shard rather than truncate**.
3. **Append the measured `replay` figure** — what reading this wrap's report back cost you — to the
   `wrap-handover` line in `GOOD-MORNING.md`'s #295 stratum. It is `unobservable` from the wrap seat by
   construction and **you are the only seat that can close it.** It has now been left open at #293,
   #294 and #295.
4. ⛔ **`#296 OPENS ON THE DECK.** He asked for the presentation by name this session and Friday is
   three days out. Nothing else in the owed list is time-boxed the way that is.

---

## THE PLACEMENT

*(written at the close of the ritual, after the store write was attempted)*

**PLACEMENT RECEIPT — DONE AT THIS SEAT, AT THE #295 WRAP, 2026-09-22.** ⛔ **THE WRAP SUB PLACED IT
ITSELF; the write is NOT owed to the conductor.** The #295 brief permitted the attempt if the memory
tools were present at this seat, they were, and the attempt succeeded:
`wrap-295-the-push-landed-and-the-wrap-gate-reached-zero.md` written (**9,520 B**, version
`7a37f72e3a49`), and the index line placed at the TOP of `index.md`, newest first (**14,268 →
19,918 of 49,152 B**, version `c054a2a3f24a`). ⇒ **This is the THIRD consecutive session in which a
sub reached the store** (#293 wrote directly, #294 could and was fenced by its brief, #295 did), and
it is the strongest evidence yet that `_RUNBOOK-capture-ritual.md` § step 3's *"a sub-lane cannot"*
should read *"reserved to the conductor"*. **That one-line runbook correction is still owed and is
ruling-shaped.**

⚠ **DECLARED SKIP, WITH ITS SIZE — THE INDEX NOW HOLDS FOUR LINES, NOT THE STANDING THREE.** The
`#292` line (**≈5,900 B**) was **NOT** cut to an archive shard. The reason is measured, not
convenience: `MEMORY-ARCHIVE-2.md` stands at **45,733 of 49,152 B** and **cannot take that line
whole**, so the standing rule inscribed in the index itself applies — **open `MEMORY-ARCHIVE-3.md`,
never truncate, never suspend** — and opening a new shard from this seat means hand-transcribing a
5,900-character heavily-marked-up line, **which is the single act most likely to corrupt the record it
exists to preserve**. ⛔ **There is NO cap pressure: `index.md` is at 19,918 of 49,152 B**, so nothing
is at risk by deferring. **The cut is OWED at #296's opener and the shard to open is
`MEMORY-ARCHIVE-3.md`.** ⚠ This is the same shape as #294's own declared skip on the #291 line, which
was discharged at #295's opener — the mechanism works, and the skip is declared rather than found.

⚠ **NOTHING ELSE IN THE STORE WAS TOUCHED.** No `areas/` file was edited at this seat: the
presentation note (`areas/presentation-friday-25th-288.md`) already carries his Friday-25th context,
and **this session produced no NEW durable `[stated]` line about Dave that the store does not already
hold** — his four utterances are all about repo work and are inscribed repo-side as `s295-D1` …
`s295-D4` and verbatim in `notes/_lanes/295/DAVE-RULINGS-2026-09-21.md`. **The repo is the record;
memory is the accelerator.**

## ⛔★★ THE ARM WAS ASKED TWICE, AND THE SECOND ASKING FOUND A THIRD BLINDNESS — DECLARED, NOT DISCOVERED LATER

`_capture_gate.hook_open_items_recheck` was driven twice, per `_HANDOFF-145` § POST-WRAP 2
(*"ask the arm twice, or inherit its silence"*). **Two blindnesses were met, and the second is new.**

**1 — it does not find this file on its own.** Called as the wrap gate calls it, it returns:

```
hook re-check (`s271-D4`): DECLARED SKIP — mount … carries no .auto-memory/MEMORY.md.
The memory store lives OUTSIDE this repo (a Cowork surface); on a tree without it there is
genuinely nothing to re-read. This is UNKNOWN, never a pass. Pass an explicit hook path to drive it.
```

✅ **It says UNKNOWN rather than passing, which is the honest form** — but it means **the wrap gate's
own run never reads the hook**, and the arm only bites when a seat passes `path=` by hand. Driven that
way it reads correctly: **13 "Open, Dave's" item(s) read · 13 written as a QUESTION PUT and PASSING by
the ruling's own form · 0 item(s) named.**

**2 — ⛔★★ AND ITS COMPARISON SET IS EMPTY BY CONSTRUCTION AT A DATE-SPLIT WRAP, WHICH NOTHING HAS
WRITTEN DOWN BEFORE.** Its own second line:

```
hook re-check: the ledger holds NO ruling dated 2026-09-22, so nothing can have been closed by
the session's own rulings — 13 item(s) stand unchallenged. An empty comparison set is DECLARED,
never read as a clean bill.
```

⇒ **The arm matches the hook's items against rulings dated the SAME DAY as the hook.** This hook is
dated **2026-09-22** (the ritual's day, correctly, so the gate's `is not today` check grades a true
statement) while **all four of this session's rulings are dated 2026-09-21** (the session's day,
correctly, per `s294-D11` — *"session date on keys and filenames"*). ⇒ ⛔ **`s294-D11`'s date discipline
and `s271-D4`'s same-day match are in direct tension: at EVERY date-split wrap the arm's comparison set
is empty and it can never name an item, however many the session closed.** ✅ **It DECLARES the empty
set rather than reporting a clean run, which is exactly right and is why this was findable at all** —
but the strike list below was therefore produced **entirely by hand**, against `knowledge/_rulings.json`
read directly, and the machine half contributed **nothing** at this wrap. ⚠ **Third blindness in this
arm's short life** (#294 found `HOOK_OPEN_LABEL_RE` wanting a label and a colon on one line; #225's
sibling found the 2c carry gate going quiet). ⇒ **Should the match be on the ruling's SESSION ORDINAL
rather than its date? That is ruling-shaped and is HIS.** [[instrument-without-a-consumer]]
