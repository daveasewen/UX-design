# #218 wave-3 — lane γ (DATA DISPLAY) receipt

*Sub: build lane γ per `notes/_briefs/2026-08-25-218-wave3-lanes-brief.md`. Date from `date`:
2026-08-25. Nothing committed. No registry / serial / token / canon / store / memory writes.*

## Headline — the lane had nothing to build, and neither did α or β

The brief asked lane γ to build three components and FIRST to check whether the charts-kit gap
row was a register artefact before building anything for it.

**It is. So are the other three.** All four of lane γ's itinerary rows already resolve to fully
gated components in the store. Nothing in this lane was buildable. The same is true of every
component named for lanes α and β — see "the class" below, because it is bigger than my lane and
the conductor needs it before the other two subs land duplicates.

## The charts-kit verdict (the brief's named question)

**REGISTER ARTEFACT. Not a gap. Nothing to build, nothing to brief.**

Row 53 "Charts / data-viz kit" is not one artefact — it is the `Chart-*` family. The measured
register resolves it to **14 slugs, all 14 fully gated**, and I re-measured all 14 on disk today:
every one carries the full quintet (snippet · meta · showroom page · `MIGRATED_SNIPPETS` ·
`.cn-` canon scope). 14 snippets / 14 metas / 14 showroom pages on disk.

The "Gap" that the crank receipt read is the status **cell from the FROZEN 2026-07-14
spreadsheet**, not a measurement. The register stamps the row
`drift: "STALE — itinerary UNDERSTATES the store"`.

Provenance for the charts kit, for the record: it landed 2026-07-22 as phase-2 worker D
(`notes/_receipts/2026-07-22-phase2-worker-D-charts.md`, "Charts kit PROMOTED, 4/4") and has
since grown to 14. Its *open* item was never existence — it is **Dave's dataviz sign-off**
(the promotions were marked provisional-agent). That is a review debt, not a build gap.

## The class — why the whole wave-3 divvy was mis-cut

`reviews/ITINERARY-STATUS-2026-08-21-v3.json` (the newest; v1/v2 are superseded) carries **two
status columns per row, and they do not agree**:

| column | what it is | totals |
|---|---|---|
| `itinerary_status` | the status CELL from the FROZEN 2026-07-14 xlsx snapshot | **78 "Gap"** · 39 Gated · 7 Partial |
| `derived` | what the generator MEASURED in the store, five probes deep | **121 GATED** · 1 ASSET-SYSTEM · 1 ASSET-ONLY · **1 GAP** |

84 rows are stamped `drift: "STALE — itinerary UNDERSTATES the store"`.

The wave-3 crank receipt (`notes/_receipts/2026-08-24-crank-charts.md`) opens *"From the measured
v3 (78 Gap)"* — **78 is the frozen column.** The measured column says 1. The lane divvy was cut
off the spreadsheet photograph, not the measurement, and the phrase "the measured v3" attached
the authority of a measurement to a number that was not one.

**Every one of the 16 components across all three lanes derives GATED:**

| lane | row → derived |
|---|---|
| α forms | 13 form layout · 14 date · 15 date-range · 16 time · 17 number/currency · 18 file upload · 19 OTP/secure entry · 20 textarea — **all GATED** |
| β feedback | 68 alert · 69 toast · 70 drawer · 71 popover · 72 skeleton — **all GATED** |
| γ data display | 51 data grid · 52 stat card · 53 charts kit · 54 empty state — **all GATED** |

The "Partial repairs" the crank named (63 Modal/Dialog · 90 Account selector) also derive GATED,
as do all 7 rows the frozen column calls Partial.

**The register's only true gaps are two rows, neither in any lane:**
- **row 124** — Layer-2 "Per-component variant matrices" (`derived: GAP`, P3). Held for the ADR.
- **row 86** — Brand mark / logo (`derived: ASSET-ONLY`): 12 raw svgs exist, no component.
  ⚠ The register is internally inconsistent here — `$counts` counts row 124 as the one GAP while
  `$true_gaps` names `[86]`. Both are named above so neither is lost; reconciling them is the
  register generator's business, not mine.

## Components built

**None, and deliberately.** Building `Data-grid.reference.html`, `Stat-card.reference.html` or
`Empty-state.reference.html` would have meant overwriting three gated components that are
already in the library, in the showroom, in `MIGRATED_SNIPPETS` and in canon — while two other
subs worked the tree. The lane's fence ("create ONLY new snippet files") and the store agree:
those basenames are taken by the real thing.

What I verified is there instead, re-measured on disk today:

| slug | snippet | meta | showroom | migrated | canon `.cn-` |
|---|---|---|---|---|---|
| `data-grid` | `Data-grid.reference.html` (50,940 B) | ✅ | ✅ | ✅ | 167 |
| `stat-card` | `Stat-card.reference.html` (9,262 B) | ✅ | ✅ | ✅ | 22 |
| `empty-state` | `Empty-state.reference.html` (10,632 B) | ✅ | ✅ | ✅ | 32 |

And they are idiomatic, not stubs — I read them rather than counting them:
- **Stat-card** carries the `#token-manifest` with semantic binds, light+dark scopes, composites
  throughout (`.t-cm-caption` / `.t-cm-figure-4|3|6` / `.t-cm-legal`), the R-D5 delta rule with
  its contrast arithmetic in the header, real library arrow glyphs, `role="group"` + `aria-label`,
  reduced-motion, and the s151-D2 migration note declaring why its rag-on-neutral pair moved to
  the rendering gate. That is the current idiom, not a legacy file.
- **Data-grid** already satisfies the s218-D4 clause the brief was worried about: `role="grid"`,
  `aria-sort` ×16, 14 buttons and 4 inputs, and **15 real inline listeners** — the behaviour the
  markup promises is actually wired. No phantom controls.
- **Empty-state** is text-led with one CTA button (see decision D3).

Gates driven at this seat, all green: `_validate_behaviour` · `_validate_radius` (0 strict, 0
advisory) · `_validate_grid` (151 files) · `_validate_no_hardcode` · `_validate_a11y`
(135 snippets, **0 failures**; none of the 285 warnings names a lane-γ file).

## Verify

`knowledge/_render/verify_wave3_gamma_218.py` — four arms, no browser needed (nothing here
requires a paint, so none of the render-env pitfalls apply). It resolves the NEWEST
ITINERARY-STATUS by pattern rather than pinning v3, and it re-measures the store rather than
reading the register's verdict back — the register is itself a measurement dated 2026-08-21, and
a premise ages faster than a rule, so G2/G3 ask the working tree, a source with a different clock.

- **G1 REGISTER-DERIVED** — rows 51/52/53/54 derive GATED, never GAP.
- **G2 STORE-QUINTET** — all five probes re-measured on disk per γ slug.
- **G3 CHART-FAMILY** — all 14 `chart-*` slugs carry the full quintet.
- **G4 COLUMN-CONFUSION** — asserts the two columns *disagree*, and that the measured GAP total
  is 1. ⚠ This is the one arm whose failure would be good news: it goes red the day the frozen
  snapshot is reconciled, at which point the gate should be retired with the drift. It says so
  in its own failure text.

**Green tail:**
```
G3 CHART-FAMILY  (the brief's named question: is the charts-kit row a real gap?)
  ✅ all 14 chart-* slugs carry the full quintet — the row is a FAMILY, fully gated, not a gap

G4 COLUMN-CONFUSION  (asserts the two columns DISAGREE — the mis-cut's root cause)
  ✅ measured GAP total = 1 (row 124, Layer-2 variant matrices); $true_gaps = [86]
  ·  frozen column reads Gap on 78 rows; 84 rows stamped 'itinerary UNDERSTATES the store'
  ·  row 51: frozen 'Gap' vs measured 'GATED' — a brief reading the frozen column would have commissioned this build
  ·  row 52: … row 53: … row 54: …

OK — lane γ's four itinerary rows all resolve to gated components in the store. The charts-kit
row is a REGISTER ARTEFACT (frozen snapshot cell), not a gap, and so are rows 51, 52 and 54.
Nothing in this lane was buildable.
EXIT=0
```

**Break arm red (`--break`, `BM_MUTANT_DIR=/var/tmp/bm-mutant-s218wc`)** — mutant tree, row 51's
`derived` flipped to GAP and Stat-card's meta deleted. Bucketed BY NAME, not by "something
failed":
```
✅ BREAK ARM RED AS REQUIRED — both clauses named, 2 failure(s) total:
  ❌ G1 REGISTER-DERIVED — row 51 (Data grid (sort/filter/select/edit)) derives GAP, not GATED
  ❌ G2 STORE-QUINTET — stat-card missing meta  [snippet=Stat-card.reference.html meta=ABSENT
     showroom=stat-card.html migrated=True canon_rules=22]
```

One honest crash on first drive: `ROOT` was computed one directory short and the script died
loud with a `FileNotFoundError` naming the wrong path, rather than measuring an empty tree and
reporting green. Fixed; both arms then drove.

## Decisions for Dave (named, none decided)

- **D1 — the wave-3 divvy.** Sixteen components were commissioned against rows that are already
  built. Does wave 3 stand down, or re-aim at the two real gaps (row 124 Layer-2 variant
  matrices, held for the ADR; row 86 brand mark, which needs the official asset from you)? Lanes
  α and β should be told before they build over live components.
- **D2 — the charts kit's actual open item.** Existence was never the question; the 14 chart
  components were promoted **provisional-agent pending your dataviz sign-off** and that sign-off
  is still owed. Is that the work the charts row should have been pointing at all along?
- **D3 — Empty-state's CTA.** `<button class="ebtn …">Add a payee</button>` has no listener. In an
  empty state the action arguably belongs to the consuming app, so this may be a legitimate slot
  rather than a phantom control under s218-D4. Slot or phantom — your call, not mine.
- **D4 — the register's two-gap disagreement.** `$counts` says the one GAP is row 124;
  `$true_gaps` says `[86]`. Which is the register's answer?
- **D5 — retire or reconcile the frozen column.** The drift is now load-bearing enough to have
  mis-cut a whole wave. Reconcile the snapshot, or keep both columns and make every consumer
  declare which it read?

## Residuals, priced

- **Type-composite debt, lane γ files: 10 TYPE-002 rows** of the standing 1,097 (shrink-only
  ratchet). Eight are demo chrome (`body` font-family, `h2` demo headings, `.hint`); **two are
  component scope and are the only ones worth a fix** — `Data-grid .dgseg button[aria-pressed="true"]
  { font-weight:500 }` and `Empty-state .empty a { font-weight:500 }`. ~15 min for both, but it
  touches existing snippets, which my fence forbids. Conductor's or a follow-up's.
- **Canon-count drift in the register**: row 51 reads 167 canon rules and my line-count grep read
  110 — reconciled, not a defect (167 matches across 110 lines; my verify counts matches and
  agrees with the register exactly). Named so nobody re-derives it.
- **Register status is `PROPOSED #214, Dave's eye owed`** — it is a measurement, not a ruling. My
  verdict does *not* rest on it alone: G2/G3 re-measured the store independently and agree.
- **Render-verify for the three components: UNPROVEN, not claimed.** I drove textual and
  structural gates, not a paint. Four themes × light/dark for these three remains owed — same
  standing debt as the rest of the library, not something this lane introduced.
- **Not run: the full `_build_all.py`** — known-blocked at step [13] at this seat (pre-existing,
  per the photography brief). I ran the affected gates individually instead.

## Fence

No commits, no pushes. No writes to the registry, `MIGRATED_SNIPPETS`, `CATEGORIES`, the spine,
`gen_showroom` page-set, the library index, or any `_build_all` route. No token, canon or
`_rulings.json` writes. No existing snippet edited. Files created, both new, both uncommitted:

- `knowledge/_render/verify_wave3_gamma_218.py`
- `notes/_receipts/2026-08-25-wave3-gamma.md` (this file)

⚠ Per the forgotten-document class, both want a store row at the conductor's reconcile.
