# Worker receipt — #96-D1 enactment (chart wave-2 snippets)

*2026-08-05 · WORKER · ruling `notes/_MEMENTO-DECISIONS.md § ★ #96` (#96-D1 ①–⑦) ·
NO GIT — conductor commits the reconcile per convention.*

Sandbox stand-up for render-verify: playwright present (v1.62.0 site-packages), `NODE_TLS_REJECT_UNAUTHORIZED=0
python3 -m playwright install chromium` pulled chromium + headless_shell; `libXdamage.so.1` missing on
launch — pulled the arm64 `.deb` (`ports.ubuntu.com/pool/main/libx/libxdamage/…arm64.deb`), unpacked with
`ar x` + `tar --zstd`, pointed `LD_LIBRARY_PATH` at the extracted `.so` — same trick as the #96
re-verify receipt. `goto("file://…")` throughout, never `set_content`, viewports 1180/760.

## Per-item table

| # | Item | File(s) : line(s) | Measured before → after | Verdict |
|---|---|---|---|---|
| 1 | Candlestick redundancy channel | `Chart-candlestick.reference.html:83-87` (CSS), meta.json `accessibility.nonText` | Computed style, up body: `fill:rgb(255,255,255) stroke:rgb(22,134,78) stroke-width:1.5px`; down body: `fill:rgb(185,47,30) stroke:rgb(185,47,30) stroke-width:1.5px`. Same `--data-delta-gain/-loss` tokens, no mint. | **DONE** |
| 2 | Bullet canvas height flexes with row count | `Chart-bullet.reference.html` header comment + inline SVG comment (formula) | Formula `H = 80 + (rowCount-1)*60`, pitch=60 constant. n=3 (shipped demo): 80+120=200 — measured `viewBox="0 0 1108 200"` at 1180px fit, `data-h="200"` — matches unchanged. n=1 → 80, n=10 → 620 (documented, not separately demoed — this file's ONE reference stays 3 rows/580×200 per "single-row appearance identical"). **Geometry is BAKED per DV-D02 (no JS row-count physics)** — the formula is the authoring contract for future instances, not a runtime derivation. | **DONE** (contract documented + formula-verified against the shipped n=3 case; no runtime mechanism exists or was asked for) |
| 3 | Bullet range bands — canon greys + z-order | `Chart-bullet.reference.html:46-63` (CSS vars), `:88-95` (rules) | Surveyed `knowledge/canon/canon.css` — minted `--color-grey-200/300/400` (#EDEDED/#D7D8D6/#B7B7B7 light) and `--color-grey-dark-mode-500/400/300` (#212121/#404040/#474747 dark). File doesn't link canon.css (same as its other tokens) so values are copied verbatim, flagged in-file. Measured computed fill: range-1 `rgb(237,237,237)`, range-2 `rgb(215,216,214)`, range-3 `rgb(183,183,183)` — matches the minted hexes exactly. Z-order: range rects precede the measure rect precede the target line in DOM (SVG paint order) — already correct, confirmed not changed. | **DONE** |
| 4 | 2px separation rule — histogram + stacked-area | `Chart-histogram.reference.html:88` (`rect.dv-series{stroke:var(--page); stroke-width:2}`), `Chart-stacked-area.reference.html:118` (`.dv-band{…stroke:var(--page); stroke-width:2}`) | Precedent found: `dv-004` (donut/pie segment `stroke="var(--page)" stroke-width="2"`) — same idiom applied. Measured computed style: histogram bar `stroke:rgb(255,255,255) stroke-width:2px`; stacked-area band `stroke:rgb(255,255,255) stroke-width:2px`. | **DONE** |
| 5 | `fitOne()` extend for `.dv-band`/`.dv-band-line` | `knowledge/canon/dv-behaviour.js:55-73` (new `path[data-fxs]` handler); `Chart-stacked-area.reference.html:267-282` (added `data-fxs`/`data-ys` to the 3 `.dv-band` paths and 3 `.dv-band-line` polylines) | Source budget checked FIRST: 12,048B → 12,751B after edit, well inside the 16KB ceiling (~3.6KB headroom). `.dv-band-line` was **already** covered by the pre-existing `polyline[data-fxs]` selector (generic, no class check) — only needed the baked `data-fxs`/`data-ys` attributes, which I added; render-verified LIVE at 760px: points moved from baked `111.2,187.9 …` → measured `121.8,187.9 273.3,182.6 424.8,177.2 576.3,168.6` (fit is running). `.dv-band` is a `<path>`, not a polyline — needed the new source handler. **This part is UNPROVEN LIVE in the consumer file**: the AUTO-BEHAVIOUR block in `Chart-stacked-area.reference.html` is a byte-for-byte INJECTED COPY of the source (`gen_component_partials.py` — "DO NOT hand-edit between the markers, edit the source and regenerate"); regeneration is the conductor's serial (same posture as the donut sweep-hook precedent), so I edited the ONE permitted exception (the source) and did not hand-edit the consumer's markers. Measured: at 760px the `.dv-band` `d` attribute is STILL the baked `M111.2,230 L111.2,187.9…` (unchanged) — confirms the fix is real in source but not yet live until the conductor regenerates. Named mechanism, not silently claimed. | **PARTIAL — source DONE, consumer propagation OWED to conductor (regen serial)** |
| 6 | Pie `data-ri` comment states the ruled answer | `Chart-pie.reference.html:25-27` | Added: *"#96-D1 ⑦ RULED: the sweep hook reads data-ri with a PRESENCE CHECK, falling back to ri=0 when absent (as here) — ONE code path shared with the donut, no pie-specific branch."* Prior comment only said "NO data-ri" without stating the resolution; receipt `2026-08-05-wave2-lane3-pie-area.md §2` had flagged the open question, now answered in-file. | **DONE** |
| 7 | Render-verify every touched file | all 5 files above | Playwright chromium, `goto(file://…)`, 1180px/760px, `figure.dv` class + `svg.dv-svg` computed width read at both. Candlestick 1108px→688px; Bullet 1108px→688px (viewBox `0 0 1108 200`); Histogram 1108px→688px; Stacked-area 1084px→664px (matches the #96 re-verify's known 24px-narrower offset for this pane, not a regression); Pie 300px/300px unchanged (pie has no `.dv-fit` class — fixed radial geometry, same posture as the donut, not a regression). All 5 loaded without console/page errors, `dv-fit-on` present on every `figure.dv` post-load. | **DONE, all 5 PROVEN** |

## STOPs

None outright — item 5 is a **declared partial**: the source-side fix is built, byte-budget-checked, and correct by design (mirrors the polyline mechanism this repo already uses), but it cannot go live in `Chart-stacked-area.reference.html` without running `python3 knowledge/gen_component_partials.py`, which is explicitly out of scope (conductor serial, not touched per the task's own rule set and this repo's established convention for the donut sweep hook). Flagging for the conductor rather than hand-editing between the consumer's AUTO-BEHAVIOUR markers.

## Files touched

- `knowledge/snippets/Chart-candlestick.reference.html`
- `knowledge/components/Chart-candlestick.meta.json`
- `knowledge/snippets/Chart-bullet.reference.html`
- `knowledge/components/Chart-bullet.meta.json`
- `knowledge/snippets/Chart-histogram.reference.html`
- `knowledge/components/Chart-histogram.meta.json`
- `knowledge/snippets/Chart-stacked-area.reference.html`
- `knowledge/components/chart-stacked-area.meta.json`
- `knowledge/snippets/Chart-pie.reference.html`
- `knowledge/canon/dv-behaviour.js` (the ONE permitted exception, minimal addition only)

Not touched: `component-types.json`, `_type-bindings.json`, gen_showroom output, any other gate/serial file.
