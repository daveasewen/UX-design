# Worker receipt — Wave lane ② · Chart-donut + Chart-sparkline (chart-revisit fan-out)

*2026-07-24 ~17:20 BST (date from `date`) · FABLE WORKER, lane ② (Fable·medium per the ratified
divvy) · brief: `notes/_briefs/2026-07-24-chart-wave-lane2-donut-sparkline.md` · baseline at lane
start: `fa29858` (verified `git log --oneline -1` — NOTE: newer than the brief's cut; the control
changes ARE canon here: single-cell seg toggles, `--control-h` 32px, dropdown solid arrow, CSV
copy⇄tick — all consumed verbatim) · **NO GIT — conductor commits.** Fences honoured: writes
limited to the two snippets + two metas + this receipt.*

**Context gauge at authoring: 🟠 AMBER ~58%** (in-head tally, ±15%) — verify claims against the
listed evidence before trusting; all mechanical checks were re-run AFTER the last edit.

## Outcome

Both files landed per brief. Donut = FULL Layer-2 on the exemplar pattern (popover · table-popover ·
legend filter/isolate/highlight · value ⇄ % seg · title + H-stack head · HC mode · sweep contract
baked). Sparkline = COMPACT Layer-2 (popover + fit only). Gates 0 blocking on both; parity scripted
clean; render-verified real font (light + dark + HC + filtered + % view + table-open + 2 widths +
JS-off) — **two real defects caught by render, one fixed in-fence, one flagged**.

## Files touched (mine)

- `knowledge/snippets/Chart-donut.reference.html` — rewritten (16.6 → ~30KB)
- `knowledge/snippets/Chart-sparkline.reference.html` — Layer-2 edits
- `knowledge/components/chart-donut.meta.json` · `chart-sparkline.meta.json` — refreshed
- this receipt. **Also touched (validator side-effects, NOT hand-edits):** gate report files
  regenerated by my read-only gate runs (`_DATAVIZ-GATE.md`, `_A11Y-GATE.md`, `_COVERAGE-GATE.md`,
  `_ICON-SOURCE-AUDIT.md`, `_SNIPPET-AUDIT.md`, `_TYPE-BLAST-GATE.md`, `_THEME-PROVENANCE-GATE.md`,
  `_ADVISORY-SIGNALS.md`, `_LIVE-STATE-CHECK.md`) — deterministic, the conductor's build re-emits.
  **Shared tree note:** lane ① (`Chart-bar`, modified) and lane ③ (`Chart-combo`, untracked, no
  meta yet — the coverage FAIL is theirs, in-flight) plus `reviews/RADIUS-CORNER-TUNER-…-v2.html`
  were live in the tree throughout; I did not touch any of them.

## What landed — donut

1. **Popover:** every segment = focus stop (`tabindex=0`, `role=img`, aria == data-tip == table,
   one source; native `<title>` removed). Segments carry `data-series-group` + `.dv-marker` so the
   EXISTING dv-behaviour highlight/filter modules take them with zero source edits.
2. **Table-view popover** (dropdown + solid rotating arrow + floating card) + **CSV copy⇄tick** +
   **H-stack head with optional title** — exemplar canon verbatim, all controls literal 32px
   (`--control-h`; verified computed: 7 controls = [32]).
3. **Legend-as-filter** (spider variant): quiet buttons, shift/double-click isolate, hover/focus
   highlight (`.dv-quiet` data-layer only), AA hollow off-state (verified computed: text #626262,
   transparent bg, 2px axis-grey border). **Judged:** filtering hides segments IN PLACE (honest
   gap); **centre total stays the FULL total** — a filtered ring that re-totalled would lie about
   composition; flag if Dave wants the sum to follow.
4. **Value ⇄ % (menu 5):** the Segmented-control atom driving BAKED variant groups
   (`data-dv-view="value"/"percent"`) — **zero behaviour edits needed; `segView` drives it as-is.**
   Centre total (2320 ⇄ 100%), direct-label value lines, and the table column (Amount ⇄ Share %)
   all mirror; JS-off = value view baked. **Judged:** tips/aria always carry BOTH readings (a tip
   that switched would desync from a screen-reader's earlier read). **CSV always exports both
   columns** (copyCsv serialises all cells regardless of visibility — truth over view; flag if
   export should follow the active mode).
5. **Variants:** spider + direct both stay, D-Q2 NOT picked; letters-on-segments NOT resurrected.
6. **DV-D08 rebind:** leader/legend letters → `t-cm-chart-key` 12/700 · labels/legend/controls →
   `t-cm-chart-label` 12/500 · direct values → `t-cm-chart-value` 12/500 tabular · centre total
   stays `t-cm-figure-3` 24/500. Verified computed at render.
7. **HC mode:** `data-contrast="high"` remap onto `data/series-high-contrast/1–5` (store values,
   hex from canon.css; manifest pairs declared).
8. **Sweep contract BAKED:** segments carry `data-cx/-cy/-ro/-ri/-a1/-a2`, annotations carry
   `data-seq` + `.show` (visible by default — JS-off/reduced-motion/pre-hook all complete).

## What landed — sparkline (compact, per brief)

Popover: the standalone trend line = ONE focus stop (aria == tip; per-point stops would fight the
axis-free idiom — judged). Fit: rides dv-behaviour's existing init alone — `.dv-fit-on` releases
the fixed 340px to `width:100%`, pure CSS (no text → DV-D02 vacuous; verified 928@1000 / 408@480 /
**340 JS-off**). No legend/toolbar/title (compact posture — flag if the wave wants toolbar parity).
KPI inline specimen untouched (passive, aria-hidden; Stat-card seam/B-Q6 intact). DV-D08: summary +
caption → `t-cm-chart-label`; table stays `t-cm-legal`. Empty AUTO-BEHAVIOUR markers pre-landed.

## ★ FOR THE CONDUCTOR — serials this lane needs

1. **Register both files** in `component-types.json` group `dataviz` + run
   `gen_component_partials.py` (markers pre-landed empty in both).
2. **★ THE SWEEP HOOK — and the per-capability split is now ARITHMETIC, not a hunch:** proposed
   hook code = `outputs/proposed-sweep-hook.js` this session (also verified LIVE — see renders;
   lands exactly on the baked `d` at animation end, reduced-motion honoured, try/catch shell).
   **Measured: hook 2,740 B + current source 14,620 B = 17,360 > the 16,384 gate cap.** It cannot
   land in the single source. Options for the conductor/Dave: (a) split per capability
   (`dv-behaviour` core + per-family modules, registry `$behaviour` per member — the brief's
   anticipated restructure, ADR-0013 accretion from observed need), or (b) re-dial the 16KB cap
   (it is Dave's "light/fast" made gateable — his ruling to amend, not ours).
3. **Blast-radius `--update`:** `.seg.sm button` now matches Chart-bar (lane ①) + Chart-donut —
   one acknowledged-radius update covers the wave (expect combo too).
4. **Type-composites standalone run FAILS 988× across 80/80 files** — pre-existing, not wired into
   the build; not chased (gate-glob rule).

## Defects caught by render-verify (attribute-the-diff)

- **FIXED (mine, in-fence):** the promoted direct-labels svg was `viewBox 0 0 400 260` at
  `width=440 height=286` — a silent **1.1× text stretch (DV-D02's banned physics)** AND left-anchored
  labels painted outside the box, clipped by the stage ("Other" rendered ")ther"). Now
  `viewBox="-72 0 592 260" width=592 height=260`, 1:1, all annotations inside. Pre-existing from
  the 07-22 promotion (render-verify was OWED then); a mechanical check never saw it.
- **FLAGGED (shared pattern, NOT fixed here):** JS-off, the seg's ACTIVE cell is invisible —
  `aria-pressed=true → color:var(--page)` on a transparent track whose `.ind` fill is JS-positioned.
  **Chart-line's Monthly/Year-to-date pair has the same wart** (pattern copied verbatim). Fix
  belongs at the atom/exemplar (e.g. a CSS-only default fill for the first cell), conductor/Dave.

## Verification evidence

- **Gates (re-run after last edit):** dataviz PASS donut 2 charts + spark 1, **0 blocking / 0
  advisory** · snippets 67/0 · a11y 0 fail (no new warnings) · grid PASS · radius 0 · icons 0
  UNKNOWN · behaviour gate PASS (source untouched, 14,620 B) · partials ratchet 0 strict ·
  legacy-leak 0 · state-snap OK · token-tiers 0 strict.
- **Parity script (mechanical, ALL CLEAN):** 10 segments — angle spans == values/2320 to 0.01° ·
  arc `M` endpoints == angle-derived to 0.15 · tip == aria == table BOTH figures incl. the %
  column (41/18/13/11/17, sums 100) · centre variants baked (2×2320, 2×100%) · every percent
  element baked `.dv-off`, no value element off · `data-seq` 0–4 × 5 carriers · spark 12 points ==
  table exact (y = 86−(v−80)/24·82 ±0.2).
- **Render-verify (real font asserted, headless shell per runbook):** donut light+dark @1180 ·
  @560 (head wraps; stage scrolls = DV-D02 donut static answer) · HC · %-view + filtered-B +
  table-open (hollow swatch, honest gap, Share column) · JS-off @1180 · spark light+dark @1000+480
  + JS-off. Live DOM: focus tip "A · Housing: £950 (41%)" · isolate C via Shift+click
  [f,f,t,f,f] · panel focus/Esc via delegated handlers · seg slide · filter visibility:hidden.
  PNGs in session outputs (agent-side only); behaviour tested on TEST copies with dv-behaviour +
  the proposed hook injected (repo files carry empty markers — JS-off state IS the repo state).

## Open Qs (lane-scoped)

- Centre total under filter (full vs visible sum) · CSV both-columns export · sparkline toolbar
  parity · the JS-off seg wart — all flagged above, none blocking.
- D-Q2 (spider vs direct default) untouched, Dave's.

*No Dave rulings were absorbed mid-flight this lane (none arrived). No writes outside the fence.*
