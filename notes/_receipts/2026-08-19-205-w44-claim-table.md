# Claim table — #205 W-44 build-PM lane (schema'd claim/challenge tables + join + evidence linter)

*⛔ **GENERATED — DO NOT HAND-EDIT.** Source of truth: `notes/_claims/205-w44-claims.jsonl`. Regenerate with*
*`python3 knowledge/_gen_claim_table_md.py notes/_claims/205-w44-claims.jsonl --out <this file>`.*
*Write-once, ADR-0017: one home for the live facts, this file is an address.*

**Generated tally (24 row(s)):** PROVEN 19 · MEASURED 0 · CLAIMED 0 · UNPROVEN 5

## A · The tools built

| id | claim | evidence pointer | tag | note |
|---|---|---|---|---|
| `W44-1` | knowledge/_claimtable.py exists and defines the claim/challenge JSONL schema (id · kind · claim · evidence · tag · verdict · note · fence · rc · section · lane) with a loader that never skips a line silently | `python3 knowledge/_claimtable.py --selftest` -> rc=0 · 12 arms print, ending `_claimtable selftest PASS` | **PROVEN** | — |
| `W44-2` | knowledge/_join_claim_tables.py joins claim+challenge JSONL on id and emits ONLY DISAGREEMENT / NEW / FENCE / UNTESTED / UNCHALLENGED rows; plain CONFIRMED collapses to a count | `python3 knowledge/_join_claim_tables.py --selftest` -> rc=0 · `exactly ONE row collapsed into the CONFIRMED count` | **PROVEN** | — |
| `W44-3` | the join SURFACES and never suppresses: four hideable classes are proven to appear by a dedicated suppression arm (fenced-CONFIRMED, unmatched claim, unmatched challenge, contradiction) | `python3 knowledge/_join_claim_tables.py --selftest` -> rc=0 · lines `suppression arm: S-1/S-3/N-9/S-4 surfaces as DISAGREEMENT/FENCE/NEW/UNCHALLENGED` | **PROVEN** | s204-D1: a row it hides is a decision nobody made. The fenced CONFIRMED is the one a naive collapse would swallow. |
| `W44-4` | an unparseable row makes every consumer fail LOUD and NAMED with file:line and a declared RESIDUAL count, never a silent skip | `python3 knowledge/_join_claim_tables.py --selftest` -> rc=0, residual arm prints `RESIDUAL: 1 row(s) are NOT in the analysis below` and returns rc=1 from main() | **PROVEN** | a-crash-is-not-a-fail |
| `W44-5` | knowledge/_gen_claim_table_md.py generates the human markdown FROM the JSONL, stamps its source, generates its tallies, and REFUSES to render a partial table | `python3 knowledge/_gen_claim_table_md.py --selftest` -> rc=0 · `plant caught: an unparseable row makes the render REFUSE (rc=1)` | **PROVEN** | write-once ADR-0017 leg of s204-D1 |
| `W44-6` | THIS claim table is itself generated from JSONL by the tool this lane built — the dogfood test of leg 4 | `python3 knowledge/_gen_claim_table_md.py notes/_claims/205-w44-claims.jsonl --out notes/_receipts/2026-08-19-205-w44-claim-table.md` -> rc=0 | **PROVEN** | amendment ① (incremental claim table): rows appended as each piece landed, md regenerated each time. |

## B · Driven on REAL data — the acceptance test

| id | claim | evidence pointer | tag | note |
|---|---|---|---|---|
| `W44-7` | #204's two markdown tables are converted to the new schema as DERIVED copies; the originals are untouched | `python3 notes/_claims/_convert_204_tables.py --write` -> rc=0 · outputs notes/_claims/204-buildpm-claims.jsonl (62 rows) + notes/_claims/204-verifier-challenges.jsonl (64 rows) · `git status --porcelain notes/_receipts/2026-08-19-204-buildpm-claim-table.md notes/_receipts/2026-08-19-204-verifier-challenge-table.md` -> empty | **PROVEN** | the converter is kept in-repo (s191-D2) so the derivation is reproducible, not hand-typed |
| `W44-8` | the join runs clean on the real pair: 126 rows in, 23 surfaced, 44 plain CONFIRMED collapsed to a count, 0 parse residual, 0 loud inconsistencies | `python3 knowledge/_join_claim_tables.py notes/_claims/204-buildpm-claims.jsonl notes/_claims/204-verifier-challenges.jsonl --md notes/_claims/204-JOIN.md` -> rc=0 · `Surfaced rows: 23 (DISAGREEMENT 3 · NEW 4 · FENCE 2 · UNTESTED 11 · UNCHALLENGED 3)` | **PROVEN** | — |
| `W44-9` | the join produced a finding NOBODY had: three #204 claims (C-5, F-7, F-8) were never challenged at all, and the verifier's own §4 OMISSIONS list does not name them | notes/_claims/204-JOIN.md section `## UNCHALLENGED — 3` lists C-5, F-7, F-8 | **PROVEN** | this is the class the join exists for: an inner join would have hidden all three |
| `W44-10` | the mechanical verdict counts are CONFIRMED 46 · CONTRADICTED 3 · UNTESTED 11 · NEW 4 — they DO NOT reconcile with the #204 headline's 34/3/12/4 on CONFIRMED or UNTESTED | `python3 knowledge/_join_claim_tables.py notes/_claims/204-buildpm-claims.jsonl notes/_claims/204-verifier-challenges.jsonl` -> `Verdict tally (parsed rows only): CONFIRMED 46 · CONTRADICTED 3 · UNTESTED 11 · NEW 4` | **PROVEN** | NOT MASSAGED. CONTRADICTED 3 and NEW 4 reproduce exactly. CONFIRMED and UNTESTED do not reproduce under any consistent counting rule tried: by SOURCE ROW the table has 40 CONFIRMED / 7 UNTESTED; by ID after splitting combined cells, 46/11. Neither is 34/12. The headline is a typed count and typed counts lie (#88) — which is precisely why this lane exists. |
| `W44-11` | the evidence linter on the real pair finds 5 genuine s182-D1 failures in #204's own tables after this lane repaired its own converter defects | `python3 knowledge/_validate_evidence.py notes/_claims/204-buildpm-claims.jsonl notes/_claims/204-verifier-challenges.jsonl --sample 12 --seed 205` -> rc=1 · `s182-D1 LINT: 5 failure(s)` | **PROVEN** | the five: L1-1 DEAD POINTER `tokens/_blast-radius.json` (written without its knowledge/ prefix, twice); L1-4, C-2, H-5 carry only cross-references to other rows, no probeable token of their own. |
| `W44-12` | the sampler re-ran 12 seeded rows: 3 reproduced their declared rc exactly, 3 were REFUSED [SIDE-EFFECTS] rather than run, 6 ran with no rc declared in the source prose to compare against | same run · `SAMPLER · seed=205 · pool=65 row(s) with a command · drawn=12: C-1, C-7, D-3, F-4, G-2, G-4, G-7, H-4, L1-1, L1-11, L1-6, L1-8` · `RAN H-4 rc=1 (matches declared)` · `RAN L1-1 rc=0 (matches)` · `RAN L1-6 rc=1 (matches)` · `DECLARED GAP: 3 of 12 sampled row(s) were NOT run` | **PROVEN** | the 3 refusals are bare `python3 knowledge/_validate_*.py` forms, which rewrite tracked audits — #204 declared that stop twice and the sampler honours it rather than dirtying the tree to look thorough |

## C · Fix loop (amendment ②) — my own tools contradicted me three times

| id | claim | evidence pointer | tag | note |
|---|---|---|---|---|
| `W44-13` | the linter's first real run reported 18 failures; 6 were a defect in MY converter (clean() stripped lone `*`, turning `REVIEW-204-*.html` into the dead path `REVIEW-204-.html`) | notes/_claims/_convert_204_tables.py clean() now carries the fix and its reason inline; re-run -> `s182-D1 LINT: 9 failure(s)`, down from 18 | **PROVEN** | repaired in-lane, not carried out as a residual |
| `W44-14` | 3 more were a false-positive class in MY linter: a glob PATTERN is not a dead pointer. Fixed with a two-direction selftest arm — the pattern is exempt, a genuinely dead path under the same prefix is still flagged | `python3 knowledge/_validate_evidence.py --selftest` -> rc=0 · `glob arm: reviews/REVIEW-204-*.html is a pattern, not a dead pointer` and `glob arm (other direction): a genuinely dead path is still flagged` | **PROVEN** | — |
| `W44-15` | the sampler had a hang hazard: an evidence command quoted without its file operand (`grep -c 'data-apollo-theme'`) read stdin until the timeout and was then misreported as an environment gap. stdin is now closed and a non-terminating command is its own named refusal class | knowledge/_validate_evidence.py sample() passes `stdin=subprocess.DEVNULL` and raises DOES-NOT-TERMINATE; the same row now reports `RAN C-7 -> rc=1` in under a second | **PROVEN** | a wrong refusal reason is the measuring-tool-must-not-guess class inside the measuring tool |
| `W44-24` | the linter run on THIS table caught two dead pointers in my own row W44-22: I wrote `knowledge/_CHAIN.md` and `knowledge/_LIVE-STATE.md`, and both live at the repo root | `git ls-files \| grep -E '(^\|/)(_CHAIN\|_LIVE-STATE)\.md$'` -> `_CHAIN.md` and `_LIVE-STATE.md`, no knowledge/ prefix; row W44-22 repaired and `python3 knowledge/_validate_evidence.py notes/_claims/205-w44-claims.jsonl --no-sample` -> rc=0 | **PROVEN** | the dead-pointer check earned its keep on its author's own table within one run |

## D · PROPOSED schema choices — CONDUCTOR TO PUT TO DAVE, nothing here is ruled

| id | claim | evidence pointer | tag | note |
|---|---|---|---|---|
| `W44-16` | PROPOSED: NEW is a fourth VERDICT value, not a derived state. Rejected alternative: derive NEW from an unmatched id only. Kept BOTH — the join cross-checks them against each other and a mismatch is a LOUD inconsistency, so a mislabelled finding cannot pass quietly | knowledge/_claimtable.py VERDICTS = (CONFIRMED, CONTRADICTED, UNTESTED, NEW); knowledge/_join_claim_tables.py raises on `verdict=NEW but a claim row with that id EXISTS` and on `verdict!=NEW but NO claim row has that id` | **UNPROVEN** | PROPOSED — Dave's or the conductor's. The spec's own ground truth counts NEW as a fourth bucket, which is the argument for the enum value. |
| `W44-17` | PROPOSED: id uniqueness is KIND-AWARE — claim ids must be unique, challenge ids may repeat. Rejected alternative: force 1:1 and mint a synthetic id for the second verdict | #204's D-7 really did receive two verdicts (CONTRADICTED on its cited 16-hit figure, CONFIRMED on its substance); a synthetic id would join to nothing and would report D-7 as UNCHALLENGED | **UNPROVEN** | PROPOSED — forced by real data, not by taste. The selftest carries a regression arm for it. |
| `W44-18` | PROPOSED: a `fence` string field, and ANY row carrying it surfaces in the join even when CONFIRMED. Rejected alternative: no fence field, and let fence touches ride in free-text `note` | on the real data this surfaced D-7 and F-11 as FENCE rows that the CONFIRMED collapse would otherwise have swallowed — see notes/_claims/204-JOIN.md section `## FENCE — 2` | **UNPROVEN** | PROPOSED — a fence touch collapsed into a count is exactly `a row it hides is a decision nobody made` |
| `W44-19` | PROPOSED: an UNCHALLENGED class — a claim with no challenge row surfaces. Rejected alternative: an inner join, which is what two prose documents effectively are | it found C-5, F-7 and F-8 on the real #204 data, none of which the verifier's own OMISSIONS section names | **UNPROVEN** | PROPOSED — the strongest single argument in this lane for the mechanisation, since it found something a careful adversarial verifier missed |
| `W44-20` | PROPOSED: UNKNOWN FIELD NAMES are a parse FAILURE, not a silent drop; and CLAIMED/UNPROVEN/UNTESTED rows are EXEMPT from the s182-D1 probeable-token requirement. Rejected alternatives: tolerate unknown keys (a typo'd field is a lost row) and require a token on every row (which manufactures false provenance on rows whose whole point is that nothing was established) | knowledge/_claimtable.py validate_row() rejects unknown fields; knowledge/_validate_evidence.py is_mechanical() gates the token requirement — proven by the selftest arm `exempt arm: an UNPROVEN row is not forced to carry a probeable token` | **UNPROVEN** | PROPOSED — the exemption boundary is the one most likely to be wrong in practice |

## E · Fences honoured

| id | claim | evidence pointer | tag | note |
|---|---|---|---|---|
| `W44-21` | nothing was wired into _build_all.py or CI, and _build_all.py was never run | `git status --porcelain knowledge/_build_all.py .github` -> empty · `grep -c 'claim_table\\|_join_claim\\|_validate_evidence' knowledge/_build_all.py` -> 0 | **PROVEN** | — |
| `W44-22` | the #204 receipt originals are byte-unchanged; the DO-NOT-RULE files are untouched | `git status --porcelain notes/_receipts/ knowledge/_rulings.json GOOD-MORNING.md _CHAIN.md _LIVE-STATE.md notes/_MEMENTO-DECISIONS.md knowledge/_DS-IMPROVEMENTS.md` -> only the new 205 receipt appears | **PROVEN** | — |
| `W44-23` | the doc-row gate was run and reacted to rather than assumed | `python3 knowledge/_gate_doc_rows.py` -> see W-44's own store row; the new files are knowledge/*.py and notes/_claims/* , outside PATTERNS = ['notes/_briefs', '_BRIEF-'] by construction (gate-glob-scope-rule) | **PROVEN** | — |
