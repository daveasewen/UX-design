# Lane N (Sonnet) — #213 small-chores receipt

> ⛔ DATED PERIOD RECORD, NOT A LIVE HOME. Written 2026-08-21 by the #213 Lane N sub (Sonnet).
> Nothing here is a ruling. No git command was run (fence 1). No shared-state file was edited
> (fence 2) — GOOD-MORNING.md corrections are handed below for the CONDUCTOR to apply.

| governance | value |
|---|---|
| serves | `notes/_briefs/2026-08-21-213-mine-burn-fanout-brief-v1.md` § LANE N (proposed divvy, `2026-08-21-213-c1-residual-survey-v1.md` §6) |
| files touched | `.github/workflows/gates.yml` (checkout/setup-python pins only) |
| files NOT touched | `GOOD-MORNING.md` (corrections handed below, per fence) |

---

## JOB 1 — GitHub Actions version bump

**Probe:** `grep -n "checkout@v\|setup-python@v" .github/workflows/*.yml` → `actions/checkout@v4` ×2 (lines 163, 217), `actions/setup-python@v5` ×2 (lines 169, 223), both in `.github/workflows/gates.yml`.

**Version check (network available):**
- `GET https://api.github.com/repos/actions/checkout/tags` → newest tag `v6` (`v6.0.3`, commit `df4cb1c`). ⚠ A separate call to `/repos/actions/checkout/releases/latest` returned `v7.0.1` (published 2026-07-20) — this **conflicts** with both the tags endpoint and the rendered `github.com/actions/checkout/releases` page, which independently shows **`v6.0.3` tagged "Latest"** with no `v7` entry anywhere in the list. Two independent sources (tags API + releases HTML) agree on v6; the one `/releases/latest` call disagreeing is treated as unreliable and NOT acted on. Declaring this rather than silently picking one.
- `GET https://api.github.com/repos/actions/setup-python/tags` → newest tag `v6` (`v6.2.0`).

**Action taken:** bumped both action pins one major each, in `.github/workflows/gates.yml`, at all 4 call sites (both the `gates` job and the `render` job each have one `checkout` + one `setup-python` step):
- `actions/checkout@v4` → `actions/checkout@v6`
- `actions/setup-python@v5` → `actions/setup-python@v6`

Nothing else in the workflow was restructured (fence: "do not otherwise restructure").

**Breaking-change caveats from the changelogs (quoted/paraphrased):**
- **checkout v5→v6**: Node 24 runtime (requires Actions runner ≥ v2.327.1); v6 additionally moved `persist-credentials` to store creds under `$RUNNER_TEMP` instead of the local git config (v6-beta note: requires runner ≥ v2.329.0 for Docker-container-action scenarios reading persisted creds). GitHub-hosted `ubuntu-latest` runners are already current, so this should be a no-op here, but it is the actual behavioural change between v4 and v6.
- **setup-python v5→v6**: also a Node 24 upgrade (v6.0.0, requires runner ≥ v2.327.1); v6.0.0's dependency bump also moved setup-python's OWN internal use of `actions/checkout` from 4→5 (irrelevant to this repo, noted for completeness). No documented breaking change to the action's own input/output contract between v5 and v6.

**Residual:** I did not verify GitHub-hosted `ubuntu-latest` runner version against the v2.327.1/v2.329.0 floors — normally not user-controllable and current runners exceed both by a wide margin, so treated as low-risk and not blocking.

---

## JOB 2 — re-probe the render/pixel gates locally (browserless-red claim)

**Probe (named in the render job of `.github/workflows/gates.yml`), each run once, this box has no `playwright` installed** (`pip show playwright` → not found; `pillow` 12.3.0 and `jsonschema` 3.2.0 ARE present):

| command | rc | reading |
|---|---|---|
| `python3 knowledge/_validate_state_contrast.py --selftest` | **77** | `COULD-NOT-ASK` — declared refusal, not a fail. Quote: *"the 'playwright' module is not installed … THIS IS NOT A SKIP: these arms … run BLOCKING in the `render` job … which installs chromium — that job is where this gate's proof of record lives."* |
| `python3 knowledge/_validate_state_contrast.py` (full sweep, no `--selftest`) | **77** | same COULD-NOT-ASK class, same message. |
| `python3 knowledge/_probe_registry/_registry.py --run` | **1** | registry-level rc=1 comes from **P-4** (premise-vs-store diff, 49 real findings — an unrelated staleness class, not browserless). Inside the same run, **P-3** (dangling-var PIXEL, sandbox-render) and **P-6** (text-box-trim canary, sandbox-render) each individually reported **rc=77 COULD-NOT-ASK**, both for the identical `playwright is not importable` reason, and the registry's own summary lists them separately under "⛔ COULD-NOT-ASK (2) … They do not fail this run; they also do not pass." |

**Which exit codes CI reads as failure vs declared refusal, read from `.github/workflows/gates.yml` (as bumped, see Job 1):**
- `render` job step **"State-contrast selftest — every arm … (BLOCKING)"** runs `_validate_state_contrast.py --selftest` **without** `continue-on-error` → any non-zero rc (77 included) FAILS the job. In CI this step runs AFTER a step that installs `playwright` + `playwright install --with-deps chromium`, so in a correctly-provisioned CI run this rc should be 0, not 77 — the 77 I measured here is a browserless-environment artefact of THIS sandbox, not a CI finding.
- `render` job step **"Full state-contrast sweep — ADVISORY …"** and **"Probe registry … (W-45, advisory)"** both carry `continue-on-error: true` → their rc (77 or 1) **never fails the job**, by design. This is the CI-side confirmation of the survey's "P-3/render job COULD-NOT-ASK class" claim: the ONE place P-3 is driven for real is this job's chromium-installing step, and even there its outcome is advisory-only, never blocking.
- Net: **CI treats a genuine browserless refusal (rc=77) identically to a real defect (any non-zero rc) only on the BLOCKING selftest step; everywhere else (the sweep, the registry) rc is captured but cannot redden the job.** This box, lacking playwright, is exactly the "browserless" environment the survey named — the COULD-NOT-ASK class re-probed here true to its own declared words, not simulated.

**Residual:** did not install playwright/chromium here (out of scope for a chores lane; would need network + ~10min per the workflow's own timeout comment) — so I cannot report what rc these steps return WITH a browser present. That is exactly what the CI `render` job already measures per its own inline comments (#209 premise-correction note in the workflow: job conclusion=success on recent runs with this step's exit masked by `continue-on-error`).

---

## JOB 3 — Stat-card spark slot: SCOPE ONLY (nothing built)

**Probe:**
```
grep -ic spark knowledge/snippets/Stat-card.reference.html   → 0
grep -in spark knowledge/components/stat-card.meta.json      → no match (rc=1)
```
Confirmed absent, exactly as the survey (`2026-08-21-213-c1-residual-survey-v1.md` §4) states. `Chart-sparkline` DOES exist: `knowledge/snippets/Chart-sparkline.reference.html` + `knowledge/components/chart-sparkline.meta.json`.

**Scoped proposal (PROPOSED, not ruled — Dave's per `s182-D2`, which the survey already flags: *"ruled the sparkline 'an atom alone' and moved the table CTA to a future trend-card — so the slot's shape is arguably Dave's, not enactment"*):**

- **What already anticipates this seam:** `chart-sparkline.meta.json`'s `scale` prop already has an `inline` variant (`200×48 at 44px, stroke 2, dot r3, aria-hidden — decorative — the composed value carries meaning`) whose own `$note` names *"the Stat-card seam"* by name, and its `relationships.livesInside` names *"Stat-card / KPI tiles (the receipted seam)"* directly. **The composing component already expects this**; the gap is one-sided — Stat-card's own `props`/`edges` have no matching slot or `renders`/`composes` edge back.
- **What a spark slot would consume:** a new optional prop on `stat-card.meta.json` (e.g. `trend`, type `component-ref`, composing `Chart-sparkline` at `scale: "inline"`), plus a markup slot in `Stat-card.reference.html` between `value` and `delta`/`period`. It would need: (a) a `mustNotNeighbour` cross-check against Stat-card's EXISTING rule *"A chart making the identical claim in the same view (pick one headline form)"* — an inline composed sparkline is arguably the SAME claim, not a neighbour, so that rule may need a clarifying note rather than a block; (b) a decision on whether the slot is decorative-only (matching `inline`'s `aria-hidden`, consistent with Stat-card's own accessibility doctrine that "the composed value carries meaning") or gets its own `role="img"` (breaking that doctrine); (c) a `binds`/token entry (the sparkline's `data/series/1` bind already exists, so likely a straight compose, not a new token).
- **S/M/L job-window price:** **S** (≲40K). It is one meta-prop addition + one markup slot insertion + a driven four-theme render proof (mono/legacy/console/supercharge, light+dark) showing the composed sparkline sits inside the existing Cards recipe without breaking the `mustNotNeighbour` rule's intent. No new token minting expected (the inline scale and its bind already exist). Sizing assumption: Dave has already ruled the sparkline's own shape (`s182-D2`) — this job is wiring the KNOWN atom into the KNOWN slot, not designing either.
- **Open question for Dave, stated in his terms:** *"Chart-sparkline's own metadata already names Stat-card as its inline home, but Stat-card doesn't compose it yet — do you want a `trend` slot added to Stat-card, or does `s182-D2`'s 'atom alone' ruling mean this stays a future trend-card's job instead?"*

Nothing was built. `Stat-card.reference.html` and `stat-card.meta.json` are unedited.

---

## JOB 4 — verify the two named §C·4 stale-green claims

### Claim A — `s155-D1` mono green fork, `GOOD-MORNING.md:418`

Current text (quoted, unedited by me): *"Ruled, NOT enacted — canon/tokens owed, per-theme tested."*

**Probe (independent of the survey's own grep, re-run fresh):**
```
grep -rn 137F3C knowledge/canon/canon.css knowledge/tokens/semantic-colour.json knowledge/tokens/palettes/rag/mono.json
grep -rn 66CC8D  knowledge/canon/canon.css knowledge/tokens/semantic-colour.json knowledge/canon/gen_theme_cascade.py knowledge/tokens/themes/_themes.json
```
Both values are live and multiply-cited:
- `#137F3C` at `canon.css:318` (`--rag-success-ink: #137F3C;`) and `canon.css:3890` (finding note naming `sign=positive ADDED — the one ruled green-text seat (s155-D1). Ink = rag/success-ink, background-keyed #137F3C-on-white / #66CC8D-else`), plus `semantic-colour.json` (multiple `$value`/`$note` sites tracing `s155-D1`→`s144-D1` provenance) and `palettes/rag/mono.json:219` (`$value: #137F3C`).
- `#66CC8D` at `canon.css:315-316` (`--rag-success-background`/`-glyph`), `canon.css:689-692` (`--rag-success-ink: #66CC8D`, per-theme), `canon.css:9497/12714/15312/18368` (`--success: #66CC8D` — four theme blocks), `gen_theme_cascade.py:570-571` (an assertion literally checking `base_value("step/complete", mode) == "#66CC8D"`), and `_themes.json`.

**Verdict: line 418 is STALE-GREEN in the OPPOSITE direction from a normal false-positive** — it under-reports enactment. The line reads "NOT enacted," but the fork is live in the generator (`gen_theme_cascade.py` has a self-check asserting the exact value), in `canon.css` across all four theme blocks, and in the token store with explicit `s155-D1` provenance in the `$note` chain. This is the survey's own §C·4 finding, independently re-confirmed here, not merely re-quoted.

**Correction line handed to the conductor** (GOOD-MORNING.md:418, exact replacement text, conductor applies):
> `> **① `s155-D1` ENACTMENT — the mono green fork.** Values RESOLVED #155-A via `s144-D1`: **`#137F3C`-on-white / `#66CC8D`-else, MONO ONLY** (legacy/console/supercharge take no fork). ✅ ENACTED — `#137F3C`/`#66CC8D` are live in `knowledge/canon/canon.css` (`--rag-success-ink`/`-background`/`-glyph`, four theme blocks), `knowledge/tokens/semantic-colour.json`, `knowledge/tokens/palettes/rag/mono.json`, and asserted by `knowledge/canon/gen_theme_cascade.py`. Queue line was stale (assertion-propagation class, 4th sighting per the #213 survey). Per-theme RENDER PROOF still owed if not already shown — that is the only real residual. [born #155 · guards: this line + `s155-D1` · until: a render proof is shown, or Dave rules the code evidence sufficient]`

### Claim B — test_gates.py "known gap" prose, `GOOD-MORNING.md:419` (second half)

Current text (quoted, unedited): *"Same pass: `knowledge/_tests/test_gates.py:23–25` still calls the CI render gap 'a known gap' — FALSE since the render job landed #155 (assertion-propagation class)."*

**Probe:** `Read knowledge/_tests/test_gates.py` lines 1–28. Verbatim, lines 22–27:
> ```
> NOT covered here (documented, deliberate):
>   * _validate_state_contrast.py / _validate_screen.py --render — require a
>     browser engine; run them via the render path where chromium is available.
>     Since #155 the CI `render` job (.github/workflows/gates.yml) runs
>     `_validate_state_contrast.py --selftest` BLOCKING and the full sweep
>     ADVISORY — they are no longer untested in CI.
> ```

**Verdict: CONFIRMED corrected.** The file no longer says "a known gap" anywhere in this block; it explicitly names the #155 render job's BLOCKING selftest + ADVISORY sweep as closing that gap. `GOOD-MORNING.md:419`'s claim that the prose IS corrected (i.e. the queue line's own "FALSE since #155" assertion) is itself accurate — this is the one part of the queue-line pair that does NOT need correcting.

**⚠ Bonus finding, beyond what the survey named (declared, not smoothed) — the FIRST half of line 419 is now ALSO stale, independently discovered here:** line 419 opens *"The gates job's survey runs the state-contrast selftest browserless ⇒ guaranteed red for a reason the `render` job already covers."* I ran `python3 knowledge/_build_survey.py --timeout 60` (the exact command the `gates` job's "Survey the COMMITTED tree" step runs) locally, browserless: rc=1, but the **1 FAIL is `[117] memento schematic determinism check` (unrelated — a stale diagram, #127 class), NOT state-contrast.** `_validate_state_contrast.py --selftest`'s browserless rc=77 shows up in the survey's own **`COULD-NOT-ASK (2)`** bucket, which the survey's own printed banner states explicitly: *"These do NOT fail the survey; they also do not pass."* So the `gates` job's survey step does **not** go red because of the state-contrast selftest — it treats the browserless refusal as a declared COULD-NOT-ASK, not a FAIL, since the #193 could-not-ask convention (survey step `[125] could-not-ask convention selftest (#193)` passed ✅ in the same run). Line 419's premise (browserless ⇒ "guaranteed red") predates that convention and is now false on its own terms, not just the test_gates.py half it already flags.

**Correction line handed to the conductor** (GOOD-MORNING.md:419, exact replacement text, conductor applies):
> `> **② CI survey/browser tidy-up — ✅ BOTH HALVES NOW STALE, RE-PROBED #213.** Originally: "the gates job's survey runs the state-contrast selftest browserless ⇒ guaranteed red." Re-run today: `python3 knowledge/_build_survey.py --timeout 60` rc=1, but the ONE FAIL is `[117]` (memento schematic staleness, #127 class, unrelated); `_validate_state_contrast.py --selftest`'s browserless rc=77 lands in the survey's own `COULD-NOT-ASK (2)` bucket, which its banner states explicitly does NOT fail the survey (the #193 could-not-ask convention, selftest `[125]` ✅ in the same run). `knowledge/_tests/test_gates.py:23–27` already reads the corrected prose (verified verbatim). Nothing left to fix here — CLOSE the line. [born #155 · guards: this line · closed #213, Lane N]`

---

## RESIDUALS (declared, not smoothed)

1. Job 1: did not verify hosted-runner minimum version against the v5/v6 floors named in the changelogs (low risk, not user-controlled on GitHub-hosted runners).
2. Job 2: no playwright/chromium installed here — the "healthy" rc for the two BLOCKING/advisory browser steps was not measured on this box; only the browserless refusal path was driven, which is exactly the declared-refusal class the brief asked me to re-probe.
3. Job 3: nothing built, per fence. The `mustNotNeighbour` tension named above (identical-claim vs neighbouring-claim) is flagged, not resolved — Dave's.
4. Job 4: the two corrections above are proposed exact-text replacements for the conductor to apply to `GOOD-MORNING.md:418–419` in the serial set; I did not touch that file (fence 2).
5. No git command was run (fence 1). `.github/workflows/gates.yml` is left UNCOMMITTED, version-don't-overwrite not needed (single in-place edit, no `-vN` file created).
