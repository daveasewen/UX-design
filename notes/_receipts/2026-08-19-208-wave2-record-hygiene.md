# #208 WAVE 2 — RECORD HYGIENE (Opus work sub)

**Lane:** six small mechanical repairs, ruled by Dave (*"fix all mechanical loose ends"*).
**Seat:** Opus work sub, Fable conducting. **Committed nothing, pushed nothing, checked out nothing, ran no build.**
**Claim table:** `notes/_claims/208-wave2-claims.jsonl` (21 rows) — `python3 knowledge/_validate_evidence.py notes/_claims/208-wave2-claims.jsonl` → **rc=0**, 0 lint failures, 0 rc mismatches, 1 declared SIDE-EFFECTS refusal.
**Mid-lane check-in:** `python3 knowledge/_checkin.py` ran at the item-4/5 seam — FILL **103,361 real**, boot 57,050, room to the ADVISORY stop line **150,929** = 47,568. SEAM CLEAR.

---

## MODIFIED PATHS — the complete list

| Path | What changed | Item |
|---|---|---|
| `knowledge/_state.json` | +13 lines: row **W-55** appended via `_state.add()` | 1 |
| `knowledge/_assertions.json` | ASSERT-009 `asserted_in` −1 entry + provenance appended; ASSERT-001 `last_verified` re-based + provenance appended | 2, 3 |
| `knowledge/_ASSERTIONS.md` | GENERATED — rewritten by running `_validate_assertions.py` (that gate always writes its report) | 3 |
| `_LIVE-STATE.md` | **+1 line only** (one new bullet after line 91) | 4 |
| `knowledge/_build_all.py` | docstring ARGV CONTRACT block; `ARGV_FLAGS` / `ARGV_REMEDY` / `check_argv()` / `_refuse_argv()`; guard as the first statement of `main()`; selftest arm (f) | 5 |
| `knowledge/_gate_doc_rows.py` | `staged_adds()` added; `population(staged=None)` widened; `check()` reports the staged count; selftest gains a staged-arm and a de-vacuumed mutation-arm | 6 |
| `notes/_claims/208-wave2-claims.jsonl` | NEW — this lane's claim table | all |
| `notes/_receipts/2026-08-19-208-wave2-record-hygiene.md` | NEW — this receipt | all |

**NOT MINE, present in the tree, left alone (the parallel sub's lane):** `.github/workflows/gates.yml` · `knowledge/_git_commit.sh` · `knowledge/_governs.py` · `knowledge/_CAPTURE-GATE.md` (regenerated by `_capture_gate.py`, dated 2026-08-19, scope 151→161 files) · `notes/_REHEARSAL-LOG.jsonl` · `notes/_dream/_GRADE-DECISIONS.jsonl` (both already dirty at lane open).

---

## ITEM 1 — P-4: the unrowed brief

**Premise verified — HALF TRUE, and the correction matters.** The briefing said the plan has *"no store row"*. Two instruments disagreed in the same minute:

- `python3 knowledge/_probe_registry/probe_premise_store.py` → **rc=1**, `⛔ UNROWED-DOC notes/_briefs/2026-08-16-memento-closeout-plan.md is named by no store row's `home``
- `python3 knowledge/_gate_doc_rows.py --check` → **rc=0**, `population 21 … unrowed 0`

Why: the doc WAS reachable, but only through **section** homes — `W-17.home = "notes/_briefs/2026-08-16-memento-closeout-plan.md### Phase 3 — rebuild parts 4–5"` and `W-18.home = "…#**Compaction brief questions**"`. `probe_premise_store.py:94` matches **EXACTLY** (`rel not in homes and os.path.basename(rel) not in homes`); `_gate_doc_rows.py:118` matches by **SUBSTRING** (`p in h or base in h`). So the probe was right that the DOCUMENT has no row of its own, and the gate was right that the path is mentioned. The repair is the row either way.

**Change.** `knowledge/_state.json` — row **W-55** (next free; max was W-54, no gaps), added through the store's own writer, W-50 shape copied from commit `d07e85c`:

```
id W-55 · project memento · state open · owner claude · opened 208
home  notes/_briefs/2026-08-16-memento-closeout-plan.md
title #185 Memento close-out plan (the PLAN DOCUMENT itself, conductor-minted row)
closes_when W-17 closes (the last still-open lane homed inside this plan); the plan document
            then becomes historical record
```

**Drive, both directions.** Before: P-4 `rc=1`, 1 finding. After: `python3 knowledge/_probe_registry/probe_premise_store.py --check` → **rc=0**, `P-4 premise-vs-store diff: 67 doc(s) … 0 finding(s)`. Store still valid: `python3 knowledge/_state.py` → rc=0, `project split: apollo 39 · memento 37 (of 76 items)`. `_state.save()` is order-stable — verified before writing (sorting the loaded doc is a byte no-op), so the diff is **+13 / −0**.

**Residual (priced, NOT ruled here).** The two matchers disagree **by construction** and will disagree again on the next section-homed doc. Harmonising them is one small change (give `_gate_doc_rows.py` the probe's exact-match semantics, or give the probe the gate's substring semantics) but it moves a gate's meaning, so it is named, not taken: **~15 min, one file, plus a re-drive of both selftests.** The W-17/W-18 section homes were left exactly as they are.

## ITEM 2 — ASSERT-009 home rot

**Premise verified first-hand.** `grep -n "92" GOOD-MORNING.md` returns the meta-count at **one** line only — `GOOD-MORNING.md:27`, and that line is the **#207 session-report entry about the README repair** (`knowledge/README.md:13` **38 → 92** against ASSERT-009), not a live statement of the corpus size. GM's own banner does not carry the count. The figure now lives at `knowledge/README.md:13` (`components/ (92 metas — count registered as ASSERT-009, re-tested not repeated)`), repaired at commit `2103316` as briefed.

Each remaining home was grepped before the pointer was cut: `_LIVE-STATE.md:85 / :597 / :598` · `notes/_MEMENTO-DECISIONS.md:5620 / :5628` · `_DECISION-HISTORY/2026-08-08-131-the-legacy-rag-fills-and-the-design-kg-nothing-checks.md:92 / :109` · `knowledge/README.md:13`. All four really carry it.

**Change.** `knowledge/_assertions.json` — `GOOD-MORNING.md` removed from ASSERT-009's `asserted_in` (four homes remain), and the **reason appended to `provenance`**, which is where the history lives. Nothing was deleted: the `consequence` field still names *"GOOD-MORNING.md's banner"* as a #131-era fan-out and is deliberately left standing as the record of where the figure used to live.

**Why removal is the correct repair, not a re-point.** GM is a **rolling** file: a pointer into it ages out by construction (`s177-D1` — no evidence pointers into rolling files), so at the next count flip the gate would have named an innocent document and left the real one unnamed. `asserted_in` is the fan-out list; a wrong entry is worse than a short one.

**Drive.** `python3 knowledge/_validate_assertions.py` → **rc=0**, `[✓] ASSERT-009 environment count=92 (want eq 92) in root=repo`, `✅ assertion gate passed — 8 claim(s) still true.` Registry well-formedness (`asserted_in` non-empty) holds — the selftest asserts it and passed.

## ITEM 3 — ASSERT-001 overdue recheck (webfonts)

**Premise verified.** Before the repair the gate itself said so: `⚠ ASSERT-001: last verified 32d ago, window is 30d` (record: `last_verified 2026-07-18`, `recheck_days 30`).

**The recheck RAN in this sandbox — no refusal was needed.** Method = the registered predicate plus the same first-hand enumeration the 2026-07-18 provenance describes:

- `python3 knowledge/_validate_assertions.py` → `[✓] ASSERT-001 blocker 0 matches in root=repo`
- `ls knowledge/assets/fonts/` → the same five script packs (Helvetica Armenian, M Ying Hei SC, M Ying Hei TC, Tazugane Japanese, Univers Arabic) + `_desktop`
- `find knowledge/assets/fonts -iname "*.woff*" | wc -l` → **50** (five packs × ten files, unchanged)
- `find knowledge/assets/fonts -iname "*latin*"` → Latin faces exist **only** under `_desktop/TTF/` (`HSBC_MtUnivers_Latin-*.ttf`) — the DESKTOP licence class the original note already warns must not be mistaken for the pack

**Change.** `knowledge/_assertions.json` — `last_verified` **2026-07-18 → 2026-08-19**, with the full method + figures appended to `provenance`. The claim is re-based in **DATE only**, not in substance.

**NOT RULED HERE.** `clears_when` watches for an Ultralight weight **in the WEBFONT pack**; `ULt`/`ULtIt` are present in the DESKTOP TTFs and always were. That is not the trigger and was not treated as one — the "sixth weight = a TYPE RULING" call stays Dave's if a webfont pack ever lands.

**Drive.** `python3 knowledge/_validate_assertions.py` → **rc=0**, `✅ assertion gate passed — 8 claim(s) still true.` — the stale-warning tail is **gone** (it printed `⚠ 1 blocker(s) past their recheck window` before).

## ITEM 4 — the missing #204 dossier back-link

**Premise verified.** `grep -c "2026-08-19-204-" _LIVE-STATE.md` → **0** before the edit. The #205 entry at `_LIVE-STATE.md:91` had already declared it: *"(b) its claimed `_LIVE-STATE.md` back-link does not exist (grep → 0)"*. Target exists: `ls _DECISION-HISTORY/2026-08-19-204-the-topology-that-earned-its-amendments.md` → rc=0.

**Change — BY ADDITION, one line.** `_LIVE-STATE.md:92`, a new bullet immediately after the #205 entry that declared the gap, in the neighbouring `History: …` / `Arc: …` idiom, closing (b) **without editing (b)**. `git diff --stat _LIVE-STATE.md` → `1 file changed, 1 insertion(+)`. Nothing else in the spine file was touched.

**Drive.** `grep -c "2026-08-19-204-" _LIVE-STATE.md` → **1**.

## ITEM 5 — `_build_all.py` argv guard

**Premise verified in the source, not from the banner.** `git show HEAD:knowledge/_build_all.py` — `main()` read argv by **membership test only** (`"--selftest" in argv`, `"--range" in argv`, `"--resume" in argv`) with no else-branch refusal. An unrecognised token was therefore not rejected; it was **not looked at**, and "no recognised flag" is spelled *build everything*. That is exactly the #204 accident.

**Change.** `knowledge/_build_all.py`:

- module docstring — a `Run:` block naming all four legal forms + an **ARGV IS A CONTRACT (#208)** paragraph (`-h`/`--help` are answered earlier by `_helpgate.help_gate` and never reach the contract)
- `ARGV_FLAGS = {"--selftest": 0, "--range": 1, "--resume": "?"}` + `ARGV_REMEDY`
- `check_argv(argv)` — **pure**: never builds, never writes, never exits; returns the NAMED reason or `None`. Catches unknown tokens (with a near-miss hint), a value-taking flag with no value, and `--range` + `--resume` together
- `_refuse_argv(reason, argv)` — prints argv seen, the reason, `⛔ NOTHING WAS BUILT — … do not read it as green and do not read it as a gate failure`, and the four-form remedy; returns **2**
- `main()` — the guard is the **first statement**, above `check_routes()` and far above the first `subprocess.run`

**Drive — both directions, and NEVER a real build.** Every path taken returns before step 1:

- `python3 knowledge/_build_all.py notes/_briefs/whatever.md` → **rc=2**, `❌ _build_all.py REFUSED — ARGV CONTRACT (#208 guard; the #204 accidental build)` / `reason: UNKNOWN ARGUMENT 'notes/_briefs/whatever.md' — known flags are --range, --resume, --selftest`
- `python3 knowledge/_build_all.py --wrap` → **rc=2** (a flag borrowed from another script — the other half of the #204 shape)
- `python3 knowledge/_build_all.py --selftest` → **rc=0**, `selftest (f): #208 argv contract — 5 illegal forms refused NAMED, 6 legal forms accepted, mutation control bites, main() returns rc=2 before step 1 ✓`, then `selftest PASS — exact-ID failure routing over 124 steps`
- `python3 knowledge/_build_all.py --help` → **rc=0** (help gate unaffected)

Selftest arm (f) carries a **mutation control**: it makes the junk token legal, proves the guard then accepts it, restores `ARGV_FLAGS`, and asserts the guard refuses again — a guard permanently blinded by a bad restore cannot pass.

**⛔ NO BUILD WAS RUN AT ANY POINT.** The refusal path returns above `check_routes()`; `--selftest` short-circuits before the step loop (its only writes are into a `tempfile.mkdtemp()`).

**Residual.** No caller passes arguments to `_build_all.py` today (`grep -rn "_build_all.py"` over `*.py`/`*.sh`/`*.yml` finds only prose references, `_validate_wiring.py`'s STEPS parse, and `_retired`/`_to_delete` copies), so the guard changes no existing invocation. It is a **new gate with a real consumer** (every human/agent CLI call), not an instrument without one.

## ITEM 6 — the doc-row gate's single-commit blindspot

**Premise verified.** `population()` derived its whole world from `git log --diff-filter=A`, which by construction knows only about **already-committed** files. A session that adds a brief and commits **once** — the ordinary wrap shape — ships it unrowed while the gate prints PASS.

**Proven on a REAL git index, without touching this repo's index.** A scratch repo at `/tmp/drt` with one brief `git add`-ed and never committed, both gate versions pointed at it by patching `ROOT`/`STORE`:

- HEAD (pre-fix): `population(): []` → `doc-row gate: population 0 … unrowed 0` / `✅ PASS` — **green while shipping an unrowed doc**
- fixed: `staged_adds(): [('2026-08-19', 'notes/_briefs/2026-08-19-208-scratch-brief.md')]` → `population 1 · of which staged-in-THIS-commit 1 · unrowed 1` / `⛔ FAIL`, **rc=1**
- fixed, after a row naming the doc is added to the scratch store: `unrowed 0` / `✅ PASS`, **rc=0**

**Change.** `knowledge/_gate_doc_rows.py`:

- `staged_adds()` — `git diff --cached --diff-filter=A --name-only -- notes/_briefs '_BRIEF-*'`, stamped with today's date (the date the commit will bear). A failure to diff (no HEAD) prints a **named refusal** and returns `[]` rather than silently restoring the blindspot
- `population(staged=None)` — merges the staged adds; a committed first-add date **wins** over today's stamp; `staged` is injectable so the selftest can plant one without going near the real index
- `check()` — prints the staged count separately: `… · of which staged-in-THIS-commit N (#207 postscript: the single-commit blindspot) · …`
- selftest **staged-arm** — a ghost doc must enter the population and be flagged unrowed, both asserted; it also reports how many real staged adds the tree has
- selftest **mutation-arm de-vacuumed** — it picked `pop[-1]` blindly, which after this change can be an **unrowed** staged doc; then the "deleted reference" deletes nothing and the arm passes without biting. It now picks a currently-**ROWED** victim and refuses loud if none exists

**Drive on the live tree.** `python3 knowledge/_gate_doc_rows.py --check` → **rc=0**, `population 21 … of which staged-in-THIS-commit 0 … unrowed 0`. `python3 knowledge/_gate_doc_rows.py --selftest` → **rc=0**, all five arms green (fail-arm 20 unrowed against `6b98be3`; pass-arm clean; staged-arm flags the index-only doc; mutation-arm bites).

### ⛔ PUNTED TO THE CONDUCTOR — the code fix alone does NOT close the class

`knowledge/_git_commit.sh` is the other sub's file. **The seam is ordered wrong for this fix:** the doc-row gate runs at **`_git_commit.sh:246`**, and EXPLICIT-PATH STAGING happens at **`:541`**. The index is therefore still empty of this commit's new docs when the gate asks. `staged_adds()` is correct and returns `[]` there — honestly, and uselessly.

**Exact patch, to be applied by whoever owns the file.** Insert immediately after `git diff --cached --quiet && fail "nothing staged — empty commit refused"` (`knowledge/_git_commit.sh:547`), i.e. in the same post-staging position and the same idiom as the mention-map second half already sitting there:

```sh
# ── DOC-ROW GATE, SECOND HALF: THE DOCS OF *THIS* COMMIT (#208) ─────────────────────────────
# The pre-staging run above can only see docs that are ALREADY COMMITTED (its population comes
# from `git log`), so a single-commit session that adds a brief ships it unrowed and the gate
# reports PASS while doing it (#207 postscript). `_gate_doc_rows.py` now ALSO reads
# `git diff --cached --diff-filter=A` — but that set is EMPTY until the staging loop above has
# run, so the answer only exists here. Same DOC_ROW_ACK hatch: declared passes, silent fails.
if [ -z "${DOC_ROW_ACK:-}" ]; then
  python3 knowledge/_gate_doc_rows.py ||
    fail "doc-row gate REFUSED (post-staging) — a document STAGED IN THIS COMMIT has no store row (list printed above). Add the row via _state.add(), or re-run with DOC_ROW_ACK=\"<real reason>\" to pass it DECLARED."
  echo "— doc rows present for this commit's staged adds (_gate_doc_rows.py, post-staging)"
fi
```

Alternative if two runs are unwanted noise: **move** the existing `:245–251` block to that position. Either is a one-block change; **neither was applied by this lane.** Until one of them lands, item 6 is **UNPROVEN AT THE SEAM** — the instrument can now see staged adds, but nothing asks it at a moment when they exist.

---

## CONSEQUENCES / PITFALLS (mandatory)

1. **`knowledge/_ASSERTIONS.md` is in the diff and nobody edited it.** `_validate_assertions.py` rewrites its report on **every** run, including bare verification runs. Reconcile it as GENERATED; do not read it as a hand edit, and do not drop it from the commit (a stale generated report is the `no-gate-parses-the-artefact` class).
2. **A green doc-row gate at this commit still proves less than it looks.** With the seam patch unapplied, `unrowed 0` at `_git_commit.sh:246` is a statement about the PREVIOUS commit's files. Do not quote this session's PASS as evidence that this session's new docs are rowed — they are rowed because item 1 added the row and because `notes/_receipts/*` and `notes/_claims/*` are **outside the gate's population** (`PATTERNS = ["notes/_briefs", "_BRIEF-"]`) entirely.
3. **The receipt and claim table are themselves unrowed docs — by design, not by omission.** Neither `notes/_receipts/` nor `notes/_claims/` is in scope for `_gate_doc_rows.py` or P-4. Whether the #185 class should extend to them is a **scope question for Dave**, not an agent's call; flagged, not taken.
4. **`check_argv` refuses `--range 1-3 --resume` as mutually exclusive.** `main()` never supported the combination (`--range` wins the `if`), so this names existing behaviour rather than changing it — but a caller who *thought* it composed will now get rc=2 instead of a silent `--range`-only run. That is the intended direction (loud over silent) and is called out here so nobody reads it as a regression.
5. **rc=2 is not a gate failure.** The argv refusal and the help-gate write refusal both use rc=2. Any wrapper that treats "non-zero from `_build_all.py`" as *the build gate failed* will now mis-report a typo as a red build. The refusal banner says so in its own text; a wrapper that parses only the exit code needs a look.
6. **The store's writer sorts and rewrites the whole file.** `_state.save()` is order-stable **today** (verified before writing: sorting the loaded doc is a byte no-op, +13/−0 in the diff). If a future row lands out of sort order, the next `add()` will produce a whole-file reflow — surprising in review, harmless in content. Worth knowing before blaming a sub for a 2,000-line diff.
7. **ASSERT-001's `asserted_in` still names `GOOD-MORNING.md`, and so do ASSERT-006 and ASSERT-008.** Same rolling-file class as item 2, **not checked this lane** — item 2's premise was ASSERT-009 only and I did not launder its finding into the others. Priced: one grep per assertion, ~10 min for all three, plus the same provenance-append discipline.
8. **The matcher divergence (item 1 residual) is live.** Until it is harmonised, `_gate_doc_rows.py --check` and P-4 can return opposite verdicts on the same doc. Whichever one a wrap quotes, quote **which** it was.
