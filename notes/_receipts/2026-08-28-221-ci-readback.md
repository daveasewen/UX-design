# #221 post-wrap CI READ-BACK — delivered in chat 2026-08-28, homed here (s203-D1)

Read live from GitHub Actions in Dave's signed-in Chrome (his sign-in restored this morning —
the block named in the #220 carries is CLEARED). Runs read: #427–#442; job pages opened for
#441 (`9dbf413`) and #442 (`4f83413`, HEAD of the #221 push).

## The diagnosis that discharges the owed-verdict queue
- **Run #442 (HEAD): overall FAILURE — but ONE job of three fails: `gates`.**
  **`render` SUCCEEDED 13m 4s** (blocking state-contrast selftest + full sweep GREEN, 0 text
  failures over 135 — Lane C's `except` widening held in blocking CI). **`release` SUCCEEDED
  21s** (its exit-1 annotation is the ADVISORY ship-list-drift step). Same job-level shape at
  #441 — **run-level red has been the `gates` job alone**, and a run-page "error" annotation is
  NOT a job verdict (three of #441's/#442's five annotations sit on advisory steps of GREEN jobs).
- **The `gates` job's survey names its three fails, and every one is a STANDING Dave-adjacent
  red that predates the #221 wave:** `[13]` pre-flight stamp on GOOD-MORNING (the red-13 streak)
  · `[18]` `_gm_usage` two-wraps section-usage grammar (W-179 Q2) · `[119]` chain-ratio, chain at
  50.5% of GM vs a <40% floor (W-167 Q1 — what should this gate assert). Each closes with his
  word, not a code fix.
- **What the #221 wave changed, visible in CI:** gates job 2m12 → 4m28 because the five
  `if: success()`-skipped steps now RUN (Lane A) — gate self-tests, advisory bite-tests and the
  commit-script selftest executed GREEN in CI for the first time in two sessions; both new
  advisory gates (`_gate_artefact_fresh`, `_gate_inline_style_parse`) ran clean. `gen_dashboard
  --check` now REFUSES honestly (exit 77, untracked provenance inputs) instead of blocking
  blind; 8 environment-bound checks report COULD-NOT-ASK. Survey: **53 pass · 3 FAIL · 8 CNA ·
  0 unaskable**.
- **The instant-death era (#427–#438, 3–9s runs) ended at `707e5aa` (#439)** — before the wave;
  the wave ended the *skipping*, not the instant death. L1's first-step-red claim was true of
  its era and is now fully mapped to the three standing causes above.

## Declared, not smoothed
- The Knowledge-build step's exact failing line (step 6 : 270) was NOT extracted — the three
  standing reds sit in its blocking set, but a fourth cause there is UNPROVEN either way.
- The claim-table evidence linter (advisory, W-44) is red now that it RUNS — newly VISIBLE,
  not proven newly caused (it was skipped at #441, so no prior verdict exists to compare).
- ⛔ Conductor mis-claim, corrected in chat within minutes: "release flipped green" — FALSE,
  release was already green-with-annotation at #441. An annotation is not a verdict; the
  conductor read the run page's error list as job verdicts before opening the job pages.
- ⛔ Gauge: the conductor pre-declared the 190,000 and 200,000 crossings, then crossed the
  **256,000 wall UNDECLARED during this read-back** — measured 277,735 real when Dave asked.
  A pre-declaration of the lines you expect to cross does not license silence at the one you
  said you wouldn't reach. Same class as #220 pitfall (d), third instance, now Dave-prompted
  twice out of three.

## For the #222 opener (digest, post-wrap-corrections class)
1. Inscribe `94c5a18` into the COMMIT STATE #221 block (the wrap's declared self-hash gap).
2. This receipt is the CI state — do NOT re-derive from local proxies; runs #427–#442 are read.
3. Mono's gallery default: STILL OPEN, page `reviews/MONO-GALLERY-DEFAULT-2026-08-27-v1.html`
   presented, his conditional premise verified TRUE, his one word pending.
4. The three CI standing reds are HIS three decisions (pre-flight stamp policy · two-wraps
   grammar · chain-ratio assertion) — a sitting candidate, not a repair lane.
