# #218 wave-3 — LANE α (FORMS) receipt

**Seat:** Opus build sub under `notes/_briefs/2026-08-25-218-wave3-lanes-brief.md`, lane α.
**⛔ Nothing committed. No registry / MIGRATED_SNIPPETS / CATEGORIES / spine / `gen_showroom`
page-set / library index / `_build_all` route touched. No rulings, no token or canon edits, no
store / lane / GM / LS / memory writes.** One new file in the tree (the verify), plus this receipt.

## Exec summary — the lane built ZERO of its eight, on purpose

The brief's eight components **already exist, gated, in the store today.** All eight were probed
live on all five of the register's own signals — snippet · meta · showroom · MIGRATED_SNIPPETS ·
canon `.cn-` rules — and all five are green for all eight, measured 2026-08-25.

Building them would have produced **eight duplicates of gated canonical artefacts** and handed
them to the conductor's regen serial. So the lane stopped, built the instrument that proves the
premise is an artefact, drove it both ways, and returns the finding. (Divvy: *"a worker needing a
file outside its region STOPS and returns the finding instead of touching it."*)

**⚠ THIS IS NOT LOCAL TO LANE α — it hits lane β and lane γ identically. See §Cross-lane.**

## The artefact, named mechanically

The wave-3 draft (`notes/_receipts/2026-08-24-crank-charts.md` §Wave-3 lane-brief draft) opens
*"From the measured v3 (78 Gap)"*. That figure comes from the **wrong column** of
`reviews/ITINERARY-STATUS-2026-08-21-v3.json`:

| field | what it is | value over 124 rows |
|---|---|---|
| `itinerary_status` | the **2026-07-14 planning spreadsheet's frozen column** | `{Gap: 78, Gated: 39, Partial: 7}` ← the 78 |
| `derived` | what the generator **MEASURED against the working tree** | `{GATED: 121, ASSET-SYSTEM: 1, ASSET-ONLY: 1, GAP: 1}` |
| `$true_gaps` | — | `[86]` (row 86, "Brand mark / logo", derived ASSET-ONLY) |
| `$drift_counts` | — | `{AGREES: 40, "STALE — itinerary UNDERSTATES the store": 84}` |

**The register declares its own drift, in a field the draft did not read.** All eight lane-α rows
carry `derived: "GATED"` and `drift: "STALE — itinerary UNDERSTATES the store"`.

`$status` on the file also reads *"a MEASUREMENT of the store, not a ruling"* — it was never a
worklist. Retrieval's default hid the ruling: the row's own `notes` field ("Biggest single gap",
"Core banking input") is 2026-07-14 prose, and it reads exactly like a live queue.

### The eight, measured 2026-08-25

| row | itinerary row name | slug | snippet | js ln | meta | showroom | migrated | canon `.cn-` |
|---|---|---|---|---|---|---|---|---|
| 13 | Form layout + validation | `form-layout` | 33,105 B | 55 | ✓ | ✓ | ✓ | 129 |
| 14 | Date picker | `date-picker` | 34,669 B | 133 | ✓ | ✓ | ✓ | 229 |
| 15 | Date-range picker | `date-range-picker` | 35,655 B | 159 | ✓ | ✓ | ✓ | 229 |
| 16 | Time picker | `time-picker` | 23,401 B | 89 | ✓ | ✓ | ✓ | 91 |
| 17 | Number / currency (amount) input | `amount-input` | 16,932 B | 32 | ✓ | ✓ | ✓ | 125 |
| 18 | File upload / dropzone | `file-upload` | 27,760 B | 64 | ✓ | ✓ | ✓ | 77 |
| 19 | OTP / PIN / secure entry | `secure-entry` | 24,035 B | 48 | ✓ | ✓ | ✓ | 88 |
| 20 | Textarea | `textarea` | 13,802 B | 16 | ✓ | ✓ | ✓ | 83 |

Not empty shells: `js_lines` measured with `gen_library_214.py`'s **own** predicate (16–159 lines
of real behaviour each), and the a11y surface the brief called for is already authored —
`aria-live` 1–5 per file, `aria-invalid` 2–7 (File-upload 0, see residual R2),
`aria-describedby` 3–8, Form-layout carrying the `role="alert"` form-level error.

Corroborating, from a source with a different clock: `knowledge/_validate_snippets.py` run this
session — **135 snippets, 0 failures**. All eight appear in `knowledge/_REVIEW-SIGNOFF.md`
(2–5 hits each). **None** of the eight is in `knowledge/_REVIEW-QUEUE.json`.

## Cross-lane — β and γ rest on the SAME stale column ⚠ URGENT

The brief asked lane γ to check *"whether the charts-kit gap row is a register artefact before
briefing/building it"*. Right instinct, wrong blast radius — **the artefact is the column, not
the row.** Measured here from the same register:

| lane | row | name | `itinerary_status` | `derived` | on disk |
|---|---|---|---|---|---|
| β | 68 | Alert / inline callout | Gap | **GATED** | ✓ |
| β | 69 | Toast / snackbar | Gap | **GATED** | ✓ |
| β | 70 | Drawer / side sheet | Gap | **GATED** | ✓ |
| β | 71 | Popover | Gap | **GATED** | ✓ |
| β | 72 | Skeleton loader | Gap | **GATED** | ✓ |
| γ | 51 | Data grid (sort/filter/select/edit) | Gap | **GATED** | ✓ |
| γ | 52 | Stat / metric card | Gap | **GATED** | ✓ |
| γ | 53 | Charts / data-viz kit | Gap | **GATED** | ✓ all 14 chart slugs |
| γ | 54 | Empty state | Gap | **GATED** | ✓ |

**All 17 wave-3 components across all three lanes already exist and are gated.** If β and γ are
live in parallel right now, they are building duplicates — worth a stop message before their
reconcile, not after.

## Verify — driven both ways by this seat, not trusted from a banner

`knowledge/_render/verify_wave3_alpha_218.py` (new, untracked). No browser, no network, no font
farm: every claim this lane makes is a claim about files. Mutant dir session-suffixed,
`$AW_MUTANT_DIR` default `/var/tmp/218w3a-alpha-s218wa`.

```
$ python3 knowledge/_render/verify_wave3_alpha_218.py
green arm: 55/55 checks green (3 control · 12 drift · 40 store)
OK: all eight lane-α rows are GATED in the store; the '8 to build' premise is a stale-column
artefact. TRUE gaps in the register: row 86 only.

$ python3 knowledge/_render/verify_wave3_alpha_218.py --break
--break arm: controls GREEN | drift(control) 12/12 green | store 40/40 RED by name
OK: every store check is a real measurement (it can fail); the drift finding holds.
```

Design notes, because a verifier that cannot fail proves nothing:

- The 40 **store** checks probe the working tree directly (filesystem, an *imported*
  `_validate_radius.MIGRATED_SNIPPETS`, `canon.css` text). They never read the verdict out of the
  register — that would be a tautology, green whether or not a single file exists.
- The 12 **drift** checks are claims *about* the register, so they are controls: green in both
  arms. Two further controls (`register-readable` 124 rows, `store-root-populated`) must also be
  green in the break arm, or the run reports FAILED rather than passed — a harness that failed to
  build its mutant would otherwise read as a clean red sweep.
- **The break arm earned its keep on the first run.** The mutation stripped
  `MIGRATED_SNIPPETS` entries by *slug*, but entries are the capitalised filename
  (`"Form-layout.reference.html"`), so 8 of 40 store checks stayed green with the artefacts
  removed. The arm reported **FAILED by name** rather than laundering a partial mutation as a
  pass; the regex was fixed and both arms re-driven. Noted in-file at the fix site.

## Decisions for Dave — every judgment call named, NONE decided here

1. **The wave-3 programme itself.** With all 17 components gated, is wave-3 **closed**, or is its
   real subject *quality* rather than *existence* — a deepening pass over the gated forms family
   (behaviour, a11y, four-theme correctness) rather than net-new builds? These are different jobs
   with different prices. Not decided here.
2. **Row 86 is the register's only true gap** ("Brand mark / logo", derived ASSET-ONLY). Memory
   carries `crescent = Apollo-only` on brand marks, so this is squarely Dave's, and it is not a
   forms-lane item. Does it want a lane at all?
3. **Does the itinerary spreadsheet column get retired, re-based, or left frozen as history?**
   Under ADR-0017 (WRITE-ONCE: live facts one home, history frozen) `itinerary_status` is
   *history* and `derived` is the *live fact* — but the two sit adjacent in one file and the
   history column has now misled a build brief. Ruling-shaped; see residual R1 for the mechanical
   options, priced but not chosen.
4. **File-upload carries `aria-invalid` 0** where its seven siblings carry 2–7. Real defect or
   correct for a dropzone (where the error surface is per-file, not per-control)? A component
   judgment, one file. Not touched — snippet edits were the *build* mandate this lane declined.
5. **Lane β / γ stop-or-continue** is the conductor's call, but it is time-critical rather than
   ruling-shaped — flagged here so it is not read as merely informational.

## Residuals, priced

- **R1 · The stale column will mislead again** (4th recurrence of the stale-queue class on §C·1 —
  the charts receipt already flagged recurrences 1–3, then reproduced it). This is the
  *gate-don't-patch* case, Dave #215: *"always real fixes never patches, they just get lost."*
  Priced options, none enacted: **(a)** `gen_itinerary_status.py` stops emitting bare
  `itinerary_status` and emits `itinerary_status_1907_FROZEN` — one generator string plus its
  consumers, ~1 lane-hour, and every future reader trips over the name; **(b)** a gate asserting
  no brief cites a Gap count that disagrees with `derived` — gates the PRESENCE class, but needs
  a brief-parser that does not exist, ~half a day; **(c)** the register's `$caveat` promoted into
  a `$DO-NOT-READ-THIS-COLUMN-AS-A-WORKLIST` banner, ~10 min, weakest. **Recommend (a)**: it fixes
  the class at the one writer, and the rename is self-enforcing. Generator + `reviews/` are both
  outside this lane's region — named, not enacted.
- **R2 · File-upload `aria-invalid` 0.** Measurement above; decision 4. Unpriced until ruled —
  one snippet edit if it is a defect, zero if the dropzone reading is right.
- **R3 · `#185` forgotten-document class.** `knowledge/_render/verify_wave3_alpha_218.py` and this
  receipt have **no `_state.json` store rows** — the store is fenced from this seat. **Rows owed
  at the conductor's wrap.** Same class the behaviour and charts lanes both declared.
- **R4 · Verify-harness duplication is NOT incurred here.** This lane's verify shares no lines
  with the three `verify_behaviour_218w3_*.py` scripts (no Playwright, different subject), so
  finding F6's ~130-line harness duplication does not grow. Stated so the conductor need not
  re-measure.
- **R5 · UNPROVEN, declared.** This lane drove **no renders** — no four-theme × light/dark pass,
  no font farm, no thumbnails. Correct for the work actually done (a claim about files needs no
  browser), but it means the eight gated snippets' *visual* four-theme correctness is **carried
  from their own gating, not re-proven here**. Price to re-prove if wave-3 becomes a quality
  pass: one render matrix per component, 8 components × 4 themes × 2 modes.
- **R6 · The register snapshot is 4 days old** (`$measured: 2026-08-21`) and is explicitly *"a
  snapshot of a LIVE working tree"*. The **drift** figures quoted here are properties of that
  snapshot; the **store** figures were re-probed live at 2026-08-25 by the verify, which is why
  the verify probes the tree rather than trusting the file. Re-running
  `gen_itinerary_status.py` would refresh it — `reviews/` is fenced, so not run.

## Tree state left for the conductor (reconcile every path)

- **New (untracked):** `knowledge/_render/verify_wave3_alpha_218.py` · this receipt.
- **Modified:** none. **No snippet, generator, registry, canon or store file was touched.**
- Pre-existing dirt at boot (not this lane's): the Window 2 / Window 3 crank changes described in
  their own receipts.
