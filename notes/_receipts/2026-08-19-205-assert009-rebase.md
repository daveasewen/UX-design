# Receipt — #205 ASSERT-009 re-base (77 → 92), Dave: "do it"

```
provenance: #205 · 2026-08-19 · conductor seat (Fable) · authorised by Dave in chat
```

## What was measured (first-hand, in-sandbox, this session)

| claim | command | figure |
|---|---|---|
| dir metas | `ls knowledge/components/*.meta.json \| wc -l` | **92** |
| dir entries | `ls knowledge/components/ \| wc -l` | **96** |
| non-meta files in dir | `ls \| grep -v meta.json` | same four as #131 (schema, conformance, 2 nodes files) |
| repo-wide `*.meta.json` | `find . -name "*.meta.json"` | **176** — a DIFFERENT population (snippet metas); the old "77 repo-wide" framing retired from the claim |

## What was changed (one pass, corrections by addition)

1. `knowledge/_assertions.json` — ASSERT-009: claim text re-based, predicate `n` 77 → 92, `last_verified` 2026-08-19, provenance line appended.
2. `_LIVE-STATE.md` § OPEN #131 — re-base note added above the guard line; guard count 76 → 92; #131 prose kept verbatim.
3. `notes/_MEMENTO-DECISIONS.md` § FINDING #131 — one-line addendum pointing at the live copy.
4. `_DECISION-HISTORY/2026-08-08-131-…` § Finding 3 — dated addendum, nothing trimmed.
5. `knowledge/_ASSERTIONS.md` — regenerated by `_validate_assertions.py` itself (✗ → ✓).

## Verification

`python3 knowledge/_validate_assertions.py` → **rc=0, "8 claim(s) still true"**, ASSERT-009 `count=92 (want eq 92)`.

## Declared, not fixed

- **`asserted_in` names `GOOD-MORNING.md` but GM no longer carries the figure** (grep 0 hits — the #131 item rolled to archive). Home-pointer-rot class; the list entry is left as-is since the assertion's own doc list is history too. Noted here so the next re-base doesn't hunt GM.
- **ASSERT-001 is past its 30d recheck window** (Latin webfont files) — unrelated, surfaced by the same run, not touched.
- **The remedy question (a)/(b)/(c) is UNCHANGED and Dave's** — this re-base deliberately does not count as picking a mechanism; `clears_when` still stands.
- The #204 `_build_all` full-pass blocker is now removed at step 10; the 49 never-green steps beyond it remain never-green.
