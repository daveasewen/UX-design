{"id": "W1-1", "kind": "challenge", "section": "1 · WAVE 1 — CI legibility", "claim": "✅ CONFIRMED", "evidence": "Re-run first-hand: `python3 knowledge/_governs.py --selftest` -> rc=0, last line `_governs.py selftest: all bites green`. The run also prints the two ⬛ DECLARED blocks (1 bare-directory entry `s202-D3`->`knowledge`, and 32 slashed-directory entries) rather than swallowing them.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W1-2", "kind": "challenge", "section": "1 · WAVE 1 — CI legibility", "claim": "✅ CONFIRMED — line number is 292, not 280", "evidence": "`grep -n 'def matches' knowledge/_governs.py` -> 292; `grep -n 'if \"/\" not in gn and gn in t.replace' knowledge/_governs.py` -> 303 (the removed arm, quoted verbatim inside the matches() docstring). The claim cites `:280`; the docstring block runs 292-321. The substance — the removed arm is quoted verbatim in matches() — is exact.", "verdict": "CONFIRMED", "rc": 0, "note": "figure · the cited line 280 is `names_a_directory`'s docstring, 12 lines above the construct named"}
{"id": "W1-3", "kind": "challenge", "section": "1 · WAVE 1 — CI legibility", "claim": "✅ CONFIRMED — reproduced with my OWN monkeypatch, not theirs", "evidence": "Inline `python3` importing `_governs`, replacing `matches` with a hand-typed pre-#208 body (the four-clause loop plus `if \"/\" not in gn and gn in t.replace(\"/\", \" \").split(): return True`), then calling `selftest()` under `contextlib.redirect_stdout` -> `MUTANT failures: 2`, verbatim: `an unrelated path matched a ruling — the matcher is too loose to carry information` and `a bare entry \\`knowledge\\` matched a PATH SEGMENT`. Restoring the original -> `RESTORED failures: 0`. The green is not an assertion.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W1-4", "kind": "challenge", "section": "1 · WAVE 1 — CI legibility", "claim": "✅ CONFIRMED", "evidence": "`python3 knowledge/_inscribe_ruling.py --selftest` -> rc=0.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W1-5", "kind": "challenge", "section": "1 · WAVE 1 — CI legibility", "claim": "✅ CONFIRMED", "evidence": "`python3 knowledge/_capture_gate.py --selftest` -> rc=0, `capture-gate selftest: all failure classes bite; green control passes`.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W1-6", "kind": "challenge", "section": "1 · WAVE 1 — CI legibility", "claim": "✅ CONFIRMED — both directions, in the claimed clone shape, first-hand", "evidence": "Built the shape myself: `git clone -q --no-hardlinks file:///…/UX-design full` (full history, 47M .git). FIXED tree -> `python3 knowledge/_capture_gate.py --selftest` rc=**77**, `⊘ capture-gate selftest: every reachable arm passed; 3 arm(s) could not be asked in this checkout`, the three named as ds-034/ds-034/ds-035 evidence excluded by the repo's own .gitignore. BASELINE -> `git show HEAD~2:knowledge/_governs.py > knowledge/_governs.py` then the same command -> rc=**1**. 1 and 77 are the two directions, exactly as claimed.", "verdict": "CONFIRMED", "rc": 77}
{"id": "W1-7", "kind": "challenge", "section": "1 · WAVE 1 — CI legibility", "claim": "✅ CONFIRMED", "evidence": "`python3 knowledge/_build_graph_mention_map.py --check` -> rc=0, `graph mention map --check: current (101 of 101 node(s) mentioned)`.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W1-8", "kind": "challenge", "section": "1 · WAVE 1 — CI legibility", "claim": "✅ CONFIRMED", "evidence": "`grep -n 'open(' knowledge/_build_graph_mention_map.py | grep \"'w'\"` -> exactly one line, 176, and it opens `OUT_PATH`; `OUT_PATH` is defined at :33 as `knowledge/_graph-mention-map.json`. The other `open()` calls (:167, and the selftest tempfile) are read-mode. One file, whole-file overwrite.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W1-9", "kind": "challenge", "section": "1 · WAVE 1 — CI legibility", "claim": "✅ CONFIRMED — but the claim's own plant is NOT the reproducible one; I had to find the real lever", "evidence": "My first plant — appending a line naming three node ids to `notes/_MEMENTO-DECISIONS.md` — left `--check` at rc=0. Reading the generator: the corpus is NOT markdown; `build()` reads `knowledge/_decision-graph.json` and `knowledge/_memento-index.json` only (`GRAPH_PATH`/`INDEX_PATH`, :31-32). Re-planted correctly in a scratch clone — appended one record to `_memento-index.json['records']` whose blob names node `ADR-0001`: rc_before=0 -> rc_mutated=**1** `STALE — regenerate` -> `python3 knowledge/_build_graph_mention_map.py` (`101 of 101 node(s) mentioned, 1100 record hit(s)`) -> rc_after_regen=0 -> restore index + regenerate -> `cmp` **byte-identical**. Both directions, no residue.", "verdict": "CONFIRMED", "rc": 1, "note": "⚠ a re-runner following the claim's wording alone would plant in the wrong place and read a false green"}
{"id": "W1-10", "kind": "challenge", "section": "1 · WAVE 1 — CI legibility", "claim": "✅ CONFIRMED — all FIVE arms driven first-hand on REAL landed commits in a full clone", "evidence": "Arm FRESH: `SESSION_N=208 bash knowledge/_git_commit.sh --reconciled /sessions/relaxed-keen-euler/vfy/full/outputs/m_a1.txt /sessions/relaxed-keen-euler/vfy/full/notes/_vfy.txt` -> `— mention map fresh (…#208 [110] class gate)`, rc=0, commit f6211a7 landed. Arm STALE (index re-planted): rc=**1**, `— mention map STALE — regenerating the ONE file this generator owns:` then `✗ MENTION-MAP GATE (#208, the [110] re-stale class, 3rd recurrence)`; `git diff --cached --name-only | wc -l` -> **0**; HEAD unchanged. Arm HATCH: `MENTION_MAP_ACK=\"verifier drive\"` -> `— mention-map gate: DECLARED GAP — verifier drive`. SECOND HALF, not staged: map hand-edited, path omitted -> rc=**1**, `✗ MENTION-MAP GATE (#208, second half): … differs from HEAD and is NOT staged`. SECOND HALF, named: same run with `knowledge/_graph-mention-map.json` appended -> rc=**0**, `— committed: 68ce12b`. Gate body at knowledge/_git_commit.sh:286-298 and :555-559.", "verdict": "CONFIRMED", "rc": 1}
{"id": "W1-11", "kind": "challenge", "section": "1 · WAVE 1 — CI legibility", "claim": "✅ CONFIRMED", "evidence": "`grep -n 'REUSED-MSGFILE GATE' knowledge/_git_commit.sh` -> 182 (the new #208 door gate), 480 (the #170 in-branch gate, left in place). The #170 arm at :487 is `re.match(r\"after #\\d+ \\d{4}-\\d{2}-\\d{2} — \", first)` — anchored, `after `-only, inside T3's non-wrap branch, exactly as described.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W1-12", "kind": "challenge", "section": "1 · WAVE 1 — CI legibility", "claim": "✅ CONFIRMED, and I drove the gate itself rather than only its selftest", "evidence": "`bash knowledge/_git_commit.sh --selftest` -> rc=0, `✓ selftest OK — 10 bites: 5 fire, 5 stay silent`. Then seven adversarial msgfiles through the real door in a scratch clone. REFUSED (correct): doubled em-dash prefix · a prefix with two spaces after `after` · uppercase `AFTER` · leading whitespace. PASSED the door (correct — unreachable shapes): `after #208 2026-8-19 — …` (unpadded month) and `after #208 2026-08-19 —no-space` — T3 emits `date +%F` and `— ` literally (knowledge/_git_commit.sh:416, :493), so neither shape can arise from reuse. See NEW-2 for the one case that is a false positive.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W1-13", "kind": "challenge", "section": "1 · WAVE 1 — CI legibility", "claim": "✅ CONFIRMED on a REAL landed commit, not a receipt", "evidence": "Full clone, real script: `SESSION_N=208 bash knowledge/_git_commit.sh --reconciled /sessions/relaxed-keen-euler/vfy/full/outputs/msgD.txt /sessions/relaxed-keen-euler/vfy/full/notes/_vfy.txt` -> rc=0, `— committed: 4387b7a after #208 2026-08-19 — verifier scratch commit, do not ship`. `head -1 /sessions/relaxed-keen-euler/vfy/full/outputs/msgD.txt` AFTER the run -> `verifier scratch commit, do not ship` — the author's line, unmutated. T3 writes `${MSGFILE}.t3-rendered` (:415) and commits with `-F \"$RENDERED\"` (:587).", "verdict": "CONFIRMED", "note": "⚠ the commit path is unreachable in a bare clone until `knowledge/.token-cache.json` is copied in — `_gen_chain.py --check` returns COULD-NOT-ASK first (#173). I copied it; without that step this row is untestable from a clone. ⚠ rc REMOVED: this row cites three commands with three different exit codes and the schema carries ONE scalar `rc`; the sampler drew the third and called it a mismatch. See ERGONOMICS 6."}
{"id": "W1-14", "kind": "challenge", "section": "1 · WAVE 1 — CI legibility", "claim": "✅ CONFIRMED — I made the assert BITE on a real commit by mutation, both directions", "evidence": "In the full clone I patched T3's non-wrap headline line to `f\"after #{session_n} {today} — after #207 2026-08-18 — {first}\"`, simulating exactly what a wrap whose GOOD-MORNING summary already carries a prefix produces. Result: the commit LANDED as `cdba859 after #208 2026-08-19 — after #207 2026-08-18 — verifier doubled-subject drive` and the script exited **1** with `✗ SUBJECT PREFIX COUNT = 2 (must be exactly 1) — the doubled-subject class (#208, 8 documented instances).` CONTROL: mutation reverted, same command -> rc=**0**, `— subject carries exactly ONE T3 prefix`. The assert reads `git log -1 --format=%s`, post-commit, at :628-636. ⚠ I did NOT reproduce the claim's specific route (a `--wrap` run with a planted GOOD-MORNING banner); I reproduced the CLASS through a different door.", "verdict": "CONFIRMED", "rc": 1}
{"id": "W1-15", "kind": "challenge", "section": "1 · WAVE 1 — CI legibility", "claim": "UNTESTED — unfalsifiable at HEAD", "evidence": "The lane's own words are `git -C . status --short shows only the modified/created files…`. The conductor has since committed both wave commits; `git status --short` on the mount is now EMPTY, so the assertion has no observable referent. Nothing about the lane's discipline is in doubt; the row simply cannot be re-checked from this seat once the wrap landed. This is a general property of every `0 · lane discipline` row — see ERGONOMICS 5.", "verdict": "UNTESTED", "note": "the claim was tagged CLAIMED, so it is EXEMPT from the evidence linter — correctly"}
{"id": "W1-16", "kind": "challenge", "section": "1 · WAVE 1 — CI legibility", "claim": "✅ CONFIRMED in a fresh clone I built myself", "evidence": "`python3 knowledge/_tests/test_gates.py` in the depth-1 clone of 83977f5 -> `27 test(s), 1 failure(s)`, rc=1, the single failure being the a11y sub-24 target-floor arm; tail also prints `skipped by design: state-contrast / screen --render (need a browser)`. Pre-existing, unrelated to the lane.", "verdict": "CONFIRMED", "rc": 1}
{"id": "W2-1", "kind": "challenge", "section": "2 · WAVE 2 — record hygiene", "claim": "✅ CONFIRMED verbatim, both matchers read", "evidence": "`sed -n '92,96p' knowledge/_probe_registry/probe_premise_store.py` -> `if in_scope and rel not in homes and os.path.basename(rel) not in homes:` (EXACT). `sed -n '116,120p' knowledge/_gate_doc_rows.py` -> `if not any(p in h or base in h for h in homes):` (SUBSTRING). Two instruments, two matchers, one question — the disagreement is structural and still present at HEAD.", "verdict": "CONFIRMED", "rc": 1}
{"id": "W2-2", "kind": "challenge", "section": "2 · WAVE 2 — record hygiene", "claim": "✅ CONFIRMED", "evidence": "`python3 knowledge/_probe_registry/probe_premise_store.py --check` -> rc=0, `P-4 premise-vs-store diff: 67 doc(s) over ['notes/_briefs/*.md'] vs 76 store item(s) / 205 ruling(s) · 0 finding(s)`.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W2-3", "kind": "challenge", "section": "2 · WAVE 2 — record hygiene", "claim": "✅ CONFIRMED", "evidence": "`python3 knowledge/_state.py` -> rc=0, `project split: apollo 39 · memento 37 (of 76 items)`.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W2-4", "kind": "challenge", "section": "2 · WAVE 2 — record hygiene", "claim": "✅ CONFIRMED", "evidence": "`grep -c \"92\" GOOD-MORNING.md` -> 20. Reading them: GOOD-MORNING.md:27 is the #207 session-report entry `knowledge/README.md:13 **38 → 92** against ASSERT-009` — a report ABOUT the README repair, not GM asserting the corpus size. No other line states the meta count.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W2-5", "kind": "challenge", "section": "2 · WAVE 2 — record hygiene", "claim": "✅ CONFIRMED — all four homes carry the figure", "evidence": "`sed -n '85p' _LIVE-STATE.md` -> contains 92 · `sed -n '5628p' notes/_MEMENTO-DECISIONS.md` -> contains 92 · `grep -c \"92\" _DECISION-HISTORY/2026-08-08-131-…-nothing-checks.md` -> 1 · `sed -n '13p' knowledge/README.md` -> the Components row. Four live homes, verified independently of the lane.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W2-6", "kind": "challenge", "section": "2 · WAVE 2 — record hygiene", "claim": "✅ CONFIRMED — nothing was deleted from history", "evidence": "Read `knowledge/_assertions.json` through json: ASSERT-009 `asserted_in` = 4 entries, GOOD-MORNING.md ABSENT; the provenance tail reads `The live homes that DO carry the figure, each verified by grep this session: _LIVE-STATE.md:85/597/598 - … - knowledge/README.md:13. The consequence field's prose still names GOOD-MORNING.md's banner as a #131-era fan-out and is left standing as HISTORY, deliberately`. Removal + reason APPENDED, history frozen — the ADR-0017 shape.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W2-7", "kind": "challenge", "section": "2 · WAVE 2 — record hygiene", "claim": "✅ CONFIRMED on substance · ⛔ THE EVIDENCE POINTER IS DEAD AT HEAD", "evidence": "The row's evidence is `git show HEAD:knowledge/_assertions.json`. Run today that returns `last_verified = 2026-08-19` — the REPAIRED value, the opposite of what the row asserts. The claim reproduces only at `HEAD~2`: `git show HEAD~2:knowledge/_assertions.json` -> ASSERT-001 `last_verified` = **2026-07-18**, `recheck_days` 30; 2026-07-18 to 2026-08-19 is **32 days**, matching the quoted `32d ago, window is 30d`. The substance is exact. The POINTER FORM is not: `HEAD` moved the moment the conductor committed the claim table, so the row's own evidence now contradicts it. See ERGONOMICS 2.", "verdict": "CONFIRMED", "rc": 0, "note": "pointer form · the correct ref is HEAD~2 (8c09888)"}
{"id": "W2-8", "kind": "challenge", "section": "2 · WAVE 2 — record hygiene", "claim": "✅ CONFIRMED", "evidence": "`find knowledge/assets/fonts -iname \"*.woff*\" | wc -l` -> **50**; the pack directories are Helvetica Armenian · M Ying Hei Simplified Chinese · M Ying Hei Traditional Chinese · Tazugane Japanese · Univers Arabic — five script packs, zero Latin. The 2026-07-18 enumeration reproduces exactly.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W2-9", "kind": "challenge", "section": "2 · WAVE 2 — record hygiene", "claim": "✅ CONFIRMED", "evidence": "`python3 knowledge/_validate_assertions.py` -> rc=0, `✅ assertion gate passed — 8 claim(s) still true.`, and no stale-warning tail.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W2-10", "kind": "challenge", "section": "2 · WAVE 2 — record hygiene", "claim": "✅ CONFIRMED", "evidence": "`grep -c \"2026-08-19-204-\" _LIVE-STATE.md` -> 1.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W2-11", "kind": "challenge", "section": "2 · WAVE 2 — record hygiene", "claim": "✅ CONFIRMED", "evidence": "`ls _DECISION-HISTORY/2026-08-19-204-the-topology-that-earned-its-amendments.md` -> rc=0.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W2-12", "kind": "challenge", "section": "2 · WAVE 2 — record hygiene", "claim": "✅ CONFIRMED on substance · ⛔ THE EVIDENCE POINTER IS DEAD AT HEAD", "evidence": "Same class as W2-7. `git show HEAD:knowledge/_build_all.py | grep -c 'ARGV CONTRACT'` -> **3** (the guard IS at HEAD — the row asserts it was absent). At the right ref: `git show HEAD~2:knowledge/_build_all.py | grep -c 'ARGV CONTRACT'` -> **0**. Pre-fix main() had no else-refusal; substance exact, pointer dead.", "verdict": "CONFIRMED", "rc": 0, "note": "pointer form · correct ref is HEAD~2"}
{"id": "W2-13", "kind": "challenge", "section": "2 · WAVE 2 — record hygiene", "claim": "✅ CONFIRMED and WIDENED — I fed it eight plausible-but-unknown tokens, refusal paths only", "evidence": "`python3 knowledge/_build_all.py <tok>` for `--wrap` · `--dry-run` · `--check` · `--force` · `--range` (flag with no value) · `-v` · `--Selftest` (case near-miss) · a bare path token (`notes/x.md…`, a path that does not exist) -> **rc=2 for all eight**, each printing the `_build_all.py REFUSED — ARGV CONTRACT` banner. ⛔ NO REAL BUILD WAS RUN: every one of these returns from `check_argv` before `check_routes()`.", "verdict": "CONFIRMED", "rc": 2}
{"id": "W2-14", "kind": "challenge", "section": "2 · WAVE 2 — record hygiene", "claim": "✅ CONFIRMED", "evidence": "`python3 knowledge/_build_all.py --selftest` -> rc=0, `selftest (f): #208 argv contract — 5 illegal forms refused NAMED, 6 legal forms accepted, mutation control bites, main() returns rc=2 before step 1 ✓`.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W2-15", "kind": "challenge", "section": "2 · WAVE 2 — record hygiene", "claim": "✅ CONFIRMED by reading the guard's POSITION, which is the load-bearing part", "evidence": "`sed -n '1095,1112p' knowledge/_build_all.py` -> `def main(argv=None):` / `argv = sys.argv[1:] …` / `bad = check_argv(argv)` / `if bad: return _refuse_argv(bad, argv)` / `if \"--selftest\" in argv: return selftest()` / `check_routes()`. The guard is the first statement after argv resolution, above `check_routes()` and above the step loop. Every one of my eight drives above returned before any `subprocess.run`.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W2-16", "kind": "challenge", "section": "2 · WAVE 2 — record hygiene", "claim": "✅ CONFIRMED on substance by my OWN plant-and-detect · ⛔ POINTER DEAD AT HEAD", "evidence": "`git show HEAD:knowledge/_gate_doc_rows.py | grep -c -- '--cached'` -> **25** (the staged arm IS at HEAD). At `HEAD~2` -> **0**. Then the real proof, on a real git index in my full clone: created `/sessions/relaxed-keen-euler/vfy/full/notes/_briefs/2026-08-19-999-verifier-planted-unrowed-brief.md`, `git add`ed it, and ran BOTH gates against the SAME index — `python3 /sessions/relaxed-keen-euler/vfy/full/knowledge/_gate_doc_rows_OLD.py --check` (the HEAD~2 blob) -> rc=**0**, `population 21 · unrowed 0`, `✅ PASS` **while an unrowed doc sits staged**; `python3 knowledge/_gate_doc_rows.py --check` (HEAD) -> rc=**1**, `⛔ UNROWED: /sessions/relaxed-keen-euler/vfy/full/notes/_briefs/2026-08-19-999-verifier-planted-unrowed-brief.md`. The blindspot and its repair are both real.", "verdict": "CONFIRMED", "rc": 1}
{"id": "W2-17", "kind": "challenge", "section": "2 · WAVE 2 — record hygiene", "claim": "✅ CONFIRMED — selftest AND an independent adversarial plant", "evidence": "`python3 knowledge/_gate_doc_rows.py --selftest` -> rc=0, `✅ staged-arm: a doc present ONLY in the index is seen and flagged` · `✅ mutation-arm: deleting the notes/_briefs/2026-08-19-207-wrap-brief.md reference is flagged` · `✅ SELFTEST PASS — the green can fail`. My own plant (W2-16 above) reproduces the same catch on a doc the selftest never saw.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W2-18", "kind": "challenge", "section": "2 · WAVE 2 — record hygiene", "claim": "✅ CONFIRMED", "evidence": "`sed -n '178,190p' knowledge/_gate_doc_rows.py` -> `rowed = [p for _, p in pop if not any(p == q for _, q in unrowed(live, pop))]` then `if not rowed: print(\"⛔ SELFTEST MUTATION-ARM: no rowed doc to mutate — arm is vacuous, not green.\"); return 1` then `victim = rowed[-1]`. A ROWED victim, with a loud refusal when none exists.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W2-19", "kind": "challenge", "section": "2 · WAVE 2 — record hygiene", "claim": "✅ CONFIRMED in TWO environments", "evidence": "Mount: `python3 knowledge/_gate_doc_rows.py --check` -> rc=0, `population 21 (added >= 2026-08-15, PICKED) · of which staged-in-THIS-commit 0 · unrowed 0`. Full clone of the same commit: byte-identical output, rc=0. The fix widened the population without inventing a finding. ⚠ In a `--depth 1` clone the same command returns rc=1 — see NEW-3.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W2-20", "kind": "challenge", "section": "2 · WAVE 2 — record hygiene", "claim": "⛔ CONTRADICTED as written — the patch WAS applied, in the very commit that landed this claim table", "evidence": "The row says the seam patch is `written in the receipt and NOT applied, the file being another sub's`. At HEAD there IS a post-staging doc-row gate: `grep -n '_gate_doc_rows' knowledge/_git_commit.sh` -> **246** (pre-staging, the one the row names) AND **568**, under the header at :564 `…reports PASS while doing it (#207 postscript). _gate_doc_rows.py now ALSO reads…`, printing `— doc rows present for this commit's staged adds (_gate_doc_rows.py, post-staging)`. Provenance: `git show HEAD~2:knowledge/_git_commit.sh | grep -c 'post-staging'` -> **0**; `git log -S 'post-staging' -- knowledge/_git_commit.sh` -> **5882813**, the waves-1+2 commit. So wave 1 applied it in the same commit that shipped wave 2's claim that it had not been. The DIAGNOSIS (gate at :246 fires before staging at :541) is exact and reproduces; the STATUS clause does not.", "verdict": "CONTRADICTED", "rc": 0, "note": "status clause · the diagnosis stands, the 'NOT applied' does not"}
{"id": "W2-21", "kind": "challenge", "section": "2 · WAVE 2 — record hygiene", "claim": "UNTESTED — unfalsifiable at HEAD", "evidence": "Same class as W1-15: `git status --short` on the mount is now empty because the conductor committed. No observable referent survives the wrap.", "verdict": "UNTESTED"}
{"id": "W3-1", "kind": "challenge", "section": "3 · WAVE 3 — externality repairs", "claim": "✅ CONFIRMED exactly, by re-driving the probe against the PRE-repair blobs", "evidence": "In the full clone: `git checkout 83977f5~1 -- reviews/` then `python3 knowledge/_probe_registry/probe_dup_ids.py --check` -> rc=**1**, `P-2 duplicate-ID/IDREF scan: 45 file(s) over ['reviews/REVIEW-*.html'] · 46 finding(s) · 56 WARN-tier fragment miss(es)`; `grep -c DUPLICATE-ID` -> **10**, `grep -c UNRESOLVED-IDREF` -> **36**. 10+36=46, 45 files. Restoring the repaired blobs returns findings=0. Every figure in the row reproduces.", "verdict": "CONFIRMED", "rc": 1}
{"id": "W3-2", "kind": "challenge", "section": "3 · WAVE 3 — externality repairs", "claim": "✅ CONFIRMED indirectly — the per-pane assertion is not directly re-runnable, but its consequence is", "evidence": "The repair script's `n==1` per-pane assertion ran inside the lane and is gone. What IS re-checkable is its consequence, and it holds: after the repair `probe_dup_ids.py --check` reports **0 DUPLICATE-ID** across the 45-file glob (was 10), and my independent mask (W3-5) shows every changed byte lies inside an id/IDREF/fragment VALUE — i.e. no cross-pane reference was left pointing at a renamed id, which is exactly what the per-pane claim predicts. `git show --stat HEAD -- reviews/` -> 7 files, **220 insertions, 220 deletions** — equal-line on every file.", "verdict": "CONFIRMED", "rc": 0, "note": "the assertion itself is UNTESTABLE-FROM-THIS-SEAT (an ephemeral script); its prediction is confirmed"}
{"id": "W3-3", "kind": "challenge", "section": "3 · WAVE 3 — externality repairs", "claim": "✅ CONFIRMED, with the claim's own probe re-run and widened", "evidence": "`grep -c \"getElementById\\|querySelector\"` over each of the seven repaired files -> **0** for all seven. I also parsed each file's `<style>` blocks for CSS id selectors: three files return one apparent hit each, all three being the literal `#token-manifest` inside a CSS COMMENT (`derived from the snippet's own #token-manifest, not typed by hand`), not a selector. No id-keyed CSS, no id-keyed script. The renames cannot change rendering.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W3-4", "kind": "challenge", "section": "3 · WAVE 3 — externality repairs", "claim": "✅ CONFIRMED verbatim at the pre-repair blob", "evidence": "`git show HEAD~1:reviews/REVIEW-203-time-picker-four-themes-v1.html | sed -n '66p;69p'` -> line 66 `<p class=\"tp-help t-ed-body-small\" id=\"f-time-help-mono-light\">24-hour format, like 14:30.</p>` and line 69 `… aria-describedby=\"f-time-help f-time-msg-mono-light\">`. The first token lacks the pane suffix its target carries; the second token already has it. Exactly the shape the row describes.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W3-5", "kind": "challenge", "section": "3 · WAVE 3 — externality repairs", "claim": "✅ CONFIRMED — re-run with MY OWN masker, plus two controls the lane did not run", "evidence": "I wrote an independent masker (11 IDREF attributes, `id=`, and `(xlink:)?href=\"#…\"`) and compared each file's `HEAD~1` blob to its `HEAD` blob: **all seven masked-identical=True**. Substitution counts per file 48/40/331/447/72/200/48 — identical before and after in every file. EXTRA CONTROL the lane did not run: visible-text length (tags stripped) is unchanged on all seven — 5541 · 21325 · 11461 · 13988 · 22451 · 9208 · 29825, before == after. MASKER MUTATION CONTROL: injecting one word into a file makes the masked texts differ -> True, so the masker is not eating content. I ALSO ran the lane's own `/var/tmp/diffverify.py` (it survives in this sandbox) -> rc=0, masked-identical=True on all seven.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W3-6", "kind": "challenge", "section": "3 · WAVE 3 — externality repairs", "claim": "✅ CONFIRMED on substance", "evidence": "The lane's specific bite (`'Transaction history' -> 'Transaction HISTORY'`) is not re-runnable — it mutated a working-tree file that is now committed. I re-established the property with my own instrument instead: my masker's mutation control flips masked-identical to False on a one-word content change in the same class of file. The fence check is not a check that cannot fail. Probeable token: `python3 /sessions/relaxed-keen-euler/vfy/mask.py` -> rc=0, and its last line prints `MUTATION CONTROL — a one-word content change is NOT masked away: True`.", "verdict": "CONFIRMED", "rc": 1, "note": "the lane's exact bite is UNTESTABLE-FROM-THIS-SEAT; the property is confirmed by an independent instrument"}
{"id": "W3-7", "kind": "challenge", "section": "3 · WAVE 3 — externality repairs", "claim": "✅ CONFIRMED", "evidence": "`python3 knowledge/_probe_registry/probe_dup_ids.py --check` -> rc=0, `45 file(s) … 0 finding(s) · 56 WARN-tier fragment miss(es)`.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W3-8", "kind": "challenge", "section": "3 · WAVE 3 — externality repairs", "claim": "✅ CONFIRMED", "evidence": "`grep -c '\"by\": \"Dave\"' knowledge/_rulings.json` -> **205**; `grep -c '\"id\":' knowledge/_rulings.json` -> **205**. 205 of 205.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W3-9", "kind": "challenge", "section": "3 · WAVE 3 — externality repairs", "claim": "✅ CONFIRMED", "evidence": "`grep -rn 'get(\"by\")' --include=*.py knowledge/` -> rc=1, no output. No python anywhere under knowledge/ selects on the field.", "verdict": "CONFIRMED", "rc": 1}
{"id": "W3-10", "kind": "challenge", "section": "3 · WAVE 3 — externality repairs", "claim": "✅ CONFIRMED at all three cited lines", "evidence": "`sed -n '62p' knowledge/_inscribe_ruling.py` -> `KEYS = (\"id\", \"ruled\", \"date\", \"by\", \"says\", \"governs\", \"evidence\", \"status\")`; `:108` -> `for k in (\"id\", \"ruled\", \"date\", \"by\", \"says\", \"status\"):` (the non-empty requirement); `sed -n '85p' knowledge/_probe_registry/_registry.py` -> `ENVIRONMENTS = (\"sandbox\", \"sandbox-render\", \"unknown\")` — the contrast case, an allowed-value set that `by` has no equivalent of. `by` is required and unconstrained: a constant, not a discriminator.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W3-11", "kind": "challenge", "section": "3 · WAVE 3 — externality repairs", "claim": "⛔ CONTRADICTED on one figure — `21 of 205` does not reproduce under the row's own words", "evidence": "Read `knowledge/_rulings.json` and `knowledge/_state.json` live. REPRODUCES EXACTLY: 76 items · `Counter({'claude': 47, 'dave': 29})` · `Counter({'open': 50, 'done': 24, 'parked': 1, 'blocked': 1})` · **22** open+owner=dave · **19** UNCONDITIONED (`closes_when` missing or empty) · 205 rulings. DOES NOT REPRODUCE: the row says `rulings with a NON-EMPTY open field is 21 of 205`. Measured three readings — `sum(1 for x in r if x.get('open'))` (truthy) -> **18**; `sum(1 for x in r if 'open' in x)` (key present) -> **21**; `non-empty string` -> **2**. 21 is reachable only under KEY-PRESENT, which is the one reading the row's own word `non-empty` excludes. The DIRECTION is untouched — the field is a weak replacement source either way — but the figure as stated is 18, not 21. This is the #204 `D-7` class recurring: a figure whose stated rule does not regenerate it.", "verdict": "CONTRADICTED", "rc": 0, "note": "figure · direction CONFIRMED, count CONTRADICTED"}
{"id": "W3-12", "kind": "challenge", "section": "3 · WAVE 3 — externality repairs", "claim": "✅ CONFIRMED", "evidence": "`python3 knowledge/_probe_registry/probe_dangling_var_pixel.py --check` -> rc=**77**, first line `COULD-NOT-ASK: P-3 dangling-var PIXEL test — NOT-IN-THIS-ENVIRONMENT: playwright is not importable. Stage it per knowledge/_RUNBOOK-render-verify.md (PYTHONPATH=/var/tmp/pylibs). REFUSED — not a pass.`", "verdict": "CONFIRMED", "rc": 77}
{"id": "W3-13", "kind": "challenge", "section": "3 · WAVE 3 — externality repairs", "claim": "✅ CONFIRMED", "evidence": "`python3 knowledge/_probe_registry/probe_dangling_var_pixel.py --selftest` -> rc=**77**, first line `COULD-NOT-ASK: P-3 selftest — NOT-IN-THIS-ENVIRONMENT: playwright is not importable…`", "verdict": "CONFIRMED", "rc": 77}
{"id": "W3-14", "kind": "challenge", "section": "3 · WAVE 3 — externality repairs", "claim": "✅ CONFIRMED on the empty-population arm; the black-pixel arm is UNREACHABLE here", "evidence": "`python3 knowledge/_probe_registry/probe_dangling_var_pixel.py --check --glob 'knowledge/snippets/NOPE-*.html'` -> rc=**1**, `⚠ THE GLOB MATCHED NOTHING — an empty population is not a pass.` / `PROBE P-3 — findings=1`. A real failure still exits 1; the refusal did not swallow the gate. ⚠ The second half — the findings arm over the real 14-page glob with a measured black-pixel suspect — needs Playwright, which is not importable from this seat. UNTESTABLE-FROM-THIS-SEAT.", "verdict": "CONFIRMED", "rc": 1, "note": "half-confirmed: the empty-population arm driven, the pixel arm not reachable without a staged harness"}
{"id": "W3-15", "kind": "challenge", "section": "3 · WAVE 3 — externality repairs", "claim": "✅ CONFIRMED", "evidence": "`python3 knowledge/_probe_registry/_registry.py --run` -> rc=**0** with a dedicated block: `⛔ COULD-NOT-ASK (1) — these probes did NOT run, and this run says NOTHING about their class. They do not fail this run; they also do not pass.` and `[P-3] … COULD-NOT-ASK: … REFUSED — not a pass.` The refusal is excluded from the run's exit code. `verdict()` at _registry.py:215, the block at :280.", "verdict": "CONFIRMED", "rc": 0}
{"id": "W3-16", "kind": "challenge", "section": "3 · WAVE 3 — externality repairs", "claim": "✅ CONFIRMED", "evidence": "`python3 knowledge/_probe_registry/_registry.py --selftest` -> rc=0, tail `✅ verdict: a skipped probe is neither` / `✅ _registry selftest PASS — every planted manifest defect is named, and its removal goes green (both directions).`", "verdict": "CONFIRMED", "rc": 0}
{"id": "W3-17", "kind": "challenge", "section": "3 · WAVE 3 — externality repairs", "claim": "✅ CONFIRMED", "evidence": "`grep -rn probe_dangling knowledge/_build_all.py` -> rc=1, no output — the registry is not wired in. `sed -n '1131p' knowledge/_build_all.py` -> `        if r.returncode == 77:` — the third verdict is already understood for the steps it does run.", "verdict": "CONFIRMED", "rc": 1}
{"id": "W3-18", "kind": "challenge", "section": "3 · WAVE 3 — externality repairs", "claim": "✅ CONFIRMED — and I hit the SAME wall writing THIS table", "evidence": "The row says `_validate_evidence.py` has no legal form for claiming an absence. Confirmed at the source: `knowledge/_validate_evidence.py:16-17` — `⛔ A PATH token that does NOT EXIST is a HARD FAIL: a dead pointer is worse than no pointer`. There is no ABSENT/EXPECTED-MISSING escape. I met it directly: every row of mine whose finding IS an absence (W3-9, W3-17, and NEW-4) had to express it as a `grep -rn … -> rc=1, no output` FIGURE rather than name the path that is missing. The linter is satisfied by hiding the subject — exactly as the row says. See ERGONOMICS 3.", "verdict": "CONFIRMED", "rc": 1}
{"id": "NEW-1", "kind": "challenge", "section": "4 · NEW — found by the verifier", "claim": "⛔ THE EVIDENCE SAMPLER COMPARES EXIT CODES, NOT OBSERVATIONS — and it certified two rows whose evidence I had just proved FALSE", "evidence": "`python3 knowledge/_validate_evidence.py notes/_claims/208-wave2-claims.jsonl --seed 208` -> rc=**0**, `✅ EVIDENCE GATE PASS`, including `✅ RAN W2-12 — git show HEAD:knowledge/_build_all.py → rc=0 (matches declared)` and `✅ RAN W2-7 — git show HEAD:knowledge/_assertions.json → rc=0 (matches declared)`. Both commands DO exit 0 — and both now print the POST-fix file, which is the opposite of what those rows assert (see W2-7, W2-12). `git show`, `grep -c`, `find`, `ls` all return 0 for any content. So for read-style evidence the sampler proves RUNNABILITY, not REPRODUCTION, and its green is close to vacuous. This is the single thing I would fix before wiring W-44 into CI: the row needs an optional expected-STDOUT or expected-COUNT field so the sampler can compare an observation, not a status.", "verdict": "NEW", "rc": 0}
{"id": "NEW-2", "kind": "challenge", "section": "4 · NEW — found by the verifier", "claim": "⛔ THE #208 DOOR GATE HAS NO LEGAL FORM FOR AN HONEST SUBJECT THAT QUOTES A PRIOR ONE — the counter is UNANCHORED where the #170 gate it widens is ANCHORED", "evidence": "`printf 'wave1 report on the fix #208 2026-08-19 — as quoted\\n\\nbody\\n' > /sessions/relaxed-keen-euler/vfy/scratch/outputs/m7.txt` then `bash knowledge/_git_commit.sh --reconciled /sessions/relaxed-keen-euler/vfy/scratch/outputs/m7.txt /sessions/relaxed-keen-euler/vfy/scratch/notes/_vfy-probe.txt` -> rc=**1**, `✗ REUSED-MSGFILE GATE (#208, widening #170): line 1 … already carries a T3 prefix`. Line 1 is a FRESH subject that happens to quote a prefix mid-line; nothing was reused. Cause: the door gate uses `re.findall` (:147, `prefix_count`), so a prefix-shaped string ANYWHERE in line 1 fires, while the #170 gate it widens uses `re.match` at :487 — anchored. `grep -n 'MSGFILE_ACK\\|REUSE_ACK\\|ALLOW_PREFIX' knowledge/_git_commit.sh` -> no match: there is NO declared-gap hatch for this gate, unlike its two neighbours (`SESSION_ACK`, `MENTION_MAP_ACK`). The selftest's near-miss bite for this shape is `'fixes the class #205 found'` — a session ref with NO DATE, so it never exercises the case. ⬛ Anchoring the door counter, or giving it an ACK, is a change to another sub's gate and is not mine to make. ⚠ Every path in this row lives in a scratch clone; naming it repo-relative is a HARD FAIL in `_validate_evidence.py` and the ruled `s191-D2` `(NON-REPO: …)` marker is NOT honoured — see ERGONOMICS 4.", "verdict": "NEW", "rc": 1}
{"id": "NEW-3", "kind": "challenge", "section": "4 · NEW — found by the verifier", "claim": "A gate whose verdict depends on CLONE DEPTH — and the `fetch-depth: 0` line in gates.yml is doing real work, which I proved by removing it", "evidence": "In a `git clone --depth 1` of 83977f5: `python3 knowledge/_gate_doc_rows.py --check` -> rc=**1**, flagging `notes/_briefs/2026-08-12-s157-palette-tier-brief.md (added 2026-08-19)` — a doc whose real add-date is a week outside the window. Cause: population comes from `git log --diff-filter=A`, and a shallow clone dates every file to the single commit. In a FULL clone of the same commit: rc=**0**, `population 21 · unrowed 0`, identical to the mount. Same shape for `_capture_gate.py --selftest`: depth-1 -> rc=**1** on `_governs: ruling s198-D2 points at 'commit 5b77cce…' which is not a commit in this repo`; full clone -> rc=**77**. ✅ CI IS ALREADY SAFE: `.github/workflows/gates.yml:167` and `:208` both set `fetch-depth: 0`, and the header comment at :138 names this as the SECOND independent #173 cause. This row is a MUTATION PROOF of that config line rather than a defect report — it is load-bearing and must never be trimmed.", "verdict": "NEW", "rc": 1}
{"id": "NEW-4", "kind": "challenge", "section": "4 · NEW — found by the verifier", "claim": "A probe that expected to contradict and did not: `_governs.matches()` is case-blind by construction, and it holds only because EVERY caller normalises", "evidence": "`_norm` (knowledge/_governs.py:95-96) lowercases, but `matches()` compares the lowered entry against RAW targets. `matches({'governs':['BOOT_FIRSTTURN_TK']}, {'BOOT_FIRSTTURN_TK'})` -> **False**. A census over the store finds **223** non-anchor `governs` entries containing an uppercase character (`TAPE_TO_BILL`, `GOOD-MORNING.md`, `_CHAIN.md`, `knowledge/_RUNBOOK-context-gauge.md`, …) — every one of which a raw-target caller would silently miss. I then checked every call site: `grep -rn 'surface(' --include=*.py .` -> `_capture_gate.py:4273` passes `{_governs._norm(p) for p in _governs.changed_files()}`, `:5522` passes an already-lowercase literal, and `_governs.py:745` norms argv. All three normalise, so the hole is not live. Recorded because the selftest's symbol bite uses `measure_tokens` — already lowercase — so nothing in the suite would notice a fourth caller that forgot. A one-line `t=_norm(t)` inside `matches()` would make the invariant local instead of conventional. ⬛ Not mine to apply.", "verdict": "NEW", "rc": 0}
{"id": "NEW-5", "kind": "challenge", "section": "4 · NEW — found by the verifier", "claim": "Two claim rows are UNFALSIFIABLE the moment the wrap commits, and both are `0 · lane discipline` rows", "evidence": "W1-15 and W2-21 both assert a property of the WORKING TREE (`git status --short` shows only the lane's paths / no staged entries). `git status --short` on the mount today -> **empty**, because the conductor committed 5882813 and 83977f5. A verifier arriving after the wrap can neither confirm nor refute them. The `0 · lane discipline` section is structurally post-hoc-unverifiable in a topology where the conductor commits before the verifier runs — either the verifier runs BEFORE the commit, or the row must carry a pre-commit `git status` transcript as its evidence rather than a command to re-run.", "verdict": "NEW", "rc": 0}
