# DIVVY PLAN — #82, window 2 (worker)

provenance: written #82 2026-08-02 by the conductor window, after Dave ruled CRANK at the opener
status: standing

---

## The split, and the rule that decides it

**Window 1 (this one) is the CONDUCTOR and it is the ONLY window that commits.** Window 2 is a
WORKER: it edits, it measures, it writes notes — it does **not** run `_git_commit.sh` and does not
touch `GOOD-MORNING.md`, `_LIVE-STATE.md` or `_CHAIN.md`.

⚠ **The divvy is by FILE, not by topic**, because both windows share one working tree. A
topic-shaped split reads well and collides anyway. [[feedback-worktree-reconcile-trail]] — every
path gets reconciled deliberately at the wrap, never a blind `git add -A`.

| | window 1 (conductor) | window 2 (worker) |
|---|---|---|
| owns | `knowledge/_capture_gate.py` · `knowledge/_gen_chain.py` · `knowledge/_rulings.json` · `GOOD-MORNING.md` · `_LIVE-STATE.md` · `_CHAIN.md` | `knowledge/_RUNBOOK-context-gauge.md` · `showroom/` + behaviour/legend pages · its own new files under `notes/` |
| commits | **yes** | **no** |

---

## Window 2's jobs, in order

### 1. `knowledge/_RUNBOOK-context-gauge.md` — the file contradicts itself

`:463–505` still teaches the **retired two-unit tape/bill system**, contradicting **line 31 of the
same file**. Open since #81 and named in that session's own "still open" list.

★ **It is now worse than it was**, and that is why this job is first: #82-D1 wired
`measure_tokens()` to the native counter this morning, so the runbook is teaching a retired unit
*and* a superseded instrument. A cold session sent to the runbook for the bands gets both wrong.

Also in scope, same file: **`ds-025`'s floor** — currently *"disk half measured (17,810 tape /
~14.0 pts, #37), harness half unreachable."* The disk half is now measurable in **real tokens**.
Re-measure it; **do NOT convert it** (`#54`, Dave's: converting is the defect —
[[measure-dont-convert-units]]). Publish the new reading beside the old with its unit named, and
leave the harness half exactly as unreachable as it is.

⚠ **Do not re-denominate history.** Historical readings in `notes/_GAUGE-LOG.md` and the archived
strata are *correct as written* — they record what was measured, in the unit it was measured in.
They need a **labelled unit**, never a new number.

### 2. Item (b) — headroom, before any legend change

Group **78 B free**, last measured **#80**. Budget contract: **16KB/source + 32KB/group**
(`[[behaviour-page-budget]]`), and splitting must never buy headroom. Measure it; report the
figure and the method. ⛔ **No legend change until this number exists** — that is the standing
condition on DV-D19, and the specimen is fully signed off, so there is nothing to spend headroom
on that is worth reopening it for.

### 3. Seed the trigger index — but as a PROPOSAL, not an edit

`knowledge/_rulings.json` is **window 1's file** (#82-D1's own entry landed there this morning).
Window 2 writes candidates to **`notes/2026-08-02-82-rulings-seed-proposal.md`** and window 1
merges them at the wrap.

Nine rulings are indexed; Dave's own words are *"8 is a start, not coverage."* Good candidates are
rulings that **govern code** — a ruling nothing can touch does not need a trigger.

⚠ **Pointers only.** `says` is a one-line reminder whose job is to make the reader go and read
`evidence`. Substance in the index makes it the eleventh copy, which is the defect it was built to
end. And **every `evidence` path must RESOLVE** — the selftest refused two forward-references this
morning and was right to.

---

## Three things window 2 must know before it touches anything

1. **`pip install tiktoken --break-system-packages` first.** Cold sandbox, every time. The gauge
   REFUSES loud and named without it (`MeasurementRefused`, #79-D1) and `_build_all.py`'s P5 step
   is **ABORT** as of #80 — without it there is no build.
2. **Do NOT run `_build_all.py` to check.** A partial run strands the tree in a documented
   mid-build state (it gutted 33 `knowledge/compliance/` files at #61 and a reconcile waved them
   through). Run the specific selftest you need.
3. **Forcing a fallback now needs `CAPTURE_GATE_NO_REAL=1` as well as `CAPTURE_GATE_NO_HEAL=1`.**
   As of #82-D1 a REAL tier sits above the whole cascade, so hiding tiktoken no longer degrades
   anything on a machine with a key. **Three test arms silently stopped testing anything** until
   this was found. If a test of yours forces a degraded path, suppress *every* tier above it.

---

## Handback

Window 2 finishes by writing **one note** naming: what it changed, what it measured (with the
unit), and what it did not do. Window 1 reconciles every path by hand and commits.
