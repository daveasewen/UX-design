# #288 lane A — THE s219-D3 GENERATION ARM IS BUILT

provenance: session 288 · 2026-09-19 · conductor Fable 5.1 · lane A (Opus 5, 1M) · state row `W-288a`

**Dave's order, verbatim** (`notes/_lanes/287/DAVE-RULINGS-2026-09-19.md`, the wrap call):

> I'm not sure that 0 is right for mono, lets have a proper review and fix this: 'The generation arm
> that would do that was ruled at #219 and never built.' and any other problem with this, I just need
> the one-shot design to not disappoint

**and his correction of the framing** (same file, earlier):

> The problem with the gutters it that they are deliberately different for the themes and the gutters
> are also different for the inner and outer bentos we essentially have a structural bento and
> embedded bentos or tile groupings.

---

## 1 · WHAT THE ARM DOES

**The defect, at file:line, before this lane.** `knowledge/snippets/Template-dashboard-bento.reference.html`
:802 and :804 pinned **mono's** dashboard pair as literals — `--bento-gutter:40px` on the outer wall,
`--bento-gutter:4px` on each tile-group. `gen_canon_components.py` projects the snippet verbatim, so
`knowledge/canon/canon.css:18119` / `:18121` carried the same two literals into the delivered
stylesheet. **A page in legacy, console or supercharge got mono's numbers** — measured, below.

**The mechanism, smallest correct.** A new generator,
`knowledge/canon/gen_bento_role_vars.py` (289 lines, new file):

1. reads **`knowledge/_render/_bento_edit_rails.json`** — the s219-D3 (6) *one generated file*
   (*"library, editor and generator read one generated file so none can drift"*). This lane is its
   **first consumer**; the file has declared itself `$groundwork_only` and sat un-consumed since #219;
2. **cross-checks** it against the owner the file itself names — `defaults.$owner`
   = `gen_foundations_217.py`, `defaults.$resolved_from` = `gen_foundations_217.ROLE_DEFAULTS`
   = `role_defaults_219.DEFAULTS`, which is **parsed** from Dave's own receipt
   `notes/_receipts/2026-08-25-219-role-defaults-exports.md`, never typed. **A divergence is a NAMED
   refusal, not a silent pick**;
3. mints, between new `AUTO-BENTO-ROLE-VARS` markers in `canon.css` (placed **before**
   `AUTO-THEMES START`, because `gen_theme_cascade.py` rebuilds everything from that marker onward and
   would silently destroy a block placed after it — selftest bite 6b asserts exactly this):

```css
:root,
[data-apollo-theme="mono"]{ --bento-dashboard-main:40px; --bento-dashboard-sub:4px; }
[data-apollo-theme="legacy"]{ --bento-dashboard-main:24px; --bento-dashboard-sub:4px; }
[data-apollo-theme="console"]{ --bento-dashboard-main:40px; --bento-dashboard-sub:4px; }
[data-apollo-theme="supercharge"]{ --bento-dashboard-main:24px; --bento-dashboard-sub:2px; }
```

`:root` carries **mono** because mono is the **base** theme (ADR-0011: one baseline library, four
themes as override sets) — a page naming no theme gets the baseline, as every other token does.
**Theme selection was read, not assumed**: `gen_theme_cascade.py`'s docstring and 1,635 occurrences in
`canon.css` establish `[data-apollo-theme="<theme>"]` on the root element as the selector, with
`[data-theme="light|dark"]` as the orthogonal mode axis.

**The consumer.** The snippet (:800–:816 after the edit) now declares
`--bento-gutter:var(--bento-dashboard-main,40px)` on the wall and
`var(--bento-dashboard-sub,4px)` on each group. **The snippet stays single-theme by convention** (the
meta's third `$awaitingDave` says so) and **the mono fallbacks are load-bearing, not leftovers**: no
snippet links `canon.css` (the meta's own probe: 0 of 137), so a standalone snippet document has no
theme block to read and must still render mono correctly. The **delivered** CSS is theme-aware.

**No ruled token VALUE was changed.** `--layout-bento-gutter` is untouched. Mono's `0` stays `0`.

---

## 2 · MONO'S 0, READ FROM SOURCE (not from any report)

- `knowledge/tokens/layout.json` → `layout/bento/gutter`: `"$value": "0px"`, `"$type": "dimension"`,
  `$note` *"MINTED s217-D2 — BASE tier (mono is the base theme; supercharge inherits it). ⚠ THE ONLY
  PER-THEME BENTO DIVERGENCE: legacy and console override this to 24px in their own override sets."*
- `knowledge/canon/canon.css:537` → `--layout-bento-gutter: 0;`
- measured live in all four themes (table below, rightmost column): mono **0**, supercharge **0**,
  legacy **24px**, console **24px**.

**s217-D2 is intact and says what the token says.** The `0` is a ruled value, not a defect.

---

## 3 · THE DISAGREEMENT — LEFT FOR DAVE, NO WINNER PICKED

Two **ruled** sources name the OUTER gutter of a dashboard-of-bentos, and they disagree per theme:

| source | mono | legacy | console | supercharge |
|---|---|---|---|---|
| (a) `--layout-bento-gutter` — s217-D2, via canon's own role rule `.c-bento[data-bento-role="dashboard"]:has(> .c-bento__grid > .c-bento){--bento-gutter:var(--layout-bento-gutter)}` (`canon/gen_canon_bento.py` `role_css`) | **0** | 24 | **24** | **0** |
| (b) `mainSpacing` — s219-D1 (5), via the rails manifest / `role_defaults_219.DEFAULTS` | **40** | 24 | **40** | **24** |

They agree **only on legacy**.

**This arm delivers from (b)**, because that is the source **s219-D3 names**: the rails manifest's own
`roles.dashboard.dials` are `mainSpacing · subSpacing · keylines · bentoBg · grouping`, and the
separate `spacing` dial is scoped `"types": ["display","gallery"]` — the dashboard grammar has no
`spacing` dial at all. The template's pre-existing literals were also (b)'s mono values.

⛔ **No winner is picked and none should be.** Dave's correction says outer and inner are two different
quantities, each deliberately per theme; it does **not** say which ruled token carries the outer one
for the **dashboard role**. **His doubt — *"I'm not sure that 0 is right for mono"* — is about exactly
this seam.** Filed as the fifth `$awaitingDave` entry on the template meta.

---

## 4 · THE FOUR-THEME PROOF (rendered DOM, chromium headless-shell, `goto("file://…")`)

Harness: `outputs/a288_probe.py` — builds four minimal pages per canon build, each
`<html data-apollo-theme="…" data-theme="light">` linking the real `canon.css`, body scoped
`.cn-template-dashboard-bento`, a `.tpl-wall` dashboard bento whose tiles are `.tpl-group` bentos.
**Declared** = `getComputedStyle(el).getPropertyValue('--bento-gutter')`.
**Measured** = `getComputedStyle(grid).columnGap` (canon: `.c-bento__grid{gap:var(--bento-gutter)}`).
Seat env: `knowledge/_render/seat_env.sh /var/tmp/render-env-288b` →
`SEAT_ENV: OK seat=trusting-youthful-franklin shell=…/headless_shell faces=10/404 farm=10/10 libs=2`.

### BEFORE (canon.css as it stood at the lane's open — the pinned literals)

| theme | OUTER declared | OUTER measured | INNER declared | INNER measured | `--layout-bento-gutter` |
|---|---|---|---|---|---|
| mono | 40px | **40px** | 4px | **4px** | 0 |
| legacy | 40px | **40px** ✗ | 4px | **4px** | 24px |
| console | 40px | **40px** ✗ | 4px | **4px** | 24px |
| supercharge | 40px | **40px** ✗ | 4px | **4px** ✗ | 0 |

⇒ **every theme got mono's 40/4.** The defect the #287 handoff names, reproduced.

### AFTER (this lane's build)

| theme | OUTER declared | OUTER measured | INNER declared | INNER measured | `--bento-dashboard-main` / `-sub` | `--layout-bento-gutter` |
|---|---|---|---|---|---|---|
| mono | 40px | **40px** | 4px | **4px** | 40px / 4px | 0 |
| legacy | 24px | **24px** | 4px | **4px** | 24px / 4px | 24px |
| console | 40px | **40px** | 4px | **4px** | 40px / 4px | 24px |
| supercharge | 24px | **24px** | 2px | **2px** | 24px / 2px | 0 |

**Declared equals measured in all eight readings, and every pair is its own theme's**, matching
`role_defaults_219.DEFAULTS["dashboard"]` exactly (mono 40/4 · legacy 24/4 · console 40/4 ·
supercharge 24/2) and the s219-D1 (5) ruling text *"main 24/40/40/24, sub 4/4/4/2 in
legacy/mono/console/supercharge order"*.

⚠ **NOT PROVEN HERE:** the four themes **side by side for Dave's eye** — that is #288 move 2's other
half and belongs to the review lane, not to this arm.

---

## 5 · FILES TOUCHED

| file | + | − | what |
|---|---|---|---|
| `knowledge/canon/gen_bento_role_vars.py` | +289 | 0 | **new** — the arm. `--check` / `--table` / `--selftest` (6 bites), help gate + pack gate |
| `knowledge/snippets/Template-dashboard-bento.reference.html` | +17 | −4 | the two pinned literals become `var(--bento-dashboard-main,40px)` / `var(--bento-dashboard-sub,4px)`, with the reasoning and Dave's words in the comment |
| `knowledge/canon/canon.css` | +54 | −4 | **REGENERATED, never hand-edited** — `gen_canon_components.py` (the snippet projection at `:18129` / `:18133`) then `gen_bento_role_vars.py` (the new block at `:21998`–`:22034`) |
| `knowledge/components/template-dashboard-bento.meta.json` | +3 | −1 | `$awaitingDave` 3 → 5, **by addition only**: entry 4 records what this build resolves, entry 5 is the new question for Dave. Nothing deleted, `$tokenGaps` untouched (neither gap is resolved by this build) |
| `knowledge/_state.json` | +19 | 0 | row `W-288a`, minted through `_state.add()` / `_state.save()` — **not hand-edited**. The file's own diff reads +38: lane M's `W-288m` is the other +19 |
| `outputs/a288_probe.py` | new | — | the proof harness (outputs, not the repo tree) |

---

## 6 · GATE VERDICTS, VERBATIM

**Green, in scope:**

```
snippet gate: 137 snippet(s), 0 failure(s)
```
```
gen_canon_components --check OK — 137 components in sync.
```
```
gen_bento_role_vars --check OK — 4 themes x 2 dials in sync with the rails manifest.
```
```
gen_bento_role_vars selftest OK — 6 bites
```
```
AUTO-BENTO in sync with tokens/layout.json (layout/bento).
```
```
gen_theme_cascade --check OK — 230 override path(s), 397 component projection(s) in sync.
```
```
gen_theme_cascade selftest OK
```
```
gen_canon_tokens --check OK — canon.css token layer is in sync (577 root vars, 195 dark overrides). Nothing written.
```
```
binds-resolve gate: 137 snippets (137 with manifests, 2321 vars) · 137 metas (116 binds addresses) · 137/137 canon blocks · 0 failure(s)
```
```
property-resolves gate (C2): 149 file(s), 0 failure(s)
```
```
_validate_radius: 0 strict fail(s), 0 advisory file(s) pending migration, 4 physical corner longhand(s) DECLARED ADVISORY (#221) -> _RADIUS-GATE.md
```
```
✅ pro-forma universal gate passed (11 tranche file(s)).
```
```
✅ CSS-governed gate passed (11 tranche file(s)).
```
```
✅ No-hardcode gate passed (11 tranche file(s)).
```
```
PASS — 0 failure(s), 61 declared deferral(s)          [_validate_dtcg.py]
```
```
LANE OWNERSHIP: OK — no staged path under another session's lane (this is #288).
```
```
theme-provenance gate (ADVISORY): 50 hardcoded foreign-theme hex(es) in 166 Mono files. See _THEME-PROVENANCE-GATE.md
```
```
_validate_help_gate --selftest OK — 5 bites (guard-first · write-before-guard RED · missing-guard RED · library · argparse).
```
```
⚠ ADVISORY: an unconsumed mint is not a defect on its face — a forgotten wire and a reserved slot wear
the same shape, and no presence test can tell them apart. Promotion to blocking needs a
$consumer/$reserved declaration on the token, and is Dave's word.          [_gate_minted_consumption.py]
```

**Red, and INHERITED — none of the named files is one this lane touched:**

```
GRID GATE FAIL — 6 off-grid layout value(s).
```
All six are `padding: 7px` — `canon.css` ×3 and the `Navigations` / `Sidebar-nav` / `Tab-bar`
snippets they are projected from. `Template-dashboard-bento` is **not named**.

```
GATE RED: 4 fork(s) not in the ledger
```
`--cell-py` (`.cn-table` / `.cn-data-grid`), `--status-negative` / `--status-neutral` /
`--status-positive` (`.cn-chart-bar` / `.cn-chart-sparkline`). No bento declaration is named; the new
`--bento-dashboard-*` block does not fork.

```
❌ knowledge/_bite_goal.py: NO HELP GATE — add          [_validate_help_gate.py, 15 files]
```
15 files, all pre-existing (3 in `knowledge/`, 7 `_render/verify_demo_slides_268*`, 5 under `_tmp/`).
`gen_bento_role_vars.py` is **not named** — it carries the guard as its first executable statement.

**A gate that refused, and what it was telling me:**

```
✖ REFUSED (write-gate): _gate_artefact_fresh.py WRITES FILES and was invoked with NO ARGUMENTS. It
would overwrite: (transiently) knowledge/_A11Y-GATE.md, _ADVISORY-SIGNALS.md,
_THEME-PROVENANCE-GATE.md, _INTEGRITY-REPORT.md, _INSTRUMENT-FIT.md + .json, _RADIUS-GATE.md,
_NO-HARDCODE-GATE.md — each restored.
  A bare run is not a stated intention (#158 write-by-default class, no-args leg).
```
Correct refusal, **not worked around**: a freshness sweep that rewrites seven report artefacts is not
this lane's to run in wave 1.

**Not run, and why:** `_validate_composition.py` / `_validate_screen.py` drive against *"a REAL bento
artefact (never the library)"* — this lane changed the library and no artefact. `_build_all.py` and the
three KG generators are fenced by the lane's standing rules.

---

## 7 · WHAT IS **NOT** DONE

1. **The outer-gutter disagreement is unresolved and is Dave's** — §3. Mono's `0` is untouched.
2. **The four themes have not been put side by side for his eye.** #288 move 2's other half.
3. **The template's own QUALITY is untouched** — that is #288 move 3, a separate lane by his word
   (*"we need to look at this too"*).
4. **Both `$tokenGaps` stand** (`layout/bento/columns` unitless, `layout/bento/packing` under
   `$extensions`). Neither is resolved by this build, so neither was annotated.
5. **The other two `$awaitingDave` entries stand** — the dark page ground, and whether a snippet may
   link `canon.css`. The `+8KB` duplicated-bento-rules measurement behind the second one is unchanged
   by this arm.
6. **`_bento_edit_rails.json` was not regenerated** — it was read, and found already in sync with
   `role_defaults_219.DEFAULTS` for all four themes (the cross-check is now a permanent refusal in the
   generator, so the next drift reds instead of silently winning).
7. **Nothing was committed.** No git write was intended by this lane; **one was attempted in error** —
   a `git stash push` used to A/B a gate against the pre-change tree. It created **no stash entry**
   (`git stash list` empty before and after) and **no file was altered by it**; the working tree was
   verified intact immediately afterwards (canon block, snippet vars, meta entries, new generator all
   present). Declared here rather than left for the commit lane to find.
8. **The `--layout-bento-gutter` role rule in `gen_canon_bento.py` was not changed.** It still governs
   display and gallery walls and the canon-level dashboard-of-bentos rule; the template's own
   declaration out-specifies it. Whether canon's rule should itself read the role default is part of
   the §3 question.
