# #282 lane LL — the logo review, landed

**2026-09-18 · opus · brief `notes/_lanes/282/logo-land/BRIEF.md` · evidence `notes/_lanes/282/logo-land/`**

Dave's own export of the #282 logo-review page came back at 08:20:48Z with nine answers, four
notes and three overrules. This lane inscribes that as `s282-D5`, merges the two logo
guidelines into one under Apollo's word, writes the three rules his notes contain, and binds
the eight logo nodes that had been leaves since #277.

## The merged guideline's shape

`knowledge/guidelines/logos.md` is now the ONE logo guideline. It was a 27-line 2025 sticker
sheet that said the standard lived on create.hsbc; it is now 156 lines that hold the standard
itself, in this order: front matter naming **both** sources and `s282-D5`, and a `precedence`
block carrying his sentence — "our decisions here over-ride the refresh" — as the rule for
reading the file; the eight-variant logo set (`s282-D4`) with the hexagon pair kept on his
q1 (a); the five-step height scale as a table (`s282-D3`, raw height, no viewBox); the three
new Apollo rules; the seven 2026-refresh bullets moved out of `brand-refresh-assets.md`; the
three 2025-page bullets moved out of `visual-assets.md`; the export receipt; cross-references.

Each moved block sits under a heading that names its vintage and where it came from, so the
boundary `bra26-003` protects is kept INSIDE the file rather than across two files. That is
the answer to the against-case the option sentence printed, and it is the only part of the
merge shape that was mine to pick.

**One correction to the brief, measured:** the brief says to move `va25-015`, `va25-016` and
`va25-017` "from `brand-refresh-assets.md`". They were never there — all three are in
`visual-assets.md` (`grep -n 'va25-01[567]' knowledge/guidelines/*.md`: 3 hits, all
`visual-assets.md`). They moved from where they actually were, and `visual-assets.md` got its
own one-line pointer as well as `brand-refresh-assets.md`.

**`va25-014` did NOT move.** It is the print clear space (1× hexagon height on all sides) and
the 105×20px minimum, the ruling names it as the print rule that stays the print rule, and the
brief's list did not include it. It stays in `visual-assets.md` under the original heading,
now the only bullet there.

## The three new rules, one line each

- **`logo26-008` — BLOCKING** — the masthead logo is the FULL-COLOUR Masterbrand on both
  grounds in all four themes (`masterbrand-light-colour` on light, `masterbrand-dark-colour`
  on dark; Legacy, Mono, Console, Supercharge alike — a monochrome theme does not make a
  monochrome masthead). `va25-015` says a Masterbrand is in every digital masthead; this names
  the variant. Ends `(s282-D5, Dave 2026-09-18)`.
- **`logo26-009` — BLOCKING** — digital clear space is a FLOOR, not the print rule: vertical
  clear space ≥ 0.25 × logo height, snapped UP to the 4px grid — 24 → 8 · 28 → 8 · 32 → 8 ·
  36 → 12 · 40 → 12 px; anything at or above the floor that sits on the 4px grid is legal. His
  note is quoted in the rule text in full. **The rule line states in its own words that his
  note says VERTICAL and that HORIZONTAL clear space is not stated and is not ruled.** Ends
  `(s282-D5, Dave 2026-09-18)`.
- **`logo26-010` — ADVISORY** — the hexagon alone on the nav-rail head, app tile and favicon,
  each conditional on Create Direct approval AND "HSBC" in view (`va25-016`'s two conditions
  carried through unrelaxed), AND in responsive layouts at the smaller sizes — tablet-portrait
  and mobile — where the Masterbrand does not fit. ADVISORY because every use is gated on a
  human approval this repo cannot check. This answers `s230-D2`'s declared residue: the 56px
  rail head now has a lockup that fits. Ends `(s282-D5, Dave 2026-09-18)`.

## What moved, and what survived the move

Ten rule bullets moved byte-for-byte. The move script asserts each block's exact bytes are
present in the new file before it writes anything, and asserts every `{#id}` survives:

| from | ids | to |
| --- | --- | --- |
| `brand-refresh-assets.md` § Logos (2026) | `logo26-001` … `logo26-007` | `logos.md` |
| `visual-assets.md` § Logos (2025 page…) | `va25-015`, `va25-016`, `va25-017` | `logos.md` |

`logo26-001`'s `(s282-D1, Dave 2026-09-17)` native-app clause is in the moved bytes unchanged.
Every destiny tag — including the non-indexed `[IN FORCE…]`, `[reference]`, `[structure]` and
`[composition-level rule…]` tags on `logo26-002`…`007` — moved untouched. Photography and
Creative Hexagons stayed in `brand-refresh-assets.md`; a one-line pointer sits where each moved
section was.

## What the generator did and did not pick up

`gen_kg_icons.py --land --ratified s277-D4` was RUN FIRST, as the brief directs, and **it did
not pick the new rules up.** Reading its source: `governedBy` is in its own `NULL_ONLY_TYPES`
tuple, and its only `governedBy` route is `declare(...)`, which always writes `t: null`.
`GOVERNS_LOGO_RX` is not an edge route at all — it counts rulings that name a logo `.svg` for
the declared-null `why` string. Measured, the regen changed **6 lines and drew 0 edges**: each
hexagon/mono `why` went from "holds 0 rules whose file is logos.md … 0 rulings name a logo
.svg" to "holds 7 … 8 rulings" (because the index now carries seven `logos.md` rules and
`s282-D5` names eight logo `.svg` files in `governs`). The edges stayed null.

So the twelve `governedBy` edges are **hand-authored**, in the same shape as the file's
existing edges, and that is flagged below. The generator's side effect on
`knowledge/_icon_nodes.json` was restored byte-identical from HEAD
(`git show HEAD:knowledge/_icon_nodes.json > knowledge/_icon_nodes.json`; md5
`8e8974f3d6542e96a75721815da2a535` both sides). The `notes/_lanes/277/icons-propose/` copies
were not touched by the run — `git status` on them is clean.

## COUNTS

| measure | before | after |
| --- | --- | --- |
| `_rulings.json` rulings | 617 | 618 (`s282-D5` last) |
| `_rulings.json` span | — | +33 lines / −0, 10,309 bytes inserted before the `\n ]\n}` tail |
| `_rules-index.json` count | 470 | 473 |
| `byDestiny` | ADVISORY 321 · BLOCKING 59 · REVIEW 34 · TASTE 56 | ADVISORY 322 · BLOCKING 61 · REVIEW 34 · TASTE 56 |
| rule ids LOST | — | **0** |
| rule ids NEW | — | 3 (`logo26-008`, `logo26-009`, `logo26-010`) |
| rule ids whose `file` MOVED | — | 4 (`logo26-001`, `va25-015`, `va25-016`, `va25-017` → `logos.md`) |
| rule `text` or `destiny` CHANGED | — | **0** |
| rules whose `file` is `logos.md` | 0 | 7 |
| `_logo_nodes.json` edges | 27 | 33 |
| `governedBy` edges | 6, all `t: null` | 12, all drawn |
| `_logo_nodes.json` `unresolved` | 9 | 3 (2 `defaultFor` + 1 `usesLogo`; the 6 `governedBy` resolved) |
| `_rule_nodes.json` nodes / edges | 521 / 709 | 525 / 712 |
| hand-authored `restsOn` lines | 75 | **75** (asserted before and after the edit) |
| explorer | v1.25 | v1.26 · nodes 4809 · edges 8630 · orphans 0 |
| explorer asset edges / SKIPPED | 1295 / 26 | 1307 / **14** (the pre-lane baseline) |
| **drawn edges out of the 8 `logo:` nodes, in the built HTML** | **0** | **12** |
| `logos.md` | 27 lines | 156 lines (+156 / −9) |
| anchors of the `<file>.md#<id>` form in the corpus | 0 outside the brief | 0 — nothing to fix |
| `_validate_kg.py` | OK | OK |
| `_validate_lane_ownership.py` | OK | OK (unchanged) |

Numstat of the working tree (gates' own appends excluded):

```
 6	 2  knowledge/_build_kg_explorer.py
107	51  knowledge/_logo_nodes.json
 67	 9  knowledge/_rule_nodes.json
 33	 0  knowledge/_rulings.json
 28	 7  knowledge/guidelines/_rules-index.json
  1	30  knowledge/guidelines/brand-refresh-assets.md
156	 9  knowledge/guidelines/logos.md
  2	17  knowledge/guidelines/visual-assets.md
  2	 2  notes/_KG-EXPLORER.html
```

The hexagons' `deg` was 1 before and 1 after — **`deg` is not the lit test**: their one edge
was a declared null, drawn by nothing. The measure above counts edges with both ends non-null
in the built HTML's own edge array, and it is 0 → 12.

## ruling-shaped

Four things were mine to choose and are named so he can overturn any of them.

1. **The twelve `governedBy` edges are hand-authored into a file that says "never hand-edit".**
   The ratified generator cannot draw them, and the brief authorises the fallback. The file's
   `$description` and its `edge_types.governedBy` now say so, name `s282-D5`, and name
   `notes/_lanes/282/logo-land/bind.py` as the thing to re-apply after any regeneration —
   because a regen WILL re-declare the nulls.
2. **`edge_types.governedBy` was widened from "DECLARED-NULL ONLY — never drawn, never
   resolved" to a drawn type, and `ASSET_DRAWN` in `_build_kg_explorer.py` with it (v1.26).**
   `s277-D7` made it null-only because nothing bound a lockup. `s282-D5` is the ruling that
   ends that condition, so the widening follows the ruling — but the wording `s277-D7` chose
   was his, and this changes it.
3. **`knowledge/_rule_nodes.json` was hand-edited** — four rules re-homed to `logos.md`, their
   `definedIn` re-pointed, an `artefact:` node minted for `logos.md`, and the three new rules
   added from the regenerated index verbatim. `gen_kg_rules.py` would do this and is banned for
   this lane (it deletes the 75 hand-authored `restsOn` lines). Without it the new rules have no
   node, the twelve edges point at nothing, and the explorer skips them — which is exactly what
   the first build measured (SKIPPED 26 vs the 14 baseline). The `restsOn` count is asserted
   intact at 75 on both sides.
4. **All four masterbrand nodes bind to both new masterbrand rules**, including the two mono
   variants, per the brief's "the 4 masterbrand nodes gain `governedBy` to the masthead-default
   and clear-space rules". `logo26-008` names the colour pair as the masthead lockup; binding
   the mono pair to it is how the rule reaches them with "mono is not the masthead lockup".
   Only the four hexagons bind to `logo26-010`; the masterbrands do not.

Two smaller ones: the merged file's **vintage labelling** (the answer to `bra26-003`'s
objection) and the **ADVISORY destiny on `logo26-010`** are the conductor's reading, not his
word — he ruled the permission, not its grade.

## UNPROVEN

- **Horizontal clear space has no figure and no check.** His note says vertical. `logo26-009`
  says so in its own text, this report says so, and `W-282ll` carries it. It is NOT completed
  by symmetry.
- **His "rule of thumb for digital is half of this dimension" is recorded, not inscribed.** He
  ruled a floor of 0.25, not a thumb of 0.5; the thumb appears only inside the quoted note.
- **No logo clear-space or size token exists.** The five heights and the five floors are prose
  in a guideline; nothing in `knowledge/tokens/` carries them and no gate checks a rendered
  logo against either. `s282-D3`'s per-size masters still do not exist.
- **No component enacts any of the three rules yet.** The Header/masthead component is still
  deferred; `app-shell-nav-rail` still has no logo in its snippet. `logo26-010` makes the rail
  head legal, it does not fill it — the `usesLogo` declared null on `app-shell-nav-rail`
  (`s230-D2`) is deliberately left standing, because rebinding a component is not this lane's.
- **The `logo26-002`…`logo26-007` bullets are not in `_rules-index.json`** and never were —
  their tags are the non-indexed `[IN FORCE…]` / `[reference]` / `[structure]` /
  `[composition-level rule…]` class. They moved, and they are still invisible to the index.
  That is the generator's contract, not a loss.

## REPLAY-THESE

- `python3 notes/_lanes/277/icons-propose/gen_kg_icons.py --land --ratified s277-D4` → **will
  re-declare the six `governedBy` nulls and delete the twelve edges.** Re-apply
  `notes/_lanes/282/logo-land/bind.py` after any run, then rebuild the explorer.
- `python3 knowledge/gen_kg_rules.py …` → would rebuild `_rule_nodes.json` correctly from the
  index (the four re-homes and the three new nodes come free) **and delete the 75 hand-authored
  `restsOn` lines.** Still the footgun lane RL named at #281. Not run.
- `python3 knowledge/guidelines/gen_rules_index.py` is idempotent here and safe to re-run.
- `python3 knowledge/_build_kg_explorer.py` is safe; the baked coordinates move because four
  dots and fifteen lines entered the layout.

## `_state.json`

- **`W-282lr` CLOSED** — `closed_by` = his export (`notes/_lanes/282/logo-review/DAVE-EXPORT-2026-09-18.json`,
  08:20:48.214Z, 12 rows, 9 answered, 4 notes, 3 overrules) **+ `s282-D5`**.
- **`W-282ll` OPENED**, owner dave, `closes_when` he rules on BOTH: whether the retired
  create.hsbc material is re-integrated as an AI-readable "Ask create" assistant (his q6
  forward note, filed by `s282-D5` as a carry and not enacted), and what the HORIZONTAL digital
  clear space is.

## Evidence

`notes/_lanes/282/logo-land/` — `inscribe.py` (the textual-span insert), `merge.py` (the
byte-for-byte move, with its own assertions), `bind.py` (the twelve edges), `rulenodes.py` (the
`_rule_nodes.json` hand-edit), `state.py`, `index-diff.json` (470→473, 0 lost, 4 moved, 0 text
changed), `edge-diff.json` (the generator verdict and the twelve edges), `logo_nodes.before.json`.
