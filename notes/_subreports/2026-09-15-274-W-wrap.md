# `#274`-`W` — the delegated capture ritual for "the line and the rules"

provenance: 274 · 2026-09-15
status: observed

session: `#274` · 2026-09-15
window: delegated wrap sub (OPUS 5), conductor Fable
sub index: `W`
brief: `_HANDOFF-125-the-line-and-the-rules.md` (the wrap brief was cut from it; no separate
`notes/_briefs/` file was written for this sub)
tokens: `UNMEASURED — a wrap sub cannot read its own conductor-side spend; the conductor may append
it at the next opener`

## VERDICT

**GREEN WITH FIVE DECLARED, INHERITED REFUSALS.** Every step of
`knowledge/_RUNBOOK-capture-ritual.md` — 1, 1b, 2, 2c, 2d, 2e, 2f, 2g, 3, 4, 4b, 4c, 4d, 5, 5b —
ran. Three blocking reds were HEALED by the ritual; five are carried in the `#243` form and every
one of them is another session's append-only testimony that this seat is forbidden to rewrite.

## COUNTS

COUNTS: findings 6 · ruling-shaped 6 · UNPROVEN 0

`capture gate [wrap]`: **212 in scope · 5 fail · 17 warn** (the five: boot-drift ceiling breach;
boot double-count #243, #264, #272, **#273**) · rulings **566 → 578** (`s274-D1`…`s274-D12`, all
inscribed by the session itself) · Memento index **2,185 records** · carries **459 → 467 items, 8
new, 1 struck, 1 corrected** · ★ LATEST banner **1,197 tape / 10 lines** against the `s241-D2` cap
of 1,200 / 10 · `_CHAIN.md` **9,311 tape** · commits **20 local and unpushed when this wrap opened,
21 after it**.

## WHAT RAN, WITH RECEIPTS

- **1** — `_LIVE-STATE.md` refreshed; `Last refreshed` stamped **2026-09-15 from `date`**; #270's
  `Previous:` chain segment (3,623 chars) moved VERBATIM to `_LIVE-STATE-ARCHIVE.md`
  § Rolled 2026-09-15 #274 (`notes/_lanes/274/W/lastrefreshed274.py`, assertions on both ends plus
  a span-reconstruction check). `_validate_standing_instructions.py`: **28 standing docs reachable,
  PASS**.
- **1b** — `_DECISION-HISTORY/2026-09-15-274-the-line-and-the-rules.md`, provenance + status fields
  present, both-way links to the spine and the ledger.
- **2 / 2c / 2d / 2f** — every move through `knowledge/_gm_move.py` from a session-owned, uniquely
  named ops file (`notes/_lanes/274/W/ops-274-rolls-1789482842.json`), asserted for existence and a
  size floor **in the writing process** before the mover read it. Receipts read back against the ops
  intended: #272 banner (12 ln) → `_GM-ARCHIVE.md` § Batch 2026-09-15 #274 · #271 ⏱ delta (24 ln) →
  `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-15 #274 · `roll_2f` #273 (24 ln → `notes/_GAUGE-LOG.md`
  at true EOF, 2 ln → `_GM-ARCHIVE.md`). `_roll_state.py` then reports **2c OK (banners 2/2) · 2d OK
  (deltas 3/3) · 2f OK (strata 1, log #273)**, and the gate's roll-claim check agrees with the
  measured tree.
- **2c (i)** — `_CARRIES.md` § `## residual → #275` written by ONE programmatic pass
  (`notes/_lanes/274/W/carry275.py`): **608 age brackets bumped**, **one strike** (#274's item ①,
  the four list-vs-card decisions, DISCHARGED by `s274-D1`…`s274-D5` at `04c51c6`/`58f56ef`) and
  **one correction by addition** (#274's item ②, whose clause *"`record-list` waits on LC-1"* went
  false — the carry is NOT discharged and rides on). Both carry their `s183-D1`/`s188-D2` receipts.
- **2e** — DO-FIRST unchanged this session; no new notice or tombstone was written, so no lifecycle
  tag was minted and nothing was retro-tagged. **Size of the skip: zero lines.**
- **2g** — `_build_memento_index.py` run **LAST**, after every GM/LS edit was final: **2,185
  records**.
- **3** — **SEAT LIMIT, DECLARED, NOT A SKIP:** the memory store is read-only from this seat by the
  brief. The hook body and its one `MEMORY.md` index line were written in the `s271-D4` form to
  `notes/_lanes/274/WRAP-MEMORY-HOOK.md` for the conductor to place.
- **4** — `s274-D1`…`s274-D12` were already inscribed in `knowledge/_rulings.json` by the session
  itself (578 total, verified at this seat); this wrap inscribed no ruling and decided nothing.
- **4b** — both names GENERATED by `knowledge/_gen_titles.py --session 274`, receipt written; the
  forward title is in `GOOD-MORNING.md` and the rename is delivered in chat only. Both match the
  brief's lines character-for-character.
- **4c** — `_gate_scratch_hygiene.py --clean`: this session's own `/tmp` entries removed; **825 MB
  owned by dead sessions reported as a total only**, unremovable from inside; `/sessions` **97%**.
- **4d** — `_render_rulings.py` rebuilt `notes/_RULINGS.html` (578 rulings, 150 sessions) and
  `--check` prints **FRESH**.
- **5 / 5b** — committed through `knowledge/_git_commit.sh` in the DECLARED not-a-wrap branch
  (`#243` form), subject read back before committing. **Not pushed — the push is Dave's word.**

## THE QUOTE GATE — IT REFUSED EVERYTHING, AND THE REFUSAL IS ABOUT THE INDEX

Nine sentences are presented as Dave's across `GOOD-MORNING.md`, `_LIVE-STATE.md`, `_CARRIES.md`,
the dossier and the memory hook. All nine were copied character-for-character from
`notes/_lanes/274/DAVE-RULINGS-2026-09-15.md` and then run through
`python3 knowledge/_quote_gate.py`: **0/9**, with word-level misses on *"actuality"*, *"prez"*,
*"egregious"* and *"'headers"*. ⛔ **This is #272's finding recurring unchanged — the index the gate
searches covers neither the `notes/_lanes/` files nor `_rulings.json`'s `says` field, so his words
are invisible to it on the day he speaks them.** The verbatim check was therefore made directly
against the lane file, exact substring: **10/10**, including two sentences the banner does not use.
The gate's blindness is recorded as a measurement, not treated as a pass.

## WHAT I GOT WRONG, OR NEARLY DID

- **The gate found a FOURTH boot double-count — #273's — and this wrap minted it by doing the right
  thing.** It appeared only when this ritual's legal `roll_2f` moved #273's stratum into
  `notes/_GAUGE-LOG.md`; the line that trips the arm is *their* `wrap-handover` line restating
  `boot 81,419`. Every record this wrap had already written said "four refusals, three
  double-counts" and all five surfaces were corrected before the commit. **The roll is correct, the
  arm is correct, and the repair is neither this seat's to make nor a reason not to roll.**
- **That correction pushed the ★ LATEST banner to 1,224 tape, over the `s241-D2` cap of 1,200.**
  Repaired by tightening **sixteen phrases of our own prose** to 1,197 — the last five to buy room for this report's own PATH on the banner, so the `s218-D7` citation check has something to find — no quote, no item, no carry,
  no declared skip and no receipt name was dropped to fit, which is the clause the cap cares about.
- **I verified before committing that this wrap's own stratum cannot mint a `#274` double-count** —
  `_capture_gate._parse_boot_samples` over the `#### 2026-09-15 #274` block returns exactly ONE row,
  `(274, 81,898)`, on the post-mortem line. #273's wrap wrote its figure a second time inside the
  `wrap-handover` line; this one does not.
- **The `size:` stamp and `_CHAIN.md` took six passes to converge**, because the stamp sits inside
  the chain slice (#240's declared shape: a wrap that shortens its banner can still hand the next
  session a bigger chain through its header). The figures written are the FINAL ones, all four
  mutually consistent at the last measurement: GM **35,364**, LS **62,451**, corpus **97,815**,
  chain **9,311**.
- **The `_gm_usage.py` section-sizes line had to be re-measured four times** for the same reason,
  and the figure on the record is the one taken after the stamp converged.
- **The wrap brief named seven commits; `git rev-list --count @{u}..HEAD` read 20 at this seat,**
  because `5685d31` (the handoff) landed after the brief was cut and #272's and #273's commits are
  still local. The repo-state claim is taken from `git`, never from the brief
  [[premise-ages-faster-than-rule]].
- **The showroom sync gate refused this wrap's commit for the second consecutive session and the gap
  is DECLARED, not closed.** `gen_showroom.py --check` reads **108 of 137 pages stale**; the payload
  diff is `:is(…)` → `:where(…)`, a CSS specificity change, one line per generated page, predating
  both #273 and #274. Landing a 108-page visual diff unreviewed at a wrap seat is what
  [[art-director-reviews-lane-output-268]] forbids. Committed with `SHOWROOM_ACK`.

## RULING-SHAPED QUESTIONS

1. **The `$description` on `knowledge/_rule_nodes.json` still reads "PROPOSED … NOT RATIFIED"** on a
   store that `s274-D7`…`s274-D12` ratified and `00f3a87` landed. It is **generator-owned text**, so
   the fence is one span at #275 and **not** a wrap-seat repair — the Chart-boxplot `yieldsTo`
   lesson running the other way. **Whose span, and whether the generator or the file is amended, is
   Dave's.**
2. **Lane LCE's coined vocabulary is now sitting on a ruled meta.** `bordered-per-record` and
   `$level: "typeset"` are the lane's words, not his. Whether they stand, or the model is expressed
   in his own terms, is **Dave's**.
3. **The 99 of 634 rule edges that draw only with two explorer layers enabled.** A reader with one
   layer on sees a graph 16% smaller than the store. Whether that is the intended default is
   **Dave's**; so is the chip word (`guidelinerules` vs "Guideline rules").
4. **The quote gate's corpus.** It cannot see `notes/_lanes/` or `says`, so it refuses his own
   sentences on the day he speaks them and a wrap must verify by hand. Repointing it is
   **ruling-shaped and Dave's** — and it is the second consecutive wrap to say so.
5. **The `#243` boot double-count is a GATE defect, not a record defect** (#269's separation), and
   it is now **four sessions wide** — #243 by mis-attribution, #264, #272 and #273 by real
   restatements inside their own append-only blocks. Whether the gate's attribution is fixed, or
   `BOOT_CEILING_TK` raised, is **Dave's word alone** — a wrap may not pay that price to unblock
   itself.
6. **When is the showroom regenerated, and by whom?** Unchanged from #273's question 4, and now two
   wraps old.

## REPLAY-THESE

REPLAY-THESE: none — the stub carries everything; the commands below are the gate replays, each under 1K tape.

```
python3 knowledge/_capture_gate.py --wrap
python3 knowledge/_roll_state.py
python3 knowledge/_gen_chain.py --check
python3 knowledge/_render_rulings.py --check
python3 knowledge/_build_memento_index.py
python3 knowledge/_parked.py --check
PYTHONPATH=knowledge python3 -c "import _capture_gate as c;print(len(c._carry_items([l for l in open('_CARRIES.md') if '#275:**' in l][0])))"
```
