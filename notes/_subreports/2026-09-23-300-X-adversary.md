# #300 · Lane X — the adversary on lanes A, B, C, D

provenance: 300 · 2026-09-23 · lane X, second launch (Opus 5.5 sub, cloud container + Dave's Mac through device_bash) · read-only on the repo apart from this report and `notes/_lanes/300/X/` · no Project-memory writes (memory_list ×2 pages, memory_read ×1 call of 3 files) · no git status, no commits
work files: `notes/_lanes/300/X/x_replay.py` (container-side transcript checks) · `drill_proxy.py` (the cold-seat drill, mechanical proxy) · `lookup_probe.py` (n-gram / BM25 look-up test)

## For Dave, in plain words

**Most of what the four lanes found holds up. Three things don't, and they change the plan.**

1. **The one-page start card is not enough on its own yet.** I took 18 things the last handoff and chain tell a fresh chat and checked which ones each start-up read contains. The card carries 4. The card plus the chain carry 12. The handoff plus the chain carry all 18. The ones the card drops are the ones that stop a fresh chat breaking things: how to push, what to do with a stuck lock, which paths to leave alone, and your rulings behind the owed questions. **So keep the handoff and the chain at the start for now.** That is lane C's version 2, and it still saves about 50,000 tokens a session. Move to the card only after a fresh-chat quiz passes (the drill is below).
2. **Moving the conductor to Claude Code on your Mac is still the biggest saving, but it is not a quick switch.** Claude Code would read a July instruction file in the repo (AGENTS.md). It points a fresh chat at 311 KB of files and at rules you have since changed. The render setup only runs in the Linux sandbox, and 17 scripts assume the sandbox's folder names. It belongs after Friday, and only after AGENTS.md is rewritten.
3. **Stopping the conductor mid-run costs more than it looks.** At 16:56 you stopped the chat to add your n-gram question. The two lanes then running were thrown away (about 22 minutes of work), and the four lane summaries dropped out of the conductor's memory, so it had to go back and re-read them. Your 16:08 and 16:16 notes, typed while it worked, got through without stopping anything.

**Your two questions.**
- **16:08, balancing against the context the agents need.** Your principle holds. The quiz is how to keep it: a cut only replaces a read after a fresh chat passes the quiz with it.
- **16:56, n-grams, Jev and the graph.** A better look-up helps, but it cannot replace the start-of-chat read. Today's search finds 2 of the 18 answers. With the handoff and chain added to the index and a smarter n-gram ranking (BM25), it finds 11, and the best mix finds 13. Jev could choose which extra sections to fetch. But you ruled it a tool for development, never a dependency, and changes wait until after Friday.

**Safe before Friday:** paste lane C's version-2 instructions. Make no memory writes while a chat is live. Don't stop the conductor to add a note; just type it.

**Next step:** P folds these verdicts into the plan page. Anything marked FALSE below does not go on the page as a recommendation.

---

## 1. Verdicts at a glance

| # | claim (lane) | verdict | the receipt, in one line |
|---|---|---|---|
| A1 | Boot 126,185 = prefix 96,179 (tools + system) + first turn 30,006 | **HOLDS** | transcript row 26 usage; msg 2 `cache_read` 126,183 = msg 1 whole input |
| A2 | Ratios: tools ×0.983, prose ×1.453 | **HOLDS** (independent check) | 6 sub first-turns (my script): real/tape 1.397–1.403 on prose; the re-send gives 1.43 |
| A3 | Thinking is retained in full | **HOLDS**, across user turns too | row 128 boundary: `cache_read` 187,861 = the whole prior input, which holds turn 1's thinking. If thinking had been stripped, the cached prefix would break at ~126K |
| A4 | Re-send byte-identical; the version triggers it, not the content | **HOLDS** | sha256 `8388cdd7538824d9` rows 18 and 130; version `e53a7626…`→`e87e7f1c…` (64-hex server ETag); CLI fetch sends `If-None-Match` per main-thread turn (`/opt/claude-code/bin/claude` @200302861) |
| A5 | "The listing is alphabetical and shows the first 150 files" | **FALSE** (second half) | displayed sorted, yes. But the first 150 by path overlap only 87 of the 150 shown (D's census, my script). D's "opaque" stands |
| A6 | Path prefix 28 tape × 150 = 4,200 | **HOLDS** | my count: 4,136 tape removed when the prefix is stripped in place |
| A7 | Conductor's retained outputs 56,311 at msg 17 | **HOLDS** | hand sum of output_tokens msgs 1–16 = 56,311 |
| A8 | Desktop vs cloud: +40K tools, +19K memory, −7.7K prompt | **HOLDS, caveated** | the #294 arithmetic leaves no room for a 19K listing. But the CLI strips laptop-leg snapshots from transcripts (`I4e`), and `_HANDOFF-129:23` recorded a "cloud listing ≈8K" at #278, so the desktop memory term is not proven zero |
| B1 | Thinking "does not appear to be carried" | **FALSE** | see A3. B's re-send check counted msg 13's output as its visible text (1,882 tape) instead of the 4,220 output tokens that re-enter; with those, 1.43 fits |
| B2 | Claude Code on the Mac boots ~30K | **WEAKENED** | the saving stands as a projection (the core, 20.1K, was measured in print/API-key mode only). Missed: stale `AGENTS.md` auto-loads (§3.2) · 17 `knowledge/*.py` hardcode `/sessions/` or `mnt/Projects--UX-design` · render env is aarch64-Linux + `TMPDIR=/dev/shm` · the gate's band arm trips on a large drop (§3.4) |
| B3 | Tool access "On demand" could save up to 33.5K | **WEAKENED** (expected ≈0) | S5 (read 2026-09-23): "Connectors aren't loaded until Claude searches…", scoped to connectors, "Your selection only applies to that conversation". Chrome, computer use and browser are already deferred at boot; widgets, memory and the bridge are not connectors. Still a free test |
| B4 | Pause memory: −31.7K at boot, account-wide | **HOLDS**, with a sequencing conflict | S1 (read 2026-09-23): "won't use memory or make new memories"; "Each project has its own separate memory space" (no per-project switch quoted). It removes the tools that C's wrap placement and D's export and delete need |
| B5 | `read_key()` misses the bare-key line, so the gauge path is dead | **FALSE** (the parsing half) | `_gauge_tokens.py:406–417` returns any line starting `sk-ant-`; `API-KEY.txt` line 4 is one. The 401 half is not re-tested (it would spend Dave's key) |
| B6 | Local Cowork is gone for this account | **HOLDS as sourced** | S2 re-read; the S4 re-fetch hit a 429. #296–#300 all ran in the cloud container |
| B7 | Every cloud sub boots at ~66.3K | **HOLDS** | mine 66,373 · P 66,398 · first X 66,818 |
| B8 | origin URL embeds a credential | **HOLDS** | `.git/config` url carries a 93-char `github_pat_` token (masked). **No leak found**: 0 full-token hits in 5 flagged repo files and 0 in every #300 transcript |
| C1 | Opener cost 81,049 = 61,678 + 19,371 re-send | **HOLDS** | fills match my row dump exactly |
| C2 | v2 saves ~50.6K per session, nothing built | **HOLDS** for the conductor's window | the items v2 drops carry no unique context (§4). Plan usage moves to the wrap's memory sub (~66K boot + ~15–21K) |
| C3 | "Folder miss" costs 2,653 | **WEAKENED** | the first call's wasted listing is 446 tape. The 2,653 step is mostly the 80-line repo listing (1,300 tape). v2 still removes both |
| C4 | `_checkin.py` dead since #296; rehearse rows 0 on 09-22 and 09-23 | **HOLDS** | glob `:84`, exit `:87–97`; my count: rehearse 41 (09-21), 0, 0; wrap-open 32, 25; GRADE-DECISIONS last row 2026-09-21T21:41:57 |
| C5 | `BOOT_FIRSTTURN_TK` deleted; only `BOOT_CEILING_TK = 72_768` | **HOLDS** | `_gauge_tokens.py:281, 351, 885–886` |
| C6 | Runbook's "only the conductor can write the store" is false in the cloud | **HOLDS** | `_HANDOFF-150:60`, `notes/_lanes/299/WRAP-BRIEF.md:10` |
| C7 | Handoffs and chain are not in the Memento index | **HOLDS** | 14 kinds in `_memento-index.json`, no handoff or chain kind (`gm-section` ×20 holds the chain's source) |
| C8 | v3-A (card + chain) ~142K / v3-B (card only) ~131K | **v3-A WEAKENED · v3-B FALSE as "sufficient"** | drill proxy: card 4/18, card + chain 12/18 (§5) |
| C9 | Rollback file is byte-exact | **HOLDS** | the Project instructions in transcript row 19 plus a trailing newline give md5 `a6e11674…` = C's file |
| C10 | The wrap gate's ceiling arm stays RED whatever the opener does | **HOLDS, and sharper** | settings levers (at best ~90–95K) all stay over 72,768. Only the seat move clears it, and that trips the band arm (§3.4) |
| D1 | Census 250 = 234 Apollo + 16 account | **HOLDS** | my own two-page `memory_list` |
| D2 | ~200 imported notes were never committed | **HOLDS** | 0 of 12 random imported basenames in 9,461 distinct paths of `git log --all` (my command), no on-disk twin by name. The original Cowork folder on the Mac is outside the mount, so it is unprovable either way |
| D3 | Ratio 1.531–1.634; snapshot 20.4–21.7K real | **WEAKENED** | it counted msg 13's output as visible tape (910 + 972) instead of the 4,220 output tokens that re-enter. The true figure is ~1.43, so the snapshot is ~19.0–19.4K real |
| D4 | Cut to 5 files saves 18.9–20.2K per boot | **WEAKENED** in size | (13,296 − 953) × 1.43 ≈ 17.7K. Direction holds |
| D5 | Natural experiment: no store write means no re-send | **HOLDS** (n=1) | the 16:56 human turn (row 198) carried no snapshot. Newest store update 14:48:39 (my `memory_list`) |
| D6 | Account files would not fill the listing | **HOLDS** | 16 files, 14 unlisted |
| D7 | The 18 newest wrap notes are hidden from the boot list | **HOLDS** | shown wraps stop at `wrap-281` |

**The brief's own premises:**
- **"Cloud is the source" — WEAKENED.** At Dave's 184,309 (msg 11) the fill splits three ways. About 74.7K is harness the desktop seat also paid (40%). About 51.5K is added by the cloud boot (28%). About 58.1K is our own opener (32%). After msg 13 the largest single item is the conductor's own retained thinking (21,985 on one message). The cloud is the biggest step change. It is not most of the bill.
- **"Store writes trigger the re-send" — HOLDS, with a wider trigger.** The client asks the server every main-thread turn with `If-None-Match: <version>` and re-attaches on any new version (bundle @200302861–200304220: 200 = new, 304 = unchanged, 204 = none). The version is a server ETag over something wider than the rendered text: the bodies changed and the text did not. **Inference:** writes from Dave's other chats to account files that share the snapshot (profile, preferences) would also re-send into a live Apollo chat. `/people/son.md` was written from another chat on 09-17.
- **"prompt_snapshot equals what is billed" — HOLDS at boot, WEAKENED for the running band.** The transcript stores thinking as a summary plus an encrypted signature. For example, msg 14 billed 21,985 output tokens, but its visible thinking is 6,800 tape. Across the session, ~58% of the conductor's output tokens are not visible text. Any tape rebuild of the running band must use `output_tokens` (A did).

## 2. The re-measurements, with my own scripts

**Thinking retention (settles A against B).**
- `x_replay.py` §1 prints, for every human turn, whether the cache prefix survived.
- At Dave's #300 message: prev fill 187,863 → next `cache_read` 187,861, prefix kept. Turn 1's thinking blocks (msgs 1–12) sit inside that prefix, so they were not stripped.
- The delta +23,591 = 4,220 (msg 13's full output, re-entering 1:1 as within-turn steps show: msg 15 +22,715 against msg 14's 21,985 + ~670) + 19,371 for the snapshot and Dave's words, so 13,528 tape × 1.432.
- B's "thinking not carried" and D's 1.53–1.63 ratio both came from counting msg 13's output as its visible transcript text instead of that 4,220.

**The prose ratio, a third seat.**
- Six sub first-turns (mine, P, first X, B, C, D): real/tape 1.397 · 1.397 · 1.398 · 1.400 · 1.400 · 1.403.
- The tape here is my JSON-walked text, not a byte-exact render, so read ≈1.40–1.45.
- That agrees with A's 1.453 and the re-send's 1.43. It does not agree with D's 1.53–1.63.

**Which 150 are listed.**
- My script over D's census: the first 150 by path overlap only 87 of the 150 shown.
- Other overlaps: oldest 150 by update 89, newest 150 by update 127, smallest 150 94, largest 150 99.
- By update time the pattern is: the oldest 61 hidden, a long shown run, then a mix among recent files. No simple rule fits. **D's "opaque" HOLDS; A's "first 150" is FALSE.**

**Census and the natural experiment.**
- My `memory_list` (two pages) shows 250 = 234 + 16. The newest update is `index.md` at 14:48:39Z, the conductor's own opener writes.
- Rows 128–219 carry exactly one `cowork_memory_context` (row 130). The 16:56 human turn carries none.

## 3. Missed dependencies and externalities (new)

### 3.1 An interrupt discards the in-flight turn, including sub stubs (RUNNING band, Memento risk)
- **Measured** (`x_replay.py` §3). Message `uVpTXJ` had fill 256,949 and had just read the four lane stubs, delivered as queued commands, rows 170–173. Its thinking says: "All four lanes are back now, and Dave dropped in two mid-turn notes".
- Dave stopped the chat at 15:56:22Z (rows 189–191, "[Request interrupted by user for tool use]").
- The next message `QwntDv` read the cache only to 250,517, the end of `ygtHie`'s input, and its fill FELL to 253,280 although a turn was added.
- The conductor's own words at `QwntDv`: "The four lanes returned, but their actual reports were delivered via SubagentHandback rather than appearing directly in my context". It then re-read the stubs from its transcript, and a triple print cost it ~7K of window.
- **Mechanism (code, inference on the path):** the bundle carries a truncating resume, `--resume-session-at` with `--resume-drops-turn` ("declare the prompt uuid of the turn the truncating resume intends to discard", @101380598).
- **Externality:** the first X and P were rejected by the same stop and never filed. Hand sums: X 47 msgs, 9,474,298 input processed, 76,725 output. P 48 msgs, 7,676,037 and 38,588. Both were relaunched.
- **Lessons for P:**
  - Dave's queued notes (16:08, 16:16) reached the conductor without a stop.
  - Stubs must be re-read from `notes/_subreports/`, never trusted to stay in the window.
  - A stop kills running lanes.

### 3.2 The seat move auto-loads a stale operating manual (B2's missed dependency)
- The repo has `AGENTS.md` (7,270 B, last commit 2026-07-19) and no `CLAUDE.md`.
- Claude Code "reads `AGENTS.md` only when you have no `CLAUDE.md`… At session start" (code.claude.com/docs/en/memory.md, read 2026-09-23; v2.1.277+).
- Its "How to work" tells a cold agent to start at `GOOD-MORNING.md` (113,222 B), then `_LIVE-STATE.md` (197,832 B). It also carries the July rules "Dave only clicks + pushes" and `mcp__cowork__allow_cowork_file_delete`.
- The chain itself says: "Do NOT now open `GOOD-MORNING.md`". **A Claude Code conductor would boot with instructions that contradict the chain.** That is the Memento failure in its purest form.
- **Precondition for any seat trial:** write a `CLAUDE.md` (it overrides `AGENTS.md`) or rewrite `AGENTS.md` first.

### 3.3 The seat move's porting bill
- The render recipe: `ensure_env.sh` builds aarch64-Linux Chromium, and `seat_env.sh` derives the seat from `/sessions/<seat>/mnt/` and defaults `TMPDIR=/dev/shm`. macOS has no `/dev/shm`.
- 17 `knowledge/*.py` hardcode `/sessions/` or `mnt/Projects--UX-design` (grep count).
- `_checkin.py`'s glob.
- In its favour: `_git_commit.sh` already handles BSD `date` (`:187–190`) and self-heals tiktoken (`:443–453`).
- **Not before Friday.** The deck's rebuild goes through the render env.

### 3.4 The wrap gate grades a boot DROP as a step change
- `_capture_gate.py:5095` is `if abs(delta) <= red_line: return`, so the band arm is symmetric. Today's band is 89,443 ±26,087 (n=7, C's read-only run), and red is ±52,174.
- A Claude Code boot of ~30K sits about −59K from the mean, so it fails "STEP CHANGE" until a `#111-D1` declaration with matching figures is written to `_GAUGE-LOG.md`.
- Conversely, the ceiling arm clears only when every post-breach reading sits under 72,768 (`:5018`, `s244-D1`). That is the seat move and nothing else. Pausing memory (~94K) or any other settings lever leaves `--wrap` refused.
- **For P:** the seat move is the only lever that ends the declared not-a-wrap path, and it costs one declaration.

### 3.5 Pause memory collides with the other memory moves
- C's wrap placement and D's export and delete both need the memory tools. Pausing (account-wide, S1) stops memory "being used".
- The order must be: export (D) → verify → decide → only then pause or cut. Never pause first.
- The Friday strand note (`areas/presentation-friday-25th-288.md`, 4,144 B) would be unreachable while paused. It is mostly twinned: 6 of 8 distinctive phrases were found in `notes/_lanes/285–299`, the handoffs, GM, LS and decision history ("Consistency/All" and "catalogue went from 36 to 134" were not).
- **Dave's client work** loses memory for the whole pause (B named this).

### 3.6 The dream pass's memory input is already dead
- `.claude/agents/dreamer.md:27` makes "`MEMORY.md` memory index" its spine. `_HANDOFF-129` says there has been no `MEMORY.md` since #278.
- `_gardener.py` grades the repo mirror and calls the store unreachable (`:189–222`).
- So retiring or cutting the store breaks nothing in the repo's dream machinery. The claude.ai dream-pass skill's text was not read: UNSOURCED.
- `list_triggers` returns no scheduled tasks, so no scheduled session inherits a Project-instructions change.

### 3.7 Smaller ones
- **The ending chat pays the re-send.** The #300 conductor is at 262,384 (msg `gdCEvb`). A wrap-seat placement followed by one more message from Dave lands ~19.4K on a window already past 256K. Place the hook after Dave's last words, or skip placement until after Friday. The repo mirror is what the gardener reads.
- **ADR-0007** ("Project memory as a temporal decision-graph") is the repo's decision graph with `_LIVE-STATE` as the spine. It is not the claude.ai store (its first lines), so D's question is answered.

## 4. Dave's 16:08 principle — what each lever takes from a cold seat

| lever | context a cold seat loses | confident-false-claim risk | verdict under 16:08 |
|---|---|---|---|
| No memory writes in a live chat (hook at the wrap, or none) | none. The hook's content is the handoff's | none | **safe** |
| Drop `index.md` + `memory_list` + archive reads at the opener (C v2, D option B) | the index's three wrap lines. I checked the #299 line's facts against `_HANDOFF-150`: build_c.py, never build_e.py, rail labels, never git status, `--wrap` strands the lock, the handoff name are all present. The rest of the index is the ritual's own bookkeeping ("THE CUT WAS PAID…") | none found | **safe** |
| Card replaces the handoff (C v3-B) | 14 of 18 drill facts (§5) | **high**: the push method (the `--push` arm runs `git status`), the lock recovery, dirty-by-declaration paths, "the demo brief comes last", "he ruled only on 09" | **not safe until the drill passes** |
| Card + chain, no handoff (C v3-A) | 6 of 18: commit prerequisites, lock recovery, rail labels, dirty paths, `_state.add`, the brief source | medium: all six are "don't break it" facts | **safe only if the card carries the handoff's ⛔ cold-seat section verbatim** (699 tape; lifts the card over the 1,500 cap) |
| Pause memory | the 150 lesson titles at boot, the Friday note (mostly twinned) | low for Apollo | **safe for context; unsafe for his other work** |
| Cut the store to ~5 | ~0.7 MB of imported notes that exist nowhere else | medium until exported and indexed | **safe only after export + indexing** |
| Seat move | Project memory, Project instructions | **high**: a stale `AGENTS.md` loads at boot (§3.2) | **not safe until AGENTS.md or CLAUDE.md is rewritten** |
| Thinking and writing offloaded to subs | the conductor's own synthesis; stubs can vanish (§3.1) | medium | **safe with stubs re-read from disk** |
| Tool access On demand; Chrome and computer use off | nothing for Apollo | none | **safe** (small or zero saving) |

**The drill — "the cold-card drill".**
1. **Seat:** a fresh sub (or a fresh chat) given only the lean opener's payload: the card, or the card and chain. It gets the fetch commands the card names, and no handoff.
2. **Quiz:** the 18 questions in `notes/_lanes/300/X/drill_proxy.py`, plus 6 action probes:
   - "push the wrap commit";
   - "the build fails on `index.lock`";
   - "Dave asks for the demo brief, draft it?";
   - "tidy `notes/_dream/_GRADE-DECISIONS.jsonl`";
   - "rebuild the deck";
   - "which lanes' reports exist for #300?".
3. **Grading:** each answer is one of correct · fetched-then-correct (allowed; count the fetch tokens) · "don't know" (allowed) · **confident-wrong (FAIL)**.
4. **Pass bar:** 0 confident-wrong, at least 22 of 24 correct after fetches, and fetch cost ≤ the handoff read it replaces (~7.4K real).
5. **Machinery:** reuse `knowledge/_recall_probe.py`'s hash design. Plant sha256(salt + answer) from the full handoff and chain; the cold seat answers; `--check` grades. The answers then never sit on disk in plain text.
6. **Runs:** 3 seats before adopting v3, and again after any change to the card generator.
7. **Today's mechanical bound** (`drill_proxy.py`): card 4/18 · chain 11/18 · card + chain 12/18 · handoff 16/18 · handoff + chain 18/18. Q1–Q14 come from the handoff and Q15–Q18 from the chain, which is a bias toward the handoff that the model drill removes.

## 5. Dave's 16:56 ideas — n-gram, BM25, the KG, Jev

**Measured look-up test** (`lookup_probe.py`): 18 cold-seat questions in natural words, and whether a top-3 hit contains the answer.

| look-up | hit@3 |
|---|--:|
| today's Memento door (`_search_core`: substring match, crude stem, count score, **no IDF**) over the index as it is | **2 / 18** |
| the same ranker, with the newest handoff + `_CHAIN.md` cut into `##` sections and added | 8 / 18 |
| **BM25, unigrams + bigrams**, over the same | **11 / 18** |
| either of the two (union) | 13 / 18 |

Reading:
- Coverage is the first fix: the handoffs and chain are not indexed. The ranker is the second.
- Even the best mix misses 5 (the −27 ruling, the push method, never-build_e, the chain ruling, the 415 count). **A look-up complements the start-of-chat read; it cannot replace it.** A seat that does not know a fact exists will not ask for it.
- The 9 s run is pure-python BM25 over 2,324 records on the mount. Building it at the wrap into the index makes a query cheap.

What each door costs and breaks:
- **BM25 / n-gram ranking.** Cost: stdlib only; a sidecar script avoids the verbatim set. Editing `_search_core.py` or `_memento_search.py` instead needs the two `memento-package/` copies (`_validate_package_delta.py`). Break risk: rankings shift for every door that shares `_search_core` (the DS door `_consult.py` too), so re-run `--selftest` on both.
- **`_quote_gate.py`** (word-n-gram exact phrase + nearest bigram, ~0.1 s, advisory). The right tool for one new card-gate leg: every line the card quotes as Dave's must be FOUND verbatim. That catches the card paraphrasing a ruling.
- **`_near_dupes.py`** (8-shingle Jaccard). The handoff and chain share only 9% of the handoff's 8-gram shingles (13% at 5-grams, my measure). The two opener reads are mostly not duplicates, so cutting either one loses unique content. That backs Dave's "the chain is fundamental".
- **The KG.** `_decision-graph.json` (102 nodes, 172 edges, ~11.8K tokens, from the J7 note) could give rulings stable ids that a card cites instead of paraphrasing. But the KG explorer is part of Friday's demo (J7 note: "the demo's show-stopper on the 25th"). Nothing touches it before Friday.
- **Jev.** The repo already holds the design: `notes/_lanes/293/J7-IDEA-jev-selects-over-the-kg.md`. It is a two-hop selector: a skeleton in, one Choice over ≤255 ids, then full text for the chosen ids. Applied here, it would be one Choice over the ~12 handoff and chain section ids plus the top BM25 refs, given Dave's first message.
  - **Standing rulings** (memory `areas/jev-integration.md`, read-only): "Jev is a dev-time instrument only, never a dependency"; "build time may depend on Jev, run time may not"; "any changes with impact wait until after the presentations and demos".
  - ⇒ Jev may **add** sections to a fixed guard set as an optional re-ranker that degrades to the fixed opener without a key or network. It may **never remove** a guard fact. An oracle's wrong pick is a silent context loss.
  - Cost: `.env.local` key, the seat's egress to TypeSafe, 1–2 calls a session, a 64K context bound.
  - After Friday.

## 6. For P — what may and may not go on the page

**FALSE — not a recommendation:** "the card alone is a sufficient opener" (v3-B) until the drill passes · "thinking is not carried" · "`read_key()` cannot read the key" · "the listing is the first 150 files".

**WEAKENED — on the page only with the caveat:**
- the seat move (AGENTS.md, porting bill, band-arm declaration; after Friday);
- Tool access On demand (expect ≈0; free test);
- D's real figures (use ×1.43: snapshot ~19K, cut-to-5 ~17.7K);
- v3-A (card must carry the ⛔ section verbatim);
- "cloud is the source" (28% of the 184K).

**Before Friday, surviving X** (minutes, reversible, cannot touch the deck):
1. Paste C's v2 Project instructions. The rollback is md5-verified.
2. No store writes in a live chat. Place the #300 hook after Dave's last words, or not until after Friday.
3. To add a note mid-run, type it; don't press stop.
4. Optional: one throwaway chat with Tool access = On demand, to read its boot.

**Not before Friday:** pause memory (account-wide; blocks the export and wrap placement) · deleting store files (permanent; export first) · the seat move · any look-up, KG or Jev build · v3.

## 7. REPLAY-THESE

- **Container** (the transcript lives there; copy the script from the mount first): `python3 notes/_lanes/300/X/x_replay.py`. It prints:
  - prefix kept 187,861 at Dave's turn;
  - the interrupt fill 256,949 → 253,280, prefix broken at 250,517;
  - snapshot sha `8388cdd7538824d9` ×2;
  - billed output 68,293 against 72,007 chars of visible thinking.
- **Mac, repo root:** `python3 notes/_lanes/300/X/drill_proxy.py` → card 4 · handoff 16 · chain 11 · card + chain 12 of 18.
- **Mac:** `python3 notes/_lanes/300/X/lookup_probe.py` → hit@3 2 · 8 · 11 of 18.
- **Mac, gate symmetry:** `sed -n '5094,5096p' knowledge/_capture_gate.py`.
- **Mac, AGENTS.md staleness:** `git --no-optional-locks log -1 --format=%ci -- AGENTS.md; sed -n '44,50p' AGENTS.md`.
- **Mac, read_key:** `sed -n '406,417p' knowledge/_gauge_tokens.py`.

## 8. Spend

Hand sum of `…/d75f51cc-…/subagents/agent-ae6c882974bd851ae.jsonl` (this lane) per distinct `message.id`, at filing:
- 73 assistant messages; boot 66,373.
- Last fill 354,601. It is high because I read the four reports whole and one full `memory_list`.
- Cumulative input processed 17.69M, almost all cache reads.
- Cache writes 297,027; output 114,840.
- A few messages follow for the hand-back.
