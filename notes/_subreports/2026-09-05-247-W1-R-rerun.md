# `#247`-`W1-R` — DENSITY re-run: the frozen prompt on lane D's library, blind then sighted

session: `#247` · 2026-09-05
window: `W1 density · lane R (the re-run)`
sub index: `W1-R`
brief: `notes/_briefs/2026-09-05-247-W1-density-brief.md` (RE-RUN + judge-folder sections) over `notes/_briefs/2026-09-05-246-lane-B-baseline-brief.md` (the protocol, verbatim)
tokens: `UNMEASURED — subagent cannot read message.usage; estimate ≈230K real (≈60 tool calls, two 1440-wide PNG reads, one 300-line snippet read, four review/plan text extractions). Context at the arm-A→arm-B seam: ≈90K.`

## VERDICT

DONE. Both arms built and frozen (A `80beca7e…`, B `bfa25da3…`), measured with one instrument that was also re-run on the #246 pages. **The edit moved the sighted page's first chart from 837.8 → 678.6px at 1440 light (−159.2px — identical to lane D's snippet delta 675.7 → 516.5), put all six KPI tiles and the whole chart canvas above a 900px fold (baseline: four tiles, cut canvas), and seed 1 (the 2×2 board) does not recur.** Richness on the sighted arm is unchanged at 2/2/0/2 (baseline B 2/2/0/2): density re-packs the same component set, it adds none. Seeds 2–6 fail exactly as on the baseline; each is a later wave's. Arm B gates green (screen exit 0, receipt exit 0). The blind arm REGRESSED (chart y 1554.4, richness 1/1/0/2) for a reason unrelated to the edit: the blind splice carried the Tabs snippet's demo width and the wall collapsed to one column — a blind-variance finding, not a density finding. Score sheet `notes/_dp-scores/w1.json`: met 8 · missed 18 · n/a 3. Review page `reviews/W1-DENSITY-2026-09-05-v1.html`. Judge folder prepared with a randomised key. Nothing under knowledge/ apollo-spider/ showroom/ edited; `_SCREEN-GATE.md` restored (diff 0). Nothing of Dave's ruled.

COUNTS: findings `9` · ruling-shaped `5` · UNPROVEN `3`

## What was done

**Arm A — blind** (`outputs/w1-density/arm-A-blind/`): `brief.md` (six grill-me questions skipped/defaulted, Mono announced, bento-first asked) at `outputs/w1-density/brief.md`; the page assembled by `_assemble.py` from byte ranges of the template (with lane D's GROUP 1 as shipped: six compact tiles L852-936), Chart-line, Data-grid, Filter-toolbar-bar, Tabs; `READ-LOG.md` (every file, range, copied/adapted, sha12 per script block); `USED-MISSING.md` (10 gaps). DECLARED protocol control: the baseline's READ-LOG/USED-MISSING (in `outputs/`, not fenced) were read for the reading PATH and the baseline page grepped for its SKELETON (tab names, section order) so the composition matches and the template edit is the only variable; no baseline content/CSS/JS read or copied. Frozen `FROZEN.sha256`. No render, gate or validator before freezing.

**Seam check-in** between arms: arm A frozen; context ≈90K; `git status` showed the same four pre-existing `M` files (rulings, the lane-D snippet, rehearsal log, grade decisions) — none mine.

**Arm B — sighted** (`outputs/w1-density/arm-B-sighted/`): it0 = arm A bytes; three iterations logged in `ITERATION-LOG.md` (it1 three visible defects; it2 the baseline's canon.css composition switch + receipt; it3 logo path). Final gates `gate-it3-screen.txt` exit 0, `gate-it3-receipt.txt` exit 0. Frozen.

**Measurement 1–7, both arms** via `outputs/w1-density/_render_arm.py`: renders `render-{1440,820}-{light,dark}.png` + `render-1440-light-payments-USD-sorted.png`; `probe-render.json` (first-chart group/svg y, lead h, tiles/cols/rows, fold facts, fonts, errors); `drive-render.json` (nav, tabs, sort, filter, persistence, period seg, legend, table, tooltip, FX chart, kpi-cta); `outputs/w1-density/measure-inventory.json` (inventory, script sha-identity, hex counts); gates on arm A `gate-screen.txt` exit 1 / `gate-receipt.txt` exit 1 (blind page, NO-RECEIPT — as baseline A). The SAME script re-run on the #246 pages: `outputs/w1-density-baseline-reprobe/arm-{A,B}/probe-reprobe.json` (baseline folder untouched).

**Deliverables**: `notes/_dp-scores/w1.json` (29 rows + 6 seeds + richness per arm + first-chart-y + conflict ledger, 2 rows) · `reviews/W1-DENSITY-2026-09-05-v1.html` (swiss idiom, CSS copied from DASHBOARD-PRINCIPLES; baseline beside W1 at 1440 light as data URIs; fold table; seeds; richness; the 29-row sheet generated from the JSON by `outputs/w1-density/_build_review.py`; CONFLICT LEDGER; findings; RSQs) · judge folder `outputs/w1-density/judge/` — `x.png` = baseline sighted, `y.png` = W1 sighted (random seed 84664, flip=False), key in `KEY.md`; the judge is not told.

**Gate side effect**: `_validate_screen.py` wrote `knowledge/_SCREEN-GATE.md` (restored via `git show HEAD:` after the last run — `git diff HEAD -- knowledge/_SCREEN-GATE.md | wc -l` = 0) and `knowledge/_screen-gate/dashboard.md` (untracked, existed before this lane's first `git status`; left as found). `_build_all.py` not run. No commit, no push.

## Findings

1. **−159.2px on the sighted page = −159.2px on the snippet.** Chart group top 837.8 → 678.6 at 1440 light (`probe-render.json` 1440-light vs `…baseline-reprobe/arm-B/probe-reprobe.json`). 820 light: 1273.4 → 1109.6. The composition above the wall did not change between waves, so the edit passed through intact.
2. **Fold facts**: KPI tiles above a 900px fold 4 → 6; chart svg bottom visible false → true; lead group 309.7 → 150.5px (same probe keys `aboveFold`, `lead.h`).
3. **Seed 1 passes; richness does not move** — 2/2/0/2 both waves on the sighted arm (`w1.json` richness facts). Instrument A scores the component set; the set is byte-identical (script sha12s in `READ-LOG.md`, `measure-inventory.json`).
4. **Blind-arm variance exceeds the edit.** Arm A: cols=1 rows=6, chart y 1554.4 (baseline blind 953.8). Cause: `Tabs.reference.html` L80 + L120 demo width/cap carried blind; the #246 blind builder wrote a `.tpl-tabs .tabs{width:100%}` harness line and this one did not. One harness line is worth more than the W1 edit on the blind arm.
5. **Protocol copy-range defect corrected**: toolbar CSS L34-251 (baseline log) leaves the `@container (max-width:400px)` open at L251; the Tabs CSS nested inside it and never applied (first it1 render: bare tabs). L252 closes it. (`ITERATION-LOG.md` it1.)
6. **DP-08**: 4/6 tiles carry value + signed delta + named period; 2/6 (page L283, L309; snippet L899, L925) carry count/status — specimen content, lane D's RSQ 4.
7. **DP-22 loudest on the fuller screen**: two falling-is-good metrics wear red (L270, L296); six coloured deltas visible at once, ten signal elements above the fold (probe `signalsAboveFold`).
8. **Relabel miss**: marker aria-labels keep "Current/Savings/Investments" (drive `chart_tooltip` "A · Current · Jan: 12") while legend/table/title say EUR/USD/GBP — both arms.
9. **Narrow-band cost** carried from lane D (520px: +287px) — ledger row 2, not fixed here.

## RULING-SHAPED QUESTIONS

1. **Does W1 count as "moved"?** The fold moved (−159px, +2 tiles, whole canvas); instrument A did not (2/2/0/2 → 2/2/0/2). (a) moved — the fold is the finding · (b) did not move — richness is the instrument · (c) add a fold row to instrument A for W2+. Not recommended either way; the instruments are Dave's.
2. **The two status tiles in the headline row** (DP-08 missed on them): (a) leave as specimens · (b) signed delta vs a period · (c) demote to Stat cards — lane D's RSQ 4, re-asked.
3. **Six or four** — lane D's RSQ 1 stands; new fact: the judge's blind verdict at `outputs/w1-density/judge/verdict.md`.
4. **Blind-arm harness variance** (finding 4): (a) put Tabs' demo width (L80) and panel cap (L120) under a DEMO CHROME marker so a blind splice cuts it, a snippet edit · (b) leave, W7 measures it · (c) a rule-2 line "drop every snippet's demo width". Recommend (a) — same class as the demo-bar the bento template already fences.
5. **The 520px cost** — lane D's RSQ 2 stands.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** tile/chart heights under the real HSBC face — sandbox has no Univers Next; the DELTA is face-independent, the absolute px are not (lane D's caveat, inherited). Price: one render on a host with the font (~3K).
- **UNPROVEN:** the first it1 render (bare tabs, before the L252 fix) — the PNG was overwritten by the corrected it1 render; the probe line survives only in the session transcript. Price: re-run `_assemble.py` + range L34-251 (~2K).
- **UNPROVEN:** whether the judge's reading matches the probe — `verdict.md` is lane J's and had not returned when this report was filed; the key is appended by R after J returns (not yet).
- **CLAIMED:** richness 2/2/0/2 for the baseline arms — from the W1 brief and the #246 plan table, not re-scored here; the baseline pages were re-PROBED (y, fold) but not re-driven.

## Evidence

`outputs/w1-density/` — `brief.md` · `arm-A-blind/` (dashboard.html, _assemble.py, READ-LOG.md, USED-MISSING.md, FROZEN.sha256, render-*.png, probe-render.json, drive-render.json, gate-*.txt) · `arm-B-sighted/` (dashboard.html, _assemble_b.py, ITERATION-LOG.md, FROZEN.sha256, render-it{0,1,2}-*.png, render-*.png, probe-render*.json, drive-render.json, gate-it{0..3}-*.txt) · `_render_arm.py` (the instrument) · `_build_review.py` · `measure-inventory.json` · `judge/{x,y}.png, KEY.md` · `template/` (lane D's). `outputs/w1-density-baseline-reprobe/arm-{A,B}/probe-reprobe.json` — the same instrument on the #246 pages. `notes/_dp-scores/w1.json`. `reviews/W1-DENSITY-2026-09-05-v1.html`.

REPLAY-THESE: `outputs/w1-density/arm-B-sighted/render-1440-light.png` (~1.5K tk) · `notes/_dp-scores/w1.json` (~4K tk)

## Addendum (full-page captures)
- Re-rendered `outputs/w1-density/arm-B-sighted/dashboard.html` and `arm-A-blind/dashboard.html` at 1440 light with `full_page=True` (viewport PNGs untouched): `arm-B-sighted/render-1440-light-full.png` 1440×1157 · `arm-A-blind/render-1440-light-full.png` 1440×2719; the baseline `outputs/baseline-246/arm-B-sighted/render-1440-light.png` was already full-page at 1440×1316.
- Fold-marked copies (1px line at y=900, #DB0011 @60%, "fold 900" label right edge, Pillow): `arm-B-sighted/render-1440-light-full-fold.png` · `arm-A-blind/render-1440-light-full-fold.png` · `outputs/w1-density/judge/baseline-1440-light-full-fold.png` (baseline-246/ not written).
- `reviews/W1-DENSITY-2026-09-05-v1.html`: the three side-by-side data-URI images swapped for the fold-marked full-page versions, alts unchanged, one sentence added ("Both captures are full-page; the red line is the 900px fold."); page 773 KB → 1000 KB.
