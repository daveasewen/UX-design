# `#265`-W — the #265 capture ritual, delegated; steps 1→5 with step 3 an ANNOUNCED seat limit

provenance: 265 · 2026-09-09
status: observed

session: `#265` · 2026-09-09
window: conductor **Fable** (`Apollo - #265: the /goal bite-test, and the four ungraded glyphs`) + 4 Opus lanes + 2 blind drivers
sub index: `W` (wrap)
brief: IN-CHAT — no file under `notes/_briefs/` or `notes/_lanes/` for #265's wrap; declared rather than invented
tokens: `UNMEASURED — a sub cannot read its own message.usage from inside itself; _checkin.py at this seat reads the CONDUCTOR's transcript, which is what the s214-D5 sub-cut term is for.`

## VERDICT

**DONE.** `_CARRIES.md` § `residual → #266` written with SEVEN new items and TWO strikes (one of
them a HALF strike); ritual steps 1, 1b, 2, 2c, 2d, 2e, 2f, 2g, 4, 4b, 4c, 4d and 5 ran in order
and through the named scripts; both title lines were GENERATED by `_gen_titles.py --session 265`;
the ★ LATEST banner measures **exactly 1,200 tape across 9 substantive lines** against `s241-D2`'s
cap of 1,200 / 10. ⛔ **NOTHING WAS PUSHED — `s133-D2` makes the push Dave's word and he is
asleep**; the session's six commits plus this wrap's stand LOCAL. **Step 3 (memory) is a SEAT
LIMIT, DECLARED NOT SKIPPED, for the FIFTH consecutive delegated wrap — and the SECOND at which the
brief announced it in front and the conductor wrote the hook before delegating.** ⛔ The `--wrap`
gate carries **one recorded refusal**, and it is not this session's doing: the `s241-D2` S5
boot-double-count arm fails on **#264's rolled stratum**, which states a first-turn figure twice.
**This seat ruled nothing, graded no glyph, and answered none of the thirteen lane ruling-shaped
questions.**

COUNTS: findings `6` · ruling-shaped `0` · UNPROVEN `3`

## What was done

- **Carries (`s225-D2`).** `notes/_lanes/265/W/carry266.py` built `_CARRIES.md` §
  `## residual → #266` from § `## residual → #265` in ONE programmatic pass: **536 age brackets
  bumped** (4 of them `NEW — 0` → `1`), then **2 surgical edits, each asserted UNIQUE before it was
  made**, then **7 new items written in front**. Probe **392 → 396**
  (`_capture_gate._carry_items()`, the repo's own definition).
- **Step 1.** `_LIVE-STATE.md` — a new `Last refreshed` summary for #265, #264's demoted to
  `Previous:`; `_validate_standing_instructions.py` PASS (28 standing docs reachable).
- **Step 1b.** `_DECISION-HISTORY/2026-09-09-265-goal-bite.md` — six sections plus resolved/open
  state, provenance and status lines mechanical.
- **Steps 2 · 2c · 2d · 2e · 2f · 2g.** New ★ LATEST banner (**1,200 tape / 9 substantive lines**);
  the **#263** banner rolled to `_GM-ARCHIVE.md` § Batch 2026-09-09 #265 (12 ln); the **#262** ⏱
  delta (18 ln) **and its 5,474-character `Previous:` chain segment** rolled VERBATIM to
  `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-09 #265; `roll_2f` split the `#### 2026-09-09 #264`
  stratum (20 ln → `notes/_GAUGE-LOG.md`, 2 ln → `_GM-ARCHIVE.md`); 2e rolled **0 dated strata**
  (DO-FIRST 10,140 real / 78 raw lines, inside the band); the retrieval index rebuilt LAST (2,132
  records). **Every move went through `knowledge/_gm_move.py` with session-owned, uniquely-named
  ops files under `notes/_lanes/265/W/`, each asserted to exist with a size floor in the writing
  process before the mover read it.**
- **Steps 4 · 4b · 4c · 4d.** Rulings `s265-D1` and `s265-D2` read back at entries 440–441 (store
  439 → 441), both `by` Dave · `date` 2026-09-09 · `status` ruled; both titles GENERATED by
  `_gen_titles.py --session 265` with its receipt written; `_gate_scratch_hygiene.py --clean`
  removed this session's own `/tmp` scratch (0M owned by dead sessions); `_render_rulings.py`
  re-rendered `notes/_RULINGS.html` — the rehearsal had found it **STALE** — and `--check` printed
  **FRESH** at sha256 `e2bd1b3f…`.
- **Step 5.** One commit in the **#243 form**, with the gate's single refusal RECORDED rather than
  routed around. **No push.**

## Findings

1. **The `s241-D2` S5 boot-double-count arm fails on #264's stratum, and the roll is what exposed
   it.** Probe: the gate names `notes/_GAUGE-LOG.md` line 3206 (#264's POST-MORTEM) and line 3208
   (#264's `wrap-handover` line, which carries `boot 69,955`). ⛔ **The check is scoped to
   `_GAUGE-LOG.md` only, so a stratum sitting in `GOOD-MORNING.md` is ungraded — the defect was
   invisible for a whole session and became a blocking failure the moment 2f moved it.**
   ★ **This wrap did NOT edit #264's ratified block** (that is a rewrite of an append-only
   measurements record, and Dave is asleep); it **records the refusal** and **stops the class at
   its own stratum instead** — `#265`'s `wrap-handover` line no longer carries a boot figure and
   its `pre-flight` line says the reading is stated ONCE, in the POST-MORTEM line. What to do about
   #264's block is Dave's.
2. **The wrap gate's B2 PLAN BLOCK arm failed on a preposition.** Probe: `CHAIN_STOP_RE` is
   `stop line\s*\**\s*([\d][\d,]*)`, and the ⏱ delta as first written said *"delegated stop line
   **of** 180,000"* — so `STOP` did not resolve from state and the wrap was refused. ⛔ **The gate
   is right and the prose was wrong**: the next seam's plan block is rendered from the chain, and
   an un-parseable stop figure is an un-renderable block. Fixed by dropping the `of`; re-run GREEN.
   ★ **A number in the read chain is a FIELD as well as a sentence** — worth knowing before the
   next wrap words that clause freely.
3. **The `s214-D5` hand-over is MEASURED for a second consecutive wrap, and this one is small.**
   brief-cut **147,425 real** (conductor, declared) · sub-cut **148,308 real** (first-hand
   `_checkin.py`, 47 turns) · **delta 883 real** · replay unobservable (conductor-side, post-wrap).
   #264's delta was 6,898; the field now has n≥2 with a spread, which is what the `s214-D4` re-base
   is staged on.
4. **This delegation has a cause the stop line does not describe, and it is written as one.** The
   conductor cut at **147,425 real — 32,575 UNDER the `s260-D2` line of 180,000** and 52,575 under
   the working ceiling. The wrap is delegated because **Dave went to bed** and the conductor's seat
   is held for his read-back. ⛔ Every prior delegated wrap in this dataset was delegated at or over
   the line; recording a NEW cause rather than filing it under the old one is the point.
5. **Boot 70,260 real, MEASURED, is a BREACH — and the eight-reading clean run ends here.** #264
   read 69,955 (45 under). ⛔ `s240-D2`/`s241-D1` make the 70,000 literal **shrink-only**: the
   remedy is to cut the boot, raising it is Dave's word alone, and it is not a price a wrap may pay
   to unblock itself. The rehearsal at this seat's opener also named the standing ceiling refusal
   on **#255's 70,127**, unchanged.
6. **The carry probe counts 396 where seven items were added, and the cause is the #264 finding
   repeating.** Probe: `_capture_gate._AGE_RE` is `\[(\d+)(?:\s*[,—-][^\]]*)?\]` — it needs a digit
   immediately after `[`, so a `[NEW — 0, DAVE'S]` bracket carries no age and the segment it heads
   is not counted as a carry. ⛔ **Consequence, declared rather than discovered: the probeable count
   moved 392 → 396 for SEVEN new items, and the `residual → #266` line's own item ① is invisible to
   the counter** — which is also why a pairing pass over this wrap's two strikes reports only ONE
   unpaired. Not repaired here: changing the count's definition mid-wrap would silently move a
   number three surfaces quote.

## RULING-SHAPED QUESTIONS

**None from this seat.** Everything ruling-shaped in #265 belongs to Dave and is CARRIED, not asked
here: the four ungraded `-active` glyphs and `global-money`; the **thirteen lane ruling-shaped
questions** (B:2 · C:5 · D:3 · E:3), each filed with priced options and a recommendation; the two
`_PROPOSED-263.html` defects; `DECLARED_EDGE_TYPES`, which fences `gen_kg_edges.py`; whether a gate
report should be refused at commit when its own tree ≠ HEAD (#263, age 2); #241's midnight-wrap
question (age 23); what ritual step 3 should say about a delegated seat, now with five datapoints;
and, new at this wrap, what to do about #264's double-counted boot reading in `notes/_GAUGE-LOG.md`.
The DO-NOT-RULE list was obeyed in full: no glyph graded, no lane RSQ answered, no ruling added to
the two already inscribed, no push.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** `python3 knowledge/_capture_gate.py --selftest` was not re-driven whole — it
  exceeds the sandbox call wall, the fifth consecutive wrap to declare it. Lane B's new arm was
  driven in isolation and both touched files compile; end-to-end greenness of the selftest chain
  with the new arm in it is **not** established from this seat. Price to prove: one CI run of the
  `gates` job.
- **UNPROVEN:** the full `knowledge/_tests/test_gates.py` suite was not run at this seat.
- **CLAIMED:** every figure describing what happened BEFORE this seat opened — the six commits'
  contents, the lanes' gate counts (5 UNKNOWN → 0, 16 → 14, 13 → 5), the 441-line regeneration
  diff, the browser measurements, the two `/goal` grades and the six sub token figures — is copied
  off the five filed reports and the wrap brief, not re-derived by re-running the lanes' probes.

## Evidence

`notes/_lanes/265/W/` — the session-owned working set this wrap ran from and through:
`carry266.py` + `carry266-new.txt` (the carry pass), `build_ops.py` and the nine `ops-265-*.json`
mover ops files (every roll, insert and replace, with their stdout receipts above),
`banner265.txt`, `delta265.txt`, `new265.txt` (the `Last refreshed` summary), `stratum265.txt`,
`seg262.txt` (the chain segment as it was moved) and `_lr264.txt` (the pre-edit `Last refreshed`
line). ⛔ Nothing was written to session scratch.

REPLAY-THESE: none — this file and the ★ LATEST banner carry everything the conductor needs to
close #265. The carried items are in `_CARRIES.md` § `residual → #266` (`carries:residual-266`,
probe 396); the narrative is in `_DECISION-HISTORY/2026-09-09-265-goal-bite.md`.
