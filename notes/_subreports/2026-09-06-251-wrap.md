# #251 — the delegated capture ritual (wrap sub, Opus 5)

provenance: 251 · 2026-09-06
status: observed

VERDICT: **RITUAL COMPLETE, STEPS 1 → 5b, WITH ONE GATE REFUSAL NAMED, A SECOND LEGAL DISCHARGE
DECLINED ON PURPOSE, AND ONE EXIT-CHECK CATCH.** `s251-D1` and `s251-D2` read back at this seat
(store **373**, 371 → 373, both parsing); the `--wrap` form REFUSED by the ceiling arm for the
**fifth** wrap running; the commit made in the #243/#244/#248/#249/#250 form and **PUSHED**.

COUNTS: findings `7` · ruling-shaped `4` · UNPROVEN `2`

REPLAY-THESE: `PYTHONPATH=knowledge python3 -c "import _capture_gate as c;print(c.boot_constant_drift_check('.')[0])"` (~1K tk — the one fail, and the `s244-D1` discharge it offers) · `PYTHONPATH=knowledge python3 -c "import _capture_gate as c;print(len(c._carry_items([l for l in open('_CARRIES.md') if '#252:**' in l][0])))"` (~200 tk — the carry count) · `git log --oneline -2` (~200 tk — the subject read back) · `knowledge/_tmp/wrap251/` (the four scripts this wrap ran; NOT committed, `.gitignore:79`)

---

## What ran

| step | receipt |
|---|---|
| **1** | `_LIVE-STATE.md` — new ⏱ LATEST DELTA #251 (13 ln), #250 demoted to ⏱ PRIOR, `Last refreshed` rebuilt (23,250 → 25,013 chars); `_validate_standing_instructions.py` PASS (28 standing docs) |
| **1b** | `_DECISION-HISTORY/2026-09-06-251-strip-orients-card-details.md` — the WHY/HOW, eight sections |
| **2** | `GOOD-MORNING.md` — ★ LATEST #251 banner, `TITLE THE NEXT CHAT` rewritten, `size:` stamp written LAST |
| **2c** | EXIT CHECK on the rolling **#249** banner (12 ln, 1 marker line, **276 of 276** carry items probed by name against `_CARRIES.md` § residual → #251) → moved verbatim to `_GM-ARCHIVE.md` § Batch 2026-09-06 #251, via `_gm_move.py` |
| **2d** | EXIT CHECK on the rolling **#248** delta (14 ln, 6 marker lines) — ⛔ **it BIT** (finding 1) — → moved verbatim to `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-06 #251 with its **5,222-char** `Previous:` refresh-chain segment and a dated trim note left in the spine |
| **2e** | **0 dated strata** in `## ⬛ DO THIS FIRST` (**110 raw lines** measured, DOFIRST:10140 real) — nothing to roll, size declared |
| **2f** | `roll_2f` #250 → **20 ln** to `notes/_GAUGE-LOG.md` (appended at true EOF) + **2 ln** to `_GM-ARCHIVE.md`; new `#### 2026-09-06 #251` stratum written (21 ln) + `COMMIT STATE #251` |
| **2g** | `_build_memento_index.py` rebuilt LAST, after every GM/LS edit was final |
| **3** | **0 bytes at this seat** — the brief assigns step 3 to the CONDUCTOR |
| **4** | `s251-D1`/`s251-D2` READ BACK (not re-inscribed); seven `_state` rows minted (**W-455…W-461**) |
| **4b** | `_gen_titles.py --session 251` — both lines generated |
| **4c** | `_gate_scratch_hygiene.py` run |
| **5 / 5b** | commit in the #243 form, subject read back from `git log`, **pushed** |

## Findings

**1 — The 2d EXIT CHECK bit, and what it caught had survived ten sessions without a home.**
The rolling **#248** delta's *STILL HIS* line carried **the midnight-wrap stamp** — #241's
ruling-shaped question about what a wrap spanning midnight should stamp, recurred at #248, age
**10**. `grep` finds it **0 times** in `_CARRIES.md § residual → #251`. It lived ONLY on a rolling
delta, and that delta rolled at this wrap. It was **copied up into § residual → #252 at its TRUE
age `[10]`**, not a fabricated `[NEW — 0]` — the age is the part a cold reader cannot reconstruct
and the part that makes the carry alarming. ★ This is the 07-24 chart-banner shape exactly, and it
is the check's whole purpose: dated homes do not count.

**2 — The gate offered a LEGAL way to quiet one third of the ceiling arm, and this seat declined
it in writing.** `boot_constant_drift_check` returns **1 fail** (#247 70,822 · #248 70,974 · #249
71,113 over the 70,000 ceiling) **and** names `s244-D1`'s discharge-by-declaration for **breach
#249**, pre-filled with its own computed figures: *"breach #249 71,113 · ceiling 70,000 ·
post-breach readings #250 69,293 all under"*. It was **NOT written**, for two stated reasons: the
ceiling arm is fenced to Dave in the #251 brief's STILL-DAVE'S list, and discharging #249 leaves
#247 and #248 breaching so the arm stays RED regardless — the only thing gained would be a line
this seat had no authority to write. ⛔ **An available discharge that is declined must be
DECLARED, or the next reader cannot tell refusal from oversight.**

**3 — The brief's push premise HELD, and was re-driven rather than copied.** #250's lesson was a
stale premise in the wrap's own brief. Probed here first: `git status -sb` reads `##
master...origin/master` with no ahead/behind and `git log --oneline -1` reads **`fc1209b`**. Dave's
push landed, so the **eight-deep unpushed backlog carried since #244 is gone**; what remains owed is
the **CI verdict**, for `fc1209b` and for this wrap's commit. The carry was corrected in place with
its `s188-D2` receipt rather than struck — the subject changed, the item did not close.

**4 — The `s241-D2` banner cap was met by writing shorter, and the shortening is on the record.**
The first draft ran **1,484 tape across 11 substantive lines** against a cap of **1,200 / 10**. Two
bullets were MERGED (the invented fact into the build bullet; the push and the lock into the gauge
bullet), five bullets were tightened phrase by phrase, and one of `s251-D1`'s three verbatim quotes
was left to the store with a pointer in its place. Final: **1,199 tape / 10 lines** — the line limit
met EXACTLY, margin 1 on tape. ⛔ Not one item, carry, declared skip, receipt name, path or figure
left the record; what left the banner is in the ⏱ LATEST DELTA, which the cap's own text names as
its home.

**5 — Four of the five NEW verifier findings are the same class, and the class has a name now.**
NEW 21 (the strip→card link), NEW 23 (an over-declared `consumes`), NEW 24 (a ragged row) and
`V-MUT-5` before them are all **relationships**, and this repo gates **quantities**. Bytes, counts
and physics fail loudly; "this points at that", "this is what that declares" and "no row is ragged"
fail silently or not at all. Each is a DEFINITION question before it is a build question, which is
why all four are carried to Dave rather than built here.

**6 — The doc-row gate's five FAILs were repairs, not edits.** `--check` was RED at the opener on
all five #251 lane documents (two briefs, three reports). Seven rows were minted — **W-455…W-461**,
the two lane briefs, the three lane reports, this wrap's brief and this wrap's report — and **not a
byte of any report was touched** (ADR-0017 / `s192-D1`). `_state.add()` refused `W-461` until this
file existed, which is the refusal working as designed.

**7 — The two dirty jsonl files were inspected, not assumed.** `git diff` shows
`notes/_REHEARSAL-LOG.jsonl` **+3 lines** and `notes/_dream/_GRADE-DECISIONS.jsonl` **+3 lines**,
all appends, all dated 2026-09-06, all written by this session's own `_checkin.py` runs (two
`rehearse` rows naming the stale index and the ceiling breach; three B3 sidecar `alert` rows). They
are **today's own tool output and are committed**, not declared as strays. The untracked
`notes/_subreports/assets/2026-09-06-251-V-dp08-verifier/` (**176 KB, 12 files** — `vr.py`/`vr.json`,
`drive.py`, `adva.py`, `advc.py`, `fb.py`, five PNGs) was inspected and **committed as evidence**
under `s218-D7` clause 3, because scratch dies with the window and a report whose evidence has
evaporated is a claim.

## RULING-SHAPED QUESTIONS

1. **The DP-20 carrier budget against 26/9.** The build ADDED carriers because the strip's chips are
   carriers, and the canon page is shorter than the specimen, so the fold reads denser. Three is the
   budget. What counts as a carrier, and how many the fold may hold, is the budget's own definition
   and no gate fails on it. HIS.
2. **A guard for the strip→card link (`NEW 21`).** Building it changes what the gate REFUSES, and
   the thing it would refuse is the subject of `s251-D1`. That makes it his, not a wrap's.
3. **A definition of a ragged row, so `s249-D5` can be gated (`NEW 24`).** *"No ragged layouts"* is
   his own wording and has no mechanical consumer. A gate needs a threshold — a trailing-row fill
   fraction, a maximum void in px — and a threshold is a ruling.
4. **The ceiling arm, and specifically whether `s244-D1`'s discharge should be written for breach
   #249.** The gate says it is legal and pre-fills the figures; two consecutive readings (#250
   69,293, #251 69,321) are now under. This seat declined (finding 2). Whether it is written, and
   whether #247/#248 get the same treatment as further under-readings accumulate, is his.

## UNPROVEN

1. **This seat's own token spend.** A sub cannot read its own `message.usage`; declared unobservable
   rather than estimated.
2. **The memory write.** Ritual step 3 is the conductor's by the brief's own words; the store is
   invisible to the shell and to every gate, so this seat records **0 bytes written here** and
   verifies nothing (the D1a honest scope).
