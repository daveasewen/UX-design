# 2026-09-07 · #253 · LANE B — the six interchangeability fields on the 13 non-chart-bar chart metas

- **Lane:** B (builder), Apollo session #253, model opus.
- **Brief:** author `provides · answers · shape · span · priority · when` (+ `not-with` where the meta's own prose demands one) on every `knowledge/components/*chart*.meta.json` carrying `intent`, minus `chart-bar`; one swiss review page; targeted gates only; no commit.
- **Rulings leaned on:** s251-D3…D11 (the DESK eight), s251-D5 (`when` is a predicate, field list unruled), s251-D6 (`not-with` slug required, reciprocals derived), s251-D10 (12-col span canon), s251-D15 (Claude authors / Dave vetoes), s252-D1 (roles.json membership adopted; its priority/`when` ILLUSTRATIVE, the metas become canon), s218-D7 (this filing shape).
- **Adopted this morning and honoured here:** R2 (six fields authored as ONE object per meta, then cross-checked; every `when` names the sibling it beats or yields to, quoting that sibling's roles.json `when`) and R3 (one-hop by hand before authoring: roles.json siblings + the meta's own `edges.mustNotNeighbour`, both cited per meta on the page).

## VERDICT

**GREEN.** 13 metas found, 13 authored, four targeted gates green over those 13, review page built with every count computed from the metas by script. Nothing committed (conductor commits the wave). Nothing ruled.

`COUNTS: metas 13 · authored 13 · shape values 11 · when fields 12 · gates GREEN`

Supplementary counts, same script: `not-with` edges 7 across 6 metas · span bands 6 · addresses resolved by the validator for this lane's set 34.

## What was done

1. **Corpus fixed and counted.** `knowledge/components/*chart*.meta.json` carrying `intent` = **14**. Minus `chart-bar` (lane A's, because it is a `$composes` slug of the bento template) = **13**. Confirmed by script, not by eye. The 13: Chart-boxplot · Chart-bullet · Chart-butterfly-h · Chart-butterfly-v · Chart-candlestick · Chart-histogram · Chart-scatter · chart-combo · chart-donut · chart-line · chart-pie · chart-sparkline · chart-stacked-area.

2. **R3 one-hop, by hand, before authoring.** For each meta I pulled (a) its roles.json co-providers with their illustrative `priority`/`when`, and (b) its own `edges.mustNotNeighbour` block (the field is `edges.mustNotNeighbour`, mirrored in legacy prose at `relationships.mustNotNeighbour`). Both are cited in that meta's section on the page.

3. **Authored, by addition only.** First attempt used `json.dump`, which re-flowed the hand-authored one-line-per-prop style and produced a 817-insert / 123-delete diff. Reverted; re-did it as a **surgical text insert** after the `"intent":` line, with a post-insert assertion that the parsed object minus the seven new keys is byte-identical to the original. Final diff: **186 insertions, 0 deletions**, 13 files.

4. **`answers` is `intent` copied verbatim** by the script (`d["answers"] = d["intent"]`), never retyped — so check 7 `intent-equals-answers` cannot drift. `chart-stacked-area`'s `intent` is the array `["change-over-time","composition"]`; the array is copied, not collapsed. `intent` was not renamed or removed anywhere.

5. **`provides`:** `chart-panel` on 12; `headline-metric` on Chart-bullet; `chart-sparkline` is `chart-panel` at priority **30** per ROLES-REVIEW §03.

6. **Gates.** `_validate_roles_resolve.py` (GREEN for all 13 — see Findings on lane A's ten), `_validate_kg.py` (OK, 138 metas, idempotent-clean), `_build_integrity.py` (PASS — 0 errors, 22 warnings, schema valid 136/136), `gen_component_partials.py` (0 blocks injected, all in sync — it wrote nothing). `_build_all.py` NOT run, per brief.

7. **Page** `reviews/META-TAGS-B-2026-09-07-v1.html` (~49.7 KB), swiss-design-system, grid/typography copied from the ROLES page. Sections: read-me · 13 per-meta sections (six values, `when` spelled out, siblings table, mustNotNeighbour list, authored `not-with`) · vocabulary this batch introduced · priority ladder · 10 numbered calls. Generated by `/tmp/laneB/gen_page.py`, which reads the metas and roles.json — **every count and every listed value on the page is computed, none typed**. Tag balance checked programmatically: no unclosed, no mismatched tags. No `$` figures on the page (one `$composes` reference, which is a field name).

## Findings

**F1 — the resolver is RED overall, and none of it is mine.** After lane A landed its 11, `_validate_roles_resolve.py` reports `RESULT: FAIL (10)` — ten `answers-resolves` failures on lane A's non-chart words (`one-number`, `where-am-i`, `what-can-i-do`, `what-is-this`, `where-does-it-go`, `where-can-i-go`, `status-of-one`, `details-before-commit`, `what-holds-the-page`). That is the designed behaviour: s251-D11 says a new word enters `chart-intents.json` only by Dave's ruling, and lane A's page proposes them. Failures naming one of this lane's 13 stems: **zero**. Before lane A landed, the same script printed `RESULT: PASS`.

**F2 — no priority collision with lane A, but a disagreement worth Dave's eye.** Lane A authored `chart-bar` at **84**, which places it *below* `chart-line` (88). This lane's page proposes **92** — chart-bar at the top of the role, because the bento template composes it and comparison is the most-reached-for intent. Both are unique, so the gate is happy either way; the ordering question is not a gate question. Page call 2 puts it to Dave with the landed 84 shown.

**F3 — roles.json's ties had to break somewhere.** roles.json puts five charts at 60 and three more at 40. Check 5 (`priority-unique` in-role) forbids that once the metas are canon. This lane's ladder is 88→30 in steps of 4 (chart-line 88 · chart-donut 80 · Chart-histogram 76 · Chart-scatter 72 · chart-stacked-area 64 · Chart-boxplot 60 · chart-combo 56 · chart-pie 48 · Chart-candlestick 44 · Chart-butterfly-h 40 · Chart-butterfly-v 36 · chart-sparkline 30), preserving roles.json's ordering wherever it expressed one. The only place I imposed an order roles.json did not have is the three-way tie at 40: pie 48 > candlestick 44 > butterfly-h 40 (page call 3).

**F4 — the derived `instead-of` grade makes `shape` equality load-bearing.** The validator grades substitutes by comparing `shape` (equal?) and `span` (overlapping?). So writing `parts-of-whole` on BOTH chart-donut and chart-pie, and `categories × two-series-mirrored` on both butterflies, is not cosmetic — it is what makes the derived grade say `equivalent` for genuine twins. Confirmed against the printed derivation.

**F5 — `not-with` authored in one direction only, on 6 metas (7 edges).** Chart-histogram (chart-bar, stat-card) · Chart-butterfly-h (stat-card) · Chart-butterfly-v (stat-card) · chart-pie (chart-donut) · chart-sparkline (chart-line) · chart-stacked-area (chart-bar). The validator derived and printed all seven reciprocals and wrote nothing back, as s251-D6 requires.

**F6 — the dv-014 backlog stayed backlog.** ~10 `edges.mustNotNeighbour` entries across the 13 are variants of "a second chart re-using these series colours for DIFFERENT data" — they name no slug, and s251-D6 makes `slug` required. Not promoted; left verbatim as declared migration backlog. chart-combo's "a same-unit pair drawn as a combo" is a condition on itself, not a neighbour, so it also stayed put.

**F7 — `span` is readable straight off each meta's `responsive` prose**, which turned out to be the least invented of the six fields. Six bands emerged: 6–12 (fit fractions declared) · 8–12 (the wide-panel butterfly) · 6–8 (meta says "FIT hooks are NOT wired" / "a runtime fit would be Layer-2, not baked") · 6–6 (DV-D02 excludes circles from width-compression) · 4–6 (Chart-bullet, baked 580 wide, headline-metric) · 2–4 (chart-sparkline's own two reviewed scales, 200×48 inline and 340 standalone). 100px per column at the 1200px max is the conversion.

**F8 — 11 shape values, 12 when fields, all listed on the page.** shape: `bins × frequency` · `categories × five-number-summary` · `categories × two-series-mirrored` (×2) · `parts-of-whole` (×2) · `periods × ohlc` · `points × two-measures` · `rows × measure-target-bands` · `shared-x × two-measures-different-units` · `time-series × 1–5-series` · `time-series × parts-of-whole` · `time-series × single-series-no-axes`. `when` fields: answers · series · categories · variables · bins · parts · total · axes · units · span.cols · target · shape. The field list is derived by the page script from the LEFT half of each predicate only (before the em-dash), so a sibling's name quoted in the beats/yields clause is not miscounted as a field.

## RULING-SHAPED QUESTIONS

1. **chart-bar: 84 (lane A) or 92 (this lane)?** Does chart-bar top the `chart-panel` role, or does chart-line? The two lanes disagree and the gate cannot settle it.
2. **Is `shape` promoted to a closed store with a resolver?** 11 values from 13 charts; lane A adds more. It is the only one of the eight that resolves against nothing today — a declared UNPROVEN, not an oversight.
3. **Is the `when` field list closed at these 12?** s251-D5 explicitly deferred it. Twelve is what the chart batch needed; forms will want more.
4. **Does the em-dash two-half `when` grammar stand?** Left = gate (fields only), right = which sibling it beats or yields to, with that sibling's priority. R2 asked for the second half; a future parser reads the left alone.
5. **Does chart-sparkline stay a `chart-panel` provider at all?** ROLES-REVIEW §03 kept it at 30 while calling it "normally a complement of 01, not a panel". Kept as briefed; it is the one provider whose membership its own `when` argues against.
6. **The three-way tie at roles.json 40** (pie / candlestick / butterfly-h) was broken by reach-for frequency. Is that the right tie-breaker, or should it be intent coverage?

## UNPROVEN

- **That any assignment is RIGHT.** The resolver proves no address dangles and no in-role priority collides. It says so itself. Correctness of a `shape` word, a `span` band or a `priority` slot is Dave's eye against the meta's own `purpose`; that is what the page is for.
- **`shape` resolves against nothing.** No store, no vocabulary, no check. Values are counted and listed only.
- **The `when` predicates are not parsed by anything.** They are strings. Nothing enforces that a field named in one exists, or that a `beats`/`yields` claim is consistent with the ladder — I cross-checked those by hand (R2), which is not a gate.
- **Span bands are a reading of prose, not a measurement.** No renderer was driven at 4, 6, 8 or 12 columns to check that a chart survives its declared minimum. The 100px-per-column conversion is this lane's arithmetic, not a ruled figure.
- **The page was not viewed in a browser.** Local-file preview is unavailable in this session; structure was verified programmatically (tag balance, computed counts, no `$` figures) and the CSS is the ROLES page's, unchanged.
- **Nothing was committed and nothing was pushed.**

## Files touched

Authored (13, by addition, 186 inserts / 0 deletes):
- `knowledge/components/Chart-boxplot.meta.json`
- `knowledge/components/Chart-bullet.meta.json`
- `knowledge/components/Chart-butterfly-h.meta.json`
- `knowledge/components/Chart-butterfly-v.meta.json`
- `knowledge/components/Chart-candlestick.meta.json`
- `knowledge/components/Chart-histogram.meta.json`
- `knowledge/components/Chart-scatter.meta.json`
- `knowledge/components/chart-combo.meta.json`
- `knowledge/components/chart-donut.meta.json`
- `knowledge/components/chart-line.meta.json`
- `knowledge/components/chart-pie.meta.json`
- `knowledge/components/chart-sparkline.meta.json`
- `knowledge/components/chart-stacked-area.meta.json`

Created:
- `reviews/META-TAGS-B-2026-09-07-v1.html`
- `notes/_subreports/2026-09-07-253-BUILD-B.md` (this file)

Not touched, per brief: `chart-bar.meta.json` · any non-chart meta · `knowledge/roles.json` · `knowledge/chart-intents.json` · `knowledge/components/meta.schema.json` · `knowledge/_validate_roles_resolve.py`. Verified by `git status`: the only other working-tree changes are `notes/_REHEARSAL-LOG.jsonl`, `notes/_dream/_GRADE-DECISIONS.jsonl` and an untracked `notes/_subreports/2026-09-07-253-R-hyper-fm-tpami.md`, none of them this lane's.

Scratch (outside the repo, not for commit): `/tmp/laneB/author_b.py`, `/tmp/laneB/insert_b.py`, `/tmp/laneB/gen_page.py` — the last is the page generator, so if the page must be rebuilt it is regenerated, not hand-edited.
