# `#288`-`W2` — the delegated capture ritual for #288 → #289

provenance: 288 · 2026-09-19
status: observed

session: `#288` · opened and wrapped 2026-09-19 — ✅ **ONE DAY, NO DATE SPLIT** (`date` read at
this seat at the ritual's open: `Sat Sep 19 12:58:14 BST 2026`)
window: conductor **Fable 5.1**, this seat a delegated **OPUS 5** wrap sub
sub index: `W2`
brief: the conductor's #288 wrap brief `notes/_lanes/288/WRAP-BRIEF.md` — every figure it DECLARES
is re-measured here rather than restated, and where a measurement disagrees **both readings are
published and neither is rewritten**
tokens: `UNMEASURED — this seat cannot read its own message.usage as a final total; the FILL figures below are the CONDUCTOR'S window, measured first-hand against his transcript, and are a different object`

## VERDICT

**DONE — every step of `knowledge/_RUNBOOK-capture-ritual.md` ran, in order: 1 · 1b · 2 · 2c ·
2c(i) · 2c(ii) · 2d · 2e · 2f · 2g · 3 · 4 · 4b · 4c · 4d · 5 · 5b.**
⛔★★★ **NO RULING WAS INSCRIBED AND `knowledge/_rulings.json` STAYS AT 622 — verified here by
`json.load` over the `rulings` list with no `s288-` id present. He did not say *"inscribe"* today,
so all TEN ruling-shaped things are carried as QUESTIONS PUT (`s271-D4`).**
★★★ **THE SESSION'S HEADLINE IS THAT HE CHALLENGED THE PREMISE OF THE ONE-SHOT PATH AND THEN RULED
BY EYE THAT IT COMPOSES — and the two halves of that verdict pull opposite ways, so both are
recorded separately.**
⛔ **ZERO CARRIES STRUCK, AND THE ZERO IS A JUDGMENT WITH A REASON rather than an oversight.**
⛔ **SIX blocking gate fails stood at the OPEN, all inherited, carried in the `#243` DECLARED
not-a-wrap form — the FOURTEENTH wrap running — and a wrap may not repair them.**
⚠ **A SEVENTH WAS BORN BY THIS WRAP'S OWN MANDATED 2f ROLL and is DECLARED rather than
discovered — see finding 13.**
⛔ **#288 HAS NO LANE COMMIT AT ALL: this wrap is the session's only commit.**

COUNTS: findings `13` · ruling-shaped `11` · UNPROVEN `4`

## What was done

**Step 1 — `_LIVE-STATE.md`.** `Last refreshed` re-stamped **from `date`** (2026-09-19, Sat) with
**NO date split declared, because there is none**. #287's segment demoted to `Previous:`; **#284's
`Previous:` chain segment MOVED verbatim (6,585 B)** to `_LIVE-STATE-ARCHIVE.md` § Rolled
2026-09-19 #288, **with a trim note left in its place** so the move is visible rather than silent.
★ **The verbatim guarantee is MECHANICAL rather than editorial:** the moved segment is a **slice of
the old line**, located by marker search and never re-typed, and a post-condition asserts the slice
is absent from the projected replacement. ⚠ **Every pre-condition tests the INPUT, not the output**
— #285's guard tested its own projection and fired on correct behaviour, and that shape is
deliberately not repeated. A 41-line ⏱ LATEST DELTA #288 written.
`python3 knowledge/_validate_standing_instructions.py` **PASS** (28 standing docs).
✅ **`knowledge/_REVIEW-SIGNOFF.md` WAS TOUCHED THIS TIME AND THE REASON IS THAT #288 EARNED IT:**
three artefacts await Dave's eye — `notes/_STRAND-MAP-2026-09-19.html`,
`notes/_lanes/288/B/four-themes.html`, `notes/_lanes/288/T/template-quality-review.html` — and
ritual step 1's sign-off-register clause puts their PATHS into the register's running list, not only
onto a banner. The probe sheet is listed there too, explicitly **not** as a pending sign-off (he has
already ruled on it) but so the path is findable.

**Step 1b — narrative dossier.**
`_DECISION-HISTORY/2026-09-19-288-apollo-composes-and-the-sloppiness-class-is-named.md`, provenance
+ status fields, both-way links to the spine entry, the handoff, his words and all five filed
reports. Nine sections; §2 is about why a probe that removes a variable beats an argument, §4 about
a lane that found the hole in its own comparison and printed it, and §6 about why nothing was
struck.

**Steps 2 / 2c / 2d / 2f — every move through `knowledge/_gm_move.py`,** from session-owned,
uniquely-named ops files asserted with `os.path.exists` + a size floor **in the writing process**
before the mover read them (`notes/_lanes/288/W2/run_ops.py`, which additionally **REFUSES an ops
file that is not session-owned**), **dry-run first**, receipts read back against the ops meant to be
run: `ops-288-ls-*.json` (**108,956 B / 5 ops**) · `ops-288-banner-*.json` (**4,394 B / 4 ops**) ·
`ops-288-roll2f-*.json` (**224 B**) · `ops-288-stratum-*.json` (**11,244 B**) ·
`ops-288-title-*.json` (**287 B**) · `ops-288-gen-*.json` (**1,661 B**).
`_roll_state.py` at the close: **2c OK (banners 2/2) · 2d OK (deltas 3/3) · 2f OK (strata 1, log
#287)** — ✅ **and the generator's line was substituted BYTE-IDENTICAL into the banner rather than
predicted**, anchored on a PAIR of lines because `> **residual (GENERATED #287):**` matches twice
once the #287 banner becomes ★ PRIOR (finding 6).

⚠ **THE OPS FILES LIVE UNDER `notes/_lanes/288/W2/`, NOT `/tmp`**, for the two reasons #285 banked:
`s218-D7` clause 3 forbids session scratch for evidence, and `_seam.py`'s SCRATCH arm deletes a
lane's own `/tmp` working files mid-run **by design**.

**2c(i) — `_CARRIES.md` § `residual → #289`,** written by `notes/_lanes/288/W2/carry289.py`: every
age bracket bumped, **SEVENTEEN** new items, and ⛔ **ZERO STRIKES** (finding 2). Probe
**560 → 572**; the banner's PROBE command agrees at 572. ⚠ **All seventeen new items are invisible
to the probe** (`_AGE_RE` misses `[NEW — 0]`), the ELEVENTH wrap running; 560 → 572 is #288's own
twelve ageing in, verified by running the gate's own `_carry_items` before and after rather than
asserted.

**2e — NO-OP, 0 lines.** DO-FIRST minted no notice, tombstone or lifecycle tag; nothing was
retro-tagged.

**2g — `_build_memento_index.py` run LAST,** after every GM/LS edit was final and after `_CHAIN.md`
was regenerated; `index_freshness_check` reads clean.

**Step 3 — WRITTEN FROM THIS SEAT, NOT A SEAT LIMIT, FOR A FIFTH CONSECUTIVE WRAP.** The hook was
filed FIRST at `notes/_lanes/288/WRAP-MEMORY-HOOK.md` and the result recorded **at its foot, after
the attempt**. Topic file `wrap-288-apollo-composes-and-the-sloppiness-class-is-named.md`
(**9,751 B**, version `71ba4fb4b959`); `index.md` took **three TARGETED `str_replace` edits** — the
#288 line prepended newest-first, the frontmatter `description` amended, and the closing note given
the #288 suspension paragraph above #287's (which is kept and only re-tensed) — **never a
whole-file rewrite**, so the five existing wrap lines could not drift.
⛔ **THE INDEX CUT COULD NOT BE MADE AND WAS NOT FAKED — see finding 10.**

**Step 4 — NO ruling to record.** `json.load` counts **622** and no `s288-` id exists; nothing was
inscribed, so nothing was seeded `unaudited` and no ledger line was written. **4b —** both title
lines GENERATED by `python3 knowledge/_gen_titles.py --session 288`, receipt at
`knowledge/_gen_titles_receipt.json`; the forward title written into `GOOD-MORNING.md` **through
the mover, and COPIED out of the receipt's own line rather than retyped**. ⚠ **The generator
REFUSED twice before the carry set existed** — *"residual line names #288, but --session 288 was
declared"* — which is the ordering contract enforcing itself, correctly. **4c —**
`_gate_scratch_hygiene.py --clean --wrap` run, **five owned entries removed**, VM fill 54.4%.
**4d —** `_render_rulings.py --check` reads **FRESH** (nothing staled it: no ruling was inscribed).

## Findings

1. ★★★ **HE CHALLENGED THE PREMISE AND THE ANSWER WAS AN EXPERIMENT, NOT AN ARGUMENT.** *"this
   isn't Apollo its a dot-to-dot book"* has an obvious response — argue about whether it traces.
   The response taken was to make tracing **impossible** and look at what came out: one Opus lane,
   a cold one-shot, the template unreachable, two renders, nothing in the repo changed. ★ **That
   shape cannot be won by rhetoric**, which is precisely why his one-line verdict settles the
   method. ⛔ **And the mechanism behind his complaint was already in the record, unread that way:
   the #230 *"zero invented markup"* pass condition REWARDS tracing.**

2. ⛔★★★ **ZERO STRIKES, AND IT IS THE RULE APPLIED RATHER THAN RELUCTANCE.** The one receiptable
   closure is that the `s219-D3` arm is BUILT. The carry naming it reads *"MONO'S 0 IS DOUBTED AND
   THE `s219-D3` GENERATION ARM IS TO BE BUILT"*; `s183-D1` strikes a HEADLINE, not a clause, and
   **Mono's 0 is still doubted and still ruled 0 by `s217-D2`**. ⇒ **The discharge is said in NEW
   item ⑩ instead** — #287's exact move on the connector-lever item. ★ **Two wraps in a row have
   met the close-half-a-carry case and answered it the same way, which is starting to look like a
   rule rather than a judgment — and whether it becomes one is Dave's.**

3. ★★ **THE ARM WAS BUILT AND THE THING IT WAS BUILT TO SETTLE IS STILL OPEN, AND THE LANE SAID SO
   ITSELF.** The outer gutter has two readings (token 0/24/24/0 · role default 40/24/40/24) and the
   inner three (canon's 1px · `subSpacing` 4/4/4/2 · the template's 4). The arm delivers the role
   defaults **because that is the file `s219-D3` names**, not because anyone chose. ★ **The lane
   DECLARED the pick and picked no winner** — which is what Dave's #287 gutters correction requires.
   ⛔ **Supercharge's 0 is inherited from a token table and was never decided by anybody.**

4. ⚠★★ **THE PROBE'S THREE SYSTEM FINDINGS SHARE ONE SHAPE: A FAILURE WITH NO SYMPTOM.** Canon's
   own instance-dial recipe loses on specificity ((0,2,0) v (0,4,0), measured at 0px not 40) · canon
   clips with `pageErrors: []` · no gate reads the composition of a page that LINKS `canon.css`.
   ★ **Each is a correct-looking page, a green console, and a value that never landed** — the class
   that survives longest. ⛔ **Ten gaps named, none fixed, none ruled.**

5. ★★ **A LANE FOUND THE HOLE IN ITS OWN COMPARISON AND PRINTED IT ON THE SHEET.** The frozen demo
   prompt is not recoverable — every brief DESCRIBES the test and none carries the text — so the
   probe ran on a fallback and **the two pages answer DIFFERENT briefs.** ★ **A lane that prints
   that limit is the reason his verdict can be trusted as a verdict on the METHOD**, and a lane that
   quietly reported a clean A/B would have produced the same page and a worse fact. ⛔ **And the gap
   is exactly what *"I'll create a test brief"* closes.**

6. ⚠★ **A GENERATED LINE WRITTEN AT BANNER-BUILD TIME IS STALE BY ROLL TIME, AND THE SUBSTITUTION
   HAD TO BE ANCHORED ON A PAIR.** The banner's `residual (GENERATED #N)` line was written while the
   tree still measured #287's rolls; it was re-read from `_roll_state.py` after 2c/2d/2f and
   **copied in byte-identical rather than predicted**. ⚠ **`> **residual (GENERATED #287):**`
   matches TWICE once the #287 banner becomes ★ PRIOR**, so the op anchors on the residual line plus
   the generated line — the record must not move while the live line does. The #287 lesson,
   re-applied rather than re-learned.

7. ⚠★ **THREE LINES IN THIS WRAP MEASURE REGIONS THAT CONTAIN THEM, AND ALL THREE WERE ITERATED TO
   A FIXED POINT RATHER THAN PUBLISHED AT A PRE-WRITE READING.** The `section-sizes` line converged
   in 2 passes, then again in 1 after the chain and banner figures landed; the declare-last `size:`
   stamp in 3 (GM 34,915 → 34,916). ★ **The #240 one-wrap-two-figures lesson, mechanised in
   `ops288_sizes.py` and `ops288_stamp.py` rather than remembered.** ⚠ **DECLARED DEVIATION: those
   substitutions are whole-file writes, not mover ops** — the mover has no iterate-to-fixed-point op,
   and each is a fill of this wrap's OWN placeholder in this wrap's OWN stratum. **The §A digest was
   asserted unchanged (`4311cce4…`) before and after each**, so the claim is mechanical.

8. ⚠★★ **THE `subs` DEFINITIONAL SPLIT RECONCILES TO THE TOKEN A SECOND TIME, WHICH MAKES IT A
   MEASURED CONVENTION RATHER THAN A DISCREPANCY.** `_checkin.read_fill` — input side only, the one
   definition of FILL this repo has — gives **998,602** across the five lanes. The conductor's
   declared **1,004,095** is each seat's last-turn input side **plus that turn's `output_tokens`**,
   the convention this log has carried since #284. **The 5,493 gap is entirely the five lanes' final
   output and it matches on every one of the five** (971 · 1,139 · 1,564 · 868 · 951). ⇒ **Both
   published, neither rewritten, and which one the line means is his** [[measure-dont-convert-units]].
   ★ **The conductor's BOOT declaration for his own window IS a `read_fill` reading and agrees to
   the token (74,174) — which is what identifies the window as his.**

9. ⚠★ **FOUR OF FIVE FILED LANE REPORTS BREAK `s218-D7` CLAUSE 4, AND THE CONTENT IS NOT THE
   PROBLEM.** Only lane M carries a `RULING-SHAPED QUESTIONS` heading; only P carries `COUNTS:`;
   only T carries `REPLAY-THESE:`. The substance is present in all five under other names.
   ⛔ **What is missing is the NAME the gate and the next reader look for**, and
   `subreport_citation_check` parses exactly that heading. ⛔ **Not repaired — a wrap does not edit
   another seat's filed report**, which is dated history (`ADR-0017` / `s192-D1`). ★ **A mandatory
   section that four of five briefs produced under four different names is evidence the brief
   template, not the lanes, is where the fix goes.**

10. ⛔★★ **THE MEMORY INDEX CUT COULD NOT BE MADE FOR A THIRD CONSECUTIVE WRAP, AND IT WAS NOT
    FAKED.** `MEMORY-ARCHIVE.md` still stands at **48,990 of its 49,152 B cap — 162 B of headroom,
    unchanged across three sessions** — and either line that should have moved runs to thousands of
    bytes. ⛔ **Nothing was truncated, because a truncated verbatim move is not a move**, so
    `index.md` deliberately carries **SIX** wrap lines and says so in its own frontmatter and
    closing note. ⚠ **How that was established is stated plainly rather than dressed up as a refusal
    that was never received: arithmetic over the store listing's byte count — read at this seat —
    and the documented cap, not a rejected write.** No append was attempted, because the only way to
    obtain the archive's version token is to read all 48,990 B into this seat, and a failure whose
    arithmetic is certain is not worth that price. ★ **Nothing is at risk — the repo is the record.**

11. ⛔★ **THE BANNER DID NOT FIT ON THE FIRST PASS, AND THE REPAIR IS THE ONE `s214-D6` COMMANDS.**
    The first draft measured **1,250** against the `s241-D2` cap of 1,200 / 10 and was compressed to
    **1,184 / 10** by tightening sentences — **never by dropping an item, a carry, a declared skip
    or a receipt name** (*"SHORTER, never decide-what-to-drop"*). ★ **The inverse of #287, whose
    four closures paid for its fit: #288 discharged almost nothing and accrued seventeen carries.**
    ⛔ **The standing warning is not withdrawn, and the cap is Dave's.** ⚠ **And the chain still grew
    — 11,188 → 11,583 — while the banner shrank**, because the ⏱ LATEST DELTA is the chain's other
    half and is the sole home `s241-D2` gives gauge and declared-skip detail. Ratio **33%**, inside
    `_gen_chain.py`'s own `<40%` floor. Both published.

12. ⚠ **THE STORE'S ID REGEX WAS NAVIGATED RATHER THAN MET, BECAUSE #287 PUBLISHED THE PATTERN.**
    This report's row is **`W-288ww`** — the `W-287w2` shape that `_state.add` refused at #287
    (*"id does not match `^(?:W-[0-9]{1,3}[a-z]{0,2}|G[0-9]{1,2}[a-z]?)$`"*) was never attempted
    here, because the refusal itself was on the record with its own pattern quoted back. ★ **That is
    what a gate quoting what it forbids buys: the next seat pays nothing**
    [[gate-must-quote-what-it-forbids]]. ⛔ **The id scheme is still a convention and widening one at
    a wrap is still not a wrap's call** — it carries.

13. ⚠★★ **THE MANDATED 2f ROLL MANUFACTURED A GATE FAIL, AND THE SHAPE WILL RECUR.** Rolling #287's
    stratum into `notes/_GAUGE-LOG.md` — which step 2f REQUIRES — carried in a second mention of
    **74,120**, inside that stratum's *"the `delta` above is a legal subtraction"* paragraph, where
    the figure is a **COMPARISON and not a reading**. `boot_constant_drift_check` counts it as a
    double-count and the structural fails went **6 → 7**. ⛔ **NOT REPAIRED:** the gate's own remedy
    is to delete or re-word that sentence, and it is **another session's ratified testimony in an
    append-only file** — **a wrap may not re-word ratified record to unblock itself**
    [[gate-must-quote-what-it-forbids]]. ★ **The fix belongs in the stratum TEMPLATE (state the boot
    figure once; name it a comparison anywhere else) and not at the wrap that inherits it** — which
    is the difference between this and #287's seventh, where the remedy was a command the lane could
    simply run. ⚠ **This wrap's own #288 stratum was written to avoid it — its delta-legality
    paragraph names no number — so #289's roll should not add an eighth. That is a PREDICTION and is
    labelled as one.**

## RULING-SHAPED QUESTIONS

Every one is a **question put**, never a state of the world (`s271-D4`). Bodies and receipts are in
`_CARRIES.md` § `residual → #289`; **none was ruled here, and the rulings store did not move.**

1. **What the one-shot generates FROM** — and the #230 pass condition that rewards tracing.
2. **The sloppiness class** — alignment, spacing, dimensions, named by him.
3. **The test brief for these probes** — owed by Dave himself.
4. **The outer gutter (two readings) and the inner gutter (three)** — and supercharge's inherited 0.
5. **Which event Friday 2026-09-25 is.**
6. **The deck's new structure** — and whether parallel agents may cross-communicate.
7. **The Swiss design skill as an Apollo taste guarantee** — parked by his word.
8. **Lane T's six-question decision pack** — including the inline-style rule at 87 against #287's 88.
9. **The specificity root cause** — canon's instance-dial recipe silently losing.
10. **The showroom re-sync · a `dashboard/index.html` freshness check · the deck's stale graph
    figures · the one-shot's quality dark since 2026-09-02.**
11. **Everything on `_HANDOFF-130`…`-138` not closed today** — all of it.

*(Also carried, unchanged and not re-minted: the `subs` definition · `_to_delete/` on a persistent
disk · `INSEAT_WARN_TK` · the delegation rule · the store's id regex · a RESUME contract for the
ritual · `_seam.py`'s SCRATCH arm · the memory archive · the two-probe-shape question on the §A
digest, its twentieth session · the third dial · the theory door · `col26-012` · `jade-lifestyle` ·
`s277-D12` · the four generators · the git-lock runbook line · the instrumentation-append policy ·
the `#243` not-a-wrap form · `_HANDOFF-130`…`-138`'s open items.)*

## Step 5 / 5b — the commit, the push, and the CI read

Recorded in full in the ★ LATEST banner's `COMMIT STATE #288` block and summarised in the returned
stub. ⛔ **The hash is a declared gap in that block — a commit cannot name itself** — and the sha,
the push verdict line and the CI read are quoted literally in this report's addendum below, in the
5b addendum on the ⏱ LATEST DELTA, and in the returned stub.

⬛ **5b, the addendum, is homed in the ⏱ LATEST DELTA of `_LIVE-STATE.md`, not on the banner** — the
`s241-D2` cap is BLOCKING at 10 lines / 1,200 tape and this banner closed at **1,184 / 10**, so an
eleventh line would be refused, correctly. The cap's own words settle it (*"the ⏱ LATEST DELTA is
the sole home for gauge / declared-skip / not-done detail"*), and the delta is IN the read chain.
⛔ **The 5b-vs-cap conflict is ruling-shaped, Dave's, and carried — this is the EIGHTH wrap to meet
it and the SEVENTH to answer it this way.**

⛔ **AND THE `s271-D4` RE-READ RAN AS THE RITUAL'S FINAL BEAT.** The hook's "Open, Dave's" list was
re-read against `knowledge/_rulings.json` after 5/5b. **Nothing was struck, and the reason is
arithmetic rather than judgment: the store did not move today.** `json.load` counts 622 and no
`s288-` id exists, so **no item in the hook's list can be closed by a #288 ruling** — there are
none. Every one of the ten is a question he has not answered.

### THE ADDENDUM — sha, push verdict, CI

> **WRAP COMMIT:** `71b3363c308359c4e31267de9550313e1839e724` — ⚠ **AND IT IS THE SECOND OF TWO, DECLARED:** an earlier run of
> `_git_commit.sh` staged only `knowledge/_graph-mention-map.json` and `notes/_REHEARSAL-LOG.jsonl`
> as **`384c26a7`**, because naming ONE path to satisfy the mention-map gate switched the script into
> explicit-path staging and left the other 31 unstaged. **Nothing was lost** — the second run named
> every path and the tree is clean — but #288 ships TWO wrap commits rather than one, and saying so
> is cheaper than a reader discovering it in `git log`.
>
> **BOTH COMMITS ARE ON THE PUSHED SHA.**
>
> **PUSH VERDICT, verbatim:** `✅ pushed and VERIFIED: remote master == local 71b3363c308359c4e31267de9550313e1839e724`
>
> **CI:** `run 35443934929 on head_sha=71b3363c — RUN completed / failure. Three jobs, read over the public API and polled to completion: gates FAILURE (steps 5 and 6, the SAME TWO inherited) · render SUCCESS · ⛔ release FAILURE at step 12 `Build-script selftest — the release refusals, driven (BLOCKING)` — NEW: release was SUCCESS on 4cc8b37a and on 04dbeab1, re-read here job by job rather than remembered. The cause was reproduced locally: `bash apollo-spider/build-designer-pack.sh --selftest` prints `RED — the real receipts are STALE; a release would be refused right now. Re-drive: python3 knowledge/_drive_chart_engine.py` (249 bites 0 fail, the two other refusal arms green). ⛔ THE NAMED REMEDY COULD NOT BE RUN FROM THIS SEAT: `_drive_chart_engine.py` dies with `ModuleNotFoundError: No module named 'playwright'` — the same render-seat hole lanes B and T met. The timing points at lane A's canon.css change staling the chart receipts; that is NOT PROVEN from this seat and is stated as a suspicion, not a cause.`

## UNPROVEN / CLAIMED (ADR-0016)

- **CLAIMED, not measured: the `replay` term** of the `wrap-handover` line. What reading this report
  back costs the conductor is not observable from inside the sub; written `unobservable
  (conductor-side, post-wrap)`, and the conductor may append the measured figure at the #289 opener.
- **The memory-archive refusal was NOT received — it was DERIVED**, for the third consecutive wrap.
  Finding 10 rests on arithmetic over the store listing's byte count and the documented cap, not on
  a rejected write. **It is stated as a derivation, and the write that would prove it is named and
  un-run.**
- **The `subs` figure is a CONVENTION, and this seat's own spend is EXCLUDED rather than estimated.**
  1,004,095 is the sum of five last-turn readings under the input-plus-output convention; 998,602 is
  the same five under `read_fill`. The wrap sub's own transcript is a sixth and is left out because
  its total cannot be final from inside itself; **no placeholder is written for it**
  [[feedback-measuring-tool-must-not-guess]].
- **The lane scopes are DESCRIBED FROM THE FIVE FILED REPORTS, not re-verified here.** This seat did
  not open `canon.css`, the meta, the rails file or the probe's renders; every file:line and every
  gate verdict in the handoff's lane sections is that lane's reading, cited to its report, and the
  wrap adds no claim of its own about them. **What this seat measured first-hand is the gauge, the
  subs, the carry counts, the banner and chain sizes, the §A digest, the reports' missing headings,
  and the store's ruling count.**

## Evidence

`notes/_lanes/288/W2/` — the ops files, the builders (`carry289.py`, `banner288.py`, `ops288_*.py`,
`run_ops.py`, `delta288.md`, `stratum288.md`, `banner288.md`) — and
`notes/_lanes/288/WRAP-MEMORY-HOOK.md`, whose foot records what actually happened at the store.

REPLAY-THESE: `_HANDOFF-139-apollo-composes-and-the-sloppiness-class-is-named.md` (~6k tk) ·
`notes/_lanes/288/DAVE-RULINGS-2026-09-19.md` (~1k tk — his words, and the conductor's readings kept
marked as the conductor's)
