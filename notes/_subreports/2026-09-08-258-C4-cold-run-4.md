# #258 lane C4 — the FOURTH cold run (tree at `84f0a17`)

**Read-only against `knowledge/`, `showroom/`, `apollo-spider/`. Nothing built, moved or ruled there.
No build, no commit, no push, no checkout/stash. `git status --porcelain` is empty at the end.**

## Paths
- arm: `outputs/coldrun-258-v4/arm-A-blind/` — `dashboard.html` (122,876 B, sha256 `6b9e699f5477…7023b`),
  `brief.md`, `READ-LOG.md`, `GAPS.md`, `drive.json` (40 steps), four renders,
  `GATE-validate_receipt.log`, `GATE-validate_screen.log`,
  `GATE-LEDGER-copy-of-knowledge-_screen-gate-dashboard.md`
- review: `reviews/COLDRUN-258-2026-09-08-v4.html`
- this report: `notes/_subreports/2026-09-08-258-C4-cold-run-4.md`

## Done
- Ran `ADS-grill-me`'s defaults (no `briefs/`), announced **mono**, built bento-first from
  `Template-dashboard-bento.reference.html`, linked `canon.css` + `type.css` and dropped every
  component in as its `.cn-*` scope class.
- Carried `dv-behaviour` and `dp08-anchor` VERBATIM; carried `Filter-toolbar-bar`'s script VERBATIM;
  EXTENDED `Data-grid`'s and `Drawer`'s scripts (marked in place, declared in `#behaviour-manifest`).
- One data model (`MODEL`, 84 payments / 6 entities / 6 currencies / 12 months) drives every panel.
- Minted the receipt (step 5's new order), then both gates. Drove 40 steps with playwright at 1440
  and 820, light and dark.

## NOT done (declared)
- The arm was **not** frozen before the gates: the skill's step 5 mints INTO the page, and step 6 says
  drive it and read the console. Two lane-made splice defects (a double-wrapped `<script>` around the
  Drawer script; `page.inert` with no `#page`) were found at step 6 and repaired; the pre-repair
  pageerror counts (6, then 4) are reported in the review beside the final 0.
- No second independent probe pass (v3 ran one); no `_validate_composition.py` in isolation;
  no accessibility audit beyond the gate's own `a11y: ✅`.
- The `.pg-chart-head` harness rule uses a raw `gap:16px` rather than a spacing token — declared.

## Gate handling
`_validate_screen.py` modified `knowledge/_screen-gate/dashboard.md` and `knowledge/_SCREEN-GATE.md`.
The ledger was copied into the arm folder and both repo files restored with
`git show HEAD:<path> > <path>` (no checkout, no stash). Repo clean.

## Scores
3 visually rich · 2 full · 3 persistent · 3 interactive.

## Context
Lane fill at the build→measure seam: ~215K of the 1M window; ~235K at the end of measurement.
