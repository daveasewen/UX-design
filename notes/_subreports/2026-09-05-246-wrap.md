# `#246`-`wrap` — the delegated capture ritual for #246: seven rulings inscribed in Dave's verbatim words and read back at 355, the stale push premise corrected in the chain's source, three lane reports rowed and cited, the residual → #247 transform, and the commit run against a stale `index.lock`

session: `#246` · 2026-09-05
window: conductor window (FABLE), delegated wrap sub (FABLE)
sub index: `wrap`
brief: `notes/_briefs/2026-09-05-246-delegated-wrap-brief.md` — filed by the conductor BEFORE this sub launched (the #245 shape, `W-424`); it points at `notes/_briefs/2026-09-03-242-delegated-wrap-brief.md` for the ritual's shape and at `knowledge/_RUNBOOK-capture-ritual.md` for the mechanics.
tokens: `UNMEASURED at the sub's own seat as a SPEND; the seat's FILL is measurable and is 172,813 real, peak across 68 turns (knowledge/_checkin.py, first-hand, newest mounted transcript). The two are different quantities and neither is converted into the other.`

## VERDICT

The capture ritual ran **COMPLETE** inside the brief's DO-NOT-RULE fence: **seven rulings `s246-D1`…`s246-D7` were
INSCRIBED at this seat through `knowledge/_inscribe_ruling.py` with `says` = Dave's verbatim lines, reconstruction proof
PASSED ×7, the store read back 348 → 355**; then steps 1, 1b, 2, 2c, 2d, 2e, 2f, 2g, 4, 4b and 4c all executed, **2e was
legitimately empty and is declared with its size**, step 3 (memory) was the conductor's and is declared as ZERO bytes
written here, and step 5 was run through `_git_commit.sh --wrap` **WITHOUT a push** (not authorised — declared). Every
roll went through `knowledge/_gm_move.py` from session-owned, uniquely-named ops files under `knowledge/_tmp/wrap246/`,
each asserted for existence and a size floor **in the writing process**, with every receipt read back against the ops it
was meant to run; nothing was hand-edited. **Nothing of Dave's was ruled beyond his seven verbatim answers, and nothing
was re-worded, re-dated, re-stamped or re-scoped; no constant, band, ceiling, floor, advisory, stop line, wall, roster or
version was moved.** ⛔ `s246-D7` is recorded as RULED-WITH-A-TEST — *"Id like to test all three somehow"* — and NOT as a
pick; the DP-17 three-way test design is carried to #247.

★ **The headline is that the chain's opener premise was stale and the correction is made where the chain is generated
from.** The #245 chain said "7 ahead, NOT PUSHED"; at the opener `origin/master` = `4dc12b6` = HEAD, and CI #483 had
already run on it (release GREEN 9/9; gates red at steps 5 + 6 = [18], Dave's). The new banner and ⏱ delta say so, and
`_gen_chain.py` regenerates `_CHAIN.md` from them. `_CHAIN.md` was never edited by hand.

**SEVEN RULINGS INSCRIBED AND READ BACK** — `knowledge/_rulings.json` parses at **355** (348 before), every `s246-D<n>`
with `by` Dave · `date` 2026-09-05 · `status` ruled; each `says` quotes the question text and the chosen option's text
from `reviews/DASHBOARD-PRINCIPLES-2026-09-05-v1.html` § 05 and then his line verbatim, with his closing sentence on all
seven as context. **Eight store rows were MINTED** (`W-421` … `W-428`): three lane briefs, the wrap brief, three lane
reports, this report. **TWO carries were STRUCK** with `s188-D2` receipts (both discharged by `s245-D10`), **TWO were
CORRECTED** with receipts and NOT struck (the push state; the `s203-D1` read-back), **349 ages were bumped**, ten
`[NEW — 0]` became `[1]`, and **twelve** new carries were written at age 0.

COUNTS: findings `7` · ruling-shaped `4` · UNPROVEN `5`

## What was done

**Step 0 — the brief, read; the shape, read; the rulings, inscribed FIRST (the brief's order).** The seven entries
were composed from the page's Q1–Q7 text (extracted from the built HTML, not retyped), the lettered option each answer
names, and Dave's line verbatim; `governs` names the files each ruling binds (`knowledge/guidelines/_rules-index.json`,
`knowledge/components/template-dashboard-bento.meta.json`, `filter-toolbar-bar.meta.json`, `kpi-tile.meta.json`,
`summary.meta.json`, `status-indicator.meta.json`, `knowledge/_render/_bento_edit_rails.json`,
`apollo-spider/skills/grill-me/SKILL.md`, the review page); `evidence` carries the `chat #246 2026-09-05 - Dave: "…"`
legal form, the page section and the filed report's RSQ. Seven `--dry-run` proofs, then seven `--write` runs:
`INSCRIBED: s246-D1 … rulings 348 → 349 … reconstruction proof PASSED` through `s246-D7 … 354 → 355`. Read back:
count 355, ids `s246-D1`…`D7`, all `by` Dave / 2026-09-05 / ruled. ⚠ `s246-D1`'s body carries the scope note that Dave
said "Eighteen" of a 29-principle page and that DP-19…29's inclusion is ruling-shaped; `s246-D3`, `D5` and `D6` carry his
own revisit clauses ("lets try … and see how it works", "this for now", "lets see how it goes") as status notes.

**Check-in at the opener** (`python3 knowledge/_checkin.py`): **1 STRUCTURAL** (the retrieval index STALE — the #32
defect, healed by 2g run LAST) · 2 heals-at-wrap (the two date zones) · **24 warn**, one CHANGED since the rehearse
record (git: 16 uncommitted paths) · FILL 172,813 real, boot 67,883, peak over 68 turns · SEAM CLEAR.

**Step 1 — `_LIVE-STATE.md` refreshed.** New `## ⏱ LATEST DELTA — 2026-09-05 … #246` (14 lines incl. blanks; 10
bullets), the #245 delta demoted to ⏱ PRIOR, and the `*Last refreshed:*` header rebuilt with the #246 summary leading,
#245 and #244 kept verbatim as `Previous:` segments. Date from `date` (Sat 5 Sep 2026, 14:24 BST).
`python3 knowledge/_validate_standing_instructions.py` → **PASS, 28 standing docs reachable, GOOD-MORNING structure
intact.** `knowledge/_REVIEW-SIGNOFF.md` gained **one row** — the principles page, AWAITING his word on Q8–Q16 and on
whether DP-19…29 are inside "lets do it" — written through the mover.

**Step 1b — dossier written.** `_DECISION-HISTORY/2026-09-05-246-the-library-dictates.md` with its
`provenance: 246 · 2026-09-05` / `status: observed` block and both-way links. Eight sections: why the session opened
on a stale premise and how the chain corrects it · why the focus narrowed to one sentence · why two baselines and why one
was Opus · why the finding is the library's and not the models' · why the principles came from his seeds before the
literature · why seven of sixteen were ruled and the seventh is a test · what the lanes cost · what is resolved and open.

**Step 2 — `GOOD-MORNING.md`.** New ★ LATEST #246 banner: **10 substantive lines, 1,197 tape** against `s241-D2`'s
cap of 10 / 1,200 — the sixth banner bound by it, met with a margin of **3**. Girth, never content: the three report paths
+ the brief + this report on ⓪, the seven rulings as ids with two-word glosses on ①, the finding on ②, the page on ③, the
stale premise on ④, not-done as a name list on ⑤, the gauge on ⑥. `TITLE THE NEXT CHAT` → `Apollo - #247: enact DP-01…18,
then re-run the prompt` (the brief's title; the top ⬛ carry's bolded clause is written so `_gen_titles.py` derives the
same words). `size:` stamp re-written LAST and iterated to an **EXACT fixed point** with `_gen_chain.py` — GM **35,864 tape** (+45 on
#245's 35,819) · §A **6,957** (byte-identical to #237…#245) · LS **57,496** (+1,186) · corpus 93,360; `_CHAIN.md` **9,154 tape =
slice 8,301 + wrapper 853**, GM header+LATEST 4,603, LS delta 3,698 — **1,669 DOWN on #245's 10,823**, inside the `s214-D6`
~10–12K band and above M10's 7,700 advisory.

**2c — banner roll + the carry set.** `## Batch 2026-09-05 #246` opened in `_GM-ARCHIVE.md`; the ★ PRIOR **#244**
banner moved there **verbatim, 12 lines**, by `{"op":"move"}` after its EXIT CHECK (1 marker line; 15 names probed in
`_CARRIES.md` § residual → #247, 15/15 present); the #245 banner demoted to ★ PRIOR in the same transaction that inserted
#246's. The carry set was written straight into `_CARRIES.md` § `## residual → #247` (clause (i)); the banner carries the
pointer, the ⬛ top item's headline and the probeable count. **`_capture_gate._carry_items` reads 234 on the #246 line
and 244 on the #247 line.**

**2d — delta roll.** The #243 ⏱ PRIOR DELTA moved **verbatim, 16 lines**, to `_LIVE-STATE-ARCHIVE.md` §
`Rolled 2026-09-05 #246`, and #243's `Previous:` chain segment (**2,554 chars**) moved into the **same** section in the
**same** mover batch, replaced in the spine by a dated trim note. EXIT CHECK: 7 marker lines, 12 Dave-owed names probed
present in the #246 carry line before the move; the 119-sweep re-run still stands in `_LIVE-STATE.md` § 🔀 SPIN-OFF LANE.
Deltas now **3/3**.

**2e — nothing to do, and the size is ZERO DATED STRATA.** `## ⬛ DO THIS FIRST` measures **109 raw lines / 7,120
tape** and a `#### ` scan returns **0**.

**2f — stratum split.** `{"op":"roll_2f","session":245}` → **19 lines** appended at true EOF of `notes/_GAUGE-LOG.md`
and **2 lines** into `_GM-ARCHIVE.md` under the newest batch, driven with the SESSION-QUALIFIED anchor
`> **COMMIT STATE #245:**`. The new `#### 2026-09-05 #246` stratum — 20 lines as written, plus the `s214-D6` line
inserted after the stamp — then written into `GOOD-MORNING.md` § ⏱ SESSION STRATA (#96/#95/#40/#41/#42 EXEMPT,
untouched). It carries: the boot **stated once** · `subs 1,249,971 tokens (n=7)` · the `wrap-handover:` row (brief-cut
165,789 · sub-cut 172,813 · delta 7,024) · the ✅ CEILING ARM line quoting the gate verbatim after the roll (0 fail; #244
and #245 graded fresh and under) · consult-receipts (honest negative) · section-usage/section-sizes · the `s214-D6` chain
measurement · thirteen declared skips, each with a size · the four EXIT CHECKS · six premise checks · `COMMIT STATE #246`
naming the lock. After the roll `_roll_state.py` prints `2c OK (banners 2/2) · 2d OK (deltas 3/3) · 2f OK (strata 1,
log #245)` and that line is on the banner verbatim.

**2g — retrieval index rebuilt LAST**, after every GM/LS edit and this report were final, and staged.

**Step 3 — memory: ZERO bytes written at this seat.** The conductor's, done at his
(`dashboards-one-shotable-is-the-focus-246`). NON-REPO, outside every gate; neither written nor read back here.

**Step 4 / 4b — decision nodes + titles.** `W-421` (lane B brief) · `W-422` (lane O brief) · `W-423` (lane P brief) ·
`W-424` (the wrap brief) · `W-425` (B) · `W-426` (O) · `W-427` (P/P2/P3) · `W-428` (this report) MINTED via
`_state.add`, every one refused-or-accepted by the store's own close-condition check (the first attempt was REFUSED on
`W-428` because this file did not yet exist — the refusal is the feature; the file was written and the batch re-run);
nothing closed; store **493 → 501**. `_gate_doc_rows.py --check` → **0 UNROWED** at the opener (the eight documents were
untracked and outside its population), re-run after staging in the commit's own output. `_gen_titles.py --session 246`
run and the receipt regenerated. **Step 4c** — `_gate_scratch_hygiene.py --clean` run; the wrap's own ops files live in
`knowledge/_tmp/wrap246/` (gitignored, in-mount, never `/tmp`).

**Step 5 — commit WITH `--wrap`, NO push.** `SESSION_N=246 bash knowledge/_git_commit.sh --wrap --reconciled
<msgfile> <paths>`. ⚠ **A stale `.git/index.lock` (0 B, 2026-09-04 07:33) sits on the mount; the script REFUSES to stage
while any `.git/*.lock` exists and never removes it, and the brief's instruction is STOP AND REPORT, not delete or move.**
The outcome — a hash and subject read back from `git log -1 --format='%h %s'`, or the script's refusal quoted — is in the
chat stub, because this file cannot carry its own commit hash. ⛔ **No push:** `origin/master` = `4dc12b6`.
NOT staged by the brief's fence: `knowledge/_screen-gate/dashboard.md`. `outputs/baseline-246*` are gitignored.

## Findings

1. **The chain was stale on a fact that `git` answers in one call, and the wrap is the first seat that checked.** "7
   ahead, NOT PUSHED" was true when #245 wrote it and false by #246's opener; the brief flagged it and this seat re-drove
   `git rev-parse origin/master` and `git rev-list --count origin/master..HEAD` (= 0). The correction is written in the
   banner and the delta only — the assertion-propagation class, corrected at its source.
2. **`s246-D7` is the first ruling in the store whose `ruled` head says NO OPTION WAS PICKED.** Dave's "test all three"
   is a real answer and not a deferral, but it is not one of (a)/(b)/(c); the entry records the test as the ruling and the
   pick as his, later. A wrap that inferred a lean would have ruled for him.
3. **Two of the twelve new carries are corrections of carries born only two days ago.** The radius scope question (④ at
   #246) was answered by `s245-D10` the same evening it was carried, at a commit after the wrap that carried it; the
   push-state premise (①) was falsified by Dave's own push. Both struck/corrected here with the commit as receipt. A carry
   set can age by one session and still be two rulings behind.
4. **Lanes B and O filed reports without the `_TEMPLATE.md` `COUNTS:` line or a `RULING-SHAPED QUESTIONS` heading.**
   Their questions sit on the review pages; the wrap's citation parser therefore reads nothing off them. Declared in
   the delta, the stratum and here; NOT edited (ADR-0017). Whether the doc-row gate should refuse a report without the
   line is RULING-SHAPED 2.
5. **Every one of the day's outputs that counts as evidence is outside git.** `outputs/` is gitignored (`.gitignore:67`),
   so both baseline trees (3.1 MB + 6.9 MB) exist only on this disk and in the two review pages. The review pages carry
   crops and line numbers, which is why they were built; the raw dashboards are one `rm -rf` from gone. RULING-SHAPED 3.
6. **The carry count moved 234 → 244 with twelve items added, and the arithmetic is the #230 arithmetic.** +10 is exactly
   the ten `[NEW — 0]` of #245 becoming countable; the twelve arrive uncounted. Two of the new items name step ids in
   prose ("[18]") and were worded so `_AGE_RE` cannot read them as ages.
7. **The stale `index.lock` is a fact of the mount, not of this wrap.** Lane B named it on 4 September (it blocked
   `git checkout` there too); the brief names it; the script names it. Three declarations and no remover, because the
   sandbox delete-guard makes `mv` the only legal move and the brief withheld even that. The commit's outcome is in the
   stub.

## RULING-SHAPED QUESTIONS

⛔ Do-not-rule honoured: Q8–Q16, "Eighteen" vs 29, the DP-17 test, the template chart, glue vs partial, splice vs link,
mono for the SH, parts-with-addresses, the cold test, [18], the radius build, the 4px audit, every prior carry at its age.
None re-worded, re-scoped or closed.

1. **Who moves the stale `.git/index.lock`?** (a) Dave, from the host (one `mv`) · (b) the next conductor, `mv` per the
   #242 brief's rule · (c) `_git_commit.sh` learns a `--move-stale-lock` that moves (never removes) a lock older than N
   hours and prints where it went. **Recommend (b) for tomorrow and (c) as a priced candidate**; a lock that three
   sessions have named and none may touch is a gate that cannot pass in one environment.
2. **Should `_gate_doc_rows.py` refuse a filed report with no `COUNTS:` line (finding 4)?** (a) yes, BLOCKING — the
   template says the stub's figures are copied off it · (b) warn only · (c) leave; the citation check already warns.
   **Recommend (b)**; two reports in one day shipped without it and nothing said so until the wrap.
3. **Do the baseline output trees get a durable home (finding 5)?** (a) commit `outputs/baseline-246*` (10 MB, mostly
   PNGs — the #238 asset-size policy is still open) · (b) copy the four `dashboard.html` files (~200 KB) into
   `notes/_subreports/assets/2026-09-05-246-*/` and leave the renders · (c) the review pages are the record. **Recommend
   (b)** — the built pages are what #247's re-run diffs against.
4. **Does `s246-D1`'s "until a second dashboard is built" count #247's re-run as the second dashboard?** (a) yes — the
   re-run against the enacted library is the second · (b) no — a second dashboard is a different brief, not the same
   prompt again. Not decided here; it is the clause that gates every later promotion from ADVISORY.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** the CI verdict for this wrap's commit — unread until Dave pushes. CI #483's reading (release 9/9, gates
  red at 5 + 6) is the conductor's, CLAIMED here.
- **UNPROVEN:** `python3 knowledge/_capture_gate.py --selftest` was NOT re-driven (it exceeds the ~180 s sandbox call
  wall — declared, not attempted) and `_build_all.py` was NOT run (fenced, and sandbox-impossible in one process).
- **UNPROVEN:** the lanes' own figures — the scores, the 20/137 and 25/27 `behaviour` counts, the 130/281 hex counts,
  the 14 + 9 + 12 sources — are quoted from the three filed reports and the page, NOT re-driven at this seat. What WAS
  re-driven: the rulings count (348/355) and each `s246-D<n>`'s fields, the Q1–Q7 text (extracted from the built HTML),
  `origin/master`, the doc-row gate, the standing-instructions gate, `boot_constant_drift_check`, `_roll_state.py`, the
  check-in, the `.gitignore` coverage of `outputs/`, the size of `knowledge/_screen-gate/dashboard.md`.
- **UNPROVEN:** whether the recall probe's quiz was run — `knowledge/_probe/session-246.json` is PLANTED (n=4,
  2026-09-04T14:10Z); no quiz result is visible at this seat and none is claimed.
- **UNPROVEN:** lane P's crops (its own UNPROVEN-1) — not eye-checked in a browser by anyone yet.
- **CLAIMED:** every figure attributed to the conductor's seat — the brief-cut 165,789, the five sub token totals, Dave's
  quoted words, the memory write, the CI #483 reading, the "probe planted" statement.

## Evidence

No evidence directory: every claim above quotes its probe inline (the inscriber's seven receipts, the mover's stdout
receipts, `_capture_gate`'s own `measure_tokens` / `boot_constant_drift_check` / `_carry_items`, `_roll_state.py`'s
generated line, `_gen_chain.py`'s generated footer, `_gm_usage.py --sizes --session 246`, `_gate_doc_rows.py --check`,
the `git` read-backs named in the stub). The wrap's scripts and ops files are in `knowledge/_tmp/wrap246/` — gitignored,
in-mount, owned by this session — and are working inputs, not receipts.

REPLAY-THESE: this file § RULING-SHAPED QUESTIONS (~700 tk) · `_CARRIES.md` § `## residual → #247`, the TWELVE new
items, the TWO strikes and the TWO corrections only (~4,500 tk) · `knowledge/_rulings.json` § `s246-D1`…`D7`, the seven
`says` fields (~1,200 tk) · `reviews/DASHBOARD-PRINCIPLES-2026-09-05-v1.html` § 05 Q8–Q16 (~900 tk)
