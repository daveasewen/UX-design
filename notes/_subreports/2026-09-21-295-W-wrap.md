# 295 · W — the wrap: the true `--wrap` path after twenty `#243` forms, the gate at ZERO, and a conductor's seam readings that did not reproduce

session: `#295` · opened 2026-09-21 · ritual 2026-09-22 · lane `W` (DELEGATED WRAP) · model Opus 5 (1M) · conductor Fable 5.1
job: run every step of `knowledge/_RUNBOOK-capture-ritual.md`, commit on the `--wrap` path if the gate allows, push, read CI back.

COUNTS: findings `12` · ruling-shaped `4` · UNPROVEN `4`

⛔ **DATE SPLIT, THE SIXTH ON THIS RUN.** Session and all eleven commits Mon 2026-09-21; ritual and
wrap commit Tue 2026-09-22 (`date` here: `Tue Sep 22 08:03:58 BST 2026`). **Nothing was re-dated** —
this report's own stem keeps the SESSION date per `s294-D11`.

---

## ⛔★★ THE HEADLINE: THE `#243` DECLARED NOT-A-WRAP FORM WAS NOT NEEDED, AND THE COMMIT WENT ON THE TRUE `--wrap` PATH

**Twenty consecutive wraps** (#275 → #294) took the `#243` form because seven inherited structural
fails blocked `--wrap` and *a wrap may not repair an inherited gate fail*. **This wrap did not.**
Measured at step 0, before any write:

```
capture gate [wrap]: 232 in scope · 2 fail · 369 warn
```

⛔ **BOTH of the two are this ritual's OWN date stamps** — `_LIVE-STATE.md` and `GOOD-MORNING.md` not
carrying today — **which are exactly the fails steps 1 and 2 exist to close**, and they are named in
the gate's own words:

```
❌ FAIL _LIVE-STATE.md: "Last refreshed" zone does not carry today (2026-09-22) — refresh it (ritual steps 1 / 2)
❌ FAIL GOOD-MORNING.md: header date zone does not carry today (2026-09-22) — refresh it (ritual steps 1 / 2)
```

⇒ ★★ **THE SEVEN INHERITED FAILS MEASURE AT ZERO.** The brief's declaration is confirmed at this
seat, independently, by running the gate rather than by inheriting a lane's close. **No gate was
weakened, none was acked, and no `*_ACK` was set at any point in this ritual.**

---

## ⛔★★ THE ONE THING IN THE BRIEF I FOUND WRONG, AND IT IS THE GAUGE

**The brief declares the conductor's seam readings as `214,824 / 23 turns` at the wrap recommendation
and `218,396 / 25 turns` at the brief's cut, both `_seam.py`, and instructs me to reproduce them off
the conductor's top-level transcript with `_checkin.read_fill`. THEY DO NOT REPRODUCE — and this is
the first wrap in six where that is true.**

`read_fill` against `/sessions/happy-magical-mayer/mnt/.claude/projects/session/e298bf6b-64b1-4de6-a59c-48c83a68d0f1.jsonl`:

```
{'available': True, 'boot': 72768, 'now': 219167, 'peak': 219167,
 'turns': 26, 'continuous': 24, 'continuity_of': 25,
 'drops': [], 'compaction_records': 0, 'skipped_synthetic': 0}
```

The per-turn cumulative `usage` series, distinct values, in order:

```
73146 · 89573 · 139458 · 152248 · 173115 · 173672 · 173854 · 185848 · 186479 · 188186 · 189557
190267 · 191070 · 192516 · 195236 · 198333 · 201856 · 202244 · 205305 · 208461 · 209874 · 212454
215019 · 216235 · 219030 · 223282
```

⛔ **NEITHER 214,824 NOR 218,396 IS IN IT.** The nearest distinct readings are **215,019** (the 23rd,
+195 against the declared 23-turn figure) and **219,030** (the 25th, +634 against the declared 25-turn
figure). **The turn ORDINALS match; the VALUES do not.**

✅ **THE BOOT AGREES EXACTLY at 72,768**, which is what identifies the window as the same one and makes
the `s214-D5` hand-over subtraction legal at all — and it is the whole reason this is a discrepancy to
publish rather than a different transcript.

⚠ **WHAT I DO AND DO NOT CLAIM.** The shape — declared figures sitting *below* the completed-turn
reading at the same ordinal, on both readings, in the same direction — is **consistent with** the
declarations being taken mid-turn rather than off a completed `usage` record. ⛔ **That is a reading of
the difference, not a claim about its cause**, and I did not diagnose `_seam.py` [[measuring-tool-must-not-guess]].
**Both series are published in the stratum, the delta, the handoff and the hook; neither is rewritten.**

★ **The sibling reading points the same way.** Measured / declared `subs`: I **165,794 / 166,974** ·
P **132,012 / 133,668** · I2 **127,516 / 129,194** · I3 **265,483 / 267,906** — **the declaration is
HIGH on all four and LOW on none, by 6,937 (0.99%)**, which is the **same direction** as #294's
systematic 8,353 (0.59%) across eight lanes. ⇒ **a property of how the conductor's figures are taken,
not of any lane** — and #294's stratum had already written that down as *"the conductor's figures were
taken one turn later than `read_fill`'s newest `usage` record"*. **This wrap's reading is the opposite
sign of that sentence and both stand unreconciled.**

---

## ⚙ THE MEASUREMENTS, ALL FIRST-HAND

| | measured here | declared in the brief |
|---|---|---|
| boot (first turn) | **72,768** | 72,768 ✅ **agrees to the token** |
| FILL / peak | **219,167** | — |
| turns | **24 continuous of 26** | 23 / 25 at the two seams |
| seam @ wrap recommendation | **not present; nearest 215,019** | 214,824 |
| seam @ brief cut | **not present; nearest 219,030** | 218,396 |
| subs total | **690,805 (n=4)** | 697,742 (n=4) |
| rulings | **638** (`json.load`, ids `s295-D1`…`D4` newest) | 638 ✅ |
| remote HEAD | **`5e9e2cbe`** (`git ls-remote`) | `5e9e2cbe` ✅ |
| unpushed at open | **0** (`git log origin/master..master` empty) | — |

**`wrap-handover: brief-cut 218,396 · sub-cut 219,167 · delta 771 · replay unobservable`** — the
**smallest** hand-over on the `s214-D5` record (#294: 9,453 · #293: 1,871 · #292: 954). Cause named:
**no conductor turn falls between the brief's cut and this reading.** ⚠ **A legal subtraction whose
identification rests on the BOOT alone**, because the seam readings do not agree — weaker evidence
than the last three, and said so in the stratum rather than left to be inferred.

**BANDS:** 39,167 past the 180,000 quality line — **DECLARED**; **200,000 exceeded by 19,167 —
DECLARED**; 220,000 clear by 833; 256,000 clear by 36,833. ⛔ **No literal moved.**

★★★ **HE STOPPED ON THE INSTRUMENT.** The 214,824 / 23 reading was quoted to him past the 180,000 line
with the wrap recommended and **his whole reply was *"wrap"***. ⇒ **`s283-D1`'s override shape STAYS AT
n=2. Nothing is added.**

---

## ✅ WHAT I VERIFIED OF THE BRIEF, ITEM BY ITEM

- **Rulings 634 → 638** — ✅ `json.load`, id and count only, never a printed record.
- **Everything pushed to `5e9e2cbe`** — ✅ `git ls-remote origin refs/heads/master` equals local HEAD;
  `git log origin/master..master` is empty. ⛔ **The remote URL and its token were never printed.**
- **`_gen_chain.py --check` is fixed (lane I3)** — ✅ **VERIFIED AT THIS SEAT, on a tree whose HEAD had
  advanced two commits past the chain's own**: `✅ _CHAIN.md is FRESH at the commit it was generated at
  (c896bcaf) — HEAD has since advanced to 5e9e2cbe`, exit 0. ⇒ **`_HANDOFF-145` § POST-WRAP ADDENDUM 4
  is STRUCK with that as the receipt.**
- **Wrap gate 7 → 0 STRUCTURAL** — ✅ measured at step 0 (above), not inherited.
- **Three dirty other-seat paths** — ✅ **`notes/_dream/_GRADE-DECISIONS.jsonl`,
  `notes/_lanes/293/J7-IDEA-jev-selects-over-the-kg.md` and `notes/_lanes/294/WRAP-MEMORY-HOOK.md` are
  ALL STILL DIRTY** at this seat, exactly as the brief asks me to check. A fourth,
  `notes/_lanes/295/DAVE-RULINGS-2026-09-21.md`, was also dirty at the open (the conductor appended to
  it after the last lane commit) and **is mine to stage** — it is this session's own record.
- **`_render_rulings.py --check`** — ✅ FRESH, sha256 `96ee3fce…`, before and after.
- **`check_preflight` on the generated line** — ✅ **ZERO FAILS**, one note. ⛔ **Graded with
  `check_preflight`, the arm the gate actually calls — NOT `check_preflight_tokens`**, per
  `_HANDOFF-145`'s named trap.

---

## ⛔ WHAT I FOUND WRONG IN THE BRIEF — SAID PLAINLY

1. ⛔★★ **THE SEAM READINGS.** Above. **Two declared figures, neither in the transcript's own series.**
2. ⚠ **"subs DECLARED: I 166,974 · P 133,668 · I2 129,194 · I3 267,906 = 697,742".** The arithmetic of
   the declaration is correct; **every one of its four terms reads high against the transcript.**
3. ⚠ **"the `post-mortem #295:` line in `notes/_GAUGE-LOG.md`".** ⛔ **The runbook homes this session's
   post-mortem in `GOOD-MORNING.md`'s `### ⏱ SESSION STRATA` block and rolls it to
   `notes/_GAUGE-LOG.md` at the NEXT wrap's 2f** — which is what `roll_2f` did here for **#294's**.
   Writing #295's directly into the log would have produced a key the duplicate-key guard refuses at
   #296. **The brief says the runbook is the authority on mechanics, so the runbook was followed and
   the divergence is declared rather than absorbed.** The `s241-D2` requirement the brief actually
   cares about — **the first-turn figure stated ONCE** — is satisfied: `72,768` appears once in a
   `POST-MORTEM #295` line.
4. ⚠ **"#294 OWED item 9 (boot ceiling arithmetic)" has no headline segment of its own in
   `_CARRIES.md`'s `residual → #295` set** — the #294 banner cap merged it away, as that wrap's own
   stratum records. **It is therefore struck at the HANDOFF level, in `_HANDOFF-146` § THE STRIKES, and
   the absence of a carries-level home is stated there** rather than the strike being silently skipped
   or attached to a clause. ⛔ **`s183-D1`: a strike lands on a headline, never a clause.**
5. ⚠ **"7 inherited fails → 0 STRUCTURAL (measure it at step 0)".** Measured: **2 fails**, both the
   ritual's own date stamps, **0 inherited**. The brief's number is right about what it names; the
   gate's raw count at step 0 is 2 and this report publishes the raw count as well as the reading.

---

## THE RITUAL, STEP BY STEP

**1 / 1b.** `_LIVE-STATE.md` refreshed with a new `*Last refreshed: 2026-09-22*` stamp and a full
⏱ LATEST DELTA. Dossier written:
`_DECISION-HISTORY/2026-09-21-295-the-push-landed-and-the-wrap-gate-reached-zero.md` (20,339 B), with
`provenance:` and `status:` fields and both-way links. `_validate_standing_instructions.py`: **PASS,
28 standing docs reachable.**

**2.** `GOOD-MORNING.md`: a **SIXTH DATE-SPLIT header line added BY ADDITION**, the five standing ones
byte-untouched — the `s294-D11` shape. New ★ LATEST banner; #294's renamed to ★ PRIOR.

**2c.** #293's ★ PRIOR banner MOVED VERBATIM to `_GM-ARCHIVE.md` § `Batch 2026-09-22 #295` (11 lines,
mover receipt). **EXIT CHECK run over it before the move**: its ⚠/⬛ items all have standing homes in
`_CARRIES.md` § `residual → #296` at their true ages.

**2d.** #292's ⏱ PRIOR DELTA MOVED VERBATIM to `_LIVE-STATE-ARCHIVE.md` § `Rolled 2026-09-22 #295`
(28 lines), **and #290's `Previous:` chain segment moved with it at the same boundary**, with a trim
note left in place naming where it went. Both through the mover, one transaction, all-or-nothing.

**2e.** ⛔ **RAN AND MOVED NOTHING, AND THE SIZE OF THAT IS STATED:** DO-FIRST carries **no
session-keyed stratum at all** — every lifecycle tag in it is an `until: <condition>` form, not an
`until: <session>` form — so the `LATEST + 1` roll has **no eligible member** and the section is
unchanged at **10,140 tape**.

**2f.** `roll_2f` session 294: **34 lines → `notes/_GAUGE-LOG.md`** (appended at true EOF, no anchor)
and **2 lines → `_GM-ARCHIVE.md`** under the newest batch. `gauge_log_continuity` now sees `#### 2026-09-21 #294`.

**2g.** `_build_memento_index.py` run **LAST**, after every GM/LS edit was final.

**3.** ⛔★★ **THE STORE WRITE WAS ATTEMPTED AND IT SUCCEEDED — this seat placed it, and the write is
NOT owed to the conductor.** `wrap-295-the-push-landed-and-the-wrap-gate-reached-zero.md` written
(9,520 B) and the index line placed at the TOP of `index.md` (14,268 → 19,918 of 49,152 B). ⇒ **Third
consecutive session in which a sub could reach the store**; the runbook's *"a sub-lane cannot"* is
wrong and its correction is ruling-shaped. ⚠ **DECLARED SKIP WITH ITS SIZE:** the index now holds
**FOUR** lines, not the standing three — `MEMORY-ARCHIVE-2` is at **45,733 of 49,152 B** and cannot
take the #292 line (**≈5,900 B**) whole, so the standing rule says open `MEMORY-ARCHIVE-3`, and
**hand-transcribing a 5,900-character marked-up line from a wrap seat is the act most likely to
corrupt the record it exists to preserve.** There is **no cap pressure** (19,918 of 49,152). Owed at
#296's opener. **The `hook_open_items_recheck` arm was asked TWICE**, per `_HANDOFF-145` § POST-WRAP 2
— *"ask the arm twice, or inherit its silence"*.

**4 / 4b.** Doc rows minted through `_state.add()` with **true** close conditions, before staging.
Titles regenerated by `_gen_titles.py` after the banner.

**4c.** `_gate_scratch_hygiene.py --clean --wrap` run, **and `pip install tiktoken
--break-system-packages` re-run after it** — the sixth consecutive wrap to meet that.

**4d.** `_render_rulings.py --check`: **FRESH** at both ends. No re-render was needed — this wrap
inscribed nothing.

**5 / 5b.** See § THE COMMIT and § POST-WRAP ADDENDUM.

---

## THE LOCKS

Every `.git/index.lock` met at this seat was **`mv`'d on-device** to
`notes/_lanes/_orphan-locks/stale-<name>-295-wrap-<HHMM>`. ⛔ **NOTHING WAS EVER `rm`'d** — this mount
refuses `unlink` (`s282-D4`), and git's own
`warning: unable to unlink '…/.git/index.lock': Operation not permitted` is **git failing to clean up
after itself, not a foreign lock.** ⚠ **One was already stranded at the open, before this seat ran any
git command — the FIFTH consecutive session this has been measured**, and `git status` alone strands
one at the fourth.

---

## RULING-SHAPED QUESTIONS

1. ⛔★★ **THE CONDUCTOR'S DECLARED FIGURES AND `read_fill` DISAGREE, AND UNTIL #295 THEY NEVER DID.**
   #291–#294 reproduced every declared seam reading **to the token**; #295 reproduces neither, while
   the boot agrees exactly and the `subs` declarations are systematically high on every lane. ⇒ **Is
   `_seam.py`'s reading taken at a different instant from `read_fill`'s, and if so which one is the
   `s214-D5` field's unit?** ⛔ **The `wrap-handover` `delta` is a subtraction of one against the
   other, so this is not cosmetic — it is the field's definition.** No diagnosis was attempted from
   this seat and none is claimed.
2. ⛔★ **`s295-D1` NARROWED THE ARM, AND THIS WRAP IS THE FIRST STRATUM WRITTEN UNDER THE NARROWED
   RULE.** My stratum states `72,768` once in its `POST-MORTEM #295` line and restates it in the
   pre-flight line and the boot-ceiling comparison — **legal under the narrowing, illegal under the
   pre-#295 arm.** ⇒ **Is the pre-flight line's boot figure intended to be outside the count, or is it
   inside and merely un-charged because it is not a post-mortem line?** The ruling reads the way the
   code behaves; **nobody has said out loud that a stratum now legally carries the figure three
   times.**
3. ⚠ **THE BANNER CAP CLOSED AT EXACTLY 1,200 OF 1,200 FOR A THIRD CONSECUTIVE WRAP** — 1,633 tape
   drafted, **seven** shortening passes, and **three things compressed out and named** (the standing
   ⓪ DATE-SPLIT bullet merged into ①; lane P's verbatim mechanism quote reduced to a pointer; his own
   *"we need to get back to the presentation soon"* moved off the residual line to `_CARRIES.md` ①).
   ⇒ **#292's, #293's and #294's complaint is literal for a FOURTH session.** Reported, not appealed.
4. ⚠ **THE RUNBOOK SAYS A SUB CANNOT REACH THE MEMORY STORE AND A SUB HAS NOW REACHED IT THREE
   SESSIONS RUNNING.** ⇒ **amend § step 3 to read *reserved to the conductor* rather than
   *unreachable from a sub*?** Third session this has been raised.

### counts

- rulings inscribed **0** (this wrap inscribed nothing) · store **638**, unchanged and verified
- gate: **2 fails at open → see § POST-WRAP for the close**; **0 INHERITED at both ends** · gates
  weakened **0** · gates acked **0** · `*_ACK` set **0** · constants moved **0**
- mover transactions **8**, ops **20**, every ops file uniquely named under the gitignored
  `knowledge/_tmp/` with `os.path.exists` + a size floor asserted **in the writing process**
- `_CARRIES.md` § `residual → #296`: **642 probeable items, 13 new, 2 struck with receipts** (probe
  run, not typed)
- ★ LATEST banner: **exactly 1,200 of 1,200 tape**, 8 substantive lines of 10, measured with the
  gate's own arm
- memory store: **2 writes, both landed** (body 9,520 B; index line, file 14,268 → 19,918 B) ·
  **1 declared skip with its size**
- locks `rm`'d **0** · locks `mv`'d on-device: see § THE LOCKS

### UNPROVEN, named rather than implied

1. **THE WHOLE CAPTURE-GATE SUITE.** `--selftest` exceeds the ~165 s sandbox call wall at this seat as
   at every other; **CI is the only asker and it returns exit 1.** Not run here, not claimed.
2. **THE CAUSE OF THE SEAM DISCREPANCY.** Measured, published, **not diagnosed.**
3. **THAT `f81bbdd4` IS THE RIGHT SHA FOR ALL TEN** back-stamped #294 rulings. Taken from lane I3's
   drive over the commit's own diff and message — stronger than lane I's inherited claim — **but not
   verified ruling-by-ruling at this seat.**
4. **THAT 72,768 HOLDS.** His own word is ***"possibly"***, n=1, and **the first post-#295 cold boot —
   the first reading the re-based ceiling will actually be judged on — has not happened.**

REPLAY-THESE: `python3 knowledge/_capture_gate.py --wrap` (expect the closing count in § POST-WRAP) · `python3 knowledge/_roll_state.py` (expect `2c OK (banners 2/2) · 2d OK (deltas 3/3) · 2f OK (strata 1, log #294)`) · `python3 knowledge/_gen_chain.py --check` (expect exit 0) · `python3 knowledge/_render_rulings.py --check` (expect FRESH) · `python3 -c "import json;r=json.load(open('knowledge/_rulings.json'))['rulings'];print(len(r),r[-1]['id'])"` (expect `638 s295-D4`) · `git ls-remote origin refs/heads/master` (expect the wrap sha) · `PYTHONPATH=knowledge python3 -c "import _capture_gate as c;print(len(c._carry_items([l for l in open('_CARRIES.md') if '#296:**' in l][0])))"` (expect **642**) · `python3 knowledge/_checkin.py --preflight-line 295`

## RULING-SHAPED QUESTIONS — WHAT I FOUND WRONG IN MY OWN BRIEF

See § WHAT I FOUND WRONG IN THE BRIEF above — five items, the first of which is the gauge itself.
