# `#238`-`P` — the polarity gate: one home, five refusals, proved on the real rows

session: `#238` · 2026-09-02
window: Fable conductor, lane P (build + wire + mutation self-test)
sub index: `P`
brief: `notes/_briefs/2026-09-02-238-P-polarity-gate-brief.md`
tokens: `UNMEASURED — no message.usage at a sub's seat`. SHAPE: ~70 tool calls · 1 migration generator + 1 validator-generator written · 55-arm selftest run 8 times while it was built (3 defects in the arms found and fixed, none in the gate) · the seam harness run 3 times (44 arms) · 3 independent probe scripts filed and run · 0 git commands · 0 writes to `_rulings.json` / `_state.json` / spine / memory · seat `wonderful-adoring-euler`.

## VERDICT

All six deliverables are **DONE** and every claim below names its probe. The home is `knowledge/brain/` — **DECLARED by this lane**, because `_PLAN-designers-brain-2026-09-02-v1.html` §P1 names no path (`grep -c "knowledge/brain"` → 0). The 145-row register is seeded byte-for-byte (sha256 `c0165ace…` on both sides, `cmp` silent). The 30 rows are migrated by a GENERATOR (`_migrate_tensions.py`, its judgement declared as data) into **30 polarity nodes · 68 parties · 15 declared stubs · 21 typed links** (resolvedBy 7 · explainedBy 1 · challengedBy 4 · touches 9), `side_a/side_b/apollo_touch` gone, two dangling ids resolved with receipts. The validator-generator `knowledge/_validate_polarities.py` reads the schema file as its grammar, asks the five refusals of `s238-D7` (named, rc 1, nothing written), derives the three generated files with a clock and checks them by CONTENT with the clock held, and carries a textual writer proven by reconstruction. Its `--selftest` drives **55 arms** through the same `gate()` the build and the seam call, on copies of the real rows: **45 red arms, 45/45 red by name**, 7 green, 3 no-fire/77. The gate is wired as `_build_all.py` STEPS **[141]/[142]** (appended; join total; the survey asks them) and as a block in `_git_commit.sh` — both driven green on the real files and red on a mutated copy without a commit, and the seam harness is green at 44/44 with two new arms. **Two things are honest rather than green:** the `Defaults used:` declaration declares **0** of the 20 open polarities because the T asset carries no per-row "which side is conservative" datum (rule printed, 20 rows UNPROVEN); and the typed migration lands the sort at **6 · 4 · 20**, not the `6-9-15` quoted in `s238-D6` — that figure was the naive variant lane T warned would close four rows wrongly.

COUNTS: findings `15` · ruling-shaped `8` · UNPROVEN `6` · rows 30 · parties 68 · stubs 15 · links 21 (resolvedBy 7 · explainedBy 1 · challengedBy 4 · touches 9) · refusals 5 · selftest arms 55 (red 45/45) · generated files 3 · pitfalls 10

## What was done

**Ground.** The seven `s238` rulings and `s237-D1/D2/D5/D9`, `s219-D3`, `s143-D1` read from `_rulings.json` (the store, not memory); lane T's report findings 2·3·4·5·7·8·9 and its assets; `_derive_sort.py` re-run at this seat with the other seat's hardcoded path substituted → **6·3·21 reproduced byte-for-byte** (`diff` of counts + buckets against the filed `tension-sort.json`: identical); `_build_all.py` docstring + STEPS/ROUTE_ROWS contract; `_git_commit.sh` end to end; `_test_git_commit.py` and its blind-harness detector; `_inscribe_ruling.py` (the writer model); `_could_not_ask.py`; `_validate_wiring.py`; `_gen_pack_manifest.classify()`.

**1 · The home** — `knowledge/brain/`:
- `principles.json` — `cp` of the R1 register; `sha256sum` both = `c0165ace59d3364dbc8610b02808ee7b610402764c8af2441cc73c3cce53bc96`; `cmp` silent. No edits.
- `polarities.json` — GENERATED by `assets/…/_migrate_tensions.py --write` from `tensions.json` (sha256 `a3b12028…`, pinned in the file's `$migration` block). Shape per brief: `id` (`pl-NN`, the `s238-D4` rename; `tn-NN` kept under `sources[].id`), `parties[{ref, role, note?}]`, `mediating_variable`, `links[{type, ref, quote?}]`, `sources`. Deterministic: two runs `cmp` identical. 21,492 bytes.
- `stubs.json` — 15 stubs, each EXACTLY `{id, phrase}` (finding 8's phrases, verbatim).
- `schema/polarity.schema.json` — draft 2020-12 core + one house keyword `maxWords` (declared in `x-vocabulary`); `additionalProperties: false` at every level; no `status`, no judgement field; link `type` enum = the four; `parties.minItems: 2`; the four link types' semantics written into the schema `description` with their subject named (see finding 12); `x-refusals` lists the names the gate prints.
- `_generated/polarity-status.json` · `polarity-edges.json` · `defaults-declaration.txt` — written by `--write`, header `GENERATED — do not hand-edit (…)`, `generated_at`, `content_sha256` over the body.

**2 · The migration receipt** (all from `assets/…/migration-table.txt`, printed by the same function that generates):
- `pr-info-scent` → `pr-information-scent` (tn-28 → pl-28; T finding 7).
- `pr-two-red-law` → ruling `s151-D1` as a PARTY of pl-15, the R1 spelling kept in `note` (s151-D1 `says` opens "THE TWO-RED LAW, background-keyed, FIRM").
- side strings → parties by rule P1; the ruling on tn-16's side_b (finding 8) → party `s116-D1` note "data marks" + stub `st-dense-financial-tables`.
- every `apollo_touch` → typed link(s) by rules L1–L5, quote ≤15 words verbatim (the gate re-verifies verbatim-ness: 21/21).

**3 · The validator-generator** `knowledge/_validate_polarities.py` (help-gated; argv contract; rc 0/1/2/77):
(i) parses in the consumer's grammar — JSON, then the schema file itself, refusing any schema keyword it does not implement (`SCHEMA-KEYWORD-UNSUPPORTED`, arm 50); (ii) resolves every `ref` against principles + stubs + `_rulings.json` (a `superseded_by` ruling is not live); (iii) the five refusals, named: `R1-DANGLING/SUPERSEDED/SELF-PARTY/CIRCULAR/WRONG-REGISTER/UNDECLARED-STUB` · `R2-UNTYPED/UNKNOWN-TYPE` · `R3-JUDGEMENT-FIELD/QUOTE-TOO-LONG/NOTE-TOO-LONG/QUOTE-NOT-VERBATIM` · `R4-AUTHORED-GENERATED/AUTHORED-EDGES/STRAY-FILE` · `R5-TYPED-STATUS`, plus `S-*` schema refusals; (iv) derives status (rules R-OBLIGATION → R-RESOLVED → R-OPEN, ported from `_derive_sort.py`; RULE K/D fields copied per open row from `open-tensions.json`, FLOATED), the pairwise edge view, and the s238-D5 declaration; (v) `--check` re-derives with the ON-DISK clock and compares bytes (never mtime); a file without the header or failing its own sha is `R4-AUTHORED-GENERATED`, a different verdict from `STALE-GENERATED`; (vi) `--add-polarity/--add-stub FILE --dry-run|--write`: textual span insertion, reconstruction `==` original, parse to exactly one more entry equal to the submission, the WHOLE gate on the resulting text before a byte is written.

**4 · `--selftest`** — 55 arms, table in `assets/…/selftest-table.txt`; every arm copies the REAL home under `/dev/shm`, mutates one thing, and calls `gate(brain)` — the same function `--check` calls from the build and the seam; a crash is reported as a FAIL, never a pass; a refused `--write` is proven to leave every byte (tree hashes equal).

**5 · Wiring** — `_build_all.py` STEPS entries [141] `--check` (GATE, remedy names the refusal classes and `--write`) and [142] `--selftest` (ABORT), APPENDED with their ROUTE_ROWS in the same edit; `_git_commit.sh` block "POLARITY GATE — s238-D7" after the showroom gate, `POLARITY_ACK` hatch spelled like its neighbours; `_test_git_commit.py` stub `STUB_POLARITY_EXIT` + arm `polarity_gate_refusal_and_ack_s238D7` + mutation control `MUTATION_polarity_gate_block_removed_bites_s238D7`. All three files edited BY ADDITION only (lane M's W-355 blocks untouched; re-read before each edit).

**Written** (repo-relative): `knowledge/_validate_polarities.py` (new) · `knowledge/brain/{principles,polarities,stubs}.json` · `knowledge/brain/schema/polarity.schema.json` · `knowledge/brain/_generated/{polarity-status.json,polarity-edges.json,defaults-declaration.txt}` · `knowledge/_build_all.py` (+31) · `knowledge/_git_commit.sh` (+19, my block) · `knowledge/_test_git_commit.py` (+83, my stub/env/arms) · this report · `notes/_subreports/assets/2026-09-02-238-P-polarity-gate/` (13 files). Nothing else changed by this lane.

## THE COUNTS, from the gate's own output (`assets/…/check-real.txt`)

```
rows 30 · parties 68 (principle 45 · obligation 6 · ruling 2 · stub 15) · stubs declared 15
links 21 by type challengedBy=4 explainedBy=1 resolvedBy=7 touches=9 · refusals asked: R1 R2 R3 R4 R5 — none fired
quotes 21: verbatim against their source rows — 21 verified, 0 UNVERIFIED
status derived: settled-by-obligation 6 · resolved 4 · open 20   (237-T at its clock: 6 · 3 · 21)
edges derived: 22 (18 polarities carry an edge, 12 carry none; 17 parties are stubs/rulings and cannot)
D-DECLARE: open 20 · declared 0 · UNPROVEN 20
```

## THE MIGRATION TABLE — every former `apollo_touch` → typed link(s) + the quote that justifies the type

Rules (printed by `_migrate_tensions.py`): **L1** a ruling id cited in `how_it_resolves` with a resolving verb → `resolvedBy` · **L2** an `apollo_touch` saying the polarity's principle IS the ruling → `explainedBy` (the s237-D5 relation) · **L3** an `apollo_touch` saying a ruling SHOULD change → `challengedBy` · **L4** a ruling id listed with no verb, or a verb this lane cannot settle → `touches`, the least claim (it never closes an open question), marked UNPROVEN where a stronger reading exists · **L5** no ruling id in the value → no link; the words stay at the frozen R1 row, reachable via `sources`.

| R1 → node | link | rule | the quote (≤15 words, verbatim, gate-verified) |
|---|---|---|---|
| tn-01 → pl-01 | `explainedBy s151-D1` | L2 | "The law IS a Von Restorff budget with a legal contrast floor attached." |
| tn-02 → pl-02 | `touches s217-D5` | L4 ⚠ UNPROVEN | "three types with ruled option sets is exactly this trade, made once" — T finding 2 reads a resolution; this lane reads an INSTANCE of the trade |
| tn-03 → pl-03 | none | L5 | no ruling id ("Chart hover latency and skeleton states…") |
| tn-04 → pl-04 | none | L5 | no ruling id ("Apollo's review pages are gorgeous and ruled by eye…") |
| tn-05 → pl-05 | none | L5 | no ruling id ("#235's behaviour address" is a session, not a ruling) |
| tn-06 → pl-06 | none | null in R1 | |
| tn-07 → pl-07 | `resolvedBy s116-D1` · `touches s114-D3` · `touches s114-D5` | L1 · L4 · L4 | "Split the rule by object class, which Apollo already did: s116-D1 exempts data marks" · "s114-D3/D5 hit-area sweep" |
| tn-08 → pl-08 | `touches s135-D1` | L4 | "the shell exists; the ROUTING RULE does not" |
| tn-09 → pl-09 | none | null in R1 | |
| tn-10 → pl-10 | none | null in R1 | |
| tn-11 → pl-11 | `challengedBy s234-D2` · `challengedBy s234-D6` | L3 | "The factory's quality rubric (s234-D2/D6) should carry consequence, not just frequency." |
| tn-12 → pl-12 | none | null in R1 | |
| tn-13 → pl-13 | none | null in R1 | |
| tn-14 → pl-14 | `challengedBy s217-D2` · `challengedBy s217-D5` | L3 | "Bento module counts (s217-D2/D5) should be justified by grouping and scanning, NOT by 7±2." |
| tn-15 → pl-15 | `resolvedBy s151-D1` · `touches s149-D1` · `touches s194-D1` | L1 · L4 · L4 | "Background-keyed colour rules, which is exactly what s151-D1 does" · "s149-D1 (mono error ink camp)" · "s194-D1 (contrast duty attaches to label and glyph, not chrome)" |
| tn-16 → pl-16 | `resolvedBy s234-D3` · `resolvedBy s116-D1` | L1 | "adopt 2.5.5 for controls (s234-D3)" · "exempt marks explicitly with a named check (s116-D1)" |
| tn-17 → pl-17 | `resolvedBy s202-D1` · `touches s217-D2` | L1 · L4 | "s202-D1 inherits console's tuned dimensions across the square themes" · "s217-D2 (gutter is the only divergence)" |
| tn-18 → pl-18 | none | L5 | no ruling id |
| tn-19 → pl-19 | none | L5 | no ruling id |
| tn-20 → pl-20 | none | L5 | no ruling id |
| tn-21 → pl-21 | none | L5 | no ruling id ("series-3 4.61:1 and the hover token set" names no ruling) |
| tn-22 → pl-22 | `resolvedBy s217-D8` | L1 | "keyline HUGS the module rather than sitting centred in the gutter, which resolves this precisely" |
| tn-23 → pl-23 | none | null in R1 | |
| tn-24 → pl-24 | none | null in R1 | |
| tn-25 → pl-25 | none | L5 | no ruling id |
| tn-26 → pl-26 | none | null in R1 | |
| tn-27 → pl-27 | `resolvedBy s234-D6` · `touches s234-D2` | L1 · L4 | "Dave already leans this way: 'mechanical over inference' (s234-D6)" · "s234-D2 factory goal" |
| tn-28 → pl-28 | none | null in R1 | (party rename receipt: `pr-info-scent` → `pr-information-scent`) |
| tn-29 → pl-29 | `touches s234-D3` | L4 ⚠ UNPROVEN | "Apollo adopted 2.4.13 at s234-D3" — "adopted" could read as resolvedBy; status is settled-by-obligation either way |
| tn-30 → pl-30 | none | L5 | no ruling id ("#234's generation chain"; T finding 9's s237-D4 is not named by the row — not guessed, see Q7) |

Totals from the table: 21 links · 2 UNPROVEN link types · 9 rows with no ruling id · 9 rows null in R1.

## THE SELFTEST ARM TABLE (`assets/…/selftest-table.txt`, rc 0)

```
#  result  expect  rc  named  arm
 1 PASS green  0  -    CONTROL: the real 30 rows, untouched
 2 PASS red    1  yes  R1 dangling PARTY ref (pr-does-not-exist)
 3 PASS red    1  yes  R1 dangling LINK ref (s999-D9)
 4 PASS red    1  yes  R1 link to a SUPERSEDED ruling (s129-D1, real store)
 5 PASS red    1  yes  R1 PARTY that is a superseded ruling (s129-D1)
 6 PASS red    1  yes  R1 SELF-PARTY (pl-01 lists itself)
 7 PASS red    1  yes  R1 CIRCULAR polarity→polarity LINK (pl-01 → pl-02)
 8 PASS red    1  yes  R1 CIRCULAR polarity as a PARTY of another (pl-02 on pl-01)
 9 PASS red    1  yes  R1 link to a PRINCIPLE id (wrong register)
10 PASS red    1  yes  R1 undeclared STUB (st-not-declared)
11 PASS red    1  yes  R1 a declared stub REMOVED from stubs.json (pl-05's party dangles)
12 PASS red    1  yes  R1 homoglyph ref (Cyrillic а in pr-jakobs-law) — non-ASCII id
13 PASS red    1  yes  R2 UNTYPED link (type key absent)
14 PASS red    1  yes  R2 UNKNOWN (fifth) link type `relatedTo`
15 PASS red    1  yes  R2 link type wrong case `ResolvedBy`
16 PASS red    1  yes  R2 link type empty string
17 PASS red    1  yes  R3 judgement field `how_it_resolves` on a node
18 PASS red    1  yes  R3 judgement field of ANOTHER name (`verdict`)
19 PASS red    1  yes  R3 judgement smuggled into a link `quote` (16 words)
20 PASS red    1  yes  R3 judgement smuggled into a party `note` (16 words)
21 PASS red    1  yes  R3 judgement field inside a PARTY object (`why`)
22 PASS red    1  yes  R3 stub with a third key (`rationale`)
23 PASS red    1  yes  R3 link quote PARAPHRASED (not verbatim in the source row)
24 PASS red    1  yes  R4 AUTHORED file at the generated path (no header)
25 PASS red    1  yes  R4 generated file HAND-EDITED under its header (self-sha breaks)
26 PASS red    1  yes  R4 AUTHORED edge file beside the homes (knowledge/brain/edges.json)
27 PASS red    1  yes  R4 STRAY second shape beside the homes (polarities-v2.json)
28 PASS red    1  yes  R4 AUTHORED file INSIDE _generated/ beside the derived three
29 PASS red    1  yes  R4 defaults-declaration.txt HAND-EDITED (a default declared by hand)
30 PASS red    1  yes  R5 typed `status` on a node
31 PASS red    1  yes  R5 typed status under another spelling (`derivedStatus`)
32 PASS red    1  yes  S 1 party (minItems 2)
33 PASS red    1  yes  S DUPLICATE party (same ref twice on pl-01)
34 PASS red    1  yes  S DUPLICATE polarity id
35 PASS red    1  yes  S unicode polarity id (fullwidth digit)
36 PASS red    1  yes  S party role outside the enum (`side_d`)
37 PASS red    1  yes  S ONE-SIDED: every party on side_a (no pull)
38 PASS red    1  yes  S mediating_variable empty
39 PASS red    1  yes  S sources missing
40 PASS red    1  yes  S polarities.json does not parse
41 PASS red    1  yes  FRESHNESS: home edited, generated files not re-derived → STALE
42 PASS red    1  yes  FRESHNESS: a generated file deleted → MISSING
43 PASS red    1  yes  WRITE: --write on a refused home writes NOTHING (hashes equal)
44 PASS green  0  yes  WRITE: stale → --write → fresh (rc 1 → 0 → 0)
45 PASS green  0  yes  WRITE: determinism — two writes differ only in generated_at
46 PASS green  0  yes  ADD-POLARITY: dry-run leaves bytes; --write = original + one span; 31 rows; gate green after
47 PASS red    1  yes  ADD-POLARITY: an entry with a typed status is REFUSED by name, nothing written
48 PASS green  0  yes  ADD-STUB: good stub appended textually; 3-key stub REFUSED (S-STUB-SHAPE)
49 PASS nofire 1  -    BITE-THE-BITE: with minItems removed from the SCHEMA the 1-party refusal does NOT fire — the schema file drives the check
50 PASS red    1  yes  SCHEMA: a keyword this gate does not implement (maxItems) is REFUSED, never silently ignored
51 PASS green  0  yes  CLI: `--check` rc 0 on the real home, rc 1 + R2-UNTYPED on the mutant (via --brain AND via POLARITY_BRAIN_DIR)
52 PASS red    2  yes  CLI: bare and unknown argv refuse with rc 2 (argv contract, #208 class)
53 PASS 77    77  yes  COULD-NOT-ASK: home directory absent → rc 77 + marker naming the path
54 PASS 77    77  yes  COULD-NOT-ASK: declaration source absent → 77 on a clean home, but a real refusal is STILL rc 1 first
55 PASS green  0  yes  WIRING: STEPS has --check + --selftest rows; _git_commit.sh invokes --check on a live line; the seam harness stubs it
arms 55 · red arms 45 (went red by name 45/45) · green arms 7 · no-fire/77 arms 3 · failures 0
```

## Findings

1. **The home is `knowledge/brain/`, declared here.** Probe: `grep -c "knowledge/brain" _PLAN-designers-brain-2026-09-02-v1.html` → `0`; `grep -o "[a-z_/]*brain/…"` → no hits; repo-wide the only reference was this lane's brief. §P1 says "the schema written down" and names no path.
2. **Lane T's sort reproduces at this seat.** `_derive_sort.py` re-run with the other seat's hardcoded `dreamy-relaxed-noether` path substituted and `ASSETS` redirected to scratch: counts + per-row buckets `diff` identical to the filed `tension-sort.json` (`SORT-REPRODUCED-IDENTICAL`). Its probe also names the two dangling party ids this lane resolves: `pr-two-red-law` (tn-15) and `pr-info-scent` (tn-28).
3. **The typed migration lands 6 · 4 · 20, not 6 · 9 · 15.** `s238-D6` quotes "this one choice moves the sort 6-3-21 to 6-9-15" — that was lane T's SENSITIVITY figure for the naive variant that read every `apollo_touch` id as a resolution, four of which "would close wrongly" (T finding 2). Typed by the L-rules, exactly one open row closes: tn-22 (`s217-D8`, whose `how_it_resolves` says "resolves this precisely"). Probe: `assets/…/independent-readers.txt` — a recount straight from the frozen R1 rows gives obligation rows == generated == T's (`['tn-15','tn-16','tn-19','tn-20','tn-25','tn-29']`) and resolved = T's three + `tn-22`. The generated status file carries this delta in `delta_vs_237T` so a later reader does not take the ruling's parenthetical as the expected count.
4. **Five refusals, 45 red arms by name, through the real entry point on the real rows.** Every arm copies `knowledge/brain/` and calls `gate()` — the one function `--check` calls — asserting rc 1, the refusal NAME in the output, no traceback, and that the mutated tree's hashes are unchanged after the run (nothing written on refusal). Probe: `selftest-table.txt`, line `red arms 45 (went red by name 45/45)`.
5. **An independent reader agrees.** `jsonschema 3.2.0` is installed here: the schema is metaschema-valid, the 30 real nodes give 0 errors, and it refuses 10/10 of the same structural mutants (untyped link · fifth type · judgement field · typed status · 1 party · bad role · unicode id · extra party key · missing sources · non-ASCII ref). Probe: `assets/…/_probe_independent.py` → `independent-readers.txt`. What it cannot cross-check is declared: R1 resolution and `maxWords` are this gate's own.
6. **The schema file drives the check — bitten both ways.** Arm 49 removes `minItems` from the SCHEMA copy and the 1-party arm stops firing (so the rule lives in the file, not in code); arm 50 adds a keyword the gate does not implement (`maxItems`) and the gate REFUSES (`SCHEMA-KEYWORD-UNSUPPORTED`) instead of half-enforcing the file.
7. **Hand-edited and stale are different verdicts.** A generated file lacking the header or failing its own `content_sha256` is `R4-AUTHORED-GENERATED` (arms 24, 25, 29) and is refused even by `--write` — nothing is blown away; a self-consistent file whose body differs from a fresh derivation at its own clock is `STALE-GENERATED` (arm 41) and `--write` cures it (arm 44). Determinism: two writes differ only in `generated_at` (arm 45).
8. **The gate is a consumer of every commit — driven, not asserted.** (a) `_build_all.py`: `check_routes()` → 142 steps, join total; the two rows are the LAST two entries; `_gen_chain._steps_in` → (142, 142) and `_gen_schematic.build_rows` → 142 (one slicer); the two entries driven in the build's own `[sys.executable, path] + args` form: rc 0 on the real home, rc 1 + the GATE remedy naming `R5-TYPED-STATUS` on a mutated copy (`build-step-drive.txt`); `_build_survey.py --range 141:142` → `2 pass` (`survey-range-141-142.txt`). (b) `_git_commit.sh`: the block extracted verbatim between its marker and `fi` and executed with `fail()` defined — (a) real home rc 0, (b) mutated copy via `POLARITY_BRAIN_DIR` rc 1 with `R2-UNTYPED` named and `fail` fired, (c) the same mutant under `POLARITY_ACK` rc 0 DECLARED (`seam-probe.txt`). (c) `_test_git_commit.py --selftest` → `ALL GREEN — 44 arms (incl. 9 mutation controls)`, including `polarity_gate_refusal_and_ack_s238D7` (seen to run with `--check`; red blocks; hatch passes named; the hatch does not re-ask) and its mutation control (block stripped → arm red) (`seam-harness.txt`); `_gate_harness_stubs.py` → green.
9. **The declaration declares nothing, honestly.** `open-tensions.json` has no per-row field for "which side is conservative" (`grep -i conservative` over the T assets: only `_derive_open_table.py`'s docstring, which asserts every default "takes the conservative side" as prose). Rule printed in `defaults-declaration.txt`: bent = UNKNOWN ⇒ 0 declared ⇒ 20 UNPROVEN. The default TEXTS stay FLOATED in the source and in `polarity-status.json` (`factory_default`, the grill's sense named per pitfall 5), never in the declaration.
10. **Quotes are receipts, and the gate checks they are.** Every link quote is ≤15 words and a verbatim substring of its node's source row, verified by the gate against `sources[].path`/`id` (21/21); a paraphrase is `R3-QUOTE-NOT-VERBATIM` (arm 23). Where a source is unreachable the quote is printed UNVERIFIED, declared.
11. **The third verdict is wired, and it stays biting.** Home absent → rc 77 + `COULD-NOT-ASK:` naming the path (arm 53); `open-tensions.json` absent → 77 on a clean home but a real refusal is STILL rc 1 first (arm 54). `_gen_pack_manifest.classify()` run in-process on the real refusal text → `('REPO-BOUND', 'refuses for knowledge/brain-not-shipped/polarities.json, a repo resource the pack does not ship…')` (`pack-classifier-reading.txt`) — so a re-cut pack will not ship this gate as RUNNABLE and then go red on an environment fact (#173).
12. **Two of the four link types are worded from the ruling's side.** `resolvedBy` reads polarity→ruling ("the polarity is resolved by"); `explainedBy` and `challengedBy` read ruling→polarity ("the ruling is explained/challenged by this polarity" — s237-D5's direction and T finding 2's wording "challenged by the tension"). The names are RULED (`s238-D6`) and were not touched; the semantics are seated in the schema's `links.description` with the subject named for each. Ruling-shaped Q1.
13. **Two rows put a ruling on a side AND cite it as the route.** pl-15 (`s151-D1` party + `resolvedBy`), pl-16 (`s116-D1` party + `resolvedBy`) — inherited from R1's own structure (T finding 8). Left as one fact, no refusal (a sixth clause is not ruled); Q2.
14. **The pre-existing wiring red is not this lane's.** `_validate_wiring.py` was red BEFORE this lane on `ORPHAN: _validate_receipt.py` (#235's) and is red after for the same single reason; the wired count moved 49 → 50 (this gate). Probe: `wiring-and-helpgate.txt`; the baseline run is quoted in this lane's transcript.
15. **Sandbox: `/dev/shm` does not survive a tool-call boundary at this seat.** A migration dry-run written to `/dev/shm/p/brain-dry` in one call was gone at the next (`cmp: No such file or directory`); persistent scratch went mount-side (`mnt/outputs/p238/`). `TMPDIR=/dev/shm` is correct WITHIN a call — the selftest's copies live and die inside one `gate()` run. Timing: `--check` 0.05 s (3 runs), `--selftest` ~1.4 s, seam harness 6.3 s.

## RULING-SHAPED QUESTIONS

⛔ **MANDATORY SECTION.** Nothing below is decided. Each is Dave's.

1. **Link-type subjects.** The ruled names mix subjects (finding 12). (a) keep the four names as ruled, semantics seated in the schema description (as built); (b) rename `explainedBy → explains`, `challengedBy → challenges` so every type reads polarity→ruling. Recommend **(a)** — a rename re-opens `s238-D6` for a wording gain; the schema now says what each asserts.
2. **A ruling that is both a party and the route** (pl-15, pl-16). (a) allow, advisory (as built); (b) refuse as a sixth clause; (c) fold the party into the link's `quote`. Recommend **(a)** — it is R1's own reading and refusing it would edit a frozen row's meaning.
3. **pl-02's `s217-D5`: `touches` (as migrated) or `resolvedBy` (T finding 2)?** Moves the sort 6·4·20 → 6·5·19 and removes tn-02's default from the declaration set. Recommend Dave's eye on the quote: "three types with ruled option sets is exactly this trade, made once" — an instance or the rule?
4. **pl-29's `s234-D3`: `touches` or `resolvedBy`?** Status is settled-by-obligation either way; only the link's meaning moves.
5. **A descriptive `pull` field on the node?** The brief's shape carries none; `the_pull` and `how_it_resolves` stay at the frozen R1 row reachable via `sources` (ADR-0017). (a) none, as built; (b) add `pull` (descriptive, ≤N words, not a judgement) for retrieval. Recommend **(a)** until a consumer needs the words on the node.
6. **The FLOATED defaults' live home.** The gate derives the declaration from a dated T asset; when Dave rules the four ask-whens (`s238-D3`) the proposals need a home that can carry a `conservative_side` datum. (a) `knowledge/brain/floated-defaults.json` (added to the gate's closed set); (b) keep deriving from the T asset until then. Recommend **(a)** at that moment, not before.
7. **pl-30 and `s237-D4`.** T finding 9 says s237-D4 "substantially answers" tn-30; the row never names the id, so no link was minted (not guessed). Add `resolvedBy s237-D4`? Recommend yes — after Dave reads the row; it would move the sort to 6·5·19 (or 6·6·18 with Q3).
8. **`_validate_receipt.py` is an unwired orphan** (#235) that keeps `_validate_wiring.py` red for every commit. Wire it or exempt it by name — not this lane's file.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** which side is conservative for each of the **20 open polarities** — the T asset asserts it in a docstring, not per row; hence 0 declared. Price: a `conservative_side` datum per row in a live home (Q6) — Dave's eye per row, ~20 decisions.
- **UNPROVEN:** pl-02's link type (`touches` vs `resolvedBy`, Q3). Price: Dave's eye, one quote.
- **UNPROVEN:** pl-29's link type (`touches` vs `resolvedBy`, Q4). Price: Dave's eye, one quote.
- **UNPROVEN:** the pack-side classification is proven by `classify()` in-process on the real refusal text, not by a real `_gen_pack_manifest.py --probe` staging (which runs git). Price: one `--probe` run at a seat that may run git, ~5 min.
- **UNPROVEN:** CI's full survey on the committed tree — proven locally for [141]/[142] only (`--range 141:142`). Price: the conductor's CI run after commit.
- **UNPROVEN:** the memento schematic's committed step count (140 → now 142) — its `--check` is COULD-NOT-ASK here (tape tier, gitignored key), so whether a real-tier seat reads it stale is not observable from this seat. Price: `python3 knowledge/_gen_schematic.py` at a real-tier seat.
- **CLAIMED:** `_release/_gate_release_audit.py --check` is unaffected because it regenerates at the manifest's own recorded commit, where this file does not exist — reasoned from its docstring, not run (it runs git). Re-read costs one `--check` at a seat that may run git.

## PITFALLS (consequences replayed, Dave #165 — owner per row)

| | risk | what is built against it | owner |
|---|---|---|---|
| 1 | The gate derives the declaration from a DATED `notes/` asset — a copy-chain smell; if that frozen file ever moved the declaration goes stale and the seam refuses | the source's sha256 is pinned in the generated header; the asset is ADR-0017 frozen; Q6 names the live home | Dave (Q6) |
| 2 | Every edit to `knowledge/brain/*.json` now needs `--write` BEFORE the commit — a new serial step; forgetting it is a red at the seam | the seam refusal names the command and says to NAME the regenerated files in the reconciliation (P5) | whoever edits the home |
| 3 | `generated_at` changes on every `--write` → one diff line per regen, in three files | the clock is what `s238-D3` asks for; `--check` holds it so an unchanged home never re-stales | accepted |
| 4 | `POLARITY_ACK` can pass a red gate DECLARED — a lazy ack is possible | printed on the record, same standing as `SHOWROOM_ACK`/`DOC_ROW_ACK`; the hatch does not re-ask (harness asserts it) | Dave's read of commit output |
| 5 | `R4-STRAY-FILE` closes `knowledge/brain/` — a future legitimate file (e.g. Q6's) is refused until added to `ALLOWED_TOP` | the refusal names the rule and the allowed set | the author of that file |
| 6 | An unparseable `_rulings.json` turns this gate red at the seam (`S-PARSE`) | correct — the live register is an input; loud, named | whoever broke the store |
| 7 | `_validate_wiring.py` stays red on #235's orphan regardless of this lane; a reader may attribute it here | finding 14 records the baseline | conductor / #235 |
| 8 | The step count moved 140 → 142; anything quoting the count is stale (schematic; CI notes) | `_gen_chain._steps_in` and `build_rows` agree at 142; the schematic regen needs a real-tier seat | conductor |
| 9 | `--selftest` writes ~55 copies of the home (~14 MB) under `/dev/shm` and removes them; on a disk-full VM (#227) a copy could fail | a mutation-setup crash is reported as a FAIL arm, never a pass; the tempdir is removed on every path | sandbox hygiene |
| 10 | ids are ASCII by the schema pattern, so a legitimate non-ASCII id can never be a party (homoglyphs were the reason) | refused by name (`S-ID`); unruled — flagged here | Dave |

## Evidence

`notes/_subreports/assets/2026-09-02-238-P-polarity-gate/` —
`_migrate_tensions.py` (the migration GENERATOR: rules P1/P2/L1–L5/Q as printed code + declared data; re-run it for the receipt) · `migration-table.txt` (its output: counts, the two dangling-id receipts, the 30-row table, the 15 stubs, the source sha256) · `check-real.txt` (`--check` on the real home: counts, the printed rules, the freshness lines) · `selftest-table.txt` (the 55-arm table, rc 0) · `_probe_seam.sh` + `seam-probe.txt` (the live seam block driven green / red-named / declared) · `_probe_build_step.py` + `build-step-drive.txt` (join total, appended last, the build's subprocess form green / red with the GATE remedy) · `survey-range-141-142.txt` (`_build_survey.py` asks the two steps: 2 pass) · `seam-harness.txt` (`_test_git_commit.py --selftest`: 44 arms all PASS incl. the two new) · `wiring-and-helpgate.txt` (wiring gate 50 wired + the pre-existing orphan; help gate OK; harness-stub detector green; `_git_commit.sh --selftest` OK) · `_probe_independent.py` + `independent-readers.txt` (jsonschema 3.2.0 cross-check; the recount from the frozen rows; every sha256) · `writer-on-real-home.txt` (`--add-polarity` dry-run and a refused `--write` on the REAL home, bytes untouched) · `pack-classifier-reading.txt` (`classify()` → REPO-BOUND).

Receipts (sha256, from `independent-readers.txt`): `principles.json` == `principle-register.json` `c0165ace59d3364dbc8610b02808ee7b610402764c8af2441cc73c3cce53bc96` · source `tensions.json` `a3b120288e448d5bde0b644bd2e0b6ad6b0d2eff24247b9c7d49f10d85dc62a6` (matches `$migration.sha256`) · `polarities.json` `02d56dc4bca8be53ee5d6e83778bc104c0b2624ee0900bab3e2bbdad5f531c1b` · `stubs.json` `a7d3e274d65a009772e8a716757412aba229d36aefc8f3e0b851e0bbadc8e9c7`.

REPLAY-THESE: `knowledge/brain/_generated/defaults-declaration.txt` (~1.2K — the rule and the 20 UNPROVEN rows; the only place the s238-D5 verdict is stated) · the MIGRATION TABLE above, rows pl-02 and pl-29 (~0.4K — the two link types marked UNPROVEN, Dave's eye) · finding 3 (~0.3K — 6·4·20, not the ruling's 6·9·15) · RULING-SHAPED Q1, Q2, Q6 (~0.5K — the three that shape the next build) · `assets/…/seam-probe.txt` + `build-step-drive.txt` (~0.6K — the two consumers driven, the proof s238-D7's last sentence asks for)
