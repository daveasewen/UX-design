# `#246`-`P` — dashboard composition principles: research + draft (reveal · density · execute · rhythm)

session: `#246` · 2026-09-05
window: lane P (Fable), conductor Fable
sub index: `P`
brief: `notes/_briefs/2026-09-05-246-lane-P-dashboard-principles-brief.md`
tokens: `≈240,000` real — read off the harness `total_tokens` counter (15,000,000 → 14,760,802 at the last probe before this file was written); `message.usage` was not readable from inside the lane, so the figure is CLAIMED at counter precision, not UNMEASURED

## VERDICT

DONE. One review page in the swiss idiom, `reviews/DASHBOARD-PRINCIPLES-2026-09-05-v1.html`, carrying Dave's four seeds each pinned to a baseline defect (file + line + render crop), a 32-finding research digest across the four topics with a URL per finding, eighteen draft principles `DP-01…DP-18` in the `_rules-index.json` shape (statement · sourced rationale · PROPOSED tag · binding home · defect prevented), an enactment table naming ONE landing source per group, and seven ruling-shaped questions. Both baselines (Fable lane B, Opus lane O) were read and cited. Nothing under `knowledge/`, `showroom/` or `apollo-spider/` was edited; nothing committed; nothing ruled. The one honest weakness: the page could not be rendered in the sandbox (no `libXdamage`), so the crop offsets are arithmetic from the PNG headers (1440×1316 Fable, 1440×1275 Opus), not eye-checked — declared below as UNPROVEN.

COUNTS: findings `5` · ruling-shaped `7` · UNPROVEN `1`

## What was done

1. **Baselines read.** `reviews/BASELINE-246-2026-09-05-v1.html` and `reviews/BASELINE-246-OPUS-2026-09-05-v1.html` stripped to text; renders `outputs/baseline-246/arm-B-sighted/render-1440-light.png` and `outputs/baseline-246-opus/arm-B-sighted/final-1440-light.png` viewed. Defect lines located by grep in both built `dashboard.html` files (Fable L158–202 toolbar, L236–276 KPI tiles, L292/L427 group spans; Opus L1284–1311, L1340/L1427, L1444–1445 "Needs attention / View all").
2. **Existing canon grepped.** `_rules-index.json` (470 rules): 63 match filters/hierarchy/density/charts; **zero** mention dashboard composition, progressive disclosure, KPI rows or bento rhythm. `_rulings.json` (348): the bento chain s217-D2…s219-D3, s234-D4, s245-D6/D7/D8/D9, s182-D2 and s165-D6 read and cited where they close a question. `_bento_edit_rails.json` dials enumerated (spacing, mainSpacing, subSpacing, keylines, mode, edge, rounding, bentoBg, capBg, grouping — no spans dial). `template-dashboard-bento.meta.json`, `kpi-tile.meta.json` (the unused `compact` variant), `canon.css` L1098–1104 (`data-c` 1–6), `gen_bento_matrix_217.py` L1660–1690 (the display wall's irregular spans and the squaring pass).
3. **Web research.** 14 external sources fetched or searched: NN/g progressive disclosure (fetched, primary), NN/g dashboards-preattentive (fetched), Few "Formatting and Layout Matter" PDF (fetched — failures 1, 4, 5, 9 are the spine of the density and rhythm groups), SAP Fiori Overview Page v1.50 (fetched — snapped filter bar default, micro actions, card counter, six-action cap, five-item list cap), Carbon dashboards v10 (fetched), Carbon filtering (search summary), Polaris index filters (fetched — View/Filter mode, 2–3 promoted filters), MOJ filter-a-list (fetched — starts hidden, unfiltered first, no persistence by default), Atlassian dashboard filters (search), Salesforce Lightning home (search), Material 3 canonical layouts (search), Müller-Brockmann PDF (search/archive link), deck.gallery Apple bento breakdown (search), Helios filter patterns (search), ClearPoint 5–9 metrics (search, marked secondary). Carbon v11 dashboards and SAP sap.com design-system pages returned empty (JS-rendered) — replaced by v10 and experience.sap.com equivalents.
4. **Drafted the page** with the `swiss-design-system` skill loaded, on the house review idiom (same tokens, nav, dimension sections and footer as the two baseline reviews). Crops are CSS transforms over the baseline PNGs (no image files written).
5. **Wrote this report.** No assets directory created (nothing to store — the renders are the baselines' own).

## Findings

1. **The rules index has no composition tier at all.** `python3` over `_rules-index.json` with the pattern `progressive|disclos|densit|dashboard|bento|hierarch|filter|default state|rhythm|kpi` → 63 hits, every one about type, colour, chart anatomy, tags/pills or notifications; none about what goes where on an overview. Both baselines therefore shipped the template's specimen arrangement unchanged — the meta itself says "HOW MANY groups … is Dave's" and the brief skipped every question.
2. **The machinery for irregular walls already exists and the dashboard template never uses it.** `canon.css` L1098–1104 carries `data-c="1"…"6"`; `gen_bento_matrix_217.py` L1660–1675 mints irregular spans for the DISPLAY wall and runs `square_wall_for_role`; every span in both built dashboards is 3 or 6. The gap is a default, not a grammar (DP-14/15; enact table row 4 = one attribute each).
3. **The row form of the KPI board is already drawn and unused.** `kpi-tile.meta.json` variants: `compact` — "dense boards where many tiles share a row". DP-06 is one snippet edit in the lead group (four `compact` tiles, one row).
4. **The literature is unanimous that filter controls on an overview start collapsed and that the designer sets the state** — SAP (snapped by default, "users can start right away"), MOJ (`startHidden: true` on wide screens), Polaris (View/Filter toggle). Dave's seed matches the literature exactly; `filter-toolbar-bar.meta.json` has no `defaultState` prop (DP-02).
5. **Few's failure 9 is Dave's third seed almost verbatim** — "Enforcing a rigid symmetrical grid … everything forced into quadrants of equal size … rarely useful", with the counterweight in failure 5 (separate only as much as groups) and the rule that asymmetry must be intentional. Both are primary-source backing for DP-14/15/16; only DP-17 (odd counts, one break) lacks a primary source and is marked TASTE on the page.

UNPROVEN-1: the four render crops (`.crop.kpi/.filters/.bento/.opus-rail`) are positioned by arithmetic from the PNG headers; the page was not rendered in a browser (sandbox chromium fails on `libXdamage.so.1`). Price of proving: one open of the file in Chrome, or one Playwright run on the host — under 1K tokens.

## RULING-SHAPED QUESTIONS

Copied from the page § 05, unanswered:

1. Which tags — take the proposed set (BLOCKING: DP-03 chips, DP-05 re-measure, DP-08 no-gauge, DP-10 count, DP-13 declared consumer; REVIEW: DP-02, DP-15b, DP-18; TASTE: DP-17; ADVISORY: rest), or all ADVISORY until a second dashboard is built?
2. Does "the designer decides the default state" become a grill-me question ("Filters: open or collapsed on load?", skip ⇒ collapsed) or a template default with no question?
3. Do importance-weighted spans need a new bento rails dial (a `spans` chord: 4+2 · 2+4 · 3+3 · 6), or is a template default enough?
4. Is the "needs attention / tasks" surface a new component (`attention-list`, actions + states) or a composition recipe over summary + status-indicator + button?
5. Does the KPI row replace the 2×2 as the template's shipped default, with 2×2 kept as an edit-pass option?
6. May generate-from-canon add up to three composition questions to grill-me (which number first · filters open/collapsed · what can be actioned here), given #241's "getting excessive"?
7. Is DP-17's odd-count / one-break rhythm kept as a TASTE chord, promoted to ADVISORY, or dropped in favour of DP-14/15?

## Declared gaps

- Tidwell (Designing Interfaces) and Cooper (About Face) were searched but not fetched in text; the digest cites neither directly. Tidwell's "Responsive Disclosure" and Cooper's "excise" would strengthen DP-01/DP-04 if a copy is to hand.
- GOV.UK's central design system has no published filter pattern (backlog issue #133); the MOJ system stood in and is labelled as such.
- Banking treasury portals: no public design guidance was found beyond product marketing; SAP and Salesforce carry the execute-on-page evidence.
- Hochuli was not reached; Müller-Brockmann carries the editorial-grid point alone.
- The 5–9 metric count rests on secondary sources only and is tagged ADVISORY for that reason.

## REPLAY-THESE

- Review: `reviews/DASHBOARD-PRINCIPLES-2026-09-05-v1.html` — 18 principles, 5·4·4·5 by group, 7 RSQ. *(Superseded by § P2 below: 27 principles, 5·4·4·5·9, 13 RSQ.)*
- Nothing under `knowledge/`, `showroom/`, `apollo-spider/` touched; no commit; no ruling.
- UNPROVEN-1: crops not eye-checked in a browser.

---

# P2 — signal (preattentive) group added to the SAME page

session: `#246` · 2026-09-05 · lane P2 (Fable), an ADDITION to lane P's draft, same page edited in place
seed: Dave, verbatim, same afternoon — "Preattentive processing is super important to me … discerning predators, the sky, blood, foliage, fruit and game … part of the dark cockpit work we've done previously. this can also be reflected in copy I feel."
tokens: `≈95,000` real, read off the harness `total_tokens` counter (14,961,305 at lane open → ≈14,808,000 at the last probe before this section was written; **CLAIMED at counter precision**, `message.usage` not readable from inside the lane)

## VERDICT

DONE. The page gains a fifth seed (Dave's words, pinned to two defects and one gap), a fifth digest group E (9 findings, a URL each, secondary sources labelled), a fifth principle group **Signal (preattentive)** `DP-19…DP-27` in the index shape with Dave's metaphor mapped to tier names (sky · foliage · fruit · game · blood · predator), a fifth enact row, and six ruling-shaped questions Q8–Q13. Header, stats, headings and footer updated; the page parses clean (27 `.dp`, 13 RSQ). Nothing under `knowledge/`, `showroom/`, `apollo-spider/` touched; nothing committed; nothing ruled.

COUNTS: findings `4` · ruling-shaped `6` · UNPROVEN `1` (inherits lane P's — crops not eye-checked)

## What is already encoded (Dave asked: "they might be encoded already i guess")

Partly. The **attributes** are there; the **tiering** is not.
- Colour never alone: `avd-001`, `dv-011`, `icon-013` (BLOCKING), `dv-line-002`.
- Colour as focus, not ground: `dv-012`; red tactically: `col25-001`, `col26-003` ("if everything competes for attention, nothing stands out"), `webf-003`, `type25-003`/`type26-004`/`col26-016` (no red type outside CTAs).
- Bright area cap: `neuro-014` (≤20 %), `neuro-042` (the "calm ceiling", REVIEW).
- Movement last: `neuro-036` (TASTE, with the hover receipt), `neuro-035`, `mot-004`, `mot-005`, `mot-006`.
- Weight/size as hierarchy: `type26-009`, `copy-011`, `neuro-022`, `va25-006`.
- Rulings: `s151-D1` two-red law · `s155-D1` green mirror · `s144-D1` "the only coloured text is for plus and minus" · `s145-D1` the `-ink` rung · `s157-D1` "an unsigned amount is not a success state" · `s182-D3` spark stroke semantic · `s122-D2` mono RAG map · `s130-D5` error signal off the label.
- **Not encoded (0 hits):** a signal tiering; a per-screen budget; a metric's good-direction (`kpi-tile.meta.json` has `trend` but no polarity); a motion role for monitoring pages; a written mono "colour = signal" rule with an id (it lives only as riders in s144/s151/s157); copy tiering for status words; the word "cockpit" anywhere in the repo (re-grepped: still 0).

## Findings

1. **The strongest signal on both baselines points the wrong way.** "Net FX exposure (unhedged)" fell (−8.1 % Fable L250–258, −11.2 % Opus L1297–1305) and both arms paint spark and delta red, because `kpi-tile.meta.json` binds `trend:down → rag.error-ink` and nothing declares which direction is good. Falling unhedged exposure is good news. DP-22 (`polarity` prop, BLOCKING proposed) is the one-field fix; s182-D3's semantics are preserved by binding the ink to a derived `data-signal` instead of `data-trend`.
2. **No dark cockpit: everything lit, nothing dominant.** Opus "Needs attention" L1448–1458: 3 of 3 rows carry a coloured chip (warn/err/inf at equal weight). Both pages animate every chart series and marker on load (`animation-delay` ×42 Fable, ×27 Opus). The one *game* metric ("Payments awaiting approval", 11 payments, 4 cut-offs) is flat-neutral with no affordance. DP-19/20/23.
3. **Mono's law has no id.** Dave's "colour only when it carries a signal" is enforced by his eye and appears only as riders in three rulings; a builder cannot cite a rider. DP-25 proposes a rule id in `colour-usage.md` plus a `signalOnly` flag on the theme registry entry so the census gate reads the theme.
4. **Copy already half-agrees.** Fable's "Held" is the GOV.UK/Polaris shape (one word, past participle); the same list carries "Approval due" and Opus mixes "Returned — beneficiary" / "Approval due 16:00" / "Due 30 Sep". `copy-030`, `ctkn-012`, `copy-017` cover buttons, titles and exclamation marks; no rule covers the status word. DP-26.

## Sources (P2): 9 external, 3 metas, canon.css, both built dashboards

Fetched in text: Healey & Enns PP page (primary), Few *Dashboard Design for at-a-glance monitoring* course PDF (primary — the cockpit line, "keep bright colors to a minimum, using them only to make a few items stand out"), GOV.UK Tag (primary), Polaris Badge (primary), isa.org ISA18 committee page (scope only). Search-summarised, not fetched: Axis Maps on Bertin (secondary, faithful), airflow.blog + lean.org on the dark cockpit (secondary — the Airbus FCOM is not public), industrialmonitordirect on EEMUA 191 / ISA-18.2 (secondary — standards paywalled), IOPscience "Flightcrew Light Alerting Design with Dark Cockpit Philosophy" (returned empty). NN/g dashboards-preattentive was fetched by lane P and is cited through P's digest B5 only.

## RULING-SHAPED QUESTIONS (P2 → page § 05 Q8–Q13)

8. Signal budget as a validator arm (rag-resolved colours + running animations in the first viewport, one dominant) — and is three the number?
9. Mono "colour = signal": rule id + hue-census gate — mono only, or console too (shares the neutral ramp s157-D2, not the RAG lane s122-D3)?
10. Dark-cockpit tiering: all-theme composition rule (tiers say *what*, themes say *how*) or mono/console only?
11. Polarity: `polarity` prop with no default on `kpi-tile` (+ `amount-display`); good-down painted green, or neutral with the symbol alone?
12. Copy tiering: `copywriting.md` guideline, or a variant→word map in `status-indicator.meta.json`, or both?
13. Load motion on monitoring surfaces: template attribute or `motion-standards.md` clause — and does hover scale-physics (mot-007) stay?

## Declared gaps

- Ware (*Visual Thinking for Design* / *Information Visualization*) was searched, not fetched in text; the page cites Healey & Enns for the taxonomy and conjunction limit instead. Ware's "tunable" attention and channel ranking would strengthen DP-21/24 if a copy is to hand.
- Few's *Information Dashboard Design* book chapter was not reachable; the 2010 course PDF (same author, same material) stands in.
- The dark-cockpit primary (Airbus FCOM / A310 design philosophy) is not public; two secondary sources and the IOP paper title carry it.
- EEMUA 191 / ISA-18.2 figures (80/15/5, ten-in-ten) are from a secondary digest; the standards are paywalled.
- The number three in DP-20's budget is a proposal with no source; the page says so.
- Nothing was rendered: the new seed reuses lane P's `.crop.kpi` / `.crop.opus-rail` transforms, so UNPROVEN-1 covers it.

## REPLAY-THESE (P2)

- Same page: `reviews/DASHBOARD-PRINCIPLES-2026-09-05-v1.html` — now 27 principles, 5·4·4·5·9, 13 RSQ; P2 blocks carry `class="p2"`.
- Nothing under `knowledge/`, `showroom/`, `apollo-spider/` touched; no commit; no ruling.
- `kpi-tile.meta.json` props.trend is the seat DP-22 names; `canon.css` L9645–9709 the spark selectors it would re-point.

---

# P3 — locality group + the HELD horizon/banding thought, SAME page

session: `#246` · 2026-09-05 · lane P3 (Fable), an ADDITION to lanes P/P2, same page edited in place
seeds: Dave, verbatim, same afternoon — (a) the green/blue horizon, "white controls all below … the footer is the ground … maybe hold this thought"; (b) "rules or principles about proximity, like CTAs triggering actions close to them … maybe validate". Dave then said "add" to DP-28 action locality and DP-29 feedback locus, with the horizon thought HELD.
tokens: `≈125,000` real, read off the harness `total_tokens` counter (14,961,468 at lane open → 14,842,826 at the last probe before this section was written; **CLAIMED at counter precision**, `message.usage` not readable from inside the lane)

## VERDICT

DONE. The page gains a sixth seed block (both quotes, pinned to two defects and one observation), a sixth digest group F (16 findings, a URL each, two secondary sources labelled), a sixth principle group **Locality** `DP-28…DP-29` in the index shape, one **HELD** block (`class="held"`, tag HELD, not a DP, not tagged, not enacted — at most one advisory line, proposed only if Dave lifts his own "maybe hold"), a sixth enact row, and three ruling-shaped questions Q14–Q16. Header, stats, headings and footer updated; the page parses clean (29 `.dp`, 1 `.held`, 16 RSQ; P3 blocks carry `class="p3"`). Nothing under `knowledge/`, `showroom/`, `apollo-spider/` touched; nothing committed; nothing ruled.

COUNTS: findings `4` · ruling-shaped `3` · UNPROVEN `1` (inherits lane P's — crops not eye-checked; the seed reuses `.crop.filters`)

## What is already encoded (Dave: "maybe validate")

Partly, and only at field scale.
- Proximity: `aid-004` (ADVISORY — labels close to controls, form errors close to their fields, reflow keeps related content after its section). The only proximity rule in 470.
- Added-content placement: `acd-023` (place it AFTER its trigger; move focus or announce).
- Landmarks: `acd-005`; all seven app-shell snippets carry header · nav · main · footer[role=contentinfo]; `showroom/index.json` has a `shell` level (7 shells).
- Toast scope: `ctkn-004` (nice-to-know, auto-dismiss), `ctkn-011` placement; `toast.meta.json` antiPatterns already forbid an error toast and an action that exists ONLY in the toast.
- Popconfirm anchor rule (meta antiPattern): if the object is off-screen the pattern is a Modal.
- Edges: `triggeredBy` on 13 of 137 metas, 2 with a resolved `ref` (drawer, popover); `groupsWith` ruled s245-D7, unpopulated in the metas grepped.
- Placement homes exist: `card-header-lockup` (module toolbar), `page-header-lockup` (title zone, right-aligned action pair), `action-bar` (flow footer).
- **Not encoded (0 hits):** a rule that an action sits on the surface it acts on; a rule for where feedback lands relative to its trigger; "horizon", "banding", "zone" as a tonal rule; a footer on the dashboard template.

## Findings

1. **Both baselines break DP-28 in the same place, and the one control that obeys it is the one that works.** The page-title filter toolbar acts on nothing (`toolbar-probe.json`: 24 → 24 on entity, currency, search, both arms; Fable `drive.json` toolbar.period-Quarter `anything-else-changed: "no consumer wired"`), while the grid's own search — inside the grid module — filters to 9. Locality is not decorative here; it is the difference between the wired control and the dead one.
2. **The feedback ladder has zero rungs.** All feedback on either page is chip/count-class inside the grid (`role="status"` count line, applied-filter chips); nothing on the control, nothing inline, and no toast because no action fires. DP-29 is recorded so DP-11's actions do not ship with a toast as their only receipt.
3. **The edge exists and is empty.** `meta.schema.json` carries `edges.triggeredBy` (v2, ref|null) and `groupsWith` (s245-D7); 13/137 metas carry `triggeredBy`, 2 resolve to a node. A placement arm has nothing to read today — Q14 asks whether to populate it (~10 dashboard consumers, Dave's-eye refs per s131-D2) or fold the fact into DP-13's `controls` edge so control → consumer lives ONCE.
4. **The horizon thought validates on three primary sources and stays HELD.** Palmer 1992 (common region beats proximity — a band IS a zone), Tufte (layers separated by value do not compete; 1 + 1 = 3), W3C APG (banner / main / contentinfo are the sky/field/ground already, one each, derived from the areas designers mark by alignment and spacing). Bertin limits it: a band carries position + value, not kind — the "red horizon = state" half is a signal question (DP-19/21), not a banding one. What is missing is only the tonal rule and a ground: the dashboard template has no `<footer>` (0 hits) so both baselines shipped sky + field, no ground. Proposed at most ONE advisory line, no build, and only if Dave lifts his own "maybe hold" (Q16).

## Sources (P3): 12 external, 6 metas, the schema, 7 shell snippets, showroom index, both drive files, toolbar-probe

Fetched in text: NN/g closeness of actions and objects (primary, Nielsen 2010), W3C APG landmark regions (primary), GOV.UK error summary (primary; body persisted to a tool-results file, key rule quoted from the page's own guidance). Search-summarised, not fetched in full: Palmer 1992 (ScienceDirect abstract + NN/g common region), NN/g proximity, NN/g Fitts (MacKenzie's Shannon form named from memory of the standard formulation), NN/g natural mappings (for Norman), Raskin Center summary (for The Humane Interface), NN/g visibility of system status, Material m1 errors + snackbars and m2 top app bars (contextual action bar), SAP Fiori action placement + toolbar overview, Polaris card layout, Tufte Envisioning Information (PDF located, ch. 3 summarised from secondary digests), Bertin (Stony Brook lecture digest — secondary, labelled).

## RULING-SHAPED QUESTIONS (P3 → page § 05 Q14–Q16)

14. DP-28: is `triggeredBy` the edge a placement arm consumes (populate ~10 consumers), or does control → consumer fold into DP-13's `controls` edge — ONE edge, one home?
15. DP-29: does `toast.meta.json` gain an antiPattern "feedback for a change the reader can already see", or does the ladder stay a notifications-guideline clause only?
16. Horizon/banding: HELD (Dave's "maybe hold"), or one ADVISORY line + id, or a footer on the dashboard template only with no rule?

## Declared gaps

- Norman (DOET) and Raskin were reached through NN/g and the Raskin Center summary, not the books; Fitts through NN/g, not MacKenzie 1992 directly.
- Tufte ch. 3 was not read in the PDF; the 1 + 1 = 3 and value-separation points come from three secondary digests that agree.
- The GOV.UK error-summary page body overflowed the tool result; the "both summary and inline message" rule is the page's headline guidance and is stated from it, not quoted.
- No rendering: the seed reuses `.crop.filters`, so UNPROVEN-1 covers it.
- `groupsWith` population was grepped on the six metas that matter here, not all 137.

## REPLAY-THESE (P3)

- Same page: `reviews/DASHBOARD-PRINCIPLES-2026-09-05-v1.html` — now 29 principles, 5·4·4·5·9·2, + 1 HELD, 16 RSQ; P3 blocks carry `class="p3"`, the held block `class="held"`.
- Nothing under `knowledge/`, `showroom/`, `apollo-spider/` touched; no commit; no ruling.
- `meta.schema.json` L220 / L306 `triggeredBy`, L305 `groupsWith` are the seats DP-28 names; `toast.meta.json` antiPatterns the seat DP-29 names; `Template-dashboard-bento.reference.html` has no footer — the HELD block's one hard fact.
