# #301 lane G — the window's hard line raised 256,000 → 300,000

status: observed
provenance: 2026-09-23, session #301, helper lane G (sub), at Dave's seat on the mount. Not committed.

## Verdict

**Done.** `BUDGET_HARD` is 300,000 real tokens, PICKED by Dave #301 as an experiment until the Mac
seat fix. AMBER 160,000, WORKING 200,000, BOOT_CEILING_TK 72,768 (shrink-only), STOP_LINE_TK 180,000 and
TOLERATED_TK 220,000 are unchanged (read back from the live module: `160000 200000 300000 boot_ceiling 72768 stop 180000 tol 220000`).

Dave's words (19:45 / 19:47 BST), quoted at the constant and in the gate pin:
*"okay what we'll do is just raise the ceiling until we do the Mac seat fix, hopefully there wont be too many
consequences. let just use subs to stretch the window as much as possible. Lets try 300k and cross our fingers"*
— then *"1. is right, might be a good experiment."*

## What changed (file:line, post-edit)

`git --no-optional-locks diff --stat -- knowledge/`: 4 files, +49 / −14.

| file:line | change |
|---|---|
| `knowledge/_gauge_tokens.py:86` | `BUDGET_HARD = 256_000` → `300_000`. Comment rewritten to: PICKED by Dave #301 (2026-09-23), experiment until the Mac seat fix, both quotes verbatim, warning that 256K–300K has no published recall measurement, what did not move. Old provenance kept at :96–97 (`WAS (pre-#301): 256_000, SOURCED — last publicly measured-good recall point (93% MRCR v2)`). The two historical "NAME IS HISTORICAL" lines after it are left as they were. |
| `knowledge/_gauge_tokens.py:56–58` | added a ⛔ #301 header line above the SOURCED account (the account is kept, marked as the pre-#301 record). |
| `knowledge/_gauge_tokens.py:133–134` | added a #301 addendum above the STOP_LINE "NOT a wall … 256,000" comment. |
| `knowledge/_gauge_tokens.py:260–261` | "BUDGET_HARD (256,000)" → "(256,000 then; 300,000 from #301 …)". |
| `knowledge/_gauge_tokens.py:615–618` | **live CLI output**: it printed `(SOURCED — 93% MRCR v2)` and a literal `256,000`. It now prints `(PICKED Dave #301 — experiment; was 256,000 SOURCED, 93% MRCR v2)` and `{BUDGET_HARD:,}`, so the printout matches the constant. |
| `knowledge/_gauge_tokens.py:647–648` | selftest docstring triple `(160_000, 200_000, 256_000)` → `300_000`, with a #301 note. |
| `knowledge/_capture_gate.py:7106–7114` | **the pin**: `(160_000, 200_000, 256_000)` → `(160_000, 200_000, 300_000)`. Comment quotes Dave. The failure message now says HARD is PICKED #301 (was 256,000, SOURCED). |
| `knowledge/_capture_gate.py:7076–7079` | **fixture that would have broken**: "past the HARD line — FAILS EVEN WHEN MARKED" was 26,897 + 200,000 + 40,000 = **266,897**. At 300K that total sits inside the line, so a marked stamp is *allowed* and the fixture would go red. Changed to job 250,000 → **316,897**, with a note. Bite confirmed below. |
| `knowledge/_capture_gate.py:1425–1432` | live fail message for a pre-flight past the hard line. It said "That line is SOURCED … last context length at which recall has been publicly measured", which is false at 300K. It now says the line is PICKED by Dave #301 as an experiment, above the last measured point (256,000 — 93% MRCR v2). Comment line added. |
| `knowledge/_capture_gate.py:168`, `:4490–4491` | #301 addendum comment lines. The old text is untouched. |
| `knowledge/_seam.py:51` | docstring verdict table `BUDGET_HARD 256,000` → `300,000 (#301, was 256,000)`. The code already reads `G.BUDGET_HARD`. |
| `knowledge/_RUNBOOK-context-gauge.md:57` | **BY ADDITION**: a dated ⛔ #301 line under the band table (Dave's words, what moved and what did not, "256K–300K has no published recall measurement"). The table row still reads 256,000 SOURCED, as the pre-#301 record. |
| `knowledge/_RUNBOOK-context-gauge.md:96` | **BY ADDITION**: a #301 addendum under the `s214-D1` conditional-band section. It flags that the band's upper edge is **unruled** (see "Dave must decide"). |

## Every other 256K hit, and why it was left

The grep covered 256_000 / 256000 / 256,000 / 256K in *.py, *.yml, *.yaml, *.sh, *.json, *.toml and *.md. It excluded notes/, archives, _DECISION-HISTORY, outputs/ and knowledge/_tmp/.

- `knowledge/_gauge_tokens.py:59–79, 89–96 (old numbering)`: the SOURCED/#286 history prose and Dave's #58b quote ("200K and 256K come from established research"). This is a historical record, so it is kept and prefixed by the #301 header line.
- `knowledge/_gauge_tokens.py:~890` (`BUDGET_HARD` in the E-arm tuple) compares to the live constant, not a literal. No change needed.
- `knowledge/_capture_gate.py:1417–1423, 4484–4486, 7065–7067`: the #286 wording-history comments. Left as record. Addenda were added where the text claims "not moved".
- `knowledge/_recall_probe.py:6`: docstring "200,000–256,000 CONDITIONAL BAND (`s214-D1`)". This names a ruling, not the constant, so it is left. It depends on the open question below.
- `knowledge/_standing.md:19`: "256,000 is not a wall for this model". The file is **ratified as written by Dave (`s287-D1`)**, so it is not touched. It is still true as worded, but it is now stale on the number. Rewording it is his.
- `knowledge/_rulings.json` (13 hits), `designer-skills-v2/knowledge/_rulings.json` (11), `knowledge/_state.json` (15), `knowledge/_memento-index.json` (300): these are the ruling ledger, state and generated index. They record history (`gauge-band` #56, `s214-D1/D2`, `s272-D93`). No code asserts them against the constant, so they are left. A #301 ruling entry, if wanted, is the conductor's or wrap's to inscribe. The memento index regenerates.
- `knowledge/_RUNBOOK-context-gauge.md` lines 53, 60, 69, 73–89, 375–393, 770, 875, 929–999: these are the band table, the conditional-band section and dated history. They were amended by addition only (2 lines), per the brief.
- `.github/workflows/gates.yml`: no gauge hit (its only "256" is a sha256). The root prose (`_CARRIES.md`, `_LIVE-STATE*.md`, `GOOD-MORNING.md`, `_GM-ARCHIVE.md`, briefs) is session record and out of scope.
- knowledge/_tmp/* and outputs/* JSON and `_capture_gate` snapshots are scratch copies and are not live.

## Tests (at the seat, TMPDIR=/dev/shm, tiktoken 0.14.0 installed with --break-system-packages)

- `python3 knowledge/_gauge_tokens.py --selftest`: **rc=0**, "selftest OK -- counting path exact on 5 fixtures; … band/floor guard logic verified relative to the live constants."
- `python3 knowledge/_seam.py --selftest`: **rc=0**, "seam selftest: 15 arms, all GREEN" (the HARD LINE BREACHED arm is driven at G.BUDGET_HARD = 300,000).
- `_capture_gate.selftest_preflight_tokens()` (the arm that owns the pin): **0 failures across 13 fixtures.**
  - Pin bite: with `BUDGET_HARD` monkey-patched back to 256,000, it gives **1 failure** (`budget = (160000, 200000, 256000), ruled (160,000 / 200,000 at #56 · 300,000 at #301)…`). The re-pinned arm bites.
  - Fixture receipts: the old 266,897 marked stamp is now **allowed** (1 warn), which proves the fixture had to move. The new 316,897 marked stamp **FAILs** with the new PICKED-#301 message.
- `python3 knowledge/_capture_gate.py --selftest` (full, 109 s): **rc=1**, from **pre-existing, unrelated** failures only. The final block shows 4 fails, all `_governs.py` selftest failures on rulings `s282-D2…D5` ("evidence … carries NO pointer"). `python3 knowledge/_governs.py --selftest` run on its own fails the same 4 (rc=1). `_rulings.json` and `_governs.py` have no diff, and the last `_rulings.json` commit is ac55c7b2 (#295). No failure line mentions budget, pre-flight/tokens, quality-max, 256,000 or 300,000. The other ❌ lines in that output belong to fixture-repo runs, which are expected to fail (dated 2026-07-26/27). The PRE-FLIGHT note now reads "hard 300,000".
- Not run, per the brief: `git status`, `_build_all.py`, `_capture_gate.py --wrap`. Nothing was committed. The working tree already held 6 unrelated modified files under notes/ before this lane. They were not touched.

## Dave must decide

1. **The `s214-D1` conditional band's upper edge.** The band was ruled "200,000–256,000, legal under three conditions", and `s214-D2` said 256,000 stays the unqualified line. The code has no band-edge literal: the hard fail reads `BUDGET_HARD`, so it now sits at 300,000. Should the mechanical-only / probe-green band run to 300,000, or should 256,000–300,000 carry extra conditions? Until he rules, the gate enforces 300,000 and the runbook says the edge is unruled.
2. **`knowledge/_standing.md:19`** (ratified-as-written) still names 256,000. The wording is his if he wants it updated.
3. The `_governs` s282-D2…D5 evidence-pointer failures are pre-existing, not from this lane. They keep `_capture_gate.py --selftest` red for someone else to fix.

---

## § Follow-up (same lane, #301): working line raised 200,000 → 256,000

Dave's words (20:21 BST), answering whether the 200K–256K "mechanical work only" band should stretch to 300K: *"200k isnt enough make it 256"*.
**Result:** working 256,000 · hard 300,000 · amber 160,000 · boot ceiling 72,768 (all unchanged apart from working). The mechanical-only band is now **256,000–300,000**. Live read-back: `triple 160000 256000 300000 boot 72768 stop 180000 tol 220000`.
This **supersedes "Dave must decide" item 1 and item 2 above**: the band edge is now ruled, and `_standing.md` has been updated on his numbers.

### What changed

| file | change |
|---|---|
| `knowledge/_gauge_tokens.py:103` | `BUDGET_WORKING = 200_000` → `256_000`. PICKED by Dave #301 with his words, and the note "band now 256,000–300,000". The old text is kept on its own line: `WAS (pre-#301): 200_000, SOURCED — the line jobs are priced against`. The #301 header above the SOURCED account also names the working move. The selftest docstring triple is now `(160_000, 256_000, 300_000)`. The CLI status line now shows "(working PICKED Dave #301, was 200,000)". |
| `knowledge/_capture_gate.py` pin (`selftest_preflight_tokens`) | `(160_000, 200_000, 300_000)` → `(160_000, 256_000, 300_000)`. The comment and fail message quote Dave, and WORKING is labelled PICKED #301 (was 200,000 SOURCED). |
| `knowledge/_capture_gate.py` `PREFLIGHT_TOKEN_FIXTURES` + the `_PRICED` s294-D9 fixture | **These tests would have broken.** `check_preflight_tokens()` fails any stamp whose `of N` differs from `BUDGET_WORKING`, so all 11 `of 200,000` in the block became `of 256,000`, plus the `_PRICED` fixture. The "nobody ruled" mutation is now `of 256,000 → of 500,000`. **The two "over the working budget" fixtures were at 206,897**, which is now AMBER. The UNMARKED one would still have "failed", but only through the band mis-read, not through the check it names. Both were lifted to **266,897** (job 200,000 + wrap 40,000): over 256K and under 300K. |
| `knowledge/_capture_gate.py` `_WALL` (FILL-declaration regex, s271-D2) | `256,000\|256000` added. `200,000\|200000` is **kept** so post-mortem blocks written against the old line still read as declared. The "never a move of the 200,000 literal, which is SOURCED" fail text is now "working-ceiling literal, which is Dave's (PICKED #301, was 200,000 SOURCED)". The summary note says "PICKED Dave #301" instead of "SOURCED". Two addendum comment lines were added (header + the s271-D2 arm). |
| `knowledge/_capture_gate.py` over-working / wrong-budget fail messages | "(Dave #56)" → "(Dave #56, re-dialled #301)". |
| `knowledge/_standing.md:19` | This is the only line touched. It now reads: `180,000 FILL is the QUALITY line and stands; working 256,000 and hard 300,000 are not walls in Cowork (#284). #301 2026-09-23, Dave: "Lets try 300k and cross our fingers" / "1. is right, might be a good experiment." / "200k isnt enough make it 256".` ⚠ My first, longer wording broke `_seam.py --selftest` with `[standing] 301 cl100k over the 300 ceiling`. The standing block has a 300-token ceiling, so the line was tightened ("for this model in Cowork (Dave's correction, #284)" → "in Cowork (#284)"). The block is now 291 by the seam's own count, **9 under the ceiling. That headroom is thin.** |
| `knowledge/_RUNBOOK-context-gauge.md` | Two more dated lines, **by addition**. One under the band table: WORKING 200,000 → 256,000, PICKED, with his words. One under the conditional band: "RULED, supersedes the 'not ruled' line just above: the band now runs 256,000–300,000". |
| `knowledge/_recall_probe.py:7–8` | Docstring note added: "#301: the band is now 256,000–300,000". |
| `knowledge/_memento-index.json` | **Regenerated** with `python3 knowledge/_build_memento_index.py`. The runbook is in its source set (`_build_memento_index.py:74`), so my runbook additions made the index stale. The full gate selftest then showed a new `❌ retrieval index is STALE`, and the gate's own remedy is this rebuild (ritual step 2g). `--check`: `current (2317 records)`. The diff is 21 lines, all runbook-section text and line-number shifts. |

### Left alone, and why

- `_capture_gate.py:4347` `10_000 <= tk <= 200_000` is the **boot-reading sanity range** in `_parse_boot_rows` (per the comment at :4215, "the 10,000–200,000 sanity range"). It bounds a boot sample, not the working line, and boots are ~55–75K. Changing it would change which GAUGE-LOG lines parse as boot samples, so it is left.
- `_context_gauge.py:31` `DEFAULT_WINDOW = 200_000` and the `_checkin.py:54` docstring are a **window denominator** for an optional ratio, and a documented stale figure. They are not the working budget, so they are left.
- The historical prose in the gauge, gate and runbook (#56/#58b account, "do not reach 200,000", the 150,929 derivation, the 104,364-OVER-200,000 examples) is record. Addenda were added instead of rewrites.
- `STOP_LINE_TK` 180,000 and `TOLERATED_TK` 220,000 did not move. **Both now sit under the working line**, and no code asserts an ordering between them and WORKING. Worth Dave knowing, see below.
- Post-mortem blocks from #260 onward with a declared FILL between 200,001 and 256,000 **no longer count as working-ceiling breaches** in the s271-D2 advisory arm, because it grades against the live constant. This follows from the ruling. It is not a defect.

### Tests (seat, TMPDIR=/dev/shm)

- `_gauge_tokens.py --selftest`: **rc=0**, "selftest OK … band/floor guard logic verified relative to the live constants".
- `_seam.py --selftest`: **rc=0**, "15 arms, all GREEN". It went red once on the long standing line and was green after tightening.
- `selftest_preflight_tokens()`: **0 failures / 13 fixtures**. Per-fixture verdicts all match expectation: over-working unmarked 266,897 FAIL "over the working budget (256,000…)", marked allowed, 316,897 FAIL past QUALITY-MAX 300,000, 500,000 FAIL. **Bite:** with WORKING patched back to 200,000 → **8 failures**.
- `selftest_fill_ceiling()`: **0 failures**. `_WALL` matches both "OVER the 256,000 line" and "OVER the 200,000 ceiling".
- `_capture_gate.py --selftest` (full): **rc=1**. There are **27 ❌**, the same set as the first run before any edit. Fixture-repo runs (dated 2026-07-26/27) show expected fails. The final block has the 4 pre-existing `_governs` s282-D2…D5 evidence-pointer fails. STALE count is 0 after the index rebuild, and no budget, pre-flight/tokens or fill-ceiling failure appears.
- `_recall_probe.py --selftest`: **flaky, pre-existing**. HEAD's own copy went RED on 1 leg ("plaintext answers absent from the store"). With my docstring edit, over 4 runs: 2 legs RED once, then 1 RED, 1 RED, **GREEN**. The planted answers are random, and the result varies run to run on identical code. It is not caused by this lane.
- Not run: `git status`, `_build_all.py`, `--wrap`. Not committed. `git --no-optional-locks diff --stat -- knowledge/`: 7 files, +118 / −56.

### Dave must decide (updated)

1. **The stop line (180,000) and tolerance (220,000) now sit under the working line (256,000).** Before, the order was stop < working < tolerance < hard. It is now stop < tolerance < working < hard, and the seam's "OUTSIDE TOLERANCE, no more lanes" fires 36,000 before the working line. Should the stop and tolerance lines move with it, or stay as they are? They were left untouched because the brief did not name them.
2. The pre-existing `_governs` s282 failures and the flaky recall-probe leg keep two selftests red for someone else to fix. Neither is from this lane.
