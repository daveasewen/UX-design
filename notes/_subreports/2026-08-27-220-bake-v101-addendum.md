# `#220`-bake-v101-ADDENDUM — Gumdrop retrieval fixed at cause · the two KeyErrors were the gate's own footprint

session: `#220` · 2026-08-27
window: bake-preparation lane, addendum
sub index: `bake-v101-addendum`
brief: `notes/_briefs/2026-08-27-220-bake-v101-addendum.md`
supersedes (by measurement only): `notes/_subreports/2026-08-27-220-bake-v101.md` § ② ⚠ box,
finding 5, question 5 — see § ② below. Everything else in that report stands.
tokens: `UNMEASURED` — a sub cannot read its own `message.usage`.

## VERDICT

**Item 1 GREEN — at cause, and neither of the brief's two options was the cause.** Both were
driven and both are measurably false. The real cause is named in the pack's own machinery
manifest as open generalisation debt, and this lane closed it: the Gumdrop cut now carries an
index bootstrap, and a new declared **companion closure** in the generator carries it to the
plugin mirror the seed map cannot reach. From a fresh stage, `_memento_search.py` **answers**,
`--fetch` returns a record verbatim, and an unknown id refuses — in **both** machinery dirs.

**Item 2 GREEN, and the premise was STALE.** Neither compliance builder is broken. The two
`KeyError: 'verification'` reds were the measuring instrument's own footprint: the gate's probe
of `_build_compliance_kg.py` rewrote `graph-index.json` inside the shared copy, stripping the
`verification` block, and the next two files read the mutilated file. `_gate_pack_imports.py`
had already been given a pristine-tree-per-probe restore at 14:13 — **after** the prior report
(14:00) and after the addendum brief (14:02) were written. Mutation control below reproduces the
prior report's counts **exactly** when that clause is disabled. **No exclusion table entry, no
shipped refusal, no ruled gate count moved.**

**Item 3 GREEN except one NEW red-by-design.** MISSING-LOCAL 0 and, now, **ERROR-AT-IMPORT 0**.
Render re-measured from a fresh extraction: bento 44/44, gallery 30/30, 0 broken. Cold start
runs end to end **including retrieval** for the first time. ⛔ `_gate_release_audit.py --check`
and `--selftest` now **REFUSE** — correctly — until the new builder is committed. That is a
second designed red beside `--pack`'s, it clears in the same landing commit, and it is spelled
out in REPLAY-THESE step 1.

COUNTS: items 3 · green 3 · red 0 · could-not-run 0 · new bites 27 (generator 185→195, new
bootstrap 17) · mutations driven 3, all named-red · ruling-shaped questions 3 · findings 4

**Nothing committed. No bake. No `--release`. No ledger row. No promotion. `_build_all.py`
untouched.** `apollo-spider/dist/Apollo-Spider-v1.0.0.zip` re-measured `md5
7a28800b18c1f74b244607a4f254c266` and the frozen gate agrees independently:
`PASS — 3 arm(s) asked, no frozen surface moved`.

---

## ⓪ THE RE-MEASURED FIGURES

| reading | prior report | this lane | how |
|---|---|---|---|
| stage files (at `2f7c47bb904b` + closure) | 1,641 | **1,641** | `--stage`, `find -type f` |
| stage files **with the pending pair** | — | **1,643** | `/var/tmp/stageC`, measured |
| stage bytes (1,643) | — | **40,090,368** | `du -sb` |
| ship paths at `2f7c47bb904b` | 1,640 | **1,640** | unchanged — the new file is uncommitted |
| ship paths at the landing commit | — | **1,641** *(projected)* | +1, group claim measured |
| closure files | 1 | **2** *(projected)* | 1 import + 1 companion, both measured |
| pack files at the landing commit | 1,641 | **1,643** *(projected)* | equals the measured stageC |
| `gates` group | 55 / 35 | **55 — UNCHANGED** | `by_group.gates.files` on disk |
| `gumdrop` group | 18 | **19** *(projected)* | `claimed by: ['gumdrop']`, driven |
| zip bytes / entries / sha256 | 18,977,431 / 1,644 / `9fecdc8f5361755e` | **identical, re-measured** | this lane baked nothing |
| packed `.py` probed | 76 | **78** | +2 builder copies |
| MISSING-LOCAL | 0 | **0** | |
| ERROR-AT-IMPORT | **2** | **0** | § ② — the 2 were the gate's footprint |
| `bento.html` | 44 / 0 broken | **44 / 0 broken** | fresh extraction, re-measured |
| `grids-gallery.html` | 30 / 0 broken | **30 / 0 broken** | fresh extraction, re-measured |
| `photography.html` | 60 / 442 broken | **60 / 442 broken** | unchanged — still question 2 |
| new file | — | **20,093 bytes / 411 lines** | `wc` |

⚠ **`ship paths`, `closure files`, `pack files` and `gumdrop` at the landing commit are
PROJECTIONS, labelled as such** [[planning-estimate-is-not-a-measurement]]. Their two inputs are
measured — `claimed by: ['gumdrop']` driven on the real `groups()` predicates, and
`companion_closure` driven over the real 1,640-path ship list with and without the pending path
(`unsourced 2 → 0`, `seeds [] → [the plugin mirror copy]`). The arithmetic on top is not. The
manifest **cannot** be regenerated in this lane — see the red in § ③ — and 1,643 is measured
directly on the stage, which is the number that matters.

---

## ① GUMDROP RETRIEVAL, AT CAUSE

### The red, reproduced from a fresh stage FIRST

```
$ python3 knowledge/_release/_gen_pack_manifest.py --stage /var/tmp/stageA --commit 2f7c47bb904b
staged 1640 paths + 1 import-closure copies -> /var/tmp/stageA
$ find /var/tmp/stageA -name '*.pyc' -o -name '__pycache__'      # clean stage, no residue
$ cd /var/tmp/stageA/memento-package/machinery
$ python3 _memento_search.py "how do I record a decision"
memento-search (Memento door): index missing at /var/tmp/stageA/memento-package/machinery/
_memento-index.json — run the build (python3 knowledge/_build_all.py); REFUSING to search nothing.
$ echo $?
1
```

### ⛔ OPTION (a) IS FALSE — measured, and the pack's own spec forbids it

The brief's pitfall said to grep the packed tree for every consumer before choosing (a). Done,
on the actual extraction:

```
$ cd /var/tmp/freshpack/Apollo-Spider-v1.0.0 && grep -rn "_build_all" . | wc -l
80
$ grep -rln "_build_all" . | wc -l
38
```

**80 sites across 38 packed files.** This is not a targeted repoint; it is a content rewrite of
the whole shipped tree. And `memento-package/machinery/_MACHINERY-MANIFEST.md` rules it out in
its own words — this is the cut's spec, quoted:

> **Rule (spec):** copies only, every copy delta-audited … These are **VERBATIM copies** —
> Apollo names (`GOOD-MORNING.md`, `_LIVE-STATE.md`, `knowledge/` paths) are still inside them,
> **on purpose**. Generalisation is a build step, not a copy step; **a half-renamed copy would
> be neither auditable nor runnable.**

A mechanical strip or repoint IS that half-rename. **The pointer is not the defect. It is the
declared consequence of a ruled packaging decision**, and rewriting it would also break
`_validate_package_delta.py`'s verbatim arm, which this lane re-ran green (`0 failure(s)`).

### ⛔ OPTION (b) IS FALSE — driven, not argued

Apollo's own builder was copied into the fresh stage and run:

```
$ cp knowledge/_build_memento_index.py /var/tmp/stageA/memento-package/machinery/
$ cd /var/tmp/stageA/memento-package/machinery && python3 _build_memento_index.py
  File ".../_build_memento_index.py", line 278, in parse_lanes
    import _gen_lanes
ModuleNotFoundError: No module named '_gen_lanes'
```

And that is only the first obstacle, not the binding one
[[refusal-names-the-first-obstacle]]. Its `build_records()` declares a corpus of
`GOOD-MORNING.md` (via `_gm_usage`), `_LIVE-STATE.md`, `_GM-ARCHIVE.md`,
`_LIVE-STATE-ARCHIVE.md`, `notes/_MEMENTO-DECISIONS.md`, `notes/_GAUGE-LOG.md`,
`notes/_briefs/*.md`, `notes/_dream/*.md`, `knowledge/_lanes.json` and the component registry —
and it **REFUSES** when any source class contributes zero records. That is the whole Apollo
memory. **A closure carries files; it cannot carry a corpus**, and it must not carry Apollo's
private record into a designer's project.

### ✅ THE ACTUAL CAUSE — declared, ordered, unbuilt, and named in the pack itself

`_MACHINERY-MANIFEST.md`, "Generalisation debt (the port's build list, in order)":

```
1. Four-function gauge shim (above) — smallest, unblocks _gen_chain.py standalone.
2. Configurable source-file names (GOOD-MORNING.md/_LIVE-STATE.md → package config) …
3. Index bootstrap for a fresh project (the no-chain arm of the ratified boot rule).
4. Capture-ritual extraction (unscoped — do not start without pricing).
```

**Item 3 is exactly this gap.** The refusal string is a symptom of an unbuilt debt item, not a
packaging accident. The pack meanwhile *promises* retrieval in two shipped documents —
`memento-package/README.md:28` ("retrieval: search + fetch anything not in the chain") and
`skills/memento-boot/SKILL.md:75` ("use `python3 machinery/_memento_search.py "<query>"`, then
`--fetch <id>`") — so shipping without it is a promise the pack cannot keep.

### THE FIX, in two halves

**(i) The bootstrap** — `apollo-spider/gumdrop/machinery/_build_memento_index.py` (411 lines,
20,093 bytes), a Gumdrop-shaped index builder over the corpus a Gumdrop project actually has:

| source | kind | required? |
|---|---|---|
| `runbooks/*.md` (`##`/`###`) | `runbook-section` | **REQUIRED** |
| `GOOD-MORNING.md` (`##`) | `gm-section` | optional — written at FIRST-SESSION §4b |
| `_LIVE-STATE.md` (`##`) | `ls-section` | optional — written at FIRST-SESSION §4b |
| `_rulings.json` `rulings[]` | `ledger-section` | optional — **ships empty on purpose** (`s219-D5` Q1) |

* **Optional-absent is SKIPPED BY NAME** in a printed roster, never silently
  [[measuring-tool-must-not-guess]]. Optional-**present-and-empty** REFUSES — that is a broken
  parser, not a quiet corpus. REQUIRED missing REFUSES. Zero records overall REFUSES.
  Unparseable store REFUSES.
* **The corpus root is WALKED, not assumed** — the nearest ancestor holding
  `_GUMDROP-MANIFEST.md`. The door ships in two machinery dirs and only one has a `runbooks/`
  sibling; a builder assuming `../runbooks` would refuse in the plugin copy for a reason that
  has nothing to do with the project.
* ⛔ **THE KIND CONSTRAINT, DERIVED AND NOT TYPED.** `_memento_search.py`'s `bucket_for()`
  returns `None` for any kind outside `KIND_ORDER`, and `_search_core.py:100-101` **skips a
  `None` bucket silently** — a record with an unknown kind is indexed and then invisible. So the
  bootstrap reads `KIND_ORDER` **out of the door beside it, by AST, at build time**, and refuses
  to emit a kind that door cannot show. The door owns its vocabulary; nothing here re-states it
  [[no-gate-parses-the-artefact]].

**(ii) The packaging decision** — `DOOR_COMPANIONS` + `companion_closure()` in
`_gen_pack_manifest.py`. The seed map lands the bootstrap at `memento-package/machinery/` and
**only there** — precisely the v1.0.0 shape that left the plugin mirror's `_gen_chain.py`
unimportable. So the mirror is filled the same way, through the proven `apply_closure` (bytes
already in the stage, so `check_pack` verifies a companion copy against the commit like any
other path), and merged into the **one** `import_closure.seeds` list every consumer already
reads. It is DECLARED rather than derived, with its reason at the declaration: `import_closure`
can read an `import`; nothing static can read *"this tool refuses until you run that other
tool"* — that dependency lives in a runtime refusal string.

`build_manifest` **REFUSES** if a declared companion is carried nowhere, and the message names
the remedy. `companions` is recorded in the manifest's `import_closure` block.

### PROVED FROM A FRESH STAGE — the brief's own test

Stage at `2f7c47bb904b`, then the two builder copies placed exactly where `pack_path` and the
companion closure will place them at the landing commit:

```
$ cd /var/tmp/stageB/memento-package/machinery && python3 _build_memento_index.py
✅ wrote …/memento-package/machinery/_memento-index.json — 21 record(s).
  sources:
    runbooks/_RUNBOOK-capture-ritual.md runbook-section  10
    runbooks/_RUNBOOK-context-gauge.md  runbook-section  11
    memento-package/GOOD-MORNING.md     gm-section       ABSENT — optional, written at FIRST-SESSION §4b
    memento-package/_LIVE-STATE.md      ls-section       ABSENT — optional, written at FIRST-SESSION §4b
    _rulings.json                       ledger-section   EMPTY — ships empty on purpose (s219-D5 Q1)

$ python3 _memento_search.py "how do I capture a decision"
memento: "how do I capture a decision"
runbook sections (5 of 18 shown — --all for more):
  [runbook:capture-ritual:when-to-run-it] When to run it
      source: memento-package/runbooks/_RUNBOOK-capture-ritual.md:16  (stage 2: --fetch …)
  …

$ python3 _memento_search.py --fetch runbook:capture-ritual:when-to-run-it
[runbook:capture-ritual:when-to-run-it] kind=runbook-section
source: memento-package/runbooks/_RUNBOOK-capture-ritual.md:16
## When to run it
At the end of **every** session that changed anything …

$ python3 _memento_search.py --fetch runbook:no-such-thing
fetch: unknown id `runbook:no-such-thing` — REFUSING (fail-loud; ds-016 class). Near misses: …
```

**And in the plugin mirror — the v1.0.0 defect's own directory** — the same three green:
`✅ wrote …/claude-plugin/memento/machinery/_memento-index.json — 21 record(s)`, and
`"throttle not a thermometer"` returns
`[runbook:context-gauge:the-gauge-is-a-throttle-not-a-thermometer]`.

### ⛔ A REAL DEFECT THE COLD START FOUND, FIXED BEFORE IT SHIPPED

The first version of the bootstrap read `GOOD-MORNING.md`/`_LIVE-STATE.md` from the **pack
root**. Driving the cold start found `FIRST-SESSION.md` §4b putting them in
**`memento-package/`** — *"These go in `memento-package/`, and the chain generator reads them by
their exact headings"*. A designer following the shipped instructions would have written the two
files and watched retrieval keep saying ABSENT. Corrected to read the **cut's** root, which is
the same root `_gen_chain.py` uses, so the chain and the index can never disagree about which
`GOOD-MORNING.md` is yours. Bitten in both directions —
`a _LIVE-STATE.md at the PROJECT root is not the cut's copy`. This is the § ① class exactly:
green in the repo, wrong in the pack, and only the drive could see it.

### BITTEN, AND MUTATION-DRIVEN

`_build_memento_index.py --selftest`: **17 bites, 0 fails** — the bite total is COUNTED at run
time, never typed (a literal goes stale the first time a bite is added and then reads as
evidence that nothing changed). The selftest scaffolds a **synthetic** door, never the real one:
in the repo this builder has no door beside it (the door arrives in the pack), so a selftest
keyed on the real file would be green only where it is not the subject. The real door's
vocabulary is proved on the STAGE, above, which is the only place the two files sit together.

`_gen_pack_manifest.py --selftest`: **195 bites, 0 fails** (was 185). Ten new companion bites,
mutation-driven:

| mutation | reds |
|---|---|
| `DOOR_COMPANIONS` emptied | `companion/fills-only-the-dir-the-seed-map-cannot-reach`, `companion/source-is-the-nearest-carrier`, `companion/needed-by-names-the-door`, `companion/why-travels-with-the-seed`, `companion/door-with-no-builder-anywhere-is-unsourced` |
| the "already beside it" check removed | `companion/fills-only-the-dir-the-seed-map-cannot-reach` (2 seeds instead of 1), `companion/idempotent-once-satisfied` |

⚠ **One bite was written wrong and the fixture corrected it, not the code.** The first version
expected the closure to fill **both** machinery dirs. It fills exactly one: `pack_path` already
lands the builder in `memento-package/machinery/`, so that door needs nothing, and the plugin
mirror is the only dir the seed map cannot reach — which is the v1.0.0 defect's dir. The bite
now says that, with the reason at the assertion.

---

## ② THE TWO KeyErrors — the measuring tool's own footprint

**The premise handed to this lane is STALE, and the timestamps say so.** Prior report 14:00:01 ·
addendum brief 14:02:41 · `knowledge/_gate_pack_imports.py` last written **14:13:57**, carrying
a comment that reads *"This is the #220-addendum fix; see the docstring."* The gate acquired a
pristine-tree-per-probe restore **after** both documents were written, and the prior report's
§ ② ⚠ box, finding 5 and question 5 describe the gate as it was before that.
[[premise-ages-faster-than-rule]] — verify the premise like repo state.

### Reproduced, then caused, then disproved — in that order

1. **The shipped index HAS the block.** `graph-index.json` in the stage:
   `top keys ['$description','by_component','by_sc','external_automatable_refs','generated','rules','totals','verification']` · `has verification: True`.
2. **Importing `_build_compliance_kg.py` STRIPS it.** It is a WRITES-AT-IMPORT file — it
   regenerates `graph-index.json` and 39 rule files at module scope. Driven in a copied tree:

   ```
   before: verification True
   Index: 38 SCs x 133 components
   AFTER import _build_compliance_kg.py — keys: ['$description','by_component','by_sc','generated','rules','totals']
   has verification: False
   ```

   The `verification` and `external_automatable_refs` blocks are added later in a full build by
   `_build_verification_edges.py`, which the probe never runs.
3. **On the mutilated file, both downstream builders raise the exact red:**
   `_build_kg_diagram.py:22` → `KeyError: 'verification'`, `_embed_kg_fragment.py:20` →
   `KeyError: 'verification'`.
4. **On a PRISTINE pack copy, `_build_kg_diagram.py` imports and does its job:**
   `wrote …/_KG-COMPLIANCE-DIAGRAM.html · {'nsc': 38, 'ncomp': 133, 'nedge': 816, 'verified': 4, 'axe': 16}`.

### The mutation control — it reproduces the prior report's counts exactly

`restore()`'s loop replaced with `for rel in []:` and run on the frozen v1.0.0 zip:

```
  75 python file(s) … · ERROR-AT-IMPORT 2 · MISSING-LOCAL 1 · MISSING-THIRD-PARTY 1 · OK 68 · RAN-AS-SCRIPT 3
  ⚠ ERROR-AT-IMPORT  knowledge/compliance/_build_kg_diagram.py   KeyError: 'verification'
  ⚠ ERROR-AT-IMPORT  knowledge/compliance/_embed_kg_fragment.py  KeyError: 'verification'
  ⬛ WRITES-AT-IMPORT — 57 file(s) changed the pack just by being imported.
```

**Byte-for-byte the prior report's § ② line.** With the clause restored, the same command on the
same zip gives `MISSING-LOCAL 1 · MISSING-THIRD-PARTY 1 · NEEDS-ARGV 1 · OK 69 · RAN-AS-SCRIPT 3`
and `WRITES-AT-IMPORT — 5`. The 57-vs-5 gap is the residue cascade: without the restore, one
file's audit output is attributed to every file probed after it.

### VERDICT — no exclusion, no fix, nothing shipped is broken

Neither builder is defective in v1.0.0 or in the new stage. `_embed_kg_fragment.py` is now
classified `NEEDS-ARGV` — *"Usage: python3 knowledge/compliance/_embed_kg_fragment.py
<trace.html>"*, an `IndexError` whose own traceback indexes `sys.argv` — which is honest and is
named so it is never re-triaged as a packaging defect. **The s219-D9 exclusion table is
untouched, the ruled gate count is untouched, and nothing was fenced.** The prior report's
question 5 ("fix, or exclude?") does not need an answer: there is nothing to fix.

⛔ **The general lesson, and it is the expensive one.** A gate that mutates its subject reports
its own footprint as the subject's defect, and it reports it *by name*, *with a traceback*, and
*consistently across two subjects* — which is exactly what evidence looks like. Two documents
and one delegated brief carried the false finding forward before anything drove it. The clause
that removes it is eight lines.

---

## ③ THE RE-RUN — every gate, real exit codes

Captured with each command's own `$?`, never through a pipe.

| command | rc | reading |
|---|---|---|
| `_gate_pack_imports.py --stage /var/tmp/stageC` | **0** | `78 python file(s) … MISSING-THIRD-PARTY 1 · NEEDS-ARGV 1 · OK 73 · RAN-AS-SCRIPT 3` · `✅ every packed module imports from where it lands` — **MISSING-LOCAL 0, ERROR-AT-IMPORT 0** |
| `_gate_pack_imports.py --zip …v1.0.0.zip` (control) | **0** | `MISSING-LOCAL 1` — exactly the predicted file and module. The known-answer control still bites |
| `_gate_pack_imports.py --selftest` | **0** | `selftest: 18 bites, 0 fail(s)` (was 11) |
| `_gen_pack_manifest.py --selftest` | **0** | `selftest: 195 bites, 0 fail(s)` (was 185) |
| `_build_memento_index.py --selftest` | **0** | `selftest: 17 bites, 0 fail(s)` — NEW |
| `_gate_frozen_release.py --check` | **0** | `PASS — 3 arm(s) asked, no frozen surface moved` |
| `_gate_frozen_release.py --selftest` | **0** | `selftest: 14 bites, 0 fail(s)` |
| `_gate_ci_template.py --check` | **0** | `PASS — the template parses, ships what it calls, and hides nothing` |
| `_gate_ci_template.py --selftest` | **0** | `selftest: 10 bites, 0 fail(s)` |
| `_gate_doc_rows.py --check` | **0** | `population 96 · unrowed 0` |
| `_validate_package_delta.py` | **0** | `0 failure(s)` — verbatim set byte-identical, both chains clean |
| `build-designer-pack.sh --selftest` | **0** | all refusal arms green; the dirty-tree arm DROVE |
| `_gate_release_audit.py --drift` | **1** | ADVISORY, `continue-on-error` in CI. PRE-EXISTING: manifest at `2f7c47b`, HEAD `367b4182` |
| `_gate_release_audit.py --pack` | **1** | ⛔ RED BY DESIGN until the bake — unchanged from the prior report |
| `_gate_release_audit.py --check` | **1** | ⛔ **NEW RED BY DESIGN — see below** |
| `_gate_release_audit.py --selftest` | **1** | ⛔ **same cause, same remedy** |

### ⛔ THE NEW RED, AND WHY IT IS CORRECT

```
RuntimeError: COMPANION CLOSURE REFUSED — 2 packed door(s) declare a companion the pack carries
NOWHERE at this commit: [('memento-package/claude-plugin/memento/machinery/_memento_search.py',
'_build_memento_index.py'), ('memento-package/machinery/_memento_search.py',
'_build_memento_index.py')].
   The ship list comes from `git ls-tree` AT THE COMMIT, so an UNCOMMITTED companion is
   invisible here and this refusal is what an incomplete pair looks like.
   REMEDY, one of: (1) commit the companion … and re-run at the new sha; or (2) delete its
   DOOR_COMPANIONS entry, which is a decision to ship a door that cannot be opened.
```

Both arms regenerate the manifest at a real commit, and at `2f7c47bb904b` the pack genuinely
ships a door with no builder anywhere — the finding itself. **This is not the declaration
running ahead of the tree; it is the tree being the thing the declaration describes.** It is the
same designed-red shape as `--pack`, and it clears in the same landing commit. Driven:
`companion_closure(ship)` → `unsourced 2`; `companion_closure(ship + [the pending path])` →
`unsourced 0, seeds ['memento-package/claude-plugin/memento/machinery/_build_memento_index.py']`.

⚠ **Consequence the conductor must hold: `_pack_manifest.json` on disk cannot be regenerated in
this lane.** It still describes the 1,640-path v1.0.1 cut and is correct for that cut; it does
not yet know about the bootstrap. Step 1 of REPLAY-THESE fixes both in one commit.

### THE RENDER PROOF — re-measured from a FRESH extraction

Subject: `/var/tmp/drybake/Apollo-Spider-v1.0.0.zip`, the prior lane's dry-bake at
`2f7c47bb904b`, verified before use — **18,977,431 bytes · 1,644 entries · sha256
`9fecdc8f5361755e…`**, identical to the prior report. Extracted clean into `/var/tmp/fresh220`
(1,644 files). This lane changed no image, page or manifest group, so this zip is the correct
subject; the two builder copies were added only for the cold start below.

Headless Chromium per `_RUNBOOK-render-verify.md` — `ls` then **`ldd`, never a launch attempt**
(`0` "not found"); `PLAYWRIGHT_BROWSERS_PATH=/var/tmp/pw-browsers-220`,
`LD_LIBRARY_PATH=/var/tmp/chromelibs-220/root/usr/lib/aarch64-linux-gnu`,
`PYTHONPATH=/var/tmp/pylibs`. Every `<img>` forced `loading='eager'`, page scrolled to the
bottom, **every image awaited via `img.decode()`** before counting.

```
bento.html               44 images ·  44 loaded ·   0 broken · 0 failed req
grids-gallery.html       30 images ·  30 loaded ·   0 broken · 0 failed req
bento-rails.html          7 images ·   7 loaded ·   0 broken · 0 failed req
grids-display.html        7 images ·   7 loaded ·   0 broken · 0 failed req
photography.html        502 images ·  60 loaded · 442 broken · 243 failed req
```

**Dave's ask holds on re-measurement.** `photography.html` is unchanged and remains the prior
report's question 2, undecided here.

### THE COLD-START DRIVE — including retrieval, for the first time

From `/var/tmp/fresh220/Apollo-Spider-v1.0.0`, following `FIRST-SESSION.md` in order:

| step | command | reading |
|---|---|---|
| 1 | `python3 memento-package/_state.py` | `items 0 · live 0 · conditioned 0 · UNCONDITIONED 0` |
| 2 | `_gen_chain.py --check` | `✗ … GOOD-MORNING.md is missing` — correct; §4b is where the designer writes it |
| 3 | the same on the **plugin** copy | identical sentence — the v1.0.0 red stays gone |
| 4 | wrote §4a/§4b skeletons, then `_gen_chain.py` | `✅ _CHAIN.md: 833 tiktoken cl100k_base · FILE 833 = slice 200 + wrapper 633 · fixed point in 2 pass(es)` |
| 5 | `_gen_chain.py --check` | `✅ _CHAIN.md is FRESH — byte-matches the live chain` |
| 6 | **`_build_memento_index.py`** | `✅ wrote … — 23 record(s)` · roster names all five classes, GM **1**, LS **1** |
| 7 | **`_memento_search.py "what did I open first session"`** | answers with `[gm:do-this-first]`, `[ls:latest-delta-chat-1-2026-08-27]` and `runbook sections (5 of 19 shown — --all for more)` |
| 8 | **`--fetch ls:latest-delta-chat-1-2026-08-27`** | returns the designer's own delta **verbatim** |
| 9 | `_build_memento_index.py --check` | `✅ _memento-index.json is FRESH — 23 record(s)` |
| 10 | the whole of 6–8 on the **plugin** copy | `✅ wrote … — 23 record(s)`, answers identically |

**The designer's own record is retrievable on day one.** Steps 6–10 could not be run at all
before this lane.

**The Copilot bridge stays UNPROVEN — declared, not claimed.** `.github/prompts/` and
`copilot-instructions.md` are in the extraction; nothing in this sandbox can open VS Code.

---

## FINDINGS — four

1. ✅ **REPAIRED.** Gumdrop retrieval could not be started, and neither offered option was the
   cause: the pointer is a ruled consequence of the verbatim-copy rule (80 sites, 38 files), and
   Apollo's builder cannot travel (driven: `ModuleNotFoundError: _gen_lanes`, plus a corpus of
   the whole Apollo memory). The cause is **generalisation debt item 3**, declared in the pack's
   own machinery manifest and unbuilt. Closed, with a companion closure so it reaches both
   machinery dirs.
2. ✅ **INVALIDATED, not repaired — there was nothing to repair.** The two
   `KeyError: 'verification'` reds were `_gate_pack_imports.py`'s own footprint. Reproduced,
   caused, and mutation-controlled. **The prior report's finding 5 and question 5 are withdrawn.**
3. ⛔ **A gate that mutates its subject manufactures evidence.** The false finding was named,
   traceback'd, and stable across two subjects — indistinguishable from a real one — and it
   travelled through a report, a brief and a delegation before anything drove it. The class:
   before believing a new instrument's red, ask what the instrument wrote.
   [[green-tests-cannot-see-scope]] [[measuring-tool-must-not-guess]]
4. ⛔ **`showroom/_foundations/photography.html` still ships 442 broken images** (502 refs, 60
   resolved), re-measured. Unchanged; still the prior report's question 2.

---

## RULING-SHAPED QUESTIONS → DAVE (nothing decided here)

The prior report's questions 1, 2, 4 and 6 stand unchanged. **Question 5 is WITHDRAWN** (§ ②).
**Question 3 is ANSWERED IN PART and re-put** below, plus two new.

1. **Question 3, re-put with the measurement behind it.** The prior report proposed shipping
   `_build_memento_index.py`; driving it proved Apollo's copy *cannot* ship. What ships instead
   is a **new 411-line Gumdrop bootstrap**, and it widens the ship list: `gumdrop` group
   **18 → 19**, ship paths **1,640 → 1,641**, pack files **1,641 → 1,643**. The `gates` roster
   stays **55**, so `s219-D9`'s ruled number does not move — measured, `claimed by: ['gumdrop']`.
   **PROPOSED: yes** — the pack's README and memento-boot skill both already promise retrieval,
   and without this they promise something that cannot start. Reversible by deleting one file
   and one `DOOR_COMPANIONS` entry.
2. **Is `_rulings.json` → `ledger-section` the right mapping?** The shipped door's `KIND_ORDER`
   is fixed and Apollo-shaped; the Gumdrop cut has a rulings **store**, not a decisions
   **document**. Mapping rulings onto `ledger-section` is the closest honest fit and is declared
   at the source, but it is a vocabulary decision, not a measurement. **PROPOSED: keep** — the
   alternative is a kind the door drops silently.
3. **Should `_MACHINERY-MANIFEST.md`'s generalisation-debt list be updated to strike item 3?**
   It sits in `memento-package/machinery/`, which is the frozen package `_validate_package_delta.py`
   guards, and is outside this lane's regions. **NOT DONE, named.** Leaving it says an item is
   open that is now closed; editing it is a package-surface change. **PROPOSED: strike it at the
   landing commit, in the same edit as the version bump**, and re-run `_validate_package_delta.py`.

---

## REPLAY-THESE (conductor) — SUPERSEDES the prior report's list

⛔ **Steps 1 and 2 are new and step 1 now gates everything.** Steps 3–9 below replace the prior
report's 1–9; where a step is unchanged it says so.

1. ⛔ **`git add apollo-spider/gumdrop/machinery/_build_memento_index.py` AND bump the version in
   BOTH homes, in the same commit.** Until the bootstrap is committed,
   `_gate_release_audit.py --check` **and** `--selftest` both refuse and no manifest can be
   generated at any sha:
   ```
   knowledge/_release/_gen_pack_manifest.py:107   VERSION = "v1.0.1"
   knowledge/_release/_gen_pack_manifest.py:109   MEMENTO_CUT_VERSION = "v1.0.1"   # confirm with Dave
   apollo-spider/build-designer-pack.sh:38        VERSION="v1.0.1"
   ```
2. ⛔ **Mint a store row for BOTH new instruments** before the wrap — the doc-row gate does not
   police `.py`, so nothing will bite [[forgotten-document-class]]:
   ```
   knowledge/_gate_pack_imports.py                            (prior report's step 2)
   apollo-spider/gumdrop/machinery/_build_memento_index.py     (NEW — this lane)
   ```
3. **Re-probe and re-manifest AT THE LANDING COMMIT** (unchanged, but now also clears step 1's
   red):
   ```
   bash apollo-spider/build-designer-pack.sh --manifest --commit <landing-sha>
   python3 knowledge/_review/_make_review.py reviews/RELEASE-SPIDER-<date>-v1.html
   ```
   ⚠ `--manifest` rewrites `reviews/RELEASE-SPIDER-*.html` and strips the review pair's stamps;
   the generator now prints the correct next command — run it verbatim.
4. **Expect these numbers at the landing commit** — if they differ, something else moved:
   ```
   ship paths   1,641 + whatever the landing commit adds   ← 1,640 + the bootstrap
   pack files   ship + 2   (1 import closure + 1 companion closure)
   gates group  55 files / 35 runnable   ← must NOT be 56
   gumdrop group 19 files                ← must NOT be 18; if it is, the group rule missed the file
   import_closure.files 2 · import_closure.companions 1 · unmodelled_path_inserts 0
   ```
   The stage measured directly with the pending pair in place is **1,643 files /
   40,090,368 bytes** — the stage at the landing commit must match.
5. **Bake, check, re-seed** (unchanged):
   ```
   bash apollo-spider/build-designer-pack.sh --release --commit <landing-sha>
   bash apollo-spider/build-designer-pack.sh --check apollo-spider/dist/Apollo-Spider-v1.0.1.zip --commit <landing-sha>
   python3 knowledge/_release/_gate_frozen_release.py --seed
   ```
   ⚠ `--release` refuses on a dirty tree — commit first. ⚠ Re-seeding all rows at the bake commit
   is safe and proven so (prior report § ③(b)).
6. ⛔ **Two BLOCKING arms are rc=1 until step 5 lands, not one.** `--pack` (prior) **and**
   `--check` (this lane, companion closure). Both clear at the bake. Do not push the manifest
   change without the bake in the same push. Re-read both after — they must be `PASS`.
7. **Drive the pack-import gate on the baked zip and record the reading** (advisory, unwired):
   ```
   python3 knowledge/_gate_pack_imports.py --zip apollo-spider/dist/Apollo-Spider-v1.0.1.zip
   ```
   Expect **`MISSING-LOCAL 0` and `ERROR-AT-IMPORT 0`**. Known-answer control unchanged: the same
   command on `Apollo-Spider-v1.0.0.zip` must report exactly **1** MISSING-LOCAL.
8. ⛔ **NEW — drive retrieval on the baked zip, because nothing else will.** Extract clean, then:
   ```
   python3 memento-package/machinery/_build_memento_index.py        # expect 21 records, 5-class roster
   python3 memento-package/machinery/_memento_search.py "capture ritual"
   python3 memento-package/claude-plugin/memento/machinery/_build_memento_index.py   # the mirror
   ```
   A `⛔ index build REFUSED` here means the companion closure did not place the builder — check
   `import_closure.companions` in the manifest before doing anything else.
9. **Render proof + still-owed items** (unchanged): env survives at `/var/tmp/pw-browsers-220`,
   `/var/tmp/chromelibs-220`, `/var/tmp/pylibs`; force `loading='eager'` and `await img.decode()`
   or lazy loading gives a false green. Still owed from the replay lane:
   `python3 knowledge/_build_memento_index.py` (Apollo's, for step `[107]`) staged before the
   `#220` wrap.
10. ⛔ **Tell Dave the prior report's finding 5 / question 5 are WITHDRAWN.** He may already have
    seen "two compliance builders are broken in the pack". They are not.

---

## WHAT WAS WRITTEN

```
 M knowledge/_release/_gen_pack_manifest.py    # § ①(ii) DOOR_COMPANIONS + companion_closure
                                               #         + 10 bites   (+172 lines this lane)
?? apollo-spider/gumdrop/machinery/_build_memento_index.py   # § ①(i) NEW, 411 lines
```

**Not mine and untouched**, all already dirty when this lane opened:
`knowledge/_release/_gate_frozen_release.py`, `knowledge/_release/_pack_manifest.json`,
`reviews/RELEASE-SPIDER-2026-08-26-v1{,.REVIEW}.html`, `knowledge/_gate_pack_imports.py`,
`knowledge/_graph-mark-observations.jsonl`, `notes/_REHEARSAL-LOG.jsonl`,
`notes/_dream/_GRADE-DECISIONS.jsonl`, and every other `#220` brief and sub-report.
`_build_all.py`, `.github/workflows/gates.yml` and the frozen v1.0.0 zip untouched.

---

## UNPROVEN / CLAIMED (ADR-0016)

* ⛔ **UNPROVEN: the companion closure at a commit that contains the bootstrap.** The lane may
  not commit, so every manifest figure above that depends on the new file being in `git ls-tree`
  is a PROJECTION over two measured inputs (§ ⓪). What IS measured: the stage with the pending
  pair in place (1,643 files), the group claim (`['gumdrop']`), and `companion_closure` over the
  real ship list with and without the path (`unsourced 2 → 0`). Priced at REPLAY-THESE step 3,
  which runs anyway.
* ⛔ **UNPROVEN: a baked v1.0.1 zip carrying the bootstrap.** No zip was baked. Every stage proof
  is a `--stage` + overlay, not an extraction. Step 8 is the real test.
* **UNPROVEN: the Copilot bridge.** Unchanged — nothing here can run VS Code.
* **UNPROVEN: anything CI says.** No Actions run was read; blocking/advisory routing read in
  `gates.yml` source.
* **DECLARED LIMIT:** the companion closure is a DECLARED table, not a derivation. A door added
  later with an undeclared companion is invisible to it, exactly as a module the pack carries
  nowhere is invisible to the import closure. The runtime gate cannot see this class either — a
  missing index is a refusal at run time, not at import.
* **CLAIMED, then re-read:** every figure here was taken from a probe run in this window and
  quoted from its own output. Exit codes captured with each command's own `$?`.

⚠ **RED FLAG — concurrent writers.** `knowledge/_gate_pack_imports.py` changed on disk at 14:13,
after both the prior report and this lane's brief were written, carrying a comment naming this
addendum. Another agent or the conductor was editing release-side files during this lane.
Reconcile the working tree by path before committing; do not `git add -A`
[[feedback-worktree-reconcile-trail]].
