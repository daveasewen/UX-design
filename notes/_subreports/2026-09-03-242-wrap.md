# `#242`-`wrap` — the delegated capture ritual for #242: two rulings, `W-387` closed, and the boot-drift line the previous wrap correctly refused to write

session: `#242` · 2026-09-03
window: conductor window (FABLE), delegated wrap sub (OPUS)
sub index: `wrap`
brief: `notes/_briefs/2026-09-03-242-delegated-wrap-brief.md`
tokens: `UNMEASURED at the sub's own seat as a SPEND; the seat's FILL is measurable and is 167,019 real, peak across 38 turns (knowledge/_checkin.py, first-hand). The two are different quantities and neither is converted into the other.`

## VERDICT

The capture ritual ran **COMPLETE** inside the brief's DO-NOT-RULE fence: steps 1, 1b, 2, 2c, 2d, 2f and 2g
all executed, 2e was legitimately empty and is declared with its size, and step 3 (memory) was the
conductor's and is declared as ZERO bytes written here. Every roll went through `_gm_move.py` from
session-owned, uniquely-named ops files under `knowledge/_tmp/wrap242/`, each asserted for existence and a
size floor **in the writing process**; nothing was hand-edited except the `notes/_GAUGE-LOG.md` append the
gate's own legal form demanded. **Nothing was ruled beyond `s242-D1` and `s242-D2`, no prior ruling was
re-worded, and no constant, band, ceiling, floor, advisory, stop line or wall was moved.**

★ **The headline is that the brief's central prediction was CONFIRMED at this seat, and it is the exact
prediction #241's wrap refused to act on.** `boot-drift` went **RED on #241's 69,092** the moment this
wrap's 2f roll put that reading into `notes/_GAUGE-LOG.md` — *newest #241 69,092 · derived band 74,799
±2,589 (n=7, #235–#241) · delta −5,706 · past the 2× red line of ±5,177* — and the DECLARED line was
written in the gate's own legal form with the gate's own figures. `boot_constant_drift_check` then returns
**0 fails**.

`s242-D1` and `s242-D2` were inscribed via `_inscribe_ruling.py` (**333 → 334 → 335**, reconstruction proof
PASSED twice) and both ids were read back off the store. `W-387` was **CLOSED** with its receipt and eight
rows were **MINTED** (`W-394` … `W-401`). One carry was **STRUCK** with its `s188-D2` receipt, **320 ages
were bumped**, seven `[NEW — 0]` became `[1]`, and **nine** new carries were written at age 0.

COUNTS: findings `8` · ruling-shaped `4` · UNPROVEN `5`

## What was done

**Rulings first, because the record cites them.** `knowledge/_inscribe_ruling.py --entry … --dry-run` then
`--write`, twice. `s242-D1` — `MEMORY.md` is a progressive-disclosure stub. `s242-D2` — the plugin-roster
skills are pruned from the boot. Both carry Dave's three-line answer verbatim in `says`; both carry a
**NON-REPO `governs` entry DECLARED per `s191-D2`** (the auto-memory store; the plugin panel), which the
inscriber accepted without a refusal. ⚠ **`s242-D2`'s sequencing clause is written into the ruling body as
the CONDUCTOR'S RECOMMENDATION, explicitly not as ruled** — the brief demanded it and the reason is
`s241-D2`'s own history (a ruling is write-once, and #241's carries lane D's inflated headline forever).

**Step 1 — `_LIVE-STATE.md` refreshed.** New `## ⏱ LATEST DELTA — 2026-09-03 … #242` (17 lines); the
`*Last refreshed:*` header rebuilt with the #242 summary leading, #241 and #240 kept as `Previous:`
segments, and a dated trim note in #239's place. Date from `date` (2026-09-03, Thu), never from belief.
`python3 knowledge/_validate_standing_instructions.py` → **PASS, 28 standing docs reachable.**
`knowledge/_REVIEW-SIGNOFF.md` NOT touched — this session left no review artefact awaiting Dave.

**Step 1b — dossier written.**
`_DECISION-HISTORY/2026-09-03-242-the-first-dieted-morning-and-the-polarity-receipt.md` with its
`provenance: 242 · 2026-09-03` / `status: observed` block and both-way links. It records the arc: the stale
premise the session opened on · why the verifier had to run in the same wave as the builder · why the boot
decomposition's remainder is not a measurement · how a lane finding was falsified by Dave inside the same
session · why a recommendation was kept out of a ruling's `ruled` text · and why the boot figure is stated
once.

**Step 2 — `GOOD-MORNING.md`.** New ★ LATEST #242 banner: **10 substantive lines, exactly 1,200 tape**
against `s241-D2`'s cap — the second banner ever bound by it, and again at zero margin (finding 2).
`TITLE THE NEXT CHAT` → `Apollo - #243: the stub's first boot — polarity V2 is live`. `size:` stamp
re-written LAST and iterated to an **EXACT fixed point** (GM 35,331 tape, claim 35,331).

**2c — banner roll + the carry set.** `## Batch 2026-09-03 #242` opened in `_GM-ARCHIVE.md`; the ★ PRIOR
**#240** banner moved there **verbatim, 16 lines**, by `{"op":"move"}`; the #241 banner demoted to ★ PRIOR
in the same `replace` that inserted #242's. The whole carry set was written into `_CARRIES.md` §
`## residual → #243` (clause (i), authored in its home), and the banner carries the pointer + the probeable
count. **`_capture_gate._carry_items` reads 207 on the #242 line and 214 on the #243 line.**

**2d — delta roll.** The #239 ⏱ PRIOR DELTA moved **verbatim, 14 lines**, to `_LIVE-STATE-ARCHIVE.md` §
`Rolled 2026-09-03 #242`, and #239's `Previous:` chain segment (**6,494 chars**) moved into the **same**
section in the same batch, replaced in the spine by a dated trim note.

**2e — nothing to do, and the size is ZERO DATED STRATA.** `## ⬛ DO THIS FIRST` measures **109 lines /
7,120 tape** and a `#### ` scan of the region returns **0**.

**2f — stratum split.** `{"op":"roll_2f","session":241}` → **17 lines** appended at true EOF of
`notes/_GAUGE-LOG.md` and **3 lines** into `_GM-ARCHIVE.md` under the newest batch, driven with the
SESSION-QUALIFIED anchor `> **COMMIT STATE #241:**`. The new `#### 2026-09-03 #242` stratum (18 lines,
plus the `s214-D6` measurement inserted after the stamp) then written into `GOOD-MORNING.md` § ⏱ SESSION
STRATA, GM keeping LATEST only (#96/#95/#40/#41/#42 are EXEMPT by Dave's #58 ruling and were not touched).

**2g — retrieval index rebuilt LAST**, after every GM/LS/carries edit was final, and staged.

**Step 3 — memory: ZERO bytes written at this seat.** The conductor's, done at his. NON-REPO, outside every
gate; this sub neither wrote nor read it back and claims nothing about the store's contents.

**Step 4 / 4b — decision nodes + titles.** `W-387` CLOSED in `knowledge/_state.json` via `_state.py`'s
`load`/`add`/`save` API with a `closed_by` naming lane P, V2 and P2; `W-394` … `W-401` MINTED (the three
lane/verifier reports, `_boot_decompose.py`, the three briefs under the forgotten-document class, and this
report). `_gen_titles.py --session 242` run and the receipt regenerated — finding 5 records where its
generated titles differ from the brief's. **Step 4c** — the wrap's ops files live in
`knowledge/_tmp/wrap242/` (gitignored, in-mount, never `/tmp`).

**Step 5 — commit + push.** `SESSION_N=242 bash knowledge/_git_commit.sh --wrap --reconciled <msgfile>`
then the authorised push (`s207-D1`). ⬛ **DECLARED GAP: this file cannot carry its own commit hash or the
push verdict — a report written before the commit cannot name it.** Both are read back from
`git log -1 --format='%h %s'` and `git rev-parse origin/master`, and quoted in the stub.

## Findings

1. **The brief's central prediction was CONFIRMED, and it is the one #241 was right to refuse.** #241's wrap
   was told to write a `boot-drift DECLARED` line, found `0 fails` at its own seat, and refused — correctly:
   both boot checks parse `notes/_GAUGE-LOG.md` only, and #241's reading was still in the GM stratum. This
   wrap's 2f roll put it in the log, the check fired with **1 fail**, and the declaration was written with
   the gate's own figures. ⇒ **A refused false inscription at one wrap became a true one at the next, with
   nothing lost in between** — the honest posture cost one session of delay and zero accuracy.
2. **The S1 cap was met a second time, again at exactly 1,200 tape, and again by moving rather than
   dropping.** Eight measured drafts. The gauge, the declared skips and the not-done list live in the ⏱
   LATEST DELTA, the home the cap's own text names. ⚠ Lane E's UNPROVEN ("whether a banner can carry its
   record inside 1,200 tape") is now answered YES twice — for a three-lane session and a four-sub session.
   It is still not answered in general.
3. **⛔ A CAPPED BANNER DOES NOT CAP THE CHAIN, and this wrap is the measurement.** `_CHAIN.md` went
   **8,194 → 8,385 tape, +191 UP**, while the banner sat at exactly 1,200 in both sessions. The rise is
   entirely in the other two thirds of the chain: the GM **header** (a longer `size:` stamp) and the LS
   **⏱ LATEST delta** (**3,181 tk** against #241's 2,967). ★ **Two of the chain's three parts are outside
   the cap entirely**, so `s241-D2`'s S1 can hold forever and the chain can still grow. `M10 read chain`
   warns at 7,700 and names 10,000 as its block-candidate; the chain was already over the advisory at #241
   and is further over now. ⛔ Reported, ADVISORY, not repaired — the remaining levers are Dave's.
4. **The 2c/2d EXIT CHECKS found one term with no literal home, and it turned out to be SATISFIED rather
   than orphaned.** The rolling #239 delta carries *“a legal form for a NEW / RETIRED polarity”*; a probe of
   `_CARRIES.md` § `residual → #243` returns no literal match for it. It is present **in substance** — that
   legal form is exactly what `s240-D3` gave the KG and what lane P built — and its carry is the ONE struck
   at this wrap. ⚠ **The probe is named because a probe that misses is not an absence**
   [[unmatched-grep-is-not-an-absence]]: a term can be discharged rather than dropped, and only reading the
   claim tells the two apart.
5. **The generated titles and the brief's titles differ, and the brief's are the ones delivered.**
   `_gen_titles.py --session 242` derives RENAME `Apollo - #242: first dieted morning, back to product:
   w-387 built, verified, closed` and NEXT-TITLE `Apollo - #243: dave's answer to the six-controls reading —
   literal shape vs green` (truncated from the ⬛ bullet). The brief names `Apollo - #242: first dieted
   morning — back to product` and `Apollo - #243: the stub's first boot — polarity V2 is live`, and the GM
   header carries the latter. `title_generation_check` asserts the receipt exists and matches the banner
   session — it does not assert equality with the header — so both stand and the divergence is reported.
6. **The carry-count arithmetic moved 207 → 214 while NINE items were added, and the cause is unchanged
   since #230.** `_capture_gate._carry_items` counts only `·`-segments carrying a DIGIT bracket: #241's
   seven `[NEW — 0]` items became countable on ageing and this wrap's nine arrive uncounted. Measured in one
   programmatic pass: **320 title-closing age brackets bumped** (`[N]**`, `[N, DAVE'S]**`, `[N]~~**`,
   `[N, DAVE'S]~~**`) plus **7 `[NEW — 0]` → `[1]`**. ⚠ **Eight `[NEW — 0]` tokens exist in the line but
   only seven are title-closing** — the eighth is a MENTION inside a body, and it was correctly not
   transformed, which is the whole reason the bump is anchored on the following `**`.
7. **The two unreconciled ordinals are still unreconciled, and this is the third consecutive wrap to say
   so.** `grep -c 'BANNER-DISCIPLINE MEASUREMENT' notes/_GAUGE-LOG.md` reads **28** while the newest NAMED
   ordinal is TWENTY-FIFTH; the `wrap-handover:` count has the same shape (prose mentions of the probe
   counted alongside real rows). ⛔ Both REPORTED, neither repaired, and no third figure invented.
8. **The `wrap-handover` row for this session is honest but not clean, and it is labelled.** `brief-cut` is
   the conductor's **DECLARED ≈140,000**, not a measurement, so the `delta 27,019` inherits that
   imprecision. ⚠ `s214-D4`'s staging waits on n≥2 clean pairs; this row should not be counted as one.

## RULING-SHAPED QUESTIONS

1. **Should the S1 cap be joined by a chain-side cap, now that finding 3 has measured the leak?** The banner
   is capped and the chain still grew. (a) leave it — the chain has an ADVISORY and that is enough ·
   (b) cap the LS ⏱ LATEST delta the way the banner is capped · (c) cap the GM header's `size:` stamp ·
   (d) cap `_CHAIN.md` itself and let the wrap choose where to give. **Recommend (b)**, because the delta is
   where the cap's own text sends the displaced material, so the two caps have to be designed together or
   the first simply pushes cost into the second. ⛔ Not done here — gate change and Dave's number.
2. **Should the boot checks read the CURRENT session's GM stratum?** Finding 1 shows the lag is real: the
   wrap that produces a step change cannot see it and the wrap that inherits it declares someone else's.
   This is `notes/_subreports/2026-09-02-241-wrap.md` § RULING-SHAPED 2, carried unanswered. (a) leave it —
   the log is the one home and the lag is honest · (b) have the parser also read the live GM stratum ·
   (c) move the 2f roll earlier in the ritual. **Recommend (a) now**, having watched it work: the delay
   produced a *correct* declaration rather than a guessed one.
3. **Do the two unreconciled ordinals get repaired, retired, or left reported (finding 7)?** Third
   consecutive wrap to raise it. (a) fix the probes (`grep -c '^> \*\*wrap-handover:'`) and re-derive once ·
   (b) stop naming ordinals and publish only the probe · (c) leave both reported. **Recommend (b)** — an
   ordinal nobody can reproduce is a claim wearing a receipt's clothes.
4. **Should a `wrap-handover:` row be allowed to carry a DECLARED brief-cut at all (finding 8)?** (a) yes,
   labelled as here · (b) require the conductor to measure it or write `unobservable` · (c) add a
   `declared:`/`measured:` field so `s214-D4`'s staging can filter. **Recommend (c)**, and it is small.
   ⛔ Not done here.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** the CI state for `a09a3ea`, `7f8801f` and this wrap's push — no verdict was read at this
  seat and the next blocker past step 10 of 142 is unknown. Price: the capped route, run page + one JS grep,
  ~10K.
- **UNPROVEN:** `python3 knowledge/_capture_gate.py --selftest` was NOT re-driven here (it exceeds the
  ~178 s sandbox call wall), and `_build_all.py` was NOT run (fenced, and sandbox-impossible in one process).
- **UNPROVEN:** that the S1 cap is livable beyond these two sessions. n=2, both delegated wraps of similar
  shape.
- **UNPROVEN:** the attribution of the −5,706 boot step between the roster diet and the `MEMORY.md`
  compaction. One reading cannot decompose into two causes, and the declaration says so.
- **CLAIMED:** every figure attributed to the conductor's seat — the FILL series (74,107 / 123,303 /
  134,296), the brief-cut ≈140,000, the sub token totals (P 225,941 · F 117,270 · V2 177,041 · P2 138,652),
  Dave's quoted three lines, the quota panel, and the `MEMORY.md` 3,569 → 1,417 enactment. What was
  RE-READ here is the repo side: `knowledge/_rulings.json` parses at **335** and carries `s242-D1` ·
  `s242-D2` with their `says` fields read off the store; `_checkin.py` at this seat reads **boot 70,710
  real** and **FILL 167,019 real, peak across 38 turns**, first-hand.
- **CLAIMED:** the four subs' findings — quoted from their filed reports, not re-driven at this seat. In
  particular the polarity selftest's **137 arms / 0 failures** and V2's **43/29/29/4/0** are lane figures.

## Evidence

No evidence directory: every claim above quotes its probe inline (the mover's stdout receipts,
`_capture_gate`'s own `measure_tokens` / `boot_constant_drift_check` / `_carry_items`, `_gen_chain.py`'s
generated footer, `_roll_state.py`'s generated residual line, `_gm_usage.py --sizes --session 242`,
`_inscribe_ruling.py`'s reconstruction receipts, and the two `git` read-backs named in the stub). The wrap's
ops files were written to `knowledge/_tmp/wrap242/` — gitignored, in-mount, owned by this session — and are
working inputs, not receipts.

REPLAY-THESE: this file § RULING-SHAPED QUESTIONS (~900 tk) · `_CARRIES.md` § `## residual → #243`, the
NINE new items and the ONE strike only (~2,400 tk)
