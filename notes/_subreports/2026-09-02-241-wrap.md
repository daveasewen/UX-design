# `#241`-`wrap` — the delegated capture ritual for #241: two rulings, three lanes, and the first banner ever bound by a cap

session: `#241` · 2026-09-02
window: conductor window (FABLE), delegated wrap sub (OPUS)
sub index: `wrap`
brief: `notes/_briefs/2026-09-02-241-delegated-wrap-brief.md`
tokens: `UNMEASURED — no message.usage at a sub's own seat; UNOBSERVABLE and DECLARED, never estimated`

## VERDICT

The capture ritual ran **COMPLETE** inside the brief's DO-NOT-RULE fence: steps 1, 1b, 2, 2c, 2d, 2f and 2g
all executed, 2e was legitimately empty and is declared with its size, and step 3 (memory) was the
conductor's and is declared as ZERO bytes written here. Every roll went through `_gm_move.py` from
session-owned, uniquely-named ops files under `knowledge/_tmp/wrap241/`; nothing was hand-edited except
the two runbook additions the 2f EXIT CHECK required. **Nothing was ruled and no `s241-D*` was re-worded;
no constant, band, ceiling, floor, advisory, stop line or wall moved beyond what lanes B and E had already
committed to the tree.** `W-386` was CLOSED with a receipt (the build landed), five rows were MINTED
(`W-389` … `W-393`, forgotten-document class), one carry was STRUCK with its `s188-D2` receipt and seven
new carries were written at age 0.

⚠ **The wrap's own headline is that the brief's central prediction was FALSIFIED at this seat.** The brief
said `boot-drift` would grade 69,092 against the derived band and go RED, and instructed a DECLARED line
in the new form. **It did not go red — 0 fails** — because both boot checks parse `notes/_GAUGE-LOG.md`
only, and this session's figure sits in the GM stratum that does not reach the log until #242's own 2f
roll. **No DECLARED line was written: a declaration for a gate that did not fire is a false inscription.**

⏱ **SUPERSEDED IN PART BY THE 2026-09-03 SECOND PASS AT THE FOOT OF THIS FILE** — the wrap gate was RED
when this pass's work was re-driven the next morning (`3 fail`), the commit had never landed, and two of
this file's figures (GM 37,411 · chain 7,949) are superseded there. Nothing above is rewritten.

COUNTS: findings `12` · ruling-shaped `6` · UNPROVEN `5`

## What was done

**Step 1 — `_LIVE-STATE.md` refreshed.** New `## ⏱ LATEST DELTA — … #241` (13 lines) written; the
`*Last refreshed:*` header rebuilt with the #241 summary leading, #240 and #239 kept as `Previous:`
segments, and a dated trim note in #238's place. Date from `date` (2026-09-02), never from belief.
`python3 knowledge/_validate_standing_instructions.py` → **PASS, 28 standing docs reachable.**
`knowledge/_REVIEW-SIGNOFF.md` NOT touched — this session left no review artefact awaiting Dave.

**Step 1b — dossier written.** `_DECISION-HISTORY/2026-09-02-241-the-measured-diet-and-the-derived-band.md`
with its `provenance: 241 · 2026-09-02` / `status: observed` block and both-way links. It records the arc:
the measurement #240 could only promise · why the ceiling is 70,000 and not 69,092 (and why waiting was
rejected) · Dave's *"getting excessive"* as the session's hinge · the ordinal-attribution class fix · the
headline that was counted once and attributed twice · what a write-once ruling does with a corrected
figure · where the next cut actually is.

**Step 2 — `GOOD-MORNING.md`.** New ★ LATEST #241 banner: **10 substantive lines, 1,200 tape** against
`s241-D2`'s cap of 10 / 1,200 — the first banner ever written under it, and it took eleven measured
drafts. `TITLE THE NEXT CHAT` → `Apollo - #242: first dieted morning — back to product`. `size:` stamp
re-written LAST and iterated to an EXACT fixed point (GM 37,411 tape, claim 37,411).

**2c — banner roll + the carry set.** `## Batch 2026-09-02 #241` opened in `_GM-ARCHIVE.md`; the ★ PRIOR
**#239** banner moved there **verbatim, 17 lines**, by `{"op":"move"}`; the #240 banner demoted to ★ PRIOR
in the same `replace` that inserted #241's. The whole carry set was written into `_CARRIES.md` §
`## residual → #242` (clause (i), authored in its home), and the banner carries the pointer + the
probeable count.

**2d — delta roll.** The #238 ⏱ PRIOR DELTA moved **verbatim, 12 lines**, to `_LIVE-STATE-ARCHIVE.md` §
`Rolled 2026-09-02 #241`, and #238's `Previous:` chain segment (**5,645 chars**) moved into the **same**
section in the same batch, replaced in the spine by a dated trim note.

**2e — nothing to do, and the size is ZERO LINES.** `## ⬛ DO THIS FIRST` measures 10,140 real and a
`#### ` scan of the region returns **0** dated strata.

**2f — stratum split.** `{"op":"roll_2f","session":240}` → **17 lines** appended at true EOF of
`notes/_GAUGE-LOG.md` and **2 lines** into `_GM-ARCHIVE.md` under the newest batch, driven with the
SESSION-QUALIFIED anchor `> **COMMIT STATE #240:**` (Finding 3). The new `#### 2026-09-02 #241` stratum
(19 lines) then written into `GOOD-MORNING.md` § ⏱ SESSION STRATA, GM keeping LATEST only (#96/#95/#40/
#41/#42 are EXEMPT by Dave's #58 ruling and were not touched).

**2g — retrieval index rebuilt LAST**, after every GM/LS/carries edit was final, and staged.

**Step 3 — memory: ZERO bytes written at this seat.** The conductor's, done at his. NON-REPO, outside
every gate; this sub neither wrote nor read it back and claims nothing about the store's contents.

**Step 4 / 4b — decision nodes + titles.** Both rulings were inscribed at the conductor's seat via
`_inscribe_ruling.py`; **not one word was re-worded here.** `_gen_titles.py --session 241` run, receipt
regenerated (Finding 8 records where its generated titles differ from the brief's). **Step 4c** — scratch
hygiene run; the wrap's ops files live in `knowledge/_tmp/wrap241/` (gitignored, in-mount, never `/tmp`).

**Step 5 — commit + push.** `SESSION_N=241 bash knowledge/_git_commit.sh --wrap --reconciled <msgfile>`
then the authorised push (`s207-D1`). ⬛ **DECLARED GAP: this file cannot carry its own commit hash or the
push verdict — a report written before the commit cannot name it.** Both are read back from
`git log -1 --format='%h %s'` and the push gate's own `✅ pushed and VERIFIED` line, and quoted in the stub.

## Findings

1. **The brief's central prediction was FALSIFIED, and the falsification is the finding.** The brief said
   `boot-drift` would grade 69,092 against the derived band, go RED, and need a DECLARED line. Driven at
   this seat AFTER the 2f roll: `boot_constant_drift_check('.')` returns **0 fails** and reports
   **DERIVED band 75,672 ±641 (n=7, #234–#240) · newest #240 76,138 · delta +465 · red beyond ±1,281 ·
   ceiling 70,000**. Cause: `_parse_boot_samples` / `boot_stratum_double_count_check` read
   `notes/_GAUGE-LOG.md` **only**, and #241's reading is in the GM stratum until #242's own 2f roll.
   ⚠ **#242 is where it fires** — a −6,580 step against a 641 spread is far past the 2× line — and lane
   B's form, with figures copied from the gate's own failure text at that seat, is what to write then.
2. **The derived band now reproduces Dave's own figure to the token.** After the #240 stratum rolled in,
   the gate reports **75,672 ±641** over #234–#240 — exactly the number he was shown before ruling
   `s240-D1`. Before the roll it read 75,994 ±1,219 over #233–#239. Both are quoted; neither is converted.
3. **`> **COMMIT STATE` is still not a unique anchor, and the runbook still taught the bare form.** #240
   found it and repaired it in its own run only; `grep -c "^> \*\*COMMIT STATE" GOOD-MORNING.md` reads
   **4** (the rolling stratum plus the three EXEMPT #40/#41/#42 blocks). The lesson had **no standing
   home** — the 2f EXIT CHECK's trigger — so it was homed BY ADDITION in
   `knowledge/_RUNBOOK-capture-ritual.md` § `s214-D6` BANNER DISCIPLINE **before** `roll_2f` ran.
4. **A second unhomed lesson rolled with the same stratum: the chain must be re-measured AFTER the
   declare-last stamp.** #240 measured `_CHAIN.md` at 11,074 tape mid-wrap and 11,319 after the stamp —
   the stamp is inside the chain slice. `grep -rn 'bigger chain through its header' knowledge/_RUNBOOK-*.md`
   returned **0**, so it too was homed by addition in the same section, and this wrap obeyed it: the
   footer figure in the #241 stratum was generated after the stamp, not before.
5. **The chain fell 11,319 → 7,949 tape, the largest single-wrap fall this field has recorded, and it is
   two ruled things compounding.** Lane E's D2 removed 2,289 tape of bare ids from the generator's slice;
   `s241-D2`'s S1 cap took the ★ LATEST banner from **3,353 tape to 1,200**. ⛔ The cap was met by writing
   SHORTER: the gauge, declared skips and not-done list moved to the ⏱ LATEST DELTA — the home the cap's
   own message names — and no item, carry, declared skip or receipt name was omitted.
6. **The cap is genuinely tight, and the honest report of that is eleven drafts.** The banner was measured
   after every edit with the gate's own `measure_tokens` on `_latest_banner_region`; it landed at exactly
   **1,200 tape**, zero margin. ⚠ A wrap with more lanes, or more Dave-quoted words, will not fit the same
   shape — which is lane E's own UNPROVEN ("whether a #241 banner can carry its record inside 1,200 tape
   is not established by anything I can run"), now answered YES for this session and only this one.
7. **The carry count moved 202 → 207 while seven items were added, and the cause is unchanged since #230.**
   `_capture_gate._carry_items` counts only `·`-segments carrying a DIGIT bracket: #240's five `[NEW — 0]`
   items became countable on ageing, and this wrap's seven arrive uncounted. Age bump measured before and
   after: **175 `[N]**` · 138 `[N, DAVE'S]**` · 1 `[N]~~**` · 1 `[N, DAVE'S]~~**` = 315 aged**, plus **5
   `[NEW — 0]` → `[1]`**, **320 brackets** touched in one programmatic pass.
8. **The generated titles and the brief's titles differ, and the brief's are the ones delivered.**
   `_gen_titles.py --session 241` derives RENAME `Apollo - #241: diet measured, band built, ritual layer
   on a diet` and NEXT-TITLE `Apollo - #242: the polarity-receipt build — s240-d3 is ruled, still not
   built, and` (truncated from the ⬛ bullet). The brief names `Apollo - #241: measure the diet, build the
   derived band` and `Apollo - #242: first dieted morning — back to product`, and the GM header carries the
   latter. `title_generation_check` asserts the receipt exists and matches the banner session — it does not
   assert equality with the header — so both stand, and the divergence is reported rather than smoothed.
9. **The `wrap-handover:` ordinal cannot be claimed from its own probe.** `grep -c 'wrap-handover:'
   notes/_GAUGE-LOG.md` reads **30** after this wrap's roll, but the count includes PROSE MENTIONS of the
   probe itself, not only rows — the same shape as the BANNER-DISCIPLINE ordinal discrepancy #240 declared
   and did not repair (now off by three: `grep -c 'BANNER-DISCIPLINE MEASUREMENT'` reads **28**, newest
   NAMED ordinal is TWENTY-FIFTH). ⛔ Both are REPORTED; neither is repaired here, and no third figure is
   invented.

## RULING-SHAPED QUESTIONS

1. **Does the S1 cap need a `DECLARED:` field, now that one wrap has met it?** Lane E's Q2 and lane D's S2
   both say the cap can enforce LENGTH and not HONESTY. This wrap met 1,200 tape by MOVING the gauge,
   skips and not-done into the ⏱ delta — which is what the cap's text asks for — but nothing mechanical
   distinguishes that from a banner that met the cap by DELETING them. (a) leave it, with the warning text
   as the only guard · (b) add a required `DECLARED:`-shaped field (lane D's S2 in miniature) · (c) raise
   the cap. **Recommend (a) for now and (b) with S2**, because a field bolted on alone makes the banner
   longer, not shorter. ⛔ Not done here — it is a gate change and Dave's number.
2. **Should the two boot checks also read the CURRENT session's stratum in `GOOD-MORNING.md`?** Finding 1
   shows the gate is structurally one wrap behind on its own session: the wrap that produces a step change
   cannot see it, and the wrap that inherits it declares someone else's. (a) leave it — the log is the one
   home and the lag is honest · (b) have `_parse_boot_rows` also parse the live GM stratum, so a wrap
   grades its own reading · (c) move the 2f roll earlier in the ritual. **Recommend (b)**, scoped to
   reading only. ⛔ Not done here — it touches a gauge check under the fence.
3. **Do the two unreconciled ordinals get repaired, retired, or left reported?** Findings 3 and 9: the
   `BANNER-DISCIPLINE` count and the `wrap-handover` count both include prose mentions of their own
   probes, so no published ordinal is re-derivable. (a) fix the probes (`grep -c '^> \*\*wrap-handover:'`)
   and re-derive once · (b) stop naming ordinals and publish only the probe · (c) leave both reported.
   **Recommend (b)** — an ordinal nobody can reproduce is a claim wearing a receipt's clothes.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** the CI state for `a09a3ea` and for this wrap's push — no verdict was read at this seat and
  the next blocker past step 10 of 142 is unknown. Price: the capped route, run page + one JS grep, ~10K.
- **UNPROVEN:** that the S1 cap is livable beyond this session. n=1, and this session had three lanes and
  two rulings; a busier one is untested. Price: the next wrap.
- **UNPROVEN:** `python3 knowledge/_capture_gate.py --selftest` was NOT re-driven here (it exceeds the
  ~178 s sandbox call wall). Lane E's run is quoted, including its ONE inherited M10 failure, which lane E
  attributed by driving three module versions in one process.
- **UNPROVEN:** the attribution of the −7,046 boot fall between the roster diet and the `MEMORY.md`
  compaction. One reading cannot decompose into two causes.
- **CLAIMED:** every figure attributed to the conductor's seat — the FILL series (85,964 / 111,012 /
  123,062), the sub token totals (B 203,905 · D 167,078 · E 210,618), Dave's quoted words. What was
  RE-READ here is the repo side: `knowledge/_rulings.json` parses at **333** and carries `s241-D1` ·
  `s241-D2` with their `says` fields read off the store, and `_checkin.py` at this seat reads **boot
  69,092 real** and **FILL 130,846 real, peak across 25 turns**, first-hand.
- **CLAIMED:** the three lanes' findings — quoted from their filed reports, not re-driven at this seat.

## Evidence

No evidence directory: every claim above quotes its probe inline (the mover's stdout receipts, the
`str.count` presence probes, `_capture_gate`'s own measurements and check functions, `_gen_chain.py`'s
generated footer, and the two `git` read-backs named in the stub). The wrap's ops files were written to
`knowledge/_tmp/wrap241/` — gitignored, in-mount, owned by this session — and are working inputs, not
receipts.

REPLAY-THESE: this file § RULING-SHAPED QUESTIONS (~950 tk) · `_CARRIES.md` § `## residual → #242`, the
SEVEN new items and the ONE strike only (~2,000 tk)

---

## ⏱ SECOND PASS — 2026-09-03, THE WRAP THAT SPANNED MIDNIGHT

*(Appended, never rewritten. Everything above is the 2026-09-02 pass as it was written that night and
stands verbatim; two of its figures are SUPERSEDED here and are named below rather than edited in place.
provenance: 241 · 2026-09-03 · status: observed.)*

**What happened:** steps 1 → 4c completed on 2026-09-02 and **the commit never landed.** HEAD was still
`a09a3ea` (#240's wrap) the following morning, with 28 dirty paths carrying three lanes' work. This pass
is step 5 only — plus the two repairs the gate demanded before it would let step 5 run.

**⛔ THE WRAP GATE WAS RED, AND THE FIRST PASS COULD NOT HAVE KNOWN IT.** Driven at 08:16 on 09-03:
`180 in scope · 3 fail · 15 warn`. The three:

1. ⛔ **`B2 PLAN BLOCK: STOP does not resolve from state` — A REAL REGRESSION, AND THE S1 CAP CAUSED IT.**
   `CHAIN_STOP_RE = re.compile(r"stop line\s*\**\s*([\d][\d,]*)")` needs the LITERAL `stop line`
   immediately before the figure. #240's chain carried *"PAST the advisory **stop line 150,929** by
   10,556"* and matched. #241's banner, rewritten to fit `s241-D2`'s 1,200-tape cap, compressed the
   same fact to *"room to the **150,929** advisory **20,083**"* — the figure survived, **the literal did
   not**, and the next seam's STOP field resolved to `UNKNOWN`. ★ **This is the S1 cap's first
   measured side-effect: a cap that enforces LENGTH cannot see a downstream LITERAL, and the
   compression that satisfied the cap silently broke a probe two files away.** ⚠ #240's own `size:`
   stamp names *"the `stop line` literal `CHAIN_STOP_RE` needs"* as one of four repairs its gate
   demanded — so this is the SECOND consecutive wrap to break it and the first to break it via the cap.
   **Repair:** the ⏱ LATEST delta's gauge line now reads *"room to the advisory **stop line 150,929**
   is **20,083**"*. ⛔ **The figure 150,929 was NOT moved** — only the phrasing the probe reads.
   `CHAIN_STOP_RE` → `150,929`, and `_checkin.py` reports **✅ SEAM CLEAR**.
2. **`_LIVE-STATE.md` "Last refreshed" zone does not carry today (2026-09-03).**
3. **`GOOD-MORNING.md` header date zone does not carry today (2026-09-03).**

**⬛ THE DATE DECISION, DECLARED BECAUSE IT IS A JUDGMENT AND NOT A MECHANICAL STEP.** Both date checks
are substring tests over `HEADER_LINES = 40`. Three options existed: **(a)** re-date the record to
09-03 — which would falsify the session's own date and orphan every `## Batch 2026-09-02 #241` /
`§ Rolled 2026-09-02 #241` archive key, the 1b dossier and four sub-report filenames; **(b)** run the
gate with its `today=` override and commit under a suppressed check; **(c)** let the record carry BOTH
dates truthfully. ✅ **(c) was taken.** Each header zone now carries a dated note saying the session ran
2026-09-02, the commit landed 2026-09-03, and nothing was re-dated to match the commit. ⚠ **The check
exists to catch an UNREFRESHED handoff; this handoff was refreshed, and a true statement satisfies it
without a back-date** — the inverse of the T-D12 incident this repo's own spine header warns about, and
the reason (a) was refused. ⬛ **RULING-SHAPED, DAVE'S: what a wrap that spans midnight should stamp.**

**DECLARE-LAST, RE-RUN.** The stamp and the chain figure the first pass wrote were pre-repair readings,
so both were re-measured and the sequence re-run in the runbook's order — repairs → `_gen_chain.py` →
`_checkin.py` → `size:` stamp iterated to an EXACT fixed point → `_gen_chain.py` AGAIN (the `s214-D6`
addition this very wrap homed: the stamp sits inside the chain slice) → `_build_memento_index.py` LAST.

| reading | 09-02 pass | **09-03 final** | vs #240 |
|---|---|---|---|
| GM | 37,411 | **37,652** | 2,812 DOWN |
| LS | 53,323 | **53,613** | 630 UP |
| corpus GM+LS | 90,734 | **91,265** | — |
| `_CHAIN.md` | 7,949 | **8,194** | **3,125 DOWN** on 11,319 |

⚠ **The two superseded figures above are the first pass's GM 37,411 / chain 7,949**; they were correct
when written and are wrong now, so they are named here and left standing in the text above rather than
edited out. The `#### 2026-09-02 #241` stratum's chain figures were corrected in place **length-neutrally**
(same digit counts) precisely so the declare-last stamp stayed at its fixed point.

⚠ **THE REPAIR PASS COST 245 TAPE ON THE CHAIN (7,949 → 8,194) AND IT IS NOT FREE.** `M10 read chain`
warns at **7,700** and names **10,000** as its block-candidate, so the chain was ALREADY over the
advisory before this pass and is further over now. Both readings are ADVISORY and neither is a trim
order. ⛔ Reported, not repaired.

**Final gate: `180 in scope · 0 fail · 15 warn`.** Three warns CHANGED (all three are the size readings
this pass moved); twelve are unchanged since the rehearse record.

### Second-pass findings

10. **A wrap's own ritual can be complete and its session still uncaptured.** Every artefact existed,
    every roll had landed, the report was written — and the record was one `git commit` away from being
    lost, with nothing in the tree saying so. ⚠ The gate's `git: N uncommitted path(s)` line is a WARN,
    not a fail, so a wrap that dies before step 5 leaves **no blocking evidence at all**.
11. **The S1 cap's first side-effect was a broken literal, not a lost fact** (finding 1 above). Lane E's
    own UNPROVEN asked whether a banner could carry its record inside 1,200 tape; the first pass answered
    YES. ⛔ **The fuller answer is: yes, and the compression moved a load-bearing phrase out of a probe's
    reach without dropping the information.** Nothing a length gate can see.
12. **`CHAIN_STOP_RE` has no test that fires at the moment of breakage.** It failed at the B2 seam —
    one file and one step downstream of the edit that broke it — and only at the NEXT gate run.

### Second-pass RULING-SHAPED QUESTIONS

4. **What should a midnight-spanning wrap stamp?** (a) the session's date throughout, with a commit-date
   note, as done here · (b) re-date the whole record to the commit date · (c) let the gate accept the
   session date when a declared note names the span. **Recommend (a)**, and it is what stands.
5. **Should `CHAIN_STOP_RE` stop depending on prose?** The stop figure is a number the seam needs and a
   phrase a banner may legally rewrite. (a) leave it · (b) have `_gen_chain.py` emit a machine line
   (`stop-line: 150929`) the probe reads instead of scanning prose · (c) add the literal to the S1 cap's
   warning text. **Recommend (b)** — a probe that reads prose is a probe a cap can break.
6. **Does the S1 cap need a literal-preservation check?** Two consecutive wraps broke the same literal.
   ⛔ Not done here — gate change, Dave's.

**Second-pass UNPROVEN:** that the 09-03 repairs are complete beyond the gate's own reckoning — the gate
is green and `_build_all.py` was NOT run (fenced, and sandbox-impossible in one process). CI on this
push is the single-process verdict, and it is the CONDUCTOR'S read-back, carried.
