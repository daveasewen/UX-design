# #254 VERIFIER V-S — cold seat on BUILD LANE S (s254-D2 items 1, 2, 3, 5, 7)

**Date** 2026-09-07 · **Subject** `notes/_subreports/2026-09-07-254-BUILD-S.md`
**Seat** COLD, read-only on the repo (no git write of any kind; `_build_all.py` NOT run).
**Tree state** uncommitted wave. The conductor changed ONE line after the lane
(`runway-bar.meta.json` `intent` `composition` → `one-number`, a Claude call under s251-D15).
Every claim below was re-derived independently; the report's own output was never trusted.

`claims 34 · green 30 · red 0 · mismatches 4`

Method: s182-D1 — one row per mechanical claim, each carrying the probe that was DRIVEN.
Where the conductor's line moved the ground under a claim that was true when written, the row
says SUPERSEDED and counts GREEN; a MISMATCH is an artefact IN THE TREE that is now false.

---

## Rows

| # | Claim (BUILD-S.md) | Probe driven | Grade |
|---|---|---|---|
| 1 | Resolver runs GREEN on the live corpus | `python3 knowledge/_validate_roles_resolve.py` → `RESULT: PASS`, exit 0 | GREEN |
| 2 | 25 carriers / 25 of 25 clean (the ruling's target) | same run: `carrying an interchangeability term: 25 · addresses resolved: 85`, zero fails | GREEN |
| 3 | Field population `provides 24 · answers 25 · shape 25 · span 25 · priority 24 · when 25 · with 1 · not-with 7` | same run, header line — identical | GREEN |
| 4 | BEFORE state: 24 carriers, sparkline a chart-panel provider, runway-bar untagged | `git show HEAD:` sweep of all 137 component metas for the DESK eight → carrying 24; `git show HEAD:knowledge/roles.json` → 13 chart-panel providers incl. chart-sparkline (priority 30); HEAD runway-bar carries none of the eight | GREEN |
| 5 | Corpus is 136 metas | `ls knowledge/components/*.meta.json` → 137; `metas()` excludes `EXAMPLE-*` (line 191–193) → 136 | GREEN |
| 6 | (item 1) `knowledge/shapes.json` is a store on the roles.json pattern: `$description` + `$grammar` + `$provenance` + `$extension` + `shapes` | json load → exactly those five top keys | GREEN |
| 7 | 23 values in the store | json load → `len(shapes) == 23` | GREEN |
| 8 | Every `shape` on every meta is IN the store | own sweep of 137 metas: 25 carriers, 23 distinct, `ON META NOT IN STORE: []` | GREEN |
| 9 | Every store entry is USED by at least one meta (CLOSED both ways) | same sweep: `IN STORE UNUSED: []` | GREEN |
| 10 | Grammar `<x-dimension> × <mark>`, multiplication sign U+00D7 consistently, one space each side | per-key check: 22 of 23 match, space-either-side holds, no `x`/`*`/`✕`/`⨯` impostor anywhere; the single exception is `parts-of-whole`, DECLARED as pre-grammar in `$grammar` and carrying its own `$note` | GREEN |
| 11 | donut and pie still SHARE `parts-of-whole` | sweep: `parts-of-whole → ['chart-donut', 'chart-pie']` | GREEN |
| 12 | kpi-tile ≠ stat-card, separated by the mark | `kpi-tile → one-measure × value-delta-and-series`, `stat-card → one-measure × value-and-delta` | GREEN |
| 13 | Each store entry's `used-by` list is true | programmatic compare of every `used-by` against the live carriers → zero mismatches | GREEN |
| 14 | The eleven page-A values were re-cut and every `when` that quoted an old spelling was updated in the SAME edit | `grep -rn` for `one-measure-with-series` / `one-measure-with-delta` / `categories-by-series` across `knowledge/**` → only the `--mutate unknown-shape` fixture (by design) and the `_memento-index.json` snapshot; zero live metas | GREEN |
| 15 | The thirteen page-B chart values are byte-identical to lane B's | `git diff HEAD --stat`: of the chart metas only `chart-bar` (a page-A value) and `chart-sparkline` are touched; the other thirteen are unmodified | GREEN |
| 16 | Check 9 `shape-known` exists and refuses | `--selftest` bites `unknown shape refuses → FAIL` and `a shape IN the store resolves → green` both PASS (the bite runs against a FIXTURE store, so `bins × frequency` is legitimately unknown there) | GREEN |
| 17 | (item 2) `knowledge/when-fields.json` — one home, 26 legal field names, `$grammar` + `$operators` | json load → `fields` 26, `$operators` `= == != >= <= ≥ ≤ < > in spans` | GREEN |
| 18 | The store is exactly the corpus's operator-token set, both directions | MY OWN sweep: split each of the 25 `when` values on the first em-dash, split the gate on AND/OR, take the left of the first operator → 26 distinct LHS; `LHS NOT IN STORE: []`, `STORE NOT USED: []` | GREEN |
| 19 | The printed per-field counts (answers 15 · shape 8 · series 6 · span.cols 5 · axes 2 · categories 2 · total 2 · fifteen singletons + verdict) | my sweep's counts are IDENTICAL, field for field | GREEN |
| 20 | 10 gate clauses carry no operator | my sweep: `noop = 10` | GREEN |
| 21 | Check 10 `when-fields-known` refuses an unknown field; prose in a gate and a field after the em-dash are not parsed | three `--selftest` bites, all PASS | GREEN |
| 22 | The stale FIXTURE (`span >= 6` → `span.cols >= 6`) was corrected, not the check relaxed | line 638 reads `when="span.cols >= 6"`; `span` is absent from `when-fields.json` and check 10 still refuses it (`--mutate unknown-when-field` RED) | GREEN |
| 23 | (item 3) Zero `"priority"` keys remain in `roles.json` | raw substring count 0 AND recursive key walk 0 | GREEN |
| 24 | "108 priority lines deleted" | `git diff HEAD -- knowledge/roles.json` deletes **109** `"priority"` lines; HEAD had 109. 108 = the ruled deletions, +1 = the sparkline provider entry's own 30 (item 5) — the report's own arithmetic in its diff-stat paragraph. Consistent, not an error | GREEN |
| 25 | `$description` no longer claims priority orders the providers; the "ILLUSTRATIVE until the 25-meta pass" sentence is updated | new text: "the METAS are the ONE home for `priority` (s254-D2 item 3 …) … this file's provider lists are a MEMBERSHIP CROSS-CHECK, not a source"; the ILLUSTRATIVE clause is gone from the diff | GREEN |
| 26 | No script anywhere reads a roles.json priority | `grep -rln "roles.json" --include=*.py knowledge/` → only `_validate_roles_resolve.py` and `_build_all.py`; `cross_check()` reads `p["slug"]` only (line 497, inside `def cross_check` at 484); `derive()` reads `docs[n]["priority"]` (the metas). `gen_itinerary_status.py` / `_state.py` "priority" are unrelated (a sheet column, a state override) | GREEN |
| 27 | (item 5) chart-panel is 13 → 12 providers, sparkline appended to `$not-a-provider` (a prose string) with reason + ruling id | roles.json load: 12 providers, `chart-sparkline` absent; `$not-a-provider` is `str` and names it with "s254-D2 item 5" | GREEN |
| 28 | chart-sparkline's `provides` and `priority` REMOVED (not zeroed), the deleted facts preserved VERBATIM in a `$provides` annotation | diff shows both keys removed and a `$provides` prose note added — but the note records the ROLE only; **the number `priority: 30` is nowhere in it**. The fact is described, not preserved verbatim | **MISMATCH** |
| 29 | kpi-tile's `with: [{slug: chart-sparkline, rel: recommends}]` validates against `meta.schema.json` | `jsonschema.Draft7Validator` against `meta.schema.json`: kpi-tile 0 errors (also runway-bar 0, chart-sparkline 0); `python3 knowledge/_build_integrity.py` → `integrity: PASS — 0 errors, 22 warnings · schema valid: 136/136`; `python3 knowledge/_validate_kg.py` → OK (both `--help`-checked first) | GREEN |
| 30 | The reciprocal is DERIVED and printed, never written | resolver prints `with: chart-sparkline with kpi-tile -> DERIVED suggests (from recommends; NOT asserted …)`; no `with` on chart-sparkline's meta | GREEN |
| 31 | (item 7) runway-bar's six fields are a fair reading of its OWN prose | `purpose`: "A READOUT that answers one question: does the money cover what is already scheduled, and until when? … shows the arithmetic that produces it (balance − committed = buffer), and draws the split on a bar." ⇒ `answers: one-number` and `shape: one-measure × committed-vs-balance-split` are a FAIR reading — the "one question" and the split are both literally in the prose; `verdict != none` is legal because `verdict` is one of the meta's own seven props. `responsive.rule`: "Fluid to a 480px max-width column; the bar is width:100% of its container. Legend and head wrap. Verified at 1400px and 480px." ⇒ `span.cols 3–6` is a fair-but-generous reading: 480px is ~4 cols of a 12-col 1400px band, min 3 is supported by the declared wrap, max 6 (~700px) exceeds the stated 480px cap and is justified only by the max-width clamping it. `priority: 30` and `provides: headline-metric` are the ruling's own | GREEN |
| 32 | 12 mutation arms behave as their names say | every arm listed by `MUTATIONS` driven: unknown-role RED-OK · known-role GREEN-OK · unknown-answer RED-OK · answer-disagrees RED-OK · dup-priority RED-OK · span-inverted RED-OK · notwith-ghost RED-OK · derive-demo GREEN-OK · unknown-shape RED-OK · known-shape GREEN-OK · unknown-when-field RED-OK · when-prose-not-parsed GREEN-OK; all exit 0. Each RED arm's NEW fail names the check its arm is named for | GREEN |
| 33 | `--selftest` → PASS, 15 bites | driven → `RESULT: PASS`, 15 bites printed | GREEN |
| 34 | The other gates pass | `_validate_intent_resolve.py` → PASS (15 carriers, 16 addresses); `_validate_help_gate.py` → OK, 325 scripts; `_build_integrity.py` → PASS; `_validate_kg.py` → OK. All four match the report | GREEN |

### Rows against the tree the lane left behind

| # | Finding | Probe | Grade |
|---|---|---|---|
| A | Receipt 7c ("resolver ends RED-BY-DESIGN on runway-bar, 1 fail") | SUPERSEDED by the conductor's line — the resolver is now `RESULT: PASS`, `base_fails = 0` in every mutation arm. The claim was true as written; the ruling's 25/25 is met. Counted GREEN in row 2 | SUPERSEDED |
| B | runway-bar's `$interchangeability` annotation still reads "⚠ `intent: composition` predates this and DISAGREES with `answers` … the resolver's check 7 is RED-BY-DESIGN on this meta until Dave rules" | the file's `intent` is now `"one-number"`; the annotation is false of its own meta. One sentence, in the same file the conductor edited | **MISMATCH** |
| C | `_build_all.py`'s selftest step row still says "roles/answers resolve selftest — **10 bites**" | the selftest now runs 15; the lane edited the adjacent gate row in the same file and left this label behind | **MISMATCH** |
| D | `meta.schema.json`'s `shape` description still says "FREE STRING: the vocabulary is NOT RULED … deliberately unenumerated here" | item 1 CLOSED that vocabulary and `meta.schema.json` is named in s254-D2's own `governs` list. The lane claimed no schema edit, so this is a gap in the enactment, not a false claim | **MISMATCH** |

### (h) `git diff HEAD --stat` vs the lane's file list

Every file the lane lists IS in the diff, and no file in the diff is one the lane touched but did not list.
The four extra paths are accounted for and are NOT lane S's: `knowledge/_gm_usage.py` (lane R, ignored by
brief), `knowledge/_rulings.json` (the conductor recording s254-D2), `notes/_REHEARSAL-LOG.jsonl` and
`notes/_dream/_GRADE-DECISIONS.jsonl` (session harness appends). Two untracked lane files present:
`knowledge/shapes.json`, `knowledge/when-fields.json`.

Numbers: the report's `runway-bar | 12 ++` is now `14`, and its "37 insertions / 19 deletions" across the
thirteen meta files is now **38 / 20** (`git diff HEAD --numstat`). The delta is exactly +1/−1 — the
conductor's single `intent` line. No other drift.

### (i) The lane's own questions

- **Q2 — CONFIRMED.** `knowledge/components/chart-line.meta.json:16`, inside `when`:
  `… and to chart-sparkline (30, "inline scale, no axes") when span.cols < 6.`
  Priority 30 is now held by runway-bar alone (sweep of all metas), and chart-sparkline carries no
  `priority` and no `provides` at all — so the clause names a co-provider that no longer exists at a
  number that now belongs to a different component.
- **Q3 — CONFIRMED**, and wider than the lane said. `reviews/META-TAGS-A-2026-09-07-v1.html` lines 75 and
  101 read `priority 50 roles.json illustrative: 50` and `priority 60 roles.json illustrative: 60`;
  `reviews/META-TAGS-CALLS-CONTEXT-2026-09-07-v1.html:134` reads `priority 84, not roles.json's 60`;
  `reviews/ROLES-REVIEW-2026-09-07-v1.html:303` reads "Priorities are 10–60 in steps of 5 … Illustrative on
  this page"; `META-TAGS-B` quotes `priority → 50` in its strike-syntax example. All four pages cite a
  roles.json field that no longer exists. **Additionally** `META-TAGS-A` quotes the PRE-cut shape spellings
  (`one-measure-with-series`, `one-measure-with-delta`, `categories-by-series`) at lines 73, 99, 124 — stale
  against item 1 as well, which Q3 did not mention.
