# `#238`-`A` — plan v2: the designer's-brain plan re-cut against what #237 and #238 closed

session: `#238` · 2026-09-02
window: lane A (plan v2), conductor Fable
sub index: `A`
brief: `notes/_briefs/2026-09-02-238-A-plan-v2-brief.md`
tokens: `UNMEASURED — no message.usage at a sub's seat`. SHAPE: ~45 tool calls — 14 `Read`, 4 `Write`, ~8 `Edit`, ~19 `bash` (one heavy render call at a time; longest ~17 s), 1 `Skill`. 0 git · 0 `_build_all.py` · 0 `_gen_chain.py` · 0 writes to any fenced file.

## VERDICT

**DONE.** `_PLAN-designers-brain-2026-09-02-v2.html` is at repo root, **GENERATED** from a template plus `knowledge/_rulings.json` by `_gen_plan_v2.py` in this lane's assets, so all **17 quoted rulings** (`s237-D1`…`D10`, `s238-D1`…`D7`) are copied from the store by machine and a `--check` proves them verbatim (17/17) and refuses the old word in prose (0 prose hits of 34). All five things the brief says v2 records are on the page: every §8 question carries its ruling id and ruled text (12/12, two at #238); the seven gaps carry receipt paths with anchors into lane G's folder, R2-2 is recorded as **dropped by Dave**, R2-3 as closed by his terminal via `s237-D8`, and R1-1's names-only / licensed-draft qualification travels in its row; P1 now **derives** the grades from `s237-D1` and the polarity shape from `s238-D1/D6/D7` and points rather than restates; the lane map P · B · C · M · V is drawn as **RUNNING** with empty receipt slots; and **28 open items** are listed with an owner each and none resolved. v1 is untouched (84,593 bytes, mtime 11:59, unchanged). Rendered light + dark at 1440 and 390: **no horizontal overflow in any of the four** (scrollWidth = clientWidth, measured on the live page); two real layout defects found at 1440 and fixed at the cause. One departure from the brief's wording is recorded in finding 2.

COUNTS: findings `9` · ruling-shaped `0` · UNPROVEN `4`

*(the brief's counts, also parsed: **questions 12 · ruled 12 · gaps 7 · discharged 7 · open items 28**)*

## What was done

**Ground first.** `_PLAN-designers-brain-2026-09-02-v1.html` in full; `knowledge/_rulings.json` — all 17 `s237-*`/`s238-*` entries printed whole (`ruled`, `says`, `governs`, `evidence`), store size **328** (321 committed + the seven uncommitted `s238-*`); `notes/_subreports/2026-09-02-237-G-gap-discharge.md` and the keys/anchors of all eight of its asset JSONs; `notes/_subreports/2026-09-02-237-T-tensions-schema.md` and its `open-tensions.json` (21 rows, four `DEFAULT+CONDITIONAL`) and `tensions.json` (30 rows); `_DECISION-HISTORY/2026-09-02-237-p0-gaps-and-tensions.md`; the five sibling briefs (B, C, M, P, COMMON) for the lane map; `notes/_subreports/2026-09-02-235-L1-receipt-gate.md` § RULING-SHAPED for L1's three questions; `knowledge/_state.json` rows W-343, W-347, W-348, W-355…W-361; the `swiss-design-system` skill via the Skill tool; the render runbook's seventh stratum. No archive was loaded whole; retrieval was by targeted `sed`/`grep`/`json`.

**v2 built as v1 + marks + rulings, never a rewrite.** Sections 0–10 keep v1's shape and prose; every v1 table gains a "State at v2" (or verdict / sort / receipt) column; §9 becomes the gaps table; a new §10 "Still open" and a new §11 receipts section (v1's §10 moved down one). The nav has 12 entries.

**The generator.** `notes/_subreports/assets/2026-09-02-238-A-plan-v2/_gen_plan_v2.py` reads `plan-v2.template.html` and the store, resolves `{{RULED:<id>}}`, `{{DATE}}`, `{{BY}}`, `{{SAYS-FRAG:<id>|<fragment>}}` (refuses unless the fragment is a substring of `says`), `{{BYTES:<path>}}` (measured), `{{OPEN-COUNT}}` (counted off the template's `<tr class="open">` rows, never typed) and `{{RULINGS-TOTAL}}`, and writes the root HTML. `--check` re-reads the OUTPUT and refuses (rc 2) if any quoted ruling is not byte-identical to the store or if the old word survives in prose. Self-test driven on mutated copies under `/dev/shm` (finding 3).

**Renders.** `_render_plan_v2.py` (modelled on `knowledge/_render/render.py`, minus the HSBC-face assert this page cannot satisfy): four renders, each measuring `scrollWidth`/`clientWidth`, body background, accent colour, and a canvas font probe; full-page PNG + legible section crops; `render-receipt.json` beside them. Eight crops read back by eye (light 1440 §0 §8 §10 · dark 1440 §4 §9 §11 · light 390 §0 §8 · dark 390 §4).

**Files written — all new, none edited:** `_PLAN-designers-brain-2026-09-02-v2.html` (repo root, 143,752 bytes, sha256 `98f00e9a…8b43a4`) · this report · under `notes/_subreports/assets/2026-09-02-238-A-plan-v2/`: `_gen_plan_v2.py`, `plan-v2.template.html`, `_render_plan_v2.py`, `_probe_open_items.py`, `open-items-probe.json`, `render-receipt.json`, 12 `crop-*.png`, 4 `plan-v2-<theme>-<width>.png` (filed as 32-colour palette), 4 `*-thumb.png` contact strips. **Outside the repo, declared:** `/sessions/wonderful-adoring-euler/mnt/outputs/syslibs/` (libxdamage1 `dpkg -x`'d per the runbook's seventh stratum — the mount is the durable half). **Not touched:** v1, the T review, any dated report or brief, `_rulings.json`, `_state.json`, `_CARRIES.md`, `GOOD-MORNING.md`, `_LIVE-STATE.md`, `_CHAIN.md`, `_REVIEW-SIGNOFF.md`, memory.

## Findings

1. **Every ruled text on the page is the store's, proved by re-reading the output.** `_gen_plan_v2.py --check` → `quoted rulings: 17 (17 distinct) — verbatim in output: 17/17 … CHECK OK`, rc 0. *Probe:* the command, and `sha256sum _PLAN-designers-brain-2026-09-02-v2.html` = `98f00e9a0f394edcf3890eb0df7b62713558722ed0a2872b67bdb904b98b43a4` at filing.

2. **Departure from the brief, recorded here per `s237-D7`.** The brief's line "Lane T's five questions → `s238-D3`…`D7`" is shorthand the store does not support: `s238-D1` says *"This is lane T's reading (b)"* (T Q1, jointly with R1 Q4), `s238-D3` *"Lane T's question 2"*, `s238-D5` *"Lane T's question 4"*, `s238-D6` *"Lane T's question 5"*, and `s238-D4` answers T Q3 (the rename); **`s238-D7` is the gate that answers lane T's eight-row consequence register**, not one of the five. v2 §8 maps them as the store states it (T1→D1, T2→D3, T3→D4, T4→D5, T5→D6, gate→D7). No id was invented and no text re-worded. *Probe:* the `ruled` strings quoted above, `grep -n "Lane T's question" knowledge/_rulings.json`.

3. **The checker is a gate, not a banner — driven with a control and three arms on copies under `/dev/shm`.** Control (real output) → `CHECK OK` rc 0. Arm 1, a prose "tension" injected into an `<h2>` → `REFUSED: the rename is incomplete` rc 2. Arm 2, one character changed inside the quoted `s237-D1` → `verbatim 16/17 … REFUSED … ['s237-D1']` rc 2. Arm 3, `s237-D9` paraphrased from memory ("a second typed home is refused") → `REFUSED … ['s237-D9']` rc 2. *Probe:* `PLAN_V2_OUT=/dev/shm/a/m1.html python3 _gen_plan_v2.py --check` etc.; the env override exists only for this.

4. **The rename is complete in prose, by classification of every hit.** 34 hits for `tension` on the generated page (style block excluded): **25 path**, **3 quoted store text** (`s238-D1` "CALLED TENSIONS", `s238-D4` twice), **1 quoted report text** (lane T's own question wording, inside `<q class="rq">`), **1 footnote**, **4 store-title** (W-347, W-358, W-359, W-360 as `_state.json` carries them), **0 prose**. *Probe:* the `--check` output line.

5. **Open items: 28, each probed against the store before being listed.** `_probe_open_items.py` greps `ruled + says` of all 328 rulings per item and writes `open-items-probe.json`. Every hit is the ruling that *names* the item (`s238-D3` ask-whens, `s238-D5` conservative side, `s237-D4` the 13, `s237-D5` touchpoints, `s237-D6` bedrock + intent, `s238-D2` retrieval set, `s237-D8/D9/D10` the unbuilt P6 borrows) or a word collision in another sense (`s223-D5` "ask", `s146-D1`/`s147-D1` the `gen_snippet_tokens` writer path, `s133-D2`/`s186-D2` a PAT expiry, `s178-D1` the SESSION_ACK hatch, `s220-D3`/`s222-D2`/`s227-D7` other things called L3/L4/L5). Twelve items return **zero** hits (L1 Q1/Q3, G Q1–Q3, P6b/c/d, the backlog eight, the primaries read, lane V). *Probe:* `python3 notes/_subreports/assets/2026-09-02-238-A-plan-v2/_probe_open_items.py`.

6. **v1 carries an arithmetic slip, recorded not corrected.** v1 §7 prose says "Thirteen map onto a phase … ten are not placed"; its own table, parsed at v2, places **15** and leaves **8** backlog (R1-8, R1-9, R1-10, R1-12, R2-5, R2-8, R2-9, R2-10). v2 says so in §7 and leaves v1 as dated history. *Probe:* regex over v1's §7 rows → `ideas rows 23 placed 15 backlog 8`.

7. **Two real layout defects at 1440, both fixed at the cause.** (a) The SVG lane band's receipt strings (`receipt: ▢ 238-B-L2-behaviour-address`, 37 chars at 9.5 px) overran their 150 px boxes and overlapped the neighbour; fixed by widening the boxes to 154 px and breaking each receipt onto two lines — not by shrinking the type. (b) Three tables (§4 lane map, §9 gaps, §11 receipts) carried long `white-space:nowrap` paths and anchors (`#does_it_function_as_a_rails_file_for_a_generating_agent`, 55 chars) that forced the table past the 1180 px wrap and clipped the last column at 1440; fixed by a wrapping mono class `.kw{overflow-wrap:anywhere}` on those cells. Also carried from lane T finding 12: `.grid-2 > *, .grid-half > * {min-width:0}` so a grid child can shrink to its scroll wrapper. *Probe:* `crop-dark-1440-s4.png` before/after (the after is filed); `render-receipt.json` `overflow: NONE` ×4.

8. **The font question lane T left (its UNPROVEN 6) is settled at this seat: the page renders in Liberation Sans.** Canvas widths of one 41-character string at 16 px: `"Helvetica Neue"` alone **280.5** = `serif` **280.5** (the family is absent, so a lone name falls to the default serif — lane T's "distinct face" was this fallback, not a Helvetica); the page's declared stack **308.0** = `sans-serif` **308.0** = **Liberation Sans 308.0**, the only family of 95 installed that measures 308.0; DejaVu Sans measures 348.2 although `fc-match sans-serif` names it — Chromium's generic sans-serif here is Liberation Sans, not fontconfig's answer. *Probe:* `render-receipt.json#renders[*].probe.canvas` and the per-family sweep printed in the lane transcript (`matches: ['Liberation Sans']`).

9. **The `Defaults used:` "conservative side" has no field, and the derive script says the proposed default IS the conservative side.** `grep -c -i conservative open-tensions.json` → **0**; `_derive_open_table.py` line 23: *"factory_default_proposed - this lane's words for R1's how_it_resolves, conservative side."* Read literally, every proposed default was written on the conservative side and `s238-D5`'s declaration set is empty — which is exactly why lane P's brief tells it to print the rule it uses and mark UNPROVEN. Recorded in v2 §10 row 3, not resolved. *Probe:* the two commands.

## RULING-SHAPED QUESTIONS

⛔ **MANDATORY SECTION.** **None.** Nothing in this lane needed a decision that was not already ruled: the brief's DO NOT RULE ("nothing new is decided in v2") was the whole job. The 28 open items are Dave's or a named owner's and are listed in v2 §10 with a close condition each; this report adds no recommendation on any of them.

## PITFALLS (consequences replayed — Dave #165)

| # | what could go wrong with what this lane built | fence / what to do | owner |
|---|---|---|---|
| 1 | **The page is read as a status report.** Every "RUNNING" mark and every empty receipt slot is a claim about a *launch*, generated before any lane P/B/C/M report existed. | The slots are drawn empty with ▢; §9 A-2 declares it. The conductor fills them from the filed reports — or, better, regenerates v3 from the template once the reports exist. | conductor |
| 2 | **The next version edits the HTML instead of the template.** The generator's guarantees (verbatim rulings, counted open rows) evaporate the moment someone hand-edits the output. | `_gen_plan_v2.py` and its template are filed beside the page; v3 = copy the template to a new dated name, edit that, generate, `--check`. ADR-0017: v2 stays as generated. | whoever builds v3 |
| 3 | **A future ruling closes an item and §10 keeps saying "open".** The page has no clock on its open list beyond the generation stamp. | `open-items-probe.json` re-runs in seconds and prints new hits; the page's §10 caption says a row is falsifiable by one grep. | the next opener |
| 4 | **The asset folder is ~11 MB** — four full-page PNGs (1.3–2.0 MB each, 32-colour) plus 12 crops. The sandbox cannot delete files on the mount (`mv … Operation not permitted`), so the four full-page files and four contact strips could not be withdrawn once written. | The eye-check evidence is the 12 crops + `render-receipt.json`; the conductor may leave the four `plan-v2-*.png` and four `*-thumb.png` uncommitted at the reconcile with no loss of receipt (their sizes and sha256s are in the receipt JSON). | conductor at reconcile |
| 5 | **The `SAYS-FRAG` quotes from `s237-D8` read as prose.** Two fragments of Dave's terminal paste are quoted (§3 P6a, §9 R2-3); if `says` is ever re-worded the generator refuses, which is the intended failure. | Leave them; the refusal is the fence. | — |
| 6 | **Lane V is drawn on the map.** If no verifier lane ran, the map shows a box for a thing that did not happen. | The box is dashed and its receipt line says UNPROVEN; §9 A-1 and §10 row 28 carry it. Remove it in v3 if the conductor says it never launched. | conductor |
| 7 | **`_REVIEW-SIGNOFF.md` has no row for v2.** | By the brief: that is the wrap's step, not this lane's. Nothing was written there. | wrap |

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN: that lane V launched.** No `notes/_briefs/2026-09-02-238-V-*.md` existed at generation time (`ls notes/_briefs | grep 238` → A, B, C, COMMON, M, P) and a repo-wide grep for `238-V` / `lane V` returned nothing outside this lane's own files. Price to prove: the conductor's word.
- **UNPROVEN: what lanes P, B, C and M delivered.** Every lane-map cell was read from the *briefs*; no lane report existed when v2 was generated. Price: read the four reports when filed (~4–6K each); regenerate v3 with the receipt slots filled.
- **UNPROVEN: that the 28 open items are exhaustive.** They are every open thing found in v1, the G and T reports, the L1 report, the decision-history dossier and the 17 rulings' own carried clauses; a keyword probe was run per item but an item nobody wrote down cannot be probed for. Price: Dave's eye on §10, ~2K.
- **UNPROVEN: the exact byte size of this lane's own token spend** — no `message.usage` at a sub's seat; the shape is in the header.
- **Not a gap, stated plainly:** the four full-page PNGs are filed as 32-colour palette conversions of the RGBA renders (byte sizes and sha256s of the filed files in `render-receipt.json#full_page_renders`); the overflow and font probes were measured on the live page in the browser, never on a PNG, so the conversion does not touch any measurement.

## Evidence

`notes/_subreports/assets/2026-09-02-238-A-plan-v2/` —

- **`_gen_plan_v2.py`** + **`plan-v2.template.html`** — the generator and its template; `--check` = the verbatim-ruling and rename gate (findings 1, 3, 4). Proves the page's rulings came from the store.
- **`_probe_open_items.py`** + **`open-items-probe.json`** — the 28-row store probe with every hit listed (finding 5). Proves each "open" claim was tested, not assumed.
- **`_render_plan_v2.py`** + **`render-receipt.json`** — four renders with `overflow: NONE`, body background and accent per theme (`rgb(255,255,255)`/`rgb(218,26,0)` light, `rgb(18,18,18)`/`rgb(246,96,76)` dark — the two-red law, ink only), the canvas font widths (finding 8), and the full-page file table.
- **`crop-light-1440-s0/s8/s10.png`, `crop-dark-1440-s0/s4/s9/s11.png`, `crop-light-390-s0/s2/s8.png`, `crop-dark-390-s0/s4/s10.png`** — the legible section crops; eight of the twelve were read back by eye and are the basis for finding 7.
- **`plan-v2-{light,dark}-{1440,390}.png`** (32-colour) and **`*-thumb.png`** contact strips — whole-page shape; see PITFALLS 4.

REPLAY-THESE: `_PLAN-designers-brain-2026-09-02-v2.html` §0 answer + §10 open list (~4K — Dave's decision surface; the 28 rows are the only place the open set is listed with owners) · finding 2 above (~0.3K — the T-question→ruling mapping differs from the brief's shorthand; the store's wording governs) · `notes/_subreports/assets/2026-09-02-238-A-plan-v2/open-items-probe.json` (~1.5K — re-run `_probe_open_items.py` before any later session quotes an item as still open) · PITFALLS row 4 (~0.2K — decide at the reconcile whether the eight large PNGs are committed)
