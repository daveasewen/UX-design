# 2026-09-07 · #253 · VERIFIER B — adversary over lane B's 13 chart metas

- **Role:** Verifier B (adversary, not builder), Apollo session #253, model opus.
- **Under test:** `notes/_subreports/2026-09-07-253-BUILD-B.md`, wave commit `866312b`, `_HANDOFF-252-lanes-cold.md` §B.
- **Method:** every claim re-derived by script from the metas / roles.json / the page's own HTML. Nothing taken from lane B's report. No meta, page, roles.json, chart-intents.json, schema or validator edited; nothing committed; `_build_all.py` not run.
- **Scratch (outside the repo):** `/tmp/vb/c1b.py` (corpus + fields + priority uniqueness) · `/tmp/vb/page.py` (page→meta six-value diff) · `/tmp/vb/voc.py` (vocabulary both-sides diff) · `/tmp/vb/r2.py` (R2) · `/tmp/vb/r3.py` (R3).

## VERDICT

**GREEN.** All eight checks pass on their load-bearing claims. Two mismatches are logged; neither falsifies a check — **M1** is a rendering defect confined to §14, **M2** is a membership row inside the explicitly-unruled `when` vocabulary table. First mismatch token: `&amp;middot;` × 35, all in section 14.

`COUNTS: claims 221 · green 219 · red 0 · mismatches 2`

---

## Check 1 — the corpus is 13, and no resolver failure names one of them

Token: `chart metas with intent 14 · lane B set 13 · resolver failures naming the 13 = 0`

- 137 metas on disk; **15** carry `intent`. **14** of those have `chart` in the filename (case-insensitively — a lowercase `*chart*` glob finds only 7, which is why this was scripted with `.lower()`). Minus `chart-bar` ⇒ **13**. Exactly lane B's list.
- The 15th `intent` carrier is **`runway-bar`** (`intent: composition`), not chart-named, `provides`/`priority` absent. Outside both lanes' scope by the brief's wording; flagged below as a ruling-shaped question, not a failure.
- `python3 knowledge/_validate_roles_resolve.py` → `RESULT: FAIL (10)`. All ten are `[answers-resolves]` on **lane A's** slugs: app-shell-top-nav · breadcrumbs · button · headers · kpi-tile · layout-utilities · navigations · stat-card · status-indicator · summary. **Zero** failures name any of the 13. The cross-check block prints `roles.json membership and the metas' provides AGREE for every meta that carries the field (24)`.

## Check 2 — the six-field values on the metas

Token: `check2 FAILS: []` (13/13)

| meta | answers==intent | provides | priority |
|---|---|---|---|
| Chart-boxplot | ✓ | chart-panel | 60 |
| Chart-bullet | ✓ | **headline-metric** | 40 |
| Chart-butterfly-h | ✓ | chart-panel | 40 |
| Chart-butterfly-v | ✓ | chart-panel | 36 |
| Chart-candlestick | ✓ | chart-panel | 44 |
| Chart-histogram | ✓ | chart-panel | 76 |
| Chart-scatter | ✓ | chart-panel | 72 |
| chart-combo | ✓ | chart-panel | 56 |
| chart-donut | ✓ | chart-panel | 80 |
| chart-line | ✓ | chart-panel | 88 |
| chart-pie | ✓ | chart-panel | 48 |
| chart-sparkline | ✓ | chart-panel | **30** |
| chart-stacked-area | ✓ (**list** `["change-over-time","composition"]`, copied not collapsed) | chart-panel | 64 |

`chart-sparkline.priority == 30` → True. `Chart-bullet` is the only `headline-metric`. String equality used for 12, list equality for chart-stacked-area.

## Check 3 — targeted gates

| gate | invocation | result |
|---|---|---|
| `_validate_kg.py` | default | `OK — every ref parses+resolves … gen_kg_edges.py is idempotent-clean` (138 metas) |
| `_build_integrity.py` | default | `integrity: PASS — 0 errors, 22 warnings` · `schema valid: 136/136` |
| `gen_component_partials.py` | `--help` read first; ran `--check` (the read-only build gate; the bare form WRITES) | `--check OK — all AUTO-PARTIAL blocks in sync, contracts hold`, rc 0 |
| `_validate_roles_resolve.py` | default | `FAIL (10)` — all ten lane A's, see check 1 |

`git status --porcelain` after the gate run: only `notes/_REHEARSAL-LOG.jsonl` and `notes/_dream/_GRADE-DECISIONS.jsonl`, both pre-existing and not this wave's. **No gate mutated a tracked file.**

## Check 4 — the page parsed by script

Token: `per-meta sections: 13 · MISMATCHES(six values): 0`

- **13** per-meta sections (`<p class="slug">`), slugs exactly the 13.
- **Six values × 13 = 78 field renderings compared to the meta files, exact equality, zero diffs** — including the full `when` string byte-for-byte (after one HTML-unescape), `span` rendered as `cols <min>–<max>` against `span.cols`, and `answers` on chart-stacked-area rendered as the joined array.
- **Vocabulary §14, both sides computed:**
  - `shape` — page 11 values, metas 11 values, **set diff empty, per-value membership identical**.
  - `span` — page 6 bands, metas 6 bands, **set diff empty, membership identical** (6–12 ×6 · 8–12 · 6–8 ×2 · 6–6 ×2 · 4–6 · 2–4).
  - `when` fields — page lists 12; every one of the 11 tokens that appear with an operator (`=`/`≥`/`≤`) in a left half is on the page, none missing. **One membership diff (M2), below.**
- **Priority ladder** — 17 rows parsed; every "This lane" number equals the meta's `priority` and every "roles.json" number equals roles.json's illustrative value. The only two `—` cells are stat-card and kpi-tile, which is a lane-attribution dash, not a value claim (both now carry 60 / 50 on disk from lane A, matching roles.json).
- **chart-bar** — the ladder row is `<td class="n prop">84</td>` with the note "authored by lane A — this lane proposed **92**, the top of the role; see call 2". `chart-bar.meta.json` on disk carries **priority 84**, `provides: chart-panel`, `shape: categories-by-series` — lane A's values. `git show 866312b -- knowledge/components/chart-bar.meta.json` shows the 84 insert, and the lane-B diff is **186 insertions / 0 deletions over exactly the 13 files** (11+11+17+17+11+21+11+11+11+11+17+17+20 = 186), chart-bar not among them. **Lane B wrote nothing into chart-bar.**
- **`not-with` prose** — all 7 authored edges appear in their section with the slug AND the `when` text verbatim; the 7 metas with none say "none authored". 0 diffs.

## Check 5 — R2 clause

Token: `R2 YES: 13 · NO: 0`

Every one of the 13 `when` strings names at least one roles.json co-provider of its own role AND carries a `beats` / `yields` verb. Named siblings per meta: boxplot→Chart-histogram · bullet→stat-card, kpi-tile · butterfly-h→chart-bar, Chart-butterfly-v · butterfly-v→chart-bar, Chart-butterfly-h · candlestick→chart-line · histogram→Chart-boxplot · scatter→chart-combo · combo→chart-bar, chart-line · donut→chart-stacked-area, chart-pie · line→chart-combo, Chart-candlestick, chart-sparkline · pie→chart-donut, chart-stacked-area · sparkline→chart-line · stacked-area→chart-line, chart-donut, chart-pie. **No no's.**

## Check 6 — R3 clause

Token: `R3 sections with a diff: 0`

For all 13: the "Siblings consulted (roles.json, N co-providers of `<role>`; the M this predicate names)" header's N equals the actual roles.json co-provider count (12 for chart-panel, 3 for headline-metric), the role name equals the meta's `provides`, M equals the count computed from the `when` string, and the cited sibling table lists exactly those M. The "`edges.mustNotNeighbour` on this meta (N)" count and the number of `<li>` entries both equal the meta's actual `edges.mustNotNeighbour` length in every section. **Cited = actual, everywhere.**

## Check 7 — priority uniqueness across ALL metas carrying `provides`

Token: `every role unique=True`

| role | n | priorities |
|---|---|---|
| chart-panel | 13 | 88 84 80 76 72 64 60 56 48 44 40 36 30 — unique |
| headline-metric | 3 | 60 50 40 — unique |
| wayfinding | 2 | 60 40 — unique |
| status-surface | 2 | 60 40 — unique |
| page-frame / action / page-title / arrangement | 1 each | unique trivially |

24 metas carry `provides` (13 lane B + 11 lane A). **No collision anywhere, including across the two lanes.**

## Check 8 — currency and counts

Token: `£ 0 · $ figures 0 · $composes 1`

- No `£` and no `$` figure appears on the page: **the page carries no currency at all**. The single `$` is `$composes`, a field name in prose. The brief's `£`-not-`$` clause is satisfied vacuously — nothing to convert, nothing to mis-denominate.
- Hero counts, all recomputed: 13 in scope ✓ · 13 authored ✓ · 11 shape values ✓ · 12 when fields ✓ (see M2) · 7 not-with edges ✓ · 6 span bands ✓. Body counts "14 of them" (chart metas with intent) ✓ · "the 6 whose own prose demanded one" ✓ · "12 co-providers" ✓ · "13 slots" ✓.

---

## MISMATCHES

**M1 — 35 double-escaped `&amp;middot;`, all in section 14 (Vocabulary).** Every separator in the shape / when / span membership tables renders as the literal text `&middot;` instead of `·`. Located by walking back to the nearest `<p class="idx">`: all 35 are under idx **14**; the 13 per-meta sections and the ladder are clean. Cause is a generator that escaped an already-escaped entity in one code path. Cosmetic, but it is the batch's own vocabulary table — the section Dave is meant to read to rule a closed list. Regenerating from `/tmp/laneB/gen_page.py` (lane B's own scratch, outside the repo) is the fix; the page is generated, not hand-edited.

**M2 — the `when` field table's `categories` row claims 3 metas; a token-level recomputation finds 2.** Chart-butterfly-v's predicate left half reads `… AND the category axis is ordered left-to-right (time, rank) AND span.cols ≥ 8` — prose, not the token `categories`. Chart-bullet's `target` is the same class (`… one measure against a target …`). So the page's stated method ("collected from the LEFT half of each predicate") is prose-inclusive, and the field vocabulary is **not mechanically derivable** from the strings: an operator-token sweep yields 11 fields, the page's reading yields 12. The set is a superset, not a contradiction — no field is missing and none is invented — but the count "12" and the row memberships are a human reading, and lane B's report presents them as script-computed.

## RULING-SHAPED QUESTIONS

1. **Is `runway-bar` in or out?** It carries `intent: composition` and roles.json lists it under `headline-metric` at 30, but it is not chart-named, so neither lane authored it. It is the only `intent` carrier of the 15 left bare. Does it get the six fields, and if so in which lane's pass?
2. **Which shape grammar is canon?** Lane A wrote `categories-by-series` on chart-bar; lane B wrote `<x-dimension> × <mark>` throughout (`time-series × 1–5-series`). The resolver's derived `instead-of` grade compares `shape` strings for equality, so the grammar is load-bearing, not stylistic — two lanes with two grammars means chart-bar can never grade `equivalent` to a lane-B twin.
3. **Does the `when` field list get a machine-readable form?** M2 shows the current grammar mixes `field=value` gates with prose conditions, so no parser can reproduce the page's own table. If the list is to be closed (lane B's question 3), the left half likely has to be tokens only.
4. **chart-bar 84 or 92?** Confirmed unresolved: 84 on disk (lane A), 92 proposed on lane B's page and in its call 2. The gate is silent either way — both unique.
5. **Does `chart-sparkline` stay in `chart-panel`?** Independently corroborated as odd: the resolver grades it `[distant] shape=differs span=disjoint` against chart-line — the only chart-panel provider whose span does not overlap the role's top provider. Its own `when` argues it out of the role its `provides` puts it in.

## UNPROVEN

- **Nothing here proves a value is RIGHT.** Every check is an equality or a resolution. `shape`, `span` and `priority` correctness against each meta's own `purpose` is Dave's eye; this verifier only re-derived what lane B claimed.
- **The page was not rendered in a browser.** M1 was found by reading the source, not by looking at it; there may be further render-level defects (layout, contrast, print) that source parsing cannot see. Local-file preview is unavailable in this session.
- **`_build_all.py` was not run**, per brief, so the full runner's verdict over this wave is untested here. The four targeted gates are green.
- **`_validate_roles_resolve.py --selftest` / `--mutate` were not run** — not in the brief, and the validator is lane C's.
- **The span bands were not driven.** No renderer was exercised at 2, 4, 6, 8 or 12 columns; the 100px-per-column conversion remains lane B's arithmetic.
- **Nothing was committed, nothing pushed, no meta or page edited by this verifier.**
