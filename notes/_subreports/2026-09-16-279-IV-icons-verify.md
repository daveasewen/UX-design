# LANE IV — VERIFY — the icons land at f641242, re-derived from the live tree

#279 · 2026-09-16 · verify seat (Fable 5.1) · read-only on the tree · against `f641242`, not against lane IL's REPORT.

**VERDICT: GREEN WITH FIXES.** Every count the report claims reproduces from `knowledge/_icon_nodes.json` + `_logo_nodes.json` with my own code; selftest 18/18 and 26/26 mutants re-run green; A3 is anchored on `s230-D2`'s own clause and bite 18 fails for real under the old substring mechanism; the two `edges` descriptions in `meta.schema.diff` are count-free; `_validate_kg.py` grew three kinds by addition only; the door still refuses all 600 live ids outside `RATIFIES`. The fixes are about what did NOT land: 28 of the 32 declared nulls — including D7's ten `governedBy` nulls that the ruling says "enter VISIBLY" and D6's fifteen — exist in no file under `knowledge/`; their only committed copy is a stale proposal-era `dry-run.json`.

---

## Checks, each with the command and the number I got

1. **Nodes** — own script over the two landed files: `688` = icon `666` / iconGroup `10` / logo `12`; 688 unique ids; every node `fam: "assets"`. ✓
2. **Edges** — `1,299` = inGroup `666` / activeVariantOf `234` / usesIcon `371` / usesLogo `18` / defaultFor `2` / ruledBy `8`; every edge `fam: "assets"`. usesIcon spans 105 components / 81 icons; usesLogo 9 components / 4 lockups; ruledBy ×8 all → `ruling:s264-D3`; defaultFor = the two `masterbrand-{light,dark}-colour` stems, `t: null`, `ruling: s230-D2`. ✓
3. **themedBy** `0` · **defaultActive** `0`. ✓
4. **activeVariantOf sourced at a base** — 432 base icons, 234 actives; edges with `s` in the base set: `0`. 232 resolve, 2 orphans (`circle-active`, `menu-more-verticle-active`) carry `t: null` + `$note`. Of the 31 actives on the 15 multi-active bases: **31 carry an activeVariantOf → base edge** (this is the D6 question, §D6 below). ✓ as landed
5. **Null-target edges** `4` (2 defaultFor + 2 activeVariantOf); `0` without a `$note`. ✓
6. **Declared nulls 32** — `gen_kg_icons.py --dry-run /tmp/iv279/dry` → `unresolved` = 32: defaultActive 15 · governedBy 10 · activeVariantOf 2 · defaultFor 2 · icon (menu-search) 1 · usesLogo (app-shell-nav-rail) 1 · ruledBy (s212-D9) 1. ✓ — but see FIX 1: only the 4 null-target edges of these 32 are in any landed file.
7. **Dry-run vs landed** — `_icon_nodes.json` / `_logo_nodes.json` from the scratch dry-run differ from `knowledge/` in exactly two keys each: `ratified` (absent → `s277-D4`) and `$description` (PROPOSED… → RATIFIED…); `nodes`, `edges`, `edge_types`, `family`, `generated_by` identical. `PROPOSED` / `NOT RATIFIED` occur 0 times in the landed files. `git status` unchanged by the dry-run. ✓
8. **`--selftest`** — `python3 notes/_lanes/277/icons-propose/gen_kg_icons.py --selftest` → 18 `ok`, `SELFTEST PASS`, rc 0. ✓
9. **`_mutate.py`** — `python3 notes/_lanes/277/icons-propose/_mutate.py /tmp/iv279/mut` → `BASELINE PASS`, 26 mutants, `ALL MUTANTS CAUGHT`, 0 survivors, rc 0; M26 → RED 10,18; M16 → RED 10,18; M18 → RED 12,17; M19 → RED 12; M20 → RED 17. `git status` unchanged. ✓
10. **A3 anchor** — `RESIDUE_RX` (gen_kg_icons.py:198) = `([A-Za-z0-9][A-Za-z0-9._-]*)\s+deliberately NOT rebound`; run over all 604 live rulings it matches exactly one, `s230-D2`, yielding `['App-shell-nav-rail']`. `build()` iterates `residue_clauses(dr)` (line 545), case-folds into the metas, and never loops "slug in ruling text". `56px` appears in the generator once — in bite 10's negative assertion. ✓
11. **A3 bite would fail under the old mechanism** — I did not trust M26; I hand-applied the old test to a scratch copy (`_low = (ruled+says).lower(); for s in sorted(metas) if s.lower() in _low`) and ran its selftest: `SELFTEST FAIL — bites [10, 18]`, 0 tracebacks (a logic fail, not a crash). Also confirmed `text` is in scope at M26's patch site (line ~523), so M26 itself fails by logic, not NameError. ✓
12. **A4** — `grep -nE "758|Measured live|102 inline|835|\b371\b|12%"` over the two `edges` descriptions: 0 hits; both now say "NO COUNT IS WRITTEN HERE". Integers surviving in the diff are in the run-log / rationale prose (lines 163–215: 613, 232, 389…), not in landing text — except one: the `via` property description (diff L128) still reads "fires on **137 of 137** metas", and the live tree has **139** metas. See FIX 2. ✓ with residue
13. **`_validate_kg.py` +3 by addition** — `git diff f641242~1 f641242 -- knowledge/_validate_kg.py` = +41/−2; the two `−` lines are `REF_RE` and the `NODE_KINDS` tail, re-emitted with all ten original kinds intact plus `icon|iconGroup|logo`; additions: `ICON_MANIFEST`, `LOGO_DIR`, `icon_ids()`, `logo_ids()`, three `resolvers` entries. No existing kind, resolver or store removed. `python3 knowledge/_validate_kg.py` → rc 0 on the live tree; `git status` unchanged. ✓
14. **`git diff --numstat f641242~1 f641242`** — 11 rows. The report's §10 names 10 paths; all 10 are present with the stated shapes (`_icon_nodes.json` +18169, `_logo_nodes.json` +282, `_validate_kg.py` +41/−2, gen_kg_icons.py +143/−64, `_mutate.py` +12/−4, `_simulate_validator.py` +6, `meta.schema.diff` +11/−2, BRIEF +28, REPORT +259, subreport +259). The 11th row is `notes/_REHEARSAL-LOG.jsonl` +4 — rows the commit gate itself writes (the prior commit `c9af467` carries 2 the same way; s137-D1 names this file as the instruments' own). Not a stray edit; the report's table is one script-written row short. ✓ (note)
15. **The door** — imported `gen_kg_icons` and called `land(ratified=id)` for all 604 live ruling ids + `None`, `nope`, `s999-D99`, skipping the 4 in `RATIFIES`: **0 landed**; 565 refused at the allowlist ("does not ratify THIS proposal"), 35 live ids + `nope` at the shape gate, 1 no-id, 1 unrecorded. 600 live ids outside `RATIFIES`, 600 refusals. md5 of both landed files unchanged after; `git status` unchanged. ✓
16. **Report ↔ subreport** — `cmp` silent: `notes/_subreports/2026-09-16-279-IL-icons-land.md` is byte-identical to the lane REPORT. ✓
17. **Tree hygiene** — I wrote nothing under the tree except this file and its subreport copy. A `.git/index.lock` (0 B, mtime 14:36:24, not mine; `notes/_lanes/279/reader/` is untracked so another lane is live) is present — **left in place, not moved**; the conductor should check it before committing.

---

## FIXES (for the conductor; none of them re-opens the land)

**FIX 1 — 28 of the 32 declared nulls are not in the landed tree.** D4 says "Declared nulls carried, never dropped"; D7 says the ten unbound lockups "enter VISIBLY with a declared-null `governedBy` each"; D6 says "each of the 31 is a declared null". In `knowledge/` the only nulls are the 4 `t: null` edges. The 15 `defaultActive`, 10 `governedBy`, `icon:menu-search`, the rail `usesLogo` and the `s212-D9` `ruledBy` nulls live only in `dry-run.json`, which `--land` does not write, and the committed copy at `notes/_lanes/277/icons-propose/dry-run.json` is stale (`ratifies: []`, the pre-A3 rail `why` with "56px"). The ten logo nodes carry no field or note saying they are unbound. Fix: have `--land` write the `unresolved` list into the node files (an `unresolved` key beside `edges`, or `$note`-bearing `governedBy`/`defaultActive` edges with `t: null`, which is the convention the 4 already use) — then the chip lane can render D7's "visibly" from the landed files — and regenerate the committed `dry-run.json` so the proposal record matches the generator that landed.

**FIX 2 — A4 residue.** `meta.schema.diff` L128 (`via` description): "fires on 137 of 137 metas" is a live count (139 today). Drop the figures or pin them "at 5520d43". Lower priority: the landed `_validate_kg.py` docstrings say "the 666 slugs under 10 group keys" and "the twelve files" — docstrings, not schema, but they go stale the day `menu-search` is reconciled (667).

**Note 3** — REPORT §10's staged-set list omits `notes/_REHEARSAL-LOG.jsonl` (+4, script-written). One line to add.

---

## D6 — which reading the text supports (a question for Dave, not a ruling)

D6's recorded words — "the 15 bases carry NO `activeVariantOf` edge and each of the 31 is a declared null" — name `activeVariantOf` (not `defaultActive`) and count the nulls per active (31, not 15), and Dave's own note says "they are not the active variants", so the text as recorded supports reading **(b)**: strip the 31 edges (1,299 → 1,268) and declare 31 nulls (32 → 63). But the page Dave ticked told him in RI-3's lede that "`activeVariantOf` draws all 232 twins whichever way this goes", and option (c) said only "`defaultActive` waits for that list", so what he ratified on the page is reading **(a)** — as landed — and the `ruled` line was written in (b)'s words for (a)'s option. The question for Dave: on those 15 bases, are the 31 `-active` glyphs still *active variants of* their base with only the default open (keep the 31 edges, as landed), or is even that relation in doubt until his manual review (cut them: 1,299 → 1,268, nulls 32 → 63)?
