# `#220`-bake-seam — Spider/Gumdrop v1.0.1: three commits landed, the artefact BUILT and fully driven, `--release` BLOCKED by a live parallel lane

session: `#220` · 2026-08-27
window: conductor-seat commit-and-bake seam
sub index: `bake-seam`
brief: delegated in-chat (conductor seat, git authority path-scoped to the release lane)
consumes: `notes/_subreports/2026-08-27-220-bake-v101.md` ·
`notes/_subreports/2026-08-27-220-bake-v101-addendum.md` (its REPLAY-THESE **supersedes** the
first report's)
tokens: `UNMEASURED` — a sub cannot read its own `message.usage`.

## VERDICT

**Steps ① ② ③ ④ landed. Step ⑥'s verification is DONE and every number the addendum projected
came back EXACT. Step ⑥'s `--release` is REFUSED — by design, on a cause outside this seam — and
steps ⑦ ⑧ are OWED behind it.**

Three commits landed. The v1.0.1 pack **exists, is measured, and has been driven end to end**:
`--check` green against the manifest, the pack-import gate reports **MISSING-LOCAL 0 and
ERROR-AT-IMPORT 0** with its known-answer control still biting on v1.0.0, and **retrieval works
from a clean extraction in BOTH machinery dirs** — the thing v1.0.0 promised in two shipped
documents and could not do.

⛔ **What is NOT done, and why it must not be improvised.** `build-designer-pack.sh --release`
refuses on a dirty working tree. A **parallel bento/showroom lane is live in the shared tree
right now** and has held 8–10 paths dirty for the whole seam (it grew a new asset directory
during the final poll). The refusal is correct and I did not route around it. The zip in
`/var/tmp/bake220/` was produced by `--dry-run`, which the script explicitly permits on a dirty
tree — and `--dry-run` output is *by the script's own words* **"nothing here is a release"**.
Copying it into `dist/` would be me deciding the release ran when the release gate refused, which
is precisely the class the whole release shape exists to prevent.

**Nothing was pushed, and pushing now would be WRONG independently of the dirty tree** — see § ⑤.

COUNTS: commits 3 · store rows minted 14 · version homes moved 4 (reports named 2) · gate arms
driven 17 · designed-reds cleared 3 · designed-reds remaining 1 · projected figures verified 8/8 ·
ruling-shaped questions 5 · findings 5 · OWED items 4

---

## ⓪ THE COMMITS

| # | sha | subject (T3 prefix trimmed) | files |
|---|---|---|---|
| A | **`3f7a63a`** | Spider/Gumdrop v1.0.1 **LANDING COMMIT** — version bumped in every home the bake reads, the Gumdrop index bootstrap and the pack-import gate land, 14 store rows minted | 43 |
| B | **`d64b83e`** | Spider v1.0.1 **stage 2** — manifest + gate probe + go/no-go page regenerated AT the landing commit; every projected figure MATCHED | 4 |
| C | **`af5b5be`** | Spider v1.0.1 **pre-bake** — go/no-go page carries the MEASURED zip fingerprint; the v1.0.1 artefact is built and every downstream gate driven on it | 3 |

`3f7a63a` is **the landing commit**: the manifest, the probe and the zip are all generated AT it.
The manifest lands one commit later by construction (it describes the commit it ships from, so it
cannot be inside it) — the same three-stage shape `#219` used (`416e1a6` → `aa74faa` → `ef44b1a`).

⚠ **Full landing sha, for the replay:** `3f7a63a39e861a33bfa603b86c80fba24ec20c75`.

---

## ① THE VERSION BUMP WAS **FOUR** HOMES, NOT THE TWO BOTH REPORTS NAMED

Both REPLAY-THESE lists name `_gen_pack_manifest.py:107/109` and `build-designer-pack.sh:38`.
Following only those would have shipped a pack that **disagrees with itself** and a gate that
**refuses its own release**.

| home | what it sets | named by a report? |
|---|---|---|
| `knowledge/_release/_gen_pack_manifest.py:107` `VERSION` | the pack version | ✅ yes |
| `knowledge/_release/_gen_pack_manifest.py:109` `MEMENTO_CUT_VERSION` | the carried cut | ✅ yes |
| `apollo-spider/build-designer-pack.sh:38` `VERSION` | the zip name | ✅ yes |
| **`knowledge/_release/_gate_frozen_release.py`** `SURFACES` apollo-spider row | **the frozen ledger row's version** | ⛔ **NO** |

### ⛔ THE THIRD HOME, AND WHY IT WOULD HAVE STOPPED THE BAKE

`seed()` reads a ledger row's `version` **from the `SURFACES` declaration literal**, not from the
zip's filename. The ledger's own `_README` states the law:

> Editing a frozen surface means cutting a NEW release: change the files, re-seed, and **BUMP
> that row's `version` in the same commit**. Re-seeding without a bump is caught by the gate's
> laundering arm.

Baking v1.0.1 into `dist/` moves `content_sha256`. Re-seeding with `SURFACES` still saying
`v1.0.0` would have moved content while version stood still — **the exact shape
`arm_laundering()` calls a laundered edit**. The gate would have refused the release it was
recording. Moved to `v1.0.1`, with the reason written at the literal.

⚠ **`--check` still PASSES at `af5b5be`** (`3 arm(s) asked, no frozen surface moved`), because no
arm compares the `SURFACES` literal to the ledger row. The bump is **inert until `--seed` runs**.
That is correct pre-positioning — and it is also finding 4 below.

### THREE MORE TYPED COPIES INSIDE `build-designer-pack.sh`, now DERIVED

The Gumdrop cut's version was typed **three times** in the shell — `PROVENANCE.json`, the README
provenance table, and the `memento-package/` bullet — while its one home is the generator's
`MEMENTO_CUT_VERSION`. At the bump all three would have shipped `v1.0.0` **inside a pack whose
manifest said v1.0.1**. They now read `carries.name` / `carries.version` out of the manifest the
generator wrote (ADR-0017, one home); a missing key dies loud under `set -e` rather than
defaulting to a version nobody chose.

**Read back off the extracted artefact, not off the script:**

```
PROVENANCE.json : pack Apollo-Spider-v1.0.1 | version v1.0.1 | carries Memento — Gumdrop v1.0.1
README.md       : | version | `v1.0.1` |   | carries | `Memento — Gumdrop v1.0.1` |
```

### AND DAVE'S GO/NO-GO PAGE WAS TELLING HIM THE WRONG FOLDER

`_gen_pack_manifest.py:2063` typed the pack root as `Apollo-Spider-v1.0.0/` in the page's "What
unzipping looks like" note — the one path a designer types first. Derived from `PACK_SLUG` +
`VERSION` now. The page reads `Apollo-Spider-v1.0.1` on disk, measured.

### ONE FIXTURE MOVED BY HAND, DELIBERATELY

`_gen_pack_manifest.py:2725`, the `naming/memento-cut-is-named` bite, pins the cut's version as a
literal. That is its **design**: it makes every bump of the carried cut something a person typed.
Moved `v1.0.0` → `v1.0.1` in the same edit as the constant, with that reason at the assertion.
**No gate was weakened and no assertion was loosened.**

---

## ② THE STORE — 14 ROWS, MINTED THROUGH THE STORE'S OWN WRITER

`knowledge/_state.py`'s `add()`, never a hand edit. `check()` green, **0 unresolvable homes, 0 new
unconditioned items**, store 270 → 284 items.

| id | home | owner |
|---|---|---|
| `W-198` | `knowledge/_gate_pack_imports.py` | dave |
| `W-199` | `apollo-spider/gumdrop/machinery/_build_memento_index.py` | dave |
| `W-200`…`W-205` | the six `notes/_briefs/2026-08-27-220-*.md` | claude |
| `W-206`…`W-211` | the six `notes/_subreports/2026-08-27-220-*.md` | dave |

**Nothing of Dave's was closed, reworded, or given a close condition it did not have.** The two
instrument rows close on **his** ruling (the pack-import gate's ship/promote pair; the addendum's
question 3 on striking generalisation-debt item 3). `_rulings.json` was **not opened for writing**.

⛔ **The doc-row gate has the last word, post-staging:** `population 108 · staged-in-THIS-commit
27 · unrowed 0 · PASS`. The `#207` blindspot half fired and found nothing missing.

### ⚠ THE `W-99z*` ID LANE IS EXHAUSTED

`ID_RE` is `W-[0-9]{1,3}[a-z]{0,2}`, and the overflow family already runs to **`W-99zz`** — the
last legal id in that shape. A future sub reaching for "the next `W-99z…`" has nowhere to go. The
main lane (`W-0`…`W-197`, now `W-211`) is fine and is what I used. Named, not fixed — widening an
id grammar is not a seam's call.

---

## ③ THE MANIFEST AT THE LANDING COMMIT — 8 OF 8 PROJECTIONS EXACT

The addendum's § ⓪ labelled four figures as PROJECTIONS. All four resolved, and every REPLAY-THESE
step-4 expectation hit on the nose.

| reading | addendum expected | **measured at `3f7a63a`** |
|---|---|---|
| ship paths | 1,641 *(projected)* | **1,641** ✅ |
| pack files | 1,643 *(projected)* | **1,643** ✅ |
| `gates` group | 55 — **must NOT be 56** | **55** ✅ |
| `gumdrop` group | 19 — **must NOT be 18** | **19** ✅ |
| `import_closure.files` | 2 | **2** ✅ |
| `import_closure.companions` | 1 | **1** ✅ |
| `unmodelled_path_inserts` | 0 | **0** ✅ |
| closure files | 2 (1 import + 1 companion) | **2** ✅ |

manifest `sha256 5686a992498843f7` · 39,767,327 bytes · `version v1.0.1` ·
`carries "Memento — Gumdrop" v1.0.1` · `status RATIFIED`.

⚠ **The manifest was generated TWICE** — once directly, once through the sanctioned
`build-designer-pack.sh --manifest` call form — and came out to **the same sha256 both times**.

The companion closure did the thing it was declared for:

```
closure: memento-package/claude-plugin/memento/machinery/_build_memento_index.py
      <- apollo-spider/gumdrop/machinery/_build_memento_index.py
      (for memento-package/claude-plugin/memento/machinery/_memento_search.py)
closure: memento-package/claude-plugin/memento/machinery/_could_not_ask.py
      <- apollo-spider/gumdrop/machinery/_could_not_ask.py
      (for memento-package/claude-plugin/memento/machinery/_gen_chain.py)
```

---

## ④ THE ARTEFACT — BUILT, MEASURED, AND DRIVEN

Produced by `--dry-run --out-dir /var/tmp/bake220`, which the script permits on a dirty tree
because *"it reads the COMMIT, not the tree, and writes nothing the repo keeps."* Build time
**6.9s**.

```
Apollo-Spider-v1.0.1.zip
  bytes    18,992,272
  entries  1,646
  sha256   c8934ebdb63306526223f8478561ccd3e18f502b4db01746c0f468ba6e6923d1
  staged   1,641 paths + 2 import-closure copies
```

Measured with `os.path.getsize` and `len(ZipFile.namelist())` over the actual file, and the
sha256 over its bytes — not read off the build log.

| against v1.0.0 (shipped) | v1.0.0 | **v1.0.1** |
|---|---|---|
| bytes | 13,339,337 | **18,992,272** |
| entries | 1,613 | **1,646** |
| packed `.py` that cannot import | **1** | **0** |
| retrieval can be started | **no** | **yes** |

### EVERY GATE DRIVEN ON THE ARTEFACT, real exit codes (`$?`, never through a pipe)

| command | rc | reading |
|---|---|---|
| `build-designer-pack.sh --check <zip> --commit 3f7a63a` | **0** | `CHECK GREEN — matches the manifest at 3f7a63a39e86` |
| `_gate_pack_imports.py --zip <v1.0.1 zip>` | **0** | 78 files · **MISSING-LOCAL 0 · ERROR-AT-IMPORT 0** · OK 73 · RAN-AS-SCRIPT 3 · NEEDS-ARGV 1 · MISSING-THIRD-PARTY 1 · `✅ every packed module imports from where it lands` |
| `_gate_pack_imports.py --zip <v1.0.0 zip>` (control) | **0** | **MISSING-LOCAL 1**, the predicted file and module. **The control still bites**, so the 0 above is a reading and not a broken probe |
| `_gate_release_audit.py --check` | **0** | PASS, byte-identical to a fresh generation at `3f7a63a` — ⛔ **was the addendum's NEW designed-red; CLEARED by the landing commit** |
| `_gate_release_audit.py --selftest` | **0** | `8 bites, 0 fail(s)` — ⛔ **same designed-red, CLEARED** |
| `_gate_release_audit.py --drift` | **0** | PASS — the manifest is now **AT HEAD**. This was a PRE-EXISTING advisory red in both reports |
| `_gate_release_audit.py --pack` | **1** | ⛔ **THE ONE REMAINING DESIGNED RED.** `dist/` still holds v1.0.0's zip. See § ⑤ |
| `_gate_frozen_release.py --check` | **0** | `PASS — 3 arm(s) asked, no frozen surface moved` |
| `_gate_frozen_release.py --selftest` | **0** | `14 bites, 0 fail(s)` — after the `SURFACES` bump |
| `_gen_pack_manifest.py --selftest` | **0** | `195 bites, 0 fail(s)` — after the version + fixture edits |
| `_gate_doc_rows.py` (at each commit) | **0** | `population 108 · unrowed 0` |
| `_gen_chain.py --check` | **0** | FRESH after regeneration |
| `gen_showroom.py --check` | **0** | 135 pages + index in sync |
| `_build_graph_mention_map.py --check` | **0** | current, 101 of 101 nodes |
| `_session.py --declare 220` | **0** | every witness present agrees |
| `bash -n build-designer-pack.sh` | **0** | after the `MEM_NAME`/`MEM_VERSION` edits |

### RETRIEVAL, FROM A CLEAN EXTRACTION — the addendum's step 8, and it is the real test

Extracted clean into `/var/tmp/fresh101` (1,646 files). This is the first time any of this has run
from an **extracted zip** rather than a `--stage` overlay.

| where | command | reading |
|---|---|---|
| `memento-package/machinery/` | `_build_memento_index.py` | `✅ 21 record(s)`, **5-class roster**, two optionals named ABSENT and the store named EMPTY-on-purpose |
| " | `_memento_search.py "capture ritual"` | answers — `runbook sections (5 of 12 shown)` |
| " | `--fetch runbook:capture-ritual:when-to-run-it` | returns the section **VERBATIM** |
| " | `--fetch runbook:no-such-thing` | **rc=1**, REFUSES with near misses (fail-loud held) |
| **the plugin mirror** | all three of the above | **all green**, `21 record(s)`, `throttle not a thermometer` → `[runbook:context-gauge:…]` |
| both dirs | `_gen_chain.py --check` | **rc=1, byte-identical honest refusal**: `GOOD-MORNING.md is missing` |

⛔ **The v1.0.0 `ModuleNotFoundError` is gone from a real extraction, in the mirror.** Both bake
reports carried this as **UNPROVEN** ("no zip was baked; every stage proof is a `--stage` +
overlay"). It is now measured. The two machinery dirs give the same sentence for the same reason.

---

## ⑤ ⛔ THE BLOCKER — `--release` REFUSED, AND WHAT I DID NOT DO ABOUT IT

```
$ bash apollo-spider/build-designer-pack.sh --release --commit 3f7a63a…      # rc=2
REFUSED: the working tree is dirty. A RELEASE is cut from a clean tree so that what is on
         GitHub and what is in the zip are the same thing — commit or stash first.
```

**The dirt is not mine.** A parallel bento/showroom lane is live in the shared tree and held these
paths dirty for the entire seam — it was *quiet* when the seam opened and woke up mid-flight,
growing a new asset directory during the final poll (6 polls over 2 minutes: 9 → 9 → 9 → 10 → 10 →
10 dirty):

```
 M knowledge/_render/_bento_edit_rails.json
 M knowledge/_render/gen_bento_matrix_217.py
 M knowledge/_render/gen_foundations_217.py
 M showroom/_foundations/{bento,bento-rails,grids-dashboard,grids-display,grids-gallery}.html
?? notes/_briefs/2026-08-27-220-rung-enact-neutral5.md
?? notes/_subreports/assets/2026-08-27-220-rung-enact/
```

Every one of my own paths is committed; the tree is clean **except** for that lane.

### WHAT I CONSIDERED AND REJECTED, stated so the conductor can overrule it knowingly

1. **Copy the `--dry-run` zip into `dist/`.** ⛔ **Refused.** The script prints *"DRY RUN —
   nothing here is a release"* over that file. The bytes would very probably be identical
   (`same commit + same manifest ⇒ byte-identical` is the script's documented, twice-proven
   property) — but "probably identical bytes" is not the point. Moving a dry-run artefact into
   the release surface is *deciding the release ran when the release gate refused*, and it would
   be indistinguishable afterwards from a release that was cut properly.
2. **Commit the parallel lane's files.** ⛔ **Refused** — explicitly outside this seam's paths,
   and it would sweep another lane's half-finished work into a release commit ([[feedback-worktree-reconcile-trail]]).
3. **`git stash`.** ⛔ **Refused.** The script names it as a remedy, but stashing a *live writer's*
   working files could destroy their session's work mid-edit.
4. **Bake in a clean `git worktree` / clone at `d64b83e`.** ⚠ **NOT DONE — and this is the one
   worth Dave's or the conductor's word.** It arguably satisfies `require_clean()`'s stated
   purpose *more* strictly than the parent tree does (a fresh checkout has nothing uncommitted at
   all), and the zip's identity is independently re-verifiable in the real repo with
   `--check <zip> --commit <landing>`. But **where a release is cut is a release-process
   decision**, not a seam's, and I searched the runbooks before calling it blocked
   ([[feedback-read-the-runbook]]): `_RUNBOOK-git-commit.md`, `_RUNBOOK-parallel-conductor.md`
   (§2.5 covers reconciling for *commits*, not for a release bake) and the script's own `--help`
   contract carry **nothing** on this case. This is ruling-shaped question 1.

### THE PUSH IS WITHHELD — and the dirty tree is the SECOND reason, not the first

⛔ **Even with a clean tree, pushing right now would be wrong.** `_gate_release_audit.py --pack`
is **rc=1**, and CI's `Baked-pack audit` step (`gates.yml:407`) is **BLOCKING** with only a `77`
COULD-NOT-ASK escape — `1` is not `77`:

```yaml
- name: Baked-pack audit — a zip in dist/ must match the manifest (BLOCKING; refuses while nothing is baked)
  run: |
    rc=0; python3 knowledge/_release/_gate_release_audit.py --pack || rc=$?
    if [ "$rc" = "77" ]; then echo "^ COULD-NOT-ASK …"; rc=0; fi
    exit $rc
```

Both bake reports say the same thing in the same words: *"Do not push the manifest change without
the bake in the same push."* The three commits sit **unpushed on local `master`, ff-ahead of
`origin/master`**, exactly as they should until the bake lands.

**`bash knowledge/_git_commit.sh --push` was NOT RUN.** I deliberately did not drive it even to
watch it refuse: it refuses on the dirty tree *before* pushing, but if the parallel lane had
committed between my `git status` and that call, it would have pushed a CI-red state. A refusal
worth watching is not worth that risk.

⛔ **THE CI READ-BACK IS OWED TO THE CONDUCTOR AND TO DAVE (`s203-D1`) AND I DO NOT CLAIM IT.**
Nothing here has been on GitHub. Every CI verdict above is a local reading of the same script plus
the blocking/advisory routing read in `gates.yml` source.

---

## FINDINGS — five

1. ⛔ **The version bump is FOUR homes and both REPLAY-THESE lists named two.** The unnamed one,
   `_gate_frozen_release.py`'s `SURFACES` literal, is the one that would have made the frozen gate
   refuse the release it was recording. A REPLAY-THESE list is a checklist written by a lane that
   could not run the step; it is not a survey [[premise-ages-faster-than-rule]].
2. ⛔ **A version constant had FOUR MORE typed copies downstream of it** — three in
   `build-designer-pack.sh`, one in Dave's go/no-go page — every one of which would have shipped
   `v1.0.0` inside a `v1.0.1` pack. ADR-0017 was written for exactly this and nothing enforces it
   on a *value*. All five now derive. The class is live: **nothing greps for a version literal
   outside its home.**
3. ✅ **Every projection in the addendum resolved EXACT — 8 of 8.** The lane that could not commit
   still got the arithmetic right because it measured its two inputs and labelled the rest
   [[planning-estimate-is-not-a-measurement]]. That is what an honest projection buys.
4. ⚠ **The `SURFACES` version bump is INERT and nothing checks it.** `--check` compares the ledger
   on disk to the measured surface; no arm compares the declared literal to the recorded row. So a
   bump that is never seeded is silent, and a `--seed` run against a stale literal is caught only
   *afterwards*, by the laundering arm, one commit later. Named, not fixed.
5. ⛔ **A shared working tree is a release-blocking dependency and nothing declares it.**
   `require_clean()` reads the WHOLE repo, so any lane touching any file anywhere can block a
   release bake indefinitely. The parallel lane did nothing wrong. This seam's plan had no step
   for it because no runbook has one.

---

## RULING-SHAPED QUESTIONS → DAVE (nothing decided here)

1. ⛔ **May a release be cut from a clean `git worktree` / clone at the manifest commit when a
   parallel lane holds the shared tree dirty?** The clean-tree refusal's stated purpose — "what is
   on GitHub and what is in the zip are the same thing" — is satisfied *more* strictly by a fresh
   checkout than by the parent tree. **PROPOSED: yes, with the standing condition that the zip is
   re-verified in the real repo by `--check <zip> --commit <landing>` before it is committed** —
   which makes the artefact auditable regardless of where its bytes were assembled. **Not done,
   and deliberately not done:** where a release is cut is your call, not a seam's.
2. **Should the release ratification be re-keyed per version?** `_gen_pack_manifest.py`'s
   `RATIFY_ID = "s219-D10"` — your word *"bake"*, dated 2026-08-26, for **v1.0.0**. The generator
   read `RATIFIED` for **v1.0.1** without anyone asking you again, and it will read RATIFIED for
   v1.0.2 and every version after. Your word for *this* bake exists (it is why this seam ran), but
   **the machinery did not check for it.** **PROPOSED: key the ratification to the version being
   cut**, so a bump makes the manifest go PROPOSED until you say the word again. This is the
   `s219-D4(2)` promise ("release is his word, not the script's") quietly not being kept.
3. **Should `apollo-spider/dist/` hold both zips?** The frozen surface is the DIRECTORY, so
   v1.0.0 stays beside v1.0.1 and the row's `version` names only the newest, while `files` goes
   1 → 2. **PROPOSED: keep both** — a shipped artefact should never be deleted — **but the ledger
   may want one row per VERSION rather than one row per SURFACE.** Not changed.
4. **`project` for `W-199`.** The Gumdrop index bootstrap is *Memento* machinery living in the
   *Apollo* release lane; the enum is `("apollo","memento")` and forces a choice. I wrote
   `apollo`, matching every other release row (`W-99zy`, `W-99zz`). **Flagged, not asserted** —
   a near-miss classification disappears from every filtered view.
5. **The `W-99z*` id lane is exhausted at `W-99zz`** (§ ②). Widening `ID_RE`, or ruling that the
   overflow family is closed and everything goes to the main lane, is yours.

---

## REPLAY-THESE (conductor) — FOUR items, and item 1 is one command

1. ⛔ **THE BAKE. Run it the moment the parallel lane's paths are committed.** Everything it needs
   is already in place; nothing needs regenerating.
   ```
   LAND=3f7a63a39e861a33bfa603b86c80fba24ec20c75
   git status --porcelain                    # must be EMPTY, or --release refuses again
   bash apollo-spider/build-designer-pack.sh --release --commit $LAND
   bash apollo-spider/build-designer-pack.sh --check apollo-spider/dist/Apollo-Spider-v1.0.1.zip --commit $LAND
   python3 knowledge/_review/_make_review.py reviews/RELEASE-SPIDER-2026-08-26-v1.html
   ```
   ⚠ `--release` **rewrites `reviews/RELEASE-SPIDER-2026-08-26-v1.html`** (it folds the measured
   zip bytes + sha into Dave's page) and strips the review pair's stamps — hence the third line.
   ✅ **EXPECT the zip to be byte-identical to the verified artefact:**
   `18,992,272 bytes · 1,646 entries · sha256 c8934ebdb63306526223f8478561ccd3e18f502b4db01746c0f468ba6e6923d1`.
   **If it is not, STOP** — that means something moved between this seam and the bake.
2. ⛔ **Commit the bake, then seed, then commit the ledger — TWO commits, in that order.**
   `seed()` measures `apollo-spider/dist/` from `git ls-tree` **at a commit**, so the zip must be
   committed before the seed can see it; and the laundering arm reads the ledger at the parent
   commit, so ledger and content land separately. This is `#219`'s shape exactly
   (`ef44b1a` zip+page → `3b6824f` ledger).
   ```
   # commit D:  apollo-spider/dist/Apollo-Spider-v1.0.1.zip  reviews/RELEASE-SPIDER-2026-08-26-v1{,.REVIEW}.html
   python3 knowledge/_release/_gate_frozen_release.py --seed
   # commit E:  knowledge/_release/_frozen-releases.json
   ```
   ✅ **EXPECT the seeded `apollo-spider` row to read `version: "v1.0.1"`, `files: 2`.** If it
   reads `v1.0.0`, the `SURFACES` bump was reverted — see finding 1.
3. ⛔ **Re-read `_gate_release_audit.py --pack` AFTER the bake commit. It must be `PASS`.** It is
   the only BLOCKING CI arm still red, and it is the one thing that must not be pushed past.
4. **Then push, and the CI read-back is still OWED.**
   ```
   bash knowledge/_git_commit.sh --push
   ```
   ⚠ It refuses on a dirty tree (only `notes/_REHEARSAL-LOG.jsonl` is excluded, `s137-D1`), so the
   parallel lane must have landed. **`s203-D1`: read the Actions run back to Dave in chat. This
   seam claims nothing about CI.**

---

## WHAT WAS WRITTEN

```
 M apollo-spider/build-designer-pack.sh              version + 3 Gumdrop literals now DERIVED
 M knowledge/_release/_gen_pack_manifest.py          VERSION, MEMENTO_CUT_VERSION, page root
                                                     derived, bite fixture moved
 M knowledge/_release/_gate_frozen_release.py        SURFACES apollo-spider version -> v1.0.1
 M knowledge/_release/_pack_manifest.json            regenerated at 3f7a63a
 M knowledge/_release/_pack_gate_probe.json          re-probed at 3f7a63a
 M knowledge/_state.json                             14 rows, through _state.add()
 M knowledge/_memento-index.json                     1,796 -> 1,803 records, ZERO LOST
 M _CHAIN.md                                         regenerated after the store rows
 M reviews/RELEASE-SPIDER-2026-08-26-v1{,.REVIEW}.html
?? notes/_subreports/2026-08-27-220-bake-seam.md     this report
+ the six #220 briefs, six #220 filed reports, their asset dirs, and two #220 review pages,
  all first committed here
```

**Not mine and untouched:** `knowledge/_render/*`, `showroom/_foundations/*`,
`notes/_briefs/2026-08-27-220-rung-enact-neutral5.md`,
`notes/_subreports/assets/2026-08-27-220-rung-enact/` — the live parallel lane.
**`knowledge/_rulings.json` was never opened for writing.** No gate was promoted. No gate was
weakened. `_build_all.py` untouched. `apollo-spider/dist/` untouched — **v1.0.0's zip still
measures `md5 7a28800b18c1f74b244607a4f254c266`.**

---

## UNPROVEN / CLAIMED (ADR-0016)

* ⛔ **UNPROVEN: a zip produced by `--release`.** The verified artefact came from `--dry-run`.
  Same commit, same manifest, same code path from `--stage` onward — and the script's
  determinism property is documented and was proven twice by the prior lane — but **`--release`
  has not run**, so this is a strong expectation, not a measurement. REPLAY-THESE step 1 states
  the expected bytes so the claim can be falsified in one command.
* ⛔ **UNPROVEN: the seeded v1.0.1 ledger row.** `--seed` was not run; it needs the zip committed.
* ⛔ **UNPROVEN: anything CI says.** No Actions run was read. Blocking/advisory routing read in
  `gates.yml` source.
* **UNPROVEN: the Copilot bridge.** Unchanged — nothing in this sandbox can open VS Code.
* **UNPROVEN: the render proof at v1.0.1.** Not re-measured here. The addendum's readings
  (`bento.html` 44/44, `grids-gallery.html` 30/30, `photography.html` 60/502 with 442 broken)
  stand on its zip, and **no image, page or manifest group moved in this seam** — `library` group
  bytes and files are unchanged. `photography.html` remains the prior report's question 2,
  undecided.
* **DECLARED LIMIT:** the parallel lane's dirty paths include
  `showroom/_foundations/bento.html` and `grids-gallery.html`, which **are in the ship list**.
  The bake reads `git archive` at `3f7a63a`, so their uncommitted edits **cannot** enter the zip
  — but the pack they produce is therefore *older* than the tree. That is correct and is the
  whole point of baking from a named commit; naming it so nobody re-derives it later.
* **CLAIMED, then re-read:** every figure here was taken from a command run in this window and
  quoted from its own output. Exit codes captured with each command's own `$?`, never through a
  pipe. The zip's bytes/entries/sha256 were measured on the file, not read from the build log.
