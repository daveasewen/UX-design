# `#294`-B — four rulings enacted: `s294-D2`, `s294-D3`, `s294-D9`, `s294-D12`

session: `#294` · 2026-09-21
window: conductor Fable 5.1 · lane B (sibling lanes A and G concurrent)
sub index: `B`
brief: lane B opener, this session (no brief file on disk)
tokens: `UNMEASURED — this seat's own `message.usage` is not readable from a lane; the two FILL
lines quoted below are the CONDUCTOR'S window, read by `knowledge/_checkin.py`, and they are a
different object from this lane's spend [[measure-dont-convert-units]]`

## VERDICT

**All four rulings are ENACTED and every one carries a selftest that was driven both ways.** Two
files changed, both mine this session: `knowledge/_capture_gate.py` and
`knowledge/_gate_scratch_hygiene.py`. No constant was moved. `_checkin.py`, `_gen_chain.py`,
`_gardener.py`, `_gauge_tokens.py` and `_git_commit.sh` are untouched — `_git_commit.sh` was READ
to find the wrap subject shape, which is what `s294-D3` asks for. ⛔ **Two of the four enactments
go RED on sight against the live tree, and that is the ruling's intent rather than a defect:**
the live wrap gate went **8 fails → 12** (3 × `s294-D2` on sibling lanes' uncommitted reports, 1
× `s294-D12` on 106 of 108 `_to_delete/` entries). The `s294-D12` refusal cannot be discharged
from inside the repo — `s282-D4` records that this mount refuses unlink — so it is a DECLARED
backlog handed to Dave, which is what the ruling says it is for. ⚠ **One premise did not
reproduce:** `s294-D2` states the COUNTS line parses in 173 of 363 filed reports; re-measured at
this seat with the gate's own `SUBREPORT_COUNTS_RE` it is **145 of 375**. The ruling's conclusion
is unaffected (still under half) and nothing was loosened to fit.

COUNTS: findings `10` · ruling-shaped `4` · UNPROVEN `3`

## What was done

### `s294-D3` — `WRAP_COMMIT_SUBJECT_RE` repointed, with a bite

`knowledge/_capture_gate.py`

- `WRAP_COMMIT_SUBJECT_RE` now reads `^#(\d+)\s+\d{4}-\d{2}-\d{2}\s+—\s` (was
  `^after\s+#\d+\b`), with a ~20-line header block stating the measurement and naming
  `_git_commit.sh:219-220` as the source of the two shapes.
- A sibling `NONWRAP_COMMIT_SUBJECT_RE` was added (the `after …` shape) so the selftest can
  assert the two are DISJOINT rather than merely assert the new one matches.
- Three prose refusals that quoted the wrong shape were corrected: `subreport_citation_check`'s
  DID-NOT-RUN warn, `regen_serial_check`'s SKIPPED note, and the header comment above
  `SUBREPORT_CITE_BLOCKING`.
- **ARM 9** added to `selftest_subreport_citation`: 2 positive forms, 5 negative controls
  (including the non-wrap shape, a mid-line mention, a hyphen separator and a dateless subject),
  a two-commit fixture repo proving `_last_wrap_commit` picks the wrap and not its non-wrap
  child, and an arm against the real `git log --format=%s -150` asserting the population is
  NON-EMPTY and that no real subject matches both patterns. **9 bites.**

### `s294-D9` — the pre-flight stamp's form widened

`knowledge/_capture_gate.py`

- `PREFLIGHT_MEASURED_RE` added beside `PREFLIGHT_UNMEASURED_RE`. It requires the `✅ CAPTURED`
  glyph run AND all three of the arm's named figures (`boot`, `now`, `peak`) AND the `real
  tokens` unit — scoped, like the #73 refusal, so it cannot be produced by accident.
- A branch in `check_preflight`, placed **after** the #56 `ABS_TOTAL_RE` dispatch so the priced
  form always wins and only the previous fall-through is caught. It appends a **WARN**, never a
  note: the line is legal but it is the SPEND, so the wrap is told in terms that the
  price-versus-actual dataset still has no estimate for that session.
- The final "no legal form" fail now names **three** legal forms instead of two.
- `selftest_preflight` gained the `s294-D9` block: all three forms planted together with one
  asserted verdict each (measured → no fail + labelled warn; priced → graded by the priced
  branch, NOT the measured one; #73 refusal → still a declared-gap warn), plus **4 mutation
  controls** proving the widening accepts the MEASUREMENT and not the word. **7 bites.**

### `s294-D2` — the three machine-read lines, BLOCKING

`knowledge/_capture_gate.py`

- New `SUBREPORT_LINES_BLOCKING = True` (the tier is one line, as the house does it) with a
  header block stating the scope choice and the grandfather mechanism.
- New `_subreports_vs_head()`, `_lines_missing()` and `subreport_lines_gate()`. The three
  regexes are **reused**, not re-written.
- Wired into `wrap_checks` beside the advisory citation check, which is left exactly as it was
  (different window, different clause — citation, not parse).
- `selftest_subreport_lines_gate()` — 8 arms, including the two that matter: a malformed report
  already in `HEAD` must NOT fail (the grandfather control), and that same report EDITED in the
  working tree MUST fail (the growth control, proving the exemption is keyed on the sha and not
  on the filename). Each of the three clauses is also driven alone. **13 bites.**

### `s294-D12` — `_to_delete/` gated, N = 3 sessions

`knowledge/_gate_scratch_hygiene.py` (the measurement) + `knowledge/_capture_gate.py` (the seat
that refuses)

- `_gate_scratch_hygiene.py`: `TO_DELETE_DIRNAME`, `TO_DELETE_MAX_AGE_SESSIONS = 3`,
  `ENTRY_SESSION_RE`, `COMMIT_SESSION_RE`, `_repo_root()`, `_commit_timeline()`,
  `entry_session()`, `to_delete_backlog()`, `to_delete_lines()`. `report()` prints the backlog on
  every run; `__main__` returns 1 on `--wrap` only. The docstring's "Exit codes: 0 always" line
  was amended rather than deleted.
- `_capture_gate.py`: `TO_DELETE_BLOCKING = True` and `to_delete_check(repo)`, which IMPORTS the
  arm rather than re-implementing it, wired into `wrap_checks`.
- Selftests: `selftest_to_delete()` in the hygiene file (8 arms) and
  `selftest_to_delete_backlog()` in the gate (the seat's own 4 assertions, and it DRIVES the
  hygiene suite rather than copying its fixtures). **12 bites.**

## Findings

1. **`s294-D3`, MEASURED.** `git log --format=%s -150`: the old pattern matched **129 of 150**
   subjects, the fixed one **21**, and the two patterns' match sets are disjoint. The 21 are all
   `#277 2026-09-16 — …`. Both live consumers now get a non-empty population; the last wrap
   commit resolves to `b5b2129d` (`#277 2026-09-16 — HANDOFF #128 written by the conductor …`).
2. **No session since #277 has made a `--wrap` commit.** The newest wrap subject in 150 commits
   is five days and sixteen sessions old. That is consistent with the #243 DECLARED NOT-A-WRAP
   path the last nineteen wraps have taken, and it is why the "newer than the last wrap" scope
   was rejected for `s294-D2` (finding 4).
3. **`s294-D9`, the defect confirmed by construction.** `PREFLIGHT_RE` matches the arm's
   `> **pre-flight #N:** ✅ CAPTURED …` line (tag group ` #294`), `"NOT CAPTURED" in line` is
   False for `✅ CAPTURED`, and `ABS_TOTAL_RE` finds no `= N of N` — so the line reached the final
   `fails.append` and was graded "pre-flight stamp is in no legal form". The mutation controls in
   the new selftest block re-drive exactly that path.
4. **`s294-D2`'s two offered scopes are both unusable, and the reasons are measurable.**
   (a) "staged in the commit": `_git_commit.sh` runs `python3 knowledge/_capture_gate.py --wrap`
   at its line ~527 and stages at ~line 845, so the index is empty when the gate runs and
   `--cached` would report green over everything. (b) "newer than the last wrap": with D3 fixed
   that window opens at #277 (finding 2) and sweeps in every report committed by #278…#293 —
   retro-failing sixteen sessions, which the ruling forbids in terms. **Scope taken:
   NEW-OR-MODIFIED AGAINST `HEAD`** (`git diff --name-only HEAD` ∪ `git ls-files --others`,
   restricted to `_is_subreport`). It is measurable at the moment the gate runs, it is exactly
   the set this working tree can still fix, and the refusal says so.
5. **⚠ `s294-D2`'s COUNTS premise does not reproduce.** Over all 375 filed reports on disk, with
   the gate's own three regexes: **COUNTS 145 · REPLAY-THESE 140 · RULING-SHAPED QUESTIONS 172**.
   The ruling states 173 / 136 / 169 over 363. REPLAY (+4) and QUESTIONS (+3) track the 12 new
   files; COUNTS is **28 LOWER** on a population that grew, so the 173 figure was not produced by
   `SUBREPORT_COUNTS_RE`. The ruling's conclusion holds either way — 39% is further under half
   than 48% — and nothing in the pattern was loosened to close the gap.
6. **The grandfathered population is 372 and it is COUNTED on every run** (`s186-D2` shape):
   missing COUNTS 227 · missing REPLAY-THESE 232 · missing RULING-SHAPED QUESTIONS 200. Not one
   is graded; a report enters the population only by being written or edited in this working
   tree.
7. **`s294-D2` fires on three reports right now, and they are not this lane's.**
   `notes/_subreports/2026-09-21-293-J5-recall-proposal.md`,
   `notes/_subreports/2026-09-21-293-J6-typesafe-docs.md` and
   `notes/_subreports/2026-09-21-294-C-commit.md` are uncommitted and carry **none** of the three
   lines. Unlike `s294-D12` this IS fixable inside the session: three lines per report.
8. **Ritual step 4c cannot refuse a wrap, and that is measurable rather than an opinion.**
   Nothing reads `_gate_scratch_hygiene.py`'s exit code: it is a hand-run runbook step
   (`knowledge/_RUNBOOK-capture-ritual.md:740`), `knowledge/_seam.py` imports only `mine()` and
   `_rm()`, and neither `_build_all.py` nor CI invokes it. `_capture_gate.py --wrap` is the only
   wrap check with a consumer that stops a commit (`_git_commit.sh:527-528`: fails the run and
   stages nothing). ⇒ **the measurement is seated at 4c and the refusal at the gate**, one
   implementation, imported. Flipping the hygiene probe's own tier was rejected separately: its
   docstring has said "ADVISORY, DELIBERATELY … promotion to blocking is Dave's" since #227.
9. **`_to_delete/`, measured at this seat 2026-09-21.** 108 top-level entries (the ruling's
   figure, reproduced; `ls -1` shows 106 because two are dotfiles). **106 are more than 3
   sessions behind #294**, 2 are inside the window. Oldest is `outputs_wrap110_part1.py.…` at
   session **#109 — 185 sessions**. Session derivation: **the entry's own name when it carries
   one** (40 of 108 do), **otherwise the newest commit older than its mtime** (66), and **undated
   entries are reported, never refused on**. Name-only would have silently excused 68 entries.
10. **The four inherited `_governs.py` selftest failures are NOT mine.** The full
    `_capture_gate.py --selftest` run ends on 5 ❌ lines, all of them the `_governs.py`
    trigger-index arm complaining that `s282-D2`…`s282-D5` evidence strings carry no pointer.
    Lane I's own filed report today (`notes/_subreports/2026-09-21-294-I-inscribe.md:146`) heads
    the same four with "**FOUR FAILS, ALL PRE-EXISTING, NONE OF THEM MINE**". Zero failures from
    any of the four enactments; zero COULD-NOT-ASK refusals.

## RULING-SHAPED QUESTIONS

1. **`s294-D12` refuses this session's wrap and the remedy is outside the repo — does the arm
   get a DECLARED form, or does it stand bare?** 106 of 108 entries are stale on the arm's first
   run, `s282-D4` says the mount refuses unlink, so nothing inside the repository can clear it.
   The wrap's existing legal path is the #243 DECLARED NOT-A-WRAP commit, which the last nineteen
   wraps have taken, so nothing is stranded today — but if a wrap ever wants `--wrap` again, this
   arm is the thing standing in front of it. Options as I see them: (a) leave it bare, which is
   the ruling read literally and makes the backlog impossible to ignore; (b) give it the house's
   declared-gap form (a named line in the receipt saying the purge is owed to Dave); (c) count
   the 108 as frozen at #294 and refuse only on GROWTH past that figure, the `s186-D2` shape
   applied to this directory too. **I enacted (a). I did not enact (b) or (c) — inventing a
   hatch for a ruling is the same overreach as inventing the ruling.**
2. **Is `s294-D2`'s scope the one he wants?** The ruling offered "staged in the commit" or "newer
   than the last wrap" and said to pick what the gate can measure and say which. I picked a THIRD
   thing — new-or-modified against `HEAD` — because both offered scopes are unusable for the
   reasons in finding 4. It is the nearest honest reading of "staged in the commit", but it is
   not the words he was given.
3. **`s294-D2`'s COUNTS premise is off by 28 (finding 5).** 173 of 363 is in the inscribed
   ruling text; 145 of 375 is what the gate's own regex reads today. Does the ruling's premise
   get amended by addition, or is the discrepancy just carried in this report? ⛔ I did not touch
   `_rulings.json` — it is not this lane's file and amending a ratified premise is not a seat's
   call.
4. **Does `--preflight-line` grow a priced half, and who writes it?** `s294-D9` is now satisfied
   on the GATE side: a measured line passes and is labelled as the spend. The dataset still has
   no ESTIMATE for a session that stamps only the measured line, so the hole moved rather than
   closed. **The diff `_checkin.py` would need (lane E1's file, not mine):** `preflight_line()`
   currently returns only the CAPTURED fill. To make one line carry both objects it would append
   the #56 priced clause after the measured one — `… real tokens over N continuous turn(s). ·
   PRICED: boot <N> + job <N> est + wrap <N> est = <N> of 200,000 — <BAND>` — where the three
   estimate terms are **arguments the opener supplies**, never figures the arm guesses
   [[feedback-measuring-tool-must-not-guess]]; the gate needs no further change, because
   `ABS_TOTAL_RE` would then find `= N of N` and the priced branch would win by construction
   (which the new `want == "priced"` selftest arm already pins).

## UNPROVEN

1. **`s294-D2`'s behaviour at a real `--wrap` commit is UNPROVEN.** I am read-only on git this
   session, so the arm was driven against `_capture_gate.py --wrap` (which shows the 3 fails) and
   never through `_git_commit.sh --wrap`. Price of proving it: one throwaway commit in a fixture
   repo with the real script, ~5,000 tokens, and it needs a seat that may write git.
2. **The exact exit code of the whole-suite run is UNPROVEN, though its verdict is not.** The
   sandbox call wall means `--selftest` can only be polled, not awaited in one call; the run
   printed its complete failure list (5 lines, all inherited) and no COULD-NOT-ASK lines, and
   `_selftest_body` returns 1 on that path by construction — but I did not capture `$?` from a
   surviving process. Price: a seat whose background process outlives its shell call.
3. **`s294-D12`'s commit-derived sessions are UNPROVEN for entries whose mtime was changed by a
   move.** `_to_delete/` is populated BY moving, so an entry's mtime may be the move's, not the
   work's — which would date it to the moving session rather than the originating one. That makes
   the arm's ages a FLOOR, never an overstatement, so it cannot produce a false refusal; it can
   produce a too-small age. Price of proving: a per-entry `git log` archaeology over paths that
   are gitignored and therefore have no history — i.e. it is probably not provable at all.

## Selftest and bite count

| ruling | arm | bites | verdict |
|---|---|---|---|
| `s294-D3` | `selftest_subreport_citation` ARM 9 (+ 8 pre-existing arms re-run) | 9 | GREEN |
| `s294-D9` | `selftest_preflight` `s294-D9` block | 7 | GREEN |
| `s294-D2` | `selftest_subreport_lines_gate` (new) | 13 | GREEN |
| `s294-D12` | `_gate_scratch_hygiene.selftest_to_delete` + `selftest_to_delete_backlog` | 12 | GREEN |

- `python3 knowledge/_gate_scratch_hygiene.py --selftest` → **`11 arms, all GREEN (3 scratch + 8
  s294-D12)`**, exit 0.
- `python3 knowledge/_capture_gate.py --selftest` → run in the background under `timeout` and
  polled (it exceeded the sandbox call wall at #293 and did again here, ~8 minutes). **5 ❌
  lines, all four the inherited `_governs.py` `s282-*` evidence-pointer failures plus their
  header; zero from these four enactments; zero COULD-NOT-ASK.** The suite has no arm-selection
  argv, so the four new arm groups were additionally driven by name via
  `python3 -c "import _capture_gate as g; g.selftest_…()"` — all four returned `[]`.
- `python3 knowledge/_validate_help_gate.py` → neither changed file is among its 15 ❌ (all
  pre-existing, in other files).
- `python3 knowledge/_capture_gate.py --wrap` on the live tree → **231 in scope · 12 fail · 366
  warn** (was 8 fail before this lane: the 7 standing inherited fails + #294's own). The 4 new
  ones are findings 7 and 9.

## What I could not do

- **No git writes of any kind.** No commit, no staging, no lock touching — sibling lanes A and G
  were live. The conductor stages by name from the stub.
- **`knowledge/_checkin.py` NOT edited** (lane E1's). The diff it would need is written out in
  ruling-shaped question 4 rather than applied.
- **`knowledge/_rulings.json` NOT edited.** Finding 5 contradicts an inscribed premise; amending
  it is not a seat's call (question 3).
- **`_to_delete/` NOT purged.** `s294-D12` says purging is not the arm's job and `s282-D4` says
  the mount refuses unlink. Nothing was removed, moved or renamed.
- **The whole-suite exit code not captured** — UNPROVEN 2.

## The two FILL lines

⚠ **BOTH ARE THE CONDUCTOR'S WINDOW, NOT THIS LANE'S** — `knowledge/_checkin.py` reads the newest
top-level transcript, which is the conductor's, and this seat's own `message.usage` is not
reachable from a lane. They are identical because the conductor has not spoken since this lane
opened; that is the `s294-D9` lesson in miniature, and the two figures are not this lane's spend
and are never converted into one [[measure-dont-convert-units]].

- **open** — `FILL 173,377 real  RESIDENT CONTEXT — what the last call actually sent` ·
  `ratio 87% of the 200,000 you passed — ON FILL, the comparable unit`
- **close** — `FILL 173,377 real  RESIDENT CONTEXT — what the last call actually sent` ·
  `ratio 87% of the 200,000 you passed — ON FILL, the comparable unit`

REPLAY-THESE: none — the stub carries everything, and everything else is in this file.
