# `#249`-`DP18` — DP-18 fit analysis: two engines fought; ONE two-axis engine in `__dvBehaviour` fills the tile (304 of 438, 0px slack) and every mark rides its line

session: `#249` · 2026-09-06
window: `W2 debt · lane DP18 (evidence tile / context rail share one auto row; every tile fills its cell)`
sub index: `DP18`
brief: the conductor's DP18 lane prompt in chat (read `2026-09-05-248-W2-R-rerun.md` finding 4 / RSQ 1 and `2026-09-05-248-W2-D3-reflow-vfit.md` section B, findings 2/4/5/7, RSQ 2/3; diagnose, prototype a single engine in `outputs/w2-debt/dp18/`, assess the rail, propose the library change)
tokens: `UNMEASURED — message.usage is not readable from a subagent; harness counter ≈115K drawn across 24 tool calls at the report's write`

## VERDICT

**Diagnosed and prototyped, 19/19 physics checks PASS, nothing ruled.** (a) The collision is two engines selecting the SAME `svg.dv-fit` set (`dv-behaviour.js` L23 and the template stub L1268 of `Template-dashboard-bento.reference.html`) with different height authorities: the stub's `fitHeight` takes the tile's slack, `__dvBehaviour`'s `fitOne` (`dv-behaviour.js` L54) reads `data-h` = 260 and re-pins the viewBox last — gridlines re-derived for the tall plot, viewBox 260 (it2 `gridMaxY` 274 vs 260, cited at `_assemble_b.py` L368–372). (b) VFIT lacks a y path for `polyline/path[data-ys]` and, sharper than the brief anticipated, for the three MARK glyph shapes Chart-line uses: `circle` (cy), `rect.dv-mk` (11×11, which VFIT's bar rule would SCALE) and `polygon.dv-mk` (diamond, untouched). (c) The one home is the FIT module of `knowledge/canon/dv-behaviour.js` — it already owns the x-axis contract, the resize listener and the init pass, and reaches every composed page through the `AUTO-BEHAVIOUR dv-behaviour` partial (`gen_component_partials.py`, `component-types.json` group `dataviz`, 14 members). The prototype (`outputs/w2-debt/dp18/after.html`) carries VFIT INTO `__dvBehaviour`, adds polyline/circle/rect-mark/polygon-mark y re-derivation, releases `height:auto`, and gives the ticks `data-dy="3"`. At 1440 the composed page's chart goes **821×260 → 821×304** in the 893×438 tile, empty ground under the figure **44.1 → 0**; at 1100 the content-sized row keeps **948×260**; live resize `821 304 → 948 260 → 821 304`. Label/tick font-size and grid/series stroke identical across widths; all 36 marks ride their line at deviation 0; glyph sizes unchanged. The rail's DP-18 case: option (b) `align-items:start` alone is a MEASURED NO-OP (268.4 → 268.4) because the template's own `.tpl-group > .c-bento__grid > .stat-card{height:100%}` pins the card; with that released too, Needs attention drops 268.4 → 223.4 (ragged seam). On the composed page the rail is content-sized at both widths (no stretch to remove) because rule 6b never reached it. Recommendation: proposal option (a), one lane, the physics script as its gate — the pick is Dave's.

`COUNTS: findings 9 · ruling-shaped 4 · UNPROVEN 5`

## What was done

1. Read `knowledge/canon/dv-behaviour.js` FIT module (L16–96: `fitCharts` L22, `gutterPL` L36, `fitOne` L51–91, resize L93–96, init L245–247) and the template's stub (`Template-dashboard-bento.reference.html` L1242–1420: guard `__dvFit` L1259, `slackBelow` L1313, `fitHeight` L1322, `fitY` L1330–1362, `fitOne` L1363–1408). Read Chart-line's markup (`Chart-line.reference.html` L429–481: `polyline[data-fxs][data-ys]` L442/466, `g.dv-marker[data-fx][data-x0] > circle` L443–454, ticks L436–439 with `data-dx="-8"` and no `data-dy`) and the composed page's marks (`dashboard.html` L396 `rect.dv-mk` 11×11, L408 `polygon.dv-mk`). Read how the partial reaches a page: `_assemble_b.py` L32 copies Chart-line L665–922 (the `AUTO-BEHAVIOUR dv-behaviour` block) verbatim; `component-types.json` `/component-type/dataviz` lists 14 members; the shipped page has **0** `__dvFit` (grep) — the stub was dropped at it3 (`_assemble_b.py` L368–372).
2. `outputs/w2-debt/dp18/_patch.py` → `before.html` (verbatim copy of `arm-B-sighted/dashboard.html`) and `after.html` (5 edits, all inside the page: VFIT block inserted after `gutterPL` in the `__dvBehaviour` script; `fitOne` takes `H = fitHeight(svg, W)` and calls `fitY` after the pin; polyline/path y from cached `data-fys`; `figure.dv-fit-on .dv-svg{height:auto}` in the page style; `data-dy="3"` on the 4 evidence-chart tick texts). No second engine exists on either file.
3. `outputs/w2-debt/dp18/_render.py` (Playwright `goto('file://…')` only) → `dp18-{before,after}-{1440,1100}-{full,wall}.png`, `probe-dp18.json`, the 19-check physics assertion (exit 0), live resize 1440→1100→1440, and option (b) on the composed page's rail.
4. Rail on the template: `template-scratch.html` (a COPY; `knowledge/` untouched) rendered at 1100 → `probe-dp18-rail.json`, `probe-dp18-rail-b2.json`, `dp18-rail-template-1100-stretch.png` (as-is), `dp18-rail-option-b-template-1100.png` (b + `height:auto`).
5. `outputs/w2-debt/dp18/DIAGRAM.json` — the mechanism for the conductor's SVG (nodes/edges, 260/304/415/438/44).
6. NOT done: no edit under `knowledge/`, `reviews/`, or `notes/` beyond this file; no git state change; no gate run on the prototype (it is a scratch page, not a snippet).

## Findings

1. **The collision is two engines with two height authorities on ONE selector.** `dv-behaviour.js` L23 `document.querySelectorAll('svg.dv-fit')` and the stub L1268 (same string) each fit every chart; the stub's guard is `__dvFit` (L1259) precisely so the partial is not skipped. `fitOne` in the partial (L54) sets `H = data-h || 260` and re-pins `viewBox` (L60) — whichever runs LAST owns the viewBox, and neither knows the other moved y. Token: `_assemble_b.py` L371 quotes it2 `evidence.gridMaxY 274 vs viewBox height 260`; R-rerun finding 4 quotes it1 `373 vs "0 0 736 260"`.
2. **What VFIT lacks for a line chart is FOUR y paths, not one.** The stub's `fitY` (L1350) selects `rect[y],line[y1],text[y]` only. On the composed chart that leaves (i) `polyline[data-ys]` at the baked y (36 points, 3 series), (ii) `circle` cy (12 marks), (iii) `rect.dv-mk` — which the rect rule WOULD have re-scaled as a bar (`data-fh × plot`), turning an 11px glyph into a 15.5px one at plot 371/216, (iv) `polygon.dv-mk` untouched. Token: first prototype pass (circle-only) rendered series C's diamonds ~14px above their line at 1440 (eyeballed, `dp18-after-1440-wall.png` before the fix); the fixed pass reports `markMaxDev` **0** and `markSizes` `['circle:5.5','rect:11','polygon:null']` identical before/after (`probe-dp18.json` `after-1440`).
3. **After, 1440 light**: tile `893.3×438.1` at y678.6 (unchanged from before); figure h **346 → 390.1**; svg **821.3×260 → 821.3×304.1**; viewBox `0 0 821 260 → 0 0 821 304`; `slackBelowFig` **44.1 → 0**; gridY `[230,158,86,14] → [274,187.3,100.7,14]` (count 4 = 4; `gridMaxY` 274 = `vbH − PB` = 304 − 30); category labels y 246 → 290 (= baseline + 16); ticks 3px under their gridline at both widths (`tickMinusGrid` `[3,3,3,3]`). Not 415: the composed tile is 438 and the figure's head + legend take 86px more than the template's bar chart; 415 is the template's number (D3 finding 2), 304 is this page's.
4. **After, 1100 light**: tile `1020×394`, svg `948×260`, viewBox `0 0 948 260`, slack 0 — the content-sized row keeps `data-h`, identical to before at 1100. The probe-at-`data-h` step (`fitHeight`) is what makes this not ratchet (D3 finding 5 re-confirmed on a line chart).
5. **Physics assertion 19/19 PASS** (`_render.py`, exit 0): slack <1px · svg box h = viewBox h at both widths (1:1 pin) · tick font-size set `{12px}` = `{12px}` · label font-size equal · grid stroke `1px` = `1px` · series stroke `2.5px` equal · gridline count unchanged · gridMaxY = baseline and ≤ viewBox h · every series y within `[PT, vbH−PB]` · all 36 marks on their line · glyph sizes unchanged · ticks 3px · labels baseline+16 · live resize returns · 0 page errors.
6. **Live resize** (`after-live-resize`): `0 0 821 304` → `0 0 948 260` → `0 0 821 304`, slack 0 at every step, `gridMaxY` 274 → 230 → 274.
7. **The rail's stretch does not exist on the composed page.** At 1100 the context rail is `666.7` wide (2 of 3 columns, cards stacked at x=40, `railCards` h 231.1/184 with content 183.1/136 = 48px padding each, no air); at 1440 h 231.1/203, same 48. Option (b) on this page: 231.1/184 → 231.1/184 (`after-1100-rail-align-start`). The 47px case is the TEMPLATE's rule-6b side-by-side layout (D3 finding 7), which `_assemble_b.py` never carried.
8. **Option (b) on the template needs TWO releases.** `template-scratch.html` at 1100: cards 516×268.4 / 516×268.4, content 244.4 / 199.4 (Needs attention carries ~45px; D3 read 47 with a different content measure). `align-items:start` on the rail grid alone → 268.4 / 268.4 (no-op) because `.tpl-group > .c-bento__grid > .stat-card{height:100%}` (template CSS, matched rule per `getComputedStyle` walk) pins the item. With `height:auto` on the cards too → 268.4 / **223.4**; the row and the context section stay 268.4 (`probe-dp18-rail-b2.json`). Crop `dp18-rail-option-b-template-1100.png`: a ragged seam, two unequal panels.
9. **Where two-axis fit belongs**: the FIT module of `dv-behaviour.js` (L16–96) — the same 60 lines own the x contract (`data-fx/fw/dx/x0/fx2/fxs`), the gutter measure, the resize listener (L93–96, "the ONE resize listener (ADR-0015)") and the init opt-in (L245–247). A second home (the template stub) is the defect by construction: the stub's own comment (L1253–1255) says delete it when the partial is present. `gen_component_partials.py` injects the module into all 14 `dataviz` members; no member other than Chart-bar (template copy) carries `data-pt/pb/h-min` today (`dashboard.html` L601 has them, L371 does not).

## PROPOSALS (options; the pick is Dave's)

**(a) — recommended.** ONE engine, two axes, in the library:
1. `knowledge/canon/dv-behaviour.js` FIT module — add `H_MIN_DEFAULT`, `slackBelow`, `fitHeight`, `fitY` (from the stub L1313–1362) with the four additions the prototype proved: `circle[cy]` → `data-fy`; `.dv-mk` rect → centre-anchored, height untouched; `polygon.dv-mk` → `data-pts0` + centre translate; `polyline/path[data-ys]` → cached `data-fys`, `Y(fys[k])`. `fitOne`: `H = fitHeight(svg, W)` before the pin, `Y = fitY(svg, H)` after it. Why: findings 1, 2, 9. (`outputs/w2-debt/dp18/_patch.py` is the diff, line-for-line.)
2. Chart-line / Chart-bar (and the other 12 members) svg attrs — `data-pt="14" data-pb="30" data-h-min="200"` on `svg.dv-fit`; `data-dy="3"` on tick texts (Chart-line L436–439). Why: finding 3's tick offset is fixed only with `data-dy`; the defaults in the engine cover a member that omits them, so this can be staged.
3. Canon rule per member: `figure.dv-fit-on .dv-svg{height:auto}` beside the existing `width:100%` release (`canon.css` L8991 for Chart-line, L7305 Chart-bar, …) — via the members' own `<style>` and `gen_canon_components.py`. Why: `height:260px` (L8987) is the CSS pin the viewBox re-pin cannot beat.
4. Delete the template's stub (`Template-dashboard-bento.reference.html` L1242–1420) and its `height:auto` rule (L676) once the partial carries VFIT; either register the dashboard role in `component-types.json` `dataviz` so the generator injects the real thing, or keep the template's Chart-bar block's own `AUTO-BEHAVIOUR` copy. Why: finding 1 — one selector, one engine.
5. Regenerate: `python3 knowledge/gen_component_partials.py` (write mode), `_validate_snippets.py`, `_validate_composition.py` on the template.
6. TEST — `knowledge/_validate_fit_physics.py` (or a `_render_*` beside the snippet): the 19 checks of `outputs/w2-debt/dp18/_render.py` run against Chart-line AND Chart-bar in a bento tile of fixed 438px height at 1440 and 1100: exit 0 iff font-size/stroke equal across widths, svg box = viewBox in both axes, slack <1px, every mark on its line, glyph sizes unchanged, ticks 3px, live resize returns. This is the mutation test: remove `fitY` and check 11 fails; scale the viewBox instead and check 3 fails.
7. Estimate: **1 lane** (engine + Chart-line/Chart-bar attrs + canon rule + template stub delete + regenerate + the test), ~40 tool calls; a second lane if all 14 members get attrs and a per-member physics run.

**(b)** Make the stub yield to `__dvBehaviour` — the template keeps VFIT only where no chart partial is present; every composed page with a real chart loses two-axis fit. Cheapest (5 lines), and it leaves s248-D2/D3 unmet on the pages that matter.

**(c)** Register the dashboard role in `component-types.json` `dataviz` without (a) — the generator then injects the width-only module into the template too; the template's VFIT is lost. Not sufficient alone.

## RULING-SHAPED QUESTIONS

1. **Which proposal** — (a) library engine + test (recommended), (b) stub yields, (c) register only. Dave's; D3 RSQ 2 and R-rerun RSQ 1 re-asked with a working prototype.
2. **Mark vs bar discrimination** — the prototype tells a glyph from a bar by the `dv-mk` class (finding 2). Is class the contract, or should marks carry an explicit `data-mark` attribute so a bar chart with a `.dv-mk`-less rect and a line chart with a differently named glyph both stay correct? An attribute is one more thing every member must author; the class is already there.
3. **The rail (finding 8)** — (a) accept the air; (b) `align-items:start` AND release the template's `.stat-card{height:100%}` (measured: 268.4 → 223.4, ragged seam); (c) a Stat-card fit (content answers height; s248-D3's reading — a library question). Note (b) contradicts the canon comment at `canon.css` L1108–1122 ("the stretch and the fill are one fix") only at the rail level, not the wall's. Recommend none — Dave's eye.
4. **The floor 200px** (`data-h-min`, D3 RSQ 4) — never binds on this page either (min tile seen 394 at 1100); stays PROVISIONAL-AWAITING-DAVE.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** the prototype on the other 12 `dataviz` members (donut, scatter, boxplot, candlestick, stacked-area, …) — each has y-bearing shapes the four paths may not cover (arcs, `path d` with baked y, `rect` bars WITH `dv-mk`-like classes). Price: one physics run per member (~2K each) — the lane that carries (a).
- **UNPROVEN:** dark theme and the other two themes — rendered light only (the geometry does not read colour; the font metrics do). Price: 4 panes (~3K).
- **UNPROVEN:** the JS-off answer after `height:auto` (scoped to `.dv-fit-on`, so the static 580×260 should hold) — not rendered with JS disabled. Price: 1 pane (~1K).
- **UNPROVEN:** the faint Aug marks in `dp18-after-1440-wall.png` are the `dv-animate` entrance mid-fade at the 900ms screenshot (markup `animation-delay:1041ms` at Aug in the snippet L450) — not re-shot at 3s. Price: one screenshot (~0.5K).
- **CLAIMED:** the evidence lives in `outputs/w2-debt/dp18/` by the conductor's hard rule (work only there), not in `notes/_subreports/assets/<stem>/` as `_TEMPLATE.md` asks — a declared deviation for the conductor's reconcile to copy.

## Evidence (paths)

`outputs/w2-debt/dp18/` — `before.html` · `after.html` · `_patch.py` (the diff, 5 edits) · `_render.py` (19-check physics assertion, exit 0) · `probe-dp18.json` (before/after × 1440/1100, live resize, rail option b on the page) · `dp18-before-1440-full.png` · `dp18-before-1440-wall.png` · `dp18-after-1440-full.png` · `dp18-after-1440-wall.png` · `dp18-before-1100-full.png` · `dp18-before-1100-wall.png` · `dp18-after-1100-full.png` · `dp18-after-1100-wall.png` · `dp18-rail-option-b-1100-wall.png` (composed page, no-op) · `template-scratch.html` · `probe-dp18-rail.json` · `probe-dp18-rail-b2.json` · `dp18-rail-template-1100-stretch.png` · `dp18-rail-option-b-template-1100.png` · `DIAGRAM.json`.
Conductor's before-crops: `outputs/w2-debt/crop-wall-1440.png`, `probe-page.json`.

REPLAY-THESE: `dp18-before-1440-wall.png` beside `dp18-after-1440-wall.png` (~1.5K tk each — 44px of ground vs 0) · `dp18-rail-option-b-template-1100.png` (~1K tk, the ragged seam) · the 19 PASS lines of `_render.py` (~0.4K tk).
