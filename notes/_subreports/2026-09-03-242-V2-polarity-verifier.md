# `#242`-`V2` — `W-387`: the polarity receipt ATTACKED — the gate holds, `#239`'s seam block IS stale, and the drop-out is a ROW drop-out, not a FILE drop-out

session: `#242` · 2026-09-03
window: Fable conductor, lane V2 (adversarial verifier against lane P's build of `s240-D3`)
sub index: `V2`
brief: conductor's message, this window (no brief file on disk)
tokens: `UNMEASURED — no message.usage at a sub's seat`. SHAPE: ~14 tool calls · **0 repo files changed** (one real-tree schema byte flipped and restored byte-exact, `git diff --numstat` unchanged) · 43 self-constructed attacks + 136 selftest arms + 2 full re-drives of V's 48-row harness + 6 of V's rows hand-driven standalone · 0 git commands · 0 writes to `polarities.json` / `principles.json` / `stubs.json` / `_generated/` / the store / the spine / memory / lane P's report or assets · seat `eager-wizardly-lovelace`.

## VERDICT

**The built gate held against every attack I could construct.** 43 self-constructed arms, driven against the gate as built: 29 expect-red, **29 caught BY NAME**, **0 ESCAPED, 0 crashes, 0 misnamed**; 8 green controls green; 4 pre-existing UNRULED escapes reproduced and confirmed unchanged. `--selftest` is **136 arms, 119/119 red by name, 11 new `#242` arms, 0 failures** — driven first-hand. I mutated the fixture in ways lane P did not: wrong-case receipt ids, a real id from the wrong register (`pr-jakobs-law`), a list-valued `$seed`, an empty-string `$seed`, `sources: []` beside a `$seed`, all three receipt keys at once, a retired node with no birth receipt, a stray fourth file under `_generated/`. **Every one is red BY NAME, not by accident.** I also separated the PIN from the FLOORS — something lane P's arms conflate: moving `SCHEMA_SHA256` honestly to the mutated schema's sha, six of seven schema mutations still go `SCHEMA-LOOSENED` by name, so the new `$seed`/`retiredBy` floors are real. **`#239`'s filed `_seam_block.sh` IS STALE** — checked, not believed: it contains **zero** occurrences of `POLARITY_BRAIN_DIR`, the live seam contains four, and re-driving V's harness with it produces a **false `D2` escape and a total of 5**; lane P's re-extracted block is **byte-identical** to `knowledge/_git_commit.sh:301-333` and gives **`CAUGHT 44 · ESCAPED 4`**. Lane P's finding 6 is CONFIRMED. On the six false-red controls I agree with lane P's reading and rule it **FAITHFUL, not a re-interpretation** — the clause is the ruling's own first sentence, *"the quote rule stands (every polarity traces to something Dave ruled) but the anchor widens"*: a node citing a path it invented traces to nothing Dave ruled, so it cannot be legal under a ruling whose first clause is that the quote rule stands. **TWO THINGS LANE P DID NOT PROVE.** (1) A retired node does **not** drop out of everything under `_generated/`: retire all 30 nodes, re-derive, gate GREEN — and `polarity-status.json` still contains the strings `pl-02` and `pl-22`, because `delta_vs_237T.cause` is a HARDCODED literal at `_validate_polarities.py:1208-1212` that names them by id and is emitted whether they are live or retired. Lane P filed that narrowing as a defect FIX (its finding 4) and never named it as a READING of the ruling's words "drops out of everything generated from the KG", nor put it to Dave. (2) The receipt's WIDENING is not floored: re-adding `sources` to the schema's `required` is a TIGHTENING, passes `SCHEMA-LOOSENED` even with the pin honestly moved, and silently makes every `$seed` node illegal (`S-SCHEMA — pl-40.sources: required key 'sources' is missing`) — no floor and no arm of the 136 catches it.

COUNTS: attacks driven `43` · expect-red `29` · caught `29` · ESCAPED `4` · crash `0` · misnamed `0` · findings `10` · ruling-shaped `5` · UNPROVEN `3`

*(ESCAPED `4` = the four PRE-EXISTING UNRULED rows `241 · 243 · 245 · 301` (`W-374`), reproduced twice and hand-driven standalone. **NEW escapes: 0.** Attacks driven counts only arms I constructed; it excludes the 136 selftest arms and the 96 harness row-drives, each reported separately.)*

## What was done

**(a) `--selftest`, driven.** `TMPDIR=/dev/shm python3 knowledge/_validate_polarities.py --selftest` → `arms 136 · red arms 119 (went red by name 119/119) · green arms 15 · no-fire/77 arms 2 · new #239 arms 72 · new #242 (s240-D3 receipt) arms 11 · failures 0`, 5.7 s. Lane P's arm count and failure count are CONFIRMED (`assets/…/selftest-tail.txt`).

**(a2) 22 self-constructed arms, breaking the fixture differently from lane P** (`assets/…/_v2_attack.py`, `attacks.txt`). Lane P's 11 new arms break the receipt one way each; mine break it sideways. Results in full in the asset; the shape is 16 red-expected → 16 red BY NAME, 6 green-expected → 6 green, 0 tracebacks. The arms that matter:

| arm | expect | result |
|---|---|---|
| `A5` `$seed: "S240-D3"` — a real id in the WRONG CASE | red | `R1-DANGLING` |
| `A6` `$seed: "s240-D9"` — a plausible absent neighbour | red | `R1-DANGLING` |
| `A7` `$seed: "pr-jakobs-law"` — a REAL id from the wrong register | red | `R1-DANGLING` |
| `A3` `$seed` is a LIST on a node that also has `sources` | red | `S-SCHEMA` |
| `A4` `$seed: ""` (empty string), no `sources` | red | `S-SCHEMA` + `S-SOURCE` + `R1-DANGLING` |
| `A2` `sources: []` beside a `$seed` (is "empty" neither, or both?) | red | `S-SCHEMA` (`minItems`) |
| `A9` all three keys (`sources` + `$seed` + `retiredBy`) on one node | red | `S-SOURCE` |
| `A8` `retiredBy` with NEITHER birth receipt | red | `S-SOURCE` |
| `A21` a RETIRED node carrying a bogus `resolvedBy` quote | red | `R3-QUOTE-NOT-VERBATIM` |
| `A22` a fourth file dropped into `_generated/` | red | `R4-STRAY-FILE` |
| `A17` a `$seed` node that is ALSO retired (lane P's "additive" reading) | green | rc 0 |
| `A18`/`A19` `$seed` naming a SUPERSEDED (`s129-D1`) / NOT-RULED (`ds-021`) id | green | rc 0 |

**(b) V's `escaped-repro.txt`, re-driven twice** (`assets/…/escaped-repro.txt`, `escaped-repro-with-239-seam-block.txt`). Lane P's harness copied and re-seated on **my own** `F` and `OUT_DIRS` — it wrote nothing into lane P's assets. With lane P's re-extracted seam block: **`TOTAL: 48 rows re-driven · CAUGHT 44 · still ESCAPED 4 (RULED 0 · PROMISED 0 · UNRULED 4)`**, `RULED 16/16`, `PROMISED 26/26`, `UNRULED 2/6`, still escaping `[241, 243, 245, 301]`. **Lane P's figure is exact.** With `#239`'s FILED block: `CAUGHT 43 · ESCAPED 5`, the fifth being `D2 PROMISED ESCAPED`.

**(b2) Six of V's 48 rows hand-driven STANDALONE, in V's own form, with my own commands** (`assets/…/_v2_hand_drive_V_rows.py`, `hand-drive-V-rows.txt`) — because re-driving lane P's own script proves lane P's script, not the gate. `241 · 243 · 245 · 301` all return **rc 0 at both doors** (write and check) — genuinely escaping, and exactly the four UNRULED rows. `310` (schema `additionalProperties` + a `judgement` key) → `R3-JUDGEMENT-FIELD` + `SCHEMA-LOOSENED` + `SCHEMA-PIN-MISMATCH`; `30` (a `resolvedBy` to a prose-superseded ruling) → `R1-SUPERSEDED` + `R3-QUOTE-MISSING`. All six agree with the harness.

**(c) The false-red controls, literal AND legal, driven by me** (`assets/…/_v2_controls.py`, `controls-and-dropout.txt`) — see finding 5 for the ruling and the clause I rely on.

**(d) `SCHEMA_SHA256` on the REAL tree** (`assets/…/pin-real-tree.txt`). One byte flipped in `knowledge/brain/schema/polarity.schema.json` (`"Polarity node"` → `"Polarity Node"`), the pin left alone → rc 1, `⛔ REFUSED (SCHEMA-PIN-MISMATCH) — … sha256 ce7276c83b83e00a… is not the pinned c2c165ac126d878d…`. Restored from a pre-flip copy; sha256 back to `c2c165ac…`, `git diff --stat` still `18 insertions(+), 6 deletions(-)`, gate rc 0.

**(d2) The FLOORS, separated from the PIN** (`assets/…/_v2_floor.py`, `floors.txt`) — see findings 4 and 6.

**(e) The drop-out, over EVERY generator and EVERY output** — see findings 3 and 9.

**(f) / (g)** Two receipts on one node: refused BY NAME, three ways — `A1` (a NEW node carrying both), `A9` (all three keys), `A2` (an empty `sources` array beside a `$seed`). A `$seed` naming a nonexistent ruling id: refused BY NAME, three ways — `A5` (wrong case), `A6` (absent neighbour), `A7` (a real id from the wrong register). All `R1-DANGLING` / `S-SOURCE`, none by accident.

## Findings

1. **`#239`'s filed `_seam_block.sh` IS STALE — confirmed by drive, not by lane P's word.** Probe: `grep -c POLARITY_BRAIN_DIR` over `notes/_subreports/assets/2026-09-02-239-F-polarity-fix/_seam_block.sh` returns **0**; the same grep over `sed -n '301,333p' knowledge/_git_commit.sh` returns **4**. `diff` of the live seam against lane P's re-extracted block is **empty**. Driving V's harness with the stale block yields `TOTAL 48 · CAUGHT 43 · ESCAPED 5`, the extra row being `D2 PROMISED ESCAPED · (b) POLARITY_BRAIN_DIR=<clean copy> rc=0 names=[]` — the exact false regression lane P warned of. `assets/…/seam-block-staleness.txt`.

2. **`ESCAPED 4` is real and unchanged, and it is not lane P's harness telling me so.** Two independent routes agree: the harness (`CAUGHT 44 · ESCAPED 4`) and six rows hand-driven standalone from V's own `escaped-repro.txt` text. `241 · 243 · 245 · 301`, all UNRULED, all `W-374`, none touched by `s240-D3`. **No new escape was introduced by this build.**

3. ⛔ **THE DROP-OUT IS A ROW DROP-OUT, NOT A FILE DROP-OUT — and the ruling's words are "everything generated".** Probe (`assets/…/controls-and-dropout.txt`): retire all 30 nodes, `--write`, `--check` → **rc 0, GREEN**; then a RAW-TEXT scan of each of the three files for each retired id returns `defaults-declaration.txt NONE · polarity-edges.json NONE · polarity-status.json ['pl-02','pl-22']`. The cause is `knowledge/_validate_polarities.py:1208-1212` — `delta_vs_237T.cause` is a **hardcoded string literal inside `derive()`** that names `pl-22` and `pl-02` by id and is emitted regardless of whether they are live or retired. `generated_node_ids()` (`:1328`) deliberately does not read that position. Lane P's finding 4 records the narrowing as a **defect fix** ("a text scan over a generated file cannot tell a derived row from a quoted example") — which is a good engineering call — but never names it as a **reading of `s240-D3`**, and its arm 112 makes the leak a standing GREEN control, so the selftest now certifies the leak as correct. Consequence: if `pl-02` or `pl-22` is ever genuinely retired, a generated file will name it forever and the gate will say green; a consumer that greps rather than parses would still see it. **This is the single most important thing lane P did not prove.** RULING-SHAPED 1.

4. ⛔ **THE RECEIPT'S WIDENING IS NOT FLOORED — `$seed`'s legality can be legislated away by an edit the gate calls a TIGHTENING.** Probe (`assets/…/floors.txt`, `_v2_floor_tightening.py`): put `"sources"` back into the schema's `required`, move `SCHEMA_SHA256` honestly to the new sha (exactly what a legitimate two-file schema edit does), then add one legal `$seed` node → **rc 1, `⛔ REFUSED (S-SCHEMA) — pl-40.sources: required key 'sources' is missing`**. The `required` floor is a **`superset`** check (`_validate_polarities.py:198`), so adding a key is a tightening and passes `SCHEMA-LOOSENED`. Nothing pins that `sources` is NOT required. Lane P's finding 1 ("nothing loosened — the floor moved into code") is true for BOTH and NEITHER, which `check_receipt` enforces; it does not cover the **permission** the ruling granted. RULING-SHAPED 2.

5. **On the six false-red controls, lane P's reading is FAITHFUL — I rule it so, and name the clause.** Driven by me (`assets/…/controls-and-dropout.txt`): the literal shapes stay red — `C100` fictional R1 path → `S-SOURCE`; `C225` `/etc/hostname` → `S-ID` + `S-SOURCE`; `C248` path `'x'` → `S-SOURCE`; `C321` 30 rows DELETED → `S-SOURCE`. Their `s240-D3` legal forms go green — the same node with `$seed` → rc 0; the 30 rows RETIRED rather than deleted → `--write` rc 0, `--check` rc 0. The clause I rely on is `s240-D3`'s own first sentence: *"the quote rule stands (every polarity traces to something Dave ruled) but the anchor widens"*. A node citing a path it invented traces to **nothing Dave ruled**; making it literally green would contradict the clause that the ruling opens with, and would re-open V's escapes `100 · 225 · 248 · 324`. The ruling's *"the six false-red controls become legal under it"* is therefore satisfied by supplying the legal form, not by passing the literal fixture bytes. **Not a re-interpretation.** Kept as RULING-SHAPED 3 because it is a reading of Dave's words and mine is a verifier's opinion, not his.

6. **The floors ARE real, independently of the pin — a thing lane P's arms cannot show.** Lane P's arm 111 asserts `SCHEMA-LOOSENED` **and** `SCHEMA-PIN-MISMATCH` together, so it cannot tell which one bit. Probe (`assets/…/_v2_floor.py`): import the gate as a module, mutate the schema, **move `SCHEMA_SHA256` to the mutant's sha**, drive. Six of seven still go red with `SCHEMA-LOOSENED` alone and name the floor in words — e.g. `$seed.maxWords: 5 loosens the pinned floor 1`, `$seed.pattern: '.*' loosens the pinned floor '^[A-Za-z0-9][A-Za-z0-9._-]*$'`, `$seed.type: 'object' loosens the pinned floor 'string'`, and the same three for `retiredBy`. The seventh is finding 4.

7. **A receipt may name a SUPERSEDED or a NOT-RULED ruling; a `resolvedBy` to the same id may not.** Probe: `A18` `$seed: "s129-D1"` (the store's one `superseded_by` row) → rc 0 GREEN; `A19` `$seed: "ds-021"` (status not `ruled`) → rc 0 GREEN; `A20` `retiredBy: "s129-D1"` → rc 0 GREEN. Against V's row 30, a `resolvedBy` to a **prose**-superseded ruling → `R1-SUPERSEDED`. The asymmetry is exactly what lane P declared as its RULING-SHAPED 5 ("PRESENCE only") — it is now **driven**, not just asserted. RULING-SHAPED 4.

8. **A retired node is still fully validated — confirmed independently.** Probe: `A21` retires `pl-01` and replaces its link with a `resolvedBy` carrying a quote that is verbatim nowhere → rc 1, `R3-QUOTE-NOT-VERBATIM`. Only the derivation ignores a retired node; every refusal still reaches it. Lane P's finding 8 stands.

9. **"Every generator" resolves to ONE generator, and the answer is provable rather than asserted.** Probe: `GEN_FILES` is the three files (`:154`); `derive()` (`:1140`) is the single place all three are built and it now takes `live_nodes(...)`; a repo-wide grep for `polarity-status` / `polarity-edges` / `defaults-declaration` / `polarities.json` outside `_to_delete/`, `outputs/`, `_subreports/` and `brain/_generated/` returns only `_validate_polarities.py`, `_build_all.py` and `_git_commit.sh` (both of which merely INVOKE `--check`, neither writes), plus prose mentions in `_memento-index.json`, `_CARRIES.md` and two `_tmp/wrap*` files; and `A22` proves a fourth file under `_generated/` is `R4-STRAY-FILE`. So no second generator can exist there unnoticed. The gap is not "another generator" — it is finding 3, inside the one generator.

10. **The repo is untouched by this lane.** After every mutation and the real-tree pin flip: `sha256 knowledge/brain/polarities.json = 02d56dc4bca8be53ee5d6e83778bc104c0b2624ee0900bab3e2bbdad5f531c1b` (lane P's / lane F's receipt, unchanged), `schema/polarity.schema.json = c2c165ac126d878d…` (the new pin), and `git status --porcelain` lists `knowledge/brain/_generated/` **not at all** — the three generated files are byte-identical to HEAD, so the derivation genuinely did not move. `git diff --numstat` is unchanged at `233/12` for the gate and `18/6` for the schema. Every mutation lived under `/dev/shm` or `/sessions/…/v2`.

## RULING-SHAPED QUESTIONS

⛔ **MANDATORY SECTION.** Nothing below is decided. Each is Dave's.

1. **Does "drops out of EVERYTHING generated from the KG" mean every derived ROW, or every BYTE of the generated files?** As built it is rows: a retired `pl-02` / `pl-22` still appears in `polarity-status.json`'s static `delta_vs_237T.cause` prose and the gate stays green (finding 3). Options: **(a)** as built — a prose mention is not a derived row, and the narrow read is what stops the gate refusing a correctly derived file (~0 lines, already built, but the selftest now certifies the leak as correct); **(b)** derive `delta_vs_237T.cause` from the live set — the two ids become live lookups and disappear when retired (~15 lines in `derive()`, and the three generated files change bytes, so `_generated/` moves and every clock is re-stamped); **(c)** keep the narrow leak check but have `derive()` scrub retired ids out of any emitted prose (~8 lines, same byte consequence). Recommend **(b)** — it is the only option under which the ruling's own words are literally true, and it removes the one hardcoded pair of node ids from the derivation. **This is the item I most want Dave to see.**
2. **Should the receipt's WIDENING be floored, the way its refusals are?** Today `SCHEMA_FLOORS`' `required` entry is a `superset` check, so re-adding `sources` to `required` is a legal tightening that silently kills every `$seed` node (finding 4). Options: **(a)** as built — a tightening is always allowed, and the consequence would be caught the first time someone authored a `$seed` node; **(b)** add a `not-superset` / `excludes` floor kind pinning that `sources` is NOT in `required`, plus one selftest arm (~12 lines); **(c)** leave the floor and add the arm only, so the selftest names the trap without forbidding it (~6 lines). Recommend **(b)** — `#239`'s whole point was that a schema edit cannot legislate a refusal away; the same protection should cover a permission the ruling granted.
3. **Did "the six false-red controls become legal under it" mean their LEGAL FORMS (as built and as I verify) or their LITERAL fixture shapes?** My verdict is legal forms, on the ruling's own opening clause (finding 5), and I recommend Dave confirm it in one word rather than leave a verifier's reading standing as the record. The alternative re-opens V's escapes `100 · 225 · 248 · 324` and I did not build it.
4. **Must a `$seed` / `retiredBy` ruling be LIVE?** Now DRIVEN, not just declared: a receipt naming a superseded (`s129-D1`) or not-ruled (`ds-021`) id is GREEN, while a `resolvedBy` to a prose-superseded id is `R1-SUPERSEDED` (finding 7). Lane P recommends as built (a receipt names history). I agree, and add the observation that the asymmetry should be **stated in the schema's `$seed` description** so a reader does not infer the `resolvedBy` rule applies (~2 lines). Dave's.
5. **Lane P's RULING-SHAPED 1–8 are all still carried**, in particular its 2/3 (the quote oracle a `$seed` node does not have), its 4 (`retiredBy` additive) — which I drove green at `A17` and found consistent — its 6 (`R4-RETIRED-GENERATED` as a new sub-form name) and its 7 (`stubs.json` gets no `$seed`). Nothing I drove contradicts any of them.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** 42 of V's 48 rows rest on lane F/P's harness `_escaped_now_caught.py`, which I copied and re-seated on two constants but did not re-implement. I hand-drove 6 of the 48 (`241 · 243 · 245 · 301 · 310 · 30`) standalone from V's own text and all six agreed — so the harness is corroborated at 6 points, not audited. Price to close: re-writing all 48 standalone forms from `escaped-repro.txt`, ~3 windows of drive.
- **UNPROVEN:** `knowledge/_test_git_commit.py --selftest` (the 44-arm seam harness) — not run; it spawns `git` inside its fixture and the fence forbids it. The seam block was not edited by lane P (`_git_commit.sh` is absent from `git status`), so the risk is inherited, not new. Price: one git-capable run, ~7 s.
- **UNPROVEN:** the build LOOP and CI. `_build_all.py` was not run (fence), so rc 1 routing from the polarity STEPS entry through `main()` is still only driven in the build's subprocess form (row `D1` of the harness). Price: the conductor's CI run.
- **DISCHARGED (was going to be UNPROVEN):** that finding 3's leak is the ONLY hardcoded node id in the derivation. Probe run: `sed -n '1140,1258p' knowledge/_validate_polarities.py | grep -n "pl-[0-9]"` returns exactly two lines, both inside the one `delta_vs_237T.cause` literal (`:1208-1209`); over the whole file before the selftest frame (`awk 'NR<1688'`) the only other hits are `:1330-1331`, the `generated_node_ids()` docstring describing that same literal. **So the leak is one string, in one place, and finding 3's RULING-SHAPED 1 (b) is a ~15-line change to exactly that literal — no second site.**
- **CLAIMED:** that no repo file outside `knowledge/brain/` changed during this lane — asserted from `git status --porcelain` and three sha256 reads taken after the last mutation, not from a `git stash`-and-compare. The three `.jsonl` files and `knowledge/_boot_decompose.py` that show as modified/untracked were already so when lane P arrived and when I arrived; neither of us touched them.

## Evidence

`notes/_subreports/assets/2026-09-03-242-V2-polarity-verifier/` —
`attacks.txt` (**the 22 self-constructed arms** with rc and refusal names) + `_v2_attack.py` (its driver) · `floors.txt` (**the pin/floor separation**, 7 arms with the pin honestly moved, plus the F6 tightening consequence) + `_v2_floor.py` + `_v2_floor_tightening.py` · `controls-and-dropout.txt` (**the false-red controls literal vs legal, and the raw-text drop-out scan that finds the `pl-02`/`pl-22` leak**) + `_v2_controls.py` · `hand-drive-V-rows.txt` (**six of V's rows driven standalone, outside lane P's harness**) + `_v2_hand_drive_V_rows.py` · `escaped-repro.txt` (**V's 48 rows re-driven — `CAUGHT 44 · ESCAPED 4`**, lane P's seam block) · `escaped-repro-with-239-seam-block.txt` (the same run with `#239`'s stale block — `CAUGHT 43 · ESCAPED 5`, the false `D2`) · `seam-block-staleness.txt` (**the staleness proof**: greps, the empty diff against the live seam, and the two totals side by side) · `pin-real-tree.txt` (the real-tree byte flip, its refusal line, and the byte-exact restore) · `selftest-tail.txt` (136 arms, 0 failures, driven first-hand).

REPLAY-THESE: finding 3 + `assets/…/controls-and-dropout.txt` (~0.5K — **the retired ids that stay in a generated file while the gate says green**; RULING-SHAPED 1) · finding 4 + `assets/…/floors.txt` F6 block (~0.3K — the tightening that kills `$seed`; RULING-SHAPED 2) · finding 5 (~0.4K — the clause that makes lane P's controls reading faithful; RULING-SHAPED 3) · `assets/…/seam-block-staleness.txt` (~0.3K — **read before anyone re-runs the harness**) · `assets/…/escaped-repro.txt` TOTAL block (~0.2K — `CAUGHT 44 · ESCAPED 4`, the four UNRULED unchanged)
