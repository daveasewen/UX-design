# `#259`-`W` — the delegated capture ritual for #259

session: `#259` · 2026-09-08
window: delegated wrap sub (Opus 5), conductor Fable
sub index: `W`
brief: `notes/_briefs/2026-09-08-259-delegated-wrap-brief.md`
tokens: `UNMEASURED — a sub cannot read its own message.usage; the conductor's transcript reads 155,870 real at this seat`

## VERDICT

The ritual ran WHOLE — steps 1, 1b, 2, 2c, 2d, 2e (nothing to roll), 2f, 4b, 5 — and the wrap gate
REFUSES with **3 fails**, two of them expected and one NEW. Expected: the boot-drift ceiling arm on
**#255's 70,127** (the literal is shrink-only; it was NOT moved, and the commit is made in the #243
form) and the retrieval-index staleness, which step 2g clears after the wrap commit. **NEW and
ruling-shaped: `GOOD-MORNING.md`'s `#### 2026-09-08 #259` stratum is UNROLLABLE** — the conductor's
opener wrote `#### 2026-09-08 #259 — ceiling discharge` into `notes/_GAUGE-LOG.md` under `s244-D1`,
so the session key was taken before the stratum existed and `roll_2f`'s duplicate-key guard will
refuse it at #260, the permanent condition #40/#41/#42 sit in. `STRATA_EXEMPT` is a CLOSED list of
three (Dave #58) and the discharge block is dated and append-only, so no repair was available at
this seat: it is DECLARED in the stratum, in `COMMIT STATE #259`, and carried as item ⑦.

COUNTS: findings 4 · ruling-shaped 3 · UNPROVEN 2

## What was done

- **1 / 1b** — `_LIVE-STATE.md` `Last refreshed` rewritten (date from `date`, 2026-09-08) with a new
  #259 summary; dossier `_DECISION-HISTORY/2026-09-08-259-a-renderer-not-a-recipe.md` written with
  its `provenance:` / `status:` lines.
- **2** — new ★ LATEST banner (#259). **S1 cap (`s241-D2`) MET: 1,014 tape / 10 substantive lines**
  against 1,200 / 10, measured with `_capture_gate.py`'s OWN slice (★ LATEST heading → ★ PRIOR
  start). Written SHORTER, never quieter: the ruling's full words, the seven-lane gauge, the two
  reds' three ruling shapes, the declared skips with their sizes and the whole STILL-HIS line live
  in the ⏱ LATEST delta, which the cap's text names as their home.
- **2c / 2d / 2f** — every move through `python3 knowledge/_gm_move.py --ops <session-owned, uniquely
  named ops file>`, existence + size asserted in the writing process, receipts read back. The **#259
  batch headers were created FIRST**. 2c: **12 ln** (the #257 banner plus its trailing `>`
  separator) → `_GM-ARCHIVE.md` § Batch 2026-09-08 #259. 2d: **16 ln** (#256 ⏱ delta) + its
  `Previous:` chain segment → `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-08 #259, in one section.
  2f: **14 ln** → `notes/_GAUGE-LOG.md`, **3 ln** → `_GM-ARCHIVE.md`; the mover DECLARED the append
  as *behind later blocks [259]*, permitted under D5 (a).
- **2e** — nothing to roll: DO-FIRST measures **109 raw lines**, `####` scan returns **0**.
- **`_CARRIES.md` § `residual → #260`** — built from the `→ #259` line by ONE programmatic pass
  (`[NEW — 0…]`→`[1…]`, `[N…]`→`[N+1…]`), then three ADDITIONS with receipts: carry ① STRUCK
  (`s183-D1`/`s188-D2`; the engine landed at `f6f762a`), blank `behaviour` metas CORRECTED 51→45,
  stale showroom CORRECTED 60→≈66. The `⑩ CONTEXT-AWARE RESPONSIVE BEHAVIOURS` item that #258's
  post-wrap capture appended to the `## residual → #259` HEADING was folded into the line at its
  bumped age so it could not be dropped. **487 aged · 7 new · 1 STRUCK · 2 CORRECTED · probe 349.**
- **`_state.json`** — 6 rows added, one per lane report (doc-row gate 369 in scope, **unrowed 0**).
- **`knowledge/_REVIEW-SIGNOFF.md`** — 6 rows added, one per committed chart-engine test page.
- **`_CHAIN.md`** regenerated to a fixed point AFTER the declare-last `size:` stamp: footer reads
  **7,933 tape**, GM **34,409**, chain **23%** of GM against the generator's `<40%` floor.
- **4b** — `_gen_titles.py` re-run against the #259 banner (its receipt was stale from #258).

## Findings

1. **The S1 cap was met with 186 tape of headroom** — measured three times through the gate's own
   slice, not estimated.
2. **The chain is the leanest this line has carried since `s214-D6` was written** — 7,933 tape
   against the ~10–12K target and #240's 11,319; quoted from the generated footer, re-read after the
   stamp because the stamp sits inside the slice.
3. **The #259 stratum key collision** (see VERDICT). Probe: `_capture_gate.py --wrap` fail line 1,
   and `grep -n "^#### " notes/_GAUGE-LOG.md | tail -1` → `#### 2026-09-08 #259 — ceiling discharge`.
4. **`carry_wording_check()` is still blind** (both carry lists live in `_CARRIES.md`); it reports
   `1 aged carries on the PRIOR residual, 0 paired`. The `s188-D2` invariant was held BY HAND here
   and that is DECLARED rather than discovered later.

## RULING-SHAPED QUESTIONS

1. **The #259 stratum key** — (a) exempt this one block, (b) re-key it, (c) teach the `s244-D1`
   discharge form to use a NON-session key so a declaration can never eat a stratum's key again.
   Recommend (c) plus (a) for this instance: (c) closes the class, (a) unblocks #260's roll.
2. **The page budget `s250-D1`** (carried, not ruled here) — core priced once per page · raise ·
   drop `dv-legend` where the engine draws its own legend. Measured: once-per-page fits combo with
   607 spare.
3. **The dv-004 class** — do driven receipts become the gate for engine-drawn types, or does the
   static gate learn to skip them? [[no-gate-parses-the-artefact]], owed twice.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** every #259 build figure in this wrap's record — the engine byte counts, the 8/8
  theme drives, the 43,518 / 37,787 / 34,816 budget readings, the 2.199px ring, the sub-token
  totals. They are QUOTED from the wrap brief and the six lane reports with their source named;
  nothing was re-driven at this seat (no render, no browser). Price to prove: one Playwright matrix
  per type plus two gate runs.
- **CLAIMED:** the conductor's `≈146,000` brief-cut FILL — declared as an approximation in the brief
  header and never converted into a measured figure; his last MEASURED check-in was 144,935.

## Evidence

No evidence files: every claim above quotes its probe inline (mover receipts on stdout, the gate's
own slice, `_gen_chain.py`'s generated footer, `_roll_state.py`, `_gate_doc_rows.py --check`).

REPLAY-THESE: `notes/_GAUGE-LOG.md` § `#### 2026-09-08 #258` (~1,400 tk, the rolled stratum) ·
`_CARRIES.md` § `residual → #260` (~100,000 tk — do NOT read whole; slice it)

---

## POST-COMMIT ADDENDUM (written by this same sub, in the same window)

Two commits landed, in the runbook's order:

- **`7b52cde`** — `after #259 2026-09-08 — capture ritual, DELEGATED wrap: the chart engine landed,
  two reds declared ruling-shaped` · 16 files, +596 / −159, staged BY PATH.
- **`0c232ea`** — `after #259 2026-09-08 — _memento-index.json rebuilt LAST after the #259 capture
  (ritual step 2g)` · 2,092 records, built from GM/LS AS COMMITTED.

Two declared gate acknowledgements were needed to stage: `SHOWROOM_ACK` (the ≈66 stale pages the
brief forbids regenerating) on both commits, and `knowledge/_graph-mention-map.json` regenerated by
the commit script's own mention-map gate and staged with each.

Final wrap gate after 2g: **198 in scope · 2 fail · 14 warn** — the retrieval-index fail CLEARED;
what remains is the #255 ceiling breach (the arm's refusal, recorded not routed around) and the
#259 stratum key collision (Dave's). Step 4c ran: session-owned `/tmp` scratch removed; 3M of
dead-session orphans reported as unremovable from inside. NOT PUSHED — Dave pushes via GitHub
Desktop.
