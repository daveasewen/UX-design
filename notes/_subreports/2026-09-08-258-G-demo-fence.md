# 258-G — the demo fence in the SKILL and in the gate (s258-D3), no snippets touched

Lane G. Four files, all outside `knowledge/snippets/` — lanes A/B/C's territory was not entered
(`git status --porcelain knowledge/snippets/` was never mine to change; I opened no snippet).

## Files changed

| file | what |
|---|---|
| `apollo-spider/skills/generate-from-canon/SKILL.md` | rule 2 (end, before 2a): the ⛔ "never copy inside a fence" clause, both marker forms, and the named refusal · rule 2a: one clause, the fenced `<script>` is harness and is never the declared `behaviour.script` bytes · Procedure step 3: "drop every `APOLLO-DEMO` fenced span on the way in" |
| `knowledge/_validate_receipt.py` | docstring § WHAT IT CHECKS gains **3b**; `DEMO_FENCE_RE` / `DEMO_FENCE_NAME_RE` beside the splice-marker primitives; the check in `check()` immediately after the REGION-UNRECEIPTED loop; selftest arms **AA–AE** |
| `knowledge/_RUNBOOK-compose-from-canon.md` | § Compose a screen, step 3: the three-marker grammar table (`AUTO-*` injected / `APOLLO-SPLICE` spliced / `APOLLO-DEMO` showroom-only) + the unfenced-var rule |
| `apollo-spider/skills/check-with-gates/SKILL.md` | one clause on the receipt-gate row (see NOT DONE — it lists exit codes, not FAIL codes) |

## The gate clause, as written

`check()` now ends:

```
    # 3b (s258-D3): the showroom harness was copied in with the component. Marker-string only.
    if DEMO_FENCE_RE.search(html):
        ...
        fails.append("DEMO-CHROME-COPIED")
        lines.append("FAIL:DEMO-CHROME-COPIED — the page carries fenced snippet harness (%s); "
                     "the fence is showroom-only (width dials, state switchers, demo labels and "
                     "their script), re-splice without it — every APOLLO-DEMO span deletes and "
                     "the component still renders" % named)
```

`DEMO_FENCE_RE = re.compile(r'APOLLO-DEMO[^\n]*(START|END)')` — the brief's regex verbatim.
Deliberately run over the **raw** `html`, not the comment-masked text: the marker *is* a comment,
so masking would hide exactly what is being looked for. `DEMO_FENCE_NAME_RE` only names the
`<what>` values in the message (first six, then `…`); it is not the decision.

## Acceptance — run, quoted

```
python3 knowledge/_validate_receipt.py --selftest
  … A … Z2 all ✅ (41 pre-existing arms, unchanged)
  ✅ AA no APOLLO-DEMO marker anywhere — clean page still PASSES (the fence is not a tax) -> PASS | unproven=1
  ✅ AB the page carries a fenced HTML harness span copied from the snippet -> DEMO-CHROME-COPIED | unproven=1
  ✅ AC the harness came in as CSS/JS comment form inside a <style> — same fence, same fail -> DEMO-CHROME-COPIED | unproven=1
  ✅ AD a HALF fence (END marker only) is still harness bytes, still named -> DEMO-CHROME-COPIED | unproven=1
  ✅ AE the words 'demo' and a splice fence alone do NOT trip it (no false positive) -> PASS | unproven=1
SELFTEST: PASS ✅        EXIT=0
```

46 arms, all green. AB is the **bite** ([[mutation-tests-the-clause-not-the-feature]]: each arm
drives `check()` on a real temp file); AE is the false-positive guard — `class="demo-note"` and the
word "demo" in prose do not red, so the fence is a marker check and not a word hunt.

Two more runs:

```
python3 knowledge/_validate_receipt.py --help                       exit 0 (help gate intact)
python3 knowledge/_validate_receipt.py dashboards/international-banking-dashboard.regen-v2-receipt.html --quiet
  verdict: PASS (+2 UNPROVEN)      RESULT: PASS ✅      exit 0
```
That page is the repo's only live receipted page; it stays green, so 3b costs nothing to work
already done. `grep -rl "APOLLO-DEMO" --include=*.html .` outside `knowledge/snippets/` → **0 files**,
so the new refusal reds nothing that exists today.

## NOT DONE (and why)

1. **`check-with-gates/SKILL.md` does not list the FAIL codes.** The brief made that edit
   conditional. Its § "Reading a verdict" grades *exit codes* (0 / 1 / 77 / 2), not named
   failures, and there is no FAIL-code table anywhere in the file. I added a single clause to the
   receipt-gate row of the "your question → run" table instead, so a reader who meets
   `DEMO-CHROME-COPIED` can place it; I did not invent a FAIL-code table.
2. **No pack re-bake, no version bump.** `apollo-spider/skills/` is the SOURCE; `apollo-spider/dist/`
   holds the baked releases and v1.0.7 is out. Two skill sources changed today, so the next bake
   must go out as a NEW version — re-baking a zip at v1.0.7 with different bytes is the laundering
   the frozen-gate ruling names. That is the conductor's call, not mine; I ruled nothing.
3. **Older copies left alone**: `designer-skills-v1/`, `designer-skills-v2/`, `_to_delete/…` all
   carry their own `generate-from-canon/SKILL.md`. The brief named the `apollo-spider` path only.
4. **No snippet touched, no `_validate_screen.py`, nothing under `knowledge/_screen-gate/`, no
   `_build_all.py`, no git operation.** Playwright was not needed: lane G has no render to compare.

## Consequence worth naming for the conductor

The gate now reds any generated page carrying the marker — including one built from a snippet
lanes A/B/C are fencing *right now*. That is the intent (the fence exists so the harness is
refusable), but it means the order matters: a page composed from a fenced snippet by a copier that
has not read the new rule 2 will red rather than silently ship the dial. That is the defect the
cold run found, made visible.

## Tokens

I have no `message.usage` reader inside a delegated lane, so I will not quote a FILL number I did
not measure — UNPROVEN. What I can say mechanically: 5 file reads (brief, probe, SKILL.md, two
slices of `_validate_receipt.py`, the runbook slice), 4 greps, 5 shell runs, 7 edits, 1 write. Small
lane; no seam check-in was due inside it.
