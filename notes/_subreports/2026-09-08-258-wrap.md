# `#258`-`wrap` — the delegated capture ritual for #258

session: `#258` · 2026-09-08
window: conductor **Fable** (16 Opus 5 lanes) → this OPUS 5 delegated wrap sub
sub index: `wrap`
brief: `notes/_briefs/2026-09-08-258-delegated-wrap-brief.md`
tokens: `UNMEASURED — a delegated sub cannot read its own message.usage` (the conductor's
transcript WAS measured first-hand at this seat: FILL 197,766 real over 100 turns, boot 68,826)

## VERDICT

Every step of `knowledge/_RUNBOOK-capture-ritual.md` ran — 1, 1b, 2, 2c, 2d, 2e (declared with
its size), 2f, 2g, 3 (the conductor's, 0 bytes at this seat), 4, 4b, 4c, 5. The four rulings
`s258-D1`…`s258-D4` were READ BACK from `knowledge/_rulings.json` (store 398 → 402, entries
399–402, each `by` Dave · `date` 2026-09-08 · `status` ruled) and **none was re-inscribed**. The
`--wrap` form of the capture gate is REFUSED for the TWELFTH wrap running by the boot-drift
ceiling arm — #255's 70,127 and #256's 70,241 stand over the shrink-only 70,000 literal even
though #257's 68,837 and #258's own 68,826 are both under it — so the literal was NOT moved and
the commit was made in the #243 form, with the refusal recorded rather than routed around.

COUNTS: findings 6 · ruling-shaped 3 · UNPROVEN 2

## What was done

- **1 / 1b** — `_LIVE-STATE.md` refreshed (new `Last refreshed` for #258, new ⏱ LATEST delta);
  dossier `_DECISION-HISTORY/2026-09-08-258-five-cold-runs-and-the-sentence-2a-forbade.md`
  written whole, with `provenance:`/`status:` lines. `knowledge/_REVIEW-SIGNOFF.md` **+1 row**
  naming all five `reviews/COLDRUN-258-2026-09-08-v*.html` paths.
- **2 / 2c / 2d / 2f** — every move through `python3 knowledge/_gm_move.py --ops <session-owned
  ops file>`, batch headers created FIRST: 2c **11 ln** (#256 banner) → `_GM-ARCHIVE.md`
  § Batch 2026-09-08 #258 · 2d **17 ln** (#255 delta) + its `Previous:` chain segment →
  `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-08 #258 · 2f **13 ln** → `notes/_GAUGE-LOG.md` and
  **3 ln** → `_GM-ARCHIVE.md` under the #258 key. New ★ LATEST banner: **10 substantive lines /
  1,193 tape**, inside the `s241-D2` S1 cap of 10 / 1,200.
- **2e** — SKIPPED, size declared: `## ⬛ DO THIS FIRST` measures **77 raw lines**, `#### ` scan
  returns **0**.
- **2g** — `knowledge/_build_memento_index.py` rebuilt LAST, after every GM/LS edit was final.
- **3** — the CONDUCTOR's, before the brief was cut (`one-shot-series-and-2a-removed-258.md` +
  one `MEMORY.md` line). **0 bytes at this seat**; the store is invisible here (D1a).
- **4 / 4b** — rulings read back, not written; both title lines returned in chat and the forward
  title written to the top of `GOOD-MORNING.md`.
- **4c** — `knowledge/_gate_scratch_hygiene.py --clean` run: this session's own `/tmp` scratch
  removed while its owner still exists.
- **5** — one wrap commit in the #243 form, then `--push`.
- **`_CARRIES.md`** — `## residual → #259` written straight in under `s225-D2` (i): **475** age
  brackets bumped in ONE programmatic pass, **2** surgical edits (one `s183-D1` strike, one
  `s188-D2` correction), **8** new items prepended, probeable count **339**.

## Findings

1. **The carry struck at this seat is the one the session was named for, and only HALF of it may
   be struck.** `→ #258` item ① bundled *the frozen prompt's cold run* with *his rulings page*.
   The cold-run half is dead — five blind runs, each verified by driving, `reviews/COLDRUN-258-
   2026-09-08-v1.html`…`-v5.html`, commits `0b8c0c7`·`68e5c25`·`c9bf745`·`c7068a2`. The
   rulings-page half is untouched and carries forward for a third session at a store of 402.
   A compound carry must not retire on the strength of the half that moved.
2. **`notes/_GAUGE-LOG.md` had no `#### 2026-09-08 #257` block until this wrap rolled one.**
   `grep -c` read 0 before 2f and 1 after; `gauge_log_continuity` (BLOCKING) would have refused
   the wrap otherwise. The stratum was sitting unrolled in `GOOD-MORNING.md` §C, which is 2f's
   job and not a HOLE.
3. **`## ⬛ DO THIS FIRST` now measures 77 raw lines, a THIRD figure for the same section.**
   #252/#253/#255/#256 measured 109; #254 measured 70. All three are reported; none is averaged
   into another [[measure-dont-convert-units]].
4. **`_memory_cap_check.py` is OVER CAP at this seat and was not repaired.** `MEMORY.md` reads
   **1,867 tape against the 1,802 cap, +65** — every session pays it at boot. Repair is ritual
   step 3, which the brief assigns to the conductor.
5. **The doc-row gate found 18 unrowed documents from this session** (3 lane briefs, 15 filed
   reports) before this wrap rowed them — the forgotten-document class, caught by its own gate.
6. **The conductor's FILL estimate was half the measurement (≈95K vs 187,384) and the cause is
   structural, not careless:** sixteen lanes' RETURNS are the FILL. Reported as the brief
   instructs — stated, not argued.

## RULING-SHAPED QUESTIONS

1. **`_validate_screen.py` writes its ledger into `knowledge/_screen-gate/` keyed on BASENAME
   and clobbered tracked files four times today** — option (a) key on content hash or a run id,
   (b) refuse to overwrite a tracked row, (c) move the directory out of the tree entirely.
   Recommend (b) first because it is the smallest change that makes the damage impossible, with
   (a) behind it; ⛔ but a gate's write policy is Dave's, not a wrap's.
2. **What to do with the boot-ceiling arm now that TWO consecutive readings stand under the
   literal** (68,837 · 68,826) while two historical breaches keep it refusing — option (a) leave
   it (the `--wrap` form stays refused indefinitely), (b) re-base the arm on a window of the
   last N readings, (c) his word to clear the two breaches. ⛔ The literal is shrink-only and
   raising it is never a price a wrap pays to unblock itself; no recommendation is offered
   beyond naming that (a) is the status quo and costs a twelfth refusal.
3. **`check_screen` counts hex literals inside CSS comments** — a one-line `strip_css_comments`
   fix, unordered and unscheduled. Not ruling-shaped in substance, but it is unowned, which is
   the state that makes small fixes immortal.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** every cold-run score, gate verdict, commit hash and pixel figure in this
  session's record is **CLAIMED from the fifteen filed lane reports and the wrap brief**, not
  re-driven at this seat — price to prove: five browser runs plus two gate suites, far beyond a
  wrap's budget. The commit *subjects* and the 10-commit range WERE re-read from `git log`.
- **UNPROVEN:** the `subs ≈2,600,000 (n=16)` figure is a conductor-DECLARED harness total. This
  seat cannot read `subagent_tokens` — price to prove: none available from here.
- **CLAIMED:** memory step 3 (the hook and the `MEMORY.md` line) is the conductor's word; the
  store is invisible to this seat by D1a and nothing about it is asserted here.

## Evidence

No evidence files: every claim above quotes its probe inline (mover receipts on stdout, gate
output, `git log`, `_capture_gate._carry_items()`).

REPLAY-THESE: `_DECISION-HISTORY/2026-09-08-258-five-cold-runs-and-the-sentence-2a-forbade.md`
(~3,400 tk) · `_CARRIES.md` § `residual → #259` (~100,000 tk — ⛔ do NOT replay whole; fetch
`carries:residual-259` only when a body is needed)
