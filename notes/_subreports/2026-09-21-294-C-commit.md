# 294 · C — the mid-session commit: `dd374131`, zero refusals, first run

session: `#294` · 2026-09-21 · lane `C` · model Opus 5 (1M) · conductor Fable 5.1
job: ONE mid-session commit, no push.

## THE SHA

**`dd374131fd569803ff42ed118f3b4ad9dfb6c768`** — `master`, 2026-09-21 16:40:28 +0100,
**14 files changed, 3,975 insertions, 2 deletions**, 9 new files.

Subject as generated by the script's T3 headline (exactly one prefix, asserted by the #208 gate):

> `after #294 2026-09-21 — #294 — the judgement-only review page is built and he took all twelve recommendations; lane J…`

Invocation:

```
SESSION_N=294 bash knowledge/_git_commit.sh --reconciled \
  knowledge/_tmp/msg-294-C-commit-1638.txt <15 named paths>
```

Fresh msgfile, fresh name, under the gitignored `knowledge/_tmp/`; line 1 carried the subject with
**no** `after #N …` prefix — the script adds it.

## THE STAGED PATHS — FIFTEEN NAMED, `add -A` NEVER USED

Lane R (doc row `W-294r`):

- `notes/_lanes/294/R/review-page.html`
- `notes/_subreports/2026-09-21-294-R-review-page.md` (includes the R2 addendum)

His words:

- `notes/_lanes/294/DAVE-RULINGS-2026-09-21.md` — twelve rulings taken in chat, verbatim

Lane J4, the #293 Jev lane picked up here (doc row `W-293j4`) — `__pycache__/` EXCLUDED (gitignored,
never staged):

- `notes/_lanes/293/J4/rerank.py`
- `notes/_lanes/293/J4/rerank-results.md`
- `notes/_lanes/293/J4/fixture.jsonl`
- `notes/_lanes/293/J4/rerank-raw.json`
- `notes/_lanes/293/J4/rerank-raw-global.json`
- `notes/_subreports/2026-09-21-293-J4-memento-rerank.md`

The machine appends, NAMED in the commit body:

- `knowledge/_jev-receipts.jsonl`
- `notes/_REHEARSAL-LOG.jsonl`
- `notes/_dream/_GRADE-DECISIONS.jsonl`

⚠ **The body makes no byte-unchanged claim about `notes/_REHEARSAL-LOG.jsonl`** — the commit's own
gates append to it during the run, so the body says *"appended to by THIS RUN'S OWN GATES"*. That is
true at commit time; a byte-unchanged claim would not have been.

Store and chain:

- `knowledge/_state.json`
- `_CHAIN.md`

## THE DOC ROWS — MINTED BEFORE THE FIRST COMMIT ATTEMPT

Both through `_state.add()`, each with a stated close condition. **761 items → 763.**
`_state.check()` returned ok with 0 fails before `save()`.

| id | home | closes_when (abridged) |
|---|---|---|
| `W-294r` | `notes/_subreports/2026-09-21-294-R-review-page.md` | Dave has said whether the twelve rulings he took in chat are inscribed as `s294-D*` records — he ruled but did not say *inscribe* |
| `W-293j4` | `notes/_subreports/2026-09-21-293-J4-memento-rerank.md` | Dave has ruled whether the retrieval gap J4 measured is worked on at all — the re-rank is FLAT and not to be wired on this evidence |

⚠ **`_state.add()` REFUSED `W-293j4` once, exactly as designed, and that refusal is paid below.**
Shape copied from `W-293j3` (`46a55895`): `state: open` · `owner: dave` · `project: memento` ·
`opened: <the lane's own session>` · `condition: stated` · `home` = the subreport · `links` = the
lane's artefacts plus the sibling subreport.

`_CHAIN.md` regenerated with `knowledge/_gen_chain.py`; `--check` returned
**`✅ _CHAIN.md is FRESH — byte-matches the live chain`**, 11,669 tiktoken cl100k_base, fixed point
in 2 passes. `pip install tiktoken --break-system-packages` ran first (already satisfied).

The post-staging doc-row gate: **population 537 · staged-in-THIS-commit 2 · unrowed 0 · ✅ PASS.**

## REFUSALS PAID — ONE, VERBATIM, AND IT WAS THE STORE GATE

The commit script itself refused **nothing**: `_git_commit.sh` exited 0 on its **first** run. Three
sessions running, the doc-row gate was anticipated rather than met.

The one refusal, from `_state.add()` on the first mint attempt, verbatim:

```
_state.StateError: REFUSED: W-293j4: missing required field(s) closes_when
```

Cause: `W-293j4` was drafted without a `closes_when`. Fix: the condition above was written and
stated; nothing was weakened and no gate was touched. The refused item was removed from the doc by
`add()` itself and nothing was saved by the refused run.

⚠ **The wrap gate read RED — visible, not blocking**, because this commit is DECLARED not-a-wrap
(`#74-D1`): `capture gate [wrap]: 231 in scope · 7 fail · 29 warn`. **Not a refusal and not paid:**
the script's own line is *"The session's FINAL commit must run with `--wrap`, where red BLOCKS."*
It is carried to whoever runs the wrap.

## THE LOCK'S FATE — `mv`'d ASIDE, NEVER `rm`'d, AND THE CROSS-DEVICE WRINKLE IS ON THE RECORD

A **0-byte `.git/index.lock` dated 16:03**, stranded by another session's lane J4 (report filed
16:09, nothing written since 16:20) — STALE, and the lane's own report says it never touched git.
Rule `[[git-lock-mv-not-rm]]` obeyed.

⚠ **The brief's destination could not take the original, and the reason is structural, not a
permission:** `/sessions/great-dreamy-noether/` is `/dev/nvme1n1` and the repo is its own mount
(`/proc/self/fd/3`), so `mv` across them is copy-then-unlink and the sandbox delete-guard refuses
the unlink inside `.git`:

```
mv: cannot remove '.git/index.lock': Operation not permitted
```

⇒ **Two artefacts, both 0 B, and neither is a `rm`:** the copy landed at
`/sessions/great-dreamy-noether/stale-index.lock-294-c` (outside the repo, as briefed), and the
**original was renamed on-device** — a true rename, no unlink — into the gitignored orphan-lock
directory the runbook's own choreography uses:
`notes/_lanes/_orphan-locks/stale-index.lock-294-c`. Both are named in the commit body.
**The per-session delete grant was never requested and never used.**

⛔ **No live lock was ever touched.** No new foreign lock appeared during the run. The locks that
respawned afterwards were git's own, created by **my** `git status` / `git log` calls — the
documented respawn (`_RUNBOOK-git-commit.md` step 4) — and each was `mv`'d to
`_to_delete/_stale_locks/`. The script's closing line: **`✓ done — locks clear, safe for Dave to
push via GitHub Desktop`**.

⚠ **One respawned 0-byte `index.lock` exists at the close of this lane**, created by the verification
`git status` that this report quotes; it is `mv`'d aside as the last action, and any later git
command in this session will strand another. Nothing is stranded that a subsequent `mv` cannot clear.

## `_rulings.json` — UNCHANGED, AND THE COUNT

`json.load` at commit time, **ids and counts only, no record printed**:
**622 ruling records** under `rulings` (plus a 20-key `_README`). Not staged, not dirty, not in the
commit, and **no `s293-` or `s294-` id exists in it**. Last touched at `75a490cd` (#287).
Dave ruled twelve items in chat and did **not** say *inscribe*; the twelve live in
`notes/_lanes/294/DAVE-RULINGS-2026-09-21.md` as his word on the record.

## AFTERWARDS

`git status --short` → **clean** (empty). `git log -1 --stat` → the 14 paths above, 9 created.
**NOTHING IS PUSHED** — no remote consulted, no `git ls-remote`, no CI run claimed on `dd374131`.

## ⚙ THE FILL LINE AT CLOSE

`python3 knowledge/_checkin.py --window 200000 --no-block`, taken at the close of this lane:

> **FILL 163,311 real · peak 163,311 real across 17 turns · ratio 82% of the 200,000 passed**

---

## ⚙ THE THREE MACHINE-READ LINES — ADDED BY LANE C2 AT STAGING, AND SAID SO

⚠ **Lane C filed this report at 16:40, BEFORE lane B wired `s294-D2` later in the same session, so
it carried none of the three lines `s218-D7` requires** — and lane B's finding 7 names this file as
one of the three the new gate fires on. Lane C2, which stages it, added the lines rather than
exempting the file by name. The figures are **C2's count of THIS report's own text**, derived and
shown, never invented:
**findings 9** = the commit script exited 0 on its first run · `_state.add()` refused `W-293j4` for
a missing `closes_when` · the wrap gate read `231 in scope · 7 fail · 29 warn` · a 0-byte
`index.lock` dated 16:03 was stranded by another session's lane J4 · the destination is a different
device, so `mv` is copy-then-unlink and the sandbox delete-guard refuses the unlink inside `.git` ·
`_rulings.json` held 622 records with no `s293-`/`s294-` id, last touched at `75a490cd` · the locks
that respawned afterwards were git's own, created by this lane's `git status`/`git log` · the
doc-row gate read `population 537 · staged-in-THIS-commit 2 · unrowed 0` · `_CHAIN.md` measured
11,669 tiktoken cl100k_base, fixed point in 2 passes.
**ruling-shaped 2** = the two items listed under the heading below.
**UNPROVEN 0** = the report files no UNPROVEN section and makes no claim it did not measure; its
one explicit non-claim (*"no CI run claimed on `dd374131`"*) is a declared absence, not an unproven
assertion.

COUNTS: findings 9 · ruling-shaped 2 · UNPROVEN 0

REPLAY-THESE: `python3 knowledge/_gen_chain.py --check` · `python3 knowledge/_capture_gate.py --wrap` · `python3 -c "import json; print(len(json.load(open('knowledge/_rulings.json'))['rulings']))"` · `git log -1 --stat dd374131` · `git show --stat dd374131`

## RULING-SHAPED QUESTIONS

⚠ **Lane C raised no such section of its own.** The two below are the items its BODY carries
forward rather than settles, quoted from it and not invented here: (1) **the wrap gate is red at 7
fails and is "carried to whoever
runs the wrap"** — the body declares it *"Not a refusal and not paid"* under `#74-D1`, so whether
the seven (the boot-ceiling breach plus six `_GAUGE-LOG.md` double-counts) are cleared, declared or
re-dialled is Dave's; (2) **one respawned 0-byte `index.lock` exists at the close of this lane and
"any later git command in this session will strand another"** — whether the orphan-lock directory
`notes/_lanes/_orphan-locks/` needs a purge ritual of its own, on a mount that refuses unlink, is
ruling-shaped and the body stops at naming it. ⛔ No question was attributed to lane C that its own
text does not carry.
