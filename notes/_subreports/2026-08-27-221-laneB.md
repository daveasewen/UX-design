# `#221`-laneB — GENERATORS: the repair wave's generator lane

session: `#221` · 2026-08-27 · **Model:** Opus · **Lane:** B of the #221 repair wave
brief: `notes/_briefs/2026-08-27-221-repair-wave-divvy.md` §LANE B + GLOBAL FENCE + GLOBAL PITFALLS
sources: `notes/_subreports/2026-08-27-220-audit-L2.md` · the mechanical findings of
`notes/_subreports/2026-08-27-220-audit-L3.md` · `notes/_subreports/2026-08-27-220-replay-discharge.md`
HEAD at open: `9dbf413` (the audit measured `707e5aa`; **every premise below was replayed at
`9dbf413` before it was touched**, and every one still reproduced).

## COUNTS

```
items taken                    16
fixed-at-cause                  9   distinct causes (19 literal cause-sites among them)
bites added                    20   (11 of them a NEW advisory instrument, born advisory)
mutations driven BOTH ways      6
declined-with-reason           14
UNPROVEN                        4
```

**No git. No `_rulings.json` write. No promotion. No `_state.json` row. No `_build_all.py`.
No ruled VALUE moved. `DataViz-interactive.html` untouched.** Every fallback repair re-derives a
value canon itself resolves; not one is a new number.

---

## FINDINGS

### ① `gen_dashboard.py --check` — the FAIL path is REACHABLE, and all three verdicts are driven

**Replayed first.** At `9dbf413`, `--check` returned **rc=77** with the same cause L2 named.
Isolated to one variable — the scraper's candidate set was **exactly one token**:

```
CANDIDATES: 1
   '_inscribe_ruling.py' <- provenance
UNTRACKED TODAY: [('provenance', '_inscribe_ruling.py')]
```

**Cause.** `untracked_inputs()` took a **bare basename** out of a gate's prose and asked git about
it as a **root-relative path**. `git ls-files -- _inscribe_ruling.py` is empty; the file lives at
`knowledge/_inscribe_ruling.py`. Gates name their siblings by bare name as a matter of course, so
the scraper classified a tracked file as untracked, `mismatch_verdict()` refused on **every** run
where the page differed, and its FAIL branch was unreachable — a BLOCKING build step that could
not accuse its own artefact. [[instrument-without-a-consumer]]

**Fixed at cause** (`knowledge/gen_dashboard.py:210-247`). A token carrying a slash keeps the
exact-path question; a bare token is asked *"is a file of this NAME tracked anywhere?"* via a
second pathspec (`*/<name>`), and is only reported when the answer is no. **The refusal's clause is
unchanged** — an `outputs/…` path a checkout cannot have still refuses.

**All three verdicts, driven, not asserted:**

| state | rc | how |
|---|---|---|
| committed page, fix in place | **1** | `--check` against the stale page — *the branch that did not exist* |
| planted untracked path in a real gate's output | **77** | mutation, one line added to a live gate result |
| after regeneration | **0** | `--check OK — dashboard/index.html in sync` |

```
WITH planted untracked path  -> rc=77  (77 = COULD-NOT-ASK)
REAL gate output, unmutated  -> rc=1   (1  = FAIL, the branch that was unreachable)
untracked_inputs(real) = []
```

**Three new bites** (`refusal_selftest`, DIRECTION 3): a tracked bare basename is not an untracked
input · a mismatch naming only a tracked sibling FAILS (1) and never refuses · a bare basename
tracked **nowhere** is still detected, so the clause survives. `--selftest` reads **32 arms, all
GREEN**.

⚠ **`gates.yml:54-55` is stale about this step and I did not edit it** — it attributes step `[59]`'s
77 to `_governs` naming `outputs/…`, while the live cause was the bare basename. That file is
**Lane A's region**; handed over below.

### ② The dashboard is regenerated, and the serial question was answered by probe, not by habit

**Which serial?** The ordered regen serial is fed by `knowledge/canon/`, `knowledge/snippets/`,
`knowledge/tokens/`, `knowledge/components/`. Probed, not assumed:

```
git status --porcelain -- knowledge/canon/ knowledge/snippets/ knowledge/tokens/ knowledge/components/
  → EMPTY
```

So the canon/token serial is **not entered** by this lane. What my §④ edits *did* stale is this
family's own ordered serial, and that was run **whole, in order**:

```
[1] gen_bento_matrix_217.py --rails   ⬛ EDIT-PASS RAILS written · rail [1,2,4,16,24,40] · 9 dial(s)
[2] gen_foundations_217.py            8 page(s) -> showroom/_foundations/ (8 written)
[3] gen_library_214.py    (index LAST) 143 component(s) — index + index.json BYTE-IDENTICAL, correctly
[4] gen_dashboard.py                  wrote dashboard/index.html (461,242 bytes)
```

Upstream members the dashboard **bakes** were confirmed in sync before it ran
(`gen_showroom --check OK — 135 page(s) + index`, `gen_theme_cascade --check OK — 230 override
path(s), 387 component projection(s)`).

The page moved from L2's measured `#219 / 194 items / 115 open` to **`session #221` · `All 299` ·
`219 things are open`** — matching the brief's "store now ~299 items".

⚠ **Declared, and it is the pre-existing design, not something I changed.** This page bakes LIVE
gate output. `outputs/` is git-**ignored** and present on this mount, so `_governs.py --selftest` is
green here (`0 checks fail across 0 rulings`). In a clean checkout those evidence files are absent,
`_governs` prints `outputs/…` FAIL lines, and `--check` will correctly return **77** there. That is
`s172-D1`'s clause working, and my fix preserves it — the mutation above proves the `outputs/…`
shape still refuses.

### ③ `gen_library_214.py` — the Linux case-sensitivity class, fixed at cause, with a both-platform bite

⚠ The file is at **`knowledge/_render/gen_library_214.py`**, not `knowledge/` as L2's report
addressed it.

**Premise replayed, and the mount is a trap worth naming:** this Linux sandbox mounts Dave's APFS
folder **case-INsensitively** (`os.path.exists('…/chart-boxplot.meta.json')` and
`'…/Chart-boxplot.meta.json'` are both True). So the defect cannot be reproduced in place. It was
reproduced by copying the metas to a genuinely case-sensitive directory and changing **one
variable**:

```
REPO META      no_meta: []                          | unfiled: []
CASE-SENS META no_meta: ['chart-boxplot', 'chart-bullet', 'chart-butterfly-h',
                         'chart-butterfly-v', 'chart-candlestick', 'chart-histogram',
                         'chart-scatter']            | unfiled: (the same 7)
ROWS THAT DIFFER: 7
    ('chart-boxplot', 'pattern' -> 'unfiled', blurb 117 -> 0)   … and six more
```

Exactly L2's seven, exactly its levels and blurb lengths.

**Fixed at cause — the REFERENCE is normalised; no file was renamed.** New `_meta_index()` /
`resolve_meta(slug)` resolve exact-case first, then case-insensitively, and return
`exact_case=False` so the caller **declares** rather than skips. `collect()` gains
`residuals["meta_case"]`, printed by both `--selftest` and `report()` — the silent
enumerate-and-skip is now a named residual.

**The bite that fails on a casing mismatch on BOTH platforms** — because it compares strings from
`os.listdir` (case-preserving everywhere) rather than asking `os.path.exists` (which answers
differently on APFS and ext4). Three new bites:

- **39** — a case-sensitive filesystem yields the **identical** index. Driven against a real
  case-sensitive scratch dir, `_cs_rows == rows`, `no_meta == []`.
- **40** — ⬛ MUTATION: with the pre-#221 exact-path lookup restored, the same run demonstrably
  loses those rows to `unfiled`. The bite is falsifiable, not merely green.
- **41** — the ADVISORY `--casing` arm reaches **both** verdicts on this platform: a lowercased
  mirror clears it, the repo's own metas fire it.

**New `--casing` arm, ⬛ ADVISORY at birth and it says so in its own docstring.** Not in
`_build_all.py`, not in `gates.yml`. Driven on real data both ways:

```
DIRTY (real tree)          rc=1   ⚠ 7 slug(s) resolve only case-insensitively (each named)
CLEAN (lowercased mirror)  rc=0   ✅ every showroom slug has an EXACT-CASE meta
```

⛔ **One bug this caught in my own work, recorded because it is the lane's own risk class:** the arm
first `return`ed its verdict, and `main()`'s return value is discarded at the bottom of that file —
it printed RED and exited **0**. An [[instrument-without-a-consumer]] I created and then drove.
Fixed to `sys.exit(casing_arm())`. **Found by driving it, not by reading it.**

`--selftest` **41 bites, rc=0**. `--check` **OK — 143 component(s), index + index.json + stub in
sync** (the artefact is unchanged on this mount and is now identical on Linux — that is bite 39).

**Whether to rename the 7 files stays Dave's** (L2's Q2) — see RULING-SHAPED §Q1.

### ④ `SPACING_STOPS` — ONE home + an address, and the lying comment is retired

**Premise replayed:** both homes still present at `9dbf413`, and
`gen_bento_matrix_217.py:189-190` still claimed `RULED_SPACING_RAIL` was *"the only place they are
typed"* while `role_defaults_219.py:92` typed the same six ruled numbers as **strings**.

**Import direction checked before choosing, per L2's cycle warning** — measured, not guessed:
`role_defaults_219` does not appear anywhere in the matrix's module-level import chain
(`gen_bento_roles_217`, `gen_gallery_compare_217`); the matrix imports it only inside `selftest()`.
Both import orders were driven: **0.01s each, no cycle**. So no third module was needed.

**ONE HOME = `gen_bento_matrix_217.RULED_SPACING_RAIL`** (it carries the ruling's own words).
`role_defaults_219.py` now **addresses** it and types nothing:

```python
from gen_bento_matrix_217 import RULED_SPACING_RAIL      # the ONE home of the six
SPACING_STOPS = [str(s) for s in RULED_SPACING_RAIL]
assert SPACING_STOPS == ["1", "2", "4", "16", "24", "40"], (…)
```

⛔ **No value moved.** `role_defaults_219 --selftest` re-derives
`ruled rails ['1', '2', '4', '16', '24', '40']` — character for character the reading
replay-discharge item **B1** recorded.

**The false clause is struck** and replaced with what actually happened, so the next reader is not
misled the way #219→#221 were.

**`R4c` upgraded from an equality check to a DERIVATION bite, mutation-driven.** An equality bite
over two typed literals cannot tell a derivation from a coincidence — which is precisely how the
two homes agreed for two sessions *while being two homes*. It now asserts: the foreign rail equals
the derived string form · the six numbers are **not re-typed** in that file's source · the
derivation expression **is** in it · and — the arm that cannot be faked — **widening the one home
makes the other module REFUSE on reload**:

```
clean derived rail:   ['1', '2', '4', '16', '24', '40']
MUTATION (7th stop into the ONE home): REFUSED, by name ->
    s219-D1(4): the ruled spacing rail moved under this module — ['1','2','4','8','16','24','40']
restored derived rail: ['1', '2', '4', '16', '24', '40']
```

⚠ **A trap worth inscribing:** my first version asserted `_rd219.RULED_SPACING_RAIL is
RULED_SPACING_RAIL` and went red for a reason that has nothing to do with the rail — under
`--selftest` the matrix is `__main__`, so `role_defaults_219`'s import binds a **second module
object** and every identity comparison is False. Replaced with equality + source probes + the
reload mutation. **An `is` check across a `__main__`/module boundary is a false red waiting to
happen.**

`gen_bento_matrix_217 --selftest` → **OK — 104 bites**. `gen_foundations_217 --check` → **8 page(s)
in sync**.

### ⑤ The 16 drifted fallback cause-sites — repaired, and the class gated. The gate then found **3 more**

**All 16 replayed at HEAD**, at the lines L3 named. Canon's own answers were re-measured through
the repo's own resolver (not a private cascade model) and match L3 exactly:

| token | canon resolves | old fallback | repaired to |
|---|---|---|---|
| `--focus-ring` | `#305A85` light · `#4587A7` dark, **all four themes** | `#1A1A1A` | **`#305A85`** |
| `--border-strong` | `#808080` mono/legacy/console · `#524842` supercharge | `#767676` | **`#808080`** |
| `--tertiary-background-hover` | `#F0F0F0` light · `#232323` dark · `#DFDEDC`/`#2E2A25` sc | `#F3F3F3` | **`#F0F0F0`** |
| `--surface-subtle` | `#F0F0F0` · `#1F1F1F` · `#DFDEDC` · `#2A2621` | `#F3F3F3` | **`#F0F0F0`** |
| `var(--line-2,…)` alias consumers ×5 | must equal the alias's own declaration | `#767676` | **`#808080`** |

⛔ **Not a ruled-value change, and the test was applied per site:** every old literal is a value
canon resolves in **no theme and no mode**; every new one is a value canon **does** resolve for
that exact token. The precedent is the tree's own — `s220-D1` made this identical repair once, in
`gen_foundations_217.py:912`, with its receipt. The sharpest instance was **five lines below that
receipt**: `--focus: var(--focus-ring,#1A1A1A)`, a **black focus ring where the ruled ring is
blue**, drifted in 8 of 8 theme × mode. Its own repair swept its line and not its block.

**The class fix: `knowledge/_render/_gate_fallback_drift_221.py`** — ⬛ **ADVISORY AT BIRTH, and it
says so in its own header.** It lifts bite 12's drift arm out of one module and puts a **glob**
under it (`knowledge/_render/*.py`), because [[gate-glob-scope-rule]]: the rule was only ever as
wide as its gate's glob. Three verdicts, and the third is F-2's fix:

- **DRIFTED** — canon resolves the token, the literal is not one of canon's answers → RED
- **LOCAL-DRIFT** — an alias declared `--x: var(--canon,#A)` consumed as `var(--x,#B)` → RED
- **UNCHECKED** — canon resolves the token in **no** theme and **no** mode, so nothing is compared.
  Bite 12 swallowed the `KeyError` and the row vanished; **~40 radius / hit-area / ring-width
  fallbacks were never compared to anything**, with `0px` happening to be the house default —
  the shape #204 measured as invisible. It is now **counted and printed**, and it deliberately
  does **not** turn the gate red: a declared hole, not a pass.

⛔ **DRIVEN ON REAL DATA, AND IT CAUGHT WHAT I HAD JUST OPENED.** L3's F-1 warned in as many words:
*"the alias and its consumers must move in ONE motion."* My repair moved `--surface-2`'s
declaration to `#F0F0F0` and left three **consumers** at `#F3F3F3`. The gate's first live run:

```
⛔ LOCAL-DRIFT knowledge/_render/gen_bento_canon_217.py    :: var(--surface-2,#F3F3F3) — the alias declares #F0F0F0
⛔ LOCAL-DRIFT knowledge/_render/gen_bento_roles_217.py    :: var(--surface-2,#F3F3F3) — the alias declares #F0F0F0
⛔ LOCAL-DRIFT knowledge/_render/gen_gallery_compare_217.py:: var(--surface-2,#F3F3F3) — the alias declares #F0F0F0
❌ 0 drifted + 3 local-drift.
```

Eight further sites repaired. **So the real count is 19 cause-sites, not 16** — L3's sweep did not
carry the `--surface-2` consumers. Final run, whole glob:

```
_bento_recut_219.py        5 fallback(s) · 0 drifted · 0 local-drift · 5 unchecked
gen_bento_canon_217.py    14 fallback(s) · 0 · 0 · 0
gen_bento_matrix_217.py   21 fallback(s) · 0 · 0 · 0
gen_bento_roles_217.py    14 fallback(s) · 0 · 0 · 0
gen_foundations_217.py     8 fallback(s) · 0 · 0 · 0
gen_gallery_compare_217.py 15 fallback(s) · 0 · 0 · 0
gen_grids_218.py           6 fallback(s) · 0 · 0 · 6 unchecked
⬛ UNCHECKED: 11 fallback(s) … DECLARED, not passed
✅ every var() fallback literal in the glob is one of canon's own answers for its token.   rc=0
```

**11 bites, mutation driven BOTH ways** (planted `#1A1A1A` fires and clears; planted local-drift
fires and clears; the UNCHECKED bucket is asserted to name its tokens; and six bites assert **the
glob reaches each of the six generators**, because the bite that mattered here was reach, not
logic). The gate reaches **7** generator sources where bite 12 reached **1**.

### ⑥ A second, unnamed instance of the same class — the s220-D1 **gate upgrade** was never swept either

Not in any audit report. Found by driving `gen_foundations_217 --selftest` after the repair:

```
❌ 18 · every authored colour is a neutral — no red, no green, the two-red law unreached
     got:  (['#305A85'], False)
     want: ([], False)
```

`s220-D1` split bite 12 into **AUTHORED hexes** vs **var() FALLBACK literals**, and the split made
the gate *stricter*. That upgrade was made in **one** generator. `gen_foundations_217`'s bite 18 is
the **pre-split** form — it cannot tell an authored colour from a canon-resolved fallback, so it
read the **ruled** focus ring as an illegal chromatic authored colour and went red on the repair
that put the ruled ring back. **The same [[gate-glob-scope-rule]] miss as the fallbacks it was built
to catch, one layer up.**

**Fixed at cause** by applying the ruled s220-D1 split here too — this is not a weakened gate: it
gains an assertion it did not have (`fb_chromatic` must be exactly
`[("--focus-ring", "#305A85")]`). `--selftest` **OK — 44 bites**. The four sibling generators were
then driven and carry no equivalent unsplit bite (canon 8 bites · roles 9 · compare 8 · recut 8,
all rc=0).

The repair is visible in the shipped artefact:

```
showroom/_foundations/bento.html:  var(--focus-ring,#305A85) ×1 · var(--surface-subtle,#F0F0F0) ×3
```

### ⑦ Probe-registry rot — the fix is **no number**, not a corrected number

**Premise replayed.** Driven today on the default globs:

```
P-7 container self-query scan: 135 file(s) · 0 finding(s) · 0 WARN-tier   PROBE P-7 — findings=0
P-8 dangling-var TEXT scan:    135 file(s) · 0 finding(s) in 0 file(s)    PROBE P-8 — findings=0
```

against a README asserting *"P-7 6 findings + 3 WARN, P-8 58 findings in 9 files"* — **64 standing
findings that do not exist**, for nine sessions. Both were repaired at **#211**, in the artefacts
(`knowledge/snippets/Layout-utilities.reference.html:244`) and at the generator cause
(`knowledge/gen_token_ramp.py:28`), and **the registry was never told**. This is the exact class
**P-5** exists to find, sitting in P-5's own registry, outside P-5's population.

**"Where a figure should be generated rather than typed, generate it" — taken literally.** The
README no longer types any findings count. Its ONE home is a run, and the document carries the
**address** (ADR-0017), plus a **dated receipt** generated from the runner's own summary rather than
retyped from a report:

```
python3 knowledge/_probe_registry/_registry.py --run --survey
  → P-1 0 · P-2 0 · P-4 52 · P-5 0 · P-7 0 · P-8 0
  → P-3, P-6 COULD-NOT-ASK (playwright not importable) — a refusal, NOT a pass
```

The two manifest `note` fields say the same, and each names the #211 repair that closed it.

⛔ **Proved mechanically that no governance act occurred** — L3 declined this precisely because the
manifest is promotion evidence:

```
fields changed: [('P-7', 'note'), ('P-8', 'note')]
caught identical everywhere: True
comment header preserved: True
```

`_registry.py --list` → 8 probes, rc=0. `_registry.py --selftest` → **PASS, every planted manifest
defect named and its removal green (both directions)**. `_promote.py` runs and its `caught`
arithmetic is untouched.

### ⑧ Fork-ledger rot — a `--reconcile` arm that makes it self-checking, and a green line that states its own question

**Premise replayed exactly** — all five rows, same verdicts as L3:

```
--- LEDGER RECONCILE (ADVISORY, read-only, #221) ---
  declared   : 50 row(s)   measured today: 45 fork(s)
  LEDGER-STALE: --ring     declared FORK, measured BENIGN_MODE_AXIS
  LEDGER-STALE: --rule     declared FORK, measured BENIGN_THEME_AND_MODE_AXIS
  LEDGER-STALE: --scrim    declared FORK, measured UNIFORM
  LEDGER-STALE: --success  declared FORK, measured BENIGN_THEME_AND_MODE_AXIS
  LEDGER-STALE: --tail-x   declared FORK, measured UNIFORM
  ⛔ 5 declared row(s) no longer describe the tree. NOT pruned …
  undeclared forks in the tree today: none
```

Two of them are **UNIFORM** — there is nothing left to rule, and they have sat on Dave's queue
since #209 regardless.

⛔ **The ledger was NOT written** (`$do_not`: *"No script may add to this file automatically"*).
Proved: `md5sum -c` before/after → **OK**. Pruning the rows is Dave's; **re-stamping `$measured`
without re-running would be the carried-figure defect the arm exists to expose**.

**And the misleading green is now honest** (L3's F-3, its zero-risk half):

```
GATE GREEN: no UNDECLARED fork. ⚠ This did NOT ask whether forks exist: 98 fork(s) stand and are
declared in the ledger. Run --strict for the full list, --reconcile for ledger rows the tree no
longer agrees with.
```

⛔ **Nothing about the wiring, the thresholds or any fork value moved**, and all three verdicts are
unchanged: `baseline rc=0` · `--strict rc=1 (GATE RED: 98 fork(s))` · `--selftest PASS`.

### ⑨ TRIAGE TABLE — the 98 forks, classified by a stated rule

Derived from `--strict --json` (written to `/var/tmp`, **not** the repo). 98 instances over **45
distinct names**. Each row is classified by a **measurable** rule, named beside the bucket, not by
opinion:

| # | bucket | rule applied | count |
|---|---|---|---|
| **A** | **REAL DEFECT candidate — TOKEN BYPASS** | one side declares a raw `#hex`, the other declares `var(…)`, same name | **23** |
| **A2** | **REAL DEFECT candidate — NEAR-MISS drift** | two raw hexes, channel distance ≤ 60 — a drift, not a decision | **6** |
| **B** | INTENDED OVERRIDE — non-colour | value is not a hex pair: per-component geometry / motion (`--slot`, `--glyph`, `--phys-size`, `--ease`, `--spring`, `--l-min`, …) | **34** |
| **C** | INTENDED OVERRIDE — deliberate re-point | two raw hexes, distance > 60 across different scopes | **14** |
| **D** | INTENDED OVERRIDE — token re-point | alias vs alias: a scope re-points onto a different **token** | **21** |
| | | **TOTAL** | **98** |
| **E** | **STALE** | ledger rows the tree no longer agrees with (§⑧ — these are **not** among the 98) | **5** |

**Bucket A in full — the 23 token bypasses.** Every row is a raw hex sitting where the same name is
elsewhere an alias, so the token system is bypassed at that site:

| name | theme/mode | a | b | values | Δ |
|---|---|---|---|---|---|
| `--err` | mono any | `canon.css:907` | `canon.css:1842` | `#f6604c` vs `#a8000b` | 239 |
| `--err` | mono dark | `canon.css:907` | `canon.css:1855` | `#f6604c` vs `#a8000b` | 239 |
| `--warn` | mono any | `canon.css:906` | `canon.css:1843` | `#e0a61f` vs `#ffbb33` | 72 |
| `--warn` | mono dark | `canon.css:906` | `canon.css:1856` | `#e0a61f` vs `#ffbb33` | 72 |
| `--info` | mono any | `canon.css:908` | `canon.css:1845` | `#78a7e8` vs `#305a85` | 248 |
| `--info` | mono dark | `canon.css:908` | `canon.css:1857` | `#78a7e8` vs `#305a85` | 248 |
| `--ink` | legacy any | `canon.css:20355` | `canon.css:20495` | `#ffffff` vs `#333333` | 612 |
| `--ink` | supercharge any | `canon.css:24402` | `canon.css:24655` | `#ffffff` vs `#13110e` | 715 |
| `--muted` | mono any | `canon.css:878` | `canon.css:3549` | `#1a1a1a` vs `#767676` | 276 |
| `--muted` | mono dark | `canon.css:878` | `canon.css:3552` | `#ffffff` vs `#9a9a9a` | 303 |
| `--data-grid` | mono any | `canon.css:7138` | `canon.css:7422` | `#e1e1e1` vs `#626262` | 381 |
| `--data-series-1..5` | mono any | `canon.css:401-405` | `canon.css:7151-8400` | five pairs | 47–150 |
| `--mark` | mono any | `canon.css:646` | `canon.css:1940` | `#1a1a1a` vs `#333333` | 75 |
| `--pressed` | mono any | `canon.css:1393` | `canon.css:3415` | `#626262` vs `#767676` | 60 |
| `--disabled` | mono any | `canon.css:2376` | `canon.css:2569` | `#d7d8d6` vs `#e1e1e1` | 30 |
| `--border-disabled` | mono any | `canon.css:2110` | `canon.css:3486` | `#e1e1e1` vs `#d7d8d6` | 30 |
| `--err-t` / `--info-t` / `--warn-t` | mono any | `canon.css:1846/1849/1847` | `canon.css:4091/4094/4092` | tint pairs | 22–69 |

**Bucket A2 in full — the 6 near-misses.** These read as drift, not decision:

| name | theme/mode | values | Δ |
|---|---|---|---|
| `--surface` | supercharge dark | `#2a2621` vs `#2e2a25` | **12** |
| `--surface` | supercharge dark | `#2a2621` vs `#1a1a1a` | 35 |
| `--panel` | supercharge dark | `#2a2621` vs `#1a1a1a` | 35 |
| `--text-disabled` | mono any | `#e1e1e1` vs `#d7d8d6` | 30 |
| `--text-disabled` | mono dark | `#808080` vs `#767676` | 30 |
| `--ter-border` | supercharge any | `#000000` vs `#13110e` | 50 |

⛔ **Three things in that table are for Dave's eye specifically and I changed none of them:**
1. **`--err` carries a THIRD red.** `:root` resolves `#f6604c` (the ruled dark-side red);
   `.cn-notifications` declares `#a8000b`. `s151-D1` names `#DA1A00` and `#F6604C` and **only**
   those. `#a8000b` is neither.
2. **`--ter-border` supercharge resolves `#000000`** — pure black, against the standing ink rule
   (*blackest, not pure black*).
3. **`--pri` is a vocabulary collision**, not a colour fork: it is a hex in one scope and `50%` in
   another. Same name, two kinds of thing. [[vocabulary-collision-switch-202]]

⚠ **The classifier's declared blind spot:** it reads `declared`/`resolved` from the gate's own JSON
and does not model selector specificity, so "different scope" is a **string** distinction. Buckets
B/C/D are *candidates for* intended, not proof of it; buckets A/A2 are the ones worth Dave's time
and are quoted with file:line so he can look rather than take my word.

---

## RULING-SHAPED QUESTIONS (Dave's — nothing here was decided)

**Q1 — The seven capitalised `Chart-*.meta.json`: rename, or leave them normalised?**
The reference is fixed either way (the index is now identical on APFS and ext4). But every sibling
`Chart-*.reference.html` snippet **is** capitalised, so the casing may be a convention the
generator cannot see. `--casing` (advisory) names all seven on every run so the question stays
visible. **Renaming is ~1K and a repo sweep shows 0 case-folding collisions** — but it is a naming
convention, which is yours.

**Q2 — The fork gate asks two questions. Which one is the gate?** Unchanged from L3's Q1, now with
the triage table above as the work queue if `--strict` becomes the wired mode. A third reading is
now cheaper than it was: `--strict` becomes the gate **after** the 5 stale rows of §⑧ come out.

**Q3 — The 5 stale ledger rows.** Two are UNIFORM — there is no divergence left to rule. They are
your #209 sanctions; `--reconcile` reports them on demand and prunes nothing.

**Q4 — Do the three dated `2026-08-25` review pages get regenerated?** `BENTO-CANON-…-v6`, `-v7`,
`GALLERY-COMPARE-…-v3`. Their generators changed today, so generator and artefact now disagree.
**I declined to rewrite them** — they are dated surfaces your eye is owed on (replay-discharge S5-4),
they carry the declared-divergence paragraph waiting on your word (S5-6), and ADR-0017 freezes dated
history. The drift is **invisible by construction** (canon always loads; the fallback is a fence),
so no decision surface is currently misleading anyone. One command each when you say so.

**Q5 — `--err`'s third red, `--ter-border`'s pure black, `--pri`'s two kinds of thing.** §⑨. Each is
a fork VALUE and therefore yours; each is quoted with file:line.

**Q6 — "Never tight" still has two readings.** `gen_foundations_217.py:413` excludes `1` from the
dashboard's main gutter on `s217-D5`, against a matrix whose option space reaches 1px under
`s219-D1(4)`. Replay-discharge **A3**'s second half. Deciding it means deciding which ruling governs
that dial — **declined, filed**.

---

## REPLAY-DISCHARGE RED ITEMS — taken or declined, each named

| item | verdict |
|---|---|
| **A3** (first half) — the ruled rail's two homes, ADR-0017 unsatisfied | ✅ **TAKEN** — §④ |
| **③ finding 6** — same, restated | ✅ **TAKEN** — §④ |
| **A3** (second half) — "never tight" two readings | ⛔ **DECLINED** — ruling-shaped (Q6) |
| **L5-3 / S3-3 / ③ finding 2** — `s149-D1.status` still false | ⛔ **DECLINED** — GLOBAL FENCE 1: `_rulings.json` is READ-ONLY to a sub |
| **S1-1d / ③ finding 9** — memento index stale on the #221 briefs | ⛔ **DECLINED** — the conductor's wrap step (2g), and my brief is one of the causes |
| **S3-4b / ③ finding 3** — `_gm_usage.py --selftest`, #218 wrapped twice | ⛔ **DECLINED** — outside region, ruling-shaped |
| **S5-6** — gallery `squaring` still `False` | ⛔ **DECLINED** — a ruled value; FENCE 4 |
| **N1-5b / S8-4 / ③ finding 5** — ledger re-seed, warning names the wrong mechanism | ⛔ **DECLINED** — release machinery, Lane C |
| **N2-4** — pack runner's three verdicts vs the repo's four | ⛔ **DECLINED** — Lane C |
| **N3-2 / N3-3 / ③ finding 1** — delta gate's missing RUN arm; broken `_gen_chain.py` in the shipped zip | ⛔ **DECLINED** — Lane C / Lane D |
| **N1-2 / ③ finding 4** — `_make_review.py` path wrong in two filed reports | ⛔ **DECLINED** — frozen history (ADR-0017), not my region |
| **③ finding 7** — preserved probe pinned to a dead mount | ⛔ **DECLINED** — evidence asset, outside region |

---

## HANDOFFS (real fixes that want a file outside my region — filed, not edited)

1. **⛔ LANE A — `gates.yml:54-55` is stale about step `[59]`.** It attributes `gen_dashboard.py
   --check`'s 77 to `_governs` naming `outputs/…`. The live cause was a **bare basename**, and it is
   fixed as of today, so the step's verdict behaviour has changed. The comment should say so.
2. **⛔ THE CASE-SENSITIVITY CLASS HAS FOUR MORE CONSUMERS**, all outside my region, all doing
   exact-case lookup from a lowercased slug — the same silent Linux miss:
   `knowledge/_build_prototype_grade_audit.py:81` (`name.lower() + ".meta.json"` — **will always
   miss all 7**) · `knowledge/gen_itinerary_status.py:621` ·
   `knowledge/_render/verify_wave3_alpha_218.py:152,209`. A fifth,
   `verify_wave3_gamma_218.py:154`, was **immunised locally at some point and the class was never
   fixed** — Dave #215: *"always real fixes never patches, they just get lost."* `resolve_meta()`
   in `gen_library_214.py` is the shared shape they should all import.
3. **`_gate_fallback_drift_221.py` has no consumer yet.** It is advisory and deliberately unwired
   (FENCE 2). It should be **named in the gates roster as ADVISORY**, or run at each regen seam, or
   it becomes the instrument it was built to prevent. Wiring is the conductor's / Dave's.
4. **11 UNCHECKED fallbacks are a real hole with a real price.** `_bento_recut_219.py` (5) and
   `gen_grids_218.py` (6) consume aliases declared in a *sibling* source, which this gate cannot
   see cross-file. Widening it to a cross-file alias index is **~4K** and is a second job.

---

## REPLAY-THESE (exact commands a verifier reruns)

```bash
cd <repo>

# ① gen_dashboard: all three verdicts. 0 now; 1 and 77 are proved by the selftest + the mutation.
python3 knowledge/gen_dashboard.py --selftest        # 32 arms, all GREEN  (rc=0)
python3 knowledge/gen_dashboard.py --check           # in sync             (rc=0)
python3 - <<'PY'   # the FAIL/REFUSE pair, driven through the real functions
import sys; sys.path.insert(0,"knowledge")
import gen_dashboard as G, io, contextlib
real = G.run_gates(True)
mut  = [dict(g) for g in real]
mut[0]["lines"] = list(mut[0]["lines"]) + ["FAIL: evidence at `outputs/__ghost_221__.md` is missing"]
with contextlib.redirect_stdout(io.StringIO()):
    a, b = G.mismatch_verdict(mut), G.mismatch_verdict(real)
print("planted untracked ->", a, "| real ->", b, "| expect 77 and 1")
PY

# ③ gen_library_214: the case class, both platforms
TMPDIR=/var/tmp python3 knowledge/_render/gen_library_214.py --selftest   # 41 bites (39/40/41 are #221)
TMPDIR=/var/tmp python3 knowledge/_render/gen_library_214.py --casing     # rc=1, names the 7
TMPDIR=/var/tmp python3 knowledge/_render/gen_library_214.py --check      # 143 component(s) in sync

# ④ SPACING_STOPS: one home, and the address REFUSES when the home moves
python3 knowledge/_render/gen_bento_matrix_217.py --selftest              # 104 bites, R4c is #221's
python3 knowledge/_render/role_defaults_219.py --selftest                 # ruled rails ['1','2','4','16','24','40']
cd knowledge/_render && python3 - <<'PY'
import sys, importlib; sys.path.insert(0,".")
import gen_bento_matrix_217 as M, role_defaults_219 as R
s = M.RULED_SPACING_RAIL
try:
    M.RULED_SPACING_RAIL = (1,2,4,8,16,24,40)
    try: importlib.reload(R); print("NO REFUSAL — the address is dead")
    except AssertionError as e: print("REFUSED ->", str(e).splitlines()[0][:90])
finally:
    M.RULED_SPACING_RAIL = s; importlib.reload(R)
PY

# ⑤/⑥ the fallback class
python3 knowledge/_render/_gate_fallback_drift_221.py --selftest          # 11 bites, both ways
python3 knowledge/_render/_gate_fallback_drift_221.py --verbose           # rc=0, 7 sources reached
python3 knowledge/_render/gen_foundations_217.py --selftest               # 44 bites (18 is the s220-D1 split)
python3 knowledge/_render/gen_foundations_217.py --check                  # 8 page(s) in sync
grep -o 'var(--focus-ring,#[0-9A-Fa-f]*)' showroom/_foundations/bento.html   # -> #305A85

# ⑦ probe registry
python3 knowledge/_probe_registry/_registry.py --run --survey             # P-7 0 · P-8 0
python3 knowledge/_probe_registry/_registry.py --selftest                 # loader PASS, both directions
git diff -- knowledge/_probe_registry/manifest.jsonl                      # ONLY two `note` fields

# ⑧ fork ledger — read-only, and prove it
md5sum knowledge/_TOKEN-FORK-LEDGER.json > /var/tmp/L.md5
python3 knowledge/_validate_token_forks.py --reconcile ; md5sum -c /var/tmp/L.md5
python3 knowledge/_validate_token_forks.py            # rc=0, green line now states its question
python3 knowledge/_validate_token_forks.py --strict   # rc=1, GATE RED: 98 fork(s)  (UNCHANGED)
python3 knowledge/_validate_token_forks.py --selftest # PASS
```

---

## UNPROVEN (honest, priced)

1. **UNPROVEN: the browser/render leg.** `verify_foundations_217.py` and `verify_bento_matrix_217.py`
   were **not** driven — chromium will not launch in this sandbox (`exitCode=127`, missing shared
   libs; `/var/tmp/chromelibs-*` for this session does not exist). So the `#305A85` focus ring is
   proved **in the source and in the emitted HTML**, and **not** in pixels. ⚠ This matters more than
   usual: the fallback only paints when canon fails to load, which is a state no verifier currently
   renders. **Price: one dedicated seam to stage the render env per
   `_RUNBOOK-render-verify.md` (+ the sixth `curl`-from-ports.ubuntu.com stratum replay-discharge
   finding 8 recorded and the runbook still lacks), ~3K tokens.**
2. **UNPROVEN: what `gen_dashboard --check` does in CI.** `outputs/` is git-ignored and **present**
   on this mount, so `_governs.py --selftest` is green here. In a clean checkout it will name
   `outputs/…` paths and `--check` will return **77** — which is `s172-D1` working, and is
   unchanged by my fix (the mutation proves the `outputs/…` shape still refuses). **No Actions run
   was read; `gh` is absent.** Price: one authenticated `gh run view`.
3. **UNPROVEN: the three dated review artefacts.** `BENTO-CANON-…-v6`/`-v7`,
   `GALLERY-COMPARE-…-v3` were **deliberately not regenerated** (Q4), so their generators and
   artefacts now disagree by the repaired fallback literals. Declared, not silent. Price: three
   commands, ~1K, once Dave rules Q4.
4. **UNPROVEN: buckets B/C/D of the triage.** The classifier does not model selector specificity —
   "different scope" is a string comparison. B/C/D are *candidates for* intended override, not
   proof. A/A2 (the 29 defect candidates) are quoted with `file:line` so they can be looked at
   rather than believed. Price to prove properly: a per-selector cascade model, **~6K**, and it is
   the same blind spot `_gate_dataviz_vars.py` publishes about itself.

**CLAIMED, then re-read:** every figure in this report was quoted from a command run in this window,
not carried from L2/L3. Where a source report's figure was superseded (bite counts 94→104, L3's
16 cause-sites→19, `gen_library_214.py`'s path), the new reading is given **with its cause named**.

---

## FILES TOUCHED (for the conductor's reconcile)

**Modified**
```
knowledge/gen_dashboard.py                        ① scraper fix + 3 bites
knowledge/_render/gen_library_214.py              ③ resolve_meta + --casing + 3 bites
knowledge/_render/role_defaults_219.py            ④ SPACING_STOPS derives, no longer typed
knowledge/_render/gen_bento_matrix_217.py         ④ comment struck + R4c mutation-driven
knowledge/_render/gen_bento_canon_217.py          ⑤ 5 fallback literals
knowledge/_render/gen_bento_roles_217.py          ⑤ 5 fallback literals
knowledge/_render/gen_gallery_compare_217.py      ⑤ 5 fallback literals
knowledge/_render/gen_foundations_217.py          ⑤ 2 literals · ⑥ bite 18 s220-D1 split
knowledge/_render/_bento_recut_219.py             ⑤ 2 alias-consumer literals
knowledge/_probe_registry/README.md               ⑦ typed counts -> address + dated receipt
knowledge/_probe_registry/manifest.jsonl          ⑦ P-7/P-8 `note` ONLY (`caught` untouched)
knowledge/_validate_token_forks.py                ⑧ --reconcile (read-only) + honest green line
```

**Generated (the ordered serial, in order)**
```
showroom/_foundations/bento.html · bento-rails.html · grids-12col.html · grids-dashboard.html
showroom/_foundations/grids-display.html · grids-gallery.html · logos.html · photography.html
dashboard/index.html                              (+2,594 / −1,033)
```
`showroom/index.html` + `index.json` were regenerated **index-LAST** and came back **byte-identical**
— correct, and proved rather than skipped.

**New**
```
knowledge/_render/_gate_fallback_drift_221.py     ⬛ ADVISORY at birth, unwired by FENCE 2
```

**NOT touched, deliberately:** `knowledge/_rulings.json` · `knowledge/_state.json` ·
`knowledge/_TOKEN-FORK-LEDGER.json` (md5-verified either side of `--reconcile`) ·
`knowledge/_proforma/DataViz-interactive.html` · `.github/workflows/gates.yml` (Lane A's) ·
the three dated `reviews/*.html` pages (Q4) · the 7 `Chart-*.meta.json` filenames (Q1).

⚠ **Concurrent lanes are writing the same tree.** At filing, `git status` also carried
`.github/workflows/`, `knowledge/_capture_gate.py`, `_search_core.py`, `_validate_no_hardcode.py`,
`_validate_radius.py`, `_validate_hit_area.py`, `_validate_state_contrast.py`,
`_validate_descender_computed.py`, `_tests/test_gates.py`, `_RADIUS-GATE.md`, `apollo-spider/**`
and `knowledge/_graph-mark-observations.jsonl` — **none of them mine.** The list above is exact.

**USAGE:** `UNMEASURED — declared`. A sub cannot read its own `message.usage` in this harness; the
conductor's panel is the only honest reading of this lane's spend.
