# `#247`-`wrap` — the delegated capture ritual for #247: five rulings read back at 360 (not re-inscribed), the residual → #248 transform (one struck, four corrected, ten new), two review pages registered, the boot over the ceiling carried, and the commit run against a fresh `index.lock`

session: `#247` · 2026-09-05
window: conductor window (FABLE), delegated wrap sub (FABLE)
sub index: `wrap`
brief: `notes/_briefs/2026-09-05-247-delegated-wrap-brief.md` — filed by the conductor BEFORE this sub launched; it points at `notes/_briefs/2026-09-05-246-delegated-wrap-brief.md` § 4 and the #242 template for the ritual's shape and at `knowledge/_RUNBOOK-capture-ritual.md` for the mechanics.
tokens: `UNMEASURED at the sub's own seat as a SPEND; the seat's FILL is measurable and is 186,787 real, peak across 83 turns (knowledge/_checkin.py, first-hand, newest mounted transcript). The two are different quantities and neither is converted into the other.`

## VERDICT

The capture ritual ran **COMPLETE** inside the brief's DO-NOT-RULE fence: **the five rulings `s247-D1`…`s247-D5` were
READ BACK from `knowledge/_rulings.json` at 360 (each parses, `by` Dave · `date` 2026-09-05 · `status` ruled, `says`
verbatim) and NOT re-inscribed**; then steps 1, 1b, 2, 2c, 2d, 2e, 2f, 2g, 4, 4b and 4c all executed, **2e was legitimately
empty and is declared with its size**, step 3 (memory) was the conductor's and is declared as ZERO bytes written here, and
step 5 was run through `_git_commit.sh --wrap` **WITHOUT a push** (not authorised — declared). Every roll went through
`knowledge/_gm_move.py` from session-owned, uniquely-named ops files under `knowledge/_tmp/wrap247/`, each asserted for
existence and a size floor **in the writing process**, with every receipt read back against the ops it was meant to run;
nothing was hand-edited. **Nothing of Dave's was ruled beyond his five verbatim answers, and nothing was re-worded,
re-dated, re-stamped or re-scoped; no constant, band, ceiling, floor, advisory, stop line, wall, roster or version was
moved.** ⛔ Dave's *"its just my gut reaction, we need this test…"* is recorded as NOT a ruling; the blind arm stays.

★ **The headline the next session needs: the boot at the #247 opener was 70,822 — 822 OVER the `s241-D1` ceiling — and
the ceiling arm reads 0 fail tonight only because it reads `notes/_GAUGE-LOG.md`, which this stratum reaches at #248's
2f roll.** Carried as `_CARRIES.md` § residual → #248 ⑩, stated once in the stratum, named in the stamp.

COUNTS: findings `6` · ruling-shaped `4` · UNPROVEN `4`

## What was done

**Step 0 — the brief, read; the shape, read; the rulings, read back.** `knowledge/_rulings.json` § `rulings` parses at
**360**; `s247-D1`…`D5` present with the fields above; `governs` name the review pages, the bento snippet, `kpi-tile.meta.json`
and `_rules-index.json`. The #246 wrap's own scripts (`knowledge/_tmp/wrap246/*.py`, gitignored, still on disk) were read as
the mechanical template and re-cut for #247 — same mover, same ops-file discipline.

**Check-in at the opener** (`python3 knowledge/_checkin.py`): **1 STRUCTURAL** (the retrieval index STALE — the #32
defect, healed by 2g run LAST) · 0 heals-at-wrap · **25 warn**, one NEW (`MEMORY.md` 1,808 tape against a 1,802 cap, +6,
ADVISORY — the conductor's file, declared not fixed) and one CHANGED (git: 14 uncommitted paths) · FILL 186,787 real, boot
70,822, peak over 83 turns · SEAM CLEAR.

**Step 1 — `_LIVE-STATE.md` refreshed.** New `## ⏱ LATEST DELTA — 2026-09-05 … #247` (15 lines incl. blanks; 11
bullets), the #246 delta demoted to ⏱ PRIOR, and the `*Last refreshed:*` header rebuilt with the #247 summary leading, #246
and #245 kept verbatim as `Previous:` segments. Date from `date` (Sat 5 Sep 2026, 19:59 BST). `python3
knowledge/_validate_standing_instructions.py` → **PASS, 28 standing docs reachable, GOOD-MORNING structure intact.**
`knowledge/_REVIEW-SIGNOFF.md` gained **two rows** through the mover — the DP test plan (AWAITING his word on its four
RSQs) and the W1 density review page (SEEN and ruled `s247-D2`…`D5`; its remaining items named).

**Step 1b — dossier written.** `_DECISION-HISTORY/2026-09-05-247-enact-29-carefully-and-test.md` with its
`provenance: 247 · 2026-09-05` / `status: observed` block and both-way links. Twelve sections: why "do we need both?" came
before an edit · why 29 and why "carefully" became a method · why the plan is a page · why W1 was one edit in the lead group
· why fold and richness were measured separately · why the blind arm regressed and stays · why the judge was blind and why
"nothing leads" matters · why the status tiles became a flexibility ruling · why a patch lane ran · why the probe is reported
twice · what the lanes cost · resolved and open.

**Step 2 — `GOOD-MORNING.md`.** New ★ LATEST #247 banner: **10 substantive lines, 1,177 tape** against `s241-D2`'s cap of
10 / 1,200 — the seventh banner bound by it, met with a margin of **23**. Girth, never content: the two report paths + the two
briefs + this report on ⓪, the five rulings as ids with short glosses on ①, the plan on ②, W1's figures on ③, the blind arm
and the judge on ④, not-done as a name list on ⑤, the gauge on ⑥. `TITLE THE NEXT CHAT` → `Apollo - #248: W2 rhythm — one
tile leads`. `size:` stamp re-written LAST and iterated to an **EXACT fixed point** with `_gen_chain.py` — GM **36,126
tape** (+262 on #246's 35,864) · §A **6,957** (byte-identical to #237…#246) · LS **58,061** (+565) · corpus 94,187;
`_CHAIN.md` **8,919 tape = slice 8,071 + wrapper 848**, GM header+LATEST 4,554, LS delta 3,517 — **235 DOWN on #246's
9,154**, under the `s214-D6` ~10–12K band and above M10's 7,700 advisory.

**2c — banner roll + the carry set.** `## Batch 2026-09-05 #247` opened in `_GM-ARCHIVE.md`; the ★ PRIOR **#245** banner
moved there **verbatim, 12 lines**, by `{"op":"move"}` after its EXIT CHECK (1 marker line; 18 names probed in
`_CARRIES.md` § residual → #248, 18/18 present); the #246 banner demoted to ★ PRIOR in the same transaction that inserted
#247's. The carry set was written straight into `_CARRIES.md` § `## residual → #248` (clause (i)): **359 ages bumped**,
twelve `[NEW — 0]` became `[1]`, **ONE STRUCK** with its `s188-D2` receipt ("Eighteen" vs 29 → `s247-D1`), **FOUR
CORRECTED** with receipts and NOT struck (the enact wave — scope 29, W1 landed; the DP-17 test — drafted, scheduled W6;
parts-with-addresses — his frustration line added as evidence, not a ruling; the strays — the 09-04 lock moved by the #246
commit, a fresh lock + `_tools/` declared), and **TEN new** at age 0 (W2 · the status surface · the plan's four RSQs · a fold
row · the Tabs demo width · the stale comments · the probe miss · the two unread CI verdicts · the un-rendered page · the
boot over the ceiling). **`_capture_gate._carry_items` reads 244 on the #247 line and 257 on the #248 line.**

**2d — delta roll.** The #244 ⏱ PRIOR DELTA moved **verbatim, 15 lines**, to `_LIVE-STATE-ARCHIVE.md` § `Rolled
2026-09-05 #247`, and #244's `Previous:` chain segment (**3,263 chars**) moved into the **same** section in the **same**
mover batch, replaced in the spine by a dated trim note. EXIT CHECK: 6 marker lines, 16 Dave-owed names probed present in
the #248 carry line before the move. Deltas now **3/3**.

**2e — nothing to do, and the size is ZERO DATED STRATA.** `## ⬛ DO THIS FIRST` measures **78 raw lines / 10,140 tape**
(to the next heading; `_gm_usage.py` agrees at DOFIRST:10140) and a `#### ` scan returns **0**.

**2f — stratum split.** `{"op":"roll_2f","session":246}` → **19 lines** appended at true EOF of `notes/_GAUGE-LOG.md` and
**2 lines** into `_GM-ARCHIVE.md` under the newest batch, driven with the SESSION-QUALIFIED anchor `> **COMMIT STATE
#246:**`. The new `#### 2026-09-05 #247` stratum — 19 lines as written, plus the `s214-D6` line inserted after the stamp
— then written into `GOOD-MORNING.md` § ⏱ SESSION STRATA (#96/#95/#40/#41/#42 EXEMPT, untouched). It carries: the boot
**stated once** (70,822, +2,939 on #246, 822 OVER the ceiling) · `subs 480,393 tokens (n=4)` · the `wrap-handover:` row
(brief-cut 180,791 · sub-cut 186,787 · delta 5,996) · the ceiling-arm line quoting the gate verbatim after the roll (0
fail; #244 · #245 · #246 graded fresh and under) AND the warning that #248's roll will name a breach · consult-receipts
(honest negative) · section-usage/section-sizes · the `s214-D6` chain measurement · fifteen declared skips, each with a
size · the four EXIT CHECKS · seven premise checks · `COMMIT STATE #247` naming the lock. After the roll `_roll_state.py`
prints `2c OK (banners 2/2) · 2d OK (deltas 3/3) · 2f OK (strata 1, log #246)` and that line is on the banner verbatim.

**2g — retrieval index rebuilt LAST**, after every GM/LS edit and this report were final, and staged.

**Step 3 — memory: ZERO bytes written at this seat.** The conductor's, done at his (`w1-density-landed-247`). NON-REPO,
outside every gate; neither written nor read back here.

**Step 4 / 4b — decision nodes + titles.** `W-429` (the W1 brief) · `W-430` (the wrap brief) · `W-431` (lane D) · `W-432`
(lane R) · `W-433` (this report) MINTED via `_state.add`, every one refused-or-accepted by the store's own close-condition
check; nothing closed; store **501 → 506**. `_gate_doc_rows.py --check` → **0 UNROWED** at the opener (the new documents
were untracked and outside its population), re-run after staging in the commit's own output. `_gen_titles.py --session
247` run and the receipt regenerated. **Step 4c** — `_gate_scratch_hygiene.py --clean` run; the wrap's own ops files live
in `knowledge/_tmp/wrap247/` (gitignored, in-mount, never `/tmp`).

**Step 5 — commit WITH `--wrap`, NO push.** `SESSION_N=247 bash knowledge/_git_commit.sh --wrap --reconciled
<msgfile> <paths>`. ⚠ **A 0 B `.git/index.lock` dated 2026-09-05 14:46 (the #246 commit's own minute) sits on the mount;
the script's `clear_locks` MOVES any `.git/*.lock` it finds into `_to_delete/_stale_locks/` (never `rm`), and the brief's
instruction is STOP AND REPORT if the commit is refused.** The outcome — a hash and subject read back from `git log -1
--format='%h %s'`, or the script's refusal quoted — is in the chat stub, because this file cannot carry its own commit
hash. ⛔ **No push:** `origin/master` = `4dc12b6`; the tree is TWO ahead after this commit. NOT staged by the brief's
fence: `_tools/` (92 KB) · `knowledge/_screen-gate/dashboard.md` (7.9 KB) · the lock. `outputs/w1-density/` (4.8 MB) is
gitignored (`.gitignore:67`).

**Citation note for the gate.** The wrap gate warns that `notes/_subreports/2026-09-05-246-B-baseline.md`, `notes/_subreports/2026-09-05-246-O-opus-baseline.md`, `notes/_subreports/2026-09-05-246-P-principles.md` and `notes/_subreports/2026-09-05-246-wrap.md` were filed "since the last wrap" — its anchor is `4dc12b6`, because `04099a8` (the #246 wrap) is not yet the last wrap it recognises. They are #246's reports, cited by path in #246's own banner and delta (now ★ PRIOR / ⏱ PRIOR), and are deliberately not re-carried on #247's banner; named here so the pointer is not unread.

## Findings

1. **The boot breached the ceiling and no gate said so at this wrap.** 70,822 against 70,000 — the arm reads the gauge log,
   and a stratum reaches the log one wrap later. The #242 breach (70,710) was found the same way, one wrap late. The lag is
   structural, declared here and in the stratum, and carried (⑩); whether the arm should ALSO read the un-rolled stratum in
   GM is RULING-SHAPED 1.
2. **The #246 brief's STOP clause on the lock was never reached, and the #246 record says the lock was "NOT touched".** Both
   true: `_git_commit.sh` moved it itself (`clear_locks`), the wrap sub did not. The strays carry said the 09-04 lock was
   still there; it is corrected here with `git log -1` as the receipt, and a NEW 0 B lock from the #246 commit's own minute
   is named. A lock that git leaves behind on this mount after every commit is a fact of the mount, not of any wrap.
3. **The judge's verdict is the first conflict-ledger entry that came from a reader who did not know which page was which.**
   Lane R's ledger had two rows (both from the sighted builder); J's "nothing leads" is DP-07/DP-14 pushing on DP-06 from
   outside. It is W2's opening fact and the next title.
4. **The plan's own four questions were not answered by "go w1".** W1 ran on the plan's defaults; the four are carried as a
   single item (③) so they cannot be silently answered by the next wave running on the same defaults.
5. **The recall probe's miss was a truncation, and the truncation was the conductor's own `tail -8`.** The plant rule already
   forbids it; this is the second violation of that rule by the same mechanism. Reported both ways, as the brief asked.
6. **Two lanes shipped review surfaces without rendering them, and a third lane paid 60,440 quota to see them.** The class
   is named in memory (no gate parses the artefact); nothing in the review-page path renders. Carried (⑨).

## RULING-SHAPED QUESTIONS

⛔ Do-not-rule honoured: the status surface, the plan's four RSQs, the fold row, the Tabs demo width, the stale comments,
Q8–Q16, the DP-17 pick, and every #246 carry at its age. None re-worded, re-scoped or closed.

1. **Should `boot_constant_drift_check` also read the un-rolled `#### <date> #<N>` stratum in `GOOD-MORNING.md`** (finding
   1), so a breach is graded at the wrap that records it rather than one later? (a) yes — read both, dedupe by session ·
   (b) no — the log is the one instrument; the lag is the price of one home · (c) the wrap sub declares the breach in prose
   (done here) and that is enough. Recommend (a) as a priced candidate; the #242 and #247 breaches were both found late.
2. **Does `_git_commit.sh` report the lock it moved by path in its own output** so the next record does not say "NOT
   touched" about a lock the script touched? (a) print the moved path · (b) leave; the receipt is in `_to_delete/` ·
   (c) stop moving locks and STOP instead. Recommend (a).
3. **Should a probe read-back through `tail`/`head` be REFUSED by the plant tool** (finding 5)? (a) yes — detect a
   non-tty pipe and refuse · (b) no — the rule is prose and stands · (c) print the plant twice. Recommend (a); the rule has
   now failed twice by the same mechanism.
4. **Do review-page briefs carry a render-proof line** (finding 6)? (a) yes, one playwright call, the PNG beside the report ·
   (b) warn only · (c) leave. Recommend (a).

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** the commit's outcome (hash) — this file is written before the commit and cannot name it; the chat stub does.
- **UNPROVEN:** the `s241-D2` S1 cap is graded by the gate on the ★ LATEST banner text; 1,177 / 10 was measured with
  `_capture_gate.measure_tokens` on the inserted text, not read from a gate verdict line.
- **UNPROVEN:** whether the conductor's memory write (`w1-density-landed-247`) exists — NON-REPO, invisible here.
- **UNPROVEN:** `_carry_items` +13 (244 → 257) against an expected +12: the thirteenth countable item was not traced.
- **CLAIMED:** every W1 figure is copied off the two filed reports and the brief, never re-measured here.

## Evidence

`knowledge/_tmp/wrap247/` (gitignored, in-mount): `ls_step1.py` · `carries.py` · `ops_2d_signoff.py` · `gm_2c.py` ·
`stratum.py` · `stamp.py` · `rows.py` · `banner247.txt` · every `ops-*.json` the mover consumed · `chain-out.txt`. The
mover's stdout receipts are quoted above. Standing homes touched: `GOOD-MORNING.md` · `_LIVE-STATE.md` · `_CARRIES.md` ·
`_GM-ARCHIVE.md` · `_LIVE-STATE-ARCHIVE.md` · `notes/_GAUGE-LOG.md` · `knowledge/_REVIEW-SIGNOFF.md` ·
`knowledge/_state.json` · `knowledge/_memento-index.json` · `knowledge/_gen_titles_receipt.json` · `_CHAIN.md` (generated).

REPLAY-THESE: this file § RULING-SHAPED QUESTIONS (~500 tk) · `_CARRIES.md` § `## residual → #248`, the TEN new items (~2.5K tk)
