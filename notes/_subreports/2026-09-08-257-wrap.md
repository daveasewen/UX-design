# `#257`-`wrap` — the delegated capture ritual: two ratifications, a frozen gate that was right about #245, and the first boot reading under the ceiling

session: `#257` · 2026-09-08
window: delegated wrap sub (OPUS 5) — ✅ **the conductor's transcript WAS read at this seat** (`knowledge/_checkin.py`)
sub index: `wrap`
brief: `notes/_briefs/2026-09-08-257-delegated-wrap-brief.md`, cut at a DECLARED FILL of 216,975 tokens
tokens: `UNMEASURED at this seat — a sub cannot read its own message.usage. The conductor's FILL is
MEASURED first-hand here at 227,409 real over 89 turns against his DECLARED brief-cut of 216,975,
and his boot 68,837 is the same instrument's first-turn reading.`

provenance: 257 · 2026-09-08
status: observed

## VERDICT

The ritual ran whole — steps 1, 1b, 2, 2c, 2d, 2e, 2f, 2g, 3 (declared), 4, 4b, 4c, 5 — and every
move went through `_gm_move.py`. **The `--wrap` form is REFUSED for the ELEVENTH wrap running**,
on the boot-ceiling arm, even though ✅ **this session's own boot is 68,837 — the FIRST reading
UNDER the 70,000 ceiling after two consecutive breaches, and it is #256's `MEMORY.md` stub cut
measured at last.** The arm's clause is EVERY post-breach reading under the ceiling and #255's
70,127 / #256's 70,241 still stand over it; the literal was NOT moved and the commit is made in
the #243 form. **`s257-D1`, `s257-D2` and `s257-D3` were inscribed by the conductor and read back
here at entries 396/397/398 (store 395 → 398); none was re-inscribed.** One carry was **STRUCK**
(v1.0.6 PROPOSED — he ratified it, then ratified a second version on top of it) and **two
CORRECTED** (the boot ceiling; `META-TAGS-CALLS-CONTEXT` unread for a third consecutive session).

COUNTS: findings 6 · ruling-shaped 2 · UNPROVEN 3

## What was done, by step

- **1** — `_LIVE-STATE.md` `Last refreshed` rebuilt for #257 (#256 demoted to `Previous:`, #254's
  chain segment trimmed to `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-08 #257 at the same 2d
  boundary, verbatim). Date taken from `date`, never from belief.
- **1b** — `_DECISION-HISTORY/2026-09-08-257-two-ratifications-and-the-frozen-proposed-zip.md`,
  six findings with their arcs, provenance/status lines, both-way links.
- **2 / 2c** — the **#255** banner (11 lines) rolled to `_GM-ARCHIVE.md` § Batch 2026-09-08 #257
  via the mover; the #256 banner demoted to ★ PRIOR; the #257 ★ LATEST written under the
  `s241-D2` cap at **1,193 tape / 10 lines**, met by **twelve measured passes of prose-tightening
  only** — no item, carry, declared skip, receipt name, path or figure was dropped.
- **2c (i)** — the carry set written straight into `_CARRIES.md` § `## residual → #258`: **468 age
  brackets bumped by ONE programmatic pass**, seven new items, one strike and two corrections,
  each with its `s188-D2` receipt. Probeable count **332**.
- **2d** — the **#254** ⏱ delta (17 lines) rolled to `_LIVE-STATE-ARCHIVE.md`; #256 demoted to
  ⏱ PRIOR; the #257 ⏱ LATEST DELTA written as the sole home for gauge, declared skips and
  not-done detail.
- **2e** — nothing dated to roll; declared with its size (DO-FIRST **108 raw lines**).
- **2f** — `roll_2f` split #256's stratum: **14 ln → `notes/_GAUGE-LOG.md`** (appended at EOF) and
  **2 ln → `_GM-ARCHIVE.md`** under the #257 batch key. The `#### 2026-09-08 #257` stratum was
  then written with pre-flight (REFUSED, not inherited), post-mortem, `wrap-handover`,
  section-usage/sizes, consult-receipts and COMMIT STATE.
- **2g** — `_build_memento_index.py` rebuilt LAST, after every GM/LS edit was final.
- **3** — **0 bytes at this seat.** The conductor wrote `demo-audience-is-david-rice-hsbc-257.md`
  and re-trimmed `MEMORY.md` to 1,781 tape (cap 1,802) before cutting the brief. The store is
  invisible to this seat (D1a) and nothing here is a claim about it.
- **4** — the three rulings READ BACK from `knowledge/_rulings.json` and quoted verbatim; NOT
  re-inscribed, per the brief.
- **4b** — both title lines generated by `_gen_titles.py --session 257`, receipt written.
- **4c** — `_gate_scratch_hygiene.py` run.
- **5** — one wrap commit in the #243 form, then `--push`.

## Findings

1. **The frozen gate was right and the defect is #245's.** `RE-RECORDED WITHOUT A VERSION BUMP`
   (recorded `398c695a`, measured `85f4f4db`) fired because a **PROPOSED** zip was seeded as
   frozen twelve sessions ago; a ratified re-bake at the same version launders a hash by the
   ledger's own rule. The bump was the honest remedy; a re-seed would have rewritten the record
   the gate protects.
2. **An inference in a dated home reads as an observation one session later.** "the Sutherland
   demo" was written at #246 L31 as an inference and survived eleven sessions untested until Dave
   asked. Corrected BY ADDITION.
3. **A premise was checked rather than assumed:** no gate binds a skill's `name:` to its
   directory, which is why the name-only rename was safe and why the directory question is still
   open rather than silently answered.
4. **The boot cut is measured, one session late, exactly where #256 predicted.** 68,837 — the
   first under-ceiling reading after two breaches.
5. **A seam-less run is how a conductor reaches 216,975.** `_checkin.py` at the ends only;
   release work is many cheap measured steps, so the seat feels busy rather than full.
6. **`ADS-grill-me` was verified INSIDE the zip**, not inferred from the tree — the rename's
   purpose is what a reader sees in a list, and only the shipped artefact shows that.

## UNPROVEN from this seat

- Whether the pushed tree passes CI (`s203-D1` read-back for `82b310c` is owed; `_build_all.py` is
  sandbox-impossible).
- Whether `MEMORY.md`'s current content is what the conductor says it is — the store is outside
  the repo and invisible here (D1a).
- Whether `reviews/META-TAGS-CALLS-CONTEXT-2026-09-07-v1.html` renders — never opened in a browser
  by anybody, at any seat.

## RULING-SHAPED QUESTIONS

1. **The frozen-ledger birth clause.** Should `--seed` REFUSE a PROPOSED surface outright, or
   should the birth clause admit a PROPOSED → RATIFIED transition at ONE version? Priced, not
   taken; the v1.0.7 bump routed around it.
2. **The boot-ceiling arm.** A cut has now been proven to move the reading. What happens to the
   arm — and to the two breach readings still over the literal — is Dave's word alone; the
   literal is shrink-only and was not moved.

REPLAY-THESE: `python3 knowledge/_capture_gate.py --wrap` · `python3 knowledge/_gen_titles.py --session 257 --check` · `python3 knowledge/_roll_state.py` · `PYTHONPATH=knowledge python3 -c "import _capture_gate as c;print(len(c._carry_items([l for l in open('_CARRIES.md') if '#258:**' in l][0])))"`
