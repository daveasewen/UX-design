# #261 G — the grid header (Data grid column-header design pass)

**Component**: the column-header row of `data-grid`. **Not shared with `table`** — Table's header is a passive
`th scope="col"` that collapses to cards (rubric decision A, 2026-06-22); nothing was bolted onto it and
`Table.reference.html` / `table.meta.json` are untouched. Fence: `Data-grid.reference.html`,
`data-grid.meta.json`, generated `showroom/data-grid.html`, and my `notes/` files.

## What shipped

1. **Anatomy per column** — one `.th-in` flex row: truncating label · sort button · optional 24px column filter ·
   resize handle. Nothing can collide with the label any more.
2. **Group header row** — optional `th scope="colgroup"` row ("Transaction detail" / "Value (GBP)"), sticky at
   `top:0` with the column row sticking below it at `top:var(--grp-h)`. It COUNTS: `aria-rowcount` and every
   `aria-rowindex` shift by one when it is on (18 with, 17 without, on 16 matches).
3. **Sticky, provably** — the scroll region took a `max-block-size` (`--dg-vh`, 380px) so the sticky header is a
   driven state, not a promise. Sticky + `border-collapse` drops cell borders while scrolling, so every header
   rule is an inset shadow.
4. **Per-column filter** — a form-furniture button (Search-field precedent: colour states, no press physics)
   opening a menu under its own cell; Enter commits, Esc closes, outside click closes. Off/on are two DIFFERENT
   library glyphs (`global-controls/filter.svg`, `filter-active.svg`), never one recoloured (icon-011). Filters
   became `{key, term}`, so a column filter renders as a chip that names its column ("Payee: acme"); the toolbar
   filter is the same shape with `key:null`. Amount ships WITHOUT a filter — the affordance is per-column.
5. **Resize** — 12px grip, 24px hit area, `role="separator"` + live `aria-valuenow`, driving `<col>` widths in a
   `table-layout:fixed` table (96–480px clamp). Pointer drag, and keyboard WITHOUT a second tab stop:
   `Alt+←/→` on the header cell enters resize mode, arrows size (Shift = 32px), Esc/Enter returns.
6. **Density** — `data-density` comfortable|compact: 44px header row and targets dial to 32px. Above the 24px
   dial-down floor, never below (s116-D1).
7. **Disabled column** — Reference (the editable one) is not sortable: re-ordering rows under an open editor is
   the bug. `aria-disabled`, no `aria-sort`, no glyph, alpha mute.
8. **Sorted vs filtered take DIFFERENT EDGES** — sorted = ground shift + 3px accent UNDER; filtered = 3px accent
   LEADING. One column can be both and stay readable. Each is doubled by a glyph and by aria; colour never alone.

## Gate receipts (this fence)

```
_validate_snippets.py     137 snippet(s), 0 failure(s)
_validate_a11y.py         137 snippet(s), 0 failure(s), 204 warning(s) — none on Data-grid
_validate_behaviour.py    exit 0 (behaviour-contract gate OK)
_validate_dataviz.py      exit 0
_validate_coverage.py     137 meta / 137 snippet, 0 failure(s)
_validate_no_hardcode.py  exit 0 · _validate_palette_tier.py exit 0 · _validate_compose.py PASS
_validate_type_composites 2 pre-existing TYPE-002 on this file (body, .hint) — unchanged, none added
gen_component_partials --check   Data-grid IN SYNC (out-of-sync = Tab-bar/Navigations, nav lane)
gen_showroom.py --check   OK — 137 page(s) + index in sync
CHROMIUM (playwright + libxdamage1 per the brief recipe, arm64 prefix):
_validate_hit_area.py     ADVISORY, exit 0 — no BREACH-FLOOR. 4× .colf at 24×24 (the dial-down floor, Tags
                          dismiss-× size) reported UNDER the 44px default; demo-chrome buttons pre-existing.
_validate_state_contrast  0 text failures · 0 carrier failures · 3 declared holes (the closed column menus)
_validate_kg.py           the 14 FAILs are filter-toolbar-bar (6), footer (4), legend (3), gen_kg_edges (1) —
                          other lanes. The one data-grid line (selection-checkbox $note) is present at HEAD too.
```

**Driven, four themes × two modes** (`notes/_lanes/261-G-drive-grid-header.py`, `goto file://` only):
sort 44px comfortable / 32px compact · filter 24×24 both · select-all label 44/32 · handle 12×44 ·
sticky parks at 33px after a 70px scroll · keyboard resize `aria-valuenow` 176 → 192 · sorted accent
`inset 0 -3px` and filtered accent `inset 3px 0`, both `form/border/active`, in all eight combinations.
Screenshots: `notes/_lanes/261-G-grid-header-{mono,legacy,console,supercharge}.png` + `-mono-compact.png`.
Review page: `notes/_lanes/261-G-grid-header-review.html` (OLD vs NEW, 13 states, 4 themes × 2 modes, receipts).

## Two things that want Dave's word

1. **The group row has no ground of its own.** `--grouphdr` binds `table/column/background`, which resolves to the
   same hex as `table/header/background` in all four themes today, so the row reads by caption type and its rule.
   Either a theme splits the two tokens, or the group row stays a typographic distinction. **split** or **type**.
2. **The disabled column is muted with alpha, not `text/disabled`.** That token is #E1E1E1 and is illegible on the
   #F0F0F0 header ground — the `.pbtn[disabled]` precedent only works because pagination sits on the page.
   **alpha** or **token**.
