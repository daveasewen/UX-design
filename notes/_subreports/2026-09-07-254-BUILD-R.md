# `#254`-`BUILD-R` — `_gm_usage.py`: teach the reader "a session that wrapped twice"

session: `#254` · 2026-09-07
window: `BUILD LANE R`
sub index: `BUILD-R`
brief: in-message (no brief file filed for this lane)
tokens: `UNMEASURED — this seat cannot read its own message.usage`

## VERDICT

DONE. `python3 knowledge/_gm_usage.py --selftest` was RED since #253 and is now GREEN, exit 0,
78 bites (was 69). The fix is entirely in the READER, per `s253-D2`: neither #218 testimony was
edited, deleted or excluded, `notes/_GAUGE-LOG.md` was not touched, and the disagreement clause
still refuses — the mutation bite proves it, byte for byte. What was added is a VOCABULARY: a
wrap index read from the `section-usage` line's own parenthetical (`(second wrap; …)`), so two
testimonies one wrap apart are two honest records rather than a falsification. The LAST wrap is
the session's closing figure, and `history_report` now SAYS SO in its own output. Only
`knowledge/_gm_usage.py` was modified. Nothing was committed, pushed, stashed or checked out.

COUNTS: findings `5` · ruling-shaped `1` · UNPROVEN `1`

## What was done

`knowledge/_gm_usage.py` — 157 insertions, 17 deletions (`git diff --stat`), one file:

- **New `wrap_index(parenthetical)` + `WRAP_LABEL_RE` / `WRAP_WORDS`** (~line 424). Reads an
  ordinal-then-`wrap` label out of the line's own parenthetical. UNLABELLED IS 1 — the meaning an
  unlabelled line has always carried. ds-025 discipline held: an unrecognised ordinal is NOT
  guessed forward to "probably the next one"; it stays 1, and the disagreement check then fires,
  which is the loud outcome.
- **New `_at(wrap)`** — the wrap qualifier for a refusal message. Deliberately EMPTY at wrap 1 so
  the single-wrap refusal is byte-identical to the one the corpus and `_capture_gate.py` know.
- **`usage_history()` re-keyed** from `session` to `(session, wrap)`. All three refusal branches
  (codes-vs-codes, codes-then-UNMEASURED, UNMEASURED-then-codes) now compare within one wrap
  index and are otherwise unchanged. After the walk, the closing figure per session is the
  MAXIMUM wrap; earlier wraps are kept and named in a new `MULTI-WRAP: …` note. The returned
  `rows` shape is UNCHANGED (`[(session, {id: code}), …]`), so `usage_streaks`,
  `deferral_candidates` and `_capture_gate.usage_history_probe` need no edit and the DEFER_STREAK
  logic reads the closing figure automatically.
- **`history_report()`** inserts the `MULTI-WRAP` note directly under the table header, so the
  reader is told WHICH wrap the `#218` column is before it reads a single streak.
- **Three new selftest blocks** (9 bites) — named below.

## Findings

1. **BEFORE, verbatim** (`python3 knowledge/_gm_usage.py --selftest`, exit 1):

   ```
   [_gm_usage selftest] FAIL:
     ✗ real-repo usage history reads clean (got: ['session #218 testifies DIFFERENTLY in notes/_GAUGE-LOG.md and notes/_GAUGE-LOG.md — one of them is false and this reader cannot tell which. REFUSED.'])
     ✗ real-repo report closes in exactly ONE of its two legal states, never a remedy choice
   ```

   **AFTER, verbatim** (exit 0):

   ```
   [_gm_usage selftest] OK — 78 bites, all fired or held as contracted (sizes method: real)
   ```

2. **The corpus was surveyed, not assumed.** Every `section-usage` parenthetical in
   `notes/_GAUGE-LOG.md` + `GOOD-MORNING.md` was extracted and grepped for `wrap`. `#218` is the
   ONLY line in 189 sessions carrying a wrap ordinal (`(second wrap; self-report, delegated OPUS
   wrap sub)`); every other parenthetical mentioning "wrap" says `wrap sub` / `conductor wrap`,
   which `WRAP_LABEL_RE` correctly does not match (no ordinal). The regex was not aimed at #218 —
   it is aimed at the label form, and a bite pins the negative case.

3. **`--history` now publishes and names the choice.** `python3 knowledge/_gm_usage.py --history`
   exits 0 and prints, as line 2 of the report:

   ```
     ⚠ MULTI-WRAP: #218 wrapped 2 times (wraps 1, 2) — the series carries wrap 2 as its CLOSING testimony. The earlier wrap(s) are READ and KEPT — a session that wrapped twice is a real event honestly recorded, not a contradiction — and are NEVER averaged, summed or blended into the closing figure (#254).
   ```

4. **The gate's own consumer is green.** `_capture_gate.usage_history_probe('.')` driven
   directly: `issues: []`, `usage-history: 189 sessions of testimony read as a series`. It is the
   only consumer outside this file (`grep -rn "usage_history\|history_report\|usage_streaks\|
   deferral_candidates" --include=*.py .`), and its tier is unchanged (ADVISORY,
   `USAGE_HISTORY_BLOCKING`).

5. **THE THREE BITES ASKED FOR, by name** (in `selftest()`, block `#254`, `td5`):

   - **(b) clean read + wrap 2 is closing.** Fixture log carries `#60` twice: wrap 1 with
     `DEAD:R`, wrap 2 labelled `(second wrap; observed, self-report)` with `DEAD:U`. Bites:
     *"#254: a `(second wrap` testimony that DISAGREES with the first reads CLEAN — it is a
     second wrap of one session, not a falsification"*; *"#254: the CLOSING figure is the LAST
     wrap (wrap 2's `DEAD:U`, not wrap 1's `R`)"*; *"#254: both wraps are NAMED in the notes —
     the earlier one is kept, never dropped"*; *"#254: the REPORT says which wrap the column is
     — a silent pick between two honest testimonies is a false inscription by omission"*.
   - **(c) THE MUTATION.** The identical disagreeing pair with the label stripped:
     *"#254 MUTATION: strip the wrap label and the identical pair REFUSES again — the gate was
     NOT narrowed, a vocabulary was added (s253-D2)"* asserts the exact substrings
     `session #60 testifies DIFFERENTLY` **and** `cannot tell which. REFUSED.`; its partner
     *"…and the refusal message is UNCHANGED at wrap 1 — no `(wrap N)` qualifier leaks into the
     single-wrap case"* asserts no `(wrap ` appears anywhere in the refusal.
   - **Plus the collision at wrap 2**, which the brief did not ask for and the shape demands:
     *"#254: two testimonies at the SAME wrap index that differ still REFUSE, and the refusal
     NAMES the wrap"* — asserts `session #60 (wrap 2) testifies DIFFERENTLY`. Without this, the
     new key could hide a real falsification inside wrap 2.
   - Two label bites guard the parser itself: unlabelled → 1; `(second wrap; …)` → 2 and
     `3rd wrap` → 3, while `delegated OPUS wrap sub` → 1.

6. **(d) STEP 18 COULD NOT BE RUN IN ISOLATION, and that is a property of `_build_all.py`, not a
   skipped check.** `python3 knowledge/_build_all.py --help` documents exactly four argv forms:
   bare (full build), `--selftest`, `--range A-B`, `--resume [N]`. There is no `--only`.
   `python3 knowledge/_build_all.py --range 18-18` REFUSES:

   ```
   ❌ CHUNK REFUSED: no state at /var/tmp/_build_all_state.json — a pass must START at step 1, not 18
   ```

   Starting at step 1 would run `compliance/_build_compliance_kg.py`, which is the abort that
   strips the 41 compliance artefacts the brief forbids. So step 18 was identified by parsing
   `STEPS` with `ast` — index 18 is `("section-usage instrumentation selftest (#23)",
   "_gm_usage.py", ["--selftest"])`, with 17 = GM/LS mover selftest and 19 = lane records — and
   its command was driven verbatim and standalone. Step 18's content is green; the *harness*
   running it was not exercised.

7. **The help gate holds.** `python3 knowledge/_validate_help_gate.py` → `help-gate OK — 325
   script(s) scanned; every entry point answers --help before it can write.` Its `--selftest` →
   `OK — 5 bites`. `python3 knowledge/_gm_usage.py --help` still prints the docstring.

## RULING-SHAPED QUESTIONS

1. **Should the `MULTI-WRAP` fact reach the WRAP GATE, or stay in the `--history` table only?**
   Today it surfaces in `history_report`'s text (so anyone running `--history` or reading the
   probe's full report sees it), but `_capture_gate.usage_history_probe` summarises the report
   into three notes and the multi-wrap line is not among them — a wrap sub reading only the gate
   would not learn that #218's column is wrap 2. Option (a) leave it: the table is where a series
   is read, and the gate is not a series reader. Option (b) add one line to
   `usage_history_probe`'s notes (~5 lines in `_capture_gate.py`, a file this lane was not
   permitted to touch). **Recommend (a)** — the fact is about the RECORD, not about the wrap
   being gated, and (b) spends an edit in a gate file to repeat something the report already
   says. But it is a Dave call, not a lane's.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** that `_build_all.py` step 18 passes *as run by the harness*. Only the step's own
  command was driven. Price to prove: a full `--range 1-18` chunked pass, which rebuilds the
  compliance KG and is exactly what the brief forbids — so the gap is DECLARED, not closed.
- **CLAIMED:** that #218's two wrap events are a real event honestly recorded. Read from the
  `#218` block in `notes/_GAUGE-LOG.md` (the `★★ SECOND WRAP EVENT, 2026-08-25` banner and the
  `roll_2f` duplicate-key quote it carries), not independently re-derived from git history.

## Evidence

No evidence files: every claim above quotes its probe inline.

REPLAY-THESE: `knowledge/_gm_usage.py` lines ~420–480 (the `#254` comment block, `wrap_index`,
`_at`) (~1,400 tk) · `knowledge/_gm_usage.py` selftest block `td5` (~900 tk)
