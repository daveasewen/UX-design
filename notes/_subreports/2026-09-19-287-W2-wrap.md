# `#287`-`W2` — the delegated capture ritual for #287 → #288

provenance: 287 · 2026-09-19
status: observed

session: `#287` · opened and wrapped 2026-09-19 — ✅ **ONE DAY, NO DATE SPLIT** (`date` read at
this seat at the ritual's open: `Sat Sep 19 09:48:02 BST 2026`)
window: conductor **Fable 5.1**, this seat a delegated **OPUS 5** wrap sub
sub index: `W2`
brief: the conductor's #287 wrap brief — every figure it DECLARES is re-verified here rather than
restated, and where a measurement disagrees **both readings are published and neither is rewritten**
tokens: `UNMEASURED — this seat cannot read its own message.usage as a final total; the FILL figures below are the CONDUCTOR'S window, measured first-hand against his transcript, and are a different object`

## VERDICT

**DONE — every step of `knowledge/_RUNBOOK-capture-ritual.md` ran, in order: 1 · 1b · 2 · 2c ·
2c(i) · 2c(ii) · 2d · 2e · 2f · 2g · 3 · 4 · 4b · 4c · 4d · 5 · 5b.** ★★ **THE RULINGS STORE MOVED
FOR THE FIRST TIME IN SEVEN SESSIONS — `knowledge/_rulings.json` 620 → 622, verified here by
`json.load` over the `rulings` list with `s287-D1` and `s287-D2` both present and no duplicate.**
★★★ **FOURTEEN CHARACTERS SETTLED THREE CARRIES:** *"1. inscribe · 2. keep `sizes` and rebuild. ·
3. do it"*. ✅ **FOUR CARRIES STRUCK WITH RECEIPTS — the most this run — AND A FIFTH DELIBERATELY
NOT STRUCK because its headline stays true.** ⛔ **SIX blocking gate fails stood at the OPEN, all
inherited, carried in the `#243` DECLARED not-a-wrap form — the THIRTEENTH wrap running — and a
wrap may not repair them.** ⚠ **A SEVENTH WAS BORN MID-SESSION BY A LANE AND HEALED BY THE REMEDY
THE GATE ITSELF NAMED, measured both ways.** ★★ **THE SESSION'S HEADLINE IS THAT AN ACCEPTANCE TEST
WAS HONOURED: #286 wrote the condition that would upgrade its own finding, #287 ran exactly that
before cutting a lane, and the −7,031 connector lever is now a measured effect at n=2.**
⛔ **AND THE SESSION'S SUBJECT CHANGED IN THE AFTERNOON — #288 opens on a strand map and a path to
Friday the 25th, not on the work #287 was built to finish.**

COUNTS: findings `13` · ruling-shaped `13` · UNPROVEN `4`

## What was done

**Step 1 — `_LIVE-STATE.md`.** `Last refreshed` re-stamped **from `date`** (2026-09-19, Sat) with
**NO date split declared, because there is none**. #286's segment demoted to `Previous:`; **#283's
`Previous:` chain segment MOVED verbatim (7,680 B)** to `_LIVE-STATE-ARCHIVE.md` § Rolled
2026-09-19 #287. ★ **The verbatim guarantee is MECHANICAL rather than editorial:** the moved segment
is a **slice of the old line**, located by a marker search and never re-typed, and a post-condition
asserts the slice is absent from the projected replacement. ⚠ **Every pre-condition tests the
INPUT, not the output** — #285's guard tested its own projection and therefore fired on correct
behaviour, and that shape is deliberately not repeated. A 37-line ⏱ LATEST DELTA #287 written.
`python3 knowledge/_validate_standing_instructions.py` **PASS** (28 standing docs).
⚠ **`knowledge/_REVIEW-SIGNOFF.md` was NOT touched and the reason is stated rather than skipped:**
#287 produced no `reviews/*.html`, no proposal brief and no showroom pane awaiting Dave's eye — the
two reviews he ordered are #288's work, not artefacts sitting for sign-off.

**Step 1b — narrative dossier.**
`_DECISION-HISTORY/2026-09-19-287-the-rulings-land-and-the-strand-map-is-ordered.md`, provenance +
status fields, both-way links to the spine entry, the ledger (which for once is a real link — the
store moved), the handoff, his words and all five filed reports. Eleven sections; §1 is about a
discharge session becoming a direction-setting one, and §2 about why *"inscribe"* is the interesting
word rather than *"okay go on everything"*.

**Steps 2 / 2c / 2d / 2f — every move through `knowledge/_gm_move.py`,** from session-owned,
uniquely-named ops files asserted with `os.path.exists` + a size floor **in the writing process**
before the mover read them (`notes/_lanes/287/W2/run_ops.py`, which additionally **REFUSES an ops
file that is not session-owned**), **dry-run first**, receipts read back against the ops meant to be
run: `ops-287-ls-*.json` (**113,793 B / 5 ops**) · `ops-287-banner-*.json` (**4,176 B / 4 ops**) ·
`ops-287-roll2f-*.json` (**224 B**) · `ops-287-stratum-*.json` (**12,785 B**) ·
`ops-287-title-*.json` (**280 B**) · `ops-287-gen-*.json` (**1,819 B**).
`_roll_state.py` at the close: **2c OK (banners 2/2) · 2d OK (deltas 3/3) · 2f OK (strata 1, log
#286)** — ✅ **and the generator's line was substituted BYTE-IDENTICAL into the banner rather than
predicted**, which is finding 6.

⚠ **THE OPS FILES LIVE UNDER `notes/_lanes/287/W2/`, NOT `/tmp`**, for the two reasons #285 banked:
`s218-D7` clause 3 forbids session scratch for evidence, and `_seam.py`'s SCRATCH arm deletes a
lane's own `/tmp` working files mid-run **by design**.

**2c(i) — `_CARRIES.md` § `residual → #288`,** written by `notes/_lanes/287/W2/carry288.py`: every
age bracket bumped, **TWELVE** new items, and ⛔ **FOUR STRIKES**. Probe **551 → 560**; the banner's
PROBE command agrees at 560. ⚠ **All twelve new items are invisible to the probe** (`_AGE_RE`
misses `[NEW — 0]`), the TENTH wrap running; 551 → 560 is #286's nine ageing in, verified by
running the gate's own `_carry_items` against a two-line fixture rather than asserted.

**2e — NO-OP, 0 lines.** DO-FIRST minted no notice, tombstone or lifecycle tag; nothing was
retro-tagged.

**2g — `_build_memento_index.py` run LAST,** after every GM/LS edit was final;
`index_freshness_check` reads clean.

**Step 3 — WRITTEN FROM THIS SEAT, NOT A SEAT LIMIT, FOR A FOURTH CONSECUTIVE WRAP.** The hook was
filed FIRST at `notes/_lanes/287/WRAP-MEMORY-HOOK.md` and the result recorded **at its foot, after
the attempt**. Topic file `wrap-287-the-rulings-land-and-the-strand-map-is-ordered.md` (**8,701 B**,
version `8ab98481e8f4`); `index.md` took **three TARGETED `str_replace` edits** — the #287 line
prepended newest-first, the frontmatter `description` amended, and the closing note given the #287
suspension paragraph — **never a whole-file rewrite**, so the four existing wrap lines could not
drift. ⛔ **THE INDEX CUT COULD NOT BE MADE AND WAS NOT FAKED — see finding 10.**

**Step 4 — two rulings recorded.** `json.load` counts **622**, `s287-D1` and `s287-D2` present, no
duplicate; the inscription was lane I's through the sanctioned writer and is re-verified here rather
than restated. **4b —** both title lines GENERATED by `python3 knowledge/_gen_titles.py --session
287`, receipt at `knowledge/_gen_titles_receipt.json`; the forward title written into
`GOOD-MORNING.md` **through the mover, and COPIED out of the receipt's own line rather than
retyped**. **4c —** `_gate_scratch_hygiene.py --clean --wrap` run, three owned entries removed, VM
fill 53.9%. **4d —** `_render_rulings.py --check` reads **FRESH** (lane W re-rendered after
annotating; nothing staled it here).

## Findings

1. ★★★ **AN ACCEPTANCE TEST WRITTEN BY A SESSION THAT COULD NOT PASS IT WAS HONOURED BY THE NEXT
   ONE.** #286 published −7,031 as a *direction* and named exactly what would upgrade it: *"two
   readings within a few hundred tokens of each other would make it measured."* #287 took the second
   cold boot **first, before any lane was cut**: **74,120 against 73,832, Δ +288**, on a setup whose
   prior seven-reading spread was **11,526** wide. ⇒ **MEASURED EFFECT, n=2.** ★ **This is the
   cheapest form of rigour the record has — a finding that names its own upgrade condition, and a
   successor that runs exactly that.** ⛔ **And it changes nothing about the ceiling: the TWELFTH
   post-diet reading over 70,000, shrink-only, not a re-base.**

2. ★★★ **#286's REFUSAL TO SMOOTH A JOIN IS WHAT MADE TODAY'S RULING CHEAP.** Because #286 recorded
   that reading *"okay go on everything"* as ratification-as-written was the CONDUCTOR'S inference —
   labelled, and sitting next to the act it licensed — #287 did not have to reconstruct anything.
   The question went back in one line and returned as one word. ★ **Lane I then did the same thing
   again:** `s287-D1` carries the join on its own face, and `s287-D2` records that `sizes` was the
   lane's reading until *"keep `sizes` and rebuild."* made it his. ⇒ **A ruling that records which
   part of itself was an inference is the only kind that can be corrected without an archaeology
   dig.**

3. ⛔★★★ **THE CONDUCTOR FRAMED A FINDING AND DAVE CORRECTED THE FRAMING, AND BOTH STAND.** Lane X
   found that `layout/bento/gutter` (`s217-D2`) and dashboard `mainSpacing` (`s219-D1(5)`) disagree
   per theme in three of four themes. The conductor took that to him as *"two ruled spacing sources
   disagree."* His reply names two quantities rather than a contradiction: **the OUTER (structural
   bento) gutter and the INNER (embedded bento / tile-group) gutter, each deliberately per-theme.**
   ★ **The wrong framing was NOT deleted from `DAVE-RULINGS-2026-09-19.md`** — a correction that
   erases what it corrects leaves no evidence the correction was needed. ⛔ **What survives is real
   and narrower: the `s219-D3` generation arm was never built**, and `_bento_edit_rails.json` has
   declared itself `$groundwork_only` since #219.

4. ★★ **A CENSUS THAT ANSWERED THE QUESTION HE WAS ACTUALLY ASKING RATHER THAN THE ONE HE SPOKE.**
   *"We still seem to be writing a lot of inline styles"* has a cheap answer — **596** in the snippet
   corpus. Lane X classified the **whole** population: 62% defensible (data-driven bar lengths,
   token bindings, token-bound fills), the showroom's 138 pages carry **zero**, and
   `grep -rho 'style="--bento[^"]*"'` returns **zero hits repo-wide** against a rule stated in five
   places. ⇒ **The class that would actually hurt does not exist here**, and the actionable debt is
   **88 raw px/rem values with no `var()`**. ★ **"A lot of X" is a volume claim and the useful answer
   is almost never the volume.** ⛔ **No ruling on inline styles exists across all 622** — a
   convention obeyed everywhere and never inscribed.

5. ⛔★★ **TWO STATUSES SHIP FOR ONE ARTEFACT, AND THE METADATA PREDICTED IT.** The meta says
   **PROPOSED / NOT GATED / NOT RULED / NOT REGISTERED** and adds *"that is the serial's own
   behaviour, not a registration"*; `showroom/index.json` — the one status surface the composition
   skill tells builders to search — says **`beta`**, because the generator globs every snippet in.
   With *"a skip is a yes"* in that skill, the template is the **silent default** for any dashboard
   request. ⇒ **A consumer following the skill's own instructions cannot discover that the template
   is unratified.** ★ **A file that predicts its own mis-registration and is right is evidence the
   mechanism, not the author, is the defect.**

6. ⚠★ **A GENERATED LINE WRITTEN AT 2c TIME IS STALE BY 2f TIME, AND THE GATE CAUGHT IT.** The
   banner's `residual (GENERATED #N)` line was written while the tree still measured #286's rolls;
   `roll_claim_check` re-derived it through `_roll_state.py` — one measurer, no second slicer — and
   FAILED, quoting both. ★ **Correct behaviour by the gate, and the repair was to COPY the
   generator's current line in byte-identical rather than predict it.** ⚠ **And the substitution had
   to be anchored on a PAIR of lines**, because `> **residual (GENERATED #286):**` matches twice
   once the #286 banner becomes ★ PRIOR — the record must not move while the live line does.

7. ⚠★ **TWO LINES IN THIS WRAP MEASURE REGIONS THAT CONTAIN THEM, AND BOTH WERE ITERATED TO A FIXED
   POINT RATHER THAN PUBLISHED AT A PRE-WRITE READING.** The `section-sizes` line converged in 2
   passes; the declare-last `size:` stamp in 2 (GM 35,230 → 35,238). ★ **The #240 one-wrap-two-
   figures lesson, mechanised in `ops287_sizes.py` and `ops287_stamp.py` rather than remembered.**
   ⚠ **DECLARED DEVIATION: those two substitutions were whole-file writes, not mover ops** — the
   mover has no iterate-to-fixed-point op, and each is a fill of this wrap's OWN placeholder in this
   wrap's OWN stratum, not a move of anyone's text. The §A digest was asserted unchanged
   (`4311cce4…`) before and after.

8. ⚠★★ **THE `subs` CONVENTION AND THE FILL RULE ARE TWO DIFFERENT QUANTITIES, AND THE GAP
   RECONCILES TO THE TOKEN.** `_checkin.read_fill` — input side only, the one definition of FILL
   this repo has — gives **568,179** across the five lanes. The conductor's declared **573,531** is
   each seat's last-turn input side **plus that turn's `output_tokens`**, the convention this log has
   carried since #284. **The 5,352 gap is entirely the five lanes' final output, and it matches on
   every one of the five.** ⇒ **Both published, neither rewritten, and which one the line means is
   his** [[measure-dont-convert-units]]. ★ **The conductor's FILL declarations for his own window ARE
   `read_fill` readings and agree to the token** — his 180,375 is exactly turn 23's input side and
   his 74,120 exactly the first turn's — **so the split is confined to one line, and those two
   independent agreements are what identify the window as his.**

9. ⚠★ **THREE LANES MADE JUDGMENTS THEY COULD HAVE DEFERRED, AND EACH DECLARED ITSELF.** Lane C
   wrote **three other lanes' `_state.json` rows** through the module API rather than shipping
   `DOC_ROW_ACK` and *"three invisible documents"*; lane C **measured** that the dashboard's
   9,590-line diff is mostly inherited (**11,617 lines move from the UNCHANGED HEAD store alone**);
   and lane K's un-deletable 4.3 MB before-copy went to `_to_delete/` by the #284/#286 `mv`
   precedent with `.gitignore` untouched. ★ **A judgment named in a report is a judgment the
   conductor can correct; a judgment discovered at the wrap is one he inherits.** ⛔ **Ruling-shaped
   and not inscribed: `gen_dashboard.py` has no `--check` arm**, which is why the staleness was
   invisible until a lane happened to regenerate.

10. ⛔★★ **THE MEMORY INDEX CUT COULD NOT BE MADE FOR A SECOND CONSECUTIVE WRAP, AND IT WAS NOT
    FAKED.** `MEMORY-ARCHIVE.md` still stands at **48,990 of its 49,152 B cap — 162 B of headroom**
    — and either line that should have moved runs to thousands of bytes. ⛔ **Nothing was truncated,
    because a truncated verbatim move is not a move**, so `index.md` deliberately carries **FIVE**
    wrap lines and says so in its own frontmatter and closing note. ⚠ **How that was established is
    stated plainly rather than dressed up as a refusal that was never received: arithmetic on two
    measured quantities, not a rejected write.** No append was attempted, because the only way to
    obtain the archive's version token is to read all 48,990 B into this seat, and a failure whose
    arithmetic is certain is not worth that price. ★ **Nothing is at risk** — the repo is the record.
    **What to do about the archive is Dave's, and the dream pass is its usual seat.**

11. ✅★ **THE BANNER FIT THE CAP ON THE FIRST PASS AND THE REASON IS WORTH NAMING: FOUR CARRIES
    CLOSED.** 1,145 of the `s241-D2` 1,200 over 10 of 10 substantive lines, measured with the gate's
    own instrument and with the generated line substituted before measuring. ★ **A wrap whose
    session DISCHARGES items has room in this idiom; one whose session only accrues does not.**
    ⛔ **The standing warning is not withdrawn, and the cap is Dave's.** ⚠ **And the chain still
    grew — 10,821 → 11,188 — while the banner shrank**, because the ⏱ LATEST DELTA is the chain's
    other half and is the sole home `s241-D2` gives gauge and declared-skip detail. Both published.

12. ⚠ **THE STORE'S ID REGEX REFUSED A FOURTH ID IN THREE SESSIONS, AT THIS SEAT, AND THE REFUSAL IS
    THE RECEIPT.** Writing this report's own row as `W-287w2` was **REFUSED by `_state.add`** —
    verbatim: *"REFUSED: W-287w2: id does not match `^(?:W-[0-9]{1,3}[a-z]{0,2}|G[0-9]{1,2}[a-z]?)$`"*
    — because the pattern allows at most two trailing lowercase letters and **no digit after them**.
    The row is `W-287ww`. #285 met it twice (`W-285lm2`, `W-285c2`) and #286 once (`W-286r2`), and
    the carry recorded it as *"third instance in two sessions"*; this is the **fourth in three**, and
    the first met by a wrap seat rather than a lane. ⛔ **A finding, not a fix — an id scheme is a
    convention and widening one at a wrap is not a wrap's call.** ★ **The refusal is worth more than
    the workaround: the gate quoted its own pattern back, which is what let the rename be one
    character rather than an investigation** [[gate-must-quote-what-it-forbids]].

13. ⚠ **WRITING THAT ONE ROW RE-STALED TWO GENERATED SURFACES, EXACTLY AS LANE C RECORDED IT WOULD.**
    `knowledge/_state.json` moved **709 → 710** items, which put `dashboard/index.html` out of sync
    with its own source again and re-staled `_CHAIN.md` (the chain prints the item counts). Both
    were regenerated by their own generators — `gen_dashboard.py` and `_gen_chain.py`, neither
    fenced — and re-checked green. ★ **Lane C named this coupling in its own report three hours
    earlier, which is the only reason it was expected here rather than discovered.** ⛔ **It is also
    the second independent argument for finding 9's missing `--check` arm: a file that nothing
    grades goes stale on every unrelated write.**

## RULING-SHAPED QUESTIONS

Every one is a **question put**, never a state of the world (`s271-D4`). Bodies and receipts are in
`_CARRIES.md` § `residual → #288`; **none was ruled here.**

1. **The strand map and the Friday-25th path** — and where the presentation strand actually lives.
2. **Mono's 0, and building the `s219-D3` generation arm.**
3. **The bento template's QUALITY.**
4. **An inline-style rule** — 88 raw values, and `DEF-004`'s population.
5. **The template's two statuses** — and whether `showroom/index.json` can express *proposed*.
6. **Which definition the `subs` line means** — input-side or input-plus-output.
7. **A freshness check for `dashboard/index.html`.**
8. **`_to_delete/` accumulating on a persistent disk.**
9. **`INSEAT_WARN_TK = 10,000`** and the INSEAT block's placement.
10. **The delegation rule** — obeyed a third session, still uninscribed.
11. **The memory archive at 48,990 of 49,152 B.**
12. **The two-probe-shape question on the §A digest** — its nineteenth session.
13. **What should a session DO at the hard wall?** — eighth wrap running.

*(Also carried, unchanged and not re-minted: whether a verifier lane may mutate the tree · the
store's id regex · a RESUME contract for the ritual · `_seam.py`'s SCRATCH arm and its four printed
"HARD LINE BREACHED" strings · the third dial · the theory door · `col26-012` · `jade-lifestyle` ·
`s277-D12` · the four generators · the git-lock runbook line · the instrumentation-append policy ·
the `#243` not-a-wrap form · `_HANDOFF-130`…`-137`'s open items.)*

## Step 5 / 5b — the commit, the push, and the CI read

Recorded in full in the ★ LATEST banner's `COMMIT STATE #287` block and summarised in the returned
stub. ⛔ **The hash is a declared gap in that block — a commit cannot name itself** — and the sha,
the push verdict line and the CI read are quoted literally in this report's addendum below, in the
5b addendum on the ⏱ LATEST DELTA, and in the returned stub.

⬛ **5b, the addendum, is homed in the ⏱ LATEST DELTA of `_LIVE-STATE.md`, not on the banner** — the
`s241-D2` cap is BLOCKING at 10 lines / 1,200 tape and this banner closed at **1,145 / 10**, so an
eleventh line would be refused, correctly. The cap's own words settle it (*"the ⏱ LATEST DELTA is
the sole home for gauge / declared-skip / not-done detail"*), and the delta is IN the read chain.
⛔ **The 5b-vs-cap conflict is ruling-shaped, Dave's, and carried — this is the SEVENTH wrap to meet
it and the SIXTH to answer it this way.**

⛔ **AND THE `s271-D4` RE-READ RAN AS THE RITUAL'S FINAL BEAT.** The hook's "Open, Dave's" list was
re-read against `knowledge/_rulings.json` after 5/5b. **Nothing was struck, and the reason is
construction rather than luck:** the two rulings this session inscribed (`s287-D1`, `s287-D2`) close
items that were struck from the carry set *before* the hook was written, so no item in the hook's
list is closed by a #287 ruling. Every one of the eleven is a question he has not answered.

### THE ADDENDUM — sha, push verdict, CI

> **WRAP COMMIT:** `{{SHA}}`
>
> **PUSH VERDICT, verbatim:** `{{PUSH}}`
>
> **CI:** `{{CI}}`

## UNPROVEN / CLAIMED (ADR-0016)

- **CLAIMED, not measured: the `replay` term** of the `wrap-handover` line. What reading this report
  back costs the conductor is not observable from inside the sub; written `unobservable
  (conductor-side, post-wrap)`, and the conductor may append the measured figure at the #288 opener.
- **The memory-archive refusal was NOT received — it was DERIVED**, for the second consecutive wrap.
  Finding 10 rests on arithmetic over the store listing's byte count and the documented cap, not on
  a rejected write. **It is stated as a derivation, and the write that would prove it is named and
  un-run.**
- **The `subs` figure is a CONVENTION, and this seat's own spend is EXCLUDED rather than estimated.**
  573,531 is the sum of five last-turn readings under the input-plus-output convention; 568,179 is
  the same five under `read_fill`. The wrap sub's own transcript is a sixth and is left out because
  its total cannot be final from inside itself; **no placeholder is written for it**
  [[feedback-measuring-tool-must-not-guess]].
- **The `s219-D3`-arm and template-quality scopes are DESCRIBED FROM LANE X'S REPORT, not
  re-verified here.** This seat did not open `canon.css`, the meta or the rails file; every file:line
  in the handoff's afternoon sections is lane X's reading, cited to its report, and the wrap adds no
  claim of its own about them.

## Evidence

`notes/_lanes/287/W2/` — the ops files, the builders (`carry288.py`, `banner287.py`, `ops287_*.py`,
`run_ops.py`, `delta287.md`, `stratum287.md`) — and `notes/_lanes/287/WRAP-MEMORY-HOOK.md`, whose
foot records what actually happened at the store.

REPLAY-THESE: `_HANDOFF-138-the-rulings-land-and-the-strand-map-is-ordered.md` (~5k tk) ·
`notes/_lanes/287/DAVE-RULINGS-2026-09-19.md` (~1k tk — his words, and the correction beside the
framing it corrects)
