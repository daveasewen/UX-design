# #221 LANE D — priced-never-built machinery debts

**Seat:** Opus sub, Lane D of the #221 repair wave. **Region:** `knowledge/_capture_gate.py`
(EXCLUSIVE) · new instrument code I create · `knowledge/_state.py` READ-ONLY.
**Sources:** `notes/_briefs/2026-08-27-221-repair-wave-divvy.md` (LANE D) · origin receipt for
five of the six items is `notes/_briefs/2026-08-24-218-gates-wave-brief.md` · S1-5 of
`notes/_subreports/2026-08-27-220-replay-discharge.md`.

COUNTS: findings `7` · ruling-shaped `4` · UNPROVEN `5`

**COUNTS (the #221 brief's shape, carried beside the store's):** 6 taken (+1 found in-region) ·
4 fixed-at-cause · 31 bite arms added · 11 mutations driven both-ways · 1 declined-with-reason ·
5 UNPROVEN.
⚠ Both lines are here on purpose: `_capture_gate.py::SUBREPORT_COUNTS_RE` parses only the first
form, and the #221 divvy asked for the second. Home by addition rather than pick one and fail a
gate [[home-by-addition-then-cut]].

> ⛔ **THE HEADLINE, AND IT IS A PREMISE FINDING, NOT A BUILD FINDING.** Four of my six items were
> ALREADY BUILT — at #218's gates wave and #211's W-92 lane — and one of the four was built
> *because the same brief text was handed to that session too*. The #221 divvy re-issued the
> #218 gates-wave brief nearly verbatim without replaying it. I replayed all six at HEAD before
> touching anything [[premise-ages-faster-than-rule]]; receipts below. Only ONE of the six
> (the regen-serial check) had never been built.

---

## FINDINGS

### ① Boot-drift parser — **PREMISE STALE, fixed at #218. No edit made.**
Brief: *"`_parse_boot_samples` refused a prose phrase carrying a delta (#215 case)."*
**REPLAYED on the live log:** `GOOD: 78 · REFUSED: 0 · DELTAS: 5`. The #215 sentence
(`notes/_GAUGE-LOG.md:2223`, "the brief declared boot drift of 2,345 over the band") is
classified as a DELTA, not a reading and not a refusal — correct on both counts.
The parser carries the three-shape split (`boot drift/delta of N` · `boot ±N` · `boot N
over/under/outside`) at `_capture_gate.py:3698-3763`, and the bite the brief asks for **already
exists with the #215 line VERBATIM** — `selftest_boot_delta_parse()`, two arms: the #215
post-mortem line must read `60,248` and refuse nothing, and the #215 premises line must be a
DELTA `[2345]` "and nothing else". A third arm carries the #218 line verbatim.
⇒ Nothing to fix. The delta values the parser reports today (`2,345 · 8,942 ×2 · 9,272 ×2`) were
each traced back to a real `boot drift of N` phrase in the log — no false positives.

### ② The #158 write-door — **PREMISE STALE at the named door; a REAL residual found and fixed.**
Brief + GLOBAL PITFALL: *"`--selftest` WRITES `knowledge/_CAPTURE-GATE.md` … restore it after
every run."* **This is false at HEAD, and every lane was told to work around a door that is shut.**

**DRIVEN THREE WAYS on the real tracked file across a complete `--selftest`:**
- md5 `704ad5b908b04ce73ffa01126051c2b6` **identical** before and after;
- mtime **unmoved** (`1787603195`);
- `git status --porcelain knowledge/_CAPTURE-GATE.md` **empty**;
- plus a **filesystem-hook probe** (monkeypatched `open(w/a/x/+)`, `os.remove/unlink/rename/
  replace`, `shutil.copy/copy2/move`) around the whole suite: **`PROBE VERDICT: 0 python-level
  hit(s)`** on that path.

⚠ **A false alarm of my own, declared, because it is the more useful half of this finding.** My
first probe reported the file ABSENT mid-run. It was my harness, not the repo: I wrote
`cd "$R" && nohup … &`, and `&` backgrounds the **whole `cd … && …` list**, so the polling shell
never left `/sessions/eloquent-zealous-bohr` and every *relative* path read absent. Re-driven with
absolute paths: PRESENT throughout, md5 stable. **An unmatched path is not an absence**
[[unmatched-grep-is-not-an-absence]] — and it very nearly became this lane's headline.

The door was closed at #218 by the **argv contract** (`argv_refusal()`): `--build` is the only
writing argv, a bare run refuses `REFUSED (write-gate)`, any unknown flag refuses
`REFUSED (argv contract)`.

**THE REAL RESIDUAL — the third leg, and it is the one the class statement actually names.** The
argv contract closes the doors that EXIST; it cannot close a door a future arm OPENS. `selftest()`
calls `run()` many times, and nothing but an author's memory stops an arm passing `report=REPORT`
(a copy-paste from the `--build` arm) and landing a transient verdict in the committed report.
**FIXED AT CAUSE:** module flag `_SELFTEST_ACTIVE`, armed in `selftest()` (not in `_selftest_body`,
so every arm added later is covered *by construction*), plus a pure-function guard
`selftest_write_veto(report)` consulted by the report writer in `run()`. Refuses **loud and named**
(`REFUSED (selftest write-door)`), never a silent skip.
⇒ **The class is now impossible, not merely unlikely** [[gate-dont-patch]].

### ③ The regen-serial check — **THE ONE GENUINE GAP. BUILT.**
`grep` for `regen.serial|regen_serial|ramp first|index last` across `knowledge/**/*.py` returns
**one comment line and no machinery**; the rule lives only as prose, restated by hand in
**three** conductors' briefs (`…-213-…`, `…-217-…`, `…-221-…`). A rule every conductor must retype
is a rule with no instrument.

**BUILT:** `regen_serial_check(repo, sha=None)` + `_regen_serial_positions` + `_regen_owner` +
`_regen_owned_map`. **ADVISORY AT BIRTH** (`REGEN_SERIAL_BLOCKING = False`), wired into
`wrap_checks` WARNS.

★ **Nothing is typed.** Serial ORDER is **`ast`-parsed** out of `_build_all.py::STEPS` — its one
home, ADDRESSED not copied (ADR-0017). ⛔ Parsed, never imported: executing `_build_all.py` to
learn its own step order would put the build's module-level code on the wrap path. Artefact
OWNERSHIP is read from each artefact's own generation banner, and **believed only when the script
it names is itself a serial member** — `_REVIEW-SIGNOFF.md` really does carry the prose sentence
"generated review inherits them", and a looser rule would eat it.

Three arms, and the second one is the honest part:
- **MEMBERSHIP** — the wave changed artefacts owned by members i…j and skipped one INSIDE that
  span. This is the #210 shape exactly.
- **TERMINATION** — `index last`. ⛔ **Deliberately narrow:** it asks about `_build_memento_index.py`
  (the member the standing ritual names, step 2g) and **nothing else**. The obvious generalisation
  — "the highest-positioned artefact-owning member" — is WIDER THAN THE RULE, and I removed it
  after it fired on `_build_instrument_fit.py` [[gate-glob-scope-rule]].
- **ORDER** — mtimes only testify while the wave is **uncommitted**; once committed they are
  checkout noise, so the arm **DECLARES ITSELF UNOBSERVABLE** rather than grade on rubbish (#173).

⚡ Cost: the ownership map is a ~1,400-file header sweep (~8 s), so it is **lazy** — the cheap
question ("is any *changed* path claimed?") is asked first, and a wrap that regenerated nothing
stops before the sweep.

**DRIVEN ON TODAY'S REAL DATA, and it caught something live** (`--wrap --lane`):
> `⚠️ WARN REGEN SERIAL (advisory): this wave regenerated `_validate_radius.py` (step 57) alone
> but did NOT re-run `_build_memento_index.py` (step 109), which the capture ritual runs LAST`

That is a **true catch on a parallel lane's in-flight work** (`knowledge/_RADIUS-GATE.md` is dirty
at HEAD). ⚠ It is a MID-WAVE reading and is *expected to discharge* when the conductor runs the
serial before committing — it is evidence the instrument works, **not** an accusation.

### ④ Register↔store join — **PREMISE STALE, built at #218. Driven on today's real data.**
`governing_records_join_check` exists, is wired ADVISORY into `wrap_checks`, and carries a
7-arm selftest including a tier pin. **DRIVEN on the live tree:**
> `17 register row(s) joined against 18 _state.json row(s) — ids and statuses AGREE (1 declared
> gap(s))` · `G18` reported as a DECLARED gap, which passes.

The three drifted rows the brief cites (#212: G3/G7/G8) are **repaired** — 0 warns today.

### ⑤ `mask_comments` dedup — **PREMISE STALE (one home since #211); the COMPARER built.**
There is exactly **one** `def mask_comments` in the tree (`knowledge/_htmlmask.py:32`); both
generators `import` it and each already asserts `mask_comments.__module__ == "_htmlmask"`.
No duplication exists, so there is nothing to compare and **no generator was touched**.

⛔ **But those two bites are asserted BY the two known consumers, ABOUT themselves.** A THIRD file
writing its own `def mask_comments` tomorrow is invisible to both — the duplicate-home defect
re-opening **by addition rather than by edit**, which a per-consumer assertion structurally cannot
see [[green-tests-cannot-see-scope]]. **That** is the residual, and it is what the brief's
"advisory comparer" is for.

**BUILT:** `shared_helper_dedup_check(repo)` + `SHARED_HELPERS = {"mask_comments": "_htmlmask.py"}`.
ADVISORY at birth (`SHARED_HELPER_BLOCKING = False`), wired into `wrap_checks` WARNS. Counts
implementations tree-wide, names every consumer reaching the helper by a non-home route, and
reports unparseable files as UNKNOWN rather than clean.
⚠ **Parsed with `ast`, never grepped** — a `def mask_comments` inside a docstring must not read as
a duplicate, and that trap is its own bite arm.
⛔ **It compares only.** Which implementation survives a real duplicate is a refactor with a ruling
attached; the gate names the duplicate, it does not pick the winner.

**DRIVEN ON REAL DATA (1.34 s):**
> `SHARED HELPER: mask_comments() has exactly ONE implementation (knowledge/_htmlmask.py) and
> 3 importer(s) across 271 tracked .py file(s)`

### ⑥ INSTRUMENT-STRAY re-scope — **PREMISE STALE, re-scoped at #218 (seam 1, `adb5130`). No edit.**
`COMMITTED_SURFACE_DIRS` (`:4026`) carries `knowledge/assets/photography-web` with its `product`
pattern, its ruling (`s217-D1`) and its generator, and `instrument_stray_check` grades three things
separately: `.uuid*` = FAIL **anywhere, committed surface included** · a ruled product = NOTE ·
anything else under the surface = FAIL. `selftest_instrument_stray()` drives **both directions**,
including "`.uuid` INSIDE the committed surface must still fail". Ruled-legal passes, #138 still
fires. Nothing to fix.

### ⑦ **NOT ON MY LIST — found while filing this report, in my region. FIXED AT CAUSE.**
`SUBREPORT_COUNTS_RE` demanded **bare digits**, while `notes/_subreports/_TEMPLATE.md:45` — the
skeleton every sub is told to copy — prescribes the figures in **backticks**. So a sub that obeyed
the instruction produced a line the gate called unparseable.

**MEASURED across `notes/_subreports/`: 37 of the 44 filed reports carrying a COUNTS line FAILED
the parse — the four #220 audit reports among them.** The advisory warn has been saying "carries
no parseable COUNTS line" about reports whose COUNTS line was correct. I found it because *this*
report failed it, which is the only reason anyone would.

**FIXED ON THE GATE, not on 37 reports** — where a queue and its canon disagree, the defect is the
side that is not the instruction. Two decoration classes now parse: backticks/bold around the word
and the figures, and **a trailing continuation** after the third field (`… · UNPROVEN 5 · new
gates 19`), which many lanes write and which leaves all three required figures present and in
order. **Parse: 7/44 → 24/44.**

⛔ **AND NO FURTHER, DELIBERATELY.** A third form exists — `COUNTS: 14 findings · 3 ruling-shaped`,
the figure *before* the noun, worth 3 more reports — and I did **not** accept it. That is a change
to the contract's **vocabulary**, not its decoration, and "do not loosen it blind" is the #218
lesson on this very check. Filed as a ruling-shaped question instead.

Bitten both ways in `selftest_subreport_citation`: four legal forms (template's own first) must
parse, four malformed ones (missing field · non-numeric · out of order · comma separators) must
still fail. Mutants: old-strict pattern → 3 reds · `anything containing COUNTS` → 5 reds ·
restored → 0.

⚠ **The residual 20 are a real contract divergence, and it is the conductor's to resolve:** the
#221 divvy's own REPORT CONTRACT prescribes `COUNTS: (items taken · fixed-at-cause · bites added
· mutations · declined · UNPROVEN)` — a **different field set** from the template and the gate.
That is why this report carries **both** lines. Briefs and skeleton should be reconciled to one
home (ADR-0017).

---

## RULING-SHAPED QUESTIONS

1. **Promote either new check to BLOCKING?** Both are born ADVISORY per Fence 2. Tiers live at
   `REGEN_SERIAL_BLOCKING` and `SHARED_HELPER_BLOCKING`; each has a **tier-pin bite** that goes RED
   if an agent flips it. Promotion is your word.
2. **The two M10 chain-budget selftest failures — DECLINED, with a MEASURED reason (see below).**
   The real `_CHAIN.md` is over its Dave-ruled budget. Re-dialling `CHAIN_BUDGET_TK`, arming the
   block tier, or retiring it is yours alone; the code says so at `:1075`.
3. **`_state.json` row owed (Fence 3 — I do not mint).** No new FILE was created, so the #185
   forgotten-document gate is not triggered; but two new advisory instruments now run every wrap.
   If the store carries instrument rows, they want rows. **Conductor's to mint.**
4. **Does the COUNTS contract admit `14 findings` as well as `findings 14`?** (finding ⑦.) I fixed
   the DECORATION and stopped at the VOCABULARY. Accepting the reversed order buys 3 more reports;
   refusing it means 3 lanes rewrite one line. **And the wider question underneath it:** the #221
   divvy prescribes a COUNTS field set the template and gate do not accept — which of the two is
   the home? One of them should be changed, not both tolerated.

---

## DECLINED, WITH REASON — S1-5's three capture-gate selftest failures

The 3 failures reproduce **exactly** as the replay-discharge recorded (baseline re-run today,
unchanged after all my edits). I DECLINE all three, and I replaced the inherited "ruling-shaped"
label with a **measurement** [[planning-estimate-is-not-a-measurement]]:

- `M10: a fat §A/§C warned the CHAIN` and `M10: an ordinary chain warned` — **not code defects.**
  `check_budgets(td)` derives the chain via `_gen_chain.build(td)`, and the fixture supplies only
  `GOOD-MORNING.md`, so the measured chain is **12,517 tape** for the "ordinary" fixture and
  **12,520** for the fat one — both **PAST the block-candidate 10,000** (warn 7,700). Both bites
  assert *no* chain warn, so **they cannot pass in this environment** — the #173 class.
  ⇒ Two real fixes exist: **(a)** give the fixture its own chain inputs so the bite tests the
  CLAUSE not the live repo — this needs `_gen_chain.py`'s input set and is **OUTSIDE my region**
  (handoff below); **(b)** move `CHAIN_BUDGET_TK` — **FORBIDDEN**, Dave-ruled (`s212-D11`), Fence 4.
- `#70/#71 non-catch: _gen_chain.py --selftest is NOT green` — `_gen_chain.py` is outside my region.

**HANDOFF:** fix (a) above belongs to whoever owns `_gen_chain.py`. It would clear 2 of the 3
standing capture-gate selftest failures without touching a ruled value.

---

## REPLAY-THESE

REPLAY-THESE: `knowledge/_capture_gate.py --selftest` (expect exactly 3 pre-existing reds, tracked
report md5 unchanged) · `selftest_regen_serial()` + `selftest_shared_helper_dedup()` (expect 0/0,
then flip each tier and expect 1 each) · `selftest_write_veto()` driven as a pure function
(outside=None · tracked=refusal · scratch=None) · `regen_serial_check('.')` and
`shared_helper_dedup_check('.')` on the live tree · `_capture_gate.py --wrap --lane` (both new
lines must land in WARN/NOTE, never FAIL) · the DECLINED M10 measurement (~12,517 tape fixture
chain vs the ruled 10,000 block-candidate).

```sh
cd /sessions/eloquent-zealous-bohr/mnt/UX-design

# ① boot parser: the #215 premise, replayed. Expect GOOD 78 · REFUSED 0 · DELTAS 5.
python3 -c "import sys;sys.path.insert(0,'knowledge');import _capture_gate as c;\
g,r,d=c._parse_boot_samples(open('notes/_GAUGE-LOG.md',encoding='utf-8').read());print(len(g),len(r),len(d))"

# ② write door: prove it is SHUT. md5 must be identical and git must stay clean.
md5sum knowledge/_CAPTURE-GATE.md
python3 knowledge/_capture_gate.py --selftest > /var/tmp/v.txt 2>&1
md5sum knowledge/_CAPTURE-GATE.md; git status --porcelain knowledge/_CAPTURE-GATE.md
grep -cE "❌ selftest" /var/tmp/v.txt      # expect 3 (all pre-existing, named in S1-5)

# ②b the veto clause, driven as a pure function (no write needed, by design)
python3 -c "import sys,os,tempfile;sys.path.insert(0,'knowledge');import _capture_gate as c;\
print('outside:',c.selftest_write_veto(c.REPORT));c._SELFTEST_ACTIVE=True;\
print('tracked:',bool(c.selftest_write_veto(c.REPORT)));\
print('scratch:',c.selftest_write_veto(os.path.join(tempfile.gettempdir(),'x.md')))"
# expect: outside=None · tracked=True · scratch=None

# ③ + ⑤ the two new selftests, and their mutants (both directions)
python3 -c "import sys;sys.path.insert(0,'knowledge');import _capture_gate as c;\
print('regen',len(c.selftest_regen_serial()),'shared',len(c.selftest_shared_helper_dedup()))"
# expect 0 0. Then flip a tier and watch the pin bite:
python3 -c "import sys;sys.path.insert(0,'knowledge');import _capture_gate as c;\
c.REGEN_SERIAL_BLOCKING=True;print('tier-flip failures:',len(c.selftest_regen_serial()))"
# expect 1

# ③ + ④ + ⑤ driven on REAL data
python3 -c "import sys;sys.path.insert(0,'knowledge');import _capture_gate as c;\
print(c.regen_serial_check('.'));print(c.shared_helper_dedup_check('.'));\
print(c.governing_records_join_check('.'))"

# the live wrap: new lines must appear in WARN/NOTE, never in FAIL
python3 knowledge/_capture_gate.py --wrap --lane 2>&1 | grep -E "REGEN SERIAL|SHARED HELPER"

# ⑦ the COUNTS parse: the template's own line must parse; malformed ones must not.
python3 -c "import sys,glob;sys.path.insert(0,'knowledge');import _capture_gate as c;\
print('template form:',bool(c.SUBREPORT_COUNTS_RE.search('COUNTS: findings \`18\` · ruling-shaped \`5\` · UNPROVEN \`4\`')));\
print('out of order :',bool(c.SUBREPORT_COUNTS_RE.search('COUNTS: ruling-shaped 5 · findings 18 · UNPROVEN 4')));\
print('filed parsing:',sum(1 for p in glob.glob('notes/_subreports/*.md') if c.SUBREPORT_COUNTS_RE.search(open(p,encoding='utf-8').read())))"
# expect True · False · 24   (was 7 before the fix)
python3 -c "import sys;sys.path.insert(0,'knowledge');import _capture_gate as c;\
print('citation selftest failures:',len(c.selftest_subreport_citation()))"   # expect 0

# the DECLINED M10 measurement
python3 -c "import sys,os,tempfile;sys.path.insert(0,'knowledge');\
os.environ['CAPTURE_GATE_NO_REAL']='1';import _capture_gate as c;\
d=tempfile.mkdtemp();print([x[:120] for x in c._warns_for(d)[1] if 'M10 read chain' in x])"
```

**Mutations driven both ways (9):** regen-serial membership-blind → 2 reds · order-blind → 1 red ·
fires-on-everything → 7 reds · tier flipped → 1 red · shared-helper dup-blind → 1 red ·
grep-shaped → 5 reds (incl. the docstring trap) · restore → 0 in every case. In-fixture
plant/remove pairs: membership skip→re-run, order inversion→in-order, duplicate def→removed.

---

## UNPROVEN (honest, priced)

1. **The regen-serial check has never met a real, complete regen wave.** No serial ran today. It
   is proven on fixtures and on a partial live tree; its behaviour across a genuine ramp→index
   wave is UNPROVEN. Price: ~0, it will be observed at the conductor's next regen wrap.
2. **The ORDER arm's mtime half has never been observed on a real uncommitted wave** in this repo
   — fixtures only. It declares itself unobservable today, which is the honest state, not a pass.
3. **Wrap-time cost not measured on CI hardware.** Locally the sweep is ~8 s and lazy (skipped when
   nothing was regenerated); the ownership map is ~1,400 file headers. UNMEASURED elsewhere.
4. **`_build_all.py::STEPS` was parsed as it stands mid-wave.** Lanes A–C are editing the tree
   concurrently; if a lane adds STEPS entries, positions shift. The parse is re-run every wrap, so
   this self-corrects — but my quoted step numbers (57 / 109 / 125) are a MOMENT, not a constant.
5. **The provenance-banner population is what the banners say it is.** 49 generators claim
   artefacts under the loose marker; only those also in STEPS are believed. A generator that
   writes NO banner owns nothing as far as this check can see, and would be silently outside the
   membership arm. Priced as the next widening, not fixed today.

---

## FILES TOUCHED (for the conductor's reconcile)

- `knowledge/_capture_gate.py` — **the only file edited.** `+745 / −3`. Compiles; full
  `--selftest` re-run after every edit; baseline of 3 pre-existing failures **unchanged**, no
  refusals, and the tracked `knowledge/_CAPTURE-GATE.md` md5-identical and git-clean throughout.
- `notes/_subreports/2026-08-27-221-laneD.md` — this report (new).

**No other path was written.** No commit, no checkout, no push. `knowledge/_rulings.json`,
`knowledge/_state.json`, `_build_all.py` and every ruled value **untouched**; `_state.py` read-only;
`_build_all.py` **never run** (parsed with `ast`, never imported or executed).

**Hunk map — three separate, revertable fixes** (`git diff -U0 knowledge/_capture_gate.py`):

| Fix | Hunks (new-file line anchors) | What |
|---|---|---|
| ③ regen-serial | `+4234` (check, first ~230 lines of the block) · `+4808` (wire) · `+7864` (`selftest_regen_serial`) · `+8500` (aggregator line 1) | new advisory check |
| ⑤ shared-helper | `+4234` (comparer, remainder of the block) · `+4808` (wire) · `+7864` (`selftest_shared_helper_dedup`) · `+8500` (aggregator line 2) | new advisory comparer |
| ② write-door veto | `+5149` (`run()` write site) · `+7508`/`+7518` (`selftest()` arms/disarms) · `+8614` (`selftest_write_veto`) · `+8763` (argv-contract arm) | class fix |
| ⑦ COUNTS parse | `SUBREPORT_COUNTS_RE` + its comment block (~`:4604`) · the COUNTS arms at the head of `selftest_subreport_citation` | parser fix at cause |

⚠ Fixes ③ and ⑤ share the `+4234` and `+4808` hunks (adjacent insertions); split by function name
if they must be committed separately.

**USAGE:** UNMEASURED — declared. This seat cannot read its own `message.usage`; no figure is
invented [[feedback-measuring-tool-must-not-guess]].
