# #300 · Lane P — the boot-diet plan page (synthesis)

provenance: 300 · 2026-09-23 · lane P, second launch (Opus 5.5 sub; cloud container + Dave's Mac through device_bash) · built from reports A, B, C, D and the adversary X · read-only on the repo apart from the page, this report and `notes/_lanes/300/P/` · no Project-memory calls of any kind · no git, no commits
page: `notes/_PLAN-300-boot-diet-2026-09-23-v1.html` (72,041 B at filing, md5 `7414d500b77d345307540021e22118ad`, verified on the Mac after the last copy)
renders: `notes/_lanes/300/P/shots/plan-desktop.png` (1280 wide) · `plan-phone.png` (390) · `plan-desktop-open.png` (Technical blocks open) · driver `notes/_lanes/300/P/shoot_p.py`

## For Dave, in plain words

**The plan: cut our own start routine now, and move the conductor's seat after Friday.**

**Why.** The cloud seat loads 126K before anyone speaks, which is 51K more than the old desktop seat. Our own start routine adds 62K. Project memory then sends its 19K file list a second time with your first message. So today you pass the 200K working line before your first message has finished.

**Before Friday — four moves, minutes each, all undoable, none near the deck:**
1. **Paste the new start-of-chat instructions** (the text is on the page). About 50K lighter every session.
2. **No memory writes while a chat is live.** The note goes in once you say you're done, or waits until after Friday. The repo keeps every note.
3. **Helpers do the long thinking and writing.** Up to 37K lighter on a heavy day.
4. **To add a note mid-run, type it. Don't press stop.** Today's stop threw away two helpers' work.

**After Friday.** Claude Code on your Mac would boot at about 30K. First the repo's July instruction file has to be rewritten and the render setup ported. Then comes the short start card, but only after a fresh-chat quiz proves it carries what the handoff does. Today it carries 4 of 18 facts.

**Your two mid-run questions.**
- **16:08:** your principle holds. The page's rule is that a cut replaces a read only after a fresh chat passes the quiz without it.
- **16:56:** a better look-up helps, taking the search from 2 of 18 facts to 11–13. It cannot replace the start-of-chat read.

**Next step (yours):** open the page, paste the new instructions, and say yes to moves 2 and 3.

---

## 1. What the page is

A Swiss-system research page (archetype B with an A hero). Every section opens with the label pattern, has one landmark per chunk, and puts its numbers beside their units. Nothing above the Technical section carries an ID code, a file name or a session number (checked by script, §6).

| section | what it carries |
|---|---|
| Answer (hero) | the verdict in one line, four chunks, four stats: 126K boot · 211K at the first message · ~161K after the Friday set · ~30K on the Mac |
| Before Friday | four move cards (changes · saves · undo · the deck · the adversary's verdict), the optional Tool-access probe, five "not before Friday" traps, one next step |
| Where the tokens go | stacked bars: the window at the first message (today / Friday set / Mac) and at a heavy moment (today / Friday set), against the 160K / 200K / 256K lines. Then the four bands as a strip: the seat's load, our start routine, what grows, where it sits |
| The biggest items | the 20 largest items, red where the Friday set removes them, with when each can go |
| Every lever, ranked | 13 levers with saving, risk, undo, whose, what else it touches and the adversary's verdict. Then groundwork (saves nothing, makes the rest safe) and off the table |
| The Memento balance | the 16:08 answer; a meter of facts found per start-up read (card 4 · chain 11 · card + chain 12 · handoff 16 · handoff + chain 18 of 18); a per-cut table (what a cold seat loses, false-confident risk, guard); the quiz |
| Look-up | the 16:56 answer; a meter of top-3 hits (2 · 8 · 11 · 13 of 18); n-gram, BM25, the graph and Jev, each with cost and risk |
| After Friday | six steps in order, and seven rulings in words |
| The adversary's check | held · weakened · false (removed) · new |
| Technical (collapsed) | the paste-ready instructions, the probe, units, where the lanes disagreed, ranking method, a security note, receipts |

**Charts** (dataviz method; emphasis form):
- Palette: one accent `#DA1A00` plus greys `#8A8A8A` and `#3A3A3A`.
- Validator result: CVD ΔE ≥ 13, normal-vision ΔE ≥ 23, and every mark ≥ 3:1 on white. The grey slots fail the categorical chroma rule by design.
- Hover tooltips on the bar segments. An in-segment label is hidden when it does not fit, never clipped.
- The threshold lines are solid hairlines.

## 2. How the adversary's verdicts were folded in

Rule from the brief addendum: a lever marked FALSE does not go on the page as a recommendation.

| verdict (X) | what changed on the page |
|---|---|
| Card alone sufficient — **FALSE** (4/18 facts) | Removed as a lever. Listed under "off the table". The card appears only as "card + chain, after three quiz passes" (rank 5). "Your ruling on the chain" became "keep both reads (today's answer)" |
| Card + chain — **WEAKENED** | Its saving is cut to ~12K (C's 13.6K less ~1K for the handoff's cautions carried verbatim). Guard: the quiz |
| Thinking not carried (B) — **FALSE**; thinking retained (A) — **HOLDS** | Move 3 stands, with X's guard added: reports are read from disk, because a stop can wipe the stubs |
| Tool access On demand — **WEAKENED**, expect ≈0 | Demoted from Friday move 4 to an optional probe line. Ranked 9 at "expect ~0" |
| Seat move — **WEAKENED** | Kept as the biggest saving. The preconditions are named on the page: the July `AGENTS.md` (P re-checked: last commit 2026-07-19, points at GOOD-MORNING then _LIVE-STATE, no CLAUDE.md), 17 scripts, the Linux-only render env, one gate declaration. "After porting" is added to the hero |
| D's ratio 1.53–1.63 — **WEAKENED** | Memory figures use ×1.43: the list is ~19K and the cut to five files saves ~18K (not 19–20K) |
| "Cloud is the source" — **WEAKENED** (28% at 184K) | The hero now says "51K more than the desktop seat". The seat band says the addition was 28% of the 184K, not most of it |
| `read_key()` cannot parse — **FALSE** | The groundwork line became "check the token gauge: the key was refused once, and the model name is out of date" |
| Listing = first 150 — **FALSE** | Not on the page |
| New: an interrupt discards the running turn | Became Friday move 4 ("type, don't stop") and lever rank 4 |
| New: the ending chat pays the re-send; the conductor is past 256K | Move 2: "Tonight: leave the note out of this chat" |
| New: any store version change re-sends, possibly writes from other chats (inference) | Noted in move 2's check line |
| New: the KG explorer is in Friday's demo; Jev rulings (dev-time only, never a dependency, after the presentations) | Look-up cards. Jev appears only as "an optional extra that can add sections, never remove one", plus a seventh ruling |
| X's before-Friday list 1–3 | Became Friday moves 1, 2 and 4. X's optional 4 became the optional probe line |
| X's cold-card drill | Became "The quiz": 24 items, grading, pass bar, August's three measures, three fresh chats, answers stored as fingerprints |

**One edit by P to C's paste-ready text (flagged on the page).**
- C's step 4 said: "Only if the handoff says it was NOT placed, place it through a sub."
- Combined with X's "no writes in a live chat", that fallback would bring the 19K re-send back at the next session's second turn.
- The page's step 4 now reads: "Do not read or write Project memory at the opener, or anywhere while the chat is live … The last session's note is placed only after Dave says he is done; if it was not placed, leave it — the repo keeps every note at `notes/_lanes/<n>/WRAP-MEMORY-HOOK.md`."
- The rest of C's text is unchanged. The rollback file is C's, and X verified it (md5 `a6e11674…`).

**One P addition kept from before X landed:** Friday move 3 (helpers think). X did not list it in its before-Friday four, but it did not mark it false. A3 and A7 hold, and X's §4 table rates it "safe with stubs re-read from disk". The guard is on the page.

## 3. The lever ranking

Method: saving × confidence (1 measured · 0.5 projected or weakened · 0.25 untested) × undo (1 easy · 0.3 only from a copy) ÷ risk (1 low · 2 medium · 3 medium–high · 4 high), then two judgements:
- a Friday-set move ranks first among equals, because it is cheap to try and to undo;
- the project-scoped memory cut ranks above the account-wide pause, because the pause's cost lands on Dave's client work.

The order:
1. new instructions ~50K
2. helpers think ≤37K
3. Mac seat ~96K
4. type, don't stop
5. card + chain ~12K
6. memory cut ~18K
7. pause memory ~31K + re-sends
8. ask Anthropic
9. Tool access ~0
10. Chrome and computer use ~3.7K
11. outside the Project ~3K
12. chat search ~0.6K
13. repo in the cloud ~9K

## 4. Where the lanes disagreed (as settled on the page)

- **Thinking is retained.** A was right and B was wrong. X showed it with cache_read 187,861 at Dave's #300 turn. P's own trace agrees: the 14:49→15:01 boundary delta of +23,591 fits 4,220 retained output plus ~19.4K of snapshot and message.
- **The fill drop at 15:56.** P's trace saw 256,949 → 253,280 across Dave's interrupt. X explains it: the truncating resume discarded the interrupted turn. It is not general thinking-stripping.
- **The prose ratio** is ≈1.43 (A 1.453, re-send 1.431, X 1.40–1.45). D's 1.53–1.63 is superseded.
- **Which 150 files are listed:** opaque (D holds, A's "first 150" is false).
- **The gauge key:** the parse claim is false. The single 401 was not re-tested.
- **The first cloud seat** was #296 (C), not #297. `BOOT_FIRSTTURN_TK` is already deleted (C). Neither is on the decision surface.

## 5. Externalities named on the page

- **Client work.**
  - Pausing memory is account-wide.
  - Chrome and computer use off costs his client work both features.
  - Code execution off and the Office skills are don'ts.
- **Friday's deck.**
  - No move touches the deck build or the render lines.
  - The graph explorer is in the demo, so nothing touches it before Friday.
  - The Friday strand note in memory stays; nothing is deleted.
- **Other Apollo chats** see memory notes later, or not at all if the ritual is retired (ruling 2).
- **Plan usage.** Each helper boots at 66K. The wrap's memory sub costs its own window.
- **The wrap gate.**
  - Only the seat move clears the ceiling arm.
  - The seat move trips the band arm once, and one declaration covers it (ruling 5).
- **Runbooks.** Two lines are amended by addition: step 3's seat limit and render-verify's check-in line.
- **Security** (a note, not a lever): the `.git/config` remote carries a GitHub token. X found no leak. It is worth moving to a credential helper or the GitHub app.

## 6. P's own checks

- **Renders at the seat.** Each ran in one bash call: `export TMPDIR=/dev/shm; bash knowledge/_render/ensure_env.sh; source knowledge/_render/seat_env.sh; python3 notes/_lanes/300/P/shoot_p.py …` with `executable_path=$RENDER_SHELL` and `goto(file://…)`. Final run: desktop scrollWidth 1280/1280, phone 390/390, no overflowing element, no hidden segment label. Looked at by eye in crops: the hero, the Friday cards, the chart, the items, the lever table, the Memento meters and table, the quiz, look-up, After Friday, the adversary section, the expanded Technical section, and the phone chart plus the stacked lever cards.
- **Fixed after looking:**
  - limit-label collision (moved to two tiers);
  - red move numerals (now decorative grey);
  - the proposition gap;
  - phone nav overflow (CTA hidden under 700px);
  - phone limit lines collapsing to the right edge (missing `left:0`);
  - the lever and Memento tables now stack as labelled cards on phones instead of scrolling sideways;
  - "cloud sandbox" corrected to "the Linux sandbox on your Mac";
  - the 28% framing tied to the 184K Dave saw.
- **ID scan.** A regex over the text above Technical found no ruling ids, `.py`/`.md` names, lane letters, v2/v3 or session numbers.
- **Spot-checks of X.**
  - `git --no-optional-locks log -1 -- AGENTS.md` → 2026-07-19, and `sed -n 44,50p AGENTS.md` sends a cold agent to GOOD-MORNING then _LIVE-STATE. No CLAUDE.md.
  - `python3 notes/_lanes/300/X/lookup_probe.py` → hit@3 2 · 8 · 11 of 18.
  - `python3 notes/_lanes/300/X/drill_proxy.py` → card 4 · handoff 16 · chain 11 · card + chain 12.
- **Copy-lag hazard (tooling).** `device_commit_files` twice wrote the previous version of the page, even though the staged file had changed seconds earlier. P re-committed until the Mac md5 matched the staged md5. Anyone copying files this way should verify the md5 after each copy.

## 7. For the conductor (not on the page)

- **The window.** The conductor's transcript showed 262,384 at 15:57 UTC (X), which is past the 256K quality ceiling. Keep heavy work in subs and place no memory in this chat (page, move 2).
- **Nothing is committed.** To commit through `knowledge/_git_commit.sh`, if the conductor chooses:
  - the page;
  - `notes/_lanes/300/P/` (driver and shots — the shots are ~5 MB of PNG, so the conductor may prefer to leave them uncommitted);
  - this report.
- **Paste-ready text.** The page's Technical block holds the new instructions (C's text with P's step-4 edit). If Dave pastes them, today's text is at `notes/_lanes/300/C/PROJECT-INSTRUCTIONS-as-of-2026-09-23-ROLLBACK.txt`.

## 8. REPLAY-THESE

- **Mac, repo root** (render + layout facts): `export TMPDIR=/dev/shm; bash knowledge/_render/ensure_env.sh; source knowledge/_render/seat_env.sh; python3 notes/_lanes/300/P/shoot_p.py notes/_PLAN-300-boot-diet-2026-09-23-v1.html notes/_lanes/300/P/shots` → `desktop {'scrollW': 1280, 'clientW': 1280, …}` · `phone {'scrollW': 390, 'clientW': 390, …}`.
- **Mac:** `md5sum notes/_PLAN-300-boot-diet-2026-09-23-v1.html` → `7414d500b77d345307540021e22118ad`.
- **Mac:** `python3 notes/_lanes/300/X/drill_proxy.py | tail -1` → `card 4 · handoff 16 · chain 11 · card+chain 12`. Then `python3 notes/_lanes/300/X/lookup_probe.py | tail -1` → `2 · 8 · 11`.
- **Container** (the probe on the page, run on the conductor's own transcript): the one-liner in the page's Technical block prints `BOOT 126185` · `LOADED 68 {core 26, claude_ai 7, claude-code-remote 6, memory 5, remote-devices 7, widgets 17}` · `MEMORY LIST AT BOOT True`.

## 9. Spend

Hand sum of `/root/.claude/projects/-home-claude/d75f51cc-494a-56b1-9272-190e7fc46cbb/subagents/agent-af3fc8adae5b52530.jsonl` (this lane), per distinct `message.id`, at filing:
- 115 assistant messages;
- boot 66,398; last fill 421,906;
- cumulative input processed 32.35M, of which 31.99M was cache reads;
- cache writes 364,332; output 153,893.

A few messages follow for the copy and the hand-back. The first lane-P launch (interrupted at 15:56, never filed) is not included. X measured it at 48 messages, 7.68M processed and 38,588 output.
