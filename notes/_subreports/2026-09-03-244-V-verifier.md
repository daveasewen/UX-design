# `#244`-`V` — same-wave verifier of lanes A, B and C

session: `#244` · 2026-09-03 · verifies `notes/_subreports/2026-09-03-244-lane-A-nav-family.md`,
`-lane-B-radius-tuner.md`, `-lane-C-debt-sweep.md` · house shape from
`notes/_subreports/2026-09-03-243-V3-six-controls-verifier.md`
tokens: `UNMEASURED` — a lane sub cannot read its own `message.usage`.

## VERDICT

**SATISFIED, with two FINDINGs (both LOW) and one registered fix.** Every load-bearing number in
all three lane reports re-measures first-hand at this seat: 136 snippets / 0 failures · 16/16
nav-family members carry snippet + meta + showroom · Navigation = 11 rows, 11 `derived: GATED` ·
the radius tuner renders 0 fails across 136 computed-radius assertions with `console_errors []` ·
`_lanes.json` valid with the §C·1 step `active` and `lane_routing_check` green · the seam-block
pointer resolves to a file that exists · `_memory_cap_check.py --selftest` 13 bites OK, `--check`
rc 1 over cap / rc 0 under / rc 2 missing. Two mutations were driven at this seat, one of them
through the real UI control rather than the page's JS API. Item D is enacted: ds-021 is gone and
the two expected STRUCTURAL lines are the only ones left.

COUNTS: SATISFIED `11` · FINDING `2` (both LOW) · UNPROVEN `1` · mutations driven `2`

---

## A — LANE A'S NAV FAMILY

| claim | lane A | **V measured** | verdict |
|---|---|---|---|
| `_validate_snippets.py` | 136 snippets, 0 failures | **`snippet gate: 136 snippet(s), 0 failure(s)`**, rc 0 | SATISFIED |
| 16 nav members: snippet + meta + showroom | all present | **16/16** — every `knowledge/snippets/*.reference.html`, `knowledge/components/*.meta.json` and `showroom/*.html` `os.path.exists` True; probed off the filesystem, not the register | SATISFIED |
| register Navigation category | 11 rows, all `derived: GATED` | **11 rows, 11× `GATED`** (`reviews/ITINERARY-STATUS-2026-08-25-v4.json`); category tally re-derived independently | SATISFIED |
| register store totals | `GATED 121 · ASSET-SYSTEM 1 · ASSET-ONLY 1 · GAP 1`, `$true_gaps [86]` | **identical**, `$row_count 124` | SATISFIED |
| canon `.cn-` rule counts (71/65/49/…/111) | live grep | **reproduce exactly under `grep -o … | wc -l`** (Tabs 71 · Navigations 78 · top-nav 143 · page-header-lockup 111) | SATISFIED — see Finding A1 |
| `canon.css` 1,903,149 B | — | **1,903,149 chars / 1,908,064 bytes** — the figure is a Python text-mode `len(read())`, not bytes | SATISFIED, unit mislabelled (Finding A1) |

### The frozen-column finding — GRADED: lane A is RIGHT, fifth recurrence stands

Both sides quoted verbatim, as asked.

**`GOOD-MORNING.md:338`** (§C·1 strand (b)):

> **(b) Wave 3 fan-out (component library)** … — ~26 itinerary gaps remain (`reviews/ITINERARY-2026-07-14…`);

**`reviews/ITINERARY-STATUS-2026-08-25-v4.json`**, the two columns, verbatim from `$columns`:

> `itinerary_status_2026_07_14_FROZEN`: "THE FROZEN 2026-07-14 SPREADSHEET CELL — history, not a
> measurement. Hand-typed on 2026-07-14 and never re-statused. ⛔ NOT A WORKLIST: counting 'Gap'
> here counts a photograph of July, not the store."
> `derived`: "THE MEASURED COLUMN — five probes per slug (snippet · meta · showroom · radius
> ratchet · canon .cn- rules) taken at $measured. This is the live fact. Brief off THIS."

**Measured at this seat:** the frozen column tallies `{'Gated': 39, 'Partial': 7, 'Gap': 78}`; the
derived column tallies `GAP 1`. **`~26` agrees with neither — not with 78, not with 1.** The queue
line does not even point at the measured register: its citation is
`reviews/ITINERARY-2026-07-14…`, the July spreadsheet, so the #218 rename could not have reached
it. `$drift_counts` = `{'AGREES': 40, 'STALE — itinerary UNDERSTATES the store': 84}` — 84 of 124
rows are known-stale in the frozen column the queue descends from.

**GRADE: lane A's finding 2 is SATISFIED and correctly framed.** The prose number is
hard-coded, disagrees with both columns, and is reached by a reader who never opens the measured
one. `[[instrument-without-a-consumer]]` is the right hook. The same string appears twice in
`knowledge/_memento-index.json` (grep count 2) and once in `GOOD-MORNING.md`, so a fix at the
GM alone would leave the retrieval surface still serving `~26`. Lane A's recommended option (b)
— generate the count — is the only one of its three that closes both copies at once.

**FINDING A1 (LOW) — two of lane A's probe figures are correct numbers under an unstated method.**
The canon counts are OCCURRENCES (`grep -o`), while the register's own recorded probe for the same
slug is `grep -c` (LINES): register row 28 records `grep -c '\.cn-tabs' → 68`, lane A reports 71,
and my line-count is 43 against an occurrence-count of 71. Both are true of different questions.
Likewise `1,903,149 B` is a character count, not bytes (bytes = 1,908,064). Neither changes a
verdict; both would mislead a later reader diffing against the register. Fix shape: name the
command beside the number, as the register does.

**FINDING A2 (LOW) — lane A's row 32 label.** The report calls row 32 "Nav / Menu (`Navigations`)";
the register's `name` is `Nav / Menu`. Cosmetic, recorded so the row is findable.

---

## B — THE RADIUS TUNER, RE-RENDERED AT THIS SEAT

Driver re-run first-hand, not read: `source knowledge/_render/seat_env.sh` →
`shell=…/outputs/_render-env-229/pw-browsers/chromium_headless_shell-1234/chrome-linux/headless_shell`,
then `python3 notes/_subreports/assets/2026-09-03-244-lane-B-radius-tuner/verify_radius_tuner.py`.
Load is `goto("file://…")` (read at driver line 18/55); `set_content` appears nowhere in the file.

| claim | lane B | **V measured** | verdict |
|---|---|---|---|
| file size | 94,445 B | **94,445 B** | SATISFIED |
| zero external refs | grepped | **0** — no `src=`/`href=` of any kind, no `@import`, no `url(`, `grep -c 'http'` = **0** | SATISFIED |
| render proof | 0 fails / 136 assertions | **`=== FAILS: 0`**, `console_errors: []`, readout `rows: 13` in every pane, `indw: 55` in all 8 panes | SATISFIED |
| two-red law lines 568 / 621 / 623 | — | **exact**: `:root{… --tn-bg:#FFFFFF; --tn-red:#DA1A00;}` (568) · `.tn-pane[data-theme="dark"]{background:#1A1A1A; … --tn-red:#F6604C;}` (621) · `.tn-stage[data-apollo-theme="supercharge"] .tn-pane[data-theme="light"]{background:#F7F6F4; … --tn-red:#F6604C;}` (623). `#DA1A00` occurs on white ground ONLY; every other `#F6604C` hit (305/310/353/355/360/418) is a specimen `--error`/`--*-bg` inside a pasted canon AUTO-THEMES block, not tuner chrome | SATISFIED |

### The driver's STORE is NOT a tautology — checked against the token store

The house risk (V3's Finding 2 shape) is a driver that takes its oracle from the thing under test.
`verify_radius_tuner.py` hard-codes `STORE`, so I re-derived every value from
`knowledge/tokens/layout.json` + `knowledge/tokens/themes/*.overrides.json` independently:

- **base `layout.json`** — all 13 `border-radius/*` slots `0px`. → mono, and the floor for the rest.
- **`apollo-console.overrides.json`** — `default 4 · control 8 · surface 20 · container 20 ·
  segmented-container xs/s/m/l = 6/8/10/12 · segmented-thumb xs/s/m/l = 4/6/8/8`; **no
  `border-radius/indicator` entry at all.**
- **`apollo-legacy.overrides.json`** — `border-radius/default: 0`, `$note` "Dave 2026-07-21:
  Legacy = square, same as Mono."
- **`apollo-supercharge.overrides.json`** — **no `border-radius` entry of any kind.**

Every one of these matches the driver's `STORE` dict exactly, and matches the rendered computed
values. ≥3 computed values per theme × 4 themes is satisfied many times over (17 specimens × 2
modes × 4 themes). Lane B's **Finding 2** (Console `indicator` is aliased, not overridden) and
**Finding 3** (Legacy explicit `0` vs Supercharge inheritance) both re-measure TRUE at the file
level. Console's chip renders `4px` — the alias arriving, as claimed.

### MUTATION 1, DRIVEN AT THIS SEAT, THROUGH THE REAL UI CONTROL

Lane B's MUT1 goes through `window.__radiusTuner.setDial(...)`, the page's own JS API. I drove the
**actual `<input type=range>`** instead — located by its `.tn-name` text `border-radius/surface`,
alias checkbox `click()`ed as a user would, value set through the native
`HTMLInputElement.prototype.value` setter with a bubbling `input` event. Verbatim:

```
DRIVE: checkbox checked=true range disabled=true | clicked alias checkbox -> checked=false rangeDisabled=false | range now 18 readout=18px driftClass=true
BEFORE mono: {"button":"0px","input":"0px","tag":"0px","card":"0px","banner":"0px","dialog":"0px","dlgbtn":"0px","chip":"0px"}
AFTER  mono light: {"button":"0px","input":"0px","tag":"0px","card":"18px","banner":"18px","dialog":"18px","dlgbtn":"0px","chip":"0px"}
AFTER  mono dark : {"button":"0px","input":"0px","tag":"0px","card":"18px","banner":"18px","dialog":"18px","dlgbtn":"0px","chip":"0px"}
V-MUT (UI slider) VERDICT: PASS
console_errors: []
```

**Cards, banner and dialog moved to 18px in BOTH modes; button, input, tag and the dialog's own
button stayed 0px.** Role isolation is real, the alias checkbox is the ADR-0010 mechanism it
claims to be (the slider is `disabled` until unlinked), and the drift flag lights. This is a
stronger arm than lane B's: it exercises the control a human touches, not the API behind it.

### MUTATION 2, re-driven (lane B's concentric arm)

`segmented-container/m` 10 → 16 in Console: `seg_m` **16px**, `thumb_m` **14px** =
`max(16−2,0)`, `seg_l` **12px** and `thumb_l` **8px** unmoved, both modes. Per-scale isolation and
the concentric law hold. Lane B's Finding 1 (the four minted Console thumbs 4/6/8/8 are exactly
what the law yields from tracks 6/8/10/12) re-derives arithmetically against the override file.

---

## C — LANE C'S FOUR ITEMS

**C1 — records. SATISFIED.** `knowledge/_lanes.json` parses. `_gen_lanes.load_lanes()` →
`validate errors: []`. The step at `/lanes[1]/sequence[3]` reads
`"step": "§C·1 strands (a)–(d) — chart expansion · wave 3 · templates/shells · enact window"`,
`"state": "active"`, receipt naming *"WORK IN FLIGHT AT #244: lane A — the nav/menu family … lane
B — the radius tuner"*. `_capture_gate.lane_routing_check('.')` returns
`([], ['lane-routing: GM eager ROUTING line agrees with 4 lane records (BLOCKING — O1′ #24;
records are the truth).'])` — **FAILS empty, green.** `_gen_lanes.py --check` →
`✓ lanes view in sync with records`, rc 0, so the declared `AUTO-LANES` regenerate landed and left
no residue.

**C2 — the `_seam_block.sh` pointer. SATISFIED, path stat'd.** `_LIVE-STATE.md:19` names
`notes/_subreports/assets/2026-09-03-242-lane-P-polarity-receipt/_seam_block.sh` as current.
`stat`: **EXISTS, 3,155 B, mtime 2026-09-03 10:59:55**. Both paths it marks as dated history also
exist (1,863 B each, 2026-09-02) — so the line names three real files and mis-points at none. It
sits at line 19, above `## 🛤 LANES` at line 21, i.e. inside the header zone as claimed, and
`Last refreshed: 2026-09-03` is intact (1 occurrence, unmoved).

**C3 — the cap. SATISFIED, both directions driven.**

| drive | rc | reading |
|---|---|---|
| `--selftest` | **0** | `[_memory_cap_check selftest] OK — 13 bites; every refusal fired, green controls held` |
| `--check --path` over cap (18,000 B fixture) | **1** | `✗ OVER CAP: … 4,001 cl100k tape against a cap of 1,802 tape — +2,199 tape` |
| `--check --path` under cap (10 B fixture) | **0** | `✓ WITHIN CAP: … 3 … 1,799 tape of headroom` |
| `--check --path` nonexistent | **2** | `DECLARED MISSING (not zeroed): … does not exist.` — rc 2 ≠ rc 1, exactly as lane C's Finding 4 claims |
| `--check --path <live MEMORY.md>` | **0** | **1,502 tape · 4,884 B · 33 lines** — lane C's Finding 3 figure, reproduced to the digit |

`MEMORY_CAP_TAPE` imports as **1802**; `wc -l` = **221 lines**, as claimed.

**C4 — the CI verdict. SATISFIED as a report-shape check ONLY; browser NOT re-opened, per brief.**
Lane C's Finding 6 carries a run URL —
`https://github.com/daveasewen/UX-design/actions/runs/33752385428` — and a verbatim status line,
**`Status: `Failure``**, alongside run **#482**, `gates.yml`, `on: push`, commit `9628bae`, branch
`master`, duration `13m 57s`, and the annotation `5 errors and 1 warning`. The requirement (a URL
plus a verbatim status) is met. **UNPROVEN at this seat:** the verdict itself — I did not load the
page, so #482's Failure is CLAIMED from lane C's read-back, not re-measured. Declared skip, priced
by lane C at ≈8–15K real per step-log open.

---

## D — THE REGISTERED FIX (ds-021)

**BASELINE, measured before touching anything** — `unit_vocabulary_audit('.')` returned exactly one
failure:

> `ds-021 (C): knowledge/_memory_cap_check.py counts tokens (cl100k) and is NOT in MEASURERS — an
> UNREGISTERED measurer.`

**EDIT — one entry appended to `MEASURERS` in `knowledge/_capture_gate.py` (the file the gate
names, line ~509), copying `_boot_decompose.py`'s exact shape:** key
`"_memory_cap_check.py"`, tier **`estimate-only`**, WHY carrying the required sentence *"cl100k
tape of a non-repo file; never a real-token claim"* plus the birth-catch provenance, the
`/sessions/*/mnt` reason no `message.usage` can ever be taken over the graded file, the
`[[measure-dont-convert-units]]` prohibition and the `[[gate-cannot-pass-in-one-environment]]`
reason the arm stays advisory. `py_compile` clean. **Nothing else in the file was touched.**

**AFTER:** `unit_vocabulary_audit('.')` → `FAILURES AFTER: []`. The entry now prints as the fourth
DECLARED GAP warn, beside `_boot_decompose.py`, `_boot_remeasure.py` and `_context_gauge.py` —
which is the intended conversion of a blocking failure into a declared, named advisory, not a
silencing.

**`python3 knowledge/_checkin.py` re-run, rc 0. ds-021 is GONE. The remaining STRUCTURAL lines,
verbatim and both LEFT ALONE as instructed:**

> ⛔ STRUCTURAL retrieval index is STALE — it does not match GOOD-MORNING.md / _LIVE-STATE.md as they now stand, so `_memento_search.py` is serving a PREVIOUS session's record. Run `python3 knowledge/_build_memento_index.py` and stage the result (ritual step 2g). This is the #32 defect — do not close over it.

> ⛔ STRUCTURAL boot-drift CEILING BREACH: `_gauge_tokens.BOOT_CEILING_TK` = 70,000 and 1 post-diet reading(s) EXCEED it — #242 70,710. ⛔ `s240-D2`/`s241-D1` make this number SHRINK-ONLY: boot may go DOWN past it and never up, and the remedy is to CUT THE BOOT, never to raise the literal. Raising it is Dave's word alone and is not a price a wrap may pay to unblock itself [[gate-must-quote-what-it-forbids]].

Exactly the two expected: ritual 2g, and Dave's. The rehearsal footer reads
`2 STRUCTURAL fail(s)` (was 3) and `20 warn(s) (was 19)`, and names the new warn by name —
`NEW SINCE 2026-09-03 — ds-021 (C) DECLARED GAP — knowledge/_memory_cap_check.py …` — which is the
gate reporting its own delta correctly.

---

## DECLARED SKIPS, each with its size

1. **Run #482's failing STEP LOGS — NOT OPENED.** Brief says do not re-open the browser. Price if
   wanted: 2 step-log opens, ≈8–15K real each (#239's uncapped route cost ≈55,692 real).
2. **`_capture_gate.py --selftest` END TO END — NOT RUN.** Same wall lane C hit (>178 s sandbox
   call cap). What IS established here: the module compiles, imports, and the ds-021 arm was driven
   before and after the edit with opposite verdicts. Price: one run outside the call wall,
   ≈180–400 s.
3. **`_build_all.py`, git, any ruling, and every fenced file — NOT TOUCHED**, as instructed. No
   constant moved; `MEMORY_CAP_TAPE` left at 1802.
4. **Pixel-equivalence of the tuner's specimens vs `canon.css`** — lane B's own UNPROVEN, not
   re-opened here. Price: 64 comparisons, ≈one render lane.
5. **The 9 ratchet-missing nav files under `_validate_radius.py` STRICT** — lane A's UNPROVEN,
   left standing. Price: one throwaway run, ~5 min.

## TREE STATE

- **Modified by V — exactly one path:** `knowledge/_capture_gate.py` (one `MEASURERS` entry).
  Lane C had already modified this file for `MEMORY_CAP_BLOCKING` / `memory_cap_check`; V's edit is
  additive and in a different region.
- **New (untracked), MINE — one path:** `notes/_subreports/2026-09-03-244-V-verifier.md`.
- **Not mine, declared:** `_LIVE-STATE.md`, `knowledge/_lanes.json` (lane C) ·
  `knowledge/_graph-mark-observations.jsonl`, `notes/_REHEARSAL-LOG.jsonl`,
  `notes/_dream/_GRADE-DECISIONS.jsonl` (instrument appends; `_REHEARSAL-LOG.jsonl` gained one row
  from V's own `_checkin.py` run) · `knowledge/_memory_cap_check.py`,
  `reviews/RADIUS-TUNER-2026-09-03-v1.html`, the three lane reports and lane B's assets directory.
- **Nothing under `notes/_subreports/assets/2026-09-03-244-lane-B-radius-tuner/` was modified** —
  the driver was re-run in place, read-only. V's two mutation scripts were written to `/dev/shm`
  and are not in the repo.
