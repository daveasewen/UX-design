# #299-E: slide 04's bold line, and its heading back to full size

provenance: 299 · 2026-09-23
status: observed
session: #299 · 2026-09-23 · lane E (Opus 5.5, remote-device seat)
brief: the conductor's in-chat brief to lane E. Dave's words, 14:56 BST (verbatim in the brief): he chose the bold line "Checked automatically, then by a human." — "cool lets do it, it's punchier anyway".
tokens: UNMEASURED (this lane's message.usage cannot be read from the device seat)

## VERDICT

**DONE, in one commit.** On slide 04 ("What we tested"), the bold line now reads **"Checked automatically, then by a human."**, and the heading is back at full size: **54px at 1920x1080** and **53.28px at 1440x900**. It sits on **exactly two lines** at both sizes: the light line, then the bold line, with the forced break after "available." kept.

- **Lane D's size cut is undone.** The rule is back to exactly what it was before #299 D: `#s5x h2{max-width:21em;font-size:clamp(32px,3.7vw,54px)}`. The 43px cap is gone, and so is D's `max-width:none`, which is no longer needed.
- **Pixel diff, all 16 slides plus 11's second state, at both sizes under reduced motion: only 04 changes.** The 1440 rail flicker on 15 and 16 also shows up between two shots of the same deck, so it isn't from this edit.
- The source was edited and the deck rebuilt with `build_c.py`. `build_e.py` was not run, and `build_c.py` is byte-identical to HEAD.

## The change

The source is `notes/_lanes/296/C/v14-plain-before-c.html`. The edit script is `notes/_lanes/299/E/_work/edit_e.py`, which does two exact-match replacements, each asserted to occur exactly once.

- **Markup (line 472):** `Built from the 36 components available.<br><b>The work checked automatically, and then by a human.</b>` became `Built from the 36 components available.<br><b>Checked automatically, then by a human.</b>`. The `<br>` from #299 D stays, so the break is still forced after "available.".
- **CSS (line 791):** `#s5x h2{max-width:none;font-size:clamp(32px,3.7vw,43px)}`, with #299 D's 3-line comment, became `#s5x h2{max-width:21em;font-size:clamp(32px,3.7vw,54px)}`, with a 2-line non-rendered #299 E comment.
  - The rule text is **byte-identical to the pre-#299-D line** (checked against #299 D's `_work/source.before.html`, line 791).
  - This rule is slide 04's own, from 296-B2. #299 D didn't add it, it only changed its values, so the override D added was the values, and they're reverted. There's no other slide-04-only rule to remove.
  - Print keeps its own `#s5x h2{font-size:40px}` (line 852), untouched.
- **Why `max-width` is back at 21em and not `none`:** the probe (`_work/probe_fit.py`) measured the new line against both measures. It fits inside 21em at both sizes, so D's widening isn't needed.

| render size | font-size | max-width (21em) | column (`.inner`) | light line | bold line | lines |
|---|---|---|---|---|---|---|
| 1920x1080 | **54px** | 1134px (the column bounds it: 1120) | 1120px | 924.5px | **1034.2px** | **2** |
| 1440x900 | **53.28px** | 1118.9px | 1120px | 911.9px | **1020.1px** | **2** |

- Headroom on the bold line is 85.8px at 1920 and 98.8px at 1440.
- For comparison, before this lane (D's 43px): 736.2 / 1099.9px, on 2 lines. D's old bold line at full size needed 1381px.
- The heading is 116.6px tall at 1920 (it was 92.9 at 43px), so the centred block below sits a little lower. The steps diagram and the footnote are otherwise unchanged.

⚠ **One wording note.** The brief called 54 / 53.28 "the same rule as the sibling headings". Those are the sizes 04 had before #299 D, from its own rule (cap 54px). The deck's base `h2` is `clamp(34px,5.2vw,64px)`, which puts 06's heading at 64px, for example. I restored 04's pre-D rule, as the brief's numbers say, and left the base `h2` alone.

## Render checks

Every render ran at the seat, on the mount, one bash call per viewport: `export TMPDIR=/dev/shm; bash knowledge/_render/ensure_env.sh; source knowledge/_render/seat_env.sh; python3 notes/_lanes/299/E/_work/shoot_e.py <deck> <dir> WxH`.

- `shoot_e.py` is #299 D's `shoot_d.py`, unchanged: `reduced_motion='reduce'`, `executable_path=$RENDER_SHELL`, `goto("file://…")`, and the same freezes and pointer block.
- The diff script is `_work/pdiff_e.py`, #299 D's `pdiff_d.py` unchanged. It diffs the rail band (x<200) and the content separately.
- The before deck is `_work/before.html`, a copy of the deck at HEAD `a7b2c9f2`, taken before any edit. It was checked byte-identical, and the source, the deck and `build_c.py` were all byte-identical to HEAD at the start.

**Before against after:**

| slide | 1920x1080 | 1440x900 |
|---|---|---|
| 01-03, 05-11, 11b, 12-14 | SAME | SAME |
| **04** s5x | content DIFF 400-1520 x 268-795 (the heading, and the block below it moves) | content DIFF 200-1320 x 178-704 |
| 15, 16 | SAME | rail-band flicker only, x 37-40, max 45 / 74 of 765, content SAME |

- **The flicker isn't an edit.** Two shots of the same unedited deck at 1440 (`before-1440` against `before-1440-rep`) differ on 15's rail, with the same bbox and the same count, 87. Two shots of the edited deck (`after-1440` against `after-1440-rep`) differ on 15 and 16 in the same way. `before-1440` against `after-1440-rep` differs on **04 only**. This is the same harness flicker #299 D recorded.
- **DOM facts (`_facts.json`):**
  - s5x's text differs only in the bold line, and its element count is unchanged (the `<br>` was already there).
  - No other slide's text, count, pagenum or step changed, and `anatomyState()` is identical.
  - There are 0 page and 0 console errors, before and after, at both sizes.
- **Rail and overflow:** 04 has 0 rail hits and 0 overflow at both sizes. The closest element to the rail is 247.9px away at 1920 and 47.9px at 1440, both unchanged. The one flagged render, 11b's 4 box items on "Proposal:", was there before and is unchanged.
- **By eye (1440):** the light line is over the bold line, full size, both on the left edge of the column, and the diagram sits below with its gap unchanged.

**Before/after: `notes/_lanes/299/E/before-after.png`** (2952x1088, 188 KB). It shows 04 at 1440x900, before (43px) on the left and after (53.28px) on the right. The driver is `_work/sheet_e.py`.

## Commit

One commit, via `SESSION_N=299 INSTRUMENT_AUTOSTAGE=0 DOC_ROW_ACK="…" bash knowledge/_git_commit.sh --reconciled --quiet=notes/_lanes/299/E/_gitcommit-E1.log notes/_lanes/299/E/_msg-E1.txt <4 paths>`.

- The msgfile was written by python. Line 1 is a plain summary with no "after #N" prefix; the script adds that.
- It stages four paths: the source, the rebuilt deck, `notes/_lanes/299/E/before-after.png` and this report. The sha is in § POST-COMMIT.
- `git status`, `git diff`, `add -A`, `reset --hard` and `checkout` were never run. **Not pushed.**

## UNPROVEN / CLAIMED

- **UNPROVEN:** print. The print rule (40px) wasn't rendered. By the ratio, the bold line is about 766px at 40px, inside 21em (840px), so it should stay on two lines.
- **CLAIMED:** "exactly two lines" is the count of distinct line boxes in the heading's text range, from `getClientRects`, at each viewport, together with the renders.

## PATHS

Changed, committed: `notes/_lanes/296/C/v14-plain-before-c.html` (the source) · `notes/_DEMO-SLIDES-apollo-2026-09-22-v14-plain.html` (rebuilt).
Created, committed: `notes/_lanes/299/E/before-after.png` · this report.
Working files, **not committed**, all in `notes/_lanes/299/E/_work/`:
- pre-edit copies: `before.html` (the deck) and `source.before.html`; the post-build copy `after.html`
- the fit probe's variants `_variant-v21em.html` and `_variant-vnone.html`, and `mk_variants.py`
- the render folders `before-1920`, `before-1440`, `before-1440-rep`, `after-1920`, `after-1440` and `after-1440-rep`, each with `_facts.json`
- the drivers `shoot_e.py`, `pdiff_e.py`, `probe_fit.py` and `sheet_e.py`, and the edit script `edit_e.py`

Also not committed: the msgfile `notes/_lanes/299/E/_msg-E1.txt` and the transcript `_gitcommit-E1.log`.

---

## § POST-COMMIT (added AFTER the commit, not in it)

- **Commit: `119f3615`** (`119f361542e5d792ae3f5db9d1e6b822ed05db70`). Subject: `after #299 2026-09-23 — slide 04's bold line reads "Checked automatically, then by a human." and the heading is back at full size`. 4 files, +94 / -8: the source, the rebuilt deck, `notes/_lanes/299/E/before-after.png` and this report.
- **Gates:**
  - Green: reuse, chain fresh, showroom, polarity, mention map, session witness #299.
  - The doc-row gate is a DECLARED GAP, covering this report and C's and D's if they're still unrowed.
  - The wrap gate is red but visible, because this is declared not-a-wrap.
  - Also printed: the same two boot-drift ❌ lines (#297's 127,600 reading) and the duplicate-heading ⚠ lines that #299 C and D saw. Neither belongs to this lane.
  - The transcript is `notes/_lanes/299/E/_gitcommit-E1.log` (exit 0).
- **Read-back:** the four paths, plus `build_c.py`, are byte-identical to HEAD. The committed deck is byte-identical to `_work/after.html`, which is what was rendered. `git log`: `119f3615` ← `a7b2c9f2` ← `e3f0de60`.
- **Locks:** git printed `unable to unlink .git/index.lock` once, and the script's own clearing handled it ("locks clear"). `.git/*.lock` and `.git/refs/heads/*.lock` were empty afterwards, so nothing was moved to `_orphan-locks`.
- **Not pushed.**
- **Owed to the conductor:** the `_state.json` row for this report, and for #299 C's and D's if they're still unrowed.
- This tail is itself uncommitted.
