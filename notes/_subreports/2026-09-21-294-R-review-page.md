# #294 lane R — the judgement-only review page (Move 3)

session: `#294` · 2026-09-21 · lane `R` · model Opus 5 (1M) · conductor Fable 5.1
brief: Move 3 of Dave's #293 ask — *"then wen they are cut down lets get a new review page with anything
that needs mu judgement on it excluding anything settled"*. Moves 1 and 2 landed at #293; Move 3 was
not reached.
scope: ⛔ READ-ONLY on git (a foreign `.git/index.lock` was present at open; not touched, not `mv`'d,
no git write of any kind ran). Two files written: the page and this report. No ruling inscribed —
`knowledge/_rulings.json` was read ONLY by field extraction (id / ruled / date / `says`[:400] /
`status`[:220]), never printed whole, and stands at **622** records with **no `s293-` or `s294-` id**.

## WHAT WAS BUILT

`notes/_lanes/294/R/review-page.html` — **33,215 B measured**, single self-contained file, inline CSS,
**0 external references, 0 script tags**, Swiss design system idiom (accent `#DB0011`, label pattern,
1/3 : 2/3 dimension splits with decorative index numerals, hairline `grey-2` section rules, no
shadows / radius / decorative borders). It **REPLACES `notes/_lanes/293/DA/dream-enactment-audit.html`
as the ruling surface** and says so in its nav and its footer; the audit is named on the page as the
evidence.

Structure, in Dave's reading order: answer sheet first (ten numbered rows, each with its exact
one-word slot, blank) → four group headings → ten items → footer. Each item carries, in this order:
a plain-prose title with no ruling ids in it · two or three sentences of what it means and why it is
open · **my recommendation**, one clause · the **one-word rule** slot with the exact words · a
`Technical` line with source file, ruling ids, ages in days, constants.

### Item count by heading — 10 items, 4 headings

| heading | items | numbers |
|---|---|---|
| The dream passes you were never shown | **5** | 1–5 |
| Instruments | **2** | 6–7 |
| The two stamps on the front page | **2** | 8–9 |
| Jev | **1** | 10 |

The ten, with their slots and their sources:

1. The drifted verdict sentence — `generate / pin / leave` — pass 10 P3, `s125-D1`'s `watch`
2. Filed reports' three PARSED contract lines at 48% / 37% / 47% — `enforce / drop / leave` — pass 10 P1, `s218-D7`
3. `WRAP_COMMIT_SUBJECT_RE` selects non-wrap commits — `fix / retire / leave` — pass 10 P2
4. `GRADE_AGING_DAYS = 30` and `POPULATION_DELTA_THRESHOLD = 5` — `keep / change / later` — pass 9 P3, `s183-D1`, `s256-D2`
5. The push credential's scope, expiry `2026-11-06` — `yes / no / later` — pass 6 P4, `s186-D2`
6. The B3 memory grader: re-point or review the data that exists — `re-point / review` — pass 9 P4 + passes 8/11/13
7. The `8,470` boot-floor term: may it be re-measured at all — `re-measure / hold` — pass 13 P2, `s241-D1`, `ds-025`
8. The `size:` figure: generate or keep stamping — `generate / stamp` — pass 13 P3 strong form
9. The pre-flight stamp's form: widen or second emitter — `widen / emitter` — pass 9 P1's residue, `ds-023`, #56
10. Jev as a dev-time instrument only, never a blocking dependency — `yes / no` — lanes J/J2/J3

All five items the handoff § OWED item 1 reserved for this page are on it: (a) pass 10's three → items
1, 2, 3, with `s125-D1`'s `watch` quoted in item 1's body as the reason the hand-fix is forbidden ·
(b) the B3 re-point-or-review → item 6 · (c) `GRADE_AGING_DAYS = 30` → item 4 · (d) generate-vs-stamp
on `size:` → item 8 · (e) pass 6 P4's stamped expiry → item 5. The neighbour the handoff calls
*"ruling-shaped and HIS"* — the pre-flight stamp's form — is item 9.

Dave's words are quoted, never paraphrased: his Move-3 ask in the header, and in item 10 the two Jev
lines recovered verbatim by lane J3 (*"obviously it wouldn't work for anyone who doesn't have the
api"* / *"okay, ill be guided by you"*).

## WHAT WAS EXCLUDED, AND WHY — counted, no bodies

**Dream proposals examined: 39** (passes 6–13, on lane DA's corrected pass mapping).

| class | count | why |
|---|---|---|
| ruled + enacted before today | **22** | settled with a commit behind them |
| enacted today | **9** | 8 in `03605215` (lane E1) + 1 in the conductor's seat (pass 13 P5, the memory ceiling discharged by a shard and a standing rule) |
| overtaken | **3** | pass 9 P2, P5, P6 — the #278 store move changed the world under them |
| **remain** | **5** | pass 9 P3, pass 9 P4, pass 10 P1, P2, P3 — **all five are on the page** |

**Ruling-shaped questions examined: 18** (lane DA 9 · lane E1 6 · lane J3 3). **8 are on the page**
(DA Q4, Q5 ×3, Q6, Q8, Q9's two constants; E1 Q3, Q5; J3 Q2 — Q5 and Q6 merge into item 8). **10 are
not:**

| class | count | which |
|---|---|---|
| answered by today's enactments | **3** | DA Q1 (pass 6 P1 built), DA Q2 (pass 13 P6 enacted), DA Q7 (pass 13 P5 sharded) |
| the conductor's call, not Dave's | **4** | DA Q3 (pass-9 sitting vs folded — a sequencing choice) · E1 Q1 (wire `_parked.notice("cold-boot")` — a measured five-line wiring cost) · E1 Q4 (`component:icon-button` mints one retrieval id from two files — a defect with a clear fix) · J3 Q1 (the Jev receipts rotation rule, which **J3's own report names as "still the conductor's call"**) |
| purpose already ruled — enactment, not judgement | **1** | E1 Q2 `_validate_demo_page.py`: `s268-D3` built it as the pre-bake drive instrument and says what it is for |
| a note, not a question | **1** | E1 Q6 (`_state.py`'s three-character suffix, named so the next rename is not read as a new defect) |
| lane work | **1** | J3 Q3 (the fixture queries for the Memento re-rank) |

**Also excluded:** the **four sub-clauses** DA Q9 records as reserved deliberately and not oversights
(pass 11 P1(b), P4(a); pass 12 P2(b), P4(b)); and **the `_near_dupes.py` half of pass 13 P7** — the
audit shows it wired to nothing, but **`s273-D4` already inscribes it as the advisory watcher over the
`when` field definitions** (*"Synonym drift is watched by the EXISTING advisory door `_near_dupes.py`"*),
so wiring it is building, not deciding. ⚠ **This is a finding, not a judgement: the page would have
carried a "wire or retire" item for it had the ruling store not been searched.** Lane J3's technical
follow-ups (a)–(e) — the fixture M re-run, the level-0 firing, the `html_to_state` loss measurement,
the self-consistency repeat, the latency distribution — are lane work by the brief's own instruction
and are off the page.

Four items on the page (6, 7, 8, 9) are the **unfinished half** of something enacted today. Each says
so in its own `Technical` line, and the footer says it once in general, so Dave is not asked to rule
on work already done.

## VERIFICATION RUN

1. **Every item grepped against `knowledge/_rulings.json`** by field extraction over all 622 records
   (id/count only in the search pass; `says`/`status` truncated on the ten records read in full).
   Matches found and read: `s183-D1` (status: *"grading unit and GRADE_AGING_DAYS remain DAVE'S at the
   B3 return-with-numbers"* → **unruled, confirms item 4**) · `s256-D2` (*"NOT enacted, still Dave's …
   the picked POPULATION_DELTA_THRESHOLD=5"* → **unruled, confirms item 4**) · `s125-D1` (rules the
   step COUNT generated and forbids the hand-fix; the frozen **green** count is a different figure →
   **item 1 live**) · `s241-D1` (the 70,000 ceiling, shrink-only, *"Dave's to move"* → the term's
   re-measure is **not** ruled; **item 7 live**) · `ds-023` (pre-flight *arithmetic*, not the stamp's
   form → **item 9 live**) · `ds-025` (re-scopes the boot-floor decomposition; does not rule the
   re-measure) · `s186-D2` (promoted pass 6 P1–P4; the token **scope** reserved → **item 5 live**) ·
   `s268-D3`, `s269-D9`, `s273-D4` (the two instruments — **excluded**, above).
   **Zero matches** for `WRAP_COMMIT_SUBJECT` (item 3), `jev`/`typesafe` (item 10), and the verdict
   line's figure (item 1).
2. **`notes/_MEMENTO-DECISIONS.md` grepped** for the same nine tokens: 0 for `GRADE_AGING_DAYS`,
   `POPULATION_DELTA_THRESHOLD`, `near_dupes`, `validate_demo_page`, `WRAP_COMMIT_SUBJECT`, `jev`,
   `75 of 1`; 5 for `8,470`; 1 for `2026-11-06` — both the inscribed premises the items already name.
3. **Ages computed from file dates** (`stat` mtimes agree with the filenames to the day) against
   Monday 2026-09-21, stated in days, labelled measured on the page: pass 6 `2026-08-09` = **43** ·
   pass 9 `2026-08-23` = **29** · pass 10 `2026-08-30` = **22** · pass 13 `2026-09-20` = **1** ·
   grades sidecar `2026-09-13` = **8** · expiry `2026-11-06` = **46 days out** · ruling dates
   `s125-D1` 45 · `s183-D1` / `s186-D2` 36 · `s241-D1` 19 · `s256-D2` 13 · Jev lanes 0.
4. **Parsed with `html.parser`**: 0 unclosed tags, 0 mismatches. Counted **10 `section.item`, 10
   recommendation blocks, 10 rule boxes, 10 `Technical` lines, 10 answer-sheet rows, 4 group
   headings**, index numerals `01`…`10` in order. **Every answer-sheet slot string is byte-equal to
   its item's slot string** (checked pairwise, 10/10 OK). 0 external `src`/`href`, 0 `<script>`.

## WHAT I COULD NOT VERIFY

- **The `#226` digest date.** The page says pass 10's three were digested to Dave at #226 and never
  raised again, on lane DA's authority and the handoff's. I did not open the 45.8 KB digest HTML or
  date it, so **"#226" is carried, not measured** — the 22-day age is measured from the proposals file.
- **`s269-D9` / `s273-D4` as a complete disposition of `_near_dupes.py`.** Both records name the file
  and `s273-D4` gives it a consumer by name; neither is a *wire-it* instruction with a date. I judged
  that enough to exclude the item as enactment rather than judgement. **If the conductor disagrees,
  it is an eleventh item and the slot is `wire / retire`.**
- **Nothing on the page was probed live at this seat.** Every probe output quoted (`GARDENER BLOCKED`,
  `build_verdict_line()`'s string, the 173/136/169 parse rates, `WRAP_COMMIT_SUBJECT_RE` at `:5694`,
  the 185 `NOT CAPTURED` occurrences, the Jev latencies) is **lane DA's, lane E1's or lane J3's
  measurement, re-quoted**, not re-run — a read-only lane building a ruling surface should not move
  the instruments it cites. Provenance is named in each `Technical` line.
- **The `size:` stamp's "six of the last seven wrap commits"** and the sign-disagreement at #288 are
  pass 13 P3's own figures, carried.

## RULING-SHAPED QUESTIONS THAT AROSE IN BUILDING IT

1. **Two genuine judgement items were left off because they are outside the brief's five-plus-neighbours
   scope, and both should probably be on the next edition of this page.** `#241`'s question — what a
   midnight-spanning wrap should stamp — is untouched at **age 52** by the handoff's own count; and
   `_to_delete/` is an accumulating squatter that gate `4c` does not reach, which the handoff says in
   as many words *"is his"*. Neither is a dream proposal nor a lane question, so neither was eligible.
   **Adding them is a one-line edit and makes the page 12 items.**
2. **Does this page become the standing surface, or is it a one-off?** It is built to be re-issued:
   items are numbered independently of their ruling ids, the answer sheet is generated from the same
   slot strings as the items, and the two exclusion tallies are kept separate so they can be re-counted
   without arithmetic that pretends to sum. If it is standing, it wants a generator — which is the
   same argument items 1 and 8 make about typed figures, applied to this file.
3. **Four of the ten items are the residue of something enacted today** (6, 7, 8, 9). That is a
   pattern worth a rule: when a lane enacts the weak form of a proposal, the strong form should be
   *filed as a question put* in the same commit, rather than surviving only in a lane report's
   ruling-shaped list where the next census has to rediscover it.

## CHECKIN

Run once at open and once at close, `--window 200000 --no-block`, after
`pip install tiktoken --break-system-packages` (already satisfied in this sandbox).

- **OPEN:** `FILL           143,951 real  RESIDENT CONTEXT — what the last call actually sent`
- **CLOSE:** `FILL           143,951 real  RESIDENT CONTEXT — what the last call actually sent`

⛔ **THE TWO LINES ARE IDENTICAL, AND THAT IS A READING ABOUT THE INSTRUMENT, NOT ABOUT THIS LANE.**
`FILL` reports the **conductor's** resident context at the last logged call, not this sub's, so a lane
that runs entirely inside one conductor turn moves it by zero. My own consumption is therefore
**UNMEASURED at this seat** — it is not 0, and the matching pair must not be read as "the lane cost
nothing". ⚠ Recording the method failure too: I drafted the close line as `165,097` from expectation
**before running the arm**, and the run returned 143,951. The invented figure was struck, not
smoothed. This is the pass 9 P1 class exactly — a number written from what the writer assumed the
instrument would say — and it is worth a rule: **a gauge line is pasted from the run or it is not
written.**

⚠ Both runs printed `GRADES ⛔ SIDECAR PRESENT BUT VOID` — the new fence from `03605215` firing
correctly; grades are UNKNOWN, not clean, and no alert counts are claimed here. That is item 6.

COUNTS: items on page 10 · headings 4 · dream proposals examined 39 · dream proposals excluded 34 (22
ruled+enacted · 9 enacted today · 3 overtaken) · dream proposals remaining 5, all on the page · lane
ruling-shaped questions examined 18 · on page 8 · excluded 10 · sub-clauses excluded 4 · instruments
excluded as already-ruled 2 · rulings inscribed 0 · rulings store 622 unchanged · git writes 0 ·
files written 2 · page bytes 33,215 · external refs 0 · script tags 0 · parse errors 0 · ages
measured 12 · ages carried 1 (the #226 digest date) · checkin FILL open 143,951 · close 143,951
(identical — the conductor's window; this lane's own spend is UNMEASURED, not zero) · figures struck
after a failed measurement 1

REPLAY-THESE:
```
# no ruling exists for any item — field extraction only, id/count, never whole records
python3 - <<'PY'
import json,re
r=json.load(open('knowledge/_rulings.json'))['rulings']; print("TOTAL:",len(r))
for k,p in {'GRADE_AGING_DAYS':'GRADE_AGING_DAYS','POPULATION_DELTA':'POPULATION_DELTA_THRESHOLD',
 'near_dupes':'near.dupes','validate_demo_page':'validate_demo_page','8470':r'8,?470',
 'preflight':r'pre-?flight','jev':r'\bjev\b|typesafe','expiry':'2026-11-06',
 'WRAP_COMMIT_SUBJECT':'WRAP_COMMIT_SUBJECT','verdict75':r'75 of 1[0-9][0-9]'}.items():
    print(k, [v.get('id') for v in r if re.search(p, json.dumps(v), re.I)])
print("s293/s294:", [v.get('id') for v in r if str(v.get('id','')).startswith(('s293','s294'))])
PY

# the decisions ledger, grep only
for p in GRADE_AGING_DAYS POPULATION_DELTA_THRESHOLD near_dupes validate_demo_page \
         "8,470" WRAP_COMMIT_SUBJECT "2026-11-06" jev "75 of 1"; do
  printf '%s ' "$p"; grep -c -i -- "$p" notes/_MEMENTO-DECISIONS.md; done

# ages: file dates agree with filenames to the day
for f in notes/_dream/2026-08-09-proposals.md notes/_dream/2026-08-23-proposals.md \
         notes/_dream/2026-08-30-proposals.md notes/_dream/2026-09-20-proposals.md; do
  stat -c '%y %n' "$f"; done
python3 -c "
import datetime; t=datetime.date(2026,9,21)
for d in ['2026-08-07','2026-08-09','2026-08-16','2026-08-23','2026-08-30','2026-09-02',
          '2026-09-08','2026-09-13','2026-09-20','2026-11-06']:
    y,m,dd=map(int,d.split('-')); print(d,(datetime.date(y,m,dd)-t).days if d>'2026-09-21' else (t-datetime.date(y,m,dd)).days)"

# the page parses, and its answer sheet matches its items
python3 - <<'PY'
from html.parser import HTMLParser; import re
src=open('notes/_lanes/294/R/review-page.html',encoding='utf-8').read()
print("sections:",src.count('<section class="item">'),"| sheet rows:",
      len(re.findall(r'class="slot">',src)),"| tech:",src.count('class="tech"'),
      "| scripts:",src.lower().count('<script'),"| ext refs:",
      len(re.findall(r'(?:src|href)=["\'](?!#)',src)),"| bytes:",len(src.encode()))
c=lambda x:re.sub(r'\s+',' ',re.sub(r'<[^>]+>','',x)).strip()
a=[c(x) for x in re.findall(r'class="slot">(.*?)</span></li>',src,re.S)]
b=[c(x) for x in re.findall(r'class="words">(.*?)</div>',src,re.S)]
print("slots match:", a==b, len(a))
PY

# pass numbering and the P-headings the items cite
for f in 2026-08-09 2026-08-23 2026-08-30 2026-09-20; do
  head -1 notes/_dream/$f-proposals.md; grep -n "^### P" notes/_dream/$f-proposals.md | cut -c1-90; done

# open and close gauge
python3 knowledge/_checkin.py --window 200000 --no-block | grep -i FILL
```

## FILES WRITTEN

- `notes/_lanes/294/R/review-page.html` — the judgement-only review page. 10 items, 4 headings,
  answer sheet at the top, exclusion tallies in the footer. 33,215 B, self-contained.
- `notes/_subreports/2026-09-21-294-R-review-page.md` — this file.

Nothing else was written, staged or committed. `knowledge/_rulings.json`, `GOOD-MORNING.md`,
`_CARRIES.md`, `_LIVE-STATE.md`, `_CHAIN.md`, `notes/_lanes/293/**` and `dist/` were not touched; no
generator and no `_build_all.py` was run; **no git command was run at all**, read or write, because
the foreign `.git/index.lock` from another lane was present for the whole of this seat and the safest
read is the one you do not need. The conductor commits these two paths via
`knowledge/_git_commit.sh`.

---

## ADDENDUM — #294 lane R2: items 11 and 12 added by ADDITION

provenance: **conductor, on lane R's own could-not-resolve note** — § RULING-SHAPED QUESTIONS THAT
AROSE IN BUILDING IT, item 1, which names both of these and says *"both should probably be on the
next edition of this page"*. Nothing above this line is rewritten. Scope: ⛔ READ-ONLY on git
(a foreign `.git/index.lock` was present for the whole of this seat; no git command was run, read or
write, and the lock was not touched). Two files changed: the page, and this addendum.

### WHAT WAS ADDED

A **fifth group, "Housekeeping that is yours"**, and two items in the existing item markup exactly
(plain-prose title · 2–3 sentences of meaning · **My recommendation** one clause · **One word** slot ·
**Technical** receipts line):

- **11. What a wrap that crosses midnight should stamp** — slot `session / commit / both`,
  recommendation **both**.
- **12. The holding directory nothing is allowed to empty** — slot `purge / keep / gate`,
  recommendation **gate**.

Also updated: `<title>` and `<h1>` (**Ten → Twelve**), the header label (*"Your judgement, twelve
items"*), the standfirst count sentence, two new answer-sheet rows, one new footer note recording the
addition and stating that the two exclusion tallies are **unchanged** (they counted what they
counted), and the footer signature. Page **33,215 B → 40,681 B**.

### MEASUREMENTS TAKEN AT THIS SEAT

**Item 11 — #241's stamp question.** Five `WRAP DATE SPLIT` lines stand at `GOOD-MORNING.md`
**lines 9–13**, added by ADDITION at sessions 241 · 248 · 261 · 277 · 291, none removed. Origin
`_DECISION-HISTORY/2026-09-02-241-the-measured-diet-and-the-derived-band.md`, file date
**2026-09-02** (mtime `2026-09-02 23:15:44`), so the question is **19 days** old measured against
Monday 2026-09-21. `s241-D1` and `s241-D2` both carry `date 2026-09-02`; neither rules the stamp.
The nine `s261-D*` records mention the split in `evidence` and none disposes of it.

⚠ **THE CONDUCTOR'S FIGURE DID NOT VERIFY AND IS CORRECTED RATHER THAN CARRIED.** The instruction
said *"age 52 days measured from 2026-08-01"*. Both halves fail a check: 2026-08-01 → 2026-09-21 is
**51** days, not 52, and **#241 is not dated 2026-08-01** — it is 2026-09-02, which is 19 days.
The 7 / 20 / 36 / 50 series the five header lines carry is **sessions since #241**, not days —
248−241=7 · 261−241=20 · 277−241=36 · 291−241=50, exact on all four — which at #294 makes it the
**53rd session**. ⛔ **The unit is unlabelled on every one of the five lines, and that is what made
the figure travel wrong.** The page states both numbers with their units and names the arithmetic.
This is the same class as the struck `165,097` above: a figure written from expectation, caught by
the run.

**Item 12 — `_to_delete/`.** Measured 2026-09-21 by `os.walk` / `os.lstat`: **108** top-level
entries · **5,000** files and directories in total · **134,691,274 bytes** (128.5 MiB). Oldest
top-level entry **2026-08-06**, age **46 days**. Oldest file anywhere
`_to_delete/logos-identifier-282/masterbrand-identifier-light-colour.svg`, **2026-06-17**, age
**96 days**. Newest is `_to_delete/_s276-D6-lane-ownership-guard/` from today's lane E1 — one file,
`_validate_lane_ownership.py`, **3,542 B**. Gitignored at `.gitignore:34`.

★ **4c PROVABLY DOES NOT REACH IT, AND THIS IS THE RECEIPT RATHER THAN THE CLAIM.** Step 4c is
`knowledge/_gate_scratch_hygiene.py`; its `SCRATCH_ROOTS` (lines 35–40) are `/var/tmp`, `/tmp`,
`~/tmp`, `~/.cache`, plus `~/.local` under `--wrap` — **all outside the repo** — and `_to_delete`
occurs **0** times in its 150 lines. The squatter is named verbatim in `GOOD-MORNING.md`'s #293
commit-state block: *"`_to_delete/` is still an accumulating squatter that `4c` does not reach — now
joined by `_to_delete/_s276-D6-lane-ownership-guard/` from lane E1 — and what to do about it is
his."* ⚠ **A hand purge may not even be possible:** `s282-D4` (ruled 2026-09-18, the only one of the
**622** records that names the directory) calls it *"a gitignored holding dir: **the mount refuses
unlink**"*. That is why `gate` is recommended and `purge` is not, and the recommendation says so.

### VERIFICATION RUN

Parsed with `html.parser` (explicit open/close stack, void elements excluded): **0 mismatches,
0 unclosed tags**. Counted **12 `section.item` · 12 `rec` blocks · 12 `rulebox` · 12 `Technical`
lines · 12 answer-sheet rows · 5 group headings**, index numerals `01`…`12` in order. **All twelve
answer-sheet slot strings are byte-equal to their items' slot strings** (tag-stripped,
whitespace-normalised, compared pairwise and by `.encode()`; 12/12 OK). **0 `<script>` tags,
0 external `src`/`href`** — still self-contained. **0 remaining occurrences** of `Ten things` /
`ten items`.

⛔ **NOTHING WAS INSCRIBED.** `knowledge/_rulings.json` stands at **622**, read by field extraction
only (id / date / `says`[:160] / `status`); no `s294-` id exists. `GOOD-MORNING.md`, `_CARRIES.md`,
`_LIVE-STATE.md`, `_CHAIN.md` and `knowledge/_gate_scratch_hygiene.py` were **read, never written** —
in particular the five header lines were NOT collapsed, because collapsing them is what item 11 asks
Dave to authorise.

COUNTS: items on page 12 (was 10) · headings 5 (was 4) · items added 2 · slot strings byte-equal
12/12 · parse errors 0 · page bytes 40,681 (was 33,215, +7,466) · external refs 0 · script tags 0 ·
`_to_delete/` top-level 108 · `_to_delete/` total entries 5,000 · `_to_delete/` bytes 134,691,274 ·
`_to_delete/` oldest top-level 46 days · oldest file 96 days · DATE-SPLIT header lines 5 · #241 age
19 days / 53 sessions · rulings inscribed 0 · rulings store 622 unchanged · git commands run 0 ·
conductor figures corrected after measurement 1 (the "52 days from 2026-08-01")

REPLAY-THESE:
```
# _to_delete/ measured
python3 - <<'PY'
import os,datetime
root='_to_delete'; t=datetime.date(2026,9,21)
tot=n=0; oldest=None
for dp,dn,fn in os.walk(root):
    n+=len(dn)
    for f in fn:
        p=os.path.join(dp,f); st=os.lstat(p); tot+=st.st_size; n+=1
        if oldest is None or st.st_mtime<oldest: oldest,on=st.st_mtime,p
print("top-level:",len(os.listdir(root)),"| entries:",n,"| bytes:",tot)
rows=sorted((os.lstat(os.path.join(root,e)).st_mtime,e) for e in os.listdir(root))
d=datetime.date.fromtimestamp(rows[0][0]); print("oldest top-level:",rows[0][1],d,(t-d).days,"days")
d=datetime.date.fromtimestamp(oldest); print("oldest file:",on,d,(t-d).days,"days")
PY

# 4c cannot see it
grep -n "SCRATCH_ROOTS\|WRAP_ONLY_ROOTS" knowledge/_gate_scratch_hygiene.py
grep -c "_to_delete" knowledge/_gate_scratch_hygiene.py   # expect 0
grep -n "_to_delete" .gitignore                            # expect 34

# #241: file date, and the header ages are SESSIONS not days
stat -c '%y %n' _DECISION-HISTORY/2026-09-02-241-*.md
grep -c "WRAP DATE SPLIT" GOOD-MORNING.md                  # expect 5
python3 -c "
import datetime;t=datetime.date(2026,9,21)
print('days since 2026-09-02:',(t-datetime.date(2026,9,2)).days)
print('sessions:',[(s,s-241) for s in (248,261,277,291,294)])"

# the page: 12 items, 12 rows, slots byte-equal, still self-contained
python3 - <<'PY'
from html.parser import HTMLParser
import re
src=open('notes/_lanes/294/R/review-page.html',encoding='utf-8').read()
VOID={'br','hr','img','meta','link','input','source','area','base','col','embed','param','track','wbr'}
class P(HTMLParser):
    def __init__(s): super().__init__(convert_charrefs=True); s.st=[]; s.err=[]
    def handle_starttag(s,t,a):
        if t not in VOID: s.st.append(t)
    def handle_endtag(s,t):
        if t in VOID: return
        if not s.st or s.st[-1]!=t: s.err.append((t,s.getpos()))
        else: s.st.pop()
q=P(); q.feed(src); print("errors:",q.err or "NONE","| unclosed:",q.st or "NONE")
c=lambda x:re.sub(r'\s+',' ',re.sub(r'<[^>]+>','',x)).strip()
a=[c(x) for x in re.findall(r'class="slot">(.*?)</span></li>',src,re.S)]
b=[c(x) for x in re.findall(r'class="words">(.*?)</div>',src,re.S)]
print("items:",src.count('<section class="item">'),"| rows:",len(a),"| byte-equal:",a==b,
      "| scripts:",src.lower().count('<script'),
      "| ext:",len(re.findall(r'(?:src|href)=["\'](?!#)',src)),"| bytes:",len(src.encode()))
print("idx:",re.findall(r'class="idx">(\d+)<',src))
print("stale ten:",re.findall(r'[Tt]en [Tt]hings|[Tt]en items',src) or "NONE")
PY
```

### FILES CHANGED BY THIS ADDENDUM'S SEAT

- `notes/_lanes/294/R/review-page.html` — items 11 and 12 + group five added; title, header label,
  standfirst, answer sheet and footer updated for the count. **40,681 B**, self-contained.
- `notes/_subreports/2026-09-21-294-R-review-page.md` — this addendum, APPENDED. The report above it
  is unmodified.

Nothing else was written, staged or committed. **No git command was run at all** — the foreign
`.git/index.lock` was present for the whole of this seat and was not touched.
