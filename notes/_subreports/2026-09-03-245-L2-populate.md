# `#245`-`L2-populate` — L2 RULED AND ENACTED: six rulings inscribed, 20 metas typed, 11/14 fallbacks proven by JS-off render, 20 snippets carry `#behaviour-manifest`, prose `behaviour` now BLOCKS

session: `#245` · 2026-09-03
window: lane L2-populate (Apollo v1.0.6 build) — one Fable sub, parallel to lanes D / L3 / L4 / R
sub index: `L2-populate`
brief: in-chat lane brief (no `notes/_briefs/` file was cut); ruling source = chat #245, Dave: *"take the recommendations"*
tokens: `UNMEASURED — no message.usage at a sub's seat`. Shape for the conductor's ledger: ~75 tool
calls; whole-file reads of the L2 report (318 lines) and `_TEMPLATE.md`; sliced reads of
`_inscribe_ruling.py`, `_governs.py`, `_validate_receipt.py`, `gen_component_partials.py`,
`gen_library_214.py`, `_build_all.py::STEPS`; 1 PNG viewed by eye (Calendar JS-off); no archive
re-read beyond one `_memento_search.py` probe (miss) and a grep of `_CARRIES.md` for the serial order.

## VERDICT

**All six brief items DONE; the live tree is changed and every change is measured.** (1) Six
rulings `s245-D1`…`s245-D6` INSCRIBED through `_inscribe_ruling.py` (dry-run then `--write`,
reconstruction proof PASSED on each; 338 → 344; all six ids read back). (2) The L2 schema fragment
is APPLIED to `knowledge/components/meta.schema.json` with `partial` widened to
`string | string[] | null` (D2) and `events` optional (D4); the 20 metas are MIGRATED BY ADDITION
(prose verbatim under `$note`) by textual span replacement, 136/136 live metas validate; the 14
candidate `fallback` readings were RENDERED JS-OFF (D5), **11 PROVEN and written, 3 UNPROVEN and
left `null`** — and one candidate (Calendar) was FALSE: the live grid is script-rendered and draws
EMPTY with JS off. (3) The generator was already wired live by #238 (silent on prose); the ordered
regen serial was run step by step (ramp first, index last, never `_build_all.py`): **20 snippets
now carry `#behaviour-manifest` beside `#token-manifest` with the meta's four address fields
verbatim; the other 116 are byte-identical** (sha256 before/after). The serial surfaced a defect
the migration exposed — `gen_library_214.py` counted the JSON manifest as behaviour JS and flipped
6 passive components to `ships_behaviour:true` — fixed by addition and re-run. (4) The gate ratchet
(D3): a meta whose `behaviour` is still prose is now `FAIL:BEHAVIOUR-PROSE`, rc 1, named; NONE stays
UNPROVEN; driven on the REAL receipted dashboard standalone and through `_validate_screen.py`
(bites both prose forms, restored, control green); 0 live metas are red (20 TYPED · 116 NONE · 0
PROSE). Both the gate and the generator now read `partial` as a list through ONE function; 4 gate +
2 generator mutants bite, 5 live-tree bites, controls green. (5) Rule 2a sits beside rule 2 in
`apollo-spider/skills/generate-from-canon/SKILL.md`, rule 2 unworded; the pack-doc gate's finding
count is unchanged by it (215 → 215, 0 name the file). (6) The L2 row in `_REVIEW-SIGNOFF.md` reads
RULED #245. **Nothing committed, nothing pushed, memory untouched; `_capture_gate.py` and
`notes/_GAUGE-LOG.md` were another lane's and were not opened.**

**Two things the conductor must not skim.** (a) Pitfall 2 of the L2 brief was REAL: of the 14
candidate readings, one was false (Calendar), one was untestable on its own specimen
(Command-palette, authored open), and one was half-right (File-upload — the Browse `<button>` is
dead JS-off, but the `<label for=fu-input>` opens the native chooser). Accepting the 14 on a reading
would have written a false `fallback` into canon. (b) The library index was ALREADY stale before
this lane (135 → 136 components: `Template-dashboard-bento` was committed at `e7cf3db` without an
index regen, and its thumbnail is still missing) — the serial regenerated it; that drift is not
this lane's but it now sits in the working tree with mine.

COUNTS: findings `9` · ruling-shaped `2` · UNPROVEN `5`

Brief's own counts line: **rulings 338 → 344 (`s245-D1`…`s245-D6`) · metas populated 20/20 ·
fallbacks proven 11/14, unproven 3/14 · snippets changed 20/136, byte-identical 116/136 · gate arms
35 → 39 (S flipped; S2 S3 W2 U2 added), gate mutants 4/4 bite, generator mutants 2/2 bite, live
bites 5/5 · schema arms 13/13 · JS-off assertions 67 named + 18 observed over 14 snippets,
pageerrors 0.**

## What was done

**Region 1 — inscribe.** Read `knowledge/_inscribe_ruling.py` (docstring: exact keys
`id/ruled/date/by/says/governs/evidence/status`; R1–R6; `_governs.evidence_form` ladder — `chat #<n>`
and `commit <sha>` are the non-rolling legal forms). Probed the store: `python3 -c "import json;
d=json.load(open('knowledge/_rulings.json'))['rulings']; print(len(d), d[-1]['id'])"` → **338
`s244-D2`**. Wrote six entries (`assets/…/rulings/s245-D*.json`): `says` carries the recommendation
text put to Dave plus his words *"take the recommendations"*; `evidence` = `chat #245 2026-09-03 -
Dave: "take the recommendations", quoted in says` + the L2 report path + `commit 7ad3a26`. `governs`:
D1 schema / gate / generator / `knowledge/components/`; D2 schema / gate / generator; D3 gate /
`_validate_screen.py` / components; D4 schema / generator / components; D5 components / schema /
`_RUNBOOK-render-verify.md`; D6 `knowledge/snippets/Template-dashboard-bento.reference.html` +
`knowledge/canon/canon.css` (the ONE source and its regenerated projection — from L3's
`knowledge/_tmp/l3-245/rename-plan.json`, read only). Dry-run ×6 (`inscribe-dry-run.txt`, all
"would insert cleanly … reconstruction proof PASSED"), `--write` ×6 (`inscribe-write.txt`: 338 → 339
→ … → 344, each "reconstruction proof PASSED (all other bytes identical)"), re-probe **344**, ids read
back `['s245-D1', 's245-D2', 's245-D3', 's245-D4', 's245-D5', 's245-D6']`. `git diff --stat
knowledge/_rulings.json` = **+108 / −0**.

**Region 2 — schema + 20 metas + JS-off fallbacks.** Read the L2 report's REPLAY-THESE (§§ 02/06 of
the review page via the report's own text; finding 6 + drive-D; `behaviour-migration.json` items
`date-picker` and `badge`; findings 7/8; `_validate_receipt.py` docstring 82–113). Copied the #238
fragment to `assets/…/behaviour.schema.fragment.json` with ONE change — `partial` →
`{"type": ["string","array","null"], "pattern": …, "items": {…}, "minItems": 1, "uniqueItems": true}`
— and the descriptions re-pointed at `s245-D1/D2/D3/D4`; applied it with the L2 applier
(`apply_schema.py`, text surgery on two anchors, refuses on drift, `check_schema` before write):
`meta.schema.json` **+89 / −1** (`meta.schema.applied.diff`), 230 → 318 lines. Probed the 20 metas
for round-trip safety: **18/20 do NOT round-trip through `json.dumps`**, so `migrate_metas.py` does a
string-aware TEXTUAL SPAN REPLACEMENT of the top-level `"behaviour"` value only, with four proofs per
file (one key · reconstruction `==` · object equality with only `behaviour` replaced · Draft-7
valid). Dry-run 20/20, write 20/20 (`migrate-dry-run.txt`, `migrate-write.txt`). Validation
(`validate-metas.txt`): **136 live metas · 136 valid · 0 red · `behaviour_state` {NONE: 116, TYPED:
20}**. Then D5: `jsoff_render.py` — playwright chromium via `knowledge/_render/seat_env.sh` (`SEAT_ENV:
OK … faces=10/404 farm=10/10`), `browser.new_context(java_script_enabled=False)`,
`page.goto("file://…")` for each of the 14, NEVER `set_content`; each candidate clause about the
component's OWN behaviour became a named assertion that was DRIVEN (typed, clicked, blurred, keyed,
viewport resized for the container query) and read off the DOM; before/after PNG per snippet (28).
Two probe defects were mine and fixed before the verdicts were taken (Textarea `#t1` is pre-filled so
`startswith` was wrong → `endswith`; Tree's `.tr-loading` matched a static ladder specimen → scoped to
`#live-tree`); the File-upload probe gained the `<label for=fu-input>` arm after the DOM census showed
the label. Result (`jsoff-render.txt`, `jsoff-render.json`): **PROVEN 11 — amount-input, anchor-nav,
date-picker, date-range-picker, form-layout, secure-entry, stepper, tab-bar, textarea, time-picker,
tree; UNPROVEN 3 — calendar, command-palette, file-upload**; 67 assertions, 18 observations, 0 page
errors. `update_fallback.py` wrote the 11 `fallback` sentences (MEASURED wording, each clause mapping
to a named assertion, not the #238 candidates copied) and dropped their `$unproven`; the 3 keep
`fallback: null` + `$unproven: ["fallback"]`; all 14 gained `$fallbackRender` (a `$`-key the schema's
`patternProperties ^\$` permits) saying PROVEN/UNPROVEN by the render, the counts, what was seen, and
the evidence path. Re-validated: **136/136 valid; typed fallback set 17 (6 passive + 11 proven), null
3** (`validate-metas-after-fallback.txt`). `git diff --stat knowledge/components/*.meta.json` = 20
files, +250 / −54 (the −54 are the prose lines relocated verbatim under `$note`).

**Region 3 — generator + serial.** `gen_component_partials.py` was ALREADY wired live at #238
(`run()` calls `run_behaviour_manifests(write)` at line 796; silent on prose). Hashed all 136
snippets first (`snippets-sha256-before.txt`). `--check` before: **rc 1, exactly the 20 out of sync**
(`gen-check-before.txt`). Widened `behaviour_manifest_fails` to iterate `VR.partial_names(typed)`
(new in the gate — ONE place for the list reading) and added selftest arm (vii-b) (list with a ghost
refused; list of registered names emitted and carried verbatim). Serial, one step per bash call, in
`_build_all.py::STEPS` order (`regen-serial.txt`): `gen_component_partials.py` (write) → **"20
consumer block(s) injected/refreshed"**, `--check` OK · `gen_token_ramp.py` (RAMP FIRST) → 0 synced,
148 in sync · `canon/gen_canon_components.py` → no change (136), `--check` OK · `gen_snippet_tokens.py
--check --quiet` → 5015 bindings, 0 would change · `_validate_snippets.py` → 136, 0 failures ·
`canon/gen_theme_cascade.py --check` OK · `gen_showroom.py` → **20 written**, `--check` OK ·
`_render/gen_library_214.py --check` → **OUT OF SYNC** → write → finding 3 (6 passives flipped to
`ships_behaviour:true`) → `SCRIPT_RE` fixed by addition → re-run → `--selftest` rc 0 → `--check` OK
(144 components) · `_build_memento_index.py` (INDEX LAST) → 1994 records, `--check` current.
`_render/gen_thumbs.py` NOT run (declared — see UNPROVEN). Hash table (`snippets-hash-table.txt`):
**136 snippets · 20 changed · 116 byte-identical**; the 20 manifests carry `script/partial/events/
fallback` equal to the meta's and sit after `#token-manifest`, before the inline `<script>`, 20/20;
0 byte-identical snippets carry a manifest. Live bites on the real tree, each restored and
hash-verified (`live-bites-generator.txt`): B1 FOREIGN address → refused naming "ANOTHER component";
B2 missing `.js` → "does not exist"; B3 manifest deleted → out of sync; B4 manifest address tampered
one byte → out of sync; B5 meta reverted to prose while the block stands → orphan refused. **5/5
bite, control green after restore.**

**Region 4 — gate ratchet (D3) + list `partial` (D2).** `_validate_receipt.py` (937 → 983 lines):
docstring 4b rewritten for PROSE (blocking, s245-D3) and `partial` (list, s245-D2);
`behaviour_verdict` returns `FAIL:BEHAVIOUR-PROSE — <region>: meta <path> carries a behaviour key
that is still pre-s234-D5 prose (<type>) … type it … and regenerate` when `state == "PROSE"`; PROSE
removed from the end-of-page UNPROVEN gap set (NONE / NO-META stay); new `partial_names(typed)`;
the partial loop judges every name. Selftest: arm S flipped from PASS+UNPROVEN to `BEHAVIOUR-PROSE`,
S2 (string) and S3 (list) added, W2 (list of one registered, loaded → PASS) and U2 (list with a ghost
→ UNREGISTERED) added: **39 arms green** (`selftest-validate-receipt.txt`). `_validate_screen.py` NOT
modified — step 0 inherits (proved below). Mutants on a scratch copy of the tree (`mutants.txt`,
`mutants-generator.txt`): gate M1 ratchet removed → S/S2/S3 red; M2 wrong reason name → red; M3 list
first-name-only → U2 red; M4 list ignored → U2 red; generator M1 list skipped → red; M2 block carries
first name only → red; **both controls green**. Drive on the REAL page
`dashboards/international-banking-dashboard.regen-v2-receipt.html` (`gate-drive-real-page.txt`):
after the 20 are typed → `RESULT: PASS`, 5 regions `meta:NONE` still UNPROVEN (unchanged from #238's
drive-0); chain → `receipt: ✅ 12 region(s)`, `compose: ❌ 191 hex` (L1 Q2, pre-existing, do-not-rule);
BITE — `pagination.meta.json` given a prose string → standalone `rc=1 FAIL:BEHAVIOUR-PROSE`, chain
`receipt: ❌ BEHAVIOUR-PROSE`; BITE 2 — prose as an object without `script` → rc 1; restored (sha
equal), control PASS. Default population (`gate-default-population.txt`): 7 × `UNPROVEN:NO-RECEIPT`,
`RESULT: PASS`. Schema arms (`schema-arms.txt`) 13/13.

**Region 5 — rule 2a.** Inserted the L2 § 07 text VERBATIM as `2a.` directly after rule 2 in
`apollo-spider/skills/generate-from-canon/SKILL.md` (the pack source the manifest claims —
`_gen_pack_manifest.py:2663`; `designer-skills-v1/` is a frozen RELEASE and was not touched), in the
8a house style (4-space continuation). `git diff` = **+9 / −0**, rule 2's three lines unchanged.
Pack-doc gate (`pack-doc-gate.txt`): staged the HEAD manifest (`ff1b2f53`) to `/dev/shm` with
`_gen_pack_manifest.py --stage`, overlaid the edited file at `skills/generate-from-canon/SKILL.md`,
ran `_gate_pack_docs.py --stage <dir>` before and after: **215 findings → 215; findings naming
`generate-from-canon` 0 → 0.** (The 215 are the pre-existing advisory baseline; the build script runs
this gate advisory, not `--strict`.)

**Region 6 — sign-off row.** `knowledge/_REVIEW-SIGNOFF.md` L2 row status cell replaced with
`✅ **RULED #245 — Dave, in chat … *"take the recommendations"* ⇒ s245-D1…D5 (+D6) …**` carrying the
enactment counts and `*(was: ⬛ AWAITING …)*`, the file's convention (rows 202–364). +1 / −1 on that
row; no new row (no new surface was created).

### FILES TOUCHED (for the reconcile)

| path | delta | state |
|---|---|---|
| `knowledge/_rulings.json` | +108 / −0 | six entries via `_inscribe_ruling.py` |
| `knowledge/components/meta.schema.json` | +89 / −1 | fragment applied; `partial` list; `events` optional |
| `knowledge/components/*.meta.json` (20) | +250 / −54 | typed by addition; prose under `$note`; 11 fallbacks + 14 `$fallbackRender` |
| `knowledge/snippets/*.reference.html` (20) | +253 / −0 | `#behaviour-manifest` derived; 116 byte-identical |
| `knowledge/_validate_receipt.py` | +56 / −10 (937 → 983) | PROSE blocks; `partial_names`; arms S/S2/S3/W2/U2 |
| `knowledge/gen_component_partials.py` | +15 / −3 (1255 → 1267) | list `partial` via `VR.partial_names`; arm (vii-b) |
| `knowledge/_render/gen_library_214.py` | +5 / −1 | `SCRIPT_RE` excludes `type="application/json"` |
| `apollo-spider/skills/generate-from-canon/SKILL.md` | +9 / −0 | rule 2a beside rule 2 |
| `showroom/*.html` (20) + `showroom/index.html` + `showroom/index.json` | 20 × ±1 line; index.html +7 / −7, index.json +19 / −2 | regenerated by the serial (index: pre-existing 135→136 drift, finding 4) |
| `knowledge/_memento-index.json` | +21 / −21 | INDEX LAST |
| `knowledge/_REVIEW-SIGNOFF.md` | +1 / −1 (L2 row) | RULED #245 |
| `knowledge/_screen-gate/international-banking-dashboard.regen-v2-receipt.md` | +1 / −0 | REGENERATED by the chain drive (the record gains the `UNPROVEN:behaviour-address` line the #238 gate emits; the committed record predated it). ⚠ The BITE run had left it reading `❌ BEHAVIOUR-PROSE`; a post-restore chain run reset it — `gate-drive-real-page.txt` § RECORD RESET |
| `notes/_subreports/2026-09-03-245-L2-populate.md` + `assets/…/` (66 files, 2.3 MB incl. 28 PNG = 1.9 MB) | NEW | this report + evidence |

⛔ Not touched: `knowledge/_capture_gate.py`, `notes/_GAUGE-LOG.md` (another lane's; both already
`M` in `git status` before this lane began) · `dashboard/index.html` (`M`, mtime 19:12:49, written by
`gen_dashboard.py` — which only `_build_all.py` calls and no command of this lane invokes; another
lane's, appeared mid-window) · `_validate_screen.py` · `gen_provenance_receipt.py` ·
`designer-skills-v*/` · `_to_delete/` · the 14 dataviz metas · `_CARRIES.md` · memory · git (no
commit, no push).

## Findings

**1 — THE CALENDAR CANDIDATE WAS FALSE: THE LIVE GRID IS SCRIPT-RENDERED AND DRAWS EMPTY WITH JS
OFF.** Probe: JS-off `document.querySelector('#cal-body').innerHTML.length` → **0**;
`#cal-table tbody button.cal-day` → **0**; `#cal-title` → `'\xa0'`; `Calendar.reference.html:268`
`<tbody id="cal-body"></tbody>`, `:486` `document.createElement('button')`, `:515 function render()`.
The #238 census that read "month grid renders (server-authored buttons)" counted the STATIC states
specimen's buttons (`:302–348`), not the live grid. Screenshot `jsoff-calendar-before.png` (viewed by
eye: nav arrows and weekday heads, no title, no days). `fallback` stays `null`; `$fallbackRender`
says so. This is the brief's pitfall 2 made concrete — the render, not the reading, was right.

**2 — THE FILE-UPLOAD `<button>` IS DEAD JS-OFF BUT ITS `<label>` IS NOT.** Probe: `expect_file_chooser`
on `#fu-browse` (a `<button type=button>`, `File-upload.reference.html:249`) → **no chooser**; on
`label[for='fu-input']` (`:243`) → **chooser opened**; `#fu-input` computed `1px × 1px, opacity 0,
absolute`. The candidate's conditional ("Browse still works IF the control wraps a native input") is
half-right in a way that matters to a designer: the visible control does nothing, the label does.
Left UNPROVEN; the measured sentence is one word away in `$fallbackRender`.

**3 — TYPING THE METAS FLIPPED SIX PASSIVE COMPONENTS TO `ships_behaviour:true` IN THE LIBRARY INDEX.**
Probe: `git diff showroom/index.json` after the first `gen_library_214.py` run → `ships_behaviour
false → true` on account-card, action-bar, badge, confirmation, eyebrow, summary; cause
`gen_library_214.py:454` `SCRIPT_RE` excluded only `id="token-manifest"`, so the new
`<script type="application/json" id="behaviour-manifest">` counted as JS lines (`js_lines`, `:458`).
Fixed by addition: `(?![^>]*type="application/json")`. After: "no behaviour JS: 36" (was 30 before
the manifest existed — 30 + the 6 passives), `--selftest` rc 0, index diff no longer touches
`ships_behaviour`. A derived block that is DATA must be excluded by CLASS, not by id, or the next
manifest repeats this.

**4 — THE LIBRARY INDEX WAS ALREADY STALE BEFORE THIS LANE (135 → 136).** Probe: `git diff
showroom/index.json` → `"$component_count": 135 → 136`, `"$count": 143 → 144`, one new entry
`template-dashboard-bento`; `git log -1 -- showroom/index.json` = `71bb2f7`, `git log -1 --
knowledge/snippets/Template-dashboard-bento.reference.html` = `e7cf3db`; `gen_library_214.py` prints
"missing thumbnail: 1 — template-dashboard-bento". The bento template was committed without the
index regen. The serial fixed the index; the thumbnail is `gen_thumbs.py`'s (not run — UNPROVEN
below). Not this lane's defect; it now rides in the working tree beside this lane's changes.

**5 — 18 OF THE 20 METAS DO NOT ROUND-TRIP THROUGH `json.dumps`.** Probe: for each meta,
`json.dumps(obj, indent=i, ensure_ascii=e)` for i ∈ {1,2,4,tab}, e ∈ {T,F} vs raw → match on 2/20
(anchor-nav, command-palette). A load→mutate→dump migration would have reformatted 18 files
wholesale — the `_inscribe_ruling.py` defect class, on metas. Hence the span-replacement writer with
reconstruction proof (`migrate_metas.py`, `update_fallback.py`).

**6 — THE #238 GENERATOR AND GATE WERE BUILT SINGULAR; D2 WOULD HAVE MADE A LIST A REFUSAL.** Probe:
`gen_component_partials.py` (before) `if not isinstance(partial, str) or partial not in reg: fails…`
— any list refused; `_validate_receipt.py` (before) `if partial not in reg` — a list raises
`TypeError: unhashable type: 'list'` at the first list meta. Both now go through `VR.partial_names`
(one place). Arms W2/U2 and (vii-b); mutants M3/M4 and gen M1/M2 prove each side can fail.
Population today: 0 live metas carry a list (the dataviz migration is a carry).

**7 — `_validate_screen.py` NEEDED NO CHANGE FOR D3.** Probe: `gate_receipt()` (`_validate_screen.py:
54–74`) downgrades ONLY `fails == ["NO-RECEIPT"]`; every other fail blocks. The chain drive shows
`- receipt: ❌ BEHAVIOUR-PROSE` + `RESULT: FAIL`, rc 1, with the gate file the only edit.

**8 — THE PROSE RATCHET REDS 0 LIVE METAS TODAY, BY MEASUREMENT.** Probe: `VR.behaviour_state` over
136 metas → `{'NONE': 116, 'TYPED': 20}`; PROSE 0. The L2 recommendation's "0 would go red then"
holds; the one bite needed a planted prose value.

**9 — THE TEXTAREA IS PRE-FILLED, AND A NAIVE PROBE READS THAT AS A FAIL.** Probe: `Textarea.reference.
html:170` `<textarea id="t1" … maxlength="300">Please prioritise this run …</textarea>` (78/300);
`value.startswith(typed)` → False, `value.endswith(typed)` → True. Recorded because a JS-off pass that
asserts on the value of a specimen must read the specimen first; the first run's "FAIL" was mine.

## RULING-SHAPED QUESTIONS

⛔ The L1 questions (blocking date for NO-RECEIPT · composed-page CSS · dashboard rows), L3's
`groupsWith` schema, the row-height model and the dataviz 14-meta migration are on this lane's
do-not-rule list and are NOT re-put here; the dataviz migration is a CARRY (D2 makes it mechanical:
~2K tokens, `partial` as a list from the AUTO-BEHAVIOUR markers, `script` = the registry source).

1. **Does the schema's LEGACY-PROSE branch retire now that the gate blocks prose (D3)?** The L2
   report's recommendation 3 carried a parenthetical — *"(The schema's legacy branch retires on the
   same word.)"* — that was NOT in the five recommendations as put to Dave in chat, so it is not
   ruled by *"take the recommendations"*. Today: `meta.schema.json` still ACCEPTS a prose `behaviour`
   (schema arm 13 green) and the receipt gate REDS it — the `s140-D1` posture. Option (a) leave it:
   the gate is the enforcement, the schema stays permissive, 0 population either way; option (b)
   retire the `else` branch so a prose value is red at `jsonschema` time too (one edit, ~200 tokens,
   plus one schema arm flipped). **Recommend (a) for now**: the gate is the consumer that bites on a
   real page, and a schema red would fire only in gates that validate metas, none of which are in
   the receipt chain.
2. **The three UNPROVEN fallbacks — take the MEASURED sentence, or leave them null?** For calendar /
   command-palette / file-upload the render did not prove the #238 candidate but DID measure what the
   component does JS-off (finding 1, finding 2, and the palette's open specimen); each measured
   sentence sits in that meta's `$fallbackRender`. Option (a) promote the measured sentence into
   `fallback` (three edits, ~150 tokens; Calendar's would read "the live grid renders EMPTY — the
   days are script-rendered"); option (b) leave `null` until the snippets are repaired or a page
   authors them differently. **Recommend (a) for calendar and file-upload** (the render is the
   criterion s245-D5 named, and it settled them — just not in the candidate's favour) **and (b) for
   command-palette** (its specimen cannot show the closed state).

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN: `_render/gen_thumbs.py` for the 20 regenerated showroom pages and the missing
  `template-dashboard-bento` thumbnail.** Not run — thumbnails depend on the page's render, and the
  20 pages changed by one JSON block each (`showroom/*.html` ±1 line), which does not move a pixel;
  the bento thumbnail was missing before this lane (finding 4). Price: one `gen_thumbs.py` run at the
  conductor's seat, ~1K tokens + render time.
- **UNPROVEN: `_tests/test_gates.py` / a full `_build_all.py`.** Neither run — `_build_all.py` is
  forbidden to a lane and the tests copytree `knowledge/assets` (L1's ENOSPC precedent). What IS
  established: every serial step above rc 0 with `--check` green after it; both touched modules'
  `--selftest` green; `_validate_screen.py` default population PASS. Price: one CI run.
- **UNPROVEN: the 20 regenerated showroom pages render as before.** They differ from HEAD by one
  `<script type="application/json">` block each — inert by type — but no render was taken. Price: one
  render pass over 20 pages, ~2K tokens.
- **UNPROVEN: the mint's `$scriptNote` / `behaviour_address()` in `gen_provenance_receipt.py`** (L2
  finding 8) is now FALSE for 20 snippets ("meta `behaviour` is untyped prose") — L1's file, not
  opened here; a page minted today over one of the 20 would carry `script: null` in the receipt and
  the gate's meta side does the work (DISAGREES fires only on two non-null addresses). Price: ~500
  tokens in L1's file.
- **UNPROVEN: the `events` arrays are the snippet's listener names TODAY.** They were copied from
  the #238 measurement (`behaviour-migration.json`), not re-measured in this window; the snippets'
  inline scripts are byte-identical to #238 (the 116/20 hash table shows only the manifest block
  moved), so the measurement stands by construction, not by re-probe. Price: re-run
  `build_migration.py`'s listener census, ~400 tokens.
- **CLAIMED — none.** Every figure above is from a probe in this window: counts from the transcripts
  in the evidence dir; rulings count and ids from `json.load` of the store after `--write`; hashes
  from `sha256sum`; the pack-doc baseline from two runs in one bash call.

## PITFALLS (consequences replayed)

| # | what could go wrong | owner |
|---|---|---|
| 1 | A commit that carries the 20 typed metas WITHOUT the 20 snippets (or the reverse) is red at `gen_component_partials.py --check` — they move together, one commit | conductor at commit |
| 2 | Any new prose `behaviour` in a meta now reds every receipted page that splices that component — by ruling; the message names the cure | whoever types a meta |
| 3 | The `$fallbackRender` key is a `$`-note, not schema-typed; a future reader may treat it as the value — it is provenance for `fallback`, never a substitute | meta readers |
| 4 | `showroom/index.json` now carries the pre-existing bento entry with `ships_behaviour:true` and no thumbnail — a `gen_thumbs.py` run is owed before the library is looked at | conductor |
| 5 | The 3 `null` fallbacks say "UNPROVEN" in the meta; rule 2a tells an agent to put that in the Gaps list — a page carrying Calendar JS-off must say the grid is empty, not invent a fallback | page authors |
| 6 | `gen_library_214.py`'s class exclusion (`type="application/json"`) would also exclude a future JSON block that IS behaviour data — unlikely, but it is now a CLASS rule | index owner |
| 7 | The dataviz migration (14 metas, `partial` lists) is mechanical under D2 but NOT done; until it is, those 14 stay `meta:NONE`/UNPROVEN on every receipted page | next lane, ~2K tk |

## Evidence

`notes/_subreports/assets/2026-09-03-245-L2-populate/` (66 files, 2.3 MB — 28 PNG = 1.9 MB)

| file | proves |
|---|---|
| `rulings/s245-D1.json` … `s245-D6.json` · `inscribe-dry-run.txt` · `inscribe-write.txt` | the six entries as submitted; dry-run ×6 clean; write ×6, 338 → 344, reconstruction proof each |
| `behaviour.schema.fragment.json` · `apply_schema.py` · `meta.schema.before.json` · `meta.schema.applied.diff` | the fragment as applied (`partial` widened); +89/−1 against the live schema |
| `migrate_metas.py` → `migrate-dry-run.txt` · `migrate-write.txt` · `metas-migration.diff` | span-replacement writer with four proofs; 20/20; the full meta diff |
| `validate-metas.txt` · `validate-metas-after-fallback.txt` · `schema-arms.txt` | 136/136 valid both times; states 116 NONE / 20 TYPED; 13/13 schema arms |
| `jsoff_render.py` → `jsoff-render.txt` · `jsoff-render.json` · `jsoff-<slug>-{before,after}.png` (28) | the JS-off renders: 67 assertions + 18 observations, 11 PROVEN / 3 UNPROVEN, 0 page errors |
| `update_fallback.py` → `update-fallback-write.txt` | the 11 measured fallback sentences written, 3 left null, 14 `$fallbackRender` |
| `snippets-sha256-before.txt` · `-after.txt` · `snippets-hash-table.txt` | 136 hashed twice; 20 changed / 116 identical; 20/20 manifests agree with the meta and sit beside `#token-manifest` |
| `gen-check-before.txt` · `regen-serial.txt` | the 20 out of sync, then the ordered serial step by step with rc per step, the index defect and its re-run |
| `live-bites-generator.txt` | B1–B5 on the real tree, 5/5 bite, restore hashes equal, control green |
| `_validate_receipt.before.py` · `gen_component_partials.before.py` · `code-changes.diff` | the pre-edit modules and the exact code delta (+ `gen_library_214.py`) |
| `selftest-validate-receipt.txt` (39 arms) · `selftest-gen-component-partials.txt` · `mutants.txt` · `mutants-generator.txt` | both selftests green; 4 gate + 2 generator mutants bite, controls green |
| `gate-drive-real-page.txt` · `gate-default-population.txt` | the real dashboard: PASS after typing; prose bite reds standalone and through the chain; restored; 7-screen default population PASS |
| `skill-rule-2a.diff` · `pack-doc-gate.txt` | +9/−0 beside rule 2; pack-doc gate 215 → 215, 0 findings name the file |
| `files-changed-stat.txt` | `git diff --stat` over this lane's paths (70 files, +855 / −120 incl. the serial's regenerated files) |

**I READ the L2 REPLAY-THESE line** (`notes/_subreports/2026-09-02-238-B-L2-behaviour-address.md`):
review page §§ 02 and 06 (through the report's Findings and RULING-SHAPED sections, which the page is
generated from; the page itself was probed for § 07's rule-2a text, not read back whole) · finding 6
+ drive-D (the FOREIGN hole; re-driven here as live bite B1) · `behaviour-migration.json` items
`date-picker` and `badge` (the provenance blocks; the candidate/quote/what_would_prove shape is what
the JS-off probes were designed from) · findings 7/8 (carried above as UNPROVEN, L1's file) ·
`_validate_receipt.py` docstring 82–113 (4b — rewritten here for D2/D3 in the same register).

REPLAY-THESE: `assets/…/jsoff-render.txt` lines for Calendar, Command-palette, File-upload (~500 tk) · `jsoff-calendar-before.png` (one look, the empty live grid) · finding 3 above + `regen-serial.txt` lines 40–60, the index defect and its fix (~400 tk) · `gate-drive-real-page.txt` (~600 tk) · RULING-SHAPED 1 and 2 above (~500 tk) · `knowledge/components/calendar.meta.json` `behaviour` block, to see one UNPROVEN meta whole (~300 tk)
