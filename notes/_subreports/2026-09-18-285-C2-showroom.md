# #285 — C2 (showroom re-sync commit lane) — 2026-09-18

Cut on Dave's **"1. go"**. This lands the commit #285 lane C measured, verified and then reverted
as a DECLARED gap (`notes/_subreports/2026-09-18-285-C-commit.md`, run 1).

**Final sha: `ff354475`**
Subject as committed: `after #285 2026-09-18 — showroom re-synced to canon — 108 pages, :is( → :where( — and the regenerated masters are accepted by eye`

**2 runs** (cap was 4). Locks clear at the end; no `.git/*.lock` remains. Nothing pushed.

## Step 0 — the delete grant

`mcp__cowork__allow_cowork_file_delete` called first per runbook ★ STEP 0 — granted ("File deletion
is now enabled for the \"UX-design\" folder"). **No stranded `.git/index.lock` was present at lane
start**, and nothing inside `.git` was ever `rm`'d.

## The generator — only the one the gate names

The gate at `knowledge/_git_commit.sh:315-317` (s191-D1) names its own remedy:

```
python3 knowledge/gen_showroom.py --check ||
  fail "showroom sync gate REFUSED (s191-D1) … Run: python3 knowledge/gen_showroom.py …"
```

So exactly one command was run:

```
python3 knowledge/gen_showroom.py
→ gen_showroom: 137 page(s) -> showroom/ (108 written, 0 orphan(s) pruned;
  index owned by knowledge/_render/gen_library_214.py)
```

None of `gen_kg_icons.py`, `gen_kg_rules.py`, `land_rests_on.py`, `_build_all.py` was run.

## The diff — verified on all 108, not a sample

```
108 files changed, 108 insertions(+), 108 deletions(-)
```

Every changed page changes exactly one line: the base64 `<script id="payload">` blob. Sample
(`showroom/accordion.html`) — the only changed line in the file:

```
-<script id="payload" type="application/octet-stream">PCFET0NUWVBFIGh0bWw+…
+<script id="payload" type="application/octet-stream">PCFET0NUWVBFIGh0bWw+…
```

Because the visible diff is opaque base64, the check was done **by decoding**, for all 108 pages,
old (`git show HEAD:<path>`) against new:

- **non-payload lines: byte-identical in all 108.** No generator timestamp, no index line, no
  orphan pruning — there is nothing else in the diff to account for.
- **payload diff: exactly one changed line in all 108**, and in all 108 the new line is the old
  line with a leading `:is(` replaced by `:where(`, every remaining byte identical:

```
-:is(button,a,label,span,small,strong,em,b,i,th,td,dt,dd,li,figcaption,legend,caption,summary,
  output,time,input[type=text],…,textarea):not(:has(svg)){text-box-trim:trim-both;…
+:where(button,a,label,span,small,strong,em,b,i,th,td,dt,dd,li,figcaption,legend,caption,summary,
  output,time,input[type=text],…,textarea):not(:has(svg)){text-box-trim:trim-both;…
```

⚠ **One thing worth naming, and it is a reassurance rather than a surprise.** A first, naive check
(apply `:is(`→`:where(` to the whole old payload and compare) flagged 16 pages as mismatching:
`app-shell-multi-column`, `app-shell-side-nav`, `cascader`, `combobox`, `data-grid`,
`feature-grid-lockup`, `filter-toolbar-bar`, `form-layout`, `multi-select`, `tags-input`,
`template-create-edit`, `template-detail`, `template-empty`, `template-list-index`,
`template-settings`, `tree`. The cause is the **check**, not the diff: those 16 carry additional
`:is(` occurrences in their own component CSS, which the generator correctly **left untouched**.
The substitution is confined to the one canon selector line. Line-level diffing confirms all 16.

⚠ **`git show --stat` prints `rename`/`copy` lines** (e.g. `rename showroom/{badge.html =>
view-options.html} (74%)`). That is git's similarity detection firing on near-identical base64
blobs, not files moving — `--no-renames` shows 108 plain modifications and no path appeared or
disappeared.

## The 138-vs-108 measurement (measured only — nothing deleted)

```
ls showroom/*.html | wc -l   → 138
```

`showroom/` **still holds 138 top-level `.html`**, and the inscribed "108 stale" was never the
population. The decomposition:

| count | what it is |
|---|---|
| 137 | pages `gen_showroom.py` owns ("137 page(s) -> showroom/") |
| 1 | `showroom/index.html` — owned by `knowledge/_render/gen_library_214.py`, not by this generator |
| **138** | total top-level `.html` |
| 108 | of the 137, the subset actually rewritten this run — i.e. the stale set |
| 29 | of the 137, already in sync (no rewrite needed) |

So 138 and 108 are not in tension: 108 ⊂ 137 ⊂ 138. `0 orphan(s) pruned`; nothing was deleted,
moved or pruned by this lane.

## Dave's acceptance — `W-285lm` amended

`notes/_lanes/285/DAVE-RULINGS-2026-09-18.md` carries his word verbatim: **"2. accept"** — the
regenerated masterbrand masters are accepted by eye, replacing the #284 acceptance he reopened
himself with the B crop.

`_state.py` has **no dedicated amend function** (`add` / `load` / `check` / `save` / `live` /
`counts` are the surface), but amending through the store's own writer — `load()` → mutate →
`check()` → `save()` — needs no invented close condition, so the note was recorded that way, into
the row's `body` (the free-text field 609 of the store's rows already use):

> ⬛ #285: Dave's word on the regenerated masters was "accept" — THE EYE-HALF OF `closes_when` IS
> CLOSED (the 4x before/after contact sheet was looked at and the masters accepted by eye). ⚠ The
> row stays OPEN: registration in `_logo_nodes.json` (behind the `gen_kg_icons.py` fence) is STILL
> OWED and still Dave's word on how. `closes_when` is UNCHANGED — no close condition was invented
> or rewritten here.

`closes_when` untouched, `state` still `open`, `condition` still `stated`. `check()` green before
the save.

## Run log

### Run 1 — REFUSED (T3 headline generation)

```
T3 REFUSES (s130-D3): non-wrap commit with no SESSION_N — there is no current-session source to
generate a subject from, and inheriting the on-disk ★ LATEST banner is the #128 wrong-subject defect.
Re-run as SESSION_N=<n> bash knowledge/_git_commit.sh --reconciled <msgfile>.
✗ T3 headline generation REFUSED … Nothing has been staged.
```

Repair = exactly what the refusal names: `SESSION_N=285`. No stage widening, no msgfile edit.

The showroom gate itself passed on this run (`— showroom in sync (gen_showroom --check passed,
s191-D1)`), i.e. the regeneration had already cleared the gap the last lane declared.

### Run 2 — COMMITTED

```
SESSION_N=285 bash knowledge/_git_commit.sh --reconciled \
  outputs/msg-285-C2-showroom-resync-2026-09-18.txt showroom/ knowledge/_state.json

— committed: ff354475 after #285 2026-09-18 — showroom re-synced to canon — 108 pages, :is( → :where( — …
— subject asserted identical to the headline T3 generated (in memory, not re-read from the msgfile — #171)
— subject carries exactly ONE T3 prefix (#208 doubled-subject assert)
✓ done — locks clear, safe for Dave to push via GitHub Desktop
```

The msgfile headline was started at the actual subject — **T3 generates the `after #285 2026-09-18 — `
prefix itself** (s130-D3, runbook step 3, #185 doubling).

Other gates green on the way through: chain fresh · doc rows present · polarity · mention map ·
session witness (#285) · spine-writer selftest. Wrap gate RED but **visible, not blocking** —
this commit is DECLARED not-a-wrap (#74-D1); the session's final commit must run `--wrap`.

## What landed, and what did not

**Committed (110 paths):** the 108 `showroom/*.html` · `knowledge/_state.json` (the `W-285lm`
amendment) · `notes/_REHEARSAL-LOG.jsonl` — the third is the script's own declared
instrumentation auto-stage (`INSTRUMENT_AUTOSTAGE`, #261 M2: the script commits the dirt the
script made), not a widening by this lane.

**Left dirty, deliberately:**
- `notes/_lanes/285/DAVE-RULINGS-2026-09-18.md` — modified; it is the conductor lane's record and
  its to commit. This lane quotes "accept" from it, it does not stage it.
- `notes/_subreports/2026-09-18-285-C-commit.md` — the previous lane's report, still untracked and
  still needing its own `_state.add()` row (suggested `W-285c`) before it can clear the doc-row gate.
- This report — written after the commit, so it is uncommitted and will need a row of its own
  (suggested `W-285c2`).

`pip install tiktoken --break-system-packages` ran first: already satisfied.
