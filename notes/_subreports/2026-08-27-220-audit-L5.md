# `#220`-`L5` — THE CONSOLIDATED SITTING PAGE: 122 calls indexed, 97 open, 25 candidate-closed

session: `#220` · 2026-08-27
window: `#220` six-lane audit wave — lane L5
sub index: `L5`
brief: `notes/_briefs/2026-08-27-220-audit-wave-divvy.md` §L5 + SHARED RULES
tokens: `UNMEASURED` — a delegated lane cannot read its own `message.usage`; the conductor's
window carries this lane's spend.

## VERDICT

**DONE.** `reviews/SITTING-220-2026-08-27-v1.html` exists, renders clean in light and dark in all
four themes, and indexes **122 calls — 97 open and 25 CANDIDATE-CLOSED** across six surface
groups. Every RULING-SHAPED QUESTIONS section in `notes/_subreports/2026-08-2[567]-*.md` was read
(33 sections across 31 reports, extracted mechanically by heading, not by eye), plus the standing
33 cards of `reviews/SITTING-219-2026-08-25-v2.html` (W-184). **Nothing was closed.** Every
candidate-close is drawn as a struck card carrying the clause or the probe that answers it, and
each was **probed in the tree today** rather than inherited from the report that proposed it —
which is how three of them turned out to be answered by an *enactment* rather than by a ruling,
and one by a **chat sentence that lives only in a code comment**. All 189 links resolve (file
exists, and where a fragment is named its `id` is present in the target's bytes). 0 page errors,
0 console errors, 0 failed requests, no horizontal overflow at 480px. **One render defect was
found by eye and fixed inside this lane's own region: `✅` and `⛔` are TOFU in the real HSBC cut**
— and the #219 index Dave has already been sent carries both.

COUNTS: findings 9 · ruling-shaped 3 · UNPROVEN 4

---

## What was done

**Region, exactly as briefed:** `reviews/SITTING-220-2026-08-27-v1.html` (NEW, 160,425 B) · this
report · `notes/_subreports/assets/2026-08-27-220-audit-L5/` (3 PNGs). **Nothing else in the tree
was written.** No ruling, no store row, no memory, no git, no promotion, no repair outside the
region. `knowledge/_rulings.json` was read only (262 rulings).

**① The read, mechanically.** A heading-scanner over
`notes/_subreports/2026-08-2[567]-*.md` extracted every section whose heading matches
`RULING-SHAPED|BLOCKED ON A RULING` up to the next heading of equal-or-higher level — **33
sections, 77,946 B, across 31 of the 37 reports**. The six with no such section
(`seam2`, `seam3`, `seam5`, `seam7`, `sitting-index`, and `seam3`'s conductor block) were read at
their "FOR THE CONDUCTOR" / "still owes upward" / "RETURNED, not fixed" equivalents instead, which
is where seam 3's `s149-D1` return and seam 7's REPLAY-THESE items live. Naming the probe per
[[unmatched-grep-is-not-an-absence]]: six reports had **no** ruling-shaped section, and that is a
measured absence, not an unrun search.

**② The dedupe.** Restatements were merged into one card citing every raiser. The largest
collapses: the **delta gate RUN arm** (N3 Q1 · seam9 Q2 · replay Q2 → `GATES 1`); the
**semantic caption-ground role** (ladder Q4 · rung Q2 · theme-lift Q4 → `BENTO 4`); **mono's
gallery caption ground in dark** (default-switch Q1 · theme-lift Q1 · enact-B Q5 · enact-C Q3 →
`BENTO 1`); the **pack runner's verdicts + `--baseline`** (N2 handoff 3 · R2 Q4 · replay Q4 →
`PACK 7`); the **`s149-D1` status field** (lane5 · seam3 §6 · replay Q3 → `PROCESS 1`).

**③ The candidate-close pass.** 25 cards. Twelve are answered by `s220-D1`/`s220-D2` read from the
store; the rest by a landed enactment. **Each was probed:** `RATIFY_ID`, `VERSION`, the parked-
correction discharge, the `X2` status field, `squaring`, the ratchet baseline, the manifest's
group counts and ship paths, the delta gate's arm list, `notes/_claims`' three call sites, and the
`_MACHINERY-MANIFEST.md` debt list were all read in the tree today.

**④ Continuity.** The #219 index's `EYE`/`WORD`/`REC` keys are **not reused and not renumbered** —
that page says in its own banner that renumbering would silently repoint every citation. Each of
its 33 calls carries forward onto exactly one new card, with its old key printed in small print as
"previously indexed as". Verified one-to-one: all 33 accounted (`EYE 2`, already struck there, is
carried as `WAS K` so the arithmetic is visible rather than quietly dropped).

**⑤ Chrome copied, nothing re-drawn** ([[specimen-starts-from-reference]]). The CSS block, the
sticky header, the `.seg` switch, the `.sx-*` card grammar and the hash router are copied verbatim
from `SITTING-219-2026-08-25-v2.html`. **One addition, declared in the file's own header comment:**
`.sx-was`, the "previously indexed as" small print. **No specimen is drawn on the page** — it is an
index; every colour, wall and chord it names is rendered on the page it links to, which is why
"copied-from-artefact" has nothing to bite on here.

**⑥ Verification, driven.** Playwright + the real HSBC cut, using the session's already-staged farm
(`/var/tmp/chromelibs-220` — `ls`'d before trusting: 2 entries, not hollow; `/var/tmp/fonts-220`;
`/var/tmp/pw-browsers-220`). Font control probe, asserted with controls rather than a boolean:

| probe | measured | reading |
|---|---|---|
| `HSBC_MtUnivers_Latin` | **333** | the real cut |
| `"Univers Next HSBC"` | **333** | alias resolves |
| `"Univers Next for HSBC"` | **333** | alias resolves |
| `DejaVu Sans` — control | 355 | genuinely different face |
| nonexistent face — control | 298 | default fallback |

Both aliases land on the target and neither lands on the nonexistent-face number.

**Ground parity, measured at 850 ms settle** (a computed-style probe taken straight after a theme
flip measures the transition, not the state — the #219 index's finding 1, obeyed rather than
re-discovered): mono `255,255,255` / `26,26,26` · legacy `255,255,255` / `26,26,26` · console
`255,255,255` / `26,26,26` · supercharge `247,246,244` / `26,26,26`. All eight legs land on their
token.

**Page health:** `pageerrors: []` · `console (error|warning): []` · `requestfailed: []`.
**Hash router driven:** `#theme=supercharge&m=dark` → `data-apollo-theme="supercharge"`,
`data-theme="dark"`, ground `26,26,26`. **480px:** `scrollWidth 480 == clientWidth 480` — no
page-level horizontal overflow. **Structure read back from the live document:** 122 `article.sx-card`,
25 `.sx-closed` — matching the header's printed 97 + 25.

---

## Findings

**1 · `✅` AND `⛔` ARE TOFU IN THE REAL HSBC CUT — and the page Dave has already been sent carries
both.** Found by *looking* at the console-light PNG: the banner's `&#9989;` rendered as an empty
box. Confirmed by canvas measurement against a private-use control character (a char with
certainly no glyph), body face `14px "Univers Next HSBC"`:

| glyph | width | verdict |
|---|---|---|
| U+E123 (control, no glyph) | **7.17** | tofu baseline |
| `✅` U+2705 | **7.17** | **TOFU** |
| `⛔` U+26D4 | **7.17** | **TOFU** |
| `👀` U+1F440 | **7.17** | **TOFU** |
| `⬛` U+2B1B | **7.17** | **TOFU** |
| `⚠` U+26A0 | 13 | renders |
| `★` U+2605 | 13 | renders |
| `✓` U+2713 | 12 | renders |
| `—` `Δ` `×` `→` | 14.18 / 9.56 / 9.42 / 14 | render |

**Fixed inside this lane's region only:** 2 × `&#9989;` → `&#10003;`, and 13 visible `⛔` glyphs
dropped (the words they preface are already bold or inside a tag, so nothing is lost). ⛔ **NOT
fixed, and it is a live defect on Dave's own surface:** `reviews/SITTING-219-2026-08-25-v2.html`
uses `&#9989;` in its top banner and `&#128064;` (👀) in card `EYE 4` — **both are boxes in the
face the page is designed for.** Same class in any page that copied that chrome.
★ **The general shape:** the repo's authoring grammar (⛔ / ⚠ / ★ / ✅ / ⬛) is a *comment* grammar,
and half of it has no glyph when it crosses into rendered body text. **The verified-safe set is
`⚠ ★ ✓`.** Price to sweep the class: one canvas probe over every `reviews/*.html` + `showroom/**`
body text, ~4–6K tk; price to fix the #219 page alone: ~200 tk, and it is a Dave-facing page so
the edit is the conductor's.

**2 · A CHAT SENTENCE IS THE ONLY RECEIPT FOR A LICENCE POSITION IN A SHIPPED ARTEFACT.** The
photography derivatives were fenced out of the pack for LICENCE — quoted at
`knowledge/_release/_gen_pack_manifest.py:309-310`, *"Getty/EyeEm stock originals"* /
*"LICENCE, NARROWED AT #220"*. Thirty now ship: `_pack_manifest.json` group
`library.photography` = **30 paths**, and the constant's own comment reads
`# THE PHOTOGRAPHY SPECIMEN (#220, Dave's ask: "30 or so… so that the bentos and gallery work")`.
**Probed: no ruling in `knowledge/_rulings.json` mentions Getty, EyeEm, Stocksy or stock imagery
redistribution.** `s219-D5(Q2)` settled the *font* licence and says nothing about images. So a
redistribution position now governs a shipped zip and its only home is a Python comment
([[premise-ages-faster-than-rule]] in its sharpest form — the next release will read the comment).
Card `PACK 2`. Price to inscribe: one ruling, ~600 tk, Dave's word only.

**3 · THE RELEASE RATIFICATION IS NOT KEYED TO A VERSION, AND v1.0.1 SHIPPED ON v1.0.0's WORD.**
Probed: `knowledge/_release/_gen_pack_manifest.py:567` `RATIFY_ID = "s219-D10"`, and `:107`
`VERSION = "v1.0.1"`. `_pack_manifest.json` reads
`"status": "RATIFIED — s219-D10, Dave's word 'bake' (2026-08-26); s219-D4(2) satisfied by the
store, not by prose"` **for v1.0.1**. The generator will read RATIFIED for every future version
without asking again. `s219-D4(2)`'s promise — a release is his word, not the script's — is
mechanically not kept. The bake seam raised it as its Q2 and PROPOSED the fix; nothing moved.
Card `PACK 1`. Price: one constant becomes a per-version lookup, ~1.5K tk + a selftest bite; the
*decision* is Dave's.

**4 · THE DELTA GATE STILL HAS NO ARM THAT EXECUTES ANYTHING — three lanes, one instrument, zero
change.** Probed `knowledge/_validate_package_delta.py` by function list: `check_verbatim_set`,
`check_cross_copy_identity`, `check_unknown_files`, `check_shim_provenance` — **four arms, all
byte/path/AST comparison, none of them imports or runs the copied code.** This is exactly the
divvy's own lesson ("a gate can be green over an artefact that cannot RUN") and it is the
*second* live instance: the gate was green while the package was unrunnable, then green again over
a shipped `ImportError`. N3, seam 9 and the replay lane all recommended the same fifth arm
independently. Card `GATES 1`. Price to build: ~40 lines + a selftest, ~3K tk; **it widens a
gate's glob, so it is Dave's.**

**5 · THE TWO-HOMES CLASS HAS FOUR LIVE INSTANCES, NOT ONE.** The divvy names `SPACING_STOPS`.
Probed today, all four still true: **(a)** the spacing rail in two generators (enact-A Q6 / replay
Q5); **(b)** `VERSION` in `_gen_pack_manifest.py:107` **and**
`apollo-spider/build-designer-pack.sh:38` — both `v1.0.1`, so nothing is broken and nothing
enforces it; **(c)** `notes/_claims` passed literally at `knowledge/_build_all.py:564` **and**
`.github/workflows/gates.yml:269`, while the gate now defaults to that same path — three homes;
**(d)** a component meta restating live facts whose home is the ruling store plus the snippet
(sparkline Q4). Cards `GATES 2`–`GATES 4` and `GATES 13`. ★ **Four instances of one ADR-0017
breach is an argument for ruling the CLASS, not the cases** — which is what card `GATES 4` says on
its own face.

**6 · A PROPOSED EXCLUSION IS ENFORCED IN THE PRODUCT WHILE ITS RECORD SAYS "PROPOSED".** Probed:
`knowledge/_render/gen_bento_matrix_217.py:962` — `{"id": "X2", "was": "P3", "status": "proposed",
…}` — while `capsule_legal` inside `validate_settings` refuses the edgeless capsule in the
browser. `s220-D2 (1)` **declined** to promote X1/X2, which correctly answers the *promotion*
question and leaves the machinery stricter than the manifest. Its sibling `X1` is worse: measured
by the readings lane as unable to fire on **any** console gallery chord, so a proposed rule reads
as a live fence while being structurally inert. Cards `GATES 11` and `GATES 12`. Price to
reconcile X2: one question, ~0 tk. Price to repair X1: it is Reading B's machinery, which the
ruling rejected — so the honest options are repair-or-retire.

**7 · THE FIVE `#220` REVIEW PAGES CARRY NO ANCHORS AT ALL.** Probed: `grep -c 'id='` returns
**0** for `CAPTION-DARK-LADDER-2026-08-27-v1.html`, `CORRECTION-READINGS-…`, `DEFAULT-SWITCH-…`,
`RUNG-ENACT-…`, `THEME-LIFT-…` and `RELEASE-SPIDER-2026-08-26-v1.html`. The #219 pages carry 3–10
each (`#squaring`, `#candidate-a`, `#rowA`, …). **Consequence, paid on this page:** an index can
only link to those pages *whole*, so a reader following "where it lives" lands at the top of a long
page and hunts. It also means those pages have **no theme/mode switch** (the switch needs
`id="themes"`/`id="modes"`), so a reader cannot flip a dark-mode caption question into dark mode on
the page that asks it. Declared on the page's own footer rather than hidden. Price: ~150 tk per
page to add section ids at the generator; the switch is a larger job and belongs to whichever
generator owns each page.

**8 · THE #219 INDEX'S OWN COUNT IS HONEST AND ITS SCOPE IS NOT WHAT A COLD READER WOULD ASSUME.**
It says 33 and its banner says why: it indexed one day's seven reports, and it named fourteen
further enact-lane questions as *"filed and un-indexed"* rather than rounding them in. Reading the
full 25/26/27 corpus turns 33 into **122**. ⚠ **This is a scope fact, not an error in that page** —
and it is exactly the [[premise-ages-faster-than-rule]] trap: "33 open calls" was quoted forward by
the replay lane as though it were the whole standing set. **The new page states the derivation on
its own face** so the next reader cannot make the same jump.

**9 · THE TYPE-RATCHET BASELINE ON DISK IS 1,091 — AND IT IS QUOTED AS TWO OTHER NUMBERS
ELSEWHERE.** Probed `knowledge/_type_ratchet.json`: `"baseline": 1091`, measured at wiring, ruled
shrink-only. The #218 quality lane recorded a movement `1097 → 1093` at its own reconcile; the
standing memory hook says *"debt 1,097 may only shrink"*; the pack ships 1,091 as its own baseline
against a 138-file population where the repo gates 150, which is **427 of slack inside the pack**
(N2 Q1, card `PACK 6`). **All three figures are individually explainable and no two agree.** Not
repaired — a ratchet write is a ruled surface. Price to reconcile the record: ~400 tk to read the
three sources and write one line; the memory-hook correction is the conductor's.

---

## RULING-SHAPED QUESTIONS

⛔ **These are this LANE's own three. The 97 on the page are Dave's and are not restated here.**

1. **Does the sitting page get a store row, and does it supersede `W-184`?** The brief bans
   `knowledge/_state.json` writes, so **none was minted** — which puts this page in the
   [[forgotten-document-class]] the moment the wave ends: a review surface with no store row is
   invisible to every filtered view. `W-184` (the #219 index) is the row this page supersedes, and
   `W-181` before it is already a [[home-pointer-rot-class]] casualty pointing at v1.
   **(a)** Conductor mints one row for this page whose `links` address `W-184`, at the reconcile —
   ~300 tk, and it is the pattern seams 5 and 6 already used. **(b)** Reword `W-184`. ⛔ (b)
   touches a Dave-owned row. **Recommend (a).**

2. **Should the tofu-glyph rule be gated, or left as a lesson?** Finding 1 is a *class* — the
   authoring grammar crosses into rendered text and half of it has no glyph. A gate is cheap and
   mechanical: parse each `reviews/*.html` and `showroom/**/*.html` body, flag any codepoint
   outside a verified-safe set appearing in rendered text. **(a)** Build it ADVISORY, ~2.5K tk —
   the [[gate-dont-patch]] shape, and it fixes the class rather than the two instances.
   **(b)** Fix the #219 page and move on, ~200 tk, and the next page re-invents the defect.
   ⛔ Not built: new gates are a promotion decision. **Recommend (a) as advisory**, because the
   defect is invisible to every existing instrument — no gate parses rendered glyph coverage.

3. **Is "candidate-closed" the right register, and does confirming 25 of them cost one word or
   twenty-five?** The page draws each struck card with its own receipt, so Dave *can* confirm them
   one at a time — but 25 confirmations is a sitting of its own. **(a)** He confirms the twelve
   with a `s220-*` clause as a block (they quote his own words back) and works the other thirteen
   individually, since those were closed by *enactment* rather than by his word. **(b)** All 25
   individually. **(c)** The conductor proposes a single "confirm the struck set" ruling and Dave
   strikes any he disputes. **Recommend (a)** — it splits on the honest seam, which is *whose word
   closed it*.

---

## UNPROVEN / CLAIMED (ADR-0016)

1. **UNPROVEN — the page was SEEN at one theme (console), two modes, one width (1180).** Mono,
   legacy and supercharge are asserted **numerically** (ground parity per theme × mode, zero
   errors, ink contrast) and were **not looked at**; the 480px reading is a scrollWidth assertion,
   not a screenshot. Bounded verification (`s172-D3`), residual named rather than implied. Price to
   close: 6 more screenshots + an eye, ~3K tk.
2. **UNPROVEN — no link was followed.** "Resolves" means the file exists on disk and, where a
   fragment is named, its `id` is present in the target's bytes (189 hrefs: 6 same-page, 175
   file-only, 8 with a fragment, 0 bad). Whether each target section still *frames* the question
   the card says it frames is inherited from the reports, not re-verified by opening 20 pages.
   Price: ~8K tk.
3. **UNPROVEN — 122 is what these named sources carry.** It is not a claim that 122 is everything
   open to Dave anywhere. `knowledge/_REVIEW-SIGNOFF.md` was read at rows `W-136`/`W-137` only, as
   the #219 index did — **not end to end** (the file is ~110 KB). A call living only in a document
   none of the 31 reports cites would not appear. Price to widen: read the sign-off tracker whole,
   ~12K tk.
4. **CLAIMED — the "four reds at the release commit" figure in card `WAS Q`** is taken from the
   #219 seam-7 probe's own printed verdict, not re-driven here. Gate state is lane L1's region and
   driving the fleet from this lane would duplicate it. The card says so on its face.

⚠ **NOT MINE, declared for the reconcile:** `git status` at hand-off shows this lane's three paths
only — `reviews/SITTING-220-2026-08-27-v1.html`, this report, and
`notes/_subreports/assets/2026-08-27-220-audit-L5/`. Five other lanes are writing concurrently;
attribute nothing else here.

## Evidence

`notes/_subreports/assets/2026-08-27-220-audit-L5/` —
`sitting220-console-light.png` and `sitting220-console-dark.png` (the header block and the first
bento card, both modes, in the real HSBC cut — these are the renders that were **looked at**, and
the light one is where the `✅` tofu was seen); `sitting220-gates-dark.png` (the machinery group in
dark, shot **after** the glyph fix — it is the proof that the ⛔ removal reads correctly and that
the card grammar holds at depth).

REPLAY-THESE: `reviews/SITTING-220-2026-08-27-v1.html` — open it, do not read it (~0 tk; it is
Dave's surface, not the conductor's) · finding 1, the tofu class, and finding 2, the uninscribed
licence position (~600 tk) — **both belong in the conductor's #220 receipt whether or not Dave
works the sitting today** · ruling-shaped question 1, the store row (~300 tk) — this report and the
page are both currently in the forgotten-document class.
