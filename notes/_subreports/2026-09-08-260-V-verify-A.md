# #260 V — cold-seat verification of lane A (driven receipts), commit 223310b

Read-only verification. Every claim was tested by RUNNING the thing, not by reading lane A's report.
Two mutations of my own were performed and reverted.

## Verdict per claim

| # | Claim | Verdict |
|---|-------|---------|
| 1 | `_drive_chart_engine.py` exists, `--check` = 6 FRESH | **CONFIRMED** |
| 2 | `_validate_dataviz.py` grades engine-drawn donut/pie via receipt, BLOCKING, static route untouched | **CONFIRMED with a correction** (pie is *not* engine-drawn in the shipped snippet) |
| 3 | 6 new selftest bites | **CONFIRMED** |
| 4 | dv-004 = 2.109px donut / 2.108px pie in 8/8 combos | **CONFIRMED** |
| 5 | 4 gates green | **CONFIRMED** |

## Evidence lines

**1.** `python3 knowledge/_drive_chart_engine.py --check` → exit 0,
`✅ all 6 receipt(s) FRESH (driven 2026-09-08T18:01:19Z, Chromium 151.0.7922.34)` —
`[FRESH]` for bar / combo / donut / line / sparkline / stacked-area.

**2.** `python3 knowledge/_validate_dataviz.py` → exit 0,
`✅ DataViz gate passed (15 chart surface file(s))`. The driven verdict is written into the
committed report `knowledge/_DATAVIZ-GATE.md`, not to stdout (no `--verbose` flag exists):

    ## ✓ snippets/Chart-donut.reference.html — PASS
    - ⚠ donut#cd1 — dv-004: PASSED by driven receipt — donut.html/fig-donut measured 2.109px
      (worst of 8 measurements across 8 theme x mode combos, rule >=2.0px) …

**3.** `--selftest` → exit 0, `✅ selftest`, with six new starred cases:
fresh-8-combos PASSES · one combo 1.9px BLOCKING · STALE hash BLOCKING · NO receipt BLOCKING ·
UNMAPPED engine snippet BLOCKING · control: static donut with ≥2px stroke still passes statically.

**4.** Read straight out of `knowledge/_tests/chart-engine/_receipts.json`:
all 8 combos (console/legacy/mono/supercharge × light/dark) carry
`fig-donut 2.109` and `fig-pie 2.108`. 8 sources hashed, incl. `dv-render-donut.js`, `canon.css`.

**5.** `_validate_behaviour.py` → `Behaviour-contract gate OK`.
`gen_component_partials.py --check` → `all AUTO-PARTIAL blocks in sync`.
`canon/gen_canon_components.py --check` → `136 components in sync`. All exit 0.

## Mutation 1 — one combo below the rule (assigned)

Hand-edited `mono/dark` → `fig-donut.dv004_px = 1.95`. Gate went **red**, exit 1, quoting the
combo and the measured figure:

    [FAIL] snippets/Chart-donut.reference.html  (2 charts, 1 blocking, 0 advisory)
       ✗ dv-004: driven receipt FAILS — donut.html/fig-donut measured 1.950px of separation
         in combo mono/dark (rule is >=2.0px). Source: knowledge/_tests/chart-engine/_receipts.json

Reverted with `git checkout knowledge/_tests/chart-engine/_receipts.json`.

## Mutation 2 — no mapped receipt (probe)

Renamed the receipts key `pages["donut.html"]` → `pages["donut-RENAMED.html"]`. Gate went **red**,
exit 1 — it refuses rather than skipping:

    ✗ dv-004: donut is ENGINE-DRAWN and maps to donut.html, but
      knowledge/_tests/chart-engine/_receipts.json holds no driven receipt for that page.
      Record one: python3 knowledge/_drive_chart_engine.py

Reverted. Gate re-run clean; `_DATAVIZ-GATE.md` back to its committed bytes.

## What lane A's report omitted

1. **`Chart-pie.reference.html` never takes the driven route.** It has zero `dvRender(` calls and
   16 static `dv-series` marks, so `is_engine_drawn()` returns False and it is graded by the
   *static* stroke rule. Both mutations above turned donut red and left pie **green** — proof.
   The `ENGINE_TEST_PAGE` mapping `"Chart-pie.reference.html" → "donut.html"` is therefore dead
   code today; the pie 2.108px figures are measured and committed but nothing consumes them.
   The claim "grades engine-drawn donut **and pie**" is true of the receipt, not of the shipped
   snippets.
2. **Only 1 of the 15 chart surfaces reaches the driven path at all** — `donut#cd1` inside
   `Chart-donut.reference.html` (2 charts there; the other is static). The blast radius of s260-D3
   in production is a single figure.
3. **The driven verdict is invisible on stdout.** It rides as an *advisory* line into
   `_DATAVIZ-GATE.md`. Anyone verifying by watching the terminal will not see the words
   "driven receipt" on a green run — only the advisory count `1 advisory` on the donut line.
4. **`_drive_chart_engine.py --check` is not wired into any gate.** Freshness is enforced
   independently, by source-hash comparison inside `_validate_dataviz.py`, so the driver's own
   `--check` is a human convenience, not a gate step. Nothing else in the repo invokes it.
5. `git status` finished clean apart from the pre-existing `notes/_REHEARSAL-LOG.jsonl` and
   `notes/_dream/_GRADE-DECISIONS.jsonl` modifications.

Not run, per instruction: `_build_all.py`, `gen_showroom.py`, `_validate_screen.py`, `test_gates.py`.
No `git stash` at any point.
