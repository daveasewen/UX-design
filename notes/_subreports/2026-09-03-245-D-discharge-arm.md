# `#245`-`D` — `s244-D1` BUILT: ARM 1 of `boot_constant_drift_check` has a declared-discharge form; the ceiling literal did not move

session: `#245` · 2026-09-03
window: `lane D — discharge arm (Apollo #245)`
sub index: `D`
brief: chat brief, #245 conductor (no `notes/_briefs/` file was named)
tokens: `UNMEASURED — a delegated sub cannot read its own `message.usage`; the conductor's lane figure is the one to quote`

## VERDICT

DONE, all four regions, with one premise corrected. Dave's `s244-D1` ruling (quoted verbatim in Finding 1) is BUILT by addition in `knowledge/_capture_gate.py`: a `boot-drift CEILING DECLARED` line in `notes/_GAUGE-LOG.md` discharges a ceiling breach ONLY when its breach session/figure, its ceiling and every post-breach reading it lists MATCH what the gate computes AND every post-breach reading in the window is under the ceiling; a mis-stated line fails LOUDER under its own name (`CEILING DECLARATION MISMATCH`); a later over-ceiling reading un-discharges. A new selftest arm (`selftest_boot_ceiling_discharge`, wired into `_selftest_body`) drives arms (a)–(d) plus a staleness arm and a form-strictness arm, and was mutation-tested against the GATE (8 scratch mutations, all caught; two escapes found and closed during the lane). The live declaration was written at the log's tail with the gate's own figures and `python3 knowledge/_checkin.py` now reports **0 STRUCTURAL** (was 1: the ceiling breach). `BOOT_CEILING_TK`, `BOOT_BAND_WINDOW`, `BOOT_BAND_SIGMA` untouched (`git diff knowledge/_gauge_tokens.py` is empty). The existing boot arm `selftest_boot_delta_parse()` reads 0 failures before and after, and the pre-#245 breach message is byte-for-byte the same head (asserted by the new arm against an independent literal). ⚠ PREMISE CORRECTED: the brief said post-breach readings "#243, #244" — the gauge log has NO #244 block; #244's 67,294 sits in `GOOD-MORNING.md:471` and reaches the log at #245's 2f roll. The gate holds ONE post-breach reading today (#243 67,135), so the declaration lists exactly that; listing #244 would be a MISMATCH by design (Finding 3). Forward-simulated: after the roll the discharge STANDS and names #244 as graded fresh (Finding 6).

COUNTS: findings `8` · ruling-shaped `1` · UNPROVEN `2`

## What was done

1. **Reproduced the red** — `python3 knowledge/_checkin.py` → `⛔ STRUCTURAL boot-drift CEILING BREACH: … #242 70,710` (`assets/checkin-before.txt` line 29). Probed the arm's inputs directly: window #237–#243, post-diet readings `[(241, 69092), (242, 70710), (243, 67135)]`, ceiling 70,000 (`assets/selftest-boot-arm-before.txt`; md5 of both files before edits in `assets/md5-before.txt`).
2. **Parser + legal form** — `knowledge/_capture_gate.py` lines 3963–4017 (by addition, directly after `_parse_legacy_boot_declarations`): the `s244-D1` comment block, `BOOT_CEILING_DECL_RE`, `BOOT_CEILING_DECL_READ_RE`, `BOOT_CEILING_LEGAL_FORM`, `_parse_boot_ceiling_declarations()`. Legal form, documented in code:
   `> **boot-drift CEILING DECLARED #<N> (<YYYY-MM-DD>):** breach #<B> <F> · ceiling <C> · post-breach readings #<s1> <r1>, #<s2> <r2> all under · DISCHARGED per `s244-D1`; the ceiling literal was NOT moved.`
3. **Discharge logic in ARM 1** — same file, the `if over:` branch (lines ~4344–4440). Per breach: `matched` (breach figure, ceiling and every listed reading equal the gate's, session by session) · `mismatched` (a declaration for this breach that is not matched) · `post_over` (later readings over the ceiling). Four outcomes: DISCHARGED note (`s244-D1`, names any newer unlisted-and-under readings) · `CEILING DECLARATION MISMATCH (CEILING BREACH #B F STANDS)` fail · `CEILING BREACH #B F ⛔ NOT DISCHARGED` fail when a later reading is over (the new breach also fails in its own right) · the pre-#245 `CEILING BREACH:` message, byte-identical, now followed by the legal form filled with the gate's computed figures when the breach is dischargeable.
4. **Selftest arm** — `selftest_boot_ceiling_discharge()` at line 8711, wired at line 9578 of `_selftest_body` beside `selftest_boot_delta_parse()`. Arms: (d) no declaration → original message byte-for-byte (independent literal) + legal form + computed figures; (a) matching → 0 fails + `DISCHARGED (`s244-D1`)` note, and the line is inert to `_parse_boot_samples`; (b) six single-field mutations of the declaration (breach +1, ceiling ±1, post-breach figure +1, a reading the gate does not hold, a pre-breach reading listed as post-breach) → `CEILING DECLARATION MISMATCH` naming the standing breach; a declaration addressed to the wrong breach session → original red stands; (c) a later reading over → NOT DISCHARGED + the new breach by name, no discharge note; staleness → an unlisted newer UNDER reading keeps the discharge and is named; form → a line without `all under` is not a declaration.
5. **Mutation test of the GATE** (`assets/selftest-mutation-transcript.txt`): eight scratch copies of `_capture_gate.py`, one clause broken each — M1 drop breach-figure match · M2 drop ceiling match · M3 drop post-breach match · M4 drop the EVERY clause · M5 fail on unlisted newer readings · M6 drop the legal-form hint · M7 one byte of the pre-#245 message (`DOWN`→`down`) · M8 regex no longer requires `all under`. Control M0 green; **M1–M8 all RED by the named letter**. Two escapes were found on the way and closed: M7 escaped a head-only literal (widened to the whole message), M8 escaped before the form arm existed (added).
6. **Live declaration** — appended as the last line of `notes/_GAUGE-LOG.md` (line 2841, tail of the `#### 2026-09-03 #243` block — the placement ARM 2's `DECLARED #242` line used at the tail of #241's block), figures copied from the gate's own hint: `breach #242 70,710 · ceiling 70,000 · post-breach readings #243 67,135 all under`. Parsed back: `[{'sess': 245, 'bsess': 242, 'bfig': 70710, 'ceiling': 70000, 'reads': [(243, 67135)]}]`; reading count 110 → 110, deltas 7 → 7, refused 0 → 0, `boot_stratum_double_count_check` 0 fails (`assets/live-arm-after.txt`).
7. **Index rebuilt** — the appended log line staled `knowledge/_memento-index.json` (the index covers `notes/_GAUGE-LOG.md`; `_build_memento_index.py` docstring line 17), which surfaced as a DIFFERENT STRUCTURAL fail (`assets/checkin-after-1-index-stale.txt` line 29). Ran `python3 knowledge/_build_memento_index.py` alone (1.1 s, 1994 records; NOT `_build_all.py`) — the wrap's 2g re-runs it regardless. Then `_checkin.py` → **0 STRUCTURAL** (`assets/checkin-after.txt` line 30).
8. NOT done, by rule: no constant/band/sigma touched · `_build_all.py` not run · nothing committed · no memory written · no filed report or GM/LS file edited · `_RUNBOOK-capture-ritual.md` (named in the ruling's `governs`) NOT edited — reported under Findings 8.

## Findings

1. **The ruling, verbatim** (`knowledge/_rulings.json`, id `s244-D1`, `ruled`): "ARM 1 OF `boot_constant_drift_check` GETS A DECLARED-DISCHARGE FORM, AND THE CEILING LITERAL DOES NOT MOVE. A CEILING BREACH already in `notes/_GAUGE-LOG.md` may be DISCHARGED BY DECLARATION when EVERY post-breach reading is UNDER the ceiling; the discharge is a declaration that must match the gate's own figures, on the `s241-D1` ARM 2 pattern (`boot-drift DECLARED`), and it clears the arm for that breach only. `BOOT_CEILING_TK = 70,000` is NOT moved, NOT widened and NOT re-based - the ceiling stays shrink-only and Dave's. Option (b) of the #243 wrap report's RULING-SHAPED question 1; options (a) leave the red standing every wrap and (c) grade only the newest reading are REJECTED. The BUILD is NOT this wrap's - it is carried to #245, and until it exists the wrap gate stays RED on the arm and the `--wrap` FINAL commit stays OWED." `says`: "Dave at #244 … '1. (b)'."
2. **Before/after check-in heads** (probe: `python3 knowledge/_checkin.py`, REHEARSAL block). BEFORE: `⛔ STRUCTURAL boot-drift CEILING BREACH: `_gauge_tokens.BOOT_CEILING_TK` = 70,000 and 1 post-diet reading(s) EXCEED it — #242 70,710. …` / `rehearsal [wrap-gate, early]: 1 STRUCTURAL fail(s)`. AFTER: no ⛔ line / `rehearsal [wrap-gate, early]: 0 STRUCTURAL fail(s) — fix NOW, cheap · 0 heals-at-wrap (ritual steps 1/2) · 14 warn(s) (was 14 at the last logged run)`. The only changed warn is the git uncommitted-path count (4 → 9; the other lanes' files are in that count too — see `git status --short`).
3. **The brief's premise "#243, #244" was wrong by one roll.** `grep -n "#244" notes/_GAUGE-LOG.md` → 3 hits, all prose inside the #243 block; `grep -n "^#### " notes/_GAUGE-LOG.md | tail -1` → `2823:#### 2026-09-03 #243`. #244's boot is `GOOD-MORNING.md:471` "**boot 67,294 real**". The gate reads the log only, so the figure it can verify today is `#243 67,135` alone — and the form treats a listed reading the gate does not hold as a MISMATCH (arm (b) "a reading the gate does NOT hold (not rolled yet)"), so writing #244 into today's line would have failed louder. This is the #243 stratum's own lesson ("the gate is structurally one wrap behind on its own session") applied to the declaration.
4. **Byte-for-byte on the existing arm** — `selftest_boot_delta_parse()` 0 failures before (`assets/selftest-boot-arm-before.txt`) and after (`assets/selftest-arms-individual.txt` last lines). The undeclared breach message is rebuilt from `undischarged` rather than `over`, which is the same list whenever nothing is discharged; the new arm asserts the full pre-#245 text against a literal typed independently of the gate's string, and M7 proves that assertion bites.
5. **Semantics chosen for "cannot go stale", and why** — the declaration is matched against the CURRENT computation every run (it cannot discharge a breach it does not name, and any later over-ceiling reading un-discharges it — the forward counterfactual in Finding 6 shows both fails). Newer UNDER readings that arrive by later 2f rolls are NOT required to be listed: they did not exist when the line was written, the gate verifies the invariant on them itself, and requiring them would force a fresh declaration at #245, #246, #247 and #248 (until #242 leaves the n=7 window) — i.e. option (a) by another route, which Dave rejected. They are named in the discharge note as "not listed by the declaration, graded fresh here and under". M5 proves the arm would catch the stricter reading being smuggled in.
6. **Forward simulation** (`assets/forward-sim-after-244-roll.txt`, scratch copy of the live log + a `#### 2026-09-03 #244` block): (i) with `boot 67,294 real` rolled in → 0 fails, note "… DISCHARGED (`s244-D1`) … 1 newer reading(s) not listed by the declaration, graded fresh here and under: #244 67,294"; (ii) counterfactual `boot 70,001 real` → 2 fails: `CEILING BREACH #242 70,710 ⛔ NOT DISCHARGED … #244 70,001` and the plain `CEILING BREACH: … #244 70,001`. So the #245 wrap should read green on this arm after its 2f roll without writing anything, PROVIDED #244's stratum rolls in as 67,294.
7. **Timing** — the new arm runs in ~4 ms; `boot_constant_drift_check` on the live log is sub-second; `_checkin.py` ~15 s each run. The full `--selftest` (32 arms in `_selftest_body`) was NOT run — it exceeds the ~178 s call wall (declared skip, size: 32 arms; 2 arms driven directly + 8 gate mutations).
8. **`_RUNBOOK-capture-ritual.md` carries no line for either declared form** — `grep -n "boot-drift DECLARED\|CEILING" knowledge/_RUNBOOK-capture-ritual.md` → 0 hits, though the ruling's `governs` names the runbook. Not edited (outside the brief's deliverables). The gate's own fail text now prints the legal form with the computed figures, so a wrap sub is not blind without the runbook; the runbook addition is a one-line carry for the conductor.

## RULING-SHAPED QUESTIONS

1. **Two breaches in one window** — the ruling's clause "EVERY post-breach reading is UNDER" is applied literally: if a second over-ceiling reading lands while #242 is still in the n=7 window, #242's discharge stops standing (and the new breach fails by name) until #242 rolls out of the window — the older breach can never be re-discharged while the newer one sits after it, even once readings after the NEWER breach are all under. Option (a) keep literal (built; self-heals as the window slides, at most 6 wraps red on the older breach); option (b) "every post-breach reading under OR itself a separately-discharged breach" (a chain of discharges — one more clause, one more selftest arm, ~60 lines). Recommend (a) until it actually happens: the literal reading is Dave's text, and a second breach is exactly the moment the red SHOULD be loud. Not built either way beyond (a).

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** that the FULL `--selftest` is green with the new arm wired — price: one ~4–5 min run outside the 178 s sandbox wall (`python3 knowledge/_capture_gate.py --selftest`), or the CI run on the next commit. What IS proven: the wired call resolves (`inspect.getsource(_selftest_body)` contains it), the arm returns `[]` on the real module, and the two boot arms pass individually.
- **UNPROVEN:** that #245's wrap 2f roll will write #244's stratum with `boot 67,294 real` in a shape `_parse_boot_samples` reads (the forward sim used a hand-copied one-line block) — price: the wrap itself; if the roll's shape differs, the gate's note/fail will say which.
- **CLAIMED:** `_checkin.py`'s 14 warns are unchanged in content apart from the git-path count — read from the "was 14 at the last logged run" line, not diffed body-by-body (`--warns-full` not run; ~16K tape).

## Evidence

`notes/_subreports/assets/2026-09-03-245-D-discharge-arm/` —
`checkin-before.txt` (the reproduced red, line 29) · `checkin-after-1-index-stale.txt` (ceiling red GONE, index-stale red present — the side-effect of any gauge-log line) · `checkin-after.txt` (0 STRUCTURAL, line 30) · `selftest-boot-arm-before.txt` (existing arm 0 fails + live arm 1 fail, pre-edit) · `selftest-arms-individual.txt` (arms (a)–(d) driven one by one with their verbatim fails/notes; existing arm 0 fails post-edit; wiring check) · `selftest-mutation-transcript.txt` (M0 green, M1–M8 red by letter) · `live-arm-after.txt` (parse-back of the declaration, live arm 0 fails, double-count 0) · `forward-sim-after-244-roll.txt` (Finding 6) · `capture_gate.diff` (the whole code change, 338 lines) · `md5-before.txt` / `md5-after.txt`.

REPLAY-THESE: `notes/_subreports/assets/2026-09-03-245-D-discharge-arm/selftest-mutation-transcript.txt` (~1.1K tk) · `notes/_GAUGE-LOG.md` line 2841 (~350 tk) · `knowledge/_capture_gate.py` lines 3963–4017 and 4344–4440 (~2.6K tk)
