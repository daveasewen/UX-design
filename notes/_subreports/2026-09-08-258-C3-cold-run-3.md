# #258 lane C3 — THE THIRD COLD RUN, against the tree at `5c241f7` (s258-D3 enacted)

**Read-only against canon / knowledge / showroom / apollo-spider. Moved nothing. Ruled nothing.**

## Paths

- arm — `outputs/coldrun-258-v3/arm-A-blind/dashboard.html` (80,977 bytes, frozen before any measurement)
- also in the arm — `READ-LOG.md`, `brief.md` (six grill answers, all *skipped/defaulted*, + used/missing + Gaps),
  `render-{1440,820}-{light,dark}.png`, `drive.json`, `probe2.json`, `gate-screen.txt`,
  `gate-screen-ledger.md`, `gate-receipt.txt`, `_app.js` (the authored JS, before inlining),
  `_drive.py`, `_probe2.py`, `coldrun258v3.canon.html` (the copy the gates ran on)
- review — `reviews/COLDRUN-258-2026-09-08-v3.html` (four-column delta: #246 · v1 · v2 · v3)
- subreport — this file

## Scores: 3 / 2 / 3 / 2

| | | earning fact |
|---|---|---|
| 3 | visually rich | all six dataviz vars resolve in both modes off the rendered page (`--baseline` `#1A1A1A` light / `#FFFFFF` dark); 15 components; every bar, sparkline point and KPI computed at run time |
| 2 | full | 50 payments, 12 accounts, 5 entities, 6 currencies, 24-month series, footer present — held at 2 by the grid rendering 760px inside a 1,311.98px content box |
| 3 | persistent | 14 observed keys identical across a cold reload of `?view=payments&q=payroll&period=365&sort=gbp&dir=ascending&size=12`; a filter also survives nav-away-and-back |
| 2 | interactive | 39 driven steps, 0 pageerrors, 15 of 18 control families live — the three toolbar listboxes are unreachable (`opacity:0; pointer-events:none`; `elementFromPoint` returns the toolbar) |

`pageerror` on load: **0**. Across the whole drive incl. a reload: **0**. Console errors: **0**.

## Measurement 10

- `grep -c APOLLO-DEMO outputs/coldrun-258-v3/arm-A-blind/dashboard.html` → **0**
- payments grid at 1440: `#dg` **760.00px** in a tile of **1,359.98px** (content box **1,311.98px**) —
  **551.98px dead band, 57.9% fill**, Amount column clipped. Cause: `canon.css:10341`
  `:where(.cn-data-grid) .dg{width:var(--demo-width, 760px); max-width:100%}` — s258-D3 moved the dial in
  the snippet (`--dg-max`) but **canon.css is not in commit `5c241f7`**; that line dates to `0272996` (#215).
  `grep -c demo-width knowledge/canon/canon.css` = **36**, of which **28** are `width:var(--demo-width, …)`.

## Top three findings

1. **s258-D3 fixed the snippets and left canon.css behind.** The fence held perfectly (0 markers), and the
   defect the ruling was written against still reaches the render through the canon route that skill step 3
   tells the builder to take.
2. **`FAIL:DEMO-CHROME-COPIED` is unreachable for a first-run page.** Mutation test: clean and mutant both
   return `FAIL:NO-RECEIPT` and the fence is never inspected; after `--mint`, clean PASSes and the mutant
   FAILs on the fence. The skill never says to mint.
3. **The Filter-toolbar-bar's open-state contract (`.menu[data-open="true"]`) is invisible to the procedure.**
   The address is in `filter-toolbar-bar.meta.json` `behaviour.script`; skill step 2 lists variants, states,
   antiPatterns and relationships — not `behaviour`. Three filters died on it.

Four more are numbered F04–F07 on the review page (compose gate vs rule 2 on the Data-grid checkbox; the
screen gate writing into the repo then refusing over what it wrote; 10 of 11 chart types unused for the
fourth run running; two nav destinations resolving to overlapping walls).

## Creativity judgement (labelled as one, carried forward from the v2 brief)

The rewrite invited a *derivation layer* that a 2a-bound build could not have produced: one `DATA` object,
and every KPI, delta, sparkline, chart bar, rail row, header-strip chip and grid page computed from it by a
single `renderAll()`. Concretely — the corridor chart's domain, gridlines, axis labels and bar geometry are
generated per filter against the snippet's own `data-fx`/`data-fw` fraction grammar, so `dv-behaviour.js`
re-fits authored geometry it never saw baked. That is authored code cooperating with a carried behaviour,
which is precisely what rule 2a's removal made legal.

## Context use at the build → measure seam

The arm was frozen before any render, gate or browser call. At that seam the lane had consumed roughly
60K of fill: the two skills in full, ~350 lines of the bento template, ~350 of Data-grid, ~90 of
Filter-toolbar-bar, greps over canon.css, and the two authored files. Measurement, gates and the review
added the rest.

## NOT done — declared

- No sighted arm. One arm only, as the brief asks.
- No provenance receipt minted on the arm itself (only on `/tmp/` copies, for the mutation test) — minting
  edits the page, and the arm is frozen.
- `_validate_composition` returned `UNPROVEN: C9 bands` rather than a verdict; not chased.
- Playwright a11y/target-size warnings were not enumerated beyond the gate's `a11y: ✅`.
- The three dead listboxes were **not fixed**. The arm is the measurement.
- Nothing under `knowledge/`, `showroom/` or `apollo-spider/` was edited; no build, commit, push or checkout.
  `_validate_screen.py` wrote `knowledge/_screen-gate/coldrun258v3.canon.md`; the ledger was copied into the
  arm folder and the repo file removed, so `git status --porcelain knowledge/` is clean.

## Tokens

~118K of context consumed by this lane end to end (build ≈60K at the seam, measurement + review ≈58K).
