# `#228` — THE REPAIR LANE: Dave's confirmed 9/9 set, enacted

session: `#228` · 2026-08-31
window: the repair lane · conductor: Fable · Opus build sub
brief: `notes/_briefs/2026-08-31-228-repair-lane-brief.md` (Dave's confirm-pass set, ruled)
tokens: `UNMEASURED` — a sub cannot read its own `message.usage`; the conductor holds the figure

## COUNTS

**enacted 23 · repaired 2 · ruling-shaped 3 · UNPROVEN 6**
(plus **HELD 1** — C8, by Dave's instruction, and **DEFERRED 1** — the manifest regen, which is
the first step of the cut and the cut is not this lane.)

## COMMITS

| hash | subject |
|---|---|
| `cec5997` | after #228 2026-08-31 — cold-start contract: the nine confirmed repairs, the B2 merge, and grill-me on the Copilot path |
| `a6c2add` | after #228 2026-08-31 — L8 and s227-D7: the underline stays straight, and concentric reaches all four scales |
| `53303b0` | after #228 2026-08-31 — the theme-key alias, the FAB defaults, B1 shipping, and a disk preflight at the opener |

All three on `master`, **unpushed** — the conductor pushes and reads CI back (`s203-D1`).

⚠ **One process note, declared.** The first commit was hand-rolled from the runbook's manual
sequence before I opened `_git_commit.sh`, so it landed with **no T3 session prefix**. Caught
by reading `git log`, not by a gate. It was `git reset --soft HEAD~1`'d while still unpushed
and re-made through the script; `cec5997` is the script's commit and carries exactly one
prefix. Every commit after it went through `SESSION_N=228 bash knowledge/_git_commit.sh
--reconciled`. This is the [[feedback-read-the-runbook]] class in its smallest form — the
runbook's first line says run the script, and I reconstructed the sequence instead.

---

## ENACTED — 23

### The contract repairs — 9 of 10 (brief item 1)

The confirm page's `C<n>` labels map one-to-one onto the red team's `S<n>` findings
(`notes/_subreports/2026-08-30-227-contract-redteam.md`); I verified that mapping off the
rendered card text before touching anything, because `C8` is HELD and a mis-mapped number
would have enacted the held one.

1. **C2** `skills/grill-me/brief-template.md` — the filled example now shows a **skipped**
   theme, and the skip fact lives in the table row.
2. **C3** `FIRST-SESSION.md` — day one's bug-report sentence gains the declared lane, the
   brief, and a pointer to `cold-start/REPORT-TEMPLATE.md`.
3. **C4** `verify_placement.py` — `MARKER` → `MARKERS`, two of them (§1's lane line and §3's
   `**Never invent.**`), `all()` in `inspect()`, and the detail names *which* one is absent.
   A partially-present contract now reports as such rather than as PLACED.
4. **C5** `gen_projections.py` — `BYTE_BUDGET = 4096` beside the line budget, with a second
   refusal in `read_source()` naming the overage in bytes. The shipped contract is **30 lines,
   3,717 bytes** — inside both, with 379 bytes of headroom. ⚠ C8, when it lands, adds ~215
   bytes; still inside, but the headroom is now worth watching.
5. **C6** `DESIGN-CONTRACT.md` §3 — "Check before you show" now says *open* the skill and do
   what it says, and that reading a file is not running a gate.
6. **C7** `DESIGN-CONTRACT.md` §3 — rule 4 points at `skills/generate-from-canon/SKILL.md`;
   `_bento_edit_rails.json` is named as the dial vocabulary it reads, not a layout.
7. **C10** `DESIGN-CONTRACT.md` §4 — a non-Apollo design skill **is freestyle**, declared in
   the same reply, with the skill named.
8. **C12** `DESIGN-CONTRACT.md` §2 — the truncation fallback gains the one question that
   survives being cut: which theme.
9. **N1** `verify_placement.py` `run()` — a `--root` that is not a directory prints
   `NOT A DIRECTORY: <path> — nothing was inspected.` instead of a confident
   "⚠ 3 of 3 hosts start COLD here". Driven both ways: a path that does not exist, and a path
   to a real file.

⛔ **C8 IS NOT IN THE TREE.** Its exact before/after is in REPLAY-THESE below, ready to paste.

### B1 — the whole cold-start deliverable was shipping nowhere (brief item 2)

10. `knowledge/_release/_gen_pack_manifest.py` gains a **named `cold-start` group**, placed
    before `skills` on the same ordering argument `gumdrop` records for itself.
11. The `skills` match now claims `*/brief-template.md` as well as `*/SKILL.md` (red-team
    S11): `grill-me/SKILL.md` shipped a line pointing at a file that was not in the pack.
12. **S13, the two pre-existing holes**: `apollo-spider/.github/copilot-instructions.md` said
    *"The five files under `skills/`"* with no grill-me row — now six, with the row — and
    `.github/prompts/grill-me.prompt.md` is the missing sixth slash command, copied from the
    shape of the other five. It ships already: the `gumdrop` group claims `.github/` whole.

**DRIVEN, with the red team's own probe** (`groups()` called against `git ls-files`):

```
SHIPS   apollo-spider/cold-start/DESIGN-CONTRACT.md            ['cold-start']
SHIPS   apollo-spider/cold-start/REPORT-TEMPLATE.md            ['cold-start']
SHIPS   apollo-spider/cold-start/gen_projections.py            ['cold-start']
SHIPS   apollo-spider/cold-start/projections/.github/copilot-instructions.md ['cold-start']
SHIPS   apollo-spider/cold-start/projections/AGENTS.md         ['cold-start']
SHIPS   apollo-spider/cold-start/projections/CLAUDE.md         ['cold-start']
SHIPS   apollo-spider/cold-start/verify_placement.py           ['cold-start']
SHIPS   apollo-spider/skills/grill-me/brief-template.md        ['skills']
```

Every one of those read `DROPPED []` in the red team's run. **The other half of B1 was already
closed before I arrived**: `apollo-spider/cold-start/` is TRACKED (`git ls-files` returns all
7). The red team's "untracked, so `git archive` ships zero files" finding has aged out — a
premise worth re-checking rather than inheriting [[premise-ages-faster-than-rule]].

⚠ `GROUP_LEAD` and `GROUP_ORDER` both needed the new group. **I did not notice; the generator's
own selftest did**, going red on exactly the two omissions the #219 seam-9 guard was built for
(`a group with no GROUP_LEAD entry is a KeyError at render`). 216 bites, 0 fail after.

### B2 — the copilot projection is now a MERGE (brief item 3)

13. `gen_projections.py` renders the copilot host from **both sources**: the design contract,
    then `---`, then the pack's own `.github/copilot-instructions.md` verbatim. A second
    header (`MERGED_HEADER_FMT`) names both sources. A missing boot file **refuses loudly**
    rather than silently emitting an un-merged projection. `MERGE_SOURCE_REL` is relative to
    the script, so it resolves identically in the repo and in an unzipped pack.
14. `verify_placement.py` `report()` — a `no-contract` row now reads
    `MERGE cold-start/projections/… INTO … — do not overwrite it, that file may already carry
    other rules`. A `missing` row still says copy.

The failure this closes, in the red team's words: the tool used to *instruct the designer to
overwrite* the boot rule, the skills table and the prompt-file bridge, in the one layout
`FIRST-SESSION.md` tells them to work in. There is now no state in which placing the
projection loses the boot file.

### S1 — the report template's discriminator (brief item 4)

15. `REPORT-TEMPLATE.md` box 2 now asks for the next sentence too, if the assistant said it
    could not reach the skills — *"an 'on-canon' build that could not open a skill is the same
    as no rules at all"*.

### W8 — a checking flag that wrote (brief item 5)

16. `knowledge/canon/gen_canon_tokens.py` dispatched on `--selftest` only, so `--check` — and
    any typo — fell through to the write path and silently re-minted `canon.css`. Now: a real
    `--check` that compares and returns 0/1 **writing nothing**, and `KNOWN_FLAGS` so an
    unknown argument refuses `rc=2`. A fourth selftest bite drives `--check` against the real
    `canon.css` and asserts **both bytes and mtime** are untouched.

### L8 — the underline stays straight (brief item 6)

17. `knowledge/snippets/Search-field.reference.html` — `border-radius:var(--border-radius-control)`
    moves off the base `.search` rule and onto `.search.boxed`. The binding is unchanged; the
    var still resolves per theme on the component, it is only *consumed* by the boxed variant.
    Canon now carries it at one place only:
    `:where(.cn-search-field) .search.boxed{… border-radius:var(--border-radius-control);}`.
    The snippet comment records that Dave overruled the Dropdown / Input-fields precedent
    **on the eye**, and why an underline is a different case.

### s227-D7 — concentric reaches all four scales (brief item 7)

18. `SEGMENTED_CONCENTRIC_SCALES` `("xs","s")` → `("xs","s","m","l")`. The **store** was
    re-minted, not the artifact: console `border-radius/segmented-thumb/m` 4 → **8**
    (track 10 − padding 2) and `/l` 6 → **8** (track 12 − padding 4), each `$note` carrying
    the ruling, the arithmetic and the prior mint. `$concentricOpenQuestion` is rewritten from
    RULING-SHAPED to **CLOSED**, naming what closed it. mono / legacy / supercharge are 0 at
    every scale under both laws — unmoved, verified in the `--assert-mint` table.

    `--assert-mint`: **16 theme/scale pairs, all OK**. In canon, under
    `[data-apollo-theme="console"]` only: `--border-radius-segmented-thumb-m: 8px`,
    `--border-radius-segmented-thumb-l: 8px`.

    ⚠ **A selftest arm had to be repaired, not deleted.** The s202-D2 dial proof picked "the
    first scale NOT in the concentric set" and appended a failure when there was none — with
    all four concentric there is no such scale. The dial branch is still the law for
    `scale=None` and for any scale outside the tuple, so the proof now falls back to the
    unscaled call and says so in its output. Losing a proof because the scope moved would be
    an instrument that quietly stopped measuring.

### L1 / L2 — the FAB defaults (brief item 8)

19. `REVEAL_DEFAULT = 'hotcorner'` (was `'always'`), used at both default-resolution points
    and in the head-comment contract. `CORNER_DEFAULT = 72` keeps its number and loses its
    "PROPOSED, not ruled" label — Dave gave no other number and the card said 72 stands.
20. **The button face is the library icon.** `knowledge/assets/icons/global-controls/settings.svg`
    — its 18×18 viewBox, its single `fill-rule="evenodd"` path, its `currentColor` fill. The
    path string was compared **programmatically** against the library file: identical, 1,911
    chars. The glyph it replaces was hand-drawn, in a pack whose first rule is *never invent
    an icon*; `settings` is the library's own name for it and the same file
    `Template-settings.reference.html` already uses.

    ⚠ **Five new checks in `verify_apollo_fab_227.js` drive a mount with NO CONFIG AT ALL.**
    Every one of the existing 59 arms names its `reveal` explicitly, so **all 59 stayed green
    the whole time the default was still `always`** — a ruling that lives only in a constant
    is a ruling no test can see [[instrument-without-a-consumer]]. 64 checks, all pass.

### s227-D8 — the W5 safe path (brief item 9)

21. **(a) THE ALIAS, ADDITIVE.** `tokens/themes/_themes.json` gains
    `"attrAliases": ["common"]` on `apollo-legacy` with a `$attrAliasNote` carrying the ruling
    and the ⛔ against removing `legacy`. `gen_theme_cascade.py` gains `theme_attrs()` and
    `sel_group()`, and every selector it emits for the theme is now a list covering both keys
    — **canonical first, verbatim**. 413 occurrences each way in the built CSS.

    A new selftest arm proves the alias is additive **in both directions**: the canonical key
    must still be present, the alias must appear the same number of times, and the canonical
    key must still lead its group. An alias that *replaced* the old key would have satisfied
    every other check in that file while stripping the theme off every saved page.

    Three existing selftest assertions matched single-selector strings literally and had to be
    re-expressed through `sel_group()` — they went red first, which is the correct behaviour
    and is why they are worth having.

22. **(b) THE RECONCILE SCRIPT** — `knowledge/canon/reconcile_theme_key.py`, new. Rewrites the
    attribute VALUE `legacy` → `common` in a designer's own project. **Dry run by default**,
    `--write` to commit, `--root` to point elsewhere, `--selftest` for 6 arms.

    - Five exact spellings (HTML both quote styles, CSS selector both quote styles,
      `setAttribute`). **The bare word is never rewritten** — it is ordinary English in
      comments, class names, paths and prose, and a tool that rewrote all of it would do more
      damage than the inconsistency it fixes. Driven on six negative cases including
      `class="legacy-panel"` and `data-theme="legacy"`.
    - ⛔ **It refuses `canon.css`, `_themes.json` and `gen_theme_cascade.py` by name**, even
      under `--write`, and says why: those are where both keys are *defined*, and rewriting
      them deletes the compatibility half of the alias. Proven by an arm that plants a
      `canon.css` and checks the bytes afterwards **and** that the skip was named, not silent.
    - It lives in `knowledge/canon/` deliberately: that directory ships whole under the
      manifest's `engine-canon.canon` group, so the script reaches designers with **no
      manifest change**. A file at `knowledge/` root would have been claimed by nothing — the
      B1 class, one day later.

    **(c) the full rename was NOT started.**

### Add-on B — the two CI reds — repaired 2

**R1 · R2** — `knowledge/_gate_scratch_hygiene.py` and `knowledge/_render/gen_fab_meta.py` were born
    #227 with no help gate and are two of CI run #466's reds (the #158-class gate aborts build
    step 6 on them). Both now carry the **identical** five-line pattern their own siblings use
    (`_gate_doc_rows.py:47-52`, `gen_library_214.py:76-81`) — copied, not invented.

    `_validate_help_gate.py`: **222 scripts, 0 failures** (was 2). Driven: `--help` on both
    prints the docstring and writes nothing (`apollo-fab-meta.json` md5 unchanged across the
    call); both normal runs unaffected.

### Add-on C — the disk preflight

23. `knowledge/_checkin.py` gains `disk_reading()`, `disk_lines()` and `--selftest-disk`, wired
    into the opener output **at the head, beside freshness**, and into `--json`. It reports
    percent-used for `/` and `/sessions` and prints a loud `⛔ DISK <mount> IS N% FULL` line —
    naming the mount — at ≥ 90%, with the remedy (`_gate_scratch_hygiene.py`) and the reason
    (at 100% the sandbox cannot create its user and no session boots — #227 and #228).

    Not a flag, on the same argument that wired the B3 grade alerts there: an opt-in preflight
    is one nobody runs, and it rides a read that is already law at the opener
    [[checkin-is-mandatory-not-optional]]. Advisory — it never changes the exit code. UNKNOWN
    is never defaulted: an unreadable mount is reported with its reason, never counted as 0%.

    `--selftest-disk`: **7 arms**, including the CONTROL (two cool mounts must warn about
    nothing) and the `89.9%` boundary (the threshold is `>=`, not "about ninety").

    Live, in the real opener output:
    ```
      DISK         VM scratch, percent used (no live GC — at 100% no session boots):
        /              48.9%  5,094,844 KB free
        /sessions      65.4%  3,346,988 KB free
    ```
    ⚠ **The VM disk is NOT full today** — 49% and 65%. The brief's standing context says
    "VM disk still 100%"; that premise has aged out since the #227 wrap.

---

## HELD — 1

**C8 is not in the tree.** Dave: *"lets just do the check on C8 first"*. Its premise — that a
designer's own global instruction file can outrank the contract — is BELIEVED, not measured
(red-team UNPROVEN 3). The exact edit is in REPLAY-THESE below, one paste.

## RULING-SHAPED — 3

1. **"Everything new emits `common`"** (brief item 9a's middle clause) was NOT enacted for the
   FAB, and this is a finding rather than an omission. The FAB writes `data-apollo-theme` onto
   Dave's existing pages. `grep -rln 'data-apollo-theme="legacy"'` across `showroom/` and
   `reviews/` returns **13+ pages carrying their own LOCAL `[data-apollo-theme="legacy"]`
   selectors** — including `reviews/FAB-READINGS-2026-08-30-v1.html`, which the FAB drives.
   Canon answers to both keys; **those local stylesheets do not**. Flipping the emitted value
   would break exactly the pages the alias exists to protect. Moving them is the rename lane
   (s227-D8(c)) and the reconcile script now exists to do it. ⇒ **Dave's, or the rename lane's.**
2. **`_gate_scratch_hygiene.py` is a WIRING ORPHAN** and `_validate_wiring.py` is red on it:
   *"exists on disk and NOTHING RUNS IT … wire it or exempt it BY NAME with a reason and a
   date."* Its own docstring says the only moment scratch can be cleaned is **at the wrap**, so
   the honest home is the `_git_commit.sh` wrap seam, not `_build_all.STEPS`. Editing the
   commit script mid-lane, when every remaining commit runs through it, was not a risk worth
   taking for a gate that is advisory by design. ⇒ **Recommend: wrap seam. One line.**
3. **`gen_projections --check` is wired NOWHERE** (red team's REPLAY-THESE, re-confirmed:
   nothing in the repo references it). Its byte-derived drift guard is therefore decoration —
   [[instrument-without-a-consumer]]. Adding it to `.github/workflows/gates.yml` is a
   CI-surface decision and the brief carries no word on it. ⇒ **Conductor's or Dave's.**

## DEFERRED — 1

**`knowledge/_release/_pack_manifest.json` was NOT regenerated.** The generator is repaired and
proven; the regen is the first step of the v1.0.4 cut (brief § "Then the cut") and the cut is
explicitly not this lane. `_validate_package_delta.py` therefore stays `rc=1`, and CI's release
step stays red, exactly as the brief predicts.

---

## WHAT WAS DRIVEN

```
python3 apollo-spider/cold-start/gen_projections.py --check       rc=0  (3 projections in sync)
python3 apollo-spider/cold-start/gen_projections.py --selftest    rc=0  10 arms (was 7)
python3 apollo-spider/cold-start/verify_placement.py --selftest   rc=0   6 arms (was 4)
python3 apollo-spider/cold-start/verify_placement.py --root .           MERGE line, live
python3 apollo-spider/cold-start/verify_placement.py --root <typo>      NOT A DIRECTORY, ×2
python3 knowledge/gen_radius_derive.py --check                    rc=0
python3 knowledge/gen_radius_derive.py --assert-mint              rc=0  16 pairs
python3 knowledge/gen_radius_derive.py --selftest                 rc=0
python3 knowledge/gen_snippet_tokens.py --check --quiet           rc=0
python3 knowledge/canon/gen_canon_tokens.py --check               rc=0  ← the new arm
python3 knowledge/canon/gen_canon_tokens.py --selftest            rc=0   4 bites (was 3)
python3 knowledge/canon/gen_canon_tokens.py --dry-run             rc=2  ← the new refusal
python3 knowledge/canon/gen_canon_components.py --check           rc=0
python3 knowledge/canon/gen_theme_cascade.py --check              rc=0
python3 knowledge/canon/gen_theme_cascade.py --selftest           rc=0  ← incl. the alias arm
python3 knowledge/canon/reconcile_theme_key.py --selftest         rc=0   6 arms
python3 knowledge/gen_showroom.py --check                         rc=0
python3 knowledge/gen_component_partials.py --check               rc=0
python3 knowledge/_validate_snippets.py                           rc=0  135 snippets, 0 failures
python3 knowledge/_validate_help_gate.py                          rc=0  222 scripts, 0 fail (was 2)
python3 knowledge/_release/_gen_pack_manifest.py --selftest       rc=0  216 bites, 0 fail
python3 knowledge/_checkin.py --selftest-disk                     rc=0   7 arms
python3 knowledge/_checkin.py --selftest-compaction               rc=0   5 arms (unmoved)
python3 knowledge/_checkin.py --no-rehearse --no-block --no-grades      DISK block live
node    knowledge/_render/verify_apollo_fab_227.js                rc=0  64 checks (was 59)
```

**Regen serial, ramp first / index last** (`[[regen-serial-set-is-ordered]]`), run twice — once
for L8 + s227-D7, once for the alias: `gen_radius_derive` · `gen_snippet_tokens` ·
`canon/gen_canon_tokens` · `canon/gen_canon_components` · `canon/gen_theme_cascade` ·
`gen_showroom` · `gen_component_partials`.

**Full validator sweep, 43 scripts.** Non-zero after this lane, every one accounted for:

| script | rc | why |
|---|---|---|
| `_validate_descender_computed.py` | 77 | declared skip — needs a headless browser |
| `_validate_state_contrast.py` | 77 | declared skip |
| `_validate_hit_area.py` | 2 | usage error — it needs paths or `--all`, not a failure |
| `_validate_type_composites.py` | 1 | **1091 violations — the standing ratchet debt, unmoved** (1091 at #227 too) |
| `_validate_evidence.py` | 1 | pre-existing: WIRE-14 / WIRE-21 observation mismatches, both about `_build_all` and `gates.yml`, neither touched here |
| `_validate_package_delta.py` | 1 | **expected** — the manifest regen is the cut's first step (DEFERRED 1) |
| `_validate_wiring.py` | 1 | **pre-existing** — `_gate_scratch_hygiene.py` orphan (ruling-shaped 2) |

`_validate_help_gate.py` was on this list at the start of the lane and is not on it now.

---

## UNPROVEN — 6

1. **NOT ONE PIXEL WAS RENDERED.** No headless browser was driven in this lane. L8's straight
   underline, the concentric `m`/`l` thumbs, the library icon on the FAB's face and the
   hot-corner reveal are **structurally** proven — parsed artifacts, quoted CSS, driven
   selftests, a byte-identical icon path — and **visually unproven**. Dave's eye is the first
   render of every one of them.
2. **`_build_all.py` was never run** — sandbox-impossible (~49s against a ~45s call kill), and
   a partial run strands the tree mid-build. So the full build's verdict on this tree is
   unknown; what is known is 20+ individual `--check`/`--selftest` return codes and the
   43-script validator sweep above.
3. **No cold host was driven.** The merged copilot projection has never been read by a real
   Copilot, and no fresh Claude session has been walked through the contract. The red team's
   UNPROVEN 1 and 4 stand entirely — including whether Copilot reads `AGENTS.md` or
   `.github/instructions/*.instructions.md`, which would have made B2 cheaper still.
4. **The v1.0.4 dry-run byte-match was not attempted** (the cut is not this lane). That the
   manifest regen produces the *expected* delta — skills 5→6, a new cold-start group,
   `/carries/what` drift clearing — is **predicted, not measured**. What was measured is that
   `groups()` now claims all 8 previously-dropped paths.
5. **CI has not seen any of this.** Three commits sit unpushed on master. Whether the two
   help-gate repairs actually clear CI #466's build step 6 is unproven until the conductor
   pushes and reads back.
6. **The disk warning has never fired on a real hot disk.** The ≥90% path is proven on
   synthetic rows only, because the live disk is at 49% / 65%. The threshold arithmetic, the
   naming, the boundary and the control are all driven; the wording under a genuine ENOSPC is
   not.

---

## REPLAY-THESE (conductor)

1. **⛔ C8 — take this to Dave in chat, one message, before it lands.** File:
   `apollo-spider/cold-start/DESIGN-CONTRACT.md`, §2, appended to its last sentence
   (`"…and say that default out loud before anything is built."`):

   > **after:** `…and say that default out loud before anything is built. If another instruction file tells you never to ask questions, say that in the same reply and still ask question 1 — the theme is the one answer nobody can guess back afterwards.`

   The caveat he was given on the card: **the premise is believed, not measured** — that a
   designer's own global instructions can outrank the contract. Confirming C8 confirms the
   *clause*; the check is still owed. It adds ~215 bytes to a contract with 379 bytes of
   byte-budget headroom, so it fits — but it is the last thing that will fit without a cut.
   After it lands: `python3 apollo-spider/cold-start/gen_projections.py` then `--check`.
2. **The push, and the CI read-back** (`s203-D1`). Expected after these three commits: the
   help-gate reds gone from build step 6; the release step **still red** until the manifest
   regen.
3. **The three ruling-shaped items above** — the FAB's emitted theme key, `_gate_scratch_hygiene`
   wiring, `gen_projections --check` in `gates.yml`. Each is one line once someone decides.
4. **The cut's first step is the manifest regen**, and it is now safe: the red team's
   ⛔ *"do not regenerate until the cold-start group question is ruled"* is discharged — the
   group exists, is named, has a lead and a card, and claims all seven files.
5. **`notes/_briefs/2026-08-31-228-repair-lane-brief.md` line 51 has aged out** — "VM disk
   still 100%" is false today (49% / 65%). The new preflight in `_checkin.py` is what will say
   so next time, at the opener, instead of a tool failing for a reason that reads as something
   else.
6. **⛔ THIS REPORT IS OWED A STORE ROW.** It was committed with
   `DOC_ROW_ACK` — the doc-row gate refused it, correctly, as the forgotten-document class
   (#185). Minting the row is the CONDUCTOR's act under `s218-D7`, not a sub's, which is why
   it was declared rather than self-served. `python3` → `_state.add()` with
   `home=notes/_subreports/2026-08-31-228-repair-lane.md`. Until then this file is invisible
   to every later retrieval.
7. **Working-tree residue I deliberately did NOT commit** (not mine, DO-NOT-TOUCH):
   `knowledge/_rulings.json`, `notes/_REHEARSAL-LOG.jsonl`, `notes/_dream/_GRADE-DECISIONS.jsonl`,
   `knowledge/_probe/session-228.json`, `knowledge/_probe/session-228.void-truncated-plant.json`.
   Every path in `git status` at hand-off is one of those five or this report.
