# 294 · W — the delegated wrap: the twelve were ruled, inscribed and enacted in one day

provenance: 294 · 2026-09-21
status: observed

session: `#294` · 2026-09-21 · lane `W` (delegated wrap sub) · model Opus 5 (1M) · conductor Fable 5.1
job: the whole capture ritual except step 3's memory-store write, which is a **SEAT LIMIT** (#278) and
is owed to the conductor as `notes/_lanes/294/WRAP-MEMORY-HOOK.md`.

> ⛔ **EVERY FIGURE BELOW WAS MEASURED AT THIS SEAT UNLESS IT SAYS `declared`. Where a measurement
> disagrees with the brief's declaration, BOTH READINGS ARE PUBLISHED AND NEITHER IS REWRITTEN.**

## VERDICT

**The ritual ran end to end: 1 · 1b · 2 · 2c · 2d · 2e · 2f · 2g · 3 (hook only, seat limit) · 4 · 4b
· 4c · 4d · 5.** Nothing was skipped. **Seven blocking gate fails stood at the open, all inherited,
and the wrap commit takes the `#243` DECLARED NOT-A-WRAP path — the twentieth consecutive wrap.**
Three fails were this ritual's own and all three were closed at the thing named, never by weakening a
gate: the ★ LATEST banner over cap (shortened to **exactly 1,200 of 1,200**), a stale retrieval index
(2g) and a stale title receipt (4b). ✅ **The `s294-D9` pre-flight stamp is CAPTURED for the first
time in ninety-four sessions and the gate graded it at ZERO FAILS before it was pasted.** ✅ **The
`s294-D12` `_to_delete/` arm reads CLEAN — his own hands.**

COUNTS: findings 18 · ruling-shaped 5 · UNPROVEN 3

---

## 1 · THE `_to_delete/` ARM — CLEAN, AND THE ARM'S OWN WORDS ARE THE RECEIPT

`python3 knowledge/_gate_scratch_hygiene.py`, **exit 0**, verbatim:

> `· `_to_delete/`: 1 top-level entry, none older than 3 sessions (current #294) — `s294-D12` clean.`

**Against lane B's measurement earlier the same day: 108 top-level entries, 106 of them older than
3 sessions, oldest `outputs_wrap110_part1.py.1786015959` at session #109 — 185 sessions.** ⛔ **No
seat could clear it: `s282-D4` records that this mount refuses `unlink`.** He asked *"teh whole
contents of to delete? all files and folders?"*, was answered *yes, everything to the Trash (not
Empty Trash), keep the empty folder*, and emptied it from Finder.

⚠ **The one surviving entry is `.DS_Store`, 16,388 B, mtime 18:34 today** — Finder's own artefact of
the emptying. It is inside the N = 3 window **by construction**, not a survivor of the backlog, and
the arm is right to pass it. Stated so that a later reader does not read `1 entry` as `1 left over`.

⛔ **NOTHING IN `_to_delete/` WAS PURGED, MOVED OR RENAMED FROM THIS SEAT.**

## 2 · THE PRE-FLIGHT STAMP — CAPTURED, AND GRADED BEFORE IT WAS PASTED

`python3 knowledge/_checkin.py --preflight-line 294` returned a **✅ CAPTURED** line, and
`_capture_gate.check_preflight` was then run **against that exact text** at this seat:

```
check_preflight(<the generated line>) → 0 fails · 1 warn · 0 notes
```

⇒ **the generated line IS #294's stamp.** Legal under `s294-D9` (*"widen"*), enacted by lane B as
`PREFLIGHT_MEASURED_RE`. The `⛔ NOT CAPTURED` refusal ran **#199 → #291, 185 occurrences**, each
copying the last with the ordinal bumped on a stated cause that was FALSE; #293 replaced it with a
legal refusal on a cause that was TRUE; **#294 replaces the refusal itself.**

⚠ **THE WARN IS PUBLISHED RATHER THAN SMOOTHED, AND IT IS THE HALF THAT DID NOT CLOSE.** The
measured line is the **SPEND**, not the **PRICE**. **#294 was never priced at its opener**, so the
price-versus-actual dataset has **no ESTIMATE for this session and none was invented**
[[feedback-measuring-tool-must-not-guess]]. The hole MOVED rather than closed — lane B's
ruling-shaped question 4, with the `_checkin.py` diff written out in a file it does not own.

★ **A METHOD NOTE WORTH MORE THAN THE OUTCOME:** the two arms `check_preflight` and
`check_preflight_tokens` disagree on this text — called directly, the tokens arm returns **2 fails**
(*silent on job, wrap* · *no total against a budget*). They do not conflict: `check_preflight` at
`_capture_gate.py:1473` owns the BRANCH SELECTION and delegates to the tokens arm only on the priced
shape (`:1549`), and **only `check_preflight` is wired into wrap mode** (`:6339`). ⇒ **A gate arm
called outside its own selector is not the gate.** This was measured before anything was written to
`GOOD-MORNING.md`, which is the only reason the stamp is the measured form and not a third refusal.

## 3 · THE GAUGE — FIVE DECLARED READINGS, FIVE EXACT REPRODUCTIONS

`_checkin.read_fill` **imported, never re-implemented**, against the session's TOP-LEVEL transcript
`/sessions/great-dreamy-noether/mnt/.claude/projects/session/501c6cd1-77ce-4c94-adf4-7d0edfcee9fa.jsonl`:

> **FILL 192,062 real · peak 192,062 · boot 74,656 · 28 continuous turns of 29 · 0 compaction
> records · 0 drops**

✅ **ZERO DIFFERENCE on every one of the conductor's five declared seam readings** — `143,951` ·
`163,311` · `168,205` · `173,377` · `182,609` — each located in the transcript's own per-turn `usage`
sequence (69 assistant `usage` records over 29 `read_fill` turns).

> **wrap-handover: brief-cut 182,609 real (conductor, DECLARED) · sub-cut 192,062 real (first-hand,
> this seat) · delta 9,453 real · replay unobservable (conductor-side, post-wrap)**

✅ **LEGAL subtraction** — both terms read the same window, and five agreements to the token are the
identification. ⚠ **9,453 is the LARGEST hand-over on the `s214-D5` record** (#293: 1,871; #292:
954), and the cause is nameable: **four conductor turns fall between the brief's cut and this
reading** — he answered the `_to_delete/` question and called the wrap inside that span. ⛔
**`s214-D4`'s re-derivation of the 49,071 wrap term is staged on n≥2; it has it, and re-basing a
constant is Dave's.**

**Bands, stated and never converted:** **12,062 past the 180,000 quality line — DECLARED**; 200,000
clear by **7,938**, 220,000 by **27,938**, 256,000 by **63,938**. ⛔ **No literal was moved.**

**Boot 74,656** is **4,656 over `BOOT_CEILING_TK` 70,000** (shrink-only, `s240-D2`/`s241-D1`) and
**452 above #293's 74,204**. It reconciles exactly with lane G's independent reading.

## 4 · SUBS — MEASURED 1,396,532 (n=8) AGAINST 1,404,885 DECLARED, AND THE OFFSET IS SYSTEMATIC

| lane | measured | declared | Δ |
|---|---|---|---|
| R | 129,210 | 130,117 | +907 |
| R2 | 115,754 | 116,472 | +718 |
| C | 85,327 | 85,871 | +544 |
| I | 188,891 | 190,158 | +1,267 |
| A | 181,735 | 182,914 | +1,179 |
| B | 203,815 | 204,690 | +875 |
| G | 259,604 | 260,893 | +1,289 |
| C2 | 232,196 | 233,770 | +1,574 |
| **total** | **1,396,532** | **1,404,885** | **+8,353 (0.59%)** |

⛔ **The declaration reads HIGH on all eight lanes and LOW on none** — a systematic offset, not
noise, and the shape says the conductor's figures were taken one turn later than `read_fill`'s newest
`usage` record. **Neither reading is corrected into the other.** ✅ **Every lane verified
`"model":"opus"` · `"spawnDepth":1` · `"agentType":"general-purpose"` from its own `.meta.json`** —
the first session since #291 with a transcript for **every** lane that ran. ⚠ **UNIT: REAL tokens,
QUOTA not FILL** — never summed with the fill figures. This wrap sub's own window is **excluded**, as
#292's and #293's were.

## 5 · WHAT THE RITUAL DID, STEP BY STEP

- **1** — `_LIVE-STATE.md` refreshed: a new `Last refreshed` line from `date`, a new ⏱ LATEST DELTA
  (18 blocks). `_validate_standing_instructions.py`: **28 standing docs reachable, PASS**.
  `knowledge/_REVIEW-SIGNOFF.md` fed — the review page's path entered **already struck**, because it
  was filed and answered in the same session and therefore never waited.
- **1b** — dossier at `_DECISION-HISTORY/2026-09-21-294-the-twelve-were-ruled-inscribed-and-enacted-in-one-day.md`.
- **2c** — #292's ★ PRIOR banner rolled to `_GM-ARCHIVE.md` under batch key `2026-09-21 #294` via
  `_gm_move.py` (11 lines, receipt on stdout). **EXIT CHECK ran first and is declared in the archive
  itself**: every ⚠/⬛ item on it is already held in `_CARRIES.md` § `residual → #293`, a file that
  never deletes, and has since aged into § `#294` and § `#295`.
- **2d** — #291's ⏱ PRIOR DELTA rolled to `_LIVE-STATE-ARCHIVE.md` (40 lines) under
  `## Rolled 2026-09-21 #294`. Deltas back to LATEST + 2 PRIOR.
- **2e** — DO-FIRST checked against its contract and its 120/180 cap: **no new stratum, no new
  notice, nothing over cap, nothing to roll.** §C likewise **deliberately not amended** — the whole
  forward set lives in `_CARRIES.md` with a probeable pointer (`s225-D2`), and the twelve rulings are
  a different batch from §C·2's sixteen. **Declared, not silent.**
- **2f** — `roll_2f` for #293: **38 lines → `notes/_GAUGE-LOG.md`** (appended at true EOF) and **2
  lines → `_GM-ARCHIVE.md`** (after the #294 batch key). Driven with the session-qualified
  `> **COMMIT STATE #293:**` anchor, per the #241 correction — the bare anchor matches four lines in
  this file and the mover refuses it, correctly.
- **2g** — `_build_memento_index.py`: **2,287 records**, fourteen kinds. It reports three duplicate
  headings, all pre-existing (two archive sections and `component:icon-button` in two trees).
- **3** — **SEAT LIMIT, never a skip.** `notes/_lanes/294/WRAP-MEMORY-HOOK.md` written in the #293
  shape for the conductor to place at #295's opener; the `s271-D4` re-read of its open items was run
  against `_rulings.json` as the ritual's final beat.
- **4b** — `_gen_titles.py` run **after** the banner, receipt written. RENAME and NEXT-TITLE both
  generated; the NEXT-TITLE line in `GOOD-MORNING.md` replaced. **27 tape against the 120 cap.**
- **4c** — `_gate_scratch_hygiene.py --clean --wrap`, and `tiktoken` reinstalled after it, per the
  brief. **12M is owned by dead sessions and is UNREMOVABLE from inside** — reported for drift
  visibility, not action.
- **4d** — `_render_rulings.py` rebuilt `notes/_RULINGS.html` (**634 rulings, 161 sessions,
  1,147,155 B**) and `--check` prints FRESH against sha256 `9401d3c1…`.
- **2 (DECLARE-LAST)** — ★ **`_gen_size_stamp.py --write`, the `s294-D8` generator's FIRST LIVE USE:**
  `WROTE — GM 37,088 · LS 69,650 · corpus 106,738 tape (cl100k tier) · fixed point in 3 pass(es)`.
  `--check` then read **0.00% drift on all three**; after the stratum's own lines landed it reads
  **GM 1.19% · LS 0.00% · corpus 0.42%** against a 10% tolerance — declared, not re-taken (#241).
  ⛔ **No figure on that line was typed by this seat.**
- **`s214-D6`** — quoted from `_CHAIN.md`'s generated footer **after** the stamp: **10,480 tape =
  slice 9,627 + wrapper 853**, GM 37,088, **ratio 28%** against the generator's own `<40%` floor.
  **Inside the ~10–12K target and DOWN 1,189 on #293's 11,669.** A second regeneration after the
  banner-discipline line landed returned **10,480 again** — the stratum is outside the chain slice,
  so no post-stamp chain drift is owed this wrap.

## 6 · THE LOCK DANCE — AND THE BOOT NOTE IS NOW WRONG IN A NAMEABLE WAY

A stale **0-byte `.git/index.lock` (18:35), stranded by this seat's OWN `git status`**, was found at
**18:53 — eighteen minutes old** — and was **`mv`'d on-device into
`notes/_lanes/_orphan-locks/stale-index.lock-294-wrap-1853`, NEVER `rm`'d** [[git-lock-mv-not-rm]].
It is the fifth such file that directory took today.

⛔★★ **AND THEN `git reset -q` — THE VERY COMMAND THE #294 BOOT NOTE PRESCRIBES BEFORE STAGING —
STRANDED THREE MORE IN ONE CALL:** `.git/HEAD.lock`, `.git/refs/heads/master.lock` **and**
`.git/index.lock`, all 0 B, all 18:53, each with its own `unable to unlink … Operation not permitted`.

⇒ **The note *"a no-op `git add` on this mount strands a lock, so `git reset -q` before staging"* is
HALF WRONG: the remedy strands MORE locks than the thing it remedies, and it strands them on REFS as
well as on the index.** Corrected **by addition**; the original wording is not rewritten anywhere.

## 7 · ANOTHER WORKER'S LANES — TWO CARRIED, TWO LEFT IN FLIGHT

| path | last write | report | disposition |
|---|---|---|---|
| `notes/_lanes/293/J5/` + `…-293-J5-recall-proposal.md` | 16:51 / 16:52 | filed | **CARRIED**, doc row `W-293j5` |
| `notes/_lanes/293/J6/` + `…-293-J6-typesafe-docs.md` | 17:05 / 17:06 | filed | **CARRIED**, doc row `W-293j6` |
| `notes/_lanes/293/J7-IDEA-jev-selects-over-the-kg.md` | 18:25 | **none** | **LEFT, by declaration** |
| `notes/_lanes/293/IDEA-notice-board-for-seats.md` | 18:51 | **none** | **LEFT, by declaration** |

Both carried lanes were **stable for over ninety minutes** against the brief's fifteen-minute test,
on the **J3/J4 precedent**. ⛔ **In-flight is not a stray and its absence is not a drop (#70); a
later commit loses nothing.** `IDEA-notice-board-for-seats.md` appeared at **18:51, during this
ritual** — the other worker is still active.

⚠ **NEITHER CARRIED REPORT HELD ANY OF THE THREE `s218-D7` MACHINE-READ LINES**, so the `s294-D2`
arm — which reads the STAGED set only — would have refused the commit that carries them. The lines
were added at staging, **counted off each report's own text, derivation shown item by item, and each
addition says in the file that the #294 wrap made it**, on lane C2's own precedent from this session.
⛔ **No sentence of either lane's was altered, deleted or renumbered, and no question was put in
either author's mouth.** Verified with the gate's own `_lines_missing()`: `[]` for both.
**J5: findings 20 · ruling-shaped 2 · UNPROVEN 6. J6: findings 20 · ruling-shaped 6 · UNPROVEN 1.**

★ **And J6 carries the sharpest single finding of either lane, which this wrap flags because it
falsifies a figure four other lanes measure against:** the *"declared 70–500 ms latency band"* **does
not appear anywhere in TypeSafe's current documentation** — the only latency figure on the site is a
cookbook's own measured 114 ms. **Recorded as unverified, not as wrong.**

## 8 · THE FOUR STRIKES, AND WHAT WAS DELIBERATELY NOT STRUCK

`s183-D1` strike form with an `s188-D2` receipt on every one, **by addition, never a silent deletion**:

1. **#294 carry ①, the review page** — receipt `dd374131`, the page built and answered the same day.
2. **#294 carry ②, the five judgement items** — all five ruled AND enacted: `s294-D1` · `s294-D2` ·
   `s294-D3` · `s294-D6` · `s294-D4` · `s294-D8` · `s294-D5`; receipts `ea930e72` + `698d8ddf` +
   `f81bbdd4`.
3. **#294 carry ⑩, the Jev DECISION HALF only** — `s294-D10`, with his rider quoted verbatim. ⚠ The
   rest of that item, the lane's provenance correction and the fourth-surface lesson, **stands**.
4. **#294 carry ⑫, the pre-flight stamp** — `s294-D9`, with §2 above as the receipt.

⛔ **NOT STRUCK, and the precision is the point:** **carry ③** (*"dream pass 13's seven … not one of
them ruled"*) took a **BY-ADDITION correction** instead — P3's strong form IS now ruled as `s294-D8`,
but the other six remain enacted-and-unruled and **`s183-D1` strikes a headline, never a clause.**
**Carry ④** likewise: pass 9's P3 is ruled as `s294-D4`, P4 is still live, and the pass as a pass
still has no disposition ruling. ⚠ *"A strike that is wrong is worse than an item that is merely
stale"* (`s271-D4`).

**`_CARRIES.md` § `residual → #295`: 639 items, 13 new, 4 STRUCK with receipts.** 627 → 639 is
#294's twelve ageing in; **the 13 new are INVISIBLE to the probe, the 17th wrap running**
(`_AGE_RE` misses `[NEW — 0]`).

## 9 · THE BANNER CAP — EXACTLY 1,200 OF 1,200, FOR THE SECOND CONSECUTIVE WRAP

Drafted at **1,228 tape across 10 bullets**, shortened **nine** times with **two** bullets merged
away, landing at **exactly 1,200 of the `s241-D2` cap over 9 of 10 substantive lines** — measured by
the gate's own arm (`BANNER_LATEST_RE` → next `★ PRIOR`), not by eye, after one iteration in which
the gate read **1,201** to this seat's 1,200 because the region includes the trailing blank line.

⇒ **#292's and #293's complaint is not withdrawn and is now literal for a third session.** This wrap
could not give the **eight lanes' ruling-shaped question sets** a bullet of their own; they were
merged into `_CARRIES.md` ⑨ ⑩ ⑪ instead, and the `wrap-handover` line was dropped from the banner
(it stands in the ⏱ delta and the stratum). **Reported, not appealed — the cap is Dave's.**
[[gate-inside-the-growth-loop]]

## 10 · THE GATE — SEVEN IN, ALL INHERITED

Baseline before any write: **231 in scope · 7 fail · 367 warn.** The seven: the **boot-drift CEILING
BREACH** and **six boot double-counts** (#243 ×5, #264, #272, #273, #274, #287), every one another
session's append-only testimony. ⛔ **A wrap may not repair an inherited gate fail** — the `#243`
DECLARED NOT-A-WRAP path, **the twentieth consecutive wrap.**

Three fails were this ritual's own and each was closed **at the thing named**: the banner cap
(shortened), the retrieval index (2g), the title receipt (4b). **No gate was weakened, no tier moved,
no constant touched.**

⚠ **THE GATE ITSELF NAMED THE `s294-D2` HOLE, IN ITS OWN VOICE, ON THE PASSING PATH** — worth quoting
because it is the second owed item to #295 and it is not this seat's opinion:

> `FILED SUB-REPORT LINES: the check DID NOT RUN — the index is EMPTY, so the staged population is
> empty and this arm has measured NOTHING. ⛔ UNKNOWN, NEVER GREEN. ⚠ knowledge/_git_commit.sh runs
> _capture_gate.py --wrap at :646 and stages at :896, so from that seam the index is empty BY
> CONSTRUCTION and s294-D2's bite cannot land there.`

## RULING-SHAPED QUESTIONS

1. **Does the `s294-D2` arm get a consumer, and where?** The gate says in its own words that from
   the commit seam the index is empty **by construction**. Moving the gate past `:896`, or the
   staging before `:646`, changes `_git_commit.sh`'s *"Nothing has been staged"* refusal contract —
   which lane C2 already declared as Dave's call and which this wrap did not touch either.
2. **Who stamps `status: enacted` on a ruling, and with what evidence pointer?** Twelve are
   inscribed, eleven are enacted in code and **none is stamped** — correctly, because no lane stamps
   a ruling and this seat did not either. **The store therefore understates the day by eleven
   records.** `s294-D12` is a third state again: enacted in code AND discharged in fact, by his
   hands, which the field has no word for.
3. **Is a conductor-seat memory act a receipt under `s188-D2` in general?** #293's wrap struck an
   item on one and said the question was ruling-shaped; this wrap struck four items on **commit
   shas and ruling ids**, so it did not need the answer — and the question is therefore a session
   older without having been put to him.
4. **The eighth consecutive `consult-receipts: none` is a reading about the instrument, not about
   the wraps.** Every artefact eight successive rituals needed was named BY PATH in the brief, so
   there was no query to put. ⇒ **a retrieval surface a fully-briefed seat never needs has one
   consumer, the cold opener, and nothing measures whether it is used there**
   [[instrument-without-a-consumer]].
5. **`_gate_scratch_hygiene.py` reports 12M owned by dead sessions and can never remove it.** 4c's
   own contract says the wrap is the last moment cleanup is possible; this figure is the part that
   was already too late. **Whether that number is allowed to grow without a ceiling is his.**

## UNPROVEN AT THIS SEAT

1. **`python3 knowledge/_capture_gate.py --selftest` is UNASKED** — at this seat as at every other
   this session. It exceeded the sandbox call wall at 165 s at lane E1's seat (#293) and lane B's own
   run ends on five ❌ lines, **all inherited** from the `_governs.py` trigger-index arm. **CI is the
   first honest asking and it is behind the unpushed commits.**
2. **No CI run exists on any of the six shas and none is claimed.** `git ls-remote` was not consulted
   because there is nothing to compare.
3. **`replay`** — what reading this report back costs the conductor. `unobservable
   (conductor-side, post-wrap)`; the conductor may append the measured figure at #295's opener.

## REPLAY-THESE

```
python3 knowledge/_gate_scratch_hygiene.py                     # the s294-D12 arm, exit 0
python3 knowledge/_checkin.py --preflight-line 294             # the CAPTURED stamp
python3 knowledge/_gen_size_stamp.py --check                   # 3 figures, drift under 10%
python3 knowledge/_gen_chain.py --check                        # FRESH
python3 knowledge/_render_rulings.py --check                    # FRESH, sha256 9401d3c1…
python3 knowledge/_roll_state.py                                # 2c 2/2 · 2d 3/3 · 2f strata 1, log #293
python3 knowledge/_validate_standing_instructions.py            # 28 standing docs, PASS
python3 -c "import json; r=json.load(open('knowledge/_rulings.json'))['rulings']; print(len(r), [x['id'] for x in r[-12:]])"
PYTHONPATH=knowledge python3 -c "import _capture_gate as c;print(len(c._carry_items([l for l in open('_CARRIES.md') if '#295:**' in l][0])))"
python3 knowledge/_capture_gate.py --wrap                       # 231 in scope · 7 fail (all inherited)
```

---

## ⬛ POST-COMMIT ADDENDUM (5b) — THE SHA, THE PUSH VERDICT, THE CI READ

*(Appended after the commit, which is why this report is left MODIFIED in the working tree — a
commit cannot name itself, and the same shape lane C2's report took earlier today.)*

- **Wrap commit sha:** **`359b646b`** — `master`, 2026-09-21, **25 files changed, 3,991 insertions,
  1,097 deletions**, 8 new files. Subject as the script's T3 headline generated it (exactly one
  prefix, asserted by the #208 gate): `after #294 2026-09-21 — #294 — the twelve were ruled,
  inscribed and enacted in one day; the pre-flight stamp is captu…`
- ⛔★★ **AND THE SEQUENCE IS PUBLISHED RATHER THAN SMOOTHED: `--wrap` WAS ATTEMPTED FIRST AND THE
  GATE REFUSED IT, WHICH IS WHAT THE `#243` FORM ACTUALLY IS.** On `--wrap` the gate has its
  consumer (`#74-D1`) and **red BLOCKS**; the seven fails are inherited and **a wrap may not repair
  an inherited gate fail**, and there is **no ACK hatch for that arm** (unlike `DOC_ROW_ACK` /
  `SHOWROOM_ACK` / `POLARITY_ACK` / `MENTION_MAP_ACK` / `SESSION_ACK` / `PAT_EXPIRY_ACK`). So the
  commit was made on the **DECLARED NOT-A-WRAP path — the twentieth consecutive wrap** — where the
  gate's red is *"visible, not blocking"* in the script's own words, and the subject correctly takes
  the `after #N …` shape that `s294-D3`'s new `NONWRAP_COMMIT_SUBJECT_RE` now matches and its
  `WRAP_COMMIT_SUBJECT_RE` correctly does not. ⚠ **`SESSION_N=294` was required and paid**
  (`s130-D3`), as were three earlier refusals, each fixed at the thing named: `--reconciled` missing
  · `knowledge/_graph-mention-map.json` regenerated by the gate and **not staged, because the script
  never stages a path you did not name** (P5) · the session declaration. **Nothing was staged by any
  refused run.**
- ⚠ **FOUR DIRTY PATHS WERE DELIBERATELY NOT STAGED and the script said so** — the other worker's
  four unreported files (§7). **Declared, not dropped.**
- **Push verdict, verbatim:** *"not pushed — conductor's judgement."* Six commits are LOCAL:
  `dd374131` · `ea930e72` · `698d8ddf` · `e31aa34b` · `f81bbdd4` · `359b646b`. The script's own
  closing line: *"✓ done — locks clear, safe for Dave to push via GitHub Desktop."*
- **CI:** ⛔ **no run exists on any of the six and none is claimed.** The read-back job by job is the
  first line of what #295 owes, and it is **not routine**.
- **Locks at exit:** the lock dance is in §6. Every lock this seat met was **`mv`'d on-device into
  `notes/_lanes/_orphan-locks/`, never `rm`'d.**
- **`s271-D4` re-read:** run as the ritual's final beat, against `knowledge/_rulings.json`. The hook's
  open items were struck by addition where this session's own rulings closed them, each naming the
  ruling id.
- ⛔★★ **AND THE MACHINE HALF WAS BLIND WHEN FIRST ASKED — FOUND HERE, FIXED HERE, AND IT IS A
  SIXTEENTH FINDING THIS REPORT OWES ITS OWN COMMIT BODY.** `_capture_gate.hook_open_items_recheck`
  read **`0 "Open, Dave's" item(s)`** off the hook as first written, because `HOOK_OPEN_LABEL_RE`
  wants the label **and a colon within 80 characters on one line** and the hook's prose heading
  (`### OPEN, DAVE'S — every one a QUESTION PUT AT THE WRAP CALL, not a state of the world`) has no
  colon at all. ⇒ **the arm would have reported a clean run over an unread list** — the same shape as
  the 2c carry gate going quiet after `s225-D2`, and the same shape as lane G's ruling-shaped
  question 5 one step earlier (*a hook filed with no index line in its index-line section*). A
  machine-read line in the form the arm parses was added **beside** the numbered list, not in place
  of it, with the human list left as the authority on the bodies. **Re-asked: `10 items read · 10
  written as a QUESTION PUT and PASSING by the ruling's own form · 12 same-day rulings in the ledger
  · 0 items named`** (ADVISORY tier, `HOOK_OPEN_RECHECK_BLOCKING`). ⚠ **A wrap that had not asked
  the arm twice would have inherited the silence.** [[instrument-without-a-consumer]]
- ⛔★★★ **AND A BRAND-NEW STRUCTURAL CONSEQUENCE OF `s294-D1`, MEASURED AT THE 5b COMMIT AND OWED TO
  #295 BY NAME: EVERY COMMIT NOW STALES `_CHAIN.md` BY CONSTRUCTION.** Lane A's
  `build_verdict_line()` publishes a HEAD comparison — *"that run is at `e31aa34b`, HEAD is
  `f81bbdd4` — the verdict is that tree's, not this one's"* — and `_head_short()` reads live `git`.
  ⇒ **the moment the wrap commit lands, the chain it just committed is stale against the new HEAD**,
  and `_git_commit.sh` refuses the next commit on `_gen_chain.py --check` **before staging
  anything**. Measured here: `359b646b` staled the chain by exactly that one clause, the 5b run
  refused, the chain was regenerated (**10,480 → 10,479 tape**) and named in the 5b list. ⚠ **This is
  correct behaviour and an honest comparison — it is not a defect to be silenced** — but it means
  **a 5b commit must ALWAYS carry a regenerated `_CHAIN.md`, and a session's LAST commit will always
  leave the chain one sha behind.** ⛔ **Nobody wrote this down, because the ruling was one day old.**
  It joins lane I's *"a doc-row mint is what takes `_gen_chain.py --check` red"* as the second
  undocumented staler of the same file, found the same day. [[premise-ages-faster-than-rule]]
- **The 5b commit:** this addendum, the hook's machine-read line and the handoff's own addendum are
  carried in a **second commit** — a post-wrap-gate edit gets its own beat (`5b`), and nothing was
  back-dated or amended into `359b646b`.

---

## ⬛ SECOND POST-WRAP ADDENDUM (5b, part 2) — A HAND-UP ARRIVED AT 19:19 AND CHANGES THE §7 DISPOSITION BY ADDITION

⛔ **§7 above said the other worker's four unreported files were LEFT IN FLIGHT BY DECLARATION. That
was true when written and is now SUPERSEDED BY THEIR OWN AUTHOR'S HAND-UP, not by this seat's
judgement.** `notes/_lanes/293/_FOR-THE-WRAP-worker-seat-hand-up.md` appeared at **19:19**, two
minutes before this seat's next read, and says in its own first line that it was *"written the moment
Dave said the wrap is running"*. It **names every untracked path**, declares *"this seat has run NO
git commands and modified NO existing tracked file"*, and asks explicitly: *"Name each in the commit
body; do not sweep silently."*

⇒ **An explicit hand-up from the writing seat outranks a fifteen-minute mtime proxy**, which exists
only because a seat usually cannot say. All four idea notes and the hand-up itself are therefore
committed at ⬛ **SHA PENDING — filled from `git log` after the commit lands; no placeholder figure is written here, because a commit cannot name itself**, named one by one in that commit's body. ⛔ **The 15-minute test is not
weakened: it is what you use when there is no hand-up. Here there was one.**

**Two corrections the hand-up itself needs, measured at this seat:**

1. ⛔ **`knowledge/_jev-receipts.jsonl` IS NOT OUTSTANDING.** The hand-up says *"19 lines (tracked
   copy has 5); 14 appended by J4"*. Measured: `wc -l` reads **19** and `git show HEAD:<path> | wc
   -l` **also reads 19** — lane C's `dd374131` already carried J4's fourteen. **The claim was true
   when the worker seat last looked and was stale by the time it was written.** Nothing is owed on
   that path.
2. ✅ **`notes/_lanes/293/J4/__pycache__/` is CORRECTLY EXCLUDED** and needed no decision:
   `git check-ignore -v` returns `.gitignore:17:__pycache__/`. The hand-up's *"do not commit"* and
   the repo's own ignore rule agree.

★★ **AND THE HAND-UP CARRIES ELEVEN MORE OF DAVE'S VERBATIM LINES FROM THAT SEAT, WHICH NO #294
SURFACE HELD.** They are HIS words at the other lane, not this session's, and they are **not
paraphrased, not moved into `notes/_lanes/294/DAVE-RULINGS-2026-09-21.md`** — that file is #294's
conductor's window and these belong to #293's worker seat. ⬛ **Two of them bear directly on items
#295 owes and are flagged here rather than left in a hand-up nobody cites:**

- ⛔★★ ***"it's fine, i understand this much better now, I've switched off what I think we can
  ditch"*** — **HE SWITCHED BOOT FEATURES OFF AT THAT SEAT TODAY.** The note
  `notes/_lanes/293/NOTE-boot-has-three-bands.md` says the 22–28 August **+24k step is toggleable
  feature blocks**, and that **the NEXT COLD BOOT'S TURN-1 READING IS THE MEASUREMENT** (that seat
  read **74,220**; this seat's conductor read **74,656**). ⇒ **#295's opening boot figure is a
  MEASUREMENT OF HIS CHANGE, not another drift reading**, and it lands against a shrink-only ceiling
  of 70,000 that seven post-diet readings have already breached. **Read #295's boot number as
  evidence, and do not average it with the seven.**
- ⚠ ***"another thing, the wrap takes a long time to run... is this something we could work on in the
  future? any ideas"*** — answered at that seat as
  `notes/_lanes/293/IDEA-wrap-is-slow-five-levers.md`, and **its lever 3 is *"rule the seven
  inherited gate fails closed"* — a RULING, no build.** ⛔ **That is the same seven this wrap carried
  in the `#243` form for the twentieth consecutive time**, so the cost he noticed and the backlog
  this ritual declares are **the same object**, and the cheapest lever on it is his word.
- ⚠ **And the hand-up DISPUTES lane J2 on the record:** J6 *"disputes J2's 'jev-1.13.0 not
  requestable'"*, and its own author says **both stand until measured.** Neither is resolved here.
