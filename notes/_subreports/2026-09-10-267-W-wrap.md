# `#267`-`W` — the #267 capture ritual, delegated

session: `#267` · 2026-09-10
window: delegated WRAP sub (Opus 5)
sub index: `W`
brief: cut in chat by the conductor (Fable) at **≈185,000 real FILL** — **already past** the
`s260-D2` delegated line of 180,000, so this wrap is delegated on a BREACH rather than on a margin.
tokens: ESTIMATED ≈95K at this seat (no `message.usage` reading is available from inside a sub;
the figure is an estimate and is written as one).

provenance: 267 · 2026-09-10
status: observed

## VERDICT

**The ritual ran 1 → 5b in order and the gate closes with THREE blocking refusals, every one
declared in the #243 form and none routed around.** Steps 2c, 2d and 2f all RAN CLEAN — no
prior-session `size:` stamp is being carried — and the ★ LATEST banner is **1,191 tape across 10
substantive lines** against the `s241-D2` cap of 1,200/10, met over seven measured drafts. Step 3
(memory) is **0 bytes at this seat and DONE at the conductor's**, the second consecutive delegated
wrap where it is closed rather than declared a seat limit.

COUNTS: findings 4 · ruling-shaped 4 · UNPROVEN 3

REPLAY-THESE: none — the stub carries everything the conductor needs. The three ruling-shaped
questions are carried in `_CARRIES.md` § `residual → #268` and named on the ⏱ LATEST DELTA.

---

## The two title lines, verbatim (`_gen_titles.py`, receipt at `knowledge/_gen_titles_receipt.json`)

    RENAME THIS SESSION → `Apollo - #267: v1.0.10 released; 52/78 edges wrong; run-of-show exists`
    NEXT SESSION TITLE → `Apollo - #268: rehearse the run-of-show, v1.0.11 canon, the islands`

## The rolls — 2c / 2d / 2f, every move through `_gm_move.py`

Ops file: `notes/_lanes/267/W/ops-267-rolls.json`, written by `build_ops.py` from **that same
process**, with `os.path.exists` + a size floor asserted before the mover read it (the #166
stale-msgfile trap). Receipts read back against the ops:

| step | move | receipt |
|---|---|---|
| 2c | the **#265** ★ PRIOR banner, **11 ln** → `_GM-ARCHIVE.md` § Batch 2026-09-10 #267 | `✔ MOVE 11 ln` |
| 2d | the **#264** ⏱ PRIOR delta, **17 ln** → `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-10 #267 | `✔ MOVE 17 ln` |
| 2d | the **#264** `Previous:` chain segment, **6,032 chars**, verbatim into the same archive section | `lastrefreshed.py` |
| 2f | the **#266** stratum, **22 ln** → `notes/_GAUGE-LOG.md`; its commit state, **2 ln** → `_GM-ARCHIVE.md` | `✔ ROLL-2f #266` |
| 2e | **0 dated strata rolled** — nothing in DO-FIRST aged past LATEST+1 | declared, with its size |

`_roll_state.py` at close: `2c OK (banners 2/2) · 2d OK (deltas 3/3) · 2f OK (strata 1, log #266)`.

## FINDING 1 — one word broke the next seat's stop-line probe, for the SECOND wrap running

`_checkin.py`'s `CHAIN_STOP_RE` is `stop line\s*\**\s*([\d][\d,]*)`. This wrap's first draft of the
⏱ LATEST DELTA wrote *"past the `s260-D2` delegated stop line **of** 180,000"* — and the **"of"**
made the number unreachable. The wrap gate's **B2 PLAN BLOCK** check failed with
`STOP does not resolve from state — UNKNOWN, no 'stop line <N>' figure found in _CHAIN.md`, and the
repair was to write `**stop line 180,000**` instead. ★ **The chain is a machine-read surface as well
as a human-read one, and a wrap can break a probe with a preposition.** Fixed and re-verified by
running `_checkin.py`: `STOP: FILL 180,000 real — the stop line as stated in _CHAIN.md`.

⛔ **AND IT IS A RECURRENCE, NOT A FIRST — WHICH IS THE ACTUAL FINDING.** #266's own wrap commit
records the identical repair: *"the +2 is the four words that gave `CHAIN_STOP_RE` the literal
'stop line 180,000' it parses, repairing a B2-block refusal in the delta rather than routing
around it"* (`f33c67a`). Two consecutive wraps have written the delta's gauge sentence in prose,
broken the probe, and repaired it at the gate. ★ **Ruling-shaped, and cheap: the literal belongs
in a fixed generated line rather than in a sentence a wrap author re-writes every session** —
[[gate-inside-the-growth-loop]], with the growth being the prose. Carried, not built.

## FINDING 2 — the 2f roll MANUFACTURED a blocking refusal, and the repair is not an edit

`boot_stratum_double_count_check` scans **`notes/_GAUGE-LOG.md` only**. Rolling #266's stratum into
that file moved a line reading *"TWO BLOCKING GATE REFUSALS ARE CARRIED IN THE **#243** FORM"* —
with boot figures beside it — into the check's scope, and the check now reports **session #243
stating a first-turn figure twice** (lines 2826 and 3256). The identical shape hit **#266** one wrap
earlier with **#264** (lines 3206 / 3208), which #266 declared as inherited rather than editing.

⛔ **NOT REPAIRED, and the reason is the rule rather than convenience.** A rolled stratum is verbatim
record (`s183-D1`); quieting a gate by rewriting archived text is the failure mode this project has
paid for three times (#49 / #51 / #153). ⇒ declared, named for the next seat, left for Dave.

★ **The mechanism, stated so it is not re-diagnosed:** the check's population is a file that step 2f
keeps ADDING prose to, so **every wrap that names an old session beside a boot figure manufactures a
fresh refusal**. The SCOPE wants fixing, not the strata. Ruling-shaped, below.

## FINDING 3 — eight documents had no store row, and one of them was this report

`_gate_doc_rows.py --check` at this wrap's opener: **6 UNROWED** (four #267 briefs, two #267 filed
reports), plus an untracked seventh brief that this wrap's own commit makes tracked, plus this
report. All eight rowed as `W-267a` … `W-267g`, `W-267w` through `_state.add()` — never by hand —
each with a real close condition. `--check` now: **`✅ PASS — every in-scope document has a store
row`** (population 414, unrowed 0). ⚠ Note the shape: `_state.add()` **refused** `W-267w` until this
file existed, which is the store's home-resolution guard doing exactly its job.

## FINDING 4 — the brief said nine lanes and named ten

The wrap brief opens *"conductor Fable, 9 lanes"* and then lists **C6, P, P2, R, K, E, G, S, N, F**
— ten. The roster is the measurement and the count was a miscount; the record says **TEN LANES**
with the discrepancy named rather than either number quietly preferred.

## Sizes, measured — never estimated (`tiktoken cl100k_base`)

| object | #266 | **#267** | Δ |
|---|---|---|---|
| `GOOD-MORNING.md` | 34,495 | **34,267** | **−228** |
| `_LIVE-STATE.md` | 63,472 | **66,771** | **+3,299** |
| corpus GM+LS | 97,967 | **101,038** | +3,071 |
| `_CHAIN.md` (`s214-D6`) | 9,860 | **10,700** | **+840** |
| ★ LATEST banner (`s241-D2` cap 1,200 / 10 lines) | 1,200 | **1,191 / 10 lines** | −9 |
| §A slice `What Apollo is` → `1. ★ NEXT STRANDS` | 3,764 tape / 14,132 B | **identical**, sha `b9aef5f5673225f7…` | **byte-identical for a 30th consecutive wrap, PROVEN by string comparison** |

⚠ **The banner FELL and the chain still ROSE** — the #240 lesson, re-taken: `s241-D2` makes the ⏱
LATEST DELTA the sole home for a ten-lane day's gauge, declared skips and not-done detail, and the
delta is inside the chain slice. chain/GM ratio **31.2%** against the `<40%` floor: green.

## Gauge

⛔ **FILL is a breach and is written as one.** Conductor DECLARED **≈185,000 real** at the wrap call
(already past `s260-D2`'s 180,000); first-hand at this seat `_checkin.py` reads **201,554 real over
75 turns** — past the **200,000 working wall** as well. Both stand; neither is averaged.

⛔ **BOOT 70,717 real, measured first-hand here — 717 OVER the 70,000 shrink-only literal and the
THIRD CONSECUTIVE breach: #265 70,260 · #266 70,442 · #267 70,717, a RISING series.**
⚠ **The brief declared boot unmeasured at this seat.** That was true of the *conductor's* seat, whose
opener check-in did not print it, and is **not** true of this one — `_checkin.py` prints boot
first-hand. A number that can be taken is taken rather than declared absent.

⛔ **NO `subs` LINE.** The only figure available is the brief's aggregate *"sub tokens ≈1.4M"* across
ten lanes; no per-lane `message.usage` was captured and lane E's own report states its ≈115K as an
estimate. `subs 1,400,000 (n=10)` would inscribe an approximation in the one field whose contract is
that it is measured [[feedback-measuring-tool-must-not-guess]] ⇒ absent, with the approximation
recorded as an approximation. **The largest delegation day on the board produced no datapoint.**

`wrap-handover` (`s214-D5`): brief-cut **≈185,000** (declared approximate) · sub-cut **201,554**
(first-hand, 75 turns) · delta **≈16,554** · replay `unobservable (conductor-side, post-wrap)`.

## Gate result

`python3 knowledge/_capture_gate.py --wrap` → **206 in scope · 3 fail · 19 warn**, and **all 19
warns are UNCHANGED since the wrap-open record** (no new warn was introduced by this wrap).
The three fails, each in the #243 form:

1. `boot-drift CEILING BREACH` — 70,000 literal, shrink-only by `s240-D2`/`s241-D1`; Dave's word
   alone. **Twenty-first wrap running.**
2. `boot double-count #264` — **INHERITED**, declared in these same words at #266.
3. `boot double-count #243` — **NEW, and created by this wrap's own 2f roll** (FINDING 2).

## Declared skips, each with its size

- **2e — 0 dated strata rolled.** `## ⬛ DO THIS FIRST` measures **10,140 real by `_gm_usage.py`**
  and **7,120 tape / 109 raw lines** by this seat's own slice — two slicers, two numbers, neither
  converted into the other. Inside the 120-warn / 180-block band; section UNCHANGED by this wrap.
- **`_build_all.py`** — NOT RUN, fenced by the brief.
- **`gen_kg_edges.py`** — NOT RUN, fenced by #265's measured 441-line destructive diff **and** now
  by the `supersedesClause` seam `s267-D3` opened.
- **The render gates** (`_validate_screen.py`, `_validate_fit_physics.py`) — NOT RUN. A chromium
  drive in this sandbox now needs `PLAYWRIGHT_BROWSERS_PATH=/tmp/pw` **and** `NODE_EXTRA_CA_CERTS=…`
  because `/sessions` is **98% full**; that recipe was not exercised here.
- **`knowledge/_tests/test_gates.py`** — SKIPPED, copytrees ≈5 GB, ENOSPC.
- **Step 4c ran and cleaned this seat's `/tmp`** at VM fill **95.5%** — ⚠ including `/tmp/pw` if it
  was present, so the next chromium drive re-downloads. The wrap is the last moment that cleanup is
  possible; a skipped 4c is a permanent squatter.

## Carries

`_CARRIES.md` § `## residual → #268`, written by `notes/_lanes/267/W/carry268.py`:
**552 age brackets bumped** (9 of them `NEW — 0` → `1`), **8 new items**, **1 carry STRUCK WHOLE**
with its `s183-D1`/`s188-D2` receipt — the #267 lead, all three of whose items were discharged the
same day and none of them where the carry expected. Probe count **403 → 412**
(`_capture_gate._carry_items`, the repo's own definition, run rather than typed).

## RULING-SHAPED QUESTIONS

1. ⬛ **The boot double-count check is scoped to a file that 2f keeps growing.** Two of this wrap's
   three blocking refusals are artefacts of the ritual's own roll, not of any session's testimony,
   and a third will be manufactured at the next wrap by this session's own stratum (which contains
   the phrase *"#243 form"* beside boot figures). **Is the check re-scoped to `post-mortem #N:`
   lines only, or is the phrase "the #243 form" retired from wrap vocabulary?** Reported, not
   repaired — editing rolled record is the fix this project has already been burned by.
2. ⬛ **The boot ceiling has been breached three sessions running and the series is RISING**
   (70,260 · 70,442 · 70,717). The literal is shrink-only and the remedy is to cut the boot, which
   no wrap can do. **Is a boot-reduction lane owed at #268, or does the ceiling move on his word?**
3. ⬛ **`CHAIN_STOP_RE`'s literal lives in hand-written prose and has now broken twice in a row**
   (#266's commit `f33c67a` records the same repair). **Does the stop line move into a generated
   line of the chain, or does the wrap keep re-typing it?** See FINDING 1.
4. ⬛ **The largest delegation day on record produced no `subs` datapoint.** `s214-D4`'s re-base is
   staged on n≥2 and did not advance. **Does the conductor capture each lane's `usage` at reconcile,
   or is the field retired?**

## UNPROVEN from this seat

- That the ★ LATEST banner's compression lost nothing. The `s241-D2` cap is enforced on tape and
  lines; **whether a shorter banner dropped a declaration is not machine-checkable** and the gate
  says so itself. Every declared skip, refusal and RED on the delta is also named on the banner —
  asserted by the author, not proven by a tool.
- That `notes/_RUN-OF-SHOW-…-v1.html` and `notes/_DEMO-EVIDENCE-…-v1.html` render correctly. Neither
  was opened in a browser at this seat (no chromium; see the declared skips).
- That the memory hook the conductor wrote is accurate. The memory store is invisible to every gate
  (step 3's honest scope) and **0 bytes were written to it from this seat**.
