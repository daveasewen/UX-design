# `#291`-`W` — the delegated capture ritual for #291 → #292

provenance: 291 · 2026-09-21
status: observed

session: `#291` · ⛔ **DATE SPLIT, THE `#241` SHAPE BY ADDITION** — the session opened Sunday 2026-09-20 in the morning and all nine lanes and BOTH commits landed that day; **the push and this entire ritual are Monday 2026-09-21**, `date` at this seat reading `Mon Sep 21 08:29:29 BST 2026`. **Nothing was re-dated**; this wrap's own stamps carry today, and the report stem is `2026-09-21-291-W-wrap.md`.
window: conductor **Fable 5.1**, this seat a delegated **OPUS 5** wrap sub · sub index: `W` · brief: `notes/_lanes/291/WRAP-BRIEF.md` — every figure it DECLARES is re-measured here, and where a measurement disagrees **both readings are published and neither is rewritten**
tokens: `UNMEASURED — this seat cannot read its own message.usage as a final total; the FILL figures below are the CONDUCTOR'S window, measured first-hand against his transcript, and are a different object`

## VERDICT

**DONE — every step of `knowledge/_RUNBOOK-capture-ritual.md` ran, in order: 1 · 1b · 2 · 2c · 2c(i) · 2c(ii) · 2d · 2e · 2f · 2g · 3 · 4 · 4b · 4c · 4d · 5 · 5b.**
⛔★★★ **NO RULING WAS INSCRIBED AND `knowledge/_rulings.json` STAYS AT 622** — `json.load` over the `rulings` list at this seat, no `s291-` id, newest three still `s283-D1` · `s287-D1` · `s287-D2`. He did not say *"inscribe"*, so every ruling-shaped thing carries as a QUESTION PUT (`s271-D4`).
★★★ **THE SESSION'S HEADLINE IS THAT HE RULED ONE DRAWING BY EYE SIX TIMES IN ONE DAY, AND THAT THE SEAM CHECK WAS OVERRIDDEN ONCE BEFORE IT WAS OBEYED** — the third firing on record and the FIRST human override.
✅ **ONE CARRY STRUCK WITH ITS RECEIPT — the one the brief named, and no other.**
⛔ **SEVEN blocking gate fails stood at the OPEN, all inherited, carried in the `#243` DECLARED not-a-wrap form — the SEVENTEENTH wrap running; no eighth was born by the mandated 2f roll.** ✅ **NINE lanes, all Opus, NONE in seat.**
COUNTS: findings `11` · ruling-shaped `5` · UNPROVEN `4`

## WHAT LANDED

**Step 1 — `_LIVE-STATE.md`.** `Last refreshed` re-stamped from `date` (2026-09-21, Mon) with the **DATE SPLIT declared**; #290's stamp demoted to `Previous:`; **#287's `Previous:` chain segment MOVED verbatim (3,974 B)** to `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-21 #291, a trim note left in its place. The slice is located by marker SEARCH and never re-typed; post-conditions assert it is absent from the projection and present in the archive text, and every pre-condition tests the INPUT (the #285 lesson). A 37-line ⏱ LATEST DELTA #291 written. `_validate_standing_instructions.py` **PASS** (28 standing docs). ✅ **`knowledge/_REVIEW-SIGNOFF.md` gained ONE row — deck v12 and the line-workers v6** — inserted through the mover before the v11 row, because #291 leaves one artefact awaiting his eye and step 1's register clause puts its PATH in the durable queue, not only on a banner.

**Step 1b — dossier.** `_DECISION-HISTORY/2026-09-21-291-six-versions-by-his-eye-and-the-instrument-overridden-once-before-it-was-obeyed.md`, provenance + status, both-way links. Nine sections; §1 is about #290 and #291 being a matched pair on the delegation question, §4 about two of Dave's own instructions disagreeing about one cell, §5 about an advisory instrument behaving like one, §8 about where this wrap's own honesty was tested.

**Steps 2 / 2c / 2d / 2f — every move through `knowledge/_gm_move.py`,** from session-owned, uniquely-named ops files asserted with `os.path.exists` + a size floor **in the writing process** (`notes/_lanes/291/W/run_ops.py`, which REFUSES an ops file that is not session-owned), **dry-run first**, receipts read back against the ops meant to be run: `ops-291-ls-*.json` (**95,697 B / 5 ops**) · `ops-291-signoff-*.json` (**3,538 B / 1 op**) · `ops-291-gm-*.json` (**19,019 B / 7 ops**) · `ops-291-gen-*.json` (**379 B / 1 op**) · `ops-291-headline-*.json` (**1,503 B / 2 ops**) · `ops-291-tophead-*.json` (**1,416,430 B / 2 ops**) · `ops-291-title-*.json` (**307 B / 1 op**). `_roll_state.py` at the close: **2c OK (banners 2/2) · 2d OK (deltas 3/3) · 2f OK (strata 1, log #290)** — and the generator's line was substituted **BYTE-IDENTICAL** after the rolls landed, never predicted. `roll_2f` was driven with the session-qualified `> **COMMIT STATE #290:**` anchor, because the bare form matches four lines in this file and the mover refuses it, correctly.

**2c EXIT CHECK.** The rolling ★ PRIOR (#289) banner was scanned for ⚠/⬛/AWAITING items before the roll rather than after: its two ⬛ lines are its own `residual → #290` pointer and an OWED clause naming the catalogue page, proposal v3 and v11's four D2 flags — **every body probed by string match in `_CARRIES.md` § `residual → #292` and found standing.** Nothing needed copying up. **The 2d EXIT CHECK was run the same way** over the rolling #288 ⏱ delta: its two flagged items (the composition verdict and the 5b-versus-cap conflict) both stand in the carry set, probed the same way. **2c(i) — `_CARRIES.md` § `residual → #292`,** written by `notes/_lanes/291/W/carry292.py`: every age bracket bumped, **SIX** new items, **ONE STRIKE naming its receipt** (`s183-D1` strike form, `s188-D2` receipt). Probe **605 → 611**; the banner's PROBE command agrees at 611. ⚠ **All six new items are invisible to the probe** (`_AGE_RE` misses `[NEW — 0]`), the FOURTEENTH wrap running.

**2e — NO-OP, 0 lines.** DO-FIRST minted no notice, tombstone or lifecycle tag; nothing was retro-tagged. **2g — `_build_memento_index.py` run LAST** (2,272 records), after every GM/LS edit was final and after `_CHAIN.md` was regenerated to FRESH at **10,792 tape** (slice 9,939 + the generator's fixed 853 wrapper), ratio **30%** against the generator's `<40%` floor.

**Step 4 — NO ruling to record.** **4b —** both title lines GENERATED by `python3 knowledge/_gen_titles.py --session 291`, receipt at `knowledge/_gen_titles_receipt.json`; the forward title written into `GOOD-MORNING.md` **through the mover and COPIED out of the receipt rather than retyped**. ⚠ **The generator REFUSED the first RENAME at 123 tape against its 120 cap and named the remedy** — *"shorten the source headline, never truncate blindly at generation time"* — so **the banner headline was shortened through the mover and the generator re-run**, and the same was done for the top carry headline when the NEXT-TITLE trimmed mid-clause. **4c —** `_gate_scratch_hygiene.py --clean --wrap`, **ten entries removed**, VM fill 55.3%. ⚠ **It removed `~/.local/lib` and with it `tiktoken`, which every size measurement in this ritual needs** — re-installed at this seat, the second consecutive wrap to meet and name the same ordering. **4d —** `_render_rulings.py --check` **FRESH** (nothing staled it: no ruling was inscribed).

## Findings

1. ★★★ **HE RULED ONE DRAWING BY EYE SIX TIMES IN ONE DAY, AND THE SIXTH IS ON THE DECK WITH AN *"ALMOST"* STILL ATTACHED.** Six consecutive Opus lanes (L → L6), each cut on one of his notes and each answered with a rest render and an orbit render while he waited. ⛔ **An ACCEPTANCE BY EYE is not an inscription**, and the unspecified remainder in *"okay almost"* was neither itemised nor guessed at.
2. ★★ **"THE WORKERS LOOK LIKE ROBOTS" NAMED A CONSTRUCTION METHOD, NOT A RENDERING DEFECT.** His four reference images were described in his own sentence as *"human bodies drawn with a construction method"*, and the lane rebuilt the figures that way rather than tuning what was there. ★ *The same lesson #290 learned on a page mirrored rather than rotated, in a second venue — the defect the eye reports and the defect in the construction are different objects.*
3. ★★ **DECK v12 IS THE WORKERS SWAP PLUS SLIDE 10, MEASURED RATHER THAN DECLARED.** v11 and v12 both carry **12 numbered slides `s1…s12` inside 13 `section.slide` blocks** at this seat; s4 `#gb` → `#lw` + `#lwPrint`, s5's robots `#ln` and s6's catalogue `#ct` untouched, the gearbox displaced to an off-screen `#gbHost`. ⛔ **The four D2 flags are untouched and the dark robots slide is not back on dark — all gated on his slide-by-slide read.**
4. ⚠★★ **TWO OF HIS OWN INSTRUCTIONS DISAGREE ABOUT ONE CELL, AND BOTH ARE PUBLISHED.** #290's written *"and the parts image on, slide 10"* put the CATALOGUE in 01 Parts and v11 shipped it that way; #291's screenshot showed the GEARBOX and v12 ships that (`MAP` = `an1→gbPrint`, verified here). **The wrap did not resolve it** — which he meant is his.
5. ⚠★ **`amPrint` — THE ARM — NOW BAKES INTO NOTHING.** Slide 10 lost Tools and Process with their cells, so the arm drawing has no consumer anywhere in the deck, counted at this seat against `ctPrint`'s four. **An [[instrument-without-a-consumer]] living in the deck instead of in the tooling.**
6. ★★★ **THE SEAM CHECK FIRED, WAS OVERRIDDEN ONCE, AND THEN OBEYED — THE FIRST HUMAN OVERRIDE ON RECORD.** **FILL 220,302 / 42 · OUTSIDE 220,000 — wrap** was quoted to him; **his answer was another note**, the conductor said "one more, not wrap yet" in chat, lane P3 ran, and the wrap came on *"okay push and wrup"*. ⇒ **#283 and #290 obeyed it; #291 is the third firing and the first override.** ⛔ **This is evidence about the tolerance arm, NOT an argument for or against making it BLOCKING — a blocking arm would have refused a lane Dave asked for, and whether that is what he wants is his.**
7. ⚠★★ **THE BRIEF NAMED A TRANSCRIPT THAT IS A PREFIX OF THE SESSION, AND THE WINDOW WAS IDENTIFIED RATHER THAN ACCEPTED.** `173634f1-…jsonl` opens on the same *"Good Morning!"*, reads the same boot **to the token**, and stops after **16 turns at FILL 163,756**; the live continuation `274c73c8-…jsonl` opens two minutes later and carries all **76**. **Both readings are published**, and the `delta` subtraction is taken against the window that carries the whole session.
8. ⚙★ **THE `subs` FIGURE WAS MEASURED AND AGREES WITH THE DECLARATION TO WITHIN 0.5%, AND THE TWO ARE STILL NOT MERGED.** **1,413,300 (n=9)** here — L 194,835 · L2 205,181 · L3 213,701 · L4 170,098 · L5 161,330 · L6 164,290 · P 111,851 · P2 101,877 · P3 90,137 — against **≈1,420,000 (n=9)** declared from the harness usage lines. ⚠ **A sum of final-FILL readings is not the same object as a sum of cumulative tokens billed** [[measure-dont-convert-units]], and the agreement is a reason to trust both, not to average them.
9. ⛔★★ **THE MOUNT DEFECT BECAME TOTAL AND THE PREVENTION IS AN ORDERING RULE, NOT A DISCOVERY.** Every git write stranded an un-unlinkable `.git/index.lock`, so the commit script's per-path `git add` loop failed on its second path; **three refusals were paid before one clean run** (REUSED-MSGFILE with his own T3 prefix on line 1 for a THIRD session, the doc-row gate on eight unrowed reports, then the lock). ✅ **`mcp__cowork__allow_cowork_file_delete` on `.git/index.lock` fixed it — and it was called AFTER the third refusal rather than BEFORE the first git write.** ⛔ **Runbook step 0: call the grant at the FIRST commit.** The grant is per-session and does not survive into the next window.
10. ✅★★ **NO EIGHTH GATE FAIL WAS BORN BY THE MANDATED 2f ROLL, AND THE PREDICTION WAS TESTED RATHER THAN TRUSTED.** `_parse_boot_samples` run over the exact text the roll would append returned **ONE row for #290, not three**, and the post-roll gate still reads **seven**. ⚠ **AND THE SAME SLIDING-WINDOW READING AS #290 FELL OUT AGAIN: the CEILING BREACH list reads SEVEN before and after, because #280 fell out of it as #290 came in.** Declared rather than left to be re-discovered a third time.
11. ⚠★ **THE GATE READ NINE FAILS AT THE OPEN, NOT SEVEN, AND THE DIFFERENCE IS NAMED.** Seven are inherited and carry in the `#243` form; **two were this ritual's own** — `_LIVE-STATE.md` and `GOOD-MORNING.md` not yet carrying 2026-09-21 — and steps 1 and 2 closed them. ★ *Publishing "seven inherited" without the nine would have been true and misleading at the same time.*

## RULING-SHAPED QUESTIONS
Every one is a **question put**, never a state of the world (`s271-D4`). Bodies and receipts are in `_CARRIES.md` § `residual → #292`; **none was ruled here, and the rulings store did not move.**

1. **Six acceptances by eye and an *"almost"*** — whether a drawing accepted by eye is ever inscribed, and what the remaining *"almost"* is. **Nobody asked him, and this wrap did not guess.**
2. **Parts = the gearbox on slide 10, read from his screenshot, against #290's word that put the catalogue there.** The catalogue stays on s6. His.
3. **Slide 10 is three cells and the arm drawing has no home in the deck.** His.
4. **The seam check overridden once by a note and then obeyed** — advisory versus blocking on `s283-D1`'s tolerance arm. His.
5. **Slide 4's copy was written against the gearbox and now sits beside the workers** — it reads fine to the conductor's eye, which is a reading and not a ruling. His.

*(Also carried, unchanged and not re-minted: his slide-by-slide read of v12 and the four D2 flags · proposal v3 · the week's plan and the internal Friday the 25th, FOUR days out · the chart-engine re-drive and the inherited release red, FOUR sessions old · index sharding · the memory archive's 162 B · the in-seat delegation exception from #290 · `s218-D7` clause 4 · `_to_delete/` on a persistent disk · the two-probe-shape question on the §A digest, its twenty-third session · the `subs` definition · the store's id regex · the `#243` not-a-wrap form · the 5b-versus-cap conflict · `_HANDOFF-130`…`-141`'s open items.)*

## Step 5 / 5b — the commit, the push, the CI read

Recorded in full in the ★ LATEST banner's `COMMIT STATE #291` block and summarised in the returned stub. ⛔ **The hash is a declared gap in that block — a commit cannot name itself** — and the sha, the push verdict and the CI read are quoted literally in the addendum below and in the ⏱ LATEST DELTA's 5b line. ⬛ **5b, the addendum, is homed in the ⏱ LATEST DELTA of `_LIVE-STATE.md`, not on the banner** — the `s241-D2` cap is BLOCKING at 10 lines / 1,200 tape and the banner closed at **1,179 / 10**, so an eleventh line would be refused. ⛔ **The 5b-vs-cap conflict is ruling-shaped, Dave's, and carried — the ELEVENTH wrap to meet it and the TENTH to answer it this way.**

## UNPROVEN / CLAIMED (ADR-0016)

- **CLAIMED, not measured: the `replay` term** of the `wrap-handover` line — unobservable from inside the sub. **`brief-cut`, `sub-cut` and `delta` are all measured**, and the boots agreeing to the token is what makes the subtraction legal.
- **The memory-archive ceiling was RE-READ at this seat but the refusal was still DERIVED**, for a sixth consecutive wrap: arithmetic over the store listing's byte count and the documented cap, not a rejected write; the write that would prove it is named and un-run.
- **The conductor's boot reading and his two seam readings are DECLARATIONS, not readings this seat can re-take** — no `_checkin.py --no-block` output was captured to a file. What this seat measured first-hand is the window's FILL, turns, peak, boot, compactions and drops, and the nine lanes' own FILL.
- **The rendered figures for slide 10 are LANE P3's browser readings, cited to its report** (image box 340×135 → 340×229.5 CSS px, cell 330 → 424px, drawings 52% → ~88% of the cell width). **This seat verified the CSS in the file** — `clamp(150px,25.5vh,240px)`, `object-fit:contain` — **and did not open a browser.** The same holds for the declared "12 slides / 13 sections at every step, 0 page errors": the shape was verified in the file, the page-error count is the lanes'.

## Evidence

`notes/_lanes/291/W/` — the ops files, the builders (`carry292.py`, `build_ls_ops.py`, `build_gm_ops.py`, `build_title_ops.py`, `fill291.py`, `measure_banner.py`, `run_ops.py`, `rows291.py`) and the drafts (`delta291.md`, `stamp291.txt`, `banner291.md`, `pointer291.md`, `stratum291.md`) — and `notes/_lanes/291/WRAP-MEMORY-HOOK.md`, whose foot records what actually happened at the store.
REPLAY-THESE: `_HANDOFF-142-six-versions-by-his-eye-and-the-instrument-overridden-once-before-it-was-obeyed.md` (~5k tk) · `notes/_lanes/291/DAVE-RULINGS-2026-09-20.md` (~1k tk — his eleven entries, verbatim)

### THE ADDENDUM — sha, push verdict, CI
> **WRAP COMMIT:** `753d48a100b928ea14091ada8872da9924673eff` — the session's THIRD commit, after the conductor's `b9ab75b0` and `b721144a`. ⚠ **THE SCRIPT REFUSED ONCE FIRST AND THE REFUSAL WAS CORRECT:** MENTION-MAP (`knowledge/_graph-mention-map.json` stale, regenerated by the script, which then refused to stage a path this seat had not named — P5) — **#288's, #289's and #290's exact trap, met a FOURTH time and paid the same way: the path appended, a FRESH msgfile with a FRESH name, a clean run, exit 0.** ✅ **`.git/index.lock` was clear at the open and no lock was stranded at this seat** — the per-session delete grant the conductor called was still active, and `rm -rf` on the lane's `__pycache__` worked without incident.
> **PUSH VERDICT, verbatim:** `✅ pushed and VERIFIED: remote master == local 753d48a100b928ea14091ada8872da9924673eff` — independently re-verified by `git ls-remote origin HEAD`, range `b721144a..753d48a1`. ⛔ **The conductor's own two commits were ALREADY pushed when this ritual opened** (`git ls-remote` read `b721144a` at the open, matching the brief's declaration), so this push carries the wrap alone.
> ⚠ **AND THE `s214-D6` CHAIN FIGURE HAS TWO READINGS, BOTH PUBLISHED — THE #240 LESSON MET AGAIN AND DECLARED RATHER THAN DISCOVERED.** The stratum carries **10,792 tape**, taken after the declare-last `size:` stamp and before 5b, which is what the #241 rule prescribes. **This addendum lands INSIDE the ⏱ LATEST DELTA, which IS the chain slice, and the regenerated chain reads 11,500 tape (slice 10,648 + the 852 wrapper) — +708.** ⇒ **The figure the next session actually pays is 11,500; the stratum's 10,792 is the reading the ritual's own ordering produces. Neither is rewritten** [[measure-dont-convert-units]]. ✅ **Both are inside the `s214-D6` ~10–12K target** and the ratio stays under the generator's `<40%` floor.
> **CI:** run `35575122198` on `head_sha=753d48a1` — **RUN completed / failure**, read job by job over the public API and **polled to completion**: ⛔ **gates FAILURE (steps 5 and 6, the SAME TWO inherited)** · ✅ **render SUCCESS** · ⛔ **release FAILURE at step 12 `Build-script selftest — the release refusals, driven (BLOCKING)`**. ⛔ **Every red is INHERITED and none was born here:** the CI on the conductor's own `b721144a` was ALSO read at this seat, polled to completion, and returned the **identical three colours** (run `35573076214`), as did #289's `77a77eea` and #290's `7e0227fd`. **The release red was reproduced at #288 as stale chart receipts with the remedy named (`python3 knowledge/_drive_chart_engine.py`) and declared #289's first mechanical job — no lane has taken it since, so it is FOUR SESSIONS OLD and is #292's.** ⚠ **`render` stayed `in_progress` for roughly ten minutes after `gates` and `release` had both closed** (the BLOCKING state-contrast sweep); the verdict above is the one taken after it finished, not the one available first.

> ⚠ **AND THE 5b COMMIT'S OWN CI WAS READ TOO, POLLED TO COMPLETION, RATHER THAN LEFT AS A LOOSE END: run `35576701477` on `head_sha=51a46209` — RUN completed / failure, the IDENTICAL three colours job by job (gates FAILURE steps 5 and 6 · render SUCCESS · release FAILURE step 12).** That is **four** shas now read at this seat carrying exactly the same reds — `b721144a`, `753d48a1`, `51a46209` here, against #290's `7e0227fd` — which is what makes "inherited" a measurement rather than an assumption. ⛔ **THE RECURSION TERMINATES HERE AND THE TERMINATION IS DECLARED: this line is itself committed, and a commit cannot read its own CI.** The last commit of this wrap therefore ships with its CI UNREAD BY CONSTRUCTION — named, never implied — and the next session can read it from `git log` in one call.
