# D2 · deck v10 — three more drawings in, the five-up anatomy — #289 — 2026-09-20

Brief: `notes/_lanes/289/deck/BRIEF-v10.md`. Source: `..._2026-09-20-v9.html` (untouched). Output:
`notes/_DEMO-SLIDES-apollo-2026-09-20-v10.html` — 12 slides. No commit.

## What moved
- **s5 · The robots** was a grey copy-only card; it is now the drawing layout, white, type left and the LINE SCENE
  right — `notes/_lanes/289/illustration/line.html` ported as `#ln` + `#lnPrint`. Its copy is v9's word for word; only
  the h2's inline clamp and the body's inline width go, so the shared card rules own the type.
- **s8 · The inspector** was dark; it is now the same layout with the CALLIPERS, `illustration/callipers.html` ported
  as `#cp` + `#cpPrint`. The `d = NN.NN mm` caption and its scale bar are dropped (`caption()` left defined and
  uncalled, as v9 did for the books); the body line's `color:var(--g3)` goes with the dark ground.
- **The drawing-card rule set** — ground, `.inner` grid, `.type`, the scoped `h2`/`.lead`/`.body`/`.foot`, `.draw`,
  `canvas`, the print `<img>`, the 900 px breakpoint, the print block — was `#s4,#s6,#s7` and is now
  `#s4,#s5,#s6,#s7,#s8`; nothing in it changed but the selector list.
- **s2 · The strip** is cut from eight titles in 4×2 to Dave's five in one row: Why now · The line · The robots ·
  What we built (the inventory · the assembly engineer · the inspector, as the existing `b em` sub-line) · The build
  and the ask. One line of CSS: `repeat(4,1fr)` → `repeat(5,1fr)`. The stepped-down numerals stay.
- **s10** becomes the five-up anatomy: h2 "Five things she is made of.", five cells, a baked drawing over each label,
  the brief's foot. `.grid5` is six lines extending `.grid4`; the G3 lane block's `#s10 .grid4` override goes from
  three columns to five. The three-role copy is gone; the three roles survive in the foot.

## The five baked images — no fallback needed
Baked on load, the brief's first option. Nothing is re-drawn for §10: every drawing IIFE already renders its
resting frame at init and bakes it to a print `<img>` at `setTimeout(snap, 1200)` — the gearbox's pattern, which
v9 gave the books and the brain. v10 adds the same to the line and the callipers, ports the arm as `#am` in an
off-screen `#amHost` (fixed, 600×420, outside every slide so no overflow probe sees it, `display:none` in print)
with **no observer and no loop** — it draws once purely to be baked — then one small poller copies the five data-URLs
into `#an1…#an5`. `.grid5 .pic` sizes them to ~200 px wide with `mix-blend-mode:multiply`, dropping the white canvas
ground over the grey card. Confirmed cold with **no scrolling at all**: all five arrive (four 737×616, the arm 600×420).
Otherwise the ports are the v9 recipe exactly: geometry, ink and resting views untouched; an IntersectionObserver on
`#s5` / `#s8` so they draw only in view; the blur/visibility stops; the orbit spring that stays where the mouse left
it; `snap()` → `window.lnSnap` / `cpSnap` / `amSnap`. 12 slides stay 12, every `rv d1…d5` kept, pagenums untouched.

## Verification
`playwright`, chromium, 1440×810, DPR 2 — `notes/_lanes/289/deck/render-v10/s01.png … s12.png`, looked at.
- 12 slides; **zero page errors, zero console errors**; document scrollWidth 1440 = viewport. **Zero overflow**: no
  descendant of any slide crosses its section box by more than 1 px (every element, all twelve).
- All six canvases drawn, not blank: `gb` 4.89 % non-white, `bk` 7.29 %, `br` 3.17 %, `ln` 3.33 %, `cp` 1.94 %, `am` 3.11 %.
- Looked at s2 (five chapters, one row), s5 (two arms either side of the conveyor, the red workpiece the one accent),
  s8 (the callipers, no caption, no scale bar), s10 (five cells, five drawings, legible at 1440).
- Print: `print-s5.png`, `print-s8.png`, `print-s10.png` — the baked `<img>`s stand in for the canvases, anatomy included.

## Unsure of
1. **Five white cards in a row.** s5 and s8 were the grey and the dark that broke up s4 / s6 / s7; s4–s8 are now one unbroken white run — the drawings earn it, but the rhythm is flatter than v9's.
2. **The gearbox's own `i = 6.43 : 1` scale bar** rides into the §10 "Parts" cell, which copies the gearbox's existing
   bake. Unreadable at that size; removing it means touching the v9 gearbox, which was not asked for.
3. **The callipers card is short** — the instrument is long and flat, so it fills half the 615 px `.draw` box and reads
   emptier than s4 / s6 / s7. Leaving the fit alone was the conservative call.
