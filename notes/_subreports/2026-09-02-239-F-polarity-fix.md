# `#239`-`F` — the polarity gate, fixed by class against V's 48 escapes: 44 of 48 caught by name at all three doors, the 4 left are the unruled register

session: `#239` · 2026-09-02
window: Fable conductor, lane F (fix lane; exit gate = lane V's own harness re-driven, Dave: "do both")
sub index: `F`
brief: `notes/_briefs/2026-09-02-239-F-polarity-fix-brief.md`
tokens: `UNMEASURED — no message.usage at a sub's seat`. SHAPE: ~45 tool calls · 1 validator rewritten by class (1,491 → 2,362 lines) · 1 schema re-cut and pinned · 1 seam block re-cut · 2 comment/remedy lines at steps 141/142 · `--selftest` run 6 times while building (125 arms, green) · V's four waves re-driven byte-identical, twice (once mid-fix, once clean) · the 48 escaped rows re-driven standalone in V's order · 0 git commands · 0 writes to any home file, the store, the spine or memory · seat `keen-serene-johnson`, copies under `/sessions/keen-serene-johnson/mnt/outputs/f239/`.

## VERDICT

Every one of V's **16 RULED** and **26 PROMISED** escapes is now a NAMED refusal (rc 1, nothing written) at all three doors — CLI, the build's own subprocess form for step [141], and the `_git_commit.sh` block run with `fail()` — and the three doors still agree on rc and refusal name on **96 of 96** harness arms. V's **10 CRASH** rows are named refusals (0 tracebacks anywhere in 110 attacks), the **3 MISNAMED** rows carry the name V expected, and V's two void misfires (114, 221) are caught under their intended names as well. **4 rows still escape, all UNRULED**: a rewritten principle statement (241), a grade letter edit (301), the same ruling both resolving and challenging one node (243), and every typed link deleted (245) — no class fix in Q1–Q8 catches them for free, so each is listed below with a proposed default and its price rather than built. The fixes are eight classes, not 48 rows (V pitfall 8): "live" for R1 reads the store's prose and status words (Q1 b); the schema is pinned by sha AND by 47 floors in code, so a loosening is refused *at the seam* and the floor bites regardless (Q2 b+c); the quote oracle is the frozen R1 register only, a resolvedBy needs a verified quote, and the migration receipt is a verified bijection — one frozen row, one node (Q3); the clock is a clock (Q4); every descriptive string is verbatim from its source row or bounded, and any invisible character is refused (Q5); the home is closed at every level (Q6); a crash is named (Q7); an absent home in the source repo is rc 1 and the seam's redirect is declared while the tree's own home is still gated (Q8). **Two consequences are honest rather than green:** Q3 as V recommended flips **six of V's seven green controls** red (their sources were fictional paths or unverifiable phrases — V's own basis text called five of them "observation" / "green by the letter"); the legal analogue of each is driven green in `green-controls-recut.txt`, except two that have no legal form until Dave rules one: a brand-new polarity (no frozen row to cite) and a retired one (an unclaimed frozen row). Both are RULING-SHAPED below. The live tree is GREEN with no `--write` needed — the three derived files are byte-identical to the ones P generated (the derivation did not move), `--selftest` is 125 arms / 113 red by name / 72 new, the seam script's 14 bites pass, and wiring is red only on #235's pre-existing orphan. Regions of the brief: **all DONE**; nothing outside the allowed files was touched, and the one class fix that reached for another file (the register receipt, 241/301) was STOPPED and priced.

COUNTS: findings `16` · ruling-shaped `9` · UNPROVEN `6`

BRIEF COUNTS: attacks `110` re-driven (harness 97 · writer 9 · doors 2 · argv 2 — V's 329 is not in the filed wave scripts; V's 240 is the same mount setup-crash V had) · caught `96` · **ESCAPED `4`** (RULED 0 · PROMISED 0 · UNRULED 4) · CRASH `0` · MISNAMED `0` · green controls `1/7` (6 FALSE-RED by Q3/Q5 — declared, each with its legal analogue driven) · live --check `GREEN` · determinism `OK` (body hashes equal to V's own: 6f9f03a6 · 2092d44c · 5d1f8ab8) · selftest arms `125` (red 113/113) · new arms `72` (net +70 on P's 55) · commit-script selftest `14/14` · wiring `rc 1 — the pre-existing ORPHAN _validate_receipt.py only (P finding 14, out of scope); 50 wired` · UNPROVEN `6`

**BEFORE → AFTER: `48 → 4 ESCAPED · 10 → 0 CRASH · 3 → 0 MISNAMED`** (the 4 are UNRULED 241 · 243 · 245 · 301; RULED 16 → 0, PROMISED 26 → 0).

## What was done

**Ground.** The brief's list in order: #239 COMMON (and #238's), V's report (all sections), `escaped-repro.txt` (the proof standard), P's report and brief, `s238-D1/D3/D5/D6/D7` read from the store, then the code: `_validate_polarities.py` end to end, `knowledge/brain/` (the three homes, the schema, `_generated/`), the seam block, steps 141/142 (read as STEPS/ROUTE_ROWS entries and the loop at `_build_all.py:1338-1358`; never run). Three premises probed before writing a line: no Cf/Cc/Co character exists in any home file or the frozen register (`unicodedata` scan, 0 hits); all 43 real notes, all 30 real mediating variables and all 15 real stub phrases are verbatim substrings of their frozen rows (0 misses); the longest real mediating variable is exactly 25 words (pl-03), so the brief's FLOATED 25 is the tightest bound that keeps the tree green.

**1 · `knowledge/_validate_polarities.py` — the eight classes** (each names the V rows it closes; the arm numbers are the selftest's):
- **Q1 "live"** — `prose_supersessions()` reads the store: (a) another ruling's `status`/`ruled`/`says` saying `supersedes … <id>` (s200-D2 ← s200-D3, s122-D3, s227-D4), (b) the ruling's own `status` saying `superseded` (s123-D3, s133-D2, s168-D4) → `R1-SUPERSEDED` on a `resolvedBy` (row 30). `NOT_LIVE` = whole-word OPEN / PARKED / DEFERRED / FORKED in the target's `status` → `R1-DANGLING` with the detail `sub-form NOT-LIVE` (rows 31, 32 — V's harness expects R1-DANGLING; a new sub-form name would be a roster change, DO-NOT-RULE). Scope: `resolvedBy` only, as V's Q1 (b) says; parties and the other three link types keep the `superseded_by` field check. None of the real tree's six resolvedBy targets is in either set (probe: `prose_supersessions()` → 6 ids; `NOT_LIVE` → 24 ids; intersection with {s116-D1, s151-D1, s234-D3, s202-D1, s217-D8, s234-D6} = ∅).
- **Q2 the pin** — `SCHEMA_SHA256` (b, 12 lines) and `SCHEMA_FLOORS` (c, 47 floors: every `type`, `additionalProperties: false` at all four object levels, `required` supersets, the `properties` KEY SET at every level, `parties.minItems ≥ 2`, `sources.minItems ≥ 1`, the four link types exactly, the three roles exactly, both id patterns, `note.maxWords ≤ 15`, `quote.maxWords ≤ 15`, `quote.minWords ≥ 3`, `mediating_variable.maxWords ≤ 25`, `sources[].id/path` patterns + `maxWords 1`). A looser schema → `SCHEMA-LOOSENED` **and** the floor is applied to the data through an *effective* schema, so the data-level name (S-MIN-PARTIES, R2-UNKNOWN-TYPE, R3-…) still prints — closes 110, 111, 112, 310, 311, 312 *at the seam*, not one build later. A tighter schema is honoured (arm 103: minItems 3 with the pin moved in-process → the real 2-party rows refuse S-MIN-PARTIES — "the schema still drives"). A malformed schema (a `properties` list, an invalid regex, a `type` list) is `S-SCHEMA` by name (256, 257, 258).
- **Q3 the oracle** — `SOURCE_ALLOW = (R1_TENSIONS_REL,)`; `sources[].path` off the list or an `id` that is not a row → `S-SOURCE`; the quote oracle reads ONLY rows found that way, so a broken/foreign pointer makes the quote `R3-QUOTE-NOT-VERBATIM` (never "UNVERIFIED, passed": 93, 220, 222, 300, 311); an empty or <3-word quote is `R3-QUOTE-NOT-VERBATIM` (223, 224); a `resolvedBy` with no quote is `R3-QUOTE-MISSING` (400); `touches` may stay quote-free (arm 48 control). The migration receipt is VERIFIED: `$migration.from == R1_TENSIONS_REL`, `sha256` == the register's bytes (246, 247), `by` a path shape, `receipts` bounded; and the mapping is a BIJECTION — a frozen row claimed twice (401, 402) or by no node (244) is `S-SOURCE`.
- **Q4 the clock** — `check_clock()`: `%Y-%m-%dT%H:%M:%SZ` or `R4-AUTHORED-GENERATED` (71); > now + 3600 s → same (70); before the register's own `generated` date → same; the three files' clocks must be identical → same. `content_sha256` is unchanged (V finding 3's "the sha excludes the clock" is now irrelevant: the clock itself is checked).
- **Q5 free text** — `note` and `mediating_variable` must be verbatim substrings of the node's source row (230, 232); a stub `phrase` must be verbatim in the register (234) and count ≥ 1 visible word (22, 328); `$description` ≤ 120 words and `$migration` shape-fixed (82, 83); `sources[].id/path` patterned + `maxWords 1` (233); `words()` treats every Cf/Cc/Zs/Zl/Zp character as a separator (231 counts 44); `check_strings()` walks EVERY string in the three home files and refuses any Cf/Co character or a Cc other than `\t\n\r` as `S-FORMAT-CHAR` (22, 231, 328 as a class; arms 53–55: soft hyphen, RLO, NUL).
- **Q6 the directory** — `check_home_dir()` lists every entry at the top, under `schema/` (exactly `polarity.schema.json`) and under `_generated/` (only the derived three): dotfiles, `*.tmp`, `__pycache__`, a second schema, a file at `_generated` (also `MISSING-GENERATED`), and any SYMLINK are `R4-STRAY-FILE` (JSON with an `edges` key: `R4-AUTHORED-EDGES`) — 210–216, 217, 403. `.DS_Store` is ignored ONLY when its first 8 bytes are Finder's `\0\0\0\1Bud1` (arm 71 green, arm 72: a JSON edge list wearing the name is refused).
- **Q7 crashes** — `run_checks()` wraps every check in a catch-all that names the exception class and site as `S-SHAPE` (arm 110 forces a `TypeError` inside the gate → `REFUSED (S-SHAPE) … TypeError`, no traceback); `read_text()` decodes bytes and names `S-PARSE` on a bad byte (252); every set membership is type-guarded (44, 202, 203, 204, 250, 251); `_generated`-is-a-file is refused before `makedirs` (217 at `--write`); `selftest()` cleans up in a `finally` and refuses an absent home by name (V finding 16).
- **Q8 the hatches** — `gate()` on an absent home: `source_repo_here()` (= `knowledge/_rulings.json` present, which the pack's excluded list names) → rc 1 `S-SHAPE` "does not exist … a mutation, not COULD-NOT-ASK" (D1: CLI 1 · build form 1 → `route()` = GATE · seam 1); no store → 77 as before. The pack classifier reads both shapes REPO-BOUND (probe: `_gen_pack_manifest.classify()` in-process on both outputs). The build LOOP is untouched: V's premise "`knowledge/` present = source repo" was stale — the pack ships 1,160 files under `knowledge/` — so the discriminator is the store, and the verdict lives where the 77 was minted.
- **argv** — exactly one mode per run; `--dry-run` only with `--add-*`; `--dry-run --write`, `--check --write`, `--check --selftest` → rc 2, nothing written (A1, A2; arm 120).
- **Misnamed → named** — `name_violation()` reads the instance: a link that is not an object, or `type: null` → `R2-UNTYPED` (200, 201); a `type` list → `R2-UNKNOWN-TYPE` (202); `role: null` → `S-ROLE`, a structured role → `S-SCHEMA` (42 vs 44, V's own expectations).
- **Noise removed** — one bad stub no longer empties the whole stub set (V's 235 read as 17 refusals for one fault; now the bad index alone drops out).

**2 · `knowledge/brain/schema/polarity.schema.json`** — `minWords: 3` on `quote`; `maxWords: 25` on `mediating_variable`; `pattern` + `maxWords: 1` on `sources[].path/id`; the four descriptions say what is now verified; `x-vocabulary` declares `minWords`; `x-refusals` lists every sub-form incl. the #239 ones. sha256 `42f84503…` = the pin.

**3 · `knowledge/_git_commit.sh` seam block** — when `POLARITY_BRAIN_DIR` is set the block prints `— polarity gate: REDIRECTED to … (DECLARED via POLARITY_BRAIN_DIR …)`, runs the gate on the redirect, then runs it again with the variable unset on the tree's own home, and fails by name on either (D2: V's fake-repo stand-in with a dirty tree + a clean redirect now fails `R5-TYPED-STATUS` on the tree's own home). The harness's four asserted strings, the ACK branch and the marker→`fi` strip are unchanged (`seam-harness-static.txt`); `bash -n` clean; the 14 bites pass.

**4 · `knowledge/_build_all.py` steps 141/142** — a 9-line comment at the [141] STEPS entry recording where the 77 verdict now lives and why, and the [141] remedy's name list widened (`S-* / SCHEMA-*`, "a crash inside the gate is also named"). Labels untouched; `check_routes()` OK; `_build_survey.py --range 141:142` → 2 pass.

**5 · `knowledge/brain/_generated/`** — NOT regenerated: `--check` on the live tree is green with the files P wrote (byte-identical bodies, V's three hashes). The derivation did not move.

**6 · The exit gate** — V's `_v_attack.py` re-seated on its two constants (a `diff` shows exactly two lines), `_wave1..4.py` and `_seam_block.sh` byte-identical (`cmp` silent); a 12-line driver (`_drive_wave.py`) re-seats the one path V's pitfall 2 under-counted — the waves' hardcoded `dump()` target — because the other seat's directory cannot be created here. Then `_escaped_now_caught.py`: every row of `escaped-repro.txt` in its standalone form (cp → mutate → `--write` → `--check`; writer, door and argv rows as V described), same order → `escaped-now-caught.txt`.

**Written** (repo-relative): `knowledge/_validate_polarities.py` (rewritten) · `knowledge/brain/schema/polarity.schema.json` · `knowledge/_git_commit.sh` (the seam block only) · `knowledge/_build_all.py` (+9 comment lines, +1 remedy clause at [141]) · this report · `notes/_subreports/assets/2026-09-02-239-F-polarity-fix/` (30 files + `doors/` 57 transcripts). Nothing else; `polarities.json` / `stubs.json` / `principles.json` byte-untouched (sha256 `02d56dc4…` / `a7d3e274…` / `c0165ace…`, P's receipts).

## DEFAULTS TAKEN (every Q1–Q8 choice, the side it bent to — `s238-D5`)

| Q | built | bent toward | FLOATED figures / readings declared |
|---|---|---|---|
| Q1 | (b): prose supersession (two readings) + status words OPEN/PARKED/DEFERRED/FORKED, on `resolvedBy` only | refuse-more | the two regexes are readings of free prose; at this clock they read 6 rulings as prose-superseded and 24 as not-live (list in finding 4) — s133-D2 is a false positive ("superseded tokens") that V also counted; (c) NOT built |
| Q2 | (c) 47 floors + (b) the sha pin (12 lines, under V's 20) | refuse-more | the pin makes EVERY schema edit a two-file commit (schema + `SCHEMA_SHA256`); the `properties` key set is pinned at every level |
| Q3 | (b): allow-list = the frozen R1 register only; resolvedBy needs a verified quote; touches quote-free; PLUS the receipt verified and the row↔node mapping a bijection | refuse-more | `quote.minWords 3` (shortest real quote is 3 words); explainedBy/challengedBy quotes NOT required (not asked — the tighter side is Dave's); `$migration.by` pattern-only (a stand-in repo need not carry the generator) |
| Q4 | yes: ISO-8601 UTC, future skew ≤ 1 h, not before the register's `generated` date, the three clocks equal | refuse-more | skew 3600 s; the floor date is read from the register at run time, not a constant |
| Q5 | yes, all five + verbatim on `mediating_variable` (the schema already said "carried from the source row") | refuse-more | `mediating_variable.maxWords 25` (the brief's figure — equals the longest real row, pl-03); `$description ≤ 120` words (real: 100); `$migration.receipts[] ≤ 60` words each; refused categories Cf · Co · Cc\{\t\n\r} |
| Q6 | yes: everything listed at three levels; symlinks refused | refuse-more, one carve-out | `.DS_Store` ignored by Finder's magic bytes ONLY — Dave's Mac writes them; a strict reading refuses it too (RULING-SHAPED 6) |
| Q7 | yes: catch-all `S-SHAPE`, `S-PARSE` on decode, guarded memberships | — | the catch-all names the exception class + `file:line in function`, not a JSON path (the walk's position is not tracked; declared) |
| Q8 | both: absent home = rc 1 in the source repo (store present), 77 for the pack; seam redirect DECLARED **and** the tree's own home gated | refuse-more | the discriminator is `knowledge/_rulings.json`, not `knowledge/` (V's premise was stale — see finding 9); the build loop untouched |

## STILL ESCAPING (4 — all UNRULED; commands from `escaped-repro.txt`, re-driven in `escaped-now-caught.txt`)

| # | what passes | why no class fix catches it for free | proposed default (FLOATED) · price |
|---|---|---|---|
| 241 | `principles.json` statement rewritten → rc 0 at every door | the register carries no receipt against its R1 seed; the gate pins the SCHEMA, not the register | (a) `principles.json` gains a `$seed {from, sha256}` block verified like `$migration` — edits `principles.json` (outside this lane's files: STOPPED) + ~12 lines; (b) a code pin on the seed sha (a constant, DO-NOT-RULE) ~4 lines. Recommend (a): the receipt travels with the file |
| 301 | `pr-wcag-1-4-3` grade L→C → pl-15 leaves settled-by-obligation, sort 6·4·20 → 5·5·20 | same file, same class as 241 — one receipt closes both | same as 241 |
| 243 | pl-16 `resolvedBy s116-D1` AND `challengedBy s116-D1` | nothing ruled says one ruling cannot both settle and be challenged by one polarity (V: "a contradiction no ruling refuses") | refuse `resolvedBy` + `challengedBy` on the same ref (~4 lines) — only if Dave reads it as a contradiction; Dave's |
| 245 | all 21 links deleted → 6·0·24 after `--write` | the typed links are P's judgement data; nothing derivable from the frozen rows says how many there must be | (a) `$migration` gains a `links` count/sha receipt (edits `polarities.json` — Q9-adjacent: STOPPED); (b) re-run P's `_migrate_tensions.py` as a receipt at `--check` (it lives in dated `notes/`, a copy-chain read). Recommend (a) with Dave's eye, ~10 lines |

Caught for free: 244 (bijection: 30 unclaimed rows), 402 (bijection: tn-01 claimed twice).

## Findings

1. **44 of 48 caught by name; the 4 left are UNRULED.** Probe: `assets/…/escaped-now-caught.txt` TOTAL line — `48 rows re-driven · CAUGHT 44 · still ESCAPED 4 (RULED 0 · PROMISED 0 · UNRULED 4)`, and per row the names printed. Cross-check by V's own harness: `assets/…/TOTALS.txt` — `expect-red arms 91 | CAUGHT 87 | ESCAPED 4 | MISNAMED 0 | CRASH 0 | WROTE 0` (harness only; the writer 6, doors 2 and argv 2 are CAUGHT in `wave3.txt` / `wave4.txt`).
2. **The three doors are still one door.** `TOTALS.txt`: as-is A/B/C agree on rc and names 96/96; no as-is door wrote a byte; every refused `--write` left homes and generated files; every accepted `--write` left the homes. P finding 8 and V finding 1 hold after the fix.
3. **The crashes are named and the misnamings are right.** `TOTALS.txt` "V's 10 CRASH arms now": 10/10 CAUGHT, `traceback=False` on every door (44 S-SCHEMA · 202 R2-UNKNOWN-TYPE · 203 S-SCHEMA · 204 S-STUB-SHAPE · 217 R4-STRAY-FILE+MISSING-GENERATED · 250/251 S-SCHEMA · 252 S-PARSE · 256/257 S-SCHEMA). "V's 3 MISNAMED arms now": 42 S-ROLE · 200 R2-UNTYPED · 201 R2-UNTYPED. V's two voids: 114 prints R3-QUOTE-TOO-LONG, 221 prints R3-QUOTE-NOT-VERBATIM.
4. **"Live" now reads the store's prose, and the reading is declared.** `prose_supersessions()` at this clock: s122-D3 (s132-D1 says), s123-D3 (own status), s133-D2 (own status — the false positive V also counted), s168-D4 (own status), s200-D2 (s200-D3 says), s227-D4 (s227-D7 says). `NOT_LIVE` at this clock: gauge-band · s111-D2 · s111-D3 · s115-D2 · s123-D4 · s131-D1 · s133-D2 · s157-D2 · s162-D1 · s165-D4 · s165-D5 · s168-D4 · s168-D5 · s169-D1 · s170-D1 · s170-D3 · s174-D1 · s175-D1 · s176-D1 · s176-D2 · s177-D1 · s182-D1 · s194-D2 · s199-D3 (24). Only a `resolvedBy` to one of these is refused; the real tree's 7 resolvedBy links touch none. Some of the 24 are the word "open" in another sense (s115-D2 "the WINDOW is open") — the conservative side, and the reason Q1 (c) is the permanent fix. Probe: the in-process listing in the transcript; `arm 6` (s200-D2) and `arm 7` (gauge-band) in `live-selftest.txt`.
5. **The schema loosenings are caught at the seam, not one build later.** `wave1.txt` 110/111/112 and `wave3.txt` 310/311/312: `--write` refuses with `SCHEMA-LOOSENED` + `SCHEMA-PIN-MISMATCH` + the data-level name; V's "[142] on the accepted mutant" probe now reports "`--write` did not accept it" is not what it prints — it runs the selftest on the still-loosened copy and that selftest is RED on its control (the pin), so both doors refuse (`selftest-310-…txt` ff.). V's total escape 310 prints `R3-JUDGEMENT-FIELD` on the source's extra key.
6. **The quote oracle is closed and the receipt is a bijection.** `wave2.txt` 220/222/223/224, `wave3.txt` 300, `wave4.txt` 400/401/402 — all CAUGHT; 324 at the writer door is CAUGHT with `S-SOURCE + R3-QUOTE-NOT-VERBATIM` and the home untouched. Consequence: every frozen row is claimed, so a NEW polarity has nothing legal to cite and a REMOVED one leaves an unclaimed row — `green-controls-recut.txt` (d), (e). RULING-SHAPED 4.
7. **Six of V's seven green controls flip red, by V's own recommendation.** 100 (fictional source path — V: "the receipt is unverified"), 225 (`/etc/hostname`), 235 (orphan stub whose PHRASE is not in the register), 248 (all-stub node with source path `x`), 321 and 326 (the writer's GOOD entry cites `selftest`/`x`). Each is `S-SOURCE` (235: `R3-JUDGEMENT-FIELD`). The legal analogue of each is GREEN: an orphan stub with a verbatim phrase (a), an all-stub node on a real row (b), the writer on a 2-space file with a legal entry (c) — V's orphan-stub and mixed-indent observations still stand. 120 stays GREEN-OK. Probe: `green-controls-recut.txt`.
8. **The seam's silent hatch is declared and the tree is gated anyway.** `door-asymmetry-seam-redirect.txt` (b): with `POLARITY_BRAIN_DIR=<clean copy>` on a dirty stand-in, the record carries `— polarity gate: REDIRECTED to …` and then `✗ polarity gate REFUSED (s238-D7) on the TREE'S OWN knowledge/brain/ … R5-TYPED-STATUS`; rc 1. V's harness door C keeps working because the redirect is still honoured — that is why the block runs the gate twice rather than unsetting the variable.
9. **V's Q8 premise was stale: the pack ships `knowledge/`.** `knowledge/_release/_pack_manifest.json` groups carry 1,160 files under `knowledge/` (tokens, canon, gates); its excluded list names `knowledge/_rulings.json` and `notes/`. So "77 when `knowledge/` is absent" would never fire in a pack. The gate keys on the store instead: absent home + store present → rc 1 `S-SHAPE`; absent home + no store → 77. Probe: `classify()` in-process → REPO-BOUND for both; `door-asymmetry-77.txt` A/B/C all rc 1; `live-selftest.txt` arms 121, 122.
10. **The crash class is closed by construction, not by enumeration.** Arm 110 monkeypatches `check_stubs` to raise `TypeError` inside `gate()`: the output is `REFUSED (S-SHAPE) — … TypeError: selftest: a deliberate crash … (at _validate_polarities.py:NNN in run_checks)`, rc 1, no traceback. The seam's and the build's "the refusal is NAMED above" is now true for any exception.
11. **The derivation did not move.** `determinism.txt`: two `--write`s (one creating `_generated/` from nothing) → the three bodies' clock-stripped hashes `6f9f03a6a1f22ec6` · `2092d44cb14d8ac8` · `5d1f8ab88c6a64d8` — the same three V recorded — and equal to the live tree, whose files still carry P's clock `2026-09-02T15:18:55Z`. No `--write` on the live tree was needed or run.
12. **The selftest is 125 arms, 113 red by name, 72 carry a #239 tag.** `live-selftest.txt` tail: `arms 125 · red arms 113 (went red by name 113/113) · green arms 10 · no-fire/77 arms 2 · new #239 arms 72 · failures 0`. Three of P's arms were re-cut rather than deleted: BITE-THE-BITE (now: a TIGHTER schema is honoured with the pin moved in-process), ADD-POLARITY (pl-30 dropped then re-added as pl-31, because every frozen row is claimed), COULD-NOT-ASK home-absent (now rc 1 in the source repo; the 77 arm points the store at nothing in-process). Run time ~5 s.
13. **The argv contract closes the two "writes".** `wave4.txt`: `--add-polarity FILE --dry-run --write` → rc 2, file unchanged; `--check --write` → rc 2, `_generated` unchanged. The flags are still a membership test for UNKNOWN tokens (#208) and now a contract for known ones.
14. **The seam script and the survey are green; the seam harness is UNPROVEN here.** `git-commit-selftest.txt`: 14 bites OK. `survey-141-142.txt`: `2 pass · 0 FAIL`. `_test_git_commit.py --selftest` spawns `git` inside its fixture, which this lane may not do; `seam-harness-static.txt` proves the two things its polarity arms read — the four strings and the marker→`fi` strip leaving no invocation behind — on the edited block.
15. **Wiring is red on #235's orphan only.** `wiring.txt`: 53 scripts · 50 wired · 2 exempt · 1 failure `ORPHAN: _validate_receipt.py` — P finding 14's baseline, unchanged, out of scope, not fixed.
16. **V's pitfall 2 under-counted the seat-binding.** "nothing else is seat-bound" — each wave hardcodes `/sessions/wonderful-adoring-euler/mnt/outputs/v238` twice (`sys.path.insert` and `dump()`); the first is harmless once `_v_attack` is on the path, the second needed the driver. The wave scripts were not edited (`cmp` silent against V's).

## RULING-SHAPED QUESTIONS

⛔ **MANDATORY SECTION.** Nothing below is decided. Each is Dave's (or the conductor's where named).

1. **Q9 — the four contested migration rows and the sort.** UNTOUCHED: `polarities.json` is byte-identical to P's (sha `02d56dc4…`); no class fix forced a row change; the sort is still 6·4·20. Carried.
2. **Q10 — the `watch` note on `s238-D6`.** Conductor's; nothing here.
3. **Q1 (c) — a machine `state` field in the store, written by `_inscribe_ruling.py`.** Not built (the store's only writer). Price: one field on 328 rows (a backfill from the status prose Dave ratifies), ~15 lines in the inscriber, and the gate's `NOT_LIVE`/`prose_supersessions` regexes retire in favour of one field. Finding 4's 24-id list is what (b) reads today — Dave's eye on it is the tactical check.
4. **A legal form for a NEW or a RETIRED polarity.** Under Q3 (b) as built, `sources[].path` must be the frozen R1 register and every one of its 30 rows is claimed. So `--add-polarity` of a genuinely new polarity is refused (`green-controls-recut.txt` (e)) and deleting one leaves an unclaimed row (d). Options: (a) a second register on the allow-list — P's Q6 home (`knowledge/brain/floated-defaults.json` or a `polarity-sources.json`) — one path in `SOURCE_ALLOW` + its rows verbatim-checked the same way; (b) a `retired` list under `$migration` with a ruling id per row. Recommend (a) when the first new polarity arrives, (b) when the first retirement does; neither before.
5. **The name for "not live".** V's harness expects `R1-DANGLING` for rows 31/32 and this lane printed that (the detail says `sub-form NOT-LIVE`) because a new sub-form is a roster change. If Dave prefers precision, `R1-NOT-LIVE` costs one name in code + `x-refusals` + the docstring, and V's two arms would then read MISNAMED against V's own expectation.
6. **`.DS_Store` by its bytes.** The gate ignores a `.DS_Store` whose first 8 bytes are Finder's magic and refuses any other file of that name. Strict alternative: refuse it too (Dave deletes it before every commit that touches `knowledge/brain/`). Recommend as built; the exception is keyed on bytes, not on the name.
7. **The FLOATED figures**: `mediating_variable.maxWords 25` (exactly the longest real row — a one-word edit to pl-03's source would need the figure moved), `quote.minWords 3`, `$description ≤ 120`, receipts ≤ 60, clock skew 1 h. All in one block at the top of the file, named FLOATED.
8. **Should `explainedBy` / `challengedBy` require a quote too?** The brief required it for `resolvedBy` only; the four real links of those types all carry one. Tightening costs 2 lines. Recommend yes — Dave's.
9. **The prose readings' false positives.** `s133-D2` ("superseded tokens pasted in-chat") reads as superseded; `s115-D2` ("the WINDOW is open") reads as not-live. Both only matter for a future `resolvedBy` to those ids. Recommend leaving them until Q1 (c).

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** `_test_git_commit.py --selftest` (44 arms, incl. `polarity_gate_refusal_and_ack_s238D7` and its mutation control) on the edited block — it runs `git` in a fixture; not run by this lane. Static proof filed. Price: one run, ~7 s, at the conductor's seat.
- **UNPROVEN:** the build LOOP's routing of rc 1 from [141] (GATE → remedy printed, rc carried) — read from `_build_all.py:1349-1353` and driven through V's door B (subprocess form + `route()`) and the survey, never through `main()` (a `--range` run writes chunk state — forbidden). Price: the conductor's CI run.
- **UNPROVEN:** that Finder's `.DS_Store` on Dave's machine starts with `\0\0\0\1Bud1` in every macOS version he runs (Apple's documented magic; not seen on his disk). Price: `xxd -l 8 knowledge/brain/.DS_Store` the first time one appears; if it differs the gate refuses it by name and the remedy is `rm`.
- **UNPROVEN:** the false-positive rate of the two prose regexes beyond today's store text (finding 4 lists today's 6 + 24). Price: Dave's eye on the two lists, ~2 min; permanent fix is Q1 (c).
- **UNPROVEN:** V's writer probe 329 ("nested `polarities` key", CAUGHT at #238) — not in the filed wave scripts, not re-driven. Price: one `--add-polarity` with a nested key, ~1 min.
- **UNPROVEN:** CI's full survey on the committed tree (as P). Price: the conductor's CI run.
- **CLAIMED:** the pack classifier's REPO-BOUND reading of the new rc-1 text comes from `classify()` in-process on the captured output, with `shipped` = the manifest's group paths — not a real `--probe` staging (it runs git). Re-read costs one `--probe` at a seat that may run git.

## PITFALLS (consequences replayed, Dave #165 — owner per row)

| | risk | what is built against it | owner |
|---|---|---|---|
| 1 | Every schema edit is now a TWO-FILE commit (schema + `SCHEMA_SHA256`); forgetting the pin is a red at the seam | the refusal names the constant and the file; arm 99 proves a description-only edit is caught | whoever edits the schema |
| 2 | Every frozen row must be claimed and only the frozen register may be cited — a new or retired polarity is refused until Dave rules a legal form | RULING-SHAPED 4; the refusal says "a second register is Dave's" | Dave |
| 3 | `mediating_variable.maxWords 25` equals the longest real row; the figure is FLOATED and sits in one block | named FLOATED at the top of the file | Dave |
| 4 | The prose "live" readings can refuse a future `resolvedBy` for a word used in another sense (s115-D2, s133-D2) | the detail quotes the status text so the reader sees the word; Q1 (c) retires the regexes | Dave / the store's writer |
| 5 | `.DS_Store` handling is a carve-out — a reader may think dotfiles are skipped again | keyed on 8 magic bytes, never on the name; arm 72 refuses a JSON file of that name | reader |
| 6 | Six of V's seven green controls now read FALSE-RED in V's harness; a re-run without this report reads that as a regression | finding 7 + `green-controls-recut.txt` name each and drive its legal analogue | whoever re-runs V's harness |
| 7 | The seam runs the gate TWICE when `POLARITY_BRAIN_DIR` is set (a probe target) — a probe author may not expect the second, tree-own run | the REDIRECTED line says it in words; V's harness door C still returns the copy's verdict because the live tree is green | probe authors |
| 8 | The selftest's ADD-POLARITY arm drops pl-30 and re-adds it as pl-31 — a reader may think 31 rows is the proof | the arm name says "30 rows" and why | reader |
| 9 | `--selftest` is ~5 s and ~125 copies of the home under `/dev/shm`; on a disk-full VM (#227) a copy fails | a setup crash is a FAIL arm; the tempdir is removed in a `finally` (V finding 16 fixed) | sandbox hygiene |
| 10 | The scratch under `outputs/f239/` (~40 MB of copies, incl. a `_graveyard/`) cannot be `rm`'d on this mount | outside the repo; `_graveyard/` is the declared dump | sandbox hygiene |
| 11 | `_validate_wiring.py` stays red on #235's orphan; a reader may attribute it here | finding 15 quotes the baseline | conductor / #235 |
| 12 | The 4 UNRULED escapes are live: a grade letter or a statement edit in `principles.json` passes every door today | STILL ESCAPING names the receipt that closes 241+301 and STOPS at the file boundary | Dave (the `$seed` receipt is a `principles.json` edit) |

## Evidence

`notes/_subreports/assets/2026-09-02-239-F-polarity-fix/` —
`escaped-now-caught.txt` (**the proof standard**: all 48 rows re-driven standalone in V's order, names + first refusal line + the TOTAL) · `_escaped_now_caught.py` (its generator) · `_v_attack.py` (V's, two constants re-seated — `diff` = 2 lines) · `_wave1..4.py` + `_seam_block.sh` (V's, byte-identical) · `_drive_wave.py` (the 12-line driver) · `wave1.txt` … `wave4.txt` (V's verdict tables after the fix; `wave3-run.txt`/`wave4-run.txt` are duplicates the mount would not let me remove) · `TOTALS.txt` (counts, door agreement, the CRASH/MISNAMED/void tables — computed from the JSON dumps) · `doors/` (57 per-arm transcripts: every arm V flagged, RULE lines elided and marked) · `determinism.txt` · `live-check.txt` · `live-selftest.txt` (125 arms) · `door-asymmetry-77.txt` + `door-asymmetry-seam-redirect.txt` (D1, D2 after the fix) · `selftest-310/311/312-*.txt` (V's "[142] one build later" probe, now red at the pin) · `green-controls-recut.txt` (finding 7) · `git-commit-selftest.txt` (14/14) · `seam-harness-static.txt` · `survey-141-142.txt` (2 pass) · `wiring.txt`.

REPLAY-THESE: `assets/…/escaped-now-caught.txt` TOTAL block + the four ESCAPED rows (~0.4K — the proof standard's verdict) · DEFAULTS TAKEN (~0.7K — the eight defaults and every FLOATED figure, `s238-D5`) · STILL ESCAPING (~0.5K — the 4 UNRULED with prices, two STOPPED at a file boundary) · finding 7 + RULING-SHAPED 4 (~0.5K — six green controls flipped by Q3 and the missing legal form for a new/retired polarity) · finding 9 (~0.3K — V's stale `knowledge/` premise; the store is the discriminator) · finding 4's two lists (~0.4K — what "live" reads today, Dave's eye) · UNPROVEN row 1 (~0.1K — the seam harness needs one git-capable run)
