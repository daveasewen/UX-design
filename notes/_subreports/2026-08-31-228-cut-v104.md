# #228 — the v1.0.4 cut

*Filed 2026-08-31 by the Opus build sub. Conductor: Fable. Status: BAKED.*

**enacted 6 · ruling-shaped 3 · UNPROVEN 3 · reds before 6 → after 1**

Apollo — Spider **v1.0.4** is baked into `apollo-spider/dist/Apollo-Spider-v1.0.4.zip`,
sha256 `617a7ba385ec9c127ce6024e23cb5deca87358b5e7ba24d59d1da4aa82a938ca`, byte-identical to
both dry-run twins by `cmp` over the whole file. Ten commits on `master`, **unpushed** — the
conductor pushes and reads CI back (`s203-D1`).

---

## COMMITS

| hash | subject |
|---|---|
| `ddc7821` | the v1.0.4 enact set — the ratifying key moves, the frozen chain copy is ported, and two orphans get consumers |
| `ba139f9` | hold the designer gate roster at the ruled 58 — the scratch probe is fenced, and the question goes to Dave |
| `ce6c2c3` | the v1.0.4 ship list — regenerated at ba139f9, carrying the cold-start group and RATIFIED by the store |
| `b5bee67` | the zip stops being named by hand — two stale v1.0.3 literals the first dry-run found |
| `388747d` | the ship list re-generated at b5bee67 — the version fix carried into the manifest |
| `dfa9cee` | the dry-run proof lands on Dave's go/no-go page — twin fingerprint 617a7ba385ec9c12 |
| `fb433dd` | **Apollo — Spider v1.0.4 BAKED** — 20 MB into dist/, byte-identical to the dry-run twins |
| `7fd6128` | the frozen ledger records v1.0.4 — five zips, surface sha 36400d52fc77 |
| `3b2cd7d` | the fourth blind harness, caught before the push — the scratch probe gets its stub and an arm |
| `0588deb` | the dashboard is regenerated — it had been stale since #221 and CI was red on it all along |

Every commit went through `SESSION_N=228 bash knowledge/_git_commit.sh --reconciled` with a
fresh `printf` msgfile and named paths. No `--all-dirty`. One path is left dirty deliberately:
`notes/_REHEARSAL-LOG.jsonl`, the `s137-D1` instrument dirt, which the gates rewrite on every
run — it was committed twice mid-lane where `--release` needed a clean tree, and it is dirty
again now for the same reason.

⚠ **`knowledge/_rulings.json` was committed, not written.** It arrived dirty carrying `s228-D2`
and `s228-D3` as the conductor inscribed them before this lane. Nothing in it was written by
this sub; it is in `ddc7821` because `--release` refuses on any dirty path and the cut could not
otherwise run. The store rows the conductor still owes are unaffected.

---

## GATE ZERO — the local reproduction of CI's step 5

`.github/workflows/gates.yml` step 5 is `python3 knowledge/_build_survey.py --timeout 60`
(step 6 is `_build_all.py`, which was NOT run — a partial run strands the tree and the full
single-process run is sandbox-impossible; CI delivers that verdict). The survey was driven here
in `--range` chunks covering all 140 steps, twice: once on the tree as inherited, once after the
cut.

### REDS BEFORE — 6 named

| step | check | verdict |
|---|---|---|
| `[18]` | `_gm_usage.py --selftest` | **FAIL** — `session #218 testifies DIFFERENTLY in notes/_GAUGE-LOG.md and notes/_GAUGE-LOG.md — one of them is false and this reader cannot tell which. REFUSED.` |
| `[61]` | `gen_dashboard.py --check` | **FAIL** — `dashboard/index.html is OUT OF SYNC` |
| `[124]` | `_validate_package_delta.py --selftest` | **FAIL** — 2 findings: both frozen `_gen_chain.py` copies differ from the source |
| `[135]` | `_gate_release_audit.py --check` | **FAIL** — `THE SHIP LIST ON DISK IS NOT WHAT THE GENERATOR PRODUCES at 1e028a1b5bf9` |
| `[136]` | `_gate_release_audit.py --selftest` | **FAIL** — `the manifest reads version 'v1.0.3' and NO zip in dist/ carries it` |
| `[13]` | `_capture_gate.py --selftest` | ⏱ TIMEOUT at 20s — **NOT A VERDICT** |

### REDS AFTER — 1

| step | check | verdict |
|---|---|---|
| `[18]` | `_gm_usage.py --selftest` | **FAIL, unchanged** — ruling-shaped, see below |

`[124]` `[135]` `[136]` cleared **by the cut**, exactly as the brief predicted. `[61]` cleared by
regeneration. `[13]` is not a red at all: re-driven at a 90 s timeout it **passes** — the 20 s
budget was too tight for this box. CI allows 60 s, which is between the two, so `[13]` may still
time out there; that would be a runner fact, not a verdict, and it says so in its own output.

Final survey, all 140 steps: **57 pass · 1 FAIL · 3 COULD-NOT-ASK · 3 unaskable (all timeouts,
all pass when re-driven alone — see UNPROVEN 3) · 76 not asked (mutating)**. The three refusals
are the standing ones — `_validate_state_contrast` (no playwright here; its proof of record is
the `render` job) and the two `_gen_schematic` arms (`real` tier unreachable, `#173`/`#183`).

---

## ENACTED — 6

### A · `s228-D1` — the ratifying key moves

`knowledge/_release/_gen_pack_manifest.py`, `RATIFY_IDS["v1.0.3"]`: `s224-D1` → `s225-D1`, with
the comment rewritten to say why (`s224-D1` authorised the tier-1 port-back cut; `s225-D1` is
the ratifying word; `s223-D3`'s distinction is between exactly those two). The "rows are ADDED,
never moved" line above it is kept and qualified rather than deleted — it still stands as the
default, and this row moved on Dave's explicit word, which is the only thing that may move one.

A `v1.0.4` row was added beside it. **See ruling-shaped item 1 — its key is a declared judgment,
not a settled one.**

### B · `s228-D2` — the FAB theme key ⇒ **STOPPED, ruling-shaped item 2**

### C · `s228-D3` — the wrapper diet ported into the frozen copies

`_validate_package_delta.check_verbatim_set()` was read first: it compares **byte-identity**
against `knowledge/<name>` for **two** copies, `memento-package/machinery/` (`COPY_A`) and
`memento-package/claude-plugin/memento/machinery/` (`COPY_B`). The source was copied over both.
All four files of `VERBATIM_SET` verified byte-identical in both copies afterwards.

- `_validate_package_delta.py` → `0 failure(s)`, `✅ VERBATIM SET byte-identical (both copies) ·
  shim provenance clean (both chains) · copies identical to each other · no unknown files`
- `--selftest` → all bites pass, including `ARM2(h)`, the control that requires the **real** pack
  to be green.

### D · C8 — the DESIGN-CONTRACT sentence

Appended verbatim to §2 of `apollo-spider/cold-start/DESIGN-CONTRACT.md`. Budget re-measured
after: **3,897 bytes** (199 under the 4,096 byte budget), **30 lines** (10 under the 40-line
budget). `gen_projections.py` regenerated all three projections; `--check` → `OK — 3
projection(s) in sync`.

### E · both wirings

**(1) `_gate_scratch_hygiene.py` at the `--wrap` seam of `_git_commit.sh`.** Placed on the repair
report's recommendation and on the gate's own docstring — *"THE ONLY MOMENT SCRATCH CAN BE
CLEANED IS WHILE ITS OWNER STILL EXISTS"*. Wrap-only and advisory, with `|| true` so the posture
does not depend on the gate's exit code staying 0 forever, and **without** `--clean`: deleting a
worker's live scratch at wrap time is a bigger risk than the litter, and the list is the
deliverable. `_validate_wiring.py` → `0 failure(s)`, the orphan gone.

⚠ **The first draft of that comment broke a different gate, and the mechanism is worth keeping.**
It named `_gate_pack_imports.py` as the advisory precedent. `_validate_wiring.py` reads
`_git_commit.sh` as a **bare substring search** (`re.search(re.escape(name), commit_text)`), so
the mention marked that unwired gate as *wired* and turned its standing exemption into a
`STALE EXEMPTION` failure. Measured, then unnamed — the comment now refers to it without the
filename and says why.

**(2) `gen_projections.py --check` into `gates.yml`.** In the `release` job, BLOCKING, following
that job's step conventions. Home is `release` rather than `gates` because the projections are a
pack surface. BLOCKING per the fourth entry's posture split: mechanical determinism, no
environment input, no legitimate refusal — so no `rc=77` wrapper was written for one, and the
comment says to add that shape rather than `continue-on-error` if it ever grows one.

### F · `s225-D3` — the Gumdrop single-source ships

The enactment was **already in the repo** (the bake-time stamp block in
`build-designer-pack.sh`, the three literals, the corrected manifest sentence). What the ruling
owed at this cut is the *version story*: `MEMENTO_CUT_VERSION` → `v1.0.4` beside `VERSION`, and
the three repo literals moved with it so the stamp is byte-neutral and prints no DRIFT line.

The generator's own selftest fixture had **hand-typed** `("Memento — Gumdrop", "v1.0.3")` and
went red. It now asserts against `VERSION`. That is a strengthening, not a loosening: a typed
literal there asserts the two lineages are independent, which is the opposite of what Dave
ruled, and a bump of one without the other now fails *here* instead of at arm 5 of the pack-docs
gate one release later. The **name** half stays typed — renaming the carried cut is an
independent decision and `s219-D8` is what that bite guards. `--selftest` → **216 bites, 0 fail**.

---

## THE CUT

| | |
|---|---|
| version | **v1.0.4** |
| manifest commit | `b5bee6793a83` |
| ship list | **1,659 files · 41,887,584 bytes · sha256 `93a358f1a46bcb53`** |
| pack | 1,661 files |
| status | `RATIFIED — s227-D6 names v1.0.4 in the store` (read from the store, never typed) |
| carries | `Memento — Gumdrop v1.0.4` |
| zip | `apollo-spider/dist/Apollo-Spider-v1.0.4.zip`, **19,917,389 bytes** |
| **sha256** | **`617a7ba385ec9c127ce6024e23cb5deca87358b5e7ba24d59d1da4aa82a938ca`** |
| dry-run byte-match | ✅ **GREEN** — two dry-runs into two different directories, `cmp` over the whole file: identical to each other **and** to the released zip |
| `--check <zip>` | `CHECK GREEN — matches the manifest at b5bee6793a83` |
| baked-pack audit | `PASS`; v1.0.0…v1.0.3 correctly SKIPPED as frozen history |
| frozen ledger | re-seeded at `fb433dd`: apollo-spider 4 → **5** zips, `b88424fdaf01` → **`36400d52fc77`**, version `v1.0.3` → `v1.0.4` |

### The roster — measured, and where the expectation lives

**58** — held, not drifted. The brief was right to say not to assume it, and wrong about which
group would move.

- **The expectation has no live machine home.** No gate asserts a roster count. The figure lives
  in `s223-D6` in the store (*"the v1.0.2 gates-group roster is RULED at 58 files … 58 becomes
  the figure the release audit holds future probes against for this cut"*), superseding
  `s219-D9`'s 55, and in the **Q6 card** on Dave's go/no-go page, whose receipt bites assert the
  historic **55**, not a current count. The ship-list audit compares the manifest to a fresh
  generation — byte-identity, not a number — so a roster move passes it silently.
- **The cold-start group did NOT move the gates group.** It is a **NEW group of 7 files**
  (`DESIGN-CONTRACT.md`, `REPORT-TEMPLATE.md`, `gen_projections.py`, `verify_placement.py`, the
  three projections) — `s227-D5`/B1's remedy, so the contract now ships.
- **The gates group moved 58 → 59 for an unrelated reason, and was held back.** See
  ruling-shaped item 3.

Other group movement against the v1.0.3 list: `skills` +2 (`grill-me/SKILL.md`,
`grill-me/brief-template.md` — the claim `s227-D6`'s scope names), `gumdrop` +1
(`.github/prompts/grill-me.prompt.md`, the Copilot prompts shim), `engine-canon` +1.

---

## RULING-SHAPED — 3

### 1. `RATIFY_IDS["v1.0.4"]` is keyed to `s227-D6`, and that is a judgment

**Enacted, because without a row `--release` dies and there is no cut — but flagged, because
`s228-D1` sharpens exactly this question.**

`s227-D6` is an **AUTHORISATION** (*"Yes — cut it Monday"*). For v1.0.3 a later, more specific
**ratifying** word existed — `s225-D1` — and `s228-D1` just moved the row onto it. **No later
word exists for v1.0.4.** `s227-D6` is the only ruling in the store carrying Dave's release word
for this version; it names v1.0.4 in its own `ruled` line, and its `says` states that
*"ratification mechanics at the cut follow the standing release machinery (RATIFY_IDS + manifest
regen + dry-run byte-match)"*. It is keyed on that basis and the reasoning is written at the row.

⬛ **If Dave gives a fresh ratifying word at this bake, that row is the one to repoint** — the
same move `s228-D1` just made, and his alone to call.

### 2. `s228-D2` — the FAB theme key: the ruling's `governs` path holds no theme

**STOPPED, per the brief's ⛔ clause. Nothing was changed.**

- The ruling governs `knowledge/_render/gen_fab_meta.py`. Probes: `grep -in theme
  knowledge/_render/gen_fab_meta.py` → **0 lines**; `grep -ci legacy
  knowledge/_render/apollo-fab-meta.json` → **0**. That file generates a `cn-*` → component
  metadata lookup table and contains no theme key, no `data-apollo-theme` string, and no live
  theme context of any kind.
- **The FAB already emits the currently-selected theme.** `Fab.prototype._readTargetState`
  (`knowledge/_render/apollo-fab.js:526-530`) reads `data-apollo-theme` off the target
  document's `<html>`, then its `<body>`, falling back to `'mono'`; `_paint` (`:548-549`) writes
  back whatever that resolved to. So for the page's *initial* state the ruling is already true.
- **The one hardcode is the button's VALUE**, `apollo-fab.js:83-88`: `THEMES` is a static
  `[value, label]` array whose second entry is `['legacy', 'Common']`. It is defined at
  script-parse time, before any document exists.
- **The exact code question:** when a designer clicks **Common** on a page whose current theme is
  `console` or `supercharge` — no common-family evidence on the page at all — which key should
  the button write? `legacy` (today; safe for the 13+ pages carrying their own local
  `[data-apollo-theme="legacy"]` selectors, which canon's alias does not reach) or `common` (what
  `s227-D8` says new work emits)? *"Just use the current theme selected"* gives no answer in that
  case, and a default there is a pick from an incomplete set.
- **Secondary:** does the `|| 'mono'` fallback at `:529` also need to change? It fires only when
  the page carries no theme attribute at all — again, no current theme to read.

### 3. The designer gate roster moved 58 → 59 with nobody deciding — HELD at 58

**Enacted as a HOLD, and it is reversible in one line.**

Regenerating at `ddc7821` moved the gates group 58 → 59 with exactly one addition,
`knowledge/_gate_scratch_hygiene.py`. **Nothing about the #228 wiring caused it** — the gates
selector is a path glob (`knowledge/_gate_*.py`), so the gate has been eligible since it was born
at #227; this is simply the first manifest generated at a commit that contains it.

The block already standing above `RELEASE_SIDE_GATES` says what to do about precisely this:
*"Letting a new gate in raises 55 to 56 with nobody deciding anything … The roster stays at 55
and whether this gate should ship is asked, not assumed."* So the name was added to
`RELEASE_SIDE_GATES` and the roster reads 58, the ruled figure.

The gate probes the **Cowork sandbox VM's disk** — `/var/tmp` and `/tmp` entries by POSIX owner
(`pwd.getpwuid`), against the fact that the VM persists across sessions while each session is a
throwaway Linux user. A designer who unzips this pack has no Cowork VM and no throwaway user;
the gate would ship, be counted, and have nothing to say — word for word the #220 argument for
`_gate_pack_imports.py`.

⬛ **Dave's:** delete the name, regenerate, and the roster reads 59.

---

## FOUND BY DOING, NOT BRIEFED — 4 defects, all repaired

### ① The build script named the zip by hand — and it would have shipped

`build-designer-pack.sh` typed `VERSION="v1.0.3"` at the head of the file, 75 lines above the
generator it follows. The **first dry-run of this cut** staged v1.0.4 content, wrote a v1.0.4
README and a v1.0.4 PROVENANCE, and named the zip **`Apollo-Spider-v1.0.3.zip`** — a pack that
disagrees with itself about which release it is, under a filename a designer reads as the
previous cut. Caught by reading the dry-run's **output**, not its exit code (it exited 0).

`VERSION` and `PACKNAME` are now read from the manifest, inside the `dryrun|release` branch,
**after** the manifest has been proved to be the one for this commit. Deriving earlier would read
whatever manifest is on disk — the stale-copy-list defect wearing the version's clothes.
`--manifest`, `--check` and `--selftest` never touch either variable.

### ② `FIRST-SESSION.md` told designers to open a `v1.0.3` directory

Caught by the pack-docs gate's arm 3 in the same dry-run, by name: *"this pack is v1.0.4 — a
version string was not swept at bake"*. Fixed at source, which is what that gate's guidance says.

⚠ **PRICED, NOT TAKEN:** arm 3 is ADVISORY, so it found the defect and exited 0. Two hand-typed
pack-version literals surfaced at one cut. The permanent fix is either promoting arm 3 to
blocking, or extending the bake-time Gumdrop stamp to `Apollo-Spider-v*` the same way. Both are
⬛ **Dave's** — promotion is his by derivation governance, and widening a stamp ruled for the
carried cut is a ruling, not a tidy.

### ③ The frozen ledger's version literal had been stale since #225 — and `--seed` downgraded it

`_gate_frozen_release.py` declares each surface's version as a **literal**, and `seed()` takes
the row's version from there. It still read `v1.0.2` while the ledger JSON read `v1.0.3`: the
#225 bake moved the JSON and left the literal behind. So the **first `--seed` of this cut
DOWNGRADED the recorded row v1.0.3 → v1.0.2** — a ledger saying a shipped release is older than
it is, written by the instrument that exists to stop exactly that.

Caught by reading the seed's printed table against the file it had just rewritten. **No arm
compares that literal to the manifest's `VERSION`**, and the laundering arm cannot see it: it
keys on `content_sha256` alone. Bumped to `v1.0.4`; the ledger re-seeded correctly afterwards
and `--check` passes.

⬛ **The permanent fix is Dave's and is priced, not taken.** This is a fourth version home
(generator · build script — fixed here · ledger literal · ledger JSON). The remedy is to derive
it from the manifest the way the build script now does, or to add an arm that fails when the two
disagree. Deriving it would make the gate read the thing it audits, which is a design decision.

### ④ The fourth blind harness — `W-33` bit before the push

Wiring the scratch probe into `_git_commit.sh` made `_test_git_commit.py` **blind**: the harness
builds a fixture `knowledge/` dir and every gate the script invokes needs a stub there, or every
commit-path arm crashes on `can't open file` and the crash reads as a result
`[[a-crash-is-not-a-fail]]`. The detector named it: *"BLIND HARNESS — `_git_commit.sh` invokes
unstubbed gate(s): `_gate_scratch_hygiene.py`"*, and its mutation control went red alongside for
the right reason.

**Fourth occurrence** — #188 doc-row, #191 showroom, #208 mention-map, #228 this. At #208 it
reached CI; here it was caught locally, and **only because the survey was re-run after the
wiring rather than before it**.

The fix is a stub **plus an arm that drives it**, not a stub alone — the #192 doc-row arm's own
docstring is the reason (*"a stub existed for this gate since #191 but NOTHING drove it, so
`STUB_DOC_ROWS_EXIT` was never once non-zero — the stub made the arms RUN, not the gate
TESTED"*). `scratch_hygiene_advisory_on_wrap_228` forces the probe **red** and asserts the wrap
commit lands anyway — that is the entire content of ADVISORY — and asserts it **absent** from a
non-wrap commit. `--selftest`: **ALL GREEN, 33 arms (was 32), 6 mutation controls.**

---

## UNPROVEN — 3, declared

1. **`_build_all.py` (gates.yml step 6) was NOT run.** Deliberate: a partial run strands the
   tree, and the full single-process run is sandbox-impossible. **CI delivers this verdict.**
2. **`knowledge/_tests/test_gates.py` (step 7) CANNOT run on this box, and it is arithmetic, not
   a regression.** It `shutil.copytree`s the whole of `knowledge/` into a temp dir. `knowledge/`
   is **5.2 G**; the VM disk has **4.9 G free** and the session volume **3.2 G**. Driven twice,
   with `TMPDIR` on each disk in turn: `[Errno 28] No space left on device` both times, before
   any arm ran. Nothing in this lane touched a gate's failure class; the CI runner has the room.
   `test_advisory.py` (step 8) was not driven for the same reason.
3. **Three steps are TIMEOUT-SENSITIVE on this box and may report red in CI as a runner fact.**
   `[13]` `_capture_gate --selftest`, `[119]` `_gen_chain --selftest`, `[126]` instrument-fit.
   Each times out at 20–25 s and each **passes** when re-driven alone at 80–90 s. CI's budget is
   60 s, between the two measurements. A timeout is explicitly *"not a verdict"* in the survey's
   own words — nobody asked the step and it never said why — so if one appears in CI, re-drive it
   before treating it as a finding. This lane touched none of the three.

Also unproven and unchanged: `_validate_state_contrast` and the two `_gen_schematic` arms refuse
here (`COULD-NOT-ASK`, 77) — standing refusals with their own proofs of record.

---

## THE ONE RED LEFT — `[18]`, and it is Dave's

`_gm_usage.py --selftest` fails on `notes/_GAUGE-LOG.md`: *"session #218 testifies DIFFERENTLY in
notes/_GAUGE-LOG.md and notes/_GAUGE-LOG.md — one of them is false and this reader cannot tell
which. REFUSED."*

The two testimonies, both inside the single `#218` block (the one-block-per-session contract
forced the second wrap to append rather than open a new block):

- **`:2276`** `section-usage #218 (self-report, delegated OPUS wrap sub)` — `PRIOR:R … C1:R …
  LANES:R`
- **`:2294`** `section-usage #218 (second wrap; self-report, delegated OPUS wrap sub)` —
  `PRIOR:U … C1:U … LANES:U`

Not repairable mechanically: both are **ratified record**, the header-wins rule says add and
never trim, and choosing which is false is a judgment about what two sub-sessions actually did.
The honest shapes are (a) teach the reader that a session may testify twice and how to fold two
self-reports, or (b) Dave rules which line stands. ⬛ **His.**

---

## WHAT WAS DRIVEN

```
knowledge/_build_survey.py --range 1:45 / 46:100 / 101:140      before  6 reds named
knowledge/_build_survey.py --range 1:70 / 71:140                after   1 red
knowledge/_build_survey.py --timeout 90 --range 13:13           rc=0    ([13] is a timeout, not a verdict)
knowledge/_validate_package_delta.py                            rc=0    0 findings (was 2)
knowledge/_validate_package_delta.py --selftest                 rc=0    all bites, real-pack control green
knowledge/_validate_wiring.py                                   rc=0    50 wired · 0 failures (was 1 orphan)
knowledge/_test_git_commit.py --selftest                        rc=0    33 arms, 6 mutation controls
knowledge/_git_commit.sh --selftest                             rc=0    14 bites
knowledge/gen_dashboard.py --check                              rc=0    in sync (was FAIL)
knowledge/_release/_gen_pack_manifest.py --selftest             rc=0    216 bites, 0 fail
knowledge/_release/_gate_release_audit.py --check               rc=0    byte-identical at b5bee67
knowledge/_release/_gate_release_audit.py --selftest             rc=0    10 bites, 0 fail
knowledge/_release/_gate_release_audit.py --pack                rc=0    the v1.0.4 zip matches the manifest
knowledge/_release/_gate_frozen_release.py --check              rc=0    3 arms, no frozen surface moved
knowledge/_release/_gate_frozen_release.py --selftest           rc=0    14 bites, 0 fail
apollo-spider/build-designer-pack.sh --selftest                 rc=0    all three refusals driven
apollo-spider/cold-start/gen_projections.py --check             rc=0    3 projections in sync
build-designer-pack.sh --dry-run  (x2, two directories)         rc=0    cmp: BYTE-IDENTICAL
build-designer-pack.sh --release --commit b5bee67               rc=0    617a7ba385ec9c12…
build-designer-pack.sh --check <zip> --commit b5bee67           rc=0    CHECK GREEN
python3 -c "yaml.safe_load(gates.yml)"                          ok      3 jobs, release job 13 steps
```

⚠ **The #219 finding is still live in TWO modes, not one.** `--manifest` rewrites
`reviews/RELEASE-SPIDER-2026-08-26-v1.html` and strips the review pair's stamps — it says so in
its own output, and the overlay was re-injected with `_review/_make_review.py` each time.
**`--dry-run` writes it too**, which the finding does not record, and that is why `--release`
refused once on a tree already believed clean. Both pages were reconciled and committed; the
dry-run's write is a real enrichment (the zipped download size and the twin-fingerprint receipt
now stand on Dave's go/no-go page).

---

## REPLAY-THESE — for the conductor

1. **Push** `master` — ten commits, `ddc7821` … `0588deb`, tip `0588deb`. ⛔ Read CI back: the
   `gates` job's step 5 should now be red on **`[18]` only**; the `release` job should be green
   throughout, including the new BLOCKING cold-start projections step. `[13]` may time out at
   CI's 60 s — that is a runner fact, printed as such.
2. **Mint the store rows** this lane did not write, and note that `knowledge/_rulings.json` is
   already committed at `ddc7821` carrying `s228-D2`/`s228-D3` unmodified.
3. **Put ruling-shaped item 1 to Dave** — is `s227-D6` the ratifying word for v1.0.4, or does he
   want to give a fresh one at the bake and repoint the row?
4. **Put ruling-shaped item 2 to Dave** — the FAB's *Common* button on a `console` page. The
   store's `governs` for `s228-D2` names a file with no theme in it; the live site is
   `apollo-fab.js:83-88`.
5. **Put ruling-shaped item 3 to Dave** — does the Cowork scratch probe belong in the designer
   roster? 58 today, one line from 59.
6. **Price the two permanent fixes** found by the bake: pack-version literals (arm 3 blocking, or
   extend the stamp) and the frozen-ledger version literal (derive from the manifest, or an arm
   that fails on disagreement).
7. **`[18]` is Dave's** — which #218 self-report stands, or teach the reader to fold two.
8. Dave's **live cold test on a second machine** is the acceptance test for v1.0.4, per
   `s227-D6`, and follows the push.
