# #298-B — the brain turns to reading B on Dave's pick

session: #298 · 2026-09-23
window: #298 conductor, lane B (Opus 5.5, remote-device seat)
sub index: B
brief: the conductor's in-chat brief to lane B (no brief file). Dave's whole word: "B".
tokens: UNMEASURED — this lane's message.usage cannot be read from the device seat

## VERDICT

**DONE.** Reading B is in the deck. The brain on slide 10 ("Fourth: a designer's judgement, built into the system.") turns +8° on the yaw toward the words, from the shared −35 / 20 to **−27 / 20**. The Judgment thumbnail on slide 12 follows it by construction. Nothing else in the deck moved: every other slide is pixel-identical at 1920×1080 and at 1440×900, and the DOM text, pagenums, rail checks and overflow checks are unchanged. One constant moved in the source. The deck was rebuilt with `build_c.py` and never hand-edited, and `build_e.py` was not run. **Three things to know.** (1) The brief's suggested line 1 for the commit would have been refused by the committer's reuse gate, so line 1 carries no `#298 date —` shape and the committer added its own `after #298 2026-09-23 — ` prefix. (2) The doc-row gate needs a store row for this report, and the brief limited the commit to four paths, so the gate was passed as a DECLARED gap. The row is owed. (3) The instrumentation auto-stage was switched off for the same reason.

COUNTS: findings 9 · ruling-shaped 0 · UNPROVEN 2

## What was done

1. **Reading B, verified from lane A's own files, not guessed.** Lane A's variant source `notes/_lanes/297/A/_work/src-yaw-27.html` (the source behind `_work/r-27/10-s7b.png`, panel B of `notes/_lanes/297/A/brain-pivot-readings.png`) was diffed against the source as committed in `57e885b0`. The two files differ in exactly **one line**, the brain's camera line:
   - A (committed): `var YAW0 = -35*Math.PI/180, PIT0 = 20*Math.PI/180;`
   - B: `var YAW0 = -27*Math.PI/180, PIT0 = 20*Math.PI/180;`
   Lane A's report (`notes/_subreports/2026-09-22-297-A-plain-deck-brain-and-footnotes.md`, section "THE THREE PIVOT READINGS") gives the same line for B. `sheet.py` shows the B panel is `_work/r-27/10-s7b.png`, captioned "yaw -27° / pitch 20°".
2. **The edit, in the source only:** `notes/_lanes/296/C/v14-plain-before-c.html`.
   - The brain's camera line (the one just below the `#297 A` comment, now line 3404) went from `YAW0 = -35*…` to **`YAW0 = -27*…`**. `PIT0` stays at 20. AOV 33, `YAW_R` 25 and `PIT_R` 12 are unchanged. The other nine `YAW0 = -35` lines belong to the books, gearbox, shells, callipers and the other drawings, and are untouched.
   - ⚠ **Declared: two non-rendered comments updated so they stay true.** The JS comment above the line no longer says "A -35 (this)". It now records "#298 B: Dave picked B ("B") - YAW0 -35 -> -27 (this), PIT0 20 kept", and notes that the plate turns off the shared parallel. The s7b HTML comment (line 570) gains "#298 B: Dave picked reading B, rest yaw -27 / 20". Neither is rendered, and the pixel and DOM checks below show nothing visible besides the brain moved.
   - Size: the source went from 510,165 to 510,375 bytes. The rebuilt deck went from 523,108 to 523,318 bytes. The deck's diff against its pre-edit copy is 10 changed lines, all of them from the edits above.
3. **Rebuild:** `python3 notes/_lanes/296/C/build_c.py`, run twice, with the same output (`git hash-object` d1c980eb…). The premise held: before the edit, the deck, the source and `build_c.py` were each byte-identical to HEAD (`8ba44da0`), checked with `git hash-object` against `git rev-parse HEAD:<path>`. `git status` was never run, and there were no `.git/*.lock` files at the start.
4. **Render matrix, at the seat, on the mount.** Each render ran in one bash call: `export TMPDIR=/dev/shm; bash knowledge/_render/ensure_env.sh; source knowledge/_render/seat_env.sh; python3 notes/_lanes/297/W/_work/shoot_w.py <deck> <dir> WxH`. That harness uses `page.goto("file://…")`, `executable_path=$RENDER_SHELL`, and lane A's freezes and pointer block. Each viewport took about 43 s, one per call. Diffs were done with `notes/_lanes/297/W/_work/pdiff_w.py`. Working folders, not committed: `notes/_lanes/298/B/_work/before-1920`, `before-1920-rep`, `before-1440`, `before-1440-rep`, `after-1920`, `after-1440`, with 17 renders each (16 slides plus slide 11's second state).
5. **The before/after sheet:** **`notes/_lanes/298/B/brain-B-before-after.png`** (3024×2894). It has three rows, each with BEFORE (A, −35 / 20) and AFTER (B, −27 / 20) side by side: slide 10 in full, slide 12 in full, and a close-up of slide 12's Judgment cell. The panes come from the 1920×1080 renders. The driver is `notes/_lanes/298/B/_work/sheet_b.py` (not committed).

## Render checks

**Pixel diff, the unedited deck (second shot) against the rebuilt deck.** The same result held at both viewports:

| slide | 1920×1080 | 1440×900 |
|---|---|---|
| 01 s1 … 09 s8 | SAME | SAME |
| **10 s7b** (the designer's brain) | **DIFF**, content bbox 908–1579 × 250–830 (the drawing only) | **DIFF**, 738–1302 × 206–695 |
| 11 s6b, 11b s6b (second state) | SAME | SAME |
| **12 s10** (three main elements) | **DIFF**, bbox 1215–1451 × 386–590 (the Judgment thumbnail only) | **DIFF**, 1020–1246 × 301–497 |
| 13 s10map … 16 s12 | SAME | SAME |

- The rail band is SAME on every slide.
- **DOM facts** (section order, element counts, the full slide text, pagenums 01 / 16 … 16 / 16) are SAME at both viewports.
- The rail-collision and text-overflow checks are identical before and after. The one flagged render, 11b-s6b with 4 box-overflow items on the "Proposal: sign off the system…" line, is pre-existing and unchanged.
- There were 0 page errors and 0 console errors before and after.
- **Anatomy (slide 12):** `an3` ← `brPrint` went from 34,226 to 34,102 bytes, and its ink crop from 671×580 to 671×559. The shared plate scale stays at **0.3524**, so the Components (`an1`) and Knowledge (`an2`) cells are byte-identical.

**The brain matches reading B.** The deck's layout has moved on since #297 A: at 2× the brain is drawn 1128 px wide, where lane A's sheet had 1244. Pixel identity with lane A's frame is therefore not available. So s7b was shot alone at 1440×900 at 2× (lane A's scale, driver `notes/_lanes/298/B/_work/shoot_s7b.py`). Each brain was cropped to its ink box and resampled to one size. The comparison against lane A's three readings:

| this lane's frame | vs A (−35) | vs B (−27) | vs C (−19) |
|---|---|---|---|
| **after** | IoU 0.076 · MAD 14.19 | **IoU 0.839 · MAD 1.99** | IoU 0.077 · MAD 14.73 |
| before (control) | **IoU 0.883 · MAD 1.39** | IoU 0.078 · MAD 14.18 | IoU 0.053 · MAD 15.10 |

The ink-box aspect ratios agree too: after 1.1975 against B's 1.1985, and before 1.1557 against A's 1.1561. **By eye** (the sheet and `brain-pivot-readings.png`, both read at this seat): the plate, the frontal pole and the red groove sit as in panel B. The brain is fully in frame, and nothing is clipped on slide 10 or in slide 12's cell.

## Findings

1. **Reading B is one constant: the brain's `YAW0`, −35 → −27.** Probe: `diff <(git show 57e885b0:notes/_lanes/296/C/v14-plain-before-c.html) notes/_lanes/297/A/_work/src-yaw-27.html` returns exactly one changed line (3397). Lane A's report table and `sheet.py` say the same.
2. **The brain's plate is now off the shared parallel, as lane A warned.** Its plate edges on screen are **9.89° / 33.87°**, against 13.47° / 26.03° for the other drawings at −35 / 20. I re-derived these at this seat as atan(sin 20° · tan 27°) and atan(sin 20° / tan 27°). The formula also reproduces lane A's A and C figures (13.47 / 26.03 and 6.72 / 44.81). Dave saw the trade-off on the sheet before he picked.
3. **Slide 12's Judgment thumbnail changes by construction**, not as a side effect (`an3` ← `brPrint`). Probe: the pixel-diff bbox is the cell only, and `anatomyState()` shows the brPrint bytes and crop changing while the shared scale 0.3524 holds.
4. **The harness has one sub-visible flicker.** On 07 s6 at the rail, 10 pixels differ (max channel sum 3 / 765). They differed the same way between two shots of the **same unedited deck**, at both viewports (bbox 34–42 × 274–277 at 1920 and 34–42 × 248–251 at 1440). It is not the edit. Against the second before-shot, the after deck differs **only** on 10 and 12. Probe: `pdiff_w.py before-<v> before-<v>-rep`, then `before-<v>-rep after-<v>`.
5. **The layout has moved on since #297 A**, so a pixel match with lane A's B frame is not available (the ink box is 1128 wide here against 1244 there, and the text column moved). The match was proven on normalised ink crops instead (table above). Probe: `s7b-after-2x.png` and `s7b-before-2x.png` in `_work/`.
6. ⚠ **The brief's suggested commit line 1 would have been refused.** `#298 2026-09-23 — …` is the wrap-shape T3 prefix, which the reuse gate counts (`prefix_count`, `^(?:after )?#\d+ \d{4}-\d{2}-\d{2} — `). A non-wrap run would stack it into `after #298 … — #298 … — …`. Probe: `knowledge/_git_commit.sh` lines 312–326 and 434–440, and its selftest arm "wrap-into-non-wrap stack counts 2". So the msgfile's line 1 is the plain summary, "the brain turns to reading B on Dave's pick", which does not start with "after #N". T3 generates the committed subject `after #298 2026-09-23 — the brain turns to reading B on Dave's pick`, the same shape as every non-wrap commit in the log.
7. ⚠ **The doc-row gate is passed as a DECLARED gap, not rowed.** This report is a filed sub-report added in this commit, so `_gate_doc_rows.py` (post-staging) needs a `_state.json` row whose `home` names it. That is what refused lane A's first attempt at #297 (`notes/_lanes/297/A/_gitcommit-A1.log` line 238). The brief limits this commit to four paths, and `knowledge/_state.json` is not one of them. So the commit runs with `DOC_ROW_ACK` set to a stated reason, and **a store row for this report is owed to the conductor.** Related: row **W-297a**'s close condition ("Dave has picked a brain pivot reading … and ruled or parked the chapter-rail title overlap…") is now **half met**: the pick is B. The rail half is not this lane's.
8. ⚠ **The instrumentation auto-stage was switched off** (`INSTRUMENT_AUTOSTAGE=0`). Otherwise the committer would add `notes/_REHEARSAL-LOG.jsonl` (#261 M2), including any append made earlier in the session at the conductor's seat, and the brief limits the commit to four paths. Any append the gates make stays uncommitted dirt for the conductor. What happened is recorded in § POST-COMMIT.
9. **Two non-rendered comments were updated** so the source stays true (What was done, item 2). Probe: the source diff is 2 hunks, 1 + 3 lines, and the pixel and DOM checks show nothing visible besides the brain moved.

## RULING-SHAPED QUESTIONS

none. Dave ruled B. The plate coming off the shared parallel was on the sheet he picked from (lane A's "trade-off Dave should see").

## UNPROVEN / CLAIMED

- **UNPROVEN:** print (Cmd-P) was not rendered. `brPrint` is regenerated (its bytes changed), but no printed page was looked at. Price: one print-to-PDF render of slides 10 and 12.
- **UNPROVEN:** only the rest pose was rendered. Under the mouse the orbit is ±25°, so B now reaches −2° to −52° yaw. Lane A reports that the fit is re-derived from the orbit sweep at every YAW0 (CLAIMED from its report, not re-probed here). Price: two renders with `brSet(±25, 0)`.

## PATHS

Changed, committed: `notes/_lanes/296/C/v14-plain-before-c.html` (the source) · `notes/_DEMO-SLIDES-apollo-2026-09-22-v14-plain.html` (rebuilt).
Created, committed: `notes/_lanes/298/B/brain-B-before-after.png` · this report.
Working files, **not committed**: `notes/_lanes/298/B/_work/` (the pre-edit copies `deck.before.html` and `source.before.html`, the six render folders with `_facts.json`, `s7b-before-2x.png`, `s7b-after-2x.png`, `shoot_s7b.py`, `sheet_b.py`) · the msgfile `notes/_lanes/298/B/_msg-B1.txt` and the committer's transcript.

REPLAY-THESE: none — the stub carries everything.

---

## § POST-COMMIT — added AFTER the commit, NOT in it (by addition)

- **Commit `f54562e2`** (`f54562e2f84aa54c0885be1340a7799a4b91965f`), subject `after #298 2026-09-23 — the brain turns to reading B on Dave's pick`. It was made with `knowledge/_git_commit.sh` in one run, with `SESSION_N=298`, `INSTRUMENT_AUTOSTAGE=0`, `DOC_ROW_ACK` declared, and `--quiet`. The full transcript is `notes/_lanes/298/B/_gitcommit-B1.log` (exit 0, 53 s).
  - **4 files, +95 / −8:** the source, the rebuilt deck, `notes/_lanes/298/B/brain-B-before-after.png` and this report. Nothing was auto-staged.
  - Gates: reuse gate passed · chain fresh · doc-row DECLARED GAP · showroom in sync · polarity green · mention map fresh · session witness agrees (#298) · wrap gate and spine selftest green (declared not-a-wrap) · subject identical to T3's headline, exactly one prefix · `✓ done — locks clear`.
  - The script's own `clear_locks` moved two `index.lock` files that git spawned during this run into `_to_delete/_stale_locks/` (`index.lock.1790160745258026281` and `index.lock.1790160747595094323`). That is its built-in mechanism. **No lock was stranded:** `.git/*.lock` and `.git/refs/heads/*.lock` were empty after the run, and no orphan-lock recipe was needed.
- **Read-back:** each of the four paths on disk is byte-identical to `HEAD` (`git hash-object` against `git rev-parse HEAD:<path>`). The deck in HEAD is byte-identical to a fresh `build_c.build()` of the source in HEAD (523,318 bytes).
- ⚠ **Left uncommitted, as declared in finding 8.** This run's gates appended **796 bytes** to `notes/_REHEARSAL-LOG.jsonl` (md5 af307bb2f28e → e95c6ba8bfdc, 2,157,671 → 2,158,467 bytes). With the auto-stage off, that append and any earlier ones in the session stay dirt for the conductor's next commit. `knowledge/_graph-mark-observations.jsonl` and `notes/_dream/_GRADE-DECISIONS.jsonl` are unchanged.
- The committer counted **61 dirty paths not staged**. They belong to other seats or are this lane's working files, and none were touched. `git status` was not run by this lane.
- **Not pushed**, per the brief.
- **Owed to the conductor:** a `_state.json` row whose `home` is this report (the doc-row gate was passed DECLARED), and an update to W-297a's close condition, whose pick half is now met (B).
- This § POST-COMMIT tail is itself uncommitted.
