# #255 BUILD LANE B — `_validate_composition.py --selftest` aborts

**Date** 2026-09-07 · **Form enacted** `s253-D2` (repair the reader's anchors to the real artefact,
never narrow or weaken the gate)
**Scope** `knowledge/_validate_composition.py` ONLY, and inside it only `selftest()` — the anchors
and the two arms whose mutation is derived from an anchor. No gate condition touched (C9/C1/C7/C8/C4
and their readers are byte-identical to HEAD). No artefact edited. `_pack_gate_probe.json` restored
to HEAD's copy (manifest pinned at f6a8340; re-cutting is Dave's, `s219-D4`).
**Verdict** the ABORT is GONE and all 17 arms now RUN (was: 0 arms ran). 12 arms GREEN, 5 arms RED.
The 5 remaining reds are ONE fact, not five: **the real artefact is C9 RED at HEAD** and has been
since 0979a9b (#249). That is ruling-shaped, not a lane repair — see below.

`COUNTS: claims 11 · green 10 · red 1`

---

## BEFORE — the state this lane inherited

```
$ git show HEAD:knowledge/_validate_composition.py > knowledge/_head255_vc.py
$ python3 knowledge/_head255_vc.py --selftest
  File ".../knowledge/_head255_vc.py", line 378, in selftest
    for a in (KPI, GROUP_GAP, WALL_GAP): assert real.count(a) == 1, a
AssertionError: <div class="c-bento__tile kpi-tile has-cta" role="group" aria-label="Closing balance" data-c="3" data-r="1">
```

Zero arms ran. The pack probe classifier (`_gen_pack_manifest.py` L1038–1045: rc 0 → `green`,
traceback → `crashed`, else → `red`) therefore reads `crashed` for this gate at commit HEAD.

Anchor drift, measured against the commit the anchors were written at (5ae4d32, #245):

```
$ python3 -  # counts of each anchor in the artefact, now vs at 5ae4d32
DECL      now= 2   at 5ae4d32= 2      <- intact
KPI       now= 0   at 5ae4d32= 1      <- DRIFTED (the abort)
GROUP_GAP now= 1   at 5ae4d32= 1      <- intact
WALL_GAP  now= 1   at 5ae4d32= 1      <- intact
```

Exactly one anchor drifted. `DECL`'s `== 2`, `GROUP_GAP`'s and `WALL_GAP`'s `== 1` are untouched.

---

## PER-ANCHOR

### KPI — REPAIRED (re-pointed, not loosened)

**WHAT** `data-c="3"` → `data-c="1"` in the anchor string; the `== 1` assertion is unchanged.
**WHY** #249/#251 (0979a9b / fc1209b, DP-08 + `s247-D3`/`s251-D1`) took the two status tiles out of
the lead row, and page rule 10a pinned that grid to four columns at every band, so the lead group's
four true KPIs are now `data-c="1"`. Same tile, same aria-label, same uniqueness contract.
**RECEIPT**
```
$ grep -n 'Closing balance' knowledge/snippets/Template-dashboard-bento.reference.html
991:        <div class="c-bento__tile kpi-tile has-cta" role="group" aria-label="Closing balance" data-c="1" data-r="1">
```

### DECL / GROUP_GAP / WALL_GAP — UNCHANGED

**WHAT** nothing. **WHY** each still resolves to its ruled count on the current artefact (2 / 1 / 1,
table above). **RECEIPT** the gate's own read of the live artefact still names them:
```
C1 · 'This month' line 987: gap 4px  <- .tpl-page .c-bento.tpl-group[data-bento-role="dashboard"]      (GROUP_GAP)
C1 · 'Account overview' line 972: gap 40px <- ...tpl-wall...:has(> .c-bento__grid > .c-bento)          (WALL_GAP)
C9 · bands read off the artefact: base=6 cols · <=1100px=3 · <=820px=2 · <=520px=1                      (DECL)
```

---

## THE TWO ARMS RE-DERIVED (meaning kept, mutation re-computed)

C9 has two legs. The arms were split across them at #245 and stay split.

* **M1 — SPAN LEGALITY leg.** Was `data-c 3 → 2` (2 does not divide the 3-column band). On a
  `data-c="1"` tile that mutation is a no-op class, so it is re-derived as `1 → 4`: 4 does not
  divide 6 at the base band. Same leg, same code.
* **M2 — SUM-TO-WHOLE-ROWS leg.** Was `3 → 6` (divides at every band, grid stops summing to whole
  rows). Re-derived as `1 → 3`: 3 divides 6 / 3 / clamps to 2 / clamps to 1 at every band, and the
  grid's spans sum to 5 at `<=820px` (2 cols) — an orphan cell. Same leg, same code.

**RECEIPT** both produce the named condition on the CURRENT artefact:
```
GREEN  M1 · one KPI tile data-c 1 -> 4 (4 does not divide 6: span-legality orphan at the base band)  (rc 1, want 1)
GREEN  M2 · one KPI tile data-c 1 -> 3 (divides at every band, but the grid no longer sums to whole rows at <=820px)  (rc 1, want 1)
```

No arm deleted. Arms before **17**, arms after **17**. No `== 1` relaxed to `>= 1`. No condition,
reader, verdict rule or `BLOCKING_ARMS` entry changed.

---

## AFTER

```
$ python3 knowledge/_validate_composition.py --selftest
GREEN  M1 … M2 … M3 … M4 … M5 … M6 … M7 … M8 … M8b … M9 … M9b … K4      (12 green)
RED    R  · the REAL artefact as shipped -> GREEN                       (rc 1, want 0)
RED    R0 · the artefact with its column-count literal STRIPPED         (rc 1, want 77)
RED    K1 · control: group gutter 4 -> 2                                (rc 1, want 0)
RED    K2 · control: wall gutter 40 -> 24                               (rc 1, want 0)
RED    K3 · control: `border:`/`--border:` must not read as `order:`    (rc 1, want 0)
selftest: 17 arm(s) · 5 failed
EXIT=1 ; Traceback count in output = 0
```

Every one of the five failing arms is an arm that expects the REAL BASE to be clean. Their printed
reds are **only C9 reds, and only the artefact's own pre-existing five** — no C1, C7, C8 or C4 red
appears anywhere in the failing output, so the three controls (K1 gutter still legal, K2 gutter
still legal, K3 the `border:`/`order:` substring trap) still hold their own meaning; they fail on
the inherited base alone. R0's three reds are the `<=1100px` subset, as expected once the column
literal is stripped.

The real gate on the real artefact, its normal invocation (docstring L27):

```
$ python3 knowledge/_validate_composition.py knowledge/snippets/Template-dashboard-bento.reference.html
C9 RED  line 1066 data-c=4 in 'Account overview': span 4 does not divide 6 columns at band base
C9 RED  line 1124 data-c=2 in 'Account overview': span 2 does not divide 3 columns at band <=1100px
C9 RED  grid 'Account overview' (973): spans sum to 8 at band <=1100px (3 cols) - not whole rows
C9 RED  grid 'This month' (989): spans sum to 4 at band base (6 cols) - not whole rows
C9 RED  grid 'This month' (989): spans sum to 4 at band <=1100px (3 cols) - not whole rows
_validate_composition: RED  … C9 reds 5 [BLOCKING] · C1 0 · C7 0 · C8 0 · C4 0 · unproven 0
```

Bisected over the artefact's own history (`git show <sha>:…` → the gate):

```
5ae4d32 (#245)  GREEN   C9 reds 0
0979a9b (#249)  RED     C9 reds 3
fc1209b (#251)  RED     C9 reds 5
dbf2780 f0191c0 202ae6f (HEAD)  RED  C9 reds 5
```

Pack probe: a fresh `--probe --commit HEAD` stages from the COMMIT, not the worktree, so it cannot
see this lane's edit — it still reads `crashed`, and `_pack_gate_probe.json` was restored to HEAD's
copy immediately. Applying the classifier's own rule to the worktree run above (rc 1, no traceback)
gives `red`, not `green`. **`selftest: green` is unreachable while the artefact is C9 red.**

---

## RULING-SHAPED

1. **The flagship bento artefact fails its own BLOCKING gate, and has since #249.** C9 blocks the
   screen (`s245-D7` Q4 (b)); this red is the arm `_validate_screen.py` imports. It is not a lane
   repair either way — closing it means either the artefact moves or the gate's C9 conditions move,
   and both are Dave's.
2. **All five reds trace to two deliberate, ruled declarations the gate's reader cannot see.**
   Rule 10a (`grid-template-columns:repeat(4,…)` on the lead grid, `s247-D3`/`s251-D1`) and rule 6b
   (i) (`grid-column:1 / -1` at `<=1100px`, `s248-D1`). The artefact says so itself at rule 6b:
   "C9 … reads only canon-shaped clamps, so it does not see (i)". Teaching the reader those is
   non-selftest code and was fenced out of this lane by the brief.
3. **`s248-D1`'s 4+2 wall and C9's span-legality leg contradict each other.** 4+2 sums to a whole
   6-column row but 4 does not DIVIDE 6, so C9 reds a split Dave ruled. Whether the divisibility leg
   survives that ruling is a decision, not a fix. Arms R / R0 / K1 / K2 / K3 stay as authored,
   red, until it is made.
