# `#253`-`wrap` — the delegated capture ritual: three rulings inscribed, eighteen calls left standing as unvetoed, and the runner's red moved from step 8 to step 18

session: `#253` · 2026-09-07
window: delegated wrap sub (OPUS 5) — ⚠ **the conductor's transcript was NOT read at this seat**
sub index: `wrap`
brief: chat-only wrap brief from the #253 conductor (no brief FILE was written this session)
tokens: `UNMEASURED — a sub cannot read its own message.usage, and unlike #250…#252 this seat took
no _checkin.py reading of the conductor's transcript either. His FILL 134,053 real over ~60 turns
and his boot 69,664 are DECLARED figures, recorded as HIS and corroborated by nothing here.`

provenance: 253 · 2026-09-07
status: observed

## VERDICT

The ritual ran whole — steps 1, 1b, 2, 2c, 2d, 2e, 2f, 2g, 3 (declared), 4, 4b, 4c, 5 — and every
move went through `_gm_move.py`. **The wrap gate closes at `192 in scope · 1 fail · 41 warn`, and
the single fail is the standing `boot_constant_drift_check` on breaches #247 70,822 and #248
70,974.** That is the seventh consecutive wrap at which `--wrap` is refused, and the commit is
therefore made in the #243 form. Nothing was faked, no constant was moved, and #253's own
under-ceiling boot was not used to argue the arm shut. **Three of Dave's rulings were inscribed and
read back at entries 388/389/390; the eighteen review-page calls were NOT inscribed — they are
recorded as CLAUDE-AUTHORED and UNVETOED, which is what his answer actually left behind.**

COUNTS: findings 8 · ruling-shaped 3 · UNPROVEN 4

## What was done, by step

- **1** — `_LIVE-STATE.md` `Last refreshed` rebuilt for #253 (the #252 summary demoted to
  `Previous:`, the #250 segment trimmed out to the archive);
  `_validate_standing_instructions.py` **PASS** (28 standing docs reachable).
  `knowledge/_REVIEW-SIGNOFF.md` **+2 rows** — `reviews/META-TAGS-A-2026-09-07-v1.html` and
  `-B-`, both logged as *SEEN, NOT STRUCK — the veto is open*.
- **1b** — `_DECISION-HISTORY/2026-09-07-253-meta-tags-landed.md`, provenance + status present,
  eight sections, both-way linked.
- **2** — new ★ LATEST banner (#253); `TITLE THE NEXT CHAT →` set to
  `Apollo - #254: strike the tags` (the conductor's label, per the brief); `size:` stamp written
  LAST and iterated to a fixed point **three times** (each later edit to GM moved it).
- **2c** — #252 banner demoted to ★ PRIOR; **#251 banner, 12 ln → `_GM-ARCHIVE.md` § Batch
  2026-09-07 #253**, through the mover.
- **2d** — new ⏱ LATEST DELTA #253 (17 ln); #252 demoted; **#250 delta, 16 ln + its 6,163-char
  `Previous:` chain segment → `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-07 #253**.
- **2e** — `## ⬛ DO THIS FIRST` measured at **109 raw lines**, `#### ` scan returns **0** dated
  strata. Nothing to roll; the size is declared rather than the skip.
- **2f** — `roll_2f` split the #252 stratum: **20 ln → `notes/_GAUGE-LOG.md`** (true EOF) + **3 ln
  → `_GM-ARCHIVE.md`**. The `#### 2026-09-07 #253` stratum was then written in its place. ⛔ See
  finding 6 — the commit-state half landed under the wrong batch key and was NOT repaired by a
  route-around.
- **2g** — `_build_memento_index.py` rebuilt LAST: **2,055 records**, and `index_freshness_check`
  passes on the re-run.
- **3** — memory is the CONDUCTOR's step by the brief (`meta-tags-landed-253` + the `MEMORY.md`
  pointer written by him); **0 bytes written at this seat**, which cannot see the store (D1a).
- **4** — `s253-D1`, `s253-D2` and `s253-D3` inscribed into `knowledge/_rulings.json` **by surgical
  text insert** (a `json.dumps` round-trip reflows ~548 lines of mixed historical indentation — the
  diff is **+56, −0**), then read back: store **387 → 390**, entries **388 / 389 / 390**, each
  parsing `by` Dave · `date` 2026-09-07 · `status` ruled.
- **4b** — `_gen_titles.py --session 253` run; receipt written; both lines below.
- **4c** — `_gate_scratch_hygiene.py --clean`: **7 owned entries removed** (`/tmp/laneB`, `/tmp/v`,
  `/tmp/vb` — the three lanes' scratch — plus four files); re-run reads *no scratch owned by this
  user*; VM fill **59.6%**. 3M of dead-session orphans named, not actionable.
- **5** — `_git_commit.sh --reconciled`, no `--wrap`; push from here.

Also: **`_HANDOFF-252-lanes-cold.md` DELETED** (`git rm`), as its own text instructs, its content
now in the ⏱ delta and on the carry line. **Nine `_state.json` rows added** — the eight #253 lane
reports the lanes filed unrowed, plus this one.

## Findings

1. **The wave the #252 handoff briefed cold ran exactly as written, and the fill is the receipt.**
   134,053 real over ~60 turns — 16,876 under the stop line and the widest of four consecutive
   under-advisory cuts — with three build lanes, two verifiers, a three-file research side-lane and
   two ~50 KB review pages inside it. #252 spent nearly the same room on none of that. ★ The
   difference is not discipline but ROUTING: the two pages were authored inside lanes and never
   entered the conductor's window, which is #252's carry ⑤ answered in practice rather than argued.
2. **The eighteen calls are the session's most important non-event, and recording them correctly
   was the wrap's judgement call.** Dave's *"Im tired today play this back to me please"* / *"all
   your pal"* is neither a strike nor an adoption. Under `s251-D15` the honest record is
   CLAUDE-AUTHORED and UNVETOED — written into the 1b dossier, the ⏱ delta, the `_REVIEW-SIGNOFF`
   rows and carry ①, and **NOT** into `_rulings.json`. The same treatment is given to **chart-bar
   84 → 92**, which the conductor enacted at `6a41915` on his own authority.
3. **`s253-D3` is the clearest demonstration this project has of a red that is a design.** Ten
   metas stood RED on `answers-resolves` for the length of a review cycle; the ruling added nine
   words to `chart-intents.json` and the resolver went **GREEN 24/24 with no edit to any meta**.
   The alternative shape — the lane adds the word it needs — would have been green all day and
   nobody would have decided anything.
4. **The runner's red moved one step, and the new one is a vocabulary defect, not a falsification.**
   Re-measured at this seat, not carried on report: `_gm_usage.py --selftest` refuses because
   *"session #218 testifies DIFFERENTLY in notes/_GAUGE-LOG.md and notes/_GAUGE-LOG.md"* — and the
   two testimonies are `notes/_GAUGE-LOG.md:2276` and `:2294`, the second labelled *(second wrap;
   …)*. #218 wrapped twice; the reader has no term for that state and refuses, correctly. ⚠ The
   brief located these at ~2264/2266; the measured lines are 2276 and 2294 and the measured figure
   is the one recorded.
5. **The hazard behind that red is the abort, not the red.** `_build_all.py` step 1 rewrites
   `compliance/graph-index.json` wholesale and steps 8/9 rebuild what it discards, so any abort
   after step 1 strips **41 files, 75 insertions / 1,707 deletions** (lane C finding 4). This wrap
   therefore did NOT run `_build_all.py` at all, and says so as a declared skip with its reason.
6. **One roll landed under the wrong batch key, and the mover REFUSED to repair it — the refusal is
   recorded rather than routed around.** 2f was run before 2c created `## Batch 2026-09-07 #253`,
   so `_gm_move.py` appended the `COMMIT STATE #252` block under `## Batch 2026-09-07 #252`. The
   in-file repair is refused in the mover's own words — *"move within `_GM-ARCHIVE.md`: src == dst
   … a 'move' that stays is a rewrite — NOTHING written"* — so the block was left where it landed
   and a pointer line was inserted under the #253 batch. ★ **The lesson is an ORDER lesson: create
   the 2c batch header BEFORE running `roll_2f`.**
7. **The 2c EXIT CHECK found one item with no standing home, and it is named rather than counted as
   an absence.** Eleven of the twelve Dave-owed items on the rolling #251 banner probe PRESENT by
   name on `_CARRIES.md § residual → #254`. The twelfth — #251's *INVENTED FACT `40% drawn`* — was
   resolved in-session by the conductor, so it is not a Dave-owed deferral; but its LESSON (*say
   "use canon's own figures" in every build brief*) lives only on the **#251 ⏱ delta, which stays
   live this wrap and rolls at #255**. ⇒ **due at the #254 wrap's EXIT CHECK**, declared here so
   the next wrap does not have to re-find it.
8. **`s253-D2` is enacted in files that are not in the repo.** `.gitignore:79` carries
   `knowledge/_tmp/`, so the eighteen help gates that make step 8 green are **local only** — `git
   ls-files knowledge/_tmp` returns nothing. A fresh clone's step 8 is green for a different reason
   (absence, not compliance), and `s218-D7`'s *"evidence beside the report, never session scratch"*
   is not satisfiable by anything under that directory — including this wrap's own scripts. Carried
   as ⑥.

## The carry transform

**436 aged · 7 `[NEW — 0]`→`[1]` · 0 STRUCK · 4 CORRECTED · 6 new · probeable count 308.** Nothing
was struck because nothing carried was retracted this session. The four corrections each carry an
`s188-D2` receipt and keep their original wording verbatim after it: the **CI read-back** (backlog
now nine verdicts, and the three carried ones confirmed PUSHED — `origin/master` reads `7c6bfb9`);
the **boot** (69,664, a fourth consecutive under-ceiling reading, arm still red on two); the
**recall probe** (the five-session 0-plant streak BROKEN — #253 planted; ⚠ a plant is not a
read-back and no recall claim is made); the **conductor's fill** (fourth consecutive under-advisory
cut, widest of the four, and the first spent on a full wave).

⚠ **The brief's carry item (1) — the three `$extension` reminders — was NOT written as a new
carry**, because it already stands as carry ① of `residual → #253`, ageing to `[1]` here with its
wording unchanged. Duplicating it would have created a second copy of a carry the ageing machinery
already tracks. The `roles.json` `$extension` string is instead quoted VERBATIM in the ⏱ delta's
*STILL HIS* line: *"EXPECTED, by ruling only: input sub-roles when forms are authored (non-factory
modes, s251-D7); media / identity for marketing pages; needs-attention only if Dave names that
molecule."*

## RULING-SHAPED QUESTIONS

1. **The eighteen calls.** They are Dave's to strike or adopt, and the next session is named for
   them. Nothing in the repo fails while they stand — which is exactly why they need a carry: the
   next authoring pass will otherwise answer them by default, in a lane.
2. **Step 18: teach the reader that a session may testify twice, or make #218's second block say so
   in a form it parses?** Both are small. `s253-D2` already ruled the SHAPE of this class of choice
   (fix the artefacts, do not narrow the gate), which suggests the second — but the reader's schema
   is the thing that has no term, and that is a definition, which is Dave's.
3. **Where does wrap scratch live, now that `knowledge/_tmp/` is gitignored?** Finding 8 is a
   collision between `s253-D2`'s enactment and `s218-D7`'s evidence clause. Either wrap/lane scratch
   gets a tracked home, or the repo accepts that a gate's receipt can be local-only — and that
   second answer changes what a filed report's evidence pointer means.

## UNPROVEN / CLAIMED (ADR-0016)

- **CLAIMED:** the conductor's **FILL 134,053 real over ~60 turns** and **boot 69,664** are his
  DECLARED figures. ⛔ **Unlike #250, #251 and #252, this seat took NO first-hand `_checkin.py`
  reading of his transcript**, so neither figure is corroborated here and the `s214-D5`
  wrap-handover line records `sub-cut` and `delta` as **unobservable, with the reason**.
- **CLAIMED:** the five #253 commit hashes and their order come from `git log --oneline`, which IS
  first-hand; what each commit CONTAINS is read from the lane reports, not re-derived.
- **UNPROVEN:** that the ceiling arm clears at ≈#255. The figure is #252's derivation from the n=7
  window's membership and this wrap adds only a fourth under-ceiling reading; price to prove: one
  re-run of `boot_constant_drift_check` at each of #254 and #255.
- **UNPROVEN (and it is the wave's own declaration, repeated here so it is not lost):** **neither
  review page has been rendered in a browser by anybody** — both lanes and both verifiers say so.
  Every claim about those pages is a claim about their source text.
- **DECLARED, not proven:** `_build_all.py` was not run at this seat (finding 5), so nothing here
  says the tree is green through the runner. The wrap gate, `_validate_standing_instructions.py`,
  `_roll_state.py`, `_gen_chain.py`, `_build_memento_index.py`, `_gate_doc_rows.py --check` and
  `_gm_usage.py --sizes` were each run directly and are quoted.

## Evidence

No evidence directory: every claim above quotes its probe inline (the gate's own strings, the
mover's receipts, `_gm_usage.py --sizes --session 253`, `_gen_chain.py`'s generated footer). ⛔ **The
wrap's own scripts are session-owned and uniquely named under `knowledge/_tmp/wrap253/` —
`rulings253.py` · `carry254.py` · `content253.py` · `mkops253.py` · `lastref253.py` · `rows253.py`
and their `ops-253-*.json` files, each asserted to exist with a size floor in the writing process
before the mover read it (the #166 stale-msgfile lesson) — and that directory is GITIGNORED, so
this evidence is LOCAL ONLY.** That is finding 8, and it applies to this report's own receipts.

REPLAY-THESE: `_CARRIES.md § residual → #254` first six items (~1,600 tk — the new carries,
including the eighteen unstruck calls and the step-18 red) · `notes/_subreports/2026-09-07-253-BUILD-C.md`
§ RULING-SHAPED QUESTIONS (~700 tk — eight of them, of which two are carried and six are not)
