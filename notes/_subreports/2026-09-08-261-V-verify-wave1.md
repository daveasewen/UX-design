# #261 V — wave 1 verified cold (19 commits `7647aaf..HEAD`)

## 1 — the gate set on the integrated tree

`_validate_snippets` **PASS** 137/0 · `_validate_coverage` **PASS** 137 meta/137 snippet, 0 fail · `_validate_behaviour` **PASS**, ledger byte-stable over two default runs · `_validate_dataviz` **PASS** 15 chart surfaces · `_validate_a11y` **PASS** 0 fail / 204 warn / 1450 controls + 68 marks / 24 marks <24px · `_validate_roles_resolve` **PASS**, no address dangles, roles.json agrees with all 24 `provides` metas · `_validate_partials` **PASS** 0 strict fail / 67 census rules · `gen_showroom --check` **PASS at commit time, FAIL now** (§5).

Two reds, **both INHERITED — driven on a worktree at `7647aaf`:**
- `_validate_kg` FAIL — `PROMOTE data-grid.meta.json / hasPart / selection-checkbox` has no edge in the rebuilt corpus. **Identical failure before the wave.**
- `_validate_type_composites` FAIL — HEAD 1083 violations / 88 files vs base **1087 / 90**. Wave 1 IMPROVED it; Data-grid's 2 TYPE-002 hits are byte-identical at base.

`showroom/index.html` + `index.json` regenerated via `_render/gen_library_214.py` (s215-D5 — NOT gen_showroom's) and committed `#261 V: library index regenerated after wave 1`.

## 2 — five mechanical claims DRIVEN, then MUTATED

| # | claim (lane) | driven | mutated |
|---|---|---|---|
| 1 | M1: `--full-stage` selftest 221 bites; `arm_line()` names the shape that ran | `_release/_gen_pack_manifest.py --selftest` → **221 bites, 0 fail** | `differential_arm`→`differential_ARMX`: **RED** at `arm/disarmed` + `arm/disarmed-names-consequence`. Restored → 221/0 |
| 2 | M5: the behaviour gate no longer rewrites its own ledger; drift REDS | two default runs, md5 `413b31d8`→`413b31d8`, exit 0 | fed a HEAD-era ledger: **exit 1, `LEDGER DRIFT`**, remedy printed, ledger **byte-unchanged after the red**. Restored → green |
| 3 | D3: `_drive_chart_engine.py --check` → 27/27 FRESH | **27 FRESH**, Chromium 151.0.7922.34 | one comment appended to `canon/dv-render-donut.js`: **3 STALE, exit 1**. Restored → exit 0 |
| 4 | K: the KPI tile's minus rung is the ruled two-red pair | snippet gate green on Kpi-tile | `--neg-ink:#DA1A00`→`#DB0011`: **`DRIFT --neg-ink (light) = #DB0011 but rag/error-ink = #DA1A00`**. Restored → 137/0 |
| 5 | L: the legend is REGISTERED, meta and snippet paired | coverage green, `legend.meta.json` ↔ `Legend.reference.html` | manifest `"component":"Legend"`→`"Legend-XX"`: **2 failures**, both halves named. Restored → 0 |

Every mutation restored with `git show HEAD:<path> > <path>`; `git checkout` never used.

## 3 — cross-lane collisions: absorbed, nothing lost

- **Every lane's files are present at HEAD** — meta + snippet + showroom page all resolve for kpi-tile · data-grid · filter-toolbar-bar · sidebar-nav · navigations · tab-bar · footer · legend.
- **All 9 subreports are tracked.**
- **`0903bb6` CONFIRMED — attribution, not loss.** Ft's subject carries K's `kpi-tile.meta.json`, `Kpi-tile.reference.html`, `showroom/kpi-tile.html`, K's review page, 4 K PNGs and K's own subreport (26 files). K's later `5691d09` adds only the store row. Nothing missing; the history misattributes.
- **`_state.json` written by three lanes, one carrying three others' rows**: `e71111b` (G) → `W-100b`; `108493b` (L) → `W-261l`; **`5691d09` (K) → `W-100a` (M), `W-261n` (N), `W-510` (F) + its own `W-509`.** All four survive at HEAD.
- **⛔ THREE lanes have NO row at all: D3, R, Ft.** The doc-row gate refused my commit and named exactly those three subreports; confirmed independently in `_state.json`. Their reports are invisible to every carry. Fix = one `_state.add()` each. V did not write another lane's rows.
- Row ids are minted in two schemes (`W-509`/`W-510` vs `W-100a`/`W-261l`) — no collision today.
- **In the worktree, in no commit:** `notes/_lanes/261-component-lane-brief.md` (the shared brief), `261-Ft-footer-narrow.png`, `261-Ft-review-legacy-dark.png`, `_ft-drive.html`, `notes/_lanes/261M/`. Only the brief looks like it wants a commit.

## 4 — the two-red law, per design snippet (comments stripped, red-dominant literals read in place)

- **Kpi-tile** — `--neg-ink:#DA1A00` light / `#F6604C` dark; `--down:#F6604C` vs `--up:#66CC8D`. **Ruled.**
- **Data-grid** — no red literal at all; borrows the rung. **Ruled.**
- **Filter-toolbar-bar** — `--error-ink:#DA1A00`/`#F6604C`, `--pos-ink:#137F3C`/`#66CC8D`. **Ruled.**
- **Sidebar-nav / Navigations / Tab-bar** — one red, `--nav-indicator:#DB0011`, same in both modes: HSBC brand red as an indicator FILL, declared as precedent in the snippet's own comment. No numeral is tinted, so the law (plus/minus numerals) is not engaged. **Consistent** — Dave's eye, not a gate fail.
- **Footer** — `--env-alert`/`--degraded:#E0A61F`, rag/warning fill, mode-invariant (s122-D2) with `#1A1A1A` glyph ink (s134-D4). Not a red rung. **Ruled.**
- **Legend** — `--delta-down:#DA1A00`/`#F6604C` against `#137F3C`/`#66CC8D`; `#A45C3A` is series 2. **Ruled.**

No snippet hard-codes a red outside its token block; every outside-block hex was prose or provenance.

## 5 — churn: COMMIT the gate reports, and do NOT let the chart sources ride along

**(a) COMMIT AS-IS** — the 8 reports (`_A11Y`, `_COVERAGE`, `_DATAVIZ`, `_ICON-SOURCE-AUDIT`, `_PARTIALS`,
`_SNIPPET-AUDIT`, `_STATE-CONTRAST-AUDIT`, `_TYPE-BLAST`) + `_graph-mark-observations.jsonl` + `_GRADE-DECISIONS.jsonl`. They are pure functions of the sources (M5 stripped the timestamp for exactly this), the sources moved, and a tracked report disagreeing with its own gate is the drift M5 just closed. One commit naming all ten paths.

**(b) NOT gate churn — 15 chart-family sources are also dirty.** `canon/dv-render.js` (+400 bytes of #260
A9 doc note, code-only bytes unchanged), all 14 `Chart-*.reference.html` with the same note re-injected, `_tests/chart-engine/_receipts.json` (worktree `driven 08:09:55Z`, HEAD `07:36:13Z`) and `showroom/chart-donut|pie.html`. Real work, dated before my first commit, invisible to the session-opening `git status`. **Do not restore, do not fold into (a)** — whoever drove that engine at 08:09 must commit it AND regenerate the showroom, because **`gen_showroom --check` is FAIL now** (stale: chart-bar, -boxplot,
-bullet, -butterfly-h, -butterfly-v, -candlestick, …). That is a hard refusal inside `_git_commit.sh`, so the next lane to commit will be tempted to spend a `SHOWROOM_ACK` on someone else's dirt.

**Declared:** V's commits spent `DOC_ROW_ACK` (the three unrowed subreports are D3/R/Ft's — writing another
lane's row is the #253 class) and `SHOWROOM_ACK` (b), and needed `SESSION_N=261` (s130-D3). Also observed, not chased: `_capture_gate.py --selftest` is RED — ruling `s257-D2`'s evidence is bare prose with no `chat #<n>`/`commit ` pointer. Lane R's territory, and it is a live refusal on the wrap path.
