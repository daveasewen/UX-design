# #261 V — wave 1 verified cold (19 commits `7647aaf..HEAD`)

Verifier lane. Nothing re-done: gates run, claims driven, collisions traced.

## 1 — the gate set on the integrated tree

| gate | result |
|---|---|
| `_validate_snippets.py` | **PASS** — 137 snippets, 0 failures |
| `_validate_coverage.py` | **PASS** — 137 meta / 137 snippet, 0 failures |
| `_validate_behaviour.py` | **PASS** — ledger byte-stable over two default runs |
| `_validate_dataviz.py` | **PASS** — 15 chart surfaces |
| `_validate_a11y.py` | **PASS** — 0 fail, 204 warn, 1450 controls + 68 marks, 24 marks < 24px |
| `_validate_roles_resolve.py` | **PASS** — no address dangles; roles.json agrees with all 24 `provides` metas |
| `_validate_partials.py` | **PASS** — 0 strict fail, 67 census rules |
| `gen_showroom.py --check` | **PASS at commit time**, **FAIL now** — see §5 |
| `_validate_kg.py` | **FAIL — INHERITED.** `PROMOTE data-grid.meta.json / hasPart / selection-checkbox` has no edge in the rebuilt corpus. Driven on a worktree at `7647aaf`: **identical failure before wave 1.** |
| `_validate_type_composites.py` | **FAIL — INHERITED, and improved.** HEAD 1083 violations / 88 files; base `7647aaf` **1087 / 90**. Data-grid's 2 TYPE-002 hits are byte-identical at base. |

Both reds pre-date the wave. Neither is a wave-1 regression; wave 1 moved type_composites 4 violations
and 2 files in the right direction.

`showroom/index.html` + `index.json` regenerated via `knowledge/_render/gen_library_214.py` (s215-D5 —
the index is NOT gen_showroom's) and committed as `#261 V: library index regenerated after wave 1`.
Two `_ACK`s were spent to get past gates that are other lanes' business, both declared below.

## 2 — five mechanical claims DRIVEN, then MUTATED

| # | claim (lane) | driven | mutated |
|---|---|---|---|
| 1 | M1: `--full-stage` selftest is 221 bites, 0 fail; `arm_line()` names the shape that ran | `_release/_gen_pack_manifest.py --selftest` → **221 bites, 0 fail** | renamed `differential_arm` → `differential_ARMX`: **RED** at `arm/disarmed` and `arm/disarmed-names-consequence`. Restored → 221/0 |
| 2 | M5: the behaviour gate no longer rewrites its own ledger; drift REDS with the remedy named | two default runs, ledger md5 `413b31d8` → `413b31d8`, exit 0 | fed it a HEAD-era ledger: **exit 1, `LEDGER DRIFT`**, remedy `--write` printed, and the ledger **byte-unchanged after the red** (not absorbed). Restored → green |
| 3 | D3: `_drive_chart_engine.py --check` → 27/27 FRESH | **27 receipts FRESH**, Chromium 151.0.7922.34 | appended one comment to `knowledge/canon/dv-render-donut.js`: **3 STALE lines, exit 1**. `git show HEAD:… > …` → exit 0 |
| 4 | K: the KPI tile's minus rung is the ruled two-red pair | snippet gate green on Kpi-tile | `--neg-ink:#DA1A00` → `#DB0011` (brand red): **`DRIFT --neg-ink (light) = #DB0011 but rag/error-ink = #DA1A00`**, 1 failure. Restored → 137/0 |
| 5 | L: the legend is a REGISTERED component, meta and snippet paired | coverage gate green, `legend.meta.json` ↔ `Legend.reference.html` | manifest `"component": "Legend"` → `"Legend-XX"`: **2 failures**, both halves of the pairing named. Restored → 0 failures |

Every mutation was restored with `git show HEAD:<path> > <path>`; `git checkout` was never used.

## 3 — cross-lane collisions: absorbed, nothing lost

- **Every lane's shipped files are present at HEAD.** meta + snippet + showroom page all resolve for
  kpi-tile · data-grid · filter-toolbar-bar · sidebar-nav · navigations · tab-bar · footer · legend.
- **All 9 subreports are tracked.** `git ls-files` matches every `2026-09-08-261-*.md` on disk.
- **The `0903bb6` collision is CONFIRMED and is attribution, not loss.** Ft's subject carries K's
  `kpi-tile.meta.json`, `Kpi-tile.reference.html`, `showroom/kpi-tile.html`, K's review page, four K
  PNGs and K's own subreport — 26 files under one lane's name. K's later `5691d09` adds only the store
  row and a subreport addendum. Nothing is missing; the history lies about who wrote what.
- **`knowledge/_state.json` was written by three lanes and one of them carried three others' rows.**
  `e71111b` (G) → `W-100b`. `108493b` (L) → `W-261l`. **`5691d09` (K) → `W-100a` (M), `W-261n` (N),
  `W-510` (F) and its own `W-509`.** All four survive at HEAD.
- **THREE lanes have NO store row at all: D3, R, Ft.** The doc-row gate refused my commit and named
  exactly those three subreports as unrowed. Corroborated independently by reading `_state.json`.
  M's own report predicted two of the three. This is the real §3 defect: three lane reports are
  invisible to every carry. The fix is one `_state.add()` each; V did not write another lane's rows.
- **Row ids are minted inconsistently** — `W-509`/`W-510` alongside `W-100a`/`W-100b`/`W-261l`/`W-261n`.
  No collision today, but the two schemes will meet.
- **In the worktree, in no commit:** `notes/_lanes/261-component-lane-brief.md` (the shared brief every
  lane was told to read), `261-Ft-footer-narrow.png`, `261-Ft-review-legacy-dark.png`, `_ft-drive.html`,
  and `notes/_lanes/261M/` (msgfiles + attic). Only the brief looks like it wants a commit.

## 4 — the two-red law, per design snippet

Method: strip HTML comments, then read every hex literal that is red-dominant, with its declaration.

- **Kpi-tile** — `--neg-ink:#DA1A00` light / `#F6604C` dark, `--down:#F6604C` paired with `--up:#66CC8D`. **Ruled.**
- **Data-grid** — no red literal anywhere; the grid borrows the rung, it does not restate it. **Ruled.**
- **Filter-toolbar-bar** — `--error-ink:#DA1A00` light / `#F6604C` dark, `--pos-ink:#137F3C`/`#66CC8D`. **Ruled.**
- **Sidebar-nav / Navigations / Tab-bar** — one red, `--nav-indicator:#DB0011`, identical in both modes.
  That is HSBC brand red as an indicator FILL, declared in the snippet as precedent, not as coloured
  text — the two-red law governs plus/minus numerals, and no numeral is tinted here. **Consistent**,
  and the nav lane says so in its own comment. Worth Dave's eye, not a gate failure.
- **Footer** — one warm literal, `--env-alert`/`--degraded:#E0A61F`, rag/warning fill, mode-invariant
  per s122-D2 with `#1A1A1A` glyph ink on it (s134-D4). Not a red rung. **Ruled.**
- **Legend** — `--delta-down:#DA1A00` light / `#F6604C` dark against `#137F3C`/`#66CC8D`; `#A45C3A` is
  categorical series 2, not a rung. **Ruled.**

All four themes × two modes are declared in each snippet's cascade block; no snippet hard-codes a red
outside its token block. Every hex found outside the token block was prose or provenance comment.

## 5 — gate-artefact churn: my recommendation is RESTORE, then re-run

Two different things are dirty and they need opposite treatment.

**(a) The 8 gate reports + 2 instrumentation logs** — `_A11Y-GATE.md`, `_COVERAGE-GATE.md`,
`_DATAVIZ-GATE.md`, `_ICON-SOURCE-AUDIT.md`, `_PARTIALS-GATE.md`, `_SNIPPET-AUDIT.md`,
`_STATE-CONTRAST-AUDIT.md`, `_TYPE-BLAST-GATE.md`, `_graph-mark-observations.jsonl`,
`_GRADE-DECISIONS.jsonl`. **COMMIT AS-IS.** They are pure functions of the sources (M5 removed the
timestamp from the behaviour ledger for exactly this reason), the sources moved in wave 1, and a
tracked report that disagrees with its own gate is the drift M5 just spent a commit closing. One
commit, `#261 <lane>: gate reports refreshed after wave 1`, naming all ten paths.

**(b) 15 chart-family sources are ALSO dirty and are NOT gate churn** —
`knowledge/canon/dv-render.js` (+400 bytes: the #260 A9 "two fit passes are not symmetric" note, code-only
bytes unchanged) and all 14 `Chart-*.reference.html` with the same note re-injected, plus
`knowledge/_tests/chart-engine/_receipts.json` (worktree `driven 08:09:55Z`, HEAD `07:36:13Z`) and
`showroom/chart-donut.html` / `chart-pie.html`. These are somebody's real work, dated before my first
commit but invisible to the session-opening `git status`. **Do not restore them, and do not let them
ride on (a).** They must be committed by whoever drove that engine at 08:09 — and the same commit must
regenerate `showroom/chart-*.html`, because **`gen_showroom.py --check` is FAIL right now**: 6+ stale
pages (`chart-bar`, `chart-boxplot`, `chart-bullet`, `chart-butterfly-h`, `chart-butterfly-v`,
`chart-candlestick`, …). That gate is a hard refusal inside `_git_commit.sh`, so the next lane to commit
will hit it and be tempted to spend a `SHOWROOM_ACK` on someone else's dirt.

**Declared, so it is not discovered later:** V's own commit spent `DOC_ROW_ACK` (the three unrowed
subreports belong to D3, R and Ft — writing another lane's store row is the #253 class) and required
`SESSION_N=261` (s130-D3). Also observed and NOT chased: `knowledge/_capture_gate.py --selftest` is RED
on a `_governs` failure — ruling `s257-D2`'s evidence is bare prose with no `chat #<n>`/`commit ` pointer.
That is lane R's territory (the rulings page), and it is a live refusal on the wrap path.
