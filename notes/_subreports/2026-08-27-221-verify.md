# `#221`-`verify` — adversarial replay of the four repair lanes

session: `#221` · 2026-08-27
window: repair wave, adversarial verifier (after lanes A–D)
sub index: `verify`
brief: `notes/_briefs/2026-08-27-221-repair-wave-divvy.md` § VERIFIER + GLOBAL FENCE + PITFALLS
tokens: `UNMEASURED — a sub cannot read its own message.usage from this seat`

## VERDICT

Every REPLAY-THESE item in all four lane reports was re-driven at `9dbf413` (the same HEAD every
lane measured). **82 mechanical claims graded GREEN · 1 RED · 9 COULD-NOT-RUN** — and the one red
is a wrong *expected figure* in Lane D's replay prose, not a broken instrument: the shared-helper
tier-flip produces **3** coherent tier-pin failures where the report says "expect 1 each" (the
regen flip does produce exactly 1). All four cross-lane seams verified GREEN. Nothing was
repaired, ruled, promoted, or committed by this seat; the working tree's only deltas since my
baseline are four paths written by a CONCURRENT writer (the mono-gallery decision surface —
finding 2), plus this report. Both guarded md5s (`_CAPTURE-GATE.md`, `_TOKEN-FORK-LEDGER.json`)
read OK at close.

⚠ **The one thing the conductor must act on before committing:** Lane B's new
`_gate_fallback_drift_221.py` FIRED TODAY on `knowledge/_render/gen_mono_gallery_221.py` — the
file the concurrent writer created mid-verification carries `var(--surface-subtle,#F3F3F3)`, the
exact stale fallback value this wave just repaired across seven generators (finding 2).

COUNTS: findings `10` · ruling-shaped `0` · UNPROVEN `6`

## What was done — per-lane grade table

Item granularity is mine (one driven command or one compared artefact = one item). A claim I
could not drive is COULD-NOT-RUN, never green.

| lane | green | red | could-not-run | the could-not-runs, named |
|---|---|---|---|---|
| **A** | 22 | 0 | 3 | CI read-back (`gh` absent, API 404s — Dave's, as A itself declares) · the `_build_all.py` step (fence 7) · the six unprobed regex gates of L1-16 (A declares them UNPROVEN; I did not probe them either) |
| **B** | 24 | 0 | 1 | the render/pixel leg — playwright not importable here; P-3/P-6 printed their own REFUSED, which I witnessed |
| **C** | 23 | 0 | 4 | live VS Code bridge · bake `--dry-run` (fence — no bake) · the third playwright mode (installed-but-no-browsers) · the completed-walkthrough "4 bites → 1" count (state not cheaply reconstructable; the repo-side lone-failure IS verified) |
| **D** | 13 | **1** | 1 | the in-code mutants (membership-blind → 2 reds, grep-shaped → 5 reds, …) — driving them means editing `_capture_gate.py`, which is read-only to me; verified instead via the selftests' internal plant/clear fixtures + both tier flips |

Replay reconstruction notes: Lane A's clone leg was rebuilt at `/sessions/eloquent-zealous-bohr/
l1aclone` (2.0G free on `/var/tmp` vs a 1.3G clone — declared scratch relocation), with Lane A's
8 modified + 2 new files copied in, exactly as their own replay implies. Lane C's pack drives
required refreshing the repo-side fixes into the extracted zip (their own F12 note, which I
extend: it applies to `run-gates.py` and the machinery mirrors too — the shipped copies are
v1.0.1-frozen, so a literal replay of their commands against a virgin unzip shows the OLD
behaviour). The survey was chunked `1:28` / `28:55` for the ~178s wall; rc=1 lives in the first
chunk, `28:55` is clean.

## Findings

1. **THE ONLY RED — Lane D's "flip each tier and expect 1 each."** Measured:
   `REGEN_SERIAL_BLOCKING=True → 1` failure (as claimed); `SHARED_HELPER_BLOCKING=True → 3`
   failures. All three are coherent tier-pin arms ("promoted to blocking without Dave's word" ·
   "the comparer reached the wrap's FAILS" · "not wired into wrap_checks at all"), so the
   instrument is STRICTER than its own report — the defect is the report's figure, nothing else.
   Both directions still hold: flip fires, restore clears (0/0 on fresh import).

2. **A LIVE CATCH, post-filing, and it is commit-blocking.** `_gate_fallback_drift_221.py
   --verbose` returns **rc=1 today**: `⛔ DRIFTED knowledge/_render/gen_mono_gallery_221.py ::
   var(--surface-subtle,#F3F3F3) — canon answers #1F1F1F, #2A2621, #DFDEDC, #F0F0F0`. That file
   was created at 22:15:35 tonight by a concurrent writer (the #221 mono-gallery decision
   surface), AFTER Lane B filed — Lane B's "rc=0, whole glob clean" was true of its population,
   and the seven repaired sources remain clean. The class recurred within hours and the new gate
   caught it on its first unscripted encounter: Lane B's handoff 3 ("wire it or it becomes the
   instrument it was built to prevent") now has live evidence. **Conductor: repair the fallback
   in `gen_mono_gallery_221.py` (and regenerate its review page) before the commit.**

3. **Pack-side `_gen_chain.py --selftest` CRASHES in a pre-walkthrough pack — pre-existing, not
   Lane C's.** In a bare extracted pack (no walkthrough state), both the refreshed machinery AND
   the shipped v1.0.1 copy die identically: `FileNotFoundError: …/_CHAIN.md` (a tempdir path).
   The discriminating probe was run both ways, so this predates the lane and lives in the frozen
   release's class (N3-3-adjacent). A crash is not a fail — but it is also not the loud, named
   refusal a designer should meet if they run the bites before completing FIRST-SESSION §4b.
   Priced as a next-release repair in the machinery (~1K): refuse by name when no walkthrough
   state exists. Handoff, not a ruling.

4. **SEAM 2 (COUNTS parse) — GREEN, arithmetic reconstructed.** Template form parses `True`,
   out-of-order refused `False`. Today: **24 parsing of 48** `notes/_subreports/*.md`; subtract
   the three non-parsing #221 lane reports (A/B/C carry the divvy's field set) and `_TEMPLATE.md`
   and you land on Lane D's **24/44 exactly**. The three audit reports still failing (L3, L4, L6)
   carry the reversed/freeform vocabulary Lane D refused on purpose (quoted: `## COUNTS` ·
   `**COUNTS:** pack v1.0.1 · sha256 …` · `COUNTS: 140/140 …`); L1's `COUNTS: findings \`18\` ·
   ruling-shaped \`5\` · UNPROVEN \`4\`` now parses. One prose nit: "the four #220 audit reports
   among them" — six exist and all six failed pre-fix. ⚠ The population moves: a concurrent
   `2026-08-27-221-mono-specimen.md` landed during verification, and this report adds one more
   parsing member.

5. **SEAM 1 (`_validate_state_contrast.py`) — GREEN, and the word is load-bearing.** The diff is
   exactly one code token (`except ModuleNotFoundError` → `except ImportError`) plus comment; no
   Lane A edit touches the file. Composition proven on the full multi-lane tree: `_validate_wiring`
   reads `50 gate script(s) on disk (43 _validate_* · 7 _gate_*, #221 widened) · 48 wired · 2
   exempt by name · 0 failure(s)` and `test_gates.py` in the clone reads 31/0. Driven three ways:
   ImportError stub → `COULD-NOT-ASK`, rc=77 · working stub → 0 CNA lines (past the guard, then a
   loud real failure) · **the word reverted in a scratch copy + ImportError stub → bare traceback,
   rc=1, 0 CNA** — the exact pre-fix state L4 drove.

6. **SEAM 3 (library index byte-identity) — GREEN.** `git status --porcelain showroom/index.html
   showroom/index.json` is EMPTY (byte-identical to HEAD after the regen), while
   `dashboard/index.html` shows exactly `+2,594 / −1,033` — Lane B's numstat to the digit — and
   the eight `showroom/_foundations/*.html` are the eight modified pages the serial wrote.

7. **SEAM 4 (`--selftest` writes nothing) — GREEN.** md5 `704ad5b908b04ce73ffa01126051c2b6` and
   mtime `1787603195` identical either side of a full `--selftest`; `git status` empty for the
   file; exactly the 3 declared pre-existing reds (M10 ×2 + #70/#71), none new. The veto drives
   to all three verdicts as a pure function, and the tracked-path refusal is loud and named.

8. **Lane D's UNPROVEN 2 is now half-discharged — and half-polluted by me.** The ORDER arm did
   testify on a real uncommitted wave tonight ("`_validate_radius.py` regenerated AFTER
   `gen_dashboard.py`") — the mechanism fires outside fixtures. But my own verification re-ran
   gates whose reports rewrite byte-identically (md5 stable, **mtime moved**), so the specific
   ordering testimony is contaminated by the verifier and I do not grade its truth. Mechanism
   proven; testimony deferred to the conductor's own wrap.

9. **Open handoff confirmed live, one comment line.** `.github/workflows/gates.yml` (~:54) still
   attributes step `[59]`'s 77 to `_governs` naming `outputs/…` — Lane B's handoff 1 to Lane A
   was not taken (it post-dates A's filing). One stale comment for the conductor's reconcile.
   Lane B's handoff-2 premise also quote-verified: `_build_prototype_grade_audit.py:81` is
   `os.path.join(COMP, name.lower() + ".meta.json")`.

10. **Two instrument errors of my own, declared and corrected in-flight.** (a) A pipeline `$?`
    read tail's exit code, briefly misreading `--casing` as rc=0 — re-measured clean: rc=1.
    (b) `grep -c "✗"` counted 4 in `_gen_chain.py --selftest` where three are expected-red prints
    inside PASSING arms; the selftest's own verdict line is `✗ _gen_chain selftest: 1 bite(s)
    failed` — exactly Lane C's "1 pre-existing failure". Matched ≠ presence; quote the line.

## RULING-SHAPED QUESTIONS

**None.** Nothing this verification surfaced needs Dave's word that is not already carried by the
lanes' own filed questions (A: Q1–Q5 · B: Q1–Q6 · C: Q1–Q6 · D: Q1–Q4 — all stand un-adjudicated
and none were re-put here). Findings 2, 3 and 9 are conductor machinery, priced above.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN: anything CI actually says.** `gh` absent, API 404s — same seat-limits as every lane.
  All CI-shaped greens here are the local bare-clone proxy at `9dbf413`. Price: one authenticated
  `gh run view` after the conductor pushes.
- **UNPROVEN: the `_build_all.py` step.** Fence 7 honoured absolutely — never run, tree or clone.
  Price: one full CI run.
- **UNPROVEN: the render/pixel leg** (B's focus ring in pixels, C's real-browser gates, P-3/P-6).
  Playwright is not importable here and every instrument said so honestly. Price: the staged
  render seam per `_RUNBOOK-render-verify.md`, ~3K.
- **UNPROVEN: Lane C's completed-walkthrough count ("4 bites failed → 1").** The walkthrough
  state was not reconstructed; the repo-side lone failure and the two-tree F10 branch proof were
  verified instead. Price: one scripted walkthrough in a scratch pack, ~2K.
- **UNPROVEN: Lane D's in-code mutant tallies** (membership-blind → 2, grep-shaped → 5, …).
  Replaying them requires editing `_capture_gate.py` (read-only to me). The selftests' internal
  plant/clear fixtures + both tier flips stand in. Price: ~2K on a `/var/tmp` copy with sys.path
  staging.
- **UNPROVEN: the ORDER arm's testimony about THIS wave** (finding 8) — polluted by my own runs.
  Price: ~0, observed at the conductor's next wrap.

## Evidence

No evidence files: every claim above quotes its probe inline. Full transcripts sat in
`/var/tmp/verify221/` during the session (scratch — does not survive the window; anything
load-bearing is quoted here).

REPLAY-THESE: `_gate_fallback_drift_221.py --verbose` after repairing `gen_mono_gallery_221.py` — must read rc=0 before the commit (~1K) · `gates.yml` ~:54 stale `[59]` comment, one line at reconcile (~0.2K) · finding 3's pack-crash repair, next release (~1K)

USAGE: `UNMEASURED — declared.` This seat cannot read its own `message.usage`; the conductor's
panel is the only honest reading.
