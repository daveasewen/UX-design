# 296 · W — the wrap: two decks for Friday, a gauge no seat could read, and a carry that was false for eight handoffs

session: `#296` · 2026-09-22 (one day) · lane `W` (DELEGATED WRAP) · model Opus 5.5 · conductor Fable 5.1
job: run every step of `knowledge/_RUNBOOK-capture-ritual.md`, commit on the `--wrap` path if the gate allows, push, read CI back.

COUNTS: findings `6` · ruling-shaped `4` · UNPROVEN `3`

✅ **ONE DAY, NO DATE SPLIT.** `date` at this seat: `Tue Sep 22 19:24:12 UTC 2026` (20:24 BST).

---

## THE GATE AT OPEN — ZERO FAILS BEFORE ANY WRITE

```
capture gate [wrap]: 233 in scope · 0 fail · 44 warn
```

The date-stamp fails that #295 met at its open do not arise: #295's own ritual already stamped
`GOOD-MORNING.md` and `_LIVE-STATE.md` with 2026-09-22, the same day as this wrap. ⚠ **The brief quotes
the conductor's attempts at `233 · 1 fail · 44 warn` (declared not-a-wrap).** This seat measured **0
fail** on the unchanged tree before its first write. Both readings are published; the difference is
not diagnosed here (the conductor ran the gate through `_git_commit.sh`, whose own staging differs
from a bare `--wrap` run). The two warns that changed since the wrap-open record: the M10 read-chain
block-candidate (13,034 tape) and "4 uncommitted paths".

## THE GAUGE — A DECLARED ABSENCE AT EVERY SEAT

`python3 knowledge/_checkin.py --preflight-line 296`, verbatim:

```
> **pre-flight #296:** ⛔ NOT CAPTURED — UNMEASURED. no top-level transcript matched `/sessions/*/mnt/.claude/projects/*/*.jsonl` at this seat, so the conductor's window is unreadable from here — an ABSENCE OF A MATCH, named as such, not a proven absence.
```

`/sessions/*/mnt/.claude` does not exist at this seat. ⇒ **FILL, peak, boot, turns, the `s214-D5`
hand-over delta and the `subs` line are all UNMEASURED**, and none is written as a number. The
conductor's ~185,000 and the five lanes' spend are published as DECLARATIONS in the stratum.

## WHAT I VERIFIED OF THE BRIEF

- **Rulings 638** — ✅ `json.load` over `rulings`: `638 s295-D4`. Nothing inscribed at #296.
- **HEAD `c358fc6b`, three unpushed session commits** — ✅ `git log origin/master..master` lists
  `cf97ccc4` · `1351175a` · `c358fc6b`; `origin/master` (local ref) = `8af95161`.
- **`c358fc6b` by plain `git commit -F`** — ✅ its message carries the DECLARED paragraph.
- **The frozen prompt at the #258 brief's lines 13–17** — ✅ read at this seat: `## THE PROMPT
  (verbatim, FROZEN — do not improve it)` at line 13, the fenced prompt at 15–17.
- **Three other-seat dirty paths** — ✅ `notes/_dream/_GRADE-DECISIONS.jsonl`,
  `notes/_lanes/293/J7-IDEA-jev-selects-over-the-kg.md`, `notes/_lanes/294/WRAP-MEMORY-HOOK.md` —
  still dirty; not staged.
- **`_render_rulings.py --check`** — ✅ FRESH, sha256 `96ee3fce…`. No re-render needed.
- **`_gen_chain.py --check` at open** — ✅ FRESH at `1351175a`, HEAD advanced to `c358fc6b`.

## THE RITUAL, STEP BY STEP

**1 / 1b.** `_LIVE-STATE.md`: a new `*Last refreshed: 2026-09-22 … #296 wrap*` stamp and a ⏱ LATEST
delta. Dossier: `_DECISION-HISTORY/2026-09-22-296-two-decks-for-friday-ruled-by-his-eye-and-his-boss.md`.

**2.** New ★ LATEST banner; #295's renamed ★ PRIOR. **No date-split header line** (one day).
Banner measured with the gate's own region and `measure_tokens`: see § POST-WRAP.

**2c.** #294's ★ PRIOR banner (11 lines) MOVED VERBATIM to `_GM-ARCHIVE.md` § `Batch 2026-09-22 #296`.
EXIT CHECK: its ⬛/⚠ items are carried in `_CARRIES.md` from § `residual → #295` onward, now § `residual
→ #297`, at their ages. Carries: § `residual → #297` written — **654 probeable items (probe run), 12
new, 2 struck with receipts**, ages +1, wording unchanged except the two strikes.

**2d.** #293's ⏱ delta (34 lines) MOVED to `_LIVE-STATE-ARCHIVE.md` § `Rolled 2026-09-22 #296`, and
#291's `Previous:` chain segment (6,527 chars) moved with it, with a trim note left in place.

**2e.** Ran and moved nothing: DO-FIRST carries no session-keyed stratum.

**2f.** `roll_2f` session 295: 30 lines → `notes/_GAUGE-LOG.md`, 3 → `_GM-ARCHIVE.md`.

**2g / 4b / 4c / 4d / size stamp / chain.** See § POST-WRAP for the readings as they closed.

**3.** Memory: **owed to the conductor** by the brief — `notes/_lanes/296/WRAP-MEMORY-HOOK.md`. This seat
did not write to the store.

**4.** Doc rows minted for every new document, before staging.

## RULING-SHAPED QUESTIONS

1. ⬛ **Which deck carries Friday** — and, if the plant deck, is the day of plain rulings ported to it?
   Put to Dave at the wrap call.
2. ⬛ **The brain's angle**: its own −50 / 20 / 33, or the shared −35 / 20? Put to Dave at the wrap call.
3. ⬛ **`_git_commit.sh`'s first-add stranding**: a mechanism (the script moves a stranded lock aside and
   retries its own add once, declared) or a standing workaround? Not a seat's to choose.
4. ⬛ **The missing transcript**: when no seat can read the conductor's window, should the conductor's
   declared figure be accepted as the `brief-cut` term at all, or is the whole `wrap-handover` line
   `unobservable`? This wrap wrote the declaration with its label and every other term unobservable.

### UNPROVEN, named rather than implied

1. **Why no transcript matched** at any seat — not diagnosed.
2. **Why the conductor's gate runs read 1 fail and this seat's reads 0** — not diagnosed.
3. **The capture-gate selftest** — exceeds the call wall; CI is the only asker.

REPLAY-THESE: `python3 knowledge/_capture_gate.py --wrap` · `python3 knowledge/_roll_state.py` (expect `2c OK (banners 2/2) · 2d OK (deltas 3/3) · 2f OK (strata 1, log #295)`) · `python3 knowledge/_gen_chain.py --check` · `python3 -c "import json;r=json.load(open('knowledge/_rulings.json'))['rulings'];print(len(r),r[-1]['id'])"` (expect `638 s295-D4`) · `PYTHONPATH=knowledge python3 -c "import _capture_gate as c;print(len(c._carry_items([l for l in open('_CARRIES.md') if '#297:**' in l][0])))"` (expect **654**) · `git ls-remote origin refs/heads/master` (expect the wrap sha)

---
