# #268 cold run 9 — the RELEASED v1.0.13 zip, cold-run-8's fourteen assertions, unchanged

**Model: Opus (per #250). Dated period record (ADR-0017). Read-only against `knowledge/`,
`outputs/coldrun-267/`. Nothing built, moved or ruled there. No `_build_all.py`, no
`gen_kg_edges.py`, no checkout, no stash, no push.**

**SAME SHAPE AS C8, SAME FOURTEEN ASSERTIONS, ONE VARIABLE CHANGED — the zip.** C9 does not
re-build and does not widen the arm. It re-drives C6's frozen arm page against the **released
v1.0.13 canon**, so the only difference between C8 and C9 is the file the release was cut for.
There is no score, because nothing was built.

⛔ **NOTHING WAS PATCHED AFTER THE PIN.** Every assertion below was taken off the zip in
`apollo-spider/dist/`, and the fix it grades landed at `7ddab70`, before the cut commit.

## Paths

- pack — `apollo-spider/dist/Apollo-Spider-v1.0.13.zip`
  · sha256 `5d4df132b1f57f1764736d6bf46d3260bb7b7d0393a8317ea11c8f6b8bb282c6`
  · 21,202,835 bytes · cut at `08e315dca376` · unzipped to `/var/tmp/cr9/Apollo-Spider-v1.0.13`
- arm — a COPY of `outputs/coldrun-267/arm-A-blind/` at `/var/tmp/cr9/arm`, with
  `canon/canon.css` + `canon/type.css` replaced by the **v1.0.13 zip's**. The repo copy was not
  touched; `git status outputs/coldrun-267/` is clean.
- driver — `/var/tmp/cr9/drive9.py`, byte-derived from C8's `drive8.py` (paths and port only),
  served over `http://127.0.0.1:8719` (not `file://`), chromium via
  `knowledge/_render/seat_env.sh` (`SEAT_ENV: OK seat=elegant-vibrant-davinci`)
- screenshots — `notes/_subreports/assets/2026-09-11-268-cold-run-9-dropdown-open.png` and
  `…-nav-{mono,legacy,console,supercharge}.png` (the sidebar, per theme, AS SHIPPED)
- this report — `notes/_subreports/2026-09-11-268-cold-run-9.md`

## Assertions — 14 / 14 PASS at 1440

| # | assertion | verdict | detail |
|---|---|---|---|
| 1 | `a.nv-item` exists | PASS | present |
| 2 | `.nv-item` matched by canon rules | PASS | **53 rules** mention `.nv-item` |
| 3 | `.nv-item` display is not `inline` | PASS | `flex` |
| 4 | `.nv-item` has padding | PASS | `0px 16px` |
| 5 | `.nv-item` underline removed | PASS | `none` |
| 6 | `.nv-item` height ≥ 24px (s262-D2 floor) | PASS | 44.0px |
| 7 | entity dropdown opens on click | PASS (soft) | `aria-expanded=true`, menu in the box tree |
| 8 | currency dropdown opens on click | PASS (soft) | ditto |
| 9 | 0 pageerrors | PASS | `[]` |
| 10 | 0 console errors | PASS | `[]` |
| 11 | `.nv-label` descenders intact — **mono** | **PASS** | 8 labels, edge `text`, box 21px, worst clip **0.00px** |
| 12 | `.nv-label` descenders intact — **legacy** | **PASS** | identical numbers |
| 13 | `.nv-label` descenders intact — **console** | **PASS** | identical numbers |
| 14 | `.nv-label` descenders intact — **supercharge** | **PASS** | identical numbers |

⚠ Assertions 7 and 8 stay **SOFT GREEN**, exactly as C7 and C8 named them: playwright's
`is_visible()` ignores `opacity`, and C7's finding 1 (the arm's own script sets `aria-expanded`
but not `data-open`, so the menu paints at `opacity:0`) is **still not fixed** — it is the arm's
defect, not the pack's, and C9 did not touch the arm. Nothing here re-confirms that the menus are
*seen*. **This is carried, unchanged, for a fourth run.**

## C8's F1 IS CLOSED — the four reds are green and the numbers moved the whole way

| | computed `text-box-edge` | box height | worst clip | verdict |
|---|---|---|---|---|
| v1.0.11 (cold run 7) | `cap alphabetic` | 12px | 5.50px | RED |
| v1.0.12 (cold run 8) | `cap alphabetic` | 12px | 5.50px | **4/4 FAIL** |
| **v1.0.13 (cold run 9)** | **`text`** | **21px** | **0.00px** | **4/4 PASS** |

C8's F1 named the cause precisely: v1.0.12 fixed canon's BASE leading-trim default, and every
component absorbed into canon carries its **own private copy** of that default — 108 of them
still `:is(...)` at (0,1,2). `cn-template-dashboard-bento`, which the arm's `<body>` wears,
matched the nav label two scopes away and outranked the (0,1,0) ds-005 override. `7ddab70` made
that one list `:where(...)` in all 108 reference snippets and regenerated canon (137 components,
111 copies `:where()`, 0 `:is()`). This run is that fix, measured on the shipped bytes.

## The new step that made this run boring, which is the point

**The pre-bake drive.** `knowledge/_validate_demo_page.py` was written before the cut and run
against the **dry zip**: it unzips the pack, stages *this same page* against the PACK's canon,
serves it over http and drives all four themes — 7/7 assertions, 0.00px, before anything was
pinned. Only then was the cut ratified. Re-run against the released zip it says the same thing.

⛔ **This is what v1.0.11 and v1.0.12 both lacked.** Both were green at the pin and both shipped
a clipped sidebar, because the only thing ever driven was a fixture built to contain the fix
[[conflated-fix-guarantees-recurrence]]. Nine release gates parsed the manifest, the file list
and the version literals, and not one opened the pack and looked at the demo
[[no-gate-parses-the-artefact]]. There is now a gate that does.

⚠ The gate is measured **REPO-BOUND** in the release probe and therefore does **not ship inside
the pack** — the page it drives (`outputs/coldrun-267/arm-A-blind/dashboard.html`) is a repo
artefact the pack does not carry. That is the honest verdict and it is the same position nine
other gates already hold. It is a RELEASE-SIDE instrument, not a designer-side one.

## NOT done (declared)

- **No blind build, no score, no second arm, no grill-me, no control arm.** C9 is a targeted
  re-drive of C6's frozen arm; the 2/3/3/2 scoreboard is not re-issued.
- **C7's finding 1 was not re-tested and not fixed** — the arm's script still omits `data-open`,
  so assertions 7 and 8 remain soft greens on a menu at `opacity:0`.
- **Only two dropdowns were driven.** The 19-control sweep, the persistence reload, both
  validators, the renders at 820, the receipt mint and the chart-completeness pass were not
  re-run.
- **Dark mode not re-driven.** Light only, at 1440. The descender arm ran all four THEMES in
  light mode.
- Nothing in the arm was fixed or touched.

## Sandbox

`/tmp/pw` survived; `source knowledge/_render/seat_env.sh` printed `SEAT_ENV: OK` first try and
was re-sourced in **every** bash call. `python3 -m http.server` was backgrounded and killed
inside the same call. ⚠ **Background processes do not survive a tool-call boundary in this
sandbox** — a `setsid nohup` probe was measured dying at the call end, which is why the release
probe was driven in resumable per-gate chunks rather than one long run; the chunking reuses one
stage and one sorted order, so each gate is measured exactly as a single pass would measure it.
`/` sat at 86% after the bake; four stale stages were removed before it.
