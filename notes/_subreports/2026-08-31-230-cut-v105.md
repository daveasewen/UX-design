# #230 — the v1.0.5 cut

*Filed 2026-08-31 by the Opus build sub. Conductor: Fable. Status: **STAGED AND PROVED, NOT BAKED** — held at PROPOSED by the machine, exactly as the brief's ⛔ requires.*

**enacted 6 · ruling-shaped 3 · UNPROVEN 4 · reds before 4 → after 4 (one closed, one opened by the bump and closes AT the bake)**

Apollo — Spider **v1.0.5** is fully staged, its ship list regenerated, its go/no-go page
regenerated, and its bake proved deterministic by two dry-run twins that are byte-identical over
the whole file. **No zip was released.** `apollo-spider/dist/` is untouched and still holds five
zips ending at v1.0.4. `--release` refuses — by design, and it printed why.

⛔ **THE FINGERPRINT BELOW IS PROVISIONAL AND MUST BE RE-TAKEN AFTER THE CONDUCTOR COMMITS.**
This is the single most important sentence in this report. See § THE FINGERPRINT IS PROVISIONAL.

---

## COUNTS

| | |
|---|---|
| version | **v1.0.5** (was v1.0.4) |
| manifest commit | `7a43f14cbba6` (HEAD, unpushed) |
| ship list | **1,659 files · 41,977,694 bytes · sha256 `6937f2d4cd441dd8`** |
| pack (manifest totals) | 1,661 files · 42,004,351 bytes |
| staged tree / zip entries | 1,664 |
| carries | `Memento — Gumdrop v1.0.5` |
| **status** | **`PROPOSED — no ruling is keyed to v1.0.5 yet`** (read from the store, never typed) |
| dry-run twin zip | `Apollo-Spider-v1.0.5.zip`, **19,948,872 bytes** (v1.0.4 was 19,917,389) |
| **sha256 (PROVISIONAL)** | **`6e7c6dd4d4658b1c4edbb2c721ecf4c09f9bc94a11240fb02aacddcecd6c3df8`** |
| dry-run byte-match | ✅ **GREEN** — two dry-runs into two different directories, `cmp` over the whole file: identical |
| roster (gates group) | **58 — HELD**, unmoved, no line touched (`s228-D5`) |
| `RATIFY_IDS` before | `"v1.0.4": "s227-D6"` · **no v1.0.5 row** |
| `RATIFY_IDS` after | `"v1.0.4": "s228-D4"` · **still no v1.0.5 row — deliberate** |
| survey reds before | **4** — `[3]` `[18]` `[61]` `[111]` |
| survey reds after | **4** — `[18]` `[61]` `[111]` `[136]` |
| go/no-go page | `reviews/RELEASE-SPIDER-2026-08-26-v1.html` (+ `.REVIEW.html` overlay re-injected) |
| dist/ | **UNCHANGED** — v1.0.0 … v1.0.4, no v1.0.5 |

**No git operations were performed.** No `_rulings.json` write, no W-row, no state write, no
memory write, no roster edit, no canon/snippet edit, no `_build_all.py`. Eleven paths are left
dirty and named below; the conductor reconciles and commits them.

---

## ⛔ THE FINGERPRINT IS PROVISIONAL — READ THIS BEFORE QUOTING `6e7c6dd4`

A dry-run stages from **`git archive <commit>`**, not from the working tree. Six of this lane's
edits are in the working tree and **not committed**, because commits are fenced to the conductor.
So the twins prove **determinism** — same commit + same manifest ⇒ same bytes, twice, into two
directories — but they are the fingerprint of a bake at `7a43f14` *without* this lane's fixes in
the stage.

**Measured, path by path, not assumed.** Every dirty path was compared against its staged twin:

| dirty path | ships? | moves the sha? |
|---|---|---|
| `apollo-spider/FIRST-SESSION.md` | ✅ ships | ⚠ **YES** |
| `knowledge/tokens/_blast-radius.json` | ✅ ships | ⚠ **YES** |
| `apollo-spider/gumdrop/_state.json` | ✅ ships | no — stamped at bake |
| `apollo-spider/gumdrop/runbooks/_RUNBOOK-capture-ritual.md` | ✅ ships | no — stamped at bake |
| `apollo-spider/gumdrop/runbooks/_RUNBOOK-context-gauge.md` | ✅ ships | no — stamped at bake |
| `knowledge/_GRAPH-REPORT.md` | not in pack | no |
| `knowledge/_release/_gen_pack_manifest.py` | not in pack | no |
| `knowledge/_release/_pack_gate_probe.json` | not in pack | no |
| `knowledge/_release/_pack_manifest.json` | not in pack | no |
| `reviews/RELEASE-SPIDER-2026-08-26-v1.html` | not in pack | no |
| `reviews/RELEASE-SPIDER-2026-08-26-v1.REVIEW.html` | not in pack | no |

The three Gumdrop literals are byte-identical *after the stamp* — that is the `s225-D3` stamp
working exactly as designed, and it is why they print DRIFT lines but do not move the fingerprint.
`build-designer-pack.sh` said so in its own words at the bake: *"Its committed blob no longer
matches what ships, so `--check` will name this path until the repo copy is synced."*

**Exactly two shipped paths will move the sha.** After the conductor commits, the release
fingerprint will differ from `6e7c6dd4…` and the twins must be re-driven.

⚠ **AND THE PROVISIONAL FINGERPRINT IS ALREADY ON DAVE'S GO/NO-GO PAGE.** `--dry-run` writes it
there (the #219 finding, widened at #228). The page currently carries `6e7c6dd4d4658b1c`. If Dave
rules on that page and the conductor then commits, **the receipt on his decision surface will not
be the fingerprint of the zip he gets.** The page must be regenerated after the commit, before
his word is taken as ruling on *these bytes*. This is a real instance of
`[[a-report-cannot-list-the-commit-that-files-it]]`'s cousin: a receipt taken before the last
write it describes.

---

## GATE ZERO — the local reproduction of CI's step 5

`.github/workflows/gates.yml` step 5 is `python3 knowledge/_build_survey.py --timeout 60`. Driven
here in `--range` chunks covering all 140 steps, twice: once on the tree as inherited, once after
the cut. Step 6 (`_build_all.py`) was NOT run — fenced by the brief, and sandbox-impossible as a
single process; CI delivers that verdict.

### REDS BEFORE — 4 named

| step | check | verdict |
|---|---|---|
| `[3]` | `_build_blast_radius.py --check` | **FAIL** — 2 files out of sync with a fresh `compute()` (`_blast-radius.json` 81,547 disk vs 81,602 fresh; `_GRAPH-REPORT.md` differs at line 22) |
| `[18]` | `_gm_usage.py --selftest` | **FAIL** — the standing #218 double-testimony red |
| `[61]` | `gen_dashboard.py --check` | **FAIL** — `dashboard/index.html is OUT OF SYNC` |
| `[111]` | `_build_memento_index.py --check` | **FAIL** — index stale against the corpus |

**The whole release half was GREEN before the cut** — `[124]` `[133]` `[134]` `[135]` `[136]`
`[137]` `[138]` all passed. That is the baseline the brief's "a NEW red in the release half stops
the lane" clause is measured against.

### REDS AFTER — 4

| step | check | verdict |
|---|---|---|
| `[18]` | `_gm_usage.py --selftest` | **FAIL, unchanged** — Dave's, standing since #228 |
| `[61]` | `gen_dashboard.py --check` | **FAIL, unchanged** — stale since the #230 commits; does NOT ship |
| `[111]` | `_build_memento_index.py --check` | **FAIL, unchanged** — stale; does NOT ship |
| `[136]` | `_gate_release_audit.py --selftest` | **NEW, AND EXPECTED** — see below |

**`[3]` CLOSED by measurement and regeneration — and it was a real packaging defect.**
`knowledge/tokens/_blast-radius.json` **is in the ship list** (`engine-canon.tokens`). Tonight's
W-318 enact lane moved tokens/components without regenerating it, so a bake at HEAD would have
shipped a blast-radius that disagreed with the tokens beside it in the same pack. Regenerated
(`python3 knowledge/tokens/_build_blast_radius.py`), `--check` now **PASS**. `_GRAPH-REPORT.md`
moved with it as a side effect of the same command and does not ship.

**`[136]` is not a new red in the meaningful sense — it is the pre-bake shape, word for word.**
Its output: *"the manifest reads version 'v1.0.5' and NO zip in dist/ carries it (zips present:
… Apollo-Spider-v1.0.4.zip) — the manifest names a release nobody baked."* #228 saw this exact
sentence with `v1.0.3` in it, in its own REDS-BEFORE table, and recorded that it *"cleared by the
cut"*. It is the arithmetic of a manifest bumped ahead of its bake, it is **the honest state of a
PROPOSED cut**, and it closes the moment the zip lands in `dist/`. `[135]` (`--check`, the ship
list audit) is **GREEN**: the manifest on disk is byte-identical to a fresh generation at
`7a43f14cbba6`.

`[61]` and `[111]` are each one deterministic regeneration away (`python3
knowledge/gen_dashboard.py` / `python3 knowledge/_build_memento_index.py`). Both were left alone:
neither ships, both are gates-half, and the brief declares the gates-job red as not this lane's.

---

## ENACTED — 6

### A · `RATIFY_IDS["v1.0.4"]` MOVED `s227-D6` → `s228-D4`

The brief's first ⛔, and the v1.0.3 pattern discharged. `s228-D4`'s own `ruled` line is the
authority and was read from the store, not from the brief: *"v1.0.4 IS RATIFIED — Dave's word;
RATIFY_IDS moves to this id at the NEXT cut, per the v1.0.3 pattern."* This cut is that next cut.

`s227-D6` is an **AUTHORISATION** (*"Yes — cut it Monday"*); `s228-D4` is the **RATIFYING word**;
`s223-D3`'s whole distinction is between the ruling that names a cut and the word that ratifies
it. #228 keyed `s227-D6` here and flagged it as a declared judgment for precisely this reason —
that flag is now discharged, and the reasoning is written at the row. The v1.0.4 zip in `dist/`
was cut against `s227-D6` and its bytes are frozen history: **the ledger caught up, nothing was
re-baked.** The standing *"rows are ADDED, never moved"* line above is kept and qualified, as
`s228-D1` left it.

### B · **NO `v1.0.5` ROW — the ⛔ STOP, enacted as an absence**

The store was searched for a ruling carrying Dave's release word for v1.0.5 (`s230-*`: **none
exists — there is no `s230` ruling in the store at all**). Keying anything here would be inventing
the word the gate exists to wait for. So the row is absent, `ratification_status()` returns
**PROPOSED**, and `--release` refuses. A comment block at the site says all of that, so the next
reader cannot mistake the absence for an oversight `[[unrun-search-indistinguishable-from-absent-record]]`.

**Proved, not assumed** — `build-designer-pack.sh --selftest`, the third refusal arm:
`green — manifest status is PROPOSED, so --release cannot run`. The machine is enforcing the
brief's ⛔, not this sub's discipline.

### C · `VERSION` and `MEMENTO_CUT_VERSION` → `v1.0.5`

Both in `knowledge/_release/_gen_pack_manifest.py`, moved together per `s225-D3`'s one-version
story. Neither that file nor `build-designer-pack.sh` ships (measured against the manifest's
1,659 paths), so the bump is stage-neutral in itself.

### D · the four hand-typed version literals swept

- `apollo-spider/FIRST-SESSION.md:51` — *"the unzipped `Apollo-Spider-v1.0.4` directory"* → v1.0.5.
- `apollo-spider/gumdrop/_state.json:17`, `gumdrop/runbooks/_RUNBOOK-capture-ritual.md:3`,
  `gumdrop/runbooks/_RUNBOOK-context-gauge.md:3` — the three `s225-D3` Gumdrop literals → v1.0.5,
  so the bake-time stamp is byte-neutral and `--check` goes green once committed.

### E · `knowledge/tokens/_blast-radius.json` regenerated — see `[3]` above

### F · the ship list and Dave's go/no-go page regenerated

`--probe` then `--manifest --commit 7a43f14` (driven separately for the ~178 s call wall), then
`--page`, then `_review/_make_review.py` to re-inject the overlay the page write strips (#219).
Nothing hand-edited.

---

## FOUND BY DOING, NOT BRIEFED — 2

### ① `FIRST-SESSION.md` told designers to open a `v1.0.4` directory — the #228 defect ②, RECURRING AT THE VERY NEXT CUT

#228 caught this exact defect on `v1.0.3`, fixed it at source, and priced the permanent fix:
*"arm 3 is ADVISORY, so it found the defect and exited 0 … The permanent fix is either promoting
arm 3 to blocking, or extending the bake-time Gumdrop stamp to `Apollo-Spider-v*`."* It was
⬛ Dave's, was not taken, and **the defect came back one release later, in the same file, found
the same way** — by reading the dry-run's output, not its exit code:

```
=== VERSION — 1 finding(s) ===
[FIRST-SESSION.md] Apollo-Spider-v1.0.4  →  this pack is v1.0.5 — a version string was not swept at bake
218 finding(s). ADVISORY — exiting 0. This gate is not blocking; promotion is Dave's word.
```

This is now a **two-cut recurrence with an unclosed permanent fix**, which is exactly the shape
`s228-D7` (RCA before patches, tactical and permanent PAIRED, permanent owner NAMED) exists to
stop. Tactical fix taken here; the permanent one is still ⬛ Dave's and is now overdue.
`[[gate-dont-patch]]` — *"always real fixes never patches, they just get lost"*.

⚠ Note the finding still printed on this run *after* the tactical fix, because the stage is
`git archive HEAD` and the fix is uncommitted. It will clear at the conductor's commit; it is not
a second instance.

### ② A shipped derived artefact was stale, and no release gate could see it

`knowledge/tokens/_blast-radius.json` ships, and the enact lane left it stale. **No release-side
gate caught it** — `_gate_release_audit.py --check` compares the manifest to a fresh *generation*
(byte-identity of the ship LIST), and the pack's blob-fidelity arm compares shipped bytes to the
*commit's* blobs. Both were perfectly green on a stale file, because the file matched itself.
The only instrument that saw it was `[3]`, a gates-half determinism check with no release-side
consumer `[[no-gate-parses-the-artefact]]`. A cut run without a full survey would have shipped it.

---

## RULING-SHAPED — 3

### 1. The brief tells this lane to bake, and its own ⛔ plus two hard fences forbid it

The brief's job line says *"dry-run twins → fingerprint on the regenerated go/no-go page → bake →
`cmp` byte-match over the whole zip"*. Three things in the same brief make a bake impossible, and
they agree with each other:

- ⛔ *"THE BAKE IS PROPOSED, NOT RATIFIED, until Dave's fresh word in chat … produce the go/no-go
  page, the fingerprint, and STOP."*
- **NO git operations** — `--release` calls `require_clean`, and this lane necessarily dirties
  eleven paths.
- **NO `_rulings.json`** — and `--release` calls `ratified || die`, which needs a store row.

Read as written, the ⛔ and the fences outrank the job line's shorthand, and the machine
independently reaches the same answer. **That is how it was resolved: STOP at PROPOSED.**
⬛ **If the conductor intended a bake tonight, the fences have to move, not the interpretation** —
specifically: who commits, and what ruling id v1.0.5's row points at.

### 2. `[136]` is red until the bake, and CI will see it

The release-audit selftest arm goes red the instant `VERSION` moves ahead of `dist/`, and stays
red for the whole PROPOSED interval. #228's interval was minutes because the same lane baked. This
lane's interval is **however long it takes Dave to give his word** — and CI runs in between.
⬛ **Dave's / the conductor's:** accept a knowingly-red release job across the PROPOSED window, or
have that arm learn the difference between *"nobody baked it"* and *"it is PROPOSED and not baked
yet, correctly"*. The arm has the manifest's own `status` field available and does not read it.

### 3. Dave's go/no-go page carries a fingerprint that will not be the shipped one

Stated in full in § THE FINGERPRINT IS PROVISIONAL. The mechanism is ruled-in
(`--dry-run` writes the page), the consequence is new: with the bake split off from the staging
lane, the page's receipt now has a window in which it is wrong. ⬛ **Dave's:** either the page is
regenerated after every commit that precedes his word, or the dry-run stops writing a fingerprint
it cannot guarantee is final.

---

## UNPROVEN — 4, declared

1. **`_build_all.py` (gates.yml step 6) was NOT run** — fenced by the brief and sandbox-impossible
   as a single process. CI delivers this verdict.
2. **`knowledge/_tests/test_gates.py` (step 7) and `test_advisory.py` (step 8) were NOT driven.**
   #228 measured why: the harness `copytree`s the whole of `knowledge/` (5.2 G) and no volume here
   has the room. Re-measured today: `/` 4.8 G free, `/sessions` 3.2 G, `/dev/shm` 512 M. Nothing
   in this lane touched a gate's failure class.
3. **The release fingerprint is UNPROVEN.** `6e7c6dd4…` is a determinism proof, not a release
   fingerprint. Two shipped paths move it. Named, measured, above.
4. **`--check` on the twin is RED, expectedly, and its green is UNPROVEN until the commit.**
   `CHECK RED — 3 file(s) differ from the commit's blobs: memento-package/_state.json,
   memento-package/runbooks/_RUNBOOK-capture-ritual.md,
   memento-package/runbooks/_RUNBOOK-context-gauge.md` — exactly the three stamped Gumdrop paths,
   exactly what the stamp block's own comment predicts while the repo copy is unsynced-in-commit.
   The working tree IS synced; only the commit is missing.

Also unproven and unchanged: `_validate_state_contrast` (no playwright; the `render` job is its
proof of record) and the two `_gen_schematic` arms (`real` tier unreachable, #173/#183) — three
standing `COULD-NOT-ASK` refusals, not verdicts.

---

## WHAT WAS DRIVEN

```
knowledge/_build_survey.py --timeout 60 --range 1:45 / 46:100 / 101:140   before  4 reds named
knowledge/_build_survey.py --timeout 60 --range 1:70 / 71:140             after   4 reds
knowledge/tokens/_build_blast_radius.py                        rc=0    regenerated (2 files)
knowledge/tokens/_build_blast_radius.py --check                rc=0    PASS (was FAIL)
knowledge/_release/_gen_pack_manifest.py --selftest            rc=0    216 bites, 0 fail
knowledge/_release/_gen_pack_manifest.py --probe   --commit 7a43f14   rc=0
knowledge/_release/_gen_pack_manifest.py --manifest --commit 7a43f14  rc=0  1659 files, 6937f2d4cd441dd8
knowledge/_release/_gen_pack_manifest.py --page <go/no-go>     rc=0    135,378 → 135,746 bytes
knowledge/_review/_make_review.py <go/no-go>                   rc=0    overlay re-injected (x2)
knowledge/_release/_gate_release_audit.py --check              rc=0    byte-identical at 7a43f14cbba6
knowledge/_release/_gate_release_audit.py --selftest           rc=1    10 bites, 1 fail — the PRE-BAKE arm
knowledge/_release/_gate_frozen_release.py --check             rc=0    3 releases, no frozen surface moved
knowledge/_release/_gate_frozen_release.py --selftest          rc=0    14 bites, 0 fail
knowledge/_validate_package_delta.py                           rc=0    0 failures
knowledge/_validate_wiring.py                                  rc=0    0 failures, 49 wired
apollo-spider/cold-start/gen_projections.py --check            rc=0    3 projections in sync
apollo-spider/build-designer-pack.sh --selftest                rc=0    all three refusals GREEN,
                                                                       incl. "PROPOSED, so --release cannot run"
build-designer-pack.sh --dry-run (x2, two directories)         rc=0    cmp: BYTE-IDENTICAL, 19,948,872 bytes
build-designer-pack.sh --check <twin> --commit 7a43f14         rc=1    3 stamped Gumdrop paths — expected
```

⚠ `TMPDIR` and both dry-run out-dirs were put on `/sessions/…/tmp`, **not `/var/tmp`** — the
`[[sandbox-html-rendering]]` #227 sixth-stratum warning that `/var/tmp` is a trap on this VM.
`/dev/shm` (512 M) was too small for two 42 MB stages plus zips with margin.

---

## REPLAY-THESE — for the conductor

1. ⛔ **Do not quote `6e7c6dd4…` to Dave as the v1.0.5 fingerprint.** It is a determinism proof
   at `7a43f14`. Two shipped paths move it. § THE FINGERPRINT IS PROVISIONAL names them.
2. **Reconcile and commit the eleven dirty paths** (listed in § THE FINGERPRINT IS PROVISIONAL).
   ⛔ `notes/_REHEARSAL-LOG.jsonl` was NOT touched by this lane; if it is dirty it is the
   `s137-D1` instrument dirt, as at #228.
3. **Then re-run the cut's tail, in this order** — the ship list must be regenerated at the commit
   that contains the fixes, or the stale-copy-list defect returns wearing the version's clothes:
   `--probe` → `--manifest --commit <new sha>` → `--page` → `_make_review.py` → two `--dry-run`s
   into two throwaway dirs → `cmp`. That gives the **real** fingerprint for Dave's page.
4. **Put ruling-shaped item 1 to Dave (or rule it as conductor):** the brief says bake; its own ⛔
   and the no-git / no-rulings fences say stop. This lane stopped. If a bake is wanted tonight,
   name who commits and what ruling id v1.0.5's `RATIFY_IDS` row points at.
5. **When Dave gives the word, add ONE row:** `RATIFY_IDS["v1.0.5"] = "<his fresh ruling id>"` in
   `knowledge/_release/_gen_pack_manifest.py`, regenerate the manifest, then `--release`. The
   comment block at the site says this. ⛔ Key it to a **ratifying** word, not an authorisation —
   that is the whole content of `s228-D1`/`s228-D4`.
6. **`[136]` will be red in CI for the whole PROPOSED window.** Expected, named, and it closes at
   the bake. Ruling-shaped item 2 asks whether the arm should learn to read `status`.
7. **`[61]` and `[111]` are one regeneration each** (`gen_dashboard.py`, `_build_memento_index.py`)
   and neither ships. Left alone deliberately per the brief's declared-gates-red clause.
8. **`[18]` is still Dave's** — the #218 double self-report. Unchanged since #228.
9. **The FIRST-SESSION.md version literal has now recurred across two consecutive cuts with an
   open permanent fix.** Promote `_gate_pack_docs.py` arm 3 to blocking, or extend the bake-time
   stamp to `Apollo-Spider-v*`. ⬛ Dave's, priced at #228, overdue now. `s228-D7` applies.
10. **Mint this report's store row** — this lane wrote no W-row and no state
    `[[forgotten-document-class]]`.
11. **Roster is 58 and was not touched.** `s228-D5` holds; the explicit-membership fix is W-303 and
    was not this lane.
