# #255 VERIFIER V-A — cold seat on BUILD LANE A (the five bento-template reds)

**Date** 2026-09-07 · **Subject** `notes/_subreports/2026-09-07-255-BUILD-A.md`
**Seat** COLD. Read-only on the repo except this file. No `git checkout` / `stash` / `reset` / commit.
Every mutation was driven on a `/tmp` copy. `_build_all.py` NOT run.
**Verdict** **ACCEPT.** All five findings are green in the tree, and `canon.css` is proven a
FIXED POINT of its two generators (byte-identical after regeneration — the strongest form of the
"not hand-edited" claim). Two MISMATCHES, both pasted-receipt text, neither touching a fact.

`COUNTS: claims 25 · green 23 · red 0 · mismatches 2`

Method: s182-D1 — one row per mechanical claim, each carrying the probe DRIVEN. The report's own
pasted output was never trusted; every command was re-run and diffed. Grade counts by CLAIM, not
by the lane's own 14-item numbering (my rows split several of its compound claims).

---

## Rows — the BEFORE state the lane says it inherited

| # | Claim | Probe driven | Grade |
|---|---|---|---|
| 1 | 2× off-grid `gap: 6px` (canon + snippet), GRID GATE FAIL | `git show HEAD:` both files → `/tmp/va/head/`, `_validate_grid.py` on the pair → `✗ off-grid: gap: 6px` ×2, `GRID GATE FAIL — 2 off-grid layout value(s).` verbatim | GREEN |
| 2 | Fork `--bento-cols-now`, `canon.css:1142` "1" vs `:17304` "4"; `FORK 47 · UNIFORM 526`; `GATE RED: 1 fork(s)` | HEAD copy of `knowledge/canon/*.css` (canon.css + type.css = the DEFAULT_GLOB set) → `FORK 47 · UNIFORM 526`, `GATE RED: 1`, and the fork block prints `:1142` and `:17304` with those selectors. **Exact, line numbers included** | GREEN |
| 3 | 2 ds-018 failures: `--layout-bento-caption-space`, `--layout-bento-packing` | reproduced by mutation bite 2 below — deleting the lane's two declarations raises exactly those two named failures, wording identical to the report's paste | GREEN |

## Rows — items 1+2, the 4px gap

| # | Claim | Probe driven | Grade |
|---|---|---|---|
| 4 | Snippet edited at the SOURCE, `gap:4px` | `grep -n "summary__k{ display:flex" …reference.html` → `865:  .tpl-na-head .summary__k{ display:flex; flex-direction:column; gap:4px; }` — line number and text both exact | GREEN |
| 5 | `canon.css` hunk is `gap:6px` → `gap:4px` only | `git diff knowledge/canon/canon.css` read in full: that hunk is the gap line + the 4px-vs-8px decision comment carried with it. Nothing else | GREEN |
| 6 | `GRID GATE PASS — … (152 file(s))` | driven → identical, exit 0, selftest line above it | GREEN |

## Rows — item 3, the `--bento-cols-now` fork

| # | Claim | Probe driven | Grade |
|---|---|---|---|
| 7 | 10a → `grid-template-columns:repeat(4,minmax(0,1fr))`; 6b(ii) → `repeat(var(--bento-columns),…)`; both in snippet AND projected copy | both diffs read in full; both files carry both rewrites plus the 10a rationale comment | GREEN |
| 8 | The ONLY reader of `--bento-cols-now` is `grid-template-columns` | `grep -n "var(--bento-cols-now" knowledge/canon/canon.css` → **1088 and 16852** (report pasted **16850**). Snippet: line 360, same property. Tree-wide sweep of `knowledge/`: the only other live consumer is `_validate_composition.py`'s `bands()`, which the report itself names. **The claim is TRUE; the pasted line number is not** — 16850 is the adjacent `--bento-cols-now:var(--bento-columns)` *declaration*, not a reader | **MISMATCH** |
| 9 | New 10a out-specifies canon's base rule, (0,4,0) vs (0,1,0) | base rule at `canon.css:16850` is `:where(.cn-template-dashboard-bento) .c-bento__grid` — `:where()` contributes 0, one class ⇒ (0,1,0). 10a is `:where(…) .tpl-page .c-bento.tpl-group-lead > .c-bento__grid` ⇒ four classes, (0,4,0). Wins | GREEN |
| 10 | Rendered geometry unchanged | **See GEOMETRY below. `.tpl-group-lead` renders at 4 columns at the widest band, and at 4 at EVERY band — identical to pre-change** | GREEN |
| 11 | `GATE GREEN: no UNDECLARED fork.` · `FORK 46 · UNIFORM 527` | driven on the tree, default glob → `FORK 46 · UNIFORM 527`, `GATE GREEN`, exit 0. Delta from row 2 is exactly −1 fork / +1 uniform | GREEN |
| 12 | Ledger NOT touched; 51 declared forks before and after | `git diff --stat -- knowledge/_TOKEN-FORK-LEDGER.json` → empty; gate header prints `51 declared fork(s)`. The fork was REMOVED, not filed | GREEN |
| 13 | `_validate_composition.py`'s `bands()` reads the name only inside `@container (max-width:…)`; neither edited rule is one | L95–107 read: the loop is `re.finditer(r"@container[^{]*\(max-width:\s*(\d+)px\)…")`. 10a is unconditional, 6b(ii) is `min-width:821px`. Neither is matched. Nothing that gate measures moved | GREEN |

## Rows — items 4+5, the two unreachable names

| # | Claim | Probe driven | Grade |
|---|---|---|---|
| 14 | Both names declared in BOTH theme blocks; `grep -c` → 2 | driven → `2`. Diff confirms one insertion in the `:root`/light block and one in `[data-theme="dark"]`, identical text | GREEN |
| 15 | Declared, NOT fallback-patched — references stay bare | `grep` for both `var(--layout-bento-*)` reads: `343: --bento-packing: var(--layout-bento-packing);` and `482: --bento-caption-space:var(--layout-bento-caption-space);`. No comma, no fallback, 1× each — matching the gate's "referenced 1×" | GREEN |
| 16 | Values are canon's own, `canon.css:540–541` | `sed -n '540,541p'` → `--layout-bento-packing: row dense;` / `--layout-bento-caption-space: 86px;`. Both trace to `knowledge/tokens/layout.json` `bento/packing` (`cssKeyword: "row dense"`) and `bento/caption-space` (`$value: "86px"`, the s217-D3 derivation). Not invented | GREEN |
| 17 | `property-resolves gate (C2): 148 file(s), 0 failure(s)` | driven → identical, exit 0 | GREEN |

## Rows — the AFTER block and the lane's discipline claims

| # | Claim | Probe driven | Grade |
|---|---|---|---|
| 18 | `snippet gate: 136 snippet(s), 0 failure(s)` | driven → identical, exit 0 | GREEN |
| 19 | `gen_canon_bento selftest OK (13 bites: …)` | driven → identical string, all 13 bites named | GREEN |
| 20 | `gen_canon_components.py` → `generated 136 components -> .cn-<scope>` | driven → **`gen_canon_components: no change (136 components in sync).`** Different line. The generator is idempotent and prints a different message when it writes nothing; the lane's run was the one that WROTE the block, so its line was true then and is unreproducible now. Count 136 agrees. Receipt text differs from what I observe ⇒ recorded as a mismatch, not a red | **MISMATCH** |
| 21 | **`canon.css` was NOT hand-edited — it moved only under the generators** | `--help`/docstrings of both generators read first (`gen_canon_bento.py` mints the AUTO-BENTO block from `tokens/layout.json`; `gen_canon_components.py` regenerates AUTO-COMPONENTS verbatim from the gated snippets). `shasum` of the tree file taken to `/tmp/canon.pre.css` = `f8d1ba95…`; **both generators re-run in order**; `cmp` → IDENTICAL, `git diff --numstat` unchanged at `20 4`. **canon.css is a fixed point of its generators.** Nothing beyond the lane's diff appears | GREEN |
| 22 | `git diff --stat knowledge/canon/canon.css → 20 insertions(+), 4 deletions(-)` | `git diff --numstat` → `20	4	knowledge/canon/canon.css` | GREEN |
| 23 | No gate file edited | `git diff --stat -- 'knowledge/_validate_*.py'` → **empty** | GREEN |
| 24 | Nothing outside scope touched | full `git status --porcelain`: modified = `canon.css`, the snippet, and the three harness logs (`knowledge/_graph-mark-observations.jsonl`, `notes/_REHEARSAL-LOG.jsonl`, `notes/_dream/_GRADE-DECISIONS.jsonl`). Untracked = the lane's own report + `knowledge/_probe/session-255.json`, which is the session memory-plant (`"session": 255, "planted_at": …, salted K2/K6 facts`) — the conductor's, not a lane artefact | GREEN |
| 25 | The `_validate_composition.py --selftest` abort is PRE-EXISTING, not this lane's | driven → `AssertionError: <div class="c-bento__tile kpi-tile has-cta" … data-c="3" data-r="1">` at L378. Independently corroborated: the snippet diff touches only the `<style>` block — **no DOM line is in the diff at all**, so no lane edit can reach that fixture | GREEN |
| 26 | No `[data-apollo-theme]` block declares a `layout-bento` name | `grep -c 'data-apollo-theme.*layout-bento' knowledge/canon/canon.css` → `0` | GREEN |

---

## GEOMETRY — the R1 column-count question, answered directly

**`.tpl-group-lead` renders at 4 columns at the widest band. That EQUALS the pre-change 4.**

Pre-change, at the widest band (no `@container` matches): 10a set `--bento-cols-now:4` at (0,4,0),
beating canon's `.c-bento__grid{--bento-cols-now:var(--bento-columns)}` at (0,1,0); the base
`grid-template-columns:repeat(var(--bento-cols-now),…)` then read 4. **⇒ 4.**
Post-change: the base declaration still reads `--bento-cols-now` (now resolving to 6 in that
subtree), but 10a's own `grid-template-columns:repeat(4,…)` at (0,4,0) out-specifies it. **⇒ 4.**

It is also unchanged at *every narrower band*, which is the part worth stating: the band rules
(`:where(…) .c-bento__grid{--bento-cols-now:3|2|1}`, canon.css:16894/16898/16902) are all (0,1,0),
so pre-change they never reached this grid either — the lead group was 4-across at all bands
before and is 4-across at all bands now. The `[data-c]` span clamps that ride in the same band
blocks are separate selectors and were not touched. `.tpl-group-context` (6b(ii)) is likewise
equivalent: 6 above 821px by an out-specifying rule, band-driven 3/2/1 below it, either way.

---

## MUTATION BITES — three driven, all on `/tmp` copies

**Bite 1 · the grid gate.** `/tmp/va/mut-grid.html` = tree snippet with line 865 reverted to
`gap:6px` (one-line `diff` shown). `_validate_grid.py` takes file arguments (`--help` read first).
```
control (tree copy) → GRID GATE PASS — all layout dimensions on the 4px grid (1 file(s)).
mutant              → ✗ off-grid: gap: 6px  (…/mut-grid.html) — not a 4px multiple
                      GRID GATE FAIL — 1 off-grid layout value(s).
```

**Bite 2 · the property-resolves gate.** This gate takes NO file argument — it globs
`REPO/knowledge/snippets/*.reference.html`, `REPO = dirname(dirname(__file__))`. So an isolated
repo was built at `/tmp/va/repo/knowledge/` (the gate + `_helpgate.py` + one snippet), which makes
`REPO` resolve to `/tmp/va/repo` with no repo file involved. Mutant = the two declaration lines
deleted from both theme blocks.
```
control → property-resolves gate (C2): 1 file(s), 1 failure(s)      [the 1 is an artefact of the
          isolated copy: "<link> to ../canon/type.css does not exist" — not in scope]
mutant  → ❌ FAIL … `--layout-bento-caption-space` referenced 1× with no fallback and declared
             nowhere this file can reach … (ds-018)
          ❌ FAIL … `--layout-bento-packing` … (ds-018)
          property-resolves gate (C2): 1 file(s), 3 failure(s)
```
Delta is exactly +2, and the two are exactly the two names the lane claims to have fixed.

**Bite 3 · the fork gate** (not required, driven because R1 is the load-bearing claim).
`/tmp/va/forks/canon.css` = tree canon.css with 10a reverted to `--bento-cols-now:4;`. Gate run
with `--glob /tmp/va/forks/*.css --ledger knowledge/_TOKEN-FORK-LEDGER.json`.
```
control → GATE GREEN: no UNDECLARED fork.
mutant  → FORK  --bento-cols-now  theme=mono mode=any
             …/canon.css:1142   .c-bento__grid                          --bento-cols-now: 1 -> 1
             …/canon.css:17316  :where(.cn-template-dashboard-bento) … .tpl-group-lead > .c-bento__grid
                                                                       --bento-cols-now: 4 -> 4
          GATE RED: 1 fork(s) not in the ledger
```
This reproduces the BEFORE block the report pasted, name for name (line 17316 vs HEAD's 17304 —
the lane's comment added twelve lines above it).

---

## RULING-SHAPED — carried forward, not graded

**R1 stands, and its warning is the real deliverable.** The lane's claim that the
`--bento-columns` dial route *moves* the fork rather than removing it follows from the gate's own
scope key (`context_of` takes the LAST `.cn-*` class), which I read: every rule the projector
prefixes with `:where(.cn-template-dashboard-bento)` collapses to ONE scope holding ONE value per
property name. So the lane's ⚠ is correct and is Dave's to rule, not a lane's: **under this gate no
per-instance column count can be expressed on `--bento-cols-now` or `--bento-columns` inside a
`.cn-*` projection.** If Dave wants the dial route, `_validate_token_forks.py`'s scope key is what
has to change. I did not re-derive the lane's VAR-A/VAR-B measurements; bite 3 proves the shipped
shape (VAR-C) is the one the gate accepts, which is what the tree has to be true of.

**R2 (4px, not 8px) is a design call, correctly flagged strikeable.** Both values are on the grid,
so no gate decides it. The lane's reasoning — `margin-bottom:8px` already separates the head row
from the body, so an 8px inner stack would flatten the key/caption pair to peers — is checkable and
checks out (`canon.css:17329`, `.tpl-na-head{margin-bottom:8px …}`, unchanged). Dave's to strike.

**The one carry worth keeping:** `gen_canon_components.py` parses custom-property syntax out of
PROSE, so a `--name:value` pair inside a snippet comment is emitted into `canon.css` as a real
declaration. The lane hit it, fixed it, and left a ⚠ beside the comment. The shipped tree is clean
(fixed-point test, row 21), but this is a generator sharp edge that will bite again.

---

## Both mismatches, stated plainly

Neither is a false fact and neither is a red. Both are pasted receipts whose text differs from what
I observe: **(a)** the fork item's receipt cites `canon.css:16850` for the second
`grid-template-columns` reader; it is `16852` (16850 is the adjacent declaration line). The claim
it supports — that `grid-template-columns` is the only reader — is TRUE. **(b)** the AFTER block
pastes `generated 136 components -> .cn-<scope>` for `gen_canon_components.py`; re-run it prints
`gen_canon_components: no change (136 components in sync).` The lane's run wrote the block and mine
did not, so the difference is expected — but a report that says "re-run this" should paste the line
a re-run produces.
