# #219 seam 6 — the reconcile, and THE OWED INDEX REGEN

**Seat:** seam-6 sub, Opus. **Base:** `11b5779` + conductor telemetry + lane C (`enact-C`, sole lane).
**Brief:** `notes/_briefs/2026-08-25-219-crank-divvy.md`. **Lane report replayed:**
`notes/_subreports/2026-08-25-219-enactC-chords.md`.
**Mandate:** prepare the tree. **NO COMMIT, NO PUSH** — and none was made.

**TREE READY.** 16 lines at entry, **22 at hand-off**, every one attributed, **0 UNCLAIMED**.
The library index owed by lane C's finding 11 is **CLOSED**: `showroom/index.html` + `index.json`
carry `foundation-bento-rails`, `$count` 142 → 143, `$foundation_count` 7 → 8. The thumbnail is
**SHOT, NOT OWED** — `gen_thumbs.py` takes `--range`, so exactly **one** page was shot and the other
142 were not touched. Lane C's UNPROVEN ④ (narrow width) **closes GREEN**: every resolved value at
480px is identical to 1280px, cell for cell.

Two things the serial surfaced that no lane's green could see: **`knowledge/_consult-index.json` has
been STALE AT HEAD since seam 3** (§4), and **`data-apollo-theme="mono"` is a NO-OP as a pin** —
`[data-apollo-theme="mono"]` occurs **zero** times in `canon.css`, so three specimens on the new page
render the PAGE's theme (§6). Both are REPORTED, neither repaired.

**COUNTS:** tree lines **22** (16 in, +6 this seam) · UNCLAIMED **0** · manifest rows 22 · verdicts
quoted **35** (10 serial steps · 3 `--check` · 4 selftests · 4 CI-drift · 5 store/doc-row · 3
narrow-width · 6 misc) · findings 6 · ruling-shaped 2 · UNPROVEN 3 · store rows minted 1 (`W-186`, this
report) · lane C's 5 ruling-shaped questions **UNTOUCHED, still open** · subs 0

---

## 1 · THE RECONCILE — 21 paths, 0 unclaimed

Ownership is taken from each writer's own declaration, then **probed**, never assumed. Lane C's
"Files this lane owns and touched" list is the authority for its six; the conductor's two telemetry
paths were each opened and read before being called telemetry.

### At entry — 16 lines

| path | st | owner | the evidence that ownership is real |
|---|---|---|---|
| `knowledge/_render/gen_bento_matrix_217.py` | M | **lane C** | named in lane C §"Files this lane owns and touched"; the chord/exclusion layer |
| `knowledge/_render/gen_foundations_217.py` | M | **lane C** | named; `page_bg_decl()` / the page rail |
| `knowledge/_render/gen_grids_218.py` | M | **lane C** | named — "one filter line", `controls_for()` names the page group |
| `knowledge/_render/gen_library_214.py` | M | **lane C** | named — "one FOUNDATIONS entry" |
| `knowledge/_render/verify_bento_matrix_217.py` | M | **lane C** | named; the new chord + page-rail browser arms |
| `knowledge/_render/_bento_edit_rails.json` | M | **lane C** | named — regenerated by `--rails` |
| `showroom/_foundations/bento.html` | M | **lane C** | named; generated by `gen_foundations_217` |
| `showroom/_foundations/grids-display.html` | M | **lane C** | named |
| `showroom/_foundations/grids-gallery.html` | M | **lane C** | named |
| `showroom/_foundations/grids-dashboard.html` | M | **lane C** | named |
| `showroom/_foundations/photography.html` | M | **lane C** | named |
| `notes/_subreports/2026-08-25-219-enactC-chords.md` | ?? | **lane C** | its own filed report; store row `W-99zr` homes here |
| `showroom/_foundations/bento-rails.html` | ?? | **lane C** | the new page; store row `W-99zs` homes here |
| `knowledge/_rulings.json` | M | **conductor** | see §2 — a byte-clean append of ONE object, `s219-D3` |
| `knowledge/_state.json` | M | **conductor / lane C's mint** | see §2 — `+26/−0`, two whole objects, `W-99zr` + `W-99zs` |
| `notes/_REHEARSAL-LOG.jsonl` | M | **conductor telemetry** | `+1/−0`; the appended line is `{"date":"2026-08-25", … "kind":"wrap-open", …}` — a check-in append |

**Lane C's negative claims hold under probe.** It declared `logos.html` and `grids-12col.html` were
rebuilt and came out **byte-identical**; neither appears in `git status`. It declared it did **not**
touch `_rulings.json`, `GOOD-MORNING.md`, `_CHAIN.md`, `_LIVE-STATE.md`, constants or
`notes/_receipts/**`; none of those is dirty except `_CHAIN.md`, which **this seam** regenerated (§5)
and whose staleness is provably lane C's *store rows*, not a lane C edit. An unmatched `git status`
line is not on its own an absence — each of these was checked BY PATH
([[unmatched-grep-is-not-an-absence]]).

**`_to_delete/` is gitignored and therefore absent from `git status` by design** — confirmed against
`.gitignore`'s `_to_delete/` clause, not inferred from the silence. Lane C's eight full-page + eight
section shots are at `_to_delete/_shots219c/` for the conductor.

### Added by THIS SEAM — 6 lines

| path | st | why |
|---|---|---|
| `showroom/index.html` | M | **the owed regen** — `gen_library_214.py`, serial's last generator step (§3) |
| `showroom/index.json` | M | same run, same generator |
| `showroom/_thumbs/foundation-bento-rails.png` | ?? | **one** targeted `gen_thumbs.py --range 63:64` shot (§3) |
| `knowledge/_consult-index.json` | M | `_build_consult_index.py` — **stale at HEAD since seam 3** (§4) |
| `_CHAIN.md` | M | `_gen_chain.py` — stale because lane C's two store rows are rendered into it (§5) |
| `notes/_subreports/2026-08-25-219-seam6-reconcile.md` | ?? | this report (+ its store row `W-186`, §2) |

16 + 6 = **22**, and `git status --porcelain | wc -l` reads **22** — quoted in §7. The two numbers are
made to meet on purpose: a manifest that does not reconcile to the tool's own count is a table, not a
reconcile.

**UNCLAIMED: none.**

---

## 2 · THE TWO TELEMETRY FILES WERE VERIFIED BEFORE BEING CALLED TELEMETRY

⛔ Both are on the DO-NOT-TOUCH list, so neither was edited. Being forbidden to edit a file is not a
reason to include it unread — a conductor commits what the reconcile vouches for.

**`knowledge/_rulings.json` — `git diff --numstat` = `16 0`.** Sixteen insertions, **zero deletions**.
The diff is ONE whole appended object, `s219-D3`, inserted after the last existing entry with the
closing `]` untouched. No standing ruling is edited, reworded or reordered. **Byte-clean append —
INCLUDE.** (`_inscribe_ruling.py` is the only legitimate writer; the shape is consistent with it.)

**`knowledge/_state.json` — `git diff --numstat` = `26 0` AT ENTRY, `42 0` at hand-off.** Two whole
appended objects at entry and nothing removed, so no concurrent row could have been clobbered. ⚠ The
extra **16 insertions, 0 deletions** are **this seat's own row `W-186`** (§11) — declared here rather
than left for the conductor to discover a number that no longer matches the lane's report:

- **`W-99zr`** — home `notes/_subreports/2026-08-25-219-enactC-chords.md`, owner **dave**, state
  open, `closes_when` names the five ruling-shaped questions. **INTACT, unedited.**
- **`W-99zs`** — home `showroom/_foundations/bento-rails.html`, owner **dave**, state open,
  `closes_when` names *"AND the library index has been regenerated at the regen seam so the new
  Foundations entry is reachable from `showroom/index.html`"*. **INTACT, unedited.**

⚠ **`W-99zs`'s close condition has TWO clauses and this seam satisfied exactly ONE.** The index
clause is now met (§3, quoted verdict). **The other clause is Dave's eye and remains open** — the row
stays `open` and was not touched. Naming which half moved is the point; a row half-satisfied and
reported as satisfied is the [[premise-ages-faster-than-rule]] failure.

**`notes/_REHEARSAL-LOG.jsonl` — `+1/−0`.** One appended line, `kind: "wrap-open"`, 27 warns / 0
fails / 0 structural. Append-only telemetry. **INCLUDE.**

---

## 3 · THE OWED INDEX REGEN — the ordered serial, run WHOLE, ramp first / index last

Lane C's finding 11 and UNPROVEN ①: *"`showroom/index.html` + `index.json` are OUT OF SYNC because
the new Foundations entry needs the library index regenerated — and that is the regen serial's LAST
step, which this lane was told not to run."* The red was **reproduced before it was fixed**:

```
gen_library_214 --check: OUT OF SYNC — ['showroom/index.html', 'showroom/index.json']
Run: python3 knowledge/_render/gen_library_214.py
```

Order taken from `knowledge/_build_all.py::STEPS` and the precedent set at seam 1 of this same
session — **never `_build_all.py` itself** (a partial run strands the tree), every step driven
**individually** as its own bash call, `tiktoken` resident and confirmed (`import tiktoken` → ok)
before anything else ran, so no build verdict below is a missing-dependency artefact.

| # | step | verdict (VERBATIM) | rc |
|---|---|---|---|
| 0 | `canon/gen_canon_tokens.py` | `TOTAL: 577 root vars, 195 dark overrides` / `Wrote …/knowledge/canon/canon.css` | 0 |
| 1 | `gen_token_ramp.py` **(RAMP FIRST)** | `gen_token_ramp: 0 file(s) synced (0 with block, 0 block-removed), 147 already in sync.` | 0 |
| 2 | `canon/gen_canon_components.py` | `gen_canon_components: no change (135 components in sync).` | 0 |
| 3 | `gen_snippet_tokens.py` | `4804 manifest bindings across 135 snippets + 9 tranches; 0 value(s) projected; 0 canon.css literal(s) projected.` / `OK — snippets + tranches + canon.css in sync with tokens.` | 0 |
| 4 | `canon/gen_theme_cascade.py` | `gen_theme_cascade: no change (in sync).` | 0 |
| 5 | `gen_showroom.py` | `135 page(s) -> showroom/ (0 written, 0 orphan(s) pruned; index owned by knowledge/_render/gen_library_214.py)` | 0 |
| 5b | `_render/gen_foundations_217.py` | `8 page(s) -> showroom/_foundations/ (0 written)` · `squaring pass … squared — ladder (4, 3, 2, 1), rows 96 / 128 / 191 / 251, 4 tile(s) re-spanned` · `photography: 251 derivative(s), residual missing-file: none` · `logos: 12 variant(s), residual missing-file: none` | 0 |
| 5c | `_render/gen_thumbs.py --range 63:64` | `gen_thumbs: font probe {'target': 347, 'alias_uf': 347, 'alias_font': 347, 'control_real': 375, 'control_absent': 301}` / `gen_thumbs: 1 shot, 0 failed -> showroom/_thumbs/` | 0 |
| 6 | `_render/gen_library_214.py` **(INDEX)** | `gen_library_214: 135 component(s) + 8 foundation(s) -> showroom/index.html + showroom/index.json (2 file(s) written)` · `missing thumbnail: 0 — none` · `foundation page missing: none` | 0 |
| 7 | `_build_memento_index.py` **(INDEX LAST)** | `memento index: 1791 records → knowledge/_memento-index.json (brief:108, component-meta:137, context-node:216, dream:9, gauge-block:199, gm-archive-section:188, gm-section:20, lane:4, ledger-section:86, ls-archive-section:385, ls-section:11, pattern-node:374, runbook-section:54)` | 0 |

**Step 5b is in the serial DELIBERATELY, and it wrote 0 pages — which is the load-bearing fact.**
`gen_foundations_217.py` is the one writer of `showroom/_foundations/*.html`. It reported `0 written`
on a tree lane C had edited, so **every foundations page on disk — `bento-rails.html` included — is
byte-identical to the artefact lane C verified in the browser**, and lane C's eight-state greens are
not being inherited across a changed file. Step 7 likewise left `_memento-index.json` byte-identical
(the file is absent from `git status`), so nothing there drifted either. `notes/_subreports/` is
**not** in the memento corpus (`_build_memento_index.py` globs `notes/_briefs/*.md`), so this report
cannot stale that index.

### ⛔ THE INDEX PICKED UP THE ENTRY — measured in the artefact, not in the banner

Re-read from the generated files, never quoted from the run's own output
([[enactment-register-adr-0016]]):

```
showroom/index.json   "$count":            142 → 143
                      "$foundation_count":   7 → 8
                      + { "slug": "foundation-bento-rails", "name": "Bento rails",
                          "level": "foundation", "level_label": "Foundations", "group": null,
                          "page": "_foundations/bento-rails.html",
                          "thumbnail": "_thumbs/foundation-bento-rails.png",
                          "usage": "structure", "status": "beta", "foundation": true, … }
showroom/index.html   3 occurrences of `foundation-bento-rails`
                      (bento-rails · bento-rails.html · bento-rails.png)
git diff --numstat    showroom/index.html  5 5   ·   showroom/index.json  19 2
```

`group: null` is lane C's deliberate choice preserved through the regen — grouping would pre-empt the
library IA v2 word-set (`W-99zg`). The generator did not invent one.

**Verdict after:** `gen_library_214 --check OK — 143 component(s), index + index.json + stub in sync.`

### THE THUMBNAIL — SHOT, and 142 files NOT churned

The brief's own condition: *"if `gen_thumbs` re-shoots everything and would churn, shoot ONLY the new
page's thumb if the machinery allows targeting, else declare the thumb owed."* **The machinery allows
targeting**, so the thumb is shot rather than declared — this was READ out of the generator, not
assumed from seam 1's precedent of skipping it ([[feedback-read-the-runbook]]):

```
gen_thumbs.py  --range lo:hi   # slugs [lo,hi) of the sorted list      → targeting EXISTS
               --resume        # skip slugs already on disk
```

The target was **derived, never counted by hand**: `gen_thumbs.slugs().index("foundation-bento-rails")`
→ **63**, and an independent sweep of the same list for absent PNGs returned
`missing: ['foundation-bento-rails']` — **exactly one**, which is lane C's UNPROVEN ② stated as a
measurement. `--range 63:64` shot that one page.

```
git status --porcelain -- showroom/_thumbs/   →   ?? showroom/_thumbs/foundation-bento-rails.png
                                                  (ONE line — the other 142 are untouched)
PNG header                                    →   320 × 200          (the convention: 640×400 @ dsf 0.5)
gen_library_214 residual · missing thumbnail  →   0 — none           (was: 1)
```

Seam 1 of this session **restored 11 pixel-identical re-encodes** that a blind full `gen_thumbs` run
had produced. Running the generator whole here would have manufactured that churn at 142 files. The
`--range` arm makes "shoot the new one" and "churn nothing" the same command.

---

## 4 · ⛔ FINDING: `_consult-index.json` HAS BEEN STALE AT HEAD SINCE SEAM 3

Driven because the brief named `[107]` and **`[107]` no longer points where it pointed** (§8). The
step that currently sits at `STEPS[107]` is `_build_consult_index.py`, so it was driven — and it
**wrote**:

```
git diff --numstat knowledge/_consult-index.json     →   11 3
   "count": 759 → 760
   "gate":   42 → 43
 + { "id": "validate-hidden-display", "kind": "gate",
     "text": "[hidden]-vs-author-display sweep — ADVISORY. Never blocks, never writes.",
     "file": "knowledge/_validate_hidden_display.py", "status": "BLOCKING", … }
   one `bite` string refreshed (a token-file list: `motion.json` → `spacing.json`)
```

**This is not lane C's and not this seam's.** Proved by clock, not by inference:

```
git log -1 --format=%h -- knowledge/_validate_hidden_display.py   →  d11b6a6   ("#219 crank seam 3")
git log -1 --format=%H -- knowledge/_consult-index.json           →  adb5130   (earlier)
```

The gate landed at seam 3; the index it belongs in was last written before that. **Three seams have
committed since without the consult index catching up**, because `_build_consult_index.py` is not in
the "ramp first / index last" set the seams have been driving — it is a *different* index, at a
different STEPS position, and the shorthand hid it. Re-running is **idempotent** (`md5sum` identical
across two consecutive runs), so this is staleness, not nondeterminism.

⚠ **The advisory/blocking mismatch is left exactly as generated.** The row's own `text` says
*"ADVISORY. Never blocks"* while its `status` reads `BLOCKING` and its `bite` reads
`"unknown — read the script"`. That is what the generator derived from the gate as it stands. Fixing
it would be a promotion of an advisory gate, which the brief's DO-NOT-RULE list forbids to every
lane. **Surfaced, not repaired.**

---

## 5 · `_gen_chain.py --check` WAS RED — and the cause is not where the message points

```
✗ _CHAIN.md is STALE — it does not match GOOD-MORNING.md / _LIVE-STATE.md as they now stand …
rc=1
```

The message names two files that are both **clean at HEAD** — `git status -- GOOD-MORNING.md
_LIVE-STATE.md _CHAIN.md` returns EMPTY. Reading the generator rather than the banner
([[feedback-read-the-runbook]]) resolves it: `_gen_chain.py:326` renders the **OPEN WORK** section
`from _state.json, never from prose`. **Lane C's two store rows are the staleness.** The regenerated
diff is exactly that and nothing else:

```
git diff --numstat _CHAIN.md   →   5 3
- 249 items · 170 live · 92 Dave's · 78 mine · 235 … 14 UNCONDITIONED
+ 251 items · 172 live · 94 Dave's · 78 mine · 237 … 14 UNCONDITIONED
+ `W-99zr` … + `W-99zs` (both with their ratified closes_when)
- (Chain ends. 43,906 real …   →   + 44,183 real …
```

Regenerated per the brief's *"regen if stale"*. **`_LIVE-STATE.md` was NOT touched and did not need
to be** — it is not the source of that block; `grep -c W-99zr _LIVE-STATE.md` → **0**, `_CHAIN.md` →
**1**.

**It was then regenerated a SECOND time**, after this seat minted `W-186` (§2), because the same
mechanism applies to this seat's own row. Skipping that pass would have handed the conductor a chain
that was fresh for lane C's rows and stale for mine — the [[wrap-skipped-chain-certifies-wrong-session]]
shape at one row's scale. Final state, re-checked:

```
✅ _CHAIN.md is FRESH — byte-matches the live chain · GM header+LATEST 23644 tk ·
   LS LATEST delta only (of 52 delta lines) 6330 tk · FILE 44,189 real =
   slice 29,974 + wrapper 14,215 · fixed point in 2 pass(es)          rc=0
git diff --numstat _CHAIN.md   →   6 4
```

⚠ The chain grew **43,906 → 44,189 real, +283 tokens on three store rows** (~94 each). Not a
threshold, not a ruling — a figure the conductor may want in the gauge line, and a rate worth knowing
before the next multi-row wave.

---

## 6 · THE NARROW-WIDTH DRIVE — lane C's UNPROVEN ④ CLOSES GREEN, and found something else

Lane C: *"The chord specimens were driven at 1280×900 only. The library page's cards are a responsive
grid (`auto-fit, minmax(320px,1fr)`); the canon container-query bands were not re-driven at narrow
widths on this page."*

**NON-REPO (`/var/tmp/s219seam6/`)** — `s191-D2` marker. Three scratch probes, driven with the render
runbook's staging (`PLAYWRIGHT_BROWSERS_PATH=/var/tmp/pw-browsers-215`,
`PYTHONPATH=/var/tmp/pylibs-s219l1`,
`LD_LIBRARY_PATH=/var/tmp/chromelibs-s213e2/root/usr/lib/aarch64-linux-gnu` (`ldd` → **0** "not
found"), `FONTCONFIG_FILE=/var/tmp/fonts-219eb.conf` (`fc-match` → `HSBC_MtUnivers_Latin-Rg.ttf`),
`TMPDIR=/var/tmp`). Font probe is a **control** probe on every run and reported the same triple lane C
did: **target 347 · real control 375 · absent control 301 — DISCRIMINATES**.

### ① 480px, four states — NO page-level overflow

```
W=480 mono/light        cards=7  gridCols=[1,1]  docScrollW=480 vs clientW=480  hOverflow=no  bodyBg=rgb(255,255,255)
W=480 mono/dark         cards=7  gridCols=[1,1]  docScrollW=480 vs clientW=480  hOverflow=no  bodyBg=rgb(26,26,26)
W=480 console/light     cards=7  gridCols=[1,1]  docScrollW=480 vs clientW=480  hOverflow=no  bodyBg=rgb(255,255,255)
W=480 console/dark      cards=7  gridCols=[1,1]  docScrollW=480 vs clientW=480  hOverflow=no  bodyBg=rgb(26,26,26)
```

The `auto-fit` grid collapses to **one column** in both `.br-cards` grids; all seven cards measure
exactly **448px** (480 − the page's own padding); nothing rides past the viewport.

### ② The 44 elements past the document edge are the TABLES, INSIDE THEIR OWN SCROLLER

A naive read of "44 elements exceed `documentElement.clientWidth`" is an alarm. It was **positively
disproved** rather than waved away — an element past the edge is only benign if it is inside a
scroller, and that is a statement to be asserted, not assumed:

```
past-edge elements: 44 · ALL inside a .br-scroll: True · outside: none
scrollers: [{"ox":"auto","cw":448,"sw":448,"scrollable":false},
            {"ox":"auto","cw":448,"sw":569,"scrollable":true},
            {"ox":"auto","cw":448,"sw":569,"scrollable":true},
            {"ox":"auto","cw":448,"sw":598,"scrollable":true}]
non-scroller overflow: none        (no .br-card and no .br-spec overflows its own box)
```

Every one is a `TABLE`/`THEAD`/`TR`/`TH`/`TD` under a `.br-scroll` with `overflow-x:auto` and
`scrollWidth > clientWidth` — the option-space tables scrolling **inside** their container, which is
lane C's defect-2 fix working under pressure. The document itself never scrolls sideways.

### ③ The specimens survive the squeeze

```
.br-spec  414 × 274   image box 380 × 154   (all seven, every state)
console capsule tile radius 20px · rounded-chord tile radius 0px    ← lane C's defect-1 fix HOLDS
```

Nothing collapsed, nothing zero-height, no photograph lost.

### ④ THE VERDICT: 480px and 1280px are IDENTICAL, cell for cell

Driven at **both** widths precisely so the answer could distinguish "narrow width is fine" from
"narrow width is broken in a way 1280 hides":

```
                       page=mono                    page=console
spec  pinned      1280      480             1280      480
 0    console     20px      20px            20px      20px
 1    console     20px      20px            20px      20px
 2    console     20px      20px            20px      20px
 3    console      0px       0px             0px       0px
 4    mono         0px       0px            20px      20px
 5    mono         0px       0px            20px      20px
 6    mono         0px       0px            20px      20px
```

**Every 480 cell equals its 1280 cell. UNPROVEN ④ closes GREEN — the page is width-stable.**
Second proof, the repo's own instrument on the same bytes:
`verify_foundations_217.py --page bento-rails` → `OK — 8 state(s), no dangling property, theme
reached the paint in all four` (mono 17.4 / legacy 12.63 · 17.4 / console 17.4 / supercharge 17.45 ·
16.11).

### ⑤ ⛔ BUT THE TABLE ABOVE CARRIES A SECOND FINDING — `data-apollo-theme="mono"` IS A NO-OP

Read the last three rows across, not down. Specimens 4–6 are the **page-background** cards and the
markup pins them `data-apollo-theme="mono"`. They render **0px on a mono page and 20px on a console
page** — the pin does nothing. The cause is one grep:

```
grep -c 'data-apollo-theme="mono"' knowledge/canon/canon.css      →   0
--border-radius-container: 20px  is declared under  [data-apollo-theme="console"]
--border-radius-container: var(--border-radius-default)  is declared at  :root
```

**Mono IS `:root`.** It has no selector of its own anywhere in canon, so pinning a nested element to
mono cannot reset an ancestor theme's overrides — there is no rule to win the cascade with. The
console pins work (nothing overrides console away), which is why lane C's `_spec_vars()` fix measured
green: **it was only ever driven in the direction that can succeed.** Every var still resolves to
something, so nothing can go red — the shape of
[[dangling-dataviz-var-renders-silent-black]] with a plausible value instead of a black one.

**It is NOT width-related and NOT a regression of lane C's fix** — the 1280 column above proves both.

**On a supercharge page the card and its specimen contradict each other in print:**

```
page-theme=supercharge, specs 4–6, pinned=mono
  --background-default  #F7F6F4     --surface-subtle  #DFDEDC     painted  rgb(247,246,244) / rgb(223,222,220)
the card's own printed labels say:   --background-default light #FFFFFF   dark #1A1A1A
```

The page-ground section is the one place on this page whose job is to show what a **named page rail
member** looks like; on supercharge it shows the supercharge ramp under a mono label. **Surfaced,
never swapped** — the warm supercharge ramp is a standing grey-tint item for Dave's eye, and choosing
a repair here would be choosing between "pin harder" and "let the specimen follow the page", which is
a taste call. See §9.

---

## 7 · THE FULL VERIFY SWEEP — 24 verdicts, all quoted

### Lane C's four generator selftests, re-driven AFTER the serial

```
gen_bento_matrix_217 --selftest   OK — 94 bites.        (lane C: 94 — MATCHES)
   reachable console      {'display': 48, 'gallery': 864, 'dashboard': 288} (total 1200)
   reachable supercharge  {'display': 48, 'gallery': 648, 'dashboard': 288} (total 984)
gen_foundations_217  --selftest   OK — 39 bites.        (lane C: 39 — MATCHES)
gen_grids_218        --selftest   OK — 16 bites.        (lane C: 16 — MATCHES)
gen_library_214      --selftest   OK — 38 bites.        (lane C: 38 — MATCHES)
   residual · missing thumbnail: 0 entry(s)             (lane C: 1 — CLOSED)
   residual · foundation page not on disk: none
```

### The two `--check` arms the brief named

```
gen_foundations_217 --check OK — 8 page(s) in sync.                                        rc=0
gen_library_214     --check OK — 143 component(s), index + index.json + stub in sync.      rc=0
```

### The four CI-drift checks — GREEN, and one of them is not where the brief says it is

```
[40]   gen_token_ramp: 0 file(s) DRIFTED (0 with block, 0 block-removed), 147 already in sync.       rc=0
[45]   gen_canon_components --check OK — 135 components in sync.                                     rc=0
[50]   gen_theme_cascade --check OK — 230 override path(s), 387 component projection(s) in sync.      rc=0
[107→111] memento index --check: current (1791 records)                                              rc=0
```

⚠ **`[107]` HAS MOVED.** `len(STEPS)` is now **132**, and the memento-index determinism check sits at
**`STEPS[111]`**; `STEPS[107]` is now `_build_consult_index.py`. Seam 3 of this same session inserted
four STEPS lines, which shifted every index above them. A bracketed survey number is an **AGE, not an
ID** ([[banner-brackets-are-ages]]) — so all four were driven **by module + argument**, the identity
seam 1 quoted verbatim, and the index was re-derived from `_build_all.STEPS` rather than trusted.
**Driving `[107]` by its number alone would have driven the wrong check and reported it green.** The
number was still honoured — `STEPS[107]` was driven too, and that is §4.

### The store and the doc-row gate — driven BEFORE and AFTER the `W-186` mint

```
BEFORE the mint
_state.py --selftest        _state selftest: 57 bites, all GREEN                                     rc=0
_state.py --check           by state: {'open': 172, … 'done': 60, … 'parked': 19}
                            live by owner: {'dave': 94, 'claude': 78}                                rc=0
_gate_doc_rows.py           doc-row gate: population 84 (added >= 2026-08-15, PICKED) ·
                            of which staged-in-THIS-commit 0 · unrowed 0
                            ✅ PASS — every in-scope document has a store row.                       rc=0

AFTER the mint (252 items; W-99zr, W-99zs, W-186 all read back BY ID from the file)
_state.py --selftest        _state selftest: 57 bites, all GREEN                                     rc=0
_gate_doc_rows.py           … unrowed 0 · ✅ PASS                                                    rc=0
W-99zr  open · dave   · notes/_subreports/2026-08-25-219-enactC-chords.md      INTACT
W-99zs  open · dave   · showroom/_foundations/bento-rails.html                 INTACT
W-186   open · claude · notes/_subreports/2026-08-25-219-seam6-reconcile.md    NEW
```

⚠ **The doc-row gate CANNOT yet see this report** — its population is *git-tracked* files and reads
`staged-in-THIS-commit 0` (the `#207` single-commit blindspot, printed in its own banner). Its PASS is
therefore not evidence that `W-186` was needed; the row was minted because `s218-D7` + the
forgotten-document class ask for it at creation, and the gate will only start enforcing it after the
commit ([[instrument-without-a-consumer]] — a gate that does not run cannot fail).

The `--check` warnings (14 unconditioned, 0 deadline/effort coverage, project split, 17 `path:line`
homes) are **standing declared debt, unchanged by this seam** — each is Dave's word to give, none is
new.

### The page itself, live

```
verify_foundations_217.py --page bento-rails
  font probe (controls pass): {'target': 347, 'alias_uf': 347, 'alias_font': 347,
                               'control_real': 375, 'control_absent': 301}
  foreign properties probed per state (12) … no empty resolution
  OK — 8 state(s), no dangling property, theme reached the paint in all four.                        rc=0
```

### Final tree

```
git status --porcelain | wc -l   →   22        (18 modified + 4 untracked)
_gen_chain.py --check            →   ✅ FRESH                                                        rc=0
```

---

## 8 · FINDINGS

1. **`knowledge/_consult-index.json` has been stale at HEAD since seam 3** — three commits. §4. The
   cause is a *vocabulary* one: "the index" in "ramp first, index last" has been read as the memento
   index, and a second index at a different STEPS position has been sailing past every seam.
2. **`[107]` is an age, not an ID.** The four CI-drift checks moved when seam 3 inserted STEPS lines;
   `[107]` now names a different script. Driving by number would have reported the wrong green. §7.
3. **`data-apollo-theme="mono"` cannot pin anything** — `[data-apollo-theme="mono"]` occurs **0**
   times in `canon.css`; mono is `:root`. Three specimens on the new page therefore render the page's
   theme, and on supercharge the card's printed label and its own specimen disagree. §6⑤.
4. **Lane C's UNPROVEN ④ closes GREEN** — 480px is identical to 1280px in every measured cell, the
   cards collapse to one column at 448px, and the only elements past the document edge are the option
   tables inside their own `overflow-x:auto` scroller. §6.
5. **The thumbnail was targetable all along.** `gen_thumbs.py --range` exists and is documented in the
   generator's own docstring; seam 1's decision to skip it was right for *its* tree (nothing had
   moved) and wrong to generalise into "thumbs always churn". One page shot, 142 untouched. §3.
6. **`_gen_chain.py`'s staleness message names the wrong files.** It says *"does not match
   GOOD-MORNING.md / _LIVE-STATE.md"* when the actual source of the diff was `_state.json`, which the
   generator reads directly at line 326. A reader trusting the message would have opened two clean
   files looking for a change that was never in them. §5.

---

## 9 · RULING-SHAPED — NOT TAKEN

⛔ Lane C's **five** ruling-shaped questions are **UNTOUCHED and still open**, exactly as briefed. The
two below are this seam's own and are **additional**, not substitutes.

1. ⬛ **CAN THE DEFAULT THEME BE PINNED AT ALL?** Finding 3. Every specimen surface in the repo that
   pins a nested `data-apollo-theme` is asserting something canon cannot deliver for mono. The
   candidate repairs are a real choice, not a bug fix: (a) mint a real `[data-apollo-theme="mono"]`
   block in canon that restates the `:root` defaults — a token-tier change, derivation governance;
   (b) let page-rail specimens follow the page theme and drop the pin, relabelling the cards; (c)
   leave it and note it on the page. **Not taken.** A gate would also be cheap here (a pinned theme
   whose selector does not exist in canon is a silent no-op, and no instrument looks for it) — priced
   for `_DS-IMPROVEMENTS.md`, not written.
2. **Should `_build_consult_index.py` join the seam's regen serial?** Finding 1. It is a generated
   index with a real consumer that no seam has been driving. Adding it to the seam ritual is a
   RUNBOOK edit, which is corpus, which is the memento index's input — an ordering the conductor
   should rule rather than a sub. **Not taken; the file IS regenerated (§4), the ritual is not.**

---

## 10 · UNPROVEN — DECLARED

1. **The three other matrix mutation arms were still not driven.** `--break-layout`, `--break-inner`,
   `--break-keylines` — lane A's UNPROVEN ② and lane C's UNPROVEN ③, carried forward unchanged. This
   seam drove no mutant. `--break-legality` was lane C's and is not re-driven here: nothing in the
   serial touched `gen_bento_matrix_217.py` (it is `0 written` throughout), but that is an
   **inference**, and a mutation test proves the clause, not the feature
   ([[mutation-tests-the-clause-not-the-feature]]).
2. **`verify_bento_matrix_217 --themes` and `verify_photography_218` were not re-driven.** Both are on
   the conductor's REPLAY-THESE. The argument for not re-driving is that `gen_foundations_217`
   reported `0 written`, so the bytes they measured are the bytes on disk — again an inference from a
   generator banner, and the honest label is UNPROVEN rather than "unnecessary".
   `verify_foundations_217 --page bento-rails` **was** driven (§7) and is green on the real file.
3. **The narrow-width drive lives in `/var/tmp/s219seam6/`, not in the repo** (`s191-D2` NON-REPO
   marker). Its readings are reproducible only while this sandbox lives; the probe source is short
   enough that the quoted verdicts in §6 are the durable record. **A permanent narrow-width arm on
   `verify_foundations_217.py` is OWED and PRICED** — the script hard-codes
   `viewport={"width": 1180, "height": 900}` at line 162 and has no `--width` argument, so every
   Foundations page in the repo is verified at exactly one width. Not built: adding an arm is
   lane-shaped work and this seat's mandate is the tree.

---

## 11 · REPLAY-THESE (conductor)

- **The tree is prepared and NOT committed.** 22 lines, 0 unclaimed. `git status --porcelain | wc -l`
  → **22** is the number to re-read before staging (18 M + 4 ??).
- ⛔ **`W-99zs`'s close condition is HALF met** — the index clause is satisfied and quoted; **Dave's
  eye on the page is not**. The row is correctly still `open`; do not close it on the index alone.
- ⛔ **A store row was minted by this seat:** `W-186`, homed at this report, owner `claude`. If the
  conductor's ritual also mints one for the seam report, **do not double-mint**.
- `_CHAIN.md` was regenerated and must be staged **with** `_state.json` — a chain staged without its
  store rows, or store rows staged without the chain, re-creates the staleness at HEAD.
- ⚠ **Re-run `_gen_chain.py` once more AFTER any further store write**, including the conductor's own
  row for this report. The chain renders `_state.json`; every new row stales it.
- **Put lane C's five ruling-shaped questions to Dave, untouched.** Question 1 (mono's access to the
  dark-caption chord) is the one `s219-D3(3)` expressly left him.
- **Dave's eye owed** (lane C's ask, unchanged): `showroom/_foundations/bento-rails.html` — the three
  capsule grounds side by side, and the dark-mode grey/white collapse. Shots at
  `_to_delete/_shots219c/`; narrow-width shots at `/var/tmp/s219seam6/rails-480-*.png`.
- **New for his eye:** on a **supercharge** page the page-ground cards print mono's hexes over
  supercharge's ramp (§6⑤). One look answers §9 question 1.
- `knowledge/_consult-index.json` is **in** the tree, regenerated and idempotent. Its
  advisory/blocking mismatch is the generator's own derivation and was left alone.
