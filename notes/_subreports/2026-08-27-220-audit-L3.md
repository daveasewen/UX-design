# #220 audit wave — L3 TOKEN & CANON HEALTH (filed report)

**Lane:** L3 of the six-lane #220 audit wave · **Model:** Opus · **Date:** 2026-08-27
**Brief:** `notes/_briefs/2026-08-27-220-audit-wave-divvy.md` §L3 + SHARED RULES
**Region honoured:** this report only. **Nothing repaired, nothing ruled, no promotion, no git, no memory.**
Repo writes by this lane: this file. (See F-12 — one repo file *did* move during the lane; it was **not** this lane's write, and the cause is named.)

---

## COUNTS

```
INSTRUMENTS DRIVEN                       9 of 9 named + 5 adjacent = 14 · 0 could-not-run · 0 crashes
  green (exit 0, no findings)            9   forks(baseline) · dataviz-vars · P-7 · P-8 · property-resolves ·
                                             binds-resolve · no-hardcode · palette-tier · dark-surfaces
  green-with-standing-advisories         4   minted-consumption · hidden-display · token-tiers · theme-provenance
  red under its OWN stricter flag        1   token-forks --strict (exit 1, 98 forks) vs baseline exit 0

CLASS SWEEPS (mine, /var/tmp scratch, 650 files, 9,508 distinct var refs)
  A · SILENT BLACK (var resolves in NO theme x mode)
      live populations (snippets/showroom/foundations/proforma/canon)   0   ✅ CLEAN
      frozen reviews/*.html                                           118   (24 files; 0 are defects — see F-11)
      partial (resolves in some theme x mode, not all)                  1
  B · HEX BYPASS (raw #hex on a paint property, not a mint, not a comment)
      canon.css + type.css                                             28   (0.23% of 12,009 mints)
      showroom/*.html                                                 810   incl. #305A85 x270
      showroom/_foundations/*.html                                    450
      knowledge/snippets/*.reference.html                              25
      knowledge/_proforma/*.html                                        1
      GATED BY AN EXISTING INSTRUMENT                                   0   of 1,314  (see F-6)
  C · STALE FALLBACK (`var(--x,#lit)` where canon answers --x differently)
      GENERATOR CAUSE SITES (canon tokens)                             11   in 4 generator sources
      page-local-alias consumer drift (bite-12 second-arm class)         5   in 4 generator sources
      shipped-artefact occurrences                                    118   distinct (file,var,fallback) in 44 files
      of which on TODAY'S live decision surfaces (2026-08-2x reviews)   76
  D · PER-THEME OVERRIDE INTEGRITY AFTER THE #220 MINTS
      foundations pages x watched caption/surface vars   8 pages x 28 vars, 0 holes   ✅ CLEAN
      s220-D1 mint resolves per theme x mode              8/8, values match the ruled table exactly

WORST FINDS                              12 filed (F-1 .. F-12) · 4 rated HIGH
RED FLAGS FOR THE CONDUCTOR              3 (F-9 stale probe README · F-10 fork-ledger rot · F-12 retrieval writes)
```

---

## PART 1 — INSTRUMENT VERDICTS (driven, not read)

| # | Instrument | Invocation | Exit | Verdict / measurement |
|---|---|---|---|---|
| 1 | `knowledge/_gate_minted_consumption.py` | bare, and `--orphans` | 0 | ADVISORY. canon.css declares **973** custom properties (base 972 · theme 394). consumed **601** · unconsumed **372** = ALIAS-TARGET 83 · ALIAS-SHADOW 24 · **ORPHAN 265** (6 of them minted per-theme). |
| 2 | `knowledge/_validate_hidden_display.py` | bare | 0 | ADVISORY. **135 snippets scanned, 17 with signals** — 3 STATIC · 0 DYNAMIC · 3 STATIC? · 6 DYNAMIC? · 13 UNRESOLVED (25 signal lines). |
| 3 | `knowledge/_validate_token_forks.py` | bare (baseline) | **0** | `GATE GREEN: no undeclared fork`. Parsed 12,003 declarations / 15,293 contexts / 974 names. FORK **45** · UNIFORM 530 · BENIGN_MODE 53 · BENIGN_THEME 32 · BENIGN_THEME_AND_MODE 314. Ledger `knowledge/_TOKEN-FORK-LEDGER.json` = **50** declared. |
| 3b | same | `--strict` | **1** | `GATE RED: 98 fork(s)`. Same tree, same run, opposite verdict — the two modes are two different questions and only the permissive one is wired. |
| 4 | `knowledge/_validate_token_tiers.py` | run on a `/var/tmp` shadow of `knowledge/tokens` (the live script writes `_TOKEN-TIER-AUDIT.md` into the repo; shadowing kept the region clean) | 0 | **0 strict failures, 1 advisory.** The advisory is quoted at F-4. Diff of the shadow's generated audit vs the committed `knowledge/_TOKEN-TIER-AUDIT.md`: **IDENTICAL** — the committed artefact is not stale. |
| 5 | `knowledge/_gate_dataviz_vars.py` | bare | 0 | `✅ every colour presentation attribute resolves in at least one theme` — 18 files, **767** references, 4 themes. |
| 6 | `knowledge/_probe_registry/probe_container_self_query.py` (P-7) | bare | 0 | **`PROBE P-7 — findings=0`**, 135 files, 0 WARN. |
| 7 | `knowledge/_probe_registry/probe_dangling_var_text.py` (P-8) | bare | 0 | **`PROBE P-8 — findings=0`**, 135 files, `by kind {}`. |
| 8 | `knowledge/_validate_property_resolves.py` (C2) | bare | 0 | `property-resolves gate (C2): 147 file(s), 0 failure(s)`. |
| 9 | `knowledge/_validate_binds_resolve.py` | bare | 0 | `135 snippets (135 with manifests, 2164 vars) · 135 metas (116 binds addresses) · 135/135 canon blocks · 0 failure(s)`. |
| + | `knowledge/_validate_no_hardcode.py` | bare | 0 | `✅ No-hardcode gate passed (11 tranche file(s))` — **glob = 11 proforma files only** (F-6). |
| + | `knowledge/_validate_palette_tier.py` | bare | 0 | 4 themes, 3 palettes, 42 declared keys, 135 metas, 135 manifests. `OK`. |
| + | `knowledge/_validate_dark_surfaces.py` | bare | 0 | `0 flat-white failure(s), 9 annotated exception(s)`. |
| + | `knowledge/_validate_theme_provenance.py` | bare | 0 | ADVISORY: **48 hardcoded foreign-theme hexes in 164 Mono files**. |

**COULD-NOT-RUN: none.** No crash, no refusal, no exit 77 in this lane.
**Deliberately not run** (would have written repo files or planted defects): every `--selftest` arm, and `_validate_token_tiers.py` in place. `pip install tiktoken --break-system-packages` ran first and reported `Requirement already satisfied` / import OK; no gauge-touching run was required by §L3.

**Method note, declared, not buried.** Sweeps A/C/D use a scratch resolver at `/var/tmp/L3/tm.py` that models the cascade as `base < theme < mode < (theme,mode)`. **Selector specificity and scope are NOT modelled** — the same blind spot `_gate_dataviz_vars.py` publishes about itself. Every finding below that names a token VALUE was re-measured a second time through the repo's own `gen_bento_matrix_217.resolve_token()`, so no value in this report rests on my parser alone. My parser produced **two false positives on its first pass** (`--undefined` read out of a CSS comment; `--sc` read past an inline `style=""` declaration); both were fixed and re-run before any count here was taken.

---

## PART 2 — FINDINGS

### F-1 · HIGH · The s220-D1 fallback-drift arm exists in ONE generator; four siblings minting the identical preamble were never swept

**Class:** [[gate-glob-scope-rule]] + [[gate-dont-patch]] — the brief's own live lesson #3, repeated inside the repair that lesson produced.

s220-D1 built a genuinely new assertion into `knowledge/_render/gen_bento_matrix_217.py` bite 12:

> `# (b) var() FALLBACK literals — minted from canon. A chromatic one is legal ONLY if canon`
> `#     itself resolves that exact token to that exact value in some theme x mode. That is a`
> `#     NEW assertion the old gate never made: a fallback that has DRIFTED from canon is now a`
> `#     red, in either direction, chromatic or not.`

It reads **only that generator's own `rules` string**. The page-chrome preamble it checks is copied, near-verbatim, into four sibling generators — and there it is unchecked. Measured against the repo's own resolver:

| Fallback as written | Canon actually answers (all 8 theme x mode) | Cause sites |
|---|---|---|
| `--focus: var(--focus-ring,#1A1A1A);` | `#305A85` light · `#4587A7` dark, **in all four themes** | `gen_bento_canon_217.py:254` · `gen_bento_roles_217.py:318` · **`gen_foundations_217.py:917`** · `gen_gallery_compare_217.py:255` |
| `--line-2: var(--border-strong,#767676);` | `#808080` (mono/legacy/console) · `#524842` (supercharge) | `gen_bento_canon_217.py:251` · `gen_bento_roles_217.py:315` · `gen_gallery_compare_217.py:252` |
| `--surface-2: var(--tertiary-background-hover,#F3F3F3);` | `#F0F0F0` light · `#232323` dark · `#DFDEDC`/`#2E2A25` supercharge | `gen_bento_canon_217.py:249` · `gen_bento_roles_217.py:313` · `gen_gallery_compare_217.py:250` |
| `--surface-2: var(--surface-subtle,#F3F3F3);` | `#F0F0F0` · `#1F1F1F` · `#DFDEDC` · `#2A2621` | `gen_foundations_217.py:910` |

**11 canon-token cause sites**, plus **5 page-local-alias consumer drifts** of the same shape (`gen_bento_canon_217.py:360`, `gen_bento_roles_217.py:359`, `gen_gallery_compare_217.py:297`, `_bento_recut_219.py:448`, `_bento_recut_219.py:456` — all `var(--line-2,#767676)`), which is exactly bite 12's *second* arm (`local_drift`) applied outside its file.

**The sharpest instance is inside the repaired file itself.** `gen_foundations_217.py:912` carries the s220-D1 repair with its receipt —

> `--line-2:    var(--border-strong,#808080);   /* s220-D1: was #767676 — a value canon resolves in`
> `                                                no theme and no mode. See gen_bento_matrix_217`
> `                                                bite 12's fallback-drift arm. */`

— and **five lines below it**, line 917 still reads `--focus: var(--focus-ring,#1A1A1A);`, drifted in 8 of 8 theme x mode. The instance was fixed with its receipt written; the block it sat in was not swept.

**Why it is not currently visible:** every shipped page `<link>`s `../knowledge/canon/canon.css`, so the fallback never fires. It is a fence, and the generator says so out loud (`gen_foundations_217.py:904` — *"the fallbacks are the fence"*). But a fence built out of values canon never produces fails **safe-looking**: the day canon does not load (a pack export, a moved file, an offline copy, a designer opening a page outside the repo) the whole chrome paints `#767676` borders and a **black focus ring where the ruled ring is blue** — and there is no visible signature.

**PRICE.** Repair at cause: 16 one-line literal edits across 5 generator files + regenerate their artefacts — **~4–6K tokens, 30–40 min, low risk** (byte-diff confirms scope). The **class fix** is the one that matters: lift bite 12's drift arm into a shared helper (or a standalone advisory gate) with a glob over `knowledge/_render/*.py` + the emitted pages — **~10–14K tokens**, one new instrument, mutation test both directions. ⚠ The alias and its consumers must move in ONE motion: repairing `--line-2` to `#808080` while leaving `.badge{... var(--line-2,#767676)}` opens the second-arm class it closes.
⛔ **Not a builder's pick:** whether the drift arm becomes blocking is Dave's (derivation governance).

---

### F-2 · HIGH · The fallback-drift arm is structurally blind to every non-colour token

**Class:** [[gate-narrows-its-own-rule]] — a silent skip that reads as coverage.

Bite 12's filter is `drifted = sorted([(t, h) for t, h in _fb if _canon_answers(t) and h not in _canon_answers(t)])`. `_canon_answers` swallows `KeyError` from `resolve_token`, so a token the store cannot resolve returns an **empty set** and the row is skipped in silence. Driven against the repo's own resolver:

```
--border-radius-surface     KeyError in all 8 theme x mode
--border-radius-container   KeyError in all 8
--border-radius-control     KeyError in all 8
--target-min                KeyError in all 8
--focus-ring-width          KeyError in all 8
```

Every radius, hit-area and ring-width fallback in the bento/foundations chrome (`var(--border-radius-container,0px)`, `var(--target-min,44px)`, `var(--focus-ring-width,2px)`, ~40 sites) is therefore **never compared to anything**. `0px` happens to be the house default, which is precisely the shape #204 measured as invisible (a self-referential radius killed console's 20px surface radius across 8 panes with every gate green).

**PRICE.** Make the skip LOUD: `_canon_answers` returns `None` for unresolvable and bite 12 reports an `UNCHECKED` list beside `drifted` — **~1.5K tokens**, no behaviour change, converts a silent hole into a declared one. Extending real coverage to the radius/spacing stores is a second, larger job (**~8K**) and should be priced separately.

---

### F-3 · HIGH · `_validate_token_forks.py` answers two opposite verdicts on one tree, and only the permissive one is wired

**Class:** the s218-D7 REFUSES-vs-WARNS shape, in a single instrument.

Same tree, same minute, real exit codes captured without a pipe:

```
python3 knowledge/_validate_token_forks.py            -> exit 0   "GATE GREEN: no undeclared fork"
python3 knowledge/_validate_token_forks.py --strict   -> exit 1   "GATE RED: 98 fork(s)"
```

Baseline mode asks *"is any fork **undeclared**?"*; strict asks *"is any fork **present**?"*. The ledger's own header says the answer is not settled — `"$status": "UNRULED-BASELINE. Every entry awaits Dave: real fork (fix) or sanctioned divergence (declare)."` A reader who runs the gate the way CI runs it sees GREEN over 98 standing forks, including the two-red-law-adjacent one the strict run prints first:

```
FORK  --warn  theme=mono mode=any
   knowledge/canon/canon.css:906   :root            --warn: var(--rag-warning)  ->  #e0a61f
   knowledge/canon/canon.css:1843  .cn-notifications --warn: #FFBB33            ->  #ffbb33
```

**PRICE.** Zero-token fix available today: make the green line *state its own question* — `GATE GREEN: no undeclared fork (98 declared forks stand; --strict for the full list)` — **~0.5K tokens**. Whether strict becomes the wired mode is **Dave's**, and it is the same decision the ledger has been waiting on since 2026-08-09.

---

### F-4 · MED · A drifted `$value` cache on a RED token, standing as an advisory

`_validate_token_tiers.py`, verbatim:

```
- primary/border/hover (dark): $value #D61412 != resolve(color/red/700) #BA1110
```

The cached value and the alias chain disagree by 21/4/2 in R/G/B, in **dark mode, on a red**. The gate classes it ADVISORY because the token is outside the 47-token MIGRATED set, so it cannot block. Two-red law (`s151-D1`) governs `#DA1A00` / `#F6604C` and does not name `primary/border/hover`, so this is a *cache* defect and not a law breach — but it is a red that resolves two ways, and Dave cannot judge reds by eye ([[colour-stability-red-yellow-problem]]).

**PRICE.** Re-resolving the cache is a one-value store edit (**~1K tokens**) but is a **derivation-governance promotion** — token values are Dave's. The auditable act available to a builder is to add `primary/border/hover` to `MIGRATED` so the drift becomes STRICT and cannot re-open (**~1K**), which is also Dave's word.

---

### F-5 · MED · 265 orphan mints, and the six that were minted PER THEME

Six tokens carry theme-tier declarations — a designer wrote them once per theme — and **nothing in the glob reads any of them**:

```
--color-neutral-2                        base:58    · supercharge:23693
--color-neutral-3                        base:59    · supercharge:23694
--color-neutral-raise-3                  base:74    · supercharge:23705
--padding-card-internal                             console:22180        (ruled s201-D4)
--size-segmented-control-min-hit-area    base:565   · console:22204
--tabs-inactive                          base:379   · legacy:20081 · supercharge:23788
```

`--tabs-inactive` is the worst of the six: **three homes, zero readers**. `--padding-card-internal` is the instance the gate's own docstring names as ruled-but-unwired, and my independent probe confirms it: `grep -rl "var(--padding-card-internal" knowledge/snippets/ showroom/ knowledge/_proforma/` returns **nothing**. `--size-segmented-control-min-hit-area` is the same shape beside the s202-D1 segmented radii that were dark for 17 sessions.

**PRICE.** The gate is right that presence cannot separate *forgotten wire* from *reserved slot*, and it is right to stay advisory. What is cheap and useful is a **triage of the six theme-tier ones only** (not the 265): confirm against each token's ruling whether a consumer was named — **~5K tokens**. The `$consumer`/`$reserved` schema the gate asks for is Dave's.

---

### F-6 · MED · 1,314 hex bypasses, and the only gate for the class covers 11 files

`_validate_no_hardcode.py` reports `✅ No-hardcode gate passed (11 tranche file(s))`. Its population is `knowledge/_proforma/*-interactive.html`. My sweep B counted raw `#hex` on a **paint property** (colour/background/border-color/fill/stroke/outline/shadow), excluding custom-property mints and comments:

| Population | bypass sites | worst hexes |
|---|---|---|
| `showroom/*.html` | **810** | `#FFFFFF` x540 · **`#305A85` x270** |
| `showroom/_foundations/*.html` | **450** | `#1A1A1A` x114 · `#FFFFFF` x103 · `#D7D8D6` x84 · `#545454` x78 · `#F0F0F0` x36 · `#808080` x31 · `#F3F3F3` x4 |
| `knowledge/canon/canon.css` + `type.css` | **28** | `#FFFFFF` x21 · **`#767676` x2** · `#2A2A30`/`#0E0E12` (gradient) · `type.css:124 background:#1A1A1A; color:#fff` |
| `knowledge/snippets/*.reference.html` | 25 | `#FFFFFF` x21 · `#767676` x2 |
| `knowledge/_proforma/*.html` | 1 | `#999` |

The `#305A85` x270 has one cause, and it is instructive:

```
knowledge/gen_showroom.py:215   header>button:focus-visible{outline:2px solid #305A85; outline-offset:2px;}
knowledge/gen_showroom.py:221   .seg button:focus-visible{outline:2px solid #305A85; outline-offset:2px;}
```

135 showroom pages hardcode the **light-mode** focus ring. There is no `#4587A7` anywhere in `gen_showroom.py` (grep named, result quoted), so the showroom chrome's focus ring **cannot go dark and cannot be re-themed**. Set beside F-1 this is one token failing in two opposite directions at once: the showroom bakes the right value permanently, the foundations fall back to the wrong one.

`_validate_theme_provenance.py` is the nearest existing consumer (48 foreign-theme hexes / 164 Mono files, ADVISORY) but asks only about foreign-theme provenance, not token bypass.

**PRICE.** Widening `_validate_no_hardcode.py`'s glob to showroom would go red on 1,260 sites on day one and is **not** a builder's call. What is priceable: the two `gen_showroom.py` lines → `var(--focus-ring,#305A85)` — **~1K tokens, 10 min**, and it makes the ruled ring reach 135 pages including dark mode. The wider class is a **PROPOSAL to Dave**, not a repair.

---

### F-7 · MED · The `#767676` fallback is not only a fallback — canon paints it directly, twice

Beside the 11 stale-fallback cause sites of F-1, canon itself carries the literal on a paint property:

```
knowledge/canon/canon.css:12405   color: #767676
knowledge/canon/canon.css:12408   color: #767676
```

Canon still declares `--color-grey-600: #767676` (line 20) and uses it as a `--disabled` / `--pressed` value (lines 2379, 3415), so the hex is not retired — but `--border-strong`, the token the F-1 fallbacks name, resolves to `#808080`/`#524842` and to `#767676` in **no theme and no mode** (the s220-D1 wording, re-measured).

**PRICE.** Naming the two canon sites' intended token and binding them: **~1.5K tokens** — but canon is generated, so the edit belongs in `knowledge/canon/gen_canon_components.py`, and a token re-point is Dave's.

---

### F-8 · LOW · 12 phantom-in-the-a11y-tree signals stand; 3 are CERTAIN and all 3 are charts

`_validate_hidden_display.py` (135 snippets, 17 with signals). The three CERTAIN ones are one component copied three times:

```
[STATIC] Chart-butterfly-h.reference.html: STATIC hidden-bearing <div> (class=dv-empty-frame) at line 317
         is painted by `.dv-empty-frame{display:flex}` (line 220, specificity (0, 1, 0)); the file has NO
         matching `[hidden]{display:none}` remedy
[STATIC] Chart-butterfly-v.reference.html: ... line 320 ... painted by line 224 ...
[STATIC] Chart-histogram.reference.html:   ... line 271 ... painted by line 182 ...
```

Same shape as `s218-D5` clause 3 (Command-palette `#cp2-o1`), which was fixed **as an instance**; Dave put the repo-wide sweep on the gates backlog and this instrument is that sweep. The class has now been swept and it found three more, in the *empty-state frame* of three charts — an element whose whole job is to be absent when there is data. 13 UNRESOLVED signals are the instrument's declared blind spot and are counted, not treated as clean.

**PRICE.** One line per file (`.dv-empty-frame[hidden]{display:none}`) x 3 — **~1K tokens, 15 min, near-zero risk**. ⚠ These are `.reference.html` snippets, so the repair belongs at the snippet + its canon block, and the 6 `DYNAMIC?` / 3 `STATIC?` signals are combinator-blind and must be looked at, not batch-fixed.

---

### F-9 · RED FLAG · The probe registry's README and manifest assert findings that no longer exist

**Class:** [[conclusions-are-debt-s129-d5]] + [[premise-ages-faster-than-rule]] — in the very instrument built to catch stale premises.

`knowledge/_probe_registry/README.md`, still on disk:

> `- **P-7 and P-8 are NOT green on the current tree, and that is deliberate** (#210). They were`
> `  registered ADVISORY with their live findings standing and **nothing repaired** — P-7 6 findings`
> `  + 3 WARN, P-8 58 findings in 9 files.`

`manifest.jsonl`, P-7 note: *"⚠ THE PROBE IS NOT GREEN ON THE CURRENT TREE and that is the point — 6 findings stand"*. P-8 note: *"⚠ THE PROBE IS NOT GREEN ON THE CURRENT TREE: 58 findings stand, reported and NOT repaired."*

Driven today, on the default globs, no arguments:

```
P-7 container self-query scan: 135 file(s) · 0 finding(s) · 0 WARN-tier      PROBE P-7 — findings=0
P-8 dangling-var TEXT scan:    135 file(s) · 0 finding(s) in 0 file(s)       PROBE P-8 — findings=0
```

Both were repaired at #211 and the repair is documented **in the artefact** — `knowledge/snippets/Layout-utilities.reference.html:244` carries the P-7 fix with its verbatim before-state, and `knowledge/gen_token_ramp.py:28` carries the P-8 generator cause (*"HTML COMMENTS ARE NOT CSS, AND THIS GENERATOR NOW KNOWS THAT (#211, P-8's generator cause)"*). The registry that records the class was never told.

This matters more than a stale sentence: it is the exact defect **P-5 (stale-figure grep)** exists to find, sitting in P-5's own registry, outside P-5's population. Anyone briefing off this README carries a false premise about 64 standing findings, and the `--promote` reading of the ledger is computed from `caught`, which is unaffected — so the arithmetic is fine and only the prose lies.

**PRICE.** Correcting the two README sentences + the two manifest `note` fields with a dated *"green as of #220, 2026-08-27, repaired #211"* clause: **~2K tokens**. The class fix — extend P-5's glob (or add a probe) to `knowledge/_probe_registry/*` so a probe's own prose is inside somebody's population — **~4K tokens** and is Dave's to promote. ⛔ Not touched by this lane: the manifest is the promotion evidence and rewriting it is a governance act.

---

### F-10 · RED FLAG · The fork ledger has 5 rows the tree stopped agreeing with, and the wired gate cannot see them

Ledger header: `"$measured": "2026-08-09 s139, glob knowledge/canon/*.css"` — **18 days and ~81 sessions old**. Declared 50; measured today 45; undeclared 0. Diffed name-by-name against the gate's own `--json` output:

| Ledger row | Ledger status | Verdict today |
|---|---|---|
| `--scrim` | `SANCTIONED-#209 (Dave, live: 'Ledger the 5' ...)` | **UNIFORM** — no divergence at all |
| `--tail-x` | `SANCTIONED-#209 ...` | **UNIFORM** — no divergence at all |
| `--ring` | `SANCTIONED-#209 ...` | BENIGN_MODE_AXIS |
| `--rule` | `SANCTIONED-#209 ...` | BENIGN_THEME_AND_MODE_AXIS |
| `--success` | `UNRULED-BASELINE-s139` | BENIGN_THEME_AND_MODE_AXIS |

Baseline mode fails only on an **undeclared** fork, so a declared row that stops being true is invisible forever — and the ledger says `"Every entry awaits Dave"`, which means **four of Dave's five #209 'Ledger the 5' items and one s139 item are on his queue for divergence the tree no longer has**. Two of them are UNIFORM: there is nothing left to rule.

**PRICE.** A `--reconcile` arm printing `LEDGER-STALE: <name> declared FORK, measured <verdict>` (non-blocking, no writes): **~3K tokens**, and it is the arm that keeps `$measured` honest. Pruning the 5 rows is **Dave's** — they are his sanctions. Re-stamping `$measured` without re-running is exactly the carried-figure defect ([[banner-figures-are-parsed-not-prose]]).

---

### F-11 · LOW · Six review specimens have been rendering with no canon type since 2026-08-08

A `<link>` audit across `reviews/`, `showroom/`, `knowledge/snippets/`, `knowledge/_proforma/` and `notes/**` found **6 broken stylesheet links, all the same href**:

```
reviews/_alert-dark-specimen-2026-08-08-s134-v1.html : <link rel="stylesheet" href="../canon/type.css">
  ... -v2, -v3, and _alert-light-specimen-...-v1/-v2/-v3   (6 files)
```

From `reviews/` that path resolves to repo-root `canon/type.css`, which does not exist. The correct prefix is `../knowledge/canon/`, which **130 other review pages use** and these 6 do not. The specimens have been shown without canon's type composites for 19 days.

**Also declared, and NOT a defect:** sweep A's 118 dead `var()`s in 24 frozen review pages are **all** explained and none is filed. 31 of them are `reviews/RADIUS-CORNER-TUNER-2026-08-18-v1/v2/v3.html`, whose `--pv-*` / `--r-*` / `--sg-*` names are set at runtime by the page's own controller (`preview.style.setProperty('--pv-' + k, c[k])`, line 422) — the declared JS blind spot of every text-tier probe, not a hole. The rest are `--pa-*`/`--seg-*`/`--chip-*` in dated `.REVIEW.html` pairs, frozen history under ADR-0017. **Live populations: zero.**

**PRICE.** 6 one-token path edits — **~0.5K tokens, 5 min**. ⚠ These are dated `_alert-*-s134-*` artefacts; under ADR-0017 history is frozen, so **whether to touch them at all is a question, not a repair** (see Q4).

---

### F-12 · RED FLAG · Every lane's retrieval writes a repo file, and the brief forbids repo writes

While this lane ran, `knowledge/_graph-mark-observations.jsonl` went MODIFIED (+14 lines). **It was not this lane's write** — the appended rows carry `"query": "ordered regen serial ramp first index last"`, which is another lane's search. Traced to cause:

```
knowledge/_memento_search.py:126   notice = _graph_edges.record_observation("memento", query, r)
knowledge/_graph_edges.py:42       OBSERVATIONS_PATH = os.path.join(HERE, "_graph-mark-observations.jsonl")
```

The SHARED RULES say *"Retrieval: `python3 knowledge/_memento_search.py`"* and, one line earlier, *"Your only repo writes: the report."* Those two instructions cannot both be kept — the retrieval tool appends by design. With six lanes searching one working tree into one append-only file, the wrap faces an unreconciled diff whose authorship is not recoverable from the diff itself.

**PRICE.** Zero to fix in-lane (correctly: nothing to fix). For the conductor: **reconcile `knowledge/_graph-mark-observations.jsonl` deliberately at the wrap** — it is a legitimate observation log and should almost certainly be kept, but it must be a decision, not a blind `git add -A` ([[feedback-worktree-reconcile-trail]]). For the next divvy: add a `--no-observe` flag to `_memento_search.py`, or exempt the file by name in the SHARED RULES — **~1K tokens**.

---

## PART 3 — WHAT CAME BACK CLEAN, WITH ITS RECEIPT

These are measurements, not assumptions, and they are the load-bearing greens of the lane.

**① The SILENT BLACK class is CLEAN on every live population.** 650 files, **9,508** distinct (file, var) references, resolved across 4 themes x 2 modes: **zero** dead references in `knowledge/snippets/`, `showroom/`, `showroom/_foundations/`, `knowledge/_proforma/` or `knowledge/canon/`. The one PARTIAL is `--padding-card-internal` on a *reviews* page — the console-only mint of F-5, resolving in console and nowhere else, which is what the mint says it is.

**② The #220 caption mints resolve in every theme x mode, and match the ruled table exactly.** Driven on the shipped `showroom/_foundations/bento.html`:

```
mono/light  mono/dark  legacy/light  legacy/dark  console/light  console/dark   --bm-cap-dark-darkgrey -> #313131
supercharge/light  supercharge/dark                                             --bm-cap-dark-darkgrey -> #312C26
```

s220-D1 ruled `mono #313131, legacy #313131, console #313131 (all neutral/5), supercharge #312C26 (warm/5)`. The tree matches, per theme, per mode. All 8 foundations pages carry a **byte-identical** mint block (`md5 cab74b63c5f8af65c292129934589095` on all five that carry it) — one cause, one value, no per-page drift. Across those 8 pages, **28 watched caption/surface vars x 8 theme x mode = zero resolution holes.** The `[data-theme="dark"]` scoping holds: light mode is untouched, as ruled.

**③ Four dangling-var instruments agree.** `_gate_dataviz_vars` (767 refs, 18 files), C2 `_validate_property_resolves` (147 files), `_validate_binds_resolve` (2,164 vars, 135/135 canon blocks), and P-8 (135 files) are all green, and my independent theme x mode sweep — which uses a *different* cascade model and a wider glob than any of them — agrees. Four instruments and one outsider converging is stronger evidence than four greens from one family.

**④ The committed `_TOKEN-TIER-AUDIT.md` is not stale.** Generated fresh into a `/var/tmp` shadow and diffed against the repo copy: **IDENTICAL**.

**⚠ And the honest limit on all four.** Every one of these is a TEXT-tier measurement. None of them ran a browser, so none can see a per-selector scope miss, a value that resolves to the *wrong* colour, or anything a script builds at runtime — P-3's territory, and unproven here.

---

## RULING-SHAPED QUESTIONS (Dave's)

**Q1 — The fork gate asks two questions. Which one is the gate?**
Baseline says GREEN over 98 forks; `--strict` says RED. The ledger has said *"Every entry awaits Dave"* since 2026-08-09. Does `--strict` become the wired mode (and the 98 become a work queue), or does baseline stay and the ledger become the permanent record of sanctioned divergence? A third reading: strict becomes the wired mode **after** the 5 stale rows of F-10 come out. *Minting:* the CI wiring, the ledger's `$status`, and whether 98 rows are debt or documentation.

**Q2 — Are the fallback literals a fence, or are they dead weight?**
The generators say *"the fallbacks are the fence"* against silent black. But 16 of them name values canon resolves nowhere, so the fence catches you and drops you somewhere wrong. Three readings: **(a)** repair all 16 to canon's own answer and keep the fence; **(b)** keep the fence but make bite 12's drift arm shared across all five generators so it can never drift again (my recommendation, F-1); **(c)** decide the fence is obsolete now that four gates prove resolution, and strip the fallbacks so nothing can drift. *Minting:* a rule about what a fallback literal is FOR.

**Q3 — Should the showroom's focus ring be a token?**
135 pages hardcode `#305A85`, so the showroom chrome cannot go dark and cannot be re-themed. Binding it to `var(--focus-ring,#305A85)` is two lines and makes the ruled ring reach every page in both modes — but it changes the appearance of 135 pages in dark mode, which is your eye's call, not a builder's. *Minting:* whether showroom chrome is inside or outside the token system.

**Q4 — Do frozen artefacts get repaired?**
Six `_alert-*-s134-*` specimens have linked a nonexistent stylesheet for 19 days (F-11). ADR-0017 says history is frozen and dated, not stale. But these are *broken*, not merely old — they never showed what they claimed to show. Does "frozen" cover a defect that was always a defect? *Minting:* a clause on ADR-0017 about broken vs outdated history.

**Q5 — The six theme-tier orphans.**
`--tabs-inactive` was minted in three themes and is read by nothing; `--padding-card-internal` was ruled at s201-D4 and wired to nothing. The gate correctly refuses to guess *forgotten wire* vs *reserved slot*. Is the `$consumer`/`$reserved` declaration worth the store-schema change, or is a six-row triage enough for now? *Minting:* the token store's schema.

---

## REPLAY-THESE (conductor)

1. **⚠ RECONCILE `knowledge/_graph-mark-observations.jsonl` DELIBERATELY.** It is dirty from *another* lane's `_memento_search.py` (F-12), by design, not by accident. Keep it or drop it, but decide — and note that the SHARED RULES' "only repo write = your report" is unkeepable while retrieval writes. Same for the pre-existing `notes/_REHEARSAL-LOG.jsonl` and `notes/_dream/_GRADE-DECISIONS.jsonl`, which were already modified when this lane started.
2. **F-1 is the wave's headline and it belongs to L2 as well.** The `#767676`/`#1A1A1A`/`#F3F3F3` fallbacks are the `SPACING_STOPS` two-homes class in a *fifth* home. Cross-check my 16 cause sites against L2's two-homes sweep before either report is read as complete — if L2's sweep did not name these, its glob needs widening, and if it did, the two counts must be reconciled to one number.
3. **F-9 needs a decision before the next brief cites the probe registry.** Two live documents assert 64 standing findings that are gone. Anything briefed off `_probe_registry/README.md` today carries a false premise. ⛔ I did not touch it — the manifest is promotion evidence.
4. **Hand F-3 to L1.** A gate with two exit codes over one tree is a gate-correctness finding; I own the measurement, L1 owns the verdict on whether the wiring is right.
5. **Hand F-6's showroom counts to L6** only if page weight is in scope — 810 bypass sites in `showroom/*.html` are 810 opportunities the trim list may already be counting from the other side.
6. **Do NOT read Part 3's greens as a clean bill.** Every one is a TEXT-tier measurement; no browser ran in this lane. P-3 (pixel) and the render-verify leg are the unproven half, and `_validate_state_contrast` was not driven here.
7. **Nothing in this report was fixed, promoted, or ruled.** Every advisory gate is still advisory. Every price above is an estimate, not a measurement ([[planning-estimate-is-not-a-measurement]]).

---

## APPENDIX — reproduction

Scratch instruments (NOT in the repo, deliberately — [[non-repo-home-or-declare]]): `/var/tmp/L3/tm.py` (theme x mode resolver), `sweepA.py` (silent black), `sweepB.py` (hex bypass), `sweepC.py` (stale fallback), `sweepD.py` (mint integrity). Raw output: `/var/tmp/L3_{hidden,forks,dvvars,p7,p8,tiers,orphans,strict}.txt`, `/var/tmp/L3_forks.json`, `/var/tmp/L3/sweep{A,B,C}.json`. **These live in the sandbox and do not survive it** — the counts above are the durable record, and every one of them is re-derivable from the commands quoted in Part 1.

Second-opinion resolver used for every value claim:
```
cd knowledge/_render && python3 -c "import gen_bento_matrix_217 as G; print(G.resolve_token('--focus-ring','mono','light'))"
```
