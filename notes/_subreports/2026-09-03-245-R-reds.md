# `#245`-`R` — the CI #482 gates-job reds, reproduced at a bare clone, one fixed, two named

session: `#245` · 2026-09-03
window: `lane R ("the reds")`
sub index: `R`
brief: in-chat lane brief (conductor, #245) — no `notes/_briefs/` file was written for this lane
tokens: `UNMEASURED — a lane cannot read its own message.usage from inside the lane; the conductor's _checkin.py reads the seat`

## VERDICT

Run #482 (`9628bae`) painted the `gates` job red in exactly two steps — **the survey (step 5) and the knowledge build (step 6)** — and the three suites behind them (`test_gates.py`, `test_advisory.py`, `_git_commit.sh --selftest`) were GREEN in CI and are GREEN here (31/0 · 19 biting · 14 bites). The survey's failure set at `9628bae`, reproduced in a bare clone with CI's `--timeout 60` (the #194 method), is **four steps: [13] [18] [124] [136]**; the working tree adds [61] (local-only). **[13] is FIXED** — a Claude-owned fixture that rotted when the `s241-D2` diet cut 2,415 tape out of the chain the fixture measures; proven before/after in the clone (exit 1 → exit 77, the legal refusal). **[61] was REGENERATED** with its own generator (local-only red; CI refuses it as 77). **[18] and [124] are NOT fixed and are named below as ruling-shaped**: [18] needs a decision about which of two #218 testimonies in `notes/_GAUGE-LOG.md` stands, and [124] is a port into the two "frozen" `memento-package` copies that moved under a ruling last time (`s228-D3`). **[136] is the conductor's** (manifest v1.0.5, no zip) and was not touched. The build red (step 6) is the FIRST `abort`-routed failure, [13]; with [13] green the build will next abort at [18], so **step 6 stays red until [18] is ruled**. Brief regions 1–4 DONE; `_build_all.py` NOT run (fenced).

COUNTS: findings `10` · ruling-shaped `2` · UNPROVEN `3`

## What was done

**Region 1 — the survey in ranges.** `_build_all.STEPS` = **142** steps. Working tree (HEAD `33062c1` + the other lanes' uncommitted edits) surveyed in five chunks at `--timeout 25` (`survey-1-20.txt` … `survey-121-142.txt`, 64.5s / 4.1s / 5.8s / 42.3s / 64.5s wall), the two TIMEOUTs re-run alone at `--timeout 120` (`survey-13-13-t120.txt`, `survey-126-126-t120.txt`). Then the CI proxy: `git clone --no-hardlinks . /tmp/ciclone && git -C /tmp/ciclone checkout 9628bae` (no `API-KEY.txt`, no `.token-cache.json`, clean status), surveyed in five chunks at CI's `--timeout 60` (`ciclone-9628bae-survey-*.txt`). Collated table: `assets/2026-09-03-245-R-reds/SURVEY-TABLE.md` (142 rows). Totals — working tree: 57 pass · 4 FAIL · 2 TIMEOUT(→ both pass alone) · 3 COULD-NOT-ASK · 76 not asked (mutating). Clone @ `9628bae`: 57 pass · 4 FAIL · 5 COULD-NOT-ASK · 76 not asked.

**Region 2 — the three suites.** `knowledge/_tests/test_gates.py` → **31 test(s), 0 failure(s)** in 76.5s (⚠ with a DECLARED environment patch, finding 9). `knowledge/_tests/test_advisory.py` → **19 case(s), all bite, exit 0** (0.4s). `bash knowledge/_git_commit.sh --selftest` → **14 bites: 6 fire, 7 silent, 1 hatch** (0.2s). None needed splitting.

**Region 3 — classification + fixes.** Table below. Files changed: `knowledge/_capture_gate.py` (my hunks: +13 −5 at `selftest_growth`, lines 7609–7633) and `dashboard/index.html` (regenerated by `python3 knowledge/gen_dashboard.py`, 790,959 bytes, +3,991 −2,426 vs HEAD).

**Region 4 — in-flight caveat.** `git diff --stat HEAD` at my first read: `knowledge/_capture_gate.py | 306 +` (hunks at :3962, :4288, :8570, :9288 — lane D's `s244-D1` discharge arm; NONE touch the M10 path, `grep -c "chain_file_tk\|check_budgets\|_warns_for\|selftest_growth"` over that diff = 0), `notes/_GAUGE-LOG.md | 1 +`, `knowledge/_REVIEW-SIGNOFF.md | 2 +`, `_graph-mark-observations.jsonl | 15 +`, `_memento-index.json | 2 ±`, `_REHEARSAL-LOG.jsonl | 7 +`, `_GRADE-DECISIONS.jsonl | 5 +`; by my last read also `_rulings.json`, `_validate_receipt.py`, 21 `components/*.meta.json`, `meta.schema.json`. **No red in this lane names any of those edits** — every FAIL reproduces in the clean clone at `9628bae` except [61], whose cause is the committed render's age (#228), not today's edits. Nothing was reverted, checked out, stashed or reset in the user's tree.

### The reds

| step | verdict (CI proxy @ 9628bae) | class | fixed | one line why |
|---:|---|---|---|---|
| [13] `_capture_gate.py --selftest` | FAIL (exit 1, 8.1s) | (a) mechanical, Claude-owned | **yes** → exit 77 | M10 fixture (24 fat lines) fell under the 7,700 warn floor after `s241-D2` cut 2,415 tape from the chain; fixture → 34 lines, `CHAIN_BUDGET_TK` untouched |
| [18] `_gm_usage.py --selftest` | FAIL | (b) judgment | no | two #218 `section-usage` lines in `notes/_GAUGE-LOG.md` (:2276 first wrap, :2294 second wrap) testify differently; the reader refuses by design; red since `aa26947` (#219) |
| [61] `gen_dashboard.py --check` | COULD-NOT-ASK (77); FAIL locally | (a)+(c) stale generated file, local-only | **yes** (regenerated) | committed `dashboard/index.html` is the #228 render — `_build_all` has aborted at [13] before reaching [61] since then |
| [124] `_validate_package_delta.py --selftest` | FAIL | (b) judgment (s228-D3 precedent) | no | both `memento-package` `_gen_chain.py` copies are pre-`7f8801f` (23 lines differ); the last port of this kind was a RULING |
| [136] `_release/_gate_release_audit.py --selftest` | FAIL | (b) Dave's (`s219-D4(2)`) | no — conductor's | manifest v1.0.5, no v1.0.5 zip in `apollo-spider/dist/`; the SAME failure also reds the `release` job step 8 |
| step 6 "Knowledge build" | FAIL | derived | partly | `_build_all` aborts at the first `abort`-routed step = [13]; next in line is [18] (also `abort`), then [124] (`gate`), [136] (`abort`) |

## Findings

1. **CI #482's shape, read from the Actions API (public, unauthenticated; `ci-run-482-jobs-steps.json`).** `gates`: step 5 survey **failure**, step 6 build **failure**, steps 7/8/9 (`test_gates`, `test_advisory`, `_git_commit.sh --selftest`) **success**, 10–12 advisory success. `release`: step 8 (release-audit selftest) failure, 9–14 skipped. `render`: all success. Probe: `curl -s https://api.github.com/repos/daveasewen/UX-design/actions/runs/33752385428/jobs`. Step LOGS are `403 Must have admin rights` unauthenticated — the remote URL in `.git/config` carries a PAT and I did **not** use it; the clone is the substitute.
2. **[13] root cause, single-variable.** Same fixture (`_gm_fixture(fat_banner=24)`), same measurer, chain built by pre-#241 `_gen_chain.py` (`git show 7f8801f^:knowledge/_gen_chain.py`) = **9,297 tape**; by HEAD's = **6,882 tape**; delta **2,415**; warn floor `CHAIN_BUDGET_TK[0]` = **7,700**. So the bite "a 24-fat-line banner did not warn the chain" went red at `7f8801f` for a change in the WRAPPER (`s241-D2` cut the 461 bare ids from the state block), not in the budget. Sweep: fat_banner 0→1,022 · 17→5,174 · 24→6,882 · 30→warn · 34→**9,322** (≈ the pre-diet 9,297). Fixture set to 34 via `_m10_fat_lines`, comment names the measurement. Before/after in the clone: `ciclone-9628bae-step13-before.txt` (exit 1, one ❌) → `ciclone-9628bae-step13-after-fix.txt` (exit **77**, 7.9s, refusals = the three gitignored `ds-034/035` `outputs/…` pointers of #194, the form the survey excludes). Working tree, full run after fix: **exit 0, 156.9s wall / 15.5s user** (`step13-local-full-after-fix.txt`).
3. **[13]'s local TIMEOUT is the mount, not the gate — class (c).** Clone on sandbox disk: 8.1s. Same file on `/sessions/…/mnt/UX-design`: >120s wall, 12.5s user (`survey-13-13-t120.txt`). Per-arm timing in-process (`/tmp/time_arms.py`): `selftest_argv_contract` 46.0s, `selftest_rehearsal` 30.7s, `selftest_growth` 12.9s, `selftest_budgets` 7.8s, `selftest_bare_token` 7.1s, `selftest_handoff_history` 5.7s — all I/O-bound (fixture repos + `git` subprocesses on the mount). ⚠ #244 wrap declared skip (5) "`_capture_gate.py --selftest` exceeds the ~178s wall" is therefore a fact about the seat, and this lane's 156.9s run says it just fits when run alone.
4. **[18] has been red for 12 consecutive CI runs (#471→#482, all `failure`; `ci-runs-471-482.json`) and both #218 lines landed in one commit, `aa26947` (#219, 2026-08-26)** — `git log -S"section-usage #218 (second wrap"` and `-S"section-usage #218 (self-report"` both → `aa26947`. The `gates.yml` #221 note already names `[18] _gm_usage` as red at `9dbf413`. The reader's rule (`_gm_usage.py:484-488`): same session number, different testimony ⇒ "one of them is false … REFUSED" — a designed refusal, mutation-covered by its own bite (`:818`). Fixing it means either editing a dated ledger line or changing what "one session" means to the reader. Not this lane's.
5. **[61] is local-only.** Clone: `COULD-NOT-ASK` (the `_governs` untracked-`outputs/…` refusal of #193). Working tree: FAIL, and the diff (`step61-dashboard-diff-local.txt`) is age — `session #228` → `#245`, 293→397 open, 375→484 items, 135→136 snippets, `_LIVE-STATE` refreshed 2026-08-31→2026-09-03. Cause: `_build_all` routes [13] `abort`, so no build since `aa26947` has reached step 61. Regenerated with `python3 knowledge/gen_dashboard.py`; `--check` → `OK — dashboard/index.html in sync` (`step61-gen_dashboard-check-after.txt`). ⚠ It bakes the stores as they stand mid-afternoon and WILL go stale again as lanes write; the wrap's build rewrites it anyway.
6. **[124]: the two package copies are the pre-#241 file.** `python3 knowledge/_validate_package_delta.py` → 2 findings, both `VERBATIM SET … 23 line(s) differ (sha256 9a133a837646 vs source 42e4887e89ba)` for `memento-package/machinery/_gen_chain.py` and `memento-package/claude-plugin/memento/machinery/_gen_chain.py`; 23 = the +15/−8 of `7f8801f`. Precedent: `ddc7821` (#228) ported the `s227-D2` diet into "BOTH frozen `_gen_chain.py` copies" under `s228-D3`, alongside a version move (`s225-D3`, v1.0.4). The selftest's red is its "real repo: clean" control arm — the gate itself is working.
7. **Build step 6 = [13], then [18].** `_build_all.route()`: [13] `abort`, [18] `abort`, [61] `gate`, [124] `gate`, [136] `abort`. An `abort` returns at that step (`_build_all.py:1366-1373`). So with this lane's fix the build gets from step 13 to step 18 and dies there; the survey keeps [18] [124] [136] red. **Step 5 and step 6 both stay red on the next push** unless [18] is resolved.
8. **[126] is not a red.** TIMEOUT at 25s locally; alone at `--timeout 120`: pass in 29.1s; clone at 60s: pass.
9. **`test_gates.py` cannot run unpatched at this seat — class (c).** `shutil.copytree(knowledge/)` ×3, and `knowledge/` here is **5.3G** (`knowledge/assets/photography/` 2.5G + `knowledge/assets/assets (5).zip` 2.5G, both gitignored — `git check-ignore -v` → `.gitignore:76`; tracked `knowledge/assets` = 54.6MB; the clone's `knowledge/` = 101M). Sandbox free: 2.3G. First run died `[Errno 28] No space left on device`. Re-run with `IGNORE` extended by exactly `photography` + `*.zip` (the gitignored population CI never copies) → 31/0. The patch is printed as the first line of `test_gates.txt`.
10. **COULD-NOT-ASK set is stable and environmental.** [74] chromium (proof of record: `render` job, GREEN in #482); [121] [122] `real`-tier stamp vs tape-only checkout (#173/#183); clone-only [61] and [132] (gitignored `outputs/…` pointers, #194). None is a red anywhere.

## RULING-SHAPED QUESTIONS

1. **[18] — which #218 `section-usage` testimony stands?** `notes/_GAUGE-LOG.md:2276` (first wrap: `PRIOR:R C1:R … LANES:R`) and `:2294` (second wrap: `PRIOR:U C1:U … LANES:U`) both carry `#218`; the reader refuses by design and has for 12 CI runs. Options: **(a)** teach the reader that a parenthetical containing `second wrap` keys a distinct row (`#218` + `#218b`) — ~40 lines + 2 bites in `_gm_usage.py`, no ledger edit, but it inscribes a "session can testify twice" semantics nobody ruled; **(b)** strike or annotate ONE of the two ledger lines — a one-line edit to a dated ledger, Dave's; **(c)** reader takes the LATER testimony when duplicates disagree — smallest code, but it silently picks a winner, which is the exact thing `:486` refuses to do. Recommend **(b)** if Dave knows which wrap's reading he trusts, else **(a)**; NOT (c).
2. **[124] — port `7f8801f` into the two frozen `memento-package` `_gen_chain.py` copies now, or hold for the v1.0.6 cut?** Mechanically `cp knowledge/_gen_chain.py memento-package/machinery/_gen_chain.py` (+ the `claude-plugin/memento/machinery/` twin), 23 lines each, audit 2 → 0, selftest green — ~2 minutes. But `ddc7821` did the identical move under `s228-D3` beside a version move, so a bare copy here would be a Claude-ported change to a surface the repo calls frozen. Options: **(a)** rule the port now (a `s245-Dn`, no version move, on the `s228-D3` shape); **(b)** hold and bundle into the v1.0.6 cut that already clears [136] — [124] stays red until then. Recommend **(b)** only if the cut is this week; the red costs one `gate`-routed step per run meanwhile.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** that the fixed [13] is green ON GITHUB — this lane does not commit or push; the clone at `9628bae` is a proxy (same tree, no key/cache), not the runner. Price: one push, ~5 min of CI.
- **UNPROVEN:** [12] `_capture_gate.py --build` (mutating, `abort`-routed, runs BEFORE [13]) was NOT asked in either tree — the survey lists it as not-asked and I honoured the `_build_all` fence. If [12] is red, the build dies there first. Price: run `--build` on the clone (~10s; mutates the clone only).
- **UNPROVEN:** the exact text of #482's own survey output — logs are 403 without admin rights and the PAT was not used. The clone reproduces the same four FAILs and the count (4 survey reds + 1 release red = the "5 errors" the read-back reported), which is consistent, not identical.
- **CLAIMED:** that `selftest_argv_contract`'s 46s is fixture-repo `git` I/O — inferred from `user` vs `real` and the arm's docstring ("run against a REAL fixture repo"), not profiled. Re-read: `python3 -X importtime`/`cProfile` on the arm, ~1 call.

## Evidence

`notes/_subreports/assets/2026-09-03-245-R-reds/` —
`SURVEY-TABLE.md` (142 rows, both trees, generated from the chunk files) · `survey-1-20.txt` `survey-21-50.txt` `survey-51-90.txt` `survey-91-120.txt` `survey-121-142.txt` (working tree, `--timeout 25`) · `survey-13-13-t120.txt` `survey-126-126-t120.txt` (the two TIMEOUTs re-asked alone) · `ciclone-9628bae-survey-{1-12,13-13,14-50,51-120,121-142}.txt` (CI proxy, `--timeout 60`) · `ciclone-9628bae-step13-before.txt` / `-after-fix.txt` (exit 1 → exit 77) · `step13-selftest_growth-after.txt` (0 failures, 34 lines → 9,322 tape) · `step13-local-full-after-fix.txt` (exit 0, 156.9s) · `step18-gm_usage-selftest.txt` · `step61-gen_dashboard-check-local.txt` / `-after.txt` · `step61-dashboard-diff-local.txt` · `step123-package_delta-real.txt` · `step124-package_delta-selftest.txt` · `test_gates.txt` (31/0, env patch declared on line 1) · `test_advisory.txt` (19) · `git_commit-selftest.txt` (14 bites) · `ci-run-482-jobs-steps.json` (every job's step conclusions) · `ci-runs-471-482.json` (12 runs, all `failure`).

**Declared skips, with sizes:** (1) `_build_all.py` NOT run — 142 steps, fenced. (2) [12] `--build` NOT asked — 1 mutating step, see UNPROVEN. (3) #482 step logs NOT read — 403; 0 bytes. (4) `test_gates.py` copies exclude 5.0G of gitignored assets — declared in its output. (5) No commit, no push, no memory write, 0 bytes to `_CHAIN.md` / `_CARRIES.md` / `_LIVE-STATE.md` / `GOOD-MORNING.md`. (6) `git checkout -- knowledge/_capture_gate.py` was run ONCE, in `/tmp/ciclone` only (my sandbox clone, to restore it after the before/after), never in the user's tree.

REPLAY-THESE: `assets/2026-09-03-245-R-reds/SURVEY-TABLE.md` (~4,500 tk) · `assets/2026-09-03-245-R-reds/ciclone-9628bae-step13-before.txt` § the `❌ selftest:` line only (~120 tk) · `knowledge/_capture_gate.py:7609-7633` (~450 tk)
