# `#256`-`wrap` — the delegated capture ritual: v1.0.6 re-cut twice and STILL only proposed, the `--pack` gate red without anything breaking, dream 11 enacted in his order, and a boot cut that could not move its own reading

session: `#256` · 2026-09-08
window: delegated wrap sub (OPUS 5) — ✅ **the conductor's transcript WAS read at this seat** (`knowledge/_checkin.py`)
sub index: `wrap`
brief: the conductor's wrap brief, cut at a DECLARED FILL of 141,987 tokens
tokens: `UNMEASURED at this seat — a sub cannot read its own message.usage. The conductor's FILL is
MEASURED first-hand here at 146,786 real over 64 turns against his DECLARED brief-cut of 141,987,
and his boot 70,241 is the same instrument's first-turn reading.`

provenance: 256 · 2026-09-08
status: observed

## VERDICT

The ritual ran whole — steps 1, 1b, 2, 2c, 2d, 2e, 2f, 2g, 3 (declared), 4, 4b, 4c, 5 — and every
move went through `_gm_move.py`. **The `--wrap` form is REFUSED for the TENTH wrap running**, on
this session's own boot of **70,241 — 241 over the shrink-only 70,000 ceiling, 114 above #255, the
SECOND consecutive breach and the seventh reading of the rising series**. ⚠ **A boot CUT landed
today** (`MEMORY.md` 2,616 → 1,794 tape, cap 1,802 ✓) **and was NOT used as an argument against the
breach**, because the index is read at the next session's first turn and cannot have moved a figure
taken at this one's. **`s256-D1` and `s256-D2` were inscribed by the conductor and read back here at
entries 394/395 (store 393 → 395); neither was re-inscribed.** One carry was **STRUCK** (the v1.0.6
re-cut — it ran, twice) and **five CORRECTED**.

COUNTS: findings 7 · ruling-shaped 3 · UNPROVEN 4

## What was done, by step

- **1** — `_LIVE-STATE.md` `Last refreshed` rebuilt for #256 (#255 demoted to `Previous:`, the
  **#253** segment — **6,372 chars** — trimmed out to the archive at the 2d boundary);
  `_validate_standing_instructions.py` **PASS** (28 standing docs reachable).
  `knowledge/_REVIEW-SIGNOFF.md` **+0 rows** — no review page was made this session.
- **1b** — `_DECISION-HISTORY/2026-09-08-256-recut-and-boot.md`, provenance + status present, five
  numbered sections, both-way linked.
- **2** — new ★ LATEST banner (#256); `TITLE THE NEXT CHAT →` set to the conductor's dictated
  `Apollo - #257: dashboards one-shotable — drive the frozen prompt; his rulings page`; `size:`
  stamp written LAST.
- **2c** — ⛔ **THE #256 BATCH HEADERS WERE CREATED FIRST, for the third consecutive wrap** (#253's
  finding 6, obeyed). #255 banner demoted to ★ PRIOR; **#254 banner, 11 ln → `_GM-ARCHIVE.md`
  § Batch 2026-09-08 #256**, through the mover.
- **2d** — new ⏱ LATEST DELTA #256 (15 ln); #255 demoted; **#253 delta, 17 ln + its 6,372-char
  `Previous:` chain segment → `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-08 #256**.
- **2e** — `## ⬛ DO THIS FIRST` measured at **109 raw lines**, `#### ` scan returns **0** dated
  strata. Nothing to roll; the size is declared rather than the skip. ⚠ **109 agrees with #252,
  #253 and #255; #254's 70 is still not reproducible at this seat.** Both reported, no third
  invented.
- **2f** — `roll_2f` split the #255 stratum: **14 ln → `notes/_GAUGE-LOG.md`** (true EOF) + **2 ln
  → `_GM-ARCHIVE.md`** under the #256 key. The `#### 2026-09-08 #256` stratum was written in its
  place, boot stated ONCE (`s241-D2` S5).
- **2g** — `_build_memento_index.py` rebuilt LAST, after every GM/LS edit and after `_gen_chain.py`.
- **3** — memory is the CONDUCTOR's step by the brief; **0 bytes written at this seat**, which
  cannot see the store (D1a). The cut he made is recorded, not re-asserted.
- **4** — `s256-D1` and `s256-D2` were inscribed BY THE CONDUCTOR and **read back** here: store
  **393 → 395**, entries **394**/**395**, each parsing `by` Dave · `date` 2026-09-08 · `status`
  ruled. Nothing was re-inscribed.
- **4b** — `_gen_titles.py --session 256` run, receipt written. ⚠ **Its derived NEXT-TITLE and the
  title actually written DISAGREE, declared rather than reconciled** — see finding 5.
- **4c** — `_gate_scratch_hygiene.py --clean`: VM fill **62.0%**, **93 owned entries removed**;
  **3M owned by dead sessions remains UNREMOVABLE from inside** and is named for drift visibility.
- **5** — `_git_commit.sh --reconciled`, no `--wrap`; push from here.

Also: **two `_state.json` rows added** — `W-485` (the dream-enact lane report, which the lane did
not file a row for) and `W-486` (this report). ⚠ **`_gate_doc_rows.py --check` read PASS / 0 unrowed
on arrival and that PASS was MISLEADING**: the dream-enact report was UNTRACKED, so the gate's
population could not see it. Finding 4.

## Findings

1. **A clean gate run produced a decision, not a verdict — and that is why the re-cut ran twice.**
   Re-cut #1 (`311723a`, manifest at `b15b7d9`) was green on every arm — 1,669 files,
   `--manifest-check` PASS, `--drift` PASS, dry-run zip `--check` GREEN — and reported a **new
   REPO-BOUND** entry, `knowledge/_validate_roles_resolve.py`: a gate that ships in the pack but
   cannot run from it, because `roles.json`/`shapes.json`/`when-fields.json` are repo-side only.
   That is a pack shipping a lock without its key. It became `s256-D1` and forced re-cut #2
   (`3c8832a` at `1f4c355`): **1,673 files · 43,160,016 B · sha `abb080c52ea771ea` · probe 41
   RUNNABLE / 9 REPO-BOUND / 4 NEEDS-DEP** — the validator moved to RUNNABLE, which is **the
   ruling's effect measured rather than asserted**. ★ A re-run is a proof; a hand-patched manifest
   would have been a claim.
2. **The `--pack` gate is RED at this seat and nothing broke — the exact inverse of #255.**
   `_gate_release_audit.py --pack` prints `❌ apollo-spider/dist/Apollo-Spider-v1.0.6.zip does NOT
   match the manifest at 1f4c35587f30` — **6 manifest paths MISSING** from the pack
   (`_validate_fit_physics.py`, `_validate_roles_resolve.py`, `canon/dp08-anchor.js`, `roles.json`,
   `shapes.json` first), **79 files differing**, the README carrying the wrong sha, and a shipped
   `_MANIFEST.json` that is not the `s223-D8` status-free derivation. `dist/` was untouched by
   ruling; v1.0.6 stays **PROPOSED**. ★ **One wrap ago #255 used this same gate's PASS to retract a
   carry that had been wrong for ten sessions; one wrap later it is red on the same file. A gate
   whose subject is half-updated BY DESIGN reads identically to a gate whose subject is broken** —
   so the record says which question each reading answered instead of filing a defect nobody owns.
3. **A boot cut landed and the boot reading still rose, and the two must not be netted.**
   `MEMORY.md` went 2,616 → 1,794 tape against a 1,802 cap (`_memory_cap_check.py` ✓), hooks
   #237–#251 moved verbatim to `hook-overflow-2026-09-08-256.md` — half 2 of the `s214-D6`
   boot-reduction pair, owed since #214. #256's boot still read **70,241**. ★ **The index is read
   at the NEXT session's first turn, so the cut and this reading are on different clocks**; the
   tempting error is to publish "cut 822 tape" beside "boot 70,241" and let a reader net them into
   a claim nobody measured [[measure-dont-convert-units]]. **The measurement is #257's first turn.**
4. **A green doc-row gate was green because it could not see the file.** `_gate_doc_rows.py --check`
   read `population 339 · unrowed 0` on arrival, while
   `notes/_subreports/2026-09-08-256-DREAM-ENACT.md` had **no row and was untracked**. ⛔ The gate's
   population is built from tracked files, so **an unrowed report is invisible until the commit that
   would have needed the row already exists** — the same shape as finding 2, one layer down:
   a check that is structurally blind reports identically to a check that passed.
5. **The title generator and the conductor disagree about #257, and the disagreement is declared.**
   `_gen_titles.py --session 256` derived `NEXT SESSION TITLE → Apollo - #257: v1.0.6 is re-cut and
   still only *proposed* — ratification is dave's` from this banner's top ⬛ residual item. The line
   written into `TITLE THE NEXT CHAT →` is the conductor's dictated `Apollo - #257: dashboards
   one-shotable — drive the frozen prompt; his rulings page`. ⛔ **The banner was NOT doctored to
   make the generator agree**, and `title_generation_check` checks the receipt's freshness against
   the banner, not equality with the GM line — so both stand, and the derivation is quoted verbatim
   in the `COMMIT STATE #256` block.
6. **A proposal's premise expired between the dream pass and its enactment.** P4(a) argued from
   `knowledge/_screen-gate/dashboard.md` being untracked; at enactment `git ls-files` lists all ten
   subjects. So P4(b)'s new refusal in `_validate_screen.py` **refuses nothing today** and its only
   evidence is a planted untracked file. ★ **A gate built against a state that has since healed is
   indistinguishable, on a green run, from a gate that works** — the mutation proof is the only
   thing separating them, and it exists here.
7. **The wrap's own scratch is still gitignored, for a fourth wrap.** `knowledge/_tmp/wrap256/`
   holds `carry257.py`, seven ops files, the banner / delta / stratum / stamp sources — each
   asserted to exist with a size floor **in the writing process** before the mover read it (#166's
   stale-msgfile lesson) — and that directory is GITIGNORED. #253's finding 8, #254's and #255's,
   unrepaired.

## The carry transform

**462 aged · 6 `[NEW — 0]`→`[1]` · 1 STRUCK · 5 CORRECTED · 6 new · probeable count 327.** The line
was **never re-typed and `_CARRIES.md` was never read whole** (7,277,828 bytes): one programmatic
pass, `knowledge/_tmp/wrap256/carry257.py`, bumped every age bracket (462 substitutions) and applied
six surgical edits, each asserted unique before it was made.

- **STRUCK ①** the v1.0.6 re-cut — receipts `311723a` and `3c8832a`; what remains is RATIFICATION,
  carried forward as `→ #257` item ①.
- **CORRECTED ④** the ceiling — 70,241, a second consecutive breach, with the different-clocks note.
- **CORRECTED ④** (aged) the `META-TAGS-CALLS-CONTEXT` read — **not read at #256 either**, second
  consecutive session.
- **CORRECTED ⑩** dream pass 11 — no longer "0 ruled, awaiting his eye"; `s256-D2` batched four,
  three remain his.
- **CORRECTED ⑥** `dashboard.md` — the P4(a) premise expired, the file is TRACKED, the question is not.
- **CORRECTED ⑧** the CI read-back — `origin/master` **in sync at `44c8061`** before this wrap;
  the four #256 commits were pushed during the session, and **zero** verdicts were read at any seat.

Every correction keeps its original wording verbatim after the receipt.

## RULING-SHAPED QUESTIONS

1. **`POPULATION_DELTA_THRESHOLD = 5` is a placeholder that has shipped.** The enacting lane declared
   it a placeholder **in the code**, and nothing distinguishes 5-because-ruled from 5-because-nobody-
   said. Its alarm already fired once today (`47 vs 33 (+14)`). Dave's.
2. **The `--pack` red has no owner and two possible meanings.** It is one ratification away from
   green and one defect away from nothing. Whether a PROPOSED release should leave this gate red
   between the manifest cut and the ratify word — or whether the audit should learn the PROPOSED
   state — is a definition, not a build.
3. **Where does wrap and lane scratch live?** Unchanged from #253's, #254's and #255's question 3,
   re-raised because this wrap's own scripts sit under the gitignored `knowledge/_tmp/wrap256/`.
   `s218-D7`'s *"evidence beside the report, never session scratch"* is still unsatisfiable there.

## UNPROVEN / CLAIMED (ADR-0016)

- **CLAIMED:** the **subs figure** (108,386, n=1) is the conductor's reading of the harness's
  `subagent_tokens`, **not** `message.usage`, and is recorded as QUOTA. This seat did not observe it.
- **CLAIMED:** every figure attributed to the two re-cuts and to the dream-enact lane (1,669/1,673
  files, 43,160,016 B, sha `abb080c52ea771ea`, probes 40/10/4 and 41/9/4, zip sha256 `4781994f…`,
  selftests 216/216 and 7/0 · 19/0→22/0, gardener 33→47) is **quoted from the wrap brief and the
  filed lane report and NOT re-driven at this seat.** The `--pack` and `--drift` readings ARE
  first-hand.
- **CLAIMED, and it is the one that will age fastest:** the `MEMORY.md` cut (2,616 → 1,794 tape,
  cap 1,802) is the conductor's declared figure. **The store is outside the repo and invisible from
  this seat (D1a)**; nothing here is a first-hand reading of it.
- **UNPROVEN:** that the boot cut reduces boot at all. This wrap adds a breach, not a discharge;
  price to prove: one `_checkin.py` reading at #257's first turn.

## Evidence

No evidence directory: every claim above quotes its probe inline (the mover's receipts,
`_roll_state.py`'s generated line, `_gm_usage.py --sizes --session 256`, `_gate_doc_rows.py --check`,
`_gate_release_audit.py --pack` / `--drift`, `_gen_chain.py`'s generated footer, `_checkin.py`'s
first-turn and rehearsal readings, `git rev-parse HEAD origin/master`, `ls knowledge/_probe/`,
`_gate_scratch_hygiene.py --clean`). ⛔ **This wrap's own scripts and ops files are session-owned and
uniquely named under `knowledge/_tmp/wrap256/` — `carry257.py` · `ops-256-headers.json` ·
`ops-256-ls-a.json` · `ops-256-ls-b.json` · `ops-256-banner.json` · `ops-256-roll2f.json` ·
`ops-256-stratum.json` · `ops-256-generated.json` · `ops-256-size.json` · `ops-256-fills.json` ·
`banner256.md` · `delta256.md` · `stratum256.md` · `lastrefreshed256.txt` — and that directory is
GITIGNORED, so this evidence is LOCAL ONLY.** Finding 7.

REPLAY-THESE: `_CARRIES.md § residual → #257` first four items (~1,800 tk — the v1.0.6 ratification
and the red pack, the three un-batched dream items, the expired premise, the two clocks) ·
`notes/_subreports/2026-09-08-256-DREAM-ENACT.md` § RULING-SHAPED (~300 tk — the threshold literal
and the dashboard question in the lane's own words)
