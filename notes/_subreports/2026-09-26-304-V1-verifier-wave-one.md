# #304 V1 — verifier, wave one: Run 1 PASS · Run 2 PASS WITH FIXES (twelve ratification words un-stamped) · Run 5 PASS

provenance: 304 · 2026-09-26 (Saturday evening) · Fable 5.1 verifier seat, built none of the three runs · mount HEAD `571d458c`, read-only (git `--no-optional-locks` only, no `git status`, no writes but this file) · every run and receipt in a fresh full-history clone at `$HOME/clone304v1` (dies with the VM)
status: observed — every count below was printed by a script or a survey log named in the text; the verdicts are this seat's readings, not rulings
window: UNMEASURED (a seat cannot read its own usage)
COUNTS: clone survey BEFORE 127 pass · 15 FAIL · 4 could-not-ask (re-derived, matches A4/R1 by name) · AFTER as the tree stands 128 · 11 · 7 (TWO NEW REDS, `[139]` `[140]`) · AFTER with the one fix 130 · 9 · 7 (R1's claim, 0 green→red) · stamps sampled 30 of 184, receipt errors 0, consumer-breaking 2 (of 12 in the population) · closes sampled 30 of 53, errors 0 · door rehearsal in the clone: 1 refusal (no SESSION_N), then committed 9a86d3fd in 12.6 s, locks clear
CITES: the #304 plan (`notes/_lanes/304/F/page.src.html`, Runs 1/2/5, Test it hard) · `notes/_subreports/2026-09-26-304-R1-ci-sees-to-146.md` · `…-304-R2-store-tells-the-truth.md` · `…-304-R5-mcp-probe.md` · `…-304-A4-health-and-dependencies.md` · s219-D4(2) · s223-D3 · s234-D5 · s245-D4 · s295-D2 · P5 (2026-08-02) · #208

## THE ANSWER FIRST

Run 1 holds. Run 5 reproduces byte for byte. Run 2 has one defect that turns two green steps red, and it is not in any of the three reports: its audit stamped `enacted` on the twelve rulings that ARE the release ratification keys (`RATIFY_IDS` in `knowledge/_release/_gen_pack_manifest.py`: s219-D10, s223-D7, s225-D1, s228-D4, s257-D1, s257-D3, s260-D4, s262-D6, s267-D2, s268-D1, s268-D2, s268-D3). `ratification_status()` reads the store with `status == "ruled"`, exactly, so every cut v1.0.0 → v1.0.13 now reads PROPOSED to the generator, the fresh manifest no longer matches the one on disk, and the BLOCKING gate `[139]` pack ship-list audit plus its selftest `[140]` go red. Measured in the clone: HEAD's `_rulings.json` → `[139]` PASS, selftest 10 bites 0 fails, `RATIFIED — s268-D3 names v1.0.13`; the wave's `_rulings.json` → `[139]` FAIL, `[140]` 1 fail, `PROPOSED — v1.0.13 is keyed to ruling s268-D3, which is not 'ruled' in the store`. Nobody saw it because R1's clone never held R2's stamps and R2 ran no survey by instruction.

The fix is twelve calls through the sanctioned writer, no hand edit, and I drove it in the clone: `python3 knowledge/_inscribe_ruling.py --set-status <id> ruled --write` for each of the twelve (the writer accepts `ruled` because it is in the store's vocabulary; count stays 638; `_governs.py --selftest` all bites green). With that applied, the full 146-step mutating survey reads exactly what R1 claims — 130 pass · 9 FAIL · 7 could-not-ask, FAIL set `[81] [86] [94] [127] [128] [132] [134] [135] [144]`, no step green before is red after — and the non-mutating survey (CI's step 5) on the rehearsal commit reads 64 pass · 1 FAIL `[128]` · 4 could-not-ask `[13] [61] [74] [136]`.

Everything else the three reports claim, I re-derived and it held: the before/after by name, the door gate's behaviour under stale, refusing, tier-77, named and acknowledged paths (harness 48 arms green in the clone), the twelve help-gate guards (one line each, `--help` only), the four s282 pointers coexisting with R2's stamps (`s282_pointers.py .` prints ALREADY POINTED ×4), the 184 stamps touching only `status`/`evidence`, the 53 closes all Claude's and only `state`/`closed_by`, no close on Dave's word, the store gate rc 0 with 72 selftest bites green, and Run 5's two catalogue hashes (`46e76935…`, `803f96e0…`) with nothing written outside its lane folder.

## VERDICT PER RUN

**Run 1 — PASS.** Nothing to fix. Two things to know, neither blocking (§1).

**Run 2 — PASS WITH FIXES.** Fix 1 (must, before any commit that carries `knowledge/_rulings.json`): restore the twelve `RATIFY_IDS` rulings to status `ruled` through `_inscribe_ruling.py --set-status`, exactly as listed in §2. Fix 2 (must): do not commit `notes/_lanes/304/R2/cite_lines.json` — 90.8 MB, 4,044,412 lines of regenerable working data; GitHub warns above 50 MB and the door has no size gate. Fix 3 (should): mint the three doc rows before the door runs (`_gate_doc_rows.py` refuses the three reports unrowed; rows W-304r1, W-304r2 and a W-304r5 landed the gate in my rehearsal).

**Run 5 — PASS.** `bash notes/_lanes/304/R5/run_all.sh` in the clone: 17.6 s, `levels all {'L1': 137, 'L2': 132, 'L3': 17}`, dashboard `{18, 13, 4}`, T1 10/10 and 137/137, T2 14 planted defects all refused, T3 warm median 61 ms and 0 writes, when-evaluator A 17/20 · B 18/20 · reader 15/20, output sha256 identical to the mount's RUN-LOG (`46e76935…` all, `803f96e0…` dashboard). Files newer than a marker outside `notes/_lanes/304/R5/`: none (the one temp file lives in `$HOME/tmp`, as disclosed). The `RUN-LOG.txt` changes on every run (timestamps); harmless.

## 1 · RUN 1, ADVERSARIALLY

**The clone survey, re-derived.** Fresh `git clone --depth 1 file://<mount>` + `fetch --unshallow` (1,632 commits), HEAD `571d458c`. BEFORE, four chunks (`--include-mutating --resume --range`, timeout 60): 127 pass · 15 FAIL `[8 10 13 38 81 82 86 94 127 128 132 134 135 136 144]` · 4 could-not-ask `[61 68 73 74]`. Identical by name to A4 and R1. Then `git checkout . && git clean -fd`, the mount's `git diff --binary HEAD` overlaid (37 files, applies clean; one "new blank line at EOF" warning, in `notes/_subreports/2026-09-22-297-A-…`, pre-wave dirt), plus the untracked `notes/_lanes/304/{R1,R2,R5,F}`, the three reports, the plan and the two proposal pages.

AFTER as the tree stands (R2's stamps unfixed): 128 · 11 · 7, FAIL `[81 86 94 127 128 132 134 135 139 140 144]`. `[139]` and `[140]` are green→red; cause and proof in § THE ANSWER FIRST. AFTER with the twelve restored, on the rehearsal commit `9a86d3fd`: 130 · 9 · 7, FAIL `[81 86 94 127 128 132 134 135 144]`, could-not-ask `[10] [13] [61] [68] [73] [74] [136]`. That is R1's table exactly.

**The door gate, line by line.** Both halves read as R1 describes. Can it wrongly refuse? Only in the ways R1 already declared: a stale schematic with its path unnamed costs one refusal (the #208 shape, P5), and a schematic input left dirty and unnamed (`knowledge/_graph-mark-observations.jsonl`, `knowledge/_state.json`) renders into the committed page but is not committed, so CI's `[125]` reads stale. I add one input to that list that neither R1 nor the gate comments name: **`notes/_BUILD-VERDICT-LOG.jsonl`**. The schematic's "steps GREEN in the newest recorded run" figure and the chain both read it, and every `_build_survey.py` run appends to it. Measured: after the door commit, `_gen_schematic.py --check` and `_gen_chain.py --check` were both FRESH; one non-mutating survey chunk later (which recorded a row), both `[120]` and `[125]` read STALE with `notes/_BUILD-VERDICT-LOG.jsonl` the only dirty path; `git checkout` of that file and `--no-record` made them green again. For CI this is safe (step 5 is one process and records at its end; step 6 regenerates at `[124]` before `[125]`). For the seat it means: never run the survey on the mount, and if the mount's `notes/_BUILD-VERDICT-LOG.jsonl` is ever dirty at commit time, name it. Today it is clean.

Locks: `fail()` is `echo; exit 1` before any `git add`, and the second half sits where the mention-map second half sits; the rehearsal ended `✓ done — locks clear`, and a refusal (no `SESSION_N`) left `.git/*.lock` empty and nothing staged. Timing: `_gen_schematic.py --check` 2.4 s in the clone. In-run inputs: nothing after the gate (line ~596) writes an input the schematic reads (`_session.py --declare`, `_capture_gate.py --wrap`, `_build_live_state.py --selftest` write elsewhere; `record_observation` lives in `_graph_edges.py` and is called from `_memento_search.py`/`_consult.py`, neither of which the door runs). `_test_git_commit.py` in the clone: 48 arms ALL GREEN, 11 mutation controls, 7.8 s. The two new arms exercise stale-unnamed (refused, nothing staged, HEAD unmoved), a refusing generator (blocks), tier 77 (declared, not regenerated, not blocking), stale-named (rides), `SCHEMATIC_ACK` (declared gap), and fresh-on-disk-unstaged (refused; naming clears); both mutation controls bite.

**Help-gate guards.** The five `knowledge/` files gain exactly one line each (`from _helpgate import help_gate …`), the seven `_render/` drivers exactly the six-line walk-up form; `help_gate` answers only `-h`/`--help` and is a no-op on import (`_helpgate.py` docstring), so no invocation that stated a real argument changes. `[8]` and `[9]` green in the clone.

**`[38]`: newer truth or silent reversal?** Not a reversal of any ruling. s234-D5 makes the meta the one home and the block DERIVED; s245-D4 makes `events` "generated from the snippet, never authored". The #261 N lane (Claude, `c0225a0e`) hand-edited the derived blocks and left the metas behind. R1 typed the metas from those blocks, and the typed `events` match the snippets' own listeners, which I re-measured at HEAD: Navigations click+keydown, Sidebar-nav click+keydown, Tab-bar click+keydown+resize. That is s245-D4's rule applied. The judgment in it is the `fallback` prose: R1 took #261 N's prose over the pre-#261 prose (kept verbatim under `behaviour.$r1_304.before`), and Tab-bar's `$fallbackRender` receipt ("PROVEN by JS-off render #245") was written for the OLD wording. Neither wording is Dave's; the new one is unproven by render. Worth one line on Tuesday's page, not a block. The other meta diff lines are the trailing commas the addition needs.

**`[82]`** `--update` re-seed: +`Tab-bar.reference.html` on `.seg button` (0 `<button>` inside any `.seg` in the DOM, so no pixel moves), `.tabbar .tabbar__item` → `[]`, `_generated` date, one note extended; one note's `—` re-serialised as `—` (the serializer-defaults class, cosmetic). **ASSERT-009** 137→138 matches the mount (138 `*.meta.json`, 142 entries). **`[3]`, `[59]`**: the mount's regenerated `_blast-radius.json`, `_TYPE-BLAST-GATE.md`, three showroom pages and three snippets are byte-identical to the clone's. One note: the clone's mutating step `[1]` moves `_GRAPH-REPORT.md`'s "38 rules x 134 components" to 135 (the compliance KG takes the newly typed navigations meta). The committed pair stays consistent (non-mutating `[3]` green), and CI's step 6 regenerates both; the capture gate's advisory names the same thing as "35 serial members not re-run".

## 2 · RUN 2, ADVERSARIALLY

**The diffs are what the reports say.** `_rulings.json`: 638 → 638, same ids in the same order, no top-level key moved; exactly 184 rulings differ, each only in `status` and `evidence`, old evidence a prefix of new (the four s282 rows carry R1's `commit <sha> - ` prefix inside R2's stamp); the 184 equal `decisions.json`'s ENACTED set exactly, `git diff -w --numstat` 549/368. `_state.json`: 855 → 855, 53 rows differ, every one `owner: claude`, `open` → `done`, only `state` and `closed_by` touched, `-w` 159/106; Dave's 443 untouched; the seven whose condition names Dave (W-43, W-47, W-488 "relayed to Dave in chat"; W-394, W-403, W-412, W-415 "ruled by Dave OR explicitly carried") close on the declared-chat limb or the carry limb, never on his word. `_state.py` +97/−0, the arm is a NOTE until `REGROWTH_BLOCKING`, `python3 knowledge/_state.py` rc 0 (live 693), `--selftest` 72 bites all GREEN, `_gate_doc_rows.py --check` PASS on the committed population.

**Thirty stamps, seed 304** (`random.seed(304); random.sample(enacted, 30)`): s218-D4 s223-D5 s264-D2 s273-D3 s268-D2 s191-D1 s225-D2 s281-D3 s241-D1 s227-D2 s276-D2 s225-D1 s188-D3 s190-D1 s238-D7 s228-D1 s210-D5 s240-D2 s259-D1 s218-D5 s222-D1 s258-D3 s268-D4 s275-D3 s258-D1 s277-D1 s283-D1 s241-D2 s275-D5 s276-D6. For each: the sha resolves, its commit date ≥ the ruling date, the status carries the sha and the old status verbatim, an evidence pointer carries the sha; S-A: the id is in the commit body with no negation and the named files are in the commit; S-B: the id is on the named `path:line` at `571d458c` and the introducing commit names it and touches the path; MANUAL: hand-read. Seven needed a hand read (range forms `s264-D1/D2`, `s218-D4/D5/D6`, `s275-D1..D6`, `s223-D1..D5`; s241-D2's "inflated" sentence is not a negation; s281-D3 rests on s281-D6's landing, `_build_kg_explorer.py:453`; s268-D4's commit message mislabels it s268-D3 but the register file it introduced names s268-D4). **Receipt errors: 0 of 30.** Consumer-breaking: 2 of 30 (s225-D1 = v1.0.3, s268-D2 = v1.0.12), which is the twelve-of-184 class above, 6.5% of the population. The receipts on those twelve are real; the category is wrong: a ratification word is not "enacted in code", it is the key the release gate reads, and the store's one exact-match consumer proves it.

**Thirty closes, seed 304:** W-205 W-299k W-377 W-400 W-383 W-197 W-344 W-460 W-356 W-346 W-415 W-489 W-492 W-430 W-354 W-347 W-202 W-487 W-369 W-100ah W-274 W-403 W-441 W-420 W-499 W-404 W-203 W-226 W-385 W-506. For each: every 8-hex commit resolves with the quoted date and subject prefix, every `path:line` at `571d458c` carries the basename it is said to name (six first read as misses; all six were my own trailing-period parse, the bug R2 also hit, and every line names the file on re-read), every "present at HEAD" path is there, every "ancestor of origin/main" holds. **Errors: 0 of 30.** Two readings are loose and R2 says so in the row: the "#N+1 opener has digested this wrap's stub" limb is met by the next brief naming the previous one as "Form to COPY" (W-377, W-383, W-385), and "each of its N ruling-shaped questions carried" is met by a unit carry line (W-403, W-404, W-415, W-420). Declared gaps, not errors.

**The regrowth arm.** Read whole; `latest` is computed from the store's `opened`, a row is named only when every session it names is below it, closed rows and conditions naming the latest session are not named. Six selftest arms plus the pattern-gutted mutation are in the module and green.

**Residuals for Tuesday's page, not for the seat.** After the twelve are restored to `ruled`, their evidence arrays still carry R2's pointer text "`<id>` enacted in code; the commit is the proof" (the writer appends, it does not remove); harmless to every gate, slightly untrue to a reader. Whether a ratification word should ever carry an `enacted` status, and whether `ratification_status()` should read a lead word instead of an exact string, are release-machinery questions under s219-D4(2)/s223-D3 and Dave's.

## 3 · WHAT THE COMMIT SEAT MUST DO, IN ORDER (rehearsed in the clone, door exit 0)

1. **Restore the twelve** on the mount, through the writer: `for id in s219-D10 s223-D7 s225-D1 s228-D4 s257-D1 s257-D3 s260-D4 s262-D6 s267-D2 s268-D1 s268-D2 s268-D3; do python3 knowledge/_inscribe_ruling.py --set-status $id ruled --write; done`. Then `python3 knowledge/_release/_gate_release_audit.py --check` must print PASS and `--selftest` 10 bites 0 fails, `python3 notes/_lanes/304/R1/s282_pointers.py .` ALREADY POINTED ×4, `json.load` count 638.
2. **Commit Runs 1, 2 and 5 in ONE wave**, not Run 1 alone. `knowledge/_rulings.json` carries both runs and cannot be split, and the schematic renders `knowledge/_state.json` as it stands on disk, so a Run-1-only commit would publish a schematic drawn on R2's uncommitted closes and CI's `[125]` would read stale. If the conductor still wants Run 1 alone, name `knowledge/_state.json` too and accept that R2's closes ride early.
3. **Mint three doc rows** before the door (W-304r1 and W-304r2 as their reports propose; a W-304r5 for `notes/_subreports/2026-09-26-304-R5-mcp-probe.md`, home that path, closes on Dave's answers to its seven decisions or the PoC catalogue step citing it).
4. **Regen serial**, this order, all on the mount: `tokens/_build_blast_radius.py` → `_build_memento_index.py` → `_build_graph_mention_map.py` → `_gen_chain.py` → `_gen_schematic.py`. (`gen_showroom.py --check` is already green there.)
5. **Name every path**, including `_CHAIN.md`, `reviews/MEMENTO-SCHEMATIC-2026-08-07-v2.html`, `knowledge/_memento-index.json`, `knowledge/_graph-mark-observations.jsonl` (dirty since 14:44 today, the schematic counts it), and reconcile the four pre-wave dirty files from #293/#294/#297 (`notes/_dream/_GRADE-DECISIONS.jsonl`, `notes/_lanes/293/J7-IDEA-jev-selects-over-the-kg.md`, `notes/_lanes/294/WRAP-MEMORY-HOOK.md`, `notes/_subreports/2026-09-22-297-A-plain-deck-brain-and-footnotes.md`) either in or out, by name. **Leave out `notes/_lanes/304/R2/cite_lines.json`** and both `__pycache__/` (gitignored anyway).
6. Run the door as `SESSION_N=304 bash knowledge/_git_commit.sh --quiet=<log> --reconciled <msgfile> <paths…>`; without `SESSION_N` T3 refuses (s130-D3), as it did once in my rehearsal. Declared not-a-wrap in the body. Expected in the clone: mention map fresh, schematic fresh, wrap gate RED-visible-not-blocking (241 in scope · 2 fail · 99 warn), doc rows PASS, `✓ done — locks clear`, 12.6 s.
7. Push, read CI back by name. Predicted step 5: 64 pass · `[128]` · could-not-ask `[13] [61] [74] [136]`. Predicted step 6: `_build_all.py` asks step 146 and exits red on `[81] [86] [94] [127] [128] [132]` (his), `[134] [135] [144]` advisory. x64 v arm64 stays the one UNPROVEN.

## UNPROVEN / NOT DONE

- UNPROVEN: CI (x64) equals the arm64 clone; one push prices it.
- Not re-run: R2's `mutation_test.py` (its in-module arms ran green under `--selftest`); the twelve `--help` write-probes (the validator `[8]` covers the class).
- Not done, by fence: no write on the mount but this file; no `git status`, commit or push there; no `_build_all.py` or `_capture_gate.py --wrap` on the mount (the door ran only in the clone); no Project memory. My logs (`before_*`, `after_*`, `final_*`, `nm_*`, `door_rehearsal3.log`, `sample_stamps.py`, `sample_closes.py`, `rulings_fixed12.json`) are at `$HOME/v1logs/` in the VM and are not in the repo; every figure they hold is quoted above.

REPLAY-THESE: § THE ANSWER FIRST · § VERDICT PER RUN · § 3
