# `#262`-`R` — v1.0.9 is PROPOSED at `902d96b`, proved to the edge of Dave's word

session: `#262` · 2026-09-09 · sub index: `R` · provenance: 262 · 2026-09-09 · status: observed
lane: release prep — **DID NOT RELEASE**; `--release` is Dave's word (s219-D4(2)).

## VERDICT
Manifest byte-identical to a fresh generation at its own commit (**1698 files, sha256 `99d3c521b3610c4f`**); the zip bakes **twice** to `c6fcc6cbf61f43477de0a941f91c6c9cd92a7260b7d05517faafd13a77d9d7f7`; `--check` GREEN; generator **221 bites, 0 fails**. Release gates **7/9**, the two reds the known pre-bake pair. ⛔ `dist/` untouched (8 zips, no v1.0.9), `RATIFY_IDS` untouched, no ruling inscribed, no `--release`.
COUNTS: findings 2 · ruling-shaped 0 new (2 carried) · declared skips 2

## Step 1 — the version story (`1e564c0`)
`VERSION` + `MEMENTO_CUT_VERSION` v1.0.8 → v1.0.9 together (s225-D3, one version story), plus the four literals `--check` reads off the stage: `FIRST-SESSION.md`, `gumdrop/_state.json`, the two gumdrop runbooks. **s261-D1 named in the comment as the SCOPE ruling**, with s262-D1/D2/D3 beside it.
⛔ **NO `RATIFY_IDS` ROW**, and a comment says why in the v1.0.5/v1.0.7 shape: s261-D1 is an AUTHORISATION naming what the release carries, never the RATIFYING word (s223-D3). Read back off the module first: `PROPOSED — no ruling is keyed to v1.0.9 yet`. Derived, never typed (s237-D9). `_gate_frozen_release.py:132` stays `v1.0.8` — it moves at `--seed`, after the bake.

## Step 2 — FINDING 1: a #261 repair took a gate out of the pack (`fe500ff`)
First probe: **40/4/10** against v1.0.8's 41/4/9. Exactly one verdict moved, diffed out of the two probe artefacts rather than eyeballed:
`_validate_behaviour.py  RUNNABLE → REPO-BOUND — "runs, but its verdict is about knowledge/_BEHAVIOUR-GATE.md, which the pack does not ship"`
Cause is **#261 M5**, not packaging drift. That lane rightly stopped the gate grading its own record: the write is now opt-in (`--write`) and the default run COMPARES the tracked ledger and REDS on drift. That **flipped the file from an output into an input** — and nothing claims `knowledge/_*.md`, so it was unclaimed, green in the tree, red in the pack, dropped by the differential arm. **A gate that shipped in v1.0.8 silently left the v1.0.9 cut** — third instance after #256's roles/shapes and #260 R2's dataviz receipt [[gate-cannot-pass-in-one-environment]]: *a repair to a gate takes the gate out of the pack.*
Fixed in the sanctioned home the way #256 and #260 R2 used it — one path added to `GATE_DATA_CANDIDATES` with its reason beside it. ⛔ No gate weakened, no skip added, #261 M5's compare-and-red untouched. The ledger carries no timestamp (a pure function of the behaviour sources, which ship), so it is in sync either side. Probe after: **41 / 4 / 9 — v1.0.8's shape exactly**, `ran clean, verdict PASS`.

### The two inherited reds from #261's verifier are NOT pack-probe reds
- `_validate_kg.py` — `REPO-BOUND: reaches for knowledge/gen_kg_edges.py, which the pack does not ship`, unchanged since v1.0.7. Its PROMOTE-edge red is a **repo-side** finding about the KG, invisible to a designer who never gets the edge generator.
- `_validate_type_composites.py` — **RUNNABLE, `ran clean, verdict PASS`** inside the pack. 1083/88 is a repo-side census; the gate is green in both trees.
Neither blocks the cut and nothing was changed to make either green.

## Step 3 — FINDING 2: one showroom page was stale (`902d96b`)
`gen_showroom --check` OUT OF SYNC on exactly one page — `showroom/sidebar-nav.html`, left by `ce2018d` (s262-D3, the selected nav icon). **The showroom ships**, so a stale page here is a stale page in the designer's zip. Regenerated (137 pages, 1 written), re-checked in sync, **before** the manifest was cut — the ship set settles before it is measured.

## Step 4 — the manifest and the twice-proved bake (`0ce944d`)
    git archive 902d96b | tar -x -C /var/tmp/full9
    …--manifest --commit 902d96b --full-stage /var/tmp/full9
      differential arm: ARMED — REPO-BOUND verdicts are MEASURED
      commit 902d96b798a5  files 1698  bytes 45863758  sha256 99d3c521b3610c4f
    --dry-run → /var/tmp/c1  c6fcc6cbf61f4347…d9d7f7  21M
    --dry-run → /var/tmp/c2  c6fcc6cbf61f4347…d9d7f7  21M
    cmp c1 c2 → rc 0 — BYTE-IDENTICAL      --check → CHECK GREEN at 902d96b798a5
⚠ `--full-stage` again, for lane R's #260 reason; ⬛ that the flag is optional is still ruling-shaped, still carried, still not taken here.
**The six components s261-D1 scopes were verified INSIDE the zip, not inferred from the tree** (#257's lesson): `kpi-tile`, `data-grid` (the grid header lives there), `filter-toolbar-bar`, `sidebar-nav` / `navigations` / `tab-bar`, `footer`, `legend` — each as meta + `.reference.html` + showroom page. `_BEHAVIOUR-GATE.md` (5,464 B) is in too.

## Step 5 — the nine release gates
| # | gate | rc | line |
|---|---|---|---|
| 1 | frozen `--check` | 0 | `PASS — 3 arm(s) asked, no frozen surface moved.` (apollo-spider row still `v1.0.8` — correct pre-bake) |
| 2 | frozen `--selftest` | 0 | `selftest: 14 bites, 0 fail(s)` |
| 3 | audit `--check` | 0 | `PASS — byte-identical to a fresh generation at 902d96b798a5 (1698 files, sha256 99d3c521b3610c4f)` |
| 4 | audit `--selftest` | **1** | `RED [pack/frozen-history-is-skipped-not-failed] … got rc=1` |
| 5 | audit `--pack` | **1** | `❌ the manifest reads version 'v1.0.9' and NO zip in dist/ carries it` |
| 6 | ci `--check` | 0 | `PASS — the template parses, ships what it calls, and hides nothing.` |
| 7 | ci `--selftest` | 0 | `selftest: 10 bites, 0 fail(s)` |
| 8 | `build-designer-pack.sh --selftest` | 0 | `221 bites, 0 fail(s)`; all three refusal arms green, incl. `manifest status is PROPOSED, so --release cannot run` |
| 9 | `gen_projections --check` | 0 | `OK — 3 projection(s) and 3 PLACED host file(s) in sync` |

**7/9 — [4] is [5]'s arm and both are the KNOWN PRE-BAKE PAIR.** They say "the manifest names a release nobody baked", the true state of a PROPOSED cut. ⛔ Not to be made green by baking. Declared skips, unchanged: `_validate_screen.py` (no screen of this lane's to grade) and `_tests/test_gates.py` (copytrees ~5 GB, ENOSPC).

## Dave's go/no-go page
`reviews/RELEASE-SPIDER-2026-08-26-v1.html` + the re-injected `…REVIEW.html` overlay. Title: **"Apollo — Spider — the release manifest — PROPOSED"**; body carries the twice-baked fingerprint, the cut commit, the 74-file gates group with `_BEHAVIOUR-GATE.md` in it, and the six components under their registry names.

## Commits
`1e564c0` version story · `fe500ff` the behaviour ledger ships · `902d96b` showroom regenerated (THE CUT COMMIT) · `0ce944d` manifest + probe + go/no-go page. NOT PUSHED (s133-D2).

## What stays for Dave — one sentence
> **"Ratify v1.0.9."**
A RATIFYING word under s223-D3, not an authorisation naming a cut. ⬛ Inscribing it, keying `RATIFY_IDS["v1.0.9"]`, moving the frozen literal and running `bash apollo-spider/build-designer-pack.sh --release --commit 902d96b` are the conductor's steps **after** he says it — this lane wrote none of them. Expect the zip at `c6fcc6cb…d9d7f7`, byte-identical to the dry runs; that equality is the point of the dry run.

## UNPROVEN from this seat
CI green on GitHub (nothing pushed; `_build_all.py` sandbox-impossible) · the 45 shipped gates were not individually driven inside the unzipped v1.0.9 artefact (the probe drives each in a staged copy) · `_validate_fit_physics.py`'s measurement checks under a real browser — still no chromium binary.
