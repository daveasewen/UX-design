# `#245`-`K` — four rung ladders (KPI / chart / rail) on the real dashboard, under the floor Dave ruled, with the content heights measured live

session: `#245` · 2026-09-03
window: `lane K — rung ladder (Apollo v1.0.6 build, evening)`
sub index: `K`
brief: chat brief, #245 conductor (no `notes/_briefs/` file was cut for this lane)
tokens: `UNMEASURED — a delegated sub cannot read its own message.usage; the conductor's lane figure is the one to quote`

## VERDICT

DONE, all regions, nothing ruled. `reviews/RUNG-LADDER-2026-09-03-v1.html` (172,142 B, single file, sha256 `9babeed248e960c0…`) renders the real `Template-dashboard-bento` wall — the snippet's `<main>` spliced byte-for-byte, L4's splice head reused verbatim (the 15-region ledger is identical to L4's, Finding 9) — four times per theme under the model Dave ruled tonight (**FLOOR `minmax(var(--bento-row-unit),auto)` on the NESTED tile grid**, `canon.css:1124`; outer wall HELD at `--bento-row-unit:auto`, `canon.css:17225`). Only the three rung numbers differ per column: **A canon 196/380/184 · B tight 120/240/96 · C content-fitted 152/344/264 · D 2:1 level 160/548/272.** Four themes × light + dark = 8 frames × 4 columns = 32 renders, plus a hidden rung-0 probe per frame that measures the content heights on the machine that opens the page. Render-proof through `page.goto("file://…")`, never `set_content`: **1,135 assertions · 0 FAIL · 2 mutation arms · 25 bites · 0 console errors**, asserted in BOTH views (fit-vs-1:1 track delta 0.0px — the fit view is a transform, not a zoom; Finding 5). ⚠ The picture is honest and a little deflating: under the floor a rung below content never paints, so A, B and C share the same rail and chart rows (271/224 · 499) except A's 40px of KPI air and C's 264 rail floor; B's three numbers are invisible (Finding 1). D is the only ladder under which every row is exactly its rung (Finding 4). Each column carries a *point here* control and the export is ruling-SHAPED text ending in a question. `canon.css`, every meta, every generator, every snippet: untouched (`git status` shows only this lane's three new paths and the one appended signoff row). `knowledge/_build_all.py` not run. Nothing committed. No memory written. No filed report edited.

COUNTS: findings 9 · ruling-shaped 4 · UNPROVEN 3

## What was done

1. **Read L4 first** — `notes/_subreports/2026-09-03-245-L4-row-height-renders.md` Findings 3, 6, 7 and its REPLAY-THESE line; then reused its lane's method rather than rebuilding it: `build_surface.py` lines 1–127 are L4's `build_surface.py` splice head byte-for-byte (the docstring header line and the OUT path are the only edits), and the drive is L4's `drive_renders.py` shape (goto, page-side measurement, assertion-per-render, mutation arms, counts.json).
2. **Measured the content before choosing numbers** — a probe run (rung 0, `grid-auto-rows:auto; align-content:start` on the nested grids, `align-items:start` on the outer wall, 1:1, 1440 stage) read the tiles' intrinsic heights: KPI 156 / 149.6, chart 344, rail 271.1 / 224, identical in all eight theme × mode frames. The probe is now built INTO the page as a hidden fifth stage per frame (`.col[data-ladder="probe"]`, rendered off-screen, never seen) so the numbers are re-read on whatever machine opens it.
3. **Built the surface** with `notes/_subreports/assets/2026-09-03-245-K-rung-ladder/build_surface.py`. Columns: A = canon's literals read from `canon.css:17230–17232` at build time; B = L4's column-3 rungs; C = tallest content per group rounded DOWN to the 8px grid (156→152, 344→344, 271→264); D = content rounded UP (156→160, 271→272) with chart = 2 × rail + the 4px sub-gutter = 548. The only non-spliced CSS is marked `K MODEL` (the floor rule, the four ladder triples, the probe) and `K CHROME`. Each column head: the three numbers in `t-ed-heading-1`, one plain sentence, the derivation, and live readouts per group — rows painted, and whether the rung or the content decided them ("air" / "floor lifted … — content decides" / "rows are exactly the rung"); C and D also print a live check that their derivation still holds on this machine, red if the font moved it.
4. **Drove it** with `drive_renders.py` (Chromium via playwright, viewport 1600×900, `goto(file://)`): per render the held level, the ruled model, the floor law, overrun; per column the claim its own sentence makes; the same at 1:1; the theme filter; the two-red token; two mutation arms. Transcript, measurements (both views), counts, 12 PNGs beside this report.
5. **Rowed the surface** — one row appended to `knowledge/_REVIEW-SIGNOFF.md` (now 378 lines), status AWAITING Dave, figures copied off `counts.json` and this file.

## Findings

1. **Under the floor, a rung below content never paints — so B's three numbers are invisible.** Probe: `one-to-one.json`, mono/light: A rail `[271.1, 224]` chart `[499.1]`; B rail `[271.1, 224]` chart `[499.1]`; C rail `[271.1, 264]` chart `[539.1]`. A, B and C differ only where a rung is ABOVE content (A's KPI 196, C's rail 264). "I do prefer the spacing on 3" (L4's column 3 under the floor) is therefore a preference for **content-only rows**, not for the numbers 120/240/96 — any triple below 156/344/224 renders identically. This is the fact the page exists to show him; it is Question 1's framing, not a recommendation.
2. **The content heights, measured not remembered.** `window.__Kprobe` (rung 0, no stretch, 1440 stage): KPI 156 (tallest of 156 / 149.6), chart 344, rail 271.1 (tallest of 271.1 / 224). Identical across all 8 frames (`the probe's content heights agree across all 8 frames -> PASS`). L4 quoted KPI ~150–156, chart ~505, rail ~224–278 — L4's chart figure was the outer-row stretch, not the chart's own content (its Finding 5); the chart tile's content is 344.
3. **The chart tile carries air under EVERY ladder.** The chart sits beside the rail stack in the outer row, so its one row is always the taller of (its rung, rail row 1 + rail row 2 + 4px): 499.1 in A and B (155px of air inside the tile), 539.1 in C (195px), 548 in D (204px). Probe: the page's chart readout, `chart content 344 → N px of air inside the tile`, and `one-to-one.json`. No ladder removes it; only shorter rail content or a taller chart would. Question 2.
4. **D is the only ladder where every row is exactly its rung.** `D: every row is exactly its rung — the floor idles -> kpi [160, 160] chart [548] rail [272, 272]` and `chart closes level with the rail stack — dead band 0 in both -> … dead 0/0`, PASS in all 8 frames, both views. 548 = 2 × 272 + 4 by arithmetic; the floor does no work anywhere in D. Its price is Finding 3's 204px and 4–10px of KPI air.
5. **The fit view is a transform, not a zoom — and that closed L4's 6.6px caveat.** First run with CSS `zoom` (L4's method) measured the tall rail tile at 279.1px in the fit view against 271.1 at 1:1 — an 8.0px re-wrap that made 24 baseline assertions FAIL (C's "near level", D's "exactly its rung") in the fit view only. Switched the stage to `transform:scale()` with the viewport height set from `offsetHeight × scale`: `fit-vs-1:1 track deltas == 0 … -> max |Δ| 0.0px over 160 tracks`, and the readouts under a column are the same numbers in both views. Probe: `drive-transcript.txt` line `fit-vs-1:1: max |Δ| 0.0px over 160 tracks`.
6. **Canon's KPI rung carries 40 / 46px of air; C removes it, D leaves 4 / 10px.** A: `rows painted 196 / 196 px · rung above content by 40 px — air` (content 156 and 149.6). C: rows 156 / 152 (floor lifted 4 / 0). D: 160 / 160 (air 4 / 10.4). Probe: the page's KPI readouts; `one-to-one.json`.
7. **Mutation 1 bites at the CSS, not the label.** Column C's rail rung secretly forced to 96px via an injected `!important` rule: 24 assertions across the 8 renders of column C FAIL (`computed grid-auto-rows == minmax(264px, auto) -> minmax(96px, auto)`; `every row track >= rung`; `the rail's two rows are within 8px -> [271.1, 224]`; `floor lifts … at most 8px -> rail [271.1, 224]`); reverted → 0 FAIL. Probe: `grep -c "^FAIL  \[MUTATION-1" drive-transcript.txt` → 24.
8. **Mutation 2: the export follows the pick, page-wide.** Pointing at column C in the supercharge/dark frame checks all 8 frames' column-C radios (`one pick is page-wide -> 8`) and the export's line 4 reads `pointed at: column C — Content-fitted — KPI 152 / chart 344 / rail 264`; the question line ends in `?` (`export-sample.txt`).
9. **The spliced canon is L4's canon, byte for byte.** `splice-ledger.json` ledgers of K and L4 compare equal (15 regions, same line ranges, same sha256[:16]) — the two surfaces render the same canon bytes, so a difference between them is the ladder, never the splice. Probe: `python3 -c` equality of the two `ledger` arrays → `True 15`.

## RULING-SHAPED QUESTIONS

1. **Which rung ladder does `Template-dashboard-bento` carry under the floor?** Option A canon 196/380/184 (no edit; KPI air stays) · option B tight 120/240/96 (three literals that never paint — Finding 1; if this is the eye's pick, the honest form is "no rung": `0px`, or dropping the three lines and letting canon's default unit stand) · option C 152/344/264 (content-fitted, rounded down; three literal edits at `canon.css:17230–17232` via the generator) · option D 160/548/272 (2:1 level, the only ladder where the rung IS the row) · other. No recommendation: the page is built so his eye decides; the export encodes whichever he points at.
2. **Is the chart tile's inner air acceptable?** 155–204px under every ladder (Finding 3). Option (a) accept — the rail stack sets the row, the chart fills its card; option (b) shorten the rail content (a content edit to the snippet, ~1 K tokens, re-splice); option (c) give the chart a taller data area (a snippet edit, and the stat-card's chart height is a component question). Not decided here.
3. **Do the rungs go into `canon.css` as literals (as today, `.tpl-group-kpi{--bento-row-unit:196px}`) or as tokens?** Option (a) literals — the generator's template already carries them, one line each; option (b) three tokens on the 8px scale (a `gen_canon_bento.py` change + the AUTO-BENTO regen + the snippet's declared copy; the copy-rot question R3 of #231 reopens). Not proposed here — named because whichever ladder wins lands in one of the two.
4. **Are the content heights to be re-measured with Univers Next before he decides?** The sandbox rendered with Helvetica/Arial (UNPROVEN below); C and D are derived from 156/344/271 and the page flags red if the machine that opens it disagrees. Option (a) decide off the sandbox numbers, re-derive C/D if the flag shows; option (b) run `drive_renders.py` once on a machine with the typeface (~60 s) and read the probe line first.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** the renders with the real typeface. No Univers Next in the sandbox; `type.css` composites resolve (font-family string carries it, PASS) but glyphs fell back, so every content-driven height (156 / 149.6 / 344 / 271.1 / 224, and C's and D's derived rungs) can move on Dave's machine. The page's C and D checks print red if they do. Price: one run of `drive_renders.py` where the font is installed (~60 s).
- **UNPROVEN:** the spliced cascade equals a `canon.css`-linked cascade — inherited from L4 (its UNPROVEN 2); the ledger is byte-identical to L4's, so the gap is the same gap. Price: one drive of ~30 K tokens.
- **UNPROVEN:** Safari and Firefox. The page uses `grid-template-rows:subgrid`, `:has()`, container queries and `transform:scale` on a 1440px stage; verified in Chromium only. Price: one open in each, eye only.
- **CLAIMED:** `canon.css:1124` / `17225` / `17230–17232` line numbers — computed by `build_surface.py` at build time (`splice-ledger.json` → `markers`), not typed; they hold for the `canon.css` on disk at 2026-09-03 and drift with the next generator run.

## Evidence

`notes/_subreports/assets/2026-09-03-245-K-rung-ladder/` (2,057,727 B, 21 files) —
- `build_surface.py` — assembles the surface; lines 1–127 are L4's splice head verbatim. Idempotent (`python3 notes/_subreports/assets/2026-09-03-245-K-rung-ladder/build_surface.py`).
- `splice-ledger.json` — the 15 splice regions (identical to L4's), the canon line markers, the probe's CONTENT constants, the gap, the four ladders.
- `drive_renders.py` — the render-proof. `LD_LIBRARY_PATH=<libXdamage dir> python3 …/drive_renders.py` from the repo (~5 s).
- `drive-transcript.txt` (1,141 lines) — every assertion PASS/FAIL with the measured value. Probe the counts: `grep -c "^PASS"` → 1111; `grep "^FAIL" | grep -vc MUTATION` → 0; `grep -c "^FAIL  \[MUTATION-1"` → 24.
- `counts.json` — `{"assertions":1135,"fails":0,"mutation_arms":2,"mutation_bites":25,"console_errors":0,"renders":32}`.
- `measurements.json` — the baseline `window.__K` table (fit view) + `window.__Kprobe` · `one-to-one.json` — the same table at 1:1.
- `export-sample.txt` — the ruling-shaped export after Mutation 2.
- `render-<theme>-<mode>.png` ×8 (161–166 KB each) — each frame in the fit view · `col-<A|B|C|D>-mono-light-1to1.png` ×4 (120–126 KB each) — each column at 1:1. No page-full PNG (declared skip: L4's was 1.3 MB; the eight frames cover it).
- `_tail.py` (105 B) — a one-line stub. The page tail was written to it and appended into `build_surface.py`; the sandbox mount refuses deletion (`Operation not permitted`), so it is declared here rather than left unexplained. Safe to delete on the desktop.

REPLAY-THESE: `reviews/RUNG-LADDER-2026-09-03-v1.html` (open in a browser, eye only — ~0 tk; as text ~45,000 tk, do not) · `notes/_subreports/assets/2026-09-03-245-K-rung-ladder/render-mono-light.png` (~1,600 tk as an image — the four columns with their readouts, Findings 1, 3, 4, 6 in one picture) · `notes/_subreports/assets/2026-09-03-245-K-rung-ladder/export-sample.txt` (~300 tk — the question verbatim)
