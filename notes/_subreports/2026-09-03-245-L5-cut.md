# `#245`-`L5-cut` — THE CUT: s245-D7/D8/D9 inscribed and enacted, the serial run, v1.0.6 baked PROPOSED

session: `#245` · 2026-09-03
window: lane L5 (THE CUT) — Apollo v1.0.6 build, evening; one Fable sub, after lanes D / K / L2 / L3 / L4 / R
sub index: `L5-cut`
brief: in-chat lane brief (no `notes/_briefs/` file was cut); ruling source = chat #245, Dave's three lines quoted verbatim in each ruling's `says`
tokens: `UNMEASURED — no message.usage at a sub's seat`. Shape for the conductor's ledger: ~110 tool
calls; whole-file reads of the L2, L3, L4, K and R reports and `_TEMPLATE.md`; sliced reads of
`_inscribe_ruling.py`, `_validate_screen.py`, `_validate_wiring.py`, `gen_kg_edges.py`,
`gen_canon_bento.py`, `gen_bento_matrix_217.py`, `_git_commit.sh`, `build-designer-pack.sh`,
`_gate_release_audit.py`, `_gate_frozen_release.py`, `_gen_pack_manifest.py`; one PNG viewed by eye.

**I READ the L2 report's serial section (Region 3) and its REPLAY-THESE line** — the ordered serial
(`gen_component_partials` → ramp FIRST → `gen_canon_components` → `gen_snippet_tokens --check` →
`_validate_snippets` → `gen_theme_cascade --check` → `gen_showroom` → `gen_library_214` → index LAST) is
the order run here, one step per bash call, never `_build_all.py`. Also read: L3 §§ What-was-done /
Findings / RSQ whole (finding 5 and pitfall 7 drove a repair below); L4 and K whole (Q4 of L4 and
finding 1 of K are what s245-D8/D9 enact); R's Q2 (the [124] port shape).

## VERDICT

**All seven brief steps DONE; three commits landed (not two — finding 1 says why) plus one more
owed for the ledger; nothing pushed.** (1) `s245-D7`, `s245-D8`, `s245-D9` INSCRIBED through
`_inscribe_ruling.py` (dry-run then `--write`, reconstruction proof PASSED ×3; **344 → 347**; two
evidence lines amended through `--amend-evidence` after `_governs --selftest` named them). (2) L3
enacted: `groupsWith` in the schema (+1 line), **6 edges in 4 metas** (kpi-tile self, stat-card self,
chart-bar `ref:null` carve-out, template ×3), **136/136 live metas valid**; `_validate_composition.py`
(from `_tmp/l3-245/check_composition.py`) registered as step 1b of `_validate_screen.py`'s chain — C9
BLOCKING, C1/C7/C8/C4 advisory — **17 selftest arms green**, chain drive: a C9 mutant FAILs the screen,
a C1 mutant is ⚠ only; the rails `grouping` dial DERIVED (`--rails`, new bite R6e); the role-word
rename at the ONE source + its two regenerated homes; rule 7b beside 7a (pack-doc gate 215 → 215);
L3 finding 5 REPAIRED (the snippet now declares `--layout-bento-columns:6`). (3) L4 + ladder: `1fr` →
`auto` in `gen_canon_bento.py`'s template, AUTO-BENTO regenerated, the snippet's declared copy
re-spliced byte-identical, rungs 120/240/96 at the source and projected; **playwright render of the
real wall at 1440 × 4 theme/mode frames: 0 overrun, KPI rows 157/151 px content-sized, KPI board 2×2,
outer `grid-auto-rows: auto`**. (4) [124] ported: both `memento-package` `_gen_chain.py` copies now
sha `42e4887e…` = `knowledge/`'s; delta audit 0 failures, selftest green. (5) Serial run whole, every
`--check` green; 136 snippets hashed before/after the serial: **0 changed by the serial** (the one
source edit was mine, before it). (6) Release gates: 7/9 green before the bake, **9/9 green after**
(`--pack` rc 0 on the v1.0.6 zip). (7) Commits `5ae4d32` (content), `f6a8340` (version story — forced,
finding 1), and the CUT commit carrying the zip, manifest and this report; the frozen ledger is
re-seeded in a fourth commit after the zip is in history (the #228 shape). Zip
`apollo-spider/dist/Apollo-Spider-v1.0.6.zip`, **20,174,484 B, sha256
`ffffd5c281f2ebb94667183594a2f24158e2f236627f8b645c53dcfd373860bd`**, two dry-runs byte-identical,
`--check` GREEN; proving copy at repo root (gitignored). Status **PROPOSED**, derived (s237-D9).

**Three things the conductor must not skim.** (a) `gen_canon_bento.py` was silently rewriting the
PROJECTED copy of its own block inside `.cn-template-dashboard-bento` (finding 3) — fixed by `count=1`;
(b) `gen_kg_edges.py` would have DROPPED `groupsWith` on its next regeneration (finding 2) — fixed by a
declared carry-through, and the same run closed a pre-existing #231 drift on three files; (c) I
touched `_build_all.STEPS` and then REVERTED it: adding the composition selftest moved the step count
the read chain derives, which made `_CHAIN.md` stale and the commit gate refuse — the chain is the
wrap's, so the selftest wiring is a declared carry (RSQ 1), and the gate is wired as an ARM instead.

COUNTS: findings `9` · ruling-shaped `4` · UNPROVEN `6`

Brief's own counts line: **rulings 344 → 347 (`s245-D7` `s245-D8` `s245-D9`) · schema +1/−0 · metas
with `groupsWith` 4 (6 edges), 136/136 valid · composition gate 17/17 arms, chain bites 2/2 · rails
dial +1 (10 dials), R6/R6b/R6d/R6e green · rename: source 6/6 occurrences, `canon.css` 3 → 0 old /
3 new, `_memento-index.json` 18 old remain (all `_CARRIES.md` records, the conductor's) · render 4
frames × 3 groups, 0 overrun, 0 pageerrors · serial 14 steps rc 0 · snippets changed by the serial
0/136 · release gates 9/9 rc 0 (post-bake) · zip 20,174,484 B · commits 3 landed + 1 owed.**

## What was done

**Step 1 — inscribe.** Probed the store: `python3 -c "…print(len(d), d[-1]['id'])"` → **344 `s245-D6`**.
Wrote three entries (`assets/…/rulings/s245-D7.json` … `D9.json`); each `says` carries Dave's chat
line VERBATIM (D9's whole paragraph); `evidence` = `chat #245 2026-09-03 - Dave: "…"` + the surface put
to him + the lane report + `commit 33062c1` (the tree before). Dry-run ×3 clean, `--write` ×3
(`inscribe-write.txt`: 344 → 345 → 346 → 347, reconstruction proof PASSED each), ids read back
`['s245-D7', 's245-D8', 's245-D9']`. `_governs.py --selftest` then FAILED on two lines — D7's
root-level `_REVIEW-L3-…html` carries no `/` (the PATHISH form needs one) and D8's `277.7/223.8` reads
as a path — both re-cut through the sanctioned `--amend-evidence` (dry-run then write, `says`
untouched by construction; `amend-evidence.txt`), selftest rc 0.

**Step 2 — L3 enact.** Schema: the L3 fragment applied at the `mustNotNeighbour` anchor with its
description re-worded from PROPOSED to `RULED s245-D7` (+1/−0, `apply-schema-and-edges.txt`). Metas:
textual span insertion of `groupsWith` as the last `edges` key in `kpi-tile`, `stat-card`,
`chart-bar`, `template-dashboard-bento` — four proofs per file (reconstruction ==, object equality with
only `edges.groupsWith` added, Draft-7 valid) — the `$note`s re-cut from L3's proposal to say RULED and
to name the s245-D6 class words; the two content-level edges (`summary` ↔ `status-indicator`) NOT
written (Q7 (a)). `_validate_kg.py` then reported 6 DRIFTED files → finding 2 → `gen_kg_edges.py`
gained `DECLARED_EDGE_TYPES = ("groupsWith",)` carried verbatim through a regeneration; a live run
of the generator touched exactly 3 files (the pre-existing #231 drift); `_validate_kg.py` → OK.
Gate: `knowledge/_validate_composition.py` (411 lines) = `check_composition.py` + help gate + C7/C8/C4
(`check_c7_c8_c4()`), `blocking_reds()` / `advisory_reds()`, NOT-APPLICABLE for pages without
`.c-bento`; `_validate_screen.py` step 1b (`gate_composition()`; only C9 blocks; docstring says so);
`_validate_wiring.py` ARM_OF gains `_validate_composition.py` → `_validate_screen.py` AND
`_validate_receipt.py` → `_validate_screen.py` (finding 4: an orphan since #238); both imports made
literal (`import _validate_receipt as receipt`) so the wiring gate can see them → **54 scripts, 52 wired,
0 failures**, selftest 17 bites. Selftest arms: the 10 of L3 + R0 (literal stripped → 77) + M7 (wall
16px → C7) + M8/M8b (foreign `<div>` / `order:` → C8) + M9/M9b (`row-reverse` / `grid-row-start` →
C4) + K3 (`border:` must not read as `order:`) + K4 (no bento → 0): **17 arms, 0 failed**. Chain drive
in-process (`chain-drive-composition.txt`): real snippet ✅ · M1 → 3 blocking → FAIL · M3 → PASS with 3
⚠ · no-bento → n/a. Default population `_validate_screen.py` → 7 × `composition: n/a`, RESULT PASS.
Rails: `gen_bento_matrix_217.py` gained `grouping_dial()` (reads ONLY `edges.groupsWith` +
`$composes`), `dials.grouping` by addition, R6/R6b widened to 10 dials, R6e added; `--rails` rewrote
`_bento_edit_rails.json` (sha `e55922e0…` → `08784e86…`, +71/−1: the edge changes it, by construction);
`gen_foundations_217.py --check` was OUT OF SYNC → written → OK (the bento-rails page now prints the
`grouping` row). Rename: 6 occurrences in the ONE source; after the serial `canon.css` carries 0 old / 3
new (`canon.css:17239-17241`), `showroom/` 0; `_memento-index.json` keeps 18 old-name occurrences —
every one a `carries-section` record of `_CARRIES.md` (the conductor's, by addition at the wrap).
Rule 7b inserted after 7a in the pack SKILL (+22/−0), pack-doc gate over a staged HEAD manifest with
the edited file overlaid: **215 → 215 findings, 0 name `generate-from-canon`**. Finding 5 repaired:
`--layout-bento-columns:6;` appended to the two theme DECL lines (the fixture L3's own selftest used).
`_REVIEW-SIGNOFF.md` L3 row → RULED #245.

**Step 3 — L4 + ladder.** `gen_canon_bento.py` template: `minmax(var(--bento-row-unit),1fr)` →
`…,auto)` + the comment re-cut for s245-D8; `--check` STALE → write → **canon.css diff +69/−40 for a
one-word change** → finding 3 → `count=1` → the snippet's AUTO-BENTO copy re-spliced from canon's FIRST
block (10,404 → 10,597 chars, byte-identical, the header's char count updated) → `gen_canon_components`
re-projected the scope → `gen_canon_bento.py --check` OK. Rungs: `.c-bento.tpl-group-lead{
--bento-row-unit:120px }` / `-evidence 240px` / `-context 96px` at the snippet (was `-kpi 196` / `-chart
380` / `-rail 184`), projected at `canon.css:17239-17241`. grep before/after: `l4-ladder-sha-before.txt`
+ `hash-table.md`. Render (`/tmp/render_bento.py` → `render-bento-1440.txt/.json`, playwright chromium
via `seat_env.sh`, `page.goto("file://…")`, 1440×1000, mono/console × light/dark): wall 6 columns, outer
`grid-auto-rows auto`, lead `minmax(120px, auto)` rows [157.1, 151.3] tiles client==scroll, evidence
`minmax(240px, auto)` [501.2], context `minmax(96px, auto)` [273.2, 224], **overrun 0 in 12/12
group-frames, pageerrors 0, KPI board 2 × 2**. `render-bento-mono-light-1440.png` viewed by eye. L4 + K
rows → RULED #245.

**Step 4 — [124].** `cp knowledge/_gen_chain.py` → both package copies (`9a133a83…` → `42e4887e…`);
`_validate_package_delta.py` → 0 failures; `--selftest` all bites pass (`port-124.txt`).

**Step 5 — the serial** (`regen-serial.txt`, one step per bash call): `gen_component_partials --check`
OK · `gen_token_ramp` 0 synced / 148 in sync (RAMP FIRST) · `gen_canon_components` 136 → `--check` OK ·
`gen_canon_bento --check` OK · `gen_snippet_tokens --check --quiet` 5015 bindings, 0 would change ·
`_validate_snippets` 136, 0 failures · `gen_theme_cascade --check` OK · `gen_showroom` 1 written,
`--check` OK, `--selftest` 16 bites · `gen_bento_matrix_217 --rails` · `gen_foundations_217` 1 written,
`--check` OK · `gen_library_214 --check` OK (write: no change) · `gen_dashboard` written, `--check` OK ·
`_build_memento_index` 1997 records, `--check` current (INDEX LAST). Hash table: `hash-table.md`.

**Step 6 — release gates, pass 1 (before the bake, `release-gates-pass1-before-commit.txt`):** [1]
frozen `--check` 0 · [2] frozen `--selftest` 0 (14 bites) · [3] audit `--check` 0 · [4] audit
`--selftest` **1** (the PRE-BAKE arm, [136] as R named it) · [5] `--pack` **1** ("manifest reads
v1.0.5 and NO zip carries it") · [6] ci-template `--check` 0 · [7] `--selftest` 0 (10 bites) · [8]
`build-designer-pack.sh --selftest` 0 (216 generator bites; PROPOSED refusal green) · [9]
`gen_projections --check` 0. Pass 2 after the bake is in `release-gates-pass2-after-bake.txt` (table
in the final message).

**Step 7 — commit, manifest, bake.** Commit 1 via `_git_commit.sh --reconciled` with 119 named paths
(stale 0-byte `index.lock` from 18:00 moved aside by the script; the delete grant was requested so git
could clear its own lock — finding 8): **`5ae4d32` `after #245 2026-09-03 — v1.0.6 content: L2/L3/L4
enacted, ladder B, [124] port`** (294 files, +40,947 / −2,673; `commit-1.txt`). `DOC_ROW_ACK` declared
for the six lane reports (their `_state.json` rows are the reconcile's — RSQ 3). `VERSION` /
`MEMENTO_CUT_VERSION` → v1.0.6; `--probe --commit 5ae4d32` (60 s) + `--manifest` → 1667 files,
PROPOSED; dry-run bake at 5ae4d32 → `--check` **RED: 3 stamped paths** (finding 1) → the four carried
literals synced → commit 2 **`f6a8340` `after #245 2026-09-03 — v1.0.6 version story: VERSION +
MEMENTO_CUT_VERSION + the four carried literals`** (5 files, +9/−6) → `--probe` + `--manifest --commit
f6a8340` (1667 files, 42,690,401 B, manifest sha `0519a1c9741d05ba`, status PROPOSED) → dry-run ×2
(`ffffd5c2…` both, `cmp` identical, "3 staged file(s) carry the literal, 0 rewritten") →
`_make_review.py` re-injected the release page's overlay → `--check <zip> --commit f6a8340` **GREEN** →
`cp` into `apollo-spider/dist/Apollo-Spider-v1.0.6.zip` (the gitignore exception makes it tracked) and
`Apollo-Spider-v1.0.6-PROVING.zip` at the repo root (`.gitignore:40 *.zip`, never committable; sha
equal, `zip-sha256.txt`) → `_gate_frozen_release.py` literal `v1.0.4` → `v1.0.6` → commit 3 (the CUT:
zip + manifest + probe + release page pair + this report) → `--seed --at HEAD` → commit 4 (the ledger;
the #228 shape, finding 7) → release gates pass 2. The shas of commits 3 and 4 are in the final
message, read back from `git log`; this file was written before they existed.

### FILES TOUCHED (for the reconcile)

| path | delta | state |
|---|---|---|
| `knowledge/_rulings.json` | +3 entries (D7 D8 D9), 2 evidence amends | via `_inscribe_ruling.py` only |
| `knowledge/components/meta.schema.json` | +1/−0 | `groupsWith` beside `mustNotNeighbour` |
| `knowledge/components/{kpi-tile,stat-card,chart-bar,template-dashboard-bento}.meta.json` | +6 edges | span insertion; the template file re-ordered by `gen_kg_edges.py` (edges last — the generator's shape) |
| `knowledge/components/_nodes-pattern.json` · `_nodes-context.json` | +bento entries | pre-existing #231 drift closed by the live regen |
| `knowledge/gen_kg_edges.py` | +11/−0 | `DECLARED_EDGE_TYPES` carry-through |
| `knowledge/_validate_composition.py` | NEW, 411 lines | the gate, 17 arms |
| `knowledge/_validate_screen.py` | +30/−2 | step 1b; literal imports |
| `knowledge/_validate_wiring.py` | +2 ARM_OF entries | composition + receipt |
| `knowledge/_render/gen_bento_matrix_217.py` · `_bento_edit_rails.json` | +~80 · +71/−1 | `grouping_dial()`, R6e; rails regenerated |
| `knowledge/canon/gen_canon_bento.py` | template `auto`; `count=1` | s245-D8 + finding 3 |
| `knowledge/canon/canon.css` | AUTO-BENTO + the scope re-projected | `1126` / `16882` `auto`; `17239-17241` rungs + role words |
| `knowledge/snippets/Template-dashboard-bento.reference.html` | 79,320 → 80,196 B | rename ×6, rungs, columns literal ×2, copy re-spliced, comments |
| `showroom/template-dashboard-bento.html` · `showroom/_foundations/bento-rails.html` · `dashboard/index.html` · `knowledge/_memento-index.json` | regenerated | serial |
| `apollo-spider/skills/generate-from-canon/SKILL.md` | +22/−0 | rule 7b |
| `memento-package/**/_gen_chain.py` (2) | ported | [124] |
| `knowledge/_REVIEW-SIGNOFF.md` | 3 rows RULED | L3, L4, K |
| `knowledge/_release/_gen_pack_manifest.py` · `_pack_manifest.json` · `_pack_gate_probe.json` · `_gate_frozen_release.py` · `_frozen-releases.json` | v1.0.6 | the cut |
| `apollo-spider/gumdrop/_state.json` · 2 gumdrop runbooks · `apollo-spider/FIRST-SESSION.md` | v1.0.5 → v1.0.6 literals | finding 1 |
| `apollo-spider/dist/Apollo-Spider-v1.0.6.zip` | NEW 20,174,484 B | tracked by the gitignore exception |
| `reviews/RELEASE-SPIDER-2026-08-26-v1.html` + `.REVIEW.html` | rewritten by the bake, overlay re-injected | |
| `knowledge/_tmp/l3-245/check_composition.py` | replaced by a 12-line stub | the mount refuses `rm` (gitignored dir) |
| `knowledge/_screen-gate/*.md` · `_COMPOSE-AUDIT.md` · `_ENACTMENT-REGISTER.md` · `_INTEGRITY-REPORT.md` · `_PARTIALS-GATE.md` · `_enactment-register.json` | regenerated by the gates driven | |

⛔ Not touched: `GOOD-MORNING.md` · `_LIVE-STATE.md` · `_CHAIN.md` · `_CARRIES.md` · `_state.json` ·
`notes/_GAUGE-LOG.md` · the three instrumentation appends (`_REHEARSAL-LOG.jsonl`,
`_graph-mark-observations.jsonl`, `_GRADE-DECISIONS.jsonl` — left dirty, declared) · radius tokens ·
any constant/ceiling · memory · `_build_all.py` (edited, then reverted byte-identical) · no push.

## Findings

**1 — THE BRIEF'S TWO-COMMIT RECIPE CANNOT BAKE A GREEN ZIP, AND THE REASON IS THE #224 CLASS AT ITS
FOURTH CUT.** Probe: `build-designer-pack.sh --dry-run --commit 5ae4d32` printed `⚠ DRIFT:
memento-package/_state.json typed v1.0.5 and was stamped to v1.0.6` (×3); `--check <zip> --commit
5ae4d32` → `CHECK RED — 3 file(s) differ from the commit's blobs`. The stamp is byte-neutral only when
the repo copies already carry the manifest's version, so the literals (`apollo-spider/gumdrop/…` ×3
and `FIRST-SESSION.md`, the set #230 named) must be in the commit the manifest is generated at — hence
`f6a8340` between content and cut. #230's own subject called this "the #228 defect 2 RECURRING at the
very next cut, permanent fix still open"; it recurred again here. RSQ 2.

**2 — `gen_kg_edges.py` REBUILDS `edges` FROM PROSE AND WOULD HAVE DROPPED `groupsWith`.** Probe:
`_validate_kg.py` after the population → `FAIL — …kpi-tile.meta.json DRIFTED from a clean regeneration`
(and stat-card, chart-bar, template + the 2 registries); `build_edges_for_meta()` derives every edge
type from `relationships`/`$composes`/… and `splice_edges()` replaces the whole object. L3's proposal
lived in memory and could not see this. Fix by addition: `DECLARED_EDGE_TYPES = ("groupsWith",)`
carried from the file's own prior `edges`, appended last (the generator's own key order). Scratch
regen after the fix: the 3 populated component metas byte-identical; the template + 2 registries still
drifted — the SAME 3 files drift at HEAD `33062c1` (probed on a `git archive HEAD` tree): the #231
template was committed without a `gen_kg_edges.py` run, the class of L2's finding 4. Live run: exactly
those 3 files changed; `_validate_kg.py` OK.

**3 — `gen_canon_bento.py` REWROTE THE PROJECTED COPY OF ITS OWN BLOCK.** Probe: after the `1fr` →
`auto` write, `git diff --stat canon.css` = +69/−40; the diff shows `:where(.cn-template-dashboard-bento)
.c-bento{…}` rules replaced by the unscoped block — `render()` used `re.sub(START…END, block, existing)`
with no `count`, and since #231 canon.css carries TWO marker pairs (1048–1218 and the projection at
16806–16951). Fixed `count=1`, comment names the measurement; the projection is
`gen_canon_components.py`'s from the snippet, and it re-projected correctly in the serial. Any earlier
`gen_canon_bento.py` run since #231 would have done this silently until the next component regen.

**4 — `_validate_receipt.py` WAS AN ORPHAN BY THE WIRING GATE'S READING SINCE #238.** Probe:
`_validate_wiring.py` → `⛔ ORPHAN: _validate_receipt.py exists on disk and NOTHING RUNS IT`. It is
imported and called by `_validate_screen.py` (wired) but through `importlib.import_module`, which the
ARM check's `^\s*(import|from)` regex cannot see, and no ARM_OF entry existed. Declared as an arm and
the import made literal; 0 failures.

**5 — THE REAL ARTEFACT NOW PASSES THE BLOCKING GATE BECAUSE FINDING 5 OF L3 IS REPAIRED, NOT BECAUSE
THE GATE ASSUMES 6.** Probe: standalone before the repair → `UNPROVEN … unproven 1`, rc 77
(`composition-drive-before-repair.txt`); after the two-literal repair → `GREEN … unproven 0`, rc 0;
selftest arm R0 keeps the stripped form at 77. The render confirms the consequence the repair was for:
KPI board 2 × 2 at 1440 (L3 measured 4 × 1 as shipped). A build judgment, declared: the file's own
comment (line 838) and its meta always claimed the literal.

**6 — UNDER THE FLOOR THE RUNGS NEVER PAINT, EXACTLY AS LANE K SAID.** Probe: `render-bento-1440.json`
— lead rows 157.1/151.3 against a 120 rung, evidence 501.2 against 240, context 273.2/224 against 96;
every row == its tallest tile's `scrollHeight` (±1.5 px, Helvetica fallback). K's probe read 156 /
149.6 / 344 / 271.1 / 224 for the same content; the +1 px is the wall at 1376 px vs K's 1440 stage.

**7 — THE FROZEN LEDGER NEEDS THE ZIP IN HISTORY BEFORE IT CAN BE SEEDED.** Probe:
`seed()` → `surface_at()` → `git ls-tree -r <rev>`; there is no worktree form. The #228 precedent is
two commits (`fb433dd` BAKED, then `7fd6128` the ledger records v1.0.4). Followed here; the frozen
gate at the CUT commit alone reads the dist/ surface as moved without a bump until the ledger commit
lands — both go to Dave together, unpushed.

**8 — THE STALE `index.lock` PREDATES THIS LANE AND MADE MY ONE `git stash` A SILENT NO-OP.** Probe:
`.git/index.lock` 0 B, mtime 18:00 (the lane began after 20:00); `git stash -q && …; git stash pop` →
"No stash entries found" with the tree intact (97 dirty paths before and after, rulings 347). No stash
was used again; pre-state probes were taken from `git archive HEAD` trees instead. The commit script's
mv-aside worked, but git could not unlink the lock it creates per `git add` ("Operation not permitted")
until the Cowork delete grant was requested for the folder — the runbook's line 16 case.

**9 — `gen_bento_matrix_217.py --selftest` HAS 3 PRE-EXISTING REDS THAT ARE NOT IN `_build_all.STEPS`.**
Probe: working tree → bites 12, C0, C4b red (`--text-secondary` resolving `#1A1A1A` not `#545454`;
`--border-subtle` fallback `#D7D8D6`); the HEAD `33062c1` copy on a `git archive` tree → the same three
(+ bite 19, environmental: no photography in the archive). R6/R6b/R6d/R6e green in both. Colour-token
territory, not this lane's; named so the new R6e is not blamed for the rc 1.

## RULING-SHAPED QUESTIONS

⛔ Do-not-rule honoured: the radius candidate set, the #218 gauge-ledger duplicate ([18] left red), the
4px audit (not started — the carry is written INTO `s245-D9`'s `ruled` in Dave's words), `_CARRIES.md`.

1. **Wire `_validate_composition.py --selftest` into `_build_all.STEPS` — at the wrap, or as its own
   commit?** It runs today only when someone runs it; the gate itself runs on every screen via the
   chain (ARM). Adding the STEPS entry moves the count `_CHAIN.md` derives (142 → 143), so it belongs
   to whoever regenerates the chain. Option (a) the wrap sub adds the entry and regenerates the chain
   (~200 tokens); option (b) leave it an arm-only gate. **Recommend (a).**
2. **The carried-version literals — derive them, or keep re-typing them at every cut?** Four homes moved
   by hand this cut (finding 1) and the ledger's fifth (`_gate_frozen_release.py` `SURFACES`) too. #228
   priced the derivation and left it Dave's. Option (a) the build script's stamp becomes the ONE writer
   and a gate fails when a repo literal disagrees with `VERSION` (~2K tokens, one selftest arm); option
   (b) leave as is. **Recommend (a)** — it recurred at every cut since #224.
3. **The six lane reports have no `_state.json` row.** Committed at `5ae4d32` under `DOC_ROW_ACK`;
   this report rides the same declaration. The rows are the reconcile's; option (a) the wrap adds seven
   rows; option (b) rows only for reports carrying open items. **Recommend (a)** — the gate's own text.
4. **C7/C8/C4 — advisory, or does any of them block?** s245-D7 Q5 (b) wired them as arms of the same
   gate and Q4 (b) named only C9 as blocking; they are ADVISORY here by that reading. CA-2 is a WCAG A
   criterion. Option (a) keep advisory; option (b) C4 blocks. Not decided here.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN: CI green on GitHub.** Nothing pushed; the gates were driven at this seat. Price: Dave's
  push, ~5 min of CI. Known reds that survive: [18] (do-not-rule) and step 6's abort at [18].
- **UNPROVEN: the renders with the real typeface.** Sandbox Helvetica fallback; the content-sized rows
  can move a few px on Dave's machine. Price: one run of `/tmp/render_bento.py`'s twin where Univers
  Next is installed (~30 s). The 2 × 2 layout and 0-overrun cannot move (they are floor + column facts).
- **UNPROVEN: `test_gates.py` / `test_advisory.py` / a full `_build_all.py`.** Not run (fenced; the
  copytree ENOSPC precedent). What IS established: every serial step and every touched gate's
  `--selftest`/`--check` rc 0 in this window. Price: one CI run.
- **UNPROVEN: C8's RENDERED leg** (per-band contiguity of a group's members). The static leg is what
  ships; the gate's own output says so on every run. Price: a playwright arm at 1100/820/520, ~1K tokens.
- **UNPROVEN: the cold test of the zip.** `--check` GREEN against the manifest and two dry-runs
  byte-identical, but no pristine unzip + runner drive here (the #232 lane did that for v1.0.5 and found
  the design contract's last command exits 1 — not re-checked). Price: Dave's cold test with the PROVING
  copy, or one unzip + `run-gates` drive, ~3K tokens.
- **UNPROVEN: `gen_thumbs.py` for `template-dashboard-bento`** (missing thumbnail, inherited from L2's
  finding 4; the page's render changed here). Price: one run.
- **CLAIMED — none.** Every number above is from a probe in this window, in the evidence dir.

## PITFALLS (consequences replayed)

| # | what could go wrong | owner |
|---|---|---|
| 1 | A future `gen_canon_bento.py` run before this commit's `count=1` would clobber the projection again — the fix is in `5ae4d32` | anyone regenerating canon |
| 2 | Adding a new DECLARED edge type without adding it to `DECLARED_EDGE_TYPES` drops it at the next `gen_kg_edges.py` run — `_validate_kg.py`'s freshness arm catches it, loudly | meta authors |
| 3 | The v1.0.6 zip in `dist/` is PROPOSED: `--release` still refuses; the frozen ledger row says v1.0.6 once commit 4 lands — a ratifying word keys `RATIFY_IDS` later, never a re-bake | Dave |
| 4 | Pushing commit 3 without commit 4 leaves the frozen gate red at HEAD | Dave, GitHub Desktop |
| 5 | The 4px audit carry lives in `s245-D9`'s `ruled` text and in the K/L4 signoff rows — `_CARRIES.md` is the wrap's to write | wrap sub |
| 6 | `_memento-index.json` still indexes 18 `tpl-group-kpi` mentions from `_CARRIES.md`; a grep for the old word is not a rename miss | readers |

## Evidence

`notes/_subreports/assets/2026-09-03-245-L5-cut/` (≈1 MB; the two PNGs ≈ 450 KB)

| file | proves |
|---|---|
| `rulings/s245-D7.json` … `D9.json` · `*.evidence-amended.json` · `inscribe-dry-run.txt` · `inscribe-write.txt` · `amend-evidence.txt` | the three entries as submitted; 344 → 347; the two evidence amends |
| `meta.schema.before.json` · `apply-schema-and-edges.txt` | schema +1; 6 edges in 4 metas with proofs; 136/136 valid |
| `gen_kg_edges.before.py` · `gen-kg-edges-live.txt` | finding 2 — the carry-through and the live run |
| `selftest-validate-composition.txt` · `composition-drive-{before,after}-repair.txt` · `chain-drive-composition.txt` · `_validate_screen.before.py` | 17 arms; 77 → 0 on the real artefact; the chain's C9-blocks / C1-advisory behaviour |
| `gen_bento_matrix_217.before.py` · `selftest-gen-bento-matrix.txt` · `selftest-gen-bento-matrix-at-HEAD-33062c1.txt` | the dial; finding 9 (the 3 reds pre-exist at HEAD) |
| `rename-grep-before-sources.txt` · `l4-ladder-sha-before.txt` · `hash-table.md` · `snippets-sha256-{before,after}.txt` · `generated-sha256-before.txt` | the rename census; before/after hashes; 0/136 snippets changed by the serial |
| `regen-serial.txt` | every serial step with rc |
| `render-bento-1440.{txt,json}` · `render-bento-mono-{light,dark}-1440.png` | the render proof, 4 frames |
| `pack-doc-gate.txt` | 215 → 215, 0 name the file |
| `port-124.txt` | [124] shas before/after; audit 0; selftest green |
| `gates-touched-sweep.txt` | the gates driven on the touched files |
| `release-gates-pass1-before-commit.txt` · `release-gates-pass2-after-bake.txt` | the 9 steps, before and after the bake |
| `commit-1.txt` · `commit-2.txt` · `commit-3.txt` · `commit-4.txt` | the four commit-script transcripts |
| `manifest-probe.txt` · `manifest-gen.txt` · `bake-dryrun-{a,d}.txt` · `bake-dryrun-{b,e}-and-check.txt` · `zip-sha256.txt` | the probe/manifest at both shas; the RED check at 5ae4d32 and the GREEN at f6a8340; twin dry-runs; the zip's sha and size |

**Declared skips, with sizes:** `render-bento-console-*.png` not written (the JSON carries the console
frames' numbers; 2 PNGs ≈ 450 KB saved) · `/var/tmp/bake245{a,b,c,d,e}` stages (5 × ≈ 60 MB, sandbox
scratch, not evidence) · `knowledge/_tmp/l3-245/` untouched except the stub · the `_capture_gate.py
--build` step NOT run standalone (the commit script ran `--wrap` visible-not-blocking: 183 in scope, 0
fail, 21 warn).

REPLAY-THESE: finding 1 + `bake-dryrun-a.txt` lines 17–20 (~300 tk — why there are three commits) · `render-bento-1440.txt` (~600 tk — the floor and the rungs, measured) · `chain-drive-composition.txt` (~400 tk — what blocks and what warns) · `hash-table.md` (~500 tk) · RULING-SHAPED 1–4 above (~500 tk) · `knowledge/_rulings.json` `s245-D9` `ruled` (~300 tk — the 4px carry in his words)
