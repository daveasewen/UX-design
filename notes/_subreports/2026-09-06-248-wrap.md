# `#248`-`wrap` — the delegated capture ritual for #248 (session 2026-09-05, ritual 2026-09-06 — a WRAP DATE SPLIT): four rulings read back at 364 (not re-inscribed), the residual → #249 transform (0 struck, 6 corrected, 10 new, 267 probeable), the ceiling arm RED by name after the 2f roll, and the `--wrap` commit REFUSED — stopped at the first obstacle, as briefed

session: `#248` · 2026-09-05 (session) / 2026-09-06 (this report, from `date`)
window: conductor window (FABLE), delegated wrap sub (FABLE)
sub index: `wrap`
brief: the conductor's wrap brief, in chat (no brief FILE was written for this wrap); it points at `notes/_briefs/2026-09-05-247-delegated-wrap-brief.md` + `notes/_subreports/2026-09-05-247-wrap.md` for the ritual's shape and at `knowledge/_RUNBOOK-capture-ritual.md` for the mechanics — all three read whole before anything was written.
tokens: `UNMEASURED at the sub's own seat as a SPEND; the seat's FILL is measurable and is 184,553 real, peak across 43 turns (knowledge/_checkin.py, first-hand, newest mounted transcript). The two are different quantities and neither is converted into the other.`

## VERDICT

The capture ritual ran **COMPLETE THROUGH STEP 4c AND STOPPED AT STEP 5's GATE**, inside the brief's DO-NOT-RULE fence:
**the four rulings `s248-D1`…`s248-D4` were READ BACK from `knowledge/_rulings.json` at 364 (each parses, `by` Dave ·
`date` 2026-09-05 · `status` ruled, `says` verbatim) and NOT re-inscribed**; then steps 1, 1b, 2, 2c, 2d, 2e, 2f, 2g, 4, 4b
and 4c all executed, **2e was legitimately empty and is declared with its size**, step 3 (memory) was the conductor's and is
declared as ZERO bytes written here, and step 5 was driven as far as `_capture_gate.py --wrap`, which is **RED on ARM 1 of
`boot_constant_drift_check` (BLOCKING): the 2f roll put #247's boot 70,822 into `notes/_GAUGE-LOG.md`, the arm graded it a
NEW breach AND un-discharged #242's (`s244-D1`'s clause is EVERY post-breach reading under), and the discharge form cannot be
written — it needs a post-breach reading, none exists yet, and #248's own boot (70,974) is over too.** The brief said: *if a
gate refuses, name the FIRST obstacle and stop there rather than working around it.* That obstacle is named below verbatim,
nothing was worked around, and **NO COMMIT WAS MADE at this seat** — the record is written, the chain generated and FRESH, the
index rebuilt LAST, the msgfile written; the commit is the conductor's, one command, and the #243/#244 precedent for exactly
this red (an `after #248` commit DECLARED not-a-wrap, the `--wrap` FINAL owed) is named as the one legal state short of Dave's
word. Every roll went through `knowledge/_gm_move.py` from session-owned, uniquely-named ops files under
`knowledge/_tmp/wrap248/`, each asserted for existence and a size floor **in the writing process**, dry-run first, with every
receipt read back against the ops it was meant to run; nothing was hand-edited. **Nothing of Dave's was ruled beyond his
four verbatim answers, and nothing was re-worded, re-dated, re-stamped or re-scoped; no constant, band, ceiling, floor,
advisory, stop line, wall, roster or version was moved.** ⛔ Dave's other words today (12 columns / min tile 2 / rows
3-3-2-2-2 or 4-3-6 written "13" · arm L "not for stats" · the headline-stat composite · "some other messiness") are carried
as NOTES, not rulings, in his words.

★ **The headline the next session needs: the ceiling arm is now RED on TWO counts and cannot be discharged by any wrap** —
#247 70,822 is a breach with no post-breach reading; #248 70,974 will join the log at #249's roll as a third; the remedy the
gate names is CUT THE BOOT, and the literal is Dave's (`_CARRIES.md` § residual → #244 ⑤'s three options, still his).

COUNTS: findings `7` · ruling-shaped `4` · UNPROVEN `4`

## What was done

**Step 0 — the brief, read; the shape, read; the rulings, read back.** `knowledge/_rulings.json` § `rulings` parses at
**364**; `s248-D1`…`D4` present with the fields above; `governs` name the bento snippet, `Chart-bar.reference.html`, the bento
meta and the plan page. The #247 wrap's own scripts (`knowledge/_tmp/wrap247/*.py`, gitignored, still on disk) were read as
the mechanical template and re-cut for #248 — same mover, same ops-file discipline. ⚠ **Two premises of the brief were
stale and are corrected, not repeated:** HEAD was `2319d2a` (dream pass 11, committed 2026-09-06 07:28), not `deb172a`, so the
tree was THREE ahead of `origin/master` `4dc12b6` at the opener and will be FOUR after the commit; and `_LIVE-STATE.md` was
already dirty with the dream pass's §🔀 row and a header prefix, both carried VERBATIM (the prefix now stands inside the
`Previous:` chain) and committed as the scheduled lane's.

**Check-in at the opener** (`python3 knowledge/_checkin.py`): **1 STRUCTURAL** (the retrieval index STALE — the #32 defect,
healed by 2g run LAST) · 1 heals-at-wrap (the GM header date — healed by the WRAP DATE SPLIT line) · **27 warn** · FILL
184,553 real, boot 70,974, peak over 43 turns · SEAM CLEAR · DREAM: pass 11 FRESH, unruled.

**Step 1 — `_LIVE-STATE.md` refreshed.** New `## ⏱ LATEST DELTA — 2026-09-05 … #248` (14 lines incl. blanks; 10 bullets), the
#247 delta demoted to ⏱ PRIOR, the `*Last refreshed:*` header rebuilt with the #248 summary leading (dated **2026-09-06 from
`date`**, naming the split), the dream-pass-11 header that stood there and #247/#246 kept verbatim as `Previous:` segments,
and a **#248 WRAP DATE SPLIT paragraph ADDED beside #241's** (which stands verbatim; its ruling-shaped question — what a
midnight-spanning wrap should stamp — is still Dave's at age 7). `python3 knowledge/_validate_standing_instructions.py` →
**PASS, 28 standing docs reachable, GOOD-MORNING structure intact.** `knowledge/_REVIEW-SIGNOFF.md`: **0 rows added at this
seat** — the W2 review-page row was the conductor's (dirty at the opener), committed as his.

**Step 1b — dossier written.** `_DECISION-HISTORY/2026-09-05-248-w2-rhythm-one-tile-leads.md` with its `provenance: 248 ·
2026-09-05` / `status: observed` block and both-way links. Nine sections: why one edit became three lanes · why "edit mode is
coming" is a constraint · why two of three edits could not reach the composed page and where fit must live · why the gate is
red over an orphan-free render · why 12 columns is a vocabulary question · why the judge was blind again and what twice means ·
why the harness defect is a count · why the wrap was delegated and what it could not commit · what the lanes cost.

**Step 2 — `GOOD-MORNING.md`.** New ★ LATEST #248 banner: **10 substantive lines, 1,184 tape** against `s241-D2`'s cap of
10 / 1,200 — the eighth banner bound by it, met with a margin of **16**, after four rounds of girth-cutting (the first draft
read 1,353): all six filed reports cited by FULL PATH (the `s218-D7` citation check is literal — a brace-expanded path does not
count), the four rulings as ids with short glosses, W2's figures, the two edits that did not reach the page, the judge, the
not-done name list, the gauge. Headline shortened once more because `_gen_titles.py` REFUSED the derived RENAME line at 152
tape against the 120 cap. `TITLE THE NEXT CHAT` → `Apollo - #249: fit lives in the chart — D2 to the components`. The GM
header gained a **#248 WRAP DATE SPLIT line by ADDITION** after #241's (the gate's `is not today` check reads the first 40
lines). `size:` stamp re-written LAST and iterated to an **EXACT fixed point** with `_gen_chain.py` — GM **37,061 tape**
(+935 on #247's 36,126) · §A **6,957** (byte-identical to #237…#247) · LS **58,506** (+445) · corpus 95,567; `_CHAIN.md`
**9,987 tape = slice 9,135 + wrapper 852**, GM header+LATEST 4,815, LS delta 4,320 — **1,068 UP on #247's 8,919**, just under
the `s214-D6` ~10–12K band and above M10's 7,700 advisory; the LS delta (+803) is where four verbatim rulings and three edit
lanes cost.

**2c — banner roll + the carry set.** `## Batch 2026-09-05 #248` opened in `_GM-ARCHIVE.md`; the ★ PRIOR **#246** banner moved
there **verbatim, 12 lines**, by `{"op":"move"}` after its EXIT CHECK (2 marker lines; 25 names probed in `_CARRIES.md` §
residual → #249, 25/25 present); the #247 banner demoted to ★ PRIOR in the same transaction that inserted #248's. The carry set
was written straight into `_CARRIES.md` § `## residual → #249` (clause (i)): **371 ages bumped**, ten `[NEW — 0]` became `[1]`,
**NOTHING STRUCK**, **SIX CORRECTED** with `s188-D2` receipts and NOT struck (W2 — ran and landed, `s248-D4`, which tile leads
still his; the Tabs demo width — recurred, third occurrence; the stale comments — re-framed as the gate's own law L296/L301;
the CI read-back — three unpushed, four after; the strays — a new 07:33 lock; the #247 boot breach — the arm's two verbatim
fails), and **TEN new** at age 0 (fit into the chart components · canon.css unbuilt / the band rule snippet-only · C9's law vs
`s248-D1` · 12 vs 6 columns · the spans dial absent · the judge twice + the headline-stat shape · "some other messiness" · the
#248 boot breach · the probe not planted · dream pass 11's four proposals). **`_capture_gate._carry_items` reads 257 on the
#248 line and 267 on the #249 line** — probed, printed, on the banner.

**2d — delta roll.** The #245 ⏱ PRIOR DELTA moved **verbatim, 16 lines**, to `_LIVE-STATE-ARCHIVE.md` § `Rolled 2026-09-05
#248`, and #245's `Previous:` chain segment (**3,815 chars**) moved into the **same** section in the **same** mover batch,
replaced in the spine by a dated trim note. EXIT CHECK: 7 marker lines, 21 Dave-owed names probed present in the #249 carry line
before the move. Deltas now **3/3**.

**2e — nothing to do, and the size is ZERO DATED STRATA.** `## ⬛ DO THIS FIRST` measures **78 raw lines / 10,140 tape** and a
`#### ` scan returns **0** (`_gm_usage.py` agrees at DOFIRST:10140).

**2f — stratum split, run BEFORE the carries so correction 6 could quote a measurement.** `{"op":"roll_2f","session":247}`
→ **19 lines** appended at true EOF of `notes/_GAUGE-LOG.md` and **2 lines** into `_GM-ARCHIVE.md` under the newest batch,
driven with the SESSION-QUALIFIED anchor `> **COMMIT STATE #247:**`. Then `boot_constant_drift_check('.')` was DRIVEN: **2
fail** (quoted in § Findings 1). The new `#### 2026-09-05 #248` stratum — 19 lines plus the `s214-D6` line — then written into
`GOOD-MORNING.md` § ⏱ SESSION STRATA (#96/#95/#40/#41/#42 EXEMPT, untouched). It carries: the boot **stated once** (70,974,
+152 on #247, 974 OVER the ceiling, the third post-diet breach) · `subs 699,000 tokens (n=6)` (the conductor's declared ≈
figures; the wrap sub excluded) · the `wrap-handover:` row (brief-cut 183,983 · sub-cut 184,553 · delta 570) · the ceiling
arm's two fails verbatim · consult-receipts (honest negative) · section-usage/section-sizes · the chain measurement (36
BANNER-DISCIPLINE lines in the log after the roll, 35 at the opener; the ordinal not claimed) · sixteen declared skips, each
with a size · the four EXIT CHECKS · eight premise checks · `COMMIT STATE #248` naming the refusal and the #243 form. After the
roll `_roll_state.py` prints `2c OK (banners 2/2) · 2d OK (deltas 3/3) · 2f OK (strata 1, log #247)` and that line is on the
banner verbatim.

**2g — retrieval index rebuilt LAST**, after every GM/LS edit and this report were final.

**Step 3 — memory: ZERO bytes written at this seat.** The conductor's. NON-REPO, outside every gate; neither written nor read
back here.

**Step 4 / 4b — decision nodes + titles.** `W-434` (the W2 brief) · `W-435` (lane D) · `W-436` (lane D2) · `W-437` (lane D3) ·
`W-438` (lane R) · `W-439` · `W-440` (the two SEAM files — receipts, rowed because the doc-row glob covers every flat `.md` under
`notes/_subreports/`) · `W-441` (this report, minted after the file existed — the store REFUSED the row before that, correctly)
via `_state.add`, every one refused-or-accepted by the store's own close-condition check; nothing closed; store **506 → 514**.
`_gate_doc_rows.py --check` → **0 UNROWED** at the opener (the new documents were untracked and outside its population).
`_gen_titles.py --session 248` run twice — REFUSED once at 152 tape (headline shortened, the `_gen_titles` derivation given a
colon to stop at), then accepted; receipt regenerated against banner #248. **Step 4c** — `_gate_scratch_hygiene.py --clean`
removed 2 entries this user owned (`/tmp/crop.png`, `/tmp/r`); VM 59.0%; the wrap's own ops files live in
`knowledge/_tmp/wrap248/` (gitignored, in-mount). `python3 knowledge/gen_showroom.py` was run because the `s191-D1` sync gate
reads OUT OF SYNC on `showroom/template-dashboard-bento.html` after the three lanes' snippet edits — 1 page written, a
generator's output, not a hand edit, named here so the commit can stage it.

**Step 5 — `_capture_gate.py --wrap` DRIVEN, RED, STOPPED.** Final run: **187 in scope · 2 fail · 23 warn** once the index was
rebuilt (3 fail before 2g; 4 before the LS gauge bullet was given the `stop line 150,929` figure the seam block's
`CHAIN_STOP_RE` probes for — a defect of this wrap's own first draft, fixed through the mover). The two that stand are ARM 1's,
verbatim in § Findings 1. `_git_commit.sh --wrap` was NOT run: it consumes the same gate (#74-D1) and would refuse at the same
line without staging anything; running it would only have moved the lock. ⛔ **No commit, no push at this seat.** The msgfile is
written and ready: `knowledge/_tmp/wrap248/_msg-248-wrap.txt`. The conductor's one command, per the #243/#244 precedent:
`SESSION_N=248 bash knowledge/_git_commit.sh --reconciled knowledge/_tmp/wrap248/_msg-248-wrap.txt <paths below>` — no
`--wrap`; T3 subject `after #248 2026-09-06 — …`; the red prints visible, not blocking; the `--wrap` FINAL stays OWED.
**Paths to stage:** `GOOD-MORNING.md` `_LIVE-STATE.md` `_CARRIES.md` `_CHAIN.md` `_GM-ARCHIVE.md` `_LIVE-STATE-ARCHIVE.md`
`notes/_GAUGE-LOG.md` `knowledge/_REVIEW-SIGNOFF.md` `knowledge/_gen_titles_receipt.json` `knowledge/_rulings.json`
`knowledge/_state.json` `knowledge/_memento-index.json` `knowledge/snippets/Template-dashboard-bento.reference.html`
`showroom/template-dashboard-bento.html` `notes/_REHEARSAL-LOG.jsonl` `notes/_dream/_GRADE-DECISIONS.jsonl`
`notes/_dream/_MEMORY-GRADES.json` `_DECISION-HISTORY/2026-09-05-248-w2-rhythm-one-tile-leads.md`
`notes/_briefs/2026-09-05-248-W2-rhythm-brief.md` `notes/_dp-scores/w2.json` `notes/_subreports/2026-09-05-248-W2-D-SEAM.md`
`notes/_subreports/2026-09-05-248-W2-D-rhythm-edit.md` `notes/_subreports/2026-09-05-248-W2-D2-chart-fit.md`
`notes/_subreports/2026-09-05-248-W2-D3-reflow-vfit.md` `notes/_subreports/2026-09-05-248-W2-R-SEAM.md`
`notes/_subreports/2026-09-05-248-W2-R-rerun.md` `notes/_subreports/2026-09-06-248-wrap.md`
`reviews/W2-RHYTHM-2026-09-05-v1.html`. NOT staged, by the fence: `_tools/` (92 KB) · `knowledge/_screen-gate/dashboard.md`
(7,663 B) · the 0 B `.git/index.lock` of 07:33. `outputs/w2-rhythm/` (9.2 MB) is gitignored (`.gitignore:67`).

**Citation note for the gate.** The four W2 reports + two SEAMs are cited by full path on the ★ LATEST banner. The two SEAM
files warn (advisory) for carrying no `COUNTS:` / `REPLAY-THESE:` / `RULING-SHAPED QUESTIONS` — they are seam receipts, not
reports, filed by the lanes in the template's directory; NOT edited here (ADR-0017), declared.

## Findings

1. **The ceiling arm fired on TWO counts, and there is no legal discharge.** Verbatim: *"boot-drift CEILING BREACH #242
   70,710 ⛔ NOT DISCHARGED: the `s244-D1` declaration at #245 matches this gate's figures, but 1 post-breach reading(s) are
   OVER the ceiling 70,000 — #247 70,822. The ruling's clause is EVERY post-breach reading under; a later breach is a new
   breach and un-discharges the older one. The remedy is to CUT THE BOOT."* and *"boot-drift CEILING BREACH:
   `_gauge_tokens.BOOT_CEILING_TK` = 70,000 and 1 post-diet reading(s) EXCEED it — #247 70,822. ⛔ `s240-D2`/`s241-D1` make
   this number SHRINK-ONLY … Raising it is Dave's word alone and is not a price a wrap may pay to unblock itself."* The
   `BOOT_CEILING_DECL_RE` form requires ≥1 `#N figure` after "post-breach readings"; a newest-reading breach has none. #248's
   70,974 makes #249's roll a third breach. The derived band (ARM 2) is inside: 68,629 ±1,595, newest +2,192.
2. **A brief one commit stale.** HEAD was `2319d2a`, not `deb172a`; the dream pass had committed at 07:28. Corrected with `git
   log` as the receipt; the count in the CI-read-back carry corrected the same way.
3. **The citation check is literal.** A brace-expanded path list cited nothing; six full paths cost ~120 tape of a 1,200 cap and
   were paid for by girth. Worth knowing before the next banner is drafted.
4. **`_gen_titles.py` derives the NEXT title from the top residual bullet's bold clause up to its first colon, else 12 words** —
   a bracketed age inside the bold lands in the title. A colon after the label is the fix; `s225-D2`'s pointer-line shape does
   not say so.
5. **The seam block's STOP probe (`CHAIN_STOP_RE`) reads the literal `stop line <N>` from the chain**, and the only place that
   phrase lived was the LS delta's gauge bullet. A wrap that paraphrases it breaks the next opener's plan block. Fixed here;
   worth a pointer in the runbook's gauge line.
6. **The dream pass wrote into `_LIVE-STATE.md` and left it uncommitted** — a scheduled lane's edit riding in the next session's
   commit, under that session's subject. Carried as fact; whether a scheduled lane should commit its own §🔀 row is the lane's
   contract, not this wrap's.
7. **A generator had to run for the commit to be legal** (`gen_showroom.py`, the `s191-D1` sync gate) — as at #247. Three lanes
   edited the snippet and none regenerated the showroom, correctly per their fence; the wrap is where it lands.

## RULING-SHAPED QUESTIONS

⛔ Do-not-rule honoured: which tile leads, the status surface, C9's law, the column vocabulary, the spans dial, the plan's four
RSQs, the fold row, the Tabs fence, Q8–Q16, DP-17, the midnight stamp, the ceiling, and every #247 carry at its age. None
re-worded, re-scoped or closed.

1. **A discharge form for a NEWEST-reading breach, or the arm graded only on the newest reading?** (finding 1) — the three
   options already carried at residual → #244 ⑤ (leave ARM 1 red · a declared form · newest-only) are Dave's; the wrap adds
   only the fact that the current form is structurally unwritable for the newest reading, so a red at every wrap is now the
   default until the boot is cut. Recommend nothing; the constant is his.
2. **Should the `--wrap` FINAL be committed by the conductor as the #243 form now, or held for Dave's word?** (a) commit
   `after #248` now, `--wrap` owed · (b) hold the tree dirty until he rules. The #243/#244 precedent is (a) and the record says
   why; the brief's stop clause is why this seat did neither.
3. **Should a scheduled dream pass commit its own `_LIVE-STATE.md` §🔀 row** (finding 6)? (a) yes, in its own commit · (b) leave
   it for the next wrap, declared. Recommend (a).
4. **Do SEAM files belong under `notes/_subreports/`** (citation note)? They trip three advisory warns each for not being
   reports. (a) a `notes/_subreports/seams/` home outside the glob · (b) give them the three lines · (c) leave. Recommend (a).

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** the commit's outcome — none was made here; the conductor's command and its subject are the next record's.
- **UNPROVEN:** the `s241-D2` S1 cap is graded by the gate on the ★ LATEST banner text; 1,184 / 10 was measured with
  `_capture_gate.measure_tokens` on the inserted text and not read from a gate verdict line.
- **UNPROVEN:** whether the conductor's memory write for #248 exists — NON-REPO, invisible here.
- **UNPROVEN:** the subs figures (≈131K · ≈110K · ≈131K · ≈235K · 45K · 47K) are the conductor's declared harness-counter
  readings, rounded; the wrap sub's own spend is unmeasured and not added.
- **CLAIMED:** every W2 figure is copied off the four filed reports, the judge verdict and the brief, never re-measured here.

## Evidence

`knowledge/_tmp/wrap248/` (gitignored, in-mount): `ls_step1.py` · `carries.py` · `ops_2d.py` · `gm_2c.py` · `stratum.py` ·
`stamp.py` · `s214fix.py` · `rows.py` · `banner248.txt` · `banner-v2.txt` · every `ops-*.json` the mover consumed ·
`gate-1.txt` · `gate-2.txt` · `gate-final.txt` · `_msg-248-wrap.txt`. Standing homes touched: `GOOD-MORNING.md` ·
`_LIVE-STATE.md` · `_CARRIES.md` · `_GM-ARCHIVE.md` · `_LIVE-STATE-ARCHIVE.md` · `notes/_GAUGE-LOG.md` · `knowledge/_state.json`
· `knowledge/_memento-index.json` · `knowledge/_gen_titles_receipt.json` · `_CHAIN.md` (generated) ·
`showroom/template-dashboard-bento.html` (generated).

REPLAY-THESE: this file § RULING-SHAPED QUESTIONS (~500 tk) · `_CARRIES.md` § `## residual → #249`, the TEN new items (~2.5K tk) · `knowledge/_tmp/wrap248/gate-final.txt` (~3K tk)
