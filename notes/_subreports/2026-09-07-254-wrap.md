# `#254`-`wrap` — the delegated capture ritual: two rulings inscribed, the eighteen-calls carry STRUCK rather than aged, and the #253 wrap's own recall-plant claim falsified at this seat

session: `#254` · 2026-09-07
window: delegated wrap sub (OPUS 5) — ✅ **the conductor's transcript WAS read at this seat** (`knowledge/_checkin.py`), unlike #253
sub index: `wrap`
brief: chat-only wrap brief from the #254 conductor (no brief FILE was written this session)
tokens: `UNMEASURED at this seat — a sub cannot read its own message.usage. The conductor's FILL is
MEASURED first-hand here at 180,702 real over 84 turns against his DECLARED brief-cut of 176,625,
and his boot 69,797 is the same instrument's first-turn reading.`

provenance: 254 · 2026-09-07
status: observed

## VERDICT

The ritual ran whole — steps 1, 1b, 2, 2c, 2d, 2e, 2f, 2g, 3 (declared), 4, 4b, 4c, 5 — and every
move went through `_gm_move.py`. **The `--wrap` form is REFUSED for the EIGHTH wrap running** on
`boot_constant_drift_check` (breaches #247 70,822 and #248 70,974), so the commit is made in the
#243 form; the ceiling was not moved and #254's own under-ceiling boot was not used to argue the arm
shut. **Dave's two rulings were inscribed by the conductor and read back here at entries 391 and 392
(store 390 → 392); neither was re-inscribed.** Two carries were STRUCK with `s188-D2` receipts and
five CORRECTED — one of the corrections **retracts the #253 wrap's own record**.

COUNTS: findings 7 · ruling-shaped 3 · UNPROVEN 4

## What was done, by step

- **1** — `_LIVE-STATE.md` `Last refreshed` rebuilt for #254 (#253 demoted to `Previous:`, the
  **#251** segment — **6,768 chars** — trimmed out to the archive at the 2d boundary);
  `_validate_standing_instructions.py` **PASS** (28 standing docs reachable).
  `knowledge/_REVIEW-SIGNOFF.md` **+0 rows** — the `META-TAGS-CALLS-CONTEXT` row was written by the
  conductor before the brief and was verified present here, not duplicated.
- **1b** — `_DECISION-HISTORY/2026-09-07-254-strike-the-tags.md`, provenance + status present,
  eight sections, both-way linked.
- **2** — new ★ LATEST banner (#254); `TITLE THE NEXT CHAT →` set to
  `Apollo - #255: pay the bento debt, collapse intent`; `size:` stamp written LAST.
- **2c** — ⛔ **THE #254 BATCH HEADER WAS CREATED FIRST, WHICH IS #253's FINDING 6 OBEYED.** #253
  banner demoted to ★ PRIOR; **#252 banner, 12 ln → `_GM-ARCHIVE.md` § Batch 2026-09-07 #254**,
  through the mover.
- **2d** — new ⏱ LATEST DELTA #254 (16 ln); #253 demoted; **#251 delta, 16 ln + its 6,768-char
  `Previous:` chain segment → `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-07 #254**.
- **2e** — `## ⬛ DO THIS FIRST` measured at **70 raw lines**, `#### ` scan returns **0** dated
  strata. Nothing to roll; the size is declared rather than the skip. ⚠ It read **109** at #253 —
  a 39-line fall this wrap did not make, reported and not explained.
- **2f** — `roll_2f` split the #253 stratum: **12 ln → `notes/_GAUGE-LOG.md`** (true EOF) + **3 ln
  → `_GM-ARCHIVE.md` under the #254 key** — the key #253 could not reach. The
  `#### 2026-09-07 #254` stratum was written in its place.
- **2g** — `_build_memento_index.py` rebuilt LAST; `index_freshness_check` passes on the re-run.
- **3** — memory is the CONDUCTOR's step by the brief (`desk-cleared-254` +
  `decide-fast-dave-is-the-bottleneck-254` + their `MEMORY.md` pointers, written by him before the
  cut); **0 bytes written at this seat**, which cannot see the store (D1a).
- **4** — `s254-D1` and `s254-D2` were inscribed BY THE CONDUCTOR and **read back** here: store
  **390 → 392**, entries **391 / 392**, each parsing `by` Dave · `date` 2026-09-07 · `status` ruled.
  Nothing was re-inscribed.
- **4b** — `_gen_titles.py --session 254` run; receipt written; both lines in the stub.
- **4c** — `_gate_scratch_hygiene.py` run; see the findings for what it reported.
- **5** — `_git_commit.sh --reconciled`, no `--wrap`; push from here.

Also: **four `_state.json` rows added** (`W-474`…`W-477`) — the three #254 lane reports the lanes
filed unrowed, plus this one; `_gate_doc_rows.py --check` goes **⛔ FAIL (3 unrowed) → ✅ PASS**.

## Findings

1. **The session named for STRIKING ended by KEEPING all eighteen, and the record had to tell the
   difference between a veto unexercised and a veto exercised as "keep".** #253 logged the calls
   UNVETOED after *"all your pal"*, correctly — that answer engaged with nothing. `s254-D1`'s *"keep
   all and build the page please"* engages with all eighteen, so the carry is **STRUCK with its
   `s188-D2` receipt, not aged**. An aged carry reads "still open"; this one is closed, and what
   replaces it is a different claim (his READ of the context page at #255, at age 0).
2. **"Get this off our desk" is a batch authorisation, and the lane's honesty is visible in what it
   did NOT sweep.** Five items built, **two DATED**. An enactment that swept all seven would have
   used the same sentence as cover for the two that needed a date — item 4's `intent` collapse
   changes 15 metas and a gate, item 6 (R1) is a design question nobody has written down.
3. **The 2c/2d EXIT CHECK found the item #253 predicted it would, and repaired it before the roll.**
   #251's *INVENTED FACT `40% drawn`* lesson — *say "use canon's own figures" in every build brief* —
   probed **ABSENT** from every standing surface. It is now inscribed in
   `knowledge/_RUNBOOK-parallel-conductor.md` § Guardrails, with the *"never `git checkout` a shared
   directory"* clause from #253's carry ⑤ beside it, **before** the #251 delta moved. ★ This only
   worked because #253 wrote the prediction down; the lesson was one wrap from a dated home.
4. **A wrap falsified its predecessor's own banner, and the class is the expensive one.**
   `ls knowledge/_probe/` returns `session-247.json` as its newest file; there is **no
   `session-253.json`**. The #253 banner's *"Recall probe PLANTED (`--plant --session 253`), breaking
   a five-session declared streak"* is FALSE. The streak runs **#248…#254 — seven sessions, 0 plants,
   0 claims**, and **#254 planted nothing either**: planting one now to make the sentence true would
   be the worse act. ★ The record's most-trusted surface is where an unprobed claim is cheapest to
   make.
5. **The first over-advisory cut in five, and the cause is a route the conductor cannot delegate.**
   176,625 declared at the cut (25,696 past the 150,929 advisory), 180,702 measured here over 84
   turns. The three lanes spent **392,589 harness tokens outside his window**; the **nine-chunk
   `_build_all.py` ran inside it**, because the host caps one bash call at ~170 s. ⇒ **a verification
   route can cost as much as the work it verifies, and it is the one cost a conductor pays
   personally** — the #252 route-price finding in a third costume.
6. **The boot series is under the ceiling for a fifth time AND monotonically rising, which is
   reported rather than acted on.** 69,293 → 69,321 → 69,526 → 69,664 → **69,797**: +504 across five
   readings, 203 under the 70,000 literal. `s240-D2`/`s241-D1` make the ceiling shrink-only, so a
   rising-but-under series is exactly the shape that precedes a sixth breach. ⛔ Nothing was done
   about it here — what to do is Dave's.
7. **The runner's first full-coverage run since #253 surfaced only debt from three sessions back.**
   144/144, nine chunks. Five reds, **none this wave's**: 4px `gap: 6px` ×2 (bento snippet and
   `canon.css`), `--bento-cols-now: 4`, `--layout-bento-caption-space` and
   `--layout-bento-packing` — #248–#251 bento debt visible only because `canon.css` finally rebuilt
   from #251's DP-08 snippet — plus the v1.0.6 zip (#245). ★ **A partial runner is not a green
   runner.**

## The carry transform

**449 aged · 6 `[NEW — 0]`→`[1]` · 2 STRUCK · 5 CORRECTED · 6 new · probeable count 314.** The line
was **never re-typed and `_CARRIES.md` was never read whole** (6,899,772 bytes): one programmatic
pass, `knowledge/_tmp/wrap254/carry255.py`, bumped every age bracket and applied seven surgical
edits, each asserted unique before it was made.

- **STRUCK ①** the eighteen calls — receipt `s254-D1` (entry 391), the veto EXERCISED.
- **HALF-STRUCK ③** the step-18 red — receipt lane R / `dbf2780`; ⛔ **the abort-strip hazard is NOT
  discharged and is re-carried as ⑥ at age 0**, because the symptom that exposed it is gone and the
  hazard is not.
- **CORRECTED ④** lane C's Q1 **answered** (option (c) taken — the illustrative ladder this carry
  described no longer exists) while Q3 is **DATED, not answered**.
- **CORRECTED ⑦** the ceiling arm (boot 69,797, fifth under; the carry's own *"≈#255"* estimate is
  now one session from its test) · **⑧** the CI read-back (backlog **eleven**, none read at any #254
  seat) · **⑨** the recall probe (the retraction in finding 4) · **⑩** the conductor's fill.

Every correction keeps its original wording verbatim after the receipt.

## RULING-SHAPED QUESTIONS

1. **The boot series is rising under a shrink-only ceiling.** Five readings, +504, 203 of headroom
   left. Nothing in the repo fails, and the arm is already red on #247/#248 for an unrelated reason
   — which is why the trend is invisible to every gate. Whether a rising-under-ceiling series should
   itself raise something is Dave's; it is a definition, not a build.
2. **Item 4's date has no owner in the wave that follows it.** `s254-D2` says `intent` is removed
   after the SH demo with a verifier in that wave. Nothing schedules that wave, and until it lands
   two homes hold one fact on 15 metas — an ADR-0017 breach that is now *scheduled* rather than
   unnoticed. Who carries the date is the question.
3. **Where does wrap and lane scratch live?** Unchanged from #253's question 3 and re-raised because
   this wrap's own scripts sit under the gitignored `knowledge/_tmp/wrap254/`. `s218-D7`'s *"evidence
   beside the report, never session scratch"* is still unsatisfiable there.

## UNPROVEN / CLAIMED (ADR-0016)

- **CLAIMED:** the conductor's **brief-cut 176,625** is his DECLARED figure. ✅ Unlike #253, this seat
  took a first-hand `_checkin.py` reading of his transcript — **180,702 real over 84 turns** — so the
  `s214-D5` line records `brief-cut`, `sub-cut` and `delta` as MEASURED and only `replay` as
  unobservable.
- **CLAIMED:** the **subs figures** (S 176,872 · R 107,819 · V-S 107,898) are the conductor's reading
  of the harness's `subagent_tokens`, **not** `message.usage`, and are recorded as QUOTA. This seat
  did not observe them and cannot.
- **CLAIMED:** the **144/144 runner coverage and the five reds** are the conductor's chunked run,
  quoted and not re-driven at this seat (a re-drive is ~170 s × 9, and any abort after step 1 strips
  41 compliance files).
- **UNPROVEN:** that the ceiling arm clears at ≈#255. This wrap adds a fifth under-ceiling reading
  and no evidence about window membership; price to prove: one re-run of `boot_constant_drift_check`
  at #255.
- **UNPROVEN, and it is `s254-D1`'s own state:** **`reviews/META-TAGS-CALLS-CONTEXT-2026-09-07-v1.html`
  has never been rendered in a browser by anybody.** Every claim about it is a claim about its source
  text.

## Evidence

No evidence directory: every claim above quotes its probe inline (the mover's receipts,
`_roll_state.py`'s generated line, `_gm_usage.py --sizes --session 254`, `_gate_doc_rows.py --check`
before and after, `_gen_chain.py`'s generated footer, `ls knowledge/_probe/`). ⛔ **This wrap's own
scripts are session-owned and uniquely named under `knowledge/_tmp/wrap254/` —
`carry255.py` · `ops-254-headers.json` · `ops-254-rolls.json` · `new254.txt` · `delta254.md` ·
`banner254.md` · `stratum254.md`, each asserted to exist with a size floor in the writing process
before the mover read it (the #166 stale-msgfile lesson) — and that directory is GITIGNORED, so this
evidence is LOCAL ONLY.** That is #253's finding 8, unrepaired, and it applies to this report's own
receipts.

REPLAY-THESE: `_CARRIES.md § residual → #255` first six items (~1,900 tk — the bento debt, the
`intent` collapse, Q2–Q6, the #255 read, the bottleneck instruction, the abort hazard) ·
`notes/_subreports/2026-09-07-254-BUILD-S.md` § *Questions this lane could not settle* (~600 tk —
Q1–Q6, of which Q1 is answered and five are carried)
