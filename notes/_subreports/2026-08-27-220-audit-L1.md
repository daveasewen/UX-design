# `#220`-L1 — GATE FLEET (correctness): the fleet enumerated, driven, and mutation-probed

session: `#220` · 2026-08-27
window: audit wave, lane L1 (GATE FLEET — correctness)
sub index: `L1`
brief: `notes/_briefs/2026-08-27-220-audit-wave-divvy.md` §L1 + SHARED RULES
tokens: `UNMEASURED — a sub cannot read its own `message.usage` from inside this seat.`
region: this report + `notes/_subreports/assets/2026-08-27-220-audit-L1/` only. Nothing else in
the tree was written. No git, no `_rulings.json`, no `_state.json`, no memory, no promotions,
no repairs. `_build_all.py` NOT run. `_capture_gate.py --selftest` NOT run (brief fence) —
it is the only fleet member in this lane's scope that was deliberately left undriven.

## VERDICT

The fleet was enumerated from all four consumer surfaces (not one), driven both in the working
tree and in a bare clone at `707e5aa` — the #194 single-variable method — and probed with
planted defects. **The `gates` job of `.github/workflows/gates.yml` is red at its FIRST step and
has been since `aa26947` (2026-08-26), which means the Knowledge build, the 27 gate bite-tests,
the 19 advisory bite-tests, the commit-script selftest and the claim-table linter have not run in
CI for two sessions.** Four survey steps fail on the committed tree, all four ABORT-routed. Eight
false greens were established, four of them by planted-defect mutation with a firing control
beside each: two BLOCKING gates are blind to CSS logical properties, one of them is blind to
`style=""` attributes entirely, `_validate_wiring.py` is green because its glob cannot see the
`_gate_*.py` namespace, and five committed gate artefacts are stale at HEAD with nothing in the
repo comparing them. The fleet's static leg has no HTML or CSS parser anywhere in it. Region
DONE; the one declared gap is `_capture_gate.py --selftest`, fenced by the brief.

COUNTS: findings `18` · ruling-shaped `5` · UNPROVEN `4`

## What was done

1. **Enumerated** the fleet from four surfaces, not the one the brief names:
   `.github/workflows/gates.yml` (3 jobs, 21 named steps) · `knowledge/_build_all.py` `STEPS`
   (140 steps over 92 distinct scripts, read by `ast`, never imported) ·
   `knowledge/_git_commit.sh` (6 gate invocations at the commit seam) ·
   `knowledge/_tests/test_gates.py` + `test_advisory.py` + `_probe_registry/_registry.py`.
   On disk: 43 `_validate_*.py` · 5 `_gate_*.py` · 3 `_release/_gate_*.py` · 31
   `_render/verify_*.py` · 8 `_probe_registry/probe_*.py` · 2 `_tests/test_*.py`.
2. **Drove** `_build_survey.py` over all 140 steps in `--range` chunks, twice — working tree and
   bare clone — skipping only step 13 (`_capture_gate.py --selftest`, brief fence).
3. **Drove individually** the 50 fleet steps `_build_survey.py` LISTS but never runs, in the bare
   clone so the live tree was not dirtied — which is also how the writer-gates of finding 7 were
   caught.
4. **Drove** `test_gates.py` (27 bites, 0 fail, 13.5s), `test_advisory.py` (19 cases, 0 fail),
   the three release gates, `_gate_doc_rows.py`, `_gate_pack_imports.py` (selftest + against both
   shipped zips), and the eight `--selftest` arms nothing consumes.
5. **Mutation-probed** two BLOCKING gates with planted defects and firing controls, tree restored
   and verified clean between every probe.
6. **Classified** every fleet member: what it reads and in whose grammar; whether a known-answer
   test exists AND is run; whether it has a named consumer; blocking vs advisory vs refusal; and
   whether its own words agree with its wiring.

Full machine-readable fleet table: `notes/_subreports/assets/2026-08-27-220-audit-L1/fleet-table.tsv`
(140 rows: step · script · argv · route · surveyed? · driven rc · driven seconds · how it reads
the artefact).

## The fleet, in numbers

| question | answer | probe |
|---|---|---|
| steps wired in `_build_all.STEPS` | **140** over 92 distinct scripts | `ast` walk of `STEPS` |
| routed ABORT / GATE / ADVISORY | **56 / 65 / 19** | `ast` walk of `ROUTE_ROWS` |
| steps with no route row, or rows with no step | **0 / 0** | same walk — the #164 seam is closed |
| steps `_build_survey.py` actually asks | **64** (argv all in `{--selftest,--check}`) | survey's own `NON_MUTATING` |
| steps it LISTS and does not run | **76**, of which 54 are `_validate_*`/`_gate_*` | survey output, all five ranges |
| survey verdict, working tree | 57 pass · **4 FAIL** · 2 COULD-NOT-ASK | `assets/survey-working-tree.txt` |
| survey verdict, bare clone at `707e5aa` (= CI) | 54 pass · **4 FAIL** · 5 COULD-NOT-ASK | `assets/survey-bare-clone.txt` |
| scripts with a `--selftest` step wired | **49 of 92** | `STEPS` walk |
| scripts shipping a `--selftest` nothing runs | **8** | driven, all 8 — finding 9 |
| BLOCKING-routed scripts with no known-answer test that RUNS | **26** | finding 10 |
| blocking gates over HTML/CSS with no parser at all | **8** | finding 16 |
| false greens established | **8** (4 mutation-proven, 4 measured) | findings 5, 7, 8, 11, 12 |

⚠ Runtimes are recorded as DATA for L6 and are not analysed here: bare-clone survey 73.6s total
across the three chunks (28.0 + 6.4 + 39.2); working-tree survey 116s (the `real`-tier token path
costs the difference); `test_gates.py` 13.5s; the 50 individually-driven steps 18.4s in aggregate,
of which `_validate_evidence.py` 5.92s, `_validate_descender_clip.py` 2.37s,
`_validate_hidden_display.py` 1.43s and every other step under 1.3s.

## Findings

**1 — The `gates` job is red at its FIRST step, and five later steps have not run in CI for two
sessions.** `_build_survey.py` exits 1 on the committed tree. `gates.yml:240-269` runs it as the
first executable step with no `continue-on-error`, so "Knowledge build", "Gate self-tests" (the
27 bites), "Advisory bite-tests" (19), "Commit-script selftest" and "Claim-table evidence linter"
are all queued behind it. The oldest of the four failures dates to `aa26947` (2026-08-26,
`git log -S`). This is `#193`'s own lesson recurring at the job level — *"a queue of unknown
length behind one refusal"* — except these are failures, not refusals.
⚠ **CLAIMED, not read:** the GitHub Actions run conclusion was not fetched. The claim above is a
mechanical consequence of the workflow file plus the exit codes measured here and in the clone.

**2 — Why step [18] is red: two instruments disagree about the same artefact's grammar, and the
loser is ABORT-routed.** `python3 knowledge/_gm_usage.py --selftest` → exit 1, in BOTH the working
tree and the bare clone:

```
✗ real-repo usage history reads clean (got: ['session #218 testifies DIFFERENTLY in
  notes/_GAUGE-LOG.md and notes/_GAUGE-LOG.md — one of them is false and this reader
  cannot tell which. REFUSED.'])
✗ real-repo report closes in exactly ONE of its two legal states, never a remedy choice
```

The two testimonies are real and both are legal: `notes/_GAUGE-LOG.md:2276`
`**section-usage #218 (self-report, delegated OPUS wrap sub):**` and `:2294`
`**section-usage #218 (second wrap; self-report, delegated OPUS wrap sub):**`. The second exists
*because* `_gm_move.py::roll_2f` and `_capture_gate.py` enforce ONE BLOCK PER SESSION, so #218's
second wrap was appended into the #218 block — the ruled, machinery-enforced shape. `_gm_usage.py`
enforces ONE TESTIMONY PER SESSION. **Neither contract is wrong; they are incompatible, and the
one that loses is routed ABORT.** Two sub-defects ride along: the message names the same file
twice (`_gm_usage.py:486` interpolates `{seen_src[n]} and {rel}`, both the same relpath) and
carries **no line numbers**, so a reader cannot locate the conflict; and the tool calls itself
`REFUSED` in prose while exiting **1**, not the ruled `77` + `COULD-NOT-ASK:` — the exact shape
`#193` built `_could_not_ask.py` to end.

**3 — `_build_memento_index.py --check` is STALE on the committed tree.** `exit 1`,
`memento index --check: STALE — regenerate (the index on disk does not match the corpus; never
hand-edit it)`. Reproduced in the bare clone, so it is not my untracked brief.
`knowledge/_memento-index.json` was last committed **today** (`3f7a63a`), so it went stale within
#220's own commits. Routed ABORT. ⚠ Related class worth naming: the corpus globs
`notes/_briefs/*.md` (`_build_memento_index.py:333`) against the **working tree**, so any
untracked brief — an audit-wave brief, for instance — turns this ABORT-routed step red locally
while CI stays clean.

**4 — `_gen_chain.py --selftest` fails one bite at BOTH measurement tiers.**
Working tree (`real`): `✗ is materially smaller than GOOD-MORNING.md (47,915 vs 94,376 real,
50.8% of it, floor <40%; tier measured, not assumed: real)`. Bare clone (estimate tier):
`(30,808 vs 59,640 tape (cl100k ESTIMATE), 51.7% of it, floor <40%)`. Same verdict on both tiers,
so this is an ARTEFACT fact, not the #173 environment class: `_CHAIN.md` (107,648 bytes) has grown
past 40% of `GOOD-MORNING.md` (203,033 bytes). Routed ABORT. ⬛ The floor is a constant and moving
it is Dave's; the alternative — cutting the chain — is also his.

**5 — FALSE GREEN BY REFUSAL: `_gen_schematic.py --check` refuses in CI for a token reason while
the artefact is stale for a COUNTING reason.** Bare clone: exit **77**,
`COULD-NOT-ASK: … The committed page carries figures in ['real']; the only tier reachable in THIS
environment is 'tape (cl100k ESTIMATE)' … this artefact's content is UNKNOWN here`. Working tree
(where `real` IS reachable): exit **1**, STALE. The refusal is keyed at the WHOLE-ARTEFACT level,
but the drift is overwhelmingly in figures that have nothing to do with tokens — re-rendered and
diffed field by field:

| `reviews/MEMENTO-SCHEMATIC-2026-08-07-v2.html` | committed | fresh at this commit |
|---|---|---|
| `⑤ GATES` | 128 steps · 42 validators | **140 steps · 43 validators** |
| `② STORE` | 246 rulings · 139 sessions | **262 rulings · 155 sessions** |
| tests of the tests | 44 `--selftest` · 13 `--check` | **49 `--selftest` · 16 `--check`** |
| `_graph-mark-observations.jsonl` | 1,813 observations over 18 dates | **1,913 over 20 dates** |
| validators on disk | 42 | **43** |

Last regenerated `adb5130` (2026-08-24). CI has never been able to see any of it, because a
tier-keyed refusal blinds the gate to every input that is not the tier. **The published diagram a
reader consults for "how many gates are there" says 128/42; the answer is 140/43.**

**6 — The survey asks 64 of 140 steps, and its fail-safe is CORRECT — which is itself the
finding.** `_build_survey.py`'s `NON_MUTATING = {"--selftest", "--check"}` means every argv-less
validator is treated as mutating and LISTED, not run — 76 steps, 54 of them gates. The instinct
to widen the set is wrong, and finding 7 is the proof: five of those argv-less "validators" write
a repo file every time they run. The correct fix is a per-step declaration (see fix list), never a
blanket widening. Recorded here so the next lane does not "simplify" it.

**7 — FALSE GREEN, MEASURED: five committed gate artefacts are stale at HEAD and nothing in the
repo compares them.** Method: pristine bare clone, one gate at a time, `git checkout -- .`
between each (0 dirty verified before every run).

| gate | artefact it rewrites | committed → fresh at the SAME commit |
|---|---|---|
| `_validate_a11y.py` | `knowledge/_A11Y-GATE.md` | 654 → **664** notes; 1300 → **1310** controls |
| `_validate_advisory.py` | `knowledge/_ADVISORY-SIGNALS.md` | 123 → **130** signals |
| `_validate_theme_provenance.py` | `knowledge/_THEME-PROVENANCE-GATE.md` | 26 rows changed |
| `_build_integrity.py` | `knowledge/_INTEGRITY-REPORT.md` | 19 → **21** warnings; 1030 → **1035** leaf tokens; 41 → **42** groups |
| `_build_instrument_fit.py` | `_INSTRUMENT-FIT.md` + `_instrument-fit.json` | adds the missing `_validate_hidden_display.py` row |

Discrimination control, same method: `_validate_dark_surfaces.py`, `_validate_snippets.py`,
`_validate_token_tiers.py` and `_validate_edge_extremity.py` each produced **byte-identical**
output and left the clone clean — so the method separates stale from fresh. **Why nothing catches
it:** these five have no `--check` arm, so the survey cannot ask them; `_build_all.py` regenerates
them and then never compares; and `gates.yml` has no `git diff --exit-code` after the build, so CI
writes fresh copies and throws them away. The 16 generators that DO carry `--check` are asked; the
validator-writers are not.

**8 — FALSE GREEN, MEASURED: `_validate_wiring.py` is green because its glob cannot see the
namespace where the orphans are.** `python3 knowledge/_validate_wiring.py` →
`wiring gate: 43 validator(s) on disk · 42 wired · 1 exempt by name · 0 failure(s)`, rc=0. Its
population is `_validate_wiring.py:90`: `disk = [p.name for p in HERE.glob("_validate_*.py")]`.
Five `_gate_*.py` files exist in `knowledge/`; two are wired (`_gate_dataviz_vars.py`,
`_gate_minted_consumption.py`) and three are **outside the gate's population entirely**:
`_gate_doc_rows.py`, `_gate_harness_stubs.py`, `_gate_pack_imports.py`. [[gate-glob-scope-rule]] —
the rule is only as wide as its glob, and the class this gate was built for (#118: *"building an
instrument is gated; WIRING it is not"*) is unpoliced in the newer namespace. Two of the three do
have consumers (finding 15 and 17); the third does not.

**9 — Eight fleet members ship a `--selftest` that NOTHING RUNS.** Driven, all eight, in the bare
clone: `_validate_radius.py` (`selftest OK`) · `_validate_grid.py` · `_validate_evidence.py` ·
`_validate_descender_clip.py` · `_validate_edge_extremity.py` · `_validate_standing_instructions.py`
(`selftest OK — discovers 28 standing docs by glob`) · `_probe_registry/_registry.py` — all rc=0,
each under a second; `_validate_hit_area.py --selftest` is the browser case (finding 14). The bite
exists, is green, costs nothing, and no consumer asks it: [[instrument-without-a-consumer]] in its
purest form. `_build_instrument_fit.py` does NOT cover this — it grades design RULES against gates,
not gates against their own bites, so nothing in the tree can see this class.

**10 — 26 BLOCKING-routed scripts have no known-answer test that is actually run** — no wired
`--selftest`, no arm in `test_gates.py`, no case in `test_advisory.py`. Among them:
`_validate_no_hardcode.py`, `_validate_radius.py`, `_validate_grid.py`, `_validate_css_governed.py`,
`_validate_proforma.py`, `_validate_legacy_leak.py`, `_validate_token_tiers.py`,
`_validate_type_blast_radius.py`, `_validate_dtcg.py`, `_validate_binds_ratchet.py`,
`_build_integrity.py`, `gen_dashboard.py`, `guidelines/gen_rules_index.py`. The two I probed
(findings 11 and 12) are both on this list; both turned out to be blind. Full list in the fleet
table.

**11 — MUTATION-PROVEN FALSE GREEN: `_validate_radius.py` (BLOCKING) is blind to CSS logical
radius properties.** In a bare clone, planted in `knowledge/canon/canon.css`, a STRICT surface:

| probe | planted | verdict |
|---|---|---|
| A1 CONTROL | `.l1probe{border-radius:8px}` | `1 strict fail(s)` · `X canon/canon.css: border-radius:8px` · **EXIT=1** |
| A2 MUTANT | `.l1probe{border-start-start-radius:8px}` | `0 strict fail(s)` · **EXIT=0** |
| A3 MUTANT | `.l1probe{border-end-end-radius:12px}` | `0 strict fail(s)` · **EXIT=0** |

Cause, `_validate_radius.py:152`:
`DECL_RE = re.compile(r'border-radius\s*:\s*([^;}]+)')` — the literal string only. ADR-0010 says
radius is a theme flex slot; a logical-property radius is exactly as un-themeable as the one the
gate catches, and it walks straight through.

**12 — MUTATION-PROVEN FALSE GREEN: `_validate_no_hardcode.py` (BLOCKING, DEF-004) is blind to
logical border widths, and to `style=""` attributes entirely.** Planted in
`knowledge/_proforma/Masthead-interactive.html`:

| probe | planted | verdict |
|---|---|---|
| B1 CONTROL | `.l1probe{border-width:3px}` in `<style>` | `❌ No-hardcode gate FAILED` · **EXIT=1** |
| B2 MUTANT | `.l1probe{border-inline-width:3px}` | **EXIT=0** |
| B3 MUTANT | `.l1probe{border-block-start-width:3px}` | **EXIT=0** |
| B4 MUTANT | `<div style="padding:13px;border-radius:7px;border-width:5px">` after `<body>` | **EXIT=0** |

Causes: `_validate_no_hardcode.py:27`
`BORDERW = r'(?:border(?:-(?:top|right|bottom|left|width))?|outline(?:-width)?)'` — the `SPACING`
pattern one line above DOES carry `inline`/`block` longhands, so the omission is in `BORDERW`
alone; and `:35` `def styles(html): return "\n".join(re.findall(r"<style[^>]*>(.*?)</style>", ...))`
— **only `<style>` blocks are read**, so three hardcoded values in one `style=""` attribute pass
green. DEF-004's stated purpose is *"styling must be a token so MODES can override it"*, and a
style attribute is the least overridable place in CSS. `_validate_compose.py:57` already reads
`style="..."` attributes, so the extraction the fix needs is already written in the tree.

**13 — A stale literal inside a gate's own report makes it publish a self-contradicting line.**
`knowledge/_BEHAVIOUR-GATE.md` reads
`- **dataviz — page budget:** 34611 bytes (33.8 KB of 32 KB, 99%) across 3 source(s)` followed by
`## ✓ PASS`. 33.8 KB is not 99% of 32 KB. The constant is right and the prose is stale:
`_validate_behaviour.py:43` `PAGE_BYTES = 34 * 1024  # ⚠ RE-DIALLED 32→34KB by DAVE, #96` — but
three strings still type the old number by hand (`:137` contract line, `:144` report line, `:216`
console line). The percentage is computed against 34 KB and is correct; the denominator beside it
is typed and is wrong. [[banner-figures-are-parsed-not-prose]]. ⚠ Recorded as data, not analysed:
actual headroom is 205 bytes (34,611 of 34,816).

**14 — A CLASS, three instruments: browser-ABSENCE is not covered by the browser refusal, only
module-absence is.** Measured mid-lane, and the environment changed under me — **DECLARED**:
`playwright` was not importable when this lane opened (`_validate_state_contrast.py --selftest` →
**77**, the ruled refusal) and became importable partway through, installed by a sibling lane into
this shared workspace; `chromium` is still absent. With the module present and the browser absent:

| instrument | exit | what it says |
|---|---|---|
| `_validate_state_contrast.py --selftest` (**BLOCKING**) | **2** | `StateContrastSelftestError: chromium would not launch (…Executable doesn't exist at …chromium_headless_shell-1234…)` — counted as a FAIL by the survey |
| `_validate_hit_area.py --selftest` (advisory) | **1** | raw Python `Traceback` through `playwright/_impl/_browser_type.py:97` |
| `_validate_hit_area.py --all` (advisory) | **1** | playwright process log, `exitCode=127` |
| `_validate_descender_clip.py --computed` (the exempted G2 leg) | **1** | raw `playwright install` banner |

`_validate_state_contrast.py`'s own arm is named `arm_no_playwright_is_a_refusal_not_a_failure` —
scoped to the IMPORT, exactly as its name says, and the browser is a second unreachable input it
never learned to name. Today CI is safe only by accident: the `gates` job installs no playwright
(→ 77) and the `render` job installs both (→ runs). **Add `playwright` to the `gates` job's pip
line — a one-word change someone will eventually make — and step [74] turns red with a verdict
that accuses the artefact.** [[a-crash-is-not-a-fail]], [[gate-cannot-pass-in-one-environment]].

**15 — `_gate_pack_imports.py`, built today, has no consumer anywhere.** Not in
`_build_all.STEPS`, not in `gates.yml`, not in `_git_commit.sh`. Its known-answer test DOES fire —
driven against the frozen v1.0.0 zip it reports exactly the defect it was built from:
`⛔ MISSING-LOCAL memento-package/claude-plugin/memento/machinery/_gen_chain.py — wants
'_could_not_ask', which this pack CARRIES but not where this file can reach it.
ModuleNotFoundError: No module named '_could_not_ask'` (`--selftest`: `18 bites, 0 fail(s)`).
It is ADVISORY at birth by design and ⬛ promotion is Dave's — but ADVISORY and UNWIRED are
different states, and it is currently the second. Its run also surfaced the fleet fact behind
finding 7 independently: `⬛ WRITES-AT-IMPORT — 5 file(s) changed the pack just by being imported`,
naming `_validate_dark_surfaces.py`, `_validate_snippets.py` and `_validate_token_tiers.py` among
them.

**16 — The fleet's static leg contains no HTML parser and no CSS parser.** Eight BLOCKING gates
read HTML/CSS artefacts with `re` alone: `_validate_dataviz.py` (over SVG markup),
`_validate_compose.py`, `_validate_a11y.py`, `_validate_grid.py`, `_validate_radius.py`,
`_validate_css_governed.py`, `_validate_no_hardcode.py`, `_validate_property_resolves.py`. None
imports `html.parser`, `ElementTree` or any CSS parser; the CASCADE is invisible to all of them.
The gates that DO read an artefact in its consumer's grammar are the browser ones
(`_validate_state_contrast.py`, `_validate_hit_area.py`, `_validate_descender_computed.py`,
`_validate_screen.py --render`), the YAML one (`_release/_gate_ci_template.py`), the AST ones
(`_capture_gate.py`, `_validate_package_delta.py`'s shim arm) and the import one
(`_gate_pack_imports.py`). Findings 11 and 12 are two instances; the class is larger than the two
I had budget to probe.

**17 — `_gate_doc_rows.py` is BLOCKING, and only at the commit seam.** It is invoked at
`_git_commit.sh:282` and `:604` and nowhere else — not in `STEPS`, not in CI. Green today
(`doc-row gate: population 114 · unrowed 0 · ✅ PASS`). Since `s133-D2` makes `_git_commit.sh` the
only push path this is defensible, but it means CI cannot see the forgotten-document class (#185)
at all, and a commit made any other way skips it silently. Same shape for the other five commit-seam
gates: `_gen_chain.py --check`, `gen_showroom.py --check` (with a `SHOWROOM_ACK` prose escape
hatch), `_build_graph_mention_map.py --check`, `_capture_gate.py --wrap`,
`_build_live_state.py --selftest`.

**18 — Two release-side facts, both healthy, recorded so nobody re-derives them.**
`_release/_gate_release_audit.py --pack` no longer rests on a refusal: `SKIPPED — …v1.0.0.zip is
FROZEN HISTORY (not the manifest's v1.0.1)…` then `PASS — apollo-spider/dist/Apollo-Spider-v1.0.1.zip
matches the manifest at 3f7a63a39e86`, exit 0. And `_validate_package_delta.py` is **green** on the
committed tree (1.19s, exit 0) — the `gates.yml` header's standing note that the package delta step
*"was red on this commit"* and is ⬛ Dave-owned is **stale**; the premise has aged
[[premise-ages-faster-than-rule]]. Its VERBATIM_SET arm is still a byte comparison
(`_validate_package_delta.py:85`), which is the brief's live lesson: it was green over a file that
could not import, and only `_gate_pack_imports.py` — unwired — asks the runnability question.

## The top 10 priced correctness fixes

Ordered by (blast radius × how cheap the proof is). Every price is edit + its own bite + one
drive. None of these were made — this lane repairs nothing.

| # | fix | why it is the top of the list | price |
|---:|---|---|---|
| 1 | Teach `_gm_usage.py` that ONE SESSION MAY CARRY TWO TESTIMONIES when its block carries two wrap events — the shape `_gm_move.py::roll_2f` forces — and make its refusal exit 77 with line numbers, not exit 1 with the same path twice | unblocks the whole `gates` job; five CI steps have not run for two sessions | ~4K, `_gm_usage.py:486` + one bite both directions |
| 2 | Regenerate `knowledge/_memento-index.json` and `reviews/MEMENTO-SCHEMATIC-…-v2.html` at the seam that made them stale | two ABORT-routed steps; the schematic is also a page people read | ~1K, two generator runs + `--check` read-back (⬛ L2's region, named here) |
| 3 | Split `_gen_schematic.py --check`'s refusal: compare the tier-INDEPENDENT figures always, refuse only on the token figures | a refusal keyed on one input is currently blinding the gate to every other input; CI has never seen finding 5 | ~6K + a bite that plants a count drift and proves it still fires under a refusing tier |
| 4 | `BORDERW` gains the logical longhands, and `styles()` also reads `style=""` attributes (copy `_validate_compose.py:57`) | BLOCKING DEF-004 gate, mutation-proven blind in three shapes | ~3K + the three mutants of finding 12 as wired bites |
| 5 | `DECL_RE` gains the logical radius longhands | BLOCKING ADR-0010 gate, mutation-proven blind; its selftest already exists and is 7 bites long | ~2K + two bites; wire the selftest at the same time (fix 6) |
| 6 | Wire the 8 orphan `--selftest`s into `STEPS` + `ROUTE_ROWS` | all 8 green, all sub-second, all currently unasked | ~3K, 16 lines; `_validate_hit_area.py`'s needs fix 8 first |
| 7 | Give the five writer-gates of finding 7 a `--check` arm and wire it, OR add one advisory `git diff --exit-code` step after "Knowledge build" | five committed artefacts are stale at HEAD and no instrument in the repo can say so | ~8K for five `--check` arms; ~1K for the single CI step (⬛ blocking-vs-advisory is Dave's) |
| 8 | One shared `browser_reachable()` in `_could_not_ask.py`, consumed by all four browser instruments — refuse 77 on browser-absence exactly as on module-absence | three instruments crash today; one is BLOCKING and one pip-line change away from reddening CI | ~5K + a bite per consumer, driven with the browser removed |
| 9 | Widen `_validate_wiring.py`'s glob to `_validate_*.py` **and** `_gate_*.py`, and let an exemption name an indirect consumer (`_gate_harness_stubs.py` is an arm; `_gate_doc_rows.py` is commit-seam) | the anti-orphan gate cannot see the namespace where today's orphans live | ~3K, one glob + one exemption clause + two bites |
| 10 | Interpolate `PAGE_BYTES` into the three typed "32 KB" strings in `_validate_behaviour.py` | a published gate report currently contradicts itself in print | ~600 tokens, 3 lines |

Runner-up, not in the ten because it is a build not a repair: an HTML/CSS parsing leg for the
eight regex gates of finding 16 (~40K+, and it would want Dave's word on whether a parser
dependency may enter the stdlib-only gate set).

## RULING-SHAPED QUESTIONS

1. **The `#218` two-wrap collision (finding 2) — which contract yields?** (a) `_gm_usage.py`
   learns that one block may carry two testimonies and grades the LAST one, with the earlier one
   kept as history; (b) the gauge log grows a second legal block form for a second wrap event and
   `_gm_move.py`'s one-block-per-session refusal is relaxed; (c) leave both and let step [18] stay
   red. **Recommend (a)** — it is the smaller change, it keeps the ratified record untouched
   [[feedback-header-wins-over-audit]], and (b) rewrites machinery that has been correct twice.
2. **`_CHAIN.md` is 50.8% of `GOOD-MORNING.md` against a `<40%` floor (finding 4).** (a) cut the
   chain; (b) re-dial the floor with the reason recorded; (c) declare the arm advisory. ⬛ The
   constant is yours; no option here is cheap, and (c) removes an instrument rather than fixing a
   fact.
3. **Should the five writer-gates' artefacts be GATED for staleness (fix 7), and blocking or
   advisory at birth?** Advisory at birth matches the house pattern (`_gate_minted_consumption.py`,
   `_validate_hidden_display.py`); blocking matches the 16 generator `--check`s that already gate
   determinism. **Recommend advisory at birth**, promoted once it has been green for a session.
4. **`_gate_pack_imports.py` (finding 15) — wire it ADVISORY into `STEPS` now, or leave it
   unwired until you rule on promotion?** ADVISORY-and-wired is a different state from
   ADVISORY-and-unwired, and only the first can ever tell you anything. **Recommend wiring it
   advisory**; promotion to blocking stays a separate word.
5. **Do the six commit-seam gates (finding 17) belong in CI as well?** Duplicating them costs a
   little runtime and buys a second reader that does not depend on which script made the commit.
   ⬛ Your call; I have not priced it because the answer changes what to build.

## UNPROVEN / CLAIMED (ADR-0016)

- **CLAIMED:** that the `gates` job's later steps have not run in CI since `aa26947`. The
  workflow file and the local/clone exit codes were read; the **GitHub Actions run conclusion was
  not fetched**. Price to prove: one Actions API read of the runs on `707e5aa`, `b6f5ad0`,
  `aa26947` (~2K, needs network).
- **UNPROVEN:** the full extent of finding 16. Two of the eight regex gates were mutation-probed;
  the other six were classified by reading their imports and patterns, not by planting defects.
  Price to prove: ~2K per gate per mutant, ~15K for a control-plus-mutant pass over all six.
- **UNPROVEN:** `_capture_gate.py --selftest` (step 13) was NOT driven — brief fence, declared.
  It is the only step in the fleet this lane did not ask. Its route is ABORT and `_governs.py
  --selftest`, the arm behind its #208 repair, was driven and is green in both environments.
- **UNPROVEN:** the 31 `_render/verify_*.py` one-off verifiers were enumerated and classified as
  by-design-unwired moment instruments, but none was driven — all need a staged browser this box
  does not have. Price to prove: the render-verify runbook staging plus ~1–2 min each.
- **DECLARED ENVIRONMENT CHANGE:** `playwright` became importable in this shared workspace partway
  through the lane (a sibling lane installed it). Every verdict above is stamped with which state
  it was taken in; finding 14 exists *because* the change happened, and the pre-change readings
  (`77` refusals) are in `assets/survey-working-tree.txt` while the post-change readings are in the
  finding-14 table.

## Evidence

`notes/_subreports/assets/2026-08-27-220-audit-L1/`

- `fleet-table.tsv` — 140 rows, the whole fleet: step · script · argv · route · whether
  `_build_survey.py` asks it · driven rc · driven seconds · how it reads the artefact.
- `survey-working-tree.txt` — all five `--range` survey runs on the live tree (57 pass · 4 FAIL ·
  2 COULD-NOT-ASK), step 13 excluded by the brief's fence.
- `survey-bare-clone.txt` — the same survey in `git clone --no-hardlinks . /var/tmp/l1clone` at
  `707e5aa`, i.e. what CI sees (54 pass · 4 FAIL · 5 COULD-NOT-ASK). Findings 1–4 reproduce here.
- `mutation-probes.txt` — findings 11, 12 and 7: every planted defect, its firing control, the
  exit codes, and the discrimination control that produced byte-identical output.
- `schematic-check-working-tree.txt` / `schematic-check-bare-clone.txt` — finding 5's two
  verdicts (exit 1 STALE vs exit 77 COULD-NOT-ASK) from the same commit, one variable apart.
- `gate-pack-imports-v100.txt` — finding 15's known-answer test firing on the frozen v1.0.0 zip.

REPLAY-THESE: `notes/_subreports/2026-08-27-220-audit-L1.md` findings 1, 2 and 5 (~3.5K) —
finding 1 decides whether #220 can push at all, and 5 is the shape most likely to recur elsewhere ·
`assets/mutation-probes.txt` (~1K) — the four planted defects, if any of fixes 4/5 is taken ·
`assets/fleet-table.tsv` (~4K) — only if a later lane needs the per-step routes rather than the
summary above.
