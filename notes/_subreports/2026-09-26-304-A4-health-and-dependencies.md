# #304 lane A4 — health, externalities and dependencies: what will bite a weekend of big autonomous runs

provenance: 304 · 2026-09-26
status: observed
window: lane A4, a READ-ONLY Opus 5.5 sub of the #304 conductor, working at Dave's seat through `device_bash`
tokens: `UNMEASURED — a lane cannot read its own message.usage`
COUNTS: CI failure set 6 named + help-gate 12 files · a full 146-step verdict driven at HEAD 571d458c in a disposable clone: 127 pass · 15 FAIL · 4 COULD-NOT-ASK · 0 not asked · 9 reds that CI has never shown · 17 dependency edges · 14 operating rules
CITES: s133-D2 · s137-D1 · s148-D1 · s183-D1 · s219-D4(2) · s260-D2 · s271-D1 · s272-D93 · s283-D1 · s294-D5 · s295-D3 · #74-D1 · #158 · #208 · #245-R · #250 rule 7 · Dave #301 (256,000 / 300,000)

## THE ANSWER FIRST

The weekend has three structural hazards, and the biggest one is not in the handoff. **CI has not driven the knowledge build past step 8 since 2026-09-09.** `_build_all.py` aborts at the help-gate (`[8]`, route `abort`) on every push, so the 138 steps behind it are never run in CI. I drove all 146 steps at the seat, in order, in a throw-away clone of HEAD `571d458c`, using the survey's own sanctioned mode (`--include-mutating --resume --range`, four calls). That run shows **nine more reds hidden behind step 8** that no CI log has ever printed. Two of them are also `abort` routes (`[10]` and `[13]`), so fixing the help-gate alone just moves the abort two steps along. Most of the nine are regeneration steps that the #258–#261 waves (2026-09-08/09) missed, which is the class described in the `regen-serial-set-is-ordered` memory. Five of them need Dave's word or eye before they can close.

The other two hazards are ones the record already knows. First, **the commit path allows only one writer at a time**: one index, a lock dance, a chain that goes stale on every commit, and a mention map that regenerates and then refuses once. Second, **the seat is ephemeral**: a 180 s call cap, nothing survives the end of a call, the VM itself restarts (uptime was 17 min at 14:42 UTC today), and nothing runs at all unless the Mac is awake with the desktop app linked.

Safe shape for the weekend: **lanes write, one seat commits, heavy verification runs in a clone, and the regen serial runs whole before every commit.** The rules are in § 7.

---

## 1 · CI — THE SIX, THE TWELVE, AND WHAT IS BEHIND THEM

Source of record: `notes/_lanes/303/W/_ci-gates-W.log`, run `36244018143` at `efe3bb47` (the #303 wrap), gates job FAILURE at steps 5 and 6; release ✅, render ✅ (`_ci-runs-efe3bb47.txt`). I reproduced every cause below at the seat, in a full-history clone. It was cloned `file://` from the mount, which is a read-only operation on the source; the mount repo was never written, and git on it was run only as `--no-optional-locks`. Logs are in `notes/_lanes/304/A4/`.

⚠ **The survey's one-line failure summary is misleading for `[13]`.** It prints the first `❌` in the output, and for `[13]` that line (`no \`pre-flight:\` stamp`) is FIXTURE output, a bite that fires on purpose. The real failing arm is further down. The #295 P report's guess that "the fail may be false" is right about that line and wrong about the step. The step's true cause is below.

| step (route) | real cause, measured | red since (receipt) | fix size (estimate) | whose |
|---|---|---|---|---|
| `[3]` blast-radius `--check` (abort) | committed `knowledge/tokens/_blast-radius.json` and `knowledge/_GRAPH-REPORT.md` are stale: fresh compute says 137 components / 140 tokens referenced, disk says 136 / 133 (`--check` run at the seat, read-only per `main()`) | files last committed `dbf2780f` 2026-09-07; stale since the legend meta `108493b6` 2026-09-09 (inferred: first meta added after) | one regen command + commit, minutes. Passes inside `_build_all` because `[2]` regenerates first | mechanical, regen serial |
| `[13]` capture/provenance selftest (abort) | embedded arm: `_governs.py` selftest is RED, 4 failures, which are `[136]`'s four (`capture-gate-selftest-fullclone.log:259-263`). Nothing else fails in a full-history clone | this cause since `bb016c69` 2026-09-17 (#282); step red with other causes back to at least #245 (`2026-09-03-245-R-reds.md` finding 2) | closes with `[136]` | as `[136]` |
| `[38]` component-partials sync (gate) | `Navigations.reference.html` carries a `#behaviour-manifest` block but `navigations.meta.json` has no typed behaviour: "the block is DERIVED and has no home" | block added `c0225a0e` 2026-09-09 (#261 N); contract check since `7104fc96` 2026-09-02 | ~30 min: type the meta from the block, or remove the block. Typing a behaviour contract (ADR-0015) is a judgement | Opus lane, conductor reads |
| `[125]` memento schematic `--check` (abort) | `reviews/MEMENTO-SCHEMATIC-2026-08-07-v2.html` figures are read off disk at generation time, and they move whenever counts move (rect geometry diffs at lines 108 and 113) | last regenerated `f81bbdd4` 2026-09-21; red on every read since #295 | regeneration is one command, but it goes stale again on the next commit that moves a figure. It is structural, the same class `c896bcaf` fixed for `_gen_chain.py` ("HEAD-only advance"). ~1–2 h Opus for the structural fix | Opus lane |
| `[128]` package delta-audit selftest (gate) | the two `memento-package` copies of `_gen_chain.py` (`machinery/`, `claude-plugin/memento/machinery/`) differ from `knowledge/_gen_chain.py` by 295 lines. Clone: 2 findings; CI prints "got 4" (the CI line is truncated, so the other two are unread) | since `7f8801f3` #241 2026-09-02/03 (`245-R-reds.md` finding 6: the copies are the pre-#241 file); `_gen_chain.py` has changed twice since (`f81bbdd4`, `c896bcaf`, 2026-09-21) | `cp` ×2 plus the manifest, minutes. But `245-R` put this to Dave as a ruling-shaped question ("port now or hold for the cut") and it is still open | ⬛ Dave |
| `[136]` governs matcher selftest (gate) | rulings `s282-D2`, `-D3`, `-D4`, `-D5` carry evidence with NO legal pointer (`chat #<n>` / `commit <sha>`): "prose provenance needs the legal form, never a bare sentence" (`governs-selftest.log:15-18`) | inscribed `bb016c69` 2026-09-17; the rule has existed since `3b742830` #148 | ~15 min: add `chat #282 2026-09-17` / `commit bb016c69` to four evidence strings, by textual span, with `json.load` count unchanged at 638. It touches the rulings store, so it is conductor-level and not Dave's content | conductor or Opus lane |
| step 6 help-gate `[8]` (abort) | 12 entry points have no `_helpgate` guard: `_bite_goal.py` · `_bite_kg_edge_proposal.py` · `_build_kg_explorer.py` · `_gen_ruling_edges_from_recs.py` · `_kg_history.py` · `_render/verify_demo_slides_268.py`, `_v3`, `_v5`, `_v6`, `_v7`, `_v74_gearbox`, `_v75_gearbox` | the earliest of the 12 is `9a24af9e` 2026-09-09 (#265); the rest are 2026-09-10 to 09-13 (`git log --diff-filter=A`). **17 days** | 12 files × one guard line (the `_render/` six need the walk-up form `_build_all.py:38-42` uses), ~30 min Sonnet. Six of the twelve are dead one-off verify drivers from #268, and retiring them is an option | Sonnet lane |

### THE NINE CI HAS NEVER SHOWN — the full 146-step verdict at HEAD `571d458c`

Tool: `knowledge/_build_survey.py --include-mutating --resume --range A:B --timeout 60`, in a disposable clone at `$HOME/clone304`. The docstring says this mode "should be run on a clean tree you are willing to `git checkout`". I ran four ranges in STEPS order, which the docstring says reproduces the serial build. Wall times: 70 s + 13 s + 7 s + 149 s. Verdict log: `notes/_lanes/304/A4/clone-BUILD-VERDICT-LOG.jsonl`.

`127 pass · 15 FAIL · 4 COULD-NOT-ASK ([61] dashboard provenance, [68][73][74] playwright not on the plain python path) · 0 not asked.` In-order regeneration clears `[3]` and `[125]`, and `[141]`/`[142]` pass here because pyyaml is installed at the seat. The reds **not** in CI's failure set:

| step (route) | cause, verbatim or measured | introduced (receipt) | who can close it |
|---|---|---|---|
| `[10]` assertion veracity (**abort**) | `ASSERT-009 environment count=138 (want eq 137)`: the meta count grew and the regen serial's "ASSERT-009 re-base if metas grew" step was skipped | legend meta `108493b6` 2026-09-09 (#261 L) | mechanical re-base in `knowledge/_assertions.json`, 5 min. **Must be the LAST edit before a commit if metas are added this weekend** |
| `[81]` 4px grid (gate) | 6 × `padding: 7px` / `padding:0 7px`: canon.css ×3, `Navigations`, `Sidebar-nav`, `Tab-bar` snippets (the nav count badge) | `c0225a0e` 2026-09-09 (#261 N) | ⬛ Dave's eye: 8px moves pixels; the alternative is an exemption |
| `[82]` type-binding blast radius (gate) | "`.seg button` now matches ['Tab-bar.reference.html'] — outside its acknowledged radius. Namespace it, or `--update` and review the diff" (`clone-TYPE-BLAST-GATE.md:38`) | the #261 nav family (inferred: Tab-bar changed there) | Opus, 30 min. Namespace the selector, or `--update` and review the diff (serial step "re-seed + REVIEW DIFF") |
| `[86]` token fork-ban (gate) | "4 fork(s) not in the ledger": `--status-positive`/`--status-neutral` resolve differently in `.cn-chart-bar` (canon.css:7434-5) and `.cn-chart-sparkline` (:9805-6), mono theme | the chart engine `f6f762ab` 2026-09-08 (#259) | ⬛ a dataviz judgement: declare in the fork ledger or unify |
| `[94]` roles/answers resolve (gate) | `data-grid: FAIL [with-resolves]`: six `with` entries are strings, not `{slug}` objects (s251-D6) | `f9a12bf1` 2026-09-08 (#258). The #279 wrap already reported it locally as "standing red FAIL(6)" | closes with `[132]` |
| `[127]` package delta-audit (gate) | the same two VERBATIM SET drifts as `[128]` | as `[128]` | ⬛ Dave (as `[128]`) |
| `[132]` integrity lint (gate) | `integrity: FAIL — 13 errors, 25 warnings`. **All schema errors are against `meta.schema.json`**: `relationships.composes`/`$composes-verbatim` not allowed · four `stateModel` shapes the schema does not know · `with/0..5` strings not objects · `provenance.source 'hand-authored'` not in enum · `with/0: 'when'` pattern (`clone-INTEGRITY-REPORT.md:10-28`). These are the #261 dashboard metas (data-grid, filter-toolbar, KPI tile, legend) | 2026-09-08/09 (#258–#261) | ⬛ **The schema is Dave's** (`notes/_briefs/2026-09-02-234-v106-brief.md:47`: "Schema changes … are Dave's … ratify at his eye before populate"). 2–4 h Opus plus his ratification. **On the GenUI critical path**, see § 5 |
| `[134]` `[135]` `[144]` (advisory) | probe registry; claim-table linter (6 lint + 5 rc mismatches, the same as CI's step 8); pack manifest **322 commits behind HEAD** (`08e315dc` vs `571d458c`) | — | advisory. They never fail the build. `[144]`'s re-cut is Dave's (`s219-D4(2)`) |

⇒ **For `_build_all.py` to finish in CI, three abort-route reds must go in this order:** `[8]` help-gate → `[10]` ASSERT-009 → `[13]` (= `[136]`'s four pointers). After that the build runs to the end, and **it still exits red until every gate-route red is fixed**: `[38] [81] [82] [86] [94] [127] [128] [132] [136]`. Four of those need Dave (`[81]` `[86]` `[127]/[128]` `[132]/[94]`). **Realistic by early next week: the abort chain fixed and the build running to completion in CI, reporting its full set honestly for the first time since 2026-09-09. Full green needs about five decisions from him.** For step 5 (the survey) to go green, the committed tree also needs `[3]` and `[125]` regenerated in serial order as the last act before a commit, and `[125]` needs its structural fix, or it goes red again on the next commit.

⚠ **This seat ≠ CI.** The seat is aarch64 (the render shell is `chrome-headless-shell-linux-arm64`); the CI runner is x64 Python 3.12.14. The clone verdict is a strong prediction, not CI's verdict. **The verdict of record is still CI, delivered on push.**

⚠ **GitHub Actions history was not read directly.** `api.github.com/repos/daveasewen/UX-design/actions/runs` returned 403 from the cloud container ("GitHub access to this repository is not enabled for this session"), and I did not call `add_repo`. The "red since" column is built from git history plus the CI logs committed under `notes/_lanes/297`–`303` and `knowledge/_tmp/ci296-gates.log`. All of them carry the identical six-name set and the identical `❌ step 'help-gate …' failed … aborting` line.

---

## 2 · THE BUILD VERDICT — "53 OF 146 GREEN AT `e31aa34b`" IS SUPERSEDED

The 53 figure is the seat survey of 2026-09-21 17:35–17:41: three ranges at timeouts of 20/15 s, `dirty: true`, 77 not asked (`notes/_BUILD-VERDICT-LOG.jsonl`, 3 rows). It was a non-mutating survey on the working tree, with short timeouts that turned slow steps into `errored` (`[4] [13] [103] [121] [126] [130]`).

**What it takes to get a current verdict, and what I did:** `_build_all.py` must not run partially on the mount. Its docstring lines 5–21 set a strict generator order, and #204 ran the full build by accident. A single-process run cannot fit the 180 s call. CI runs it on push, but it aborts at `[8]`, so **CI has not produced a full verdict since at least 2026-09-09**. `245-R` finding 7 shows it was already aborting at `[13]`/`[18]` on 2026-09-03. The survey's `--include-mutating --resume --range` mode, run in a disposable clone, is the sanctioned substitute: ~4 min over four calls. That is the 127/15/4 verdict above. I recommend it as the standing pre-commit probe for every weekend wave (rule 5 in § 7). Clone cost, measured: `git clone --depth 1 file://…` 25 s, `git fetch --unshallow` 48 s (full history is needed: in a shallow clone `_governs` reports 158 failures, 154 of them false "not a commit in this repo"), 1.6 GB tree, and VM disk went from 7% to 25% of 9.8 GB. The clone lives only as long as the VM does.

## 3 · THE COMMIT PATH — ONE WRITER, AND WHAT EACH COMMIT RUNS

`knowledge/_git_commit.sh` (996 lines) is the only legal door (standing instruction). Order of what one run does, from the code: the T3 msgfile-prefix gate (`:427`) → tiktoken self-heal (`:450`) → `_gen_chain.py --check` (`:472`, refuses) → `_gate_doc_rows.py` (`:481`, refuses unless `DOC_ROW_ACK`) → `gen_showroom.py --check` (`:493`, `SHOWROOM_ACK`) → `_validate_polarities.py --check` (`:518`, `POLARITY_ACK`) → **mention map: regenerates `knowledge/_graph-mention-map.json`, then REFUSES once** so you add it to the named paths (`:555-567`, `MENTION_MAP_ACK`) → `_session.py --declare $SESSION_N` (`:602-617`) → **`_capture_gate.py --wrap` runs on EVERY commit** and is visible-but-not-blocking when not `--wrap` (`:644-655`) → spine-writer selftest (`:668`) → `clear_locks` (moves every `.git/*.lock` to `_to_delete/_stale_locks/`, `:287-291`), stages NAMED paths only (`git add -A` retired, P5), commits, and clears again (`:859-996`).

- **The boot-ceiling arm:** `BOOT_CEILING_TK = 72_768` (`knowledge/_gauge_tokens.py:376`, `s295-D3`, shrink-only). Cloud boot readings are 126,178–128,437 (#297–#302, `_HANDOFF-154` 5b item 1), and #303 was 128,423. So a `--wrap` commit is always refused. **Every commit this weekend goes on the DECLARED not-a-wrap path**, including the wrap, until Dave moves the ceiling or the Mac seat lands (his #301 19:45 words, `notes/_lanes/301/WRAP-BRIEF.md`).
- **The doc-row gate:** every new report under `notes/_subreports/` needs a `_state` row. `_state.add()` refuses a row whose `home` does not exist, and minting a row stales `_CHAIN.md`. So the order is fixed: files exist → mint rows → `python3 knowledge/_gen_chain.py` → commit (`_HANDOFF-154` § COLD SEAT; the #303 wrap did exactly this and passed with 0 unrowed).
- **Stranded locks:** 156 moved locks sit in `notes/_lanes/_orphan-locks/` and 165 in `_to_delete/_stale_locks/` (counted today). No live `.git/*.lock` at 14:4x UTC. The known sources are `git status` (index refresh) and `_capture_gate.py --wrap` run outside the committer, seven wraps running (#297–#303). **The committer does its own lock clearing and left no lock in #303** (`_HANDOFF-154` 5b item 2). So stranded locks come from seats running git or the gate outside it. The fix is behavioural: no lane runs `git status`, `git reset`, or `_capture_gate.py --wrap`.
- **Serialising several lanes' output in one weekend:** `_RUNBOOK-parallel-conductor.md:12-18` says "only ONE session writes the shared state and commits it … the only git hazard is `.git/index.lock` if two commits fire at once (serialize)". Every standing brief since #230 has carried "No git (conductor commits)" (e.g. `notes/_briefs/2026-08-31-230-demo-repairs-brief.md:42`). Recommendation: **lanes never commit. One seat, the conductor or a dedicated commit seat, commits per WAVE, not per lane**, after the whole regen serial and the clone survey. Each commit costs the full gate run above, plus one refused attempt whenever the mention map is stale. One commit at the #303 wrap fit inside a single `device_bash` call with `--quiet` (`notes/_lanes/303/W/_gitcommit-W1.term`: exit 0, 435 transcript lines). I did not measure its duration.
- **Push:** plain `git push origin master`, DECLARED, after checking the branch and fast-forward (#303 5b item 4). Read CI back to completion. The render job closes about 9 min after gates (`13:11:04` → `13:20:14`, run `36244018143`). #279 wrapped with **17 unpushed commits** (`2026-09-16-279-W-wrap.md` finding 3). Push per wave so CI verdicts attach to waves.
- **⏰ Push credential:** `s294-D5` expiry is **2026-11-06**. The committer warns from 2026-10-23 (the selftest boundaries are in the CI log). Re-issuing it takes Dave's hands. Not a weekend blocker, but it belongs on the roadmap.

## 4 · SEAT EXTERNALITIES

| externality | measured / recorded | what it means for unattended runs |
|---|---|---|
| `device_bash` cap | 180 s max per call. `sandbox-call-boundary-kills` memory: nothing survives a call boundary (nohup and setsid are reaped). `_HANDOFF-154`: "kills background jobs when the call returns" | every long job is chunked (`--range`/`--resume`), foreground, with the longest timeout |
| the VM is ephemeral | `uptime` at 14:42 UTC: **up 17 min**. 4 cores, 3.8 GiB RAM, `/dev/shm` 512 MB (measured) | `$HOME` clones, `/dev/shm/render-<seat>` and pip installs vanish with the VM. Rebuild per session: `pip install tiktoken jsonschema pyyaml --break-system-packages` (tiktoken was missing at this seat today) |
| render env | `ensure_env.sh` → `ENSURE_ENV: OK … chromium_headless_shell-1243 … size=408M` in 0.2 s. `seat_env.sh` → `SEAT_ENV: OK … faces=10/404 farm=10/10`. The durable half lives in gitignored `outputs/_render-env/` on the mount (#296 fix) | `$RENDER_SHELL` only exists in the same shell that sourced `seat_env.sh`, so each render is one call: `export TMPDIR=/dev/shm; bash …ensure_env.sh; source …seat_env.sh; python3 <driver>` |
| font fallback | `fc-list | grep -ci univers` = 0 system-wide. The seat's renders fall back from Univers Next; `HSBC_MtUnivers_Latin` has to be forced in the driver (`_HANDOFF-154` § deck; `_HANDOFF-153` item 3 is open) | **no unattended lane may judge type or line-wrap from a seat render**. It forces the HSBC face and says so, or it defers to Dave's screen |
| disk | VM `/sessions` 9.8 G: 7% at open, 25% after my clone (measured). #279 read 98.2% and #283 98.6% (their wrap reports). `DISK_WARN_PCT = 90` (`_checkin.py:856`). Mount: 461 G, 34% used. `knowledge/assets` 2.7 G gitignored (photography plus a zip, which makes `test_gates.py`'s three copytrees heavy, `245-R` finding 9) | check `df -h $HOME` before any render or clone lane. One clone at a time, removed when done |
| nested `UX-design/` | an empty untracked directory (`ls -la` total 0, `du` 0) | harmless unless a lane `cd`s into it. Briefs name the repo root as `$HOME/mnt/Projects--UX-design` |
| the Mac | ⚠ NOT MEASURED in the record (no hit for caffeinate or sleep in the handoffs or runbooks). By construction, `device_bash` reaches the VM through the Claude desktop app on the Mac | **a weekend run dies if the Mac sleeps or the app quits.** Before leaving: power adapter in, sleep disabled (Energy settings, or `caffeinate -dimsu` in Terminal), desktop app open and linked. Cloud-only lanes (reading or writing nothing on the mount) are the only ones that survive a sleeping Mac, and in this repo that is almost none |
| conductor window | lines: stop 180,000 (`STOP_LINE_TK`, s260-D2/s271-D1) · tolerance 220,000 (s272-D93) · amber 160,000 · working 256,000 · hard 300,000 (`_gauge_tokens.py:88-157`, Dave #301). **#303 ran to 402,607**, 102,607 over the hard line. #283's seam check (`s283-D1`) fired at 210,476 and was obeyed (`2026-09-18-283-W-wrap.md` finding 4) | a boot of ~128K leaves only ~52K to the stop line. The conductor must stay a dispatcher: short turns, lanes in their own windows, reports read by path and headline only. Hand-sum the fill from the cloud transcript (`_checkin.py` cannot see it at the cloud seat) |
| mount I/O | `245-R` finding 3: `[13]` >120 s on the mount against 8.1 s in a clone. Today: blast-radius `--check` 16.6 s on the mount vs 11.7 s in the clone (measured) | heavy selftests and the survey belong in a clone |

## 5 · THE DEPENDENCY MAP — WHAT MUST LAND BEFORE WHAT

Each edge reads "A → B": B cannot land honestly before A. Receipts are in brackets. "⬛" marks an edge that runs through Dave's word.

**Health spine (fix first, it gates every verdict):**
1. `[8]` help-gate guards → `[10]` ASSERT-009 re-base → `[13]`/`[136]` s282 pointers → **CI's `_build_all` runs past step 13 for the first time since 2026-09-03/09** (§ 1; route table from `_build_all.ROUTES`).
2. CI build runs to the end → **a CI verdict on everything built this weekend.** Until then, any wave touching metas, snippets, canon or the graph is verified only by the clone survey (§ 2).
3. Any snippet / meta / canon-input change → **the whole regen serial, in order**: `gen_token_ramp` → `canon/gen_canon_components` → `canon/gen_theme_cascade` → `gen_showroom` → `gen_kg_edges` + `_validate_kg` → `tokens/_build_blast_radius` → `_validate_coverage` + `_build_integrity` → `_validate_icons` / `_validate_type_blast_radius` (re-seed + review diff) → ASSERT-009 re-base → mention map → `_build_memento_index` → `_gen_chain` LAST (memory `regen-serial-set-is-ordered`; its #211 recurrence was one skipped step). **Most of today's hidden reds (`[3] [10] [82] [132]`) are skipped serial steps from #258–#261.** → commit.
4. New report files → `_state` doc rows → `_gen_chain.py` regen → commit (`_HANDOFF-154` § COLD SEAT).
5. Rulings inscription (Dave's word, "inscribe") → evidence in the legal pointer form → `_governs` selftest stays green (`[136]`/`[13]`). Today's `[136]` is exactly this edge broken at #282.

**Product spine (Apollo-MCP / GenUI):**
6. ⬛ Apollo-MCP proposal page → Dave's ruling on its headline ("A2UI catalogue over MCP, MCP Apps fallback") → any MCP build (`_HANDOFF-154` OWED 1; it is a proposal, not yet a ruling).
7. ⬛ `meta.schema.json` reconciled with the #261 metas (`[132]` 13 schema errors, `[94]` data-grid `with` slugs) → **a declarative component schema** (G1 gap 1: "components exist as 139 baked HTML documents … slots declared on only 35 of 138") → an A2UI-style catalogue → any run-time renderer. ⛔ Building a catalogue from metas that fail their own schema publishes the drift.
8. Slots typed on the remaining ~103 metas → schema completeness → renderer coverage (G1 gap 1).
9. Data-binding contract (generalise `dvRender(spec)`; `shapes.json`'s 23 shapes onto slot `accepts`) → the contextual-dashboard vehicle (G1 gap 2; `notes/_VISION-contextual-dashboard_2026-06-29.md`).
10. Gates given an in-memory, no-disk-write entry point → "gates as a service" MCP tools → "gates run before anything renders" (G1 gap 3: the gates write records and the render leg needs Playwright).
11. A deterministic `when` evaluator (mechanical, since Jev is ruled dev-time only, #294) → run-time selection (G1 gap 4; `when` on 39 metas, `priority` on 25).
12. ⬛ Entitlements / user-context model → the "vehicle" half (G1 gap 5: no artefact in the repo).

**Graph and explorer:**
13. Graph changes (his three observations, `_HANDOFF-154` OWED 4 · the KG-gaps families, #269) → KG build (107 s offline, G1 gap 3; fits one call, tightly) → `_build_kg_explorer.py` rebuild → mention map → memento index → chain. The explorer builder is itself one of the 12 help-gate files, so guard it first.

**Release and package:**
14. ⬛ `[127]/[128]` re-port decision (open since `245-R`) → memento-package green.
15. CI gates green (policy, not structural: the `release` job is independent and has been green through all of this, and v1.0.13 shipped 2026-09-11 while `[128]` was already red) → ⬛ Dave's "re-cut" (`s219-D4(2)`) → `_gen_pack_manifest.py --probe/--manifest` (322 commits of drift, `[144]`) → v1.0.14. **Recommendation: no cut before step 6 of CI runs to the end.**
16. ⬛ Boot ceiling moved, or the Mac seat built → `--wrap` becomes legal again (§ 3).
17. ⬛ Credential re-issued before 2026-11-06 → pushes keep working (`s294-D5`).

## 6 · MODEL ROUTING AND WHAT PAST WAVES TEACH

**`MODEL-ROUTING.md` (root, last updated #297, 2026-09-22):** Fable for "big, high-stakes, hands-off jobs … 'I need to trust this'", rationed, `claude-fable-5`, 2× Opus per token · **Opus 5.5 · high is the default for every sub** (Dave #297) · Sonnet executes to a plan (gates, metas, refactors to spec) · Haiku does mechanical chores (no adaptive thinking, no xhigh). Rules that bite this weekend: **rule 5**, verify with peer-or-stronger, adversarial verifier kept for Fable-run and high-stakes work, self-verification scaffolding removed from Opus briefs (`s215-D3`) · **rule 7**, adversary at the BRIEF (a premise probe before any canon-touching build) and verifier only at CANON (gates, `canon.css`, rulings, packs), with a `CITES:` line in every report (#250) · **Mode 2**: delegation is deliberate, and Opus guidance says "keep spawn counts low … only for large tasks that are genuinely independent" · **the governor**: under pace, bias up and spend surplus on quality. Dave's "don't worry about token spend" puts this weekend under pace. · Effort ladder `low·medium·high·xhigh·max`, selectable per session in Cowork, not per Agent-tool spawn.

**What past waves show (receipts are the wrap reports):**
- **#245** (2026-09-03): six Fable lanes (memory hook `v106-cut-and-grid-may-not-matter-245`; seven lane reports on disk) plus a Fable wrap in one day, and the conductor peaked at 183,491 (`2026-09-03-245-wrap.md:7`). Lane R reproduced CI in a bare clone and found the mount 15× slower for `[13]`. ⇒ big parallel waves work when the lanes are disjoint. The clone is the right test bed.
- **#250**: a ≈100K Opus premise probe corrected the brief's own premise before the build (`2026-09-06-250-PROBE-byte-gate.md`), and rule 7 was born from it. ⇒ spend a probe before every canon lane.
- **#261** (2026-09-08/09): eleven lane reports, **lanes committed one by one** (`e71111b`, `108493b`, `5691d09`). `_state.json` was written by three lanes, one carrying three others' rows. **Three lanes shipped with no doc row** and the doc-row gate refused the next commit (`2026-09-09-261-V-verify-wave1.md:25-31`). **The same wave left `[10] [81] [82] [132] [94]` red, and CI could not see them.** ⇒ **the failure mode for a weekend of parallel lanes: per-lane commits, shared stores written by several lanes, and a regen serial nobody ran whole.**
- **#279** (2026-09-16): ten lane commits and three Fable verifiers. Two came back "GREEN WITH FIXES": RD's skill edit turned the BLOCKING frozen-release gate red, and the explorer was unusable at 390 px (`2026-09-16-279-RV-reader-scope-verify.md:7`, `-EV-explorer-verify.md:61`). That spawned fix lanes EX2 and SC2. The session wrapped with 17 commits unpushed and `/sessions` at 98.2%. ⇒ the verifier at canon earns its cost. Budget a fix lane per canon lane.
- **#283**: the seam check fired at 210,476 and the conductor did not cut the next lane (`2026-09-18-283-W-wrap.md:44`). ⇒ the instrument works when it is obeyed.
- **#291**: nine Opus lanes (reports L…L6, P, P2, P3), but six versions serial on Dave's eye (L…L6). ⇒ **eye-bound work does not parallelise and cannot run unattended.** Queue it for when he is back; do not burn lanes on it over the weekend.
- **#294**: three enactment lanes (A, B, G), then **one dedicated Opus commit lane (C2) made ONE commit with twenty-four named paths** (`2026-09-21-294-C2-commit.md`). The conductor fill was 192,062. ⇒ **this is the shape to copy: lanes build, a commit seat serialises.**
- **#303**: picture-by-picture work drove the conductor to 402,607. ⇒ no visual iteration in the conductor's own window.

## 7 · SAFE OPERATING RULES FOR A WEEKEND OF PARALLEL LANES

1. **One committer.** Lanes never run a git write, never run `git status`, `git reset` or `_capture_gate.py --wrap`, and never run `_build_all.py` on the mount. One commit seat (the conductor, or a dedicated Opus commit lane on the #294 C2 pattern) commits per wave with `SESSION_N=304 bash knowledge/_git_commit.sh --quiet --reconciled <msgfile> <named paths>`, on the declared not-a-wrap path.
2. **Every shared file is assigned to ONE lane in the divvy** (`_RUNBOOK-parallel-conductor.md:44-45`). Name them in the plan: `knowledge/_rulings.json`, `knowledge/_state.json`, `knowledge/_assertions.json`, `meta.schema.json`, `canon.css`, `_CHAIN.md`, `GOOD-MORNING.md`, `_LIVE-STATE.md`, `_CARRIES.md`, the mention map, the memento index. Lanes deliver doc rows and ruling text as files, and the commit seat mints them (#261's three-lanes-one-store collision).
3. **Lanes write new, uniquely named files** under `notes/_lanes/304/<lane>/` and `notes/_subreports/2026-09-2x-304-<lane>-<slug>.md`, and edit only the paths their brief names.
4. **Run the regen serial whole, in order, once per wave, at the commit seat**, not per lane (§ 5 edge 3). The ASSERT-009 re-base and `_gen_chain` come last.
5. **Before every commit, run the clone survey:** `git clone file://…` + `fetch --unshallow` into `$HOME`, apply the wave, then `_build_survey.py --include-mutating --resume --range` in four chunks (~4 min). No new red may appear against today's 15 (`notes/_lanes/304/A4/clone-BUILD-VERDICT-LOG.jsonl` is the baseline). Remove the clone when done (disk).
6. **Commit order within a wave:** mint doc rows → regenerate the chain → commit → expect one mention-map refusal, add `knowledge/_graph-mention-map.json`, re-run once.
7. **Push per wave** (plain, declared, branch and fast-forward checked). Read CI back until `render` closes (~9 min after gates). Record the failure set by NAME, since step IDs shift (`[123]→[125]` at #295).
8. **180 s is the unit of work:** foreground only, chunked, nothing backgrounded. A lane whose job cannot be chunked does not run at the seat.
9. **Every render is one call:** `export TMPDIR=/dev/shm; bash knowledge/_render/ensure_env.sh; source knowledge/_render/seat_env.sh; python3 <driver>` with `executable_path=$RENDER_SHELL`. Force `HSBC_MtUnivers_Latin` and say so, and never judge type from a fallback render.
10. **Canon-touching lanes get a premise probe at the brief and an adversarial verifier at the output** (rule 7). Research and analysis lanes get neither. Budget a fix lane per canon lane (#279).
11. **Nothing Dave-gated runs unattended:** the schema, the 4px exemption, the chart forks, the package re-port, any ruling or inscription, any release cut, anything judged by eye. Lanes stage these as decision pages with a recommendation each, ready for his return (the #281/#282 decision-page pattern).
12. **The conductor dispatches and does nothing else.** Boot ~128K leaves ~52K to the 180K stop line. Read reports by path and headline, hand-sum the fill, and obey the seam check. Do not let the conductor iterate visuals (#303: 402,607).
13. **Seat hygiene each session:** `pip install tiktoken jsonschema pyyaml --break-system-packages`, check `df -h $HOME` (<90%), `ensure_env.sh` OK, and check for stranded locks before the first commit (move them to `notes/_lanes/_orphan-locks/` with a unique suffix, never `rm`).
14. **The Mac stays awake:** power in, sleep disabled, desktop app open and linked. If the link drops, lanes die at the call boundary. So every lane checkpoints to disk after each chunk and can be resumed from its last file.

## 8 · RECOMMENDED HEALTH WAVE (for the synthesis seat to size against the roadmap)

These are recommendations, not rulings. All are Opus 5.5 unless stated. Effort per the routing doc.

- **H1 · unblock the abort chain. Sonnet 5 · high**, with an Opus 5.5 conductor read. Guard the 12 help-gate files: walk-up form for `_render/`, or retire the six `verify_demo_slides_268*` drivers to an archive dir if Dave agrees. Then the ASSERT-009 re-base (last), then regenerate `[3]` blast radius and `[124]` schematic in serial position. ~1 h. Proof: a clone survey with `[8] [10] [3] [125]` green.
- **H2 · the governs pointers + nav manifest + `.seg` namespace. Opus 5.5 · high.** Four evidence pointers on `s282-D2..D5`, by textual span, `json.load` count 638 unchanged (`[136]`, `[13]`). Type `navigations.meta.json`'s behaviour from its existing manifest block (`[38]`). Namespace `.seg button` or run `--update` with the diff reviewed (`[82]`). ~2 h. Premise probe first: whether the manifest block is the source of truth or the meta is is exactly the kind of premise #244 got wrong.
- **H3 · the schematic's structural fix. Opus 5.5 · xhigh.** Give `_gen_schematic.py --check` the HEAD-only-advance treatment `c896bcaf` gave `_gen_chain.py`, so `[125]` stops re-staling on every commit. ~2 h. Adversarial verifier: it is a gate.
- **H4 · the schema reconciliation as a decision page. Opus 5.5 · xhigh, Fable 5 verifier.** For each of the 13 `[132]` errors and the six `[94]` slugs, propose the schema change or the meta change, with a recommendation each. **Do not enact.** It is Dave's (`234-v106-brief.md:47`), and it is the first edge of the GenUI spine (§ 5 edge 7). ~3–4 h.
- **H5 · the Dave-gated set as one decision page. Opus 5.5 · high.** `[81]` 7px (render the badge at 7 and 8), `[86]` the four chart forks, `[127]/[128]` port now vs at the cut, the 322-commit pack drift, the boot ceiling, the credential date. One page, one recommendation per item, ready for Monday.
- **Commit seat · Opus 5.5 · high**, the #294 C2 pattern: one commit per wave, named paths, clone survey first.
- **Sequencing:** H1 → H2 in parallel with H3 → one commit and push → read CI back (expect `_build_all` to run to the end with the gate-route reds printed for the first time) → H4 and H5 in parallel (read-only against canon). H1–H3 fit Saturday evening and Sunday morning. H4/H5 are pages for Monday's rulings.

## RULING-SHAPED QUESTIONS (Dave's)

1. `[128]`/`[127]`: port `knowledge/_gen_chain.py` into both memento-package copies now, or hold for the next cut? (Open since `245-R`, 2026-09-03.)
2. `[132]`/`[94]`: extend `meta.schema.json` to accept the #261 shapes (`composes`, object `stateModel`, `hand-authored` provenance), or bring the metas back to the schema? This is the schema a GenUI catalogue would publish.
3. `[81]`: the nav count badge's `padding:0 7px`. Move it to 8px, or exempt it from the 4px grid?
4. `[86]`: sparkline and bar resolve `--status-positive`/`--status-neutral` to different inks in mono. Declare the forks in the ledger, or unify?
5. May the six one-off `verify_demo_slides_268*` drivers be retired to an archive folder instead of being guarded?

## UNPROVEN / CLAIMED

- **UNPROVEN:** that CI will match the clone verdict. aarch64 seat vs x64 runner. Price: one push after H1.
- **UNPROVEN:** CI's two extra `[128]` findings ("got 4" vs the clone's 2). The CI line is truncated. Price: read the full step output from the Actions UI, or run `_validate_package_delta.py` in an x64 clone.
- **UNPROVEN:** the exact first red date of `[3]` and `[82]`. It is inferred from the first meta or snippet change after the last regeneration.
- **UNPROVEN:** the duration of one `_git_commit.sh` run. The #303 W1 run fit in one call, and I did not time it.
- **CLAIMED, not measured:** that a sleeping Mac kills the link. It follows from the architecture, and the record has no observation of it.
- **Not done, by fence:** no GitHub API read (403; `add_repo` not called) · no `_build_all.py` anywhere · no gate run on the mount except `ensure_env.sh` (idempotent, gitignored target) and blast-radius `--check` (no write, per its `main()`) · no git beyond `--no-optional-locks` reads on the mount · no Project memory.

## EVIDENCE

`notes/_lanes/304/A4/`: `surv_1_20.log` · `surv_21_50.log` · `surv_51_100.log` · `surv_101_146.log` (the four survey chunks) · `clone-BUILD-VERDICT-LOG.jsonl` (their four verdict rows) · `capture-gate-selftest-fullclone.log` (`[13]`'s real arm, :259-263) · `governs-selftest.log` · `clone-INTEGRITY-REPORT.md` · `clone-TYPE-BLAST-GATE.md`. CI: `notes/_lanes/303/W/_ci-gates-W.log`, `_ci-runs-efe3bb47.txt`.

REPLAY-THESE: § THE ANSWER FIRST + § 1's second table (~1.8K tk) · § 5 (~1.5K tk) · § 7 (~1.2K tk).
