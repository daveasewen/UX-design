# #261 D3 — the s260-D3 DRIVER SCOPE, closed

Owed by #260's wrap (`notes/_briefs/2026-09-08-260-wrap-brief.md:24`): dv-004 was measured on one
page; butterfly-v's baseline join was provable and ungated; dv-016/017/009/line-011 were vacuous on
an engine canvas; `requiredAria`/`requiredDeclarations` passed on a JS string literal.

## A — premise probe: all six venues DID fail open (proved, not assumed)

`knowledge/_tests/chart-engine/_probe_fail_open.py` copies a shipped snippet to a temp dir, mutates
its INLINED engine so the RENDERED DOM breaks the rule, proves the break in Chromium, then runs the
gate over the copy. One line per venue, before → after:

| venue | mutation | rendered DOM | gate BEFORE | gate AFTER |
|---|---|---|---|---|
| dv-017 | `fillOf` returns `#ff2200` | `rogue_hex=['#ff2200']` | 0 blocking — GREEN | 16 hits — RED |
| dv-016 | `fillOf` returns `#f2f2f2` | series contrast 1.12:1 | 0 blocking — GREEN | 8 hits — RED |
| dv-009 | `<linearGradient>` injected into the drawn SVG | `gradients=2` | 0 blocking — GREEN | 16 hits — RED |
| dv-line-011 | curved `dv-series` `<path>` appended | `curve_series=2` | 0 blocking — GREEN | 16 hits — RED |
| butterfly-v join | `GAP 2.2 → 0.0` | join `2.200px → -0.000px` | 0 blocking — GREEN | 1 hit — RED |
| requiredAria | every `role`/`aria-label` stripped at runtime | `role="region"`, `role="checkbox"` gone | 0 blocking — GREEN | 1 hit — RED |

`python3 knowledge/_tests/chart-engine/_probe_fail_open.py --expect red` → **6/6 venues BITE**.

**FINDING 1 — `requiredDeclarations` does not exist.** It is in `_to_delete/` and
`second-system-govuk/` only; the shipped `knowledge/_validate_snippets.py` has no such check. The
5th venue is not a live gate. Its named sibling `requiredAria` is real and does fail open.

**FINDING 2 — the aria fail-open is already live, not hypothetical.** Six shipped snippets satisfy
a `requiredAria` string ONLY from a JS string literal: `role="img"` in Chart-boxplot, -bullet,
-candlestick, -scatter, -sparkline; `aria-pressed` in Chart-histogram.

**FINDING 3 — the receipt vouched for the wrong bytes.** Receipts are looked up by BASENAME, so a
mutated copy of a snippet was graded green on the pristine snippet's receipt. Closed by recording
`self_sha256` (the artefact's own hash) and checking it — see B.

## B — the driver now measures the siblings, and every rule is routed

`_drive_chart_engine.py` records, per figure per theme×mode: the paint ATTRIBUTES the engine wrote,
`gradients`/`patterns`, `curve_series`, and worst RENDERED contrast (`getComputedStyle`, composited
over the painted surface) for series / axis-label / gridline. butterfly-v's baseline join is
measured as a stack gap and lands in `dv004_px` (2.200px), so the existing dv-004 arm grades it.

It also now drives the **14 shipped `Chart-*.reference.html` snippets**, not only the 13 test pages
(27 artefacts). Before, the receipt measured the test page while the gate graded the SNIPPET —
evidence for a different file. `--check` → all 27 FRESH.

`_validate_dataviz.py` gained `driven_siblings()` (dv-009/016/017/line-011),
`driven_dv004_figure()` (figure-scoped, see Finding 4) and `_resolve_receipt()`, all on the same
`static OR driven, never skipped` route: missing receipt / unmapped artefact / stale source /
changed artefact / short combo set are all BLOCKING and name the remedy.

**FINDING 4 — the page-scoped dv-004 arm was unsafe once snippets were driven.** Chart-donut ships
an engine donut (2.109px of geometry) next to a markup donut that meets dv-004 by the OTHER legal
mechanism (surface-coloured stroke, 0.000px gap). `driven_dv004_recorded` takes the page minimum, so
the correct chart failed on its neighbour's evidence. Each figure is now graded on its own record.

**FINDING 5 — a stroke is not a series colour.** First cut of the driven dv-016 read every mark's
stroke and scored a correct donut at 1.00:1 — that stroke IS dv-004's surface-coloured separator.
It now reads the fill, and the stroke only for unfilled marks (lines, sparks).

## Matrix — `python3 knowledge/_validate_dataviz.py --matrix`

40 figure-rows across 15 graded surfaces; **0 unanswered** (86 driven · 81 static · 48 n-a).

```
artefact                          figure   dv-004  dv-009  dv-016  dv-017  line-011  requiredAria
Chart-bar.reference.html          fig-0/1/3  n-a   driven  driven  driven  driven    driven
Chart-bar.reference.html          fig-2      n-a   static  static  static  static    driven
Chart-bar.reference.html          fig-4    static  static  static  static  static    driven
Chart-boxplot / -bullet / -butterfly-h / -candlestick / -histogram / -scatter(x2)
                                           n-a   driven  driven  driven  n-a       driven
Chart-butterfly-v.reference.html  cbv1    driven  driven  driven  driven  n-a       driven
Chart-combo / -line(x2) / -sparkline(x3)   n-a   driven  driven  driven  driven    driven
Chart-donut / Chart-pie           cd1/cp1 driven  driven  driven  driven  n-a       driven
Chart-donut / Chart-pie           fig-1   static  static  static  static  n-a       driven
Chart-stacked-area.reference.html fig-0      n-a   driven  driven  driven  n-a       driven
_proforma/DataViz-interactive.html 16 figs static/n-a — all four siblings static     n-a
```

## D — restored and green

`_validate_dataviz.py` → **PASS**, 15 surfaces, 0 blocking. `--selftest` → **✅** (all #260 receipt
bite-tests still pass). `_drive_chart_engine.py --check` → **✅ all 27 FRESH**, Chromium 151.0.7922.34.

## Commits

- `#261 D3 (A)` the fail-open mutation probe, 6 venues GREEN
- `#261 D3 (B)` driver: sibling + baseline-join + rendered-aria measurements; drives the snippets
- `#261 D3 (C)` gate: driven_siblings / driven_aria / figure-scoped dv-004 / `--matrix`
- `#261 D3 (D)` receipts re-driven, 27 artefacts
- `#261 D3 (E)` this report

## Could NOT do — the FIRST obstacle each time

1. **`_validate_snippets.py` is outside the lane fence.** Its `requiredAria` arm still reads SOURCE
   TEXT and still fails open on its own; the DOM-reading arm had to be built in
   `_validate_dataviz.py` instead. Two gates now answer the same rule from different evidence —
   ruling-shaped: fold the source-text arm into the driven one, or delete it.
2. **The six live JS-string-literal `requiredAria` passes are NOT fixed**, only detected: the driven
   arm passes them because the strings ARE in the rendered DOM (they are emitted by the engine).
   The declaration is honest at runtime; only the source-text gate was lying about why.
3. **Playwright/Chromium was absent from this sandbox.** Installed per the driver's own docstring
   (`pip install playwright --break-system-packages`, `playwright install chromium`, then
   `apt-get download libxdamage1` + `dpkg-deb -x` + `APOLLO_PW_LD_LIBRARY_PATH`). Records now read
   2.108px where #260 recorded 2.109px on x86 — a 1/1000 px arm64 rounding difference, well clear
   of the 2.0px rule, and the receipt says which Chromium made it.
4. **`knowledge/_DATAVIZ-GATE.md` is rewritten by every gate run** and so is dirty in this lane's
   tree despite being outside the fence — the gate has no `--no-write`. Same shape as #260's
   `_validate_behaviour.py` absorb-rather-than-red finding.
