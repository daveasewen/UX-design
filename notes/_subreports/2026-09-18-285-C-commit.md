# #285 — C (commit lane) — 2026-09-18

Lane C. Commit run only, via `knowledge/_git_commit.sh` — no raw `git commit`, no push, none of
the four forbidden generators (`gen_kg_icons.py`, `gen_kg_rules.py`, `land_rests_on.py`,
`_build_all.py`) was run.

**Final sha: `b99d092c067102cc2d8d0dbbf13be64d977d1892`**
Subject as committed: `after #285 2026-09-18 — the B is straightened and the seam re-quotes the standing constraints`

Tree is CLEAN after the commit; no `.git/*.lock` remains.

---

## Step 0 — the delete grant, and the stranded lock

`mcp__cowork__allow_cowork_file_delete` was called first, per runbook ★ STEP 0, and granted
("File deletion is now enabled for the \"UX-design\" folder").

A stranded `.git/index.lock` WAS present at lane start — 0 bytes, mtime `14:39:25.812`, against
`.git/index` at `14:38:59.038`. That is the documented **stale, not live** signature in the
runbook's Gotchas (0-byte lock, ~the same instant as `.git/index`, i.e. a completed git operation
that could not unlink its own lock). **It was never `rm`'d by this lane.** It was cleared by the
script's own `clear_locks()` `mv`-aside, which is the sanctioned path and the script's first action.

## Pre-commit housekeeping

`notes/_lanes/285/logo-masters-2/_tmp/` existed (48K, 10 intermediate HTML crops). Moved with a
same-mount `mv` to `_to_delete/strays-285/_tmp` BEFORE staging, as instructed. It is not in the
commit.

`git status --short` at lane start matched the intended set exactly — 30 paths, nothing extra,
nothing unaccounted for.

## Run log — 3 runs, verdict lines quoted verbatim

### Run 1 — REFUSED (showroom sync gate)

```
gen_showroom --check: OUT OF SYNC — stale: ['accordion.html', 'account-card.html', 'action-bar.html', 'amount-display.html', 'amount-input.html', 'anchor-nav.html'] orphaned: [] index: ok
Run: python3 knowledge/gen_showroom.py
✗ showroom sync gate REFUSED (s191-D1) — generated showroom out of sync with canon (list printed above). Run: python3 knowledge/gen_showroom.py — or re-run with SHOWROOM_ACK="<real reason>" to pass it as a DECLARED gap. Nothing has been staged.
```

**What was done, and why it ended in a DECLARED gap rather than a stage.** The remedy the gate
names was run: `python3 knowledge/gen_showroom.py` → *"137 page(s) -> showroom/ (108 written, 0
orphan(s) pruned…)"*. The diff is **108 files, +108 −108** — exactly one changed line per page,
the base64 `<script id="payload">` blob. Decoding old and new payloads for `showroom/accordion.html`
shows a **single** substantive change:

```
-:is(button,a,label,span,small,strong,em,b,i,th,td,dt,dd,li,figcaption,…
+:where(button,a,label,span,small,strong,em,b,i,th,td,dt,dd,li,figcaption,…
```

i.e. a canon CSS selector change (`:is(` → `:where(`) that is **already committed in canon** and
whose showroom regeneration was never committed. It predates #285, touches no logo, seam or
standing file, and nothing in this lane's reconciled set causes it.

Staging 108 regenerated pages carrying another session's derivation into a logo/seam commit is the
class `git add -A` was retired for (P5, ruled Dave 2026-08-02). So the regeneration was **reverted**
with the mount-safe restore (`git show HEAD:<path> > <path>` — `git checkout --` cannot restore on
this mount, Gotchas), the tree returned to the reconciled 30 paths, and the gate was passed
**DECLARED**:

```
SHOWROOM_ACK="showroom stale for 108 pages from a canon :is( -> :where( CSS change already committed outside #285; regenerating touches 108 pages this lane never authored, so it is owed as its own commit — see msgfile body"
```

The same reason is in the commit message body, so the commit carries it.
⬛ **OWED, and not this lane's to take:** the showroom re-sync (108 pages) belongs to the owner of
the canon change, as its own commit.

### Run 2 — REFUSED (doc-row gate, post-staging)

```
doc-row gate: population 468 (added >= 2026-08-15, PICKED) · of which staged-in-THIS-commit 3 (#207 postscript: the single-commit blindspot) · unrowed 3
  ⛔ UNROWED: notes/_subreports/2026-09-18-285-LM2-logo-masters-regen.md (added 2026-09-18) — invisible to every carry; fix = one _state.add() row
  ⛔ UNROWED: notes/_subreports/2026-09-18-285-SC-seam-standing.md (added 2026-09-18) — invisible to every carry; fix = one _state.add() row
  ⛔ UNROWED: notes/_subreports/2026-09-18-285-V-verifier.md (added 2026-09-18) — invisible to every carry; fix = one _state.add() row
⛔ FAIL — the forgotten-document class is present.
✗ doc-row gate REFUSED (post-staging) — a document STAGED IN THIS COMMIT has no store row (list printed above). Add the row via _state.add(), or re-run with DOC_ROW_ACK="<real reason>" to pass it DECLARED.
```

**Repair: three rows through the store's own writer** (`_state.add()` + `_state.save()`), each with
an `owner`, a `project` and a real `closes_when`; store total 688 → 691:

| id | home | closes_when (abridged) |
|---|---|---|
| `W-285lm` | `notes/_subreports/2026-09-18-285-LM2-logo-masters-regen.md` | Dave has looked at the 4× before/after contact sheet and either accepted the regenerated masters by eye or named what still looks wrong |
| `W-285sc` | `notes/_subreports/2026-09-18-285-SC-seam-standing.md` | Dave has approved `knowledge/_standing.md` out of DRAFT (or named what to change) |
| `W-285v` | `notes/_subreports/2026-09-18-285-V-verifier.md` | Dave has read the verdict table and either accepted the findings or named which still need work |

⚠ **The ids the brief named could not be used verbatim.** `_state.add()` refused
`W-285lm2` with: `REFUSED: W-285lm2: id does not match '^(?:W-[0-9]{1,3}[a-z]{0,2}|G[0-9]{1,2}[a-z]?)$'`
— the pattern allows at most two trailing lowercase letters and no digit after them. The LM2 row is
therefore **`W-285lm`** (not `W-285lm2`); `W-285sc` was legal as given. A third row was needed that
the brief did not name, because the V report is in the same population: **`W-285v`**.

Adding the rows re-staled the chain — `✗ _CHAIN.md is STALE — it does not match GOOD-MORNING.md / _LIVE-STATE.md as they now stand…` — repaired with `python3 knowledge/_gen_chain.py` (the remedy the check itself names), then green: `✅ _CHAIN.md is FRESH — byte-matches the live chain`.

Both repair outputs, `knowledge/_state.json` and `_CHAIN.md`, were **named explicitly** in run 3's
path list. The stage was never widened beyond what a gate asked for.

### Run 3 — COMMITTED

```
— committed: b99d092c after #285 2026-09-18 — the B is straightened and the seam re-quotes the standing constraints
— subject asserted identical to the headline T3 generated (in memory, not re-read from the msgfile — #171)
— subject carries exactly ONE T3 prefix (#208 doubled-subject assert)
✓ done — locks clear, safe for Dave to push via GitHub Desktop
```

Other gates green on the way through: chain fresh · doc rows present · polarity gate green ·
mention map fresh · session witness agrees (#285) · wrap gate green (mid-session, DECLARED
not-a-wrap) · spine-writer selftest green.

## The commit subject — one deliberate deviation from the brief

The brief gave the subject as `during #285 2026-09-18 — the B is straightened and the seam
re-quotes the standing constraints`. **T3 GENERATES the session prefix** (s130-D3), and
`_RUNBOOK-git-commit.md` step 3 is explicit: *the msgfile headline must not carry a session prefix
of its own*, or the subject comes out doubled in `git log` (observed #185, amended in-session).
`during #285 2026-09-18 — ` is that prefix in a spelling the anchored `prefix_count` gate does not
catch, so writing it would have produced
`after #285 2026-09-18 — during #285 2026-09-18 — the B is straightened…`.

The headline was therefore started at the actual subject, and the committed subject is
`after #285 2026-09-18 — the B is straightened and the seam re-quotes the standing constraints`.
The deviation is stated in the commit body too.

## What was staged, and what was left out

**Staged (the full intended set, plus two gate-demanded repairs):**
`knowledge/assets/logos/_gen_masters.py` · the 20 `knowledge/assets/logos/masters/masterbrand-*.svg` ·
`notes/_lanes/285/` (whole subtree, incl. `logo-masters-2/` and `DAVE-RULINGS-2026-09-18.md`) ·
`knowledge/_standing.md` · `knowledge/_seam.py` · the three
`notes/_subreports/2026-09-18-285-*.md` · `notes/_lanes/284/DAVE-RULINGS-2026-09-18.md` ·
`notes/_REHEARSAL-LOG.jsonl` · `notes/_dream/_GRADE-DECISIONS.jsonl` ·
**`knowledge/_state.json`** (doc-row gate) · **`_CHAIN.md`** (chain gate).

**Left unstaged / not committed:**
- `_to_delete/strays-285/_tmp/` — gitignored by construction (`_to_delete/` is never committed).
- The 108 `showroom/*.html` pages — regenerated, then reverted to HEAD; see run 1. Not in the tree,
  not in the commit, owed elsewhere.
- **This report** (`notes/_subreports/2026-09-18-285-C-commit.md`) — written after the commit, so it
  is uncommitted and, being a `notes/_subreports/*.md`, it will need its own `_state.add()` row
  (suggested id `W-285c`) before whoever commits it clears the doc-row gate.

Nothing else was dirty at any point. Nothing was pushed.
