# `#243`-`wrap` — the delegated capture ritual for #243: one ruling read back, the stub's first boot recorded, the skills prune found unenactable, and a ceiling arm that fired on a boot no seat can cut

session: `#243` · 2026-09-03
window: conductor window (FABLE), delegated wrap sub (FABLE)
sub index: `wrap`
brief: delivered in chat by the conductor (no `notes/_briefs/` file was written for this wrap — DECLARED; the forgotten-document class applies and the brief's facts are quoted below as the conductor's DECLARED figures)
tokens: `UNMEASURED at the sub's own seat as a SPEND; the seat's FILL is measurable and is 150,190 real, peak across 40 turns (knowledge/_checkin.py, first-hand, newest mounted transcript). The two are different quantities and neither is converted into the other.`

## VERDICT

The capture ritual ran **COMPLETE** inside the brief's DO-NOT-RULE fence: steps 1, 1b, 2, 2c, 2d, 2f and 2g all
executed, 2e was legitimately empty and is declared with its size, step 3 (memory) was the conductor's and is
declared as ZERO bytes written here, and step 5 committed WITHOUT a push (not authorised at this seat — declared,
not skipped) **and WITHOUT the `--wrap` form — REFUSED by the script's #74-D1 consumer on the ceiling red; the
content landed as a commit DECLARED not-a-wrap and the `--wrap` FINAL commit is OWED to Dave's ruling (below).** Every roll went through `_gm_move.py` from session-owned, uniquely-named ops files under
`knowledge/_tmp/wrap243/`, each asserted for existence and a size floor **in the writing process**; nothing was
hand-edited. **Nothing was ruled, no ruling was re-worded, re-dated or re-stamped, and no constant, band,
ceiling, floor, advisory, stop line or wall was moved.**

★ **The headline is that the wrap gate is RED BY NAME at this seat, on a true statement, and it was predicted.**
#242's stratum said its 70,710 boot would reach `notes/_GAUGE-LOG.md` at THIS wrap's 2f roll and that the
`s241-D1` ceiling arm would fire there. Driven at this seat after the roll, `boot_constant_drift_check` returns
**1 fail** — *CEILING BREACH: BOOT_CEILING_TK = 70,000 and 1 post-diet reading(s) EXCEED it — #242 70,710* — and
ARM 1 has **no declared-discharge form** (the `boot-drift DECLARED` line is ARM 2's, for a band step). The gate's
own text names the only remedies — cut the boot, or Dave's word on the literal — and neither is a wrap's. So the
red is DECLARED in the stratum with the gate's exact text, the wrap's content is committed in the script's other
legal state (not-a-wrap, red *visible, not blocking*), the `--wrap` FINAL commit is owed, and the
question of what ARM 1 should mean when it fires on a harness boot is carried to Dave as a new item. **The #243
reading itself is UNDER the ceiling and reaches the log at #244's roll.**

`s243-D1` was inscribed at the CONDUCTOR'S seat (store **335 → 336**); this seat probed the count and read the id
back. Four store rows were **MINTED** (`W-402` … `W-405`). Two carries were **STRUCK** with `s188-D2` receipts, one
was **CORRECTED** with its receipt and NOT struck, **327 ages were bumped**, nine `[NEW — 0]` became `[1]`, and
**five** new carries were written at age 0.

COUNTS: findings `7` · ruling-shaped `4` · UNPROVEN `6`

## What was done

**The ruling first, because the record cites it.** Not inscribed here — the conductor ran `_inscribe_ruling.py`
before the brief was cut. At this seat: `knowledge/_rulings.json` parses at **336** entries; `s243-D1` reads back
with `by` Dave · `date` 2026-09-03 · `status` ruled · `governs` three repo paths; its `ruled` head is *“A CARD MAY
NOT NAME ITS OWN WITNESS - #239 Q3 STANDS UNCHANGED …”* and its `says` carries Dave's question and his “yes”. The
`ruled` head is quoted in the ⏱ LATEST delta; the file was not edited.

**Step 1 — `_LIVE-STATE.md` refreshed.** New `## ⏱ LATEST DELTA — 2026-09-03 … #243` (17 lines incl. blanks; 12
bullets); the `*Last refreshed:*` header rebuilt with the #243 summary leading, #242 and #241 kept as `Previous:`
segments, and a dated trim note in #240's place. Date from `date` (Thu 3 Sep 2026), never from belief.
`python3 knowledge/_validate_standing_instructions.py` → **PASS, 28 standing docs reachable.**
`knowledge/_REVIEW-SIGNOFF.md` NOT touched — the one review surface this session made was ruled from inside the
session, so nothing is awaiting Dave.

**Step 1b — dossier written.** `_DECISION-HISTORY/2026-09-03-243-the-stubs-first-boot-and-the-witness-rule.md`
with its `provenance: 243 · 2026-09-03` / `status: observed` block and both-way links. Seven sections: the boot
A/B and why the figure is stated once · why the six controls were ruled rather than widened · build/verify/fix with
the verifier in the same wave a third time · why the skills prune changes a carry's state and not its wording ·
the cloud toggle set up and deliberately not read · the ceiling arm · what was declined and why.

**Step 2 — `GOOD-MORNING.md`.** New ★ LATEST #243 banner: **10 substantive lines, 1,169 tape** against
`s241-D2`'s 1,200 cap — the third banner bound by it and the first with margin (31). `TITLE THE NEXT CHAT` →
`Apollo - #244: the cloud toggle's first boot — seven controls green`. `size:` stamp re-written LAST and iterated to
an **EXACT fixed point** (GM 36,330 tape, claim 36,330; LS 54,757; corpus 91,087) — twice, because the `s214-D6`
measurement line was inserted after the first stamp and the stamp was re-derived on the final artefact.

**2c — banner roll + the carry set.** `## Batch 2026-09-03 #243` opened in `_GM-ARCHIVE.md`; the ★ PRIOR **#241**
banner moved there **verbatim, 13 lines**, by `{"op":"move"}`; the #242 banner demoted to ★ PRIOR in the same
`replace` that inserted #243's. The carry set was written into `_CARRIES.md` § `## residual → #244` (clause (i),
authored in its home), and the banner carries the pointer + the probeable count.
**`_capture_gate._carry_items` reads 214 on the #243 line and 223 on the #244 line.**

**2d — delta roll.** The #240 ⏱ PRIOR DELTA moved **verbatim, 12 lines**, to `_LIVE-STATE-ARCHIVE.md` §
`Rolled 2026-09-03 #243`, and #240's `Previous:` chain segment (**5,857 chars**) moved into the **same** section in
the same batch, replaced in the spine by a dated trim note.

**2e — nothing to do, and the size is ZERO DATED STRATA.** `## ⬛ DO THIS FIRST` measures **78 lines / 10,140 tape**
and a `#### ` scan of the region returns **0**.

**2f — stratum split.** `{"op":"roll_2f","session":242}` → **17 lines** appended at true EOF of
`notes/_GAUGE-LOG.md` and **2 lines** into `_GM-ARCHIVE.md` under the newest batch, driven with the
SESSION-QUALIFIED anchor `> **COMMIT STATE #242:**`. The new `#### 2026-09-03 #243` stratum (17 lines + the
`s214-D6` measurement inserted after the stamp) then written into `GOOD-MORNING.md` § ⏱ SESSION STRATA, GM keeping
LATEST only (#96/#95/#40/#41/#42 EXEMPT by Dave's #58 ruling, untouched). The stratum carries: the boot **stated
once** · `subs 382,211 tokens (n=3)` · the `wrap-handover:` row (brief-cut **148,242** DECLARED · sub-cut
**150,190** first-hand · delta **1,948** · replay unobservable) · the ⚠ CEILING line quoting the gate · consult-
receipts (honest negative) · section-usage/sizes · the declared skips (ten, each with a size) · the three EXIT
CHECKS · the premise checks · `COMMIT STATE #243`.

**2g — retrieval index rebuilt LAST**, after every GM/LS/carries edit was final, and staged.

**Step 3 — memory: ZERO bytes written at this seat.** The conductor's, done at his (the brief names
`skills-prune-is-harness-not-panel-243.md` + one index line). NON-REPO, outside every gate; this sub neither wrote
nor read it back and claims nothing about the store's contents.

**Step 4 / 4b — decision nodes + titles.** `W-402` (lane Q report + Q2 addendum) · `W-403` (V3 report) · `W-404`
(this report) · `W-405` (the review surface, so `s243-D1`'s `says` citation resolves) MINTED via `_state.add`;
nothing closed. `_gen_titles.py --session 243` run and the receipt regenerated (finding 5). **Step 4c** —
`_gate_scratch_hygiene.py` named one entry owned by this session's user (`/tmp/s243-D1.json`, the conductor's
inscribe input) and it was cleaned; the wrap's own ops files live in `knowledge/_tmp/wrap243/` (gitignored,
in-mount, never `/tmp`).

**Step 5 — commit, NO push, and NOT `--wrap`.** `SESSION_N=243 bash knowledge/_git_commit.sh --wrap --reconciled
<msgfile> <paths>` was run TWICE: the first run regenerated the stale `knowledge/_graph-mention-map.json` and asked
for it by name (P5); the second passed chain-fresh · doc-rows · showroom · polarity · mention-map and then **REFUSED
at the #74-D1 consumer: `wrap gate RED on a --wrap commit`** — the one red being ARM 1's CEILING BREACH, which no seat
can clear. The script's other legal state is a commit DECLARED not-a-wrap (`SESSION_N=243`, no `--wrap`), where the
same red is printed *visible, not blocking*; that is how this wrap's content landed, with the reason in the msgfile
body. ⛔ **The session's `--wrap` FINAL commit is OWED** — one command once Dave rules `_CARRIES.md` § residual → #244
⑤ — and this is #241's WRAP DATE SPLIT in a new shape: nothing re-dated, no ceiling moved, no roll skipped. ⬛
**DECLARED GAP: this file cannot carry its own commit hash — a report written before the commit cannot name it.** It
is read back from `git log -1 --format='%h %s'` and quoted in the stub. ⛔ **No push:** the brief did not authorise
one and runbook step 5's push is Dave's GitHub Desktop; `origin/master` stays at `9628bae` until he pushes.

## Findings

1. **The ceiling arm fired exactly where #242 said it would, and it has no legal discharge — by design, not
   omission.** ARM 2's `boot-drift DECLARED` form exists because a band step is a thing a session can truthfully
   state; ARM 1's absence of a form encodes “shrink-only, Dave's literal”. At this seat the honest outcome is a red
   wrap gate over a true statement about a boot ≈89% of which is harness. ⇒ this is the
   [[gate-cannot-pass-in-one-environment]] class *inside the wrap gate itself*: the remedy the gate names cannot be
   taken by the party the gate blocks. Carried as `_CARRIES.md` § residual → #244 ⑤ with three options and no
   choice made.
2. **The one-wrap lag cuts both ways, and this session shows the benign side.** #243's own reading (under the
   ceiling) is invisible to the gate until #244's roll; #242's (over it) is graded now. The lag produced a correct
   red rather than a hidden one — the same conclusion the #242 wrap reached about ARM 2 — but it means a session
   that FIXES a breach will still be graded red once more before the fix is seen. Reported; RULING-SHAPED 2 of
   `notes/_subreports/2026-09-03-242-wrap.md` stays open.
3. **The S1 cap was met a third time and, for the first time, with margin — and the chain went DOWN.** Banner
   **1,169 / 10 lines**; `_CHAIN.md` **8,385 → 8,347 tape (−38)** because the LS ⏱ delta also came in shorter
   (**3,095 tk** against 3,181) while the GM header grew (this stamp carries a ceiling-breach declaration). ⚠ The
   #242 finding stands: two of the chain's three parts are outside the cap, and this time they happened to shrink.
   Still above `M10`'s 7,700 advisory. Not a repair, a reading.
4. **A carry changed STATE without changing TRUTH, and the rules had a form for it.** Residual ③ (the skills prune)
   said “Dave's panel action, NOT DONE”; #243 proved no panel does it. That is a correction with a receipt under
   `s188-D2` — the wording changes only to record it, the original text stays verbatim after — and NOT a strike,
   because the item is still open (ruled, unenactable here). The two genuine discharges (① ruled, ② read) were
   struck. ⚠ Whether “corrected, not struck” should be a named third form beside strike/age is a question for the
   carry rules; it is written here so the next wrap does not have to re-derive it.
5. **The generated titles and the brief's titles differ, and the brief's are the ones delivered.**
   `_gen_titles.py --session 243` derives its NEXT-TITLE from the first ⬛ bullet's bolded clause on the residual
   line (*the cloud toggle's first boot — #244's A/B is one variable…*), truncated; the conductor's chosen title
   `Apollo - #244: the cloud toggle's first boot — seven controls green` is what the GM header carries.
   `title_generation_check` asserts the receipt exists and matches the banner session — not equality — so both
   stand and the divergence is reported.
6. **The carry-count arithmetic moved 214 → 223 while FIVE items were added, and the cause is unchanged since
   #230.** `_capture_gate._carry_items` counts only `·`-segments carrying a DIGIT bracket: #242's nine `[NEW — 0]`
   items became countable on ageing (+9) and this wrap's five arrive uncounted. One programmatic pass: **327
   title-closing age brackets bumped** + **9 `[NEW — 0]` → `[1]`**. The bump is anchored on the following `**` so
   mentions inside bodies are not transformed.
7. **The three unreconciled ordinals are still unreconciled, and this is the fourth consecutive wrap to say so.**
   `grep -c 'BANNER-DISCIPLINE MEASUREMENT' notes/_GAUGE-LOG.md` reads **31** against a newest NAMED ordinal of
   TWENTY-FIFTH (off by six, widening by one per wrap); `grep -c '^> \*\*wrap-handover:'` reads **30** against **34**
   for the bare pattern. ⛔ All REPORTED, none repaired, no third figure invented. RULING-SHAPED 3 of the #242 wrap
   report (publish only the probe, stop naming ordinals) stays open.

## RULING-SHAPED QUESTIONS

1. **What should ARM 1 (the ceiling) mean when it fires on a reading no seat can cut?** (a) leave it — a red that
   says something true, declared each wrap until the boot falls · (b) give ARM 1 a declared form like ARM 2's,
   with the same match-the-gate's-figures bar · (c) grade the ceiling only when the NEWEST reading is also over it,
   so a fixed breach is not re-graded red for one extra wrap. **Recommend (b)** — the #111-D1 asymmetry (declared
   passes, silent fails) is the repo's own answer to “true but unfixable here”, and ARM 1 is the one boot check
   without it. ⛔ Not done here — a gate change and Dave's ceiling. Carried as residual → #244 ⑤.
2. **Does “corrected, not struck” need its own carry form (finding 4)?** (a) no — `s188-D2`'s receipt clause already
   covers it · (b) yes — name it so the wording check can pair it · (c) fold it into the strike form with a
   `still open` marker. **Recommend (a) now**, because the receipt clause did the job here without a new rule, and
   the next wrap can cite this report.
3. **Should a wrap be allowed to commit over a red wrap gate at all, or only over a DECLARED one?** Today the commit
   script does not run `_capture_gate.py --wrap`; the ritual's honesty is prose. (a) leave it · (b) have
   `_git_commit.sh --wrap` run the gate and refuse on any UNDECLARED fail (a fail with no matching declaration
   line in the stratum) · (c) refuse on any fail. **Recommend (b)**, and it is the same asymmetry as question 1.
4. **Should the wrap brief itself be filed under `notes/_briefs/` when it is delivered in chat?** #242's was filed
   (`W-400`, forgotten-document class); #243's was not, and this report is the only place its figures live.
   (a) require a filed brief for every delegated wrap · (b) let the wrap report quote the brief's DECLARED figures,
   as here · (c) either, but say which. **Recommend (a)** — a brief is the one document that names what the wrap
   was NOT allowed to do, and that fence should outlive the chat.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** the CI state for `a09a3ea`, `7f8801f`, `9628bae` and #243's push-to-come — no verdict was read at
  this seat and the next blocker past step 10 of 142 is unknown. Price: the capped route, run page + one JS grep,
  ~10K.
- **UNPROVEN:** `python3 knowledge/_capture_gate.py --selftest` was NOT re-driven here (it exceeds the ~178 s
  sandbox call wall), and `_build_all.py` was NOT run (fenced, and sandbox-impossible in one process).
- **UNPROVEN:** the lanes' arm counts (137 → 148 → 150 / 0 failures / `--check` GREEN / brain files
  BYTE-UNTOUCHED) — quoted from the two filed reports and the conductor's first-hand re-run, NOT re-driven at
  this seat.
- **UNPROVEN:** that a not-a-wrap commit carrying a wrap's whole content is graded correctly by the NEXT opener's
  chain-title check (the script's own comment: *the chain title check catches that NEXT session*). The #244 opener
  should expect a `--wrap` FINAL commit still owed, not a skipped wrap.
- **UNPROVEN:** the effect of the cloud-toggle change. Nothing observable moved in #243; the reading is #244's.
- **UNPROVEN:** the attribution of the −3,575 boot step to the `MEMORY.md` stub. One variable was changed, but
  lane F's prediction is in tape and the reading is in real tokens; the excess is unattributed and stays so.
- **UNPROVEN:** that this seat's `_checkin.py` reading (newest mounted transcript, 214 records, boot 67,135) is the
  CONDUCTOR'S transcript rather than a merged one — the boot figure matches the conductor's declared opener
  reading exactly, which is the evidence for it, and the `sub-cut` figure is reported on that basis.
- **CLAIMED:** every figure attributed to the conductor's seat — the FILL series (83,559 @ 6 → 148,242 @ 38), the
  quota panel (42 % / 63 % / 9 h 17 m), the sub token totals (Q 120,688 · V3 116,923 · Q2 144,600), Dave's quoted
  words, the three panels' contents, and the memory write. What was RE-READ here is the repo side:
  `knowledge/_rulings.json` at **336** with `s243-D1` read off the store; `_checkin.py` at this seat reading **boot
  67,135 real** and **FILL 150,190 real, peak across 40 turns**; `boot_constant_drift_check` returning **1 fail**
  after the roll; the doc-row gate, the standing-instructions gate and every mover receipt.

## Evidence

No evidence directory: every claim above quotes its probe inline (the mover's stdout receipts,
`_capture_gate`'s own `measure_tokens` / `boot_constant_drift_check` / `_carry_items`, `_gen_chain.py`'s
generated footer, `_roll_state.py`'s generated residual line, `_gm_usage.py --sizes --session 243`, the two `git`
read-backs named in the stub). The wrap's scripts and ops files are in `knowledge/_tmp/wrap243/` — gitignored,
in-mount, owned by this session — and are working inputs, not receipts.

REPLAY-THESE: this file § RULING-SHAPED QUESTIONS (~900 tk) · `_CARRIES.md` § `## residual → #244`, the FIVE new
items, the TWO strikes and the ONE correction only (~2,600 tk)
