# #250 — the delegated capture ritual (wrap sub, Opus 5)

provenance: 250 · 2026-09-06
status: observed

VERDICT: **RITUAL COMPLETE, STEPS 1 → 5, WITH ONE GATE REFUSAL NAMED AND NOT WORKED AROUND.**
`s250-D1` inscribed in Dave's verbatim words (store 370 → 371, read back at this seat); the
`--wrap` form REFUSED by the ceiling arm for the fourth wrap running; the commit made in the
#243/#244/#248/#249 form. No push.

COUNTS: findings `6` · ruling-shaped `3` · UNPROVEN `2`

REPLAY-THESE: `git status -sb` (the brief said FIVE ahead; it reads `ahead 7`) · `python3
knowledge/_validate_behaviour.py` (exit 0, re-driven here, not quoted from lane E) ·
`PYTHONPATH=knowledge python3 -c "import _capture_gate as c;print(c.boot_constant_drift_check('.')[0])"`
(one fail, and #242's arm is gone by window effect, not repair)

---

## What ran

| step | receipt |
|---|---|
| **1** | `_LIVE-STATE.md` — new ⏱ LATEST DELTA #250 (15 ln), #249 demoted to ⏱ PRIOR, `Last refreshed` rebuilt; `_validate_standing_instructions.py` PASS (28 standing docs) |
| **1b** | `_DECISION-HISTORY/2026-09-06-250-adversary-at-the-brief.md` — the WHY/HOW, nine sections |
| **2** | `GOOD-MORNING.md` — ★ LATEST #250 banner, `TITLE THE NEXT CHAT` rewritten, `size:` stamp written LAST |
| **2c** | EXIT CHECK on the rolling **#248** banner (12 ln, 2 marker lines, **24 of 24** items probed by name in `_CARRIES.md` § residual → #251) → moved verbatim to `_GM-ARCHIVE.md` § Batch 2026-09-06 #250, via `_gm_move.py` |
| **2d** | EXIT CHECK on the rolling **#247** delta (15 ln, 5 marker lines, **25 of 25** probed) → moved verbatim to `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-06 #250, with its 4,061-char `Previous:` refresh-chain segment and a dated trim note left in the spine |
| **2e** | **0 dated strata** in `## ⬛ DO THIS FIRST` (109 raw lines measured) — nothing to roll, size declared |
| **2f** | `roll_2f` #249 → **19 ln** to `notes/_GAUGE-LOG.md` (appended at true EOF) + **2 ln** to `_GM-ARCHIVE.md`; new `#### 2026-09-06 #250` stratum written (22 ln) |
| **2g** | `_build_memento_index.py` rebuilt LAST, after every GM/LS edit was final |
| **3** | **0 bytes at this seat** — the conductor wrote `opus-lanes-premise-probe-250` before cutting the brief |
| **4** | `s250-D1` inscribed; five `_state` rows minted (W-448…W-452) |
| **4b** | `_gen_titles.py --session 250` — both lines generated, receipt written |
| **4c** | `_gate_scratch_hygiene.py` run |
| **5** | commit in the #243 form, no `--wrap`, no push |

## Findings

**1 — The brief's push premise was stale, and the correction is the day's own lesson.** The brief
said HEAD `49ded37` was FIVE ahead of `origin/master`; `git status -sb` reads **`ahead 7`** (the
two #249 DESK commits landed after the figure was written). Corrected in the spine, the banner and
the carry set, from the probe rather than from the brief. ★ Rule 7's premise-probe clause applied
to the wrap's own brief — a premise ages faster than the document that states it.

**2 — The gate's green was re-driven, not quoted.** A wrap that copies a lane's exit code has
verified nothing. `python3 knowledge/_validate_behaviour.py` returns **exit 0** at this seat —
behaviour 13,048 · legend 7,734 · donut-sweep 3,889 code-only, worst member Chart-donut **24,671**
of 34,816 — and `--selftest` returns **OK**.

**3 — The ceiling arm went from two fails to one, and it is a WINDOW EFFECT, not a repair.** After
the 2f roll, `boot_constant_drift_check` returns **1 fail** (#247 70,822 · #248 70,974 · #249
71,113 over the 70,000 ceiling). The `#242 NOT DISCHARGED` arm that stood at #249 is no longer
raised because the derived band's n=7 sample now runs **#243–#249** and #242 has slid out of it.
⛔ An arm going quiet because its evidence aged out of a window is not the same as an arm being
satisfied, and the difference is written down here rather than banked.

**4 — #250's own boot is the first post-diet reading UNDER the ceiling: 69,293, by 707.** It was
**graded, not acted on**; no constant moved; and it was NOT used to argue the arm shut — it is not
in the gauge log yet (it enters at #251's roll) and would not discharge the three readings that
are.

**5 — The hand-over is the smallest on record, and the reason is structural.** `wrap-handover`
delta **4,502 real** against #249's 15,970 — because the conductor cut this brief **5,681 UNDER**
the 150,929 advisory rather than 34,982 over it. This wrap was delegated by rule 7, not by
overflow.

**6 — The doc-row gate carried one pre-existing FAIL and it is repaired by a row, not an edit.**
`notes/_subreports/2026-09-06-249-DESK-interchangeability.md` was committed at #249 with no
`_state` row. Rowed here as **W-452**; the report itself was not touched (ADR-0017 / `s192-D1`).

## RULING-SHAPED QUESTIONS

1. **The `consumes` guard (`V-MUT-5`).** The page sum trusts a declaration it never checks.
   Building the cross-check changes what the gate REFUSES, which makes it Dave's, not a wrap's.
2. **Legibility (ADR-0015 A1 vs A3).** A code-only unit prices comments at zero; A3 re-points the
   conflict and does not answer it. Nothing in this repo measures legibility, so there is no
   number to fail. What (if anything) should bound the raw size is his.
3. **Rule 7 — confirm, keep as prose, or retire.** It is a working shape with no id by deliberate
   choice (*"i might be wrong dont follow me blindly"*). It paid on its first day in both
   directions. Promoting it without his word would be the overreach the project forbids.

## UNPROVEN

1. **This seat's own token spend.** A sub cannot read its own `message.usage`; declared
   unobservable rather than estimated.
2. **The memory write.** The conductor states he wrote `opus-lanes-premise-probe-250`; the store
   is invisible to the shell and to every gate, so this seat records his statement and verifies
   nothing (the D1a honest scope).
