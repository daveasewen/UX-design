# 291 · P2 — slide 10 becomes a three-up, and Parts takes the gearbox

Provenance. Dave, today, with a screenshot of a THREE-UP (01 Parts = the
GEARBOX drawing, 02 Knowledge = the books, 03 Proficiency = the brain, the same
copy the current cells carry), said verbatim:

> lets just have these the three on slide 10

That sentence and the screenshot are the whole of the instruction. Everything
below that is not a mechanical consequence of it is listed under
RULING-SHAPED QUESTIONS; nothing here has been accepted by Dave.

## VERDICT

Landed. `notes/_DEMO-SLIDES-apollo-2026-09-20-v12.html` — edited in place, the
pre-edit file preserved byte-for-byte at `notes/_lanes/291/P2/v12-before-p2.html`
— now carries a **three-up** anatomy on slide 10: 01 Parts (the gearbox),
02 Knowledge (the books), 03 Proficiency (the brain), three equal columns in the
same rule box, the headline re-cut to "Three things she is made of." The
gearbox draws once into an off-screen host (`#gbHost`, the `#bkHost` pattern
from #290) and bakes `gbPrint` without a slide-4 canvas; slide 4 keeps the
workers, slide 6 keeps the catalogue. 12 slides, 13 `class="slide"` / 13
`<section>`, **0 page errors**, **3/3 anatomy cells baked**. Looked at all
three screenshots.

## WHAT LANDED

One script, `notes/_lanes/291/P2/build_p2.py`, 17 substitutions, every one
asserted on a unique anchor (the two anchors that repeat elsewhere in the file
are cut with a `sub_gb` helper that scopes the search to the gearbox IIFE).
v12: 7637 → **7642 lines** (+5).

**1 · The gearbox gets an off-screen host (3 edits).**

- CSS, after the `#bkHost` rule:
  `#gbHost{position:fixed;left:-3000px;top:0;width:600px;height:420px;pointer-events:none;z-index:-2;}`
  — identical geometry to `#amHost` / `#bkHost`.
- `@media print`: `#gbHost{display:none!important}`, beside the other two hosts.
- markup, after the `#bkHost` div at the foot of the body:
  `<div id="gbHost"><canvas id="gb"></canvas><img id="gbPrint" alt="" style="display:none"></div>`
  — placed before the `<script>` block, so the IIFE finds it at load.

**2 · The gearbox IIFE, three lines (the file is otherwise untouched).**

- `start()` is fenced exactly as the books' is:
  `if (running || reduce || paused || (cv.parentNode && cv.parentNode.id === 'gbHost')) return;`
  so the scene renders its resting frame once (line 2003's
  `setView(YAW0, PIT0); render(0)`) and never animates. `updateView` only runs
  inside `frame()`, which only runs while `running`, so the orbit listeners —
  which stay on `window` — move nothing.
- the IntersectionObserver observed `#s4`; it now observes `#gbHost`. Harmless
  either way, since `start()` no-ops, but `#s4` is the workers' card now and
  the reference would have been a lie.
- the `focus` handler's `#s4` rect test is replaced by a `#gbHost` lookup, same
  reason.

`size()` reads `cv.parentNode.clientWidth/Height`, so the canvas takes the
host's 600×420 at DPR 1.5 → 900×630. `setTimeout(snap, 1200)` bakes `gbPrint`
as before: **197,890 B** of `data:image/png` at load, measured.

**3 · Slide 10, five-up → three-up.**

- headline `Five things she is made of.` → `Three things she is made of.`
  The eyebrow (`What she is made of`) and the footnote ("Three roles in the
  story — the inventory, the assembly engineer, the inspector — and this is
  what the engineer is made of.") are **untouched**, per the brief.
- the `an4` (04 Tools) and `an5` (05 Process) `<div>`s are deleted whole.
  01, 02 and 03 keep their `ix`, `h3` and body copy **verbatim** — "The
  inventory: 137 components in code." / "Standards, accessibility, rulings,
  governance, usage, theory — one graph." / "The judgement to compose from it."
- `an1`'s `alt` is the one word of copy that changed: "Line drawing of an open
  parts catalogue on a stand" → "Line drawing of a gearbox", because the
  picture in that cell is now the gearbox. Alt text, not visible copy.
- columns: `.grid5{grid-template-columns:repeat(5,1fr)}` → `repeat(3,1fr)` and
  the `#s10 .grid4` override likewise. Both were needed — the second overrides
  the first. Measured in Chromium at 1440: **373px 373px 373px**, three exactly
  equal columns. `.grid4`'s gutters, `border-top`/`border-left` and the
  per-cell `border-right`/`border-bottom` are untouched, so the rule box closes
  on the third cell with no orphan line.
- the narrow breakpoint for `.grid5` went `1fr 1fr` → `1fr`: two columns would
  strand the third cell alone on a second row. Below 820px only.

**4 · The anatomy MAP.**

    var MAP = [['an1','gbPrint'], ['an2','bkPrint'], ['an3','brPrint']];

01 Parts reads `gbPrint` again (v11 had put `ctPrint` there). The `an4`/`amPrint`
and `an5`/`lnPrint` rows are gone. `window.anatomyState()` now returns three
rows, all filled.

**5 · Comments** — the CSS header, the MAP header and the slide's HTML comment
said "FIVE-UP"; all three re-cut to say three. No code in them.

**The print bakes, checked, as asked.**

- `lnPrint` (the line/robots) — **still consumed**: slide 5's print row
  `#s4 #lwPrint,#s5 #lnPrint,#s6 #ctPrint,#s7 #brPrint,#s8 #cpPrint` prints it
  on the s5 card. Kept, untouched.
- `ctPrint` (the catalogue) — **still consumed** by slide 6's card, on screen
  and in print. Kept, untouched; slide 6 is pixel-identical (looked at).
- `amPrint` (the arm) — **no consumer left**. `an4` was the only one. The arm
  IIFE, `#amHost` and its print row are **left in place, inert-but-baking**
  (187,118 B at load), exactly as lane P left the gearbox: nothing references
  the result, and removing it is a separate, reversible cut. Flagged below.

No print row was removed. No slide but 10 was touched.

## RULING-SHAPED QUESTIONS

1. **01 Parts = the gearbox** was read off Dave's screenshot. #290's word put
   the *catalogue* in that cell and moved the gearbox out of the deck. Both
   drawings answer to "Parts". Which one is Parts?
2. If the gearbox is Parts on 10, what is the catalogue on 6 — still "the
   inventory"? The two cards now show different objects for what the copy on
   10 calls "The inventory: 137 components in code."
3. The cell copy is verbatim from the five-up, written for five. "Three things
   she is made of" over Parts / Knowledge / Proficiency — does the footnote's
   three roles (inventory, assembly engineer, inspector) still read as the same
   three, or does the foot want re-cutting now that the grid is also three?
4. **Tools** (the host agent in the designer's editor) and **Process**
   (runbooks) left the deck with their cells. Neither claim is made anywhere
   else in the twelve. Are they dropped from the story, or do they want a card?
5. The arm drawing now bakes a print nothing consumes (as the gearbox did
   before this lane). Retire it — IIFE, `#amHost` and its print row — or leave
   it warm against a Tools card returning?
6. The gearbox is now drawn ONLY off-screen: on the deck it exists as a
   150px-tall still in a grid cell. It was built with an orbit spring, a
   30 fps cap and an exact mesh proof. Does it want a card of its own back, or
   is the thumbnail its whole job now?

Nothing above is settled and Dave has accepted none of it; the only thing he
said is quoted at the top.

## COUNTS:

- slides (`section.slide` in the DOM): **12** (before: 12)
- `class="slide"` occurrences in the file: **13**; `<section` occurrences:
  **13** (both unchanged)
- pagenum blocks: **12** (unchanged); slide 10 still reads `10 / 12`
- page errors (Chromium `pageerror` + console `error`, load + scroll through
  s4/s5/s6/s10, 1440×900, DPR 2): **0**
- anatomy cells on 10: **3/3 filled** — an1←gbPrint 197,890 B (900×630),
  an2←bkPrint 427,198 B (1200×840), an3←brPrint 353,558 B (1474×1368)
- `#s10 .grid4` computed columns: **373px 373px 373px**
- `an4`, `an5`: absent from the DOM (both `false`)
- all eight print bakes after load, all `data:image/png`: gbPrint 197,890 ·
  bkPrint 427,198 · brPrint 353,558 · ctPrint 478,566 · lwPrint 303,274 ·
  lnPrint 320,226 · amPrint 187,118 · cpPrint 218,190
- `#gb` canvas: parent `gbHost`, 900×630, bounding rect left **−3000px**
- file: **7642 lines** (before: 7637), delta **+5**
- screenshots: **3** — `notes/_lanes/291/P2/s4.png`, `s6.png`, `s10.png`

Looked at all three. **s10**: three equal cells under "Three things she is made
of.", the gearbox (three-gear train on its base plate, red pitch circle, "1 :
6.43 : 1" scale bar) in 01 Parts, books in 02, brain in 03; the rule box closes
on the third cell — no orphan rule where 04 and 05 were, no gap at the right.
**s4**: the two workers at the conveyor, unchanged, red workpiece on the belt.
**s6**: the catalogue on its stand, unchanged, one red cell on the right page.

## REPLAY-THESE:

    # preserve, then build (idempotent from the preserved copy)
    cd /sessions/.../mnt/UX-design
    cp notes/_lanes/291/P2/v12-before-p2.html \
       notes/_DEMO-SLIDES-apollo-2026-09-20-v12.html
    python3 notes/_lanes/291/P2/build_p2.py        # 17 ok lines, 7637 -> 7642

    # counts
    grep -c 'class="slide' notes/_DEMO-SLIDES-apollo-2026-09-20-v12.html   # 13
    grep -c '<section'     notes/_DEMO-SLIDES-apollo-2026-09-20-v12.html   # 13
    grep -n 'gbHost\|an4\|an5\|grid5{' notes/_DEMO-SLIDES-apollo-2026-09-20-v12.html

    # render + probe (Chromium in the sandbox)
    export LD_LIBRARY_PATH=$HOME/.local/lib
    python3 notes/_lanes/291/P2/shoot.py           # 0 page errors, 3 cells

    # in the page, by hand
    window.anatomyState()                    // 3 rows, an1 from gbPrint
    document.getElementById('gb').parentNode.id          // 'gbHost'
    getComputedStyle(document.querySelector('#s10 .grid4')).gridTemplateColumns

Files changed: `notes/_DEMO-SLIDES-apollo-2026-09-20-v12.html` (in place).
Files added: `notes/_lanes/291/P2/v12-before-p2.html` (the preserved pre-edit
copy), `notes/_lanes/291/P2/build_p2.py`, `notes/_lanes/291/P2/shoot.py`,
`notes/_lanes/291/P2/s4.png`, `s6.png`, `s10.png`, and this report. Nothing
committed; no `rm`, no `git checkout --`; no strays.
