# `#225`-`carries-home` — `s225-D2` enacted: the carry list left the banner for `_CARRIES.md`

session: `#225` · 2026-08-30
window: `#225` conductor window — Opus enactment sub
sub index: `carries-home`
brief: conductor's inline brief (no `notes/_briefs/` file was minted for this lane — DECLARED, not omitted)
tokens: `UNMEASURED — this seat cannot read its own message.usage; the conductor holds the figure`

## VERDICT

**DONE, and the measured problem is closed for exactly one wrap.** The `#225` carry line left
`GOOD-MORNING.md`'s ★ LATEST banner by a `_gm_move.py` MOVE — **byte-identical, sha256 verified
against `git show 3c1582c:GOOD-MORNING.md`** — into a new root home, `_CARRIES.md`, which is
indexed and fetchable as `carries:residual-225`. The banner now carries a pointer, the retrieval
id, the count and **the command that recomputes the count**. `_gen_chain.py --check` and
`--selftest` are **GREEN**, and the ratio the whole exercise was about went **52.55% → 34.12%**
against its untouched `<40%` floor. Nothing was dropped: all **34** carries are in the new home,
verbatim, with their receipts and their age brackets. ⛔ **But the fix does not survive the next
roll.** The `<40%` assertion divides the whole `_CHAIN.md` — of which **12,741 tape is
`_gen_chain.py`'s own wrapper, a fixed cost in the numerator only** — by the whole of GM. When
the #224 ★ PRIOR banner (**27,210 tape**) rolls to `_GM-ARCHIVE.md` at the #226 wrap, GM falls to
**≈38,700** and the ratio returns at **≈55%, RED**. A leaner GM makes this ratio worse. That is
the finding the conductor must carry to Dave, and it is question 1 below.

COUNTS: findings `9` · ruling-shaped `3` · UNPROVEN `3`

## What was done

Region by region, in the brief's order. All paths repo-relative to `/Users/daviewen/Documents/Claude/Projects/UX-design`.

1. **Read `s225-D2` from the store first** (`knowledge/_rulings.json`), not from the brief.
2. **`_CARRIES.md` (NEW, root)** — the new home. Root, beside `_GM-ARCHIVE.md` /
   `_LIVE-STATE-ARCHIVE.md`, whose naming voice and newest-first shape it copies. Carries a
   four-clause contract header (verbatim-or-nothing · newest-first · pointer+probeable count ·
   nothing ever deleted), the WHY with its measurement, a birth-declared warning about the
   blinded carry gate, and one `## residual → #225` section.
3. **`GOOD-MORNING.md:32`** — the carry line MOVED out and a pointer line inserted in its place,
   both in **one `_gm_move.py` transaction** (`--dry-run` first, receipts read back):
   `✔ MOVE 1 ln GOOD-MORNING.md['> **residual → #225:**'…] → _CARRIES.md` ·
   `✔ INSERT 1 ln into GOOD-MORNING.md before '> ⛔ **THE CARRIES STRUCK AT #212…#223'`.
4. **`GOOD-MORNING.md:9` — the `size:` stamp re-stamped**, iterated to a fixed point on the
   artefact. It is graded against the gate's own measurement at 10% tolerance and would have
   failed by 39%. Only the **GM** figure moved; §A and LS are untouched and the stamp says so.
5. **`_CHAIN.md`** — regenerated by `python3 knowledge/_gen_chain.py` (never by hand); `--check`
   GREEN, `--selftest` all bites pass.
6. **`knowledge/_build_memento_index.py`** — two edits so the pointer resolves: one
   `parse_sections(records, errors, "_CARRIES.md", "carries", "carries-section")` call beside the
   two archives, and `"carries-section"` added to the `expected` kinds set (so an empty class
   reads as a broken parser, the same posture as every other source). Index rebuilt LAST.
7. **`knowledge/_RUNBOOK-capture-ritual.md` §2c** — a new ⛔ block telling future wraps to write
   the carry set to `_CARRIES.md` and put a pointer on the banner: the three sub-steps (i) write
   or MOVE, (ii) the pointer's required shape, (iii) re-stamp `size:`; plus two ⚠ declarations —
   the blinded carry gate, and the #226 ratio forecast with its arithmetic.
8. **NOT touched, deliberately:** `knowledge/_gen_chain.py` (no mechanical need — removing the
   line from GM removes it from the chain; the `0.40` is untouched and unread by this work) ·
   `knowledge/_capture_gate.py` (no false-red — see finding 5) · `knowledge/_gm_move.py` (it has
   no carry-specific arm; the generic `move` op did the job) · `_LIVE-STATE.md` ·
   `knowledge/_rulings.json` · `knowledge/_state.json` · any constant, band or stop line.
   **No commits, no pushes.**

## Findings

1. **THE RATIO, BEFORE AND AFTER — both sides on one measurer (`_capture_gate.measure_tokens`,
   tier `tiktoken cl100k_base`).**
   **BEFORE** (HEAD `3c1582c`, via `git show`): chain **45,656** / GM **86,879** = **52.55%** — RED.
   **AFTER** (working tree): chain **21,346** / GM **62,569** = **34.12%** — GREEN.
   GM **−24,310** · chain **−24,310** (the line sat on both sides, which is why one move fixed both).
   Probe: `python3 knowledge/_gen_chain.py --selftest` →
   *"✓ is materially smaller than GOOD-MORNING.md (21,346 vs 62,569 real, 34.1% of it, floor <40%)"*.

2. **NO CARRY WAS DROPPED, AND IT IS PROVEN BY SHA, NOT ASSERTED (`s214-D6`).** The line now in
   `_CARRIES.md` is byte-identical to `GOOD-MORNING.md:32` at `3c1582c`: **83,439 B**, sha256
   `e6f0bcf948f78366d521a0efcaf30a8cc53dcead46bbf55ed56929401078fdb9`, diffed against the
   pre-move extract AND against `git show`. **34 carries** by the repo's own definition,
   `_capture_gate._carry_items()`.

3. **THE COUNT IN THE BANNER IS PROBEABLE, AND THE PROBE WAS RUN.** The banner carries the
   command verbatim; executed from the repo root it prints **34**:
   `python3 -c "import sys;sys.path.insert(0,'knowledge');import _capture_gate as c;print(len(c._carry_items(next(l for l in open('_CARRIES.md',encoding='utf-8') if '**residual → #225:**' in l))))"`
   It uses the gate's own carry definition rather than a second counter, so the banner and the
   `s188-D2` gate can never mean different things by "a carry".

4. **RETRIEVAL REACHES IT.** `python3 knowledge/_memento_search.py --fetch carries:residual-225`
   returns `[carries:residual-225] kind=carries-section · source: _CARRIES.md:46`. The index
   reports `carries-section:2` records among 1,839. The banner names that exact id, so the
   pointer is followable and not decorative [[roll-pointer-is-not-an-absence]].

5. **`_capture_gate.py` DOES NOT FALSE-RED ON THE NEW SHAPE, SO IT WAS NOT TOUCHED — and that
   was checked by running it, not by reading it.** `carry_wording_check()` finds the LATEST
   residual line by prefix, gets **0** aged items from a pointer line, and therefore *skips*
   every pairing: it emits a NOTE, never a fail. `stale_top_item_check()` finds no owed-work
   vocabulary near the two `s225-D2` citations. `roll_claim_check()` grades the *generated*
   residual line (`residual (GENERATED #224)`), which was not touched. Measured:
   **before 3 fail / 15 warn → after 2 fail / 18 warn**.
   The **fail** that disappeared is the pre-existing STALE retrieval-index fail, healed by the 2g
   rebuild. The two remaining fails are the pre-existing wrap-owed date zones
   (`GOOD-MORNING.md` / `_LIVE-STATE.md` "does not carry today"), unchanged by this work.
   Of the three extra warns, **exactly one is this lane's**: `REGEN SERIAL (advisory): … 25 serial
   member(s) INSIDE that span were NOT re-run` — the declared cost of a targeted enactment that is
   forbidden to run `_build_all.py`; its own message names the remedy (*"say in the wrap record why
   this wave legitimately stops short"*). The other two are `FILED SUB-REPORT UNCITED` for
   `2026-08-30-225-context-system-review.md` and `2026-08-30-225-gumdrop-single-source.md`, filed
   by parallel #225 lanes.

6. **⛔ THE FIX BUYS EXACTLY ONE WRAP — MEASURED, NOT FEARED.** GM decomposes today as
   header **2,461** + ★ LATEST **3,350** + ★ PRIOR **27,210** + rest **29,548** = **62,569**. At
   the #226 wrap, 2c archives the #224 ★ PRIOR banner (27,210, most of it its own carry line) and
   writes a new pointer-shaped LATEST at about today's 3,350. GM ≈ **38,709**; the chain barely
   moves (**21,346**, of which **12,741** is `_gen_chain.py`'s wrapper, the one term the generator
   itself says *"no editing of GM or `_LIVE-STATE` can move"*). Ratio ≈ **55.1% — RED**.
   ★ **A leaner GM makes this ratio WORSE.** The assertion compares a near-constant numerator to a
   denominator the ritual is designed to shrink; every successful compaction pushes it toward red.
   That is question 1.

7. **THE `s188-D2` CARRY GATE IS NOW BLIND, AND — WORSE — IT GOES QUIET AT #226.**
   `carry_wording_check()` (BLOCKING) pairs `[N]`→`[N+1]` between GM's ★ PRIOR and ★ LATEST
   residual lines. This wrap it is still LOUD, because the PRIOR banner holds #224's real list:
   *"2c carry gate: NOTHING PAIRED … ⛔ THAT IS NOT A PASS"* and *"32 aged carries on the PRIOR
   residual, 0 paired into LATEST"*. From #226 both banners hold pointers, `prior_items` is empty,
   and the loud branch (`if prior_items and checked == 0`) cannot fire — it will report a
   clean-looking `0 aged carries`. **A BLOCKING gate that has stopped being able to fail**
   [[instrument-without-a-consumer]]. ⛔ **NOT FIXED HERE — it is outside this lane's write
   fence** (the fence licensed `_capture_gate.py` only for a false-red, and there is none). The
   change is small and local to one function: point `_residual()` at `_CARRIES.md`'s two newest
   `## residual → #N` sections instead of GM's two banner regions; everything downstream
   (`_carry_items`, `_carry_title`, `retraction_receipt`) is unchanged. Declared at birth in
   `_CARRIES.md`'s header and in the runbook so it cannot become a silent hole.

8. **ONE THING HAD TO STAY ON THE BANNER, AND IT IS NOT A DROP — IT IS A BLOCKING DEPENDENCY.**
   `_gen_titles.py::extract_top_residual()` derives NEXT-TITLE from the first ⬛ bullet's bolded
   clause **on GM's `residual → #N` line**, and `title_generation_check()` is BLOCKING with the
   receipt as its only witness. A pointer with no ⬛ bullet would have taken the next wrap red in a
   file this lane may not edit. So the pointer keeps the top item's headline, and it still parses:
   `extract_top_residual(gm, 224)` → `'RATIFY AND BAKE v1.0.3'`, identical to the receipt already
   on disk. The cost is the one duplication in this design (that headline lives in both GM and
   `_CARRIES.md`) — question 3.

9. **THE `size:` STAMP WAS THE ONLY OTHER GATED SURFACE THAT WENT STALE, AND ITS SIBLINGS ARE
   PROSE.** Fixed here (generated-and-graded): GM's `size:` stamp, re-measured to a fixed point on
   the artefact, with the GM-only scope of the re-measure stated in the stamp itself.
   ⚠ **DECLARED, NOT FIXED — prose homes that quote GM's old size and are nobody's generator:**
   (a) `_gm_usage.py --sizes` reports GM in a **different unit** (98,785 at #224 vs the gate's
   86,747) and will now report a different number again — its figures live in the #224 gauge
   stratum and in `notes/_GAUGE-LOG.md`, which are **dated history and must not be re-denominated**
   (`s192-D1` / ADR-0017); (b) the `size:` stamp's own #224 collision paragraph still quotes
   `98,785` / `86,747` / `86,736` — left standing deliberately, it is a true record of that wrap;
   (c) `_GM-ARCHIVE.md` and the archived strata quote GM sizes throughout — history, frozen.
   ⚠ **I wrote the stamp's date as "30 August 2026", NOT the ISO form, on purpose:** the wrap
   gate's LATEST-zone freshness check keys on `YYYY-MM-DD` inside the first 40 lines, and an ISO
   date here would have made a non-wrap edit satisfy a check that exists to prove a wrap happened.

## RULING-SHAPED QUESTIONS

1. **THE `<40%` CHAIN/GM RATIO IS STRUCTURALLY UNSATISFIABLE ON A LEAN GM — what replaces it?**
   Measured: numerator ≈ **12,741 fixed wrapper + ~8,600 slice**; denominator shrinks every wrap
   by design. #226 lands at ≈**55%**.
   (a) **Cut the wrapper** — the only lever inside the machinery. It needs **>~9,000 of 12,741
   tape** out of `_gen_chain.py`'s generated prose to hold `<40%` at #226, and more later; that
   prose is ratified guidance a cold session reads.
   (b) **Re-define the denominator** so the ratio compares like with like — e.g. chain against
   GM's *chain-relevant* regions (header + banners), not against a file that is mostly retrieval
   surface the chain was cut to stop paying for.
   (c) **Re-base or retire the `0.40` floor.** ⛔ Dave's constant; named here for completeness, not
   recommended by a sub.
   **Recommend putting (b) to Dave with (a) priced beside it:** (b) is the one that stops the
   assertion punishing the ritual for working, and it is a re-point of a definition rather than a
   cut to ratified record — the same shape as the #33 / #48 re-points this file already survived.
   ⛔ I did not touch the constant, the assertion or the denominator.

2. **"ITS OWN GENERATED HOME" — is `_CARRIES.md` generated ENOUGH?** As built it is a **mover
   destination**, not a generator's output: `_gm_move.py` writes it verbatim, exactly as
   `_GM-ARCHIVE.md` is written, and the banner's count is *probeable* rather than *minted*
   (the brief allowed either). (a) **Leave it** — the property Dave asked for (nothing typed
   twice, count never trusted) holds, and no new script joins the regen serial. (b) **Build
   `_gen_carries.py`** that mints both the file's section and the banner's pointer line from one
   source, so the pointer cannot drift from the list. **Recommend (a) now, (b) if the pointer ever
   drifts** — a generator is the right answer to an observed drift and an expensive answer to a
   hypothetical one.

3. **THE TOP-ITEM HEADLINE IS DUPLICATED (banner + `_CARRIES.md`) — accept, or re-point
   `_gen_titles.py`?** ADR-0017 (`s192-D1`) says live facts get one home. Finding 8 explains why
   the duplication exists: a BLOCKING title gate reads that headline off the banner.
   (a) **Accept it** and say so in the runbook (done): the banner's copy is the *hand-off*, the
   file's copy is the *record*. (b) **Re-point `_gen_titles.py` at `_CARRIES.md`** and let the
   pointer drop the headline — one home, but it touches a file outside this fence and makes the
   banner useless to a cold reader who wants to know what is next. **Recommend (a).**

## CONSEQUENCES AND PITFALLS (Dave #165)

- ⛔ **DO NOT "FIX" #226's RED BY MOVING THE ★ PRIOR CARRY LINE TOO.** It is still on the banner
  (`GOOD-MORNING.md`, the `residual → #224` line, **22,347 tape**) and moving it is the obvious
  next reflex. ⛔ **It would not help — it would hurt.** The chain slices GM only as far as the end
  of ★ LATEST, so the PRIOR line is **not in the numerator at all**: GM would fall to ≈**40,470**
  and the chain would stay at **21,346** — **52.7%, straight back to red, for nothing.** The
  LATEST line was in BOTH sides; the PRIOR line is in the denominator only. That
  asymmetry is the whole reason this move worked, and it does not repeat.
- ⛔ **DO NOT TOUCH THE `0.40`.** It is Dave's, and `_gen_chain.py`'s own comment block says a red
  there is a ruling, not a patch.
- ⚠ **A BLOCKING GATE IS NOW BLIND AND WILL GO QUIET (finding 7).** If nothing lands before #226,
  the `s188-D2` "AGES +1, WORDING UNCHANGED" invariant is prose again — the state it was in before
  #189 — and a wrap must hold it by hand. This is the single highest-value follow-up in this
  report.
- ⚠ **THE NEXT WRAP MUST NOT RE-TYPE THE LIST ONTO THE BANNER.** The runbook now says so in §2c,
  but the pull is strong: every past wrap did it. The pointer's required shape is spelled out
  there, including the ⬛ headline that `_gen_titles.py` needs.
- ⚠ **`_CARRIES.md` HAS NO `knowledge/_state.json` ROW, AND NEITHER DOES THIS REPORT.** The store
  is outside this lane's write fence. The doc-row gate's population is
  `notes/_briefs` · `_BRIEF-*` · `notes/_subreports/*.md`, so **this report WILL red
  `_gate_doc_rows.py --check` at pre-stage** until it is rowed; `_CARRIES.md` at root is outside
  that glob and will not, but the **#185 forgotten-document class** applies to it all the same —
  it should get a row on its own merits [[forgotten-document-class]].
- ⚠ **ONE ADVISORY WARN IS THIS LANE'S** (finding 5): the regen serial ran 3 members, not 25. Say
  in the wrap record why this wave legitimately stops short — the warn asks for exactly that.
- ⚠ **THE GATE'S OWN SELFTEST IS RED AT HEAD, AND IT IS NOT THIS LANE'S DOING.**
  `python3 knowledge/_capture_gate.py --selftest` exits **1** on two bites: *"M10: a fat §A/§C
  warned the CHAIN — the re-point did not take"* and *"M10: an ordinary chain warned — the budget
  fires on everything"*. **Proven independent of this work two ways:** (i) `git status` shows
  `knowledge/_capture_gate.py` **unmodified**; (ii) driving `_warns_for(td)` directly on an empty
  temp dir (its only file is a synthetic GM fixture) reproduces the warn —
  *"M10 read chain OVER THE BLOCK-CANDIDATE … 12,914 tape … PAST the block-candidate 10,000"*.
  **Cause: the `_gen_chain` wrapper alone (12,741) now exceeds `CHAIN_BUDGET_TK`'s
  block-candidate (10,000), so EVERY fixture chain warns and both controls fail.** It is the same
  fixed wrapper behind question 1, showing up in a second instrument. Pre-existing; not fixed here.
- ⚠ **`apollo-spider/gumdrop/runbooks/_RUNBOOK-capture-ritual.md` IS DIRTY AND IS NOT MINE** — the
  `s225-D3` Gumdrop lane. This lane edited only `knowledge/_RUNBOOK-capture-ritual.md`. ⚠ If the
  Gumdrop pack ports the capture ritual, the new §2c block will need porting too, and `_CARRIES.md`
  does not exist in a Gumdrop project — **do not ship the §2c block into the pack unmodified.**

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN — the #226 forecast is arithmetic on today's regions, not a measurement of a future
  tree.** It assumes 2c archives exactly one PRIOR banner and that #226's LATEST is written at
  about today's 3,350 tape. A fatter #226 banner moves the number; the *sign* does not change,
  because the wrapper is fixed and GM only shrinks. Price to prove: one dry `_gm_move.py` roll +
  one `_gen_chain.py` run on a scratch copy, ~4k tokens.
- **UNPROVEN — I captured the baseline gate COUNTS (3 fail / 15 warn), not the baseline warn
  LIST.** The attribution of the two `FILED SUB-REPORT UNCITED` warns to parallel lanes is by
  filename and date, not by a list diff. Price to prove: one `git stash`-free re-run against a
  `git archive HEAD` copy, ~3k tokens plus disk.
- **CLAIMED — the `size:` stamp's unit word.** It still spells `tk`/`real`, inherited from #224,
  while the tier this environment actually reaches is `tiktoken cl100k_base`. The ds-021 `tape`
  re-spelling is a standing WARN on that stamp and was **not** taken on here: changing the unit
  word is a ds-021 motion, not a `s225-D2` one.

## Evidence

No evidence directory: every claim above quotes its probe inline, and the two durable artefacts
(`_CARRIES.md`, the `GOOD-MORNING.md` pointer line) are in the tree. The `_gm_move.py` ops file
and the pre-move extract were written to gitignored scratch at
`_to_delete/_225-carries/` (`ops-225-carries-home.json`, `orig-line32.txt`, `pointer.txt`) —
⚠ **scratch, so it does not outlive the window; the sha256 in finding 2 is the durable receipt,
and it is re-derivable at any time from `git show 3c1582c:GOOD-MORNING.md`.**

REPLAY-THESE: `python3 knowledge/_gen_chain.py --check` (~0.3 tk) · `python3 knowledge/_gen_chain.py --selftest` (~2.5k tk) · the count probe in finding 3 (~0.2 tk) · `python3 knowledge/_memento_search.py --fetch carries:residual-225` (~1.5k tk) · finding 7 — the blinded `carry_wording_check()` re-point, OWED (~4k tk to enact) · finding 6 / question 1 — the #226 ratio forecast, Dave's (~2k tk to re-derive)
