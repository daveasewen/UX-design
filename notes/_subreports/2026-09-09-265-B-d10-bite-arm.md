# `#265`-`B` — `s263-D10`'s rulings-page wrap check gets a registered bite arm

session: `#265` · 2026-09-09
window: lane B (Dave asleep — this lane rules nothing)
sub index: `B`
brief: `IN-CHAT — no file under notes/_briefs/ for #265` (the brief was cut in the conductor's
message, not filed; declared rather than invented)
tokens: `UNMEASURED — no `message.usage` reader is reachable from a lane sub's tool surface`

## VERDICT

DONE, both regions of the brief. `rulings_page_freshness_check` (BLOCKING at birth by `s263-D10`,
named as the only structural gap in `notes/_lanes/263-E-enact-d7-d10.md` item 4) now has a
registered failure class in the gate's own regime — `selftest_rulings_freshness()` in
`knowledge/_capture_gate.py`, wired into `_selftest_body()` — and a runner in `knowledge/_tests/`
— `case_capture_gate_rulings_arm()` in `test_gates.py`, called from `main()`. The arm carries
four bites: the out-loud SKIP, the never-generated page, the GREEN control (fresh page passes AND
says FRESH), and the defect itself (page behind the store ⇒ RED, naming the fix command). The
CLAUSE, not the fixture, is what is graded: three separate FAIL-OPEN edits to the check body were
applied to copies of `_capture_gate.py` and the arm turned RED on every one, with an unmutated
control copy staying GREEN in the same run. The `test_gates.py` registration was driven both
ways too — PASS on the real tree, FAIL against a fail-opened `_capture_gate.py`. No `fresh_copy`
and no copytree was taken anywhere: the fixture is two files the arm writes itself, and the
meta-mutation uses symlinks plus one 638 KB source file, so the ~5 GB ENOSPC hazard in the brief
is not touched at all. ⚠ `_capture_gate.py --selftest` ENTIRE was NOT re-driven (it exceeds the
sandbox call wall) — declared, not claimed.

COUNTS: findings `5` · ruling-shaped `2` · UNPROVEN `2`

## What was done

**Region 1 — the registered arm.** `knowledge/_capture_gate.py`:

- new `selftest_rulings_freshness()` inserted immediately after `selftest_index_freshness()`
  (its sibling — the same #32 freshness class), at line 8108. Four bites, one fixture repo the
  arm builds from nothing in a `TemporaryDirectory`: `knowledge/_rulings.json` (one synthetic
  ruling) and `notes/_RULINGS.html` (rendered by the real `_render_rulings.build`).
- registered in `_selftest_body()` at line 9736, on the `selftest_index_freshness()` line, so a
  run of `--selftest` executes it by construction.
- 75 insertions, 0 deletions. Nothing existing was edited.

**Region 2 — the `knowledge/_tests/` runner.** `knowledge/_tests/test_gates.py`:

- new `CG_ARM_SRC` + `case_capture_gate_rulings_arm()` after `case_selftest_arms()` (line 232 ff),
  calling the arm in a subprocess and `record()`-ing the result, so a failure counts toward the
  suite's `nfail` and turns the build red.
- called from `main()` (line 542), between `case_selftest_arms()` and the `CASES` loop.
- 27 insertions, 0 deletions.

**Region 3 — evidence.** Four driven receipts + the drive scripts + the patch, in
`notes/_subreports/assets/2026-09-09-265-B-d10-bite-arm/`. Lane scratch (identical files) in
`notes/_lanes/265/B/`, per the brief.

## Findings

1. **The brief's `CASES` shape CANNOT hold this check, and the reason is structural.** A `CASES`
   row is `(mutate a copy of knowledge/) → (python3 knowledge/<script>.py) → (exit != 0 + marker
   in stdout)`. This check fails that shape twice over: **(a)** it is a *function* inside
   `wrap_checks()`, not a script with an exit code — `python3 knowledge/_capture_gate.py` with no
   mode is REFUSED by the argv contract (`REFUSED (write-gate)`) and never reaches `wrap_checks()`
   at all; **(b)** it is REPO-scoped — it reads `knowledge/_rulings.json` **and**
   `notes/_RULINGS.html`, while `fresh_copy` copies `knowledge/` alone, so a `CASES` copy has no
   `notes/` for a page to be stale against. Probe: `knowledge/_tests/test_gates.py::bite` lines
   240–252 and `fresh_copy` lines 76–99. This is written into the code as a comment above
   `CG_ARM_SRC` so the next reader does not retry it. The registration therefore took the house's
   OTHER precedent — `selftest_index_freshness` / `selftest_plan_block_check`, the two wrap checks
   that already own arms — plus a `case_*` runner in `_tests/`, which is where the brief wanted
   the registration to live.

2. **The arm grades the CLAUSE: all three fail-open edits are caught.** Probe:
   `assets/…/02-meta-mutation-drive.txt` (`python3 notes/_lanes/265/B/drive_meta_mutation.py`,
   exit 0). `if rc != 0:` → `if False:` ⇒ bites 1 and 3 FAIL. `fails.append(…STALE…)` →
   `notes.append(…)` (the silent blocking→advisory demotion) ⇒ bites 1 and 3 FAIL. An early
   `return fails, notes` before the check reads anything ⇒ **all four** bites FAIL. The unmutated
   control copy, loaded through the identical importlib path in the same process, returns `[]`.

3. **The GREEN control is load-bearing, deliberately.** Bite 2 asserts the check goes green on a
   fresh page AND reports `FRESH` in its notes — the DV-D17 / #32 lesson quoted in
   `selftest_index_freshness`'s own docstring. Mutant 3 (the gutted check) proves it: deleting the
   comparison entirely kills bite 2 as well as bite 3, so a revert cannot pass by removing the
   feature. Probe: the four `[FAIL]` lines under FAIL-OPEN 3 in `02-meta-mutation-drive.txt`.

4. **The registration in `_tests/` bites, driven both directions.** Probe:
   `assets/…/03-test-gates-case-drive.txt` (exit 0). ARM A runs the real
   `case_capture_gate_rulings_arm()` and it records `PASS`. ARM B runs the case's own subprocess
   shape against a temp `knowledge/` of symlinks whose `_capture_gate.py` is fail-opened; the
   case's own predicate (`returncode == 0 and "[FAIL]" not in out`) evaluates to `FAIL`. Without
   ARM B the new case would be an assertion that cannot fail.

5. **The disk hazard in the brief does not apply on this mount, and was avoided regardless.**
   Probe: `assets/…/04-shape-and-disk.txt` — `df -h` on the workspace mount reports **33% used,
   309 G available**, not 89%; the 89% figure is the Mac side. No copytree was taken by any drive
   in this lane: the fixture is two written files, and the meta-mutation writes one 638 KB source
   file per mutant beside symlinks. Peak scratch across the whole lane is under 3 MB. Also in that
   receipt: both files `py_compile` clean, and `git diff --stat` = `102 insertions(+)`, `0`
   deletions across the two files.

## RULING-SHAPED QUESTIONS

1. **Should `knowledge/_tests/test_gates.py` run more of `_capture_gate.py`'s arms than this
   one?** The suite now reaches exactly one of ~35 `selftest_*` arms — the one this lane owed.
   Option (a) leave it at one named arm (cost: 0; the other 34 stay reachable only through
   `--selftest`, which no CI job and no wrap runs, and which every recent wrap declares as a skip
   because it exceeds the sandbox call wall). Option (b) add a `case_capture_gate_arms()` that
   loops a NAMED LIST of cheap arms the same way (cost: ~2–4K to pick the list + one drive per
   arm; risk: an arm that needs live state turns the suite red for a reason that is not a gate
   regression). Option (c) run `--selftest` whole in the `gates` CI job only, where the call wall
   does not apply (cost: minutes of CI per run; makes the suite one that cannot pass locally —
   the `[[gate-cannot-pass-in-one-environment]]` class aimed at the bite harness itself).
   **Recommend (b)**, because it is the only option that adds coverage without adding an
   environment the suite cannot pass in — but the list is a judgement about which arms are
   "cheap", and that is Dave's word, not a lane's.

2. **Does `rulings_page_freshness_check`'s unimportable-generator branch need an arm too?** The
   branch that fails when `_render_rulings.py` cannot be imported is DECLARED unproven in the new
   arm's docstring: the fixture tree carries no generator and the check's own `import
   _render_rulings` resolves off `sys.modules`, so no fixture can exercise it honestly in-process.
   Option (a) leave it declared (cost: 0; the branch stays untested, and it is the branch that
   fires only when the repo is already broken in a way every other gate also sees). Option (b)
   prove it by driving the check in a subprocess with a poisoned `sys.path` (cost: ~3K + a second
   process shape in a file whose whole design is in-process, and it re-opens the sandbox
   call-boundary lesson `_capture_gate` states in two docstrings). **Recommend (a)** — but this
   is a coverage/complexity trade-off with two defensible sides, so it is filed here rather than
   settled.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** `python3 knowledge/_capture_gate.py --selftest` was NOT re-driven whole, so it is
  not established that the suite is green END TO END with the new arm in the chain — only that
  the new arm is green in isolation, that both files compile, and that the registration line is
  syntactically reachable. The four prior wraps (#246, #247, #248, #252) all declare the same
  skip for the same reason (it exceeds the ~178 s sandbox call wall). Price to prove: one CI run
  of the `gates` job, or a local run on a box without the call wall — ~0 tokens, but it cannot be
  done from this seat.
- **UNPROVEN:** the unimportable-`_render_rulings` branch of the check (RSQ 2 above) — price to
  prove: ~3K tokens plus a subprocess drive shape this file's design argues against.
- **CLAIMED:** that lane E's hand-driven RED arm and this registered arm grade the SAME defect.
  Read from `notes/_lanes/263-E-enact-d7-d10.md`'s quoted output (a mutated `_rulings.json` byte
  ⇒ `STALE … embeds <sha> but _rulings.json is now <sha>`) and matched against bite 3's fixture
  mutation, which produces the identical failure string from the identical code path — but lane
  E's own scratch trees were removed after its drive, so its receipt is prose, not a re-runnable
  artefact. Re-read costs: nothing further is re-readable; the new arm is the durable replacement.
- **CLAIMED:** `_render_rulings.build()` on a one-ruling fixture exercises the same
  `source-sha256` embed path as the real 653 KB store. Read from `_render_rulings.py` lines
  233–270 (the `<!-- source-sha256:%s ... -->` line is the first thing `build` appends,
  unconditionally) rather than by diffing a real render. Re-read costs ~1K.

## Evidence

`notes/_subreports/assets/2026-09-09-265-B-d10-bite-arm/` —
`01-arm-unmutated-GREEN.txt` (the new arm driven in-process on the real tree: 4/4 OK, exit 0);
`02-meta-mutation-drive.txt` (the CLAUSE proof — control GREEN + three fail-open mutants all
caught RED, exit 0); `03-test-gates-case-drive.txt` (the `_tests/` registration driven both
directions — PASS on the real tree, FAIL against a fail-opened check, exit 0);
`04-shape-and-disk.txt` (`py_compile` clean, registration lines with their line numbers, `df -h`,
`git diff --stat`); `05-diff.patch` (the whole change, 102 insertions, 0 deletions);
`drive_meta_mutation.py` and `drive_test_gates_case.py` (the two drives, re-runnable — each takes
under two seconds and copies nothing).

Identical copies in lane scratch: `notes/_lanes/265/B/`.

REPLAY-THESE: `assets/2026-09-09-265-B-d10-bite-arm/02-meta-mutation-drive.txt` (~900 tk) · `assets/2026-09-09-265-B-d10-bite-arm/03-test-gates-case-drive.txt` (~350 tk)
