— 140 steps read from _build_all.STEPS by ast (never imported) · per-step timeout 40s · non-mutating steps only

  ✅ [ 3] token blast-radius + graph report — determinism/staleness chec
  ✅ [ 4] token blast-radius + graph report selftest (#79 P6)
  ✅ [ 9] help-gate selftest — 5 mutation bites (#158 class gate)
  ✅ [11] assertion veracity selftest — verbs bite + registry well-forme

==============================================================================
SURVEY: 4 pass · 0 FAIL · 0 COULD-NOT-ASK (self-declared refusals) · 0 unaskable (missing/timed out) · 8 not asked (mutating) · 128 outside --range 1:12 (not asked)
⚠ 'not asked' is NOT 'passing' — it is excluded from the exit code, deliberately.
⚠ 'COULD-NOT-ASK' is NOT 'passing' either. It is a step that named the input it could
  not reach, so the job does not go red for an ENVIRONMENT fact dressed as an artefact
  verdict (#173/#183, generalised #193). Every one is printed in full below. A step
  that timed out or is MISSING is a different thing and still fails: nobody asked it
  and it never said why.
⚠ A RANGED SURVEY IS A PARTIAL VERDICT: only a full pass over all steps, or consecutive ranges covering them, says anything about the build.

NOT ASKED (8 mutating steps) — run --include-mutating on a clean tree to include them:
  [1] compliance knowledge graph
  [2] token blast-radius + graph report
  [5] guideline rules index (gate)
  [6] runbook index (generated)
  [7] standing-instructions reachability gate
  [8] help-gate — no entry point may write before it reads argv (#158 cl
  [10] assertion veracity gate — claims that can rot
  [12] capture/provenance gate — status+provenance on new notes+dossiers 

real	0m28.411s
user	0m27.878s
sys	0m0.077s
— 140 steps read from _build_all.STEPS by ast (never imported) · per-step timeout 40s · non-mutating steps only

  ✅ [14] gauge-tokens selftest -- counting path + cache + degraded-meas
  ✅ [15] roll-state measurer selftest (#77 T1)
  ✅ [16] commit-seam harness — _git_commit.sh fixture-repo arms (#78-D1
  ✅ [17] GM/LS mover selftest — hardened move mechanics (M5)
  ❌ [18] section-usage instrumentation selftest (#23)  exit 1
  ✅ [20] lane records determinism check (O1′ #24)
  ✅ [21] lane records selftest — schema refusals + routing contract (O1
  ✅ [26] worklist store selftest — schema presence gates incl. priority
  ✅ [28] decision-graph selftest — conflict gate bites on unresolved/op
  ✅ [29] spine-writer selftest — splice invariants + no-half-writes (#7
  ✅ [38] component-partials sync — AUTO-PARTIAL injection + contracts (
  ✅ [39] component-partials selftest (ADR-0013)
  ✅ [40] token-set distribution sync — AUTO-TOKENS injection (s121-D1)

==============================================================================
SURVEY: 12 pass · 1 FAIL · 0 COULD-NOT-ASK (self-declared refusals) · 0 unaskable (missing/timed out) · 14 not asked (mutating) · 113 outside --range 14:40 (not asked)
⚠ 'not asked' is NOT 'passing' — it is excluded from the exit code, deliberately.
⚠ 'COULD-NOT-ASK' is NOT 'passing' either. It is a step that named the input it could
  not reach, so the job does not go red for an ENVIRONMENT fact dressed as an artefact
  verdict (#173/#183, generalised #193). Every one is printed in full below. A step
  that timed out or is MISSING is a different thing and still fails: nobody asked it
  and it never said why.
⚠ A RANGED SURVEY IS A PARTIAL VERDICT: only a full pass over all steps, or consecutive ranges covering them, says anything about the build.

THE FULL FAILURE SET (1) — this is the number no single `_build_all.py` run can tell you:

  [18] section-usage instrumentation selftest (#23)
      python3 knowledge/_gm_usage.py --selftest   (exit 1)
      [_gm_usage selftest] FAIL:

NOT ASKED (14 mutating steps) — run --include-mutating on a clean tree to include them:
  [19] lane records — regenerate LS §🛤 view (O1′ #24)
  [22] cross-reference index
  [23] sutherland acceptance fixtures
  [24] states-completeness probe (advisory)
  [25] worklist store gate — open items state what would close them; opti
  [27] decision-graph — typed edges + conflict gate (advisory, ADR-0012)
  [30] _LIVE-STATE staleness gate + lifecycle-block generation (advisory,
  [31] advisory signals — prose rules (advisory)
  … and 6 more

real	0m4.622s
user	0m3.197s
sys	0m1.070s
— 140 steps read from _build_all.STEPS by ast (never imported) · per-step timeout 40s · non-mutating steps only

  ✅ [41] token-set distribution selftest (s121-D1)
  ✅ [43] Behaviour contract selftest (ADR-0015)
  ✅ [45] canon components determinism check (ADR-0013 ruling 4)
  ✅ [47] partial-ratchet selftest (ADR-0013)
  ✅ [50] theme cascade sync — [data-apollo-theme] layer (ADR-0011)
  ✅ [51] theme cascade selftest (ADR-0014)
  ✅ [53] state-snap selftest (ADR-0014)
  ✅ [55] stale-queue selftest — 13 bites incl. both mutation directions
  ✅ [57] palette-tier selftest — 10 mutation bites (s157-D2)
  ✅ [59] showroom sync — generated component library (RULED 2026-07-21)
  ✅ [60] showroom URL-rebase selftest — the srcdoc base-URL trap (2026-
  ⊘ [61] dashboard sync — generated progress dashboard (RULED #164, bui  COULD-NOT-ASK
  ✅ [67] a11y target-measurement selftest (s114-D5)
  ✅ [70] wiring gate selftest — 4 bites

==============================================================================
SURVEY: 13 pass · 0 FAIL · 1 COULD-NOT-ASK (self-declared refusals) · 0 unaskable (missing/timed out) · 16 not asked (mutating) · 110 outside --range 41:70 (not asked)
⚠ 'not asked' is NOT 'passing' — it is excluded from the exit code, deliberately.
⚠ 'COULD-NOT-ASK' is NOT 'passing' either. It is a step that named the input it could
  not reach, so the job does not go red for an ENVIRONMENT fact dressed as an artefact
  verdict (#173/#183, generalised #193). Every one is printed in full below. A step
  that timed out or is MISSING is a different thing and still fails: nobody asked it
  and it never said why.
⚠ A RANGED SURVEY IS A PARTIAL VERDICT: only a full pass over all steps, or consecutive ranges covering them, says anything about the build.

COULD-NOT-ASK (1) — each step's OWN words about the input it could not reach.
These do NOT fail the survey; they also do not pass. If one of them is here because the
environment is wrong rather than because the input is genuinely unreachable, FIX THE ENVIRONMENT:

  [61] dashboard sync — generated progress dashboard (RULED #164, built #165)
      python3 knowledge/gen_dashboard.py --check   (exit 77)
      COULD-NOT-ASK: dashboard/index.html — the page differs, but a live gate baked into it is reading INPUTS OUTSIDE THE COMMITTED TREE, so its verdict — and therefore this page — cannot be reproduced here: provenance names `_inscribe_ruling.py`. Every one of those paths is untracked, so an environment that has them and one that does not will render different prose from the SAME commit. This is a REFUSAL, not a staleness verdict in either direction: the page's agreement with the stores is UNKNOWN here and is reported as unknown. Ask it where those inputs are present (a working tree that holds them), or commit them.

NOT ASKED (16 mutating steps) — run --include-mutating on a clean tree to include them:
  [42] Behaviour contract gate — dv-behaviour size + banned patterns (ADR
  [44] canon components — regenerate from snippets (ADR-0013 ruling 4)
  [46] partial re-implementation ratchet (ADR-0013)
  [48] token projection sync — snippets/tranches/canon literals (gen_snip
  [49] snippet gate
  [52] state-snap gate — opacity states snap to the active theme's ramp (
  [54] stale-queue gate — §C·1 qprobe claims re-measured vs disk + git (B
  [56] palette-tier gate — every theme names a palette per family, no div
  … and 8 more

real	0m6.044s
user	0m2.844s
sys	0m0.555s
— 140 steps read from _build_all.STEPS by ast (never imported) · per-step timeout 40s · non-mutating steps only

  ⊘ [74] state-contrast gate selftest — paint stack, report arithmetic,  COULD-NOT-ASK
  ✅ [76] type-composites selftest
  ✅ [85] KG edge gate selftest (6 bites)
  ✅ [87] Token fork-ban selftest (2-direction)
  ✅ [91] binds-resolve selftest — 5 bites incl. any-store clause (#146)
  ✅ [93] chart-intent resolve selftest — 4 bites incl. list case + abse
  ✅ [95] DataViz gate selftest — bite-tests dv-004 + the dtype vocabula
  ✅ [97] property-resolves gate C2 selftest — 4 bites + a bite-the-bite
  ✅ [99] dataviz var-resolution gate selftest — 8 arms incl. alias chai

==============================================================================
SURVEY: 8 pass · 0 FAIL · 1 COULD-NOT-ASK (self-declared refusals) · 0 unaskable (missing/timed out) · 21 not asked (mutating) · 110 outside --range 71:100 (not asked)
⚠ 'not asked' is NOT 'passing' — it is excluded from the exit code, deliberately.
⚠ 'COULD-NOT-ASK' is NOT 'passing' either. It is a step that named the input it could
  not reach, so the job does not go red for an ENVIRONMENT fact dressed as an artefact
  verdict (#173/#183, generalised #193). Every one is printed in full below. A step
  that timed out or is MISSING is a different thing and still fails: nobody asked it
  and it never said why.
⚠ A RANGED SURVEY IS A PARTIAL VERDICT: only a full pass over all steps, or consecutive ranges covering them, says anything about the build.

COULD-NOT-ASK (1) — each step's OWN words about the input it could not reach.
These do NOT fail the survey; they also do not pass. If one of them is here because the
environment is wrong rather than because the input is genuinely unreachable, FIX THE ENVIRONMENT:

  [74] state-contrast gate selftest — paint stack, report arithmetic, named args
      python3 knowledge/_validate_state_contrast.py --selftest   (exit 77)
      COULD-NOT-ASK: _validate_state_contrast.py — the 'playwright' module is not installed (ModuleNotFoundError("No module named 'playwright'")) — this gate cannot be proven without it; run `pip install playwright && playwright install chromium` ⇒ THIS IS NOT A SKIP: these arms (including the s152-D1 mutation control) run BLOCKING in the `render` job of .github/workflows/gates.yml, which installs chromium — that job is where this gate's proof of record lives. Nothing here is claimed green; the question was unaskable on this box.

NOT ASKED (21 mutating steps) — run --include-mutating on a clean tree to include them:
  [71] compose gate (orphan wired #119)
  [72] screen gate — compose+icons+a11y over fitness fixtures (re-wired #
  [73] state-contrast gate — driven hover/pressed, light+dark (un-exempte
  [75] type-composites ratchet — tier (b), debt 1101 shrink-only (Dave #1
  [77] coverage gate
  [78] pro-forma universal gate
  [79] pro-forma CSS-governed motion gate (DEF-003)
  [80] pro-forma no-hardcode styling gate (DEF-004)
  … and 13 more

real	0m1.248s
user	0m1.122s
sys	0m0.058s
— 140 steps read from _build_all.STEPS by ast (never imported) · per-step timeout 45s · non-mutating steps only

  ✅ [101] minted-consumption selftest — 10 arms incl. both false-positiv
  ✅ [103] hidden-attr display selftest — 16 bites incl. the real #218 mu
  ✅ [108] consult tool selftest (advisory)
  ✅ [109] search core selftest — two-stage engine + receipt format (O2′ 
  ❌ [111] memento index determinism check (O2′ #25)  exit 1
  ✅ [112] memento index selftest — contract refusals + determinism (O2′ 
  ✅ [113] memento search selftest — known-answer retrieval + fetch refus
  ✅ [115] graph mention map determinism check (#115 step 0)
  ✅ [116] graph mention map selftest — hit/miss/refusal bites (#115 step
  ✅ [118] read chain determinism check — stale _CHAIN.md serves a PREVIO
  ❌ [119] read chain selftest — verbatim terms + the CUT + refusal on a   exit 1
  ❌ [121] memento schematic determinism check — a stale diagram publishe  exit 1
  ✅ [122] memento schematic selftest — figure provenance, caption geomet
  ✅ [124] memento-package delta-audit selftest — mutation-tested, all 4 
  ✅ [126] instrument-fit selftest (advisory, ds-015)
  ✅ [129] could-not-ask convention selftest (#193 — the third verdict's 
  ✅ [132] governs matcher selftest — the [12]/[13] trigger-index arm, na
  ✅ [133] frozen-release gate — v1/v2 and any baked Spider surface may n
  ✅ [134] frozen-release selftest — 11 bites on a fixture repo incl. the
  ✅ [135] pack ship-list audit — the manifest is byte-identical to a fre
  ✅ [136] release-audit selftest — manifest compare + the pack arm's res
  ✅ [137] pack-side CI template — parses, ships what it calls, hides not
  ✅ [138] pack-side CI template selftest — 10 mutants incl. a smuggled c

==============================================================================
SURVEY: 20 pass · 3 FAIL · 0 COULD-NOT-ASK (self-declared refusals) · 0 unaskable (missing/timed out) · 17 not asked (mutating) · 100 outside --range 101:140 (not asked)
⚠ 'not asked' is NOT 'passing' — it is excluded from the exit code, deliberately.
⚠ 'COULD-NOT-ASK' is NOT 'passing' either. It is a step that named the input it could
  not reach, so the job does not go red for an ENVIRONMENT fact dressed as an artefact
  verdict (#173/#183, generalised #193). Every one is printed in full below. A step
  that timed out or is MISSING is a different thing and still fails: nobody asked it
  and it never said why.
⚠ A RANGED SURVEY IS A PARTIAL VERDICT: only a full pass over all steps, or consecutive ranges covering them, says anything about the build.

THE FULL FAILURE SET (3) — this is the number no single `_build_all.py` run can tell you:

  [111] memento index determinism check (O2′ #25)
      python3 knowledge/_build_memento_index.py --check   (exit 1)
      (no ✗/FAIL line — see full output)

  [119] read chain selftest — verbatim terms + the CUT + refusal on a blank GM (#41)
      python3 knowledge/_gen_chain.py --selftest   (exit 1)
      ✗ is materially smaller than GOOD-MORNING.md (47,915 vs 94,376 real, 50.8% of it, floor <40%; tier measured, not assumed: real)

  [121] memento schematic determinism check — a stale diagram publishes a PREVIOUS tree (#127)
      python3 knowledge/_gen_schematic.py --check   (exit 1)
      ✗ reviews/MEMENTO-SCHEMATIC-2026-08-07-v2.html is STALE — a figure in it no longer matches disk, so the page is asserting something that has gone fals

NOT ASKED (17 mutating steps) — run --include-mutating on a clean tree to include them:
  [102] hidden-attr vs author-display sweep — painted while hidden (ADVISO
  [104] reverse-text edge-extremity check {#col26-020} (advisory)
  [105] compliance verification edges — applies_to vs verified_by (advisor
  [106] external automatable-check refs — axe-core import (advisory)
  [107] consult index — problem-domain query surface
  [110] memento index — the Memento door's corpus (O2′ #25)
  [114] graph mention map — the decision-graph/memento-index join (#115 st
  [117] read chain file — _CHAIN.md, the cold-start door (#41)
  … and 9 more

real	1m22.163s
user	0m39.472s
sys	0m4.364s
