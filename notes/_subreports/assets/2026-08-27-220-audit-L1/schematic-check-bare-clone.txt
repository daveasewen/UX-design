COULD-NOT-ASK: reviews/MEMENTO-SCHEMATIC-2026-08-07-v2.html — MEASUREMENT REFUSAL, NOT A CONTENT VERDICT IN EITHER DIRECTION. The committed page carries figures in ['real']; the only tier reachable in THIS environment is `tape (cl100k ESTIMATE)` (cl100k). Every token figure would differ for that reason alone. ⚠ `real` is reachable ONLY via `API-KEY.txt` or `knowledge/.token-cache.json` and BOTH ARE GITIGNORED (#173, proven by single-variable isolation), so a bare checkout can never agree with a `real` page. Give this environment the same measurer, or re-ask where it is reachable — this artefact's content is UNKNOWN here and is reported as unknown rather than guessed.
