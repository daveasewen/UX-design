# DRIVE G2 — meta reverted to PROSE while the snippet still carries the derived block — orphan refused
# fixture: /dev/shm/l2fix (rebuilt by fixture_build.py in this call)
$ python3 knowledge/gen_component_partials.py --check
gen_component_partials CONTRACT FAILURES:
  X behaviour-manifest/date-picker: knowledge/snippets/Date-picker.reference.html carries a #behaviour-manifest block but knowledge/components/date-picker.meta.json has no TYPED behaviour (PROSE) — the block is DERIVED and has no home: type the meta, or remove the block
rc=1
# EXPECT rc=1 and: ['carries a #behaviour-manifest block but', 'has no TYPED behaviour (PROSE)']
# VERDICT: as expected ✅
