# DRIVE C2 — …and the GENERATOR refuses the same meta (the derived block cannot be minted from a bad address)
# fixture: /dev/shm/l2fix (rebuilt by fixture_build.py in this call)
$ python3 knowledge/gen_component_partials.py --check
gen_component_partials CONTRACT FAILURES:
  X behaviour-manifest/date-picker: knowledge/components/date-picker.meta.json declares script `knowledge/canon/nope.js`, which names a file that does not exist (<FIXTURE>/knowledge/canon/nope.js)
rc=1
# EXPECT rc=1 and: ['behaviour-manifest/date-picker:', 'declares script `knowledge/canon/nope.js`, which names a file that does not exist']
# VERDICT: as expected ✅
