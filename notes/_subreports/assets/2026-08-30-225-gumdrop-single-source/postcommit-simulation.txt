POST-COMMIT SIMULATION — the dry-run stage with the three working-tree files copied in,
then the REAL stamp block (extracted verbatim from build-designer-pack.sh) driven over it.

carried-cut version stamped from the manifest: v1.0.3 — 4 staged file(s) carry the literal, 0 rewritten

pack-docs gate (ADVISORY) — 107 document(s), 415 name(s) resolved, 14 placeholder(s) skipped by name
  carried cut, as the shipped documents name it: Memento — Gumdrop v1.0.3 (×5) — internally consistent.
