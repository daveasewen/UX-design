✅ typed (live date-picker) valid                                   -> valid
✅ D2 partial = list of names valid                                 -> valid
✅ D2 partial = one name valid                                      -> valid
✅ D2 partial = [] RED (minItems 1)                                 -> RED: [] is too short
✅ D2 partial = ['Dv-Legend'] RED (grammar)                         -> RED: 'Dv-Legend' does not match '^[a-z][a-z0-9-]*$'
✅ D2 partial = ['a','a'] RED (uniqueItems)                         -> RED: ['a', 'a'] has non-unique elements
✅ D4 events absent still valid (optional)                          -> valid
✅ D4 events = 'click' (string) RED                                 -> RED: 'click' is not of type 'array'
✅ D1 script = bare filename RED                                    -> RED: 'Date-picker.reference.html#script' does not match '^knowledge/(?:[^#\
✅ D1 script = node-id grammar (b) RED                              -> RED: 'snippet:Date-picker.reference.html#script' does not match '^knowledge
✅ $fallbackRender ($-key) permitted                                -> valid
✅ stray key `notes` RED (additionalProperties)                     -> RED: 'notes' does not match any of the regexes: '^\\$'
✅ legacy PROSE string still schema-legal (gate blocks, s140-D1 posture) -> valid
schema arms 13/13 as expected
