# #304 Run 1 — CI sees to step 146: the abort chain cleared, the six mechanical reds fixed, the his-word reds left standing and named

provenance: 304 · 2026-09-26
status: observed
window: Run 1 lead, an Opus 5.5 sub of the #304 conductor, working at Dave's seat through `device_bash`; no Agent tool in this seat, so lanes 1a, 1b and 1c were done in series by the lead (not by Sonnet/Opus sub-lanes as the plan names)
tokens: `UNMEASURED — a lane cannot read its own message.usage`
COUNTS: findings 10 · ruling-shaped 5 · UNPROVEN 3 · clone survey before 127 pass / 15 FAIL / 4 CNA → after 130 pass / 9 FAIL / 7 CNA · 0 green steps turned red · 30 working-tree files changed · 0 commits · 0 pushes
CITES: s234-D5 · s245-D4 · s148-D1 · #193 · #208 · s177-D1 · P5 (2026-08-02) · c896bcaf · #74-D1 · s219-D4(2) · the #304 plan (notes/_lanes/304/F/page.src.html, Run 1) · notes/_subreports/2026-09-26-304-A4-health-and-dependencies.md

## THE ANSWER FIRST

The build no longer stops at step 8. In a full-history throw-away clone of `571d458c` with Run 1's changes applied, the whole 146-step survey (`_build_survey.py --include-mutating --resume --range`, sanctioned mode) went from **127 pass · 15 FAIL · 4 could-not-ask** to **130 pass · 9 FAIL · 7 could-not-ask**, and **no step that was green before is red after** (measured, by step name, `notes/_lanes/304/R1/verdicts-before-after.json`). All three abort-route reds are gone: `[8]` help-gate passes, and `[10]` and `[13]` now end in a declared could-not-ask (exit 77), which `_build_all.py:1396` treats as "build continues". So CI's `_build_all` should run to step 146 and exit red on the gate-route set only.

What is left red is exactly the set the plan calls Dave's: `[81]` the 7px badge, `[86]` the chart forks, `[127]`/`[128]` the two package copies, `[132]` the schema and `[94]` its six slugs. The other three FAILs, `[134] [135] [144]`, are advisory and never fail the build. Nothing was committed or pushed.

For CI's **step 5** (the non-mutating survey of the committed tree), the clone predicts **one FAIL, `[128]`**, but only if the commit seat runs the regen serial first. I rehearsed that: I ran the regen serial and committed through the real `_git_commit.sh` in the clone, and step 5 then read `[128]` alone.

Two of the plan's premises were wrong, and the fixes follow what the code shows rather than the brief:

1. **`[38]`: the snippet blocks were the newer truth, not the metas.** The #261 N lane hand-edited three blocks that the pipeline derives, so the metas are typed from the blocks.
2. **`[125]`: the schematic is not stale because HEAD advances.** It survives a commit. It is stale because its content figures change and nothing at the commit seam ever regenerated it. So the fix is a gate at the commit door, not the chain's HEAD pin.

## 1 · WHAT WAS FIXED, AND WHY (in the plan's fix order: [8] → pointers → the rest → ASSERT-009 last)

| step | real cause (probed) | fix | proof |
|---|---|---|---|
| `[8]` help-gate (abort) | 12 entry points had no `_help_gate` guard (A4's list; there are **seven** `_render/verify_demo_slides_268*` drivers, not six) | guard inserted directly after each docstring: plain form for the 5 `knowledge/` files, the walk-up form `_build_all.py:38-43` uses for the 7 `_render/` ones. The drivers are guarded, not retired (retiring them is Dave's decision 8) | `_validate_help_gate.py`: "help-gate OK — 264 script(s) scanned" (clone); `--selftest` 5 bites green; each of the 12 run with `--help` exits 0 and writes **0 files** (measured with `find -newer`); `_bite_kg_edge_proposal.py` still "BITE PASSED" |
| `[136]` governs selftest / `[13]` its embedded arm (abort) | `s282-D2..D5` each carried one bare evidence string with no legal pointer (`s148-D1`). D5's string names a DIRECTORY, which `PATHISH_RE` cannot read as a path | each string is prefixed `commit <sha> - ` with the commit that inscribed that exact measured text (`git log -S` on `_rulings.json`): D2 `bb016c69`, D3 `c143a1bf` (the corrected heights), D4 `da9f824e`, D5 `eaf12365`. Written **through the sanctioned writer** `_inscribe_ruling.py --amend-evidence` (reconstruction proof passed ×4; `says` untouched). ⚠ That tool writes the evidence array at its own indent (2/1 spaces, not the entry's 5/3); this is its fixed `compose_amend` behaviour, and the proof covers every other byte | `_governs.py --selftest` → "all bites green (4 pointer(s) UNASKABLE in this checkout)", rc 77; `_capture_gate.py --selftest` rc 77; `json.load` count **638**; `_inscribe_ruling --selftest` all arms green |
| `[38]` component-partials sync (gate) | **Premise probe, the #244 question: which is the source of truth?** `s234-D5` says the meta is the one home and the block is DERIVED. But git shows that `c0225a0e` (#261 N) hand-edited the DERIVED `#behaviour-manifest` block in **three** snippets (Navigations, Sidebar-nav, Tab-bar) and left the metas behind: navigations untyped, tab-bar's `events` missing `keydown`, and both fallbacks pre-#261. The blocks match each snippet's own `addEventListener` set (Navigations click+keydown · Sidebar-nav click+keydown · Tab-bar click+keydown+resize). The metas do not. Also `s245-D4`: events are "generated from the snippet, never authored" | the three metas are typed FROM the blocks, and prior values are kept verbatim under `behaviour.$r1_304` (by addition). Meta format round-trip verified byte-exact before writing. Then `gen_component_partials.py` re-derives the three blocks, a whitespace/escape-only diff (same content) | `gen_component_partials.py --check` OK; `--selftest` OK |
| `[82]` type-binding blast radius (gate) | the gate's matcher is approximate (every simple part present in the FILE). #261 N2 put `<button class="menu-fab">` BESIDE `<nav class="seg">` in Tab-bar. In the DOM there are **0 `<button>` inside any `.seg`** element (measured), so no Tab-bar element takes `.seg button` and no pixel moves | the gate's sanctioned remedy `--update`, diff reviewed: (+) Tab-bar on `.seg button`; (−) `.tabbar .tabbar__item` shrinks to `[]` (#261 N dropped that class; a shrink never fails); `_generated` date. A sentence appended to `.seg button`'s note records why | gate passed (28 selectors, corpus 151) |
| `[125]` memento schematic (abort in CI's step 5) | **Premise probe: the plan's "head-only advance" is not this defect.** I regenerated, COMMITTED, and re-asked `--check` → FRESH. The schematic renders no sha. It goes stale on CONTENT: `_state` rows, rulings, chain and GM size, dossiers, memento index, mark observations, mention map. All of these move on nearly every commit, and nothing at the commit seam regenerated it (last regenerated `f81bbdd4`, 09-21). Loosening `--check` to forgive figure drift would launder exactly what `c896bcaf`'s own M1 bite forbids | a **commit-door gate**, copied from the #208 mention-map precedent, added to `knowledge/_git_commit.sh` right after the mention-map gate (the schematic reads the map): fresh → pass; stale → regenerate the one file it owns, then **refuse unless the path is already named** (P5). A named path, or `--all-dirty`, rides in one run. It is one deliberate difference from #208, stated in the code comments. Exit 77 (its #193 tier refusal) is neither regenerated nor blocking. `SCHEMATIC_ACK` is the declared hatch. There is also an unconditional **second-half** assert after staging: differs from HEAD and not staged → refuse. Harness `_test_git_commit.py`: stub + fixture file + happy-path assertions + 2 arms + **2 mutation controls** | harness **48 arms ALL GREEN (11 mutation controls)**, both new mutations bite; W-33 stub detector green; **real-tree rehearsal in the clone**: an unnamed stale schematic was regenerated and REFUSED; the same command with the path named committed (`✓ done — locks clear`); `--check` FRESH afterwards; step 5 re-asked green |
| `[10]` assertion veracity (abort) | ASSERT-009 `count=138 (want eq 137)`: `legend.meta.json` @ `108493b6` (#261 L) is the only meta added since the #239 re-base | **done LAST**: `n` 137→138, claim "138 files … 142 entries", `last_verified`, provenance appended by addition, `knowledge/README.md:13` 137→138 (the #207 pattern). The script refuses unless the live count is exactly 138 / 142 | clone: `[10]` → could-not-ask (only ASSERT-007's mount root, which is unreachable off-seat, as in CI); selftest OK |
| `[3]` blast radius + graph report (CI step 5) | committed files stale (A4) | regenerated on the mount (`tokens/_build_blast_radius.py`), byte-identical to the clone's | `--check` PASS on the mount |
| `[59]` showroom sync (**collateral, caught by the after-survey**) | the three snippet edits staled `showroom/{navigations,sidebar-nav,tab-bar}.html`. No mutating step precedes `[59]` in the build, so the survey caught what the door's showroom gate would also have caught | `gen_showroom.py` (3 pages written, index untouched) on the mount, identical to the clone | re-asked `[59]` ✅; final survey green |

## 2 · THE CLONE SURVEY, BEFORE AND AFTER, BY STEP NAME

Clone: `$HOME/clone304r1`, `git clone --depth 1 file://…` + `fetch --unshallow` (1,632 commits), HEAD `571d458c` (= the mount's HEAD). BEFORE was run on this VM tonight and agrees with A4's fifteen names exactly. AFTER was run twice. First on the Run 1 sources, where it caught the `[59]` collateral. Then as the **final** run on commit `231c7ed0` (sources, the showroom regen, and a regen serial committed through the real door), 5 chunks, and every one of the 146 steps was asked in both runs.

| step | before | after (final) |
|---|---|---|
| `[8]` help-gate — no entry point may write before it reads argv | FAIL | **PASS** |
| `[10]` assertion veracity gate — claims that can rot | FAIL | **could-not-ask** (ASSERT-007 mount root only) |
| `[13]` capture/provenance selftest (Memento §4.1) | FAIL | **could-not-ask** (4 gitignored evidence paths; declared) |
| `[38]` component-partials sync — AUTO-PARTIAL injection + contracts | FAIL | **PASS** |
| `[82]` type-binding blast-radius gate (canon/type.css) | FAIL | **PASS** |
| `[136]` governs matcher selftest | FAIL | **could-not-ask** (same 4 unaskable pointers) |
| `[81]` 4px-grid gate | FAIL | FAIL — ⬛ Dave (7px badge) |
| `[86]` token fork-ban gate | FAIL | FAIL — ⬛ Dave (chart forks) |
| `[94]` roles/answers resolve gate | FAIL | FAIL — ⬛ Dave (closes with the schema) |
| `[127]` memento-package delta-audit | FAIL | FAIL — ⬛ Dave (the re-port) |
| `[128]` memento-package delta-audit selftest | FAIL | FAIL — ⬛ Dave (the re-port) |
| `[132]` integrity lint | FAIL | FAIL — ⬛ Dave (the schema) |
| `[134]` probe registry (advisory) | FAIL | FAIL, advisory |
| `[135]` claim-table evidence linter (advisory) | FAIL | FAIL, advisory |
| `[144]` pack ship-list drift (advisory, ⬛ re-cut) | FAIL | FAIL, advisory |
| `[61] [68] [73] [74]` (dashboard provenance; playwright not on the plain path) | could-not-ask | could-not-ask |
| every other step (127 → 130 names) | PASS | PASS |

**Predicted CI step 5** (non-mutating survey, 69 steps asked) on `231c7ed0`: **64 pass · 1 FAIL `[128]` · 4 could-not-ask `[13] [61] [74] [136]`**. On the sources WITHOUT the regen serial it read FAIL `[3] [113] [120] [125] [128]`. The regen serial is load-bearing (logs `after_nm_*` vs `after2_nm_*`).

Logs: `notes/_lanes/304/R1/before_*.log` · `after_*.log` · `after_59_reask.log` · `after_nm_*.log` · `after2_nm_*.log` · `final_*.log` · `verdicts-before-after.json` (per-step verdicts for all four runs) · `_PROGRESS.md`.

## 3 · FILES CHANGED IN THE WORKING TREE (30; nothing committed)

Every change is reproducible by an idempotent script in `notes/_lanes/304/R1/` (`guard.py`, `s282_pointers.py`, `type_nav_behaviour.py`, `seg_reseed.py`, `door_schematic.py`, `assert009_rebase.py`). A cmp against the clone's copies read identical for every file except `_rulings.json`, which Run 2 is also writing (my four pointers verified intact on the mount afterwards, "ALREADY POINTED", count 638).

- `knowledge/_bite_goal.py` · `_bite_kg_edge_proposal.py` · `_build_kg_explorer.py` · `_gen_ruling_edges_from_recs.py` · `_kg_history.py`: help-gate guard (`[8]`)
- `knowledge/_render/verify_demo_slides_268.py` · `_v3` · `_v5` · `_v6` · `_v7` · `_v74_gearbox` · `_v75_gearbox`: help-gate guard, walk-up form (`[8]`)
- `knowledge/_rulings.json`: s282-D2..D5 evidence gets its `commit <sha>` legal pointer, via `--amend-evidence` (`[136]`/`[13]`). ⚠ shared with Run 2
- `knowledge/components/navigations.meta.json` · `sidebar-nav.meta.json` · `tab-bar.meta.json`: behaviour typed from the #261 N blocks (`[38]`)
- `knowledge/snippets/Navigations.reference.html` · `Sidebar-nav.reference.html` · `Tab-bar.reference.html`: `#behaviour-manifest` re-derived by the generator (`[38]`)
- `showroom/navigations.html` · `sidebar-nav.html` · `tab-bar.html`: showroom regen after the snippet change (`[59]`)
- `knowledge/canon/_type-bindings.json`: `--update` re-seed with the diff reviewed, plus a note (`[82]`)
- `knowledge/_TYPE-BLAST-GATE.md`: the `[82]` gate's own report, rewritten each time the gate runs (derived; it is regenerated by the build anyway)
- `knowledge/_git_commit.sh`: the memento-schematic freshness gate and its second half (`[125]`, structural)
- `knowledge/_test_git_commit.py`: stub, fixture, 2 arms, 2 mutation controls for that gate
- `knowledge/tokens/_blast-radius.json` · `knowledge/_GRAPH-REPORT.md`: regenerated (`[3]`)
- `knowledge/_assertions.json` · `knowledge/README.md`: ASSERT-009 137→138, LAST (`[10]`)

New, untracked: this report, and `notes/_lanes/304/R1/` (logs, scripts, `_PROGRESS.md`, `verdicts-before-after.json`).

## 4 · FOR THE COMMIT SEAT (1d) AND THE VERIFIER (1v)

1. **Run the regen serial whole, after Runs 2 and 5 land and before the clone survey.** The minimum my changes need, measured: `tokens/_build_blast_radius.py` → `_build_memento_index.py` → `_build_graph_mention_map.py` → `_gen_chain.py` → `_gen_schematic.py`. Without them, step 5 reads `[3] [113] [120] [125]` red. `gen_showroom.py --check` is already green on the mount.
2. **The door now has a schematic gate.** Name `reviews/MEMENTO-SCHEMATIC-2026-08-07-v2.html` in the path list up front and a stale schematic is regenerated and rides in ONE run. Otherwise expect one refusal (as with the mention map, which may refuse once too).
3. ⚠ **Name `knowledge/_graph-mark-observations.jsonl` too.** The schematic counts its lines, every memento search appends to it (the mount's copy is dirty now, +61 lines or more), and the door auto-stages it only if its OWN gates appended during that run. If it is dirty but unnamed, the committed schematic is rendered against lines that are not committed, and CI's `[125]` goes red. This residual is declared in the gate's comments. It was committed at every recent wrap (`efe3bb47`, `c2d66ff0`, …).
4. **ASSERT-009 must stay the last edit.** If any run adds a `knowledge/components/*.meta.json` this wave, re-run `assert009_rebase.py` (it refuses unless the count is 138/142; bump it by hand to the new measured count).
5. **`_rulings.json` is written by Run 1 (evidence, four entries) and Run 2 (status).** Before committing, run `python3 notes/_lanes/304/R1/s282_pointers.py .`. It must print "ALREADY POINTED" ×4. If it does not, run it with `--write`.
6. **Verifier (1v) owed, per the plan: a Fable seat that did not write the door gate.** It should try to make the gate pass a stale schematic and fail a fresh one. The routes I know of are `SCHEMATIC_ACK` (declared by design), exit 77 (declared), and the unnamed-dirty-input residual in item 3.
7. **The CI verdict is x64 and the clone is arm64.** Read CI back until `render` closes and record the failure set by name.

## 5 · WHAT REMAINS RED, AND WHOSE

- ⬛ Dave: `[81]` (7px nav badge: move to 8px or exempt), `[86]` (four mono chart forks: declare or unify), `[127]`/`[128]` (port `_gen_chain.py` into both memento-package copies now or at the cut), `[132]` + `[94]` (the #261 metas against `meta.schema.json`: extend the schema or bring the metas back).
- Advisory, non-gating: `[134]`, `[135]`, `[144]` (the pack is 322+ commits behind; the re-cut is Dave's, `s219-D4(2)`).
- Seat-only, not in CI and not caused by Run 1: on the mount, `_validate_help_gate.py` also scans gitignored `knowledge/_tmp/wrap25x/carry26x.py` (3 unguarded scratch files, 385 scanned against 264 in a checkout). `_validate_assertions.py`'s selftest asserts `.auto-memory/MEMORY.md` exists under the mount root, which has been false since memory moved to the cloud at #278.
- Noted, not fixed: `.tabbar .tabbar__item` now binds nothing (#261 N renamed the class). Tab-bar labels carry `t-cm-legal` directly, so no gate reads it as a defect.

## RULING-SHAPED QUESTIONS

1. `[81]`: the nav count badge's `padding: 0 7px`. Move it to 8px, or exempt it from the 4px grid?
2. `[86]`: sparkline and bar resolve `--status-positive`/`--status-neutral` differently in mono. Declare the four forks in the ledger, or unify them?
3. `[127]`/`[128]`: port `knowledge/_gen_chain.py` into both memento-package copies now, or hold it for the next cut?
4. `[132]`/`[94]`: extend `meta.schema.json` to accept the #261 shapes, or bring the metas back to the schema?
5. Decision 8: retire the seven one-off `verify_demo_slides_268*` drivers to an archive folder instead of keeping them guarded?

## UNPROVEN / CLAIMED

- UNPROVEN: that CI (x64) matches the arm64 clone. The price is one push.
- UNPROVEN: the door gate has had no independent adversarial verification. Only my own mutation controls and a real-tree rehearsal have tested it. The Fable verifier is owed.
- UNPROVEN: CI's `[128]` "got 4" against the clone's 2. The CI line is truncated (carried from A4).
- Not done, by fence: no commit and no push on the mount; no `git status` on the mount; no `_build_all.py`; no `_capture_gate.py --wrap` on the mount (the door ran it only inside the clone); no `_state.json` or `_CARRIES.md` edits; nothing under `notes/_lanes/304/R5/`; no Project memory.

## `_state` ROW FOR THE COMMIT SEAT TO MINT

`id: W-304r1` · `home: notes/_subreports/2026-09-26-304-R1-ci-sees-to-146.md` · `title: "#304 Run 1 - CI sees to step 146: abort chain cleared, six mechanical reds fixed, his-word reds named"` · `owner: claude` · `opened: 304` · `project: apollo` · `links: [notes/_lanes/304/R1/verdicts-before-after.json, knowledge/_git_commit.sh]` · `closes_when: "the wave-1 commit carrying Run 1 is pushed, CI's gates job log shows _build_all asking step 146 with its failure set read back by name, and the Fable verifier (1v) has filed its report on the schematic door gate"`

REPLAY-THESE: § THE ANSWER FIRST (~0.6K tk) · § 1 table (~1.6K tk) · § 4 (~0.6K tk)
