# Sub-report — deck v8, lane G2 (slides 6–9) — #289 — 2026-09-19

Output: `notes/_lanes/289/deck/G2.html` — four `<section class="slide">` blocks, s6–s9, **no `<style>` block**:
every class used is already in v7's CSS (`.label .lead .foot .nums/.cell/.fig/.fig.small .nlab .ndesc .switch
.grid4 .ix .rv d1…d5 .pagenum .dark .grey`), and v7 has no id-scoped rules for s6–s9, so nothing collided.

- **s6** NEW · the library — "The bin didn't change. **It got catalogued.**" White, `.nums` three-figure card.
- **s7** the explorer — "She has read **all of it.**" Dark, re-cut of v7 s9, `.switch` kept.
- **s8** the build — "**Live build.**" plus gates-and-evals as the beat that checks her work. Dark, re-cut of
  v7 s7, `.switch` kept.
- **s9** what she is made of — four moving parts. Grey, `.grid4` re-cut of v7 s8.

Labels are chapter labels, no "Plot point NN": s6–s8 read "The evidence · 1 of 3 / 2 of 3 / 3 of 3" plus what
the slide is ("What she builds with / knows / does with it"); s9 is "What she is made of". `pagenum` is
`06 / 13` … `09 / 13`.

## Every figure and its source

All library figures were read at this seat from `showroom/index.json` (its `$generated_by` names
`knowledge/_render/gen_library_214.py`), with `python3 -c "import json;d=json.load(open('showroom/index.json'))…"`
over `$count`, `$component_count`, `$foundation_count`, `Counter(c['status'] for c in d['components'])`.

- **137 components** — `$component_count: 137`. s6's first figure. **145 entries in all** — `$count: 145`
  (137 + 8 foundations) — on the s6 footer.
- **8 foundations** — `$foundation_count: 8`. s6's third figure. The slide names "grids, logos, photography"
  because those are the actual foundation slugs (`foundation-grids-12col/-dashboard/-display/-gallery`,
  `foundation-bento`, `-bento-rails`, `foundation-logos`, `foundation-photography`) — not an invention.
- **111 stable / 34 beta** — counted over `components[].status`. Derived by the library, not by me: its
  `$status_derivation` key says stable = gated in the adopted itinerary measurement with no PROPOSED marker.
- **"a machine-readable record beside it"** — `knowledge/components/` holds **138 `*.meta.json`**
  (`ls knowledge/components/*.meta.json | wc -l`); 137 map one-to-one onto the index's components, the 138th
  is `EXAMPLE-button.meta.json`, a template. No number for this is on the slide — prose claim only.
- **4,820 nodes · 8,648 edges** — `notes/_STRAND-MAP-2026-09-19.html` §s4, "Measured": explorer builder
  **1.27**, generated 2026-09-19. On s7's lead and footer. v7's 2,837 / 4,902 is stale and is gone from
  my slides; the strand map says so in terms.
- **s9's four parts** — carried from v7 s8, whose footer credits the Apollo sponsor briefing, July 2026.

## What I did not use

- Canon: `knowledge/canon/` is 32 files — `canon.css` (30,481 lines), `type.css` (193), twelve `dv-render-*.js`,
  five `gen_canon_*.py`, two bindings JSONs. I counted 994 distinct custom-property names in `canon.css`
  (`grep -oE '^\s+--[a-z0-9-]+:' … | sort -u | wc -l`) but **kept it off the slide**: my count of declarations,
  not a figure the repo publishes.
- `102` components ship their own behaviour (`ships_behaviour` true). True, cut for room.
- Strand map §s5's 40 logo masters / 8 nodes / 33 edges — offered there as a craft proof, not mine to place.
  Flagged for the conductor in case s6 wants a second beat.

## Unsure of / left for Dave

1. **File path in a slide footer.** v7's CSS comment (line 94) says the cards "carry no file paths, no script
   names"; the brief tells s6 to "cite the file you read". The brief won — the s6 footer names
   `showroom/index.json` in plain text (no `<code>`; v7 has no rule for it). The one v7 house rule I knowingly
   broke. One deletion if Dave dislikes it.
2. **"Gates and evals" as one grid4 cell.** Evals are a line, not a fifth part, so v7's "Gates" cell was
   renamed and given the score sentence. `.grid4` is `repeat(4,1fr)` — a fifth cell would break it anyway.
3. **s9's fourth cell.** v7 said "The AI that operates it"; I wrote "The designer herself" to land the new
   arc's metaphor. My wording, not Dave's — two words to swap back.
4. **Caution ruling observed.** s7 says "More than any one human can hold"; "better than any human" appears
   nowhere in my four slides.
5. **s8 carries two `.lead` paragraphs** (cold run, then the gates beat) where every other slide has one.
   Still one idea — she builds, you check it — but the gates paragraph is the thing to demote if the conductor
   wants strict one-idea slides.

Did not touch v7 or any other file. Did not commit.
