# `#281`-`RL` — rests on, landed: `s281-D6` inscribed, 65 edges + 8 nulls from his 59-card export, explorer 1.25

session: `#281` · 2026-09-17
window: lane RL — rests on, landed
sub index: `RL`
brief: `notes/_lanes/281/rests-on-land/BRIEF.md`
tokens: `~220,000` — the difference between the remaining-budget counter reported to this lane at its
first tool call and at its last. `CLAIMED`, not `message.usage`: the seat does not hand
`message.usage` to a lane, so this is the closest honest reading available, and it is a budget
delta, which includes re-read context, not only new tokens.

## VERDICT

DONE. **`s281-D6` inscribed** (`git diff --numstat` = `28  0`, 612 rulings → 613) and enacted in one
version. `restsOn` is **many-to-many**: a rule may rest on more than one principle, and on the
thirteen rows where the radio took one answer and Dave's own sentence said two, **both options
landed, each with its own `$why`** — the sentence the card printed for that option, never a merged
one. **65 rule → principle edges over 52 of the 59 BLOCKING rules, plus 8 declared nulls** for the
rules that rest on convention alone, each carrying his note verbatim or the card's own null
sentence. **36 of the 73 lines carry his words, unedited.** `type26-002` carries BOTH the null and
option 1, because he chose convention and wrote "I think 1 is also applicable" in the same breath.

**His notes about the RULE are not rule edits, and no guideline `.md` was touched.** Fifteen of his
twenty-six notes argue with a rule's scope, grade, caveat or rationale rather than with the link.
They are filed unedited, one row each with the rule's own text and one sentence on what inscribing
them would change, in `notes/_lanes/281/rests-on-land/RULE-NOTES-2026-09-17.md`. **They are his to
rule in #282.**

Explorer **1.25**: the verb `rests-on` reads `restsOn` at force `should`, and a **`rests on` chip in
the THEORY box, OFF by default**, draws rule → principle. INSPECT's ↔ door reads the card's sentence,
the grade at the time of writing, the radio letter he saw, whether he overruled the recommendation,
and **his own note, verbatim**. Page errors `[]` in eight served contexts.

The default canvas md5 DID move, twice, and both causes are measured and separated below: the
inscription's own Constitution dots (as the brief predicted — 11 new nodes, 2,206 governance
coordinates re-solved) and, second, the **ghost stroke of the 73 new lines**, which moves **zero**
coordinates but paints 42,192 pixels at alpha 0.04. The drawn picture at the defaults is identical:
1,050 nodes, 1,384 lines, 111 dark dots, the same ids.

COUNTS: findings `6` · ruling-shaped `3` · UNPROVEN `4`

## What was done, in plain prose

**The count, re-counted from his export, agrees with the conductor's on every list.** 59 answers.
**8 convention** (`uxId: null` — `logo26-001 nam-001 photo26-002 pict-010 pict-011 type25-020
type26-002 webf-018`). **8 overruled** (`dv-009 dv-line-011 dv-pie-009 dv-pie-010 icon-005
neuro-026 pict-011 webf-027`). **26 notes**, of which **13 say the rule rests on both options** —
matched on `both 1 and 2` / `1 and 2` / `and 2`, the same thirteen ids the brief named, and the
lander asserts that set and exits non-zero if it ever moves — and **13 are about the rule**. The
two lists are disjoint and together account for all 26.

**The edges.** 51 rules land the principle he chose. 13 of those land a second, because his sentence
named both. `type26-002` lands a null AND option 1. That is **65 principle edges + 8 declared nulls
= 73 lines**, over **52 rules with at least one principle** and **24 distinct principles**.
`pr-wcag-1-4-3` carries 8, `pr-tog-readability` 7, `pr-nng-consistency` 6, `pr-wcag-perceivable` and
`pr-graphical-perception` 5 each. **The grade spread of the 65 is A 6 · B 11 · C 30 · L 18** — the
honest C is the common case, which is what s281-D3's "any grade is allowed" clause exists to permit,
and 16 of the 65 are options the page did NOT recommend.

**The eight nulls carry a reason, never a blank.** Five carry his own note (`logo26-001`,
`photo26-002`, `pict-010`, `type26-002` and, where he wrote none, the card's own null sentence for
`nam-001`, `pict-011`, `type25-020`, `webf-018`). A null without a note is refused by the lander,
which is `s275-D2`'s shape held rather than restated.

**The three ASKs**, named here and in the refinement list:
1. **`col26-012`** — "I'm actually not sure about this." His link (`pr-nng-consistency`) is landed
   and the note rides it; the open question is the rule's, not the link's — does RAG in charts carve
   out of the ban on red with the supporting palette?
2. **`aid-009`** — "maybe we need to discuss." His link (`pr-fitts`, grade A) is landed and the note
   rides it; the open question is whether 44px is measured on the visible target or on the hit area.
3. **`type26-002`** — "I think 1 is also applicable." Landed BOTH ways under `s281-D6`. The open
   question is whether he means the null to stand beside the principle or to give way to it.

**One borderline, not landed.** `type26-003`'s note reads "linked to 2 as you stated". That is not
the "both 1 and 2" his thirteen rows say, and inventing an edge out of an ambiguous sentence is
exactly what `s281-D3` forbids — so it is a fourteenth candidate, named in the refinement list, and
one word from him settles it.

**The refinement list, one line each** (`RULE-NOTES-2026-09-17.md`; full text and rule text there):
`aca-003` a top-level exception where active navigation already orients · `aid-009` visible target
versus hit area, and clear space as context · `col26-009` a brand-consistency reason beside the
contrast one · `col26-012` a named RAG carve-out and where primary red IS allowed · `dv-bar-007`
scoped to positive-only bars, the sink-below-zero form sanctioned · `icon-006` only the 16px floor is
perceptual; the ceiling keeps an icon from reading as an illustration · `logo26-001` native apps
satisfied by logon/splash, not every screen · `mot-005` the grade questioned — it is an
accessibility obligation as well as a control principle · `photo26-002` a review date on the gen-AI
clause, not an edit · `pict-001` the pictogram/icon boundary stated in his words · `pict-010` the
figures are enforcement against misuse, not a threshold · `type26-002` a link note, landed, and an
ASK · `type26-003` the borderline · **and two riders on a "both" answer**: `col26-016` a named RAG-red
exception for downward position movement in a stat card, and `neuro-026` unpaired icons allowed for
the most commonly understood set — which would put it directly against `pict-001`'s "no exceptions".

**Difference from the brief's count, named.** The brief said 13 rule-notes and then gave `col26-016`
and `neuro-026` as examples. Both of those are in the "both" thirteen: their note answers the LINK
and then states a caveat about the RULE. My count publishes **13 primary rows plus those 2 riders =
15**, with the link answers landed as edges and the caveats filed. Everything else agrees exactly.

## Findings

1. **`restsOn` could not be read in the pass that stores it.** It is filed in
   `knowledge/_rule_nodes.json` (pass C) and it points at a `ux:` node, which pass D mints. Read in
   C, every one of the 73 lines would have been counted `rule_edges_skipped` — an authored line
   disappearing into a counter nobody reads. The file is read in C and **linked in a new pass D2**,
   after the targets exist. This is the same class of defect as 1.21's `famOn[undefined]` and 1.22's
   "an edge with a family draws nothing when the dot at its end has no chip": the third time the
   pass order, not the data, was what would have swallowed an authored line.
2. **The chip gates the line INSTEAD OF its storage family, and the count follows in the same
   place.** `EON` asks a `restsOn` edge for `famOn.restson`; `recount()`'s new `ROON` asks the same
   question, so the header cannot disagree with the canvas the way 1.21's did for 14 lines. Measured:
   with `uxprinciples` + `guidelinerules` on and the chip OFF the header reads **2,572 relations and
   0 `restsOn` drawn**; with the chip ON it reads **2,637 and 65 drawn**. The difference is exactly
   65, in both numbers.
3. **The chip honestly reads 5 at the page defaults, not 65.** Its count is the lines it would
   GOVERN right now — both ends already on the stage — and at the defaults only 5 of the 130 ends are
   base nodes. That is `chipUXC`'s rule kept, not a bug: the dots are not on the stage, so neither
   are the lines, and turning the chip on at the defaults draws exactly those 5.
4. **Zero coordinates move because of the 73 lines.** `place_extra` solves each additive family's
   column on its own and the `rule:` and `ux:` dots were already placed; the base force layout never
   sees an extra edge. Measured node-for-node across the three builds: `noedges → ship` moves **0**
   of 4,770 nodes. The 2,206 that do move are `governance` and they move for **this lane's own
   inscription**, which is s281-D6's 11 new Constitution dots re-solving that column — the cause the
   brief predicted, and the only one that touches a coordinate.
5. **The md5 moved for a second reason the brief did not predict, and it is the ghost layer.** The
   canvas paints an off-chip edge at alpha 0.04, so 73 new lines appear as a faint haze at the
   defaults even though no chip draws them and nothing moved. Pixel diff, 1280×800 light, same
   script, one run: inscription **75,406** px · the 73 ghosts **42,192** px · total **95,170** of
   653,976. **Three md5s, published:** `b33ff55a7a510f06cc8e4b9f322704a4` (HEAD, 1.24) →
   `1ca47926c89ccff30e28fa884bb643be` (1.25, code + inscription, restsOn lines stripped) →
   `35c8e95914cc6e4a12d28fa1a7966e00` (1.25 as shipped). The drawn picture is identical in all
   three: 1,050 nodes, 1,628 relations in the header, 1,384 lines, 111 dark dots, the same ids.
6. **The verb map's own figures had already drifted, and one type is read by no verb.**
   `$measured` said 58 types / 8,087 edges / 135 nulls; today it is **60 / 8,527 / 140**, and
   `restsOn` is only part of the difference — lanes PH, FO and TV landed between #279 and now. Worse:
   **37 read + 22 unread = 59, one short of 60.** `inFamily` (145 lines, s281-D1, #281 lane PH) is
   read by no verb and carries no `unread` note. That gap is not `restsOn`'s and is **not closed
   here** — which verb reads a principle's research family is a reading decision, and this lane was
   told to add one verb, not to invent a second. It is written into `$measured.$drift` so the next
   reader finds it.

## RULING-SHAPED QUESTIONS

1. **Should an off-chip `restsOn` line be ghosted at all?** It is 42,192 pixels of the default
   canvas, and one line in the ghost branch (`if(ROC(e))continue`) would freeze the default canvas to
   1.24's byte for byte. The argument for shipping it as is: a ghost's only job is to tell the reader
   that a line exists and which chip would turn it on, and 73 new lines between the rule column and
   the principle column is *true*. The argument against: the default canvas is the page's contract
   and 1.24 already answered the smaller version of this question by implication (TV's Q2, 75 px).
   **Recommend as shipped**, and note that this is now the second lane to meet it — a general ruling
   on the ghost layer would settle it for every family still to land.
2. **Does the edge's carried `grade` go stale, and who notices?** s281-D3 says the line carries the
   grade, so it does, and 1.25 labels it "the principle's grade, **at the time of writing**" with the
   `ux:` node named as authoritative. Nothing re-checks it. A one-line builder assertion — carried
   grade equals the node's `grade`, printed when it disagrees — would make a re-grade visible instead
   of silent. **Recommend adding it in the lane that next touches the builder**; not done here,
   because a build-time refusal on Dave's own stored answer is a bigger decision than it looks.
3. **`gen_kg_rules.py` would delete all 73 lines.** `_rule_nodes.json` says "regenerate with
   `gen_kg_rules.py --land`; never hand-edit", and the `restsOn` block is hand-authored by ruling.
   The file's `$description` now says so and names the idempotent re-lander, but **the generator
   itself is not on this lane's file list and is unguarded**. The honest fix is one preserve-block in
   that generator. Until then this is a live footgun and it is named here rather than quietly
   accepted.

## PROOF

- **`_validate_kg.py` green**, after every change: 139 metas, 90 declared nulls, "every ref parses +
  resolves, every null carries a note, every meta has provenance, edges match schema".
- **Census, 1280 light, six never-driven cells**, `notes/_lanes/281/rests-on-land/shots/shots-after.json`:

  | cell | header | drawn | `restsOn` drawn | dark | errors |
  |---|---|---|---|---|---|
  | `default` | 1,050 / 1,628 / 21 types | 1,384 | 0 | 111 | `[]` |
  | `chipoff` (ux + HSBC rule:, chip off) | 1,711 / 2,572 / 33 | 2,448 | 0 | 0 | `[]` |
  | `restson` (the same, chip on) | 1,711 / **2,637** / 34 | 2,513 | **65** | 0 | `[]` |
  | `strata` | 1,711 / 2,637 / 34 | 2,513 | 65 | 0 | `[]` |
  | `allchips` | 2,541 / 5,125 / 46 | 4,985 | 65 | **8** | `[]` |
  | `phone` 390 | 1,711 / 2,637 / 34 | 2,513 | 65 | 0 | `[]` |

  **No dot goes dark.** 111 at the defaults is 1.24's number unchanged; the 8 at every-chip-on are
  s277-D7's logo lockups, leaves by design, unchanged since 1.23.
- **The chip lights 65 edges** and only the chip does: `chipoff` → `restson` is +65 relations in the
  header and +65 lines on the canvas, nothing else moves.
- **Two driven INSPECT passes**, read out of the modal, not asserted from source:
  - `rl-after-inspect-col26-016-1280-light.png` — the edge door on the FIRST of its two lines, and
    the node's own record beside it. It names **both** principles (`ux:pr-von-restorff`,
    `ux:pr-nng-consistency`), prints `ONE OF TWO: this rule rests on TWO principles`, the grade `B`
    at the time of writing, `option a on the card · which was the recommendation`, the card's
    sentence — and **his caveat, verbatim**: *"This is both 1 and 2, there is one caveat, red text is
    allowed, but only the RAG red when used for position movement downward, as in a stat card"*.
  - `rl-after-inspect-logo26-001-1280-light.png` — the declared null, read as
    `logo26-001 rests on — nothing: a declared null`, with the card's null sentence and **his
    rationale, verbatim**: *"So this is nuanced, the logo will appear on webpages in the nav header
    anyway but not on native apps, the rational is that the user summoned an HSBC app…"*.
- **Page errors `[]` everywhere**, eight served contexts, and zero console errors. The page is
  SERVED (`knowledge/_serve_explorer.py`), never opened from `file://`.

## GATES

Files touched, and nothing else: `knowledge/_rulings.json` (span insert only, `28  0`) ·
`knowledge/_rule_nodes.json` (`806  2` — the two deletions are the `$description` line and the
`edge_types` line that gained a comma; every existing edge is untouched) · `knowledge/_kg_verbs.json`
· `knowledge/_build_kg_explorer.py` · `knowledge/_kg_explorer.template.html` ·
`notes/_KG-EXPLORER.html` (rebuilt) · `notes/_lanes/281/rests-on-land/` · this report · one
`_state.json` row (`W-281rl`, appended by textual span, `26  0`).

**One file outside the Gates' list, and why.**
`notes/_lanes/281/rests-on/DAVE-EXPORT-2026-09-17.json` was in the working tree and **untracked**.
`s281-D6` cites it as its `says` and its first evidence line, so leaving it untracked would have
inscribed a ruling whose only proof was not in the repo. It is committed unchanged, and nothing else
under `notes/_lanes/281/rests-on/` is touched.

**No guideline `.md` changed** — checked by `git status`, and the refinement list is under
`notes/_lanes/`, which is where a question waiting on Dave belongs. **No node id and no existing edge
type moved**; `restsOn` is added to `edge_types` and to the `rests-on` verb's `reads`, and nothing
else in either file is rewritten. No `git stash`, no `gen_kg_edges.py`, no `_build_all.py`, no
`gen_kg_rules.py`, no lock moved (none was mine to move), no push.

Scratch: `/sessions/nifty-exciting-cerf/mnt/outputs/rl/` created and removed; the three throwaway
pages used for the md5 forensics were built under
`notes/_lanes/281/rests-on-land/_tmp/` and deleted before the commit. The `.py` files and the
`shots/` directory under `notes/_lanes/281/rests-on-land/` are **not** scratch — they are the
evidence, and they reproduce the landing and the census.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN: the dark colour scheme.** Every shot is `color_scheme='light'`, as the brief asked.
  The chip takes `--f-uxprinciples`, which has a dark-mode value, so it is expected to follow; not
  photographed. Price: the same pass with `color_scheme='dark'`, ~3 min.
- **UNPROVEN: the 3D cells.** Only force-2D, strata-2D and the phone sheet were photographed. The
  73 lines enter no coordinate set, so the other three cells are expected to be 1.24's to the byte
  plus the same ghost haze; not measured. Price: three more cells, ~3 min.
- **UNPROVEN: the links are RIGHT.** Every one of the 65 was proposed by one agent in one sitting at
  #281 lane RO and answered by Dave on a page. That he answered is proof of consent, not proof of
  correctness, and the 26 rows where he wrote a note are the ones where he said as much himself.
- **UNPROVEN: `gen_kg_rules.py` behaviour.** I did not run it (the Gates forbid it) and I did not
  read it far enough to prove it would drop the block — the claim in ruling-shaped 3 is inferred
  from the file's own "never hand-edit" header and from `generated_by`. Price: one read of that
  generator, ~2 min, and it should happen before anyone runs it.
- **CLAIMED:** the token figure in the header — see the note there.

## Evidence

`notes/_lanes/281/rests-on-land/` —
- `land_rests_on.py` — the lander. Reads his export and `proposals.json`, asserts the thirteen
  "both" ids against a written-down set, refuses a null with no note and refuses any end that does
  not resolve, and rewrites the whole `restsOn` block each run, so it is idempotent (verified: a
  second run leaves `806  2`).
- `inscribe_s281_D6.py` — the span inserter; pure append, idempotent, refuses on a changed tail.
- `RULE-NOTES-2026-09-17.md` — the refinement list, 15 rows, his words verbatim, **his to rule**.
- `shots.py` · `shots/` — the six never-driven cells, the two driven INSPECT passes, the legend clip,
  and `md5` mode (three pages hashed by one script at one viewport).
- `coords.json` — the node-for-node coordinate diff across the three builds (0 moved by the edges).
- `whymd5.json` — the pixel diff that separates the two causes.

REPLAY-THESE: `python3 notes/_lanes/281/rests-on-land/land_rests_on.py` (~200 tk — prints the counts,
the grade spread and the carrying principles) · `notes/_lanes/281/rests-on-land/RULE-NOTES-2026-09-17.md`
(~3k tk — the fifteen rows, and the three ASKs at the foot) · `source knowledge/_render/seat_env.sh
&& python3 notes/_lanes/281/rests-on-land/shots.py after` (~2k tk, six cells + two driven INSPECT
passes, ~4 min).
