# `#240`-`wrap` — the delegated capture ritual for #240: three rulings, a rolled record, and two builds carried

session: `#240` · 2026-09-02
window: conductor window (FABLE), delegated wrap sub (OPUS)
sub index: `wrap`
brief: `notes/_briefs/2026-09-02-240-delegated-wrap-brief.md`
tokens: `UNMEASURED — no message.usage at a sub's own seat; UNOBSERVABLE and DECLARED, never estimated`

## VERDICT

The capture ritual ran **COMPLETE** inside the brief's DO-NOT-RULE fence: steps 1, 1b, 2, 2c, 2d, 2f and
2g all executed, 2e was legitimately empty and is declared with its size, and step 3 (memory) was the
conductor's and is declared as ZERO bytes written here. Every roll went through `_gm_move.py` from
session-owned, uniquely-named ops files; nothing was hand-edited. **Nothing was ruled, no `s240-D*` was
re-worded, no `W-` row was closed, and no constant / band / floor / advisory / stop line / wall / roster
/ version moved in code** — `BOOT_FIRSTTURN_TK` is byte-untouched, so `s240-D1` is superseding its
constant **in principle and not in code**. Two carries were PARTIALLY STRUCK with their `s188-D2`
receipts (the boot band → `s240-D1`/`s240-D2`; the legal form → `s240-D3`) and both BUILDS were carried
NEW at age 0. Four `W-` rows were minted (`W-385` … `W-388`, forgotten-document class). One ADDITION was
made outside the wrap artefacts and it was the 2f EXIT CHECK's own requirement: the #239 stratum's
lesson (the ≈55K CI read-back price, cap the route at the run page plus one JS grep) had **no standing
home**, so it was homed in `knowledge/_RUNBOOK-context-gauge.md` § MEASURED, NOT RULED **before**
`roll_2f` was allowed to run — it moves no constant. The wrap commit and the authorised push are named
as DECLARED GAPS in this file (a report written before the commit cannot carry its own hash) and their
verdicts are read back from `git log -1` / the push gate and quoted in the stub.

COUNTS: findings `9` · ruling-shaped `3` · UNPROVEN `4`

## What was done

**Step 1 — `_LIVE-STATE.md` refreshed.** New `## ⏱ LATEST DELTA — … #240` (11 lines) written; the
`*Last refreshed:*` header line rebuilt with the #240 summary leading, #239 and #238 kept as `Previous:`
segments, and a dated trim note in #237's place. Date taken from `date` (2026-09-02), never from belief.
`python3 knowledge/_validate_standing_instructions.py` → **PASS, 28 standing docs reachable.**
`knowledge/_REVIEW-SIGNOFF.md` was NOT touched — this session produced no review artefact awaiting Dave.

**Step 1b — dossier written.** `_DECISION-HISTORY/2026-09-02-240-the-derived-band-and-the-roster-diet.md`
with its `provenance: 240 · 2026-09-02` / `status: observed` block and both-way links. It records the
arc: the window that could not afford a lane · Dave asking for the derived figure **before** ruling ·
the rejected 77,000 and the re-frame to a shrink-only ceiling · the dead end (moving `BOOT_FIRSTTURN_TK`
would have been the `s208-D1` re-base the rulings make unnecessary) · the diet · the attribution that is
a claim · Dave's bloat question as the shape of `s240-D3`'s answer · why thirty rows.

**Step 2 — `GOOD-MORNING.md`.** New ★ LATEST #240 banner (15 lines, eight bullets + the residual
pointer + the strike note + the exit-check line + the generated residual). `TITLE THE NEXT CHAT` →
`Apollo - #241: measure the diet, build the derived band`. `size:` stamp re-written LAST, after 2c/2d/2f.

**2c — banner roll + the carry set.** `## Batch 2026-09-02 #240` opened in `_GM-ARCHIVE.md`; the ★ PRIOR
**#238** banner moved there **verbatim, 15 lines**, by `{"op":"move"}`. The #239 banner demoted to ★
PRIOR by `{"op":"replace"}` in the same motion that inserted #240's. The whole carry set was written
into `_CARRIES.md` § `## residual → #241` (clause (i), authored in its home — it never stood on a
banner), and the banner carries the pointer + the probeable count.

**2d — delta roll.** The #237 ⏱ PRIOR DELTA moved **verbatim, 13 lines**, to `_LIVE-STATE-ARCHIVE.md`
§ `Rolled 2026-09-02 #240`, and #237's `Previous:` chain segment (4,900 chars) moved into the **same**
section in the same batch, replaced in the spine by a dated trim note.

**2e — nothing to do, and the size is ZERO LINES.** `## ⬛ DO THIS FIRST` measures 10,140 real and a
`#### ` scan of the region returns **0** dated strata.

**2f — stratum split.** `{"op":"roll_2f","session":239}` → **18 lines** appended at true EOF of
`notes/_GAUGE-LOG.md` and **3 lines** into `_GM-ARCHIVE.md` under the newest batch. The new
`#### 2026-09-02 #240` stratum (18 lines) then written into `GOOD-MORNING.md` § ⏱ SESSION STRATA, GM
keeping LATEST only (the #96/#95/#40/#41/#42 blocks are EXEMPT by Dave's #58 ruling and were not
touched).

**2g — retrieval index rebuilt LAST**, after every GM/LS/carries edit was final, and staged.

**Step 3 — memory: ZERO bytes written at this seat.** The conductor's, done at his seat
(`MEMORY.md` 21,064 → 12,454 bytes; index kept at `hook-overflow-2026-09-02-240.md`). NON-REPO, outside
every gate; this sub neither wrote nor read it back and claims nothing about the store's contents.

**Step 4 / 4b — decision nodes + titles.** The three rulings were already inscribed at the conductor's
seat via `_inscribe_ruling.py`; **not one word was re-worded here.** `_gen_titles.py --session 240` run,
receipt regenerated. **Step 4c** — scratch hygiene run; the wrap's own ops files live in
`knowledge/_tmp/wrap240/` (gitignored, in-mount, not `/tmp`) and are removed at the close.

**Step 5 — commit + push.** `SESSION_N=240 bash knowledge/_git_commit.sh --wrap --reconciled <msgfile>`
then `bash knowledge/_git_commit.sh --push` (AUTHORISED, `s207-D1`). ⬛ **DECLARED GAP: this file cannot
carry its own commit hash or the push verdict — a report written before the commit cannot name it.**
Both are read back from `git log -1 --format='%h %s'` and the push gate's own `✅ pushed and VERIFIED`
line and quoted in the stub.

## Findings

1. **#239's published pre-flight probe does not reproduce its own ordinal.** The #239 stratum printed
   *"FORTY-FIRST consecutive"* beside `grep -o 'pre-flight #[0-9]*' notes/_GAUGE-LOG.md | sort -nu`.
   Run at this seat that command returns **1** — `sort -nu` finds no leading number in `pre-flight #NN`,
   so every line compares equal and the "unique" set collapses. A corrected probe
   (`grep -oP 'pre-flight #\K\d+' … | sort -nu | wc -l`) reads **141** distinct entries with a trailing
   NOT-CAPTURED run of **50** by session key. ⛔ Both figures are REPORTED; neither is repaired here.

2. **The 2f EXIT CHECK bit, and it was right to.** `grep -n "read-back\|Actions LIST\|JS grep"
   knowledge/_RUNBOOK-context-gauge.md` returned **0** — the #239 stratum's lesson (the read-back cost
   ≈55,692 real of conductor FILL; cap the route at the run page plus one JS grep) lived only in a dated
   home. It was homed BY ADDITION in § MEASURED, NOT RULED, with its own provenance line, **before**
   `roll_2f` ran. A #240 datapoint was added in the same block: a lane NOT launched because ~19K did not
   fit at FILL 135,020.

3. **The probe file carries no verdict, and the brief's sentence is not one.** `knowledge/_probe/session-240.json`
   parses to `{session, planted_at, n, facts}` — **no `verdict`, no `last_check`, no `missed`.** The
   brief's "quiz ceiling 3/4 this seat" describes the PLANT's damage (one of four facts lost to a
   `tail -8` pipe), not a quiz result. The wrap therefore records **planted, never quizzed**.

4. **The carry count moved +7 while five items were added, and the cause is unchanged since #230.**
   `_capture_gate._carry_items` counts only `·`-segments carrying a DIGIT bracket: #239's **seven**
   `[NEW — 0]` items became countable on ageing (195 → 202) and this wrap's five arrive uncounted.

5. **The age bump has TWO bracket shapes, not one, and #239's pitfall (h) named only one.** Title-closing
   brackets in `_CARRIES.md` are `[N]**` (173) **and** `[N, DAVE'S]**` (135) — **308 together**, which is
   exactly the figure #239 published; a bump regex written for `[N]` alone would have silently frozen
   135 carries at their old age. Measured before the pass, applied in one pass, plus **7**
   `[NEW — 0]` → `[1]`.

6. **`> **COMMIT STATE` is no longer a unique anchor in `GOOD-MORNING.md`** — it matches **4** lines
   (#239's plus the three EXEMPT #40/#41/#42 blocks), so `roll_2f` was driven with the session-qualified
   anchor `> **COMMIT STATE #239:**`. A future wrap that copies the runbook's bare anchor will be
   refused by the mover (correctly, and loudly).

7. **`_gm_move.py` op ordering matters and was split into separate transactions.** The archive batch
   heading had to exist before the banner could be moved onto it, and the #239 heading had to be
   demoted in the same `replace` that inserted #240's. Five transactions were used rather than one, and
   every receipt was read back against the ops it was meant to run (the #166 stale-msgfile class).

8. **`W-384`'s `closes_when` is now SATISFIED and the row is deliberately left OPEN.** It reads "Dave has
   ruled the boot band … and the ruling is inscribed via `_inscribe_ruling.py`" — which happened
   (`s240-D1`/`s240-D2`). The brief's fence says `W-384` stays open because nothing is BUILT. ⛔ The
   discrepancy is DECLARED here rather than resolved by a wrap sub: closing it is the conductor's call.

9. **The section-sizes reading describes the file this wrap INHERITED.** GM totals moved 50,697 →
   52,674 (+1,977) against #239's line while LS is byte-identical at 73,639 — the rise is the #239
   stratum that this wrap has since rolled out. Stated as a pre-edit reading, not back-fitted.

## RULING-SHAPED QUESTIONS

1. **Does `W-384` close now that `s240-D1`/`s240-D2` are inscribed?** Its own `closes_when` is satisfied
   by the inscription, but the #240 brief's fence held it open because the BUILD has not landed.
   (a) close `W-384` and let `W-386` carry the build · (b) keep both open until `W-386` closes ·
   (c) re-word `W-384`'s `closes_when`. **Recommend (a)** — a row whose stated condition is met but which
   is held open by a different row's condition is the immortality defect `_state.py` exists to prevent.
   ⛔ Not done here: closing a row is outside this wrap's fence.

2. **Should `_capture_gate`'s `boot-drift` check be silenced, or left declaring +19,307, until `W-386`
   lands?** It now grades against a comparison `s240-D1` has retired in principle. (a) leave it loud —
   the noise is honest and the ruling is visible beside it · (b) add a one-line DECLARED-GAP naming
   `s240-D1` so the red reads as "ruled, not enacted". **Recommend (b)**, as an addition, not a
   suppression. ⛔ Not done here — it touches a gauge check.

3. **Should the pre-flight ordinal in the stratum be repaired at its source?** Finding 1 shows the
   published probe cannot reproduce the published number. (a) correct the probe in the runbook and
   re-derive the ordinal once · (b) drop the ordinal and keep only the corrected probe · (c) leave both
   figures reported and unreconciled, as this wrap did. **Recommend (a)** — a probe printed beside a
   number is a promise that the number is re-derivable.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** the roster diet's effect on boot — no post-diet first-turn reading exists. Price to
  prove: **one reading at #241's opener** (`_checkin.py`, first turn).
- **UNPROVEN:** the boot-growth attribution (connector roster · Cowork base prompt · Artifact tool ·
  three control stacks). Only `MEMORY.md`'s ≈8.5K share is measured (#233). Price: the same first-turn
  reading plus a per-input decomposition that `ds-025` item 1 says stays dark.
- **UNPROVEN:** the CI state for `4fb75f6` — no verdict was read at this seat, and the next blocker past
  step 10 of 142 is unknown. Price: the capped route, run page + one JS grep, ~10K.
- **CLAIMED:** every figure attributed to the conductor's seat — the roster removals, the `MEMORY.md`
  byte counts, the store's 328 → 331 transition and the reconstruction proof ×3, Dave's quoted words.
  What was RE-READ here is the repo side: `knowledge/_rulings.json` parses at **331** and carries
  `s240-D1` · `s240-D2` · `s240-D3` with their `says` fields read off the store.
- **CLAIMED:** lane F's #239 readings (48 → 4, the six false-red controls, `_validate_wiring.py`'s red)
  — quoted from its filed report, not re-driven at this seat.

## Evidence

No evidence directory: every claim above quotes its probe inline (the mover's stdout receipts, the
`str.count` presence probes, `_capture_gate`'s own measurements, and the two `git` read-backs named in
the stub). The wrap's ops files were written to `knowledge/_tmp/wrap240/` — gitignored, in-mount, owned
by this session — and are removed at step 4c; they are working inputs, not receipts.

REPLAY-THESE: `notes/_subreports/2026-09-02-240-wrap.md` § RULING-SHAPED QUESTIONS (~900 tk) · `_CARRIES.md` § `## residual → #241`, the five NEW items only (~1,400 tk)
