# `#248`-`W2-D2` — CHART FIT: the template's chart is made responsive by carrying Chart-bar's fit engine; rule 9 retired

session: `#248` · 2026-09-05
window: `W2 bento rhythm · lane D2 (chart responsive)`
sub index: `W2-D2`
brief: `notes/_briefs/2026-09-05-248-W2-rhythm-brief.md` (+ the conductor's lane-D2 prompt in chat: Dave's ruling "this is mostly fine as long as the chart is responsive. rule 9 is possibly a mistake" · "What is teh chart not responsive, it should be")
tokens: `≈97K by the harness counter (15,000,000 − 14,902,969 at the report's write — QUOTA drawn across 14 tool calls; message.usage is not readable from a subagent)`

## VERDICT

DONE. ONE snippet edited, `knowledge/snippets/Template-dashboard-bento.reference.html` (`git diff --stat -- knowledge/` = one file; the diff also carries lane D's uncommitted 4+2 edit — nothing else in `knowledge/` touched). The chart is now responsive: `dv-fit` on the svg (L973), the two `figure.dv-fit-on` width-release rules (L671–672), and the FIT module of `knowledge/canon/dv-behaviour.js` (fitCharts · gutterPL · fitOne · ONE rAF-debounced resize listener · init) carried declaration-for-declaration into a script block at L1214–1317; the data-fx/data-fw/data-dx/data-fx2 geometry was already on every mark. THE CHART comment block (L646–664) and rule 9 (L766–771) rewritten to say what the chart now is and why the old reading was retired (Dave, #248). Snippet gate **exit 0** (inline script NOT rejected — the STOP condition did not fire). Composition gate **exit 1**, the SAME three C9 reds as lane D (4+2 at base, 2-of-3 at ≤1100, row-sum 8 at 3 cols) — expected, not fixed. **Physics assertion PASS** (Playwright, light 1440 vs 1100): `text.dv-label` font-size **12px = 12px**, `rect.dv-series` stroke-width **1px = 1px**, height attr **90.7 = 90.7**, rendered bar box height **90.7 = 90.7**, while `.dv-chart-area` width **856 ≠ 988** and the bar width attr moved **74.5 → 86.8** (the engine ran, viewBox re-pinned `0 0 856 260` / `0 0 988 260`). Empty ground right of the canvas at 1440: **276 → 0px**. First-chart-y **516.5** unchanged at 1440; page heights unchanged at every band. Nothing of Dave's ruled.

COUNTS: findings `8` · ruling-shaped `3` · UNPROVEN `3`

## What was done

**THE ONE EDIT — `knowledge/snippets/Template-dashboard-bento.reference.html`**, five touches, all inside the one file:
1. **CSS** L671–672: `figure.dv-fit-on .dv-chart-area{width:100%;}` and `figure.dv-fit-on .dv-svg{width:100%;}` — byte-identical to Chart-bar L152–153, appended directly under the existing `.dv-svg` fixed-width rule (which stays: it is the JS-off answer).
2. **Markup** L973: `<svg class="dv-svg dv-fit" …>` — the one class Chart-bar L449 carries and the template did not. The data attributes (`data-pl/pr/h` on the svg; `data-fx`, `data-fw`, `data-dx`, `data-fx2` on every line/rect/text) were ALREADY present from the original declaration-for-declaration carry — probe: `grep -c 'data-fx=' ` on the template's chart region = 25, the same 25 marks as Chart-bar's column figure.
3. **Script** L1214–1317: a hand-carried block before the DEMO CHROME script. Contents = `knowledge/canon/dv-behaviour.js` from the `/* ---------- FIT — responsive` banner through the end of `fitOne` (77 lines, extracted programmatically from the source, not retyped; `diff` of the source against Chart-bar's injected partial L865–941 = identical), plus the `dvRAF` resize listener and the INIT that lands `.dv-fit-on` on every `figure.dv` and calls `fitCharts()`. Two deliberate deviations, both named in the block's HTML comment: (a) guard `window.__dvFit` not `__dvBehaviour`, so a later injection of the full partial is not skipped by this stub; (b) the resize callback calls `fitCharts()` alone — the source also calls `placeSegs()`, which this page has no `.seg` for and no definition of (would throw). The `PL_MAX_FRAC` / `PL_EDGE_PAD` PROVISIONAL-AWAITING-DAVE constants ride along verbatim; they only fire on a svg with `data-pl-fit`, which this chart does not carry.
4. **THE CHART comment** L646–664: rewritten — says the block now carries the fit engine, quotes Dave's #248 words, says why the pre-#248 reading (scroll a fixed canvas because the engine is JS this file does not host) was retired, and restates the DV-D02 ban with the probe that proves it (this report).
5. **Rule 9** L766–771: rewritten — "the chart module FITS its cell"; names the old rule and why it was a mistake (the promoted tile painted 276px of its width blank, W2-D Finding 5); `min-width:0` kept with its reason.

**Not done, per the brief:** `_build_all.py`, `_validate_screen.py` not run; the 3-col orphan not fixed; no commit, no push; `Chart-bar.reference.html`, `dv-behaviour.js`, `component-types.json` untouched.

**Gates**
1. `python3 knowledge/_validate_snippets.py` → `snippet gate: 136 snippet(s), 0 failure(s)` — **exit 0** (one line of output). The validator's only script handling is L203 (reads `#token-manifest`) and L266 (strips `<script>` before the visible-text check); no rule against inline script → the brief's STOP did not trigger.
2. `python3 knowledge/_validate_composition.py knowledge/snippets/Template-dashboard-bento.reference.html` → **exit 1**: `RED — C9 reds 3 [BLOCKING] · C1 0 · C7 0 · C8 0 · C4 0 [advisory] · unproven 0`. Reds verbatim: `C9 RED line 962 data-c=4 in 'Account overview': span 4 does not divide 6 columns at band base (orphan column)` · `C9 RED line 1011 data-c=2 …: span 2 does not divide 3 columns at band <=1100px (orphan column)` · `C9 RED grid 'Account overview' (line 858): spans sum to 8 at band <=1100px (3 cols) - not whole rows, an orphan cell`. Same three as lane D (line numbers shifted by this edit's added lines). Expected RED for the 3-col orphan — reported, not fixed.
3. Render: `outputs/w2-rhythm/template/_render_w2_fit.py` (Playwright, `goto("file://…")` only, `set_content()` not used), 8 full-page panes `fit-{1440,1100,820,520}-{light,dark}.png`, 0 page errors each, body bg rgb(240,240,240) light / rgb(26,26,26) dark. Measurements appended to `measure.json` under keys `fit-*`. Exit 0 = the physics assertion passed.

## Findings

1. **Physics assertion PASS** (`_render_w2_fit.py` `checks`, light 1440 vs 1100): chart-area width 856 vs 988 (DIFFERS) · `text.dv-label` computed font-size `12px` = `12px` and rendered label box height identical · `rect.dv-series` computed stroke-width `1px` = `1px`, `height` attr `90.7` = `90.7`, rendered bar box height `90.7` = `90.7` · viewBox `0 0 856 260` / `0 0 988 260` (re-pinned 1:1, not scaled) · bar `width` attr `74.5` → `86.8` (positions/widths reflowed from data-fx/data-fw). Same values at dark. Text and strokes did not scale; only x-geometry moved — DV-D02 held.
2. **Empty ground right of the canvas: 276 → 0px at 1440** (`fit-1440-light.emptyRight` = card content-right − svg right = 0.0). At 1100/820/520 also 0.0 (`.dv-stage` scrollWidth = clientWidth at every band: 856/988/708/408 — no horizontal scroll left anywhere).
3. **Chart-area width per band:** 1440 **856** · 1100 **988** · 820 **708** · 520 **408** (the 1100 figure exceeds 1440's because the evidence group spans a FULL 3-col row at ≤1100 — lane D Finding 2 — while at 1440 it takes 4 of 6).
4. **First-chart-y unchanged:** 1440 **516.5** · 1100 671 · 820 893.5 · 520 1368.6 — identical to lane D's after-numbers. Width fit does not move the chart's top.
5. **Page heights unchanged:** 1440 1064 · 1100 1602 · 820 1825 · 520 2300 (`pageH`), same as lane D. The chart's HEIGHT is still the fixed 260 canvas; the fit engine is width-only by design (`data-h` pinned).
6. **DP-18's vertical slack is untouched:** `emptyBelowFigure` at 1440 = **155.1px** (card content-bottom − figure bottom), 0 at the narrower bands where the chart sits alone. This is lane D's Finding 4 exactly; the brief scoped this lane to width. The tile now leads by INK across its width, but still carries air below — RSQ 2 of lane D stands.
7. **520 horizontal overflow is the pre-existing `.sh-actions` demo chrome** (`ovx True` at 520 only, as lane D Finding 8); the chart stage itself is 408/408, not the overflow.
8. **What the carry did NOT bring:** the popover (`dvTip`), the table panel, sort, CSV and legend modules of dv-behaviour. The template's marks still carry `data-tip` and `tabindex="0"` from the original carry, so keyboard focus lands on bars with NO tip shown — same as before this edit (probe: `grep -c data-tip` = 6 rects, no `#dvTip` in the DOM at render). Not a regression; named as RSQ 1.

## RULING-SHAPED QUESTIONS

1. **Fit only, or the whole dv-behaviour partial?** The template now hand-carries one module of a generated partial. (a) keep the fit-only stub as landed (guard `__dvFit`, ~110 lines, no registry change) · (b) register the dashboard template in `knowledge/component-types.json` group "dataviz" so `gen_component_partials.py` injects the real partial between AUTO-BEHAVIOUR markers (popover + table panel + CSV + legend come with it; the stub's comment already says "delete this block and let the generator inject") · (c) split `dv-behaviour.js` so FIT is its own injectable behaviour and the template registers for that alone. Recommend none — (b)/(c) touch the registry and the generator, both conductor serials (Chart-bar's own header calls them "conductor serials").
2. **The two carried PROVISIONAL-AWAITING-DAVE constants** (`PL_MAX_FRAC 0.42`, `PL_EDGE_PAD 2`) now live in a SECOND file. They are inert here (no `data-pl-fit`), but a hook that was provisional in one place is provisional in two. (a) leave (verbatim carry, the brief's word) · (b) strip `gutterPL` from the stub since this chart never opts in — a deviation from declaration-for-declaration. Landed (a).
3. **Chart height at wide bands.** Width is fit; the 260px canvas height is pinned by `data-h` and the engine never touches it (DV-D02 is width-compression). The 155px below the figure at 1440 (Finding 6) is now the only blank in the lead tile. Lane D's RSQ 2 options (accept · a second module under the chart · move "Needs attention") stand unchanged; this lane adds no option and recommends none.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** the resize path (window `resize` → rAF → `fitCharts`). The panes each open at their width and measure the INIT pass; no pane was resized live. Price: one Playwright `set_viewport_size` 1440→1100 on an open page + re-measure (~2K).
- **UNPROVEN:** behaviour under the real HSBC face — no Univers Next in the sandbox; font-size equality and stroke equality do not depend on the face, label BOX heights could differ by face but would still be equal across widths. Price: one 1440/1100 pair on a host with the font (~3K).
- **UNPROVEN:** whether `gen_canon_components.py` / canon.css projection changes — two new CSS declarations under `figure.dv-fit-on` in the snippet's own `<style>`; not run (fenced). Price: `--check` at the wrap (~2K).
- **CLAIMED:** `_validate_snippets.py` has no inline-script rule — from reading its two `script` mentions (L203, L266) and the exit 0, not from a mutation test that adds a script and watches it pass. Re-read costs ~1K.

## Evidence

`outputs/w2-rhythm/template/` — `fit-{1440,1100,820,520}-{light,dark}.png` (8 full-page panes, the after-state of this edit) · `measure.json` keys `fit-*` (per pane: fitOn, viewBox, chartArea, chartSvg, chartCard, firstChart, context, emptyRight, emptyBelowFigure, labelFontSize, labelBoxH, barStrokeWidth, barHeightAttr, barBoxH, barWidthAttr, barBoxW, lastBarRight, svgRight, bodyBg, pageH, overflowX, stageScrollW/ClientW, pageErrors) · `_render_w2_fit.py` (re-runnable; exit 0 iff the physics assertion holds; needs `LD_LIBRARY_PATH=~/.local/chromelibs PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS=1`) · `_peek-1440-chart.png` (a crop of the 1440 light chart row, eyeballed: bars edge to edge, 12px labels; the sandbox would not delete it — harmless, delete freely).
Lane D's `after-*.png` are the BEFORE of this edit (same file, pre-fit) — compare `after-1440-light-full.png` vs `fit-1440-light.png`.

REPLAY-THESE: `outputs/w2-rhythm/template/fit-1440-light.png` (~1.5K tk) · `outputs/w2-rhythm/template/fit-1100-light.png` (~1.5K tk, the chart on a full 3-col row) · the `PHYSICS: PASS` checks block from `_render_w2_fit.py` (~0.2K tk)
