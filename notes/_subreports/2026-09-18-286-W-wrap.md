# `#286`-`W` — the delegated capture ritual for #286 → #287

provenance: 286 · 2026-09-18
status: observed

session: `#286` · opened and wrapped 2026-09-18 — ✅ **ONE DAY, NO DATE SPLIT** (`date` read at
this seat at the ritual's open: `Fri Sep 18 21:42:07 BST 2026`)
window: conductor **Fable 5.1**, this seat a delegated **OPUS 5** wrap sub
sub index: `W`
brief: the conductor's #286 wrap brief — every figure it DECLARES is re-verified here rather than
restated, and where a measurement disagrees **both readings are published and neither is rewritten**
tokens: `UNMEASURED — this seat cannot read its own message.usage; the FILL figures below are the CONDUCTOR'S window, measured first-hand against his transcript, and are a different object`

## VERDICT

**DONE — every step of `knowledge/_RUNBOOK-capture-ritual.md` ran, in order: 1 · 1b · 2 · 2c ·
2c(i) · 2c(ii) · 2d · 2e · 2f · 2g · 3 · 4 · 4b · 4c · 4d · 5 · 5b.** ⛔ **NO RULING WAS INSCRIBED
and `knowledge/_rulings.json` STAYS AT 620 — verified here by `json.load` over the `rulings` list
AND by a scan for any `s286-` id, which returns the empty list.** ★★ **DAVE SPOKE TWICE AND BOTH
SENTENCES ARE RECORDED AS HIS WORDS RATHER THAN INSCRIBED — *"okay go on everything"* and *"okay
size-on-the-existing-node"* — and whether either becomes a ruling was PUT TO HIM at the wrap call
and is not this seat's to decide.** ✅ **THE CLOUD STORE WAS WRITTEN FROM THIS SEAT AGAIN — the
runbook's "structural" seat limit measured FALSE for another consecutive wrap** — and the runbook
line is still NOT edited, because amending a ratified step is Dave's. ⛔ **SIX blocking gate fails
stood at the OPEN, all inherited, carried in the `#243` DECLARED not-a-wrap form — the TWELFTH wrap
running — and a wrap may not repair them.** ✅ **NO SEVENTH WAS BORN IN THIS SESSION'S OWN COMMITS,
and the reason is #285's own lesson holding.** ★★ **THE SESSION'S HEADLINE IS AN EXPERIMENT THAT
REFUSED TO OVERCLAIM: boot cold read 73,832 against 80,863 with ONE variable changed, and the
finding published the spread (11,526) that is WIDER than its own delta (7,031) rather than calling
−7,031 a cause.** ✅ **THE 180,000 QUALITY LINE HELD — FILL closed 17,445 INSIDE it, the first
session in five.**

COUNTS: findings `10` · ruling-shaped `17` · UNPROVEN `3`

## What was done

**Step 1 — `_LIVE-STATE.md`.** `Last refreshed` re-stamped **from `date`** (2026-09-18, Fri) with
**NO date split declared, because there is none**. #285's segment demoted to `Previous:`; **#282's
`Previous:` chain segment MOVED verbatim (8,218 chars)** to `_LIVE-STATE-ARCHIVE.md` § Rolled
2026-09-18 #286. ★ **The verbatim guarantee is MECHANICAL rather than editorial:** the moved
segment is a **slice of the old line**, located by a marker search and never re-typed, and a
post-condition asserts the slice is absent from the projected replacement (`notes/_lanes/286/W/
lastrefreshed286.py`). ⚠ **Every pre-condition tests the INPUT, not the output** — #285 recorded a
guard of its own that tested the output and therefore fired on correct behaviour, and that shape is
deliberately not repeated here. A 33-line ⏱ LATEST DELTA #286 written.
`python3 knowledge/_validate_standing_instructions.py` **PASS** (28 standing docs).
✅ **`knowledge/_REVIEW-SIGNOFF.md` TAKES ONE STRIKE AND ONE AMENDMENT-BY-ADDITION, both
receipted:** the DRAFT `_standing.md` row is **struck** (his word came), and the regenerated-masters
row is **amended by addition** — its *"STILL NOT REGISTERED"* clause left unedited with the
registration recorded beside it, naming `7ed49d37` and the verification.

**Step 1b — narrative dossier.**
`_DECISION-HISTORY/2026-09-18-286-the-connector-lever-reads-and-the-masters-are-sized.md`,
provenance + status fields, both-way links to the spine entry, the ledger (pointed at as
UNCHANGED, the honest link when nothing was inscribed), the handoff, his words, all nine filed
reports. Seven sections; §1 is about why the session's most valuable work was deciding what it was
**not** entitled to claim.

**Steps 2 / 2c / 2d / 2f — every move through `knowledge/_gm_move.py`,** from session-owned,
uniquely-named ops files asserted with `os.path.exists` + a size floor **in the writing process**
before the mover read them (`notes/_lanes/286/W/run_ops.py`, which additionally **REFUSES an ops
file that is not session-owned**), **dry-run first**, receipts read back against the ops meant to
be run: `ops-286-lastrefreshed-*.json` (**97,186 B / 3 ops**) · `ops-286-lsdelta-*.json`
(**18,612 B / 2 ops**) · `ops-286-banner-*.json` (**4,459 B / 4 ops**) · `ops-286-roll2f-*.json`
(**224 B**) · `ops-286-stratum-*.json` (**13,245 B**) · `ops-286-title-*.json` (**294 B**).
`_roll_state.py` at the close: **2c OK (banners 2/2) · 2d OK (deltas 3/3) · 2f OK (strata 1, log
#285)** — ✅ **and this wrap asserted the generator's line BYTE-IDENTICAL against the one written
into the banner**, rather than trusting that the prediction and the generator agreed.

⚠ **THE OPS FILES LIVE UNDER `notes/_lanes/286/W/`, NOT `/tmp`**, for the two reasons #285 banked:
`s218-D7` clause 3 forbids session scratch for evidence, and `_seam.py`'s SCRATCH arm deletes a
lane's own `/tmp` working files mid-run **by design**. ⚠ **The defence is still a CONVENTION, not a
guard**, and this wrap turned one part of it into a guard rather than leaving all of it to
discipline: `run_ops.py` refuses any ops file whose directory is not this lane's.

**2c(i) — `_CARRIES.md` § `residual → #287`,** written by `notes/_lanes/286/W/carry287.py`: every
age bracket bumped, **NINE** new items, and ⛔ **TWO STRIKES**. Probe **543 → 551**; the banner's
PROBE command agrees at 551.

**2e — NO-OP, 0 lines.** DO-FIRST minted no notice, tombstone or lifecycle tag; nothing was
retro-tagged.

**2g — `_build_memento_index.py` run LAST,** after every GM/LS edit was final;
`index_freshness_check` reads clean.

**Step 3 — WRITTEN FROM THIS SEAT, NOT A SEAT LIMIT.**
`wrap-286-the-connector-lever-reads-and-the-masters-are-sized.md` (**6,663 B**, version
`36d69d3ac1c9`) and the index line newest-first above the #285 line. ⛔ **THE INDEX CUT COULD NOT BE
MADE AND WAS NOT FAKED — see finding 10.** The hook was filed FIRST at
`notes/_lanes/286/WRAP-MEMORY-HOOK.md` and the result recorded at its foot. ★ **The index line was
inserted by a TARGETED `str_replace` rather than a whole-file rewrite**, so the three existing wrap
lines could not drift — a 9,786 B retype from context is exactly the class of silent corruption the
verbatim rules exist to prevent.

**Step 4 — no ruling to record.** `json.load` counts **620**, unchanged, no `s286-` id. **4b —**
both title lines GENERATED by `python3 knowledge/_gen_titles.py --session 286`, receipt at
`knowledge/_gen_titles_receipt.json`; the forward title written into `GOOD-MORNING.md` **through
the mover, and COPIED from the receipt rather than retyped.** ✅ **The generator's 12-word trim
ended on a word without the carry set being reshaped to suit it** — #285 recorded having to shorten
its top headline BEFORE writing the carry section; this wrap wrote a ten-word headline on purpose
and the coupling did not bite. **4c —** `_gate_scratch_hygiene.py --clean --wrap` run. **4d —**
`_render_rulings.py --check` reads **FRESH** — nothing was inscribed, so nothing could stale it.

## Findings

1. ⛔★★★ **THE CONNECTOR LEVER READ −7,031, AND THE INTERESTING PART IS THAT THE SESSION REFUSED TO
   CALL IT A CAUSE.** Boot cold **73,832 real (n=1)** against #285's **80,863**, with ONE variable
   changed (the built-in Browser's 17 tools BLOCKED; skills, computer use and the other three
   connectors held constant by design). ★ **The cleanest one-variable boot experiment the record
   carries.** ⛔ **And the last seven post-diet readings span 72,110–83,636 — a spread of 11,526,
   WIDER than the delta being claimed** — so a single pair cannot separate the lever from the
   ordinary spread. The finding file says so in its own words and names what would upgrade it: **a
   second cold boot on the same setup.** ★ **This is #284's failure run in reverse:** #284 published
   an acceptance as a measurement of the world because its probes had no axis for the error Dave's
   eye caught; #286 published a number with its own instrument's blind spot attached. ⛔ **73,832 is
   the ELEVENTH post-diet reading over the 70,000 ceiling and the literal stays SHRINK-ONLY.**

2. ★★★ **SIX WORDS ANSWERED FOUR QUESTIONS, AND THE JOIN BETWEEN HIS SENTENCE AND THE ACT IS
   WRITTEN DOWN.** *"okay go on everything"* is genuinely his word on all four items the opener
   named. ⛔ **It is not his word on the WORDING of the eight standing lines.** Reading *"go on"* as
   *"proceed with the wording already drafted"* rather than *"go draft me something"* is an
   inference, and `notes/_lanes/286/DAVE-RULINGS-2026-09-18.md` marks it as the conductor's:
   *"Dave did not say 'as written', did not quote a line, and did not say 'ratified'. He said six
   words."* ★ **Writing the join down is what makes the act reversible** — recording "Dave ratified
   the eight lines" would have been a confident false inscription built out of a true sentence.
   ✅ **And the change was kept as small as the claim: 8+/3− in ONE hunk, entirely above the `---`
   rule, with the eight lines BYTE-IDENTICAL** (936 B of body, string-compared at this seat). A
   ratification that edited the text it ratified would have been unfalsifiable.

3. ★★★ **ONE SENTENCE REFUSED TWO OF THREE PROPOSED SHAPES, AND THE LANE THAT COULD NOT DO THE JOB
   IS WHY.** Lane R reported the masters **not registrable** — `gen_kg_icons.py` is blind to
   `masters/` — and came back with three shapes rather than a failure. *"okay
   size-on-the-existing-node"* refused shapes 2 and 3 and **the node count did not move.** Lane R2
   wrote a `sizes` map onto all eight nodes, merge-on-write. ✅ **VERIFIED HERE BY `json.load`
   RATHER THAN FROM THE LANE'S PROSE: 8 nodes · 33 edges · 8 of 8 carrying `sizes` with exactly
   five keys · 12 `governedBy` intact.** ⚠ **The KEY NAME is not his word** — he named the location,
   not the spelling — **and nothing reads the field yet, because the KG explorer was not rebuilt.**
   ★ **A lane that cannot do the job is not a failed lane if it returns the decision that was
   blocking it.**

4. ⛔★ **A FIX THAT CHANGED PROSE AND NOT A CONSTANT, AND THEN COUNTED WHAT IT DID NOT REACH.**
   `_gauge_tokens.py` **+25/−3 with `BUDGET_HARD` still `256_000`** — ★ **the defect was never the
   number, it was the word next to it.** Lane G then counted the residue by a NAMED probe rather
   than an impression: **58 locations across 10 live files**, `_RUNBOOK-context-gauge.md:85`'s
   *"256,000 stays the UNQUALIFIED wall (`s214-D2`)"* among them. ⛔ **Ratified runbook and ruling
   text — amending it is his** — so it is minted as a NEW carry.

5. ⛔★★ **TWO STRIKES THE BRIEF ASKED FOR WERE DECLINED, AND THAT IS THE FINDING.** The brief named
   four items as carrying to STRIKE. Two are clean whole-item closures and were struck with their
   receipts. **The other two — the 256,000 wording fix and #285's moves 2 and 3 — are each HALF of a
   carry whose HEADLINE records something that stays true:** #284's ② records the context-window
   finding *and his correction*, and #285's ④ records that a commit took six runs at #284. ⛔ **A
   strike removes the headline, not the clause** — striking either would have retracted a true
   statement. Their discharged halves are said in the new items instead. ★ **`s271-D4`'s own warning
   is the authority: a strike that is wrong is worse than an item that is merely stale** — the same
   reasoning #285 used to spare #255's item ⑨.

6. ★★ **THE DELEGATION ARGUMENT STOPPED BEING PROSE AND BECAME AN INSTRUMENT, AND THE MEASUREMENT
   REPEATED.** **NINE lanes, every one an `Agent` at `spawnDepth 1`, model opus, none in seat** —
   R2 241,918 · V 170,927 · T 145,539 · G 122,502 · C 101,132 · R 93,872 · C2 93,636 · S 83,120 ·
   P 60,602 ⇒ **subs 1,113,248 real (n=9)**. ★★ **The nine lane replies cost the conductor's window
   3,399 cl100k IN TOTAL against that — roughly 0.3%** — and the nine briefs cost 10,528 cl100k to
   send. And lane T **built** the arm: `_seam.py` +214/−6 now counts the conductor's own in-seat
   output since the last seam. ⛔ **`INSEAT_WARN_TK = 10_000` is PICKED, not ruled, and the file says
   so.** ⛔ **Two sessions' obedience is evidence the shape is affordable, not a rule that it is
   required; his sentence is still uninscribed.** ⚠ **A TENTH SUB — this seat — is excluded from the
   figure by declaration**, because a seat cannot read its own final total and `s214-D5` forbids
   defaulting an unknown into a number.

7. ⚠★ **A STALE CLAIM LIVES INSIDE A LIVE INSTRUMENT, AND THE BRIEF AND THIS SEAT DISAGREE ON WHERE.**
   `knowledge/_seam.py` still tells every seam that `_standing.md` *"is a DRAFT until Dave approves
   it"* — false since `1caaa0b1`. ⛔ **The brief locates it at `:26`; `grep -n DRAFT` at this seat
   returns `:44`. Both readings are published and neither is rewritten.** Not edited, because the
   seam's own text belongs with whoever rules on the seam, and because the sentence's second half
   (*"the seam re-quotes, it never inscribes"*) is still TRUE and must survive any edit. ★ **The
   general form: a docstring that states another file's STATUS ages the moment that status changes,
   and no gate reads docstrings.**

8. ⚠★ **AN ADVERSARIAL VERIFIER MUTATED THE TREE IT WAS VERIFYING AND RESTORED IT BYTE-EXACT.**
   Lane V read **24 of 24 testable claims PASS** — and in the course of it overwrote
   `knowledge/_capture_gate.py` with HEAD mid-run and restored it byte-exact, and moved two symlink
   farms to `_to_delete/`. ★ **The restore's exactness is the only thing that keeps this a method
   note rather than a finding about the result**, and a 24-of-24 pass from a seat that edited the
   gate it measures against is worth saying out loud. ⛔ **Ruling-shaped and not inscribed: whether a
   verifier lane may mutate the tree at all**, and if so whether it owes a declared before/after
   digest rather than an assurance.

9. ✅★ **NO SEVENTH GATE FAIL WAS BORN THIS SESSION, AND THE REASON IS LAST SESSION'S DECLARATION
   STILL FITTING.** #285's lesson was *"a lane that adds a counter adds a gate obligation, and the
   gate finds out at the wrap"*. Lane T added an `INSEAT` counting site to `knowledge/_seam.py`
   today — and `ds-021` did **not** fire, because `_seam.py` was already in `MEASURERS` as
   `estimate-only` from #285's wrap, and the new arm is the same kind of counter (tiktoken cl100k,
   no `_gauge_tokens.count()` call, no API path, never a FILL or budget claim). ★ **A gate that does
   not fire because a prior declaration still covers the new code is the registry working** — and it
   is worth recording on the passing path, because a check that is silent when it succeeds cannot be
   told from one that is dead.

10. ⛔★★ **THE MEMORY INDEX CUT COULD NOT BE MADE, AND IT WAS NOT FAKED.** `MEMORY-ARCHIVE.md` stands
    at **48,990 of its 49,152 B cap — 162 B of headroom** — and the #283 index line runs to roughly
    **4,300 bytes**, more than twenty-six times it. ⛔ **Nothing was truncated, because a truncated
    verbatim move is not a move**, so `index.md` deliberately carries **FOUR** wrap lines this
    session and says so in its own text. ⚠ **AND HOW THAT WAS ESTABLISHED IS STATED PLAINLY RATHER
    THAN DRESSED UP AS A REFUSAL THAT WAS NEVER RECEIVED: it is arithmetic on two measured
    quantities — the store listing's byte count and the documented cap — not a rejected write.** No
    append was attempted, because the only way to obtain the archive's version token is to read all
    48,990 B into this seat, and a failure whose arithmetic is already certain is not worth that
    price; the attempt is one call away for any later seat that wants the refusal itself. ★ **Nothing
    is at risk** — the repo is the record and memory is the accelerator. **What to do about the
    archive is Dave's, and the dream pass is its usual seat.**

## RULING-SHAPED QUESTIONS

Every one is a **question put**, never a state of the world (`s271-D4`). Bodies and receipts are in
`_CARRIES.md` § `residual → #287`; **none was ruled here, and nothing was inscribed.**

1. **Whether *"okay go on everything"* is inscribed as a ruling.**
2. **Whether *"okay size-on-the-existing-node"* is inscribed as a ruling.**
3. **The `sizes` KEY NAME** — he named the location, not the spelling.
4. **`INSEAT_WARN_TK = 10,000` and the INSEAT block's placement** — PICKED, not ruled, by the file's
   own words.
5. **The 58 prose "wall" locations** — ratified runbook and ruling text, his to amend.
6. **`knowledge/_seam.py`'s DRAFT line** about `_standing.md`, and the `:26`-vs-`:44` disagreement.
7. **The KG explorer**, not rebuilt to show the new `sizes` field.
8. **The store's id regex** refusing `W-286r2`-shaped ids — third instance in two sessions.
9. **Whether a verifier lane may mutate the tree it verifies.**
10. **The delegation rule** — obeyed a second session, still uninscribed.
11. **A RESUME contract for the ritual** — carried from #285.
12. **`_seam.py`'s SCRATCH arm** — keep-list, opt-in at a seam, or lanes forbidden `/tmp`.
13. **The memory archive at 48,990 of 49,152 B.**
14. **The boot ceiling** — 73,832, the eleventh breach. Cut the boot; never raise the literal.
15. **The second cold boot** that would turn −7,031 into a verdict.
16. **The two-probe-shape question on the §A digest** — its eighteenth session.
17. **What should a session DO at the hard wall?** Seventh wrap running.

*(Also carried, unchanged and not re-minted: the third dial · the theory door · `col26-012` ·
`jade-lifestyle` · `s277-D12` · `W-282a` `W-282b` `W-282ll` · the four generators · the git-lock
runbook line · the instrumentation-append policy · the `#243` not-a-wrap form ·
`_HANDOFF-130`…`-136`'s open items.)*

## Step 5 / 5b — the commit, the push, and the CI read

Recorded in full in the ★ LATEST banner's `COMMIT STATE #286` block and summarised in the returned
stub. ⛔ **The hash is a declared gap in the block itself — a commit cannot name itself** — and the
sha, the push verdict line and the CI read are quoted literally in the returned report and in the
5b addendum.

⬛ **5b, the addendum, is homed in the ⏱ LATEST DELTA of `_LIVE-STATE.md`, not on the banner** — the
`s241-D2` cap is BLOCKING at 10 lines / 1,200 tape and this banner closed at **1,179 / 10**, so an
eleventh line would be refused, correctly. The cap's own words settle it (*"the ⏱ LATEST DELTA is
the sole home for gauge / declared-skip / not-done detail"*), and the delta is IN the read chain.
⛔ **The 5b-vs-cap conflict is ruling-shaped, Dave's, and carried — this is the SIXTH wrap to meet
it and the FIFTH to answer it this way.**

⛔ **AND THE `s271-D4` RE-READ RAN AS THE RITUAL'S FINAL BEAT, WITH NOTHING TO STRIKE BY
CONSTRUCTION: #286 inscribed no ruling.** It is recorded in full at
`notes/_lanes/286/W/WRAP-REPORT.md`.

## UNPROVEN / CLAIMED (ADR-0016)

- **CLAIMED, not measured: the `replay` term** of the `wrap-handover` line. What reading this report
  back costs the conductor is not observable from inside the sub; written `unobservable
  (conductor-side, post-wrap)`, and the conductor may append the measured figure at the #287 opener.
- **The memory-archive refusal was NOT received — it was DERIVED.** Finding 10's conclusion rests on
  arithmetic over the store listing's byte count and the documented cap, not on a rejected write.
  **It is stated as a derivation, and the write that would prove it is named and un-run.**
- **The `subs` figure is a CONVENTION, and this seat's own spend is EXCLUDED rather than estimated.**
  1,113,248 is the sum of nine last-turn resident readings, the convention this log has used since
  #284. The wrap sub's own transcript is a tenth and is left out because its total cannot be final
  from inside itself; **no placeholder is written for it** [[feedback-measuring-tool-must-not-guess]].

## Evidence

`notes/_lanes/286/W/` — the ops files, the builders (`carry287.py`, `banner286.py`, `ops286_*.py`,
`lastrefreshed286.py`, `lsdelta286.md`, `stratum286.md`, `measure286.py`, `lanereplies286.py`,
`probe_carries.py`, `run_ops.py`) and `WRAP-REPORT.md`, which holds the EXIT CHECKS, the two
strikes, the two declined strikes and the `s271-D4` re-read.

REPLAY-THESE: `notes/_lanes/286/W/WRAP-REPORT.md` (~2.5k tk — the EXIT CHECKS, the strikes and the
`s271-D4` re-read) · `_HANDOFF-137-the-connector-lever-reads-and-the-masters-are-sized.md` (~4k tk)
