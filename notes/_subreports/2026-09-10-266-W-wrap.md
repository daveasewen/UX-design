# `#266`-`W` — the #266 capture ritual, delegated

```
provenance: 266 · 2026-09-10
status: observed
```

session: `#266` · 2026-09-10
window: conductor **Fable**, delegated wrap sub (OPUS 5)
sub index: `W`
brief: delivered in-chat by the conductor at FILL 304,364 real (no `notes/_briefs/` file was cut)
tokens: `UNMEASURED — a wrap sub cannot read its own `message.usage` from inside itself; an
UNKNOWN is never turned into a number`

## VERDICT

**DONE.** Ritual steps 1, 1b, 2, 2c, 2d, 2e, 2f, 2g, 4, 4b, 4c, 4d and 5 ran in order, through the
named scripts, with every mover op driven from a session-owned uniquely-named ops file under
`notes/_lanes/266/W/` and every receipt read back against the ops it was meant to run. Step 3
(memory) is **0 bytes at this seat and DONE at the conductor's** — he wrote
`kg-explorer-born-and-demo-pivot-266.md` and its `MEMORY.md` index line before delegating, which is
the first time in six delegated wraps that the step is closed rather than merely declared. ⛔
**Nothing was ruled at this seat and `knowledge/_rulings.json` stands at 441 entries, unchanged
since #265** — everything Dave decided today was a park, an acceptance in principle, a go or a
push, none of them ledger-shaped. Two BLOCKING gate refusals are carried in the **#243 form**
rather than routed around, and one of them is inherited.

COUNTS: findings `6` · ruling-shaped `3` · UNPROVEN `2`

## What was done

- **1 — `_LIVE-STATE.md`** refreshed: a new `Last refreshed` summary dated from `date`, and the
  `Previous:` chain trimmed at the LATEST+2 boundary (`notes/_lanes/266/W/lastrefreshed.py`).
- **1b — the WHY dossier:** `_DECISION-HISTORY/2026-09-10-266-kg-explorer-demo-pivot.md`, nine
  sections, provenance/status fields present, both-way linked to the banner and the ⏱ delta.
- **2 — `GOOD-MORNING.md`**: new ★ LATEST #266 banner (**exactly 1,200 tape / 10 substantive
  lines** against `s241-D2`'s 1,200 / 10), the `TITLE THE NEXT CHAT` line, and the declare-last
  `size:` stamp written LAST.
- **2c/2d/2f — all three RAN CLEAN through `python3 knowledge/_gm_move.py`**, one ops file
  (`ops-266-rolls.json`), receipts read back: the #264 banner (12 ln) → `_GM-ARCHIVE.md` § Batch
  2026-09-10 #266 · the `#### 2026-09-09 #265` stratum (22 ln → `notes/_GAUGE-LOG.md`, 2 ln →
  `_GM-ARCHIVE.md`) · the #263 ⏱ delta (24 ln) **plus its 5,093-character `Previous:` chain
  segment**, both verbatim → `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-10 #266.
- **2e —** 0 dated strata rolled; `## ⬛ DO THIS FIRST` measures 10,140 real / 98 non-blank lines
  (108 raw), inside the 120-warn / 180-block band.
- **`_CARRIES.md` § `## residual → #267`** written by `notes/_lanes/266/W/carry267.py`: **543 age
  brackets bumped, 9 new items, 1 carry STRUCK IN HALF** with its `s183-D1`/`s188-D2` receipt.
  Probe **396 → 403**.
- **2g —** `_build_memento_index.py` re-run LAST, after `_gen_chain.py` reached its fixed point.
- **4b —** `_gen_titles.py` re-run against this session's own banner; receipt fresh.
- **4c —** `_gate_scratch_hygiene.py` run.
- **4d —** `_render_rulings.py` rebuilt `notes/_RULINGS.html`; `--check` prints FRESH,
  sha256 `e2bd1b3f3df3240c…`.
- **5 —** committed and **pushed** — Dave gave the push word today: *"I also need this pushed"*.

## Findings

1. **The banner cap was met by writing shorter, six measured drafts deep.** 1,284 → 1,260 → 1,238
   → 1,234 → 1,213 → 1,205 → **1,200 tape**, each step a girth cut (a tighter clause, a pointer
   instead of a restatement, one dossier-filename shortening). Probe: `tiktoken cl100k_base` over
   the gate's own slice, cross-read by `_gm_usage.py --sizes --session 266` → `LATEST:1200`.
2. **The chain FELL for the first time in three wraps: `_CHAIN.md` 10,999 → 9,858 tape**, and the
   fall is in the ⏱ delta (4,996 → 3,842), not the banner. The generator's chain/GM ratio reads
   **29%** against its own `<40%` floor. Probe: `python3 knowledge/_gen_chain.py`, read after the
   `size:` stamp.
3. **The graph's own figures and the conductor's brief disagree, and both are recorded.** Re-running
   `knowledge/_build_kg_explorer.py` at HEAD prints base **889 / 1,176 / 137** and layered **2,780 /
   4,828**; the brief carried **1,261** and **2,771 / 4,791**. No third number was invented. The
   re-run rewrote only the page's own commit stamp (`9ee4fb7` → `6d3b61e`) and was reverted, so the
   committed `notes/_KG-EXPLORER.html` is untouched by this wrap.
4. **An INHERITED blocking red that no earlier wrap could have seen.**
   `boot_stratum_double_count_check` fails on **session #264 stating a first-turn figure twice** —
   `notes/_GAUGE-LOG.md` line 3206 (`POST-MORTEM #264`, boot 69,955) and line 3208 (the
   `wrap-handover` line, which restates `boot 69,955`). ⛔ **It landed AFTER #265's gate ran**,
   because `roll_2f` appended the #264 block during the #265 wrap. Not repaired here: the block is
   #264's testimony and `notes/_GAUGE-LOG.md` is append-only, so deleting the restatement would be
   a rewrite of an archived measurement by a session that did not take it. This wrap's own
   `wrap-handover` line deliberately omits its boot figure so it does not create a second instance.
5. **The FILL breach has a measured cause, not a mysterious one.** 304,364 real declared / 311,775
   first-hand over **137 turns** — roughly 2K a turn, a visual-iteration loop (render, look,
   adjust). ★ **A loop of cheap turns hides a breach from the instrument built to catch an
   expensive one**; check-in-at-every-seam did not fail, the loop never presented a seam.
6. **The two mechanised title lines were case-restored by two characters, and it is declared here
   rather than left to be discovered.** `_gen_titles.py` lowercases its derivation, emitting
   `the kg explorer …` and `the 3d dig defect …`; the lines delivered in chat and written to
   `GOOD-MORNING.md` restore **KG** and **3D** as the terms they are. The derivation is mechanised
   (`knowledge/_gen_titles_receipt.json` is the witness); the capitalisation is not, and the gate
   grades form and cap only.

## RULING-SHAPED QUESTIONS

1. **The #264 boot double-count (finding 4).** Option (a) delete the restatement from line 3208, as
   the gate's own text advises; option (b) leave it and carry the refusal in the #243 form
   indefinitely; option (c) narrow the checker so a `wrap-handover` line's parenthetical boot
   figure is not read as a second reading. **Recommend (c)** — the reading really is stated once;
   what is stated twice is a comparison, and (a) rewrites another session's testimony in an
   append-only log to satisfy a parser.
2. **The FILL breach (finding 5).** Nothing in the gauge fires on turn COUNT. Option (a) add a
   turn-count trigger to `_checkin.py`'s BUDGET line; option (b) a wall-clock trigger; option (c)
   nothing, and treat #266 as a one-off. **Recommend (a)**, cheapest and it measures the thing that
   actually happened.
3. **Whether `_build_kg_explorer.py` and `_kg_history.py` join the capture ritual.** Both produce
   surfaces that go stale silently against the repo — the exact shape `s263-D10` gated for
   `notes/_RULINGS.html` at step 4d. **Recommend adding them as a step-4e freshness check**, but
   ⛔ adding a ritual step is Dave's word and none was asked for at #266.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** that the 3D dig defect exists in the form Dave saw it — the conductor's headless
  probe (search → follow → Depth 2 → follow) reproduced nothing, which establishes only that the
  probe missed it. Price to prove: his screenshot, one opener turn.
- **UNPROVEN:** this wrap seat's own token spend — not readable from inside itself, and no number
  is written for it. Price: a conductor-side reading at the next opener.
- **CLAIMED:** lane K's 127,207 and lane V's 175,198 come from the conductor's brief, not from an
  `usage` object re-read at this seat. Lanes T and R were not captured at all, which is why **no
  `subs` line** was written — a total across two measured and two unknown lanes would be a number
  pretending to be a measurement.
- **CLAIMED:** memory (step 3) is DONE — asserted from the conductor's brief. The store is outside
  both directories this sub's file tools accept and invisible to the shell, so it cannot be
  re-read here. The declared-skip line says so in those terms.

## Evidence

`notes/_lanes/266/W/` — the session-owned wrap scratch, all of it committed beside this report:
`build_ops.py` + `ops-266-rolls.json` (the 2c/2d/2f ops and their receipts) · `ops-266-delta.json`
· `ops-266-banner.json` · `ops-266-stratum.json` · `lastrefreshed.py` + `lastrefreshed266.txt`
(the 2d `Previous:`-chain trim) · `carry267.py` + `carry267-new.txt` (the one programmatic carry
pass) · `banner266.txt` · `delta266.txt` · `stratum266.txt`.

REPLAY-THESE: `_DECISION-HISTORY/2026-09-10-266-kg-explorer-demo-pivot.md` (~3,400 tk) ·
`notes/_lanes/2026-09-10-266-R-v109-rating.md` (~2,200 tk, before Friday's v1.0.9 decision) ·
`notes/_lanes/2026-09-10-266-K-kg-and-decks.md` (~5,500 tk, before the run-of-show page)
