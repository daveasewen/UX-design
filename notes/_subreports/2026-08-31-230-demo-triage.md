# s230 · Demo triage — what stands between a cold seat and Dave's dashboard

**COUNTS: cards 35 · reached-cold-already 10 · adoptable-today 7 (6 work items) · needs-daves-word 9 · not-a-demo-blocker 9 · store-catches 6 · new-defects-found 1 · UNPROVEN 5**

status: floated
provenance: 230 · 2026-08-31 · Lane A build sub (Opus) · conductor Fable · row W-312
brief: `notes/_briefs/2026-08-31-230-demo-triage-brief.md`
decision surface: `reviews/DEMO-TRIAGE-2026-08-31-v1.html`
subject: Dave presents Apollo live in VS Code + Copilot 2026-09-01 (row W-308)

Card set = the 32 on `reviews/DASHBOARD-DIFF-DECISIONS-2026-08-30-v1.html` (A1–A3 · B1–B14 ·
C1–C8 · 7 Q-cards) **plus 3 port-back candidates** (P1–P3) from
`notes/_subreports/2026-08-29-224-copilot-forensics.md` § GPT'S CODE, which the brief asks to be
graded the same way. Page's own count assertion renders green: 35 cards, 10/7/9/9, ids unique.

⛔ Nothing ruled, nothing adopted, nothing enacted, no git, no `_rulings.json` write, no W-row, no
canon/snippet/token edit, no `_build_all.py`. Two files created and no others:
`reviews/DEMO-TRIAGE-2026-08-31-v1.html` and this report.

---

## 0. The headline, in one paragraph

Six things are worth doing before tomorrow and **none of them needs a design decision from Dave**.
The most valuable is the cheapest: the shipped pack now asks *which theme?* as its opening move
(v1.0.4's cold-start group) and then, two steps later, tells the generator the theme contract is
`data-theme="light|dark"` — where `console` cannot land. That is the exact mechanism that produced
the mono regen at #227, and it is a ~5-line prose fix with no serial at all. The other five are two
radius bindings that enact a ruling already made, a genuinely broken chart tooltip found in this
lane, a gate that wipes its own record, and one JSON key.

Ten cards should **not** be put to Dave at all: the pipeline has already caught up, or he ruled it
and the card predates the ruling. Two of those (Q-B4, Q-B7) would have been the #229 miss repeated.

---

## 1. STORE CATCHES — cards that are NOT open (the #229 class)

Every card graded "needs Dave's word" was searched in `knowledge/_rulings.json` (296 rulings)
before being called open. Six searches came back with an answer. These are the ones that would have
cost trust:

| card | presented as | actually |
|---|---|---|
| **Q-B7** | *"is the xs thumb 0 correct, or move `.seg.sm` onto `s`?"* | **RULED TWICE, and the measurement is stale.** `s227-D4` (2026-08-30), Dave verbatim: *"the inner radii is just not right for console on the two small sizes … especially as we had a way of calculating it, thats why I dropped it"* → `SEGMENTED_CONCENTRIC_SCALES=('xs','s')`, thumb = track − padding. `s227-D7` (2026-08-31): *"L4 console xs 4 / s 6 stand"*, m and l to concentric. HEAD console = container 6/8/10/12, **thumb 4/6/8/8**. #227's `thumb-xs: 0` was true when written and ruled away the same day. |
| **Q-B4** | *"the gutter — rails 4, canon 1px, Dave's 2px: which is it?"* | **RULED, and the three numbers are three different dials.** `s217-D2` per-theme wall gutter (mono/SC 0, common+legacy/console 24); `s217-D3` dashboard tiles at 1px; `s219-D1`(4) the ruled stop set `{1,2,4,16,24,40}`; `s219-D1`(5) dashboard `main 24/40/40/24 · sub 4/4/4/2`. Nothing to rule. |
| **C3** | *"nil in code — one number to rule, cheapest item here"* | Same as Q-B4. Console's ruled sub default is **4**, from Dave's own #219 export (`notes/_receipts/2026-08-25-219-role-defaults-exports.md`, ruled `s219-D1`(3)). His 2px is a legal edit-pass pick, not a canon default — `s219-D1`(1) is exactly this distinction. |
| **Q-A1** | *"stop re-declaring `.seg`, or put the wrapper in the two references?"* | **Neither — a third answer is ruled and built.** `s229-D3`: Dave chose the structural fix (*"fix the class the other solution is a patch not a solution"*) — a segmented PARTIAL group in `gen_component_partials.py`, 12 members, 36 rules from one atom, membership sweep in the build, 12/12 mutation arms. |
| **A1** | generation defect, live | **REPAIRED at HEAD.** 10 `.cn-*` scopes now carry `--seg-rad`/`--seg-thumb` locals; 6 carry the named `A1 REPAIR (#227)` comment; the radius block is injected. `s229-D2` + `s229-D3`. |
| **A2** | *"the Procedure never asks which theme"* | **CLOSED by v1.0.4.** `cold-start/DESIGN-CONTRACT.md` (theme question first, mandatory, default said out loud), `skills/grill-me/SKILL.md`, and `generate-from-canon/SKILL.md` **step 0**. All three verified *inside* `apollo-spider/dist/Apollo-Spider-v1.0.4.zip`, not just in the working tree. `s227-D6` scoped the cut; `s228-D4` ratified it. |

⚠ Also reached-cold without being a store catch: **B7** (stale measurement, see Q-B7), **C5**
(Dave's chart-toolbar patch landed at source and wider than he wrote it — canon.css says so in its
own comment), **P1** (GPT's `.cn-search-field` boxed radius is already in canon; `s227-D7` L8 ruled
the same change independently).

---

## 2. THE ADOPTABLE-TODAY SET — 6 work items across 7 cards, ordered

Ordered by what the room will see. Priced against the full ordered regen serial (ramp first, index
last); "no serial" means the change touches no generator input.

### T1 · The theme contract in SKILL.md step 3 — **B13** · docs only · no serial · LOW risk

The binding demo blocker, and it got sharper since #227 rather than softer.

*What v1.0.4 fixed* — step 0 of the shipped `generate-from-canon/SKILL.md`:

> `0. **Brief first.** … ask **one** question before you build — *which theme?* … **mono makes every radius zero by design**, so a mono build is radius-blind … Never pick a theme silently.`

*What it did not fix* — step 3 of the same shipped file:

> `3. **Compose.** … Root element (or <body>) gets class="canon" plus **one** theme attribute: data-theme="light" or data-theme="dark". That is the whole contract — it is what canon.css actually selects on, and it is what knowledge/_RUNBOOK-compose-from-canon.md § Compose a screen says.`

and `knowledge/_RUNBOOK-compose-from-canon.md:39` says the same. Measured at HEAD: `data-theme`
carries only `dark` (1,142) and `light` (2); the four themes live on `data-apollo-theme` —
`common` 412 · `legacy` 413 · `console` 366 · `supercharge` 414 — mono attribute-less. **Rule 5 of
the same file gets it right** (`data-apollo-theme="mono" data-theme="light"`); step 3 then defers
to a runbook that contradicts rule 5.

So the seat asks the question, Dave answers "Console", and the composition step gives the answer
nowhere to land. It ships mono, where every radius token is `0`.

- **Price:** SKILL.md step 3 (~4 lines) + `_RUNBOOK-compose-from-canon.md:39` (1 line).
- ⚠ **Three copies of SKILL.md exist with different hashes** — `apollo-spider/skills/` (`7b070e45`,
  the one inside the v1.0.4 zip), `designer-skills-v2/` (`19f49cbe`), `designer-skills-v1/`
  (`10888d75`). Fix the one the bake reads and check the others in the same motion.
- ⚠ **Rider:** DESIGN-CONTRACT names the themes *Mono, Common, Console, Supercharge*; SKILL rule 5
  says *legacy*. Under `s227-D8` the alias is additive so both resolve — but Dave says a theme name
  out loud in the room and the pack should say the same one back. One line.

### T2 · Bind the radius on the toolbar search box — **B5** (+ **P2**) · full serial · LOW-MED risk

`s227-D7` L8 ruled it: *"search radius moves to the boxed variant only, underline stays straight."*
Half landed:

```
canon.css:3170   :where(.cn-search-field) .search.boxed{… border-radius:var(--border-radius-control);}   ✅
canon.css:11580  :where(.cn-filter-toolbar-bar) .search.boxed{border:1px solid var(--border); padding:0 8px;}  ⛔
```

The missing one is the search in Dave's payments filter toolbar, on one row with neighbours that
bind the radius **inside the same block** (`.dd .trigger` :11397, `.tag` :11439). In console: an 8px
dropdown and 8px chips flanking a square box.

- **Price:** one declaration + mirror into `knowledge/snippets/Filter-toolbar-bar.reference.html`.
  ⛔ Not a hand edit to canon.css — find where the `.cn-search-field` site was written and use the
  same source. Then: canon generator selftest → canon write → `gen_snippet_tokens.py --check` →
  `gen_theme_cascade.py --check` → `gen_showroom.py` → `gen_thumbs.py` → `gen_library_214.py`.
- **Risk:** the ruling says *boxed variant only*. Gate-glob-scope rule — the change must not reach
  the underline variant. Checkable before the edit.

### T3 · Radius on `.dv-vt` and `.dv-tbl-toggle` — **B6** · full serial · MEDIUM risk (widest)

The half of B6 that Dave missed in his own file. Confirmed at HEAD:
`grep -c 'dv-vt[^{]*{[^}]*border-radius' → 0`, across **13 chart scopes** that all declare
`.dv-tbl-toggle, .dv-vt` with `border:1px solid var(--line)` and no radius.

Console chart toolbar, left to right: rounded switch → rounded "Target line" → rounded "Last year"
→ **square "Copy data (CSV)"** → **square "View as table"**. Five controls, one row, two odd ones
out — on the artefact being demoed.

- **Price:** these are buttons, not segmented members — `--border-radius-control`, like the
  `.dd .trigger` beside them. ⛔ **Not** seg-concentric members, so this does not ride `s229-D3`'s
  group; check the group's `$scan` membership first so the two mechanisms do not collide. One edit
  at the generator (the 13 scopes are generated), then the serial as T2.
- **Risk:** 13 scopes × 4 themes × light/dark. A mint assert proves values, never consumption —
  this wants an eye on a rendered chart toolbar.

### T4 · The chart tooltip is broken on every composed page — **P3** · JS only · needs a render

**New defect, found in this lane.** GPT's `dv-behaviour.js` re-parenting change reads like a
refactor. It is not. Measured:

```
canon.css   all 28 .dv-tip rules are scoped:  :where(.cn-chart-bar) .dv-tip{position:fixed; …}
            grep '.dv-tip' | grep -v ':where(.cn-'   →  (nothing unscoped)
            grep -c '#dvTip'                          →  0
dv-behaviour.js:100-104   document.body.appendChild(dvTip)   ← outside every .cn-chart-* scope
```

One tip element on `<body>`; 28 rules, every one scoped to a chart ancestor it does not have.
**Nothing matches** — not `position:fixed`, not the ground, not the border, not `.on`. It renders as
an unstyled text fragment in normal flow at the foot of the document, on every hover.

Both `dashboards/international-banking-dashboard.canon.html` (Dave's) and `.regen-v1.html` link
`dv-behaviour.js`; neither carries a local `.dv-tip` rule. **It is live on the demo artefact.** The
reason nobody caught it: `Chart-line.reference.html:350` has an **unscoped** copy, so the standalone
showroom page looks perfect and every composed page does not.

- **Price, paired (RCA, not a patch):** *tactical* — GPT's route, `tipAt(text,x,y,source)`
  re-parents into the live chart host, ~6 lines of JS, no canon change, no serial. *permanent* —
  canon carries one unscoped `#dvTip`/`.dv-tip` rule and the 28 scoped copies retire, else 28 dead
  rules stay on disk looking correct. Permanent owner: a later canon lane.
- ⚠ **Risk — the only item that cannot be settled from source.** `position:fixed` is not clipped by
  `overflow:hidden` alone, but *is* if an ancestor establishes a containing block. The dashboard
  role sets `overflow:hidden`; Dave's own harness carries `.dashboard-tile{…overflow:visible}`,
  which reads like someone who hit exactly this. **Drive a hover on a real chart inside a dashboard
  bento before shipping.**

### T5 · Stop the screen gate wiping its own record — **B14** + **Q-B14** · one file · no serial · LOW

`knowledge/_validate_screen.py:81` still rewrites `_SCREEN-GATE.md` wholesale. The file currently
holds **7 subjects** (the #227 restore held). SKILL step 5 tells every user to run it. If the seat
gates its dashboard live, six subjects vanish from a tracked file on camera.

- **Price:** ~15 lines — write `knowledge/_screen-gate/<subject>.md` per subject.
- **Shape:** per-subject files, not append — this is an ADR-0017 write-once violation in miniature
  and appending re-creates it slowly. Q-B14 is genuinely unruled (store: nothing) but the answer
  follows from a rule already made.

### T6 · `search-field.meta.json` gains the radius key — **P2** · one JSON key · LOW

GPT added `"radius": "border-radius/control"`; at HEAD the key is absent (`grep radius` on the meta
returns nothing). SKILL step 2 is *"Read the contract"* — and the contract is silent on the one
property that was wrong. **Land with T2**, or the contract promises what canon does not bind.

---

## 3. NEEDS DAVE'S DESIGN WORD — 9 cards · ⛔ do not enact before the demo

- **B2 / C1 — the dashboard tile ground.** Still true at HEAD
  (`grep 'bm-tile|bm-card|bm-wall|bm-inner' canon.css → 0`). **But the question is much narrower
  than the card:** `s217-D8` and `s218-D1` already rule how a dashboard tile expresses itself —
  *every tile wears its own 1px keyline*, radius on the four outer corners of each sub-bento, signed
  off on the live explorer (*"Yay it works. very happy with this."*). And `s219-D1`(3) makes the
  twelve tuner exports the shipped defaults, in which **dashboard keylines are OFF**. So the flat
  field the regen produced is Dave's own ruled default rendered faithfully. **The question:** does a
  tile get a ground, *or* is the demo's answer simply to turn on the keylines already ruled?
  **Keylines-on costs nothing in canon** (existing edit-pass dial, `s219-D1`(2)); a tile ground is a
  canon change that alters every dashboard bento and drags C4 with it.
  ⚠ Dave's `.dashboard-tile{…overflow:visible}` is bound up with **P3** — decide P3 first.
- **C4 — flush/bare variants.** No independent life: exists only if C1 lands. 2 metas, 2 snippets,
  2 canon blocks + the variant-naming question. ⚠ Under SKILL step 3 his
  `.dashboard-tile .cn-list-items ul.list` rule is formally out of canon.
- **B3 / C2 / Q-B3 — the row unit.** ⚠ **320px is RULED** (`s217-D2`: *"row unit 320px"*, shared
  across the four themes, from Dave's own exports) — and his dashboard overrides it twice. That is a
  finding about the ruling, not the generator. `canon.css:1090-1092` warns intrinsic rows kill the
  span vocabulary (MEASURED #217), so a silent flip is unavailable. Q-B3 (dial vs default) is
  genuinely open: `s219-D1`(2) rules all dials available in edit mode but does not make row unit a
  dial. Recommendation offered: a dial with 320 as the default.
- **B8 / C6 — the masthead logo.** `grep -c '\.logo img' canon.css → 0`, unchanged. Store: nothing
  on logo composition or mark swapping; canon's only swap is inside `.cn-template-auth` via
  `data-mode`, which SKILL step 3 explicitly forbids on the root — no precedent to copy. **Two
  decisions:** (a) composition, small; (b) the swap mechanism, no precedent. ⚠ Dave's own `<img>` is
  pinned to `hexagon-light-colour.svg` and does not swap — **if the demo touches the theme toggle,
  that is what the room sees.** Safe demo answer: don't toggle dark on the masthead.
- **C8 — responsive masthead collapse.** Hiding `.main` below 520px is a stopgap; the real fix is a
  Navigations change and the component already has the flyout machinery. ⚠ Porting the stopgap makes
  "hide primary nav" the system's answer for every screen — an accessibility posture, not a tweak.

---

## 4. NOT A DEMO BLOCKER — 9 cards

- **B1** — no bento snippet, no foundation meta (`0` / `0`, re-run at HEAD). The largest structural
  gap; SKILL rule 2 is unsatisfiable for the bento. A component-scaffold lane, not an afternoon.
  ⚠ Narrative risk: if a stakeholder asks where the layout comes from, the honest answer is
  *canon.css source*.
- **B12** — the gate cannot tell the two files apart. The reason the whole diff exists; a
  gate-design lane (first gate = parse in the consumer's grammar).
- **B4 residual** — the rails' dial names still bind to nothing
  (`grep -c 'mainSpacing|subSpacing|bentoBg|keylines' canon.css → 0`). Values ruled (§1), binding
  missing. Mechanical, medium.
- **C7** — `s219-D3`(4) rules pageBg a page-level decision with its own rail; the rail binds
  nowhere. Same class as B4 — land together.
- **A3 / Q-A3 — REPRICED UPWARD.** ⚠ **Premise correction:** #227 concluded canon has no
  visually-hidden utility, from `grep '^\.c-'`. That grep could not see it. Canon carries
  `.sr-only` **40 times**, once per component scope, in **two different recipes**
  (`margin:-4px` vs `margin:-1px`), and **nothing unscoped**. So `.c-sr-only` is a 40-site
  de-duplication with a value decision inside it, not the addition of a missing utility. The
  question to put later is *which recipe*, not *add it?*.
- **B9** — no page-width container (`.c-screen` is `width:390px`). Both files invented one in
  harness, which is legal under step 3. Invisible in the demo.
- **B10** — `.c-summary__k/__v` raw font at `canon.css:999-1000`, still present; and two objects
  named "summary" (component in the index, utility in the runbook). Under `s119-D1` the type gate is
  a shrink-only ratchet, so this is standing debt, not a new breach. Sharpest example of B12.
- **B11** — nothing says how to get an icon in; both files improvised the same sprite answer.
  ⚠ **Sibling found here:** `grep 'dv-behaviour'` across `_RUNBOOK-compose-from-canon.md` and
  `SKILL.md` returns **nothing**, yet both files link it because the reference snippet does — the
  same unwritten-convention class, on the file that carries the T4 defect.

---

## 5. UNPROVEN, declared

- **U1 · Nothing was rendered.** Every claim is read from source — CSS text, token values, selector
  scope, JSON, and the v1.0.4 zip's own bytes — quoted with file and line. **No pixel was measured.**
  T4 (P3) and T3 (B6) are the two items that most want an eye on a render before they ship.
- **U2 · No gate was run and no generator was driven.** Serial prices are the documented shape of
  the ordered serial, not a timed measurement. A planning estimate is not a measurement.
- **U3 · Store coverage is a per-item search, not a proof.** 296 rulings searched by regex per card
  before any card was called open; hits are cited on the card and in §1. An unrun search is
  indistinguishable from an absent record — a ruling I failed to cite is the most valuable
  correction available.
- **U4 · No git operation ran** (brief fence). "Landed since #227" is read from the working tree at
  HEAD, never from history. Where a thing changed, the evidence is current file content.
- **U5 · The `.dv-tip` finding is a source-level deduction about CSS matching, not an observed
  render.** The selector scoping and the `document.body` append are both quoted and re-checkable, and
  the conclusion follows — but nobody has watched the tooltip fail. The REPLAY line below is a
  browser step, deliberately.

---

## 6. REPLAY-THESE

Run from the repo root. Each re-derives a claim above. ⛔ None of these writes anything.

```bash
# §1 Q-B7 — console segmented radii at HEAD (expect container 6/8/10/12, THUMB 4/6/8/8, not 0)
grep -n 'border-radius-segmented-\(container\|thumb\)-[xsml]*:' knowledge/canon/canon.css | sed -n '9,16p'
python3 -c "import json;d=json.load(open('knowledge/_rulings.json'))['rulings'];\
print([r['says'][:200] for r in d if r['id'] in ('s227-D4','s227-D7')])"

# §1 A1 — the repair is at HEAD (expect 10 scopes with locals, 6 named markers)
grep -c ':where(\.cn-[a-z-]*) \.seg{--seg-rad' knowledge/canon/canon.css
grep -c 'A1 REPAIR (#227)' knowledge/canon/canon.css

# §1 A2 — the cold-start group is IN THE SHIPPED ZIP
unzip -l apollo-spider/dist/Apollo-Spider-v1.0.4.zip | grep -E 'DESIGN-CONTRACT|grill-me/SKILL'
unzip -p apollo-spider/dist/Apollo-Spider-v1.0.4.zip \
  'Apollo-Spider-v1.0.4/skills/generate-from-canon/SKILL.md' | sed -n '92,101p'   # step 0

# T1 / B13 — step 3 still contradicts rule 5, in the SHIPPED file
unzip -p apollo-spider/dist/Apollo-Spider-v1.0.4.zip \
  'Apollo-Spider-v1.0.4/skills/generate-from-canon/SKILL.md' | sed -n '106,110p'
sed -n '39p' knowledge/_RUNBOOK-compose-from-canon.md
grep -o 'data-theme="[a-z]*"' knowledge/canon/canon.css | sort | uniq -c        # dark 1142 / light 2
grep -o 'data-apollo-theme="[a-z]*"' knowledge/canon/canon.css | sort | uniq -c # common/legacy/console/SC
md5sum apollo-spider/skills/generate-from-canon/SKILL.md \
       designer-skills-v2/generate-from-canon/SKILL.md \
       designer-skills-v1/generate-from-canon/SKILL.md   # three different hashes

# T2 / B5 — one site has it, one does not
grep -n '\.search\.boxed{' knowledge/canon/canon.css
grep -n 'radius' knowledge/components/search-field.meta.json   # T6 — expect NOTHING

# T3 / B6 — the two Dave missed (expect 0), and the 13 scopes
grep -c 'dv-vt[^{]*{[^}]*border-radius' knowledge/canon/canon.css
grep -o ':where(\.cn-[a-z-]*) \.dv-tbl-toggle,' knowledge/canon/canon.css | sort -u | wc -l

# T4 / P3 — the tooltip's rules can never match (expect 28 / nothing / 0 / body append)
grep -c '\.dv-tip' knowledge/canon/canon.css
grep '\.dv-tip' knowledge/canon/canon.css | grep -v ':where(\.cn-'
grep -c '#dvTip' knowledge/canon/canon.css
sed -n '100,105p' knowledge/canon/dv-behaviour.js
grep -c 'dv-tip' dashboards/international-banking-dashboard.canon.html          # expect 0
grep -c 'dv-behaviour' dashboards/international-banking-dashboard.canon.html    # expect 1
sed -n '350p' knowledge/snippets/Chart-line.reference.html                      # the unscoped copy

# T5 / B14 — the clobber line, and the file's current subject count (expect 7)
sed -n '81p' knowledge/_validate_screen.py
grep -c '^## ' knowledge/_SCREEN-GATE.md

# §3 B2 — no tile ground; and the keyline rulings that ARE the structure
grep -c 'bm-tile\|bm-card\|bm-wall\|bm-inner' knowledge/canon/canon.css   # 0
sed -n '1165,1172p' knowledge/canon/canon.css
python3 -c "import json;d=json.load(open('knowledge/_render/_bento_edit_rails.json'));\
print(d['defaults']['values']['dashboard']['console'])"                   # keylines: off

# §4 A3 — the premise correction (expect 40 scoped, 0 unscoped, two recipes)
grep -c '\.sr-only' knowledge/canon/canon.css
grep '\.sr-only' knowledge/canon/canon.css | grep -v ':where(\.cn-'
grep '\.sr-only' knowledge/canon/canon.css | grep -o 'margin:-[0-9]px' | sort | uniq -c   # 36 / 4
```

**And the one that needs a browser, not a shell (U5):** open
`dashboards/international-banking-dashboard.canon.html`, hover a point on the line chart, and look
at the **bottom of the page**. If a bare unstyled value string appears in the document flow rather
than a bordered tip beside the cursor, that is T4.

---

## 7. RULING-SHAPED QUESTIONS

Four. All four are Dave's; none is asked as though it were open when it is not (§1 was run first).

**RSQ-1 · The dashboard tile: a ground, or the keylines you already ruled?**
`s217-D8` + `s218-D1` rule that every dashboard tile wears its own 1px keyline with radius on the
sub-bento's four outer corners — and `s219-D1`(3) makes *keylines off* the shipped default from his
own export. So the regen's flat field is his ruled default, faithfully rendered. **The question is
not "should canon gain a tile surface"** — it is *does a dashboard tile get a ground, or is the
answer to turn on the keylines already ruled?* Keylines-on costs nothing and is reversible; the
ground is a canon change that alters every dashboard bento and makes C4 mandatory the same day.
⚠ Decide **P3/T4 first** — his `overflow:visible` on the tile is entangled with the tooltip's home.

**RSQ-2 · Row unit: a `rowUnit` dial, or is 320px simply wrong for a dashboard?**
320px is **ruled** (`s217-D2`, shared across four themes, from his exports) and he overrode it twice
in his own dashboard. `s219-D1`(2) rules all dials available in edit mode but does not make row unit
a dial. `canon.css:1090-1092` warns (MEASURED #217) that intrinsic rows kill the span vocabulary, so
a silent default flip is unavailable. Recommendation offered, not assumed: a dial, 320 kept as the
default. ⚠ This is a case where two mocked readings would beat a paragraph, and there is no time
before tomorrow.

**RSQ-3 · The masthead mark swap — what is the mechanism?**
No ruling exists (searched). Canon's only light/dark mark swap lives inside `.cn-template-auth` via
`data-mode`, and SKILL step 3 explicitly forbids `data-mode` on the root — so there is no precedent
to copy and inventing one at speed sets a pattern by accident. Composition (C6) is small and could
ship; the swap is the real work. ⚠ His own mark is pinned to light and does not swap — **the safe
demo posture is not to toggle dark on the masthead.**

**RSQ-4 · The screen gate's record shape — per-subject files, or append?**
Genuinely unruled (searched: nothing on `_validate_screen.py` or the screen-gate file), but it
follows from a rule he has made. Recommendation: `knowledge/_screen-gate/<subject>.md` per subject,
on the ADR-0017 write-once precedent — append looks cheapest and re-creates the same defect as an
unbounded file. One word unblocks T5. ⛔ The stopgap (a line in SKILL step 5 saying "restore it
afterwards") is a patch and he has ruled on patches, so it is named here and not proposed.

---

## 8. Consequences — replayed

- **Adopting anything in §3 before the demo** changes something the room will be looking at, with no
  render behind it. The four items in T1/T2/T5/T6 are the only ones with both a ruling and no visual
  ambiguity; T3 and T4 want an eye first.
- **Skipping T1** means the demo's headline capability ("it asks you the theme") is contradicted by
  its own output two steps later — in front of the people being shown the capability.
- **Landing T2 without T6** ships a contract that promises a radius canon does not bind in that
  scope. They are one change.
- **Landing T3 without checking `$scan` membership** risks colliding with the `s229-D3` partial
  group, which fails the build on drift — by design.
- **Landing T4 without a render** risks a tooltip clipped by the dashboard role's `overflow:hidden`
  in the one layout it most needs to work in.
- **His 8 improvements exist only in his pack copy and the rescued artefact** — shipped v1.0.4 does
  not carry them. Anything adopted needs the full ordered serial, ramp first.
