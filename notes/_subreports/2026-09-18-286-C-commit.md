# `#286`-C — the commit lane: two verifier defects cleared, five lanes committed

session: `#286` · 2026-09-18
window: lane C (commit)
sub index: `C`
brief: conductor brief, lane C — commit this session's four lanes' work, verified 24/24 by lane V
tokens: `UNMEASURED — lane subagent; message.usage not readable from inside the lane`

## VERDICT

DONE. The two defects lane V left were handled first and both cleared cleanly: `_capture_gate.py`
is the genuine restored file (hash `b28301ec9c8f159b464c9b554124cc793fe4a925`, matching V's claim
exactly), carrying BOTH lane G's wording hunks and lane T's ds-021 `_seam.py` registration hunk;
and the two 449-entry symlink farms under `notes/_lanes/286/V/` were `mv`'d out to
`_to_delete/286-V-symlinks/`, which `git check-ignore` confirms is covered by `.gitignore:34`.
`notes/_lanes/286/` now contains zero symlinks. `_gen_chain.py --check` was RED on a
pre-existing staleness, so `_gen_chain.py` was run and `_CHAIN.md` added to the named paths.

COUNTS: findings 4 · ruling-shaped 0 · UNPROVEN 1

## What was done

### 1. Defect D0 — `_capture_gate.py` re-verified before staging (V's explicit instruction)

Three independent probes, all green:

```
$ git hash-object knowledge/_capture_gate.py
b28301ec9c8f159b464c9b554124cc793fe4a925          ← matches V's claimed b28301ec…

$ python3 knowledge/_capture_gate.py --selftest
capture gate [wrap]: 5 in scope · 4 fail · 3 warn
  ❌ ×4 — ALL FOUR are `_governs.py` selftest RED, evidence-pointer form on rulings
          s282-D2 / s282-D3 / s282-D4 (+ the umbrella line). Pre-existing, not this session's.

$ git diff HEAD -- knowledge/_capture_gate.py | grep -c -i quality        → 11   (lane G)
$ git diff HEAD -- knowledge/_capture_gate.py | grep -c -i estimate-only  → 3    (lane T)
$ git diff HEAD --stat -- knowledge/_capture_gate.py
 knowledge/_capture_gate.py | 49 ++++++---   (38 insertions, 11 deletions)
```

The 38/11 diffstat matches lane T's report §`git diff --stat` line verbatim (*"49 +++++++---
← SHARED: another lane's 22 insertions included"*). Lane T's hunk is visible in the diff at the
`MEASURERS` entry — `"_seam.py": ("estimate-only", …)` — and lane G's at the token-budget comment
(`amber 160,000 · working 200,000 · quality-max 256,000`). **Both lanes' work is present.** Not a
half-restored file. Staged.

⚠ One correction to the brief's phrasing: the brief said to grep the diff for `ds-021`. That
string is **not** in the `_capture_gate.py` diff — `ds-021` is the *id the gate derives* for the
`_seam.py` MEASURERS entry, not a literal in the source. The registration is present under its
real form, `"_seam.py": ("estimate-only", …)`. Verified from the diff, not from the report.

### 2. Defect D5 — the symlink farms, moved not deleted

V measured 106 entries each; the true count was higher because the mirrors contain real
subdirectories that themselves hold symlinks:

```
$ find notes/_lanes/286 -type l | sed 's#…##' | sort | uniq -c
    449 notes/_lanes/286/V/repoW
    449 notes/_lanes/286/V/repo
```

**898 symlinks, not 212.** Had `notes/_lanes/286/` been staged as a directory without this step,
the commit would have carried 898 symlink entries into the tree, not the 212 V predicted.

The #284 precedent (`mv`, never `rm` — the sandbox delete-guard `_git_commit.sh:8` documents):

```
$ mkdir -p _to_delete/286-V-symlinks
$ mv notes/_lanes/286/V/repo  _to_delete/286-V-symlinks/repo
$ mv notes/_lanes/286/V/repoW _to_delete/286-V-symlinks/repoW
$ find notes/_lanes/286 -type l | wc -l
0
$ git check-ignore -v _to_delete/
.gitignore:34:_to_delete/	_to_delete/
$ git status --short notes/_lanes/286/
?? notes/_lanes/286/
```

`_to_delete/` **is** gitignored, so the moved farms are outside the commit and outside the index.
Nothing under `_to_delete/` was named to the script.

### 3. `_CHAIN.md`

`python3 knowledge/_gen_chain.py --check` refused, verbatim:

> `✗ _CHAIN.md is STALE — it does not match GOOD-MORNING.md / _LIVE-STATE.md as they now stand, so a cold session would read a PREVIOUS session's record as if it were current. Run `python3 knowledge/_gen_chain.py` and stage the result.`

`GOOD-MORNING.md` and `_LIVE-STATE.md` are both **clean** in `git status --short`, so this
staleness is **pre-existing drift carried in from before #286**, not caused by any of this
session's five lanes. `_gen_chain.py` was run (permitted — the verifier established it is not
fenced and not a wrap act) and `_CHAIN.md` added to the named paths.

### 4. The commit

Run through `knowledge/_git_commit.sh --reconciled` with every path named explicitly (the P5
rule — nothing rode along). `--quiet` was used; the T3 prefix was added by the script, never
written by hand.

## Findings

1. **V's `b28301ec…` claim is true, and the file carries both lanes' hunks.** Verified by
   `git hash-object` + two greps of `git diff HEAD` + the 38/11 diffstat cross-checked against
   lane T's own filed stat line. The 4 `--selftest` fails are the pre-existing `_governs.py`
   evidence-pointer ones and nothing else.

2. **D5 was undercounted by a factor of four.** 898 symlinks, not the 212 V measured — V counted
   the top level of each mirror (106 × 2) and did not descend. The remedy was the same either
   way, but a lane that had used V's number to sanity-check `git status` after staging would have
   been checking against the wrong figure.

3. **`ds-021` is a derived id, not a source literal.** Anyone re-running the brief's verification
   recipe as written will get 0 hits and may wrongly conclude lane T's hunk is missing. The
   grep that actually proves it is `estimate-only` or `"_seam.py":`.

4. **The `.git/index.lock` V reported at D6 did not obstruct this lane.** The script's
   `clear_locks()` mv-aside handled it as V predicted from the code. `rm -f` was never run
   inside `.git`.

## UNPROVEN

- **Whether `--quiet` hides anything on a run shaped unlike this one.** This lane exercised it on
  the commit path only. V's D2 (`--quiet=<path>` can be aimed at a tracked path) and D3 (the
  keep-filter is column-anchored, so indented `⚠` verdicts are dropped from the live stream) are
  both carried unfixed. Neither is blocking; the transcript file is always written.

## Carried, not settled

- **Lane R's finding, carried into the commit message body:** the 40 accepted masters live in
  `knowledge/assets/logos/masters/`, which `gen_kg_icons.py` **cannot see** — a `--land` would
  register **zero** of them, not 40 and not 20. The masters are therefore **NOT registered** in
  this commit, and the commit subject says so. The shape of the fix is Dave's
  (lane R §7, confirmed by V §5·1).
- V's D1 (`_gauge_tokens.py` is wording *plus three `print()`s*), D2, D3, D4 (`W-285lm`'s
  `closes_when` and `body` disagree) — all carried as V filed them. Nothing re-derived here.
- `_seam.py`'s docstring still calls `_standing.md` a DRAFT (V §5). Cosmetic; not fixed by this
  lane, which is a commit lane and does not edit.

## Compliance

- Commits ONLY via `knowledge/_git_commit.sh`. No push. Every path named explicitly.
- `gen_kg_rules.py`, `land_rests_on.py`, `gen_kg_icons.py`, `_build_all.py`: **not run.**
- `python3 knowledge/_gen_chain.py`: run (permitted), `_CHAIN.md` staged.
- `rm -f .git/index.lock`: **never run.** The script's `clear_locks()` mv-aside is the only path used.
- Working files under `notes/_lanes/286/C/` only. `/tmp` not used for the msgfile.
- Store row `W-286c` written through `knowledge/_state.py`'s module API, not by hand.

---

## The commit — runs taken, refusals verbatim

**Two runs. Cap was four.**

### Run 1 — REFUSED, quoted verbatim

> `T3 REFUSES (s130-D3): non-wrap commit with no SESSION_N — there is no current-session source to generate a subject from, and inheriting the on-disk ★ LATEST banner is the #128 wrong-subject defect. Re-run as SESSION_N=<n> bash knowledge/_git_commit.sh --reconciled <msgfile>.`
> `✗ T3 headline generation REFUSED (reason printed above) — s130-D3: a subject is generated from a current-session source or not at all; a stale on-disk banner is never inherited. Non-wrap commits require SESSION_N=<n>. Nothing has been staged.`

Nothing was staged. `--quiet` behaved exactly as lane T's header claims on this refusal: it
replayed the **full 418-line transcript** to stderr on the non-zero exit and printed the transcript
path. **`--quiet` did not hide the refusal** — first-hand, not from the docstring.

### Run 2 — COMMITTED

`SESSION_N=286`, same named paths. Gate verdicts, in order: chain fresh · doc rows present ·
showroom in sync · polarity gate GREEN · mention map fresh · session witness agrees · spine-writer
selftest green · T3 headline generated · doc rows present post-staging · **committed** · subject
asserted identical · exactly one T3 prefix · locks clear.

```
— committed: 1caaa0b1 after #286 2026-09-18 — #286 lanes S·R·G·T·V: standing ratified, …
✓ done — locks clear, safe for Dave to push via GitHub Desktop
```

**Final sha: `1caaa0b13545b2748a593a32569ddbfd90d86e17`** · 107 files changed,
52,297 insertions, 30 deletions · `git ls-tree -r HEAD | awk '$1=="120000"'` → **0 symlinks**.
`git status --short` → clean. **NOT pushed.**

### Two gate reds seen and NOT paid to unblock

Both were visible on the committing run, both non-blocking on a declared not-a-wrap (#74-D1), and
neither was touched by this lane:

- **`boot-drift CEILING BREACH`** — `BOOT_CEILING_TK` = 70,000 with **7** post-diet readings above
  it (#277 83,636 · #278 72,110 · #279 72,447 · #280 75,740 · #281 77,400 · #282 77,474 ·
  #283 80,871). The gate quotes its own rule: the number is **shrink-only** under `s240-D2`/
  `s241-D1`, the remedy is to cut the boot, and raising the literal is Dave's word alone. **Not
  raised here.** This is lane G's territory and Dave's, not a commit lane's.
- **`boot double-count`** ×5 (#243 five statements, #264/#272/#273/#274 two each) — the `s241-D2`
  ONCE rule. Lane G's report already carries the instruction that the wrap log **73,832 once**.
  Left for the wrap; a commit lane does not edit `notes/_GAUGE-LOG.md`.

⚠ **The wrap will meet both of these as BLOCKING** when it runs with `--wrap`. The script said so
itself: *"The session's FINAL commit must run with --wrap, where red BLOCKS."* Capture gate on this
run: `223 in scope · 6 fail · 39 warn`.

### D6 recurs, post-commit

A 0-byte `.git/index.lock` is present again after the commit (`Sep 18 17:40`), and one
`git status --short` emitted `warning: unable to unlink … Operation not permitted` before the next
one ran clean. This is lane V's D6 exactly: it does **not** block `_git_commit.sh` (whose
`clear_locks()` `mv`s it aside pre-stage, proven twice today) but it will keep tripping hand-run
git. **`rm -f` was not attempted** — the delete-guard forbids it and the mv-aside is the sanctioned
path.
