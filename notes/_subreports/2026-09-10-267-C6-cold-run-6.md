# #267 lane C6 — THE SIXTH COLD RUN: the frozen prompt against the v1.0.9 ZIP, blind, light-led

**Model: Opus (per #250). Conductor: Fable (#267). Dated period record (ADR-0017). Read-only
against `knowledge/`, `showroom/`, `apollo-spider/`. Nothing built, moved or ruled there. No
`_build_all.py`, no `gen_kg_edges.py`, no commit, no push, no checkout, no stash.**

## Paths

- pack — `apollo-spider/dist/Apollo-Spider-v1.0.9.zip`
  · sha256 `736d0fb034b71048e0ef7789206837afa7ef76c33330117fa0be3410c149c780`
  · unzipped to `~/ads109/` (outside the repo; `/tmp` is per-call in this sandbox)
- arm — `outputs/coldrun-267/arm-A-blind/dashboard.html`
  · 166,443 bytes · sha256 `2667c781cff476f5cd96896ebe8cb91cf292fc86a3994d04a7e4d7a6f2d8266a`
  · frozen before any render; byte copy kept as `dashboard.FROZEN.html`
  · beside it: `brief.md`, `READ-LOG.md`, `GAPS.md`, `canon/` + `assets/` (pack files carried
    so the skill's step-3 `<link>`s resolve), `_parts/` (the authoring inputs)
- measurements — `outputs/coldrun-267/drive.json`, `renders/{1440,820}-{light,dark}.png`,
  `_validate_screen.log`, `_validate_receipt.log`
- review — `reviews/COLDRUN-267-2026-09-10-v1.html`
- this report — `notes/_subreports/2026-09-10-267-C6-cold-run-6.md`

## Done

- `briefs/` absent from the pack ⇒ `ADS-grill-me` fired; all six answers recorded
  *skipped/defaulted*; **mono announced before the build** (`brief.md`). Rule 7a's
  *"dashboard bento — is that right?"* asked; a skip is a yes; went bento-first from
  `Template-dashboard-bento.reference.html`.
- Built **blind** — no render, no screenshot, no console, no gate, no playwright, no validator
  during the build. Every read came from the unzipped zip (`READ-LOG.md` lists each file, how
  much of it, and copied-vs-re-drawn).
- Composition: `<html class="canon" data-apollo-theme="mono" data-theme="light">`, `.cn-*` scope
  classes on wrappers, `canon.css` + `type.css` linked, no component re-drawn, **0 `APOLLO-DEMO`
  markers in the page**.
- One `DATA` object (68 payments · 5 entities · 5 currencies with FX · 6 corridors · 6 channels ·
  5 statuses · 12 months × 5 entities of liquidity) drives five views, four KPI tiles, three
  charts, two panels, the status strip and the grid. Nothing hard-coded that a filter would
  falsify.
- **The chart engine was used as intended**: three specs, three `dvRender(figure, spec)` calls,
  zero authored geometry. Six engine files + the Sidebar-nav script inlined **byte-identical**
  to the ZIP (sha256 table in the review).
- Measured after the freeze: four renders (1440/820 × light/dark, light led), both gates,
  inventory, chart completeness, a 19-step playwright drive with two reloads, and the two
  C5 defects re-tested as assertions.

## Scores — 2 / 3 / 3 / 2

| | | the one fact that earned it |
|---|---|---|
| 2 | visually rich | 15 canon components, 3 engine-drawn charts, 4 sparkline KPIs, real footer, no horizontal scroll at either width, no authored hex — **but the sidebar nav renders unstyled in both modes**: `grep -c "nv-item" canon.css` = **0** while the snippet's own `<style>` has 18. |
| 3 | full | 68 payments / 5 entities / 5 currencies / 12-month × 5-entity series, nothing placeholder, footer present, `scrollWidth > innerWidth = false` at 1440 and 820. |
| 3 | persistent | Cold reload of `?view=payments&ccy=EUR&sort=counterparty&dir=asc&pp=24&mode=dark` restored **5 of 5 keys** (`drive.json → persistence`). |
| 2 | interactive | 19 controls driven, every one moved something, **0 pageerrors and 0 console errors** — but **both dropdown menus are unreachable with a mouse**: playwright 30s timeout, `#ftbStatus … intercepts pointer events`. |

## F01 / F02 — both closed

- **F01 (dark theme killed by `data-theme` on `<body>` beside a `.cn-*` class) — does NOT
  reproduce.** `data-theme` is on `<html>` (`'dark'`), `null` on `<body>`, body carries
  `cn-template-dashboard-bento`; toggling ⇒ body colour `rgb(26,26,26)→rgb(255,255,255)` and
  `.stat-card` bg `rgb(255,255,255)→rgb(31,31,31)`. v1.0.9's step 3 now names the shape and cites
  cold run 5. **Fixed in prose; no gate checks it.**
- **F02 (controls rendered after load are dead) — does NOT reproduce.** Clicking
  `#kpiRow .kpi-tile:first-child .kpi-cta` (markup written by `render()`) flipped
  `html[data-kpi-opened]` `null→'Payment value'` and opened `details.dv-tbl`. Rule 14 now carries
  the delegation convention and names cold run 5. **Fixed in prose; no gate checks it.**

## The C5 → C6 delta, in one line

**C5 3/3/3/2 (tree, v1.0.7 era) → C6 2/3/3/2 (the v1.0.9 ZIP)** — the chart engine closed C5's
F03 outright (three specs, zero authored geometry, 7/7 byte-identical borrows) and both live
defects are gone, but *rich* fell 3→2 on canon.css/snippet class divergence and **both gates
flipped PASS→FAIL because the release zip does not contain `gen_provenance_receipt.py`**.

## Top three findings

1. **canon.css has diverged from two snippets.** `.cn-sidebar-nav` in canon carries `.sn-link/.si`;
   the snippet's markup carries `.nv-item/.nv-ic/.nv-label/.nv-count/.nv-chev` — **0 occurrences in
   canon.css**. Same for the toolbar: `.ftb-primary/-trailing/-ctl/-context/-status/-hint/-clear`
   and `.th-in` are all 0. Obeying rule 2 and step 3 exactly yields an unstyled nav and pointer-dead
   menus.
2. **The released ZIP cannot pass its own gates.** `gen_provenance_receipt.py` is in the repo tree
   and in **none** of v1.0.7 / v1.0.8 / v1.0.9. Step 5 tells the consumer to run it first, so
   `_validate_receipt.py` can only answer `FAIL:NO-RECEIPT` and `_validate_screen.py` carries
   `receipt: UNPROVEN:NO-RECEIPT`. (`gen_component_partials.py` is missing too.)
3. **The engine works and rule 18 still argues with it.** `dv-render.js:25` says it *"REPLACES rule
   18"*; rule 18 still says *"until then, author the geometry yourself"* and gives the recipe. A
   consumer reading the skill top to bottom hand-authors arithmetic the library owns.

Four more (F4–F7) are on the review page: the icon gate's one UNKNOWN is a string inside the pack's
own `dv-render-donut.js:113`; chart chrome needs a second nested `.cn-chart-*` scope inside a bento
tile; eight of eleven chart types and both facility components (`limits-meter`, `runway-bar`) unused
for the sixth run running.

## Recommendation (a recommendation, not a ruling)

**SHIP-WITH-CAVEAT** — the engine v1.0.9 was cut for did its job blind (3 specs, 0 authored
geometry, 7/7 byte-identical borrows, 0 console errors) and both cold-run-5 defects are closed under
assertion; the caveat is two packaging/CSS facts, `gen_provenance_receipt.py` absent from the zip
and `canon.css` unable to style the Sidebar-nav and Filter-toolbar-bar markup it ships.

## Context (the mid-lane check-in, at the build→measure seam)

Lane fill at the freeze: **≈205K of the 1M window** — both skills, ~700 lines of snippet markup
across five components, the engine headers, greps over `canon.css`, plus the authored page. The
measurement sweep, the review page and this report took it to **≈250K**. Well inside the wall.

## NOT done (declared)

- **No sighted second arm.** One arm, as the brief asks.
- **Nothing was fixed.** Finding 1 was found by the post-freeze render; the arm was already frozen.
  No repair attempted, on the page or in the pack.
- **No receipt minted** — the minter is not in the zip (Finding 2). Both gates therefore ran, and
  are reported, in their failing state; neither was worked around.
- The screen gate's `composition: UNPROVEN: C9 bands` was accepted as printed, not chased.
- No isolated `_validate_composition.py`, `_validate_dataviz.py` or a11y audit beyond the gate's
  `a11y: ✅`. No mutation test of the fence check.
- Renders are **full-page** screenshots, not viewport crops; dark was verified both by computed
  style and by eye at 1440, and by computed style only at 820.
- Playwright note: the sandbox had no browser. `pip install playwright` then
  `python3 -m playwright install chromium` failed twice — first on host-requirement validation, then
  on `UNABLE_TO_GET_ISSUER_CERT_LOCALLY`. What worked: `libXdamage1` downloaded with `apt-get
  download`, `dpkg-deb -x`'d into `~/chromelibs`, plus **three** env vars —
  `LD_LIBRARY_PATH=~/chromelibs`, `PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS=1`,
  `NODE_TLS_REJECT_UNAUTHORIZED=1`→`0` for the download only — and
  `PLAYWRIGHT_BROWSERS_PATH=$HOME/pwb`, because **`~/.cache` did not survive between bash calls in
  this sandbox generation** while `$HOME` did. That is a new gotcha for
  `knowledge/_ROBUSTNESS-PORTABILITY.md`, offered as an observation, not written there by this lane.
- The gate wrote its row to `~/tmp/apollo-screen-gate/dashboard.md`, not to
  `knowledge/_screen-gate/` — the gate itself redirected because the page is outside the repo
  (#261 M3). Nothing under `knowledge/` was touched.

## Tokens

≈250K of the 1M context window at the close of the lane. Four subagent calls: none (single lane).
