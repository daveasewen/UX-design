# `#255`-`wrap` — the delegated capture ritual: the predicted boot-ceiling breach ARRIVED and was written into the record, the v1.0.6 "missing zip" carry RETRACTED on a first-hand probe, and one ruling read back at 393

session: `#255` · 2026-09-07
window: delegated wrap sub (OPUS 5) — ✅ **the conductor's transcript WAS read at this seat** (`knowledge/_checkin.py`)
sub index: `wrap`
brief: `knowledge/_tmp/wrap255/BRIEF.md`, cut by the Fable 5.1 conductor at a DECLARED FILL of 144,466
tokens: `UNMEASURED at this seat — a sub cannot read its own message.usage. The conductor's FILL is
MEASURED first-hand here at 148,620 real over 66 turns against his DECLARED brief-cut of 144,466,
and his boot 70,127 is the same instrument's first-turn reading.`

provenance: 255 · 2026-09-07
status: observed

## VERDICT

The ritual ran whole — steps 1, 1b, 2, 2c, 2d, 2e, 2f, 2g, 3 (declared), 4, 4b, 4c, 5 — and every
move went through `_gm_move.py`. **The `--wrap` form is REFUSED for the NINTH wrap running**, and
this time the refusal is **this session's own**: boot read **70,127**, which is **127 over the
shrink-only 70,000 ceiling** and the first over-ceiling reading since #248 — the sixth breach #254's
stratum said the rising series was about to produce. ⚠ The gate's own arm names only **#248** today
(#247 has slid out of the window; 70,127 reaches `notes/_GAUGE-LOG.md` at the #256 wrap's 2f roll),
and that narrower fact is what the record says rather than the paraphrase. **`s255-D1` was inscribed by the
conductor and read back here at entry 393 (store 392 → 393); it was not re-inscribed.** One carry was
**STRUCK** (the five bento reds, paid), **three CORRECTED**, and the correction to the first of them
**retracts a claim the record has carried since #245**: the v1.0.6 zip is not missing.

COUNTS: findings 7 · ruling-shaped 3 · UNPROVEN 4

## What was done, by step

- **1** — `_LIVE-STATE.md` `Last refreshed` rebuilt for #255 (#254 demoted to `Previous:`, the
  **#252** segment — **4,686 chars** — trimmed out to the archive at the 2d boundary);
  `_validate_standing_instructions.py` **PASS** (28 standing docs reachable).
  `knowledge/_REVIEW-SIGNOFF.md` **+0 rows** — no review page was made this session.
- **1b** — `_DECISION-HISTORY/2026-09-07-255-an-abort-is-not-a-green.md`, provenance + status
  present, seven sections, both-way linked.
- **2** — new ★ LATEST banner (#255); `TITLE THE NEXT CHAT →` set to
  `Apollo - #256: re-cut v1.0.6 at a gate-clean HEAD, cut the boot`; `size:` stamp written LAST.
- **2c** — ⛔ **THE #255 BATCH HEADERS WERE CREATED FIRST, for the second consecutive wrap** (#253's
  finding 6, obeyed). #254 banner demoted to ★ PRIOR; **#253 banner, 11 ln → `_GM-ARCHIVE.md`
  § Batch 2026-09-07 #255**, through the mover.
- **2d** — new ⏱ LATEST DELTA #255 (16 ln); #254 demoted; **#252 delta, 16 ln + its 4,686-char
  `Previous:` chain segment → `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-07 #255**.
- **2e** — `## ⬛ DO THIS FIRST` measured at **109 raw lines**, `#### ` scan returns **0** dated
  strata. Nothing to roll; the size is declared rather than the skip. ⚠ **109 agrees with #252 and
  #253; #254 recorded 70 and that figure is not reproducible at this seat.** Both reported, no
  third invented.
- **2f** — `roll_2f` split the #254 stratum: **15 ln → `notes/_GAUGE-LOG.md`** (true EOF) + **2 ln
  → `_GM-ARCHIVE.md` under the #255 key**. The `#### 2026-09-07 #255` stratum was written in its
  place, boot stated ONCE (`s241-D2` S5).
- **2g** — `_build_memento_index.py` rebuilt LAST, after every GM/LS edit and after `_gen_chain.py`.
- **3** — memory is the CONDUCTOR's step by the brief; **0 bytes written at this seat**, which
  cannot see the store (D1a).
- **4** — `s255-D1` was inscribed BY THE CONDUCTOR and **read back** here: store **392 → 393**,
  entry **393**, parsing `by` Dave · `date` 2026-09-07 · `status` ruled. Nothing was re-inscribed.
- **4b** — `_gen_titles.py --session 255` run; ⚠ it **REFUSED first** (the derived RENAME measured
  **121 tape** against the 120 cap), the banner headline was shortened at the source rather than
  the line truncated, and the re-run produced both lines and wrote the receipt.
- **4c** — `_gate_scratch_hygiene.py` run; see the findings.
- **5** — `_git_commit.sh --reconciled`, no `--wrap`; **no push from here**.

Also: **one `_state.json` row added** (`W-484`, this report). The five #255 lane reports were
already rowed by their lanes — `_gate_doc_rows.py --check` read **PASS, 0 unrowed** on arrival,
which is the first wrap in three where the lanes filed their own rows.

## Findings

1. **The prediction was correct, nobody owned it, and writing the truth is what fired the gate.**
   #254's stratum recorded five under-ceiling boots rising monotonically (69,293 → 69,797, +504) and
   called the shape *"exactly the shape that precedes a sixth breach"*. #255 booted at **70,127**.
   ★ **A trend that breaches nothing breaches no gate either** — five rising readings passed five
   wraps because there is nothing to fail on "still under, but climbing". ⚠ **And the arm cannot see it yet, which is
   said out loud rather than left to be found:** `_capture_gate.py --wrap` names **ONE** breach at this
   seat — **#248 70,974** — because #247 has slid out of the band window and 70,127 does not reach
   `notes/_GAUGE-LOG.md` until the #256 wrap's 2f roll. ⛔ The cheapest available evasion was to write
   a smaller number or none; it was not taken.
2. **A carry survived ten sessions because the probe was one command away.** *"The v1.0.6 zip is
   still missing"* has been carried since **#245**, was restated on the #254 banner and sat in the
   `→ #255` carry set. `python3 knowledge/_release/_gate_release_audit.py --pack` at this seat:
   **`PASS — apollo-spider/dist/Apollo-Spider-v1.0.6.zip matches the manifest at f6a834064ba3`**.
   ★ Same class as #254's retraction of #253's plant claim, one wrap later: **the record's cheapest
   place to be wrong is a carry nobody re-probes.**
3. **The brief and the tree disagreed, and the tree won.** The brief said HEAD was **31** commits
   past the manifest; `--drift` at this seat reads **30**. Both are reported; no third number was
   invented and neither was averaged.
4. **The banner cap is BLOCKING, not a guide, and the gate had to say so before it was met.**
   Five measured passes — **1,203 → 1,220 → 1,211 → 1,207 → 1,193** — against `s241-D2`'s **1,200
   tape / 10 line** cap, which `_capture_gate.py --wrap` enforces by name. ⛔ Not one item, carry,
   declared skip, receipt name, path or figure was dropped to get under it: every pass was prose
   tightened, and the ⑤ correction that ADDED the arm's true state was absorbed by trimming ②, ③
   and ⓪. ★ **An earlier draft called 1,207 "7 over an approximate guide, declared" — the gate
   answered that it is a cap, and the gate was right.**
5. **The title generator REFUSED, and the refusal was taken at the source.** `_gen_titles.py`
   measured the derived RENAME at **121 tape** against its 120 cap and refused by name. The repair
   was to shorten the ★ LATEST banner's own headline; ⛔ the line was not truncated at generation
   time, which is precisely what the refusal text forbids.
6. **The doc-row gate was green on arrival for the first time in three wraps.** `_gate_doc_rows.py
   --check` read `population 338 · unrowed 0` before this wrap wrote anything. #254 had to add three
   rows the lanes had not filed; #255's five lanes filed their own.
7. **The wrap's own scratch is still gitignored, and this report's receipts are still local-only.**
   `knowledge/_tmp/wrap255/` holds `carry256.py`, `build_ops_banner.py`, five ops files, the banner /
   delta / stratum / stamp sources — each asserted to exist with a size floor **in the writing
   process** before the mover read it (#166's stale-msgfile lesson) — and that directory is
   GITIGNORED. That is #253's finding 8 and #254's, unrepaired, for a third wrap.

## The carry transform

**449 aged · 6 `[NEW — 0]`→`[1]` · 1 STRUCK · 3 CORRECTED · 7 new · probeable count 320.** The line
was **never re-typed and `_CARRIES.md` was never read whole** (7,277,828 bytes): one programmatic
pass, `knowledge/_tmp/wrap255/carry256.py`, bumped every age bracket (455 substitutions) and applied
four surgical edits, each asserted unique before it was made.

- **STRUCK ①** the five bento reds — receipt `202ae6f`, four gates GREEN, V-A ACCEPT. The same edit
  **CORRECTS** the zip half on the `--pack` PASS (finding 2).
- **CORRECTED ④** the `META-TAGS-CALLS-CONTEXT` read — it did **not** happen at #255, so the carry's
  *"at #255"* is now a claim about a closed session; the read is owed at #256.
- **CORRECTED ⑦** the ceiling arm — the *"≈#255"* estimate is falsified in the direction nobody
  wanted (finding 1).
- **CORRECTED ⑧** the CI read-back — `origin/master` is at `f0191c0`, **ahead 3**, so #254's set was
  pushed; four go up with this wrap and **zero** verdicts were read at any #255 seat.

Every correction keeps its original wording verbatim after the receipt.

## RULING-SHAPED QUESTIONS

1. **The boot is now over a shrink-only ceiling and the ruled remedy has no owner.** `s240-D2`/
   `s241-D1` say cut the boot, never raise the literal. Nothing schedules a cut, and the arm cannot
   clear until the window slides past #247, #248 **and** #255. Whether to cut, and what to cut, is
   Dave's; a wrap may not move the number to unblock itself and did not.
2. **Which scopes may write a literal `repeat(n)` to opt out of the band model.** `s255-D1` made a
   literal `repeat(4, …)` legal *because a ruling declares it*, which means any scoped grid can now
   be argued into legality the same way. Lane C raised it; it is a definition, not a build.
3. **Where does wrap and lane scratch live?** Unchanged from #253's and #254's question 3 and
   re-raised because this wrap's own scripts sit under the gitignored `knowledge/_tmp/wrap255/`.
   `s218-D7`'s *"evidence beside the report, never session scratch"* is still unsatisfiable there.

## UNPROVEN / CLAIMED (ADR-0016)

- **CLAIMED:** the **subs figures** (A 120,695 · V-A 95,756 · B 84,100 · C 140,008 · V-C 88,041 ·
  C-followup 83,553 = 612,153, n=6) are the conductor's reading of the harness's `subagent_tokens`,
  **not** `message.usage`, and are recorded as QUOTA. This seat did not observe them and cannot.
- **CLAIMED:** every gate verdict attributed to the three lanes (four gates GREEN at `202ae6f`,
  17/17 arms at `4c57bdb`, 22 arms / 0 failed / three mutants RED at `b4adf44`) is **quoted from the
  filed lane reports and NOT re-driven at this seat.**
- **UNPROVEN:** that the ceiling arm can clear at all before #256. This wrap adds a breach, not a
  discharge; price to prove: one re-run of `boot_constant_drift_check` after a boot cut that nothing
  has scheduled.
- **UNPROVEN, and it is `s254-D1`'s own state, for a second wrap:**
  **`reviews/META-TAGS-CALLS-CONTEXT-2026-09-07-v1.html` has never been rendered in a browser by
  anybody, and was not reported read at any #255 seat.** Every claim about it is a claim about its
  source text.

## Evidence

No evidence directory: every claim above quotes its probe inline (the mover's receipts,
`_roll_state.py`'s generated line, `_gm_usage.py --sizes --session 255`, `_gate_doc_rows.py --check`,
`_gate_release_audit.py --pack` / `--drift`, `_gen_chain.py`'s generated footer, `_checkin.py`'s
first-turn and rehearsal readings, `git status -sb`, `ls knowledge/_probe/`). ⛔ **This wrap's own
scripts and ops files are session-owned and uniquely named under `knowledge/_tmp/wrap255/` —
`carry256.py` · `build_ops_banner.py` · `ops-255-headers.json` · `ops-255-rolls.json` ·
`ops-255-banner.json` · `ops-255-stratum.json` · `ops-255-generated.json` · `ops-255-size.json` ·
`ops-255-sizes.json` · `ops-255-stratum-tail.json` · `banner255.md` · `delta255.md` ·
`stratum255.md` · `stratum-tail255.md` · `lastrefreshed255.txt` · `size255.txt` — and that directory
is GITIGNORED, so this evidence is LOCAL ONLY.** Finding 7.

REPLAY-THESE: `_CARRIES.md § residual → #256` first four items (~1,700 tk — the v1.0.6 re-cut, the
abort class, the `repeat(n)` scope question, the breached ceiling) ·
`notes/_subreports/2026-09-07-255-BUILD-B.md` § the five C9 reds and their trace to rules 10a / 6b(i)
(~600 tk — the contradiction that became `s255-D1`)
