# `#248`-`W2-D` — RHYTHM: the wall below the headline row becomes 4+2 (evidence leads, context rails)

session: `#248` · 2026-09-05
window: `W2 bento rhythm · lane D (the edit)`
sub index: `W2-D`
brief: `notes/_briefs/2026-09-05-248-W2-rhythm-brief.md` (§ THE ONE EDIT items 1–4 + Arm L, § GATES, under § RULES)
tokens: `≈120K by the harness counter (15,000,000 − 14,879,591 at the report's write — that is QUOTA drawn across 30 tool calls, not fill; message.usage is not readable from a subagent)`

## VERDICT

DONE, with the composition gate RED by construction. The plan's defaults landed in `knowledge/snippets/Template-dashboard-bento.reference.html`: GROUP 2 evidence `data-c="3"→"4"` (L947), GROUP 3 context `data-c="3"→"2"` (L996), both comments rewritten (L938–945, L990–994). `git diff --stat -- knowledge/` = ONE file, 15+/4−, HEAD `deb172a`. Rendered at 1440 light: first-chart-y **516.5 → 516.5** (no regression; the wall edit does not move the lead row), wall 4+2 with every group's left on a column line (delta 0.00px at all four bands), tops aligned (0.00px). Snippet gate exit 0. **Composition gate exit 1** — C9 reds 3: the gate's per-tile arm (`_validate_composition.py` L296, `cols % eff`) rejects `data-c="4"` at base even though 4+2 fills the row, and the row-sum arm (L301) catches the REAL orphan at the 3-col band (spans 3+3+2 = 8, 8 % 3 = 2). The gate encodes the header's "ONLY 6 and 3" law (L69–75), so the plan's DP-15 default and the snippet's own squaring law disagree — Dave's, RSQ 1. Not squared here: no band rule added, per the brief. DP-18 is INVERTED in this specimen: the context tiles carry **0px** empty ground; the CHART is the stretched tile (155px below, 276px right of its fixed canvas at 1440). No one-line fix inside the snippet — RSQ 2. Spans dial ABSENT (`knowledge/_render/_bento_edit_rails.json` has 10 dials, chords `capsule`/`rounded` only; `4+2` count 0) — not built. Arm L rendered at `outputs/w2-rhythm/arm-L/armL-1440-light.png` (five tiles, 2+1+1+1+1 = 6; the brief's six-tile reading wraps and is kept as a second PNG). Nothing of Dave's ruled.

COUNTS: findings `9` · ruling-shaped `5` · UNPROVEN `3`

## What was done

**Item 1 — the wall (DP-14/15).** `knowledge/snippets/Template-dashboard-bento.reference.html`: L947 `data-c="4"` (GROUP 2, aria-label "Spending analysis"), L996 `data-c="2"` (GROUP 3, aria-label "Position"). Comments L938–945 and L990–994 say what the groups now are, the per-band arithmetic, and that the 3-col orphan is rendered-and-reported, not squared. Band rules L375–387 read, not edited. Nothing else in the file changed; the two stale "only 6 and 3" comments at L73–74 and L837 are STILL STALE and untouched (the brief: leave unless Dave's carry says otherwise — it does not).

**Item 2 — edges on column lines (DP-16).** Playwright assertion in `outputs/w2-rhythm/template/_render_w2.py` (`onLine` — each outer-group `getBoundingClientRect().left` against the wall grid's column start lines computed from `gridTemplateColumns` + gap + padding). Result: delta 0.00px for all three groups at 1440 / 1100 / 820 / 520, light and dark (`measure.json` key `onLine[].delta`). Column lines at 1440: `[32, 268, 504, 740, 976, 1212]`, tracks 196px, gap 40px. Evidence x=32 w=904 (cols 1–4); context x=976 w=432 (cols 5–6). Tops aligned: `topsAligned` 0.00px at 1440 (the two groups share the row); at 1100/820/520 the groups stack, so "tops aligned" is n/a and `topsAligned` reads the stack offset (384px = evidence height 344 + gap 40).

**Item 3 — the stretch arm (DP-18).** Measured, not fixed (see Findings 4–5). No CSS line changed.

**Item 4 — the spans dial (`s246-D3`).** Probed, absent, not built (Finding 7).

**Arm L.** `outputs/w2-rhythm/arm-L/Template-dashboard-bento.LEAD.html` — a COPY of the edited snippet; "Closing balance" (the has-cta tile) `data-c="1"→"2"` and `.t-cm-figure-3→.t-cm-figure-4` (32px figure, measured `fontSize` 32/24/24/24/24). The brief says "leaving five compact tiles (2+1+1+1+1 = 6 cols)": six tiles at 2+1×5 = 7 cols WRAPS the last tile alone onto a second row (rendered first, kept as `armL-7col-6tiles-1440-light.png`: lead 304.2px, first-chart-y 670.2). The brief's SUM (6 cols) is the operative spec, so the copy drops the LAST tile ("Available overdraft") to make five — a content choice named in the copy's comment and RSQ 4. Final arm L: five tiles on one row, lead 156px (vs 150.5 landed — the 32px figure lifts it 5.5px), first-chart-y 522.0, 4+2 wall beneath. `diff` landed-snippet vs arm L = 18 lines; `git diff --stat` unaffected (outputs/ only).

**Seam check-in** written before gates: `notes/_subreports/2026-09-05-248-W2-D-SEAM.md` (5 lines; its "L997" is L996 — corrected here).

**Gates**
1. `python3 knowledge/_validate_snippets.py` → `snippet gate: 136 snippet(s), 0 failure(s)` — **exit 0** (whole output, one line).
2. `python3 knowledge/_validate_composition.py knowledge/snippets/Template-dashboard-bento.reference.html` → **exit 1**: `RED — C9 reds 3 [BLOCKING] · C1 0 · C7 0 · C8 0 · C4 0 · unproven 0`. The three reds verbatim: `C9 RED line 946 data-c=4 in 'Account overview': span 4 does not divide 6 columns at band base (orphan column)` · `C9 RED line 995 data-c=2 …: span 2 does not divide 3 columns at band <=1100px (orphan column)` · `C9 RED grid 'Account overview' (line 842): spans sum to 8 at band <=1100px (3 cols) - not whole rows, an orphan cell`. C9's own band read: `[6, 4, 2] -> base [6,4,2]; <=1100 [3,3,2]; <=820 [2,2,2]; <=520 [1,1,1]`. Gate NOT edited (fenced).
3. Render: Playwright `1.61.0` (`pip install playwright==1.61.0 --break-system-packages`), `chromium_headless_shell-1228` (install with `NODE_EXTRA_CA_CERTS` + `PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS=1` — the first attempt without the skip env ROLLED BACK the whole `~/.cache/ms-playwright`, the runbook's known trap, so the recipe needs both env vars at INSTALL time too), `libXdamage.so.1` via `apt-get download libxdamage1` → `~/.local/chromelibs`. tiktoken was already present (not installed). Panes via `goto("file://…")` only; relative `../canon` / `../assets` resolved through symlinks `outputs/w2-rhythm/{canon,assets}` → `knowledge/…` so no file entered `knowledge/`. 8 after-panes + 2 before-panes + arm L, 0 page errors each; body background read off the DOM rgb(240,240,240) light / rgb(26,26,26) dark and the PNGs eyeballed (1440 light, 1100 light, arm L) — compositor sane.
4. `_validate_screen.py` NOT run (writes into `knowledge/`); `git diff HEAD -- knowledge/_SCREEN-GATE.md | wc -l` = 0.
5. `_build_all.py` NOT run (fenced). No commit, no push.

## Findings

1. **First-chart-y at 1440 light: 516.5 → 516.5px** (`measure.json` `before-1440-light.firstChart.y` / `after-1440-light.firstChart.y`; dark identical). W1's number holds; a wall-span edit cannot move the lead row. Arm L: 522.0 (+5.5, the 32px figure).
2. **Wall spans landed per band, rendered** (`measure.json` `groups[].rect`): 6 cols → **4+2** (904 + 432 of 1376) · 3 cols → **3 / 2-of-3** (evidence full row 1036; context 677.3 of 1036, ONE EMPTY COLUMN of 318.7px + 40px gap = **358.7px** empty ground to the rail's right at 1100) · 2 cols → **2 / 2** (each a full row, 756) · 1 col → **1 / 1** (456). Squareness: square at 6, 2, 1; ORPHAN at 3. The brief said measure it and do not add a band rule — done; the option landed is the plan's default (4+2) with the 3-col orphan reported, not squared. A `data-c="3"` on the context rail at ≤1100 would square it, but that is a new band rule (the header forbids hand-copying one) — RSQ 1(c).
3. **The composition gate makes the plan's default RED at BASE, not only at 3 cols.** `_validate_composition.py` L296 checks `cols % eff` per TILE, so `span 4` in 6 columns is red although 4+2 = 6 fills the row. That is the header's law (L69–75 "ONLY 6 and 3 … even number of 3s") enacted as a gate arm — DP-15's "not an even split" and the snippet's "only spans that divide the count" are in direct conflict. The row-sum arm (L301) is the one that measures orphans; it fires only at ≤1100. Probe: gate exit 1, reds quoted above.
4. **DP-18 INVERTED — the rail does not stretch; the chart does.** At 1440 the outer wall's row is `auto` (`wallRows` `150.516px 499.062px`), so the row takes the TALLER group: the context rail at **499.1px** (Balances 271.1 + gap 4 + Needs attention 224, both content-sized — `ctxRows` `271.062px 224px`, row-unit floor 96 never binds). Empty ground per context tile beyond its 24px padding: **Balances 0.0px · Needs attention 0.0px** (`ctxTiles[].emptyBeyondPadding`, identical before/after and at every band). The chart card (natural **344px**, seen at ≤1100 where it sits alone) is stretched to **499.1px**: **155.1px** of empty ground below the figure beyond padding (179.1 raw). The brief's premise ("the evidence chart is taller") is false for this specimen.
5. **The promoted evidence tile does not USE its extra width.** The chart canvas is a fixed 580×260 (`chartSvg.w` 580 at every band; rule 9 L754–756 "scrolls its own fixed canvas rather than stretching it"). Card content-right at 1440 after = 912px, canvas right = 636px → **276px** of empty ground to the chart's right (before, at data-c="3": 40px). So 4+2 hands the lead tile 236px it paints as blank. For the judge this is the tile that "leads" by area but not by ink — the mechanism behind lane J's W1 note. Not fixed: widening `.dv-svg` is Chart-bar's own rule, not one line inside this snippet's fence.
6. **The mechanism of the stretch is the ratified fix, not rule 7.** `height:100%` (L750–751) is not what stretches the chart: the nested bento flexes (`L365–369`, `flex:1 1 auto` + `minmax(row-unit,auto)`) into the outer `auto` row, and a grid item stretches to its cell by default. Removing `height:100%` would not shorten the card; capping the row would reinstate the L351–364 dead-band defect. Hence no one-CSS-line fix → RSQ 2.
7. **Spans dial ABSENT.** `knowledge/_render/_bento_edit_rails.json` exists (41,991 bytes, `$generated_by: knowledge/_render/gen_bento_matrix_217.py --rails`), dials = `spacing, mainSpacing, subSpacing, keylines, mode, edge, rounding, bentoBg, capBg, grouping` (+`$theme_locks`); `constraints.chords` = `['capsule','rounded']` (ground chords, s219-D3); every `intersections/*/dashboard/chords` = `[]`; substring counts `4+2` 0 · `2+4` 0 · `3+3` 0 · `importance` 0 · `span` 2 (both about `mode=bento`'s "span grid"). `s246-D3` in `_rulings.json` governs that file + `knowledge/components/template-dashboard-bento.meta.json` (grep `chord|spans` in the meta: 0 hits). Nothing built.
8. **520 horizontal overflow is PRE-EXISTING demo chrome, not this edit.** `documentElement.scrollWidth` 631 vs 520 at HEAD and after alike; the overflowing element is `DIV.sh-actions` (masthead action buttons, r=631) — demo/masthead, not the wall. `ovx` False at 820/1100/1440.
9. **Page heights** (`pageH`): 1440 1064 (unchanged) · 1100 1602 · 820 1825 · 520 2300. First-chart-y at other bands after: 1100 671 · 820 893.5 · 520 1368.6 — identical to W1's after-numbers (the wall edit is below the chart top at every band).

## RULING-SHAPED QUESTIONS

1. **4+2 vs the snippet's squaring law (Finding 3).** The plan's default is gate-RED at base and orphans at 3 cols. (a) keep 4+2, amend the header L69–75 + L837 and C9's per-tile arm to a ROW-SUM law (4+2 legal where rows fill) and accept the 3-col orphan · (b) keep 4+2 and add ONE band rule (`@container (max-width:1100px) .tpl-group-context{grid-column:span 3}`) — the header calls this a third home for a minted fact · (c) revert to 3+3 and give rhythm to the wall by ROW HEIGHT / content instead of span (DP-15 by another lever) · (d) 4+2 at base with the C9 red carried as a declared exception. Recommend none — the conflict ledger's row; the judge's verdict on the three renders is the evidence Dave asked for.
2. **DP-18, the chart is the stretched tile (Findings 4–6).** (a) accept — a chart module with air below it is the specimen's look · (b) let the chart canvas take its cell (`.dv-svg{width:100%;height:auto}` in the snippet's own block — one line, but it overrides Chart-bar's rule 9 and rescales chart type) · (c) give the evidence group a second module under the chart (content answers the height — DP-18's own preferred answer, a content decision) · (d) make the rail the shorter group by moving "Needs attention" elsewhere (s247-D3 territory, the status surface — not drawn here). Recommend (c) as the DP-18 reading; it is Dave's.
3. **The spans dial (Finding 7, `s246-D3` TRIAL).** Absent. (a) scope it as a `dials.spans` enum `4+2 · 2+4 · 3+3 · 6` on the dashboard role in `_bento_edit_rails.json` via `gen_bento_matrix_217.py --rails` (the file is `$do_not_edit`, generated) · (b) defer until Q1 is ruled (a chord that C9 rejects is a dial that cannot pass a gate — the #244 rule). Recommend (b) ordering.
4. **Arm L's tile count.** The brief's "five compact tiles" and its sum "2+1+1+1+1 = 6" cannot both hold with six tiles. The copy dropped "Available overdraft" (last, flat-spark, DP-08-weak). (a) that drop · (b) drop a different tile · (c) keep six and let the lead be `data-c="2"` at 7 cols with a wrap (rendered, `armL-7col-6tiles-1440-light.png`, lead 304px — the wrap orphans a tile). The judge sees (a). Which tile leads the row, and whether one leads at all, remains `s247-D4`'s open weight question.
5. **The two stale "only 6 and 3" comments (L73–74, L837)** — carried from W1 RSQ 3, now doubly stale (the wall itself no longer uses only 6 and 3). Same options as W1: (a) edit at the wrap · (b) leave until the 4px audit re-puts the file.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** absolute heights under the real HSBC face — no Univers Next in the sandbox; the DELTAS (0px empty ground on the rail, chart stretched 344→499.1, 276px canvas slack) do not depend on the face, the px do. Price: one 1440 render on a host with the font (~3K).
- **UNPROVEN:** whether `gen_canon_components.py` / canon.css changes from this edit — attribute-only edit + comments, so the CSS projection should be byte-stable; not run (fenced). Price: `--check` at the wrap (~2K).
- **UNPROVEN:** C8's rendered contiguity leg (the gate itself says "the per-band RENDERED leg is UNPROVEN here"). The renders show each group contiguous at every band by eye (1440, 1100 eyeballed; 820/520 by `groups[].rect` only). Price: a per-tile assertion (~2K).
- **CLAIMED:** `_validate_snippets.py`'s 136 count is "the" snippet gate — as W1 claimed; not re-verified beyond its own one-line output.

## Evidence

`outputs/w2-rhythm/template/` — `before-1440-{light,dark}.png` (+`-full`), `after-{1440,1100,820,520}-{light,dark}.png` (+`-full`, 16 PNGs), `before-HEAD.reference.html` (the `git show HEAD:` arm), `measure.json` (12 keys: `before-1440-*`, `after-*`, `armL-1440-light`, `armL-7col-6tiles-1440-light` — per pane: colLines, tracks, gap, groups, onLine deltas, firstChart, chartCard, chartSvg, lead, leadTiles, context, ctxRows, ctxTiles with emptyRaw/emptyBeyondPadding, topsAligned, bottomsDelta, bodyBg, pageH, overflowX, pageErrors), `_render_w2.py` (re-runnable; needs `LD_LIBRARY_PATH=~/.local/chromelibs PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS=1`).
`outputs/w2-rhythm/arm-L/` — `Template-dashboard-bento.LEAD.html`, `armL-1440-light.png` (+`-full`) — the judge's arm; `armL-7col-6tiles-1440-light.png` (+`-full`) — the six-tile reading, for the record.
`outputs/w2-rhythm/{canon,assets}` — symlinks into `knowledge/`, so the copies resolve `../canon/canon.css`; delete freely.
`notes/_subreports/2026-09-05-248-W2-D-SEAM.md` — the seam check-in.

REPLAY-THESE: `outputs/w2-rhythm/template/after-1440-light.png` (~1.5K tk) · `outputs/w2-rhythm/arm-L/armL-1440-light.png` (~1.5K tk) · `outputs/w2-rhythm/template/after-1100-light-full.png` (~1.5K tk, the 3-col orphan column) · the three C9 red lines above (~0.2K tk)
