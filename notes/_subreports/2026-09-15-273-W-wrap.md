# `#273`-`W` — the delegated capture ritual for "the address and the registry"

provenance: 273 · 2026-09-15
status: observed

session: `#273` · 2026-09-15
window: delegated wrap sub (OPUS 5), conductor Fable
sub index: `W`
brief: `_HANDOFF-124-the-address-and-the-registry.md` (the wrap brief was cut from it; no separate
`notes/_briefs/` file was written for this sub)
tokens: `UNMEASURED — a wrap sub cannot read its own conductor-side spend; the conductor may append
it at the next opener`

## VERDICT

**GREEN WITH FOUR DECLARED, INHERITED REFUSALS.** Every step of
`knowledge/_RUNBOOK-capture-ritual.md` — 1, 1b, 2, 2c, 2d, 2e, 2f, 2g, 3, 4, 4b, 4c, 4d, 5, 5b — ran.
Three blocking reds were HEALED by the ritual; four are carried in the `#243` form and every one of
them is another session's append-only testimony that this seat is forbidden to rewrite.

## COUNTS

`capture gate [wrap]`: **211 in scope · 4 fail · 18 warn** (the four: boot-drift ceiling breach; boot
double-count #243, #264, #272) · rulings **562 → 566** (`s273-D1`…`s273-D4`; `s272-D93` enacted, not
newly inscribed) · Memento index **2,180 records** · carries **453 → 459 items, 8 new, 2 struck** ·
★ LATEST banner **1,197 tape / 10 lines** against the `s241-D2` cap of 1,200 / 10 · commits **11 local
and unpushed when this wrap opened, 12 after it**.

## WHAT RAN, WITH RECEIPTS

- **1** — `_LIVE-STATE.md` refreshed; `Last refreshed` stamped **2026-09-15 from `date`**; #269's
  `Previous:` chain segment (3,649 chars) moved VERBATIM to `_LIVE-STATE-ARCHIVE.md`
  § Rolled 2026-09-15 #273 (`notes/_lanes/273/W/lastrefreshed273.py`, assertions on both ends).
  `_validate_standing_instructions.py`: **28 standing docs reachable, PASS**.
- **1b** — `_DECISION-HISTORY/2026-09-15-273-the-address-and-the-registry.md`, provenance + status
  fields present, both-way links to the spine and the ledger.
- **2 / 2c / 2d / 2f** — every move through `knowledge/_gm_move.py` from session-owned, uniquely
  named ops files under `notes/_lanes/273/W/`, each asserted for existence and a size floor **in the
  writing process** before the mover read it. Receipts read back against the ops intended:
  #271 banner (12 ln) → `_GM-ARCHIVE.md` § Batch 2026-09-15 #273 · #270 ⏱ delta (22 ln) →
  `_LIVE-STATE-ARCHIVE.md` · `roll_2f` #272 (24 ln → `notes/_GAUGE-LOG.md` at true EOF, 2 ln →
  `_GM-ARCHIVE.md`). `_roll_state.py` then reports **2c OK (banners 2/2) · 2d OK (deltas 3/3) ·
  2f OK (strata 1, log #272)**, and the gate's roll-claim check agrees with the measured tree.
- **2e** — DO-FIRST unchanged this session; no new notice or tombstone was written, so no lifecycle
  tag was minted and nothing was retro-tagged.
- **2g** — `_build_memento_index.py` run **LAST**, after every GM/LS edit was final: **2,180 records**.
- **3** — **SEAT LIMIT, DECLARED, NOT A SKIP:** the memory store is read-only from this seat by the
  brief. The hook body and its one `MEMORY.md` index line were written in the `s271-D4` form to
  `notes/_lanes/273/WRAP-MEMORY-HOOK.md` for the conductor to place.
- **4** — `s273-D1`…`s273-D4` were already inscribed in `knowledge/_rulings.json` by the session
  itself (566 total, verified at this seat); this wrap inscribed no ruling and decided nothing.
- **4b** — both names GENERATED by `knowledge/_gen_titles.py`, receipt written; the forward title is
  in `GOOD-MORNING.md` and the rename is delivered in chat only.
- **4c** — `_gate_scratch_hygiene.py --clean`: this session's own `/tmp` entries removed;
  **825 MB owned by dead sessions reported as a total only**, unremovable from inside; `/sessions`
  **96%**.
- **4d** — `_render_rulings.py` rebuilt `notes/_RULINGS.html` (566 rulings, 149 sessions) and
  `--check` prints **FRESH**.
- **5 / 5b** — committed through `knowledge/_git_commit.sh` in the DECLARED not-a-wrap branch
  (`#243` form), subject read back before committing. **Not pushed — the push is Dave's word.**

## THE QUOTE GATE

Seven sentences are presented as Dave's across `GOOD-MORNING.md`, `_LIVE-STATE.md`, `_CARRIES.md`,
the dossier and the memory hook. All seven were copied character-for-character from
`notes/_lanes/273/DAVE-RULINGS-2026-09-15.md` and then run through
`python3 knowledge/_quote_gate.py` after 2g: **7/7 verbatim, 0 not in the record.**

## WHAT I GOT WRONG, OR NEARLY DID

- The first ★ LATEST banner measured **1,483 tape over 11 substantive lines** against the `s241-D2`
  cap of 1,200 / 10, because the measured section includes the two `residual` lines and the drafting
  had priced the banner alone. Repaired by MERGING two bullets and compressing prose — **no item,
  carry, declared skip or receipt name was dropped to fit**, which is the clause the cap actually
  cares about.
- The stratum's gate-refusal line as first drafted **restated the boot figure 81,419**, which would
  have minted a `#273` boot double-count at the next wrap — exactly the `#243`/`#264`/`#272` defect
  this block complains about. Caught before the commit and rewritten to point at the post-mortem
  line instead of repeating the number.
- The `#272` boot double-count is **new to the gate and not new to the world**: it appeared only when
  this wrap's legal `roll_2f` moved their stratum into `notes/_GAUGE-LOG.md`. The roll is correct and
  the restatement is theirs; it is named rather than left looking like damage this wrap did.
- The wrap brief said **10 local commits**; `git rev-list --count @{u}..HEAD` read **11** at this
  seat, because `402d8cd` (the handoff) landed after the brief was cut. The repo-state claim is taken
  from `git`, never from the brief [[premise-ages-faster-than-rule]].

- **The showroom sync gate refused this wrap's commit and the gap is DECLARED, not closed.**
  `gen_showroom.py --check` reads **108 of 137 pages stale**. The regen was run here and then
  **reverted**: decoding the payload shows the diff is **not** `s273-D1`'s `provides` metadata but
  **`:is(…)` → `:where(…)`, a CSS specificity change, one line in each of 108 generated pages**. The
  staleness predates #273 — canon moved and the showroom was never regenerated — and landing a
  specificity change across every showroom surface unreviewed at a wrap seat is what
  [[art-director-reviews-lane-output-268]] forbids. Committed with `SHOWROOM_ACK`; files restored
  one at a time via `git show HEAD:<path>` (never `git checkout`, never `git stash`) and the index
  verified empty afterwards.

## RULING-SHAPED QUESTIONS

1. **The `example` key asymmetry.** `s273-D4` requires a definition AND an example from any lane that
   adds a `when` name; the 13 new entries have one and the **26 legacy entries do not**. Whether the
   legacy 26 get examples, or `example` is a forward-only obligation the schema must therefore not
   require, is **Dave's** and is deliberately not decided here.
2. **The four near-dupe pairs** (`records`/`rows`, `entry`/`content`, `exits`/`destinations`,
   `steps`/`parts`). A merge rewrites inscribed `when` predicates on live metas. **Dave's.**
3. **Chart-boxplot's dropped `span.cols` sentence.** Restoring it means moving content from a
   generator-owned field to an authored one, which is a decision and not a repair. **Not made here.**
4. **When is the showroom regenerated, and by whom?** It is a **108-page visual diff carrying a
   specificity change**, not housekeeping, and no wrap seat should land it. **Dave's.**
5. **The `#243` boot double-count is a GATE defect, not a record defect** (#269's separation), and it
   is now three sessions wide. Whether the gate's attribution is fixed, or `BOOT_CEILING_TK` raised,
   is **Dave's word alone** — a wrap may not pay that price to unblock itself.

## REPLAY-THESE

```
python3 knowledge/_capture_gate.py --wrap
python3 knowledge/_roll_state.py
python3 knowledge/_render_rulings.py --check
python3 knowledge/_build_memento_index.py
python3 knowledge/_parked.py --check
PYTHONPATH=knowledge python3 -c "import _capture_gate as c;print(len(c._carry_items([l for l in open('_CARRIES.md') if '#274:**' in l][0])))"
```
