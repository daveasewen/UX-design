# #258 lane C2 — THE SECOND COLD RUN (the frozen prompt against the tree at `e774fb7`)

**Model: Opus 5. Conductor: Fable (#258). Read-only — moved nothing in the library, ruled nothing.**

## DONE

- Read the brief chain (`2026-09-08-258-cold-run-2-brief.md` → `2026-09-08-258-cold-run-brief.md` →
  `2026-09-05-246-lane-B-baseline-brief.md § Arm A`) and the swiss idiom.
- Recorded HEAD `e774fb75a884899d5c2d21940d4e0856ac9a60be` in the READ-LOG. Pack under test = the
  working tree; skill resolved by frontmatter name `ADS-generate-from-canon`.
- **Arm A, blind.** No render, screenshot, gate, validator or playwright during the build. No
  `notes/`, `reviews/`, `_CHAIN.md`, `_rulings.json`, `_*.py`, no `outputs/coldrun-258/`.
  `briefs/` absent ⇒ ADS-grill-me fired; all six answers recorded *skipped/defaulted*, theme
  announced as **mono** before building (`brief.md`).
- Frozen: `outputs/coldrun-258-v2/arm-A-blind/dashboard.html`, sha256
  `f5f81124f117143f340c38f754ddb0b5f1904aea1dd8c91efa71461c03d64c1d`, 229,820 bytes. Never touched
  after the freeze.
- **Measured** (after the freeze): 4 renders at 1440/820 × light/dark; both gates + a control run of
  the unmodified template through the same gate; inventory; script provenance by sha256; 24 driven
  playwright steps including a cold reload from a state-bearing URL; `pageerror` and console capture.
- **Deliverables**
  - `outputs/coldrun-258-v2/arm-A-blind/` — `dashboard.html`, `READ-LOG.md`, `brief.md`, `GAPS.md`,
    `drive.json`, `render-{1440,820}-{light,dark}.png`.
  - `reviews/COLDRUN-258-2026-09-08-v2.html` — the review, three-column delta, 8 findings, 6 RSQs.
  - this file.

## SCORES — 3 / 2 / 3 / 3 (rich / full / persistent / interactive)

| score | value | the one fact that earned it |
|---|---|---|
| visually rich | 3 | 20 canon components on one page; every bar, sparkline coordinate and KPI figure computed from the data model at run time. |
| full | 2 | 52 payments / 8 accounts / 6 entities / 7 currencies, nothing placeholder, footer present — but the whole payments region renders broken at both breakpoints and both modes (F01). |
| persistent | 3 | A cold load of `?view=…&entity=de&ccy=EUR&range=all&sort=amount&dir=descending&page=2&size=8` restored `aria-sort="descending"`, `9–13 of 13`, all three chips and both trigger labels. #246 and v1 both scored 0. |
| interactive | 3 | 21 controls driven, zero dead; every filter re-drives grid **and** KPIs **and** chart **and** both context cards in one pass (rule 14's own test). |

**JS `pageerror` on load: 0.** 0 across the whole 24-step drive including a reload; 0 console errors.

## THE THREE-COLUMN DELTA IN ONE LINE

#246 2/2/0/2 → #258 v1 3/2/0/2 → #258 v2 **3/2/3/3**: s258-D1/D2 moved persistence 0→3 and
interaction 2→3 (dead masthead nav and non-filtering filters both now work, state survives reload,
uncaught errors 1→0), while *full* did not move and both gates still FAIL exactly as in v1.

## TOP THREE FINDINGS

1. **F01** — the payments region renders broken (grid ~400px inside a ~1300px tile, `#dgHint`
   overlapping the table, the Data-grid's `.dgseg` demo switcher spliced in with the component,
   from `Data-grid.reference.html` L323 and its `--demo-width` dial at L144). Invisible to the
   gates, to the console and to the drive log — this is the residue of the blind condition.
2. **F02** — a build that obeys rule 2 + rule 7a *cannot* pass `_validate_screen.py`. Control:
   `Template-dashboard-bento.reference.html`, byte-unmodified, fails the pack's own gate with
   `147 hex colour(s)` and the same four `c-bento*` class redefinitions (`knowledge/_screen-gate/tpl-probe.md:5`).
3. **F03** — rule 16 and Procedure step 6 require a console and a driven browser; the VS Code
   condition has neither, and the skill offers no fallback wording for the skip.

## CREATIVITY JUDGEMENT (labelled as a judgement)

What the rewrite bought is the **wiring between components that no single snippet can own** — one
filter change fanning out to grid, KPIs, chart and cards in a single pass, with all of it reflected
into the URL. A 2a-bound build can copy every component and still connect none of them, because
connective tissue belongs to no component; the old rule forbade exactly the part that makes a
dashboard a dashboard rather than a wall of specimens.

## NOT DONE (declared)

- **No sighted arm.** The brief specifies ONE arm. F01 is therefore reported, not fixed.
- **No receipt minted.** `gen_provenance_receipt.py --mint` was not run — the skill never instructs
  it, and minting would have changed the frozen arm.
- **No `sidebar-nav`, no second chart type, no tabs.** Rule 7a routed to the bento template, whose
  shell is the masthead; the extra components stayed on the "available, unused" list.
- **Grid row selection does not persist** across reload (filters, sort, page, size and view do).
- **Chart legend absent** — the template ships `dv-behaviour` without `dv-legend`; unchanged from v1.
- **`_validate_composition.py` C8 advisory not chased**: `advisory C8 RED group 'Payments' line 1651:
  3 non-tile element(s) interleaved with its members` — related to F01, left as measured.

## ⛔ FILES THE GATE WROTE INTO THE REPO — FOR THE CONDUCTOR

`_validate_screen.py` keys its ledger on the page *basename*, so running it on `/tmp/gate/dashboard.html`
still wrote into `knowledge/`. This lane may not run `git checkout`, so they are named:

- `knowledge/_SCREEN-GATE.md` — **modified**, restore with `git show HEAD:knowledge/_SCREEN-GATE.md`
- `knowledge/_screen-gate/dashboard.md` — **modified** (was a tracked PASS row), same restore
- `knowledge/_screen-gate/tpl-probe.md` — **untracked**, delete

## CONTEXT / TOKENS

Mid-lane check-in at the build→measure seam: ≈120K of the lane's context consumed at the freeze
(estimate, labelled as one — no `_gauge_tokens.py` read is permitted to this lane's blind half).
At wrap: ≈175K. The lane ran inside one conductor fill; no delegation.
