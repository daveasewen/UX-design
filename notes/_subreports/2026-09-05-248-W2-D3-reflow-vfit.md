# `#248`-`W2-D3` — REFLOW + VFIT: the 3-col band re-flows (rail fills, cards 3+3) and the chart fills its tile's height from data

session: `#248` · 2026-09-05
window: `W2 bento rhythm · lane D3 (s248-D1 re-flow + s248-D2 vertical fit)`
sub index: `W2-D3`
brief: the conductor's lane-D3 prompt in chat (two edits, one file; rulings `s248-D1` · `s248-D2` · `s248-D3` in `knowledge/_rulings.json` L6157–6203)
tokens: `≈120K by the harness counter (15,000,000 − ~14,880,000 at the report's write — QUOTA drawn across 20 tool calls; message.usage is not readable from a subagent)`

## VERDICT

DONE, both edits, in the ONE file `knowledge/snippets/Template-dashboard-bento.reference.html` (253+/21−). **A (`s248-D1`)**: rule 6b at L762–782 — two NAMED band rules (`grid-column:1 / -1` on the rail at wall ≤1100; the rail's grid keeps its base column count at rail ≥821) plus the two rail cards `data-c="6"→"3"`. Rendered at 1100: rail **1036px = the full row** (empty ground right of the rail 358.7 → **0px**), the two cards **side by side** at x=32 w=516 and x=552 w=516, same y=1055. **B (`s248-D2`)**: the carried fit engine grew a VFIT block (L1297–1362) — the svg's height answers its TILE (probe at `data-h`, take the tile's slack, floor **200px** via `data-h-min`), y/height of every mark re-derived from cached DATA fractions, `figure.dv-fit-on .dv-svg{height:auto}` releases the fixed 260. Chart at 1440: **856×415** (was 856×260; empty ground below the figure **155.1 → 0.1px**); at 1100: **988×260** (content-sized row, the preferred height). **Physics assertion PASS** — 14 checks: label and tick font-size 12px = 12px, bar and grid stroke 1px = 1px, svg height 415 ≠ 260, bar heights re-scaled from data (Housing/Groceries = 950/420 at both), rendered bar box = height attr, viewBox re-pinned 1:1 in both axes. Live resize 1440→1100→1440→820→1440 shrinks and grows back (`0 0 856 415` · `988 260` · `856 415` · `708 260` · `856 415`). First-chart-y **516.5** at 1440 (no regression). Page heights 1440 **1064** (unchanged) · 1100 **1374** (was 1602, the re-flow) · 820 1825 · 520 2300. Snippet gate **exit 0**. Composition gate **exit 1** — the SAME three C9 reds as lanes D and D2 (per-tile: base `data-c=4`, `data-c=2` at ≤1100; row-sum 8 at 3 cols): the gate reads only canon-shaped clamps and cannot see rule 6b, so its static `[3,3,2]` read of the 3-col band no longer matches the browser's 3 / 3 — gate-RED vs render-orphan-free is the ruling-shaped question (RSQ 1). Gate NOT edited. `Chart-bar.reference.html` NOT edited (RSQ 2). Nothing of Dave's ruled beyond D1–D3.

COUNTS: findings `9` · ruling-shaped `4` · UNPROVEN `4`

## What was done

**A · `s248-D1` — the re-flow band.** `knowledge/snippets/Template-dashboard-bento.reference.html`:
- L762–776 comment "6b · THE RE-FLOW BAND" — names both rules, quotes Dave, says why a band rule is the only clean lever, and that C9 cannot read it.
- L777–779 `@container bento (max-width:1100px){ .tpl-page .c-bento__tile.tpl-group-context{ grid-column:1 / -1; } }` — queries the WALL (the rail's nearest container). `1 / -1` is "the whole row" at every band ≤1100, so it needs no re-statement at ≤820/≤520 and cannot overflow a 2- or 1-column grid the way `span 3` would.
- L780–782 `@container bento (min-width:821px){ .tpl-page .c-bento.tpl-group-context > .c-bento__grid{ --bento-cols-now:var(--bento-columns); } }` — queries the RAIL's own width. Without it canon's ≤1100 clamp makes the rail a 3-column grid at 1036px, where `data-c="3"` is the full row (stacked); with it the rail keeps 6 columns while wider than the 2-column band, so 3+3 is two columns. `var(--bento-columns)` not a literal 6 — the instance dial stays honoured.
- L1042, L1053 the two rail cards `data-c="6"→"3"` — 3+3 of 6 is legal by the snippet's own law at every band (C9 reads the 'Position' grid `[3,3] → base [3,3]; ≤1100 [3,3]; ≤820 [2,2]; ≤520 [1,1]`, no red).
- GROUP 2 comment (L977–983) and GROUP 3 comment (L1030–1037) rewritten to say the 3-col band now re-flows.
- **Why a band rule, not a span:** the brief asked for the smallest CSS inside the snippet's band rules or the `.tpl-group-context` rules. No span value does it — `data-c="2"` is what 6 columns need and nothing the canon clamps do to a 2 gives 3 of 3; grid has no "fill the rest of my row" (`-3 / -1` puts the rail in cols 2–3 with col 1 empty; `span 3` overflows at 2 cols). So the band rule is written in the template's OWN rules block (rule 6b), not inside the canon `[data-c]` clamps at L375–387 (untouched, byte for byte), and not copied from another file.

**B · `s248-D2` — the vertical fit.** Same file:
- L674–676 `figure.dv-fit-on .dv-svg{height:auto;}` (JS-on release of the fixed 260px height; the svg's box then follows the viewBox 1:1 in both axes; JS-off keeps 580×260).
- L998 svg gains `data-pt="14" data-pb="30" data-h-min="200"` — the plot frame's top pad and label zone (the kit's bake, read off the baked geometry: top gridline y=14, baseline y=230=260−30) and the floor.
- L1000–1008 the five tick texts gain `data-dy="3"` — a fixed pixel offset from their gridline, the y-axis analogue of `data-dx`; without it the 3px would scale with the plot.
- L1297–1362 the VFIT block in the carried script: `H_MIN_DEFAULT = 200` · `slackBelow(svg)` (tile content-bottom − figure bottom, walking up to the nearest `.c-bento__tile`) · `fitHeight(svg, W)` (probe the layout at `data-h`, add the slack, floor) · `fitY(svg, H)` (caches the bake ONCE as `data-fy`/`data-fh`/`data-fy1`/`data-fy2` fractions of the baked plot height, then re-derives y/height per pass; a text anchored below the baseline — the category labels at y=246 — is re-anchored to the baseline with `data-dy` 16 so it rides the baseline at a fixed offset). `fitOne` (L1364) now calls `fitHeight` before the viewBox pin and `fitY` after the x-loop; the viewBox is `0 0 W H`.
- The script's HTML comment (L1247–1252), the FIT banner (L1262–1264) and THE CHART CSS comment (L659–662) say the copy now also fits height and that carrying it back is an RSQ.
- **The floor is 200px** (`data-h-min`, default `H_MIN_DEFAULT`). It only binds when a tile is SHORTER than `data-h` (260) — with `minmax(row-unit,auto)` rows that never happens in this template; it exists for `s248-D3`'s edit mode, where a fixed short tile is plausible.

**Gates**
1. `python3 knowledge/_validate_snippets.py` → `snippet gate: 136 snippet(s), 0 failure(s)` — **exit 0** (one line of output).
2. `python3 knowledge/_validate_composition.py knowledge/snippets/Template-dashboard-bento.reference.html` → **exit 1**. Reds verbatim: `C9 RED  line 987 data-c=4 in 'Account overview': span 4 does not divide 6 columns at band base (orphan column)` · `C9 RED  line 1039 data-c=2 in 'Account overview': span 2 does not divide 3 columns at band <=1100px (orphan column)`. · `C9 RED  grid 'Account overview' (line 883): spans sum to 8 at band <=1100px (3 cols) - not whole rows, an orphan cell` — three reds, the same three as lanes D/D2; the third is the static `[3, 3, 2]` read that rule 6b makes stale (Finding 3). Gate NOT edited.
3. Render: `outputs/w2-rhythm/template/_render_w2_reflow.py` (derived from lane D2's `_render_w2_fit.py`; Playwright `goto("file://…")` only, `set_content()` not used) → `reflow-{1440,1100,820,520}-{light,dark}.png` (8 full-page panes), 0 page errors each, body bg rgb(240,240,240) light / rgb(26,26,26) dark; `measure.json` keys `reflow-*` + `reflow-live-resize`. **Exit 0** = the physics assertion passed.
4. `git diff --stat -- knowledge/` shows TWO files: this snippet (253+/21−) and `knowledge/_rulings.json` (49+) — the latter is the conductor's uncommitted `s248-D1/D2/D3` rulings (`git diff` `+"id":` = exactly those three), not this lane's. This lane touched ONE file.
5. `_build_all.py`, `_validate_screen.py` NOT run; `Chart-bar.reference.html` NOT edited; no commit, no push.

## Findings

1. **Rail at 1100 fills its row, cards side by side** (`measure.json` `reflow-1100-light`): `context.w` **1036** (= the wall's 3-col row; lane D measured 677.3 with 358.7px empty), `emptyRightOfRail` **0**; `railCards` `[(32, 1055, 516, 271.1), (552, 1055, 516, 271.1)]` — two columns of 516 with the 4px sub-gutter, same y; `railCols` = two 516px tracks of the rail's 6-column grid. Eyeballed `_peek-d3-reflow-1100-light.png`: chart on a full row, Balances | Needs attention beneath as two panels. Dark identical.
2. **1440 unchanged in layout, changed in ink** (`reflow-1440-light`): rail 432 wide, cards stacked `(976, 516.5, 432, 271.1)` / `(976, 791.6, 432, 224)` — the `min-width:821px` rule does not fire on a 432px rail. Chart card the same 499.1px row; the svg now **856×415** (viewBox `0 0 856 415`), `emptyBelowFigure` **0.1px** (was 155.1), `emptyRight` 0. First-chart-y **516.5**. Page height **1064**, unchanged.
3. **The composition gate stays RED by its own law, and its static read of the 3-col band is now WRONG about the render.** C9 (`_validate_composition.py` L100–107) reads only canon-shaped clamps (`.c-bento__grid > .c-bento__tile[data-c="N"]{grid-column:span S;}`), so rule 6b's `grid-column:1 / -1` is invisible to it: it still reads the wall as `[3, 3, 2]` at ≤1100 (row-sum 8) while the browser lays out 3 / 3 (row-sum 9, `context.w` 1036). Side by side: gate line `C9 RED  line 1039 data-c=2 …: span 2 does not divide 3 columns at band <=1100px (orphan column)` vs render `emptyRightOfRail 0`. The base red (`data-c=4`, 4+2 = 6 fills the row) is the per-tile arm lane D already named. Both are the gate's law vs `s248-D1` — RSQ 1, not edited here.
4. **Physics assertion PASS, 14 checks** (`_render_w2_reflow.py`, light 1440 vs 1100): chart-area width 856 ≠ 988 · label font-size `12px` = `12px` · label box height identical · tick font-size identical · bar stroke `1px` = `1px` · grid stroke identical · **svg height 415 ≠ 260** · bar height attrs differ (`[155.8, 66.8, 352.4, 96.5, 78, 111.3]` vs `[90.7, 38.9, 205.2, 56.2, 45.4, 64.8]`) · Housing/Groceries = 950/420 at both (352.4/155.8 = 2.262; 205.2/90.7 = 2.262; data 2.262) · rendered bar box = height attr (1:1) · viewBox `0 0 856 415` / `0 0 988 260` · empty below the figure <1px at 1440 · svg ≥ 200 · bar width moved 74.5 → 86.8. Same at dark.
5. **The resize path is proven both ways** (`reflow-live-resize`): one page, `set_viewport_size` 1440→1100→1440→820→1440, viewBoxes `856 415` → `988 260` → `856 415` → `708 260` → `856 415`, 0 page errors. The probe-at-`data-h` step is what stops the height ratcheting up: without it a content-sized row after a stretched one would measure 0 slack against the already-grown svg and never shrink.
6. **Page heights**: 1440 **1064** (=) · 1100 **1374** (was 1602: the rail's cards side by side take 271 instead of 271+4+224, and the orphan column's ground is gone) · 820 **1825** (=) · 520 **2300** (=). First-chart-y per band 516.5 · 671 · 893.5 · 1368.6 — identical to lanes D and D2.
7. **DP-18 inside the rail at 1100**: "Needs attention" (content 224) is stretched to Balances' 271.1 in the shared row — 47px of air under its three rows (`railCards[1].h` 271.1). Same mechanism lane D found on the chart; the chart's version is now closed by VFIT, the rail's is not (a Stat-card has no fit engine). `s248-D3`'s constraint ("every component placed in a bento tile MUST be responsive … width and height") names this exactly — RSQ 3.
8. **What VFIT does not carry**: polylines/paths (`data-fxs`/`data-ys`) keep their baked y — this chart has none; the sparklines in the KPI row are not `svg.dv-fit` and are untouched. The `<g data-dv-view>` wrapper has no y and is skipped. The cache attributes (`data-fy` etc.) are written by the engine, not authored — the markup stays bake-only.
9. **520 horizontal overflow** is still the pre-existing `.sh-actions` demo chrome (`ovx True` at 520 only; lane D Finding 8). Not this lane's.

## RULING-SHAPED QUESTIONS

1. **The gate's law vs `s248-D1` (Finding 3).** The render is orphan-free at every band and the gate is RED twice: the per-tile arm rejects 4 of 6 at base (a row that FILLS), and the static band read cannot see a `grid-column:1 / -1` re-flow. (a) teach C9 a row-sum law (4+2 legal where rows fill) AND to read `grid-column:1 / -1` in a scoped band rule as "full row" — the gate then matches the render; (b) keep C9 as the canon-only reader and carry the two reds as a declared exception on this template; (c) revert the re-flow to a canon-shaped clamp (`[data-c="2"]{span 3}` at ≤1100 inside the canon block) so C9 reads it — a global rule the brief forbade, and it would re-span every data-c="2" tile on the page. Recommend (a) — the gate should parse what the artefact does (the "no gate parses the artefact" hook is exactly this). Dave's.
2. **Carry VFIT back to `Chart-bar.reference.html` / `dv-behaviour.js`?** The template now has a fit engine the component lacks — `s248-D2` governs BOTH files and `s248-D3` says every component in a tile must fit both axes. (a) carry VFIT into `dv-behaviour.js`'s FIT module (+ `data-pt`/`data-pb`/`data-h-min` on Chart-bar's svg, `data-dy` on its ticks, the `height:auto` release) and regenerate the partial — then the template's stub can be deleted per its own comment; (b) leave the template as the specimen until the library-wide fit question is scoped. Recommend (a), as its own lane, because the physics assertion here is the test that lane needs.
3. **The rail's stretched card (Finding 7).** "Needs attention" carries 47px of air at 1100 in the shared row. (a) accept — a panel with air under its list is the specimen's look; (b) `align-items:start` on the rail's grid at the side-by-side band (the cards stop stretching; the seam reads as two unequal panels); (c) a Stat-card "fit" — content answers height (`s248-D3`'s reading, a library question). Recommend none.
4. **The floor constant 200px** (`H_MIN_DEFAULT`, `data-h-min`) — a number picked as "sane", not ruled; it never binds in this template. (a) keep 200 as PROVISIONAL-AWAITING-DAVE; (b) tie it to the evidence row-unit (240) minus the head; (c) drop the floor and let a short tile clip. Recommend (a).

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** the re-flow band under the real HSBC face — no Univers Next in the sandbox; the geometry (1036 / 516+516, svg 415) does not depend on the face, the rail card heights (271.1 / 224) do. Price: one 1100 + 1440 render on a host with the font (~3K).
- **UNPROVEN:** the floor path (a tile shorter than 260px) — no band in this template produces one; `H < Hmin` is exercised only by reading. Price: a Playwright pane with the chart card given a fixed 240px height and a measure (~2K).
- **UNPROVEN:** whether `gen_canon_components.py` / canon.css projection changes — three new CSS declarations in the snippet's own `<style>` (`height:auto`, rule 6b ×2); not run (fenced). Price: `--check` at the wrap (~2K).
- **UNPROVEN:** the JS-off render (no `.dv-fit-on`: 580×260 fixed, horizontal scroll) after the `height:auto` rule — the rule is scoped to `.dv-fit-on` so the static answer should be untouched; not rendered with JS disabled. Price: one pane with `java_script_enabled=False` (~1K).
- **CLAIMED:** the container-query nesting reading (rule 6b (i) queries the wall, (ii) queries the rail) — from the CSS Containment spec and from the render matching it at all four bands, not from an isolated probe of `getComputedStyle` per container.

## Evidence

`outputs/w2-rhythm/template/` — `reflow-{1440,1100,820,520}-{light,dark}.png` (8 full-page panes, the after-state of both edits) · `_peek-d3-reflow-1440-light.png`, `_peek-d3-reflow-1100-light.png` (crops of the wall, eyeballed) · `measure.json` keys `reflow-*` (lane D2's keys + `svgH`, `barHeights`, `barBoxHs`, `baselineY`, `catLabelY`, `tickFontSize`, `gridStroke`, `railCards`, `railCols`, `wallCols`, `emptyRightOfRail`) and `reflow-live-resize` · `_render_w2_reflow.py` (re-runnable; exit 0 iff the 14-check physics assertion holds; needs `LD_LIBRARY_PATH=~/.local/chromelibs PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS=1`).
Lane D2's `fit-*.png` are the BEFORE of this edit — compare `fit-1100-light.png` (orphan column, stacked rail) vs `reflow-1100-light.png`, and `fit-1440-light.png` (155px under the chart) vs `reflow-1440-light.png`.

REPLAY-THESE: `outputs/w2-rhythm/template/_peek-d3-reflow-1100-light.png` (~1.5K tk, the re-flow) · `outputs/w2-rhythm/template/_peek-d3-reflow-1440-light.png` (~1.5K tk, the chart at 415) · the two C9 red lines + Finding 3's render numbers side by side (~0.3K tk)
