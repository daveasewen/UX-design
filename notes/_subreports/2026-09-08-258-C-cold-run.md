# #258 lane C — THE COLD RUN: the frozen prompt against the v1.0.7 ZIP, blind

**Model: Opus 5. Conductor: Fable (#258). Brief: `notes/_briefs/2026-09-08-258-cold-run-brief.md`. Read-only against canon/knowledge. Nothing of Dave's ruled.**

## What was done

1. **Unzipped the pack, not the tree.** `apollo-spider/dist/Apollo-Spider-v1.0.7.zip`, sha256 `13a593de1b117b144398bd928e024e430bada004f561957e8895147928974228` — matches `s257-D3`. Unpacked to `/tmp/ads-1.0.7/`, 1,678 files, one top-level dir. Every Arm A read came from there.
2. **The prompt resolved.** `/generate-from-canon` → `skills/generate-from-canon/SKILL.md`, whose frontmatter name is now `ADS-generate-from-canon`. Directory unchanged; command and skill name now differ by prefix (F10).
3. **Grill-me fired** (`briefs/` absent in the zip). All six recorded skipped/defaulted, Mono announced before the build. Rule 7a's *"dashboard bento — is that right?"* asked and defaulted — a skip is a yes. `outputs/coldrun-258/arm-A-blind/brief.md`, with a 7-item Gaps list.
4. **Arm A built blind.** Bento-first splice of `Template-dashboard-bento.reference.html` + `Sidebar-nav` + `Filter-toolbar-bar` + `Data-grid`. No JS authored. No render, screenshot, gate, playwright or validator before the freeze; no `notes/`, `reviews/`, `_CHAIN.md`, `_rulings.json` or any `_*.py` opened during the arm. `outputs/baseline-246*` not opened until after the freeze.
   **FROZEN:** `outputs/coldrun-258/arm-A-blind/dashboard.html`, sha256 `7619d71861ba3d2d55fdf370118f2c543172b49871bd5155c21bdbda646a1d3e`, 214,957 bytes. Never touched again.
5. **Playwright installed** per `knowledge/_ROBUSTNESS-PORTABILITY.md` — 4 calls: `pip install playwright` (1.62.0), `python3 -m playwright install chromium-headless-shell` with `NODE_EXTRA_CA_CERTS` + `PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS=1`, the `libXdamage.so.1` user-space fix (`apt-get download libxdamage1` → `dpkg-deb -x` → `~/.local/chromelibs/`), launch. The runbook was exactly right, including the failure mode.
6. **Measured** at 1440 and 820, light and dark: 4 renders, both gates, inventory, chart-completeness by sha, 17 driven controls, persistence across reload, data believability, four scores. `outputs/coldrun-258/drive.json`.
7. **Review page** written in the swiss idiom: `reviews/COLDRUN-258-2026-09-08-v1.html` — renders, gates, inventory, chart completeness, interactions, persistence, scores, the #246→#258 delta table, 12 numbered findings, 8 ruling-shaped questions left unanswered. Checked at 1440 and 820: no horizontal overflow, all 4 renders load, no page errors.

## Mid-lane check-in — context at the build→measure seam

Taken at the moment the arm was frozen, before the first gate ran: **≈138.8K cumulative tokens** consumed by this lane (15,000,000 → 14,861,190 remaining on the harness counter). At report time: **≈198K**. The single largest cost was `Template-dashboard-bento.reference.html` — 1,801 lines, 123,798 bytes — which rule 7a makes mandatory for any dashboard; I read its structure by grep and its body region (894–1180) in full rather than the whole file, which is F12 on the review page.

## Scores (rich / full / persistent / interactive, 0–3)

**3 / 2 / 0 / 2** — versus #246 Arm A's 2 / 2 / 0 / 2. Earning facts on the review page and in the return note.

## Top findings (12 on the review page)

1. **Rule 7a and step 5 contradict each other.** The pack's own unmodified `Template-dashboard-bento.reference.html`, through the pack's own copy of `_validate_screen.py`, prints `compose: ❌ 146 hex colour(s) … redefines canon class(es): ['c-bento','c-bento__caption','c-bento__grid','c-bento__tile']` → FAIL. Splicing the snippet the skill names cannot pass the gate the skill names.
2. **Rule 2a's premise is false where it matters.** 21 of 136 metas carry a `behaviour` key; `data-grid`, `filter-toolbar-bar` and `sidebar-nav` have none at all yet ship 17,594 / 5,403 / 3,908 bytes of working script. `_validate_receipt.py` has nothing to check for the three components carrying the page's interactivity.
3. **Everything that works is self-contained; everything dead reaches across.** Grid sort/filter/paging works, nav highlight works, chart tooltip works. Filter dropdowns leave the grid at 8 rows and the chart bar heights identical (`chart_unchanged:true`); the view switch changes no view; the KPI's table CTA opens nothing; the masthead never moves `aria-current`.

## What was NOT done (declared)

- **No sighted arm.** The brief specifies one arm; #246's Arm B is the sighted comparison and was not re-run.
- **The layout defect in the frozen arm is mine, not the pack's.** My harness comment contained `.c-*/`, which closes a CSS comment early and swallowed `.ads-shell{display:flex}`. Proved: the parsed stylesheet holds `.ads-shell > nav.sn` but not `.ads-shell`, and computed `display` is `block`. The arm was frozen, so it was declared, not fixed — the sidebar therefore stacks above the content at 1440 instead of sitting beside it. It affects the *appearance* of the renders; no interaction, gate result or score depended on it.
- **`--render` (state-contrast) leg of `_validate_screen.py` not run.**
- **Console theme (`data-apollo-theme`) legs not rendered** — Mono only, per the skill's own default. Dark was produced by setting `data-theme` from outside, because removing the template's declared demo chrome removes the only theme toggle (F05).
- **Not read blind:** `knowledge/_render/_bento_edit_rails.json` (rule 7 points at it; outside the #246 Arm A allowed list, which this brief inherits).
- **No judgement offered on what sentence 1 alone would have produced** — the brief did not ask for it this time.
- **Symbol-id collision left in place** (F06): `ic-account` differs between `Sidebar-nav` and the template. Rule 2 says copy verbatim, so it was copied and logged as a Gap rather than renamed.

## Side effects, declared

- `_validate_screen.py` writes into `knowledge/` and keys its ledger on **basename**. Because the arm is `dashboard.html`, the run overwrote the tracked row `knowledge/_screen-gate/dashboard.md` (previously `PASS ✅`) and flipped its line in `knowledge/_SCREEN-GATE.md`. **Both restored** with `git show HEAD:<path> > <path>` — targeted writes to the two exact paths, never `git checkout` (per the #253 hook: two lanes wiped each other that way).
- **One file I could not remove:** `knowledge/_screen-gate/tpl-unmodified.md`, 864 bytes, untracked, written by the gate during the control probe. `rm` returns `Operation not permitted` on this mount. **Left on disk for the conductor to delete.**
- `knowledge/_graph-mark-observations.jsonl` and `notes/_REHEARSAL-LOG.jsonl` are also dirty — **not mine** (a memento query record and two rehearsal rows). Not touched.
- Nothing under `showroom/`, `apollo-spider/`, or any snippet / meta / canon file was edited. No `_build_all.py`. No commit, no push. Nothing of Dave's ruled — the 8 RSQs sit unanswered on the review page.
- Build and measure tools live outside the repo: `/tmp/build/build.py`, `/tmp/drive.py`, `/tmp/probe2.py`.

## Tokens

≈138.8K cumulative at the build→measure seam; **≈198K at report time**, on the harness counter.

## Paths

- `outputs/coldrun-258/arm-A-blind/dashboard.html` (frozen, `7619d718…`) · `brief.md` · `READ-LOG.md` · `canon/type.css` + `assets/logos/` copied beside it so the frozen page resolves
- `outputs/coldrun-258/renders/{1440,820}-{light,dark}.png` · `outputs/coldrun-258/drive.json`
- `reviews/COLDRUN-258-2026-09-08-v1.html`
- `notes/_subreports/2026-09-08-258-C-cold-run.md`
