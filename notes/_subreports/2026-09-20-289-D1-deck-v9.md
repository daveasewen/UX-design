# D1 · deck v9 — the recut on Dave's seven beats, with the three drawings — #289 — 2026-09-20

Brief: `notes/_lanes/289/deck/BRIEF-v9-recut.md`. Source: `notes/_DEMO-SLIDES-apollo-2026-09-19-v8.html` (untouched).
Output: `notes/_DEMO-SLIDES-apollo-2026-09-20-v9.html` — 12 slides. No commit.

## What moved
- **The gearbox moved from s3 to s4.** The `#gb` canvas and `#gbPrint` img moved with it; `io3` now observes `#s4`,
  the focus-restart probe reads `#s4`, and every `#s3 …` card rule (card ground, `.inner` grid, `.type`, `.draw`,
  `canvas`, the print img, the 900px breakpoint, the print block) was generalised to `#s4, #s6, #s7`. The gearbox
  itself — geometry, ink, callouts, its `i = 6.43 : 1` scale bar, the 30 fps cap, DPR 1.5 — is byte-for-byte v8.
- **s1 verbatim** from v8 but for the pagenum; **s12** is v8's s11 verbatim (4,820 · 8,648), with the `.close-line` /
  `.count` rules re-pointed `#s11 → #s12` in both the head CSS and the G3 lane block.
- **s10** is v8's s9 with Dave's names on the three cells (The inventory · The assembly engineer · The inspector);
  the `.grid4{3 cols}` rule re-pointed `#s9 → #s10`. **s11** is v8's s10, ask text and the one line under it replaced,
  `.draft` stamp and the research paragraph kept.
- **s9** is a re-cut of v8's s8 (the build). **Dropped entirely:** v8's s4 breakdown card and its `data-lane="G1"`
  style block, v8's s5 turn card, v8's s6 number card (`.nums`), v8's s7 explorer card.

## What is new
- **s3 · Why now** — the premise, dark, his words in his order: "Dev got fast." / "Build fast, test with real,
  shippable code. Design became the bottleneck." / "So we asked: how do we speed up design?" Foot: the first experiment.
- **s5 · The robots** — grey. **s8 · The inspector** — dark. Both copy-only cards.
- **s2** now carries the eight chapter titles, not five with descriptions; `.chapters` is re-gridded to 4×2 and the
  numerals stepped down so two rows sit on the card.
- **s6 · the books** (`#bk`) and **s7 · the brain** (`#br`), ported from `notes/_lanes/289/illustration/books.html`
  and `brain.html` as two more IIFEs (the brain's inline `TRACE` data comes with it). Geometry, ink and resting
  views are unchanged. What the deck added, and only this: an IntersectionObserver on `#s6` / `#s7` so they draw
  only in view (plus the blur/visibility stops the gearbox has), and a `snap()` that bakes the resting frame to
  `#bkPrint` / `#brPrint`, copied from the gearbox's snap. The books page's `n = 7 vol` caption / scale bar is
  dropped; the brain page had no caption in canvas. All three drawings share the one card treatment — white
  canvas ground, no border, the `.inner` grid as the only inset — because they now share the one rule set.
- New scoped type sizes for the three drawing cards (`h2` clamp 24–34, `.lead` 16–19, `.body` 15) so the long
  beats fit the 38fr type column beside a 58fr drawing.

## Figures
All sourced, none invented: 36 (Dave's word for the Figma count), 125 / 12 / 8 / 137 (showroom/index.json),
101 = 137 − 36, 4,820 · 8,648 (strand map). `rv d1…d5` kept on every revealed element; `pagenum` is `NN / 12`.

## Verification
`playwright`, chromium, 1440×810, DPR 2 — `notes/_lanes/289/deck/render-v9/s01.png … s12.png`, looked at.
- 12 slides; **zero page errors, zero console errors**.
- **Zero overflow**: no descendant of any slide crosses its section box by more than 1 px (probe over every element
  on all twelve).
- The three canvases are drawn, not blank: `gb` 4.67 % non-white pixels, `bk` 6.61 %, `br` 3.25 %.
- Print: `gbSnap/bkSnap/brSnap` bake 268 KB / 598 KB / 322 KB data-URLs; print-media screenshots at
  `render-v9/print-s6.png` and `print-s7.png` show the baked books and brain in place of the canvases.

## Unsure of
1. **s3's shape.** Dave said he was "not sure how to weave this in". I gave the premise a whole card at the front and
   split his sentence across h2 / two leads in his order rather than compressing it. Easy to demote to a foot on s4.
2. **The card treatment.** The brief says "white card, same border/inset as the gearbox's `.draw`" — the gearbox's
   `.draw` in v8 has neither a border nor an inset, so I read that as "whatever the gearbox has, all three get" and
   added nothing. The brain's own illustration page *does* draw a 1px `--g2` border; if that was the intent, one
   rule on the shared `.draw` selector adds it to all three.
3. **s2's second row** has no rule above it (the `.chapters` border-top is on the list, not the row). It reads fine
   at 1440; it is the one place the eight-item strip differs from the five-item one.
4. **s11's two voices.** "The ask is Dave's, with his colleagues — open." is about the deck, not in the deck's voice;
   it sits as a lead under the ask. It may want to be the foot instead.
