# `#260`-`wrap` — the delegated capture ritual for #260

session: `#260` · 2026-09-08
window: delegated wrap sub (Opus 5), conductor **Fable**
sub index: `wrap`
brief: `notes/_briefs/2026-09-08-260-wrap-brief.md`
tokens: `UNMEASURED — a sub cannot read its own message.usage; the figure at this seat is unobservable rather than large-or-small`
provenance: 260 · 2026-09-08
status: observed

## VERDICT

**Every step of `knowledge/_RUNBOOK-capture-ritual.md` was run and ONE was REFUSED by machinery
rather than skipped.** Steps 1, 1b, 2, 2c, 2d, 2e, 2g, 3 (declared not-mine), 4, 4b, 4c and 5 are
DONE; **step 2f is REFUSED**, with the mover's own words recorded in four places. Two structural
gate fails stand at the close and the commit is made in the **#243 form** rather than routed
around: the boot-drift ceiling arm (#255's 70,127, the fourteenth consecutive refusal) and the
duplicate session key that leaves the `#### 2026-09-08 #259` stratum in `GOOD-MORNING.md`.
**Nothing was renamed, re-keyed, exempted or de-keyed to make a gate read green.**

COUNTS: findings 4 · ruling-shaped 1 · UNPROVEN 0

REPLAY-THESE: the 2f refusal and why no repair was taken (~250 tk) · the two-armed stop line now
in the chain (~120 tk) · the size/chain figures (~150 tk).

## What was done

- **1 / 1b** — `_LIVE-STATE.md` refreshed: new ⏱ LATEST DELTA #260, `Last refreshed` rebuilt with
  the #260 summary and the #257 `Previous:` segment trimmed at the same 2d boundary. Dossier
  written: `_DECISION-HISTORY/2026-09-08-260-the-budget-ruling-and-the-fast-followers.md`.
- **2** — new ★ LATEST banner #260: **1,197 tape / 10 substantive lines** against the `s241-D2`
  cap of **1,200 / 10**, measured with `_capture_gate.py`'s own slice. Written SHORTER, never
  quieter — the gauge, the declared skips, the not-done list and the STILL-HIS line live in the
  ⏱ delta, which the cap's own text names as their home. `size:` stamp re-written LAST.
- **2c** — the **#258** banner (11 ln) → `_GM-ARCHIVE.md` § `Batch 2026-09-08 #260`.
- **2d** — the **#257** ⏱ delta (15 ln) + its `Previous:` chain segment → `_LIVE-STATE-ARCHIVE.md`
  § `Rolled 2026-09-08 #260`, in the same section, which is the pairing the retirement table
  demands. Batch headers were created as the FIRST mover transaction, before any roll ran.
- **2e** — nothing dated to roll; the SIZE is declared (`## ⬛ DO THIS FIRST` = 108 raw lines).
- **2f** — ⛔ **REFUSED. See finding 1.**
- **2g** — `knowledge/_build_memento_index.py` rebuilt LAST, after every GM/LS edit: 2,097 records.
- **3** — memory is the CONDUCTOR's by the brief; **0 bytes at this seat**, and the store is
  invisible to the shell and to every gate (D1a).
- **4** — four rulings READ BACK from `knowledge/_rulings.json` entries **404–407**, never
  re-inscribed. `knowledge/_state.json`: **12 rows** (eleven filed lane reports + this one);
  `knowledge/_REVIEW-SIGNOFF.md`: **7 rows**, one per committed fast-follower test page.
- **4b** — both titles GENERATED by `knowledge/_gen_titles.py --session 260` (receipt written).
- **4c** — scratch hygiene RUN and it mattered: **351 owned entries removed**, VM fill
  **79.7% → 61.9%**.
- **5** — one wrap commit by the sanctioned path, then `knowledge/_memento-index.json` separately.

## Findings

1. **2f was refused by the mover, and the refusal is the honest outcome.** `_gm_move.py roll_2f`
   (dry run, then not attempted live) printed verbatim: *"roll_2f: notes/_GAUGE-LOG.md already
   carries a block for #259 — the contract is one block per session, and a duplicate key silently
   splits the record of one session across two places — NOTHING written"*. Cause: the #259 opener
   wrote `#### 2026-09-08 #259 — ceiling discharge` into the log under `s244-D1`, and
   `_key_session` reads the key, not the suffix. **The brief licensed a rename of that heading "if
   the runbook allows"; it does not.** `notes/_GAUGE-LOG.md` is append-only by its own declared
   contract, and `ds-022 (d)`'s docstring records what a key edited to quiet a parser cost last
   time: three unrolled sessions read as rolled, and a handoff instructed a roll of twelve blocks
   that was really nine. De-keying is that move with the sign flipped. #259 also put the three
   options (exempt · re-key · teach the discharge form a non-session key) to Dave, so choosing one
   here would rule his item. ⇒ **NAMED, not repaired.** `GOOD-MORNING.md` closes with two live
   strata against 2f's cap of one, and `_roll_state.py`'s generated line says so on the banner:
   `2f OVER (strata 2, log #259)`.
2. **`_checkin.py`'s B2 plan block could not resolve a stop line at all, and that was repaired.**
   `CHAIN_STOP_RE` found no `stop line <N>` figure in `_CHAIN.md`, so the next seam's block could
   not be rendered from state. The ⏱ delta's gauge bullet now carries **both arms** — *"stop line
   150,929 real when the wrap runs INLINE · 180,000 real when the wrap is DELEGATED (`s260-D2`)"*
   — with the conservative arm first, because the regex takes the first match and an inline wrap is
   the assumption that costs least if wrong.
3. **The chain was measured twice and both readings are published.** `_gen_chain.py` reached a
   fixed point at **8,894 tape** after the declare-last `size:` stamp, and at **8,982 tape**
   (slice 8,131 + wrapper 851) after finding 2's repair. **The second is the figure #261 pays.**
   Against #259's 7,937 the chain is **1,045 tape UP**, inside the `s214-D6` ~10–12K target for a
   fifth consecutive wrap, and **24%** of `GOOD-MORNING.md` against the generator's `<40%` floor.
4. **GM grew and the cause is one unrollable block, not prose.** GM **34,587 → 38,632 tape**; the
   STRATA region alone went **6,986 → 11,041 real** because #259's stratum stayed. §A is
   **BYTE-IDENTICAL for a twenty-third consecutive wrap** (16,489 B, digest `4311cce4f78114cb…`,
   proven at this seat against `git show HEAD:GOOD-MORNING.md`).

## RULING-SHAPED QUESTIONS

1. **The `#### <date> #<N>` key collision between a ceiling-discharge declaration and a session
   stratum — unchanged from #259 and now MEASURED rather than predicted.** Three shapes, his pick:
   add this one block to `STRATA_EXEMPT` (a closed list of three by Dave #58) · re-key the
   discharge block · or teach the `s244-D1` discharge form to use a NON-session key so a
   declaration can never eat a stratum's key again. ⛔ **Nothing here chooses.** Until it is ruled,
   every future wrap inherits a permanently over-cap strata stack.

*(The rest of this session's ruling-shaped material — twelve items — is the conductor's list,
carried by name into `_CARRIES.md` § `## residual → #261`. It is not re-stated here and was not
ruled at this seat.)*

## Declared skips, each with its size

`_CARRIES.md` never read whole (**9,301,931 B**; only the `→ #260` line was read and the `→ #261`
line built from it by ONE programmatic pass — **491** age bumps + **5** surgical edits, each
asserted unique) · memory **0 bytes at this seat** · `_build_all.py` NOT run at any seat ·
`_validate_screen.py` NOT run (its clobber is the reason) · `test_gates.py` NOT RUNNABLE (≈5 GB
copytree) · no render, no browser, no retrieval query, **no recall plant — zero recall claims** ·
`dist/`, the frozen ledger and the twelve lane reports NOT touched at this seat.
