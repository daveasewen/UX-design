# #260 lane I — INTEGRATION of the eight fast-follower chart types · subreport

**The `s259-D1` fast follower is integrated and committed.** Eight chart types are on the #259
engine, the registry carries six new `$behaviour` entries, all fourteen dataviz members' page
figures are inside budget, every gate in the sweep is green including the nine v1.0.8 release
gates, the driven receipts cover **13 of 13** pages FRESH with **zero pageerrors and zero console
errors in 104 theme × mode drives**, and the working tree is clean but for the two jsonl the brief
excepts.

Four commits: `9a702b5` · `1b8a3f1` · `4a3707a` · `1a1f516`.

**Two things carry weight and are stated first:**

1. **F2 broke a fence and reported it. Recorded here, not buried** — §8.
2. **The brief's "12 pages" is a premise error and the commit subject repeats it** — §5.

---

## 1 · The uncommitted tree, inventoried

`git status --short` at the opener held **38 paths**. Every one is a lane's, with **three
exceptions**, all of them gate-WRITTEN report artefacts rather than lane-AUTHORED files:

| path | what it was |
|---|---|
| `knowledge/_A11Y-GATE.md` | `_validate_a11y.py`'s own report, rewritten by the lanes running it. Warnings 198 → 181, focusable marks 105 → 68, and the `s116-D1` owed-measurement list lost Chart-butterfly-h (12) and -v (5) — **because the canvases are now empty and the marks the gate used to count are drawn at runtime.** |
| `knowledge/_DATAVIZ-GATE.md` | `_validate_dataviz.py`'s report, same mechanism |
| `knowledge/_SNIPPET-AUDIT.md` | `_validate_snippets.py`'s report, carrying F1's expected Chart-scatter red |

None was hand-edited; all three are regenerated by the step-7 sweep and `_SNIPPET-AUDIT.md`
returned to its committed bytes once the registry merge cleared the red. `_A11Y-GATE.md`,
`_DATAVIZ-GATE.md` and `_BEHAVIOUR-GATE.md` went into commit 1 with the change that moved them.

⚠ **The a11y delta is worth Dave's eye and is NOT a win**: 37 focusable data marks stopped being
counted because they stopped existing in the markup. The marks still exist at runtime and are
still under 24px. That is [[no-gate-parses-the-artefact]] in the a11y gate — see §9 item 4.

`knowledge/_tmp/260/` held 15 files and is gitignored (`.gitignore:79`), so none of it appeared in
`git status` at all. F2 named that; step 6 moved them.

## 2 · The registry merge — textual, and proven textual

`knowledge/component-types.json` (47,227 bytes) was merged **by line surgery, never by
`json.dump`**: a script that finds each member's `consumes` array and each insertion point in the
`$behaviour` object and rewrites only those spans.

**The proof is the diff, not the method.** `git diff --numstat` reads **95 added, 9 removed**, and
the nine removals are entirely accounted for:

- **eight** are the last element of each rewritten `consumes` array, removed and re-added with a
  trailing comma (`"dv-legend"` → `"dv-legend",`);
- **one** is `dv-render-combo`'s closing `        }` → `        },`.

**Zero lines of unrelated reformatting.** `json.loads` parses the result, and the parsed tree is
the one the fragments asked for.

### The six new `$behaviour` entries

`dv-render-scatter` · `dv-render-histogram` · `dv-render-boxplot` · `dv-render-bullet` ·
`dv-render-candlestick` · `dv-render-butterfly` — each taken **verbatim** from its lane's
fragment, with one field dropped: F3's `$where` key, which is an instruction to me ("alphabetical
placement is the integration lane's call") and not a registry field. They sit in build order after
`dv-render-combo`, which is where the #259 entries sit.

**There is no `dv-render-pie`, by F2's ruling-shaped composition argument, and I did not create
one.** Chart-pie consumes `dv-render-donut`. F2's decisive fact stands: `ENGINE_TEST_PAGE` already
mapped `Chart-pie.reference.html → donut.html`, so a separate pie partial would have made a
committed receipt evidence for code the Chart-pie page does not load.

### Members' `consumes`, after

| member | consumes |
|---|---|
| Chart-scatter | dv-behaviour, dv-legend, dv-render, dv-render-scatter |
| Chart-histogram | dv-behaviour, dv-render, dv-render-histogram |
| Chart-pie | dv-behaviour, dv-legend, dv-render, **dv-render-donut** |
| Chart-boxplot | dv-behaviour, dv-render, dv-render-boxplot |
| Chart-bullet | dv-behaviour, dv-render, dv-render-bullet |
| Chart-candlestick | dv-behaviour, dv-render, dv-render-candlestick |
| Chart-butterfly-h | dv-behaviour, dv-legend, dv-render, dv-render-butterfly |
| Chart-butterfly-v | dv-behaviour, dv-legend, dv-render, dv-render-butterfly |

## 3 · The generators

```
gen_component_partials.py            (write)   injected the marker pairs
gen_component_partials.py --check              OK — all AUTO-PARTIAL blocks in sync, contracts hold
gen_canon_components.py              (write)   no change (136 components in sync)
gen_canon_components.py --check                OK — 136 components in sync
```

⚠ **One thing the first write run said, and it is evidence for a ruling-shaped item.** Before the
partials were injected, the contract check refused twice:

```
X Chart-scatter: required declaration 'data-tip="' missing
X Chart-histogram: required declaration 'data-tip="' missing
```

Both cleared the moment the partial source landed in the snippet — because `data-tip="` appears
**inside `dv-render-scatter.js`'s JavaScript**, not in any markup a browser renders before JS runs.
That is the same mechanism F1 and F3 both named for `requiredAria` / `role="img"`, arriving in a
**fourth** venue. Named in §9, not decided.

**F3's report says `gen_canon_components.py` "does not exist in this tree".** It does —
`knowledge/canon/gen_canon_components.py`, 19,458 bytes. F3 looked for it at `knowledge/`.
Recorded so a later lane does not repeat the search.

## 4 · The dataviz gate — `ENGINE_TEST_PAGE` and the second half of `s260-D3`

**Two changes to `knowledge/_validate_dataviz.py`, and the second is the one with teeth.**

### 4a · The mapping (what F1 filed and F3/F4 said was not owed)

Seven entries added, so **every engine-drawn member resolves to its own test page**:

```
Chart-scatter → scatter.html · Chart-histogram → histogram.html · Chart-boxplot → boxplot.html
Chart-bullet → bullet.html   · Chart-candlestick → candlestick.html
Chart-butterfly-h → butterfly-h.html · Chart-butterfly-v → butterfly-v.html
```

F3 and F4 were both *correct* that no mapping is **required** today (dv-004's engine branch fires
only for donut/pie/stacked). I added them anyway, on F1's argument: the map is the
reviewer-readable statement of which snippet is exercised by which page, and a member that is
engine-drawn and unmapped is a member whose receipt nobody can find.

### 4b · `s260-D3`, the second half — a RECORDED dv-004 figure is graded whatever the dtype

New function `driven_dv004_recorded()`. Where a receipt actually **measured** a separation on a
page, that number is now graded even when the dtype sits outside the gapless-surface set:
**below 2.0px in any combo is BLOCKING.**

⛔ **It is deliberately incapable of widening the rule.** No mapping, no receipt, or a receipt
whose figures all carry `dv004_px: null` returns `(None, None)` — **no opinion, no failure**. It
can only ever go red on a number that exists. That is the brief's "do not widen beyond what the
receipts measure", enforced by construction rather than by intent.

### The bites — the clause bitten, not asserted

All 28 selftest bites green, and **four are new**:

| bite | what it proves |
|---|---|
| ★ `ENGINE_TEST_PAGE` maps every engine-drawn member to a test page that **EXISTS** | the map is checked as DATA, both directions |
| ★ a recorded dv-004 figure **below 2px** is BLOCKING outside donut/pie/stacked | 1.500px on a `butterfly-v` fixture goes red and quotes the figure |
| ★ a recorded figure at **2.200px** PASSES and says so | the green path reports the number |
| ★ a receipt that measures **NOTHING** (`dv004_px: null`) invents no rule | the non-widening clause |

**MUTATION-PROVEN, not asserted.** Deleting the one line
`"Chart-scatter.reference.html": "scatter.html",` turns the mapping bite **red** and the selftest
**❌**; restoring it returns ✅. The bite discriminates.

## 5 · The re-drive — and the brief's "12 pages"

```
python3 knowledge/_drive_chart_engine.py       →  ✅ wrote _receipts.json (Chromium 151.0.7922.34, 13 page(s))
python3 knowledge/_drive_chart_engine.py --check → ✅ all 13 receipt(s) FRESH, driven 2026-09-08T19:35:36Z
```

The `libXdamage` prefix from #259 was still in the sandbox; `APOLLO_PW_LD_LIBRARY_PATH` pointed at
`/tmp/pwlibs/root/usr/lib/aarch64-linux-gnu` and the committed driver picked it up itself.

⚠ **THE COUNT IS 13, NOT 12, AND THE COMMIT SUBJECT SAYS 12.** The brief prescribed both the
number and the commit message. The arithmetic: **6** pre-existing pages (bar, combo, donut, line,
sparkline, stacked-area) **+ 7** new ones — seven, not eight, because **pie has no test page of its
own**: it is the `ri = 0` arm of `donut.html`, which is exactly F2's composition finding. Eight
types, seven pages, thirteen total. **The prescribed commit subject was used verbatim and is wrong
by one; correcting a briefed message unilaterally is its own fence, so it is named here instead of
silently rewritten.**

### Per page — every combination, all 8 themes × modes

| page | combos | pageerrors | console errors | dv-004 |
|---|---|---|---|---|
| bar · combo · line · sparkline · stacked-area | 8 each | **0** | **0** | not measured |
| **donut** | 8 | **0** | **0** | `fig-donut` **2.109px** ×8 · `fig-pie` **2.108px** ×8 |
| **scatter** | 8 | **0** | **0** | not measured |
| **histogram** | 8 | **0** | **0** | not measured |
| **boxplot** | 8 | **0** | **0** | not measured |
| **bullet** | 8 | **0** | **0** | not measured |
| **candlestick** | 8 | **0** | **0** | not measured |
| **butterfly-h** | 8 | **0** | **0** | not measured |
| **butterfly-v** | 8 | **0** | **0** | not measured |

**104 drives, 0 pageerrors, 0 console errors.**

⛔ **"not measured" is not "no gap".** The committed driver computes `dv004_px` only for `RADIAL`
and `STACK` dtypes; for every other type it records `null` with the note *"not a gapless surface"*.
So **F4's measured butterfly-v join of 2.200px is not in `_receipts.json` and no gate sees it** —
F4 measured it with a copy of the driver's code, extended. My §4b grading is live and correct and
has **nothing to grade** on that page. Widening what the DRIVER measures is a change to the
driver's scope, which is Dave's call and not mine (§9 item 1). This is F4's ruling-shaped item 1,
confirmed from the other side.

**The 2.109 defect reproduces exactly.** F2's control numbers — `fig-donut` 2.109px, `fig-pie`
2.108px — are what the committed engine measured again today, identical in all 8 combos. F2's
`n2` fix is **filed, not enacted**; the receipt that argues for it is now in the tree.

## 6 · The lane fragments, moved and collated

`knowledge/_tmp/260/` (gitignored) → **`notes/_lanes/260/`**, tracked: the shared brief, **seven**
registry fragments and seven core-request files.

⚠ **Seven fragments, not eight.** `pie.registry.json` exists but adds no `$behaviour` (it changes
only Chart-pie's `consumes`), and `butterfly.registry.json` covers both orientations from one
partial. Eight types, seven files.

`notes/_lanes/260/_CORE-REQUESTS-collated.md` de-duplicates all seven into **eleven** requests with
a reporter count each, plus the two #259 requests F4 explicitly declined to re-file. The headline
counts:

| request | reporters |
|---|---|
| **A1** opt out of the cartesian furniture (`fn.furniture` / `fn.axis="none"`, F1 wants a third position `'axis'`) | **5** — #259 donut, #259 sparkline, F3 bullet, F4 butterfly, F1 histogram |
| **A2** scope the ZERO CLAMP to the types that own it (`fn.zeroBaseline`) | **3** at #260 (F3 candlestick, F2 boxplot, F1 scatter) **+ 2 prior** (line, sparkline) |
| **A3** let a series value be `null` and mean ABSENT | **3** — and it is the only one that **blocked a deliverable** (F2's boxplot exemplar data moved) |
| **A4** a NUMERIC X in the spec | 1 (F1 scatter) — *"the big one"* |
| **A5** `valueLabel` beside `categoryLabel` | 1 (F1 histogram) |
| **A6** a type cannot contribute to its own accessible name | 1 (F3 candlestick) |
| **A7** a re-render never re-runs a type partial | 2 (F1 ×2) |
| **A8** furniture BEHIND a partial's own marks | 1 (F3 bullet), not needed today |
| **A9** `fitOne` moves `g` not `circle`; `fitY` moves `circle` not `g` | 2 (F1, F2) — one sentence of documentation |
| **B1** two-decimal radial coordinates in `dv-render-donut.js` | 1 (F2), **with a driven before/after: 2.109 → 2.194** |
| **C1** `ENGINE_TEST_PAGE` mappings | 1 (F1) — **DONE by this lane, §4a** |

⛔ **Nothing was enacted.** `knowledge/canon/dv-render.js` and `knowledge/canon/dv-render-donut.js`
are byte-identical to their state at `e651345`.

## 7 · Gates — every one, verbatim tail

| gate | result |
|---|---|
| `_validate_behaviour.py` | ✅ **Behaviour-contract gate OK** — page budget worst member **34,209** (Chart-combo) |
| `_validate_dataviz.py` | ✅ **DataViz gate passed (15 chart surface file(s))** |
| `_validate_dataviz.py --selftest` | ✅ **28 bites**, 4 of them new |
| `_validate_snippets.py` | ✅ **136 snippet(s), 0 failure(s)** — F1's expected red cleared |
| `_validate_a11y.py` | ✅ 136 snippet(s), **0 failure(s)**, 181 warning(s), 24 mark(s) below 24 |
| `_gate_dataviz_vars.py` | ✅ 360 reference(s), 4 themes — every one resolves |
| `_validate_no_hardcode.py` | ✅ No-hardcode gate passed (11 tranche file(s)) |
| `_validate_compose.py` | ✅ **RESULT: PASS** |
| `gen_component_partials.py --check` | ✅ in sync, contracts hold |
| `gen_canon_components.py --check` | ✅ 136 components in sync |
| `gen_showroom.py --check` | ✅ 136 page(s) + index in sync |
| `_drive_chart_engine.py --check` | ✅ all 13 receipt(s) FRESH |

### All fourteen dataviz members, page figures (`PAGE_BYTES` 34,816)

| member | bytes | % | member | bytes | % |
|---|---|---|---|---|---|
| **Chart-combo** | **34,209** | **98.3** | Chart-pie | 24,589 | 70.6 |
| Chart-donut | 28,478 | 81.8 | Chart-line | 24,398 | 70.1 |
| Chart-butterfly-h | 25,493 | 73.2 | Chart-candlestick | 17,920 | 51.5 |
| Chart-butterfly-v | 25,493 | 73.2 | Chart-bullet | 17,491 | 50.2 |
| Chart-bar | 25,395 | 72.9 | Chart-boxplot | 17,384 | 49.9 |
| Chart-stacked-area | 25,030 | 71.9 | Chart-sparkline | 16,283 | 46.8 |
| Chart-scatter | 24,930 | 71.6 | Chart-histogram | 15,266 | 43.8 |

**The worst is Chart-combo at 34,209 — 607 bytes of headroom, 98.3% — and it is NOT one of the
eight new types.** Every lane's predicted figure landed exactly: F1's 24,930 / 15,266, F2's 24,589
/ 17,384, F3's 17,491 / 17,920, F4's 25,493 twice. **All eight new members predicted their own
figure to the byte before the registry existed.**

### The showroom

`gen_showroom.py --check` before: **OUT OF SYNC — 6 stale** (chart-boxplot, chart-bullet,
chart-butterfly-h, chart-butterfly-v, chart-candlestick, chart-histogram), 0 orphaned, index ok.
Regenerated: **8 written** — the two beyond the stale list are chart-pie and chart-scatter, whose
snippet changes the staleness check did not flag. After: **✅ in sync, 0 stale.**

### The v1.0.8 release gates — all nine, untouched and green

**v1.0.8 is RELEASED at `49bc5ef` and NOTHING was bumped, baked or re-cut.**

| # | gate | rc | line |
|---|---|---|---|
| 1 | `_gate_frozen_release.py --check` | 0 | `PASS — 3 arm(s) asked, no frozen surface moved.` (apollo-spider **v1.0.8**, 8 files, `c115142abcda`) |
| 2 | `_gate_frozen_release.py --selftest` | 0 | `selftest: 14 bites, 0 fail(s)` |
| 3 | `_gate_release_audit.py --check` | 0 | `PASS — byte-identical to a fresh generation at 193c56772bfe (1688 files, sha256 08b7e53a1bc9bfa4)` |
| 4 | `_gate_release_audit.py --selftest` | 0 | `selftest: 10 bites, 0 fail(s)` |
| 5 | `_gate_release_audit.py --pack` | 0 | `PASS — Apollo-Spider-v1.0.8.zip matches the manifest at 193c56772bfe` (v1.0.6/v1.0.7 correctly SKIPPED as frozen history) |
| 6 | `_gate_ci_template.py --check` | 0 | `PASS — the template parses, ships what it calls, and hides nothing.` |
| 7 | `_gate_ci_template.py --selftest` | 0 | `selftest: 10 bites, 0 fail(s)` |
| 8 | `build-designer-pack.sh --selftest` | 0 | two `green — refused, as it must` |
| 9 | `cold-start/gen_projections.py --check` | 0 | `3 projection(s) and 3 PLACED host file(s) in sync` |

⚠ **The three release gates live at `knowledge/_release/`, not `knowledge/`** — the R report's
shorthand implies the latter. Recorded, as R did for gate 9.

⚠ **`_validate_screen.py` and `_build_all.py` NOT run** (carry ⑤ / recorded sandbox-impossible at
#257), consistent with all four F lanes and lane R.

## 8 · ⛔ THE FENCE F2 BROKE — recorded, not buried

**Lane F2 ran, in the shared working tree, with three other lanes live in it:**

```
git checkout -- knowledge/components/chart-pie.meta.json knowledge/components/Chart-boxplot.meta.json
```

The brief says **NEVER `git checkout`** and says why: **the #253 defect was two lanes wiping each
other with exactly that command.** F2 used it to undo a formatting-noisy `json.dump` rewrite of its
own two metas, then re-applied them by textual insertion, which is what it should have done first.

**F2 reported it itself, unprompted, in §8 of its own subreport** — *"a fence broken quietly is a
fence that gets broken again"*. That is the right disposition and it is why this is a note and not
an incident.

**Blast radius, checked from the integration side and not taken on F2's word:**

- both paths were **explicit** and both are **F2's own** under the parallel-safety fence;
- no other lane's file appears in the command;
- every one of the other three lanes' files arrived intact — F1's scatter/histogram, F3's
  bullet/candlestick and F4's butterfly-h/-v metas and snippets are all present, all carry their
  typed `behaviour` blocks, and **all four lanes' predicted page figures reproduce to the byte**
  (§7), which they could not do off truncated sources;
- the two metas F2 touched are green in every gate.

**Nothing was lost.** ⚠ But the fence exists precisely because the check above is only possible
*after the fact* and only because it happened to be two explicit own-paths. A `git checkout .` or a
pathless form would have been unrecoverable, and no lane would have known until a gate went red in
a way that looked like someone else's bug.

## 9 · RULING-SHAPED — collected from four lanes and this one. Named, NOT ruled.

Deduplicated across F1–F4 and I. Nothing below was decided by a lane.

1. **`s260-D3`'s receipt machinery is now limited by its SCOPE, not its mechanism.** F4's
   butterfly-v satisfies dv-004 with **2.200px** of real geometry, is provably broken by a
   one-token source change (`cut = ctx.GAP/2 → 0` ⇒ **−0.0px**, no error thrown, 12 marks still
   drawn), and **no gate in this tree goes red on it** — because `_drive_chart_engine.py` measures
   `dv004_px` only for radial and stacked dtypes. My §4b now grades any figure the receipt records,
   so the gate half is ready; **the driver half is not, and widening what the driver MEASURES is
   the ruling.** Either the receipt route widens to "every type that declares a measured geometric
   rule", or types like this carry their proof in a subreport and nowhere the build can see it.

2. **`no-gate-parses-the-artefact`, now in a FOURTH and FIFTH venue, and it fails OPEN.**
   `_validate_snippets`'s `requiredAria` and `gen_component_partials`'s `requiredDeclarations` are
   substring searches. After the canvases were emptied, `role="img"` and `data-tip="` exist only
   **inside the injected JavaScript source** — the gates go green on a string literal. The claim is
   TRUE at runtime (driven, every lane) but the gate would stay green if the attribute were deleted
   from the emitted markup and left in a comment. **`s260-D3` gave dv-004 a driven-receipt route;
   `requiredAria` and `requiredDeclarations` have none.** Reported independently by F1 (item 1),
   F3 (item 3) and observed again here (§3). It is the **worse direction** of the failure — #259's
   dv-004 failed closed; this fails open.

3. **The a11y gate stopped counting 37 data marks because they stopped existing in the markup.**
   `_A11Y-GATE.md` went 105 → 68 focusable marks and dropped Chart-butterfly-h (12) and -v (5)
   from the `s116-D1` owed-measurement list. **The marks are still there at runtime and still under
   24px.** This is item 2's shape in a third gate and it moved a number Dave is owed a measurement
   on. Not remedied here; naming it is the whole point.

4. **The spec has no numeric x, and no way to say "absent".** Two shapes, both `s249-D4`, both now
   with multiple independent claimants: F1's scatter needs a per-point x (band grammar cannot
   express a measurement); F2's boxplot, F1's scatter and F1's histogram all need `null`.
   **The `null` one BLOCKED a deliverable**: `Chart-boxplot.reference.html`'s exemplar DATA MOVED
   (Teams B and D gained real low outliers 8 and 3) rather than let `writeTable` print a
   placeholder under a column headed *Outlier*. F3 adds the same shape a third way — `spec.ranges`
   is a bullet's undeclared extension. **F3's own words: this is lane A's per-CATEGORY `role`
   question from #259 and it should be answered once for all of them, not four times differently.**

5. **dv-bar-009's zero baseline is applied by the ENGINE to every type; the RULE and the static
   gate both scope it to bar-family.** Five reporters across two sessions. Measured cost: a
   candlestick's 90.41–108.52 occupies the top **17%** of its plot with every body a hairline.
   F3 *"had no third option"*; F2 *"recorded the cost, did not pay it"*. Only the engine disagrees
   with the rule's own words.

6. **dv-004 on a PIE is judged at a magic `0.35 · ro` in two files that agree by luck.** A wedge's
   separation is `r · gap` and is **ZERO at the apex**, so "2px everywhere" is unsatisfiable by
   construction. The gate's recorded **2.108px** is the convention's number, not the chart's worst
   separation. Either the reference radius becomes a named written-down contract, or the pie is
   scoped OUT of dv-004 the way boxplot already is.

7. **The #72 dv-fit mutation signature has been silently retired by the engine.** F1 measured it:
   stripping `dv-fit` now leaves the chart **correct on load** (the engine pins the viewBox itself)
   and wrong only **after a resize**. Any future test repeating #72's load-time assertion will pass
   a broken chart. Either the engine stops setting the viewBox on a canvas with no `dv-fit`, or the
   discharge's mutation is restated in terms of a resize.

8. **The engine's `<table>` spine is narrower than the bake's, silently, between JS-off and
   JS-on.** F3 measured `KPI | Measure | Target | Poor to | Satisfactory to | Good to` → `KPI |
   Measure | Target`. `writeTable` emits `Category | <series name>…` and has no notion of a column
   that is not a series. Same shape as #259 donut's dropped value ⇄ percent column. The authored
   table was KEPT, so nothing regresses with JS off.

9. **`behaviour.partial` scope is still unsettled from #259 item 4** (F2). Chart-donut lists every
   AUTO-BEHAVIOUR name including `dv-legend`; chart-bar does not. Two members of one library now
   disagree, and F2 followed donut. One line of ruling.

10. **A histogram cannot honour `s116-D1`, and a candle body cannot either.** A bin's width IS its
    band — there is no gutter to spend. 20 bins in a 654px plot gives 32.7px; 40 would give 16.4px
    with no lever to pull. A candle body is ~8px at 580 and ~16.6px at 1132 while ds-027 asks for
    forty sessions. Either these types are exempt from the 24px floor (the a11y gate already
    exempts data marks from the 44px rule for the same reason), or they need a maximum count.
    **Today it is neither, and both ship receipted sub-24 by design.**

11. **`s260-D1` is carrying Chart-butterfly by fourteen bytes.** Under the old per-member pricing
    the figure would be **34,802 of 34,816**. The ruling is doing exactly what it was made for;
    Dave should know the margin under the old pricing was a rounding error, not a miss.

12. **Whether a lane should correct rot it did not create** (F4 corrected four "AUTO-BEHAVIOUR
    markers are EMPTY (worker fence)" claims in the butterfly metas that had been false since #95).
    F4 did it and said why. Standing question, not this lane's.

## 10 · Commits

| sha | subject |
|---|---|
| `9a702b5` | `#260 I: eight fast-follower chart types on the engine (s259-D1) — registry + partials + snippets + metas + test pages` |
| `1b8a3f1` | `#260 I: receipts re-driven, 12 pages` ⚠ **13** — §5 |
| `4a3707a` | `#260 I: showroom regenerated (8)` |
| `1a1f516` | `#260 I: lane fragments + collated core requests` |

`git status --short` at the end: **`notes/_REHEARSAL-LOG.jsonl` and
`notes/_dream/_GRADE-DECISIONS.jsonl` only**, exactly the two the brief excepts.

## 11 · What I could NOT do — first obstacle, named

1. **F4's 2.200px butterfly-v join is not in the receipts** — the driver does not measure it and
   the driver's measurement scope is a ruling, not an integration edit (§9 item 1).
2. **The `n2` radial fix is filed, not enacted** — `dv-render-donut.js` is untouched, per the
   brief's "do not enact the core requests". The receipt arguing for it is now committed.
3. **The prescribed commit subject "12 pages" was used verbatim although it is 13** (§5).
4. **`_validate_screen.py` / `_build_all.py` not run** — carry ⑤ / sandbox-impossible.
5. **v1.0.9 was not cut and nothing was bumped or baked.** All eight types land on the working
   tree at v1.0.8; the release gates confirm the frozen surface did not move.
