# #267 lane R — THE RELEASE of Apollo — Spider v1.0.10

**Model: Opus. Ruling `s267-D2` (`318e46d`): "v1.0.10 IS RATIFIED", Dave's word "okay inscribe"
to the go/no-go v2 page's one question. Steps 2–5 of lane P2's post-ratification list, each its
own commit. NOT PUSHED — the push is Dave's word.**

## THE FOUR COMMITS, IN ORDER

| # | sha | what |
|---|---|---|
| 2 | **`7210dcb`** | v1.0.10 RATIFIED (s267-D2) — `RATIFY_IDS` keyed, manifest RATIFIED at `a9218cc`, frozen literal `v1.0.9 → v1.0.10` |
| — | **`515ef2d`** | the session's five untracked lane artefacts, so the bake had the clean tree `require_clean` demands |
| 3 | **`de8ed57`** | v1.0.10 RELEASED at `a9218cc` — the zip |
| 5 | **`6f948c2`** | frozen ledger re-seeded — apollo-spider 10 zips, `1455d4390ec4` |

## 2. THE KEY (`7210dcb`)

`RATIFY_IDS["v1.0.10"] = "s267-D2"`, the #257 / #260 / #262 shape, with the "HAD NO ROW through
TWO PROPOSED cuts" comment block above it (625be64 HELD by s267-D1, a9218cc PROPOSED until the
word). A second comment block by `VERSION` now reads **★ v1.0.10 IS RATIFIED — s267-D2**, the
v1.0.9 block's shape exactly. Status **read back off the module** before the manifest was touched,
never typed (s237-D9):

    VERSION v1.0.10 · ratify_id s267-D2
    RATIFIED — s267-D2 names v1.0.10 in the store; s219-D4(2) satisfied by the store, not by prose

`--manifest --commit a9218cc --full-stage /var/tmp/full10r` (a `git archive` of the commit,
458 MB, populated first — `--full-stage` does not create its directory, lane P2's sandbox note):
**1714 files, 45,967,883 bytes**, unchanged from P2's PROPOSED re-cut. **Exactly one line moved**
in `_pack_manifest.json` — the `status` line. The manifest's OWN sha256 moves with it:
`4cd9229adb2df432` (PROPOSED) → **`5d37abc0a13ed486`** (RATIFIED). The file set does not, which is
what s223-D8 cares about.

`_gate_frozen_release.py:134` apollo-spider literal `v1.0.9 → v1.0.10`, with a one-line #267
comment in the running series, in this same commit — the fourth version home.

## 3. THE BAKE (`de8ed57`)

    bash apollo-spider/build-designer-pack.sh --release --commit a9218cc
      219 finding(s). ADVISORY — exiting 0.
      d76ffd507319ae53ccc96f4ea4090baa0369e2761985231954b7250eca5dc488
        apollo-spider/dist/Apollo-Spider-v1.0.10.zip   size 21M

- path **`apollo-spider/dist/Apollo-Spider-v1.0.10.zip`**
- sha256 **`d76ffd507319ae53ccc96f4ea4090baa0369e2761985231954b7250eca5dc488`**
- size **21,174,371 bytes**

★ That is lane P2's twice-baked dry-run fingerprint (`/var/tmp/dry10c` + `/var/tmp/dry10d`,
`cmp` rc 0) **to the byte**. The brief's stop condition was NOT reached. Two things fall out of
the match, measured rather than asserted: the release path and the rehearsal path build the same
artefact, and the ratification — which is all `7210dcb` changed — packs byte-identically, which
is s223-D8's claim driven rather than quoted.

`reviews/RELEASE-SPIDER-2026-08-26-v1.html` was rewritten by the bake (the script says so itself)
and the overlay re-injected with `knowledge/_review/_make_review.py`; the pair is in this commit.

## 4. VERIFY

    bash apollo-spider/build-designer-pack.sh --check apollo-spider/dist/Apollo-Spider-v1.0.10.zip --commit a9218cc
    CHECK GREEN — matches the manifest at a9218cc06e8f          rc 0

Gates 4 and 5 — the two lane P2 could only run RED pre-bake — re-run first, then the full ten.

## THE TEN RELEASE GATES, POST-BAKE AND POST-SEED — 10/10 GREEN

| # | gate | rc | line |
|---|---|---|---|
| 1 | `_release/_gate_frozen_release.py` | 0 | `PASS — 3 arm(s) asked, no frozen surface moved.` |
| 2 | `_release/_gate_frozen_release.py --selftest` | 0 | `selftest: 14 bites, 0 fail(s)` |
| 3 | `_release/_gate_release_audit.py --check` | 0 | `PASS — byte-identical to a fresh generation at a9218cc06e8f (1714 files, sha256 5d37abc0a13ed486)` |
| 4 | `_release/_gate_release_audit.py --selftest` | 0 | `selftest: 10 bites, 0 fail(s)` |
| 5 | `_release/_gate_release_audit.py --pack` | 0 | `PASS — Apollo-Spider-v1.0.10.zip matches the manifest at a9218cc06e8f` (v1.0.0–v1.0.9 `SKIPPED — FROZEN HISTORY`) |
| 6 | `_release/_gate_ci_template.py --check` | 0 | `PASS — the template parses, ships what it calls, and hides nothing.` |
| 7 | `_release/_gate_ci_template.py --selftest` | 0 | `selftest: 10 bites, 0 fail(s)` |
| 8 | `apollo-spider/build-designer-pack.sh --selftest` | 0 | `selftest: 233 bites, 0 fail(s)` |
| 9 | `apollo-spider/cold-start/gen_projections.py --check` | 0 | `OK — 3 projection(s) and 3 PLACED host file(s) in sync` |
| 10 | `_release/_gate_pack_docs.py --stage /var/tmp/stage10r/Apollo-Spider-v1.0.10` | 0 | `219 finding(s). ADVISORY` |

Gate 10 was run against the **unzipped release zip**, not a stage the bake happened to leave
behind. It REFUSES (rc 2) without `--stage` — it will not guess a tree.

## 5. THE FROZEN LEDGER (`6f948c2`)

    python3 knowledge/_release/_gate_frozen_release.py --seed
      seeded knowledge/_release/_frozen-releases.json at de8ed5775d50
        designer-skills-v1   version v1          6 file(s)  b83d048483b7
        designer-skills-v2   version v2        849 file(s)  e1d8019b97cc
        apollo-spider        version v1.0.10    10 file(s)  1455d4390ec4

Content moved AND version moved — the pairing the laundering arm requires. Run **after** the
release commit, per #262 FINDING 1: `--seed` hashes the tree at HEAD, so seeding one step early
records the prior digest and file count in silence. Ten zips: v1.0.5 was never baked and still is
not, so the count skips it.

## ⚠ RED / NOT DONE (declared)

- **NOT PUSHED.** `git log @{u}..HEAD` = **13** commits local. The push is Dave's word.
- **`_rulings.json` untouched** this lane — the ruling was already inscribed at `318e46d`.
  No `_build_all.py`, no `gen_kg_edges.py`, no `git stash`.
- **`515ef2d` is a commit the brief did not name.** `--release` runs `require_clean`, and
  `git status --porcelain` counts UNTRACKED files; five were sitting in the tree from earlier
  #267 lanes (three briefs, lane C6's subreport, `reviews/COLDRUN-267-2026-09-10-v1.html`).
  They were committed as written, unedited, in their own commit before the bake. This does not
  touch what the zip carries — `--release` stages from the COMMIT (`git archive a9218cc`), and
  the fingerprint match proves it did not.
- **Gate 8's no-ratification refusal arm reads `SKIPPED — the manifest is already ratified`.**
  Unproven in this pass BY CONSTRUCTION, not by omission: lanes P and P2 drove it red before the
  key landed. Gate 8's bite count is **233**, not #262's 221 — lane P2's seven `chartpages/` bites
  and #263–#266's additions.
- **Gate 4's output prints an `❌` line** (`the manifest reads version 'v1.0.10' and NO zip in
  dist/ carries it`) — that is a BITE's captured fixture text inside the selftest, not this
  tree's verdict; `selftest: 10 bites, 0 fail(s)`, rc 0, and gate 5 says the zip matches. Named
  because it reads like a red on a fast scroll.
- **Gate 4 also prints the ADVISORY drift line**: the manifest was generated at `a9218cc`, HEAD is
  6 commits later at bake time. Advisory by design; the cut is the commit Dave ratified.
- `knowledge/_validate_screen.py` NOT RUN at this seat. `knowledge/_tests/test_gates.py` SKIPPED
  (copytrees ≈5 GB, ENOSPC in this sandbox) — the standing skips from #260 / #262.
- No browser render was attempted; nothing in this lane produced a page for the eye.

## ⚠ SANDBOX

No `.git/index.lock` was present at any point; nothing was moved to
`knowledge/_tmp/_orphan-locks/`. All commits by plain
`git -c user.name=… -c user.email=… commit`, never `_git_commit.sh` (#262 FINDING 3):
`require_clean` passed on the first attempt.

## TOKENS

**ESTIMATED, not measured** — a lane has no `message.usage` reader and `_checkin.py` is the
conductor's instrument [[measuring-tool-must-not-guess]]. Best estimate **~55K FILL at wrap** on
~20 tool calls; the two 458 MB `git archive`/`unzip` runs and the ~90 s bake were tailed or
grepped, never read whole, and the pack-docs gate's 219 findings were truncated at the tail. If
the conductor needs a number it must come from the gauge, not from this line.

## PATHS

- `apollo-spider/dist/Apollo-Spider-v1.0.10.zip` — the release
- `knowledge/_release/_gen_pack_manifest.py` — `:120` the ★ RATIFIED block, `:864` the v1.0.10
  `RATIFY_IDS` row and its comment block
- `knowledge/_release/_gate_frozen_release.py` — `:134` the #267 comment, `:136` the literal
- `knowledge/_release/_pack_manifest.json` (`status`) · `knowledge/_release/_frozen-releases.json`
- `reviews/RELEASE-SPIDER-2026-08-26-v1.html` (+ `.REVIEW.html`) — rewritten by the bake
- stages: `/var/tmp/full10r` (the manifest's full stage), `/var/tmp/stage10r` (the unzipped
  release, for gate 10) — both throwaway
