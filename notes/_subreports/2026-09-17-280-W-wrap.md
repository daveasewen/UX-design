# `#280`-`W` — the delegated capture ritual for #280 → #281

provenance: 280 · 2026-09-17
status: observed

session: `#280` · opened 2026-09-16, ritual and commit 2026-09-17
window: conductor **Fable 5.1** (transcript `d56e1452-71cd-4df7-983f-94811e86de38.jsonl`), this seat a delegated **OPUS 5** wrap sub
sub index: `wrap`
brief: `notes/_lanes/280/W/WRAP-BRIEF.md` — the figures it DECLARES are re-verified here rather than restated, and the two that disagree are published as two readings
tokens: `UNMEASURED — this seat cannot read its own message.usage; the FILL figures below are the CONDUCTOR'S window, measured first-hand against his transcript, and are a different object`

## VERDICT

**DONE — every step of `knowledge/_RUNBOOK-capture-ritual.md` ran: 1 · 1b · 2 · 2c · 2c(i) · 2d · 2e · 2f · 2g · 3 · 4 · 4b · 4c · 4d · 5 · 5b.** Both rulings of the session were verified **by span against `git show --numstat`** rather than restated (605 → 607, `19  0` and `18  0`, zero deletions). ⚠ **ONE STEP RAN OUT OF ORDER, DECLARED AND DELIBERATE: step 3 (cloud memory) was run FIRST**, so that every surface written afterwards could state a MEASURED result instead of a prediction — #279 wrote its banner while the runbook's seat-limit claim was unmeasured and had to correct two files in a post-wrap addendum. ✅ **THE STORE WAS WRITTEN FROM THIS SEAT — the second consecutive wrap to measure the runbook's "structural" seat limit FALSE**, and the runbook line is still NOT edited, because amending a ratified step is Dave's. ⛔ **SEVEN blocking gate fails stood at the OPEN and SIX at the CLOSE, and none was repaired: every one is another session's append-only testimony and a wrap may not repair them** — the brief declares SIX, which is the closing state; the seventh (the boot-drift STEP CHANGE) cleared because this wrap's own 2f roll re-derived the band. Both readings are published.

COUNTS: findings `9` · ruling-shaped `4` · UNPROVEN `2`

## What was done

**Step 1 — `_LIVE-STATE.md`.** `Last refreshed` re-stamped **from `date`** (2026-09-17, Thu — never from belief); #279's segment demoted to `Previous:` and #276's `Previous:` chain segment MOVED verbatim (**6,717 chars**) to `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-17 #280, span reconstruction asserted **in the writing process before the write** (`notes/_lanes/280/W/lastrefreshed280.py`, `lastrefreshed280-header.py`). An 18-line ⏱ LATEST DELTA #280 written. `python3 knowledge/_validate_standing_instructions.py` **PASS** (28 standing docs). Sibling fed: `knowledge/_REVIEW-SIGNOFF.md` takes **two new rows** (the six-cell contact sheet; the orphan census with his ten radios) and the #279 15-base row is amended **by addition with its receipt** — its *"NOT INSCRIBED"* clause was made false by this session's own lanes.

**Step 1b — narrative dossier.** `_DECISION-HISTORY/2026-09-17-280-the-matrix-and-the-front-door.md`, provenance + status fields, both-way links to the spine entry and the ledger.

**Steps 2 / 2c / 2d / 2f — every move through `knowledge/_gm_move.py`,** from session-owned, uniquely-named ops files asserted with `os.path.exists` + a size floor **in the writing process** before the mover read them (`notes/_lanes/280/W/run_ops.py`), **dry-run first**, receipts read back against the ops meant to be run: `ops-280-rolls-*.json` (**1,214 B / 5 ops**) · `ops-280-lsdelta-*.json` (**10,985 B / 2 ops**) · `ops-280-banner-*.json` (**4,030 B / 2 ops**) · `ops-280-stratum-*.json` (**9,885 B / 1 op**) · plus four single-op transactions for the GENERATED line, the next-chat title, the `s214-D6` line and the `size:` stamp. `_roll_state.py` at the close: **2c OK (banners 2/2) · 2d OK (deltas 3/3) · 2f OK (strata 1, log #279)**.

**2c(i) — `_CARRIES.md` § `residual → #281`,** written by `notes/_lanes/280/W/carry281.py`: **656 age brackets bumped** (8 of them `NEW — 0` → `1`), **TWO** surgical edits — both STRIKES carrying their `s183-D1`/`s188-D2` receipts — and **SIX** new items. Probe **507 → 515**; the banner's PROBE command agrees at 515.

**2e — NO-OP, 0 lines.** DO-FIRST minted no notice, tombstone or lifecycle tag and nothing was retro-tagged.

**2g — `_build_memento_index.py` run LAST,** after every GM/LS edit was final.

**Step 3 — WRITTEN, NOT A SEAT LIMIT.** `wrap-280-the-matrix-and-the-front-door.md` (**7,607 B**) and the index line newest-first (`index.md` **6,882 B of the 49,152 B cap**, 14.0% used). Hook filed FIRST regardless at `notes/_lanes/280/WRAP-MEMORY-HOOK.md`, with the result recorded at its foot. ⛔ **No placement is owed at the #281 opener.**

**Step 4 — the rulings.** Both verified by span, neither inscribed here (the lanes inscribed their own). **4b —** both title lines GENERATED by `python3 knowledge/_gen_titles.py --session 280`, receipt at `knowledge/_gen_titles_receipt.json`; the forward title written into `GOOD-MORNING.md` through the mover. **4c —** `_gate_scratch_hygiene.py --clean`. **4d —** `_render_rulings.py` re-rendered and `--check` reads **FRESH**.

## Findings

1. **The two rulings are real, and both are insertions-only.** `git show --numstat` reads `19  0` on `53a91bd` (`s280-D1`) and `18  0` on `9e04796` (`s280-D2`); `json.load` reads **607** rulings against #279's inscribed 605. Verified at this seat, not copied from a lane report.

2. **FILL measured, and the window NAMED.** `knowledge/_checkin.py` at this seat reads **191,294 real over 28 turns**, boot **75,740**, against the conductor's own transcript `d56e1452-71cd-4df7-983f-94811e86de38.jsonl`, whose FIRST user record is Dave's own opener (*"Good Morning!"*). The brief DECLARES **185,127 real / 25 turns** ⇒ **delta 6,167**, and the subtraction is legal because both terms read the same window — the boot figure agrees to the token. ✅ **The 200,000 working line was NOT breached, the first wrap in three**; ⛔ the RULED **180,000 stop line was passed by 11,294**, inside the ≤220,000 tolerance, DECLARED.

3. **BOOT 75,740 real, n=1** vs the `s129-D1` floor **70,794** — **+4,946, NOT a re-base.** `BOOT_FIRSTTURN_TK` untouched, no error bar widened, n=1 published as n=1. It is also above `BOOT_CEILING_TK` 70,000, the standing shrink-only fail, which is another session's to cut.

4. **A `subs` line is written this wrap and #279's was not, and the difference is the rule working.** `s214-D5` makes an ABSENT line legal and forbids defaulting an unknown into a number; it does not forbid writing a **measurement**. The brief declares eight per-lane figures MEASURED by the harness (LY 221,093 · IN 205,442 · LS 197,571 · IN2 209,660 · LM 247,259 · EX3 284,250 · OC 204,942 · EX4 263,571) and they sum to **1,833,788 (n=8)** — QUOTA, never FILL.

5. **The tree was NOT in sync at the ritual's open** — `git log @{u}..HEAD` reads **17**: all seventeen #280 lane commits stood unpushed, a full day of build work local. (#279 opened at 17 as well and pushed; #278 opened at 0.)

6. **THE INHERITED FAIL COUNT MOVED DURING THE RITUAL — SEVEN AT THE OPEN, SIX AT THE CLOSE — AND THE CAUSE IS THE RITUAL'S OWN 2f ROLL.** At the open this seat measured seven: the boot-drift **ceiling breach**, the boot-drift **step change**, and **five** boot double-counts (#243, #264, #272, #273, #274). After 2f appended #279's stratum to `notes/_GAUGE-LOG.md`, the boot band **re-derived over a new window** and the step-change arm went quiet, leaving six. ⚠ **The wrap brief's DECLARED SIX is the CLOSING state and is correct for it; neither reading is retro-fitted and neither is averaged.** Every one is another session's append-only testimony and a wrap is forbidden to repair them, so the commit took the `DECLARED not-a-wrap` path with `SESSION_N=280` for the **sixth** wrap running. ⇒ **a wrap's own ritual step can move a gate's DERIVED figure, which is a property of derived bands and not a defect** — worth knowing before a future wrap reads a count at its open and quotes it at its close.

7. **`knowledge/_state.py --check` read CLEAN at this ritual's open** (exit 0, zero ⛔) — unlike #279's, which inherited a broken `W-278wr` row. **Nothing was repaired here, because nothing was broken, and no repair was invented to match the precedent.** The wrap's own row `W-280wr` went in by textual span, `git diff --numstat` `20  0`.

8. **The standing reds reproduce exactly as carried.** `_validate_roles_resolve.py` **FAIL(6)**, all six `data-grid` `with`-slugs · `_validate_lane_ownership.py --selftest` **2/3** · `showroom/` holds **138** top-level `.html` here against an inscribed *"108 stale"* — **the figure is NOT rewritten**; it is a standing gap of Dave's and both readings are published side by side, as at #278/#279. `/sessions` **99%, 158,032 KB free** (`df -k`); the VM scratch mount is a different object at **54.9%**.

9. **`_CHAIN.md` came in at 8,945 tape — BELOW the `s214-D6` ~10–12K band for a third consecutive wrap — and what that means is still not ruled.** The clause commands SHORTER and never decide-what-to-drop, so a chain below band is either a success or an under-record and the clause does not say which. ⚠ **The ★ LATEST banner was written to the cap deliberately: 1,158 tape / 10 substantive lines against 1,200 / 10 — 42 tape and ZERO lines of headroom** — and reaching it required compressing the seventeen lane commit shas **to a pointer** at the ⏱ LATEST DELTA. That choice is named on the banner itself so it reads as a pointer and not as a dropped receipt.

## RULING-SHAPED QUESTIONS

1. **The orphan census export.** Ten radios, his, not received; the census becomes the orphan plan on export. ⛔ Its largest set is a **switch left off** whose flip puts a **FAMILY NODE** on the stage — a new kind of thing, and therefore his word, not a lane's. **#281's first move.** Not taken here.

2. **The eye-check of the six matrix cells.** `s280-D1` is ruled and the six cells are law until he looks; the conductor **expects** Floors or Orbits to go, which is an expectation and not a finding. ⛔ No lane may thin a ruled matrix on it.

3. **The wrap-sub memory seat limit, now n=2.** `knowledge/_RUNBOOK-capture-ritual.md` step 3 says a delegated wrap sub *"cannot reach the store at all"* and calls it STRUCTURAL; #279's and #280's wrap subs both wrote it. The line is **not edited by either wrap**. Is the limit real, and if it is not, does the hook file remain as the belt-and-braces receipt or retire?

4. **The `#243` form, SIXTH wrap running.** A genuine wrap must pass `SESSION_N` on the script's `DECLARED not-a-wrap` path to get past gate refusals that are ALL another session's append-only testimony and that this wrap is forbidden to repair. That a wrap must declare itself not-a-wrap is ruling-shaped and is Dave's; recorded here rather than smoothed.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** the **`replay`** term of the `wrap-handover` line — what reading this report back costs the conductor is not observable from inside the sub. Written `unobservable (conductor-side, post-wrap)`; the conductor may append the measured figure at the #281 opener.
- **UNPROVEN:** Dave's *"how hot are we"* is DECLARED by the conductor in the wrap brief and appears in no file this seat can read — it is recorded in `notes/_lanes/280/DAVE-RULINGS-2026-09-17.md` **with that provenance named**, and nothing is built on it.
- **CLAIMED, not re-driven:** the eight lane reports' internal figures quoted in the banner, the delta and the handoff are copied from `notes/_subreports/2026-09-1{6,7}-280-*.md`, which are dated history under `s192-D1`; the COMMITS behind them were re-verified here with `git log` / `git show --numstat`, the reports' own measurements were not re-driven.

## Evidence

`notes/_lanes/280/W/` — the ritual's working set, all session-owned and uniquely named: `run_ops.py` (the ops-file assertion harness, the #166 lesson made mechanical) · `banner280.py` (the ★ LATEST banner held in ONE place and measured before it was written) · `delta280.md` · `stratum280.md` · `carry281.py` · `signoff280.py` · `state280.py` · `lastrefreshed280.py` + `lastrefreshed280-header.py` (both span-with-reconstruction) · `ops280_rolls.py` / `ops280_lsdelta.py` / `ops280_banner.py` / `ops280_stratum.py` and the `ops-280-*.json` files they wrote · `WRAP-REPORT.md`.

REPLAY-THESE: `notes/_lanes/280/DAVE-RULINGS-2026-09-17.md` (~1.3K tk — his words verbatim) · `notes/_lanes/280/orphan-census/ORPHANS-2026-09-17.html` (the ten radios, unanswered) · `_HANDOFF-131-the-matrix-and-the-front-door.md` (~4K tk)
