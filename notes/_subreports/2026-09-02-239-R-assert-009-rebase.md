# `#239`-`R` — Re-base `ASSERT-009` 136 → 137 BY ADDITION

session: `#239` · 2026-09-02
window: chore lane R
sub index: R
brief: `notes/_briefs/2026-09-02-239-R-assert-009-rebase-brief.md`
tokens: `UNMEASURED — no message.usage at a sub's seat`

## VERDICT

DONE. Both counts re-measured in-sandbox and matched the brief's FACT line exactly (no STOP
triggered). `ASSERT-009`'s predicate, claim text and README pointer re-based 136→137 / 140→141;
provenance appended (never rewritten) in the established six-times-used form. The three other
`asserted_in` homes were grepped and classified — all hits found were HISTORY (dated strata); zero
LIVE hits existed to repoint, so nothing was touched in `_LIVE-STATE.md`, `notes/_MEMENTO-DECISIONS.md`
or the `_DECISION-HISTORY` file. Gate re-run: **`✅ assertion gate passed — 8 claim(s) still true.`**
with `ASSERT-009` `[✓] count=137 (want eq 137)`.

COUNTS: findings `6` · ruling-shaped `2` · UNPROVEN `1`

## What was done

1. **Re-measured, in-sandbox** (repo `/sessions/keen-serene-johnson/mnt/UX-design/`):
   `ls knowledge/components/*.meta.json | wc -l` → **137**. `ls knowledge/components | wc -l` → **141**.
   Non-meta four confirmed unchanged: `meta.schema.json`, `_ACCESSIBILITY-CONFORMANCE.md`,
   `_nodes-context.json`, `_nodes-pattern.json`. Matches the brief's FACT line exactly — no STOP.
   137th meta confirmed present: `knowledge/components/template-dashboard-bento.meta.json`.
2. **`knowledge/_assertions.json`** (`ASSERT-009` entry, lines ~183–203):
   - Field the gate reads confirmed by reading `_validate_assertions.py:152-157` (`p_glob_count`
     reads `predicate["n"]` against `op` default `"eq"`) — not guessed.
   - `predicate.n`: `136` → `137` (line 191).
   - `claim` text: "136 files" → "137 files", "140 entries" → "141 entries" (line 185). Left the
     pre-existing "the 92 includes the EXAMPLE template meta" clause untouched — the brief named
     only the two substrings to update, and this stale "92" reference predates this rebase.
   - `provenance` (line 202): appended, not rewritten, the brief's exact sentence: `Re-based #239
     2026-09-02 (BY ADDITION; the growth is the bento dashboard template —
     template-dashboard-bento.meta.json @ e7cf3db, after #231; Dave: 'Okay rebase, do both'): 136
     -> 137 measured in-sandbox (ls knowledge/components/*.meta.json | wc -l = 137). Dir entries
     140 -> 141, same four non-meta files. README updated same commit.`
3. **`knowledge/README.md:13`**: `136 metas` → `137 metas`; the "count registered as ASSERT-009,
   re-tested not repeated" pointer phrase left intact, unchanged.
4. **Other `asserted_in` homes** — grepped and classified, see table below. Zero LIVE hits in any
   of the three ⇒ zero edits made to any of them (brief's explicit fallback).
5. **Gate**: `python3 knowledge/_validate_assertions.py` → tail line `✅ assertion gate passed — 8
   claim(s) still true.`, with `[✓] ASSERT-009 environment  count=137 (want eq 137) in root=repo`.
   `--selftest` also run: `selftest OK — 5 verbs bite-tested, 0 native check(s), 8 assertion(s)
   well-formed, 3 mutation(s) killed.`
6. **Parse check**: `python3 -c "import json;json.load(open('knowledge/_assertions.json'))"` →
   exits clean, no output, file still valid JSON.

## Findings

1. `_validate_assertions.py:152-157` (`p_glob_count`) reads `predicate.n` and `predicate.op`
   (default `"eq"`) — confirms the correct field to move was `predicate.n`, not `claim` prose alone.
2. `_LIVE-STATE.md` has exactly one hit for the bare figure in a meta-count context: line 66,
   `"...20 migrations 20/20, 136/136 live metas green..."` — this sits inside the section headed
   `## ⏱ LATEST DELTA — 2026-09-02 (#238...)` (line 60), a dated stratum quoting sub-report B's
   own findings verbatim. Classified **HISTORY** (a dated ledger entry citing what a report said on
   that day), not a present-tense statement of the corpus's current size. All other `136` hits in
   the file (lines 55, 386, 388, 391, 403, 419, 422, 431, 440, 449, 465, 521, 528, 531) are `#136`
   session-number references, unrelated to the meta count. Zero LIVE hits ⇒ **file untouched.**
3. `notes/_MEMENTO-DECISIONS.md` carries the corpus figure at only two places, lines 5815 and 5823
   — both frozen at **92** (the #205/#207-era figure) and line 5823 explicitly says "kept verbatim
   as history." No `136`, `140` or `141` appears anywhere in the file tied to this count (the one
   bare `136` hit at line 5965 is `#136 first build lane`, a session reference). This file was
   never live-updated across the four subsequent #209/#210 rebases either — it has been
   HISTORY-only since #207/208. Zero LIVE hits ⇒ **file untouched.**
4. `_DECISION-HISTORY/2026-08-08-131-the-legacy-rag-fills-and-the-design-kg-nothing-checks.md`
   carries the figure at lines 92 and 109 (76 and 92 respectively) — the same two lines the #208
   provenance entry names. This file is dated history by directory construction (ADR-0017); no
   `136`/`137` appears in it at all. **HISTORY by construction ⇒ file untouched**, per brief
   instruction (no grep-driven judgement call needed here — the directory itself settles it).
5. `knowledge/_assertions.json` re-parses clean after all three edits (predicate, claim, provenance
   append) — `python3 -c "import json;json.load(...)"` exits 0 with no error.
6. **Rule tension noted, not triggered**: `notes/_briefs/2026-09-02-238-COMMON-lane-rules.md:7`
   NEVER-lists writes to `_LIVE-STATE.md` ("`_rulings.json`, `_state.json`, `_CARRIES.md`,
   `GOOD-MORNING.md`, `_LIVE-STATE.md`, `_CHAIN.md`, memory"), and the #239 COMMON file says that
   NEVER list "stands unchanged." This brief's own step 4 lists `_LIVE-STATE.md` as a home to
   possibly edit (repoint a LIVE hit). This pass the conflict never materialized — zero LIVE hits
   meant zero writes — but the two documents disagree in principle and a future rebase could hit a
   live figure there. See RULING-SHAPED Q2.

## RULING-SHAPED QUESTIONS

1. `notes/_MEMENTO-DECISIONS.md` is named in `ASSERT-009`'s `asserted_in` but has carried zero live
   figures since #207/208 (frozen at 92, six rebases behind) — option (a) trim it from
   `asserted_in` now that it's confirmed permanently HISTORY-only; option (b) leave it, since
   ADR-0017/s192-D1 already treat it as frozen-by-design and the list documents provenance, not
   just live pointers. Not trimmed here per the brief ("do NOT trim it yourself").
2. A future `ASSERT-009` rebase could find a genuine LIVE hit inside `_LIVE-STATE.md` (this pass
   found none). The #238 COMMON NEVER-list forbids any write to that file; this brief's step 4
   pattern assumes repointing LIVE hits there is normal. Recommend whoever briefs the next rebase
   resolve this ahead of time — either add `_LIVE-STATE.md` to a rebase-specific carve-out of the
   NEVER list, or drop it from `asserted_in`'s edit path and route any live-hit repoint through the
   conductor instead of a sub.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** whether CI run #479 (the next run after this edit lands) reproduces the same
  `[✓] ASSERT-009 count=137` result — this pass only proves the in-sandbox seat's repo state agrees
  with the brief; CI's checkout is a different environment and was not re-driven here (no `_build_all.py`,
  no git, per NEVER list). Price to prove: one CI run after the conductor commits and pushes.

## Evidence

No evidence files: every claim above quotes its probe (command + output, or file + line) inline.

REPLAY-THESE: `knowledge/_assertions.json` ASSERT-009 entry (~750 tk) · `knowledge/README.md:13` (~40 tk)
