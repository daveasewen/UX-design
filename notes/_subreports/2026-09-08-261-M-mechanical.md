# #261 M — six mechanical carries from the #260 ⏱ delta

Lane M. Six items, one commit each, own files only. Five enacted; **one STOPPED as ruling-shaped**.

| # | owed text (≤25 words) | what I did | receipt | commit |
|---|---|---|---|---|
| 1 | `--full-stage` is an optional flag that silently decides the ship set; nothing in the output says which shape ran | probe artefact records `differential_arm` ARMED/DISARMED + the stage path; `arm_line()` prints it on `--probe` AND beside the file count/sha256 on `--manifest`; third state `UNRECORDED` for pre-M1 probes. Whether the flag should be optional is UNTOUCHED | `--selftest` 216 → **221 bites, 0 fail**; two real probes at HEAD `--only _validate_dataviz.py` print DISARMED and ARMED and the JSON carries both | `68daf1f` |
| 2 | `_git_commit.sh` appends to `_REHEARSAL-LOG.jsonl` after staging, so the ruled commit path can never leave the clean tree `--release` requires | REPRODUCED, then the narrowest of the three named fixes: hash every `INSTRUMENTATION_PATHS` entry at script start, auto-stage only the ones **this run** changed. `s137-D1`'s exclusion and `require_clean` both UNTOUCHED; `INSTRUMENT_AUTOSTAGE=0` restores the old behaviour | reproduction: commit `68daf1f` staged ONE unrelated file and moved the log md5 `d17ca683` → `16c70ab4`, leaving it dirty. After the fix the log is **absent from the post-commit dirty list** for the first time, and has been on every commit since | `425318f` |
| 3a | `_validate_screen.py`'s basename clobber of tracked `_screen-gate/` rows, still unrepaired, four observed clobbers | destination is now a function of the SOURCE PATH: outside-repo subjects go to `$TMPDIR/apollo-screen-gate/` and say so; an in-repo row whose recorded `- source:` differs REFUSES (`SCREEN_GATE_REBIND=1` is the legal move) | REPRODUCED: a `/var/tmp` copy with one rogue hex rewrote the tracked row `PASS → FAIL` (md5 `a3332287` → `a91e56b6`). Same run on the fixed gate: tracked row **byte-unchanged**, row written to `$TMPDIR`, and the in-repo second-source arm prints the refusal | `70eebbf` |
| 3b | `knowledge/_tests/test_gates.py` copytrees ≈5 GB and cannot run in this sandbox at all | `fresh_copy` copies what **git tracks** — equal to a fresh CI checkout. The ⛔ `IGNORE`-the-assets question #221 priced as a handoff is NOT taken: no tracked file is withheld. Loud fallback to `copytree` when git cannot answer | working `knowledge/` **5.3 GB** vs tracked copy **108 MB / 2028 files / 2.2 s**, on a box with **3.1 GB free**. `python3 knowledge/_tests/test_gates.py` → **31 test(s), 0 failure(s), exit 0** — first completion in this sandbox | `70eebbf` |
| 4 | the 2dp donut emission: 2.109 → 2.194 measured, filed, unapplied; pie judged at a magic 0.35·ro | local `n2` in `dv-render-donut.js` for path geometry only (`pt`, `arc`); `data-*` keep `n1` (tip/label contract). Same swap in the two injected copies so `gen_component_partials --check` is byte-exact. ⛔ GAP stays 2.2 and the pie constant is untouched — both ⬛ Dave's | re-driven, Chromium 151.0.7922.34, 4 themes × 2 modes: **dv-004 `fig-donut` 2.194px** (worst of 8) — F2's predicted value exactly. `_drive_chart_engine.py --check`: **27/27 FRESH**. `_validate_dataviz.py --check` PASS · snippets 137/0 · behaviour page budget PASS | `06538b4` |
| 5 | `_validate_behaviour.py` rewrites its own tracked `_BEHAVIOUR-GATE.md`, so a drift is absorbed instead of going red | the write is now `--write`; the default run compares and REDS with the remedy named. The report carries no timestamp, so it is a pure function of the sources. `_build_all.py`'s no-arg call becomes the CHECKING form | two default runs `ab4fbdb2 → ab4fbdb2 → ab4fbdb2` **NO DIFF**; two `--write` runs **NO DIFF**; drift probe → exit 1, `LEDGER DRIFT`, ledger md5 **unchanged after the red** (not absorbed); `--write` restores it byte-identical; `--selftest` green | `4a9a7dd` |
| 6 | the boot arm | ⛔ **STOPPED — ruling-shaped, and the carry says so in its own words** | see below | none |

## Item 6 — why it stopped

The carry's ⑪ reads: *"THE BOOT-CEILING ARM STILL REFUSES ON ONE BREACH, AND IT IS UNCHANGED
[NEW — 0, DAVE'S]"*, and: *"`s240-D2`/`s241-D1` make it shrink-only; raising it is Dave's word
alone and is never a price a wrap may pay to unblock itself."* #257's delta puts it plainer still:
*"What to do with the arm now that two readings under it stand is HIS."*

The arm is not broken. `knowledge/_capture_gate.py:3881` and `BOOT_CEILING_FROM_SESSION = 241`
encode `s240-D2`'s regime boundary and `s241-D1`'s 70,000 literal correctly; the four consecutive
under-ceiling readings (68,837 · 68,826 · 68,994 · 69,086) sit under it and #255's 70,127 still
stands over it, so the refusal is the clause working. **Every available action is a change to a
ruled literal or to the clause that reads it** — move the literal, narrow the window, or accept
the fourteenth consecutive `--wrap` refusal. There is no mechanical half to take, so lane M took
none.

## Declared, so it is not discovered later

- **Rows written before M3 carry no `- source:`** and are unprotected until their next run, when
  they gain one. Only `canon-gallery.canon.md` was re-run and committed here.
- **`git checkout` was used ONCE**, on `knowledge/_screen-gate/canon-gallery.canon.md`, to undo
  damage this lane's own reproduction had just done to it. It hung on the sandbox `.git` unlink
  guard; the restore that worked was `git show HEAD:<path> > <path>`. No other lane's file was
  touched by it.
- **Three `_ACK` declarations were used on every commit**: the doc-row gate's two unrowed
  subreports belong to lanes D3 and R, and the stale showroom pages belong to another live #261
  lane's canon edits. Both would have meant staging another lane's file (#253).
- **Gate reports outside the fence went dirty** (`_A11Y-GATE.md`, `_DATAVIZ-GATE.md`,
  `_COVERAGE-GATE.md` and others) — `test_gates.py`'s control case runs real gates in the repo.
  Absorbed per the lane brief, not committed by M.
- **`_validate_dataviz.py --check` did not catch M4's staleness**; the driven arm
  (`driven_dv004`) did, and reported `STALE — dv-render-donut.js has changed`. The `--check`
  surface list and the driven arm answer from different evidence — same shape as D3's obstacle 1.
