# `#238`-`M` — render-runbook EIGHTH stratum (per-seat generation, driven) + the `W-355` declared form (fixture-proven)

session: `#238` · 2026-09-02
window: `lane M (Fable sub, quota-burn day)`
sub index: `M`
brief: `notes/_briefs/2026-09-02-238-M-runbook-and-W355-brief.md`
tokens: `UNMEASURED — no message.usage at a sub's seat`. Shape: ~48 tool calls; whole-file reads of
`_RUNBOOK-render-verify.md` (520 lines) and `_git_commit.sh` (three slices, ~700 lines), harness
slices (~350 lines), `_session.py` slices (~190 lines); three PNG crops read by eye (112 KB, 76 KB,
77 KB); 14 sandbox probe/render/fixture calls, the longest 5.5 s.

## VERDICT

Both regions DONE, with one premise corrected. **PART 1:** the runbook already carried a SEVENTH
stratum (#233), so the brief's "seventh" is filed as the **EIGHTH STRATUM** — it names the two causes
the brief asked for (the #219 hollow lib dir; lane T's finding 11, the stored `fonts.conf` carrying
`determined-affectionate-euler`) **plus a third found today** (the 229 font FARM is symlinks with
absolute foreign-seat targets — `ls` shows 10, `-e` resolves 0 here), and homes the fix as machinery:
`knowledge/_render/seat_env.sh`, sourced in the render call, generates the seat-bound half under
`/dev/shm` from `pwd` and ASSERTS every path before any launch. Driven from THIS seat
(`wonderful-adoring-euler`, from `pwd`): `_REVIEW-tensions-schema-2026-09-02-v1.html` at **1440 light
(5.5 s) and 390 dark (3.8 s)**, canvas probe with controls (never `fonts.check`), both PNGs read by
eye, tree asserted clean after each. **PART 2:** `W-355`'s fix (1) chosen — `SESSION_N + SESSION_ACK`
together as a DECLARED form — implemented in `_git_commit.sh` as two marked blocks BY ADDITION (the
existing chain untouched), proven on fixtures under `/dev/shm/w355/`: the CONTROL (script with the
blocks stripped) reproduces the deadlock BOTH ways; the declared form lands from the wrapped seat AND
the next seat; `SESSION_N` alone still refuses a real overtake; T3's wrap assertion still refuses
through the declared form. Nine permanent harness arms added to `_test_git_commit.py` (2 mutation
controls); the whole harness is **42/42 green in 5.4 s**. `W-355` is NOT closed — its `closes_when`
needs a real post-wrap handoff committed through the live script; nothing here touches the store.

**stratum added 1 · renders 2 (ok 2) · W-355 fix arms 9 (behave 9/9) + fixture drive 7/7 · files
changed: 5 repo files + 1 report + 10 assets (listed under Evidence).**

COUNTS: findings 18 · ruling-shaped 2 · UNPROVEN 4

## What was done

**PART 1 — `knowledge/_RUNBOOK-render-verify.md`.** Read the strata newest-first (EIGHTH now sits above
the SEVENTH; the SEVENTH, SIXTH, FIFTH and every earlier stratum untouched). Probed the mount:
`outputs/_render-env-{229,230,231}/fonts.conf` all seat-bound; `_render-env-229` is the only durable
env (pylibs = playwright 1.62.0, `pw-browsers/chromium_headless_shell-1234`, `chromelibs` = libXdamage
×2). Wrote `knowledge/_render/seat_env.sh` (sourced; derives the seat from the repo mount path, asserts
lib dir non-hollow → `ldd` clean → playwright imports, writes the #138 conf body + a fresh farm + fccache
under `$TMPDIR/render-<seat>/`, asserts 10/10 farm links resolve and fontconfig sees ≥10 HSBC faces AND
the system set, exports the six vars + `RENDER_SHELL`/`RENDER_SEAT`; prints `SEAT_ENV: OK …` or
`SEAT_ENV: FAIL <which>` and returns 1). Mutation-drove it (hollow lib dir → FAIL "fifth-stratum shape";
absent env dir → FAIL). Wrote the driver `render_probe_238.py` (assets) and drove the two renders in
two calls, each sourcing the generator in the same call. Read the crops. Added the EIGHTH stratum
(`_RUNBOOK-render-verify.md` lines 14–66) by addition, naming all three causes, the per-call `/dev/shm`
measurement, the recipe as run, the probe numbers, the provenance and the UNPROVEN.

**PART 2 — `W-355`.** Quoted the row (`python3 knowledge/_state.py`, `_state.json` item `W-355`):
title *"POST-WRAP ADDENDUM DEADLOCK — a _HANDOFF-<next>.md cannot be committed from the wrapped seat OR
from the next seat while it exists (R3 CHAIN OVERTAKEN fires on the file being committed; SESSION_ACK is
only read when SESSION_N is absent, and T3 then refuses a non-wrap commit with no SESSION_N)"*;
`closes_when`: *"a post-wrap _HANDOFF-<n+1>.md is committed through _git_commit.sh from EITHER seat
without a raw git commit, proven by the commit landing and its subject read back; the fix is either
SESSION_N + SESSION_ACK together as a legal DECLARED form, or ritual step 5b addenda stop being
_HANDOFF-<next> files"*. Read `_git_commit.sh` :337–359 (the seam), :481–580 (T3), `_session.py`
:118–235 (witnesses, R1–R4, the `acknowledge` path), ritual step 5b (`_RUNBOOK-capture-ritual.md`
:649–656), `_RUNBOOK-git-commit.md` (no session-seam section existed). **Picked fix (1)** — see finding
11–12 for why. Implemented: `_git_commit.sh` block (1/2) at :347–375 (before the unchanged chain) and
block (2/2) at :389–398 (after it). `bash -n` OK; `--selftest` 14 bites OK. Added `w355_strip`,
`build_fixture_w355`, 7 arms + 2 mutation controls to `knowledge/_test_git_commit.py` (real `_session.py`
+ `_helpgate.py` in the fixture; post-wrap #238 disk state; `_HANDOFF-239-x.md` untracked and named for
staging). Drove `--only w355` (9/9) and the whole harness (42/42) with `TMPDIR=/dev/shm/w355`. Drove the
brief's literal fixture (`w355_fixture_drive.sh`, 7 arms, log filed) — cleanup verified: 0 dirs, 0 git
processes, 0 locks. Added a "session seam" section to `_RUNBOOK-git-commit.md` by addition (the declared
form's home). Ran `_validate_help_gate.py` (OK, 226 scripts), `_validate_wiring.py` (one PRE-EXISTING
red, finding 17), `_session.py --selftest` (all bites green).

## Findings

1. **The brief's premise was a stratum behind.** `_RUNBOOK-render-verify.md` :14 already read `★★
   **SEVENTH STRATUM 2026-09-01 (#233)…` before this lane touched it. Filed as EIGHTH; the ordinal is the
   only correction. *(Probe: `grep -n "SEVENTH STRATUM" knowledge/_RUNBOOK-render-verify.md` pre-edit.)*
2. **All three stored confs are seat-bound, and to three different seats.** `_render-env-229/fonts.conf`
   → `determined-affectionate-euler`; `-230` → `gracious-inspiring-wozniak`; `-231` → `gifted-kind-allen`;
   **0/3 resolve at `wonderful-adoring-euler`**. *(Probe: `grep -n "sessions/" outputs/_render-env-*/fonts.conf`.)*
3. **A THIRD cause, new today: the 229 font farm dangles.** Every link is absolute into the foreign seat
   (`… -> /sessions/determined-affectionate-euler/mnt/UX-design/knowledge/assets/fonts/_desktop/TTF/…`);
   measured `links=10 resolve_here=0`. A corrected conf pointed at that farm would expose 0 HSBC faces
   and fall back silently. *(Probe: `for f in *.ttf; do [ -e "$f" ] && ok=$((ok+1)); done` in the farm.)*
4. **`/dev/shm` is per-call.** A marker written in one bash call was absent in the next
   (`DEV_SHM_PERSISTS=no`). So the seat-bound half must be generated in the render call itself, and
   the W-355 fixture had to build-and-drive in one call. *(Probe: two consecutive calls, `cat marker`.)*
5. **`fc-list : family` DEDUPES identical family strings** — 10 files, 6 lines — and the generator's first
   assertion failed on its own instrument: `SEAT_ENV: FAIL fontconfig sees 6 HSBC faces (want >=10)`.
   Counting full patterns (`fc-list | grep -c`) gives 10, as #138 measured. Fixed in `seat_env.sh` and
   banked in the stratum. *(Probe: the FAIL line, then `fc-list | grep -c HSBC_MtUnivers_Latin` → 10.)*
6. **The canvas probe discriminates, both renders, to the #138 table's two decimals.**
   `HSBC_MtUnivers_Latin` **346.88** · `"Univers Next HSBC"` **346.88** · `"Univers Next for HSBC"`
   **346.88** · `DejaVu Sans` 375.39 · nonexistent 301.07 (40px `Handgloves 12345`).
   `document.fonts.check('16px HSBC_MtUnivers_Latin')` returned `true` and was recorded, not used.
   *(Probe: `probe-light-1440.json` / `probe-dark-390.json` → `checks.fonts.widths`.)*
7. **The review page's body face is `Liberation Sans` — lane T's "which face" UNPROVEN closes at this
   seat.** The page stack `"Helvetica Neue",Helvetica,Arial,sans-serif` measures 333.59 = `Liberation
   Sans` (unique width-match); `"Helvetica Neue"` alone = 301.07 (the default fallback). ⚠ `fc-match
   Helvetica` says `Nimbus Sans` (331.48) but Chromium paints Liberation Sans — Chromium's own family
   equivalence wins over fontconfig's, so **an `fc-match` probe is not a probe of what Chromium paints**.
   *(Probe: `checks.page_face` in both JSONs; `fc-match` under the generated conf.)*
8. **390 dark layout holds: `scrollWidth 390 = clientWidth 390`**, and the four 640 px tables each sit in
   a 350 px `overflow-x:auto` wrapper (`wrap_scrollW 640 / wrap_clientW 350`). Lane T's finding-12 fix
   (`min-width:0` on grid children, 3 hits) is doing its job. **Observation for Dave's eye:** in the
   headless shot there is no scroll affordance — the clipped third column is the only hint the table
   scrolls (`crop-dark-390-table.png`). *(Probe: playwright evaluate over `table`/parent rects.)*
9. **The page's light ink computes to `rgb(0, 0, 0)`** (`getComputedStyle(body).color`, 1440 light).
   `s176-D1` (Dave, #176, FIRM): *"'BLACKEST INK' RESOLVES PER THEME, AND IT IS THE BLACKEST INK THAT
   ISN'T PURE BLACK, TO AVOID HALATION"* — governs *"the meaning of 'ink' … never #000000"*. The page is
   lane T's dated review artefact; surfaced, not swapped ([[feedback-grey-tint-check]]).
   *(Probe: `checks.body.color` in `probe-light-1440.json`.)*
10. **`seat_env.sh` can fail, two ways proven; one by construction.** Hollow lib dir → `SEAT_ENV: FAIL
    lib dir hollow or absent … (fifth-stratum shape)`; absent env dir → `SEAT_ENV: FAIL envdir absent`.
    The farm assertion is guarded by construction (links are made from files that exist) and was NOT
    mutated — declared in the stratum, priced under UNPROVEN.
11. **`W-355`: `_session.py` has ALWAYS accepted the combined form.** `verdict(repo, declared, acknowledge)`
    takes both; its own selftest line 317 runs `verdict(d2, declared=86, acknowledge="wrap owed, running
    the wrap now")` and bites *"a declared gap is MARKED, not silently clean"*. Only the shell's
    `if SESSION_N … elif SESSION_ACK` ever made them exclusive. So fix (1) is WIRING — two shell blocks,
    zero change to `_session.py`. *(Probe: `_session.py` :155, :224–229, :316–319.)*
12. **Why (1) over (2).** Ritual step 5b (`_RUNBOOK-capture-ritual.md` :649–656) prescribes *append to
    the ★ LATEST banner, re-run the wrap gate, commit* — it never names `_HANDOFF-<next>` as the vehicle,
    so option (2) would change no ritual text; and a real `_HANDOFF-<n+1>` written for the next seat is
    exactly what R3 exists to make that seat read (`_session.py` :196–201 — a handoff *"outranks the
    chain"*). Removing the trigger removes the witness; (1) adds a declared form and removes no check
    (the brief's own criterion). `grep -c "_HANDOFF-" knowledge/_RUNBOOK-capture-ritual.md` → 0.
13. **`_RUNBOOK-git-commit.md` had no home for the session seam at all** — `grep -c -i
    "session_n\|session_ack"` → 0 lines (16 "session" hits, none about the seam). `SESSION_N=` lived only
    in wrap briefs, `_LIVE-STATE.md`, `_CHAIN.md`. A "session seam" section is now added by addition.
14. **Proof counts.** Harness `--only w355`: 9/9 (CONTROL reproduces the deadlock both ways on the
    stripped script — R3 with `SESSION_N`, T3 `s130-D3` with `SESSION_ACK` alone; 2 mutation controls
    bite, one of them proving block (2/2) is load-bearing on its own). Whole harness 42/42 in 5.4 s.
    Fixture drive 7/7 (`w355-fixture-drive.log`): subjects read back — `after #238 2026-09-02 — post-wrap
    handoff for #239 (arm 3)` with `_HANDOFF-239-x.md` as the only file; `after #239 2026-09-02 — …
    (arm 5)`; `#238 2026-09-02 — ✅ **W-355 FIXTURE**` on the wrap arm.
15. **The declared gap prints twice** — once from block (1/2), once from the unchanged chain's own
    `SESSION_ACK` branch (its `--acknowledge` re-run is `|| true`). The cost of by-addition; cosmetic;
    RULING-SHAPED 1 offers the one-line tidy.
16. **No Python consumer reads `SESSION_N` from the environment** (`grep -rn "environ.*SESSION"
    knowledge/*.py` → 0). Block (2/2) restores with `export` anyway, because `unset` drops the export
    attribute and the restore must not be the thing that changes that.
17. **`_validate_wiring.py` is red on a PRE-EXISTING orphan**: *"`_validate_receipt.py` exists on disk
    and NOTHING RUNS IT"* — the #235 L1 instrument, not this lane's; none of this lane's edits name a
    gate file (the #228 substring-scan trap was checked for). Declared, not owned.
18. **Rule slip, declared:** this lane's FIRST bash call included `git -C . log -1 --oneline` (read-only,
    no lock, no state change) before the COMMON "no `git` of any kind" had been internalised. Every later
    `git` ran only inside `/dev/shm/w355/` fixtures. Zero effect on the live repo; on the record.

## RULING-SHAPED QUESTIONS

1. **Collapse the double DECLARED print?** Option (a) leave the two blocks as they are (pure addition,
   the chain untouched — safe while lane P's block is landing in the same file); option (b) once lane P
   has landed, a one-line tidy of the chain's `--declare` line to `--declare "$SESSION_N"
   ${SESSION_ACK:+--acknowledge "$SESSION_ACK"}` and retire block (1/2)'s parking. **Recommend (a) now,
   (b) at a quiet seam** — (b) modifies an existing line, which today's coordination rule forbade.
   Conductor's call; the harness arms pin the behaviour either way.
2. **Promote `seat_env.sh` to the runbook's single recipe?** The EIGHTH stratum references it; §6 and
   `render.py` still glob `~/.cache/ms-playwright` and assert with `fonts.check` — a seat following §6
   verbatim gets a false-green font check (#138's finding, still live in canon). Option (a) leave §6 as
   history and let the strata carry the truth; option (b) add a canvas-probe leg to `render.py` by
   addition and point §6 at `seat_env.sh`. **Recommend (b)** — promotion is Dave's
   ([[derivation-governance]]), priced ~2K + one driven render.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** the EIGHTH-stratum recipe on a mount with NO `_render-env-*` at all — the install lines
  (`pip install playwright`, `playwright install chromium-headless-shell`, `apt-get download libxdamage1`)
  were REUSED from `_render-env-229`, not re-run — price: one call, the seventh stratum's four lines.
- **UNPROVEN:** that `seat_env.sh`'s farm assertion CAN fail — guarded by construction, not mutated —
  price: ~1K, a mutation pointing `$ttf` at an empty dir.
- **UNPROVEN:** whether lane T's own render (at `determined-affectionate-euler`'s successor seat) had any
  HSBC face available — their probe measured `Helvetica Neue` vs `sans-serif` only, and the dangling farm
  (finding 3) would have been silent. Moot for their PNGs (the page uses no HSBC face); unprovable now.
- **UNPROVEN, BY DESIGN — `W-355` stays OPEN:** a REAL post-wrap `_HANDOFF-<n+1>.md` committed through
  the LIVE script. Not run (forbidden here, and nothing real to commit). Price: the conductor's next real
  post-wrap addendum, `SESSION_N=<n> SESSION_ACK="<why>" bash knowledge/_git_commit.sh --reconciled
  <fresh msgfile> _HANDOFF-<n+1>-<slug>.md`, subject read back. That is the row's own `closes_when`.
- **CLAIMED:** the SEVENTH stratum's "disk 55%, orphan farms gone" reading — today `df /` = 56%, and no
  `/var/tmp/pylibs*` or `pw-browsers*` exist; consistent, re-read only that far.

## PITFALLS (consequences replayed — Dave #165; owner per row)

1. **`seat_env.sh` defaults to `outputs/_render-env-229` as the durable half.** Prune or rebuild it and
   the generator says `SEAT_ENV: FAIL envdir absent` — the remedy is the SEVENTH stratum's install lines
   into a new `_render-env-<n>` and `source … <that dir>`. Owner: the next renderer; the stratum says so.
2. **Sourcing in one call and rendering in another launches with NO fontconfig** (`/dev/shm` per-call).
   Mitigation: the driver refuses when `RENDER_SHELL` is unset; the stratum says SAME CALL twice.
   Owner: whoever writes the next driver — copy `render_probe_238.py`'s refusal.
3. **The declared form covers EVERY refusal `_session.py` printed (R2/R4 too)** — a wrong `SESSION_N` with
   an ACK passes as declared, printed on the record, exactly as `SESSION_ACK` alone always has. A
   narrower `--acknowledge-r3` would be a `_session.py` change. Owner: the caller's honesty now; Dave's
   if a filter is wanted.
4. **The record is stdout-only** (parity with every `*_ACK` hatch). The block's own echo tells the caller
   to put the reason in the msgfile body; the runbook section repeats it. Owner: the caller.
5. **Lane P's parallel block, if it lands BETWEEN the two W-355 blocks and reads `SESSION_N`, will see it
   UNSET while parked.** Owner: the conductor at reconcile — `grep -n SESSION_N` between the
   `W-355 DECLARED FORM (1/2)` and `(2/2)` markers must show only the original chain.
6. **`w355_strip` keys on the marker text and the first column-0 `fi`.** Reflow the blocks and the
   mutation controls fail LOUD (*"mutation target not found"*) — designed, but it blocks CI until the
   harness is updated. Owner: whoever edits the blocks.
7. **The runbook now has eight strata and two live recipes** (the SEVENTH's manual exports, the
   EIGHTH's generator). Until RULING-SHAPED 2 is answered, a reader can follow either; only the EIGHTH
   asserts before launching. Owner: Dave (promotion).
8. **The wiring gate is red today on someone else's orphan** (finding 17) — a conductor running the
   full gate set will see a failure this lane did not cause and could mis-attribute. Owner: #235's
   instrument owner / the conductor.

## Evidence

`notes/_subreports/assets/2026-09-02-238-M-runbook-and-W355/`:
- `render_probe_238.py` — the driver: canvas probe with controls, theme/layout/reflow asserts, verdict
  printed before cleanup.
- `probe-light-1440.json`, `probe-dark-390.json` — every number in findings 6–9 (seat, viewport, theme,
  layout, body colour/face, all 12 widths, reflow heights, PNG bytes, seconds).
- `review-light-1440.png` (1,668,890 B, 1440×13701), `review-dark-390.png` (1,354,688 B, 390×21892) —
  the two deliverable renders, full page.
- `crop-light-1440-top.png`, `crop-dark-390-top.png`, `crop-dark-390-table.png` — the crops actually READ
  by eye (the smallest crop that carries each verdict).
- `w355_fixture_drive.sh` — the brief's literal fixture (copy of `_git_commit.sh` + real `_session.py` +
  `git init` under `/dev/shm/w355/`, fake `_CHAIN.md`, `_HANDOFF-239-x.md`), 7 arms, self-cleaning.
- `w355-fixture-drive.log` — its run: 7/7 PASS, every R3 / DECLARED / T3 line, subjects read back,
  `leftover dirs: 0 · git processes: 0 · locks: 0`.

Repo files changed (all by addition): `knowledge/_RUNBOOK-render-verify.md` (EIGHTH stratum, :14–68) ·
`knowledge/_render/seat_env.sh` (new) · `knowledge/_git_commit.sh` (blocks at :347–375 and :389–398) ·
`knowledge/_test_git_commit.py` (W-355 section before `ARMS`, 9 entries appended) ·
`knowledge/_RUNBOOK-git-commit.md` (session-seam section at the end).

REPLAY-THESE: `knowledge/_git_commit.sh` :347–398 — the two blocks (~1.2K tk) · `knowledge/_RUNBOOK-render-verify.md` :14–68 — the EIGHTH stratum (~1.4K tk) · `notes/_subreports/assets/2026-09-02-238-M-runbook-and-W355/w355-fixture-drive.log` — the deadlock and its release, verbatim (~1.6K tk) · PITFALLS row 5 — lane P's block vs the parked `SESSION_N` (~0.1K tk) · RULING-SHAPED 2 — `render.py`/§6 still assert with `fonts.check` (~0.2K tk)
