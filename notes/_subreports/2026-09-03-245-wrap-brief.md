# `#245`-`wrap-brief` — the FILED brief for the delegated capture ritual of #245 (the reds, then the tuner; v1.0.6 CUT)

session: `#245` · 2026-09-03
window: conductor window (FABLE), delegated wrap sub (FABLE)
sub index: `wrap-brief`
brief: THIS FILE — written FIRST by the wrap sub, verbatim from the conductor's in-chat brief, before any ritual step ran (closes RULING-SHAPED 4 of `notes/_subreports/2026-09-03-244-wrap.md` for this session: *"require a filed brief for every delegated wrap"* — the #243 and #244 wraps ran from chat-only briefs; this one has a file)
tokens: `n/a — a brief spends nothing; the wrap's own seat is UNMEASURED (a sub cannot read its own message.usage)`

provenance: 245 · 2026-09-03
status: observed

## VERDICT

This is a BRIEF, not a report: it records what the conductor asked the wrap sub to do and what the
wrap sub was forbidden to do, so the fence outlives the chat. The wrap's own report is
`notes/_subreports/2026-09-03-245-wrap.md`. Every figure below is the CONDUCTOR'S DECLARED figure
as briefed; the wrap re-probes each one at its own seat and the report says which held.

COUNTS: findings `0` · ruling-shaped `0` · UNPROVEN `0`

## What was asked (the brief, as delivered)

**Seat.** Delegated wrap sub for Apollo session #245, 2026-09-03 (Thu), evening. Repo (bash)
`/sessions/compassionate-dazzling-tesla/mnt/UX-design`; file tools
`/Users/daviewen/Documents/Claude/Projects/UX-design`. Each bash call independent, dies at ~178 s;
`pip install tiktoken jsonschema --break-system-packages` per call. ⛔ Never run
`knowledge/_build_all.py`; never push; never write memory (the conductor did step 3:
`v106-cut-and-grid-may-not-matter-245`, NON-REPO — declare it as his, 0 bytes at the wrap seat).

**Read first, in this order.** (1) `knowledge/_RUNBOOK-capture-ritual.md` — the ritual is the
contract; follow its steps by number (1 → 2c/2d/2e/2f/2g → 4 → 5), including the DECLARE-LAST size
stamp, the ⏱ LATEST DELTA in `_LIVE-STATE.md`, the ★ LATEST banner in `GOOD-MORNING.md` under the
`s241-D2` S1 cap (≤1,200 tape over ~10 substantive lines — write SHORTER never QUIETER; gauge, skips
and not-done list live in the ⏱ delta), the `#### 2026-09-03 #245` stratum, `_gm_move.py` for
moves, `_gen_chain.py` (regenerates `_CHAIN.md` — it sets the NEXT title line: `Apollo - #246:
<short label>`), `_build_memento_index.py` LAST (2g), `_CARRIES.md` § `## residual → #246`
transformed PROGRAMMATICALLY from `## residual → #245` (never read whole — declare bytes skipped),
`_REVIEW-SIGNOFF.md` rows, `_state.json` rows via `knowledge/_state.py` for the seven lane reports
(L5 RSQ 3 — mint rows for all seven; that is the conductor's call and he takes (a)), the wrap commit
through `bash knowledge/_git_commit.sh --wrap` (its gate must be green; the `s244-D1` discharge form
now exists — the check-in shows 0 STRUCTURAL). (2) This file — WRITE IT FIRST from the prompt (W-row
minted at creation), then the seven lane reports
`2026-09-03-245-{L4-row-height-renders,L3-composition-edge,D-discharge-arm,R-reds,L2-populate,K-rung-ladder,L5-cut}.md`
— read each REPLAY-THESE line and cite every one (the check-in's "filed sub-report uncited" warns
must clear). (3) `_CHAIN.md` (the current contract; it names #245's title `Apollo - #245: the reds,
then the tuner`).

**The day, for the stratum and the delta (every figure to be re-probed at the wrap seat, never
believed).**
- Opener: boot 67,470 real (`_checkin.py`, under the 70,000 ceiling); Dave's panel: All 48% · Fable
  73% · reset 4h58m. Dave: *"lets get 1.0.6 ready for tomorrow, use fable liberally, watch out for
  dependencies"*. Order per `s244-D2`: reds first.
- Six Fable lanes + this wrap, all Fable (no 529 today): L4 206,247 · L3 266,771 · D 151,657 · R
  165,798 · L2 285,560 · K 161,668 · L5 366,398 subagent tokens (QUOTA never FILL). Conductor FILL at
  the last check-in 157,157 real @36 turns, past the 150,929 advisory; brief-cut for this wrap
  ~175,000 (DECLARED, approximate).
- Rulings 338 → 347: `s245-D1…D6` (Dave: *"take the recommendations"* — the five L2 answers + role
  words `-lead/-evidence/-context`), `s245-D7` (L3 § 06 all recommendations: *"I'll go with all the
  recommendations"*), `s245-D8` (row height: *"Okay it's 2 but I do prefer the spacing on 3."*),
  `s245-D9` (ladder B 120/240/96 + his 4px/grid words — quote them from the ruling, they are long).
- Built: D discharge arm (`s244-D1` BUILT; `_capture_gate.py` +298, `_GAUGE-LOG.md` declaration
  line, check-in 1 → 0 STRUCTURAL); R reds ([13] fixed, [61] regenerated, [18] Dave's —
  `_GAUGE-LOG.md:2276` vs `:2294`, [124] ported by L5, [136] cleared by the cut); L2 populated (20/20
  metas, 11/14 fallbacks proven JS-off, prose now blocks, rule 2a); L3 enacted (6 module-level
  `groupsWith` edges, `_validate_composition.py` step 1b, rails dial, rename, rule 7b); L4 + K
  surfaces then enacted (floor `minmax(unit,auto)` nested, outer auto, rungs 120/240/96, 0 overrun at
  1440); L5 cut: commits `5ae4d32` · `f6a8340` · `d806a90` · `5a196d4` (read subjects back from `git
  log`), zip `apollo-spider/dist/Apollo-Spider-v1.0.6.zip` 20,174,484 B sha256
  `ffffd5c281f2ebb94667183594a2f24158e2f236627f8b645c53dcfd373860bd`, manifest @`f6a8340`, status
  PROPOSED (derived, `s237-D9`), release job 9/9 rc 0 locally, proving copy at root. ⛔ NOT PUSHED —
  Dave's; 6 commits ahead of origin before the wrap commit.
- CI read-back (`s203-D1`): R read #482's step verdicts via the public Actions API (gates step 5
  survey FAIL, step 6 build FAIL, 7–9 green). Read-backs still owed for `a09a3ea`, `7f8801f`,
  `7ad3a26`, `33062c1` and tonight's push.
- Still Dave's (carry, his words): the radius candidate set pasted from
  `RADIUS-TUNER-2026-09-03-v1.html` (control 6 / surface 8 / container 12 + segmented xs–l), UNRULED
  and unscoped — question: Console only or all four themes? · #218 duplicate gauge line ("keep the
  first" recommended, not answered) · the 4px audit of components/type/spacing (his insight: the grid
  may not matter for containers; D after the audit) · cold test of v1.0.6 = ratify/hold word · L5's
  RSQs 1, 2, 4 · L2-populate's 2 · L3's, L4's, D's, K's, R's ruling-shaped questions by name · the
  four UNRULED escapes · `BOOT_BAND_SIGMA` · diet S2/S3/S4/S6 · `SCHEMA-LOOSENED` · everything in
  `_CARRIES.md` at its age.
- Deviations to declare: L5 added then REVERTED a `_build_all.STEPS` entry (chain staleness);
  `DOC_ROW_ACK` used on four commits; `gen_bento_matrix_217.py --selftest` 3 pre-existing reds (not in
  STEPS); probe NOT planted, no quiz; `_build_all.py` not run; full `_capture_gate.py --selftest` not
  re-driven (wall).

**DO-NOT-RULE.** Everything in the "Still Dave's" line. Do not re-word, re-scope or close any of his
items. Do not move any constant/ceiling/version.

**RETURN, SHORT.** The wrap commit sha + subject read back from `git log -1`; the `_CHAIN.md` footer
line (tape figure) and the `TITLE THE NEXT CHAT →` line verbatim; the ★ LATEST banner's tape count
vs the 1,200 cap; check-in tail (STRUCTURAL / warn counts); declared skips with sizes; anything red.

## Findings

None — a brief carries no findings; the wrap report does.

## RULING-SHAPED QUESTIONS

**None.** A brief asks; it does not answer. The wrap report's RULING-SHAPED section carries what the
ritual could not settle.

## UNPROVEN / CLAIMED (ADR-0016)

- **CLAIMED, by construction:** every figure in this file is the conductor's DECLARED figure as
  briefed. The wrap report re-probes each at its seat and names which held.

## Evidence

No evidence directory: this file IS the evidence (the brief, filed). The wrap's ops files and
scratch scripts live under `knowledge/_tmp/wrap245/` (gitignored, in-mount, session-owned).

REPLAY-THESE: this file § What was asked (the brief) (~1,400 tk) — the fence, when reading the wrap report
