# `#262`-`O` — the three-option opener (s262-D4), and v1.0.9 re-cut at the new HEAD

session: `#262` · 2026-09-09 · sub index: `O` · provenance: 262 · 2026-09-09 · status: observed
lane: enact s262-D4 in the pack, then repeat lane R's cut one commit later. **DID NOT RELEASE.**

## VERDICT
The opener menu is in the pack's ONE source and projected to all three hosts. v1.0.9 re-cut at
**`50bdedc`** — 1698 files, manifest sha256 `3f20975355746cd0`, zip twice-baked to
`b6a61cd051af6827c12946f67509d6ee6a554caeea0230c4969d245b0ae6d6e7`, `--check` GREEN.
**Still PROPOSED**: no `--release`, no `RATIFY_IDS` row, no ruling inscribed.
COUNTS: findings 1 · ruling-shaped 0 · declared skips 1

## Step 1 — the opener (`50bdedc`)
`cold-start/COPILOT-BOOT.md` is the source, so all the writing happened there:
- **Arm 0 step 3** — "Ask what they want to build" → "**Offer the three options below**"; the
  do-not-insist sentence about the walkthrough is untouched.
- **Arm 1** — now reads the chain **back** in two or three sentences, then offers the same menu;
  and *if the chain says work is mid-flight, that item is named FIRST* and the menu follows it.
- **New section "The three options"**, a numbered list of three, one line each, fired on "good
  morning" or any other salutation: 1 **Interview me** → reply *"describe what you would like as
  the kick-off"*, then `skills/grill-me/SKILL.md`. 2 **Typed prompt or brief** → then **offer**
  grill-me, offer and do not insist. 3 **Describe the goal or vision** → propose the shape,
  confirm before building.
- ⛔ A closing refusal: **three, not four**. Dave left a fourth slot blank; the file says so, so
  the next reader does not fill it in.

`FIRST-SESSION.md` Step 5 now promises what the boot rule delivers — "offers you three ways to
start" — instead of "asks what you want to do next".

**FINDING 1 — two of the ruling's `governs` files could not be hand-edited, and did not need to be.**
`CLAUDE.md` and `AGENTS.md` are GENERATED from `cold-start/DESIGN-CONTRACT.md` alone, and that
contract says nothing about the opener — it is the 40-line/4096-byte build contract, not the boot
rule. Correctly silent, left alone. The two `copilot-instructions.md` files ARE opener-carrying,
generated by MERGING DESIGN-CONTRACT with COPILOT-BOOT, so they moved via
`python3 cold-start/gen_projections.py` — never by hand.

Gates: `gen_projections --check` **OK** (3 projections + 3 PLACED hosts) · `verify_placement.py`
**3/3 PLACED** · `_gate_frozen_release.py --check` **PASS** · `_gate_pack_docs.py --stage`
**ADVISORY rc=0**, 216 findings, none against the four files touched (pre-existing runbook names).

## Step 2 — the re-cut (`b0d81e8`)
s262-D4 landed after lane R proved v1.0.9 at `902d96b` and before any ratifying word.
**`902d96b` never shipped** — no zip carries it, no `RATIFY_IDS` row was written — so moving the
cut is clean, and nothing here is a recorded version re-baked at the same number.

    git archive 50bdedc | tar -x -C /var/tmp/full9o
    --probe    --commit 50bdedc --full-stage /var/tmp/full9o   41 / 4 / 9 — lane R's shape exactly
    --manifest --commit 50bdedc --full-stage /var/tmp/full9o
        commit 50bdedc41f56  files 1698  bytes 45866645  sha256 3f20975355746cd0
        differential arm: ARMED — REPO-BOUND verdicts are MEASURED
    --dry-run → /var/tmp/o1  b6a61cd051af6827…6d6e7 · → /var/tmp/o2 same · cmp rc 0 BYTE-IDENTICAL
    --check /var/tmp/o1/Apollo-Spider-v1.0.9.zip --commit 50bdedc → CHECK GREEN
⚠ `--manifest` REFUSED until the probe was re-run at the new commit — a manifest may not inherit a
probe taken at another sha. That refusal did its job here. The go/no-go page
`reviews/RELEASE-SPIDER-2026-08-26-v1.html` was rewritten by the dry run, overlay re-injected.

## Commits · what stays for Dave · unproven
`50bdedc` the opener · `b0d81e8` manifest + probe + page. NOT PUSHED (s133-D2).
> **"Ratify v1.0.9."** — now against `50bdedc`, not `902d96b`. Expect `b6a61cd0…6d6e7`.
UNPROVEN: the nine release gates were not re-run in full (lane R's table stands, only four files
moved — declared skip); no live assistant was driven through the new menu.
