# #232 — the v1.0.5 RE-STAGE (with the bento snippet)

*Filed 2026-09-01 by the Opus build sub. Conductor: Fable. Row `W-331`. Status: **RE-STAGED, PROVED, AND HELD `PROPOSED` BY THE MACHINE** — and **STOPPED at the brief's bento clause**.*

**enacted 5 · found-by-doing 3 · ruling-shaped 4 · UNPROVEN 4 · survey reds before 6 → after 6 (same set, none new, none closed)**

Apollo — Spider **v1.0.5** is re-staged at `9cd4b23`, its ship list regenerated, its go/no-go page
regenerated with the real fingerprint, and its bake proved deterministic by two dry-run twins that
are **byte-identical over the whole file**. **No zip was released**; `dist/` is untouched and still
ends at v1.0.4. `--release` refuses and printed why.

## ⛔ THE HEADLINE, AND IT IS A NO-GO ON ONE COLUMN

**The pack now carries the bento snippet — and the bento snippet reds two SHIPPED, BLOCKING gates.**
Measured on a pristine unzip of the twin, with the pack's own runner, the command the design
contract itself ends on:

```
python3 ci-template/run-gates.py     →  35 pass · 2 FAIL · 0 could-not-ask     exit 1
```

`#230`'s repair lane left that column at **`37 pass · 0 FAIL · 1 could-not-ask`, exit 0**
(`notes/_subreports/2026-08-31-230-demo-repairs.md:42`). It is now **exit 1**, and both failures
are the bento snippet's arrival:

| gate | what it says, verbatim |
|---|---|
| `_validate_token_forks.py` | `GATE RED: 1 fork(s) not in the ledger` — `--bento-row-unit theme=mono mode=any`: `canon.css:1070 .c-bento → 320px` vs `canon.css:17232 :where(.cn-template-dashboard-bento) .c-bento.tpl-group-rail → 184px` |
| `_validate_type_blast_radius.py` | `ESCAPED: \`.btn\` now matches ['Template-dashboard-bento.reference.html']` and `ESCAPED: \`.status\` now matches [...]` — outside their acknowledged radius |

Both exit **1** (measured directly, not through a pipe). Both **ship** in the pack
(`knowledge/_validate_token_forks.py`, `knowledge/_validate_type_blast_radius.py` are in the ship
list). Both are driven by `knowledge/_build_all.py` (`_build_all.STEPS` lines 355, 361–362), which is
**gates.yml step 6** — so **CI's gates job will be red on step 6 at `9cd4b23`**, independently of
this lane.

The brief's clause is explicit: *"If the bento snippet fails a pack gate: STOP and report. The
snippet is ratified work from #231 — you package it, you do not edit it."* **Nothing was edited.**
The three available fixes are each outside this lane's fences and are put as ruling-shaped items 1
and 2 below: declare the fork in `knowledge/_TOKEN-FORK-LEDGER.json`, `--update` the type-blast
acknowledgement, or namespace `.btn`/`.status` in the ratified snippet.

⚠ **This does not stop the demo pack from existing.** The zip is baked, byte-matched, and cold-
testable; the design contract's *last command* is what goes red on it.

---

## COUNTS

| | before (manifest at `9c329a17`) | after (this lane, at `9cd4b23`) |
|---|---|---|
| version | v1.0.5 | **v1.0.5** (unmoved — this is a re-stage, not a cut) |
| manifest commit | `9c329a179188` | **`9cd4b2315433`** (HEAD, unpushed) |
| ship list | 1,661 files · 42,048,114 B · sha256 `81414f417929487d` | **1,664 files · 42,355,750 B · sha256 `14f9197c8ed17569`** |
| staged tree / zip entries | — | **1,669** |
| dry-run twin zip | — | **`Apollo-Spider-v1.0.5.zip`, 20,073,139 bytes** |
| **zip sha256** | — | **`92f5e10ea2cf60fc27854eab009214414fb8132b798430fc46f16865620e69f8`** |
| dry-run byte-match | — | ✅ **GREEN** — two dry-runs into two directories, `cmp` over the whole file: identical |
| `--check <twin> --commit 9cd4b23` | (RED at #230, 3 stamped paths) | ✅ **`CHECK GREEN — matches the manifest at 9cd4b2315433`** |
| status | `PROPOSED` | **`PROPOSED — no ruling is keyed to v1.0.5 yet`** (read from the store, never typed) |
| `RATIFY_IDS["v1.0.4"]` | `s228-D4` | **`s228-D4` — UNMOVED, no key touched** |
| `RATIFY_IDS["v1.0.5"]` | absent | **still absent — deliberate** |
| gates-group roster | **57** | **57 — UNMOVED by this lane** (see finding ②) |
| snippets group | 137 | **138** |
| components group | 139 | **140** |
| showroom group | 288 | **289** |
| survey reds | **6** — `[3] [18] [61] [111] [118] [136]` | **6 — the same six.** None new, none closed |
| cold `run-gates.py` on the twin | (37 pass · 0 FAIL · 1 CNA, exit 0 @#230) | **35 pass · 2 FAIL · 0 CNA, exit 1** |
| `dist/` | v1.0.0 … v1.0.4 | **UNCHANGED** — no v1.0.5 |

**No git operations were performed.** No `_rulings.json` write, no W-row, no state write, no memory
write, no roster edit, no canon/snippet edit, no `_build_all.py`, no threshold pick, no bento-RSQ
answer, no ratification claim.

### THE MEMBERSHIP MOVE — measured path by path, and it is EXACTLY THREE

Regenerating at `9cd4b23` added three paths and removed none. All three are the bento snippet's own
family, all three committed at `e7cf3db` (#231):

```
ADDED  : knowledge/components/template-dashboard-bento.meta.json      (engine-canon.components 139→140)
         knowledge/snippets/Template-dashboard-bento.reference.html   (engine-canon.snippets   137→138)
         showroom/template-dashboard-bento.html                       (library.showroom        288→289)
REMOVED: (none)
```

Every other group is byte-for-byte the same membership. **The licensed +1 is the only membership
change**, and it arrives as +3 files because the ratified #231 work is a snippet, its meta, and its
generated showroom page — not one file.

### PRESENCE PROBE — `grep -rl '\.c-bento'` over the STAGED pack's knowledge dir

Driven inside `…/twinA/Apollo-Spider-v1.0.5/`, the tree the zip is made from:

```
knowledge/canon/canon.css
knowledge/canon/gen_canon_bento.py
knowledge/snippets/Template-dashboard-bento.reference.html      ← the snippet, matched by path
knowledge/_TOKEN-FORK-LEDGER.json
knowledge/components/template-dashboard-bento.meta.json
```

`grep -c 'c-bento' knowledge/snippets/Template-dashboard-bento.reference.html` → **59**. And in the
zip's own namelist: `Apollo-Spider-v1.0.5/knowledge/snippets/Template-dashboard-bento.reference.html`
plus `…/knowledge/components/template-dashboard-bento.meta.json` and
`…/showroom/template-dashboard-bento.html`. **Matched, quoted, and present in the artefact — not
inferred from a count.**

---

## ✅ THE FINGERPRINT IS REAL THIS TIME — #230's ⛔ IS DISCHARGED

`#230` filed its fingerprint as PROVISIONAL because six of its edits sat uncommitted while a dry-run
stages from `git archive <commit>`. **That condition does not hold here, and it was proved, not
assumed.** Every dirty path in the tree at the moment of the bake was tested against the ship list:

| dirty path at bake time | ships? |
|---|---|
| `knowledge/_graph-mark-observations.jsonl` | no |
| `knowledge/_rulings.json` | no (EXCLUDED by rule) |
| `knowledge/_state.json` | no (EXCLUDED by rule) |
| `notes/_REHEARSAL-LOG.jsonl` | no (`notes/` EXCLUDED; `s137-D1` instrument dirt) |
| `notes/_dream/_GRADE-DECISIONS.jsonl` | no |
| `knowledge/_probe/session-232.json` *(untracked)* | no |
| `notes/_briefs/2026-09-01-232-restage-v105-brief.md` *(untracked)* | no |

**ZERO dirty shipped paths.** `92f5e10e…` is therefore the fingerprint of a bake at `9cd4b23` with
nothing missing from the stage — the same number a `--release` at this commit would produce. It is
on Dave's go/no-go page and it is safe to quote. Corroborated three ways: two dry-runs `cmp`-
identical over 20,073,139 bytes; `--check <twin>` GREEN against the manifest; and
`_gate_release_audit --check` GREEN (`byte-identical to a fresh generation at 9cd4b2315433`).

---

## THE PROVING ZIP FOR DAVE'S COLD TEST — WHERE IT IS, AND WHY IT WILL NOT VANISH AGAIN

**The receipt on the vanished one, found not guessed.** `_LIVE-STATE.md:81` records it in the #231
seat's own words: the #230 twin was written to `outputs/` — **`(NON-REPO: the Cowork session outputs
folder)`, `s191-D2`** — at `5c9b6247…` / 19,972,910 B, and by #231 *"`outputs/` holds no zip at all;
a tree-wide `find` and `find / -xdev` both return nothing"*. **The mechanism is not a mystery and is
not a loss: the Cowork outputs folder is SESSION-SCOPED.** A zip parked there is gone the moment the
session that made it ends. Confirmed here: `/sessions/epic-tender-cray/mnt/outputs/` was **empty** at
this lane's open. Nothing was deleted by anybody; the file was written to a surface with a shorter
life than the decision it supports. Declared as a **RECEIPT, not UNKNOWN**.

**The fix, taken:** the proving zip is written to **two** homes this time, the first of which
survives the session —

1. `/Users/daviewen/Documents/Claude/Projects/UX-design/Apollo-Spider-v1.0.5-PROVING.zip`
   — on Dave's own disk, in the repo working tree, and **untrackable**: `git check-ignore -v` says
   `.gitignore:40:*.zip`, so it can never be swept into a commit and it is not in `git status`.
   ⛔ It is deliberately **not** named `Apollo-Spider-v*.zip` under `apollo-spider/dist/`, because
   that one path has a gitignore *exception* and a zip there becomes committable — which is
   `--release`'s act, not this lane's.
2. `<Cowork session outputs>/Apollo-Spider-v1.0.5.zip` — for the conductor to hand to Dave in this
   session. **This copy is the one that dies with the session**; home 1 is the durable one.

Both verified identical to the twin: `sha256 92f5e10ea2cf60fc27854eab009214414fb8132b798430fc46f16865620e69f8`,
20,073,139 bytes, on all three files.

---

## GATE ZERO — the local reproduction of CI's step 5

`.github/workflows/gates.yml` step 5 is `python3 knowledge/_build_survey.py --timeout 60`, driven
here in `--range` chunks covering all 140 steps, **twice** — once on the tree as inherited, once
after the re-stage. Step 6 (`_build_all.py`) was NOT run (fenced; sandbox-impossible as one process).

### REDS BEFORE — 6, and REDS AFTER — the same 6

| step | check | verdict, before → after |
|---|---|---|
| `[3]` | `_build_blast_radius.py --check` | **FAIL → FAIL, unchanged.** See finding ① — it is a SHIPPED file |
| `[18]` | `_gm_usage.py --selftest` | **FAIL → FAIL** — the standing #218 double-testimony red, Dave's since #228 |
| `[61]` | `gen_dashboard.py --check` | **FAIL → FAIL** — stale; does NOT ship; one regeneration away |
| `[111]` | `_build_memento_index.py --check` | **FAIL → FAIL** — stale; does NOT ship; one regeneration away |
| `[118]` | `_gen_chain.py --check` | **FAIL → FAIL** — `_CHAIN.md` is stale against GOOD-MORNING/_LIVE-STATE. Gates-half, inherited, not this lane's |
| `[136]` | `_gate_release_audit.py --selftest` | **FAIL → FAIL** — the PRE-BAKE arm, word for word as #230 declared it: *"the manifest reads version 'v1.0.5' and NO zip in dist/ carries it"*. It closes AT the bake |

**THE RELEASE HALF IS OTHERWISE GREEN, BEFORE AND AFTER** — `[124]` `[133]` `[134]` `[135]` `[137]`
`[138]` all pass. `[135]`, the ship-list audit, is **GREEN after the regeneration**: the manifest on
disk is byte-identical to a fresh generation at `9cd4b2315433`. **No new red opened in the release
half, so the brief's stop-the-lane clause did not fire on the survey.** (The bento clause did — that
is a different instrument, above.)

`[13]` `_capture_gate --selftest` **timed out >60 s, before and after** — the #228 timeout-sensitive
step, *"not a verdict"* in the survey's own words, and untouched by this lane.

---

## ENACTED — 5

**A · The gate probe re-run at HEAD** — `--probe --commit 9cd4b23` (49.8 s). Mandatory: `--manifest`
refuses when the probe's commit differs. Against the committed probe, **five gates changed verdict**
(see finding ③) and the ship-membership classification moved for none of them.

**B · The ship list regenerated at HEAD** — 1,664 files, 42,355,750 bytes, `14f9197c8ed17569`.
`_gen_pack_manifest.py --selftest` → **216 bites, 0 fail**.

**C · Dave's go/no-go page regenerated, not hand-edited** — `--page reviews/RELEASE-SPIDER-2026-08-26-v1.html`
with `--zip-bytes 20073139 --zip-sha 92f5e10e…` (136,300 B written, 136,580 on disk after the
overlay), then `knowledge/_review/_make_review.py` to re-inject the review pair's stamps that the page
write strips (the #219 finding, still live in both `--manifest` and `--dry-run` modes). Both files
carry `92f5e10e`, verified by `grep -c`.

**D · Two dry-run twins into two different directories, `cmp` over the whole file** — byte-identical,
20,073,139 bytes each, `92f5e10e…`.

**E · The proving zip placed in a home that outlives the session** — see above.

---

## FOUND BY DOING, NOT BRIEFED — 3

### ① `knowledge/tokens/_blast-radius.json` IS STALE AT HEAD, AND IT SHIPS — the bento snippet made it stale

`[3]` is red, and the numbers say exactly why:

```
_blast-radius.json  disk 81,602 bytes  vs  fresh 83,563 bytes
_GRAPH-REPORT.md    disk '...135 components · 1043 tokens...'  vs  fresh '...136 compo...'
```

**135 → 136 components is `template-dashboard-bento.meta.json` arriving at `e7cf3db`.** The file is
in `engine-canon.tokens` and therefore **in this zip** — so v1.0.5 ships a blast-radius computed for
135 components beside a component set of 136. This is **#230's finding ② recurring at the very next
stage**, and its diagnosis holds unchanged: no release-side gate can see it (`--check` compares the
manifest to a fresh *generation*, the pack's blob-fidelity arm compares shipped bytes to the
*commit's* blobs — both are green on a stale file, because the file matches itself).

⛔ **DELIBERATELY NOT FIXED HERE, AND THE REASON IS THE FINGERPRINT.** Regenerating it dirties a
**shipped** path; a dry-run stages from `git archive`, so the fix could not reach this zip, and the
fingerprint on Dave's page would go back to being provisional — the exact defect #230 filed and this
lane discharged. Handing Dave a real fingerprint on a slightly stale support artefact beats handing
him a provisional fingerprint on a fresh one, on demo day. **The fix is one deterministic command
and it is the conductor's**, paired with a re-run of the tail (ruling-shaped item 3).

### ② THE ROSTER IS 57, NOT 58 — a gate left the pack at #230 with nobody deciding

The brief inherits *"roster shipped HELD at 58 under `s228-D5`"*, and `_LIVE-STATE.md:81` says
**"roster HELD at 58"** beside the very manifest that reads 57. **Both are stale.** Measured across
the committed manifests:

| manifest generated at | gates group |
|---|---|
| `b5bee679` (v1.0.4 cut, #228) | 58 |
| `97d7b865` (#230 cut) | 58 |
| **`9c329a17`** (#230 demo-repairs) | **57** |
| `9cd4b231` (this lane) | **57 — unmoved by me** |

The path that left: **`knowledge/_validate_wiring.py`**, and the mechanism is measured, not guessed —
the probe's `repo_bound` list went **8 → 9** at `9c329a17`, gaining `_validate_wiring.py` with the
reason *"refuses for `knowledge/_build_all.py`, a repo resource the pack does not ship — not a
dependency the designer can install"*. **REPO-BOUND gates do not ship.** So #230's demo-repair — the
fix that taught pack gates to REFUSE for repo resources rather than fail, the one that took the cold
column to `0 FAIL` — *also* silently removed a gate from the designer roster. Deliberate in
mechanism, undecided as a roster move. This is **#228 ruling-shaped item 3 in reverse**: there, a
gate arriving raised 58→59 and was held; here, a gate leaving lowered 58→57 and nobody was asked.

⚠ **AND THE BRIEF'S `58 → 59` IS A CONFLATION OF TWO DIFFERENT ROSTERS.** The 58 of `s228-D5` is the
**gates group**. The bento snippet is not a gate and can never move it; it lands in
`engine-canon.snippets` (137→138). Both figures are stamped in COUNTS above so the next reader cannot
repeat the merge. `[[premise-ages-faster-than-rule]]`.

### ③ Five gates changed probe verdict between `9c329a17` and `9cd4b231` — two of them are the STOP

```
_validate_token_forks.py        exit 0 → 1   "ran clean, verdict PASS" → "ran, verdict FAIL"
_validate_type_blast_radius.py  exit 0 → 1   selftest green → red
_validate_descender_computed.py  selftest red → crashed   (why: "playwright install chromium" → "playwright")
_validate_hit_area.py            selftest red → crashed   (same)
_validate_state_contrast.py      selftest red → crashed   (same)
```

The first two are the bento snippet, and they are the headline. **The three playwright rows are an
ENVIRONMENT difference between the #230 VM and this one, not a code change** — the probe's own `why`
degraded from an install hint to a bare dependency name. `[[gate-cannot-pass-in-one-environment]]`.
The published verdict tally is **unmoved either way**: 37 RUNNABLE / 9 REPO-BOUND / 3 NEEDS-DEP,
before and after.

---

## RULING-SHAPED — 4

### 1. The bento fork `--bento-row-unit` is undeclared, and declaring it is a ruling ⬛

`canon.css:17232` sets `--bento-row-unit: 184px` under `:where(.cn-template-dashboard-bento)
.c-bento.tpl-group-rail` against the `.c-bento` default of `320px`, same theme, same mode. The
fork-ban gate (`s136-D1` clause D) reds because it is not in
`knowledge/_TOKEN-FORK-LEDGER.json` (50 declared forks today; the ledger already names `.c-bento`).
⬛ **Dave's / the conductor's:** declare it in the ledger (it is an intentional rail geometry), or
unify the two values. Neither is a tidy — a ledger row is a standing declaration.

### 2. `.btn` and `.status` escaped their acknowledged blast radius via the bento snippet ⬛

`_validate_type_blast_radius.py`: `.btn` 25 → **26** files, `.status` 11 → **12**, corpus 149 → 150,
the new file in every case being `Template-dashboard-bento.reference.html`. Its own remedy line
offers exactly two: *"Namespace it, or `--update` and review the diff."* **Namespacing means editing
a ratified snippet — forbidden here by the brief.** `--update` means widening an acknowledged radius,
which is a derivation-governance act. ⬛ **Dave's.** ⚠ Note the third option nobody has priced: this
gate is BLOCKING and ships, so *every* future template snippet that uses `.btn` will hit it.

### 3. The stale shipped blast-radius: ship it, or commit and re-cut? ⬛

Finding ① in one question. **Ship as-is** = a real fingerprint today and a support artefact that
disagrees with the component set by one. **Fix and re-cut** = `python3
knowledge/tokens/_build_blast_radius.py`, commit, then re-run the whole tail
(`--probe → --manifest → --page → _make_review.py → two dry-runs → cmp`) for a *new* fingerprint, and
Dave's page must be regenerated before his word is taken on it. ⬛ **The conductor's, and it is a
clock decision, not a correctness one.**

### 4. The roster is 57 and nobody ruled it ⬛

Finding ② in one question: is `_validate_wiring.py` correctly out of the designer roster (it refuses
in a pack, so it would ship and have nothing to say — word for word the #220 `_gate_pack_imports.py`
argument and the #228 scratch-probe argument), or should it be held in the way `s228-D5` held 58?
⬛ **Dave's.** Whatever he says, **the `58` in `_LIVE-STATE.md:81` and in briefs is now false and
should be restated**, because it has already misled one brief.

---

## UNPROVEN — 4, declared

1. **`_build_all.py` (gates.yml step 6) was NOT run** — fenced, and sandbox-impossible as one
   process. ⚠ **But its verdict is not unknown:** two of its steps (355, 361) are the gates this lane
   measured red directly. **CI's gates job will be red on step 6.** Named, not discovered later.
2. **`knowledge/_tests/test_gates.py` (step 7) and `test_advisory.py` (step 8) were NOT driven.**
   #228 measured the arithmetic: the harness `copytree`s all of `knowledge/`. Re-measured today:
   `/` 4.3 G free, `/sessions` 2.6 G, `/dev/shm` 512 M. Nothing in this lane touched a gate's
   failure class.
3. **No push, no CI read-back.** `s203-D1` is the conductor's; this lane performed no git operation
   at all, so there is nothing of mine on a remote to read back.
4. **The cold-test column was measured with `ci-template/run-gates.py` on the twin's STAGE, not on a
   fresh unzip on a second machine.** The stage is the exact tree the zip is built from and
   `--check <zip>` is GREEN against it, so the two are the same bytes — but **Dave's live cold test
   on a second machine remains the acceptance test** (`s227-D6`, `s229-D1`), and it is not this.

Also unproven and unchanged: `_validate_state_contrast` (`[74]`, no playwright) and the two
`_gen_schematic` arms (`[121]` `[122]`, `real` tier unreachable, #173/#183) — three standing
`COULD-NOT-ASK` refusals, not verdicts.

---

## WHAT WAS DRIVEN

```
knowledge/_build_survey.py --timeout 60 --range 1:70 / 71:114 / 115:140   BEFORE  6 reds named
knowledge/_build_survey.py --timeout 60 --range 1:70 / 71:114 / 115:140   AFTER   the same 6
knowledge/_release/_gen_pack_manifest.py --probe    --commit 9cd4b23   rc=0   49.8 s
knowledge/_release/_gen_pack_manifest.py --manifest --commit 9cd4b23   rc=0   1664 files, 14f9197c8ed17569
knowledge/_release/_gen_pack_manifest.py --page … --zip-bytes --zip-sha rc=0   136,300 bytes
knowledge/_release/_gen_pack_manifest.py --selftest                    rc=0   216 bites, 0 fail
knowledge/_review/_make_review.py <go/no-go>                           rc=0   overlay re-injected
build-designer-pack.sh --dry-run (x2, two directories)                 rc=0   cmp: BYTE-IDENTICAL
build-designer-pack.sh --check <twinA> --commit 9cd4b23                rc=0   CHECK GREEN
build-designer-pack.sh --selftest                                      rc=0   3/3 refusals green, incl.
                                                                              "status is PROPOSED, so --release cannot run"
knowledge/_release/_gate_release_audit.py --check                      rc=0   byte-identical at 9cd4b2315433
knowledge/_release/_gate_release_audit.py --selftest                   rc=1   10 bites, 1 fail — the PRE-BAKE arm
knowledge/_release/_gate_frozen_release.py --check / --selftest        rc=0   3 arms · 14 bites, 0 fail
knowledge/_validate_package_delta.py                                   rc=0   0 failures
knowledge/_validate_wiring.py                                          rc=0   0 failures, 49 wired
apollo-spider/cold-start/gen_projections.py --check                    rc=0   3 projections + 3 hosts in sync
knowledge/_validate_token_forks.py                                     rc=1   ⛔ 1 undeclared fork
knowledge/_validate_type_blast_radius.py  (and --selftest)             rc=1   ⛔ 2 escaped selectors
ci-template/run-gates.py  (inside the twin's pristine stage)           rc=1   ⛔ 35 pass · 2 FAIL · 0 CNA
knowledge/tokens/_build_blast_radius.py --check                        rc=1   2 files stale (NOT regenerated — see ①)
```

⚠ **`TMPDIR` was `/sessions/epic-tender-cray/tmp` throughout, never `/var/tmp`** (#227 sixth
stratum). **But `--probe` cannot be steered:** `_gen_pack_manifest.py` **hardcodes** `dir="/var/tmp"`
at lines 939, 2717 and 2751 and forces `TMPDIR="/var/tmp"` into the child env at 969 and 1016. It
left a 46 MB orphan stage (`/var/tmp/packgateprobe-znhhtt2c`), **removed by hand here**; `/var/tmp`
is back to 104 K. On a VM already at 56 % this is one probe from the #227 trap.

---

## WHAT THIS LANE LEFT DIRTY — for the conductor to reconcile

| path | ships? | why it moved |
|---|---|---|
| `knowledge/_release/_pack_manifest.json` | no | regenerated at `9cd4b23` — the deliverable |
| `knowledge/_release/_pack_gate_probe.json` | no | re-probed at `9cd4b23` — `--manifest` refuses without it |
| `reviews/RELEASE-SPIDER-2026-08-26-v1.html` | no | Dave's go/no-go page, regenerated with the real fingerprint |
| `reviews/RELEASE-SPIDER-2026-08-26-v1.REVIEW.html` | no | overlay re-injected after the page write stripped it |
| `knowledge/_TYPE-BLAST-GATE.md` | no | ⚠ **written by the gate itself when driven.** Not an edit of mine. Its committed copy claims *"every appended selector is registered and within its acknowledged blast radius"*, which is **false at HEAD**; the working copy now records the two escapes truthfully |

**Untouched by this lane and dirty on arrival:** `knowledge/_graph-mark-observations.jsonl`,
`knowledge/_rulings.json`, `knowledge/_state.json`, `notes/_REHEARSAL-LOG.jsonl` (`s137-D1` instrument
dirt), `notes/_dream/_GRADE-DECISIONS.jsonl`, and untracked `knowledge/_probe/session-232.json`.

⛔ **`Apollo-Spider-v1.0.5-PROVING.zip` at the repo root is NOT in `git status`** — `.gitignore:40
*.zip` catches it. It cannot be swept into a commit by any `git add`, including `-A`.

---

## REPLAY-THESE — for the conductor

1. ⛔ **LEAD WITH THE STOP, NOT THE FINGERPRINT.** The pack is byte-matched and the fingerprint is
   real — **and the design contract's own last command exits 1 on a pristine unzip.** If Dave cold-
   tests before ruling items 1 and 2, that is what he will see. He should be told first.
2. ✅ **`92f5e10ea2cf60fc27854eab009214414fb8132b798430fc46f16865620e69f8` IS SAFE TO QUOTE** —
   20,073,139 bytes, zero dirty shipped paths at bake time, `--check` GREEN, twins `cmp`-identical.
   This is #230's ⛔ discharged; unlike `6e7c6dd4…`, this one is not provisional.
3. **Hand Dave the DURABLE copy**, `Apollo-Spider-v1.0.5-PROVING.zip` at the repo root — the outputs-
   folder copy dies with this session, which is precisely how the #230 twin vanished.
4. **Put ruling-shaped 1 and 2 to Dave together** — they are one question wearing two gate names:
   what does the ratified bento snippet owe the fork ledger and the type-blast radius? Both fixes are
   one command each; neither is a sub's to take.
5. **Decide ruling-shaped 3 on the clock** — regenerate `_blast-radius.json` and re-cut for a fresh
   fingerprint, or ship the real fingerprint with a one-component-stale support artefact. If
   re-cutting: `--probe → --manifest → --page → _make_review.py → 2 dry-runs → cmp`, **in that order**,
   or the stale-copy-list defect returns wearing the version's clothes.
6. **Restate the roster.** `_LIVE-STATE.md:81` says 58; the manifest it cites says 57. Correct the
   live line whichever way Dave rules, and ⛔ **never brief a roster figure again without naming its
   COLUMN** — this brief's `58 → 59` merged the gates roster with the snippets group.
7. **`RATIFY_IDS` was NOT touched.** `v1.0.4` still points at `s228-D4`; there is still no `v1.0.5`
   row, and `--release` still refuses with `status is PROPOSED`. When Dave ratifies, add **one** row
   keyed to that **fresh ratifying word** — never an authorisation, never by re-baking a shipped zip.
8. **`[136]` stays red for the whole PROPOSED window and CI will see it.** #230's ruling-shaped item 2
   (should the arm read the manifest's own `status`?) is still open and still unanswered.
9. **`[61]`, `[111]` and `[118]` are one regeneration each and none of them ships.** `[18]` is Dave's,
   unchanged since #228.
10. **Price the `/var/tmp` hardcode** in `_gen_pack_manifest.py` (5 sites). `TMPDIR` cannot steer the
    probe off a disk #227 already named a trap.
11. **Mint this report's store row** — this lane wrote no W-row and no state
    `[[forgotten-document-class]]`.

---

## `s214-D5` — wrap-handover cost

| | |
|---|---|
| brief cut | **1,188 tokens** (cl100k ESTIMATE, `tiktoken` 0.14.0 — a measurement in a NAMED unit, not the real tokeniser) · 4,403 bytes |
| sub cut (this report) | **8,261 tokens** (cl100k ESTIMATE) · 28,460 bytes |
| delta | **+7,073 tokens, ×6.95** — the report is the handover; the brief is the ask |
| chat stub | ~180 tokens — version · sha head · byte-match · ratify-key · roster · page path · this line |

**UNOBSERVABLES, DECLARED.** This sub cannot read its own `message.usage`, so **its window fill and
its share of the session budget are UNKNOWN here and are not estimated** — the conductor's
`_checkin.py` at the lane seam is the only instrument that can price them
`[[measuring-tool-must-not-guess]]`. The figures above are **artefact sizes**, which is a different
measurement from context spend and is labelled as such `[[measure-dont-convert-units]]`. Wall-clock
spend was dominated by three items whose costs are real and named: the survey driven twice over 140
steps (6 ranged runs), the gate probe (49.8 s), and two full dry-run bakes plus a `cmp` over
20,073,139 bytes.
