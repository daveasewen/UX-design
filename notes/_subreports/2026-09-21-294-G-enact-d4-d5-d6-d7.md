# 2026-09-21 · #294 · LANE G — ENACTING `s294-D4` · `s294-D5` · `s294-D6` · `s294-D7`

**Seat:** delegated Opus 5 lane, no git writes, read-only on the index. Files owned and touched:
`knowledge/_gardener.py` · `knowledge/_gauge_tokens.py` · `knowledge/_git_commit.sh`, plus the
generated sidecar `notes/_dream/_MEMORY-GRADES.json` and its log
`notes/_dream/_GRADE-DECISIONS.jsonl`. **Nothing else was edited.** All four rulings were read by
id out of `knowledge/_rulings.json` by field extraction; the file was never printed whole.

**Both FILL lines, and they are the CONDUCTOR'S WINDOW, not this lane's** — `_checkin.py` reads
the top-level transcript by `find_transcript()`, and a delegated lane is a sidechain that
instrument does not see, so the two readings are the conductor's seat measured twice:

```
open   FILL  173,377 real  RESIDENT CONTEXT — what the last call actually sent
close  FILL  173,377 real  RESIDENT CONTEXT — what the last call actually sent
```

⚠ **Identical, and declared rather than explained away:** the same conductor turn is the newest
`usage` on that transcript at both readings (`boot 74,656 real`, `peak 173,377 real across 23
turns`, `ratio 87% of the 200,000 passed, ON FILL`). This lane's own token spend is **not in
either figure** and no figure here is offered as a lane reading.

---

## `s294-D4` — the two numbers are RULED, and the label came off

**Path changed:** `knowledge/_gardener.py`.

**Values byte-unchanged, asserted:** `GRADE_AGING_DAYS = 30`, `POPULATION_DELTA_THRESHOLD = 5`.
`git diff` on those two lines shows the COMMENT moving and the literal standing.

1. A sentinelled **`⬛ s294-D4 RULED BLOCK`** now wraps both constants, quoting the ruling, its
   date, its id and the one piece of evidence that cut towards ruling (the 30 moved 13 entries
   and took the stale count 1 → 16 on the first cycle).
2. The word `provisional` is off both constants and — the part that actually reaches a reader —
   **off the two grade strings the module PRINTS beside the number**: the `AGING` vocabulary line
   in the sidecar and `grade_entry`'s AGING `why`, both now `RULED s294-D4`.
3. The two earlier annotations (`s182-D1` CALL 2's "EXPLICIT PLACEHOLDER", #256's "PLACEHOLDER,
   picked not derived") are **quoted and marked spent, not deleted** — `s183-D1`/`s188-D2` form.

**The bite:** `provisional_label_bite()`, run by `--selftest-grades`. It fires on a line that
NAMES either constant (or the `aging_days` parameter that carries the limit into a printed
string) AND carries the word. **Scope is the line on purpose** — a file-wide ban would forbid the
ruling from stating itself, and the grade SCHEMA is still provisional under `s182-D1`, so a bite
that could not tell the two apart would assert more than Dave said. One carve-out, by greppable
sentinel pair `D4-BITE-SELF-EXEMPT BEGIN/END`, covering only the bite's own docstring and the
arms that plant the label.

**Bites: 4 new** — `d4a` plant (label restored beside `GRADE_AGING_DAYS` ⇒ fires, names the line)
· `d4b` plant (same for `POPULATION_DELTA_THRESHOLD`) · `d4c` control (live source clean) ·
`d4d` the two values are still 30 and 5.

---

## `s294-D5` — the expiry is CHECKED at push, not remembered

**Path changed:** `knowledge/_git_commit.sh`.

**⛔ First, a premise in the brief that did not survive the probe, and it matters for where the
gate could legally look.** The brief says the token "is in a gitignored env file". **It is not.**
The push credential lives in **`git config remote.origin.url` on Dave's disk** (`s133-D2`,
`notes/_MEMENTO-DECISIONS.md:5884`). The repo's one gitignored env file, `.env.local`, holds a
TypeSafe/Jev API key and nothing to do with the push. The gate therefore **never touches the
credential at all**: it reads one date out of the ledger and compares it to today.

**The date's one home, found as instructed** (`_memento_search.py "token scope expiry"` →
`--fetch`, then `git log -S`): the **W-24 stamp added at #188** in
`notes/_MEMENTO-DECISIONS.md`, the line `The token in force was minted **2026-08-08**, so a
90-day life expires **~2026-11-06**`. The script now READS that line rather than restating the
date — `pat_expiry_read()` anchored on `EXPIRY STAMPED (W-24`.

**Behaviour, in the `--push` path, after the credential check and before `git push`:**

| today vs `2026-11-06` | verdict | what happens |
|---|---|---|
| more than 14 days out | `OK` | one line stating the date and days remaining; push proceeds |
| 14 days or fewer | `WARN` | warns, quotes the date and `s294-D5`; **push proceeds** |
| past it | `EXPIRED` | **REFUSES**, quotes the date, today, the days past, and `s294-D5` |
| stamp unreadable | `UNREADABLE` | **REFUSES** — with a `PAT_EXPIRY_ACK="<reason>"` declared-gap hatch spelled like `SHOWROOM_ACK`/`DOC_ROW_ACK`/`SESSION_ACK` |

The refusal names the remedy as **Dave's hands at GitHub** (mint a fresh fine-grained PAT, this
repo only, Contents read/write only, 90 days; install it himself in a terminal; update the W-24
stamp so the gate reads the new date) and says the credential never transits the chat.

**The "paste into chat" clause: already satisfied, verified rather than assumed.** The one
refusal that used to instruct it was reworded at #188. Grep over the script finds no unnegated
`paste` instruction — the only occurrence is the negative `⛔ do NOT paste it into chat`. That is
now **asserted as a count**, so a new one cannot land quietly.

**Selftest via a fake date env var, never the real clock alone:** `APOLLO_FAKE_TODAY=YYYY-MM-DD`,
read in exactly one place, real clock as the default.

**Bites: 14 new** (script total 28) — the date is read from the ledger and equals `2026-11-06` ·
`2026-01-01 ⇒ OK` · **`2026-10-22 ⇒ OK` and `2026-10-23 ⇒ WARN`, the boundary asserted from both
sides** · `2026-11-05 ⇒ WARN` · **`2026-11-06` itself ⇒ WARN, not yet expired** · `2026-11-07 ⇒
EXPIRED` · `2027-03-01 ⇒ EXPIRED` · days-remaining is computed (`2026-10-30 ⇒ 7`) · an
unreadable stamp ⇒ `UNREADABLE`, a refusal not a pass · control: with no fake clock the gate
reads the real date · no refusal instructs pasting a credential into chat · **the credential is
only ever TESTED, never printed — asserted as the count of lines reading
`git config remote.origin.url` without a quiet pipe** · the gate's POSITION, after the credential
check and before `git push`.

**What I could not do, declared:** `--push` **end to end is unreachable at this seat** — there is
no credential in this sandbox's remote URL, so `--push` refuses at the credential gate and never
reaches the expiry gate; and a seat that COULD reach it would push. So the gate's LOGIC is bitten
through `pat_expiry_verdict` (the exact function the push path branches on) and its POSITION is
bitten by line order. Both facts are written into the selftest beside the arms.

---

## `s294-D6` — the grader is RE-POINTED, and it declares what it cannot grade

**Paths changed:** `knowledge/_gardener.py`; `notes/_dream/_MEMORY-GRADES.json` (regenerated);
`notes/_dream/_GRADE-DECISIONS.jsonl` (rows appended by the boot surface).

**The consumer sweep first** (`grep WRAP-MEMORY-HOOK | MEMORY-ARCHIVE | .auto-memory` across
`knowledge/` and `notes/`) — every consumer of the dead path, so the re-point could not be made
in one file while others kept pointing at nothing:

| file | what it does with the old path | mine? |
|---|---|---|
| `knowledge/_gardener.py:123,165` | `MEMORY_DIRS` + `MEMORY_INDEX_BASENAME` — **the grader itself** | **yes — re-pointed** |
| `knowledge/_checkin.py:1388–1440` | the #293 void fence; probes the sidecar's `memory_index` | no — **works unchanged**, see below |
| `knowledge/_boot_decompose.py:133` | probes the mount path, prints `MISSING` | no — already honest |
| `knowledge/_boot_remeasure.py:36` | a **hard-coded** `/sessions/upbeat-compassionate-darwin/…` path | no — diff owed, below |
| `knowledge/_memory_cap_check.py:55` | `MEMORY_REL = .auto-memory/MEMORY.md` | no — diff owed, below |
| `knowledge/_capture_gate.py:4475` | `HOOK_STORE_SKIP = ("MEMORY.md", "MEMORY-ARCHIVE.md")` | no (B's) — no change needed |
| `knowledge/_validate_assertions.py:358–401` | probes mount-vs-repo presence | no — already probes, no change needed |

**The population it CAN read:** `notes/_lanes/<n>/WRAP-MEMORY-HOOK*.md`, **23 files**, one per
wrap since #271. One mirror FILE is one hook, and hook ids are written relative to
`notes/_lanes/`, so `hook_file_body()` and `grade_entry()` resolve a mirror hook with **no special
case downstream** — the file remains the grading unit exactly as `s188-D1` ruled.

Three things had to be measured before the code could be honest about them:

1. **The index line is written in five heading shapes across the run.** A matcher keyed on the
   literal "THE INDEX LINE" caught **8 of 23**; the widened one catches **23 of 23**, and the
   SECTION (not a fence, not a list item, not a blockquote) is the unit, because the line appears
   as all three. Result: **1 declared fallback** (`283/`, whose section is empty) instead of 19.
2. **The store's own filenames must not read STALE.** Every mirror file names where it is going —
   `index.md`, `wrap-<n>-<slug>.md` — and those are cloud paths this repo does not contain.
   Probing them graded live placements STALE: **19 of 23 on the first drive**, the exact
   false-STALE class `resolve_claimed_path`'s docstring warns about. They are folded into the
   **`s191-D2` NON-REPO carve-out mechanically** for the mirror source, because the hand-written
   marker `s191-D2` asks for cannot be demanded of other sessions' append-only testimony. The
   declaration is stated once, in code, instead of 23 times by hand.
3. **A source change is not a shrink.** The first re-pointed run read 23 against the dead
   sidecar's 52 and the population alarm called it a **73% cut and refused**. It is not a cut:
   the two counts count different things. `population_delta()` now takes the source on both
   sides and DECLARES `INCOMPARABLE` with no delta computed — and the previous source is
   **PROBED** from the old sidecar's own `memory_index`, not assumed. dream-11 P1(a)'s shrink
   refusal is intact within one source (asserted both ways).

**It declares what it cannot grade, in the sidecar AND printed at every refresh** (`cannot_grade`,
3 entries): the **cloud store itself** (claude.ai Project memory — unreachable from any sandbox
seat, no path to probe, UNGRADED and not reported clean) · **any store entry with no repo
mirror** (invisible by construction; the mirror is the record of what was PLACED, not the
store's contents) · **the store's own properties** (index ordering, the 49,152 B cap, whether a
placement landed).

**The void fence still trips.** `memory_index` is now the population's **provenance anchor**: the
newest mirror file — a real file this module does not write, which stops resolving exactly when
the mirror goes missing. A generated index would have made `_checkin.py`'s fence dead the day it
landed. ⛔ **And the anchor is recorded REPO-RELATIVE, which corrects the old sidecar's shape:**
the dead sidecar stored `/sessions/<session>/mnt/.auto-memory/MEMORY.md`, and the mount name
changes every session, so a plain `os.path.exists` would have reported a perfectly resolvable
path as void at **every seat but the one that wrote it**. `population_index_abs` carries the
absolute form for a human and is **not** what the fence reads.

### The numbers the refresh returned — reported, not interpreted

```
population      : repo-mirror (s294-D6)
  glob          : notes/_lanes/*/WRAP-MEMORY-HOOK*.md
index (anchor)  : notes/_lanes/293/WRAP-MEMORY-HOOK.md
refreshed_at    : 2026-09-21T17:55:57
hooks graded    : 23   FRESH 14 · AGING 0 · STALE 3 · UNPROVABLE 6
unlinked lines  : 1 DECLARED, not graded  (283/WRAP-MEMORY-HOOK.md — empty index-line section)
grade changes   : 0
probe source    : hook FILE 23 · index-line FALLBACK 0 · pinned 0
countable rows  : 492 of 507 at >= window_open 2026-08-16T06:10:28Z  (alert 467 · decision 7)
alert surface   : 4 line(s) at boot
population_age  : n=23 · oldest 6.9d · newest 0.1d · limit 30d · limit_can_bite = False
```

**STALE 3:** `277/` (1 of 6 paths) · `279/` (1 of 15) · `288/` (2 of 18).
**UNPROVABLE 6:** `273/` `274/` `275/` `276/` `284/` `285/` — each because every path its file
names is a store path, declared NON-REPO.

⚠ **`AGING 0` is STRUCTURAL, not a clean bill of health, and the instrument now says so itself.**
The oldest mirror file is **6.9 days** old against the ruled 30-day limit, so the AGING arm
**cannot bite on any member of this population yet**. A new `population_age` field and receipt
line exist for exactly this, because the counts alone cannot tell "nothing is aging" from "the
limit cannot reach anything".

**The boot surface moved as the ruling required.** Before:
`GRADES ⛔ SIDECAR PRESENT BUT VOID — its memory_index does not resolve on disk
(/sessions/busy-festive-davinci/mnt/.auto-memory/MEMORY.md) …`. After: a reading, with
`⛔ STALE 277/ · ⛔ STALE 279/ · ⛔ STALE 288/ · · 4 UNPROVABLE (of 17 starred/blocked)`, surface
cost measured.

**Bites: 6 new** (`--selftest-grades` 32 arms green; `--selftest` 7 green) — `d6a` mirror present
⇒ graded over the mirror, anchor relative AND resolving, cannot-grade declared · **`d6b` PLANT:
mirror deleted ⇒ the anchor stops resolving, which is the fence's own predicate (`kind:
alert-void`)** · **`d6c` PLANT: no mirror at refresh ⇒ loud block naming both paths tried** ·
`d6d` a hook naming only store files ⇒ UNPROVABLE with the declaration, never false STALE ·
`d6e` source change ⇒ incomparable / same source ⇒ still a shrink refusal · `g7b` control: the
mount-source refusal is intact. `g7` was **re-aimed, not retired** — with neither store index nor
mirror the refusal must name both.

**The two owed reviews are NOT held here.** `s294-D6`'s order is the ruling: re-point, THEN
review. The re-point is done and the numbers above are the real ones the review is owed.

---

## `s294-D7` — the `8,470` term re-measured, published BY ADDITION

**Path changed:** `knowledge/_gauge_tokens.py`. **No constant moved** — `BOOT_CEILING_TK`
(70,000), `STOP_LINE_TK`, `BUDGET_HARD` and `s240-D1`'s derived band are byte-unchanged, and
`BOOT_FIRSTTURN_TK` stays deleted.

**What the file does with the term:** it is a **comment-only floor decomposition** at `:161`
(`first turn ~65,400` split into `MEMORY.md 8,470` + `remainder 56,308`, then `+ _CHAIN.md`
`= floor ~75,900`). **No code reads 8,470** — the single grep hit is that comment. It is also
restated at runtime in the `fmethod` provenance string (`:~448`), which listed `MEMORY.md` as a
covered term.

**Method, and it is the same glob `_checkin.find_transcript()` uses:**
`/sessions/*/mnt/.claude/projects/*/*.jsonl`, newest by mtime →
`/sessions/great-dreamy-noether/mnt/.claude/projects/session/501c6cd1-…jsonl`, the conductor's
top-level transcript. Only `usage` fields and system-prompt **block sizes** were read; no prompt
content was printed.

**The reading:**

```
boot, first turn   74,656 REAL   message.usage, first assistant message:
                                 input 2 + cache_creation 38,612 + cache_read 36,042 = 74,656
                                 ✅ reconciles exactly with _checkin.py's own boot line
MEMORY.md               0        ⛔ NOT smaller — GONE. /sessions/<s>/mnt/.auto-memory/MEMORY.md
                                 probed ABSENT, and the store's CONTENTS are fetched by tool call
                                 during the session, never laid into the opener.
<user_memory>       5,413 tape   AND THIS IS WHAT BOOTS IN ITS PLACE — block 3 of the 4 strings
                                 in the opener's prompt_snapshot.systemPrompt, and that block IS
                                 the <user_memory> span exactly (23,385 chars). Nested inside it:
                                 <privacy_requirements> 2,279 · <memory_application> 851.
⇒ the memory term went 8,470 → 5,413, a fall of 3,057, and it stopped being CONTENT (which grew
  with the store) and became FIXED HARNESS TEXT.
```

⚠ **Unit, stated not converted.** 5,413 is cl100k **tape**; 74,656 is **real**; they are not
summed. The old 8,470 was tape too ("tokenised off the mounted auto-memory") published inside a
decomposition of a real total — **that mix is inherited and declared, not compounded**: the
comparison 8,470 → 5,413 is tape-to-tape, same instrument, same unit.

**Bonus, same reading, same transcript — the `remainder 56,308` line is now partly lit:**

```
systemPrompt (4 blocks)  26,984 tape   7,901 · 8 · 13,662 · 5,413   (<env> is 10,095 of it)
opener attachments        6,719 tape   skills roster 2,717 · MCP instruction blocks 2,400 ·
                                       deferred tool NAME list 986 · agent roster 552 · rest 64
⇒ residual              ≈40,953        ⛔ ESTIMATED BY SUBTRACTION (74,656 real − 33,703 tape),
                                       MIXED UNITS BY CONSTRUCTION — the loaded tool SCHEMAS
                                       plus Anthropic's own harness. Quote with the label or not
                                       at all.
```

⛔ **What this reading is NOT:** a band. n=1, one seat, one day. `s240-D1`'s band is derived from
`notes/_GAUGE-LOG.md` and is untouched. Whether any of it may move a constant is Dave's under
`s241-D1`.

**Bite: 1 new arm, 4 assertions** (`_gauge_tokens.py --selftest`, arm **E**) — the old 8,470 line
and the re-measure must stand **side by side** (either one missing fails: D7 forbids replacing) ·
`BOOT_CEILING_TK` still 70,000 · `BOOT_FIRSTTURN_TK` has not come back. **Proven plant-then-detect
and it caught a real self-reference:** the first cut wrote the needle as an inline literal, so the
check's own source satisfied it and deleting the real line left the arm green. Needles are now
built at runtime. Verified by deleting `:161` on a scratch copy — the arm goes RED with its named
message, and green again on restore.

### `P-293-1` — could NOT be closed by this lane, and here is the exact edit owed

It is **not** a `_state.json` row (it appears only inside `W-293e1`'s `closes_when` prose). It
lives at **`knowledge/_parked.json`, item `P-293-1`**, and that register is **hand-authored**:
`_parked.py`'s own docstring says *"This door never writes"*, and it exposes no write API
(`load` / `evaluate` / `report` / `notice` only). `_state.py` has **no `add_argument` at all** —
no CLI write path either. So, **for the conductor**, one field:

```diff
   "id": "P-293-1",
-  "status": "parked",
+  "status": "enacted",
```

with, if a note is wanted beside it: *enacted #294 2026-09-21 by `s294-D7`, which unparks it in
its own words; the reading is published by addition at `knowledge/_gauge_tokens.py:~165` and in
the runtime `fmethod` string — no constant moved.* `_parked.py --check` will then stop surfacing
it. `P-293-1`'s premise line *"`:443` … still names MEMORY.md as a covered term"* is now false:
that string carries the correction by addition.

---

## Diffs owed in files this lane does not own

1. **`knowledge/_checkin.py:1394`** — the boot label still reads
   `first turn = system + tools + MEMORY.md + CLAUDE.md`. `MEMORY.md` is not a boot input at any
   seat. Suggested, by addition: `… system + tools + <user_memory> instructions + CLAUDE.md
   (s294-D7 2026-09-21: MEMORY.md re-measured to 0 — it is not a boot term)`. ⚠ The **fence
   itself needs no change** and was left alone: it probes `memory_index` existence, which now
   points at the mirror anchor and resolves.
2. **`knowledge/_checkin.py:1411`** — the void reason still says *"the store moved at #278 and
   nothing re-pointed the grader"*. Something has now. Suggested tail, by addition: `… — since
   s294-D6 the grader reads the REPO MIRROR (notes/_lanes/*/WRAP-MEMORY-HOOK*.md); this fence
   now means the MIRROR did not resolve.`
3. **`knowledge/_checkin.py:1408–1412`** — hardening, optional: the fence does
   `os.path.exists(_mi)` on a value that is now repo-relative, so it is correct when run from the
   repo root (the runbook's invocation) and errs towards UNKNOWN-not-clean otherwise. One line
   removes the assumption: `_mi = _mi if os.path.isabs(_mi) else os.path.join(REPO, _mi)`.
4. **`knowledge/_boot_remeasure.py:36`** — a **hard-coded** dead path,
   `("MEMORY.md (auto-memory index)", "/sessions/upbeat-compassionate-darwin/mnt/.auto-memory/
   MEMORY.md")`. Not merely stale: it names another session's mount. It should either resolve by
   glob and declare MISSING (the shape `_boot_decompose.py` already uses) or the row should be
   struck by addition with `s294-D7`'s reading beside it.
5. **`knowledge/_memory_cap_check.py:55`** — `MEMORY_REL = .auto-memory/MEMORY.md`. The cap it
   checks (49,152 B) now belongs to the cloud store's `index.md`, which no sandbox seat can
   read. This instrument has had no readable subject since #278 and should say so rather than
   probe a path that cannot exist.

⛔ **No diff is owed in `_capture_gate.py` or `_gate_scratch_hygiene.py` (B's) or `_gen_chain.py`
(A's)** by any of these four rulings. `_capture_gate.py:4475`'s `HOOK_STORE_SKIP` still does the
right thing for either population.

---

## RULING-SHAPED QUESTIONS

*(each with what was measured under it — the heading is the `s218-D7` / `s294-D2` machine-read
form, narrowed from "Ruling-shaped questions, each with what was measured under it" by lane C2 at
staging; no item was changed.)*

1. **Is the repo mirror the RIGHT population, or only the available one?** `s294-D6` says
   "re-point at the store's new home"; the store's new home is unreachable from every seat that
   could run a grader, so what was re-pointed at is the repo's **record of what was placed**.
   23 files, 1 per wrap since #271. Whether that is the population the B3 review should read, or
   whether the review should instead be held from the conductor's seat against the store itself,
   is **his**. This lane declared the gap rather than deciding it.
2. **`AGING` cannot bite for another 23 days.** The population's oldest member is 6.9 days old
   against the ruled 30. The 30 was ruled on evidence from a 122-hook store where it moved 13
   entries; on a 23-file mirror it will move nothing until mid-October. **The value is his and is
   not touched** — but whether the aging arm means the same thing over this population is a
   question the re-point creates.
3. **17 of the 23 mirror hooks are starred** (`★★`/`⛔` in their index headline) against 11
   before the headline matcher was widened, and `s179-D1`'s alert rule is starred-only. The boot
   surface went from 2 lines to 4 and its measured cost from **112 real to 203 real**, both
   figures taken at this seat by the surface's own measurement. **Whether "starred"
   still selects anything on a population where nearly everything is starred** is a surface
   question, not a code one.
4. **The push credential's SCOPE is still only claimed, never verified.** `s294-D5` narrows it
   and the expiry is now checked mechanically, but #133 said on the record that the token
   actually in force is **broader than the ruled shape**, and nothing in this repo can read a
   token's scope. The gate can prove the DATE and can never prove the SCOPE. Only the re-issue
   closes that, and it is his hands.
5. **`283/WRAP-MEMORY-HOOK.md` has an index-line section with no index line in it.** Graded from
   its H1 and DECLARED. Whether the wrap ritual should assert its own hook's index line is
   present before the file is filed is ruling-shaped, and it is the [[instrument-without-a-
   consumer]] shape one step earlier.
6. **`s241-D1` shrink-only versus the measurement.** `s294-D7` settled that we may KNOW; it did
   not settle what to do with the knowing. Boot measured **74,656 real** today against a
   shrink-only ceiling of **70,000**, with 7 post-diet readings already over it, and the memory
   term's fall of 3,057 does not close a 4,656 gap. **That arithmetic is his.**

---

## ⚙ THE THREE MACHINE-READ LINES — ADDED BY LANE C2 AT STAGING, AND SAID SO

⚠ **Lane G filed this report before `s294-D2` was wired, so it carried none of the three lines
`s218-D7` requires.** Lane C2, which stages it, added them rather than exempting the file. The
figures are **C2's count of THIS report's own text**, derived and shown, never invented:
**findings 15** = the brief's env-file premise disproved (the credential is in
`git config remote.origin.url`) · the expiry date's one home, the W-24 stamp at #188 · no unnegated
`paste` instruction survives in the script · the seven-consumer sweep of the dead memory path · the
index line written in five heading shapes (literal matcher 8 of 23, widened 23 of 23) · probing
store filenames graded 19 of 23 falsely STALE · a source change read as a 73% shrink (23 against
52) · the old sidecar stored a per-session absolute mount path · `MEMORY.md` measured **0** and
`<user_memory>` 5,413 tape boots in its place (8,470 → 5,413) · boot first turn **74,656 real**,
reconciling exactly with `_checkin.py` · no code reads 8,470 (comment at `:161` plus the runtime
`fmethod` string) · `systemPrompt` 26,984 tape / attachments 6,719 / residual ≈40,953 by
subtraction · `P-293-1` lives in the hand-authored `_parked.json` with no write API · `AGING 0` is
structural (oldest mirror 6.9d against a 30d limit) · 17 of 23 mirror hooks starred, boot surface
2 → 4 lines and 112 → 203 real.
**ruling-shaped 6** = the six numbered items under the heading above, as the lane numbered them.
**UNPROVEN 1** = `--push` end to end is unreachable at this seat (no credential in the sandbox's
remote URL, so the push path refuses at the credential gate and never reaches the expiry gate; the
gate's LOGIC is bitten through `pat_expiry_verdict` and its POSITION by line order instead). The
five "diffs owed in files this lane does not own" are declared hand-offs with the exact edit
written out, not unproven claims, and are not counted as such.

COUNTS: findings 15 · ruling-shaped 6 · UNPROVEN 1

REPLAY-THESE: `python3 knowledge/_gardener.py --selftest-grades` (32 arms) · `python3 knowledge/_gardener.py --selftest` (7 arms) · `bash knowledge/_git_commit.sh --selftest` (28 bites, 14 new) · `APOLLO_FAKE_TODAY=2026-11-07 bash knowledge/_git_commit.sh --selftest` (the expiry boundary) · `python3 knowledge/_gauge_tokens.py --selftest` (arm E) · `python3 knowledge/_parked.py --check`
