# `#236`-`P` — the programme plan built: "The designer's brain", HTML v1

session: `#236` · 2026-09-02
window: lane P (build sub; siblings R1 = principles survey, R2 = SDLC playbook)
sub index: `P`
brief: `notes/_briefs/2026-09-02-236-P-plan-build-brief.md`
tokens: `UNMEASURED — no message.usage at a sub's seat`. SHAPE: 28 tool calls — **14 bash**
(1 file listing · 4 data extracts off the two lanes' JSON assets · 4 render-environment probes ·
4 renders · 1 final tree verify) · **3 markdown reads** (the brief + both research reports, whole) ·
**3 PNG read-backs** (the renders, seen by eye) · **1 skill invoke** (`swiss-design-system`) ·
**2 writes** (the page, this report) · **5 edits** (4 to the page, 1 to this line). **0 git
commands. No repo write outside the page, this report and its assets directory** — the render
environment lives under `outputs/`, which `.gitignore:67` covers.

## VERDICT

**DONE.** One self-contained HTML file at `_PLAN-designers-brain-2026-09-02-v1.html` (84,593
bytes), sections 0–10 in the brief's order, each opening with its one-line answer. **Rendered and
SEEN** — two PNGs, light and dark, read back by eye from the delivered file. Every figure was
counted off the lanes' JSON assets rather than retyped from the brief, and that discipline earned
its keep twice: it confirmed R1's grade line exactly (`A 6 · B 28 · C 75 · D 9 · L 27`, 145 rows,
38 `grade_alt`), and a DOM probe on the rendered page caught **my own** drifted tally in the
licence-tier prose before it shipped. Nothing on the page is ruled: every recommendation is
attributed ("R1 recommends…", "R2 recommends…"), R1's Q4 is shown as carrying *no* recommendation,
and the twelve open questions are collected in section 8 with their options and receipts.

COUNTS: findings `4` · ruling-shaped `0` · UNPROVEN `2`

## What was done

- **Sources read whole**: `notes/_subreports/2026-09-02-236-R1-principles-survey.md` (36,896 B) and
  `notes/_subreports/2026-09-02-236-R2-sdlc-playbook.md` (38,007 B). Figures then **counted off**
  `assets/2026-09-02-236-R1-principles-survey/{source-register,principle-register,tensions}.json`
  and `assets/2026-09-02-236-R2-sdlc-playbook/borrow-matrix.json` with `python3 -c`, never read
  from the brief's pointer counts. `W-347` / `W-348` titles and homes pulled from
  `knowledge/_state.json` for the receipts section.
- **Skill**: `anthropic-skills:swiss-design-system` invoked before a line of HTML was written and
  followed — label pattern, 1/3 : 2/3 dimension splits, hairline section rules, decorative index
  numerals, tables with rules and no chrome, no radius / shadow / decorative border, white space
  left load-bearing. Project accent substituted per the system's own instruction: **`s151-D1`, the
  two-red law** — `#DA1A00` on white (5.09:1) and `#F6604C` on dark (6.0:1), mono, no second hue.
- **Page written**: `_PLAN-designers-brain-2026-09-02-v1.html` at repo root, beside
  `_REVIEW-B3-grades-2026-08-15-v1.html`. Sections 0–10; 10 tables; the dependency map as inline
  SVG with `<title>`/`<desc>` and CSS-variable strokes so it inverts with the theme; no external
  script, no external font, no network at view time; light and dark both defined on `:root` and
  under `prefers-color-scheme`.
- **Render-check** per `knowledge/_RUNBOOK-render-verify.md`, mount-side, `TMPDIR=/dev/shm`, all
  exports in the same bash call as the render, `launch()` with no `env=`. Two PNGs saved to
  `notes/_subreports/assets/2026-09-02-236-P-plan-build/` and **read back by eye**.

## Findings

1. **The seventh stratum's install half does NOT run at this seat; the mount-side env from `#229`
   does.** `pip install playwright` succeeded, then
   `python3 -m playwright install chromium-headless-shell` failed:
   `Error: Failed to download Chrome Headless Shell 151.0.7922.34 … Download failure, code=1` —
   while the network is fine (`curl` to `cdn.playwright.dev` → `307`, `example.com` → `200`) and the
   disk is fine (`df -h /` → **56%**). What worked, first try, was the durable mount half the
   runbook itself predicts: `outputs/_render-env-229/` holds `pylibs` (playwright 1.62),
   `pw-browsers/chromium_headless_shell-1234`, `chromelibs/usr/lib/aarch64-linux-gnu`, `fonts/`,
   `fccache/` and `fonts.conf`. `ldd headless_shell | grep "not found"` → **only
   `libXdamage.so.1`**, which `chromelibs` supplies. **This corroborates the runbook's class rule
   ("THE DURABLE HALF IS THE MOUNT") and adds a datum it does not have: at `#236` the *download*
   half of the seventh stratum is dead, so the recipe as written would have produced a false
   "cannot render".**
2. **R1's counts survive a recount off the file.** `principle-register.json` → 145 rows,
   `Counter(grade)` = `{'A':6,'B':28,'C':75,'D':9,'L':27}`, `grade_alt` non-null on **38** rows;
   `source-register.json` → 32 families; `tensions.json` → 30; `borrow-matrix.json` `totals` →
   `M 1/9/2 · D 3/8/1 · P 2/4/6`. Every one matches the reports' own COUNTS lines. No drift.
3. **R1's question 5 tiers only half the register, and the European Accessibility Act is in the
   untiered half.** Q5 names *safe now* — GOV.UK, W3C/WCAG/APG/COGA, EN 301 549, FCA, DSA, EDPB and
   every peer-reviewed primary — and *pointer-only* — Laws of UX, NN/g, Baymard. Counted against the
   32 families that is **16 safe-now, 3 pointer-only, 13 not named at all**. The EAA is legal-grade
   and is *not* named, so the page marks it `not tiered` rather than inferring. **P2's membership
   rule, as it stands, cannot decide 13 of 32 families** — surfaced on the page in §2.2, not
   resolved there.
4. **A DOM probe on the rendered page caught a retyped count in my own prose.** After marking the
   EAA untiered I wrote "15 safe-now … 14 not tiered" by hand; the probe
   (`querySelectorAll('#s2 … .chip')` tallied by label) returned
   `{"Safe now":16,"Pointer-only":3,"not tiered":13}`. Corrected and re-rendered, with an assertion
   in the render script that the prose string is present in the DOM. **The pitfall the page itself
   lists at §5 row 11's neighbour — "retyped counts drift" — fired inside the lane that wrote it.**

## RULING-SHAPED QUESTIONS

**None.** Nothing in this lane needed a decision that was not already ruled. The lane's fence was
explicit — no ruling, no re-wording of any `s2xx-D*`, no KG writes, no new schema — and the page
describes the schema the research *proposes*, attributing every recommendation to its report. The
twelve genuinely open questions are R1's and R2's, reproduced verbatim-in-substance in the page's
section 8; they are not this lane's to add to.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** *That the page reads correctly at every breakpoint.* Two viewports were driven —
  1440×1100 (light) and 390×844 — and both returned
  `document.documentElement.scrollWidth > clientWidth` = **false**, so nothing overflows the body at
  either. Tablet widths, and the *appearance* at 390px, were not screenshotted. Price to prove: one
  render pass, ~3 screenshots.
- **CLAIMED:** *That the two contrast figures quoted on the page (`#DA1A00` 5.09:1 on white,
  `#F6604C` 6.0:1 on `#121212`) are correct.* They were computed in-lane from the sRGB relative-
  luminance formula, not read back from a contrast gate, and the second is against this page's own
  dark background rather than any Apollo token. The `s151-D1` two-red pairing itself is quoted from
  the ruling, not recomputed. Re-read costs: one run of the repo's contrast helper.

## Evidence

`notes/_subreports/assets/2026-09-02-236-P-plan-build/`

- **`01-top-light.png`** (175,964 B) — the masthead through the first two of the three answer lines,
  light scheme, 1440×1100. Proves: the page renders, the nav carries all eleven section landmarks,
  the "NOTHING ON THIS PAGE IS RULED" notice sits above the answer, and the accent appears exactly
  where the Swiss system allows it (label, notice rule, nav hover) and nowhere else.
- **`02-dependency-map-dark.png`** (91,587 B) — section 4's figure, **dark scheme**, element-clipped.
  Proves: the inline SVG inverts correctly (`body` background `rgb(18,18,18)`, ink
  `rgb(237,237,237)`, accent `rgb(246,96,76)` — all read from `getComputedStyle` in the same run),
  the P0 gate reads as the accent-stroked gate, the P1→P4 chain and the two dashed parallel tracks
  are legible, and the external-input band is readable at 9.5–10.5px in dark.

Both PNGs were **read back by eye** in this lane, from the delivered file, after the final edit —
not inferred from a "RENDER OK" banner.

REPLAY-THESE: `_PLAN-designers-brain-2026-09-02-v1.html` §8, the twelve ruling-shaped questions with options and each report's recommendation (~1,900 tk — this is the only section that asks Dave for anything) · this report's Finding 1, the render-environment datum, before any later lane trusts the runbook's seventh-stratum install recipe (~250 tk) · this report's Finding 3, the 16/3/13 licence tally, before P2's membership rule is written (~200 tk)
