# `#251`-`V-dp08` — cold verification of the DP-08 A+B2 canon build: every number re-driven, three constructs fired at the gate

session: `#251` · 2026-09-06
window: `Opus 5 ADVERSARIAL VERIFIER, cold (MODEL-ROUTING rule 5 / rule 7 ii)`
sub index: `V-dp08`
brief: chat brief from the conductor (verify `notes/_briefs/2026-09-06-251-dp08-build-brief.md` against `notes/_subreports/2026-09-06-251-BUILD-dp08.md`)
tokens: `UNMEASURED — a subagent cannot read message.usage; harness counter 15,000,000 → 14,884,101 ≈ 116K QUOTA across 20 tool calls (5 PNG reads included)`

## VERDICT

**The build lane's report is honest.** I re-drove every mechanical claim from scratch — both gates, all
three mutation modes, six renders of the canon file at 1440/1100/820 light+dark with my own probe
script, the anchor drive, the greps, the currency census and the `consumes`/payload correspondence by
hand — and **every geometric and byte figure in the build report reproduces EXACTLY**: 14,869 code-only
of 34,816, gate exit 0, `--check OK`, tiles 4/4/4 with `maxWdiff 0` / `lastRowVoid 0` / one row at all
six renders, Needs-attention `{976, 516.5, 432, 295.3}` at 1440, the 1100 two-up at identical `295.3`
heights, 0 page errors on all six, `scrollY 0 → 235`, `activeElement dp08-na`, `tabindex="-1"`,
highlight present at 600ms and gone at 1400ms, reduced-motion never applying the class, DP-20 26/9 at
1440 and 20/6 at both narrow bands, and the three greps at 0/0/1. **Nothing was found overstated.**
**Every ruling in scope is met**: Undrawn is strip-only (1 rendered instance, in the strip, `s251-D1`),
the strip and card head carry `14 payments · 5 need you` byte-identically (`s251-D2`, and `5 of 14` is
0 hits), the lead group is a ROW at every band (`s247-D4`), no raggedness (`s249-D5`), the rail is
Needs-attention-then-Balances (`s248-D1` two-up intact), the behaviour is at a named address with the
meta as its one home and the snippet block generated (`s234-D5`), and `git diff` on
`knowledge/_validate_behaviour.py` and `knowledge/canon/canon.css` is **empty** (`s250-D1`, caps
UNMOVED). The lane's declared `_rulings.json` dirt is exactly the two `s251-D1`/`D2` inscriptions and
carries no build-lane content — the claim that it predates the lane is consistent with what is in it.
**Two of the lane's UNPROVEN entries I CLOSED**: 820 legibility is now **PROVEN** — I read the tile row
at 820 in both themes and the strip at 820, and every glyph is complete (`£184,205.10` / `£71,880.00` /
`£62,410.00` / `£3,204.80`, deltas, sparklines), plus a machine overflow probe returns **0 overflowing
and 0 clipped elements at all six renders**; and **keyboard arrival is PROVEN** — 12 `Tab` presses
reach the chip, it paints a real `2px solid rgb(48, 90, 133)` focus ring, and `Enter` produces the same
arrival as the click. **The currency verdict is the one place I go further than the lane**: the mix is
not a footnote, it is visible in a single glance — the 820 full-page render shows the strip's
`Undrawn facilities $120.0m` sitting one row above four `£` KPIs on an account page whose own subtitle
reads `Business current account · 40-12-06 · ending 8841`. Seven `&pound;` against two `$`, and the two
`$` are precisely the lane's two additions. **The `Available overdraft` → `Undrawn facilities` mapping
is LEGITIMATE as a mapping and ILLEGITIMATE as a paste**: the fact is the same fact, the strings are
not. Three adversarial constructs fired: two were caught RED by `gen_component_partials.py --check`;
**the third — removing `id="dp08-na"` and clicking the chip — is NOT caught by any gate**, and the page
degrades silently, which is a NEW unguarded invariant.

COUNTS: findings `14` · ruling-shaped `1` · UNPROVEN `3`

## The table

| # | verdict | claim under test | evidence |
|---|---|---|---|
| 1 | **CONFIRMED** | `_validate_behaviour.py` exit 0; `Template-dashboard-bento 14869 consumes dv-behaviour, dp08-anchor`; worst member Chart-donut 24,671 | re-ran, output pasted below |
| 2 | **CONFIRMED** | `gen_component_partials.py --check OK` | re-ran, exit 0 |
| 3 | **CONFIRMED** | three `--mutate` modes: code-pad RED, comment-pad GREEN, string-slash RED, each exit 0 | re-ran all three, tails pasted |
| 4 | **CONFIRMED** | tiles 4/4/4 at 1440/1100/820 × light+dark; `maxWdiff 0`, `lastRowVoid 0`, `rows 1 [4]`, `gap 4`, `h 150.5`; 0 page errors ×6 | my own probe, `assets/…-V-dp08-verifier/vr.py` + `vr.json` |
| 5 | **CONFIRMED** | tile x/w per width exactly as reported (32/341… · 32/256… · 32/186…) | same, all 24 boxes match to 0.1px |
| 6 | **CONFIRMED** | Needs-attention `{976,516.5,432,295.3}` / `{32,900.5,516,295.3}` / `{32,968.5,756,295.3}`; Balances 271.1 at 1440 and 820 | same |
| 7 | **CONFIRMED** | 1100 rail two-up: same `y 900.5`, same `w 516`, **same `h 295.3 / 295.3`** — `s248-D1`, no orphan | same; `ctxCols` = six 169.328px tracks |
| 8 | **CONFIRMED** | anchor drives: `0 → 235`, `activeElement dp08-na`, `hash #dp08-na`, `tabindex -1`, class True@600 False@1400, reduced-motion class never set, 0 errors | `drive.py`, my own run |
| 9 | **CONFIRMED** | `#p1` unreachable in canon: `[role="tabpanel"].length === 0` at every render; `grep -c '#p2'` = 0 | probe + grep |
| 10 | **CONFIRMED** | greps: `5 of 14` 0 · `#p2` 0 · `href="#dp08-na"` 1 · `id="dp08-na"` 1; in-page repeats `Awaiting approval ×2 · 14 payments ×2 · 5 need you ×2 · N-of-M ×0 · Undrawn ×1` | grep + `document.body.innerText` at all six |
| 11 | **CONFIRMED** | DP-20 above the fold: **26 elements / 9 carriers** at 1440, **20 / 6** at 1100 and 820, carrier list identical | `fb.py`, re-run census |
| 12 | **CONFIRMED** | caps and constants unmoved: `git diff knowledge/_validate_behaviour.py` and `git diff knowledge/canon/canon.css` both **empty**; `MAX_BYTES 16*1024`, `PAGE_BYTES 34*1024` in place | `git diff --stat` on both paths |
| 13 | **CONFIRMED** | `s234-D5`: meta gains a typed `behaviour` object (`script: null` positive, `partial` array, real `fallback`, `$note`), and the `#behaviour-manifest` block in the snippet is generator-derived beside `#token-manifest` | `git diff` on the meta + snippet lines 1305–1317 |
| 14 | **CONFIRMED** | `s251-D1` Undrawn strip-only: 4 file occurrences = 1 rendered `<span class="tpl-strip-plain">` in the strip + 3 comments; **0 inside the card** | `grep -n 'Undrawn facilities'` lines 822/917/923/1106 |
| 15 | **CONFIRMED** | rail order: Needs-attention (`id="dp08-na"`, line 1099) precedes Balances (line 1126); rendered `y 516.5` before `815.8` at 1440 | markup + render |
| 16 | **CONFIRMED** | byte-gate honesty (V-MUT-5): `consumes` = `["dv-behaviour","dp08-anchor"]` and the snippet carries **exactly two** AUTO-BEHAVIOUR blocks, each containing its source file **verbatim** (`dv-behaviour` src 19,679 B inside a 19,982 B block; `dp08-anchor` src 3,868 B inside 4,169 B). No third payload, no `<script src>` | hand byte-compare, script below |
| 17 | **CONFIRMED** | the lane's `_rulings.json` disclaimer: the 34-line diff is the two `s251` ruling objects and nothing else — no build content | `git diff knowledge/_rulings.json`, read in full |
| 18 | **REFINED** | lane: *"the page now carries `$120.0m` and `$2.7m` among `£` KPIs"* — TRUE, and **worse than stated**: the mix is co-visible in one viewport at every band, not merely present in the file | `full-820-light.png` read: strip `$120.0m` directly above `£184,205.10 / £71,880.00 / £62,410.00 / £3,204.80` |
| 19 | **REFINED** | lane's UNPROVEN *"820 tiles remain LEGIBLE"* → **PROVEN legible**, light and dark | `tiles-820-light.png`, `tiles-820-dark.png` read; overflow probe 0/0 at all six |
| 20 | **REFINED** | lane's UNPROVEN *"keyboard arrival"* → **PROVEN**: 12 Tabs to the chip, focus ring `2px solid rgb(48,90,133)`, `Enter` → `y 235`, `active dp08-na`, class True | `drive.py` keyboard leg |
| 21 | **NEW** | **The chip→card link is UNGUARDED.** Removing `id="dp08-na"` (leaving `data-dp08-target`) leaves the page *silently* broken — click gives `scrollY 0`, `activeElement BODY`, `arrivedAnywhere 0`, **0 page errors** — and **both gates stay exit 0**. `requires.declarations` checks the two `data-*` hooks, never that `href` resolves | ADV-A, `adva.py` |
| 22 | **NEW** | **The `--mutate` harness cannot bite `dp08-anchor`.** All three mutations hard-code `reg[...]['$behaviour']['dv-behaviour']`; the new source is never mutated, so the lane's "all three modes behaved" proves the gate's teeth on the OLD file only | `_validate_behaviour.py` lines 409–420, read |
| 23 | **NEW** | **The byte gate alone cannot detect a dishonest `consumes`.** Declaring `dv-legend` on the template with no payload injected still exits 0 and cheerfully charges the page **22,603** bytes; only `gen --check` goes RED (2 failures). The honesty invariant lives entirely in the partial generator | ADV-B |
| 24 | **NEW** | **`--bento-cols-now:4` in page rule 10a is load-bearing and gate-invisible.** Set to 3, the row becomes **2 rows `[3,1]`** with a last-row void of **920 / 693.3 / 506.7 px** at 1440/1100/820 — a flat `s249-D5` and `s247-D4` breach that **no gate in the repo catches**; only a render probe does | ADV-C, `advc.py` |
| 25 | **NEW** | The snippet's DEMO-CHROME script carries an **un-debounced `addEventListener('resize', …)`** whose bytes are never charged to the page sum. Pre-existing (3 hits at HEAD), correctly excluded by the meta's `$note` — logged as the shape of the unguarded seam, **not** as this lane's defect | `git show HEAD:… | grep -c reportWidth` = 3 |
| 26 | **CONTRADICTED (minor)** | lane finding 5: `DRIVE normal BEFORE {'active': 'BODY'}` | at load `document.activeElement` is **`div.tpl-bento-body`**, not `BODY` — the lane's DRIVE reported `id||tagName`, so a classless read printed `BODY`. Cosmetic; the AFTER state, which is the claim that matters, is exact |

## The two gate runs, verbatim

```
  [PASS] dataviz/dv-behaviour — 13048 code-only bytes (12.7 KB of 16) · 19768 raw, 6720 comment/blank · 15 member(s)
  [PASS] dataviz/dv-legend — 7734 code-only bytes (7.6 KB of 16) · 15131 raw, 7397 comment/blank · 15 member(s)
  [PASS] dataviz/dv-donut-sweep — 3889 code-only bytes (3.8 KB of 16) · 5511 raw, 1622 comment/blank · 15 member(s)
  [PASS] dataviz/dp08-anchor — 1821 code-only bytes (1.8 KB of 16) · 3884 raw, 2063 comment/blank · 15 member(s)
  [PASS] dataviz page budget — worst member 24671 code-only bytes (24.1 KB of 34) across 4 source(s)
         Chart-donut                   24671  consumes dv-behaviour, dv-legend, dv-donut-sweep
         …
         Template-dashboard-bento      14869  consumes dv-behaviour, dp08-anchor
         Chart-boxplot                 13048  consumes dv-behaviour
Behaviour-contract gate OK — see knowledge/_BEHAVIOUR-GATE.md
EXIT=0

gen_component_partials --check OK — all AUTO-PARTIAL blocks in sync, contracts hold.
EXIT=0

MUTATION code-pad     : baseline code-only 13048 → mutated 17054 (+4006)  expected RED · got RED — OK   EXIT=0
MUTATION comment-pad  : baseline code-only 13048 → mutated 13048 (+0)     expected GREEN · got GREEN — OK EXIT=0
MUTATION string-slash : baseline code-only 13048 → mutated 17057 (+4009)  expected RED · got RED — OK   EXIT=0
```

## My six renders (independent script, canon file, `file://`)

```
== 1440-light colsNow 4 rows 1 [4] maxWdiff 0 lastRowVoid 0 gap 4 err 0
   tiles [('Closing balance',32,341,150.5),('Money in',377,341,150.5),('Money out',722,341,150.5),('Card spend',1067,341,150.5)]
   NA {'x':976,'y':516.5,'w':432,'h':295.3}  strip {'x':0,'y':277,'w':1440,'h':24}  ctxCols 432px
   rail [('Needs attention',516.5,432,295.3),('Balances',815.8,432,271.1)]
   OVERFLOW 0 []   CLIPPED 0 []   amtFont ['24px','24px','24px','24px']  page_h 1135
== 1100-light colsNow 4 rows 1 [4] maxWdiff 0 lastRowVoid 0 gap 4 err 0
   tiles [(…,32,256,150.5),(…,292,256,150.5),(…,552,256,150.5),(…,812,256,150.5)]
   rail [('Needs attention',900.5,516,295.3),('Balances',900.5,516,295.3)]   ← two-up, EQUAL height
   OVERFLOW 0   CLIPPED 0   page_h 1244
== 820-light colsNow 4 rows 1 [4] maxWdiff 0 lastRowVoid 0 gap 4 err 0
   tiles [(…,32,186,150.5),(…,222,186,150.5),(…,412,186,150.5),(…,602,186,150.5)]
   NA {'x':32,'y':968.5,'w':756,'h':295.3}  strip {'x':0,'y':345,'w':820,'h':24}  ctxCols 376px 376px
   OVERFLOW 0   CLIPPED 0   page_h 1587
```
Dark is byte-for-byte identical geometry at all three widths. Repeats at every render:
`{'aw':2,'p14':2,'n5':2,'NofM':0,'und':1}`; `panels: 0`.

## The anchor, driven by me

```
== normal BEFORE {'y':0,'active':'tpl-bento-body','hash':'','tabindex':None,'arrived':False,'naTop':517}
   t~150  {'y':215,'active':'dp08-na','hash':'#dp08-na','tabindex':'-1','arrived':True,  'naTop':302}
   t~600  {'y':235,'active':'dp08-na','hash':'#dp08-na','tabindex':'-1','arrived':True,  'naTop':282}
   t~1400 {'y':235,'active':'dp08-na','hash':'#dp08-na','tabindex':'-1','arrived':False, 'naTop':282}
   t~2400 {'y':235,…,'arrived':False}                                          errors []
== reduce  every sample: arrived False, y 235, active dp08-na, tabindex -1   errors []
== KEYBOARD tabs to reach chip: 12  reached True
   focus ring on chip {'outline':'2px solid rgb(48, 90, 133)','boxShadow':'none'}
   AFTER Enter {'y':235,'active':'dp08-na','hash':'#dp08-na','tabindex':'-1','arrived':True}
== HASH-NAV (deep link #dp08-na, js on) scrollY 235  active dp08-na
```
Note the mid-flight sample at 150ms (`y 215`, `naTop 302`): the scroll is genuinely smooth under
normal motion and genuinely instant under `reduce` (`y 235` already at 150ms). That is the
reduced-motion branch working, not merely the class being withheld.

## The three adversarial constructs

**ADV-A — remove `id="dp08-na"` from the card, keep `data-dp08-target`, click the chip.**
Result: `{'y':0,'active':'BODY','hash':'#dp08-na','naById':False,'arrivedAnywhere':0}`, **0 page
errors**, `gen --check` exit 0, `_validate_behaviour.py` exit 0. **NOT CAUGHT.** The behaviour fails
gracefully (no throw) and the browser's own hash jump has nothing to jump to, so the strip chip becomes
a dead control with no signal anywhere in the toolchain. This is the correspondence `s251-D1` actually
rules on — "strip items point at their detail" — and it is the one part of the build with no probe.
Reverted.

**ADV-B — declare `dv-legend` in the template's `consumes` with no payload injected.**
```
gen_component_partials CONTRACT FAILURES:
  X Template-dashboard-bento: required declaration 'class="dv-legrow' missing
  X dataviz/dv-legend: Template-dashboard-bento is a member but carries no AUTO-BEHAVIOUR markers
genEXIT=1
```
**CAUGHT** — but only by the generator. `_validate_behaviour.py` stayed **exit 0** and summed the
member page at **22,603**, i.e. the byte gate will charge for a partial that is not on the page. The
lane's declaration is honest (finding 16 verifies it by hand), and both guards were run. Reverted.

**ADV-C — set page rule 10a to `--bento-cols-now:3`.**
```
1440 {'rows':2,'rowSizes':[3,1],'lastRowVoid':920.0,  'cols':'456px 456px 456px'}
1100 {'rows':2,'rowSizes':[3,1],'lastRowVoid':693.3,  'cols':'342.656px ×3'}
 820 {'rows':2,'rowSizes':[3,1],'lastRowVoid':506.7,  'cols':'249.328px ×3'}
```
**VISIBLY WRONG, CAUGHT BY NO GATE.** Two rows with a half-empty last row is exactly `s249-D5`'s
raggedness and exactly the not-a-ROW `s247-D4` forbids, and the repo's static gates are all green
through it. `[[no-gate-parses-the-artefact]]` again, at a new address.

**ADV-D (bonus) — gut the injected `dp08-anchor` payload, keep `consumes`.**
`gen_component_partials --check: AUTO-PARTIAL blocks OUT OF SYNC: Template-dashboard-bento
(dp08-anchor)`, exit 1. **CAUGHT.**

All four edits reverted; `md5sum -c` on the snippet and the registry both `OK`, and the closing
`git status --short` / `git diff --stat` are byte-identical to the lane's (7 modified, 272 insertions,
49 deletions).

## CURRENCY — the verdict asked for

**The mix is real, it is the lane's own doing, and it is worse on screen than in the file.**

| symbol | count in file | where |
|---|---|---|
| `&pound;` | 7 | all four KPI tile amounts (`£184,205.10 · £71,880.00 · £62,410.00 · £3,204.80`) + three Balances rows (`£184,205.10 · £310,000.00 · £3,204.80`) |
| `$` (money) | 2 | strip `Undrawn facilities $120.0m · 40% drawn · reviewed 1 Sep` (line 923) · card head `$2.7m` (line 1109) |
| `€` | 0 | — |

At HEAD the file had **0** `£`-symbol conflicts and **2** `$` occurrences, both in prose/JSON, none in
money. The lane added the only two dollar amounts on the page. I read `full-820-light.png`: the strip's
`$120.0m` sits **one row above** four sterling tiles, on a page headed *"Business current account ·
40-12-06 · ending 8841"* — a UK sort code. There is no reading of that page on which the two currencies
are the same account.

**Was mapping `Available overdraft` → "Undrawn facilities" legitimate?** *The mapping, yes. The paste,
no — and the two must not be conflated.*
- **Legitimate as a mapping.** `s247-D3` names "the two status tiles in the headline row (Awaiting
  approval · Undrawn facilities — the W1 Q2 tiles)" and *governs*
  `knowledge/snippets/Template-dashboard-bento.reference.html`. A ruling that both names a tile and
  names this file as governed is asserting the correspondence itself: canon's `Available overdraft ·
  None used · Limit reviewed 4 Aug` **is** the tile Dave called Undrawn facilities. An undrawn facility
  with a review date is one fact in two vocabularies. Removing it from the headline row is precisely
  what `s247-D3` + the brief's region 2 instruct.
- **Not legitimate as a paste.** Nothing in `s251-D1`, `s251-D2` or the brief rules the *amounts*.
  `s251-D2` inscribes a currency-free string (`14 payments · 5 need you`) and rules nothing about
  `$2.7m`; the brief's `$120.0m / 40% drawn / reviewed 1 Sep` was written against the probe's corporate
  treasury page, not against canon. Carrying the *shape* was ruled; carrying the *figures* was a
  transcription that invented a fact — canon's overdraft is `None used`, and `40% drawn` asserts the
  opposite. **`40% drawn` is the invented fact, not the currency symbol**: the currency is a mismatch,
  the drawdown percentage is a contradiction of the datum it replaced.
- The lane's option (b) — re-word to canon's own data, keeping every ruled string — costs one edit and
  no re-render (I measured the strip at `h 24` with no wrap at all three widths, with 116px of slack at
  820 before the `Undrawn` span would reach the wall). **I do not rule it**; RSQ 1 below carries it to
  Dave with the extra fact the lane did not have.

## 820 LEGIBILITY — the verdict asked for

**PROVEN LEGIBLE, both themes.** The probe left it UNPROVEN and the build lane inherited that; I closed
it two ways.
1. *Machine.* An overflow probe over every text-bearing descendant of the four lead tiles and the strip
   (`scrollWidth > clientWidth + 1`) returns **0** at all six renders, and a clipping probe (any child
   rect exceeding its tile rect by >0.6px) returns **0** at all six. Amount font-size is `24px` on all
   four tiles at 820, unchanged from 1440 — the tiles do not down-scale their figures, they simply hold
   a 186px column, and `£184,205.10` fits it.
2. *Eye.* I read `tiles-820-light.png` and `tiles-820-dark.png`. Every label, amount, delta arrow,
   percentage, `vs July` legal and sparkline is complete and unambiguous; no ellipsis, no clipped
   glyph, no baseline collision, and the dark leg keeps the same green/red delta ink at legible
   contrast. `strip-820-light.png` shows the strip on ONE line at `h 24`, both chips and the plain
   Undrawn span intact behind their divider.
The only reservation I record, and it is a design note not a defect: the `Undrawn facilities` plain span
is the palest text on the strip at 820, deliberately (it is `t-cm-legal` with no chip, by `s251-D1`'s
"state, not an action"). It is legible; it is also the first thing that would go if the strip ever had
to wrap.

## RULING-SHAPED QUESTIONS

1. **The strip's Undrawn figures: keep the brief's `$120.0m · 40% drawn`, or re-cut to canon's data?**
   Beyond the lane's own RSQ 1, I add one fact it did not carry: `40% drawn` does not merely change
   currency, it **contradicts** the datum it replaced (`None used`). So option (b) is not only the
   cheaper edit, it is the only one that leaves the page internally true without also re-cutting the
   four KPI tiles, the three Balances rows and the account subtitle to a dollar treasury persona.
   Options unchanged: (a) keep the brief's strings and schedule a whole-page content re-cut;
   (b) re-word to canon (`Undrawn overdraft £25,000 · none used · reviewed 4 Aug`, card head `£9,840.55`)
   — every ruled string survives, geometry unaffected (measured); (c) something else. **No
   recommendation** — this is Dave's product-content call, and the brief did name the strings.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** the meta's `fallback` claim for the no-JS leg — *"the strip chip is still a real in-page
  link to a real id, so the browser jumps unaided"*. I proved the **deep-link** half (`#dp08-na` in the
  URL with JS on lands at `y 235`, `active dp08-na`), but my JavaScript-disabled click leg did not
  report before I closed the browser and I did not re-run it. Price: one Playwright context with
  `java_script_enabled=False` (~1.5K).
- **UNPROVEN:** the visual quality of the arrival highlight, unchanged from the lane. I proved the class
  arrives, animates and leaves and that the keyframe is a `box-shadow` wash inside the card's box, and I
  read five PNGs — none of them mid-animation. Price: two 1440 screenshots at ~300ms after the click,
  light and dark (~4K).
- **UNPROVEN:** that no other gate in the repo objects. Like the lane, I ran only the two the brief
  named. `_validate_composition.py` (C9 / band clamps), `_validate_receipt.py`, the doc-row and
  blast-radius gates have still not seen page rule 10a, the `behaviour` meta object or the
  `#behaviour-manifest` block, and `_build_all.py` is forbidden here too. Price: ~10K to run the three
  individually. ⚠ This is the largest remaining hole and it is the same hole the lane declared — a
  verifier that runs the same two gates has not widened the net, only re-driven it.
- **CLAIMED:** that the `_rulings.json` / `_REHEARSAL-LOG.jsonl` / `_GRADE-DECISIONS.jsonl` dirt
  predates the build lane. I verified the **content** is consistent with that (the ruling diff is two
  `s251` objects and nothing else) but I cannot verify the **timing** — no working-tree timestamp
  survives into my window. Price: unrecoverable from here; the conductor's own record settles it.

## Evidence

`notes/_subreports/assets/2026-09-06-251-V-dp08-verifier/` —
- `vr.py` / `vr.json` — my independent six-render probe (its own PROBE JS, not the lane's), carrying the
  overflow and clipping probes the lane never ran.
- `drive.py` — click drive at four timestamps × normal/reduce, plus the keyboard `Tab`×12 + `Enter` leg.
- `advc.py` / `adva.py` — the ADV-C 3-column and ADV-A id-removal constructs, as run.
- `fb.py` — DP-20 carrier census at all three widths + the deep-link leg.
- `tiles-820-light.png` · `tiles-820-dark.png` · `strip-820-light.png` · `full-820-light.png` ·
  `tiles-1440-light.png` — the five images actually READ for the legibility and currency verdicts.

Playwright bootstrap in this window: `~/.cache/ms-playwright/chromium_headless_shell-1234` survived;
`libxdamage1` re-fetched (`apt-get download` → `dpkg-deb -x` → `LD_LIBRARY_PATH=/tmp/deps/root/usr/lib/aarch64-linux-gnu`), 1 tool call, no root.

### Closing tree state (identical to the lane's)

```
 knowledge/_BEHAVIOUR-GATE.md                       |   5 +-
 knowledge/_rulings.json                            |  34 ++-
 knowledge/component-types.json                     |  16 +-
 .../components/template-dashboard-bento.meta.json  |   9 +
 .../Template-dashboard-bento.reference.html        | 255 +++++++++++++++++----
 notes/_REHEARSAL-LOG.jsonl                         |   1 +
 notes/_dream/_GRADE-DECISIONS.jsonl                |   1 +
 7 files changed, 272 insertions(+), 49 deletions(-)
```
Nothing built, nothing fixed, nothing ruled, nothing committed. The only paths this lane added are its
own report and its own evidence directory.

CITES: `s251-D1` · `s251-D2` · `s247-D3` · `s247-D4` · `s248-D1` · `s249-D2` · `s249-D5` · `s250-D1` · `s234-D5` · ADR-0015 A2/A3 · ADR-0016 · DP-20 · `[[no-gate-parses-the-artefact]]`

REPLAY-THESE: `notes/_subreports/assets/2026-09-06-251-V-dp08-verifier/vr.json` (~4K tk — every geometry number above) · `notes/_subreports/assets/2026-09-06-251-V-dp08-verifier/full-820-light.png` (~2K tk — the currency verdict in one image) · `python3 knowledge/_validate_behaviour.py` + `python3 knowledge/gen_component_partials.py --check` (~1K tk, 2 calls, both exit 0)
