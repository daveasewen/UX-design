# #268 cold run 8 — the RELEASED v1.0.12 zip, cold-run-7's ten assertions plus a DESCENDER assertion per theme

**Model: Opus (per #250). Dated period record (ADR-0017). Read-only against `knowledge/`,
`outputs/coldrun-267/`. Nothing built, moved or ruled there. No `_build_all.py`, no
`gen_kg_edges.py`, no checkout, no stash, no push.**

**SAME SHAPE AS C7, ONE ASSERTION WIDER.** C8 does not re-build either. It re-drives C6's frozen
arm page against the **released v1.0.12 canon**, so the only variable between C7 and C8 is the
file the release was cut for. There is no score, because nothing was built. The new arm is the
descender assertion C7's finding 2 asked for, and it is run **once per theme** rather than once,
which is what made it bite.

## Paths

- pack — `apollo-spider/dist/Apollo-Spider-v1.0.12.zip`
  · sha256 `14b51e88ecab22c00f1cb7a51e56f9066bc426b9736c8d16b13fc6322a9d6764`
  · 21,202,053 bytes · unzipped to `/var/tmp/cr8/Apollo-Spider-v1.0.12`
- arm — a COPY of `outputs/coldrun-267/arm-A-blind/` at `/var/tmp/cr8/arm`, with
  `canon/canon.css` + `canon/type.css` replaced by the **v1.0.12 zip's**. The repo copy was not
  touched; `git status outputs/coldrun-267/` is clean.
- driver — `/var/tmp/cr8/drive8.py`, served over `http://127.0.0.1:8712` (not `file://`),
  chromium via `knowledge/_render/seat_env.sh` (`SEAT_ENV: OK seat=elegant-vibrant-davinci`,
  faces 10/404, farm 10/10)
- screenshots — `notes/_subreports/assets/2026-09-11-268-cold-run-8-dropdown-open.png` and
  `…-nav-{mono,legacy,console,supercharge}.png` (the sidebar, per theme, AS SHIPPED), plus
  `…-nav-mono-AFTER-FIX.png` (the same crop with the tree's post-pin canon.css)
- this report — `notes/_subreports/2026-09-11-268-cold-run-8.md`

## Assertions — 10 / 14 PASS at 1440

| # | assertion | verdict | detail |
|---|---|---|---|
| 1 | `a.nv-item` exists | PASS | present |
| 2 | `.nv-item` matched by canon rules | PASS | **53 rules** mention `.nv-item` |
| 3 | `.nv-item` display is not `inline` | PASS | `flex` |
| 4 | `.nv-item` has padding | PASS | `0px 16px` |
| 5 | `.nv-item` underline removed | PASS | `none` |
| 6 | `.nv-item` height ≥ 24px (s262-D2 floor) | PASS | 44.0px |
| 7 | entity dropdown opens on click | PASS (soft) | `aria-expanded=true`, menu in the box tree |
| 8 | currency dropdown opens on click | PASS (soft) | ditto |
| 9 | 0 pageerrors | PASS | `[]` |
| 10 | 0 console errors | PASS | `[]` |
| 11 | `.nv-label` descenders intact — **mono** | **FAIL** | 8 labels, edge `cap alphabetic`, box 12px, worst clip **5.50px** |
| 12 | `.nv-label` descenders intact — **legacy** | **FAIL** | identical numbers |
| 13 | `.nv-label` descenders intact — **console** | **FAIL** | identical numbers |
| 14 | `.nv-label` descenders intact — **supercharge** | **FAIL** | identical numbers |

⚠ Assertions 7 and 8 stay **SOFT GREEN**, exactly as C7 named them: playwright's `is_visible()`
ignores `opacity`, and C7's finding 1 (the arm's own script sets `aria-expanded` but not
`data-open`, so the menu paints at `opacity:0`) was **not fixed** — it is the arm's defect, not
the pack's, and C8 did not touch the arm. Nothing here re-confirms that the menus are *seen*.

## ⛔ F1 — THE DESCENDER FIX IN v1.0.12 IS REAL BUT NARROWER THAN THE CLAUSE. The labels still clip

`94bcc8a` fixed the base leading-trim default in `canon.css` — `:where(.canon) :is(button,a,…,
input[type=text],…):not(:has(svg))` (0,1,2) became `:where(…)` (0,0,1) — and that fix is genuinely
in the shipped zip (`grep -c ":where(.canon) :where(button"` = 1). It was mutation-tested before
the bake, both ways, 4 themes × 2 modes × 6 labels: shipped edge `text`, box 21px, 0/48 clipped;
the pre-fix cascade re-injected, edge `cap alphabetic`, box 12px, 48/48 clipped.

**The test page was narrower than the clause.** It carried `.canon` and `.cn-sidebar-nav` and
nothing else. A real page carries more: the cold-run arm's `<body>` wears
`cn-template-dashboard-bento`. Reading the matched rules off the live page, in order:

    :where(.canon) :where(button,a,…)                                (0,0,1)  ← fixed in 94bcc8a
    :where(.cn-sidebar-nav) :where(button, :where(.cn-sidebar-nav) a, …)
                                                                     (0,0,1)  ← fixed at #261
    :where(.cn-sidebar-nav) .nv-label {text-box-edge:text text}       (0,1,0)  ← the ds-005 override
    :where(.cn-template-dashboard-bento) :is(button, …, input[type=text], …)
                                                                     (0,1,2)  ← STILL :is(). WINS.

Every component absorbed into canon carries its **own private copy** of that default, and 108 of
them were still `:is(...)`. One of them matches the nav label two scopes away and outranks the
override, so `.nv-label` computes `cap alphabetic`, the box ends on the baseline at 12px, and the
label's own `overflow:hidden` cuts 5.50px off every g, y and p — **"Pavments", "Liauiditv",
"Settinas"**, exactly as C7 read them off v1.0.11. Visible in the four `…-nav-<theme>.png` shots.

**Same defect class, one level out.** [[conflated-fix-guarantees-recurrence]] in its purest form:
the fix was tested against a page built to contain the fix, not against the page the finding came
from.

## The remedy, driven — and where it is

Fixed at source in **`7ddab70`**, at the snippet (the seat `gen_canon_components` carries
verbatim, and the seat #261 used for the three nav snippets): `:is(` → `:where(` on that one list
in **108 reference snippets**, canon regenerated (137 components, 111 copies now `:where()`, 0
`:is()`), chart-engine receipts re-driven.

Re-driven on this same arm page with the tree's canon.css, all four themes:

| | computed `text-box-edge` | box height | worst clip | verdict |
|---|---|---|---|---|
| as shipped in v1.0.12 | `cap alphabetic` | 12px | **5.50px** | 4/4 FAIL |
| tree, post-`7ddab70` | **`text`** | **21px** | **0.00px** | **4/4 PASS** |

`_validate_descender_clip.py` is **GREEN END TO END** for the first time since #261 —
`✓ SPECIFICITY RATCHET AT ZERO` and `DESCENDER-CLIP GATE PASS — 153 file(s)`. It was RED when
this session opened (8 cascade-dead overrides against an allowance of 0, plus two hard reds in
`Filter-toolbar-bar` and `Kpi-tile`); all ten are gone.

⛔ **`7ddab70` IS NOT IN THE v1.0.12 ZIP.** v1.0.12 is baked, pinned and ledger-seeded; a recorded
zip is never re-baked at its own version (the same rule that produced this bump). The fix ships at
the next cut.

⬛ **DAVE'S.** The nav labels clip in the released v1.0.12 pack. The fix exists, is driven and is
committed at source. Whether that warrants a v1.0.13 before the demo, or rides to the next cut, is
his word (s219-D4(2)).

## Cold-run-7 → cold-run-8, in one line

**C7's ten assertions all still pass on v1.0.12, and `--check` is GREEN for the first time since
v1.0.9** — and the descender assertion C7 asked for, run per theme against a real page rather than
a synthetic one, says the crop is **not** closed in the shipped zip and **is** closed in the tree.

## NOT done (declared)

- **No blind build, no score, no second arm, no grill-me, no control arm.** C8 is a targeted
  re-drive of C6's frozen arm; the 2/3/3/2 scoreboard is not re-issued.
- **C7's finding 1 was not re-tested and not fixed** — the arm's script still omits `data-open`,
  so assertions 7 and 8 remain soft greens on a menu at `opacity:0`.
- **Only two dropdowns were driven.** The 19-control sweep, the persistence reload, both
  validators, the renders at 820, the receipt mint and the chart-completeness pass were not re-run.
- **Dark mode not re-driven.** Light only, at 1440. The descender arm ran all four THEMES in light
  mode; C7's 820 × 1000 re-run was not repeated.
- Nothing in the arm was fixed. F1 was found by the drive and left exactly as found.

## Sandbox

`/tmp/pw` survived; `source knowledge/_render/seat_env.sh` printed `SEAT_ENV: OK` first try and was
re-sourced in **every** bash call. `PLAYWRIGHT_BROWSERS_PATH=/tmp/pw` is additionally required for
`_drive_chart_engine.py`, which launches `channel="chromium"` (the full build) rather than the
headless shell the seat env points at. `python3 -m http.server` was backgrounded and killed inside
the same call each time. `/` hit 100% during the second dry-bake and the bake failed with
`OSError: [Errno 28]`; four stale `/var/tmp/full11*` and `dry11*` stages were removed (1.6G free)
and the bake re-run clean — the byte-equality claim below rests on the re-run, not the failed one.
