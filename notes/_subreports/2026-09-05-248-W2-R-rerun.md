# `#248`-`W2-R` — RHYTHM re-run: the frozen prompt on the D + D2 + D3 library, blind then sighted

session: `#248` · 2026-09-05
window: `W2 bento rhythm · lane R (the re-run)`
sub index: `W2-R`
brief: `notes/_briefs/2026-09-05-248-W2-rhythm-brief.md` § RE-RUN under § RULES, with the conductor's chat updates (arm L retired for stats; the spans dial absent; the composition gate RED for a reason C9 cannot see — record, do not fix), over `notes/_briefs/2026-09-05-246-lane-B-baseline-brief.md` (the protocol, verbatim), as run in `notes/_subreports/2026-09-05-247-W1-R-rerun.md`
tokens: `UNMEASURED — subagent cannot read message.usage; harness counter 15,000,000 − 14,774,624 ≈ 225K QUOTA drawn across ≈40 tool calls (four 1440-wide PNG reads, three subreports + two briefs read whole, one 160-line script read). Context at the arm-A→arm-B seam ≈65K (SEAM.md).`

## VERDICT

DONE. Both arms built and frozen (A `80b2ed7a…`, B `c5910c9e…`), measured with the W1 instrument byte-for-byte plus a wall probe. **The 4+2 wall lands on the sighted page: evidence 893.3px (cols 1–4) beside the rail 426.7px (cols 5–6), every group's left on a column line (Δ 0.00), tops aligned (Δ 0.0), one row below the lead; seed 4 (the symmetric wall) does not recur; DP-14/15/16 met.** First-chart-y is **678.6 = W1** (W0 837.8 → W1 678.6 → W2 678.6): a wall-span edit cannot move the lead row, as lane D said; the chart svg inside moved up 28px (738.6 → 710.6) because the wider tile keeps the chart head on one line. Richness sighted **2/2/0/2** = W1; blind **1/1/0/2** = W1. **Two of the library's three W2 edits did not reach the composed page**: (i) rule 6b and `height:auto` live only in the snippet's `<style>` and canon.css was not regenerated (build fenced) — at 1100 the composed page orphans the rail 2-of-3 (666.7 of 1020) while the template re-flows (1036); (ii) the template's hand-carried VFIT stub and Chart-line's own dv-behaviour both fit every `svg.dv-fit` and fought over the line chart (gridlines re-derived for 415px, viewBox re-pinned to 260, series at baked y) — the sighted builder dropped the stub at it3, so the page's chart is 821×260 with 44.1px of ground under it (DP-18 met on the template, missed on the page). **The Tabs blind-harness defect recurred byte-for-byte** (cols 1, chart y 1554.4, page 2719 — W1: 1554.4 / 2719), not fixed under the arm. Arm B gates green (screen exit 0, receipt exit 0). Score sheet `notes/_dp-scores/w2.json`: met 10 · missed 16 · n/a 3 (W1 8 · 18 · 3). Review page `reviews/W2-RHYTHM-2026-09-05-v1.html` (863,405 B), rendered and looked at before shipping. Judge folder written FIRST (x = template reflow, y = W2 sighted added after arm B, z = W1 sighted; seed 248005). `_SCREEN-GATE.md` restored (diff 0). Nothing of Dave's ruled.

COUNTS: findings `10` · ruling-shaped `6` · UNPROVEN `4`

## What was done

**Judge folder first** (`outputs/w2-rhythm/judge/`): `random.seed(248005); random.sample(['x','y','z'],3)` → z = W1 sighted, x = template reflow 1440 light, y = W2 sighted (written after arm B froze; the order is noted in `KEY.md`). Arm L is not in the folder (retired for stats by Dave, #248).

**Arm A — blind** (`outputs/w2-rhythm/arm-A-blind/`): `_assemble.py` derived from W1's with the template's byte ranges re-mapped to today's 1436-line file (CSS L113-799 incl. rule 6b + fit CSS; symbols L824-832; masthead L834-846; header L848-871; GROUP 1 L885-977; chart-bar L990-1027; GROUP 3 L1031-1074 at `data-c="2"`, cards `data-c="3"`; the fit engine script L1256-1417 carried byte-identical, sha12 415485b97cda, as a template block); the authored evidence wrapper carries `data-c="4"` to match the template's GROUP 2; every W1 content swap asserted and applied; `READ-LOG.md`, `USED-MISSING.md`, `brief.md`; frozen `FROZEN.sha256`. No render, gate or validator before the freeze.

**Seam check-in**: `notes/_subreports/2026-09-05-248-W2-R-SEAM.md` (5 lines).

**Arm B — sighted** (`outputs/w2-rhythm/arm-B-sighted/`): it0 = arm A bytes; it1 the three W1 harness fixes (`_assemble_b_it1.py`) — the wall becomes 4+2 and a NEW defect shows (two fit engines, `_peek-it1-wall.png`); it2 the canon.css composition switch + receipt (`_assemble_b.py`); it3 the template's fit stub dropped + logo path. `ITERATION-LOG.md`. Gates `gate-it3-screen.txt` exit 0 · `gate-it3-receipt.txt` exit 0. Frozen.

**Measurement 1–7, both arms**: `outputs/w2-rhythm/_render_arm.py` (byte-identical copy of W1's instrument: `render-{1440,820}-{light,dark}.png`, `probe-render.json`, `drive-render.json`) + `_probe_w2.py` (the wall: column lines, group rects + `data-c`, on-line Δ, tops Δ, chart viewBox / ground below the figure / gridMaxY, rail tiles' ground, page height, engines present, live resize; at 1440/1100/820/520 light+dark → `probe-w2-*.json`, `render-1440-light-full(-fold).png`); `measure-inventory.json` (inventory, script sha-identity, hex counts, spans read from the files); gates on arm A exit 1 / exit 1 (NO-RECEIPT, as W1).

**Deliverables**: `notes/_dp-scores/w2.json` (29 rows with W1's carried where the edit did not touch, DP-14/15/16/18 with file+line; seeds 1..6; richness per arm; first-chart-y W0/W1/W2; spans-dial and composition-gate facts; conflict ledger 5 rows = W1's 2 + 3) · `reviews/W2-RHYTHM-2026-09-05-v1.html` (swiss idiom; W1 sighted beside W2 sighted, full page, fold line at 900; the template crop beside the it1 defect crop; first-chart-y table; the wall table per band; seeds W1/W2; richness; the 29-row sheet with a W1 column and ▲ on moved rows; conflict ledger; the four s248 rulings quoted from `_rulings.json`; RSQs as `<details>` accordions; built by `outputs/w2-rhythm/_build_review.py`, rendered at 1440 with 0 page errors and three viewport checks eyeballed) · `knowledge/_REVIEW-SIGNOFF.md` one row appended.

**Gate side effect**: `_validate_screen.py` wrote `knowledge/_SCREEN-GATE.md` — restored via `git show HEAD:` after each run (`git diff HEAD -- knowledge/_SCREEN-GATE.md | wc -l` = 0); `knowledge/_screen-gate/dashboard.md` untracked before this lane, left as found. `_build_all.py` not run. No commit, no push.

## Findings

1. **The 4+2 wall lands on the composed page.** `probe-w2-render.json` 1440-light: colLines `[40, 273.3, 506.7, 740, 973.3, 1206.6]`; evidence x 40 w 893.3, context x 973.3 w 426.7; onLineDelta 0.00 all groups; topsAlignedDelta 0.0; wallRowsBelowLead 1. 820: two full rows (w 740 each). Seed 4 passes on spans (page L323 `data-c="4"`, L466 `data-c="2"`, L469/L480 `data-c="3"`; blind bytes identical, `measure-inventory.json`).
2. **First-chart-y 678.6 = W1** (group top, `probe-render.json` both waves); svg top 738.6 → 710.6 (−28, the chart head on one line in the 4-col tile). 820: 1109.6 = W1. Lane D's 516.5 → 516.5 on the snippet holds on the page.
3. **The Tabs blind-harness defect recurred byte-for-byte**: cols 1, rows 6, chart group y 1554.4, docH 2719 at 1440 (W1 arm A 1554.4 / 2719). Reported, not fixed, so the arm is comparable (1/1/0/2 = W1 A).
4. **Two fit engines on one page break the line chart.** it1 `probe-w2-it1.json` 1440-light: `evidence.gridMaxY` 373 vs `svgViewBox` "0 0 736 260"; it2: 274 vs 260. Mechanism: the template's stub (`__dvFit`, with VFIT — snippet L1256-1417) re-derives rect/line/text y for the tile's height; Chart-line's dv-behaviour (`__dvBehaviour`, page L665-922 of the source) then re-pins the viewBox to 260; polylines have no y re-derivation in either. Visible only when the evidence tile is STRETCHED (4+2 at 1440); the blind arm's one-column wall had 0 slack and hid it.
5. **s248-D2's vertical fit does not reach the composed page.** it3 dropped the stub (the stub's own comment: delete when the full partial is present). Final: `svgViewBox` "0 0 821 260", `emptyBelowFigure` 44.1px, `engines {dvFit:false, dvBehaviour:true}`; the template renders 856×415 with 0.1px (`template/measure.json` reflow-1440-light). DP-18 missed on the page, met on the template — ledger row 4.
6. **Rule 6b does not reach the composed page.** canon.css carries only `tpl-group-context{--bento-row-unit:96px}` (L17241) — no `grid-column:1 / -1`, no `height:auto` (build fenced). `probe-w2-render.json` 1100-light: context w 666.7 of 1020 (the orphan `s248-D1` ruled a defect); in the spliced it1 state the rail filled its row (w 892, cards 444+444), so the rule works when its CSS is present. Page height 1100: 1742 (template 1374).
7. **The composition gate is RED for a reason it cannot see** — carried from lane D3 finding 3, re-confirmed on arm A's own bytes (gate-screen.txt "C9 RED line 1733 data-c=4 … line 1876 data-c=2 … spans sum to 8"); under the canon model the page reports "UNPROVEN: C9 bands". Recorded, not fixed.
8. **The spans dial does not exist**: `grep -c spans knowledge/_render/_bento_edit_rails.json` = 0. Fact recorded on the sheet and the page; `s246-D3` stays Dave's TRIAL.
9. **Richness cannot see rhythm**: 2/2/0/2 for the third wave; the component set is byte-identical (script sha12s unchanged from W1 except the dropped stub). The judge's x/y/z read is the instrument that can.
10. **Carried unchanged from W1**: marker relabel miss ("A · Current · Jan: 12"), period seg without a consumer, dead kpi-cta, DP-22 red on falling-is-good, the 520px cost; the toolbar L252 fix.

## RULING-SHAPED QUESTIONS

1. **Where does the two-axis fit live** (findings 4–5)? (a) carry VFIT into `dv-behaviour.js`, regenerate the partial, delete the template's stub (lane D3 RSQ 2) · (b) make the stub yield to `__dvBehaviour` (the template then loses VFIT on every composed page) · (c) register the dashboard role in `component-types.json` dataviz (lane D2 RSQ 1b). Recommend (a) as its own lane; not ruled.
2. **Regenerate canon.css for the W2 rules, or carry them in the page** (finding 6)? (a) run the build (conductor serial) · (b) page-harness copy of the template's rules block (a `.c-bento__tile` rule in a page `<style>`, compose-gate territory) · (c) accept the lag until the next build — this lane did (c) and says so.
3. **The gate's law vs `s248-D1`** (finding 7) — lane D3 RSQ 1 re-asked with a page fact; recommend (a) teach C9 a row-sum law + read a scoped `grid-column:1 / -1` as full row.
4. **Blind-arm harness variance, third occurrence** — W1 RSQ 4 stands; the count is the fact.
5. **DP-18 on the page and the rail's stretched card** — `s248-D3` territory (which components get a fit engine); a library question.
6. **The spans dial** — absent; lane D's ordering (after Q3) stands.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** behaviour under the real HSBC face — no Univers Next in the sandbox; column-line geometry is face-independent, tile heights (438.1, 44.1px ground) are not. Price: one 1440 render on a host with the font (~3K).
- **UNPROVEN:** the line chart's series drawn only to June in both waves' captures — read as Chart-line's draw animation caught at 600ms (`dv-animate`), not checked with a longer wait. Price: one capture at 3s (~1K).
- **UNPROVEN:** whether a page-level copy of rule 6b would pass the compose gate (RSQ 2b) — not tried. Price: one it-iteration on a scratch copy (~3K).
- **UNPROVEN:** the judge's reading — `verdict.md` had not returned when this report was filed; the key was written before the judge ran and y.png added after arm B (order in `KEY.md`).
- **CLAIMED:** W1's 22 untouched rows carry W1's verdict + fact after a grep confirming the same line numbers on this file (`tpl-group-lead` L236, `stat-card` L326, context L466, `Needs attention` L482); the facts were not each re-driven.

## Evidence

`outputs/w2-rhythm/` — `brief.md` · `arm-A-blind/` (dashboard.html, _assemble.py, READ-LOG.md, USED-MISSING.md, FROZEN.sha256, render-*.png, render-1440-light-full(-fold).png, probe-render.json, probe-w2-render.json, drive-render.json, gate-*.txt) · `arm-B-sighted/` (dashboard.html, _assemble_b.py, _assemble_b_it1.py, ITERATION-LOG.md, FROZEN.sha256, render-it{1,2}-*.png, render-*.png, render-1440-light-full(-fold).png, _peek-it1-wall.png, probe-render*.json, probe-w2-{it0,it1,it2,render}.json, drive-render.json, gate-it{0,3}-*.txt) · `_render_arm.py` · `_probe_w2.py` · `_build_review.py` · `measure-inventory.json` · `judge/{x,y,z}.png, KEY.md` · `template/` (lanes D/D2/D3) · `arm-L/` (lane D; retired). `notes/_dp-scores/w2.json`. `reviews/W2-RHYTHM-2026-09-05-v1.html`. `notes/_subreports/2026-09-05-248-W2-R-SEAM.md`.

REPLAY-THESE: `outputs/w2-rhythm/arm-B-sighted/render-1440-light-full-fold.png` (~1.5K tk) · `outputs/w2-rhythm/arm-B-sighted/_peek-it1-wall.png` (~1K tk, the two-engine defect) · `notes/_dp-scores/w2.json` (~5K tk)
