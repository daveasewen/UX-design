# `#264`-W — the #264 capture ritual, delegated; steps 1→5b with step 3 an ANNOUNCED seat limit

provenance: 264 · 2026-09-09
status: observed

session: `#264` · 2026-09-09
window: conductor **Fable** (`Apollo - #264: the stat card arrow seat, /goal and the sidebar chart glyph`)
sub index: `W` (wrap)
brief: `notes/_lanes/264-W-wrap-brief.md`
tokens: `UNMEASURED — a sub cannot read its own `message.usage` from inside itself; `_checkin.py` at this seat reads the CONDUCTOR's transcript, which is what the `s214-D5` sub-cut term is for.`

## VERDICT

**DONE.** Every region of the brief ran: `_CARRIES.md` § `residual → #265` written with the two
named strikes; ritual steps 1, 1b, 2, 2c, 2d, 2e, 2f, 2g, 4, 4b, 4c, 4d, 5, 5b in order and through
the named scripts; both title lines GENERATED (not authored) by `_gen_titles.py`; the banner inside
`s241-D2`'s cap; the five local commits plus this wrap's pushed and verified with `git ls-remote`.
**Step 3 (memory) is a SEAT LIMIT, DECLARED NOT SKIPPED — the fourth consecutive delegated wrap at
which that has happened — but the FIRST at which the brief announced it in front and the conductor
wrote the hook himself before delegating.** ⛔ The `--wrap` gate form is REFUSED at this seat for the
eighteenth wrap running, on the boot-drift ceiling arm, which fails on **#255's 70,127** — a
standing breach this session did not cause and may not clear, since `s240-D2`/`s241-D1` make the
literal shrink-only. **This seat ruled nothing and graded no glyph.**

COUNTS: findings `5` · ruling-shaped `0` · UNPROVEN `2`

## What was done

- **Carries (`s225-D2`).** `notes/_lanes/264/carry265.py` built `_CARRIES.md` § `## residual → #265`
  from § `## residual → #264` in ONE programmatic pass: **532 age brackets bumped** (6 of them
  `NEW — 0` → `1`), **2 surgical edits each asserted UNIQUE before it was made**, **4 new items**
  written in front. Probe **387 → 392** (`_capture_gate._carry_items()`, the repo's own definition).
- **Step 1.** `_LIVE-STATE.md` — new `Last refreshed` summary for #264, #263's demoted to
  `Previous:`; `_validate_standing_instructions.py` PASS (28 standing docs reachable).
- **Step 1b.** `_DECISION-HISTORY/2026-09-09-264-glyph-twins-and-derived-actives.md` — five
  sections plus resolved/open state.
- **Step 2 + 2c + 2d + 2e + 2f + 2g.** New ★ LATEST banner (**1,194 tape / 10 substantive lines**
  against the 1,200 / 10 cap); the **#262** banner rolled to `_GM-ARCHIVE.md` § Batch 2026-09-09
  #264; the **#261** ⏱ delta (19 ln) **and its 3,984-character `Previous:` chain segment** rolled
  VERBATIM to `_LIVE-STATE-ARCHIVE.md` § Rolled 2026-09-09 #264; `roll_2f` split the
  `#### 2026-09-09 #263` stratum (21 ln → `notes/_GAUGE-LOG.md`, 2 ln → `_GM-ARCHIVE.md`); 2e rolled
  **0 dated strata** (DO-FIRST 109 raw lines / 10,140 real, inside the band); the retrieval index
  rebuilt LAST. **Every move went through `knowledge/_gm_move.py` with session-owned, uniquely-named
  ops files under `notes/_lanes/264/`, each asserted to exist with a size floor before the mover
  read it.**
- **Step 4 / 4b / 4c / 4d.** Rulings `s264-D1` … `s264-D3` read back at entries 437–439 (store
  436 → 439), all `by` Dave · `date` 2026-09-09; both titles generated by `_gen_titles.py --session
  264` with its receipt written; `_gate_scratch_hygiene.py --clean` removed this session's own
  `/tmp` scratch (0M owned by dead sessions); `_render_rulings.py` re-rendered `notes/_RULINGS.html`
  and `--check` printed **FRESH** at sha256 `c17b45e3…`.
- **Step 5 / 5b.** One commit in the **#243 form** (the `--wrap` gate refusal RECORDED, not routed
  around), carrying the uncommitted `knowledge/_rulings.json` amend by instruction, then `git push`
  and a `git ls-remote` verification.

## Findings

1. **The rehearsal caught a STALE rulings page, and step 4d is what closed it.** `_checkin.py` at
   this seat's opener reported ⛔ STRUCTURAL: `notes/_RULINGS.html` embedded `f281686e…` while
   `_rulings.json` had moved to `c17b45e3…`. Probe: the rehearsal block quoted both hashes;
   `_render_rulings.py` then `--check` → `FRESH … c17b45e3…`. ★ In its **second** outing `s263-D10`
   caught a real staleness rather than a hypothetical one.
2. **The `s214-D5` hand-over is fully MEASURED for the first time in three wraps.** brief-cut
   **214,299 real** (conductor, declared in the brief) · sub-cut **221,197 real** (first-hand
   `_checkin.py`, 101 turns) · **delta 6,898 real** · replay unobservable (conductor-side,
   post-wrap). #263's line carried `brief-cut unobservable`; this one carries a number, which is
   what the `s214-D4` re-base is staged on.
3. **Boot has a MEASURED reading and no hole: 69,955 real**, 45 under the 70,000 shrink-only
   literal — the eighth consecutive under-ceiling reading — because `tiktoken` was installed at the
   conductor's opener rather than after the fact. ⛔ The gate still refuses on **#255's 70,127**, and
   that is correct: raising the literal is Dave's word alone and is not a price a wrap may pay to
   unblock itself.
4. **The carry pass leaves THREE items unpaired and only two of them are strikes.** Probe: every
   #264 carry normalised through `_carry_items` and matched at age+1 against the #265 line. The
   third is the #264 item ① whose INLINE age pointers (`[1]`→`[2]`, references to its own list's
   items ② and ③) were bumped by the same mechanical pass. **That bump keeps the pointer TRUE and is
   not a re-wording** — declared here rather than discovered by a later reader.
5. **`_AGE_RE` does not match the `[NEW — 0, …]` form its own docstring says it strips.** Probe:
   `_capture_gate._AGE_RE.pattern` is `\[(\d+)(?:\s*[,—-][^\]]*)?\]` — it requires a digit
   immediately after `[`, so a `NEW — 0` segment carries an age only if some LATER bracket in the
   same `·`-segment supplies one. ⛔ **Consequence, and it is small but real: the probeable carry
   count is not exactly "the number of carries" — new items can be counted or not depending on
   unrelated inline brackets.** Not repaired here (the count is a mechanical probe, and changing its
   definition mid-wrap would silently move a number three surfaces quote).

## RULING-SHAPED QUESTIONS

**None from this seat.** Everything ruling-shaped this session belongs to Dave and is CARRIED, not
asked here: the `/goal` bite-test (ruled *"do it"*, not started), the four ungraded twins and whether
`global-money` is DRAWN, whether a gate report is refused at commit when its tree ≠ HEAD, #241's
midnight-wrap question (age 22), and where ritual step 3 lives now that four consecutive delegated
seats could not perform it. The brief's DO-NOT-RULE list was obeyed in full: no glyph was graded, no
release-blocking judgment was made, and no ruling was added to the three already inscribed.

## UNPROVEN / CLAIMED (ADR-0016)

- **UNPROVEN:** the full `knowledge/_tests/test_gates.py` suite — price to prove: ≈93 MB per
  case-copy against 1.1 GB free at 90% disk, i.e. the suite's own harness (copytrees ≈5 GB) cannot
  run here at all. Only the `icon` and `twin` cases were driven, in-process.
- **UNPROVEN:** `notes/_PROPOSED-263.html` and both `notes/_REVIEW-264-active-glyphs*.html` pages
  were not driven in a browser AT THIS SEAT — price: a Chromium install per
  `chromium-in-sandbox-recipe` (~4 calls) plus a driven pass. The #264 review pages WERE driven at
  the conductor's seat; `_PROPOSED-263.html` still never has been.
- **CLAIMED:** every figure describing what happened BEFORE this seat opened — the five commits'
  contents, the calibration counts, the `$total` 658 → 666 and glyph 750 → 758 movements — is taken
  from the wrap brief and `git log --oneline`, not re-derived by re-running the generators.

## Evidence

`notes/_lanes/264/` — the session-owned working set this wrap ran from and through:
`carry265.py` + `carry265-new.txt` (the carry pass), `build_ops.py` and the seven `ops-264-*.json`
mover ops files (every roll and insert, with their stdout receipts above), `banner264.txt`,
`delta264.txt`, `lastrefreshed264.txt`, `stratum264.txt`, `stamp264.txt`, and `seg261.txt` (the
chain segment as it was moved). ⛔ Nothing was written to session scratch.

REPLAY-THESE: none — the stub and this file carry everything the conductor needs to close #264. The
carried items are in `_CARRIES.md` § `residual → #265` (`carries:residual-265`, probe 392); the
narrative is in `_DECISION-HISTORY/2026-09-09-264-glyph-twins-and-derived-actives.md`.
