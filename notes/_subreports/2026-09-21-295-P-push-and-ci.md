# 295 · P — twelve commits pushed and CI read job by job: gates red (6 → 7 fails, ONE newly red), release green, render green

session: `#295` · 2026-09-21 · lane `P` · model Opus 5 (1M) · conductor Fable 5.1
job: PUSH `master` to origin and read CI back JOB BY JOB. Push verdict is the conductor's: PUSH.

COUNTS: findings `14` · ruling-shaped `4` · UNPROVEN `3`

---

## ⬛ THE PUSH

**TWELVE commits, counted with `git log origin/master..master --oneline` after a `git fetch`** —
`dd374131` … `d30aebe1`. The handoff's OWED item 1 said **six**; it was written before #295's own
three (`95cb58dd`, `9d8c728b`, `d30aebe1`) and #294's wrap tail existed. **Twelve is the measured
count, six is the inherited estimate, and neither is rewritten.**

```
d30aebe1  after #295 — lane I report's lock and refusal counts corrected upward
9d8c728b  after #295 — the #295 lane I filed report completed with its commit sha
95cb58dd  after #295 — two rulings inscribed on his word and both enacted in code
57aa43ef  after #294 — the ritual closes: hand-up commit's sha filled in, chain regenerated
34cb5824  after #294 — the worker seat's hand-up, carried
cb849dcd  after #294 — the 5b addendum: the wrap sha, the --wrap refusal named
359b646b  after #294 — the twelve were ruled, inscribed and enacted in one day (the wrap)
f81bbdd4  after #294 — the twelve rulings enacted: eleven in code across three lanes
e31aa34b  after #294 — lane I's filed report completed
698d8ddf  after #294 — the rulings page catches up to the twelve, 622 → 634
ea930e72  after #294 — twelve rulings inscribed on his word, s294-D1 … D12
dd374131  after #294 — the judgement-only review page is built
```

**PUSH VERDICT, verbatim:**

```
To https://github.com/daveasewen/UX-design.git
   6d01b855..d30aebe1  master -> master
```

`git ls-remote origin refs/heads/master` → `d30aebe1c32c2b817d6e28bf3f75036ebb9b48c0` = local HEAD.
**VERIFIED EQUAL, independently, after the push.**

### ⛔★★ THE SANCTIONED PATH WAS TRIED FIRST, IT REFUSED, AND IT REFUSED AT A DIFFERENT GATE THAN THE ONE ON RECORD

`bash knowledge/_git_commit.sh --push` was run first. **It did NOT refuse at the credential gate.**

⇒ ★★ **LANE G'S #294 DECLARATION IS NOW FALSE AT THIS SEAT AND THE CORRECTION IS PUBLISHED RATHER
THAN SMOOTHED.** OWED item 7 records: *"Lane G declares its `--push` arm UNPROVEN end to end: no
credential in the sandbox's remote URL, so the push path refuses at the credential gate and never
reaches the expiry gate."* **Measured here: `git config remote.origin.url | grep -q "@github.com"`
returns 0 — THE CREDENTIAL IS PRESENT IN THIS SEAT'S REMOTE URL.** The credential gate would have
passed. ⛔ The URL and the token were never printed, logged or echoed at any point in this lane;
every transcript line that touched them is `sed`-redacted to `<redacted>`.

**What it actually refused on — the DIRT gate, `_git_commit.sh:225`, verbatim:**

```
✗ push refused: tree not clean — commit first (s133-D2; rehearsal log excluded per s137-D1). Dirty paths:
 M notes/_dream/_GRADE-DECISIONS.jsonl
 M notes/_lanes/293/J7-IDEA-jev-selects-over-the-kg.md
 M notes/_lanes/294/WRAP-MEMORY-HOOK.md
— DECLARED: instrumentation appends among the dirty paths (W-22; dream pass 6 P2 is FLOATED, not ruled):
    notes/_dream/_GRADE-DECISIONS.jsonl
      written by: _checkin.py B3 grade alerts / _gardener.py --grade-decision
      status:     NOT excluded — its POLICY is ⬛ DAVE'S, unruled (dream pass 6 P2)
  ⇒ this dirt is machine-written, not an uncommitted draft. ⛔ The POLICY (exclude / commit / relocate) is DAVE'S; this line only DECLARES it.
```

**All three dirty paths are OTHER SEATS' and the brief fences them: *"do not touch other seats' dirty
files."*** `_GRADE-DECISIONS.jsonl` is machine-written instrumentation whose exclusion policy is
declared DAVE'S and unruled; the other two are #293's J7 idea note and #294's memory hook, both
LEFT UNCOMMITTED BY DECLARATION in OWED item 13. **The dirt gate has NO `*_ACK` hatch** — unlike
`DOC_ROW_ACK` / `SHOWROOM_ACK` / `POLARITY_ACK` / `SESSION_ACK` / `PAT_EXPIRY_ACK`.

### ⬛ DECLARED FALLBACK, AND WHY

⛔ **THE PUSH WENT BY PLAIN `git push origin master`, AND THAT IS A DECLARED FALLBACK, NOT THE
SANCTIONED PATH.** The brief authorised a fallback *"if it refuses at the credential gate"*; **it
refused at the DIRT gate instead, which is not the case the brief named**, so the fallback is taken
on the conductor's PUSH verdict and the divergence is published here rather than absorbed. The
fallback pays a real price, named rather than implied: **the `s294-D5` expiry gate NEVER RAN**, the
fast-forward-only and master-only gates never ran, and the script's own post-push verification never
ran. **The verification was re-done by hand (`git ls-remote`, above) and the branch was confirmed
`master` before the push; the expiry check was NOT re-done and nothing here claims it.**
⇒ ⛔ **`--push` IS STILL UNPROVEN END TO END — but for a NEW reason, and the old reason is retired.**

⚠ **AND A SECOND, OLDER CONTRACT WAS CROSSED, NAMED RATHER THAN DISCOVERED LATER.**
`knowledge/_RUNBOOK-capture-ritual.md:781-783`: *"**Dave pushes via GitHub Desktop only** — never
terminal push, never a Desktop commit"* (memory `git-push-method`). `s133-D2` supersedes it **BY
ADDITION** — *"Desktop remains"* — and the addition it made is `_git_commit.sh --push`, **which is
the path that refused.** A bare terminal `git push` is the shape the runbook names as the thing
never to do. **The conductor's PUSH verdict is the authority this lane acted on; whether it reaches
a bare terminal push or only the sanctioned arm is a ruling-shaped question, below.**

### ⬛ LOCKS — THREE STRANDED AND `mv`'D, NEVER `rm`'D

The cold-seat note said the boot lock advice is half wrong. **Confirmed a third time, at a third
seat.** Three 0-byte `.git/index.lock` files were stranded and moved on-device:

- `notes/_lanes/_orphan-locks/stale-index.lock-295-P-2007` — present at the open, **before this lane
  ran any git command at all** (inherited from the #295 commit lane)
- `notes/_lanes/_orphan-locks/stale-index.lock-295-P-2008-b` — stranded by a bare `git status`
- `notes/_lanes/_orphan-locks/stale-index.lock-295-P-2015-c` — stranded by `_git_commit.sh --push`'s
  own `git status --short`

⛔★★ **CORRECTED UPWARD BY ADDITION, AFTER THE COMMIT — THE FINAL COUNT IS ELEVEN, NOT THREE.** The
three above are what this lane had stranded when the section was written. **Committing the report
stranded EIGHT MORE**, and the sequence is published because it is the whole mechanism:

- **`stale-index.lock-295-P-2027-f` / `-h`, `-2028-i` / `-j`, `-2029-k` / `-l`** — six more index
  locks, one per `_git_commit.sh` staging attempt. ★ **The trigger is NOT the mount alone: it is a
  NO-OP `git add`.** The first commit attempt staged the report; the retry's `git add` on the
  already-staged path was a no-op, **stranded the lock, and the NEXT named path (`_CHAIN.md`) was
  then refused with `fatal: Unable to create '.git/index.lock': File exists`.** ⇒ **the script
  reported `✗ could not stage '_CHAIN.md' — named in the reconciliation but git refused it`, which
  names the wrong object: `_CHAIN.md` was fine; the SUBREPORT's no-op add was the cause.**
- **`stale-HEAD.lock-295-P-2029-reset` + `stale-master.lock-295-P-2029-reset`** — `git reset -q`,
  the prescribed remedy, **stranded exactly the two REFS locks #294 recorded, on cue.** It was run
  deliberately, to make every subsequent `add` a real one; **that worked, and the commit went
  through on the next attempt.**

⇒ ★★ **THE BOOT NOTE'S TWO HALVES ARE BOTH CONFIRMED AND THEY COMPOSE INTO A RECIPE:** a no-op
`git add` strands an index lock · `git reset -q` strands two refs locks and clears the no-op ·
**so the working sequence on this mount is `mv` the index lock → `git reset -q` → `mv` the two refs
locks → run the committer once, with every path genuinely unstaged.** **Eleven files for one commit,
and the directory has now taken sixteen today.**

**Beyond those two reset-stranded files, no other `.git/HEAD.lock` or `.git/refs/heads/master.lock` appeared at this seat** — the #294 seat's
refs-locks were stranded by `git reset -q`, which this lane never ran. ⇒ **the refs-lock class is
specific to `git reset`, not to the mount.** ⛔ **Nothing was `rm`'d.** `git status` alone stranding
a lock is now measured at two consecutive sessions.

---

## ⬛ CI READ BACK, JOB BY JOB

**RUN `35642904396` · `head_sha` `d30aebe1c32c2b817d6e28bf3f75036ebb9b48c0` · workflow `gates` ·
created 2026-09-21T19:08:14Z · POLLED TO COMPLETION over ~13 minutes · RUN CONCLUSION: FAILURE.**

★ **This is the first CI run that exists on ANY of the twelve.** OWED item 1: *"No CI run exists on
any of them and none is claimed."* It exists now, and it is read below.

| job | id | colour | failing steps |
|---|---|---|---|
| `gates` | `106476276816` | ⛔ **FAILURE** | **5** `Survey the COMMITTED tree — every check/selftest before any regeneration` · **6** `Knowledge build — all derived views + blocking gates` |
| `release` | `106476277021` | ✅ **SUCCESS** | none — all 14 steps green |
| `render` | `106476276523` | ✅ **SUCCESS** | none — all 8 steps green |

⚠ **`render` stayed `in_progress` for roughly nine minutes after `gates` and `release` had both
closed** (the BLOCKING state-contrast sweep, step 6). **The verdict above is the one taken after it
finished, not the one available first** — the #291/#292 lesson, met a third time and obeyed.

### GATES — every step, with its conclusion

```
 1 success  Set up job
 2 success  Run actions/checkout@v6
 3 success  Run actions/setup-python@v6
 4 success  Install tiktoken + jsonschema
 5 FAILURE  Survey the COMMITTED tree — every check/selftest before any regeneration
 6 FAILURE  Knowledge build — all derived views + blocking gates
 7 success  Gate self-tests — every gate bites its failure class
 8 success  Advisory bite-tests — advisory checks must bite (and never block)
 9 success  Commit-script selftest — the msgfile-prefix gate bites both directions (#208)
10 success  Claim-table evidence linter — s182-D1 tokens + expected observations (W-44, advisory)
11 success  Committed gate artefacts vs a fresh run (ADVISORY; born advisory, #221)
12 success  Inline-style parse gate — `style=""` read with an HTML parser (ADVISORY; #122 seed)
23 skipped  Post Run actions/setup-python@v6
24 success  Post Run actions/checkout@v6
25 success  Complete job
```

★★ **STEP 9 IS THE ONE THAT MATTERS AND IT IS GREEN.** `bash knowledge/_git_commit.sh --selftest`
ran in CI and passed on a tree where `_git_commit.sh` was edited this wave —
`✓ selftest OK — 28 bites: 6 fire, 7 stay silent, 1 hatch present, 14 on the s294-D5 expiry gate`,
including `the expiry gate sits AFTER the credential check and BEFORE the push`. **The `s294-D5` arm
`--push` could not reach is PROVEN by its own selftest in CI**, on the fake clock, across all seven
boundary dates. ⛔ **That is the gate's POSITION and its ARITHMETIC proven; it is NOT `--push` proven
end to end.**

### ⬛ STEP 5 — THE SURVEY, AND THE ONE NUMBER THAT MOVED

```
SURVEY: 58 pass · 7 FAIL · 4 COULD-NOT-ASK · 0 unaskable · 77 not asked (mutating)
```

**Against #292's run `35599914864` on `301f6fbf`, read from its own log at this seat:**

```
SURVEY: 57 pass · 6 FAIL · 4 COULD-NOT-ASK · 0 unaskable · 77 not asked (mutating)
```

⇒ **6 → 7. ONE step is newly red and the other six are the identical six, by name.**

| # | step | verdict |
|---|---|---|
| `[3]` | token blast-radius + graph report — determinism/staleness (#79 P6) | ⛔ **still red (inherited)** |
| `[13]` | capture/provenance selftest (Memento §4.1) | ⛔ **still red (inherited)** |
| `[38]` | component-partials sync — AUTO-PARTIAL injection + contracts (ADR-0013) | ⛔ **still red (inherited)** |
| `[120]` | read chain determinism check — stale `_CHAIN.md` (#41) | ⛔★ **NEWLY RED — THIS WAVE'S** |
| `[125]` | memento schematic determinism check (#127) | ⛔ **still red (inherited, was `[123]`)** |
| `[128]` | memento-package delta-audit selftest (#79) | ⛔ **still red (inherited, was `[126]`)** |
| `[136]` | governs matcher selftest — the `[12]`/`[13]` trigger-index arm (#208) | ⛔ **still red (inherited, was `[134]`)** |

⚠ **THE STEP NUMBERS SHIFTED AND THE STEPS DID NOT** — `[123]→[125]`, `[126]→[128]`, `[134]→[136]`.
**Two steps were inserted upstream this wave.** ⇒ ★ **a survey step ID is NOT a stable identifier
across sessions, and any record that cites one by number is citing a moving target.** Matched by
NAME here, deliberately.

**THE NEWLY RED ONE, verbatim from the run log:**

```
  [120] read chain determinism check — stale _CHAIN.md serves a PREVIOUS session's record (#41)
      python3 knowledge/_gen_chain.py --check   (exit 1)
      ✗ _CHAIN.md is STALE — it does not match GOOD-MORNING.md / _LIVE-STATE.md as they now stand,
        so a cold session would read a PREVIOUS session's record
```

⛔★★ **THIS IS EXACTLY THE FAILURE THE COLD-SEAT NOTE PREDICTED, AND IT WAS PREDICTED IN THE BRIEF
TOO.** *"the doc-row MINT is itself what takes `_gen_chain.py --check` red"* / *"it will, every
commit stales it by the HEAD clause."* **The #295 commit lane committed after its last chain
regeneration, so the pushed tree ships a stale `_CHAIN.md`.** ⚠ **NOT REPAIRED HERE — the brief
fences it (*"do not repair anything CI reports"*)** and the repair is structurally unfinishable at a
committing seat: regenerating the chain requires a commit, and that commit re-stales it.
⇒ ★ **The chain check can be green on the WORKING TREE and red on every PUSHED tree, permanently, by
construction. #294's wrap did not push, so nobody had seen this before.**

### ⬛ DID `_capture_gate.py --selftest` RUN IN CI, AND WHAT DID IT SAY — THE BRIEF'S HEADLINE QUESTION

★★ **YES. IT RAN, AND CI IS THE FIRST HONEST ASKING JUST AS THE HANDOFF SAID IT WOULD BE.** Verbatim
from run `35642904396`, step 5:

```
  [13] capture/provenance selftest (Memento §4.1)
      python3 knowledge/_capture_gate.py --selftest   (exit 1)
      ❌ FAIL GOOD-MORNING.md: no `pre-flight:` stamp — the handoff must carry the estimate the
         session was priced with (runbook § ★ Half 0b). Form (Dave #56
```

**(The survey truncates each failure to its first line at ~150 chars; that truncation is the
instrument's, not this report's.)**

**THE VERDICT, and it is the one worth carrying:** ⛔ **exit 1 — but the failure is BYTE-IDENTICAL to
#292's on `301f6fbf`, before any of this wave's edits to `_capture_gate.py` existed.** Compared
line-for-line against `/tmp/prev_gates.log` at this seat. ⇒ ★★ **THE SIX `_capture_gate.py` EDITS
THIS WAVE MADE — `s295-D1`'s boot double-count narrowing, `s295-D2`'s enacted-sha pointer, and
#294's four — BROKE NOTHING THE SELFTEST CAN SEE. The gate's own selftest is NO WORSE than it was
twelve commits ago.** ⛔ **It is also NO BETTER, and it was already failing.**

⚠ **AND THE FAIL MAY BE FALSE, WHICH IS A FINDING AND NOT A REPAIR.** `grep -n 'pre-flight:'
GOOD-MORNING.md` at this seat returns **three hits** (lines 523, 539, 555), each a full refusal-shaped
stamp. ⇒ **The file DOES carry `pre-flight:` stamps; the arm is looking for one in a particular
place or shape and not finding it there.** ⛔ **Not diagnosed further, not touched** — the brief
fences repair, and `#294`'s cold-seat warning is explicit that grading a gate with the wrong arm *"is
exactly how a false cause ran for ninety-three sessions."*

### ⬛ DID THE SEVEN (NOW ONE) INHERITED GATE FAILS SHOW UP?

⛔★ **NO — AND THE QUESTION AS PUT CONFLATES TWO DIFFERENT OBJECTS, WHICH IS ITSELF THE FINDING.**

- **The "seven → one"** of lane I's `gate 7 → 1` is the **`_capture_gate.py --wrap` mode's** fail
  count: six boot double-counts closed by `s295-D1`'s code narrowing, leaving only the ceiling
  breach. **`--wrap` IS NOT WIRED INTO CI AT ALL** — no step in `.github/workflows/gates.yml` calls
  it, at any of the three jobs. **So the seven never could have shown up, and their closing cannot
  show up either.**
- **The "seven FAIL"** in CI's step 5 is the **survey's** failure set — a different seven, of
  different steps, that happens to have been six last session and is seven now.

⇒ ★★ **TWO UNRELATED SEVENS HAVE BEEN TRAVELLING UNDER ONE NAME IN THIS SESSION'S BRIEFS.** The
handoff's *"rule the seven inherited gate fails closed"* (IDEA-wrap-is-slow lever 3) is the WRAP
seven; CI's is the SURVEY seven; **`s295-D1` moved the first from 7 to 1 and moved the second from 6
to 7, in opposite directions, in the same commit.** ⛔ **Nothing is wrong in either count. The
collision is in the WORD.**

### ⬛ STEP 6 — THE KNOWLEDGE BUILD, UNCHANGED IN EVERY PARTICULAR

```
❌ step 'help-gate — no entry point may write before it reads argv (#158 class gate)' failed (exit 1) — aborting
```

**12 `NO HELP GATE` lines**, and **#292's run had the same 12, on the same 12 files** — diffed at
this seat. Named in full because a count alone hides substitution:
`_bite_goal.py` · `_bite_kg_edge_proposal.py` · `_build_kg_explorer.py` ·
`_gen_ruling_edges_from_recs.py` · `_kg_history.py` · `_render/verify_demo_slides_268.py` ·
`…_v3.py` · `…_v5.py` · `…_v6.py` · `…_v7.py` · `…_v74_gearbox.py` · `…_v75_gearbox.py`.
⇒ ⛔ **STILL RED, WHOLLY INHERITED, IDENTICAL.** The build aborts at the help-gate and never reaches
the derived views behind it — **so every gate downstream of step 6's help-gate is UNASKED in CI, not
passing**, which the log does not say and this report does.

### ⬛ DID release / render / gates CHANGE COLOUR?

| job | #292 (`301f6fbf`, run `35599914864`) | #295 (`d30aebe1`, run `35642904396`) | verdict |
|---|---|---|---|
| `gates` | ⛔ FAILURE, steps 5 + 6 | ⛔ FAILURE, steps 5 + 6 | **STILL RED — same two steps; the SET inside step 5 grew 6 → 7** |
| `release` | ✅ SUCCESS | ✅ SUCCESS | **STILL GREEN — third consecutive sha** |
| `render` | ✅ SUCCESS | ✅ SUCCESS | **STILL GREEN** |

★ **NO JOB CHANGED COLOUR. Release's green is now proven on `d5a0eb8c`, `301f6fbf` and `d30aebe1`
— three shas, across twelve commits that touched the build spine — against FOUR sessions of red
before it.** ⛔ **And the twelve commits touched `_build_all.py`: `release` step 12
`Build-script selftest — the release refusals, driven (BLOCKING)` is GREEN on the edited tree.**
⚠ **`render`'s BLOCKING state-contrast sweep (135 surfaces, 0 text failures) is green on a tree
whose `_gardener.py` and `_gauge_tokens.py` moved — neither is on render's path, so that green
proves nothing about them and is not claimed to.**

---

## ⛔ REFUSALS PAID, AND WHAT EACH COST

1. ⛔ **`_git_commit.sh --push` REFUSED at the dirt gate** — three other seats' paths, no ACK hatch,
   fenced by the brief. **Cost: the expiry / ff-only / master-only / verify gates all skipped.**
2. ⛔ **`_gen_chain.py --check` red NOT REPAIRED** — brief fences repair, and the repair is
   structurally unfinishable at a committing seat.
3. ⛔ **`_capture_gate.py --selftest`'s `pre-flight:` fail NOT DIAGNOSED** past measuring that the
   stamps exist on disk. **Grading with the wrong arm is the #294 cold-seat's named trap.**
4. ⛔ **THE FULL `--selftest` OUTPUT IS UNREAD, ONE LINE DEEP ONLY.** The survey truncates; no CI
   step runs `_capture_gate.py --selftest` outside the survey. ⇒ **exit 1 and its FIRST failure are
   measured; whether it has a SECOND failure is UNKNOWN and is reported as unknown.**

## ⬛ UNPROVEN AT THIS SEAT (3)

1. **`--push` END TO END.** Still unproven — now blocked by DIRT, no longer by CREDENTIAL.
2. **THE `s294-D5` EXPIRY GATE against a REAL push.** Its arithmetic and position are proven by CI
   step 9's 14 bites on a fake clock; **no real push has ever passed through it.**
3. **WHETHER `_capture_gate.py --selftest` HAS EXACTLY ONE FAILURE.** See refusal 4.

---

## RULING-SHAPED QUESTIONS

1. ⛔★★ **DOES THE CONDUCTOR'S `PUSH` VERDICT REACH A BARE `git push`, OR ONLY THE SANCTIONED ARM?**
   `s133-D2` superseded `git-push-method` BY ADDITION, and the addition is `_git_commit.sh --push`.
   When that arm refuses on OTHER SEATS' dirt, a lane either **does not push** or **crosses a runbook
   line written in the imperative** (*"never terminal push"*). **This lane crossed it on the
   conductor's word and is declaring it.** ⛔ **Which of the two is right is Dave's.**
2. ⛔★ **DOES THE DIRT GATE GET AN ACK HATCH, SPELLED LIKE ITS SIX NEIGHBOURS?**
   `PUSH_DIRT_ACK="<real reason>"`. The gate's own declaration already says the dirt it found was
   *machine-written instrumentation whose policy is Dave's and unruled* — **the script diagnoses the
   exact case that would justify the hatch and then has no hatch to offer.** ⚠ This is the same
   object as dream pass 6 P2, still FLOATED not ruled, now **blocking a push** rather than a commit.
3. ⛔★★ **`_gen_chain.py --check` IS RED ON EVERY PUSHED TREE BY CONSTRUCTION. IS THAT A GATE OR A
   TAUTOLOGY?** The chain is stale the instant the commit that regenerates it exists. **A check that
   cannot be green at the moment it is asked is measuring the ritual, not the artefact.** ⇒ should it
   read the chain against the tree **as of the PARENT commit**, become advisory, or be ruled closed
   as history? **Only visible because #295 pushed; twenty wraps never saw it.**
4. ⛔ **THE TWO SEVENS NEED DIFFERENT WORDS.** `--wrap`'s seven fails (now one) and CI's survey seven
   fails (was six) are unrelated sets that moved in opposite directions in the same commit.
   **Every brief this session called both "the seven inherited gate fails."** ⇒ name them — *wrap
   gate fails* and *survey fails* — before a session rules one closed and means the other.

---

## ⚠ FOR THE NEXT SEAT

- **The recursion terminates here and the termination is declared: this report is itself committed,
  and a commit cannot read its own CI.** The report commit ships with its CI **UNREAD BY
  CONSTRUCTION** — named, never implied. **It was NOT pushed** (the brief conditions that on the
  first push being clean; the first push WAS clean, but the report commit re-stales `_CHAIN.md` and
  would ship a second known-red tree to no purpose). ⇒ **the next seat pushes it, or rules it.**
  ★ **THIS COMMIT IS `280e4e82`**, and **Q3 IS NOW PROVEN RATHER THAN ARGUED: `_gen_chain.py
  --check` was regenerated GREEN, staged, and committed — and re-reading it IMMEDIATELY after
  `280e4e82` returns `✗ _CHAIN.md is STALE` again.** The chain was fresh in the tree that was
  committed and stale in the tree the commit produced, **in the same second, with no edit between.**
  ⛔ **A gate that a passing commit turns red is not measuring the artefact.**
- **A survey step ID moves between sessions.** `[123]→[125]`, `[126]→[128]`, `[134]→[136]` this wave
  alone. **Cite survey steps by NAME.**
- **`git status` alone strands a `.git/index.lock` on this mount** — second consecutive session
  measured, three files at this seat. **`git reset -q` was never run here and NO refs-locks
  appeared** ⇒ the refs-lock class belongs to `reset`, not to the mount.
- **`notes/_lanes/_orphan-locks/` took three more files today**, on top of the five #294 recorded.
  ⚠ **Lane C's sibling question — whether that directory needs a purge ritual of its own on a mount
  that refuses `unlink` — is now eight files old.**

REPLAY-THESE: `git log origin/master..master --oneline` (expect **0** — pushed) · `git ls-remote origin refs/heads/master` (expect `d30aebe1c32c2b817d6e28bf3f75036ebb9b48c0`) · `bash knowledge/_git_commit.sh --selftest` (expect `✓ selftest OK — 28 bites`) · `python3 knowledge/_gen_chain.py --check` (expect **exit 1, STALE** — the newly-red one) · `python3 knowledge/_capture_gate.py --selftest` (expect **exit 1**, `pre-flight:` stamp arm; ⚠ exceeds the ~165 s sandbox call wall — CI is where it is asked) · `grep -c 'pre-flight:' GOOD-MORNING.md` (expect **3** — the stamps the failing arm does not see)
