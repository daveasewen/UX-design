# #254 BUILD LANE S — s254-D2 items 1, 2, 3, 5, 7

**Date** 2026-09-07 · **Ruling enacted** `s254-D2` (Dave, #254: "just go for it lets get this of our desk")
**Scope** items 1, 2, 3, 5, 7. Items 4 (`intent` alias removal) and 6 (R1) are DATED and were NOT touched.
**Verdict** items 1, 2, 3, 5, 7 all GREEN. The resolver ends **24 clean / 1 RED-BY-DESIGN of 25** — the single
fail is item 4's own subject (runway-bar's pre-existing `intent: composition` against the ruled
`answers: one-number`), which this lane was forbidden to touch. Proof it is the only blocker is receipt 7c.

`COUNTS: claims 31 · green 30 · red-by-design 1`

---

## BEFORE — the state this lane inherited

Probe (HEAD's own copy of the resolver, run against the live stores; scratch copy deleted after):

```
git show HEAD:knowledge/_validate_roles_resolve.py | sed 's#^HERE = …dirname(abspath(__file__))#…#' \
  > knowledge/_tmp/s254/_before_roles_resolve.py ; python3 knowledge/_tmp/s254/_before_roles_resolve.py
→ roles resolve — metas: 136 · carrying an interchangeability term: 24 · addresses resolved: 58
  field population: provides 24 · answers 24 · shape 24 · span 24 · priority 24 · when 24 · with 0 · not-with 7
  `shape` values in use (vocabulary NOT RULED — listed, not checked): 22 distinct values
  `when` predicates (field list NOT RULED — collected for the review page)
  CROSS-CHECK … AGREE for every meta that carries the field (24).
  RESULT: PASS
  --selftest → RESULT: PASS (10 bites)
```

So: 24 carriers, `shape` resolving against NOTHING, `when` field names resolving against NOTHING,
`priority` in two homes, chart-sparkline a chart-panel provider, runway-bar untagged.

---

## Receipt 1 — `shape` becomes a CLOSED store (item 1) · GREEN

**Built** `knowledge/shapes.json` — ONE home (ADR-0017), `$description` + `$grammar` + `$provenance` +
`$extension` on the `roles.json` pattern, **23 values** under a `shapes` object. The only grammar is page B's
`<x-dimension> × <mark>` (s254-D1 call B4).

The eleven page-A values were re-cut, meaning preserved, and every `when` clause that quoted the old
spelling was updated in the same edit:

| meta | before | after |
|---|---|---|
| stat-card | `one-measure-with-delta` | `one-measure × value-and-delta` |
| kpi-tile | `one-measure-with-series` | `one-measure × value-delta-and-series` |
| chart-bar | `categories-by-series` | `categories × series` |
| summary | `name-value-pairs` | `rows × name-value-pairs` |
| status-indicator | `one-state` | `one-state × dot-and-label` |
| layout-utilities | `arbitrary-blocks` | `no-data × arbitrary-blocks` |
| breadcrumbs | `ancestor-path` | `ancestor-path × links` |
| headers | `title-and-optional-action` | `no-data × title-and-optional-action` |
| button | `no-data` | `no-data × control` |
| navigations | `destination-set` | `destination-set × links` |
| app-shell-top-nav | `page-regions` | `no-data × page-regions` |

- **Call A8 stands.** button and layout-utilities keep the "no data" reading — it became the explicit
  x-dimension `no-data` rather than being dropped; the mark half names what they actually draw.
- **Call B4 stands.** The thirteen page-B chart values are byte-identical to what lane B authored;
  none was re-cut.
- **Deliberate collisions preserved.** `parts-of-whole` is still shared by chart-donut and chart-pie
  (kept verbatim as a pre-grammar value, with a `$note` saying so). kpi-tile and stat-card share the
  x-dimension `one-measure` and are separated by the MARK — still distinguishable, and now visibly
  substitutes, which is what the `instead-of` grade reads.

**Check 9 `shape-known`** added to `_validate_roles_resolve.py`: every `shape` must be a key of `shapes`.

```
python3 knowledge/_validate_roles_resolve.py
→ `shape` values in use (23 of the 23 in knowledge/shapes.json — CLOSED store, s254-D2 item 1): …
  (no shape-known failure; every store value has at least one carrier)
```

Selftest bites: `unknown shape refuses → FAIL` · `a shape IN the store resolves → green`.
Mutation arms: `--mutate unknown-shape` (chart-bar reverts to `categories-by-series`) → **expected RED · got
RED — OK**; `--mutate known-shape` (chart-bar becomes `parts-of-whole` — wrong for a bar chart but legal,
proving the check reads the STORE, not the meaning) → **expected GREEN · got GREEN — OK**.

## Receipt 2 — the `when` field list (item 2) · GREEN

**Built** `knowledge/when-fields.json` — ONE home, **26 legal left-hand field names**, each with a one-line
definition and a `kind`, plus `$grammar` and `$operators` (`= == != >= <= ≥ ≤ < > in spans`).

**Method, verbatim from V-B's M2** (`notes/_subreports/2026-09-07-253-V-B.md`): a `when` is `GATE — PROSE`,
split on the FIRST em-dash; only the gate half is read; the gate is clauses joined by `AND`/`OR`; a clause
opening `<field> <operator>` is a FIELD CLAIM; a clause with no operator is prose inside the gate and is
neither parsed nor judged. Every one of the 26 names was READ OFF the corpus, not invented — 25 from the 24
metas authored at #253, plus `verdict` from runway-bar (item 7), which is one of that meta's own props.

**Check 10 `when-fields-known`** added: an unknown field name refuses, loud and named. The per-field counts
are PRINTED, which is what closes V-B's M2 (page B said 12 fields, the script found 11; nobody counts by eye
again):

```
python3 knowledge/_validate_roles_resolve.py
→ `when` FIELD COUNTS (26 of the 26 in knowledge/when-fields.json — SCRIPT OUTPUT …):
   answers 15 · shape 8 · series 6 · span.cols 5 · axes 2 · categories 2 · total 2 ·
   axis.x 1 · bins 1 · content 1 · delta 1 · depth 1 · destinations 1 · emphasis 1 · label 1 ·
   level 1 · lockup 1 · nav.levels 1 · parts 1 · platform 1 · rows 1 · scope 1 · surface 1 ·
   units 1 · variables 1 · verdict 1
   gate clauses carrying NO operator (prose inside the gate — not parsed, not judged): 10
```

Selftest bites: `unknown when field refuses → FAIL` · `prose in the gate is NOT a field claim → green` ·
`a field named only AFTER the em-dash is not parsed → green`.
Mutation arms: `--mutate unknown-when-field` (chart-bar's gate gains `prominence >= 2`) → **RED, OK**;
`--mutate when-prose-not-parsed` (a prose clause with no operator) → **GREEN, OK**.

**The new check caught a stale FIXTURE, not a meta.** Selftest bite 1 carried `when: "span >= 6"` since #253.
`span` is not a legal field; `span.cols` is. The fixture was corrected and the correction is commented in
place — the check was not relaxed to admit it.

## Receipt 3 — `priority` leaves roles.json (item 3) · GREEN

- **108 `"priority": …` lines deleted** from `roles.json` providers; `slug` and `when` kept, nothing
  reformatted. `provider objects still carrying priority: 0` (probe: a json load counting the key).
- `$description` re-written in one clause: it no longer claims priority orders the providers; it says the
  METAS are the one home and this file is a membership cross-check. The follow-on sentence that called the
  values "ILLUSTRATIVE until the 25-meta pass" was updated to say the pass is done.
- **No reader was left dangling.** `cross_check()` reads `providers[].slug` only (line 497); `derive()`
  reads the METAS' `priority`, never roles.json's. Repo-wide probe: `grep -rn "providers" --include=*.py`
  outside `_tmp/` returns only `_validate_roles_resolve.py` and `_build_all.py`'s error message — no
  script anywhere consumed a roles.json priority.
- `--mutate dup-priority` still RED and `--mutate known-role` still GREEN, so the priority checks (5, 6)
  are still driven off the metas.

## Receipt 5 — chart-sparkline leaves chart-panel (item 5) · GREEN

Read first: `$not-a-provider` is a single PROSE STRING listing the placed-not-provider set — so the
mechanism is an appended clause, not a new array.

- `roles.json`: the chart-sparkline provider entry removed from `chart-panel` (13 → **12** providers);
  `chart-sparkline` appended to `$not-a-provider` with the reason and the ruling id.
- `chart-sparkline.meta.json`: `provides` and `priority` **removed** — not zeroed. A priority with no role
  is a number about nothing (check 6), so removing one without the other would have gone RED; the deleted
  facts are preserved verbatim in a `$provides` annotation (archive-never-delete). Its `answers`, `shape`,
  `span`, `when` and `not-with` stand exactly as authored under s254-D1.
- `kpi-tile.meta.json`: the schema's `with` term (added at #253) **does** permit this — it is an array of
  `{slug, rel}` objects, `rel ∈ {recommends, suggests}` — so `with: [{slug: "chart-sparkline", rel:
  "recommends"}]` was authored, one direction only.

```
python3 knowledge/_validate_roles_resolve.py
→ field population: … with 1 …
  with: chart-sparkline with kpi-tile -> DERIVED suggests (from recommends; NOT asserted …)
  (priority-in-role GREEN; cross-check still AGREES for every meta carrying `provides` (24))
```

## Receipt 7 — runway-bar's six fields (item 7) · GREEN, with one RED-BY-DESIGN

`provides: headline-metric` · `priority: 30` · `answers: one-number` are the ruling's own three.
The other three were read off **this meta's** prose, never off roles.json:

- **purpose** — "A READOUT that answers one question: does the money cover what is already scheduled, and
  until when? It states a verdict with a date, shows the arithmetic that produces it (balance − committed =
  buffer), and draws the split on a bar." ⇒ `shape: "one-measure × committed-vs-balance-split"`, and the
  `verdict != none` clause in the gate.
- **responsive.rule** — "Fluid to a 480px max-width column; the bar is width:100% of its container. Legend
  and head wrap. Verified at 1400px and 480px." ⇒ `span.cols 3–6`: a 480px max-width column is a quarter to
  a half of a 12-col band, and the head/legend wrap is what lets it hold at 3.
- **`when`, R2 form** (gate — beats/yields prose naming siblings):
  `answers = one-number AND shape = one-measure × committed-vs-balance-split AND verdict != none — beats
  stat-card (60, …) and kpi-tile (50, …) when the reading is "money vs what is already scheduled" and the
  takeaway is a VERDICT WITH A DATE rather than a delta or a trend; yields to stat-card when there is
  nothing committed to net off, to kpi-tile when a series is the claim, and to Chart-bullet (40, …) when a
  target band, not a coverage date, is the benchmark.`

A `$interchangeability` annotation on the meta records which prose each field was read off, quoted.

**7c — the one RED, and the proof it is the only one.** The resolver ends:

```
python3 knowledge/_validate_roles_resolve.py
→ roles resolve — metas: 136 · carrying an interchangeability term: 25 · addresses resolved: 85
  field population: provides 24 · answers 25 · shape 25 · span 25 · priority 24 · when 25 · with 1 · not-with 7
  CROSS-CHECK … AGREE for every meta that carries the field (24).
  runway-bar: FAIL [intent-equals-answers] — `intent` 'composition' and `answers` 'one-number' DISAGREE.
  RESULT: FAIL (1)
```

`runway-bar` has carried `intent: "composition"` since the charts wave — V-B flagged it at #253 as the
fifteenth `intent` carrier that neither lane touched. It is not an alias of `answers`; it is a stale
chart-intent tag on a molecule that is not a chart. Item 4 of s254-D2 is the ruling that governs it and is
**DATED**, and this lane's brief says in as many words: *do not touch `intent`*. So the value stands and the
resolver is RED-BY-DESIGN on exactly one meta, on the s253-D3 precedent (the resolver stayed red on the nine
answers words until Dave ruled them in).

Driven proof that nothing else blocks (in-memory copy of the corpus; NOTHING on disk touched):

```
copy knowledge/components → tmp; drop runway-bar's `intent`; check(tmp)
→ WITHOUT the stale intent — fails: 0 · carrying: 25
```

That is the ruling's **25/25 GREEN**, one deletion away. The fix is one line and is Dave's, not a lane's:
either drop `intent` from runway-bar (item 4 early, for one meta) or rule that a non-chart may carry an
`intent` that differs from `answers`, which would loosen check 7.

---

## The mutation harness verdict became BASELINE-RELATIVE — declared

The four GREEN arms (`known-role`, `derive-demo`, `known-shape`, `when-prose-not-parsed`) run against a copy
of the LIVE corpus, so the standing runway-bar red made all four report `MUTATION NOT PROVEN` — a harness
that cannot be driven is the [[instrument-without-a-consumer]] class. `mutate()` already computed and printed
a baseline (#244's rule); its verdict now asks *did THIS mutation make the check say something it was not
already saying* (`new_fails = fails − base_fails`) instead of `bool(fails)`.

**`check()` itself is untouched, no fail is hidden**: the baseline is printed line-by-line above the mutated
result, and every arm still proves the check READS the field it mutates. Two fixtures inside `derive-demo`
were corrected for the same reason as the selftest bite — it invented `shape: "series"` (now refused by check
9) and used priority 60/50, which now collide with live providers; it uses the store's own value and the
metas' own 92/88, and the arm drives `derive()` to 31 printed lines including
`chart-line instead-of chart-bar [equivalent] shape=same span=overlaps`.

```
12/12 arms:
unknown-role RED-OK · known-role GREEN-OK · unknown-answer RED-OK · answer-disagrees RED-OK ·
dup-priority RED-OK · span-inverted RED-OK · notwith-ghost RED-OK · derive-demo GREEN-OK ·
unknown-shape RED-OK · known-shape GREEN-OK · unknown-when-field RED-OK · when-prose-not-parsed GREEN-OK
python3 knowledge/_validate_roles_resolve.py --selftest → RESULT: PASS (15 bites)
```

## The other gates

```
python3 knowledge/_validate_kg.py          → OK — every ref parses+resolves … (138 metas)
python3 knowledge/_build_integrity.py      → integrity: PASS — 0 errors, 22 warnings · schema valid: 136/136
python3 knowledge/_validate_intent_resolve.py → PASS — 15 intent carriers, 16 addresses
python3 knowledge/_validate_help_gate.py   → help-gate OK — 325 scripts
python3 knowledge/_checkin.py              → FILL 147,815 real · room to stop line 3,114 (the CONDUCTOR's
                                             session, not this lane's; read as a warning about wave size)
```

`_build_all.py` was NOT run (its step-18 abort strips 41 compliance artefacts). Its step-message for this
gate was extended by one clause so it names the two new refusal causes.

## git diff --stat (this lane's files only)

```
 knowledge/_build_all.py                          |   2 +-
 knowledge/_validate_roles_resolve.py             | 264 +++++++++++++++++++---
 knowledge/components/app-shell-top-nav.meta.json |   2 +-
 knowledge/components/breadcrumbs.meta.json       |   4 +-
 knowledge/components/button.meta.json            |   2 +-
 knowledge/components/chart-bar.meta.json         |   2 +-
 knowledge/components/chart-sparkline.meta.json   |   3 +-
 knowledge/components/headers.meta.json           |   2 +-
 knowledge/components/kpi-tile.meta.json          |  11 +-
 knowledge/components/layout-utilities.meta.json  |   2 +-
 knowledge/components/navigations.meta.json       |   4 +-
 knowledge/components/runway-bar.meta.json        |  12 ++
 knowledge/components/stat-card.meta.json         |   4 +-
 knowledge/components/status-indicator.meta.json  |   4 +-
 knowledge/components/summary.meta.json           |   4 +-
 knowledge/roles.json                             | 117 +---------
 + untracked: knowledge/shapes.json · knowledge/when-fields.json
```

Thirteen meta files, **37 insertions / 19 deletions** — surgical inserts and one-value replacements, lane B's
#253 method, zero reformat. roles.json's 117 is the 108 ruled priority deletions plus the sparkline entry
and the two `$description` sentences.

## Files touched

1. `knowledge/shapes.json` — NEW (item 1)
2. `knowledge/when-fields.json` — NEW (item 2)
3. `knowledge/_validate_roles_resolve.py` — checks 9 and 10, two store loaders, the `when` parser, the field-count report, 5 selftest bites, 4 mutation arms, baseline-relative mutate verdict, 2 fixture corrections, docstring
4. `knowledge/roles.json` — items 3 and 5
5. `knowledge/_build_all.py` — one error-message clause
6. `knowledge/components/` — `runway-bar` (item 7), `kpi-tile` + `chart-sparkline` (item 5), and the eleven shape re-cuts: `app-shell-top-nav`, `breadcrumbs`, `button`, `chart-bar`, `headers`, `kpi-tile`, `layout-utilities`, `navigations`, `stat-card`, `status-indicator`, `summary`

Not touched, by brief: `intent` on any meta · R1 · `knowledge/_gm_usage.py` · any commit, push, checkout or stash · `_build_all.py` as a run.

## Questions this lane could not settle

- **Q1 — runway-bar's `intent: composition` vs `answers: one-number`.** The one standing RED. Fix is one
  line and belongs to item 4 / Dave (receipt 7c). Until then the resolver exits non-zero, which means
  `_build_all.py` step for this gate is RED — the conductor should expect that, or take the deletion.
- **Q2 — chart-line's `when` still names chart-sparkline as a co-provider** ("yields to chart-sparkline (30,
  \"inline scale, no axes\") when span.cols < 6"), and the priority 30 it quotes no longer exists. Lane B
  authored that prose and s254-D1 keeps it AS AUTHORED, so this lane did not edit it. It is now factually
  stale: a sparkline is a complement, not a fallback provider. One sentence, Dave's or the conductor's.
- **Q3 — the four review pages quote roles.json priority numbers** that this lane deleted
  (`META-TAGS-A/B`, `META-TAGS-CALLS-CONTEXT`, `ROLES-REVIEW`). They are dated artefacts of a state that no
  longer exists. Left untouched; a reader at #255 will see numbers with no home.
- **Q4 — 10 gate clauses carry no operator** and are therefore unparsed prose inside a predicate
  (e.g. "parts sum to a whole", "both axes carry quantitative measures", "series 1–5 on ONE continuous time
  axis"). That is V-B's declared method and this lane kept it, but it means a third of the gates are still
  partly prose. Whether those should be re-cut into field clauses is a further ruling, not this lane's.
- **Q5 — the right-hand side of a clause is never parsed.** `axes = absent`, `bins = contiguous`,
  `axis.x = categorical` — those value vocabularies have no store. Check 10 proves the field name is legal
  and stops there; it cannot catch `series = purple`.
- **Q6 — `shape` has 23 values for 25 carriers**, so it discriminates almost perfectly and the `instead-of`
  grade it feeds returns `near`/`distant` nearly everywhere (only the mutated arm produces `equivalent`).
  Whether that is right — a shape vocabulary this fine means very few components are declared swappable —
  is a design question for the SH-demo wave, not a build one.
