# #256 ENACT LANE — dream pass 11 (P1a · P2 · P3b · P4b)

**Date** 2026-09-08 · **Ruling** `s256-D2` (Dave: *"do it in your order"*) · **Lane** Opus 5, id `af6fa00854d260f91`,
108,386 subagent tokens, 45 tool uses · **Source** `notes/_dream/2026-09-06-proposals.md` (commit `2319d2a`)
**Scope** four files, nothing else: `knowledge/_gardener.py` · `knowledge/_validate_screen.py` ·
`knowledge/_RUNBOOK-capture-ritual.md`. No commit by the lane; committed by the conductor at `44c8061`.
**Verdict** all four ENACTED, each with a mutation proof. Selftests: `--selftest` 7/0 → 7/0 ·
`--selftest-grades` 19/0 → **22/0**. `_validate_screen.py` has no selftest arm; `write_index()` was driven directly.

`COUNTS: claims 4 · green 4 · red 0`

---

## What landed

**P1(a) — population alarm** · `_gardener.py:146-153` (`POPULATION_DELTA_THRESHOLD = 5`, declared a placeholder),
`:990-1005` `population_delta()`, `:1085` reads `prev_doc["hooks_seen"]` from the existing sidecar (no new state file),
`:1096-1105` the refusal, CLI `:69, :1271, :1300-1315`. Prints `⚠ POPULATION CHANGED: N hooks this run vs M last run`
on |Δ| > 5; REFUSES on a shrink. Mutation: `if pop["shrank"] …` → `if False:` ⇒ `[FAIL] (g15) refused=False`.
Live `--refresh --dry-run`: `47 vs 33 (+14)` — growth, not blocked. Sidecar NOT rewritten (non-dry refresh not run).

**P2 — every link on a line** · `_gardener.py:705-709` `_HOOK_LINK_RE`, `:730-743` (`_HOOK_RE` still gates *is this a
hook line* so prose lines stay in `unlinked_index_lines`; `finditer` yields every link), fixture `:1513-1518`, arm `:1773-1791`.
Mutation: revert to `[_HOOK_RE.match(s)]` ⇒ `[FAIL] (g14) two links ⇒ 2 graded entries — ids=['first.md']; hooks_seen=1`.
Effect on the live index: **33 → 47 graded hooks**, 3 unlinked lines unchanged. `MEMORY.md` untouched.

**P3(b) — stray provenance** · `_RUNBOOK-capture-ritual.md:450-456`, one bullet at the end of the `s214-D5` hand-over
section: UNCOMMITTED from any seat; UNKNOWN only after `grep -rn "<path>"` over `notes/`, `_DECISION-HISTORY/` and the last
two sessions' briefs returns nothing; else *"provenance not established from this seat"*. `_tools/` untouched.

**P4(b) — untracked input refusal** · `_validate_screen.py:52` (`subprocess`), `:134-152` `_untracked_subjects()`,
`:155-172` refusal at the top of `write_index()` before any write. Mutation: plant `_screen-gate/_ZZ-planted-untracked.md`
⇒ `⛔ SCREEN-GATE INDEX REFUSED — a generated index may not be built from state a clone cannot see | untracked subject: …`;
planted file removed, control run builds with no refusal. `_SCREEN-GATE.md` byte-identical; `_build_all.py` not run.

## Beyond the literal wording — declared

- `--accept-population-change` added (`:1271`). Without it a shrink blocks forever (sidecar keeps the old count) —
  an unpassable gate [[gate-cannot-pass-in-one-environment]]. The refusal names the flag.
- `_untracked_subjects()` returns `[]` (no refusal) when git cannot answer — not a work tree, no binary. In the docstring.

## RULING-SHAPED — named, NOT ruled (Dave's)

1. **P4(a) premise expired**: `knowledge/_screen-gate/dashboard.md` is TRACKED at enactment (`git ls-files` lists all 10).
   The new arm is green today and refuses nothing. Whether dashboards are permanent gate subjects is still unanswered.
2. `POPULATION_DELTA_THRESHOLD = 5` is picked, not derived — same register as `GRADE_AGING_DAYS`; B3 review.
3. P2 re-based the B3 return-with-numbers dataset (33 → 47) **without** P1(b). `_MEMORY-GRADES.json` still carries only
   the 2026-08-16 incomparability notice; the #242 cut and this recovery are both undeclared in the sidecar's prose.
4. **P1(b)** (grade the overflow file too) NOT enacted — changes what B3 measures.
5. `notes/_REHEARSAL-LOG.jsonl` gained two `wrap-open` rows during the lane's window from a writer the lane did not run
   (conductor's `_checkin.py` / `_git_commit.sh`, most likely) — flagged, not investigated.

REPLAY-THESE: none — every claim above is reproducible from the named line ranges and the two selftest commands.
