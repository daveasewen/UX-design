# #220 audit wave — §L6 EFFICIENCY LEDGER (the stopwatch lane)

**Lane:** L6 · Opus · 2026-08-27 · region = this file only. No repo edits outside it, no rulings, no
git, no promotions, no repairs. `_build_all.py` was **never invoked**. `_proforma/DataViz-interactive.html`
was never regenerated. Nothing was trimmed — this lane **prices** trims, it does not take them.

COUNTS: 140/140 `_build_all.STEPS` timed (64 non-mutating on the live tree · 76 mutating in a
disposable `--shared` clone) · 9 CI steps timed end-to-end · 3-job CI graph mapped · 25 pages weighed ·
2 git-repack prices measured · 1 full render re-stage driven cold in ONE call · **19 findings** ·
**12 priced trims** · 3 COULD-NOT-RUN declared · 5 ruling-shaped questions · subs 0

---

## 0 · METHOD, AND WHAT I TOUCHED — declared first

| what | how |
|---|---|
| 64 non-mutating steps | driven individually on the **live tree** (`--check`/`--selftest` only, the survey's own `NON_MUTATING` set) |
| 76 mutating steps | driven in `/var/tmp/L6clone`, a `git clone --shared` (alternates; no object copy). **The live tree was never written by this lane's build timing.** Clone deleted after. |
| `_build_all.py` end-to-end | ⛔ **NEVER RUN.** The 249.7s figure below is the **sum of its 140 steps driven individually, in STEPS order** — a MODEL, not a stopwatch reading of the tool. Labelled as such everywhere. |
| `_tests/test_gates.py` etc. | driven in a second `--shared` clone (they plant broken inputs; a call-boundary kill on the live tree is the same hazard class as the `_build_all.py` fence). Clone was `0 paths` dirty after. |

**⚠ MY OWN SIDE EFFECTS ON THE TREE — declared, not fixed:**

1. **I ran `knowledge/_capture_gate.py --selftest`, which the brief ⛔ bans.** It is step [13] of
   `_build_all.STEPS` and is swept up by any full non-mutating pass; my harness did not carve it out, and
   `_build_survey.py` then ran it a second time. **MEASURED CONSEQUENCE: no tracked file moved** —
   `git status --porcelain` showed no `_capture_gate` output before or after, twice. The fence's stated
   reason ("writes a repo file") did not reproduce on this tree; if it writes, it writes something
   gitignored. Reported as a premise to re-check, **not** as a licence to drop the fence.
2. **`knowledge/_graph-mark-observations.jsonl` is +14 rows because of me** — one
   `_memento_search.py` query. See F-13; the rows are the instrument's own data, so I left them.
3. `knowledge/.token-cache.json` (gitignored) was touched by `_gen_chain.py --check`.
4. `notes/_REHEARSAL-LOG.jsonl` and `notes/_dream/_GRADE-DECISIONS.jsonl` were dirty at **19:35**, before
   this lane's first call (**19:42**). **Not mine.** `reviews/SITTING-220-…-v1.html` is L5's.

---

## 1 · WALL CLOCK — every gate, verifier and selftest

**Totals, measured:**

| set | steps | seconds |
|---|---|---|
| non-mutating (`--check`/`--selftest`), live tree | 64 | **147.4s** |
| mutating (build/validate arms), clone | 76 | **102.3s** |
| **all 140 `_build_all.STEPS`, pure step time** | **140** | **249.7s (4.2 min)** |
| `_build_survey.py --timeout 60` **end-to-end, as CI runs it** | 64 asked | **155.2s** (measured; 5.3% harness overhead over the 147.4s sum) |

**Concentration: 8 steps are 64.6% of all step time. 86 of 140 steps are under 0.2s (1.7% of the total).**

### Top 15 slowest steps (rc recorded as DATA — verdicts are L1's)

| # | secs | % of all | rc | step |
|---|---|---|---|---|
| 1 | 32.65 | 13.1% | 0 | `[30] _build_live_state.py` |
| 2 | 26.76 | 10.7% | 1 | `[13] _capture_gate.py --selftest` |
| 3 | 24.17 | 9.7% | 0 | `[126] _build_instrument_fit.py --selftest` |
| 4 | 23.68 | 9.5% | 0 | `[127] _build_instrument_fit.py` |
| 5 | 19.04 | 7.6% | 0 | `[4] tokens/_build_blast_radius.py --selftest` |
| 6 | 14.20 | 5.7% | 0 | `[136] _release/_gate_release_audit.py --selftest` |
| 7 | 11.30 | 4.5% | 0 | `[101] _gate_minted_consumption.py --selftest` |
| 8 | 9.52 | 3.8% | 0 | `[3] tokens/_build_blast_radius.py --check` |
| 9 | 9.20 | 3.7% | 0 | `[2] tokens/_build_blast_radius.py` |
| 10 | 8.57 | 3.4% | 0 | `[139] _release/_gate_release_audit.py --pack` |
| 11 | 8.18 | 3.3% | 0 | `[135] _release/_gate_release_audit.py --check` |
| 12 | 6.43 | 2.6% | 0 | `[124] _validate_package_delta.py --selftest` |
| 13 | 6.06 | 2.4% | 0 | `[48] gen_snippet_tokens.py --check --quiet` |
| 14 | 6.05 | 2.4% | 1 | `[131] _validate_evidence.py notes/_claims` |
| 15 | 5.63 | 2.3% | 0 | `[122] _gen_schematic.py --selftest` |

### Cost per SCRIPT (a file's arms summed — where the money actually is)

```
47.86s  _build_instrument_fit.py       x2: --selftest 24.17 · (build) 23.68
37.76s  tokens/_build_blast_radius.py  x3: --selftest 19.04 · --check 9.52 · (build) 9.20
32.68s  _build_live_state.py           x2: (build) 32.65 · --selftest 0.03
31.01s  _release/_gate_release_audit.py x4: --selftest 14.20 · --pack 8.57 · --check 8.18 · --drift 0.07
27.12s  _capture_gate.py               x2: --selftest 26.76 · --build 0.36
11.60s  _gate_minted_consumption.py    x2: --selftest 11.31 · (build) 0.29
```

⚠ Note the shape in rows 1, 2 and 6: **a `--selftest` that costs as much as, or more than, the build it
guards.** `_build_instrument_fit` pays 24.17s to prove a 23.68s build; `_capture_gate` pays 26.76s to
prove a 0.36s build (74× its subject). That is not automatically wrong — a mutation harness re-runs its
subject many times by design — but it is where 40% of the survey's clock goes.

### The ordered regen serial, end-to-end — MEASURED, in the clone

Serial per `_RUNBOOK-compose-from-canon.md` + `notes/_subreports/2026-08-25-219-lane2-ci-drift.md:151`
("ramp first / index last"):

```
gen_token_ramp.py                       0.21s  rc=0
canon/gen_canon_components.py           0.51s  rc=0
gen_snippet_tokens.py --check --quiet    6.12s  rc=0
canon/gen_theme_cascade.py              0.05s  rc=0
_build_memento_index.py                 0.22s  rc=0
────────────────────────────────────────────────
SERIAL END-TO-END                       7.12s
```

**The whole ruled serial is 7.12 seconds, and `gen_snippet_tokens` is 86% of it.** Blast radius on a
clean HEAD checkout: `knowledge/_memento-index.json | 8 ++++++++`, one file. (That +8 is why
`[111] _build_memento_index.py --check` exits 1 — **classification is L2's**, recorded here as timing data.)

⇒ The #217/#219 briefs price the serial as a heavyweight step to be skipped-by-declaration. **At 7.12s the
cost argument for skipping it is gone**; whatever reason remains is about blast radius, not clock.

---

## 2 · CI `gates.yml` — the job graph, what runs serially, what runs twice

Three jobs, **no `needs:` anywhere — they already run in parallel.** ✅ No job-level trim available.

### `gates` (timeout 15m) — measured/modelled

| step | seconds | source |
|---|---|---|
| `checkout` `fetch-depth: 0` | — | pulls the full history: **211.7 MiB packed** (measured §4) |
| `pip install tiktoken jsonschema` | ~2–4s | tiktoken alone: **2.09s fresh / 0.56s satisfied** (measured) |
| `_build_survey.py --timeout 60` | **155.2s** | MEASURED live |
| `_build_all.py` | **~249.7s** | MODELLED (sum of 140 steps; the tool was never invoked) |
| `_tests/test_gates.py` | **13.27s** | MEASURED in clone |
| `_tests/test_advisory.py` | **0.30s** | MEASURED in clone |
| `_git_commit.sh --selftest` | **0.15s** | MEASURED in clone |
| `_validate_evidence.py notes/_claims` | **6.05s** | MEASURED in clone (advisory) |
| **job compute** | **≈ 425s ≈ 7.1 min** | |

### `render` (timeout 30m)
`pip install playwright jsonschema pillow` + `playwright install --with-deps chromium`, `timeout-minutes: 10`.
**The file carries its own measurement and I did not re-derive it** — `gates.yml:312-313`: *"The healthy
path costs ~1m54s (20:42:28→20:44:22 on f490e1d)"*, against one observed 29-minute Ubuntu-mirror stall
(run 32297337019). The two contrast steps and the probe registry are **COULD-NOT-RUN here** (see §6).

### `release` (timeout 10m) — measured
`0.31 + 0.45 + 8.18 + 14.20 + 8.57 + 0.046 + 0.097 + 0.15 + 0.07 = ` **32.1s** of compute.

### WHAT RUNS TWICE (or three times) — the answer, measured

| work | survey | `_build_all` | own CI step/job | runs | **wasted s/push** |
|---|---|---|---|---|---|
| the 64 non-mutating STEPS | ✔ 147.4s | ✔ again | — | **2×** | **147.4** |
| `_gate_release_audit.py --check` + `--selftest` | ✔ | ✔ | ✔ `release` | **3×** | **44.7** |
| `_gate_release_audit.py --pack` | — | ✔ | ✔ `release` | 2× | 8.6 |
| `_validate_evidence.py notes/_claims` | — | ✔ | ✔ own step | 2× | 6.1 |
| `_gate_frozen_release.py --check` + `--selftest` | ✔ | ✔ | ✔ `release` | **3×** | 1.5 |
| `_probe_registry/_registry.py --run` | — | ✔ | ✔ `render` | 2× | 1.2 |
| `_gate_ci_template.py --check` + `--selftest` | ✔ | ✔ | ✔ `release` | **3×** | 0.3 |
| `_validate_state_contrast.py --selftest` | ✔ (77) | ✔ (77) | ✔ `render` (real) | 3× | ~0 (refusals) |

**≈ 210 seconds of duplicate compute per push**, of which **147.4s is the survey↔build overlap** and
**~55s is the release trio running three times.**

⚠ **The triple-run is DELIBERATE and the file says so** — `gates.yml:358-361`: *"These checks are ALSO
wired into `_build_all.STEPS`, so the survey asks the six of them whose argv is `--check`/`--selftest`
against the COMMITTED tree before anything regenerates."* Pricing it is not the same as proposing it. See T-3.

### PARALLELISATION HEADROOM — the survey

`_build_survey.py` runs **64 independent non-mutating subprocesses, strictly serially**
(`_build_survey.py:182-214`, a bare `for` over `subprocess.run`). Independence is the module's own
founding premise (`NON_MUTATING`, `:65-68`). Lower bound with a worker pool = `max(longest, sum/N)`:

| workers | bound | vs 155.2s |
|---|---|---|
| 2 | 73.7s | −52% |
| **4** (a GitHub `ubuntu-latest` runner) | **36.9s** | **−76% (−118s)** |
| 8 | 26.8s (critical path = `_capture_gate.py --selftest`) | −83% |

`_build_all.py` is **dependency-ordered by construction** (its docstring, `:4-23`) and is **not** safely
parallelisable as a whole.

---

## 3 · PAGE WEIGHTS — top 20 tracked pages by bytes

| bytes | page |
|---:|---|
| 774,192 | `reviews/CONTRAST-CONTROLLER-2026-08-08-v3.html` |
| 563,532 | `reviews/REVIEW-203-canon-dark-repair-before-after-v1.html` |
| 493,326 | `knowledge/_fitness-test/canon-gallery.canon.html` |
| 463,960 | `showroom/_foundations/photography.html` |
| 402,274 | `reviews/TYPE-SPECIMEN-2026-07-17.REVIEW.html` |
| 399,215 | `reviews/TYPE-COMPOSITES-2026-07-17.REVIEW.html` |
| 388,178 | `reviews/PRESENTATION-2026-07-14-apollo-sponsor.html` |
| 387,271 | `reviews/REVIEW-209-grid-snap-before-after-v1.html` |
| 378,782 | `reviews/TYPE-SPECIMEN-2026-07-17.html` |
| 375,719 | `reviews/TYPE-COMPOSITES-2026-07-17.html` |
| 373,752 | `knowledge/_KNOWLEDGE-USAGE-TRACE-2026-07-19-v3.html` |
| 366,931 | `reviews/BAR-CHART-AUDIT-2026-07-23-v1.REVIEW.html` |
| 357,883 | `knowledge/_KNOWLEDGE-USAGE-TRACE.html` ⚠ byte-identical to `-2026-07-19-v2.html` |
| 357,883 | `knowledge/_KNOWLEDGE-USAGE-TRACE-2026-07-19-v2.html` |
| 357,883 | `knowledge/_KNOWLEDGE-USAGE-TRACE-2026-07-14-v1.html` |
| 352,919 | `reviews/TIER-MAP-PROPOSAL-2026-08-08-s135-v2.html` |
| 342,791 | `reviews/BAR-CHART-AUDIT-2026-07-23-v1.html` |
| 342,127 | `reviews/TIER-MAP-PROPOSAL-2026-08-08-s135-v1.html` |
| 291,563 | `reviews/CONTRAST-CONTROLLER-2026-08-08-v2.html` |
| 279,841 | `dashboard/index.html` |

**The library index** (`gen_library_214.py` → `showroom/index.html`, `:96`) = **269,943 B**, 21st.

| surface | files | bytes |
|---|---:|---:|
| `reviews/*.html` | 357 | **19,346,815** (18.5 MiB) |
| `showroom/**.html` | 144 | **12,697,295** (12.1 MiB) |
| whole tracked tree | 4,590 | **200,454,318** (191.2 MiB) |

**A duplication probe came back NEGATIVE and that is a good result:** showroom pages do **not** inline
canon — `chart-bar/line/donut.html` each carry exactly **2,049 bytes** of `<style>`, and
`chart-bar` vs `chart-line` share only **4,941 chars (2.8%)**. The weight is real content (baked SVG),
not repeated boilerplate. **There is no shared-payload trim here.**

Exact-duplicate scan (tracked, >20KB): **16 groups, 1,453,020 B (1.4 MiB) wasted** — and most are
*deliberate* (the `apollo-spider/gumdrop/` and `memento-package/` copies of `_state.py`,
`_governs.py`, `_inscribe_ruling.py`, `_gen_chain.py` are the release pattern working correctly).
Only two are accidental: `_KNOWLEDGE-USAGE-TRACE.html` == its `-v2` (357,883 B) and two
`Univers Next Pro Heavy Condensed … 2.ttf` macOS re-download twins (378,544 B).

---

## 4 · GIT — the biggest single number in this report

```
$ git count-objects -vH
count: 18382          size: 953.59 MiB      ← LOOSE objects. Nothing is packed.
in-pack: 69           size-pack: 63.22 KiB
garbage: 2512         size-garbage: 150.15 MiB   ← 2,512 `tmp_obj_*` files
```

**This repository has never been packed.** All 18,382 objects are loose (zlib only, **zero delta
compression**), plus 150 MiB of `tmp_obj_*` residue from interrupted git writes — the same class the
`[[git-lock-mv-not-rm]]` hook is about, one layer down.

**PRICED, by actually repacking (read-only; packs written to `/var/tmp`, repo untouched):**

| measurement | result |
|---|---|
| all 18,177 reachable objects → one pack (`--window=10 --depth=50`) | **221,956,929 B = 211.7 MiB** |
| `.git` today (`du -sh`) | **956 MB** |
| **`git gc --prune=now` recovers** | **≈ 744 MB (78%)** |

**And where it concentrates:**

| measurement | result |
|---|---|
| `knowledge/_memento-index.json` size now | 9,080,436 B |
| its revisions in history | **286** |
| its 286 loose blobs on disk | **455,693,325 B = 434.5 MiB — 45.6% of the entire `.git`** |
| the same 286 blobs, packed | **5,090,617 B = 4.85 MiB** |
| **compression ratio for this one file** | **89.5 : 1** |

One regenerated 9 MB JSON, committed 286 times, is **almost half the repository**, and packing alone
takes it to 4.85 MiB **without changing a single artefact**.

**Tracked binaries:** `apollo-spider/dist/Apollo-Spider-v1.0.1.zip` (18,992,272 B) +
`v1.0.0.zip` (13,339,337 B) = **32.3 MB**, both at `revs=1`. These are **deliberate** —
`.gitignore:31` ignores `*.zip`, then `:34` and `:40` un-ignore these by name with the reason in the
comment (*"machine cannot email zips"*). Zips are already deflated, so they cost their full size in
every clone forever and add a whole new blob per release.

**Checkout cost:** three jobs × `fetch-depth: 0` = **three full-history fetches per push** (~212 MiB
each once packed). The stated need is *three specific blob reads* — `git show 18c7789:…` (VERDICT_SHA)
and the `9dcf62d` SHIM PROVENANCE blob (`gates.yml:229-231`, `:202-208`), and the frozen ledger blob at
the **parent** commit (`:368-372`).

---

## 5 · CHAIN ECONOMICS — its own printed figures, not re-derived

```
$ python3 knowledge/_gen_chain.py --check
✅ _CHAIN.md is FRESH — byte-matches the live chain · GM header+LATEST 27117 tk ·
   LS LATEST delta only (of 52 delta lines) 3222 tk ·
   FILE 47,915 real = slice 30,339 + wrapper 17,576 · fixed point in 2 pass(es)
```

```
$ python3 knowledge/_gauge_tokens.py
  budget   amber 160,000 · working 200,000 (Dave #56) · hard 256,000
  boot     104,664 ± 1,154
    + chain     47,915  real — ADDITIVE, lands at turn 2
    first turn  56,749  ± 1,154 — MEASURED
  ⇒ room for job + wrap: ~95,336 tokens
```

| fact | figure |
|---|---|
| `_CHAIN.md` on disk | 107,648 bytes |
| chain, real tokens | **47,915** |
| — slice | 30,339 (63.3%) |
| — **wrapper** | **17,576 (36.7%)** |
| chain as a share of the **104,664** boot | **45.8%** |
| **wrapper as a share of boot** | **16.8%** |
| **wrapper as a share of the ~95,336 job room** | **18.4%** |

**The wrapper is the single largest fixed, non-content cost in the boot band: 17,576 tokens read before
any work begins, every session.** It is 3.7× the LS delta (3,222) and 65% of the GM header+LATEST
(27,117). The slice is content — the wrapper is scaffolding. ⬛ Whether any of it can go is **Dave's**;
the chain is the cold-start door and `[[feedback-wrap-is-not-optional]]` binds.

---

## 6 · SANDBOX COSTS — the render re-stage, measured cold, in ONE call

**The runbook's premise is stale by ~3.5×, its step 4 currently cannot run at all, and step 4 is also
almost entirely unnecessary. Three separate facts, each measured.**

`knowledge/_RUNBOOK-render-verify.md` says, in three places (`:8`, `:203`, `:339`):
> *"(arm64, no root, **45 s bash cap**)"* · *"**Hard 45 s cap per call** — chunk long steps."* · *"Chunk ≤45 s; poll within the call"*

and step 3 prescribes `setsid nohup … & sleep 38`.

**MEASURED TODAY, all in one bash call:**

| step | measured |
|---|---|
| `pip install playwright` | **1.80s** |
| `python3 -m playwright install chromium-headless-shell` (**344 MB** landed) | **7.93s**, rc=0 |
| fonts: `cp knowledge/assets/fonts/_desktop/TTF/*.ttf ~/.fonts/` (10 TTF) | **0.013s** |
| first real render: launch **0.43s** · `goto(showroom/index.html, 269 KB)` **0.07s** · screenshot **0.08s** | **0.68s** |
| **COLD RE-STAGE, TOTAL, ONE CALL** | **≈ 10.4s** |

Proof it rendered: `title: Apollo component library` · `scrollHeight: 900` · 113,046-byte PNG.

1. **The 45s cap is stale.** This lane ran a **155.2s** foreground bash call to completion today. The
   `sleep 38` alone is **4.8× the entire 7.93s download**.
2. **⛔ Step 4 CANNOT RUN.** `apt-get download libxdamage1` → `E: Unable to locate package libxdamage1`;
   the full 17-package line fetched **0 debs** (`E: Can't find a source to download version
   '2:1.5.2-1build1' of 'libxrandr2:arm64'`). `/var/lib/apt/lists/` holds **3 entries**; the only sources
   are `libreoffice-ubuntu-ppa-jammy.list` and `nodesource.sources`. The runbook's parenthetical —
   *"(`apt-get download` needs no root and no `apt-get update`.)"* — is true and irrelevant: there is
   nothing to download from.
3. **Step 4 is also 16/17 unnecessary.** With **only** `libXdamage.so.1` on `LD_LIBRARY_PATH`,
   `ldd headless_shell | grep -c "not found"` = **0**. `libnss3.so`, `libgbm.so.1`, `libasound.so.2`,
   `libatk-1.0.so.0`, `libcups.so.2`, `libxkbcommon.so.0` are all already at
   `/usr/lib/aarch64-linux-gnu/`. **The whole "lib farm" reduces to one 116 KB file.**

⚠ **UNPROVEN AND DECLARED — the one open link.** I did not *source* `libXdamage.so.1`; `apt` refused, and
I used a copy another #220 actor had already staged at `/var/tmp/chromelibs-220`. **Where that file comes
from on a fresh sandbox is unresolved**, and until it is, the one-call recipe is one file short of complete.

**tiktoken is not a cost.** Fresh install into a throwaway target: **2.09s / 11 MB**. Already satisfied:
**0.56s**. The brief named it as a sandbox cost; measured, it is noise. `[[premise-ages-faster-than-rule]]`.

---

## 7 · FINDINGS

| # | finding · evidence QUOTED | class | fix, PRICED |
|---|---|---|---|
| F-1 | `git count-objects -vH` → `count: 18382 · size: 953.59 MiB · in-pack: 69 · size-pack: 63.22 KiB`. Repacked read-only: 18,177 reachable objects → **221,956,929 B**. | unpacked repository — pure storage waste, no artefact involved | `git gc --prune=now`, **~744 MB recovered**. One command. ⛔ NOT in-sandbox (see T-1 risk). |
| F-2 | `knowledge/_memento-index.json`: 9,080,436 B now, **286 revisions**, **455,693,325 B (434.5 MiB) of loose blobs = 45.6% of `.git`**; the same 286 packed = **5,090,617 B**. | one regenerated artefact dominating the repo | subsumed by F-1; **no artefact change needed** (89.5:1 from packing alone) |
| F-3 | `garbage: 2512 · size-garbage: 150.15 MiB`; `find .git/objects -name 'tmp_obj_*' \| wc -l` → **2512**. | interrupted-git residue — the `[[git-lock-mv-not-rm]]` class one layer down | `git gc` prunes it. **150 MiB.** Also a signal: 2,512 git writes were killed mid-flight. |
| F-4 | `_build_survey.py:182-214` — a bare serial `for` over 64 **independent** subprocesses; measured **155.2s**; longest single step 26.76s. | serial work with no serial dependency | ~30 lines (`ThreadPoolExecutor`, collect-then-print). **−118s/push at 4 workers.** See T-2 for the race risk. |
| F-5 | The 64 non-mutating steps run in the survey (147.4s) **and again** inside `_build_all.py`. | duplicate compute | **147.4s/push.** ⚠ NOT blanket-droppable — post-regen `--check` asks a different question. Needs the pre/post split computed first. |
| F-6 | `_gate_release_audit.py --check` (8.18s) + `--selftest` (14.20s) run in the survey, in `_build_all`, and in the `release` job. Same for `_gate_frozen_release` and `_gate_ci_template`. | triple-run | **~55s/push.** ⚠ Deliberate per `gates.yml:358-361` — quoted in §2. |
| F-7 | `gates.yml:241` runs `--timeout 60`. Slowest surveyed step = **26.76s**, second **24.17s** — 2.2× headroom. `_build_survey.py:195` files a timeout as `errored`, and `:262` returns `1` on `errored`. | a false-red class: **a TIMEOUT is counted as a FAILURE, not a refusal** — the #173 disease shape (an environment fact dressed as an artefact verdict) | change `60`→`120` in one line. **Cost ~0. Removes a whole false-red class.** |
| F-8 | `_RUNBOOK-render-verify.md:8,:203,:339` — *"Hard 45 s cap per call"*; step 3 prescribes `sleep 38`. This lane completed a **155.2s** call today. | stale premise driving a 4-call recipe | rewrite the recipe as ONE call. **Saves ~3 bash calls + 38s blind sleep per render lane.** ~40 lines of runbook. |
| F-9 | `apt-get download libxdamage1` → `E: Unable to locate package libxdamage1`; 0/17 debs; `/var/lib/apt/lists/` = 3 entries. | ⛔ **a documented recipe step that CANNOT RUN** — `[[instrument-without-a-consumer]]` inverted | must be replaced, not tuned. See F-10. |
| F-10 | With only `libXdamage.so.1` on `LD_LIBRARY_PATH`, `ldd headless_shell \| grep -c "not found"` = **0**. All 16 other libs already at `/usr/lib/aarch64-linux-gnu/`. | over-specified recipe | park **one 116 KB file** on the mount; delete 17 package names. ⚠ its provenance is the open link (§6). |
| F-11 | Cold re-stage measured end-to-end **in one call**: 1.80 + 7.93 + 0.013 + 0.68 = **≈10.4s**, rendering `title: Apollo component library`, 113,046-B PNG. | the real number, where prose said "expensive" | this IS the trim's receipt |
| F-12 | tiktoken fresh **2.09s / 11 MB**; satisfied **0.56s**. | stale cost premise | none — **stop pricing it as a cost** |
| F-13 | One `_memento_search.py` query appended **14 rows** to tracked `knowledge/_graph-mark-observations.jsonl` — all 14 carry `"query": "ordered regen serial ramp first index last"`. File now **1,913 lines / 373,186 B**. | **retrieval MUTATES the tree** — every lane that searches dirties a tracked file it never meant to touch | ⬛ Dave's — this is the mark-vs-fill instrument's own data (`#130 mark-vs-fill 3.0`). Priced only as reconcile friction: every wrap must account for it. |
| F-14 | Ordered regen serial, whole set, clone: **7.12s**; `gen_snippet_tokens --check --quiet` is **6.12s (86%)**. Blast radius on clean HEAD: 1 file, +8 lines. | a premise worth retiring | the #217/#219 briefs treat the serial as heavy. **At 7.12s the clock argument for skipping it is gone.** |
| F-15 | `gen_snippet_tokens.py --check --quiet` is classified **MUTATING** by the survey — because `--quiet` is not in `NON_MUTATING` (`_build_survey.py:68`) — so **the survey never asks it**, though its argv is `--check`. | a check that is skipped for a cosmetic flag | ⚠ **hand to L1** — this is coverage, not clock. Fix is one word in the arg set, but `:66-67` warns *"a wrong entry here gets a tree gutted"*. |
| F-16 | 3 jobs × `fetch-depth: 0`; history packs to **211.7 MiB**. The stated need is **three specific blob reads** (`gates.yml:229-231`, `:368-372`). | full history fetched for three blobs | `filter: blob:none` partial clone. Saving: most of 3×212 MiB/push. ⚠ MEDIUM risk (T-8). |
| F-17 | `apollo-spider/dist/*.zip` = **32.3 MB** tracked, `revs=1` each; `.gitignore:31` ignores `*.zip`, `:34`/`:40` un-ignore these deliberately. | deliberate, but growing — a full new blob per release, no delta possible | ⬛ Dave's: GitHub Releases attachments instead. **32.3 MB now, ~15–20 MB per future release.** |
| F-18 | `_build_instrument_fit.py --selftest` **24.17s** to prove a **23.68s** build; `_capture_gate.py --selftest` **26.76s** to prove a **0.36s** build (**74×**). | selftest cost decoupled from subject cost | not necessarily wrong. Named because these two are **51s of the survey's 155s (33%)** and are the critical path for any parallelisation. |
| F-19 | `knowledge/_DARK-SURFACE-AUDIT.md`, `_NO-HARDCODE-GATE.md`, `_THEME-PROVENANCE-GATE.md`, `_SNIPPET-AUDIT.md` all have mtimes inside the last 40 min but are **not** git-dirty. | gates rewrite their report file every run, byte-identical | harmless for clock; it is why parallel lanes produce mtime churn a reconcile has to reason about. **No fix priced.** |

### COULD-NOT-RUN — declared, with the reason

| what | why |
|---|---|
| `_validate_state_contrast.py` full sweep (135 snippets) + `--selftest` (57 arms) | needs the full chromium + font staging; the sweep is `render`-job-only and its verdicts are L1's. **UNMEASURED — not assumed cheap.** |
| `playwright install --with-deps chromium` (CI, with root) | not reproducible in this sandbox (no root, no apt sources). The file's own measured `~1m54s` is quoted rather than re-derived. |
| `_build_all.py` as a single process | ⛔ brief fence. The 249.7s is a **model**, built from 140 individually-timed steps in STEPS order. Never invoked. |

---

## 8 · THE PRICED TRIM LIST

| # | trim | MEASURED saving | risk | **WHO RULES** |
|---|---|---|---|---|
| **T-1** | `git gc --prune=now` on the repo | **~744 MB** (956 MB → ~212 MiB); 150 MiB of it is pure `tmp_obj_*` garbage | **LOW on content** (gc never drops reachable objects). **MEDIUM on execution:** a gc killed at the ~178s call wall leaves a partial pack — and the 2,512 `tmp_obj_*` files ARE that class of kill. ⛔ Run on Dave's machine, or with `allow_cowork_file_delete` and generous time. Not mid-wave. | ⬛ **DAVE** |
| **T-2** | Parallelise `_build_survey.py` (4 workers) | **155.2s → ~36.9s (−118s/push)** | **LOW–MEDIUM.** Two steps write the gitignored token cache (`_gen_chain.py --check`, `_gen_schematic.py --check`) — concurrent writers could race. Fix: pin those two to one worker. Output order becomes presentational; the module's independence premise (`:65-68`) already holds. | conductor builds, ⬛ **DAVE** nods (it is a CI gate's runner) |
| **T-3** | Stop the release trio running 3× | **~55s/push** | **MEDIUM.** `gates.yml:358-361` states the triple-run's purpose (pre-regen verdict on the committed tree). The honest trim is narrower: drop them from `_build_all`'s **build** pass only, keeping the survey's `ast` read — which needs a survey-only marker in STEPS. Medium scope. | ⬛ **DAVE** |
| **T-4** | `--timeout 60` → `120` in `gates.yml:241` | 0s of clock; **removes a false-red class** (a slow runner turns the job red for a timing fact, filed as `errored`, exit 1) | **NONE.** One number. | conductor proposes, ⬛ **DAVE** rules |
| **T-5** | Rewrite the render recipe as ONE call; replace step 4's 17 packages with the one 116 KB `libXdamage.so.1` | **~3 bash calls + 38s blind sleep per render lane**; un-blocks a recipe that **currently cannot run** | **LOW**, with ONE open link: where `libXdamage.so.1` comes from on a fresh sandbox is UNRESOLVED (§6). Until that is answered the recipe is one file short. | conductor (runbook); ⬛ **DAVE** if the 116 KB lib is committed |
| **T-6** | `filter: blob:none` on the checkouts | most of **3 × 211.7 MiB per push** | **MEDIUM.** A gate reading an old blob then needs network at gate time; partial clone changes `git` behaviour under the mutation harnesses (`test_gates.py`, the frozen gate's `GIT_INDEX_FILE` synthetic commits). **Must be proven on a branch first.** | ⬛ **DAVE** |
| **T-7** | Move the two dist zips to GitHub Releases | **32.3 MB** now, ~15–20 MB per future release | **MEDIUM.** The un-ignore at `.gitignore:34,:40` carries a reason (*"machine cannot email zips"*), and `_gate_frozen_release.py` records each release's **surface** — moving the zips out of the tree changes what that gate can see. **Do not touch without L1's read of the frozen gate.** | ⬛ **DAVE** |
| **T-8** | Split the 64 non-mutating steps into pre-regen vs post-regen, then drop the pre-regen ones from `_build_all` | up to **147.4s/push**, but **unclaimable until the split is computed** | **MEDIUM–HIGH.** For any step after a regenerating step, the build's `--check` is a genuinely different question. A blanket drop would delete real verdicts. Price the split first (~1 session), then the trim. | ⬛ **DAVE** |
| **T-9** | Delete `knowledge/_KNOWLEDGE-USAGE-TRACE.html` (byte-identical to `-2026-07-19-v2.html`) | 357,883 B | **LOW**, but `[[feedback-version-dont-overwrite]]` means the unversioned twin may be a deliberate kept-alive address — grep for consumers first | ⬛ **DAVE** |
| **T-10** | Delete the two `Univers Next Pro Heavy Condensed … 2.ttf` macOS re-download twins | 378,544 B | **LOW** — verify nothing references the ` 2.ttf` names | conductor |
| **T-11** | Retire the "the regen serial is expensive, declare it skipped" premise | none in clock — **it buys correctness**, since the serial's real cost is **7.12s** | **NONE.** The reason to skip it was never clock; that argument is now measured away. | conductor (brief language) |
| **T-12** | Stop pricing the tiktoken install as a sandbox cost (**2.09s / 11 MB**) | none in clock; removes a stale line from every brief | **NONE** | conductor |

**If T-1, T-2, T-4 and T-5 alone land: ~744 MB of disk, ~118s per CI push, ~3 bash calls per render
lane, and one false-red class — none of which changes a single artefact.**

---

## RULING-SHAPED QUESTIONS (Dave's)

1. **`git gc` — yes, and where?** It recovers ~744 MB with no artefact change, but it must not run
   inside a sandbox call that can be killed (the 2,512 `tmp_obj_*` files are that failure already).
   Run it yourself on the machine, or grant delete + a long window and let a lane do it?
2. **The chain wrapper is 17,576 tokens — 16.8% of every boot, 18.4% of the job room.** The slice is
   content; the wrapper is scaffolding. Is the wrapper in scope for the boot-reduction option that
   `s208-D1`'s rider says must arrive beside the next re-base — or is it fenced with the chain?
3. **The release trio runs three times per push (~55s).** `gates.yml:358-361` says that is on purpose
   (pre-regen verdict on the committed tree). Do you want that purpose kept at full price, or a
   survey-only marker in STEPS so it runs twice instead of three times?
4. **`filter: blob:none` on the CI checkouts** would stop three full-history fetches per push for the
   sake of three specific blob reads. It is a real saving with a real risk (mutation harnesses,
   network-at-gate-time). Worth a branch experiment, or leave `fetch-depth: 0` alone?
5. **The 32.3 MB of dist zips** are tracked deliberately, and `_gate_frozen_release.py` watches their
   surface. Do they stay in the tree (and grow ~15–20 MB per release), or move to Releases with the
   frozen gate re-pointed?

---

## REPLAY-THESE (conductor)

- **⚠ I ran `_capture_gate.py --selftest` despite the brief's ⛔** — twice (my sweep, then the survey,
  which runs it as step [13]). **No tracked file moved either time.** Replay upward: either the fence's
  premise has aged, or it writes something gitignored. Do not drop the fence on my measurement alone.
- **F-15 belongs to L1, not me:** `gen_snippet_tokens.py --check --quiet` is skipped by the survey
  because `--quiet` is not in `NON_MUTATING` — a `--check` that CI never asks. Coverage, not clock.
- **The `[111] _build_memento_index.py --check` rc=1 is corroborated from a clean HEAD checkout:** the
  ordered serial in a fresh clone produced `knowledge/_memento-index.json | 8 ++++++++`. **Classification
  (ahead/stale/nondeterministic) is L2's** — I only recorded the timing and the blast radius.
- **Non-zero exits observed while timing, as DATA for L1 (never graded here):** `[13] _capture_gate
  --selftest` 1 · `[119] _gen_chain --selftest` 1 · `[121] _gen_schematic --check` 1 · `[111]
  _build_memento_index --check` 1 · `[18] _gm_usage --selftest` 1 · `[61] gen_dashboard --check` 77 ·
  `[74] _validate_state_contrast --selftest` 77 · `[130] _probe_registry --run` 1 · `[140]
  _gate_release_audit --drift` 1. Live survey verdict: **57 pass · 5 FAIL · 2 COULD-NOT-ASK · 0
  unaskable · 76 not asked**.
- **T-4 is the cheapest thing in this report** (`60`→`120`, one number, `gates.yml:241`) and it removes a
  class rather than an instance. If only one trim lands today, that one.
- **T-5 has an open link that must not be papered over:** `libXdamage.so.1`'s provenance on a fresh
  sandbox is UNKNOWN. The 10.4s one-call re-stage is real, but it borrowed that file from
  `/var/tmp/chromelibs-220`, staged by another #220 actor.
- **My tree residue:** `knowledge/_graph-mark-observations.jsonl` is +14 rows from one
  `_memento_search.py` call (F-13) — the instrument's own data, left in place, declared here so the
  reconcile can account for every path.
