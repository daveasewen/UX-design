# 300 · W — the wrap: the boot was measured, and the reading stays

session: `#300` · 2026-09-23 · lane `W` (DELEGATED WRAP) · model Opus 5.5 · conductor Opus 5.5 (cloud)
provenance: 300 · 2026-09-23
status: observed
job: every step of `knowledge/_RUNBOOK-capture-ritual.md` as #299's wrap seat ran it; save his pasted Project instructions verbatim and diff them; commit every #300 file, push, read CI back; place the #300 memory note LAST under his new instructions; the 5b addendum; 4c last. ⛔ **DECLARED DEVIATION, EXPECTED BY THE BRIEF: the TRUE `--wrap` path is refused by the wrap gate's boot-ceiling arm, a condition only Dave can move; the wrap commit goes on the DECLARED not-a-wrap path — § THE GATE BEFORE THE COMMIT.**

## VERDICT

DONE, with the memory step and 5b after the commit as the brief ordered. The session measured the cloud boot four ways and had the measurements attacked (lanes A B C D, adversary X, plan P); the reading at the start stays; before "wrap" Dave pasted the plan's new Project instructions — saved byte-exact here, identical to the page and to the conductor's transcript, differing from lane C's draft at step 4 only. Nothing inscribed (638). One carry headline struck with receipts.

COUNTS: findings 9 · ruling-shaped 5 · UNPROVEN 3

✅ **NO DATE SPLIT.** `date` at this seat: `Wed Sep 23 16:58:14 UTC 2026` (17:58 BST). Session, subs and ritual are all 2026-09-23.

---

## WHAT I VERIFIED OF THE BRIEF

- **Rulings 638** — ✅ `json.load` over `rulings`: `638 s295-D4`, at open.
- **Push state** — ✅ branch `master`; `HEAD` = `origin/master` = `git ls-remote` = `6afe3f8b` (#299's 5b); `git log origin/master..master` empty — **no commits this session before the wrap.** No stale lock at open.
- **FILL** — ✅ **read first-hand in the cloud shell** over `/root/.claude/projects/-home-claude/d75f51cc-494a-56b1-9272-190e7fc46cbb.jsonl` (input + cache_creation + cache_read per distinct `message.id`, 25 distinct messages): boot **126,185** · **157,635** (5th) · **184,309** (11th) · **211,454** (14th, +23,591) — **each of the brief's figures reproduces to the token.** ⚠ The brief's "184,309 at the first reply" is the conductor's own fill reading taken just BEFORE the reply (the 11th message is a tool call that hand-summed its window); the reply itself went out with the 13th at **187,863**. Both published. Later: 250,519 (17th, lanes launched) · 256,949 (18th) · 253,280 (19th, after the stop) · 270,882 (24th, last reply) · **278,666** (25th, 16:57:16 UTC, the message that launched this seat). `_checkin.py --preflight-line 300` on the device: `⛔ NOT CAPTURED — … no top-level transcript matched` — pasted verbatim into the stratum.
- **His words** — ✅ each re-read from the transcript: 15:46, 15:58, 16:08 and 17:56 verbatim as briefed. ⚠ **ONE MORE:** his n-gram question was first sent at **16:16 BST** as a queued note (`queued_command`, 15:16:30 UTC) and is not in the brief's list; the 16:56 message repeats it word for word after his stop (the interrupt at 15:56:22 UTC). Both are in `notes/_lanes/300/WRAP-BRIEF.md`.
- **The new Project instructions** — ✅ **byte-exact three ways**: the brief's quote = the transcript's `session_context` at row 254 (16:56:07 UTC, `changed: true`, arriving with the "wrap" turn) = this seat's own session context; md5 `9d7837a3a5558553d404463adf8e72bc` (text + one newline, the rollback file's convention). The boot `session_context` (row 19) reproduces the rollback's md5 `a6e11674ea1bf0c74ffbd37cb10c5489`. Diff in § THE INSTRUCTIONS DIFF.
- **Sub spend** — hand sum over `…/subagents/agent-*.jsonl`, final FILL: **A 278,853 · B 344,935 · C 306,107 · D 305,928 · X first launch 260,764 · P first launch 218,025 · X 374,835 · P 437,591 = 2,527,038 (n=8)**, this seat excluded. Input processed ~139.0M; the two launches his stop threw away 17,150,335.
- **The #299 hook's placement at #300's opener** — DECLARED by the conductor (file 3,361 B · `index.md` 6,091 B, #299 · #298 · #297 · the #296 line VERBATIM in `MEMORY-ARCHIVE-3.md` § Batch 2026-09-23 #300 (opener), 20,721 B · the Friday area file 4,144 B). This seat touched no memory before the commit; the store is read at the memory step and the result lands in § POST-WRAP.
- **Secrets** — ✅ no GitHub, Anthropic, Slack or AWS token pattern and no email address in any #300 file, the two `.tgz` archives or lane B's `.json.gz` (scan at this seat) before anything was staged.

## THE INSTRUCTIONS DIFF

`notes/_lanes/300/PROJECT-INSTRUCTIONS-v2-LIVE.md` (with the verbatim text) and `.txt` (byte-exact). python `difflib`, `n=0`:
- **against the plan page's paste-ready block** (HTML-unescaped): **IDENTICAL** — he pasted the page's text.
- **against lane C's v2 draft** (report § 6): **step 4 only** — lane P's edit, flagged on the page (C: *"Only if the handoff says it was NOT placed, place it through a sub."* → live: *"… or anywhere while the chat is live … if it was not placed, leave it — the repo keeps every note at notes/_lanes/<n>/WRAP-MEMORY-HOOK.md."*).
- **against the rollback copy**: the first paragraph names the folder exactly · the opener's seven lines become five numbered steps, dropping the memory list and `index.md` read, the `pip`/`_checkin.py` line and the opener's hook placement · Standing gains the cloud-seat check-in sentence · Rendering unchanged.

## THE GATE AT OPEN

```
capture gate [wrap]: 237 in scope · 1 fail · 67 warn
```

The one fail is the boot ceiling: *"❌ FAIL boot-drift CEILING BREACH: `_gauge_tokens.BOOT_CEILING_TK` = 72,768 and 2 post-diet reading(s) EXCEED it — #297 127,600 · #298 127,661."* The new warns: the six #300 reports uncited (cleared by the ⏱ delta's citations, which the chain carries), 17 `s218-D7` form warns on five of them, and 90 uncommitted paths. `s294-D12` did not fire. Log: `notes/_lanes/300/W/_gate-open.log`. ⚠ **A first attempt, backgrounded with `nohup … &`, died with the call** — `device_bash` runs each call in a `bwrap --die-with-parent` sandbox — and left an empty log; re-run in the foreground (≈2 minutes). ⚠ **The run stranded `.git/index.lock`** (the fourth wrap running) — moved to `notes/_lanes/_orphan-locks/index.lock.300-W-1`.

## THE GATE BEFORE THE COMMIT

```
capture gate [wrap]: 238 in scope · 1 fail · 66 warn
```

The one fail is the boot ceiling, now with THREE cloud readings — 2f rolled #299's stratum into `notes/_GAUGE-LOG.md`: *"❌ FAIL boot-drift CEILING BREACH: `_gauge_tokens.BOOT_CEILING_TK` = 72,768 and 3 post-diet reading(s) EXCEED it — #297 127,600 · #298 127,661 · #299 126,178. ⛔ `s240-D2`/`s241-D1` make this number SHRINK-ONLY …"*. The `s244-D1` discharge form needs a LATER reading under the ceiling, and none exists (cloud boots read ~126–128K; #300's 126,185 joins them when this stratum rolls). **So the wrap commit is made with `_git_commit.sh` WITHOUT `--wrap`** — the DECLARED not-a-wrap path (#74-D1), as at #298 and #299 — with `SESSION_N=300` and the reason in the message body. ✅ **The six #300 reports are cited** (the ⏱ delta's WHY/HOW line, carried by `_CHAIN.md`). ⚠ Five advisory warns name #299's filed reports (C, D, E, R, W) as uncited — they are cited in #299's own record, now ★ PRIOR; the gate measures "since the last wrap" from `c0ad1a8`, #297's true `--wrap` commit. Not repaired. ⚠ **This run stranded `.git/index.lock` too** (≈40 s this time): moved as `index.lock.300-W-2`; `git reset -q` then stranded `HEAD.lock` and `refs/heads/master.lock`, moved as `*.300-W-3`. Log: `notes/_lanes/300/W/_gate-prewrap.log`.

## THE RITUAL, STEP BY STEP

**1 / 1b.** `_LIVE-STATE.md`: a new `*Last refreshed: 2026-09-23 … #300 wrap*` stamp and a ⏱ LATEST delta citing all six lane reports and this one by path; #299's renamed ⏱ PRIOR. Dossier `_DECISION-HISTORY/2026-09-23-300-the-boot-was-measured-and-the-reading-stays.md`. Cited upload: the Cowork `Selected_folders.txt` of 15:46, saved verbatim at the foot of `notes/_lanes/300/WRAP-BRIEF.md`. His pasted instructions saved byte-exact.

**2.** No date split. New ★ LATEST banner; #299's renamed ★ PRIOR; title → `Apollo - #301: the ask on slide 15, then polish for friday`.

**2c.** #298's ★ PRIOR banner (9 lines) MOVED to `_GM-ARCHIVE.md` § `Batch 2026-09-23 #300`. EXIT CHECK: its ⬛/⚠ items are carried in `_CARRIES.md` from § `residual → #299` onward. **Carries: § `residual → #301` written — 692 probeable (probe run), 5 new (invisible to the probe), 1 HEADLINE struck with receipts, every age bracket +1, wording unchanged except the strike.** ⚙ The +1 rule was checked, not assumed: bumping every bracket of #299's line reproduces #300's old part exactly except #299's two strikes (1,752 segments, 2 differ).

**2d.** #297's ⏱ delta (18 lines) MOVED to `_LIVE-STATE-ARCHIVE.md` § `Rolled 2026-09-23 #300`. ⚠ The `Previous:` trim moved nothing — none is left.

**2e.** Ran and moved nothing: DO-FIRST carries no session-keyed stratum.

**2f.** `roll_2f` session 299: 26 lines → `notes/_GAUGE-LOG.md`, 2 → `_GM-ARCHIVE.md`. #300's stratum written.

All twelve moves ran as ONE `_gm_move.py` transaction (`notes/_lanes/300/W/_ops-300W-main.json`, dry-run first; receipts `notes/_lanes/300/W/_gm-move-300W-main.log`). `_roll_state.py`: `2c OK (banners 2/2) · 2d OK (deltas 3/3) · 2f OK (strata 1, log #299)`.

**Size stamp.** GENERATED by `_gen_size_stamp.py --write`: GM **33,073** · LS **57,143** · corpus **90,216** tape, fixed point in 2 passes; `--check` 0.00% drift on all three. `section-sizes #300` from `_gm_usage.py --sizes --session 300` (LATEST **617**, against `s241-D2`'s 1,200 / 10).

**Chain.** `_gen_chain.py`: **6,822 tape = slice 5,970 + wrapper 852, ratio 21%** of the stamp's GM — quoted into the `s214-D6` line (the NINETEENTH of the current series; `grep -c` 88, gap 70, stable); regenerated after that line, `--check` FRESH.

**4b.** `_gen_titles.py --session 300`: RENAME `Apollo - #300: the boot was measured, and the reading stays` · NEXT `Apollo - #301: the ask on slide 15, then polish for friday` (receipt `knowledge/_gen_titles_receipt.json`).

**2g.** `_build_memento_index.py` LAST after every GM/LS/carries edit: 2,317 records. **4d.** `_render_rulings.py --check`: FRESH (sha256 `96ee3fce…`); nothing inscribed. **3.** Memory: under his pasted instructions the note is placed by THIS seat after the commit and the push — `notes/_lanes/300/WRAP-MEMORY-HOOK.md` holds the index line and the body; the receipts land in § POST-WRAP. **4.** Nothing inscribed, so no decision node. Store rows minted for every new document before staging: W-300a · W-300b · W-300c · W-300d · W-300x · W-300p · W-300g · W-300i · W-300v · W-300h · W-300k · W-300w · W-300dh (821 → 834 items); by-addition notes on W-299h and W-299k.

**Strike.** ONE HEADLINE, on `_HANDOFF-150` § OWED (addendum at its foot) and in `_CARRIES.md`: item 8's first headline, his "careful plan" for where the memory hook is placed and what it costs at the opener — the plan made and measured (C, X, P) and its first move enacted by his own paste. Item 8's other headlines (the chain made more efficient, his 256K question) and items 1–7 and 9 stand.

## FINDINGS

1. **His n-gram question was first sent at 16:16, not 16:56** — a queued note the conductor's word list omits; the 16:56 message repeats it after his stop. Probe: the transcript's `queued_command` attachment (15:16:30 UTC) and user row at 15:56:53 UTC.
2. **"184,309 at the first reply" is the reading taken before the reply**; the reply went out at 187,863 (13th message). Probe: the 11th message is a tool call that hand-summed the window; the 13th carries the reply text.
3. **His paste reached the conductor with the "wrap" turn** (`session_context`, `changed: true`, 16:56:07 UTC) — byte-identical to the page and to this seat's own context. That turn also re-sent the skills listing and the deferred-tools list, but **no memory list** (no store write since the opener): the one-word "wrap" moved the fill 270,882 → 278,666 (+7,784 created).
4. **A backgrounded gate run dies with the `device_bash` call** (`bwrap --unshare-pid --die-with-parent`); the gate must run in the foreground. Probe: an empty `_gate-open.log` after `nohup … &`, and `pgrep` showing only the sandbox's own processes.
5. **The wrap gate strands `.git/index.lock` for the fourth wrap running** (#297–#300). Read-only git with `--no-optional-locks` stranded nothing at this seat.
6. **Five of the six lane reports lack the `s218-D7` skeleton** (no COUNTS line, no `REPLAY-THESE:` line, no RULING-SHAPED QUESTIONS heading; C lacks only the `REPLAY-THESE:` line) — 17 advisory warns. The lane brief asked for plain words and a stub. Reported, not repaired.
7. **Seven of the eight lane subs ended past 256,000** (all but the first P); P read 437,591 at its last message.
8. **The 28 new rows in `knowledge/_graph-mark-observations.jsonl`** (15:35 UTC, queries *"memento principles chain retrieval not recall"* ×18 and *"slide 15 ask"* ×10) came from lane runs, not the conductor — committed with the wrap as a #300 by-product.
9. **The conductor's window never ran the Memento door** this session; `consult-receipts #300` is the fourteenth consecutive honest negative.

## RULING-SHAPED QUESTIONS

0. ⛔ **The boot ceiling: cut the boot, raise `BOOT_CEILING_TK` (72,768, shrink-only, his word alone), or keep the DECLARED not-a-wrap path until the seat is decided?** New: lane X shows no settings lever gets under it; only the Mac seat does. For Dave.
1. ⬛ **Does his pasted Project instructions text become a ruling?** It changes ritual step 3 (the note is placed after "done", not at the next opener) and drops the check-in from the opener; two runbook lines are now stale. Carried as a question, not enacted. For Dave.
2. ⬛ **Is striking one HEADLINE of a multi-headline carry the right form** (#299's item 8), or may a carry only ever be struck whole? This wrap struck the "careful plan" headline and left the chain and 256K headlines open. For Dave.
3. ⬛ **Should every lane brief carry the `s218-D7` skeleton lines** (COUNTS, `REPLAY-THESE:`, RULING-SHAPED QUESTIONS)? Five of six #300 reports lacked them. For Dave.
4. ⬛ **Is the hand sum over the cloud transcript the sanctioned wrap measurement** (carried from #297–#299, still open)? For Dave.

### UNPROVEN, named rather than implied

1. **That the new instructions cut the opener as projected** (~161K after his first message) — #301 is the first measurement.
2. **That this seat's memory placement costs the #300 chat nothing** — true only if he writes nothing more in it.
3. **The capture-gate selftest** — exceeds the call wall; CI is the only asker.

REPLAY-THESE: `python3 knowledge/_capture_gate.py --wrap` (foreground, ≈2 min) · `python3 knowledge/_roll_state.py` (expect `2c OK (banners 2/2) · 2d OK (deltas 3/3) · 2f OK (strata 1, log #299)`) · `python3 knowledge/_gen_chain.py --check` · `python3 -c "import json;r=json.load(open('knowledge/_rulings.json'))['rulings'];print(len(r),r[-1]['id'])"` (expect `638 s295-D4`) · `PYTHONPATH=knowledge python3 -c "import _capture_gate as c;print(len(c._carry_items([l for l in open('_CARRIES.md') if '#301:**' in l][0])))"` (expect **692**) · `md5sum notes/_lanes/300/PROJECT-INSTRUCTIONS-v2-LIVE.txt` (expect `9d7837a3…`) · `git ls-remote origin refs/heads/master` (expect the 5b sha)

---

## POST-WRAP — the readings as they closed

*Filed by addition after the commit, the push and the memory placement; nothing above is rewritten.*

- **Commit:** `72c6ca9c`, the DECLARED not-a-wrap path. The gate inside the committer: `238 in scope · 1 fail · 66 warn` → `⚠ wrap gate RED — visible, not blocking`. 138 files, +21,935 / −1,001. Exit 0, `✓ done — locks clear`. Transcripts `notes/_lanes/300/W/_gitcommit-W1.log` · `.term`. ⚠ The first run refused at `_gen_chain.py --check` — the chain's wrapper carries the store's counts and the rows were minted after the chain was generated (the handoff's own order, "mint rows and regenerate `_CHAIN.md` BEFORE the first commit attempt", broken at this seat) — and staged nothing (`_gitcommit-W1-refused-stalechain.log`). The mention map was stale before the first attempt and was regenerated and named.
- **Push:** `6afe3f8b..72c6ca9c` by plain `git push origin master`, DECLARED; branch and fast-forward checked first; `ls-remote` = local HEAD `72c6ca9cdaff…`.
- **CI:** run `35895619087` — `release` ✅ 17:27:17 · `gates` ⛔ 17:33:36 (steps 5, 6) · `render` ✅ 17:40:50, verdict after `render` closed. `SURVEY: 59 pass · 6 FAIL · 4 COULD-NOT-ASK · 0 unaskable · 77 not asked` — the inherited six by name (`[3]` · `[13]` · `[38]` · `[125]` · `[128]` · `[136]`), identical to #299's `35873846048`; **no fail beyond the six**; `[120]` read chain determinism GREEN; step 6 `help-gate: 264 script(s) scanned, 12 failure(s)`. Nothing repaired.
- **Memory (step 3), placed LAST by this seat:** `wrap-300-…` 3,972 B (= 576 frontmatter + 3,396 body, byte for byte) · the #297 line appended VERBATIM to `MEMORY-ARCHIVE-3.md` § *Batch 2026-09-23 #300 (wrap)* (21,467 B), then removed from `index.md` with the same string — the match held (694 B) · `index.md` 6,825 B at 17:28:41 UTC, #300 · #299 · #298, description fixed. The #299 opener placement re-read first: 3,361 B · 6,091 B · 20,721 B · 4,144 B, every declared figure matching (receipt appended to `notes/_lanes/299/WRAP-MEMORY-HOOK.md`). ⚠ **Correction to UNPROVEN 2 above:** the conductor's next turn — reading this seat's stub — should itself carry the memory list once more (~19K), because the store changed (INFERENCE from lane X's version-check finding).
- **Titles (`_gen_titles.py --session 300`):** RENAME `Apollo - #300: the boot was measured, and the reading stays` · NEXT `Apollo - #301: the ask on slide 15, then polish for friday`.
- **Banner:** LATEST 617 tape (`_gm_usage.py`), against `s241-D2`'s 1,200 / 10.
- **Chain:** 6,822 tape (slice 5,970 + 852), ratio 21% at the wrap commit → **7,570 (slice 6,718 + 852), ratio 23%** of a 33,427-tape GM after the 5b delta line. The size stamp drifts 1.06–1.29% after 5b (inside 10%); not re-taken (the #241 rule).
- **4d:** `_render_rulings.py --check` FRESH (sha256 `96ee3fce…`); nothing inscribed.
- **Rulings:** 638, newest `s295-D4` (`json.load`). The `s271-D4` re-read strikes nothing. The advisory arm `hook_open_items_recheck`, pointed at `notes/_lanes/300/WRAP-MEMORY-HOOK.md`, read **0** open items and 0 same-day rulings — its parser does not match the hook's heading form (as at #297–#299); reported, not repaired.
- **4c:** run LAST, after the 5b commit.
- **Spend (this seat), hand sum of its own transcript `…/subagents/agent-a761249f1280f799e.jsonl` at filing:** 158 messages · boot 69,590 · last fill 473,401 · input processed 48.9M · output 172,059. A few messages follow for the 5b commit, 4c and the hand-back.
