# #246 lane B — BASELINE: the demo prompt, run cold, blind then sighted

**Model: Fable. Brief: `notes/_briefs/2026-09-05-246-lane-B-baseline-brief.md`. Read-only against canon/knowledge. Nothing ruled.**

## What was done
1. **Grill-me fired** (`briefs/` absent) — six questions recorded skipped/defaulted, Dave not asked, Mono announced as the default. `outputs/baseline-246/brief.md`.
2. **Arm A (blind)** — built from pack surfaces only (skills, index.json, metas, snippets, canon/, tokens/, assets/icons/), no render/gate/validator. Bento-first from `Template-dashboard-bento.reference.html`, plus Chart-line, Data-grid, Filter-toolbar-bar, Tabs by splice. FROZEN sha `1f76c0af…`. `arm-A-blind/{dashboard.html, READ-LOG.md, USED-MISSING.md, FROZEN.sha256}`.
3. **Playwright installed** per `_ROBUSTNESS-PORTABILITY.md` (4 calls: pip, chromium-headless-shell, libXdamage user-space, launch test).
4. **Arm B (sighted)** — it0 = A's bytes; 3 iterations, each rendered (1440/820 × light/dark) and gated. it1 fixed the two visible defects and minted a receipt; it2 switched to canon.css-linked `.cn-*` composition; it3 cleared compose residuals. **Both gates exit 0 at it3.** `arm-B-sighted/{dashboard.html, ITERATION-LOG.md, gate-it0..3-*.txt, render-it0/it2/final-*.png, probe*.json, FROZEN.sha256}`.
5. **Measured both identically** — renders, gates (A: screen 1 / receipt 1; B: 0 / 0), inventory, chart script sha vs source, 20 driven interactions (`drive.json` each arm), persistence (tab nav + reload), data believability, four scores.
6. **Review page**: `reviews/BASELINE-246-2026-09-05-v1.html` (swiss idiom; renders side by side, four tables, 12 findings, 7 RSQs).

## Scores (rich / full / persistent / interactive, 0–3)
- Arm A: 2 / 2 / 0 / 2 · Arm B: 2 / 2 / 0 / 2. Sight bought two visible fixes (stacked toolbar from a CSS copy truncated at L231; serif tabs from a body-font dependency) and green gates, and moved no score.

## Top findings (full list on the review page)
1. No dashboard-set component wires to another — "make filtering work" needed 14 lines of authored glue that rule 2a forbids (`behaviour` on 25/27 metas: none).
2. The receipt gate rewards splicing; the compose gate fails every spliced page (281 hex + `.c-bento` redefinition = the snippet CSS itself). Green only by abandoning the splice model at it2.
3. Template-dashboard-bento ships its chart stripped (no toolbar/engine, by declared choice) — the "partial chart" is in the template every dashboard starts from.
4. Persistence exists nowhere: reload resets everything, 0 storage keys, no rule mentions it.

## What was NOT done (declared)
- No third arm for sentence 1 alone — a labelled judgement is on the review page instead.
- Arm A scripts were carried by byte-exact line extraction; a real Copilot re-emits text, so the sha column is generous to the blind condition (READ-LOG.md).
- Not read blind: `knowledge/_render/_bento_edit_rails.json` (rule 7 points at it; outside the allowed list).
- Console (`data-apollo-theme`) legs not rendered — mono only, per the skill default.
- `--render` (state-contrast) leg of `_validate_screen.py` not run.
- Iteration cap held at 3; the remaining a11y warnings (24px dismiss targets, 11px chart markers) are the snippets' own and were not touched.

## Side effects, declared
- `_validate_screen.py` writes into `knowledge/`: `_SCREEN-GATE.md` restored via `git show HEAD:` after every run (a stale `.git/index.lock`, 2026-09-04 07:33, blocks `git checkout`; not removed — not mine). `knowledge/_screen-gate/dashboard.md` (gate-written, untracked) could not be removed (mount refuses rm) and is left on disk — one file, 7.9 KB. Nothing else under knowledge/, showroom/, apollo-spider/ touched. No `_build_all.py`. No commit, no push.
- Build/measure tools live outside the repo: `<session outputs>/build/{build_armA.py, build_armB.py, it2.py, render.py, drive.py}`.

## Tokens
- Context at the A→B seam (after Arm A frozen): ≈215K cumulative token throughput, per the harness counter.
- At report time: ≈300K. Largest single reads: Template-dashboard-bento (1172 lines), Chart-line L1-635, Data-grid L84-746.

## Paths
- `outputs/baseline-246/arm-A-blind/dashboard.html` · `outputs/baseline-246/arm-B-sighted/dashboard.html`
- `reviews/BASELINE-246-2026-09-05-v1.html` · `notes/_subreports/2026-09-05-246-B-baseline.md`
