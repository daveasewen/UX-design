# Parallel sessions — the conductor pattern

**Promoted to canon → `knowledge/_RUNBOOK-parallel-conductor.md`.** This note is kept only as the
origin marker (2026-07-19: two sessions both tried to commit + rewrite `GOOD-MORNING.md`; Dave:
"I should be running a cold agent from the main chat"). Do not duplicate the pattern here — edit the
runbook.
