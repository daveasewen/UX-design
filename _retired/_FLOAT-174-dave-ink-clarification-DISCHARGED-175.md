# ✅ DISCHARGED #175 (2026-08-14) — RETIRED HERE, NOT DELETED

**Dave picked READING 3, the narrowest — component-scoped.** Ruled as `s175-D1`
(`knowledge/_rulings.json`, entry 155). The rule he gave is SEMANTIC, not visual:
a **continuous quantity** (`Progress-bar`) is **INK ONLY in all four themes**; **discrete steps**
(`Progress-tracker`, `Stepper`, including their responsive collapse to a bar shape) **MAY use
colour** — Legacy DEFINITE, Mono/Console/Supercharge **UNDECIDED, FLOATED, NOT RULED**.

⛔ **`s151-D1` THE TWO-RED LAW and `s149-D1` THE MONO ERROR INK CAMP ARE UNTOUCHED AND WERE NOT
REOPENED.** The float's own warning — that reading 3 is attractive because it makes an open problem
disappear — was honoured: the fill-on-track contrast failure is FIXED for the bar and **STILL OPEN
on the step components** (Legacy dark 1.75:1, Supercharge dark 2.38:1). It did not disappear.

★ The text below is the float **exactly as written at #174**, preserved verbatim as the record of
what was asked. It is no longer live. Retired from `notes/` to `_retired/` by the #175 wrap.

---

# FLOAT — Dave's ink rule, #174, POST-WRAP CLARIFICATION

⛔ **THIS IS A FLOAT, NOT A RULING.** It is not in `_rulings.json` and must not be enacted
until Dave rules it at the #175 opener. Written post-wrap deliberately as a small standalone
file rather than by reopening the #174 wrap — see the post-wrap-corrections-bloat class.

## What Dave said, verbatim, in order

**First** (asked whether to revert or keep the 25 stripped `--status-*` declarations in
Legacy / Console / Supercharge):

> "lets keep it nice and simple, all of them use the appropriate dark ink on white and the
> lightest ink for dark"

**Then, unprompted, mid-wrap:**

> "no red just black ink and white"

## What was NOT done

Nothing was enacted. The `--status-*` strip stands as committed in `76b024c`, which is the
state Dave saw and answered on. No token was changed. No generator was run on the strength
of these words.

## ⚠ THE TENSION THAT MUST BE PUT TO HIM — do not resolve it by inference

The conductor's first read-back was "a background-keyed ink rule, the two-red-law shape".
The second sentence does not fit that read. **"No red" appears to bear on `s151-D1`, THE
TWO-RED LAW, which is FIRM** — background-keyed `#DA1A00`-on-white / `#F6604C`-else, mono
only — and on `s149-D1`, the mono error ink camp, which Dave **saw and approved by eye at
#159**. A firm, eye-approved ruling is not overturned by an inferred scope.

**Three readings, all live, none picked:**

1. **Narrow — the `--status-*` set only.** The four status hues (`breach` / `healthy` /
   `info` / `watch`) stop being bespoke per theme and become black ink on white, lightest
   ink on dark. The two-red law is untouched because it governs error/RAG text, not these.
2. **Wider — all status and RAG colour.** Red goes as a status signal everywhere; meaning is
   carried by position, weight and text, not hue. This **does** collide with the two-red law
   and with the mono error ink camp, and would need both reopened explicitly.
3. **Narrowest — this component only.** The progress bar's fill/track use ink, not hue.
   This would also dissolve the open fill-on-track contrast finding (Legacy dark 1.75:1,
   Supercharge dark 2.38:1) rather than needing it ruled separately.

⚠ Reading 3 is attractive precisely because it makes an open problem disappear, which is
reason to distrust it, not to prefer it.

## Ask him this at the #175 opener, in plain prose, before anything else

Which of the three does he mean — and is he reopening the two-red law, or ruling only on
the status set that came up in front of him? A pick from an incomplete set reads as a
ruling; this set is incomplete.

## Related open items this touches

- The uncommitted-at-the-time, now-committed `--status-*` strip (`[50]`, was Dave's eye).
- The fill-on-track contrast finding in Legacy dark / Supercharge dark, queued to
  `_DS-IMPROVEMENTS.md`.
- `s151-D1` the two-red law · `s149-D1` the mono error ink camp.

*Written #174, 2026-08-14, at `76b024c`. Carried by the `residual → #175` line and by the
next-session title.*
