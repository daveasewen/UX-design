---
name: vision-contextual-dashboard
description: Parked horizon-3 idea — Promenaut engine in a second vehicle (contextual dashboard); NOT a roadmap change
metadata: 
  node_type: memory
  type: project
  originSessionId: 7f474f88-c345-4f03-ba55-d74d8e45a206
---

PARKED 2026-06-29, fenced from the active roadmap (do not let it compete with the locked sequence in [[promenaut-discovery-decisions]] / [[pipeline-mental-model]]). Dave's framing: he does NOT want to pivot Promenaut — he asked whether the same engine could go into a *different vehicle with a different purpose*.

The engine = Promenaut's IP (gated canon + criteria-as-executable-checks + enforcement) reframed as a generic "trust layer for assembled UI." Two vehicles: (1) broad = the safety substrate that makes freeform GenUI trustworthy (someday-vision, different product); (2) tight + recommended first = a **contextual dashboard** — assembles itself per user from a *certified card set*, deciding which cards / order / emphasis. "Generation" collapses to selection + ranking + composition = objective, so NO run-time taste-call (taste was spent certifying the card set) → consistent with the "don't automate taste" rule.

Inputs: profile + **entitlements** (a hard blocking gate — only show what they're allowed; = verification=enforcement applied to data visibility), company data, comms signals (email/Teams/Slack — sensitive, defer), chronological triggers (cut-offs, month-end). New work needed: user-context model, connectors, **relevance/ranking engine** (the real hard part), run-time-capable gates, consent/privacy/audit. Shared bridge investment serving BOTH timeframes = making the criteria contract + gates run-time-capable (fast, inline, per-instance).

Full note: `_VISION-contextual-dashboard_2026-06-29.md`. Revisit triggers: design-time loop proven on one real HSBC screen AND a real role-based-dashboard need surfaces, OR run-time gates get built anyway.
