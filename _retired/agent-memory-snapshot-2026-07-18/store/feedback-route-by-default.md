---
name: feedback-route-by-default
description: "RULE — model-route by DEFAULT and announce the Opus/Sonnet split up front; don't wait for Dave to nudge"
type: feedback
---

**RULE (Dave, 2026-07-14 — corrected me TWICE in one session: "should we be model routing this" then "you should
be routing this stuff surely").** Model-routing (Mode 2) is **DEFAULT-ON, not opt-in.** Dave should not have to tell
me to route mechanical work. I keep defaulting to doing everything on Opus and only routing when nudged — backwards.

**Why:** doing it myself is frictionless for me but costs Dave tokens + time, and it's the exact capability-gap trap
([[capability-gap-and-obsolescence]]) — burning the expensive wave on chores. The convenience of just-doing-it is the smell.

**How to apply (EVERY substantive task):** at the **START**, state the split up front — what stays on **Opus** (judgment:
root-cause, design decisions, what to promote, synthesis that needs whole-session context) vs what goes to **Sonnet/Haiku**
(drafting, assembly, mechanical edits, throughput-to-a-spec). Route by default; **announce each time** (per
[[model-selection-by-phase]]). Legit exception to keep on the main model: summarising/compacting the CURRENT conversation
(context is already in-context; delegating = re-dumping the transcript = more waste). Pairs with [[working-style-divergent]].
