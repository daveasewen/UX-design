---
name: component-review-program
description: "Component-review program — bring each ★ journey component to the Tabs-bar 9/9 standard; the loop, decision A, what's done, what's next"
metadata: 
  node_type: memory
  type: project
  originSessionId: 4c1b6f21-694d-42b1-81a9-55ae524d5ef0
---

Refining each HSBC Common Toolkit component to "prototype-grade" (the Tabs-bar standard — `knowledge/_RUBRIC-prototype-grade.md`, a 9-signal advisory scorer in `_build_prototype_grade_audit.py`). Loop: refine in `_fitness-test/` (never gated) → Dave reviews visually → promote into `snippets/*.reference.html` → rebuild green → re-score (the rising score is the metric; nothing enters canon without Dave's go-ahead).

**Done (9/9):** Tabs, List-items, Status-indicator, Table, Selection-controls, Progress-tracker, Modals, Cards, Input-fields, **Button** + **Links** + **Tags** + **Notifications** (all finished 2026-06-24). **ALL 12 PAYMENTS-JOURNEY ★ NOW AT 9/9** — board 13 at 9/9, avg 7.5. Commits landed: Button 4446fad, Links 2c82fb1, Tags 7fede50; Notifications handed to Dave. Next: pick a new tranche of ★ (non-journey components still 6–7.5) or the icon hard-gate, or token-level dark-rag fix.

**Notifications (DONE 2026-06-24):** was 7.0/9. Dave flagged the canon was a single rough banner — REBUILT to the full Figma family (node 45002-355009) by pulling metadata + screenshots + variable defs via the Figma MCP: Contextual (compact + form multi-link), Global (solid fill, reverse text/icon, Action 1/2; warning = dark text), Inline (bare), Snackbar (tinted+shadow, no error). ICONS rebased onto real library status glyphs (error=triangle, others circle+marks) via a <symbol> sprite using `style="fill:var(--mark)"` inner marks (custom props inherit through <use>) → fixed hand-drawn approximations + err/warn both-triangle bug + undeclared circle/rect info blind-spot; icon audit 9/9 verified, 0 bespoke. Type corrected to font-5 (16px). states (links+actions+dismiss) + stateModel:simple; responsive to 320px. DARK: interim per Dave — rag accents use LIGHT values for stroke/surface + white icons (dark rag/information is broken, see [[dark-rag-token-gaps]]). NEW GATE FEATURE: `_validate_snippets.py` now supports a manifest `driftAllow` block (annotated intentional token-drift → warning not failure; mirrors contrast/dark-surface allow-lists) — used for the two interim dark drifts. state-contrast clean; build green.

**Decision C (ENACTED 2026-06-24, Dave):** the `states` signal under-credited genuinely SIMPLE interactives (only default+hover/pressed+focus, no selected/checked/disabled — e.g. Tags) — capped at 0.5 since they can't reach ≥4 enumerated selectors. Fix (same shape as A): meta may set `"stateModel":"simple"`; scorer credits states=1 when :hover+:active+:focus-visible all present, guarded by fallback-to-count. Added to `_build_prototype_grade_audit.py`, `meta.schema.json` (new `stateModel` field), `_RUBRIC-prototype-grade.md`. Only Tags declares it.

**Tags (DONE 2026-06-24):** was 7.5/9 (states + responsive). RESPONSIVE: applied-filters bar reflows via container-query → full-width rows + label ellipsis at ≤360px (320px verified). STATES: :active (combined hover/pressed) + Decision C stateModel:simple → ✅. FINESSE (Dave: "roll in the previous work") = promoted the blessed chip interactions from _fitness-test/tags-chip-animation.html: em-based sizing, tag-link press 1.05/0.97, tactile dismiss × 1.4/1.15, V1 collapse+fade dismiss; documented in tags.meta `motion`. Static tags get NO motion. Library close.svg via sprite (icon audit verified). state-contrast clean; build green.

**Links (DONE 2026-06-24):** was 7.5/9, missing states + responsive. STATES: added :active (faithful to the meta's COMBINED hover/pressed — same treatment, not a new look) + aria-current="page" current-link state styled weight+thick-underline (never colour-only, 1.4.1) → states ✅. RESPONSIVE: added a related-links LIST (responsive unit; single/inline links already fluid) that reflows via container-query — row+wrap wide, stacked w/ larger tap targets (2.5.8) at ≤420px, verified 320px. BONUS: added the ICON-LINK family member (4th type, was missing from canon; homes the disabled state) — glyph = real library download.svg via <symbol> sprite (icon audit 5→6 verified, 0 unknown). state-contrast clean; _build_all green. Loop: _fitness-test/links-responsive.html → render → Dave blessed → promoted.

**Button (DONE 2026-06-24):** was 8.0/9, missing ONLY responsive (Dim 11). Closed by adding a container-query ACTION BAR (the primary+secondary / confirm-cancel pair = the responsive unit; single buttons already fluid): inline+right-aligned (primary last) on wide; at ≤420px container it stacks, each btn full-width, PRIMARY promoted to top (order:-1), hover-grow disabled when full-width (.full rule). Verified to 320px/WCAG 1.4.10 (0 h-overflow), both themes, via headless-Chromium render. Added `--surface`=tertiary/background/hover + a width-slider in `.demo-controls`. SCHEMA CHANGE: added `responsive` as a first-class optional meta field (parity with motion/dimensions) in `components/meta.schema.json` — documented in button.meta `responsive` block. state-contrast clean; _build_all green. Loop followed: built in _fitness-test/button-responsive.html → rendered → Dave blessed → promoted. NOTE: sandbox can't write the mounted `.git` (index.lock unlink = Operation not permitted) — commit was handed to Dave to run.

**Input-fields (DONE):** TWO styles kept (Dave "both styles exist") — BOXED (Figma node 65570:211753: full form/border box, optional GBP prefix / trailing icon / (?) help, 0/100 counter, states incl. 4px bottom-stroke active/error emphasis, std+large) + the prior UNDERLINE. Input value+placeholder weight light→regular (same fallback-font thinness call as Table cell data). Icons from the real library via a `<symbol>` sprite (see [[icon-source-rule]]). 9/9 + state-contrast clean. date-picker calendar + multi-line deferred.

**Table (DONE):** chose option (a) — static semantic data table, `interactive:false`; AT = caption + th scope + focusable role=region (no sort bolted on). Responsive = CARD-COLLAPSE per Figma "vertical" variant (node 2137:115845): rows reflow to cards, column headers repeat as labels, row-header → card title; first-column tables show bare values. Variants covered: headerType {first-row/first-column/both} via a live scope-switching control + cellAlign + sub-header. TYPE deviation: cell data promoted Figma light(350)→REGULAR(400) — Univers Light too thin in Chrome (candidate token-store flag).

**Decision A (LOCKED 2026-06-22):** a meta may set `"interactive": false`; the scorer then credits AT via role/aria-live/label rather than a keyboard handler — but a passive component exposing nothing still scores 0, so the bar stays real. Flagged passive: Status-indicator, Divider, Badge, Loading-indicator.

**Decision B (ENACTED 2026-06-22):** the AT signal now credits NATIVE-keyboard interactivity — an interactive component keyboard-operable via a native element (`<a>`/`<button>`/`<input>`/`<select>`/`<textarea>`) + a visible `:focus-visible` indicator earns AT=1.0, as well as the role+aria+keydown path; guard keeps a component exposing nothing at 0. Board avg 6.8→7.3. The earlier band-aids stay (SC chips / Cards selectable-card), but native inputs/links no longer need one. Logged in `_RUBRIC-prototype-grade.md`.

**Icon-source guardrail (NEW 2026-06-22):** never invent icons — see [[icon-source-rule]]. `_validate_icons.py` (advisory) matches each snippet's inline SVG paths to `assets/icons/`. MIGRATED 2026-06-22: 52 → 0 UNKNOWN (15 verified-bespoke) — content icons swapped to library glyphs, control/animated glyphs marked `data-bespoke`.

**Atom-reuse discipline:** components MIRROR canon primitives, never re-implement (List-items reuses Avatar / Tags / Loading-indicator spinner / Status-indicator RAG chip). Cross-library naming = hub-and-spoke [[code-binding-hub-spoke]]. Relates to [[gated-snippets-and-motion]] and [[pipeline-mental-model]].
