# HANDOFF #137 — #286 → #287 — THE CONNECTOR LEVER READS −7,031 AND IT IS A READING, NOT A CAUSE; THE 40 MASTERS ARE SIZED ONTO THE EIGHT NODES; THE STANDING FILE IS OUT OF DRAFT ON SIX WORDS

provenance: 286 · 2026-09-18
status: observed

*Written by the delegated OPUS 5 wrap sub at the close of #286 (conductor Fable 5.1). Every figure
here was MEASURED at this seat unless it says otherwise; where a measurement disagrees with the
brief's declaration, both readings are published and neither is rewritten.*

✅ **NO DATE SPLIT.** #286 opened, ran and wrapped inside **2026-09-18** (`date` read at this seat).
⛔ **NO RULING WAS INSCRIBED. `knowledge/_rulings.json` STAYS AT 620**, verified here by
`json.load` over the `rulings` list, with **no `s286-` id in the store**.

---

## ⛔ READ FIRST, IN THIS ORDER

1. **This file.** It is newer than `_CHAIN.md` and **OUTRANKS it**.
2. `_CHAIN.md` — the read contract (header → ★ LATEST banner → ⏱ LATEST delta).
3. ⛔ **It does NOT replace `_HANDOFF-130`…`-136`.** Every open item on those seven still stands
   except the two closed below, each with a receipt. **Strike nothing else without one.**
4. `notes/_lanes/286/DAVE-RULINGS-2026-09-18.md` — his words, verbatim. **Quote them; never
   paraphrase them**, and note that it records the conductor's own *reading* of one of them,
   marked as the conductor's.
5. `_CARRIES.md` § `residual → #287` when you need the bodies — **551 probeable items**. Do not
   read it at boot; fetch the section (`_memento_search.py` → `--fetch carries:residual-287`).

---

## ⛔⛔ DAVE'S — VERBATIM, AND THERE ARE ONLY TWO

- ***"okay go on everything"*** — his **entire** reply to an opener that named **three** items (the
  eight standing lines' wording · **HOW** the accepted masters get registered · the 256,000 wording
  fix) **plus** the question whether one cold-boot reading was enough.
- ***"okay size-on-the-existing-node"*** — the **SHAPE** of a master registration.

⛔ **NEITHER IS INSCRIBED AS A RULING, AND WHETHER EITHER SHOULD BE IS HIS.** It was put to him at
the wrap call and is not the wrap's to decide (`s271-D4`).

⚠ **ONE JOIN IS NAMED RATHER THAN SMOOTHED.** Reading *"go on everything"* on item 1 as
**ratification of the eight lines AS WRITTEN** — rather than *"go draft me something"* — is the
**CONDUCTOR'S reading, not Dave's sentence.** He did not say "as written", did not quote a line and
did not say "ratified". The rulings file says so on its own face, and `_standing.md`'s new header
cites it, **so the act stays reversible by one word from him.**

⚠ **What is still NOT his even after the second sentence:** the **KEY NAME** of the registration
field. He named the LOCATION, not the SPELLING.

---

## ★ WHAT LANDED — 3 commits, all pushed and verified BEFORE this ritual opened

| sha | what |
|---|---|
| `1caaa0b1` | lanes S · R · G · T · V — standing out of DRAFT, gauge wording, seam `INSEAT` arm, `--quiet` |
| `db0830f7` | lane C's own filed report completed with the sha and its two refusals |
| `7ed49d37` | lane R2 — the 40 masters registered as `sizes` on the 8 logo nodes |

✅ **A FIRST ON THIS RUN: the tree was IN SYNC at the ritual's open** — `git rev-parse origin/main`
read `7ed49d37` against the same local HEAD, so nothing was owed from a lane. ⚠ One dirty path was
inherited and is committed by this wrap: `notes/_subreports/2026-09-18-286-C2-commit-and-push.md`,
to which lane C2 appended its post-push CI table **after** committing — the lane doing the honest
thing in the wrong order. **This wrap's sha, the push verdict line and the CI read are in the
★ LATEST banner and the filed report** — a commit cannot name itself.

---

## ★★★ THE CONNECTOR LEVER — A READING, AND THE DESIGN IS THE POINT

**BOOT COLD 73,832 real (n=1)** against #285's **80,863** ⇒ **delta −7,031**, with **ONE variable
changed**: Dave set the built-in Browser connector's **17 `mcp__Claude_Browser__*` tools to
BLOCKED**. Held constant **by design**: skills untouched, computer use off, the other three
connectors on (Claude Docs, Claude in Chrome, GitHub). Same instrument, same moment (first turn).

★ **This is the cleanest one-variable boot experiment the record carries.**

⛔ **AND IT IS NOT A CAUSE.** The last seven post-diet readings span **72,110–83,636 — a spread of
11,526, WIDER than the delta being claimed.** One reading against one reading cannot separate the
lever from that spread.

⇒ **The direction is consistent with the block having worked; the magnitude is not established.**
**What would make it a verdict is named and NOT done: a SECOND cold boot on the same setup**, same
instrument, first turn. Two readings within a few hundred tokens of each other would make it
measured; a reading back near 80K would say the delta was spread.

⛔ **73,832 IS 3,832 OVER `BOOT_CEILING_TK` 70,000 — THE ELEVENTH POST-DIET READING OVER IT.** The
literal is **SHRINK-ONLY** (`s240-D2` / `s241-D1`): **cut the boot, never raise the number**, and
raising it is his word alone.

⛔ **THE NEXT LEVER IS NAMED AND NOT RANKED.** `_checkin.py` reports boot as ONE figure; its
decomposition into system prompt / tool schemas / deferred-tool list / MCP instructions is
`ds-025` item 1 and is **still dark from every mount**. So **no connector is ranked and no expected
saving is stated** — to make a candidate measurable, either the decomposition becomes readable, or
the same one-variable experiment is repeated per connector.

⚠ **The figure is stated ONCE, in the `post-mortem #286:` line** (`s241-D2`), and the finding file
`notes/_lanes/286/BOOT-COLD-2026-09-18.md` is deliberately **not** in `notes/_GAUGE-LOG.md`.

---

## ★★★ THE MASTERS — REGISTERED, AND ONE SENTENCE REFUSED TWO LANES

Lane R was sent to register the forty accepted masters and came back saying **it could not be
done**: `gen_kg_icons.py` is **blind to `masters/`**. It then did the useful thing — it laid out
three shapes and left the choice open as ruling-shaped:

1. a **size field** on the lockup's existing node,
2. forty new `logo:<stem>-<h>` nodes,
3. a new `logoMaster:` kind.

His answer: ***"okay size-on-the-existing-node"*** ⇒ **shapes 2 and 3 are refused and the node
count does not move.**

Lane R2 wrote a **`sizes`** map onto all eight existing nodes, five raw heights each
(24 · 28 · 32 · 36 · 40), **merge-on-write** so nothing already in a node was overwritten.

✅ **VERIFIED AT THIS SEAT BY `json.load`, NOT TAKEN FROM THE LANE'S PROSE: 8 nodes · 33 edges ·
8 of 8 nodes carrying `sizes` with exactly five keys · 12 `governedBy` edges intact** — the same 8
and 33 that `s282-D4` / `s282-D5` left behind. **Row `W-285lm` is CLOSED.**

⚠ **THE KEY NAME IS NOT HIS WORD AND IS NOT CLAIMED AS ONE.** `sizes` is lane R2's reading of the
file's own plural vocabulary (`nodes`, `edges`, `unresolved`, `fills`), recorded in its report as
the lane's reasoning. **The spelling is ruling-shaped and open**, and a rename is cheap while
nothing reads it — ⛔ **and nothing does: the KG explorer was NOT rebuilt to show the new field.**

★ **The shape worth keeping: a lane that cannot do the job is not a failed lane if it comes back
with the decision that was blocking it.**

---

## ★★ THE STANDING FILE — OUT OF DRAFT, AND THE CHANGE IS AS SMALL AS THE CLAIM

Measured at this seat against `09ddf155`: **8 insertions, 3 deletions, in ONE hunk, entirely above
the `---` rule**, with the **eight lines BYTE-IDENTICAL** (936 B of body, compared
programmatically, never by eye). **Row `W-285sc` is CLOSED.**

⛔ **An instrument reciting a constraint still does not inscribe it**, and the new header says so on
its own face while citing `notes/_lanes/286/DAVE-RULINGS-2026-09-18.md`.

⚠ **AND A STALE CLAIM SURVIVES INSIDE A LIVE INSTRUMENT.** `knowledge/_seam.py` still tells every
seam that `_standing.md` *"is a DRAFT until Dave approves it"* — **false since `1caaa0b1`.**
⛔ **The wrap brief locates it at `:26`; this seat measures `:44`. Both readings are published and
neither is rewritten**, and the line is **not edited**, because the seam's own text belongs with
whoever rules on the seam. ⚠ Note that the sentence's second half is still TRUE and must survive
any edit (*"the seam re-quotes, it never inscribes"*), which is why this is a wording question and
not a deletion.

---

## ★★ THE SEAM'S `INSEAT` ARM AND `--quiet` — #285'S MOVES 2 AND 3, BUILT

`knowledge/_seam.py` **+214/−6** · `knowledge/_git_commit.sh` **+60/−1**.

The arm reads the conductor's **own** transcript (non-sidechain `tool_result` blocks), counts what
has landed **in seat since the last seam** against a gitignored marker, and warns past
`INSEAT_WARN_TK`; `--no-inseat` suppresses it **and does not move the marker**.

★ **It turns #284's delegation argument from prose that lived once in a handoff into a number that
fires while the session can still change course** — the `_seam.py` move of #283, applied to a
second quantity.

⛔ **`INSEAT_WARN_TK = 10_000` IS *PICKED*, NOT RULED, AND THE FILE SAYS SO ITSELF** (the same
standing as `DISK_WARN_PCT`), reasoned between a conductor's ~45K of in-seat lane work and a
delegated lane's ~300. **Both the figure and the block's placement are his.**

---

## ⛔ THE 256,000 WORDING — FIXED WHERE THE AUTHORITY LIVES, OWED IN 58 PLACES

`knowledge/_gauge_tokens.py` reads **+25/−3** and **`BUDGET_HARD` is still `256_000`.**
★ **The constant did not move, and that is the point: the defect was never the number, it was the
word next to it.**

⛔ **58 locations across 10 live files still call 256,000 a wall or a hard line**, counted by a
named probe (`256,000|256000|256_000|256K`, filtered to lines also carrying *hard* or *wall*):

| file | count |
|---|---|
| `notes/_MEMENTO-DECISIONS.md` · `knowledge/_rulings.json` | 10 each |
| `notes/_RULINGS.html` | 9 |
| `knowledge/_state.json` | 8 |
| `knowledge/_RUNBOOK-context-gauge.md` · `_LIVE-STATE.md` | 7 each |
| `GOOD-MORNING.md` | 3 · `_CHAIN.md` 2 · `dashboard/index.html` 1 |
| `knowledge/_standing.md` | 1 — already correct |

★ The sharpest: `_RUNBOOK-context-gauge.md:85` — ***"⛔ 256,000 stays the UNQUALIFIED wall
(`s214-D2`)"*** — and `:926`'s *"256,000 is the absolute hard stop"*. ⛔ **NOT EDITED: those are
ratified runbook and ruling texts, and amending them is Dave's word alone.**

---

## ⚠ THREE SMALLER FINDINGS, EACH NAMED RATHER THAN FIXED

**(a) A VERIFIER MUTATED THE TREE IT WAS VERIFYING.** Lane V read **24 of 24 testable claims PASS**
— and **overwrote `knowledge/_capture_gate.py` with HEAD mid-run and restored it byte-exact**, and
moved two symlink farms to `_to_delete/`. **The restore's exactness is what keeps this a method
note rather than a finding about the result**, but a 24-of-24 pass from a seat that edited the gate
deserves to say so out loud. ⛔ **Ruling-shaped and not inscribed: whether a verifier lane may
mutate the tree at all**, and if so whether it owes a declared before/after digest.

**(b) THE STORE'S ID REGEX REFUSED A THIRD ID IN TWO SESSIONS.**
`^(?:W-[0-9]{1,3}[a-z]{0,2}|G[0-9]{1,2}[a-z]?)$` allows at most two trailing lowercase letters and
**no digit after them**, so lane R2's row is `W-286rb`. #285 met it twice (`W-285lm2`, `W-285c2`).
⛔ **A finding, not a fix — an id scheme is a convention and widening one at a wrap is not a wrap's
call.**

**(c) THE CLOUD MEMORY ARCHIVE IS FULL AND THE NEWEST-THREE RULE IS SUSPENDED FOR ONE SESSION.**
`MEMORY-ARCHIVE.md` stands at **48,990 of its 49,152 B cap — 162 B of headroom** — and the #283
index line runs to roughly **4,300 bytes**. ⛔ **It was NOT truncated, so `index.md` carries FOUR
wrap lines by declaration.** ⚠ **How that was established is stated plainly: arithmetic on two
measured quantities, not a rejected write** — the only way to get the archive's version token is to
read all 48,990 B into the wrap seat, and a failure whose arithmetic is certain is not worth that
price. ★ **Nothing is at risk: the repo is the record and memory is the accelerator.**

---

## ★★ THE DELEGATION RULE — OBEYED A SECOND SESSION, STILL UNINSCRIBED

Measured at this seat by **importing `_checkin.read_fill`** rather than re-implementing it (one
definition of the FILL rule, not two), against the conductor's own transcript
`f00a81c6-812c-4fb9-b2e1-90eec24a3c47.jsonl`:

**NINE lanes, every one an `Agent` at `spawnDepth 1`, model opus** — R2 241,918 · V 170,927 ·
T 145,539 · G 122,502 · C 101,132 · R 93,872 · C2 93,636 · S 83,120 · P 60,602 ⇒
**subs 1,113,248 real (n=9)**, QUOTA and never FILL. **No lane work was done in seat.**

★★ **THE PRICE, BOTH SIDES: the nine lane replies cost the conductor's window 3,399 cl100k IN
TOTAL** — the `s218-D7` stub contract working — against **1,113,248 real of sub FILL**, roughly
**0.3%**; the nine briefs cost **10,528 cl100k** to send.

⚠ **A TENTH SUB — the wrap seat itself — is deliberately EXCLUDED**: its own total cannot be final
from inside itself, and `s214-D5` forbids turning an unknown into a number.

⛔ **THIS DOES NOT CLOSE THE CARRY.** Two sessions' obedience is evidence the shape is affordable,
not a rule that it is required. **Inscribing is his.**

---

## ⬛ #287 — FIRST MOVES, IN ORDER

1. ★ **A SECOND COLD BOOT on the same setup** — Browser blocked, skills and connectors unchanged,
   computer use off, same instrument at the first turn. **That, and only that, turns −7,031 from a
   direction into a measured effect.** A reading, not a lane.
2. **Whether his two sentences are inscribed as rulings** — his word; nothing else can settle it.
3. **The `sizes` KEY NAME**, and **rebuilding the KG explorer** so something actually reads the
   field.
4. **The 58 prose "wall" locations** — ratified text, his to order.
5. **`_seam.py`'s stale DRAFT line** about `_standing.md`, and **`INSEAT_WARN_TK`'s picked 10,000**.
6. Everything still open on `_HANDOFF-130`…`-136`. **Strike nothing without a receipt.**

---

## ⬛ OPEN, RULING-SHAPED, NOT INSCRIBED — the `s271-D4` form: every one is a QUESTION PUT

1. **Whether *"okay go on everything"* is inscribed as a ruling.**
2. **Whether *"okay size-on-the-existing-node"* is inscribed as a ruling.**
3. **The `sizes` key name** — he named the location, not the spelling.
4. **`INSEAT_WARN_TK = 10,000` and the INSEAT block's placement** — PICKED, not ruled.
5. **The 58 prose "wall" locations**, `_RUNBOOK-context-gauge.md:85` among them.
6. **`_seam.py`'s DRAFT line** about `_standing.md` — and the `:26`-vs-`:44` disagreement.
7. **The KG explorer**, not rebuilt to show `sizes`.
8. **The store's id regex** refusing `W-286r2`-shaped ids — third instance in two sessions.
9. **Whether a verifier lane may mutate the tree it verifies** — raised by lane V.
10. **The delegation rule** — obeyed a second session, still uninscribed.
11. **A RESUME contract for the ritual** — carried from #285.
12. **`_seam.py`'s SCRATCH arm** — keep-list, opt-in at a seam, or lanes forbidden `/tmp`.
13. **The memory archive at 48,990 of 49,152 B** — condense, split, or something else.
14. **The boot ceiling** — 73,832, the eleventh breach. Cut the boot; never raise the literal.
15. **The two-probe-shape question on the §A digest** — its eighteenth session.
16. **The third dial** · **the theory door** · **`col26-012`** · **`jade-lifestyle`** ·
    **`s277-D12`** · **what a session should DO at the hard wall** · **the four generators** ·
    **the git-lock runbook line** · **the instrumentation-append policy** — all carried, all aged,
    none touched.
17. **Everything on `_HANDOFF-130`…`-136` not closed today.**

⛔ **CLOSED TODAY, WITH RECEIPTS, AND NOTHING ELSE:** the masters' **registration** (`7ed49d37`,
verified 8/33/8/12 here — row `W-285lm`) and **`knowledge/_standing.md` out of DRAFT** (`1caaa0b1`,
header-only, eight lines byte-identical — row `W-285sc`).

⚠ **TWO THINGS THE WRAP BRIEF NAMED AS STRIKES ARE DELIBERATELY *NOT* STRUCK.** The 256,000 wording
fix and #285's moves 2 and 3 are each **half of a carry whose headline records something that stays
true** — #284's ② (the context-window finding AND his correction) and #285's ④ (the commit took six
runs). **Their discharged halves are said in new carry items instead.** ★ **`s271-D4`: a strike that
is wrong is worse than an item that is merely stale.**

---

## ⛔ STRUCTURAL REDS — inherited, measured here, NOT repairable by a wrap

**SIX blocking gate fails stood at this ritual's open**, carried in the `#243` DECLARED not-a-wrap
form — **the TWELFTH consecutive wrap on that path.** Every one is another session's append-only
testimony in `notes/_GAUGE-LOG.md`:

- **boot-drift CEILING BREACH** — `BOOT_CEILING_TK` 70,000, seven post-diet readings over it
  (#277 83,636 · #278 72,110 · #279 72,447 · #280 75,740 · #281 77,400 · #282 77,474 · #283 80,871).
- **five boot double-counts** — #243 (×5), #264, #272, #273, #274.

✅ **NO SEVENTH WAS BORN IN THIS SESSION'S OWN COMMITS**, and the reason is #285's lesson holding:
lane T added an `INSEAT` counting site to `knowledge/_seam.py`, and the file was **already
registered** in `MEASURERS` as `estimate-only` at #285's wrap — so the obligation was discharged
before it could fire. ★ **A gate that does not fire because last session's declaration still fits is
the registry working.**

Also standing and NOT repaired: `carry_wording_check()` is **blind** (both residual lines are
pointers, so it has nothing to pair and goes quiet); `_gate_scratch_hygiene.py` reports the fill of
`/` rather than `/sessions`; `_seam.py`'s SCRATCH arm still deletes a lane's `/tmp` files mid-run.

---

## ⚙ THE NUMBERS THIS SESSION MUST HAND FORWARD

Measured at this seat against the conductor's own transcript
`f00a81c6-812c-4fb9-b2e1-90eec24a3c47.jsonl` — this wrap seat being
`subagents/agent-a28ac1f02cc558847.jsonl` beneath it, which is what makes the subtraction legal
[[measure-dont-convert-units]].

| term | measured | declared at the brief cut | delta |
|---|---|---|---|
| **FILL** | **162,555 real / 22 turns** | 161,544 / 20 | **1,011** — the hand-over's cost, the smallest on this run |
| **BOOT** | **73,832 real, n=1** | 73,832 | agrees to the token, **and that agreement is what identifies the window** |
| **subs** | **1,113,248 real (n=9)** | ≈1.12M across 9 lanes | agrees |
| lane replies | **3,399 cl100k** (9 briefs cost 10,528) | — | measured here |

✅★★ **THE 180,000 QUALITY LINE WAS NOT REACHED — the close sits 17,445 INSIDE it, the FIRST
session in five.** ✅ 57,445 inside the ≤220,000 tolerance. ✅ **256,000 clear for the fourth session
running.** ✅ **No compaction record and no fill drop** in the conductor's transcript, so the reading
is a fill and not an artefact.

⛔ **BOOT 73,832 IS THE ELEVENTH POST-DIET READING OVER THE 70,000 CEILING AND IS NOT A RE-BASE** —
but it is the first taken with a lever actually pulled at it.

---

## ★ NEXT CHAT TITLE

Generated by `python3 knowledge/_gen_titles.py --session 286`; the receipt is at
`knowledge/_gen_titles_receipt.json` and the line stands at the top of `GOOD-MORNING.md`:

> `Apollo - #287: the connector lever reads −7,031 and the ceiling still stands`
