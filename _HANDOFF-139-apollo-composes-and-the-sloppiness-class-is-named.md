# HANDOFF #139 — #288 → #289 — HE CALLED THE TEMPLATE PATH "A DOT-TO-DOT BOOK", A PROBE RAN WITH THE TEMPLATE FENCED OFF, AND HE RULED BY EYE THAT APOLLO COMPOSES — THEN NAMED THE DEFECT CLASS: ALIGNMENT, SPACING, DIMENSIONS

provenance: 288 · 2026-09-19
status: observed

*Written by the delegated OPUS 5 wrap sub at the close of #288 (conductor Fable 5.1). Every figure
here was MEASURED at this seat unless it says otherwise; where a measurement disagrees with the
brief's declaration, both readings are published and neither is rewritten.*

✅ **NO DATE SPLIT.** #288 opened, ran and wrapped inside **2026-09-19** (`date` read at this seat:
`Sat Sep 19 12:58:14 BST 2026`).
⛔★★ **NO RULING WAS INSCRIBED. `knowledge/_rulings.json` STAYS AT 622** — verified here by
`json.load` over the `rulings` list, with **no `s288-` id present**. **He did not say *"inscribe"*
today.** Everything ruling-shaped below is a **QUESTION PUT** (`s271-D4`), never a state of the
world.

---

## ⛔ READ FIRST, IN THIS ORDER

1. **This file.** It is newer than `_CHAIN.md` and **OUTRANKS it**.
2. `_CHAIN.md` — the read contract (header → ★ LATEST banner → ⏱ LATEST delta).
3. ⛔ **It does NOT replace `_HANDOFF-130`…`-138`.** Every open item on those nine still stands.
   ⛔ **NOTHING WAS STRUCK TODAY** — see § THE ZERO STRIKES, which is a decision with a reason.
4. `notes/_lanes/288/DAVE-RULINGS-2026-09-19.md` — his words, verbatim. **Quote them; never
   paraphrase them.** The conductor's own *readings* are marked as the conductor's in that file;
   keep them marked.
5. `_CARRIES.md` § `residual → #289` when you need the bodies — **572 probeable items**. Do not
   read it at boot; fetch the section (`_memento_search.py` → `--fetch carries:residual-289`).

---

## ⛔⛔ DAVE'S — VERBATIM, AND THERE ARE FIVE MOMENTS

**At the opener** — parked, and explicitly not for now:

> one thing I noticed and something to add to future design experimentation, the Swiss design skill
> is very very successful, the we managed to hit the aesthetic pretty bang on. I want to borrow some
> of it for Apollo as some sort of taste guarantee or guidence.
>
> Not for now but don't let me forget this.

**On the presentation** — it is in the plan, and its structure changes:

> so we also have to actually prepare and create the presentation, we made a good start but I want
> to change the structure. please include this is the plan. we might have to parallelise some of
> this to get it nailed quickly. One question on this, can parallel agents cross communicate rather
> than having solo lanes that report to the orchestrator?

**On parallelism, then the go:**

> cool, but lets think about parallelising  the entire task  set too

> go

**After wave 1 landed, on seeing the showroom template — the premise is challenged:**

> there is a definitely a problem here, 2 actually, the template is a bit wonky and it's basically
> copied by the AI, what is the point of building the KG if the agent just traces an existing file,
> its very safe but 1. its of average quality anyway... its okay but not great 2. what was the point
> of building the system if it just traces from existing examples???
>
> the results with VS co-pilot are pretty much identical to this, this isn't Apollo its a dot-to-dot
> book

**On the probe sheet — his ruling by eye:**

> so better is someways and worse in others, its more interesting and complete page , but its more
> sloppy .
>
> I'll create a test brief for these tests.
>
> we need to do something about the sloppiness and, its all about alignment spacing and dimensions,
> this looks like its working the way I'd have expected, at least it's diverged from the tamplete

⚠ **WHAT THE CONDUCTOR SAW ON THE RENDER IS THE CONDUCTOR'S AND IS MARKED AS HIS** in the rulings
file: KPI tiles with dead space below the sparklines, a half-empty programmes-table card against a
full-height decisions card, two bottom-row cards not sharing a bottom edge, a filter bar narrower
than the table it filters, and column gutters differing between the KPI row and the rows below.
**None of that is Dave's sentence.**

---

## ★★★ THE SESSION IN ONE ARC

Wave 1 cut **four Opus lanes** and all four landed: the strand map, the four-theme gutter sheet, the
`s219-D3` generation arm, the template quality review. Then Dave looked at the artefact three of
them had been circling all morning and **did not comment on its gutters — he challenged the premise
of the entire one-shot path.**

The reply was not an argument. A fifth Opus lane ran a **composition probe with the template
UNREACHABLE** — a cold one-shot, two renders side by side, nothing in the repo changed. That shape
has a property worth naming: **it cannot be won by rhetoric.** Either the composed page is
recognisably Apollo or it is not, and Dave decides by eye.

He did, and **his verdict splits in two, and the split is the finding.** The **METHOD** is endorsed
— *"diverged from the template"*, *"working the way I'd have expected"*. The **EXECUTION** is not —
*"more sloppy"*. Collapsing them into one verdict would lose whichever half is inconvenient, so the
record keeps both, separately.

⛔ **AND THE MECHANISM BEHIND HIS COMPLAINT IS ALREADY IN THE RECORD AND HAD NEVER BEEN READ THAT
WAY: the #230 *"zero invented markup"* pass condition REWARDS tracing.** A builder that splices the
template byte-identically scores perfectly against it. The condition was written to stop invention;
it also, silently, stops composition. **Whether it is re-cut, scoped or replaced is HIS.**

---

## ★ WHAT LANDED — 0 lane commits; **this wrap is #288's ONLY commit**

⛔ **#288 HAS NO LANE COMMIT AT ALL.** At this ritual's open `git status --porcelain` read **9
modified and 5 untracked paths** against HEAD `4cc8b37a`, and every one is accounted for: lane A's
four edited files, lane M's strand map, the whole of `notes/_lanes/288/`, the five filed lane
reports, `knowledge/_state.json` (the lane rows), and two instrumentation-append files. ⚠ **THREE
PATHS WERE INHERITED DIRTY FROM #287 AND ARE COMMITTED HERE:** `notes/_REHEARSAL-LOG.jsonl`,
`notes/_dream/_GRADE-DECISIONS.jsonl`, and `notes/_lanes/287/DAVE-RULINGS-2026-09-19.md` **with six
lines appended AFTER #287's commit — the wrong-order class, THIRD consecutive session.**

★ **#287's report named the cause of its own clean single-invocation commit — a commit lane that
reads the prior lane's report first — and #288 did not repeat the shape**, so every gate this wrap
must drive green was driven at the wrap seat under wrap heat instead. **This wrap's sha, push
verdict and CI read are in the ★ LATEST banner and the filed report** — a commit cannot name itself.

| deliverable | path | filed report |
|---|---|---|
| the strand map | `notes/_STRAND-MAP-2026-09-19.html` | `notes/_subreports/2026-09-19-288-M-strand-map.md` |
| the four-theme gutter sheet | `notes/_lanes/288/B/four-themes.html` | `…-288-B-bento-renders.md` |
| the `s219-D3` arm | `knowledge/canon/gen_bento_role_vars.py` | `…-288-A-s219-d3-arm.md` |
| the template quality review | `notes/_lanes/288/T/template-quality-review.html` | `…-288-T-template-quality.md` |
| the composition probe | `notes/_lanes/288/P/{probe-sheet,composed-dashboard}.html` | `…-288-P-composition-probe.md` |

---

## ★★ THE `s219-D3` ARM IS BUILT — AND IT STILL DOES NOT SETTLE MONO'S 0

Lane A wrote `knowledge/canon/gen_bento_role_vars.py`, **289 lines**, the **FIRST CONSUMER** of
`knowledge/_render/_bento_edit_rails.json` — the `s219-D3` *one generated file*, self-declared
`$groundwork_only` and un-consumed since **#219**. It:

1. **cross-checks the manifest against the owner the file itself names** —
   `gen_foundations_217.ROLE_DEFAULTS` → `role_defaults_219.DEFAULTS`, **parsed from Dave's own
   receipt** `notes/_receipts/2026-08-25-219-role-defaults-exports.md` rather than typed — and **a
   divergence is a NAMED REFUSAL, not a silent pick**;
2. mints per-theme `--bento-dashboard-main` / `--bento-dashboard-sub` between new
   `AUTO-BENTO-ROLE-VARS` markers placed **BEFORE `AUTO-THEMES START`**, because
   `gen_theme_cascade.py` rebuilds everything from that marker onward and would silently destroy a
   block placed after it — **asserted by the generator's own selftest bite, not remembered**;
3. turns the snippet's two pinned literals at `:802` / `:804` into `var()` references whose mono
   fallbacks are **load-bearing, not leftovers** (no snippet links `canon.css` — 0 of 137).

✅ **Gates green, quoted verbatim in the lane's report:** `gen_bento_role_vars --check OK — 4 themes
x 2 dials in sync with the rails manifest` · `gen_bento_role_vars selftest OK — 6 bites` ·
`gen_canon_components --check OK — 137 components in sync` · `gen_theme_cascade --check OK — 230
override path(s), 397 component projection(s)` · `binds-resolve gate: 137 snippets … 0 failure(s)`.

⛔ **AND NOW THE PART THAT MATTERS: IT DOES NOT ANSWER MONO'S 0.**

| quantity | readings | ruled? |
|---|---|---|
| OUTER (structural bento) gutter | token `layout/bento/gutter` **0/24/24/0** (`s217-D2`) · role defaults **40/24/40/24** | **NO — no winner picked** |
| INNER (embedded bento / tile-group) gutter | canon's literal **1px** · `subSpacing` **4/4/4/2** · the template's pinned **4** | **NO** |
| supercharge's 0 | inherited from the token table | **NEVER DECIDED BY ANYONE** |

The arm delivers the role-default reading **because that is the file `s219-D3` names**, not because
anyone chose. ★ **The lane DECLARED its pick and picked no winner** — which is exactly what Dave's
#287 gutters correction requires: outer and inner are two deliberate quantities, not a contradiction.

⚠ **Lane B confirmed the structure FROM SOURCE AT HEAD**, reading every line through
`git show HEAD:<file>` because lane A was editing the same tree in parallel: `canon.css:1082` gives
any bento the theme gutter, `:1182` gives a plain-tile dashboard the inner `1px`, and `:1198`
restores the theme gutter for a **bento-of-bentos**, with canon's own comment saying so — *"a
dashboard whose tiles are themselves bentos IS the outer wall."*

---

## ⚠★★ THE PROBE MEASURED THREE THINGS NO GATE IN THIS TREE CAN SEE

`COUNTS: decisions 39 · gaps 10 · findings 6 · UNPROVEN 2` — **39 reads, 0 fenced**, each measured
on a live render.

1. ⛔ **CANON'S OWN INSTANCE-DIAL RECIPE LOSES SILENTLY.** Canon instructs *"declare instance dials
   as `.c-bento.my-wall{…}`"* at specificity **(0,2,0)**; the `:has()` rule that restores the outer
   gutter for a bento-of-bentos is **(0,4,0)**. **MEASURED: a bare
   `.c-bento.wall-ops{--bento-gutter:40px}` had NO EFFECT and the wall rendered at 0px, not 40.**
2. ⛔ **CANON CLIPS SILENTLY.** A 694px list stretched a row to 791px inside a wall canon fixes at
   320px and `overflow:hidden` swallowed it — **no symptom in the DOM, `pageErrors: []`**.
3. ⛔ **NO GATE READS THE COMPOSITION OF A PAGE THAT LINKS `canon.css`.**
   `_validate_composition.py` reads the grammar from the artefact's own `@container` blocks, so the
   orphan-cell arithmetic `DP-16` and `s249-D5` rest on is uncheckable there — and this is the first
   linked page in the tree.

★ **The common shape of all three is worth carrying: a failure with NO SYMPTOM** — a correct-looking
page, a green console, and a value that never landed. **Ten gaps named `G1`…`G10`; none fixed, none
ruled** — every one is a generator or gate change, and a probe lane may not make one.

⛔ **AND THE FROZEN DEMO PROMPT IS NOT RECOVERABLE FROM THE RECORD.** Lane P searched
`_memento_search.py` (and `--all`), the #229 / #230 / #258 briefs, and grepped the repo for the
prompt's distinctive phrases. **Every brief DESCRIBES the test; none carries the text.** The probe
ran on a fallback wording, **printed on the sheet**, and ⛔ **the two pages compared side by side
ANSWER DIFFERENT BRIEFS** — the traced template was built against a business-banking account
overview. ⇒ **The comparison is about HOW each was made, never about which answers a shared ask
better.** ★ **A lane that prints the limit of its own comparison is the reason his verdict can be
trusted as a verdict on the METHOD** — and the gap it exposes is exactly what *"I'll create a test
brief"* closes.

---

## ★★ THE TEMPLATE QUALITY REVIEW — THE DRAWN PAGE IS SOUND, THE PAPERWORK IS NOT

Lane T, read-only, no generator and no gate run. **Zero invented markup · zero dangling `var()` (82
names referenced, 125 declared) · zero raw `font:` declarations · zero hex outside the two theme
blocks · zero raw inline lengths.** Every finding is in the metadata, the status derivation or the
delivery path — **i.e. in exactly the layer a one-shot machine reads.**

- ⛔ **Four of the five claims in the meta's own `$status` block measure FALSE.**
- ⚠ **The inline-style census re-run: 87 attributes carry a raw px/rem with no `var()`**, against
  #287's **88**. The lane names the cause — six attributes span line breaks, so a per-line grep and
  a multi-line regex disagree by one — and **both readings are published, neither rewritten.** The
  91 raw declarations inside them are **layout geometry, not theming risk**; six `font-size`
  specimens are the only theme-fragile ones.
- ⚠ **Correction to the wrap brief, from the lane: the meta carries THREE `$awaitingDave` entries
  at HEAD, not four** (`:190-193`), plus two `$tokenGaps` (`:195-196`). See § THE THREE COUNTS.
- ⬛ **A six-question decision pack, unanswered.**

---

## ⛔ THE ZERO STRIKES — A DECISION WITH A REASON, NOT AN OVERSIGHT

**Exactly one thing #288 closed can be receipted: the `s219-D3` arm is BUILT** (lane A, gates green,
four-theme proof). The carry that names it reads **"MONO'S 0 IS DOUBTED AND THE `s219-D3` GENERATION
ARM IS TO BE BUILT"**.

`s183-D1` strikes a **HEADLINE**, not a clause of one — and the first half of that headline is still
true: **`s217-D2` still rules the value 0 for mono and Dave ruled nothing on it today.** Striking
would delete a true claim in order to record a true one.

⇒ **The discharge is said in NEW carry item ⑩ instead** — the identical move #287 made for the
connector-lever item, for the reason `s271-D4` gives in its own words: ***"a strike that is wrong is
worse than an item that is merely stale."***

⚠ **#138's move 2 is therefore PARTLY discharged and the receipt is named:** its *"the generation
arm … was ruled at #219 and never built"* clause is closed by
`notes/_subreports/2026-09-19-288-A-s219-d3-arm.md` and this wrap's commit; **its Mono's-0 clause is
not, and no other item on `_HANDOFF-130`…`-138` was touched.**

★ **The general shape, because it keeps recurring:** when a session closes HALF of a carry, the carry
does not move and a new item records the half. **Two wraps in a row have now answered this the same
way** — whether it becomes a rule is Dave's.

---

## ⬛ OPEN, RULING-SHAPED, NOT INSCRIBED — the `s271-D4` form: every one is a QUESTION PUT

1. **What the one-shot generates FROM** — and what becomes of the #230 *"zero invented markup"*
   pass condition, which rewards tracing.
2. **The sloppiness class — alignment, spacing, dimensions.** Named by him; what is done about it is
   not settled.
3. **The test brief for these probes — OWED BY DAVE HIMSELF.** *"I'll create a test brief for these
   tests."* No lane may write it.
4. **The outer gutter (two readings) and the inner gutter (three).** Supercharge's 0 inherited and
   never decided.
5. **Which event Friday 2026-09-25 is** — small internal demo or David Rice. **NOT ESTABLISHED**,
   and the strand map says so on a band of its own.
6. **The deck's new structure** — his, not yet given. **Wave 2 is blocked on it.** And his question
   in the same turn: *"can parallel agents cross communicate rather than having solo lanes that
   report to the orchestrator?"*
7. **The Swiss design skill as an Apollo taste guarantee** — **PARKED BY HIS WORD**, with *"don't
   let me forget this"*. Cloud copy `swiss-skill-as-apollo-taste-guarantee-288.md`.
8. **Lane T's six-question decision pack** — the inline-style rule (87 / 88), the template's status
   and whether `showroom/index.json` can express *proposed*, the three `$awaitingDave`, a max-width
   ruling.
9. **The specificity root cause** — canon's instance-dial recipe silently losing for the dashboard
   role, measured at 0px instead of 40.
10. **The showroom re-sync after the arm · a `--check` arm for `dashboard/index.html` · the deck's
    stale graph figures (2,837 on the slide against 4,820 today) · the one-shot's quality dark since
    2026-09-02.**
11. **Everything on `_HANDOFF-130`…`-138` not closed today** — all of it, **nothing struck**.

*(Also carried, unchanged and not re-minted: the `subs` definition · `_to_delete/` on a persistent
disk · `INSEAT_WARN_TK` · the delegation rule · the store's id regex · a RESUME contract for the
ritual · `_seam.py`'s SCRATCH arm · the memory archive at 48,990 of 49,152 B · the two-probe-shape
question on the §A digest, now at its TWENTIETH session · the third dial · the theory door ·
`col26-012` · `jade-lifestyle` · `s277-D12` · the four generators · the git-lock runbook line · the
instrumentation-append policy · the `#243` not-a-wrap form.)*

---

## ⚠ FOUR DECLARED LANE JUDGMENTS, AND ONE MEASURED DEFECT IN THE REPORTS THEMSELVES

**(a) Lane A ran `git stash push`** to A/B a gate against the pre-change tree — **a git write from a
build lane, against the standing rule** — and declared it itself: no stash entry was created
(`git stash list` empty before and after), no file was altered, and the tree was verified intact
immediately afterwards. ★ **A judgment named in a report is one the conductor can correct; one
discovered at the wrap is one he inherits.**

**(b) Lane P's `_validate_screen.py` run wrote a stray** `knowledge/_screen-gate/composed-dashboard.md`,
moved by the conductor to `_to_delete/288-P-stray/` by **`mv`, never `rm`** (the #284 / #286 / #287
precedent), `.gitignore` untouched, nothing excluded from staging. `_COMPOSE-AUDIT.md` was restored
to HEAD by the lane.

**(c) `knowledge/_render/seat_env.sh` globs a playwright path 1.63 no longer ships** — **lane B
symlinked around it and rendered; lane T could not render at all** and said so rather than reporting
a CSS reading as a rendered one. **A one-line fix is owed and NOT done.**

**(d) ⚠ FOUR OF THE FIVE FILED LANE REPORTS CARRY NO `RULING-SHAPED QUESTIONS` HEADING**, although
`s218-D7` clause 4 makes it mandatory — measured here by grep, not assumed. **Only lane M carries
it.** Lane T has a *Decision pack — six questions* doing the same work under another name; A, B and
P carry the substance under *WHAT IS NOT DONE*, *WHAT I COULD NOT ESTABLISH* and *Gaps*. ⛔ **The
CONTENT is present in every case — what is missing is the NAME the gate and the next reader look
for**, and `subreport_citation_check` parses exactly that heading. Also measured: **only lane P
carries a `COUNTS:` line and only lane T a `REPLAY-THESE:` line.** ⛔ **NOT REPAIRED HERE** — a wrap
does not edit another seat's filed report, which is dated history (`ADR-0017` / `s192-D1`).

---

## ⚠ THE THREE COUNTS — PUBLISHED, NOT RECONCILED

`$awaitingDave` in `knowledge/components/template-dashboard-bento.meta.json`: **`_HANDOFF-138` says
four · lanes M and T each read three at HEAD · lane A's additions take the tree to five, by
ADDITION.** ⛔ **Nothing is reconciled and nothing is rewritten** — four is a dated reading, three is
HEAD's, five is the tree after lane A — and **no fourth number is minted**, because the file itself
answers the question for one read [[measure-dont-convert-units]].

---

## ⛔ STRUCTURAL REDS — inherited, measured here, NOT repairable by a wrap

**SIX blocking gate fails stood at this ritual's open**, carried in the `#243` DECLARED not-a-wrap
form — **the FOURTEENTH consecutive wrap.** Every one is another session's append-only testimony in
`notes/_GAUGE-LOG.md`: the **boot-drift CEILING BREACH** and **five boot double-counts** (#243 ×5,
#264, #272, #273, #274). **A wrap may not repair an inherited gate fail** — the ceiling's remedy is
to CUT THE BOOT, never to raise the literal [[gate-must-quote-what-it-forbids]].

⚠★★ **AND A SEVENTH WAS BORN BY THIS WRAP'S OWN MANDATED 2f ROLL, DECLARED HERE RATHER THAN
DISCOVERED: `boot double-count: session #287 states a first-turn figure 2 times`.** Rolling #287's
stratum into `notes/_GAUGE-LOG.md` — which step 2f REQUIRES — carried in a second mention of
**74,120**, inside that stratum's *"the `delta` above is a legal subtraction"* paragraph, where the
figure is a **COMPARISON and not a reading**. ⛔ **NOT REPAIRED:** the gate's own remedy is to delete
or re-word that sentence, and it is **another session's ratified testimony in an append-only file** —
**a wrap may not re-word ratified record to unblock itself.** ★ **The shape will RECUR: any stratum
that states its boot figure twice manufactures this fail the moment 2f rolls it, so the fix belongs
in the stratum TEMPLATE — state the figure once, name it a comparison elsewhere — and not at the wrap
that inherits it.** ⚠ **This wrap's own #288 stratum was written to avoid it (its delta-legality
paragraph names no number), so #289's roll should not add an eighth — a prediction, labelled as one.**

Also standing and NOT repaired: `carry_wording_check()` is **blind** (both residual lines are
pointers, so it has nothing to pair and goes quiet — the `s188-D2` invariant was held BY HAND here,
fourth wrap); `_gate_scratch_hygiene.py` reports the fill of `/` rather than `/sessions`;
`_seam.py`'s SCRATCH arm still deletes a lane's `/tmp` files mid-run; two REGEN-SERIAL advisory
warns stand uncleared because their runner is `_build_all.py` and this seat was fenced from it.

---

## ⛔★★ A NEW CI RED THAT IS **NOT** INHERITED — read job by job on the pushed sha

Run **`35443934929`** on `head_sha=71b3363c` is **completed / failure**, polled to completion over
the public API:

| job | verdict | steps |
|---|---|---|
| **gates** | **failure** | 5 and 6 — **the same two inherited** |
| **render** | **success** | — |
| **release** | ⛔ **failure** | **12 — `Build-script selftest — the release refusals, driven (BLOCKING)`** |

⛔ **`release` was SUCCESS on `4cc8b37a` and on `04dbeab1` — re-read here job by job rather than
remembered — so this one is NEW.** The cause was reproduced locally:
`bash apollo-spider/build-designer-pack.sh --selftest` prints **`RED — the real receipts are STALE;
a release would be refused right now. Re-drive: python3 knowledge/_drive_chart_engine.py`** (249
bites, 0 fail; the other two refusal arms green).

⛔ **THE NAMED REMEDY COULD NOT BE RUN FROM THIS SEAT:** `_drive_chart_engine.py` dies with
`ModuleNotFoundError: No module named 'playwright'` — **the same render-seat hole lanes B and T
met**, which is now a fail with two independent victims and a third consequence. ⇒ **#289's first
mechanical job is to install the render seat and re-drive the chart engine.** ⚠ **The timing points
at lane A's `canon.css` change staling the chart receipts; that is NOT PROVEN from this seat and is
stated as a suspicion, never as a cause.**

⚠ **AND #288 SHIPS TWO WRAP COMMITS, DECLARED: `384c26a7` then `71b3363c`.** Naming one path to
satisfy the mention-map gate switched `_git_commit.sh` into explicit-path staging and left 31 paths
unstaged on the first run; the second named every one and the tree closed clean. Nothing was lost —
but a reader should not discover it in `git log`.


---

## ⚙ THE NUMBERS THIS SESSION MUST HAND FORWARD

Measured at this seat by **importing `_checkin.read_fill`** rather than re-implementing it, against
the conductor's own transcript `f0d5834b-1313-45d3-9961-2f10be3c3817.jsonl` — this wrap seat being
`subagents/agent-aa9e27e75caeae075.jsonl` beneath it, which is what makes the subtraction legal
[[measure-dont-convert-units]].

| term | measured | declared at the brief cut | delta |
|---|---|---|---|
| **FILL** | **197,234 real / 33 turns** | 180,585 / 27 | **16,649** — the largest hand-over on this run |
| **BOOT** | **74,174 real, n=3 on this setup** | 74,174 | agrees to the token, **and that agreement is what identifies the window** |
| **subs** | **1,004,095 real (n=5)** — and **998,602** by `read_fill` | 1,004,095 | ⚠ two definitions, both published |
| peak / compaction | **197,234 · 0 records, 0 drops** | — | measured here |

⛔ **THE 180,000 QUALITY LINE WAS CROSSED BY 17,234, AND THE BRIEF SAYS WHERE: the stop line was
crossed by 585 ON THE WRAP CALL ITSELF.** The ritual fired *at* the line as `ds-023` asks; the
16,649 that followed is the hand-over, and its cause is nameable rather than mysterious — **six
further conductor turns landed after the brief was cut** (the DAVE-RULINGS file, the brief itself,
the lane reconcile). ✅ **22,766 inside the ≤220,000 tolerance.** ✅ **256,000 clear for the sixth
session running.** ✅ **No compaction record and no fill drop**, so the reading is a fill and not an
artefact.

⛔ **BOOT 74,174 IS THE THIRTEENTH POST-DIET READING OVER THE 70,000 CEILING AND IS NOT A RE-BASE.**
★ **n=3 on the Browser-blocked setup — 73,832 · 74,120 · 74,174, a spread of 342 where the prior
seven-reading spread was 11,526 wide** — so the −7,031 connector lever holds as a measured effect.

⚠★★ **THE `subs` DEFINITIONAL SPLIT IS RE-MEASURED AND RECONCILES AGAIN TO THE TOKEN.** Per lane:
M 162,304 · B 177,908 · A 174,021 · T 190,369 · P 299,493 (the harness's per-agent figures, as
declared) against M 161,333 · B 176,769 · A 172,457 · T 189,501 · P 298,542 by `read_fill` —
**input side only, the ONE definition of FILL this repo has**. **The gap is 5,493 and it is exactly
each lane's final-turn `output_tokens`, matching on every one of the five (971 · 1,139 · 1,564 ·
868 · 951).** ⇒ **Both published, neither rewritten; which one the `subs` line means is HIS**, its
SECOND consecutive wrap. ⚠ **A SIXTH SUB — this wrap seat — is EXCLUDED BY DECLARATION and no
placeholder is written for it** (`s214-D5`).

---

## ★ NEXT CHAT TITLE

Generated by `python3 knowledge/_gen_titles.py --session 288`; the receipt is at
`knowledge/_gen_titles_receipt.json` and the line stands at the top of `GOOD-MORNING.md`:

> `Apollo - #289: apollo composes and the sloppiness is alignment spacing and dimensions`
