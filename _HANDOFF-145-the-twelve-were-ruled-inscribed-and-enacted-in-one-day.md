# HANDOFF #145 — #294 → #295 — THE TWELVE WERE RULED, INSCRIBED AND ENACTED IN ONE DAY

provenance: 294 · 2026-09-21
status: observed

*Written by the delegated OPUS 5 wrap sub at the close of #294 (conductor Fable 5.1). Every figure
here was MEASURED at this seat unless it says otherwise; where a measurement disagrees with the
brief's declaration, both readings are published and neither is rewritten.*

✅ **NO DATE SPLIT.** The session opened Monday **2026-09-21**; all eight lanes ran that day and **all
five commits carry it** — `dd374131` · `ea930e72` · `698d8ddf` · `e31aa34b` · `f81bbdd4` — as does
this wrap's own. `date` at this seat read `Mon Sep 21 18:35:33 BST 2026`. **No sixth DATE-SPLIT line
was added to `GOOD-MORNING.md`'s header and the five that stand were not touched** — and that
restraint is now RULED rather than improvised: **`s294-D11`** (*"both"*) puts the **session date on
keys and filenames** and the **commit date on ONE header line**, which is the shape the five already
take. ⛔ **#241's ruling-shaped question is CLOSED**, and the age it was carried at was **WRONG**: the
handoffs said *"age 52"*; lane R2 re-measured it as **19 days** (2026-09-02 → 2026-09-21). **The 52
is SESSIONS and the unit was never labelled on any of the five surfaces that repeated it.**

⛔★★★ **TWELVE RULINGS WERE INSCRIBED. `knowledge/_rulings.json` READS 634** — verified here by
`json.load` over the `rulings` list, **id and count only, never a printed record**: **622 → 634**,
the newest twelve reading `s294-D1` … `s294-D12` in the review page's own order, immediately after
`s287-D2`. **He said *"inscribe, N+3 is fine"*.** ⛔ **INSCRIPTION IS NOT STAMPING: eleven of the
twelve are ENACTED IN CODE and NONE carries `status: enacted`** — see § OWED item 3.

---

## ⛔ READ FIRST, IN THIS ORDER

1. **This file.** It is newer than `_CHAIN.md` and **OUTRANKS it**.
2. `_CHAIN.md` — the read contract (header → ★ LATEST banner → ⏱ LATEST delta). **10,480 tape**
   (slice 9,627 + the generator's 853 wrapper), **ratio 28%** against its own `<40%` floor. ✅ Inside
   the `s214-D6` ~10–12K target and **DOWN 1,189 on #293's 11,669** — #292's ★ PRIOR banner rolled
   out at 2c while this wrap wrote its banner at exactly the cap.
3. ⛔ **It does NOT replace `_HANDOFF-130`…`-144`.** Every open item on those fifteen still stands
   **except the FOUR struck here with receipts** — see § THE FOUR STRIKES.
4. `notes/_lanes/294/DAVE-RULINGS-2026-09-21.md` — his seven entries, verbatim, in order, with what
   was done on each. **Quote them; never paraphrase.**
5. `_CARRIES.md` § `residual → #295` when you need the bodies — **639 probeable items**. Do not read
   it at boot; fetch the section (`_memento_search.py` → `--fetch carries:residual-295`).
6. The eight filed lane reports, `notes/_subreports/2026-09-21-294-{R,C,I,A,B,G,C2,W}-*.md`, plus the
   two of another worker's carried here (`…-293-J5-…`, `…-293-J6-…`).

---

## ⛔⛔ DAVE'S WORDS — SEVEN ENTRIES VERBATIM

> Good Morning! There is another worker lane running, you will be conducting

> go

> ill go with all the recommendations

> In the future if we decided to actually integrate jev as a tool for Apollo (It's doubtful we will be able to on a work computer) I'd like to have the option, but this probably doesn't change the decision.

> inscribe, N+3 is fine

> teh whole contents of to delete? all files and folders?

> done, you can wrap

**Read three of them carefully.** ⚠ **The RIDER is not noise, and it is the one clause a later
reader will drop.** It ends *"probably doesn't change the decision"* and he is right — it does not
change the ruling, it **changes what enactment may do**: no arm may be built that would make full
integration harder, and **the adapter stays, uncalled by the build.** It is inscribed INSIDE
`s294-D10` rather than beside it, so the ruling cannot be read without it. ⚠ **`"inscribe, N+3 is
fine"` is the whole authority for TWO things** — the twelve going into the store, and `s294-D12`'s
**N = 3 sessions**. ⚠ **`"teh whole contents of to delete?"` is a QUESTION he asked, not a ruling**;
it was answered *yes, everything to the Trash (not Empty Trash), keep the empty folder*, and **he
then did it by hand.**

---

## WHAT LANDED — FIVE COMMITS BEFORE THIS WRAP'S, ALL 2026-09-21

| sha | what |
|---|---|
| `dd374131` | **lane C** — the review page, his rulings file, lane J4 carried. **Zero refusals, first run**; 14 files, 3,975 insertions |
| `ea930e72` | **lane I** — the twelve inscribed, `_rulings.json` 622 → 634 |
| `698d8ddf` | **lane I** — `notes/_RULINGS.html` re-rendered, clearing the `s263-D10` red its own first commit caused |
| `e31aa34b` | **lane I** — its filed report completed with both shas |
| `f81bbdd4` | **lane C2** — the enactment wave, three lanes, **twenty-four named paths, `add -A` never used** |

⛔ **NOTHING IS PUSHED.** `git ls-remote` was not consulted because there is nothing to compare: **the
push verdict is *"not pushed — conductor's judgement"***, by the brief's own instruction, and **no CI
run exists on any of the six shas. None is claimed.**

**1 — THE PAGE WAS BUILT AND HE TOOK ALL TWELVE.** Lane **R** (Opus, depth 1, read-only on git —
a foreign `.git/index.lock` was present at its open and was not touched) built
`notes/_lanes/294/R/review-page.html`: **33,215 B, single self-contained file, 0 external references,
0 script tags**, Swiss idiom, **answer sheet first**, ten items in four groups, each with a
plain-prose title, two or three sentences of what it means, **one recommendation** and the **exact
one-word slot**. It **REPLACES `notes/_lanes/293/DA/dream-enactment-audit.html` as the ruling
surface** and says so in its own nav and footer. Exclusions were **measured, not asserted**: 39
proposals examined · 34 excluded (22 ruled+enacted · 9 enacted the previous day · 3 overtaken) · 5
remaining, all on the page; 18 lane ruling-shaped questions examined, 8 on the page.
★ **Lane R2** then added items **11** and **12** **by addition on lane R's own could-not-resolve
note** — the cheapest brief available, and it closed two items open for nineteen days and one hundred
and eighty-five sessions.

**2 — THE TWELVE INSCRIBED, AND THE TOOL WAS RED ON ARRIVAL.** Lane **I**: every one of the twelve
went in through `knowledge/_inscribe_ruling.py --write`, **the only sanctioned writer, one entry per
invocation**, each printing its own reconstruction proof; `git diff --numstat` read **217 insertions,
0 deletions** — a pure append, with the 622 pre-existing records **compared object-by-object against
a pre-inscription copy: IDENTICAL**, and `_README` (20 keys) identical too. **Rehearsed before it was
written:** all twelve `--dry-run`'d individually against a copy, then the full twelve-in-sequence run
played end-to-end against a second copy and checked, **before the live file was touched.** ⛔ **Three
refusals were paid**, including two dead selftest arms in the inscription tool itself and the
`s263-D10` stale rulings page. ⚠ **Lane I's own open question: the `--amend-evidence` path had a hole
for as long as `armA1`'s plant was dead; both are fixed, and whether any amend run between #193 and
today SWALLOWED A BYTE is NOT MEASURED and would need a diff sweep of the file's history.**

**3 — ELEVEN ENACTED IN CODE, THREE LANES, ONE COMMIT, NO CONSTANT MOVED.**
⛔★★ **THE SHARPEST RESULT IS AN ABSENCE, AGAIN: `s294-D1` COULD NOT BE ENACTED AS WRITTEN BECAUSE
THE SOURCE OF TRUTH IT ASSUMES DID NOT EXIST.** Lane **A** measured first: `_build_survey.py`
**printed its verdict and wrote nothing** (one `open()` in the file, for `ast.parse`), **no CI run
record on disk**, and `grep -rn "SURVEY" knowledge/*.py` outside the survey returned **zero**. So it
**built the source of truth at the instrument that takes the reading** — `notes/_BUILD-VERDICT-LOG.jsonl`
— and the chain's most-read sentence now reads, generated: **"53 of 146 steps GREEN — 9 FAIL · 1
COULD-NOT-ASK · 6 unaskable · 77 NOT ASKED (mutating)"**, with the #62 clause surviving as its own
fact from its own AST (**71 have never been in that verdict**; 75 existed at `18c7789`). ⚠ **Step
INDICES, not counts** — a full non-mutating pass **cannot be taken in one call at this seat**
(measured twice, cut at 178 s then 72 s; *"the cap is not even stable"*), so the sentence is
permanently assembled from chunks, indices union, the newest record wins per step, and a step whose
verdict changed inside one sha is **counted (`conflicts`) and published, not smoothed**.
★ **AND A SECOND CONSUMER WAS FOUND BY GREP AND FIXED WHERE IT WAS FOUND** — `_gen_schematic.py`
rendered the same sentence from the same pinned sha; *"fixing only the home the ruling names would
have left two artefacts publishing different green claims from one repo."*
`knowledge/_gen_size_stamp.py` is **NEW, 23 bites**, and its **live stamp was deliberately left for
this wrap.** `_build_all.py` went **144 → 146 STEPS with their `ROUTE_ROWS` in the same edit** (a
STEPS entry with no route aborts every build above step 1 — #119/#164).

Lane **B** (`s294-D2` · `s294-D3` · `s294-D9` · `s294-D12`), two files, both its own: the
report-lines gate scoped to the **STAGED** set; `WRAP_COMMIT_SUBJECT_RE` re-pointed at the shape
`_git_commit.sh:219-220` actually writes (**129 matches → 21**) with a sibling
`NONWRAP_COMMIT_SUBJECT_RE` **asserted DISJOINT rather than merely matching**; the pre-flight stamp
widened to admit a measured CAPTURED line; the `_to_delete/` arm at **N = 3**, with session
derivation from **the entry's own name where it carries one (40 of 108), otherwise the newest commit
older than its mtime (66), and undated entries reported rather than refused on** — *"name-only would
have silently excused 68 entries."*
⛔★★ **TWO OF THE FOUR WENT RED ON SIGHT AND LANE B SAID SO IN ITS FIRST PARAGRAPH: *"that is the
ruling's intent rather than a defect."*** The live gate went **8 → 12**. And on `s294-D12` it named
three possible shapes and **enacted only the literal one**: *"I enacted (a). I did not enact (b) or
(c) — inventing a hatch for a ruling is the same overreach as inventing the ruling."* **Four hours
later the gate was green because he emptied the directory by hand — the bare arm was right.**

Lane **G** (`s294-D4` · `s294-D5` · `s294-D6` · `s294-D7`): `provisional` came off
`GRADE_AGING_DAYS = 30` and `POPULATION_DELTA_THRESHOLD = 5` — **both literals byte-unchanged,
asserted** — and off **the two grade strings the module PRINTS beside the number**, with the two
earlier annotations **quoted and marked spent, not deleted**; a token-expiry check in
`_git_commit.sh` with `APOLLO_FAKE_TODAY=2026-11-07` driving the boundary **both ways**; the B3
grader re-pointed at the **repo hook mirror** (23 files, one per wrap since #271 — **14 fresh · 3
stale · 6 unprovable**), after the literal index-line matcher was found to read **8 of 23** and the
widened one **23 of 23**, and after probing store filenames had graded **19 of 23 falsely STALE**;
and ⛔★★ **`s294-D7` re-measured the boot term: `MEMORY.md` MEASURES ZERO** — it has no path on disk,
`git ls-files` reads 0, it was the Cowork LOCAL index retired by the #278 move — with a **5,413-tape
`<user_memory>` block booting in its place** against the **8,470** the record attributed to the file.
`systemPrompt` 26,984 tape · attachments 6,719 · residual ≈40,953 by subtraction.

**4 — THE TWELFTH RULING WAS HIS OWN HANDS, AND `s294-D12` READS CLEAN AT ITS FIRST WRAP.** Lane B
measured **108 top-level entries, 106 older than 3 sessions**, oldest
`outputs_wrap110_part1.py.1786015959` at session **#109 — 185 sessions** — and **no seat could clear
it** (`s282-D4`: this mount refuses `unlink`). He asked, was answered, and emptied it from Finder.
Measured here by the arm itself, **exit 0**:

> `· `_to_delete/`: 1 top-level entry, none older than 3 sessions (current #294) — `s294-D12` clean.`

⚠ **The one entry is `.DS_Store` (16,388 B, mtime 18:34 today) — Finder's own artefact of the
emptying, inside the N = 3 window BY CONSTRUCTION and not a survivor.** Said out loud so nobody reads
`1 entry` as `1 left over`.

**5 — THE NINETY-THREE-SESSION REFUSAL IS OVER, AND HOW IT ENDED IS THE PART TO KEEP.** The
generated line was produced **and then graded with the gate's own `check_preflight`, which returned
ZERO FAILS, before it was pasted.** ⇒ **the measured CAPTURED line IS #294's pre-flight stamp** —
the first in ninety-four sessions.
★★ **AND THE THING THAT NEARLY MADE IT A FOURTH REFUSAL, WHICH #295 SHOULD READ ONCE:**
`check_preflight_tokens`, called **directly**, returns **2 fails** on that same text (*silent on job,
wrap* · *no total against a budget*). The arms do not conflict — `check_preflight`
(`_capture_gate.py:1473`) owns the **branch selection** and delegates to the tokens arm only on the
priced shape (`:1549`), and **only `check_preflight` is wired into wrap mode** (`:6339`).
⇒ ⛔ **A GATE ARM CALLED OUTSIDE ITS OWN SELECTOR IS NOT THE GATE.** Grading with the inner arm would
have produced a fourth consecutive refusal on a cause that was, once again, false.
⚠ **The half that did NOT close, published rather than smoothed:** the measured line is the **SPEND**,
not the **PRICE**. **#294 was never priced at its opener, so the dataset has no ESTIMATE for it and
none was invented** [[feedback-measuring-tool-must-not-guess]]. **The hole moved; it did not shut.**

**6 — THE `size:` STAMP IS GENERATED FOR THE FIRST TIME.** `s294-D8`, lane A's generator, first live
use at this wrap: `WROTE — GM 37,088 · LS 69,650 · corpus 106,738 tape (cl100k tier) · fixed point in
3 pass(es)`, `--check` then **0.00% drift on all three**; after the stratum's own lines landed it
reads **GM 1.19% · LS 0.00% · corpus 0.42%** against a 10% tolerance — **declared, not re-taken**
(#241). ⛔ **No figure on that line was typed by this seat.** §A is **NOT** generated (a different
instrument) and **neither is the session ordinal** (a label, not a measurement).

---

## ⛔ EIGHT LANES — ALL EIGHT VERIFIED, A FIRST SINCE #291

R · R2 · C · I · A · B · G · C2. ✅ **Verified at this seat rather than taken on trust:** every one
carries `"model":"opus"`, `"spawnDepth":1` and `"agentType":"general-purpose"` in its own
`.meta.json`, and so does this wrap sub's. **A transcript exists for every lane that ran** — the
first session since #291 where that is true.

⛔ **`s204-D1` was obeyed: nothing ran in seat except the conductor's own memory reads.**

---

## ★★★ HE STOPPED ON THE INSTRUMENT — AND THE n DOES NOT MOVE

At **FILL 182,609 real / 24 turns** the seam reading was quoted to him **past the 180,000 quality
line**. He answered the `_to_delete/` question and then, verbatim and entire: ***"done, you can
wrap"***.

⇒ **#283, #290, #293 and #294 stopped ON the instrument. #291 and #292 each overrode it exactly once
with a note and then obeyed.** ⛔ **`s283-D1`'s override shape STAYS AT n=2. This session adds nothing
to it and no rule is invented from it here.**

---

## ⚙ THE NUMBERS, MEASURED AT THIS SEAT

**The conductor's window** — `_checkin.read_fill` **imported rather than re-implemented**, against
`/sessions/great-dreamy-noether/mnt/.claude/projects/session/501c6cd1-77ce-4c94-adf4-7d0edfcee9fa.jsonl`,
the session's TOP-LEVEL transcript (the eight lanes and this sub being `subagents/agent-*.jsonl`
beneath it):

> **FILL 192,062 real / 28 continuous turns of 29 · peak 192,062 · boot 74,656 · 0 compaction
> records · 0 drops**

✅ **EVERY ONE of the conductor's five declared seam readings reproduces EXACTLY at this seat — zero
difference on all five:** `143,951` · `163,311` · `168,205` · `173,377` · `182,609`.

> **wrap-handover: brief-cut 182,609 · sub-cut 192,062 · delta 9,453 · replay unobservable
> (conductor-side, post-wrap)**

✅ The subtraction is **legal**: both terms read the same window, and the five agreements to the token
are the identification. ⚠ **9,453 is the LARGEST hand-over on the `s214-D5` record** (#293: 1,871 ·
#292: 954), and the cause is nameable — **four conductor turns fall between the brief's cut and this
reading.** ⛔ **`s214-D4`'s re-derivation of the 49,071 wrap term is staged on n≥2; it has it, and
re-basing a constant is Dave's.**

⛔ **12,062 past the 180,000 quality line — DECLARED. 200,000 clear by 7,938 · 220,000 by 27,938 ·
256,000 by 63,938. No literal was moved.**

**`subs`** — **MEASURED 1,396,532 (n=8)** against **1,404,885 DECLARED**, and **both are published**:
R **129,210 / 130,117** · R2 **115,754 / 116,472** · C **85,327 / 85,871** · I **188,891 / 190,158** ·
A **181,735 / 182,914** · B **203,815 / 204,690** · G **259,604 / 260,893** · C2 **232,196 /
233,770**. ⛔ **The declaration reads HIGH on all eight and LOW on none — a systematic +8,353 (0.59%),
not noise**, and the shape says the conductor's figures were taken one turn later than `read_fill`'s
newest `usage` record. **Neither reading is corrected into the other.** ⚠ This wrap sub's own window
is excluded, as #292's and #293's were.

**Boot 74,656** is over `BOOT_CEILING_TK` 70,000 by **4,656** — **SHRINK-ONLY, cut the boot, never
raise the literal** — and **452 above #293's 74,204**. It reconciles exactly with lane G's
independent reading.

---

## ⛔ THE GATES — SEVEN IN, SEVEN OUT, ALL INHERITED

Baseline measured before any write: **231 in scope · 7 fail · 367 warn.** Every one is another
session's append-only testimony in `notes/_GAUGE-LOG.md`: the **boot-drift CEILING BREACH** and **six
boot double-counts** (#243 ×5, #264, #272, #273, #274, #287). ⛔ **A wrap may not repair an inherited
gate fail** — the `#243` DECLARED NOT-A-WRAP path, **the TWENTIETH consecutive wrap.**

**THREE fails were this ritual's own and all three were closed AT THE THING NAMED, never by
weakening a gate:** the ★ LATEST banner over cap → **shortened**; the retrieval index stale → **2g
run**; the title receipt generated against banner #293 → **`_gen_titles.py` re-run after the banner**.

⚠ **THE WAVE'S OWN EXCURSION IS #294's RECORD, NOT THIS WRAP'S INHERITANCE, AND IT IS PUBLISHED
RATHER THAN NETTED OUT:** lane B took the live gate **8 → 12 by design**; lane C2 brought it to **8**
by narrowing D2 to the staged set and bringing every report it carried into the contract; **his hands
closed the eighth after that commit.**

⛔★★ **AND A DECLARED HOLE, THE SECOND SESSION RUNNING: `python3 knowledge/_capture_gate.py
--selftest` IS UNASKED AT EVERY SEAT THIS SESSION HAD.** Lane B's own run ends on five ❌ lines, **all
inherited** from the `_governs.py` trigger-index arm complaining that `s282-D2`…`s282-D5` evidence
strings carry no pointer — **zero failures from any of the eleven enactments.** This wrap ran the
gate **live** through `_git_commit.sh`, but **a live arm firing is not the selftest passing**, and
**the first surface that will ask it honestly is CI — which is gated behind the unpushed commits.**

★ **AND THE GATE NAMED ITS OWN NEWEST HOLE, IN ITS OWN VOICE, ON THE PASSING PATH:**

> `FILED SUB-REPORT LINES: the check DID NOT RUN — the index is EMPTY … ⛔ UNKNOWN, NEVER GREEN.
> ⚠ knowledge/_git_commit.sh runs _capture_gate.py --wrap at :646 and stages at :896, so from that
> seam the index is empty BY CONSTRUCTION and s294-D2's bite cannot land there.`

---

## ✅ THE COMMIT PATH

⛔ **This ritual did NOT need a step-0 commit** — lanes C and C2 had already committed everything of
the session's own, so the wrap commit carries only the ritual's surfaces plus the two carried J lanes
and the three machine appends (`knowledge/_graph-mark-observations.jsonl`,
`notes/_REHEARSAL-LOG.jsonl`, `notes/_dream/_GRADE-DECISIONS.jsonl`), **named rather than swept in
silently.**

✅ **THE DOC-ROW GATE WAS ANTICIPATED RATHER THAN MET, A THIRD SESSION RUNNING:** every row was
minted through `_state.add()` and `_CHAIN.md` regenerated **before** the first commit attempt — and
**lane I's report names the reason nobody had written down: a doc-row mint is itself what takes
`_gen_chain.py --check` red**, so any lane that mints a row will meet it on its next commit. Rows:
`W-294w` (this wrap) · `W-293j5` · `W-293j6`; **closed: `W-294r`** (his *"inscribe"* met its
condition) **and `W-294i`** (all twelve have a commit behind them).

---

## THE FOUR STRIKES, AND WHAT WAS DELIBERATELY NOT STRUCK

`s183-D1` strike form with an `s188-D2` receipt on each, **BY ADDITION, never a silent deletion**:

1. ✅ **#293's OWED ITEM 1 / #294's carry ①, THE JUDGEMENT-ONLY REVIEW PAGE** — receipt `dd374131`;
   built and answered in the same session.
2. ✅ **#294's carry ②, THE FIVE JUDGEMENT ITEMS THE PAGE MUST CARRY** — all five ruled AND enacted:
   (a) → `s294-D1`/`D2`/`D3`, (b) → `s294-D6`, (c) → `s294-D4`, (d) → `s294-D8`, (e) → `s294-D5`.
   Receipts `ea930e72` + `698d8ddf` + `f81bbdd4`.
3. ✅ **#293's OWED ITEM 5 / #294's carry ⑩ — THE JEV DECISION HALF ONLY** — `s294-D10`, with his
   rider verbatim. ⚠ **The rest of that item stands**: the lane's provenance correction and the
   fourth-provenance-surface lesson are not struck.
4. ✅ **#294's carry ⑫, THE PRE-FLIGHT ARM vs THE GM STAMP** — `s294-D9`, with the gate's own
   zero-fail grading of the generated text as the receipt.

⛔ **NOT STRUCK, AND THE PRECISION IS THE POINT.** **Carry ③** (*"dream pass 13's seven — six
enacted, one carried, and NOT ONE OF THEM RULED"*) took a **BY-ADDITION correction** instead: P3's
strong form IS now ruled as `s294-D8`, **the other six remain enacted-and-unruled, and `s183-D1`
strikes a headline and never a clause.** **Carry ④** likewise: pass 9's P3 is ruled as `s294-D4`, **P4
is still live**, and the pass as a pass still has no disposition ruling. ⚠ *"A strike that is wrong is
worse than an item that is merely stale"* (`s271-D4`).

⛔ **NOTHING ELSE WAS STRUCK.**

---

## ⛔★★ THE BANNER CAP CLOSED AT ZERO HEADROOM FOR THE SECOND CONSECUTIVE WRAP

Drafted at **1,228 tape across ten bullets** and shortened **nine** times, with **two bullets merged
away**, to land at **EXACTLY 1,200 of the `s241-D2` cap of 1,200, over 9 of 10 substantive lines —
ZERO tokens of headroom**, measured by the gate's own arm rather than by eye. (One iteration read
**1,201** to this seat's 1,200: the gate's region includes the trailing blank line before ★ PRIOR.
Worth knowing.)

⇒ **#292's and #293's complaint is not withdrawn and is now literal for a THIRD session: the cap has
stopped constraining girth and started shaping the record.** This wrap could not give the **eight
lanes' ruling-shaped question sets** a bullet of their own — they were merged into `_CARRIES.md` ⑨ ⑩
⑪ — and the `wrap-handover` line was dropped from the banner (it stands in the ⏱ delta and the
stratum). **Reported, not appealed — the cap is Dave's.** [[gate-inside-the-growth-loop]]

---

## ⬛ OWED TO #295, IN ORDER

1. ⛔★★ **THE PUSH, AND A CI READ-BACK JOB BY JOB — AND IT IS NOT ROUTINE.** Six commits are LOCAL:
   `dd374131` · `ea930e72` · `698d8ddf` · `e31aa34b` · `f81bbdd4` and this wrap's. **No CI run
   exists on any of them and none is claimed.** ⚠ **The wave touched `knowledge/_capture_gate.py`,
   `knowledge/_git_commit.sh`, `knowledge/_gen_chain.py`, `knowledge/_build_all.py`,
   `knowledge/_gardener.py` and `knowledge/_gauge_tokens.py`** — the gate, the committer, the chain
   generator, the build spine and two instruments, in one day — and **the capture gate's full
   selftest is UNASKED at every seat this session had.** CI is the first honest asking.
2. ⛔ **THE `s294-D2` ARM HAS NO BLOCKING CONSUMER AT THE COMMIT SEAM, AND THE GATE SAYS SO ITSELF.**
   `_git_commit.sh:646` gates, `:896` stages; from that seam the index is empty **by construction**.
   Moving either changes the script's *"Nothing has been staged"* refusal contract — **lane C2
   declared it Dave's call and this wrap did not touch it either.**
3. ⛔ **STAMP THE TWELVE `status: enacted` WHERE TRUE.** Eleven are enacted in code and **none is
   stamped** — no lane stamps a ruling and this seat did not either. **The store understates the day
   by eleven records.** ⚠ `s294-D12` is a third state again: **enacted in code AND discharged in
   fact, by his hands**, which the field has no word for.
4. ⛔ **LANE B's PREMISE CORRECTION ON `s294-D2` IS UN-AMENDED BY DESIGN.** The inscribed text says
   the COUNTS line parses in **173 of 363**; re-measured with the gate's **own**
   `SUBREPORT_COUNTS_RE` it is **145 of 375** (REPLAY-THESE 140 · RULING-SHAPED QUESTIONS 172). The
   conclusion holds either way — 39% is further under half than 48% — and **nothing was loosened to
   close the gap.** *"Amending a ratified premise is not a seat's call."* **Amend by addition, or
   carry it?**
5. ⛔ **`P-293-1` IS ANSWERED BY `s294-D7` AND THERE IS NO WRITE API TO CLOSE IT.**
   `knowledge/_parked.json` is hand-authored and `_parked.py` has no closing writer.
6. ⛔ **THE FIVE DIFFS LANE G WROTE OUT AND DOES NOT OWN** — `knowledge/_checkin.py` (the
   `--preflight-line` priced half, its three estimate terms supplied as ARGUMENTS by the opener and
   **never guessed**), `knowledge/_boot_remeasure.py` and `knowledge/_memory_cap_check.py` (both
   still reading the retired `MEMORY.md` path). **Declared hand-offs with the exact edit written out,
   not unproven claims.**
7. ⛔ **`s294-D5`'s RE-ISSUE OF THE PUSH TOKEN BEFORE 2026-11-06 IS DAVE'S HANDS.** The gate can
   prove the DATE and **can never prove the SCOPE**, and **#133 said on the record that the token in
   force is BROADER than the ruled shape.** Lane G declares its `--push` arm UNPROVEN end to end: no
   credential in the sandbox's remote URL, so the push path refuses at the credential gate and never
   reaches the expiry gate.
8. ⛔ **`dashboard/index.html` IS STALE AND WAS ALREADY STALE** — whoever regenerates it owns the
   unrelated store deltas it will pull in.
9. ⛔ **BOOT 74,656 AGAINST A SHRINK-ONLY CEILING OF 70,000, WITH SEVEN POST-DIET READINGS ALREADY
   OVER IT.** `s294-D7` settled that we may KNOW; **it did not settle what to do with the knowing**,
   and the memory term's fall of 3,057 does not close a 4,656 gap. ⛔ **That arithmetic is his.**
10. ⛔ **A LOCK-WATCHER RAN AND WAS KILLED — A NEAR-MISS, ON THE RECORD RATHER THAN OFF IT.** Lane C2
    started a background watcher over `.git/index.lock` during the wave's commit and killed it. On a
    mount that refuses `unlink`, a surviving watcher is exactly the class `s282-D4` and 4c exist for.
    ⚠ **Sibling, still open: lane C's own question — whether `notes/_lanes/_orphan-locks/` needs a
    purge ritual of its own on a mount that refuses unlink.**
11. ⬛ **THE EIGHT LANES' RULING-SHAPED QUESTION SETS — 5 (A) + 4 (B) + 6 (G) + 6 (I) + 3 (R) + 2 (C)
    + C2's, plus J5's 2 and J6's 6.** Bodies in the filed reports, headlines at `_CARRIES.md` §
    `residual → #295` ⑨ ⑩ ⑪ ⑬. ★ **Two are worth reading first:** lane A's *"which tier the front
    page's headline figure should be — `real` or `tape`"* (the `size:` line currently says both, and
    `#82-D1` and `ds-021` each name a different one), and lane R's proposed rule — **when a lane
    enacts the WEAK form of a proposal, the STRONG form should be FILED AS A QUESTION PUT in the same
    commit**, because four of the ten items on this page were exactly that residue.
12. ⬛ **EVERYTHING #292 AND #293 OWED THAT #294 DID NOT TOUCH**, each at its true age in
    `_CARRIES.md` § `residual → #295`: **his v12/v13 notes** (the read is DONE, the output owed, the
    four D2 flags and the dark robots slide gated on them) · **the deck's brain still at PASS SIX**
    (v13 inlines `YAW0 10` / `PIT0 8` / `AOV 12` against the drawing file's `−50 / 20 / 33`) · **the
    workers' finessing — *"we'll work on that together"*, NOT a lane brief** · **the dashboard
    review, and WHICH cold brief** (unnamed to the record; the frozen demo prompt was declared NOT
    RECOVERABLE at #288) · **lane B3's plate question** · **the two hidden-line flaws lane B2 named**
    · **lane C's runner question** (wire `_drive_chart_engine.py --check` — blocking or advisory?) ·
    **lane H's two** · **lane D's nine** (chief among them whether **DP-01…29** become the standard)
    · **the deck's counter** (thirteen cards, twelve reading `NN / 12`, one `10A / 12`).
    ⛔ **FRIDAY THE 25TH IS NOW FOUR DAYS OUT, AND IT IS THE INTERNAL.**
13. ⬛ **THE OTHER WORKER'S J LANES.** J5 and J6 are **carried here with doc rows**; **J7
    (`notes/_lanes/293/J7-IDEA-jev-selects-over-the-kg.md`, 18:25, 7,035 B) and
    `notes/_lanes/293/IDEA-notice-board-for-seats.md` (18:51, 3,172 B) are LEFT UNCOMMITTED BY
    DECLARATION** — no filed report for either, and **in-flight is not a stray (#70).** ⚠ **Their
    substance is UNRECONCILED at this seat and their questions are unread here** — #295 should read
    them where they stand. ★ **J6 carries the sharpest single finding of either: the *"declared
    70–500 ms latency band"* DOES NOT APPEAR ANYWHERE in TypeSafe's current documentation**, searched
    across all 59 pages it fetched — and it is the figure **J2, J3, J4 and J5 all measure against**.
    **Recorded as unverified, not as wrong; the sentence wants re-sourcing before it is shown to
    anyone.**
14. ⬛ **THE MEMORY HOOK IS OWED TO THE CONDUCTOR** —
    `notes/_lanes/294/WRAP-MEMORY-HOOK.md`, written in #293's shape, **with one extra beat named in
    it: `areas/jev-integration.md` does not carry his #294 rider, and that is the only durable line
    this session produced which the store does not already hold.**

---

## ⚠ THINGS A COLD SEAT SHOULD KNOW BEFORE IT TOUCHES ANYTHING

- ⛔★★ **THE BOOT NOTE ABOUT LOCKS IS NOW HALF WRONG, AND THE CORRECTION IS BY ADDITION.** It said:
  *"a no-op `git add` on this mount strands a lock, so `git reset -q` before staging."* At this seat
  **`git status` ALONE stranded a 0-byte `.git/index.lock`** (found eighteen minutes later, `mv`'d
  on-device into `notes/_lanes/_orphan-locks/stale-index.lock-294-wrap-1853`, **never `rm`'d** —
  the **fifth** such file that directory took today) — **and then `git reset -q`, the prescribed
  remedy, STRANDED THREE MORE IN ONE CALL: `.git/HEAD.lock`, `.git/refs/heads/master.lock` AND
  `.git/index.lock`.** ⇒ **The remedy strands more locks than the thing it remedies, and it strands
  them on REFS as well as on the index.** Expect to find refs locks, not just an index lock.
- ⛔★★ **A GATE ARM CALLED OUTSIDE ITS OWN SELECTOR IS NOT THE GATE.** `check_preflight` owns the
  branch selection and is the only arm wired into wrap mode; `check_preflight_tokens` returns 2 fails
  on a text the gate passes. **Grade with the arm the gate actually calls, or you will write a
  refusal about the wrong object** — which is exactly how a false cause ran for ninety-three sessions.
- ⛔ **`_state.add()` REFUSES WITHOUT `closes_when`** — and correctly; *"inventing a close condition
  for Dave's open work is the same overreach as inventing his ruling."* 14 legacy items carry none
  and the frozen set may only shrink.
- ⛔ **THE `s294-D2` GATE READS THE **STAGED** SET ONLY.** A report filed but not staged is counted
  and NAMED, never graded — *"a gate must not charge a session for another seat's in-flight file."*
- ⛔ **MINT THE DOC ROWS AND REGENERATE `_CHAIN.md` *BEFORE* THE FIRST COMMIT ATTEMPT.** Three
  sessions running. ★ **And the reason nobody had written down: the doc-row MINT is itself what takes
  `_gen_chain.py --check` red.**
- ⛔ **`_git_commit.sh`: a MID-SESSION commit needs `SESSION_N=<n>`** (`s130-D3`), plus **a FRESH
  msgfile with a FRESH name and NO `after #N …` prefix on line 1** — the script adds it.
- ⛔ **THE `_gm_move.py` OPS FILE IS A MSGFILE: unique name, session-owned path, `os.path.exists` +
  a size floor asserted IN THE WRITING PROCESS.** Six ops files were used at this wrap, each named
  for its step and its minute.
- ⛔ **`> **COMMIT STATE` IS NOT A UNIQUE ANCHOR** — `grep -c` reads **4** in `GOOD-MORNING.md` (the
  rolling stratum plus the three EXEMPT #40/#41/#42 blocks). **Drive `roll_2f` with
  `> **COMMIT STATE #<N>:**`.** Paid again here.
- **`pip install tiktoken --break-system-packages` FIRST** in a cold sandbox, and **again after**
  `_gate_scratch_hygiene.py --clean --wrap`, which removes `~/.local/lib`. **Fifth consecutive wrap
  to meet it.**
- **The sub-transcripts are readable from a sandbox seat** at
  `/sessions/*/mnt/.claude/projects/session/<id>.jsonl` and `…/<id>/subagents/agent-*.jsonl`, each
  with a `.meta.json` naming model, `spawnDepth` and description. ★ **And the CONDUCTOR'S top-level
  transcript is readable from a sub seat too** — `python3 knowledge/_checkin.py --preflight-line <N>`.
- ⛔ **`_gate_scratch_hygiene.py` REPORTS 12M OWNED BY DEAD SESSIONS AND CAN NEVER REMOVE IT.** 4c's
  own contract says the wrap is the last moment cleanup is possible; that figure is the part that was
  already too late. Reported for drift visibility, **not action**.
- ⚠ **THE EIGHTH CONSECUTIVE `consult-receipts: none`, AND IT IS A READING ABOUT THE INSTRUMENT.**
  Every artefact eight successive rituals needed was named BY PATH in the brief, so there was no
  query to put. ⇒ **a retrieval surface a fully-briefed seat never needs has exactly one consumer,
  the cold opener, and nothing measures whether it is used there.**
  [[instrument-without-a-consumer]]
- ⚠ **THE `size:` STAMP IS GENERATED NOW AND YOU MAY NOT HAND-TYPE ITS FIGURES** (`s294-D8`). Run
  `python3 knowledge/_gen_size_stamp.py --write` at step 2, DECLARE-LAST, and read its stdout into
  the residual; **if it REFUSES (exit 77 `COULD-NOT-ASK`, or an oscillation refusal) publish the
  refusal and do not type the figure it would not write.** It did not refuse here.
- ⚠ **§A HAS NOT MOVED IN TWENTY-SIX SESSIONS** (`sha256 4311cce4…`, 198 lines), and the two-probe-
  shape question #272 raised — `b9aef5f5…`/169 lines against `4311cce4…`/198 — **is at its
  twenty-sixth session and is still Dave's.**

---

*Filed report: `notes/_subreports/2026-09-21-294-W-wrap.md`. Dossier:
`_DECISION-HISTORY/2026-09-21-294-the-twelve-were-ruled-inscribed-and-enacted-in-one-day.md`.
Memory hook: `notes/_lanes/294/WRAP-MEMORY-HOOK.md`. His words:
`notes/_lanes/294/DAVE-RULINGS-2026-09-21.md`. Lane reports:
`notes/_subreports/2026-09-21-294-R-review-page.md` ·
`notes/_subreports/2026-09-21-294-C-commit.md` ·
`notes/_subreports/2026-09-21-294-I-inscribe.md` ·
`notes/_subreports/2026-09-21-294-A-enact-d1-d8.md` ·
`notes/_subreports/2026-09-21-294-B-enact-d2-d3-d9-d12.md` ·
`notes/_subreports/2026-09-21-294-G-enact-d4-d5-d6-d7.md` ·
`notes/_subreports/2026-09-21-294-C2-commit.md`. Another worker's, carried:
`notes/_subreports/2026-09-21-293-J5-recall-proposal.md` ·
`notes/_subreports/2026-09-21-293-J6-typesafe-docs.md`.*
