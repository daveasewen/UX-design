# HANDOFF #123 — #272 → #273 — THE CUT AND THE INSCRIPTION

> ⚠ **This file is NEWER than `_CHAIN.md` and OUTRANKS it.** Written by the #272 conductor (Fable) at
> FILL ≈175,000 real — under the 180,000 working line — BEFORE the delegated ritual. Every claim is a receipt.

## ⛔ READ FIRST, IN THIS ORDER
1. This file. 2. `_CHAIN.md`. 3. `python3 knowledge/_parked.py --check` (P-272-1..4 are the sidequests he asked to return to).
★ Stale-mount check: `git log --oneline -3` vs `ls -la --time-style=full-iso _CHAIN.md`.

## ⛔⛔ DAVE'S — VERBATIM. Source: `notes/_lanes/272/DAVE-RULINGS-2026-09-15.md` (every sentence, in order)
- Opener (session opened 2026-09-14 evening, his answers 2026-09-15 — a DATE SPLIT, the #241 shape; nothing re-dated): *"1. we work until around 180, thats the safest but I wont loose any sleep over hitting 220 for example"* → **`s272-D93`** INSCRIBED (180,000 working/soft · ~220,000 tolerated · 256,000 hard). ⛔ **Code constants UNCHANGED** — `STOP_LINE_TK` stays 180,000; whether the ~220 tolerance becomes an enacted arm (and where) is ONE question for #273, not ruled. *"2. I've done a lot, I'm going through the last 31 now"* → sheet in progress.
- The export, pasted whole into chat → saved verbatim `notes/_lanes/272/harvest-decisions-2026-09-14.json` (115/115: 104 ratify · 3 option · 8 decline). His 19 non-empty notes verbatim in DAVE-RULINGS.
- On the cut page: *"were good, inscribe, lets make sure we return yo any of the open questions and sidequsts"* → **`s272-D1..D92`** INSCRIBED, incl. the 79 accepted-by-click (his word overrides the cutter's NOTE-ABSENT refusal; each `says` names the click, never a quote) · 15 unanimous `when`s onto metas · P-272-1..4 parked.
- *"Wrap"* → this ritual, delegated.

## ⛔ NOT RULED — DO NOT RULE
The ~220 tolerance ENACT (arm + blocking tier) · the 8 declines (B-10, B-20, B-22, R-A-09, R-A-10, R1, R11, R15 — sidequests, NOT rulings) · P-272-1..4 shapes (list-vs-card definition, modal taxonomy, nav strategy, the 10 smaller) · s271-D2 / s271-D4 blocking tier · the 3 explorer hues · roles.json drift 108 vs 24 · door v2 (handoff-121 § C) · Common Toolkit specs → `_INGESTION-QUEUE.md` · all handoff-121 § D remainder · every #119 open.

## WHAT #272 DID — RECEIPTS (4 commits, LOCAL; push = his word, check `git rev-list --count @{u}..HEAD`)
1. `42a3f4d` **lane CUT built** — `knowledge/_cut_harvest_rulings.py` (Opus lane, 155,994 sub tokens): export JSON → proposed rulings + when-patch + review page; 12 selftest arms incl. quote-gate mutation; `--inscribe` refuses without `--ratified`. Export schema derived from `exportObj()` and CONFIRMED by driving the real page in chromium; row family is by `data-sec` NOT id prefix (lane-B ids are bare `B-nn`). v2 sheet fixed by addition: a note typed <400 ms before reload/close was lost → flush on `focusout` + `beforeunload`. Stale `.git/index.lock` (09-14 22:51) removed via `allow_cowork_file_delete`. Lane wrote no REPORT.md (harness refused) — its report is in the #272 transcript.
2. `d1af846` **THE CUT on the real export** — `notes/_lanes/272/cut/real/` (PROPOSED-RULINGS 92 · SIDECAR · WHEN-PATCH 15 · UNDECIDED 8 · QUOTE-GATE · `_REVIEW-cut-rulings-2026-09-15.html`, reviewed on sight at 1280). Cutter change by addition: "ratify" on a contested row = the lane's recommendation becomes the `ruled` text; NOTE-ABSENT `says` = "ratify — NOTE-ABSENT; accepted by click: …".
3. `b46ea90` **INSCRIBED** — `_rulings.json` 469 → 562 (`s272-D1..D93`), every entry via `_inscribe_ruling.py --write` after 92/92 clean dry-runs; `_governs.py --selftest` green. 15 `when`s onto metas by TEXTUAL SPAN (12 inserted, 3 replaced on the charts; 15 ins / 3 del total; parse-and-diff proof that only `when` moved) — ⛔ the FIRST attempt reformatted 15 files (the #179 class), reverted, redone. `_validate_kg.py` OK. `_parked.json` +P-272-1..4 (list-vs-card → `list-items.meta.json`; modal taxonomy → `modals.meta.json`; nav strategy → `navigations.meta.json`; 10 smaller → dream-pass event).
4. Quote gate reads the Memento index → 0/13 found is EXPECTED until step 2g rebuilds it; re-run `python3 knowledge/_cut_harvest_rulings.py notes/_lanes/272/harvest-decisions-2026-09-14.json --out /tmp/x --write` after 2g and expect 13/13.

## #273 — FIRST MOVES
0. Opener: ONE question — enact the ~220 tolerance as a named arm (advisory) or leave `s272-D93` as a ruling only? Then: has he pushed?
1. Door v2 per handoff-121 § C · Common Toolkit specs → `_INGESTION-QUEUE.md` · roles.json drift page (108 vs 24).
2. Sidequests are PARKED, not queued: P-272-1..4 fire at the next dream pass and on their meta's next commit. He asked to return to them — the list-vs-card definition (P-272-1) is the one with the most rows behind it (4).
**Cautions:** `model: opus` every lane, verifier same wave, `_checkin.py` every ~10 turns, screenshot + text diff before presenting, never `json.dump` a meta or `_rulings.json` (textual span only), never `git stash`, never `gen_kg_edges.py`, `tiktoken` first.

## STRUCTURAL REDS AT THE SEAM (wrap heals or declares)
Memento index stale (today's files, the 19 notes) → 2g · boot 81,050 = EIGHTH breach, shrink-only, his · #243/#264 double-counts · MEMORY.md over cap (stub rule) · `_REHEARSAL-LOG.jsonl` / `_GRADE-DECISIONS.jsonl` dirty (instrument-written) · dream pass 12 closed; P-272-4 is dream-pass-triggered.
**Gauge:** boot 81,050 real · FILL 172,124 real at the last check-in (42 turns), ≈175,000 at handoff · subs: cut lane 155,994 (n=1).
