GENERATED — do not hand-edit (knowledge/_validate_polarities.py --write)
generated_at: 2026-09-02T15:18:55Z
content_sha256: ad220ea01e3207c48dd3377ffe6b8b97b5eacff3b21743fc693f50397ae9b04d
---
DEFAULTS DECLARATION (s238-D5) — the `Defaults used:` lines the grill would carry.
RULE D-DECLARE (s238-D5): a polarity's factory default is DECLARED iff it BENT AWAY FROM THE CONSERVATIVE SIDE — not all of them, not on demand.
SENSE: `factory_default` is the GRILL's default (the value used because nothing was asked),
       not s219-D3's shipped-option default — two senses already exist, so the field is named.
SOURCE OF "WHICH SIDE IS CONSERVATIVE": notes/_subreports/assets/2026-09-02-237-T-tensions-schema/open-tensions.json (sha256 e2f984ae9e40c919…)
       carries NO per-row field for it. Its generator's docstring (_derive_open_table.py)
       asserts every factory_default_proposed "takes the conservative side" — prose, not a
       per-row datum. RULE APPLIED: bent = UNKNOWN for every row => nothing can be declared
       as bent => 0 declared; every open row is UNPROVEN until the conservative side is
       carried as data (Dave's).
open polarities: 20 · declared: 0 · UNPROVEN: 20 · no proposal in the source: 0
Defaults used:
  (none — 0 of 20 bent from the conservative side; see UNPROVEN)
UNPROVEN (conservative side not carried as data; the default text stays FLOATED in the source
          and in polarity-status.json, it is NOT declared here):
  pl-01 (tn-01) DEFAULT+CONDITIONAL — ask when: Q4 names a brand mandate that changes a MECHANISM (placement, control behaviour), not just a signal
  pl-02 (tn-02) DEFAULT+DECLARE — ask when: never — Q3 already carries it
  pl-03 (tn-03) DEFAULT+DECLARE — ask when: never — the component contract says which kind of operation it is
  pl-04 (tn-04) DEFAULT+DECLARE — ask when: never — the running skill says which mode this is
  pl-05 (tn-05) DEFAULT+DECLARE — ask when: never — a behaviour rule, homed at #235's behaviour address
  pl-06 (tn-06) DEFAULT+DECLARE — ask when: never — build sequencing
  pl-08 (tn-08) DEFAULT+CONDITIONAL — ask when: the brief or the data names an irreversible or time-critical event AND the screen carries a notification surface
  pl-09 (tn-09) DEFAULT+DECLARE — ask when: never — a refusal default; the artefact either measures progress or fabricates it
  pl-10 (tn-10) DEFAULT+DECLARE — ask when: never — a pattern default
  pl-11 (tn-11) DEFAULT+CONDITIONAL — ask when: Q6 was SKIPPED and the surface handles money, eligibility, health or identity
  pl-12 (tn-12) DEFAULT+DECLARE — ask when: never — checkable on the artefact
  pl-13 (tn-13) DEFAULT+DECLARE — ask when: never — Q3 density proxies the expertise moderator
  pl-14 (tn-14) DEFAULT+DECLARE — ask when: never — a citation rule about what the brain may say
  pl-18 (tn-18) DEFAULT+DECLARE — ask when: never — reported through the inferential half of the rubric (s234-D6)
  pl-21 (tn-21) DEFAULT+CONDITIONAL — ask when: Q4 names a chart style that lowers the encoding rank (donut, 3-D, area for comparison)
  pl-23 (tn-23) DEFAULT+DECLARE — ask when: never — a property of the event
  pl-24 (tn-24) DEFAULT+DECLARE — ask when: never — checkable on the artefact
  pl-26 (tn-26) DEFAULT+DECLARE — ask when: never — a build-time check on the removal
  pl-28 (tn-28) DEFAULT+DECLARE — ask when: never — structure is mechanically checkable
  pl-30 (tn-30) DEFAULT+DECLARE — ask when: never — s237-D4 already routes pointer-only families
