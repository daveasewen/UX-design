{
  "name": "Page-header-lockup",
  "category": "organism",
  "$layer": "2 Lock-up",
  "purpose": "The page-header region: an eyebrow/breadcrumb, the page title, an optional Tabs row and a right-aligned action pair, arranged so the title stays legible and the actions collapse to their own row on a narrow container. Composed ENTIRELY from existing gated/proposed atoms (Breadcrumbs, Eyebrow, Tabs, Button, Badge) — no new visual vocabulary, only arrangement. Itinerary row 115 (Layer 2, class NO-ARTEFACT-CLASS at mint; PROPOSED here, not ruled).",
  "props": [
    { "name": "arrangement", "type": "enum", "values": ["simple", "breadcrumb-tabs", "meta-badge"], "default": "simple",
      "$note": "Three arrangement variants drawn in the reference: A simple (eyebrow+title+actions), B breadcrumb+title+tabs, C eyebrow+title+badge+meta-line+actions. Structural variance, not a colour/size parameter — this is a VARIANTS-axis prop, following the same class-C6 reading Tags' own `type` prop carries (s142-D1)." },
    { "name": "actions", "type": "slot", "values": ["none", "one", "two"], "default": "two",
      "$note": "0-2 Button instances (any tier). The organism does not draw a new button style." },
    { "name": "showBreadcrumb", "type": "boolean", "default": "false" },
    { "name": "showTabs", "type": "boolean", "default": "false" },
    { "name": "showBadge", "type": "boolean", "default": "false" }
  ],
  "variants": [
    { "name": "simple", "use": "Eyebrow + title + a right-aligned action pair. The default — most page headers need nothing more." },
    { "name": "breadcrumb-tabs", "use": "A drill-down page (e.g. an account detail) that also switches between sub-views via Tabs." },
    { "name": "meta-badge", "use": "A page whose title carries a live count (e.g. unread messages) and a short meta line (e.g. a timestamp)." }
  ],
  "tokens": {
    "title": "borrowed from canon/type.css .t-cm-heading composite (32px/400) — never a raw declaration",
    "eyebrow": "text/secondary via Eyebrow.reference.html, unmodified",
    "breadcrumb": "text/default + the '/' or chevron separator via Breadcrumbs.reference.html, unmodified",
    "tabs": "the full tabs/* token set via Tabs.reference.html, unmodified, including its own responsive overflow",
    "actions": "button/{primary,secondary,tertiary,quaternary}/* via Button.reference.html, unmodified",
    "badge": "badge/background + rag/text/on-light via Badge.reference.html's standalone form",
    "$note": "Every token above resolves through the ATOM's own manifest; this organism binds nothing new. The lock-up's own CSS (`.ph-*`) is flex arrangement only — no colour, no type-size, no radius declaration of its own."
  },
  "responsive": {
    "$status": "PROPOSED at #210 wave-5 (not ruled) — reflow verified in a real browser at 3 widths.",
    "rule": "Wide: title left, actions right, one row. Container <=560px (container-query, WCAG 1.4.10 reachable to 320px): the action pair falls to its own full-width row beneath the title, right-aligned. Tabs (arrangement B) collapses independently via ITS OWN unmodified ResizeObserver logic — this organism never re-implements tab overflow.",
    "$note": "s210-D3 discipline: the lock-up declares no width of its own; the container gives it one."
  },
  "relationships": {
    "livesInside": ["Page (top of content area)", "App shell (below Header/Navigations)"],
    "mustNotNeighbour": [],
    "commonPatterns": ["page title + primary action", "drill-down header with sub-view tabs", "inbox-style header with an unread count"]
  },
  "accessibility": {
    "relatedSC": ["2.4.6 Headings and Labels", "2.4.7 Focus Visible", "1.4.10 Reflow", "4.1.2 Name, Role, Value"],
    "structure": "The title is a single <h1> per page instance (heading order, 1.3.1); Breadcrumbs keeps its own nav landmark + aria-current; Tabs keeps its own role=\"tablist\"/tab/tabpanel triad, all unmodified from the source atom.",
    "focus": "Every borrowed atom keeps its OWN :focus-visible ring — the organism adds no new focusable element and no new focus style."
  },
  "antiPatterns": [
    "Re-drawing the title, breadcrumb, tab or button CSS instead of composing the existing atom — the #202 defect this brief names explicitly",
    "Giving the lock-up a fixed width — s210-D3 reserves that for the container",
    "Putting more than one <h1> on the page by nesting a second lock-up",
    "Re-implementing Tabs' overflow collapse at the organism layer instead of leaving Tabs' own JS untouched"
  ],
  "tokenValidation": {
    "date": "2026-08-20",
    "against": "the four source atoms' own token manifests (Breadcrumbs/Eyebrow/Tabs/Button/Badge), re-declared verbatim by name here except Badge's `--surface`/`--number`/`--host`, renamed `--badge-surface`/`--badge-number`/`--badge-host` to avoid a semantic collision with Button's own `--surface` demo-chrome var (declared in the snippet's own file banner; VALUES unchanged, only the custom-property NAME differs)",
    "result": "PASS — `_validate_snippets.py` reports 0 findings for Page-header-lockup.reference.html; see the #210 wave-5 Lane D receipt for the exact command."
  },
  "provenance": {
    "source": "gap-report",
    "figma_node": "",
    "code_path": "knowledge/snippets/Page-header-lockup.reference.html"
  },
  "edges": {
    "renderedBy": [
      {
        "ref": "snippet:Page-header-lockup.reference.html"
      }
    ],
    "containedBy": [
      {
        "ref": "component:navigations",
        "$note": "App shell (below Header/Navigations)"
      }
    ],
    "usedInContext": [
      {
        "ref": "context:page-top-of-content-area",
        "$note": "Page (top of content area)"
      }
    ],
    "commonPattern": [
      {
        "ref": "pattern:page-title-primary-action"
      },
      {
        "ref": "pattern:drill-down-header-with-sub-view-tabs"
      },
      {
        "ref": "pattern:inbox-style-header-with-an-unread-count"
      }
    ]
  }
}
