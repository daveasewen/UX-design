{
  "$description": "ADR-0013 — the component-type tier: ONE registry, both halves. (1) VALUES: this file is a token store — a path component-type/<group>/<param> resolves here, and each parameter $alias-references its semantic home (the alias hop: component var -> type-group -> semantic role -> default; $value is the gated cache, _STANDARDS.md §1 convention). (2) RULES: the $-keys carry the structural halves — $members (who consumes, with their selector map) and $partials (named rule-blocks injected from a source atom into consumers between AUTO-PARTIAL markers by gen_component_partials.py; sync gated in _build_all). Groups accrete from OBSERVED duplication only, never speculation (ADR-0013 ruling 3). Every member must carry the partial's AUTO-PARTIAL markers, the required vars, and the $manifestBinds manifest bindings — gen_component_partials.py --check fails the build otherwise.",
  "component-type": {
    "button-family": {
      "$description": "Press-physics controls sharing one interaction-motion definition (ADR-0013 ruling 3 first population). B-D7 (Dave, 2026-07-22): the Icon-button SIZE-SCOPED model is the family canon — travel is PIXEL-TRUE, scale(calc(1 ± travel/--phys-size)), darken 0.94 — superseding Button's proportional 1.04/0.95 + 0.85 (rule shape survives: grow toward cursor, press recede + darken, disabled stays put, reduced-motion snaps). Motion is a THEME DIAL: Legacy + Supercharge override travel->0 darken->1 (colour-only state feedback); Mono + Console carry the movement (Console inherits, not fenced — RULED, Dave 2026-07-22: 'Console and mono can share the motion tokens for now'; divergence later = a deliberate override). --phys-size = the member's LOCAL characteristic-size number (geometry, like --h; buttons ~120 wide, icon button 44 — the Tranche-1 size-scoping). Survey ground truth at ruling time: 13/40 snippets carried a local button recipe, 7 carried the scale-press by copy, 4 pressed with translateY(1px). MULTI-CONTROL members register an :is() selector list (wave-1 convention, 2026-07-22) — a bare comma list would leave rewritten pseudo-classes bound to the last selector only; mixed sizes inside one member = a LOCAL --phys-size override on the smaller control (Drawer's .sheet .close 44 vs .dbtn 120 — geometry, not a token).",
      "$members": {
        "Button": {
          "role": "source"
        },
        "Modals": {
          "selector": ".btn"
        },
        "Progress-tracker": {
          "selector": ".nav button"
        },
        "Icon-button": {
          "selector": ".iconbtn"
        },
        "Form-layout": {
          "selector": ".fl-btn"
        },
        "Secure-entry": {
          "selector": ".se-btn"
        },
        "Alert": {
          "selector": ".alert .x"
        },
        "Toast": {
          "selector": ":is(.toast .x, .toast .act)"
        },
        "Banner": {
          "selector": ":is(.banner .x, .banner .actions .abtn)"
        },
        "Drawer": {
          "selector": ":is(.dbtn, .sheet .close)"
        },
        "Popover": {
          "selector": ".pop .x"
        },
        "Modal-lightbox": {
          "selector": ".lb-ctl"
        },
        "Empty-state": {
          "selector": ".ebtn"
        },
        "Data-grid": {
          "selector": ":is(.dg .sort, .dg .fchip .x, .dg .clearbtn, .dg .pbtn)"
        },
        "Date-picker": {
          "selector": ".dp-nav"
        },
        "Date-range-picker": {
          "selector": ".dr-nav"
        },
        "File-upload": {
          "selector": ":is(.fu-browse, .fu-remove)"
        },
        "Stepper": {
          "selector": ".st-nav button"
        }
      },
      "$partials": {
        "press-physics": {
          "source": "Button",
          "rootSelector": ".btn",
          "requires": {
            "vars": [
              "--press-travel",
              "--press-darken",
              "--phys-size",
              "--spring",
              "--press"
            ],
            "matchValues": [
              "--spring",
              "--press"
            ],
            "declarations": [
              "transform var(--spring)"
            ]
          },
          "$manifestBinds": {
            "--press-travel": "component-type/button-family/press-travel",
            "--press-darken": "component-type/button-family/press-darken"
          },
          "$description": "hover grow + press recede/darken + full-width and disabled guards + reduced-motion override — single-sourced from Button's PARTIAL block. requires.vars must be declared by every consumer (--phys-size is local geometry, not a token); matchValues must equal the source atom's declared value (duration/easing composites — already tokens, pinned by match until composite manifest-binding exists); declarations are substrings the consumer's CSS must contain (the control's transition must carry the spring)."
        }
      },
      "press-travel": {
        "$alias": "motion/press/travel",
        "$value": 2,
        "$type": "number",
        "$description": "type-group parameter — px of size change per interaction step (B-D7 pixel-true model). Cache of motion/press/travel; a theme may override the group without touching the semantic role, or the role to move every family at once."
      },
      "press-darken": {
        "$alias": "motion/press/darken",
        "$value": 0.94,
        "$type": "number",
        "$description": "type-group parameter — :active brightness factor. Cache of motion/press/darken."
      }
    },
    "dataviz": {
      "$description": "ADR-0015 (accepted Dave 2026-07-23) — the behaviour-partial group: retrieval reaches JS. ONE hand-authored source (knowledge/canon/dv-behaviour.js: value popover dvTip · responsive fit reflow · table-view popover panel · legend-as-filter) injected into every member between AUTO-BEHAVIOUR markers by gen_component_partials.py, --check sync-gated, byte-exact; the performance contract on the source (≤16KB raw · no setInterval/network/polling · single rAF-debounced resize · delegated events) is gated by _validate_behaviour.py. Members accrete as the chart-revisit wave lands (exemplar first: Chart-line, 2026-07-23). DELIBERATE NON-MEMBERSHIP of button-family: legend/table-toggle controls are quiet utility pressables, NOT B-D7 press physics (chart-revisit Q5, flagged to Dave, standing). $manifestBinds carry the DV-D07 two-channel chrome roles (data/axis + data/grid, colour + alpha slot) — the mint rides the same lane. Contracts fire on registration (ADR-0013 posture).",
      "$members": {
        "Chart-line": {
          "selector": "figure.dv",
          "extraContract": {
            "vars": [
              "--data-axis",
              "--axis-alpha",
              "--data-grid",
              "--grid-alpha"
            ],
            "declarations": [
              "data-fxs=\"",
              "class=\"dv-tbl-toggle",
              "class=\"dv-legrow",
              "class=\"dv-leg-sw",
              "class=\"dv-tablepanel"
            ],
            "manifestBinds": {
              "--data-axis": "data/axis/color",
              "--axis-alpha": "data/axis/alpha",
              "--data-grid": "data/grid/color",
              "--grid-alpha": "data/grid/alpha"
            }
          }
        },
        "Chart-bar": {
          "selector": "figure.dv",
          "extraContract": {
            "vars": [
              "--data-axis",
              "--axis-alpha",
              "--data-grid",
              "--grid-alpha"
            ],
            "declarations": [
              "class=\"dv-tbl-toggle",
              "class=\"dv-legrow",
              "class=\"dv-leg-sw",
              "class=\"dv-tablepanel"
            ],
            "manifestBinds": {
              "--data-axis": "data/axis/color",
              "--axis-alpha": "data/axis/alpha",
              "--data-grid": "data/grid/color",
              "--grid-alpha": "data/grid/alpha"
            }
          }
        },
        "Chart-combo": {
          "selector": "figure.dv",
          "extraContract": {
            "vars": [
              "--data-axis",
              "--axis-alpha",
              "--data-grid",
              "--grid-alpha"
            ],
            "declarations": [
              "data-fxs=\"",
              "class=\"dv-tbl-toggle",
              "class=\"dv-legrow",
              "class=\"dv-leg-sw",
              "class=\"dv-tablepanel"
            ],
            "manifestBinds": {
              "--data-axis": "data/axis/color",
              "--axis-alpha": "data/axis/alpha",
              "--data-grid": "data/grid/color",
              "--grid-alpha": "data/grid/alpha"
            }
          }
        },
        "Chart-donut": {
          "selector": "figure.dv",
          "extraContract": {
            "vars": [
              "--data-axis",
              "--axis-alpha"
            ],
            "declarations": [
              "class=\"dv-tbl-toggle",
              "class=\"dv-tablepanel",
              "class=\"dv-legrow",
              "class=\"dv-leg-sw",
              "data-tip-percent=\""
            ],
            "manifestBinds": {
              "--data-axis": "data/axis/color",
              "--axis-alpha": "data/axis/alpha"
            }
          }
        },
        "Chart-sparkline": {
          "selector": "figure.dv"
        },
        "Chart-scatter": {
          "selector": "figure.dv",
          "consumes": ["dv-behaviour"],
          "extraContract": {
            "declarations": [
              "class=\"dv-tbl-toggle",
              "class=\"dv-tablepanel"
            ]
          }
        }
      },
      "$behaviour": {
        "dv-behaviour": {
          "source": "canon/dv-behaviour.js",
          "requires": {
            "vars": [],
            "matchValues": [],
            "declarations": [
              "data-tip=\""
            ]
          },
          "$manifestBinds": {},
          "$description": "The Layer-2 interaction layer (ADR-0015). NO LONGER SINGLE-SOURCED as of 2026-07-26 — see the sibling dv-legend entry: the deferred cap question recorded at the end of this description was RULED by Dave (split the source AND re-scope the gate to a per-page sum), so this file now carries fit/tip/table/seg/csv only and the legend model lives next door. ORIGINAL NOTE FOLLOWS, kept because the deferral is part of the record: The Layer-2 interaction layer, single-sourced (ADR-0015). PER-CAPABILITY SPLIT ENACTED 2026-07-24 (conductor absorb, chart-revisit wave — the exemplar's pre-authorised 'split from observed need' trigger; the wave landed 4 more members, whose real hook/chrome sets diverge from the line exemplar's). The UNIVERSAL contract now holds only the one hook every member carries (popover data-tip=); everything capability-specific lives in each member's $members[…].extraContract {vars · declarations · manifestBinds}, merged by check_contracts. Observed capability tiers: CARTESIAN CHROME (DV-D07 two-channel axis/grid — vars + manifestBinds) = line/bar/combo carry axis+grid, donut carries axis only (radial, no gridlines), sparkline carries neither (axis-free idiom). POLYLINE FIT (data-fxs=) = line+combo only (bar/donut/sparkline fit by rect/arc/CSS). TABLE+LEGEND (dv-tbl-toggle · data-series-toggle · dv-tablepanel) = all but sparkline (sparkline is compact: popover only). NOTE: the donut SWEEP animation hook is a SEPARATE future addition that would push dv-behaviour.js past the 16KB cap (measured 17,360B) — DEFERRED to Dave: amend the cap vs modularise per family. The existing under-cap module (14,620B) injects into all members as-is; sweep stays baked-static."
        },
        "dv-legend": {
          "source": "canon/dv-legend.js",
          "requires": {
            "vars": [],
            "matchValues": [],
            "declarations": []
          },
          "$manifestBinds": {},
          "$description": "The LEGEND INTERACTION MODEL — DV-D11 (dual gesture, two render levels, additive isolate) + DV-D12 (trapezoidal donut sweep keyed to segment spans) + DV-D13 (typed tooltip + selection-following centre). SECOND behaviour source for the group, added 2026-07-26. WHY A SECOND SOURCE, both beats: the sweep hook was DEFERRED on 2026-07-24 because it pushed dv-behaviour.js past the 16KB cap; the full v5.5 legend model made the overshoot decisive (one file would have landed at 26.0KB raw, 21.8KB even with every comment stripped). Dave ruled 2026-07-26: SPLIT the source AND re-scope the gate — _validate_behaviour.py now checks per-source 16KB (legibility) AND a per-group 32KB PAGE budget, so a future split cannot buy headroom. The single-rAF-debounced-resize check moved to group level in the same beat, for the same reason: it is a page invariant, and a second source legitimately carries zero resize listeners. UNIVERSAL contract hook = class=\"dv-legrow (the dual-gesture row); the swatch/label/reset chrome and the value-percent seg are member-specific and ride each member's extraContract. NOT a member of this contract: Chart-scatter and Chart-sparkline carry no legend. ★ MIGRATION CLOSED 2026-07-27 — and the promotion the older note instructed was TRIED, MEASURED and REVERSED the same hour; both beats are recorded here because the reversal is the more useful fact. DURING the wave it was deliberately EMPTY: no hook was carried by every member, so the legend hooks rode each MIGRATED member's extraContract and the contract tightened per member rather than failing four members for not having arrived yet. All four legend-carrying members (donut · bar · combo · line) have now moved and `_check_legend_migration.py` exits 0. Promoting class=\"dv-legrow to this universal contract FAILED THE BUILD, correctly: Chart-sparkline (and Chart-scatter) are members of the dataviz behaviour GROUP but carry no legend at all, so a hook required of every member is one two members can never satisfy. ⇒ THE UNIVERSAL CONTRACT STAYS EMPTY, for a different and permanent reason than during the wave: the GROUP IS BROADER THAN THE CAPABILITY. The older instruction ('when the last member migrates, promote it here') assumed group == legend-carrying members; it does not. The hook therefore stays on each of the four members' extraContracts, which is where a per-capability hook belongs. The real fix is the already-logged open item — a per-member behaviour OPT-IN in the registry schema (which would also stop Chart-sparkline carrying an inert legend payload); that is a schema change and Dave's call, not a lane's. What stays member-specific: class=\"dv-leg-sw and the value/percent tip hooks (chrome and seg vocabulary differ per member — bar and combo and line carry no value⇄percent seg, so they carry no typed tips). NAMING: the family is .dv-leg* (list .dv-leg, row .dv-legrow, controls .dv-leg-sw / .dv-leg-item / .dv-leg-reset). The v5.5 prototype called the list .lg, which COLLIDES with canon's .seg.lg size variant — the blast-radius gate caught it on first build. State classes .is-ghost/.is-faded/.is-peek keep their names because DV-D11 inscribes them verbatim. ★ RESOLVED 2026-07-28 (ADR-0015 Amendment 2, Dave, TENTATIVE): the per-member OPT-IN this note asked for now exists — a universal-default consumes-manifest on the member object (absent = everything, unchanged; declared = only what is listed; fail-loud both ways, gen_component_partials.py). And a correction, kept as loud as the claim: this note’s aside that Chart-scatter is a group member was DRIFT — $members never listed it and it carries 0 AUTO-BEHAVIOUR markers (measured #26); the promotion trial’s member-side failure was Chart-sparkline alone. First narrow declaration lands with DV-J2 (scatter joins consuming dv-behaviour only)."
        }
      }
    }
  }
}
