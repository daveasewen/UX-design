# Knowledge-base integrity report

> CI gate over the authored canon. **ERROR** = the graph is inconsistent (fix before relying on it); **WARNING** = best-effort / probably fine but worth a look. Regenerate: `python3 knowledge/_build_integrity.py` (exits non-zero on any ERROR).

**Result:** PASS ✅ — 0 errors, 17 warnings. Schema: 32/32 metas valid. Token store: 608 leaf tokens, groups 30.

## Errors (0)

_No errors — the canon is internally consistent. 🎉_

## Warnings (best-effort) (17)

**Hero**
- token path may not resolve: 'blur/surface'

**Input fields**
- token path may not resolve: 'rag/border'

**Modals**
- token path may not resolve: 'overlay/background-blur'

**Notifications**
- token path may not resolve: 'rag/icon'

**Pagination**
- token path may not resolve: 'border/active/default'
- token path may not resolve: 'border/high-contrast/default'

**Quick actions**
- token path may not resolve: 'text/on-dark'
- token path may not resolve: 'icon/on-dark'
- token path may not resolve: 'border/on-light/neutral-2'

**Selection controls**
- token path may not resolve: 'icon/text'

**Slider**
- token path may not resolve: 'border/on-light/neutral-6'

**Status indicator**
- token path may not resolve: 'rag/neutral-tint'

**Tags**
- token path may not resolve: 'border/high-contrast/default'
- token path may not resolve: 'border/high-contrast'
- token path may not resolve: 'text/on-light'
- token path may not resolve: 'text/on-dark'

**View options**
- token path may not resolve: 'icon/on-dark'
