# Component interaction review — sign-off tracker

Durable record of the component-by-component interaction review (gallery frontier).
**Once a component is ✅ signed off, it is locked — we do not reopen it.**

---

## ⬛ PENDING — FULL CONSOLIDATED REVIEW (Apollo Mono baseline) — added 2026-07-19
Dave will run **one big review page** over the whole Mono baseline **when the build-out is "done"** — *"I just
need to get this nailed"* (2026-07-19). **Do NOT drip-review the tranches piecemeal — accumulate everything into
that single pass.** Running list of what it must cover:
- **Tranches T1–T9 as they render NOW (post-tokenisation).** The per-component sign-offs below are dated
  2026-06-29/30 and **predate the Mono ramp + R-D16 + the tokenise sweep** — they do not cover current values.
- **Tokenise deltas to eyeball (2026-07-19):** dark divider lightened + more prominent (`--line2` `#3A3A3A → #808080`);
  focus ring now **blue** (`focus/ring` #305A85/#4587A7), was mono near-black; primary button dark slightly brighter
  near-white (`#F2F2F2 → #FFFFFF`).
- **Open decisions to close in-review:** mono **primary-action** token (mint `action/primary/*` + `icon/on-inverse`,
  or adopt `text/on-inverse`); **success/verified kept MONO** (ink check) vs teal; **blue-vs-mono focus ring**.
- **Also owed:** DataViz sign-off (parked "good enough"); **Tranche-9 first review**; Masthead/Hero/Headers revisits
  already flagged below.
- **★ NEW 2026-07-27 (#7) — TWO FELT CONSEQUENCES OF DV-D17, NEITHER RULED, BOTH NEED DAVE'S EYE LIVE.**
  Surfaces: `showroom/chart-donut.html`, `chart-bar.html`, `chart-combo.html`, `chart-line.html`.
  **(a) An enactment call that is the agent's, not Dave's.** Release-on-add also sets `st.visible[id] = true`,
  so the series you clicked is showing afterwards. The strictly literal reading of DV-D17 bite (i) restores
  `visible[]` **alone** — both avoid the all-on failure, and they diverge only when a series dimmed *before*
  isolating is then the one clicked: literal leaves it dimmed, so the click that ended the mode does nothing
  visible to the thing clicked. **One line either way.**
  **(b) A DV-D13 consequence the ruling never named.** Isolate Housing (`950 / 41%`), then check a second
  series: that click now **releases**, so the donut's centre returns to `2320 / 100%` instead of growing to
  `1250 / 54%`. DV-D13 is intact — the centre still follows the SELECTION; the selection is simply everything
  again. **Judge it live, not from this description.**
  ⚠ **Both are DOM-proven only** (`_verify_dv_legend*.js`, 108/108 + 27/27) — **the render in the licensed cut
  is OWED** and is `GOOD-MORNING` §DO-FIRST item 0(a) for the next window.
- **★ NEW 2026-07-27 — THE WHOLE SHOWROOM CHANGED TYPE, and nobody has looked at it yet (ds-013).** Dave
  reported the chart legend labels rendering too big; the cause was `srcdoc` re-basing every payload's
  relative URL, so **`canon/type.css` 404'd in all 49 panes that link it** and every `.t-cm-*` composite
  and selector binding in it was inert. Fixed + gated in `gen_showroom.py` (build 56/56). ⚠ **Consequence
  to eyeball: 49 showroom panes are now rendering CANON type for the first time.** Measured on the charts
  (labels 16px/400 → 12px/500, keys → 12px/700); everywhere else the delta is unmeasured. Anything that
  looked acceptable at browser defaults may have moved. **This wants a deliberate sweep — it is the
  cheapest moment to catch type regressions across the library, and it is not a chart question.**
- **★ NEW 2026-07-27 (later morning #2) — Chart-bar `cb5` STACKED COLUMN re-geometried + alpha keys
  re-coloured; Dave has not seen it rendered.** Pane: `showroom/chart-bar.html` (also
  `knowledge/snippets/Chart-bar.reference.html` standalone). Both changes are DV-D14/DV-D15 enactments
  and both are **measured green** (`knowledge/_verify_dv_stacked_enactment.py`: 2.00px separation on all
  8 boundaries; keys white at 5.26 / 5.04 / **4.61:1**), but the *look* is his call:
  **(i)** segments now understate by **2.0–2.6%** to buy the gap — accepted at ruling time, never seen;
  **(ii)** the keys flipped ink → white, which changes the chart's whole texture at a glance;
  **(iii)** ⚠ **series-3's 4.61:1 leaves 0.11 of margin over AA** — flagged because it constrains any
  future re-tune of that hue, and he should know the ceiling is that close before he approves the look.
- **Video-player re-themed — ★ FAST FOLLOWER, do NOT hold for the consolidated pass (Dave 2026-07-21
  end-of-session: "fine-ish for now, lets make it a fast follower"):** primary action
  now the B-D1 ladder ink (red = Legacy-only via override); `--muted` bound to `text/secondary` — a **deliberate
  darkening** #767676/#9a9a9a → #1A1A1A/#FFFFFF (Dave: *"resolve to the near black... we can review later"*);
  focus ring → `focus/ring` (was rogue #4d9fff); scrim/controls tokenised (`overlay/version2` / `text/reverse`).
  **Eyeball in-review:** the darkened caption/timestamp weight; **the bigplay ON-SCRIM treatment — ENACTED
  same evening** (Dave: "yes but let me eyeball") — light-mode ink fill measured **1.2:1** vs the video (fail;
  the old red was itself 2.7:1) → play button now joins the on-video control family: reverse fill BOTH modes
  (14–19:1) + #333333 glyph (12.6:1, driftAllow'd), themed accent stays on the scrub. **Applies in ALL themes —
  Legacy's play button is white now too (its scrub stays red) — confirm at eyeball.** Also review the scrub
  played-bar contrast against its track (pre-existing, all themes). Worker-B receipt 2026-07-21 has probe values.
- **✅ Legend isolate/toggle v5.x — SIGNED OFF (Dave 2026-07-26, "good done, love this" on v5.5) — LOCKED.**
  `reviews/LEGEND-ISOLATE-TOGGLE-PROTOTYPE-2026-07-24-v5.5.html` = the reference implementation; v5.1–v5.4
  retained as the decision arc. **INSCRIBED same session: DV-D11** (legend model: dual gesture · two fade
  levels full/ghost-12% · additive isolate · hover live in both modes + 24% peek) · **DV-D12** (trapezoidal
  sweep easing keyed to segment spans) · **DV-D13** (typed tooltip + centre figure follows selection) in
  `_DATAVIZ-DECISIONS.md`; seed fed (81 nodes / 138 edges); build 55/55 GREEN. Enactment = the
  donut+bar+combo wave bakes DV-D11/12/13 into `dv-behaviour.js`. Residual asks riding the wave: the
  aria-both-forms asymmetry (DV-D13 ⚠) + the seed-uncheck edge (DV-D11 open edge).
- **Radius/corner tuner v1+v2 (added 2026-07-26, P4):** `reviews/RADIUS-CORNER-TUNER-2026-07-24-v1/-v2.html` — the ★★ "return SOON" item; owed = tweaks + numbers, then per-theme ruling. *(Served to Dave again #66 — no verdict yet.)*
- **Molecules pack — RULED #66, ENACTS PENDING (added 2026-08-01 #66):** `reviews/MOLECULES-KEYFILTER-LOCKUP-2026-08-01-v2.html` — D1 A2 permanent-strict · D2 sparkline sheds · D3 scatter connects · D4 dv-lockup shape approved (ledger § ★ #66-D6). Not awaiting signoff — awaiting the #67 enact wave; stays here until the four enacts land so the ruled-but-unenacted state has a durable home. ⚠ Doc predates the two-register + export-pair runbook laws it triggered; regenerate per `_RUNBOOK-review-doc.md` only if re-presented.
- **Data-marks-exempt hit-area rule + a11y gate rebuild (added 2026-07-26, P4):** `notes/_briefs/2026-07-25-hit-area-rule-and-gate-proposal.md` — sign-off inscribes the rule + green-lights the markup-driven gate rebuild (44 blocking for controls, marks exempt).
- **⬛ Donut legend ENACTED — awaiting Dave's eye (added 2026-07-26, legend-wave session):**
  `showroom/chart-donut.html` (and `knowledge/snippets/Chart-donut.reference.html`) — the first member
  running the real DV-D11/12/13 model out of `canon/dv-legend.js`. Verified numerically **27/27**
  (`knowledge/_verify_dv_legend.js`, real source against the real snippet, incl. 950/41% and 1250/54%);
  ✅ **Render-verify CLEARED 2026-07-26 (lane ①)** — the standing owed note is discharged: Playwright was
  staged per `_RUNBOOK-render-verify.md` and the donut shot at 1180px + 760px in the licensed HSBC cut
  (font assert passed). Both figures, the legend rows, the Reset and the centre figure all read correctly.
  ⚠ **Two deltas from the signed-off v5.5 need your eye, both forced by gates:** row padding 5px/9px →
  **4px/8px** (DEF-005 4px grid) and `border-radius:2px` → `var(--border-radius-default)` (radius gate).
  Resurrect-verbatim is not gate-exempt — same door the 273d18c~1 stepper came through.
- **⬛ Chart-bar legend ENACTED + ds-010 CLOSED — awaiting Dave's eye (added 2026-07-26, legend-wave lane ①):**
  `showroom/chart-bar.html` — wave member 2, and the first member carrying **TWO legends on one page**
  (cb4 grouped · cb5 stacked). Verified **54/54** (`knowledge/_verify_dv_legend_members.js` — DV-D11
  conformance per legend, plus 8 new cross-talk checks proving one legend cannot move the other's marks
  or write to its live region) and **render-verified at 1180px + 760px in the licensed HSBC cut**.
  ⚠ **ONE DELIBERATE VISUAL DELTA needs your eye:** the shaped swatches are gone — `.sw-circle` /
  `.sw-square` / `.sw-diamond` are now plain 12px squares, as on the donut. Reasoning: bar's marks are
  rects, so no marker shapes exist for the swatches to encode; bar's real non-colour channel is the
  on-chart LETTER key (A/B/C). A diamond swatch promised a diamond mark that was never there.
  **Chart-line's markers genuinely ARE circle/square/diamond** — it keeps shape modifiers in lane ③,
  so the two members will differ on purpose. Say the word and I'll put bar's shapes back.
  ✅ Also visible here: **ds-010 closed** — every figure now renders its true colours (h-bar teal per
  DV-D09, the full R-D9 status ramp red/amber/green/blue), where before all four were series-1 purple.
  ⬛ And the same render surfaced **ds-012**: all six h-bar category labels are clipped at the left
  (worst 16.8px) — a 38px gutter sized against a fallback face, not the real cut. Logged, NOT fixed.
- **Five chart showroom panes (added 2026-07-26, P4):** `showroom/chart-{bar,line,donut,combo,sparkline}.html` — the DataViz sign-off = Dave eyeballs the 5 panes → canon flips provisional-agent→canon (open-014). Same object as the "DataViz sign-off" line above — pane paths pinned here so the eyeball has its list.
- **Eight wave-2 chart panes (added 2026-08-05, #95):** `showroom/chart-{butterfly-h,butterfly-v,histogram,boxplot,bullet,candlestick,pie,stacked-area}.html` — ✅ render-verified #96 all 8 PROVEN (receipt `notes/_receipts/2026-08-05-96-render-verify-wave2.md`; stacked-area's missing `dv-fit-on` overrides fixed #96 then re-verified) — Dave's eyeball still OWED; metas carry the #96 resolution note. Per-lane flags for Dave are listed in `_proforma/_DATAVIZ-DECISIONS.md` § ★ #95 (bullet proportions + grey tints · candlestick dv-011 · histogram static key · stacked-area alpha dial + fit gap · pie sweep data-ri).

Method + template controls (live variant/state spread from meta · light/dark toggle · responsive slider · comment
overlay) per the **Method** section. Pointer in `_LIVE-STATE.md` PLANNED/TARGET; memory `full-review-pending`.

## Method (per component)
1. Review the **HTML snippet** (`snippets/<Name>.reference.html`) live in a browser — check interactivity, responsiveness, visuals. (Source of truth; fully interactive; theme toggle + `@media` + reduced-motion built in.)
2. Dave points at issues → fix the **snippet** (never canon.css directly).
3. Regenerate: `python3 knowledge/canon/gen_canon_components.py` → render → `python3 knowledge/_validate_screen.py --render`.
4. Re-gate the gallery + journey. Green + Dave's eyes → **sign off**.

Issue types we're hunting: **missing interaction decisions** (motion / hover / pressed / focus / open-close), **incomplete / missing** (states, variants, dropped sub-parts), **finesse** (spacing, hierarchy, proportion, colour nuance).

**Review specimens show the component ALIVE + its full variant/state spread (Dave, 2026-07-19).** A review doc that features a component must present it as a *live, working* example and show *every variant and state it carries* — never one frozen instance. The spread is not invented: **read it from the component's meta** (`components/<name>.meta.json` — `props` / `variants` / sizes / documented states), then add theme (light+dark) and motion states (normal / active / reduced-motion). A pick (type size, colour, spacing) can only be judged across the component's real range. A variant that plainly exists but is absent from the meta is itself a finding. Then obey the standard review-doc rule: build clean in `reviews/`, inject the overlay with `_make_review.py`, present the `.REVIEW.html`.

**Template controls — every review carries a light/dark toggle + a responsive-width slider (Dave, 2026-07-19).** The spread shows the variants; the controls let Dave stress the component across theme and viewport. Durable target: bake both into `_review/_review-overlay.html` so every generated review inherits them (tracked in `_FUTURE-STATE`); until then add them to each doc's chrome by hand.

Legend: ⬜ pending · 🔧 in fix · ✅ signed off (locked)

---

## Tranche 1 — Buttons & actions (interactive triggers)
| Component | Status | Signed | Notes |
|---|---|---|---|
| Button | ✅ | 2026-06-29 | Good — promoted (incl. leading-trim) |
| Links | ✅ | 2026-06-29 | Signed off — active = label hover/pressed (underline). Filled-active icon DEFERRED (glyph missing) → _ICON-GAPS.md |
| Tags | ✅ | 2026-06-29 | Good — promoted |
| Quick-actions | ✅ | 2026-06-29 | Good — promoted (Button scale-physics motion added) |
| Badge | ✅ | 2026-06-29 | Good — promoted |
| Status-indicator | ✅ | 2026-06-29 | Good — promoted |

## Tranche 2 — Inputs & selection
| Component | Status | Signed | Notes |
|---|---|---|---|
| Input-fields | ✅ | 2026-06-29 | Good — promoted |
| Search-field | ✅ | 2026-06-29 | Promoted — Boxed + Underline; clear only when value present; native cross suppressed; icon centred |
| Selection-controls | ✅ | 2026-06-29 | Good — promoted |
| Slider | ✅ | 2026-06-29 | Good — promoted |
| View-options | ✅ | 2026-06-29 | Promoted — sliding active indicator; single container border |
| Dropdown | ✅ | 2026-06-29 | Promoted — Underline + Boxed (full-border) variants |

## Tranche 3 — Containers & data display
| Component | Status | Signed | Notes |
|---|---|---|---|
| Cards | ✅ | 2026-06-29 | Promoted — undecorated ghost actions at standard button size (h44/pad20) |
| Account-card | ⏸ defer | | DEFERRED (Dave) — needs more work, not a priority; revisit later |
| Table | ✅ | 2026-06-29 | Promoted — card-collapse left-aligns all data (label + value) |
| List-items | ✅ | 2026-06-29 | Promoted — two-line aligned row (title↔status, desc↔amount); density scales both sides; descenders safe |
| Divider | ✅ | 2026-06-29 | Promoted |
| Accordion | ✅ | 2026-06-29 | Good — promoted |

## Tranche 4 — Navigation & wayfinding
| Component | Status | Signed | Notes |
|---|---|---|---|
| Tabs | ✅ | 2026-06-29 | Good — promoted |
| Navigations | ✅ | 2026-06-29 | Promoted (fine for now; Dave to develop further later) |
| Breadcrumbs | ✅ | 2026-06-29 | Promoted — slash + library-chevron separator versions |
| Pagination | ✅ | 2026-06-29 | Promoted — Button press states (inverting fill + depress) added |
| Headers | ⏸ revisit | 2026-06-29 | Subtitle fixed (regular); DEFERRED — marked for revisit (full header set) |
| Reorder | ✅ | 2026-06-29 | Promoted — pointer drag works (+ keyboard moves). FUTURE: "portfolio-level interactions" pass invited by Dave |

## Tranche 5 — Feedback & overlays
| Component | Status | Signed | Notes |
|---|---|---|---|
| Notifications | ✅ | 2026-06-29 | Promoted — close × first-line aligned (22px) across all; --text-shift 7px optical centre; Global 3 variants (none/close/actions) + flush bottom-right actions; title/desc stack 8px; multi-link stack 10px |
| Modals | ✅ dialog | 2026-06-29 | Promoted AS DIALOG; GAP noted: add true modals + lightboxes (desktop+mobile) from Figma → _COMPONENT-GAPS.md |
| Tooltip | ✅ | 2026-06-29 | Promoted — icon-at-end-of-label canon rule + space-aware positioning |
| Progress-tracker | ✅ | 2026-06-29 | Promoted — reworked to Figma (inline label + continuous red fill) |
| Loading-indicator | ✅ | 2026-06-29 | Promoted (fine for now; Dave to tinker later) |
| Countdown-timer | ✅ | 2026-06-29 | Promoted — butt caps; number centred; animates only final 5s (no pause needed); subtitle regular |

## Tranche 6 — Identity & media
| Component | Status | Signed | Notes |
|---|---|---|---|
| Avatar | ✅ | 2026-06-29 | Good — promoted |
| Hero | ⏸ revisit | 2026-06-29 | Promoted (fine for now); Dave to develop further — marked for revisit |
| Video-player | ✅ | 2026-06-29 | Promoted — square play button + Button scale-physics |

---

## Queued after review — 5 gap-patterns → gated components
Follow the **account-card** template. Components: **summary, tab-bar, action-bar, eyebrow, confirmation/success**.

| Gap-pattern | Status | Signed | Notes |
|---|---|---|---|
| summary | ✅ | 2026-06-30 | Promoted — dl key/value + emphasised total row |
| tab-bar (bottom nav) | ✅ | 2026-06-30 | Promoted — A standard labelled bar + B segmented sliding-pill islands (interactive; View-options easing; full-width; inverting black/white selected; Menu in the exclusive group; Insights added). **ISLANDS marked for REVISIT (Dave)** |
| action-bar | ✅ | 2026-06-30 | Promoted — Back(tertiary,left)+Cancel+Confirm; reflow stacks primary-top/Back-bottom; reuses Button |
| eyebrow | ✅ | 2026-06-30 | Promoted — sentence case (house type rule); leading-trim kicker |
| confirmation/success | ✅ | 2026-06-30 | Promoted (finesse later) — success-solid pop + staggered rise; mobile vertical-centre; desktop variant logged in _COMPONENT-GAPS.md |

---

## Awaiting Dave — running list (dream-pass v2 P4, ruled 2026-07-26)

| Artefact | Raised | State |
|---|---|---|
| `reviews/DS-018-DISABLED-STATE-2026-07-27-v1.REVIEW.html` | 2026-07-27 #11 | **A2·B2·C2 RULED same session.** ⛔ **The RECESSIVE VALUE remains UNRULED** — the doc's four candidates (`#9D9D9D` canon today · `#808080` B-D4 · `#B5B5B5` · `#6B6B6B`) were offered as candidates, **never as a recommendation**, per derivation governance. Light/dark pairing also unruled. **Enactment must put the value to him.** ⚠ Collides with the flagged drift: B-D4 ruled `#808080`, canon ships `#9D9D9D` (`canon.css:351`). |
| `notes/2026-07-31-what-memento-is-onepager-v1.md` | 2026-07-31 #63 | **Approved in draft same session** (Dave: "really encapsulates the idea"). Awaiting his VOICE pass + a release-home decision (where it publishes). |
| `notes/_bite-matrix-gm-move-DRAFT.md` | 2026-07-31 #63 | DRAFT, replay done — **count reconciliation owed** (4 unverdicted rows) before ratification. |
| `notes/_bite-matrix-chain-and-index.md` | 2026-07-31 #63 | DRAFT, conductor-built, mutations quoted — awaiting ratification. |
| `notes/_bite-matrix-capture-gate-PHASE1-DRAFT.md` | 2026-07-31 #63 | DRAFT phase 1 (no mutations by design) — phase 2 = the 30 UNCOVERED rows, queued #64. |
| `notes/_briefs/2026-08-01-dv-lockup-scope-brief.md` | 2026-08-01 #67 | dv-lockup scope. ✅ BUILT #68 (`2a3f6ee`) — per-chart-block + name confirmed at the #68 opener; premise correction inscribed in the brief. |
| `knowledge/snippets/Chart-donut.reference.html` (cd2 title) + `Chart-scatter.reference.html` (both titles) | 2026-08-01 #68 | ✅ **RULED #69 (Dave, in-session):** cd2 → "Housing dominates the month's spend" (edited via `data-lockup-title`, regenerated, sync green) · cs1 "Savings rise with monthly income" + cs2 "Savings rise with income across every segment" RATIFIED as-written (placeholder wording adopted as Dave's copy, no file change). ⚠ Text-only change; pages not re-rendered — geometry untouched. |
| `reviews/2026-08-01-72-D3-scatter-responsive-v1.html` (D3 scatter width-behaviour) | 2026-08-01 #72 | ⬛ **AWAITING DAVE — D3 deliberately UNRULED at #72** (he asked to SEE it again): width scrubber over both surfaces, fixed 1180/600/420 spread, mutation table. Registered at the #74 EXIT CHECK — the row was owed by #72's own wrap and lived only on rolling banners. |
| `knowledge/canon/dv-legend.js` + 5 chart snippets (★ DV-D18 additive isolate) | 2026-08-01 #70 | ⬛ **AWAITING DAVE — review pair NOT built, deferred by his own budget ruling at the #70 wrap** (~140K of a 200K line; a review build landed ~205K — declared and forked, not spent silently). **#71 opens by building it.** DV-D18 is gate-proven (108/108, both mutants red) but **NOT eye-proven**. Also still owed: **D3 lockup rework detail, Dave's — asked at the #70 opener, not given.** |
| ⛔ **Review-overlay export defect — ROOT-CAUSED AND FIXED #70** | 2026-08-01 #70 | ✅ **The radios were NEVER WIRED, in ANY review doc — an absence, not a #66 regression.** `_review-overlay.html`'s `buildPrompt()` built from `comments[]` only; zero radio reads. #66's fix repaired comment-pin export under a sentence claiming to repair *picks* — two problems conflated under one sentence, so #69's recurrence was inevitable. **Fixed:** `scanPicks()` (unruled groups declared "(not ruled)", never omitted) + `isDecisionControl()` exempting choice controls from the review-mode `preventDefault()`. ⚠ **UNPROVEN — Dave has not used it yet; #71's doc is its first real test.** |
| `knowledge/snippets/Chart-scatter.reference.html` (ds-020 axis/grid migration) | 2026-08-01 #69 | ✅ **RULED #69 (Dave, screenshots — review-doc export FAILED him, see defect row below):** D1 axis/grid colours APPROVED · D2 title-left/toggle-right APPROVED · **D3 legend lockup NEEDS REWORK** (keys-left/Reset-right + 520px wrap-centre placeholder rejected as built — no detail given yet, get it at the #70 opener) · **D4 swatch-during-isolate → DISCUSS LIVE next session** (DV-D17 vs Dave's "additive" wording — collision unresolved, DV-D17 stands until ruled) · D5 AGREED: fit hooks + four-theme wiring (incl. Mono/Console switcher in review controls — asked twice #69) = next session line. Review pair: `reviews/CHART-SCATTER-DS020-LOCKUP-2026-08-01-v1(.REVIEW).html`. |
| Review-overlay export path (`_make_review.py` pair) | 2026-08-01 #69 | ⛔ **DEFECT, RECURRED from #66: Dave ruled via screenshots AGAIN — "the radio buttons do nothing", no way to transfer picks.** The overlay was present (probed in-context) but the export was not usable/discoverable at the moment of ruling. The #66 hard lesson said the pair is mandatory; the pair EXISTED and still failed the person. Diagnose at #70: export discoverability (button placement/labelling) vs radios-outside-overlay-scope (doc's own radios may not be captured by the overlay's export). Until fixed, every review doc must state IN THE DOC how picks reach Claude. |
| `reviews/DV-D19-MODE-LATCH-PROTOTYPE-2026-08-02-v1.html` (DV-D19 mode latch, A/B against DV-D18) | 2026-08-02 #76 | ⚠ **BEHAVIOUR SIGNED OFF, ARTEFACT REJECTED.** Dave: *"DV-D19 is correct, but obviously it isn't styled correctly as this is a prototype."* The ruling is CLOSED — enacted in `canon/dv-legend.js`, mutation-proven by check 24 (112/116 under the old predicate, 116/116 restored). ⛔ The SPECIMEN is defective and it is my defect: it invented an `.is-solo` treatment instead of canon's (`canon.css:3508` — ink border + 6% ink tint, 12px swatch, 44px hit). **Repair = rebuild against real `canon.css` + real snippet markup, NOT a restyle of the approximation. OWED #77.** ✅ **REPAIR DELIVERED #78 — superseded by the v2 row below.** |
| `reviews/DV-D19-MODE-LATCH-2026-08-02-v2(.REVIEW).html` (DV-D19 on REAL canon — the #76 repair) | 2026-08-02 #78 | ⬛ **AWAITING DAVE — two pins.** Composed from linked canon.css + verbatim snippet figure + the #76-D1 behaviour trio; zero component-selector overrides (replayed); `.is-solo` = resolved `--ink` + 6% tint EXACT both themes (`canon.css:3508`); release path real-clicked both panes; verifier control 116/116. **Open pins, presented as OPEN in-doc:** (1) swatch-at-rest (narrow reading badged "shipped today", Dave's #75 wording quoted) · (2) ✅ **the fade — CLOSED #78 post-wrap** (Dave: *"the fade levels are good btw"* → ladder read back → *"— is correct"*; PRESERVE not ADD, closure in `_DATAVIZ-DECISIONS.md`). Committed `f73b9ec`; **ONE pin remains: swatch-at-rest.** ✅ **FULLY SIGNED OFF #79 (2026-08-02) — the last pin is CLOSED.** Swatch-at-rest put to Dave at the opener with both doc options verbatim + a fresh source probe of what the wide reading would actually cost (`visible[]` freezes at first click; the Reset guard `dv-legend.js:147` breaks in both halves; 14 legend-bearing surfaces). He ruled **NARROW** in his own words: *"DV-D19 — the mode latch, shown on real canon prototype is perfect as it is."* Read back as sensation before inscribing. **No code change — shipped behaviour ratified.** Closure in `_DATAVIZ-DECISIONS.md` (by addition, nothing trimmed). |
| `notes/2026-08-02-the-tokenizer-unit-five-whys.md` (the `ds-021` three-homes ENACTMENT — a decision brief, not an HTML review) | 2026-08-02 #80 | ⬛ **AWAITING DAVE — FOUR SHAPES, none pre-selected. ⚠ THE UNIT IS NOT ON THE TABLE: he RULED it at #54** (*"ONE unit — real Claude tokens; `cl100k` … never a unit a cap is stated in"*), declaring it NOT ENACTED that window — and it is still unenacted 26 sessions on. ⛔ **Read the note's CORRECTION BLOCK first:** the ratio was #53's measurement (×1.559, `knowledge/_measure_tokenizer.py`), not #80's; #80 re-derived it and nearly filed it as new. #80 re-measured `_CHAIN.md` 5,761 → **9,079** and `GOOD-MORNING.md` 27,171 → **42,435** ⇒ every tape-vs-budget comparison is dimensionally invalid. **New at #80:** the ROOT CAUSE — `measurement_degraded()` is present, wired, pinned and **blind** (the vocabulary has no word for REAL) ⇒ the remaining work is a CODE change, not the "write-up" §C·4 claimed. Shapes: **(A)** make the unit a TYPE (`Tokens(n, unit)`, refuses cross-unit compare) · **(B)** ONE measuring authority (delete two of three paths) · **(C)** a CROSS-INSTRUMENT gate — cheapest, and the only one that catches the NEXT occurrence · **(D)** calibrate and keep `cl100k` — **named for completeness and argued against** (a conversion where a measurement is available; ratio not constant, ×1.79 prose vs ×1.56 here). ⚠ **Deliberately NOT shipped:** whichever lands, the re-measurement touches the GM `size:` stamps, `ds-025`'s floor and the amber line. Ledger: `notes/_MEMENTO-DECISIONS.md` § ★ #80. |
| `showroom/index.html` (side tree nav #97 → ONE-BAR #98) | 2026-08-05 #98 | ⬛ **AWAITING DAVE, narrowed:** #97 flag ① CLOSED (#97-D1/#98 — viewbar + index theme seg REMOVED, one bar per page, bites 6–6e) · sources pure canon (#98-D1, receipt `notes/_receipts/2026-08-05-98-one-bar-chrome.md`). Still his eye: **bullet flex-height · Confirmation Replay disabled** (motion is display-toggle, not `dv-animate` — migrate idiom? canon behaviour change) · #97 flag ② 700px tree→intro gap, cosmetic. ✅ **BOTH RULED #103:** Replay = second detection idiom, snippet untouched (ds-029, enacted + mutation-tested) · bullet flex-height SUPERSEDED by ds-030 (all charts fully horizontally responsive; donut/pie = fluid container + fixed circle, render-proven). Remaining his eye: legend-variant centring (group vs svg-alone) · #97 flag ②. ✅ candlestick hollow/filled RULED #100 (ds-027: solid two-state, table = a11y fallback; spread `reviews/CANDLESTICK-FOURSTATE-2026-08-05-v1.html`) — **enactment owed #101**. ✅ **#97 flag ② CLOSED #104** — `.shell{align-content:start}` at ≤760px, `gen_showroom.py:338` (1 line) + regen; gap `intro.top−tree.bottom` 700px 322.5px→24px, 375px 259px→24px (24px = `.intro`'s own padding), 1180px byte-identical (zero regression); `--check OK — 75 in sync` · `--selftest OK — 11 bites`. ⛔ **CORRECTED AT SOURCE #104 — the ds-029 "enacted + mutation-tested" claim above covered the DETECTION clause only.** Driving the real page found Replay restarted only 3 of 4 Confirmation elements — `svg.success` never restarted (`void el.offsetWidth` is undefined on SVGElement, HTMLElement-only per CSSOM View, so the `none→prev` toggle never flushed). Fixed: `void el.getBoundingClientRect()`, `gen_showroom.py:285` + regen; all four elements now restart (proof: `460,finished→0,running→460,finished` etc.); mutation-tested both directions (reverted ⇒ reproduces the exact defect; restored ⇒ all four pass). Gates re-run green. **Lesson:** a mutation test proves the clause you mutated is load-bearing, not that the feature works — full arc `_DECISION-HISTORY/2026-08-05-104-unit-error-and-the-defective-enactment.md`. Legend-variant centring now MEASURED not decided → see the dedicated row below. |
| `reviews/OPACITY-PRIMITIVES-2026-08-05-v1(.REVIEW).html` (the 4% ladder spread, #96-D2/#98-D3) | 2026-08-05 #99 | ✅ **RULED IN FULL #99 — D1 full ladder · D2 `--alpha-04…96` · D3 migrate-all ties-DOWN · D4 REVERSED by #99-D1 (charts SOLID, state-changes-only licence).** Enacted + render-proven same window (ledger § ★ #99, ruling ds-026). ⬛ **ONE FORK REMAINS FOR DAVE: the wider literal-opacity corpus — 125 declarations / 49 snippet files (MEASURED #99, `_DS-IMPROVEMENTS.md` § #99) — sweep-now vs per-component-as-opened.** ⬛ **RESIDUAL ① `--pri-hover` — MEASURED, NOT PROMOTED → see the dedicated row below (#104).** |
| `reviews/LEGEND-CENTRING-SPREAD-2026-08-05-v1.html` (donut/pie legend-variant centring, Option A vs B) | 2026-08-05 #104 · **RULED #106** | ✅ **RULED #106-D1 (Dave, ds-031): OPTION A — KEEP AS-IS.** The 109px offset is ACCEPTED; Option B not taken. Original measurement retained below. ⬛ **AWAITING DAVE'S EYE — MEASURED, NOT DECIDED.** Option A (current, `.dv-donut-row{justify-content:center}`): ring **−109px off true container centre**, CONSTANT at 1180px and 760px — offset = (legend 190 + gap 28)/2 = 109 exactly, legend-footprint-driven not proportional. Option B (not enacted): a hidden `.dv-leg-spacer{min-width:190px}` mirroring the legend ⇒ **0px** offset. Light+dark, both widths, screenshotted. Nothing enacted pending his pick. |
| `reviews/PRI-HOVER-MEASUREMENT-2026-08-05-v1.md` + `.html` (`--pri-hover` opacity-derivation, #99 residual ①) | 2026-08-05 #104 · **RULED #106** | ✅ **RULED #106-D2 (Dave, ds-032): `--alpha-68` APPROVED — but SCOPED TO THE BUTTON ATOM, not 8 per-component edits.** NOT ENACTED: Dave ruled blast radius measured first (58 declarations / 10 of 77 files; 8-sites vs 9-files delta UNRECONCILED → #107). Original measurement retained below. ⬛ **AWAITING DAVE'S WORD — a promotion, not a pick (derivation governance).** All 8 candidate colours are pure neutral grey — no problem-hue hit. Group A (Icon-button/Empty-state/Form-layout/Stepper, live `color-mix(…70%…)`): swap to `--alpha-68` is a quantified delta — L `#5F5F5F→#636363` (+4/255), D `#B7B7B7→#B2B2B2` (−5/255); label contrast 6.39→6.01:1 (L), 6.3→5.96:1 (D), both clear 4.5:1. Group B (Modals/Action-bar/Confirmation/Drawer, flat `var(--pri-hover)`): exact if ramp-snap kept (re-snapping at 68% lands on the SAME stored steps, 0px change). Legacy `#BA1110` + Supercharge overrides: NOT promotable, never opacity-derived. Sub's recommendation (NOT a decision): swap Group A, leave Group B's stored hex, correct Group B's `$note` derivation number. |
| `reviews/PRI-HOVER-COMPARE-2026-08-17-v1.html` (`--pri-hover` LIVE COMPARE — stored vs re-derived, light + dark) | 2026-08-17 #197 · **RULED #198** | ✅ **RULED #198 — `s198-D1` + `s198-D2` (Dave, off this page: *"the suggestions are approved"*, readback confirmed; inscribed by the conductor via `_inscribe_ruling.py`, rulings 184→186, commit `5297f47`).** The stored `--pri-hover` is **re-derived at 0.68** — light `#626262`→**`#636363`**, dark `#B7B7B7`→**`#B2B2B2`** — and **`color/mono/hover-1|hover-2` + neutral mirrors are MINTED** with the aliases re-pointed. Doc row `W-37` **CLOSED**. ⚠ Still open and DAVE'S: the **hover-1/hover-2 NAMING** and **Supercharge's ramp-snapped hover** (warm/7 · warm/10 scope-preserving overrides, zero pixel change, no 0.68 warm derivation). ⚠ Group B components render `var(--pri-hover)` flat (+1/−5 per channel) and are **UNRENDERED by eye**. Original AWAITING text retained below. ⬛ **AWAITING DAVE'S EYE — he chose LIVE-COMPARE-FIRST instead of a ruling at #197** (`s197-D1` is a different item). Doc row `W-37`; it closes when he rules. ★ **FINDING SURFACED, NOT RULED: the DARK stored hex reproduces `0.70` exactly, while the LIGHT stored hex implies α≈0.6856 — it was never a clean `0.70` composite.** ⛔ No re-derived hex was written into canon, tokens or any record; the page is the only place they appear. Promotion is Dave's alone (derivation governance). |

| `_RESEARCH-graph-engineering-2026-08-05-v1.html` + `-v2.html` + `-v3.html` (graph-engineering audit, Swiss) | 2026-08-05 #105 · **-v3 DELIVERED #107** | ✅ **-v3 DELIVERED #107, pays #105-D1** — `_RESEARCH-graph-engineering-2026-08-05-v3.html`, Dave verbatim *"I want it to be honest."* Six honesty defects fixed; worst: the closing band self-labelled "the honest read" still carried the pre-correction four-type framing. Also reconciled a 7-vs-9 edge-type contradiction against `_build_decision_graph.py:38–53` + ADR-0012's dated amendment (nine is current, the 7→9 change now dated explicitly). Responsive gap declared in v2 CLOSED by numeric assertion. ⬛ **AWAITING DAVE'S READ.** ⛔ **CORRECTED #108: "v3 untracked, not yet staged" was STALE the moment it was written — `git log` shows v3 WAS staged and committed in #107's own wrap (`90c1d9d`).** |
| `reviews/TYPE-CSS-180-SPECIMEN-2026-08-06-v1.html` (`type.css:180` dark-mode specificity, #106-D3 deferred pending a specimen) | 2026-08-06 #107 · **RULED #108** | ✅ **RULED #108-D1 (Dave, by eye, `ds-033`): ink = `#1A1A1A`, not `#111111`.** Measured: delta +9 on R,G,B identically — achromatic, so the red/amber instability caveat does NOT apply; contrast vs white body text 18.88:1 (`#111`) vs 17.40:1 (`#1A1A1A`), both AAA — this was preference, not a contrast question. ⬛ **NOT YET ENACTED** — the literal at `type.css:180` still reads `#111`; #109's job. ⚠ **Record correction (from #107):** `type.css:180` really is `[data-theme="dark"]{background:#111;color:#fff;}` at 0-1-0, but there is no literal `body{background:var(--page)}` line INSIDE `type.css` — that 0-0-1 pattern lives in each consuming snippet's local stylesheet (e.g. `Account-card.reference.html:32`). **Bonus finding:** the same line 180 also overrides `color:#fff` over each snippet's `color:var(--text)` — invisible today because both resolve white, same literal-over-token pattern. |
| `outputs/_PARTITION-button-primary-2026-08-06-v1.md` (button/primary scope partition, enacts #107-D1 "partition first") | 2026-08-06 #107 · **#108-D2 EXTENDS #108** | ⬛ **STILL AWAITING DAVE'S FINAL SCOPE RULING.** #108-D2 (Dave): measure the other 35 colliding token names BEFORE ruling scope — only 5 of 40 were value-checked; extends #107-D1 one level deeper. Inscribed `ds-034`. ⚠ **The sweep's SPEC CHANGED this session (see the new finding row below): test value agreement PER THEME, not globally** — a global sweep would manufacture ~35 false findings, because themes are EXPECTED to diverge on the same token name (`ds-035`, Dave's four-theme standing constraint). #109's first job, per-theme. Groups from #107: **A** full button ladder (4 files: Button, Form-layout, Icon-button, Stepper) · **B** simple CTA, default+hover only (5: Action-bar, Confirmation, Drawer, Empty-state, Modals) · **C** brand scope, NOT a button case (1: Hero) · **D** secondary only (1: File-upload). ⚠ Lives in the gitignored `outputs/` folder — not a git-tracked path; Dave reads it locally. |
| `outputs/_FINDING-canon-pri-hover-brand-mono-fork-2026-08-06-v1.md` (`--pri-hover`/`--sec-hover` cross-theme "fork", measured + self-corrected) | 2026-08-06 #108 | ✅ **CLOSED, INFORMATIONAL — not a pending decision.** Measured live in `canon.css`: `--pri-hover` = `#BA1110` at `:root` vs `#626262`/`#B7B7B7` under `.cn-button`/`.cn-modals`; `--sec-hover` resolves to three different upstream tokens across three components. The MEASUREMENT stands; the DEFECT conclusion the doc first reached does not, once Dave's four-theme requirement (`ds-035`) is applied — the fork is almost certainly the theme layer working, not debt. ★ Governs `ds-034`'s sweep spec (PER THEME, not global). ⚠ Lives in the gitignored `outputs/` folder; Dave reads it locally. |
| `notes/_briefs/2026-08-06-graph-candidates-pricing-brief.md` (graph-engineering candidates 1–4, priced; the `-v3` research doc's enactment) | 2026-08-06 #115 | ⬛ **AWAITING DAVE — ONE ITEM, AND IT IS ITEM 4 (DEMOTE).** Items 1, 3 and the **mark-half of 2** were RULED and ENACTED this session (#115-D1/#115-D2, `9b47152` · `6a16633` · `ce0cc7f`). ⛔ **Item 4, DEMOTION, is NOT ruled and NOT scheduled** — it is decided on **`python3 knowledge/_graph_edges.py --tally`** (save-vs-noise evidence from `knowledge/_graph-mark-observations.jsonl`), never on recollection; the observation window was instrumented precisely so this ruling has provenance (Dave's catch: *"do I have to write these down on a postit or something??"*). First tally: 15 marked results, 4 with ⛔, `ls:LIFECYCLE` the predicted noise class (17 dead-node mentions — a section ABOUT supersessions). ★ **Read the mark correctly before ruling: ⛔ means the result MENTIONS a superseded node, NOT that the result is dead.** ⚠ Re-priced in-flight: node-id↔record overlap measured **0/575**, so candidate 1's *"Small"* in the research doc is **Medium**. |
| `_TRIAGE-118-bucket-sort-v1.md` (the decision-backlog bucket sort, run on Dave's *"Show me the bucket sort before you act"*; **floated, nothing enacted**) | 2026-08-07 #118 | ⬛ **AWAITING DAVE — ONE ITEM AND ONE ITEM ONLY: the type-composite gate's TIER, (a) BLOCKING now vs (b) SHRINK-ONLY RATCHET at 1,101.** Claude recommends **(b)** and names the risk against its own recommendation — *a baseline at today's count has exactly the shape of "a cap raised to clear its own gate."* ⛔ The other 16 rows need nothing from him: 8 were taken back as mis-escalated, 1 is stale, bucket D is Claude's entirely. ⚠ 9 of the 17 were sorted on nature, UNPROBED — declared. Verbatim home: `GOOD-MORNING.md` §C·2. |
| `reviews/outputs/mark-map-controller-v6.html` (the #122 mark-map pass — three lanes) | 2026-08-07 #122 · **SIGNED OFF #123** | ✅ **SIGNED OFF #123 — DAVE DROVE IT AND IT PASSED ("mega").** This closes #122's residual ① *and* the sixth parked consequence (*NOT DRIVEN VISUALLY*) in one act, and it was the licence for the four rulings that followed (`s123-D1`…`s123-D4`, `_rulings.json` 58–61): the 4.56 white-on-teal legacy success leg is **accepted with this pass**, and the SC badge shift was ratified having been seen rendered. Arc: `_DECISION-HISTORY/2026-08-07-123-rag-world-signoff-and-tint-opacities.md`. *(Original entry, verbatim, as the record of what was awaiting:)* ⬛ **AWAITING DAVE'S EYE — the RULINGS are closed, the RENDER is not.** `s122-D2`…`s122-D5` were taken by eye off v5; v6 is the post-enactment state: **mono editable** · **console+SC** with a live `console == supercharge` assert · **legacy display-only**, asserting fills against {#col25-017} AND marks against `s122-D5`. ⚠ **NOT DRIVEN VISUALLY — nobody has eyeballed rendered marks on the new fills.** ⚠ Six parked consequences ride with it (legacy warn/info backgrounds unruled · `ownsHexes` stale · SC badge shift · legacy success leg 4.56 · `*-tint` unruled · the visual confirm itself) — standing home `_LIVE-STATE.md` § OPEN. ⚠ Lives in the gitignored `reviews/outputs/` folder; Dave opens it locally. |
| `reviews/outputs/tint-opacity-tuner-v1.html` + `-v2.html` (the `rag/*-tint` scope question — v2 = FIVE lanes) | 2026-08-07 #123 | ✅ **SIGNED OFF #123 VIA DAVE'S OWN EXPORT + THE BADGE RULING.** The tuner is not a document he was asked to approve — it is the instrument he ruled `s123-D3` on and **exported the values from**: base `rag/*-tint` are his tuner-v2 composites, snapped to `--alpha-04..96`. Lane A also carried the SC badge leg (white label `4.51 → 5.94`), which he ratified as `s123-D4` (*"SC badge is fine"*). ⚠ **DECLARED GAP — no pixel render:** chromium is TLS-blocked in-sandbox (environmental, same fence as `_validate_state_contrast.py`), so the artefact was **runtime-asserted in node instead — 36 elements, all lanes populated.** That is a weaker proof than a render and is named as such, not laundered. ⚠ Lives in the gitignored `reviews/outputs/` folder; Dave opens it locally. |
| `reviews/outputs/graph-mark-tally-digest-v1.html` (the #115 tally distilled — 79 marks → 10 live-surface cards + 27-record archive bulk, SAVE/DROP controls + export) | 2026-08-07 #124 · **SIGNED OFF #124** | ✅ **SIGNED OFF #124 — Dave: "i've gone with all your recommendations."** SAVE ×3 ENACTED as `_LIVE-STATE.md` citation fixes (R-D1 → re-anchored R-D7/R-D15 · R-D4 hexes marked superseded per R-D12.B · R-D10 rebind amended by R-D11); DROP ×7 incl. the 27-record archive class. This judges the tally material and CLOSES the #115 observation window's evidence question. ⛔ **The DEMOTE ruling itself (candidates-brief item 4) is STILL DAVE'S — his ratification here judged the marks' save-vs-noise, not the demotion design; not laundered.** ⚠ Probe pollution declared on the card face: ~half the raw marks were s124's own queries; every judged card had clean sightings. ✅ Rendered AND driven at 1180/480 per `_RUNBOOK-render-verify.md` (the "chromium TLS-blocked" claim carried from #123 was STALE — the runbook works; read the runbook, not the hook). ⚠ Lives in the gitignored `reviews/outputs/` folder; Dave opens it locally. |
| *(update row)* `notes/_briefs/2026-08-06-graph-candidates-pricing-brief.md` item 4 | 2026-08-07 #124 · **CLOSED** | ✅ **s124-D1: DEMOTE RETIRED — mark-only stands, permanent.** Ruled by Dave on the tally evidence he judged the same session (76/79 noise; digest row above). The #115 graph programme is now fully closed: items 1–3 ruled/enacted #115, item 4 ruled #124. `_rulings.json` entry 62. |
| `reviews/MEMENTO-SCHEMATIC-2026-08-07-v2.html` (the Memento schematic v2 — GENERATED by `knowledge/_gen_schematic.py`; seven panels, 39 rows, every figure read off disk at generation time) | 2026-08-07 #127 | ⬛ **AWAITING DAVE.** Built at the third attempt (rolled #125, #126). ⚠ **Not a document he was asked to approve a value in — it is a MAP, and the question is whether it shows the right six subsystems and reads cleanly.** ★ Each panel computes its own *"what re-checks this"* and renders a red **NOTHING RE-CHECKS THIS** where the answer is none; it fired that about ITSELF until the 98 → 102 wiring landed. **Guarded by `_gen_schematic.py --check` (wired, blocking) so it cannot publish a previous tree.** ⬛ **One open call rides on it: ⛔/★ GLYPHS vs ASCII in generated review artefacts — UNRULED, Dave's.** |
| `reviews/MEMENTO-SCHEMATIC-2026-07-26-v1.html` (v1 — hand-authored, superseded) | 2026-07-26 · **TOMBSTONED #127** | ✅ **NOT A PENDING DECISION — KEPT AS EVIDENCE.** +29/−0, purely additive banner; **its stale figures are deliberately untouched** (*"27 blocking validators in a 55-step build"* against a disk of 30 and 98 at #127). Dave's #125 disposition, enacted: **KEEP and TOMBSTONE, do not correct and do not delete.** Read figures at v2, never here. |
| `reviews/CONTRAST-CONTROLLER-2026-08-08-v1.html` + `-v2.html` + `-v3.html` (the #130 state-contrast decision controller — banner actions, check/selection labels, chips pressed, tabs badges, legacy banners; **v3 holds the FULL measured grid and the four-theme fall-through overlay, all four themes explicit**) | 2026-08-08 #130 · **RULED `s130-D4`/`D5`/`D6` — ⛔ NOT ENACTED** | ⬛ **RULED, ENACTMENT LICENCE PENDING DAVE'S WORD.** Values → `knowledge/tokens/*.json`, consumption → snippet corpus + regen + gate replay. ⚠ Legacy success `#00847F` + white sits AT the 4.5 boundary and is OWED a measurement at enactment. |
| `reviews/MONO-MARK-ERROR-2026-08-08-v1.html` (the mono `--mark-error` companion — options measured `#FFFFFF` 6.02 / `#F0F0F0` 5.28) | 2026-08-08 #130 · **⬛ AWAITING DAVE** | ⬛ **NOT RULED — his error-mark image DID NOT ARRIVE.** The conductor's provisional *white shape, red glyph* reading (both legs 6.02) is recorded as provisional and may not be enacted from. |
| `reviews/LEGACY-RAG-BANNERS-2026-08-08-s131-v1.html` (the `s131-D1` render-proof — 4 legacy RAG severities x light/dark, computed values asserted EXACT on all 8 legs at 1180 and 480, real HSBC face `document.fonts.check` true) | 2026-08-08 #131 · **VIEWED BY THE CONDUCTOR, crop confirmed** | ✅ **NOT A PENDING DECISION — this is the ENACTMENT EVIDENCE for `s131-D1`** (error `#A8000B` / warning `#FFBB33` / success `#00847F` / information `#305A85`; white text+marks on error, success, information; amber the sole dark-ink exception). Contrast receipts, conductor-measured on the committed values: white/error **7.87** - ink/warning **11.57** - white/success **4.56** (the tight leg, re-measured) - white/info **7.17**. ⬛ **What is still AWAITING DAVE is the ERROR-MARK image confirm (carried from #130), not these fills.** |
| `reviews/INFO-BLUE-CONTROLLER-2026-08-08-s132-v1.html` (the console/SC info-blue controller — L/S/H sliders + presets, 4 live WCAG legs, family context on 4 theme/mode surfaces, ruling-sentence export; contrast maths verified against an independent Python implementation before handover) | 2026-08-08 #132 · **RULED FROM — `s132-D1`** | ✅ **NOT A PENDING DECISION — Dave ruled `s132-D1` from this instrument via its own export (#5A85C1, ink mark), confirmed firm on readback including the mark flip.** |
| `reviews/CONSOLE-SC-INFO-BLUE-2026-08-08-s132-v1.html` (the `s132-D1` render-proof — console+SC × light/dark, chips + banner + tint, values read LIVE from the regenerated canon.css) | 2026-08-08 #132 · **VIEWED BY THE CONDUCTOR, crop confirmed** | ✅ **ENACTMENT EVIDENCE for `s132-D1`** (fill `#5A85C1`, mark INK `#1A1A1A`; error now the sole white mark on console/SC). 24/24 computed-value assertions EXACT at 1180 and 480, real HSBC face. Console tints re-composited `#D1DDEE`/`#364963` at the s123-D3 ruled alphas; ⚠ **SC tints deliberately UNCHANGED — SOLID pins per s123-D3, re-hue is a separate unruled call, flagged to Dave.** ⚠ **DECLARED: the #131 chain's "two console/SC information-REST items" resolves to NO record in `_STATE-CONTRAST-AUDIT.md`** — a Banner-scoped re-run on the enacted bytes reproduced only the 4 known pressed-button fails at 4.09 (s130-D4 territory, untouched); the white-on-info worry the gloss described is dissolved arithmetically by the mark flip (ink 4.61 at REST), but no named audit record existed to close. The gloss/record mismatch is recorded here, not smoothed. |
| `reviews/KG-EDGES-REVIEW-2026-08-08-s133-v1.html` (+ `.md` twin — the Dave's-eye KG migration review: 35 near-miss nodes, 98 ref:null prose edges grouped by type, 47 unvetted governedBy candidates, token value/$note grammar proposal) | 2026-08-08 #133 · **⬛ AWAITING DAVE** | ⬛ **PENDING — the prose→reference batch may not land without his eye** (s131-D2 watch + s133-D1 status). Mechanical half ENACTED and gated: 653 typed edges + 98 declared prose, registries 214+139, `_validate_kg.py` green + live-mutation-tested, index 642→1073. |
| `reviews/MONO-ALERT-OPTIONS-2026-08-08-s134-v1.html` (the mono alert live options controller — shell treatment + glyph colour, JSON export) | 2026-08-08 #134 · **RULED FROM — `s134-D4`** | ✅ **NOT A PENDING DECISION — Dave ruled `s134-D4` off its own export, firm ("nailed to the mast"): TINT-ONLY shell (no border/accent), glyph `#1A1A1A` both themes, JSON export confirmed on readback.** |
| `reviews/ALERT-ERROR-MARK-REVIEW-2026-08-08-s134-v3.html` (v1→v3 arc: matrix-driven re-base after the stale-snippet find, all four themes) | 2026-08-08 #134 | ⬛ **AWAITING DAVE — the [0] eye pass on the tint-only alert's committed bytes (s134-D4 watch line, residual ⑧).** v1/v2 superseded by the spine re-base; v3 holds the current legs 5.55/7.99/8.77/7.04, not yet render-confirmed by Dave. |
| `reviews/NOTIF-BORDER-RULE-2026-08-08-s135-v1.html` + `-v2.html` (contextual notification shell — live controller, four themes × light/dark, border + radius) | 2026-08-08 #135 · **RULED FROM — `s135-D1`** | ✅ **NOT A PENDING DECISION — Dave ruled `s135-D1` off v2's own export ("love it: ruled"): legacy+SC keep the 1px border, mono+console tint-only; console radius bound to `border-radius/surface` (12, provisional).** RULED NOT ENACTED — enactment queues with the s130 remainder (Notifications snippet is legacy-tagged, hardcoded radius 0 + unconditional border). |
| `reviews/KG-REVIEW-EXPLAINER-2026-08-08-s135-v1.html` (method explainer) + `reviews/KG-REVIEW-ROWS-2026-08-08-s135-v1.html` (row controller) + `reviews/KG-REVIEW-VERDICTS-2026-08-08-s135-v1.json` (computed verdicts, the ruled set) | 2026-08-08 #135 · **RULED FROM — `s135-D2`** | ✅ **NOT A PENDING DECISION — Dave ruled `s135-D2` at the computed defaults, conditional-accept verified first** (*"as long as the repetitions are not stylistically different i'll go with your recommendations"* — the 5 exact-match context nodes checked as pure placement registrations, zero styling to lose). Verdicts: NEAR-MISS merge 5/leave 30 · PROSE promote 52/stay-null 46 · GOVERNEDBY attach 25/skip 22 · TOKENS split all 24. ⛔ **APPLICATION BLOCKED — lane A found `gen_kg_edges.py` has no preservation path for NEARMISS/PROSE/GOVERNED (0/82 could land, freshness gate refuses every shape); only TOKENS (9/24) landed.** Exceptions ledger: `notes/_briefs/2026-08-08-135-laneA-kg-apply.md`. Enactment rides on `s135-D4`. |
| `reviews/TIER-MAP-PROPOSAL-2026-08-08-s135-v1.html` (424 nodes tiered: 14 atom / 35 molecule / 31 organ / 0 template / 209 lexicon-fold / 134 mutation-key / 29 shells; 16 low-confidence rows flagged) | 2026-08-08 #135 | ⬛ **AWAITING DAVE — the 16 low-confidence rows, his eye, controller export.** Follows `s135-D3`'s hierarchy requirement (atoms→molecules→organs→templates, shells = slot mechanism at any scale) — this proposal is the substrate mapping, not yet ruled. |
| `reviews/RESEARCH-hierarchy-best-practice-2026-08-08-s135-v1.html` (industry-practice research: flexible graph+tiers, Figma Slots, same-button-recontextualised, W3C DTCG) | 2026-08-08 #135 | ✅ **NOT A PENDING DECISION — INFORMATIONAL, cited by `s135-D3`.** Verdict: flexible graph+tiers is current practice (no rebuild needed); Figma Slots ≈ shells; same-button-recontextualised is the settled industry answer; W3C DTCG stable Oct 2025. ⬛ **Three possible alignment rulings FLOATED, NOT RULED, DO NOT UPGRADE:** DTCG wire format · Figma-Slots semantics for shells · fork-ban gate. |
| `reviews/RULING-CANDIDATE-three-axis-2026-08-08-s136-v1.html` (the three-axis model candidate Dave ruled from) | 2026-08-08 #136 · **RULED FROM — `s136-D1`** | ✅ **NOT A PENDING DECISION — Dave ruled `s136-D1` off this candidate via controller export, no amendments** (`reviews/THREE-AXIS-VERDICTS-2026-08-08-s136.json`, verbatim). Subsumes all three #135 floated alignment items (DTCG wire format · Figma-Slots semantics for shells · fork-ban gate). RULED NOT ENACTED — enact lanes queued for #137. |
| `reviews/THREE-AXIS-DEMO-2026-08-08-s136-v1.html` (live mechanism demo — parameters/variants/slots walked through interactively) | 2026-08-08 #136 | ✅ **NOT A PENDING DECISION — SUPPORTING EVIDENCE for `s136-D1`.** Dave: visuals help him embed concepts — keep this pattern for future ruling candidates. |
| `reviews/TIER-MAP-CONTROLLER-2026-08-08-s136-v1.html` (the 16 low-confidence tier-map rows as a one-question-per-row controller, embedded showroom iframes + export; supersedes the #135 `TIER-MAP-PROPOSAL` for THIS purpose) | 2026-08-08 #136 | ⬛ **AWAITING DAVE — the 16 rows, carried from `s135-D3`'s substrate mapping.** Picks persist via localStorage, per-browser (NOT pre-populated by a sandbox drive-test click — corrected this session). Export lands verbatim once ruled, per the live-controller convention. |
| `knowledge/_TOKEN-FORK-LEDGER.json` (+ `knowledge/_FORK-BAN-GATE.md` — the fork-ban gate's 42 measured forks, recorded `UNRULED-BASELINE-s139`) | 2026-08-09 #139 | ⬛ **AWAITING DAVE — 42 FORK VERDICTS, ALL OF THEM HIS.** The gate (`knowledge/_validate_token_forks.py`) parses `canon.css` in the consumer's grammar (specificity-ladder `var()` resolution), mutation-tested on real bytes + a two-direction selftest. ★ **It re-prices #108: “40 colliding token names” is measurably STALE — there are 104, with 20 FORK verdicts** — and it reproduces #108's `--pri-hover` (48 contexts / 4 values) and `--sec-hover` (18 / 8) INDEPENDENTLY, which is what makes the new figure trustworthy. ⛔ **The ledger is a BASELINE, NOT A SANCTION** — nothing in it is ruled, and a fork is not assumed to be debt (Dave's four-theme standing constraint means divergence on a shared token name is often the theme layer WORKING, per `ds-035`). Rides with residual ①'s ruling queue. |
| `reviews/SCHEMA-AMENDMENT-CONTROLLER-2026-08-09-s140-v1.html` (+ export `reviews/SCHEMA-AMENDMENT-VERDICTS-2026-08-09-s140.json` — the `meta.schema.json` binds/slots amendment, three questions with named alternatives) | 2026-08-09 #140 · **RULED FROM — `s140-D1`** | ✅ **NOT A PENDING DECISION — Dave ruled all three off this controller, verbatim: *"D1. lets do hybrid / D2. accept and make mandatory / D3. Permit now, enforce by gate later"*.** `props[].binds` HYBRID (string \| array \| intent map) · slots contract with **`use` MANDATORY**, `accepts` by tier/capability only (child list mechanically refused) · **permit-now/gate-later** staging. RULED **AND ENACTED** same session (`8b548b8`): 75/76 metas validate, the 1 fail PRE-EXISTING on `EXAMPLE-button` and attributed by a control run (delta zero); 13/13 drives green incl. the REFUSED child-list arm. ⛔ **The intent vocabulary is deliberately UNRULED — legal but open.** |
| `reviews/SLOTS-DRAFT-REVIEW-2026-08-09-s140-v1.html` (+ `reviews/SLOTS-DRAFT-2026-08-09-s140-v1.json` — 42 slot contracts across 28 components, provisional-agent, 4 rows self-flagged `$doubt`) | 2026-08-09 #140 · **RULED FROM — `s140-D2`** | ✅ **NOT A PENDING DECISION — Dave ruled off this review's export + a chat follow-up:** 24 accepted verbatim · **`cards` AMENDED to DUAL-AXIS** (variants WILL be built AND content stays slot-injectable — slot kept with a `$note`; ★ the axes are not exclusive, which the draft had assumed) · **`empty-state.body` → text param** (folds into the floated 38-row text-param refinement) · **`segmented-control.content` → variant-axis candidate** · **`textarea.rows` REJECTED — audit false positive.** ENACTED `d79d382`: 25/76 metas carry slots, textual insertion with per-file rest-of-file parse-equal asserted. ⬛ **DECLARED: this review HTML was checked STRUCTURALLY, NOT RENDER-PROVEN — no browser run happened this session.** ⛔ **The capability vocabulary is agent-proposed and UNRULED.** |
| `reviews/BINDS-REVIEW-2026-08-09-s141-v1.html` (+ export `reviews/BINDS-DRAFT-2026-08-09-s141-v1.json` — the DTCG/axis-A controller: number→dimension ×104, `layout.json` scale-1/2/3 encoding, the binds draft 12 proposed / 120 `$doubt` across 277 props, composite `lineHeight`) | 2026-08-09 #141 · **RULED FROM — `s141-D1`** | ✅ **NOT A PENDING DECISION for the four questions it asked — Dave ruled all four off this controller's own export, read back in chat before recording:** (A) number→dimension **MIGRATE-TICKED, all 104, none excluded** (unit-strip priced separately, **not waived**) · (B) `layout.json` scale-1/2/3 = **B1 dimension + `$extensions`**, breakpoint set preserved verbatim · (C) binds draft **ACCEPT-PROPOSED**, 12 bindings land in 11 metas · (D) composite `lineHeight` **OMIT**, `DEF-COMPONENT-LINEHEIGHT` stands. RULED **AND ENACTED** same session (`9196e13` + `b4f59e6`, both pushed): `_validate_dtcg.py` **PASS — 0 failures, 61 deferrals**; `_validate_binds_ratchet.py` **PASS**, floor 0→11, lowering refused. ⛔ **STILL PENDING AND EXPLICITLY NOT RULED: the 120 `$doubt` rows** — plus the ~97 visual props with **no spine address at all** (★ the lane's finding: **391 of 561 `tokens`-map entries resolve to no address; axis A is an AUTHORING job, not a migration**; Dave's pick-lists exist for 62). ⬛ **DECLARED: this review HTML was checked STRUCTURALLY, NOT RENDER-PROVEN** — no browser run happened this session; render-proof debt now ×2 with the #140 slots review. |
| `reviews/BINDS-AUTHORING-REVIEW-2026-08-10-s142-v3.html` (+ export `reviews/BINDS-AUTHORING-VERDICTS-2026-08-10-s142-v3.json` — the binds authoring wave as 12 class cards + 7 confirm-sets + the row-level remainder; v1/v2 kept as the arc's record, superseded for THIS purpose; judgment basis `notes/_briefs/2026-08-10-142-binds-judgment.md`, PROPOSED/basis-tagged) | 2026-08-10 #142 · **RULED FROM — `s142-D1`** | ✅ **NOT A PENDING DECISION for 113 of its 114 rows — Dave ruled BY CLASS off v3's own export (verbatim in chat + read-back): 12 class verdicts (C1–C12) + 3 `surface` row-picks (`background.default`; C13's class judgment NOT ruled) + 7 C12 confirm-sets.** ★ This resolves the s141 row's "120 `$doubt` STILL PENDING" — those rows are now class-ruled under `s142-D1` (by ADDITION; the s141 row stands as written). ⛔ **STILL PENDING: `tooltip.tip` (the one unruled row) · the ENACTMENT (binds into metas — #143 residual ①) · the colour-spine values behind the C4/surface ADDRESS-INTENTs (blocked on the colour-spine DTCG migration, never done — `b4f59e6` was 5 files, colour not among them).** v3 render-proven this session (3 PNGs light/dark/480); **the ×2 render-proof debt (s141 BINDS-REVIEW · s140 SLOTS-DRAFT-REVIEW) is UNCHANGED.** |
| `reviews/RAG-TEXT-RUNG-2026-08-10-s144-v1.html` (the plus/minus text-rung SCOPING controller — two rung-name spellings, export works either way) | 2026-08-10 #144 | ✅ **NOT A PENDING DECISION — SUPPORTING EVIDENCE, the instrument Dave narrowed scope FROM.** Off this controller and two further verbal narrowings, Dave ruled coloured text = plus/minus red/green ONLY; information and warning coloured text OUT. The rung-NAME question it poses (`rag.<hue>-text` vs `-ink`) is carried to #145, Dave's. **RESOLVED BY ADDITION AT #145: Dave ruled `rag/<severity>-ink` (`s145-D1`), minted in `knowledge/tokens/semantic-colour.json`.** |
| `reviews/PLUSMINUS-TUNER-2026-08-10-s144-v1.html` (plus/minus text tuner v1 — saturation & brightness, hue locked) | 2026-08-10 #144 | ✅ **NOT A PENDING DECISION — SUPERSEDED BY v2, kept as the arc's record.** The pass where the gamut clip was caught: `green-light gamutClipped:true` while the per-channel RGB clamp silently moved the hue +6.84° (142.94°→149.78°) with nothing chasing the consequence. |
| `reviews/PLUSMINUS-TUNER-2026-08-10-s144-v2.html` (plus/minus text tuner v2 — ground selector, two-way contrast, hard gamut guard) | 2026-08-10 #144 · **RULED FROM — `s144-D1`** | ✅ **NOT A PENDING DECISION — Dave ruled `s144-D1` off this tuner's own export** (`drivingModelAtExport: hsb`, `selectedGroundRoleId: 'selected'`): minus/red light `#DA1A00` · minus/red dark `#F6604C` (unchanged) · plus/green light `#137F3C` · plus/green dark `#66CC8D` (unchanged), hue locked to the `s122-D2` fills, saturation/brightness only. v2's hard chroma-clamp held hue drift to +0.087° against v1's uncaught +6.84°. |
| `_review/state-contrast-mono-preview-v5.html` (the mono state-contrast preview, v5 — the one-treatment proposal) | 2026-08-11 #149 · **CITED BY — `s149-D1`** | ✅ **NOT A PENDING DECISION — this is the preview Dave ruled AGAINST**, and it is cited as evidence on `knowledge/_rulings.json` § `s149-D1`. Committed rather than left chat-only: a chat-only attachment is an un-retrievable citation. |
| `_review/state-contrast-ruled-preview-v3.html` (+ generator `_review/_gen_ruled_preview_v3.py`) | 2026-08-11 #149 · **CITED BY — `s149-D1`** | ✅ **NOT A PENDING DECISION — SUPPORTING EVIDENCE: this is where `#B92F1E` under black text was measured dead at 2.89**, which is what killed the `s130-D4` fill move for mono. ⛔ Its D4-a glyph/bare-role follow-up and D5-a box-border question are recorded `open` on the `s149-D1` entry, **UNRULED**. |
| `_review/state-contrast-controller-v1.html` · `-v2.html` · `_review/state-contrast-mono-preview-v4.html` (+ generators `_build_v4.py`, `_build_v5.py`) | 2026-08-11 #149 | ✅ **NOT PENDING — the arc's record, superseded for their purpose.** ⚠ **v1 carries a `<base>` bug: its specimens lose `type.css`** — noted, NOT fixed. ⚠ **v2/v3 re-derived the SETTLED `s130-D4` before Dave caught it** — the conductor's [[survey-before-build]] defect, recorded here so the artefacts are not read as fresh proposals. |
| `_review/chip-pressed-isolation-v1.html` (the chip pressed-state isolation specimen — reversal vs tint, light/dark) | 2026-08-11 #151 · **RULED FROM — `s151-D2`** | ✅ **NOT A PENDING DECISION — Dave ruled `s151-D2` off this specimen: chip pressed takes NO reversal and is TINT-SYMMETRIC** (black tint light, the equivalent white tint dark); the 16 rag-on-neutral pairs move to the rendering gate. Committed rather than left chat-only — a chat-only attachment is an un-retrievable citation. |
| `_review/atoms-reach-and-star-v1.html` (the atom-reach + star-glyph isolation specimens — checkbox/radio/switch/indeterminate dash, star options A/B/C) | 2026-08-11 #151 · **RULED FROM — `s151-D3`** | ✅ **NOT A PENDING DECISION for what it asked — Dave ruled `s151-D3` off it:** atom reach extends to radios/switches/indeterminate dash; star glyph = **option C** (ink flips with the wash); pressed ramp-normalised `#CECECE`/`#484848`. ⛔ **RULED NOT ENACTED for legs (1) and (2)** — #152 residual ①. ⚠ **The star ruling carries a DECLARED UNPROVEN caveat on the 1.66 diagnosis — verify the pair BEFORE enacting.** |
| `_review/s152-enactment-review.html` (+ the driven renders `_review/s152-atoms-{light,dark}.png` and `_review/s152-chip-{rest,hover,pressed}-{light,dark}.png` — the `s151-D3` enactment review: atom reach on all four control types in error, and the chip star measured in real hover/pressed) | 2026-08-11 #152 | ✅ **NOT A PENDING DECISION — VERIFICATION EVIDENCE.** Presented live to Dave, who confirmed option C off it (*"I thought I'd said to enact c"*). It is the receipt that the 1.66 diagnosis is FALSE (hover `#FFF`/`#232323` = **15.72**, pressed `#FFF`/`#484848` = **9.15**) and that the enactment landed. Cited by `knowledge/_rulings.json` § `s152-D1`. |
| `knowledge/snippets/Amount-display.reference.html` § `sign=positive` (+ the driven renders `_review/s157-amount-light.png` · `_review/s157-amount-dark.png` — the `s157-D1` enactment specimen: all three sizes, the `+` sign carried redundantly with colour per 1.4.1, `--success-ink` `#137F3C` light / `#66CC8D` dark) | 2026-08-12 #157 · **RULED FROM / APPROVED IN-WINDOW — `s157-D1`** | ✅ **NOT A PENDING DECISION for the mono seat — Dave saw the render live and approved it: *"for mono it's perfect"*.** Contrast MEASURED in-render, not derived: **5.09:1 on white** · **8.77:1 on `#1A1A1A`**, both AA; computed inks read back `rgb(19,127,60)` / `rgb(102,204,141)`. ⚠ **The specimen currently carries RAW HEX in its two `[data-theme]` blocks** — it swaps to the projected var once the `s157-D2` palette tier exists (`notes/_briefs/2026-08-12-s157-palette-tier-brief.md`). ⛔ **NON-MONO THEMES ARE NOT APPROVED BY THIS** — they inherit the mono greens by silent fall-through, which is the finding that produced `s157-D2`. |
| `_review/160-nine-snippet-fails-v1.html` (the nine snippet fails as a one-question-per-fail controller — Alert ×3 · Badge ×2 · Form-layout ×1 · List-items ×3, with export) | 2026-08-12 #160 · **RULED FROM — `s160-D2`** | ✅ **NOT A PENDING DECISION — Dave ruled off this controller's export (keep ×3 + badge monoink) plus the rule he found himself:** *"pLease find this rule, maybe its failing"* ⇒ `s134-D1` + `s151-D1` clause 4, so **seven rag-on-tint pairs were resolved BY INTENT, not by value** (migrated to the meaning-carrier gate, the `s151-D2` motion). Amendment verbatim: *"if there are any reports generated, there should still be a soft warning but not a block, I don't care how we label it"* — **already satisfied by the 🟡 advisory, exit-code-exempt tier; nothing new was built.** Snippet gate **75/0** (was 9). ✅ **APPROVED BY DAVE'S EYE in light AND real dark** — ⚠ the first dark shot failed to flip `data-theme` and **Dave caught the two identical previews**; the re-render is what he approved. ⛔ **Layout was UNPROVEN when built** (sub-built), later rendered fine via the `/var/tmp/pw-browsers-s136` farm. |
| `reviews/pri-hover-verdicts-42-v1.html` (the #139 token-fork ledger as a one-question-per-fork controller — 42 cards, live hover contrast, localStorage persistence, JSON export) | 2026-08-12 #164 | ⬛ **AWAITING DAVE — 42 verdicts, NONE pre-selected** (markup verified: zero pre-selected inputs; render-verified: 42 cards, 0 console errors, export well-formed). ⚠ **THE FILENAME UNDERSELLS THE ARTEFACT AND THAT IS AN OPEN QUESTION, NOT A FIX:** the 42 are the WHOLE token-fork ledger (`knowledge/_TOKEN-FORK-LEDGER.json`, #139), of which `--pri-hover` is ONE card — the name came from the original brief. ⛔ **NOT renamed by the #164 wrap; renaming a review artefact re-scopes the review, and that is Dave's.** |
| `_BRIEF-borrowed-instruments-2026-08-12-v1.md` + `-v2.md` (the borrowed-instruments side-lane product) | 2026-08-12 #164 · commit `434c455` (pushed) | ⬛ **RULED-TO-EXPLORE, enactment PARKED for the Friday sessions on Dave's word.** Five Dave-gates open inside it (incl. §5 grades fork + the Tier-1 carve-out). ⛔ Nothing enacted; the wrap ruled none of the gates. |
| `_BRIEF-progress-dashboard-2026-08-13-v1.md` (the visual progress dashboard / "mission control") | 2026-08-12 #164 · commit `7543b48` (pushed) | ⬛ **RULED A PRIORITY BY DAVE #164** — *"this is a priority after the side quest, it will really help me."* Build session **2026-08-13**, AFTER his sidequest lane. Mono components + `swiss-design-system` aesthetic (both his words). ⬛ **Panel layout/content beyond the brief is UNRULED — Dave rules by eye at the build.** |
| `reviews/PRESENTATION-2026-08-12-designer-community-v3.html` (the designer-community promotion deck — Memento + Apollo as TWO SEPARATE PRODUCTS, sponsor-deck visual system, 13 slides) | 2026-08-12 **POST-WRAP ADDENDUM to #164** (worker lane `tender-stoic-clarke`; receipt `notes/_receipts/2026-08-12-tender-stoic-clarke-designer-community-deck.md`) | ⬛ **AWAITING DAVE'S EYE — AND THE CALL IS 2026-08-13, so this is a DATED sitting, not an open queue row.** ⚠ **NOT RENDER-VERIFIED** — playwright absent in the worker's sandbox; HTML structure parse clean for all three versions, nothing more is claimed. ⚠ **THE WORKER'S OWN FLAG, CARRIED UNSOFTENED:** the component trajectory line — **75 today → ~135 near → ~300 horizon, "enabling autonomous production for low-risk projects"** — is **the deck's most forward-leaning PUBLIC claim**; soften on Dave's request before the call [[dave-public-positioning]]. ⛔ **The deck was NOT edited by this addendum**, and ⛔ **v1/v2 were NOT deleted** (version-don't-overwrite; the worker names them as `_to_delete` candidates AFTER the call, which is Dave's). ⛔ **The Dave figures inside it are FLOATED/OBSERVED, not rulings** — no ledger or ADR carries them. |
| `dashboard/index.html` (the generated progress dashboard / "mission control" — exec summary · gates strip · stat cards · two plates · provenance panel · forward lane · kanban 19/13/1/4 · scored priority) | 2026-08-13 #165 · **RULED FROM / BUILT — `s165-D2`, `s165-D6`** | ⬛ **AWAITING DAVE'S EYE on PANELS AND LAYOUT — the brief put those with him and this build did not take them.** ✅ Render-verified; `gen_dashboard.py --check` mutation-tested; **WIRED into `_build_all.py` as step "dashboard sync", routed GATE** in mirror of `gen_showroom.py --check`. ⚠ **The priority ORDER on the page is a PROPOSAL, never a decision** — a score computed at generation from six criteria whose weights are printed on the page, never written back to a store, regenerated every build (`s165-D2`). ⚠ **LOW-CONFIDENCE flags name their missing inputs**; **links coverage is 0/37**, reported as a flagged problem and repaired nowhere. ⛔ **Interactive drill-down/manipulation is a v2 LANE, ruled future by Dave (`s165-D6`) — not missing, deferred.** |
| `_PROPOSAL-links-backfill-2026-08-13-v1.md` (the `_state.json` links backfill — 6 of 37 candidates, each with its evidence) | 2026-08-13 #165 · **RULED A PROBLEM TO FIX — `s165-D4`** | ⬛ **AWAITING DAVE, AND THE RATIFICATION IS PER-LINE, NOT PER-DOCUMENT.** Dave ruled that empty `links` across all 37 items is a defect, not a tolerable gap. ⛔ **NO LINK HAS BEEN WRITTEN TO THE STORE and none may be until he ratifies line by line** — a backfilled link that nobody ratified is exactly the authored-provenance class the 15 open provenance fails are already teaching. ⚠ **31 of 37 items have NO candidate proposed** — the proposal is partial by construction and says so. |
| `reviews/REVIEW-168-graphic-amber-blue-picker-v1.html` (the AMBER + BLUE light-leg picker for the `rag/*-graphic` rung) | 2026-08-13 #168 · **RIDES ON — `s168-D4`** | ⬛ **AWAITING DAVE'S PICK — TWO VALUES, BOTH HIS.** He ruled the red and green light legs onto the already-ruled text values and said *"choose a new amber and blue"*; ⚠ **the light amber `#C58900` and light blue `#306EC6` currently in `knowledge/canon/canon.css` are ENACTED-PENDING-EYE PLACEHOLDERS, not picks and not ruled values** — they exist so the tier renders and are the thing this picker replaces. Carried to #169. |
| `reviews/REVIEW-168-rag-glyph-tier-v1.html` · `-v2.html` (the graphic-strength rung specimen — v2 supersedes v1 for this purpose, v1 kept as the arc's record) | 2026-08-13 #168 · **CITED BY — `s168-D4`** | ⬛ **AWAITING DAVE'S EYE on the v2 specimen.** The ruling **subsumes and ratifies 82-B as-built pending that look**; all four DARK legs stand unchanged by his word (*"I'm happy with the current colours in dark mode"*). ⚠ **The rung NAME `-graphic` is a DECLARED DEFAULT, UNRATIFIED.** ⚠ **A dark-leg `$note` contrast figure is IN DISPUTE** — 3.66:1 vs 3.42:1 on `#CC4333` — unresolved, and the species may affect the other three. |
| `reviews/REVIEW-168-eye-owed-v1.html` (the eye-owed collection for this session's rendered surfaces) | 2026-08-13 #168 | ⬛ **AWAITING DAVE'S EYE — incl. the rendered look of the dashboard's effort-rung block and its unit sentence** (`s168-D2`). ⚠ Not a decision surface: nothing here is ruled, and no value was authored from it. |
| `_PROPOSAL-148-reds-2026-08-13-v1.md` (the #148 gate-reds proposal — 82-A/82-B graphic-strength-tier options) | 2026-08-13 #168 · **RULED FROM (82-B) — `s168-D4`** | ✅ **NOT A PENDING DECISION for 82 — Dave picked 82-B and `s168-D4` subsumes it.** ⚠ **STILL OPEN: RED 30** — he asked *"which theme?"*, option C (theme-key the store) is the **unconfirmed** recommendation. ⚠ **Reds 36/45/51 measure GREEN and are mutation-proven biting, but their CLOSURE PROVENANCE IS UNPROVEN** — they may be collateral; no closure is claimed. |
| `notes/_PROPOSAL-effort-gauge-2026-08-13-v1.md` (the S/M/L effort gauge — Options A/B/C with trade-offs named) | 2026-08-13 #168 · **RULED FROM (Option C) — `s168-D2`** | ✅ **NOT A PENDING DECISION — Dave ruled Option C: *"this sounds good"*.** Unit = real job-window tokens, boot/wrap and **delegated sub spend EXCLUDED**; band edges **45K/85K at n=21 are DERIVED at build from gauge-log quartiles — DERIVED, NOT RULED** (the derivation METHOD is the ruled thing). ⛔ **All 37 item effort VALUES remain Dave's; none authored.** |
| `_BRIEF-component-scaffold-2026-08-14-v1.md` (the component scaffold sub brief — 342 lines; six gates, one closed) | 2026-08-14 #173 · **GATE 1 RULED FROM — `s173-D1`** | ⬛ **AWAITING DAVE ON GATES 2–6, AND THEY ARE HIS FOR THE WEEKEND:** scaffolder-vs-component · the indeterminate variant · the `component-type` family · correcting the two stale inventory documents · who builds it and at what price. ✅ **GATE 1 IS CLOSED — `s173-D1`(a): the progress bar (determinate, linear and circular) is the first component through the route**, on grounds that are OBSERVED DUPLICATION rather than speculation (`role="progressbar"` already improvised inside `Progress-tracker`, `File-upload` and `Stepper`; re-verified first-hand at the #173 wrap). ★ **THE BRIEF'S MOST USEFUL OUTPUT IS THAT IT FOUND ITS OWN INHERITED PREMISE STALE — the scaffold LARGELY ALREADY EXISTS** (runbook, meta schema + 76 metas, 75 snippets, canon generator, 4-theme cascade, 76-page showroom, 12-step gate chain); missing is narrower — **a scaffolder, a standard per-theme render harness, and any index/checklist over the metas**. ⚠ **TWO STALE INVENTORY DOCUMENTS FLAGGED AND NEITHER CORRECTED** (that is gate 5, his): `knowledge/_COMPONENT-LIBRARY-TARGET.md` (*"~38 components, ~20 P1 gaps"* vs 75 snippets / 76 showroom pages, only **Brand mark / logo** genuinely absent) and `reviews/ITINERARY-2026-07-14-apollo-component-library.html` (**86 stale Gap/Partial rows**). |
| `_BRIEF-component-scaffold-2026-08-14-v1.md` — **SUPERSESSION ROW, BY ADDITION** (the #173 row above stands verbatim as written; this row records what changed, and does not rewrite it) | 2026-08-14 #174 · **GATES 2–6 RULED FROM — `s174-D1`** | ✅ **NOT A PENDING DECISION — ALL SIX GATES ARE NOW CLOSED AND THE BRIEF IS CONSUMED.** `s173-D1` closed gate 1; `s174-D1` closes 2–6 on a plain-prose read-back: **gate 2 — BUILD THE COMPONENT, NOT A SCAFFOLDER**, keeping a friction log (grounds: the route exists as runbook + gate chain, so what is missing is convenience not capability, and a scaffolder now would be a speculative instrument under `s172-D3`'s observed-failure rule) · **gate 3 — DETERMINATE ONLY** (the indeterminate state overlaps `Loading-indicator` and ownership is unruled) · **gate 4 — NO `component-type` FAMILY MINTED**, queued as a proposal, `component-types.json` untouched · **gate 5 — the two stale inventory documents are a SEPARATE LANE, after the build** · **gate 6 — one OPUS sub, one window, ~110–130K**, no Sonnet subs. ⚠ **GATE 5 IS THE ONE STILL OWED** — the correction lane has not run. |
| `reviews/REVIEW-174-progress-bar-four-themes-v1.html` (the progress bar across all four themes × light/dark — 8 panes; the FIRST component through the scaffold route) | 2026-08-14 #174 · **BUILT UNDER — `s174-D1`** (gate 2) | ⬛ **AWAITING DAVE'S EYE ON TWO THINGS, AND THE SECOND ONE IS A DECISION, NOT A LOOK.** ✅ **Render verified first-hand:** all 8 panes correct, rendered colours match the token maths, `fillW/trackW = 260.4/420 = 62.0%` matches `aria-valuenow="62"`, and the font was asserted by **canvas measurement against two controls** (target 347 · DejaVu 375 · nonexistent 301), never `fonts.check()`. ⛔ **THE DECISION: fill-on-track fails 3:1 in LEGACY DARK (1.75:1) and SUPERCHARGE DARK (2.38:1).** Checked **BEFORE** building and deliberately **NOT declared as a gated `contrastPair`** — declaring it would have been a green that cannot fail in two themes; meaning is carried instead by a numeric value in text plus `aria-valuetext`. **Pre-existing — the same pair sits under `Progress-tracker`.** Queued to `knowledge/_DS-IMPROVEMENTS.md` with a four-theme table and three options. ⛔ **DAVE RULES IT; this wrap did not.** |
| `reviews/STEP-COLOUR-CONTROLLER-2026-08-14-v1.html` (the step-tracker colour controller — the page that put the three undecided themes to Dave, and the page whose computed-style measurement found the HEAD blank) | 2026-08-14 #176 · **RULED FROM — `s176-D2`** | ✅ **NOT A PENDING DECISION — SUPERSEDED FOR ITS PURPOSE by the STEP-SUCCESS-SYSTEM pages, kept as the arc's record.** ★ **Its lasting output is not a colour: measuring COMPUTED STYLES on this page is what found that `--step-complete` painted NOTHING in Mono and Console at HEAD** — `canon.css`'s TOKENS block had never been regenerated since #175 minted the token, so it had no `:root` definition to resolve against and every gate stayed green. ⚠ The page's own Legacy pick (`#A8000B`, "Legacy error") was **corrected by Dave off the NEXT page** to `#DB0011`; read it as a superseded proposal, never as a value. |
| `reviews/STEP-SUCCESS-SYSTEM-2026-08-14-v1.html` (the success-roundel inheritance, all four themes × light/dark, Progress-tracker + Stepper + collapsed bar + the Legacy bar alongside; BEFORE/AFTER per cell) | 2026-08-14 #176 · **RULED FROM — `s176-D2`** | ✅ **NOT A PENDING DECISION — this is the page Dave corrected the Legacy leg ON:** looking at it he said *"Use the primary red for Legacy"* (**#DB0011**, final), superseding his own earlier *"Legacy error, sorry my mistake"* (#A8000B). ★ **The correction only happened because there was a rendered page to look at — a list of hexes would have shipped #A8000B.** Superseded for ratification purposes by `-v2`; kept as the arc's record. |
| `reviews/STEP-SUCCESS-SYSTEM-2026-08-14-v2.html` (+ builders `reviews/_STEP-SUCCESS-2026-08-14-v1-build.py`, `-v2-build.py`, the HEAD control `_STEP-SUCCESS-2026-08-14-v1-BEFORE-canon.css` and the measured `-v2-contrast.json`) | 2026-08-14 #176 · **RATIFIED FROM — `s176-D2`** | ✅ **NOT A PENDING DECISION — RATIFIED BY DAVE'S EYE ON THIS PAGE:** *"There will be reviews by others later so some changes might be made in the future, thats just the process. lets get these in for now!"* ⛔ **Ratified AS-BUILT with future third-party revision EXPECTED — that is not a hedge on firmness**, and `step/on-complete`'s NAME stands under the same clause. ✅ Gated contrast measured on the page, not derived: tick-on-fill **Mono 8.77/8.77 · Legacy 5.22/17.40 · Console 6.34/6.34 · SC 6.34/6.34**, labels **12.63–17.45**. ⛔ **The fill-on-page and fill-on-track cells are DECLARED ABSENCES under `s176-D2`(d)'s doctrine** — *"the label and the symbol must carry the contrast not the decoration"* — never fixed by a value move and never faked into a pass. ⚠ **The Legacy dark tick `#1A1A1A` is a DECLARED EXCEPTION** taken by the build (an exact roundel-mark match would be white-on-white) and is accepted by this ratification. |
| `reviews/2026-08-15-dream-pass-7-report-v1.html` (the dream-pass 7 report — the four proposals as Dave read them at the ruling) | 2026-08-15 #177 · **RULED FROM — `s177-D1`** | ✅ **CONSUMED, NOT PENDING:** Dave ruled all four off this report the same session (*"I'll go with your recommendation"*), P4b signed later in the window. Listed for retrieval, not as an open queue row. |
| `_GARDENER-REVIEW.html` (the gardener review controller — 9 real cards from `notes/_dream/_GARDENER-QUEUE.json`, generated by `knowledge/gen_gardener_controller.py`) | 2026-08-15 #179 | ⬛ **AWAITING DAVE — NOTHING ON IT HAS BEEN DECIDED.** Built under `s179-D1` as the priority build (the #179 promotion probe found **0 ruling refs** across four dream-proposal files ⇒ review friction is the binding constraint). 9 cards (T1 0 · T2 6 · T3 3, six `flag_only` mechanical), provenance stamped, backpressure strip **9/Q=15**, deferment line; taps compile to **ONE paste-able ruling message**. ⛔ **The page WRITES NOTHING — it is not a fifth register**; `flag_only` cards auto-preselect and disable accept. Driven via a DOM stub: PAUSED mutation, honest-empty and fail-loud-on-missing-queue all observed. ⛔ No red authored, no canon tokens bound. **Owed with it:** the `--apply-tier1` write path is **ENACTED, NOT VERIFIED**, and the §3-fence-1 vs lane-brief wording question is Dave's. |
| `_BRIEF-compaction-strategy-2026-08-15-v1.html` (the context-compaction research + brief — Swiss-design page; verdict PUT: compaction is a parachute UNDER the wrap, never a substitute) | 2026-08-15 #181 | ⬛ **AWAITING DAVE — FIVE QUESTIONS OPEN IN ITS §06, ALL UNANSWERED AND PARKED [0 — DAVE'S].** Dave's word closing the lane: *"we'll park it for now."* ⚠ **Parked is not answered.** Q1 the framing itself (parachute-under-the-wrap) · Q2 one paragraph into `knowledge/_RUNBOOK-capture-ritual.md` (W1) · Q3 the gauge reading `usage.iterations` (W2) · Q4 the sub-compaction policy (W3) · Q5 one surface line in the check-in (W5). ⛔ **NOTHING IN THE BRIEF IS RULED** — `knowledge/_rulings.json` carries no compaction entry, and the verdict above is a PUT. ✅ Build discipline checked: two-red law `#DA1A00`-on-white, ink `#1A1A1A`, markup parse-verified, accent + ink checked; ⛔ no new colour value authored (`s151-D1` / `s149-D1` untouched). ⚠ **NOT render-verified** — declared, not claimed. |
| `_REVIEW-B3-grades-2026-08-15-v1.html` (the B3 grades review deck — four calls put to Dave; the page WRITES NOTHING and compiles taps into one paste-able ruling message) | 2026-08-15 #182 · **RULED FROM — `s182-D1`** | ✅ **CONSUMED, NOT PENDING — all four calls settled the same session, and CALL 1 was REOPENED AND AMENDED LIVE** after alternatives were put (*"no i think you are right"*): probe commissioning is **PARKED behind the first-cycle numbers**, UNPROVABLE is an **honest register** not a defect, and any future commissioning is scoped to the **starred/blocked 49, never all 122**. ★ **IN FORCE NOW: the probe-shaped-hooks AUTHORING CONVENTION.** ⚠ **STILL PLACEHOLDER, BY RULING: the 30-day AGING threshold** — picked, not derived; derive it from cycle data. ⛔ **The counting window opens at the FIRST SCHEDULED pass (2026-08-16 07:10); the 2026-08-15 manual pass does NOT count.** ★ **The deck also carried the session's correction: the record's "the Monday slot is unscheduled" claim was FALSE** — `memento-dream-pass` is scheduled Sundays 07:10, ENABLED, verified by a live `list_scheduled_tasks`. |
| `reviews/SPARKLINE-ATOM-TUNER-2026-08-15-v1.html` (the sparkline atom tuner, v1 — 37-chip colour picker + 4px/ramp snapline) | 2026-08-15 #182 | ✅ **SUPERSEDED FOR ITS PURPOSE BY `-v2`, kept as the arc's record** (version-don't-overwrite). ⛔ **Its headline feature died of a ruling, not a defect:** `s182-D3` made the stroke **semantic** — red/green ink keyed by trend and background across all four themes — so **a 37-candidate picker had nothing left to ask**. Read the picker as a superseded question, never as a set of candidate values. |
| `reviews/SPARKLINE-ATOM-TUNER-2026-08-15-v2.html` (the sparkline atom tuner, v2 — 31,509 B; picker removed, semantic strokes rendered live in both directions) | 2026-08-15 #182 · **RE-SCOPED BY — `s182-D3`** | ⬛ **AWAITING DAVE'S EYE — AND SCALING IS THE ONLY OPEN DECISION ON THE PAGE [0 — DAVE'S, #183's natural opener].** Responsive width; height snapped to the **4px grid** in the manner of the `EDIT-MODE-UX-PROTOTYPE-2026-07-19-v3` countdown dial (continuous dial, `round(d/4)*4` output, numerals to the type ramp). ⛔ **No colour question remains and no value was minted** — every ink on the page is already ruled (`s151-D1` / `s155-D1` / the standing ink). ⚠ **CARRIED WITH IT, UNRESOLVED AND DAVE'S: `s151-D1` and `s149-D1` are inscribed MONO ONLY while `s182-D3` keys the same inks across ALL FOUR THEMES** — a live contradiction in the record, reported rather than reconciled. ⚠ **A pre-existing dashoffset/endpoint render artefact is visible** — attributed with a control, ⛔ not a regression from this session. |
| `reviews/STATUS-CHART-RENAME-2026-08-16-v1.html` (the `s184-D3` status-chart vocabulary rename — both themes, the four-row mapping table `breach→negative` / `watch→monitor` / `healthy→positive` / `info→neutral`, and the rationale in Dave's own terms; the rename itself is RULED and ENACTED, render-proven byte-identical, so what is owed here is his EYE on the page, not a decision) | 2026-08-16 #184 | ⬛ **AWAITING DAVE'S EYE** |
| `notes/_briefs/2026-08-16-memento-closeout-plan.md` (the Memento Close-Out Programme — scope rule, three phases, dated externalities, pitfalls; `status: standing`) | 2026-08-16 #185 | ⬛ **AWAITING DAVE AT THE PHASE 1 SITTING — THE PROGRAMME IS APPROVED, ITS CONTENTS ARE NOT.** Dave approved the programme in chat, verbatim: *"okay sounds good … go for it"* ⇒ the file is **`status: standing`**, ⛔ **NOT ruled**, and `knowledge/_rulings.json` carries **no `s185-D*`**. What is owed at the sitting: compaction **Q1 / Q2 / Q4 / Q5** (Q3 parked with the stated trigger *"compaction live somewhere in our stack"*) · **dream pass 6 P1–P5** (P3 to be **verified-moot first** — the git runbook already carries the #141 correction; P5 is one word on `--all-dirty`, which was **USED live** at #185's dream-seam commit) · **dream pass 7 P1** (liveness check, deferred at `s177-D1`) · **ratification of the close conditions on the two new store rows `W-17` / `W-18`**, both of which are the conductor's proposal and his to amend. ⛔ **Phase 1 is WORDS ONLY** — anything build-shaped gets a priced row and moves to Phase 2. |
| `reviews/HOVER-NAMES-2026-08-18-v1.html` (the `hover-1`/`hover-2` primitive naming options — Dave picked **B**) | 2026-08-18 #199 · **RULED FROM (option B) — `s199-D2`** | ✅ **NOT A PENDING DECISION — CONSUMED; enacted at `dcc16de` (`hover-light` / `hover-dark` across `colour.json`, the neutral mirrors, the semantic aliases and the Supercharge overrides)** |
| `reviews/RADIUS-CORNER-TUNER-2026-08-18-v1.html` (the live radius/corner tuner — border-radius tokens from `knowledge/tokens/layout.json`; container + data-mark sliders flagged **PROPOSED**; export preserves the `$alias` cascade; ★ carries the finding that Supercharge has **no** border-radius override and inherits the mono base) | 2026-08-18 #199 | ⬛ **AWAITING DAVE'S EYE — built and UNRULED; the "return SOON" item returned** |
| `reviews/GROUPB-HOVER-EYE-2026-08-18-v1.html` (Group B under the `s198-D1` hover — `cn-modals` / action-bar / confirmation / drawer, light + dark) | 2026-08-18 #199 | ✅ **APPROVED #200 — Dave, in chat: *"I've approved the corner radius already, and the hover ruling"*; #199's residual ② CONSUMED** |
| `reviews/RADIUS-CORNER-TUNER-2026-08-18-v2.html` (the radius/corner tuner, v2 — the segmented-switch radius carve-out Dave asked for at #199) | 2026-08-18 #200 | ✅ **SUPERSEDED FOR ITS PURPOSE BY `-v3`, kept as the record of the carve-out step** |
| `reviews/RADIUS-DERIVE-SPECIMEN-2026-08-18-v1.html` (the derive specimen — the mint list `s200-D2` was read off; generator `knowledge/gen_radius_derive.py`, `--check`/`--selftest` mutation-proven) | 2026-08-18 #200 · **RULED FROM — `s200-D1` · `s200-D2` · `s200-D3`** | ✅ **NOT A PENDING DECISION — it was the surface the rulings were read off** |
| `reviews/RADIUS-CORNER-TUNER-2026-08-18-v3.html` (the radius/corner tuner, v3 — dimension-first, `xs`/`s`/`m`/`l` labels after Dave vetoed `scale-1..4`, heights 24/36/44/48) | 2026-08-18 #200 · **RULED FROM (the `s` scale) — `s200-D4`** | ⬛ **AWAITING DAVE — `xs`/`m`/`l` are UNTUNED; he tunes them on this page, and his veto on the `s200-D4` read-back is open** |
| `reviews/SEGMENTED-SCALE-SPECIMEN-2026-08-18-v1.html` (the segmented-scale specimen — all four `xs`/`s`/`m`/`l` scales live on the real console palettes, light + dark, 8/8 computed-style asserts, hit-zone overlay driven, checked at 1240 + 480) | 2026-08-18 #201 · **RULED FROM — `s201-D1` / `s201-D2` / `s201-D3`** | ✅ **APPROVED BY EYE #201 — Dave, in chat: *"this is great"*.** ⇒ #200's residual ④ (the `s200-D4` tokens UNPROVEN in render) is **CONSUMED FOR CONSOLE, and only for console**. ⚠ An eye-read caught a stacking defect the 8/8 asserts passed (fixed, re-verified) — an approval by eye is **not** a gate: the page is wired into no build step. |
| `reviews/SEGMENTED-SCALE-SPECIMEN-2026-08-18-v3.html` (the segmented-scale specimen, v3 — the APPROVED v1 grammar carried forward with the `s202-D2` corrected thumbs 0/2/4/6 and the labels re-set on the REAL type scale 12/14/16/16 = `font-7`/`font-6`/`font-5`; the earlier pages' 11px was OFF-SCALE) | 2026-08-18 #202 · **BUILT UNDER — `s202-D1` / `s202-D2`** | ⛔ **REJECTED BY DAVE'S EYE, POST-WRAP #202 — his words: "all of these are wrong" (all four scales, screenshot supplied).** Observed in his screenshot: the white thumb on the LAST segment ("Month") overflows the track's RIGHT edge — no trailing inset; the approved-v1 seating logic fails on the trailing segment (the #202 compare pages hit the same class on other sides). ⚠ FIRST ITEM AT #203 — fix starts from `knowledge/snippets/Segmented-control.reference.html` seating, not another approximation. ⬛ the label-binding decision remains separately OPEN, Dave's. ⛔ **NOTHING RULES THAT A SEGMENTED LABEL TAKES `font-7`/`font-6`/`font-5`** — the binding is PROPOSED by this build and is his to confirm or move; the *thumb* values on the page are ruled (`s202-D2`) and are not in question. ⚠ **An approval by eye is not a gate: the page is wired into no build step and no CI job**, so its computed-style asserts run only when a human opens it [[instrument-without-a-consumer]]. ⚠ **NOT PUSHED — CI has not seen this file.** |
| `reviews/SEGMENTED-SCALE-SPECIMEN-2026-08-18-v2.html` + `reviews/SEGMENTED-SWITCH-COMPARE-2026-08-18-v1.html` · `-v2.html` · `-v3.html` · `-v4.html`, plus the switch-carve-out tuner `reviews/RADIUS-CORNER-TUNER-2026-08-18-v4.html` (the pages built on the FIRST, WRONG reading of `s199-D3`'s "switch" — a two-state variant rather than the sliding thumb) | 2026-08-18 #202 · **DISSOLVED BY — `s202-D2`** | ✅ **NOT A PENDING DECISION — STAMPED `SUPERSEDED` / `QUESTION-DISSOLVED` IN-FILE AND KEPT, NEVER DELETED** (version-don't-overwrite). ⚠ **THE COUNT IS FOUR ON DISK, AND THREE WERE PUT TO HIM** — `v1`/`v2`/`v3` are the three he rejected; `v4` also exists and carries the `DISSOLVED` stamp. ⛔ **Dave rejected all three compare pages he was shown, by eye — *"the switch is still wrong"*, then *"is your sub on drugs?"*** — and what ended the question was a STORE READ (`s199-D3` + `s200-D1`), not another build. ★ **Read them as the record of a vocabulary collision, never as candidate designs**; the class-fix is `s202-D3`'s provenance binding, and a glossary remains **FLOATED, Dave's**. |
| **`reviews/REVIEW-203-INDEX-2026-08-19-v1.html`** (the #203 review INDEX — the entry point to the eighteen wave-3a four-theme spreads, with each lane's flags) | 2026-08-19 #203 | ⬛ **AWAITING DAVE — THIS IS THE DOOR.** The banner points here rather than at ~50 individual PROPOSED items across twelve receipts. ⛔ Nothing behind it was ruled by the #203 wrap. |
| **The eighteen wave-3a four-theme spreads** — `reviews/REVIEW-203-{alert,amount-display,amount-input,data-grid,date-picker,date-range-picker,drawer,empty-state,file-upload,form-layout,popover,secure-entry,skeleton-loader,stat-card,stepper,textarea,time-picker,toast}-four-themes-v1.html` | 2026-08-19 #203 | ⬛ **AWAITING DAVE.** Built by lanes A–F **instead of** the components they were briefed to build (all eighteen already existed, gated, since 2026-07-22). Each carries measured conformance findings against #203's ruled rules. ⚠ **`stat-card`'s arrows sit in the FILL seat at 1.98:1 and the new `kpi-tile` inherits it — 7 of 8 panes disagree; the two-red law says they must move together, and the move is HIS** (`s151-D1`). |
| **The nine wave-3b four-theme spreads for the NEW components** — `reviews/REVIEW-203-{command-palette,sidebar-nav,anchor-nav,combobox,multi-select,tags-input,kpi-tile,timeline,avatar-group}-four-themes-v1.html` | 2026-08-19 #203 | ⬛ **AWAITING DAVE.** Nine genuinely-absent rows, built and gated (library 76 → 85). ⚠ **`kpi-tile` is drawn as a PROPOSED proposal, not a closed gap** — `s182-D2` names the KPI/trend tile and explicitly declines to rule it; ⛔ never launder it into a ruling. ⬛ **`sidebar-nav` vs the existing Navigations component is an unadjudicated overlap.** |
| `reviews/REVIEW-203-canon-dark-repair-before-after-v1.html` (lane G's before/after for the canon dark-drop repair — 33 rules across 19 of 76 snippets that `gen_canon_components.py` was discarding under `[data-theme="dark"]`) | 2026-08-19 #203 | ⬛ **AWAITING DAVE'S EYE ON THE DARK LEGS.** The repair is mutation-tested (6 arms rc=0) and regeneration-verified; **the mono error ink camp reaches dark for the first time in 19 components**, which is a visible change he has not seen. ⚠ CI has seen none of it. |
| `reviews/ITINERARY-STATUS-2026-08-19-v1.html` (+ sidecar `-v1.json`, renders `-v1-1180.png` / `-v1-480.png`) — lane H's DERIVED itinerary status, generated by `knowledge/gen_itinerary_status.py` | 2026-08-19 #203 | ⬛ **AWAITING DAVE — AND THE QUESTION IS BIGGER THAN THE PAGE:** does this generated surface RETIRE `reviews/ITINERARY-2026-07-14-apollo-component-library.xlsx` as the source of truth? Measured 86 agree · **35 STALE** · 3 OVERSTATED · 0 UNRESOLVED; TRUE gaps = 23 Layer-1 rows (P1 = row 86 alone) + Layer 2 as ONE structural gap. ⛔ The frozen 2026-07-14 files were not touched. |
| **The six #204 P2 four-theme spreads** — `reviews/REVIEW-204-{popconfirm,footer,layout-utilities,document-row,payment-card-visual,runway-bar}-four-themes-v1.html` | 2026-08-19 #204 | ⬛ **AWAITING DAVE — ALL SIX SHIP `PROPOSED`, NOTHING HERE IS RULED.** The 7 TRUE P2 gaps from `reviews/ITINERARY-STATUS-2026-08-19-v1.json` `$true_gaps`; six built, **one refused**. ⚠ **Row 91 Transaction/ledger row was NOT built — it is a DUPLICATE of a component Dave already promoted**: `list-items.meta.json` `build.$status` reads *"PROMOTED 2026-06-22 (Dave) — TRANSACTION row brought to the Tabs-bar standard"*. The itinerary's own derived snapshot called it a true gap because **a slug-shaped probe cannot see a component that ships as a VARIANT of another** — a class finding, not a one-row miss. ⚠ Row 92 `document-row` is a genuine gap **only in its two-action form**; if Dave rules single-action only, it collapses into List-items too. ⚠ **Row 94 `runway-bar` originates in a TEST FIXTURE, not a product need** — `_COMPONENT-GAPS.md` line 18: *"the waterfall / runway / proportional bars **were invented**"* by the 2026-06-30 SME-Payments fitness tests; **declining it is a legitimate answer and is his first decision on that page**. ⚠ Row 93 `payment-card-visual` carries **three declared, empty, aria-hidden brand slots** (issuer mark, network mark, EMV chip) — brand marks are not a worker's to invent — and has **no dark card face** pending the missing `inverse/hero surface` role. ⛔ **`popconfirm` surfaces a NEW two-red seam Dave has not seen: in Apollo Legacy the destructive glyph `#DA1A00` and the primary button `#DB0011` are `1.03:1` apart in one bubble, so the REVERSIBLE confirm reads as more alarming than the destructive one** — same species as #203's arrow seat (`s151-D1`), inherited and flagged, **not resolved**. ⛔ **The token store has no destructive button seat at all** (probed: no `danger`/`destructive`/`critical`); none was invented. ⚠ Registered in the radius ratchet (a GATE ratchet) but **deliberately NOT registered in `gen_showroom.CATEGORIES`** — they fall to the "More" bucket, because showroom placement is promotion and promotion is Dave's. Receipts: `notes/_receipts/2026-08-19-204-wave-lane{M,N,P}-*.md`; claim table: `notes/_receipts/2026-08-19-204-buildpm-claim-table.md`. |
| `reviews/REVIEW-209-grid-snap-before-after-v1.html` (the 4px-grid snap before/after — the **29 off-grid values** snapped under Dave's ruled nearest/ties-up policy, built on his word *"Snap + review page"*) | 2026-08-20 #209 · row `W-60` | ⬛ **AWAITING DAVE'S EYE — THE SNAP IS ENACTED, THE LOOK IS NOT SIGNED OFF.** The gate `[8778e6d]` is GREEN and the policy applied is the ruled one; ⛔ **nothing on this page was ruled by the #209 wrap**, and no colour or token value was authored. |
| `reviews/REVIEW-209-input-trim-before-after-v1.html` (the leading-trim sweep before/after — the **54 → 65** trim blocks across text-bearing inputs + textarea, plus the section **"The 11"** for the blockless snippets added on *"Add to all 11"*) | 2026-08-20 #209 · row `W-61` | ⬛ **AWAITING DAVE'S EYE, AND IT IS A TWO-PART LOOK.** ⛔ **CARRY THE MEASUREMENT WITH THE PAGE:** Chromium **COMPUTES but does NOT ENACT** `text-box-trim` in form controls — span 32 → 11.56, input 32 → **32.00** — so the input half of the sweep is a **forward-compatibility bet, kept on Dave's own word *"Keep + canary"***, with probe **`P-6`** registered as the canary (fires proven by mutation; first CI drive GREEN). The **11** half DID move real labels (Form-layout **−37px**). ⛔ Nothing here is ruled. |
| **The nine wave-3 surfaces, by PATH** — `showroom/{back-to-top,fab,limits-meter,range-slider,rating,split-button,standing-order-mandate-row,transaction-row,transfer-list}.html` (+ their `knowledge/snippets/*.reference.html` and metas); receipts `notes/_receipts/2026-08-20-209-wave3-lane{A,B,C}-*.md` | 2026-08-20 #209 · rows `W-62` / `W-63` | ⬛ **AWAITING DAVE — LIBRARY 91 → 100, AND THE SITTING HAS TWO EXISTENCE QUESTIONS BEFORE IT HAS ANY DESIGN ONES.** ⛔ **`transaction-row`** — #204 Lane N ruled row 91 a **DUPLICATE** of a component Dave already promoted, and only the ledger form was built; the built page says so on its face. ⛔ **`limits-meter`** — `Progress-bar`'s meta already **claims the use**. Whether either should exist at all is HIS. Plus **~20 named design questions** in the receipts' `$decisionsForDave`. ⚠ **Deliberately NOT registered in `gen_showroom.CATEGORIES`** (they fall to the *More* bucket, the #204 precedent) — **placement is promotion and promotion is Dave's**. ⚠ 5 new var collisions were renamed **component-local at birth**, 4 off-grid values snapped under the ruled policy, and invented icons were replaced by **byte-matched** library glyphs. ⛔ Nothing in these receipts was ruled by the #209 wrap. |
| `reviews/REVIEW-210-existence-side-by-side-v1.html` (the two #209 EXISTENCE questions put side by side — **List-items vs Transaction-row** and **Progress-bar vs Limits-meter**, each pane a LIVE iframe of the REAL `knowledge/snippets/*.reference.html` file, light and dark) | 2026-08-20 #210 · row `W-64` | ⬛ **AWAITING DAVE — THESE ARE THE TWO QUESTIONS THAT COME BEFORE `W-63`'s ~20 DESIGN ONES.** ⛔ **Nothing on the page is ruled, recommended or preferred** — it states what each answer would IMPLY and stops; no third answer is proposed. (1) **LIST or LEDGER** — #204 Lane N ruled row 91 a DUPLICATE of the promoted List-items transaction row, and #209 built only the ledger form; if the statement is a LIST, `transaction-row` should not exist and nothing downstream registers it, so removing it costs nothing. (2) **`limits-meter`** — `progress-bar.meta.json` `commonPatterns` already names *"savings goal / limits meter"* verbatim; keep / fold into Progress-bar / merge into Runway-bar. ⚠ Limits-meter **vs Runway-bar** (receipt Q9) is a SEPARATE open question and is deliberately NOT put on this page. ⛔ **No specimen is hand-rolled** (#202) — the panes are the real files. ⚠ **MEASURED, and declared on the page's own face:** over `file://` Chromium gives every file an opaque origin and returns `null` for a frame's document (probed first-hand, not assumed), so the dark control falls back to re-serving **the same file's bytes** via `srcdoc` with exactly two mechanical substitutions — the `<body>` theme attribute, and `href="../canon/type.css"` → `href="../knowledge/canon/type.css"` (a srcdoc inherits the parent's base URL; without the rewrite type.css drops SILENTLY, #29). A control render proves the srcdoc panes match the files opened directly (same title, same computed body font, `canon/type.css` present, `--uf` resolving). Over `http://` the direct path is used and no copy is served. ✅ Render-verified in-sandbox, light + dark, at 1440 and 900 — all four specimens paint in both themes; the HSBC face asserts against two controls (target 346.88 · DejaVu 375.39 · nonexistent 301.07). ⚠ Three of the four snippets compute `Times New Roman` on `<body>` — **PRE-EXISTING**, proven by a direct-render control, ⛔ not introduced by this page. ⚠ Generated by a **NON-REPO** throwaway script (`s191-D2`: the sub's outputs mount, not in the tree). ⚠ CI has not seen this file. |
| `reviews/REVIEW-210-spacing-fixes-v1.html` (the two spacing fixes Dave named off the #209 trim page — Time-picker's error variant and Data-grid's state switcher, BEFORE/AFTER renders of the REAL snippet files, light + dark, every gap measured) | 2026-08-20 #210 · row `W-66` | ⬛ **AWAITING DAVE'S EYE — BOTH VALUES ARE PROPOSED, NEITHER IS RULED.** ★ **The Time-picker diagnosis inverted the complaint:** the gap UNDER the red stroke was never the defect — 8.00 px to the message, 12.00 px to the triangle, clip test 0.00 px overflow (**not clipped**), and 8 px is the whole Input-fields family value. The defect is ABOVE the field: the #209 leading-trim block ends a `<label>` box at the **alphabetic baseline**, so the y-descender of "Payment time" (3.00 px measured) hangs outside it and ate the 4 px margin — **optical clearance to the field border measured 1.00 px**. `.tp-lblrow` 4 px → **8 px** restores 5.00 px; value taken from `Secure-entry`'s `.se-lblrow{margin:0 0 8px}` in the same family. ⛔ **The trim block was NOT removed** (`Keep + canary`, probe `P-6`) — the fix is spacing *around* it. Data-grid's `.dgseg` measured **0.00 px** to the "Transactions" section label and gains `margin:0 0 28px`; **28 px = 1.75 rem is surveyed, not invented** — Split-button, Button and Toast all measure 28.00 px for the same control-row-above-a-section-label relationship. ✅ Gates after, verbatim: grid **PASS** (116 files) · snippets **100 / 0 failures** · a11y **100 / 0 failures** · descender-clip **PASS**. ⚠ **A green gate is not a green claim: none of the four could see either defect** — the grid gate passes 0 px and 4 px alike, and the descender-clip gate only fires on labels that *truncate*. Both were found by measuring rendered pixels. ⚠ **BOTH DEFECTS ARE CLASSES, REPORTED NOT SWEPT:** seven sibling field snippets (Date-picker, Date-range-picker, Amount-input, Textarea, Combobox, Multi-select, Tags-input) carry the identical `margin:0 0 4px` under a *lock-step* comment, so Time-picker is now out of step with its family; and `Table` (`.seg`→`.wrap`) and `Segmented-control` (`.seg md`→`.section-head-row`) both measure **0.00 px** too. ⛔ No colour value touched — the two-red law and the mono error ink camp are untouched. ⚠ `canon.css` **NOT regenerated**; measured at **1180 only** (the 480 leg not re-measured); **not committed, not pushed — CI has not seen this file**. |
| `reviews/REVIEW-210-meter-unification-v1.html` (the unified **METER** proposal — Progress-bar and Limits-meter rolled up into ONE component on Dave's own instruction; the specimen pane is the REAL new file `knowledge/snippets/Meter.reference.html`, and the two parents are iframed BESIDE it, unchanged, as proof) | 2026-08-20 #210 · row `W-68` | ⬛ **AWAITING DAVE — PROPOSED, AND THE FOLD IS NOT ENACTED.** ⛔ **Nothing on the page is ruled, recommended or preferred.** Built on his words, carried VERBATIM into the artefacts' own prose: *"if these are the base components we just have progress variants, also I would like the option of the bigger titles that are in the progress bar. Again what we have is a set of meter variants embedded in an organism, in this case a daily transfer limit lock-up."* The grammar is his: **base `.meter` molecule → variants (linear 4px · emphasis 8px · non-percentage scale · dial · the `progress`/`allowance` SEMANTIC markers, which paint nothing) → organism specimens** (the daily-transfer-limit lock-up, within-limit and at-limit, plus the three-meter payment-limits set). ⛔ **`Progress-bar.reference.html` · `progress-bar.meta.json` · `Limits-meter.reference.html` · `limits-meter.meta.json` are BYTE-UNTOUCHED and NOTHING RETIRES** — Progress-bar sha256[:16] **`255edfd60790390c`** and Limits-meter **`2d68517b6c64ecee`** are IDENTICAL to the figures `W-64`'s page recorded at its own build, i.e. proven against an independently-recorded number rather than asserted. **The fold is a later, separate act**, and *what* retires is question **Q5**, unanswered. ★ **NOTHING BAR- OR RING-SHAPED WAS RE-DRAWN (#202):** every variant that reproduces an existing form is COPIED — **30 declaration bodies diffed against source, 0 drift**; the leading-trim block is byte-identical to Command-palette line 36 (328 chars); the AUTO-TOKENS ramp and the theme blocks are verbatim. **The one new affordance is the title-size option**, drawn BOTH WAYS on the same meter in §B6 (`t-cm-legal` 12px vs `t-cm-label` 16px) because **which is the default is HIS**. ⚠ **DECLARED, NOT SMOOTHED:** the modifier's COLOUR leg is a **no-op inside the snippet** — `text/secondary` and `text/default` both resolve to `#1A1A1A` light / `#FFFFFF` dark in the local theme block copied from Limits-meter — so on the page the option reads as a **SIZE difference only** (measured: 12px vs 16px, same colour). ⛔ **No colour value authored, and no RAG colour on any track or fill in any variant or state, including at the ceiling** — `s151-D1` and `s149-D1` untouched. ⬛ **`role="meter"` MIRRORED FROM BOTH PARENTS AND NOT RULED** — search re-run at #210 (`s202-D3`): **0 of 101** snippets carry the role in markup, `_rulings.json` mentions *meter* **0** times; if he rules it, **THREE** files change together. ✅ Gates, verbatim: grid **PASS (117 files)** · snippets **101 / 0 failures** · a11y **101 / 0 failures** · descender-clip **PASS (117 files)** · type composites **1097, IDENTICAL before and after** (this file contributes zero) · `probe_meta_schema.py --check` **102 metas, 0 findings** · `_validate_binds_resolve.py` **1 failure — no `.cn-meter` block in canon.css, EXPECTED and DECLARED for a new unregistered snippet** (the #209 Lane A claim-12 precedent). ⚠ `_validate_kg.py` reports the two node registries drifted — the new meta adds itself to EXISTING `sources` lists, **no new node keys** — shared files, the conductor's serial step, **not regenerated here**. ✅ Render-verified in-sandbox, `goto file://` only, **light AND dark at 1180 and 480, screenshots looked at**: 12 meters / 12 tracks, every computed fill width equals its authored value (**62 · 62 · 28 · 72.5 · 31 · 64 · 64 · 64 · 100 · 0 · 64 · 83.56 %**), heights 4px and 8px only, fill/track `#1A1A1A` on `#F0F0F0` light and `#FFFFFF` on `#484848` dark, dial dasharray **264.5** / dashoffset **100.5** / stroke-width **8**, **zero horizontal overflow**; the HSBC face asserts against two controls (target **346.88** = `--uf` 346.88, DejaVu 375.39, nonexistent 301.07). ★ **A green gate is not a green claim — one defect was found BY LOOKING:** the §B6 annotation sat flush against the title it annotates because the borrowed caption rule carries `margin:0`; fixed with a NEW demo-chrome class, **the borrowed rule was not edited to fix a demo problem**. ⚠ The four-theme fill-on-track contrast figures are **CARRIED** from the two parents' declarations and were **NOT re-measured** at #210. ⚠ The `W-64` srcdoc mechanism is reused **unchanged and re-proven**: over `file://` a frame's `contentDocument` is `null` (measured), so dark re-serves the same file's bytes with exactly two substitutions — the `<body>` theme attribute and `href="../canon/type.css"` → `href="../knowledge/canon/type.css"` (#29); verified in dark that `--uf` resolves and all three panes paint. ⚠ Generated by a **NON-REPO** throwaway builder (`s191-D2`). ⚠ **Not committed, not pushed — CI has not seen these files.** |
| **The seven wave-4 surfaces, by PATH** — `showroom/{calendar,tree,cascader,splitter,qr-code,carousel,image-block}.html` (+ their `knowledge/snippets/*.reference.html` and metas); receipts `notes/_receipts/2026-08-20-210-wave4-lane{A,B,C}-*.md` | 2026-08-20 #210 · row `W-74` | ⬛ **AWAITING DAVE — THE HEAVY 7, BUILT PROPOSED-NOT-RULED.** ⚠ The qr-code lane's own open question is **dark-mode QR polarity** (does the inverted render still scan?) — a scanner-compatibility call, not a token call. 19 open questions across the lanes, in the receipts' `$decisionsForDave`. |
| **The eleven wave-5 Layer-2 P2 surfaces, by PATH** — `showroom/{app-shell-top-nav,app-shell-side-nav,app-shell-multi-column,template-dashboard,template-list-index,template-detail,template-create-edit,template-wizard,template-auth,page-header-lockup,filter-toolbar-bar}.html` (+ snippets + metas); receipts `notes/_receipts/2026-08-20-210-wave5-lane{A,B,C,D}-*.md` | 2026-08-20 #210 · row `W-79` | ⬛ **AWAITING DAVE — LAYER 2 OPENS: 3 SHELLS + 6 TEMPLATES + 2 LOCK-UPS, ALL PROPOSED.** ⬛ The **artefact-class convention** (reference-snippet grammar) is PROPOSED with them and is a separate call. |
| **The sixteen wave-6 Layer-2 P3 surfaces, by PATH** — `showroom/{app-shell-split,app-shell-focused,app-shell-nav-rail,app-shell-doormat,template-settings,template-empty,template-error,template-confirmation,template-report,section-heading-lockup,card-header-lockup,hero-variants,stats-band-lockup,footer-doormat-lockup,cta-lockup,feature-grid-lockup}.html` (+ snippets + metas); receipts `notes/_receipts/2026-08-20-210-wave6-lane{A,B,C,D}-*.md` | 2026-08-20 #210 · row `W-84` | ⬛ **AWAITING DAVE — 4 SHELLS + 5 TEMPLATES + 7 LOCK-UPS. LAYER 2 IS COMPLETE EXCEPT ROW 124** (the variant matrices), which is deferred to him by name. |
| **The three #212 triage control pages** — `reviews/REVIEW-212-boot-and-desk-triage-v1.html` (boot passengers + the desk as ONE strike-list) · `reviews/REVIEW-212-dials-v1.html` (the 14 dials, seen before ratified) · `reviews/REVIEW-212-rule-now-v1.html` (every open call, visual where it can be) | 2026-08-21 #212 · row `W-95` | ✅ **LARGELY CONSUMED — NOT AWAITING.** Dave ruled off all three the same session: **twelve rulings, `knowledge/_rulings.json` § `s212-D1`…`s212-D12`**, closing/parking `G1` `G2` `G5` `G6` `G9` `G10` `G11` `G12` `G13b` `G14` `G17` and `W-14` `W-15` `W-16` `W-0c` `W-0d`. ⛔ **They are listed here because ONE part is still genuinely his and cannot be delegated: the connector/skill strike-list on the boot-and-desk page needs HIS UI clicks** — no agent can strike a connector or a skill. ⬛ Also still open off these pages: **`P8`** (the routing conflict, three readings, deliberately unruled at `s212-D12`) · **`W-99d`** (the `bill_of()` 1.57× repair + the re-PICK of `SIZE_BUDGET` 8000 / `TITLE_CAP_TAPE` 120) · **`W-96`** build-or-park · **`W-98`** dv-lockup titles · **`W-99c`** the G15+G16 donut sitting. ⚠ Generated in-session by the conductor; **CI has not seen these files** until this wrap's commit. |
| **The #213 four-theme wave review** — `reviews/REVIEW-213-wave-components-four-theme-v1.html` (the 43 wave-3…6 components × 4 themes × light+dark, **172 questions + 5 rule-once panels**, each component iframing its OWN showroom page; generator `knowledge/_render/gen_review_213_wave_components.py`; receipt `notes/_receipts/2026-08-21-213-laneR-review-surface.md`) | 2026-08-21 #213 · row `W-99i` | ⬛ **AWAITING DAVE — AND HIS PLAN IS TRANCHES, IN HIS OWN WORDS:** *"I'll tackle this in tranches over the next few days so we can return to it"*. ⚠ **28 of the 43 metas carry no `$decisionsForDave`**, so those questions live only in receipt prose — named, not repaired. |
| **LIBRARY v2** — `reviews/LIBRARY-2026-08-21-v2.html` (generator `knowledge/_render/gen_library_214.py`; `gen_showroom.py` gains a `#chrome=0` embed mode as an **opt-OUT** so REVIEW-213 keeps its pins, guarded by a selftest bite; receipt `notes/_receipts/2026-08-21-214-library-interface-v2.md`) | 2026-08-21 #214 · row `W-99zg` | ⬛ **AWAITING DAVE'S EYE — AND TWO CALLS COME WITH IT.** True header (theme · dark · width), search + **68 aliases** + cmd-K, level facet chips at a config swap point, **135 components**, driven headless **24/25**. ⛔ **The two questions are HIS:** does v2 **REPLACE** `showroom/index.html`, and **which level word-set** fills the `LEVELS` config — three candidates are commented beside it and `W-99zd`'s research says the trade-offs are even. ⚠ **Findings from the driving, named not repaired:** side-nav `aria-current` is **authored static** (the rail toggle works, destination selection does not) and **46 of 135 snippets ship no behaviour JS** (sidebar-nav 0, command-palette 0, all lock-ups), so Replay is disabled with the reason stated on the page. ⚠ Library chrome is **light-only**, as v1. ⚠ **Not wired into `_build_all.py`.** ⛔ Nothing on the page is ruled. |
| **The itinerary v3 snapshot** — `reviews/ITINERARY-STATUS-2026-08-21-v3.html` (+ sidecar `-v3.json`); generator fix + receipt `notes/_receipts/2026-08-21-214-itinerary-generator-fix.md`, commit `7f32e34` | 2026-08-21 #214 · row `W-99zb` | ⬛ **AWAITING DAVE ON TWO CALLS THE FIX SURFACED.** The Layer-2 shortcut is repaired at cause — `resolve()` no longer early-returns rows **97–124**, `LAYER2_NOTE` is now GENERATED from measurement, and **orphans fell 28 → 1** with GATED **94 → 121**. ⛔ **His two calls:** whether **row 124** (per-component variant matrices, an honest GAP) joins the true-gap list, and the **27 Layer-2 snippets** now visible as off the radius-ratchet (advisory **26 → 53**). ⚠ The receipt **corrects #213's snippet count 20 → 27**; the `status-indicator` 67→86 canon jump is **observed, not diagnosed**. ⚠ v1/v2 are **WRITE-ONCE and untouched**. |
| **The #214 descender pair** — audit `notes/_receipts/2026-08-21-214-descender-cascade-audit.md` + build/repair `notes/_receipts/2026-08-21-214-descender-gate-and-repair.md`; commits `0360a88` · `4ad4cef` | 2026-08-21 #214 · rows `W-99ze` / `W-99zf` | ⬛ **AWAITING DAVE ON THE `canon.css` 48 — THE REPAIR ITSELF IS DONE AND GREEN.** The audit inverted the suspicion: **`ds-005` is NOT regressed and the components are fully tokenised — the failure was SPECIFICITY**, 18 dead overrides across 9 snippets and 6 review components, invisible because the gate compared authored selector STRINGS rather than computed edges. G1 (the specificity leg) is **BUILT and catches 18/18**; the **18 selectors are REPAIRED on his nod** (including `Sidebar-nav`, which `ds-005`'s own fence guards against "fixing"); full gate **GREEN across 151 files**, showrooms regenerated after the repair, sync + descender gates green cold. ⛔ **HIS CALL:** `canon.css` carries **48 of its own** via a `.cn-` absorb prefixer that is **not specificity-preserving** — it inflates trim `(0,1,2)→(0,3,2)` against an override's `+1 class`, so **a repair that WINS in a snippet LOSES in canon**; held as a **shrink-only ratchet at 48**, and `canon.css:9316` already says the cross-file remedy is his. ⚠ **G2, the `--computed` render leg, is PRICED-NOT-BUILT (~3h/~45K) and it is the only thing that would prove the FEATURE rather than the clause.** ⚠ **`ds-005` has no `_rulings.json` row** — it lives only in `_DS-IMPROVEMENTS.md` prose, so a retrieval-by-default query answers "never decided"; retro-inscription is a promotion and promotions are his. |
| **The REVIEW-213 tranche map + the taxonomy research** — `notes/_briefs/2026-08-21-214-review-213-tranche-map-v1.md` · `notes/_briefs/2026-08-21-214-taxonomy-research-v1.md` | 2026-08-21 #214 · rows `W-99zc` / `W-99zd` | ⬛ **AWAITING DAVE — THESE ARE THE PLAN FOR HIS OWN STATED PLAN.** He said he would take REVIEW-213 *"in tranches over the next few days"*; the map proposes **6 sittings of ~20–35 questions each** over the 172, confirms the **statement family at 5 members** (incl. Template-list-index) and measures **lock-ups as NOT a family (14.8%)**. The research finds the industry consensus is **Foundations / Tokens / Components / Patterns — NOT atomic-as-navigation**, that *"lock-up"* is **a coinage** (the industry word is *block*), and that **aliases + facets + search beat taxonomy for findability**. ⛔ **Adopting, amending or discarding the plan is his, and the level word-set pick is his** — it can ride a tranche sitting. Nothing here is ruled. |
| **THE COMPONENT LIBRARY, WHICH IS NOW THE INDEX** — `showroom/index.html` (+ `showroom/index.json`, 135 rows; `showroom/_thumbs/*.png`, 135 screenshots); generator `knowledge/_render/gen_library_214.py`, thumbnails `knowledge/_render/gen_thumbs.py`, drive `knowledge/_render/drive_library_215.py`; receipts `notes/_receipts/2026-08-22-215-library-v2-ia.md` + `-library-swiss-restyle.md`; commits `46eb387` · `1b08f68` | 2026-08-22 #215 · rows `W-102` / `W-103` / `W-105` · **BUILT UNDER — `s215-D4` / `s215-D5`** | ⬛ **AWAITING DAVE'S EYE — THE RULINGS ARE ENACTED, THE RESULT IS NOT SIGNED OFF.** ⛔ **#214's LIBRARY-v2 row above is SUPERSEDED for its two open calls: both were RULED at #215** — v2 REPLACES `showroom/index.html` (`s215-D5`(1)) and the level word-set is the ruled ladder (`s215-D4`). What is NEW and awaiting him is everything the enactment PROPOSED beneath the rulings: **16 usage-group assignments** and the **Charts → Data-display fold** (IA receipt §4) · the **status derivation rule** (§6) · **grey-4 decorative numerals measured at 2.01:1** · **related-cluster membership** · the **empty Foundations and Tokens tiers** (ruled as tiers, populated by nothing yet). ⚠ The Swiss restyle carries **two declared project substitutions over the skill contract** — accent `#DA1A00` not the skill's `#DB0011` (`s151-D1`, the two-red law) and ink `#1A1A1A` not `#000000` — **selftest bite 25 proves neither skill value appears in the page**; both are standing law, neither is a preference. ⚠ **46 of 135 snippets still ship no behaviour JS** and side-nav `aria-current` is authored static — carried, not repaired. ⛔ Nothing on the page beyond the two rulings is ruled. |
| **The taxonomy research v2** — `notes/_briefs/2026-08-22-215-taxonomy-research-v2.md` (+ the six real nav renders at `notes/_briefs/assets-215-nav/`) | 2026-08-22 #215 · rows `W-104` / `W-106` | ⬛ **AWAITING DAVE — 12 PROPOSED-FOR-DAVE ROWS OVER 41 COMPONENTS, §2.** The Carbon deep-dive, the 135-component ladder map and the lifecycle vocabulary sit behind them. ⚠ The six nav screenshots are **real headless renders** (Carbon ×2, Material, Atlassian, GOV.UK, shadcn) and are rowed by **DIRECTORY ADDRESS** (`W-106`, `home` ending in `/`) — the first use of that class fix; ⚠ **re-shooting is NOT guaranteed**, the capture depended on a foreign session's chromium libs at `/var/tmp/chromelibs-s213e2` and the four environmental obstacles are declared in §5.1 of the doc. ⛔ Nothing here is ruled. |
| **The bento + photography-gallery exploration** — `knowledge/_fitness-test/bento-gallery-showcase-v1.html` (+ 14 locally generated placeholder photographs and their deterministic generator at `knowledge/_fitness-test/assets-bento-gallery/`; render/drive probe `knowledge/_render/verify_bento_gallery_v1.py`) | 2026-08-22 #216 · **no itinerary row — raised in-session as an exploration, not off a wave** | ⬛ **AWAITING DAVE'S EYE — TWO LAYOUTS THE LIBRARY DOES NOT HAVE, BOTH PROPOSED, NOTHING RULED.** A CSS-Grid-only **bento** (repeating columns · `grid-auto-rows` · four span utilities · `grid-auto-flow:dense` as a toggle, never a default) and a **photography gallery** in both honest modes — CSS `columns` masonry and a uniform grid with `object-fit:cover` — with a **lightbox that contains no JavaScript at all** (Popover API driven by `command`/`commandfor`; no `:target`, no checkbox hack). ⛔ **No colour value is authored anywhere in the file** (proven: the only two hex strings in it are inside prose quoting a measurement) — the two-red law `s151-D1` and the mono error ink camp `s149-D1` are untouched, and **no red and no green appears on the page**, including in the placeholder photographs, whose palette is cool-only by construction. ⛔ **Radius is canon's per-theme value, never authored** — and the measurement **corrected the build's own first draft of the prose**: **Console is the ONLY rounded theme** (surface `20px`, control `8px`, `s199-D3`); Mono, Legacy **and Supercharge** all resolve to `0`. ★ **ONE TOKEN FINDING, SURFACED AS A CONTROL RATHER THAN WORKED AROUND:** `surface/raised` and `tertiary/background/default` are **identical to `background/default` in light in all four themes** (`#FFFFFF` on `#FFFFFF` under Mono/Legacy/Console, `#F7F6F4` on `#F7F6F4` under Supercharge), so a card painted with the semantically correct token is invisible except for its hairline; only `surface/subtle` differs. A **Tile surface** control offers all three, the measurement is printed on the page's own face, and **the starting position (Subtle) is declared as legibility-only, not a recommendation** — which token a card should use is Dave's. ⬛ **Six open decisions are wired as live controls, none defaulted as a proposal:** bento columns / gap / row-height / span preset (including a **Flat (no spans)** control so *"no bento"* stays an available answer) · dense on-off · gallery mode · aspect ratio (1:1 / 4:3 / 3:2 / free) · tile surface · stage width. ⚠ **TWO TRADE-OFFS ARE SHOWN, DELIBERATELY NOT RESOLVED:** `columns` masonry and `grid-auto-flow:dense` both part visual order from DOM/reading order — an **Order tracer** control numbers both sections in source order so the divergence is visible rather than described; and `object-fit:cover` crops, measured on the page's own photographs (**at 1:1 the 5:2 panorama keeps 40.0% of its frame; median kept 66.7%**). ⛔ **Native CSS masonry (Grid Lanes) is NOT a dependency** — `CSS.supports('grid-template-rows','masonry')` is measured live in the reading browser and reported in a badge; it read **false** in Chrome 151. ✅ **Render-verified in-sandbox, screenshots looked at — 11 shots:** all four themes light, Mono dark, Supercharge dark (masonry), Mono light (grid at 1:1), narrow **420px viewport**, the **380px stage harness**, and the **lightbox open in two themes, opened by CLICKING the `commandfor` button** (not by calling `showPopover()`) — with a **negative control**: a dangling `commandfor` opens **0** popovers. Font asserts against two controls (target **346.88** = `--uf` 346.88 · DejaVu 375.39 · nonexistent 301.07). **Every page-local custom property resolves non-empty across 4 themes × light/dark** (15 properties × 8 = 120 checks, 0 empty) — the dangling-var silent-black class cannot bite here. Horizontal overflow **0px** in both gallery modes. ✅ Gates run on the file: **descender-clip PASS** and **type-composite PASS** (the file authors **zero** `font-size` and **zero** `font-weight`); repo-wide type debt **1097, UNCHANGED** — this file contributes **0**. ★ **A green gate is not a green claim, and two defects were caught by instruments rather than by looking:** the repo's own `_validate_descender_clip.py` found `.bx-rows .bx-r-name` slicing its descenders and **prescribed the exact `text-box-edge:text text` remedy**, which was applied; and the runbook's **two-control canvas font probe** caught a **broken font farm** (relative symlinks) that `document.fonts.check()` could not have detected. ⚠ **`_validate_a11y.py` ignores a path argument and ran repo-wide (135 snippets, 0 failures) — it did NOT assess this file. UNPROVEN by scope, declared.** ⚠ The page is wired into **no build step and no CI job**, so its own capability badges and controls run only when a human opens it [[instrument-without-a-consumer]]. ⚠ **Not committed, not pushed — CI has not seen these files.** ⛔ Nothing on this page is ruled, recommended or preferred.<br><br>**➤ v2 — `knowledge/_fitness-test/bento-gallery-showcase-v2.html` (+ probe `knowledge/_render/verify_bento_gallery_v2.py`), 2026-08-22 #216. SUPERSEDE-FORWARD: v1 above stands and is BYTE-UNTOUCHED; v2 is v1 plus ONE new section and nothing was removed.** ⬛ **AWAITING DAVE'S EYE — A THIRD LAYOUT, PROPOSED, NOTHING RULED.** **"3 · Full-scale bento"**, modelled on the Bolt.com-style reference Dave saw: a full-width composition on a **12-column CSS Grid** whose rows alternate **7/5 · 5/7 · 7/5**, each tile carrying an eyebrow, a `t-ed-display-2` heading, one line of copy, a **"More about →"** text link and an illustration. Pure CSS — the only script on the page is still the review chrome, and deleting it leaves the section fully working. ⛔ **No colour value authored: the page's own `<style>` contains ZERO hex literals, proven mechanically** (`NO-AUTHORED-COLOUR` probe) — the two-red law `s151-D1` and the mono error ink camp `s149-D1` are untouched. ⛔ **Radius is canon's per-theme value, never authored** (Console 20px surface / 8px control; Mono, Legacy and Supercharge 0 — seen in the renders). **RESPONSIVENESS IS A CONTAINER QUERY, AND THE CHOICE IS FORCED, NOT PREFERRED:** the page ships a Stage-width harness (Full/900/640/380), and a `@media` query **cannot see it** — at stage 380 on a 1400px screen every viewport breakpoint still reads 1400. Driven and measured: **full 7/5 asymmetry at Full · 2-up EQUAL at 900 · single column at 640**, horizontal overflow **0px** at every step, plus a **negative control** — killing `container-type` on the stage returns the layout to 2,2,2 (no collapse), restoring it returns single-column, so the query is proven load-bearing rather than decorative. A **SECOND, NESTED** container (`fstile`) lets each tile decide its own interior from **its own content box**, which is why in one row the 7-span tile keeps art beside text (content 696.7px) while the 5-span tile stacks it (content 467.3px) — measured, and a viewport query cannot express it at all. ⬛ **Three open decisions wired live, none defaulted as a recommendation:** **split ratio 7/5 · 8/4 · 6/6** (6/6 is on the control precisely so *"don't be asymmetric"* stays a pickable answer; 7/5 is a starting position because it is the reference's shape) · **illustration-side alternation on/off** · **tile surface**, which is **the SAME open token question section 1 raises, not a second one** — the v1 finding is carried and restated on the section's own face, and the three collapse thresholds (1000 / 640 / 520) are named on the page as proposed and his to move. ★ **THE SECTION HAS NO READING-ORDER DIVERGENCE TO DISCLOSE, and that is measured not asserted** — unlike section 1's dense packing and section 2's `columns` masonry, alternation is an `order` swap only; visual order read left-to-right, row by row, is `[1,2,3,4,5,6]` in both alternation states, and the Order tracer now covers this section too. ✅ **Verification — `verify_bento_gallery_v2.py`, three probe stages, ALL PASS:** var resolution extended to the `.fs` block over **4 themes × light/dark** (20 properties × 8 = 160 checks, 0 empty; plus a per-theme assert that a tile's heading and link ink are never equal to its own surface) · the split ratio **measured off rendered boxes** against the control's claim (7/5 → 1.400, 8/4 → 2.000, 6/6 → 1.000, all exact) · the two stage collapses + the negative control · the nested tile query · the palette leg · the v1 legs all still green (font target **346.88** = `--uf`, DejaVu 375.39, nonexistent 301.07; lightbox opened and closed **by clicking the `commandfor` invoker**; dangling `commandfor` opens **0** popovers). ✅ Gates: **descender-clip PASS (151 files)**; **repo type debt 1097, IDENTICAL to v1's figure — v2 contributes 0** (the file authors zero `font-size` and zero `font-weight`; the only three occurrences of those strings are inside prose). ✅ **Render-verified in-sandbox, 7 shots taken and 5 LOOKED AT:** Mono light, Console light (the rounded theme), Legacy **dark**, the 900 mid-collapse, the 380 narrow collapse, plus a real **430px viewport** — the composition, the collapses and the dark legs are all as measured. ★★ **TWO DEFECTS FOUND BY INSTRUMENTS THAT WOULD HAVE SHIPPED SILENTLY, both fixed at cause:** ⑴ the first draft wrote `gap:var(--fs-gap,24px)` with **no declaration of `--fs-gap`** — it RENDERED CORRECTLY because the fallback fired, while the property resolved **EMPTY in all four themes**, which is indistinguishable from a dangling var to any instrument and silently becomes the shipped value the moment anyone assumes the token exists; the extended var sweep caught it. ⑵ ⛔ **A MEASURED PLATFORM FACT, BANKED IN THE FILE:** a size container's **own** `grid-template-columns` is **not re-styled by its own container query** — inside the *same* `@container` block the descendant rules applied (`aspect-ratio` computed `16/9`, `order` computed `2`) while the container's own track list still computed **two tracks**. The rule was present, it matched, and it did nothing. Fixed by moving the change onto the CHILDREN (`grid-column:1/-1`). ⛔ **A THIRD FINDING CORRECTS THE v1 ROW ABOVE, AND IT IS A FALSE CLAIM NOT A TYPO:** v1's row states *"no red and no green appears on the page, including in the placeholder photographs, whose palette is cool-only by construction"*. **That is not true.** `_gen-photos.py`'s own palette comment lists **GREENS** among its duotones, and **three of the fourteen placeholders — `p02-ridge`, `p07-canopy`, `p10-estuary` — sit in the green hue band** (`#8CC7B4`, `#A3C69B`, `#8CC7B4`; every hex converted to HLS). Three of them were the first pictures this section reached for; **caught by LOOKING at the render**, then confirmed numerically. The full-scale section is swapped to the green-free six, a **`PALETTE` probe leg now classifies every hex in every image the section references by hue angle**, and the correction is printed on the page's own face. ⚠ **REPORTED, NOT SWEPT: the three green images are still in use by section 2 and by v1** — changing what Dave has already been shown is a decision, not a tidy-up, and it is his. ⚠ **UNPROVEN, DECLARED:** `_validate_a11y.py` still ignores a path argument (v1's declared gap, unchanged — it did NOT assess this file); the new section's **contrast was not measured** (no colour was authored, but no ratio was computed either); the **1000 / 640 / 520 thresholds are proposed values, not surveyed ones**; and the `t-ed-display-2` heading at the 380 stage wraps to three lines — observed and left alone, because narrowing it would mean authoring a font size. ⚠ The page remains wired into **no build step and no CI job**. ⚠ **Not committed, not pushed — CI has not seen these files.** ⛔ **Nothing in section 3 is ruled, recommended or preferred.** <br><br>**➤ v3 — `knowledge/_fitness-test/bento-gallery-showcase-v3.html` (+ probe `knowledge/_render/verify_bento_gallery_v3.py`), 2026-08-22 #216. SUPERSEDE-FORWARD: v1 and v2 above stand and are BYTE-UNTOUCHED; v3 is v2 plus ONE new section and nothing was removed.** ⬛ **AWAITING DAVE'S EYE — A FOURTH SECTION, PROPOSED, NOTHING RULED.** **"4 · Bento designer"**, built after Dave saw **nev.club**'s *designed-not-generated* grid: **BOTH routes to that feel, governed by one 12-column grid.** **A · SNAP DESIGNER** — an authoring canvas on the 12-col lattice: drag on empty lattice to **draw** a tile, drag a tile to **move** it, drag any of eight corner/edge handles to **resize** it, a per-tile chip to **cycle its kind** (text / photo / stat, the v2 tile kinds) and one to **delete**. **B · PARAMETRIC SHUFFLE** — a **seeded** generator: tile count, hero/medium/small weights, hero on-off, fill-holes on-off, row unit, seed field + reroll. **C · EXPORT** — one control serving both modes, minting **(1)** a static **CSS+HTML recipe** with every span written out as an integer and **zero JavaScript**, and **(2)** a **JSON recipe** `{grid:12,rowUnit,tiles:[{col,row,colSpan,rowSpan,kind}]}`, plus a **re-import** field that round-trips a recipe back into the designer. ⛔ **THE TOOL HAS SCRIPT; THE ARTEFACT HAS NONE.** Sections 1–3 remain script-free and untouched — v3 adds a SECOND `<script>` block that is the authoring tool only. `s200-D1` is stated on the page's own face: **the generator mints concrete values; a component page would ship the minted CSS, never live randomness** ("a slot machine cannot be reviewed, screenshotted, regression-tested or signed off"). Proven mechanically: the exported recipe is **script-free, `random`-free, 0 hex literals** (colour is Apollo tokens only) with **one concrete span rule per tile**. ⛔ **SNAP IS THE LAW OF THE TOOL, and it is DRIVEN not asserted** — no free-pixel placement exists; every pointer position resolves to whole columns and whole row units before it reaches the model, and the model holds no pixels at all. ⛔ **12 COLUMNS ALWAYS** — declared on the page: the canvas does NOT re-column when the stage narrows (that would silently change what a recipe means); what the container query shrinks is the **row unit and gap**, and the **minted recipe carries its own `@media (max-width:640px)` full-width collapse**, which is the honest home for that decision. Measured: 12 columns at Full **and** at stage 380, row `96px→52px`, gap `12px→4px`, overflow **0px**. ⛔ **No colour authored: the page's own `<style>` still contains ZERO hex literals** (`NO-AUTHORED-COLOUR` probe) — two-red law `s151-D1` and mono error ink camp `s149-D1` untouched, **no red and no green anywhere including the placeholder art**: section 4 can only reach the **green-free six**, and the palette probe now reads the photo list off the **script's own array** rather than off what happens to be on screen, so a green image one kind-cycle away would still be caught (`p02-ridge`, `p07-canopy`, `p10-estuary` remain fenced out). ✅ **Verification — `verify_bento_gallery_v3.py`, FIVE probe stages, ALL PASS.** New legs: **DESIGNER DRIVEN WITH SYNTHETIC POINTER EVENTS** — a drag from cell (2,1) to (5,2) mints exactly `col 2 / span 4, row 1 / span 2`; a move gives `col 4, row 2`; an **SE-handle drag** to (9,4) gives `span 6 × span 3`; **keyboard** arrow-move → col 5, shift-arrow-resize → rowSpan 4, `K` cycles kind, `Delete` empties the canvas; **negative control** — a **3px** drag still mints a whole `1×1` at (7,3), which is where a leaked pixel would show. **GENERATOR DETERMINISM AS BYTE EQUALITY** — seed 2026 vs seed 7 differ; seed 2026 **revisited after seed 7** is **byte-identical in both JSON and CSS**; at most **1 hero** in an 11-tile layout; **0 overlapping cells**, all tiles inside 12 columns. **ROUND-TRIP** — generated → JSON → designer → JSON is **byte-equal (846 bytes)**, CSS byte-equal too; **importer negative control**: a `grid:8` recipe is REFUSED with a named message and leaves the standing tiles alone. Var sweep extended to the `.dz` block over **4 themes × light/dark** (24 properties × 8, **0 empty**) with new per-theme silent-black asserts — lattice line ≠ page, ghost ≠ page, handle ≠ tile, textarea ink ≠ textarea field. All v1 and v2 legs still green (font target **346.88** = `--uf`, DejaVu 375.39, nonexistent 301.07; split ratios 1.400 / 2.000 / 1.000 exact; the two stage collapses + `container-type` negative control; the nested tile query; lightbox opened and closed by clicking the `commandfor` invoker; dangling `commandfor` opens **0** popovers; overflow **0px** everywhere). ★★ **TWO DEFECTS FOUND BY DRIVING, BOTH FIXED AT CAUSE, BOTH BANKED IN THE FILE:** ① **A CLIPPED HIT BOX MADE THE TOOL DO THE WRONG VERB.** The resize handles were first hung half-outside the tile in the usual design-tool idiom; the tile carries `overflow:hidden` (a photo tile must clip its image to the corner radius), which clips **hit-testing as well as painting**. The handle still looked grabbable, `getBoundingClientRect` still reported the full box — and the probe's resize drag silently performed a **MOVE**. Handles are inset now. A screenshot would never have caught it; only driving it did. ② **THE OBVIOUS FILL-HOLES ASSERTION IS FALSE, AND IT WENT RED BEFORE IT WAS WEAKENED.** *"dense must be at least as dense as sparse at every seed"* is **not a theorem** — greedy first-fit is not optimal and back-filling an early hole can leave the tail worse. It failed at **seed 2026 (ON 0.720 vs OFF 0.792)**: **the page was right and the assertion was wrong.** What is asserted instead is the **mean over a fixed 8-seed set (ON 0.792 vs OFF 0.768)** plus *the toggle changed the layout at 5/8 seeds*, and **the page's own face now carries the caveat with the numbers in it**. ⚠ A third, smaller one, also banked: the sliders write `--dz-row-base` and the stylesheet derives `--dz-row` from it, because an **inline** `--dz-row` would beat the `@container` rule and leave the responsive collapse as dead code that still looked present — the same species as v2's banked container-own-property fact. ✅ **Render-verified in-sandbox, 9 shots taken and 8 LOOKED AT:** the designer with 5 tiles **drawn by the mouse** in Mono light, Console light (the rounded theme) and Supercharge **dark**; the generator at **two seeds** (element shots, so a tall wall is not silently cropped) and in Legacy **dark**; the **900 mid** and **380 narrow** stage collapses; and the **export panel** with both recipes minted. ★ **One thing was changed BECAUSE of a render, not a number:** eight solid ink handles per tile turned the canvas into a field of dots and buried the layout being judged — they are now held at low opacity until a tile is hovered or focused. ⬛ **Four more open decisions wired, none defaulted as a recommendation:** which of the two routes the library should offer (or neither) · the generator's default distribution (hero 3 / medium 3 / small 7, hero on, fill on, 11 tiles, seed 2026 — a legibility starting position, stated as such) · the **seven-shape size alphabet** (hero 8×2; mediums 6×2, 4×2, 6×1; smalls 3×1, 4×1, 3×2) and whether the library should have **one span vocabulary or two** (section 1's four utilities on 4 columns vs this on 12) · and whether a minted bento is how **"related components"** should appear on showroom pages at all. ⛔ **THE RELATED-CLUSTER QUESTION IS REFERENCED AND NOT ANSWERED.** The intended library use — a minted *related components* bento at the foot of each showroom component page — is annotated as context only; **which components belong in which cluster is ALREADY open in the component-library row (#215) above**, and this page states on its face that it does not answer it, does not propose an answer, and **`showroom/` was not touched**. ⚠ **UNPROVEN, DECLARED:** **keyboard cannot DRAW a new tile** — there is no keyboard equivalent of drag-to-draw; *Start me off*, *Clear* and the generator are the only keyboard routes to a new tile, and the gap is named on the page rather than designed around. The **per-tile chips are hidden below a 460px stage** (they cannot fit a 52px row) — `K` still works, but pointer-only users lose the kind cycler there. **Neither the descender-clip nor the type-composite gate reaches `knowledge/_fitness-test/` at all** (their globs are `canon.css` + `snippets/*.html` + `_proforma/*.html`) — both were run repo-wide and are green/unchanged (**descender PASS 151 files; type debt 1097, IDENTICAL to v2's figure, so v3 contributes 0** — the four `font-size`/`font-weight` strings in the file are all inside prose), but **neither assessed this file**; the conventions are honoured, not leaned on. `_validate_a11y.py` still ignores a path argument (v1/v2's declared gap, unchanged). **No contrast ratio was computed** for the new section. **The designer has no undo, no multi-select, no snapping to other tiles' edges, and tiles may overlap if you drag them onto each other** — the tool does not prevent it and the export will faithfully record it. ⚠ The page remains wired into **no build step and no CI job**. ⚠ **Not committed, not pushed — CI has not seen these files.** ⛔ **Nothing in section 4 is ruled, recommended or preferred.** <br><br>**➤ v4 — `knowledge/_fitness-test/bento-gallery-showcase-v4.html` (+ probe `knowledge/_render/verify_bento_gallery_v4.py`), 2026-08-22 #216. SUPERSEDE-FORWARD: v1, v2 and v3 above stand and are BYTE-UNTOUCHED; v4 is v3 plus ONE addition and nothing was removed.** ⬛ **AWAITING DAVE'S EYE — A SCALED BENTO USED AS THE GALLERY'S HEADER, PROPOSED, NOTHING RULED.** Dave: *"the bento is great for the photo library btw, '2 · Photography gallery' version, could you stick a scaled bento as the header"*. Section 2 now opens with a **masthead**: section 3's **twelve-column idiom scaled down** — a fixed row unit, a composition exactly **two row units tall**, four photo tiles (`hero` 6×2, `tall` 3×2, two `unit` 3×1) with `object-fit:cover`, canon radius, canon hairline, and an **existing spacing token for the gap** (`--sp-3`, `--sp-2` when narrow). It sits above the section's own mode controls, inside the SAME `.rv-stage`, so the Stage-width harness governs it. ⛔ **Nothing structural was invented** — same 12 columns, same whole-column span vocabulary expressed as a `data-span` attribute, same `@container stage` responsiveness, same tokens; what changes is scale. ⛔ **GREEN BAND FENCED, AND MEASURED NOT ASSERTED:** the masthead uses `p01-harbour`, `p13-portico`, `p08-tideline`, `p12-dunes`; `p02-ridge`, `p07-canopy` and `p10-estuary` do not appear, and the **v2/v3 `PALETTE` leg now reads `#glHead`'s `src` attributes off the RENDERED page** and classifies every hex in every one of those files by hue angle — **7 images swept, all `cool only`**. ⛔ **No colour authored: the page's own `<style>` still contains ZERO hex literals** (`NO-AUTHORED-COLOUR` probe) — two-red law `s151-D1` and mono error ink camp `s149-D1` untouched. ✅ **Verification — `verify_bento_gallery_v4.py`, SIX probe stages, ALL PASS.** New masthead leg, every number read off the rendered box: **spans measured** (hero 670.0 · tall 329.0 · unit 329.0 at gap 12 → hero/unit = **2.000** exact) · **two row units tall** (row unit 104px, measured height **220.0** = 2 units + gap, hero the same) · **`object-fit:cover` on every tile with the crop as a number** (harbour keeps 48.9%, portico 50.0%, tideline 78.0%, dunes 46.8%) · **both container collapses driven through the page's OWN Stage control** — 12-col `perRow [3,1]` at Full and at 900 (row unit 104→88), **six columns `[1,3]` at 640** (row unit 80), **three columns `[1,3]` at 380** (row unit 72), horizontal overflow **0px** at every step · plus a **negative control**: killing `container-type` at stage 380 returns `[3,1]` (no collapse) and restoring it returns `[1,3]`, so the query is load-bearing. Var sweep extended to the `.gh` block over **4 themes × light/dark** (`--gh-gap`, `--gh-row`, `--gh-c`, `--gh-r`, **0 empty**) with two new per-theme silent-black asserts — a tile hairline equal to its own surface, and a tile shorter than one row unit (which is what a dangling `--gh-row` would look like). All v1–v3 legs still green (font target **346.88** = `--uf`, DejaVu 375.39, nonexistent 301.07; split ratios 1.400 / 2.000 / 1.000 exact; nested tile query; designer drags, keyboard and round-trip; lightbox by `commandfor`; dangling `commandfor` opens **0** popovers; overflow **0px**). ✅ **Render-verified in-sandbox, 4 shots taken and 3 LOOKED AT:** Mono **light**, Console **dark** (the rounded theme — the 20px corners are visible on the masthead tiles), and the **380 stage collapse**. ★ **One change was made BECAUSE of a render, not a number:** the `tall` tile was first `p09-stairwell`, which crops to a near-copy of the hero's blue horizon and made the masthead read as one picture twice; it is `p13-portico` now, which carries its own shapes. ⬛ **TWO OPEN DECISIONS, LABELLED UNRULED ON THE PAGE'S OWN FACE, not defaulted as proposals:** **hero-LEFT** (the mirror is the same spans and equally available — picked so there is something to look at) and the **two-row-unit height** (the row unit is an **authored pixel value**: no spacing token is the right size for a photographic band, and how tall a masthead should be is a judgement about the pictures). The probe reports the hero side as `INFO`, never as a pass condition. ⚠ **UNPROVEN, DECLARED:** the masthead images are `alt=""` (**decorative** — every one appears again in the gallery below with a real description), which is a content judgement that was **made, not measured**; **no contrast ratio was computed** (no colour was authored); the **104 / 88 / 80 / 72 row units and the 640 / 380 thresholds are proposed values, not surveyed ones**; the masthead is **not keyboard-reachable and not clickable** — it is a header, not a second gallery, and no lightbox was wired to it; and neither the descender-clip nor the type-composite gate reaches `knowledge/_fitness-test/` (v3's declared gap, unchanged), while `_validate_a11y.py` still ignores a path argument. ⚠ The page remains wired into **no build step and no CI job**. ⚠ **Not committed, not pushed — CI has not seen these files.** ⛔ **Nothing in the masthead is ruled, recommended or preferred.** |
| **The Nio dashboard recreated in Apollo Console** — `knowledge/_fitness-test/nio-dash-console-v1.canon.html` (+ its mapping table `knowledge/_fitness-test/nio-dash-console-v1.mapping.md` and its generator `knowledge/_render/gen_nio_dash.py`); reference `Nio-Dash.png` at the repo root | 2026-08-22 #216 · **no itinerary row — raised in-session** | ⬛ **AWAITING DAVE'S EYE — A WHOLE SCREEN COMPOSED FROM THE LIBRARY, NOTHING RULED.** Built on Dave's constraint, verbatim: *"recreate this using console components, don't invent anything apart from the header and footer."* ★ **In the event NOTHING was newly authored — both permitted inventions were spent on nothing:** the library already carries `App-shell-top-nav` (the masthead) and `Footer` (the slim legal band), so the header and footer are composed like everything else. **40 mapped regions: 24 EXACT · 11 CLOSEST · 5 GAP**, itemised in the sibling `.mapping.md`. ⛔ **ZERO authored colour and ZERO authored type:** the page's own `<style>` carries no hex and no `font-size`/`font-weight` — every text element wears a `.t-cm-*`/`.t-ed-*` composite, and the reference's HSBC reds and greens arrive instead from `--rag-*`, `--data-series-*` and `.delta up/down` under Console resolution; the two-red law `s151-D1` and the mono error ink camp `s149-D1` are untouched. Radius is canon's per-theme value (Console surface 20px / control 8px), never authored. ★ **Chart geometry is MINTED, not hand-typed** (`s200-D1`): the generator holds the data as plain lists and derives every rect, polyline, donut arc and `data-fx` reflow hook with the arithmetic canon's own snippets encode — hand-rolling that geometry is exactly the #202 defect class. ⬛ **THE FIVE GAPS, NAMED:** (1) no **bell/notification** glyph in `assets/icons`; (2) no **kebab / overflow-menu** affordance (and `_validate_icons` flags shape-only kebabs by rule); (3) no **chart-type switcher** on the donut; (4) **no RAG-toned meter fill anywhere** — Meter, Progress-bar, Limits-meter and Runway-bar are all ink-on-track by design (`s210`: progress is structure, not status), so the reference's teal/amber/red confidence bar renders neutral with the RAG carried by a `.stat` chip beneath it; (5) the HSBC **brand gradient sweep** and the short **red rule** under each feature heading, both of which would be authored colour. ⚠ **THREE THINGS THAT ARE DAVE'S, NOT MINE:** ① **the currency order is inverted against canon's own `copy-025`** — Amount-display puts the code BEFORE the value closed up, the reference prints `112,514.11GBP`, and the screen follows the reference; ② the donut's name→value list is composed as **`.dv-leg` (the filter) BESIDE `.cn-summary` (the figures)** because canon's chart legend carries name only — *should the dataviz legend carry a value column?*; ③ canon's line-marker rotation defines **three** shapes, so a fourth series reuses the square. ⚠ **THE REFERENCE'S OWN ARITHMETIC DOES NOT CLOSE and is NOT silently reconciled:** its six spending rows sum to **13,734** while it prints **£12,684** in the donut centre and in its Total row; the rows are verbatim and the total shown is the arithmetic sum, because the arcs derive from the rows. ⚠ **The four chart panels print no numbers, only shapes** — their series are **read off the drawn heights** against the reference's own 0–100 axis, declared as a reading, not a measurement. ✅ **Gates run, verbatim:** `_validate_compose.py` **PASS** (28 canon classes, 0 rogue hex, 0 redefines, all resolve, title unique across 6 screens) · `_validate_screen.py` (no `--render`) **PASS** — compose ✅, **icon-source ✅ all paths library-matched** (every glyph is a byte-match from `assets/icons` or a verbatim path lifted from a gated snippet; nothing drawn), a11y ✅ with dense-data-mark warnings of the same `s116-D1` advisory class the `canon-gallery` screen already carries. ⛔ **`_validate_screen.py --render` (the state-contrast arm) reads RED, and the reading is DECLARED rather than dressed up:** its wrapper counts every sink record as a failure, including the records the real gate classifies as advisory. Broken down first-hand: **13 SEAT declarations** — `_validate_state_contrast.carrier_lines()` returns verdict `seat` for these and its own text says *"ADVISORY, never a failure"*; **5 hover PARSE REFUSALS** on `.btn.primary`, every one an `oklab(…)` background — the in-flight-interpolation signature the render runbook banks, i.e. UNMEASURED, not failed; and **8 pressed-state TEXT readings at 1.92:1** on the account rows, which a **settled** direct measurement (transitions disabled, class applied, read after settle) **contradicts**: white `rgb(255,255,255)` on `rgb(98,98,98)` = **6.1:1**, comfortably over 4.5. ⬛ **I did not adjudicate the gate itself** — the coarse wrapper reading is a DS-defect candidate, not this screen's defect, and it is the same wrapper that would redden `canon-gallery.canon.html` (34 `data-carries` elements against this page's 13). ★ **TWO DEFECTS WERE FOUND BY LOOKING AND FIXED AT CAUSE, both banked in the mapping doc:** ① **`.cn-badge` is the wrong component for a category label** — canon's `.badge` is `position:absolute` (a count badge pinned to a glyph), so the payee card's "Utility" chip flew to the top-right corner of the whole page; it is a **Tag**. ② **Scoping two component wrappers onto one element let the LATER one win at zero specificity** — `.cn-tags` on the same div as `.cn-list-items` overrode `.row:active .tag{color:var(--reverse)}`, and the pressed rows measured **1.17:1**; List-items owns its own `.tag`, so the second scope was simply wrong. ⚠ Two further layout-only overrides are declared on the page's face rather than hidden: the shell's `.sh` **specimen frame** (min-height 560px, 1px border, 20px radius) is neutralised because a real masthead is full-bleed, and Progress-tracker's 520px **`--demo-width`** is released because it otherwise trips the component's own `@container` collapse and hides the dots. ✅ **Render-verified in-sandbox, 4 shots taken and 4 LOOKED AT:** full page **Console light** and **Console dark** at 1440, the **charts close-up** (grouped bars · four-series line with circle/square/diamond markers · legends), and a **680px narrow** full page — single-column collapse, **zero horizontal overflow**. Font asserts against two controls (target **347** = `--uf` 347 = `"Univers Next for HSBC"` 347 · DejaVu 375 · nonexistent 301). ⚠ **UNPROVEN, DECLARED:** the mid-page **donut centre re-render** — the baked SVG carries `£13,734` but canon's live `dv-legend` behaviour rewrites the centre from `data-total` and prints `13734`, dropping the currency symbol and the thousands separator (**DS-defect candidate, not repaired here**); **no contrast ratio was computed** for the page as a whole beyond the pressed-row measurement above; the reference's **faded 8th account row** and its `⊗` invoice-remove control are not reproduced (no component carries either); and `_validate_a11y.py` still ignores a path argument, so its 0-failure figure is repo-wide and did **not** assess this file. ⚠ The page is wired into **no build step and no CI job**. ⚠ **Not committed, not pushed — CI has not seen these files.** ⛔ **Nothing on this page is ruled, recommended or preferred.** <br><br>**➤ v2 — `knowledge/_fitness-test/nio-dash-console-v2.canon.html` (+ change table `knowledge/_fitness-test/nio-dash-console-v2.mapping.md`, generator `knowledge/_render/gen_nio_dash_v2.py`, probe `knowledge/_render/verify_nio_dash_v2.py`), 2026-08-22 #216. SUPERSEDE-FORWARD: v1 above stands and is BYTE-UNTOUCHED — it is the interesting experiment and the comparison; v2 is v1 plus SIX requested changes and nothing was removed.** ⬛ **AWAITING DAVE'S EYE — NOTHING RULED, AND NEITHER SIDE OF EITHER VARIANT TOGGLE IS A RECOMMENDATION.** ① **A light grey page — and the reason v1 had none is NOT what it looked like.** v1 declared `body{ margin:0; background:var(--surface-hover) }` and **that rule was never parsed**: v1's harness comment wrote its exclusion list with literal asterisks (`no .c-<star>/.cn-<star>`) and `<star>`-then-slash is a CSS COMMENT TERMINATOR, so the comment closed early and the parser **discarded the very next rule**. Measured: v1's page `<style>` parses to **45 rules with NO `body` rule in the list**, and v1 still carries the browser's default **8px body margin** — the other dead declaration. A specificity fix would have been a false fix on a true symptom. Same species as **ds-039's second form**, which `gen_canon_components.py` already guards at the EMITTER for `canon.css` — but a composed screen writes its own `<style>` and **no gate parses it**; second-order, the same defect **blinds the compose gate's own hex scan**, because v1's comment contains the literal word `<style>` in prose and the scan's `<style>(.*?)</style>` capture starts there. Logged as **ds-041**. v2 paints by **re-pointing canon's own `--page`** on `<body>` rather than out-specifying `.canon{background:var(--page)}` — proved not to leak into component scopes (the donut's inter-arc `stroke="var(--page)"` still resolves to `--background-default` in both modes). ⚠ **THE GREY IS PER-MODE, AND THAT IS A TOKEN DEFECT NOT A PREFERENCE — Dave's eye is owed here.** Under Console **no single surface token is grey-and-distinct-from-card in both modes**: light `--surface-subtle` **#F0F0F0** vs card #FFFFFF is distinct while `--background-default` #FFFFFF is IDENTICAL to the card; dark `--surface-subtle` #1F1F1F is IDENTICAL to the card while `--background-default` **#1A1A1A** is distinct. v2 therefore uses **`--surface-subtle` in light and `--background-default` in dark** — **zero values minted**, both read verbatim. Same defect the bento row above surfaced from the light side, now seen in dark too. Logged. ② **1600px column** by re-pointing `.l-container`'s own `--l-max` (canon default 1120px), not overriding it; masthead and footer inners take the same measure so chrome stays full-bleed. Proved by RENDERED width (1600px in an 1800px viewport), not just by the variable. ③ ★★ **THE INPUT MISS, ROOT-CAUSED BY MEASUREMENT — AND IT IS NEITHER CASCADE ORDER NOR SPECIFICITY NOR SCOPE.** The search field IS inside `.cn-search-field` and `--border-radius-control` resolves to **8px on that very element**; **the component's CSS never consumes it**. Measured under Console: `.cn-search-field .search.boxed` **0px** while `.cn-input-fields`, `.cn-dropdown .trigger`, `.cn-account-selector .as-trigger`, `.cn-amount-input .ai-box` and `.btn.primary` all read **8px** — one square control in a family of rounded ones, which is exactly what *'generic, not Console'* looks like, and it is INVISIBLE under mono (radius 0), which is why it survived review. **THE CLASS:** of the 135 `.cn-*` scopes in `canon.css`, **16 declare no `border-radius` at all and SEVEN of those draw a full 1px box** — Search-field, Headers (`.frame`), Quick-actions (`.qa`), Reorder (`ul.reorder`), View-options (`.seg`), Splitter (`.demo-box`), Pagination (`.pg a`). **THE REAL FIX IS IN THE SNIPPET, NOT canon.css** (`canon.css` is GENERATED from `snippets/*.reference.html`, so a canon edit is erased on regeneration) — **deliberately NOT applied**, because it is a gated-snippet change, un-ruled, and because `canon.css` is shared and would **retroactively restyle v1**, the artefact being compared. Logged as **ds-040**; v2 fixes it in-page at the CLASS (`.nio-shell .search.boxed{border-radius:var(--border-radius-control)}` — names no `.cn-*` selector, so the compose gate's redefinition check stays satisfied). ⚙ **WHAT A COMPILED PER-THEME SHEET WOULD HAVE PREVENTED (evidence for the ADR-0011 / `s200-D1` proposal — NOT built here):** a compiler minting Console from the override store emits a concrete `border-radius: 8px` for every rule that draws a box, so a boxed rule with **no radius output** becomes a MISSING OUTPUT the compiler can be asked about and can refuse to emit. Today the miss is a **NON-EVENT** — the token resolves, nothing dangles, no gate has anything to look at, and the only detector is a human noticing one square corner. **The defect is not a wrong value; it is an UNCONSUMED value, and only an emitter that enumerates consumers can see an absent consumer.** Cheap interim named in ds-040: a static rule *'any rule setting a full `border:` on a control must also set `border-radius`'* would have caught all seven. ④ **Light/dark switch** — `data-theme` on `<html>`, the bento mechanism, driven from a canon `.cn-segmented-control` (real buttons, so keyboard is native; the sliding indicator is re-placed through **canon's own** `dv-behaviour.placeSegs()`, not re-implemented). Driven BOTH ways in the probe: clicked, and focus+Enter. ⑤ **DONUT LEGEND — CAPSULE vs LIST, live toggle, NEITHER RECOMMENDED.** The list variant merges v1's two blocks (`.dv-leg` the filter + `.cn-summary` the figures) into **one list beside the donut** carrying swatch + letter + label + value, plus a Total row. ★ **It needed NO behaviour-layer extension, and that is the finding:** `dv-legend.js` binds nothing to a layout — every listener is delegated at document level and resolves by **CLASS CONTRACT** (`.dv-leg`, `.dv-legrow[data-series]`, `.dv-leg-sw`, `.dv-leg-item`, `.dv-leg-reset`, `.dv-leg-name`), state parked on `host.__dv`, so honouring the contract inherits hover-fade, ghost-toggle, the DV-D19 isolate latch, the 'at least one must stay' guard, Reset and the live region for free; the value cell sits INSIDE `.dv-leg-item` after `.dv-leg-name` so `nameOf()` is unmoved. **PARITY DRIVEN, NOT ASSERTED:** both legends put through the same two gestures — uncheck swatch 3 ghosts `3` in both with centre `12684`; isolate label 1 ghosts `2,3,4,5,6` in both with centre `8500` — plus a **MUTATION** that sends the same assertion red against a wrong series id. ⇒ **COMPONENT-VARIANT CANDIDATE (a value column + a list presentation for dv-legend), gap-logged, NOT added to the showroom** — it answers v1's own mapping row 19 with a working shape rather than an opinion. ⚠ **Two limits found and declared, neither hidden:** dv-legend keeps **one state record per host**, so the switch **RESETS the outgoing legend** and selection does not carry across; and its hover path resolves a figure's legend with `querySelector('.dv-leg')` — the FIRST match — so the hidden legend has its `dv-leg` class removed (asserted: exactly **1** discoverable `.dv-leg` at any time), without which hovering an arc drives the wrong record. ⑥ **CHROMELESS ACCOUNT LIST, live toggle against v1's card form.** `.cn-list-items` draws chrome in exactly three places; chromeless switches off `ul.list`'s background and border and **KEEPS the `li+li` separator** — read back per variant: card `1px` border / opaque, chromeless `0px` / transparent, separator `1px solid` in **all three**, row `min-height` 76px unchanged in all three, rows still focusable buttons. Because the `.tag` outline is genuinely ambiguous as chrome, **BOTH readings are offered** (tags kept / tags plain) rather than one being picked. ⇒ **COMPONENT-VARIANT CANDIDATE for `.cn-list-items` (a `flush` form), gap-logged, NOT added to the showroom.** ★★ **TWO FURTHER DEFECTS FOUND WHILE BUILDING, BOTH MEASURED, BOTH LOGGED, NEITHER INVENTED:** ⑴ **ds-042 — canon scope bleed:** `:where(.cn-template-dashboard) .dv-svg{width:580px;height:260px}` is component-specific chart geometry living in the PAGE-SHELL scope, and it comes LATER in `canon.css` than `:where(.cn-chart-donut) .dv-svg`, which sets no width at all — so on any dashboard-templated page the donut is forced to 580px although its own markup says `width="300"`; **in v1 that makes the donut overflow its 426px column and become horizontally SCROLLABLE**, hidden by `.dv-stage{overflow-x:auto}`. v2 restores the 300×260 the svg's own attributes already declare — read off the markup, not invented. ⑵ **ds-043 — the series palette stops at 5 and the sixth category renders SILENT BLACK:** `--data-series-6/7/8` are defined **NOWHERE** in `canon.css` (0 occurrences; 1–5 have 13/8/9/4/4). The Nio donut has six categories, so measured in **both** modes: series 6's legend swatch `--sc` is **empty** → `rgba(0,0,0,0)`, **invisible**, while its arc falls back to the SVG initial `rgb(0,0,0)`, **pure black** — nearly invisible on a `#1F1F1F` dark card. Present in v1, **inherited unchanged by v2 because the honest fix is a new palette step and this page mints no colour**, and ASSERTED in the probe so it is a measurement in the record rather than an impression. ✅ **Gates: `_validate_compose.py` PASS** (28 canon classes, 0 rogue hex, 0 redefines, all resolve, title unique across 7 screens) **· `_validate_screen.py` PASS** (compose ✅ · icon-source ✅ all paths library-matched · a11y ✅ with the same `s116-D1` dense-data-mark advisories v1 carries). ✅ **`verify_nio_dash_v2.py` — three stages, 55 checks, ALL GREEN**, and **FIVE of them are POSITIVE CONTROLS RUN ON v1** so the instrument is shown able to fail: v1's page background is NOT distinct from a card, v1's `<style>` HAS lost its body rule, v1's body margin IS 8px, v1's `.search.boxed` IS 0px against an 8px token, v1's `--l-max` IS 1120px. Font asserted against two controls (target **346.88** = `--uf` 346.88 · DejaVu 375.39 · nonexistent 301.07); var sweep Console light+dark **0 empty**. ★ **THE PROBE ITSELF WENT RED TWICE ON A CORRECT PAGE AND BOTH WERE INSTRUMENT BUGS, FIXED AND BANKED IN THE SCRIPT:** a hex/rgb normaliser digit-scraped `#1A1A1A` into `#010101`, and a chrome assertion compared against the literal string `rgba(0, 0, 0, 0)` when `--surface-transparent` is `#FFFFFF00` and computes to transparent **WHITE** — the notation is not the fact. ✅ **Render-verified in-sandbox, 9 shots taken and 6 LOOKED AT:** full page Console **light** and **dark** at 1800, the donut in **list**-legend and in **capsule** mode, the account list **chromeless** and **chromeless plain-tags**, the masthead at 1800 with the 1600 column visible against the grey, and a **560px narrow** full page. ⚠ **UNPROVEN, DECLARED: NO CONTRAST RATIO WAS COMPUTED for v2** — not for the grey page against card ink, not for the list legend's value column, not for the chromeless list; the grey is a token read, not a measured pair. `_validate_screen.py --render` (the state-contrast arm) was **NOT re-run** on v2 — v1's row above already records that arm as reading RED for wrapper reasons that are a DS-defect candidate, and 'v2 changes nothing in that area' is an argument, not a measurement. **Only Console was tested**, although ds-040's radius defect is a four-theme defect. `_validate_a11y.py` still ignores a path argument (repo-wide figure, did NOT assess this file), and neither the descender-clip nor the type-composite gate reaches `knowledge/_fitness-test/` at all. The **series-6 black arc** and v1's **donut-centre re-render** (`13734`, no currency symbol, no thousands separator) are **carried, not repaired**. ⚠ The page is wired into **no build step and no CI job**. ⚠ **Not committed, not pushed — CI has not seen these files.** ⛔ **Nothing on this page is ruled, recommended or preferred.** |
| **REAL PHOTOGRAPHY IN THE LIBRARY** — the committed manifest `knowledge/_PHOTOGRAPHY-MANIFEST.md` (+ `.json`, 251 rows) and its generator `knowledge/_build_photo_manifest.py`; 12 web derivatives in `knowledge/assets/photography-web/`; two specimens, `knowledge/_fitness-test/photography-image-block-v1.html` and `knowledge/_fitness-test/photography-carousel-v1.html`; the priced KG note `knowledge/_PHOTOGRAPHY-KG-NOTE.md` | 2026-08-22 #217 · rows `W-108` / `W-109` / `W-110` / `W-111` / `W-113` (probe `knowledge/_render/verify_photography_specimens.py`) · **BUILT UNDER — `s217-D1`** | ⬛ **AWAITING DAVE'S EYE — TWO THINGS TO JUDGE, AND ONE OF THEM IS PHOTOGRAPHS.** ① **The 12 specimen photographs are a PICK, not a recommendation** — chosen to exercise the crops (16:9 / 4:3 / 1:1 / 3:4, standard + peek) and to span three licence sources (Getty · Stocksy · EyeEm); swapping any of them is one filename in the page and one `--derivatives` run. ② **Do the components hold up with real imagery?** Both were render-proofed light **and** dark at two widths, every image proven loaded (12/12 and 7/7, `naturalWidth > 0`), measured boxes matching the declared ratios. ⚠ **Three CSS rules were added in the SPECIMENS ONLY and canon was not touched** — canon styles `.media svg` and `.ph` but has **no rule for a real `<img>`**, so a deployment following the snippet's own instruction loses the ratio variants (Image block) or stretches the photograph (Carousel); logged as **`ds-044`**, the fix is Dave's. ⚠ **Alt text is the EXIF description VERBATIM** — including the EyeEm file's ALL-CAPS metadata — because the tagging approach is UNRULED (#211 item 3) and a rewritten alt is an invented fact. ⚠ **Licensed stock (Getty / Stocksy / EyeEm): nothing here clears any image for external use.** ⚠ The manifest's `--check` gate is **DECLARED UNROUTED** — not wired into `_build_all.py` or the commit seam, so it cannot yet fail in CI. ⚠ **Not committed, not pushed.** ⛔ **Nothing here is ruled** — no tagging taxonomy, no KG schema, no preference between photographs. |
| **THE LOGOS IN THE LIBRARY** — `reviews/LOGOS-2026-08-22-v1.html`; the 12 brand SVGs it presents are `knowledge/assets/logos/*.svg`, unchanged; findings `ds-045` / `ds-046` in `knowledge/_DS-IMPROVEMENTS.md` | 2026-08-22 #217 · row `W-112` | ⬛ **AWAITING DAVE'S EYE — AND ONE OF THE TWELVE ASSETS IS BROKEN.** All 12 variants (hexagon · masterbrand · masterbrand-with-identifier × colour|mono × on-light|on-dark) are presented **each on the ground its artwork requires** — light artwork on `#FFFFFF` tiles, dark artwork on `#1A1A1A` tiles — with colour and monotone side by side, filenames visible, and a measured inventory (viewBox, natural size, byte count, fill census) read off the files at build time rather than typed. ⚠ **`ds-045` — the two ON-DARK identifier exports carry NO identifier label:** they are the plain masterbrand artwork on a 795-unit viewBox, so 60% of the box renders empty; `masterbrand-identifier-dark-colour.svg` is byte-identical to `masterbrand-dark-colour.svg` apart from the viewBox width and one rounding digit. **Re-export or retire is Dave's call**; the SVGs were fenced from edit in this lane. ⚠ **`ds-046` — a themed element that also consumes its own token resolved the wrong value** (supercharge dark: `body`'s `--text-default` read `#F7F6F4` while its `color` resolved `#FFFFFF`); worked around page-locally by consuming on children, the class is unrepaired and **unproven outside headless chromium**. ⚠ **The tiles deliberately do NOT follow the theme** — measured: every fill in all 12 files is hardcoded (`#DB0011` / `white` / `black` / `#333333`), so no logo *can* follow one; the page chrome follows all four themes and both modes, the tiles are fixed to the artwork's ground. **Render-proved in all 8 theme×mode states** (mono · legacy · console · supercharge, light + dark) at 1400px plus mono at 480px: real HSBC face asserted against two controls (347/347 vs DejaVu 375, nonexistent 301), 12/12 images loaded in every state, every custom property the page reads resolved non-empty in every state. ⚠ **No accent colour is spent anywhere on the page**, so the two-red law and the mono error-ink camp are not reached. ⚠ **NOT wired into the library index** — the `Foundations` ladder tier in `showroom/index.html` is still empty and no generator was touched; wiring it needs a component-store entry and a regen pass, **Dave's call**. ⚠ **Not committed, not pushed.** ⛔ **No logo usage POLICY is authored** — clearspace, minimum sizes, co-branding and placement remain create.hsbc's and Dave's; the notes section carries only what `knowledge/guidelines/logos.md` already says. Surfaced but not touched: the monotone on-light artwork fills with **pure `black`** (not the project ink `#1A1A1A`), and the hexagon red is the brand primitive `#DB0011`, not the two-red law's UI ink. |
| **THE FOUNDATIONS TIER, FILLED** — `showroom/_foundations/photography.html` + `showroom/_foundations/logos.html`; generator `knowledge/_render/gen_foundations_217.py`; the library wiring in `knowledge/_render/gen_library_214.py` (`FOUNDATIONS` + two declared rows) and `knowledge/_render/gen_thumbs.py`; probe `knowledge/_render/verify_foundations_217.py`; finding `ds-047` in `knowledge/_DS-IMPROVEMENTS.md` | 2026-08-22 #217 · row `W-114` / `W-115` | ⬛ **AWAITING DAVE'S EYE — AND IT COPIES A LAYOUT THAT IS ITSELF UNSIGNED.** Dave #217 verbatim: *"so we need a foundations section in the library with images and logos displayed like the photography bento"*. The `s215-D4` Foundations rung existed and drew empty since #215; it now draws with two entries, reachable from the Type tab, the card grid and `showroom/index.json`. ⛔ **The span vocabulary and the zero-JavaScript popover lightbox are COPIED from `knowledge/_fitness-test/bento-gallery-showcase-v2.html` (#216), which is AWAITING SIGN-OFF on this same list — copying a layout Dave pointed at is not a certification of it, and if he rules the bento differently both pages follow.** ⛔ **Bento stays un-minted as canon** (he parked structural bento components at #216): every rule is page-local, scoped `.fx`, declared in the page head — no canon component, no `canon.css` edit, no token. **Nothing is re-drawn** — 12 photographs are `<img>` at their committed derivatives, 12 marks are `<img>` at the exported SVGs, and every caption, viewBox, byte count and fill census is measured at build time. ⬛ **THREE THINGS ARE PROPOSED, NOT RULED, and each is one number or one line:** **(1)** the **emphasis rhythm** — all twelve derivatives share one aspect ratio, so the aspect rule alone drew a plain two-up grid; every fifth tile in filename order is promoted to a 2×2 block (position, not preference — but the rhythm is a choice); **(2)** the **span thresholds** (4:1 → three columns, 1.45:1 → two, 1:1.15 → two rows); **(3)** **status `beta`** on both entries, which is the existing derivation's answer for a row carrying no itinerary and no meta, not a judgment. ⚠ **The two on-dark identifier marks are shown AS THE FILES ARE** — `ds-045` stands, the tiles are not quietly fixed. ⚠ **Render-proof:** 16 states (2 pages × 4 themes × light/dark), real HSBC face against two controls, every foreign custom property resolved non-empty, light ≠ dark ground in all four themes, the lightbox driven open in all 8 photography states. ⛔ **NOT committed, NOT pushed.** |
| **THE BENTO PARAMETER TUNER** — `knowledge/_fitness-test/bento-tuner-v1.html`; probe `knowledge/_render/verify_bento_tuner_217.py`; findings `ds-048` / `ds-049` in `knowledge/_DS-IMPROVEMENTS.md` | 2026-08-22 #217 · row `W-116` / `W-117` | ⬛ **AWAITING DAVE — AND IT IS A DECISION CONTROLLER, NOT A DELIVERABLE TO SIGN OFF.** Dave #217 verbatim: *"We need to promote all the various types of bento to canon, i just need to decide on some of the parameters like gutters etc"*. This page is how he decides them. ⛔ **IT MINTS NOTHING** — no token, no `canon.css` rule, no theme, no component; the promotion is his ruling and it fires when he exports, not here. **Every dial arrives on the value in force today** on `showroom/_foundations/photography.html` and the #216 showcases, labelled **PROPOSED-NOT-RULED**, with a per-dial *changed* mark so he can see what he has moved. **Dials:** gutter · outer padding · row unit · columns · tile radius (theme token or dialled) · dense packing · three aspect→span thresholds · emphasis rhythm N + offset + promoted shape · three container-query band widths. **Preview is REAL CONTENT** — the 12 committed derivatives and a 12-tile mixed-card wall, inside ONE container, so a dial moves both. **Four themes × light/dark in-page**, with live WCAG readouts on four ink-on-ground pairs. **Export** is generated from the SAME state object as the preview in the SAME pass — the pitfall the brief was written against — and carries **concrete values only** (`s200-D1`): where the radius is left on the theme token the exporter reads the RESOLVED pixel value in each of the eight states instead of emitting a `var()` chain. ⛔ **The radius dial is the BENTO TILE radius only** — component radii and the segmented-switch carve-out are radius-tuner-v2 territory, are not dialled and do not appear in the export; the page says so in its own words. ⚠ **Controller chrome is NEUTRAL only** — no red and no yellow anywhere in it, deliberately; no colour value is authored on the page at all, and it declares ZERO font-size and ZERO font-weight. ⚠ **MEASURED, AND IT IS DAVE'S TO LOOK AT:** the resolved tile radius is **`0px` in mono, legacy and supercharge — and `20px` in console**, in both light and dark. A single ruled bento radius and the current token are not the same answer. ⚠ **Render-proof:** 55 assertions over ten legs, 8 states, real HSBC face against two controls, dials driven and the GRID MEASURED (gap 16→34px, rows 180→244px, tracks 4→6, radius 0→18px), export proven against `getComputedStyle`, bands proven to move when the band dial moves, seen at two stage widths. **Three real defects the render caught and the build fixed:** the export leaked the tuner's private property names (pasted, its bands would have been inert); the readout panel reported *1 column* over a four-column wall because it read inside the stage's `max-width` transition; the clamped captions lost their descenders to the scaffold's cap/alphabetic trim (`ds-048`). ⛔ **NOT committed, NOT pushed.** |
| **THE BENTO, PROMOTED TO CANON** — canon grammar `knowledge/canon/canon.css` (AUTO-BENTO block) generated by `knowledge/canon/gen_canon_bento.py` from `knowledge/tokens/layout.json` (`layout/bento`) + the gutter overrides in `knowledge/tokens/themes/apollo-legacy.overrides.json` and `apollo-console.overrides.json`; demo `reviews/BENTO-CANON-2026-08-23-v1.html` (generator `knowledge/_render/gen_bento_canon_217.py`, probe `knowledge/_render/verify_bento_canon_217.py`); Foundations re-pointed in `knowledge/_render/gen_foundations_217.py` | 2026-08-23 #217 · rows `W-118` / `W-119` / `W-120` / `W-121` · **BUILT UNDER — `s217-D2`** | ⬛ **AWAITING DAVE'S EYE — THE RULING IS ENACTED, THE RESULT IS NOT SIGNED OFF.** What is enacted and measured: per-theme gutter **0 mono · 0 supercharge · 24 legacy · 24 console** (measured in ALL FOUR — a theme override that never lands leaves the base value silently in force, so one green theme proves nothing); container radius **20 console / 0 the other three**, on the bento's OUTER CONTAINER with `overflow:hidden`, **tile radius 0 in all eight states**; nesting with an independent parameter set per level (**outer gutter 40, three inner gutters at 1** — Dave's own example); band collapse **3 / 2 / 1** at 1100 / 820 / 520, measured on one page at one viewport because the bands are CONTAINER queries. ⛔ **Nothing beyond `s217-D2`'s words was decided** — no component radii (the segmented-switch carve-out is untouched), no surface, no ink, no typography. **The bento container paints NO ground in canon** (a structural component must not choose a surface): the demo page gives it one, which is also what makes a rounded container read as rounded. ⚠ **TWO THINGS FOR HIS EYE THAT THE RULING DOES NOT COVER, SURFACED NOT FIXED:** ⑴ on `showroom/_foundations/photography.html` the photo TILES are rounded, because `.px-open` binds `border-radius/surface` (20 in console) — that is a COMPONENT radius and component radii are out of scope, so it was left alone; whether tiles inside a bento should keep their own surface radius is his call. ⑵ the container radius is invisible on the Foundations walls, because they paint no ground behind the tiles — the radius and the clip are both there and both measured, there is simply nothing to see them against. ⚠ **The Foundations re-point CHANGED THE ARRANGEMENT**: the ruled thresholds (6.0 / 2.6 / 1:1.15, every 6th from tile 1) are not the ones that page carried (4.0 / 1.45, every 5th), and its bands moved from viewport media queries to container queries — so a narrow library pane now collapses the wall, which the pane embed always wanted. **Page-local structural CSS 19 declaration lines → 4.** ⚠ **`showroom/_thumbs/*.png` were NOT re-shot** — the two Foundations thumbnails now predate the re-point. ⚠ **Not committed, not pushed — CI has not seen these files.** ⛔ Nothing here is ruled beyond `s217-D2`. |
| **THE BENTO CANON DEMO v2 — PORTRAIT PROVEN, SQUARING PROPOSED** — `reviews/BENTO-CANON-2026-08-23-v2.html` (generator `knowledge/_render/gen_bento_canon_217.py`, probe `knowledge/_render/verify_bento_canon_217.py`); the pass itself in `knowledge/canon/gen_canon_bento.py` (`square_wall` / `place` / `is_rectangular` / `band_ladder`); Foundations photography regenerated through it; 3 new portrait derivatives in `knowledge/assets/photography-web/` with their manifest rows | 2026-08-23 #217 · rows `W-119` (re-homed to v2) / `W-122` · **BUILT UNDER — `s217-D2`, plus ONE PROPOSAL** | ⬛ **AWAITING DAVE — AND HALF OF IT IS A QUESTION, NOT A DELIVERABLE.** Two mid-session asks, answered separately on purpose. ⑴ **PORTRAIT — ALREADY RULED, NOW PROVEN.** Dave: *"if photo is portrait could it default to a taller compartment?"* `s217-D2` already carries the two-row threshold at taller than 1:1.15, so **nothing was decided**; three portrait originals were minted to derivatives (`gettyimages-1336692652` 1065×1600, `stocksy-6629948` 1067×1600, `gettyimages-ca60828-144dpi` 1106×1600 — picking specimens is not ruling) and the page shows them **two rows tall beside their landscape neighbours**. MEASURED: every portrait tile resolves `data-r="2"` in **8 states × 4 viewport widths**, exempt only at ONE column where canon itself rewrites every `data-r` to 1. ⑵ **THE SQUARING PASS — ⬛ PROPOSED, NOT RULED.** Dave: *"Is there a way to avoid orphaned compartments so the bento is always cohesively rectangular? see image"*. `square_wall()` simulates the browser's own dense placement, asks whether every cell down to the last row is filled **at every column count the bands produce** (6/3/2/1, or the instance's shorter ladder — a wall square at 6 and ragged at 3 is the same defect, later), and re-spans the **last few tiles** until it is. Deterministic, mint-time, **no JS and no CSS hack**; it emits **nothing into `canon.css`**, so it comes out in one line. Cost-ordered so a portrait is not laid on its side to square a wall. **Refuses BY NAME** when a wall cannot be a rectangle at every band (three tiles at six columns cannot). ⚠ **The page marks it PROPOSED-NOT-RULED and shows eleven deliberately awkward tiles RAGGED and SQUARED, stacked at full width** — stacked because side by side both walls fall below the 820 band, where eleven tiles happen to be rectangular anyway and the control came out square (a control that cannot show the defect is worse than none). ⚠ **MEASURED:** **352 wall measurements green** — 11 walls × 4 widths (1500/900/680/560 → the 6/3/2/1 bands) × 8 theme×mode states, **zero empty cells**; the ragged control shows 10 / 4 / 2 holes at those same widths. **MUTATION ARM RED BY NAME:** with the pass disabled the same assertion reports 14 ragged walls. Foundations photography squared at ladder 4/3/2/1 (**6 / 8 / 12 / 15 rows**, 2 tiles re-spanned). ⚠ **WHAT IS DAVE'S TO RULE:** whether spans may be adjusted at mint time at all; whether only the tail may move or the whole wall; and what a photograph may be re-cropped into. ⚠ **v1 is untouched on disk** — v2 is a new file, not an overwrite. ⛔ **NOT committed, NOT pushed.** ⛔ Nothing ruled. |
| **THE BENTO ROLES + THE KEYLINE TRIAL (v3)** — `reviews/BENTO-CANON-2026-08-23-v3.html` (generator `knowledge/_render/gen_bento_roles_217.py`, probe `knowledge/_render/verify_bento_roles_217.py`); the role grammar in `knowledge/canon/gen_canon_bento.py` → the `[data-bento-role=…]` block of `knowledge/canon/canon.css`, from `knowledge/tokens/layout.json` (`layout/bento/$roles` + `layout/bento/caption-space`); the Foundations re-point in `knowledge/_render/gen_foundations_217.py` → `showroom/_foundations/photography.html`; ledger entries in `knowledge/_TOKEN-FORK-LEDGER.json`; finding `ds-052` in `knowledge/_DS-IMPROVEMENTS.md` | 2026-08-23 #217 · rows `W-123` / `W-124` | ⬛ **AWAITING DAVE'S EYE — AND ONE THING IN IT IS A TRIAL, NOT A BUILD.** `s217-D3` is built: a bento carries a **role**, and the role decides where the radius sits, what the spacing is and whether the wall is squared. **Dashboard** — theme radius on each inner bento's CONTAINER, tiles square, tiles 1px apart, and the OUTER wall of a bento-of-bentos keeps the theme gutter (a `:has(> .c-bento__grid > .c-bento)` carve-out; a blanket 1px would collapse the structure the role exists to describe). **Brochureware** — the TILES carry the radius and the spacing, container square, no clip. **Gallery** — as brochureware, plus the squaring pass RELAXED. ⚠ **MEASURED PER THEME, never inferred from console** (8 states × 4 widths, `verify_bento_roles_217.py` ALL GREEN): dashboard container radius **20 in console, 0 in mono/legacy/supercharge** with tiles square; brochureware and gallery container **0** with **tile radius 20 in console**; dashboard inner gutters **1/1/1** against an outer of **0/0/24/24**; **zero empty cells** in every squared-role wall at 1500/900/680/560; gallery holes **4/3/2/0** counted and **tolerated, not asserted** — that tolerance IS the ruling. **MUTATION ARM RED BY NAME:** every role rewritten to `brochureware` and the per-role probe fails 6 ways. ⚠ **THE CAPTION SPACE IS RULED AND DERIVED, NOT PICKED:** 62px shipped → **86px** (+24px, +38.7%) = 12 padding-block ×2 + **3** description lines × 16 + 2 gap + 12 licence line; the 3-line clamp is derived back out of the number, so the space and the clamp cannot disagree. Measured on **360 caption blocks** across the 8 states. ⬛ **WHAT IS DAVE'S TO RULE: THE KEYLINE TRIAL.** His words were *“can we try dropping the keylines and just have a 1px gutter and row space”* — so it is a **specimen, PROPOSED NOT RULED**, shown directly beneath the ruled keylined gallery with the same photographs and the same derived spans. Every trial declaration names `.dx-trial` and a selftest bite enforces it, so the trial cannot leak into the ruled variant; the probe measures **0px border on the trial tiles and a non-zero border on the ruled ones** in all 8 states. ⚠ **TWO DEFECTS THE PROBE CAUGHT AND WHICH ARE NOW GATED:** an unterminated `/*` silently swallowed the dashboard role's radius and gutter while the stylesheet still parsed (comment-balance bite added), and the dashboard tile rule out-specified the container radius on a tile that is *also* a bento, squaring off the inner walls the role exists to round (rule removed — canon gives a tile no radius anyway, and a literal `border-radius:0` trips the radius gate). ⚠ **v2 is untouched on disk and still probes green** — v3 is a new file with its own generator and its own probe, not an overwrite. ⚠ `ds-051` (the packing token's DTCG type) is **deliberately untouched** and remains the build gate's **only** red. ⛔ **NOT committed, NOT pushed.** ⛔ Nothing ruled. |
| **GALLERY — SPAN GRID OR JUSTIFIED ROWS (THE DECISION PAGE)** — `reviews/GALLERY-COMPARE-2026-08-23-v1.html` (generator `knowledge/_render/gen_gallery_compare_217.py`, probe `knowledge/_render/verify_gallery_compare_217.py`); reads the same manifest rows as the ruled roles page through `gen_bento_roles_217.read_photos` | 2026-08-24 #217 · row `W-125` · **BUILT UNDER NOTHING — this page IS the decision surface** | ⬛ **AWAITING DAVE — AND IT IS A QUESTION, NOT A DELIVERABLE.** His words: *"'whether gallery stays a span grid with orphan tolerance, or becomes justified rows with a widow switch' would this be visually different, id like to see these two side by side if so"*. **It is very different, and the difference has one name: cropping.** **A — the ruled gallery role exactly as canon renders it**, shown at canon's default **6 columns** AND at the **4-column dial** the roles page used, because the crop cost is mostly that dial and showing only the harsh one would be a strawman; the s217-D3 **keyline trial** rides along, still proposed. **B — justified rows, Flickr-style, packed at MINT TIME** (`s200-D1`) at the **ruled 320px row unit** (`layout/bento/row-unit`, read from the store — and independently Flickr's own default target): rows justify flush by proportional `flex-grow`, so **nothing is cropped anywhere**; the short last row are **widows**, **not scaled up**, counted, and shown/hidden by a **checkbox and CSS with no JavaScript**. ⚠ **MEASURED, not asserted:** A crops **44% of image area on average at 6 columns** (worst 50%, 15/15 photographs cropped) and **13% at 4 columns** (worst 24%); A's holes **6 / 3 / 2 / 0** by band, **counted and tolerated, never enforced** — that tolerance is the ruling. B: **every rendered box within 1% of its manifest ratio** in 8 states × 3 widths, rows measuring **285 / 350 / 290 / 285px** at 1336px (a 65px spread around the 320 target — the portrait's row is the tall one), **3 widows**, and every non-widow row **flush to the container within 1px** at 1500 / 1000 / 760px in both the 24px and the 0px gutter regime. **Widow switch DRIVEN both ways** — 3 laid out → 0 → 3. **MUTATION ARM RED BY NAME:** the proportional grow replaced by fixed widths and **all four rows fail the flush assertion** (1488 / 1221 / 1462 / 1488px against 1336) — deliberately a mutant that still renders real photographs, because a mutation that collapses the layout is caught by any assertion and would prove nothing about the FLUSH one. ⚠ **ONE DATA PATH, enforced by a selftest bite:** both candidates render the same files in the same order through the roles page's own reader; two data paths would make the comparison a lie. ⬛ **THE HONEST COST OF B, DECLARED:** mint-time packing fixes which photographs share a row, so **B's rows stay flush but get shorter as the container narrows instead of re-packing** (Flickr re-packs at runtime); **A re-flows at the bands for free**. B also has **no emphasis rhythm** and its **nesting is untested**. ⚠ Captions are canon's ruled **86px** block in BOTH candidates — a `min-height`, so a long licence line can still grow it at narrow widths; that is canon's behaviour, surfaced not changed. ⚠ **Render-proved**: real HSBC face against two controls (347/347 vs DejaVu 375, nonexistent 301), 8 states, dangling sweep over 11 foreign properties per state, seen at 1500px console/light and 760px supercharge/dark. ⛔ **NOTHING IS RULED AND NOTHING WAS TOUCHED** — no canon, no token, no ruling, no commit, no push. `s217-D3` stands: the gallery is a span grid with orphan tolerance until Dave says otherwise. |
| **THE BENTO MATRIX EXPLORER** — `showroom/_foundations/bento.html`, a new **Foundations entry beside Photography and Logos**; page body `knowledge/_render/gen_bento_matrix_217.py`, written by `knowledge/_render/gen_foundations_217.py` (ONE writer, ONE shell), wired through the `FOUNDATIONS` list in `knowledge/_render/gen_library_214.py` → `showroom/index.html` + `showroom/index.json` + `showroom/_thumbs/foundation-bento.png`; probe `knowledge/_render/verify_bento_matrix_217.py`; finding `ds-053` in `knowledge/_DS-IMPROVEMENTS.md` | 2026-08-24 #217 · row `W-126` · **BUILT UNDER — `s217-D5`** | ⬛ **AWAITING DAVE'S EYE — AND HALF OF IT IS FIVE QUESTIONS, NOT A DELIVERABLE.** `s217-D5` rules the option matrix and names **five OPEN POINTS it does not rule**; this page implements each as a **PROPOSAL, labelled `PROPOSED` on the control it governs**, so he rules by eye rather than from prose. His own closing words were *“lets see this for now. this is a lot, you might want to check my logic”*. **THREE TYPES, live:** Display · Gallery · Dashboard, each with its ruled dials over real content — the **15 committed photography derivatives** plus the mixed cards and panels, all read through `gen_bento_roles_217` (**ONE data path**), and the justified rows through `gen_gallery_compare_217.pack_rows` (**ONE packing maths**). ⚠ **THE TYPES ARE CANON'S ROLES UNDER THEIR RULED NAMES** (display→`brochureware`), so no ruled radius, spacing or squaring decision is restated on the page. ⚠ **837 REACHABLE COMBINATIONS, ENUMERATED AT BUILD TIME, NOT CLAIMED** — display **54**, gallery **675** (after the two legality refusals), dashboard **108**; the count and the refusals come from the same two rules, so the page cannot lie about its own matrix. ⛔ **RULED-OUT IS ABSENT, NOT DISABLED:** the dashboard main-spacing control carries **no tight button at all**, because `s217-D5` rules it out rather than making it unavailable; the gallery bottom-edge dial exists only under Gallery-bento. **Every reachable combination renders.** ⬛ **THE FIVE PROPOSALS:** **P1** tight+keylines ON = flush tiles with inset hairlines (1px cannot hold a gap *and* a line), tight+keylines OFF = the ruled 1px show-through · **P2** a caption is judged against its **immediate ground** and same-on-same is refused **with its reason printed beside the control** · **P3** the full capsule **requires** a caption background or keylines · **P4** at Display tight the standard radius and a **stepped-down concentric** alternative are drawn **side by side** · **P5** one grey/white/transparent palette on all three types. ⚠ **A SIXTH PROPOSAL WAS FORCED BY MEASUREMENT AND IS DECLARED ON THE PAGE:** a gallery tile paints **no ground of its own** — with the tile painted, a white caption on a grey bento rendered **white-on-white while P2 called it legal**; a card or panel tile does keep a surface, because the ruled 1px show-through needs something to show through of. ⚠ **MEASURED, four themes × light/dark:** three types driven (exactly one pane, the right canon role, non-zero tiles each); **seven dials measured in pixels** — display 40/24/1 with the flush-at-tight collapse to **0** and the inset hairline read back, gallery mode and the **ragged→square span swap on 2 tiles**, both roundings at console (**tile 0 / image 20** vs **tile 20**), dashboard **outer 40 against inner 24** and **outer 24 against inner 1**; **both legality rules refused WITH reasons and the refused click proven INERT**; **export parity green over 8 dials + 11 resolved values** read out of the live document (`s200-D1` — concrete values, no `var()` chain); **zero dangling properties** over 14 canon properties × 8 states; light ≠ dark ground in all four themes; font asserted against **two controls**. ⬛ **MUTATION ARM RED BY NAME:** with the legality function stubbed out, **5 refusal assertions fail and nothing else does**. ⛔ **NO TOKEN, NO `canon.css` RULE AND NO RULING WAS TOUCHED** — every rule the page ships is page-local and scoped. ⛔ **NOT committed, NOT pushed.** ⛔ **Nothing is ruled: `s217-D5`'s five open points are still open, and this page exists to close them by eye.** |
| **THE BENTO MATRIX EXPLORER — DAVE'S FIRST SITTING, ANSWERED (`s217-D6` + TWO DEFECTS AT CAUSE)** — `showroom/_foundations/bento.html` regenerated through `knowledge/_render/gen_foundations_217.py`; page body `knowledge/_render/gen_bento_matrix_217.py`; **one canon change** in `knowledge/canon/gen_canon_bento.py` → the AUTO-BENTO block of `knowledge/canon/canon.css`; probe `knowledge/_render/verify_bento_matrix_217.py` extended; findings `ds-054` · `ds-055` · `ds-056` in `knowledge/_DS-IMPROVEMENTS.md` | 2026-08-24 #217 · row `W-126` · **BUILT UNDER — `s217-D6`, WHICH AMENDS `s217-D5`** | ⬛ **THIS ROW SUPERSEDES TWO FACTS IN THE ROW ABOVE AND ADDS NOTHING TO ITS CREDIT — the row above stands as the record of the FIRST build.** Superseded: the **837** headline count, and **P4 drawn “side by side”**. ⛔ **DEFECT 1 — DISPLAY TIGHT WAS A DIFFERENT COMPOSITION, AND THE CAUSE WAS THE PRESENTATION, NOT THE SPACING.** Dave: *“I expected display-tight to just be like standard and generous but just loosing the big padding. it seems to align to two columns.”* **CAUSE, MEASURED:** every `.c-bento` is `container-type:inline-size` (canon), so the responsive bands answer the **wall**, not the window. P4's two radius walls sat in a two-column grid; at a 1280px viewport each landed at **571px**, tripped the **820px** band and collapsed to **2 columns**, while standard/generous rendered **4** at **1166px**. **FIX:** the comparison is **STACKED, full width, both walls** — same container width, same band in force, so the only variable left is the radius, which is what P4 compares. A toggle would have been honest too but shows one side at a time; a corner crop would compare a detail, not the composition. ⛔ **AND IT IS A CLASS RULE NOW, NOT A CORRECTION:** *nothing in the explorer may present a comparison by shrinking a container a band responds to* — asserted in the probe (tight column count **==** standard column count at the same viewport, and every wall in a comparison sharing width and tracks) and in the selftest (no multi-track column axis may hold a bento wall). **MEASURED GREEN: tight 4 col(s) @1166px == standard 4 col(s) @1166px.** ⛔ **DEFECT 2 — THE DASHBOARD WALL HAD TWO CAUSES, NOT ONE, AND ONE OF THEM LIVED IN CANON.** Dave: *“Dashboard seems to be rendered oddly, we have gaps and the bottom of the first one is flat.”* **CAUSE (a), THE ORPHAN GAP:** `s217-D3` rules the squaring pass **ON** for the dashboard role, but this page wrote the three groups with a **literal `data-c="1"`**, so `square_wall_for_role` was **never asked** and the ruled pass never ran — three 1×1 groups at two columns leave the second row half empty. Asked properly it promotes the third group to `(2,1)` and the wall closes: **0 empty cells of 4**. **CAUSE (b), THE FLAT BOTTOM — A CANON DEFECT, FIXED IN CANON:** canon's `.c-bento__tile.c-bento{height:100%}` **stretches** a nested bento to fill its cell so a row of groups reads as one band, but the nested bento's **own grid has fixed rows and is top-aligned** and did not grow with it. The moment two groups in a row had different natural heights the shorter one became a rounded container **taller than its content** — **measured #217 as a 360px box holding a 240px grid**, i.e. a **120px dead band**, so the visible block read with a rounded top and a **FLAT BOTTOM** and the dead band read as another gap. ⚠ **INVISIBLE IN THE ROLES DEMO** (`gen_bento_roles_217.py`), whose nested walls happen to be equal height — so the roles demo's `--bento-row-unit:auto` was **a different fix for a different symptom**, not this one. **FIX, MINIMAL AND NAMED, IN THE GENERATOR NOT THE CSS:** `.c-bento__tile.c-bento{display:flex; flex-direction:column}` + `.c-bento__tile.c-bento > .c-bento__grid{flex:1 1 auto; grid-auto-rows:minmax(var(--bento-row-unit),1fr)}` — the row unit stays the **FLOOR**, so the fixed-row discipline that makes the span vocabulary visible is untouched and rows only grow into slack the parent already created. **MEASURED GREEN: dead band `[0,0,0]`, all four corners `20px` in console.** ⬛ **`s217-D6` (1) — GALLERY IN CONSOLE EXCLUDES KEYLINES COMPLETELY, AND ‘ABSENT’ IS ASSERTED ON THE NODE.** The control is **lifted out of the document** in console (an anchor comment holds its place) and put back in the other three; the state moves with it, so the export never carries a dial the page no longer offers. **Driven in all four themes** — `mono present · legacy present · console ABSENT · supercharge present` — because a rule that only ever runs where it excludes cannot be shown to be an exclusion rather than a deletion. ⚠ **THE COUNT IS THEME-DEPENDENT NOW AND THE PAGE SAYS SO ON ITS FACE:** **1017** reachable in mono / legacy / supercharge (display 54 · gallery **675** · dashboard **288**) and **639** in console (gallery drops to **297**). Both totals are printed, per type and per theme, and the probe asserts both appear in the shipped bytes. ⬛ **`s217-D6` (2) — SUB-BENTO SPACING IS A SNAPPING SLIDER over 1·2·4·8·12·16·20·24, replacing the three-stop control** (so **40px is no longer reachable on a sub-bento**). ⚠ **DRIVEN IN PIXELS, NOT IN STOP INDICES** — an index-only slider would be snapped by arithmetic that never happened and ‘snaps to’ would be unfalsifiable. **Ties go to the LOWER stop, DECLARED** so the selftest can hand-compute against a sentence. **EXPORT PARITY HELD:** driven **7→8 · 13→12 · 22→20 · 3→2 · 40→24 · 0→1** with the **thumb, the state object, the RESOLVED inner gutter and the EXPORT all landing on the same stop**; **eight declared CSS rules, one per stop**, never a `style=""` attribute. Chrome is neutral only — track, thumb, ticks and read-out are ink/line/surface, no red, no yellow, no green. ⬛ **PROBE, RE-DRIVEN GREEN OVER ALL 8 STATES** (4 themes × light/dark), font asserted against two controls; export parity **8 dials + 11 resolved values**; **zero dangling properties** over 14 canon properties × 8 states. ⬛ **TWO MUTATION ARMS, BOTH RED BY NAME AND NEITHER WITH COLLATERAL:** the legality arm (`--break-legality` / `--mutation`) **5 refusals red**; the NEW layout arm (`--break-layout` / `--layout-mutation`) puts **all three of Dave's symptoms back** — halved P4 container, literal dashboard span, and canon's nested fill undone from the page side — and **4 assertions go red by name** (2 composition, 1 orphan gap, 1 flat bottom at a re-measured **121px dead band**), with **0 non-layout failures**. ⚠ **ONE CANON EDIT, AND THE ORDERED SERIAL WAS RUN:** `gen_canon_bento.py --selftest` **12 bites green** → canon write → `gen_snippet_tokens.py --check` **0 changes** → `gen_theme_cascade.py --check` **in sync, 230 overrides / 386 projections** → `gen_showroom.py` **0 written** (showroom pages LINK canon.css, so nothing propagates into page bytes) → `gen_thumbs.py` for `foundation-bento` (**byte-identical**) → `gen_library_214.py` (**0 missing thumbnails**). ⛔ **NO TOKEN AND NO RULING WAS TOUCHED.** ⛔ **NOT committed, NOT pushed.** ⛔ **Nothing is ruled — `s217-D5`'s five open points are still open, and `s217-D6`'s own conductor's reading (that square-cornered themes keep the keyline control) is still Dave's to correct.** |
| **THE CROPPED KEYLINE, FIXED AT ITS CAUSE (Dave's third dashboard reading)** — `showroom/_foundations/bento.html` regenerated through `knowledge/_render/gen_foundations_217.py`; page body `knowledge/_render/gen_bento_matrix_217.py`; probe `knowledge/_render/verify_bento_matrix_217.py` extended (computed-style arm + a PIXEL arm); finding `ds-057` in `knowledge/_DS-IMPROVEMENTS.md`; two instrument potholes banked in `knowledge/_RUNBOOK-render-verify.md` | 2026-08-24 #217 · row `W-126` · **BUILT UNDER — `s217-D5`/`s217-D6`, NO NEW RULING** | ⬛ **AWAITING DAVE'S EYE.** His words: *“on – dashboard-keylines on – the corners are cropped so that the keylines are also cropped.”* ⛔ **CAUSE — THE EDGE LINE WAS BEING DRAWN BY THE WRONG BOX.** The dashboard role puts the theme radius on the CONTAINER and clips it (`s217-D2`/`s217-D3`), and the keylines were painted by the TILES: at 1px flush as `inset` right/bottom hairlines, at the open stops as a full border box. Either way the tiles at the last column and last row drew the GROUP'S OUTER EDGE — a straight line arriving at a curve — so the clip sliced it and every corner read with chopped line-ends. **THE RULE NOW IMPLEMENTED, ONE SENTENCE:** *with keylines ON in a dashboard bento the outer edge is the CONTAINER'S OWN 1px border, which curves with the radius by construction, and a tile only ever SEPARATES* — at **1px** the gutter stays and each tile paints it with an OUTSET shadow that the clip removes where it reaches the border band (P1's flush-tiles-with-hairline-separators reading is unchanged; what changed is who draws the line), and at **every wider stop** each tile keeps its FULL keyline box with the group's outer padding set to the gutter, so the boxes sit inset from the border by the same distance they sit from each other and nothing reaches the curve. ⚠ **ONE RULE, FOUR THEMES:** mono/legacy/supercharge resolve the container radius to 0 and the same border draws the same single rectangle — **measured in mono as well as console**, both regimes. ⚠ **AND IT IS A VISIBLE CHANGE IN THE SQUARE THEMES, DECLARED:** the pre-fix dashboard group had **no top or left edge at all** (only the last column's and last row's insets), so a group now reads as a CLOSED box where it used to read as two edges. That is the fix, not a regression — but it is Dave's eye that says so. ⛔ **A SECOND DEFECT FELL OUT OF IT, AND IT IS CANON'S OWN LESSON:** the wall keyline rules were DESCENDANT selectors (`.bm-wall .bm-tile`) and `.bm-outer` is also a `.bm-wall`, so a rule about the display wall reached every tile of every nested dashboard group and out-specified the dashboard's own by one attribute; all four re-scoped to `.bm-wall > .c-bento__grid > .bm-tile`. Logged with the corner class as **`ds-057`**. ⚠ **A THIRD THING THE SHORTER FIX WOULD HAVE SHIPPED, MEASURED AND REJECTED:** painting the 1px gaps as a line-coloured GRID GROUND is two lines shorter and renders a solid grey block through any group whose tiles do not fill its tracks — seen in mono, third group. The per-tile outset shadow has no such hole. ⚠ **PROBE, GREEN:** the computed-style arm drives **console (radius 20) and mono (radius 0) × sub-spacing 1px and 24px × 3 groups** and asserts the group's own border `[1,1,1,1]` + the clip + the radius, **no tile border and no INSET shadow at flush**, **every tile inset from the clip boundary by the gutter at the open stops**, and **no tile flush-with-its-own-border** (the double-stroke reading). ⬛ **AND THE SAME CLAIM IN PIXELS:** the group is shot and its rounded-rect path walked — **100% of 304 samples along the edge painted, 0 line pixels outside the curve, a 1px stroke at the left edge**, in both regimes. ⚠ **THE PIXEL ARM IS CONDITIONAL AND SAYS SO** — where Pillow is absent it reports **NOT RUN** rather than passing silently. ⬛ **MUTATION ARM RED BY NAME:** `--break-layout` now restores a FOURTH symptom (the exact pre-fix keyline declaration) and **74 layout assertions go red, with 0 non-layout failures**; the legality arm still goes red with its 5. ⚠ **Two INSTRUMENT potholes were found and banked, not worked around:** `element.screenshot()` rounds its frame and dropped the 1px bottom border of a box at a fractional `y` (read as a missing edge), and the **first** `full_page` capture reflows the page by 5px so a box measured beforehand addresses the wrong raster — the probe now warms up, re-measures after every capture and **refuses by name** if the subject moved. ⛔ **NO CANON EDIT, no token, no ruling** — the whole fix is page-local in the explorer's generator. ⛔ **NOT committed, NOT pushed.** ⚠ **Bounded verification (`s172-D3`):** the matrix selftest (30 bites), `gen_foundations_217 --check` in sync, and the four bento verifies (`matrix` · `canon` · `roles` · `foundations` · `tuner`) were re-driven green; the **full `_build_all.py` suite was NOT run to completion here** (it exceeds the sandbox call cap, and its one red in this environment — the state-contrast selftest — is the known missing-browser-env refusal, green with the env set). |
| **THE DOUBLE FRAME — THREE TREATMENTS INSIDE THE DASHBOARD PANE (Dave's fourth dashboard reading)** — `showroom/_foundations/bento.html` regenerated through `knowledge/_render/gen_foundations_217.py`; page body `knowledge/_render/gen_bento_matrix_217.py` (`SPREAD` / `spread_html` + the gating block of the page stylesheet); probe `knowledge/_render/verify_bento_matrix_217.py` extended (`SPREAD_GATE`, `SPREAD_C_EDGE`, four themes, a pixel arm over treatment C, and a fifth symptom on the `--break-layout` mutation arm) | 2026-08-24 #217 · row `W-126` · **PROPOSED, NO NEW RULING — nothing here is minted into canon** | ⬛ **AWAITING DAVE'S EYE, AND IT IS A PICK-A-LETTER.** His sighting: at dashboard sub-bento spacing ~24px with keylines ON, the bordered rounded group draws its frame AND every tile draws a full keyline box inset by the gutter — a frame inside a frame, reading as a heavy grey chrome band. **THREE TREATMENTS, SAME CONTENT, SAME SETTINGS, STACKED FULL WIDTH** (never halved — `ds-054`, and the probe asserts all three resolve the same column count, gutter and wall width): **A · TILES CARRY THE LINES** — the tiles keep their keyline boxes and the group's border DROPS, the group reading by clustering, by its own background against the page ground and by the radius clip; the group border returns at the 1px stop, where flush tiles cannot carry an outer edge. ⬛ **THE CONDUCTOR'S RECOMMENDATION, named on the page as a recommendation and not as a ruling.** **B · FRAME ONLY** — the group keeps its curved border and the tiles go bare, spacing alone separating them, so the wall reads as framed panels rather than framed cells. **C · GRID HAIRLINES** — no tile boxes; a 1px line centred in each gutter, overshooting into the border band so the container's own `overflow:hidden` stops it ALONG THE CURVE rather than by arithmetic. ⛔ **THE STRIP IS GATED, NOT PARKED.** It lives INSIDE the dashboard pane under the live wall and appears only at the state that produces the defect: keylines ON and sub-bento spacing above 1px. At the **1px stop it collapses to a note** saying the ruled behaviour (curved border + flush hairlines, clipped at the curve) stands as-is; with **keylines OFF it is absent entirely** and the single-treatment render Dave already has is untouched. ⛔ **ONLY PRESENCE IS WIRED TO THE DIALS, never styling** — every stage rule keys on `.bm-wall`/`.bm-inner`/`.bm-outer` and every spread rule on `.bm-sp-*`; the probe turns main spacing 24→40 and re-measures the three cases, so a leak would go red rather than turn the comparison into a comparison of settings. **EVIDENCE, ALL FOUR THEMES:** A group border `0` + tiles `[1]`; B group `1` + tiles `[0]`; C group `1` + tiles `[0]` + 24 hairline elements — pairwise distinct, at gutter 24, 3 columns, wall 1116px, with the expected group radius READ OFF the live dashboard group in each theme rather than typed. **C IN PAINT (console):** 100.0% of 240 samples along the group's own rounded edge painted, **0 line-coloured pixels outside the curve**, 31 painted samples in the interior gutter (a clip that ate the lines would also satisfy "nothing outside", so the interior is sampled too). **AT THE 1px STOP, with the strip in the document:** live group border `[1,1,1,1]`, gutter `1px`, 3 bare tiles each painting its separator — the ruled behaviour re-measured, which is the leak assertion for moving the strip into the pane. **MUTATION ARM:** `--break-layout` now also ships the spread UNGATED (strip always shown, cases up at the 1px stop) and the gate assertions go RED by name in all four themes — 90 layout assertion(s) fail against the mutant, and the real page is green. ⚠ **RESIDUAL:** the spread's ground follows the page-background selector (it is the page the dashboard stands on, not a property of any treatment); that is a change to the strip only, declared here rather than assumed. |
| **TREATMENT C, LIVE ON THE SLIDER — AND WHAT SUB-BENTO SPACING ACTUALLY DOES (Dave's fifth dashboard reading)** — `showroom/_foundations/bento.html` regenerated through `knowledge/_render/gen_foundations_217.py`; page body `knowledge/_render/gen_bento_matrix_217.py` (`GAPLINES`, `card_tile`, `sweep_html`, `sweep_reference_html`, `mainwall_html`, the shared hairline geometry and the live-wall C rules); probe `knowledge/_render/verify_bento_matrix_217.py` extended (`SWEEP_REPORT`, `CONVERGE`, `MAINWALL`, the rewritten keyline-edge arm, four themes throughout) | 2026-08-24 #217 · row `W-127` · **PROPOSED, NO NEW RULING — nothing here is minted into canon** | ⬛ **AWAITING DAVE'S EYE.** He picked **C (grid hairlines)** as the leading candidate and asked *"what happens with sub-bento spacing?"* — so **C now drives the LIVE dashboard wall at every one of the eight ruled `s217-D6` stops**, A and B stay in the spread below for comparison, and the page answers the question three ways. ⛔ **ONE CONSTRUCTION REPLACES TWO REGIMES.** The wall used to draw flush per-tile shadows at 1px and full tile keyline boxes above it; now no tile is ever boxed at any stop, the group pads **zero**, and each 1px line overshoots into the container's border band where the role's own `overflow:hidden` stops it **along the curve**. The double frame cannot occur at any stop. ⛔ **A HALF-PIXEL FINDING, AND IT IS THE HINGE.** The offset was `gutter / 2`; `right` measures to the LINE'S EDGE, so a 1px line's centre sat at `(g−1)/2` — **half a pixel off centre at every stop, and the previous probe asserted the error** (`lineRight == -gutter//2`). Corrected to `(gutter + 1px) / 2`, declared **once** and read by all three consumers (live wall, spread case C, sweep). **Measured worst centring error across all 8 stops × 4 themes: 0.00px**, and the centring is now measured from two tile boxes rather than read back off the offset. ⬛ **THE 1px CONVERGENCE — VERDICT: CONVERGENT, and the control is on the page.** At `g = 1` the centred line occupies `[edge, edge+1]`, exactly the band the ruled per-tile outset shadow painted. The sweep's last row draws the **ruled flush construction** at the same stop as a labelled control; the probe rasterises both, clips each to the group's padding box as the browser does, and differences them: **same tile boxes, ruled-only 0 px, stray 0 px, C paints 3026 px against the ruled 3023** — the only surplus is **3 crossing pinholes** (the 1×1 pixel where two gutters meet, which the per-tile shadow construction leaves open and C closes). Characterised, not budgeted: every surplus pixel must carry ruled paint on both axes. **In paint (console):** 100.0% of 304 samples along the group's rounded edge painted at both 1px and 24px, **0 line-coloured pixels outside the curve**, 1px stroke at the edge. ⬛ **THE SWEEP.** One group per ruled stop (1·2·4·8·12·16·20·24), labelled with its pixel value, **full width and stacked** (`ds-054` — eight walls in a row would each land near 140px and trip canon's 520px band; the probe asserts all rows share a column count and width). ⬛ **AND THE HONEST ANSWER THE SWEEP GIVES: the seam never changes weight.** One 1px hairline at 1px, one 1px hairline at 24px. What the slider moves is the **air around the line** — measured in console, the group grows **389px → 458px** across the ladder while a tile track narrows **754px → 747px**. Under C the sub-bento slider is a **rhythm control, not a line control**. ⚠ Two places for Dave's eye: an **orphan cell** (a group whose tiles do not fill its tracks) is bounded by a hairline running out into empty space and grows more conspicuous as the gutter widens; and between two groups the main gutter shows **two container borders with space between them**, which in the square themes is the heaviest seam on the wall. ⬛ **THE MAIN-WALL QUESTION — PROPOSED, NOT RULED: hairlines live INSIDE groups only.** The main wall's gutters stay line-free (space separates groups, lines separate tiles), drawn beside a **counter-example** that lines them. ⚠ **The counter-example costs a wrapper the proposal never pays.** A group is a clipped bento (canon's dashboard role — the clip is what makes the container radius read), so a hairline hung on a group is eaten by that group's own clip before it reaches the main gutter; lining the main wall needs an unclipped cell around every group. That cost is on the page as part of the argument. **Probe:** proposed 0 lines in the main gutter, counter-example 2, live wall 0 in the main gutter and 18 inside the groups — the absence asserted beside its presence, in all four themes. **MUTATION ARM:** `--break-layout` also suppresses the hairlines and restores the pre-fix keyline emission; **380 layout assertion(s) go red by name** against the mutant with no non-layout failures, `--break-legality` still red by name (5), and the real page is green in all four themes × light/dark. ⚠ **RESIDUALS, DECLARED:** (1) the previous row's evidence line for case C ("a 1px line centred in each gutter") was true of the intent and **half a pixel out in the enactment** — corrected here, and the assertion that baked it in is replaced by a measured one; (2) the sweep and the main-wall pair are **static specimens**, not dial-driven — only their presence is gated on `type=dashboard` + keylines ON, so no dial can restyle them; (3) **UNPROVEN BY SCOPE:** nothing here was driven below the 1100px band — the sweep is asserted consistent at the desktop viewport only, and C's behaviour at the 2- and 1-column bands is not measured. |
| **INNER BENTOS RUN THE SQUARING PASS (`s217-D7` ENACTED)** — canon `knowledge/canon/gen_canon_bento.py` (`inner_ladder`, `square_inner_wall`, **`square_nested_wall`** — the one-call entry point for a bento-of-bentos — + selftest bite 13); page body `knowledge/_render/gen_bento_matrix_217.py` (`inner_wall`, `card_spans`, `card_tile` now REQUIRES a minted span, the live wall / spread / sweep / main-wall call sites, the `--break-inner` arm); `knowledge/_render/gen_bento_roles_217.py` (the dashboard section mints both levels in one call); probes `knowledge/_render/verify_bento_matrix_217.py` (`INNER_GROUPS`, section 10, `--inner-mutation`) and `knowledge/_render/verify_bento_roles_217.py` (nested walls counted, not assumed); pages regenerated: `showroom/_foundations/bento.html`, `reviews/BENTO-CANON-2026-08-23-v3.html` | 2026-08-24 #217 · row `W-128` · **ENACTS `s217-D7`; no new ruling, nothing minted into canon.css** | ✅ **GREEN, AND FALSIFIABLE.** Dave: *"BTW inner bentos should run the squaring pass."* It was reaching the OUTER wall of the bento-of-bentos and stopping there. ⛔ **THE DEFECT, MEASURED BEFORE THE FIX:** the live wall's third group — three cards `(2×1, 1×1, 1×1)` in three tracks — left **2 empty cells of 6**, and under treatment C **3 of its 12 hairlines bounded that empty space**, a line separating a tile from nothing. The same shape sat in the spacing sweep (all 9 rows) and in the main-wall pair. ⛔ **FIXED AS A CLASS, NOT A CASE.** `square_nested_wall()` mints the outer wall AND every inner wall in ONE call, and `card_tile()` now refuses to draw without a span some caller obtained — and the only way to obtain one is the pass. Two call sites, or a span read off the authored literal, are what let the inner level go unasked in the first place. **GALLERY STAYS EXEMPT** (`s217-D3`/`s217-D5`) — the exemption is a role policy and a wall does not acquire squaring by being nested (canon bite 13h). ✅ **MEASURED AT THE RENDERED GEOMETRY, in all four themes:** 22 nested walls per theme, occupancy rebuilt from the used track sizes and the tiles' own boxes (never read back off `data-c`/`data-r`) — **0 empty cells, 0 double-claimed cells, 162 hairlines and 0 of them into empty space**. The roles page adds **96 nested-wall measurements, 0 empty cells**, across 4 themes × 2 modes × 4 widths. ⬛ **THE ARM WENT RED BY NAME:** `--break-inner` disables the nested pass ALONE (the outer wall stays squared) and **13 inner assertions fail, with 0 non-inner failures** — orphan counts and the hairline-into-empty-space symptom both named. ⚠ **WHAT MOVED VISUALLY:** the pass promotes each orphaned group's leading card from `2×1` to `2×2` (cheapest crop, the ordered cost), so those groups render one track taller and their bottom edge is now straight. ⚠ **UNPROVEN BY SCOPE:** the inner walls were measured at the desktop viewport only — the matrix explorer's nested walls are not driven at the 2- and 1-column bands (the mint-time ladder covers 3/2/1, but only the 3- and 2-column renders were measured live). |
| **THE NARROW BANDS — A HAIRLINE STRANDED BY A COLLAPSED BAND, AND THE COVERAGE GAP CLOSED (Dave's sixth dashboard reading)** — `showroom/_foundations/bento.html` regenerated through `knowledge/_render/gen_foundations_217.py`; page body `knowledge/_render/gen_bento_matrix_217.py` (the `.bm-mw-cell` gapline rules + a sixth `--break-layout` symptom); probe `knowledge/_render/verify_bento_matrix_217.py` extended (`NARROW_GUTTERS`, section 12: three narrow viewports × four themes + a pixel arm); `knowledge/_DS-IMPROVEMENTS.md` **`ds-058`** | 2026-08-24 #217 · **PROPOSED, NO NEW RULING — nothing here is minted into canon, no ruling written** | ⬛ **AWAITING DAVE'S EYE — AND THE DEFECT HE PHOTOGRAPHED IS NOT THE ONE THAT WAS BROKEN.** Dave's narrow screenshot showed *"two thin grey bands separated by white"* between stacked tiles. ⛔ **WHAT WAS GENUINELY BROKEN, FOUND AND FIXED AT CAUSE:** the main-wall counter-example suppressed its horizontal hairline with the rule `[data-axis="h"]{display:none}  /* one row: no horizontal gutter */` — **true at one band only.** Canon's 520px band collapses that wall to one column, its two cells STACK, and the counter-example drew **zero** lines in the only gutter it had. Fixed by deleting the assumption: both axes are emitted at every band and the overshoot is eaten by the clip, which is the SAME construction treatment C already uses on the live wall. ⛔ **THE DECLARED COVERAGE GAP IS NOW A GATE.** The prior pass wrote that C was *unmeasured below the 1100px band* and the defect lived exactly there; every assertion drove one 1280px viewport, where no band change could fail anything. The probe now drives **1000 / 700 / 500** in all four themes: for every pair of tile boxes that actually face each other across a gap — reconstructed from RENDERED geometry, never from `data-c`/`data-r` — exactly one 1px line, centred within ±1px, counted only where it survives the wall's clip; plus a pixel arm at 500px in console (one painted run per gutter, nothing outside the curve) and a named refusal if the live wall renders no hairline at all, so the probe cannot pass by finding nothing. **Green: 18 walls, 90/90/65 adjacent pairs per band, four themes.** The pre-fix rule is `--break-layout`'s sixth symptom and the arm goes red on its own name. ⬛ **WHAT DAVE ALMOST CERTAINLY SAW IS RULED CONSTRUCTION, AND IT IS HIS TO RULE, NOT MINE TO CHANGE.** At the collapsed band the outer wall stacks the three GROUPS, and each group draws its own 1px border (`s217` — "the rounded group draws its own outer edge"); so the main gutter renders **border · space · border** while every gutter around it renders one centred hairline. With main spacing and sub-bento spacing both at 24px the two gutters are the same width, so the eye compares them directly and the border pair reads as a doubled hairline. **THE QUESTION FOR DAVE: may the main wall share a gutter width with the sub-bento wall at all?** — a wider main gutter, a different main-gutter treatment at the collapsed band, or leave it. ⚠ Not committed, not pushed. |
| **KEYLINES HUG THE MODULES (`s217-D8` ENACTED) — THE CENTRED-GUTTER CONSTRUCTION RETIRED** — `showroom/_foundations/bento.html` regenerated through `knowledge/_render/gen_foundations_217.py`; page body `knowledge/_render/gen_bento_matrix_217.py` (the live-wall keyline rules split into two regimes, the A/B/C spread and `spread_html`/`SPREAD` deleted, the sweep rebuilt, `sweep_reference_html` and `mainwall_html(True)` deleted, new `--break-keylines` arm); probe `knowledge/_render/verify_bento_matrix_217.py` (keyline arm rewritten, `GUTTER_SCAN` added, `SPREAD_REPORT`/`SPREAD_C_EDGE`/`CONVERGE` deleted, `--keyline-mutation`) | 2026-08-24 #217 · row `W-127` · **ENACTS `s217-D8`; no canon edit, nothing minted** | ⬛ **AWAITING DAVE'S EYE.** His words: *"the keylines should stay, but they should go round tight to the modules not run down the middle of the spacing"*. ⛔ **WHAT THE DASHBOARD DRAWS NOW.** **Above 1px** every TILE wears its own tight 1px keyline box and the GROUP'S OUTER BORDER STEPS BACK — with the tiles carrying edges a group frame would re-create the double frame he rejected earlier in the same sitting; the group still expresses itself through its background and its clipped radius, and the tiles sit **inset from the container by the gutter** so no straight tile edge ever arrives at the curve. **At 1px, unchanged:** the rounded group border with flush tiles, the gutter itself the hairline, stopped along the curve by the container's own clip — the only stop on the pane where a line element renders at all. ⛔ **RETIRED, NOT RE-CAPTIONED:** the **A/B/C decision spread** (a decision surface for a settled question keeps a retired construction alive on the page) — replaced by a compact **ruled-behaviour note**, still conditional on the same two dials, one regime note on screen at a time; the sweep's **1px convergence control row** (there is no C to converge on — the strip's own 1px row IS the flush construction); and the main wall's **counter-example** (it drew a hairline down the middle of the main gutter). The main-wall inside-only **proposal stands**, alone. The **sweep survives** — the rhythm story is a property of the ladder, not of the construction — and now draws the ruled construction at every stop. ⬛ **MEASURED, FOUR THEMES × LIGHT/DARK, GREEN:** at stops **4 / 12 / 24** every tile border `[1,1,1,1]`, every group border `[0,0,0,0]`, group padding == the gutter, the clip and the token radius still in force, and **13 walls / 79 gutters scanned with 0 line nodes in any of them and 0 rendered lines on the pane**; at **1px** group border `[1,1,1,1]`, gutter 1px, bare tiles, the hairline pair per tile, clipped along the curve. ⛔ **THE ABSENCE IS ASSERTED TWICE — BY NODE AND BY PAINT**, because a retired line re-drawn as a background would satisfy every node assertion: the console pixel arm reports **0/121 gutter-centre samples painted across 3 bands with 55 tile-edge samples painted** at sub=24 (the presence beside the absence), and **100.0% of 304 edge samples, 0 outside the curve, 1px stroke** at sub=1. ⚠ **AND THE REFERENCE COLOUR WAS A MEASURED TRAP:** the pixel arm took its discriminator from the GROUP's `borderTopColor`, which resolves to `currentColor` (near-black) once the border is 0 — it reported a painted tile box as unpainted; it now reads the TILE's own border colour. ⛔ **NARROW BANDS DRIVEN (1000 / 700 / 500), four themes:** at sub24 **13 walls, 75/75/50 adjacent pairs, 0 lines in any gutter, every tile boxed**; at sub1 **15/15/11 pairs, one 1px hairline in each**; narrow pixel arm at console 500px **0 painted runs in any gutter interior, 9 tile-edge samples painted, 0 outside the curve**. ⚠ **A WALL'S REGIME IS ITS OWN GUTTER, NOT THE PAGE'S DIAL** — the sweep shows every ruled stop at once, so its 1px row legitimately renders hairlines while the live wall sits at 24px; a pane-wide count that ignored that failed the RULED page (measured, 12 lines) and the probe now judges each wall by its own resolved gutter. ⬛ **FOUR MUTATION ARMS, ALL RED BY NAME, NO COLLATERAL:** the NEW `--break-keylines` / `--keyline-mutation` restores the retired centred line at the open stops and **342 assertions named for `s217-D8` go red with 0 unnamed failures**; `--break-layout` **103**; `--break-inner` **10**; `--break-legality` **5**. ⚠ **THE MUTANTS WERE STALE AND IT SHOWED** — the legality arm first ran against a pre-change page and reported s217-D8 failures; all four were rebuilt before the readings above. ⬛ **SELFTEST 51 BITES GREEN**, and the bites that asserted the retired construction are **named in the file rather than dropped** (20/20a–20h the spread, 22 "the live wall draws C", 23b the control row, 24/24a the counter-example). ⛔ **NO CANON EDIT AND NO ORDERED SERIAL WAS NEEDED** — grepped: canon carries no gutter-line emission at all (`gapline` absent from `knowledge/canon/`), and `gen_bento_roles_217.py`'s dashboard already draws tile-hugging borders with the group expressed by background alone, i.e. the s217-D8 construction. ⛔ **NO TOKEN, NO RULING, NOT COMMITTED, NOT PUSHED.** ⚠ **RESIDUALS:** (1) the retired `.bm-gapline` machinery REMAINS in the page for the 1px regime — its nodes exist on every card tile and are `display:none` above 1px, so the node-level absence is "zero RENDERED lines", not "zero nodes", and the paint arm is what closes that gap; (2) `reviews/BENTO-CANON-2026-08-23-v3.html` was NOT regenerated — it needed no change, but it has not been re-driven under this pass; (3) the full `_build_all.py` suite was not run (sandbox call cap) — bounded verification per `s172-D3`: matrix selftest, `gen_foundations_217 --check`, and the matrix probe in all four themes plus all four arms. |
| **THE FOUNDATIONS > GRIDS GROUP (`s218-D2` ENACTED)** — `showroom/_foundations/grids-12col.html` + `grids-display.html` + `grids-gallery.html` + `grids-dashboard.html` (thumbs beside each); generator `knowledge/_render/gen_grids_218.py`, probe `knowledge/_render/verify_grids_218.py`; written through `knowledge/_render/gen_foundations_217.py` and wired into the library by `knowledge/_render/gen_library_214.py` | 2026-08-24 #218 · row `W-136` · **ENACTS `s218-D2`; no canon edit, nothing minted** | ⬛ **AWAITING DAVE'S EYE.** Four pages under Foundations > Grids: the 12-column baseline plus one page per bento role (display · gallery · dashboard). **Controls kept** on each page, **ONE data path** (the grids pages and the matrix explorer read the same source), and the **explorer itself is untouched**. ⬛ **MEASURED:** the sub's probes ran green; the `--break-groups` mutation arm went **RED on 5 assertions by name, 0 unnamed**. ⚠ **THE FIRST ARM RUN EXPOSED TWO PROBE HOLES AND BOTH WERE FIXED AT CAUSE:** a split on an **absent marker** (the probe read a page that never carried the token and passed), and a **mutant-dangle reading `0 == 0`** — a 0px console radius now REFUSES rather than agreeing with its own mutant. ⛔ **NOT RULED, NOT SIGNED OFF, NOTHING CLOSED.** |
| **PHOTOGRAPHY, PER-THEME (`s218-D3` ENACTED) — AND THREE CALLS COME WITH IT** — `showroom/_foundations/photography.html` (lazy-loaded) + `showroom/_thumbs/foundation-photography.png`; manifest `knowledge/_PHOTOGRAPHY-MANIFEST.md` + `.json` via `knowledge/_build_photo_manifest.py`; probe `knowledge/_render/verify_photography_218.py`; source receipt `notes/_receipts/2026-08-24-218-photography-theme-settings.md` (Dave's four verbatim exports) | 2026-08-24 #218 · row `W-137` · **ENACTS `s218-D3`** | ⬛ **AWAITING DAVE ON THREE NAMED CALLS.** **(1) The mono caption shade is PROPOSED, not ruled** — `#1A1A1A` on white, his eye owed. **(2) Five derivatives are OVER THE 300KB CEILING** and are flagged `over_target` in the manifest rather than silently re-encoded — a spec call, his. **(3) `edge:square` is RECORDED, NOT ENACTED** — reopen or leave it. ⬛ **MEASURED:** **ALL 251 derivatives minted, 42.1MB**, page lazy-loaded. ⛔ **NOTHING HERE IS RULED BY THE WRAP.** ⛔ **MARKED AT THE #218 SECOND WRAP, BY ADDITION — ALL THREE CALLS WERE ANSWERED BY DAVE AT THE DECISION SITTING, `s218-D6`, WHOSE OWN WORDS ARE QUOTED FROM THE STORE AND NOT PARAPHRASED:** **(1)** ~~PROPOSED~~ → **RATIFIED** — *"the mono caption ground is RATIFIED at the proposed shade: `#1A1A1A` via `--surface-digital-black` with `--text-reverse` white ink"*, and *"the `s218-D3` PROPOSED marker on the shade is discharged"* (his word: *"Yes — that's the one"*). **(2)** ~~a spec call, his~~ → **ACCEPTED AS FLAGGED** — the 300KB ceiling stays the rule and the `over_target` flags stay honest (*"Accept as flagged"*). **(3)** ~~RECORDED, NOT ENACTED~~ → **ENACTED** — the gallery wall is squared and 4 tiles re-spanned (*"Reopen — square it"*), ⚠ **SCOPED to the photography page's wall; the GALLERY ROLE's `s217-D3` exemption elsewhere is expressly untouched until he says wider.** ⛔ **THE ROW STAYS ⬛ AWAITING AND IS NOT CLOSED:** the three CALLS are answered, **Dave's eye over the page itself is not**, and a wrap closes no row of his. |
| **THE SPECIMEN IS PINNED BY NAME — AND THE OLDER REVIEW ARTEFACTS WERE NOT REGENERATED** — `knowledge/_render/gen_bento_roles_217.py` (`SPECIMEN_FILES`, plus the canon generator's private reader repointed at it) | 2026-08-24 #218 · row `W-137` · **CONDUCTOR'S CORRECTION, after the photography sub returned** | ⚠ **DECLARED, NOT AWAITING A RULING.** The photography sub's **filename-slice cap had swapped 14 of the 15 photographs** on the live decision surfaces. The specimen is now **PINNED BY NAME** to the ratified `s217-D1` set, verified identical, and the selftests plus **both photo-bearing grids pages were re-driven green**. ⛔ **THE FULL MATRIX RE-VERIFY AFTER THE PIN WAS NOT RE-RUN** — bounded verification (`s172-D3`), declared: the pin restores the exact files the green morning run loaded. ⚠ **AND THE STALE REVIEW ARTEFACTS `reviews/BENTO-CANON-2026-08-23-v2.html`, `-v3.html` and `reviews/GALLERY-COMPARE-2026-08-23-v1.html` RENDER THE PINNED SPECIMEN ONLY IF REGENERATED — THEY WERE NOT REGENERATED.** Named so it is countable, not repaired here. |
| **THE SIX PHANTOM SURFACES, BUILT PROPOSED — AND THE SEVENTH REFUSED (`s218-D4`(1) ENACTED)** — receipt `notes/_receipts/2026-08-25-phantom-surfaces.md`; brief `notes/_briefs/2026-08-25-218-phantom-surfaces-brief.md`; probe `knowledge/_render/verify_phantom_surfaces_218.py`; the refused seventh is `showroom/confirmation.html` | 2026-08-25 #218 (**SECOND wrap**) · **ENACTS `s218-D4`(1)** | ⬛ **AWAITING DAVE'S EYE — SIX DESIGNS, EACH LABELLED `PROPOSED` IN ITS OWN FILE HEADER.** Headers More-options (APG menu button) · Navigations Search (disclosure) + Account (menu) · Avatar-group "+N" overflow list · Standing-order/mandate-row "Manage" inline action · Kpi-tile table CTA (**panel DRAWN, nothing wired**) · Timeline Load-older. ⛔ **THE SEVENTH WAS REFUSED, NOT DEFERRED:** Confirmation's "Replay" is **not** a phantom — the button is real and lives in the showroom bar, so `#98-D1` says the honest deliverable was to **not build it**. `s218-D4`(1) ruled these **per-component design pieces, proposals to his eye, never one class patch**. ⛔ **NOTHING HERE IS RULED, SIGNED OFF OR CLOSED BY THE WRAP.** |
| **THE FROZEN ITINERARY COLUMN, FENCED AT THE WRITER — v4 EMITTED BESIDE AN UNTOUCHED v3** — `reviews/ITINERARY-STATUS-2026-08-25-v4.html` (v3 deliberately unchanged, both readable side by side); writer key `itinerary_status_2026_07_14_FROZEN`; filed report `notes/_subreports/2026-08-25-218-cA-filed-reports.md` (row `W-159`) | 2026-08-25 #218 (**SECOND wrap**) · row `W-159` · conductor accepted **cA-Q1(a)** | ⬛ **AWAITING DAVE ON THREE OF cA's FOUR QUESTIONS** — **Q2 the citation-gate promotion** (`s218-D7` says the wrap *REFUSES* an uncited report; `_capture_gate.py:4244` carries `SUBREPORT_CITE_BLOCKING = False` and only WARNS — **promotion is his word**) · **Q3 the build wiring** · **Q4 the doc-row untracked scope**. ⚠ **THE FINDING BEHIND IT:** the frozen column produced the **78-gap wave-3 premise, which measurement DISCHARGED — all 17 components pre-existed GATED**; this is the **FIFTH surface of the premise-ages-faster-than-rule class and the conductor's own miss**. ⛔ **v3 is NOT superseded by an edit — v4 sits beside it**, which is what makes the two readings comparable. |
| **THE QUALITY-FORMS LANE — 8 LIVE DEFECTS FIXED AT CAUSE, AND A KEYBOARD-UNUSABLE CONTROL** — filed report `notes/_subreports/2026-08-25-218-cB-quality-forms.md` (row `W-160`); brief `notes/_briefs/2026-08-25-218-quality-wave-forms-brief.md`; probe `knowledge/_render/verify_quality_forms_218.py` (56 checks + 16 break arms, two-sided); evidence `notes/_subreports/assets/2026-08-25-218-cB-quality-forms/` | 2026-08-25 #218 (**SECOND wrap**) · row `W-160` · `COUNTS: findings 11 · ruling-shaped 3 · UNPROVEN 4` (**copied off the file**) | ⬛ **AWAITING DAVE ON THREE QUESTIONS** — **Q1 the link composite · Q2 the Empty-state tightening · Q3 its third**. ⚠ **MEASURED AND FIXED AT CAUSE, NOT PATCHED:** eight live defects, of which **Date-range was KEYBOARD-UNUSABLE**; the `{once:true}` class was found **live in 3 more files** and fixed there too; the type ratchet moved **1097 → 1093** (shrink-only, and it shrank). ⛔ **The fixes are enacted; whether they survive his eye is not settled here, and no row of his was closed.** |
| **THE #219 SITTING INDEX v2 — 33 CALLS, AND THE DAY PIVOTED BEFORE HE WORKED IT** — `reviews/SITTING-219-2026-08-25-v2.html` (v1 `reviews/SITTING-219-2026-08-25-v1.html`, 34 calls, kept beside it); filed report `notes/_subreports/2026-08-25-219-sitting-index.md` (`COUNTS: findings 6 · ruling-shaped 0 · UNPROVEN 3`, copied off the file) | 2026-08-25 #219 · commit `8486e09` | ⬛ **AWAITING DAVE — HIS OWN QUEUE, UNWORKED.** 33 calls after the ones `s219-D1`/`D2`/`D3` struck; the `.lg` corner call is among them. ⛔ The release lane took the day by his own word, so **nothing here was ruled, closed or re-scoped at the wrap.** |
| **THE BENTO DECISION PAGES, RE-CUT POST-#218 — AND HIS COMPLAINT QUANTIFIED** — `reviews/BENTO-CANON-2026-08-25-v7.html` (v4/v5/v6 kept beside it) · `reviews/GALLERY-COMPARE-2026-08-25-v3.html` · `reviews/SQUARING-PORTRAIT-2026-08-25-v1.html`; filed report `notes/_subreports/2026-08-25-219-recut-bento-surface.md` (`COUNTS: findings 8 · ruling-shaped 8 · UNPROVEN 4 · pages emitted 3 · ledger rows 12`, copied off the file) | 2026-08-25 #219 · commit `04655de` | ⬛ **AWAITING DAVE'S EYE ON ALL THREE.** The caption measurement is his complaint made countable — **38px → 86px**. ⚠ **SQUARING-PORTRAIT is evidence, not a proposal**, and the orphan/flattening refinement (the `W-99zi` third bend) stays **UNRULED**. ⛔ Eight ruling-shaped questions come back in the report and **not one was answered here.** |
| **THE SEGMENTED-CONTROL ADOPTION GAP — DAVE'S OWN CATCH, AND THE MISSING BASE MINT RESTORED** — `reviews/SEGMENTED-ADOPTION-2026-08-25-v1.html`; filed report `notes/_subreports/2026-08-25-219-lane1-segmented-adoption.md` (`COUNTS: findings 9 · ruling-shaped 5 · UNPROVEN 3`, copied off the file) | 2026-08-25 #219 · commit `d178313` | ⬛ **AWAITING DAVE — RULING-SHAPED Q1 IS OPEN** and four more behind it. Radii were minted and never consumed; the missing `s202-D1` base mint was restored **from his own tuner numbers**, not invented. ⛔ **No dial was re-picked at the wrap.** |
| **THE UNCONSUMED-MINT INVENTORY — AN ADVISORY GATE, NOT A VERDICT** — `reviews/UNCONSUMED-MINTS-2026-08-25-v1.html`; gate `knowledge/_gate_minted_consumption.py` (**ADVISORY, and it stays advisory — promotion is his word**); filed report `notes/_subreports/2026-08-25-219-lane5-unconsumed-mints.md` (`COUNTS: 9 findings · 5 ruling-shaped · 4 UNPROVEN · 0 class-(a) wirings · 3 class-(b) · 3 class-(c) · 1 gate built (advisory, 10 arms) · 1 review page · 3 store rows · subs 0`, copied off the file) | 2026-08-25 #219 · commit `d11b6a6` | ⬛ **AWAITING DAVE ON ROWS A AND B** — the report's own REPLAY-THESE says *open it, do not read it*. ⚠ **An unconsumed mint is not a defect on its face**, which is exactly why the gate reports and never blocks. |
| **THE BENTO EDIT RAILS IN THE LIBRARY — AND THE CARD HE SAID WOULD NEVER EXIST IS STILL ON IT** — `showroom/_foundations/bento-rails.html` (library at 143 components, `gen_library_214.py --check` in sync); manifest `knowledge/_render/_bento_edit_rails.json`; filed reports `notes/_subreports/2026-08-25-219-enactA-rails.md` · `-enactB-defaults.md` · `-enactC-chords.md` | 2026-08-25 #219 · **ENACTS `s219-D1`/`D2`/`D3`** · commits `11b5779` + `71bb2f7` | ⬛ **AWAITING DAVE — AND ONE CORRECTION OF HIS IS RECORDED BUT NOT ENACTED.** His words — *“its ether cohesive capsule or rounded full image if there is no background colour on the caption”* — live at `notes/_receipts/2026-08-25-219-role-defaults-exports.md` § **PARKED CORRECTION**; **the page still shows the card he said “would never exist.”** ⛔ **The whole bento lane is PARKED BY HIS WORD behind the release**, so this was not repaired at the wrap. ⚠ **Mono's access to the dark-caption chord is expressly OPEN** (`s219-D3`(3)). |
| **THE CAPTION DARK LADDER — DAVE'S COMPLAINT DRAWN AS RUNGS, AND HIS PICK BECAME `s220-D1`** — `reviews/CORRECTION-READINGS-2026-08-27-v1.html` (three readings of his parked capsule-chord correction, rendered side by side) · `reviews/CAPTION-DARK-LADDER-2026-08-27-v1.html` (the console-dark ladder he picked from); filed reports `notes/_subreports/2026-08-27-220-readings-capsule.md` (`COUNTS: findings 9 · ruling-shaped 6 · UNPROVEN 3`, copied off the file) · `notes/_subreports/2026-08-27-220-caption-dark-ladder.md` (`COUNTS: findings 11 · ruling-shaped 6 · UNPROVEN 4`, copied off the file) | 2026-08-27 #220 · rows `W-210` / `W-208` · **the surfaces `s220-D1` and `s220-D2` were ruled off** | ⬛ **AWAITING DAVE'S EYE ON THE ENACTMENT, NOT ON THE RULE.** The rule is his and inscribed; what is proposed to his eye is the per-theme derivation below. ⚠ **MONO'S GALLERY DEFAULT IS EXPRESSLY OPEN** — `s220-D2` set the console gallery default and left mono unruled; **his one word settles it.** |
| **THE RUNG ENACTED — CONSOLE-DARK CAPSULE CAPTION GROUND → NEUTRAL/5 `#313131`** — `reviews/RUNG-ENACT-2026-08-27-v1.html`; mint site `CAPTION_GROUND_MINTS`; filed report `notes/_subreports/2026-08-27-220-rung-enact.md` (`COUNTS: files changed 8 (+394 / −9) · new selftest bites 6 (matrix) + 1 (foundations) · verifier states driven 40 · findings 7 · ruling-shaped 5 · UNPROVEN 4`, copied off the file) | 2026-08-27 #220 · row `W-214` · **ENACTS `s220-D1`** · commit `7debc1a` (PROPOSED) → `707e5aa` | ⬛ **AWAITING DAVE'S EYE.** Dark-only, caption-only, console-only at the rung; the all-theme extension is the row below. |
| **THE THEME LIFT — ALL FOUR THEMES, BY LIFT NOT HEX (`s220-D1` ENACTED)** — `reviews/THEME-LIFT-2026-08-27-v1.html`; filed report `notes/_subreports/2026-08-27-220-theme-lift.md` (`COUNTS: files changed 11 (+617 / −142) · new files 14 · matrix bites 100 → 103 · foundations bites 40 → 41 · verifier states driven 8 + 24 · mutation arms 7 · findings 8 · ruling-shaped 5 · UNPROVEN 3`, copied off the file) | 2026-08-27 #220 · row `W-216` · **ENACTS `s220-D1`** · commit `707e5aa` | ⬛ **AWAITING DAVE'S EYE ON THE DERIVED VALUES.** mono `#313131` · legacy `#313131` · console `#313131` (all neutral/5) · supercharge `#312C26` (warm/5, dL* +9.08, **correcting an INVERSION** where the caption sat darker than the page). **The RULE is his and is not in question; the per-theme picks are PROPOSED.** |
| **THE DEFAULT SWITCH — CONSOLE GALLERY = ROUNDED-CORNER IMAGE + TRANSPARENT CAPTION, BOTH MODES (`s220-D2` ENACTED)** — `reviews/DEFAULT-SWITCH-2026-08-27-v1.html`; filed report `notes/_subreports/2026-08-27-220-default-switch.md` (`COUNTS: files changed by this lane 6 + 1 new page + 13 new asset files · matrix bites 103 → 104 · foundations bites 41 → 44 · role-defaults selftest clauses +7 · browser verifier states driven 38 · mutation arms 5 · findings 7 · ruling-shaped 4 · UNPROVEN 3`, copied off the file) | 2026-08-27 #220 · row `W-217` · **ENACTS `s220-D2`, superseding `s219-D2`(3)'s capsule default** | ⬛ **AWAITING DAVE'S EYE.** ⚠ **MONO IS EXPRESSLY OPEN AND STAYS OPEN** — the ruling set console only. ⛔ **The #219 PARKED CORRECTION is discharged BY ADDITION, never by removal:** all four caption treatments stay legal per `s220-D2`(1), and the receipt cites the ruling. |
| **THE #220 CONSOLIDATED SITTING PAGE — 122 CALLS, 97 OPEN, 25 CANDIDATE-CLOSED, NOTHING CLOSED** — `reviews/SITTING-220-2026-08-27-v1.html`; filed report `notes/_subreports/2026-08-27-220-audit-L5.md` (`COUNTS: findings 9 · ruling-shaped 3 · UNPROVEN 4`, copied off the file) | 2026-08-27 #220 · rows `W-225` / `W-223` | ⬛ **AWAITING DAVE.** 33 RULING-SHAPED QUESTIONS sections across 31 reports were read mechanically by heading, plus the standing 33 cards of `reviews/SITTING-219-2026-08-25-v2.html` (`W-184`). ⛔ **NOTHING WAS CLOSED AND THE 25 CANDIDATES STAY CANDIDATE** — each is a struck card carrying the clause or probe that answers it, **probed in the tree today**, and one of them found a licence word homed only in a code comment, which is why `s220-D3` exists. |
| **THE SIX-LANE AUDIT WAVE — WHAT THE MACHINERY ACTUALLY DOES WHEN YOU DRIVE IT** — filed reports `notes/_subreports/2026-08-27-220-audit-L1.md` (findings 18 · ruling-shaped 5 · UNPROVEN 4) · `-audit-L2.md` (14 · 5 · 3) · `-audit-L3.md` (12 findings filed, 4 HIGH, 3 red flags) · `-audit-L4.md` (19 findings, 4 blocking a designer's first hour) · `-audit-L5.md` (9 · 3 · 4) · `-audit-L6.md` (19 findings · 12 priced trims · 5 ruling-shaped); divvy `notes/_briefs/2026-08-27-220-audit-wave-divvy.md` | 2026-08-27 #220 · rows `W-218`…`W-224` | ⬛ **AWAITING DAVE — AND NOT ONE FINDING WAS REPAIRED HERE.** ⛔ **CI is RED at the FIRST `gates` step since `aa26947`** — two sessions of gate bites unrun in CI (L1, CLAIMED via a local bare-clone proxy), and his read-back queue is still blocked at his GitHub sign-in. **Every lane's repairs are next-session lanes, by the wrap's own fence.** |
| **THE MONO GALLERY-DEFAULT CANDIDATE — HIS CONDITIONAL SENTENCE, AND BOTH PREMISES VERIFIED TRUE AT HEAD** — `reviews/MONO-GALLERY-DEFAULT-2026-08-27-v1.html`; filed report `notes/_subreports/2026-08-27-221-mono-specimen.md`; evidence `notes/_subreports/assets/2026-08-27-221-mono-specimen/` | 2026-08-28 #221 · rows `W-233` / `W-234` · **NOTHING RULED — `s221-D1` DOES NOT EXIST IN `knowledge/_rulings.json`, READ AT THIS WRAP** | ⬛ **AWAITING DAVE — HIS ONE WORD, AND IT IS STILL THE FIRST THING #222 OWES HIM.** His sentence, verbatim: *“there are no rounded corners in mono but I think they share the same neutral ramp so the colours can be the same if this is true.”* ⚠ **BOTH PREMISES VERIFIED TRUE AT HEAD, BY THE PAGE'S OWN COMPUTED CHIPS, NEVER TYPED:** neutral/5 resolves `#313131` **byte-identical** in mono and console, and mono's `--border-radius-container` resolves **0 in both modes** (console's 20px is the `#199` override). ⛔ **The page is a DECISION SURFACE ONLY** — console's ruled default beside the MONO CANDIDATE beside MONO TODAY, light and dark. **No ruling, no default, no token and no canon file was touched, and `s220-D2` leaves mono EXPRESSLY OPEN.** |
| **THE v-NEXT RELEASE PLAN — EVERY LOOSE END IN PRIORITY AND DEPENDENCY ORDER, NOTHING IN IT DECIDED** — `notes/_briefs/2026-08-28-222-release-plan-v1.md`; filed reports `notes/_subreports/2026-08-28-222-encoder-vendoring.md` · `notes/_subreports/2026-08-28-222-fallback-encoder.md` · `notes/_subreports/2026-08-28-222-route-a-report.md` | 2026-08-28 #222 · row `W-249` · rulings `s222-D2` · `s222-D3` (READ from `knowledge/_rulings.json` at this wrap) | ⬛ **AWAITING DAVE — FIVE DECISIONS, PLUS ONE COUNT AND ONE TEST.** Route (a) retro-confirm (item 1, already in the tree and needing his yes or his stop) · the version number (item 6) · his fresh ratifying word and whether ratify stops keying to a `#219` id (item 7) · the blind delta-gate arm — repair, re-scope or park (item 8) · his eye on the release page and the word “bake” (item 11). **Plus item 5's roster count under `s219-D9` if it still moves, and item 12 — his fifteen minutes in Copilot, after.** ⛔ `s222-D3`(2): the release outranks all other lanes until baked. |
| **THE MONO GALLERY-DEFAULT CANDIDATE — ✅ ANSWERED, BY ADDITION AND NEVER BY REMOVAL** — `reviews/MONO-GALLERY-DEFAULT-2026-08-27-v1.html` | 2026-08-28 #222 · ruling `s222-D1` (READ from `knowledge/_rulings.json`) · enacted at commit `d4e69d0` · filed report `notes/_subreports/2026-08-28-222-mono-default-switch.md` (row `W-242`) | ✅ **DAVE'S WORD GIVEN:** *“yes the defaults for mono are fine”*, then on the reflected reading *“yes this is correct”* ⇒ **square image + transparent caption, console's colours, BOTH MODES**; the grey caption ground **stays an edit-pass option** and nothing leaves the grammar. ⚠ **THE ROW ABOVE IS LEFT STANDING, NOT EDITED OR DELETED** — this row answers it [[home-by-addition-then-cut]]. ⬛ **STILL OPEN AND HIS: the mono DARK CHORD**, which `s222-D1` did not touch. |
| **THE WRAPPER DIET — FOUR MEASURED READINGS, ONE GREEN, AND NOTHING ADOPTED** — `reviews/WRAPPER-DIET-2026-08-30-v1.html`; filed report `notes/_subreports/2026-08-30-226-wrapper-diet-pricing.md` (`COUNTS: findings 12 · ruling-shaped 4 · UNPROVEN 4`, copied off the file) | 2026-08-30 #226 · ⛔ **NO ROW YET — the six #226 documents are UNROWED and the conductor mints them at the #227 opener (`s218-D7`)** | ⬛ **AWAITING DAVE'S POINT.** The wrapper is **13,061 tape**, **96.1% `state_block()`** (11,407 of it his 125 rows) against **506** of prose, so *“cut the prose”* is arithmetically impossible. **A do-nothing 55.82% RED · B prose diet 55.19% RED · C OPEN-WORK→ids-only pointer 29.61% GREEN (0 red lines) · D titles-kept 41.31% RED · D′ 39.20%, green by 0.80 pts.** ⛔ **DECISION SURFACE ONLY — nothing was adopted, the `0.40` floor was not touched, and C's cost is a real legibility loss at the opener that only he can price.** |
| **DREAM PASS 10 — THE DIGEST, AND A SEQUENCING TRAP IN P4** — `reviews/DREAM-PASS-10-DIGEST-2026-08-30-v1.html`; filed report `notes/_subreports/2026-08-30-226-dream-pass-10-digest.md` (`COUNTS: findings 8 · ruling-shaped 9 · UNPROVEN 4`, copied off the file) | 2026-08-30 #226 · ⛔ **NO ROW YET — same `s218-D7` beat as the row above** | ⬛ **AWAITING DAVE. P1–P4 ALL STILL FLOATED and nothing was promoted.** P4 is part-answered by `s225-D1`, but `RATIFY_IDS` still points at `s224-D1`: ⛔ **building P4(b)'s guard BEFORE moving the key would kill the next `--release`** (`build-designer-pack.sh:152`), so **the ORDER is the decision and it is his.** ⚠ P3's *“minimum”* option is the act `s125-D1` forbade — its own `watch` field says so. ⚠ **Light/dark render UNPROVEN (disk full)** and proven structurally instead — declared, never claimed. |
| **THE DASHBOARD REGEN COMPARE — HIS RESCUED ARTEFACT BESIDE A BY-THE-BOOK REBUILD** — `reviews/DASHBOARD-REGEN-COMPARE-2026-08-30-v1.html`; filed report `notes/_subreports/2026-08-30-227-dashboard-regen-diff.md` (`COUNTS: findings 25 · generation-defects 3 · canon-gaps 14 · dave-improvements 8 · UNPROVEN 4`, copied off the file) | 2026-08-30 #227 · rows minted `W-275`…`W-300` | ⬛ **AWAITING DAVE — AND THE LANE'S OWN PREMISE WAS CORRECTED BY HIM MID-FLIGHT.** His international-banking dashboard was built **COLD by Sol with no skills loaded**, which turns the comparison from *“was the system used right”* into *“what does a cold start reach”*. ⛔ **THE THEME QUESTION is the hinge and it is his.** |
| **THE DASHBOARD-DIFF DECISION PAGE — 32 CARDS, UNWORKED** — `reviews/DASHBOARD-DIFF-DECISIONS-2026-08-30-v1.html`; filed report `notes/_subreports/2026-08-30-227-diff-decisions-page.md` (`COUNTS: cards 32 (findings 25 · ruling-shaped 7) · UNPROVEN 2`, copied off the file) | 2026-08-30 #227 | ⬛ **AWAITING DAVE — NOT ONE CARD ADOPTED OR PARKED.** ⚠ **A KEEP, NOT A NAG:** the loose-ends page took his Sunday and this one is still open at his pace. |
| **THE FAB REVEAL READINGS — THREE READINGS FOR HIS POINT** — `reviews/FAB-READINGS-2026-08-30-v1.html`; filed report `notes/_subreports/2026-08-30-227-fab-overlay.md` (`COUNTS: built 5 · ruling-shaped 6 · UNPROVEN 7`, copied off the file) | 2026-08-30 #227 | ⬛ **AWAITING DAVE'S POINT — A, B OR C.** ⛔ **DECISION SURFACE ONLY; nothing was adopted, and the overlay's contract about not being part of the design is stated in `knowledge/_render/apollo-fab.js`'s head comment.** ⚠ Renders UNPROVEN at authoring time (disk full) — declared, never claimed. |
| **THE SEGMENTED RE-RULING PAGE — AND THE WRONG-THUMB CAUSE NAMED** — `reviews/SEGMENTED-RULING-2026-08-30-v1.html`; filed report `notes/_subreports/2026-08-30-227-segmented-ruling-enact.md` (`COUNTS: changed 2 · derived 2 · repaired 7 · ruling-shaped 7 · UNPROVEN 3`, copied off the file) | 2026-08-30 #227 · **ENACTS `s227-D4`** · commit `ef24072` · **then SUPERSEDED IN PART by `s227-D7`** | ⬛ **AWAITING DAVE'S EYE ON THE ENACTMENT, NOT ON THE RULE.** xs 28 · m pad 2 · concentric by derivation through `SEGMENTED_CONCENTRIC_SCALES`, with the **`s202-D2` dial identified as the wrong-thumb cause** [[vocabulary-collision-switch-202]]. ✅ **He then extended concentric to ALL FOUR scales on real renders (`s227-D7`)**, which is what the confirm pass row 7 asked. |
| **THE #227 LOOSE-ENDS DECISION PAGE — 44 CARDS, WORKED IN FULL AND HELD AS FLOATED** — `reviews/SESSION-227-DECISIONS-2026-08-30-v1.html`; filed report `notes/_subreports/2026-08-30-227-loose-ends-page.md` (`COUNTS: cards 44 · sources 5 · deduped 3 · excluded-as-ruled 7 · UNPROVEN 2`, copied off the file) | 2026-08-30 #227 | ⛔ **DAVE WORKED ALL 44 AND THEN FLAGGED HIMSELF NOT 100% — SO THEY ARE HELD AS FLOATED AND NOT ONE BECAME A RULING** [[feedback-dont-launder-a-premise-into-a-ruling]]. ⬛ **The 20-DISCUSS set needs a sitting, and its classification is HIS — this wrap re-classified none of it.** |
| **THE MONDAY CONFIRM PASS — HIS OWN ANSWERS BESIDE REAL RENDERS, AND HE CONFIRMED 9/9** — `reviews/CONFIRM-PASS-2026-08-31-v1.html`; filed report `notes/_subreports/2026-08-31-227-confirm-pass.md` (`COUNTS: rows 12 · rendered 6 · mocks 2 · verified-interactive 13/13 · UNPROVEN 2`, copied off the file) | 2026-08-31 #227 · row `W-298` · **rulings `s227-D7` + `s227-D8` came off it** | ✅ **ANSWERED — 9/9 CONFIRMED, WITH TWO AMENDMENTS.** Every controlled row carried a `YOU SAID:` line **checked against his actual paste**, so a mis-restatement could be caught rather than trusted. ⬛ **STILL OPEN AND HIS: C8 is HELD for his own check before it lands** (*“lets just do the check on C8 first”*), and **row 11's `W3` zero-inset readings page does not exist yet — the link was a placeholder.** The confirmed set is banked at `notes/_briefs/2026-08-31-228-repair-lane-brief.md` (`W-300`). |
| **THE v1.0.4 GO/NO-GO PAGE — THE DRY-RUN PROOF, AND HE RATIFIED THE SAME DAY** — `reviews/RELEASE-SPIDER-2026-08-26-v1.html` (regenerated three times in the cut lane, `+ .REVIEW.html`); filed report `notes/_subreports/2026-08-31-228-cut-v104.md` (`COUNTS: enacted 6 · ruling-shaped 3 · UNPROVEN 3 · survey reds 6 → 1`, copied off the file), row `W-302` | 2026-08-31 #228 · ruling `s228-D4` (READ from `knowledge/_rulings.json`) · bake `fb433dd` | ✅ **ANSWERED SAME DAY — *“ratify, lets move on”*.** The page carried the twin fingerprint `617a7ba385ec9c12…` before the bake; the baked zip is **byte-identical to both dry-run twins by `cmp` over the whole file**, 1,659 files. ⛔ **The `RATIFY_IDS` key moves to `s228-D4` at the NEXT cut, per the v1.0.3 pattern — the baked zip is never re-baked to cite its own ratification.** ⬛ **STILL OWED AND HIS: the live cold test on a second machine, now formalised as the cold-start ACCEPTANCE TEST at `notes/_briefs/2026-08-31-229-cold-start-acceptance-brief.md` (`W-304`).** |
| **THE FOUR-VISUALS REVIEW — HIS EYE FOUND TWO DEFECTS EVERY GATE HAD PASSED, AND HE SIGNED OFF ON v2** — `reviews/FOUR-VISUALS-2026-08-31-v1.html` (4/4 specimens SEEN light + dark, hot-corner DRIVEN; filed report `notes/_subreports/2026-08-31-229-four-visuals-review.md`, row `W-305`) and `reviews/FOUR-VISUALS-2026-08-31-v2.html` (the repaired pair; filed report `notes/_subreports/2026-08-31-229-eye-repairs.md`, row `W-306`) | 2026-08-31 #229 · repairs `3561c50` · class fix `notes/_subreports/2026-08-31-229-seg-class-fix.md` (`W-307`) | ✅ **ANSWERED SAME DAY, BY EYE — *“This looks perfect. 1. lets get this fixed 2. fix the class the other solution is a patch not a solution. Wrap”*.** Off v1 he found the console segmented hovers had not inherited the radii and the FAB overlay was not using the fab component; ⛔ **`--assert-mint` was 16/16 GREEN the whole time — a mint assert proves VALUES, never CONSUMPTION.** Both were repaired at source and he accepted v2. ⛔ **HIS TWO RULINGS IN THAT LINE ARE ENACTED BUT NOT IN `knowledge/_rulings.json` — inscription was illegal in-band (`s214-D1`); #230's conductor inscribes them COLD, in his words, verbatim at `notes/_briefs/2026-08-31-229-seg-class-fix-brief.md`.** ⬛ **STILL OWED AND HIS: the FOUR REPAIRED SNIPPETS (`View-options` · `Template-dashboard` · `Template-list-index` · `Template-report`) render console 8/6/6 and were SEEN BY THE LANE, NEVER BY DAVE — they landed after he ruled off v2, and they want his eye; plus the two reasoned `$exempt` entries (`Table`, `Tab-bar`).** |
| **THE DEMO TRIAGE — 35 CARDS GRADED AGAINST THE STORE BEFORE ANY WAS CALLED OPEN** — `reviews/DEMO-TRIAGE-2026-08-31-v1.html`; filed report `notes/_subreports/2026-08-31-230-demo-triage.md` (`COUNTS: cards 35 · reached-cold-already 10 · adoptable-today 7 (6 work items) · needs-daves-word 9 · not-a-demo-blocker 9 · store-catches 6 · new-defects-found 1 · UNPROVEN 5`, copied off the file), row `W-314` | 2026-08-31 #230 · brief `notes/_briefs/2026-08-31-230-demo-triage-brief.md` (`W-312`) | ⬛ **AWAITING DAVE — AND ONLY SURVIVORS REACH HIM.** ⛔ **Six cards graded “needs Dave's word” were NOT open at all**: every one was greppped against `knowledge/_rulings.json` (296 rulings) first, and two of them (`Q-B4`, `Q-B7`) would have been **the #229 miss repeated**. The 9 remaining are to be re-screened per the demo-day brief's own question — *“your look, or Sol's improvisation?”* — before any reaches him. Its four RULING-SHAPED QUESTIONS (the dashboard tile's ground vs the keylines he already ruled · a `rowUnit` dial vs 320px · the masthead mark-swap mechanism, which has **no precedent to copy** · the screen-gate's record shape) are **HIS, untaken.** |
| **THE FOUR FIXED SEG SNIPPETS, RENDERED FOR HIS EYE — AND THREE NEW FINDINGS THE PAGE PRODUCED** — `reviews/SEG-SNIPPETS-2026-08-31-v1.html` (54,356 B; six specimens spliced VERBATIM, 32/32 radius readings, real `:hover` driven 6/6, 16 distinct PNGs read by eye); filed report `notes/_subreports/2026-08-31-230-seg-snippets-eye.md`, row `W-315` | 2026-08-31 #230 · brief `notes/_briefs/2026-08-31-230-seg-snippets-eye-brief.md` (`W-313`) · serves `W-307`'s close condition | ⬛ **AWAITING DAVE'S EYE — THIS IS THE HALF OF #230's TOP CARRY THAT IS NOT DISCHARGED.** The four snippets `s229-D2` fixed are on a page, live from the real artefacts, four themes × light/dark. ⛔ **THREE NEW FINDINGS, MEASURED ON THIS LANE:** the repaired controls are **42 px tall wearing the 36 px rung's corner** (ramp `xs 28 · s 36 · m 44 · l 48`) · their **`height:40px` is a raw literal**, so only the radius joined the group · and the **hover fill is 1.05:1 against its own track in EVERY dark theme** (pre-existing, not a regression, and it governs what his eye can honestly report there). ⬛ **The two reasoned `$exempt` members — `Table` and `Tab-bar` — sit beside them as three-way decision cards and are HIS.** |
| **THE v1.0.5 GO/NO-GO PAGE — STAGED, PROVED, AND HELD `PROPOSED` BY THE MACHINE** — `reviews/RELEASE-SPIDER-2026-08-26-v1.html` (regenerated at the re-cut tail, `+ .REVIEW.html` overlay re-injected); filed report `notes/_subreports/2026-08-31-230-cut-v105.md` (`COUNTS: enacted 6 · ruling-shaped 3 · UNPROVEN 4 · survey reds 4 → 4`, copied off the file), row `W-320` | 2026-08-31 #230 · brief `notes/_briefs/2026-08-31-230-cut-v105-brief.md` (`W-319`) · manifest 1,661 files sha `81414f417929487d` at commit `9c329a1` | ⬛ **AWAITING DAVE'S RATIFY WORD — AND `--release` REFUSES UNTIL HE GIVES IT, BY DESIGN.** `RATIFY_IDS` has **no v1.0.5 row**, because a key moves on a **ratifying** word and never on an authorisation (`s228-D1`/`s228-D4`). Roster **HELD at 58** (`s228-D5`); `apollo-spider/dist/` untouched and still ending at v1.0.4. ⛔ **THE FINGERPRINT ON THE PAGE IS THE RE-CUT ONE AND THE TWIN THAT PROVED IT IS NOT ON THIS MOUNT** — probed at the #230 wrap: `outputs/` holds no zip, a tree-wide `find` and `find / -xdev` return nothing. **#231 can compare the SHA and cannot `cmp` a vanished file.** |
| **THE RETRIEVAL-vs-INTERPRETATION DETECTOR — THREE DIALS, EIGHT SAMPLES, AND THE THRESHOLD IS HIS** — `reviews/DETECTOR-READINGS-2026-08-31-v1.html` (33,994 B; 8 samples × 3 dials, light + dark + 430px render-verified); instrument `knowledge/_detect_retrieval.py` (1,051 lines, `--selftest` **79 arms · 0 failed · 1 inapplicable**); filed report `notes/_subreports/2026-08-31-231-detector.md` (`COUNTS: selftest arms 79 · failed 0 · inapplicable 1 · mutants written 12 · mutants killed 12 · real files driven 5 · false-red classes found BY DRIVING IT 2 · RSQ items 7 · REPLAY-THESE commands 12 · reds left standing 0`, copied off the file), row `W-327` | 2026-08-31 #231 · brief `notes/_briefs/2026-08-31-231-detector-brief.md` (`W-325`) · commit `e7cf3db` | ⬛ **AWAITING DAVE — THE THRESHOLD DIAL IS HIS AND NOTHING WAS PICKED.** A (0.97) / B (0.90) / C (0.78), or his own number in the **0.796–0.974** band; ⚠ **A and B are INDISTINGUISHABLE on this evidence set and the page says so**, so the pick is made knowingly. ⛔ **The paraphrase floor is UNDER-EVIDENCED (only sample P4 reaches it) and the planted-paraphrase arm is a DECLARED APPROXIMATION — Dave's own v1.0.2 output is not in this tree**, so the real-paraphrase drive is owed once he drops it. Six more RSQ behind the dial (coverage floor · shadowing default · pack membership · advisory-or-blocking · the rename and its `W-325` row pointer). |
| **THE DASHBOARD BENTO SNIPPET — F4 CLOSED THE DAY BEFORE THE DEMO, AND EIGHT QUESTIONS COME WITH IT** — `reviews/BENTO-SNIPPET-2026-08-31-v1.html` (315,752 B; four themes × light/dark × four responsive bands); artefact `knowledge/snippets/Template-dashboard-bento.reference.html` (1,166 lines / 79,685 B); filed report `notes/_subreports/2026-08-31-231-bento-snippet.md` (`COUNTS: byte-identical splice runs 21 · canon.css +764/−0 · orphan rows 0 of 57 groups · vars resolving 16/16 both modes · tiles painting a keyline 0 of 170 · raw type declarations 0 · regen serial 7 of 7 + 5 determinism passes · gates 13, red 0 (1 UNASKABLE)`, copied off the file), row `W-328` | 2026-08-31 #231 · brief `notes/_briefs/2026-08-31-231-bento-snippet-brief.md` (`W-326`) · commit `e7cf3db` · serves `s230-D1` beat 3 | ⬛ **AWAITING DAVE'S EYE — EIGHT RULING-SHAPED QUESTIONS, NOT ONE TOUCHED.** R1 the **dark page ground** (A `#1F1F1F` ruled-but-modules-vanish / **B `#1A1A1A` shipped, PROVISIONAL** / C `#313131`) — `s219-D3`(4) left it expressly open · R2 whether *tiles stay square* (`s217-D2`) reaches a module that is itself a component with a surface radius · R3 **may a snippet link `canon.css`** (today **0 of 137** do, so structural canon is COPIED at ~+8 KB a snippet) · R4 four-theme spacing defaults · R5 the keylines-ON construction inline · R6 registration or stay PROPOSED · R7 the `height:40px` retirement across 13 snippets · R8 the priced AUTO-BENTO byte-compare gate. ✅ **`grep -rl '\.c-bento' knowledge/snippets/` now returns 1 file, not 0** — the mechanical cause of `s230-D1` beat 3's PARTIAL is cleared. |
| **THE v1.0.5 GO/NO-GO PAGE, RE-CUT — ⛔ THE `9fb07fa2` FINGERPRINT SUPERSEDES `92f5e10e…`, AND THE COLD COLUMN IS GREEN** — `reviews/RELEASE-SPIDER-2026-08-26-v1.html` (136,528 B, regenerated at the #232 re-cut tail, `+ .REVIEW.html` overlay 162,764 B re-injected); filed report `notes/_subreports/2026-09-01-232-repair-and-recut.md` (`COUNTS — PHASE 2: enacted 8 · found-by-doing 3 · ruling-shaped 1 · UNPROVEN 3 · release-half reds 2 → 1`, copied off the file), row `W-336` | 2026-09-01 #232 · brief `notes/_briefs/2026-09-01-232-repair-and-recut-brief.md` (`W-335`) · predecessor `notes/_subreports/2026-09-01-232-restage-v105.md` (`W-332`, brief `W-331`) · manifest 1,664 files sha `00e8f67995609c21` at commit `ff1b2f531f93` | ⬛ **AWAITING DAVE'S COLD TEST, THEN HIS RATIFY WORD — AND THEY ARE TWO SEPARATE ACTS.** Zip sha256 **`9fb07fa2f0dc0ade56298ea2cc8d8864e25b9b586e3dc1573192ca81593e437b`, 20,073,815 B**, durable copy at the repo root as `Apollo-Spider-v1.0.5-PROVING.zip`; twins `cmp`-identical, `--check` GREEN, release-audit `--check` GREEN. ✅ **`ci-template/run-gates.py` on a pristine twin stage reads `37 pass · 0 FAIL · 0 could-not-ask`, exit 0** — the re-stage's `35 pass · 2 FAIL`, exit 1 is DISCHARGED at source, not waived. ⛔ **`RATIFY_IDS` still has no v1.0.5 row and `--release` still refuses**; `dist/` ends at v1.0.4; `[136]` is red BY DESIGN for the whole `PROPOSED` window. ⚠ **Roster 57 (`s232-D2`), gates column named.** |
| **THE DESIGNER'S BRAIN PROGRAMME PLAN v1 — SEVEN PHASES, AND TWELVE RULING-SHAPED QUESTIONS ARE HIS** — `_PLAN-designers-brain-2026-09-02-v1.html` (84,593 B, repo root, `W-349`); filed report `notes/_subreports/2026-09-02-236-P-plan-build.md` (`W-353`, COUNTS **findings 4 · ruling-shaped 0 · UNPROVEN 2**), built from `notes/_subreports/2026-09-02-236-R1-principles-survey.md` (`W-351`) + `notes/_subreports/2026-09-02-236-R2-sdlc-playbook.md` (`W-352`); rendered and SEEN light + dark (`notes/_subreports/assets/2026-09-02-236-P-plan-build/`). ⛔ **Nothing on the page is ruled** — §8 collects the twelve questions (R1 Q1–6 · R2 Q1–6) with options and each lane's recommendation. AWAITING DAVE. (added 2026-09-02, #236) |
| **THE TENSIONS-SCHEMA REVIEW — TWO READINGS OF EDGES-VS-NODES, AND THE 21 OPEN TENSIONS WITH THEIR PROPOSED DEFAULTS** — `_REVIEW-tensions-schema-2026-09-02-v1.html` (46,425 B, repo root, `W-360`); filed report `notes/_subreports/2026-09-02-237-T-tensions-schema.md` (`W-359`, COUNTS **tensions 30 · settled-by-obligation 6 · resolved-here 3 · open 21 · of open: questions 4 / default-and-declare 17 · pitfalls 8 · ruling-shaped 5 · UNPROVEN 6**, copied off the file), from brief `notes/_briefs/2026-09-02-237-T-tensions-schema-brief.md` (`W-358`); evidence `notes/_subreports/assets/2026-09-02-237-T-tensions-schema/` (incl. `open-tensions.json` — the ONLY full home of the 21 proposed defaults — `_derive_sort.py` and two PNGs); rendered and SEEN light + dark at 1440 and 390, a 390 overflow found and **fixed at the cause** (finding 12). ⛔ **Nothing on the page is ruled.** | 2026-09-02 #237 | ⬛ **AWAITING DAVE'S EYE — R1 Q4 (tensions as edges or nodes) IS RULED FROM ITS §“Two readings”, AND LANE T'S FIVE RULING-SHAPED QUESTIONS RIDE WITH IT** |
