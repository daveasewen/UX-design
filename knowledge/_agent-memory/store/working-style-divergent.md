---
name: working-style-divergent
description: "Dave thinks divergently/fractally/recursively — highly generative, self-referential tangents; support by riding the divergence AND providing a convergent counterweight (structure + thread-capture) so recursion doesn't lose work"
metadata: 
  node_type: memory
  type: user
  originSessionId: 9198cb85-9dfa-4c53-9cee-f263380a387c
---

Dave, self-described 2026-07-03 (with a wink): "very divergent and almost fractal in its recursiveness." This session bore it out — engine → register → inference → provenance → reviewing the very docs that define all of it. It's a **strength** (generative), not a problem: the best ideas came out of the tangents (the inference-ramp, the "unmistakably HSBC" recall-hole catch).

**How to work with it:** ride the divergence — don't shut tangents down — but supply the **convergent counterweight**: structure the output, capture every spawned thread so nothing is lost to the recursion (memory notes, next-session prompts, ADRs, [[session-title-convention]]), and reflect ideas back sharpened. This is the project's own convergent/divergent bright line ([[procedural-debt-and-method]]) applied to the collaboration: gate/anchor the convergent, never gate the divergent. Pairs with [[comms-style-exec-summary]] (dyslexic + time-poor → lead with a short summary + numbered next steps) and [[working-rhythm-reflection]].
