---
name: gallery-and-gap-pattern-frontier
description: "Current frontier (2026-06-29 eve) — canon gallery built, account-card promoted to a gated component, NEXT = component-by-component interaction review + build 5 remaining gap-patterns"
metadata: 
  node_type: memory
  type: project
  originSessionId: 19e99480-a06a-4f32-bf97-2b677a7876a2
---

Builds on [[composition-layer-canon-css]]. State at handoff (fresh chat to follow):

**Gap-pattern → gated-component flow PROVEN on account-card.** Built `snippets/Account-card.reference.html`
(passive molecule, reuses Tags + Status atoms) + `components/account-card.meta.json` (draft, REVIEW-flagged
where design judgment is Dave's: passive-vs-tappable, the missing display/amount type token, no Figma node
yet). Passes snippet + coverage (33/33) + icon + a11y gates → `gen_canon_components.py` (now globs snippets,
auto-picks new ones) generates `.cn-account-card` → journey uses it, `c-account-card` gap pattern removed,
`_validate_screen.py` PASS. **This is the template for the rest.** Dave chose (option 2) to make ALL 5
remaining gap-patterns gated components: **summary (key/value), tab-bar (bottom nav), action-bar, eyebrow,
confirmation/success**.

**Canon gallery = the review showcase.** `knowledge/_fitness-test/canon-gallery.canon.html`, generated by
`knowledge/gen_gallery.py`: every reviewed component shown with its OWN snippet markup wrapped in `.cn-<slug>`,
consuming canon.css (zero hand-authoring → faithful by construction). Gated by `_validate_screen.py` (compose
+ icon + a11y all ✅). Gallery STRIPS demo JS, so it now drives interactive components into a representative
shown state on load (dropdown open, modal dialog shown inline via CSS override, accordion expanded, tooltip
revealed, tabs indicator positioned). Full click-interactivity lives in each component's own snippet.

**Full gate sweep (2026-06-29) all GREEN:** snippets 33/33, icons 0-unknown, a11y, coverage, dark-surfaces,
both composed screens. So remaining issues are in the VISUAL/DESIGN layer the gates can't reach
([[gate-blindspot-state-contrast]]).

**NEXT TASK (Dave, fresh chat): go through the gallery COMPONENT-BY-COMPONENT.** Dave is seeing: some
**interaction decisions missing**, some components **incomplete or missing**, some need **finesse**. Work
through them one at a time (render each via the chromium setup, compare to snippet/Figma, fix). THEN finish
building the 5 gap-pattern components. Render setup: playwright chromium with
`PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS=1` + local libXdamage.so.1 on `LD_LIBRARY_PATH=outputs/libs`
(apt-get download libxdamage1 → dpkg-deb -x). Handoff doc = `knowledge/_HANDOFF-component-review.md`.
