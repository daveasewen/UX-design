---
name: synthetech-project-status
description: Current status and machine/model setup for the Synthetech Agentic Design Workflow project
metadata: 
  node_type: memory
  type: project
  originSessionId: 6bbe441f-ed46-4ace-87a6-5cc546e8bda4
---

**Synthetech** (repo: UX-design folder) is a portable, model-agnostic harness abstracting Dave's HDS editorial pipeline into reusable design pipelines. Name: Synthetech = synthesis + tech (design context → code). As of 2026-06-02 the situation changed from the original plan: Dave is now on the **agency machine** running **Claude (Claude Desktop)** — not ChatGPT/GPT-5.5 as originally planned. The original chats were lost. Figma + the real design system access is coming "soon"; once connected, the next step is Stage 2 knowledge ingestion (see prompts/02-ingest-knowledge.md): populate knowledge/components, tokens, compliance from real assets.

Implication: cross-**model** portability is no longer a hard day-to-day constraint (still nice-to-have for eventual handoffs), but the open-standards / Git-as-source-of-truth structure stays.

**2026-06-17 roadblock + decision:** the real React component repo is NOT accessible. Decision: ingest from **Figma only** for now (Dave has Figma access; Dev Mode MCP path). Build component metadata (`provenance.source:"figma"`) + tokens from the real Figma library; prototype via Figma Make or generated React; Code Connect mappings proposed not verified. Dave is adamant the eventual "full fat" version WILL include the pre-built React library — keep the `source:"both"` + Code Connect handoff path intact for that. Git ops must be done by Dave (GitHub Desktop); sandbox can't write to .git on the mount.

Commits so far: 5a7772b (scaffold) + e782a4a (contracts, synthetic tokens, dry-run), pushed to origin/master.

**Documentation sources (2026-06-17):** (1) component documentation lives INSIDE the Figma component library (descriptions/annotations, possibly doc pages/text layers) → feeds component meta.json `purpose`/`variants[].use`/`antiPatterns`, ingested alongside each component. (2) Branding + foundational docs live on a website (URL TBD) → ingest into `knowledge/guidelines/` (RAG prose, brand-reviewer source) as its own batch AFTER the button proof. NOTE: the website is now GENERALISED — it used to carry component-specific specs but no longer does. So Figma is the SOLE source of component-level docs; no website fallback. Dave's hand-added anti-patterns/usage rules per component are therefore canonical, not a top-up. Figma MCP connected — must authenticate as **david.ewen@emeal.nttdata.com** (NOT the personal davidasewen@gmail.com Claude/Figma account; the gmail account lacks edit access). The library is the **HSBC Common Toolkit (MCP)**, fileKey mI8hvIkV98nquoqWzKh5Kn — a low-stakes DUPLICATE of the real toolkit, barely used, so editing is fine. Structure: Cover, How-to, FOUNDATIONS (Breakpoints/grids, Font scales+tokens, Spacing scale+tokens, Colour tokens, Dark mode, Image, Icons, Elevation, Logos, Hexagon masks), ELEMENTS & PATTERNS (Accordions, ... alphabetical). Note the remote MCP only listed the "Cover" page from a bare fileKey call — need a node/page-specific URL to reach other pages.

**Ingestion order decision (2026-06-17):** FOUNDATIONS FIRST (tokens), then components alphabetically. Reason: dark-mode tokens are known to be partly unimplemented, so the token layer must be captured/verified before component bindings can be validated (invented-token check needs the full store). Dave will **branch the Figma doc** first; he sends the `/branch/<branchKey>/` URL and we use branchKey as fileKey so all reads+edits stay on the branch (review→merge/rollback). Dave is OK with me EDITING Figma on the branch to fix dark-mode token gaps — in small reviewed batches, documenting each change; nothing applied without his review. use_figma requires the /figma-use skill (load before editing).

**Branch reality:** "Gaps and edits" (branchKey/fileKey `Cgbtrmfp15ruNFkIAClpkI`) is a BRANCH of the main toolkit `mI8hvIkV98nquoqWzKh5Kn`. Use the branch key as fileKey. Quirk: bare get_metadata only lists the "Cover" page — must give a node-specific URL per page; address bar shows `0-1` (root) unless a canvas element is selected, so use "Copy link to selection". Pages seen: Breakpoints/grids/scales `471:15986`; Font scales `46048:7963`; Accordions (main file) `430:12803`.

**Key Figma learnings:** type scale = 7 steps (font-1 largest … font-7 smallest, 16px baseline at font-5). Three TScales are Figma VARIABLE MODES → get_variable_defs only returns the active mode; other modes' values are only in specimen IMAGES (OCR to read). font-N = specimen S-N. Authoritative TScale→viewport map (Dave's call): TScale:1 320–959, TScale:2 960–1599, TScale:3 1600+. Component values should reference the type STEP not a fixed px (px varies by TScale). Per-component get_variable_defs returns intent-named tokens (e.g. border/subtle, text/default, tertiary/background/hover).

Done so far (uncommitted since e782a4a): `knowledge/components/accordion.meta.json` (validates; PENDING Dave review of: missing focus-visible state, inferred anti-patterns, single-vs-multi-open behaviour); `knowledge/tokens/layout.json` (breakpoints, grid main+flyout with separate margin/gutter, scales, layout web gutter=40/margin=16, gap tokens); `knowledge/tokens/typography.json` (full 7×3 type scale, family, weights); `knowledge/tokens/spacing.json` (responsive space-0…12 + 0.5 across scale-1/2/3, fixed F4/8/12/16; PENDING Dave decision on doc-vs-variable conflict); `knowledge/tokens/_INGEST-NOTES.md` (gaps log).

**2026-06-17 Spacing page done** (page `475:16246`, node URL given). Read via screenshot OCR — get_variable_defs returned "nothing selected" and get_metadata threw repeated HTTP2 errors for this node. Big open decision (notes item 12): the page's right-hand "Space scale" doc table and left-hand R0–R12 variable-mode table AGREE for space-1..8 but DISAGREE at space-0 (0/2/4 vs R0 0/4/8), space-0.5 (doc-only, no R0.5 var), space-9 scale-3 (doc 48 vs var 44 — suspected typo), and space-10/11/12 (variable table restarts low: 24/28/32…). spacing.json captures the DOC table as canonical with $review flags; Dave must pick which table is canonical. Also: variable sub-header says "+8/+16" but rows step +4 (item 13); scale-3 marketing-only confirmed.

**2026-06-17 Colour page primitives done** (page '↳ Colour tokens' canvas `11:116`). `colour.json` written — 126 PRIMITIVES from the live swatch frames (Foundation/Data-vis/RAG/Illustration) via OCR. get_variable_defs unusable here (flattened images — Dave: "some tables are just images"). **Semantic light/dark intent layer DEFERRED by Dave's decision** (notes item 17): it's the 'Colour tokens semantic' frame `52780:24956` = 18 pasted screenshots dated 2026-02-27 (stale); Dave will provide LIVE semantic variables instead. That layer is what accordion's text/default, border/subtle, tertiary/background/hover resolve through + holds dark-mode swap logic. Quirks logged: Figma misspells 'complementary' as 'complimentary' (sic) in token names (item 19); dark-mode-only primitives neutral-dark-mode-grey-1..6 + rag-dark-*-tint (item 20); RAG fully reconciled, closes old item 10 (item 21).

**2026-06-17 BIG RE-BASE on authoritative variable export.** Dave exported live vars via Figma native "Export modes" (no Enterprise REST scope / no plugin approval available — native export was the way in). Landed in `knowledge/tokens/_raw/{brand,semantic-color,semantic-scale}/`. Transform script (`outputs/transform.py`) regenerated the whole token store, OVERWRITING the OCR files. Now canon: `colour.json` (133 brand primitives, real names, alpha-encoded), `semantic-colour.json` NEW (111 live light/dark semantic tokens + brand alias refs; 147 depricate excluded), `typography.json`/`spacing.json`/`layout.json` rebuilt from semantic-scale, NEW `icon-scale.json` + `elevation.json`. Manifests NEW: `_manifests/depricate-tokens.json` (147 for bulk-delete), `_manifests/sutherland-diffs.json` (6 brand + 85 semantic Sutherland remap diffs). Spacing doc-vs-variable conflict RESOLVED (real spacing = gap/* + padding/*, no space-0..12 var). depricate hunt DONE. Flags: F1 font-weight/bold scale-3 export glitch ("String value", coerced+$review); F2 doc-vs-variable naming mismatch; F3 'complimentary' sic; F4 Sutherland modes = mapping not canon. See [[token-collection-architecture]], [[sutherland-figma-mapping]].

**2026-06-17 Foundations pages pass started.** Dark mode page (node 46025:22918) DONE — guidance-only, no new tokens; prose captured to `knowledge/guidelines/dark-mode.md` (first file in new knowledge/guidelines/ folder for RAG/brand-reviewer source). Tooling note: get_design_context extracts text cleanly but errors on image-heavy nodes (>context); recover captions via ripgrep -o on saved tool-result files. Confirmed external guidance base URL: **create.hsbc/Guidelines/Foundations/** (was "TBD" before). Foundations done: Dark mode, Icons, Elevation, Logos, Hexagon masks (all → knowledge/guidelines/). **FOUNDATIONS SWEEP COMPLETE.** Hexagon masks page (960:26374) = sticker sheet (detail on create.hsbc/foundations-and-identity/Creative-hexagons.html); types: Iconic Hexagon, Open Hexagon, Cropped Hexagon (5 ratios 1:1/4:3/16:9/21:9/3:1 × top/mid/bottom × L/R = 30); usage "only to mask image shapes"; mask SVGs exportable on request to knowledge/assets/hexagons/ (not yet done). COMPONENTS PASS STARTED (doubles as depricate usage audit). Done: Accordion, **Badge** (node 617:78931 → badge.meta.json: surface=color/primary [primitive,flagged], text/reverse, icon/disabled). **First depricate-in-use hit:** Badge-on-icon (2450:109105) binds `interactive (depricate)/on-light/surface/primary/default` → rebind to `background/default` before bulk-delete (logged in replacement-map $usage_audit + _INGEST-NOTES). Component method: get_variable_defs on the component set for tokens; get_design_context on Page Header/rich-text/do-dont for purpose+anti-patterns; mirror accordion.meta.json schema; validate refs. Also done: **Avatar** (2136:31172/31221 → avatar.meta.json): initials/image/icon × s/m/l (36/44/56) × default/disabled × on-light/dark; uses image/opacity tokens (confirmed live in semantic-colour.json). **BIG audit finding: Avatar binds 6 depricate tokens with NO live equivalent** (non-interactive surface/border neutrals + on-dark disabled text/icon) — replacement-map all "review". **This BLOCKS the depricate bulk-delete for those tokens until live equivalents are created + Avatar rebound** — escalated (replacement-map $usage_audit, _INGEST-NOTES). Implication: the live semantic layer is missing a subtle-surface + border family. Also done: **Breadcrumbs** (41590:108466 → breadcrumbs.meta.json): states default/hover-pressed(combined)/no-link, all text/default #333333 font-6, NO depricate (clean). WCAG flag: hover==pressed + no colour change between states. Components come in NON-alphabetical order (Dave noted). **Button** (64:90 → button.meta.json): 4 types primary/secondary/tertiary/quaternary × on-light/dark × default+Large. on-light mostly migrated; on-dark HEAVILY deprecated (whole interactive(depricate)/on-dark/surface/brand-1/* set + on-dark text/icon depricate) — fix = rebind to mode-aware primary/background/*, text/reverse, rag/success (these already carry dark values). secondary clean. **Audit tally: Badge=1 (→background/default), Avatar=6 (no equiv, BLOCKER), Breadcrumbs=0 (sep=\"/\"), Button=2 on-light (clean rebinds) + large on-dark cluster (rebind to mode-aware semantics).** **Cards** (2298:53278 → cards.meta.json): family of 5 (actions, basic, basic-non-interactive, image-with-border, image-without-border). 4/5 clean; Card Basic (interactive) uses `tertiary/text/default (depricate)`. Pattern: interactive cards use tertiary/background/* state tints; static/image cards use background/default+border/subtle (clean). **Audit tally: Badge 1, Avatar 6 (BLOCKER no-equiv), Breadcrumbs 0, Button 2 light+dark cluster, Cards 1.** Recurring depricate `tertiary/text/default` now in Button(tertiary,quaternary)+Card Basic. Patterns: (a) on-dark variants least-migrated; (b) interactive components reuse tertiary/* + the tertiary/text depricate; (c) arrows/chevrons bind primitive color/primary. Note: not every URL is a component — node 2298:160129 was a UX PATTERN page ("Horizontal scroll design"/carousels) → captured to knowledge/guidelines/horizontal-scroll.md (no tokens). So the file mixes components + patterns + foundations guidance. get_metadata failed (HTTP2) on that huge node — identified via screenshot OCR. Also captured: **Contextual help** (45226:146956) — pattern page, browser + mobile-app variants → knowledge/guidelines/contextual-help.md (no tokens). Components done so far: Accordion, Badge, Avatar, Breadcrumbs, Button, Cards, Countdown timer, Dropdown, Divider, Headers, Hero. Hero (41362:3440 → hero.meta.json): responsive marketing banner; NEW live token group **blur/*** (glass-panel background blur); depricate mostly clean rebinds (transparent white/black→color/grey/transparent/*, surface/white→background/default, border/on-light/neutral-3→border/subtle, on-dark/brand-1→primary/background/default). **NEW: a 2nd legacy token namespace exists beyond 'depricate'** — e.g. `Non-interactive/Content/On Light/colour-content-primary`→text/default (logged $legacy_namespace_note). Token groups now seen across components: form/*, scrollbar/*, elevation/functional, timer/*, image/opacity/*, blur/*. Plus **Divider** (2120:30972 → divider.meta.json): subsection/inset/section(1px)+break(4px), on-light clean (divider/border/*), on-dark 2 depricate (non-interactive border neutral-3/6 → rebind mode-aware divider/border/*). Audit tally depricate-in-use: Badge 1, Avatar 6(blocker), Breadcrumbs 0, Button 2+dark cluster, Cards 1, Countdown 0, Divider 2(on-dark, rebindable). CONSISTENT on-dark pattern: dark variants bind non-interactive(depricate) border/surface neutrals; fix = rebind to mode-aware live semantics where they exist (divider/border/*, primary/background/*).

Plus **Dropdown** (45024:56778 → dropdown.meta.json): first FORM component — non-native (single/filterable/multi + list items) + native; surfaces live form/*, scrollbar/*, elevation/functional. Depricate-heavy triggers, NATIVE least migrated (~9 interactive(depricate) surface/border + icon hover + rag/border error + non-interactive surface). Most have clean form/* rebinds → migrate triggers onto form/* family. List items clean. NEW audit pattern: form components' migration target is the form/* token family. Audit tally now: Badge1, Avatar6(blocker), Breadcrumbs0, Button2+dark, Cards1, Countdown0, Divider2, Dropdown~9(mostly clean form/* rebinds), Headers3, Hero(clean rebinds+legacy ns), InputFields2(clean form/border/active + rag/error). **Input fields** (45011:112934 → input-fields.meta.json): family = single-line(+Large), multi-line, date-picker (input + `.Date` calendar cells + `.Arrow` + modal/dropdown overlay w/ elevation/functional), input label. Migrated to form/* (bg transparent/#f3f3f3/#767676, border #767676/#000000/#d7d8d6), text/default, rag/error. `.Date` cell fully clean. Same Dropdown pattern: only the 2 "bottom stroke" inner-shadow EFFECTS still bind depricate — interactive(depricate)/on-light/border/active/default→form/border/active + rag/border(depricate)/error/default→rag/error, both clean. Confirms FORM-component finding: chrome migrated, leftover depricate is in the active/error underline effects. **Links** (45015:45858, "Text links (browser)" → links.meta.json): family of 4 — Arrow link (chevron after, font-1…7, arrow=PRIMITIVE color/primary #db0011 light / icon/default-reverse dark, CLEAN), Back link (chevron before, font-5/6/7, neutral icon/default, CLEAN), Icon link (leading icon S5/6/7, only type w/ disabled), Inline link (in body, font-5 LIGHT-link, CLEAN). hover+pressed combined. **Depricate-in-use = 4, ALL Icon link ON-DARK:** on-dark/hover icon+text #fff→icon/default-reverse+text/reverse (clean); on-dark/disabled icon+text #767676→REVIEW no live equiv. **KEY ESCALATION: the on-dark disabled blocker (#767676) is now Avatar AND Icon link — no longer Avatar-only; a live on-dark disabled text/icon token must be created before those depricates can be deleted.** Arrow primitive-binding flag (color/primary, like Badge/Hero) — consider semantic icon/brand. Audit tally += Links 4 (2 clean, 2 blocker-shared-with-Avatar). **List items** (45129:305498, "Lists" → list-items.meta.json): big family — 6 tappable rows (Account, Badge, Item, Review, Review Detail, Transaction) + typeset bullet/ordered/custom lists; states default/hover(web)/pressed/disabled/processing × visual icon/avatar/image. **MAJOR FINDING: two value-identical row-surface conventions** — LIVE tertiary/background/* (Account/Review/Review-Detail) vs DEPRECATED interactive(depricate)/on-light/surface/primary/* (Item/Badge/Transaction) → clean migration = rebind surface/primary/*→tertiary/background/*. Per-type depricate: account/review-detail/bullets clean; review 1; badge~7; item~12 (incl on-dark blockers surface/black + border/neutral-6); transaction~8. Clean text/icon hover→default, on-dark/pressed→reverse, tertiary/text/default(depricate)→text/default. No-equiv blockers = same Avatar/Headers gap family (non-interactive surface/neutral-1/2, border/on-light/neutral-2/6, surface/black). Primitive leaks (Badge color/primary, bullets grey-1). 18 live refs PASS. Bullets copy rules captured. Audit tally += List items (clean rebinds for surfaces; blockers shared w/ Avatar gap family). **Loading indicator** (486:17029 → loading-indicator.meta.json): indeterminate spinner, Web/iOS/Android × on-light/dark × sizes (Web large/medium/small/xs; mobile large/medium/xs). **CLEAN, zero depricate** — spinner = icon/default / icon/default-reverse; label text/default|reverse font-5/6. A11y: needs a tag/label so SR announces activity; skeleton loaders need alt/aria-live. Do centre in container (horizontal-only if height unknown); don't show >1 spinner at once. (Last commit by Dave covered Input fields + Links + List items + manifest/notes.) **Modals** (45024:100302 → modals.meta.json): family of 3 — Dialog (interrupts + requires decision; buttons default/two/stacked), Video modal (responsive XL→XS), Partial modal/Drawer (app-only, bottom-anchored, supplementary). **CLEAN, zero depricate.** Surfaces **NEW token group `overlay/*`** — modal backdrop = overlay/version1 (#00000080 scrim) + blur/overlay (12) BACKGROUND_BLUR. (Figma flattened blur var as 'overlay/background-blur' → store token is blur/overlay, same blur/* group as blur/background-surface on Hero.) primary/background/default CTA, tertiary/border/default secondary border, background/default surface. Drawer = elevate above level 0 (shadow→overlay when expanded). Token groups now seen: form/*, scrollbar/*, elevation/functional, timer/*, image/opacity/*, blur/* (+blur/overlay), overlay/*. loading-indicator + modals are post-(last-commit) uncommitted. **Navigations** (41404:53897 → navigations.meta.json): MOST-DEPRECATED family — web Masthead (logged-out/in + main-nav/search/user flyouts), Anchors, app nav bars (bottom tab + top iOS). Clean rebinds: interactive(depricate)/on-light/surface/primary/default→background/default (all nav surfaces); non-interactive surface/white→background/default; border/neutral-3→border/subtle; icon hover-1→icon/default; border/neutral-2→divider/border/subsection; **border/brand-1/default #db0011→active-indicator underline (Anchors+top bar)→primary/border/default**. Blocker: surface/neutral-1 (user flyout, no live equiv). **2 BIG FLAGS: (1) search flyout binds a LIVE `interactive/on-light/*` namespace (un-suffixed mirror of the depricate interactive tokens = apparent migration target) that is ABSENT from the canonical export → likely Sutherland or uncaptured; if canon it's the bulk rebind target for the whole interactive(depricate) set — CONFIRM. (2) bottom nav uses raw non-tokenised shadow DAY/colour-level-shadow-1 + colour/neutral/grey-1 primitive absent from store.** overlay/* reused on flyouts. Annotated $usage_audit. loading-indicator+modals+navigations uncommitted. **Notifications** (45002:355009 → notifications.meta.json): RAG family, 4 placements — Contextual (+form multi-link), Global (banner, scale 3/4/5), Inline, Snackbar (toast, no error variant). Variants error/warning/success/information via rag/* (accent) + rag/*-tint (surface) + rag/text/on-light|on-dark. **Global/Inline/Snackbar CLEAN; only Contextual+multi-link carry depricate** rag/icon(depricate)/error+success/icon #fff → rag/text/on-dark (clean). 15 refs PASS. A11y: role=alert/aria-live; not colour-alone; snackbar not for errors. loading-indicator+modals+navigations+notifications committed (Dave). **interactive/on-light/* namespace — DECISION + FALLBACK (logged in replacement-map $interactive_namespace_note):** preferred = if interactive/on-light/* confirmed canon, migrate by dropping ' (depricate)' suffix (1:1). FALLBACK if it's Sutherland/not-canon: every interactive(depricate)/on-light token already has an in-store target by context (surface/primary/*→background/default|form/background/*|tertiary/background/*; border/active→form/border/active; high-contrast→form/border/default; low-contrast/disabled→form/border/disabled; surface/brand-1→primary/background/default; border/brand-1→primary/border/default). So namespace swap is tidier-not-required; depricate bulk-delete is NOT blocked on resolving this question. **Pagination** (264:9250 → pagination.meta.json, web-only): basic / dropdown / dropdown-stacked + atoms (page-size dropdown, Go button, Arrow button). Pagination basic CLEAN (tertiary/border/default current-page outline). Depricate: border/active/default→form/border/active (dropdown+page-size); **border/brand-2/default #000→secondary/border/default (Go+Arrow buttons)**; page-size dropdown reuses Dropdown form-trigger set. 14 refs PASS. pagination.meta + manifest/notes uncommitted. Patterns/guidelines: dark-mode, icons, elevation, logos, hexagon-masks, horizontal-scroll, contextual-help, cookie-notifications, forms. (Forms 45226:146957 = umbrella forms guidance, 14-section outline + 4 design principles captured; deep per-section prose extractable from node 45226:149920 on demand.) (Cookie notifications 45112:296788 = consent-banner content/pattern page, no tokens; types: strictly-necessary [always on] vs optional [opt-in w/ controls].) Resume from next URL Dave provides. Logos page (29:1009) = sticker sheet, minimal prose (detailed rules on create.hsbc/foundations-and-identity/Logos.html); has 12 logo SVG variants (hexagon/masterbrand/masterbrand-identifier × light/dark × colour/mono) → export script `knowledge/assets/logos/_export-logos.py` — **12 logo SVGs EXPORTED 2026-06-17 (vector, no raster)** to knowledge/assets/logos/ via a fresh token (now also should be revoked). Remaining Foundations to check (need node URLs): Image, Hexagon masks. **2026-06-17: big Foundations re-base COMMITTED by Dave** (token re-base on variable export + 4 guidelines + 658 icons + 12 logos + manifests + _raw). So that pile is now in git; remaining uncommitted work will accrue from here. ICONS page (node 2107:29115) in progress: it's an icon catalogue + has a dedicated "Export board" (node 13244:4171) with 10 export groups (Miscellaneous, Social, Touch, Informative, Volume and audio, Media, Arrows and chevrons, Products and services, Global controls, Status Icons). Sizing already in icon-scale.json, colours in semantic-colour.json — no new tokens.

**Icon catalogue export (3 ways to build → fallback to Claude builds likely high early, per Dave, so storing assets is worth it):** Pilot DONE — `knowledge/assets/icons/status/` has 4 validated SVGs. Findings: Figma raw SVG includes artboard junk (#E5E5E5 rect + giant Export-board path) → cleaner strips it; fills are baked literal hex (status icons match rag tokens: error #A8000B=rag/red, success/yes #00847F=rag/green); names from group metadata. DECISIONS (Dave): full export via REST /v1/images batch (NOT per-node MCP — too many); monochrome icons → currentColor, coloured/status icons keep baked fills. Script `knowledge/assets/icons/_export-icons.py` (self-enumerates board, batch /v1/images SVG, clean, currentColor, manifest, parallel+retry+collision-safe) + `_README.md`. **DONE 2026-06-17: full export ran via Dave's token in-sandbox — 658 icons across 10 groups** (Informative 131, Products-and-services 206, Global-controls 121, Media 99, Misc 24, Arrows 24, Status 20, Volume 16, Touch 10, Social 7) → `knowledge/assets/icons/<group-slug>/<icon>.svg` + `icons.manifest.json`. 337 currentColor (monochrome) / 321 baked (coloured/status, fills match rag tokens). Note: group slug for "Status Icons" = `status-icons`. **Dave should REVOKE the figd_ token** (was pasted in chat for the run; not stored by me). Sandbox quirks: bg processes don't persist; can't delete mount files without allow_cowork_file_delete; figma S3 asset host needs retries (intermittent DNS). Icons usage guidance DONE → `knowledge/guidelines/icons.md`. **⚠️ Key flag: HSBC says DO NOT export SVGs from the Icon Library/artwork for development — use the UI Centre. So our Figma SVG export is PROTOTYPE-ONLY; dev handoff icons come from UI Centre.** Also: default=line / active=solid-fill states (not all icons have active); thicker 1.8px variants for small sizes; create.hsbc = canonical icon source.

**Open threads / next:** (1) Dave bulk-deletes the 147 depricate vars (manifest ready). (2) Sutherland remap worklist (6+85 diffs) — Dave's team's in-progress mapping; revisit. (3) Re-validate accordion.meta.json token refs against new semantic-colour.json (spot-checked OK: border/subtle, text/default, tertiary/background/hover etc. all resolve). (4) Then components alphabetically. (5) Commit all token files + _raw export via GitHub Desktop. (6) Accordion review still open (focus-visible, inferred anti-patterns). (2) Bulk-delete the many `depricate` variables (Dave, via Variables panel search). (3) Reconcile Breakpoints-page scale ranges to the TScale ranges. (4) Accordion review still open. (5) Commit pending files via GitHub Desktop. (6) RAG status colours seen (green #00847f, red #a8000b + tints) — formalise on Colour page.

Known doc cleanup pending: root README repo map and dossier §9–§10 still use old folder names (`ux-ui/`, `research/`, `cx/`, `copywriting/`); actual folders are `ux-design/`, `ui-design/`, `cx-research/`, `cx-design/`, `ux-research/`, `ux-copy/`.
