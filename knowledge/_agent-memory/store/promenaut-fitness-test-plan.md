---
name: promenaut-fitness-test-plan
description: Plan for the next session — Tabs fitness-for-purpose test of the knowledge base (two-route experiment)
metadata: 
  node_type: memory
  type: project
  originSessionId: 92c69cca-fca7-4999-a3d7-517fe5550c6c
---

**STATUS: DONE 2026-06-19.** Result + report at `knowledge/_FITNESS-TEST-tabs.md` (builds in `knowledge/_fitness-test/`). **Headline: building Tabs faithfully from the KB produces a component that is correct in light mode but BROKEN in dark mode — tab label + selected indicator render white-on-white (1.0:1 contrast, invisible), failing 1.4.3/1.4.11.** Route A (unconstrained) passes all contrast in both themes. The KB supplies correctness scaffolding (ARIA, SC list, token resolution, and impressively flags its own P3 + ships the tabs/* fix) but NONE of the craft (focus indicator, geometry, motion, considered dark values) — in Route B that was ~90% my invention. Prioritised KB fixes: (1🔴) fix flat/wrong dark token values + make dark-mode audit CONTRAST-AWARE (it rates a token "clean" if it merely has a dark value, even when wrong); (2🔴) add a focus-indicator standard (token+guideline) — systemic, affects all 32 components; (3🟡) add a geometry/dimensions block to the meta schema (metas have no measurements); (4🟡) ingest motion guidance + motion/* tokens; (5🟡) data nits (font-weight string→numeric, empty letter-spacing/font-5, null border-radius/default, font fallback); (6🟡) fix guideline mapping (horizontal-scroll=carousel, not tab overflow); (7🟢) complete tabs/* hover/pressed. **Meta-finding: the integrity gate PASSED a dark-mode-broken component — internal consistency ≠ fitness for purpose.** Next progress metric = re-run THIS test after fixes and watch the A–B gap shrink (not another derived view).

**Post-test progress (2026-06-19):** Dave reviewed Route A and gave craft notes → fixes applied. **Fix #4 (motion) STARTED & DONE for Tabs:** added `tokens/motion.json` (DTCG duration+cubicBezier: motion/duration/{instant,fast,fade,standard}, motion/easing/{standard,linear}) + a `motion` block in tabs.meta.json (sliding indicator + panel fade, references motion/* tokens, reduced-motion rule) + `motion` added to meta.schema.json. Gate still 32/32 PASS. **Brand rule learned: angular is the default BUT Badge (fully round) and Avatar (round) are the ONLY exemptions** — captured in fitness report fix #8. Also added Tabs anti-pattern: don't change font-weight on selection (causes width jump). **Dark-mode write-back to Figma still PENDING & needs: canonical values (not my Route A demo hexes — reconcile vs neutral-dark-mode/rag-dark), the 'Gaps and edits' branch, Dave's explicit go-ahead, AND verification the Figma connector can WRITE variable mode values (reads confirmed, writes unverified).** Tabs still flags color/primary leak in the audit (meta colour bindings not yet rewired — that's the Figma write-back work).

**Progress 2026-06-19 (session 2 — Opus):** Fixes #1–#4 now DONE + **re-run #1 complete** (appended to `knowledge/_FITNESS-TEST-tabs.md`). **#1** dark values reconciled to canon (`#1D1D1D`=dark-mode/600, `#474747`=dark-mode/300; tabs/* + tertiary/* swept, $alias provenance kept honest). **#1b NOW COMPLETE:** both contrast audits (`_build_surface_contrast_audit.py`, `_build_indicator_contrast_audit.py`) GATE the build (exit non-zero on non-allowlisted dark-mode failure; `_build_all.py` folds into exit code — verified by injecting invisible text/default → fail, restore → green) AND resolve the REAL worst-case dark surface per token instead of hardcoding #1D1D1D; `*/on-light` excluded (killed the rag/text/on-light false positive); disabled exceptions in `CONTRAST_ALLOWLIST` in `_contrast_utils.py`. **#2 (focus) DONE:** searched Figma — HSBC has NO canon focus primitive (CSS-layer), so captured INFERRED `focus/ring` (mode-aware #305A85/#4587A7, ≥3:1 verified) + `layout/focus` width/offset (2px) + `guidelines/focus-indicators.md` registered GLOBAL (reaches all 32) + wired into Tabs; tagged `review` pending design sign-off. **#3 (geometry) DONE:** optional `dimensions` block added to meta.schema.json + populated on Tabs (48/20/3/1px, target 44). **Also fixed:** all 8 build scripts had hardcoded absolute session paths (broke across sessions) → now derive ROOT from `__file__`. Re-run #1 verdict: the 4 structural blockers (dark mode, focus, geometry, motion) all now answered FROM the KB — gap shrank from "fails WCAG in dark + no focus" to "shippable; residual delta is type polish + overflow richness". **Still open: #5 (type data nits — font-weight string→numeric, empty letter-spacing/font-5, font fallback), #7 (tabs/* hover/pressed surface tokens), #6 (horizontal-scroll mis-map = carousel not tab-overflow), #8 (operationalize angular rule w/ Badge+Avatar exemptions).** Next-move options floated to Dave: cheapest = #5; higher-leverage = point the fitness test at a DIFFERENT component (Dropdown/Input fields) to check the focus/geometry standards generalise. Figma dark-mode write-back still pending Dave's go-ahead.

**Original plan (for reference): Tabs fitness-for-purpose test.** Driven by the honest critique that the [[promenaut-project-status]] KB has **no consumer yet** — we've validated internal consistency, not fitness for purpose (does an agent produce better, compliant code *because of* the KB?). Dave shares the worry about a "productivity bubble" (building satisfying derived views instead of proving value).

**The experiment (two routes, KB-only FIRST for rigor):**
1. **Route B (real test, first):** build Tabs in HTML + CSS variables using ONLY the knowledge base (tabs.meta.json, token stores, compliance/, guidelines/) — no live Figma, no outside design judgement, no patching gaps from memory. Log every gap live.
2. **Route A (ceiling, "for fun"):** then build the best unconstrained Tabs (full design craft, motion, states, polish).
3. Side-by-side + **dual critique** graded independently: engineering correctness (compiles, tokens resolve, WCAG 2.2 AA checks pass) AND design craft (would an HSBC designer ship it?) — use design-critique + accessibility-review skills on our own output.
4. **Deliverable = `knowledge/_FITNESS-TEST-tabs.md` gap report** (A–B delta + gap log → prioritised list of what the KB needs for codegen). The component is the probe; the gap report is the point.

**Why B before A:** building the polished one first contaminates the test (I'd patch B's gaps from memory). **Why Tabs:** known P3 defect (indicator on color/primary primitive → dark-mode break), color/primary leak, 2 deprecated bindings (REVIEW rebinds), 6 SCs incl 2.4.11/2.5.8, responsive overflow, and prose-not-structured token bindings — exercises tokens+compliance+guidelines and a real flaw at once.

Brief saved at `SESSION-BRIEF-tabs-fitness-test.md` (workspace root). Also flagged for later: design-craft skills need firming up; single-author provenance (no validation vs real Sutherland lib). Related: [[graphify-tool]], [[token-collection-architecture]], [[wcag-state-contrast]].
