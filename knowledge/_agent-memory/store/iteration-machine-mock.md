---
name: iteration-machine-mock
description: "Front-end VISION file (Dave's notebook sketch realised) — the tool as an iteration LOOP; FACADE not wired to the engine; real-vs-faked inventory"
metadata: 
  node_type: memory
  type: reference
  originSessionId: 9198cb85-9dfa-4c53-9cee-f263380a387c
---

`_VISION-iteration-machine_2026-07-03.html` (repo root, built 2026-07-03). Dave's sketch → clickable **loop**: Input (chat + inject + analysis/vibe toggle) → Shape (quiz) → Result (cool/warm/hot register switch on a canon SME-payments screen; HOT = dark editorial hero, gradient, red-forward + 1 gate bites 2.7:1 with Repair/Harvest) → Refine (comments + quiz) → loop. Sits beside `_VISION-northstar-front-end_2026-07-02.html` as the more user-facing expression. Cool/warm/hot = the register spread; **enforcement present-but-quiet = the moat** vs commodity generators. Dave: "good expression of where we get to, not a vision yet but close." "It can run hot" — HOT deliberately expressive.

**FACADE — NOT wired to the engine.** Faked: screens hand-authored; gate pills + the 2.7:1 figure + Repair simulated in JS; quiz doesn't compile a contract. REAL: palette RETRIEVED from `tokens/colour.json` (#DB0011 etc.); and everything it fakes EXISTS + runs (gates green 18/18, contrast validator computes real ratios, contract format exists, promote loop shipped ×3). It PROVES NOTHING about the engine — alignment/target artifact only; the proof is the gate suite + GOV.UK + the calibration project. Guard against leadership mistaking the facade for the working product (the disappointment risk).

Structurally verified (all JS hooks resolve, tags balanced, 3 screens present); **NOT pixel-rendered** — sandbox renderer absent, so the placed-logo mark + hero gradient are unseen by me. Recorded in [[product-shape-flexing-engine]] / ADR-0006. Review medium = live HTML per [[review-preview-html]].
