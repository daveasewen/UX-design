---
name: fixed-flex-charter
description: "Governance model for brand-true generation — \"wide road, high curbs\"; fixed/flex rules + generate-new mechanism"
metadata: 
  node_type: memory
  type: project
  originSessionId: b6d4d256-65cf-4438-9588-8f44138e09e2
---

**`knowledge/_FIXED-FLEX-CHARTER.md`** (drafted 2026-06-30 from the SME-Payments fitness tests: sober→desktop→Swiss→portfolio). The governance for "respect the canon AND be creative where it matters."

Core model — **TWO DIALS**: (1) **canon** = vocabulary (tokens+components, gated, strong below the component line, SILENT above it); (2) **register** = temperature/voice (concept/density/heat — currently UNGOVERNED, buried in brief adjectives like "sober"). The four SME versions used the SAME canon at opposite registers — the divergence was mostly the register dial, not the canon. **Make register an explicit named input.**

- **FIXED (curbs — always RETRIEVED, never typed/recalled):** brand colour values · Univers + type ramp · a11y floor (contrast/focus/target/reduced-motion/ARIA) · safety patterns (high-value confirm, masking, modality ring) · official brand assets (mark placed, never drawn). KEY: "recall drifts, retrieval doesn't" — the portfolio looked HSBC on ~4 primitives but I TYPED them from memory.
- **FLEX (wide road):** composition/templates · concept/voice/register · motion choreography (ON canon `--spring`/`--press`) · data-viz (on a canon data palette) · density/scale.
- **UNDECIDED rules (need Dave's call):** is flatness fixed or unlocked in expressive? · brand dark/inverse surface? · how red-forward per register? · corner radius (rec: square = fixed).
- **Generate-new mechanism:** retrieval-first → derive-from-fixed → gate-against-curbs → flag-for-promotion. = the EXISTING gap-pattern pipeline (snippet→meta→gate→`.cn-*`) run at GENERATION time. Plus **multi-variant per run** (a register spread on the same curbs — makes the road width visible).
- **Tiers:** T1 canon (compose-green) · T2 candidate (retrieved fixed + flagged candidate tokens) · T3 exploration (free, recalled — a signal not a deliverable).

DEMONSTRATED by `_fitness-test/sme-payments-balanced.html` (T2): same craft as the portfolio (dark hero, count-up, cashflow viz, runway, bars, motion) but **7 colours all canon/brand (0 invented)** vs the portfolio's 16 invented; red/teal/motion retrieved via canon vars; `cn-button`/`cn-modals` reused; the one real gap (light-mode inverse surface) = flagged `--cand-inverse-*` tokens holding the actual canon dark-theme values. Gaps logged in `_COMPONENT-GAPS.md` (inverse surface · data-viz palette · register/craft tokens). See [[promenaut-product-vision]] [[procedural-debt-and-method]] [[gated-snippets-and-motion]] [[digital-experience-transformation]].
