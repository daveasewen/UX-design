# Compliance graph — verified edges

> Layers the VERIFIED edge (`verified_by`) onto the compliance graph's existing CLAIMED edge (`applies_to`). Generated by `_build_verification_edges.py` — advisory, does not gate the build. Regenerate via `knowledge/_build_all.py` (must run after the contrast audits, the a11y gate, and the token blast-radius index).

**4/31 SCs have an executable check wired into the current build.** The other 27 are applies_to-only — asserted by component metas, not machine-checked. That is expected for genuinely manual SCs (e.g. 2.1.1 Keyboard) and is not itself a defect.

## Verified

| SC | Result | Granularity | Coverage |
|---|---|---|---|
| 1.4.11 | ✅ pass | component | 6/8 applies_to components have a bound audited token |
| 1.4.3 | ✅ pass | component | 18/18 applies_to components have a bound audited token |
| 2.3.3 | ✅ pass | component | — |
| 2.5.8 | ✅ pass | component | — |

## Component-level breakdown — 1.4.3 / 1.4.11 (the blast-radius join)

### 1.4.11

6/8 applies_to components have a bound audited token. 6 pass, 0 fail, 2 not_covered.

**Not covered** (applies_to claims this SC but the blast-radius scan found no bound audited token — either the component genuinely doesn't touch this colour category, or the scan's meta-text matching missed a prose-only reference): Badge, Links

### 1.4.3

18/18 applies_to components have a bound audited token. 18 pass, 0 fail, 0 not_covered.


## Unverified (applies_to only)

| SC | Note |
|---|---|
| 1.1.1 | no executable check in this build |
| 1.2.2 | no executable check in this build |
| 1.2.5 | no executable check in this build |
| 1.3.1 | no executable check in this build |
| 1.3.2 | no executable check in this build |
| 1.3.5 | no executable check in this build |
| 1.4.1 | no executable check in this build |
| 1.4.10 | no executable check in this build |
| 1.4.13 | no executable check in this build |
| 1.4.4 | no executable check in this build |
| 2.1.1 | no executable check in this build |
| 2.1.2 | no executable check in this build |
| 2.2.1 | no executable check in this build |
| 2.2.2 | no executable check in this build |
| 2.4.1 | no executable check in this build |
| 2.4.11 | no executable check in this build |
| 2.4.3 | no executable check in this build |
| 2.4.4 | no executable check in this build |
| 2.4.5 | no executable check in this build |
| 2.4.6 | no executable check in this build |
| 2.4.7 | no executable check in this build |
| 2.4.8 | no executable check in this build |
| 2.5.7 | no executable check in this build |
| 3.3.1 | no executable check in this build |
| 3.3.2 | no executable check in this build |
| 4.1.2 | no executable check in this build |
| 4.1.3 | no executable check in this build |

## Granularity note

`1.4.3` and `1.4.11` now verify at **component** granularity: the token-level contrast audits are joined through `tokens/_blast-radius.json` (component -> tokens it binds) down to a per-component pass/fail/not_covered verdict — see the breakdown above. `2.3.3` and `2.5.8` already verified at component granularity, since the a11y gate runs per reference snippet (one per component).

