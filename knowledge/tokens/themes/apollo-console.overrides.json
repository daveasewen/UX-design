{
  "$description": "Apollo Console override set (ADR-0011). Console INHERITS Mono's palette (Dave 2026-07-21: Console + Supercharge inherit Mono; dataviz + status/RAG identical across Mono/Console/Supercharge — only Legacy differs). The one live divergence NOW is shape: Console = rounded corners. Everything absent here falls back to Mono. Anticipated flex slots (UI greys, future input error-condition colour) are declared in _themes.json declaredSlots per ADR-0010 — a value can be added here any time without re-architecture.",
  "$theme": "apollo-console",
  "$attr": "console",
  "$schemaVersion": "1.0.0",
  "overrides": {
    "border-radius/default": {
      "$value": 8,
      "$type": "number",
      "$note": "Dave 2026-07-21: Console = rounded corners NOW. Base radius — every ROLE token (control/surface/indicator) follows this via its alias unless dialed itself (Dave, same evening: one radius can't be universal; maximum flexibility, theme-generator horizon). The 8px VALUE is PROVISIONAL (agent proposal, rule on the harness)."
    },
    "border-radius/surface": {
      "$value": 12,
      "$type": "number",
      "$note": "PROVISIONAL (agent). Deliberately different from control (which inherits default=8) to DEMONSTRATE role-level divergence live — cards/dialogs rounder than buttons, Dave's own example. Both dials independent; both one-line edits."
    }
  }
}
