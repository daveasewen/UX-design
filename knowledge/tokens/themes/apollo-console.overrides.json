{
 "$description": "Apollo Console override set (ADR-0011). Console INHERITS Mono's palette (Dave 2026-07-21: Console + Supercharge inherit Mono; dataviz + status/RAG identical across Mono/Console/Supercharge — only Legacy differs). The one live divergence NOW is shape: Console = rounded corners. Everything absent here falls back to Mono. Anticipated flex slots (UI greys, future input error-condition colour) are declared in _themes.json declaredSlots per ADR-0010 — a value can be added here any time without re-architecture. || s158-D4 (Dave, 2026-08-12) OPTION B — SINGLE SOURCE: the twelve palette-owned rag hue keys (error/warning/success/information x bare/-glyph/-background) were TRIMMED from this file; the palette file named by this theme's ragPalette in _themes.json is now their ONE source, projected by canon/gen_theme_cascade.py BEFORE the override set. Every $note that lived on those entries was relocated VERBATIM into the palette file's matching key FIRST (add-then-trim), so no ruling provenance was lost. -tint keys STAY here per s123-D3 (tints derive from per-theme grounds and are NOT palette-owned); rag/text/* stays here too. Enforced by _validate_palette_tier.py --strict-absence.",
 "$theme": "apollo-console",
 "$attr": "console",
 "$schemaVersion": "1.0.0",
 "overrides": {
  "border-radius/default": {
   "$value": 4,
   "$type": "number",
   "$note": "Dave 2026-07-22: base/control = 4 (the 4px try; was 8, provisional). control + indicator follow this via alias. Roles are dialed INDEPENDENTLY — see surface below — so corners tune per component type, not uniform. Experiment; rule on the harness."
  },
  "border-radius/surface": {
   "$value": 12,
   "$type": "number",
   "$note": "Dave 2026-07-22: RESTORED to 12 (cards/dialogs rounder than buttons) — Dave: corners must tune per component type, not uniform. Independent dial from control(=4). Value provisional; one-line edit."
  },
  "rag/error-tint": {
   "light": {
    "$value": "#EECDC9",
    "$type": "color"
   },
   "dark": {
    "$value": "#60231C",
    "$type": "color"
   },
   "$note": "s123-D3 (Dave, 2026-08-07, #123, tuner v2 export): CONSOLE tint = TUNED OPACITY — #B92F1E at --alpha-24 (light) / --alpha-44 (dark) over background/default, stored composited. Console rag/* fence lifted s122-D4."
  },
  "rag/warning-tint": {
   "light": {
    "$value": "#F2DEB1",
    "$type": "color"
   },
   "dark": {
    "$value": "#654D14",
    "$type": "color"
   },
   "$note": "s123-D3 (Dave, 2026-08-07, #123, tuner v2 export): CONSOLE tint = TUNED OPACITY — #D5990B at --alpha-32 (light) / --alpha-40 (dark) over background/default, stored composited. Console rag/* fence lifted s122-D4."
  },
  "rag/success-tint": {
   "light": {
    "$value": "#D2E8DA",
    "$type": "color"
   },
   "dark": {
    "$value": "#355441",
    "$type": "color"
   },
   "$note": "s123-D3 (Dave, 2026-08-07, #123, tuner v2 export): CONSOLE tint = TUNED OPACITY — #5DAC7B at --alpha-28 (light) / --alpha-40 (dark) over background/default, stored composited. Console rag/* fence lifted s122-D4."
  },
  "rag/information-tint": {
   "light": {
    "$value": "#D1DDEE",
    "$type": "color"
   },
   "dark": {
    "$value": "#364963",
    "$type": "color"
   },
   "$note": "s123-D3 (Dave, 2026-08-07, #123, tuner v2 export): CONSOLE tint = TUNED OPACITY — #4F77B0 at --alpha-28 (light) / --alpha-44 (dark) over background/default, stored composited. Console rag/* fence lifted s122-D4. | s132-D1 (Dave, #132, ruled off info-blue-controller v1): information fill #4F77B0 -> #5A85C1, mark FLIPS WHITE -> INK #1A1A1A (4.61:1) - error is now the SOLE white mark on console/SC. Mode-invariant unchanged. Tint re-composited from the new fill at the s123-D3 ruled alphas: light #D1DDEE (28% over #FFFFFF), dark #364963 (44% over #1A1A1A). (was #CED9E9/#31435C)"
  },
  "rag/text/on-information": {
   "light": {
    "$value": "#FFFFFF",
    "$type": "color"
   },
   "dark": {
    "$value": "#FFFFFF",
    "$type": "color"
   },
   "$note": "Dave, 2026-08-14, #170: white error text OUTSIDE mono. Console + Supercharge take the same white on-information ink Legacy carries (s131-D1 shape), so the reversed error/information text is not graded as mono's dark ink. Mode-invariant (#FFFFFF light + dark)."
  }
 },
 "$marksNote": "s122-D3 (Dave, #122): the glyph knockout on each Console RAG fill. --mark-* is not a store token — gen_theme_cascade.py emits it from this key. Mode-invariant.",
 "marks": {
  "error": {
   "light": {
    "$value": "#FFFFFF",
    "$type": "color"
   },
   "dark": {
    "$value": "#FFFFFF",
    "$type": "color"
   },
   "$note": "s122-D3: glyph knockout on the error fill #B92F1E = 6.02:1."
  },
  "warning": {
   "light": {
    "$value": "#000000",
    "$type": "color"
   },
   "dark": {
    "$value": "#000000",
    "$type": "color"
   },
   "$note": "s122-D3: glyph knockout on the warning fill #D5990B = 8.39:1."
  },
  "success": {
   "light": {
    "$value": "#1A1A1A",
    "$type": "color"
   },
   "dark": {
    "$value": "#1A1A1A",
    "$type": "color"
   },
   "$note": "s122-D3: glyph knockout on the success fill #5DAC7B = 6.34:1."
  },
  "info": {
   "light": {
    "$value": "#1A1A1A",
    "$type": "color"
   },
   "dark": {
    "$value": "#1A1A1A",
    "$type": "color"
   },
   "$note": "s122-D3: glyph knockout on the information fill #4F77B0 = 4.57:1."
  }
 },
 "$guardsNote": "s158-D1 (Dave, #158): GUARDS. Not token overrides — declarations that THIS theme keeps a paint a later MONO-ONLY ruling moved away from it. They were hand-edited into canon.css's AUTO-THEMES block (which says do NOT hand-edit), and that is what held gen_theme_cascade --check red. Sourced here so the generator emits them and canon.css is clean generated output again. Provenance: .cn-banner .banner.err ink + .cn-tabs --badge-bg/--badge-ink = s149-D1 clauses (1) and (3), scoped 'this is right for mono only' — legacy s131-D1 / console+supercharge s132-D1 stay as built; --error-atom: var(--error) = s151-D1, an explicit NO-OP guard so the two-red law's atom fork cannot reach a theme that owns its own red. placement 'sorted' joins the sorted declaration namespace, 'append' is emitted after it in authored order (both reproduce the ratified bytes).",
 "guards": {
  "badge": {
   "placement": "sorted",
   "$noteLight": [
    "s160-D2 is MONO ONLY — console's badge/background override is untouched, so its numeral keeps the",
    "WHITE ink (type26-013) rather than inherit mono's new rag/text/on-light seat."
   ],
   "declarations": {
    "--number": {
     "value": "var(--text-reverse)"
    }
   }
  },
  "banner": {
   "rules": [
    {
     "$note": [
      "s149-D1 is MONO ONLY — console s132-D1 untouched: state the white ink explicitly, since mono's",
      ".banner.err default (which console previously inherited) has moved to #1A1A1A."
     ],
     "selector": ".cn-banner .banner.err",
     "body": "--ink:var(--on-dark);"
    },
    {
     "$note": [
      "s149-D1's WASH MEDIUM is MONO ONLY too — console s132-D1 untouched: mono's error-banner action washes",
      "moved to WHITE-fixed 8%/14%; this theme keeps the ink-derived 14%/22% wash exactly as built.",
      "Restated here because mono's .banner.err default (which this theme inherited) has changed."
     ],
     "selector": ".cn-banner .banner.err .actions .abtn:hover",
     "body": "background:color-mix(in srgb, var(--ink) 14%, transparent);"
    },
    {
     "$note": [
      "s149-D1 wash medium, pressed leg — see the hover note above."
     ],
     "selector": ".cn-banner .banner.err .actions .abtn:active",
     "body": "background:color-mix(in srgb, var(--ink) 22%, transparent);"
    }
   ]
  },
  "selection-controls": {
   "placement": "sorted",
   "declarations": {
    "--error-atom": {
     "value": "var(--error)",
     "$comment": "s151-D1: fork keeps its OWN red for the atom — this is a NO-OP guard, not a new fork value"
    }
   }
  },
  "tabs": {
   "placement": "append",
   "$noteLight": [
    "s149-D1 is MONO ONLY — console s132-D1 untouched: preserve the pre-existing .ovcount paint",
    "(--tabs-active / --text-reverse) rather than inherit mono's new badge seat."
   ],
   "declarations": {
    "--badge-bg": {
     "value": "var(--tabs-active)"
    },
    "--badge-ink": {
     "value": "var(--text-reverse)"
    }
   }
  }
 }
}