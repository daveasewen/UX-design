{
  "$description": "Apollo Supercharge override set (ADR-0011). Supercharge INHERITS Mono's palette in full (Dave 2026-07-21) — this set is deliberately EMPTY: it exists so the divergence hooks are live (renders as Mono, labelled, in the harness) and a future brand-uplift palette is a token edit here, never a re-architecture. Anticipated flex slots are declared in _themes.json declaredSlots per ADR-0010.",
  "$theme": "apollo-supercharge",
  "$attr": "supercharge",
  "$schemaVersion": "1.0.0",
  "overrides": {
    "progress/complete": {
      "light": { "$value": "#B92F1E", "$type": "color" },
      "dark":  { "$value": "#CC4333", "$type": "color" },
      "$note": "FIRST Supercharge divergence (Dave 2026-07-21, showroom progress-tracker review: 'in mono and console it should be black. the colours in legacy and supercharge are fine') — Mono/Console went to the ink pair; Supercharge KEEPS the status-red pair it displayed at ruling time. Supersedes this file's 'deliberately EMPTY' description for this one path; Supercharge ≡ Mono everywhere else."
    }
  }
}
