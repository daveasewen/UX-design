# _RADIUS-GATE — border-radius is token-bound (theme flex slot, ADR-0010)

*Generated by `_validate_radius.py`. Allowed values: `var(...)`, `50%` (circle),
`999px` (pill idiom), `inherit`. STRICT = canon + migrated snippets (blocking).*

## ✅ STRICT surfaces clean (canon + 35 migrated snippet(s))

## ✅ no advisory hardcodes remain
