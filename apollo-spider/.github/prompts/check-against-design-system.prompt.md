---
mode: agent
description: Review a UI candidate against the Apollo design system and report where it drifts.
---

Read `skills/check-against-design-system/SKILL.md` in this workspace and follow it for the request below.

That file is the instruction set — open it before you answer, and do what it says rather than
what you would have done anyway. It is written against **this** design system, so its rules
about tokens, components, variants and states are the ones that apply here.

If the user gave no specific target with the slash command, ask what they want it run against
before starting.

${input}
