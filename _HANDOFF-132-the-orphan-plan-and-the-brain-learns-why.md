# HANDOFF #132 — #281 → #282 — THE ORPHAN PLAN, SIX RULINGS, AND THE BRAIN LEARNS WHY

provenance: 281 · 2026-09-17
status: observed

## ⛔ READ FIRST, IN THIS ORDER

1. **THIS FILE.** It is newer than `_CHAIN.md` and **OUTRANKS** it. It does **NOT** replace
   `_HANDOFF-130` or `_HANDOFF-131`, whose open items still stand — with **two named exceptions**:
   ✅ **#131's ORPHAN CENSUS EXPORT is CLOSED** (it arrived 11:02Z carrying **eleven** radios, not
   the ten #131 predicted, every one on the recommendation, and five lanes enacted it) and
   ⛔ **#131's EYE-CHECK OF THE SIX MATRIX CELLS IS STILL NOT DONE** — asked at the #280 wrap,
   asked again here, and it is **#282's second move**. #130's four ruling-shaped questions
   (`designrulings`, a thirteenth verb + `verbVia`, the Spider `v1.0.14` cut, the `va25-013` rule
   split) are **untouched by #281 and still stand**.
2. `_CHAIN.md` (the read chain: GM header → ★ LATEST banner → `_LIVE-STATE.md`'s ⏱ LATEST delta).
3. `notes/_lanes/281/DAVE-RULINGS-2026-09-17.md` — **his words verbatim**, committed before this wrap.
4. Everything beyond that is **RETRIEVAL**, never a reading list:
   `python3 knowledge/_memento_search.py "<q>"` → `--fetch <id>`.

## ⛔⛔ DAVE'S — VERBATIM. Source: `notes/_lanes/281/DAVE-RULINGS-2026-09-17.md`

- Opener: *"Good Morning!"* · *"can you surface teh files"* · **the sentence the whole day came
  from:** *"can you recut the orphans page with recommendations and detail, guidance: making a
  designer brain with the best context we can provide"*.
- **The orphan-plan export, 11:02:54Z** — **all eleven choices match the recommendation**: wire ×4
  (homeless-edges, shapes, intents, roles) · leaf ×2 (base-rules-chip-off, logos) · ruling ×5
  (ux-cloud, polarity-reach, rules-in-the-constitution, obeys-ux, rests-on).
- A note on polarity-reach: *"Explanation ist a great label maybe something like 'UX Theory'"* —
  read as *"isn't"*, and **NOT inscribed as a rename**; put as a question. ⚠ `s281-D5` renamed the
  **triad** to SYSTEM · GOVERNANCE · THEORY, which touches the same word — **whether the
  Explanation VIEW is also renamed is NOT ruled and is his.**
- A note on logos: ***"we need to do the logo review soon"***.
- **The decisions export, 11:58:34Z** — **all five picks = (a)**; q1 and q2 clean, q3/q4/q5 carrying
  *"let's talk"* notes and **NOT inscribed until the chat**. After the chat: ***"Okay these all look
  good to me, thanks for the explanation"*** ⇒ q3 (a), q4 (a), q5 = **System · Governance · Theory**.
- **The rests-on export, 14:49:41Z** — **59 answers**: 13 *"both 1 and 2"* · 8 overruled · 8
  convention · **13 notes about the RULE itself, filed and not edited**.
- Last words of the session: ***"are we wrapped?"***

## ★ WHAT LANDED — 9 commits, all pushed by this wrap

**SIX RULINGS, `knowledge/_rulings.json` 607 → 613, every one by TEXTUAL SPAN and every one
inscribed by the lane that built it.** `git show --numstat` on the store reads `22  0` (`b95042c`),
`52  0` (`ea2c87e`), `47  0` (`d23aed9`), `28  0` (`7f714f0`) — **zero deletions anywhere** — and a
`json.load` counts **613**.

| id | commit | what it says |
|---|---|---|
| `s281-D1` | `b95042c` (PH) | research families are **NODES**; `inFamily` + `evidencedBy` on |
| `s281-D2` | record rode in `ea2c87e` (RO) — **carrier wrong, declared in FO's own report**; enacted `fd85bbe` (FO) | the guideline family owns a **double-named file**, and the **BUILDER declares the tie** |
| `s281-D3` | `ea2c87e` (RO) | **`restsOn` rule→ux is an AUTHORED edge type**; the 59 BLOCKING rules go first |
| `s281-D4` | `d23aed9` (TV) | a design's citation of a principle is drawn in the **third view**, *"cited by a design"*; governance stays at **three** provenances; **force by grade NOT ruled** |
| `s281-D5` | `d23aed9` (TV) | the triad is **SYSTEM · GOVERNANCE · THEORY**, labels only |
| `s281-D6` | `7f714f0` (RL) | `restsOn` is **many-to-many**, his *"both"* lands both; **a rule NOTE is not a rule EDIT** |

**THE EXPLORER WENT 1.20 → 1.25 AND THE DARK DOTS WENT 157 → 8.**

- **CM `06e28bb`** — 1.21, six edge types get a family, **dark 157 → 108**, two extra fall-throughs
  fixed (type chips; `EFAMFILE`). Report names this seat's **`unlink` refusal under the mount**.
- **PH `b95042c`** — 1.22, **32 family nodes, 145 `inFamily`, 134 `evidencedBy` + 11 declared
  nulls**, **dark 108 → 8**. DECLARED: `knowledge/_kg_explorer.template.html` touched outside the
  brief's file list.
- **RO `ea2c87e`** — the 59-card page; **52 on a principle, 7 convention-proposed. Nothing landed,
  by design.**
- **FO `fd85bbe`** — 1.23, **14 files tied (not the brief's 3)**, 5 guideline files re-homed,
  census **set 06 24 → 0**.
- **TV `d23aed9`** — 1.24, the 14 citation lines drawn, the triad renamed, `$why` read on **168**
  meta lines.
- **RL `7f714f0`** — 1.25, **65 `restsOn` edges + 8 declared nulls across 52 of 59 rules**, **13
  doubles**, **36 of the 73 lines carrying his own sentence unedited**, and the 15 rule notes filed.
- Conductor: **`2904db0`** (the orphan plan, the decision page, export 1) · **`0a9a297`** (the
  decisions export + his words) · **`402bf9d`** (the rests-on export + his words).

Every lane filed its report: `notes/_subreports/2026-09-17-281-CM-chip-map.md` ·
`-PH-principles-home.md` · `-RO-rests-on.md` · `-FO-file-owner.md` · `-TV-theory-view.md` ·
`-RL-rests-on-land.md`. This wrap's own: `notes/_subreports/2026-09-17-281-W-wrap.md`. Narrative
dossier (1b): `_DECISION-HISTORY/2026-09-17-281-the-orphan-plan-and-the-brain-learns-why.md`.

## ⬛ #282 — FIRST MOVES, IN ORDER

1. **THE 15 RULE NOTES — `notes/_lanes/281/rests-on-land/RULE-NOTES-2026-09-17.md`. HIS, ROW BY
   ROW.** 13 rows argue with the rule itself; 2 ride on a *"both"* answer (`col26-016`,
   `neuro-026`); a sixteenth (`type26-003`) is filed as a **borderline and deliberately NOT
   landed**. ⛔ **Nothing is inscribed and no guideline file has been changed** — `s281-D6` is what
   kept it that way. Enacting any row is a guideline edit plus a regen of `_rules-index.json`.
2. **THE EYE-CHECK OF THE SIX MATRIX CELLS.** Carried from #131, asked twice, still not done.
   Contact sheet: `notes/_lanes/280/layout-matrix/MATRIX-2026-09-16.html`. ⚠ The conductor's
   expectation that Floors or Orbits will go is an **expectation, not a finding**, and no lane may
   thin the matrix on it.
3. **⛔⛔ THE LOGO REVIEW. PRESSED, NOT PARKED.** He asked for it by name again today
   (*"we need to do the logo review soon"*), and it has been parked with a tripwire since **#277**
   (`P-277-4`). **It is now the oldest thing he has asked for twice and not been given.**

## ⬛ OPEN, RULING-SHAPED, NOT INSCRIBED — the `s271-D4` form: every one is a QUESTION PUT

Bodies and receipts: `_CARRIES.md` § `## residual → #282` (**521 probeable items, 7 new**).

- **FORCE BY GRADE** — `s281-D4` deliberately did not rule it, naming the `restsOn` export as its
  precondition. **That export has landed, so the question is live.** Its shape if ruled: a second,
  DERIVED sub-chip beside the authored one, dashed.
- **THE GHOST LAYER'S COLOUR** — drawn family or stored family. Two lanes met it (TV at 75 px, RL at
  **42,192 px**); **both recommend as shipped, and both say ONE LINE freezes the default canvas** if
  he wants it instead.
- **WHERE AN OBLIGATION ON A UX PRINCIPLE IS DRAWN** — the 14 `obeys` → `ux:` lines are **held**.
  `s277-D8` fixed governance at three provenances. CM's three options stand; **`s281-D1` made
  option (b) cheaper**, and PH recommends re-putting the question with the new price.
- **FO's TWO UNTIED GOVERNANCE ARTEFACTS** (`_rules-index.json` and the bare `knowledge/guidelines/`)
  **and whether a REPO TOOL deserves its own family** — row **`W-281fo`**, open, closes on his word.
- **THREE ASKs AND ONE BORDERLINE** — `col26-012` (*"I'm actually not sure about this"*), `aid-009`
  (*"maybe we need to discuss"*; the note under it is that the rule is **"too boolean"** — a RULE
  conversation), `type26-002` (landed **both ways** under `s281-D6`), and `type26-003` **not landed**.
- **`gen_kg_rules.py` WOULD DELETE ALL 73 HAND-AUTHORED `restsOn` LINES** — a live footgun, named by
  RL and not fixed (the generator was outside its file list). One preserve-block is the honest fix.
  And **`inFamily` is read by no verb** while `evidencedBy`'s verb now lies to half its edges —
  PH's three options, recommending (a).
- **THE SANDBOX REFUSES `unlink` UNDER THE MOUNT** (`rm`, `mv`, `os.remove` all `Operation not
  permitted`), so **a gate-failed commit run can leave `.git/index.lock` behind** for a seat that
  cannot clear it. **A runbook line is owed and it is his.**
- **WHETHER THE EXPLANATION VIEW IS RENAMED** on his *"maybe something like 'UX Theory'"* — the
  triad was renamed by `s281-D5`; the view was not.
- **Carried unchanged:** `jade-lifestyle` · the shells-3D cutaway · the sandbox's stripped snippet
  scripts · #130's four · **`s277-D12` (tokens at group+tier) NEVER STARTED — a THIRD session** ·
  the #278/#279 (f) items (`s214-D6` below band, the `s129-D1` boot-floor label, the `#243` form now
  a **seventh** wrap, the two-probe-shape §A question, the wrap-sub memory seat limit).

## ⛔ STRUCTURAL REDS — inherited, measured here, and NOT repairable by a wrap

- **SIX BLOCKING GATE FAILS** in `notes/_GAUGE-LOG.md`, all another session's **append-only
  testimony**: the boot-drift **ceiling breach** (7 post-diet readings over the shrink-only 70,000
  literal — **raising it is Dave's word alone**) and **five boot double-counts** (#243, #264, #272,
  #273, #274). The commit therefore took the `#243` **DECLARED not-a-wrap** path with
  `SESSION_N=281` — the **seventh** wrap running. ⬛ **That a genuine wrap must declare itself
  not-a-wrap is ruling-shaped and is his.**
- ✅ **A SEVENTH WAS RED AND IS HEALED HERE:** `notes/_RULINGS.html` was **stale** against
  `_rulings.json` after six rulings landed — rebuilt and staged at 4d (`s263-D10`).
- **`_validate_roles_resolve.py` FAIL(6)** (all six `data-grid` `with`-slugs) ·
  **`_validate_lane_ownership.py --selftest` 2/3** · **`showroom/` holds 138** top-level `.html`
  against an inscribed *"108 stale"* — **both readings stand, neither is rewritten, and it is his**.

## ⚙ THE NUMBER THIS SESSION MUST HAND FORWARD

**FILL 261,606 real over 56 turns**, measured at the wrap seat against the conductor's own
transcript, against **256,287 / 54 DECLARED** at the brief cut (**delta 5,319**, same window, so the
subtraction is legal). ⛔⛔★★★ **THE 256,000 HARD LINE IS BREACHED BY 5,606 — the second breach on
the record after #277's** — 81,606 past the ruled 180,000 stop line and **41,606 outside the
≤220,000 tolerance**. The breach was **declared on every turn from 232,843 onward** and lanes kept
being cut as his exports arrived. **BOOT 77,400 real, n=1** (+6,606 over the `s129-D1` floor, **NOT
a re-base**; the conductor did not record boot at the opener, so this is the wrap seat's own
reading). **subs 1,273,123 across n=6** — QUOTA, never FILL. **No pace panel was asked for.**
`/sessions` **99%**.

⬛ **What a session should DO at the hard wall is STILL NOT RULED, and the gauge has no arm that
fires there.** Carried since #277; it is his.

## ★ NEXT CHAT TITLE

```
Apollo - #282: the rule notes and the eye-check
```
