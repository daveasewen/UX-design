# HANDOFF #146 — #295 → #296 — THE PUSH LANDED, CI WAS READ JOB BY JOB, AND THE WRAP GATE REACHED ZERO

provenance: 295 · 2026-09-22
status: observed

*Written by the delegated OPUS 5 wrap sub at the close of #295 (conductor Fable 5.1). Every figure
here was MEASURED at this seat unless it says otherwise; where a measurement disagrees with the
brief's declaration, both readings are published and neither is rewritten.*

⛔★★ **DATE SPLIT, THE SIXTH ON THIS RUN.** The session opened Monday **2026-09-21**; all four lanes
ran that day and **all eleven of its commits carry it** — `95cb58dd` · `9d8c728b` · `d30aebe1` ·
`280e4e82` · `016854c1` · `0d487352` · `89f09db9` · `ac6b3810` · `ac55c7b2` · `c896bcaf` · `5e9e2cbe`.
The ritual and this file are Tuesday **2026-09-22**; `date` at this seat read
`Tue Sep 22 08:03:58 BST 2026`. ⛔ **Nothing was re-dated**: the four lane reports keep their
`2026-09-21-295-*` stems, `notes/_lanes/295/DAVE-RULINGS-2026-09-21.md` keeps its day, the four
`s295-D*` ids keep theirs, and **a SIXTH header line was added to `GOOD-MORNING.md` by addition with
the five standing untouched** — which is the shape **`s294-D11` RULED**, not a restraint improvised
again.

⛔★★★ **FOUR RULINGS WERE INSCRIBED AND ALL FOUR ARE ENACTED IN CODE. `knowledge/_rulings.json` READS
638** — verified here by `json.load` over the `rulings` list, **id and count only, never a printed
record**: **634 → 638**, the newest four reading `s295-D1` · `s295-D2` · `s295-D3` · `s295-D4`. His
authority was ***"inscribe"*** three times and ***"cool do it inscribe"*** once.

★★★ **AND THE HEADLINE IS A NUMBER THAT HAD NOT MOVED IN TWENTY WRAPS: THE WRAP GATE READS ZERO
STRUCTURAL FAILS.** Measured at this seat before any write: **232 in scope · 2 fail · 369 warn**, and
⛔ **BOTH of the two are this ritual's OWN date stamps**, which steps 1 and 2 close. **The seven
inherited fails are gone, and the `#243` DECLARED NOT-A-WRAP FORM IS NOT USED AT THIS WRAP.**

---

## ⛔ READ FIRST, IN THIS ORDER

1. **This file.** It is newer than `_CHAIN.md` and **OUTRANKS it**.
2. `_CHAIN.md` — the read contract (header → ★ LATEST banner → ⏱ LATEST delta). Figure and ratio in
   § THE NUMBERS below, quoted from its own generated footer after the declare-last `size:` stamp.
3. ⛔ **It does NOT replace `_HANDOFF-130`…`-145`.** Every open item on those sixteen still stands
   **except the THREE struck here with receipts** — see § THE STRIKES.
4. `notes/_lanes/295/DAVE-RULINGS-2026-09-21.md` — his eight entries, verbatim, in order, with what
   was done on each. **Quote them; never paraphrase.**
5. `_CARRIES.md` § `residual → #296` when you need the bodies — **642 probeable items**. Do not read
   it at boot; fetch the section (`_memento_search.py` → `--fetch carries:residual-296`).
6. The four filed lane reports, `notes/_subreports/2026-09-21-295-{I,P,I2,I3}-*.md`, plus this wrap's
   `notes/_subreports/2026-09-21-295-W-wrap.md`.

---

## ⛔⛔ DAVE'S WORDS — EIGHT ENTRIES, VERBATIM

> Good Morning!

> 1. i need to see them
> 2. I don't know the answer to this
>
> BTW, we need to get back to the presentation soon

> inscribe

> 1. I can't cut anything else permanently, this is possibly the new ceiling
> 2. cool do it inscribe

> inscribe

> wrap

⚠ **He did NOT separately say "go" on the push** — it was taken at the conductor's judgement under the
standing instruction, and every lane that pushed declared that. ⚠ **The presentation line arrived
unprompted, in the middle of a ruling turn**, and it is the top of § OWED.

---

## WHAT LANDED — ELEVEN COMMITS, ALL PUSHED

**Lane I — `95cb58dd` · `9d8c728b`.** `s295-D1` and `s295-D2` inscribed and both enacted in
`knowledge/_capture_gate.py`. **The wrap gate went 7 → 1, measured at both ends**, the one remaining
being the ceiling breach this lane could not have closed. ⛔ **And one thing was REFUSED rather than
improvised: the eleven of #294 were NOT back-stamped** — the sanctioned writer has no path to
`status`, and the available half-stamp would have produced records **still reading `status: ruled`**
while carrying an enactment sha, moving **zero** of the 98. *"A convincing-looking half of a ruling is
worse than an honest absence."*

**Lane P — `280e4e82` · `016854c1`.** **Twelve** commits pushed (`dd374131` … `d30aebe1`) — ⛔ **twelve
MEASURED where `_HANDOFF-145` estimated six, and neither figure is rewritten** — and CI run
`35642904396` read **job by job**. ★ **The first CI run that exists on any of the twelve.**

**Lane I2 — `0d487352` · `89f09db9`.** `s295-D3`: `BOOT_CEILING_TK` **70,000 → 72,768** on his word.
⛔★★ **And the headline is this lane's own brief being wrong, measured: the re-base did NOT close the
breach and the rehearsal stayed at 1.** CI run `35646532403`.

**Lane I3 — `ac6b3810` · `ac55c7b2` · `c896bcaf` · `5e9e2cbe`.** `s295-D4`: the regime boundary
**241 → 295** — **the wrap gate reaches ZERO**. The `--set-status` writer BUILT and **fourteen**
rulings back-stamped. `_gen_chain.py --check` **stops being red by construction**, proven green on a
pushed tree. CI run `35652886287`.

---

## ⛔ THE FOUR LANES — ALL FOUR VERIFIED

Every lane's transcript exists under `…/e298bf6b-…/subagents/agent-*.jsonl` and was read with the same
`_checkin.read_fill`. **Measured / declared:** I **165,794 / 166,974** · P **132,012 / 133,668** ·
I2 **127,516 / 129,194** · I3 **265,483 / 267,906**. ⛔ **The declaration reads HIGH on all four and
LOW on none — a systematic offset of 6,937 (0.99%)**, the same direction and nearly the same magnitude
as #294's 8,353 (0.59%) across eight lanes. **Neither reading is corrected into the other.**

---

## ⚙ THE NUMBERS, MEASURED AT THIS SEAT

**FILL 219,167 real · peak 219,167 · boot 72,768 · 24 continuous turns of 26 · 0 compaction records ·
0 drops**, off the conductor's top-level transcript
`/sessions/happy-magical-mayer/mnt/.claude/projects/session/e298bf6b-64b1-4de6-a59c-48c83a68d0f1.jsonl`.

⛔★★ **FOR THE FIRST TIME IN SIX WRAPS THE CONDUCTOR'S DECLARED SEAM READINGS DO NOT REPRODUCE.**
Declared: **214,824 / 23 turns** at the wrap recommendation, **218,396 / 25 turns** at the brief's cut.
**Measured: neither figure is in the per-turn cumulative series**, whose nearest distinct readings are
**215,019** (the 23rd) and **219,030** (the 25th) — **+195** and **+634**. ✅ **The BOOT agrees to the
token at 72,768**, which is what identifies the window. **Both series published, neither rewritten**;
the shape suggests the declarations were taken mid-turn rather than off a completed `usage` record, and
that is a reading of the difference, not a claim about its cause.

**Bands:** 39,167 past the 180,000 quality line — **DECLARED**; **200,000 exceeded by 19,167 —
DECLARED**; 220,000 clear by 833; 256,000 clear by 36,833. ⛔ **No literal was moved.**

**`wrap-handover: brief-cut 218,396 · sub-cut 219,167 · delta 771 · replay unobservable`** — the
**smallest** hand-over on the `s214-D5` record (against #294's 9,453, #293's 1,871, #292's 954),
because no conductor turn falls between the brief's cut and the reading. ⚠ **A legal subtraction, but
weaker evidence than the last three: the identification rests on the boot alone, because the seam
readings do not agree.**

★★★ **HE STOPPED ON THE INSTRUMENT.** The 214,824 / 23 reading was quoted to him past the 180,000 line
and his whole reply was ***"wrap"***. ⇒ **#283, #290, #293, #294 and #295 stopped ON the instrument;
#291 and #292 each overrode once then obeyed. `s283-D1`'s override shape STAYS AT n=2 and nothing is
inferred from this session.**

**BOOT, AND THIS IS THE FIRST WRAP IN TWENTY-ONE AT WHICH IT IS NOT A BREACH.** 72,768 is **equal to**
`BOOT_CEILING_TK`, because `s295-D3` re-based the literal TO that reading. The seven older readings
(#283 80,871 · #287 74,120 · #288 74,174 · #289 74,174 · #290 74,165 · #291 74,155 · #292 74,170) are
now **REPORTED and never CHARGED** under `s295-D4`'s boundary, and they are named in the gate's own
note every run.

---

## ⛔ THE GATES — SEVEN IN AT #294, ZERO IN AT #295

| reading | when | fails |
|---|---|---|
| inherited at #294's wrap | #294 | **7** |
| after `s295-D1` (lane I) | #295 | **1** |
| after `s295-D3` (lane I2) | #295 | **1** — ⛔ *the re-base did not close it* |
| after `s295-D4` (lane I3) | #295 | **0** |
| at this ritual's open | #295 | **2** — ⛔ **both this ritual's OWN date stamps** |
| at this ritual's close | #295 | see § THE COMMIT PATH |

⛔★★ **THE `#243` FORM IS NOT USED. The commit went on the TRUE `--wrap` path** — the first in
twenty-one sessions. Details and the gate's exact closing reading are in § THE COMMIT PATH.

**CI, three shas, read job by job:**

| run | sha | `gates` | `release` | `render` | survey |
|---|---|---|---|---|---|
| `35642904396` | `d30aebe1` | ⛔ steps 5+6 | ✅ | ✅ | 58 pass · **7 FAIL** |
| `35646532403` | `0d487352` | ⛔ steps 5+6 | ✅ | ✅ | 58 pass · **7 FAIL** |
| `35652886287` | `c896bcaf` | ⛔ steps 5+6 | ✅ | ✅ | 59 pass · **6 FAIL** |

⚠ **`render` stayed `in_progress` 8–11 minutes after the other two closed at EVERY run, and every
verdict was taken AFTER it finished** — the #291/#292 lesson, met a sixth time. **The six remaining
survey fails, BY NAME** (never by number — step IDs move): `[3]` token blast-radius · `[13]`
capture/provenance selftest · `[38]` component-partials sync · `[125]` memento schematic determinism ·
`[128]` memento-package delta-audit · `[136]` governs matcher selftest.

---

## THE THREE STRIKES, AND WHAT WAS DELIBERATELY NOT STRUCK

By addition, `s183-D1` form, each with the receipt `s188-D2` demands.

1. ⛔ **`_HANDOFF-145` § OWED 1 — the push and a CI read-back job by job — STRUCK, CLOSED BY ACT.**
   Receipt: **`5e9e2cbe`**, `git ls-remote origin refs/heads/master` = local HEAD, `git log
   origin/master..master` empty; three CI runs read job by job, tabulated above. ⚠ **The headline is
   struck and the clauses are not:** `--push` is still UNPROVEN end to end, and the gate's own
   selftest fails in CI — both below.
2. ⛔ **`_HANDOFF-145` § OWED 3 — stamp the twelve `status: enacted` — THE HEADLINE IS STRUCK.**
   Receipts: `s295-D2` (`95cb58dd`) and the `--set-status` writer (`ac55c7b2`); **fourteen stamped**,
   proven by `json.load` against the `HEAD` blob — *exactly 14 records differ, exactly the plan,
   nothing outside `status`/`evidence`*. ⚠ **THE CLAUSE *"all twelve"* IS NOT STRUCK, and two are
   named:** `s294-D10` is the Jev DECISION, not code; **`s294-D11` is enacted in the RITUAL SURFACES
   at `359b646b` with not one `.py` file touched**, so `f81bbdd4` is not its sha and none was
   invented. ⛔ **And the count was wrong: `f81bbdd4` enacted TEN in code, not eleven, though this
   file's predecessor says eleven in two places.**
3. ⛔ **`_HANDOFF-145` § OWED 9 — boot 74,656 against a shrink-only ceiling of 70,000 with seven
   post-diet readings over it — STRUCK.** Receipts: **`s295-D3`** (the literal re-based to 72,768 on
   his word) and **`s295-D4`** (the regime boundary 241 → 295), enacted at `0d487352` and `ac6b3810`.
   ⚠ **What is struck is the ARITHMETIC being unowned, not the ceiling being settled:** his own word on
   72,768 is ***"possibly"***, n=1, and the next cold boot is the test. That carries forward at § OWED ⑨.
   ⛔ **This strike has no headline segment of its own in `_CARRIES.md`'s #295 set** — the #294 banner
   cap merged it away — so it is struck HERE, at the handoff level, and is recorded as such rather
   than silently skipped.
4. ⛔ **ALSO STRUCK, and it is this session's own: `_HANDOFF-145` § POST-WRAP ADDENDUM 4** — *"every
   commit now stales `_CHAIN.md` by construction"*. **CLOSED by lane I3's `check()` re-ask
   (`c896bcaf`), proven by CI survey `[120]` GREEN on a pushed tree.**

⛔ **NOT STRUCK, deliberately:** the `--push` arm's end-to-end proof (it failed for a NEW reason, and
the old reason is retired rather than the item closed) · the 98 · dream pass 13's seven and pass 9's
six (enacted, still unruled — `s183-D1` strikes a headline, never a clause) · everything on the deck.
**"A strike that is wrong is worse than an item that is merely stale."**

---

## ✅ THE COMMIT PATH

⛔★★ **THE TRUE `--wrap` PATH WAS RUN AND THE GATE ALLOWED IT — the first time in twenty-one
sessions, and the `#243` DECLARED NOT-A-WRAP form was not needed.** The exact commit sha, the gate's
closing reading, the push verdict verbatim and this wrap's own CI read are in § POST-WRAP ADDENDUM
below and in `notes/_subreports/2026-09-21-295-W-wrap.md`. **A commit cannot name itself; the gap is
declared, not hidden.**

---

## ⬛ OWED TO #296, IN ORDER

1. ⬛★★★ **THE PRESENTATION. Friday 2026-09-25 is THREE DAYS OUT and it is the internal, and he asked
   for it by name, unprompted, mid-ruling:** ***"BTW, we need to get back to the presentation
   soon"***. ⛔ **#296 OPENS ON THE DECK.** Everything #292–#294 carried on it is untouched at its true
   age: his **v12/v13 notes read done, output owed**, with the four D2 layout flags and the dark
   robots slide gated on them; **the deck's own brain is still at pass six** (v13 inlines
   `YAW0 = 10` / `PIT0 = 8` / `AOV = 12` against the drawing file's **−50 / 20 / 33** — three passes
   out of step); **the workers' finessing he ruled is to be done *"together"*, not by a lane alone**;
   the **dashboard review**, with his standing note to use *"the cold brief we will be doing the demo
   with rather than the Ai platform one"*; the design-system map accepted **as a placeholder** and not
   inscribed. Filed in Project memory: `areas/presentation-friday-25th-288.md`.
2. ⬛ **THE 98 OF 131 — DEBT, OR BIND-FORWARD?** `s295-D2`'s arm reads **131 ENACTED of 638; 33 carry a
   sha, 98 do not**, and this session's fourteen stamps moved the numerator by **zero**. Does the arm
   grow its own `_FROM_SESSION` the way both boot boundaries now do, or is the 98 a backlog to be
   paid? ⚠ **Until it is answered `ENACTED_SHA_BLOCKING` can never flip** — it would be red on 98
   records the day it was promoted. **No boundary was invented at any seat.**
3. ⬛ **`s294-D11`'s STAMP AND `s294-D12`'s THIRD STATE.** Is *"enacted"* a word about CODE or about
   the repo's SURFACES? If surfaces, D11 stamps with `359b646b`; if code, D11 stays `ruled` and **two
   filed records say eleven where the commit says ten**. `s294-D12` is enacted in code AND discharged
   in fact by his own hands — **a third state named at a THIRD consecutive session**, currently living
   in the record's status prose because there is nowhere else, **which is a workaround, not an
   answer.**
4. ⬛ **THE PUSH ARM: DOES A CONDUCTOR `PUSH` VERDICT REACH A BARE `git push`, AND DOES THE DIRT GATE
   GET AN ACK HATCH?** `PUSH_DIRT_ACK="<real reason>"`, spelled like its six neighbours. The three
   paths that refused it at three seats are `notes/_dream/_GRADE-DECISIONS.jsonl` (machine
   instrumentation, policy ⬛ **DAVE'S and unruled** — dream pass 6 P2, FLOATED),
   `notes/_lanes/293/J7-IDEA-jev-selects-over-the-kg.md` and `notes/_lanes/294/WRAP-MEMORY-HOOK.md`.
   ⚠ **`--push` is still UNPROVEN end to end — for a NEW reason; the credential reason is RETIRED.**
5. ⬛ **`_capture_gate.py --selftest`'s CI FAIL — FALSE OR TRUE, UNDIAGNOSED.** Exit 1 on *"no
   `pre-flight:` stamp"*, **byte-identical to #292's**, while the file carries **three** such stamps at
   a live seat. ⛔ **Whether the suite has a SECOND failure is UNKNOWN and is reported as unknown** —
   the survey truncates at ~150 chars and no CI step runs the selftest outside it. ⚠ **The suite
   exceeds the ~165 s sandbox call wall, so no seat can ask it whole; CI is the only asker.**
6. ⬛ **`--wrap` IS WIRED INTO NO CI STEP — THE ZERO IS INVISIBLE TO CI.** Measured at four seats.
   Neither the fails, nor their closing, nor the ceiling breach, nor its discharge has ever appeared in
   CI in either direction.
7. ⬛ **EXACT-PIN vs SHRINK-ONLY, NOW AT TWO CONSTANTS.** `_gauge_tokens` arm E pins
   `BOOT_CEILING_TK` exactly; `selftest_boot_ceiling_discharge` now pins `BOOT_CEILING_FROM_SESSION`
   exactly. **Both were re-pinned rather than loosened, and loosening was available and REFUSED at
   both seats.** ⇒ every future LEGAL shrink is a two-file edit or a red selftest. One named home that
   both read, or is the friction the point? **Two lanes have paid it.**
8. ⬛ **`s295-D1`'s LITERAL `post-mortem #N:` vs THE STRATA'S `POST-MORTEM #N —` FORM.** Five of the
   six strata it closes have no colon; the matcher was widened and **that widening is a seat's reading
   of his intent, not his text.** Amend by addition, or let the code comment carry the correction?
9. ⬛ **`s295-D3` IS n=1 AND HIS WORD IS *"POSSIBLY"*.** Nothing in the code knows 72,768 is
   provisional. Does a re-base on one sample get a confirmation clause, or is a hedge always prose
   only? **The next cold boots are the test and they have not happened.**
10. ⬛ **THE OTHER WORKER'S J7 AND ITS IDEA NOTES, STILL UNRECONCILED** and still dirty, now at their
    true age. **#296 reads them where they stand or rules them.**
11. ⬛ **THE TWO SEVENS NEED DIFFERENT WORDS** — *wrap-gate fails* and *survey fails* — before a
    session rules one closed and means the other. ⚠ **And a survey step ID is not a stable identifier:
    `[123]→[125]`, `[126]→[128]`, `[134]→[136]` this wave alone. Cite survey steps by NAME.**
12. ⬛ **`--check`'s GREEN IS A THIRD VERDICT IN PROSE BUT NOT IN ITS EXIT CODE.** *"FRESH at the
    commit it was generated at"* exits 0, the same as a true FRESH. Should a HEAD-only advance have
    its own machine-readable code the way `COULD-NOT-ASK` (77) does?
13. ⬛ **EVERYTHING IN `_CARRIES.md` § `residual → #296` THAT #295 DID NOT TOUCH**, each at its true
    age — **642 items, 13 new, 2 struck with receipts**. Fetch the section; do not read the file.
14. ⬛ **THE MEMORY HOOK IS OWED TO THE CONDUCTOR** — `notes/_lanes/295/WRAP-MEMORY-HOOK.md`, index
    line plus file body, in the `s271-D4` form. See that file's § FOR THE CONDUCTOR.

---

## ⚠ THINGS A COLD SEAT SHOULD KNOW BEFORE IT TOUCHES ANYTHING

- ⛔★★ **THE LOCK RECIPE IS NOW COMPLETE AND IT IS THREE MOVES, NOT ONE.** The trigger is **a no-op
  `git add`** — a retry's add on an already-staged path strands the index lock, **and the NEXT named
  path is then refused, so the script reports the wrong object.** `git reset -q` clears the no-op **and
  strands the two REFS locks**. ⇒ **`mv` the index lock → `git reset -q` → `mv` the two refs locks →
  run the committer ONCE with every path genuinely unstaged.** ⚠ **`git status` ALONE strands one —
  four consecutive sessions measured — and one was already stranded at the open at three separate lane
  seats before any git command ran.** ⛔ **Never `rm`: this mount refuses `unlink` (`s282-D4`), and
  git's own `warning: unable to unlink … Operation not permitted` is git failing to clean up after
  itself, not a foreign lock.** `notes/_lanes/_orphan-locks/` took **sixteen** files on 09-21 alone.
- ⛔★★ **A GATE ARM CALLED OUTSIDE ITS OWN SELECTOR IS NOT THE GATE.** `check_preflight` owns the
  branch selection and is the only arm wired into wrap mode; `check_preflight_tokens` returns fails on
  a text the gate passes. **Grade with the arm the gate actually calls.** Paid again here.
- ⛔★ **`_state.add()` REFUSES A ROW WHOSE `home` DOES NOT EXIST ON DISK** — so a report file must be
  created as a declared placeholder BEFORE its row is minted. Lane I3 met this and no report had
  recorded it. It also refuses without `closes_when`, and correctly.
- ⛔ **MINT THE DOC ROWS AND REGENERATE `_CHAIN.md` *BEFORE* THE FIRST COMMIT ATTEMPT.** Four sessions
  running. ★ **The doc-row MINT is itself what takes `_gen_chain.py --check` red** — though since
  `c896bcaf` a HEAD-only advance no longer does.
- ⛔ **`_git_commit.sh`: a MID-SESSION commit needs `SESSION_N=<n>`** (`s130-D3`), **a FRESH msgfile
  with a FRESH name** and **NO `after #N …` prefix on line 1** — the script adds it. ⛔ **The
  `_gm_move.py` OPS FILE IS A MSGFILE TOO:** unique name, session-owned path, `os.path.exists` + a size
  floor asserted **in the writing process**. Nine ops files were used at this wrap, each named for its
  step and its minute.
- ⛔★ **THE SHELL EATS BACKTICKS.** Lane I3 drove fourteen stamps through a generated shell script and
  **command substitution stripped every backticked term from all fourteen status strings** — caught by
  reading the tool's own echo, not by a gate. **Restore from the `HEAD` blob through python (`git
  checkout` cannot: this mount refuses `unlink`) and re-run through `subprocess` with an argv LIST and
  no shell.**
- ⛔ **`> **COMMIT STATE` IS NOT A UNIQUE ANCHOR** — drive `roll_2f` with `> **COMMIT STATE #<N>:**`.
- ⛔ **THE `s263-D10` RULINGS-PAGE RE-RENDER IS LOAD-BEARING, NOT AN ASIDE.** Three consecutive lanes
  found their own inscription took `notes/_RULINGS.html` stale and the rehearsal red. **Inscribe, then
  `_render_rulings.py`, then re-read the count.**
- **`pip install tiktoken --break-system-packages` FIRST** in a cold sandbox, and **again after**
  `_gate_scratch_hygiene.py --clean --wrap`, which removes `~/.local/lib`. **Sixth consecutive wrap to
  meet it.**
- **The sub-transcripts are readable from a sandbox seat** at
  `/sessions/*/mnt/.claude/projects/session/<id>.jsonl` and `…/<id>/subagents/agent-*.jsonl`. ★ **And
  the CONDUCTOR'S top-level transcript is readable from a sub seat** —
  `python3 knowledge/_checkin.py --preflight-line <N>`.
- ⚠ **THE BANNER CAP CLOSED AT EXACTLY 1,200 OF 1,200 FOR A THIRD CONSECUTIVE WRAP**, after seven
  shortening passes from a 1,633-tape draft. **Three things were compressed out and each is named in
  the #295 stratum** so nothing is silently lost. ⇒ **#292's, #293's and #294's complaint is literal
  for a fourth session: the cap has stopped constraining girth and started shaping the record.**
  Reported, not appealed — **the cap is Dave's.**
- ⚠ **THE NINTH CONSECUTIVE `consult-receipts: none`**, and the reason is unchanged: every artefact
  this ritual needed was named BY PATH in the brief. ⇒ **a retrieval surface a fully-briefed seat never
  needs has exactly one consumer, the cold opener, and nothing measures whether it is used there.**
  [[instrument-without-a-consumer]]
- ⚠ **THE `size:` STAMP IS GENERATED AND YOU MAY NOT HAND-TYPE ITS FIGURES** (`s294-D8`). Its second
  live use is at this wrap and **it did not refuse**.

---

*Filed report: `notes/_subreports/2026-09-21-295-W-wrap.md`. Dossier:
`_DECISION-HISTORY/2026-09-21-295-the-push-landed-and-the-wrap-gate-reached-zero.md`. Memory hook:
`notes/_lanes/295/WRAP-MEMORY-HOOK.md`. His words:
`notes/_lanes/295/DAVE-RULINGS-2026-09-21.md`. Lane reports:
`notes/_subreports/2026-09-21-295-I-inscribe-and-enact.md` ·
`notes/_subreports/2026-09-21-295-P-push-and-ci.md` ·
`notes/_subreports/2026-09-21-295-I2-ceiling-rebase.md` ·
`notes/_subreports/2026-09-21-295-I3-regime-status-writer-chain-check.md`.*
